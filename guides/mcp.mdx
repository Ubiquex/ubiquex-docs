---
title: "ubx + your AI assistant"
description: "Connect Claude Code or Claude Desktop to ubx's ledger over MCP"
---

`ubx mcp` serves three of ubx's own read-only tools over the [Model Context
Protocol](https://modelcontextprotocol.io) (MCP), so an AI assistant — Claude
Code, Claude Desktop, any MCP-speaking client — can ask ubx questions
directly in conversation: "who changed this bucket and when," without you
first having to look up `ubx why`'s own argument shape.

<Note>
  One binary, not a second executable. `ubx mcp` is a subcommand like every
  other `ubx` verb — see [`ubx mcp --help`](#reference) for the full text.
</Note>

## The three tools

Each is a thin wrapper over the exact JSON payload the equivalent `--json`
CLI command already produces (see [Drift](/concepts/drift),
[`--json` output](/cli/scan#json-output)) — never a different API, never a
different JSON shape.

| Tool | Answers | Mirrors |
| --- | --- | --- |
| `ubx_why` | "Who changed this, when, and why?" / "What's this resource's full history?" | [`ubx why`](/cli/why) |
| `ubx_status` | "What does ubx track?" / "Has anything drifted?" | [`ubx status`](/cli/status) |
| `ubx_scan` | "What does this one resource's live state look like right now, compared to the ledger?" | [`ubx scan`](/cli/scan) |

## Boundary by omission

`ubx accept`, `ubx propose`'s ship path, `ubx writeback`, `ubx
revert-plan`'s application, and `ubx scan --surface-as` (which opens a real
GitHub issue/PR) are **not** exposed as tools — deliberately, not an
oversight. Accepting a proposal is a recorded human (or PR-merge-derived)
decision; it is never something an assistant does on your behalf
mid-conversation. This server surfaces information. It never signs
anything, never writes to a live resource, and never appends to the ledger
— `ubx_scan` makes a real read against live infrastructure (the one tool
capable of a real network call), but a read is all it ever does.

## Connecting a client

Both clients launch `ubx mcp` as a subprocess over stdio — set `cwd` to a
real ledger checkout so `.ubx/config` discovery (UBI-19) finds the same
defaults a human sitting in that directory would.

### Claude Code

```bash
claude mcp add ubx -- ubx mcp
```

Run this from inside your ledger checkout — `claude mcp add`'s default
scope (`local`, this project only) launches the server from wherever you
ran the command, so `.ubx/config` is discovered the same way any other
`ubx` invocation from that directory would find it. See `claude mcp add
--help` for the `user`/`project` scope alternatives.

### Claude Desktop

Add to your `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "ubx": {
      "command": "ubx",
      "args": ["mcp"],
      "cwd": "/path/to/your/ledger"
    }
  }
}
```

Restart Claude Desktop after saving. `ubx` must already be on `PATH` (or
give `command` an absolute path) — see [Installation](/getting-started/installation).

## A real transcript

This is real output — captured against ubx's own live-verification ledger
(a real S3 bucket, tagged and untagged for real to produce it), not
hand-written. Asked "who changed the `ubx-states` bucket, and when":

```json
{
  "tool": "ubx_why",
  "arguments": { "query": "payments.aws_s3_bucket.ubx-states" }
}
```

```json
{
  "chain": [
    {
      "id": "71ebf37af50841a3de999d8584823425892f27e0866c237d4d1abae9a0386c10",
      "kind": "drift_adopt",
      "status": "accepted",
      "intent": {
        "summary": "record drift on payments.aws_s3_bucket.ubx-states observed outside the ledger",
        "sources": [
          {
            "kind": "cloudtrail",
            "event_name": "DeleteBucketTagging",
            "event_time": "2026-07-16T21:54:58Z",
            "actor_arn": "arn:aws:iam::839333509514:user/roozbeh",
            "source_ip": "93.228.76.41"
          },
          {
            "kind": "cloudtrail",
            "event_name": "PutBucketTagging",
            "event_time": "2026-07-16T21:52:51Z",
            "actor_arn": "arn:aws:iam::839333509514:user/roozbeh",
            "source_ip": "93.228.76.41"
          }
        ]
      },
      "delta": {
        "modifies": [
          {
            "target": { "stack": "payments", "type": "aws_s3_bucket", "name": "ubx-states" },
            "before": { "tags.hotfix": "incident-412", "tags_all.hotfix": "incident-412" },
            "after": { "tags.purpose": "ubx-mcp-live-verify", "tags_all.purpose": "ubx-mcp-live-verify" }
          }
        ]
      }
    },
    {
      "id": "6398e90de690f96f2c00cb43719ddd2d7a68e50d9a67ee46297639d803cfd77a",
      "kind": "drift_adopt",
      "status": "accepted",
      "intent": {
        "summary": "record drift on payments.aws_s3_bucket.ubx-states observed outside the ledger",
        "sources": [
          { "kind": "cloudtrail_unattributed", "reason": "delivery_window" }
        ]
      }
    },
    {
      "id": "80b64f5109daf36cabd977c72c8c2ecc6cade32bd0511b3b2e01944e9714fcce",
      "kind": "adoption",
      "status": "accepted",
      "intent": { "summary": "adopt existing payments.aws_s3_bucket.ubx-states into the ledger (discovered by scan)" }
    }
  ],
  "format": 1
}
```

Real attribution, both directions: the newest entry names the real IAM
identity, event, and timestamp responsible (`ubx`'s own account, since this
mutation was made to produce this transcript); the middle entry is an
**honestly unattributed** drift from an earlier session — CloudTrail hadn't
delivered a matching event within the search window yet
(`delivery_window`), recorded as evidence in its own right rather than
guessed at or silently dropped. See [Attribution](/concepts/attribution)
for what each `cloudtrail_unattributed` reason means.

<Note>
  The full real response lists every matching CloudTrail event for the
  drift's search window (24 in this case — this bucket has a long history
  of live-verification tag mutations across many sessions), newest first.
  Trimmed here to the two most recent for readability; nothing about the
  mechanism trims it for a real client.
</Note>

## Reference

```text
$ ubx mcp --help
```

```text
Serves the Model Context Protocol (MCP) over stdio, so an AI assistant (Claude Code, Claude
Desktop, any MCP client) can ask ubx questions directly in conversation -- "who changed this bucket and
when" -- without the human already needing to know ubx's own command shapes.

Three read-only tools, each a thin wrapper over the exact JSON payload the equivalent --json CLI command
already produces:

  ubx_why     resource address or proposal ID -> its recorded history / one decision's full receipt
  ubx_status  optional stack filter, optional live-state drift check -> fleet report
  ubx_scan    single resource -> new/drifted/unchanged classification + the generated proposal, inline

Boundary by omission: ubx accept/ship/writeback/revert-plan (and scan --surface-as, which opens a real
GitHub issue/PR) are NOT exposed here, deliberately. Accepting a proposal is a recorded human (or
PR-merge-derived) decision -- never something an assistant does on a human's behalf mid-conversation.
This server surfaces information; it never signs anything, never writes to a live resource, and never
appends to the ledger.

Configuration discovers .ubx/config from this process's own working directory, exactly like every other
ubx command -- point your MCP client's "cwd" at a real ledger checkout to get the same defaults a human
sitting there would (see ubx config).

Usage:
  ubx mcp [flags]

Flags:
  -h, --help   help for mcp
```

## Related

- [`ubx why`](/cli/why), [`ubx status`](/cli/status), [`ubx scan`](/cli/scan) — the CLI commands each tool mirrors
- [Attribution](/concepts/attribution) — what a `cloudtrail`/`cloudtrail_unattributed` source means
- [Secrets](/concepts/secrets) — why a sensitive attribute is never returned as real material, over MCP or anywhere else
- [`.ubx/config`](/cli/config) — the same discovery `ubx mcp` uses from its own working directory
