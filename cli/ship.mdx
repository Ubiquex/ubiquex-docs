---
title: "ubx ship"
description: "Accept (local tier, if needed) and execute a drift_revert or change proposal against live cloud -- the only command that applies"
---

```text
ubx ship [hash] [flags]
```

Executes a [`drift_revert`](/concepts/drift) or `change` ([`ubx
resolve`](/cli/resolve), or [`ubx plan`](/cli/plan)'s fused equivalent)
proposal: for a `drift_revert`, restores the resource's live state to
match the ledger's recorded truth; for a `change`, creates, modifies, and
destroys resources for real, in one combined dependency order
(creates/modifies forward, destroys reversed), feeding each resource's
real applied output into any sibling still carrying a `$computed` marker
pointing at it. **This is the one `ubx` command that changes real
infrastructure** — [`ubx accept`](/cli/accept), [`ubx why`](/cli/why),
[`ubx status`](/cli/status), [`ubx scan`](/cli/scan),
[`ubx resolve`](/cli/resolve), [`ubx plan`](/cli/plan), and
[`ubx revert-plan`](/cli/revert-plan) only ever read or record. `ubx
revert-plan` still exists and still never applies anything itself — it's
what you reach for pre-acceptance, or if you'd rather apply the
correction through your own tooling; `ubx ship` is what actually does it.

Only `drift_revert` and `change` proposals are actually *executed*.
`adoption`/`drift_adopt` are record-only — accepting one already is the
whole action, there's nothing left to run against a real provider. `ubx
ship` still accepts these the exact same way (the same inline-accept-then-
confirm flow, or the same already-accepted-in-the-ledger path) — it just
reports success once acceptance lands, rather than ever reaching for a
provider at all. See [Shipping a record-only proposal](#shipping-a-record-only-proposal-adoption-drift_adopt),
below.

## `[hash]` is optional, and looked up two ways

**Bare `ubx ship`** (UBI-62) resolves to the most recent unshipped plan
for the resolved stack (`--stack`, or config's own default) — shown
explicitly before anything happens, never silently guessed. If plans for
more than one stack exist and `--stack` wasn't given, nothing is guessed
between stacks either: a TTY is prompted to choose, a non-TTY is refused
with the same list as a teaching error. See [Bare `ubx ship`](#bare-ubx-ship-the-latest-plan-for-your-stack)
below.

Given or resolved, `[hash]` is then looked up two ways:

1. **An already-accepted proposal id** in this stack's ledger — the
   four-verb ceremony's own path: [`ubx accept`](/cli/accept) (local or
   [`--from-merge`](/cli/accept#pr-merge-acceptance)) ran separately,
   before this command. Applied immediately, no further confirmation,
   since that acceptance already was the consent moment.
2. **A plan [`ubx plan`](/cli/plan) saved** at `.ubx/plans/<hash>.json`,
   if no accepted proposal by that id exists. For **this** path only, a
   one-line summary (stack, plan age, blast radius) renders and a typed
   `yes` is required before anything is accepted or applied — see
   [Interactive confirmation](#interactive-confirmation-the-signing-moment)
   below.

## Flags

| Flag | Description |
| --- | --- |
| `--ledger-dir string` | root directory containing `ledger/` and `.ubx/` (default `.`); unused if `.ubx/config`'s `[ledger]` names a remote store |
| `--stack string` | which stack's ledger to open — required only when `.ubx/config`'s `[ledger]` `store` is a remote store (a bare hash carries no stack of its own); also which stack's plans are considered for a bare `ubx ship` with no hash |
| `--confirm-destroys` | required for [inline acceptance](#interactive-confirmation-the-signing-moment) of any plan with `blast_radius.destroys > 0`; unused when `<hash>` is already an accepted proposal, since that confirmation already happened at its own accept time |
| `--yes` | skip the interactive "type yes" confirmation for inline local-tier acceptance (for CI/scripts) — the receipt still renders; required on a non-TTY, which never prompts |
| `--full-hashes` | render every hash in full instead of the default 12-char short form |
| `--provider string` | path to the provider binary (mutually exclusive with `--source`) |
| `--source string` | provider source address, e.g. `hashicorp/aws` (mutually exclusive with `--provider`; requires `--provider-version`) |
| `--provider-version string` | explicit provider version to acquire (required with `--source`) |
| `--provider-config string` | JSON object configuring the provider, e.g. `{"region":"us-east-1"}` (default `{}`) |
| `--timeout duration` | overall timeout for the ship run (default `5m0s`) |
| `--json` | emit one JSON document instead of human text — see [`--json` output](#json-output) |
| `-h`, `--help` | help for ship |

## Bare `ubx ship`: the latest plan for your stack

No hash at all resolves to the most recent unshipped plan for the
resolved stack — shown explicitly, then run through the identical
confirmation flow below. With only one stack's plans sitting in
`.ubx/plans/`, `--stack` isn't even required — resolution is unambiguous
and says so:

```text
$ ubx ship --provider ./fakeprovider --ledger-dir . --yes
latest plan for stack playground: 21aea70a8ff6  change  7s old
Ship  playground · 7s old · +1 create(s) ~0 modify(ies) -0 destroy(s)
accepted 21aea70a8ff6… (stack playground) via local plan
playground.fake_widget.q1
  ✓ applied                                            0:00
✓ applied: playground.fake_widget.q1
1 resource(s), 1 applied, 0 failed, 0 still unknown -- outcome: applied
```

`.ubx/plans/` is not stack-namespaced — it's a flat, content-hash-keyed
store shared by every stack that uses this ledger dir (the common case
for the default git-local store), so plans for more than one stack
sitting there at once is ordinary, not an edge case. Nothing is guessed
between them:

```text
$ ubx ship --provider ./fakeprovider --ledger-dir .
multiple stacks have unshipped plans -- pass --stack, or choose one:
  [1] 1444f917246a  payments  change  0s old
  [2] 29508ca55893  networking  change  0s old
ship: 2 plans span more than one stack -- pass --stack or an explicit hash (non-interactive, refusing to guess)
```

A real terminal gets a prompt instead of the refusal (`Ship which plan?
[1-2]: `). Naming `--stack` resolves the ambiguity the same way, and
still goes through the identical confirmation flow — `--stack` narrows
which plan is picked, it doesn't skip the "renders, then asks" step:

```text
$ ubx ship --provider ./fakeprovider --ledger-dir . --stack payments < /dev/null
latest plan for stack payments: 1444f917246a  change  0s old
Ship  payments · 0s old · +1 create(s) ~0 modify(ies) -0 destroy(s)
ship: refusing to apply without confirmation: not an interactive terminal -- pass --yes to confirm non-interactively (e.g. in CI/scripts)
```

A shipped or accepted plan is pruned from `.ubx/plans/` so "latest"
never re-offers it either way it was consumed.

## Interactive confirmation: the signing moment

When `[hash]` isn't yet an accepted proposal, `ubx ship` looks for a plan
[`ubx plan`](/cli/plan) saved locally and — on a real terminal, without
`--yes` — requires a typed `yes` (terraform's own consent pattern, not
`y`/`n`) before `core.Accept` ever runs. The prompt **is** the
local-tier signing moment. It does **not** re-render the full receipt
`ubx plan` already showed — that already was the review; a one-line
summary (stack, plan age, blast radius) is enough to confirm this is
still the plan being signed, without repeating every resource and
attribute a human already read (a real multi-resource receipt can run
to pages of formatted JSON under [the v2 receipt format](/cli/plan#plan-receipt-format-exact-from-the-founders-markup-resolved)):

```text
$ ubx plan intent.json --provider ./fakeprovider --ledger-dir .
Plan  sandbox · from intent.json

  + fake_widget.primary create
      name: "primary-widget"
      tags:
      {
        "env": "sandbox"
      }

  + fake_widget.mirror create
      name: "primary-widget"
      tags:
      {
        "peer_id": $ref:sandbox.fake_widget.primary.id
      }

delta: +2 create(s), ~0 modify(ies), -0 destroy(s)

blast radius: +2 ~0 -0
cost delta: $0/mo

ubx-proposal: 0daf78eb087e…
next: ubx ship 0daf78eb087e

$ ubx ship 0daf78eb087e --provider ./fakeprovider --ledger-dir .
Ship  sandbox · 0s old · +2 create(s) ~0 modify(ies) -0 destroy(s)

Ship this to sandbox? Only 'yes' accepted: yes
accepted 0daf78eb087e… (stack sandbox) via local plan
sandbox.fake_widget.primary
  ✓ applied                                            0:00

sandbox.fake_widget.mirror
  ✓ applied                                            0:00
✓ applied: sandbox.fake_widget.primary
✓ applied: sandbox.fake_widget.mirror
2 resource(s), 2 applied, 0 failed, 0 still unknown -- outcome: applied
```

Each resource's own progress block is followed by a blank line, so a
multi-resource ship reads as clearly separated blocks rather than every
resource's transitions running together.

Typing anything else — or `--yes` never given on a non-interactive
stdin — aborts cleanly, nothing accepted or applied, exit `0` (a
deliberate abort is not a finding or an error):

```text
$ ubx ship 0daf78eb087e --provider ./fakeprovider --ledger-dir .
...
Ship this to sandbox? Only 'yes' accepted: nope
ship aborted -- nothing accepted or applied
```

`--yes` skips the prompt for CI/scripts, but still renders the one-line
summary first — and a non-TTY without it refuses outright rather than
hang reading a stdin nobody's watching:

```text
$ ubx ship 0daf78eb087e --provider ./fakeprovider --ledger-dir . < /dev/null
...
ship: refusing to apply without confirmation: not an interactive terminal -- pass --yes to confirm non-interactively (e.g. in CI/scripts)
```

[`ubx why`](/cli/why) shows exactly the same `acceptance.method: "local"`
an ordinary `ubx accept` would have recorded — the prompt is a UX
addition, not a new acceptance tier. Its own apply-history transitions
still name every real state (`pending`/`in_flight`/`applied`) in full —
that's the permanent historical record, unrelated to `ubx ship`'s own
live progress narration, which is display-only and never touches what
gets recorded:

```text
$ ubx why 0daf78eb087ef1d6918887d8a7b66d6920c55448fdc6c7b45e2316ee5fde55e9 --ledger-dir .
proposal 0daf78eb087e… (change)
stack:  sandbox
status: accepted
intent: two widgets, one referencing the other
accepted by [roozbeh] via local at 2026-08-01T20:58:16Z
blast radius: +2 ~0 -0
+ fake_widget.primary create
    name: "primary-widget"
    tags:
    {
      "env": "sandbox"
    }

+ fake_widget.mirror create
    name: "primary-widget"
    tags:
    {
      "peer_id": $ref:sandbox.fake_widget.primary.id
    }
apply history:
  attempt 1: outcome=applied
    ...
```

A consumed plan (accepted, whether shipped cleanly afterward or not) is
pruned from `.ubx/plans/` — [bare `ubx ship`](#bare-ubx-ship-the-latest-plan-for-your-stack)
never re-offers it.

### Destroys require confirmation, even inline

`--confirm-destroys` is the exact same [acceptance-time friction
invariant](/cli/accept#confirming-a-destroy) standalone `ubx accept`
enforces — inline acceptance doesn't relax it, and it's checked BEFORE
the interactive prompt even renders (no point asking a human to type
`yes` for something that will be refused regardless):

```text
$ ubx plan destroy-intent.json --provider ./fakeprovider --ledger-dir .
Plan  sandbox

  - destroy: sandbox.fake_widget.mirror
    id: "mirror-widget"
    name: null
    tags: {}

delta: +0 create(s), ~0 modify(ies), -1 destroy(s)

blast radius: +0 ~0 -1
cost delta: $0/mo

ubx-proposal: 8bf90696be84…
next: ubx ship 8bf90696be84

$ ubx ship 8bf90696be84 --provider ./fakeprovider --ledger-dir . --yes
ship: this proposal has blast_radius.destroys > 0 -- pass --confirm-destroys to accept it
```

With `--confirm-destroys` (and `--yes`, since this transcript is
non-interactive), the destroy proceeds — including the mandatory
read-back narration every destroy goes through (see [Shipping a
destroy](#shipping-a-destroy) below). `pending`/`in_flight` never get
their own line (sub-second scaffolding, not worth a human's attention on
the happy path) — the read-back verification is the one line that
matters, and it's the one line shown:

```text
$ ubx ship 8bf90696be84 --provider ./fakeprovider --ledger-dir . --confirm-destroys --yes
Ship  sandbox · 6s old · +0 create(s) ~0 modify(ies) -1 destroy(s)
accepted 8bf90696be84… (stack sandbox) via local plan
sandbox.fake_widget.mirror
  ⠧ provider reported success -- verifying via read-back (attempt 1/10) 0:00
  ✓ applied · confirmed by reconciliation              0:00
✓ applied: sandbox.fake_widget.mirror
1 resource(s), 1 applied, 0 failed, 0 still unknown -- outcome: applied
```

A stale cross-stack pin ([`resolver.VerifyPins`](/cli/resolve#cross-stack-references))
refuses inline acceptance the exact same way it refuses standalone `ubx
accept` — the neighbor ledger moving between `ubx plan` and `ubx ship` is
caught, not silently accepted against stale state; this check (and the
plan file's own hash-integrity check, next) both run before the
confirmation step too.

### The plan file's own integrity

A plan file is always written at exactly its own content hash's path.
`ubx ship` recomputes that hash on read and refuses a mismatch — a
hand-edited or corrupted plan file is never silently shipped under a
hash that no longer describes its actual content:

```text
$ ubx ship f8e95de5e90c --provider ./fakeprovider --ledger-dir . --yes
ship: plan file at .ubx/plans/f8e95de5e90ccc94ade76dacda9fbf1d50aadb9cce2d76c246d24e6dd4cfe2df.json hashes to 2ca90e90af814182c8d100c2ba27733670f629e637bf74bcf2bd7fc9b2f2ebe8, not f8e95de5e90ccc94ade76dacda9fbf1d50aadb9cce2d76c246d24e6dd4cfe2df -- stale or corrupted plan file
```

<Note>
  Every transcript on this page is the plain/`NO_COLOR` variant, for
  transcript stability — a real terminal colors these receipts (green
  creates, yellow modifies, red destroys, blue hashes) and shows the same
  content. `--json` is untouched either way.
</Note>

<Note>
  PR-merge acceptance remains available as its own separate path
  ([`ubx accept --from-merge`](/cli/accept#pr-merge-acceptance)) for teams
  who want it — inline acceptance here is local tier only, never a
  substitute for it. See [When to use the four-verb ceremony
  instead](/cli/plan#when-to-use-the-four-verb-ceremony-instead) for the
  full comparison.
</Note>

<Note>
  If `.ubx/config`'s `[ledger]` table names a remote store,
  `--stack` becomes required — a bare proposal id carries no stack of
  its own to derive the store's address from, unlike `ubx resolve`
  (which reads it from the intent file) or `ubx accept` (from the
  proposal file). See [Ledger stores](/concepts/ledger-stores) for why.

  ```text
  $ ubx ship 3ff1769e2c4d2cadc8e492cce9ae5b465b9263b3f39a5aae946617ae8fc028c0 --provider ./fakeprovider
  ship: --stack is required to open a remote ledger store (.ubx/config's [ledger] store is "s3://ubx-states/acme/prod/", not git) -- a remote store's own per-stack chain means opening one at all requires knowing which stack first
  ```
</Note>

## Shipping a record-only proposal (adoption/drift_adopt)

An `adoption` (a brand-new resource [`ubx scan`](/cli/scan) has never seen
before) or a `drift_adopt` (recording a real drift as the new signed
truth) resolve their *entire* action at acceptance — there's nothing left
to execute against a real provider. `ubx ship` still accepts these
through the exact same inline-accept-then-confirm flow as any other
plan-store hash; it just reports success the moment acceptance lands,
instead of the confusing "ship only executes drift_revert or change
proposals" a `ubx ship` on one of these used to print — after already
accepting it, making a real success look like a failure:

```text
$ ubx scan --provider ./fakeprovider --stack payments --type fake_widget --name main-cache --lookup '{"name":"main-cache"}' --ledger-dir . --no-attribution
New resource found  payments.fake_widget.main-cache

  adoption      9ed8d1b720b0     record-only · adopts into the ledger

  saved to plan store            next: ubx ship 9ed8d1b720b0

$ ubx ship 9ed8d1b720b0 --provider ./fakeprovider --ledger-dir . --yes
Ship  payments · 6s old · +0 create(s) ~0 modify(ies) -0 destroy(s)
accepted 9ed8d1b720b0… (stack payments) via local plan
9ed8d1b720b0… (adoption) -- record-only, nothing to execute -- ✓ resolved
```

Real, unedited output. The interactive confirmation, `--yes`, and
`--confirm-destroys` double-consent for a destroy-carrying plan all still
apply identically before this point is ever reached — a record-only
proposal is never treated as a shortcut around consent, only around
*execution*, which it never needed in the first place. An **already**
accepted `adoption`/`drift_adopt` (found directly via its own ledger id,
not the plan-store fallback) reports the identical success line, since
the kind check runs the same way regardless of which of `ubx ship`'s two
lookup paths found it.

### A warning before accepting a recent, unattributed drift_adopt

A `drift_adopt`'s own attribution is a one-shot: [`ubx scan
--propose`](/cli/scan)'s own CloudTrail lookback either finds a real
actor or doesn't, and whatever it recorded becomes permanent the moment
this proposal is accepted — [`ubx blame`](/cli/blame)/[`ubx
why`](/cli/why) only ever replay it, never re-attempt it later. If the
drift being adopted is very recent (under 10 minutes since it was
resolved) and came back unattributed, `ubx ship` warns before accepting
it — CloudTrail's own delivery window is typically 2-5 minutes, so a
re-scan shortly afterward may still attribute it for real:

```text
$ ubx ship 0992e9f7459e --provider ./fakeprovider --ledger-dir . --yes
Ship  payments · 6s old · +0 create(s) ~0 modify(ies) -0 destroy(s)
warning: this drift is 6s old and has no recorded attribution (too recent for CloudTrail to have delivered a matching event yet) -- accepting now records it as unattributed permanently; consider re-scanning in a few minutes first

accepted 0992e9f7459e… (stack payments) via local plan
0992e9f7459e… (drift_adopt) -- record-only, nothing to execute -- ✓ resolved
```

Real, unedited output — advisory only, never blocking: the plan still
ships with `--yes` (or a typed `yes` at the interactive prompt) right
after the warning. Once the drift is 10 minutes old or older, or already
carries real attribution, the warning simply doesn't print at all — see
[`ubx scan`'s own attribution section](/cli/scan#attribution-when-it-comes-back-empty)
for the display-only half of this same fix.

## Example: a clean apply

```text
$ ubx ship fe4267f596bd34b2c8dc4eabe2df5e95deafd51368700e2716920317ca73d894 --provider ./fakeprovider
payments.fake_widget.api-cache
  ✓ applied                                            0:00
✓ applied: payments.fake_widget.api-cache
1 resource(s), 1 applied, 0 failed, 0 still unknown -- outcome: applied
```

Each resource's live transitions narrate as they happen (UBI-61) — not
buffered to the end — with elapsed time since that resource's own most
recent transition into a live phase. `pending`/`in_flight` never get their
own line (UBI-70): they're sub-second scaffolding on the happy path, not
something worth a human's attention.

<Note>
  **The elapsed time actually ticks (UBI-63/UBI-70).** A real call against
  a real, slower provider (or a read-back verification loop that needs
  several attempts, each with real backoff between them) can take a while
  with nothing new to report. On a real terminal, the raw provider-call
  wait shows one live "shipping" line, and the read-back verification wait
  shows one live "verifying via read-back (attempt N/M)" line — both
  re-rendered roughly once a second, in place (a carriage return), rather
  than sitting frozen until the eventual terminal event finally arrives
  with the real total. Errors along the way still get their own permanent
  line — the noise cut is about the happy path repeating itself, never
  about hiding a real problem. Real, unedited transcript — a destroy
  whose read-back keeps finding the resource present through the full
  10-attempt retry budget (`FAKEPROVIDER_APPLY_MODE` unset, the fixture's
  own default "provider claims success but doesn't actually delete
  anything" mode, the non-TTY/piped capture every transcript on this page
  uses — each real attempt still gets its own discrete line, in-place
  overwrite being a terminal-only concept a log file has no use for):

  ```text
  payments.fake_widget.ci-notifications
    ⠧ provider reported success -- verifying via read-back (attempt 1/10) 0:00
    ⠧ provider reported success -- verifying via read-back (attempt 2/10) 0:00
    ⠧ provider reported success -- verifying via read-back (attempt 3/10) 0:00
    ⠧ provider reported success -- verifying via read-back (attempt 4/10) 0:00
    ⠧ provider reported success -- verifying via read-back (attempt 5/10) 0:01
    ⠧ provider reported success -- verifying via read-back (attempt 6/10) 0:03
    ⠧ provider reported success -- verifying via read-back (attempt 7/10) 0:08
    ⠧ provider reported success -- verifying via read-back (attempt 8/10) 0:18
    ⠧ provider reported success -- verifying via read-back (attempt 9/10) 0:33
    ⠧ provider reported success -- verifying via read-back (attempt 10/10) 0:48
    ! the provider reported a successful destroy, but a post-destroy read-back found the resource still present after the full retry budget -- the delete never actually happened
    ✗ failed · provider reported success but a post-destroy read-back found the resource still present 0:48
  ```

  On a real terminal, a slow raw provider call or a between-attempts wait
  during verification instead shows ONE continuously-updating line each,
  never a flood of near-duplicate lines — see [Shipping a
  destroy](#shipping-a-destroy) below for what this exact scenario's own
  full ship report looks like.
</Note>

## Example: idempotent re-run

Every resource already `applied` in a prior attempt is skipped — re-running
`ubx ship` against a proposal that already fully shipped is a genuine no-op,
not an empty response:

```text
$ ubx ship 8fa7a1498d41886a0f68138539d05876deee940a94edbac912c947d2529de56f --provider ./terraform-provider-aws
8fa7a1498d41886a0f68138539d05876deee940a94edbac912c947d2529de56f: already fully applied -- nothing to do
```

This is exit `0`, and writes no new [apply record](/concepts/apply-record)
at all — see [Idempotency](/concepts/apply-record#idempotency-safe-to-re-run-any-number-of-times).

## Example: a provider rejects the change

A real, structured error from the provider (not a timeout or transport
failure) ends that resource's attempt immediately — never silently retried:

```text
$ ubx ship 66da902facb213b8a30fc26d1dfb6ad0b79777eb4c4f48d94c36c095b25f886a --provider ./fakeprovider
payments.fake_widget.api-cache
  ! provider returned 1 diagnostic error(s): invalid attribute value: fakeprovider: simulated terminal diagnostic
  ✗ failed                                             0:00
✗ failed: payments.fake_widget.api-cache
  terminal: provider returned 1 diagnostic error(s): invalid attribute value: fakeprovider: simulated terminal diagnostic
1 resource(s), 0 applied, 1 failed, 0 still unknown -- outcome: failed
```

The error gets its own line the moment it happens (UBI-70) — not just at
the very end, once every other resource in the whole ship run has also
finished. It's the same underlying message `ubx ship`'s own trailing
per-resource report (`terminal: ...`) already showed; now it's visible
live too.

## Example: a redacted restore target is declined, not applied

A `drift_revert` whose restore value is itself a [`$redacted`](/concepts/secrets)
marker — the ledger holds a salted hash, never the real material — is
declined outright. `ubx ship` will never construct a live apply from it, on
this run or any future one; the message points at `ubx revert-plan`'s own
manual-reconciliation path:

```text
$ ubx ship 59273c05330df824626f425cc9760f53cf8783367784fa4622688be469dec9ee --provider ./fakeprovider
payments.fake_widget.secret-box
  ! declined: after value(s) at master_password are redacted -- the ledger holds a salted hash, not the real secret material, and ubx will never construct a live apply from it; use `ubx revert-plan` for this resource's manual reconciliation steps instead
✓ pending: payments.fake_widget.secret-box
  terminal: declined: after value(s) at master_password are redacted -- the ledger holds a salted hash, not the real secret material, and ubx will never construct a live apply from it; use `ubx revert-plan` for this resource's manual reconciliation steps instead
1 resource(s), 0 applied, 1 failed, 0 still unknown -- outcome: failed
```

This is permanent, not transient: the same proposal declines identically on
every future `ubx ship` re-run, since the redacted value never changes. The
only way past it is restoring the real value out-of-band and recording the
correction through a fresh `ubx scan`/`ubx accept` cycle.

## Shipping a `change` proposal: a real create chain

A `change` proposal ([`ubx resolve`](/cli/resolve)'s own output) is shipped
the same way — creates and modifies together, in real dependency order.
Here `api-cache-mirror` cross-references `api-cache`'s `name` (concrete)
and its schema-`Computed` `id` (left as `$computed` at resolve time,
substituted with the real applied value mid-walk):

```text
$ ubx ship 7972315f4b246337388616c97a04e67947e0ab85e3c815ea7ab1d5d7fa0cf800 --provider ./fakeprovider
payments.fake_widget.api-cache
  ✓ applied                                            0:00

payments.fake_widget.api-cache-mirror
  ✓ applied                                            0:00
✓ applied: payments.fake_widget.api-cache
✓ applied: payments.fake_widget.api-cache-mirror
2 resource(s), 2 applied, 0 failed, 0 still unknown -- outcome: applied
```

`api-cache` ships first (nothing depends on it being ordered any other
way); `api-cache-mirror` is never even attempted until `api-cache` reaches
`applied` — [`ubx why`](/cli/why) shows both, including the full
`pending`/`in_flight`/`applied` transition history that's the permanent
record (`ubx ship`'s own live narration above is a display-only summary of
this same history, not a second source of truth — see [the noise-cut
note](#example-a-clean-apply) above for why `pending`/`in_flight` don't
get their own line there):

```text
$ ubx why 7972315f4b246337388616c97a04e67947e0ab85e3c815ea7ab1d5d7fa0cf800
apply history:
  attempt 1: outcome=applied
    payments.fake_widget.api-cache:
      pending at 2026-08-01T21:04:24Z
      in_flight at 2026-08-01T21:04:24Z
      applied at 2026-08-01T21:04:24Z
    payments.fake_widget.api-cache-mirror:
      pending at 2026-08-01T21:04:24Z
      in_flight at 2026-08-01T21:04:24Z
      applied at 2026-08-01T21:04:24Z
```

<Note>
  Live-verified against real AWS, not just the fixture above: a real
  two-resource create chain (`aws_sqs_queue` + `aws_sqs_queue_policy`,
  the policy's `queue_url` only known once the queue itself exists),
  shipped for real, killed with a real `kill -9` between the queue
  applying and the policy starting, and correctly recovered on re-run —
  the dependent completed using the queue's REAL recorded output, never
  re-creating the queue or losing the value. See
  docs/reliability-report.md's own UBI-27 section (in the `ubiquex`
  repo) for the full transcripts, including a real bug this work found
  and fixed along the way (a create never called the provider's
  `Configure` step, since unlike a modify it never goes through a read
  first — surfaced against a real provider as a bare transport error, not
  a clean configuration failure).
</Note>

A resource whose dependency hasn't applied yet is never attempted —
blocked with a named reason, not silently skipped or guessed at:

```text
$ ubx ship b2d5f5d1c7f5b6d15ebbadcfea64ee96b77af41008f4022c03c678671124bc51 --provider ./fakeprovider
payments.fake_widget.primary
  ! provider returned 1 diagnostic error(s): invalid attribute value: fakeprovider: simulated terminal diagnostic
  ✗ failed                                             0:00

payments.fake_widget.mirror
  ! blocked: dependency payments.fake_widget.primary has not applied -- refusing to ship a resource ahead of what it depends on
✗ failed: payments.fake_widget.primary
  terminal: provider returned 1 diagnostic error(s): invalid attribute value: fakeprovider: simulated terminal diagnostic
✓ pending: payments.fake_widget.mirror
  terminal: blocked: dependency payments.fake_widget.primary has not applied -- refusing to ship a resource ahead of what it depends on
2 resource(s), 0 applied, 2 failed, 0 still unknown -- outcome: failed
```

<Note>
  `mirror`'s own line leads green (`✓ pending:`) even though it counts
  toward the `2 failed` outcome — the glyph reflects `LastState()`
  (`pending`, since a blocked resource never transitions to `failed`
  itself), not the overall outcome. A pre-existing asymmetry, not
  something this session's color pass introduced or papered over; worth
  a future look; see STATE.md.
</Note>

Killed and re-run, a `change` proposal's already-applied resources are
recognized exactly like a `drift_revert`'s (idempotency, above) — with one
addition: a still-pending dependent recovers its dependency's real applied
output from the ledger's own history, not just from what the current
`ubx ship` invocation itself computed, so a kill between two resources
never loses the value the second one needs.

<Note>
  A `partially_applied` outcome (some resources of a multi-resource revert
  applied, others not — most commonly because reality drifted a *second*
  time between acceptance and shipping) is exercised by the hermetic
  adversarial test suite rather than shown here as its own transcript; the
  mechanism is the same "outcome" field, just a third value alongside
  `applied`/`failed`. See [Apply record — the state machine](/concepts/apply-record#the-state-machine)
  for the full behavior.
</Note>

## Shipping a destroy

A `change` proposal's `delta.destroys` ([`ubx resolve`'s own "Destroying a
resource"](/cli/resolve#destroying-a-resource)) ships through the exact
same state machine, mixed in with any creates/modifies in the same
proposal via one combined dependency walk -- never a separate pass. Before
the risky call, `ubx ship` re-reads the target live and compares it
against what was signed away: present and matching proceeds; present but
different is refused (the operator never reviewed that exact state);
already gone is a low-friction success, never a refusal (there's nothing
left to lose):

```text
$ ubx ship f015f72fb1d50e812c561bf6f2b3c9094db2f5608ce0aed319c4151d9253ad33 --provider ./fakeprovider
payments.fake_widget.old-widget
  ⠧ provider reported success -- verifying via read-back (attempt 1/10) 0:00
  ✓ applied · confirmed by reconciliation              0:00
✓ applied: payments.fake_widget.old-widget
1 resource(s), 1 applied, 0 failed, 0 still unknown -- outcome: applied
```

`pending`/`in_flight`/`unknown_post_timeout` never get their own line
(UBI-70) — they're the sub-second pivot into verification, which the
read-back line immediately following already narrates; the full
transition history is still recorded, just not narrated one line at a
time. `--json` shows it in full — the two-step reconciliation this
precheck-then-confirm shape actually produces, `present_matches` (the live
re-read, right before the destroy call) followed by `destroyed` (the
confirmation after):

```text
$ ubx ship f015f72fb1d50e812c561bf6f2b3c9094db2f5608ce0aed319c4151d9253ad33 --provider ./fakeprovider --json
```
```json
{
  "apply_record": {
    "resources": [
      {
        "address": {"stack": "payments", "type": "fake_widget", "name": "old-widget"},
        "transitions": [
          {"state": "pending", "at": "2026-08-01T21:05:49Z"},
          {"state": "in_flight", "at": "2026-08-01T21:05:49Z"},
          {"state": "unknown_post_timeout", "at": "2026-08-01T21:05:49Z", "detail": "provider reported a successful destroy -- verifying via a post-destroy read-back before recording it as destroyed"},
          {"state": "applied", "at": "2026-08-01T21:05:49Z", "detail": "confirmed by reconciliation"}
        ],
        "reconciliation": [
          {"at": "2026-08-01T21:05:49Z", "outcome": "present_matches"},
          {"at": "2026-08-01T21:05:49Z", "outcome": "destroyed"}
        ]
      }
    ],
    "summary": {"outcome": "applied", "resources_applied": 1, "resources_failed": 0, "resources_still_unknown": 0}
  }
}
```

### A provider that reports success without it being true

Real transcript (the exact GCP scenario named above, re-run with the
fix): the provider's `ApplyResourceChange` response is a clean success —
no error, no diagnostics — but the resource is still there once the
read-back's full retry budget is spent:

```text
$ ubx ship ef1a7f6b8ba84f3402aac4f4cbbdac6c8e3b24d9814073d645be3f15995eb294 --provider ./terraform-provider-google --provider-config '{"project":"personal-273114"}'
failed: ubi44.google_pubsub_topic.ubi44-fixed-1784417122
  terminal: the provider reported a successful destroy, but a post-destroy read-back found the resource still present after the full retry budget -- the delete never actually happened
1 resource(s), 0 applied, 1 failed, 0 still unknown -- outcome: failed
```

`ubx why` renders the full attempt, including every intervening retry
that found the resource still present before the budget's own final
verdict:

```text
$ ubx why ef1a7f6b8ba84f3402aac4f4cbbdac6c8e3b24d9814073d645be3f15995eb294
...
apply history:
  attempt 1: outcome=failed
    ubi44.google_pubsub_topic.ubi44-fixed-1784417122:
      pending at 2026-07-18T23:26:15Z
      in_flight at 2026-07-18T23:26:16Z
      unknown_post_timeout at 2026-07-18T23:26:16Z -- provider reported a successful destroy -- verifying via a post-destroy read-back before recording it as destroyed
      failed at 2026-07-18T23:27:13Z -- provider reported success but a post-destroy read-back found the resource still present
      reconcile: present_matches at 2026-07-18T23:26:16Z
      reconcile: inconclusive at 2026-07-18T23:26:17Z -- still present after a reported successful destroy -- retrying to allow for real propagation lag before concluding the provider's claim was false
      (... further inconclusive retries, spaced per the backoff schedule ...)
      reconcile: provider_reported_success_but_present at 2026-07-18T23:27:13Z -- the provider reported a successful destroy, but a post-destroy read-back found the resource still present after the full retry budget -- the delete never actually happened
```

Re-shipping against a provider that keeps lying fails again, the same
way, never eventually accepting the claim. See
[the reliability report's own UBI-44 chapter](https://github.com/Ubiquex/ubiquex/blob/main/docs/reliability-report.md)
for the full live investigation this transcript is drawn from.

If the target is present but **drifted** from what was signed away, the
destroy is refused outright -- never reaching `in_flight`, never calling
`ApplyResourceChange` at all. Destroying state the operator never actually
reviewed defeats the whole reason `delta.destroys[].state` is carried
inline in the proposal:

```text
$ ubx ship ea33959119167362358dd69dadbe0cdb6b3b478262a95f28b99438531a2eb655 --provider ./fakeprovider
payments.fake_widget.solo-widget
  ! destroy target drifted since it was signed away -- refusing to destroy state that was never reviewed
✓ pending: payments.fake_widget.solo-widget
  terminal: destroy target drifted since it was signed away -- refusing to destroy state that was never reviewed
1 resource(s), 0 applied, 1 failed, 0 still unknown -- outcome: failed
```

The only way past this is a fresh `ubx scan`/`ubx accept` cycle recording
the new state, then a fresh destroy resolved and accepted against it.

If the target is **already gone** by the time its own turn in this batch
comes, that's a low-friction success, never a refusal -- there's nothing
left to lose that the operator didn't already implicitly ask to lose:

```text
$ ubx ship c1ec92deb14fec35307dfb53003ca40c50eee780a3c81f12b041228dca4cc4d6 --provider ./fakeprovider
payments.fake_widget.widget-a
  ⠧ provider reported success -- verifying via read-back (attempt 1/10) 0:00
  ✓ applied · confirmed by reconciliation              0:00

payments.fake_widget.widget-b
  ✓ applied · already absent before this attempt        0:00
✓ applied: payments.fake_widget.widget-a
✓ applied: payments.fake_widget.widget-b
2 resource(s), 2 applied, 0 failed, 0 still unknown -- outcome: applied
```

```json
{
  "resources": [
    {
      "address": {"stack": "payments", "type": "fake_widget", "name": "widget-b"},
      "transitions": [
        {"state": "pending", "at": "2026-07-30T22:56:21Z"},
        {"state": "applied", "at": "2026-07-30T22:56:21Z", "detail": "already absent before this attempt"}
      ],
      "reconciliation": [
        {"at": "2026-07-30T22:56:21Z", "outcome": "already_absent"}
      ]
    }
  ]
}
```

`widget-b` above never reaches `in_flight` at all -- its own freshness
precheck read comes back not-found, and that alone is enough to conclude
`already_absent`, since nothing has landed here yet for this attempt to
have caused it.

**A clean `ApplyResourceChange` response is never sufficient by itself
to earn `destroyed`** — found live, against a real `google_pubsub_topic`:
the provider reported success (no error, no diagnostics — the identical
shape a genuine destroy produces) while never actually deleting the
resource, confirmed independently via Cloud Audit Logs. So `ubx ship`
always re-reads the target after the call, whether it reported success
or timed out ambiguously: gone confirms `destroyed` (but only because
the precheck above already confirmed it was there moments earlier -- a
bare not-found read alone is never enough to conclude anything); still
there is handled two different ways depending on what the call itself
said. If the call was ambiguous (an RPC timeout, say), a still-present
read is immediately conclusive — `failed`, the call never landed,
retried normally on the next `ubx ship` invocation. If the call reported
success, a still-present read is *not* immediately conclusive — real
propagation lag can look identical to a lie for a while — so it's
retried through the same backoff budget described below, resolving a
distinctly-worded `failed` (the provider's own claim never checked out)
only once that budget is genuinely exhausted. A destroy is idempotent
the same way every other kind is -- already-`applied` (whether the
outcome was `destroyed` or the target was already gone) is skipped
entirely on re-run.

<Note>
  **Real cloud eventual consistency, budgeted for, not silently
  ignored**: real AWS SQS, for instance, can keep reporting a queue as
  present for well beyond the ~60 seconds its own documentation
  suggests, after `DeleteQueue` already succeeded. The post-destroy
  read-back's own retry budget is sized for this — roughly a minute of
  backoff, starting fast (an honest, synchronously-consistent provider,
  the common case, resolves on the very first read, no added delay at
  all) and growing to accommodate a real slow tail. If a destroy is
  still present once that full budget is spent, `ubx ship` calls it
  plainly: either an ordinary `failed` (the call was ambiguous and never
  landed) or, more seriously, `provider_reported_success_but_present`
  (the provider's own claimed success didn't check out) — never
  silently upgraded to `destroyed` either way, and never left in a vague
  `unknown_post_timeout` limbo once the read-back itself gave a clear,
  repeated answer.
</Note>

`ubx ship` will refuse to have shipped this proposal in the first place
unless it was accepted with [`--confirm-destroys`](/cli/accept#confirming-a-destroy)
-- that gate lives in `ubx accept`, not here, since a proposal already
carrying an `Acceptance` was already confirmed at that point.

## Shipping across multiple providers

A stack with a real [`[providers]` table](/cli/config#multi-provider-stacks)
in `.ubx/config` ships a `change` proposal spanning more than one
provider binary as ONE signed unit -- `ubx resolve` already recorded
which provider owns each resource; `ubx ship` reads that back and
launches each one lazily, only the providers this specific proposal's
own resources actually need, each configured from its own
`[provider_configs]` entry. `--provider`/`--source`/`--provider-config`
are ignored (with a warning) once `[providers]` is declared -- the same
staging [`ubx resolve`'s own note](/cli/resolve) describes.

A resource whose own provider can't launch at all fails on its own,
terminally -- it never blocks a sibling resource against a different,
already-fine provider:

```text
$ ubx ship 9d6ca60737a559cf70cbec555674ae6e31fd60e44c5fd28f34e924cfe0fb44ed --ledger-dir . --yes
payments.fake_widget.main
  ! provider unavailable: provider "hashicorp/aws" is pinned to 6.61.0 in this stack's [providers] config, but this proposal recorded 6.60.0 -- re-resolve against the current config
✓ pending: payments.fake_widget.main
  terminal: provider unavailable: provider "hashicorp/aws" is pinned to 6.61.0 in this stack's [providers] config, but this proposal recorded 6.60.0 -- re-resolve against the current config
1 resource(s), 0 applied, 1 failed, 0 still unknown -- outcome: failed
ship: failed (see above)
```

The transcript above is a version pin moving out from under an
already-signed proposal (`[providers]`'s own pin advanced to `6.61.0`
after this proposal was resolved and signed against `6.60.0`) -- refused
outright, never silently launched against whichever version happens to
be current. A launch failure for an unreachable registry or bad
credentials reports the identical shape (`provider unavailable: ...`,
naming the real cause) -- the outcome seals `partially_applied` when at
least one other resource, against a different provider, still lands
normally.

<Note>
  `ubx scan --all`/`ubx status --drift` also read `[providers]` -- see
  [`ubx status`'s own multi-provider section](/cli/status#drift-across-multiple-providers).
</Note>

## `--json` output

```text
$ ubx ship 8fa7a1498d41886a0f68138539d05876deee940a94edbac912c947d2529de56f --provider ./terraform-provider-aws --json
{
  "format": 1,
  "already_applied": true,
  "apply_record": {
    "schema_version": 1,
    "id": "d3156d08dc6168d4d524ea6ffa780a307b597b34bc6dfd8393a883dd1b86bc52",
    "proposal": "8fa7a1498d41886a0f68138539d05876deee940a94edbac912c947d2529de56f",
    "parent": "",
    "attempt": 1,
    "resources": [
      {
        "address": { "stack": "payments", "type": "fake_widget", "name": "api-cache" },
        "transitions": [
          { "state": "pending", "at": "2026-07-17T11:15:08Z" },
          { "state": "in_flight", "at": "2026-07-17T11:15:08Z" },
          { "state": "applied", "at": "2026-07-17T11:15:08Z" }
        ],
        "provider_result": { "id": "computed-id", "name": "a-widget", "tags": {} }
      }
    ],
    "summary": {
      "outcome": "applied",
      "started_at": "2026-07-17T11:15:08Z",
      "finished_at": "2026-07-17T11:15:08Z",
      "resources_applied": 1,
      "resources_failed": 0,
      "resources_still_unknown": 0
    }
  }
}
```

`apply_record` is the full [apply record](/concepts/apply-record) itself —
the same object a future `ubx why` will read to answer "what actually
happened when this shipped" — wrapped, not re-derived into a bespoke shape,
the same convention [`ubx why --json`](/cli/why#json-output) already
established for `proposal`.

## Exit code: the CI contract

| Exit code | Meaning |
| --- | --- |
| `0` | every resource applied (including a genuine no-op: already fully applied) |
| `1` | `partially_applied` or `failed` — an actionable finding: retry, or investigate why |
| `2` | bad input (proposal not found, wrong kind, not accepted), or a genuine provider/ledger failure |

See [Exit codes](/cli/exit-codes) for the full per-verb reference.

## Freshness, re-verified per resource

Unlike a one-shot command, `ubx ship` can run long enough, resource to
resource, that reality moves *during* the run. Freshness is re-checked
immediately before **each** resource's own attempt, not once at the start —
a resource that drifts a second time between acceptance and its own turn in
the queue is refused, not bulldozed; resources already applied earlier in
the same run stand untouched. See [Apply record](/concepts/apply-record) for
the full state machine.

## Related

- [Apply record](/concepts/apply-record) — the ledger object `ubx ship`
  produces, and the full failure-state machine behind it
- [`ubx plan`](/cli/plan) — the other half of the two-step flow: resolving
  any medium input into the plan this command accepts inline
- [`ubx resolve`](/cli/resolve) — producing the `change` proposal this
  command ships, including `$computed`/`depends_on`
- [The two-step flow](/guides/plan-ship-flow) — `ubx plan` + `ubx ship`, end to end
- [Creating real infrastructure](/guides/create-flow) — the four-verb ceremony's own full walkthrough, live on real AWS
- [`ubx revert-plan`](/cli/revert-plan) — the pre-acceptance (or
  manual-reconciliation) alternative that never applies anything itself
- [Ledger stores](/concepts/ledger-stores) — remote stores, and why `--stack` becomes required for them
- [Drift](/concepts/drift) — what a `drift_revert` proposal is
- [Secrets](/concepts/secrets) — why a redacted restore target can never be
  shipped automatically
- [Exit codes](/cli/exit-codes) — the full 0/1/2 reference across every verb
- [`.ubx/config`](/cli/config) — project defaults for `--provider`/`--source`/`--provider-config`, and `[providers]`/`[provider_configs]` for a multi-provider stack
