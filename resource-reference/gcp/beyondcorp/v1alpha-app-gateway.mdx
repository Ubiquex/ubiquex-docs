---
title: "google_beyondcorp_v1alpha_app_gateway"
description: "`google_beyondcorp_v1alpha_app_gateway` is the Google-managed ingress point that routes authorized user traffic from the internet through to..."
---

<Warning>
  This is Google Cloud's own **v1alpha** release channel (alpha). Google may change or remove alpha/preview endpoints at any time, often with no deprecation notice -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_beyondcorp_app_gateway`.
</Warning>

`google_beyondcorp_v1alpha_app_gateway` is the Google-managed ingress point that routes authorized user traffic from the internet through to a BeyondCorp Enterprise app connection.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_beyondcorp_v1alpha --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_beyondcorp_v1alpha/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	beyondcorp "github.com/ubiquex/ubx-sdk-google/sdk/go/google_beyondcorp_v1alpha/beyondcorp"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1alpha app gateway"})

    		ubx.Resource(beyondcorp.V1alphaBeyondcorpV1alphaAppGateway, "example", beyondcorp.V1alphaBeyondcorpV1alphaAppGatewayConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_beyondcorp_v1alpha --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1alphaBeyondcorpV1alphaAppGateway } from "./local-sdk/google_beyondcorp_v1alpha/sdk/typescript/google_beyondcorp_v1alpha/beyondcorp/v1alpha_beyondcorp_v1alpha_app_gateway.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1alpha app gateway" });

      resource(V1alphaBeyondcorpV1alphaAppGateway, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_beyondcorp_v1alpha --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_beyondcorp_v1alpha/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_beyondcorp_v1alpha.beyondcorp import V1alphaBeyondcorpV1alphaAppGateway, V1alphaBeyondcorpV1alphaAppGatewayConfig

    def describe():
        ubx.intent("example own v1alpha app gateway")

        ubx.resource(V1alphaBeyondcorpV1alphaAppGateway, "example", V1alphaBeyondcorpV1alphaAppGatewayConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_beyondcorp_v1alpha_app_gateway called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="allocated_connections" type="list(object)">
  Output only. A list of connections allocated for the Gateway Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="ingress_port" type="number">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="psc_uri" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. An arbitrary user-provided name for the AppGateway. Cannot exceed 64 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="host_type" type="string">
  Required. The type of hosting used by the AppGateway. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Resource labels to represent user provided metadata. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. Unique resource name of the AppGateway. The name is ignored when creating an AppGateway. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the AppGateway. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  Required. The type of network connectivity used by the AppGateway. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. A unique identifier for the instance generated by the system. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when the resource was last modified. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uri" type="string">
  Output only. Server-defined URI for this resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>
