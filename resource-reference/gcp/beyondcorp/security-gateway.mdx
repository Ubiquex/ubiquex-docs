---
title: "google_beyondcorp_security_gateway"
description: "`google_beyondcorp_security_gateway` is BeyondCorp Enterprise's own secure web gateway resource..."
---

`google_beyondcorp_security_gateway` is BeyondCorp Enterprise's own secure web gateway resource, applying zero-trust access policy to web traffic without requiring a per-app connector.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	beyondcorp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/beyondcorp"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own security gateway"})

    		ubx.Resource(beyondcorp.SecurityGateway, "example", beyondcorp.SecurityGatewayConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityGateway } from "@ubx/sdk-google/google/beyondcorp/security_gateway";

    export default stack("example", () => {
      intent({ summary: "example own security gateway" });

      resource(SecurityGateway, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.beyondcorp import SecurityGateway, SecurityGatewayConfig

    def describe():
        ubx.intent("example own security gateway")

        ubx.resource(SecurityGateway, "example", SecurityGatewayConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_beyondcorp_security_gateway called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delegating_service_account" type="string">
  Output only. Service account used for operations that involve resources in consumer projects. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. An arbitrary user-provided name for the SecurityGateway. Cannot exceed 64 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="external_ips" type="list(string)">
  Output only. IP addresses that will be used for establishing connection to the endpoints. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="hubs" type="map(object)">
  Optional. Map of Hubs that represents regional data path deployment with GCP region as a key. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="internet_gateway" type="object">
      Configuration for this hub's own outbound internet gateway. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="assigned_ips" type="list(string)">
          The public IP address(es) assigned to this hub's own internet gateway for outbound traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="logging" type="any">
  Configuration for Cloud Logging. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Name of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="proxy_protocol_config" type="object">
  The configuration for the proxy. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allowed_client_headers" type="list(string)">
      Optional. List of the allowed client header names. Optional.
    </ResponseField>
    <ResponseField name="client_ip" type="bool">
      Optional. Client IP configuration. The client IP address is included if true. Optional.
    </ResponseField>
    <ResponseField name="contextual_headers" type="object">
      Contextual headers configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="device_info" type="object">
          The delegated device information configuration. Optional.
          <Expandable title="properties">
            <ResponseField name="output_type" type="string">
              Optional. The output type details for the delegated device. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dispatch_info" type="object">
          The delegated dispatch information configuration. Optional.
          <Expandable title="properties">
            <ResponseField name="output_type" type="string">
              Optional. The output type details for the delegated dispatch information. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="group_info" type="object">
          The delegated group configuration details. Optional.
          <Expandable title="properties">
            <ResponseField name="output_type" type="string">
              Optional. The output type of the delegated group information. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="output_type" type="string">
          Optional. Default output type for all enabled headers. Optional.
        </ResponseField>
        <ResponseField name="user_info" type="object">
          The configuration information for the delegated user. Optional.
          <Expandable title="properties">
            <ResponseField name="output_type" type="string">
              Optional. The delegated user's information. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="gateway_identity" type="string">
      Optional. The security gateway identity configuration. Optional.
    </ResponseField>
    <ResponseField name="metadata_headers" type="map(string)">
      Optional. Custom resource specific headers along with the values. The names should conform to RFC 9110: &gt;Field names can contain alphanumeric characters, hyphens, and periods, can contain only ASCII-printable characters and tabs, and must start with a letter. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_discovery" type="object">
  Settings related to the Service Discovery. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="api_gateway" type="object">
      If Service Discovery is done through API, defines its settings. Optional.
      <Expandable title="properties">
        <ResponseField name="resource_override" type="object">
          API operation descriptor. Optional.
          <Expandable title="properties">
            <ResponseField name="path" type="string">
              Optional. Contains the URI path fragment where HTTP request is sent. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The operational state of the SecurityGateway. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when the resource was last modified. Optional; if omitted, computed by Google Cloud.
</ResponseField>
