---
title: "google_beyondcorp_application"
description: "`google_beyondcorp_application` registers a private application under BeyondCorp Enterprise's own context-aware access policy..."
---

`google_beyondcorp_application` registers a private application under BeyondCorp Enterprise's own context-aware access policy, the resource IAM and access level(s) are granted against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	beyondcorp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/beyondcorp"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own application"})

    		ubx.Resource(beyondcorp.Application, "example", beyondcorp.ApplicationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Application } from "@ubx/sdk-google/google/beyondcorp/application";

    export default stack("example", () => {
      intent({ summary: "example own application" });

      resource(Application, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.beyondcorp import Application, ApplicationConfig

    def describe():
        ubx.intent("example own application")

        ubx.resource(Application, "example", ApplicationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_beyondcorp_application called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. An arbitrary user-provided name for the application resource. Cannot exceed 64 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="endpoint_matchers" type="list(object)">
  Optional. An array of conditions to match the application's network endpoint. Each element in the array is an EndpointMatcher object, which defines a specific combination of a hostname pattern and one or more ports. The application is considered matched if at least one of the EndpointMatcher conditions in this array is met (the conditions are combined using OR logic). Each EndpointMatcher must contain a hostname pattern, such as "example.com", and one or more port numbers specified as a string, such as "443". Hostname and port number examples: "*.example.com", "443" "example.com" and "22" "example.com" and "22,33" Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="hostname" type="string">
      The hostname this endpoint matcher matches incoming requests against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ports" type="list(number)">
      The port(s) this endpoint matcher matches incoming requests against, alongside `hostname`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Name of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="schema" type="string">
  Optional. Type of the external application. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when the resource was last modified. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="upstreams" type="list(object)">
  Optional. Which upstream resources to forward traffic to. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="egress_policy" type="object">
      Restricts which region(s) traffic to this upstream is allowed to egress from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="regions" type="list(string)">
          The region(s) this upstream's own egress traffic is restricted to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="external" type="object">
      Configuration for an upstream reached over the public internet, by hostname and port, rather than through a private network attachment. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="endpoints" type="list(object)">
          The external endpoint(s) (hostname and port) this upstream forwards to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="hostname" type="string">
              The hostname of this external endpoint. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="port" type="number">
              The port of this external endpoint. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network" type="object">
      Configuration for an upstream reached through a specific VPC network, rather than the public internet. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The VPC network this upstream is reached through. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="proxy_protocol" type="object">
      Configuration for headers BeyondCorp Enterprise attaches to proxied requests, carrying identity and device context to the upstream. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="allowed_client_headers" type="list(string)">
          Client-supplied header names allowed to pass through to the upstream unmodified, alongside BeyondCorp's own injected context headers. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="client_ip" type="bool">
          Whether the original client's own IP address is forwarded to the upstream in a proxy header. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="contextual_headers" type="object">
          Which identity/device/access context BeyondCorp Enterprise attaches to proxied requests as headers, and in what format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="device_info" type="object">
              Whether and how the requesting device's own posture information is attached as a header. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="output_type" type="string">
                  The encoding of the device-info header: `PROTOBUF`, `JSON`, or `NONE` to omit it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="dispatch_info" type="object">
              Whether and how routing/dispatch information for this request is attached as a header. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="output_type" type="string">
                  The encoding of the dispatch-info header: `PROTOBUF`, `JSON`, or `NONE` to omit it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="group_info" type="object">
              Whether and how the requesting user's own group membership is attached as a header. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="output_type" type="string">
                  The encoding of the group-info header: `PROTOBUF`, `JSON`, or `NONE` to omit it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="output_type" type="string">
              The default encoding applied to contextual headers that don't specify their own `output_type`: `PROTOBUF`, `JSON`, or `NONE`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_info" type="object">
              Whether and how the requesting user's own identity is attached as a header. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="output_type" type="string">
                  The encoding of the user-info header: `PROTOBUF`, `JSON`, or `NONE` to omit it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="gateway_identity" type="string">
          How the gateway identifies itself to the upstream in proxied requests: `RESOURCE_NAME` or unspecified. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="metadata_headers" type="map(string)">
          Additional caller-defined key/value headers attached to every proxied request to this upstream. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
