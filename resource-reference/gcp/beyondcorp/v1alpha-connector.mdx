---
title: "google_beyondcorp_v1alpha_connector"
description: "`google_beyondcorp_v1alpha_connector` is a real, deployed BeyondCorp connector -- the agent that actually establishes the network path a connection..."
---

<Warning>
  This is Google Cloud's own **v1alpha** release channel (alpha). Google may change or remove alpha/preview endpoints at any time, often with no deprecation notice -- confirm this endpoint is still live before depending on it in anything long-lived. No stable, generally-available equivalent is published yet.
</Warning>

`google_beyondcorp_v1alpha_connector` is a real, deployed BeyondCorp connector -- the agent that actually establishes the network path a connection depends on -- tracked by its own principal_info (its service identity) and resource_info (what it's running on), so a connection's own underlying network path can be inspected and managed as a distinct resource.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_beyondcorp_v1alpha --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_beyondcorp_v1alpha/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	beyondcorp "github.com/ubiquex/ubx-sdk-google/sdk/go/google_beyondcorp_v1alpha/beyondcorp"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1alpha connector"})

    		ubx.Resource(beyondcorp.V1alphaBeyondcorpV1alphaConnector, "example", beyondcorp.V1alphaBeyondcorpV1alphaConnectorConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_beyondcorp_v1alpha --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1alphaBeyondcorpV1alphaConnector } from "./local-sdk/google_beyondcorp_v1alpha/sdk/typescript/google_beyondcorp_v1alpha/beyondcorp/v1alpha_beyondcorp_v1alpha_connector.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1alpha connector" });

      resource(V1alphaBeyondcorpV1alphaConnector, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_beyondcorp_v1alpha --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_beyondcorp_v1alpha/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_beyondcorp_v1alpha.beyondcorp import V1alphaBeyondcorpV1alphaConnector, V1alphaBeyondcorpV1alphaConnectorConfig

    def describe():
        ubx.intent("example own v1alpha connector")

        ubx.resource(V1alphaBeyondcorpV1alphaConnector, "example", V1alphaBeyondcorpV1alphaConnectorConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_beyondcorp_v1alpha_connector called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. An arbitrary user-provided name for the connector. Cannot exceed 64 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Resource labels to represent user provided metadata. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. Unique resource name of the connector. The name is ignored when creating a connector. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="principal_info" type="object">
  PrincipalInfo represents an Identity oneof. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="service_account" type="object">
      ServiceAccount represents a GCP service account. Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          Email address of the service account. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_info" type="object">
  ResourceInfo represents the information/status of the associated resource. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Required. Unique Id for the resource. Optional.
    </ResponseField>
    <ResponseField name="resource" type="map(any)">
      Specific details for the resource. Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      Overall health status. Overall status is derived based on the status of each sub level resources. Optional.
    </ResponseField>
    <ResponseField name="sub" type="list(any)">
      List of Info for the sub level resources. Optional.
    </ResponseField>
    <ResponseField name="time" type="string">
      The timestamp to collect the info. It is suggested to be set by the topmost level resource only. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the connector. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. A unique identifier for the instance generated by the system. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when the resource was last modified. Optional; if omitted, computed by Google Cloud.
</ResponseField>
