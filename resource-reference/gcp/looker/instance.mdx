---
title: "google_looker_instance"
description: "`google_looker_instance` provisions a fully managed Looker (Google Cloud core) instance, Google's own hosted deployment of the Looker BI platform."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_looker --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_looker/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	looker "github.com/ubiquex/ubx-sdk-google/sdk/go/google_looker/looker"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own instance"})

    		ubx.Resource(looker.LookerInstance, "example", looker.LookerInstanceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_looker --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LookerInstance } from "./local-sdk/google_looker/sdk/typescript/google_looker/looker/looker_instance.ts";

    export default stack("example", () => {
      intent({ summary: "example own instance" });

      resource(LookerInstance, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_looker --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_looker/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_looker.looker import LookerInstance, LookerInstanceConfig

    def describe():
        ubx.intent("example own instance")

        ubx.resource(LookerInstance, "example", LookerInstanceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_looker_instance called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="accelerated_security_patch_enabled" type="bool">
  Optional. Accelerated security patch enabled for the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="admin_settings" type="object">
  Looker instance Admin settings fields. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allowed_email_domains" type="list(string)">
      Email domain allowlist for the instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="catalog_integration_opt_out" type="bool">
  Optional. Indicates whether catalog integration is disabled for the Looker instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="class_type" type="string">
  Optional. Storage class of the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="consumer_network" type="string">
  Network name in the consumer project. Format: `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Note that the consumer network may be in a different GCP project than the consumer project that is hosting the Looker Instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="controlled_egress_config" type="object">
  Controlled egress configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="egress_fqdns" type="list(string)">
      Optional. List of fully qualified domain names to be added to the allowlist for outbound traffic. Optional.
    </ResponseField>
    <ResponseField name="marketplace_enabled" type="bool">
      Optional. Whether marketplace is enabled. Optional.
    </ResponseField>
    <ResponseField name="web_proxy_ips" type="list(string)">
      Output only. The list of IP addresses used by Secure Web Proxy for outbound traffic. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="controlled_egress_enabled" type="bool">
  Optional. Whether controlled egress is enabled on the Looker instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the Looker instance provisioning was first requested. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="custom_domain" type="object">
  Custom domain information. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="domain" type="string">
      The domain for the looker instance's custom domain. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state" type="string">
      The current state of the looker instance's custom domain. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deny_maintenance_period" type="object">
  Specifies the maintenance denial period. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="end_date" type="object">
      Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp Optional.
      <Expandable title="properties">
        <ResponseField name="day" type="number">
          Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant. Optional.
        </ResponseField>
        <ResponseField name="month" type="number">
          Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day. Optional.
        </ResponseField>
        <ResponseField name="year" type="number">
          Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="start_date" type="object">
      Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp Optional.
      <Expandable title="properties">
        <ResponseField name="day" type="number">
          Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant. Optional.
        </ResponseField>
        <ResponseField name="month" type="number">
          Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day. Optional.
        </ResponseField>
        <ResponseField name="year" type="number">
          Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="time" type="object">
      Represents a time of day. The date and time zone are either not significant or are specified elsewhere. An API may choose to allow leap seconds. Related types are google.type.Date and `google.protobuf.Timestamp`. Optional.
      <Expandable title="properties">
        <ResponseField name="hours" type="number">
          Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value "24:00:00" for scenarios like business closing time. Optional.
        </ResponseField>
        <ResponseField name="minutes" type="number">
          Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59. Optional.
        </ResponseField>
        <ResponseField name="nanos" type="number">
          Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="number">
          Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="egress_public_ip" type="string">
  Output only. Public Egress IP (IPv4). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="encryption_config" type="object">
  Encryption configuration (i.e. CMEK). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      Name of the CMEK key in KMS (input parameter). Optional.
    </ResponseField>
    <ResponseField name="kms_key_name_version" type="string">
      Output only. Full name and version of the CMEK key currently in use to encrypt Looker data. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;ring&#125;/cryptoKeys/&#123;key&#125;/cryptoKeyVersions/&#123;version&#125;`. Empty if CMEK is not configured in this instance. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="kms_key_state" type="string">
      Output only. Status of the CMEK key. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fips_enabled" type="bool">
  Optional. Whether FIPS is enabled on the Looker instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="gemini_enabled" type="bool">
  Optional. Whether Gemini feature is enabled on the Looker instance or not. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ingress_ip_allowlist_config" type="object">
  Ingress IP allowlist configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allowlist_rules" type="list(object)">
      Optional. List of IP range rules to allow ingress traffic. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          A human-readable note explaining why this IP range is allowlisted for ingress to the instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ip_range" type="string">
          A CIDR-notation IP range allowed to reach this Looker instance's UI and API logins. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      Optional. Whether ingress IP allowlist functionality is enabled on the Looker instance. Optional.
    </ResponseField>
    <ResponseField name="google_services_enabled" type="bool">
      Optional. Whether google service connections are enabled for the instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ingress_private_ip" type="string">
  Output only. Private Ingress IP (IPv4). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ingress_public_ip" type="string">
  Output only. Public Ingress IP (IPv4). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="last_deny_maintenance_period" type="object">
  Specifies the maintenance denial period. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="end_date" type="object">
      Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp Optional.
      <Expandable title="properties">
        <ResponseField name="day" type="number">
          Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant. Optional.
        </ResponseField>
        <ResponseField name="month" type="number">
          Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day. Optional.
        </ResponseField>
        <ResponseField name="year" type="number">
          Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="start_date" type="object">
      Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp Optional.
      <Expandable title="properties">
        <ResponseField name="day" type="number">
          Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant. Optional.
        </ResponseField>
        <ResponseField name="month" type="number">
          Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day. Optional.
        </ResponseField>
        <ResponseField name="year" type="number">
          Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="time" type="object">
      Represents a time of day. The date and time zone are either not significant or are specified elsewhere. An API may choose to allow leap seconds. Related types are google.type.Date and `google.protobuf.Timestamp`. Optional.
      <Expandable title="properties">
        <ResponseField name="hours" type="number">
          Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value "24:00:00" for scenarios like business closing time. Optional.
        </ResponseField>
        <ResponseField name="minutes" type="number">
          Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59. Optional.
        </ResponseField>
        <ResponseField name="nanos" type="number">
          Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="number">
          Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="linked_lsp_project_number" type="string">
  Optional. Linked Google Cloud Project Number for Looker Studio Pro. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="looker_uri" type="string">
  Output only. Looker instance URI which can be used to access the Looker Instance UI. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="looker_version" type="string">
  Output only. The Looker version that the instance is using. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="maintenance_schedule" type="object">
  Published upcoming future maintenance schedule. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
      The scheduled end time for the maintenance. Optional.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      The scheduled start time for the maintenance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="maintenance_window" type="object">
  Specifies the recurring maintenance window. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="day_of_week" type="string">
      Required. Day of the week for this MaintenanceWindow (in UTC). Optional.
    </ResponseField>
    <ResponseField name="start_time" type="object">
      Represents a time of day. The date and time zone are either not significant or are specified elsewhere. An API may choose to allow leap seconds. Related types are google.type.Date and `google.protobuf.Timestamp`. Optional.
      <Expandable title="properties">
        <ResponseField name="hours" type="number">
          Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value "24:00:00" for scenarios like business closing time. Optional.
        </ResponseField>
        <ResponseField name="minutes" type="number">
          Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59. Optional.
        </ResponseField>
        <ResponseField name="nanos" type="number">
          Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="number">
          Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="oauth_config" type="object">
  Looker instance OAuth login settings. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="client_id" type="string">
      Input only. Client ID from an external OAuth application. This is an input-only field, and thus will not be set in any responses. Optional.
    </ResponseField>
    <ResponseField name="client_secret" type="string">
      Input only. Client secret from an external OAuth application. This is an input-only field, and thus will not be set in any responses. Optional.
    </ResponseField>
    <ResponseField name="shared_oauth_client_enabled" type="bool">
      Optional. Whether to use the shared OAuth client. Instances specifying this field do not need to provide client_id and client_secret. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="periodic_export_config" type="object">
  Configuration for periodic export. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="gcs_uri" type="string">
      Required. Cloud Storage bucket URI for periodic export. Format: gs://&#123;bucket_name&#125; Optional.
    </ResponseField>
    <ResponseField name="kms_key" type="string">
      Required. Name of the CMEK key in KMS. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;key_ring&#125;/cryptoKeys/&#123;crypto_key&#125; Optional.
    </ResponseField>
    <ResponseField name="start_time" type="object">
      Represents a time of day. The date and time zone are either not significant or are specified elsewhere. An API may choose to allow leap seconds. Related types are google.type.Date and `google.protobuf.Timestamp`. Optional.
      <Expandable title="properties">
        <ResponseField name="hours" type="number">
          Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value "24:00:00" for scenarios like business closing time. Optional.
        </ResponseField>
        <ResponseField name="minutes" type="number">
          Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59. Optional.
        </ResponseField>
        <ResponseField name="nanos" type="number">
          Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="number">
          Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="platform_edition" type="string">
  Platform edition. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="private_ip_enabled" type="bool">
  Whether private IP is enabled on the Looker instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="psc_config" type="object">
  Information for Private Service Connect (PSC) setup for a Looker instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allowed_vpcs" type="list(string)">
      Optional. List of VPCs that are allowed ingress into looker. Format: projects/&#123;project&#125;/global/networks/&#123;network&#125; Optional.
    </ResponseField>
    <ResponseField name="looker_service_attachment_uri" type="string">
      Output only. URI of the Looker service attachment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="service_attachments" type="list(object)">
      Optional. List of egress service attachment configurations. Optional.
      <Expandable title="properties">
        <ResponseField name="connection_status" type="string">
          The state of this Private Service Connect egress connection: `ACCEPTED` (established and functioning), `PENDING` (not yet established), `NEEDS_ATTENTION` (a problem with the target service attachment), `REJECTED` (explicitly rejected by the target project), `CLOSED` (the target service attachment no longer exists), or `UNKNOWN`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="failure_reason" type="string">
          Output only. A human-readable explanation of why this service attachment connection isn't `ACCEPTED`, populated when `connection_status` indicates a problem. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="local_fqdn" type="string">
          Output only. The fully qualified domain name this Looker instance uses, on its own side, to reach the published service over this PSC connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="local_fqdns" type="list(string)">
          Output only. Every local fully qualified domain name resolving to this PSC connection, when more than one is provisioned. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_service_attachment_uri" type="string">
          The URI of the published service attachment this Looker instance connects to for egress over Private Service Connect. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="psc_enabled" type="bool">
  Optional. Whether to use Private Service Connect (PSC) for private IP connectivity. If true, neither `public_ip_enabled` nor `private_ip_enabled` can be true. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="public_ip_enabled" type="bool">
  Whether public IP is enabled on the Looker instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="release_channel" type="string">
  Optional. The selected release channel for the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="reserved_range" type="string">
  Name of a reserved IP address range within the Instance.consumer_network, to be used for private services access connection. May or may not be specified in a create request. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="soft_delete_reason" type="string">
  Output only. The reason for the instance being in a soft-deleted state. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="suspended_time" type="string">
  Output only. The time when the Looker instance was suspended (soft deleted). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the Looker instance was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="user_metadata" type="object">
  Metadata about users for a Looker instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="additional_developer_user_count" type="number">
      Optional. The number of additional developer users the instance owner has purchased. Optional.
    </ResponseField>
    <ResponseField name="additional_standard_user_count" type="number">
      Optional. The number of additional standard users the instance owner has purchased. Optional.
    </ResponseField>
    <ResponseField name="additional_viewer_user_count" type="number">
      Optional. The number of additional viewer users the instance owner has purchased. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
