---
title: "google_bigtable_table_iam_policy"
description: "Real, generated bindings for `google_bigtable_table_iam_policy`."
---

`google_bigtable_table_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/bigtable"
    )

    cfg := bigtable.TableIamPolicyConfig{
        InstanceName: "example-instance-name",
        PolicyData: "example",
        Table: "example",
    }
    ubx.Resource(bigtable.TableIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TableIamPolicy } from
      "jsr:@ubx/sdk-google/google/bigtable/table_iam_policy";

    resource(TableIamPolicy, "example", {
      instanceName: "example-instance-name",
      policyData: "example",
      table: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.bigtable import (
        TableIamPolicy,
        TableIamPolicyConfig,
    )

    cfg = TableIamPolicyConfig(
        instance_name="example-instance-name",
        policy_data="example",
        table="example",
    )
    ubx.resource(TableIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_bigtable_table_iam_policy with instance_name,
policy_data, table.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="table" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
