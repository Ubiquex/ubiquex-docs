---
title: "google_bigtable_table_iam_policy"
description: "Real, generated bindings for `google_bigtable_table_iam_policy`."
---

`google_bigtable_table_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigtable "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigtable"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own table iam policy"})

    		ubx.Resource(bigtable.TableIamPolicy, "ci-runner", bigtable.TableIamPolicyConfig{
    			InstanceName: "example-instance-name",
    			PolicyData:   "example",
    			Table:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TableIamPolicy } from "jsr:@ubx/sdk-google/google/bigtable/table_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own table iam policy" });

      resource(TableIamPolicy, "ci-runner", {
        instanceName: "example-instance-name",
        policyData: "example",
        table: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigtable import TableIamPolicy, TableIamPolicyConfig

    def describe():
        ubx.intent("payments own table iam policy")

        ubx.resource(TableIamPolicy, "ci-runner", TableIamPolicyConfig(
            instance_name="example-instance-name",
            policy_data="example",
            table="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigtable_table_iam_policy, with instance name
    example-instance-name, policy data example, table example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="table" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
