---
title: "google_sasportal_node"
description: "`google_sasportal_node` is an organizational node in the Google SAS Portal's real device hierarchy..."
---

`google_sasportal_node` is an organizational node in the Google SAS Portal's real device hierarchy, used alongside deployments to structure how CBRS devices and access are managed at scale.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_sasportal --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_sasportal/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	sasportal "github.com/ubiquex/ubx-sdk-google/sdk/go/google_sasportal/sasportal"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own node"})

    		ubx.Resource(sasportal.SasportalNode, "ci-runner", sasportal.SasportalNodeConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_sasportal --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SasportalNode } from "./local-sdk/google_sasportal/sdk/typescript/google_sasportal/sasportal/sasportal_node.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own node" });

      resource(SasportalNode, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_sasportal --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_sasportal/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_sasportal.sasportal import SasportalNode, SasportalNodeConfig

    def describe():
        ubx.intent("payments own node")

        ubx.resource(SasportalNode, "ci-runner", SasportalNodeConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_sasportal_node called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="display_name" type="string">
  The node's display name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Resource name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="sas_user_ids" type="list(string)">
  User ids used by the devices belonging to this node. Optional; if omitted, computed by Google Cloud.
</ResponseField>
