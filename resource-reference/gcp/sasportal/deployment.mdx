---
title: "google_sasportal_deployment"
description: "`google_sasportal_deployment` is a real organizational grouping of CBRS devices in the Google SAS Portal (Spectrum Access System)..."
---

`google_sasportal_deployment` is a real organizational grouping of CBRS devices in the Google SAS Portal (Spectrum Access System), identified by a unique user ID; a SAS organization can contain multiple deployments, each typically holding multiple `google_sasportal_device` entries.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_sasportal --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_sasportal/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	sasportal "github.com/ubiquex/ubx-sdk-google/sdk/go/google_sasportal/sasportal"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own deployment"})

    		ubx.Resource(sasportal.SasportalDeployment, "example", sasportal.SasportalDeploymentConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_sasportal --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SasportalDeployment } from "./local-sdk/google_sasportal/sdk/typescript/google_sasportal/sasportal/sasportal_deployment.ts";

    export default stack("example", () => {
      intent({ summary: "example own deployment" });

      resource(SasportalDeployment, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_sasportal --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_sasportal/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_sasportal.sasportal import SasportalDeployment, SasportalDeploymentConfig

    def describe():
        ubx.intent("example own deployment")

        ubx.resource(SasportalDeployment, "example", SasportalDeploymentConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_sasportal_deployment called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="display_name" type="string">
  The deployment's display name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="frns" type="list(string)">
  Output only. The FCC Registration Numbers (FRNs) copied from its direct parent. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Resource name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="sas_user_ids" type="list(string)">
  User ID used by the devices belonging to this deployment. Each deployment should be associated with one unique user ID. Optional; if omitted, computed by Google Cloud.
</ResponseField>
