---
title: "google_appengine_v1beta_ingress_rule"
description: "`google_appengine_v1beta_ingress_rule` restricts which source IP address range(s) are permitted to reach an App Engine application..."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_appengine_ingress_rule`.
</Warning>

`google_appengine_v1beta_ingress_rule` restricts which source IP address range(s) are permitted to reach an App Engine application, part of its firewall configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	appengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/appengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta ingress rule"})

    		ubx.Resource(appengine.V1betaIngressRule, "example", appengine.V1betaIngressRuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaIngressRule } from "@ubx/sdk-google/google/appengine/v1beta_ingress_rule";

    export default stack("example", () => {
      intent({ summary: "example own v1beta ingress rule" });

      resource(V1betaIngressRule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.appengine.v1beta_ingress_rule import V1betaIngressRule, V1betaIngressRuleConfig

    def describe():
        ubx.intent("example own v1beta ingress rule")

        ubx.resource(V1betaIngressRule, "example", V1betaIngressRuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_appengine_v1beta_ingress_rule, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="action" type="string">
  The action to take on matched requests. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  An optional string description of this rule. This field has a maximum length of 400 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="priority" type="number">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="source_range" type="string">
  IP address or range, defined using CIDR notation, of requests that this rule applies to. You can use the wildcard character "*" to match all IPs equivalent to "0/0" and "::/0" together. Examples: 192.168.1.1 or 192.168.0.0/16 or 2001:db8::/32 or 2001:0db8:0000:0042:0000:8a2e:0370:7334. Truncation will be silently performed on addresses which are not properly truncated. For example, 1.2.3.4/24 is accepted as the same address as 1.2.3.0/24. Similarly, for IPv6, 2001:db8::1/32 is accepted as the same address as 2001:db8::/32. Optional; if omitted, computed by Google Cloud.
</ResponseField>
