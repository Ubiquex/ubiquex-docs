---
title: "google_appengine_app"
description: "`google_appengine_app` is the single, per-project App Engine application resource -- once created..."
---

`google_appengine_app` is the single, per-project App Engine application resource -- once created, a project can have exactly one, and every App Engine service and version belongs to it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	appengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/appengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own app"})

    		ubx.Resource(appengine.App, "example", appengine.AppConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { App } from "@ubx/sdk-google/google/appengine/app";

    export default stack("example", () => {
      intent({ summary: "example own app" });

      resource(App, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.appengine.app import App, AppConfig

    def describe():
        ubx.intent("example own app")

        ubx.resource(App, "example", AppConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_appengine_app called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="auth_domain" type="string">
  Google Apps authentication domain that controls which users can access this application.Defaults to open access for any Google Account. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="code_bucket" type="string">
  Output only. Google Cloud Storage bucket that can be used for storing files associated with this application. This bucket is associated with the application and can be used by the gcloud deployment commands.@OutputOnly Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="database_type" type="string">
  The type of the Cloud Firestore or Cloud Datastore database associated with this application. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_bucket" type="string">
  Output only. Google Cloud Storage bucket that can be used by this application to store content.@OutputOnly Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_cookie_expiration" type="string">
  Cookie expiration policy for this application. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_hostname" type="string">
  Output only. Hostname used to reach this application, as resolved by App Engine.@OutputOnly Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="dispatch_rules" type="list(object)">
  HTTP path dispatch rules for requests to the application that do not explicitly target a service or version. Rules are order-dependent. Up to 20 dispatch rules can be supported. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="domain" type="string">
      The domain this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path" type="string">
      A file or resource path. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="service" type="string">
      A reference to the service this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="feature_settings" type="object">
  The feature specific settings to be used in the application. These define behaviors that are user configurable. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="split_health_checks" type="bool">
      Boolean value indicating if split health checks should be used instead of the legacy health checks. At an app.yaml level, this means defaulting to 'readiness_check' and 'liveness_check' values instead of 'health_check' ones. Once the legacy 'health_check' behavior is deprecated, and this value is always true, this setting can be removed. Optional.
    </ResponseField>
    <ResponseField name="use_container_optimized_os" type="bool">
      If true, use Container-Optimized OS (https://cloud.google.com/container-optimized-os/) base image for VMs, rather than a base Debian image. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="gcr_domain" type="string">
  Output only. The Google Container Registry domain used for storing managed build docker images for this application. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="generated_customer_metadata" type="map(any)">
  Additional Google Generated Customer Metadata, this field won't be provided by default and can be requested by setting the IncludeExtraData field in GetApplicationRequest Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="iap" type="object">
  Identity-Aware Proxy Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Whether the serving infrastructure will authenticate and authorize all incoming requests.If true, the oauth2_client_id and oauth2_client_secret fields must be non-empty. Optional.
    </ResponseField>
    <ResponseField name="oauth2_client_id" type="string">
      OAuth2 client ID to use for the authentication flow. Optional.
    </ResponseField>
    <ResponseField name="oauth2_client_secret" type="string">
      OAuth2 client secret to use for the authentication flow.For security reasons, this value cannot be retrieved via the API. Instead, the SHA-256 hash of the value is returned in the oauth2_client_secret_sha256 field.@InputOnly Optional.
    </ResponseField>
    <ResponseField name="oauth2_client_secret_sha256" type="string">
      Output only. Hex-encoded SHA-256 hash of the client secret.@OutputOnly Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Identifier of the Application resource. This identifier is equivalent to the project ID of the Google Cloud Platform project where you want to deploy your application. Example: myapp. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="location_id" type="string">
  Location from which this application runs. Application instances run out of the data centers in the specified location, which is also where all of the application's end user content is stored.Defaults to us-central.View the list of supported locations (https://cloud.google.com/appengine/docs/locations). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="service_account" type="string">
  The service account associated with the application. This is the app-level default identity. If no identity provided during create version, Admin API will fallback to this one. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="serving_status" type="string">
  Serving status of this application. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ssl_policy" type="string">
  The SSL policy that will be applied to the application. If set to Modern it will restrict traffic with TLS &lt; 1.2 and allow only Modern Ciphers suite Optional; if omitted, computed by Google Cloud.
</ResponseField>
