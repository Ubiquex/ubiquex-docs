---
title: "google_servicedirectory_namespace"
description: "`google_servicedirectory_namespace` is a container grouping related service(s) together within Service Directory..."
---

`google_servicedirectory_namespace` is a container grouping related service(s) together within Service Directory, Google Cloud's own managed service registry.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_servicedirectory --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_servicedirectory/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	servicedirectory "github.com/ubiquex/ubx-sdk-google/sdk/go/google_servicedirectory/servicedirectory"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own namespace"})

    		ubx.Resource(servicedirectory.ServicedirectoryNamespace, "ci-runner", servicedirectory.ServicedirectoryNamespaceConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_servicedirectory --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicedirectoryNamespace } from "./local-sdk/google_servicedirectory/sdk/typescript/google_servicedirectory/servicedirectory/servicedirectory_namespace.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own namespace" });

      resource(ServicedirectoryNamespace, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_servicedirectory --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_servicedirectory/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_servicedirectory.servicedirectory import ServicedirectoryNamespace, ServicedirectoryNamespaceConfig

    def describe():
        ubx.intent("payments own namespace")

        ubx.resource(ServicedirectoryNamespace, "ci-runner", ServicedirectoryNamespaceConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_servicedirectory_namespace called "ci-runner", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="labels" type="map(string)">
  Optional. Resource labels associated with this namespace. No more than 64 user labels can be associated with a given resource. Label keys and values can be no longer than 63 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. The resource name for the namespace in the format `projects/*/locations/*/namespaces/*`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The globally unique identifier of the namespace in the UUID4 format. Optional; if omitted, computed by Google Cloud.
</ResponseField>
