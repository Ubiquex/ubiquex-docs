---
title: "google_servicedirectory_service"
description: "`google_servicedirectory_service` registers a logical service within Service Directory..."
---

`google_servicedirectory_service` registers a logical service within Service Directory, that endpoint(s) advertising where it's actually reachable are attached to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	servicedirectory "github.com/ubiquex/ubx-sdk-google/sdk/go/google/servicedirectory"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service"})

    		ubx.Resource(servicedirectory.Service, "example", servicedirectory.ServiceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Service } from "@ubx/sdk-google/google/servicedirectory/service";

    export default stack("example", () => {
      intent({ summary: "example own service" });

      resource(Service, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.servicedirectory import Service, ServiceConfig

    def describe():
        ubx.intent("example own service")

        ubx.resource(Service, "example", ServiceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_servicedirectory_service called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Optional. Annotations for the service. This data can be consumed by service clients. Restrictions: * The entire annotations dictionary may contain up to 2000 characters, spread accoss all key-value pairs. Annotations that go beyond this limit are rejected * Valid annotation keys have two segments: an optional prefix and name, separated by a slash (/). The name segment is required and must be 63 characters or less, beginning and ending with an alphanumeric character ([a-z0-9A-Z]) with dashes (-), underscores (_), dots (.), and alphanumerics between. The prefix is optional. If specified, the prefix must be a DNS subdomain: a series of DNS labels separated by dots (.), not longer than 253 characters in total, followed by a slash (/). Annotations that fails to meet these requirements are rejected Note: This field is equivalent to the `metadata` field in the v1beta1 API. They have the same syntax and read/write to the same location in Service Directory. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="endpoints" type="list(object)">
  Output only. Endpoints associated with this service. Returned on LookupService.ResolveService. Control plane clients should use RegistrationService.ListEndpoints. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="address" type="string">
      The IPv4 or IPv6 address this endpoint resolves to, up to 45 characters (a bare address, e.g. `8.8.8.8` -- no port suffix or brackets). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="annotations" type="map(string)">
      Optional. Free-form key/value annotations on this endpoint for consuming clients to read; keys follow the standard `prefix/name` annotation format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The resource name of this endpoint, in the form `projects/*/locations/*/namespaces/*/services/*/endpoints/*`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="network" type="string">
      The VPC network this endpoint lives in, in the form `projects/&#123;project number&#125;/locations/global/networks/&#123;network&#125;`. Only clients within a matching network can resolve it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="port" type="number">
      The port this endpoint listens on, `0`-`65535`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="uid" type="string">
      Output only. A globally unique, UUID4-format identifier for this endpoint, generated by Service Directory. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. The resource name for the service in the format `projects/*/locations/*/namespaces/*/services/*`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The globally unique identifier of the service in the UUID4 format. Optional; if omitted, computed by Google Cloud.
</ResponseField>
