---
title: "google_containeranalysis_note"
description: "`google_containeranalysis_note` defines a kind of metadata (e.g. a vulnerability or build provenance type) that Container Analysis occurrence(s) can be..."
---

`google_containeranalysis_note` defines a kind of metadata (e.g. a vulnerability or build provenance type) that Container Analysis occurrence(s) can be recorded against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	containeranalysis "github.com/ubiquex/ubx-sdk-google/sdk/go/google/containeranalysis"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own note"})

    		ubx.Resource(containeranalysis.Note, "example", containeranalysis.NoteConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Note } from "@ubx/sdk-google/google/containeranalysis/note";

    export default stack("example", () => {
      intent({ summary: "example own note" });

      resource(Note, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.containeranalysis import Note, NoteConfig

    def describe():
        ubx.intent("example own note")

        ubx.resource(Note, "example", NoteConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_containeranalysis_note called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="ai_skill_analysis" type="any">
  AISkillAnalysisNote provides the metadata of an AI-based skill analysis. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="attestation" type="object">
  Note kind that represents a logical attestation "role" or "authority". For example, an organization might have one `Authority` for "QA" and one for "build". This note is intended to act strictly as a grouping mechanism for the attached occurrences (Attestations). This grouping mechanism also provides a security boundary, since IAM ACLs gate the ability for a principle to attach an occurrence to a given note. It also provides a single point of lookup to find all attached attestation occurrences, even if they don't all live in the same project. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="hint" type="object">
      This submessage provides human-readable hints about the purpose of the authority. Because the name of a note acts as its resource reference, it is important to disambiguate the canonical name of the Note (which might be a UUID for security purposes) from "readable" names more suitable for debug output. Note that these hints should not be used to look up authorities in security sensitive contexts, such as when looking up attestations to verify. Optional.
      <Expandable title="properties">
        <ResponseField name="human_readable_name" type="string">
          Required. The human readable name of this attestation authority, for example "qa". Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="build" type="object">
  Note holding the version of the provider's builder and the signature of the provenance message in the build details occurrence. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="builder_version" type="string">
      Required. Immutable. Version of the builder which produced this build. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="compliance" type="object">
  Detail about this resource's own compliance check result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cis_benchmark" type="object">
      A compliance check that is a CIS benchmark. Optional.
      <Expandable title="properties">
        <ResponseField name="profile_level" type="number">
          The compliance benchmark profile level this check belongs to, e.g. Level 1 or Level 2. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="severity" type="string">
          How serious this vulnerability is, e.g. `CRITICAL` or `LOW`. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      A description about this compliance check. Optional.
    </ResponseField>
    <ResponseField name="impact" type="string">
      The consequence of this vulnerability if exploited. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rationale" type="string">
      A rationale for the existence of this compliance check. Optional.
    </ResponseField>
    <ResponseField name="remediation" type="string">
      A description of remediation steps if the compliance check fails. Optional.
    </ResponseField>
    <ResponseField name="scan_instructions" type="string">
      Serialized scan instructions with a predefined format. Optional.
    </ResponseField>
    <ResponseField name="title" type="string">
      The title that identifies this compliance check. Optional.
    </ResponseField>
    <ResponseField name="version" type="list(object)">
      The OS and config versions the benchmark applies to. Optional.
      <Expandable title="properties">
        <ResponseField name="benchmark_document" type="string">
          The compliance benchmark document this check is defined in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="cpe_uri" type="string">
          A Common Platform Enumeration URI identifying the affected software. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="version" type="string">
          The version identifier this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time this note was created. This field can be used as a filter in list requests. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="deployment" type="object">
  An artifact that can be deployed in some runtime. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="resource_uri" type="list(string)">
      Required. Resource URI for the artifact being deployed. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="discovery" type="object">
  A note that indicates a type of analysis a provider would perform. This note exists in a provider's project. A `Discovery` occurrence is created in a consumer's project at the start of analysis. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="analysis_kind" type="string">
      Required. Immutable. The kind of analysis that is handled by this discovery. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dsse_attestation" type="object">
  An attestation wrapped in the Dead Simple Signing Envelope (DSSE) format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="hint" type="object">
      This submessage provides human-readable hints about the purpose of the authority. Because the name of a note acts as its resource reference, it is important to disambiguate the canonical name of the Note (which might be a UUID for security purposes) from "readable" names more suitable for debug output. Note that these hints should not be used to look up authorities in security sensitive contexts, such as when looking up attestations to verify. Optional.
      <Expandable title="properties">
        <ResponseField name="human_readable_name" type="string">
          Required. The human readable name of this attestation authority, for example "cloudbuild-prod". Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="expiration_time" type="string">
  Time of expiration for this note. Empty if note does not expire. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="image" type="object">
  Basis describes the base image portion (Note) of the DockerImage relationship. Linked occurrences are derived from this or an equivalent image via: FROM Or an equivalent reference, e.g., a tag of the resource_url. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="fingerprint" type="object">
      A set of properties that uniquely identify a given Docker image. Optional.
      <Expandable title="properties">
        <ResponseField name="v1_name" type="string">
          Required. The layer ID of the final layer in the Docker image's v1 representation. Optional.
        </ResponseField>
        <ResponseField name="v2_blob" type="list(string)">
          Required. The ordered list of v2 blobs that represent a given image. Optional.
        </ResponseField>
        <ResponseField name="v2_name" type="string">
          Output only. The name of the image's v2 blobs computed via: [bottom] := v2_blobbottom := sha256(v2_blob[N] + " " + v2_name[N+1]) Only the name of the final blob is kept. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_url" type="string">
      Required. Immutable. The resource_url for the resource representing the basis of associated occurrence images. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. The type of analysis. This field can be used as a filter in list requests. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="long_description" type="string">
  A detailed description of this note. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The name of the note in the form of `projects/[PROVIDER_ID]/notes/[NOTE_ID]`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="package" type="object">
  PackageNote represents a particular package version. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="architecture" type="string">
      The CPU architecture for which packages in this distribution channel were built. Architecture will be blank for language packages. Optional.
    </ResponseField>
    <ResponseField name="cpe_uri" type="string">
      The cpe_uri in [CPE format](https://cpe.mitre.org/specification/) denoting the package manager version distributing a package. The cpe_uri will be blank for language packages. Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      The description of this package. Optional.
    </ResponseField>
    <ResponseField name="digest" type="list(object)">
      Hash value, typically a file digest, that allows unique identification a specific package. Optional.
      <Expandable title="properties">
        <ResponseField name="algo" type="string">
          The hash algorithm used, e.g. `SHA256`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="digest_bytes" type="string">
          The raw bytes of this digest. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="distribution" type="list(object)">
      Deprecated. The various channels by which a package is distributed. Optional.
      <Expandable title="properties">
        <ResponseField name="architecture" type="string">
          The CPU architecture this applies to, e.g. `x86_64`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="cpe_uri" type="string">
          A Common Platform Enumeration URI identifying the affected software. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="latest_version" type="object">
          The most recent version of this package. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="epoch" type="number">
              The package version's own epoch, used to order versions when the upstream numbering scheme changed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="full_name" type="string">
              The fully qualified name of this package or resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="inclusive" type="bool">
              Whether this version range's own boundary is inclusive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The kind of note or occurrence this is, e.g. `VULNERABILITY` or `BUILD`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="revision" type="string">
              The specific revision of this source this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="maintainer" type="string">
          The maintainer of this package. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string">
          A URL this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="license" type="object">
      License information. Optional.
      <Expandable title="properties">
        <ResponseField name="comments" type="string">
          Comments Optional.
        </ResponseField>
        <ResponseField name="expression" type="string">
          Often a single license can be used to represent the licensing terms. Sometimes it is necessary to include a choice of one or more licenses or some combination of license identifiers. Examples: "LGPL-2.1-only OR MIT", "LGPL-2.1-only AND MIT", "GPL-2.0-or-later WITH Bison-exception-2.2". Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="maintainer" type="string">
      A freeform text denoting the maintainer of this package. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      Required. Immutable. The name of the package. Optional.
    </ResponseField>
    <ResponseField name="package_type" type="string">
      The type of package; whether native or non native (e.g., ruby gems, node.js packages, etc.). Optional.
    </ResponseField>
    <ResponseField name="url" type="string">
      The homepage for this package. Optional.
    </ResponseField>
    <ResponseField name="version" type="object">
      Version contains structured information about the version of a package. Optional.
      <Expandable title="properties">
        <ResponseField name="epoch" type="number">
          Used to correct mistakes in the version numbering scheme. Optional.
        </ResponseField>
        <ResponseField name="full_name" type="string">
          Human readable version string. This string is of the form :- and is only set when kind is NORMAL. Optional.
        </ResponseField>
        <ResponseField name="inclusive" type="bool">
          Whether this version is specifying part of an inclusive range. Grafeas does not have the capability to specify version ranges; instead we have fields that specify start version and end versions. At times this is insufficient - we also need to specify whether the version is included in the range or is excluded from the range. This boolean is expected to be set to true when the version is included in a range. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          Required. Distinguishes between sentinel MIN/MAX versions and normal versions. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Required only when version kind is NORMAL. The main part of the version name. Optional.
        </ResponseField>
        <ResponseField name="revision" type="string">
          The iteration of the package build from the above version. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="related_note_names" type="list(string)">
  Other notes related to this note. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="related_url" type="list(object)">
  URLs associated with this note. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="label" type="string">
      A human-readable label for this item. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      A URL this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sbom_reference" type="object">
  The note representing an SBOM reference. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="format" type="string">
      The format that SBOM takes. E.g. may be spdx, cyclonedx, etc... Optional.
    </ResponseField>
    <ResponseField name="version" type="string">
      The version of the format that the SBOM takes. E.g. if the format is spdx, the version may be 2.3. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="secret" type="any">
  The note representing a secret. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="short_description" type="string">
  A one sentence description of this note. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time this note was last updated. This field can be used as a filter in list requests. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="upgrade" type="object">
  An Upgrade Note represents a potential upgrade of a package to a given version. For each package version combination (i.e. bash 4.0, bash 4.1, bash 4.1.2), there will be an Upgrade Note. For Windows, windows_update field represents the information related to the update. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="distributions" type="list(object)">
      Metadata about the upgrade for each specific operating system. Optional.
      <Expandable title="properties">
        <ResponseField name="classification" type="string">
          How this vulnerability or finding is classified. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="cpe_uri" type="string">
          A Common Platform Enumeration URI identifying the affected software. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="cve" type="list(string)">
          The CVE identifier for this vulnerability. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="severity" type="string">
          How serious this vulnerability is, e.g. `CRITICAL` or `LOW`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="package" type="string">
      Required for non-Windows OS. The package this Upgrade is for. Optional.
    </ResponseField>
    <ResponseField name="version" type="object">
      Version contains structured information about the version of a package. Optional.
      <Expandable title="properties">
        <ResponseField name="epoch" type="number">
          Used to correct mistakes in the version numbering scheme. Optional.
        </ResponseField>
        <ResponseField name="full_name" type="string">
          Human readable version string. This string is of the form :- and is only set when kind is NORMAL. Optional.
        </ResponseField>
        <ResponseField name="inclusive" type="bool">
          Whether this version is specifying part of an inclusive range. Grafeas does not have the capability to specify version ranges; instead we have fields that specify start version and end versions. At times this is insufficient - we also need to specify whether the version is included in the range or is excluded from the range. This boolean is expected to be set to true when the version is included in a range. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          Required. Distinguishes between sentinel MIN/MAX versions and normal versions. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Required only when version kind is NORMAL. The main part of the version name. Optional.
        </ResponseField>
        <ResponseField name="revision" type="string">
          The iteration of the package build from the above version. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="windows_update" type="object">
      Windows Update represents the metadata about the update for the Windows operating system. The fields in this message come from the Windows Update API documented at https://docs.microsoft.com/en-us/windows/win32/api/wuapi/nn-wuapi-iupdate. Optional.
      <Expandable title="properties">
        <ResponseField name="categories" type="list(object)">
          The list of categories to which the update belongs. Optional.
          <Expandable title="properties">
            <ResponseField name="category_id" type="string">
              An identifier for the category this belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          The localized description of the update. Optional.
        </ResponseField>
        <ResponseField name="identity" type="object">
          The unique identifier of the update. Optional.
          <Expandable title="properties">
            <ResponseField name="revision" type="number">
              The revision number of the update. Optional.
            </ResponseField>
            <ResponseField name="update_id" type="string">
              The revision independent identifier of the update. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kb_article_ids" type="list(string)">
          The Microsoft Knowledge Base article IDs that are associated with the update. Optional.
        </ResponseField>
        <ResponseField name="last_published_timestamp" type="string">
          The last published timestamp of the update. Optional.
        </ResponseField>
        <ResponseField name="support_url" type="string">
          The hyperlink to the support information for the update. Optional.
        </ResponseField>
        <ResponseField name="title" type="string">
          The localized title of the update. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vulnerability" type="object">
  A security vulnerability that can be found in resources. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="advisory_publish_time" type="string">
      The time this advisory was published by the source. Optional.
    </ResponseField>
    <ResponseField name="cvss_score" type="number">
      The CVSS score of this vulnerability. CVSS score is on a scale of 0 - 10 where 0 indicates low severity and 10 indicates high severity. Optional.
    </ResponseField>
    <ResponseField name="cvss_v2" type="object">
      Common Vulnerability Scoring System. For details, see https://www.first.org/cvss/specification-document This is a message we will try to use for storing various versions of CVSS rather than making a separate proto for storing a specific version. Optional.
      <Expandable title="properties">
        <ResponseField name="attack_complexity" type="string">
          Attack Complexity (AC). Defined in CVSS v2, v3, v4. Optional.
        </ResponseField>
        <ResponseField name="attack_requirements" type="string">
          Attack Requirements (AT). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="attack_vector" type="string">
          Attack Vector (AV). Defined in CVSS v2, v3, v4. Optional.
        </ResponseField>
        <ResponseField name="authentication" type="string">
          Authentication (Au). Defined in CVSS v2. Optional.
        </ResponseField>
        <ResponseField name="availability_impact" type="string">
          Availability Impact (A). Defined in CVSS v2, v3. Optional.
        </ResponseField>
        <ResponseField name="base_score" type="number">
          The base score is a function of the base metric scores. Optional.
        </ResponseField>
        <ResponseField name="confidentiality_impact" type="string">
          Confidentiality Impact (C). Defined in CVSS v2, v3. Optional.
        </ResponseField>
        <ResponseField name="exploit_maturity" type="string">
          Exploit Maturity (E). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="exploitability_score" type="number">
          A CVSS sub-score reflecting how easily this vulnerability can be exploited. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="impact_score" type="number">
          A CVSS sub-score reflecting the consequence of a successful exploit. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="integrity_impact" type="string">
          Integrity Impact (I). Defined in CVSS v2, v3. Optional.
        </ResponseField>
        <ResponseField name="privileges_required" type="string">
          Privileges Required (PR). Defined in CVSS v3, v4. Optional.
        </ResponseField>
        <ResponseField name="scope" type="string">
          Scope (S). Defined in CVSS v3. Optional.
        </ResponseField>
        <ResponseField name="subsequent_system_availability_impact" type="string">
          Subsequent System Availability Impact (SA). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="subsequent_system_confidentiality_impact" type="string">
          Subsequent System Confidentiality Impact (SC). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="subsequent_system_integrity_impact" type="string">
          Subsequent System Integrity Impact (SI). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="user_interaction" type="string">
          User Interaction (UI). Defined in CVSS v3, v4. Optional.
        </ResponseField>
        <ResponseField name="vulnerable_system_availability_impact" type="string">
          Vulnerable System Availability Impact (VA). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="vulnerable_system_confidentiality_impact" type="string">
          Vulnerable System Confidentiality Impact (VC). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="vulnerable_system_integrity_impact" type="string">
          Vulnerable System Integrity Impact (VI). Defined in CVSS v4. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cvss_v3" type="object">
      Common Vulnerability Scoring System version 3. For details, see https://www.first.org/cvss/specification-document Optional.
      <Expandable title="properties">
        <ResponseField name="attack_complexity" type="string">
          How difficult a successful exploit of this vulnerability is to execute, per CVSS. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="attack_vector" type="string">
          Base Metrics Represents the intrinsic characteristics of a vulnerability that are constant over time and across user environments. Optional.
        </ResponseField>
        <ResponseField name="availability_impact" type="string">
          The impact to system availability if this vulnerability is exploited, per CVSS. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="base_score" type="number">
          The base score is a function of the base metric scores. Optional.
        </ResponseField>
        <ResponseField name="confidentiality_impact" type="string">
          The impact to data confidentiality if this vulnerability is exploited, per CVSS. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="exploitability_score" type="number">
          A CVSS sub-score reflecting how easily this vulnerability can be exploited. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="impact_score" type="number">
          A CVSS sub-score reflecting the consequence of a successful exploit. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="integrity_impact" type="string">
          The impact to data integrity if this vulnerability is exploited, per CVSS. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="privileges_required" type="string">
          What level of privilege an attacker needs to exploit this vulnerability, per CVSS. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scope" type="string">
          What this occurrence's own analysis covers. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_interaction" type="string">
          Whether exploiting this vulnerability requires interaction from a user other than the attacker, per CVSS. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cvss_v4" type="object">
      Common Vulnerability Scoring System. For details, see https://www.first.org/cvss/specification-document This is a message we will try to use for storing various versions of CVSS rather than making a separate proto for storing a specific version. Optional.
      <Expandable title="properties">
        <ResponseField name="attack_complexity" type="string">
          Attack Complexity (AC). Defined in CVSS v2, v3, v4. Optional.
        </ResponseField>
        <ResponseField name="attack_requirements" type="string">
          Attack Requirements (AT). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="attack_vector" type="string">
          Attack Vector (AV). Defined in CVSS v2, v3, v4. Optional.
        </ResponseField>
        <ResponseField name="authentication" type="string">
          Authentication (Au). Defined in CVSS v2. Optional.
        </ResponseField>
        <ResponseField name="availability_impact" type="string">
          Availability Impact (A). Defined in CVSS v2, v3. Optional.
        </ResponseField>
        <ResponseField name="base_score" type="number">
          The base score is a function of the base metric scores. Optional.
        </ResponseField>
        <ResponseField name="confidentiality_impact" type="string">
          Confidentiality Impact (C). Defined in CVSS v2, v3. Optional.
        </ResponseField>
        <ResponseField name="exploit_maturity" type="string">
          Exploit Maturity (E). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="exploitability_score" type="number">
          A CVSS sub-score reflecting how easily this vulnerability can be exploited. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="impact_score" type="number">
          A CVSS sub-score reflecting the consequence of a successful exploit. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="integrity_impact" type="string">
          Integrity Impact (I). Defined in CVSS v2, v3. Optional.
        </ResponseField>
        <ResponseField name="privileges_required" type="string">
          Privileges Required (PR). Defined in CVSS v3, v4. Optional.
        </ResponseField>
        <ResponseField name="scope" type="string">
          Scope (S). Defined in CVSS v3. Optional.
        </ResponseField>
        <ResponseField name="subsequent_system_availability_impact" type="string">
          Subsequent System Availability Impact (SA). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="subsequent_system_confidentiality_impact" type="string">
          Subsequent System Confidentiality Impact (SC). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="subsequent_system_integrity_impact" type="string">
          Subsequent System Integrity Impact (SI). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="user_interaction" type="string">
          User Interaction (UI). Defined in CVSS v3, v4. Optional.
        </ResponseField>
        <ResponseField name="vulnerable_system_availability_impact" type="string">
          Vulnerable System Availability Impact (VA). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="vulnerable_system_confidentiality_impact" type="string">
          Vulnerable System Confidentiality Impact (VC). Defined in CVSS v4. Optional.
        </ResponseField>
        <ResponseField name="vulnerable_system_integrity_impact" type="string">
          Vulnerable System Integrity Impact (VI). Defined in CVSS v4. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cvss_version" type="string">
      CVSS version used to populate cvss_score and severity. Optional.
    </ResponseField>
    <ResponseField name="details" type="list(object)">
      Details of all known distros and packages affected by this vulnerability. Optional.
      <Expandable title="properties">
        <ResponseField name="affected_cpe_uri" type="string">
          The CPE URI of the package version affected by this vulnerability. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="affected_package" type="string">
          The name of the package affected by this vulnerability. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="affected_version_end" type="object">
          The end of the version range affected by this vulnerability. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="epoch" type="number">
              The package version's own epoch, used to order versions when the upstream numbering scheme changed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="full_name" type="string">
              The fully qualified name of this package or resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="inclusive" type="bool">
              Whether this version range's own boundary is inclusive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The kind of note or occurrence this is, e.g. `VULNERABILITY` or `BUILD`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="revision" type="string">
              The specific revision of this source this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="affected_version_start" type="object">
          The start of the version range affected by this vulnerability. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="epoch" type="number">
              The package version's own epoch, used to order versions when the upstream numbering scheme changed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="full_name" type="string">
              The fully qualified name of this package or resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="inclusive" type="bool">
              Whether this version range's own boundary is inclusive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The kind of note or occurrence this is, e.g. `VULNERABILITY` or `BUILD`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="revision" type="string">
              The specific revision of this source this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fixed_cpe_uri" type="string">
          The CPE URI of the package version that fixes this vulnerability. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fixed_package" type="string">
          The name of the package that fixes this vulnerability. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fixed_version" type="object">
          The version that fixes this vulnerability. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="epoch" type="number">
              The package version's own epoch, used to order versions when the upstream numbering scheme changed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="full_name" type="string">
              The fully qualified name of this package or resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="inclusive" type="bool">
              Whether this version range's own boundary is inclusive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The kind of note or occurrence this is, e.g. `VULNERABILITY` or `BUILD`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="revision" type="string">
              The specific revision of this source this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="is_obsolete" type="bool">
          Whether this note or occurrence has been superseded and should be treated as no longer current. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="package_type" type="string">
          The package manager/ecosystem this package belongs to, e.g. `DEBIAN` or `NPM`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="severity_name" type="string">
          A human-readable name for this vulnerability's own severity level. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="source" type="string">
          The source this build's own materials were drawn from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="source_update_time" type="string">
          When this source note was last updated. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vendor" type="string">
          The vendor of this package or software. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="severity" type="string">
      The note provider assigned severity of this vulnerability. Optional.
    </ResponseField>
    <ResponseField name="source_update_time" type="string">
      The time this information was last changed at the source. This is an upstream timestamp from the underlying information source - e.g. Ubuntu security tracker. Optional.
    </ResponseField>
    <ResponseField name="windows_details" type="list(object)">
      Windows details get their own format because the information format and model don't match a normal detail. Specifically Windows updates are done as patches, thus Windows vulnerabilities really are a missing package, rather than a package being at an incorrect version. Optional.
      <Expandable title="properties">
        <ResponseField name="cpe_uri" type="string">
          A Common Platform Enumeration URI identifying the affected software. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fixing_kbs" type="list(object)">
          The Windows Knowledge Base article(s) that fix this vulnerability. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="url" type="string">
              A URL this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vulnerability_assessment" type="object">
  A single VulnerabilityAssessmentNote represents one particular product's vulnerability assessment for one CVE. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="assessment" type="object">
      Assessment provides all information that is related to a single vulnerability for this product. Optional.
      <Expandable title="properties">
        <ResponseField name="cve" type="string">
          Holds the MITRE standard Common Vulnerabilities and Exposures (CVE) tracking number for the vulnerability. Deprecated: Use vulnerability_id instead to denote CVEs. Optional.
        </ResponseField>
        <ResponseField name="impacts" type="list(string)">
          Contains information about the impact of this vulnerability, this will change with time. Optional.
        </ResponseField>
        <ResponseField name="justification" type="object">
          Justification provides the justification when the state of the assessment if NOT_AFFECTED. Optional.
          <Expandable title="properties">
            <ResponseField name="details" type="string">
              Additional details on why this justification was chosen. Optional.
            </ResponseField>
            <ResponseField name="justification_type" type="string">
              The justification type for this vulnerability. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="long_description" type="string">
          A detailed description of this Vex. Optional.
        </ResponseField>
        <ResponseField name="related_uris" type="list(object)">
          Holds a list of references associated with this vulnerability item and assessment. These uris have additional information about the vulnerability and the assessment itself. E.g. Link to a document which details how this assessment concluded the state of this vulnerability. Optional.
          <Expandable title="properties">
            <ResponseField name="label" type="string">
              A human-readable label for this item. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="url" type="string">
              A URL this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="remediations" type="list(object)">
          Specifies details on how to handle (and presumably, fix) a vulnerability. Optional.
          <Expandable title="properties">
            <ResponseField name="details" type="string">
              Additional detail about this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="remediation_type" type="string">
              The category of fix available for this vulnerability, e.g. a package update or workaround. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="remediation_uri" type="object">
              A URI with guidance on remediating this vulnerability. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="label" type="string">
                  A human-readable label for this item. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="url" type="string">
                  A URL this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="short_description" type="string">
          A one sentence description of this Vex. Optional.
        </ResponseField>
        <ResponseField name="state" type="string">
          Provides the state of this Vulnerability assessment. Optional.
        </ResponseField>
        <ResponseField name="vulnerability_id" type="string">
          The vulnerability identifier for this Assessment. Will hold one of common identifiers e.g. CVE, GHSA etc. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="language_code" type="string">
      Identifies the language used by this document, corresponding to IETF BCP 47 / RFC 5646. Optional.
    </ResponseField>
    <ResponseField name="long_description" type="string">
      A detailed description of this Vex. Optional.
    </ResponseField>
    <ResponseField name="product" type="object">
      Product contains information about a product and how to uniquely identify it. Optional.
      <Expandable title="properties">
        <ResponseField name="generic_uri" type="string">
          Contains a URI which is vendor-specific. Example: The artifact repository URL of an image. Optional.
        </ResponseField>
        <ResponseField name="id" type="string">
          Token that identifies a product so that it can be referred to from other parts in the document. There is no predefined format as long as it uniquely identifies a group in the context of the current document. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the product. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="publisher" type="object">
      Publisher contains information about the publisher of this Note. Optional.
      <Expandable title="properties">
        <ResponseField name="issuing_authority" type="string">
          Provides information about the authority of the issuing party to release the document, in particular, the party's constituency and responsibilities or other obligations. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the publisher. Examples: 'Google', 'Google Cloud Platform'. Optional.
        </ResponseField>
        <ResponseField name="publisher_namespace" type="string">
          The context or namespace. Contains a URL which is under control of the issuing party and can be used as a globally unique identifier for that issuing party. Example: https://csaf.io Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="short_description" type="string">
      A one sentence description of this Vex. Optional.
    </ResponseField>
    <ResponseField name="title" type="string">
      The title of the note. E.g. `Vex-Debian-11.4` Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
