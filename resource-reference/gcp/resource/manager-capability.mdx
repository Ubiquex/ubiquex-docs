---
title: "google_resource_manager_capability"
description: "Real, generated bindings for `google_resource_manager_capability`."
---

`google_resource_manager_capability` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	resource "github.com/ubiquex/ubx-sdk-google/sdk/go/google/resource"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own manager capability"})

    		ubx.Resource(resource.ManagerCapability, "ci-runner", resource.ManagerCapabilityConfig{
    			CapabilityName: "example-capability-name",
    			Parent:         "example",
    			Value:          true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerCapability } from "jsr:@ubx/sdk-google/google/resource/manager_capability";

    export default stack("payments", () => {
      intent({ summary: "payments own manager capability" });

      resource(ManagerCapability, "ci-runner", {
        capabilityName: "example-capability-name",
        parent: "example",
        value: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.resource import ManagerCapability, ManagerCapabilityConfig

    def describe():
        ubx.intent("payments own manager capability")

        ubx.resource(ManagerCapability, "ci-runner", ManagerCapabilityConfig(
            capability_name="example-capability-name",
            parent="example",
            value=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_resource_manager_capability, with capability name
    example-capability-name, parent example, value true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="capability_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="value" type="bool" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
