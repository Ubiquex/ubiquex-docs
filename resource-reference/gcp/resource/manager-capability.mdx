---
title: "google_resource_manager_capability"
description: "Real, generated bindings for `google_resource_manager_capability`."
---

`google_resource_manager_capability` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/resource"
    )

    cfg := resource.ManagerCapabilityConfig{
        CapabilityName: "example-capability-name",
        Parent: "example",
        Value: true,
    }
    ubx.Resource(resource.ManagerCapability, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ManagerCapability } from
      "jsr:@ubx/sdk-google/google/resource/manager_capability";

    resource(ManagerCapability, "example", {
      capabilityName: "example-capability-name",
      parent: "example",
      value: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.resource import (
        ManagerCapability,
        ManagerCapabilityConfig,
    )

    cfg = ManagerCapabilityConfig(
        capability_name="example-capability-name",
        parent="example",
        value=True,
    )
    ubx.resource(ManagerCapability, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_resource_manager_capability with
capability_name, parent, value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="capability_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="value" type="bool" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
