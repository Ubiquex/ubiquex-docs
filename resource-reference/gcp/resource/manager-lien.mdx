---
title: "google_resource_manager_lien"
description: "Real, generated bindings for `google_resource_manager_lien`."
---

`google_resource_manager_lien` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/resource"
    )

    cfg := resource.ManagerLienConfig{
        Origin: "example",
        Parent: "example",
        Reason: "example",
        Restrictions: []string{"example"},
    }
    ubx.Resource(resource.ManagerLien, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ManagerLien } from
      "jsr:@ubx/sdk-google/google/resource/manager_lien";

    resource(ManagerLien, "example", {
      origin: "example",
      parent: "example",
      reason: "example",
      restrictions: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.resource import ManagerLien, ManagerLienConfig

    cfg = ManagerLienConfig(
        origin="example",
        parent="example",
        reason="example",
        restrictions=["example"],
    )
    ubx.resource(ManagerLien, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_resource_manager_lien with origin, parent,
reason, restrictions.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="origin" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  Required.
</ResponseField>

<ResponseField name="reason" type="string" required>
  Required.
</ResponseField>

<ResponseField name="restrictions" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
