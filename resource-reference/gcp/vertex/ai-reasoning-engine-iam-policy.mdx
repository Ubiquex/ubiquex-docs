---
title: "google_vertex_ai_reasoning_engine_iam_policy"
description: "Real, generated bindings for `google_vertex_ai_reasoning_engine_iam_policy`."
---

`google_vertex_ai_reasoning_engine_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vertex "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/vertex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ai reasoning engine iam policy"})

    		ubx.Resource(vertex.AiReasoningEngineIamPolicy, "ci-runner", vertex.AiReasoningEngineIamPolicyConfig{
    			PolicyData:      "example",
    			ReasoningEngine: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AiReasoningEngineIamPolicy } from "jsr:@ubx/sdk-google/google/vertex/ai_reasoning_engine_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own ai reasoning engine iam policy" });

      resource(AiReasoningEngineIamPolicy, "ci-runner", {
        policyData: "example",
        reasoningEngine: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vertex import AiReasoningEngineIamPolicy, AiReasoningEngineIamPolicyConfig

    def describe():
        ubx.intent("payments own ai reasoning engine iam policy")

        ubx.resource(AiReasoningEngineIamPolicy, "ci-runner", AiReasoningEngineIamPolicyConfig(
            policy_data="example",
            reasoning_engine="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vertex_ai_reasoning_engine_iam_policy, with policy
    data example, reasoning engine example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reasoning_engine" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
