---
title: "google_bigqueryconnection_v1beta1_connection"
description: "`google_bigqueryconnection_v1beta1_connection` defines credentials and configuration BigQuery uses to query an external data source (Cloud SQL..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_bigqueryconnection_connection`.
</Warning>

`google_bigqueryconnection_v1beta1_connection` defines credentials and configuration BigQuery uses to query an external data source (Cloud SQL, Spanner, or a remote function endpoint) via a federated query or connection.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigqueryconnection "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigqueryconnection"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 connection"})

    		ubx.Resource(bigqueryconnection.V1beta1Connection, "example", bigqueryconnection.V1beta1ConnectionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1Connection } from "@ubx/sdk-google/google/bigqueryconnection/v1beta1_connection";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 connection" });

      resource(V1beta1Connection, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigqueryconnection.v1beta1_connection import V1beta1Connection, V1beta1ConnectionConfig

    def describe():
        ubx.intent("example own v1beta1 connection")

        ubx.resource(V1beta1Connection, "example", V1beta1ConnectionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigqueryconnection_v1beta1_connection called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="cloud_sql" type="object">
  Connection properties specific to the Cloud SQL. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="credential" type="object">
      Credential info for the Cloud SQL. Optional.
      <Expandable title="properties">
        <ResponseField name="password" type="string">
          The password for the credential. Optional.
        </ResponseField>
        <ResponseField name="username" type="string">
          The username for the credential. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="database" type="string">
      Database name. Optional.
    </ResponseField>
    <ResponseField name="instance_id" type="string">
      Cloud SQL instance ID in the form `project:location:instance`. Optional.
    </ResponseField>
    <ResponseField name="service_account_id" type="string">
      Output only. The account ID of the service used for the purpose of this connection. When the connection is used in the context of an operation in BigQuery, this service account will serve as the identity being used for connecting to the CloudSQL instance specified in this connection. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      Type of the Cloud SQL database. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_time" type="string">
  Output only. The creation timestamp of the connection. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  User provided description. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="friendly_name" type="string">
  User provided display name for the connection. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="has_credential" type="bool">
  Output only. True, if credential is configured for this connection. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Output only. The last update timestamp of the connection. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the connection in the form of: `projects/&#123;project_id&#125;/locations/&#123;location_id&#125;/connections/&#123;connection_id&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>
