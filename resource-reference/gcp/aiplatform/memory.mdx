---
title: "google_aiplatform_memory"
description: "`google_aiplatform_memory` is a single stored memory (a durable fact or preference extracted from past conversations) held in Vertex AI Agent Engine's..."
---

`google_aiplatform_memory` is a single stored memory (a durable fact or preference extracted from past conversations) held in Vertex AI Agent Engine's own Memory Bank, retrievable to personalize a future agent interaction.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own memory"})

    		ubx.Resource(aiplatform.Memory, "example", aiplatform.MemoryConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Memory } from "@ubx/sdk-google/google/aiplatform/memory";

    export default stack("example", () => {
      intent({ summary: "example own memory" });

      resource(Memory, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.aiplatform.memory import Memory, MemoryConfig

    def describe():
        ubx.intent("example own memory")

        ubx.resource(Memory, "example", MemoryConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_aiplatform_memory called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Represents the timestamp when this Memory was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Represents the description of the Memory. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="disable_memory_revisions" type="bool">
  Optional. Input only. Indicates whether no revision will be created for this request. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Represents the display name of the Memory. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Optional. Represents the timestamp of when this resource is considered expired. This is *always* provided on output when `expiration` is set on input, regardless of whether `expire_time` or `ttl` was provided. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fact" type="string">
  Optional. Represents semantic knowledge extracted from the source content. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="metadata" type="map(object)">
  Optional. Represents user-provided metadata for the Memory. This information was provided when creating, updating, or generating the Memory. It was not generated by Memory Bank. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="bool_value" type="bool">
      A `true`/`false` value, populated when this field's own type is boolean. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="double_value" type="number">
      A floating-point value, populated when this field's own type is a double. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="string_value" type="string">
      A text value, populated when this field's own type is a string. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="timestamp_value" type="string">
      A timestamp value, populated when this field's own type is a timestamp. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Represents the resource name of the Memory. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/reasoningEngines/&#123;reasoning_engine&#125;/memories/&#123;memory&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="revision_expire_time" type="string">
  Optional. Input only. Represents the timestamp of when the revision is considered expired. If not set, the memory revision will be kept until manually deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="revision_labels" type="map(string)">
  Optional. Input only. Represents the labels to apply to the Memory Revision created as a result of this request. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="revision_ttl" type="string">
  Optional. Input only. Represents the TTL for the revision. The expiration time is computed: now + TTL. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="scope" type="map(string)">
  Required. Immutable. Represents the scope of the Memory. Memories are isolated within their scope. The scope is defined when creating or generating memories. Scope values cannot contain the wildcard character '*'. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="topics" type="list(object)">
  Optional. Represents the Topics of the Memory. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="custom_memory_topic_label" type="string">
      The label identifying this caller-defined memory topic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="managed_memory_topic" type="string">
      One of Vertex AI's own predefined memory topics, e.g. `USER_PREFERENCES`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ttl" type="string">
  Optional. Input only. Represents the TTL for this resource. The expiration time is computed: now + TTL. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Represents the timestamp when this Memory was most recently updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
