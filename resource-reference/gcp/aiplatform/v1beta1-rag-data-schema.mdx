---
title: "google_aiplatform_v1beta1_rag_data_schema"
description: "`google_aiplatform_v1beta1_rag_data_schema` defines the real schema (schema_details) that data ingested into a Retrieval-Augmented Generation (RAG)..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. No stable, generally-available equivalent is published yet.
</Warning>

`google_aiplatform_v1beta1_rag_data_schema` defines the real schema (schema_details) that data ingested into a Retrieval-Augmented Generation (RAG) corpus must conform to, keyed by a real key, so a RAG pipeline can validate and structure incoming data consistently rather than accepting arbitrary shapes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_aiplatform_v1beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_aiplatform_v1beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google_aiplatform_v1beta1/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 rag data schema"})

    		ubx.Resource(aiplatform.V1beta1AiplatformV1beta1RagDataSchema, "example", aiplatform.V1beta1AiplatformV1beta1RagDataSchemaConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_aiplatform_v1beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1AiplatformV1beta1RagDataSchema } from "./local-sdk/google_aiplatform_v1beta1/sdk/typescript/google_aiplatform_v1beta1/aiplatform/v1beta1_aiplatform_v1beta1_rag_data_schema.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 rag data schema" });

      resource(V1beta1AiplatformV1beta1RagDataSchema, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_aiplatform_v1beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_aiplatform_v1beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_aiplatform_v1beta1.aiplatform import V1beta1AiplatformV1beta1RagDataSchema, V1beta1AiplatformV1beta1RagDataSchemaConfig

    def describe():
        ubx.intent("example own v1beta1 rag data schema")

        ubx.resource(V1beta1AiplatformV1beta1RagDataSchema, "example", V1beta1AiplatformV1beta1RagDataSchemaConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_aiplatform_v1beta1_rag_data_schema called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="key" type="string">
  Required. The key of this data schema. This key should be matching the key of user specified metadata and unique inside corpus. This value can be up to 63 characters, and valid characters are /a-z-/. The first character must be a letter, the last could be a letter or a number. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Resource name of the data schema in the form of: `projects/&#123;project_number&#125;/locations/&#123;location&#125;/ragCorpora/&#123;rag_corpus&#125;/ragDataSchemas/&#123;rag_data_schema&#125;` where the &#123;rag_data_schema&#125; part should be the same as the `key` field below. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="schema_details" type="object">
  Data schema details indicates the data type and the data struct corresponding to the key of user specified metadata. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="granularity" type="string">
      The granularity associated with this RagMetadataSchema. Optional.
    </ResponseField>
    <ResponseField name="list_config" type="object">
      Config for List data type. Optional.
      <Expandable title="properties">
        <ResponseField name="value_schema" type="any">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="search_strategy" type="object">
      The search strategy for the metadata value of the `key`. Optional.
      <Expandable title="properties">
        <ResponseField name="search_strategy_type" type="string">
          The search strategy type to be applied on the metadata key. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      Type of the metadata. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
