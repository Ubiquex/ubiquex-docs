---
title: "google_aiplatform_specialist_pool"
description: "`google_aiplatform_specialist_pool` defines a named pool of human reviewer(s) (Google-managed or customer-managed) available to a Vertex AI data..."
---

`google_aiplatform_specialist_pool` defines a named pool of human reviewer(s) (Google-managed or customer-managed) available to a Vertex AI data labeling job for annotating training data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_aiplatform --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_aiplatform/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google_aiplatform/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own specialist pool"})

    		ubx.Resource(aiplatform.AiplatformSpecialistPool, "ci-runner", aiplatform.AiplatformSpecialistPoolConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_aiplatform --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AiplatformSpecialistPool } from "./local-sdk/google_aiplatform/sdk/typescript/google_aiplatform/aiplatform/aiplatform_specialist_pool.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own specialist pool" });

      resource(AiplatformSpecialistPool, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_aiplatform --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_aiplatform/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_aiplatform.aiplatform import AiplatformSpecialistPool, AiplatformSpecialistPoolConfig

    def describe():
        ubx.intent("payments own specialist pool")

        ubx.resource(AiplatformSpecialistPool, "ci-runner", AiplatformSpecialistPoolConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_aiplatform_specialist_pool called "ci-runner", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="display_name" type="string">
  Required. The user-defined name of the SpecialistPool. The name can be up to 128 characters long and can consist of any UTF-8 characters. This field should be unique on project-level. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. The resource name of the SpecialistPool. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="pending_data_labeling_jobs" type="list(string)">
  Output only. The resource name of the pending data labeling jobs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="specialist_manager_emails" type="list(string)">
  The email addresses of the managers in the SpecialistPool. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="specialist_managers_count" type="number">
  Output only. The number of managers in this SpecialistPool. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="specialist_worker_emails" type="list(string)">
  The email addresses of workers in the SpecialistPool. Optional; if omitted, computed by Google Cloud.
</ResponseField>
