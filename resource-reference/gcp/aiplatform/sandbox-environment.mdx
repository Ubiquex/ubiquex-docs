---
title: "google_aiplatform_sandbox_environment"
description: "`google_aiplatform_sandbox_environment` is an isolated..."
---

`google_aiplatform_sandbox_environment` is an isolated, managed execution environment Vertex AI Agent Engine's Code Execution capability runs caller- or agent-generated code inside, so it can't affect anything outside its own sandbox.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sandbox environment"})

    		ubx.Resource(aiplatform.SandboxEnvironment, "example", aiplatform.SandboxEnvironmentConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SandboxEnvironment } from "@ubx/sdk-google/google/aiplatform/sandbox_environment";

    export default stack("example", () => {
      intent({ summary: "example own sandbox environment" });

      resource(SandboxEnvironment, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.aiplatform import SandboxEnvironment, SandboxEnvironmentConfig

    def describe():
        ubx.intent("example own sandbox environment")

        ubx.resource(SandboxEnvironment, "example", SandboxEnvironmentConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_aiplatform_sandbox_environment called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="connection_info" type="object">
  The connection information of the SandboxEnvironment. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="load_balancer_hostname" type="string">
      Output only. The hostname of the load balancer. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="load_balancer_ip" type="string">
      Output only. The IP address of the load balancer. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="routing_token" type="string">
      Output only. The routing token for the SandboxEnvironment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="sandbox_internal_ip" type="string">
      Output only. The internal IP address of the SandboxEnvironment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="service_attachment" type="string">
      Output only. The name of the PSC-E service attachment created for private ingress to this SandboxEnvironment. Only populated when the template enables private ingress (see SandboxEnvironmentTemplate.ingress_control_config). VPC-SC customers use this to create a PSC endpoint in their VPC. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp when this SandboxEnvironment was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The display name of the SandboxEnvironment. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Optional. Timestamp in UTC of when this SandboxEnvironment is considered expired. This is *always* provided on output, regardless of what `expiration` was sent on input. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="latest_sandbox_environment_snapshot" type="string">
  Output only. The resource name of the latest snapshot taken for this SandboxEnvironment. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The name of the SandboxEnvironment. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="owner" type="string">
  Optional. Owner information for this sandbox environment. A Sandbox can only be restored from a snapshot that belongs to the same owner. If not set, sandbox will be created as the default owner. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="sandbox_environment_snapshot" type="string">
  Optional. The resource name of the SandboxEnvironmentSnapshot to use for creating this SandboxEnvironment. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/reasoningEngines/&#123;reasoning_engine&#125;/sandboxEnvironmentSnapshots/&#123;sandbox_environment_snapshot&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="sandbox_environment_template" type="string">
  Optional. The name of the SandboxEnvironmentTemplate specified in the parent Agent Engine resource that this SandboxEnvironment is created from. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="spec" type="object">
  The specification of a SandboxEnvironment. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code_execution_environment" type="object">
      The code execution environment with customized settings. Optional.
      <Expandable title="properties">
        <ResponseField name="code_language" type="string">
          The coding language supported in this environment. Optional.
        </ResponseField>
        <ResponseField name="machine_config" type="string">
          The machine config of the code execution environment. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="shell_environment" type="any">
      The shell environment. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The runtime state of the SandboxEnvironment. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ttl" type="string">
  Optional. Input only. The TTL for the sandbox environment. The expiration time is computed: now + TTL. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when this SandboxEnvironment was most recently updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
