---
title: "google_aiplatform_v1beta1_serving_profile"
description: "`google_aiplatform_v1beta1_serving_profile` is a profile that controls how a model gets served -- its own encryption configuration (cmek_config) and..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. No stable, generally-available equivalent is published yet.
</Warning>

`google_aiplatform_v1beta1_serving_profile` is a profile that controls how a model gets served -- its own encryption configuration (cmek_config) and scope -- so serving behavior for a given deployment can be defined once and referenced, rather than repeated across every deployment that needs the same real serving policy.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 serving profile"})

    		ubx.Resource(aiplatform.V1beta1ServingProfile, "example", aiplatform.V1beta1ServingProfileConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1ServingProfile } from "@ubx/sdk-google/google/aiplatform/v1beta1_serving_profile";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 serving profile" });

      resource(V1beta1ServingProfile, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.aiplatform.v1beta1_serving_profile import V1beta1ServingProfile, V1beta1ServingProfileConfig

    def describe():
        ubx.intent("example own v1beta1 serving profile")

        ubx.resource(V1beta1ServingProfile, "example", V1beta1ServingProfileConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_aiplatform_v1beta1_serving_profile called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="cmek_config" type="object">
  Configuration for Customer-Managed Encryption Keys (CMEK). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="encryption_spec" type="object">
      Represents a customer-managed encryption key specification that can be applied to a Vertex AI resource. Optional.
      <Expandable title="properties">
        <ResponseField name="kms_key_name" type="string">
          Required. Resource name of the Cloud KMS key used to protect the resource. The Cloud KMS key must be in the same region as the resource. It must have the format `projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;key_ring&#125;/cryptoKeys/&#123;crypto_key&#125;`. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Timestamp when the ServingProfile was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. The description of the ServingProfile. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The display name of the ServingProfile. The name can be up to 128 characters long and can consist of any UTF-8 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the ServingProfile. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="scope" type="string">
  Required. The specific API this ServingProfile applies to. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when the ServingProfile was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
