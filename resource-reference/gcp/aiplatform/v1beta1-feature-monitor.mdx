---
title: "google_aiplatform_v1beta1_feature_monitor"
description: "`google_aiplatform_v1beta1_feature_monitor` monitors a real, selected set of features (feature_selection_config) on a schedule (schedule_config)..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. No stable, generally-available equivalent is published yet.
</Warning>

`google_aiplatform_v1beta1_feature_monitor` monitors a real, selected set of features (feature_selection_config) on a schedule (schedule_config), so feature drift or quality issues in a feature store surface on a real cadence instead of only being noticed when a downstream model's own predictions degrade.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_aiplatform_v1beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_aiplatform_v1beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google_aiplatform_v1beta1/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 feature monitor"})

    		ubx.Resource(aiplatform.V1beta1AiplatformV1beta1FeatureMonitor, "example", aiplatform.V1beta1AiplatformV1beta1FeatureMonitorConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_aiplatform_v1beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1AiplatformV1beta1FeatureMonitor } from "./local-sdk/google_aiplatform_v1beta1/sdk/typescript/google_aiplatform_v1beta1/aiplatform/v1beta1_aiplatform_v1beta1_feature_monitor.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 feature monitor" });

      resource(V1beta1AiplatformV1beta1FeatureMonitor, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_aiplatform_v1beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_aiplatform_v1beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_aiplatform_v1beta1.aiplatform import V1beta1AiplatformV1beta1FeatureMonitor, V1beta1AiplatformV1beta1FeatureMonitorConfig

    def describe():
        ubx.intent("example own v1beta1 feature monitor")

        ubx.resource(V1beta1AiplatformV1beta1FeatureMonitor, "example", V1beta1AiplatformV1beta1FeatureMonitorConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_aiplatform_v1beta1_feature_monitor called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp when this FeatureMonitor was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the FeatureMonitor. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. Used to perform consistent read-modify-write updates. If not set, a blind "overwrite" update happens. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="feature_selection_config" type="object">
  Feature selection configuration for the FeatureMonitor. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="feature_configs" type="list(object)">
      Optional. A list of features to be monitored and each feature's drift threshold. Optional.
      <Expandable title="properties">
        <ResponseField name="drift_threshold" type="number">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="feature_id" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. The labels with user-defined metadata to organize your FeatureMonitor. Label keys and values can be no longer than 64 characters (Unicode codepoints), can only contain lowercase letters, numeric characters, underscores and dashes. International characters are allowed. See https://goo.gl/xmQnxf for more information on and examples of labels. No more than 64 user labels can be associated with one FeatureMonitor(System labels are excluded)." System reserved label keys are prefixed with "aiplatform.googleapis.com/" and are immutable. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Name of the FeatureMonitor. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/featureGroups/&#123;featureGroup&#125;/featureMonitors/&#123;featureMonitor&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="schedule_config" type="object">
  Schedule configuration for the FeatureMonitor. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cron" type="string">
      Cron schedule (https://en.wikipedia.org/wiki/Cron) to launch scheduled runs. To explicitly set a timezone to the cron tab, apply a prefix in the cron tab: "CRON_TZ=$&#123;IANA_TIME_ZONE&#125;" or "TZ=$&#123;IANA_TIME_ZONE&#125;". The $&#123;IANA_TIME_ZONE&#125; may only be a valid string from IANA time zone database. For example, "CRON_TZ=America/New_York 1 * * * *", or "TZ=America/New_York 1 * * * *". Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when this FeatureMonitor was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
