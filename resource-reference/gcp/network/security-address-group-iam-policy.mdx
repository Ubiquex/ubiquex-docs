---
title: "google_network_security_address_group_iam_policy"
description: "Real, generated bindings for `google_network_security_address_group_iam_policy`."
---

`google_network_security_address_group_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/network"
    )

    cfg := network.SecurityAddressGroupIamPolicyConfig{
        Name: "example-name",
        PolicyData: "example",
    }
    ubx.Resource(network.SecurityAddressGroupIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SecurityAddressGroupIamPolicy } from
      "jsr:@ubx/sdk-google/google/network/security_address_group_iam_policy";

    resource(SecurityAddressGroupIamPolicy, "example", {
      name: "example-name",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.network import (
        SecurityAddressGroupIamPolicy,
        SecurityAddressGroupIamPolicyConfig,
    )

    cfg = SecurityAddressGroupIamPolicyConfig(
        name="example-name",
        policy_data="example",
    )
    ubx.resource(SecurityAddressGroupIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_network_security_address_group_iam_policy with
name, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
