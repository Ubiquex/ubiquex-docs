---
title: "google_network_connectivity_gateway_advertised_route"
description: "Real, generated bindings for `google_network_connectivity_gateway_advertised_route`."
---

`google_network_connectivity_gateway_advertised_route` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connectivity gateway advertised route"})

    		ubx.Resource(network.ConnectivityGatewayAdvertisedRoute, "ci-runner", network.ConnectivityGatewayAdvertisedRouteConfig{
    			Location:    "example",
    			Name:        "ci-runner",
    			Spoke:       "example",
    			Description: "Managed by ubx.",
    			IpRange:     "example",
    			Labels:      map[string]string{"managed-by": "ubx"},
    			Priority:    1,
    			Recipient:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectivityGatewayAdvertisedRoute } from "jsr:@ubx/sdk-google/google/network/connectivity_gateway_advertised_route";

    export default stack("payments", () => {
      intent({ summary: "payments own connectivity gateway advertised route" });

      resource(ConnectivityGatewayAdvertisedRoute, "ci-runner", {
        location: "example",
        name: "ci-runner",
        spoke: "example",
        description: "Managed by ubx.",
        ipRange: "example",
        labels: { "managed-by": "ubx" },
        priority: 1,
        recipient: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import ConnectivityGatewayAdvertisedRoute, ConnectivityGatewayAdvertisedRouteConfig

    def describe():
        ubx.intent("payments own connectivity gateway advertised route")

        ubx.resource(ConnectivityGatewayAdvertisedRoute, "ci-runner", ConnectivityGatewayAdvertisedRouteConfig(
            location="example",
            name="ci-runner",
            spoke="example",
            description="Managed by ubx.",
            ip_range="example",
            labels={"managed-by": "ubx"},
            priority=1,
            recipient="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_connectivity_gateway_advertised_route
    called "ci-runner", with location example, spoke example,
    description Managed by ubx., ip range example, labels a set
    value, priority 1, recipient example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of the gateway advertised route. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_range" type="string">
  This route's advertised IP address range. Must be a valid CIDR-formatted prefix. If an IP address is provided without a subnet mask, it is interpreted as, for IPv4, a /32 singular IP address range, and, for IPv6, /128 Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional labels in key:value format. For more information about labels, see [Requirements for labels](https://docs.cloud.google.com/resource-manager/docs/creating-managing-labels#requirements). **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the resource Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the gateway advertised route. Route names must be unique. Required.
</ResponseField>

<ResponseField name="priority" type="number">
  The priority of this advertised route. You can choose a value from 0 to 65335. If you don't provide a value, Google Cloud assigns a priority of 100 to the ranges. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="recipient" type="string">
  the recipient of this advertised route Possible values: ["RECIPIENT_UNSPECIFIED", "ADVERTISE_TO_HUB"] Optional.
</ResponseField>

<ResponseField name="spoke" type="string" required>
  The name of the spoke Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time the gateway advertised route was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The current lifecycle state of this gateway advertised route. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="unique_id" type="string">
  The Google-generated UUID for the gateway advertised route. This value is unique across all gateway advertised route resources. If a gateway advertised route is deleted and another with the same name is created, the new route is assigned a different uniqueId. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  The time the gateway advertised route was last updated. Computed by GCP after `ship`.
</ResponseField>
