---
title: "google_network_services_multicast_domain"
description: "Real, generated bindings for `google_network_services_multicast_domain`."
---

`google_network_services_multicast_domain` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own services multicast domain"})

    		ubx.Resource(network.ServicesMulticastDomain, "ci-runner", network.ServicesMulticastDomainConfig{
    			AdminNetwork:         "example",
    			Location:             "example",
    			MulticastDomainId:    "example",
    			Description:          "Managed by ubx.",
    			Labels:               map[string]string{"managed-by": "ubx"},
    			MulticastDomainGroup: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicesMulticastDomain } from "jsr:@ubx/sdk-google/google/network/services_multicast_domain";

    export default stack("payments", () => {
      intent({ summary: "payments own services multicast domain" });

      resource(ServicesMulticastDomain, "ci-runner", {
        adminNetwork: "example",
        location: "example",
        multicastDomainId: "example",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
        multicastDomainGroup: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import ServicesMulticastDomain, ServicesMulticastDomainConfig

    def describe():
        ubx.intent("payments own services multicast domain")

        ubx.resource(ServicesMulticastDomain, "ci-runner", ServicesMulticastDomainConfig(
            admin_network="example",
            location="example",
            multicast_domain_id="example",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
            multicast_domain_group="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_services_multicast_domain, with admin
    network example, location example, multicast domain id example,
    description Managed by ubx., labels a set value, multicast domain
    group example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="admin_network" type="string" required>
  The resource name of the multicast admin VPC network. Use the following format: 'projects/&#123;project&#125;/locations/global/networks/&#123;network&#125;'. Required.
</ResponseField>

<ResponseField name="connection_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="connection_type" type="string" required>
    </ResponseField>
    <ResponseField name="ncc_hub" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional text description of the multicast domain. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key-value pairs. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="multicast_domain_group" type="string">
  The multicast domain group this domain should be associated with. Use the following format: 'projects/&#123;project&#125;/locations/global/multicastDomainGroups/&#123;multicast_domain_group&#125;'. Optional.
</ResponseField>

<ResponseField name="multicast_domain_id" type="string" required>
  A unique name for the multicast domain. The name is restricted to letters, numbers, and hyphen, with the first character a letter, and the last a letter or a number. The name must not exceed 48 characters. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ull_multicast_domain" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="preconfigured_ull_domain" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="connection_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="connection_type" type="string" required>
    </ResponseField>
    <ResponseField name="ncc_hub" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  The timestamp when the multicast domain was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the multicast domain. Use the following format: 'projects/*/locations/global/multicastDomains/*' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="list(object)">
  The multicast resource's state. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ull_multicast_domain" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="preconfigured_ull_domain" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="unique_id" type="string">
  The Google-generated UUID for the resource. This value is unique across all multicast domain resources. If a domain is deleted and another with the same name is created, the new domain is assigned a different unique_id. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  The timestamp when the multicast domain was most recently updated. Computed by GCP after `ship`.
</ResponseField>
