---
title: "google_network_security_mirroring_endpoint_group_association"
description: "Real, generated bindings for `google_network_security_mirroring_endpoint_group_association`."
---

`google_network_security_mirroring_endpoint_group_association` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/network"
    )

    cfg := network.SecurityMirroringEndpointGroupAssociationConfig{
        Location: "example",
        MirroringEndpointGroup: "example",
        Network: "example",
        Labels: map[string]string{"key": "value"},
        MirroringEndpointGroupAssociationId: "example",
    }
    ubx.Resource(network.SecurityMirroringEndpointGroupAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SecurityMirroringEndpointGroupAssociation } from
      "jsr:@ubx/sdk-google/google/network/security_mirroring_endpoint_group_association";

    resource(SecurityMirroringEndpointGroupAssociation, "example", {
      location: "example",
      mirroringEndpointGroup: "example",
      network: "example",
      labels: { key: "value" },
      mirroringEndpointGroupAssociationId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.network import (
        SecurityMirroringEndpointGroupAssociation,
        SecurityMirroringEndpointGroupAssociationConfig,
    )

    cfg = SecurityMirroringEndpointGroupAssociationConfig(
        location="example",
        mirroring_endpoint_group="example",
        network="example",
        labels={"key": "value"},
        mirroring_endpoint_group_association_id="example",
    )
    ubx.resource(SecurityMirroringEndpointGroupAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision
google_network_security_mirroring_endpoint_group_association with
location, mirroring_endpoint_group, network, labels,
mirroring_endpoint_group_association_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="mirroring_endpoint_group" type="string" required>
  Required.
</ResponseField>

<ResponseField name="mirroring_endpoint_group_association_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="network" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="locations" type="set(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="location" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="locations_details" type="list(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="location" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
