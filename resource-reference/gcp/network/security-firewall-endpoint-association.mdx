---
title: "google_network_security_firewall_endpoint_association"
description: "Real, generated bindings for `google_network_security_firewall_endpoint_association`."
---

`google_network_security_firewall_endpoint_association` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security firewall endpoint association"})

    		ubx.Resource(network.SecurityFirewallEndpointAssociation, "ci-runner", network.SecurityFirewallEndpointAssociationConfig{
    			FirewallEndpoint:    "example",
    			Location:            "example",
    			Name:                "ci-runner",
    			Network:             "example",
    			Disabled:            true,
    			Labels:              map[string]string{"managed-by": "ubx"},
    			Parent:              "example",
    			TlsInspectionPolicy: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityFirewallEndpointAssociation } from "jsr:@ubx/sdk-google/google/network/security_firewall_endpoint_association";

    export default stack("payments", () => {
      intent({ summary: "payments own security firewall endpoint association" });

      resource(SecurityFirewallEndpointAssociation, "ci-runner", {
        firewallEndpoint: "example",
        location: "example",
        name: "ci-runner",
        network: "example",
        disabled: true,
        labels: { "managed-by": "ubx" },
        parent: "example",
        tlsInspectionPolicy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import SecurityFirewallEndpointAssociation, SecurityFirewallEndpointAssociationConfig

    def describe():
        ubx.intent("payments own security firewall endpoint association")

        ubx.resource(SecurityFirewallEndpointAssociation, "ci-runner", SecurityFirewallEndpointAssociationConfig(
            firewall_endpoint="example",
            location="example",
            name="ci-runner",
            network="example",
            disabled=True,
            labels={"managed-by": "ubx"},
            parent="example",
            tls_inspection_policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_security_firewall_endpoint_association
    called "ci-runner", with firewall endpoint example, location
    example, network example, disabled true, labels a set value,
    parent example, tls inspection policy example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Whether the association is disabled. True indicates that traffic will not be intercepted. ~&gt; **Note:** The API will reject the request if this value is set to true when creating the resource, otherwise on an update the association can be disabled. Optional.
</ResponseField>

<ResponseField name="firewall_endpoint" type="string" required>
  The URL of the firewall endpoint that is being associated. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  A map of key/value label pairs to assign to the resource. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location (zone) of the firewall endpoint association. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the firewall endpoint association resource. Required.
</ResponseField>

<ResponseField name="network" type="string" required>
  The URL of the network that is being associated. Required.
</ResponseField>

<ResponseField name="parent" type="string">
  The name of the parent this firewall endpoint association belongs to. Format: projects/&#123;project_id&#125;. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tls_inspection_policy" type="string">
  The URL of the TlsInspectionPolicy that is being associated. Optional.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Time the firewall endpoint was created in UTC. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Whether reconciling is in progress, recommended per https://google.aip.dev/128. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="self_link" type="string">
  Server-defined URL of this resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the endpoint. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Time the firewall endpoint was updated in UTC. Computed by GCP after `ship`.
</ResponseField>
