---
title: "google_network_security_mirroring_endpoint_group"
description: "Real, generated bindings for `google_network_security_mirroring_endpoint_group`."
---

`google_network_security_mirroring_endpoint_group` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security mirroring endpoint group"})

    		ubx.Resource(network.SecurityMirroringEndpointGroup, "ci-runner", network.SecurityMirroringEndpointGroupConfig{
    			Location:                  "example",
    			MirroringEndpointGroupId:  "example",
    			Description:               "Managed by ubx.",
    			Labels:                    map[string]string{"managed-by": "ubx"},
    			MirroringDeploymentGroup:  "example",
    			MirroringDeploymentGroups: []string{"example"},
    			Type:                      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityMirroringEndpointGroup } from "jsr:@ubx/sdk-google/google/network/security_mirroring_endpoint_group";

    export default stack("payments", () => {
      intent({ summary: "payments own security mirroring endpoint group" });

      resource(SecurityMirroringEndpointGroup, "ci-runner", {
        location: "example",
        mirroringEndpointGroupId: "example",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
        mirroringDeploymentGroup: "example",
        mirroringDeploymentGroups: ["example"],
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import SecurityMirroringEndpointGroup, SecurityMirroringEndpointGroupConfig

    def describe():
        ubx.intent("payments own security mirroring endpoint group")

        ubx.resource(SecurityMirroringEndpointGroup, "ci-runner", SecurityMirroringEndpointGroupConfig(
            location="example",
            mirroring_endpoint_group_id="example",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
            mirroring_deployment_group="example",
            mirroring_deployment_groups=["example"],
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_security_mirroring_endpoint_group, with
    location example, mirroring endpoint group id example,
    description Managed by ubx., labels a set value, mirroring
    deployment group example, mirroring deployment groups a set
    value, type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  User-provided description of the endpoint group. Used as additional context for the endpoint group. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels are key/value pairs that help to organize and filter resources. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The cloud location of the endpoint group, currently restricted to 'global'. Required.
</ResponseField>

<ResponseField name="mirroring_deployment_group" type="string">
  The deployment group that this DIRECT endpoint group is connected to, for example: 'projects/123456789/locations/global/mirroringDeploymentGroups/my-dg'. See https://google.aip.dev/124. Optional.
</ResponseField>

<ResponseField name="mirroring_deployment_groups" type="list(string)">
  A list of the deployment groups that this BROKER endpoint group is connected to, for example: 'projects/123456789/locations/global/mirroringDeploymentGroups/my-dg'. See https://google.aip.dev/124. Optional.
</ResponseField>

<ResponseField name="mirroring_endpoint_group_id" type="string" required>
  The ID to use for the endpoint group, which will become the final component of the endpoint group's resource name. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of the endpoint group. If left unspecified, defaults to DIRECT. Possible values: DIRECT BROKER Optional.
</ResponseField>

## Output properties

<ResponseField name="associations" type="set(object)">
  List of associations to this endpoint group. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="network" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="connected_deployment_groups" type="set(object)">
  List of details about the connected deployment groups to this endpoint group. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="locations" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="location" type="string">
        </ResponseField>
        <ResponseField name="state" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  The timestamp when the resource was created. See https://google.aip.dev/148#timestamps. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of this endpoint group, for example: 'projects/123456789/locations/global/mirroringEndpointGroups/my-eg'. See https://google.aip.dev/122 for more details. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  The current state of the resource does not match the user's intended state, and the system is working to reconcile them. This is part of the normal operation (e.g. adding a new association to the group). See https://google.aip.dev/128. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the endpoint group. See https://google.aip.dev/216. Possible values: STATE_UNSPECIFIED ACTIVE CLOSED CREATING DELETING OUT_OF_SYNC DELETE_FAILED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The timestamp when the resource was most recently updated. See https://google.aip.dev/148#timestamps. Computed by GCP after `ship`.
</ResponseField>
