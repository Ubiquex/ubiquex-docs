---
title: "google_network_management_vpc_flow_logs_config"
description: "Real, generated bindings for `google_network_management_vpc_flow_logs_config`."
---

`google_network_management_vpc_flow_logs_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own management vpc flow logs config"})

    		ubx.Resource(network.ManagementVpcFlowLogsConfig, "ci-runner", network.ManagementVpcFlowLogsConfigConfig{
    			Location:               "example",
    			VpcFlowLogsConfigId:    "example",
    			Description:            "Managed by ubx.",
    			FilterExpr:             "example",
    			InterconnectAttachment: "example",
    			Labels:                 map[string]string{"managed-by": "ubx"},
    			MetadataFields:         []string{"example"},
    			Network:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagementVpcFlowLogsConfig } from "jsr:@ubx/sdk-google/google/network/management_vpc_flow_logs_config";

    export default stack("payments", () => {
      intent({ summary: "payments own management vpc flow logs config" });

      resource(ManagementVpcFlowLogsConfig, "ci-runner", {
        location: "example",
        vpcFlowLogsConfigId: "example",
        description: "Managed by ubx.",
        filterExpr: "example",
        interconnectAttachment: "example",
        labels: { "managed-by": "ubx" },
        metadataFields: ["example"],
        network: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import ManagementVpcFlowLogsConfig, ManagementVpcFlowLogsConfigConfig

    def describe():
        ubx.intent("payments own management vpc flow logs config")

        ubx.resource(ManagementVpcFlowLogsConfig, "ci-runner", ManagementVpcFlowLogsConfigConfig(
            location="example",
            vpc_flow_logs_config_id="example",
            description="Managed by ubx.",
            filter_expr="example",
            interconnect_attachment="example",
            labels={"managed-by": "ubx"},
            metadata_fields=["example"],
            network="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_management_vpc_flow_logs_config, with
    location example, vpc flow logs config id example, description
    Managed by ubx., filter expr example, interconnect attachment
    example, labels a set value, metadata fields a set value, network
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aggregation_interval" type="string">
  Optional. The aggregation interval for the logs. Default value is INTERVAL_5_SEC. Possible values: AGGREGATION_INTERVAL_UNSPECIFIED INTERVAL_5_SEC INTERVAL_30_SEC INTERVAL_1_MIN INTERVAL_5_MIN INTERVAL_10_MIN INTERVAL_15_MIN Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. The user-supplied description of the VPC Flow Logs configuration. Maximum of 512 characters. Optional.
</ResponseField>

<ResponseField name="filter_expr" type="string">
  Optional. Export filter used to define which VPC Flow Logs should be logged. Optional.
</ResponseField>

<ResponseField name="flow_sampling" type="number">
  Optional. The value of the field must be in (0, 1]. The sampling rate of VPC Flow Logs where 1.0 means all collected logs are reported. Setting the sampling rate to 0.0 is not allowed. If you want to disable VPC Flow Logs, use the state field instead. Default value is 1.0. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="interconnect_attachment" type="string">
  Traffic will be logged from the Interconnect Attachment. Format: projects/&#123;project_id&#125;/regions/&#123;region&#125;/interconnectAttachments/&#123;name&#125; Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Resource labels to represent user-provided metadata. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. See documentation for resource type 'networkmanagement.googleapis.com/VpcFlowLogsConfig'. Required.
</ResponseField>

<ResponseField name="metadata" type="string">
  Optional. Configures whether all, none or a subset of metadata fields should be added to the reported VPC flow logs. Default value is INCLUDE_ALL_METADATA. Possible values: METADATA_UNSPECIFIED INCLUDE_ALL_METADATA EXCLUDE_ALL_METADATA CUSTOM_METADATA Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="metadata_fields" type="list(string)">
  Optional. Custom metadata fields to include in the reported VPC flow logs. Can only be specified if \"metadata\" was set to CUSTOM_METADATA. Optional.
</ResponseField>

<ResponseField name="network" type="string">
  Traffic will be logged from VMs, VPN tunnels and Interconnect Attachments within the network. Format: projects/&#123;project_id&#125;/global/networks/&#123;name&#125; Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Optional. The state of the VPC Flow Log configuration. Default value is ENABLED. When creating a new configuration, it must be enabled. Possible values: STATE_UNSPECIFIED ENABLED DISABLED Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="subnet" type="string">
  Traffic will be logged from VMs within the subnetwork. Format: projects/&#123;project_id&#125;/regions/&#123;region&#125;/subnetworks/&#123;name&#125; Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_flow_logs_config_id" type="string" required>
  Required. ID of the 'VpcFlowLogsConfig'. Required.
</ResponseField>

<ResponseField name="vpn_tunnel" type="string">
  Traffic will be logged from the VPN Tunnel. Format: projects/&#123;project_id&#125;/regions/&#123;region&#125;/vpnTunnels/&#123;name&#125; Optional.
</ResponseField>

## Output properties

<ResponseField name="aggregation_interval" type="string">
  Optional. The aggregation interval for the logs. Default value is INTERVAL_5_SEC. Possible values: AGGREGATION_INTERVAL_UNSPECIFIED INTERVAL_5_SEC INTERVAL_30_SEC INTERVAL_1_MIN INTERVAL_5_MIN INTERVAL_10_MIN INTERVAL_15_MIN Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time the config was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="flow_sampling" type="number">
  Optional. The value of the field must be in (0, 1]. The sampling rate of VPC Flow Logs where 1.0 means all collected logs are reported. Setting the sampling rate to 0.0 is not allowed. If you want to disable VPC Flow Logs, use the state field instead. Default value is 1.0. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="metadata" type="string">
  Optional. Configures whether all, none or a subset of metadata fields should be added to the reported VPC flow logs. Default value is INCLUDE_ALL_METADATA. Possible values: METADATA_UNSPECIFIED INCLUDE_ALL_METADATA EXCLUDE_ALL_METADATA CUSTOM_METADATA Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Unique name of the configuration using the form: 'projects/&#123;project_id&#125;/locations/global/vpcFlowLogsConfigs/&#123;vpc_flow_logs_config_id&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Optional. The state of the VPC Flow Log configuration. Default value is ENABLED. When creating a new configuration, it must be enabled. Possible values: STATE_UNSPECIFIED ENABLED DISABLED Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_resource_state" type="string">
  Describes the state of the configured target resource for diagnostic purposes. Possible values: TARGET_RESOURCE_STATE_UNSPECIFIED TARGET_RESOURCE_EXISTS TARGET_RESOURCE_DOES_NOT_EXIST Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time the config was updated. Computed by GCP after `ship`.
</ResponseField>
