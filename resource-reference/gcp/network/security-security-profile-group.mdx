---
title: "google_network_security_security_profile_group"
description: "Real, generated bindings for `google_network_security_security_profile_group`."
---

`google_network_security_security_profile_group` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security security profile group"})

    		ubx.Resource(network.SecuritySecurityProfileGroup, "ci-runner", network.SecuritySecurityProfileGroupConfig{
    			Name:                    "ci-runner",
    			CustomInterceptProfile:  "example",
    			CustomMirroringProfile:  "example",
    			Description:             "Managed by ubx.",
    			Labels:                  map[string]string{"managed-by": "ubx"},
    			Location:                "example",
    			Parent:                  "example",
    			ThreatPreventionProfile: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecuritySecurityProfileGroup } from "jsr:@ubx/sdk-google/google/network/security_security_profile_group";

    export default stack("payments", () => {
      intent({ summary: "payments own security security profile group" });

      resource(SecuritySecurityProfileGroup, "ci-runner", {
        name: "ci-runner",
        customInterceptProfile: "example",
        customMirroringProfile: "example",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
        location: "example",
        parent: "example",
        threatPreventionProfile: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import SecuritySecurityProfileGroup, SecuritySecurityProfileGroupConfig

    def describe():
        ubx.intent("payments own security security profile group")

        ubx.resource(SecuritySecurityProfileGroup, "ci-runner", SecuritySecurityProfileGroupConfig(
            name="ci-runner",
            custom_intercept_profile="example",
            custom_mirroring_profile="example",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
            location="example",
            parent="example",
            threat_prevention_profile="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_security_security_profile_group called
    "ci-runner", with custom intercept profile example, custom
    mirroring profile example, description Managed by ubx., labels a
    set value, location example, parent example, threat prevention
    profile example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="custom_intercept_profile" type="string">
  Reference to a SecurityProfile with the CustomIntercept configuration. Optional.
</ResponseField>

<ResponseField name="custom_mirroring_profile" type="string">
  Reference to a SecurityProfile with the custom mirroring configuration for the SecurityProfileGroup. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of the profile. The Max length is 512 characters. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  A map of key/value label pairs to assign to the resource. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string">
  The location of the security profile group. The default value is 'global'. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the security profile group resource. Required.
</ResponseField>

<ResponseField name="parent" type="string">
  The name of the parent this security profile group belongs to. Format: 'organizations/&#123;organization_id&#125;' or 'projects/&#123;project_id&#125;'. Optional.
</ResponseField>

<ResponseField name="threat_prevention_profile" type="string">
  Reference to a SecurityProfile with the threat prevention configuration for the SecurityProfileGroup. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="url_filtering_profile" type="string">
  Reference to a SecurityProfile with the URL filtering configuration for the SecurityProfileGroup. Optional.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Time the security profile group was created in UTC. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Time the security profile group was updated in UTC. Computed by GCP after `ship`.
</ResponseField>
