---
title: "google_network_security_firewall_endpoint"
description: "Real, generated bindings for `google_network_security_firewall_endpoint`."
---

`google_network_security_firewall_endpoint` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/network"
    )

    cfg := network.SecurityFirewallEndpointConfig{
        Location: "example",
        Name: "example-name",
        Parent: "example",
        Labels: map[string]string{"key": "value"},
    }
    ubx.Resource(network.SecurityFirewallEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SecurityFirewallEndpoint } from
      "jsr:@ubx/sdk-google/google/network/security_firewall_endpoint";

    resource(SecurityFirewallEndpoint, "example", {
      location: "example",
      name: "example-name",
      parent: "example",
      labels: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.network import (
        SecurityFirewallEndpoint,
        SecurityFirewallEndpointConfig,
    )

    cfg = SecurityFirewallEndpointConfig(
        location="example",
        name="example-name",
        parent="example",
        labels={"key": "value"},
    )
    ubx.resource(SecurityFirewallEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_network_security_firewall_endpoint with
location, name, parent, labels.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="billing_project_id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="endpoint_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="jumbo_frames_enabled" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="associated_networks" type="list(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="billing_project_id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="endpoint_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="jumbo_frames_enabled" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
