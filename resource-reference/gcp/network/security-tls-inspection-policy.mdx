---
title: "google_network_security_tls_inspection_policy"
description: "Real, generated bindings for `google_network_security_tls_inspection_policy`."
---

`google_network_security_tls_inspection_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security tls inspection policy"})

    		ubx.Resource(network.SecurityTlsInspectionPolicy, "ci-runner", network.SecurityTlsInspectionPolicyConfig{
    			CaPool:             "example",
    			Name:               "ci-runner",
    			CustomTlsFeatures:  []string{"example"},
    			Description:        "Managed by ubx.",
    			ExcludePublicCaSet: true,
    			Location:           "example",
    			MinTlsVersion:      "example",
    			TlsFeatureProfile:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityTlsInspectionPolicy } from "jsr:@ubx/sdk-google/google/network/security_tls_inspection_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own security tls inspection policy" });

      resource(SecurityTlsInspectionPolicy, "ci-runner", {
        caPool: "example",
        name: "ci-runner",
        customTlsFeatures: ["example"],
        description: "Managed by ubx.",
        excludePublicCaSet: true,
        location: "example",
        minTlsVersion: "example",
        tlsFeatureProfile: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import SecurityTlsInspectionPolicy, SecurityTlsInspectionPolicyConfig

    def describe():
        ubx.intent("payments own security tls inspection policy")

        ubx.resource(SecurityTlsInspectionPolicy, "ci-runner", SecurityTlsInspectionPolicyConfig(
            ca_pool="example",
            name="ci-runner",
            custom_tls_features=["example"],
            description="Managed by ubx.",
            exclude_public_ca_set=True,
            location="example",
            min_tls_version="example",
            tls_feature_profile="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_security_tls_inspection_policy called
    "ci-runner", with ca pool example, custom tls features a set
    value, description Managed by ubx., exclude public ca set true,
    location example, min tls version example, tls feature profile
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="ca_pool" type="string" required>
  Required.
</ResponseField>

<ResponseField name="custom_tls_features" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="exclude_public_ca_set" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional.
</ResponseField>

<ResponseField name="min_tls_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tls_feature_profile" type="string">
  Optional.
</ResponseField>

<ResponseField name="trust_config" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
