---
title: "google_network_connectivity_hub_iam_policy"
description: "Real, generated bindings for `google_network_connectivity_hub_iam_policy`."
---

`google_network_connectivity_hub_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connectivity hub iam policy"})

    		ubx.Resource(network.ConnectivityHubIamPolicy, "ci-runner", network.ConnectivityHubIamPolicyConfig{
    			Hub:        "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectivityHubIamPolicy } from "jsr:@ubx/sdk-google/google/network/connectivity_hub_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own connectivity hub iam policy" });

      resource(ConnectivityHubIamPolicy, "ci-runner", {
        hub: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import ConnectivityHubIamPolicy, ConnectivityHubIamPolicyConfig

    def describe():
        ubx.intent("payments own connectivity hub iam policy")

        ubx.resource(ConnectivityHubIamPolicy, "ci-runner", ConnectivityHubIamPolicyConfig(
            hub="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_connectivity_hub_iam_policy, with hub
    example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="hub" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
