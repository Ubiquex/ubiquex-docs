---
title: "google_network_connectivity_hub_iam_policy"
description: "Real, generated bindings for `google_network_connectivity_hub_iam_policy`."
---

`google_network_connectivity_hub_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    cfg := network.ConnectivityHubIamPolicyConfig{
        Hub: "example",
        PolicyData: "example",
    }
    ubx.Resource(network.ConnectivityHubIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConnectivityHubIamPolicy } from
      "jsr:@ubx/sdk-google/google/network/connectivity_hub_iam_policy";

    resource(ConnectivityHubIamPolicy, "example", {
      hub: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.network import (
        ConnectivityHubIamPolicy,
        ConnectivityHubIamPolicyConfig,
    )

    cfg = ConnectivityHubIamPolicyConfig(
        hub="example",
        policy_data="example",
    )
    ubx.resource(ConnectivityHubIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_network_connectivity_hub_iam_policy with hub,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="hub" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
