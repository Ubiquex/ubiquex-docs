---
title: "google_network_security_address_group"
description: "Real, generated bindings for `google_network_security_address_group`."
---

`google_network_security_address_group` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security address group"})

    		ubx.Resource(network.SecurityAddressGroup, "ci-runner", network.SecurityAddressGroupConfig{
    			Capacity:    1,
    			Location:    "example",
    			Name:        "ci-runner",
    			Type:        "example",
    			Description: "Managed by ubx.",
    			Items:       []string{"example"},
    			Labels:      map[string]string{"managed-by": "ubx"},
    			Parent:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityAddressGroup } from "jsr:@ubx/sdk-google/google/network/security_address_group";

    export default stack("payments", () => {
      intent({ summary: "payments own security address group" });

      resource(SecurityAddressGroup, "ci-runner", {
        capacity: 1,
        location: "example",
        name: "ci-runner",
        type: "example",
        description: "Managed by ubx.",
        items: ["example"],
        labels: { "managed-by": "ubx" },
        parent: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import SecurityAddressGroup, SecurityAddressGroupConfig

    def describe():
        ubx.intent("payments own security address group")

        ubx.resource(SecurityAddressGroup, "ci-runner", SecurityAddressGroupConfig(
            capacity=1,
            location="example",
            name="ci-runner",
            type="example",
            description="Managed by ubx.",
            items=["example"],
            labels={"managed-by": "ubx"},
            parent="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_security_address_group called "ci-runner",
    with capacity 1, location example, type example, description
    Managed by ubx., items a set value, labels a set value, parent
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="capacity" type="number" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="items" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
