---
title: "google_network_security_dns_threat_detector"
description: "Real, generated bindings for `google_network_security_dns_threat_detector`."
---

`google_network_security_dns_threat_detector` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security dns threat detector"})

    		ubx.Resource(network.SecurityDnsThreatDetector, "ci-runner", network.SecurityDnsThreatDetectorConfig{
    			Name:                   "ci-runner",
    			ExcludedNetworks:       []string{"example"},
    			Labels:                 map[string]string{"managed-by": "ubx"},
    			Location:               "example",
    			ThreatDetectorProvider: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityDnsThreatDetector } from "jsr:@ubx/sdk-google/google/network/security_dns_threat_detector";

    export default stack("payments", () => {
      intent({ summary: "payments own security dns threat detector" });

      resource(SecurityDnsThreatDetector, "ci-runner", {
        name: "ci-runner",
        excludedNetworks: ["example"],
        labels: { "managed-by": "ubx" },
        location: "example",
        threatDetectorProvider: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import SecurityDnsThreatDetector, SecurityDnsThreatDetectorConfig

    def describe():
        ubx.intent("payments own security dns threat detector")

        ubx.resource(SecurityDnsThreatDetector, "ci-runner", SecurityDnsThreatDetectorConfig(
            name="ci-runner",
            excluded_networks=["example"],
            labels={"managed-by": "ubx"},
            location="example",
            threat_detector_provider="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_security_dns_threat_detector called
    "ci-runner", with excluded networks a set value, labels a set
    value, location example, threat detector provider example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="excluded_networks" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="location" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="threat_detector_provider" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
