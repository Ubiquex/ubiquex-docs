---
title: "google_analyticshub_query_template"
description: "`google_analyticshub_query_template` is a saved, reusable SQL query attached to a BigQuery Analytics Hub data exchange or listing..."
---

`google_analyticshub_query_template` is a saved, reusable SQL query attached to a BigQuery Analytics Hub data exchange or listing, published so subscribers can run a pre-built, provider-authored query against the shared dataset instead of writing their own.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_analyticshub --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_analyticshub/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	analyticshub "github.com/ubiquex/ubx-sdk-google/sdk/go/google_analyticshub/analyticshub"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own query template"})

    		ubx.Resource(analyticshub.AnalyticshubQueryTemplate, "ci-runner", analyticshub.AnalyticshubQueryTemplateConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_analyticshub --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnalyticshubQueryTemplate } from "./local-sdk/google_analyticshub/sdk/typescript/google_analyticshub/analyticshub/analyticshub_query_template.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own query template" });

      resource(AnalyticshubQueryTemplate, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_analyticshub --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_analyticshub/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_analyticshub.analyticshub import AnalyticshubQueryTemplate, AnalyticshubQueryTemplateConfig

    def describe():
        ubx.intent("payments own query template")

        ubx.resource(AnalyticshubQueryTemplate, "ci-runner", AnalyticshubQueryTemplateConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_analyticshub_query_template called "ci-runner",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp when the QueryTemplate was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Short description of the QueryTemplate. The description must not contain Unicode non-characters and C0 and C1 control codes except tabs (HT), new lines (LF), carriage returns (CR), and page breaks (FF). Default value is an empty string. Max length: 2000 bytes. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. Human-readable display name of the QueryTemplate. The display name must contain only Unicode letters, numbers (0-9), underscores (_), dashes (-), spaces ( ), ampersands (&) and can't start or end with spaces. Default value is an empty string. Max length: 63 bytes. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="documentation" type="string">
  Optional. Documentation describing the QueryTemplate. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource name of the QueryTemplate. e.g. `projects/myproject/locations/us/dataExchanges/123/queryTemplates/456` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="primary_contact" type="string">
  Optional. Email or URL of the primary point of contact of the QueryTemplate. Max Length: 1000 bytes. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="proposer" type="string">
  Optional. Deprecated: Use `primary_contact` instead. Email or URL of the primary point of contact of the QueryTemplate. Max Length: 1000 bytes. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="routine" type="object">
  Represents a bigquery routine. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="definition_body" type="string">
      Optional. The definition body of the routine. Optional.
    </ResponseField>
    <ResponseField name="routine_type" type="string">
      Required. The type of routine. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The QueryTemplate lifecycle state. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when the QueryTemplate was last modified. Optional; if omitted, computed by Google Cloud.
</ResponseField>
