---
title: "google_analyticshub_v1beta1_data_exchange"
description: "`google_analyticshub_v1beta1_data_exchange` is a BigQuery Analytics Hub data exchange..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_analyticshub_data_exchange`.
</Warning>

`google_analyticshub_v1beta1_data_exchange` is a BigQuery Analytics Hub data exchange, a real container that organizes a set of `google_analyticshub_listing` entries so an organization can publish and share BigQuery datasets internally, with partners, or publicly, without moving the underlying data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_analyticshub_v1beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_analyticshub_v1beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	analyticshub "github.com/ubiquex/ubx-sdk-google/sdk/go/google_analyticshub_v1beta1/analyticshub"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 data exchange"})

    		ubx.Resource(analyticshub.V1beta1AnalyticshubV1beta1DataExchange, "example", analyticshub.V1beta1AnalyticshubV1beta1DataExchangeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_analyticshub_v1beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1AnalyticshubV1beta1DataExchange } from "./local-sdk/google_analyticshub_v1beta1/sdk/typescript/google_analyticshub_v1beta1/analyticshub/v1beta1_analyticshub_v1beta1_data_exchange.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 data exchange" });

      resource(V1beta1AnalyticshubV1beta1DataExchange, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_analyticshub_v1beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_analyticshub_v1beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_analyticshub_v1beta1.analyticshub import V1beta1AnalyticshubV1beta1DataExchange, V1beta1AnalyticshubV1beta1DataExchangeConfig

    def describe():
        ubx.intent("example own v1beta1 data exchange")

        ubx.resource(V1beta1AnalyticshubV1beta1DataExchange, "example", V1beta1AnalyticshubV1beta1DataExchangeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_analyticshub_v1beta1_data_exchange called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string">
  Optional. Description of the data exchange. The description must not contain Unicode non-characters as well as C0 and C1 control codes except tabs (HT), new lines (LF), carriage returns (CR), and page breaks (FF). Default value is an empty string. Max length: 2000 bytes. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. Human-readable display name of the data exchange. The display name must contain only Unicode letters, numbers (0-9), underscores (_), dashes (-), spaces ( ), ampersands (&) and must not start or end with spaces. Default value is an empty string. Max length: 63 bytes. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="documentation" type="string">
  Optional. Documentation describing the data exchange. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="icon" type="string">
  Optional. Base64 encoded image representing the data exchange. Max Size: 3.0MiB Expected image dimensions are 512x512 pixels, however the API only performs validation on size of the encoded data. Note: For byte fields, the content of the fields are base64-encoded (which increases the size of the data by 33-36%) when using JSON on the wire. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="listing_count" type="number">
  Output only. Number of listings contained in the data exchange. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource name of the data exchange. e.g. `projects/myproject/locations/us/dataExchanges/123`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="primary_contact" type="string">
  Optional. Email or URL of the primary point of contact of the data exchange. Max Length: 1000 bytes. Optional; if omitted, computed by Google Cloud.
</ResponseField>
