---
title: "google_gkebackup_restore_plan"
description: "`google_gkebackup_restore_plan` defines how a GKE backup is restored -- which target cluster and what conflict-resolution/transformation rules apply..."
---

`google_gkebackup_restore_plan` defines how a GKE backup is restored -- which target cluster and what conflict-resolution/transformation rules apply -- when a restore is executed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_gkebackup --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_gkebackup/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gkebackup "github.com/ubiquex/ubx-sdk-google/sdk/go/google_gkebackup/gkebackup"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own restore plan"})

    		ubx.Resource(gkebackup.GkebackupRestorePlan, "ci-runner", gkebackup.GkebackupRestorePlanConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_gkebackup --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GkebackupRestorePlan } from "./local-sdk/google_gkebackup/sdk/typescript/google_gkebackup/gkebackup/gkebackup_restore_plan.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own restore plan" });

      resource(GkebackupRestorePlan, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_gkebackup --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_gkebackup/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_gkebackup.gkebackup import GkebackupRestorePlan, GkebackupRestorePlanConfig

    def describe():
        ubx.intent("payments own restore plan")

        ubx.resource(GkebackupRestorePlan, "ci-runner", GkebackupRestorePlanConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gkebackup_restore_plan called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="backup_plan" type="string">
  Required. Immutable. A reference to the BackupPlan from which Backups may be used as the source for Restores created via this RestorePlan. Format: `projects/*/locations/*/backupPlans/*`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="cluster" type="string">
  Required. Immutable. The target cluster into which Restores created via this RestorePlan will restore data. NOTE: the cluster's region must be the same as the RestorePlan. Valid formats: - `projects/*/locations/*/clusters/*` - `projects/*/zones/*/clusters/*` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp when this RestorePlan resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. User specified descriptive string for this RestorePlan. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. `etag` is used for optimistic concurrency control as a way to help prevent simultaneous updates of a restore from overwriting each other. It is strongly suggested that systems make use of the `etag` in the read-modify-write cycle to perform restore updates in order to avoid race conditions: An `etag` is returned in the response to `GetRestorePlan`, and systems are expected to put that etag in the request to `UpdateRestorePlan` or `DeleteRestorePlan` to ensure that their change will be applied to the same version of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. A set of custom labels supplied by user. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. The full name of the RestorePlan resource. Format: `projects/*/locations/*/restorePlans/*`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="restore_channel" type="string">
  Output only. The fully qualified name of the RestoreChannel to be used to create a RestorePlan. This field is set only if the `backup_plan` is in a different project than the RestorePlan. Format: `projects/*/locations/*/restoreChannels/*` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="restore_config" type="object">
  Configuration of a restore. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="all_namespaces" type="bool">
      Restore all namespaced resources in the Backup if set to "True". Specifying this field to "False" is an error. Optional.
    </ResponseField>
    <ResponseField name="cluster_resource_conflict_policy" type="string">
      Optional. Defines the behavior for handling the situation where cluster-scoped resources being restored already exist in the target cluster. This MUST be set to a value other than CLUSTER_RESOURCE_CONFLICT_POLICY_UNSPECIFIED if cluster_resource_restore_scope is not empty. Optional.
    </ResponseField>
    <ResponseField name="cluster_resource_restore_scope" type="object">
      Defines the scope of cluster-scoped resources to restore. Some group kinds are not reasonable choices for a restore, and will cause an error if selected here. Any scope selection that would restore "all valid" resources automatically excludes these group kinds. - Node - ComponentStatus - gkebackup.gke.io/BackupJob - gkebackup.gke.io/RestoreJob - metrics.k8s.io/NodeMetrics - migration.k8s.io/StorageState - migration.k8s.io/StorageVersionMigration - snapshot.storage.k8s.io/VolumeSnapshotContent - storage.k8s.io/CSINode - storage.k8s.io/VolumeAttachment Some group kinds are driven by restore configuration elsewhere, and will cause an error if selected here. - Namespace - PersistentVolume Optional.
      <Expandable title="properties">
        <ResponseField name="all_group_kinds" type="bool">
          Optional. If True, all valid cluster-scoped resources will be restored. Mutually exclusive to any other field in the message. Optional.
        </ResponseField>
        <ResponseField name="excluded_group_kinds" type="list(object)">
          Optional. A list of cluster-scoped resource group kinds to NOT restore from the backup. If specified, all valid cluster-scoped resources will be restored except for those specified in the list. Mutually exclusive to any other field in the message. Optional.
          <Expandable title="properties">
            <ResponseField name="resource_group" type="string">
              A restore action grouped with the resource(s) it applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_kind" type="string">
              The Kubernetes resource kind this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="no_group_kinds" type="bool">
          Optional. If True, no cluster-scoped resources will be restored. This has the same restore scope as if the message is not defined. Mutually exclusive to any other field in the message. Optional.
        </ResponseField>
        <ResponseField name="selected_group_kinds" type="list(object)">
          Optional. A list of cluster-scoped resource group kinds to restore from the backup. If specified, only the selected resources will be restored. Mutually exclusive to any other field in the message. Optional.
          <Expandable title="properties">
            <ResponseField name="resource_group" type="string">
              A restore action grouped with the resource(s) it applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_kind" type="string">
              The Kubernetes resource kind this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="excluded_namespaces" type="object">
      A list of Kubernetes Namespaces. Optional.
      <Expandable title="properties">
        <ResponseField name="namespaces" type="list(string)">
          Optional. A list of Kubernetes Namespaces. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="namespaced_resource_restore_mode" type="string">
      Optional. Defines the behavior for handling the situation where sets of namespaced resources being restored already exist in the target cluster. This MUST be set to a value other than NAMESPACED_RESOURCE_RESTORE_MODE_UNSPECIFIED. Optional.
    </ResponseField>
    <ResponseField name="no_namespaces" type="bool">
      Do not restore any namespaced resources if set to "True". Specifying this field to "False" is not allowed. Optional.
    </ResponseField>
    <ResponseField name="restore_order" type="object">
      Allows customers to specify dependencies between resources that Backup for GKE can use to compute a resasonable restore order. Optional.
      <Expandable title="properties">
        <ResponseField name="group_kind_dependencies" type="list(object)">
          Optional. Contains a list of group kind dependency pairs provided by the customer, that is used by Backup for GKE to generate a group kind restore order. Optional.
          <Expandable title="properties">
            <ResponseField name="requiring" type="object">
              Rule(s) that must ALL match for this restore action to apply, when combined via AND logic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="resource_group" type="string">
                  A restore action grouped with the resource(s) it applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="resource_kind" type="string">
                  The Kubernetes resource kind this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="satisfying" type="object">
              Rule(s) any one of which must match for this restore action to apply, when combined via OR logic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="resource_group" type="string">
                  A restore action grouped with the resource(s) it applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="resource_kind" type="string">
                  The Kubernetes resource kind this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="selected_applications" type="object">
      A list of namespaced Kubernetes resources. Optional.
      <Expandable title="properties">
        <ResponseField name="namespaced_names" type="list(object)">
          Optional. A list of namespaced Kubernetes resources. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="namespace" type="string">
              The Kubernetes namespace this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="selected_namespaces" type="object">
      A list of Kubernetes Namespaces. Optional.
      <Expandable title="properties">
        <ResponseField name="namespaces" type="list(string)">
          Optional. A list of Kubernetes Namespaces. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="substitution_rules" type="list(object)">
      Optional. A list of transformation rules to be applied against Kubernetes resources as they are selected for restoration from a Backup. Rules are executed in order defined - this order matters, as changes made by a rule may impact the filtering logic of subsequent rules. An empty list means no substitution will occur. Optional.
      <Expandable title="properties">
        <ResponseField name="new_value" type="string">
          The value this field is set to after transformation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="original_value_pattern" type="string">
          A regular expression the field's own original value must match for this transformation to apply. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_group_kinds" type="list(object)">
          The API group(s) and kind(s) this restore action targets. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="resource_group" type="string">
              A restore action grouped with the resource(s) it applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_kind" type="string">
              The Kubernetes resource kind this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="target_json_path" type="string">
          The JSON path this transformation writes its own result to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_namespaces" type="list(string)">
          The Kubernetes namespace(s) restored resources are placed into. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="transformation_rules" type="list(object)">
      Optional. A list of transformation rules to be applied against Kubernetes resources as they are selected for restoration from a Backup. Rules are executed in order defined - this order matters, as changes made by a rule may impact the filtering logic of subsequent rules. An empty list means no transformation will occur. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="field_actions" type="list(object)">
          The transformation(s) applied to a specific field during restore. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="from_path" type="string">
              The JSON path this transformation reads its own source value from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="op" type="string">
              The transformation operation applied, e.g. `add`, `remove`, or `replace`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path" type="string">
              A file or resource path. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource_filter" type="object">
          Restricts a restore or backup action to matching Kubernetes resource(s). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="group_kinds" type="list(object)">
              The API group(s) and kind(s) this filter matches. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="resource_group" type="string">
                  A restore action grouped with the resource(s) it applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="resource_kind" type="string">
                  The Kubernetes resource kind this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="json_path" type="string">
              The JSON path this rule applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="namespaces" type="list(string)">
              The Kubernetes namespace(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="volume_data_restore_policy" type="string">
      Optional. Specifies the mechanism to be used to restore volume data. Default: VOLUME_DATA_RESTORE_POLICY_UNSPECIFIED (will be treated as NO_VOLUME_DATA_RESTORATION). Optional.
    </ResponseField>
    <ResponseField name="volume_data_restore_policy_bindings" type="list(object)">
      Optional. A table that binds volumes by their scope to a restore policy. Bindings must have a unique scope. Any volumes not scoped in the bindings are subject to the policy defined in volume_data_restore_policy. Optional.
      <Expandable title="properties">
        <ResponseField name="policy" type="string">
          The rule(s) restore actions must satisfy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="volume_type" type="string">
          How this volume's own data is backed up, e.g. via a Persistent Disk snapshot. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the RestorePlan. This State field reflects the various stages a RestorePlan can be in during the Create operation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state_reason" type="string">
  Output only. Human-readable description of why RestorePlan is in the current `state`. This field is only meant for human readability and should not be used programmatically as this field is not guaranteed to be consistent. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. Server generated global unique identifier of [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when this RestorePlan resource was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
