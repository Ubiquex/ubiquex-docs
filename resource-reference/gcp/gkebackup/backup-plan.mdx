---
title: "google_gkebackup_backup_plan"
description: "`google_gkebackup_backup_plan` defines what a GKE Backup for GKE backup covers (which namespaces/resources) and how often it runs automatically."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_gkebackup --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_gkebackup/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gkebackup "github.com/ubiquex/ubx-sdk-google/sdk/go/google_gkebackup/gkebackup"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own backup plan"})

    		ubx.Resource(gkebackup.GkebackupBackupPlan, "ci-runner", gkebackup.GkebackupBackupPlanConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_gkebackup --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GkebackupBackupPlan } from "./local-sdk/google_gkebackup/sdk/typescript/google_gkebackup/gkebackup/gkebackup_backup_plan.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own backup plan" });

      resource(GkebackupBackupPlan, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_gkebackup --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_gkebackup/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_gkebackup.gkebackup import GkebackupBackupPlan, GkebackupBackupPlanConfig

    def describe():
        ubx.intent("payments own backup plan")

        ubx.resource(GkebackupBackupPlan, "ci-runner", GkebackupBackupPlanConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gkebackup_backup_plan called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="backup_channel" type="string">
  Output only. The fully qualified name of the BackupChannel to be used to create a backup. This field is set only if the cluster being backed up is in a different project. `projects/*/locations/*/backupChannels/*` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="backup_config" type="object">
  BackupConfig defines the configuration of Backups created via this BackupPlan. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="all_namespaces" type="bool">
      If True, include all namespaced resources Optional.
    </ResponseField>
    <ResponseField name="encryption_key" type="object">
      Defined a customer managed encryption key that will be used to encrypt Backup artifacts. Optional.
      <Expandable title="properties">
        <ResponseField name="gcp_kms_encryption_key" type="string">
          Optional. Google Cloud KMS encryption key. Format: `projects/*/locations/*/keyRings/*/cryptoKeys/*` Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="include_secrets" type="bool">
      Optional. This flag specifies whether Kubernetes Secret resources should be included when they fall into the scope of Backups. Default: False Optional.
    </ResponseField>
    <ResponseField name="include_volume_data" type="bool">
      Optional. This flag specifies whether volume data should be backed up when PVCs are included in the scope of a Backup. Default: False Optional.
    </ResponseField>
    <ResponseField name="permissive_mode" type="bool">
      Optional. If false, Backups will fail when Backup for GKE detects Kubernetes configuration that is non-standard or requires additional setup to restore. Default: False Optional.
    </ResponseField>
    <ResponseField name="selected_applications" type="object">
      A list of namespaced Kubernetes resources. Optional.
      <Expandable title="properties">
        <ResponseField name="namespaced_names" type="list(object)">
          Optional. A list of namespaced Kubernetes resources. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="namespace" type="string">
              The Kubernetes namespace this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="selected_namespace_labels" type="object">
      A list of Kubernetes labels. Optional.
      <Expandable title="properties">
        <ResponseField name="resource_labels" type="list(object)">
          Optional. A list of Kubernetes label-value pairs. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of this label or field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="selected_namespaces" type="object">
      A list of Kubernetes Namespaces. Optional.
      <Expandable title="properties">
        <ResponseField name="namespaces" type="list(string)">
          Optional. A list of Kubernetes Namespaces. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="backup_schedule" type="object">
  Defines scheduling parameters for automatically creating Backups via this BackupPlan. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cron_schedule" type="string">
      Optional. A standard [cron](https://wikipedia.com/wiki/cron) string that defines a repeating schedule for creating Backups via this BackupPlan. This is mutually exclusive with the rpo_config field since at most one schedule can be defined for a BackupPlan. If this is defined, then backup_retain_days must also be defined. Default (empty): no automatic backup creation will occur. Optional.
    </ResponseField>
    <ResponseField name="next_scheduled_backup_time" type="string">
      Output only. Start time of next scheduled backup under this BackupPlan by either cron_schedule or rpo config. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="paused" type="bool">
      Optional. This flag denotes whether automatic Backup creation is paused for this BackupPlan. Default: False Optional.
    </ResponseField>
    <ResponseField name="rpo_config" type="object">
      Defines RPO scheduling configuration for automatically creating Backups via this BackupPlan. Optional.
      <Expandable title="properties">
        <ResponseField name="exclusion_windows" type="list(object)">
          Optional. User specified time windows during which backup can NOT happen for this BackupPlan - backups should start and finish outside of any given exclusion window. Note: backup jobs will be scheduled to start and finish outside the duration of the window as much as possible, but running jobs will not get canceled when it runs into the window. All the time and date values in exclusion_windows entry in the API are in UTC. We only allow &lt;=1 recurrence (daily or weekly) exclusion window for a BackupPlan while no restriction on number of single occurrence windows. Optional.
          <Expandable title="properties">
            <ResponseField name="daily" type="bool">
              Runs this schedule once per day. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="days_of_week" type="object">
              The day(s) of the week this schedule runs on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="days_of_week" type="list(string)">
                  The day(s) of the week this schedule runs on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="duration" type="string">
              How long this operation is allowed to run before timing out. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="single_occurrence_date" type="object">
              Runs this schedule exactly once, on the specified date. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="day" type="number">
                  The day-of-month component of a date. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="month" type="number">
                  The month component of a date. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="year" type="number">
                  The year component of a date. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="start_time" type="object">
              When this event or window started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="hours" type="number">
                  The hour component of a time value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="minutes" type="number">
                  The minute component of a time value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="nanos" type="number">
                  The sub-second, nanosecond component of a time value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="number">
                  The second component of a time value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="target_rpo_minutes" type="number">
          Required. Defines the target RPO for the BackupPlan in minutes, which means the target maximum data loss in time that is acceptable for this BackupPlan. This must be at least 60, i.e., 1 hour, and at most 86400, i.e., 60 days. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cluster" type="string">
  Required. Immutable. The source cluster from which Backups will be created via this BackupPlan. Valid formats: - `projects/*/locations/*/clusters/*` - `projects/*/zones/*/clusters/*` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp when this BackupPlan resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="deactivated" type="bool">
  Optional. This flag indicates whether this BackupPlan has been deactivated. Setting this field to True locks the BackupPlan such that no further updates will be allowed (except deletes), including the deactivated field itself. It also prevents any new Backups from being created via this BackupPlan (including scheduled Backups). Default: False Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. User specified descriptive string for this BackupPlan. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. `etag` is used for optimistic concurrency control as a way to help prevent simultaneous updates of a backup plan from overwriting each other. It is strongly suggested that systems make use of the 'etag' in the read-modify-write cycle to perform BackupPlan updates in order to avoid race conditions: An `etag` is returned in the response to `GetBackupPlan`, and systems are expected to put that etag in the request to `UpdateBackupPlan` or `DeleteBackupPlan` to ensure that their change will be applied to the same version of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. A set of custom labels supplied by user. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="last_successful_backup_time" type="string">
  Output only. Completion time of the last successful Backup. This is sourced from a successful Backup's complete_time field. This field is added to maintain consistency with BackupPlanBinding to display last successful backup time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. The full name of the BackupPlan resource. Format: `projects/*/locations/*/backupPlans/*` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="protected_namespace_count" type="number">
  Output only. The number of user managed namespaces backed up in the last successful Backup created via this BackupPlan. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="protected_pod_count" type="number">
  Output only. The number of Kubernetes Pods backed up in the last successful Backup created via this BackupPlan. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="retention_policy" type="object">
  RetentionPolicy defines a Backup retention policy for a BackupPlan. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="backup_delete_lock_days" type="number">
      Optional. Minimum age for Backups created via this BackupPlan (in days). This field MUST be an integer value between 0-90 (inclusive). A Backup created under this BackupPlan will NOT be deletable until it reaches Backup's (create_time + backup_delete_lock_days). Updating this field of a BackupPlan does NOT affect existing Backups under it. Backups created AFTER a successful update will inherit the new value. Default: 0 (no delete blocking) Optional.
    </ResponseField>
    <ResponseField name="backup_retain_days" type="number">
      Optional. The default maximum age of a Backup created via this BackupPlan. This field MUST be an integer value &gt;= 0 and &lt;= 365. If specified, a Backup created under this BackupPlan will be automatically deleted after its age reaches (create_time + backup_retain_days). If not specified, Backups created under this BackupPlan will NOT be subject to automatic deletion. Updating this field does NOT affect existing Backups under it. Backups created AFTER a successful update will automatically pick up the new value. NOTE: backup_retain_days must be &gt;= backup_delete_lock_days. If cron_schedule is defined, then this must be &lt;= 360 * the creation interval. If rpo_config is defined, then this must be &lt;= 360 * target_rpo_minutes / (1440minutes/day). Default: 0 (no automatic deletion) Optional.
    </ResponseField>
    <ResponseField name="locked" type="bool">
      Optional. This flag denotes whether the retention policy of this BackupPlan is locked. If set to True, no further update is allowed on this policy, including the `locked` field itself. Default: False Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rpo_risk_level" type="number">
  Output only. A number that represents the current risk level of this BackupPlan from RPO perspective with 1 being no risk and 5 being highest risk. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rpo_risk_reason" type="string">
  Output only. Human-readable description of why the BackupPlan is in the current rpo_risk_level and action items if any. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the BackupPlan. This State field reflects the various stages a BackupPlan can be in during the Create operation. It will be set to "DEACTIVATED" if the BackupPlan is deactivated on an Update Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state_reason" type="string">
  Output only. Human-readable description of why BackupPlan is in the current `state`. This field is only meant for human readability and should not be used programmatically as this field is not guaranteed to be consistent. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. Server generated global unique identifier of [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when this BackupPlan resource was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
