---
title: "google_iam_workload_identity_pool_iam_policy"
description: "Real, generated bindings for `google_iam_workload_identity_pool_iam_policy`."
---

`google_iam_workload_identity_pool_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/iam"
    )

    cfg := iam.WorkloadIdentityPoolIamPolicyConfig{
        PolicyData: "example",
        WorkloadIdentityPoolId: "example",
    }
    ubx.Resource(iam.WorkloadIdentityPoolIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WorkloadIdentityPoolIamPolicy } from
      "jsr:@ubx/sdk-google/google/iam/workload_identity_pool_iam_policy";

    resource(WorkloadIdentityPoolIamPolicy, "example", {
      policyData: "example",
      workloadIdentityPoolId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.iam import (
        WorkloadIdentityPoolIamPolicy,
        WorkloadIdentityPoolIamPolicyConfig,
    )

    cfg = WorkloadIdentityPoolIamPolicyConfig(
        policy_data="example",
        workload_identity_pool_id="example",
    )
    ubx.resource(WorkloadIdentityPoolIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_iam_workload_identity_pool_iam_policy with
policy_data, workload_identity_pool_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="workload_identity_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
