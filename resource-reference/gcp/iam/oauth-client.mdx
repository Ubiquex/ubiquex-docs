---
title: "google_iam_oauth_client"
description: "Real, generated bindings for `google_iam_oauth_client`."
---

`google_iam_oauth_client` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own oauth client"})

    		ubx.Resource(iam.OauthClient, "ci-runner", iam.OauthClientConfig{
    			AllowedGrantTypes:   []string{"example"},
    			AllowedRedirectUris: []string{"example"},
    			AllowedScopes:       []string{"example"},
    			Location:            "example",
    			OauthClientId:       "example",
    			ClientType:          "example",
    			Description:         "Managed by ubx.",
    			Disabled:            true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OauthClient } from "jsr:@ubx/sdk-google/google/iam/oauth_client";

    export default stack("payments", () => {
      intent({ summary: "payments own oauth client" });

      resource(OauthClient, "ci-runner", {
        allowedGrantTypes: ["example"],
        allowedRedirectUris: ["example"],
        allowedScopes: ["example"],
        location: "example",
        oauthClientId: "example",
        clientType: "example",
        description: "Managed by ubx.",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iam import OauthClient, OauthClientConfig

    def describe():
        ubx.intent("payments own oauth client")

        ubx.resource(OauthClient, "ci-runner", OauthClientConfig(
            allowed_grant_types=["example"],
            allowed_redirect_uris=["example"],
            allowed_scopes=["example"],
            location="example",
            oauth_client_id="example",
            client_type="example",
            description="Managed by ubx.",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_oauth_client, with allowed grant types a set
    value, allowed redirect uris a set value, allowed scopes a set
    value, location example, oauth client id example, client type
    example, description Managed by ubx., disabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allowed_grant_types" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="allowed_redirect_uris" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="allowed_scopes" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="client_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="oauth_client_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="client_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
