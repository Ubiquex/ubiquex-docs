---
title: "google_iam_oauth_client"
description: "Real, generated bindings for `google_iam_oauth_client`."
---

`google_iam_oauth_client` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own oauth client"})

    		ubx.Resource(iam.OauthClient, "ci-runner", iam.OauthClientConfig{
    			AllowedGrantTypes:   []string{"example"},
    			AllowedRedirectUris: []string{"example"},
    			AllowedScopes:       []string{"example"},
    			Location:            "example",
    			OauthClientId:       "example",
    			ClientType:          "example",
    			Description:         "Managed by ubx.",
    			Disabled:            true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OauthClient } from "jsr:@ubx/sdk-google/google/iam/oauth_client";

    export default stack("payments", () => {
      intent({ summary: "payments own oauth client" });

      resource(OauthClient, "ci-runner", {
        allowedGrantTypes: ["example"],
        allowedRedirectUris: ["example"],
        allowedScopes: ["example"],
        location: "example",
        oauthClientId: "example",
        clientType: "example",
        description: "Managed by ubx.",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iam import OauthClient, OauthClientConfig

    def describe():
        ubx.intent("payments own oauth client")

        ubx.resource(OauthClient, "ci-runner", OauthClientConfig(
            allowed_grant_types=["example"],
            allowed_redirect_uris=["example"],
            allowed_scopes=["example"],
            location="example",
            oauth_client_id="example",
            client_type="example",
            description="Managed by ubx.",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_oauth_client, with allowed grant types a set
    value, allowed redirect uris a set value, allowed scopes a set
    value, location example, oauth client id example, client type
    example, description Managed by ubx., disabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allowed_grant_types" type="list(string)" required>
  Required. The list of OAuth grant types is allowed for the OauthClient. Required.
</ResponseField>

<ResponseField name="allowed_redirect_uris" type="list(string)" required>
  Required. The list of redirect uris that is allowed to redirect back when authorization process is completed. Required.
</ResponseField>

<ResponseField name="allowed_scopes" type="list(string)" required>
  Required. The list of scopes that the OauthClient is allowed to request during OAuth flows. The following scopes are supported: * 'https://www.googleapis.com/auth/cloud-platform': See, edit, configure, and delete your Google Cloud data and see the email address for your Google Account. * 'openid': The OAuth client can associate you with your personal information on Google Cloud. * 'email': The OAuth client can read a federated identity's email address. * 'groups': The OAuth client can read a federated identity's groups. Required.
</ResponseField>

<ResponseField name="client_type" type="string">
  Immutable. The type of OauthClient. Either public or private. For private clients, the client secret can be managed using the dedicated OauthClientCredential resource. Possible values: CLIENT_TYPE_UNSPECIFIED PUBLIC_CLIENT CONFIDENTIAL_CLIENT Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A user-specified description of the OauthClient. Cannot exceed 256 characters. Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Whether the OauthClient is disabled. You cannot use a disabled OAuth client. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  A user-specified display name of the OauthClient. Cannot exceed 32 characters. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="oauth_client_id" type="string" required>
  Required. The ID to use for the OauthClient, which becomes the final component of the resource name. This value should be a string of 6 to 63 lowercase letters, digits, or hyphens. It must start with a letter, and cannot have a trailing hyphen. The prefix 'gcp-' is reserved for use by Google, and may not be specified. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="client_id" type="string">
  Output only. The system-generated OauthClient id. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Time after which the OauthClient will be permanently purged and cannot be recovered. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. Identifier. The resource name of the OauthClient. Format:'projects/&#123;project&#125;/locations/&#123;location&#125;/oauthClients/&#123;oauth_client&#125;'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the OauthClient. Possible values: STATE_UNSPECIFIED ACTIVE DELETED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
