---
title: "google_iam_namespace"
description: "`google_iam_namespace` groups related managed identities within a Workload Identity Federation pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_iam --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_iam/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google_iam/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own namespace"})

    		ubx.Resource(iam.IamNamespace, "ci-runner", iam.IamNamespaceConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_iam --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { IamNamespace } from "./local-sdk/google_iam/sdk/typescript/google_iam/iam/iam_namespace.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own namespace" });

      resource(IamNamespace, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_iam --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_iam/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_iam.iam import IamNamespace, IamNamespaceConfig

    def describe():
        ubx.intent("payments own namespace")

        ubx.resource(IamNamespace, "ci-runner", IamNamespaceConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_namespace called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string">
  Optional. A description of the namespace. Cannot exceed 256 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional. Whether the namespace is disabled. If disabled, credentials may no longer be issued for identities within this namespace, however existing credentials will still be accepted until they expire. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Output only. Time after which the namespace will be permanently purged and cannot be recovered. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the namespace. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="owner_service" type="object">
  The Google Cloud service that owns this namespace. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="principal_subject" type="string">
      Required. The service agent principal subject, e.g. "serviceAccount:service-1234@gcp-sa-gkehub.iam.gserviceaccount.com". Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the namespace. Optional; if omitted, computed by Google Cloud.
</ResponseField>
