---
title: "google_iam_workload_identity_pool"
description: "Real, generated bindings for `google_iam_workload_identity_pool`."
---

`google_iam_workload_identity_pool` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workload identity pool"})

    		ubx.Resource(iam.WorkloadIdentityPool, "ci-runner", iam.WorkloadIdentityPoolConfig{
    			WorkloadIdentityPoolId: "example",
    			Description:            "Managed by ubx.",
    			Disabled:               true,
    			DisplayName:            "example-display-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkloadIdentityPool } from "jsr:@ubx/sdk-google/google/iam/workload_identity_pool";

    export default stack("payments", () => {
      intent({ summary: "payments own workload identity pool" });

      resource(WorkloadIdentityPool, "ci-runner", {
        workloadIdentityPoolId: "example",
        description: "Managed by ubx.",
        disabled: true,
        displayName: "example-display-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iam import WorkloadIdentityPool, WorkloadIdentityPoolConfig

    def describe():
        ubx.intent("payments own workload identity pool")

        ubx.resource(WorkloadIdentityPool, "ci-runner", WorkloadIdentityPoolConfig(
            workload_identity_pool_id="example",
            description="Managed by ubx.",
            disabled=True,
            display_name="example-display-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_workload_identity_pool, with workload identity
    pool id example, description Managed by ubx., disabled true,
    display name example-display-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attestation_rules" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="google_cloud_resource" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the pool. Cannot exceed 256 characters. Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Whether the pool is disabled. You cannot use a disabled pool to exchange tokens, or use existing tokens to access resources. If the pool is re-enabled, existing tokens grant access again. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  A display name for the pool. Cannot exceed 32 characters. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="inline_certificate_issuance_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ca_pools" type="map(string)">
    </ResponseField>
    <ResponseField name="key_algorithm" type="string">
    </ResponseField>
    <ResponseField name="lifetime" type="string">
    </ResponseField>
    <ResponseField name="rotation_window_percentage" type="number">
    </ResponseField>
    <ResponseField name="use_default_shared_ca" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="inline_trust_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="additional_trust_bundles" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="trust_anchors" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="pem_certificate" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="trust_default_shared_ca" type="bool">
        </ResponseField>
        <ResponseField name="trust_domain" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="mode" type="string">
  The mode for the pool is operating in. Pools with an unspecified mode will operate as if they are in 'FEDERATION_ONLY' mode. ~&gt; **Note** This field cannot be changed after the Workload Identity Pool is created. While 'terraform plan' may show an update if you change this field's value, 'terraform apply' **will fail with an API error** (such as 'Error 400: Attempted to update an immutable field.'). To specify a different 'mode', please create a new Workload Identity Pool resource. * 'FEDERATION_ONLY': Pools can only be used for federating external workload identities into Google Cloud. Unless otherwise noted, no structure or format constraints are applied to workload identities in a 'FEDERATION_ONLY' mode pool, and you may not create any resources within the pool besides providers. * 'TRUST_DOMAIN': Pools can be used to assign identities to Google Cloud workloads. All identities within a 'TRUST_DOMAIN' mode pool must consist of a single namespace and individual workload identifier. The subject identifier for all identities must conform to the following format: 'ns/&lt;namespace&gt;/sa/&lt;workload_identifier&gt;'. 'google_iam_workload_identity_pool_provider's cannot be created within 'TRUST_DOMAIN' mode pools. * 'SYSTEM_TRUST_DOMAIN': Pools are managed by Google Cloud services. Neither 'google_iam_workload_identity_pool_namespace's nor 'google_iam_workload_identity_pool_provider's can be created within 'SYSTEM_TRUST_DOMAIN' mode pools. All identities within a 'SYSTEM_TRUST_DOMAIN' mode pool are in one of the following formats: * 'spiffe://&lt;trust-domain&gt;/ns/&lt;kubernetes-namespace&gt;/sa/&lt;kubernetes-service-account&gt;' * 'spiffe://&lt;trust-domain&gt;/resources/&lt;resource-scope&gt;/&lt;resource-name&gt;' Possible values: ["FEDERATION_ONLY", "TRUST_DOMAIN", "SYSTEM_TRUST_DOMAIN"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workload_identity_pool_id" type="string" required>
  The ID to use for the pool, which becomes the final component of the resource name. This value should be 4-32 characters, and may contain the characters [a-z0-9-]. The prefix 'gcp-' is reserved for use by Google, and may not be specified. Required.
</ResponseField>

## Output properties

<ResponseField name="attestation_rules" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="google_cloud_resource" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="inline_certificate_issuance_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ca_pools" type="map(string)">
    </ResponseField>
    <ResponseField name="key_algorithm" type="string">
    </ResponseField>
    <ResponseField name="lifetime" type="string">
    </ResponseField>
    <ResponseField name="rotation_window_percentage" type="number">
    </ResponseField>
    <ResponseField name="use_default_shared_ca" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="inline_trust_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="additional_trust_bundles" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="trust_anchors" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="pem_certificate" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="trust_default_shared_ca" type="bool">
        </ResponseField>
        <ResponseField name="trust_domain" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="mode" type="string">
  The mode for the pool is operating in. Pools with an unspecified mode will operate as if they are in 'FEDERATION_ONLY' mode. ~&gt; **Note** This field cannot be changed after the Workload Identity Pool is created. While 'terraform plan' may show an update if you change this field's value, 'terraform apply' **will fail with an API error** (such as 'Error 400: Attempted to update an immutable field.'). To specify a different 'mode', please create a new Workload Identity Pool resource. * 'FEDERATION_ONLY': Pools can only be used for federating external workload identities into Google Cloud. Unless otherwise noted, no structure or format constraints are applied to workload identities in a 'FEDERATION_ONLY' mode pool, and you may not create any resources within the pool besides providers. * 'TRUST_DOMAIN': Pools can be used to assign identities to Google Cloud workloads. All identities within a 'TRUST_DOMAIN' mode pool must consist of a single namespace and individual workload identifier. The subject identifier for all identities must conform to the following format: 'ns/&lt;namespace&gt;/sa/&lt;workload_identifier&gt;'. 'google_iam_workload_identity_pool_provider's cannot be created within 'TRUST_DOMAIN' mode pools. * 'SYSTEM_TRUST_DOMAIN': Pools are managed by Google Cloud services. Neither 'google_iam_workload_identity_pool_namespace's nor 'google_iam_workload_identity_pool_provider's can be created within 'SYSTEM_TRUST_DOMAIN' mode pools. All identities within a 'SYSTEM_TRUST_DOMAIN' mode pool are in one of the following formats: * 'spiffe://&lt;trust-domain&gt;/ns/&lt;kubernetes-namespace&gt;/sa/&lt;kubernetes-service-account&gt;' * 'spiffe://&lt;trust-domain&gt;/resources/&lt;resource-scope&gt;/&lt;resource-name&gt;' Possible values: ["FEDERATION_ONLY", "TRUST_DOMAIN", "SYSTEM_TRUST_DOMAIN"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the pool as 'projects/&#123;project_number&#125;/locations/global/workloadIdentityPools/&#123;workload_identity_pool_id&#125;'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the pool. * 'STATE_UNSPECIFIED': State unspecified. * 'ACTIVE': The pool is active, and may be used in Google Cloud policies. * 'DELETED': The pool is soft-deleted. Soft-deleted pools are permanently deleted after approximately 30 days. You can restore a soft-deleted pool using 'UndeleteWorkloadIdentityPool'. You cannot reuse the ID of a soft-deleted pool until it is permanently deleted. While a pool is deleted, you cannot use it to exchange tokens, or use existing tokens to access resources. If the pool is undeleted, existing tokens grant access again. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
