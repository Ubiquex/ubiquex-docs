---
title: "google_iam_folders_policy_binding"
description: "Real, generated bindings for `google_iam_folders_policy_binding`."
---

`google_iam_folders_policy_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	"encoding/json"

    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own folders policy binding"})

    		accessPolicy, _ := json.Marshal(map[string]any{
    			"Version": "2012-10-17",
    			"Statement": []map[string]any{{
    				"Effect":   "Allow",
    				"Action":   "*",
    				"Resource": "*",
    			}},
    		})

    		ubx.Resource(iam.FoldersPolicyBinding, "ci-runner", iam.FoldersPolicyBindingConfig{
    			Folder:          "example",
    			Location:        "example",
    			Policy:          string(accessPolicy),
    			PolicyBindingId: "example",
    			Annotations:     map[string]string{"managed-by": "ubx"},
    			DisplayName:     "example-display-name",
    			PolicyKind:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FoldersPolicyBinding } from "jsr:@ubx/sdk-google/google/iam/folders_policy_binding";

    export default stack("payments", () => {
      intent({ summary: "payments own folders policy binding" });

      const accessPolicy = JSON.stringify({
        Version: "2012-10-17",
        Statement: [{
          Effect: "Allow",
          Action: "*",
          Resource: "*",
        }],
      });

      resource(FoldersPolicyBinding, "ci-runner", {
        folder: "example",
        location: "example",
        policy: accessPolicy,
        policyBindingId: "example",
        annotations: { "managed-by": "ubx" },
        displayName: "example-display-name",
        policyKind: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import json
    import ubx_sdk as ubx
    from ubx.google.iam import FoldersPolicyBinding, FoldersPolicyBindingConfig

    def describe():
        ubx.intent("payments own folders policy binding")

        access_policy = json.dumps({
            "Version": "2012-10-17",
            "Statement": [{
                "Effect": "Allow",
                "Action": "*",
                "Resource": "*",
            }],
        })

        ubx.resource(FoldersPolicyBinding, "ci-runner", FoldersPolicyBindingConfig(
            folder="example",
            location="example",
            policy=access_policy,
            policy_binding_id="example",
            annotations={"managed-by": "ubx"},
            display_name="example-display-name",
            policy_kind="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_folders_policy_binding, with folder example,
    location example, policy a real, valid value, policy binding id
    example, annotations a set value, display name
    example-display-name, policy kind example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="annotations" type="map(string)">
  Optional. User defined annotations. See https://google.aip.dev/148#annotations for more details such as format and size limitations **Note**: This field is non-authoritative, and will only manage the annotations present in your configuration. Please refer to the field 'effective_annotations' for all of the annotations present on the resource. Optional.
</ResponseField>

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string">
    </ResponseField>
    <ResponseField name="location" type="string">
    </ResponseField>
    <ResponseField name="title" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. The description of the policy binding. Must be less than or equal to 63 characters. Optional.
</ResponseField>

<ResponseField name="folder" type="string" required>
  The parent folder for the PolicyBinding. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location of the PolicyBinding. Required.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required. Immutable. The resource name of the policy to be bound. The binding parent and policy must belong to the same Organization (or Project). Required.
</ResponseField>

<ResponseField name="policy_binding_id" type="string" required>
  The Policy Binding ID. Required.
</ResponseField>

<ResponseField name="policy_kind" type="string">
  Immutable. The kind of the policy to attach in this binding. This field must be one of the following: - Left empty (will be automatically set to the policy kind) - The input policy kind Possible values: POLICY_KIND_UNSPECIFIED PRINCIPAL_ACCESS_BOUNDARY ACCESS Optional.
</ResponseField>

<ResponseField name="target" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="principal_set" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string">
    </ResponseField>
    <ResponseField name="location" type="string">
    </ResponseField>
    <ResponseField name="title" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the policy binding was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_annotations" type="map(string)">
  All of annotations (key/value pairs) present on the resource in GCP, including the annotations configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. The etag for the policy binding. If this is provided on update, it must match the server's etag. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the policy binding in the format '&#123;binding_parent/locations/&#123;location&#125;/policyBindings/&#123;policy_binding_id&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="policy_uid" type="string">
  Output only. The globally unique ID of the policy to be bound. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="target" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="principal_set" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The globally unique ID of the policy binding. Assigned when the policy binding is created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the policy binding was most recently updated. Computed by GCP after `ship`.
</ResponseField>
