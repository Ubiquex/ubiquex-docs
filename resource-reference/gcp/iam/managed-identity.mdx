---
title: "google_iam_managed_identity"
description: "`google_iam_managed_identity` is a managed identity within a Workload Identity Federation namespace..."
---

`google_iam_managed_identity` is a managed identity within a Workload Identity Federation namespace, representing one workload that can authenticate to Google Cloud using its own external credentials.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_iam --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_iam/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google_iam/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own managed identity"})

    		ubx.Resource(iam.IamManagedIdentity, "ci-runner", iam.IamManagedIdentityConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_iam --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { IamManagedIdentity } from "./local-sdk/google_iam/sdk/typescript/google_iam/iam/iam_managed_identity.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own managed identity" });

      resource(IamManagedIdentity, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_iam --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_iam/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_iam.iam import IamManagedIdentity, IamManagedIdentityConfig

    def describe():
        ubx.intent("payments own managed identity")

        ubx.resource(IamManagedIdentity, "ci-runner", IamManagedIdentityConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_managed_identity called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string">
  Optional. A description of the managed identity. Cannot exceed 256 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional. Whether the managed identity is disabled. If disabled, credentials may no longer be issued for the identity, however existing credentials will still be accepted until they expire. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Output only. Time after which the managed identity will be permanently purged and cannot be recovered. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the managed identity. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the managed identity. Optional; if omitted, computed by Google Cloud.
</ResponseField>
