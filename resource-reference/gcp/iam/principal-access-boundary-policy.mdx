---
title: "google_iam_principal_access_boundary_policy"
description: "Real, generated bindings for `google_iam_principal_access_boundary_policy`."
---

`google_iam_principal_access_boundary_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own principal access boundary policy"})

    		ubx.Resource(iam.PrincipalAccessBoundaryPolicy, "ci-runner", iam.PrincipalAccessBoundaryPolicyConfig{
    			Location:                        "example",
    			Organization:                    "example",
    			PrincipalAccessBoundaryPolicyId: "example",
    			Annotations:                     map[string]string{"managed-by": "ubx"},
    			DisplayName:                     "example-display-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrincipalAccessBoundaryPolicy } from "jsr:@ubx/sdk-google/google/iam/principal_access_boundary_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own principal access boundary policy" });

      resource(PrincipalAccessBoundaryPolicy, "ci-runner", {
        location: "example",
        organization: "example",
        principalAccessBoundaryPolicyId: "example",
        annotations: { "managed-by": "ubx" },
        displayName: "example-display-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iam import PrincipalAccessBoundaryPolicy, PrincipalAccessBoundaryPolicyConfig

    def describe():
        ubx.intent("payments own principal access boundary policy")

        ubx.resource(PrincipalAccessBoundaryPolicy, "ci-runner", PrincipalAccessBoundaryPolicyConfig(
            location="example",
            organization="example",
            principal_access_boundary_policy_id="example",
            annotations={"managed-by": "ubx"},
            display_name="example-display-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_principal_access_boundary_policy, with location
    example, organization example, principal access boundary policy
    id example, annotations a set value, display name
    example-display-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="annotations" type="map(string)">
  User defined annotations. See https://google.aip.dev/148#annotations for more details such as format and size limitations **Note**: This field is non-authoritative, and will only manage the annotations present in your configuration. Please refer to the field 'effective_annotations' for all of the annotations present on the resource. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="details" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enforcement_version" type="string">
    </ResponseField>
    <ResponseField name="rules" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="description" type="string">
        </ResponseField>
        <ResponseField name="effect" type="string" required>
        </ResponseField>
        <ResponseField name="resources" type="list(string)" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="display_name" type="string">
  The description of the principal access boundary policy. Must be less than or equal to 63 characters. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location the principal access boundary policy is in. Required.
</ResponseField>

<ResponseField name="organization" type="string" required>
  The parent organization of the principal access boundary policy. Required.
</ResponseField>

<ResponseField name="principal_access_boundary_policy_id" type="string" required>
  The ID to use to create the principal access boundary policy. This value must start with a lowercase letter followed by up to 62 lowercase letters, numbers, hyphens, or dots. Pattern, /a-z&#123;2,62&#125;/. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Output only. The time when the principal access boundary policy was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="details" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enforcement_version" type="string">
    </ResponseField>
    <ResponseField name="rules" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="description" type="string">
        </ResponseField>
        <ResponseField name="effect" type="string" required>
        </ResponseField>
        <ResponseField name="resources" type="list(string)" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="effective_annotations" type="map(string)">
  All of annotations (key/value pairs) present on the resource in GCP, including the annotations configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  The etag for the principal access boundary. If this is provided on update, it must match the server's etag. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the principal access boundary policy. The following format is supported: 'organizations/&#123;organization_id&#125;/locations/&#123;location&#125;/principalAccessBoundaryPolicies/&#123;policy_id&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The globally unique ID of the principal access boundary policy. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the principal access boundary policy was most recently updated. Computed by GCP after `ship`.
</ResponseField>
