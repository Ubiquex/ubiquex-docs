---
title: "google_iam_workload_identity_pool_managed_identity"
description: "Real, generated bindings for `google_iam_workload_identity_pool_managed_identity`."
---

`google_iam_workload_identity_pool_managed_identity` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workload identity pool managed identity"})

    		ubx.Resource(iam.WorkloadIdentityPoolManagedIdentity, "ci-runner", iam.WorkloadIdentityPoolManagedIdentityConfig{
    			WorkloadIdentityPoolId:                "example",
    			WorkloadIdentityPoolManagedIdentityId: "example",
    			WorkloadIdentityPoolNamespaceId:       "example",
    			Description:                           "Managed by ubx.",
    			Disabled:                              true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkloadIdentityPoolManagedIdentity } from "jsr:@ubx/sdk-google/google/iam/workload_identity_pool_managed_identity";

    export default stack("payments", () => {
      intent({ summary: "payments own workload identity pool managed identity" });

      resource(WorkloadIdentityPoolManagedIdentity, "ci-runner", {
        workloadIdentityPoolId: "example",
        workloadIdentityPoolManagedIdentityId: "example",
        workloadIdentityPoolNamespaceId: "example",
        description: "Managed by ubx.",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iam import WorkloadIdentityPoolManagedIdentity, WorkloadIdentityPoolManagedIdentityConfig

    def describe():
        ubx.intent("payments own workload identity pool managed identity")

        ubx.resource(WorkloadIdentityPoolManagedIdentity, "ci-runner", WorkloadIdentityPoolManagedIdentityConfig(
            workload_identity_pool_id="example",
            workload_identity_pool_managed_identity_id="example",
            workload_identity_pool_namespace_id="example",
            description="Managed by ubx.",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_workload_identity_pool_managed_identity, with
    workload identity pool id example, workload identity pool managed
    identity id example, workload identity pool namespace id example,
    description Managed by ubx., disabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attestation_rules" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="google_cloud_resource" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the managed identity. Cannot exceed 256 characters. Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Whether the managed identity is disabled. If disabled, credentials may no longer be issued for the identity, however existing credentials will still be accepted until they expire. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workload_identity_pool_id" type="string" required>
  The ID to use for the pool, which becomes the final component of the resource name. This value should be 4-32 characters, and may contain the characters [a-z0-9-]. The prefix 'gcp-' is reserved for use by Google, and may not be specified. Required.
</ResponseField>

<ResponseField name="workload_identity_pool_managed_identity_id" type="string" required>
  The ID to use for the managed identity. This value must: * contain at most 63 characters * contain only lowercase alphanumeric characters or '-' * start with an alphanumeric character * end with an alphanumeric character The prefix 'gcp-' will be reserved for future uses. Required.
</ResponseField>

<ResponseField name="workload_identity_pool_namespace_id" type="string" required>
  The ID to use for the namespace. This value must: * contain at most 63 characters * contain only lowercase alphanumeric characters or '-' * start with an alphanumeric character * end with an alphanumeric character The prefix 'gcp-' will be reserved for future uses. Required.
</ResponseField>

## Output properties

<ResponseField name="attestation_rules" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="google_cloud_resource" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the managed identity as 'projects/&#123;project_number&#125;/locations/global/workloadIdentityPools/&#123;workload_identity_pool_id&#125;/namespaces/&#123;workload_identity_pool_namespace_id&#125;/managedIdentities/&#123;workload_identity_pool_managed_identity_id&#125;'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the managed identity. * 'ACTIVE': The managed identity is active. * 'DELETED': The managed identity is soft-deleted. Soft-deleted managed identities are permanently deleted after approximately 30 days. You can restore a soft-deleted managed identity using UndeleteWorkloadIdentityPoolManagedIdentity. You cannot reuse the ID of a soft-deleted managed identity until it is permanently deleted. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
