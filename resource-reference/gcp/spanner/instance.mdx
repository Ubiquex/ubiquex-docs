---
title: "google_spanner_instance"
description: "Real, generated bindings for `google_spanner_instance`."
---

`google_spanner_instance` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	spanner "github.com/ubiquex/ubx-sdk-google/sdk/go/google/spanner"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance"})

    		ubx.Resource(spanner.Instance, "ci-runner", spanner.InstanceConfig_{
    			Config:       "example",
    			DisplayName:  "example-display-name",
    			Name:         "ci-runner",
    			ForceDestroy: true,
    			Labels:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Instance } from "jsr:@ubx/sdk-google/google/spanner/instance";

    export default stack("payments", () => {
      intent({ summary: "payments own instance" });

      resource(Instance, "ci-runner", {
        config: "example",
        displayName: "example-display-name",
        name: "ci-runner",
        forceDestroy: true,
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.spanner import Instance, InstanceConfig_

    def describe():
        ubx.intent("payments own instance")

        ubx.resource(Instance, "ci-runner", InstanceConfig_(
            config="example",
            display_name="example-display-name",
            name="ci-runner",
            force_destroy=True,
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_spanner_instance called "ci-runner", with config
    example, display name example-display-name, force destroy true,
    labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="autoscaling_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="asymmetric_autoscaling_options" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="overrides" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="autoscaling_limits" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="max_nodes" type="number">
                </ResponseField>
                <ResponseField name="max_processing_units" type="number">
                </ResponseField>
                <ResponseField name="min_nodes" type="number">
                </ResponseField>
                <ResponseField name="min_processing_units" type="number">
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="autoscaling_target_high_priority_cpu_utilization_percent" type="number">
            </ResponseField>
            <ResponseField name="autoscaling_target_total_cpu_utilization_percent" type="number">
            </ResponseField>
            <ResponseField name="disable_high_priority_cpu_autoscaling" type="bool">
            </ResponseField>
            <ResponseField name="disable_total_cpu_autoscaling" type="bool">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="replica_selection" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="location" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="autoscaling_limits" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="max_nodes" type="number">
        </ResponseField>
        <ResponseField name="max_processing_units" type="number">
        </ResponseField>
        <ResponseField name="min_nodes" type="number">
        </ResponseField>
        <ResponseField name="min_processing_units" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="autoscaling_targets" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="high_priority_cpu_utilization_percent" type="number">
        </ResponseField>
        <ResponseField name="storage_utilization_percent" type="number">
        </ResponseField>
        <ResponseField name="total_cpu_utilization_percent" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="config" type="string" required>
  The name of the instance's configuration (similar but not quite the same as a region) which defines the geographic placement and replication of your databases in this instance. It determines where your data is stored. Values are typically of the form 'regional-europe-west1' , 'us-central' etc. In order to obtain a valid list please consult the [Configuration section of the docs](https://cloud.google.com/spanner/docs/instances). Required.
</ResponseField>

<ResponseField name="default_backup_schedule_type" type="string">
  Controls the default backup behavior for new databases within the instance. Note that 'AUTOMATIC' is not permitted for free instances, as backups and backup schedules are not allowed for free instances. if unset or NONE, no default backup schedule will be created for new databases within the instance. Possible values: ["NONE", "AUTOMATIC"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The descriptive name for this instance as it appears in UIs. Must be unique per project and between 4 and 30 characters in length. Required.
</ResponseField>

<ResponseField name="edition" type="string">
  The edition selected for this instance. Different editions provide different capabilities at different price points. Possible values: ["EDITION_UNSPECIFIED", "STANDARD", "ENTERPRISE", "ENTERPRISE_PLUS"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  When deleting a spanner instance, this boolean option will delete all backups of this instance. This must be set to true if you created a backup manually in the console. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_type" type="string">
  The type of this instance. The type can be used to distinguish product variants, that can affect aspects like: usage restrictions, quotas and billing. Currently this is used to distinguish FREE_INSTANCE vs PROVISIONED instances. When configured as FREE_INSTANCE, the field 'edition' should not be configured. Possible values: ["PROVISIONED", "FREE_INSTANCE"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  An object containing a list of "key": value pairs. Example: &#123; "name": "wrench", "mass": "1.3kg", "count": "3" &#125;. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  A unique identifier for the instance, which cannot be changed after the instance is created. The name must be between 6 and 30 characters in length. If not provided, a random string starting with 'tf-' will be selected. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="num_nodes" type="number">
  The number of nodes allocated to this instance. Exactly one of either num_nodes, processing_units or autoscaling_config must be present in terraform except when instance_type = FREE_INSTANCE. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="processing_units" type="number">
  The number of processing units allocated to this instance. Exactly one of either num_nodes, processing_units or autoscaling_config must be present in terraform except when instance_type = FREE_INSTANCE. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="autoscaling_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="asymmetric_autoscaling_options" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="overrides" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="autoscaling_limits" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="max_nodes" type="number">
                </ResponseField>
                <ResponseField name="max_processing_units" type="number">
                </ResponseField>
                <ResponseField name="min_nodes" type="number">
                </ResponseField>
                <ResponseField name="min_processing_units" type="number">
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="autoscaling_target_high_priority_cpu_utilization_percent" type="number">
            </ResponseField>
            <ResponseField name="autoscaling_target_total_cpu_utilization_percent" type="number">
            </ResponseField>
            <ResponseField name="disable_high_priority_cpu_autoscaling" type="bool">
            </ResponseField>
            <ResponseField name="disable_total_cpu_autoscaling" type="bool">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="replica_selection" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="location" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="autoscaling_limits" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="max_nodes" type="number">
        </ResponseField>
        <ResponseField name="max_processing_units" type="number">
        </ResponseField>
        <ResponseField name="min_nodes" type="number">
        </ResponseField>
        <ResponseField name="min_processing_units" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="autoscaling_targets" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="high_priority_cpu_utilization_percent" type="number">
        </ResponseField>
        <ResponseField name="storage_utilization_percent" type="number">
        </ResponseField>
        <ResponseField name="total_cpu_utilization_percent" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_backup_schedule_type" type="string">
  Controls the default backup behavior for new databases within the instance. Note that 'AUTOMATIC' is not permitted for free instances, as backups and backup schedules are not allowed for free instances. if unset or NONE, no default backup schedule will be created for new databases within the instance. Possible values: ["NONE", "AUTOMATIC"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="edition" type="string">
  The edition selected for this instance. Different editions provide different capabilities at different price points. Possible values: ["EDITION_UNSPECIFIED", "STANDARD", "ENTERPRISE", "ENTERPRISE_PLUS"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_type" type="string">
  The type of this instance. The type can be used to distinguish product variants, that can affect aspects like: usage restrictions, quotas and billing. Currently this is used to distinguish FREE_INSTANCE vs PROVISIONED instances. When configured as FREE_INSTANCE, the field 'edition' should not be configured. Possible values: ["PROVISIONED", "FREE_INSTANCE"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  A unique identifier for the instance, which cannot be changed after the instance is created. The name must be between 6 and 30 characters in length. If not provided, a random string starting with 'tf-' will be selected. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="num_nodes" type="number">
  The number of nodes allocated to this instance. Exactly one of either num_nodes, processing_units or autoscaling_config must be present in terraform except when instance_type = FREE_INSTANCE. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="processing_units" type="number">
  The number of processing units allocated to this instance. Exactly one of either num_nodes, processing_units or autoscaling_config must be present in terraform except when instance_type = FREE_INSTANCE. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Instance status: 'CREATING' or 'READY'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
