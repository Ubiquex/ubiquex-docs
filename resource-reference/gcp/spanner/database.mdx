---
title: "google_spanner_database"
description: "Real, generated bindings for `google_spanner_database`."
---

`google_spanner_database` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	spanner "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/spanner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own database"})

    		ubx.Resource(spanner.Database, "ci-runner", spanner.DatabaseConfig{
    			Instance:             "example",
    			Name:                 "ci-runner",
    			Ddl:                  []string{"example"},
    			DefaultTimeZone:      "example",
    			DeletionProtection:   true,
    			EnableDropProtection: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Database } from "jsr:@ubx/sdk-google/google/spanner/database";

    export default stack("payments", () => {
      intent({ summary: "payments own database" });

      resource(Database, "ci-runner", {
        instance: "example",
        name: "ci-runner",
        ddl: ["example"],
        defaultTimeZone: "example",
        deletionProtection: true,
        enableDropProtection: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.spanner import Database, DatabaseConfig

    def describe():
        ubx.intent("payments own database")

        ubx.resource(Database, "ci-runner", DatabaseConfig(
            instance="example",
            name="ci-runner",
            ddl=["example"],
            default_time_zone="example",
            deletion_protection=True,
            enable_drop_protection=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_spanner_database called "ci-runner", with instance
    example, ddl a set value, default time zone example, deletion
    protection true, enable drop protection true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="database_dialect" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ddl" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="default_time_zone" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_protection" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_drop_protection" type="bool">
  Optional.
</ResponseField>

<ResponseField name="encryption_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
    </ResponseField>
    <ResponseField name="kms_key_names" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version_retention_period" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="database_dialect" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="encryption_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
    </ResponseField>
    <ResponseField name="kms_key_names" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version_retention_period" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
