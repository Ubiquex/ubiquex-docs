---
title: "google_spanner_instance_iam_policy"
description: "Real, generated bindings for `google_spanner_instance_iam_policy`."
---

`google_spanner_instance_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/spanner"
    )

    cfg := spanner.InstanceIamPolicyConfig{
        Instance: "example",
        PolicyData: "example",
    }
    ubx.Resource(spanner.InstanceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InstanceIamPolicy } from
      "jsr:@ubx/sdk-google/google/spanner/instance_iam_policy";

    resource(InstanceIamPolicy, "example", {
      instance: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.spanner import (
        InstanceIamPolicy,
        InstanceIamPolicyConfig,
    )

    cfg = InstanceIamPolicyConfig(
        instance="example",
        policy_data="example",
    )
    ubx.resource(InstanceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_spanner_instance_iam_policy with instance,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
