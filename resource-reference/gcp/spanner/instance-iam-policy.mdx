---
title: "google_spanner_instance_iam_policy"
description: "Real, generated bindings for `google_spanner_instance_iam_policy`."
---

`google_spanner_instance_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	spanner "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/spanner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance iam policy"})

    		ubx.Resource(spanner.InstanceIamPolicy, "ci-runner", spanner.InstanceIamPolicyConfig{
    			Instance:   "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceIamPolicy } from "jsr:@ubx/sdk-google/google/spanner/instance_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own instance iam policy" });

      resource(InstanceIamPolicy, "ci-runner", {
        instance: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.spanner import InstanceIamPolicy, InstanceIamPolicyConfig

    def describe():
        ubx.intent("payments own instance iam policy")

        ubx.resource(InstanceIamPolicy, "ci-runner", InstanceIamPolicyConfig(
            instance="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_spanner_instance_iam_policy, with instance example,
    policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
