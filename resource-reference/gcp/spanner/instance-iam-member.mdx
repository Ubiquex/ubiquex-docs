---
title: "google_spanner_instance_iam_member"
description: "Real, generated bindings for `google_spanner_instance_iam_member`."
---

`google_spanner_instance_iam_member` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/spanner"
    )

    cfg := spanner.InstanceIamMemberConfig{
        Instance: "example",
        Member: "example",
        Role: "example",
    }
    ubx.Resource(spanner.InstanceIamMember, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InstanceIamMember } from
      "jsr:@ubx/sdk-google/google/spanner/instance_iam_member";

    resource(InstanceIamMember, "example", {
      instance: "example",
      member: "example",
      role: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.spanner import (
        InstanceIamMember,
        InstanceIamMemberConfig,
    )

    cfg = InstanceIamMemberConfig(
        instance="example",
        member="example",
        role="example",
    )
    ubx.resource(InstanceIamMember, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_spanner_instance_iam_member with instance,
member, role.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Required.
</ResponseField>

<ResponseField name="member" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
