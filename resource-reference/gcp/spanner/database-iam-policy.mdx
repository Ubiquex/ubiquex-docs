---
title: "google_spanner_database_iam_policy"
description: "Real, generated bindings for `google_spanner_database_iam_policy`."
---

`google_spanner_database_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	spanner "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/spanner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own database iam policy"})

    		ubx.Resource(spanner.DatabaseIamPolicy, "ci-runner", spanner.DatabaseIamPolicyConfig{
    			Database:   "example",
    			Instance:   "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatabaseIamPolicy } from "jsr:@ubx/sdk-google/google/spanner/database_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own database iam policy" });

      resource(DatabaseIamPolicy, "ci-runner", {
        database: "example",
        instance: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.spanner import DatabaseIamPolicy, DatabaseIamPolicyConfig

    def describe():
        ubx.intent("payments own database iam policy")

        ubx.resource(DatabaseIamPolicy, "ci-runner", DatabaseIamPolicyConfig(
            database="example",
            instance="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_spanner_database_iam_policy, with database example,
    instance example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="database" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
