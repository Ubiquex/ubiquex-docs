---
title: "google_spanner_database_iam_policy"
description: "Real, generated bindings for `google_spanner_database_iam_policy`."
---

`google_spanner_database_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/spanner"
    )

    cfg := spanner.DatabaseIamPolicyConfig{
        Database: "example",
        Instance: "example",
        PolicyData: "example",
    }
    ubx.Resource(spanner.DatabaseIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DatabaseIamPolicy } from
      "jsr:@ubx/sdk-google/google/spanner/database_iam_policy";

    resource(DatabaseIamPolicy, "example", {
      database: "example",
      instance: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.spanner import (
        DatabaseIamPolicy,
        DatabaseIamPolicyConfig,
    )

    cfg = DatabaseIamPolicyConfig(
        database="example",
        instance="example",
        policy_data="example",
    )
    ubx.resource(DatabaseIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_spanner_database_iam_policy with database,
instance, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="database" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
