---
title: "google_spanner_instance_partition"
description: "Real, generated bindings for `google_spanner_instance_partition`."
---

`google_spanner_instance_partition` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	spanner "github.com/ubiquex/ubx-sdk-google/sdk/go/google/spanner"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance partition"})

    		ubx.Resource(spanner.InstancePartition, "ci-runner", spanner.InstancePartitionConfig{
    			Config:      "example",
    			DisplayName: "example-display-name",
    			Instance:    "example",
    			Name:        "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstancePartition } from "jsr:@ubx/sdk-google/google/spanner/instance_partition";

    export default stack("payments", () => {
      intent({ summary: "payments own instance partition" });

      resource(InstancePartition, "ci-runner", {
        config: "example",
        displayName: "example-display-name",
        instance: "example",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.spanner import InstancePartition, InstancePartitionConfig

    def describe():
        ubx.intent("payments own instance partition")

        ubx.resource(InstancePartition, "ci-runner", InstancePartitionConfig(
            config="example",
            display_name="example-display-name",
            instance="example",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_spanner_instance_partition called "ci-runner", with
    config example, display name example-display-name, instance
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="autoscaling_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="autoscaling_limits" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="max_nodes" type="number">
        </ResponseField>
        <ResponseField name="max_processing_units" type="number">
        </ResponseField>
        <ResponseField name="min_nodes" type="number">
        </ResponseField>
        <ResponseField name="min_processing_units" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="autoscaling_targets" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="high_priority_cpu_utilization_percent" type="number">
        </ResponseField>
        <ResponseField name="storage_utilization_percent" type="number">
        </ResponseField>
        <ResponseField name="total_cpu_utilization_percent" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="config" type="string" required>
  The name of the instance partition's configuration (similar to a region) which defines the geographic placement and replication of data in this instance partition. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The descriptive name for this instance partition as it appears in UIs. Must be unique per project and between 4 and 30 characters in length. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  The instance to create the instance partition in. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  A unique identifier for the instance partition, which cannot be changed after the instance partition is created. The name must be between 2 and 64 characters and match the regular expression [a-z][a-z0-9\\-]&#123;0,61&#125;[a-z0-9]. Required.
</ResponseField>

<ResponseField name="node_count" type="number">
  The number of nodes allocated to this instance partition. One node equals 1000 processing units. Exactly one of either node_count, processing_units, or autoscaling_config must be present. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="processing_units" type="number">
  The number of processing units allocated to this instance partition. Exactly one of either node_count, processing_units, or autoscaling_config must be present. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="autoscaling_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="autoscaling_limits" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="max_nodes" type="number">
        </ResponseField>
        <ResponseField name="max_processing_units" type="number">
        </ResponseField>
        <ResponseField name="min_nodes" type="number">
        </ResponseField>
        <ResponseField name="min_processing_units" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="autoscaling_targets" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="high_priority_cpu_utilization_percent" type="number">
        </ResponseField>
        <ResponseField name="storage_utilization_percent" type="number">
        </ResponseField>
        <ResponseField name="total_cpu_utilization_percent" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="node_count" type="number">
  The number of nodes allocated to this instance partition. One node equals 1000 processing units. Exactly one of either node_count, processing_units, or autoscaling_config must be present. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="processing_units" type="number">
  The number of processing units allocated to this instance partition. Exactly one of either node_count, processing_units, or autoscaling_config must be present. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The current instance partition state. Possible values are: CREATING: The instance partition is being created. Resources are being allocated for the instance partition. READY: The instance partition has been allocated resources and is ready for use. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
