---
title: "google_metastore_v1beta_metadata_import"
description: "`google_metastore_v1beta_metadata_import` bulk-loads existing Hive metastore metadata into a `google_metastore_service`..."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_metastore_metadata_import`.
</Warning>

`google_metastore_v1beta_metadata_import` bulk-loads existing Hive metastore metadata into a `google_metastore_service`, the real, one-time migration path for bringing an external Hive metadata catalog under Dataproc Metastore management.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_metastore_v1beta --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_metastore_v1beta/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	metastore "github.com/ubiquex/ubx-sdk-google/sdk/go/google_metastore_v1beta/metastore"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta metadata import"})

    		ubx.Resource(metastore.V1betaMetastoreV1betaMetadataImport, "example", metastore.V1betaMetastoreV1betaMetadataImportConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_metastore_v1beta --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaMetastoreV1betaMetadataImport } from "./local-sdk/google_metastore_v1beta/sdk/typescript/google_metastore_v1beta/metastore/v1beta_metastore_v1beta_metadata_import.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta metadata import" });

      resource(V1betaMetastoreV1betaMetadataImport, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_metastore_v1beta --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_metastore_v1beta/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_metastore_v1beta.metastore import V1betaMetastoreV1betaMetadataImport, V1betaMetastoreV1betaMetadataImportConfig

    def describe():
        ubx.intent("example own v1beta metadata import")

        ubx.resource(V1betaMetastoreV1betaMetadataImport, "example", V1betaMetastoreV1betaMetadataImportConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_metastore_v1beta_metadata_import called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The time when the metadata import was started. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="database_dump" type="object">
  A specification of the location of and metadata about a database dump from a relational database management system. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="database_type" type="string">
      The type of the database. Optional.
    </ResponseField>
    <ResponseField name="gcs_uri" type="string">
      Optional. A Cloud Storage object or folder URI that specifies the source from which to import metadata. It must begin with gs://. Optional.
    </ResponseField>
    <ResponseField name="source_database" type="string">
      Optional. The name of the source database. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional. The type of the database dump. If unspecified, defaults to MYSQL. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  Optional. The description of the metadata import. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="end_time" type="string">
  Output only. The time when the metadata import finished. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. Identifier. The relative resource name of the metadata import, of the form:projects/&#123;project_number&#125;/locations/&#123;location_id&#125;/services/&#123;service_id&#125;/metadataImports/&#123;metadata_import_id&#125;. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the metadata import. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the metadata import was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
