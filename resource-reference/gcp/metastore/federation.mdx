---
title: "google_metastore_federation"
description: "`google_metastore_federation` exposes a single, real gRPC-queryable metadata surface federating multiple real backend metastores..."
---

`google_metastore_federation` exposes a single, real gRPC-queryable metadata surface federating multiple real backend metastores, such as BigQuery datasets, Dataplex Lakes, and other Dataproc Metastore services, without physically merging their data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_metastore --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_metastore/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	metastore "github.com/ubiquex/ubx-sdk-google/sdk/go/google_metastore/metastore"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own federation"})

    		ubx.Resource(metastore.MetastoreFederation, "ci-runner", metastore.MetastoreFederationConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_metastore --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MetastoreFederation } from "./local-sdk/google_metastore/sdk/typescript/google_metastore/metastore/metastore_federation.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own federation" });

      resource(MetastoreFederation, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_metastore --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_metastore/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_metastore.metastore import MetastoreFederation, MetastoreFederationConfig

    def describe():
        ubx.intent("payments own federation")

        ubx.resource(MetastoreFederation, "ci-runner", MetastoreFederationConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_metastore_federation called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="backend_metastores" type="map(object)">
  A map from BackendMetastore rank to BackendMetastores from which the federation service serves metadata at query time. The map key represents the order in which BackendMetastores should be evaluated to resolve database names at query time and should be greater than or equal to zero. A BackendMetastore with a lower number will be evaluated before a BackendMetastore with a higher number. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="metastore_type" type="string">
      The real backend metastore type (e.g. Dataproc Metastore, BigQuery) this federation member represents. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The real, fully-qualified resource name of this federation's own backend metastore member. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the metastore federation was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="endpoint_uri" type="string">
  Output only. The federation endpoint. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  User-defined labels for the metastore federation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. The relative resource name of the federation, of the form: projects/&#123;project_number&#125;/locations/&#123;location_id&#125;/federations/&#123;federation_id&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the federation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state_message" type="string">
  Output only. Additional information about the current state of the metastore federation, if available. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The globally unique resource identifier of the metastore federation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the metastore federation was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="version" type="string">
  Immutable. The Apache Hive metastore version of the federation. All backend metastore versions must be compatible with the federation version. Optional; if omitted, computed by Google Cloud.
</ResponseField>
