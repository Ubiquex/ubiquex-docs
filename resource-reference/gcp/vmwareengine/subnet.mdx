---
title: "google_vmwareengine_subnet"
description: "Real, generated bindings for `google_vmwareengine_subnet`."
---

`google_vmwareengine_subnet` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmwareengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vmwareengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own subnet"})

    		ubx.Resource(vmwareengine.Subnet, "ci-runner", vmwareengine.SubnetConfig{
    			IpCidrRange: "example",
    			Name:        "ci-runner",
    			Parent:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Subnet } from "jsr:@ubx/sdk-google/google/vmwareengine/subnet";

    export default stack("payments", () => {
      intent({ summary: "payments own subnet" });

      resource(Subnet, "ci-runner", {
        ipCidrRange: "example",
        name: "ci-runner",
        parent: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vmwareengine import Subnet, SubnetConfig

    def describe():
        ubx.intent("payments own subnet")

        ubx.resource(Subnet, "ci-runner", SubnetConfig(
            ip_cidr_range="example",
            name="ci-runner",
            parent="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vmwareengine_subnet called "ci-runner", with ip
    cidr range example, parent example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_cidr_range" type="string" required>
  The IP address range of the subnet in CIDR format. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The ID of the subnet. For userDefined subnets, this name should be in the format of "service-n", where n ranges from 1 to 5. Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  The resource name of the private cloud to create a new subnet in. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. For example: projects/my-project/locations/us-west1-a/privateClouds/my-cloud Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Creation time of this resource. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="dhcp_address_ranges" type="list(object)">
  DHCP address ranges. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="first_address" type="string">
    </ResponseField>
    <ResponseField name="last_address" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="gateway_id" type="string">
  The canonical identifier of the logical router that this subnet is attached to. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="gateway_ip" type="string">
  The IP address of the gateway of this subnet. Must fall within the IP prefix defined above. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="standard_config" type="bool">
  Whether the NSX-T configuration in the backend follows the standard configuration supported by Google Cloud. If false, the subnet cannot be modified through Google Cloud, only through NSX-T directly. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  State of the subnet. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of the subnet. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="uid" type="string">
  System-generated unique identifier for the resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Last updated time of this resource. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="vlan_id" type="number">
  VLAN ID of the VLAN on which the subnet is configured. Computed by GCP after `ship`.
</ResponseField>
