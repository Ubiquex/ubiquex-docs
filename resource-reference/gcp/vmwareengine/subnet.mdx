---
title: "google_vmwareengine_subnet"
description: "Real, generated bindings for `google_vmwareengine_subnet`."
---

`google_vmwareengine_subnet` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vmwareengine"
    )

    cfg := vmwareengine.SubnetConfig{
        IpCidrRange: "example",
        Name: "example-name",
        Parent: "example",
    }
    ubx.Resource(vmwareengine.Subnet, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Subnet } from
      "jsr:@ubx/sdk-google/google/vmwareengine/subnet";

    resource(Subnet, "example", {
      ipCidrRange: "example",
      name: "example-name",
      parent: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.vmwareengine import Subnet, SubnetConfig

    cfg = SubnetConfig(
        ip_cidr_range="example",
        name="example-name",
        parent="example",
    )
    ubx.resource(Subnet, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_vmwareengine_subnet with ip_cidr_range, name,
parent.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_cidr_range" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="dhcp_address_ranges" type="list(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="first_address" type="string">
    </ResponseField>
    <ResponseField name="last_address" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="gateway_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="gateway_ip" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="standard_config" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="uid" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="vlan_id" type="number">
  Computed by GCP after `ship`.
</ResponseField>
