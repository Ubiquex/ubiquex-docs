---
title: "google_vmwareengine_private_cloud"
description: "`google_vmwareengine_private_cloud` is the top-level Google Cloud VMware Engine resource -- a dedicated..."
---

`google_vmwareengine_private_cloud` is the top-level Google Cloud VMware Engine resource -- a dedicated, VMware-based private cloud environment (vCenter, NSX-T, vSAN) running on Google Cloud infrastructure.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_vmwareengine --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_vmwareengine/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmwareengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google_vmwareengine/vmwareengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own private cloud"})

    		ubx.Resource(vmwareengine.VmwareenginePrivateCloud, "example", vmwareengine.VmwareenginePrivateCloudConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_vmwareengine --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwareenginePrivateCloud } from "./local-sdk/google_vmwareengine/sdk/typescript/google_vmwareengine/vmwareengine/vmwareengine_private_cloud.ts";

    export default stack("example", () => {
      intent({ summary: "example own private cloud" });

      resource(VmwareenginePrivateCloud, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_vmwareengine --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_vmwareengine/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_vmwareengine.vmwareengine import VmwareenginePrivateCloud, VmwareenginePrivateCloudConfig

    def describe():
        ubx.intent("example own private cloud")

        ubx.resource(VmwareenginePrivateCloud, "example", VmwareenginePrivateCloudConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vmwareengine_private_cloud called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Creation time of this resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. Time when the resource was scheduled for deletion. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  User-provided description for this private cloud. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="encryption_config" type="object">
  Encryption configuration for a private cloud. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="crypto_key_name" type="string">
      Optional. The resource name of the Cloud KMS key to be used for CMEK encryption. The format of this field is `projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;key_ring&#125;/cryptoKeys/&#123;crypto_key&#125;`. The key must be in the same region as the private cloud. This key is used for wrapping the key-encrypting key of vSAN clusters. This field must be provided when `type` is `CMEK` or `LEGACY_CMEK`, and must not be set when `type` is `OTHER`. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      Required. The encryption type of the private cloud. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="expire_time" type="string">
  Output only. Time when the resource will be irreversibly deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="hcx" type="object">
  Details about a HCX Cloud Manager appliance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="fqdn" type="string">
      Fully qualified domain name of the appliance. Optional.
    </ResponseField>
    <ResponseField name="internal_ip" type="string">
      Internal IP address of the appliance. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The state of the appliance. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="version" type="string">
      Version of the appliance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="management_cluster" type="object">
  Management cluster configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cluster_id" type="string">
      Required. The user-provided identifier of the new `Cluster`. The identifier must meet the following requirements: * Only contains 1-63 alphanumeric characters and hyphens * Begins with an alphabetical character * Ends with a non-hyphen character * Not formatted as a UUID * Complies with [RFC 1034](https://datatracker.ietf.org/doc/html/rfc1034) (section 3.5) Optional.
    </ResponseField>
    <ResponseField name="node_type_configs" type="map(object)">
      Required. The map of cluster node types in this cluster, where the key is canonical identifier of the node type (corresponds to the `NodeType`). Optional.
      <Expandable title="properties">
        <ResponseField name="custom_core_count" type="number">
          The number of CPU core(s) enabled per node, when overriding the node type's own default. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="node_count" type="number">
          The number of node(s) provisioned. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stretched_cluster_config" type="object">
      Configuration of a stretched cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="preferred_location" type="string">
          Required. Zone that will remain operational when connection between the two zones is lost. Specify the resource name of a zone that belongs to the region of the private cloud. For example: `projects/&#123;project&#125;/locations/europe-west3-a` where `&#123;project&#125;` can either be a project number or a project ID. Optional.
        </ResponseField>
        <ResponseField name="secondary_location" type="string">
          Required. Additional zone for a higher level of availability and load balancing. Specify the resource name of a zone that belongs to the region of the private cloud. For example: `projects/&#123;project&#125;/locations/europe-west3-b` where `&#123;project&#125;` can either be a project number or a project ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. The resource name of this private cloud. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. For example: `projects/my-project/locations/us-central1-a/privateClouds/my-cloud` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="network_config" type="object">
  Network configuration in the consumer project with which the peering has to be done. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="dns_server_ip" type="string">
      Output only. DNS Server IP of the Private Cloud. All DNS queries can be forwarded to this address for name resolution of Private Cloud's management entities like vCenter, NSX-T Manager and ESXi hosts. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="management_cidr" type="string">
      Required. Management CIDR used by VMware management appliances. Optional.
    </ResponseField>
    <ResponseField name="management_ip_address_layout_version" type="number">
      Output only. The IP address layout version of the management IP address range. Possible versions include: * `managementIpAddressLayoutVersion=1`: Indicates the legacy IP address layout used by some existing private clouds. This is no longer supported for new private clouds as it does not support all features. * `managementIpAddressLayoutVersion=2`: Indicates the latest IP address layout used by all newly created private clouds. This version supports all current features. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="vmware_engine_network" type="string">
      Optional. The relative resource name of the VMware Engine network attached to the private cloud. Specify the name in the following form: `projects/&#123;project&#125;/locations/&#123;location&#125;/vmwareEngineNetworks/&#123;vmware_engine_network_id&#125;` where `&#123;project&#125;` can either be a project number or a project ID. Optional.
    </ResponseField>
    <ResponseField name="vmware_engine_network_canonical" type="string">
      Output only. The canonical name of the VMware Engine network in the form: `projects/&#123;project_number&#125;/locations/&#123;location&#125;/vmwareEngineNetworks/&#123;vmware_engine_network_id&#125;` Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="nsx" type="object">
  Details about a NSX Manager appliance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="fqdn" type="string">
      Fully qualified domain name of the appliance. Optional.
    </ResponseField>
    <ResponseField name="internal_ip" type="string">
      Internal IP address of the appliance. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The state of the appliance. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="version" type="string">
      Version of the appliance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the resource. New values may be added to this enum when appropriate. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  Optional. Type of the private cloud. Defaults to STANDARD. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System-generated unique identifier for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Last update time of this resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="vcenter" type="object">
  Details about a vCenter Server management appliance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="fqdn" type="string">
      Fully qualified domain name of the appliance. Optional.
    </ResponseField>
    <ResponseField name="internal_ip" type="string">
      Internal IP address of the appliance. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The state of the appliance. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="version" type="string">
      Version of the appliance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
