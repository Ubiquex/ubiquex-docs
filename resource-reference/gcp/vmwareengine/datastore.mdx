---
title: "google_vmwareengine_datastore"
description: "Real, generated bindings for `google_vmwareengine_datastore`."
---

`google_vmwareengine_datastore` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmwareengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vmwareengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own datastore"})

    		ubx.Resource(vmwareengine.Datastore, "ci-runner", vmwareengine.DatastoreConfig{
    			Location:    "example",
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Datastore } from "jsr:@ubx/sdk-google/google/vmwareengine/datastore";

    export default stack("payments", () => {
      intent({ summary: "payments own datastore" });

      resource(Datastore, "ci-runner", {
        location: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vmwareengine import Datastore, DatastoreConfig

    def describe():
        ubx.intent("payments own datastore")

        ubx.resource(Datastore, "ci-runner", DatastoreConfig(
            location="example",
            name="ci-runner",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vmwareengine_datastore called "ci-runner", with
    location example, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  User-provided description for this datastore Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The user-provided identifier of the datastore to be created. This identifier must be unique among each 'Datastore' within the parent and becomes the final token in the name URI. The identifier must meet the following requirements: * Only contains 1-63 alphanumeric characters and hyphens * Begins with an alphabetical character * Ends with a non-hyphen character * Not formatted as a UUID * Complies with [RFC 1034](https://datatracker.ietf.org/doc/html/rfc1034) (section 3.5) Required.
</ResponseField>

<ResponseField name="nfs_datastore" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="google_file_service" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="filestore_instance" type="string">
        </ResponseField>
        <ResponseField name="netapp_volume" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="third_party_file_service" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="file_share" type="string" required>
        </ResponseField>
        <ResponseField name="network" type="string" required>
        </ResponseField>
        <ResponseField name="servers" type="list(string)" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="clusters" type="list(string)">
  Clusters to which the datastore is attached. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Creation time of this resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="nfs_datastore" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="google_file_service" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="filestore_instance" type="string">
        </ResponseField>
        <ResponseField name="netapp_volume" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="third_party_file_service" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="file_share" type="string" required>
        </ResponseField>
        <ResponseField name="network" type="string" required>
        </ResponseField>
        <ResponseField name="servers" type="list(string)" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the Datastore. Possible values: CREATING ACTIVE UPDATING DELETING SOFT_DELETING SOFT_DELETED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  System-generated unique identifier for the resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Last update time of this resource. Computed by GCP after `ship`.
</ResponseField>
