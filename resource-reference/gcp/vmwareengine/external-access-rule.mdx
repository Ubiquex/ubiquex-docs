---
title: "google_vmwareengine_external_access_rule"
description: "Real, generated bindings for `google_vmwareengine_external_access_rule`."
---

`google_vmwareengine_external_access_rule` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmwareengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vmwareengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own external access rule"})

    		ubx.Resource(vmwareengine.ExternalAccessRule, "ci-runner", vmwareengine.ExternalAccessRuleConfig{
    			Action:           "example",
    			DestinationPorts: []string{"example"},
    			IpProtocol:       "example",
    			Name:             "ci-runner",
    			Parent:           "example",
    			Priority:         1,
    			SourcePorts:      []string{"example"},
    			Description:      "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExternalAccessRule } from "jsr:@ubx/sdk-google/google/vmwareengine/external_access_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own external access rule" });

      resource(ExternalAccessRule, "ci-runner", {
        action: "example",
        destinationPorts: ["example"],
        ipProtocol: "example",
        name: "ci-runner",
        parent: "example",
        priority: 1,
        sourcePorts: ["example"],
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vmwareengine import ExternalAccessRule, ExternalAccessRuleConfig

    def describe():
        ubx.intent("payments own external access rule")

        ubx.resource(ExternalAccessRule, "ci-runner", ExternalAccessRuleConfig(
            action="example",
            destination_ports=["example"],
            ip_protocol="example",
            name="ci-runner",
            parent="example",
            priority=1,
            source_ports=["example"],
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vmwareengine_external_access_rule called
    "ci-runner", with action example, destination ports a set value,
    ip protocol example, parent example, priority 1, source ports a
    set value, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="action" type="string" required>
  The action that the external access rule performs. Possible values: ["ALLOW", "DENY"] Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  User-provided description for the external access rule. Optional.
</ResponseField>

<ResponseField name="destination_ip_ranges" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="external_address" type="string">
    </ResponseField>
    <ResponseField name="ip_address_range" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="destination_ports" type="list(string)" required>
  A list of destination ports to which the external access rule applies. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_protocol" type="string" required>
  The IP protocol to which the external access rule applies. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The ID of the external access rule. Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  The resource name of the network policy. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. For example: projects/my-project/locations/us-west1-a/networkPolicies/my-policy Required.
</ResponseField>

<ResponseField name="priority" type="number" required>
  External access rule priority, which determines the external access rule to use when multiple rules apply. Required.
</ResponseField>

<ResponseField name="source_ip_ranges" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ip_address" type="string">
    </ResponseField>
    <ResponseField name="ip_address_range" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_ports" type="list(string)" required>
  A list of source ports to which the external access rule applies. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Creation time of this resource. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="destination_ip_ranges" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="external_address" type="string">
    </ResponseField>
    <ResponseField name="ip_address_range" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_ip_ranges" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ip_address" type="string">
    </ResponseField>
    <ResponseField name="ip_address_range" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  State of the Cluster. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  System-generated unique identifier for the resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Last updated time of this resource. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>
