---
title: "google_vmwareengine_network_peering"
description: "Real, generated bindings for `google_vmwareengine_network_peering`."
---

`google_vmwareengine_network_peering` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmwareengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vmwareengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network peering"})

    		ubx.Resource(vmwareengine.NetworkPeering, "ci-runner", vmwareengine.NetworkPeeringConfig{
    			Name:                           "ci-runner",
    			PeerNetwork:                    "example",
    			PeerNetworkType:                "example",
    			VmwareEngineNetwork:            "example",
    			Description:                    "Managed by ubx.",
    			ExportCustomRoutes:             true,
    			ExportCustomRoutesWithPublicIp: true,
    			ImportCustomRoutes:             true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkPeering } from "jsr:@ubx/sdk-google/google/vmwareengine/network_peering";

    export default stack("payments", () => {
      intent({ summary: "payments own network peering" });

      resource(NetworkPeering, "ci-runner", {
        name: "ci-runner",
        peerNetwork: "example",
        peerNetworkType: "example",
        vmwareEngineNetwork: "example",
        description: "Managed by ubx.",
        exportCustomRoutes: true,
        exportCustomRoutesWithPublicIp: true,
        importCustomRoutes: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vmwareengine import NetworkPeering, NetworkPeeringConfig

    def describe():
        ubx.intent("payments own network peering")

        ubx.resource(NetworkPeering, "ci-runner", NetworkPeeringConfig(
            name="ci-runner",
            peer_network="example",
            peer_network_type="example",
            vmware_engine_network="example",
            description="Managed by ubx.",
            export_custom_routes=True,
            export_custom_routes_with_public_ip=True,
            import_custom_routes=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vmwareengine_network_peering called "ci-runner",
    with peer network example, peer network type example, vmware
    engine network example, description Managed by ubx., export
    custom routes true, export custom routes with public ip true,
    import custom routes true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  User-provided description for this network peering. Optional.
</ResponseField>

<ResponseField name="export_custom_routes" type="bool">
  True if custom routes are exported to the peered network; false otherwise. Optional.
</ResponseField>

<ResponseField name="export_custom_routes_with_public_ip" type="bool">
  True if all subnet routes with a public IP address range are exported; false otherwise. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="import_custom_routes" type="bool">
  True if custom routes are imported from the peered network; false otherwise. Optional.
</ResponseField>

<ResponseField name="import_custom_routes_with_public_ip" type="bool">
  True if custom routes are imported from the peered network; false otherwise. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The ID of the Network Peering. Required.
</ResponseField>

<ResponseField name="peer_network" type="string" required>
  The relative resource name of the network to peer with a standard VMware Engine network. The provided network can be a consumer VPC network or another standard VMware Engine network. Required.
</ResponseField>

<ResponseField name="peer_network_type" type="string" required>
  The type of the network to peer with the VMware Engine network. Possible values: ["STANDARD", "VMWARE_ENGINE_NETWORK", "PRIVATE_SERVICES_ACCESS", "NETAPP_CLOUD_VOLUMES", "THIRD_PARTY_SERVICE", "DELL_POWERSCALE", "GOOGLE_CLOUD_NETAPP_VOLUMES"] Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vmware_engine_network" type="string" required>
  The relative resource name of the VMware Engine network. Specify the name in the following form: projects/&#123;project&#125;/locations/&#123;location&#125;/vmwareEngineNetworks/&#123;vmwareEngineNetworkId&#125; where &#123;project&#125; can either be a project number or a project ID. Required.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Creation time of this resource. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  State of the network peering. This field has a value of 'ACTIVE' when there's a matching configuration in the peer network. New values may be added to this enum when appropriate. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state_details" type="string">
  Details about the current state of the network peering. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  System-generated unique identifier for the resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Last updated time of this resource. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="vmware_engine_network_canonical" type="string">
  The canonical name of the VMware Engine network in the form: projects/&#123;project_number&#125;/locations/&#123;location&#125;/vmwareEngineNetworks/&#123;vmwareEngineNetworkId&#125; Computed by GCP after `ship`.
</ResponseField>
