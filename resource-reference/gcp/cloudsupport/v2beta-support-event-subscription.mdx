---
title: "google_cloudsupport_v2beta_support_event_subscription"
description: "`google_cloudsupport_v2beta_support_event_subscription` subscribes a set of email addresses to updates on a Cloud Support case or asset..."
---

<Warning>
  This is Google Cloud's own **v2beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_cloudsupport_support_event_subscription`.
</Warning>

`google_cloudsupport_v2beta_support_event_subscription` subscribes a set of email addresses to updates on a Cloud Support case or asset, automatically CC'ing them on new cases raised under that asset and on updates to cases they're subscribed to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_cloudsupport_v2beta --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_cloudsupport_v2beta/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudsupport "github.com/ubiquex/ubx-sdk-google/sdk/go/google_cloudsupport_v2beta/cloudsupport"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v2beta support event subscription"})

    		ubx.Resource(cloudsupport.V2betaCloudsupportV2betaSupportEventSubscription, "example", cloudsupport.V2betaCloudsupportV2betaSupportEventSubscriptionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_cloudsupport_v2beta --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2betaCloudsupportV2betaSupportEventSubscription } from "./local-sdk/google_cloudsupport_v2beta/sdk/typescript/google_cloudsupport_v2beta/cloudsupport/v2beta_cloudsupport_v2beta_support_event_subscription.ts";

    export default stack("example", () => {
      intent({ summary: "example own v2beta support event subscription" });

      resource(V2betaCloudsupportV2betaSupportEventSubscription, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_cloudsupport_v2beta --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_cloudsupport_v2beta/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_cloudsupport_v2beta.cloudsupport import V2betaCloudsupportV2betaSupportEventSubscription, V2betaCloudsupportV2betaSupportEventSubscriptionConfig

    def describe():
        ubx.intent("example own v2beta support event subscription")

        ubx.resource(V2betaCloudsupportV2betaSupportEventSubscription, "example", V2betaCloudsupportV2betaSupportEventSubscriptionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudsupport_v2beta_support_event_subscription
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The time at which the subscription was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. The time at which the subscription was deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  Output only. Reason why subscription is failing. State of subscription must be FAILING in order for this to have a value. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the support event subscription. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="pub_sub_topic" type="string">
  Required. The name of the Pub/Sub topic to publish notifications to. Format: projects/&#123;project&#125;/topics/&#123;topic&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="purge_time" type="string">
  Output only. The time at which the subscription will be purged. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the subscription. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which the subscription was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
