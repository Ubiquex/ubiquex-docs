---
title: "google_cloudsupport_v2beta_comment"
description: "`google_cloudsupport_v2beta_comment` is a real comment posted to a `google_cloudsupport_case`..."
---

<Warning>
  This is Google Cloud's own **v2beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_cloudsupport_comment`.
</Warning>

`google_cloudsupport_v2beta_comment` is a real comment posted to a `google_cloudsupport_case`, part of the back-and-forth thread between the customer and Google Cloud support engineers working the case.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_cloudsupport_v2beta --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_cloudsupport_v2beta/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudsupport "github.com/ubiquex/ubx-sdk-google/sdk/go/google_cloudsupport_v2beta/cloudsupport"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v2beta comment"})

    		ubx.Resource(cloudsupport.V2betaCloudsupportV2betaComment, "example", cloudsupport.V2betaCloudsupportV2betaCommentConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_cloudsupport_v2beta --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2betaCloudsupportV2betaComment } from "./local-sdk/google_cloudsupport_v2beta/sdk/typescript/google_cloudsupport_v2beta/cloudsupport/v2beta_cloudsupport_v2beta_comment.ts";

    export default stack("example", () => {
      intent({ summary: "example own v2beta comment" });

      resource(V2betaCloudsupportV2betaComment, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_cloudsupport_v2beta --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_cloudsupport_v2beta/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_cloudsupport_v2beta.cloudsupport import V2betaCloudsupportV2betaComment, V2betaCloudsupportV2betaCommentConfig

    def describe():
        ubx.intent("example own v2beta comment")

        ubx.resource(V2betaCloudsupportV2betaComment, "example", V2betaCloudsupportV2betaCommentConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudsupport_v2beta_comment called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="body" type="string">
  The full comment body. Maximum of 12800 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the comment was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="creator" type="object">
  An Actor represents an entity that performed an action. For example, an actor could be a user who posted a comment on a support case, a user who uploaded an attachment, or a service account that created a support case. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      The name to display for the actor. If not provided, it is inferred from credentials supplied during case creation. When an email is provided, a display name must also be provided. This will be obfuscated if the user is a Google Support agent. Optional.
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the actor. If not provided, it is inferred from the credentials supplied during case creation. When a name is provided, an email must also be provided. If the user is a Google Support agent, this is obfuscated. This field is deprecated. Use `username` instead. Optional.
    </ResponseField>
    <ResponseField name="google_support" type="bool">
      Output only. Whether the actor is a Google support actor. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="username" type="string">
      Output only. The username of the actor. It may look like an email or other format provided by the identity provider. If not provided, it is inferred from the credentials supplied. When a name is provided, a username must also be provided. If the user is a Google Support agent, this will not be set. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. The resource name of the comment. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="plain_text_body" type="string">
  Output only. DEPRECATED. DO NOT USE. A duplicate of the `body` field. This field is only present for legacy reasons. Optional; if omitted, computed by Google Cloud.
</ResponseField>
