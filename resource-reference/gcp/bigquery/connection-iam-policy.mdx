---
title: "google_bigquery_connection_iam_policy"
description: "Real, generated bindings for `google_bigquery_connection_iam_policy`."
---

`google_bigquery_connection_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigquery"
    )

    cfg := bigquery.ConnectionIamPolicyConfig{
        ConnectionId: "example",
        PolicyData: "example",
    }
    ubx.Resource(bigquery.ConnectionIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConnectionIamPolicy } from
      "jsr:@ubx/sdk-google/google/bigquery/connection_iam_policy";

    resource(ConnectionIamPolicy, "example", {
      connectionId: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.bigquery import (
        ConnectionIamPolicy,
        ConnectionIamPolicyConfig,
    )

    cfg = ConnectionIamPolicyConfig(
        connection_id="example",
        policy_data="example",
    )
    ubx.resource(ConnectionIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_bigquery_connection_iam_policy with
connection_id, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="connection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
