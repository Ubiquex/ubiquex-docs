---
title: "google_bigquery_job"
description: "`google_bigquery_job` is an asynchronous BigQuery operation -- a query, load, extract, or copy -- run against dataset(s) and table(s)..."
---

`google_bigquery_job` is an asynchronous BigQuery operation -- a query, load, extract, or copy -- run against dataset(s) and table(s), tracked until it completes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigquery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigquery"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own job"})

    		ubx.Resource(bigquery.Job, "example", bigquery.JobConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Job } from "@ubx/sdk-google/google/bigquery/job";

    export default stack("example", () => {
      intent({ summary: "example own job" });

      resource(Job, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigquery.job import Job, JobConfig

    def describe():
        ubx.intent("example own job")

        ubx.resource(Job, "example", JobConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigquery_job, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="configuration" type="object">
  The configuration for this job. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="copy" type="object">
      JobConfigurationTableCopy configures a job that copies data from one table to another. For more information on copying tables, see [Copy a table](https://cloud.google.com/bigquery/docs/managing-tables#copy-table). Optional.
      <Expandable title="properties">
        <ResponseField name="create_disposition" type="string">
          Optional. Specifies whether the job is allowed to create new tables. The following values are supported: * CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table. * CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result. The default value is CREATE_IF_NEEDED. Creation, truncation and append actions occur as one atomic update upon job completion. Optional.
        </ResponseField>
        <ResponseField name="destination_encryption_configuration" type="object">
          Configuration for Cloud KMS encryption settings. Optional.
          <Expandable title="properties">
            <ResponseField name="kms_key_name" type="string">
              Optional. Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table. The BigQuery Service Account associated with your project requires access to this encryption key. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="destination_expiration_time" type="string">
          Optional. The time when the destination table expires. Expired tables will be deleted and their storage reclaimed. Optional.
        </ResponseField>
        <ResponseField name="destination_table" type="object">
          The table this operation writes to. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this table. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this table. Optional.
            </ResponseField>
            <ResponseField name="table_id" type="string">
              Required. The ID of the table. The ID can contain Unicode characters in category L (letter), M (mark), N (number), Pc (connector, including underscore), Pd (dash), and Zs (space). For more information, see [General Category](https://wikipedia.org/wiki/Unicode_character_property#General_Category). The maximum length is 1,024 characters. Certain operations allow suffixing of the table ID with a partition decorator, such as `sample_table$20190123`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="operation_type" type="string">
          Optional. Supported operation types in table copy job. Optional.
        </ResponseField>
        <ResponseField name="source_table" type="object">
          The table this job's own input is read from. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this table. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this table. Optional.
            </ResponseField>
            <ResponseField name="table_id" type="string">
              Required. The ID of the table. The ID can contain Unicode characters in category L (letter), M (mark), N (number), Pc (connector, including underscore), Pd (dash), and Zs (space). For more information, see [General Category](https://wikipedia.org/wiki/Unicode_character_property#General_Category). The maximum length is 1,024 characters. Certain operations allow suffixing of the table ID with a partition decorator, such as `sample_table$20190123`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_tables" type="list(object)">
          [Pick one] Source tables to copy. Optional.
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="project_id" type="string">
              The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="table_id" type="string">
              The BigQuery table ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="write_disposition" type="string">
          Optional. Specifies the action that occurs if the destination table already exists. The following values are supported: * WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the table data and uses the schema and table constraints from the source table. * WRITE_APPEND: If the table already exists, BigQuery appends the data to the table. * WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result. The default value is WRITE_EMPTY. Each action is atomic and only occurs if BigQuery is able to complete the job successfully. Creation, truncation and append actions occur as one atomic update upon job completion. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dry_run" type="bool">
      Optional. If set, don't actually run this job. A valid query will return a mostly empty response with some processing statistics, while an invalid query will return the same error it would if it wasn't a dry run. Behavior of non-query jobs is undefined. Optional.
    </ResponseField>
    <ResponseField name="extract" type="object">
      JobConfigurationExtract configures a job that exports data from a BigQuery table into Google Cloud Storage. Optional.
      <Expandable title="properties">
        <ResponseField name="compression" type="string">
          Optional. The compression type to use for exported files. Possible values include DEFLATE, GZIP, NONE, SNAPPY, and ZSTD. The default value is NONE. Not all compression formats are support for all file formats. DEFLATE is only supported for Avro. ZSTD is only supported for Parquet. Not applicable when extracting models. Optional.
        </ResponseField>
        <ResponseField name="destination_format" type="string">
          Optional. The exported file format. Possible values include CSV, NEWLINE_DELIMITED_JSON, PARQUET, or AVRO for tables and ML_TF_SAVED_MODEL or ML_XGBOOST_BOOSTER for models. The default value for tables is CSV. Tables with nested or repeated fields cannot be exported as CSV. The default value for models is ML_TF_SAVED_MODEL. Optional.
        </ResponseField>
        <ResponseField name="destination_uri" type="string">
          [Pick one] DEPRECATED: Use destinationUris instead, passing only one URI as necessary. The fully-qualified Google Cloud Storage URI where the extracted table should be written. Optional.
        </ResponseField>
        <ResponseField name="destination_uris" type="list(string)">
          [Pick one] A list of fully-qualified Google Cloud Storage URIs where the extracted table should be written. Optional.
        </ResponseField>
        <ResponseField name="field_delimiter" type="string">
          Optional. When extracting data in CSV format, this defines the delimiter to use between fields in the exported data. Default is ','. Not applicable when extracting models. Optional.
        </ResponseField>
        <ResponseField name="model_extract_options" type="object">
          Options related to model extraction. Optional.
          <Expandable title="properties">
            <ResponseField name="trial_id" type="string">
              The 1-based ID of the trial to be exported from a hyperparameter tuning model. If not specified, the trial with id = [Model](https://cloud.google.com/bigquery/docs/reference/rest/v2/models#resource:-model).defaultTrialId is exported. This field is ignored for models not trained with hyperparameter tuning. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="print_header" type="bool">
          Optional. Whether to print out a header row in the results. Default is true. Not applicable when extracting models. Optional.
        </ResponseField>
        <ResponseField name="source_model" type="object">
          Id path of a model. Optional.
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this model. Optional.
            </ResponseField>
            <ResponseField name="model_id" type="string">
              Required. The ID of the model. The ID must contain only letters (a-z, A-Z), numbers (0-9), or underscores (_). The maximum length is 1,024 characters. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this model. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_table" type="object">
          The table this job's own input is read from. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this table. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this table. Optional.
            </ResponseField>
            <ResponseField name="table_id" type="string">
              Required. The ID of the table. The ID can contain Unicode characters in category L (letter), M (mark), N (number), Pc (connector, including underscore), Pd (dash), and Zs (space). For more information, see [General Category](https://wikipedia.org/wiki/Unicode_character_property#General_Category). The maximum length is 1,024 characters. Certain operations allow suffixing of the table ID with a partition decorator, such as `sample_table$20190123`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="use_avro_logical_types" type="bool">
          Whether to use logical types when extracting to AVRO format. Not applicable when extracting models. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="job_timeout_ms" type="string">
      Optional. Job timeout in milliseconds relative to the job creation time. If this time limit is exceeded, BigQuery attempts to stop the job, but might not always succeed in canceling it before the job completes. For example, a job that takes more than 60 seconds to complete has a better chance of being stopped than a job that takes 10 seconds to complete. Optional.
    </ResponseField>
    <ResponseField name="job_type" type="string">
      Output only. The type of the job. Can be QUERY, LOAD, EXTRACT, COPY or UNKNOWN. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      The labels associated with this job. You can use these to organize and group your jobs. Label keys and values can be no longer than 63 characters, can only contain lowercase letters, numeric characters, underscores and dashes. International characters are allowed. Label values are optional. Label keys must start with a letter and each label in the list must have a different key. Optional.
    </ResponseField>
    <ResponseField name="load" type="object">
      JobConfigurationLoad contains the configuration properties for loading data into a destination table. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_jagged_rows" type="bool">
          Optional. Accept rows that are missing trailing optional columns. The missing values are treated as nulls. If false, records with missing trailing columns are treated as bad records, and if there are too many bad records, an invalid error is returned in the job result. The default value is false. Only applicable to CSV, ignored for other formats. Optional.
        </ResponseField>
        <ResponseField name="allow_quoted_newlines" type="bool">
          Indicates if BigQuery should allow quoted data sections that contain newline characters in a CSV file. The default value is false. Optional.
        </ResponseField>
        <ResponseField name="autodetect" type="bool">
          Optional. Indicates if we should automatically infer the options and schema for CSV and JSON sources. Optional.
        </ResponseField>
        <ResponseField name="clustering" type="object">
          Configures table clustering. Optional.
          <Expandable title="properties">
            <ResponseField name="fields" type="list(string)">
              One or more fields on which data should be clustered. Only top-level, non-repeated, simple-type fields are supported. The ordering of the clustering fields should be prioritized from most to least important for filtering purposes. For additional information, see [Introduction to clustered tables](https://cloud.google.com/bigquery/docs/clustered-tables#limitations). Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="column_name_character_map" type="string">
          Optional. Character map supported for column names in CSV/Parquet loads. Defaults to STRICT and can be overridden by Project Config Service. Using this option with unsupporting load formats will result in an error. Optional.
        </ResponseField>
        <ResponseField name="connection_properties" type="list(object)">
          Optional. Connection properties which can modify the load job behavior. Currently, only the 'session_id' connection property is supported, and is used to resolve _SESSION appearing as the dataset id. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of this key/value pair. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="copy_files_only" type="bool">
          Optional. [Experimental] Configures the load job to copy files directly to the destination BigLake managed table, bypassing file content reading and rewriting. Copying files only is supported when all the following are true: * `source_uris` are located in the same Cloud Storage location as the destination table's `storage_uri` location. * `source_format` is `PARQUET`. * `destination_table` is an existing BigLake managed table. The table's schema does not have flexible column names. The table's columns do not have type parameters other than precision and scale. * No options other than the above are specified. Optional.
        </ResponseField>
        <ResponseField name="create_disposition" type="string">
          Optional. Specifies whether the job is allowed to create new tables. The following values are supported: * CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table. * CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result. The default value is CREATE_IF_NEEDED. Creation, truncation and append actions occur as one atomic update upon job completion. Optional.
        </ResponseField>
        <ResponseField name="create_session" type="bool">
          Optional. If this property is true, the job creates a new session using a randomly generated session_id. To continue using a created session with subsequent queries, pass the existing session identifier as a `ConnectionProperty` value. The session identifier is returned as part of the `SessionInfo` message within the query statistics. The new session's location will be set to `Job.JobReference.location` if it is present, otherwise it's set to the default location based on existing routing logic. Optional.
        </ResponseField>
        <ResponseField name="date_format" type="string">
          Optional. Date format used for parsing DATE values. Optional.
        </ResponseField>
        <ResponseField name="datetime_format" type="string">
          Optional. Date format used for parsing DATETIME values. Optional.
        </ResponseField>
        <ResponseField name="decimal_target_types" type="list(string)">
          Defines the list of possible SQL data types to which the source decimal values are converted. This list and the precision and the scale parameters of the decimal field determine the target type. In the order of NUMERIC, BIGNUMERIC, and STRING, a type is picked if it is in the specified list and if it supports the precision and the scale. STRING supports all precision and scale values. If none of the listed types supports the precision and the scale, the type supporting the widest range in the specified list is picked, and if a value exceeds the supported range when reading the data, an error will be thrown. Example: Suppose the value of this field is ["NUMERIC", "BIGNUMERIC"]. If (precision,scale) is: * (38,9) -&gt; NUMERIC; * (39,9) -&gt; BIGNUMERIC (NUMERIC cannot hold 30 integer digits); * (38,10) -&gt; BIGNUMERIC (NUMERIC cannot hold 10 fractional digits); * (76,38) -&gt; BIGNUMERIC; * (77,38) -&gt; BIGNUMERIC (error if value exceeds supported range). This field cannot contain duplicate types. The order of the types in this field is ignored. For example, ["BIGNUMERIC", "NUMERIC"] is the same as ["NUMERIC", "BIGNUMERIC"] and NUMERIC always takes precedence over BIGNUMERIC. Defaults to ["NUMERIC", "STRING"] for ORC and ["NUMERIC"] for the other file formats. Optional.
        </ResponseField>
        <ResponseField name="destination_encryption_configuration" type="object">
          Configuration for Cloud KMS encryption settings. Optional.
          <Expandable title="properties">
            <ResponseField name="kms_key_name" type="string">
              Optional. Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table. The BigQuery Service Account associated with your project requires access to this encryption key. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="destination_table" type="object">
          The table this operation writes to. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this table. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this table. Optional.
            </ResponseField>
            <ResponseField name="table_id" type="string">
              Required. The ID of the table. The ID can contain Unicode characters in category L (letter), M (mark), N (number), Pc (connector, including underscore), Pd (dash), and Zs (space). For more information, see [General Category](https://wikipedia.org/wiki/Unicode_character_property#General_Category). The maximum length is 1,024 characters. Certain operations allow suffixing of the table ID with a partition decorator, such as `sample_table$20190123`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="destination_table_properties" type="object">
          Properties for the destination table. Optional.
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              Optional. The description for the destination table. This will only be used if the destination table is newly created. If the table already exists and a value different than the current description is provided, the job will fail. Optional.
            </ResponseField>
            <ResponseField name="expiration_time" type="string">
              Internal use only. Optional.
            </ResponseField>
            <ResponseField name="friendly_name" type="string">
              Optional. Friendly name for the destination table. If the table already exists, it should be same as the existing friendly name. Optional.
            </ResponseField>
            <ResponseField name="labels" type="map(string)">
              Optional. The labels associated with this table. You can use these to organize and group your tables. This will only be used if the destination table is newly created. If the table already exists and labels are different than the current labels are provided, the job will fail. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="encoding" type="string">
          Optional. The character encoding of the data. The supported values are UTF-8, ISO-8859-1, UTF-16BE, UTF-16LE, UTF-32BE, and UTF-32LE. The default value is UTF-8. BigQuery decodes the data after the raw, binary data has been split using the values of the `quote` and `fieldDelimiter` properties. If you don't specify an encoding, or if you specify a UTF-8 encoding when the CSV file is not UTF-8 encoded, BigQuery attempts to convert the data to UTF-8. Generally, your data loads successfully, but it may not match byte-for-byte what you expect. To avoid this, specify the correct encoding by using the `--encoding` flag. If BigQuery can't convert a character other than the ASCII `0` character, BigQuery converts the character to the standard Unicode replacement character: �. Optional.
        </ResponseField>
        <ResponseField name="field_delimiter" type="string">
          Optional. The separator character for fields in a CSV file. The separator is interpreted as a single byte. For files encoded in ISO-8859-1, any single character can be used as a separator. For files encoded in UTF-8, characters represented in decimal range 1-127 (U+0001-U+007F) can be used without any modification. UTF-8 characters encoded with multiple bytes (i.e. U+0080 and above) will have only the first byte used for separating fields. The remaining bytes will be treated as a part of the field. BigQuery also supports the escape sequence "\t" (U+0009) to specify a tab separator. The default value is comma (",", U+002C). Optional.
        </ResponseField>
        <ResponseField name="file_set_spec_type" type="string">
          Optional. Specifies how source URIs are interpreted for constructing the file set to load. By default, source URIs are expanded against the underlying storage. You can also specify manifest files to control how the file set is constructed. This option is only applicable to object storage systems. Optional.
        </ResponseField>
        <ResponseField name="hive_partitioning_options" type="object">
          Options for configuring hive partitioning detect. Optional.
          <Expandable title="properties">
            <ResponseField name="fields" type="list(string)">
              Output only. For permanent external tables, this field is populated with the hive partition keys in the order they were inferred. The types of the partition keys can be deduced by checking the table schema (which will include the partition keys). Not every API will populate this field in the output. For example, Tables.Get will populate it, but Tables.List will not contain this field. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="mode" type="string">
              Optional. When set, what mode of hive partitioning to use when reading data. The following modes are supported: * AUTO: automatically infer partition key name(s) and type(s). * STRINGS: automatically infer partition key name(s). All types are strings. * CUSTOM: partition key schema is encoded in the source URI prefix. Not all storage formats support hive partitioning. Requesting hive partitioning on an unsupported format will lead to an error. Currently supported formats are: JSON, CSV, ORC, Avro and Parquet. Optional.
            </ResponseField>
            <ResponseField name="require_partition_filter" type="bool">
              Optional. If set to true, queries over this table require a partition filter that can be used for partition elimination to be specified. Note that this field should only be true when creating a permanent external table or querying a temporary external table. Hive-partitioned loads with require_partition_filter explicitly set to true will fail. Optional.
            </ResponseField>
            <ResponseField name="source_uri_prefix" type="string">
              Optional. When hive partition detection is requested, a common prefix for all source uris must be required. The prefix must end immediately before the partition key encoding begins. For example, consider files following this data layout: gs://bucket/path_to_table/dt=2019-06-01/country=USA/id=7/file.avro gs://bucket/path_to_table/dt=2019-05-31/country=CA/id=3/file.avro When hive partitioning is requested with either AUTO or STRINGS detection, the common prefix can be either of gs://bucket/path_to_table or gs://bucket/path_to_table/. CUSTOM detection requires encoding the partitioning schema immediately after the common prefix. For CUSTOM, any of * gs://bucket/path_to_table/&#123;dt:DATE&#125;/&#123;country:STRING&#125;/&#123;id:INTEGER&#125; * gs://bucket/path_to_table/&#123;dt:STRING&#125;/&#123;country:STRING&#125;/&#123;id:INTEGER&#125; * gs://bucket/path_to_table/&#123;dt:DATE&#125;/&#123;country:STRING&#125;/&#123;id:STRING&#125; would all be valid source URI prefixes. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ignore_unknown_values" type="bool">
          Optional. Indicates if BigQuery should allow extra values that are not represented in the table schema. If true, the extra values are ignored. If false, records with extra columns are treated as bad records, and if there are too many bad records, an invalid error is returned in the job result. The default value is false. The sourceFormat property determines what BigQuery treats as an extra value: CSV: Trailing columns JSON: Named values that don't match any column names in the table schema Avro, Parquet, ORC: Fields in the file schema that don't exist in the table schema. Optional.
        </ResponseField>
        <ResponseField name="json_extension" type="string">
          Optional. Load option to be used together with source_format newline-delimited JSON to indicate that a variant of JSON is being loaded. To load newline-delimited GeoJSON, specify GEOJSON (and source_format must be set to NEWLINE_DELIMITED_JSON). Optional.
        </ResponseField>
        <ResponseField name="max_bad_records" type="number">
          Optional. The maximum number of bad records that BigQuery can ignore when running the job. If the number of bad records exceeds this value, an invalid error is returned in the job result. The default value is 0, which requires that all records are valid. This is only supported for CSV and NEWLINE_DELIMITED_JSON file formats. Optional.
        </ResponseField>
        <ResponseField name="null_marker" type="string">
          Optional. Specifies a string that represents a null value in a CSV file. For example, if you specify "\N", BigQuery interprets "\N" as a null value when loading a CSV file. The default value is the empty string. If you set this property to a custom value, BigQuery throws an error if an empty string is present for all data types except for STRING and BYTE. For STRING and BYTE columns, BigQuery interprets the empty string as an empty value. Optional.
        </ResponseField>
        <ResponseField name="null_markers" type="list(string)">
          Optional. A list of strings represented as SQL NULL value in a CSV file. null_marker and null_markers can't be set at the same time. If null_marker is set, null_markers has to be not set. If null_markers is set, null_marker has to be not set. If both null_marker and null_markers are set at the same time, a user error would be thrown. Any strings listed in null_markers, including empty string would be interpreted as SQL NULL. This applies to all column types. Optional.
        </ResponseField>
        <ResponseField name="parquet_options" type="object">
          Parquet Options for load and make external tables. Optional.
          <Expandable title="properties">
            <ResponseField name="enable_list_inference" type="bool">
              Optional. Indicates whether to use schema inference specifically for Parquet LIST logical type. Optional.
            </ResponseField>
            <ResponseField name="enum_as_string" type="bool">
              Optional. Indicates whether to infer Parquet ENUM logical type as STRING instead of BYTES by default. Optional.
            </ResponseField>
            <ResponseField name="map_target_type" type="string">
              Optional. Indicates how to represent a Parquet map if present. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="preserve_ascii_control_characters" type="bool">
          Optional. When sourceFormat is set to "CSV", this indicates whether the embedded ASCII control characters (the first 32 characters in the ASCII-table, from '\x00' to '\x1F') are preserved. Optional.
        </ResponseField>
        <ResponseField name="projection_fields" type="list(string)">
          If sourceFormat is set to "DATASTORE_BACKUP", indicates which entity properties to load into BigQuery from a Cloud Datastore backup. Property names are case sensitive and must be top-level properties. If no properties are specified, BigQuery loads all properties. If any named property isn't found in the Cloud Datastore backup, an invalid error is returned in the job result. Optional.
        </ResponseField>
        <ResponseField name="quote" type="string">
          Optional. The value that is used to quote data sections in a CSV file. BigQuery converts the string to ISO-8859-1 encoding, and then uses the first byte of the encoded string to split the data in its raw, binary state. The default value is a double-quote ('"'). If your data does not contain quoted sections, set the property value to an empty string. If your data contains quoted newline characters, you must also set the allowQuotedNewlines property to true. To include the specific quote character within a quoted value, precede it with an additional matching quote character. For example, if you want to escape the default character ' " ', use ' "" '. @default " Optional.
        </ResponseField>
        <ResponseField name="range_partitioning" type="object">
          Configures this table to be partitioned by integer ranges of a specific column. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="field" type="string">
              Required. The name of the column to partition the table on. It must be a top-level, INT64 column whose mode is NULLABLE or REQUIRED. Optional.
            </ResponseField>
            <ResponseField name="range" type="object">
              [Experimental] Defines the ranges for range partitioning. Optional.
              <Expandable title="properties">
                <ResponseField name="end" type="string">
                  [Experimental] The end of range partitioning, exclusive. Optional.
                </ResponseField>
                <ResponseField name="interval" type="string">
                  [Experimental] The width of each interval. Optional.
                </ResponseField>
                <ResponseField name="start" type="string">
                  [Experimental] The start of range partitioning, inclusive. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="reference_file_schema_uri" type="string">
          Optional. The user can provide a reference file with the reader schema. This file is only loaded if it is part of source URIs, but is not loaded otherwise. It is enabled for the following formats: AVRO, PARQUET, ORC. Optional.
        </ResponseField>
        <ResponseField name="schema" type="object">
          Schema of a table Optional.
          <Expandable title="properties">
            <ResponseField name="fields" type="list(object)">
              Describes the fields in a table. Optional.
              <Expandable title="properties">
                <ResponseField name="categories" type="object">
                  The set of category labels this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="names" type="list(string)">
                      The literal name(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="collation" type="string">
                  The collation (locale-aware string comparison rule) applied to a `STRING`-typed field, e.g. `und:ci` for case-insensitive comparison, or empty for the default case-sensitive comparison. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="data_governance_tags_info" type="object">
                  Detail about the data governance tag(s) associated with this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="data_governance_tags" type="map(string)">
                      The data governance tag(s) applied to this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="data_policies" type="list(object)">
                  Column-level data governance policies (e.g. dynamic data masking) applied to this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="data_policy_list" type="object">
                  The data policies (e.g. dynamic data masking rules) applied to this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="data_policies" type="list(object)">
                      Column-level data governance policies (e.g. dynamic data masking) applied to this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="default_value_expression" type="string">
                  A SQL expression computing this column's own default value when none is supplied on insert. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="description" type="string">
                  A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fields" type="list(any)">
                  The nested field schemas that make up a `RECORD`-typed field's own sub-structure. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="foreign_type_definition" type="string">
                  The native type definition of this column in the external system it's federated from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="generated_column" type="object">
                  A column whose own value is computed from other columns, rather than stored directly. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="generated_expression_info" type="object">
                      Detail about a generated column's own computation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="asynchronous" type="bool">
                          Whether a generated column's own value is computed asynchronously after write, rather than synchronously. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="generation_expression" type="string">
                          The SQL expression computing this generated column's own value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="stored" type="bool">
                          Whether a generated column's own computed value is physically stored, rather than recomputed on each read. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="generated_mode" type="string">
                      Whether this column's own value is stored, virtual (computed on read), or not generated at all. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="max_length" type="string">
                  The most characters a `string`-typed value may have. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="mode" type="string">
                  Whether this field is `NULLABLE` (may be absent), `REQUIRED` (must always be present), or `REPEATED` (an array of this type). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="policy_tags" type="object">
                  The Data Catalog policy tag(s) governing access to this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="names" type="list(string)">
                      The literal name(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="precision" type="string">
                  For a `NUMERIC`/`BIGNUMERIC` field, the total number of digits it can hold, both before and after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_element_type" type="object">
                  The subtype a `RANGE`-typed field holds -- required when, and only meaningful when, `type` is `RANGE`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="type" type="string">
                      The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="rounding_mode" type="string">
                  How values are rounded when they exceed this `NUMERIC`/`BIGNUMERIC` field's own scale. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="scale" type="string">
                  For a `NUMERIC`/`BIGNUMERIC` field, how many digits are kept after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="timestamp_precision" type="string">
                  The sub-second precision a `TIMESTAMP`-typed field is stored at: `6` (microsecond, the default) or `12` (picosecond). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="foreign_type_info" type="object">
              Metadata about the foreign data type definition such as the system in which the type is defined. Optional.
              <Expandable title="properties">
                <ResponseField name="type_system" type="string">
                  Required. Specifies the system which defines the foreign data type. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="schema_inline" type="string">
          [Deprecated] The inline schema. For CSV schemas, specify as "Field1:Type1[,Field2:Type2]*". For example, "foo:STRING, bar:INTEGER, baz:FLOAT". Optional.
        </ResponseField>
        <ResponseField name="schema_inline_format" type="string">
          [Deprecated] The format of the schemaInline property. Optional.
        </ResponseField>
        <ResponseField name="schema_update_options" type="list(string)">
          Allows the schema of the destination table to be updated as a side effect of the load job if a schema is autodetected or supplied in the job configuration. Schema update options are supported in three cases: when writeDisposition is WRITE_APPEND; when writeDisposition is WRITE_TRUNCATE_DATA; when writeDisposition is WRITE_TRUNCATE and the destination table is a partition of a table, specified by partition decorators. For normal tables, WRITE_TRUNCATE will always overwrite the schema. One or more of the following values are specified: * ALLOW_FIELD_ADDITION: allow adding a nullable field to the schema. * ALLOW_FIELD_RELAXATION: allow relaxing a required field in the original schema to nullable. Optional.
        </ResponseField>
        <ResponseField name="skip_leading_rows" type="number">
          Optional. The number of rows at the top of a CSV file that BigQuery will skip when loading the data. The default value is 0. This property is useful if you have header rows in the file that should be skipped. When autodetect is on, the behavior is the following: * skipLeadingRows unspecified - Autodetect tries to detect headers in the first row. If they are not detected, the row is read as data. Otherwise data is read starting from the second row. * skipLeadingRows is 0 - Instructs autodetect that there are no headers and data should be read starting from the first row. * skipLeadingRows = N &gt; 0 - Autodetect skips N-1 rows and tries to detect headers in row N. If headers are not detected, row N is just skipped. Otherwise row N is used to extract column names for the detected schema. Optional.
        </ResponseField>
        <ResponseField name="source_column_match" type="string">
          Optional. Controls the strategy used to match loaded columns to the schema. If not set, a sensible default is chosen based on how the schema is provided. If autodetect is used, then columns are matched by name. Otherwise, columns are matched by position. This is done to keep the behavior backward-compatible. Optional.
        </ResponseField>
        <ResponseField name="source_format" type="string">
          Optional. The format of the data files. For CSV files, specify "CSV". For datastore backups, specify "DATASTORE_BACKUP". For newline-delimited JSON, specify "NEWLINE_DELIMITED_JSON". For Avro, specify "AVRO". For parquet, specify "PARQUET". For orc, specify "ORC". The default value is CSV. Optional.
        </ResponseField>
        <ResponseField name="source_uris" type="list(string)">
          [Required] The fully-qualified URIs that point to your data in Google Cloud. For Google Cloud Storage URIs: Each URI can contain one '*' wildcard character and it must come after the 'bucket' name. Size limits related to load jobs apply to external data sources. For Google Cloud Bigtable URIs: Exactly one URI can be specified and it has be a fully specified and valid HTTPS URL for a Google Cloud Bigtable table. For Google Cloud Datastore backups: Exactly one URI can be specified. Also, the '*' wildcard character is not allowed. Optional.
        </ResponseField>
        <ResponseField name="time_format" type="string">
          Optional. Date format used for parsing TIME values. Optional.
        </ResponseField>
        <ResponseField name="time_partitioning" type="object">
          Configures this table to be partitioned by time, e.g. daily by a timestamp column. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="expiration_ms" type="string">
              Optional. Number of milliseconds for which to keep the storage for a partition. A wrapper is used here because 0 is an invalid value. Optional.
            </ResponseField>
            <ResponseField name="field" type="string">
              Optional. If not set, the table is partitioned by pseudo column '_PARTITIONTIME'; if set, the table is partitioned by this field. The field must be a top-level TIMESTAMP or DATE field. Its mode must be NULLABLE or REQUIRED. A wrapper is used here because an empty string is an invalid value. Optional.
            </ResponseField>
            <ResponseField name="require_partition_filter" type="bool">
              If set to true, queries over this table require a partition filter that can be used for partition elimination to be specified. This field is deprecated; please set the field with the same name on the table itself instead. This field needs a wrapper because we want to output the default value, false, if the user explicitly set it. Optional.
            </ResponseField>
            <ResponseField name="type" type="string">
              Required. The supported types are DAY, HOUR, MONTH, and YEAR, which will generate one partition per day, hour, month, and year, respectively. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="time_zone" type="string">
          Optional. Default time zone that will apply when parsing timestamp values that have no specific time zone. Optional.
        </ResponseField>
        <ResponseField name="timestamp_format" type="string">
          Optional. Date format used for parsing TIMESTAMP values. Optional.
        </ResponseField>
        <ResponseField name="timestamp_target_precision" type="list(number)">
          Precisions (maximum number of total digits in base 10) for seconds of TIMESTAMP types that are allowed to the destination table for autodetection mode. Available for the formats: CSV, PARQUET, AVRO, and Iceberg External Table. Possible values include: Not Specified, [], or [6]: timestamp(6) for all auto detected TIMESTAMP columns [6, 12]: timestamp(6) for all auto detected TIMESTAMP columns that have less than 6 digits of subseconds. timestamp(12) for all auto detected TIMESTAMP columns that have more than 6 digits of subseconds. [12]: timestamp(12) for all auto detected TIMESTAMP columns. The order of the elements in this array is ignored. Inputs that have higher precision than the highest target precision in this array will be truncated. Optional.
        </ResponseField>
        <ResponseField name="use_avro_logical_types" type="bool">
          Optional. If sourceFormat is set to "AVRO", indicates whether to interpret logical types as the corresponding BigQuery data type (for example, TIMESTAMP), instead of using the raw type (for example, INTEGER). Optional.
        </ResponseField>
        <ResponseField name="write_disposition" type="string">
          Optional. Specifies the action that occurs if the destination table already exists. The following values are supported: * WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the data, removes the constraints and uses the schema from the load job. * WRITE_TRUNCATE_DATA: If the table already exists, BigQuery overwrites the data, but keeps the constraints and schema of the existing table. * WRITE_APPEND: If the table already exists, BigQuery appends the data to the table. * WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result. The default value is WRITE_APPEND. Each action is atomic and only occurs if BigQuery is able to complete the job successfully. Creation, truncation and append actions occur as one atomic update upon job completion. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_slots" type="number">
      Optional. A target limit on the rate of slot consumption by this job. If set to a value &gt; 0, BigQuery will attempt to limit the rate of slot consumption by this job to keep it below the configured limit, even if the job is eligible for more slots based on fair scheduling. The unused slots will be available for other jobs and queries to use. Note: This feature is not yet generally available. Optional.
    </ResponseField>
    <ResponseField name="query" type="object">
      JobConfigurationQuery configures a BigQuery query job. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_large_results" type="bool">
          Optional. If true and query uses legacy SQL dialect, allows the query to produce arbitrarily large result tables at a slight cost in performance. Requires destinationTable to be set. For GoogleSQL queries, this flag is ignored and large results are always allowed. However, you must still set destinationTable when result size exceeds the allowed maximum response size. Optional.
        </ResponseField>
        <ResponseField name="clustering" type="object">
          Configures table clustering. Optional.
          <Expandable title="properties">
            <ResponseField name="fields" type="list(string)">
              One or more fields on which data should be clustered. Only top-level, non-repeated, simple-type fields are supported. The ordering of the clustering fields should be prioritized from most to least important for filtering purposes. For additional information, see [Introduction to clustered tables](https://cloud.google.com/bigquery/docs/clustered-tables#limitations). Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="connection_properties" type="list(object)">
          Connection properties which can modify the query behavior. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of this key/value pair. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="continuous" type="bool">
          [Optional] Specifies whether the query should be executed as a continuous query. The default value is false. Optional.
        </ResponseField>
        <ResponseField name="create_disposition" type="string">
          Optional. Specifies whether the job is allowed to create new tables. The following values are supported: * CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table. * CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result. The default value is CREATE_IF_NEEDED. Creation, truncation and append actions occur as one atomic update upon job completion. Optional.
        </ResponseField>
        <ResponseField name="create_session" type="bool">
          If this property is true, the job creates a new session using a randomly generated session_id. To continue using a created session with subsequent queries, pass the existing session identifier as a `ConnectionProperty` value. The session identifier is returned as part of the `SessionInfo` message within the query statistics. The new session's location will be set to `Job.JobReference.location` if it is present, otherwise it's set to the default location based on existing routing logic. Optional.
        </ResponseField>
        <ResponseField name="default_dataset" type="object">
          Identifier for a dataset. Optional.
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. A unique ID for this dataset, without the project name. The ID must contain only letters (a-z, A-Z), numbers (0-9), or underscores (_). The maximum length is 1,024 characters. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Optional. The ID of the project containing this dataset. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="destination_encryption_configuration" type="object">
          Configuration for Cloud KMS encryption settings. Optional.
          <Expandable title="properties">
            <ResponseField name="kms_key_name" type="string">
              Optional. Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table. The BigQuery Service Account associated with your project requires access to this encryption key. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="destination_table" type="object">
          The table this operation writes to. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this table. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this table. Optional.
            </ResponseField>
            <ResponseField name="table_id" type="string">
              Required. The ID of the table. The ID can contain Unicode characters in category L (letter), M (mark), N (number), Pc (connector, including underscore), Pd (dash), and Zs (space). For more information, see [General Category](https://wikipedia.org/wiki/Unicode_character_property#General_Category). The maximum length is 1,024 characters. Certain operations allow suffixing of the table ID with a partition decorator, such as `sample_table$20190123`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="flatten_results" type="bool">
          Optional. If true and query uses legacy SQL dialect, flattens all nested and repeated fields in the query results. allowLargeResults must be true if this is set to false. For GoogleSQL queries, this flag is ignored and results are never flattened. Optional.
        </ResponseField>
        <ResponseField name="maximum_billing_tier" type="number">
          Optional. [Deprecated] Maximum billing tier allowed for this query. The billing tier controls the amount of compute resources allotted to the query, and multiplies the on-demand cost of the query accordingly. A query that runs within its allotted resources will succeed and indicate its billing tier in statistics.query.billingTier, but if the query exceeds its allotted resources, it will fail with billingTierLimitExceeded. WARNING: The billed byte amount can be multiplied by an amount up to this number! Most users should not need to alter this setting, and we recommend that you avoid introducing new uses of it. Optional.
        </ResponseField>
        <ResponseField name="maximum_bytes_billed" type="string">
          Limits the bytes billed for this job. Queries that will have bytes billed beyond this limit will fail (without incurring a charge). If unspecified, this will be set to your project default. Optional.
        </ResponseField>
        <ResponseField name="parameter_mode" type="string">
          GoogleSQL only. Set to POSITIONAL to use positional (?) query parameters or to NAMED to use named (@myparam) query parameters in this query. Optional.
        </ResponseField>
        <ResponseField name="preserve_nulls" type="bool">
          [Deprecated] This property is deprecated. Optional.
        </ResponseField>
        <ResponseField name="priority" type="string">
          Optional. Specifies a priority for the query. Possible values include INTERACTIVE and BATCH. The default value is INTERACTIVE. Optional.
        </ResponseField>
        <ResponseField name="query" type="string">
          [Required] SQL query text to execute. The useLegacySql field can be used to indicate whether the query uses legacy SQL or GoogleSQL. Optional.
        </ResponseField>
        <ResponseField name="query_parameters" type="list(object)">
          Query parameters for GoogleSQL queries. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="parameter_type" type="object">
              The data type of this routine parameter. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="array_type" type="any">
                  The schema of the element type, when this field is an `ARRAY`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_element_type" type="any">
                  The subtype a `RANGE`-typed field holds -- required when, and only meaningful when, `type` is `RANGE`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="struct_types" type="list(object)">
                  The field(s) making up a `STRUCT`-typed schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="description" type="string">
                      A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="type" type="any">
                      The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="timestamp_precision" type="string">
                  The sub-second precision a `TIMESTAMP`-typed field is stored at: `6` (microsecond, the default) or `12` (picosecond). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="parameter_value" type="object">
              The value passed for this parameter. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="array_values" type="list(any)">
                  The individual element value(s) making up an array value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_value" type="object">
                  A value expressed as a `RANGE` type. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="end" type="any">
                      Where this range ends. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="start" type="any">
                      Where this range starts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="struct_values" type="map(any)">
                  The field value(s) making up a `STRUCT`-typed value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="range_partitioning" type="object">
          Configures this table to be partitioned by integer ranges of a specific column. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="field" type="string">
              Required. The name of the column to partition the table on. It must be a top-level, INT64 column whose mode is NULLABLE or REQUIRED. Optional.
            </ResponseField>
            <ResponseField name="range" type="object">
              [Experimental] Defines the ranges for range partitioning. Optional.
              <Expandable title="properties">
                <ResponseField name="end" type="string">
                  [Experimental] The end of range partitioning, exclusive. Optional.
                </ResponseField>
                <ResponseField name="interval" type="string">
                  [Experimental] The width of each interval. Optional.
                </ResponseField>
                <ResponseField name="start" type="string">
                  [Experimental] The start of range partitioning, inclusive. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="schema_update_options" type="list(string)">
          Allows the schema of the destination table to be updated as a side effect of the query job. Schema update options are supported in three cases: when writeDisposition is WRITE_APPEND; when writeDisposition is WRITE_TRUNCATE_DATA; when writeDisposition is WRITE_TRUNCATE and the destination table is a partition of a table, specified by partition decorators. For normal tables, WRITE_TRUNCATE will always overwrite the schema. One or more of the following values are specified: * ALLOW_FIELD_ADDITION: allow adding a nullable field to the schema. * ALLOW_FIELD_RELAXATION: allow relaxing a required field in the original schema to nullable. Optional.
        </ResponseField>
        <ResponseField name="script_options" type="object">
          Options related to script execution. Optional.
          <Expandable title="properties">
            <ResponseField name="key_result_statement" type="string">
              Determines which statement in the script represents the "key result", used to populate the schema and query results of the script job. Default is LAST. Optional.
            </ResponseField>
            <ResponseField name="statement_byte_budget" type="string">
              Limit on the number of bytes billed per statement. Exceeding this budget results in an error. Optional.
            </ResponseField>
            <ResponseField name="statement_timeout_ms" type="string">
              Timeout period for each statement in a script. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="system_variables" type="object">
          System variables given to a query. Optional.
          <Expandable title="properties">
            <ResponseField name="types" type="map(object)">
              Output only. Data type for each system variable. Computed by Google Cloud after `ship`.
              <Expandable title="properties">
                <ResponseField name="array_element_type" type="any">
                  The type of each element in an `ARRAY`-typed value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_element_type" type="any">
                  The subtype a `RANGE`-typed field holds -- required when, and only meaningful when, `type` is `RANGE`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="struct_type" type="object">
                  The field(s) making up a `STRUCT`-typed value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fields" type="list(object)">
                      The nested field schemas that make up a `RECORD`-typed field's own sub-structure. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="any">
                          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="type_kind" type="string">
                  The base data type this schema node represents, e.g. `STRING` or `STRUCT`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="values" type="map(any)">
              Output only. Value for each system variable. Computed by Google Cloud after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="table_definitions" type="map(object)">
          Optional. You can specify external table definitions, which operate as ephemeral tables that can be queried. These definitions are configured using a JSON map, where the string key represents the table identifier, and the value is the corresponding external data configuration object. Optional.
          <Expandable title="properties">
            <ResponseField name="autodetect" type="bool">
              Whether the table's own schema is automatically inferred from the source data, rather than explicitly specified. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="avro_options" type="object">
              Configuration specific to reading data in Avro format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="use_avro_logical_types" type="bool">
                  Whether Avro logical types (e.g. decimal, timestamp) are converted to their corresponding BigQuery types, rather than their raw underlying type. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="bigtable_options" type="object">
              Configuration specific to reading data from a federated Bigtable source. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="column_families" type="list(object)">
                  The Bigtable column family/families this configuration applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="columns" type="list(object)">
                      The column(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="encoding" type="string">
                          The text encoding this field's own byte values should be interpreted with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="field_name" type="string">
                          The name of the field this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="only_read_latest" type="bool">
                          Whether only the most recent version of each Bigtable cell is read, ignoring older versions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="proto_config" type="object">
                          Configuration for reading data encoded as protocol buffer messages. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="proto_message_name" type="string">
                              The fully qualified name of the protocol buffer message type this data is encoded as. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="schema_bundle_id" type="string">
                              A reference to the schema bundle (e.g. a set of `.proto` definitions) this configuration uses. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="qualifier_encoded" type="string">
                          The Bigtable column qualifier, in its own raw encoded (byte) form. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="qualifier_string" type="string">
                          The Bigtable column qualifier, decoded as a string. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="encoding" type="string">
                      The text encoding this field's own byte values should be interpreted with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="family_id" type="string">
                      The Bigtable column family this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="only_read_latest" type="bool">
                      Whether only the most recent version of each Bigtable cell is read, ignoring older versions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="proto_config" type="object">
                      Configuration for reading data encoded as protocol buffer messages. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="proto_message_name" type="string">
                          The fully qualified name of the protocol buffer message type this data is encoded as. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="schema_bundle_id" type="string">
                          A reference to the schema bundle (e.g. a set of `.proto` definitions) this configuration uses. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ignore_unspecified_column_families" type="bool">
                  Whether Bigtable column families not explicitly configured are silently ignored, rather than causing an error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="output_column_families_as_json" type="bool">
                  Whether Bigtable column family data is returned as a single JSON-encoded column, rather than one column per qualifier. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="read_rowkey_as_string" type="bool">
                  Whether the Bigtable row key is read as a plain string, rather than raw bytes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="compression" type="string">
              The compression codec applied to this data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="connection_id" type="string">
              A reference to the connection resource this uses to reach an external data source. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="csv_options" type="object">
              Configuration specific to reading data in CSV format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allow_jagged_rows" type="bool">
                  Whether CSV rows with fewer columns than the schema are accepted, with missing trailing fields treated as null. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_quoted_newlines" type="bool">
                  Whether newline characters are allowed inside quoted CSV field values. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="encoding" type="string">
                  The text encoding this field's own byte values should be interpreted with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="field_delimiter" type="string">
                  The character separating fields in a delimited source file, e.g. `,`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="null_marker" type="string">
                  The string that represents a null value in the source data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="null_markers" type="list(string)">
                  The string(s) that represent a null value in the source data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="preserve_ascii_control_characters" type="bool">
                  Whether ASCII control characters in CSV data are preserved as-is, rather than stripped. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="quote" type="string">
                  The character used to quote field values in a delimited source file. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="skip_leading_rows" type="string">
                  How many leading rows of the source file are skipped, e.g. a CSV header row. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="source_column_match" type="string">
                  Whether source file columns are matched to table columns by name or by position. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="date_format" type="string">
              The format date values are parsed/rendered with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="datetime_format" type="string">
              The format datetime values are parsed/rendered with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="decimal_target_types" type="list(string)">
              Which numeric type(s) `NUMERIC`/`BIGNUMERIC` source values are converted to when loaded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="file_set_spec_type" type="string">
              How the source file set is specified, e.g. as a list of URIs or a manifest file. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="google_sheets_options" type="object">
              Configuration specific to reading data from a linked Google Sheet. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="range" type="string">
                  The range this value spans. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="skip_leading_rows" type="string">
                  How many leading rows of the source file are skipped, e.g. a CSV header row. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="hive_partitioning_options" type="object">
              Configuration for reading data laid out using Hive-style directory partitioning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="fields" type="list(string)">
                  The nested field schemas that make up a `RECORD`-typed field's own sub-structure. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="mode" type="string">
                  Whether this field is `NULLABLE` (may be absent), `REQUIRED` (must always be present), or `REPEATED` (an array of this type). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="require_partition_filter" type="bool">
                  Whether queries against this table must include a filter on the partitioning column, to prevent accidental full scans. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="source_uri_prefix" type="string">
                  The common URI prefix identifying the source file(s) for a Hive-partitioned external table. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="ignore_unknown_values" type="bool">
              Whether source data values not matching any column in the table's own schema are silently dropped, rather than causing an error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="json_extension" type="string">
              How JSON source data is interpreted, e.g. as newline-delimited records or a GeoJSON feature collection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="json_options" type="object">
              Configuration specific to reading data in JSON format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="encoding" type="string">
                  The text encoding this field's own byte values should be interpreted with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_bad_records" type="number">
              The maximum number of bad records tolerated before the load job fails. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metadata_cache_mode" type="string">
              Whether and how BigLake table metadata is cached, e.g. automatically refreshed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="object_metadata" type="string">
              Metadata describing an object referenced by this table, for object tables. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="parquet_options" type="object">
              Configuration specific to reading data in Parquet format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="enable_list_inference" type="bool">
                  Whether Parquet/ORC list-of-struct fields are inferred as repeated fields. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="enum_as_string" type="bool">
                  Whether Avro/Parquet enum values are read as plain strings, rather than a dedicated enum type. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="map_target_type" type="string">
                  How Parquet map-typed fields are converted, e.g. to an array of key/value structs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="reference_file_schema_uri" type="string">
              The URI of a reference file whose own embedded schema is used for the whole external table. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="schema" type="object">
              The schema describing this table's own field structure. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="fields" type="list(object)">
                  The nested field schemas that make up a `RECORD`-typed field's own sub-structure. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="categories" type="object">
                      The set of category labels this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="names" type="list(string)">
                          The literal name(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="collation" type="string">
                      The collation (locale-aware string comparison rule) applied to a `STRING`-typed field, e.g. `und:ci` for case-insensitive comparison, or empty for the default case-sensitive comparison. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="data_governance_tags_info" type="object">
                      Detail about the data governance tag(s) associated with this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="data_governance_tags" type="map(string)">
                          The data governance tag(s) applied to this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="data_policies" type="list(object)">
                      Column-level data governance policies (e.g. dynamic data masking) applied to this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="data_policy_list" type="object">
                      The data policies (e.g. dynamic data masking rules) applied to this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="data_policies" type="list(object)">
                          Column-level data governance policies (e.g. dynamic data masking) applied to this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="default_value_expression" type="string">
                      A SQL expression computing this column's own default value when none is supplied on insert. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="description" type="string">
                      A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="fields" type="list(any)">
                      The nested field schemas that make up a `RECORD`-typed field's own sub-structure. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="foreign_type_definition" type="string">
                      The native type definition of this column in the external system it's federated from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="generated_column" type="object">
                      A column whose own value is computed from other columns, rather than stored directly. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="generated_expression_info" type="object">
                          Detail about a generated column's own computation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="asynchronous" type="bool">
                              Whether a generated column's own value is computed asynchronously after write, rather than synchronously. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="generation_expression" type="string">
                              The SQL expression computing this generated column's own value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="stored" type="bool">
                              Whether a generated column's own computed value is physically stored, rather than recomputed on each read. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="generated_mode" type="string">
                          Whether this column's own value is stored, virtual (computed on read), or not generated at all. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="max_length" type="string">
                      The most characters a `string`-typed value may have. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mode" type="string">
                      Whether this field is `NULLABLE` (may be absent), `REQUIRED` (must always be present), or `REPEATED` (an array of this type). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="policy_tags" type="object">
                      The Data Catalog policy tag(s) governing access to this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="names" type="list(string)">
                          The literal name(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="precision" type="string">
                      For a `NUMERIC`/`BIGNUMERIC` field, the total number of digits it can hold, both before and after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="range_element_type" type="object">
                      The subtype a `RANGE`-typed field holds -- required when, and only meaningful when, `type` is `RANGE`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="type" type="string">
                          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="rounding_mode" type="string">
                      How values are rounded when they exceed this `NUMERIC`/`BIGNUMERIC` field's own scale. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="scale" type="string">
                      For a `NUMERIC`/`BIGNUMERIC` field, how many digits are kept after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timestamp_precision" type="string">
                      The sub-second precision a `TIMESTAMP`-typed field is stored at: `6` (microsecond, the default) or `12` (picosecond). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="foreign_type_info" type="object">
                  Detail about how this schema's own types map to an external system's native types. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="type_system" type="string">
                      Which type system this schema's own type names are drawn from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="source_format" type="string">
              The file format of the source data, e.g. `CSV`, `AVRO`, or `PARQUET`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_uris" type="list(string)">
              The source file URI(s) this data is loaded/read from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time_format" type="string">
              The format time values are parsed/rendered with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time_zone" type="string">
              The time zone this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="timestamp_format" type="string">
              The format timestamp values are parsed/rendered with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="timestamp_target_precision" type="list(number)">
              The precision timestamp values are converted to when loaded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="time_partitioning" type="object">
          Configures this table to be partitioned by time, e.g. daily by a timestamp column. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="expiration_ms" type="string">
              Optional. Number of milliseconds for which to keep the storage for a partition. A wrapper is used here because 0 is an invalid value. Optional.
            </ResponseField>
            <ResponseField name="field" type="string">
              Optional. If not set, the table is partitioned by pseudo column '_PARTITIONTIME'; if set, the table is partitioned by this field. The field must be a top-level TIMESTAMP or DATE field. Its mode must be NULLABLE or REQUIRED. A wrapper is used here because an empty string is an invalid value. Optional.
            </ResponseField>
            <ResponseField name="require_partition_filter" type="bool">
              If set to true, queries over this table require a partition filter that can be used for partition elimination to be specified. This field is deprecated; please set the field with the same name on the table itself instead. This field needs a wrapper because we want to output the default value, false, if the user explicitly set it. Optional.
            </ResponseField>
            <ResponseField name="type" type="string">
              Required. The supported types are DAY, HOUR, MONTH, and YEAR, which will generate one partition per day, hour, month, and year, respectively. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="use_legacy_sql" type="bool">
          Optional. Specifies whether to use BigQuery's legacy SQL dialect for this query. The default value is true. If set to false, the query uses BigQuery's [GoogleSQL](https://docs.cloud.google.com/bigquery/docs/introduction-sql). When useLegacySql is set to false, the value of flattenResults is ignored; query will be run as if flattenResults is false. Optional.
        </ResponseField>
        <ResponseField name="use_query_cache" type="bool">
          Optional. Whether to look for the result in the query cache. The query cache is a best-effort cache that will be flushed whenever tables in the query are modified. Moreover, the query cache is only available when a query does not have a destination table specified. The default value is true. Optional.
        </ResponseField>
        <ResponseField name="user_defined_function_resources" type="list(object)">
          Describes user-defined function resources used in the query. Optional.
          <Expandable title="properties">
            <ResponseField name="inline_code" type="string">
              Routine logic supplied directly as inline source code, rather than referencing an external definition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_uri" type="string">
              A URI identifying this external resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="write_disposition" type="string">
          Optional. Specifies the action that occurs if the destination table already exists. The following values are supported: * WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the data, removes the constraints, and uses the schema from the query result. * WRITE_TRUNCATE_DATA: If the table already exists, BigQuery overwrites the data, but keeps the constraints and schema of the existing table. * WRITE_APPEND: If the table already exists, BigQuery appends the data to the table. * WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result. The default value is WRITE_EMPTY. Each action is atomic and only occurs if BigQuery is able to complete the job successfully. Creation, truncation and append actions occur as one atomic update upon job completion. Optional.
        </ResponseField>
        <ResponseField name="write_incremental_results" type="bool">
          Optional. This is only supported for a SELECT query using a temporary table. If set, the query is allowed to write results incrementally to the temporary result table. This may incur a performance penalty. This option cannot be used with Legacy SQL. This feature is not yet available. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reservation" type="string">
      Optional. The reservation that job would use. User can specify a reservation to execute the job. If reservation is not set, reservation is determined based on the rules defined by the reservation assignments. The expected format is `projects/&#123;project&#125;/locations/&#123;location&#125;/reservations/&#123;reservation&#125;`. Forces the query to use on-demand billing when set to `none`, which requires the project or organization to have `reservation_override_mode` set to `ALLOW_ANY_OVERRIDE`. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. A hash of this resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. Opaque ID field of the job. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="job_creation_reason" type="object">
  Reason about why a Job was created from a [`jobs.query`](https://cloud.google.com/bigquery/docs/reference/rest/v2/jobs/query) method when used with `JOB_CREATION_OPTIONAL` Job creation mode. For [`jobs.insert`](https://cloud.google.com/bigquery/docs/reference/rest/v2/jobs/insert) method calls it will always be `REQUESTED`. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
      Output only. Specifies the high level reason why a Job was created. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="job_reference" type="object">
  A job reference is a fully qualified identifier for referring to a job. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="job_id" type="string">
      Required. The ID of the job. The ID must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). The maximum length is 1,024 characters. Optional.
    </ResponseField>
    <ResponseField name="location" type="string">
      Optional. The geographic location of the job. The default value is US. For more information about BigQuery locations, see: https://cloud.google.com/bigquery/docs/locations Optional.
    </ResponseField>
    <ResponseField name="project_id" type="string">
      Required. The ID of the project containing this job. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. The type of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="principal_subject" type="string">
  Output only. [Full-projection-only] String representation of identity of requesting party. Populated for both first- and third-party identities. Only present for APIs that support third-party identities. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. A URL that can be used to access the resource again. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="statistics" type="object">
  Statistics for a single job execution. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="completion_ratio" type="number">
      Output only. [TrustedTester] Job progress (0.0 -&gt; 1.0) for LOAD and EXTRACT jobs. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="copy" type="object">
      Statistics for a copy job. Optional.
      <Expandable title="properties">
        <ResponseField name="copied_logical_bytes" type="string">
          Output only. Number of logical bytes copied to the destination table. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="copied_rows" type="string">
          Output only. Number of rows copied to the destination table. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="remote_destination_region" type="string">
          Output only. Destination region for a cross-region copy job. Not set for in-region copy jobs. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="creation_time" type="string">
      Output only. Creation time of this job, in milliseconds since the epoch. This field will be present on all jobs. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="data_masking_statistics" type="object">
      Statistics for data-masking. Optional.
      <Expandable title="properties">
        <ResponseField name="data_masking_applied" type="bool">
          Whether any accessed data was protected by the data masking. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="edition" type="string">
      Output only. Name of edition corresponding to the reservation for this job at the time of this update. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="end_time" type="string">
      Output only. End time of this job, in milliseconds since the epoch. This field will be present whenever a job is in the DONE state. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="extract" type="object">
      Statistics for an extract job. Optional.
      <Expandable title="properties">
        <ResponseField name="destination_uri_file_counts" type="list(string)">
          Output only. Number of files per destination URI or URI pattern specified in the extract configuration. These values will be in the same order as the URIs specified in the 'destinationUris' field. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="input_bytes" type="string">
          Output only. Number of user bytes extracted into the result. This is the byte count as computed by BigQuery for billing purposes and doesn't have any relationship with the number of actual result bytes extracted in the desired format. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="timeline" type="list(object)">
          Output only. Describes a timeline of job execution. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="active_units" type="string">
              How many work units this stage is currently processing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="completed_units" type="string">
              How many work units this stage has finished processing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="elapsed_ms" type="string">
              How long this stage/step has been running, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="estimated_runnable_units" type="string">
              An estimate of how many more work units could run in parallel right now, given available slots. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pending_units" type="string">
              How many work units this stage hasn't started processing yet. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="shuffle_ram_usage_ratio" type="number">
              What fraction of available shuffle memory this stage used. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="total_slot_ms" type="string">
              The total slot-milliseconds consumed across every stage of this job. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="final_execution_duration_ms" type="string">
      Output only. The duration in milliseconds of the execution of the final attempt of this job, as BigQuery may internally re-attempt to execute the job. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="global_query_remote_regions" type="list(string)">
      Output only. Regions where the global query accesses data. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="load" type="object">
      Statistics for a load job. Optional.
      <Expandable title="properties">
        <ResponseField name="bad_records" type="string">
          Output only. The number of bad records encountered. Note that if the job has failed because of more bad records encountered than the maximum allowed in the load job configuration, then this number can be less than the total number of bad records present in the input data. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="input_file_bytes" type="string">
          Output only. Number of bytes of source data in a load job. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="input_files" type="string">
          Output only. Number of source files in a load job. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="output_bytes" type="string">
          Output only. Size of the loaded data in bytes. Note that while a load job is in the running state, this value may change. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="output_rows" type="string">
          Output only. Number of rows imported in a load job. Note that while an import job is in the running state, this value may change. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="timeline" type="list(object)">
          Output only. Describes a timeline of job execution. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="active_units" type="string">
              How many work units this stage is currently processing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="completed_units" type="string">
              How many work units this stage has finished processing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="elapsed_ms" type="string">
              How long this stage/step has been running, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="estimated_runnable_units" type="string">
              An estimate of how many more work units could run in parallel right now, given available slots. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pending_units" type="string">
              How many work units this stage hasn't started processing yet. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="shuffle_ram_usage_ratio" type="number">
              What fraction of available shuffle memory this stage used. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="total_slot_ms" type="string">
              The total slot-milliseconds consumed across every stage of this job. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="num_child_jobs" type="string">
      Output only. Number of child jobs executed. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="parent_global_query_job" type="object">
      A job reference is a fully qualified identifier for referring to a job. Optional.
      <Expandable title="properties">
        <ResponseField name="job_id" type="string">
          Required. The ID of the job. The ID must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). The maximum length is 1,024 characters. Optional.
        </ResponseField>
        <ResponseField name="location" type="string">
          Optional. The geographic location of the job. The default value is US. For more information about BigQuery locations, see: https://cloud.google.com/bigquery/docs/locations Optional.
        </ResponseField>
        <ResponseField name="project_id" type="string">
          Required. The ID of the project containing this job. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="parent_job_id" type="string">
      Output only. If this is a child job, specifies the job ID of the parent. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="query" type="object">
      Statistics for a query job. Optional.
      <Expandable title="properties">
        <ResponseField name="bi_engine_statistics" type="object">
          Statistics for a BI Engine specific query. Populated as part of JobStatistics2 Optional.
          <Expandable title="properties">
            <ResponseField name="acceleration_mode" type="string">
              Output only. Specifies which mode of BI Engine acceleration was performed (if any). Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="bi_engine_mode" type="string">
              Output only. Specifies which mode of BI Engine acceleration was performed (if any). Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="bi_engine_reasons" type="list(object)">
              In case of DISABLED or PARTIAL bi_engine_mode, these contain the explanatory reasons as to why BI Engine could not accelerate. In case the full query was accelerated, this field is not populated. Optional.
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  A machine-readable code identifying this result or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable message describing this event, error, or status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="billing_tier" type="number">
          Output only. Billing tier for the job. This is a BigQuery-specific concept which is not related to the Google Cloud notion of "free tier". The value here is a measure of the query's resource consumption relative to the amount of data scanned. For on-demand queries, the limit is 100, and all queries within this limit are billed at the standard on-demand rates. On-demand queries that exceed this limit will fail with a billingTierLimitExceeded error. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="cache_hit" type="bool">
          Output only. Whether the query result was fetched from the query cache. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="dcl_target_dataset" type="object">
          Identifier for a dataset. Optional.
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. A unique ID for this dataset, without the project name. The ID must contain only letters (a-z, A-Z), numbers (0-9), or underscores (_). The maximum length is 1,024 characters. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Optional. The ID of the project containing this dataset. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dcl_target_table" type="object">
          The table a Data Control Language statement (e.g. `GRANT`) applies to. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this table. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this table. Optional.
            </ResponseField>
            <ResponseField name="table_id" type="string">
              Required. The ID of the table. The ID can contain Unicode characters in category L (letter), M (mark), N (number), Pc (connector, including underscore), Pd (dash), and Zs (space). For more information, see [General Category](https://wikipedia.org/wiki/Unicode_character_property#General_Category). The maximum length is 1,024 characters. Certain operations allow suffixing of the table ID with a partition decorator, such as `sample_table$20190123`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dcl_target_view" type="object">
          The view a Data Control Language statement (e.g. `GRANT`) applies to. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this table. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this table. Optional.
            </ResponseField>
            <ResponseField name="table_id" type="string">
              Required. The ID of the table. The ID can contain Unicode characters in category L (letter), M (mark), N (number), Pc (connector, including underscore), Pd (dash), and Zs (space). For more information, see [General Category](https://wikipedia.org/wiki/Unicode_character_property#General_Category). The maximum length is 1,024 characters. Certain operations allow suffixing of the table ID with a partition decorator, such as `sample_table$20190123`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ddl_affected_row_access_policy_count" type="string">
          Output only. The number of row access policies affected by a DDL statement. Present only for DROP ALL ROW ACCESS POLICIES queries. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="ddl_destination_table" type="object">
          The table a Data Definition Language statement created or modified. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this table. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this table. Optional.
            </ResponseField>
            <ResponseField name="table_id" type="string">
              Required. The ID of the table. The ID can contain Unicode characters in category L (letter), M (mark), N (number), Pc (connector, including underscore), Pd (dash), and Zs (space). For more information, see [General Category](https://wikipedia.org/wiki/Unicode_character_property#General_Category). The maximum length is 1,024 characters. Certain operations allow suffixing of the table ID with a partition decorator, such as `sample_table$20190123`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ddl_operation_performed" type="string">
          Output only. The DDL operation performed, possibly dependent on the pre-existence of the DDL target. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="ddl_target_dataset" type="object">
          Identifier for a dataset. Optional.
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. A unique ID for this dataset, without the project name. The ID must contain only letters (a-z, A-Z), numbers (0-9), or underscores (_). The maximum length is 1,024 characters. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Optional. The ID of the project containing this dataset. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ddl_target_routine" type="object">
          Id path of a routine. Optional.
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this routine. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this routine. Optional.
            </ResponseField>
            <ResponseField name="routine_id" type="string">
              Required. The ID of the routine. The ID must contain only letters (a-z, A-Z), numbers (0-9), or underscores (_). The maximum length is 256 characters. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ddl_target_row_access_policy" type="object">
          Id path of a row access policy. Optional.
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this row access policy. Optional.
            </ResponseField>
            <ResponseField name="policy_id" type="string">
              Required. The ID of the row access policy. The ID must contain only letters (a-z, A-Z), numbers (0-9), or underscores (_). The maximum length is 256 characters. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this row access policy. Optional.
            </ResponseField>
            <ResponseField name="table_id" type="string">
              Required. The ID of the table containing this row access policy. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ddl_target_table" type="object">
          The table a Data Definition Language statement applies to. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              Required. The ID of the dataset containing this table. Optional.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Required. The ID of the project containing this table. Optional.
            </ResponseField>
            <ResponseField name="table_id" type="string">
              Required. The ID of the table. The ID can contain Unicode characters in category L (letter), M (mark), N (number), Pc (connector, including underscore), Pd (dash), and Zs (space). For more information, see [General Category](https://wikipedia.org/wiki/Unicode_character_property#General_Category). The maximum length is 1,024 characters. Certain operations allow suffixing of the table ID with a partition decorator, such as `sample_table$20190123`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dml_stats" type="object">
          Detailed statistics for DML statements Optional.
          <Expandable title="properties">
            <ResponseField name="deleted_row_count" type="string">
              Output only. Number of deleted Rows. populated by DML DELETE, MERGE and TRUNCATE statements. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="dml_mode" type="string">
              Output only. DML mode used. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="fine_grained_dml_unused_reason" type="string">
              Output only. Reason for disabling fine-grained DML if applicable. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="inserted_row_count" type="string">
              Output only. Number of inserted Rows. Populated by DML INSERT and MERGE statements Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="updated_row_count" type="string">
              Output only. Number of updated Rows. Populated by DML UPDATE and MERGE statements. Computed by Google Cloud after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="estimated_bytes_processed" type="string">
          Output only. The original estimate of bytes processed for the job. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="export_data_statistics" type="object">
          Statistics for the EXPORT DATA statement as part of Query Job. EXTRACT JOB statistics are populated in JobStatistics4. Optional.
          <Expandable title="properties">
            <ResponseField name="file_count" type="string">
              Number of destination files generated in case of EXPORT DATA statement only. Optional.
            </ResponseField>
            <ResponseField name="row_count" type="string">
              [Alpha] Number of destination rows generated in case of EXPORT DATA statement only. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="external_service_costs" type="list(object)">
          Output only. Job cost breakdown as bigquery internal cost and external service costs. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="billing_method" type="string">
              How compute cost for this job is charged, e.g. on-demand or by reserved slots. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="bytes_billed" type="string">
              The number of bytes this job was billed for. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="bytes_processed" type="string">
              The number of bytes this job actually processed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_service" type="string">
              A reference to the external service this job used. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="reserved_slot_count" type="string">
              The number of query slots reserved for this job. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="slot_ms" type="string">
              The total slot-milliseconds this job consumed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="gen_ai_stats" type="object">
          GenAi stats for the query job. Optional.
          <Expandable title="properties">
            <ResponseField name="error_stats" type="object">
              Provides error statistics for the query job across all AI function calls. Optional.
              <Expandable title="properties">
                <ResponseField name="errors" type="list(string)">
                  A list of unique errors at query level (up to 5, truncated to 100 chars) Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="function_stats" type="list(object)">
              Function level stats for GenAI Functions. For more information, see [Generative AI overview](https://docs.cloud.google.com/bigquery/docs/generative-ai-overview). Optional.
              <Expandable title="properties">
                <ResponseField name="cache_stats" type="object">
                  Statistics about how effectively cached metadata was used for this query. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="num_cache_hit_rows" type="string">
                      The number of rows served from cached metadata. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cost_optimization_stats" type="object">
                  Statistics about how metadata caching reduced this query's own scan cost. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="message" type="string">
                      A human-readable message describing this event, error, or status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="num_cost_optimized_rows" type="string">
                      The number of rows this query avoided scanning due to metadata caching. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="error_stats" type="object">
                  Statistics about error rows encountered while loading data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="errors" type="list(string)">
                      The error(s) encountered. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="num_failed_rows" type="string">
                      The number of rows that failed to load. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="function_name" type="string">
                  The name of the routine. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="num_processed_rows" type="string">
                  The number of rows processed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="prompt" type="string">
                  The prompt text sent to the model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="incremental_result_stats" type="object">
          Statistics related to Incremental Query Results. Populated as part of JobStatistics2. This feature is not yet available. Optional.
          <Expandable title="properties">
            <ResponseField name="disabled_reason" type="string">
              Output only. Reason why incremental query results are/were not written by the query. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="disabled_reason_details" type="string">
              Output only. Additional human-readable clarification, if available, for DisabledReason. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="first_incremental_row_time" type="string">
              Output only. The time at which the first incremental result was written. If the query needed to restart internally, this only describes the final attempt. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="incremental_row_count" type="string">
              Output only. Number of rows that were in the latest result set before query completion. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="last_incremental_row_time" type="string">
              Output only. The time at which the last incremental result was written. Does not include the final result written after query completion. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="result_set_last_modify_time" type="string">
              Output only. The time at which the result table's contents were modified. May be absent if no results have been written or the query has completed. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="result_set_last_replace_time" type="string">
              Output only. The time at which the result table's contents were completely replaced. May be absent if no results have been written or the query has completed. Computed by Google Cloud after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="load_query_statistics" type="object">
          Statistics for a LOAD query. Optional.
          <Expandable title="properties">
            <ResponseField name="bad_records" type="string">
              Output only. The number of bad records encountered while processing a LOAD query. Note that if the job has failed because of more bad records encountered than the maximum allowed in the load job configuration, then this number can be less than the total number of bad records present in the input data. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="bytes_transferred" type="string">
              Output only. This field is deprecated. The number of bytes of source data copied over the network for a `LOAD` query. `transferred_bytes` has the canonical value for physical transferred bytes, which is used for BigQuery Omni billing. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="input_file_bytes" type="string">
              Output only. Number of bytes of source data in a LOAD query. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="input_files" type="string">
              Output only. Number of source files in a LOAD query. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="output_bytes" type="string">
              Output only. Size of the loaded data in bytes. Note that while a LOAD query is in the running state, this value may change. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="output_rows" type="string">
              Output only. Number of rows imported in a LOAD query. Note that while a LOAD query is in the running state, this value may change. Computed by Google Cloud after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="materialized_view_statistics" type="object">
          Statistics of materialized views considered in a query job. Optional.
          <Expandable title="properties">
            <ResponseField name="materialized_view" type="list(object)">
              Materialized views considered for the query job. Only certain materialized views are used. For a detailed list, see the child message. If many materialized views are considered, then the list might be incomplete. Optional.
              <Expandable title="properties">
                <ResponseField name="chosen" type="bool">
                  Whether this option was actually selected/used. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="estimated_bytes_saved" type="string">
                  The estimated number of bytes this query avoided scanning due to metadata caching or BI Engine acceleration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rejected_reason" type="string">
                  Why this row or record was rejected. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="table_reference" type="object">
                  A reference to a specific BigQuery table. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dataset_id" type="string">
                      The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="project_id" type="string">
                      The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="table_id" type="string">
                      The BigQuery table ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="metadata_cache_statistics" type="object">
          Statistics for metadata caching in queried tables. Optional.
          <Expandable title="properties">
            <ResponseField name="table_metadata_cache_usage" type="list(object)">
              Set for the Metadata caching eligible tables referenced in the query. Optional.
              <Expandable title="properties">
                <ResponseField name="explanation" type="string">
                  A human-readable explanation of this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="pruning_stats" type="object">
                  Statistics about how much data this query avoided reading due to partition/index pruning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="post_cmeta_pruning_parallel_input_count" type="string">
                      The number of input partitions remaining after column-metadata-based pruning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="post_cmeta_pruning_partition_count" type="string">
                      The number of partitions remaining after column-metadata-based pruning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="pre_cmeta_pruning_parallel_input_count" type="string">
                      The number of input partitions before column-metadata-based pruning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="staleness" type="string">
                  How old this cached data or metadata currently is. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="table_reference" type="object">
                  A reference to a specific BigQuery table. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dataset_id" type="string">
                      The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="project_id" type="string">
                      The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="table_id" type="string">
                      The BigQuery table ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="table_type" type="string">
                  Whether this table is a standard table, view, materialized view, or external table. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="unused_reason" type="string">
                  Why this index, cache, or optimization wasn't used for this query. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ml_statistics" type="object">
          Job statistics specific to a BigQuery ML training job. Optional.
          <Expandable title="properties">
            <ResponseField name="hparam_trials" type="list(object)">
              Output only. Trials of a [hyperparameter tuning job](https://cloud.google.com/bigquery-ml/docs/reference/standard-sql/bigqueryml-syntax-hp-tuning-overview) sorted by trial_id. Computed by Google Cloud after `ship`.
              <Expandable title="properties">
                <ResponseField name="end_time_ms" type="string">
                  When this event ended, as a Unix timestamp in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="error_message" type="string">
                  A human-readable description of the error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="eval_loss" type="number">
                  The loss value computed on the evaluation (held-out) dataset. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="evaluation_metrics" type="object">
                  The evaluation metric(s) computed for this trained model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="arima_forecasting_metrics" type="object">
                      Forecast-quality metrics for a fitted ARIMA model, one set per time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="arima_fitting_metrics" type="list(object)">
                          Goodness-of-fit statistic(s) (e.g. AIC, log-likelihood) for a fitted ARIMA time-series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="aic" type="number">
                              The Akaike Information Criterion for this fitted model -- lower values indicate a better tradeoff between fit and complexity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="log_likelihood" type="number">
                              The log-likelihood of the fitted model given the training data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="variance" type="number">
                              The variance of the model's own residuals. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="arima_single_model_forecasting_metrics" type="list(object)">
                          Forecast-quality metrics for a single fitted ARIMA time series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="arima_fitting_metrics" type="object">
                              Goodness-of-fit statistic(s) (e.g. AIC, log-likelihood) for a fitted ARIMA time-series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="aic" type="number">
                                  The Akaike Information Criterion for this fitted model -- lower values indicate a better tradeoff between fit and complexity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="log_likelihood" type="number">
                                  The log-likelihood of the fitted model given the training data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="variance" type="number">
                                  The variance of the model's own residuals. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="has_drift" type="bool">
                              Whether this ARIMA model was fit with a drift term. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="has_holiday_effect" type="bool">
                              Whether the fitted ARIMA model detected a holiday effect in the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="has_spikes_and_dips" type="bool">
                              Whether the fitted ARIMA model detected spikes and dips in the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="has_step_changes" type="bool">
                              Whether the fitted ARIMA model detected step changes in the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="non_seasonal_order" type="object">
                              The (p, d, q) ARIMA order -- autoregressive terms, differencing degree, and moving-average terms -- this forecasting model was fit with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="d" type="string">
                                  The differencing degree (`d`) of the ARIMA model -- how many times the series was differenced to make it stationary. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="p" type="string">
                                  The autoregressive order (`p`) of the ARIMA model -- how many past values it regresses on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="q" type="string">
                                  The moving-average order (`q`) of the ARIMA model -- how many past forecast errors it incorporates. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="seasonal_periods" type="list(string)">
                              The seasonal period length(s) (e.g. weekly, yearly) this ARIMA model accounts for; a model may account for more than one. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="time_series_id" type="string">
                              The identifier of the specific time series this result applies to, for a multi-series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="time_series_ids" type="list(string)">
                              The identifier(s) of the time series this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="has_drift" type="list(bool)">
                          Whether this ARIMA model was fit with a drift term. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="non_seasonal_order" type="list(object)">
                          The (p, d, q) ARIMA order -- autoregressive terms, differencing degree, and moving-average terms -- this forecasting model was fit with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="d" type="string">
                              The differencing degree (`d`) of the ARIMA model -- how many times the series was differenced to make it stationary. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="p" type="string">
                              The autoregressive order (`p`) of the ARIMA model -- how many past values it regresses on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="q" type="string">
                              The moving-average order (`q`) of the ARIMA model -- how many past forecast errors it incorporates. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="seasonal_periods" type="list(string)">
                          The seasonal period length(s) (e.g. weekly, yearly) this ARIMA model accounts for; a model may account for more than one. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="time_series_id" type="list(string)">
                          The identifier of the specific time series this result applies to, for a multi-series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="binary_classification_metrics" type="object">
                      Evaluation metrics specific to a binary (two-class) classification model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="aggregate_classification_metrics" type="object">
                          Classification evaluation metrics (precision, recall, accuracy, etc.) computed across all classes, aggregated to a single score. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="accuracy" type="number">
                              The fraction of predictions this model got exactly right, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="f1_score" type="number">
                              The harmonic mean of this model's own precision and recall, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="log_loss" type="number">
                              The logarithmic loss of the model's own predicted probabilities against the true labels -- lower is better. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="precision" type="number">
                              For a `NUMERIC`/`BIGNUMERIC` field, the total number of digits it can hold, both before and after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="recall" type="number">
                              The fraction of actual positive cases this model correctly identified, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="roc_auc" type="number">
                              The area under the ROC curve for this classification model -- closer to 1.0 indicates better separation between classes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="threshold" type="number">
                              The classification probability threshold above which a prediction is treated as positive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="binary_confusion_matrix_list" type="list(object)">
                          The confusion matrix/matrices computed at various classification thresholds, for a binary classifier. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="accuracy" type="number">
                              The fraction of predictions this model got exactly right, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="f1_score" type="number">
                              The harmonic mean of this model's own precision and recall, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="false_negatives" type="string">
                              The count of instances the model incorrectly predicted as negative. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="false_positives" type="string">
                              The count of instances the model incorrectly predicted as positive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="positive_class_threshold" type="number">
                              The classification probability threshold above which a prediction is treated as the positive class. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="precision" type="number">
                              For a `NUMERIC`/`BIGNUMERIC` field, the total number of digits it can hold, both before and after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="recall" type="number">
                              The fraction of actual positive cases this model correctly identified, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="true_negatives" type="string">
                              The count of instances the model correctly predicted as negative. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="true_positives" type="string">
                              The count of instances the model correctly predicted as positive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="negative_label" type="string">
                          The label value treated as the negative class. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="positive_label" type="string">
                          The label value treated as the positive class. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="clustering_metrics" type="object">
                      Evaluation metrics specific to a clustering model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="clusters" type="list(object)">
                          The cluster(s) produced by a clustering model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="centroid_id" type="string">
                              The identifier of the cluster centroid this applies to, for a clustering model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="count" type="string">
                              How many of this item there are. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="feature_values" type="list(object)">
                              The value(s) of the input feature(s) for this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="categorical_value" type="object">
                                  A single categorical value and its own observed count. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `categorical_value` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="feature_column" type="string">
                                  The name of the feature (input) column this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="numerical_value" type="number">
                                  A numeric value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="davies_bouldin_index" type="number">
                          A clustering-quality score measuring average similarity between each cluster and its most similar other cluster -- lower is better. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mean_squared_distance" type="number">
                          The average squared distance from each point to its own assigned cluster centroid. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="dimensionality_reduction_metrics" type="object">
                      Evaluation metrics specific to a dimensionality-reduction (e.g. PCA) model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="total_explained_variance_ratio" type="number">
                          The total fraction of variance explained across all retained principal components. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="multi_class_classification_metrics" type="object">
                      Evaluation metrics specific to a multi-class classification model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="aggregate_classification_metrics" type="object">
                          Classification evaluation metrics (precision, recall, accuracy, etc.) computed across all classes, aggregated to a single score. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="accuracy" type="number">
                              The fraction of predictions this model got exactly right, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="f1_score" type="number">
                              The harmonic mean of this model's own precision and recall, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="log_loss" type="number">
                              The logarithmic loss of the model's own predicted probabilities against the true labels -- lower is better. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="precision" type="number">
                              For a `NUMERIC`/`BIGNUMERIC` field, the total number of digits it can hold, both before and after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="recall" type="number">
                              The fraction of actual positive cases this model correctly identified, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="roc_auc" type="number">
                              The area under the ROC curve for this classification model -- closer to 1.0 indicates better separation between classes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="threshold" type="number">
                              The classification probability threshold above which a prediction is treated as positive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="confusion_matrix_list" type="list(object)">
                          The confusion matrix/matrices computed at various classification thresholds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="confidence_threshold" type="number">
                              The minimum confidence a prediction must reach to be accepted. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="rows" type="list(object)">
                              The row(s) making up this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="actual_label" type="string">
                                  The true, observed label for this evaluation instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="entries" type="list(object)">
                                  The entry/entries making up this collection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `entries` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="ranking_metrics" type="object">
                      Evaluation metrics specific to a ranking/recommendation model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="average_rank" type="number">
                          The average rank assigned across evaluation instances, for a ranking model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mean_average_precision" type="number">
                          The mean of average precision scores across queries/users, for a ranking model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mean_squared_error" type="number">
                          The average squared difference between predicted and actual values, for a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="normalized_discounted_cumulative_gain" type="number">
                          A ranking-quality score comparing the model's own result ordering to an ideal ordering, normalized to [0, 1]. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="regression_metrics" type="object">
                      Evaluation metrics specific to a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mean_absolute_error" type="number">
                          The average absolute difference between predicted and actual values, for a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mean_squared_error" type="number">
                          The average squared difference between predicted and actual values, for a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mean_squared_log_error" type="number">
                          The average squared difference between the log of predicted and actual values, for a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="median_absolute_error" type="number">
                          The median absolute difference between predicted and actual values, for a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="r_squared" type="number">
                          The proportion of variance in the target explained by the model -- closer to 1.0 indicates a better fit. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="hparam_tuning_evaluation_metrics" type="object">
                  The evaluation metric(s) recorded for one hyperparameter tuning trial. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="arima_forecasting_metrics" type="object">
                      Forecast-quality metrics for a fitted ARIMA model, one set per time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="arima_fitting_metrics" type="list(object)">
                          Goodness-of-fit statistic(s) (e.g. AIC, log-likelihood) for a fitted ARIMA time-series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="aic" type="number">
                              The Akaike Information Criterion for this fitted model -- lower values indicate a better tradeoff between fit and complexity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="log_likelihood" type="number">
                              The log-likelihood of the fitted model given the training data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="variance" type="number">
                              The variance of the model's own residuals. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="arima_single_model_forecasting_metrics" type="list(object)">
                          Forecast-quality metrics for a single fitted ARIMA time series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="arima_fitting_metrics" type="object">
                              Goodness-of-fit statistic(s) (e.g. AIC, log-likelihood) for a fitted ARIMA time-series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="aic" type="number">
                                  The Akaike Information Criterion for this fitted model -- lower values indicate a better tradeoff between fit and complexity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="log_likelihood" type="number">
                                  The log-likelihood of the fitted model given the training data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="variance" type="number">
                                  The variance of the model's own residuals. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="has_drift" type="bool">
                              Whether this ARIMA model was fit with a drift term. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="has_holiday_effect" type="bool">
                              Whether the fitted ARIMA model detected a holiday effect in the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="has_spikes_and_dips" type="bool">
                              Whether the fitted ARIMA model detected spikes and dips in the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="has_step_changes" type="bool">
                              Whether the fitted ARIMA model detected step changes in the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="non_seasonal_order" type="object">
                              The (p, d, q) ARIMA order -- autoregressive terms, differencing degree, and moving-average terms -- this forecasting model was fit with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="d" type="string">
                                  The differencing degree (`d`) of the ARIMA model -- how many times the series was differenced to make it stationary. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="p" type="string">
                                  The autoregressive order (`p`) of the ARIMA model -- how many past values it regresses on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="q" type="string">
                                  The moving-average order (`q`) of the ARIMA model -- how many past forecast errors it incorporates. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="seasonal_periods" type="list(string)">
                              The seasonal period length(s) (e.g. weekly, yearly) this ARIMA model accounts for; a model may account for more than one. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="time_series_id" type="string">
                              The identifier of the specific time series this result applies to, for a multi-series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="time_series_ids" type="list(string)">
                              The identifier(s) of the time series this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="has_drift" type="list(bool)">
                          Whether this ARIMA model was fit with a drift term. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="non_seasonal_order" type="list(object)">
                          The (p, d, q) ARIMA order -- autoregressive terms, differencing degree, and moving-average terms -- this forecasting model was fit with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="d" type="string">
                              The differencing degree (`d`) of the ARIMA model -- how many times the series was differenced to make it stationary. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="p" type="string">
                              The autoregressive order (`p`) of the ARIMA model -- how many past values it regresses on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="q" type="string">
                              The moving-average order (`q`) of the ARIMA model -- how many past forecast errors it incorporates. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="seasonal_periods" type="list(string)">
                          The seasonal period length(s) (e.g. weekly, yearly) this ARIMA model accounts for; a model may account for more than one. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="time_series_id" type="list(string)">
                          The identifier of the specific time series this result applies to, for a multi-series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="binary_classification_metrics" type="object">
                      Evaluation metrics specific to a binary (two-class) classification model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="aggregate_classification_metrics" type="object">
                          Classification evaluation metrics (precision, recall, accuracy, etc.) computed across all classes, aggregated to a single score. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="accuracy" type="number">
                              The fraction of predictions this model got exactly right, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="f1_score" type="number">
                              The harmonic mean of this model's own precision and recall, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="log_loss" type="number">
                              The logarithmic loss of the model's own predicted probabilities against the true labels -- lower is better. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="precision" type="number">
                              For a `NUMERIC`/`BIGNUMERIC` field, the total number of digits it can hold, both before and after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="recall" type="number">
                              The fraction of actual positive cases this model correctly identified, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="roc_auc" type="number">
                              The area under the ROC curve for this classification model -- closer to 1.0 indicates better separation between classes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="threshold" type="number">
                              The classification probability threshold above which a prediction is treated as positive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="binary_confusion_matrix_list" type="list(object)">
                          The confusion matrix/matrices computed at various classification thresholds, for a binary classifier. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="accuracy" type="number">
                              The fraction of predictions this model got exactly right, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="f1_score" type="number">
                              The harmonic mean of this model's own precision and recall, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="false_negatives" type="string">
                              The count of instances the model incorrectly predicted as negative. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="false_positives" type="string">
                              The count of instances the model incorrectly predicted as positive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="positive_class_threshold" type="number">
                              The classification probability threshold above which a prediction is treated as the positive class. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="precision" type="number">
                              For a `NUMERIC`/`BIGNUMERIC` field, the total number of digits it can hold, both before and after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="recall" type="number">
                              The fraction of actual positive cases this model correctly identified, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="true_negatives" type="string">
                              The count of instances the model correctly predicted as negative. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="true_positives" type="string">
                              The count of instances the model correctly predicted as positive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="negative_label" type="string">
                          The label value treated as the negative class. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="positive_label" type="string">
                          The label value treated as the positive class. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="clustering_metrics" type="object">
                      Evaluation metrics specific to a clustering model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="clusters" type="list(object)">
                          The cluster(s) produced by a clustering model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="centroid_id" type="string">
                              The identifier of the cluster centroid this applies to, for a clustering model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="count" type="string">
                              How many of this item there are. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="feature_values" type="list(object)">
                              The value(s) of the input feature(s) for this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="categorical_value" type="object">
                                  A single categorical value and its own observed count. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `categorical_value` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="feature_column" type="string">
                                  The name of the feature (input) column this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="numerical_value" type="number">
                                  A numeric value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="davies_bouldin_index" type="number">
                          A clustering-quality score measuring average similarity between each cluster and its most similar other cluster -- lower is better. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mean_squared_distance" type="number">
                          The average squared distance from each point to its own assigned cluster centroid. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="dimensionality_reduction_metrics" type="object">
                      Evaluation metrics specific to a dimensionality-reduction (e.g. PCA) model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="total_explained_variance_ratio" type="number">
                          The total fraction of variance explained across all retained principal components. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="multi_class_classification_metrics" type="object">
                      Evaluation metrics specific to a multi-class classification model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="aggregate_classification_metrics" type="object">
                          Classification evaluation metrics (precision, recall, accuracy, etc.) computed across all classes, aggregated to a single score. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="accuracy" type="number">
                              The fraction of predictions this model got exactly right, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="f1_score" type="number">
                              The harmonic mean of this model's own precision and recall, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="log_loss" type="number">
                              The logarithmic loss of the model's own predicted probabilities against the true labels -- lower is better. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="precision" type="number">
                              For a `NUMERIC`/`BIGNUMERIC` field, the total number of digits it can hold, both before and after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="recall" type="number">
                              The fraction of actual positive cases this model correctly identified, on its own evaluation data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="roc_auc" type="number">
                              The area under the ROC curve for this classification model -- closer to 1.0 indicates better separation between classes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="threshold" type="number">
                              The classification probability threshold above which a prediction is treated as positive. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="confusion_matrix_list" type="list(object)">
                          The confusion matrix/matrices computed at various classification thresholds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="confidence_threshold" type="number">
                              The minimum confidence a prediction must reach to be accepted. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="rows" type="list(object)">
                              The row(s) making up this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="actual_label" type="string">
                                  The true, observed label for this evaluation instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="entries" type="list(object)">
                                  The entry/entries making up this collection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `entries` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="ranking_metrics" type="object">
                      Evaluation metrics specific to a ranking/recommendation model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="average_rank" type="number">
                          The average rank assigned across evaluation instances, for a ranking model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mean_average_precision" type="number">
                          The mean of average precision scores across queries/users, for a ranking model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mean_squared_error" type="number">
                          The average squared difference between predicted and actual values, for a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="normalized_discounted_cumulative_gain" type="number">
                          A ranking-quality score comparing the model's own result ordering to an ideal ordering, normalized to [0, 1]. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="regression_metrics" type="object">
                      Evaluation metrics specific to a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mean_absolute_error" type="number">
                          The average absolute difference between predicted and actual values, for a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mean_squared_error" type="number">
                          The average squared difference between predicted and actual values, for a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mean_squared_log_error" type="number">
                          The average squared difference between the log of predicted and actual values, for a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="median_absolute_error" type="number">
                          The median absolute difference between predicted and actual values, for a regression model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="r_squared" type="number">
                          The proportion of variance in the target explained by the model -- closer to 1.0 indicates a better fit. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="hparams" type="object">
                  The hyperparameter value(s) used for this trial. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="activation_fn" type="string">
                      The activation function used between hidden layers of a neural network model, e.g. `RELU`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="adjust_step_changes" type="bool">
                      Whether the ARIMA model automatically adjusts for detected step changes in the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="approx_global_feature_contrib" type="bool">
                      Whether feature attribution is computed using a faster, approximate method rather than an exact one. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="auto_arima" type="bool">
                      Whether BigQuery ML automatically searches for the best-fitting ARIMA order, rather than using a caller-specified one. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="auto_arima_max_order" type="string">
                      The maximum ARIMA order considered during automatic order search. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="auto_arima_min_order" type="string">
                      The minimum ARIMA order considered during automatic order search. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="auto_class_weights" type="bool">
                      Whether class weights are automatically balanced based on label frequency in the training data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="batch_size" type="string">
                      How many examples are processed together in each training step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="booster_type" type="string">
                      The boosting algorithm used, e.g. `GBTREE` or `DART`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="budget_hours" type="number">
                      The maximum training time allotted, in hours. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="calculate_pvalues" type="bool">
                      Whether statistical p-values are computed for model coefficients. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="category_encoding_method" type="string">
                      How categorical features are encoded into numeric input for the model, e.g. one-hot. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="clean_spikes_and_dips" type="bool">
                      Whether transient spikes and dips are automatically smoothed out of the time series before fitting. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="color_space" type="string">
                      The color space used to interpret pixel data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="colsample_bylevel" type="number">
                      The fraction of features randomly sampled at each tree level, for a boosted tree model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="colsample_bynode" type="number">
                      The fraction of features randomly sampled at each tree split, for a boosted tree model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="colsample_bytree" type="number">
                      The fraction of features randomly sampled when building each tree, for a boosted tree model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="contribution_metric" type="string">
                      Which metric feature contributions/attributions are computed relative to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="dart_normalize_type" type="string">
                      How tree contributions are normalized in DART boosting, e.g. `TREE` or `FOREST`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="data_frequency" type="string">
                      The expected time interval between consecutive observations in a time series, e.g. daily or hourly. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="data_split_column" type="string">
                      The column identifying which split (train/eval) each row belongs to, for a custom data split. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="data_split_eval_fraction" type="number">
                      The fraction of training data held out for evaluation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="data_split_method" type="string">
                      How training data is divided into training and evaluation sets, e.g. random or sequential. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="decompose_time_series" type="bool">
                      Whether the time series is decomposed into trend, seasonal, and holiday components as part of fitting. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="dimension_id_columns" type="list(string)">
                      The column(s) identifying which entity/dimension each row of a multi-dimensional time series belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="distance_type" type="string">
                      The distance metric used to measure similarity between points, e.g. Euclidean or cosine. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="dropout" type="number">
                      The fraction of units randomly dropped during training, to reduce overfitting. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="early_stop" type="bool">
                      Whether training stops automatically once further iterations no longer improve the evaluation metric. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="enable_global_explain" type="bool">
                      Whether global (whole-model) feature-importance explanations are computed, in addition to per-prediction ones. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="endpoint_idle_ttl" type="string">
                      How long a deployed model endpoint is kept warm without traffic before being torn down. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="feedback_type" type="string">
                      Whether user feedback for this recommender model is implicit or explicit. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="fit_intercept" type="bool">
                      Whether an intercept term is fit as part of the model, rather than forcing it to zero. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="forecast_limit_lower_bound" type="number">
                      The lower bound of the forecast's own confidence interval. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="forecast_limit_upper_bound" type="number">
                      The upper bound of the forecast's own confidence interval. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="hidden_units" type="list(string)">
                      The number of units in each hidden layer, for a neural network model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="holiday_region" type="string">
                      The region whose public holidays are factored into ARIMA forecasting. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="holiday_regions" type="list(string)">
                      The region(s) whose public holidays are factored into ARIMA forecasting. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="horizon" type="string">
                      How many future time steps this forecast covers. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="hparam_tuning_objectives" type="list(string)">
                      The metric(s) hyperparameter tuning optimizes for. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="hugging_face_model_id" type="string">
                      The Hugging Face Hub identifier of the model this references. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_drift" type="bool">
                      Whether the ARIMA model's own drift term is included when computing forecasts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="initial_learn_rate" type="number">
                      The learning rate used at the start of training, before any line-search adjustment. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="input_label_columns" type="list(string)">
                      The column(s) holding the true label(s) used to train and evaluate the model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="instance_weight_column" type="string">
                      The column giving each training example's own relative importance weight. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="integrated_gradients_num_steps" type="string">
                      How many interpolation steps are used when computing Integrated Gradients feature attributions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="is_test_column" type="string">
                      The column marking which rows belong to the held-out test set. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="item_column" type="string">
                      The column identifying the item in a recommender model's own training data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="kmeans_initialization_column" type="string">
                      The column providing caller-specified initial centroid positions for k-means clustering. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="kmeans_initialization_method" type="string">
                      How initial centroid positions are chosen for k-means clustering, e.g. random or k-means++. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="l1_reg_activation" type="number">
                      The L1 regularization strength applied to activations, for a neural network model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="l1_regularization" type="number">
                      The L1 regularization strength applied during training. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="l2_regularization" type="number">
                      The L2 regularization strength applied during training. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="label_class_weights" type="map(number)">
                      Per-class weight(s) applied during training, to counteract class imbalance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="learn_rate" type="number">
                      The step size used to update model weights during training. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="learn_rate_strategy" type="string">
                      How the learning rate changes over training, e.g. fixed or line-search. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="loss_type" type="string">
                      The loss function optimized during training. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="machine_type" type="string">
                      The machine type provisioned for this training job. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="max_iterations" type="string">
                      The maximum number of training iterations. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="max_parallel_trials" type="string">
                      The maximum number of hyperparameter tuning trials run concurrently. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="max_replica_count" type="string">
                      The maximum number of serving replicas this deployed model may scale up to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="max_time_series_length" type="string">
                      The maximum number of historical time steps used when fitting the model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="max_tree_depth" type="string">
                      The maximum depth a single decision tree may reach. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="min_apriori_support" type="number">
                      The minimum support threshold an itemset must reach to be considered frequent, for association rule mining. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="min_relative_progress" type="number">
                      The minimum relative improvement in loss required to continue training another iteration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="min_replica_count" type="string">
                      The minimum number of serving replicas this deployed model scales down to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="min_split_loss" type="number">
                      The minimum loss reduction required to make a further split in a decision tree. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="min_time_series_length" type="string">
                      The minimum number of historical time steps required to fit the model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="min_tree_child_weight" type="string">
                      The minimum sum of instance weight required in a tree leaf, for a boosted tree model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="model_garden_model_name" type="string">
                      The Model Garden identifier of the foundation model this references. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="model_registry" type="string">
                      Which model registry this model is published to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="model_uri" type="string">
                      The Cloud Storage URI of the model artifact. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="non_seasonal_order" type="object">
                      The (p, d, q) ARIMA order -- autoregressive terms, differencing degree, and moving-average terms -- this forecasting model was fit with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="d" type="string">
                          The differencing degree (`d`) of the ARIMA model -- how many times the series was differenced to make it stationary. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="p" type="string">
                          The autoregressive order (`p`) of the ARIMA model -- how many past values it regresses on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="q" type="string">
                          The moving-average order (`q`) of the ARIMA model -- how many past forecast errors it incorporates. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="num_clusters" type="string">
                      The number of clusters the model is fit with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="num_factors" type="string">
                      The number of latent factors used in a matrix factorization model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="num_parallel_tree" type="string">
                      The number of trees built in parallel at each boosting round, for a random-forest-style ensemble. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="num_principal_components" type="string">
                      The number of principal components computed by a PCA model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="num_trials" type="string">
                      The number of hyperparameter tuning trials run. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="optimization_strategy" type="string">
                      Which algorithm is used to optimize the training objective. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="optimizer" type="string">
                      The optimization algorithm used to update model weights during training, e.g. Adam. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="pca_explained_variance_ratio" type="number">
                      The fraction of total variance each principal component explains. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="pca_solver" type="string">
                      The algorithm used to compute principal components. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="reservation_affinity_key" type="string">
                      The reservation-assignment label key this job's own reservation affinity matches on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="reservation_affinity_type" type="string">
                      Whether this job must, may, or must not use a specific reservation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="reservation_affinity_values" type="list(string)">
                      The reservation-assignment label value(s) this job's own reservation affinity matches on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sampled_shapley_num_paths" type="string">
                      How many feature-permutation paths Sampled Shapley samples to approximate attributions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="scale_features" type="bool">
                      Whether input features are automatically normalized (scaled) before training. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="standardize_features" type="bool">
                      Whether input features are standardized (zero mean, unit variance) before training. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="subsample" type="number">
                      The fraction of training examples randomly sampled for each boosting round. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tf_version" type="string">
                      The TensorFlow version this imported model was trained with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="time_series_data_column" type="string">
                      The column holding the value(s) to forecast. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="time_series_id_column" type="string">
                      The column identifying which time series each row belongs to, when training on multiple series at once. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="time_series_id_columns" type="list(string)">
                      The column(s) identifying which time series each row belongs to, when training on multiple series at once. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="time_series_length_fraction" type="number">
                      The fraction of the time series' own full length used to compute this metric. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="time_series_timestamp_column" type="string">
                      The column holding each observation's own timestamp. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tree_method" type="string">
                      The algorithm used to build each decision tree, e.g. exact or histogram-based. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="trend_smoothing_window_size" type="string">
                      The window size used to smooth the trend component when decomposing the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="user_column" type="string">
                      The column identifying the user in a recommender model's own training data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="vertex_ai_model_version_aliases" type="list(string)">
                      The Vertex AI Model Registry version alias(es) assigned to this model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="wals_alpha" type="number">
                      The confidence weighting parameter used by the Weighted Alternating Least Squares algorithm, for matrix factorization. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="warm_start" type="bool">
                      Whether training resumes from a previous model's own learned weights, rather than starting from scratch. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="xgboost_version" type="string">
                      The XGBoost library version this imported model was trained with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="start_time_ms" type="string">
                  When this event started, as a Unix timestamp in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="status" type="string">
                  The current status of this resource or operation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="training_loss" type="number">
                  The loss value computed on the training dataset. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="trial_id" type="string">
                  An identifier for this hyperparameter tuning trial. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="iteration_results" type="list(object)">
              Results for all completed iterations. Empty for [hyperparameter tuning jobs](https://cloud.google.com/bigquery-ml/docs/reference/standard-sql/bigqueryml-syntax-hp-tuning-overview). Optional.
              <Expandable title="properties">
                <ResponseField name="arima_result" type="object">
                  The fitted ARIMA model result(s), one per time series analyzed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="arima_model_info" type="list(object)">
                      Detail about one fitted ARIMA model, when a query trains multiple time series at once. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="arima_coefficients" type="object">
                          The fitted autoregressive, moving-average, and intercept coefficient(s) of an ARIMA model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="auto_regressive_coefficients" type="list(number)">
                              The fitted autoregressive coefficient(s) of the model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="intercept_coefficient" type="number">
                              The fitted intercept term of the model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="moving_average_coefficients" type="list(number)">
                              The fitted moving-average coefficient(s) of an ARIMA model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="arima_fitting_metrics" type="object">
                          Goodness-of-fit statistic(s) (e.g. AIC, log-likelihood) for a fitted ARIMA time-series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="aic" type="number">
                              The Akaike Information Criterion for this fitted model -- lower values indicate a better tradeoff between fit and complexity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="log_likelihood" type="number">
                              The log-likelihood of the fitted model given the training data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="variance" type="number">
                              The variance of the model's own residuals. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="has_drift" type="bool">
                          Whether this ARIMA model was fit with a drift term. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="has_holiday_effect" type="bool">
                          Whether the fitted ARIMA model detected a holiday effect in the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="has_spikes_and_dips" type="bool">
                          Whether the fitted ARIMA model detected spikes and dips in the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="has_step_changes" type="bool">
                          Whether the fitted ARIMA model detected step changes in the time series. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="non_seasonal_order" type="object">
                          The (p, d, q) ARIMA order -- autoregressive terms, differencing degree, and moving-average terms -- this forecasting model was fit with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="d" type="string">
                              The differencing degree (`d`) of the ARIMA model -- how many times the series was differenced to make it stationary. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="p" type="string">
                              The autoregressive order (`p`) of the ARIMA model -- how many past values it regresses on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="q" type="string">
                              The moving-average order (`q`) of the ARIMA model -- how many past forecast errors it incorporates. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="seasonal_periods" type="list(string)">
                          The seasonal period length(s) (e.g. weekly, yearly) this ARIMA model accounts for; a model may account for more than one. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="time_series_id" type="string">
                          The identifier of the specific time series this result applies to, for a multi-series model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="time_series_ids" type="list(string)">
                          The identifier(s) of the time series this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="seasonal_periods" type="list(string)">
                      The seasonal period length(s) (e.g. weekly, yearly) this ARIMA model accounts for; a model may account for more than one. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cluster_infos" type="list(object)">
                  Detail about each cluster produced by a clustering model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="centroid_id" type="string">
                      The identifier of the cluster centroid this applies to, for a clustering model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="cluster_radius" type="number">
                      The average distance from a cluster's own centroid to its member points. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="cluster_size" type="string">
                      How many training examples fall into this cluster. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="duration_ms" type="string">
                  How long this operation took, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="eval_loss" type="number">
                  The loss value computed on the evaluation (held-out) dataset. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="index" type="number">
                  The position of this item within its own containing sequence. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="learn_rate" type="number">
                  The step size used to update model weights during training. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="principal_component_infos" type="list(object)">
                  Detail about each principal component computed by a PCA model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="cumulative_explained_variance_ratio" type="number">
                      The running total of variance explained as successive principal components are added, for a PCA model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="explained_variance" type="number">
                      How much of the total variance in the data this component/model accounts for. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="explained_variance_ratio" type="number">
                      The fraction of total variance this principal component explains. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="principal_component_id" type="string">
                      The identifier of the principal component this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="training_loss" type="number">
                  The loss value computed on the training dataset. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_iterations" type="string">
              Output only. Maximum number of iterations specified as max_iterations in the 'CREATE MODEL' query. The actual number of iterations may be less than this number due to early stop. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="model_type" type="string">
              Output only. The type of the model that is being trained. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="training_type" type="string">
              Output only. Training type of the job. Computed by Google Cloud after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="model_training" type="object">
          Detail about this model's own training run. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="current_iteration" type="number">
              Deprecated. Optional.
            </ResponseField>
            <ResponseField name="expected_total_iterations" type="string">
              Deprecated. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="model_training_current_iteration" type="number">
          Deprecated. Optional.
        </ResponseField>
        <ResponseField name="model_training_expected_total_iteration" type="string">
          Deprecated. Optional.
        </ResponseField>
        <ResponseField name="num_dml_affected_rows" type="string">
          Output only. The number of rows affected by a DML statement. Present only for DML statements INSERT, UPDATE or DELETE. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="object_storage_stats" type="list(object)">
          Output only. Storage and caching statistics per cloud provider for queries over object storage. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="cache_bytes_read" type="string">
              The number of bytes served from cached metadata/results, rather than freshly read. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="cloud_provider" type="string">
              Which cloud provider hosts this external resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="object_storage_bytes_read" type="string">
              The number of bytes read from Cloud Storage objects. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="performance_insights" type="object">
          Performance insights for the job. Optional.
          <Expandable title="properties">
            <ResponseField name="avg_previous_execution_ms" type="string">
              Output only. Average execution ms of previous runs. Indicates the job ran slow compared to previous executions. To find previous executions, use INFORMATION_SCHEMA tables and filter jobs with same query hash. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="stage_performance_change_insights" type="list(object)">
              Output only. Query stage performance insights compared to previous runs, for diagnosing performance regression. Computed by Google Cloud after `ship`.
              <Expandable title="properties">
                <ResponseField name="input_data_change" type="object">
                  How the model's own input data has changed since it was last trained, if at all. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="records_read_diff_percentage" type="number">
                      How much the record count read differs from a prior baseline, as a percentage. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="stage_id" type="string">
                  An identifier for this query stage. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="stage_performance_standalone_insights" type="list(object)">
              Output only. Standalone query stage performance insights, for exploring potential improvements. Computed by Google Cloud after `ship`.
              <Expandable title="properties">
                <ResponseField name="bi_engine_reasons" type="list(object)">
                  Why this query wasn't accelerated by BI Engine, when it wasn't. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="string">
                      A machine-readable code identifying this result or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      A human-readable message describing this event, error, or status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="high_cardinality_joins" type="list(object)">
                  Join(s) in this query involving a high-cardinality key, flagged as a potential performance concern. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="left_rows" type="string">
                      The number of rows on the left side of a join stage. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="output_rows" type="string">
                      The number of rows this stage/job produced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="right_rows" type="string">
                      The number of rows on the right side of a join stage. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="step_index" type="number">
                      This step's own position within its own containing sequence. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="insufficient_shuffle_quota" type="bool">
                  Whether this query was slowed by hitting its own project's shuffle capacity limit. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="partition_skew" type="object">
                  Whether one or more partitions in this stage received disproportionately more data than others, flagged as a potential performance concern. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="skew_sources" type="list(object)">
                      The query stage(s) identified as the source of a detected data skew. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="output_bytes_max" type="string">
                          The maximum output size any single work unit in this stage produced, in bytes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="output_bytes_median" type="string">
                          The median output size across work units in this stage, in bytes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="output_bytes_p95" type="string">
                          The 95th-percentile output size across work units in this stage, in bytes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="stage_id" type="string">
                          An identifier for this query stage. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="slot_contention" type="bool">
                  Whether this stage was slowed by contention for query execution slots. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stage_id" type="string">
                  An identifier for this query stage. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="table_change_insights" type="list(object)">
              Output only. Performance insights for table-level attributes that changed compared to previous runs. Computed by Google Cloud after `ship`.
              <Expandable title="properties">
                <ResponseField name="metadata_cache_not_used_but_used_previously" type="bool">
                  Whether cached metadata was available but deliberately not used for this query, despite being used previously. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="metadata_cache_staleness_insight" type="object">
                  Detail about how stale the cached metadata used for this query was. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="avg_previous_staleness_ms" type="string">
                      The average age, in milliseconds, of the cached metadata used before this refresh. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="staleness_percentage_increase" type="number">
                      How much cache staleness increased relative to a prior baseline, as a percentage. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="table_reference" type="object">
                  A reference to a specific BigQuery table. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dataset_id" type="string">
                      The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="project_id" type="string">
                      The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="table_id" type="string">
                      The BigQuery table ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="query_info" type="object">
          Query optimization information for a QUERY job. Optional.
          <Expandable title="properties">
            <ResponseField name="optimization_details" type="map(any)">
              Output only. Information about query optimizations. Computed by Google Cloud after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="query_plan" type="list(object)">
          Output only. Describes execution plan for the query. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="completed_parallel_inputs" type="string">
              How many parallel input partitions have finished processing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="compute_mode" type="string">
              Whether this query ran using on-demand or reserved compute capacity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="compute_ms_avg" type="string">
              The average compute time per work unit in this stage, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="compute_ms_max" type="string">
              The maximum compute time for any single work unit in this stage, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="compute_ratio_avg" type="number">
              The average fraction of total stage time spent computing, versus reading/writing/waiting. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="compute_ratio_max" type="number">
              The maximum fraction of total stage time any single work unit spent computing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="end_ms" type="string">
              When this stage/step ended, in milliseconds since the job started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="id" type="string">
              An identifier for this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="input_stages" type="list(string)">
              The upstream query stage(s) this stage reads its own input from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="parallel_inputs" type="string">
              The number of input partitions this stage processes in parallel. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="read_ms_avg" type="string">
              The average time spent reading per work unit in this stage, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="read_ms_max" type="string">
              The maximum time spent reading by any single work unit in this stage, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="read_ratio_avg" type="number">
              The average fraction of total stage time spent reading. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="read_ratio_max" type="number">
              The maximum fraction of total stage time any single work unit spent reading. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="records_read" type="string">
              The number of records read. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="records_written" type="string">
              The number of records written. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="shuffle_output_bytes" type="string">
              The total number of bytes this stage wrote to shuffle. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="shuffle_output_bytes_spilled" type="string">
              The number of shuffle output bytes that spilled to disk because they didn't fit in memory. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="slot_ms" type="string">
              The total slot-milliseconds this job consumed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="start_ms" type="string">
              When this stage/step started, in milliseconds since the job started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="status" type="string">
              The current status of this resource or operation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="steps" type="list(object)">
              The ordered step(s) making up this sequence. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kind" type="string">
                  The kind of resource or value this is. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="substeps" type="list(string)">
                  The finer-grained sub-step(s) making up this step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="wait_ms_avg" type="string">
              The average time spent waiting per work unit in this stage, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="wait_ms_max" type="string">
              The maximum time spent waiting by any single work unit in this stage, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="wait_ratio_avg" type="number">
              The average fraction of total stage time spent waiting. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="wait_ratio_max" type="number">
              The maximum fraction of total stage time any single work unit spent waiting. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="write_ms_avg" type="string">
              The average time spent writing per work unit in this stage, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="write_ms_max" type="string">
              The maximum time spent writing by any single work unit in this stage, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="write_ratio_avg" type="number">
              The average fraction of total stage time spent writing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="write_ratio_max" type="number">
              The maximum fraction of total stage time any single work unit spent writing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="referenced_property_graphs" type="list(object)">
          Output only. Referenced property graphs for the job. Queries that reference more than 50 property graphs will not have a complete list. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="project_id" type="string">
              The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="property_graph_id" type="string">
              A reference to the property graph this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="referenced_routines" type="list(object)">
          Output only. Referenced routines for the job. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="project_id" type="string">
              The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="routine_id" type="string">
              A reference to the routine this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="referenced_tables" type="list(object)">
          Output only. Referenced tables for the job. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="dataset_id" type="string">
              The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="project_id" type="string">
              The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="table_id" type="string">
              The BigQuery table ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="reservation_usage" type="list(object)">
          Output only. Job resource usage breakdown by reservation. This field reported misleading information and will no longer be populated. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="slot_ms" type="string">
              The total slot-milliseconds this job consumed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="schema" type="object">
          Schema of a table Optional.
          <Expandable title="properties">
            <ResponseField name="fields" type="list(object)">
              Describes the fields in a table. Optional.
              <Expandable title="properties">
                <ResponseField name="categories" type="object">
                  The set of category labels this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="names" type="list(string)">
                      The literal name(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="collation" type="string">
                  The collation (locale-aware string comparison rule) applied to a `STRING`-typed field, e.g. `und:ci` for case-insensitive comparison, or empty for the default case-sensitive comparison. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="data_governance_tags_info" type="object">
                  Detail about the data governance tag(s) associated with this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="data_governance_tags" type="map(string)">
                      The data governance tag(s) applied to this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="data_policies" type="list(object)">
                  Column-level data governance policies (e.g. dynamic data masking) applied to this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="data_policy_list" type="object">
                  The data policies (e.g. dynamic data masking rules) applied to this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="data_policies" type="list(object)">
                      Column-level data governance policies (e.g. dynamic data masking) applied to this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="default_value_expression" type="string">
                  A SQL expression computing this column's own default value when none is supplied on insert. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="description" type="string">
                  A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fields" type="list(any)">
                  The nested field schemas that make up a `RECORD`-typed field's own sub-structure. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="foreign_type_definition" type="string">
                  The native type definition of this column in the external system it's federated from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="generated_column" type="object">
                  A column whose own value is computed from other columns, rather than stored directly. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="generated_expression_info" type="object">
                      Detail about a generated column's own computation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="asynchronous" type="bool">
                          Whether a generated column's own value is computed asynchronously after write, rather than synchronously. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="generation_expression" type="string">
                          The SQL expression computing this generated column's own value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="stored" type="bool">
                          Whether a generated column's own computed value is physically stored, rather than recomputed on each read. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="generated_mode" type="string">
                      Whether this column's own value is stored, virtual (computed on read), or not generated at all. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="max_length" type="string">
                  The most characters a `string`-typed value may have. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="mode" type="string">
                  Whether this field is `NULLABLE` (may be absent), `REQUIRED` (must always be present), or `REPEATED` (an array of this type). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="policy_tags" type="object">
                  The Data Catalog policy tag(s) governing access to this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="names" type="list(string)">
                      The literal name(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="precision" type="string">
                  For a `NUMERIC`/`BIGNUMERIC` field, the total number of digits it can hold, both before and after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_element_type" type="object">
                  The subtype a `RANGE`-typed field holds -- required when, and only meaningful when, `type` is `RANGE`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="type" type="string">
                      The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="rounding_mode" type="string">
                  How values are rounded when they exceed this `NUMERIC`/`BIGNUMERIC` field's own scale. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="scale" type="string">
                  For a `NUMERIC`/`BIGNUMERIC` field, how many digits are kept after the decimal point. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="timestamp_precision" type="string">
                  The sub-second precision a `TIMESTAMP`-typed field is stored at: `6` (microsecond, the default) or `12` (picosecond). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="foreign_type_info" type="object">
              Metadata about the foreign data type definition such as the system in which the type is defined. Optional.
              <Expandable title="properties">
                <ResponseField name="type_system" type="string">
                  Required. Specifies the system which defines the foreign data type. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="search_statistics" type="object">
          Statistics for a search query. Populated as part of JobStatistics2. Optional.
          <Expandable title="properties">
            <ResponseField name="index_pruning_stats" type="list(object)">
              Search index pruning statistics, one for each base table that has a search index. If a base table does not have a search index or the index does not help with pruning on the base table, then there is no pruning statistics for that table. Optional.
              <Expandable title="properties">
                <ResponseField name="base_table" type="object">
                  A reference to the underlying table a view or materialized view is defined over. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dataset_id" type="string">
                      The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="project_id" type="string">
                      The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="table_id" type="string">
                      The BigQuery table ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="index_id" type="string">
                  A reference to the search index this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="post_index_pruning_parallel_input_count" type="string">
                  The number of input partitions remaining after index-based pruning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="pre_index_pruning_parallel_input_count" type="string">
                  The number of input partitions before index-based pruning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="index_unused_reasons" type="list(object)">
              When `indexUsageMode` is `UNUSED` or `PARTIALLY_USED`, this field explains why indexes were not used in all or part of the search query. If `indexUsageMode` is `FULLY_USED`, this field is not populated. Optional.
              <Expandable title="properties">
                <ResponseField name="base_table" type="object">
                  A reference to the underlying table a view or materialized view is defined over. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dataset_id" type="string">
                      The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="project_id" type="string">
                      The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="table_id" type="string">
                      The BigQuery table ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="code" type="string">
                  A machine-readable code identifying this result or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="index_name" type="string">
                  The name of the search index. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable message describing this event, error, or status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="index_usage_mode" type="string">
              Specifies the index usage mode for the query. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="spark_statistics" type="object">
          Statistics for a BigSpark query. Populated as part of JobStatistics2 Optional.
          <Expandable title="properties">
            <ResponseField name="endpoints" type="map(string)">
              Output only. Endpoints returned from Dataproc. Key list: - history_server_endpoint: A link to Spark job UI. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="gcs_staging_bucket" type="string">
              Output only. The Google Cloud Storage bucket that is used as the default file system by the Spark application. This field is only filled when the Spark procedure uses the invoker security mode. The `gcsStagingBucket` bucket is inferred from the `@@spark_proc_properties.staging_bucket` system variable (if it is provided). Otherwise, BigQuery creates a default staging bucket for the job and returns the bucket name in this field. Example: * `gs://[bucket_name]` Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="kms_key_name" type="string">
              Output only. The Cloud KMS encryption key that is used to protect the resources created by the Spark job. If the Spark procedure uses the invoker security mode, the Cloud KMS encryption key is either inferred from the provided system variable, `@@spark_proc_properties.kms_key_name`, or the default key of the BigQuery job's project (if the CMEK organization policy is enforced). Otherwise, the Cloud KMS key is either inferred from the Spark connection associated with the procedure (if it is provided), or from the default key of the Spark connection's project if the CMEK organization policy is enforced. Example: * `projects/[kms_project_id]/locations/[region]/keyRings/[key_region]/cryptoKeys/[key]` Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="logging_info" type="object">
              Spark job logs can be filtered by these fields in Cloud Logging. Optional.
              <Expandable title="properties">
                <ResponseField name="project_id" type="string">
                  Output only. Project ID where the Spark logs were written. Computed by Google Cloud after `ship`.
                </ResponseField>
                <ResponseField name="resource_type" type="string">
                  Output only. Resource type used for logging. Computed by Google Cloud after `ship`.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="spark_job_id" type="string">
              Output only. Spark job ID if a Spark job is created successfully. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="spark_job_location" type="string">
              Output only. Location where the Spark job is executed. A location is selected by BigQueury for jobs configured to run in a multi-region. Computed by Google Cloud after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="statement_type" type="string">
          Output only. The type of query statement, if valid. Possible values: * `SELECT`: [`SELECT`](https://cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax#select_list) statement. * `ASSERT`: [`ASSERT`](https://cloud.google.com/bigquery/docs/reference/standard-sql/debugging-statements#assert) statement. * `INSERT`: [`INSERT`](https://cloud.google.com/bigquery/docs/reference/standard-sql/dml-syntax#insert_statement) statement. * `UPDATE`: [`UPDATE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/dml-syntax#update_statement) statement. * `DELETE`: [`DELETE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-manipulation-language) statement. * `MERGE`: [`MERGE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-manipulation-language) statement. * `CREATE_TABLE`: [`CREATE TABLE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_table_statement) statement, without `AS SELECT`. * `CREATE_TABLE_AS_SELECT`: [`CREATE TABLE AS SELECT`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_table_statement) statement. * `CREATE_VIEW`: [`CREATE VIEW`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_view_statement) statement. * `CREATE_MODEL`: [`CREATE MODEL`](https://cloud.google.com/bigquery-ml/docs/reference/standard-sql/bigqueryml-syntax-create#create_model_statement) statement. * `CREATE_MATERIALIZED_VIEW`: [`CREATE MATERIALIZED VIEW`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_materialized_view_statement) statement. * `CREATE_FUNCTION`: [`CREATE FUNCTION`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_function_statement) statement. * `CREATE_TABLE_FUNCTION`: [`CREATE TABLE FUNCTION`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_table_function_statement) statement. * `CREATE_PROCEDURE`: [`CREATE PROCEDURE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_procedure) statement. * `CREATE_ROW_ACCESS_POLICY`: [`CREATE ROW ACCESS POLICY`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_row_access_policy_statement) statement. * `CREATE_SCHEMA`: [`CREATE SCHEMA`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_schema_statement) statement. * `CREATE_SNAPSHOT_TABLE`: [`CREATE SNAPSHOT TABLE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_snapshot_table_statement) statement. * `CREATE_SEARCH_INDEX`: [`CREATE SEARCH INDEX`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_search_index_statement) statement. * `DROP_TABLE`: [`DROP TABLE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_table_statement) statement. * `DROP_EXTERNAL_TABLE`: [`DROP EXTERNAL TABLE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_external_table_statement) statement. * `DROP_VIEW`: [`DROP VIEW`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_view_statement) statement. * `DROP_MODEL`: [`DROP MODEL`](https://cloud.google.com/bigquery-ml/docs/reference/standard-sql/bigqueryml-syntax-drop-model) statement. * `DROP_MATERIALIZED_VIEW`: [`DROP MATERIALIZED VIEW`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_materialized_view_statement) statement. * `DROP_FUNCTION` : [`DROP FUNCTION`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_function_statement) statement. * `DROP_TABLE_FUNCTION` : [`DROP TABLE FUNCTION`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_table_function) statement. * `DROP_PROCEDURE`: [`DROP PROCEDURE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_procedure_statement) statement. * `DROP_SEARCH_INDEX`: [`DROP SEARCH INDEX`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_search_index) statement. * `DROP_SCHEMA`: [`DROP SCHEMA`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_schema_statement) statement. * `DROP_SNAPSHOT_TABLE`: [`DROP SNAPSHOT TABLE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_snapshot_table_statement) statement. * `DROP_ROW_ACCESS_POLICY`: [`DROP [ALL] ROW ACCESS POLICY|POLICIES`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#drop_row_access_policy_statement) statement. * `ALTER_TABLE`: [`ALTER TABLE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#alter_table_set_options_statement) statement. * `ALTER_VIEW`: [`ALTER VIEW`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#alter_view_set_options_statement) statement. * `ALTER_MATERIALIZED_VIEW`: [`ALTER MATERIALIZED VIEW`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#alter_materialized_view_set_options_statement) statement. * `ALTER_SCHEMA`: [`ALTER SCHEMA`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#alter_schema_set_options_statement) statement. * `SCRIPT`: [`SCRIPT`](https://cloud.google.com/bigquery/docs/reference/standard-sql/procedural-language). * `TRUNCATE_TABLE`: [`TRUNCATE TABLE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/dml-syntax#truncate_table_statement) statement. * `CREATE_EXTERNAL_TABLE`: [`CREATE EXTERNAL TABLE`](https://cloud.google.com/bigquery/docs/reference/standard-sql/data-definition-language#create_external_table_statement) statement. * `EXPORT_DATA`: [`EXPORT DATA`](https://cloud.google.com/bigquery/docs/reference/standard-sql/other-statements#export_data_statement) statement. * `EXPORT_MODEL`: [`EXPORT MODEL`](https://cloud.google.com/bigquery-ml/docs/reference/standard-sql/bigqueryml-syntax-export-model) statement. * `LOAD_DATA`: [`LOAD DATA`](https://cloud.google.com/bigquery/docs/reference/standard-sql/other-statements#load_data_statement) statement. * `CALL`: [`CALL`](https://cloud.google.com/bigquery/docs/reference/standard-sql/procedural-language#call) statement. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="timeline" type="list(object)">
          Output only. Describes a timeline of job execution. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="active_units" type="string">
              How many work units this stage is currently processing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="completed_units" type="string">
              How many work units this stage has finished processing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="elapsed_ms" type="string">
              How long this stage/step has been running, in milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="estimated_runnable_units" type="string">
              An estimate of how many more work units could run in parallel right now, given available slots. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pending_units" type="string">
              How many work units this stage hasn't started processing yet. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="shuffle_ram_usage_ratio" type="number">
              What fraction of available shuffle memory this stage used. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="total_slot_ms" type="string">
              The total slot-milliseconds consumed across every stage of this job. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="total_bytes_billed" type="string">
          Output only. If the project is configured to use on-demand pricing, then this field contains the total bytes billed for the job. If the project is configured to use flat-rate pricing, then you are not billed for bytes and this field is informational only. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="total_bytes_processed" type="string">
          Output only. Total bytes processed for the job. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="total_bytes_processed_accuracy" type="string">
          Output only. For dry-run jobs, totalBytesProcessed is an estimate and this field specifies the accuracy of the estimate. Possible values can be: UNKNOWN: accuracy of the estimate is unknown. PRECISE: estimate is precise. LOWER_BOUND: estimate is lower bound of what the query would cost. UPPER_BOUND: estimate is upper bound of what the query would cost. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="total_partitions_processed" type="string">
          Output only. Total number of partitions processed from all partitioned tables referenced in the job. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="total_services_sku_slot_ms" type="string">
          Output only. Total slot milliseconds for the job that ran on external services and billed on the services SKU. This field is only populated for jobs that have external service costs, and is the total of the usage for costs whose billing method is `"SERVICES_SKU"`. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="total_slot_ms" type="string">
          Output only. Slot-milliseconds for the job. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="transferred_bytes" type="string">
          Output only. Total bytes transferred for BigQuery Omni queries from the remote cloud back to Google Cloud. This tracks data movement over Google-managed connections (like query results). It doesn't include input data read from the external data lake (for example, S3) because that data stays within the remote cloud. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="undeclared_query_parameters" type="list(object)">
          Output only. GoogleSQL only: list of undeclared query parameters detected during a dry run validation. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="parameter_type" type="object">
              The data type of this routine parameter. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="array_type" type="any">
                  The schema of the element type, when this field is an `ARRAY`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_element_type" type="any">
                  The subtype a `RANGE`-typed field holds -- required when, and only meaningful when, `type` is `RANGE`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="struct_types" type="list(object)">
                  The field(s) making up a `STRUCT`-typed schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="description" type="string">
                      A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="type" type="any">
                      The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="timestamp_precision" type="string">
                  The sub-second precision a `TIMESTAMP`-typed field is stored at: `6` (microsecond, the default) or `12` (picosecond). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="parameter_value" type="object">
              The value passed for this parameter. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="array_values" type="list(any)">
                  The individual element value(s) making up an array value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_value" type="object">
                  A value expressed as a `RANGE` type. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="end" type="any">
                      Where this range ends. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="start" type="any">
                      Where this range starts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="struct_values" type="map(any)">
                  The field value(s) making up a `STRUCT`-typed value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="vector_search_statistics" type="object">
          Statistics for a vector search query. Populated as part of JobStatistics2. Optional.
          <Expandable title="properties">
            <ResponseField name="index_unused_reasons" type="list(object)">
              When `indexUsageMode` is `UNUSED` or `PARTIALLY_USED`, this field explains why indexes were not used in all or part of the vector search query. If `indexUsageMode` is `FULLY_USED`, this field is not populated. Optional.
              <Expandable title="properties">
                <ResponseField name="base_table" type="object">
                  A reference to the underlying table a view or materialized view is defined over. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dataset_id" type="string">
                      The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="project_id" type="string">
                      The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="table_id" type="string">
                      The BigQuery table ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="code" type="string">
                  A machine-readable code identifying this result or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="index_name" type="string">
                  The name of the search index. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable message describing this event, error, or status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="index_usage_mode" type="string">
              Specifies the index usage mode for the query. Optional.
            </ResponseField>
            <ResponseField name="stored_columns_usages" type="list(object)">
              Specifies the usage of stored columns in the query when stored columns are used in the query. Optional.
              <Expandable title="properties">
                <ResponseField name="base_table" type="object">
                  A reference to the underlying table a view or materialized view is defined over. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dataset_id" type="string">
                      The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="project_id" type="string">
                      The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="table_id" type="string">
                      The BigQuery table ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="is_query_accelerated" type="bool">
                  Whether this query was accelerated by BI Engine. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stored_columns_unused_reasons" type="list(object)">
                  Why a search index's own stored column(s) weren't used to answer this query. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="string">
                      A machine-readable code identifying this result or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      A human-readable message describing this event, error, or status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="uncovered_columns" type="list(string)">
                      The column(s) a search index doesn't cover, requiring a fallback table scan for queries referencing them. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="quota_deferments" type="list(string)">
      Output only. Quotas which delayed this job's start time. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="reservation_group_path" type="list(string)">
      Output only. The reservation group path of the reservation assigned to this job. This field has a limit of 10 nested reservation groups. This is to maintain consistency between reservatins info schema and jobs info schema. The first reservation group is the root reservation group and the last is the leaf or lowest level reservation group. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="reservation_id" type="string">
      Output only. Name of the primary reservation assigned to this job. Note that this could be different than reservations reported in the reservation usage field if parent reservations were used to execute this job. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="reservation_usage" type="list(object)">
      Output only. Job resource usage breakdown by reservation. This field reported misleading information and will no longer be populated. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The field's own name, unique within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="slot_ms" type="string">
          The total slot-milliseconds this job consumed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="row_level_security_statistics" type="object">
      Statistics for row-level security. Optional.
      <Expandable title="properties">
        <ResponseField name="row_level_security_applied" type="bool">
          Whether any accessed data was protected by row access policies. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="script_statistics" type="object">
      Job statistics specific to the child job of a script. Optional.
      <Expandable title="properties">
        <ResponseField name="evaluation_kind" type="string">
          Whether this child job was a statement or expression. Optional.
        </ResponseField>
        <ResponseField name="stack_frames" type="list(object)">
          Stack trace showing the line/column/procedure name of each frame on the stack at the point where the current evaluation happened. The leaf frame is first, the primary script is last. Never empty. Optional.
          <Expandable title="properties">
            <ResponseField name="end_column" type="number">
              The column position this range ends at. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="end_line" type="number">
              The line number this range ends at. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="procedure_id" type="string">
              A reference to the stored procedure this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="start_column" type="number">
              The column position this range starts at. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="start_line" type="number">
              The line number this range starts at. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="text" type="string">
              The plain-text content. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="session_info" type="object">
      [Preview] Information related to sessions. Optional.
      <Expandable title="properties">
        <ResponseField name="session_id" type="string">
          Output only. The id of the session. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Output only. Start time of this job, in milliseconds since the epoch. This field will be present when the job transitions from the PENDING state to either RUNNING or DONE. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="total_bytes_processed" type="string">
      Output only. Total bytes processed for the job. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="total_slot_ms" type="string">
      Output only. Slot-milliseconds for the job. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="transaction_info" type="object">
      [Alpha] Information of a multi-statement transaction. Optional.
      <Expandable title="properties">
        <ResponseField name="transaction_id" type="string">
          Output only. [Alpha] Id of the transaction. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="object">
  The current status of this resource or operation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="error_result" type="object">
      Error details. Optional.
      <Expandable title="properties">
        <ResponseField name="debug_info" type="string">
          Debugging information. This property is internal to Google and should not be used. Optional.
        </ResponseField>
        <ResponseField name="location" type="string">
          Specifies where the error occurred, if present. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of the error. Optional.
        </ResponseField>
        <ResponseField name="reason" type="string">
          A short error code that summarizes the error. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="errors" type="list(object)">
      Output only. The first errors encountered during the running of the job. The final message includes the number of errors that caused the process to stop. Errors here do not necessarily mean that the job has not completed or was unsuccessful. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="debug_info" type="string">
          Additional detail useful for debugging this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="location" type="string">
          The Google Cloud region or resource location this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable message describing this event, error, or status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reason" type="string">
          A machine-readable reason code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. Running state of the job. Valid states include 'PENDING', 'RUNNING', and 'DONE'. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_email" type="string">
  Output only. Email address of the user who ran the job. Optional; if omitted, computed by Google Cloud.
</ResponseField>
