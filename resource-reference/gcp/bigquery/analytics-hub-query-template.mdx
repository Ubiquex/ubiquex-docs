---
title: "google_bigquery_analytics_hub_query_template"
description: "Real, generated bindings for `google_bigquery_analytics_hub_query_template`."
---

`google_bigquery_analytics_hub_query_template` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigquery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigquery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own analytics hub query template"})

    		ubx.Resource(bigquery.AnalyticsHubQueryTemplate, "ci-runner", bigquery.AnalyticsHubQueryTemplateConfig{
    			DataExchangeId:  "example",
    			DisplayName:     "example-display-name",
    			Location:        "example",
    			QueryTemplateId: "example",
    			Description:     "Managed by ubx.",
    			Documentation:   "example",
    			PrimaryContact:  "example",
    			Submit:          true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnalyticsHubQueryTemplate } from "jsr:@ubx/sdk-google/google/bigquery/analytics_hub_query_template";

    export default stack("payments", () => {
      intent({ summary: "payments own analytics hub query template" });

      resource(AnalyticsHubQueryTemplate, "ci-runner", {
        dataExchangeId: "example",
        displayName: "example-display-name",
        location: "example",
        queryTemplateId: "example",
        description: "Managed by ubx.",
        documentation: "example",
        primaryContact: "example",
        submit: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigquery import AnalyticsHubQueryTemplate, AnalyticsHubQueryTemplateConfig

    def describe():
        ubx.intent("payments own analytics hub query template")

        ubx.resource(AnalyticsHubQueryTemplate, "ci-runner", AnalyticsHubQueryTemplateConfig(
            data_exchange_id="example",
            display_name="example-display-name",
            location="example",
            query_template_id="example",
            description="Managed by ubx.",
            documentation="example",
            primary_contact="example",
            submit=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigquery_analytics_hub_query_template, with data
    exchange id example, display name example-display-name, location
    example, query template id example, description Managed by ubx.,
    documentation example, primary contact example, submit true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_exchange_id" type="string" required>
  The ID of the data exchange. Must contain only Unicode letters, numbers (0-9), underscores (_). Should not use characters that require URL-escaping, or characters outside of ASCII, spaces. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  This field uses a custom implementation please refer to documentation under /hashicorp/terraform-provider-google-beta/website/docs/r/bigquery_analytics_hub_query_template.html.markdown for specifics Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Short description of the QueryTemplate. The description must not contain Unicode non-characters and C0 and C1 control codes except tabs, new lines, carriage returns, and page breaks. Default value is an empty string. Max length: 2000 bytes. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Human-readable display name of the QueryTemplate. The display name must contain only Unicode letters, numbers (0-9), underscores (_), dashes (-), spaces ( ), ampersands (&) and can't start or end with spaces. Default value is an empty string. Required.
</ResponseField>

<ResponseField name="documentation" type="string">
  Documentation describing the QueryTemplate. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The name of the location this data exchange query template. Required.
</ResponseField>

<ResponseField name="primary_contact" type="string">
  Email or URL of the primary point of contact of the QueryTemplate. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="query_template_id" type="string" required>
  Unique QueryTemplate ID. Required.
</ResponseField>

<ResponseField name="routine" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="definition_body" type="string">
    </ResponseField>
    <ResponseField name="routine_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="submit" type="bool">
  If set to 'true', the QueryTemplate will be submitted for approval and cannot be updated afterwards. This is a one-time action. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Timestamp when the QueryTemplate was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  This field uses a custom implementation please refer to documentation under /hashicorp/terraform-provider-google-beta/website/docs/r/bigquery_analytics_hub_query_template.html.markdown for specifics Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the QueryTemplate. e.g. 'projects/myproject/locations/us/dataExchanges/123/queryTemplates/456' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="routine" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="definition_body" type="string">
    </ResponseField>
    <ResponseField name="routine_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  The QueryTemplate lifecycle state. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Timestamp when the QueryTemplate was last modified. Computed by GCP after `ship`.
</ResponseField>
