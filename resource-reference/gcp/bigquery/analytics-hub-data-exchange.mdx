---
title: "google_bigquery_analytics_hub_data_exchange"
description: "Real, generated bindings for `google_bigquery_analytics_hub_data_exchange`."
---

`google_bigquery_analytics_hub_data_exchange` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigquery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigquery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own analytics hub data exchange"})

    		ubx.Resource(bigquery.AnalyticsHubDataExchange, "ci-runner", bigquery.AnalyticsHubDataExchangeConfig{
    			DataExchangeId:                 "example",
    			DisplayName:                    "example-display-name",
    			Location:                       "example",
    			Description:                    "Managed by ubx.",
    			Documentation:                  "example",
    			Icon:                           "example",
    			LogLinkedDatasetQueryUserEmail: true,
    			PrimaryContact:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnalyticsHubDataExchange } from "jsr:@ubx/sdk-google/google/bigquery/analytics_hub_data_exchange";

    export default stack("payments", () => {
      intent({ summary: "payments own analytics hub data exchange" });

      resource(AnalyticsHubDataExchange, "ci-runner", {
        dataExchangeId: "example",
        displayName: "example-display-name",
        location: "example",
        description: "Managed by ubx.",
        documentation: "example",
        icon: "example",
        logLinkedDatasetQueryUserEmail: true,
        primaryContact: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigquery import AnalyticsHubDataExchange, AnalyticsHubDataExchangeConfig

    def describe():
        ubx.intent("payments own analytics hub data exchange")

        ubx.resource(AnalyticsHubDataExchange, "ci-runner", AnalyticsHubDataExchangeConfig(
            data_exchange_id="example",
            display_name="example-display-name",
            location="example",
            description="Managed by ubx.",
            documentation="example",
            icon="example",
            log_linked_dataset_query_user_email=True,
            primary_contact="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigquery_analytics_hub_data_exchange, with data
    exchange id example, display name example-display-name, location
    example, description Managed by ubx., documentation example, icon
    example, log linked dataset query user email true, primary
    contact example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_exchange_id" type="string" required>
  The ID of the data exchange. Must contain only Unicode letters, numbers (0-9), underscores (_). Should not use characters that require URL-escaping, or characters outside of ASCII, spaces. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Description of the data exchange. Optional.
</ResponseField>

<ResponseField name="discovery_type" type="string">
  Type of discovery on the discovery page for all the listings under this exchange. Cannot be set for a Data Clean Room. Updating this field also updates (overwrites) the discoveryType field for all the listings under this exchange. Possible values: ["DISCOVERY_TYPE_PRIVATE", "DISCOVERY_TYPE_PUBLIC"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Human-readable display name of the data exchange. The display name must contain only Unicode letters, numbers (0-9), underscores (_), dashes (-), spaces ( ), and must not start or end with spaces. Required.
</ResponseField>

<ResponseField name="documentation" type="string">
  Documentation describing the data exchange. Optional.
</ResponseField>

<ResponseField name="icon" type="string">
  Base64 encoded image representing the data exchange. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The name of the location this data exchange. Required.
</ResponseField>

<ResponseField name="log_linked_dataset_query_user_email" type="bool">
  If true, subscriber email logging is enabled and all queries on the linked dataset will log the email address of the querying user. Once enabled, this setting cannot be turned off. Optional.
</ResponseField>

<ResponseField name="primary_contact" type="string">
  Email or URL of the primary point of contact of the data exchange. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="sharing_environment_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="dcr_exchange_config" type="list(object)">
    </ResponseField>
    <ResponseField name="default_exchange_config" type="list(object)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="discovery_type" type="string">
  Type of discovery on the discovery page for all the listings under this exchange. Cannot be set for a Data Clean Room. Updating this field also updates (overwrites) the discoveryType field for all the listings under this exchange. Possible values: ["DISCOVERY_TYPE_PRIVATE", "DISCOVERY_TYPE_PUBLIC"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="listing_count" type="number">
  Number of listings contained in the data exchange. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the data exchange, for example: "projects/myproject/locations/US/dataExchanges/123" Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="sharing_environment_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="dcr_exchange_config" type="list(object)">
    </ResponseField>
    <ResponseField name="default_exchange_config" type="list(object)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
