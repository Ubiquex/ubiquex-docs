---
title: "google_bigquery_routine_iam_policy"
description: "Real, generated bindings for `google_bigquery_routine_iam_policy`."
---

`google_bigquery_routine_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigquery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigquery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own routine iam policy"})

    		ubx.Resource(bigquery.RoutineIamPolicy, "ci-runner", bigquery.RoutineIamPolicyConfig{
    			DatasetId:  "example",
    			PolicyData: "example",
    			RoutineId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoutineIamPolicy } from "jsr:@ubx/sdk-google/google/bigquery/routine_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own routine iam policy" });

      resource(RoutineIamPolicy, "ci-runner", {
        datasetId: "example",
        policyData: "example",
        routineId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigquery import RoutineIamPolicy, RoutineIamPolicyConfig

    def describe():
        ubx.intent("payments own routine iam policy")

        ubx.resource(RoutineIamPolicy, "ci-runner", RoutineIamPolicyConfig(
            dataset_id="example",
            policy_data="example",
            routine_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigquery_routine_iam_policy, with dataset id
    example, policy data example, routine id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dataset_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="routine_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
