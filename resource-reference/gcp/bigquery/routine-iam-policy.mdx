---
title: "google_bigquery_routine_iam_policy"
description: "Real, generated bindings for `google_bigquery_routine_iam_policy`."
---

`google_bigquery_routine_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigquery"
    )

    cfg := bigquery.RoutineIamPolicyConfig{
        DatasetId: "example",
        PolicyData: "example",
        RoutineId: "example",
    }
    ubx.Resource(bigquery.RoutineIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RoutineIamPolicy } from
      "jsr:@ubx/sdk-google/google/bigquery/routine_iam_policy";

    resource(RoutineIamPolicy, "example", {
      datasetId: "example",
      policyData: "example",
      routineId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.bigquery import (
        RoutineIamPolicy,
        RoutineIamPolicyConfig,
    )

    cfg = RoutineIamPolicyConfig(
        dataset_id="example",
        policy_data="example",
        routine_id="example",
    )
    ubx.resource(RoutineIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_bigquery_routine_iam_policy with dataset_id,
policy_data, routine_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dataset_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="routine_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
