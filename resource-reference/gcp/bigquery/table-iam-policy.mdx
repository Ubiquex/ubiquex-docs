---
title: "google_bigquery_table_iam_policy"
description: "Real, generated bindings for `google_bigquery_table_iam_policy`."
---

`google_bigquery_table_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/bigquery"
    )

    cfg := bigquery.TableIamPolicyConfig{
        DatasetId: "example",
        PolicyData: "example",
        TableId: "example",
    }
    ubx.Resource(bigquery.TableIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TableIamPolicy } from
      "jsr:@ubx/sdk-google/google/bigquery/table_iam_policy";

    resource(TableIamPolicy, "example", {
      datasetId: "example",
      policyData: "example",
      tableId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.bigquery import (
        TableIamPolicy,
        TableIamPolicyConfig,
    )

    cfg = TableIamPolicyConfig(
        dataset_id="example",
        policy_data="example",
        table_id="example",
    )
    ubx.resource(TableIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_bigquery_table_iam_policy with dataset_id,
policy_data, table_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dataset_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="table_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
