---
title: "google_ces_toolset"
description: "`google_ces_toolset` groups related tool(s) together for a Customer Engagement Suite agent, letting a set of capabilities be attached and managed together."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	ces "github.com/ubiquex/ubx-sdk-google/sdk/go/google/ces"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own toolset"})

    		ubx.Resource(ces.Toolset, "example", ces.ToolsetConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Toolset } from "@ubx/sdk-google/google/ces/toolset";

    export default stack("example", () => {
      intent({ summary: "example own toolset" });

      resource(Toolset, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.ces import Toolset, ToolsetConfig

    def describe():
        ubx.intent("example own toolset")

        ubx.resource(Toolset, "example", ToolsetConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_ces_toolset called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="connector_toolset" type="object">
  A toolset that generates tools from an Integration Connectors Connection. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="auth_config" type="object">
      End-user authentication configuration used for Connection calls. The field values must be the names of context variables in the format `$context.variables.`. Optional.
      <Expandable title="properties">
        <ResponseField name="oauth2_auth_code_config" type="object">
          Oauth 2.0 Authorization Code authentication configuration. Optional.
          <Expandable title="properties">
            <ResponseField name="oauth_token" type="string">
              Required. Oauth token parameter name to pass through. Must be in the format `$context.variables.`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="oauth2_jwt_bearer_config" type="object">
          JWT Profile Oauth 2.0 Authorization Grant authentication configuration. Optional.
          <Expandable title="properties">
            <ResponseField name="client_key" type="string">
              Required. Client parameter name to pass through. Must be in the format `$context.variables.`. Optional.
            </ResponseField>
            <ResponseField name="issuer" type="string">
              Required. Issuer parameter name to pass through. Must be in the format `$context.variables.`. Optional.
            </ResponseField>
            <ResponseField name="subject" type="string">
              Required. Subject parameter name to pass through. Must be in the format `$context.variables.`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="connection" type="string">
      Required. The full resource name of the referenced Integration Connectors Connection. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/connections/&#123;connection&#125;` Optional.
    </ResponseField>
    <ResponseField name="connector_actions" type="list(object)">
      Required. The list of connector actions/entity operations to generate tools for. Optional.
      <Expandable title="properties">
        <ResponseField name="connection_action_id" type="string">
          The identifier of the connector action this tool invokes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="entity_operation" type="object">
          A structured create/update/delete action performed against an entity in the connected external system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="entity_id" type="string">
              The identifier of the entity this operation applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operation" type="string">
              Which action this performs, e.g. create, update, or delete. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="input_fields" type="list(string)">
          The input field(s) this tool or operation accepts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="output_fields" type="list(string)">
          The output field(s) this tool or operation returns. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Timestamp when the toolset was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. The description of the toolset. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. The display name of the toolset. Must be unique within the same app. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  ETag used to ensure the object hasn't changed during a read-modify-write operation. If the etag is empty, the update will overwrite any concurrent changes. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="execution_type" type="string">
  Optional. The execution type of the tools in the toolset. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="mcp_toolset" type="object">
  A toolset that contains a list of tools that are offered by the MCP server. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="api_authentication" type="object">
      Authentication information required for API calls. Optional.
      <Expandable title="properties">
        <ResponseField name="api_key_config" type="object">
          Configurations for authentication with API key. Optional.
          <Expandable title="properties">
            <ResponseField name="api_key_secret_version" type="string">
              Required. The name of the SecretManager secret version resource storing the API key. Format: `projects/&#123;project&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125;` Note: You should grant `roles/secretmanager.secretAccessor` role to the CES service agent `service-@gcp-sa-ces.iam.gserviceaccount.com`. Optional.
            </ResponseField>
            <ResponseField name="key_name" type="string">
              Required. The parameter name or the header name of the API key. E.g., If the API request is "https://example.com/act?X-Api-Key=", "X-Api-Key" would be the parameter name. Optional.
            </ResponseField>
            <ResponseField name="request_location" type="string">
              Required. Key location in the request. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="bearer_token_config" type="object">
          Configurations for authentication with a bearer token. Optional.
          <Expandable title="properties">
            <ResponseField name="token" type="string">
              Required. The bearer token. Must be in the format `$context.variables.`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="oauth_config" type="object">
          Configurations for authentication with OAuth. Optional.
          <Expandable title="properties">
            <ResponseField name="client_id" type="string">
              Required. The client ID from the OAuth provider. Optional.
            </ResponseField>
            <ResponseField name="client_secret_version" type="string">
              Required. The name of the SecretManager secret version resource storing the client secret. Format: `projects/&#123;project&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125;` Note: You should grant `roles/secretmanager.secretAccessor` role to the CES service agent `service-@gcp-sa-ces.iam.gserviceaccount.com`. Optional.
            </ResponseField>
            <ResponseField name="oauth_grant_type" type="string">
              Required. OAuth grant types. Optional.
            </ResponseField>
            <ResponseField name="scopes" type="list(string)">
              Optional. The OAuth scopes to grant. Optional.
            </ResponseField>
            <ResponseField name="token_endpoint" type="string">
              Required. The token endpoint in the OAuth provider to exchange for an access token. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_account_auth_config" type="object">
          Configurations for authentication using a custom service account. Optional.
          <Expandable title="properties">
            <ResponseField name="scopes" type="list(string)">
              Optional. The OAuth scopes to grant. If not specified, the default scope `https://www.googleapis.com/auth/cloud-platform` is used. Optional.
            </ResponseField>
            <ResponseField name="service_account" type="string">
              Required. The email address of the service account used for authentication. CES uses this service account to exchange an access token and the access token is then sent in the `Authorization` header of the request. The service account must have the `roles/iam.serviceAccountTokenCreator` role granted to the CES service agent `service-@gcp-sa-ces.iam.gserviceaccount.com`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_agent_id_token_auth_config" type="any">
          Configurations for authentication with [ID token](https://cloud.google.com/docs/authentication/token-types#id) generated from service agent. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_headers" type="map(string)">
      Optional. The custom headers to send in the request to the MCP server. The values must be in the format `$context.variables.` and can be set in the session variables. See https://docs.cloud.google.com/customer-engagement-ai/conversational-agents/ps/tool/open-api#openapi-injection for more details. Optional.
    </ResponseField>
    <ResponseField name="server_address" type="string">
      Required. The address of the MCP server, for example, "https://example.com/mcp/". If the server is built with the MCP SDK, the url should be suffixed with "/mcp/". Only Streamable HTTP transport based servers are supported. See https://modelcontextprotocol.io/specification/2025-03-26/basic/transports#streamable-http for more details. Optional.
    </ResponseField>
    <ResponseField name="service_directory_config" type="object">
      Configuration for tools using Service Directory. Optional.
      <Expandable title="properties">
        <ResponseField name="service" type="string">
          Required. The name of [Service Directory](https://cloud.google.com/service-directory) service. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/namespaces/&#123;namespace&#125;/services/&#123;service&#125;`. Location of the service directory must be the same as the location of the app. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tls_config" type="object">
      The TLS configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="ca_certs" type="list(object)">
          Required. Specifies a list of allowed custom CA certificates for HTTPS verification. Optional.
          <Expandable title="properties">
            <ResponseField name="cert" type="string">
              A TLS certificate. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_name" type="string">
              A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tool_overrides" type="list(object)">
      Optional. Overrides for individual tools within this toolset. This allows overriding specific details like descriptions, names, or pinning the tools' states so they aren't fully dynamic. Optional.
      <Expandable title="properties">
        <ResponseField name="description_override" type="string">
          A caller-chosen description presented to the model in place of this item's own default description. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name_override" type="string">
          A caller-chosen name presented to the model in place of this item's own real, underlying name. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="snapshot" type="object">
          A frozen copy of this resource's own content at a specific point in time. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="input_schema" type="object">
              The schema describing this tool's own expected input. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="additional_properties" type="any">
                  Whether -- or, given a schema, how -- an `object`-typed value may carry properties beyond those named in `properties`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="any_of" type="list(any)">
                  A list of schemas where a value must satisfy at least one to be valid. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="default" type="any">
                  The value used when this field is left unset. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="defs" type="map(any)">
                  Named sub-schemas defined once and referenced elsewhere in this schema via `ref`, avoiding repetition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="description" type="string">
                  A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="enum" type="list(string)">
                  The exact, closed set of values a `string`-typed schema allows. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="items" type="any">
                  The schema every element of an `array`-typed value must satisfy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_items" type="string">
                  The most elements an `array`-typed value may have. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="maximum" type="number">
                  The largest value a `number`/`integer`-typed value may take. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min_items" type="string">
                  The fewest elements an `array`-typed value may have. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="minimum" type="number">
                  The smallest value a `number`/`integer`-typed value may take. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="nullable" type="bool">
                  Whether `null` is a valid value for this otherwise-typed field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="prefix_items" type="list(any)">
                  The JSON Schema for each fixed-position element at the start of a tuple-shaped array, before its own variadic `items` schema applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="properties" type="map(any)">
                  The named sub-schemas an `object`-typed value's own fields must each satisfy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ref" type="string">
                  A reference to a schema defined under `defs`, by name. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="required" type="list(string)">
                  The list of `properties` keys that must be present on an `object`-typed value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="title" type="string">
                  The resource's own formal name -- a more official variant of `display_name`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="unique_items" type="bool">
                  Whether every element of an array-typed value must be distinct. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="output_schema" type="object">
              The schema describing this tool's own expected output. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="additional_properties" type="any">
                  Whether -- or, given a schema, how -- an `object`-typed value may carry properties beyond those named in `properties`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="any_of" type="list(any)">
                  A list of schemas where a value must satisfy at least one to be valid. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="default" type="any">
                  The value used when this field is left unset. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="defs" type="map(any)">
                  Named sub-schemas defined once and referenced elsewhere in this schema via `ref`, avoiding repetition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="description" type="string">
                  A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="enum" type="list(string)">
                  The exact, closed set of values a `string`-typed schema allows. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="items" type="any">
                  The schema every element of an `array`-typed value must satisfy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_items" type="string">
                  The most elements an `array`-typed value may have. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="maximum" type="number">
                  The largest value a `number`/`integer`-typed value may take. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min_items" type="string">
                  The fewest elements an `array`-typed value may have. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="minimum" type="number">
                  The smallest value a `number`/`integer`-typed value may take. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="nullable" type="bool">
                  Whether `null` is a valid value for this otherwise-typed field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="prefix_items" type="list(any)">
                  The JSON Schema for each fixed-position element at the start of a tuple-shaped array, before its own variadic `items` schema applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="properties" type="map(any)">
                  The named sub-schemas an `object`-typed value's own fields must each satisfy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ref" type="string">
                  A reference to a schema defined under `defs`, by name. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="required" type="list(string)">
                  The list of `properties` keys that must be present on an `object`-typed value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="title" type="string">
                  The resource's own formal name -- a more official variant of `display_name`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="unique_items" type="bool">
                  Whether every element of an array-typed value must be distinct. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tool" type="string">
          A reference to a single tool. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The unique identifier of the toolset. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/toolsets/&#123;toolset&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="open_api_toolset" type="object">
  A toolset that contains a list of tools that are defined by an OpenAPI schema. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="api_authentication" type="object">
      Authentication information required for API calls. Optional.
      <Expandable title="properties">
        <ResponseField name="api_key_config" type="object">
          Configurations for authentication with API key. Optional.
          <Expandable title="properties">
            <ResponseField name="api_key_secret_version" type="string">
              Required. The name of the SecretManager secret version resource storing the API key. Format: `projects/&#123;project&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125;` Note: You should grant `roles/secretmanager.secretAccessor` role to the CES service agent `service-@gcp-sa-ces.iam.gserviceaccount.com`. Optional.
            </ResponseField>
            <ResponseField name="key_name" type="string">
              Required. The parameter name or the header name of the API key. E.g., If the API request is "https://example.com/act?X-Api-Key=", "X-Api-Key" would be the parameter name. Optional.
            </ResponseField>
            <ResponseField name="request_location" type="string">
              Required. Key location in the request. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="bearer_token_config" type="object">
          Configurations for authentication with a bearer token. Optional.
          <Expandable title="properties">
            <ResponseField name="token" type="string">
              Required. The bearer token. Must be in the format `$context.variables.`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="oauth_config" type="object">
          Configurations for authentication with OAuth. Optional.
          <Expandable title="properties">
            <ResponseField name="client_id" type="string">
              Required. The client ID from the OAuth provider. Optional.
            </ResponseField>
            <ResponseField name="client_secret_version" type="string">
              Required. The name of the SecretManager secret version resource storing the client secret. Format: `projects/&#123;project&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125;` Note: You should grant `roles/secretmanager.secretAccessor` role to the CES service agent `service-@gcp-sa-ces.iam.gserviceaccount.com`. Optional.
            </ResponseField>
            <ResponseField name="oauth_grant_type" type="string">
              Required. OAuth grant types. Optional.
            </ResponseField>
            <ResponseField name="scopes" type="list(string)">
              Optional. The OAuth scopes to grant. Optional.
            </ResponseField>
            <ResponseField name="token_endpoint" type="string">
              Required. The token endpoint in the OAuth provider to exchange for an access token. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_account_auth_config" type="object">
          Configurations for authentication using a custom service account. Optional.
          <Expandable title="properties">
            <ResponseField name="scopes" type="list(string)">
              Optional. The OAuth scopes to grant. If not specified, the default scope `https://www.googleapis.com/auth/cloud-platform` is used. Optional.
            </ResponseField>
            <ResponseField name="service_account" type="string">
              Required. The email address of the service account used for authentication. CES uses this service account to exchange an access token and the access token is then sent in the `Authorization` header of the request. The service account must have the `roles/iam.serviceAccountTokenCreator` role granted to the CES service agent `service-@gcp-sa-ces.iam.gserviceaccount.com`. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_agent_id_token_auth_config" type="any">
          Configurations for authentication with [ID token](https://cloud.google.com/docs/authentication/token-types#id) generated from service agent. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ignore_unknown_fields" type="bool">
      Optional. If true, the agent will ignore unknown fields in the API response for all operations defined in the OpenAPI schema. Optional.
    </ResponseField>
    <ResponseField name="open_api_schema" type="string">
      Required. The OpenAPI schema of the toolset. Optional.
    </ResponseField>
    <ResponseField name="service_directory_config" type="object">
      Configuration for tools using Service Directory. Optional.
      <Expandable title="properties">
        <ResponseField name="service" type="string">
          Required. The name of [Service Directory](https://cloud.google.com/service-directory) service. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/namespaces/&#123;namespace&#125;/services/&#123;service&#125;`. Location of the service directory must be the same as the location of the app. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tls_config" type="object">
      The TLS configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="ca_certs" type="list(object)">
          Required. Specifies a list of allowed custom CA certificates for HTTPS verification. Optional.
          <Expandable title="properties">
            <ResponseField name="cert" type="string">
              A TLS certificate. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_name" type="string">
              A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="url" type="string">
      Optional. The server URL of the Open API schema. This field is only set in toolsets in the environment dependencies during the export process if the schema contains a server url. During the import process, if this url is present in the environment dependencies and the schema has the $env_var placeholder, it will replace the placeholder in the schema. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeout" type="string">
  Optional. The timeout for the toolset execution. If not set, the default timeout is 30 seconds for `SYNCHRONOUS` toolsets and 60 seconds for `ASYNCHRONOUS` toolsets. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tool_fake_config" type="object">
  Configuration for tool behavior in fake mode. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code_block" type="object">
      A code block to be executed instead of a real tool call. Optional.
      <Expandable title="properties">
        <ResponseField name="python_code" type="string">
          Required. Python code which will be invoked in tool fake mode. Expected Python function signature - To catch all tool calls: def fake_tool_call(tool: Tool, input: dict[str, Any], callback_context: CallbackContext) -&gt; Optional[dict[str, Any]]: To catch a specific tool call: def fake_&#123;tool_id&#125;(tool: Tool, input: dict[str, Any], callback_context: CallbackContext) -&gt; Optional[dict[str, Any]]: If the function returns None, the real tool will be invoked instead. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enable_fake_mode" type="bool">
      Optional. Whether the tool is using fake mode. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when the toolset was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
