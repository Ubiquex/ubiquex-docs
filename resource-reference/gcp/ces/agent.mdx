---
title: "google_ces_agent"
description: "Real, generated bindings for `google_ces_agent`."
---

`google_ces_agent` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	ces "github.com/ubiquex/ubx-sdk-google/sdk/go/google/ces"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own agent"})

    		ubx.Resource(ces.Agent, "ci-runner", ces.AgentConfig{
    			App:         "example",
    			DisplayName: "example-display-name",
    			Location:    "example",
    			AgentId:     "example",
    			ChildAgents: []string{"example"},
    			Description: "Managed by ubx.",
    			Guardrails:  []string{"example"},
    			Instruction: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Agent } from "jsr:@ubx/sdk-google/google/ces/agent";

    export default stack("payments", () => {
      intent({ summary: "payments own agent" });

      resource(Agent, "ci-runner", {
        app: "example",
        displayName: "example-display-name",
        location: "example",
        agentId: "example",
        childAgents: ["example"],
        description: "Managed by ubx.",
        guardrails: ["example"],
        instruction: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.ces import Agent, AgentConfig

    def describe():
        ubx.intent("payments own agent")

        ubx.resource(Agent, "ci-runner", AgentConfig(
            app="example",
            display_name="example-display-name",
            location="example",
            agent_id="example",
            child_agents=["example"],
            description="Managed by ubx.",
            guardrails=["example"],
            instruction="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_ces_agent, with app example, display name
    example-display-name, location example, agent id example, child
    agents a set value, description Managed by ubx., guardrails a set
    value, instruction example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="after_agent_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="after_model_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="after_tool_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="agent_id" type="string">
  The ID to use for the agent, which will become the final component of the agent's resource name. If not provided, a unique ID will be automatically assigned for the agent. Optional.
</ResponseField>

<ResponseField name="app" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="before_agent_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="before_model_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="before_tool_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="child_agents" type="list(string)">
  List of child agents in the agent tree. Format: 'projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/agents/&#123;agent&#125;' Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Human-readable description of the agent. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Display name of the agent. Required.
</ResponseField>

<ResponseField name="guardrails" type="list(string)">
  List of guardrails for the agent. Format: 'projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/guardrails/&#123;guardrail&#125;' Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instruction" type="string">
  Instructions for the LLM model to guide the agent's behavior. Optional.
</ResponseField>

<ResponseField name="llm_agent" type="list(object)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="model_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="model" type="string">
    </ResponseField>
    <ResponseField name="temperature" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="remote_dialogflow_agent" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="agent" type="string" required>
    </ResponseField>
    <ResponseField name="environment_id" type="string">
    </ResponseField>
    <ResponseField name="flow_id" type="string" required>
    </ResponseField>
    <ResponseField name="input_variable_mapping" type="map(string)">
    </ResponseField>
    <ResponseField name="output_variable_mapping" type="map(string)">
    </ResponseField>
    <ResponseField name="respect_response_interruption_settings" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tools" type="list(string)">
  List of available tools for the agent. Format: 'projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/tools/&#123;tool&#125;' Optional.
</ResponseField>

<ResponseField name="toolsets" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="tool_ids" type="list(string)">
    </ResponseField>
    <ResponseField name="toolset" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="after_agent_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="after_model_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="after_tool_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="before_agent_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="before_model_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="before_tool_callbacks" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="python_code" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Timestamp when the agent was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  Etag used to ensure the object hasn't changed during a read-modify-write operation. If the etag is empty, the update will overwrite any concurrent changes. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="generated_summary" type="string">
  If the agent is generated by the LLM assistant, this field contains a descriptive summary of the generation. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="llm_agent" type="list(object)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="model_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="model" type="string">
    </ResponseField>
    <ResponseField name="temperature" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The unique identifier of the agent. Format: 'projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/agents/&#123;agent&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="remote_dialogflow_agent" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="agent" type="string" required>
    </ResponseField>
    <ResponseField name="environment_id" type="string">
    </ResponseField>
    <ResponseField name="flow_id" type="string" required>
    </ResponseField>
    <ResponseField name="input_variable_mapping" type="map(string)">
    </ResponseField>
    <ResponseField name="output_variable_mapping" type="map(string)">
    </ResponseField>
    <ResponseField name="respect_response_interruption_settings" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="toolsets" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="tool_ids" type="list(string)">
    </ResponseField>
    <ResponseField name="toolset" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Timestamp when the agent was last updated. Computed by GCP after `ship`.
</ResponseField>
