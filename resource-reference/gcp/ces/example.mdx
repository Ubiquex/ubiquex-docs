---
title: "google_ces_example"
description: "Real, generated bindings for `google_ces_example`."
---

`google_ces_example` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	ces "github.com/ubiquex/ubx-sdk-google/sdk/go/google/ces"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own example"})

    		ubx.Resource(ces.Example, "ci-runner", ces.ExampleConfig{
    			App:         "example",
    			DisplayName: "example-display-name",
    			ExampleId:   "example",
    			Location:    "example",
    			Description: "Managed by ubx.",
    			EntryAgent:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Example } from "jsr:@ubx/sdk-google/google/ces/example";

    export default stack("payments", () => {
      intent({ summary: "payments own example" });

      resource(Example, "ci-runner", {
        app: "example",
        displayName: "example-display-name",
        exampleId: "example",
        location: "example",
        description: "Managed by ubx.",
        entryAgent: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.ces import Example, ExampleConfig

    def describe():
        ubx.intent("payments own example")

        ubx.resource(Example, "ci-runner", ExampleConfig(
            app="example",
            display_name="example-display-name",
            example_id="example",
            location="example",
            description="Managed by ubx.",
            entry_agent="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_ces_example, with app example, display name
    example-display-name, example id example, location example,
    description Managed by ubx., entry agent example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app" type="string" required>
  Resource ID segment making up resource 'name', defining the app the example belongs to. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Human-readable description of the example. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Display name of the example. Required.
</ResponseField>

<ResponseField name="entry_agent" type="string">
  The agent that initially handles the conversation. If not specified, the example represents a conversation that is handled by the root agent. Format: 'projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/agents/&#123;agent&#125;' Optional.
</ResponseField>

<ResponseField name="example_id" type="string" required>
  The ID to use for the example, which will become the final component of the example's resource name. In Terraform, this field is required. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name', defining what region the parent app is in. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="messages" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="chunks" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="agent_transfer" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="display_name" type="string">
            </ResponseField>
            <ResponseField name="target_agent" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="image" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="data" type="string" required>
            </ResponseField>
            <ResponseField name="mime_type" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="text" type="string">
        </ResponseField>
        <ResponseField name="tool_call" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="args" type="string">
            </ResponseField>
            <ResponseField name="display_name" type="string">
            </ResponseField>
            <ResponseField name="id" type="string">
            </ResponseField>
            <ResponseField name="tool" type="string">
            </ResponseField>
            <ResponseField name="toolset_tool" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="tool_id" type="string">
                </ResponseField>
                <ResponseField name="toolset" type="string" required>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tool_response" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="display_name" type="string">
            </ResponseField>
            <ResponseField name="id" type="string">
            </ResponseField>
            <ResponseField name="response" type="string" required>
            </ResponseField>
            <ResponseField name="tool" type="string">
            </ResponseField>
            <ResponseField name="toolset_tool" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="tool_id" type="string">
                </ResponseField>
                <ResponseField name="toolset" type="string" required>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="updated_variables" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="role" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Timestamp when the example was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  Etag used to ensure the object hasn't changed during a read-modify-write operation. If the etag is empty, the update will overwrite any concurrent changes. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="invalid" type="bool">
  The example may become invalid if referencing resources are deleted. Invalid examples will not be used as few-shot examples. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="messages" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="chunks" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="agent_transfer" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="display_name" type="string">
            </ResponseField>
            <ResponseField name="target_agent" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="image" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="data" type="string" required>
            </ResponseField>
            <ResponseField name="mime_type" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="text" type="string">
        </ResponseField>
        <ResponseField name="tool_call" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="args" type="string">
            </ResponseField>
            <ResponseField name="display_name" type="string">
            </ResponseField>
            <ResponseField name="id" type="string">
            </ResponseField>
            <ResponseField name="tool" type="string">
            </ResponseField>
            <ResponseField name="toolset_tool" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="tool_id" type="string">
                </ResponseField>
                <ResponseField name="toolset" type="string" required>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tool_response" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="display_name" type="string">
            </ResponseField>
            <ResponseField name="id" type="string">
            </ResponseField>
            <ResponseField name="response" type="string" required>
            </ResponseField>
            <ResponseField name="tool" type="string">
            </ResponseField>
            <ResponseField name="toolset_tool" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="tool_id" type="string">
                </ResponseField>
                <ResponseField name="toolset" type="string" required>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="updated_variables" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="role" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The unique identifier of the example. Format: 'projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/examples/&#123;example&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Timestamp when the example was last updated. Computed by GCP after `ship`.
</ResponseField>
