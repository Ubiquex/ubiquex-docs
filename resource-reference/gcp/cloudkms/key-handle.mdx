---
title: "google_cloudkms_key_handle"
description: "`google_cloudkms_key_handle` is an Autokey-managed reference used to request a Cloud KMS key on demand from a resource's own IAM policy..."
---

`google_cloudkms_key_handle` is an Autokey-managed reference used to request a Cloud KMS key on demand from a resource's own IAM policy, letting Autokey provision and manage the underlying key automatically.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_cloudkms --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_cloudkms/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudkms "github.com/ubiquex/ubx-sdk-google/sdk/go/google_cloudkms/cloudkms"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own key handle"})

    		ubx.Resource(cloudkms.CloudkmsKeyHandle, "example", cloudkms.CloudkmsKeyHandleConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_cloudkms --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CloudkmsKeyHandle } from "./local-sdk/google_cloudkms/sdk/typescript/google_cloudkms/cloudkms/cloudkms_key_handle.ts";

    export default stack("example", () => {
      intent({ summary: "example own key handle" });

      resource(CloudkmsKeyHandle, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_cloudkms --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_cloudkms/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_cloudkms.cloudkms import CloudkmsKeyHandle, CloudkmsKeyHandleConfig

    def describe():
        ubx.intent("example own key handle")

        ubx.resource(CloudkmsKeyHandle, "example", CloudkmsKeyHandleConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudkms_key_handle called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kms_key" type="string">
  Output only. Name of a CryptoKey that has been provisioned for Customer Managed Encryption Key (CMEK) use in the KeyHandle project and location for the requested resource type. The CryptoKey project will reflect the value configured in the AutokeyConfig on the resource project's ancestor folder at the time of the KeyHandle creation. If more than one ancestor folder has a configured AutokeyConfig, the nearest of these configurations is used. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Name of the KeyHandle resource, e.g. `projects/&#123;PROJECT_ID&#125;/locations/&#123;LOCATION&#125;/keyHandles/&#123;KEY_HANDLE_ID&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="resource_type_selector" type="string">
  Required. Indicates the resource type that the resulting CryptoKey is meant to protect, e.g. `&#123;SERVICE&#125;.googleapis.com/&#123;TYPE&#125;`. See documentation for supported resource types. Optional; if omitted, computed by Google Cloud.
</ResponseField>
