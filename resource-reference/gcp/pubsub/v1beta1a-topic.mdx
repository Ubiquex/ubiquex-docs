---
title: "google_pubsub_v1beta1a_topic"
description: "`google_pubsub_v1beta1a_topic` is a Pub/Sub topic, the named resource publishers send message(s) to and subscription(s) receive message(s) from."
---

<Warning>
  This is Google Cloud's own **v1beta1a** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_pubsub_topic`.
</Warning>

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_pubsub_v1beta1a --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_pubsub_v1beta1a/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	pubsub "github.com/ubiquex/ubx-sdk-google/sdk/go/google_pubsub_v1beta1a/pubsub"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1a topic"})

    		ubx.Resource(pubsub.V1beta1aPubsubV1beta1aTopic, "example", pubsub.V1beta1aPubsubV1beta1aTopicConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_pubsub_v1beta1a --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1aPubsubV1beta1aTopic } from "./local-sdk/google_pubsub_v1beta1a/sdk/typescript/google_pubsub_v1beta1a/pubsub/v1beta1a_pubsub_v1beta1a_topic.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1a topic" });

      resource(V1beta1aPubsubV1beta1aTopic, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_pubsub_v1beta1a --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_pubsub_v1beta1a/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_pubsub_v1beta1a.pubsub import V1beta1aPubsubV1beta1aTopic, V1beta1aPubsubV1beta1aTopicConfig

    def describe():
        ubx.intent("example own v1beta1a topic")

        ubx.resource(V1beta1aPubsubV1beta1aTopic, "example", V1beta1aPubsubV1beta1aTopicConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_pubsub_v1beta1a_topic called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string">
  Name of the topic. Optional; if omitted, computed by Google Cloud.
</ResponseField>
