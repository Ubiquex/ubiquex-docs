---
title: "google_pubsub_schema_iam_policy"
description: "Real, generated bindings for `google_pubsub_schema_iam_policy`."
---

`google_pubsub_schema_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	pubsub "github.com/ubiquex/ubx-sdk-google/sdk/go/google/pubsub"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own schema iam policy"})

    		ubx.Resource(pubsub.SchemaIamPolicy, "ci-runner", pubsub.SchemaIamPolicyConfig{
    			PolicyData: "example",
    			Schema:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SchemaIamPolicy } from "jsr:@ubx/sdk-google/google/pubsub/schema_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own schema iam policy" });

      resource(SchemaIamPolicy, "ci-runner", {
        policyData: "example",
        schema: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.pubsub import SchemaIamPolicy, SchemaIamPolicyConfig

    def describe():
        ubx.intent("payments own schema iam policy")

        ubx.resource(SchemaIamPolicy, "ci-runner", SchemaIamPolicyConfig(
            policy_data="example",
            schema="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_pubsub_schema_iam_policy, with policy data example,
    schema example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="schema" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
