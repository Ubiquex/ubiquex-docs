---
title: "google_pubsub_schema_iam_policy"
description: "Real, generated bindings for `google_pubsub_schema_iam_policy`."
---

`google_pubsub_schema_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/pubsub"
    )

    cfg := pubsub.SchemaIamPolicyConfig{
        PolicyData: "example",
        Schema: "example",
    }
    ubx.Resource(pubsub.SchemaIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SchemaIamPolicy } from
      "jsr:@ubx/sdk-google/google/pubsub/schema_iam_policy";

    resource(SchemaIamPolicy, "example", {
      policyData: "example",
      schema: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.pubsub import (
        SchemaIamPolicy,
        SchemaIamPolicyConfig,
    )

    cfg = SchemaIamPolicyConfig(
        policy_data="example",
        schema="example",
    )
    ubx.resource(SchemaIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_pubsub_schema_iam_policy with policy_data,
schema.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="schema" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
