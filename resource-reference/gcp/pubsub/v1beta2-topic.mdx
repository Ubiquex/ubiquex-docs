---
title: "google_pubsub_v1beta2_topic"
description: "`google_pubsub_v1beta2_topic` is a Pub/Sub topic, the named resource publishers send message(s) to and subscription(s) receive message(s) from."
---

<Warning>
  This is Google Cloud's own **v1beta2** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_pubsub_topic`.
</Warning>

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	pubsub "github.com/ubiquex/ubx-sdk-google/sdk/go/google/pubsub"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta2 topic"})

    		ubx.Resource(pubsub.V1beta2Topic, "example", pubsub.V1beta2TopicConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta2Topic } from "@ubx/sdk-google/google/pubsub/v1beta2_topic";

    export default stack("example", () => {
      intent({ summary: "example own v1beta2 topic" });

      resource(V1beta2Topic, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.pubsub.v1beta2_topic import V1beta2Topic, V1beta2TopicConfig

    def describe():
        ubx.intent("example own v1beta2 topic")

        ubx.resource(V1beta2Topic, "example", V1beta2TopicConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_pubsub_v1beta2_topic called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string">
  The name of the topic. It must have the format `"projects/&#123;project&#125;/topics/&#123;topic&#125;"`. `&#123;topic&#125;` must start with a letter, and contain only letters (`[A-Za-z]`), numbers (`[0-9]`), dashes (`-`), underscores (`_`), periods (`.`), tildes (`~`), plus (`+`) or percent signs (`%`). It must be between 3 and 255 characters in length, and it must not start with `"goog"`. Optional; if omitted, computed by Google Cloud.
</ResponseField>
