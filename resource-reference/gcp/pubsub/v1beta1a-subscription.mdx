---
title: "google_pubsub_v1beta1a_subscription"
description: "`google_pubsub_v1beta1a_subscription` attaches to a Pub/Sub topic and delivers its own published messages to a consumer, either by pull or push..."
---

`google_pubsub_v1beta1a_subscription` attaches to a Pub/Sub topic and delivers its own published messages to a consumer, either by pull or push, tracking per-message acknowledgment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	pubsub "github.com/ubiquex/ubx-sdk-google/sdk/go/google/pubsub"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1a subscription"})

    		ubx.Resource(pubsub.V1beta1aSubscription, "example", pubsub.V1beta1aSubscriptionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1aSubscription } from "@ubx/sdk-google/google/pubsub/v1beta1a_subscription";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1a subscription" });

      resource(V1beta1aSubscription, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.pubsub import V1beta1aSubscription, V1beta1aSubscriptionConfig

    def describe():
        ubx.intent("example own v1beta1a subscription")

        ubx.resource(V1beta1aSubscription, "example", V1beta1aSubscriptionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_pubsub_v1beta1a_subscription called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="ack_deadline_seconds" type="number">
  For either push or pull delivery, the value is the maximum time after a subscriber receives a message before the subscriber should acknowledge or Nack the message. If the Ack deadline for a message passes without an Ack or a Nack, the Pub/Sub system will eventually redeliver the message. If a subscriber acknowledges after the deadline, the Pub/Sub system may accept the Ack, but it is possible that the message has been already delivered again. Multiple Acks to the message are allowed and will succeed. For push delivery, this value is used to set the request timeout for the call to the push endpoint. For pull delivery, this value is used as the initial value for the Ack deadline. It may be overridden for each message using its corresponding ack_id with ModifyAckDeadline. While a message is outstanding (i.e. it has been delivered to a pull subscriber and the subscriber has not yet Acked or Nacked), the Pub/Sub system will not deliver that message to another pull subscriber (on a best-effort basis). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the subscription. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="push_config" type="object">
  Configuration for a push delivery endpoint. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="push_endpoint" type="string">
      A URL locating the endpoint to which messages should be pushed. For example, a Webhook endpoint might use "https://example.com/push". Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="topic" type="string">
  The name of the topic from which this subscription is receiving messages. Optional; if omitted, computed by Google Cloud.
</ResponseField>
