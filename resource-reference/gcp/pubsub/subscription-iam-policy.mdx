---
title: "google_pubsub_subscription_iam_policy"
description: "Real, generated bindings for `google_pubsub_subscription_iam_policy`."
---

`google_pubsub_subscription_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pubsub "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/pubsub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own subscription iam policy"})

    		ubx.Resource(pubsub.SubscriptionIamPolicy, "ci-runner", pubsub.SubscriptionIamPolicyConfig{
    			PolicyData:   "example",
    			Subscription: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SubscriptionIamPolicy } from "jsr:@ubx/sdk-google/google/pubsub/subscription_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own subscription iam policy" });

      resource(SubscriptionIamPolicy, "ci-runner", {
        policyData: "example",
        subscription: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.pubsub import SubscriptionIamPolicy, SubscriptionIamPolicyConfig

    def describe():
        ubx.intent("payments own subscription iam policy")

        ubx.resource(SubscriptionIamPolicy, "ci-runner", SubscriptionIamPolicyConfig(
            policy_data="example",
            subscription="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_pubsub_subscription_iam_policy, with policy data
    example, subscription example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="subscription" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
