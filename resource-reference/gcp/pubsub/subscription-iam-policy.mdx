---
title: "google_pubsub_subscription_iam_policy"
description: "Real, generated bindings for `google_pubsub_subscription_iam_policy`."
---

`google_pubsub_subscription_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/pubsub"
    )

    cfg := pubsub.SubscriptionIamPolicyConfig{
        PolicyData: "example",
        Subscription: "example",
    }
    ubx.Resource(pubsub.SubscriptionIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SubscriptionIamPolicy } from
      "jsr:@ubx/sdk-google/google/pubsub/subscription_iam_policy";

    resource(SubscriptionIamPolicy, "example", {
      policyData: "example",
      subscription: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.pubsub import (
        SubscriptionIamPolicy,
        SubscriptionIamPolicyConfig,
    )

    cfg = SubscriptionIamPolicyConfig(
        policy_data="example",
        subscription="example",
    )
    ubx.resource(SubscriptionIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_pubsub_subscription_iam_policy with policy_data,
subscription.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="subscription" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
