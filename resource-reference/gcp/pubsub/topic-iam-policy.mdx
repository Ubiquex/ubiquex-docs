---
title: "google_pubsub_topic_iam_policy"
description: "Real, generated bindings for `google_pubsub_topic_iam_policy`."
---

`google_pubsub_topic_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/pubsub"
    )

    cfg := pubsub.TopicIamPolicyConfig{
        PolicyData: "example",
        Topic: "example",
    }
    ubx.Resource(pubsub.TopicIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TopicIamPolicy } from
      "jsr:@ubx/sdk-google/google/pubsub/topic_iam_policy";

    resource(TopicIamPolicy, "example", {
      policyData: "example",
      topic: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.pubsub import (
        TopicIamPolicy,
        TopicIamPolicyConfig,
    )

    cfg = TopicIamPolicyConfig(
        policy_data="example",
        topic="example",
    )
    ubx.resource(TopicIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_pubsub_topic_iam_policy with policy_data, topic.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="topic" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
