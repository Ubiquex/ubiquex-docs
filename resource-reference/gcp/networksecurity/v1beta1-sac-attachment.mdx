---
title: "google_networksecurity_v1beta1_sac_attachment"
description: "`google_networksecurity_v1beta1_sac_attachment` is a regional Secure Access Connect resource that enables an NCC Gateway to process traffic through..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_networksecurity_sac_attachment`.
</Warning>

`google_networksecurity_v1beta1_sac_attachment` is a regional Secure Access Connect resource that enables an NCC Gateway to process traffic through a connected security service edge (SSE) provider, e.g. Palo Alto Prisma Access.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_networksecurity_v1beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_networksecurity_v1beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networksecurity "github.com/ubiquex/ubx-sdk-google/sdk/go/google_networksecurity_v1beta1/networksecurity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 sac attachment"})

    		ubx.Resource(networksecurity.V1beta1NetworksecurityV1beta1SacAttachment, "example", networksecurity.V1beta1NetworksecurityV1beta1SacAttachmentConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_networksecurity_v1beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1NetworksecurityV1beta1SacAttachment } from "./local-sdk/google_networksecurity_v1beta1/sdk/typescript/google_networksecurity_v1beta1/networksecurity/v1beta1_networksecurity_v1beta1_sac_attachment.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 sac attachment" });

      resource(V1beta1NetworksecurityV1beta1SacAttachment, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_networksecurity_v1beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_networksecurity_v1beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_networksecurity_v1beta1.networksecurity import V1beta1NetworksecurityV1beta1SacAttachment, V1beta1NetworksecurityV1beta1SacAttachmentConfig

    def describe():
        ubx.intent("example own v1beta1 sac attachment")

        ubx.resource(V1beta1NetworksecurityV1beta1SacAttachment, "example", V1beta1NetworksecurityV1beta1SacAttachmentConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_networksecurity_v1beta1_sac_attachment called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="country" type="string">
  Optional. Case-insensitive ISO-3166 alpha-2 country code used for localization. Only valid for Symantec attachments. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Timestamp when the attachment was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Optional list of labels applied to the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Resource name, in the form `projects/&#123;project&#125;/locations/&#123;location&#125;/sacAttachments/&#123;sac_attachment&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ncc_gateway" type="string">
  Required. NCC Gateway associated with the attachment. This can be input as an ID or a full resource name. The output always has the form `projects/&#123;project_number&#125;/locations/&#123;location&#125;/spokes/&#123;ncc_gateway&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="sac_realm" type="string">
  Required. SAC Realm which owns the attachment. This can be input as an ID or a full resource name. The output always has the form `projects/&#123;project_number&#125;/locations/&#123;location&#125;/sacRealms/&#123;sac_realm&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the attachment. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="symantec_options" type="object">
  Fields specific to attachments associated with Symantec Cloud SWG. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="symantec_location_name" type="string">
      Immutable. Name to be used when creating a location on the customer's behalf in Symantec's Location API. Not to be confused with Google Cloud locations. Optional.
    </ResponseField>
    <ResponseField name="symantec_site" type="string">
      Immutable. Symantec data center identifier that this attachment will connect to. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="time_zone" type="string">
  Optional. Case-sensitive tzinfo identifier used for localization. Only valid for Symantec attachments. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when the attachment was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
