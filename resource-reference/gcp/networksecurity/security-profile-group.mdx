---
title: "google_networksecurity_security_profile_group"
description: "`google_networksecurity_security_profile_group` bundles one or more security profiles together for reference from a single firewall policy rule."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_networksecurity --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_networksecurity/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networksecurity "github.com/ubiquex/ubx-sdk-google/sdk/go/google_networksecurity/networksecurity"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security profile group"})

    		ubx.Resource(networksecurity.NetworksecuritySecurityProfileGroup, "ci-runner", networksecurity.NetworksecuritySecurityProfileGroupConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_networksecurity --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworksecuritySecurityProfileGroup } from "./local-sdk/google_networksecurity/sdk/typescript/google_networksecurity/networksecurity/networksecurity_security_profile_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own security profile group" });

      resource(NetworksecuritySecurityProfileGroup, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_networksecurity --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_networksecurity/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_networksecurity.networksecurity import NetworksecuritySecurityProfileGroup, NetworksecuritySecurityProfileGroupConfig

    def describe():
        ubx.intent("payments own security profile group")

        ubx.resource(NetworksecuritySecurityProfileGroup, "ci-runner", NetworksecuritySecurityProfileGroupConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_networksecurity_security_profile_group called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Resource creation timestamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="custom_intercept_profile" type="string">
  Optional. Reference to a SecurityProfile with the CustomIntercept configuration. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="custom_mirroring_profile" type="string">
  Optional. Reference to a SecurityProfile with the CustomMirroring configuration. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="data_path_id" type="string">
  Output only. Identifier used by the data-path. Unique within `&#123;container, location&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. An optional description of the profile group. Max length 2048 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels as key value pairs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. Identifier. Name of the SecurityProfileGroup resource. It matches pattern `projects|organizations/*/locations/&#123;location&#125;/securityProfileGroups/&#123;security_profile_group&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="threat_prevention_profile" type="string">
  Optional. Reference to a SecurityProfile with the ThreatPrevention configuration. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Last resource update timestamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="url_filtering_profile" type="string">
  Optional. Reference to a SecurityProfile with the UrlFiltering configuration. Optional; if omitted, computed by Google Cloud.
</ResponseField>
