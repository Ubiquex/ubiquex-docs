---
title: "google_networksecurity_authorization_policy"
description: "`google_networksecurity_authorization_policy` defines an allow/deny access-control rule for traffic reaching a load balancer or service mesh backend..."
---

`google_networksecurity_authorization_policy` defines an allow/deny access-control rule for traffic reaching a load balancer or service mesh backend, evaluated based on the caller's own authenticated identity.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_networksecurity --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_networksecurity/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networksecurity "github.com/ubiquex/ubx-sdk-google/sdk/go/google_networksecurity/networksecurity"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own authorization policy"})

    		ubx.Resource(networksecurity.NetworksecurityAuthorizationPolicy, "ci-runner", networksecurity.NetworksecurityAuthorizationPolicyConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_networksecurity --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworksecurityAuthorizationPolicy } from "./local-sdk/google_networksecurity/sdk/typescript/google_networksecurity/networksecurity/networksecurity_authorization_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own authorization policy" });

      resource(NetworksecurityAuthorizationPolicy, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_networksecurity --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_networksecurity/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_networksecurity.networksecurity import NetworksecurityAuthorizationPolicy, NetworksecurityAuthorizationPolicyConfig

    def describe():
        ubx.intent("payments own authorization policy")

        ubx.resource(NetworksecurityAuthorizationPolicy, "ci-runner", NetworksecurityAuthorizationPolicyConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_networksecurity_authorization_policy called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="action" type="string">
  Required. The action to take when a rule match is found. Possible values are "ALLOW" or "DENY". Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Free-text description of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Set of label tags associated with the AuthorizationPolicy resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. Name of the AuthorizationPolicy resource. It matches pattern `projects/&#123;project&#125;/locations/&#123;location&#125;/authorizationPolicies/`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  Optional. List of rules to match. Note that at least one of the rules must match in order for the action specified in the 'action' field to be taken. A rule is a match if there is a matching source and destination. If left blank, the action specified in the `action` field will be applied on every request. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="destinations" type="list(object)">
      The destination(s) this rule matches traffic against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="hosts" type="list(string)">
          The host(s) this rule applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="http_header_match" type="object">
          Matches based on the value of a specific HTTP header. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="header_name" type="string">
              The HTTP header name this rule matches against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="regex_match" type="string">
              Matches values against this regular expression. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="methods" type="list(string)">
          The gRPC/HTTP method(s) this rule matches. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ports" type="list(number)">
          The port(s) this rule matches. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sources" type="list(object)">
      The source(s) this rule matches traffic from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ip_blocks" type="list(string)">
          The IP address range(s) this rule matches. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principals" type="list(string)">
          The identity/identities this rule matches against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when the resource was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
