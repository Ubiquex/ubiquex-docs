---
title: "google_networksecurity_v1beta1_tls_inspection_policy"
description: "`google_networksecurity_v1beta1_tls_inspection_policy` configures Cloud NGFW to decrypt, inspect..."
---

`google_networksecurity_v1beta1_tls_inspection_policy` configures Cloud NGFW to decrypt, inspect, and re-encrypt TLS traffic passing through a firewall policy, using a Certificate Authority Service CA pool to mint the inspection certificates.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networksecurity "github.com/ubiquex/ubx-sdk-google/sdk/go/google/networksecurity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 tls inspection policy"})

    		ubx.Resource(networksecurity.V1beta1TlsInspectionPolicy, "example", networksecurity.V1beta1TlsInspectionPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1TlsInspectionPolicy } from "@ubx/sdk-google/google/networksecurity/v1beta1_tls_inspection_policy";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 tls inspection policy" });

      resource(V1beta1TlsInspectionPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.networksecurity import V1beta1TlsInspectionPolicy, V1beta1TlsInspectionPolicyConfig

    def describe():
        ubx.intent("example own v1beta1 tls inspection policy")

        ubx.resource(V1beta1TlsInspectionPolicy, "example", V1beta1TlsInspectionPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_networksecurity_v1beta1_tls_inspection_policy
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="ca_pool" type="string">
  Required. A CA pool resource used to issue interception certificates. The CA pool string has a relative resource path following the form "projects/&#123;project&#125;/locations/&#123;location&#125;/caPools/&#123;ca_pool&#125;". Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="custom_tls_features" type="list(string)">
  Optional. List of custom TLS cipher suites selected. This field is valid only if the selected tls_feature_profile is CUSTOM. The compute.SslPoliciesService.ListAvailableFeatures method returns the set of features that can be specified in this list. Note that Secure Web Proxy does not yet honor this field. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Free-text description of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="exclude_public_ca_set" type="bool">
  Optional. If FALSE (the default), use our default set of public CAs in addition to any CAs specified in trust_config. These public CAs are currently based on the Mozilla Root Program and are subject to change over time. If TRUE, do not accept our default set of public CAs. Only CAs specified in trust_config will be accepted. This defaults to FALSE (use public CAs in addition to trust_config) for backwards compatibility, but trusting public root CAs is *not recommended* unless the traffic in question is outbound to public web servers. When possible, prefer setting this to "false" and explicitly specifying trusted CAs and certificates in a TrustConfig. Note that Secure Web Proxy does not yet honor this field. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="min_tls_version" type="string">
  Optional. Minimum TLS version that the firewall should use when negotiating connections with both clients and servers. If this is not set, then the default value is to allow the broadest set of clients and servers (TLS 1.0 or higher). Setting this to more restrictive values may improve security, but may also prevent the firewall from connecting to some clients or servers. Note that Secure Web Proxy does not yet honor this field. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. Name of the resource. Name is of the form projects/&#123;project&#125;/locations/&#123;location&#125;/tlsInspectionPolicies/&#123;tls_inspection_policy&#125; tls_inspection_policy should match the pattern:(^[a-z]([a-z0-9-]&#123;0,61&#125;[a-z0-9])?$). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tls_feature_profile" type="string">
  Optional. The selected Profile. If this is not set, then the default value is to allow the broadest set of clients and servers ("PROFILE_COMPATIBLE"). Setting this to more restrictive values may improve security, but may also prevent the TLS inspection proxy from connecting to some clients or servers. Note that Secure Web Proxy does not yet honor this field. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="trust_config" type="string">
  Optional. A TrustConfig resource used when making a connection to the TLS server. This is a relative resource path following the form "projects/&#123;project&#125;/locations/&#123;location&#125;/trustConfigs/&#123;trust_config&#125;". This is necessary to intercept TLS connections to servers with certificates signed by a private CA or self-signed certificates. Note that Secure Web Proxy does not yet honor this field. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when the resource was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
