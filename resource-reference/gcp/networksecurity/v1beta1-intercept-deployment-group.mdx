---
title: "google_networksecurity_v1beta1_intercept_deployment_group"
description: "`google_networksecurity_v1beta1_intercept_deployment_group` groups related intercept deployments together across zones..."
---

`google_networksecurity_v1beta1_intercept_deployment_group` groups related intercept deployments together across zones, forwarding traffic to the appropriate zonal deployment based on its own source.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networksecurity "github.com/ubiquex/ubx-sdk-google/sdk/go/google/networksecurity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 intercept deployment group"})

    		ubx.Resource(networksecurity.V1beta1InterceptDeploymentGroup, "example", networksecurity.V1beta1InterceptDeploymentGroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1InterceptDeploymentGroup } from "@ubx/sdk-google/google/networksecurity/v1beta1_intercept_deployment_group";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 intercept deployment group" });

      resource(V1beta1InterceptDeploymentGroup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.networksecurity import V1beta1InterceptDeploymentGroup, V1beta1InterceptDeploymentGroupConfig

    def describe():
        ubx.intent("example own v1beta1 intercept deployment group")

        ubx.resource(V1beta1InterceptDeploymentGroup, "example", V1beta1InterceptDeploymentGroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_networksecurity_v1beta1_intercept_deployment_group
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="connected_endpoint_groups" type="list(object)">
  Output only. The list of endpoint groups that are connected to this resource. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the resource was created. See https://google.aip.dev/148#timestamps. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. User-provided description of the deployment group. Used as additional context for the deployment group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels are key/value pairs that help to organize and filter resources. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="locations" type="list(object)">
  Output only. The list of locations where the deployment group is present. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="location" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="state" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. Identifier. The resource name of this deployment group, for example: `projects/123456789/locations/global/interceptDeploymentGroups/my-dg`. See https://google.aip.dev/122 for more details. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="nested_deployments" type="list(object)">
  Output only. The list of Intercept Deployments that belong to this group. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="state" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network" type="string">
  Required. Immutable. The network that will be used for all child deployments, for example: `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. See https://google.aip.dev/124. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. The current state of the resource does not match the user's intended state, and the system is working to reconcile them. This is part of the normal operation (e.g. adding a new deployment to the group) See https://google.aip.dev/128. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the deployment group. See https://google.aip.dev/216. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when the resource was most recently updated. See https://google.aip.dev/148#timestamps. Optional; if omitted, computed by Google Cloud.
</ResponseField>
