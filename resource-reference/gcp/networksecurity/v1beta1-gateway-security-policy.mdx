---
title: "google_networksecurity_v1beta1_gateway_security_policy"
description: "`google_networksecurity_v1beta1_gateway_security_policy` defines a URL and content-filtering policy applied by a Secure Web Proxy gateway to outbound..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_networksecurity_gateway_security_policy`.
</Warning>

`google_networksecurity_v1beta1_gateway_security_policy` defines a URL and content-filtering policy applied by a Secure Web Proxy gateway to outbound web traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networksecurity "github.com/ubiquex/ubx-sdk-google/sdk/go/google/networksecurity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 gateway security policy"})

    		ubx.Resource(networksecurity.V1beta1GatewaySecurityPolicy, "example", networksecurity.V1beta1GatewaySecurityPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1GatewaySecurityPolicy } from "@ubx/sdk-google/google/networksecurity/v1beta1_gateway_security_policy";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 gateway security policy" });

      resource(V1beta1GatewaySecurityPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.networksecurity.v1beta1_gateway_security_policy import V1beta1GatewaySecurityPolicy, V1beta1GatewaySecurityPolicyConfig

    def describe():
        ubx.intent("example own v1beta1 gateway security policy")

        ubx.resource(V1beta1GatewaySecurityPolicy, "example", V1beta1GatewaySecurityPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_networksecurity_v1beta1_gateway_security_policy
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Free-text description of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. Name of the resource. Name is of the form projects/&#123;project&#125;/locations/&#123;location&#125;/gatewaySecurityPolicies/&#123;gateway_security_policy&#125; gateway_security_policy should match the pattern:(^[a-z]([a-z0-9-]&#123;0,61&#125;[a-z0-9])?$). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tls_inspection_policy" type="string">
  Optional. Name of a TLS Inspection Policy resource that defines how TLS inspection will be performed for any rule(s) which enables it. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when the resource was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
