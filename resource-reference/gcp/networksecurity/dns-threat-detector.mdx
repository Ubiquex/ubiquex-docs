---
title: "google_networksecurity_dns_threat_detector"
description: "`google_networksecurity_dns_threat_detector` enables DNS Armor..."
---

`google_networksecurity_dns_threat_detector` enables DNS Armor, which sends a project's own internet-bound DNS query logs to a managed Infoblox threat-detection engine to flag malicious domains in real time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_networksecurity --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_networksecurity/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networksecurity "github.com/ubiquex/ubx-sdk-google/sdk/go/google_networksecurity/networksecurity"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dns threat detector"})

    		ubx.Resource(networksecurity.NetworksecurityDnsThreatDetector, "ci-runner", networksecurity.NetworksecurityDnsThreatDetectorConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_networksecurity --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworksecurityDnsThreatDetector } from "./local-sdk/google_networksecurity/sdk/typescript/google_networksecurity/networksecurity/networksecurity_dns_threat_detector.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own dns threat detector" });

      resource(NetworksecurityDnsThreatDetector, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_networksecurity --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_networksecurity/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_networksecurity.networksecurity import NetworksecurityDnsThreatDetector, NetworksecurityDnsThreatDetectorConfig

    def describe():
        ubx.intent("payments own dns threat detector")

        ubx.resource(NetworksecurityDnsThreatDetector, "ci-runner", NetworksecurityDnsThreatDetectorConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_networksecurity_dns_threat_detector called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Create time stamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="excluded_networks" type="list(string)">
  Optional. A list of network resource names which aren't monitored by this DnsThreatDetector. Example: `projects/PROJECT_ID/global/networks/NETWORK_NAME`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Any labels associated with the DnsThreatDetector, listed as key value pairs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. Identifier. Name of the DnsThreatDetector resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="provider" type="string">
  Required. The provider used for DNS threat analysis. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Update time stamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>
