---
title: "google_networkconnectivity_service_connection_policy"
description: "`google_networkconnectivity_service_connection_policy` configures the automated Private Service Connect connection settings (subnet allocation..."
---

`google_networkconnectivity_service_connection_policy` configures the automated Private Service Connect connection settings (subnet allocation, PSC connection limits) applied when a consumer network connects to a specific managed service.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_networkconnectivity --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_networkconnectivity/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networkconnectivity "github.com/ubiquex/ubx-sdk-google/sdk/go/google_networkconnectivity/networkconnectivity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service connection policy"})

    		ubx.Resource(networkconnectivity.NetworkconnectivityServiceConnectionPolicy, "example", networkconnectivity.NetworkconnectivityServiceConnectionPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_networkconnectivity --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkconnectivityServiceConnectionPolicy } from "./local-sdk/google_networkconnectivity/sdk/typescript/google_networkconnectivity/networkconnectivity/networkconnectivity_service_connection_policy.ts";

    export default stack("example", () => {
      intent({ summary: "example own service connection policy" });

      resource(NetworkconnectivityServiceConnectionPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_networkconnectivity --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_networkconnectivity/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_networkconnectivity.networkconnectivity import NetworkconnectivityServiceConnectionPolicy, NetworkconnectivityServiceConnectionPolicyConfig

    def describe():
        ubx.intent("example own service connection policy")

        ubx.resource(NetworkconnectivityServiceConnectionPolicy, "example", NetworkconnectivityServiceConnectionPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_networkconnectivity_service_connection_policy
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="auto_created_subnet_info" type="object">
  Information for the automatically created subnetwork and its associated IR. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="delinked" type="bool">
      Output only. Indicates whether the subnetwork is delinked from the Service Connection Policy. Only set if the subnetwork mode is AUTO_CREATED during creation. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="internal_range" type="string">
      Output only. URI of the automatically created Internal Range. Only set if the subnetwork mode is AUTO_CREATED during creation. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="internal_range_ref" type="string">
      Output only. URI of the automatically created Internal Range reference. Only set if the subnetwork mode is AUTO_CREATED during creation. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="subnetwork" type="string">
      Output only. URI of the automatically created subnetwork. Only set if the subnetwork mode is AUTO_CREATED during creation. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="subnetwork_ref" type="string">
      Output only. URI of the automatically created subnetwork reference. Only set if the subnetwork mode is AUTO_CREATED during creation. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Time when the ServiceConnectionPolicy was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  A description of this resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. The etag is computed by the server, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="infrastructure" type="string">
  Output only. The type of underlying resources used to create the connection. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  User-defined labels. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. The name of a ServiceConnectionPolicy. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/serviceConnectionPolicies/&#123;service_connection_policy&#125; See: https://google.aip.dev/122#fields-representing-resource-names Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="network" type="string">
  The resource path of the consumer network. Example: - projects/&#123;projectNumOrId&#125;/global/networks/&#123;resourceId&#125;. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="psc_config" type="object">
  Configuration used for Private Service Connect connections. Used when Infrastructure is PSC. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allowed_google_producers_resource_hierarchy_level" type="list(string)">
      Optional. List of Projects, Folders, or Organizations from where the Producer instance can be within. For example, a network administrator can provide both 'organizations/foo' and 'projects/bar' as allowed_google_producers_resource_hierarchy_levels. This allowlists this network to connect with any Producer instance within the 'foo' organization or the 'bar' project. By default, allowed_google_producers_resource_hierarchy_level is empty. The format for each allowed_google_producers_resource_hierarchy_level is / where is one of 'projects', 'folders', or 'organizations' and is either the ID or the number of the resource type. Format for each allowed_google_producers_resource_hierarchy_level value: 'projects/' or 'folders/' or 'organizations/' Eg. [projects/my-project-id, projects/567, folders/891, organizations/123] Optional.
    </ResponseField>
    <ResponseField name="limit" type="string">
      Optional. Max number of PSC connections for this policy. Optional.
    </ResponseField>
    <ResponseField name="producer_instance_location" type="string">
      Optional. ProducerInstanceLocation is used to specify which authorization mechanism to use to determine which projects the Producer instance can be within. Optional.
    </ResponseField>
    <ResponseField name="subnetworks" type="list(string)">
      The resource paths of subnetworks to use for IP address management. Example: projects/&#123;projectNumOrId&#125;/regions/&#123;region&#125;/subnetworks/&#123;resourceId&#125;. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="psc_connections" type="list(object)">
  Output only. [Output only] Information about each Private Service Connect connection. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="consumer_address" type="string">
      The IP address on the consumer side of this connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="consumer_forwarding_rule" type="string">
      A reference to the consumer's own forwarding rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="consumer_target_project" type="string">
      The project the consuming resource belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="error" type="object">
      Detail about an error that occurred. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="code" type="number">
          A machine-readable status or error code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="details" type="list(map(any))">
          Additional detail about this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_info" type="object">
      Structured detail about an error that occurred. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="domain" type="string">
          The domain this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="metadata" type="map(string)">
          Additional structured metadata about this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reason" type="string">
          A machine-readable reason code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_type" type="string">
      The category of error that occurred. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gce_operation" type="string">
      A reference to the underlying Compute Engine operation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ip_version" type="string">
      Whether this applies to IPv4 or IPv6. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="producer_instance_id" type="string">
      The identifier of the producer-side instance backing this connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="producer_instance_metadata" type="map(string)">
      Metadata describing the producer-side instance backing this connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="psc_connection_id" type="string">
      The unique identifier of this Private Service Connect connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="selected_subnetwork" type="string">
      A reference to the specific subnetwork selected for this connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="service_class" type="string">
      The class of managed service this connection belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state" type="string">
      The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_class" type="string">
  The service class identifier for which this ServiceConnectionPolicy is for. The service class identifier is a unique, symbolic representation of a ServiceClass. It is provided by the Service Producer. Google services have a prefix of gcp or google-cloud. For example, gcp-memorystore-redis or google-cloud-sql. 3rd party services do not. For example, test-service-a3dfcx. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time when the ServiceConnectionPolicy was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
