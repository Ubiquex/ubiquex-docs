---
title: "google_networkconnectivity_policy_based_route"
description: "`google_networkconnectivity_policy_based_route` overrides a VPC network's own default destination-based routing with a rule matched on additional..."
---

`google_networkconnectivity_policy_based_route` overrides a VPC network's own default destination-based routing with a rule matched on additional criteria (e.g. source IP or protocol), for traffic-steering scenarios standard routes can't express.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networkconnectivity "github.com/ubiquex/ubx-sdk-google/sdk/go/google/networkconnectivity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own policy based route"})

    		ubx.Resource(networkconnectivity.PolicyBasedRoute, "example", networkconnectivity.PolicyBasedRouteConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PolicyBasedRoute } from "@ubx/sdk-google/google/networkconnectivity/policy_based_route";

    export default stack("example", () => {
      intent({ summary: "example own policy based route" });

      resource(PolicyBasedRoute, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.networkconnectivity.policy_based_route import PolicyBasedRoute, PolicyBasedRouteConfig

    def describe():
        ubx.intent("example own policy based route")

        ubx.resource(PolicyBasedRoute, "example", PolicyBasedRouteConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_networkconnectivity_policy_based_route called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Time when the policy-based route was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. An optional description of this resource. Provide this field when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="filter" type="object">
  Filter matches L4 traffic. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="dest_range" type="string">
      Optional. The destination IP range of outgoing packets that this policy-based route applies to. Default is "0.0.0.0/0" if protocol version is IPv4 and "::/0" if protocol version is IPv6. Optional.
    </ResponseField>
    <ResponseField name="ip_protocol" type="string">
      Optional. The IP protocol that this policy-based route applies to. Valid values are 'TCP', 'UDP', and 'ALL'. Default is 'ALL'. Optional.
    </ResponseField>
    <ResponseField name="protocol_version" type="string">
      Required. Internet protocol versions this policy-based route applies to. IPV4 and IPV6 is supported. Optional.
    </ResponseField>
    <ResponseField name="src_range" type="string">
      Optional. The source IP range of outgoing packets that this policy-based route applies to. Default is "0.0.0.0/0" if protocol version is IPv4 and "::/0" if protocol version is IPv6. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="interconnect_attachment" type="object">
  InterconnectAttachment that this route applies to. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="region" type="string">
      Optional. Cloud region to install this policy-based route on interconnect attachment. Use `all` to install it on all interconnect attachments. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. Type of this resource. Always networkconnectivity#policyBasedRoute for policy-based Route resources. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  User-defined labels. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. Identifier. A unique name of the resource in the form of `projects/&#123;project_number&#125;/locations/global/PolicyBasedRoutes/&#123;policy_based_route_id&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="network" type="string">
  Required. Fully-qualified URL of the network that this route applies to, for example: projects/my-project/global/networks/my-network. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="next_hop_ilb_ip" type="string">
  Optional. The IP address of a global-access-enabled L4 ILB that is the next hop for matching packets. For this version, only nextHopIlbIp is supported. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="next_hop_other_routes" type="string">
  Optional. Other routes that will be referenced to determine the next hop of the packet. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="priority" type="number">
  Optional. The priority of this policy-based route. Priority is used to break ties in cases where there are more than one matching policy-based routes found. In cases where multiple policy-based routes are matched, the one with the lowest-numbered priority value wins. The default value is 1000. The priority value must be from 1 to 65535, inclusive. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. Server-defined fully-qualified URL for this resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time when the policy-based route was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="virtual_machine" type="object">
  VM instances that this policy-based route applies to. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="tags" type="list(string)">
      Optional. A list of VM instance tags that this policy-based route applies to. VM instances that have ANY of tags specified here installs this PBR. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="warnings" type="list(object)">
  Output only. If potential misconfigurations are detected for this route, this field will be populated with warning messages. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
      A machine-readable status or error code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="data" type="map(string)">
      The literal content of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="warning_message" type="string">
      A human-readable description of a non-fatal warning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
