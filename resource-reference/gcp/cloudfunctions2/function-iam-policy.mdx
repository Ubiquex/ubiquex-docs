---
title: "google_cloudfunctions2_function_iam_policy"
description: "Real, generated bindings for `google_cloudfunctions2_function_iam_policy`."
---

`google_cloudfunctions2_function_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/cloudfunctions2"
    )

    cfg := cloudfunctions2.FunctionIamPolicyConfig{
        CloudFunction: "example",
        PolicyData: "example",
    }
    ubx.Resource(cloudfunctions2.FunctionIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FunctionIamPolicy } from
      "jsr:@ubx/sdk-google/google/cloudfunctions2/function_iam_policy";

    resource(FunctionIamPolicy, "example", {
      cloudFunction: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.cloudfunctions2 import (
        FunctionIamPolicy,
        FunctionIamPolicyConfig,
    )

    cfg = FunctionIamPolicyConfig(
        cloud_function="example",
        policy_data="example",
    )
    ubx.resource(FunctionIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_cloudfunctions2_function_iam_policy with
cloud_function, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_function" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
