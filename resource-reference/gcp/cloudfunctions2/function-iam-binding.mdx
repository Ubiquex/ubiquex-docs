---
title: "google_cloudfunctions2_function_iam_binding"
description: "Real, generated bindings for `google_cloudfunctions2_function_iam_binding`."
---

`google_cloudfunctions2_function_iam_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloudfunctions2"
    )

    cfg := cloudfunctions2.FunctionIamBindingConfig{
        CloudFunction: "example",
        Members: []string{"example"},
        Role: "example",
    }
    ubx.Resource(cloudfunctions2.FunctionIamBinding, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FunctionIamBinding } from
      "jsr:@ubx/sdk-google/google/cloudfunctions2/function_iam_binding";

    resource(FunctionIamBinding, "example", {
      cloudFunction: "example",
      members: ["example"],
      role: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.cloudfunctions2 import (
        FunctionIamBinding,
        FunctionIamBindingConfig,
    )

    cfg = FunctionIamBindingConfig(
        cloud_function="example",
        members=["example"],
        role="example",
    )
    ubx.resource(FunctionIamBinding, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_cloudfunctions2_function_iam_binding with
cloud_function, members, role.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_function" type="string" required>
  Required.
</ResponseField>

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="members" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
