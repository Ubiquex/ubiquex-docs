---
title: "google_logging_organization_exclusion"
description: "Real, generated bindings for `google_logging_organization_exclusion`."
---

`google_logging_organization_exclusion` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/logging"
    )

    cfg := logging.OrganizationExclusionConfig{
        Filter: "example",
        Name: "example-name",
        OrgId: "example",
        Description: "example",
        Disabled: true,
    }
    ubx.Resource(logging.OrganizationExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OrganizationExclusion } from
      "jsr:@ubx/sdk-google/google/logging/organization_exclusion";

    resource(OrganizationExclusion, "example", {
      filter: "example",
      name: "example-name",
      orgId: "example",
      description: "example",
      disabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.logging import (
        OrganizationExclusion,
        OrganizationExclusionConfig,
    )

    cfg = OrganizationExclusionConfig(
        filter="example",
        name="example-name",
        org_id="example",
        description="example",
        disabled=True,
    )
    ubx.resource(OrganizationExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_logging_organization_exclusion with filter,
name, org_id, description, disabled.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="filter" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
