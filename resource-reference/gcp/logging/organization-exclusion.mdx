---
title: "google_logging_organization_exclusion"
description: "Real, generated bindings for `google_logging_organization_exclusion`."
---

`google_logging_organization_exclusion` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logging "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/logging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organization exclusion"})

    		ubx.Resource(logging.OrganizationExclusion, "ci-runner", logging.OrganizationExclusionConfig{
    			Filter:      "example",
    			Name:        "ci-runner",
    			OrgId:       "example",
    			Description: "Managed by ubx.",
    			Disabled:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationExclusion } from "jsr:@ubx/sdk-google/google/logging/organization_exclusion";

    export default stack("payments", () => {
      intent({ summary: "payments own organization exclusion" });

      resource(OrganizationExclusion, "ci-runner", {
        filter: "example",
        name: "ci-runner",
        orgId: "example",
        description: "Managed by ubx.",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.logging import OrganizationExclusion, OrganizationExclusionConfig

    def describe():
        ubx.intent("payments own organization exclusion")

        ubx.resource(OrganizationExclusion, "ci-runner", OrganizationExclusionConfig(
            filter="example",
            name="ci-runner",
            org_id="example",
            description="Managed by ubx.",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_logging_organization_exclusion called "ci-runner",
    with filter example, org id example, description Managed by ubx.,
    disabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="filter" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
