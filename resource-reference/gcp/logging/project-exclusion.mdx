---
title: "google_logging_project_exclusion"
description: "Real, generated bindings for `google_logging_project_exclusion`."
---

`google_logging_project_exclusion` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	logging "github.com/ubiquex/ubx-sdk-google/sdk/go/google/logging"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own project exclusion"})

    		ubx.Resource(logging.ProjectExclusion, "ci-runner", logging.ProjectExclusionConfig{
    			Filter:      "example",
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    			Disabled:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProjectExclusion } from "jsr:@ubx/sdk-google/google/logging/project_exclusion";

    export default stack("payments", () => {
      intent({ summary: "payments own project exclusion" });

      resource(ProjectExclusion, "ci-runner", {
        filter: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.logging import ProjectExclusion, ProjectExclusionConfig

    def describe():
        ubx.intent("payments own project exclusion")

        ubx.resource(ProjectExclusion, "ci-runner", ProjectExclusionConfig(
            filter="example",
            name="ci-runner",
            description="Managed by ubx.",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_logging_project_exclusion called "ci-runner", with
    filter example, description Managed by ubx., disabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="filter" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
