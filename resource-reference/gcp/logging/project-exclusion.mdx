---
title: "google_logging_project_exclusion"
description: "Real, generated bindings for `google_logging_project_exclusion`."
---

`google_logging_project_exclusion` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/logging"
    )

    cfg := logging.ProjectExclusionConfig{
        Filter: "example",
        Name: "example-name",
        Description: "example",
        Disabled: true,
    }
    ubx.Resource(logging.ProjectExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProjectExclusion } from
      "jsr:@ubx/sdk-google/google/logging/project_exclusion";

    resource(ProjectExclusion, "example", {
      filter: "example",
      name: "example-name",
      description: "example",
      disabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.logging import (
        ProjectExclusion,
        ProjectExclusionConfig,
    )

    cfg = ProjectExclusionConfig(
        filter="example",
        name="example-name",
        description="example",
        disabled=True,
    )
    ubx.resource(ProjectExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_logging_project_exclusion with filter, name,
description, disabled.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="filter" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
