---
title: "google_logging_folder_bucket_config"
description: "Real, generated bindings for `google_logging_folder_bucket_config`."
---

`google_logging_folder_bucket_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logging "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/logging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own folder bucket config"})

    		ubx.Resource(logging.FolderBucketConfig, "ci-runner", logging.FolderBucketConfigConfig{
    			BucketId:      "example",
    			Folder:        "example",
    			Location:      "example",
    			RetentionDays: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FolderBucketConfig } from "jsr:@ubx/sdk-google/google/logging/folder_bucket_config";

    export default stack("payments", () => {
      intent({ summary: "payments own folder bucket config" });

      resource(FolderBucketConfig, "ci-runner", {
        bucketId: "example",
        folder: "example",
        location: "example",
        retentionDays: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.logging import FolderBucketConfig, FolderBucketConfigConfig

    def describe():
        ubx.intent("payments own folder bucket config")

        ubx.resource(FolderBucketConfig, "ci-runner", FolderBucketConfigConfig(
            bucket_id="example",
            folder="example",
            location="example",
            retention_days=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_logging_folder_bucket_config, with bucket id
    example, folder example, location example, retention days 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="cmek_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string" required>
    </ResponseField>
    <ResponseField name="kms_key_version_name" type="string">
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="service_account_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="folder" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="index_configs" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="field_path" type="string" required>
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="retention_days" type="number">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="cmek_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string" required>
    </ResponseField>
    <ResponseField name="kms_key_version_name" type="string">
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="service_account_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="index_configs" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="field_path" type="string" required>
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="lifecycle_state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>
