---
title: "google_logging_folder_sink"
description: "Real, generated bindings for `google_logging_folder_sink`."
---

`google_logging_folder_sink` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logging "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/logging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own folder sink"})

    		ubx.Resource(logging.FolderSink, "ci-runner", logging.FolderSinkConfig{
    			Destination:       "example",
    			Folder:            "example",
    			Name:              "ci-runner",
    			Description:       "Managed by ubx.",
    			Disabled:          true,
    			Filter:            "example",
    			IncludeChildren:   true,
    			InterceptChildren: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FolderSink } from "jsr:@ubx/sdk-google/google/logging/folder_sink";

    export default stack("payments", () => {
      intent({ summary: "payments own folder sink" });

      resource(FolderSink, "ci-runner", {
        destination: "example",
        folder: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        disabled: true,
        filter: "example",
        includeChildren: true,
        interceptChildren: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.logging import FolderSink, FolderSinkConfig

    def describe():
        ubx.intent("payments own folder sink")

        ubx.resource(FolderSink, "ci-runner", FolderSinkConfig(
            destination="example",
            folder="example",
            name="ci-runner",
            description="Managed by ubx.",
            disabled=True,
            filter="example",
            include_children=True,
            intercept_children=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_logging_folder_sink called "ci-runner", with
    destination example, folder example, description Managed by ubx.,
    disabled true, filter example, include children true, intercept
    children true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bigquery_options" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="use_partitioned_tables" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="destination" type="string" required>
  Required.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="exclusions" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="filter" type="string" required>
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="filter" type="string">
  Optional.
</ResponseField>

<ResponseField name="folder" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="include_children" type="bool">
  Optional.
</ResponseField>

<ResponseField name="intercept_children" type="bool">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="bigquery_options" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="use_partitioned_tables" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="exclusions" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="filter" type="string" required>
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="writer_identity" type="string">
  Computed by GCP after `ship`.
</ResponseField>
