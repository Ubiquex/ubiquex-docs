---
title: "google_logging_billing_account_exclusion"
description: "Real, generated bindings for `google_logging_billing_account_exclusion`."
---

`google_logging_billing_account_exclusion` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/logging"
    )

    cfg := logging.BillingAccountExclusionConfig{
        BillingAccount: "example",
        Filter: "example",
        Name: "example-name",
        Description: "example",
        Disabled: true,
    }
    ubx.Resource(logging.BillingAccountExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BillingAccountExclusion } from
      "jsr:@ubx/sdk-google/google/logging/billing_account_exclusion";

    resource(BillingAccountExclusion, "example", {
      billingAccount: "example",
      filter: "example",
      name: "example-name",
      description: "example",
      disabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.logging import (
        BillingAccountExclusion,
        BillingAccountExclusionConfig,
    )

    cfg = BillingAccountExclusionConfig(
        billing_account="example",
        filter="example",
        name="example-name",
        description="example",
        disabled=True,
    )
    ubx.resource(BillingAccountExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_logging_billing_account_exclusion with
billing_account, filter, name, description, disabled.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="billing_account" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="filter" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
