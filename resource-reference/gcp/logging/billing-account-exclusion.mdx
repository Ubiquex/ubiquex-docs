---
title: "google_logging_billing_account_exclusion"
description: "Real, generated bindings for `google_logging_billing_account_exclusion`."
---

`google_logging_billing_account_exclusion` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logging "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/logging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own billing account exclusion"})

    		ubx.Resource(logging.BillingAccountExclusion, "ci-runner", logging.BillingAccountExclusionConfig{
    			BillingAccount: "example",
    			Filter:         "example",
    			Name:           "ci-runner",
    			Description:    "Managed by ubx.",
    			Disabled:       true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BillingAccountExclusion } from "jsr:@ubx/sdk-google/google/logging/billing_account_exclusion";

    export default stack("payments", () => {
      intent({ summary: "payments own billing account exclusion" });

      resource(BillingAccountExclusion, "ci-runner", {
        billingAccount: "example",
        filter: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.logging import BillingAccountExclusion, BillingAccountExclusionConfig

    def describe():
        ubx.intent("payments own billing account exclusion")

        ubx.resource(BillingAccountExclusion, "ci-runner", BillingAccountExclusionConfig(
            billing_account="example",
            filter="example",
            name="ci-runner",
            description="Managed by ubx.",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_logging_billing_account_exclusion called
    "ci-runner", with billing account example, filter example,
    description Managed by ubx., disabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="billing_account" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="filter" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
