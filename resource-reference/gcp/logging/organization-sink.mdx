---
title: "google_logging_organization_sink"
description: "Real, generated bindings for `google_logging_organization_sink`."
---

`google_logging_organization_sink` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	logging "github.com/ubiquex/ubx-sdk-google/sdk/go/google/logging"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organization sink"})

    		ubx.Resource(logging.OrganizationSink, "ci-runner", logging.OrganizationSinkConfig{
    			Destination:       "example",
    			Name:              "ci-runner",
    			OrgId:             "example",
    			Description:       "Managed by ubx.",
    			Disabled:          true,
    			Filter:            "example",
    			IncludeChildren:   true,
    			InterceptChildren: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationSink } from "jsr:@ubx/sdk-google/google/logging/organization_sink";

    export default stack("payments", () => {
      intent({ summary: "payments own organization sink" });

      resource(OrganizationSink, "ci-runner", {
        destination: "example",
        name: "ci-runner",
        orgId: "example",
        description: "Managed by ubx.",
        disabled: true,
        filter: "example",
        includeChildren: true,
        interceptChildren: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.logging import OrganizationSink, OrganizationSinkConfig

    def describe():
        ubx.intent("payments own organization sink")

        ubx.resource(OrganizationSink, "ci-runner", OrganizationSinkConfig(
            destination="example",
            name="ci-runner",
            org_id="example",
            description="Managed by ubx.",
            disabled=True,
            filter="example",
            include_children=True,
            intercept_children=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_logging_organization_sink called "ci-runner", with
    destination example, org id example, description Managed by ubx.,
    disabled true, filter example, include children true, intercept
    children true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bigquery_options" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="use_partitioned_tables" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A description of this sink. The maximum length of the description is 8000 characters. Optional.
</ResponseField>

<ResponseField name="destination" type="string" required>
  The destination of the sink (or, in other words, where logs are written to). Can be a Cloud Storage bucket, a PubSub topic, or a BigQuery dataset. Examples: "storage.googleapis.com/[GCS_BUCKET]" "bigquery.googleapis.com/projects/[PROJECT_ID]/datasets/[DATASET]" "pubsub.googleapis.com/projects/[PROJECT_ID]/topics/[TOPIC_ID]" The writer associated with the sink must have access to write to the above resource. Required.
</ResponseField>

<ResponseField name="disabled" type="bool">
  If set to True, then this sink is disabled and it does not export any log entries. Optional.
</ResponseField>

<ResponseField name="exclusions" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="filter" type="string" required>
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="filter" type="string">
  The filter to apply when exporting logs. Only log entries that match the filter are exported. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="include_children" type="bool">
  Whether or not to include child folders or projects in the sink export. If true, logs associated with child projects are also exported; otherwise only logs relating to the provided organization are included. Optional.
</ResponseField>

<ResponseField name="intercept_children" type="bool">
  Whether or not to intercept logs from child projects. If true, matching logs will not match with sinks in child resources, except _Required sinks. This sink will be visible to child resources when listing sinks. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the logging sink. Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  The numeric ID of the organization to be exported to the sink. Required.
</ResponseField>

## Output properties

<ResponseField name="bigquery_options" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="use_partitioned_tables" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="exclusions" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="filter" type="string" required>
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="writer_identity" type="string">
  The identity associated with this sink. This identity must be granted write access to the configured destination. Computed by GCP after `ship`.
</ResponseField>
