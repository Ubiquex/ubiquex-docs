---
title: "google_logging_log_view_iam_policy"
description: "Real, generated bindings for `google_logging_log_view_iam_policy`."
---

`google_logging_log_view_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logging "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/logging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log view iam policy"})

    		ubx.Resource(logging.LogViewIamPolicy, "ci-runner", logging.LogViewIamPolicyConfig{
    			Bucket:     "example",
    			Name:       "ci-runner",
    			Parent:     "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogViewIamPolicy } from "jsr:@ubx/sdk-google/google/logging/log_view_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own log view iam policy" });

      resource(LogViewIamPolicy, "ci-runner", {
        bucket: "example",
        name: "ci-runner",
        parent: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.logging import LogViewIamPolicy, LogViewIamPolicyConfig

    def describe():
        ubx.intent("payments own log view iam policy")

        ubx.resource(LogViewIamPolicy, "ci-runner", LogViewIamPolicyConfig(
            bucket="example",
            name="ci-runner",
            parent="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_logging_log_view_iam_policy called "ci-runner",
    with bucket example, parent example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
