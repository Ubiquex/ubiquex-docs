---
title: "google_logging_log_view_iam_policy"
description: "Real, generated bindings for `google_logging_log_view_iam_policy`."
---

`google_logging_log_view_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/logging"
    )

    cfg := logging.LogViewIamPolicyConfig{
        Bucket: "example",
        Name: "example-name",
        Parent: "example",
        PolicyData: "example",
    }
    ubx.Resource(logging.LogViewIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogViewIamPolicy } from
      "jsr:@ubx/sdk-google/google/logging/log_view_iam_policy";

    resource(LogViewIamPolicy, "example", {
      bucket: "example",
      name: "example-name",
      parent: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.logging import (
        LogViewIamPolicy,
        LogViewIamPolicyConfig,
    )

    cfg = LogViewIamPolicyConfig(
        bucket="example",
        name="example-name",
        parent="example",
        policy_data="example",
    )
    ubx.resource(LogViewIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_logging_log_view_iam_policy with bucket, name,
parent, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
