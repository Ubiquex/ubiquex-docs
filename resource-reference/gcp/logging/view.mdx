---
title: "google_logging_view"
description: "`google_logging_view` restricts a principal's own access within a Cloud Logging log bucket to a defined filtered subset of its log entries..."
---

`google_logging_view` restricts a principal's own access within a Cloud Logging log bucket to a defined filtered subset of its log entries, for role-based data isolation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_logging --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_logging/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	logging "github.com/ubiquex/ubx-sdk-google/sdk/go/google_logging/logging"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own view"})

    		ubx.Resource(logging.LoggingView, "example", logging.LoggingViewConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_logging --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LoggingView } from "./local-sdk/google_logging/sdk/typescript/google_logging/logging/logging_view.ts";

    export default stack("example", () => {
      intent({ summary: "example own view" });

      resource(LoggingView, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_logging --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_logging/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_logging.logging import LoggingView, LoggingViewConfig

    def describe():
        ubx.intent("example own view")

        ubx.resource(LoggingView, "example", LoggingViewConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_logging_view called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The creation timestamp of the view. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Describes this view. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="filter" type="string">
  Optional. Filter that restricts which log entries in a bucket are visible in this view.Filters must be logical conjunctions that use the AND operator, and they can use any of the following qualifiers: SOURCE(), which specifies a project, folder, organization, or billing account of origin. resource.type, which specifies the resource type. LOG_ID(), which identifies the log.They can also use the negations of these qualifiers with the NOT operator.For example:SOURCE("projects/myproject") AND resource.type = "gce_instance" AND NOT LOG_ID("stdout") Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource name of the view.For example:projects/my-project/locations/global/buckets/my-bucket/views/my-view Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The last update timestamp of the view. Optional; if omitted, computed by Google Cloud.
</ResponseField>
