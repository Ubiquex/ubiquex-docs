---
title: "google_logging_folder_exclusion"
description: "Real, generated bindings for `google_logging_folder_exclusion`."
---

`google_logging_folder_exclusion` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/logging"
    )

    cfg := logging.FolderExclusionConfig{
        Filter: "example",
        Folder: "example",
        Name: "example-name",
        Description: "example",
        Disabled: true,
    }
    ubx.Resource(logging.FolderExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FolderExclusion } from
      "jsr:@ubx/sdk-google/google/logging/folder_exclusion";

    resource(FolderExclusion, "example", {
      filter: "example",
      folder: "example",
      name: "example-name",
      description: "example",
      disabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.logging import (
        FolderExclusion,
        FolderExclusionConfig,
    )

    cfg = FolderExclusionConfig(
        filter="example",
        folder="example",
        name="example-name",
        description="example",
        disabled=True,
    )
    ubx.resource(FolderExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_logging_folder_exclusion with filter, folder,
name, description, disabled.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="filter" type="string" required>
  Required.
</ResponseField>

<ResponseField name="folder" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
