---
title: "google_logging_folder_exclusion"
description: "Real, generated bindings for `google_logging_folder_exclusion`."
---

`google_logging_folder_exclusion` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	logging "github.com/ubiquex/ubx-sdk-google/sdk/go/google/logging"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own folder exclusion"})

    		ubx.Resource(logging.FolderExclusion, "ci-runner", logging.FolderExclusionConfig{
    			Filter:      "example",
    			Folder:      "example",
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    			Disabled:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FolderExclusion } from "jsr:@ubx/sdk-google/google/logging/folder_exclusion";

    export default stack("payments", () => {
      intent({ summary: "payments own folder exclusion" });

      resource(FolderExclusion, "ci-runner", {
        filter: "example",
        folder: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.logging import FolderExclusion, FolderExclusionConfig

    def describe():
        ubx.intent("payments own folder exclusion")

        ubx.resource(FolderExclusion, "ci-runner", FolderExclusionConfig(
            filter="example",
            folder="example",
            name="ci-runner",
            description="Managed by ubx.",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_logging_folder_exclusion called "ci-runner", with
    filter example, folder example, description Managed by ubx.,
    disabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A human-readable description. Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Whether this exclusion rule should be disabled or not. This defaults to false. Optional.
</ResponseField>

<ResponseField name="filter" type="string" required>
  The filter to apply when excluding logs. Only log entries that match the filter are excluded. Required.
</ResponseField>

<ResponseField name="folder" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the logging exclusion. Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
