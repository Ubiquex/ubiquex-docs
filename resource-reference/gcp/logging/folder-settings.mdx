---
title: "google_logging_folder_settings"
description: "Real, generated bindings for `google_logging_folder_settings`."
---

`google_logging_folder_settings` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	logging "github.com/ubiquex/ubx-sdk-google/sdk/go/google/logging"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own folder settings"})

    		ubx.Resource(logging.FolderSettings, "ci-runner", logging.FolderSettingsConfig{
    			Folder: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FolderSettings } from "jsr:@ubx/sdk-google/google/logging/folder_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own folder settings" });

      resource(FolderSettings, "ci-runner", {
        folder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.logging import FolderSettings, FolderSettingsConfig

    def describe():
        ubx.intent("payments own folder settings")

        ubx.resource(FolderSettings, "ci-runner", FolderSettingsConfig(
            folder="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_logging_folder_settings, with folder example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="disable_default_sink" type="bool">
  If set to true, the _Default sink in newly created projects and folders will created in a disabled state. This can be used to automatically disable log storage if there is already an aggregated sink configured in the hierarchy. The _Default sink can be re-enabled manually if needed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="folder" type="string" required>
  The folder for which to retrieve settings. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  The resource name for the configured Cloud KMS key. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="storage_location" type="string">
  The storage location that Cloud Logging will use to create new resources when a location is needed but not explicitly provided. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="disable_default_sink" type="bool">
  If set to true, the _Default sink in newly created projects and folders will created in a disabled state. This can be used to automatically disable log storage if there is already an aggregated sink configured in the hierarchy. The _Default sink can be re-enabled manually if needed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  The resource name for the configured Cloud KMS key. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_service_account_id" type="string">
  The service account that will be used by the Log Router to access your Cloud KMS key. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="logging_service_account_id" type="string">
  The service account for the given container. Sinks use this service account as their writerIdentity if no custom service account is provided. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the settings. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="storage_location" type="string">
  The storage location that Cloud Logging will use to create new resources when a location is needed but not explicitly provided. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
