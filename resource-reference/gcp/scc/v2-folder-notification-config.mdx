---
title: "google_scc_v2_folder_notification_config"
description: "Real, generated bindings for `google_scc_v2_folder_notification_config`."
---

`google_scc_v2_folder_notification_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	scc "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/scc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own v2 folder notification config"})

    		ubx.Resource(scc.V2FolderNotificationConfig, "ci-runner", scc.V2FolderNotificationConfigConfig{
    			ConfigId:    "example",
    			Folder:      "example",
    			PubsubTopic: "example",
    			Description: "Managed by ubx.",
    			Location:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2FolderNotificationConfig } from "jsr:@ubx/sdk-google/google/scc/v2_folder_notification_config";

    export default stack("payments", () => {
      intent({ summary: "payments own v2 folder notification config" });

      resource(V2FolderNotificationConfig, "ci-runner", {
        configId: "example",
        folder: "example",
        pubsubTopic: "example",
        description: "Managed by ubx.",
        location: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.scc import V2FolderNotificationConfig, V2FolderNotificationConfigConfig

    def describe():
        ubx.intent("payments own v2 folder notification config")

        ubx.resource(V2FolderNotificationConfig, "ci-runner", V2FolderNotificationConfigConfig(
            config_id="example",
            folder="example",
            pubsub_topic="example",
            description="Managed by ubx.",
            location="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_scc_v2_folder_notification_config, with config id
    example, folder example, pubsub topic example, description
    Managed by ubx., location example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="config_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="folder" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional.
</ResponseField>

<ResponseField name="pubsub_topic" type="string" required>
  Required.
</ResponseField>

<ResponseField name="streaming_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="filter" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="service_account" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="streaming_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="filter" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
