---
title: "google_scc_source_iam_policy"
description: "Real, generated bindings for `google_scc_source_iam_policy`."
---

`google_scc_source_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/scc"
    )

    cfg := scc.SourceIamPolicyConfig{
        Organization: "example",
        PolicyData: "example",
        Source: "example",
    }
    ubx.Resource(scc.SourceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SourceIamPolicy } from
      "jsr:@ubx/sdk-google/google/scc/source_iam_policy";

    resource(SourceIamPolicy, "example", {
      organization: "example",
      policyData: "example",
      source: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.scc import SourceIamPolicy, SourceIamPolicyConfig

    cfg = SourceIamPolicyConfig(
        organization="example",
        policy_data="example",
        source="example",
    )
    ubx.resource(SourceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_scc_source_iam_policy with organization,
policy_data, source.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="organization" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
