---
title: "google_scc_source_iam_policy"
description: "Real, generated bindings for `google_scc_source_iam_policy`."
---

`google_scc_source_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	scc "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/scc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own source iam policy"})

    		ubx.Resource(scc.SourceIamPolicy, "ci-runner", scc.SourceIamPolicyConfig{
    			Organization: "example",
    			PolicyData:   "example",
    			Source:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SourceIamPolicy } from "jsr:@ubx/sdk-google/google/scc/source_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own source iam policy" });

      resource(SourceIamPolicy, "ci-runner", {
        organization: "example",
        policyData: "example",
        source: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.scc import SourceIamPolicy, SourceIamPolicyConfig

    def describe():
        ubx.intent("payments own source iam policy")

        ubx.resource(SourceIamPolicy, "ci-runner", SourceIamPolicyConfig(
            organization="example",
            policy_data="example",
            source="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_scc_source_iam_policy, with organization example,
    policy data example, source example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="organization" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
