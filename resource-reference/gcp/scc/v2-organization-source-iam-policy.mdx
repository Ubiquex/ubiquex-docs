---
title: "google_scc_v2_organization_source_iam_policy"
description: "Real, generated bindings for `google_scc_v2_organization_source_iam_policy`."
---

`google_scc_v2_organization_source_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	scc "github.com/ubiquex/ubx-sdk-google/sdk/go/google/scc"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own v2 organization source iam policy"})

    		ubx.Resource(scc.V2OrganizationSourceIamPolicy, "ci-runner", scc.V2OrganizationSourceIamPolicyConfig{
    			Organization: "example",
    			PolicyData:   "example",
    			Source:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2OrganizationSourceIamPolicy } from "jsr:@ubx/sdk-google/google/scc/v2_organization_source_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own v2 organization source iam policy" });

      resource(V2OrganizationSourceIamPolicy, "ci-runner", {
        organization: "example",
        policyData: "example",
        source: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.scc import V2OrganizationSourceIamPolicy, V2OrganizationSourceIamPolicyConfig

    def describe():
        ubx.intent("payments own v2 organization source iam policy")

        ubx.resource(V2OrganizationSourceIamPolicy, "ci-runner", V2OrganizationSourceIamPolicyConfig(
            organization="example",
            policy_data="example",
            source="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_scc_v2_organization_source_iam_policy, with
    organization example, policy data example, source example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="organization" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
