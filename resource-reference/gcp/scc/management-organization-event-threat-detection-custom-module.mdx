---
title: "google_scc_management_organization_event_threat_detection_custom_module"
description: "Real, generated bindings for `google_scc_management_organization_event_threat_detection_custom_module`."
---

`google_scc_management_organization_event_threat_detection_custom_module` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	scc "github.com/ubiquex/ubx-sdk-google/sdk/go/google/scc"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own management organization event threat detection custom module"})

    		ubx.Resource(scc.ManagementOrganizationEventThreatDetectionCustomModule, "ci-runner", scc.ManagementOrganizationEventThreatDetectionCustomModuleConfig{
    			Organization:    "example",
    			Config:          "example",
    			DisplayName:     "example-display-name",
    			EnablementState: "example",
    			Location:        "example",
    			Type:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagementOrganizationEventThreatDetectionCustomModule } from "jsr:@ubx/sdk-google/google/scc/management_organization_event_threat_detection_custom_module";

    export default stack("payments", () => {
      intent({
        summary:
          "payments own management organization event threat detection custom module",
      });

      resource(
        ManagementOrganizationEventThreatDetectionCustomModule,
        "ci-runner",
        {
          organization: "example",
          config: "example",
          displayName: "example-display-name",
          enablementState: "example",
          location: "example",
          type: "example",
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.scc import ManagementOrganizationEventThreatDetectionCustomModule, ManagementOrganizationEventThreatDetectionCustomModuleConfig

    def describe():
        ubx.intent("payments own management organization event threat detection custom module")

        ubx.resource(ManagementOrganizationEventThreatDetectionCustomModule, "ci-runner", ManagementOrganizationEventThreatDetectionCustomModuleConfig(
            organization="example",
            config="example",
            display_name="example-display-name",
            enablement_state="example",
            location="example",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    google_scc_management_organization_event_threat_detection_custom_module,
    with organization example, config example, display name
    example-display-name, enablement state example, location example,
    type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="config" type="string">
  Config for the module. For the resident module, its config value is defined at this level. For the inherited module, its config value is inherited from the ancestor module. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string">
  The human readable name to be displayed for the module. Optional.
</ResponseField>

<ResponseField name="enablement_state" type="string">
  The state of enablement for the module at the given level of the hierarchy. Possible values: ["ENABLED", "DISABLED"] Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Location ID of the parent organization. Only global is supported at the moment. Optional.
</ResponseField>

<ResponseField name="organization" type="string" required>
  Numerical ID of the parent organization. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Immutable. Type for the module. e.g. CONFIGURABLE_BAD_IP. Optional.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_editor" type="string">
  The editor that last updated the custom module Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the Event Threat Detection custom module. Its format is "organizations/&#123;organization&#125;/locations/&#123;location&#125;/eventThreatDetectionCustomModules/&#123;eventThreatDetectionCustomModule&#125;". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The time at which the custom module was last updated. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>
