---
title: "google_saasservicemgmt_v1beta1_unit_kind"
description: "`google_saasservicemgmt_v1beta1_unit_kind` classifies a `google_saasservicemgmt_unit` in App Lifecycle Manager..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_saasservicemgmt_unit_kind`.
</Warning>

`google_saasservicemgmt_v1beta1_unit_kind` classifies a `google_saasservicemgmt_unit` in App Lifecycle Manager, the real category a `google_saasservicemgmt_rollout` targets (optionally with additional filters) when deciding which units to update.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_saasservicemgmt_v1beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_saasservicemgmt_v1beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	saasservicemgmt "github.com/ubiquex/ubx-sdk-google/sdk/go/google_saasservicemgmt_v1beta1/saasservicemgmt"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 unit kind"})

    		ubx.Resource(saasservicemgmt.V1beta1SaasservicemgmtV1beta1UnitKind, "example", saasservicemgmt.V1beta1SaasservicemgmtV1beta1UnitKindConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_saasservicemgmt_v1beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1SaasservicemgmtV1beta1UnitKind } from "./local-sdk/google_saasservicemgmt_v1beta1/sdk/typescript/google_saasservicemgmt_v1beta1/saasservicemgmt/v1beta1_saasservicemgmt_v1beta1_unit_kind.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 unit kind" });

      resource(V1beta1SaasservicemgmtV1beta1UnitKind, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_saasservicemgmt_v1beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_saasservicemgmt_v1beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_saasservicemgmt_v1beta1.saasservicemgmt import V1beta1SaasservicemgmtV1beta1UnitKind, V1beta1SaasservicemgmtV1beta1UnitKindConfig

    def describe():
        ubx.intent("example own v1beta1 unit kind")

        ubx.resource(V1beta1SaasservicemgmtV1beta1UnitKind, "example", V1beta1SaasservicemgmtV1beta1UnitKindConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_saasservicemgmt_v1beta1_unit_kind called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Optional. Annotations is an unstructured key-value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/user-guide/annotations Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="app_params" type="object">
  AppParams contains the parameters for creating an AppHub Application. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="group" type="string">
      Optional. Grouping used to construct the name of the AppHub Application. Multiple UnitKinds can specify the same group to use the same Application across their respective units. Corresponds to the app_boundary_id in the ADC composite ApplicationTemplate. Defaults to UnitKind.name Optional.
    </ResponseField>
    <ResponseField name="scope" type="object">
      Scope of an application. Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string">
          Required. Scope Type. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="application_template_component" type="object">
  ComponentRef represents a reference to a component resource. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="component" type="string">
      Optional. Name of the component in composite.Components Optional.
    </ResponseField>
    <ResponseField name="composite_ref" type="object">
      CompositeRef represents a reference to a composite resource. Optional.
      <Expandable title="properties">
        <ResponseField name="application_template" type="string">
          Required. Reference to the ApplicationTemplate resource. Optional.
        </ResponseField>
        <ResponseField name="revision" type="string">
          Optional. Revision of the ApplicationTemplate to use. Changes to revision will trigger manual resynchronization. If empty, ApplicationTemplate will be ignored. Optional.
        </ResponseField>
        <ResponseField name="sync_operation" type="string">
          Output only. Reference to on-going AppTemplate import and replication operation (i.e. the operation_id for the long-running operation). This field is opaque for external usage. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="revision" type="string">
      Optional. Revision of the component. If the component does not have a revision, this field will be explicitly set to the revision of the composite ApplicationTemplate. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="boundary_type" type="string">
  Optional. Output only. BoundaryType describes the type of boundary the Unit Kind represents. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_flag_revisions" type="list(string)">
  Optional. Default revisions of flags for this UnitKind. Newly created units will use the flag default_flag_revisions present at the time of creation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_release" type="string">
  Optional. A reference to the Release object to use as default for creating new units of this UnitKind (optional). If not specified, a new unit must explicitly reference which release to use for its creation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="dependencies" type="list(object)">
  Optional. Immutable. List of other unit kinds that this release will depend on. Dependencies will be automatically provisioned if not found. Maximum 10. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="alias" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="unit_kind" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. An opaque value that uniquely identifies a version or generation of a resource. It can be used to confirm that the client and server agree on the ordering of a resource being written. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="input_variable_mappings" type="list(object)">
  Optional. List of inputVariables for this release that will either be retrieved from a dependency's outputVariables, or will be passed on to a dependency's inputVariables. Maximum 100. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="from" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="dependency" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="output_variable" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="to" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="dependency" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="ignore_for_lookup" type="bool">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="input_variable" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="variable" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. The labels on the resource, which can be used for categorization. similar to Kubernetes resource labels. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name (full URI of the resource) following the standard naming scheme: "projects/&#123;project&#125;/locations/&#123;location&#125;/unitKinds/&#123;unitKind&#125;" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="output_variable_mappings" type="list(object)">
  Optional. List of outputVariables for this unit kind will be passed to this unit's outputVariables. Maximum 100. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="from" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="dependency" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="output_variable" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="to" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="dependency" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="ignore_for_lookup" type="bool">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="input_variable" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="variable" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="saas" type="string">
  Required. Immutable. A reference to the Saas that defines the product (managed service) that the producer wants to manage with App Lifecycle Manager. Part of the App Lifecycle Manager common data model. Immutable once set. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The unique identifier of the resource. UID is unique in the time and space for this resource within the scope of the service. It is typically generated by the server on successful creation of a resource and must not be changed. UID is used to uniquely identify resources with resource name reuses. This should be a UUID4. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when the resource was last updated. Any change to the resource made by users must refresh this value. Changes to a resource made by the service should refresh this value. Optional; if omitted, computed by Google Cloud.
</ResponseField>
