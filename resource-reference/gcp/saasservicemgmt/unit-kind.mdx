---
title: "google_saasservicemgmt_unit_kind"
description: "`google_saasservicemgmt_unit_kind` classifies a `google_saasservicemgmt_unit` in App Lifecycle Manager..."
---

`google_saasservicemgmt_unit_kind` classifies a `google_saasservicemgmt_unit` in App Lifecycle Manager, the real category a `google_saasservicemgmt_rollout` targets (optionally with additional filters) when deciding which units to update.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_saasservicemgmt --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_saasservicemgmt/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	saasservicemgmt "github.com/ubiquex/ubx-sdk-google/sdk/go/google_saasservicemgmt/saasservicemgmt"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own unit kind"})

    		ubx.Resource(saasservicemgmt.SaasservicemgmtUnitKind, "ci-runner", saasservicemgmt.SaasservicemgmtUnitKindConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_saasservicemgmt --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SaasservicemgmtUnitKind } from "./local-sdk/google_saasservicemgmt/sdk/typescript/google_saasservicemgmt/saasservicemgmt/saasservicemgmt_unit_kind.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own unit kind" });

      resource(SaasservicemgmtUnitKind, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_saasservicemgmt --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_saasservicemgmt/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_saasservicemgmt.saasservicemgmt import SaasservicemgmtUnitKind, SaasservicemgmtUnitKindConfig

    def describe():
        ubx.intent("payments own unit kind")

        ubx.resource(SaasservicemgmtUnitKind, "ci-runner", SaasservicemgmtUnitKindConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_saasservicemgmt_unit_kind called "ci-runner", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Optional. Annotations is an unstructured key-value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/user-guide/annotations Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="boundary_type" type="string">
  Optional. Output only. BoundaryType describes the type of boundary the Unit Kind represents. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_flag_revisions" type="list(string)">
  Optional. Default revisions of flags for this UnitKind. Newly created units will use the flag default_flag_revisions present at the time of creation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_release" type="string">
  Optional. A reference to the Release object to use as default for creating new units of this UnitKind (optional). If not specified, a new unit must explicitly reference which release to use for its creation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="dependencies" type="list(object)">
  Optional. Immutable. List of other unit kinds that this release will depend on. Dependencies will be automatically provisioned if not found. Maximum 10. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="alias" type="string">
      A real, local alias this unit uses to refer to a dependency, distinct from the dependency's own real resource name. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="unit_kind" type="string">
      A reference to the real `google_saasservicemgmt_unit_kind` this dependency's own units are drawn from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. An opaque value that uniquely identifies a version or generation of a resource. It can be used to confirm that the client and server agree on the ordering of a resource being written. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="input_variable_mappings" type="list(object)">
  Optional. List of inputVariables for this release that will either be retrieved from a dependency's outputVariables, or will be passed on to a dependency's inputVariables. Maximum 100. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="from" type="object">
      The real, source side of an input variable mapping, referencing a dependency's own output variable. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dependency" type="string">
          The real, named dependency this input variable mapping's own `from` side reads from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="output_variable" type="string">
          The real, named output variable exposed by this mapping's own `dependency`, whose value feeds the mapped input. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="to" type="object">
      The real, destination side of an input variable mapping, naming which of this unit kind's own input variables receives the mapped value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dependency" type="string">
          The real, named dependency this input variable mapping's own `from` side reads from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ignore_for_lookup" type="bool">
          Whether this mapped input variable is real, excluded from the criteria App Lifecycle Manager uses to look up matching existing units. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="input_variable" type="string">
          The real, named input variable on this unit kind that receives the mapped value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="variable" type="string">
      The real, named input variable this default value applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. The labels on the resource, which can be used for categorization. similar to Kubernetes resource labels. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name (full URI of the resource) following the standard naming scheme: "projects/&#123;project&#125;/locations/&#123;location&#125;/unitKinds/&#123;unitKind&#125;" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="output_variable_mappings" type="list(object)">
  Optional. List of outputVariables for this unit kind will be passed to this unit's outputVariables. Maximum 100. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="from" type="object">
      The real, source side of an input variable mapping, referencing a dependency's own output variable. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dependency" type="string">
          The real, named dependency this input variable mapping's own `from` side reads from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="output_variable" type="string">
          The real, named output variable exposed by this mapping's own `dependency`, whose value feeds the mapped input. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="to" type="object">
      The real, destination side of an input variable mapping, naming which of this unit kind's own input variables receives the mapped value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dependency" type="string">
          The real, named dependency this input variable mapping's own `from` side reads from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ignore_for_lookup" type="bool">
          Whether this mapped input variable is real, excluded from the criteria App Lifecycle Manager uses to look up matching existing units. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="input_variable" type="string">
          The real, named input variable on this unit kind that receives the mapped value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="variable" type="string">
      The real, named input variable this default value applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="saas" type="string">
  Required. Immutable. A reference to the Saas that defines the product (managed service) that the producer wants to manage with App Lifecycle Manager. Part of the App Lifecycle Manager common data model. Immutable once set. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The unique identifier of the resource. UID is unique in the time and space for this resource within the scope of the service. It is typically generated by the server on successful creation of a resource and must not be changed. UID is used to uniquely identify resources with resource name reuses. This should be a UUID4. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when the resource was last updated. Any change to the resource made by users must refresh this value. Changes to a resource made by the service should refresh this value. Optional; if omitted, computed by Google Cloud.
</ResponseField>
