---
title: "google_saasservicemgmt_unit_operation"
description: "`google_saasservicemgmt_unit_operation` is a real, tracked operation performed against a `google_saasservicemgmt_unit` in App Lifecycle Manager..."
---

`google_saasservicemgmt_unit_operation` is a real, tracked operation performed against a `google_saasservicemgmt_unit` in App Lifecycle Manager, such as a provisioning or update step carried out as part of a rollout.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	saasservicemgmt "github.com/ubiquex/ubx-sdk-google/sdk/go/google/saasservicemgmt"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own unit operation"})

    		ubx.Resource(saasservicemgmt.UnitOperation, "example", saasservicemgmt.UnitOperationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UnitOperation } from "@ubx/sdk-google/google/saasservicemgmt/unit_operation";

    export default stack("example", () => {
      intent({ summary: "example own unit operation" });

      resource(UnitOperation, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.saasservicemgmt.unit_operation import UnitOperation, UnitOperationConfig

    def describe():
        ubx.intent("example own unit operation")

        ubx.resource(UnitOperation, "example", UnitOperationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_saasservicemgmt_unit_operation called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Optional. Annotations is an unstructured key-value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/user-guide/annotations Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="cancel" type="bool">
  Optional. When true, attempt to cancel the operation. Cancellation may fail if the operation is already executing. (Optional) Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="conditions" type="list(object)">
  Optional. Output only. A set of conditions which indicate the various conditions this resource can have. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="last_transition_time" type="string">
      The real timestamp when this SaaS offering's own condition last changed status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="message" type="string">
      A real, human-readable explanation for this condition's own current status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="reason" type="string">
      A real, short, machine-readable reason code for this condition's own current status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      The real, current status (`STATUS_TRUE`, `STATUS_FALSE`, `STATUS_UNKNOWN`) of this condition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. The timestamp when the resource was marked for deletion (deletion is an asynchronous operation). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="deprovision" type="any">
  Deprovision is the unit operation that deprovision the underlying resources represented by a Unit. Can only execute if the Unit is currently provisioned. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="engine_state" type="string">
  Optional. Output only. The engine state for on-going deployment engine operation(s). This field is opaque for external usage. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="error_category" type="string">
  Optional. Output only. UnitOperationErrorCategory describe the error category. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. An opaque value that uniquely identifies a version or generation of a resource. It can be used to confirm that the client and server agree on the ordering of a resource being written. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="flag_update" type="object">
  FlagUpdate is a UnitOperation that pushes new flag values to Units. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="flag_release" type="string">
      Required. Flag release being applied by UnitOperation. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. The labels on the resource, which can be used for categorization. similar to Kubernetes resource labels. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name (full URI of the resource) following the standard naming scheme: "projects/&#123;project&#125;/locations/&#123;location&#125;/unitOperations/&#123;unitOperation&#125;" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parent_unit_operation" type="string">
  Optional. Reference to parent resource: UnitOperation. If an operation needs to create other operations as part of its workflow, each of the child operations should have this field set to the parent. This can be used for tracing. (Optional) Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="provision" type="object">
  Provision is the unit operation that provision the underlying resources represented by a Unit. Can only execute if the Unit is not currently provisioned. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="input_variables" type="list(object)">
      Optional. Set of input variables. Maximum 100. (optional) Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string">
          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The real, declared value of this input variable, matching its own `type`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="variable" type="string">
          The real, named input variable this default value applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="release" type="string">
      Optional. Reference to the Release object to use for the Unit. (optional). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rollout" type="string">
  Optional. Specifies which rollout created this Unit Operation. This cannot be modified and is used for filtering purposes only. If a dependent unit and unit operation are created as part of another unit operation, they will use the same rolloutId. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="schedule" type="object">
  A time specification to schedule the maintenance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="start_time" type="string">
      Optional. Start of operation. If not set, will be set to the start of the next window. (optional) Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Optional. Output only. UnitOperationState describes the current state of the unit operation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The unique identifier of the resource. UID is unique in the time and space for this resource within the scope of the service. It is typically generated by the server on successful creation of a resource and must not be changed. UID is used to uniquely identify resources with resource name reuses. This should be a UUID4. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="unit" type="string">
  Required. Immutable. The Unit a given UnitOperation will act upon. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when the resource was last updated. Any change to the resource made by users must refresh this value. Changes to a resource made by the service should refresh this value. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="upgrade" type="object">
  Upgrade is the unit operation that upgrades a provisioned unit, which may also include the underlying resources represented by a Unit. Can only execute if the Unit is currently provisioned. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="input_variables" type="list(object)">
      Optional. Set of input variables. Maximum 100. (optional) Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string">
          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The real, declared value of this input variable, matching its own `type`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="variable" type="string">
          The real, named input variable this default value applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="release" type="string">
      Optional. Reference to the Release object to use for the Unit. (optional). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
