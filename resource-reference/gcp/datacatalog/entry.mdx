---
title: "google_datacatalog_entry"
description: "`google_datacatalog_entry` is a single cataloged item in Data Catalog -- a table, file set..."
---

`google_datacatalog_entry` is a single cataloged item in Data Catalog -- a table, file set, or other real or logical asset -- carrying its own searchable metadata and tag(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_datacatalog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_datacatalog/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	datacatalog "github.com/ubiquex/ubx-sdk-google/sdk/go/google_datacatalog/datacatalog"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own entry"})

    		ubx.Resource(datacatalog.DatacatalogEntry, "example", datacatalog.DatacatalogEntryConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_datacatalog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatacatalogEntry } from "./local-sdk/google_datacatalog/sdk/typescript/google_datacatalog/datacatalog/datacatalog_entry.ts";

    export default stack("example", () => {
      intent({ summary: "example own entry" });

      resource(DatacatalogEntry, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_datacatalog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_datacatalog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_datacatalog.datacatalog import DatacatalogEntry, DatacatalogEntryConfig

    def describe():
        ubx.intent("example own entry")

        ubx.resource(DatacatalogEntry, "example", DatacatalogEntryConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_datacatalog_entry called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="bigquery_date_sharded_spec" type="object">
  Specification for a group of BigQuery tables with the `[prefix]YYYYMMDD` name pattern. For more information, see [Introduction to partitioned tables] (https://cloud.google.com/bigquery/docs/partitioned-tables#partitioning_versus_sharding). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="dataset" type="string">
      Output only. The Data Catalog resource name of the dataset entry the current table belongs to. For example: `projects/&#123;PROJECT_ID&#125;/locations/&#123;LOCATION&#125;/entrygroups/&#123;ENTRY_GROUP_ID&#125;/entries/&#123;ENTRY_ID&#125;`. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="latest_shard_resource" type="string">
      Output only. BigQuery resource name of the latest shard. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="shard_count" type="string">
      Output only. Total number of shards. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="table_prefix" type="string">
      Output only. The table name prefix of the shards. The name of any given shard is `[table_prefix]YYYYMMDD`. For example, for the `MyTable20180101` shard, the `table_prefix` is `MyTable`. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bigquery_table_spec" type="object">
  Describes a BigQuery table. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="table_source_type" type="string">
      Output only. The table source type. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="table_spec" type="object">
      Normal BigQuery table specification. Optional.
      <Expandable title="properties">
        <ResponseField name="grouped_entry" type="string">
          Output only. If the table is date-sharded, that is, it matches the `[prefix]YYYYMMDD` name pattern, this field is the Data Catalog resource name of the date-sharded grouped entry. For example: `projects/&#123;PROJECT_ID&#125;/locations/&#123;LOCATION&#125;/entrygroups/&#123;ENTRY_GROUP_ID&#125;/entries/&#123;ENTRY_ID&#125;`. Otherwise, `grouped_entry` is empty. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="view_spec" type="object">
      Table view specification. Optional.
      <Expandable title="properties">
        <ResponseField name="view_query" type="string">
          Output only. The query that defines the table view. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="business_context" type="object">
  Business Context of the entry. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="contacts" type="object">
      Contact people for the entry. Optional.
      <Expandable title="properties">
        <ResponseField name="people" type="list(object)">
          The list of contact people for the entry. Optional.
          <Expandable title="properties">
            <ResponseField name="designation" type="string">
              A structured label categorizing this business term or its usage. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="email" type="string">
              An email address associated with this entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="entry_overview" type="object">
      Entry overview fields for rich text descriptions of entries. Optional.
      <Expandable title="properties">
        <ResponseField name="overview" type="string">
          Entry overview with support for rich text. The overview must only contain Unicode characters, and should be formatted using HTML. The maximum length is 10 MiB as this value holds HTML descriptions including encoded images. The maximum length of the text without images is 100 KiB. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cloud_bigtable_system_spec" type="object">
  Specification that applies to all entries that are part of `CLOUD_BIGTABLE` system (user_specified_type) Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="instance_display_name" type="string">
      Display name of the Instance. This is user specified and different from the resource name. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_source" type="object">
  Physical location of an entry. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="resource" type="string">
      Full name of a resource as defined by the service. For example: `//bigquery.googleapis.com/projects/&#123;PROJECT_ID&#125;/locations/&#123;LOCATION&#125;/datasets/&#123;DATASET_ID&#125;/tables/&#123;TABLE_ID&#125;` Optional.
    </ResponseField>
    <ResponseField name="service" type="string">
      Service that physically stores the data. Optional.
    </ResponseField>
    <ResponseField name="source_entry" type="string">
      Output only. Data Catalog entry name, if applicable. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="storage_properties" type="object">
      Details the properties of the underlying storage. Optional.
      <Expandable title="properties">
        <ResponseField name="file_pattern" type="list(string)">
          Patterns to identify a set of files for this fileset. Examples of a valid `file_pattern`: * `gs://bucket_name/dir/*`: matches all files in the `bucket_name/dir` directory * `gs://bucket_name/dir/**`: matches all files in the `bucket_name/dir` and all subdirectories recursively * `gs://bucket_name/file*`: matches files prefixed by `file` in `bucket_name` * `gs://bucket_name/??.txt`: matches files with two characters followed by `.txt` in `bucket_name` * `gs://bucket_name/[aeiou].txt`: matches files that contain a single vowel character followed by `.txt` in `bucket_name` * `gs://bucket_name/[a-m].txt`: matches files that contain `a`, `b`, ... or `m` followed by `.txt` in `bucket_name` * `gs://bucket_name/a/*/b`: matches all files in `bucket_name` that match the `a/*/b` pattern, such as `a/c/b`, `a/d/b` * `gs://another_bucket/a.txt`: matches `gs://another_bucket/a.txt` Optional.
        </ResponseField>
        <ResponseField name="file_type" type="string">
          File type in MIME format, for example, `text/plain`. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_source_connection_spec" type="object">
  Specification that applies to a data source connection. Valid only for entries with the `DATA_SOURCE_CONNECTION` type. Only one of internal specs can be set at the time, and cannot be changed later. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="bigquery_connection_spec" type="object">
      Specification for the BigQuery connection. Optional.
      <Expandable title="properties">
        <ResponseField name="cloud_sql" type="object">
          Specification for the BigQuery connection to a Cloud SQL instance. Optional.
          <Expandable title="properties">
            <ResponseField name="database" type="string">
              The database for the datacatalog entry's cloud sql. Optional.
            </ResponseField>
            <ResponseField name="instance_id" type="string">
              Cloud SQL instance ID in the format of `project:location:instance`. Optional.
            </ResponseField>
            <ResponseField name="type" type="string">
              Type of the Cloud SQL database. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="connection_type" type="string">
          The type of the BigQuery connection. Optional.
        </ResponseField>
        <ResponseField name="has_credential" type="bool">
          True if there are credentials attached to the BigQuery connection; false otherwise. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="database_table_spec" type="object">
  Specification that applies to a table resource. Valid only for entries with the `TABLE` type. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="database_view_spec" type="object">
      Specification that applies to database view. Optional.
      <Expandable title="properties">
        <ResponseField name="base_table" type="string">
          Name of a singular table this view reflects one to one. Optional.
        </ResponseField>
        <ResponseField name="sql_query" type="string">
          SQL query used to generate this view. Optional.
        </ResponseField>
        <ResponseField name="view_type" type="string">
          Type of this view. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dataplex_table" type="object">
      Entry specification for a Dataplex Universal Catalog table. Optional.
      <Expandable title="properties">
        <ResponseField name="dataplex_spec" type="object">
          Common Dataplex Universal Catalog fields. Optional.
          <Expandable title="properties">
            <ResponseField name="asset" type="string">
              Fully qualified resource name of an asset in Dataplex Universal Catalog, to which the underlying data source (Cloud Storage bucket or BigQuery dataset) of the entity is attached. Optional.
            </ResponseField>
            <ResponseField name="compression_format" type="string">
              Compression format of the data, e.g., zip, gzip etc. Optional.
            </ResponseField>
            <ResponseField name="data_format" type="object">
              Native schema used by a resource represented as an entry. Used by query engines for deserializing and parsing source data. Optional.
              <Expandable title="properties">
                <ResponseField name="avro" type="object">
                  Schema in Avro JSON format. Optional.
                  <Expandable title="properties">
                    <ResponseField name="text" type="string">
                      JSON source of the Avro schema. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="csv" type="any">
                  Marks a CSV-encoded data source. Optional.
                </ResponseField>
                <ResponseField name="orc" type="any">
                  Marks an ORC-encoded data source. Optional.
                </ResponseField>
                <ResponseField name="parquet" type="any">
                  Marks a Parquet-encoded data source. Optional.
                </ResponseField>
                <ResponseField name="protobuf" type="object">
                  Schema in protocol buffer format. Optional.
                  <Expandable title="properties">
                    <ResponseField name="text" type="string">
                      Protocol buffer source of the schema. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="thrift" type="object">
                  Schema in Thrift format. Optional.
                  <Expandable title="properties">
                    <ResponseField name="text" type="string">
                      Thrift IDL source of the schema. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Project ID of the underlying Cloud Storage or BigQuery data. Note that this may not be the same project as the corresponding Dataplex Universal Catalog lake / zone / asset. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="external_tables" type="list(object)">
          List of external tables registered by Dataplex Universal Catalog in other systems based on the same underlying data. External tables allow to query this data in those systems. Optional.
          <Expandable title="properties">
            <ResponseField name="data_catalog_entry" type="string">
              A reference to the linked Data Catalog entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="fully_qualified_name" type="string">
              The fully qualified name identifying this entry within its own source system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="google_cloud_resource" type="string">
              A reference to the underlying Google Cloud resource this entry represents. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="system" type="string">
              The external system this entry originates from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="user_managed" type="bool">
          Indicates if the table schema is managed by the user or not. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      Type of this table. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dataset_spec" type="object">
  Specification that applies to a dataset. Valid only for entries with the `DATASET` type. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="vertex_dataset_spec" type="object">
      Specification for vertex dataset resources. Optional.
      <Expandable title="properties">
        <ResponseField name="data_item_count" type="string">
          The number of DataItems in this Dataset. Only apply for non-structured Dataset. Optional.
        </ResponseField>
        <ResponseField name="data_type" type="string">
          Type of the dataset. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  Entry description that can consist of several sentences or paragraphs that describe entry contents. The description must not contain Unicode non-characters as well as C0 and C1 control codes except tabs (HT), new lines (LF), carriage returns (CR), and page breaks (FF). The maximum size is 2000 bytes when encoded in UTF-8. Default value is an empty string. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Display name of an entry. The maximum size is 500 bytes when encoded in UTF-8. Default value is an empty string. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="feature_online_store_spec" type="object">
  Detail description of the source information of a Vertex Feature Online Store. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="storage_type" type="string">
      Output only. Type of underlying storage for the FeatureOnlineStore. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fileset_spec" type="object">
  Specification that applies to a fileset. Valid only for entries with the 'FILESET' type. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="dataplex_fileset" type="object">
      Entry specification for a Dataplex Universal Catalog fileset. Optional.
      <Expandable title="properties">
        <ResponseField name="dataplex_spec" type="object">
          Common Dataplex Universal Catalog fields. Optional.
          <Expandable title="properties">
            <ResponseField name="asset" type="string">
              Fully qualified resource name of an asset in Dataplex Universal Catalog, to which the underlying data source (Cloud Storage bucket or BigQuery dataset) of the entity is attached. Optional.
            </ResponseField>
            <ResponseField name="compression_format" type="string">
              Compression format of the data, e.g., zip, gzip etc. Optional.
            </ResponseField>
            <ResponseField name="data_format" type="object">
              Native schema used by a resource represented as an entry. Used by query engines for deserializing and parsing source data. Optional.
              <Expandable title="properties">
                <ResponseField name="avro" type="object">
                  Schema in Avro JSON format. Optional.
                  <Expandable title="properties">
                    <ResponseField name="text" type="string">
                      JSON source of the Avro schema. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="csv" type="any">
                  Marks a CSV-encoded data source. Optional.
                </ResponseField>
                <ResponseField name="orc" type="any">
                  Marks an ORC-encoded data source. Optional.
                </ResponseField>
                <ResponseField name="parquet" type="any">
                  Marks a Parquet-encoded data source. Optional.
                </ResponseField>
                <ResponseField name="protobuf" type="object">
                  Schema in protocol buffer format. Optional.
                  <Expandable title="properties">
                    <ResponseField name="text" type="string">
                      Protocol buffer source of the schema. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="thrift" type="object">
                  Schema in Thrift format. Optional.
                  <Expandable title="properties">
                    <ResponseField name="text" type="string">
                      Thrift IDL source of the schema. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Project ID of the underlying Cloud Storage or BigQuery data. Note that this may not be the same project as the corresponding Dataplex Universal Catalog lake / zone / asset. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fully_qualified_name" type="string">
  [Fully Qualified Name (FQN)](https://cloud.google.com//data-catalog/docs/fully-qualified-names) of the resource. Set automatically for entries representing resources from synced systems. Settable only during creation, and read-only later. Can be used for search and lookup of the entries. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="gcs_fileset_spec" type="object">
  Describes a Cloud Storage fileset entry. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="file_patterns" type="list(string)">
      Required. Patterns to identify a set of files in Google Cloud Storage. For more information, see [Wildcard Names] (https://cloud.google.com/storage/docs/wildcards). Note: Currently, bucket wildcards are not supported. Examples of valid `file_patterns`: * `gs://bucket_name/dir/*`: matches all files in `bucket_name/dir` directory * `gs://bucket_name/dir/**`: matches all files in `bucket_name/dir` and all subdirectories * `gs://bucket_name/file*`: matches files prefixed by `file` in `bucket_name` * `gs://bucket_name/??.txt`: matches files with two characters followed by `.txt` in `bucket_name` * `gs://bucket_name/[aeiou].txt`: matches files that contain a single vowel character followed by `.txt` in `bucket_name` * `gs://bucket_name/[a-m].txt`: matches files that contain `a`, `b`, ... or `m` followed by `.txt` in `bucket_name` * `gs://bucket_name/a/*/b`: matches all files in `bucket_name` that match the `a/*/b` pattern, such as `a/c/b`, `a/d/b` * `gs://another_bucket/a.txt`: matches `gs://another_bucket/a.txt` You can combine wildcards to match complex sets of files, for example: `gs://bucket_name/[a-m]??.j*g` Optional.
    </ResponseField>
    <ResponseField name="sample_gcs_file_specs" type="list(object)">
      Output only. Sample files contained in this fileset, not all files contained in this fileset are represented here. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="file_path" type="string">
          The path to this file within its own source system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gcs_timestamps" type="object">
          Creation and modification timestamps read from Cloud Storage object metadata. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="create_time" type="string">
              Output only. The timestamp when this resource was created. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="expire_time" type="string">
              The timestamp after which this resource (or the attribute it applies to) is no longer valid. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="update_time" type="string">
              Output only. The timestamp when this resource was most recently updated. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="size_bytes" type="string">
          The size of this object, in bytes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="graph_spec" type="object">
  Specification that applies to a graph. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="edge_tables" type="list(object)">
      Optional. Edge tables of the graph. Optional.
      <Expandable title="properties">
        <ResponseField name="alias" type="string">
          An alternate name this entry is also known by. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="data_source" type="string">
          The source system this entry's own data originates from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="destination_node_reference" type="object">
          A reference to the node this edge points to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="edge_table_columns" type="list(string)">
              The column(s) identifying an edge's own source and destination nodes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_alias" type="string">
              An alternate name this graph node is also known by. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_table_columns" type="list(string)">
              The column(s) identifying this graph node's own key. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dynamic_label_column" type="string">
          The column whose own values are used as dynamic labels for this graph element. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dynamic_properties_column" type="string">
          The column whose own values populate dynamic properties for this graph element. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="element_keys" type="list(string)">
          The key(s) identifying element(s) within this schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="input_source" type="string">
          The source data this process consumed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The category of this schema element. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="label_and_properties" type="list(object)">
          The label and associated key/value properties assigned to this graph element. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="label" type="string">
              A human-readable label for this entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="properties" type="list(object)">
              The named sub-schemas an `object`-typed value's own fields must each satisfy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_node_reference" type="object">
          A reference to the node this edge originates from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="edge_table_columns" type="list(string)">
              The column(s) identifying an edge's own source and destination nodes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_alias" type="string">
              An alternate name this graph node is also known by. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_table_columns" type="list(string)">
              The column(s) identifying this graph node's own key. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      Output only. Fully qualified graph name. e.g. `named_catalog.MyGraph` Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="node_tables" type="list(object)">
      Required. Node tables of the graph. Optional.
      <Expandable title="properties">
        <ResponseField name="alias" type="string">
          An alternate name this entry is also known by. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="data_source" type="string">
          The source system this entry's own data originates from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="destination_node_reference" type="object">
          A reference to the node this edge points to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="edge_table_columns" type="list(string)">
              The column(s) identifying an edge's own source and destination nodes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_alias" type="string">
              An alternate name this graph node is also known by. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_table_columns" type="list(string)">
              The column(s) identifying this graph node's own key. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dynamic_label_column" type="string">
          The column whose own values are used as dynamic labels for this graph element. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dynamic_properties_column" type="string">
          The column whose own values populate dynamic properties for this graph element. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="element_keys" type="list(string)">
          The key(s) identifying element(s) within this schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="input_source" type="string">
          The source data this process consumed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The category of this schema element. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="label_and_properties" type="list(object)">
          The label and associated key/value properties assigned to this graph element. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="label" type="string">
              A human-readable label for this entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="properties" type="list(object)">
              The named sub-schemas an `object`-typed value's own fields must each satisfy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_node_reference" type="object">
          A reference to the node this edge originates from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="edge_table_columns" type="list(string)">
              The column(s) identifying an edge's own source and destination nodes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_alias" type="string">
              An alternate name this graph node is also known by. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_table_columns" type="list(string)">
              The column(s) identifying this graph node's own key. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="integrated_system" type="string">
  Output only. Indicates the entry's source system that Data Catalog integrates with, such as BigQuery, Pub/Sub, or Dataproc Metastore. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Cloud labels attached to the entry. In Data Catalog, you can create and modify labels attached only to custom entries. Synced entries have unmodifiable labels that come from the source system. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="linked_resource" type="string">
  The resource this metadata entry refers to. For Google Cloud Platform resources, `linked_resource` is the [Full Resource Name] (https://cloud.google.com/apis/design/resource_names#full_resource_name). For example, the `linked_resource` for a table resource from BigQuery is: `//bigquery.googleapis.com/projects/&#123;PROJECT_ID&#125;/datasets/&#123;DATASET_ID&#125;/tables/&#123;TABLE_ID&#125;` Output only when the entry is one of the types in the `EntryType` enum. For entries with a `user_specified_type`, this field is optional and defaults to an empty string. The resource string must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), periods (.), colons (:), slashes (/), dashes (-), and hashes (#). The maximum size is 200 bytes when encoded in UTF-8. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="looker_system_spec" type="object">
  Specification that applies to entries that are part `LOOKER` system (user_specified_type) Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="parent_instance_display_name" type="string">
      Name of the parent Looker Instance. Empty if it does not exist. Optional.
    </ResponseField>
    <ResponseField name="parent_instance_id" type="string">
      ID of the parent Looker Instance. Empty if it does not exist. Example value: `someinstance.looker.com` Optional.
    </ResponseField>
    <ResponseField name="parent_model_display_name" type="string">
      Name of the parent Model. Empty if it does not exist. Optional.
    </ResponseField>
    <ResponseField name="parent_model_id" type="string">
      ID of the parent Model. Empty if it does not exist. Optional.
    </ResponseField>
    <ResponseField name="parent_view_display_name" type="string">
      Name of the parent View. Empty if it does not exist. Optional.
    </ResponseField>
    <ResponseField name="parent_view_id" type="string">
      ID of the parent View. Empty if it does not exist. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="model_spec" type="object">
  Specification that applies to a model. Valid only for entries with the `MODEL` type. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="vertex_model_spec" type="object">
      Specification for vertex model resources. Optional.
      <Expandable title="properties">
        <ResponseField name="container_image_uri" type="string">
          URI of the Docker image to be used as the custom container for serving predictions. Optional.
        </ResponseField>
        <ResponseField name="version_aliases" type="list(string)">
          User provided version aliases so that a model version can be referenced via alias Optional.
        </ResponseField>
        <ResponseField name="version_description" type="string">
          The description of this version. Optional.
        </ResponseField>
        <ResponseField name="version_id" type="string">
          The version ID of the model. Optional.
        </ResponseField>
        <ResponseField name="vertex_model_source_info" type="object">
          Detail description of the source information of a Vertex model. Optional.
          <Expandable title="properties">
            <ResponseField name="copy" type="bool">
              If this Model is copy of another Model. If true then source_type pertains to the original. Optional.
            </ResponseField>
            <ResponseField name="source_type" type="string">
              Type of the model source. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. The resource name of an entry in URL format. Note: The entry itself and its child resources might not be stored in the location specified in its name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="personal_details" type="object">
  Entry metadata relevant only to the user and private to them. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="star_time" type="string">
      Set if the entry is starred; unset otherwise. Optional.
    </ResponseField>
    <ResponseField name="starred" type="bool">
      True if the entry is starred by the user; false otherwise. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="routine_spec" type="object">
  Specification that applies to a routine. Valid only for entries with the `ROUTINE` type. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="bigquery_routine_spec" type="object">
      Fields specific for BigQuery routines. Optional.
      <Expandable title="properties">
        <ResponseField name="imported_libraries" type="list(string)">
          Paths of the imported libraries. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="definition_body" type="string">
      The body of the routine. Optional.
    </ResponseField>
    <ResponseField name="language" type="string">
      The language the routine is written in. The exact value depends on the source system. For BigQuery routines, possible values are: * `SQL` * `JAVASCRIPT` Optional.
    </ResponseField>
    <ResponseField name="return_type" type="string">
      Return type of the argument. The exact value depends on the source system and the language. Optional.
    </ResponseField>
    <ResponseField name="routine_arguments" type="list(object)">
      Arguments of the routine. Optional.
      <Expandable title="properties">
        <ResponseField name="mode" type="string">
          Whether this field is `REQUIRED`, `NULLABLE`, or `REPEATED`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="routine_type" type="string">
      The type of the routine. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="schema" type="object">
  Represents a schema, for example, a BigQuery, GoogleSQL, or Avro schema. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="columns" type="list(object)">
      The unified GoogleSQL-like schema of columns. The overall maximum number of columns and nested columns is 10,000. The maximum nested depth is 15 levels. Optional.
      <Expandable title="properties">
        <ResponseField name="column" type="string">
          A reference to a specific column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="default_value" type="string">
          The value used when none is explicitly provided. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gc_rule" type="string">
          The garbage-collection rule applied to this Bigtable column family. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="highest_indexing_type" type="string">
          The strongest indexing level detected for this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="looker_column_spec" type="object">
          Metadata specific to a column sourced from Looker. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="mode" type="string">
          Whether this field is `REQUIRED`, `NULLABLE`, or `REPEATED`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ordinal_position" type="number">
          This column's own position within its containing schema. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="range_element_type" type="object">
          The data type of elements within this range-typed field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="subcolumns" type="list(any)">
          The nested column(s) contained within this column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_spec" type="object">
  Specification that applies to a Service resource. Valid only for entries with the `SERVICE` type. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cloud_bigtable_instance_spec" type="object">
      Specification that applies to Instance entries that are part of `CLOUD_BIGTABLE` system. (user_specified_type) Optional.
      <Expandable title="properties">
        <ResponseField name="cloud_bigtable_cluster_specs" type="list(object)">
          The list of clusters for the Instance. Optional.
          <Expandable title="properties">
            <ResponseField name="display_name" type="string">
              A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="linked_resource" type="string">
              A reference to the resource this entry represents. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="location" type="string">
              The Google Cloud region this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_system_timestamps" type="object">
  Timestamps associated with this resource in a particular system. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="create_time" type="string">
      Creation timestamp of the resource within the given system. Optional.
    </ResponseField>
    <ResponseField name="expire_time" type="string">
      Output only. Expiration timestamp of the resource within the given system. Currently only applicable to BigQuery resources. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Timestamp of the last modification of the resource or its metadata within a given system. Note: Depending on the source system, not every modification updates this timestamp. For example, BigQuery timestamps every metadata modification but not data or permission changes. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="spanner_table_spec" type="object">
  Specification of a Spanner table. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="foreign_keys" type="list(object)">
      Output only. The foreign keys of the table. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="column_mappings" type="list(object)">
          The mapping of source column(s) to this lineage process's own inputs and outputs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="column" type="string">
              A reference to a specific column. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="reference_column" type="string">
              The column this reference points to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="entry" type="string">
          A reference to the Data Catalog entry this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="primary_key" type="object">
      Specification of a Spanner primary key. Optional.
      <Expandable title="properties">
        <ResponseField name="columns" type="list(string)">
          Output only. Column names of the primary key. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sql_database_system_spec" type="object">
  Specification that applies to entries that are part `SQL_DATABASE` system (user_specified_type) Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="database_version" type="string">
      Version of the database engine. Optional.
    </ResponseField>
    <ResponseField name="instance_host" type="string">
      Host of the SQL database enum InstanceHost &#123; UNDEFINED = 0; SELF_HOSTED = 1; CLOUD_SQL = 2; AMAZON_RDS = 3; AZURE_SQL = 4; &#125; Host of the enclousing database instance. Optional.
    </ResponseField>
    <ResponseField name="sql_engine" type="string">
      SQL Database Engine. enum SqlEngine &#123; UNDEFINED = 0; MY_SQL = 1; POSTGRE_SQL = 2; SQL_SERVER = 3; &#125; Engine of the enclosing database instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of the entry. For details, see [`EntryType`](#entrytype). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="usage_signal" type="object">
  The set of all usage signals that Data Catalog stores. Note: Usually, these signals are updated daily. In rare cases, an update may fail but will be performed again on the next day. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="common_usage_within_time_range" type="map(object)">
      Common usage statistics over each of the predefined time ranges. Supported time ranges are `&#123;"24H", "7D", "30D", "Lifetime"&#125;`. Optional.
      <Expandable title="properties">
        <ResponseField name="view_count" type="string">
          The number of times this resource has been viewed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="favorite_count" type="string">
      Favorite count in the source system. Optional.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      The end timestamp of the duration of usage statistics. Optional.
    </ResponseField>
    <ResponseField name="usage_within_time_range" type="map(object)">
      Output only. BigQuery usage statistics over each of the predefined time ranges. Supported time ranges are `&#123;"24H", "7D", "30D"&#125;`. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="total_cancellations" type="number">
          The total number of times this task was cancelled. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="total_completions" type="number">
          The total number of times this task completed successfully. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="total_execution_time_for_completions_millis" type="number">
          The cumulative execution time, in milliseconds, across all successful completions of this task. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="total_failures" type="number">
          The total number of times this task failed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_specified_system" type="string">
  Indicates the entry's source system that Data Catalog doesn't automatically integrate with. The `user_specified_system` string has the following limitations: * Is case insensitive. * Must begin with a letter or underscore. * Can only contain letters, numbers, and underscores. * Must be at least 1 character and at most 64 characters long. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="user_specified_type" type="string">
  Custom entry type that doesn't match any of the values allowed for input and listed in the `EntryType` enum. When creating an entry, first check the type values in the enum. If there are no appropriate types for the new entry, provide a custom value, for example, `my_special_type`. The `user_specified_type` string has the following limitations: * Is case insensitive. * Must begin with a letter or underscore. * Can only contain letters, numbers, and underscores. * Must be at least 1 character and at most 64 characters long. Optional; if omitted, computed by Google Cloud.
</ResponseField>
