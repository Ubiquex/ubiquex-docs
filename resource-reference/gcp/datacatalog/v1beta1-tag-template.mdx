---
title: "google_datacatalog_v1beta1_tag_template"
description: "`google_datacatalog_v1beta1_tag_template` defines the schema (field names and types) for a reusable kind of tag that can be attached to Data Catalog..."
---

`google_datacatalog_v1beta1_tag_template` defines the schema (field names and types) for a reusable kind of tag that can be attached to Data Catalog entries, e.g. a `data_governance` tag with an `owner` field.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	datacatalog "github.com/ubiquex/ubx-sdk-google/sdk/go/google/datacatalog"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 tag template"})

    		ubx.Resource(datacatalog.V1beta1TagTemplate, "example", datacatalog.V1beta1TagTemplateConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1TagTemplate } from "@ubx/sdk-google/google/datacatalog/v1beta1_tag_template";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 tag template" });

      resource(V1beta1TagTemplate, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.datacatalog import V1beta1TagTemplate, V1beta1TagTemplateConfig

    def describe():
        ubx.intent("example own v1beta1 tag template")

        ubx.resource(V1beta1TagTemplate, "example", V1beta1TagTemplateConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_datacatalog_v1beta1_tag_template called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="dataplex_transfer_status" type="string">
  Output only. Transfer status of the TagTemplate Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name for this template. Defaults to an empty string. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fields" type="map(object)">
  Required. Map of tag template field IDs to the settings for the field. This map is an exhaustive list of the allowed fields. This map must contain at least one field and at most 500 fields. The keys to this map are tag template field IDs. Field IDs can contain letters (both uppercase and lowercase), numbers (0-9) and underscores (_). Field IDs must be at least 1 character long and at most 64 characters long. Field IDs must start with a letter or underscore. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="is_required" type="bool">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="order" type="number">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="type" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="enum_type" type="object">
          Optional; if omitted, computed by Google Cloud.
          <Expandable title="properties">
            <ResponseField name="allowed_values" type="list(object)">
              Optional; if omitted, computed by Google Cloud.
              <Expandable title="properties">
                <ResponseField name="display_name" type="string">
                  Optional; if omitted, computed by Google Cloud.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="primitive_type" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the tag template in URL format. Example: * projects/&#123;project_id&#125;/locations/&#123;location&#125;/tagTemplates/&#123;tag_template_id&#125; Note that this TagTemplate and its child resources may not actually be stored in the location in this name. Optional; if omitted, computed by Google Cloud.
</ResponseField>
