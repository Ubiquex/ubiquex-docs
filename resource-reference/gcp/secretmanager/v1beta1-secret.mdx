---
title: "google_secretmanager_v1beta1_secret"
description: "`google_secretmanager_v1beta1_secret` is a named secret in Secret Manager, that version(s) holding its own actual sensitive value are created under."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_secretmanager_secret`.
</Warning>

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_secretmanager_v1beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_secretmanager_v1beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	secretmanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google_secretmanager_v1beta1/secretmanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 secret"})

    		ubx.Resource(secretmanager.V1beta1SecretmanagerV1beta1Secret, "example", secretmanager.V1beta1SecretmanagerV1beta1SecretConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_secretmanager_v1beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1SecretmanagerV1beta1Secret } from "./local-sdk/google_secretmanager_v1beta1/sdk/typescript/google_secretmanager_v1beta1/secretmanager/v1beta1_secretmanager_v1beta1_secret.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 secret" });

      resource(V1beta1SecretmanagerV1beta1Secret, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_secretmanager_v1beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_secretmanager_v1beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_secretmanager_v1beta1.secretmanager import V1beta1SecretmanagerV1beta1Secret, V1beta1SecretmanagerV1beta1SecretConfig

    def describe():
        ubx.intent("example own v1beta1 secret")

        ubx.resource(V1beta1SecretmanagerV1beta1Secret, "example", V1beta1SecretmanagerV1beta1SecretConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_secretmanager_v1beta1_secret called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The time at which the Secret was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  The labels assigned to this Secret. Label keys must be between 1 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes, and must conform to the following PCRE regular expression: `\p&#123;Ll&#125;\p&#123;Lo&#125;&#123;0,62&#125;` Label values must be between 0 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes, and must conform to the following PCRE regular expression: `[\p&#123;Ll&#125;\p&#123;Lo&#125;\p&#123;N&#125;_-]&#123;0,63&#125;` No more than 64 labels can be assigned to a given resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource name of the Secret in the format `projects/*/secrets/*`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="replication" type="object">
  A policy that defines the replication configuration of data. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="automatic" type="any">
      A replication policy that replicates the Secret payload without any restrictions. Optional.
    </ResponseField>
    <ResponseField name="user_managed" type="object">
      A replication policy that replicates the Secret payload into the locations specified in Replication.UserManaged.replicas Optional.
      <Expandable title="properties">
        <ResponseField name="replicas" type="list(object)">
          Required. The list of Replicas for this Secret. Cannot be empty. Optional.
          <Expandable title="properties">
            <ResponseField name="location" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional. Input only. Immutable. Mapping of Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" Tags are used to organize and group resources. Tags can be used to control policy evaluation for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>
