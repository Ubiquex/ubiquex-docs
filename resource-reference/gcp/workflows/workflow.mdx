---
title: "google_workflows_workflow"
description: "Real, generated bindings for `google_workflows_workflow`."
---

`google_workflows_workflow` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workflows "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/workflows"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workflow"})

    		ubx.Resource(workflows.Workflow, "ci-runner", workflows.WorkflowConfig{
    			Name:                  "ci-runner",
    			CallLogLevel:          "example",
    			CryptoKeyName:         "example-crypto-key-name",
    			DeletionProtection:    true,
    			ExecutionHistoryLevel: "example",
    			Labels:                map[string]string{"managed-by": "ubx"},
    			Region:                "example",
    			SourceContents:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Workflow } from "jsr:@ubx/sdk-google/google/workflows/workflow";

    export default stack("payments", () => {
      intent({ summary: "payments own workflow" });

      resource(Workflow, "ci-runner", {
        name: "ci-runner",
        callLogLevel: "example",
        cryptoKeyName: "example-crypto-key-name",
        deletionProtection: true,
        executionHistoryLevel: "example",
        labels: { "managed-by": "ubx" },
        region: "example",
        sourceContents: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.workflows import Workflow, WorkflowConfig

    def describe():
        ubx.intent("payments own workflow")

        ubx.resource(Workflow, "ci-runner", WorkflowConfig(
            name="ci-runner",
            call_log_level="example",
            crypto_key_name="example-crypto-key-name",
            deletion_protection=True,
            execution_history_level="example",
            labels={"managed-by": "ubx"},
            region="example",
            source_contents="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_workflows_workflow called "ci-runner", with call
    log level example, crypto key name example-crypto-key-name,
    deletion protection true, execution history level example, labels
    a set value, region example, source contents example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="call_log_level" type="string">
  Optional.
</ResponseField>

<ResponseField name="crypto_key_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_protection" type="bool">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="execution_history_level" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional.
</ResponseField>

<ResponseField name="service_account" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_contents" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_env_vars" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="revision_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="service_account" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
