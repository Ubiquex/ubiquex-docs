---
title: "google_dataform_team_folder"
description: "`google_dataform_team_folder` is a shared-drive-like Dataform folder designed for team collaboration on code assets..."
---

`google_dataform_team_folder` is a shared-drive-like Dataform folder designed for team collaboration on code assets, distinct from a personal folder scoped to a single user.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_dataform --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_dataform/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataform "github.com/ubiquex/ubx-sdk-google/sdk/go/google_dataform/dataform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own team folder"})

    		ubx.Resource(dataform.DataformTeamFolder, "example", dataform.DataformTeamFolderConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_dataform --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataformTeamFolder } from "./local-sdk/google_dataform/sdk/typescript/google_dataform/dataform/dataform_team_folder.ts";

    export default stack("example", () => {
      intent({ summary: "example own team folder" });

      resource(DataformTeamFolder, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_dataform --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_dataform/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_dataform.dataform import DataformTeamFolder, DataformTeamFolderConfig

    def describe():
        ubx.intent("example own team folder")

        ubx.resource(DataformTeamFolder, "example", DataformTeamFolderConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataform_team_folder called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The timestamp of when the TeamFolder was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="creator_iam_principal" type="string">
  Output only. The IAM principal identifier of the creator of the TeamFolder. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The TeamFolder's user-friendly name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="internal_metadata" type="string">
  Output only. All the metadata information that is used internally to serve the resource. For example: timestamps, flags, status fields, etc. The format of this field is a JSON string. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The TeamFolder's name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp of when the TeamFolder was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
