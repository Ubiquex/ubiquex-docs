---
title: "google_dataform_workspace"
description: "`google_dataform_workspace` is a personal, uncommitted development workspace within a Dataform repository..."
---

`google_dataform_workspace` is a personal, uncommitted development workspace within a Dataform repository, where a developer edits SQLX code before pushing it to the shared Git history.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_dataform --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_dataform/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataform "github.com/ubiquex/ubx-sdk-google/sdk/go/google_dataform/dataform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own workspace"})

    		ubx.Resource(dataform.DataformWorkspace, "example", dataform.DataformWorkspaceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_dataform --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataformWorkspace } from "./local-sdk/google_dataform/sdk/typescript/google_dataform/dataform/dataform_workspace.ts";

    export default stack("example", () => {
      intent({ summary: "example own workspace" });

      resource(DataformWorkspace, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_dataform --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_dataform/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_dataform.dataform import DataformWorkspace, DataformWorkspaceConfig

    def describe():
        ubx.intent("example own workspace")

        ubx.resource(DataformWorkspace, "example", DataformWorkspaceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataform_workspace called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The timestamp of when the workspace was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="data_encryption_state" type="object">
  Describes encryption state of a resource. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="kms_key_version_name" type="string">
      Required. The KMS key version name with which data of a resource is encrypted. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="disable_moves" type="bool">
  Optional. If set to true, workspaces will not be moved if its linked Repository is moved. Instead, it will be deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="internal_metadata" type="string">
  Output only. All the metadata information that is used internally to serve the resource. For example: timestamps, flags, status fields, etc. The format of this field is a JSON string. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The workspace's name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="private_resource_metadata" type="object">
  Metadata used to identify if a resource is user scoped. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="user_scoped" type="bool">
      Output only. If true, this resource is user-scoped, meaning it is either a workspace or sourced from a workspace. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
