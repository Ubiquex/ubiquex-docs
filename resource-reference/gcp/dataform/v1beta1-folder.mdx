---
title: "google_dataform_v1beta1_folder"
description: "`google_dataform_v1beta1_folder` organizes code asset(s) (repositories holding SQLX transformations, notebooks..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_dataform_folder`.
</Warning>

`google_dataform_v1beta1_folder` organizes code asset(s) (repositories holding SQLX transformations, notebooks, saved queries) into a hierarchical structure within Dataform, similar to folders in a file system.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_dataform_v1beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_dataform_v1beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataform "github.com/ubiquex/ubx-sdk-google/sdk/go/google_dataform_v1beta1/dataform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 folder"})

    		ubx.Resource(dataform.V1beta1DataformV1beta1Folder, "example", dataform.V1beta1DataformV1beta1FolderConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_dataform_v1beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1DataformV1beta1Folder } from "./local-sdk/google_dataform_v1beta1/sdk/typescript/google_dataform_v1beta1/dataform/v1beta1_dataform_v1beta1_folder.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 folder" });

      resource(V1beta1DataformV1beta1Folder, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_dataform_v1beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_dataform_v1beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_dataform_v1beta1.dataform import V1beta1DataformV1beta1Folder, V1beta1DataformV1beta1FolderConfig

    def describe():
        ubx.intent("example own v1beta1 folder")

        ubx.resource(V1beta1DataformV1beta1Folder, "example", V1beta1DataformV1beta1FolderConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataform_v1beta1_folder called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="containing_folder" type="string">
  Optional. The containing Folder resource name. This should take the format: projects/&#123;project&#125;/locations/&#123;location&#125;/folders/&#123;folder&#125;, projects/&#123;project&#125;/locations/&#123;location&#125;/teamFolders/&#123;teamFolder&#125;, or just "" if this is a root Folder. This field can only be updated through MoveFolder. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp of when the Folder was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="creator_iam_principal" type="string">
  Output only. The IAM principal identifier of the creator of the Folder. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The Folder's user-friendly name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="internal_metadata" type="string">
  Output only. All the metadata information that is used internally to serve the resource. For example: timestamps, flags, status fields, etc. The format of this field is a JSON string. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The Folder's name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="team_folder_name" type="string">
  Output only. The resource name of the TeamFolder that this Folder is associated with. This should take the format: projects/&#123;project&#125;/locations/&#123;location&#125;/teamFolders/&#123;teamFolder&#125;. If this is not set, the Folder is not associated with a TeamFolder and is a UserFolder. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp of when the Folder was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
