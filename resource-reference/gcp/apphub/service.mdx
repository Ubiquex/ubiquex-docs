---
title: "google_apphub_service"
description: "`google_apphub_service` registers an underlying Google Cloud resource (e.g. a load balancer) as a discoverable network-facing service within an App Hub..."
---

`google_apphub_service` registers an underlying Google Cloud resource (e.g. a load balancer) as a discoverable network-facing service within an App Hub application.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apphub "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apphub"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service"})

    		ubx.Resource(apphub.Service, "example", apphub.ServiceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Service } from "@ubx/sdk-google/google/apphub/service";

    export default stack("example", () => {
      intent({ summary: "example own service" });

      resource(Service, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apphub.service import Service, ServiceConfig

    def describe():
        ubx.intent("example own service")

        ubx.resource(Service, "example", ServiceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apphub_service called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="attributes" type="object">
  Consumer provided attributes. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="business_owners" type="list(object)">
      Optional. Business team that ensures user needs are met and value is delivered Optional.
      <Expandable title="properties">
        <ResponseField name="display_name" type="string">
          A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email" type="string">
          The email address associated with this identity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="criticality" type="object">
      Criticality of the Application, Service, or Workload Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string">
          Required. Criticality Type. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="developer_owners" type="list(object)">
      Optional. Developer team that owns development and coding. Optional.
      <Expandable title="properties">
        <ResponseField name="display_name" type="string">
          A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email" type="string">
          The email address associated with this identity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="environment" type="object">
      Environment of the Application, Service, or Workload Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string">
          Required. Environment Type. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="operator_owners" type="list(object)">
      Optional. Operator team that ensures runtime and operations. Optional.
      <Expandable title="properties">
        <ResponseField name="display_name" type="string">
          A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email" type="string">
          The email address associated with this identity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Create time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. User-defined description of a Service. Can have a maximum length of 2048 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="discovered_service" type="string">
  Required. Immutable. The resource name of the original discovered service. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. User-defined name for the Service. Can have a maximum length of 63 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of a Service. Format: `"projects/&#123;host-project-id&#125;/locations/&#123;location&#125;/applications/&#123;application-id&#125;/services/&#123;service-id&#125;"` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="service_properties" type="object">
  Properties of an underlying cloud resource that can comprise a Service. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="extended_metadata" type="map(object)">
      Output only. Additional metadata specific to the resource type. The key is a string that identifies the type of metadata and the value is the metadata contents specific to that type. Key format: `apphub.googleapis.com/&#123;metadataType&#125;` Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="metadata_struct" type="map(any)">
          Additional, caller-defined structured metadata attached to this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="functional_type" type="object">
      The functional type of a service or workload. Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string">
          Output only. The functional type of a service or workload. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="gcp_project" type="string">
      Output only. The service project identifier that the underlying cloud resource resides in. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="identity" type="object">
      The identity associated with a service or workload. Optional.
      <Expandable title="properties">
        <ResponseField name="principal" type="string">
          Output only. The principal of the identity. Supported formats: * `sa://my-sa@PROJECT_ID.iam.gserviceaccount.com` for GCP Service Account * `principal://POOL_ID.global.PROJECT_NUMBER.workload.id.goog/ns/NAMESPACE_ID/sa/MANAGED_IDENTITY_ID` for Managed Workload Identity Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="location" type="string">
      Output only. The location that the underlying resource resides in, for example, us-west1. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="registration_type" type="object">
      The registration type of a service. Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string">
          Output only. The registration type of a service. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="zone" type="string">
      Output only. The location that the underlying resource resides in if it is zonal, for example, us-west1-a). Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_reference" type="object">
  Reference to an underlying networking resource that can comprise a Service. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="uri" type="string">
      Output only. The underlying resource URI. For example, URI of Forwarding Rule, URL Map, and Backend Service. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. Service state. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. A universally unique identifier (UUID) for the `Service` in the UUID4 format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Update time. Optional; if omitted, computed by Google Cloud.
</ResponseField>
