---
title: "google_apphub_service_project_attachment"
description: "Real, generated bindings for `google_apphub_service_project_attachment`."
---

`google_apphub_service_project_attachment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/apphub"
    )

    cfg := apphub.ServiceProjectAttachmentConfig{
        ServiceProjectAttachmentId: "example",
        ServiceProject: "example",
    }
    ubx.Resource(apphub.ServiceProjectAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceProjectAttachment } from
      "jsr:@ubx/sdk-google/google/apphub/service_project_attachment";

    resource(ServiceProjectAttachment, "example", {
      serviceProjectAttachmentId: "example",
      serviceProject: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.apphub import (
        ServiceProjectAttachment,
        ServiceProjectAttachmentConfig,
    )

    cfg = ServiceProjectAttachmentConfig(
        service_project_attachment_id="example",
        service_project="example",
    )
    ubx.resource(ServiceProjectAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_apphub_service_project_attachment with
service_project_attachment_id, service_project.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_project" type="string">
  Optional.
</ResponseField>

<ResponseField name="service_project_attachment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Computed by GCP after `ship`.
</ResponseField>
