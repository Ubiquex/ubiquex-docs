---
title: "google_apihub_runtime_project_attachment"
description: "Real, generated bindings for `google_apihub_runtime_project_attachment`."
---

`google_apihub_runtime_project_attachment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apihub "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apihub"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own runtime project attachment"})

    		ubx.Resource(apihub.RuntimeProjectAttachment, "ci-runner", apihub.RuntimeProjectAttachmentConfig{
    			Location:                   "example",
    			RuntimeProject:             "example",
    			RuntimeProjectAttachmentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuntimeProjectAttachment } from "jsr:@ubx/sdk-google/google/apihub/runtime_project_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own runtime project attachment" });

      resource(RuntimeProjectAttachment, "ci-runner", {
        location: "example",
        runtimeProject: "example",
        runtimeProjectAttachmentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apihub import RuntimeProjectAttachment, RuntimeProjectAttachmentConfig

    def describe():
        ubx.intent("payments own runtime project attachment")

        ubx.resource(RuntimeProjectAttachment, "ci-runner", RuntimeProjectAttachmentConfig(
            location="example",
            runtime_project="example",
            runtime_project_attachment_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apihub_runtime_project_attachment, with location
    example, runtime project example, runtime project attachment id
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="runtime_project" type="string" required>
  Required.
</ResponseField>

<ResponseField name="runtime_project_attachment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
