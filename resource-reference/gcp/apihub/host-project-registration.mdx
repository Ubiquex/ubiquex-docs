---
title: "google_apihub_host_project_registration"
description: "Real, generated bindings for `google_apihub_host_project_registration`."
---

`google_apihub_host_project_registration` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apihub "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apihub"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own host project registration"})

    		ubx.Resource(apihub.HostProjectRegistration, "ci-runner", apihub.HostProjectRegistrationConfig{
    			GcpProject:                "example",
    			HostProjectRegistrationId: "example",
    			Location:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HostProjectRegistration } from "jsr:@ubx/sdk-google/google/apihub/host_project_registration";

    export default stack("payments", () => {
      intent({ summary: "payments own host project registration" });

      resource(HostProjectRegistration, "ci-runner", {
        gcpProject: "example",
        hostProjectRegistrationId: "example",
        location: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apihub import HostProjectRegistration, HostProjectRegistrationConfig

    def describe():
        ubx.intent("payments own host project registration")

        ubx.resource(HostProjectRegistration, "ci-runner", HostProjectRegistrationConfig(
            gcp_project="example",
            host_project_registration_id="example",
            location="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apihub_host_project_registration, with gcp project
    example, host project registration id example, location example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="gcp_project" type="string" required>
  Required. Immutable. Google cloud project name in the format: "projects/abc" or "projects/123". As input, project name with either project id or number are accepted. As output, this field will contain project number. Required.
</ResponseField>

<ResponseField name="host_project_registration_id" type="string" required>
  Required. The ID to use for the Host Project Registration, which will become the final component of the host project registration's resource name. The ID must be the same as the Google cloud project specified in the host_project_registration.gcp_project field. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Part of 'parent'. See documentation of 'projectsId'. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Output only. The time at which the host project registration was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The name of the host project registration. Format: "projects/&#123;project&#125;/locations/&#123;location&#125;/hostProjectRegistrations/&#123;host_project_registration&#125;". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
