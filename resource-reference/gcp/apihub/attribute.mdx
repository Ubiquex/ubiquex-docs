---
title: "google_apihub_attribute"
description: "`google_apihub_attribute` defines a custom metadata field (e.g. a maturity level or a team owner) that can be attached to APIs, versions..."
---

`google_apihub_attribute` defines a custom metadata field (e.g. a maturity level or a team owner) that can be attached to APIs, versions, or other API hub entities for organization-specific classification.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_apihub --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_apihub/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apihub "github.com/ubiquex/ubx-sdk-google/sdk/go/google_apihub/apihub"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own attribute"})

    		ubx.Resource(apihub.ApihubAttribute, "ci-runner", apihub.ApihubAttributeConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_apihub --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApihubAttribute } from "./local-sdk/google_apihub/sdk/typescript/google_apihub/apihub/apihub_attribute.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own attribute" });

      resource(ApihubAttribute, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_apihub --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_apihub/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_apihub.apihub import ApihubAttribute, ApihubAttributeConfig

    def describe():
        ubx.intent("payments own attribute")

        ubx.resource(ApihubAttribute, "ci-runner", ApihubAttributeConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apihub_attribute called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="allowed_values" type="list(object)">
  Optional. The list of allowed values when the attribute value is of type enum. This is required when the data_type of the attribute is ENUM. The maximum number of allowed values of an attribute will be 1000. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="display_name" type="string">
      A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="string">
      An identifier for this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="immutable" type="bool">
      Whether this custom attribute's own value can't be changed after creation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cardinality" type="number">
  Optional. The maximum number of values that the attribute can have when associated with an API Hub resource. Cardinality 1 would represent a single-valued attribute. It must not be less than 1 or greater than 20. If not specified, the cardinality would be set to 1 by default and represent a single-valued attribute. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time at which the attribute was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="data_type" type="string">
  Required. The type of the data of the attribute. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="definition_type" type="string">
  Output only. The definition type of the attribute. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. The description of the attribute. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The display name of the attribute. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="mandatory" type="bool">
  Output only. When mandatory is true, the attribute is mandatory for the resource specified in the scope. Only System defined attributes can be mandatory. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The name of the attribute in the API Hub. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/attributes/&#123;attribute&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="scope" type="string">
  Required. The scope of the attribute. It represents the resource in the API Hub to which the attribute can be linked. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which the attribute was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
