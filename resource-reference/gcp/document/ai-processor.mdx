---
title: "google_document_ai_processor"
description: "Real, generated bindings for `google_document_ai_processor`."
---

`google_document_ai_processor` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/document"
    )

    cfg := document.AiProcessorConfig{
        DisplayName: "example-display-name",
        Location: "example",
        Type: "example",
        KmsKeyName: "example-kms-key-name",
    }
    ubx.Resource(document.AiProcessor, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AiProcessor } from
      "jsr:@ubx/sdk-google/google/document/ai_processor";

    resource(AiProcessor, "example", {
      displayName: "example-display-name",
      location: "example",
      type: "example",
      kmsKeyName: "example-kms-key-name",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.document import AiProcessor, AiProcessorConfig

    cfg = AiProcessorConfig(
        display_name="example-display-name",
        location="example",
        type="example",
        kms_key_name="example-kms-key-name",
    )
    ubx.resource(AiProcessor, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_document_ai_processor with display_name,
location, type, kms_key_name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
