---
title: "google_document_ai_processor_default_version"
description: "Real, generated bindings for `google_document_ai_processor_default_version`."
---

`google_document_ai_processor_default_version` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	document "github.com/ubiquex/ubx-sdk-google/sdk/go/google/document"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ai processor default version"})

    		ubx.Resource(document.AiProcessorDefaultVersion, "ci-runner", document.AiProcessorDefaultVersionConfig{
    			Processor: "example",
    			Version:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AiProcessorDefaultVersion } from "jsr:@ubx/sdk-google/google/document/ai_processor_default_version";

    export default stack("payments", () => {
      intent({ summary: "payments own ai processor default version" });

      resource(AiProcessorDefaultVersion, "ci-runner", {
        processor: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.document import AiProcessorDefaultVersion, AiProcessorDefaultVersionConfig

    def describe():
        ubx.intent("payments own ai processor default version")

        ubx.resource(AiProcessorDefaultVersion, "ci-runner", AiProcessorDefaultVersionConfig(
            processor="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_document_ai_processor_default_version, with
    processor example, version example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="processor" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
