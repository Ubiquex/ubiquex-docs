---
title: "google_document_ai_warehouse_location"
description: "Real, generated bindings for `google_document_ai_warehouse_location`."
---

`google_document_ai_warehouse_location` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	document "github.com/ubiquex/ubx-sdk-google/sdk/go/google/document"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ai warehouse location"})

    		ubx.Resource(document.AiWarehouseLocation, "ci-runner", document.AiWarehouseLocationConfig{
    			AccessControlMode:          "example",
    			DatabaseType:               "example",
    			Location:                   "example",
    			ProjectNumber:              "example",
    			DocumentCreatorDefaultRole: "example",
    			KmsKey:                     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AiWarehouseLocation } from "jsr:@ubx/sdk-google/google/document/ai_warehouse_location";

    export default stack("payments", () => {
      intent({ summary: "payments own ai warehouse location" });

      resource(AiWarehouseLocation, "ci-runner", {
        accessControlMode: "example",
        databaseType: "example",
        location: "example",
        projectNumber: "example",
        documentCreatorDefaultRole: "example",
        kmsKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.document import AiWarehouseLocation, AiWarehouseLocationConfig

    def describe():
        ubx.intent("payments own ai warehouse location")

        ubx.resource(AiWarehouseLocation, "ci-runner", AiWarehouseLocationConfig(
            access_control_mode="example",
            database_type="example",
            location="example",
            project_number="example",
            document_creator_default_role="example",
            kms_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_document_ai_warehouse_location, with access control
    mode example, database type example, location example, project
    number example, document creator default role example, kms key
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access_control_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="database_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="document_creator_default_role" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project_number" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
