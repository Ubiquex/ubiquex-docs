---
title: "google_file_backup"
description: "`google_file_backup` is a point-in-time backup of a Filestore instance's own file share, restorable to a new instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_file --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_file/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	file "github.com/ubiquex/ubx-sdk-google/sdk/go/google_file/file"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own backup"})

    		ubx.Resource(file.FileBackup, "example", file.FileBackupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_file --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FileBackup } from "./local-sdk/google_file/sdk/typescript/google_file/file/file_backup.ts";

    export default stack("example", () => {
      intent({ summary: "example own backup" });

      resource(FileBackup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_file --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_file/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_file.file import FileBackup, FileBackupConfig

    def describe():
        ubx.intent("example own backup")

        ubx.resource(FileBackup, "example", FileBackupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_file_backup called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="capacity_gb" type="string">
  Output only. Capacity of the source file share when the backup was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the backup was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the backup with 2048 characters or less. Requests with longer descriptions will be rejected. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="download_bytes" type="string">
  Output only. Amount of bytes that will be downloaded if the backup is restored. This may be different than storage bytes, since sequential backups of the same disk will share storage. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="file_system_protocol" type="string">
  Output only. The file system protocol of the source Filestore instance that this backup is created from. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kms_key" type="string">
  Immutable. KMS key name used for data encryption. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Resource labels to represent user provided metadata. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource name of the backup, in the format `projects/&#123;project_number&#125;/locations/&#123;location_id&#125;/backups/&#123;backup_id&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="source_file_share" type="string">
  Name of the file share in the source Filestore instance that the backup is created from. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="source_instance" type="string">
  The resource name of the source Filestore instance, in the format `projects/&#123;project_number&#125;/locations/&#123;location_id&#125;/instances/&#123;instance_id&#125;`, used to create this backup. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="source_instance_tier" type="string">
  Output only. The service tier of the source Filestore instance that this backup is created from. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The backup state. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="storage_bytes" type="string">
  Output only. The size of the storage used by the backup. As backups share storage, this number is expected to change with backup creation/deletion. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional. Input only. Immutable. Tag key-value pairs bound to this resource. Each key must be a namespaced name and each value a short name. Example: "123456789012/environment" : "production", "123456789013/costCenter" : "marketing" See the documentation for more information: - Namespaced name: https://cloud.google.com/resource-manager/docs/tags/tags-creating-and-managing#retrieving_tag_key - Short name: https://cloud.google.com/resource-manager/docs/tags/tags-creating-and-managing#retrieving_tag_value Optional; if omitted, computed by Google Cloud.
</ResponseField>
