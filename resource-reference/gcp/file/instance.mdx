---
title: "google_file_instance"
description: "`google_file_instance` is a Filestore instance, a fully managed NFS file server mountable by Compute Engine VMs and GKE."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	file "github.com/ubiquex/ubx-sdk-google/sdk/go/google/file"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own instance"})

    		ubx.Resource(file.Instance, "example", file.InstanceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Instance } from "@ubx/sdk-google/google/file/instance";

    export default stack("example", () => {
      intent({ summary: "example own instance" });

      resource(Instance, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.file import Instance, InstanceConfig

    def describe():
        ubx.intent("example own instance")

        ubx.resource(Instance, "example", InstanceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_file_instance called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="capacity_step_size_gb" type="string">
  Output only. The incremental increase or decrease in capacity, designated in some number of GB. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the instance was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="custom_performance_supported" type="bool">
  Output only. Indicates whether this instance supports configuring its performance. If true, the user can configure the instance's performance by using the 'performance_config' field. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="deletion_protection_enabled" type="bool">
  Optional. Indicates whether the instance is protected against deletion. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="deletion_protection_reason" type="string">
  Optional. The reason for enabling deletion protection. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the instance (2048 characters or less). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="directory_services" type="object">
  Directory Services configuration for Kerberos-based authentication. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="ldap" type="object">
      LdapConfig contains all the parameters for connecting to LDAP servers. Optional.
      <Expandable title="properties">
        <ResponseField name="domain" type="string">
          Required. The LDAP domain name in the format of `my-domain.com`. Optional.
        </ResponseField>
        <ResponseField name="groups_ou" type="string">
          Optional. The groups Organizational Unit (OU) is optional. This parameter is a hint to allow faster lookup in the LDAP namespace. In case that this parameter is not provided, Filestore instance will query the whole LDAP namespace. Optional.
        </ResponseField>
        <ResponseField name="servers" type="list(string)">
          Required. The servers names are used for specifying the LDAP servers names. The LDAP servers names can come with two formats: 1. DNS name, for example: `ldap.example1.com`, `ldap.example2.com`. 2. IP address, for example: `10.0.0.1`, `10.0.0.2`, `10.0.0.3`. All servers names must be in the same format: either all DNS names or all IP addresses. Optional.
        </ResponseField>
        <ResponseField name="users_ou" type="string">
          Optional. The users Organizational Unit (OU) is optional. This parameter is a hint to allow faster lookup in the LDAP namespace. In case that this parameter is not provided, Filestore instance will query the whole LDAP namespace. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Server-specified ETag for the instance resource to prevent simultaneous updates from overwriting each other. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="file_shares" type="list(object)">
  File system shares on the instance. For this version, only a single file share is supported. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="capacity_gb" type="string">
      The provisioned capacity, in gibibytes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="nfs_export_options" type="list(object)">
      The NFS export configuration for this file share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="access_mode" type="string">
          Whether this connection is read-only or read-write. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="anon_gid" type="string">
          The group ID mapped to anonymous or root requests, when squashing is enabled. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="anon_uid" type="string">
          The user ID mapped to anonymous or root requests, when squashing is enabled. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ip_ranges" type="list(string)">
          The IP address range(s), in CIDR notation, this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network" type="string">
          The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="squash_mode" type="string">
          How requests from root or unmapped clients are remapped. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source_backup" type="string">
      A reference to the backup this resource is created from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_backupdr_backup" type="string">
      A reference to the Backup and DR backup this resource is created from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  KMS key name used for data encryption. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Resource labels to represent user provided metadata. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="max_capacity_gb" type="string">
  Output only. The maximum capacity of the instance in GB. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="min_capacity_gb" type="string">
  Output only. The minimum capacity of the instance in GB. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource name of the instance, in the format `projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="networks" type="list(object)">
  VPC networks to which the instance is connected. For this version, only a single network is supported. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="connect_mode" type="string">
      Whether this instance is reached via direct peering or Private Service Access. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ip_addresses" type="list(string)">
      The IP address(es) assigned to this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="modes" type="list(string)">
      The mode(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="network" type="string">
      The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="psc_config" type="object">
      Configuration for connecting to this instance via Private Service Connect. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="endpoint_project" type="string">
          A reference to the project this endpoint is created in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="requested_ip_address" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reserved_ip_range" type="string">
      The IP address range reserved for this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="performance_config" type="object">
  Used for setting the performance configuration. If the user doesn't specify PerformanceConfig, automatically provision the default performance settings as described in https://cloud.google.com/filestore/docs/performance. Larger instances will be linearly set to more IOPS. If the instance's capacity is increased or decreased, its performance will be automatically adjusted upwards or downwards accordingly (respectively). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="fixed_iops" type="object">
      Fixed IOPS (input/output operations per second) parameters. Optional.
      <Expandable title="properties">
        <ResponseField name="max_iops" type="string">
          Required. Maximum IOPS. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="iops_per_tb" type="object">
      IOPS per TB. Filestore defines TB as 1024^4 bytes (TiB). Optional.
      <Expandable title="properties">
        <ResponseField name="max_iops_per_tb" type="string">
          Required. Maximum IOPS per TiB. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="performance_limits" type="object">
  The enforced performance limits, calculated from the instance's performance configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="max_iops" type="string">
      Output only. The maximum IOPS. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="max_read_iops" type="string">
      Output only. The maximum read IOPS. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="max_read_throughput_bps" type="string">
      Output only. The maximum read throughput in bytes per second. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="max_write_iops" type="string">
      Output only. The maximum write IOPS. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="max_write_throughput_bps" type="string">
      Output only. The maximum write throughput in bytes per second. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="protocol" type="string">
  Immutable. The protocol indicates the access protocol for all shares in the instance. This field is immutable and it cannot be changed after the instance has been created. Default value: `NFS_V3`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="replication" type="object">
  Optional. The configuration used to replicate an instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="replicas" type="list(object)">
      Optional. Replication configuration for the replica instance associated with this instance. Only a single replica is supported. Optional.
      <Expandable title="properties">
        <ResponseField name="last_active_sync_time" type="string">
          When this replication last successfully synced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="peer_instance" type="string">
          A reference to the replication peer instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state_reasons" type="list(string)">
          The reason(s) explaining this resource's own current state. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state_update_time" type="string">
          When this resource's own state was last updated. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="role" type="string">
      Optional. The replication role. When creating a new replica, this field must be set to `STANDBY`. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The instance state. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="status_message" type="string">
  Output only. Additional information about the instance state, if available. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="suspension_reasons" type="list(string)">
  Output only. Field indicates all the reasons the instance is in "SUSPENDED" state. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional. Input only. Immutable. Tag key-value pairs bound to this resource. Each key must be a namespaced name and each value a short name. Example: "123456789012/environment" : "production", "123456789013/costCenter" : "marketing" See the documentation for more information: - Namespaced name: https://cloud.google.com/resource-manager/docs/tags/tags-creating-and-managing#retrieving_tag_key - Short name: https://cloud.google.com/resource-manager/docs/tags/tags-creating-and-managing#retrieving_tag_value Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tier" type="string">
  The service tier of the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>
