---
title: "google_secret_manager_regional_secret_version"
description: "Real, generated bindings for `google_secret_manager_regional_secret_version`."
---

`google_secret_manager_regional_secret_version` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/secret"
    )

    cfg := secret.ManagerRegionalSecretVersionConfig{
        Secret: "example",
        SecretData: "example",
        Enabled: true,
        IsSecretDataBase64: true,
    }
    ubx.Resource(secret.ManagerRegionalSecretVersion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ManagerRegionalSecretVersion } from
      "jsr:@ubx/sdk-google/google/secret/manager_regional_secret_version";

    resource(ManagerRegionalSecretVersion, "example", {
      secret: "example",
      secretData: "example",
      enabled: true,
      isSecretDataBase64: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.secret import (
        ManagerRegionalSecretVersion,
        ManagerRegionalSecretVersionConfig,
    )

    cfg = ManagerRegionalSecretVersionConfig(
        secret="example",
        secret_data="example",
        enabled=True,
        is_secret_data_base64=True,
    )
    ubx.resource(ManagerRegionalSecretVersion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_secret_manager_regional_secret_version with
secret, secret_data, enabled, is_secret_data_base64.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="is_secret_data_base64" type="bool">
  Optional.
</ResponseField>

<ResponseField name="secret" type="string" required>
  Required.
</ResponseField>

<ResponseField name="secret_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="customer_managed_encryption" type="list(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="kms_key_version_name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="destroy_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="string">
  Computed by GCP after `ship`.
</ResponseField>
