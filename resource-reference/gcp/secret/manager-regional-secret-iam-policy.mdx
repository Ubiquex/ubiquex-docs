---
title: "google_secret_manager_regional_secret_iam_policy"
description: "Real, generated bindings for `google_secret_manager_regional_secret_iam_policy`."
---

`google_secret_manager_regional_secret_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/secret"
    )

    cfg := secret.ManagerRegionalSecretIamPolicyConfig{
        PolicyData: "example",
        SecretId: "example",
    }
    ubx.Resource(secret.ManagerRegionalSecretIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ManagerRegionalSecretIamPolicy } from
      "jsr:@ubx/sdk-google/google/secret/manager_regional_secret_iam_policy";

    resource(ManagerRegionalSecretIamPolicy, "example", {
      policyData: "example",
      secretId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.secret import (
        ManagerRegionalSecretIamPolicy,
        ManagerRegionalSecretIamPolicyConfig,
    )

    cfg = ManagerRegionalSecretIamPolicyConfig(
        policy_data="example",
        secret_id="example",
    )
    ubx.resource(ManagerRegionalSecretIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_secret_manager_regional_secret_iam_policy with
policy_data, secret_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="secret_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
