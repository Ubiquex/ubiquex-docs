---
title: "google_secret_manager_secret_iam_binding"
description: "Real, generated bindings for `google_secret_manager_secret_iam_binding`."
---

`google_secret_manager_secret_iam_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	secret "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/secret"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own manager secret iam binding"})

    		ubx.Resource(secret.ManagerSecretIamBinding, "ci-runner", secret.ManagerSecretIamBindingConfig{
    			Members:  []string{"example"},
    			Role:     "example",
    			SecretId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerSecretIamBinding } from "jsr:@ubx/sdk-google/google/secret/manager_secret_iam_binding";

    export default stack("payments", () => {
      intent({ summary: "payments own manager secret iam binding" });

      resource(ManagerSecretIamBinding, "ci-runner", {
        members: ["example"],
        role: "example",
        secretId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.secret import ManagerSecretIamBinding, ManagerSecretIamBindingConfig

    def describe():
        ubx.intent("payments own manager secret iam binding")

        ubx.resource(ManagerSecretIamBinding, "ci-runner", ManagerSecretIamBindingConfig(
            members=["example"],
            role="example",
            secret_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_secret_manager_secret_iam_binding, with members a
    set value, role example, secret id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="members" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="secret_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
