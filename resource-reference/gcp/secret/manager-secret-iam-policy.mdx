---
title: "google_secret_manager_secret_iam_policy"
description: "Real, generated bindings for `google_secret_manager_secret_iam_policy`."
---

`google_secret_manager_secret_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	secret "github.com/ubiquex/ubx-sdk-google/sdk/go/google/secret"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own manager secret iam policy"})

    		ubx.Resource(secret.ManagerSecretIamPolicy, "ci-runner", secret.ManagerSecretIamPolicyConfig{
    			PolicyData: "example",
    			SecretId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerSecretIamPolicy } from "jsr:@ubx/sdk-google/google/secret/manager_secret_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own manager secret iam policy" });

      resource(ManagerSecretIamPolicy, "ci-runner", {
        policyData: "example",
        secretId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.secret import ManagerSecretIamPolicy, ManagerSecretIamPolicyConfig

    def describe():
        ubx.intent("payments own manager secret iam policy")

        ubx.resource(ManagerSecretIamPolicy, "ci-runner", ManagerSecretIamPolicyConfig(
            policy_data="example",
            secret_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_secret_manager_secret_iam_policy, with policy data
    example, secret id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="secret_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
