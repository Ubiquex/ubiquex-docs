---
title: "google_developer_connect_git_repository_link"
description: "Real, generated bindings for `google_developer_connect_git_repository_link`."
---

`google_developer_connect_git_repository_link` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	developer "github.com/ubiquex/ubx-sdk-google/sdk/go/google/developer"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connect git repository link"})

    		ubx.Resource(developer.ConnectGitRepositoryLink, "ci-runner", developer.ConnectGitRepositoryLinkConfig{
    			CloneUri:            "example",
    			GitRepositoryLinkId: "example",
    			Location:            "example",
    			ParentConnection:    "example",
    			Annotations:         map[string]string{"managed-by": "ubx"},
    			Etag:                "example",
    			Labels:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectGitRepositoryLink } from "jsr:@ubx/sdk-google/google/developer/connect_git_repository_link";

    export default stack("payments", () => {
      intent({ summary: "payments own connect git repository link" });

      resource(ConnectGitRepositoryLink, "ci-runner", {
        cloneUri: "example",
        gitRepositoryLinkId: "example",
        location: "example",
        parentConnection: "example",
        annotations: { "managed-by": "ubx" },
        etag: "example",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.developer import ConnectGitRepositoryLink, ConnectGitRepositoryLinkConfig

    def describe():
        ubx.intent("payments own connect git repository link")

        ubx.resource(ConnectGitRepositoryLink, "ci-runner", ConnectGitRepositoryLinkConfig(
            clone_uri="example",
            git_repository_link_id="example",
            location="example",
            parent_connection="example",
            annotations={"managed-by": "ubx"},
            etag="example",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_developer_connect_git_repository_link, with clone
    uri example, git repository link id example, location example,
    parent connection example, annotations a set value, etag example,
    labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="annotations" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="clone_uri" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional.
</ResponseField>

<ResponseField name="git_repository_link_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent_connection" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_annotations" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
