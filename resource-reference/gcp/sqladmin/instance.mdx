---
title: "google_sqladmin_instance"
description: "`google_sqladmin_instance` is a Cloud SQL instance -- a fully managed MySQL, PostgreSQL, or SQL Server database server."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	sqladmin "github.com/ubiquex/ubx-sdk-google/sdk/go/google/sqladmin"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own instance"})

    		ubx.Resource(sqladmin.Instance, "example", sqladmin.InstanceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Instance } from "@ubx/sdk-google/google/sqladmin/instance";

    export default stack("example", () => {
      intent({ summary: "example own instance" });

      resource(Instance, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.sqladmin.instance import Instance, InstanceConfig

    def describe():
        ubx.intent("example own instance")

        ubx.resource(Instance, "example", InstanceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_sqladmin_instance called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="available_maintenance_versions" type="list(string)">
  Output only. List all maintenance versions applicable on the instance Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="backend_type" type="string">
  The backend type. `SECOND_GEN`: Cloud SQL database instance. `EXTERNAL`: A database server that is not managed by Google. This property is read-only; use the `tier` property in the `settings` object to determine the database type. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="connection_name" type="string">
  Connection name of the Cloud SQL instance used in connection strings. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the instance was created in [RFC 3339](https://tools.ietf.org/html/rfc3339) format, for example `2012-11-15T16:19:00.094Z`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="current_disk_size" type="string">
  The current disk usage of the instance in bytes. This property has been deprecated. Use the "cloudsql.googleapis.com/database/disk/bytes_used" metric in Cloud Monitoring API instead. Please see [this announcement](https://groups.google.com/d/msg/google-cloud-sql-announce/I_7-F9EBhT0/BtvFtdFeAgAJ) for details. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="database_center_integration_enabled" type="bool">
  Optional. If true, instance metadata is sent to the Database Center. If false, instance metadata is not sent to the Database Center. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="database_installed_version" type="string">
  Output only. Stores the current database version running on the instance including minor version such as `MYSQL_8_0_18`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="database_version" type="string">
  The database engine type and version. The `databaseVersion` field cannot be changed after instance creation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="disk_encryption_configuration" type="object">
  Disk encryption configuration for an instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="kind" type="string">
      This is always `sql#diskEncryptionConfiguration`. Optional.
    </ResponseField>
    <ResponseField name="kms_key_name" type="string">
      Resource name of KMS key for disk encryption Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="disk_encryption_status" type="object">
  Disk encryption status for an instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="kind" type="string">
      This is always `sql#diskEncryptionStatus`. Optional.
    </ResponseField>
    <ResponseField name="kms_key_version_name" type="string">
      KMS key version used to encrypt the Cloud SQL instance resource Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dns_name" type="string">
  Output only. The dns name of the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="dns_names" type="list(object)">
  Output only. The list of DNS names used by this instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="connection_type" type="string">
      Whether this connection uses Private Service Connect or a direct peering connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="dns_scope" type="string">
      Whether this DNS name resolves within the VPC only or more broadly. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="record_manager" type="string">
      The system responsible for managing this DNS record. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  This field is deprecated and will be removed from a future version of the API. Use the `settings.settingsVersion` field instead. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="failover_replica" type="object">
  The name and status of the failover replica. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="available" type="bool">
      The availability status of the failover replica. A false status indicates that the failover replica is out of sync. The primary instance can only failover to the failover replica when the status is true. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the failover replica. If specified at instance creation, a failover replica is created for the instance. The name doesn't include the project ID. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="gce_zone" type="string">
  The Compute Engine zone that the instance is currently serving from. This value could be different from the zone that was specified when the instance was created if the instance has failed over to its secondary zone. WARNING: Changing this might restart the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="gemini_config" type="object">
  Gemini instance configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="active_query_enabled" type="bool">
      Output only. Whether the active query is enabled. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="entitled" type="bool">
      Output only. Whether Gemini is enabled. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="flag_recommender_enabled" type="bool">
      Output only. Whether the flag recommender is enabled. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="google_vacuum_mgmt_enabled" type="bool">
      Output only. Whether the vacuum management is enabled. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="index_advisor_enabled" type="bool">
      Output only. Whether the index advisor is enabled. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="oom_session_cancel_enabled" type="bool">
      Output only. Whether canceling the out-of-memory (OOM) session is enabled. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="include_replicas_for_major_version_upgrade" type="bool">
  Input only. Determines whether an in-place major version upgrade of replicas happens when an in-place major version upgrade of a primary instance is initiated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="instance_type" type="string">
  The instance type. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ip_addresses" type="list(object)">
  The assigned IP addresses for the instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="ip_address" type="string">
      An IP address, in IPv4 or IPv6 format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="time_to_retire" type="string">
      When this resource will be retired and no longer usable. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ipv6_address" type="string">
  The IPv6 address assigned to the instance. (Deprecated) This property was applicable only to First Generation instances. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  This is always `sql#instance`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="maintenance_version" type="string">
  The current software version on the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="master_instance_name" type="string">
  The name of the instance which will act as primary in the replication setup. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="max_disk_size" type="string">
  The maximum disk size of the instance in bytes. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the Cloud SQL instance. This does not include the project ID. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="node_count" type="number">
  The number of read pool nodes in a read pool. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="nodes" type="list(object)">
  Output only. Entries containing information about each read pool node of the read pool. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="dns_name" type="string">
      The DNS name assigned to this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="dns_names" type="list(object)">
      The DNS name(s) assigned to this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="connection_type" type="string">
          Whether this connection uses Private Service Connect or a direct peering connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dns_scope" type="string">
          Whether this DNS name resolves within the VPC only or more broadly. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="record_manager" type="string">
          The system responsible for managing this DNS record. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="gce_zone" type="string">
      The Compute Engine zone this instance runs in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ip_addresses" type="list(object)">
      The IP address(es) assigned to this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ip_address" type="string">
          An IP address, in IPv4 or IPv6 format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time_to_retire" type="string">
          When this resource will be retired and no longer usable. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="psc_auto_connections" type="list(object)">
      Private Service Connect connection(s) automatically established for this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="consumer_network" type="string">
          A reference to the consumer-side VPC network this connection uses. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="consumer_network_status" type="string">
          The current status of the consumer-side network connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="consumer_project" type="string">
          A reference to the consumer's own Google Cloud project. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instance_auto_dns_status" type="string">
          Whether this instance's own automatic DNS name is active. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ip_address" type="string">
          An IP address, in IPv4 or IPv6 format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_connection_policy" type="string">
          A reference to the service connection policy this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_connection_policy_creation_result" type="string">
          The outcome of automatically creating a service connection policy for this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          The current status of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="write_endpoint_auto_dns_status" type="string">
          Whether this instance's own automatic DNS name for the write endpoint is active. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="psc_service_attachment_link" type="string">
      A reference to the Private Service Connect service attachment this instance publishes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state" type="string">
      The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="on_premises_configuration" type="object">
  On-premises instance configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="ca_certificate" type="string">
      PEM representation of the trusted CA's x509 certificate. Optional.
    </ResponseField>
    <ResponseField name="client_certificate" type="string">
      PEM representation of the replica's x509 certificate. Optional.
    </ResponseField>
    <ResponseField name="client_key" type="string">
      PEM representation of the replica's private key. The corresponding public key is encoded in the client's certificate. Optional.
    </ResponseField>
    <ResponseField name="dms_managed" type="bool">
      Output only. Indicates whether the resource is managed by Database Migration Service. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="dump_file_path" type="string">
      The dump file to create the Cloud SQL replica. Optional.
    </ResponseField>
    <ResponseField name="host_port" type="string">
      The host and port of the on-premises instance in host:port format Optional.
    </ResponseField>
    <ResponseField name="kind" type="string">
      This is always `sql#onPremisesConfiguration`. Optional.
    </ResponseField>
    <ResponseField name="password" type="string">
      The password for connecting to on-premises instance. Optional.
    </ResponseField>
    <ResponseField name="selected_objects" type="list(object)">
      Optional. A list of objects that the user selects for replication from an external source instance. Optional.
      <Expandable title="properties">
        <ResponseField name="database" type="string">
          A reference to the database this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source_instance" type="object">
      Reference to another Cloud SQL instance. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the Cloud SQL instance being referenced. This does not include the project ID. Optional.
        </ResponseField>
        <ResponseField name="project" type="string">
          The project ID of the Cloud SQL instance being referenced. The default is the same project ID as the instance references it. Optional.
        </ResponseField>
        <ResponseField name="region" type="string">
          The region of the Cloud SQL instance being referenced. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ssl_option" type="string">
      Optional. SSL option for replica connection to the on-premises source. Optional.
    </ResponseField>
    <ResponseField name="username" type="string">
      The username for connecting to on-premises instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="out_of_disk_report" type="object">
  This message wraps up the information written by out-of-disk detection job. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="sql_min_recommended_increase_size_gb" type="number">
      The minimum recommended increase size in GigaBytes This field is consumed by the frontend * Writers: * the proactive database wellness job for OOD. * Readers: Optional.
    </ResponseField>
    <ResponseField name="sql_out_of_disk_state" type="string">
      This field represents the state generated by the proactive database wellness job for OutOfDisk issues. * Writers: * the proactive database wellness job for OOD. * Readers: * the proactive database wellness job Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="primary_dns_name" type="string">
  Output only. DEPRECATED: please use write_endpoint instead. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="project" type="string">
  The project ID of the project containing the Cloud SQL instance. The Google apps domain is prefixed if applicable. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="psc_service_attachment_link" type="string">
  Output only. The link to service attachment of PSC instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="region" type="string">
  The geographical region of the Cloud SQL instance. It can be one of the [regions](https://cloud.google.com/sql/docs/mysql/locations#location-r) where Cloud SQL operates: For example, `asia-east1`, `europe-west1`, and `us-central1`. The default value is `us-central1`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="replica_configuration" type="object">
  Read-replica configuration for connecting to the primary instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cascadable_replica" type="bool">
      Optional. Specifies if a SQL Server replica is a cascadable replica. A cascadable replica is a SQL Server cross region replica that supports replica(s) under it. Optional.
    </ResponseField>
    <ResponseField name="failover_target" type="bool">
      Specifies if the replica is the failover target. If the field is set to `true`, the replica will be designated as a failover replica. In case the primary instance fails, the replica instance will be promoted as the new primary instance. Only one replica can be specified as failover target, and the replica has to be in different zone with the primary instance. Optional.
    </ResponseField>
    <ResponseField name="kind" type="string">
      This is always `sql#replicaConfiguration`. Optional.
    </ResponseField>
    <ResponseField name="mysql_replica_configuration" type="object">
      Read-replica configuration specific to MySQL databases. Optional.
      <Expandable title="properties">
        <ResponseField name="ca_certificate" type="string">
          PEM representation of the trusted CA's x509 certificate. Optional.
        </ResponseField>
        <ResponseField name="client_certificate" type="string">
          PEM representation of the replica's x509 certificate. Optional.
        </ResponseField>
        <ResponseField name="client_key" type="string">
          PEM representation of the replica's private key. The corresponding public key is encoded in the client's certificate. Optional.
        </ResponseField>
        <ResponseField name="connect_retry_interval" type="number">
          Seconds to wait between connect retries. MySQL's default is 60 seconds. Optional.
        </ResponseField>
        <ResponseField name="dump_file_path" type="string">
          Path to a SQL dump file in Google Cloud Storage from which the replica instance is to be created. The URI is in the form gs://bucketName/fileName. Compressed gzip files (.gz) are also supported. Dumps have the binlog co-ordinates from which replication begins. This can be accomplished by setting --master-data to 1 when using mysqldump. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          This is always `sql#mysqlReplicaConfiguration`. Optional.
        </ResponseField>
        <ResponseField name="master_heartbeat_period" type="string">
          Interval in milliseconds between replication heartbeats. Optional.
        </ResponseField>
        <ResponseField name="password" type="string">
          The password for the replication connection. Optional.
        </ResponseField>
        <ResponseField name="ssl_cipher" type="string">
          A list of permissible ciphers to use for SSL encryption. Optional.
        </ResponseField>
        <ResponseField name="username" type="string">
          The username for the replication connection. Optional.
        </ResponseField>
        <ResponseField name="verify_server_certificate" type="bool">
          Whether or not to check the primary instance's Common Name value in the certificate that it sends during the SSL handshake. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="replica_names" type="list(string)">
  The replicas of the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="replication_cluster" type="object">
  A primary instance and disaster recovery (DR) replica pair. A DR replica is a cross-region replica that you designate for failover in the event that the primary instance experiences regional failure. Applicable to MySQL and PostgreSQL. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="dr_replica" type="bool">
      Output only. Read-only field that indicates whether the replica is a DR replica. This field is not set if the instance is a primary instance. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="failover_dr_replica_name" type="string">
      Optional. If the instance is a primary instance, then this field identifies the disaster recovery (DR) replica. A DR replica is an optional configuration for Enterprise Plus edition instances. If the instance is a read replica, then the field is not set. Set this field to a replica name to designate a DR replica for a primary instance. Remove the replica name to remove the DR replica designation. Optional.
    </ResponseField>
    <ResponseField name="psa_write_endpoint" type="string">
      Output only. If set, this field indicates this instance has a private service access (PSA) DNS endpoint that is pointing to the primary instance of the cluster. If this instance is the primary, then the DNS endpoint points to this instance. After a switchover or replica failover operation, this DNS endpoint points to the promoted instance. This is a read-only field, returned to the user as information. This field can exist even if a standalone instance doesn't have a DR replica yet or the DR replica is deleted. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="root_password" type="string">
  Initial root password. Use only on creation. You must set root passwords before you can connect to PostgreSQL instances. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. This status indicates whether the instance satisfies PZI. The status is reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  This status indicates whether the instance satisfies PZS. The status is reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="scheduled_maintenance" type="object">
  Any scheduled maintenance for this instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="can_defer" type="bool">
      Whether this maintenance can be deferred to a later window. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="can_reschedule" type="bool">
      If the scheduled maintenance can be rescheduled. Optional.
    </ResponseField>
    <ResponseField name="schedule_deadline_time" type="string">
      Maintenance cannot be rescheduled to start beyond this deadline. Optional.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      The start time of any upcoming scheduled maintenance for this instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="secondary_gce_zone" type="string">
  The Compute Engine zone that the failover instance is currently serving from for a regional instance. This value could be different from the zone that was specified when the instance was created if the instance has failed over to its secondary/failover zone. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  The URI of this resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="server_ca_cert" type="object">
  SslCerts Resource Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cert" type="string">
      PEM representation. Optional.
    </ResponseField>
    <ResponseField name="cert_serial_number" type="string">
      Serial number, as extracted from the certificate. Optional.
    </ResponseField>
    <ResponseField name="common_name" type="string">
      User supplied name. Constrained to [a-zA-Z.-_ ]+. Optional.
    </ResponseField>
    <ResponseField name="create_time" type="string">
      The time when the certificate was created in [RFC 3339](https://tools.ietf.org/html/rfc3339) format, for example `2012-11-15T16:19:00.094Z` Optional.
    </ResponseField>
    <ResponseField name="expiration_time" type="string">
      The time when the certificate expires in [RFC 3339](https://tools.ietf.org/html/rfc3339) format, for example `2012-11-15T16:19:00.094Z`. Optional.
    </ResponseField>
    <ResponseField name="instance" type="string">
      Name of the database instance. Optional.
    </ResponseField>
    <ResponseField name="kind" type="string">
      This is always `sql#sslCert`. Optional.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      The URI of this resource. Optional.
    </ResponseField>
    <ResponseField name="sha1_fingerprint" type="string">
      Sha1 Fingerprint. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_account_email_address" type="string">
  The service account email address assigned to the instance.\This property is read-only. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="settings" type="object">
  Database instance settings. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="accelerated_replica_mode" type="bool">
      Optional. Whether the replica is in accelerated mode. This feature is in private preview and requires allowlisting to take effect. Optional.
    </ResponseField>
    <ResponseField name="activation_policy" type="string">
      The activation policy specifies when the instance is activated; it is applicable only when the instance state is RUNNABLE. Valid values: * `ALWAYS`: The instance is on, and remains so even in the absence of connection requests. * `NEVER`: The instance is off; it is not activated, even if a connection request arrives. Optional.
    </ResponseField>
    <ResponseField name="active_directory_config" type="object">
      Active Directory configuration, relevant only for Cloud SQL for SQL Server. Optional.
      <Expandable title="properties">
        <ResponseField name="admin_credential_secret_name" type="string">
          Optional. The secret manager key storing the administrator credential. (e.g., projects/&#123;project&#125;/secrets/&#123;secret&#125;). Optional.
        </ResponseField>
        <ResponseField name="dns_servers" type="list(string)">
          Optional. Domain controller IPv4 addresses used to bootstrap Active Directory. Optional.
        </ResponseField>
        <ResponseField name="domain" type="string">
          The name of the domain (e.g., mydomain.com). Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          This is always sql#activeDirectoryConfig. Optional.
        </ResponseField>
        <ResponseField name="mode" type="string">
          Optional. The mode of the Active Directory configuration. Optional.
        </ResponseField>
        <ResponseField name="organizational_unit" type="string">
          Optional. The organizational unit distinguished name. This is the full hierarchical path to the organizational unit. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="advanced_machine_features" type="object">
      Specifies options for controlling advanced machine features. Optional.
      <Expandable title="properties">
        <ResponseField name="threads_per_core" type="number">
          The number of threads per physical core. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="authorized_gae_applications" type="list(string)">
      The App Engine app IDs that can access this instance. (Deprecated) Applied to First Generation instances only. Optional.
    </ResponseField>
    <ResponseField name="auto_upgrade_enabled" type="bool">
      Optional. Cloud SQL for MySQL auto-upgrade configuration. When this parameter is set to true, auto-upgrade is enabled for MySQL 8.0 minor versions. The MySQL version must be 8.0.35 or higher. Optional.
    </ResponseField>
    <ResponseField name="availability_type" type="string">
      Availability type. Potential values: * `ZONAL`: The instance serves data from only one zone. Outages in that zone affect data accessibility. * `REGIONAL`: The instance can serve data from more than one zone in a region (it is highly available)./ For more information, see [Overview of the High Availability Configuration](https://cloud.google.com/sql/docs/mysql/high-availability). Optional.
    </ResponseField>
    <ResponseField name="backup_configuration" type="object">
      Database instance backup configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="backup_retention_settings" type="object">
          We currently only support backup retention by specifying the number of backups we will retain. Optional.
          <Expandable title="properties">
            <ResponseField name="retained_backups" type="number">
              Depending on the value of retention_unit, this is used to determine if a backup needs to be deleted. If retention_unit is 'COUNT', we will retain this many backups. Optional.
            </ResponseField>
            <ResponseField name="retention_unit" type="string">
              The unit that 'retained_backups' represents. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="backup_tier" type="string">
          Output only. Backup tier that manages the backups for the instance. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="binary_log_enabled" type="bool">
          (MySQL only) Whether binary log is enabled. If backup configuration is disabled, binarylog must be disabled as well. Optional.
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Whether this configuration is enabled. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          This is always `sql#backupConfiguration`. Optional.
        </ResponseField>
        <ResponseField name="location" type="string">
          Location of the backup Optional.
        </ResponseField>
        <ResponseField name="point_in_time_recovery_enabled" type="bool">
          Whether point in time recovery is enabled. Optional.
        </ResponseField>
        <ResponseField name="replication_log_archiving_enabled" type="bool">
          Optional. Deprecated: replication_log_archiving_enabled is deprecated and will be removed from a future version of the API. Use point_in_time_recovery_enabled instead. Optional.
        </ResponseField>
        <ResponseField name="start_time" type="string">
          Start time for the daily backup configuration in UTC timezone in the 24 hour format - `HH:MM`. Optional.
        </ResponseField>
        <ResponseField name="transaction_log_retention_days" type="number">
          The number of days of transaction logs we retain for point in time restore, from 1-7. Optional.
        </ResponseField>
        <ResponseField name="transactional_log_storage_state" type="string">
          Output only. This value contains the storage location of transactional logs used to perform point-in-time recovery (PITR) for the database. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="collation" type="string">
      The name of server Instance collation. Optional.
    </ResponseField>
    <ResponseField name="connection_pool_config" type="object">
      The managed connection pooling configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="connection_pooling_enabled" type="bool">
          Whether managed connection pooling is enabled. Optional.
        </ResponseField>
        <ResponseField name="flags" type="list(object)">
          Optional. List of connection pool configuration flags. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="pooler_count" type="number">
          Output only. Number of connection poolers. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="connector_enforcement" type="string">
      Specifies if connections must use Cloud SQL connectors. Option values include the following: `NOT_REQUIRED` (Cloud SQL instances can be connected without Cloud SQL Connectors) and `REQUIRED` (Only allow connections that use Cloud SQL Connectors). Note that using REQUIRED disables all existing authorized networks. If this field is not specified when creating a new instance, NOT_REQUIRED is used. If this field is not specified when patching or updating an existing instance, it is left unchanged in the instance. Optional.
    </ResponseField>
    <ResponseField name="crash_safe_replication_enabled" type="bool">
      Configuration specific to read replica instances. Indicates whether database flags for crash-safe replication are enabled. This property was only applicable to First Generation instances. Optional.
    </ResponseField>
    <ResponseField name="data_api_access" type="string">
      This parameter controls whether to allow using ExecuteSql API to connect to the instance. Not allowed by default. Optional.
    </ResponseField>
    <ResponseField name="data_cache_config" type="object">
      Data cache configurations. Optional.
      <Expandable title="properties">
        <ResponseField name="data_cache_enabled" type="bool">
          Whether data cache is enabled for the instance. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="data_disk_provisioned_iops" type="string">
      Optional. Provisioned number of I/O operations per second for the data disk. This field is only used for hyperdisk-balanced disk types. Optional.
    </ResponseField>
    <ResponseField name="data_disk_provisioned_throughput" type="string">
      Optional. Provisioned throughput measured in MiB per second for the data disk. This field is only used for hyperdisk-balanced disk types. Optional.
    </ResponseField>
    <ResponseField name="data_disk_size_gb" type="string">
      The size of data disk, in GB. The data disk size minimum is 10GB. Optional.
    </ResponseField>
    <ResponseField name="data_disk_type" type="string">
      The type of data disk: `PD_SSD` (default) or `PD_HDD`. Not used for First Generation instances. Optional.
    </ResponseField>
    <ResponseField name="database_flags" type="list(object)">
      The database flags passed to the instance at startup. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="database_replication_enabled" type="bool">
      Configuration specific to read replica instances. Indicates whether replication is enabled or not. WARNING: Changing this restarts the instance. Optional.
    </ResponseField>
    <ResponseField name="deletion_protection_enabled" type="bool">
      Configuration to protect against accidental instance deletion. Optional.
    </ResponseField>
    <ResponseField name="deny_maintenance_periods" type="list(object)">
      Deny maintenance periods Optional.
      <Expandable title="properties">
        <ResponseField name="end_date" type="string">
          When this window or period ends. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_date" type="string">
          When this window or period starts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time" type="string">
          The time this event occurred at. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="edition" type="string">
      Optional. The edition type of the Cloud SQL instance. Optional.
    </ResponseField>
    <ResponseField name="enable_dataplex_integration" type="bool">
      Optional. By default, Cloud SQL instances have schema extraction disabled for Dataplex. When this parameter is set to true, schema extraction for Dataplex on Cloud SQL instances is activated. Optional.
    </ResponseField>
    <ResponseField name="enable_google_ml_integration" type="bool">
      Optional. When this parameter is set to true, Cloud SQL instances can connect to Vertex AI to pass requests for real-time predictions and insights to the AI. The default value is false. This applies only to Cloud SQL for MySQL and Cloud SQL for PostgreSQL instances. Optional.
    </ResponseField>
    <ResponseField name="entraid_config" type="object">
      SQL Server Entra ID configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="application_id" type="string">
          Optional. The application ID for the Entra ID configuration. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          Output only. This is always sql#sqlServerEntraIdConfig Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          Optional. The tenant ID for the Entra ID configuration. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="final_backup_config" type="object">
      Config used to determine the final backup settings for the instance. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether the final backup is enabled for the instance. Optional.
        </ResponseField>
        <ResponseField name="retention_days" type="number">
          The number of days to retain the final backup after the instance deletion. The final backup will be purged at (time_of_instance_deletion + retention_days). Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="insights_config" type="object">
      Insights configuration. This specifies when Cloud SQL Insights feature is enabled and optional configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="enhanced_query_insights_enabled" type="bool">
          Optional. Whether enhanced query insights feature is enabled. Optional.
        </ResponseField>
        <ResponseField name="query_insights_enabled" type="bool">
          Whether Query Insights feature is enabled. Optional.
        </ResponseField>
        <ResponseField name="query_plans_per_minute" type="number">
          Number of query execution plans captured by Insights per minute for all queries combined. Default is 5. Optional.
        </ResponseField>
        <ResponseField name="query_string_length" type="number">
          Maximum query length stored in bytes. Default value: 1024 bytes. Range: 256-4500 bytes. Query lengths greater than this field value will be truncated to this value. When unset, query length will be the default value. Changing query length will restart the database. Optional.
        </ResponseField>
        <ResponseField name="record_application_tags" type="bool">
          Whether Query Insights will record application tags from query when enabled. Optional.
        </ResponseField>
        <ResponseField name="record_client_address" type="bool">
          Whether Query Insights will record client address when enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ip_configuration" type="object">
      IP Management configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="allocated_ip_range" type="string">
          The name of the allocated ip range for the private ip Cloud SQL instance. For example: "google-managed-services-default". If set, the instance ip will be created in the allocated range. The range name must comply with [RFC 1035](https://tools.ietf.org/html/rfc1035). Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?.` Optional.
        </ResponseField>
        <ResponseField name="authorized_networks" type="list(object)">
          The list of external networks that are allowed to connect to the instance using the IP. In 'CIDR' notation, also known as 'slash' notation (for example: `157.197.200.0/24`). Optional.
          <Expandable title="properties">
            <ResponseField name="expiration_time" type="string">
              When this resource expires. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The category of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="custom_subject_alternative_names" type="list(string)">
          Optional. Custom Subject Alternative Name(SAN)s for a Cloud SQL instance. Optional.
        </ResponseField>
        <ResponseField name="enable_private_path_for_google_cloud_services" type="bool">
          Controls connectivity to private IP instances from Google services, such as BigQuery. Optional.
        </ResponseField>
        <ResponseField name="ipv4_enabled" type="bool">
          Whether the instance is assigned a public IP address or not. Optional.
        </ResponseField>
        <ResponseField name="private_network" type="string">
          The resource link for the VPC network from which the Cloud SQL instance is accessible for private IP. For example, `/projects/myProject/global/networks/default`. This setting can be updated, but it cannot be removed after it is set. Optional.
        </ResponseField>
        <ResponseField name="psc_config" type="object">
          PSC settings for a Cloud SQL instance. Optional.
          <Expandable title="properties">
            <ResponseField name="allowed_consumer_projects" type="list(string)">
              Optional. The list of consumer projects that are allow-listed for PSC connections to this instance. This instance can be connected to with PSC from any network in these projects. Each consumer project in this list may be represented by a project number (numeric) or by a project id (alphanumeric). Optional.
            </ResponseField>
            <ResponseField name="network_attachment_uri" type="string">
              Optional. The network attachment of the consumer network that the Private Service Connect enabled Cloud SQL instance is authorized to connect via PSC interface. format: projects/PROJECT/regions/REGION/networkAttachments/ID Optional.
            </ResponseField>
            <ResponseField name="psc_auto_connection_policy_enabled" type="bool">
              Optional. Whether to set up the PSC service connection policy automatically. Optional.
            </ResponseField>
            <ResponseField name="psc_auto_connections" type="list(object)">
              Optional. The list of settings for requested Private Service Connect consumer endpoints that can be used to connect to this Cloud SQL instance. Optional.
              <Expandable title="properties">
                <ResponseField name="consumer_network" type="string">
                  A reference to the consumer-side VPC network this connection uses. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="consumer_network_status" type="string">
                  The current status of the consumer-side network connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="consumer_project" type="string">
                  A reference to the consumer's own Google Cloud project. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="instance_auto_dns_status" type="string">
                  Whether this instance's own automatic DNS name is active. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ip_address" type="string">
                  An IP address, in IPv4 or IPv6 format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="service_connection_policy" type="string">
                  A reference to the service connection policy this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="service_connection_policy_creation_result" type="string">
                  The outcome of automatically creating a service connection policy for this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="status" type="string">
                  The current status of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="write_endpoint_auto_dns_status" type="string">
                  Whether this instance's own automatic DNS name for the write endpoint is active. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="psc_auto_dns_enabled" type="bool">
              Optional. Indicates whether Private Service Connect DNS automation is enabled for this instance. When enabled, Cloud SQL provisions a universal DNS record across all networks configured with Private Service Connect auto-connections. This will default to true for new instances when Private Service Connect is enabled. Optional.
            </ResponseField>
            <ResponseField name="psc_enabled" type="bool">
              Whether PSC connectivity is enabled for this instance. Optional.
            </ResponseField>
            <ResponseField name="psc_write_endpoint_dns_enabled" type="bool">
              Optional. Indicates whether Private Service Connect write endpoint DNS automation is enabled for this instance. When enabled, Cloud SQL provisions a universal global DNS record across all networks configured with Private Service Connect auto-connections that points to the cluster primary instance. This feature is only supported for Enterprise Plus edition. This will default to true for new Enterprise Plus instances when `psc_auto_dns_enabled` is enabled. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="require_ssl" type="bool">
          Use `ssl_mode` instead. Whether SSL/TLS connections over IP are enforced. If set to false, then allow both non-SSL/non-TLS and SSL/TLS connections. For SSL/TLS connections, the client certificate won't be verified. If set to true, then only allow connections encrypted with SSL/TLS and with valid client certificates. If you want to enforce SSL/TLS without enforcing the requirement for valid client certificates, then use the `ssl_mode` flag instead of the `require_ssl` flag. Optional.
        </ResponseField>
        <ResponseField name="server_ca_mode" type="string">
          Specify what type of CA is used for the server certificate. Optional.
        </ResponseField>
        <ResponseField name="server_ca_pool" type="string">
          Optional. The resource name of the server CA pool for an instance with `CUSTOMER_MANAGED_CAS_CA` as the `server_ca_mode`. Format: projects/&#123;PROJECT&#125;/locations/&#123;REGION&#125;/caPools/&#123;CA_POOL_ID&#125; Optional.
        </ResponseField>
        <ResponseField name="server_certificate_rotation_mode" type="string">
          Optional. Controls the automatic server certificate rotation feature. This feature is disabled by default. When enabled, the server certificate will be automatically rotated during Cloud SQL scheduled maintenance or self-service maintenance updates up to six months before it expires. This setting can only be set if server_ca_mode is either GOOGLE_MANAGED_CAS_CA or CUSTOMER_MANAGED_CAS_CA. Optional.
        </ResponseField>
        <ResponseField name="ssl_mode" type="string">
          Specify how SSL/TLS is enforced in database connections. If you must use the `require_ssl` flag for backward compatibility, then only the following value pairs are valid: For PostgreSQL and MySQL: * `ssl_mode=ALLOW_UNENCRYPTED_AND_ENCRYPTED` and `require_ssl=false` * `ssl_mode=ENCRYPTED_ONLY` and `require_ssl=false` * `ssl_mode=TRUSTED_CLIENT_CERTIFICATE_REQUIRED` and `require_ssl=true` For SQL Server: * `ssl_mode=ALLOW_UNENCRYPTED_AND_ENCRYPTED` and `require_ssl=false` * `ssl_mode=ENCRYPTED_ONLY` and `require_ssl=true` The value of `ssl_mode` has priority over the value of `require_ssl`. For example, for the pair `ssl_mode=ENCRYPTED_ONLY` and `require_ssl=false`, `ssl_mode=ENCRYPTED_ONLY` means accept only SSL connections, while `require_ssl=false` means accept both non-SSL and SSL connections. In this case, MySQL and PostgreSQL databases respect `ssl_mode` and accepts only SSL connections. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kind" type="string">
      This is always `sql#settings`. Optional.
    </ResponseField>
    <ResponseField name="location_preference" type="object">
      Preferred location. This specifies where a Cloud SQL instance is located. Note that if the preferred location is not available, the instance will be located as close as possible within the region. Only one location may be specified. Optional.
      <Expandable title="properties">
        <ResponseField name="follow_gae_application" type="string">
          The App Engine application to follow, it must be in the same region as the Cloud SQL instance. WARNING: Changing this might restart the instance. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          This is always `sql#locationPreference`. Optional.
        </ResponseField>
        <ResponseField name="secondary_zone" type="string">
          The preferred Compute Engine zone for the secondary/failover (for example: us-central1-a, us-central1-b, etc.). To disable this field, set it to 'no_secondary_zone'. Optional.
        </ResponseField>
        <ResponseField name="zone" type="string">
          The preferred Compute Engine zone (for example: us-central1-a, us-central1-b, etc.). WARNING: Changing this might restart the instance. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="maintenance_window" type="object">
      Maintenance window. This specifies when a Cloud SQL instance is restarted for system maintenance purposes. Optional.
      <Expandable title="properties">
        <ResponseField name="day" type="number">
          Day of week - `MONDAY`, `TUESDAY`, `WEDNESDAY`, `THURSDAY`, `FRIDAY`, `SATURDAY`, or `SUNDAY`. Specify in the UTC time zone. Returned in output as an integer, 1 to 7, where `1` equals Monday. Optional.
        </ResponseField>
        <ResponseField name="hour" type="number">
          Hour of day - 0 to 23. Specify in the UTC time zone. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          This is always `sql#maintenanceWindow`. Optional.
        </ResponseField>
        <ResponseField name="update_track" type="string">
          Maintenance timing settings: `canary`, `stable`, or `week5`. For more information, see [About maintenance on Cloud SQL instances](https://cloud.google.com/sql/docs/mysql/maintenance). Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="password_validation_policy" type="object">
      Database instance local user password validation policy. This message defines the password policy for local database users. When enabled, it enforces constraints on password complexity, length, and reuse. Keep this policy enabled to help prevent unauthorized access. Optional.
      <Expandable title="properties">
        <ResponseField name="complexity" type="string">
          The complexity of the password. Optional.
        </ResponseField>
        <ResponseField name="disallow_compromised_credentials" type="bool">
          This field is deprecated and will be removed in a future version of the API. Optional.
        </ResponseField>
        <ResponseField name="disallow_username_substring" type="bool">
          Disallow username as a part of the password. Optional.
        </ResponseField>
        <ResponseField name="enable_password_policy" type="bool">
          Whether to enable the password policy or not. When enabled, passwords must meet complexity requirements. Keep this policy enabled to help prevent unauthorized access. Disabling this policy allows weak passwords. Optional.
        </ResponseField>
        <ResponseField name="min_length" type="number">
          Minimum number of characters allowed. Optional.
        </ResponseField>
        <ResponseField name="password_change_interval" type="string">
          Minimum interval after which the password can be changed. This flag is only supported for PostgreSQL. Optional.
        </ResponseField>
        <ResponseField name="reuse_interval" type="number">
          Number of previous passwords that cannot be reused. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="performance_capture_config" type="object">
      Performance capture configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="cpu_utilization_threshold_percent" type="number">
          Optional. Specifies the minimum percentage of CPU utilization to trigger the performance capture. Valid integers range from `10` to `99`. Enter `0` to disable the check. Optional.
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Optional. Enables or disables the performance capture feature. Optional.
        </ResponseField>
        <ResponseField name="history_list_length_threshold_count" type="number">
          Optional. Specifies the minimum number of undo log entries in the history list length to trigger the performance capture. Valid integers range from `10000` to `10000000`. Enter `0` to disable the check. Optional.
        </ResponseField>
        <ResponseField name="memory_usage_threshold_percent" type="number">
          Optional. Specifies the minimum percentage of memory usage to trigger the performance capture. Valid integers range from `10` to `99`. Enter `0` to disable the check. Optional.
        </ResponseField>
        <ResponseField name="probe_threshold" type="number">
          Optional. Specifies the minimum number of consecutive probe threshold that triggers performance capture. Optional.
        </ResponseField>
        <ResponseField name="probing_interval_seconds" type="number">
          Optional. Specifies the interval in seconds between consecutive probes that check if any trigger condition thresholds have been reached. Optional.
        </ResponseField>
        <ResponseField name="running_threads_threshold" type="number">
          Optional. Specifies the minimum number of MySQL `Threads_running` to trigger the performance capture on the primary instance. Optional.
        </ResponseField>
        <ResponseField name="seconds_behind_source_threshold" type="number">
          Optional. Specifies the minimum number of seconds replica must be lagging behind primary instance to trigger the performance capture on replica. Optional.
        </ResponseField>
        <ResponseField name="semaphore_wait_threshold_count" type="number">
          Optional. Specifies the minimum allowed number of semaphore waits to trigger the performance capture. Valid integers range from `10` to `10000`. Enter `0` to disable the check. Optional.
        </ResponseField>
        <ResponseField name="transaction_duration_threshold" type="number">
          Optional. Specifies the amount of time in seconds that a transaction needs to have been open before the watcher starts recording it. Optional.
        </ResponseField>
        <ResponseField name="transaction_kill_excluded_user_hosts" type="list(string)">
          Optional. Specifies a customer-defined list of users to exclude from transaction termination. Entries can be in the format 'user@host' or just 'user'. A standalone 'user' implies 'user@%', excluding the user from any host. Wildcard '%' is allowed in the host part of the 'user@host' format. Example: `["app_user", "db_admin@10.1.2.3", "report_user@%"]` Optional.
        </ResponseField>
        <ResponseField name="transaction_kill_threshold_seconds" type="number">
          Optional. Specifies the amount of time in seconds that a transaction needs to have been open before the watcher starts terminating it. Valid integers range from `60` to `604800` (7 days). Enter `0` to disable. If enabled (i.e., &gt; 0), this value must be greater than or equal to `transaction_duration_threshold`. Configurations where `0 &lt; transaction_kill_threshold_seconds &lt; transaction_duration_threshold` will be rejected. Optional.
        </ResponseField>
        <ResponseField name="transaction_kill_type" type="string">
          Optional. Determines which transactions are allowed to be terminated when they exceed `transaction_kill_threshold_seconds`. This allows protecting write-heavy transactions from auto-termination if desired. Defaults to `READ_ONLY_TRANSACTIONS` if unspecified. Optional.
        </ResponseField>
        <ResponseField name="transaction_lock_wait_threshold_count" type="number">
          Optional. Specifies the minimum allowed number of transactions in lock wait state to trigger the performance capture. Valid integers range from `10` to `10000`. Enter `0` to disable the check. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pricing_plan" type="string">
      The pricing plan for this instance. This can be either `PER_USE` or `PACKAGE`. Only `PER_USE` is supported for Second Generation instances. Optional.
    </ResponseField>
    <ResponseField name="read_pool_auto_scale_config" type="object">
      The read pool auto-scale configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="disable_scale_in" type="bool">
          Indicates whether read pool auto scaling supports scale in operations (removing nodes). Optional.
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Indicates whether read pool auto scaling is enabled. Optional.
        </ResponseField>
        <ResponseField name="max_node_count" type="number">
          Maximum number of read pool nodes to be maintained. Optional.
        </ResponseField>
        <ResponseField name="min_node_count" type="number">
          Minimum number of read pool nodes to be maintained. Optional.
        </ResponseField>
        <ResponseField name="scale_in_cooldown_seconds" type="number">
          The cooldown period for scale-in operations. Optional.
        </ResponseField>
        <ResponseField name="scale_out_cooldown_seconds" type="number">
          The cooldown period for scale-out operations. Optional.
        </ResponseField>
        <ResponseField name="target_metrics" type="list(object)">
          Optional. Target metrics for read pool auto scaling. Optional.
          <Expandable title="properties">
            <ResponseField name="metric" type="string">
              The name of the metric being reported. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="target_value" type="number">
              The value this metric is measured against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="replication_lag_max_seconds" type="number">
      Optional. Configuration value for recreation of replica after certain replication lag Optional.
    </ResponseField>
    <ResponseField name="replication_type" type="string">
      The type of replication this instance uses. This can be either `ASYNCHRONOUS` or `SYNCHRONOUS`. (Deprecated) This property was only applicable to First Generation instances. Optional.
    </ResponseField>
    <ResponseField name="retain_backups_on_delete" type="bool">
      Optional. When this parameter is set to true, Cloud SQL retains backups of the instance even after the instance is deleted. The ON_DEMAND backup will be retained until customer deletes the backup or the project. The AUTOMATED backup will be retained based on the backups retention setting. Optional.
    </ResponseField>
    <ResponseField name="settings_version" type="string">
      The version of instance settings. This is a required field for update method to make sure concurrent updates are handled properly. During update, use the most recent settingsVersion value for this instance and do not try to update this value. Optional.
    </ResponseField>
    <ResponseField name="sql_server_audit_config" type="object">
      SQL Server specific audit configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string">
          The name of the destination bucket (e.g., gs://mybucket). Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          This is always sql#sqlServerAuditConfig Optional.
        </ResponseField>
        <ResponseField name="retention_interval" type="string">
          How long to keep generated audit files. Optional.
        </ResponseField>
        <ResponseField name="upload_interval" type="string">
          How often to upload generated audit files. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="storage_auto_resize" type="bool">
      Configuration to increase storage size automatically. The default value is true. Optional.
    </ResponseField>
    <ResponseField name="storage_auto_resize_limit" type="string">
      The maximum size to which storage capacity can be automatically increased. The default value is 0, which specifies that there is no limit. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The tier (or machine type) for this instance, for example `db-custom-1-3840`. WARNING: Changing this restarts the instance. Optional.
    </ResponseField>
    <ResponseField name="time_zone" type="string">
      Server timezone, relevant only for Cloud SQL for SQL Server. Optional.
    </ResponseField>
    <ResponseField name="user_labels" type="map(string)">
      User-provided labels, represented as a dictionary where each label is a single key value pair. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sql_network_architecture" type="string">
  Whether this instance uses the legacy or the new (Private Service Connect based) network architecture. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="state" type="string">
  The current serving state of the Cloud SQL instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="suspension_reason" type="list(string)">
  If the instance state is SUSPENDED, the reason for the suspension. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="switch_transaction_logs_to_cloud_storage_enabled" type="bool">
  Input only. Whether Cloud SQL is enabled to switch storing point-in-time recovery log files from a data disk to Cloud Storage. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional. Input only. Immutable. Tag keys and tag values that are bound to this instance. You must represent each item in the map as: `"" : ""`. For example, a single resource can have the following tags: ``` "123/environment": "production", "123/costCenter": "marketing", ``` For more information on tag creation and management, see https://cloud.google.com/resource-manager/docs/tags/tags-overview. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="upgradable_database_versions" type="list(object)">
  Output only. All database versions that are available for upgrade. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="major_version" type="string">
      The major version of the database engine this instance runs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="write_endpoint" type="string">
  Output only. The dns name of the primary instance in a replication group. Optional; if omitted, computed by Google Cloud.
</ResponseField>
