---
title: "google_firebaseapphosting_rollout"
description: "`google_firebaseapphosting_rollout` deploys a specific build to live traffic on a Firebase App Hosting backend."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_firebaseapphosting --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_firebaseapphosting/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseapphosting "github.com/ubiquex/ubx-sdk-google/sdk/go/google_firebaseapphosting/firebaseapphosting"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own rollout"})

    		ubx.Resource(firebaseapphosting.FirebaseapphostingRollout, "ci-runner", firebaseapphosting.FirebaseapphostingRolloutConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_firebaseapphosting --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirebaseapphostingRollout } from "./local-sdk/google_firebaseapphosting/sdk/typescript/google_firebaseapphosting/firebaseapphosting/firebaseapphosting_rollout.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own rollout" });

      resource(FirebaseapphostingRollout, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_firebaseapphosting --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_firebaseapphosting/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_firebaseapphosting.firebaseapphosting import FirebaseapphostingRollout, FirebaseapphostingRolloutConfig

    def describe():
        ubx.intent("payments own rollout")

        ubx.resource(FirebaseapphostingRollout, "ci-runner", FirebaseapphostingRolloutConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebaseapphosting_rollout called "ci-runner", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Optional. Unstructured key value map that may be set by external tools to store and arbitrary metadata. They are not queryable and should be preserved when modifying objects. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="build" type="string">
  Required. Immutable. The name of a build that already exists. It doesn't have to be built; a rollout will wait for a build to be ready before updating traffic. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Time at which the rollout was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. Time at which the rollout was deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Human-readable name. 63 character limit. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="error" type="object">
  The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
      The status code, which should be an enum value of google.rpc.Code. Optional.
    </ResponseField>
    <ResponseField name="details" type="list(map(any))">
      A list of messages that carry the error details. There is a common set of message types for APIs to use. Optional.
    </ResponseField>
    <ResponseField name="message" type="string">
      A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. Server-computed checksum based on other values; may be sent on update or delete to ensure operation is done on expected resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Unstructured key value map that can be used to organize and categorize objects. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the rollout. Format: `projects/&#123;project&#125;/locations/&#123;locationId&#125;/backends/&#123;backendId&#125;/rollouts/&#123;rolloutId&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. A field that, if true, indicates that the Rollout currently has an LRO. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the rollout. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System-assigned, unique identifier. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time at which the rollout was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
