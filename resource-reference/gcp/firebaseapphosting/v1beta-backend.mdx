---
title: "google_firebaseapphosting_v1beta_backend"
description: "`google_firebaseapphosting_v1beta_backend` is a Firebase App Hosting backend, the deployable web app resource that build(s) and rollout(s) are created for."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_firebaseapphosting_backend`.
</Warning>

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_firebaseapphosting_v1beta --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_firebaseapphosting_v1beta/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseapphosting "github.com/ubiquex/ubx-sdk-google/sdk/go/google_firebaseapphosting_v1beta/firebaseapphosting"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta backend"})

    		ubx.Resource(firebaseapphosting.V1betaFirebaseapphostingV1betaBackend, "example", firebaseapphosting.V1betaFirebaseapphostingV1betaBackendConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_firebaseapphosting_v1beta --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaFirebaseapphostingV1betaBackend } from "./local-sdk/google_firebaseapphosting_v1beta/sdk/typescript/google_firebaseapphosting_v1beta/firebaseapphosting/v1beta_firebaseapphosting_v1beta_backend.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta backend" });

      resource(V1betaFirebaseapphostingV1betaBackend, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_firebaseapphosting_v1beta --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_firebaseapphosting_v1beta/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_firebaseapphosting_v1beta.firebaseapphosting import V1betaFirebaseapphostingV1betaBackend, V1betaFirebaseapphostingV1betaBackendConfig

    def describe():
        ubx.intent("example own v1beta backend")

        ubx.resource(V1betaFirebaseapphostingV1betaBackend, "example", V1betaFirebaseapphostingV1betaBackendConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebaseapphosting_v1beta_backend called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Optional. Unstructured key value map that may be set by external tools to store and arbitrary metadata. They are not queryable and should be preserved when modifying objects. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="app_id" type="string">
  Optional. The [ID of a Web App](https://firebase.google.com/docs/reference/firebase-management/rest/v1beta1/projects.webApps#WebApp.FIELDS.app_id) associated with the backend. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="automatic_base_image_updates_disabled" type="bool">
  Optional. automatic_base_image_updates_disabled acts as a way for users to opt out of ABIU. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="codebase" type="object">
  The connection to an external source repository to watch for event-driven updates to the backend. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="repository" type="string">
      Required. The resource name for the Developer Connect [`gitRepositoryLink`](https://cloud.google.com/developer-connect/docs/api/reference/rest/v1/projects.locations.connections.gitRepositoryLinks) connected to this backend, in the format: `projects/&#123;project&#125;/locations/&#123;location&#125;/connections/&#123;connection&#125;/gitRepositoryLinks/&#123;repositoryLink&#125;` The connection for the `gitRepositoryLink` must made be using the Firebase App Hosting GitHub App via the Firebase Console. Optional.
    </ResponseField>
    <ResponseField name="root_directory" type="string">
      Optional. If `repository` is provided, the directory relative to the root of the repository to use as the root for the deployed web app. Defaults to use the root of the repository if not provided. If deploying a [monorepo](https://firebase.google.com/docs/app-hosting/monorepos), this should be the directory that contains the `package.json` or `apphosting.yaml` file. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Time at which the backend was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. Time at which the backend was deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Human-readable name. 63 character limit. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="environment" type="string">
  Optional. The environment name of the backend, used to load environment variables from environment specific configuration. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. Server-computed checksum based on other values; may be sent on update or delete to ensure operation is done on expected resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Unstructured key value map that can be used to organize and categorize objects. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="managed_resources" type="list(object)">
  Output only. A list of the resources managed by this backend. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="run_service" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="service" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="mode" type="string">
  Optional. Deprecated: Use `environment` instead. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the backend. Format: `projects/&#123;project&#125;/locations/&#123;locationId&#125;/backends/&#123;backendId&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="override_env" type="list(object)">
  Optional. Override environment variables for this Backend. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="availability" type="list(string)">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="origin" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="origin_file_name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="secret" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="variable" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. A field that, if true, indicates that the system is working to make adjustments to the backend during a LRO. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="request_logs_disabled" type="bool">
  Optional. A field that, if true, indicates that incoming request logs are disabled for this backend. Incoming request logs are enabled by default. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="runtime" type="object">
  Runtime is a string that represents the runtime that is used to build the backend. Users can specify one of the following runtimes: nodejs20, nodejs22, nodejs24, nodejs. Runtime "nodejs" means that nodejs version will be determined at build time. If not specified or specified with a value that is not in the list above, the default runtime `nodejs` will be used and Automatic Base Image Updates will be disabled. See [Firebase documentation](https://firebase.google.com/docs/app-hosting/frameworks-tooling#managing_runtime_versions) for more details. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="value" type="string">
      Optional. The value of the runtime. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_account" type="string">
  Required. The name of the service account used for Cloud Build and Cloud Run. Should have the role roles/firebaseapphosting.computeRunner or equivalent permissions. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="serving_locality" type="string">
  Required. Immutable. Specifies how App Hosting will serve the content for this backend. It will either be contained to a single region (REGIONAL_STRICT) or allowed to use App Hosting's global-replicated serving infrastructure (GLOBAL_ACCESS). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System-assigned, unique identifier. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time at which the backend was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uri" type="string">
  Output only. The primary URI to communicate with the backend. Optional; if omitted, computed by Google Cloud.
</ResponseField>
