---
title: "google_cloudbilling_billing_account"
description: "`google_cloudbilling_billing_account` is a real Google Cloud Billing account..."
---

`google_cloudbilling_billing_account` is a real Google Cloud Billing account, the entity that pays for a linked set of Google Cloud projects and Google Maps Platform usage, with a display name and a currency fixed at creation time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_cloudbilling --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_cloudbilling/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudbilling "github.com/ubiquex/ubx-sdk-google/sdk/go/google_cloudbilling/cloudbilling"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own billing account"})

    		ubx.Resource(cloudbilling.CloudbillingBillingAccount, "example", cloudbilling.CloudbillingBillingAccountConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_cloudbilling --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CloudbillingBillingAccount } from "./local-sdk/google_cloudbilling/sdk/typescript/google_cloudbilling/cloudbilling/cloudbilling_billing_account.ts";

    export default stack("example", () => {
      intent({ summary: "example own billing account" });

      resource(CloudbillingBillingAccount, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_cloudbilling --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_cloudbilling/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_cloudbilling.cloudbilling import CloudbillingBillingAccount, CloudbillingBillingAccountConfig

    def describe():
        ubx.intent("example own billing account")

        ubx.resource(CloudbillingBillingAccount, "example", CloudbillingBillingAccountConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudbilling_billing_account called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="currency_code" type="string">
  Optional. The currency in which the billing account is billed and charged, represented as an ISO 4217 code such as `USD`. Billing account currency is determined at the time of billing account creation and cannot be updated subsequently, so this field should not be set on update requests. In addition, a subaccount always matches the currency of its parent billing account, so this field should not be set on subaccount creation requests. Clients can read this field to determine the currency of an existing billing account. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name given to the billing account, such as `My Billing Account`. This name is displayed in the Google Cloud Console. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="master_billing_account" type="string">
  If this account is a [subaccount](https://cloud.google.com/billing/docs/concepts), then this will be the resource name of the parent billing account that it is being resold through. Otherwise this will be empty. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource name of the billing account. The resource name has the form `billingAccounts/&#123;billing_account_id&#125;`. For example, `billingAccounts/012345-567890-ABCDEF` would be the resource name for billing account `012345-567890-ABCDEF`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="open" type="bool">
  Output only. True if the billing account is open, and will therefore be charged for any usage on associated projects. False if the billing account is closed, and therefore projects associated with it are unable to use paid services. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parent" type="string">
  Output only. The billing account's parent resource identifier. Use the `MoveBillingAccount` method to update the account's parent resource if it is a organization. Format: - `organizations/&#123;organization_id&#125;`, for example, `organizations/12345678` - `billingAccounts/&#123;billing_account_id&#125;`, for example, `billingAccounts/012345-567890-ABCDEF` Optional; if omitted, computed by Google Cloud.
</ResponseField>
