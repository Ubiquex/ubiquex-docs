---
title: "google_storagetransfer_agent_pool"
description: "`google_storagetransfer_agent_pool` groups Storage Transfer Service agent(s) together..."
---

`google_storagetransfer_agent_pool` groups Storage Transfer Service agent(s) together, used when transferring data from an on-premises or non-cloud source that needs a local agent to read from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	storagetransfer "github.com/ubiquex/ubx-sdk-google/sdk/go/google/storagetransfer"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own agent pool"})

    		ubx.Resource(storagetransfer.AgentPool, "example", storagetransfer.AgentPoolConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AgentPool } from "@ubx/sdk-google/google/storagetransfer/agent_pool";

    export default stack("example", () => {
      intent({ summary: "example own agent pool" });

      resource(AgentPool, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.storagetransfer.agent_pool import AgentPool, AgentPoolConfig

    def describe():
        ubx.intent("example own agent pool")

        ubx.resource(AgentPool, "example", AgentPoolConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_storagetransfer_agent_pool called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="bandwidth_limit" type="object">
  Specifies a bandwidth limit for an agent pool. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="limit_mbps" type="string">
      Bandwidth rate in megabytes per second, distributed across all the agents in the pool. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="display_name" type="string">
  Specifies the client-specified AgentPool description. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. Specifies a unique string that identifies the agent pool. Format: `projects/&#123;project_id&#125;/agentPools/&#123;agent_pool_id&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. Specifies the state of the AgentPool. Optional; if omitted, computed by Google Cloud.
</ResponseField>
