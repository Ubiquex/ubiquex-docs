---
title: "google_backupdr_data_source"
description: "`google_backupdr_data_source` is a workload (e.g. a Compute Engine VM or a Cloud SQL instance) registered with Google Cloud Backup and DR Service as..."
---

`google_backupdr_data_source` is a workload (e.g. a Compute Engine VM or a Cloud SQL instance) registered with Google Cloud Backup and DR Service as something backup plans can be applied to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	backupdr "github.com/ubiquex/ubx-sdk-google/sdk/go/google/backupdr"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own data source"})

    		ubx.Resource(backupdr.DataSource, "example", backupdr.DataSourceConfig{
    			BackupId:  "example",
    			RequestId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataSource } from "./local-sdk/google/sdk/typescript/google/backupdr/data_source.ts";

    export default stack("example", () => {
      intent({ summary: "example own data source" });

      resource(DataSource, "example", {
        backupId: "example",
        requestId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.backupdr import DataSource, DataSourceConfig

    def describe():
        ubx.intent("example own data source")

        ubx.resource(DataSource, "example", DataSourceConfig(
            backup_id="example",
            request_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_backupdr_data_source, with backup id example,
    request id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backup_id" type="string">
  Required. Resource ID of the Backup resource. Optional.
</ResponseField>

<ResponseField name="request_id" type="string">
  Optional. An optional request ID to identify requests. Specify a unique request ID so that if you must retry your request, the server will know to ignore the request if it has already been completed. The server will guarantee that for at least 60 minutes since the first request. For example, consider a situation where you make an initial request and the request times out. If you make the request again with the same request ID, the server can check if original operation with the same request ID was received, and if so, will ignore the second request. This prevents clients from accidentally creating duplicate commitments. The request ID must be a valid UUID with the exception that zero UUID is not supported (00000000-0000-0000-0000-000000000000). Optional.
</ResponseField>

## Output properties

<ResponseField name="backup_blocked_by_vault_access_restriction" type="bool">
  Output only. This field is set to true if the backup is blocked by vault access restriction. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="backup_config_info" type="object">
  BackupConfigInfo has information about how the resource is configured for Backup and about the most recent backup to this vault. Computed by Google Cloud after `ship`.
  <Expandable title="properties">
    <ResponseField name="backup_appliance_backup_config" type="object">
      BackupApplianceBackupConfig captures the backup configuration for applications that are protected by Backup Appliances. Optional.
      <Expandable title="properties">
        <ResponseField name="application_name" type="string">
          The name of the application. Optional.
        </ResponseField>
        <ResponseField name="backup_appliance_id" type="string">
          The ID of the backup appliance. Optional.
        </ResponseField>
        <ResponseField name="backup_appliance_name" type="string">
          The name of the backup appliance. Optional.
        </ResponseField>
        <ResponseField name="host_name" type="string">
          The name of the host where the application is running. Optional.
        </ResponseField>
        <ResponseField name="sla_id" type="string">
          The ID of the SLA of this application. Optional.
        </ResponseField>
        <ResponseField name="slp_name" type="string">
          The name of the SLP associated with the application. Optional.
        </ResponseField>
        <ResponseField name="slt_name" type="string">
          The name of the SLT associated with the application. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="gcp_backup_config" type="object">
      GcpBackupConfig captures the Backup configuration details for Google Cloud resources. All Google Cloud resources regardless of type are protected with backup plan associations. Optional.
      <Expandable title="properties">
        <ResponseField name="backup_plan" type="string">
          The name of the backup plan. Optional.
        </ResponseField>
        <ResponseField name="backup_plan_association" type="string">
          The name of the backup plan association. Optional.
        </ResponseField>
        <ResponseField name="backup_plan_description" type="string">
          The description of the backup plan. Optional.
        </ResponseField>
        <ResponseField name="backup_plan_revision_id" type="string">
          The user friendly id of the backup plan revision. E.g. v0, v1 etc. Optional.
        </ResponseField>
        <ResponseField name="backup_plan_revision_name" type="string">
          The name of the backup plan revision. Optional.
        </ResponseField>
        <ResponseField name="backup_plan_rules" type="list(string)">
          The names of the backup plan rules which point to this backupvault Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_backup_error" type="object">
      The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). Optional.
      <Expandable title="properties">
        <ResponseField name="code" type="number">
          The status code, which should be an enum value of google.rpc.Code. Optional.
        </ResponseField>
        <ResponseField name="details" type="list(map(any))">
          A list of messages that carry the error details. There is a common set of message types for APIs to use. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_backup_state" type="string">
      Output only. The status of the last backup to this BackupVault Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="last_successful_backup_consistency_time" type="string">
      Output only. If the last backup were successful, this field has the consistency date. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="backup_count" type="string">
  Number of backups in the data source. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="config_state" type="string">
  Output only. The backup configuration state. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the instance was created. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="data_source_backup_appliance_application" type="object">
  BackupApplianceApplication describes a Source Resource when it is an application backed up by a BackupAppliance. Computed by Google Cloud after `ship`.
  <Expandable title="properties">
    <ResponseField name="appliance_id" type="string">
      Appliance Id of the Backup Appliance. Optional.
    </ResponseField>
    <ResponseField name="application_id" type="string">
      The appid field of the application within the Backup Appliance. Optional.
    </ResponseField>
    <ResponseField name="application_name" type="string">
      The name of the Application as known to the Backup Appliance. Optional.
    </ResponseField>
    <ResponseField name="backup_appliance" type="string">
      Appliance name. Optional.
    </ResponseField>
    <ResponseField name="host_id" type="string">
      Hostid of the application host. Optional.
    </ResponseField>
    <ResponseField name="hostname" type="string">
      Hostname of the host where the application is running. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of the application. e.g. VMBackup Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_source_gcp_resource" type="object">
  DataSourceGcpResource is used for protected resources that are Google Cloud Resources. This name is easeier to understand than GcpResourceDataSource or GcpDataSourceResource Computed by Google Cloud after `ship`.
  <Expandable title="properties">
    <ResponseField name="alloy_db_cluster_datasource_properties" type="object">
      AlloyDBClusterDataSourceProperties represents the properties of a AlloyDB cluster resource that are stored in the DataSource. . Optional.
      <Expandable title="properties">
        <ResponseField name="cluster_uid" type="string">
          Output only. The cluster UID of the AlloyDB cluster backed up by the datasource. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="name" type="string">
          Output only. Name of the AlloyDB cluster backed up by the datasource. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="pitr_windows" type="list(object)">
          Output only. Point in time recovery windows. The order is guaranteed to be ascending by start time. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="end_time" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="log_retention_days" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="start_time" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cloud_sql_instance_datasource_properties" type="object">
      CloudSqlInstanceDataSourceProperties represents the properties of a Cloud SQL resource that are stored in the DataSource. Optional.
      <Expandable title="properties">
        <ResponseField name="database_installed_version" type="string">
          Output only. The installed database version of the Cloud SQL instance. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="instance_create_time" type="string">
          Output only. The instance creation timestamp. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="instance_tier" type="string">
          Output only. The tier (or machine type) for this instance. Example: `db-custom-1-3840` Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="name" type="string">
          Output only. Name of the Cloud SQL instance backed up by the datasource. Format: projects/&#123;project&#125;/instances/&#123;instance&#125; Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="compute_instance_datasource_properties" type="object">
      ComputeInstanceDataSourceProperties represents the properties of a ComputeEngine resource that are stored in the DataSource. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          The description of the Compute Engine instance. Optional.
        </ResponseField>
        <ResponseField name="machine_type" type="string">
          The machine type of the instance. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the compute instance backed up by the datasource. Optional.
        </ResponseField>
        <ResponseField name="total_disk_count" type="string">
          The total number of disks attached to the Instance. Optional.
        </ResponseField>
        <ResponseField name="total_disk_size_gb" type="string">
          The sum of all the disk sizes. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="disk_datasource_properties" type="object">
      DiskDataSourceProperties represents the properties of a Disk resource that are stored in the DataSource. . Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          The description of the disk. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the disk backed up by the datasource. Optional.
        </ResponseField>
        <ResponseField name="size_gb" type="string">
          The size of the disk in GB. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the disk. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="filestore_instance_datasource_properties" type="object">
      FilestoreInstanceDataSourceProperties represents the properties of a Filestore resource that are stored in the DataSource. . Optional.
      <Expandable title="properties">
        <ResponseField name="instance_create_time" type="string">
          Output only. The instance creation timestamp. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="name" type="string">
          Output only. Name of the Filestore instance backed up by the datasource. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="gcp_resourcename" type="string">
      Output only. Full resource pathname URL of the source Google Cloud resource. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="location" type="string">
      Location of the resource: //"global"/"unspecified". Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of the Google Cloud resource. Use the Unified Resource Type, eg. compute.googleapis.com/Instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Server specified ETag for the ManagementServer resource to prevent simultaneous updates from overwiting each other. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Resource labels to represent user provided metadata. No labels currently defined: Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. Name of the datasource to create. It must have the format`"projects/&#123;project&#125;/locations/&#123;location&#125;/backupVaults/&#123;backupvault&#125;/dataSources/&#123;datasource&#125;"`. `&#123;datasource&#125;` cannot be changed after creation. It must be between 3-63 characters long and must be unique within the backup vault. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The DataSource resource instance state. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="total_stored_bytes" type="string">
  The number of bytes (metadata and data) stored in this datasource. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the instance was updated. Computed by Google Cloud after `ship`.
</ResponseField>
