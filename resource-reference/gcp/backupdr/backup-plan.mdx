---
title: "google_backupdr_backup_plan"
description: "`google_backupdr_backup_plan` defines the backup schedule and retention rule(s) Backup and DR applies to resource(s) it's associated with."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	backupdr "github.com/ubiquex/ubx-sdk-google/sdk/go/google/backupdr"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own backup plan"})

    		ubx.Resource(backupdr.BackupPlan, "example", backupdr.BackupPlanConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BackupPlan } from "@ubx/sdk-google/google/backupdr/backup_plan";

    export default stack("example", () => {
      intent({ summary: "example own backup plan" });

      resource(BackupPlan, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.backupdr.backup_plan import BackupPlan, BackupPlanConfig

    def describe():
        ubx.intent("example own backup plan")

        ubx.resource(BackupPlan, "example", BackupPlanConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_backupdr_backup_plan called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="backup_rules" type="list(object)">
  Optional. The backup rules for this `BackupPlan`. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="backup_retention_days" type="number">
      How many days a backup produced by this rule is retained for. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_id" type="string">
      A reference to the backup rule this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="standard_schedule" type="object">
      A recurrence schedule defined using standard time-based fields. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="backup_window" type="object">
          The time window backups produced by this rule are permitted to start within. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="end_hour_of_day" type="number">
              The hour of the day this backup window ends at. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="start_hour_of_day" type="number">
              The hour of the day this backup window starts at. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="days_of_month" type="list(number)">
          The day(s) of the month this schedule runs on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="days_of_week" type="list(string)">
          The day(s) of the week this schedule runs on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="hourly_frequency" type="number">
          How often, in hours, this schedule recurs within its own window. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="months" type="list(string)">
          The month(s) this schedule runs in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="recurrence_type" type="string">
          How often this backup rule recurs, e.g. hourly, daily, or monthly. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time_zone" type="string">
          The time zone this schedule is evaluated in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="week_day_of_month" type="object">
          The specific weekday-of-month (e.g. the second Tuesday) this schedule runs on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="day_of_week" type="string">
              The day of the week this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="week_of_month" type="string">
              The week of the month this schedule runs on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="backup_vault" type="string">
  Required. Resource name of backup vault which will be used as storage location for backups. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/backupVaults/&#123;backupvault&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="backup_vault_service_account" type="string">
  Output only. The Google Cloud service account to be used by the BackupVault for taking backups. Specify the email address of the Backup Vault Service Account. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="compute_instance_backup_plan_properties" type="object">
  Properties for a compute instance backup plan. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="boot_disk_only" type="bool">
      Optional. If true, only the boot disk will be backed up. Optional.
    </ResponseField>
    <ResponseField name="disk_exclusion_labels" type="object">
      Message for selective disk backup exclusion labels. Optional.
      <Expandable title="properties">
        <ResponseField name="labels" type="list(object)">
          Optional. Labels used to identify disks for exclusion from the backup. If a disk carries any of these labels, it will be excluded (OR logic). Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of this attribute or label. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="guest_flush" type="bool">
      Optional. Indicates whether to perform a guest flush operation before taking a compute backup. When set to false, the system will create crash-consistent backups. Default value is false. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. When the `BackupPlan` was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. The description of the `BackupPlan` resource. The description allows for additional details about `BackupPlan` and its use cases to be provided. An example description is the following: "This is a backup plan that performs a daily backup at 6pm and retains data for 3 months". The description must be at most 2048 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="disk_backup_plan_properties" type="object">
  Properties for a disk backup plan. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="guest_flush" type="bool">
      Optional. Indicates whether to perform a guest flush operation before taking a disk backup. When set to false, the system will create crash-consistent backups. Default value is false. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. `etag` is returned from the service in the response. As a user of the service, you may provide an etag value in this field to prevent stale resources. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. This collection of key/value pairs allows for custom labels to be supplied by the user. Example, &#123;"tag": "Weekly"&#125;. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="log_retention_days" type="string">
  Optional. Applicable only for Cloud SQL resource_type. Configures how long logs will be stored. It is defined in “days”. This value should be greater than or equal to minimum enforced log retention duration of the backup vault. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="max_custom_on_demand_retention_days" type="number">
  Optional. Optional field to configure the maximum number of days for which a backup can be retained. This field is only applicable for on-demand backups taken with custom retention value. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. The resource name of the `BackupPlan`. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/backupPlans/&#123;backup_plan&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Required. The resource type to which the `BackupPlan` will be applied. Examples include, "compute.googleapis.com/Instance", "sqladmin.googleapis.com/Instance", "alloydb.googleapis.com/Cluster", "compute.googleapis.com/Disk". Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="revision_id" type="string">
  Output only. The user friendly revision ID of the `BackupPlanRevision`. Example: v0, v1, v2, etc. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="revision_name" type="string">
  Output only. The resource id of the `BackupPlanRevision`. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/backupPlans/&#123;backup_plan&#125;/revisions/&#123;revision_id&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The `State` for the `BackupPlan`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="supported_resource_types" type="list(string)">
  Output only. All resource types to which backupPlan can be applied. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. When the `BackupPlan` was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
