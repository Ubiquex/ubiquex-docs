---
title: "google_identity_platform_tenant_oauth_idp_config"
description: "Real, generated bindings for `google_identity_platform_tenant_oauth_idp_config`."
---

`google_identity_platform_tenant_oauth_idp_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	identity "github.com/ubiquex/ubx-sdk-google/sdk/go/google/identity"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own platform tenant oauth idp config"})

    		ubx.Resource(identity.PlatformTenantOauthIdpConfig, "ci-runner", identity.PlatformTenantOauthIdpConfigConfig{
    			ClientId:     "example",
    			DisplayName:  "example-display-name",
    			Issuer:       "example",
    			Name:         "ci-runner",
    			Tenant:       "example",
    			ClientSecret: "example",
    			Enabled:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PlatformTenantOauthIdpConfig } from "jsr:@ubx/sdk-google/google/identity/platform_tenant_oauth_idp_config";

    export default stack("payments", () => {
      intent({ summary: "payments own platform tenant oauth idp config" });

      resource(PlatformTenantOauthIdpConfig, "ci-runner", {
        clientId: "example",
        displayName: "example-display-name",
        issuer: "example",
        name: "ci-runner",
        tenant: "example",
        clientSecret: "example",
        enabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.identity import PlatformTenantOauthIdpConfig, PlatformTenantOauthIdpConfigConfig

    def describe():
        ubx.intent("payments own platform tenant oauth idp config")

        ubx.resource(PlatformTenantOauthIdpConfig, "ci-runner", PlatformTenantOauthIdpConfigConfig(
            client_id="example",
            display_name="example-display-name",
            issuer="example",
            name="ci-runner",
            tenant="example",
            client_secret="example",
            enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_identity_platform_tenant_oauth_idp_config called
    "ci-runner", with client id example, display name
    example-display-name, issuer example, tenant example, client
    secret example, enabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="client_id" type="string" required>
  The client id of an OAuth client. Required.
</ResponseField>

<ResponseField name="client_secret" type="string">
  The client secret of the OAuth client, to enable OIDC code flow. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Human friendly display name. Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  If this config allows users to sign in with the provider. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="issuer" type="string" required>
  For OIDC Idps, the issuer identifier. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the OauthIdpConfig. Must start with 'oidc.'. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tenant" type="string" required>
  The name of the tenant where this OIDC IDP configuration resource exists Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
