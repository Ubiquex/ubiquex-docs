---
title: "google_identity_platform_tenant"
description: "Real, generated bindings for `google_identity_platform_tenant`."
---

`google_identity_platform_tenant` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identity "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/identity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own platform tenant"})

    		ubx.Resource(identity.PlatformTenant, "ci-runner", identity.PlatformTenantConfig{
    			DisplayName:           "example-display-name",
    			AllowPasswordSignup:   true,
    			DisableAuth:           true,
    			EnableEmailLinkSignin: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PlatformTenant } from "jsr:@ubx/sdk-google/google/identity/platform_tenant";

    export default stack("payments", () => {
      intent({ summary: "payments own platform tenant" });

      resource(PlatformTenant, "ci-runner", {
        displayName: "example-display-name",
        allowPasswordSignup: true,
        disableAuth: true,
        enableEmailLinkSignin: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.identity import PlatformTenant, PlatformTenantConfig

    def describe():
        ubx.intent("payments own platform tenant")

        ubx.resource(PlatformTenant, "ci-runner", PlatformTenantConfig(
            display_name="example-display-name",
            allow_password_signup=True,
            disable_auth=True,
            enable_email_link_signin=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_identity_platform_tenant, with display name
    example-display-name, allow password signup true, disable auth
    true, enable email link signin true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allow_password_signup" type="bool">
  Optional.
</ResponseField>

<ResponseField name="client" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="permissions" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="disabled_user_deletion" type="bool">
        </ResponseField>
        <ResponseField name="disabled_user_signup" type="bool">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disable_auth" type="bool">
  Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enable_email_link_signin" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="client" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="permissions" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="disabled_user_deletion" type="bool">
        </ResponseField>
        <ResponseField name="disabled_user_signup" type="bool">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
