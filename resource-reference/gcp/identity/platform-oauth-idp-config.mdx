---
title: "google_identity_platform_oauth_idp_config"
description: "Real, generated bindings for `google_identity_platform_oauth_idp_config`."
---

`google_identity_platform_oauth_idp_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	identity "github.com/ubiquex/ubx-sdk-google/sdk/go/google/identity"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own platform oauth idp config"})

    		ubx.Resource(identity.PlatformOauthIdpConfig, "ci-runner", identity.PlatformOauthIdpConfigConfig{
    			ClientId:     "example",
    			Issuer:       "example",
    			Name:         "ci-runner",
    			ClientSecret: "example",
    			DisplayName:  "example-display-name",
    			Enabled:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PlatformOauthIdpConfig } from "jsr:@ubx/sdk-google/google/identity/platform_oauth_idp_config";

    export default stack("payments", () => {
      intent({ summary: "payments own platform oauth idp config" });

      resource(PlatformOauthIdpConfig, "ci-runner", {
        clientId: "example",
        issuer: "example",
        name: "ci-runner",
        clientSecret: "example",
        displayName: "example-display-name",
        enabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.identity import PlatformOauthIdpConfig, PlatformOauthIdpConfigConfig

    def describe():
        ubx.intent("payments own platform oauth idp config")

        ubx.resource(PlatformOauthIdpConfig, "ci-runner", PlatformOauthIdpConfigConfig(
            client_id="example",
            issuer="example",
            name="ci-runner",
            client_secret="example",
            display_name="example-display-name",
            enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_identity_platform_oauth_idp_config called
    "ci-runner", with client id example, issuer example, client
    secret example, display name example-display-name, enabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="client_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_secret" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="issuer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="response_type" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="code" type="bool">
    </ResponseField>
    <ResponseField name="id_token" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="response_type" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="code" type="bool">
    </ResponseField>
    <ResponseField name="id_token" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
