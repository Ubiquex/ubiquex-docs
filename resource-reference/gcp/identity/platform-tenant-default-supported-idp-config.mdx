---
title: "google_identity_platform_tenant_default_supported_idp_config"
description: "Real, generated bindings for `google_identity_platform_tenant_default_supported_idp_config`."
---

`google_identity_platform_tenant_default_supported_idp_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identity "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/identity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own platform tenant default supported idp config"})

    		ubx.Resource(identity.PlatformTenantDefaultSupportedIdpConfig, "ci-runner", identity.PlatformTenantDefaultSupportedIdpConfigConfig{
    			ClientId:     "example",
    			ClientSecret: "example",
    			IdpId:        "example",
    			Tenant:       "example",
    			Enabled:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PlatformTenantDefaultSupportedIdpConfig } from "jsr:@ubx/sdk-google/google/identity/platform_tenant_default_supported_idp_config";

    export default stack("payments", () => {
      intent({
        summary: "payments own platform tenant default supported idp config",
      });

      resource(PlatformTenantDefaultSupportedIdpConfig, "ci-runner", {
        clientId: "example",
        clientSecret: "example",
        idpId: "example",
        tenant: "example",
        enabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.identity import PlatformTenantDefaultSupportedIdpConfig, PlatformTenantDefaultSupportedIdpConfigConfig

    def describe():
        ubx.intent("payments own platform tenant default supported idp config")

        ubx.resource(PlatformTenantDefaultSupportedIdpConfig, "ci-runner", PlatformTenantDefaultSupportedIdpConfigConfig(
            client_id="example",
            client_secret="example",
            idp_id="example",
            tenant="example",
            enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    google_identity_platform_tenant_default_supported_idp_config,
    with client id example, client secret example, idp id example,
    tenant example, enabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="client_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_secret" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="idp_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tenant" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
