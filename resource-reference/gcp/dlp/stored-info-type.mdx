---
title: "google_dlp_stored_info_type"
description: "`google_dlp_stored_info_type` defines a custom, reusable sensitive-data pattern (a dictionary or regex) Sensitive Data Protection scans can detect..."
---

`google_dlp_stored_info_type` defines a custom, reusable sensitive-data pattern (a dictionary or regex) Sensitive Data Protection scans can detect, beyond its own built-in info types.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_dlp --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_dlp/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dlp "github.com/ubiquex/ubx-sdk-google/sdk/go/google_dlp/dlp"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own stored info type"})

    		ubx.Resource(dlp.DlpStoredInfoType, "ci-runner", dlp.DlpStoredInfoTypeConfig{
    			Config:           map[string]any{"description": "example"},
    			LocationId:       "example",
    			StoredInfoTypeId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_dlp --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DlpStoredInfoType } from "./local-sdk/google_dlp/sdk/typescript/google_dlp/dlp/dlp_stored_info_type.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own stored info type" });

      resource(DlpStoredInfoType, "ci-runner", {
        config: { description: "example" },
        locationId: "example",
        storedInfoTypeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_dlp --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_dlp/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_dlp.dlp import DlpStoredInfoType, DlpStoredInfoTypeConfig

    def describe():
        ubx.intent("payments own stored info type")

        ubx.resource(DlpStoredInfoType, "ci-runner", DlpStoredInfoTypeConfig(
            config={"description": "example"},
            location_id="example",
            stored_info_type_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dlp_stored_info_type, with config a set value,
    location id example, stored info type id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="config" type="object">
  Configuration for stored infoTypes. All fields and subfield are provided by the user. For more information, see https://docs.cloud.google.com/sensitive-data-protection/docs/creating-custom-infotypes. Optional.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Description of the StoredInfoType (max 256 characters). Optional.
    </ResponseField>
    <ResponseField name="dictionary" type="object">
      Custom information type based on a dictionary of words or phrases. This can be used to match sensitive information specific to the data, such as a list of employee IDs or job titles. Dictionary words are case-insensitive and all characters other than letters and digits in the unicode [Basic Multilingual Plane](https://en.wikipedia.org/wiki/Plane_%28Unicode%29#Basic_Multilingual_Plane) will be replaced with whitespace when scanning for matches, so the dictionary phrase "Sam Johnson" will match all three phrases "sam johnson", "Sam, Johnson", and "Sam (Johnson)". Additionally, the characters surrounding any match must be of a different type than the adjacent characters within the word, so letters must be next to non-letters and digits next to non-digits. For example, the dictionary word "jen" will match the first three letters of the text "jen123" but will return no matches for "jennifer". Dictionary words containing a large number of characters that are not letters or digits may result in unexpected findings because such characters are treated as whitespace. The [limits](https://docs.cloud.google.com/sensitive-data-protection/limits) page contains details about the size limits of dictionaries. For dictionaries that do not fit within these constraints, consider using `LargeCustomDictionaryConfig` in the `StoredInfoType` API. Optional.
      <Expandable title="properties">
        <ResponseField name="cloud_storage_path" type="object">
          Message representing a single file or path in Cloud Storage. Optional.
          <Expandable title="properties">
            <ResponseField name="path" type="string">
              A URL representing a file or path (no wildcards) in Cloud Storage. Example: `gs://[BUCKET_NAME]/dictionary.txt` Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="word_list" type="object">
          Message defining a list of words or phrases to search for in the data. Optional.
          <Expandable title="properties">
            <ResponseField name="words" type="list(string)">
              Words or phrases defining the dictionary. The dictionary must contain at least one phrase and every phrase must contain at least 2 characters that are letters or digits. [required] Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="display_name" type="string">
      Display name of the StoredInfoType (max 256 characters). Optional.
    </ResponseField>
    <ResponseField name="large_custom_dictionary" type="object">
      Configuration for a custom dictionary created from a data source of any size up to the maximum size defined in the [limits](https://docs.cloud.google.com/sensitive-data-protection/limits) page. The artifacts of dictionary creation are stored in the specified Cloud Storage location. Consider using `CustomInfoType.Dictionary` for smaller dictionaries that satisfy the size requirements. Optional.
      <Expandable title="properties">
        <ResponseField name="big_query_field" type="object">
          Message defining a field of a BigQuery table. Optional.
          <Expandable title="properties">
            <ResponseField name="field" type="object">
              General identifier of a data field in a storage service. Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  Name describing the field. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="table" type="object">
              Message defining the location of a BigQuery table. A table is uniquely identified by its project_id, dataset_id, and table_name. Within a query a table is often referenced with a string in the format of: `:.` or `..`. Optional.
              <Expandable title="properties">
                <ResponseField name="dataset_id" type="string">
                  Dataset ID of the table. Optional.
                </ResponseField>
                <ResponseField name="project_id" type="string">
                  The Google Cloud project ID of the project containing the table. If omitted, project ID is inferred from the API call. Optional.
                </ResponseField>
                <ResponseField name="table_id" type="string">
                  Name of the table. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="cloud_storage_file_set" type="object">
          Message representing a set of files in Cloud Storage. Optional.
          <Expandable title="properties">
            <ResponseField name="url" type="string">
              The url, in the format `gs:///`. Trailing wildcard in the path is allowed. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="output_path" type="object">
          Message representing a single file or path in Cloud Storage. Optional.
          <Expandable title="properties">
            <ResponseField name="path" type="string">
              A URL representing a file or path (no wildcards) in Cloud Storage. Example: `gs://[BUCKET_NAME]/dictionary.txt` Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="regex" type="object">
      Message defining a custom regular expression. Optional.
      <Expandable title="properties">
        <ResponseField name="group_indexes" type="list(number)">
          The index of the submatch to extract as findings. When not specified, the entire match is returned. No more than 3 may be included. Optional.
        </ResponseField>
        <ResponseField name="pattern" type="string">
          Pattern defining the regular expression. Its syntax (https://github.com/google/re2/wiki/Syntax) can be found under the google/re2 repository on GitHub. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location_id" type="string">
  Deprecated. This field has no effect. Optional.
</ResponseField>

<ResponseField name="stored_info_type_id" type="string">
  The storedInfoType ID can contain uppercase and lowercase letters, numbers, and hyphens; that is, it must match the regular expression: `[a-zA-Z\d-_]+`. The maximum length is 100 characters. Can be empty to allow the system to generate one. Optional.
</ResponseField>

## Output properties

<ResponseField name="current_version" type="object">
  Version of a StoredInfoType, including the configuration used to build it, create timestamp, and current state. Computed by Google Cloud after `ship`.
  <Expandable title="properties">
    <ResponseField name="config" type="object">
      Configuration for stored infoTypes. All fields and subfield are provided by the user. For more information, see https://docs.cloud.google.com/sensitive-data-protection/docs/creating-custom-infotypes. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Description of the StoredInfoType (max 256 characters). Optional.
        </ResponseField>
        <ResponseField name="dictionary" type="object">
          Custom information type based on a dictionary of words or phrases. This can be used to match sensitive information specific to the data, such as a list of employee IDs or job titles. Dictionary words are case-insensitive and all characters other than letters and digits in the unicode [Basic Multilingual Plane](https://en.wikipedia.org/wiki/Plane_%28Unicode%29#Basic_Multilingual_Plane) will be replaced with whitespace when scanning for matches, so the dictionary phrase "Sam Johnson" will match all three phrases "sam johnson", "Sam, Johnson", and "Sam (Johnson)". Additionally, the characters surrounding any match must be of a different type than the adjacent characters within the word, so letters must be next to non-letters and digits next to non-digits. For example, the dictionary word "jen" will match the first three letters of the text "jen123" but will return no matches for "jennifer". Dictionary words containing a large number of characters that are not letters or digits may result in unexpected findings because such characters are treated as whitespace. The [limits](https://docs.cloud.google.com/sensitive-data-protection/limits) page contains details about the size limits of dictionaries. For dictionaries that do not fit within these constraints, consider using `LargeCustomDictionaryConfig` in the `StoredInfoType` API. Optional.
          <Expandable title="properties">
            <ResponseField name="cloud_storage_path" type="object">
              Message representing a single file or path in Cloud Storage. Optional.
              <Expandable title="properties">
                <ResponseField name="path" type="string">
                  A URL representing a file or path (no wildcards) in Cloud Storage. Example: `gs://[BUCKET_NAME]/dictionary.txt` Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="word_list" type="object">
              Message defining a list of words or phrases to search for in the data. Optional.
              <Expandable title="properties">
                <ResponseField name="words" type="list(string)">
                  Words or phrases defining the dictionary. The dictionary must contain at least one phrase and every phrase must contain at least 2 characters that are letters or digits. [required] Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="display_name" type="string">
          Display name of the StoredInfoType (max 256 characters). Optional.
        </ResponseField>
        <ResponseField name="large_custom_dictionary" type="object">
          Configuration for a custom dictionary created from a data source of any size up to the maximum size defined in the [limits](https://docs.cloud.google.com/sensitive-data-protection/limits) page. The artifacts of dictionary creation are stored in the specified Cloud Storage location. Consider using `CustomInfoType.Dictionary` for smaller dictionaries that satisfy the size requirements. Optional.
          <Expandable title="properties">
            <ResponseField name="big_query_field" type="object">
              Message defining a field of a BigQuery table. Optional.
              <Expandable title="properties">
                <ResponseField name="field" type="object">
                  General identifier of a data field in a storage service. Optional.
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Name describing the field. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="table" type="object">
                  Message defining the location of a BigQuery table. A table is uniquely identified by its project_id, dataset_id, and table_name. Within a query a table is often referenced with a string in the format of: `:.` or `..`. Optional.
                  <Expandable title="properties">
                    <ResponseField name="dataset_id" type="string">
                      Dataset ID of the table. Optional.
                    </ResponseField>
                    <ResponseField name="project_id" type="string">
                      The Google Cloud project ID of the project containing the table. If omitted, project ID is inferred from the API call. Optional.
                    </ResponseField>
                    <ResponseField name="table_id" type="string">
                      Name of the table. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cloud_storage_file_set" type="object">
              Message representing a set of files in Cloud Storage. Optional.
              <Expandable title="properties">
                <ResponseField name="url" type="string">
                  The url, in the format `gs:///`. Trailing wildcard in the path is allowed. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="output_path" type="object">
              Message representing a single file or path in Cloud Storage. Optional.
              <Expandable title="properties">
                <ResponseField name="path" type="string">
                  A URL representing a file or path (no wildcards) in Cloud Storage. Example: `gs://[BUCKET_NAME]/dictionary.txt` Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="regex" type="object">
          Message defining a custom regular expression. Optional.
          <Expandable title="properties">
            <ResponseField name="group_indexes" type="list(number)">
              The index of the submatch to extract as findings. When not specified, the entire match is returned. No more than 3 may be included. Optional.
            </ResponseField>
            <ResponseField name="pattern" type="string">
              Pattern defining the regular expression. Its syntax (https://github.com/google/re2/wiki/Syntax) can be found under the google/re2 repository on GitHub. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. Create timestamp of the version. Read-only, determined by the system when the version is created. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="errors" type="list(object)">
      Output only. Errors that occurred when creating this storedInfoType version, or anomalies detected in the storedInfoType data that render it unusable. Only the five most recent errors will be displayed, with the most recent error appearing first. For example, some of the data for stored custom dictionaries is put in the user's Cloud Storage bucket, and if this data is modified or deleted by the user or another system, the dictionary becomes invalid. If any errors occur, fix the problem indicated by the error message and use the UpdateStoredInfoType API method to create another version of the storedInfoType to continue using it, reusing the same `config` if it was not the source of the error. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="details" type="object">
          Additional detail explaining this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="code" type="number">
              A machine-readable code identifying this result or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="details" type="list(map(any))">
              Additional detail explaining this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this result or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="extra_info" type="string">
          Additional detail about this event. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="timestamps" type="list(string)">
          The timestamp(s) recorded for this event. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. Stored info type version state. Read-only, updated by the system during dictionary creation. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="stats" type="object">
      Statistics for a StoredInfoType. Optional.
      <Expandable title="properties">
        <ResponseField name="large_custom_dictionary" type="object">
          Summary statistics of a custom dictionary. Optional.
          <Expandable title="properties">
            <ResponseField name="approx_num_phrases" type="string">
              Approximate number of distinct phrases in the dictionary. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Resource name. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="pending_versions" type="list(object)">
  Pending versions of the stored info type. Empty if no versions are pending. Computed by Google Cloud after `ship`.
  <Expandable title="properties">
    <ResponseField name="config" type="object">
      The configuration for this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dictionary" type="object">
          A custom info type defined by matching against a supplied word list or file, rather than a regex. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cloud_storage_path" type="object">
              A `gs://` Cloud Storage path this configuration reads from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="path" type="string">
                  A file or object path this configuration reads from or writes to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="word_list" type="object">
              A custom info type defined by an explicit, literal list of words/phrases to match, rather than a regex or dictionary file. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="words" type="list(string)">
                  The literal words/phrases making up this `word_list`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="display_name" type="string">
          A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="large_custom_dictionary" type="object">
          A custom dictionary sourced from a large external file (BigQuery table or Cloud Storage files), rather than an inline word list. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="big_query_field" type="object">
              A BigQuery table column supplying phrases for a large custom dictionary. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="field" type="object">
                  A reference to the field this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The identifying name for this resource, its own field, or its own info type. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="table" type="object">
                  A reference to the table this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dataset_id" type="string">
                      The BigQuery dataset ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="project_id" type="string">
                      The Google Cloud project ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="table_id" type="string">
                      The BigQuery table ID this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cloud_storage_file_set" type="object">
              The Cloud Storage file(s) supplying phrases for a large custom dictionary. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="url" type="string">
                  A URL or URI this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="output_path" type="object">
              The destination path processed output is written to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="path" type="string">
                  A file or object path this configuration reads from or writes to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="regex" type="object">
          A regular expression this rule matches against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="group_indexes" type="list(number)">
              For a regex with capture groups, which group number(s) the match applies to, instead of the whole match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pattern" type="string">
              A regular expression a `string`-typed value must match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. The timestamp when this resource was created. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="errors" type="list(object)">
      The error(s) encountered. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="details" type="object">
          Additional detail explaining this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="code" type="number">
              A machine-readable code identifying this result or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="details" type="list(map(any))">
              Additional detail explaining this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this result or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="extra_info" type="string">
          Additional detail about this event. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="timestamps" type="list(string)">
          The timestamp(s) recorded for this event. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string">
      The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="stats" type="object">
      Summary statistics for this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="large_custom_dictionary" type="object">
          A custom dictionary sourced from a large external file (BigQuery table or Cloud Storage files), rather than an inline word list. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="approx_num_phrases" type="string">
              Output only. An approximate count of the phrases loaded into this large custom dictionary. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
