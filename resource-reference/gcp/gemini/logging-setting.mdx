---
title: "google_gemini_logging_setting"
description: "Real, generated bindings for `google_gemini_logging_setting`."
---

`google_gemini_logging_setting` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gemini "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/gemini"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own logging setting"})

    		ubx.Resource(gemini.LoggingSetting, "ci-runner", gemini.LoggingSettingConfig{
    			Location:               "example",
    			LoggingSettingId:       "example",
    			Labels:                 map[string]string{"managed-by": "ubx"},
    			LogMetadata:            true,
    			LogPromptsAndResponses: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LoggingSetting } from "jsr:@ubx/sdk-google/google/gemini/logging_setting";

    export default stack("payments", () => {
      intent({ summary: "payments own logging setting" });

      resource(LoggingSetting, "ci-runner", {
        location: "example",
        loggingSettingId: "example",
        labels: { "managed-by": "ubx" },
        logMetadata: true,
        logPromptsAndResponses: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.gemini import LoggingSetting, LoggingSettingConfig

    def describe():
        ubx.intent("payments own logging setting")

        ubx.resource(LoggingSetting, "ci-runner", LoggingSettingConfig(
            location="example",
            logging_setting_id="example",
            labels={"managed-by": "ubx"},
            log_metadata=True,
            log_prompts_and_responses=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gemini_logging_setting, with location example,
    logging setting id example, labels a set value, log metadata
    true, log prompts and responses true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="log_metadata" type="bool">
  Optional.
</ResponseField>

<ResponseField name="log_prompts_and_responses" type="bool">
  Optional.
</ResponseField>

<ResponseField name="logging_setting_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
