---
title: "google_gemini_repository_group_iam_policy"
description: "Real, generated bindings for `google_gemini_repository_group_iam_policy`."
---

`google_gemini_repository_group_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gemini "github.com/ubiquex/ubx-sdk-google/sdk/go/google/gemini"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own repository group iam policy"})

    		ubx.Resource(gemini.RepositoryGroupIamPolicy, "ci-runner", gemini.RepositoryGroupIamPolicyConfig{
    			CodeRepositoryIndex: "example",
    			PolicyData:          "example",
    			RepositoryGroupId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RepositoryGroupIamPolicy } from "jsr:@ubx/sdk-google/google/gemini/repository_group_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own repository group iam policy" });

      resource(RepositoryGroupIamPolicy, "ci-runner", {
        codeRepositoryIndex: "example",
        policyData: "example",
        repositoryGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.gemini import RepositoryGroupIamPolicy, RepositoryGroupIamPolicyConfig

    def describe():
        ubx.intent("payments own repository group iam policy")

        ubx.resource(RepositoryGroupIamPolicy, "ci-runner", RepositoryGroupIamPolicyConfig(
            code_repository_index="example",
            policy_data="example",
            repository_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gemini_repository_group_iam_policy, with code
    repository index example, policy data example, repository group
    id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="code_repository_index" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="repository_group_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
