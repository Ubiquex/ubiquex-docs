---
title: "google_privateca_ca_pool_iam_policy"
description: "Real, generated bindings for `google_privateca_ca_pool_iam_policy`."
---

`google_privateca_ca_pool_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/privateca"
    )

    cfg := privateca.CaPoolIamPolicyConfig{
        CaPool: "example",
        PolicyData: "example",
    }
    ubx.Resource(privateca.CaPoolIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CaPoolIamPolicy } from
      "jsr:@ubx/sdk-google/google/privateca/ca_pool_iam_policy";

    resource(CaPoolIamPolicy, "example", {
      caPool: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.privateca import (
        CaPoolIamPolicy,
        CaPoolIamPolicyConfig,
    )

    cfg = CaPoolIamPolicyConfig(
        ca_pool="example",
        policy_data="example",
    )
    ubx.resource(CaPoolIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_privateca_ca_pool_iam_policy with ca_pool,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="ca_pool" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
