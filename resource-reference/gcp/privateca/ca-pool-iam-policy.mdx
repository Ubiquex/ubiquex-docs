---
title: "google_privateca_ca_pool_iam_policy"
description: "Real, generated bindings for `google_privateca_ca_pool_iam_policy`."
---

`google_privateca_ca_pool_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	privateca "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/privateca"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ca pool iam policy"})

    		ubx.Resource(privateca.CaPoolIamPolicy, "ci-runner", privateca.CaPoolIamPolicyConfig{
    			CaPool:     "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CaPoolIamPolicy } from "jsr:@ubx/sdk-google/google/privateca/ca_pool_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own ca pool iam policy" });

      resource(CaPoolIamPolicy, "ci-runner", {
        caPool: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.privateca import CaPoolIamPolicy, CaPoolIamPolicyConfig

    def describe():
        ubx.intent("payments own ca pool iam policy")

        ubx.resource(CaPoolIamPolicy, "ci-runner", CaPoolIamPolicyConfig(
            ca_pool="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_privateca_ca_pool_iam_policy, with ca pool example,
    policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="ca_pool" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
