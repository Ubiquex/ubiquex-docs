---
title: "google_privateca_certificate_template_iam_policy"
description: "Real, generated bindings for `google_privateca_certificate_template_iam_policy`."
---

`google_privateca_certificate_template_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/privateca"
    )

    cfg := privateca.CertificateTemplateIamPolicyConfig{
        CertificateTemplate: "example",
        PolicyData: "example",
    }
    ubx.Resource(privateca.CertificateTemplateIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CertificateTemplateIamPolicy } from
      "jsr:@ubx/sdk-google/google/privateca/certificate_template_iam_policy";

    resource(CertificateTemplateIamPolicy, "example", {
      certificateTemplate: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.privateca import (
        CertificateTemplateIamPolicy,
        CertificateTemplateIamPolicyConfig,
    )

    cfg = CertificateTemplateIamPolicyConfig(
        certificate_template="example",
        policy_data="example",
    )
    ubx.resource(CertificateTemplateIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_privateca_certificate_template_iam_policy with
certificate_template, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate_template" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
