---
title: "google_firebase_app_hosting_build"
description: "Real, generated bindings for `google_firebase_app_hosting_build`."
---

`google_firebase_app_hosting_build` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebase "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firebase"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app hosting build"})

    		ubx.Resource(firebase.AppHostingBuild, "ci-runner", firebase.AppHostingBuildConfig{
    			Backend:     "example",
    			BuildId:     "example",
    			Location:    "example",
    			Annotations: map[string]string{"managed-by": "ubx"},
    			DisplayName: "example-display-name",
    			Labels:      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppHostingBuild } from "jsr:@ubx/sdk-google/google/firebase/app_hosting_build";

    export default stack("payments", () => {
      intent({ summary: "payments own app hosting build" });

      resource(AppHostingBuild, "ci-runner", {
        backend: "example",
        buildId: "example",
        location: "example",
        annotations: { "managed-by": "ubx" },
        displayName: "example-display-name",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firebase import AppHostingBuild, AppHostingBuildConfig

    def describe():
        ubx.intent("payments own app hosting build")

        ubx.resource(AppHostingBuild, "ci-runner", AppHostingBuildConfig(
            backend="example",
            build_id="example",
            location="example",
            annotations={"managed-by": "ubx"},
            display_name="example-display-name",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebase_app_hosting_build, with backend example,
    build id example, location example, annotations a set value,
    display name example-display-name, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="annotations" type="map(string)">
  Unstructured key value map that may be set by external tools to store and arbitrary metadata. They are not queryable and should be preserved when modifying objects. **Note**: This field is non-authoritative, and will only manage the annotations present in your configuration. Please refer to the field 'effective_annotations' for all of the annotations present on the resource. Optional.
</ResponseField>

<ResponseField name="backend" type="string" required>
  The ID of the Backend that this Build applies to Required.
</ResponseField>

<ResponseField name="build_id" type="string" required>
  The user-specified ID of the build being created. Required.
</ResponseField>

<ResponseField name="display_name" type="string">
  Human-readable name. 63 character limit. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Unstructured key value map that can be used to organize and categorize objects. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location of the Backend that this Build applies to Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="codebase" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="author" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="display_name" type="string">
            </ResponseField>
            <ResponseField name="email" type="string">
            </ResponseField>
            <ResponseField name="image_uri" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="branch" type="string">
        </ResponseField>
        <ResponseField name="commit" type="string">
        </ResponseField>
        <ResponseField name="commit_message" type="string">
        </ResponseField>
        <ResponseField name="commit_time" type="string">
        </ResponseField>
        <ResponseField name="display_name" type="string">
        </ResponseField>
        <ResponseField name="hash" type="string">
        </ResponseField>
        <ResponseField name="uri" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="container" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="image" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="build_logs_uri" type="string">
  The location of the [Cloud Build logs](https://cloud.google.com/build/docs/view-build-results) for the build process. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Time at which the build was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="effective_annotations" type="map(string)">
  All of annotations (key/value pairs) present on the resource in GCP, including the annotations configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="environment" type="string">
  The environment name of the backend when this build was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="error" type="list(object)">
  The 'Status' type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each 'Status' message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
    </ResponseField>
    <ResponseField name="details" type="list(map(string))">
    </ResponseField>
    <ResponseField name="message" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="error_source" type="string">
  The source of the error for the build, if in a 'FAILED' state. Possible values: CLOUD_BUILD CLOUD_RUN Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Server-computed checksum based on other values; may be sent on update or delete to ensure operation is done on expected resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="image" type="string">
  The Artifact Registry [container image](https://cloud.google.com/artifact-registry/docs/reference/rest/v1/projects.locations.repositories.dockerImages) URI, used by the Cloud Run ['revision'](https://cloud.google.com/run/docs/reference/rest/v2/projects.locations.services.revisions) for this build. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the build. Format: 'projects/&#123;project&#125;/locations/&#123;locationId&#125;/backends/&#123;backendId&#125;/builds/&#123;buildId&#125;'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="codebase" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="author" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="display_name" type="string">
            </ResponseField>
            <ResponseField name="email" type="string">
            </ResponseField>
            <ResponseField name="image_uri" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="branch" type="string">
        </ResponseField>
        <ResponseField name="commit" type="string">
        </ResponseField>
        <ResponseField name="commit_message" type="string">
        </ResponseField>
        <ResponseField name="commit_time" type="string">
        </ResponseField>
        <ResponseField name="display_name" type="string">
        </ResponseField>
        <ResponseField name="hash" type="string">
        </ResponseField>
        <ResponseField name="uri" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="container" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="image" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  The state of the build. Possible values: BUILDING BUILT DEPLOYING READY FAILED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  System-assigned, unique identifier. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Time at which the build was last updated. Computed by GCP after `ship`.
</ResponseField>
