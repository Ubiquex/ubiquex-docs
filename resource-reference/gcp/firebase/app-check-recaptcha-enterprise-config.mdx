---
title: "google_firebase_app_check_recaptcha_enterprise_config"
description: "Real, generated bindings for `google_firebase_app_check_recaptcha_enterprise_config`."
---

`google_firebase_app_check_recaptcha_enterprise_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	firebase "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/firebase"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app check recaptcha enterprise config"})

    		ubx.Resource(firebase.AppCheckRecaptchaEnterpriseConfig, "ci-runner", firebase.AppCheckRecaptchaEnterpriseConfigConfig{
    			AppId:   "example",
    			SiteKey: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppCheckRecaptchaEnterpriseConfig } from "jsr:@ubx/sdk-google/google/firebase/app_check_recaptcha_enterprise_config";

    export default stack("payments", () => {
      intent({ summary: "payments own app check recaptcha enterprise config" });

      resource(AppCheckRecaptchaEnterpriseConfig, "ci-runner", {
        appId: "example",
        siteKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firebase import AppCheckRecaptchaEnterpriseConfig, AppCheckRecaptchaEnterpriseConfigConfig

    def describe():
        ubx.intent("payments own app check recaptcha enterprise config")

        ubx.resource(AppCheckRecaptchaEnterpriseConfig, "ci-runner", AppCheckRecaptchaEnterpriseConfigConfig(
            app_id="example",
            site_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebase_app_check_recaptcha_enterprise_config,
    with app id example, site key example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="site_key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="token_ttl" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="token_ttl" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
