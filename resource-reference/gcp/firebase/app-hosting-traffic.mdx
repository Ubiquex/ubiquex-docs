---
title: "google_firebase_app_hosting_traffic"
description: "Real, generated bindings for `google_firebase_app_hosting_traffic`."
---

`google_firebase_app_hosting_traffic` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebase "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firebase"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app hosting traffic"})

    		ubx.Resource(firebase.AppHostingTraffic, "ci-runner", firebase.AppHostingTrafficConfig{
    			Backend:  "example",
    			Location: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppHostingTraffic } from "jsr:@ubx/sdk-google/google/firebase/app_hosting_traffic";

    export default stack("payments", () => {
      intent({ summary: "payments own app hosting traffic" });

      resource(AppHostingTraffic, "ci-runner", {
        backend: "example",
        location: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firebase import AppHostingTraffic, AppHostingTrafficConfig

    def describe():
        ubx.intent("payments own app hosting traffic")

        ubx.resource(AppHostingTraffic, "ci-runner", AppHostingTrafficConfig(
            backend="example",
            location="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebase_app_hosting_traffic, with backend example,
    location example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backend" type="string" required>
  Id of the backend that this Traffic config applies to Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location the Backend that this Traffic config applies to Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="rollout_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="codebase_branch" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="disabled_time" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="splits" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="build" type="string" required>
        </ResponseField>
        <ResponseField name="percent" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Time at which the backend was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="current" type="list(object)">
  Current state of traffic allocation for the backend. When setting 'target', this field may differ for some time until the desired state is reached. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="splits" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="build" type="string">
        </ResponseField>
        <ResponseField name="percent" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="delete_time" type="string">
  Time at which the backend was deleted. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Server-computed checksum based on other values; may be sent on update or delete to ensure operation is done on expected resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the backend traffic config Format: 'projects/&#123;project&#125;/locations/&#123;locationId&#125;/backends/&#123;backendId&#125;/traffic'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="rollout_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="codebase_branch" type="string">
    </ResponseField>
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="disabled_time" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="splits" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="build" type="string" required>
        </ResponseField>
        <ResponseField name="percent" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  System-assigned, unique identifier. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Time at which the backend was last updated. Computed by GCP after `ship`.
</ResponseField>
