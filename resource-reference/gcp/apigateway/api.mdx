---
title: "google_apigateway_api"
description: "`google_apigateway_api` is the top-level API Gateway resource representing one logical API..."
---

`google_apigateway_api` is the top-level API Gateway resource representing one logical API, the parent every `google_apigateway_config` version is created and deployed under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigateway "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigateway"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own api"})

    		ubx.Resource(apigateway.Api, "example", apigateway.ApiConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Api } from "@ubx/sdk-google/google/apigateway/api";

    export default stack("example", () => {
      intent({ summary: "example own api" });

      resource(Api, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigateway import Api, ApiConfig

    def describe():
        ubx.intent("example own api")

        ubx.resource(Api, "example", ApiConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigateway_api called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Created time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Display name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Resource labels to represent user-provided metadata. Refer to cloud documentation on labels for more details. https://cloud.google.com/compute/docs/labeling-resources Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="managed_service" type="string">
  Optional. Immutable. The name of a Google Managed Service ( https://cloud.google.com/service-infrastructure/docs/glossary#managed). If not specified, a new Service will automatically be created in the same project as this API. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Resource name of the API. Format: projects/&#123;project&#125;/locations/global/apis/&#123;api&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the API. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Updated time. Optional; if omitted, computed by Google Cloud.
</ResponseField>
