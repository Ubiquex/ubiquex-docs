---
title: "google_apigateway_v1beta_gateway"
description: "`google_apigateway_v1beta_gateway` is a real, Envoy-based managed proxy that hosts one deployed `google_apigateway_config`, giving that API a public..."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_apigateway_gateway`.
</Warning>

`google_apigateway_v1beta_gateway` is a real, Envoy-based managed proxy that hosts one deployed `google_apigateway_config`, giving that API a public, externally-reachable URL on the `gateway.dev` domain.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_apigateway_v1beta --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_apigateway_v1beta/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigateway "github.com/ubiquex/ubx-sdk-google/sdk/go/google_apigateway_v1beta/apigateway"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta gateway"})

    		ubx.Resource(apigateway.V1betaApigatewayV1betaGateway, "example", apigateway.V1betaApigatewayV1betaGatewayConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_apigateway_v1beta --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaApigatewayV1betaGateway } from "./local-sdk/google_apigateway_v1beta/sdk/typescript/google_apigateway_v1beta/apigateway/v1beta_apigateway_v1beta_gateway.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta gateway" });

      resource(V1betaApigatewayV1betaGateway, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_apigateway_v1beta --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_apigateway_v1beta/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_apigateway_v1beta.apigateway import V1betaApigatewayV1betaGateway, V1betaApigatewayV1betaGatewayConfig

    def describe():
        ubx.intent("example own v1beta gateway")

        ubx.resource(V1betaApigatewayV1betaGateway, "example", V1betaApigatewayV1betaGatewayConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigateway_v1beta_gateway called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="api_config" type="string">
  Required. Resource name of the API Config for this Gateway. Format: projects/&#123;project&#125;/locations/global/apis/&#123;api&#125;/configs/&#123;apiConfig&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Created time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_hostname" type="string">
  Output only. The default API Gateway host name of the form `&#123;gateway_id&#125;-&#123;hash&#125;.&#123;region_code&#125;.gateway.dev`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Display name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Resource labels to represent user-provided metadata. Refer to cloud documentation on labels for more details. https://cloud.google.com/compute/docs/labeling-resources Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Resource name of the Gateway. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/gateways/&#123;gateway&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the Gateway. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Updated time. Optional; if omitted, computed by Google Cloud.
</ResponseField>
