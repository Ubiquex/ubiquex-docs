---
title: "google_apigateway_gateway"
description: "`google_apigateway_gateway` is a real, Envoy-based managed proxy that hosts one deployed `google_apigateway_config`, giving that API a public..."
---

`google_apigateway_gateway` is a real, Envoy-based managed proxy that hosts one deployed `google_apigateway_config`, giving that API a public, externally-reachable URL on the `gateway.dev` domain.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_apigateway --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_apigateway/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigateway "github.com/ubiquex/ubx-sdk-google/sdk/go/google_apigateway/apigateway"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway"})

    		ubx.Resource(apigateway.ApigatewayGateway, "ci-runner", apigateway.ApigatewayGatewayConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_apigateway --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApigatewayGateway } from "./local-sdk/google_apigateway/sdk/typescript/google_apigateway/apigateway/apigateway_gateway.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway" });

      resource(ApigatewayGateway, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_apigateway --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_apigateway/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_apigateway.apigateway import ApigatewayGateway, ApigatewayGatewayConfig

    def describe():
        ubx.intent("payments own gateway")

        ubx.resource(ApigatewayGateway, "ci-runner", ApigatewayGatewayConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigateway_gateway called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="api_config" type="string">
  Required. Resource name of the API Config for this Gateway. Format: projects/&#123;project&#125;/locations/global/apis/&#123;api&#125;/configs/&#123;apiConfig&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Created time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_hostname" type="string">
  Output only. The default API Gateway host name of the form `&#123;gateway_id&#125;-&#123;hash&#125;.&#123;region_code&#125;.gateway.dev`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Display name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Resource labels to represent user-provided metadata. Refer to cloud documentation on labels for more details. https://cloud.google.com/compute/docs/labeling-resources Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Resource name of the Gateway. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/gateways/&#123;gateway&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the Gateway. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Updated time. Optional; if omitted, computed by Google Cloud.
</ResponseField>
