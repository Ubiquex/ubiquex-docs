---
title: "google_migration_center_source"
description: "Real, generated bindings for `google_migration_center_source`."
---

`google_migration_center_source` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	migration "github.com/ubiquex/ubx-sdk-google/sdk/go/google/migration"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own center source"})

    		ubx.Resource(migration.CenterSource, "ci-runner", migration.CenterSourceConfig{
    			Location:    "example",
    			SourceId:    "example",
    			Description: "Managed by ubx.",
    			DisplayName: "example-display-name",
    			Managed:     true,
    			Priority:    1,
    			Type:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CenterSource } from "jsr:@ubx/sdk-google/google/migration/center_source";

    export default stack("payments", () => {
      intent({ summary: "payments own center source" });

      resource(CenterSource, "ci-runner", {
        location: "example",
        sourceId: "example",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        managed: true,
        priority: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.migration import CenterSource, CenterSourceConfig

    def describe():
        ubx.intent("payments own center source")

        ubx.resource(CenterSource, "ci-runner", CenterSourceConfig(
            location="example",
            source_id="example",
            description="Managed by ubx.",
            display_name="example-display-name",
            managed=True,
            priority=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_migration_center_source, with location example,
    source id example, description Managed by ubx., display name
    example-display-name, managed true, priority 1, type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Free-text description. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  User-friendly display name. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="managed" type="bool">
  If 'true', the source is managed by other service(s). Optional.
</ResponseField>

<ResponseField name="priority" type="number">
  The information confidence of the source. The higher the value, the higher the confidence. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_id" type="string" required>
  User specified ID for the source. It will become the last component of the source name. The ID must be unique within the project, must conform with RFC-1034, is restricted to lower-cased letters, and has a maximum length of 63 characters. The ID must match the regular expression: '[a-z]([a-z0-9-]&#123;0,61&#125;[a-z0-9])?'. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Data source type. Possible values: SOURCE_TYPE_UNKNOWN SOURCE_TYPE_UPLOAD SOURCE_TYPE_GUEST_OS_SCAN SOURCE_TYPE_INVENTORY_SCAN SOURCE_TYPE_CUSTOM SOURCE_TYPE_DISCOVERY_CLIENT Optional.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The timestamp when the source was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="error_frame_count" type="number">
  The number of frames that were reported by the source and contained errors. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The full name of the source. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="pending_frame_count" type="number">
  Number of frames that are still being processed. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the source. Possible values: ACTIVE DELETING INVALID Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The timestamp when the source was last updated. Computed by GCP after `ship`.
</ResponseField>
