---
title: "google_migration_center_settings"
description: "Real, generated bindings for `google_migration_center_settings`."
---

`google_migration_center_settings` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/migration"
    )

    cfg := migration.CenterSettingsConfig{
        Location: "example",
        DisableCloudLogging: true,
        PreferenceSet: "example",
    }
    ubx.Resource(migration.CenterSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CenterSettings } from
      "jsr:@ubx/sdk-google/google/migration/center_settings";

    resource(CenterSettings, "example", {
      location: "example",
      disableCloudLogging: true,
      preferenceSet: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.migration import (
        CenterSettings,
        CenterSettingsConfig,
    )

    cfg = CenterSettingsConfig(
        location="example",
        disable_cloud_logging=True,
        preference_set="example",
    )
    ubx.resource(CenterSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_migration_center_settings with location,
disable_cloud_logging, preference_set.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disable_cloud_logging" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="preference_set" type="string">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
