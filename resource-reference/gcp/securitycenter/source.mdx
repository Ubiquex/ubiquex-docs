---
title: "google_securitycenter_source"
description: "`google_securitycenter_source` registers an external or custom detector as a finding source within Security Command Center..."
---

`google_securitycenter_source` registers an external or custom detector as a finding source within Security Command Center, letting it publish its own finding(s) alongside Google's built-in detectors.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_securitycenter --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_securitycenter/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	securitycenter "github.com/ubiquex/ubx-sdk-google/sdk/go/google_securitycenter/securitycenter"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own source"})

    		ubx.Resource(securitycenter.SecuritycenterSource, "ci-runner", securitycenter.SecuritycenterSourceConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_securitycenter --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecuritycenterSource } from "./local-sdk/google_securitycenter/sdk/typescript/google_securitycenter/securitycenter/securitycenter_source.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own source" });

      resource(SecuritycenterSource, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_securitycenter --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_securitycenter/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_securitycenter.securitycenter import SecuritycenterSource, SecuritycenterSourceConfig

    def describe():
        ubx.intent("payments own source")

        ubx.resource(SecuritycenterSource, "ci-runner", SecuritycenterSourceConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_securitycenter_source called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="canonical_name" type="string">
  The fully qualified, canonical name of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="display_name" type="string">
  A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>
