---
title: "google_securitycenter_custom_module"
description: "`google_securitycenter_custom_module` defines a caller-written detection rule Security Command Center's Security Health Analytics evaluates alongside..."
---

`google_securitycenter_custom_module` defines a caller-written detection rule Security Command Center's Security Health Analytics evaluates alongside its own built-in detectors.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	securitycenter "github.com/ubiquex/ubx-sdk-google/sdk/go/google/securitycenter"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own custom module"})

    		ubx.Resource(securitycenter.CustomModule, "example", securitycenter.CustomModuleConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CustomModule } from "@ubx/sdk-google/google/securitycenter/custom_module";

    export default stack("example", () => {
      intent({ summary: "example own custom module" });

      resource(CustomModule, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.securitycenter import CustomModule, CustomModuleConfig

    def describe():
        ubx.intent("example own custom module")

        ubx.resource(CustomModule, "example", CustomModuleConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_securitycenter_custom_module called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="ancestor_module" type="string">
  A reference to the parent security posture module this inherits from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="cloud_provider" type="string">
  The cloud provider this resource belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="config" type="map(any)">
  The configuration for this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="display_name" type="string">
  A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="enablement_state" type="string">
  Whether this feature is enabled, disabled, or inherited from a parent. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="last_editor" type="string">
  The identity that most recently modified this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="type" type="string">
  The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when this resource was most recently updated. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>
