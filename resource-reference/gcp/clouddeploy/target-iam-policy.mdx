---
title: "google_clouddeploy_target_iam_policy"
description: "Real, generated bindings for `google_clouddeploy_target_iam_policy`."
---

`google_clouddeploy_target_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/clouddeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own target iam policy"})

    		ubx.Resource(clouddeploy.TargetIamPolicy, "ci-runner", clouddeploy.TargetIamPolicyConfig{
    			Name:       "ci-runner",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TargetIamPolicy } from "jsr:@ubx/sdk-google/google/clouddeploy/target_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own target iam policy" });

      resource(TargetIamPolicy, "ci-runner", {
        name: "ci-runner",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.clouddeploy import TargetIamPolicy, TargetIamPolicyConfig

    def describe():
        ubx.intent("payments own target iam policy")

        ubx.resource(TargetIamPolicy, "ci-runner", TargetIamPolicyConfig(
            name="ci-runner",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_clouddeploy_target_iam_policy called "ci-runner",
    with policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
