---
title: "google_integration_connectors_endpoint_attachment"
description: "Real, generated bindings for `google_integration_connectors_endpoint_attachment`."
---

`google_integration_connectors_endpoint_attachment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	integration "github.com/ubiquex/ubx-sdk-google/sdk/go/google/integration"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connectors endpoint attachment"})

    		ubx.Resource(integration.ConnectorsEndpointAttachment, "ci-runner", integration.ConnectorsEndpointAttachmentConfig{
    			Location:             "example",
    			Name:                 "ci-runner",
    			ServiceAttachment:    "example",
    			Description:          "Managed by ubx.",
    			EndpointGlobalAccess: true,
    			Labels:               map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectorsEndpointAttachment } from "jsr:@ubx/sdk-google/google/integration/connectors_endpoint_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own connectors endpoint attachment" });

      resource(ConnectorsEndpointAttachment, "ci-runner", {
        location: "example",
        name: "ci-runner",
        serviceAttachment: "example",
        description: "Managed by ubx.",
        endpointGlobalAccess: true,
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.integration import ConnectorsEndpointAttachment, ConnectorsEndpointAttachmentConfig

    def describe():
        ubx.intent("payments own connectors endpoint attachment")

        ubx.resource(ConnectorsEndpointAttachment, "ci-runner", ConnectorsEndpointAttachmentConfig(
            location="example",
            name="ci-runner",
            service_attachment="example",
            description="Managed by ubx.",
            endpoint_global_access=True,
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_integration_connectors_endpoint_attachment called
    "ci-runner", with location example, service attachment example,
    description Managed by ubx., endpoint global access true, labels
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Description of the resource. Optional.
</ResponseField>

<ResponseField name="endpoint_global_access" type="bool">
  Enable global access for endpoint attachment. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Resource labels to represent user provided metadata. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Location in which Endpoint Attachment needs to be created. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of Endpoint Attachment needs to be created. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_attachment" type="string" required>
  The path of the service attachment. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Time the Namespace was created in UTC. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="endpoint_ip" type="string">
  The Private Service Connect connection endpoint ip. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Time the Namespace was updated in UTC. Computed by GCP after `ship`.
</ResponseField>
