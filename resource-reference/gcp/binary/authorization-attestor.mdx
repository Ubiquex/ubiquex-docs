---
title: "google_binary_authorization_attestor"
description: "Real, generated bindings for `google_binary_authorization_attestor`."
---

`google_binary_authorization_attestor` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	binary "github.com/ubiquex/ubx-sdk-google/sdk/go/google/binary"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own authorization attestor"})

    		ubx.Resource(binary.AuthorizationAttestor, "ci-runner", binary.AuthorizationAttestorConfig{
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AuthorizationAttestor } from "jsr:@ubx/sdk-google/google/binary/authorization_attestor";

    export default stack("payments", () => {
      intent({ summary: "payments own authorization attestor" });

      resource(AuthorizationAttestor, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.binary import AuthorizationAttestor, AuthorizationAttestorConfig

    def describe():
        ubx.intent("payments own authorization attestor")

        ubx.resource(AuthorizationAttestor, "ci-runner", AuthorizationAttestorConfig(
            name="ci-runner",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_binary_authorization_attestor called "ci-runner",
    with description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attestation_authority_note" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="delegation_service_account_email" type="string">
    </ResponseField>
    <ResponseField name="note_reference" type="string" required>
    </ResponseField>
    <ResponseField name="public_keys" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="ascii_armored_pgp_public_key" type="string">
        </ResponseField>
        <ResponseField name="comment" type="string">
        </ResponseField>
        <ResponseField name="id" type="string">
        </ResponseField>
        <ResponseField name="pkix_public_key" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="public_key_pem" type="string">
            </ResponseField>
            <ResponseField name="signature_algorithm" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A descriptive comment. This field may be updated. The field may be displayed in chooser dialogs. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  The resource name. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="attestation_authority_note" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="delegation_service_account_email" type="string">
    </ResponseField>
    <ResponseField name="note_reference" type="string" required>
    </ResponseField>
    <ResponseField name="public_keys" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="ascii_armored_pgp_public_key" type="string">
        </ResponseField>
        <ResponseField name="comment" type="string">
        </ResponseField>
        <ResponseField name="id" type="string">
        </ResponseField>
        <ResponseField name="pkix_public_key" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="public_key_pem" type="string">
            </ResponseField>
            <ResponseField name="signature_algorithm" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
