---
title: "google_binary_authorization_attestor_iam_policy"
description: "Real, generated bindings for `google_binary_authorization_attestor_iam_policy`."
---

`google_binary_authorization_attestor_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	binary "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/binary"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own authorization attestor iam policy"})

    		ubx.Resource(binary.AuthorizationAttestorIamPolicy, "ci-runner", binary.AuthorizationAttestorIamPolicyConfig{
    			Attestor:   "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AuthorizationAttestorIamPolicy } from "jsr:@ubx/sdk-google/google/binary/authorization_attestor_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own authorization attestor iam policy" });

      resource(AuthorizationAttestorIamPolicy, "ci-runner", {
        attestor: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.binary import AuthorizationAttestorIamPolicy, AuthorizationAttestorIamPolicyConfig

    def describe():
        ubx.intent("payments own authorization attestor iam policy")

        ubx.resource(AuthorizationAttestorIamPolicy, "ci-runner", AuthorizationAttestorIamPolicyConfig(
            attestor="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_binary_authorization_attestor_iam_policy, with
    attestor example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attestor" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
