---
title: "google_gke_hub_scope_iam_binding"
description: "Real, generated bindings for `google_gke_hub_scope_iam_binding`."
---

`google_gke_hub_scope_iam_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/gke"
    )

    cfg := gke.HubScopeIamBindingConfig{
        Members: []string{"example"},
        Role: "example",
        ScopeId: "example",
    }
    ubx.Resource(gke.HubScopeIamBinding, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { HubScopeIamBinding } from
      "jsr:@ubx/sdk-google/google/gke/hub_scope_iam_binding";

    resource(HubScopeIamBinding, "example", {
      members: ["example"],
      role: "example",
      scopeId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.gke import (
        HubScopeIamBinding,
        HubScopeIamBindingConfig,
    )

    cfg = HubScopeIamBindingConfig(
        members=["example"],
        role="example",
        scope_id="example",
    )
    ubx.resource(HubScopeIamBinding, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_gke_hub_scope_iam_binding with members, role,
scope_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="members" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="scope_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
