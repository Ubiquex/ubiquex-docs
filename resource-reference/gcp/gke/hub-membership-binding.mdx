---
title: "google_gke_hub_membership_binding"
description: "Real, generated bindings for `google_gke_hub_membership_binding`."
---

`google_gke_hub_membership_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gke "github.com/ubiquex/ubx-sdk-google/sdk/go/google/gke"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hub membership binding"})

    		ubx.Resource(gke.HubMembershipBinding, "ci-runner", gke.HubMembershipBindingConfig{
    			Location:            "example",
    			MembershipBindingId: "example",
    			MembershipId:        "example",
    			Scope:               "example",
    			Labels:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubMembershipBinding } from "jsr:@ubx/sdk-google/google/gke/hub_membership_binding";

    export default stack("payments", () => {
      intent({ summary: "payments own hub membership binding" });

      resource(HubMembershipBinding, "ci-runner", {
        location: "example",
        membershipBindingId: "example",
        membershipId: "example",
        scope: "example",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.gke import HubMembershipBinding, HubMembershipBindingConfig

    def describe():
        ubx.intent("payments own hub membership binding")

        ubx.resource(HubMembershipBinding, "ci-runner", HubMembershipBindingConfig(
            location="example",
            membership_binding_id="example",
            membership_id="example",
            scope="example",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gke_hub_membership_binding, with location example,
    membership binding id example, membership id example, scope
    example, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels for this Membership binding. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Location of the membership Required.
</ResponseField>

<ResponseField name="membership_binding_id" type="string" required>
  The client-provided identifier of the membership binding. Required.
</ResponseField>

<ResponseField name="membership_id" type="string" required>
  Id of the membership Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="scope" type="string" required>
  A Workspace resource name in the format 'projects/*/locations/*/scopes/*'. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Time the MembershipBinding was created in UTC. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Time the MembershipBinding was deleted in UTC. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name for the membershipbinding itself Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="list(object)">
  State of the membership binding resource. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Google-generated UUID for this resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Time the MembershipBinding was updated in UTC. Computed by GCP after `ship`.
</ResponseField>
