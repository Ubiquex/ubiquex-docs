---
title: "google_gke_hub_membership_iam_policy"
description: "Real, generated bindings for `google_gke_hub_membership_iam_policy`."
---

`google_gke_hub_membership_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/gke"
    )

    cfg := gke.HubMembershipIamPolicyConfig{
        MembershipId: "example",
        PolicyData: "example",
    }
    ubx.Resource(gke.HubMembershipIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { HubMembershipIamPolicy } from
      "jsr:@ubx/sdk-google/google/gke/hub_membership_iam_policy";

    resource(HubMembershipIamPolicy, "example", {
      membershipId: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.gke import (
        HubMembershipIamPolicy,
        HubMembershipIamPolicyConfig,
    )

    cfg = HubMembershipIamPolicyConfig(
        membership_id="example",
        policy_data="example",
    )
    ubx.resource(HubMembershipIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_gke_hub_membership_iam_policy with
membership_id, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="membership_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
