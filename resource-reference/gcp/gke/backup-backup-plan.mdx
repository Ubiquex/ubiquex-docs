---
title: "google_gke_backup_backup_plan"
description: "Real, generated bindings for `google_gke_backup_backup_plan`."
---

`google_gke_backup_backup_plan` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gke "github.com/ubiquex/ubx-sdk-google/sdk/go/google/gke"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own backup backup plan"})

    		ubx.Resource(gke.BackupBackupPlan, "ci-runner", gke.BackupBackupPlanConfig{
    			Cluster:     "example",
    			Location:    "example",
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    			Labels:      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BackupBackupPlan } from "jsr:@ubx/sdk-google/google/gke/backup_backup_plan";

    export default stack("payments", () => {
      intent({ summary: "payments own backup backup plan" });

      resource(BackupBackupPlan, "ci-runner", {
        cluster: "example",
        location: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.gke import BackupBackupPlan, BackupBackupPlanConfig

    def describe():
        ubx.intent("payments own backup backup plan")

        ubx.resource(BackupBackupPlan, "ci-runner", BackupBackupPlanConfig(
            cluster="example",
            location="example",
            name="ci-runner",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gke_backup_backup_plan called "ci-runner", with
    cluster example, location example, description Managed by ubx.,
    labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backup_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="all_namespaces" type="bool">
    </ResponseField>
    <ResponseField name="encryption_key" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="gcp_kms_encryption_key" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="include_secrets" type="bool">
    </ResponseField>
    <ResponseField name="include_volume_data" type="bool">
    </ResponseField>
    <ResponseField name="permissive_mode" type="bool">
    </ResponseField>
    <ResponseField name="selected_applications" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="namespaced_names" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="name" type="string" required>
            </ResponseField>
            <ResponseField name="namespace" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="selected_namespace_labels" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="resource_labels" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="key" type="string" required>
            </ResponseField>
            <ResponseField name="value" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="selected_namespaces" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="namespaces" type="list(string)" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="backup_schedule" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cron_schedule" type="string">
    </ResponseField>
    <ResponseField name="paused" type="bool">
    </ResponseField>
    <ResponseField name="rpo_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="exclusion_windows" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="daily" type="bool">
            </ResponseField>
            <ResponseField name="days_of_week" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="days_of_week" type="list(string)">
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="duration" type="string" required>
            </ResponseField>
            <ResponseField name="single_occurrence_date" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="day" type="number">
                </ResponseField>
                <ResponseField name="month" type="number">
                </ResponseField>
                <ResponseField name="year" type="number">
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="start_time" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="hours" type="number">
                </ResponseField>
                <ResponseField name="minutes" type="number">
                </ResponseField>
                <ResponseField name="nanos" type="number">
                </ResponseField>
                <ResponseField name="seconds" type="number">
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="target_rpo_minutes" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cluster" type="string" required>
  The source cluster from which Backups will be created via this BackupPlan. Required.
</ResponseField>

<ResponseField name="deactivated" type="bool">
  This flag indicates whether this BackupPlan has been deactivated. Setting this field to True locks the BackupPlan such that no further updates will be allowed (except deletes), including the deactivated field itself. It also prevents any new Backups from being created via this BackupPlan (including scheduled Backups). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  User specified descriptive string for this BackupPlan. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Description: A set of custom labels supplied by the user. A list of key-&gt;value pairs. Example: &#123; "name": "wrench", "mass": "1.3kg", "count": "3" &#125;. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The region of the Backup Plan. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The full name of the BackupPlan Resource. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="retention_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="backup_delete_lock_days" type="number">
    </ResponseField>
    <ResponseField name="backup_retain_days" type="number">
    </ResponseField>
    <ResponseField name="locked" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="backup_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="all_namespaces" type="bool">
    </ResponseField>
    <ResponseField name="encryption_key" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="gcp_kms_encryption_key" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="include_secrets" type="bool">
    </ResponseField>
    <ResponseField name="include_volume_data" type="bool">
    </ResponseField>
    <ResponseField name="permissive_mode" type="bool">
    </ResponseField>
    <ResponseField name="selected_applications" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="namespaced_names" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="name" type="string" required>
            </ResponseField>
            <ResponseField name="namespace" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="selected_namespace_labels" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="resource_labels" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="key" type="string" required>
            </ResponseField>
            <ResponseField name="value" type="string" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="selected_namespaces" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="namespaces" type="list(string)" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="backup_schedule" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cron_schedule" type="string">
    </ResponseField>
    <ResponseField name="paused" type="bool">
    </ResponseField>
    <ResponseField name="rpo_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="exclusion_windows" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="daily" type="bool">
            </ResponseField>
            <ResponseField name="days_of_week" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="days_of_week" type="list(string)">
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="duration" type="string" required>
            </ResponseField>
            <ResponseField name="single_occurrence_date" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="day" type="number">
                </ResponseField>
                <ResponseField name="month" type="number">
                </ResponseField>
                <ResponseField name="year" type="number">
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="start_time" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="hours" type="number">
                </ResponseField>
                <ResponseField name="minutes" type="number">
                </ResponseField>
                <ResponseField name="nanos" type="number">
                </ResponseField>
                <ResponseField name="seconds" type="number">
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="target_rpo_minutes" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deactivated" type="bool">
  This flag indicates whether this BackupPlan has been deactivated. Setting this field to True locks the BackupPlan such that no further updates will be allowed (except deletes), including the deactivated field itself. It also prevents any new Backups from being created via this BackupPlan (including scheduled Backups). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  etag is used for optimistic concurrency control as a way to help prevent simultaneous updates of a backup plan from overwriting each other. It is strongly suggested that systems make use of the 'etag' in the read-modify-write cycle to perform BackupPlan updates in order to avoid race conditions: An etag is returned in the response to backupPlans.get, and systems are expected to put that etag in the request to backupPlans.patch or backupPlans.delete to ensure that their change will be applied to the same version of the resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="protected_namespace_count" type="number">
  The number of Kubernetes Namespaces backed up in the last successful Backup created via this BackupPlan. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="protected_pod_count" type="number">
  The number of Kubernetes Pods backed up in the last successful Backup created via this BackupPlan. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="retention_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="backup_delete_lock_days" type="number">
    </ResponseField>
    <ResponseField name="backup_retain_days" type="number">
    </ResponseField>
    <ResponseField name="locked" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  The State of the BackupPlan. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state_reason" type="string">
  Detailed description of why BackupPlan is in its current state. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Server generated, unique identifier of UUID format. Computed by GCP after `ship`.
</ResponseField>
