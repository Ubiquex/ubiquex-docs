---
title: "google_gke_hub_scope_iam_policy"
description: "Real, generated bindings for `google_gke_hub_scope_iam_policy`."
---

`google_gke_hub_scope_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/gke"
    )

    cfg := gke.HubScopeIamPolicyConfig{
        PolicyData: "example",
        ScopeId: "example",
    }
    ubx.Resource(gke.HubScopeIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { HubScopeIamPolicy } from
      "jsr:@ubx/sdk-google/google/gke/hub_scope_iam_policy";

    resource(HubScopeIamPolicy, "example", {
      policyData: "example",
      scopeId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.gke import (
        HubScopeIamPolicy,
        HubScopeIamPolicyConfig,
    )

    cfg = HubScopeIamPolicyConfig(
        policy_data="example",
        scope_id="example",
    )
    ubx.resource(HubScopeIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_gke_hub_scope_iam_policy with policy_data,
scope_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="scope_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
