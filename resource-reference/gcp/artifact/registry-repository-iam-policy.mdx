---
title: "google_artifact_registry_repository_iam_policy"
description: "Real, generated bindings for `google_artifact_registry_repository_iam_policy`."
---

`google_artifact_registry_repository_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/artifact"
    )

    cfg := artifact.RegistryRepositoryIamPolicyConfig{
        PolicyData: "example",
        Repository: "example",
    }
    ubx.Resource(artifact.RegistryRepositoryIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RegistryRepositoryIamPolicy } from
      "jsr:@ubx/sdk-google/google/artifact/registry_repository_iam_policy";

    resource(RegistryRepositoryIamPolicy, "example", {
      policyData: "example",
      repository: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.artifact import (
        RegistryRepositoryIamPolicy,
        RegistryRepositoryIamPolicyConfig,
    )

    cfg = RegistryRepositoryIamPolicyConfig(
        policy_data="example",
        repository="example",
    )
    ubx.resource(RegistryRepositoryIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_artifact_registry_repository_iam_policy with
policy_data, repository.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="repository" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
