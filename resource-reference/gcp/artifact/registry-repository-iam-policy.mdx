---
title: "google_artifact_registry_repository_iam_policy"
description: "Real, generated bindings for `google_artifact_registry_repository_iam_policy`."
---

`google_artifact_registry_repository_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	artifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/artifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own registry repository iam policy"})

    		ubx.Resource(artifact.RegistryRepositoryIamPolicy, "ci-runner", artifact.RegistryRepositoryIamPolicyConfig{
    			PolicyData: "example",
    			Repository: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegistryRepositoryIamPolicy } from "jsr:@ubx/sdk-google/google/artifact/registry_repository_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own registry repository iam policy" });

      resource(RegistryRepositoryIamPolicy, "ci-runner", {
        policyData: "example",
        repository: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.artifact import RegistryRepositoryIamPolicy, RegistryRepositoryIamPolicyConfig

    def describe():
        ubx.intent("payments own registry repository iam policy")

        ubx.resource(RegistryRepositoryIamPolicy, "ci-runner", RegistryRepositoryIamPolicyConfig(
            policy_data="example",
            repository="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_artifact_registry_repository_iam_policy, with
    policy data example, repository example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="repository" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
