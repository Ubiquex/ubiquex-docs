---
title: "google_artifact_registry_project_config"
description: "Real, generated bindings for `google_artifact_registry_project_config`."
---

`google_artifact_registry_project_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	artifact "github.com/ubiquex/ubx-sdk-google/sdk/go/google/artifact"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own registry project config"})

    		ubx.Resource(artifact.RegistryProjectConfig, "ci-runner", artifact.RegistryProjectConfigConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegistryProjectConfig } from "jsr:@ubx/sdk-google/google/artifact/registry_project_config";

    export default stack("payments", () => {
      intent({ summary: "payments own registry project config" });

      resource(RegistryProjectConfig, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.artifact import RegistryProjectConfig, RegistryProjectConfigConfig

    def describe():
        ubx.intent("payments own registry project config")

        ubx.resource(RegistryProjectConfig, "ci-runner", RegistryProjectConfigConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_artifact_registry_project_config, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  The name of the location this config is located in. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="platform_logs_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="logging_state" type="string">
    </ResponseField>
    <ResponseField name="severity_level" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  The name of the location this config is located in. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the project's config. Always of the form: projects/&#123;project&#125;/locations/&#123;location&#125;/projectConfig Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="platform_logs_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="logging_state" type="string">
    </ResponseField>
    <ResponseField name="severity_level" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
