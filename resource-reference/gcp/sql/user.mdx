---
title: "google_sql_user"
description: "Real, generated bindings for `google_sql_user`."
---

`google_sql_user` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	sql "github.com/ubiquex/ubx-sdk-google/sdk/go/google/sql"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user"})

    		ubx.Resource(sql.User, "ci-runner", sql.UserConfig{
    			Instance:          "example",
    			Name:              "ci-runner",
    			DatabaseRoles:     []string{"example"},
    			Password:          "example",
    			PasswordWo:        "example",
    			PasswordWoVersion: 1,
    			Type:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { User } from "jsr:@ubx/sdk-google/google/sql/user";

    export default stack("payments", () => {
      intent({ summary: "payments own user" });

      resource(User, "ci-runner", {
        instance: "example",
        name: "ci-runner",
        databaseRoles: ["example"],
        password: "example",
        passwordWo: "example",
        passwordWoVersion: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.sql import User, UserConfig

    def describe():
        ubx.intent("payments own user")

        ubx.resource(User, "ci-runner", UserConfig(
            instance="example",
            name="ci-runner",
            database_roles=["example"],
            password="example",
            password_wo="example",
            password_wo_version=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_sql_user called "ci-runner", with instance example,
    database roles a set value, password example, password wo
    example, password wo version 1, type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="database_roles" type="list(string)">
  A list of database roles to be assigned to the user. This option is only available for MySQL and PostgreSQL instances. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="host" type="string">
  The host the user can connect from. This is only supported for MySQL instances. Don't set this field for PostgreSQL instances. Can be an IP address. Changing this forces a new resource to be created. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  The name of the Cloud SQL instance. Changing this forces a new resource to be created. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the user. Changing this forces a new resource to be created. Required.
</ResponseField>

<ResponseField name="password" type="string">
  The password for the user. Can be updated. For Postgres instances this is a Required field, unless type is set to either CLOUD_IAM_USER or CLOUD_IAM_SERVICE_ACCOUNT. Optional.
</ResponseField>

<ResponseField name="password_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allowed_failed_attempts" type="number">
    </ResponseField>
    <ResponseField name="enable_failed_attempts_check" type="bool">
    </ResponseField>
    <ResponseField name="enable_password_verification" type="bool">
    </ResponseField>
    <ResponseField name="password_expiration_duration" type="string">
    </ResponseField>
    <ResponseField name="status" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="locked" type="bool">
        </ResponseField>
        <ResponseField name="password_expiration_time" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="password_wo" type="string">
  The password for the user. Can be updated. For Postgres instances this is a Required field, unless type is set to either CLOUD_IAM_USER or CLOUD_IAM_SERVICE_ACCOUNT. Optional.
</ResponseField>

<ResponseField name="password_wo_version" type="number">
  The version of the password_wo. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project in which the resource belongs. If it is not provided, the provider project is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The user type. It determines the method to authenticate the user during login. The default is the database's built-in user type. Optional.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="host" type="string">
  The host the user can connect from. This is only supported for MySQL instances. Don't set this field for PostgreSQL instances. Can be an IP address. Changing this forces a new resource to be created. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="iam_email" type="string">
  The email address for MySQL IAM database users. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="password_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allowed_failed_attempts" type="number">
    </ResponseField>
    <ResponseField name="enable_failed_attempts_check" type="bool">
    </ResponseField>
    <ResponseField name="enable_password_verification" type="bool">
    </ResponseField>
    <ResponseField name="password_expiration_duration" type="string">
    </ResponseField>
    <ResponseField name="status" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="locked" type="bool">
        </ResponseField>
        <ResponseField name="password_expiration_time" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project in which the resource belongs. If it is not provided, the provider project is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="sql_server_user_details" type="list(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="disabled" type="bool">
    </ResponseField>
    <ResponseField name="server_roles" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
