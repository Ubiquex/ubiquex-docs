---
title: "google_sql_source_representation_instance"
description: "Real, generated bindings for `google_sql_source_representation_instance`."
---

`google_sql_source_representation_instance` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	sql "github.com/ubiquex/ubx-sdk-google/sdk/go/google/sql"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own source representation instance"})

    		ubx.Resource(sql.SourceRepresentationInstance, "ci-runner", sql.SourceRepresentationInstanceConfig{
    			DatabaseVersion:   "example",
    			Host:              "example",
    			Name:              "ci-runner",
    			CaCertificate:     "example",
    			ClientCertificate: "example",
    			ClientKey:         "example",
    			DumpFilePath:      "example",
    			Password:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SourceRepresentationInstance } from "jsr:@ubx/sdk-google/google/sql/source_representation_instance";

    export default stack("payments", () => {
      intent({ summary: "payments own source representation instance" });

      resource(SourceRepresentationInstance, "ci-runner", {
        databaseVersion: "example",
        host: "example",
        name: "ci-runner",
        caCertificate: "example",
        clientCertificate: "example",
        clientKey: "example",
        dumpFilePath: "example",
        password: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.sql import SourceRepresentationInstance, SourceRepresentationInstanceConfig

    def describe():
        ubx.intent("payments own source representation instance")

        ubx.resource(SourceRepresentationInstance, "ci-runner", SourceRepresentationInstanceConfig(
            database_version="example",
            host="example",
            name="ci-runner",
            ca_certificate="example",
            client_certificate="example",
            client_key="example",
            dump_file_path="example",
            password="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_sql_source_representation_instance called
    "ci-runner", with database version example, host example, ca
    certificate example, client certificate example, client key
    example, dump file path example, password example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="ca_certificate" type="string">
  The CA certificate on the external server. Include only if SSL/TLS is used on the external server. Optional.
</ResponseField>

<ResponseField name="client_certificate" type="string">
  The client certificate on the external server. Required only for server-client authentication. Include only if SSL/TLS is used on the external server. Optional.
</ResponseField>

<ResponseField name="client_key" type="string">
  The private key file for the client certificate on the external server. Required only for server-client authentication. Include only if SSL/TLS is used on the external server. Optional.
</ResponseField>

<ResponseField name="database_version" type="string" required>
  The MySQL, PostgreSQL or SQL Server (beta) version to use. Supported values include MYSQL_5_6, MYSQL_5_7, MYSQL_8_0, MYSQL_8_4, POSTGRES_9_6, POSTGRES_10, POSTGRES_11, POSTGRES_12, POSTGRES_13, POSTGRES_14, POSTGRES_15, POSTGRES_16, POSTGRES_17, POSTGRES_18, SQLSERVER_2022_STANDARD, SQLSERVER_2022_ENTERPRISE, SQLSERVER_2022_EXPRESS, SQLSERVER_2022_WEB, SQLSERVER_2025_STANDARD, SQLSERVER_2025_ENTERPRISE, SQLSERVER_2025_EXPRESS, SQLSERVER_2025_WEB. Database Version Policies includes an up-to-date reference of supported versions. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="dump_file_path" type="string">
  A file in the bucket that contains the data from the external server. Optional.
</ResponseField>

<ResponseField name="host" type="string" required>
  The IPv4 address and port for the external server, or the the DNS address for the external server. If the external server is hosted on Cloud SQL, the port is 5432. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the source representation instance. Use any valid Cloud SQL instance name. Required.
</ResponseField>

<ResponseField name="password" type="string">
  The password for the replication user account. Optional.
</ResponseField>

<ResponseField name="port" type="number">
  The externally accessible port for the source database server. Defaults to 3306. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The Region in which the created instance should reside. If it is not provided, the provider region is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="username" type="string">
  The replication user account on the external server. Optional.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The Region in which the created instance should reside. If it is not provided, the provider region is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
