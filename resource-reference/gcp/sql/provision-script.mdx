---
title: "google_sql_provision_script"
description: "Real, generated bindings for `google_sql_provision_script`."
---

`google_sql_provision_script` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/sql"
    )

    cfg := sql.ProvisionScriptConfig{
        Instance: "example",
        Script: "example",
        Database: "example",
        DeletionPolicy: "example",
    }
    ubx.Resource(sql.ProvisionScript, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProvisionScript } from
      "jsr:@ubx/sdk-google/google/sql/provision_script";

    resource(ProvisionScript, "example", {
      instance: "example",
      script: "example",
      database: "example",
      deletionPolicy: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.sql import ProvisionScript, ProvisionScriptConfig

    cfg = ProvisionScriptConfig(
        instance="example",
        script="example",
        database="example",
        deletion_policy="example",
    )
    ubx.resource(ProvisionScript, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_sql_provision_script with instance, script,
database, deletion_policy.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="database" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="script" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
