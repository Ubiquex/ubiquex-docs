---
title: "google_sql_provision_script"
description: "Real, generated bindings for `google_sql_provision_script`."
---

`google_sql_provision_script` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own provision script"})

    		ubx.Resource(sql.ProvisionScript, "ci-runner", sql.ProvisionScriptConfig{
    			Instance:       "example",
    			Script:         "example",
    			Database:       "example",
    			DeletionPolicy: "example",
    			Description:    "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProvisionScript } from "jsr:@ubx/sdk-google/google/sql/provision_script";

    export default stack("payments", () => {
      intent({ summary: "payments own provision script" });

      resource(ProvisionScript, "ci-runner", {
        instance: "example",
        script: "example",
        database: "example",
        deletionPolicy: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.sql import ProvisionScript, ProvisionScriptConfig

    def describe():
        ubx.intent("payments own provision script")

        ubx.resource(ProvisionScript, "ci-runner", ProvisionScriptConfig(
            instance="example",
            script="example",
            database="example",
            deletion_policy="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_sql_provision_script, with instance example, script
    example, database example, deletion policy example, description
    Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="database" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="script" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
