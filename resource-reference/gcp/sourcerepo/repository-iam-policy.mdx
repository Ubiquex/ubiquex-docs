---
title: "google_sourcerepo_repository_iam_policy"
description: "Real, generated bindings for `google_sourcerepo_repository_iam_policy`."
---

`google_sourcerepo_repository_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sourcerepo "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/sourcerepo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own repository iam policy"})

    		ubx.Resource(sourcerepo.RepositoryIamPolicy, "ci-runner", sourcerepo.RepositoryIamPolicyConfig{
    			PolicyData: "example",
    			Repository: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RepositoryIamPolicy } from "jsr:@ubx/sdk-google/google/sourcerepo/repository_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own repository iam policy" });

      resource(RepositoryIamPolicy, "ci-runner", {
        policyData: "example",
        repository: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.sourcerepo import RepositoryIamPolicy, RepositoryIamPolicyConfig

    def describe():
        ubx.intent("payments own repository iam policy")

        ubx.resource(RepositoryIamPolicy, "ci-runner", RepositoryIamPolicyConfig(
            policy_data="example",
            repository="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_sourcerepo_repository_iam_policy, with policy data
    example, repository example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="repository" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
