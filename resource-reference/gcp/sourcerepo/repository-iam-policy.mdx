---
title: "google_sourcerepo_repository_iam_policy"
description: "Real, generated bindings for `google_sourcerepo_repository_iam_policy`."
---

`google_sourcerepo_repository_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/sourcerepo"
    )

    cfg := sourcerepo.RepositoryIamPolicyConfig{
        PolicyData: "example",
        Repository: "example",
    }
    ubx.Resource(sourcerepo.RepositoryIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RepositoryIamPolicy } from
      "jsr:@ubx/sdk-google/google/sourcerepo/repository_iam_policy";

    resource(RepositoryIamPolicy, "example", {
      policyData: "example",
      repository: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.sourcerepo import (
        RepositoryIamPolicy,
        RepositoryIamPolicyConfig,
    )

    cfg = RepositoryIamPolicyConfig(
        policy_data="example",
        repository="example",
    )
    ubx.resource(RepositoryIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_sourcerepo_repository_iam_policy with
policy_data, repository.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="repository" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
