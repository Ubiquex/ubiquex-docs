---
title: "google_firebaseappdistribution_group"
description: "`google_firebaseappdistribution_group` is a tester group in Firebase App Distribution..."
---

`google_firebaseappdistribution_group` is a tester group in Firebase App Distribution, used to manage access to pre-release app builds for many testers at once. Adding or removing a tester from the group grants or revokes their access to every release distributed to it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseappdistribution "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firebaseappdistribution"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own group"})

    		ubx.Resource(firebaseappdistribution.Group, "example", firebaseappdistribution.GroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Group } from "@ubx/sdk-google/google/firebaseappdistribution/group";

    export default stack("example", () => {
      intent({ summary: "example own group" });

      resource(Group, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firebaseappdistribution.group import Group, GroupConfig

    def describe():
        ubx.intent("example own group")

        ubx.resource(Group, "example", GroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebaseappdistribution_group called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="display_name" type="string">
  Required. The display name of the group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="invite_link_count" type="number">
  Output only. The number of invite links for this group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the group resource. Format: `projects/&#123;project_number&#125;/groups/&#123;group_alias&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="release_count" type="number">
  Output only. The number of releases this group is permitted to access. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tester_count" type="number">
  Output only. The number of testers who are members of this group. Optional; if omitted, computed by Google Cloud.
</ResponseField>
