---
title: "google_datapipelines_pipeline"
description: "`google_datapipelines_pipeline` schedules a recurring Dataflow job through Data Pipelines..."
---

`google_datapipelines_pipeline` schedules a recurring Dataflow job through Data Pipelines, wrapping a Dataflow template with its own launch schedule and monitoring.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_datapipelines --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_datapipelines/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	datapipelines "github.com/ubiquex/ubx-sdk-google/sdk/go/google_datapipelines/datapipelines"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own pipeline"})

    		ubx.Resource(datapipelines.DatapipelinesPipeline, "example", datapipelines.DatapipelinesPipelineConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_datapipelines --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatapipelinesPipeline } from "./local-sdk/google_datapipelines/sdk/typescript/google_datapipelines/datapipelines/datapipelines_pipeline.ts";

    export default stack("example", () => {
      intent({ summary: "example own pipeline" });

      resource(DatapipelinesPipeline, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_datapipelines --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_datapipelines/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_datapipelines.datapipelines import DatapipelinesPipeline, DatapipelinesPipelineConfig

    def describe():
        ubx.intent("example own pipeline")

        ubx.resource(DatapipelinesPipeline, "example", DatapipelinesPipelineConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_datapipelines_pipeline called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Immutable. The timestamp when the pipeline was initially created. Set by the Data Pipelines service. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The display name of the pipeline. It can contain only letters ([A-Za-z]), numbers ([0-9]), hyphens (-), and underscores (_). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="job_count" type="number">
  Output only. Number of jobs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="last_update_time" type="string">
  Output only. Immutable. The timestamp when the pipeline was last modified. Set by the Data Pipelines service. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  The pipeline name. For example: `projects/PROJECT_ID/locations/LOCATION_ID/pipelines/PIPELINE_ID`. * `PROJECT_ID` can contain letters ([A-Za-z]), numbers ([0-9]), hyphens (-), colons (:), and periods (.). For more information, see [Identifying projects](https://cloud.google.com/resource-manager/docs/creating-managing-projects#identifying_projects). * `LOCATION_ID` is the canonical ID for the pipeline's location. The list of available locations can be obtained by calling `google.cloud.location.Locations.ListLocations`. Note that the Data Pipelines service is not available in all regions. It depends on Cloud Scheduler, an App Engine application, so it's only available in [App Engine regions](https://cloud.google.com/about/locations#region). * `PIPELINE_ID` is the ID of the pipeline. Must be unique for the selected project and location. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="pipeline_sources" type="map(string)">
  Immutable. The sources of the pipeline (for example, Knowledge Catalog). The keys and values are set by the corresponding sources during pipeline creation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="schedule_info" type="object">
  Details of the schedule the pipeline runs on. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="next_job_time" type="string">
      Output only. When the next Scheduler job is going to run. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="schedule" type="string">
      Unix-cron format of the schedule. This information is retrieved from the linked Cloud Scheduler. Optional.
    </ResponseField>
    <ResponseField name="time_zone" type="string">
      Timezone ID. This matches the timezone IDs used by the Cloud Scheduler API. If empty, UTC time is assumed. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scheduler_service_account_email" type="string">
  Optional. A service account email to be used with the Cloud Scheduler job. If not specified, the default compute engine service account will be used. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Required. The state of the pipeline. When the pipeline is created, the state is set to 'PIPELINE_STATE_ACTIVE' by default. State changes can be requested by setting the state to stopping, paused, or resuming. State cannot be changed through UpdatePipeline requests. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  Required. The type of the pipeline. This field affects the scheduling of the pipeline and the type of metrics to show for the pipeline. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="workload" type="object">
  Workload details for creating the pipeline jobs. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="dataflow_flex_template_request" type="object">
      A request to launch a Dataflow job from a Flex Template. Optional.
      <Expandable title="properties">
        <ResponseField name="launch_parameter" type="object">
          Launch Flex Template parameter. Optional.
          <Expandable title="properties">
            <ResponseField name="container_spec_gcs_path" type="string">
              Cloud Storage path to a file with a JSON-serialized ContainerSpec as content. Optional.
            </ResponseField>
            <ResponseField name="environment" type="object">
              The environment values to be set at runtime for a Flex Template. Optional.
              <Expandable title="properties">
                <ResponseField name="additional_experiments" type="list(string)">
                  Additional experiment flags for the job. Optional.
                </ResponseField>
                <ResponseField name="additional_user_labels" type="map(string)">
                  Additional user labels to be specified for the job. Keys and values must follow the restrictions specified in the [labeling restrictions](https://cloud.google.com/compute/docs/labeling-resources#restrictions). An object containing a list of key/value pairs. Example: `&#123; "name": "wrench", "mass": "1kg", "count": "3" &#125;`. Optional.
                </ResponseField>
                <ResponseField name="enable_streaming_engine" type="bool">
                  Whether to enable Streaming Engine for the job. Optional.
                </ResponseField>
                <ResponseField name="flexrs_goal" type="string">
                  Set FlexRS goal for the job. https://cloud.google.com/dataflow/docs/guides/flexrs Optional.
                </ResponseField>
                <ResponseField name="ip_configuration" type="string">
                  Configuration for VM IPs. Optional.
                </ResponseField>
                <ResponseField name="kms_key_name" type="string">
                  Name for the Cloud KMS key for the job. Key format is: projects//locations//keyRings//cryptoKeys/ Optional.
                </ResponseField>
                <ResponseField name="machine_type" type="string">
                  The machine type to use for the job. Defaults to the value from the template if not specified. Optional.
                </ResponseField>
                <ResponseField name="max_workers" type="number">
                  The maximum number of Compute Engine instances to be made available to your pipeline during execution, from 1 to 1000. Optional.
                </ResponseField>
                <ResponseField name="network" type="string">
                  Network to which VMs will be assigned. If empty or unspecified, the service will use the network "default". Optional.
                </ResponseField>
                <ResponseField name="num_workers" type="number">
                  The initial number of Compute Engine instances for the job. Optional.
                </ResponseField>
                <ResponseField name="service_account_email" type="string">
                  The email address of the service account to run the job as. Optional.
                </ResponseField>
                <ResponseField name="subnetwork" type="string">
                  Subnetwork to which VMs will be assigned, if desired. You can specify a subnetwork using either a complete URL or an abbreviated path. Expected to be of the form "https://www.googleapis.com/compute/v1/projects/HOST_PROJECT_ID/regions/REGION/subnetworks/SUBNETWORK" or "regions/REGION/subnetworks/SUBNETWORK". If the subnetwork is located in a Shared VPC network, you must use the complete URL. Optional.
                </ResponseField>
                <ResponseField name="temp_location" type="string">
                  The Cloud Storage path to use for temporary files. Must be a valid Cloud Storage URL, beginning with `gs://`. Optional.
                </ResponseField>
                <ResponseField name="worker_region" type="string">
                  The Compute Engine region (https://cloud.google.com/compute/docs/regions-zones/regions-zones) in which worker processing should occur, e.g. "us-west1". Mutually exclusive with worker_zone. If neither worker_region nor worker_zone is specified, defaults to the control plane region. Optional.
                </ResponseField>
                <ResponseField name="worker_zone" type="string">
                  The Compute Engine zone (https://cloud.google.com/compute/docs/regions-zones/regions-zones) in which worker processing should occur, e.g. "us-west1-a". Mutually exclusive with worker_region. If neither worker_region nor worker_zone is specified, a zone in the control plane region is chosen based on available capacity. If both `worker_zone` and `zone` are set, `worker_zone` takes precedence. Optional.
                </ResponseField>
                <ResponseField name="zone" type="string">
                  The Compute Engine [availability zone](https://cloud.google.com/compute/docs/regions-zones/regions-zones) for launching worker instances to run your pipeline. In the future, worker_zone will take precedence. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="job_name" type="string">
              Required. The job name to use for the created job. For an update job request, the job name should be the same as the existing running job. Optional.
            </ResponseField>
            <ResponseField name="launch_options" type="map(string)">
              Launch options for this Flex Template job. This is a common set of options across languages and templates. This should not be used to pass job parameters. Optional.
            </ResponseField>
            <ResponseField name="parameters" type="map(string)">
              The parameters for the Flex Template. Example: `&#123;"num_workers":"5"&#125;` Optional.
            </ResponseField>
            <ResponseField name="transform_name_mappings" type="map(string)">
              Use this to pass transform name mappings for streaming update jobs. Example: `&#123;"oldTransformName":"newTransformName",...&#125;` Optional.
            </ResponseField>
            <ResponseField name="update" type="bool">
              Set this to true if you are sending a request to update a running streaming job. When set, the job name should be the same as the running job. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="location" type="string">
          Required. The [regional endpoint] (https://cloud.google.com/dataflow/docs/concepts/regional-endpoints) to which to direct the request. For example, `us-central1`, `us-west1`. Optional.
        </ResponseField>
        <ResponseField name="project_id" type="string">
          Required. The ID of the Cloud Platform project that the job belongs to. Optional.
        </ResponseField>
        <ResponseField name="validate_only" type="bool">
          If true, the request is validated but not actually executed. Defaults to false. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dataflow_launch_template_request" type="object">
      A request to launch a template. Optional.
      <Expandable title="properties">
        <ResponseField name="gcs_path" type="string">
          A Cloud Storage path to the template from which to create the job. Must be a valid Cloud Storage URL, beginning with 'gs://'. Optional.
        </ResponseField>
        <ResponseField name="launch_parameters" type="object">
          Parameters to provide to the template being launched. Optional.
          <Expandable title="properties">
            <ResponseField name="environment" type="object">
              The environment values to set at runtime. Optional.
              <Expandable title="properties">
                <ResponseField name="additional_experiments" type="list(string)">
                  Additional experiment flags for the job. Optional.
                </ResponseField>
                <ResponseField name="additional_user_labels" type="map(string)">
                  Additional user labels to be specified for the job. Keys and values should follow the restrictions specified in the [labeling restrictions](https://cloud.google.com/compute/docs/labeling-resources#restrictions) page. An object containing a list of key/value pairs. Example: &#123; "name": "wrench", "mass": "1kg", "count": "3" &#125;. Optional.
                </ResponseField>
                <ResponseField name="bypass_temp_dir_validation" type="bool">
                  Whether to bypass the safety checks for the job's temporary directory. Use with caution. Optional.
                </ResponseField>
                <ResponseField name="enable_streaming_engine" type="bool">
                  Whether to enable Streaming Engine for the job. Optional.
                </ResponseField>
                <ResponseField name="ip_configuration" type="string">
                  Configuration for VM IPs. Optional.
                </ResponseField>
                <ResponseField name="kms_key_name" type="string">
                  Name for the Cloud KMS key for the job. The key format is: projects//locations//keyRings//cryptoKeys/ Optional.
                </ResponseField>
                <ResponseField name="machine_type" type="string">
                  The machine type to use for the job. Defaults to the value from the template if not specified. Optional.
                </ResponseField>
                <ResponseField name="max_workers" type="number">
                  The maximum number of Compute Engine instances to be made available to your pipeline during execution, from 1 to 1000. Optional.
                </ResponseField>
                <ResponseField name="network" type="string">
                  Network to which VMs will be assigned. If empty or unspecified, the service will use the network "default". Optional.
                </ResponseField>
                <ResponseField name="num_workers" type="number">
                  The initial number of Compute Engine instances for the job. Optional.
                </ResponseField>
                <ResponseField name="service_account_email" type="string">
                  The email address of the service account to run the job as. Optional.
                </ResponseField>
                <ResponseField name="subnetwork" type="string">
                  Subnetwork to which VMs will be assigned, if desired. You can specify a subnetwork using either a complete URL or an abbreviated path. Expected to be of the form "https://www.googleapis.com/compute/v1/projects/HOST_PROJECT_ID/regions/REGION/subnetworks/SUBNETWORK" or "regions/REGION/subnetworks/SUBNETWORK". If the subnetwork is located in a Shared VPC network, you must use the complete URL. Optional.
                </ResponseField>
                <ResponseField name="temp_location" type="string">
                  The Cloud Storage path to use for temporary files. Must be a valid Cloud Storage URL, beginning with `gs://`. Optional.
                </ResponseField>
                <ResponseField name="worker_region" type="string">
                  The Compute Engine region (https://cloud.google.com/compute/docs/regions-zones/regions-zones) in which worker processing should occur, e.g. "us-west1". Mutually exclusive with worker_zone. If neither worker_region nor worker_zone is specified, default to the control plane's region. Optional.
                </ResponseField>
                <ResponseField name="worker_zone" type="string">
                  The Compute Engine zone (https://cloud.google.com/compute/docs/regions-zones/regions-zones) in which worker processing should occur, e.g. "us-west1-a". Mutually exclusive with worker_region. If neither worker_region nor worker_zone is specified, a zone in the control plane's region is chosen based on available capacity. If both `worker_zone` and `zone` are set, `worker_zone` takes precedence. Optional.
                </ResponseField>
                <ResponseField name="zone" type="string">
                  The Compute Engine [availability zone](https://cloud.google.com/compute/docs/regions-zones/regions-zones) for launching worker instances to run your pipeline. In the future, worker_zone will take precedence. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="job_name" type="string">
              Required. The job name to use for the created job. Optional.
            </ResponseField>
            <ResponseField name="parameters" type="map(string)">
              The runtime parameters to pass to the job. Optional.
            </ResponseField>
            <ResponseField name="transform_name_mapping" type="map(string)">
              Map of transform name prefixes of the job to be replaced to the corresponding name prefixes of the new job. Only applicable when updating a pipeline. Optional.
            </ResponseField>
            <ResponseField name="update" type="bool">
              If set, replace the existing pipeline with the name specified by jobName with this pipeline, preserving state. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="location" type="string">
          The [regional endpoint] (https://cloud.google.com/dataflow/docs/concepts/regional-endpoints) to which to direct the request. Optional.
        </ResponseField>
        <ResponseField name="project_id" type="string">
          Required. The ID of the Cloud Platform project that the job belongs to. Optional.
        </ResponseField>
        <ResponseField name="validate_only" type="bool">
          If true, the request is validated but not actually executed. Defaults to false. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
