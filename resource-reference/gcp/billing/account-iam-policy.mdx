---
title: "google_billing_account_iam_policy"
description: "Real, generated bindings for `google_billing_account_iam_policy`."
---

`google_billing_account_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/billing"
    )

    cfg := billing.AccountIamPolicyConfig{
        BillingAccountId: "example",
        PolicyData: "example",
    }
    ubx.Resource(billing.AccountIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccountIamPolicy } from
      "jsr:@ubx/sdk-google/google/billing/account_iam_policy";

    resource(AccountIamPolicy, "example", {
      billingAccountId: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.billing import (
        AccountIamPolicy,
        AccountIamPolicyConfig,
    )

    cfg = AccountIamPolicyConfig(
        billing_account_id="example",
        policy_data="example",
    )
    ubx.resource(AccountIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_billing_account_iam_policy with
billing_account_id, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="billing_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
