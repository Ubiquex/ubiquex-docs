---
title: "google_billing_subaccount"
description: "Real, generated bindings for `google_billing_subaccount`."
---

`google_billing_subaccount` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/billing"
    )

    cfg := billing.SubaccountConfig{
        DisplayName: "example-display-name",
        MasterBillingAccount: "example",
    }
    ubx.Resource(billing.Subaccount, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Subaccount } from
      "jsr:@ubx/sdk-google/google/billing/subaccount";

    resource(Subaccount, "example", {
      displayName: "example-display-name",
      masterBillingAccount: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.billing import Subaccount, SubaccountConfig

    cfg = SubaccountConfig(
        display_name="example-display-name",
        master_billing_account="example",
    )
    ubx.resource(Subaccount, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_billing_subaccount with display_name,
master_billing_account.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="master_billing_account" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="billing_account_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="open" type="bool">
  Computed by GCP after `ship`.
</ResponseField>
