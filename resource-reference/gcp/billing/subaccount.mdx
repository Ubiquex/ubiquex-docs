---
title: "google_billing_subaccount"
description: "Real, generated bindings for `google_billing_subaccount`."
---

`google_billing_subaccount` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	billing "github.com/ubiquex/ubx-sdk-google/sdk/go/google/billing"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own subaccount"})

    		ubx.Resource(billing.Subaccount, "ci-runner", billing.SubaccountConfig{
    			DisplayName:          "example-display-name",
    			MasterBillingAccount: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Subaccount } from "jsr:@ubx/sdk-google/google/billing/subaccount";

    export default stack("payments", () => {
      intent({ summary: "payments own subaccount" });

      resource(Subaccount, "ci-runner", {
        displayName: "example-display-name",
        masterBillingAccount: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.billing import Subaccount, SubaccountConfig

    def describe():
        ubx.intent("payments own subaccount")

        ubx.resource(Subaccount, "ci-runner", SubaccountConfig(
            display_name="example-display-name",
            master_billing_account="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_billing_subaccount, with display name
    example-display-name, master billing account example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="master_billing_account" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="billing_account_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="open" type="bool">
  Computed by GCP after `ship`.
</ResponseField>
