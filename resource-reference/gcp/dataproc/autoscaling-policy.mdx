---
title: "google_dataproc_autoscaling_policy"
description: "Real, generated bindings for `google_dataproc_autoscaling_policy`."
---

`google_dataproc_autoscaling_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataproc "github.com/ubiquex/ubx-sdk-google/sdk/go/google/dataproc"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own autoscaling policy"})

    		ubx.Resource(dataproc.AutoscalingPolicy, "ci-runner", dataproc.AutoscalingPolicyConfig{
    			PolicyId: "example",
    			Location: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AutoscalingPolicy } from "jsr:@ubx/sdk-google/google/dataproc/autoscaling_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own autoscaling policy" });

      resource(AutoscalingPolicy, "ci-runner", {
        policyId: "example",
        location: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dataproc import AutoscalingPolicy, AutoscalingPolicyConfig

    def describe():
        ubx.intent("payments own autoscaling policy")

        ubx.resource(AutoscalingPolicy, "ci-runner", AutoscalingPolicyConfig(
            policy_id="example",
            location="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataproc_autoscaling_policy, with policy id
    example, location example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="basic_algorithm" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cooldown_period" type="string">
    </ResponseField>
    <ResponseField name="yarn_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="graceful_decommission_timeout" type="string" required>
        </ResponseField>
        <ResponseField name="scale_down_factor" type="number" required>
        </ResponseField>
        <ResponseField name="scale_down_min_worker_fraction" type="number">
        </ResponseField>
        <ResponseField name="scale_up_factor" type="number" required>
        </ResponseField>
        <ResponseField name="scale_up_min_worker_fraction" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  The location where the autoscaling policy should reside. The default value is 'global'. Optional.
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  The policy id. The id must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), and hyphens (-). Cannot begin or end with underscore or hyphen. Must consist of between 3 and 50 characters. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="secondary_worker_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="max_instances" type="number">
    </ResponseField>
    <ResponseField name="min_instances" type="number">
    </ResponseField>
    <ResponseField name="weight" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="worker_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="max_instances" type="number" required>
    </ResponseField>
    <ResponseField name="min_instances" type="number">
    </ResponseField>
    <ResponseField name="weight" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="basic_algorithm" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cooldown_period" type="string">
    </ResponseField>
    <ResponseField name="yarn_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="graceful_decommission_timeout" type="string" required>
        </ResponseField>
        <ResponseField name="scale_down_factor" type="number" required>
        </ResponseField>
        <ResponseField name="scale_down_min_worker_fraction" type="number">
        </ResponseField>
        <ResponseField name="scale_up_factor" type="number" required>
        </ResponseField>
        <ResponseField name="scale_up_min_worker_fraction" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The "resource name" of the autoscaling policy. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="secondary_worker_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="max_instances" type="number">
    </ResponseField>
    <ResponseField name="min_instances" type="number">
    </ResponseField>
    <ResponseField name="weight" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="worker_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="max_instances" type="number" required>
    </ResponseField>
    <ResponseField name="min_instances" type="number">
    </ResponseField>
    <ResponseField name="weight" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>
