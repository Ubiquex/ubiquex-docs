---
title: "google_dataproc_metastore_database_iam_policy"
description: "Real, generated bindings for `google_dataproc_metastore_database_iam_policy`."
---

`google_dataproc_metastore_database_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataproc "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/dataproc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own metastore database iam policy"})

    		ubx.Resource(dataproc.MetastoreDatabaseIamPolicy, "ci-runner", dataproc.MetastoreDatabaseIamPolicyConfig{
    			Database:   "example",
    			PolicyData: "example",
    			ServiceId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MetastoreDatabaseIamPolicy } from "jsr:@ubx/sdk-google/google/dataproc/metastore_database_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own metastore database iam policy" });

      resource(MetastoreDatabaseIamPolicy, "ci-runner", {
        database: "example",
        policyData: "example",
        serviceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dataproc import MetastoreDatabaseIamPolicy, MetastoreDatabaseIamPolicyConfig

    def describe():
        ubx.intent("payments own metastore database iam policy")

        ubx.resource(MetastoreDatabaseIamPolicy, "ci-runner", MetastoreDatabaseIamPolicyConfig(
            database="example",
            policy_data="example",
            service_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataproc_metastore_database_iam_policy, with
    database example, policy data example, service id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="database" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
