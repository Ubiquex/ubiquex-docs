---
title: "google_dataproc_metastore_federation_iam_policy"
description: "Real, generated bindings for `google_dataproc_metastore_federation_iam_policy`."
---

`google_dataproc_metastore_federation_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataproc "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/dataproc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own metastore federation iam policy"})

    		ubx.Resource(dataproc.MetastoreFederationIamPolicy, "ci-runner", dataproc.MetastoreFederationIamPolicyConfig{
    			FederationId: "example",
    			PolicyData:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MetastoreFederationIamPolicy } from "jsr:@ubx/sdk-google/google/dataproc/metastore_federation_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own metastore federation iam policy" });

      resource(MetastoreFederationIamPolicy, "ci-runner", {
        federationId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dataproc import MetastoreFederationIamPolicy, MetastoreFederationIamPolicyConfig

    def describe():
        ubx.intent("payments own metastore federation iam policy")

        ubx.resource(MetastoreFederationIamPolicy, "ci-runner", MetastoreFederationIamPolicyConfig(
            federation_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataproc_metastore_federation_iam_policy, with
    federation id example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="federation_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
