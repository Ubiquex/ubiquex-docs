---
title: "google_dataproc_metastore_service_iam_policy"
description: "Real, generated bindings for `google_dataproc_metastore_service_iam_policy`."
---

`google_dataproc_metastore_service_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataproc "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/dataproc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own metastore service iam policy"})

    		ubx.Resource(dataproc.MetastoreServiceIamPolicy, "ci-runner", dataproc.MetastoreServiceIamPolicyConfig{
    			PolicyData: "example",
    			ServiceId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MetastoreServiceIamPolicy } from "jsr:@ubx/sdk-google/google/dataproc/metastore_service_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own metastore service iam policy" });

      resource(MetastoreServiceIamPolicy, "ci-runner", {
        policyData: "example",
        serviceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dataproc import MetastoreServiceIamPolicy, MetastoreServiceIamPolicyConfig

    def describe():
        ubx.intent("payments own metastore service iam policy")

        ubx.resource(MetastoreServiceIamPolicy, "ci-runner", MetastoreServiceIamPolicyConfig(
            policy_data="example",
            service_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataproc_metastore_service_iam_policy, with policy
    data example, service id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
