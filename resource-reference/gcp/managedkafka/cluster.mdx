---
title: "google_managedkafka_cluster"
description: "`google_managedkafka_cluster` provisions a fully managed Apache Kafka cluster through Google Cloud's own Managed Service for Apache Kafka."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_managedkafka --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_managedkafka/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	managedkafka "github.com/ubiquex/ubx-sdk-google/sdk/go/google_managedkafka/managedkafka"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cluster"})

    		ubx.Resource(managedkafka.ManagedkafkaCluster, "example", managedkafka.ManagedkafkaClusterConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_managedkafka --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedkafkaCluster } from "./local-sdk/google_managedkafka/sdk/typescript/google_managedkafka/managedkafka/managedkafka_cluster.ts";

    export default stack("example", () => {
      intent({ summary: "example own cluster" });

      resource(ManagedkafkaCluster, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_managedkafka --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_managedkafka/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_managedkafka.managedkafka import ManagedkafkaCluster, ManagedkafkaClusterConfig

    def describe():
        ubx.intent("example own cluster")

        ubx.resource(ManagedkafkaCluster, "example", ManagedkafkaClusterConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_managedkafka_cluster called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="broker_details" type="list(object)">
  Output only. Only populated when FULL view is requested. Details of each broker in the cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="broker_index" type="string">
      Output only. This broker's own index within the cluster. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string">
      Output only. This broker's own Kafka node ID. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rack" type="string">
      Output only. The availability zone (rack) this broker runs in, used for rack-aware replica placement. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="capacity_config" type="object">
  A capacity configuration of a Kafka cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="memory_bytes" type="string">
      Required. The memory to provision for the cluster in bytes. The CPU:memory ratio (vCPU:GiB) must be between 1:1 and 1:8. Minimum: 3221225472 (3 GiB). Optional.
    </ResponseField>
    <ResponseField name="vcpu_count" type="string">
      Required. The number of vCPUs to provision for the cluster. Minimum: 3. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the cluster was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="gcp_config" type="object">
  Configuration properties for a Kafka cluster deployed to Google Cloud Platform. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="access_config" type="object">
      The configuration of access to the Kafka cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="network_configs" type="list(object)">
          Required. Virtual Private Cloud (VPC) networks that must be granted direct access to the Kafka cluster. Minimum of 1 network is required. Maximum 10 networks can be specified. Optional.
          <Expandable title="properties">
            <ResponseField name="subnet" type="string">
              The subnet this cluster is reachable from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kms_key" type="string">
      Optional. Immutable. The Cloud KMS Key name to use for encryption. The key must be located in the same region as the cluster and cannot be changed. Structured like: projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;key_ring&#125;/cryptoKeys/&#123;crypto_key&#125;. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kafka_version" type="string">
  Output only. Only populated when FULL view is requested. The Kafka version of the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels as key value pairs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The name of the cluster. Structured like: projects/&#123;project_number&#125;/locations/&#123;location&#125;/clusters/&#123;cluster_id&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rebalance_config" type="object">
  Defines rebalancing behavior of a Kafka cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="mode" type="string">
      Optional. The rebalance behavior for the cluster. When not specified, defaults to `NO_REBALANCE`. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tls_config" type="object">
  The TLS configuration for the Kafka cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="ssl_principal_mapping_rules" type="string">
      Optional. A list of rules for mapping from SSL principal names to short names. These are applied in order by Kafka. Refer to the Apache Kafka documentation for `ssl.principal.mapping.rules` for the precise formatting details and syntax. Example: "RULE:^CN=(.*?),OU=ServiceUsers.*$/$1@example.com/,DEFAULT" This is a static Kafka broker configuration. Setting or modifying this field will trigger a rolling restart of the Kafka brokers to apply the change. An empty string means no rules are applied (Kafka default). Optional.
    </ResponseField>
    <ResponseField name="trust_config" type="object">
      Sources of CA certificates to install in the broker's truststore. Optional.
      <Expandable title="properties">
        <ResponseField name="cas_configs" type="list(object)">
          Optional. Configuration for the Google Certificate Authority Service. Maximum 10. Optional.
          <Expandable title="properties">
            <ResponseField name="ca_pool" type="string">
              The Certificate Authority Service CA pool trusted for validating client TLS certificates. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_options" type="object">
  UpdateOptions specifies options that influence how a cluster update is applied. These options control the behavior of the update process, rather than defining the desired end-state of a cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allow_broker_downscale_on_cluster_upscale" type="bool">
      Optional. If true, allows an update operation that increases the total vCPU and/or memory allocation of the cluster to significantly decrease the per-broker vCPU and/or memory allocation. This can result in reduced performance and availability. By default, the update operation will fail if an upscale request results in a vCPU or memory allocation for the brokers that is smaller than 90% of the current broker size. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the cluster was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
