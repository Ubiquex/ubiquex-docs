---
title: "google_managedkafka_topic"
description: "`google_managedkafka_topic` is a Kafka topic within a Managed Service for Apache Kafka cluster..."
---

`google_managedkafka_topic` is a Kafka topic within a Managed Service for Apache Kafka cluster, the named stream producers publish to and consumers read from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_managedkafka --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_managedkafka/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	managedkafka "github.com/ubiquex/ubx-sdk-google/sdk/go/google_managedkafka/managedkafka"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own topic"})

    		ubx.Resource(managedkafka.ManagedkafkaTopic, "ci-runner", managedkafka.ManagedkafkaTopicConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_managedkafka --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedkafkaTopic } from "./local-sdk/google_managedkafka/sdk/typescript/google_managedkafka/managedkafka/managedkafka_topic.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own topic" });

      resource(ManagedkafkaTopic, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_managedkafka --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_managedkafka/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_managedkafka.managedkafka import ManagedkafkaTopic, ManagedkafkaTopicConfig

    def describe():
        ubx.intent("payments own topic")

        ubx.resource(ManagedkafkaTopic, "ci-runner", ManagedkafkaTopicConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_managedkafka_topic called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="configs" type="map(string)">
  Optional. Configurations for the topic that are overridden from the cluster defaults. The key of the map is a Kafka topic property name, for example: `cleanup.policy`, `compression.type`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The name of the topic. The `topic` segment is used when connecting directly to the cluster. Structured like: projects/&#123;project&#125;/locations/&#123;location&#125;/clusters/&#123;cluster&#125;/topics/&#123;topic&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="partition_count" type="number">
  Required. The number of partitions this topic has. The partition count can only be increased, not decreased. Please note that if partitions are increased for a topic that has a key, the partitioning logic or the ordering of the messages will be affected. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="replication_factor" type="number">
  Required. Immutable. The number of replicas of each partition. A replication factor of 3 is recommended for high availability. Optional; if omitted, computed by Google Cloud.
</ResponseField>
