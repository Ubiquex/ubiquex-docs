---
title: "google_discoveryengine_v1alpha_sample_query_set"
description: "`google_discoveryengine_v1alpha_sample_query_set` is a real, named collection of sample queries (its own display_name and description) used as..."
---

`google_discoveryengine_v1alpha_sample_query_set` is a real, named collection of sample queries (its own display_name and description) used as a consistent, reusable input set for evaluating a Vertex AI Search app's own quality over time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discoveryengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/discoveryengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1alpha sample query set"})

    		ubx.Resource(discoveryengine.V1alphaSampleQuerySet, "example", discoveryengine.V1alphaSampleQuerySetConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1alphaSampleQuerySet } from "@ubx/sdk-google/google/discoveryengine/v1alpha_sample_query_set";

    export default stack("example", () => {
      intent({ summary: "example own v1alpha sample query set" });

      resource(V1alphaSampleQuerySet, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.discoveryengine import V1alphaSampleQuerySet, V1alphaSampleQuerySetConfig

    def describe():
        ubx.intent("example own v1alpha sample query set")

        ubx.resource(V1alphaSampleQuerySet, "example", V1alphaSampleQuerySetConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discoveryengine_v1alpha_sample_query_set called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp the SampleQuerySet was created at. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the SampleQuerySet. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The sample query set display name. This field must be a UTF-8 encoded string with a length limit of 128 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The full resource name of the SampleQuerySet, in the format of `projects/&#123;project&#125;/locations/&#123;location&#125;/sampleQuerySets/&#123;sample_query_set&#125;`. This field must be a UTF-8 encoded string with a length limit of 1024 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>
