---
title: "google_discoveryengine_data_store"
description: "`google_discoveryengine_data_store` is the indexed content repository (structured, unstructured..."
---

`google_discoveryengine_data_store` is the indexed content repository (structured, unstructured, or website data) that a Discovery Engine search or recommendation app is built on top of.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_discoveryengine --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_discoveryengine/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discoveryengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google_discoveryengine/discoveryengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own data store"})

    		ubx.Resource(discoveryengine.DiscoveryengineDataStore, "ci-runner", discoveryengine.DiscoveryengineDataStoreConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_discoveryengine --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DiscoveryengineDataStore } from "./local-sdk/google_discoveryengine/sdk/typescript/google_discoveryengine/discoveryengine/discoveryengine_data_store.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own data store" });

      resource(DiscoveryengineDataStore, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_discoveryengine --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_discoveryengine/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_discoveryengine.discoveryengine import DiscoveryengineDataStore, DiscoveryengineDataStoreConfig

    def describe():
        ubx.intent("payments own data store")

        ubx.resource(DiscoveryengineDataStore, "ci-runner", DiscoveryengineDataStoreConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discoveryengine_data_store called "ci-runner", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="acl_enabled" type="bool">
  Immutable. Whether data in the DataStore has ACL information. If set to `true`, the source data must have ACL. ACL will be ingested when data is ingested by DocumentService.ImportDocuments methods. When ACL is enabled for the DataStore, Document can't be accessed by calling DocumentService.GetDocument or DocumentService.ListDocuments. Currently ACL is only supported in `GENERIC` industry vertical with non-`PUBLIC_WEBSITE` content config. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="advanced_site_search_config" type="object">
  Configuration data for advance site search. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="disable_automatic_refresh" type="bool">
      If set true, automatic refresh is disabled for the DataStore. Optional.
    </ResponseField>
    <ResponseField name="disable_initial_index" type="bool">
      If set true, initial indexing is disabled for the DataStore. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="billing_estimation" type="object">
  Estimation of data size per data store. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="structured_data_size" type="string">
      Data size for structured data in terms of bytes. Optional.
    </ResponseField>
    <ResponseField name="structured_data_update_time" type="string">
      Last updated timestamp for structured data. Optional.
    </ResponseField>
    <ResponseField name="unstructured_data_size" type="string">
      Data size for unstructured data in terms of bytes. Optional.
    </ResponseField>
    <ResponseField name="unstructured_data_update_time" type="string">
      Last updated timestamp for unstructured data. Optional.
    </ResponseField>
    <ResponseField name="website_data_size" type="string">
      Data size for websites in terms of bytes. Optional.
    </ResponseField>
    <ResponseField name="website_data_update_time" type="string">
      Last updated timestamp for websites. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cmek_config" type="object">
  Configurations used to enable CMEK data encryption with Cloud KMS keys. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="is_default" type="bool">
      Output only. The default CmekConfig for the Customer. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="kms_key" type="string">
      Required. KMS key resource name which will be used to encrypt resources `projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;keyRing&#125;/cryptoKeys/&#123;keyId&#125;`. Optional.
    </ResponseField>
    <ResponseField name="kms_key_version" type="string">
      Output only. KMS key version resource name which will be used to encrypt resources `/cryptoKeyVersions/&#123;keyVersion&#125;`. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="last_rotation_timestamp_micros" type="string">
      Output only. The timestamp of the last key rotation. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="name" type="string">
      Required. The name of the CmekConfig of the form `projects/&#123;project&#125;/locations/&#123;location&#125;/cmekConfig` or `projects/&#123;project&#125;/locations/&#123;location&#125;/cmekConfigs/&#123;cmek_config&#125;`. Optional.
    </ResponseField>
    <ResponseField name="notebooklm_state" type="string">
      Output only. Whether the NotebookLM Corpus is ready to be used. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="single_region_keys" type="list(object)">
      Optional. Single-regional CMEKs that are required for some VAIS features. Optional.
      <Expandable title="properties">
        <ResponseField name="kms_key" type="string">
          The Cloud KMS key used to encrypt this resource's own data. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The states of the CmekConfig. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="configurable_billing_approach" type="string">
  Optional. Configuration for configurable billing approach. See Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="configurable_billing_approach_update_time" type="string">
  Output only. The timestamp when configurable_billing_approach was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="content_config" type="string">
  Immutable. The content config of the data store. If this field is unset, the server behavior defaults to ContentConfig.NO_CONTENT. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Timestamp the DataStore was created at. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_schema_id" type="string">
  Output only. The id of the default Schema associated to this data store. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The data store display name. This field must be a UTF-8 encoded string with a length limit of 128 characters. Otherwise, an INVALID_ARGUMENT error is returned. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="document_processing_config" type="object">
  A singleton resource of DataStore. If it's empty when DataStore is created and DataStore is set to DataStore.ContentConfig.CONTENT_REQUIRED, the default parser will default to digital parser. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="chunking_config" type="object">
      Configuration for chunking config. Optional.
      <Expandable title="properties">
        <ResponseField name="layout_based_chunking_config" type="object">
          Configuration for the layout based chunking. Optional.
          <Expandable title="properties">
            <ResponseField name="chunk_size" type="number">
              The token size limit for each chunk. Supported values: 100-500 (inclusive). Default value: 500. Optional.
            </ResponseField>
            <ResponseField name="include_ancestor_headings" type="bool">
              Whether to include appending different levels of headings to chunks from the middle of the document to prevent context loss. Default value: False. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_parsing_config" type="object">
      Related configurations applied to a specific type of document parser. Optional.
      <Expandable title="properties">
        <ResponseField name="digital_parsing_config" type="any">
          The digital parsing configurations for documents. Optional.
        </ResponseField>
        <ResponseField name="layout_parsing_config" type="object">
          The layout parsing configurations for documents. Optional.
          <Expandable title="properties">
            <ResponseField name="enable_get_processed_document" type="bool">
              Optional. If true, the processed document will be made available for the GetProcessedDocument API. Optional.
            </ResponseField>
            <ResponseField name="enable_image_annotation" type="bool">
              Optional. If true, the LLM based annotation is added to the image during parsing. Optional.
            </ResponseField>
            <ResponseField name="enable_llm_layout_parsing" type="bool">
              Optional. If true, the pdf layout will be refined using an LLM. Optional.
            </ResponseField>
            <ResponseField name="enable_table_annotation" type="bool">
              Optional. If true, the LLM based annotation is added to the table during parsing. Optional.
            </ResponseField>
            <ResponseField name="exclude_html_classes" type="list(string)">
              Optional. List of HTML classes to exclude from the parsed content. Optional.
            </ResponseField>
            <ResponseField name="exclude_html_elements" type="list(string)">
              Optional. List of HTML elements to exclude from the parsed content. Optional.
            </ResponseField>
            <ResponseField name="exclude_html_ids" type="list(string)">
              Optional. List of HTML ids to exclude from the parsed content. Optional.
            </ResponseField>
            <ResponseField name="structured_content_types" type="list(string)">
              Optional. Contains the required structure types to extract from the document. Supported values: * `shareholder-structure` Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ocr_parsing_config" type="object">
          The OCR parsing configurations for documents. Optional.
          <Expandable title="properties">
            <ResponseField name="enhanced_document_elements" type="list(string)">
              [DEPRECATED] This field is deprecated. To use the additional enhanced document elements processing, please switch to `layout_parsing_config`. Optional.
            </ResponseField>
            <ResponseField name="use_native_text" type="bool">
              If true, will use native text instead of OCR text on pages containing native text. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The full resource name of the Document Processing Config. Format: `projects/*/locations/*/collections/*/dataStores/*/documentProcessingConfig`. Optional.
    </ResponseField>
    <ResponseField name="parsing_config_overrides" type="map(object)">
      Map from file type to override the default parsing configuration based on the file type. Supported keys: * `pdf`: Override parsing config for PDF files, either digital parsing, ocr parsing or layout parsing is supported. * `html`: Override parsing config for HTML files, only digital parsing and layout parsing are supported. * `docx`: Override parsing config for DOCX files, only digital parsing and layout parsing are supported. * `pptx`: Override parsing config for PPTX files, only digital parsing and layout parsing are supported. * `xlsm`: Override parsing config for XLSM files, only digital parsing and layout parsing are supported. * `xlsx`: Override parsing config for XLSX files, only digital parsing and layout parsing are supported. Optional.
      <Expandable title="properties">
        <ResponseField name="digital_parsing_config" type="any">
          Configures documents to be parsed using their own embedded digital text, rather than OCR. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="layout_parsing_config" type="object">
          Configuration for how document layout and structure are parsed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enable_get_processed_document" type="bool">
              Whether the fully processed document content can be retrieved after ingestion. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enable_image_annotation" type="bool">
              Whether images within documents are automatically annotated during processing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enable_llm_layout_parsing" type="bool">
              Whether document layout is parsed using an LLM-based method, for improved structure detection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enable_table_annotation" type="bool">
              Whether tables within documents are automatically annotated during processing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="exclude_html_classes" type="list(string)">
              HTML class name(s) excluded from content extraction. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="exclude_html_elements" type="list(string)">
              HTML element(s) excluded from content extraction. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="exclude_html_ids" type="list(string)">
              HTML element ID(s) excluded from content extraction. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="structured_content_types" type="list(string)">
              The structured content type(s) (e.g. tables) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ocr_parsing_config" type="object">
          Configures documents to be parsed using OCR, for scanned or image-based content. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enhanced_document_elements" type="list(string)">
              Additional structural element(s) (tables, images) extracted from this document during enhanced parsing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="use_native_text" type="bool">
              Whether the document's own native, embedded text is used directly, rather than re-extracting it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="federated_search_config" type="object">
  Stores information for federated search. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="alloy_db_config" type="object">
      Stores information for connecting to AlloyDB. Optional.
      <Expandable title="properties">
        <ResponseField name="alloydb_ai_nl_config" type="object">
          Configuration for AlloyDB AI Natural Language. Optional.
          <Expandable title="properties">
            <ResponseField name="nl_config_id" type="string">
              Optional. AlloyDb AI NL config id, i.e. the value that was used for calling `SELECT alloydb_ai_nl.g_create_configuration(...)`. Can be empty. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alloydb_connection_config" type="object">
          Configuration for connecting to AlloyDB. Optional.
          <Expandable title="properties">
            <ResponseField name="auth_mode" type="string">
              Optional. Auth mode. Optional.
            </ResponseField>
            <ResponseField name="database" type="string">
              Required. The AlloyDB database to connect to. Optional.
            </ResponseField>
            <ResponseField name="enable_psvs" type="bool">
              Optional. If true, enable PSVS for AlloyDB. Optional.
            </ResponseField>
            <ResponseField name="instance" type="string">
              Required. The AlloyDB instance to connect to. Optional.
            </ResponseField>
            <ResponseField name="password" type="string">
              Required. Database password. If auth_mode = END_USER_ACCOUNT, it can be unset. In that case, the password will be inferred on the AlloyDB side, based on the authenticated user. Optional.
            </ResponseField>
            <ResponseField name="user" type="string">
              Required. Database user. If auth_mode = END_USER_ACCOUNT, it can be unset. In that case, the user will be inferred on the AlloyDB side, based on the authenticated user. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="returned_fields" type="list(string)">
          Optional. Fields to be returned in the search results. If empty, all fields will be returned. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="notebooklm_config" type="object">
      Config for connecting to NotebookLM Enterprise. Optional.
      <Expandable title="properties">
        <ResponseField name="search_config" type="string">
          Required. Search config name. Format: projects/*/locations/global/notebookLmSearchConfigs/* Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="third_party_oauth_config" type="object">
      Stores information for third party applicationOAuth. Optional.
      <Expandable title="properties">
        <ResponseField name="app_name" type="string">
          Optional. The type of the application. E.g., "jira", "box", etc. Optional.
        </ResponseField>
        <ResponseField name="instance_name" type="string">
          Optional. The instance name identifying the 3P app, e.g., "vaissptbots-my". This is different from the instance_uri which is the full URL of the 3P app e.g., "https://vaissptbots-my.sharepoint.com". Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="healthcare_fhir_config" type="object">
  Config to data store for `HEALTHCARE_FHIR` vertical. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="enable_configurable_schema" type="bool">
      Whether to enable configurable schema for `HEALTHCARE_FHIR` vertical. If set to `true`, the predefined healthcare fhir schema can be extended for more customized searching and filtering. Optional.
    </ResponseField>
    <ResponseField name="enable_static_indexing_for_batch_ingestion" type="bool">
      Whether to enable static indexing for `HEALTHCARE_FHIR` batch ingestion. If set to `true`, the batch ingestion will be processed in a static indexing mode which is slower but more capable of handling larger volume. Optional.
    </ResponseField>
    <ResponseField name="initial_filter_groups" type="list(string)">
      Optional. Names of the Group resources to use as a basis for the initial patient filter, in format `projects/&#123;project_id&#125;/locations/&#123;location_id&#125;/datasets/&#123;dataset_id&#125;/fhirStores/&#123;fhir_store_id&#125;/fhir/Group/&#123;group_id&#125;`. The filter group must be a FHIR resource name of type Group, and the filter will be constructed from the direct members of the group which are Patient resources. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="identity_mapping_store" type="string">
  Immutable. The fully qualified resource name of the associated IdentityMappingStore. This field can only be set for acl_enabled DataStores with `THIRD_PARTY` or `GSUITE` IdP. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/identityMappingStores/&#123;identity_mapping_store&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="industry_vertical" type="string">
  Immutable. The industry vertical that the data store registers. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="is_infobot_faq_data_store" type="bool">
  Optional. If set, this DataStore is an Infobot FAQ DataStore. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  Input only. The KMS key to be used to protect this DataStore at creation time. Must be set for requests that need to comply with CMEK Org Policy protections. If this field is set and processed successfully, the DataStore will be protected by the KMS key, as indicated in the cmek_config field. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. Identifier. The full resource name of the data store. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/collections/&#123;collection_id&#125;/dataStores/&#123;data_store_id&#125;`. This field must be a UTF-8 encoded string with a length limit of 1024 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="natural_language_query_understanding_config" type="object">
  Configuration for Natural Language Query Understanding. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="mode" type="string">
      Mode of Natural Language Query Understanding. If this field is unset, the behavior defaults to NaturalLanguageQueryUnderstandingConfig.Mode.DISABLED. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="serving_config_data_store" type="object">
  Stores information regarding the serving configurations at DataStore level. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="disabled_for_serving" type="bool">
      Optional. If set true, the DataStore will not be available for serving search requests. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="solution_types" type="list(string)">
  The solutions that the data store enrolls. Available solutions for each industry_vertical: * `MEDIA`: `SOLUTION_TYPE_RECOMMENDATION` and `SOLUTION_TYPE_SEARCH`. * `SITE_SEARCH`: `SOLUTION_TYPE_SEARCH` is automatically enrolled. Other solutions cannot be enrolled. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="starting_schema" type="object">
  Defines the structure and layout of a type of document data. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="json_schema" type="string">
      The JSON representation of the schema. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      Immutable. The full resource name of the schema, in the format of `projects/&#123;project&#125;/locations/&#123;location&#125;/collections/&#123;collection&#125;/dataStores/&#123;data_store&#125;/schemas/&#123;schema&#125;`. This field must be a UTF-8 encoded string with a length limit of 1024 characters. Optional.
    </ResponseField>
    <ResponseField name="struct_schema" type="map(any)">
      The structured representation of the schema. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workspace_config" type="object">
  Config to store data store type configuration for workspace data Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="dasher_customer_id" type="string">
      Output only. Obfuscated Dasher customer ID. Derived by the server from the project's GCP organization at data store creation time; any value supplied in the request payload is ignored. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="super_admin_email_address" type="string">
      Optional. The super admin email address for the workspace that will be used for access token generation. For now we only use it for Native Google Drive connector data ingestion. Optional.
    </ResponseField>
    <ResponseField name="super_admin_service_account" type="string">
      Optional. The super admin service account for the workspace that will be used for access token generation. For now we only use it for Native Google Drive connector data ingestion. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The Google Workspace data source. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
