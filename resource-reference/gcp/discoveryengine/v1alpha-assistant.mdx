---
title: "google_discoveryengine_v1alpha_assistant"
description: "`google_discoveryengine_v1alpha_assistant` configures a conversational assistant experience (grounded answers..."
---

<Warning>
  This is Google Cloud's own **v1alpha** release channel (alpha). Google may change or remove alpha/preview endpoints at any time, often with no deprecation notice -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_discoveryengine_assistant`.
</Warning>

`google_discoveryengine_v1alpha_assistant` configures a conversational assistant experience (grounded answers, follow-up suggestions) built on top of a Discovery Engine (recently rebranded Agent Search) data store or engine.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_discoveryengine_v1alpha --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_discoveryengine_v1alpha/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discoveryengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google_discoveryengine_v1alpha/discoveryengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1alpha assistant"})

    		ubx.Resource(discoveryengine.V1alphaDiscoveryengineV1alphaAssistant, "example", discoveryengine.V1alphaDiscoveryengineV1alphaAssistantConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_discoveryengine_v1alpha --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1alphaDiscoveryengineV1alphaAssistant } from "./local-sdk/google_discoveryengine_v1alpha/sdk/typescript/google_discoveryengine_v1alpha/discoveryengine/v1alpha_discoveryengine_v1alpha_assistant.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1alpha assistant" });

      resource(V1alphaDiscoveryengineV1alphaAssistant, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_discoveryengine_v1alpha --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_discoveryengine_v1alpha/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_discoveryengine_v1alpha.discoveryengine import V1alphaDiscoveryengineV1alphaAssistant, V1alphaDiscoveryengineV1alphaAssistantConfig

    def describe():
        ubx.intent("example own v1alpha assistant")

        ubx.resource(V1alphaDiscoveryengineV1alphaAssistant, "example", V1alphaDiscoveryengineV1alphaAssistantConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discoveryengine_v1alpha_assistant called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Represents the time when this Assistant was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="customer_policy" type="object">
  Customer-defined policy for the assistant. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="banned_phrases" type="list(object)">
      Optional. List of banned phrases. Optional.
      <Expandable title="properties">
        <ResponseField name="ignore_diacritics" type="bool">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="match_type" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="phrase" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="data_protection_policy" type="object">
      Contains the data protection policy config for a DataStore or a connector. Optional.
      <Expandable title="properties">
        <ResponseField name="sensitive_data_protection_policy" type="object">
          Specifies a Sensitive Data Protection (https://cloud.google.com/sensitive-data-protection/docs/sensitive-data-protection-overview) policy. Optional.
          <Expandable title="properties">
            <ResponseField name="policy" type="string">
              Optional. Specifies the resource name of the Sensitive Data Protection content policy. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="model_armor_config" type="object">
      Configuration for customer defined Model Armor templates to be used for sanitizing user prompts and assistant responses. Optional.
      <Expandable title="properties">
        <ResponseField name="failure_mode" type="string">
          Optional. Defines the failure mode for Model Armor sanitization. Optional.
        </ResponseField>
        <ResponseField name="response_template" type="string">
          Optional. The resource name of the Model Armor template for sanitizing assistant responses. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/templates/&#123;template_id&#125;` If not specified, no sanitization will be applied to the assistant response. Optional.
        </ResponseField>
        <ResponseField name="user_prompt_template" type="string">
          Optional. The resource name of the Model Armor template for sanitizing user prompts. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/templates/&#123;template_id&#125;` If not specified, no sanitization will be applied to the user prompt. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_web_grounding_toggle_off" type="bool">
  Optional. This field controls the default web grounding toggle for end users if `web_grounding_type` is set to `WEB_GROUNDING_TYPE_GOOGLE_SEARCH` or `WEB_GROUNDING_TYPE_ENTERPRISE_WEB_SEARCH`. By default, this field is set to false. If `web_grounding_type` is `WEB_GROUNDING_TYPE_GOOGLE_SEARCH` or `WEB_GROUNDING_TYPE_ENTERPRISE_WEB_SEARCH`, end users will have web grounding enabled by default on UI. If true, grounding toggle will be disabled by default on UI. End users can still enable web grounding in the UI if web grounding is enabled. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description for additional information. Expected to be shown on the configuration UI, not to the users of the assistant. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="disable_location_context" type="bool">
  Optional. Indicates whether to disable user location context. By default, user location context is enabled. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The assistant display name. It must be a UTF-8 encoded string with a length limit of 128 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="enabled_tools" type="map(object)">
  Optional. Note: not implemented yet. Use enabled_actions instead. The enabled tools on this assistant. The keys are connector name, for example "projects/&#123;projectId&#125;/locations/&#123;locationId&#125;/collections/&#123;collectionId&#125;/dataconnector The values consist of admin enabled tools towards the connector instance. Admin can selectively enable multiple tools on any of the connector instances that they created in the project. For example &#123;"jira1ConnectorName": [(toolId1, "createTicket"), (toolId2, "transferTicket")], "gmail1ConnectorName": [(toolId3, "sendEmail"),..] &#125; Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="tool_info" type="list(object)">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="tool_display_name" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="tool_name" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="generation_config" type="object">
  Configuration for the generation of the assistant response. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allowed_model_ids" type="list(string)">
      Optional. The list of models that are allowed to be used for assistant. Optional.
    </ResponseField>
    <ResponseField name="default_language" type="string">
      The default language to use for the generation of the assistant response. Use an ISO 639-1 language code such as `en`. If not specified, the language will be automatically detected. Optional.
    </ResponseField>
    <ResponseField name="default_model_id" type="string">
      Optional. The default model to use for assistant. Optional.
    </ResponseField>
    <ResponseField name="system_instruction" type="object">
      System instruction, also known as the prompt preamble for LLM calls. Optional.
      <Expandable title="properties">
        <ResponseField name="additional_system_instruction" type="string">
          Optional. Additional system instruction that will be added to the default system instruction. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. Resource name of the assistant. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/collections/&#123;collection&#125;/engines/&#123;engine&#125;/assistants/&#123;assistant&#125;` It must be a UTF-8 encoded string with a length limit of 1024 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Represents the time when this Assistant was most recently updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="web_grounding_type" type="string">
  Optional. The type of web grounding to use. Optional; if omitted, computed by Google Cloud.
</ResponseField>
