---
title: "google_discoveryengine_v1beta_sample_query_set"
description: "`google_discoveryengine_v1beta_sample_query_set` is a real, named collection of sample queries (its own display_name and description) used as..."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. No stable, generally-available equivalent is published yet.
</Warning>

`google_discoveryengine_v1beta_sample_query_set` is a real, named collection of sample queries (its own display_name and description) used as a consistent, reusable input set for evaluating a Vertex AI Search app's own quality over time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_discoveryengine_v1beta --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_discoveryengine_v1beta/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discoveryengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google_discoveryengine_v1beta/discoveryengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta sample query set"})

    		ubx.Resource(discoveryengine.V1betaDiscoveryengineV1betaSampleQuerySet, "example", discoveryengine.V1betaDiscoveryengineV1betaSampleQuerySetConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_discoveryengine_v1beta --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaDiscoveryengineV1betaSampleQuerySet } from "./local-sdk/google_discoveryengine_v1beta/sdk/typescript/google_discoveryengine_v1beta/discoveryengine/v1beta_discoveryengine_v1beta_sample_query_set.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta sample query set" });

      resource(V1betaDiscoveryengineV1betaSampleQuerySet, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_discoveryengine_v1beta --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_discoveryengine_v1beta/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_discoveryengine_v1beta.discoveryengine import V1betaDiscoveryengineV1betaSampleQuerySet, V1betaDiscoveryengineV1betaSampleQuerySetConfig

    def describe():
        ubx.intent("example own v1beta sample query set")

        ubx.resource(V1betaDiscoveryengineV1betaSampleQuerySet, "example", V1betaDiscoveryengineV1betaSampleQuerySetConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discoveryengine_v1beta_sample_query_set called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp the SampleQuerySet was created at. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the SampleQuerySet. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The sample query set display name. This field must be a UTF-8 encoded string with a length limit of 128 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The full resource name of the SampleQuerySet, in the format of `projects/&#123;project&#125;/locations/&#123;location&#125;/sampleQuerySets/&#123;sample_query_set&#125;`. This field must be a UTF-8 encoded string with a length limit of 1024 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>
