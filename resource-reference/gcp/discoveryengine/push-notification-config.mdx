---
title: "google_discoveryengine_push_notification_config"
description: "`google_discoveryengine_push_notification_config` configures an endpoint Discovery Engine sends push notification(s) to when a relevant event (e.g...."
---

`google_discoveryengine_push_notification_config` configures an endpoint Discovery Engine sends push notification(s) to when a relevant event (e.g. a new matching document) occurs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_discoveryengine --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_discoveryengine/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discoveryengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google_discoveryengine/discoveryengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own push notification config"})

    		ubx.Resource(discoveryengine.DiscoveryenginePushNotificationConfig, "ci-runner", discoveryengine.DiscoveryenginePushNotificationConfigConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_discoveryengine --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DiscoveryenginePushNotificationConfig } from "./local-sdk/google_discoveryengine/sdk/typescript/google_discoveryengine/discoveryengine/discoveryengine_push_notification_config.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own push notification config" });

      resource(DiscoveryenginePushNotificationConfig, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_discoveryengine --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_discoveryengine/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_discoveryengine.discoveryengine import DiscoveryenginePushNotificationConfig, DiscoveryenginePushNotificationConfigConfig

    def describe():
        ubx.intent("payments own push notification config")

        ubx.resource(DiscoveryenginePushNotificationConfig, "ci-runner", DiscoveryenginePushNotificationConfigConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discoveryengine_push_notification_config called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string">
  The resource name of the config. Format: tasks/&#123;task_id&#125;/pushNotificationConfigs/&#123;config_id&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="push_notification_config" type="object">
  Configuration for setting up push notifications for task updates. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="authentication" type="object">
      Defines authentication details, used for push notifications. Optional.
      <Expandable title="properties">
        <ResponseField name="credentials" type="string">
          Optional credentials Optional.
        </ResponseField>
        <ResponseField name="schemes" type="list(string)">
          Supported authentication schemes - e.g. Basic, Bearer, etc Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      A unique identifier (e.g. UUID) for this push notification. Optional.
    </ResponseField>
    <ResponseField name="token" type="string">
      Token unique for this task/session Optional.
    </ResponseField>
    <ResponseField name="url" type="string">
      Url to send the notification too Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
