---
title: "google_edgenetwork_subnet"
description: "Real, generated bindings for `google_edgenetwork_subnet`."
---

`google_edgenetwork_subnet` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/edgenetwork"
    )

    cfg := edgenetwork.SubnetConfig{
        Location: "example",
        Network: "example",
        SubnetId: "example",
        Zone: "example",
        Description: "example",
        Ipv4Cidr: []string{"example"},
    }
    ubx.Resource(edgenetwork.Subnet, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Subnet } from
      "jsr:@ubx/sdk-google/google/edgenetwork/subnet";

    resource(Subnet, "example", {
      location: "example",
      network: "example",
      subnetId: "example",
      zone: "example",
      description: "example",
      ipv4Cidr: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.edgenetwork import Subnet, SubnetConfig

    cfg = SubnetConfig(
        location="example",
        network="example",
        subnet_id="example",
        zone="example",
        description="example",
        ipv4_cidr=["example"],
    )
    ubx.resource(Subnet, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_edgenetwork_subnet with location, network,
subnet_id, zone, description, ipv4_cidr.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ipv4_cidr" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="ipv6_cidr" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="network" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vlan_id" type="number">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="zone" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="vlan_id" type="number">
  Optional; if omitted, computed by GCP.
</ResponseField>
