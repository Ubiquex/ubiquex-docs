---
title: "google_firestore_v1beta1_index"
description: "`google_firestore_v1beta1_index` defines a composite or single-field index on a Firestore database..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_firestore_index`.
</Warning>

`google_firestore_v1beta1_index` defines a composite or single-field index on a Firestore database, required for certain query shapes (e.g. filtering and sorting on different fields) to run efficiently.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_firestore_v1beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_firestore_v1beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firestore "github.com/ubiquex/ubx-sdk-google/sdk/go/google_firestore_v1beta1/firestore"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 index"})

    		ubx.Resource(firestore.V1beta1FirestoreV1beta1Index, "example", firestore.V1beta1FirestoreV1beta1IndexConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_firestore_v1beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1FirestoreV1beta1Index } from "./local-sdk/google_firestore_v1beta1/sdk/typescript/google_firestore_v1beta1/firestore/v1beta1_firestore_v1beta1_index.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 index" });

      resource(V1beta1FirestoreV1beta1Index, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_firestore_v1beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_firestore_v1beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_firestore_v1beta1.firestore import V1beta1FirestoreV1beta1Index, V1beta1FirestoreV1beta1IndexConfig

    def describe():
        ubx.intent("example own v1beta1 index")

        ubx.resource(V1beta1FirestoreV1beta1Index, "example", V1beta1FirestoreV1beta1IndexConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firestore_v1beta1_index called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="collection_id" type="string">
  The collection ID to which this index applies. Required. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fields" type="list(object)">
  The fields to index. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="field_path" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="mode" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the index. Output only. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the index. Output only. Optional; if omitted, computed by Google Cloud.
</ResponseField>
