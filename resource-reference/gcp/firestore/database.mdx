---
title: "google_firestore_database"
description: "Real, generated bindings for `google_firestore_database`."
---

`google_firestore_database` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firestore "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firestore"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own database"})

    		ubx.Resource(firestore.Database, "ci-runner", firestore.DatabaseConfig{
    			LocationId:                    "example",
    			Name:                          "ci-runner",
    			Type:                          "example",
    			PointInTimeRecoveryEnablement: "example",
    			Tags:                          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Database } from "jsr:@ubx/sdk-google/google/firestore/database";

    export default stack("payments", () => {
      intent({ summary: "payments own database" });

      resource(Database, "ci-runner", {
        locationId: "example",
        name: "ci-runner",
        type: "example",
        pointInTimeRecoveryEnablement: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firestore import Database, DatabaseConfig

    def describe():
        ubx.intent("payments own database")

        ubx.resource(Database, "ci-runner", DatabaseConfig(
            location_id="example",
            name="ci-runner",
            type="example",
            point_in_time_recovery_enablement="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firestore_database called "ci-runner", with
    location id example, type example, point in time recovery
    enablement example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_engine_integration_mode" type="string">
  The App Engine integration mode to use for this database. Possible values: ["ENABLED", "DISABLED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="cmek_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="active_key_version" type="list(string)">
    </ResponseField>
    <ResponseField name="kms_key_name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="concurrency_mode" type="string">
  The concurrency control mode to use for this database. Possible values: ["OPTIMISTIC", "PESSIMISTIC", "OPTIMISTIC_WITH_ENTITY_GROUPS"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="database_edition" type="string">
  The database edition. When set to 'ENTERPRISE', then type must be set to 'FIRESTORE_NATIVE'. Possible values: ["STANDARD", "ENTERPRISE"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="delete_protection_state" type="string">
  State of delete protection for the database. When delete protection is enabled, this database cannot be deleted. The default value is 'DELETE_PROTECTION_STATE_UNSPECIFIED', which is currently equivalent to 'DELETE_PROTECTION_DISABLED'. **Note:** Additionally, to delete this database using 'terraform destroy', 'deletion_policy' must be set to 'DELETE'. Possible values: ["DELETE_PROTECTION_STATE_UNSPECIFIED", "DELETE_PROTECTION_ENABLED", "DELETE_PROTECTION_DISABLED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "ABANDON". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="firestore_data_access_mode" type="string">
  The Firestore API data access mode to use for this database. Can only be specified for 'ENTERPRISE' edition databases. Possible values: ["DATA_ACCESS_MODE_ENABLED", "DATA_ACCESS_MODE_DISABLED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location_id" type="string" required>
  The location of the database. Available locations are listed at https://cloud.google.com/firestore/docs/locations. Required.
</ResponseField>

<ResponseField name="mongodb_compatible_data_access_mode" type="string">
  The MongoDB compatible API data access mode to use for this database. Can only be specified for 'ENTERPRISE' edition databases. Possible values: ["DATA_ACCESS_MODE_ENABLED", "DATA_ACCESS_MODE_DISABLED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  The ID to use for the database, which will become the final component of the database's resource name. This value should be 4-63 characters. Valid characters are /[a-z][0-9]-/ with first character a letter and the last a letter or a number. Must not be UUID-like /[0-9a-f]&#123;8&#125;(-[0-9a-f]&#123;4&#125;)&#123;3&#125;-[0-9a-f]&#123;12&#125;/. "(default)" database id is also valid. Required.
</ResponseField>

<ResponseField name="point_in_time_recovery_enablement" type="string">
  Whether to enable the PITR feature on this database. If 'POINT_IN_TIME_RECOVERY_ENABLED' is selected, reads are supported on selected versions of the data from within the past 7 days. versionRetentionPeriod and earliestVersionTime can be used to determine the supported versions. These include reads against any timestamp within the past hour and reads against 1-minute snapshots beyond 1 hour and within 7 days. If 'POINT_IN_TIME_RECOVERY_DISABLED' is selected, reads are supported on any version of the data from within the past 1 hour. Default value: "POINT_IN_TIME_RECOVERY_DISABLED" Possible values: ["POINT_IN_TIME_RECOVERY_ENABLED", "POINT_IN_TIME_RECOVERY_DISABLED"] Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="realtime_updates_mode" type="string">
  The Realtime Updates mode to use for this database. Can only be specified for 'ENTERPRISE' edition databases. Possible values: ["REALTIME_UPDATES_MODE_ENABLED", "REALTIME_UPDATES_MODE_DISABLED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Input only. A map of resource manager tags. Resource manager tag keys and values have the same definition as resource manager tags. Keys must be in the format tagKeys/&#123;tag_key_id&#125;, and values are in the format tagValues/456. The field is ignored when empty. The field is immutable and causes resource replacement when mutated. To apply tags to an existing resource, see the 'google_tags_tag_value' resource. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of the database. See https://cloud.google.com/datastore/docs/firestore-or-datastore for information about how to choose. Possible values: ["FIRESTORE_NATIVE", "DATASTORE_MODE"] Required.
</ResponseField>

## Output properties

<ResponseField name="app_engine_integration_mode" type="string">
  The App Engine integration mode to use for this database. Possible values: ["ENABLED", "DISABLED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="cmek_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="active_key_version" type="list(string)">
    </ResponseField>
    <ResponseField name="kms_key_name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="concurrency_mode" type="string">
  The concurrency control mode to use for this database. Possible values: ["OPTIMISTIC", "PESSIMISTIC", "OPTIMISTIC_WITH_ENTITY_GROUPS"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp at which this database was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="database_edition" type="string">
  The database edition. When set to 'ENTERPRISE', then type must be set to 'FIRESTORE_NATIVE'. Possible values: ["STANDARD", "ENTERPRISE"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="delete_protection_state" type="string">
  State of delete protection for the database. When delete protection is enabled, this database cannot be deleted. The default value is 'DELETE_PROTECTION_STATE_UNSPECIFIED', which is currently equivalent to 'DELETE_PROTECTION_DISABLED'. **Note:** Additionally, to delete this database using 'terraform destroy', 'deletion_policy' must be set to 'DELETE'. Possible values: ["DELETE_PROTECTION_STATE_UNSPECIFIED", "DELETE_PROTECTION_ENABLED", "DELETE_PROTECTION_DISABLED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "ABANDON". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="earliest_version_time" type="string">
  Output only. The earliest timestamp at which older versions of the data can be read from the database. See versionRetentionPeriod above; this field is populated with now - versionRetentionPeriod. This value is continuously updated, and becomes stale the moment it is queried. If you are using this value to recover data, make sure to account for the time from the moment when the value is queried to the moment when you initiate the recovery. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="firestore_data_access_mode" type="string">
  The Firestore API data access mode to use for this database. Can only be specified for 'ENTERPRISE' edition databases. Possible values: ["DATA_ACCESS_MODE_ENABLED", "DATA_ACCESS_MODE_DISABLED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_prefix" type="string">
  Output only. The keyPrefix for this database. This keyPrefix is used, in combination with the project id ("~") to construct the application id that is returned from the Cloud Datastore APIs in Google App Engine first generation runtimes. This value may be empty in which case the appid to use for URL-encoded keys is the project_id (eg: foo instead of v~foo). Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="mongodb_compatible_data_access_mode" type="string">
  The MongoDB compatible API data access mode to use for this database. Can only be specified for 'ENTERPRISE' edition databases. Possible values: ["DATA_ACCESS_MODE_ENABLED", "DATA_ACCESS_MODE_DISABLED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="realtime_updates_mode" type="string">
  The Realtime Updates mode to use for this database. Can only be specified for 'ENTERPRISE' edition databases. Possible values: ["REALTIME_UPDATES_MODE_ENABLED", "REALTIME_UPDATES_MODE_DISABLED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The system-generated UUID4 for this Database. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp at which this database was most recently updated. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="version_retention_period" type="string">
  Output only. The period during which past versions of data are retained in the database. Any read or query can specify a readTime within this window, and will read the state of the database at that time. If the PITR feature is enabled, the retention period is 7 days. Otherwise, the retention period is 1 hour. A duration in seconds with up to nine fractional digits, ending with 's'. Example: "3.5s". Computed by GCP after `ship`.
</ResponseField>
