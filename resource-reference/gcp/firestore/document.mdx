---
title: "google_firestore_document"
description: "`google_firestore_document` is a single Firestore document, the basic unit of storage in Firestore's own document-oriented NoSQL database..."
---

`google_firestore_document` is a single Firestore document, the basic unit of storage in Firestore's own document-oriented NoSQL database, holding a set of typed field/value pairs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firestore "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firestore"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own document"})

    		ubx.Resource(firestore.Document, "example", firestore.DocumentConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Document } from "@ubx/sdk-google/google/firestore/document";

    export default stack("example", () => {
      intent({ summary: "example own document" });

      resource(Document, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firestore.document import Document, DocumentConfig

    def describe():
        ubx.intent("example own document")

        ubx.resource(Document, "example", DocumentConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firestore_document called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The time at which the document was created. This value increases monotonically when a document is deleted then recreated. It can also be compared to values from other documents and the `read_time` of a query. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fields" type="map(object)">
  The document's fields. The map keys represent field names. Field names matching the regular expression `__.*__` are reserved. Reserved field names are forbidden except in certain documented contexts. The field names, represented as UTF-8, must not exceed 1,500 bytes and cannot be empty. Field paths may be used in other contexts to refer to structured fields defined here. For `map_value`, the field path is represented by a dot-delimited (`.`) string of segments. Each segment is either a simple field name (defined below) or a quoted field name. For example, the structured field `"foo" : &#123; map_value: &#123; "x&y" : &#123; string_value: "hello" &#125;&#125;&#125;` would be represented by the field path `` foo.`x&y` ``. A simple field name contains only characters `a` to `z`, `A` to `Z`, `0` to `9`, or `_`, and must not start with `0` to `9`. For example, `foo_bar_17`. A quoted field name starts and ends with `` ` `` and may contain any character. Some characters, including `` ` ``, must be escaped using a `\`. For example, `` `x&y` `` represents `x&y` and `` `bak\`tik` `` represents `` bak`tik ``. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="array_value" type="object">
      This field's own value, when its type is an array. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="values" type="list(any)">
          The value(s) making up this array. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="boolean_value" type="bool">
      This field's own value, when its type is a boolean. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="bytes_value" type="string">
      This field's own value, when its type is raw bytes, base64-encoded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="double_value" type="number">
      This field's own value, when its type is a floating-point number. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="field_reference_value" type="string">
      This field's own value, when it references another field by its dotted path. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="function_value" type="object">
      This field's own value, when it's the result of a Firestore pipeline function call. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="args" type="list(any)">
          The argument(s) passed to this function call. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of this function. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="options" type="map(any)">
          Named option(s) passed to this function call. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="geo_point_value" type="object">
      This field's own value, when its type is a geographic coordinate (latitude/longitude). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="latitude" type="number">
          The geographic coordinate's own latitude, in degrees. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="longitude" type="number">
          The geographic coordinate's own longitude, in degrees. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="integer_value" type="string">
      This field's own value, when its type is a 64-bit integer. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="map_value" type="object">
      This field's own value, when its type is a nested map of key/value pairs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="fields" type="map(any)">
          The key/value pair(s) making up this map. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="null_value" type="string">
      This field's own value, when it's explicitly null. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pipeline_value" type="object">
      This field's own value, when it's the result of a Firestore aggregation pipeline. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="stages" type="list(object)">
          The ordered stage(s) making up this pipeline. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="args" type="list(any)">
              The argument(s) passed to this function call. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of this function. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="options" type="map(any)">
              Named option(s) passed to this function call. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reference_value" type="string">
      This field's own value, when it references another document, by its full resource name. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="string_value" type="string">
      This field's own value, when its type is a string. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="timestamp_value" type="string">
      This field's own value, when its type is a point in time. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="variable_reference_value" type="string">
      This field's own value, when it references a pipeline variable by name. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the document, for example `projects/&#123;project_id&#125;/databases/&#123;database_id&#125;/documents/&#123;document_path&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which the document was last changed. This value is initially set to the `create_time` then increases monotonically with each change to the document. It can also be compared to values from other documents and the `read_time` of a query. Optional; if omitted, computed by Google Cloud.
</ResponseField>
