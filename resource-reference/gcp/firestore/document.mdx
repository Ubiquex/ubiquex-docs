---
title: "google_firestore_document"
description: "Real, generated bindings for `google_firestore_document`."
---

`google_firestore_document` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firestore "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firestore"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own document"})

    		ubx.Resource(firestore.Document, "ci-runner", firestore.DocumentConfig{
    			Collection: "example",
    			DocumentId: "example",
    			Fields:     "example",
    			Database:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Document } from "jsr:@ubx/sdk-google/google/firestore/document";

    export default stack("payments", () => {
      intent({ summary: "payments own document" });

      resource(Document, "ci-runner", {
        collection: "example",
        documentId: "example",
        fields: "example",
        database: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firestore import Document, DocumentConfig

    def describe():
        ubx.intent("payments own document")

        ubx.resource(Document, "ci-runner", DocumentConfig(
            collection="example",
            document_id="example",
            fields="example",
            database="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firestore_document, with collection example,
    document id example, fields example, database example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="collection" type="string" required>
  The collection ID, relative to database. For example: chatrooms or chatrooms/my-document/private-messages. Required.
</ResponseField>

<ResponseField name="database" type="string">
  The Firestore database id. Defaults to '"(default)"'. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="document_id" type="string" required>
  The client-assigned document ID to use for this document during creation. Required.
</ResponseField>

<ResponseField name="fields" type="string" required>
  The document's [fields](https://cloud.google.com/firestore/docs/reference/rest/v1/projects.databases.documents) formated as a json string. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Creation timestamp in RFC3339 format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  A server defined name for this document. Format: 'projects/&#123;&#123;project_id&#125;&#125;/databases/&#123;&#123;database_id&#125;&#125;/documents/&#123;&#123;path&#125;&#125;/&#123;&#123;document_id&#125;&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="path" type="string">
  A relative path to the collection this document exists within Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Last update timestamp in RFC3339 format. Computed by GCP after `ship`.
</ResponseField>
