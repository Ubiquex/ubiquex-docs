---
title: "google_firestore_document"
description: "Real, generated bindings for `google_firestore_document`."
---

`google_firestore_document` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firestore"
    )

    cfg := firestore.DocumentConfig{
        Collection: "example",
        DocumentId: "example",
        Fields: "example",
        Database: "example",
    }
    ubx.Resource(firestore.Document, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Document } from
      "jsr:@ubx/sdk-google/google/firestore/document";

    resource(Document, "example", {
      collection: "example",
      documentId: "example",
      fields: "example",
      database: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.firestore import Document, DocumentConfig

    cfg = DocumentConfig(
        collection="example",
        document_id="example",
        fields="example",
        database="example",
    )
    ubx.resource(Document, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_firestore_document with collection, document_id,
fields, database.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="collection" type="string" required>
  Required.
</ResponseField>

<ResponseField name="database" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="document_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="fields" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="path" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
