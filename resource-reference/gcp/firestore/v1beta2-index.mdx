---
title: "google_firestore_v1beta2_index"
description: "`google_firestore_v1beta2_index` defines a composite or single-field index on a Firestore database..."
---

<Warning>
  This is Google Cloud's own **v1beta2** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_firestore_index`.
</Warning>

`google_firestore_v1beta2_index` defines a composite or single-field index on a Firestore database, required for certain query shapes (e.g. filtering and sorting on different fields) to run efficiently.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_firestore_v1beta2 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_firestore_v1beta2/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firestore "github.com/ubiquex/ubx-sdk-google/sdk/go/google_firestore_v1beta2/firestore"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta2 index"})

    		ubx.Resource(firestore.V1beta2FirestoreV1beta2Index, "example", firestore.V1beta2FirestoreV1beta2IndexConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_firestore_v1beta2 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta2FirestoreV1beta2Index } from "./local-sdk/google_firestore_v1beta2/sdk/typescript/google_firestore_v1beta2/firestore/v1beta2_firestore_v1beta2_index.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta2 index" });

      resource(V1beta2FirestoreV1beta2Index, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_firestore_v1beta2 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_firestore_v1beta2/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_firestore_v1beta2.firestore import V1beta2FirestoreV1beta2Index, V1beta2FirestoreV1beta2IndexConfig

    def describe():
        ubx.intent("example own v1beta2 index")

        ubx.resource(V1beta2FirestoreV1beta2Index, "example", V1beta2FirestoreV1beta2IndexConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firestore_v1beta2_index called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="fields" type="list(object)">
  The fields supported by this index. For composite indexes, this is always 2 or more fields. The last field entry is always for the field path `__name__`. If, on creation, `__name__` was not specified as the last field, it will be added automatically with the same direction as that of the last field defined. If the final field in a composite index is not directional, the `__name__` will be ordered ASCENDING (unless explicitly specified). For single field indexes, this will always be exactly one entry with a field path equal to the field path of the associated field. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="array_config" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="field_path" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="order" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. A server defined name for this index. The form of this name for composite indexes will be: `projects/&#123;project_id&#125;/databases/&#123;database_id&#125;/collectionGroups/&#123;collection_id&#125;/indexes/&#123;composite_index_id&#125;` For single field indexes, this field will be empty. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="query_scope" type="string">
  Indexes with a collection query scope specified allow queries against a collection that is the child of a specific document, specified at query time, and that has the same collection id. Indexes with a collection group query scope specified allow queries against all collections descended from a specific document, specified at query time, and that have the same collection id as this index. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The serving state of the index. Optional; if omitted, computed by Google Cloud.
</ResponseField>
