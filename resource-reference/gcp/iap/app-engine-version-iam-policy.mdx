---
title: "google_iap_app_engine_version_iam_policy"
description: "Real, generated bindings for `google_iap_app_engine_version_iam_policy`."
---

`google_iap_app_engine_version_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iap "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iap"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app engine version iam policy"})

    		ubx.Resource(iap.AppEngineVersionIamPolicy, "ci-runner", iap.AppEngineVersionIamPolicyConfig{
    			AppId:      "example",
    			PolicyData: "example",
    			Service:    "example",
    			VersionId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppEngineVersionIamPolicy } from "jsr:@ubx/sdk-google/google/iap/app_engine_version_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own app engine version iam policy" });

      resource(AppEngineVersionIamPolicy, "ci-runner", {
        appId: "example",
        policyData: "example",
        service: "example",
        versionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iap import AppEngineVersionIamPolicy, AppEngineVersionIamPolicyConfig

    def describe():
        ubx.intent("payments own app engine version iam policy")

        ubx.resource(AppEngineVersionIamPolicy, "ci-runner", AppEngineVersionIamPolicyConfig(
            app_id="example",
            policy_data="example",
            service="example",
            version_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iap_app_engine_version_iam_policy, with app id
    example, policy data example, service example, version id
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service" type="string" required>
  Required.
</ResponseField>

<ResponseField name="version_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
