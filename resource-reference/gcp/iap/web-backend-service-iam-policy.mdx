---
title: "google_iap_web_backend_service_iam_policy"
description: "Real, generated bindings for `google_iap_web_backend_service_iam_policy`."
---

`google_iap_web_backend_service_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/iap"
    )

    cfg := iap.WebBackendServiceIamPolicyConfig{
        PolicyData: "example",
        WebBackendService: "example",
    }
    ubx.Resource(iap.WebBackendServiceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WebBackendServiceIamPolicy } from
      "jsr:@ubx/sdk-google/google/iap/web_backend_service_iam_policy";

    resource(WebBackendServiceIamPolicy, "example", {
      policyData: "example",
      webBackendService: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.iap import (
        WebBackendServiceIamPolicy,
        WebBackendServiceIamPolicyConfig,
    )

    cfg = WebBackendServiceIamPolicyConfig(
        policy_data="example",
        web_backend_service="example",
    )
    ubx.resource(WebBackendServiceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_iap_web_backend_service_iam_policy with
policy_data, web_backend_service.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="web_backend_service" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
