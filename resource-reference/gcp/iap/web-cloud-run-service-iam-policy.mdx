---
title: "google_iap_web_cloud_run_service_iam_policy"
description: "Real, generated bindings for `google_iap_web_cloud_run_service_iam_policy`."
---

`google_iap_web_cloud_run_service_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iap"
    )

    cfg := iap.WebCloudRunServiceIamPolicyConfig{
        CloudRunServiceName: "example-cloud-run-service-name",
        PolicyData: "example",
    }
    ubx.Resource(iap.WebCloudRunServiceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WebCloudRunServiceIamPolicy } from
      "jsr:@ubx/sdk-google/google/iap/web_cloud_run_service_iam_policy";

    resource(WebCloudRunServiceIamPolicy, "example", {
      cloudRunServiceName: "example-cloud-run-service-name",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.iap import (
        WebCloudRunServiceIamPolicy,
        WebCloudRunServiceIamPolicyConfig,
    )

    cfg = WebCloudRunServiceIamPolicyConfig(
        cloud_run_service_name="example-cloud-run-service-name",
        policy_data="example",
    )
    ubx.resource(WebCloudRunServiceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_iap_web_cloud_run_service_iam_policy with
cloud_run_service_name, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_run_service_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
