---
title: "google_iap_web_cloud_run_service_iam_policy"
description: "Real, generated bindings for `google_iap_web_cloud_run_service_iam_policy`."
---

`google_iap_web_cloud_run_service_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iap "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iap"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own web cloud run service iam policy"})

    		ubx.Resource(iap.WebCloudRunServiceIamPolicy, "ci-runner", iap.WebCloudRunServiceIamPolicyConfig{
    			CloudRunServiceName: "example-cloud-run-service-name",
    			PolicyData:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WebCloudRunServiceIamPolicy } from "jsr:@ubx/sdk-google/google/iap/web_cloud_run_service_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own web cloud run service iam policy" });

      resource(WebCloudRunServiceIamPolicy, "ci-runner", {
        cloudRunServiceName: "example-cloud-run-service-name",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iap import WebCloudRunServiceIamPolicy, WebCloudRunServiceIamPolicyConfig

    def describe():
        ubx.intent("payments own web cloud run service iam policy")

        ubx.resource(WebCloudRunServiceIamPolicy, "ci-runner", WebCloudRunServiceIamPolicyConfig(
            cloud_run_service_name="example-cloud-run-service-name",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iap_web_cloud_run_service_iam_policy, with cloud
    run service name example-cloud-run-service-name, policy data
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_run_service_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
