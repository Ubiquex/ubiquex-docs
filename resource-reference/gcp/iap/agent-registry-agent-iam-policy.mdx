---
title: "google_iap_agent_registry_agent_iam_policy"
description: "Real, generated bindings for `google_iap_agent_registry_agent_iam_policy`."
---

`google_iap_agent_registry_agent_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iap "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/iap"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own agent registry agent iam policy"})

    		ubx.Resource(iap.AgentRegistryAgentIamPolicy, "ci-runner", iap.AgentRegistryAgentIamPolicyConfig{
    			AgentId:    "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AgentRegistryAgentIamPolicy } from "jsr:@ubx/sdk-google/google/iap/agent_registry_agent_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own agent registry agent iam policy" });

      resource(AgentRegistryAgentIamPolicy, "ci-runner", {
        agentId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iap import AgentRegistryAgentIamPolicy, AgentRegistryAgentIamPolicyConfig

    def describe():
        ubx.intent("payments own agent registry agent iam policy")

        ubx.resource(AgentRegistryAgentIamPolicy, "ci-runner", AgentRegistryAgentIamPolicyConfig(
            agent_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iap_agent_registry_agent_iam_policy, with agent id
    example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="agent_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
