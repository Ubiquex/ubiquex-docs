---
title: "google_iap_tunnel_dest_group_iam_policy"
description: "Real, generated bindings for `google_iap_tunnel_dest_group_iam_policy`."
---

`google_iap_tunnel_dest_group_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/iap"
    )

    cfg := iap.TunnelDestGroupIamPolicyConfig{
        DestGroup: "example",
        PolicyData: "example",
    }
    ubx.Resource(iap.TunnelDestGroupIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TunnelDestGroupIamPolicy } from
      "jsr:@ubx/sdk-google/google/iap/tunnel_dest_group_iam_policy";

    resource(TunnelDestGroupIamPolicy, "example", {
      destGroup: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.iap import (
        TunnelDestGroupIamPolicy,
        TunnelDestGroupIamPolicyConfig,
    )

    cfg = TunnelDestGroupIamPolicyConfig(
        dest_group="example",
        policy_data="example",
    )
    ubx.resource(TunnelDestGroupIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_iap_tunnel_dest_group_iam_policy with
dest_group, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dest_group" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
