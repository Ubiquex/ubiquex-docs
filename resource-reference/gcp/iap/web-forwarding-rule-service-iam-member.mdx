---
title: "google_iap_web_forwarding_rule_service_iam_member"
description: "Real, generated bindings for `google_iap_web_forwarding_rule_service_iam_member`."
---

`google_iap_web_forwarding_rule_service_iam_member` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iap "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iap"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own web forwarding rule service iam member"})

    		ubx.Resource(iap.WebForwardingRuleServiceIamMember, "ci-runner", iap.WebForwardingRuleServiceIamMemberConfig{
    			ForwardingRuleServiceName: "example-forwarding-rule-service-name",
    			Member:                    "example",
    			Role:                      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WebForwardingRuleServiceIamMember } from "jsr:@ubx/sdk-google/google/iap/web_forwarding_rule_service_iam_member";

    export default stack("payments", () => {
      intent({ summary: "payments own web forwarding rule service iam member" });

      resource(WebForwardingRuleServiceIamMember, "ci-runner", {
        forwardingRuleServiceName: "example-forwarding-rule-service-name",
        member: "example",
        role: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iap import WebForwardingRuleServiceIamMember, WebForwardingRuleServiceIamMemberConfig

    def describe():
        ubx.intent("payments own web forwarding rule service iam member")

        ubx.resource(WebForwardingRuleServiceIamMember, "ci-runner", WebForwardingRuleServiceIamMemberConfig(
            forwarding_rule_service_name="example-forwarding-rule-service-name",
            member="example",
            role="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iap_web_forwarding_rule_service_iam_member, with
    forwarding rule service name
    example-forwarding-rule-service-name, member example, role
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="forwarding_rule_service_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="member" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
