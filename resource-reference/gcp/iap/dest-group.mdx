---
title: "google_iap_dest_group"
description: "`google_iap_dest_group` defines a named group of TCP destination(s) that Identity-Aware Proxy's own TCP forwarding tunnel access can be scoped to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iap "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iap"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dest group"})

    		ubx.Resource(iap.DestGroup, "example", iap.DestGroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DestGroup } from "@ubx/sdk-google/google/iap/dest_group";

    export default stack("example", () => {
      intent({ summary: "example own dest group" });

      resource(DestGroup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iap import DestGroup, DestGroupConfig

    def describe():
        ubx.intent("example own dest group")

        ubx.resource(DestGroup, "example", DestGroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iap_dest_group called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="cidrs" type="list(string)">
  Optional. Unordered list. List of CIDRs that this group applies to. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fqdns" type="list(string)">
  Optional. Unordered list. List of FQDNs that this group applies to. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Identifier for the TunnelDestGroup. Must be unique within the project and contain only lower case letters (a-z) and dashes (-). Optional; if omitted, computed by Google Cloud.
</ResponseField>
