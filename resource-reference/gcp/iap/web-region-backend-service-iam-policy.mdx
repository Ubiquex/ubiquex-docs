---
title: "google_iap_web_region_backend_service_iam_policy"
description: "Real, generated bindings for `google_iap_web_region_backend_service_iam_policy`."
---

`google_iap_web_region_backend_service_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iap "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iap"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own web region backend service iam policy"})

    		ubx.Resource(iap.WebRegionBackendServiceIamPolicy, "ci-runner", iap.WebRegionBackendServiceIamPolicyConfig{
    			PolicyData:              "example",
    			WebRegionBackendService: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WebRegionBackendServiceIamPolicy } from "jsr:@ubx/sdk-google/google/iap/web_region_backend_service_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own web region backend service iam policy" });

      resource(WebRegionBackendServiceIamPolicy, "ci-runner", {
        policyData: "example",
        webRegionBackendService: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iap import WebRegionBackendServiceIamPolicy, WebRegionBackendServiceIamPolicyConfig

    def describe():
        ubx.intent("payments own web region backend service iam policy")

        ubx.resource(WebRegionBackendServiceIamPolicy, "ci-runner", WebRegionBackendServiceIamPolicyConfig(
            policy_data="example",
            web_region_backend_service="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iap_web_region_backend_service_iam_policy, with
    policy data example, web region backend service example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="web_region_backend_service" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
