---
title: "google_iap_agent_registry_endpoint_iam_policy"
description: "Real, generated bindings for `google_iap_agent_registry_endpoint_iam_policy`."
---

`google_iap_agent_registry_endpoint_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iap"
    )

    cfg := iap.AgentRegistryEndpointIamPolicyConfig{
        EndpointId: "example",
        PolicyData: "example",
    }
    ubx.Resource(iap.AgentRegistryEndpointIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AgentRegistryEndpointIamPolicy } from
      "jsr:@ubx/sdk-google/google/iap/agent_registry_endpoint_iam_policy";

    resource(AgentRegistryEndpointIamPolicy, "example", {
      endpointId: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.iap import (
        AgentRegistryEndpointIamPolicy,
        AgentRegistryEndpointIamPolicyConfig,
    )

    cfg = AgentRegistryEndpointIamPolicyConfig(
        endpoint_id="example",
        policy_data="example",
    )
    ubx.resource(AgentRegistryEndpointIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_iap_agent_registry_endpoint_iam_policy with
endpoint_id, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="endpoint_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
