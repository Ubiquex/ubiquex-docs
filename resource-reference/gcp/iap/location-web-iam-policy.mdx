---
title: "google_iap_location_web_iam_policy"
description: "Real, generated bindings for `google_iap_location_web_iam_policy`."
---

`google_iap_location_web_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/iap"
    )

    cfg := iap.LocationWebIamPolicyConfig{
        Location: "example",
        PolicyData: "example",
    }
    ubx.Resource(iap.LocationWebIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LocationWebIamPolicy } from
      "jsr:@ubx/sdk-google/google/iap/location_web_iam_policy";

    resource(LocationWebIamPolicy, "example", {
      location: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.iap import (
        LocationWebIamPolicy,
        LocationWebIamPolicyConfig,
    )

    cfg = LocationWebIamPolicyConfig(
        location="example",
        policy_data="example",
    )
    ubx.resource(LocationWebIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_iap_location_web_iam_policy with location,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
