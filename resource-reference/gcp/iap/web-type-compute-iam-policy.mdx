---
title: "google_iap_web_type_compute_iam_policy"
description: "Real, generated bindings for `google_iap_web_type_compute_iam_policy`."
---

`google_iap_web_type_compute_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/iap"
    )

    cfg := iap.WebTypeComputeIamPolicyConfig{
        PolicyData: "example",
    }
    ubx.Resource(iap.WebTypeComputeIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WebTypeComputeIamPolicy } from
      "jsr:@ubx/sdk-google/google/iap/web_type_compute_iam_policy";

    resource(WebTypeComputeIamPolicy, "example", {
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.iap import (
        WebTypeComputeIamPolicy,
        WebTypeComputeIamPolicyConfig,
    )

    cfg = WebTypeComputeIamPolicyConfig(
        policy_data="example",
    )
    ubx.resource(WebTypeComputeIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_iap_web_type_compute_iam_policy with
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
