---
title: "google_iap_web_type_compute_iam_policy"
description: "Real, generated bindings for `google_iap_web_type_compute_iam_policy`."
---

`google_iap_web_type_compute_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iap "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/iap"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own web type compute iam policy"})

    		ubx.Resource(iap.WebTypeComputeIamPolicy, "ci-runner", iap.WebTypeComputeIamPolicyConfig{
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WebTypeComputeIamPolicy } from "jsr:@ubx/sdk-google/google/iap/web_type_compute_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own web type compute iam policy" });

      resource(WebTypeComputeIamPolicy, "ci-runner", {
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iap import WebTypeComputeIamPolicy, WebTypeComputeIamPolicyConfig

    def describe():
        ubx.intent("payments own web type compute iam policy")

        ubx.resource(WebTypeComputeIamPolicy, "ci-runner", WebTypeComputeIamPolicyConfig(
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iap_web_type_compute_iam_policy, with policy data
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
