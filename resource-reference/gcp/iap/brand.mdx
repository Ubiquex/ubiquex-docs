---
title: "google_iap_brand"
description: "Real, generated bindings for `google_iap_brand`."
---

`google_iap_brand` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iap "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iap"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own brand"})

    		ubx.Resource(iap.Brand, "ci-runner", iap.BrandConfig{
    			ApplicationTitle: "example",
    			SupportEmail:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Brand } from "jsr:@ubx/sdk-google/google/iap/brand";

    export default stack("payments", () => {
      intent({ summary: "payments own brand" });

      resource(Brand, "ci-runner", {
        applicationTitle: "example",
        supportEmail: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iap import Brand, BrandConfig

    def describe():
        ubx.intent("payments own brand")

        ubx.resource(Brand, "ci-runner", BrandConfig(
            application_title="example",
            support_email="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iap_brand, with application title example, support
    email example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_title" type="string" required>
  Application name displayed on OAuth consent screen. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="support_email" type="string" required>
  Support email displayed on the OAuth consent screen. Can be either a user or group email. When a user email is specified, the caller must be the user with the associated email address. When a group email is specified, the caller can be either a user or a service account which is an owner of the specified group in Cloud Identity. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier of the brand, in the format 'projects/&#123;project_number&#125;/brands/&#123;brand_id&#125;' NOTE: The name can also be expressed as 'projects/&#123;project_id&#125;/brands/&#123;brand_id&#125;', e.g. when importing. NOTE: The brand identification corresponds to the project number as only one brand can be created per project. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="org_internal_only" type="bool">
  Whether the brand is only intended for usage inside the GSuite organization only. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
