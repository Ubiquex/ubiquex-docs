---
title: "google_iap_web_forwarding_rule_service_iam_policy"
description: "Real, generated bindings for `google_iap_web_forwarding_rule_service_iam_policy`."
---

`google_iap_web_forwarding_rule_service_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iap "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/iap"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own web forwarding rule service iam policy"})

    		ubx.Resource(iap.WebForwardingRuleServiceIamPolicy, "ci-runner", iap.WebForwardingRuleServiceIamPolicyConfig{
    			ForwardingRuleServiceName: "example-forwarding-rule-service-name",
    			PolicyData:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WebForwardingRuleServiceIamPolicy } from "jsr:@ubx/sdk-google/google/iap/web_forwarding_rule_service_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own web forwarding rule service iam policy" });

      resource(WebForwardingRuleServiceIamPolicy, "ci-runner", {
        forwardingRuleServiceName: "example-forwarding-rule-service-name",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iap import WebForwardingRuleServiceIamPolicy, WebForwardingRuleServiceIamPolicyConfig

    def describe():
        ubx.intent("payments own web forwarding rule service iam policy")

        ubx.resource(WebForwardingRuleServiceIamPolicy, "ci-runner", WebForwardingRuleServiceIamPolicyConfig(
            forwarding_rule_service_name="example-forwarding-rule-service-name",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iap_web_forwarding_rule_service_iam_policy, with
    forwarding rule service name
    example-forwarding-rule-service-name, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="forwarding_rule_service_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
