---
title: "google_iap_agent_registry_iam_policy"
description: "Real, generated bindings for `google_iap_agent_registry_iam_policy`."
---

`google_iap_agent_registry_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iap"
    )

    cfg := iap.AgentRegistryIamPolicyConfig{
        Location: "example",
        PolicyData: "example",
    }
    ubx.Resource(iap.AgentRegistryIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AgentRegistryIamPolicy } from
      "jsr:@ubx/sdk-google/google/iap/agent_registry_iam_policy";

    resource(AgentRegistryIamPolicy, "example", {
      location: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.iap import (
        AgentRegistryIamPolicy,
        AgentRegistryIamPolicyConfig,
    )

    cfg = AgentRegistryIamPolicyConfig(
        location="example",
        policy_data="example",
    )
    ubx.resource(AgentRegistryIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_iap_agent_registry_iam_policy with location,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
