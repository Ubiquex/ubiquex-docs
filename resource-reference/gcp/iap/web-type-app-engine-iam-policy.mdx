---
title: "google_iap_web_type_app_engine_iam_policy"
description: "Real, generated bindings for `google_iap_web_type_app_engine_iam_policy`."
---

`google_iap_web_type_app_engine_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/iap"
    )

    cfg := iap.WebTypeAppEngineIamPolicyConfig{
        AppId: "example",
        PolicyData: "example",
    }
    ubx.Resource(iap.WebTypeAppEngineIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WebTypeAppEngineIamPolicy } from
      "jsr:@ubx/sdk-google/google/iap/web_type_app_engine_iam_policy";

    resource(WebTypeAppEngineIamPolicy, "example", {
      appId: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.iap import (
        WebTypeAppEngineIamPolicy,
        WebTypeAppEngineIamPolicyConfig,
    )

    cfg = WebTypeAppEngineIamPolicyConfig(
        app_id="example",
        policy_data="example",
    )
    ubx.resource(WebTypeAppEngineIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_iap_web_type_app_engine_iam_policy with app_id,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
