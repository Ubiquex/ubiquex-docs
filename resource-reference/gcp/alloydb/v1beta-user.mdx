---
title: "google_alloydb_v1beta_user"
description: "`google_alloydb_v1beta_user` is a database user account on an AlloyDB cluster, authenticated via password or IAM."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_alloydb_user`.
</Warning>

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	alloydb "github.com/ubiquex/ubx-sdk-google/sdk/go/google/alloydb"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta user"})

    		ubx.Resource(alloydb.V1betaUser, "example", alloydb.V1betaUserConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaUser } from "@ubx/sdk-google/google/alloydb/v1beta_user";

    export default stack("example", () => {
      intent({ summary: "example own v1beta user" });

      resource(V1betaUser, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.alloydb.v1beta_user import V1betaUser, V1betaUserConfig

    def describe():
        ubx.intent("example own v1beta user")

        ubx.resource(V1betaUser, "example", V1betaUserConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_alloydb_v1beta_user called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="database_roles" type="list(string)">
  Optional. List of database roles this user has. The database role strings are subject to the PostgreSQL naming conventions. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="keep_extra_roles" type="bool">
  Input only. If the user already exists and it has additional roles, keep them granted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Name of the resource in the form of projects/&#123;project&#125;/locations/&#123;location&#125;/cluster/&#123;cluster&#125;/users/&#123;user&#125;. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="password" type="string">
  Input only. Password for the user. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="user_type" type="string">
  Optional. Type of this user. Optional; if omitted, computed by Google Cloud.
</ResponseField>
