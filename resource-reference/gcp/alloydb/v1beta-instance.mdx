---
title: "google_alloydb_v1beta_instance"
description: "`google_alloydb_v1beta_instance` is a compute instance within an AlloyDB cluster -- either the primary read/write instance or a read pool instance..."
---

`google_alloydb_v1beta_instance` is a compute instance within an AlloyDB cluster -- either the primary read/write instance or a read pool instance serving read-only replica traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	alloydb "github.com/ubiquex/ubx-sdk-google/sdk/go/google/alloydb"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta instance"})

    		ubx.Resource(alloydb.V1betaInstance, "example", alloydb.V1betaInstanceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaInstance } from "@ubx/sdk-google/google/alloydb/v1beta_instance";

    export default stack("example", () => {
      intent({ summary: "example own v1beta instance" });

      resource(V1betaInstance, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.alloydb import V1betaInstance, V1betaInstanceConfig

    def describe():
        ubx.intent("example own v1beta instance")

        ubx.resource(V1betaInstance, "example", V1betaInstanceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_alloydb_v1beta_instance called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="activation_policy" type="string">
  Optional. Specifies whether an instance needs to spin up. Once the instance is active, the activation policy can be updated to the `NEVER` to stop the instance. Likewise, the activation policy can be updated to `ALWAYS` to start the instance. There are restrictions around when an instance can/cannot be activated (for example, a read pool instance should be stopped before stopping primary etc.). Please refer to the API documentation for more details. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="annotations" type="map(string)">
  Annotations to allow client tools to store small amount of arbitrary data. This is distinct from labels. https://google.aip.dev/128 Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="availability_type" type="string">
  Availability type of an Instance. If empty, defaults to REGIONAL for primary instances. For read pools, availability_type is always UNSPECIFIED. Instances in the read pools are evenly distributed across available zones within the region (i.e. read pools with more than one node will have a node in at least two zones). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="client_connection_config" type="object">
  Client connection configuration Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="require_connectors" type="bool">
      Optional. Configuration to enforce connectors only (ex: AuthProxy) connections to the database. Optional.
    </ResponseField>
    <ResponseField name="ssl_config" type="object">
      SSL configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="ca_source" type="string">
          Optional. Certificate Authority (CA) source. Only CA_SOURCE_MANAGED is supported currently, and is the default value. Optional.
        </ResponseField>
        <ResponseField name="ssl_mode" type="string">
          Optional. SSL mode. Specifies client-server SSL/TLS connection behavior. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="connection_pool_config" type="object">
  Configuration for Managed Connection Pool (MCP). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="authproxy_pooler_count" type="number">
      Output only. The number of running AuthProxy poolers per instance. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="authproxy_pooler_scaling_type" type="string">
      Optional. The scaling type of the AuthProxy pooler. Optional.
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      Optional. Whether to enable Managed Connection Pool (MCP). Optional.
    </ResponseField>
    <ResponseField name="flags" type="map(string)">
      Optional. Connection Pool flags, as a list of "key": "value" pairs. Optional.
    </ResponseField>
    <ResponseField name="pooler_count" type="number">
      Output only. The number of running poolers per instance. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="pooler_scaling_type" type="string">
      Optional. The scaling type of the regular pooler. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Create time stamp Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="data_api_access" type="string">
  Optional. Controls whether the Data API is enabled for this instance. When enabled, this allows authorized users to connect to the instance from the public internet using the `executeSql` API, even for private IP instances. If this is not specified, the data API is enabled by default for Google internal services like AlloyDB Studio. Disable it explicitly to disallow Google internal services as well. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="database_flags" type="map(string)">
  Database flags. Set at the instance level. They are copied from the primary instance on secondary instance creation. Flags that have restrictions default to the value at primary instance on read instances during creation. Read instances can set new flags or override existing flags that are relevant for reads, for example, for enabling columnar cache on a read instance. Flags set on read instance might or might not be present on the primary instance. This is a list of "key": "value" pairs. "key": The name of the flag. These flags are passed at instance setup time, so include both server options and system variables for Postgres. Flags are specified with underscores, not hyphens. "value": The value of the flag. Booleans are set to **on** for true and **off** for false. This field must be omitted if the flag doesn't take a value. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. Delete time stamp Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  User-settable and human-readable display name for the Instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  For Resource freshness validation (https://google.aip.dev/154) Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="gca_config" type="object">
  Instance level configuration parameters related to the Gemini Cloud Assist product. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="gca_entitlement" type="string">
      Output only. Represents the GCA entitlement state of the instance. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="gce_zone" type="string">
  The Compute Engine zone that the instance should serve from, per https://cloud.google.com/compute/docs/regions-zones This can ONLY be specified for ZONAL instances. If present for a REGIONAL instance, an error will be thrown. If this is absent for a ZONAL instance, instance is created in a random zone with available capacity. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="gemini_config" type="object">
  Deprecated and unused. This message will be removed in the near future. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="entitled" type="bool">
      Output only. Deprecated and unused. This field will be removed in the near future. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_type" type="string">
  Required. The type of the instance. Specified at creation time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ip_address" type="string">
  Output only. The IP address for the Instance. This is the connection endpoint for an end-user application. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="machine_config" type="object">
  MachineConfig describes the configuration of a machine. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cpu_count" type="number">
      The number of CPU's in the VM instance. Optional.
    </ResponseField>
    <ResponseField name="machine_type" type="string">
      Machine type of the VM instance. E.g. "n2-highmem-4", "n2-highmem-8", "c4a-highmem-4-lssd". cpu_count must match the number of vCPUs in the machine type. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="maintenance_version_name" type="string">
  Output only. Maintenance version of the instance, for example: POSTGRES_15.2025_07_15.04_00. Output only. Update this field via the parent cluster's maintenance_version field(s). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The name of the instance resource with the format: * projects/&#123;project&#125;/locations/&#123;region&#125;/clusters/&#123;cluster_id&#125;/instances/&#123;instance_id&#125; where the cluster and instance ID segments should satisfy the regex expression `[a-z]([a-z0-9-]&#123;0,61&#125;[a-z0-9])?`, e.g. 1-63 characters of lowercase letters, numbers, and dashes, starting with a letter, and ending with a letter or number. For more details see https://google.aip.dev/122. The prefix of the instance resource name is the name of the parent resource: * projects/&#123;project&#125;/locations/&#123;region&#125;/clusters/&#123;cluster_id&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="network_config" type="object">
  Metadata related to instance-level network configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allocated_ip_range_override" type="string">
      Optional. Name of the allocated IP range for the private IP AlloyDB instance, for example: "google-managed-services-default". If set, the instance IPs will be created from this allocated range and will override the IP range used by the parent cluster. The range name must comply with [RFC 1035](https://datatracker.ietf.org/doc/html/rfc1035). Specifically, the name must be 1-63 characters long and match the regular expression [a-z]([-a-z0-9]*[a-z0-9])?. Optional.
    </ResponseField>
    <ResponseField name="authorized_external_networks" type="list(object)">
      Optional. A list of external network authorized to access this instance. Optional.
      <Expandable title="properties">
        <ResponseField name="cidr_range" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enable_outbound_public_ip" type="bool">
      Optional. Enabling an outbound public IP address to support a database server sending requests out into the internet. Optional.
    </ResponseField>
    <ResponseField name="enable_public_ip" type="bool">
      Optional. Enabling public ip for the instance. Optional.
    </ResponseField>
    <ResponseField name="network" type="string">
      Output only. The resource link for the VPC network in which instance resources are created and from which they are accessible via Private IP. This will be the same value as the parent cluster's network. It is specified in the form: // `projects/&#123;project_number&#125;/global/networks/&#123;network_id&#125;`. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="nodes" type="list(object)">
  Output only. List of available read-only VMs in this instance, including the standby for a PRIMARY instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="ip" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="is_hot_standby" type="bool">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="state" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="zone_id" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="observability_config" type="object">
  Observability Instance specific configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="assistive_experiences_enabled" type="bool">
      Whether assistive experiences are enabled for this AlloyDB instance. Optional.
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      Observability feature status for an instance. This flag is turned "off" by default. Optional.
    </ResponseField>
    <ResponseField name="max_query_string_length" type="number">
      Query string length. The default value is 10k. Optional.
    </ResponseField>
    <ResponseField name="preserve_comments" type="bool">
      Preserve comments in query string for an instance. This flag is turned "off" by default. Optional.
    </ResponseField>
    <ResponseField name="query_plans_per_minute" type="number">
      Number of query execution plans captured by Insights per minute for all queries combined. The default value is 200. Any integer between 0 to 200 is considered valid. Optional.
    </ResponseField>
    <ResponseField name="record_application_tags" type="bool">
      Record application tags for an instance. This flag is turned "off" by default. Optional.
    </ResponseField>
    <ResponseField name="track_active_queries" type="bool">
      Track actively running queries on the instance. If not set, this flag is "off" by default. Optional.
    </ResponseField>
    <ResponseField name="track_active_query_plan" type="bool">
      Indicates whether to track active query plans for an instance. If not set, the default value is "off". Can only be enabled if track_active_queries is enabled. Optional.
    </ResponseField>
    <ResponseField name="track_client_address" type="bool">
      Track client address for an instance. If not set, default value is "off". Optional.
    </ResponseField>
    <ResponseField name="track_wait_event_types" type="bool">
      Output only. Track wait event types during query execution for an instance. This flag is turned "on" by default but tracking is enabled only after observability enabled flag is also turned on. This is read-only flag and only modifiable by internal API. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="track_wait_events" type="bool">
      Track wait events during query execution for an instance. This flag is turned "on" by default but tracking is enabled only after observability enabled flag is also turned on. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="outbound_public_ip_addresses" type="list(string)">
  Output only. All outbound public IP addresses configured for the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="psc_instance_config" type="object">
  PscInstanceConfig contains PSC related configuration at an instance level. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allowed_consumer_projects" type="list(string)">
      Optional. List of consumer projects that are allowed to create PSC endpoints to service-attachments to this instance. Optional.
    </ResponseField>
    <ResponseField name="psc_auto_connection_policy_state" type="string">
      Optional. Configuration for setting up PSC auto connection for the instance. Optional.
    </ResponseField>
    <ResponseField name="psc_auto_connections" type="list(object)">
      Optional. Configurations for setting up PSC service automation. Optional.
      <Expandable title="properties">
        <ResponseField name="consumer_network" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="consumer_network_status" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="consumer_project" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="ip_address" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="status" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="psc_auto_dns_state" type="string">
      Optional. Configuration for setting up PSC auto DNS for the instance. Optional.
    </ResponseField>
    <ResponseField name="psc_dns_name" type="string">
      Output only. The DNS name of the instance for PSC connectivity. Name convention: ...alloydb-psc.goog Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="psc_interface_configs" type="list(object)">
      Optional. Configurations for setting up PSC interfaces attached to the instance which are used for outbound connectivity. Only primary instances can have PSC interface attached. Currently we only support 0 or 1 PSC interface. Optional.
      <Expandable title="properties">
        <ResponseField name="network_attachment_resource" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_attachment_link" type="string">
      Output only. The service attachment created when Private Service Connect (PSC) is enabled for the instance. The name of the resource will be in the format of `projects//regions//serviceAttachments/` Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="psc_instance_info" type="object">
  Information about the Private Service Connect (PSC) for the instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="effective_psc_auto_connection_policy" type="bool">
      Output only. Indicates if the PSC auto connection policy is enabled for the instance. For older instances, this will be off by default, but for newer instances, this will be auto-enabled. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="effective_psc_auto_dns_enabled" type="bool">
      Output only. The effective state of the PSC auto DNS for the instance. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="psc_auto_dns_names" type="list(string)">
      Output only. Specifies the auto DNS names for the instance. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="service_connection_policy" type="string">
      Output only. The PSC service connection policy name. The format is "projects//regions//serviceConnectionPolicies/" Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="public_ip_address" type="string">
  Output only. The public IP addresses for the Instance. This is available ONLY when enable_public_ip is set. This is the connection endpoint for an end-user application. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="query_insights_config" type="object">
  QueryInsights Instance specific configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="query_plans_per_minute" type="number">
      Number of query execution plans captured by Insights per minute for all queries combined. The default value is 5. Any integer between 0 and 20 is considered valid. Optional.
    </ResponseField>
    <ResponseField name="query_string_length" type="number">
      Query string length. The default value is 1024. Any integer between 256 and 4500 is considered valid. Optional.
    </ResponseField>
    <ResponseField name="record_application_tags" type="bool">
      Record application tags for an instance. This flag is turned "on" by default. Optional.
    </ResponseField>
    <ResponseField name="record_client_address" type="bool">
      Record client address for an instance. Client address is PII information. This flag is turned "on" by default. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="read_pool_config" type="object">
  Configuration for a read pool instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="auto_scaling_config" type="object">
      Configuration for autoscaling. Optional.
      <Expandable title="properties">
        <ResponseField name="policy" type="object">
          Policy for the autoscaler. Optional.
          <Expandable title="properties">
            <ResponseField name="cool_down_period_sec" type="string">
              The period of time in seconds after a new node is created before the autoscaler will incorporate its resource usage (e.g. CPU utilization) into the autoscaling recommendation algorithm. Optional.
            </ResponseField>
            <ResponseField name="cpu_utilization" type="object">
              CPU utilization policy for the autoscaler. Optional.
              <Expandable title="properties">
                <ResponseField name="utilization_target" type="number">
                  Target CPU utilization as a float between 0 and 1. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="enabled" type="bool">
              If true, autoscaling is enabled for the instance. If not set, the default value is false. Optional.
            </ResponseField>
            <ResponseField name="max_node_count" type="string">
              Maximum number of nodes for the autoscaler. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="schedules" type="list(object)">
          Optional list of schedules for the MIG autoscaler. If not set, no schedules are created. Optional.
          <Expandable title="properties">
            <ResponseField name="cron_expression" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="description" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="disabled" type="bool">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="duration_sec" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="min_node_count" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="time_zone" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="node_count" type="number">
      Read capacity, i.e. number of nodes in a read pool instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. Reconciling (https://google.aip.dev/128#reconciliation). Set to true if the current state of Instance does not match the user's intended state, and the service is actively updating the resource to reconcile them. This can happen due to user-triggered updates or system actions like failover or maintenance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current serving state of the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The system-generated UID of the resource. The UID is assigned when the resource is created, and it is retained until it is deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_policy" type="object">
  Policy to be used while updating the instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="mode" type="string">
      Mode for updating the instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Update time stamp Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="writable_node" type="object">
  Details of a single node in the instance. Nodes in an AlloyDB instance are ephemeral, they can change during update, failover, autohealing and resize operations. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Output only. The identifier of the VM e.g. "test-read-0601-407e52be-ms3l". Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="ip" type="string">
      Output only. The private IP address of the VM e.g. "10.57.0.34". Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="is_hot_standby" type="bool">
      Output only. Indicates whether the node set up to be configured as a hot standby. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. Determined by state of the compute VM and postgres-service health. Compute VM state can have values listed in https://cloud.google.com/compute/docs/instances/instance-life-cycle and postgres-service health can have values: HEALTHY and UNHEALTHY. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="zone_id" type="string">
      Output only. The Compute Engine zone of the VM e.g. "us-central1-b". Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
