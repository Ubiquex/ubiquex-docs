---
title: "google_dataplex_data_product"
description: "`google_dataplex_data_product` is a curated, business-ready package of data asset(s), documentation, quality contracts..."
---

`google_dataplex_data_product` is a curated, business-ready package of data asset(s), documentation, quality contracts, and governance controls assembled in Dataplex to solve a specific business problem, the unit Dataplex's data mesh capability is organized around.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_dataplex --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_dataplex/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataplex "github.com/ubiquex/ubx-sdk-google/sdk/go/google_dataplex/dataplex"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own data product"})

    		ubx.Resource(dataplex.DataplexDataProduct, "example", dataplex.DataplexDataProductConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_dataplex --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataplexDataProduct } from "./local-sdk/google_dataplex/sdk/typescript/google_dataplex/dataplex/dataplex_data_product.ts";

    export default stack("example", () => {
      intent({ summary: "example own data product" });

      resource(DataplexDataProduct, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_dataplex --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_dataplex/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_dataplex.dataplex import DataplexDataProduct, DataplexDataProductConfig

    def describe():
        ubx.intent("example own data product")

        ubx.resource(DataplexDataProduct, "example", DataplexDataProductConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataplex_data_product called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="access_approval_config" type="object">
  Configuration for access approval for the data product. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="approver_emails" type="list(string)">
      Optional. Specifies the email addresses of users who are potential approvers and are notified when an access request is made for the data product. The maximum number of emails allowed is 10. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="access_groups" type="map(object)">
  Optional. Data product access groups by access group id as key. If data product is used only for packaging data assets, then access groups may be empty. However, if a data product is used for sharing data assets, then at least one access group must be specified.Example: &#123; "analyst": &#123; "id": "analyst", "displayName": "Analyst", "description": "Access group for analysts", "principal": &#123; "googleGroup": "analysts@example.com" &#125; &#125; &#125; Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="display_name" type="string">
      A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="string">
      An identifier for this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="principal" type="object">
      The identity this permission grant applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="google_group" type="string">
          A Google Group this permission or contact applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_account" type="string">
          The service account this resource acts as (or is managed by) when calling other Google Cloud APIs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="asset_count" type="number">
  Output only. Number of data assets associated with this data product. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time at which the data product was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the data product. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. User-friendly display name of the data product. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="icon" type="string">
  Optional. Base64 encoded image representing the data product. Max Size: 3.0MiB Expected image dimensions are 512x512 pixels, however the API only performs validation on size of the encoded data. Note: For byte fields, the content of the fields are base64-encoded (which increases the size of the data by 33-36%) when using JSON on the wire. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. User-defined labels for the data product.Example: &#123; "environment": "production", "billing": "marketing-department" &#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Resource name of the data product. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/dataProducts/&#123;data_product_id&#125;. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="owner_emails" type="list(string)">
  Required. Emails of the data product owners. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System generated unique ID for the data product. This ID will be different if the data product is deleted and re-created with the same name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which the data product was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
