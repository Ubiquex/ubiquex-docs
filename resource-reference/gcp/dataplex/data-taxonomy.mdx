---
title: "google_dataplex_data_taxonomy"
description: "`google_dataplex_data_taxonomy` is a hierarchical classification scheme (a tree of attribute nodes) Dataplex uses to tag data with sensitivity or..."
---

`google_dataplex_data_taxonomy` is a hierarchical classification scheme (a tree of attribute nodes) Dataplex uses to tag data with sensitivity or business categories for governance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_dataplex --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_dataplex/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataplex "github.com/ubiquex/ubx-sdk-google/sdk/go/google_dataplex/dataplex"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own data taxonomy"})

    		ubx.Resource(dataplex.DataplexDataTaxonomy, "example", dataplex.DataplexDataTaxonomyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_dataplex --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataplexDataTaxonomy } from "./local-sdk/google_dataplex/sdk/typescript/google_dataplex/dataplex/dataplex_data_taxonomy.ts";

    export default stack("example", () => {
      intent({ summary: "example own data taxonomy" });

      resource(DataplexDataTaxonomy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_dataplex --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_dataplex/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_dataplex.dataplex import DataplexDataTaxonomy, DataplexDataTaxonomyConfig

    def describe():
        ubx.intent("example own data taxonomy")

        ubx.resource(DataplexDataTaxonomy, "example", DataplexDataTaxonomyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataplex_data_taxonomy called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="attribute_count" type="number">
  Output only. The number of attributes in the DataTaxonomy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="class_count" type="number">
  Output only. The number of classes in the DataTaxonomy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the DataTaxonomy was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the DataTaxonomy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. User friendly display name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. User-defined labels for the DataTaxonomy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The relative resource name of the DataTaxonomy, of the form: projects/&#123;project_number&#125;/locations/&#123;location_id&#125;/dataTaxonomies/&#123;data_taxonomy_id&#125;. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System generated globally unique ID for the dataTaxonomy. This ID will be different if the DataTaxonomy is deleted and re-created with the same name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the DataTaxonomy was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
