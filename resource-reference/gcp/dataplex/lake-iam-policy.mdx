---
title: "google_dataplex_lake_iam_policy"
description: "Real, generated bindings for `google_dataplex_lake_iam_policy`."
---

`google_dataplex_lake_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/dataplex"
    )

    cfg := dataplex.LakeIamPolicyConfig{
        Lake: "example",
        PolicyData: "example",
    }
    ubx.Resource(dataplex.LakeIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LakeIamPolicy } from
      "jsr:@ubx/sdk-google/google/dataplex/lake_iam_policy";

    resource(LakeIamPolicy, "example", {
      lake: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.dataplex import (
        LakeIamPolicy,
        LakeIamPolicyConfig,
    )

    cfg = LakeIamPolicyConfig(
        lake="example",
        policy_data="example",
    )
    ubx.resource(LakeIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_dataplex_lake_iam_policy with lake, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="lake" type="string" required>
  Required.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
