---
title: "google_dataplex_task_iam_policy"
description: "Real, generated bindings for `google_dataplex_task_iam_policy`."
---

`google_dataplex_task_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/dataplex"
    )

    cfg := dataplex.TaskIamPolicyConfig{
        Lake: "example",
        PolicyData: "example",
        TaskId: "example",
    }
    ubx.Resource(dataplex.TaskIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TaskIamPolicy } from
      "jsr:@ubx/sdk-google/google/dataplex/task_iam_policy";

    resource(TaskIamPolicy, "example", {
      lake: "example",
      policyData: "example",
      taskId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.dataplex import (
        TaskIamPolicy,
        TaskIamPolicyConfig,
    )

    cfg = TaskIamPolicyConfig(
        lake="example",
        policy_data="example",
        task_id="example",
    )
    ubx.resource(TaskIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_dataplex_task_iam_policy with lake, policy_data,
task_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="lake" type="string" required>
  Required.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="task_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
