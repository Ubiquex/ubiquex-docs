---
title: "google_dataplex_task_iam_policy"
description: "Real, generated bindings for `google_dataplex_task_iam_policy`."
---

`google_dataplex_task_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataplex "github.com/ubiquex/ubx-sdk-google/sdk/go/google/dataplex"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own task iam policy"})

    		ubx.Resource(dataplex.TaskIamPolicy, "ci-runner", dataplex.TaskIamPolicyConfig{
    			Lake:       "example",
    			PolicyData: "example",
    			TaskId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TaskIamPolicy } from "jsr:@ubx/sdk-google/google/dataplex/task_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own task iam policy" });

      resource(TaskIamPolicy, "ci-runner", {
        lake: "example",
        policyData: "example",
        taskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dataplex import TaskIamPolicy, TaskIamPolicyConfig

    def describe():
        ubx.intent("payments own task iam policy")

        ubx.resource(TaskIamPolicy, "ci-runner", TaskIamPolicyConfig(
            lake="example",
            policy_data="example",
            task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataplex_task_iam_policy, with lake example, policy
    data example, task id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="lake" type="string" required>
  Required.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="task_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
