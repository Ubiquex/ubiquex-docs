---
title: "google_dataplex_data_asset"
description: "`google_dataplex_data_asset` is a curated data asset packaged as part of a Dataplex Data Product -- one of the underlying dataset(s), table(s)..."
---

`google_dataplex_data_asset` is a curated data asset packaged as part of a Dataplex Data Product -- one of the underlying dataset(s), table(s), or view(s) that make up a governed, business-ready data offering.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_dataplex --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_dataplex/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataplex "github.com/ubiquex/ubx-sdk-google/sdk/go/google_dataplex/dataplex"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own data asset"})

    		ubx.Resource(dataplex.DataplexDataAsset, "ci-runner", dataplex.DataplexDataAssetConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_dataplex --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataplexDataAsset } from "./local-sdk/google_dataplex/sdk/typescript/google_dataplex/dataplex/dataplex_data_asset.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own data asset" });

      resource(DataplexDataAsset, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_dataplex --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_dataplex/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_dataplex.dataplex import DataplexDataAsset, DataplexDataAssetConfig

    def describe():
        ubx.intent("payments own data asset")

        ubx.resource(DataplexDataAsset, "ci-runner", DataplexDataAssetConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataplex_data_asset called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="access_group_configs" type="map(object)">
  Optional. Access groups configurations for this data asset.The key is DataProduct.AccessGroup.id and the value is AccessGroupConfig.Example: &#123; "analyst": &#123; "iamRoles": ["roles/bigquery.dataViewer"] &#125; &#125; Currently, at most one IAM role is allowed per access group. For providing multiple predefined IAM roles, wrap them in a custom IAM role as per https://cloud.google.com/iam/docs/creating-custom-roles. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="iam_roles" type="list(string)">
      The IAM role(s) granted. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time at which the data asset was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. User-defined labels for the data asset.Example: &#123; "environment": "production", "billing": "marketing-department" &#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Resource name of the data asset. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/dataProducts/&#123;data_product_id&#125;/dataAssets/&#123;data_asset_id&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="resource" type="string">
  Required. Immutable. Full resource name of the cloud resource represented by the data asset. This must follow https://cloud.google.com/iam/docs/full-resource-names. Example: //bigquery.googleapis.com/projects/my_project_123/datasets/dataset_456/tables/table_789 Only BigQuery tables and datasets are currently supported. Data asset creator must have getIamPolicy and setIamPolicy permissions on the resource. Data asset creator must also have resource specific get permission, for instance, bigquery.tables.get for BigQuery tables. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System generated globally unique ID for the data asset. This ID will be different if the data asset is deleted and re-created with the same name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which the data asset was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
