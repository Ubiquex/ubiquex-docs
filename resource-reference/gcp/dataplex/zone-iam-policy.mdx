---
title: "google_dataplex_zone_iam_policy"
description: "Real, generated bindings for `google_dataplex_zone_iam_policy`."
---

`google_dataplex_zone_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataplex "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/dataplex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own zone iam policy"})

    		ubx.Resource(dataplex.ZoneIamPolicy, "ci-runner", dataplex.ZoneIamPolicyConfig{
    			DataplexZone: "example",
    			Lake:         "example",
    			PolicyData:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ZoneIamPolicy } from "jsr:@ubx/sdk-google/google/dataplex/zone_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own zone iam policy" });

      resource(ZoneIamPolicy, "ci-runner", {
        dataplexZone: "example",
        lake: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dataplex import ZoneIamPolicy, ZoneIamPolicyConfig

    def describe():
        ubx.intent("payments own zone iam policy")

        ubx.resource(ZoneIamPolicy, "ci-runner", ZoneIamPolicyConfig(
            dataplex_zone="example",
            lake="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataplex_zone_iam_policy, with dataplex zone
    example, lake example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dataplex_zone" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="lake" type="string" required>
  Required.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
