---
title: "google_workstations_workstation_iam_policy"
description: "Real, generated bindings for `google_workstations_workstation_iam_policy`."
---

`google_workstations_workstation_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workstations "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/workstations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workstation iam policy"})

    		ubx.Resource(workstations.WorkstationIamPolicy, "ci-runner", workstations.WorkstationIamPolicyConfig{
    			PolicyData:           "example",
    			WorkstationClusterId: "example",
    			WorkstationConfigId:  "example",
    			WorkstationId:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkstationIamPolicy } from "jsr:@ubx/sdk-google/google/workstations/workstation_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own workstation iam policy" });

      resource(WorkstationIamPolicy, "ci-runner", {
        policyData: "example",
        workstationClusterId: "example",
        workstationConfigId: "example",
        workstationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.workstations import WorkstationIamPolicy, WorkstationIamPolicyConfig

    def describe():
        ubx.intent("payments own workstation iam policy")

        ubx.resource(WorkstationIamPolicy, "ci-runner", WorkstationIamPolicyConfig(
            policy_data="example",
            workstation_cluster_id="example",
            workstation_config_id="example",
            workstation_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_workstations_workstation_iam_policy, with policy
    data example, workstation cluster id example, workstation config
    id example, workstation id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="workstation_cluster_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="workstation_config_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="workstation_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
