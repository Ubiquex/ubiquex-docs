---
title: "google_workstations_v1beta_workstation_cluster"
description: "`google_workstations_v1beta_workstation_cluster` is the network and infrastructure grouping that Cloud Workstations configuration(s) and their..."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_workstations_workstation_cluster`.
</Warning>

`google_workstations_v1beta_workstation_cluster` is the network and infrastructure grouping that Cloud Workstations configuration(s) and their own workstation(s) are deployed within.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	workstations "github.com/ubiquex/ubx-sdk-google/sdk/go/google/workstations"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta workstation cluster"})

    		ubx.Resource(workstations.V1betaWorkstationCluster, "example", workstations.V1betaWorkstationClusterConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaWorkstationCluster } from "@ubx/sdk-google/google/workstations/v1beta_workstation_cluster";

    export default stack("example", () => {
      intent({ summary: "example own v1beta workstation cluster" });

      resource(V1betaWorkstationCluster, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.workstations.v1beta_workstation_cluster import V1betaWorkstationCluster, V1betaWorkstationClusterConfig

    def describe():
        ubx.intent("example own v1beta workstation cluster")

        ubx.resource(V1betaWorkstationCluster, "example", V1betaWorkstationClusterConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_workstations_v1beta_workstation_cluster called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Optional. Client-specified annotations. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="conditions" type="list(object)">
  Output only. Status conditions describing the workstation cluster's current state. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="details" type="list(map(any))">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="message" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="control_plane_ip" type="string">
  Output only. The private IP address of the control plane for this workstation cluster. Workstation VMs need access to this IP address to work with the service, so make sure that your firewall rules allow egress from the workstation VMs to this address. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Time when this workstation cluster was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="degraded" type="bool">
  Output only. Whether this workstation cluster is in degraded mode, in which case it may require user action to restore full functionality. The conditions field contains detailed information about the status of the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. Time when this workstation cluster was soft-deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Human-readable name for this workstation cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="domain_config" type="object">
  Configuration options for a custom domain. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="domain" type="string">
      Immutable. Domain used by Workstations for HTTP ingress. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. Checksum computed by the server. May be sent on update and delete requests to make sure that the client has an up-to-date value before proceeding. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="gateway_config" type="object">
  Configuration options for Cluster HTTP Gateway. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="http2_enabled" type="bool">
      Optional. Whether HTTP/2 is enabled for this workstation cluster. Defaults to false. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. [Labels](https://cloud.google.com/workstations/docs/label-resources) that are applied to the workstation cluster and that are also propagated to the underlying Compute Engine resources. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Full name of this workstation cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="network" type="string">
  Immutable. Name of the Compute Engine network in which instances associated with this workstation cluster will be created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="private_cluster_config" type="object">
  Configuration options for private workstation clusters. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allowed_projects" type="list(string)">
      Optional. Additional projects that are allowed to attach to the workstation cluster's service attachment. By default, the workstation cluster's project and the VPC host project (if different) are allowed. Optional.
    </ResponseField>
    <ResponseField name="cluster_hostname" type="string">
      Output only. Hostname for the workstation cluster. This field will be populated only when private endpoint is enabled. To access workstations in the workstation cluster, create a new DNS zone mapping this domain name to an internal IP address and a forwarding rule mapping that address to the service attachment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="enable_private_endpoint" type="bool">
      Immutable. Whether Workstations endpoint is private. Optional.
    </ResponseField>
    <ResponseField name="service_attachment_uri" type="string">
      Output only. Service attachment URI for the workstation cluster. The service attachment is created when private endpoint is enabled. To access workstations in the workstation cluster, configure access to the managed service using [Private Service Connect](https://cloud.google.com/vpc/docs/configure-private-service-connect-services). Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. Indicates whether this workstation cluster is currently being updated to match its intended state. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="subnetwork" type="string">
  Immutable. Name of the Compute Engine subnetwork in which instances associated with this workstation cluster will be created. Must be part of the subnetwork specified for this workstation cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. A system-assigned unique identifier for this workstation cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time when this workstation cluster was most recently updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="workstation_authorization_url" type="string">
  Optional. Specifies the redirect URL for unauthorized requests received by workstation VMs in this cluster. Redirects to this endpoint will send a base64 encoded `state` query param containing the target workstation name and original request hostname. The endpoint is responsible for retrieving a token using `GenerateAccessToken` and redirecting back to the original hostname with the token. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="workstation_launch_url" type="string">
  Optional. Specifies the launch URL for workstations in this cluster. Requests sent to unstarted workstations will be redirected to this URL. Requests redirected to the launch endpoint will be sent with a `workstation` and `project` query parameter containing the full workstation resource name and project ID, respectively. The launch endpoint is responsible for starting the workstation, polling it until it reaches `STATE_RUNNING`, and then issuing a redirect to the workstation's host URL. Optional; if omitted, computed by Google Cloud.
</ResponseField>
