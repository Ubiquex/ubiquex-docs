---
title: "google_workstations_workstation_cluster"
description: "Real, generated bindings for `google_workstations_workstation_cluster`."
---

`google_workstations_workstation_cluster` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	workstations "github.com/ubiquex/ubx-sdk-google/sdk/go/google/workstations"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workstation cluster"})

    		ubx.Resource(workstations.WorkstationCluster, "ci-runner", workstations.WorkstationClusterConfig{
    			Network:              "example",
    			Subnetwork:           "example",
    			WorkstationClusterId: "example",
    			Annotations:          map[string]string{"managed-by": "ubx"},
    			DisplayName:          "example-display-name",
    			Labels:               map[string]string{"managed-by": "ubx"},
    			Location:             "example",
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkstationCluster } from "jsr:@ubx/sdk-google/google/workstations/workstation_cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own workstation cluster" });

      resource(WorkstationCluster, "ci-runner", {
        network: "example",
        subnetwork: "example",
        workstationClusterId: "example",
        annotations: { "managed-by": "ubx" },
        displayName: "example-display-name",
        labels: { "managed-by": "ubx" },
        location: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.workstations import WorkstationCluster, WorkstationClusterConfig

    def describe():
        ubx.intent("payments own workstation cluster")

        ubx.resource(WorkstationCluster, "ci-runner", WorkstationClusterConfig(
            network="example",
            subnetwork="example",
            workstation_cluster_id="example",
            annotations={"managed-by": "ubx"},
            display_name="example-display-name",
            labels={"managed-by": "ubx"},
            location="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_workstations_workstation_cluster, with network
    example, subnetwork example, workstation cluster id example,
    annotations a set value, display name example-display-name,
    labels a set value, location example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="annotations" type="map(string)">
  Client-specified annotations. This is distinct from labels. **Note**: This field is non-authoritative, and will only manage the annotations present in your configuration. Please refer to the field 'effective_annotations' for all of the annotations present on the resource. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string">
  Human-readable name for this resource. Optional.
</ResponseField>

<ResponseField name="domain_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="domain" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Client-specified labels that are applied to the resource and that are also propagated to the underlying Compute Engine resources. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string">
  The location where the workstation cluster should reside. Optional.
</ResponseField>

<ResponseField name="network" type="string" required>
  The relative resource name of the VPC network on which the instance can be accessed. It is specified in the following form: "projects/&#123;projectNumber&#125;/global/networks/&#123;network_id&#125;". Required.
</ResponseField>

<ResponseField name="private_cluster_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allowed_projects" type="list(string)">
    </ResponseField>
    <ResponseField name="cluster_hostname" type="string">
    </ResponseField>
    <ResponseField name="enable_private_endpoint" type="bool" required>
    </ResponseField>
    <ResponseField name="service_attachment_uri" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="subnetwork" type="string" required>
  Name of the Compute Engine subnetwork in which instances associated with this cluster will be created. Must be part of the subnetwork specified for this cluster. Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource manager tags bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workstation_authorization_url" type="string">
  Specifies the redirect URL for unauthorized requests received by workstation VMs in this cluster. Redirects to this endpoint will send a base64 encoded 'state' query param containing the target workstation name and original request hostname. The endpoint is responsible for retrieving a token using 'GenerateAccessToken' and redirecting back to the original hostname with the token. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="workstation_cluster_id" type="string" required>
  ID to use for the workstation cluster. Required.
</ResponseField>

<ResponseField name="workstation_launch_url" type="string">
  Specifies the launch URL for workstations in this cluster. Requests sent to unstarted workstations will be redirected to this URL. Requests redirected to the launch endpoint will be sent with a 'workstation' query parameter containing the full workstation resource. The launch endpoint is responsible for starting the workstation, polling it until it reaches 'STATE_RUNNING', and then issuing a redirect to the workstation's host URL. Optional.
</ResponseField>

## Output properties

<ResponseField name="conditions" type="list(object)">
  Status conditions describing the current resource state. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
    </ResponseField>
    <ResponseField name="details" type="list(map(string))">
    </ResponseField>
    <ResponseField name="message" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="control_plane_ip" type="string">
  The private IP address of the control plane for this workstation cluster. Workstation VMs need access to this IP address to work with the service, so make sure that your firewall rules allow egress from the workstation VMs to this address. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Time when this resource was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="degraded" type="bool">
  Whether this resource is in degraded mode, in which case it may require user action to restore full functionality. Details can be found in the conditions field. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="domain_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="domain" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="effective_annotations" type="map(string)">
  All of annotations (key/value pairs) present on the resource in GCP, including the annotations configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Checksum computed by the server. May be sent on update and delete requests to ensure that the client has an up-to-date value before proceeding. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the cluster resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="private_cluster_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allowed_projects" type="list(string)">
    </ResponseField>
    <ResponseField name="cluster_hostname" type="string">
    </ResponseField>
    <ResponseField name="enable_private_endpoint" type="bool" required>
    </ResponseField>
    <ResponseField name="service_attachment_uri" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  The system-generated UID of the resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="workstation_authorization_url" type="string">
  Specifies the redirect URL for unauthorized requests received by workstation VMs in this cluster. Redirects to this endpoint will send a base64 encoded 'state' query param containing the target workstation name and original request hostname. The endpoint is responsible for retrieving a token using 'GenerateAccessToken' and redirecting back to the original hostname with the token. Optional; if omitted, computed by GCP.
</ResponseField>
