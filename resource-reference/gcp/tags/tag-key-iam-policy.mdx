---
title: "google_tags_tag_key_iam_policy"
description: "Real, generated bindings for `google_tags_tag_key_iam_policy`."
---

`google_tags_tag_key_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/tags"
    )

    cfg := tags.TagKeyIamPolicyConfig{
        PolicyData: "example",
        TagKey: "example",
    }
    ubx.Resource(tags.TagKeyIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TagKeyIamPolicy } from
      "jsr:@ubx/sdk-google/google/tags/tag_key_iam_policy";

    resource(TagKeyIamPolicy, "example", {
      policyData: "example",
      tagKey: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.tags import (
        TagKeyIamPolicy,
        TagKeyIamPolicyConfig,
    )

    cfg = TagKeyIamPolicyConfig(
        policy_data="example",
        tag_key="example",
    )
    ubx.resource(TagKeyIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_tags_tag_key_iam_policy with policy_data,
tag_key.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tag_key" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
