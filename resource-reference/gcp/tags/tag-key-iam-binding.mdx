---
title: "google_tags_tag_key_iam_binding"
description: "Real, generated bindings for `google_tags_tag_key_iam_binding`."
---

`google_tags_tag_key_iam_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tags "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/tags"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tag key iam binding"})

    		ubx.Resource(tags.TagKeyIamBinding, "ci-runner", tags.TagKeyIamBindingConfig{
    			Members: []string{"example"},
    			Role:    "example",
    			TagKey:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TagKeyIamBinding } from "jsr:@ubx/sdk-google/google/tags/tag_key_iam_binding";

    export default stack("payments", () => {
      intent({ summary: "payments own tag key iam binding" });

      resource(TagKeyIamBinding, "ci-runner", {
        members: ["example"],
        role: "example",
        tagKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.tags import TagKeyIamBinding, TagKeyIamBindingConfig

    def describe():
        ubx.intent("payments own tag key iam binding")

        ubx.resource(TagKeyIamBinding, "ci-runner", TagKeyIamBindingConfig(
            members=["example"],
            role="example",
            tag_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_tags_tag_key_iam_binding, with members a set value,
    role example, tag key example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="members" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tag_key" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
