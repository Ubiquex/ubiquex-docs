---
title: "google_tags_tag_value_iam_policy"
description: "Real, generated bindings for `google_tags_tag_value_iam_policy`."
---

`google_tags_tag_value_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tags "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/tags"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tag value iam policy"})

    		ubx.Resource(tags.TagValueIamPolicy, "ci-runner", tags.TagValueIamPolicyConfig{
    			PolicyData: "example",
    			TagValue:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TagValueIamPolicy } from "jsr:@ubx/sdk-google/google/tags/tag_value_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own tag value iam policy" });

      resource(TagValueIamPolicy, "ci-runner", {
        policyData: "example",
        tagValue: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.tags import TagValueIamPolicy, TagValueIamPolicyConfig

    def describe():
        ubx.intent("payments own tag value iam policy")

        ubx.resource(TagValueIamPolicy, "ci-runner", TagValueIamPolicyConfig(
            policy_data="example",
            tag_value="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_tags_tag_value_iam_policy, with policy data
    example, tag value example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tag_value" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
