---
title: "google_tags_tag_value_iam_policy"
description: "Real, generated bindings for `google_tags_tag_value_iam_policy`."
---

`google_tags_tag_value_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/tags"
    )

    cfg := tags.TagValueIamPolicyConfig{
        PolicyData: "example",
        TagValue: "example",
    }
    ubx.Resource(tags.TagValueIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TagValueIamPolicy } from
      "jsr:@ubx/sdk-google/google/tags/tag_value_iam_policy";

    resource(TagValueIamPolicy, "example", {
      policyData: "example",
      tagValue: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.tags import (
        TagValueIamPolicy,
        TagValueIamPolicyConfig,
    )

    cfg = TagValueIamPolicyConfig(
        policy_data="example",
        tag_value="example",
    )
    ubx.resource(TagValueIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_tags_tag_value_iam_policy with policy_data,
tag_value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tag_value" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
