---
title: "google_tags_tag_key_iam_member"
description: "Real, generated bindings for `google_tags_tag_key_iam_member`."
---

`google_tags_tag_key_iam_member` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tags "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/tags"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tag key iam member"})

    		ubx.Resource(tags.TagKeyIamMember, "ci-runner", tags.TagKeyIamMemberConfig{
    			Member: "example",
    			Role:   "example",
    			TagKey: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TagKeyIamMember } from "jsr:@ubx/sdk-google/google/tags/tag_key_iam_member";

    export default stack("payments", () => {
      intent({ summary: "payments own tag key iam member" });

      resource(TagKeyIamMember, "ci-runner", {
        member: "example",
        role: "example",
        tagKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.tags import TagKeyIamMember, TagKeyIamMemberConfig

    def describe():
        ubx.intent("payments own tag key iam member")

        ubx.resource(TagKeyIamMember, "ci-runner", TagKeyIamMemberConfig(
            member="example",
            role="example",
            tag_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_tags_tag_key_iam_member, with member example, role
    example, tag key example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="member" type="string" required>
  Required.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tag_key" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
