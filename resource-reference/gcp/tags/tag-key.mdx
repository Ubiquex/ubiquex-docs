---
title: "google_tags_tag_key"
description: "Real, generated bindings for `google_tags_tag_key`."
---

`google_tags_tag_key` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	tags "github.com/ubiquex/ubx-sdk-google/sdk/go/google/tags"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tag key"})

    		ubx.Resource(tags.TagKey, "ci-runner", tags.TagKeyConfig{
    			Parent:             "example",
    			ShortName:          "example-short-name",
    			AllowedValuesRegex: "example",
    			Description:        "Managed by ubx.",
    			Purpose:            "example",
    			PurposeData:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TagKey } from "jsr:@ubx/sdk-google/google/tags/tag_key";

    export default stack("payments", () => {
      intent({ summary: "payments own tag key" });

      resource(TagKey, "ci-runner", {
        parent: "example",
        shortName: "example-short-name",
        allowedValuesRegex: "example",
        description: "Managed by ubx.",
        purpose: "example",
        purposeData: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.tags import TagKey, TagKeyConfig

    def describe():
        ubx.intent("payments own tag key")

        ubx.resource(TagKey, "ci-runner", TagKeyConfig(
            parent="example",
            short_name="example-short-name",
            allowed_values_regex="example",
            description="Managed by ubx.",
            purpose="example",
            purpose_data={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_tags_tag_key, with parent example, short name
    example-short-name, allowed values regex example, description
    Managed by ubx., purpose example, purpose data a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allowed_values_regex" type="string">
  Regular expression constraint for dynamic tag values, follows RE2 syntax. If present, it implicitly allows dynamic values (constrained by the regex). Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  User-assigned description of the TagKey. Must not exceed 256 characters. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent" type="string" required>
  Input only. The resource name of the new TagKey's parent. Must be of the form organizations/&#123;org_id&#125; or projects/&#123;project_id_or_number&#125;. Required.
</ResponseField>

<ResponseField name="purpose" type="string">
  Optional. A purpose cannot be changed once set. A purpose denotes that this Tag is intended for use in policies of a specific policy engine, and will involve that policy engine in management operations involving this Tag. Possible values: ["GCE_FIREWALL", "DATA_GOVERNANCE"] Optional.
</ResponseField>

<ResponseField name="purpose_data" type="map(string)">
  Optional. Purpose data cannot be changed once set. Purpose data corresponds to the policy system that the tag is intended for. For example, the GCE_FIREWALL purpose expects data in the following format: 'network = "&lt;project-name&gt;/&lt;vpc-name&gt;"'. Optional.
</ResponseField>

<ResponseField name="short_name" type="string" required>
  Input only. The user friendly name for a TagKey. The short name should be unique for TagKeys within the same tag namespace. The short name can have a maximum length of 256 characters. The permitted character set for the shortName includes all UTF-8 encoded Unicode characters except single quotes ('), double quotes ("), backslashes (\\), and forward slashes (/). Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Output only. Creation time. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The generated numeric id for the TagKey. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="namespaced_name" type="string">
  Output only. Namespaced name of the TagKey. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Update time. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>
