---
title: "google_apigee_endpoint_attachment"
description: "Real, generated bindings for `google_apigee_endpoint_attachment`."
---

`google_apigee_endpoint_attachment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint attachment"})

    		ubx.Resource(apigee.EndpointAttachment, "ci-runner", apigee.EndpointAttachmentConfig{
    			EndpointAttachmentId: "example",
    			Location:             "example",
    			OrgId:                "example",
    			ServiceAttachment:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointAttachment } from "jsr:@ubx/sdk-google/google/apigee/endpoint_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint attachment" });

      resource(EndpointAttachment, "ci-runner", {
        endpointAttachmentId: "example",
        location: "example",
        orgId: "example",
        serviceAttachment: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import EndpointAttachment, EndpointAttachmentConfig

    def describe():
        ubx.intent("payments own endpoint attachment")

        ubx.resource(EndpointAttachment, "ci-runner", EndpointAttachmentConfig(
            endpoint_attachment_id="example",
            location="example",
            org_id="example",
            service_attachment="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_endpoint_attachment, with endpoint
    attachment id example, location example, org id example, service
    attachment example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="endpoint_attachment_id" type="string" required>
  ID of the endpoint attachment. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location of the endpoint attachment. Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  The Apigee Organization associated with the Apigee instance, in the format 'organizations/&#123;&#123;org_name&#125;&#125;'. Required.
</ResponseField>

<ResponseField name="service_attachment" type="string" required>
  The resource URL of the service attachment in the format: 'projects/*/regions/*/serviceAttachments/*'. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="connection_state" type="string">
  State of the endpoint attachment connection to the service attachment. Possible values are: 'CONNECTION_STATE_UNSPECIFIED', 'PENDING', 'ACCEPTED', 'REJECTED', 'CLOSED'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="host" type="string">
  Host that can be used in either HTTP Target Endpoint directly, or as the host in Target Server. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the Endpoint Attachment in the following format: organizations/&#123;organization&#125;/endpointAttachments/&#123;endpointAttachment&#125;. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
