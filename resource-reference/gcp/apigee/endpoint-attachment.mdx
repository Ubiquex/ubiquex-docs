---
title: "google_apigee_endpoint_attachment"
description: "Real, generated bindings for `google_apigee_endpoint_attachment`."
---

`google_apigee_endpoint_attachment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigee "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/apigee"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint attachment"})

    		ubx.Resource(apigee.EndpointAttachment, "ci-runner", apigee.EndpointAttachmentConfig{
    			EndpointAttachmentId: "example",
    			Location:             "example",
    			OrgId:                "example",
    			ServiceAttachment:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointAttachment } from "jsr:@ubx/sdk-google/google/apigee/endpoint_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint attachment" });

      resource(EndpointAttachment, "ci-runner", {
        endpointAttachmentId: "example",
        location: "example",
        orgId: "example",
        serviceAttachment: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import EndpointAttachment, EndpointAttachmentConfig

    def describe():
        ubx.intent("payments own endpoint attachment")

        ubx.resource(EndpointAttachment, "ci-runner", EndpointAttachmentConfig(
            endpoint_attachment_id="example",
            location="example",
            org_id="example",
            service_attachment="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_endpoint_attachment, with endpoint
    attachment id example, location example, org id example, service
    attachment example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="endpoint_attachment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_attachment" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="connection_state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="host" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
