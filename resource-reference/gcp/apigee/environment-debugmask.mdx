---
title: "google_apigee_environment_debugmask"
description: "Real, generated bindings for `google_apigee_environment_debugmask`."
---

`google_apigee_environment_debugmask` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own environment debugmask"})

    		ubx.Resource(apigee.EnvironmentDebugmask, "ci-runner", apigee.EnvironmentDebugmaskConfig{
    			EnvId:             "example",
    			FaultXPaths:       []string{"example"},
    			Namespaces:        map[string]string{"managed-by": "ubx"},
    			RequestJsonPaths:  []string{"example"},
    			RequestXPaths:     []string{"example"},
    			ResponseJsonPaths: []string{"example"},
    			ResponseXPaths:    []string{"example"},
    			Variables:         []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnvironmentDebugmask } from "jsr:@ubx/sdk-google/google/apigee/environment_debugmask";

    export default stack("payments", () => {
      intent({ summary: "payments own environment debugmask" });

      resource(EnvironmentDebugmask, "ci-runner", {
        envId: "example",
        faultXPaths: ["example"],
        namespaces: { "managed-by": "ubx" },
        requestJsonPaths: ["example"],
        requestXPaths: ["example"],
        responseJsonPaths: ["example"],
        responseXPaths: ["example"],
        variables: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import EnvironmentDebugmask, EnvironmentDebugmaskConfig

    def describe():
        ubx.intent("payments own environment debugmask")

        ubx.resource(EnvironmentDebugmask, "ci-runner", EnvironmentDebugmaskConfig(
            env_id="example",
            fault_x_paths=["example"],
            namespaces={"managed-by": "ubx"},
            request_json_paths=["example"],
            request_x_paths=["example"],
            response_json_paths=["example"],
            response_x_paths=["example"],
            variables=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_environment_debugmask, with env id example,
    fault x paths a set value, namespaces a set value, request json
    paths a set value, request x paths a set value, response json
    paths a set value, response x paths a set value, variables a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="env_id" type="string" required>
  The Apigee environment group associated with the Apigee environment, in the format organizations/&#123;&#123;org_name&#125;&#125;/environments/&#123;&#123;env_name&#125;&#125;. Required.
</ResponseField>

<ResponseField name="fault_x_paths" type="list(string)">
  List of XPath expressions that specify the XML elements or attributes that the debug mask applies to for fault messages. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="namespaces" type="map(string)">
  Map of namespaces to URIs. Optional.
</ResponseField>

<ResponseField name="request_json_paths" type="list(string)">
  List of JSONPath expressions that specify the JSON elements or attributes that the debug mask applies to for request messages. Optional.
</ResponseField>

<ResponseField name="request_x_paths" type="list(string)">
  List of XPath expressions that specify the XML elements or attributes that the debug mask applies to for request messages. Optional.
</ResponseField>

<ResponseField name="response_json_paths" type="list(string)">
  List of JSONPath expressions that specify the JSON elements or attributes that the debug mask applies to for response messages. Optional.
</ResponseField>

<ResponseField name="response_x_paths" type="list(string)">
  List of XPath expressions that specify the XML elements or attributes that the debug mask applies to for response messages. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="variables" type="list(string)">
  List of variables that the debug mask applies to. Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the debug mask. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
