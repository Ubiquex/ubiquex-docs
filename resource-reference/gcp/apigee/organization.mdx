---
title: "google_apigee_organization"
description: "Real, generated bindings for `google_apigee_organization`."
---

`google_apigee_organization` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organization"})

    		ubx.Resource(apigee.Organization, "ci-runner", apigee.OrganizationConfig{
    			ProjectId:                        "example",
    			AnalyticsRegion:                  "example",
    			ApiConsumerDataEncryptionKeyName: "example-api-consumer-data-encryption-key-name",
    			ApiConsumerDataLocation:          "example",
    			AuthorizedNetwork:                "example",
    			ControlPlaneEncryptionKeyName:    "example-control-plane-encryption-key-name",
    			Description:                      "Managed by ubx.",
    			DisableVpcPeering:                true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Organization } from "jsr:@ubx/sdk-google/google/apigee/organization";

    export default stack("payments", () => {
      intent({ summary: "payments own organization" });

      resource(Organization, "ci-runner", {
        projectId: "example",
        analyticsRegion: "example",
        apiConsumerDataEncryptionKeyName:
          "example-api-consumer-data-encryption-key-name",
        apiConsumerDataLocation: "example",
        authorizedNetwork: "example",
        controlPlaneEncryptionKeyName: "example-control-plane-encryption-key-name",
        description: "Managed by ubx.",
        disableVpcPeering: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import Organization, OrganizationConfig

    def describe():
        ubx.intent("payments own organization")

        ubx.resource(Organization, "ci-runner", OrganizationConfig(
            project_id="example",
            analytics_region="example",
            api_consumer_data_encryption_key_name="example-api-consumer-data-encryption-key-name",
            api_consumer_data_location="example",
            authorized_network="example",
            control_plane_encryption_key_name="example-control-plane-encryption-key-name",
            description="Managed by ubx.",
            disable_vpc_peering=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_organization, with project id example,
    analytics region example, api consumer data encryption key name
    example-api-consumer-data-encryption-key-name, api consumer data
    location example, authorized network example, control plane
    encryption key name example-control-plane-encryption-key-name,
    description Managed by ubx., disable vpc peering true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="analytics_region" type="string">
  Primary GCP region for analytics data storage. For valid values, see [Create an Apigee organization](https://cloud.google.com/apigee/docs/api-platform/get-started/create-org). Optional.
</ResponseField>

<ResponseField name="api_consumer_data_encryption_key_name" type="string">
  Cloud KMS key name used for encrypting API consumer data. Optional.
</ResponseField>

<ResponseField name="api_consumer_data_location" type="string">
  This field is needed only for customers using non-default data residency regions. Apigee stores some control plane data only in single region. This field determines which single region Apigee should use. Optional.
</ResponseField>

<ResponseField name="authorized_network" type="string">
  Compute Engine network used for Service Networking to be peered with Apigee runtime instances. See [Getting started with the Service Networking API](https://cloud.google.com/service-infrastructure/docs/service-networking/getting-started). Valid only when 'RuntimeType' is set to CLOUD. The value can be updated only when there are no runtime instances. For example: "default". Optional.
</ResponseField>

<ResponseField name="billing_type" type="string">
  Billing type of the Apigee organization. See [Apigee pricing](https://cloud.google.com/apigee/pricing). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="control_plane_encryption_key_name" type="string">
  Cloud KMS key name used for encrypting control plane data that is stored in a multi region. Only used for the data residency region "US" or "EU". Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Description of the Apigee organization. Optional.
</ResponseField>

<ResponseField name="disable_vpc_peering" type="bool">
  Flag that specifies whether the VPC Peering through Private Google Access should be disabled between the consumer network and Apigee. Required if an 'authorizedNetwork' on the consumer project is not provided, in which case the flag should be set to 'true'. Valid only when 'RuntimeType' is set to CLOUD. The value must be set before the creation of any Apigee runtime instance and can be updated only when there are no runtime instances. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name of the Apigee organization. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project_id" type="string" required>
  The project ID associated with the Apigee organization. Required.
</ResponseField>

<ResponseField name="properties" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="property" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="retention" type="string">
  Optional. This setting is applicable only for organizations that are soft-deleted (i.e., BillingType is not EVALUATION). It controls how long Organization data will be retained after the initial delete operation completes. During this period, the Organization may be restored to its last known state. After this period, the Organization will no longer be able to be restored. Default value: "DELETION_RETENTION_UNSPECIFIED" Possible values: ["DELETION_RETENTION_UNSPECIFIED", "MINIMUM"] Optional.
</ResponseField>

<ResponseField name="runtime_database_encryption_key_name" type="string">
  Cloud KMS key name used for encrypting the data that is stored and replicated across runtime instances. Update is not allowed after the organization is created. If not specified, a Google-Managed encryption key will be used. Valid only when 'RuntimeType' is CLOUD. For example: 'projects/foo/locations/us/keyRings/bar/cryptoKeys/baz'. Optional.
</ResponseField>

<ResponseField name="runtime_type" type="string">
  Runtime type of the Apigee organization based on the Apigee subscription purchased. Default value: "CLOUD" Possible values: ["CLOUD", "HYBRID"] Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="apigee_project_id" type="string">
  Output only. Project ID of the Apigee Tenant Project. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="billing_type" type="string">
  Billing type of the Apigee organization. See [Apigee pricing](https://cloud.google.com/apigee/pricing). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ca_certificate" type="string">
  Output only. Base64-encoded public certificate for the root CA of the Apigee organization. Valid only when 'RuntimeType' is CLOUD. A base64-encoded string. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Name of the Apigee organization. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="properties" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="property" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="subscription_type" type="string">
  Output only. Subscription type of the Apigee organization. Valid values include trial (free, limited, and for evaluation purposes only) or paid (full subscription has been purchased). Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
