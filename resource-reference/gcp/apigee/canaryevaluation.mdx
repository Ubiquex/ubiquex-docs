---
title: "google_apigee_canaryevaluation"
description: "`google_apigee_canaryevaluation` evaluates the real-time health impact of a runtime change rolled out to a small..."
---

`google_apigee_canaryevaluation` evaluates the real-time health impact of a runtime change rolled out to a small, canary subset of an Apigee instance's own message processors before Apigee decides whether to continue rolling it out further.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_apigee --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_apigee/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google_apigee/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own canaryevaluation"})

    		ubx.Resource(apigee.ApigeeCanaryevaluation, "ci-runner", apigee.ApigeeCanaryevaluationConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_apigee --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApigeeCanaryevaluation } from "./local-sdk/google_apigee/sdk/typescript/google_apigee/apigee/apigee_canaryevaluation.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own canaryevaluation" });

      resource(ApigeeCanaryevaluation, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_apigee --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_apigee/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_apigee.apigee import ApigeeCanaryevaluation, ApigeeCanaryevaluationConfig

    def describe():
        ubx.intent("payments own canaryevaluation")

        ubx.resource(ApigeeCanaryevaluation, "ci-runner", ApigeeCanaryevaluationConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_canaryevaluation called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="control" type="string">
  Required. The stable version that is serving requests. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Create time of the canary evaluation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="end_time" type="string">
  Required. End time for the evaluation's analysis. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="metric_labels" type="object">
  Labels that can be used to filter Apigee metrics. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="env" type="string">
      The environment ID associated with the metrics. Optional.
    </ResponseField>
    <ResponseField name="instance_id" type="string">
      Required. The instance ID associated with the metrics. In Apigee Hybrid, the value is configured during installation. Optional.
    </ResponseField>
    <ResponseField name="location" type="string">
      Required. The location associated with the metrics. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Name of the canary evalution. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="start_time" type="string">
  Required. Start time for the canary evaluation's analysis. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the canary evaluation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="treatment" type="string">
  Required. The newer version that is serving requests. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="verdict" type="string">
  Output only. The resulting verdict of the canary evaluations: NONE, PASS, or FAIL. Optional; if omitted, computed by Google Cloud.
</ResponseField>
