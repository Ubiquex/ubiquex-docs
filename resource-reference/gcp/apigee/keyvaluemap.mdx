---
title: "google_apigee_keyvaluemap"
description: "`google_apigee_keyvaluemap` is a named, persistent key/value store Apigee proxy policy logic reads and writes at runtime..."
---

`google_apigee_keyvaluemap` is a named, persistent key/value store Apigee proxy policy logic reads and writes at runtime, for caching or configuration data outside the proxy bundle itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own keyvaluemap"})

    		ubx.Resource(apigee.Keyvaluemap, "example", apigee.KeyvaluemapConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Keyvaluemap } from "@ubx/sdk-google/google/apigee/keyvaluemap";

    export default stack("example", () => {
      intent({ summary: "example own keyvaluemap" });

      resource(Keyvaluemap, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee.keyvaluemap import Keyvaluemap, KeyvaluemapConfig

    def describe():
        ubx.intent("example own keyvaluemap")

        ubx.resource(Keyvaluemap, "example", KeyvaluemapConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_keyvaluemap called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="encrypted" type="bool">
  Required. Flag that specifies whether entry values will be encrypted. This field is retained for backward compatibility and the value of encrypted will always be `true`. Apigee X and hybrid do not support unencrypted key value maps. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="masked_values" type="bool">
  Optional. Flag that specifies whether entry values will be masked when returned. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. ID of the key value map. Optional; if omitted, computed by Google Cloud.
</ResponseField>
