---
title: "google_apigee_keyvaluemap"
description: "`google_apigee_keyvaluemap` is a named, persistent key/value store Apigee proxy policy logic reads and writes at runtime..."
---

`google_apigee_keyvaluemap` is a named, persistent key/value store Apigee proxy policy logic reads and writes at runtime, for caching or configuration data outside the proxy bundle itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_apigee --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_apigee/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google_apigee/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own keyvaluemap"})

    		ubx.Resource(apigee.ApigeeKeyvaluemap, "ci-runner", apigee.ApigeeKeyvaluemapConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_apigee --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApigeeKeyvaluemap } from "./local-sdk/google_apigee/sdk/typescript/google_apigee/apigee/apigee_keyvaluemap.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own keyvaluemap" });

      resource(ApigeeKeyvaluemap, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_apigee --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_apigee/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_apigee.apigee import ApigeeKeyvaluemap, ApigeeKeyvaluemapConfig

    def describe():
        ubx.intent("payments own keyvaluemap")

        ubx.resource(ApigeeKeyvaluemap, "ci-runner", ApigeeKeyvaluemapConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_keyvaluemap called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="encrypted" type="bool">
  Required. Flag that specifies whether entry values will be encrypted. This field is retained for backward compatibility and the value of encrypted will always be `true`. Apigee X and hybrid do not support unencrypted key value maps. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="masked_values" type="bool">
  Optional. Flag that specifies whether entry values will be masked when returned. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. ID of the key value map. Optional; if omitted, computed by Google Cloud.
</ResponseField>
