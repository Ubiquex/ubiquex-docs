---
title: "google_apigee_api_deployment"
description: "Real, generated bindings for `google_apigee_api_deployment`."
---

`google_apigee_api_deployment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own api deployment"})

    		ubx.Resource(apigee.ApiDeployment, "ci-runner", apigee.ApiDeploymentConfig{
    			Environment: "example",
    			OrgId:       "example",
    			ProxyId:     "example",
    			Revision:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApiDeployment } from "jsr:@ubx/sdk-google/google/apigee/api_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own api deployment" });

      resource(ApiDeployment, "ci-runner", {
        environment: "example",
        orgId: "example",
        proxyId: "example",
        revision: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import ApiDeployment, ApiDeploymentConfig

    def describe():
        ubx.intent("payments own api deployment")

        ubx.resource(ApiDeployment, "ci-runner", ApiDeploymentConfig(
            environment="example",
            org_id="example",
            proxy_id="example",
            revision="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_api_deployment, with environment example,
    org id example, proxy id example, revision example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="environment" type="string" required>
  Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="proxy_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="revision" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
