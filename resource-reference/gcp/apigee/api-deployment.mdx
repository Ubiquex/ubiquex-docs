---
title: "google_apigee_api_deployment"
description: "Real, generated bindings for `google_apigee_api_deployment`."
---

`google_apigee_api_deployment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own api deployment"})

    		ubx.Resource(apigee.ApiDeployment, "ci-runner", apigee.ApiDeploymentConfig{
    			Environment: "example",
    			OrgId:       "example",
    			ProxyId:     "example",
    			Revision:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApiDeployment } from "jsr:@ubx/sdk-google/google/apigee/api_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own api deployment" });

      resource(ApiDeployment, "ci-runner", {
        environment: "example",
        orgId: "example",
        proxyId: "example",
        revision: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import ApiDeployment, ApiDeploymentConfig

    def describe():
        ubx.intent("payments own api deployment")

        ubx.resource(ApiDeployment, "ci-runner", ApiDeploymentConfig(
            environment="example",
            org_id="example",
            proxy_id="example",
            revision="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_api_deployment, with environment example,
    org id example, proxy id example, revision example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="environment" type="string" required>
  The Apigee Environment associated with the Apigee API deployment. Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  The Apigee Organization associated with the Apigee API deployment. Required.
</ResponseField>

<ResponseField name="proxy_id" type="string" required>
  The Apigee API associated with the Apigee API deployment. Required.
</ResponseField>

<ResponseField name="revision" type="string" required>
  The revision of the API proxy to be deployed. Required.
</ResponseField>

<ResponseField name="service_account" type="string">
  The Google Cloud IAM service account to use as the identity for the deployed proxy. The format must be '&#123;ACCOUNT_ID&#125;@&#123;PROJECT&#125;.iam.gserviceaccount.com'. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  The ID of the API deployment in the format 'organizations/&#123;&#123;org_id&#125;&#125;/environments/&#123;&#123;environment&#125;&#125;/apis/&#123;&#123;proxy_id&#125;&#125;/revisions/&#123;&#123;revision&#125;&#125;/deployments'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
