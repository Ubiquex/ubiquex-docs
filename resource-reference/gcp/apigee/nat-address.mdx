---
title: "google_apigee_nat_address"
description: "Real, generated bindings for `google_apigee_nat_address`."
---

`google_apigee_nat_address` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/apigee"
    )

    cfg := apigee.NatAddressConfig{
        InstanceId: "example",
        Name: "example-name",
        Activate: true,
    }
    ubx.Resource(apigee.NatAddress, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { NatAddress } from
      "jsr:@ubx/sdk-google/google/apigee/nat_address";

    resource(NatAddress, "example", {
      instanceId: "example",
      name: "example-name",
      activate: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.apigee import NatAddress, NatAddressConfig

    cfg = NatAddressConfig(
        instance_id="example",
        name="example-name",
        activate=True,
    )
    ubx.resource(NatAddress, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_apigee_nat_address with instance_id, name,
activate.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="activate" type="bool">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_address" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
