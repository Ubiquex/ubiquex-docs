---
title: "google_apigee_space"
description: "`google_apigee_space` creates an identity-scoped isolation boundary within an Apigee organization, letting different teams manage their own proxies..."
---

`google_apigee_space` creates an identity-scoped isolation boundary within an Apigee organization, letting different teams manage their own proxies, products, and shared flows under separate IAM control without needing a fully separate organization.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_apigee --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_apigee/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google_apigee/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own space"})

    		ubx.Resource(apigee.ApigeeSpace, "ci-runner", apigee.ApigeeSpaceConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_apigee --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApigeeSpace } from "./local-sdk/google_apigee/sdk/typescript/google_apigee/apigee/apigee_space.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own space" });

      resource(ApigeeSpace, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_apigee --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_apigee/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_apigee.apigee import ApigeeSpace, ApigeeSpaceConfig

    def describe():
        ubx.intent("payments own space")

        ubx.resource(ApigeeSpace, "ci-runner", ApigeeSpaceConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_space called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Create timestamp of the space. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Display name of the space. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. Id of the space. This field is used as the resource name, and must follow [AIP-122](https://google.aip.dev/122) guidelines. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Last modified timestamp of the space. Optional; if omitted, computed by Google Cloud.
</ResponseField>
