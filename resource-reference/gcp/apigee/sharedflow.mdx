---
title: "google_apigee_sharedflow"
description: "Real, generated bindings for `google_apigee_sharedflow`."
---

`google_apigee_sharedflow` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigee "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/apigee"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sharedflow"})

    		ubx.Resource(apigee.Sharedflow, "ci-runner", apigee.SharedflowConfig{
    			ConfigBundle:  "example",
    			Name:          "ci-runner",
    			OrgId:         "example",
    			DetectMd5hash: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Sharedflow } from "jsr:@ubx/sdk-google/google/apigee/sharedflow";

    export default stack("payments", () => {
      intent({ summary: "payments own sharedflow" });

      resource(Sharedflow, "ci-runner", {
        configBundle: "example",
        name: "ci-runner",
        orgId: "example",
        detectMd5hash: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import Sharedflow, SharedflowConfig

    def describe():
        ubx.intent("payments own sharedflow")

        ubx.resource(Sharedflow, "ci-runner", SharedflowConfig(
            config_bundle="example",
            name="ci-runner",
            org_id="example",
            detect_md5hash="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_sharedflow called "ci-runner", with config
    bundle example, org id example, detect md5hash example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="config_bundle" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="detect_md5hash" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="latest_revision_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="md5hash" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="meta_data" type="list(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
    </ResponseField>
    <ResponseField name="sub_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="revision" type="list(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
