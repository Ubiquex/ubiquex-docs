---
title: "google_apigee_sharedflow_deployment"
description: "Real, generated bindings for `google_apigee_sharedflow_deployment`."
---

`google_apigee_sharedflow_deployment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sharedflow deployment"})

    		ubx.Resource(apigee.SharedflowDeployment, "ci-runner", apigee.SharedflowDeploymentConfig{
    			Environment:    "example",
    			OrgId:          "example",
    			Revision:       "example",
    			SharedflowId:   "example",
    			ServiceAccount: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SharedflowDeployment } from "jsr:@ubx/sdk-google/google/apigee/sharedflow_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own sharedflow deployment" });

      resource(SharedflowDeployment, "ci-runner", {
        environment: "example",
        orgId: "example",
        revision: "example",
        sharedflowId: "example",
        serviceAccount: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import SharedflowDeployment, SharedflowDeploymentConfig

    def describe():
        ubx.intent("payments own sharedflow deployment")

        ubx.resource(SharedflowDeployment, "ci-runner", SharedflowDeploymentConfig(
            environment="example",
            org_id="example",
            revision="example",
            sharedflow_id="example",
            service_account="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_sharedflow_deployment, with environment
    example, org id example, revision example, sharedflow id example,
    service account example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="environment" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="revision" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_account" type="string">
  Optional.
</ResponseField>

<ResponseField name="sharedflow_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
