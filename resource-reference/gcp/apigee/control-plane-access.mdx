---
title: "google_apigee_control_plane_access"
description: "Real, generated bindings for `google_apigee_control_plane_access`."
---

`google_apigee_control_plane_access` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own control plane access"})

    		ubx.Resource(apigee.ControlPlaneAccess, "ci-runner", apigee.ControlPlaneAccessConfig{
    			Name:                         "ci-runner",
    			AnalyticsPublisherIdentities: []string{"example"},
    			SynchronizerIdentities:       []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ControlPlaneAccess } from "jsr:@ubx/sdk-google/google/apigee/control_plane_access";

    export default stack("payments", () => {
      intent({ summary: "payments own control plane access" });

      resource(ControlPlaneAccess, "ci-runner", {
        name: "ci-runner",
        analyticsPublisherIdentities: ["example"],
        synchronizerIdentities: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import ControlPlaneAccess, ControlPlaneAccessConfig

    def describe():
        ubx.intent("payments own control plane access")

        ubx.resource(ControlPlaneAccess, "ci-runner", ControlPlaneAccessConfig(
            name="ci-runner",
            analytics_publisher_identities=["example"],
            synchronizer_identities=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_control_plane_access called "ci-runner",
    with analytics publisher identities a set value, synchronizer
    identities a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="analytics_publisher_identities" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="synchronizer_identities" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
