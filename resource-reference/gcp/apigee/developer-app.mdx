---
title: "google_apigee_developer_app"
description: "Real, generated bindings for `google_apigee_developer_app`."
---

`google_apigee_developer_app` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own developer app"})

    		ubx.Resource(apigee.DeveloperApp, "ci-runner", apigee.DeveloperAppConfig{
    			CallbackUrl:    "example",
    			DeveloperEmail: "example",
    			Name:           "ci-runner",
    			OrgId:          "example",
    			ApiProducts:    []string{"example"},
    			ConsumerKey:    "example",
    			ConsumerSecret: "example",
    			KeyExpiresIn:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DeveloperApp } from "jsr:@ubx/sdk-google/google/apigee/developer_app";

    export default stack("payments", () => {
      intent({ summary: "payments own developer app" });

      resource(DeveloperApp, "ci-runner", {
        callbackUrl: "example",
        developerEmail: "example",
        name: "ci-runner",
        orgId: "example",
        apiProducts: ["example"],
        consumerKey: "example",
        consumerSecret: "example",
        keyExpiresIn: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import DeveloperApp, DeveloperAppConfig

    def describe():
        ubx.intent("payments own developer app")

        ubx.resource(DeveloperApp, "ci-runner", DeveloperAppConfig(
            callback_url="example",
            developer_email="example",
            name="ci-runner",
            org_id="example",
            api_products=["example"],
            consumer_key="example",
            consumer_secret="example",
            key_expires_in="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_developer_app called "ci-runner", with
    callback url example, developer email example, org id example,
    api products a set value, consumer key example, consumer secret
    example, key expires in example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_products" type="set(string)">
  List of API products associated with the developer app. Optional.
</ResponseField>

<ResponseField name="app_family" type="string">
  Developer app family. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="attributes" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="value" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="callback_url" type="string" required>
  Callback URL used by OAuth 2.0 authorization servers to communicate authorization codes back to developer apps. Required.
</ResponseField>

<ResponseField name="consumer_key" type="string">
  Optionally specify a static consumer key for the developer app's credential. If not set, the API auto-generates a key. The consumer key must be unique across all developer apps in an organization. Changing this field forces the resource to be recreated. This is a write-only input used at create time: the provider creates the credential with this key via the keys API and removes the auto-generated one. The effective key is exposed in the 'credentials' output. Optional.
</ResponseField>

<ResponseField name="consumer_secret" type="string">
  Optionally specify a static consumer secret for the developer app's credential. Required if 'consumer_key' is specified. If not set, the API auto-generates a secret. Changing this field forces the resource to be recreated. This is a write-only input used at create time; the effective secret is exposed in the 'credentials' output. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="developer_email" type="string" required>
  Email address of the developer. This value is used to uniquely identify the developer in Apigee hybrid. Note that the email address has to be in lowercase only. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_expires_in" type="string">
  Expiration time, in milliseconds, for the consumer key that is generated for the developer app. If not set or left to the default value of -1, the API key never expires. The expiration time can't be updated after it is set. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the developer app. Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  The Apigee Organization associated with the Apigee instance, in the format 'organizations/&#123;&#123;org_name&#125;&#125;'. Required.
</ResponseField>

<ResponseField name="scopes" type="set(string)">
  Scopes to apply to the developer app. The specified scopes must already exist for the API product that you associate with the developer app. Optional.
</ResponseField>

<ResponseField name="status" type="string">
  Status of the credential. Valid values include approved or revoked. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="app_family" type="string">
  Developer app family. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="app_id" type="string">
  ID of the developer app. This ID is not user specified but is automatically generated on app creation. appId is a UUID. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="attributes" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="value" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created_at" type="string">
  Time at which the developer was created in milliseconds since epoch. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="credentials" type="list(object)">
  Output only. Set of credentials for the developer app consisting of the consumer key/secret pairs associated with the API products. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="api_products" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="apiproduct" type="string">
        </ResponseField>
        <ResponseField name="status" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="attributes" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="consumer_key" type="string">
    </ResponseField>
    <ResponseField name="consumer_secret" type="string">
    </ResponseField>
    <ResponseField name="expires_at" type="string">
    </ResponseField>
    <ResponseField name="issued_at" type="string">
    </ResponseField>
    <ResponseField name="scopes" type="list(string)">
    </ResponseField>
    <ResponseField name="status" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="developer_id" type="string">
  ID of the developer. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_modified_at" type="string">
  Time at which the developer was last modified in milliseconds since epoch. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Status of the credential. Valid values include approved or revoked. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
