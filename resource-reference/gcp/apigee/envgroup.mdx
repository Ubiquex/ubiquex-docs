---
title: "google_apigee_envgroup"
description: "`google_apigee_envgroup` (Environment Group) binds a set of hostnames to one or more Apigee environments..."
---

`google_apigee_envgroup` (Environment Group) binds a set of hostnames to one or more Apigee environments, determining which environment's own deployed proxies handle traffic for a given incoming hostname.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_apigee --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_apigee/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google_apigee/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own envgroup"})

    		ubx.Resource(apigee.ApigeeEnvgroup, "ci-runner", apigee.ApigeeEnvgroupConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_apigee --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApigeeEnvgroup } from "./local-sdk/google_apigee/sdk/typescript/google_apigee/apigee/apigee_envgroup.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own envgroup" });

      resource(ApigeeEnvgroup, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_apigee --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_apigee/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_apigee.apigee import ApigeeEnvgroup, ApigeeEnvgroupConfig

    def describe():
        ubx.intent("payments own envgroup")

        ubx.resource(ApigeeEnvgroup, "ci-runner", ApigeeEnvgroupConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_envgroup called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="created_at" type="string">
  Output only. The time at which the environment group was created as milliseconds since epoch. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="hostnames" type="list(string)">
  Required. Host names for this environment group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="last_modified_at" type="string">
  Output only. The time at which the environment group was last updated as milliseconds since epoch. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  ID of the environment group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the environment group. Values other than ACTIVE means the resource is not ready to use. Optional; if omitted, computed by Google Cloud.
</ResponseField>
