---
title: "google_apigee_envgroup_attachment"
description: "Real, generated bindings for `google_apigee_envgroup_attachment`."
---

`google_apigee_envgroup_attachment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/apigee"
    )

    cfg := apigee.EnvgroupAttachmentConfig{
        EnvgroupId: "example",
        Environment: "example",
    }
    ubx.Resource(apigee.EnvgroupAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EnvgroupAttachment } from
      "jsr:@ubx/sdk-google/google/apigee/envgroup_attachment";

    resource(EnvgroupAttachment, "example", {
      envgroupId: "example",
      environment: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.apigee import (
        EnvgroupAttachment,
        EnvgroupAttachmentConfig,
    )

    cfg = EnvgroupAttachmentConfig(
        envgroup_id="example",
        environment="example",
    )
    ubx.resource(EnvgroupAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_apigee_envgroup_attachment with envgroup_id,
environment.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="envgroup_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="environment" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
