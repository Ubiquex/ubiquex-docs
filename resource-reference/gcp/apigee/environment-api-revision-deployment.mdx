---
title: "google_apigee_environment_api_revision_deployment"
description: "Real, generated bindings for `google_apigee_environment_api_revision_deployment`."
---

`google_apigee_environment_api_revision_deployment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own environment api revision deployment"})

    		ubx.Resource(apigee.EnvironmentApiRevisionDeployment, "ci-runner", apigee.EnvironmentApiRevisionDeploymentConfig{
    			Api:              "example",
    			Environment:      "example",
    			OrgId:            "example",
    			Revision:         1,
    			Override:         true,
    			SequencedRollout: true,
    			ServiceAccount:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnvironmentApiRevisionDeployment } from "jsr:@ubx/sdk-google/google/apigee/environment_api_revision_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own environment api revision deployment" });

      resource(EnvironmentApiRevisionDeployment, "ci-runner", {
        api: "example",
        environment: "example",
        orgId: "example",
        revision: 1,
        override: true,
        sequencedRollout: true,
        serviceAccount: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import EnvironmentApiRevisionDeployment, EnvironmentApiRevisionDeploymentConfig

    def describe():
        ubx.intent("payments own environment api revision deployment")

        ubx.resource(EnvironmentApiRevisionDeployment, "ci-runner", EnvironmentApiRevisionDeploymentConfig(
            api="example",
            environment="example",
            org_id="example",
            revision=1,
            override=True,
            sequenced_rollout=True,
            service_account="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_environment_api_revision_deployment, with
    api example, environment example, org id example, revision 1,
    override true, sequenced rollout true, service account example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api" type="string" required>
  Apigee API proxy name. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="environment" type="string" required>
  Apigee environment name. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Apigee organization ID. Required.
</ResponseField>

<ResponseField name="override" type="bool">
  If true, replaces other deployed revisions of this proxy in the environment. Optional.
</ResponseField>

<ResponseField name="revision" type="number" required>
  API proxy revision number to deploy. Required.
</ResponseField>

<ResponseField name="sequenced_rollout" type="bool">
  If true, enables sequenced rollout for safe traffic switching. Optional.
</ResponseField>

<ResponseField name="service_account" type="string">
  Optional service account the deployed proxy runs as. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="basepaths" type="list(string)">
  Basepaths associated with the deployed proxy. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deploy_start_time" type="string">
  RFC3339 timestamp when deployment started. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Deployment state reported by Apigee. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
