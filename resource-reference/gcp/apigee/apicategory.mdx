---
title: "google_apigee_apicategory"
description: "`google_apigee_apicategory` defines a category label used to group and organize published API product(s) within an Apigee developer portal."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apicategory"})

    		ubx.Resource(apigee.Apicategory, "example", apigee.ApicategoryConfig{
    			Name:       "example",
    			Id:         "example",
    			SiteId:     "example",
    			UpdateTime: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Apicategory } from "@ubx/sdk-google/google/apigee/apicategory";

    export default stack("example", () => {
      intent({ summary: "example own apicategory" });

      resource(Apicategory, "example", {
        name: "example",
        id: "example",
        siteId: "example",
        updateTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee.apicategory import Apicategory, ApicategoryConfig

    def describe():
        ubx.intent("example own apicategory")

        ubx.resource(Apicategory, "example", ApicategoryConfig(
            name="example",
            id="example",
            site_id="example",
            update_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_apicategory called "example", with id
    example, site id example, update time example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  ID of the category (a UUID). Optional.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the category. Optional.
</ResponseField>

<ResponseField name="site_id" type="string">
  Name of the portal. Optional.
</ResponseField>

<ResponseField name="update_time" type="string">
  Time the category was last modified in milliseconds since epoch. Optional.
</ResponseField>

## Output properties

<ResponseField name="data" type="object">
  `ApiCategory` represents an API category. [Catalog items](https://cloud.google.com/apigee/docs/reference/apis/apigee/rest/v1/organizations.sites.apidocs) can be tagged with API categories; users viewing the API catalog in the portal will have the option to browse the catalog by category. Computed by Google Cloud after `ship`.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      ID of the category (a UUID). Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of the category. Optional.
    </ResponseField>
    <ResponseField name="site_id" type="string">
      Name of the portal. Optional.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Time the category was last modified in milliseconds since epoch. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="error_code" type="string">
  Unique error code for the request, if any. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="message" type="string">
  Description of the operation. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="request_id" type="string">
  Unique ID of the request. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Status of the operation. Computed by Google Cloud after `ship`.
</ResponseField>
