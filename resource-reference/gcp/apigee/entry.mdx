---
title: "google_apigee_entry"
description: "`google_apigee_entry` is a single key/value pair stored inside an Apigee key-value map, read and written by proxy policy logic at runtime."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_apigee --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_apigee/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google_apigee/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own entry"})

    		ubx.Resource(apigee.ApigeeEntry, "ci-runner", apigee.ApigeeEntryConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_apigee --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApigeeEntry } from "./local-sdk/google_apigee/sdk/typescript/google_apigee/apigee/apigee_entry.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own entry" });

      resource(ApigeeEntry, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_apigee --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_apigee/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_apigee.apigee import ApigeeEntry, ApigeeEntryConfig

    def describe():
        ubx.intent("payments own entry")

        ubx.resource(ApigeeEntry, "ci-runner", ApigeeEntryConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_entry called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string">
  Resource URI that can be used to identify the scope of the key value map entries. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="value" type="string">
  Required. Data or payload that is being retrieved and associated with the unique key. Optional; if omitted, computed by Google Cloud.
</ResponseField>
