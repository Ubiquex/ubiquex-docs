---
title: "google_apigee_security_action"
description: "Real, generated bindings for `google_apigee_security_action`."
---

`google_apigee_security_action` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security action"})

    		ubx.Resource(apigee.SecurityAction, "ci-runner", apigee.SecurityActionConfig{
    			EnvId:            "example",
    			OrgId:            "example",
    			SecurityActionId: "example",
    			State:            "example",
    			ApiProxies:       []string{"example"},
    			Description:      "Managed by ubx.",
    			ExpireTime:       "example",
    			Ttl:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityAction } from "jsr:@ubx/sdk-google/google/apigee/security_action";

    export default stack("payments", () => {
      intent({ summary: "payments own security action" });

      resource(SecurityAction, "ci-runner", {
        envId: "example",
        orgId: "example",
        securityActionId: "example",
        state: "example",
        apiProxies: ["example"],
        description: "Managed by ubx.",
        expireTime: "example",
        ttl: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import SecurityAction, SecurityActionConfig

    def describe():
        ubx.intent("payments own security action")

        ubx.resource(SecurityAction, "ci-runner", SecurityActionConfig(
            env_id="example",
            org_id="example",
            security_action_id="example",
            state="example",
            api_proxies=["example"],
            description="Managed by ubx.",
            expire_time="example",
            ttl="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_security_action, with env id example, org id
    example, security action id example, state example, api proxies a
    set value, description Managed by ubx., expire time example, ttl
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allow" type="list(object)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="api_proxies" type="list(string)">
  If unset, this would apply to all proxies in the environment. If set, this action is enforced only if at least one proxy in the repeated list is deployed at the time of enforcement. If set, several restrictions are enforced on SecurityActions. There can be at most 100 enabled actions with proxies set in an env. Several other restrictions apply on conditions and are detailed later. Optional.
</ResponseField>

<ResponseField name="condition_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="access_tokens" type="list(string)">
    </ResponseField>
    <ResponseField name="api_keys" type="list(string)">
    </ResponseField>
    <ResponseField name="api_products" type="list(string)">
    </ResponseField>
    <ResponseField name="asns" type="list(string)">
    </ResponseField>
    <ResponseField name="bot_reasons" type="list(string)">
    </ResponseField>
    <ResponseField name="developer_apps" type="list(string)">
    </ResponseField>
    <ResponseField name="developers" type="list(string)">
    </ResponseField>
    <ResponseField name="http_methods" type="list(string)">
    </ResponseField>
    <ResponseField name="ip_address_ranges" type="list(string)">
    </ResponseField>
    <ResponseField name="region_codes" type="list(string)">
    </ResponseField>
    <ResponseField name="user_agents" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deny" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="response_code" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional user provided description of the SecurityAction. Optional.
</ResponseField>

<ResponseField name="env_id" type="string" required>
  The Apigee environment that this security action applies to. Required.
</ResponseField>

<ResponseField name="expire_time" type="string">
  The expiration for this SecurityAction. Uses RFC 3339, where generated output will always be Z-normalized and uses 0, 3, 6 or 9 fractional digits. Offsets other than "Z" are also accepted. Examples: "2014-10-02T15:01:23Z", "2014-10-02T15:01:23.045123456Z" or "2014-10-02T15:01:23+05:30". Optional.
</ResponseField>

<ResponseField name="flag" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="headers" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  The organization that this security action applies to. Required.
</ResponseField>

<ResponseField name="security_action_id" type="string" required>
  The ID to use for the SecurityAction, which will become the final component of the action's resource name. This value should be 0-61 characters, and valid format is (^a-z?$). Required.
</ResponseField>

<ResponseField name="state" type="string" required>
  Only an ENABLED SecurityAction is enforced. An ENABLED SecurityAction past its expiration time will not be enforced. Possible values: ["ENABLED", "DISABLED"] Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ttl" type="string">
  The TTL for this SecurityAction. A duration in seconds with up to nine fractional digits, ending with 's'. Example: "3.5s". Optional.
</ResponseField>

## Output properties

<ResponseField name="allow" type="list(object)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="condition_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="access_tokens" type="list(string)">
    </ResponseField>
    <ResponseField name="api_keys" type="list(string)">
    </ResponseField>
    <ResponseField name="api_products" type="list(string)">
    </ResponseField>
    <ResponseField name="asns" type="list(string)">
    </ResponseField>
    <ResponseField name="bot_reasons" type="list(string)">
    </ResponseField>
    <ResponseField name="developer_apps" type="list(string)">
    </ResponseField>
    <ResponseField name="developers" type="list(string)">
    </ResponseField>
    <ResponseField name="http_methods" type="list(string)">
    </ResponseField>
    <ResponseField name="ip_address_ranges" type="list(string)">
    </ResponseField>
    <ResponseField name="region_codes" type="list(string)">
    </ResponseField>
    <ResponseField name="user_agents" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  The create time for this SecurityAction. Uses RFC 3339, where generated output will always be Z-normalized and uses 0, 3, 6 or 9 fractional digits. Offsets other than "Z" are also accepted. Examples: "2014-10-02T15:01:23Z", "2014-10-02T15:01:23.045123456Z" or "2014-10-02T15:01:23+05:30". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deny" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="response_code" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="flag" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="headers" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The update time for this SecurityAction. This reflects when this SecurityAction changed states. Uses RFC 3339, where generated output will always be Z-normalized and uses 0, 3, 6 or 9 fractional digits. Offsets other than "Z" are also accepted. Examples: "2014-10-02T15:01:23Z", "2014-10-02T15:01:23.045123456Z" or "2014-10-02T15:01:23+05:30". Computed by GCP after `ship`.
</ResponseField>
