---
title: "google_apigee_environment_keyvaluemaps_entries"
description: "Real, generated bindings for `google_apigee_environment_keyvaluemaps_entries`."
---

`google_apigee_environment_keyvaluemaps_entries` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigee "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/apigee"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own environment keyvaluemaps entries"})

    		ubx.Resource(apigee.EnvironmentKeyvaluemapsEntries, "ci-runner", apigee.EnvironmentKeyvaluemapsEntriesConfig{
    			EnvKeyvaluemapId: "example",
    			Name:             "ci-runner",
    			Value:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnvironmentKeyvaluemapsEntries } from "jsr:@ubx/sdk-google/google/apigee/environment_keyvaluemaps_entries";

    export default stack("payments", () => {
      intent({ summary: "payments own environment keyvaluemaps entries" });

      resource(EnvironmentKeyvaluemapsEntries, "ci-runner", {
        envKeyvaluemapId: "example",
        name: "ci-runner",
        value: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import EnvironmentKeyvaluemapsEntries, EnvironmentKeyvaluemapsEntriesConfig

    def describe():
        ubx.intent("payments own environment keyvaluemaps entries")

        ubx.resource(EnvironmentKeyvaluemapsEntries, "ci-runner", EnvironmentKeyvaluemapsEntriesConfig(
            env_keyvaluemap_id="example",
            name="ci-runner",
            value="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_environment_keyvaluemaps_entries called
    "ci-runner", with env keyvaluemap id example, value example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="env_keyvaluemap_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="value" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
