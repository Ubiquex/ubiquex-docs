---
title: "google_apigee_datacollector"
description: "`google_apigee_datacollector` defines a named, typed variable that Apigee proxy logic can write custom analytics data into..."
---

`google_apigee_datacollector` defines a named, typed variable that Apigee proxy logic can write custom analytics data into, later queryable alongside Apigee's own built-in analytics dimensions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_apigee --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_apigee/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google_apigee/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own datacollector"})

    		ubx.Resource(apigee.ApigeeDatacollector, "example", apigee.ApigeeDatacollectorConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_apigee --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApigeeDatacollector } from "./local-sdk/google_apigee/sdk/typescript/google_apigee/apigee/apigee_datacollector.ts";

    export default stack("example", () => {
      intent({ summary: "example own datacollector" });

      resource(ApigeeDatacollector, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_apigee --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_apigee/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_apigee.apigee import ApigeeDatacollector, ApigeeDatacollectorConfig

    def describe():
        ubx.intent("example own datacollector")

        ubx.resource(ApigeeDatacollector, "example", ApigeeDatacollectorConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_datacollector called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="created_at" type="string">
  Output only. The time at which the data collector was created in milliseconds since the epoch. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the data collector. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="last_modified_at" type="string">
  Output only. The time at which the Data Collector was last updated in milliseconds since the epoch. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  ID of the data collector. Must begin with `dc_`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  Immutable. The type of data this data collector will collect. Optional; if omitted, computed by Google Cloud.
</ResponseField>
