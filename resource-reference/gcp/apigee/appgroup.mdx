---
title: "google_apigee_appgroup"
description: "`google_apigee_appgroup` groups multiple developer apps together under a single logical owner (e.g. a company or team) for API access management..."
---

`google_apigee_appgroup` groups multiple developer apps together under a single logical owner (e.g. a company or team) for API access management, an alternative to per-individual-developer app ownership.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own appgroup"})

    		ubx.Resource(apigee.Appgroup, "example", apigee.AppgroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Appgroup } from "@ubx/sdk-google/google/apigee/appgroup";

    export default stack("example", () => {
      intent({ summary: "example own appgroup" });

      resource(Appgroup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import Appgroup, AppgroupConfig

    def describe():
        ubx.intent("example own appgroup")

        ubx.resource(Appgroup, "example", AppgroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_appgroup called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="app_group_id" type="string">
  Output only. Internal identifier that cannot be edited Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="attributes" type="list(object)">
  A list of attributes Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The literal value of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="channel_id" type="string">
  channel identifier identifies the owner maintaing this grouping. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="channel_uri" type="string">
  A reference to the associated storefront/marketplace. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="created_at" type="string">
  Output only. Created time as milliseconds since epoch. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  app group name displayed in the UI Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="email" type="string">
  Optional. Email of the AppGroup. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="last_modified_at" type="string">
  Output only. Modified time as milliseconds since epoch. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. Name of the AppGroup. Characters you can use in the name are restricted to: A-Z0-9._\-$ %. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="organization" type="string">
  Immutable. the org the app group is created Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="status" type="string">
  Valid values are `active` or `inactive`. Note that the status of the AppGroup should be updated via UpdateAppGroupRequest by setting the action as `active` or `inactive`. Optional; if omitted, computed by Google Cloud.
</ResponseField>
