---
title: "google_apigee_aliase"
description: "`google_apigee_aliase` (an Alias) is a single named certificate or key entry stored inside an Apigee keystore or truststore..."
---

`google_apigee_aliase` (an Alias) is a single named certificate or key entry stored inside an Apigee keystore or truststore, referenced by TLS configuration on a proxy or target endpoint.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_apigee --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_apigee/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google_apigee/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own aliase"})

    		ubx.Resource(apigee.ApigeeAliase, "ci-runner", apigee.ApigeeAliaseConfig{
    			ContentType: "example",
    			Data:        "example",
    			Extensions:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_apigee --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApigeeAliase } from "./local-sdk/google_apigee/sdk/typescript/google_apigee/apigee/apigee_aliase.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own aliase" });

      resource(ApigeeAliase, "ci-runner", {
        contentType: "example",
        data: "example",
        extensions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_apigee --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_apigee/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_apigee.apigee import ApigeeAliase, ApigeeAliaseConfig

    def describe():
        ubx.intent("payments own aliase")

        ubx.resource(ApigeeAliase, "ci-runner", ApigeeAliaseConfig(
            content_type="example",
            data="example",
            extensions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_aliase, with content type example, data
    example, extensions a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="content_type" type="string">
  The HTTP Content-Type header value specifying the content type of the body. Optional.
</ResponseField>

<ResponseField name="data" type="string">
  The HTTP request/response body as raw binary. Optional.
</ResponseField>

<ResponseField name="extensions" type="list(map(any))">
  Application specific response metadata. Must be set in the first response for streaming APIs. Optional.
</ResponseField>

## Output properties

<ResponseField name="alias" type="string">
  Resource ID for this alias. Values must match the regular expression `[^/]&#123;1,255&#125;`. Computed by Google Cloud after `ship`.
</ResponseField>

<ResponseField name="certs_info" type="object">
  Detail about the certificate(s) associated with this keystore/alias. Computed by Google Cloud after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cert_info" type="list(object)">
      Chain of certificates under this name. Optional.
      <Expandable title="properties">
        <ResponseField name="basic_constraints" type="string">
          The X.509 Basic Constraints extension, indicating whether this certificate is a CA. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="expiry_date" type="string">
          When this certificate expires. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="is_valid" type="string">
          Whether this certificate is currently valid (not expired, not revoked). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="issuer" type="string">
          The issuer identity of this certificate. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="public_key" type="string">
          The public key of this certificate or key pair. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="serial_number" type="string">
          This certificate's own serial number. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sig_alg_name" type="string">
          The name of the algorithm used to sign this certificate. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subject" type="string">
          The subject identity of this certificate. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subject_alternative_names" type="list(string)">
          Additional identity/identities (e.g. DNS names) this certificate is also valid for. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="valid_from" type="string">
          When this certificate becomes valid. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="version" type="number">
          The version identifier this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Type of alias. Computed by Google Cloud after `ship`.
</ResponseField>
