---
title: "google_apigee_app_group"
description: "Real, generated bindings for `google_apigee_app_group`."
---

`google_apigee_app_group` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app group"})

    		ubx.Resource(apigee.AppGroup, "ci-runner", apigee.AppGroupConfig{
    			Name:        "ci-runner",
    			OrgId:       "example",
    			ChannelId:   "example",
    			ChannelUri:  "example",
    			DisplayName: "example-display-name",
    			Status:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppGroup } from "jsr:@ubx/sdk-google/google/apigee/app_group";

    export default stack("payments", () => {
      intent({ summary: "payments own app group" });

      resource(AppGroup, "ci-runner", {
        name: "ci-runner",
        orgId: "example",
        channelId: "example",
        channelUri: "example",
        displayName: "example-display-name",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import AppGroup, AppGroupConfig

    def describe():
        ubx.intent("payments own app group")

        ubx.resource(AppGroup, "ci-runner", AppGroupConfig(
            name="ci-runner",
            org_id="example",
            channel_id="example",
            channel_uri="example",
            display_name="example-display-name",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_app_group called "ci-runner", with org id
    example, channel id example, channel uri example, display name
    example-display-name, status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attributes" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="value" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="channel_id" type="string">
  Channel identifier identifies the owner maintaining this grouping. Optional.
</ResponseField>

<ResponseField name="channel_uri" type="string">
  A reference to the associated storefront/marketplace. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string">
  App group name displayed in the UI Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the AppGroup. Characters you can use in the name are restricted to: A-Z0-9._-$ %. Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  The Apigee Organization associated with the Apigee app group, in the format 'organizations/&#123;&#123;org_name&#125;&#125;'. Required.
</ResponseField>

<ResponseField name="status" type="string">
  Valid values are active or inactive. Note that the status of the AppGroup should be updated via UpdateAppGroupRequest by setting the action as active or inactive. Possible values: ["active", "inactive"] Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="app_group_id" type="string">
  Internal identifier that cannot be edited Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="attributes" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="value" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created_at" type="string">
  Created time as milliseconds since epoch. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_modified_at" type="string">
  Modified time as milliseconds since epoch. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="organization" type="string">
  App group name displayed in the UI Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
