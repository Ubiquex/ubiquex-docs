---
title: "google_chronicle_watchlist"
description: "Real, generated bindings for `google_chronicle_watchlist`."
---

`google_chronicle_watchlist` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chronicle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/chronicle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own watchlist"})

    		ubx.Resource(chronicle.Watchlist, "ci-runner", chronicle.WatchlistConfig{
    			DisplayName:       "example-display-name",
    			Instance:          "example",
    			Location:          "example",
    			Description:       "Managed by ubx.",
    			MultiplyingFactor: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Watchlist } from "jsr:@ubx/sdk-google/google/chronicle/watchlist";

    export default stack("payments", () => {
      intent({ summary: "payments own watchlist" });

      resource(Watchlist, "ci-runner", {
        displayName: "example-display-name",
        instance: "example",
        location: "example",
        description: "Managed by ubx.",
        multiplyingFactor: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import Watchlist, WatchlistConfig

    def describe():
        ubx.intent("payments own watchlist")

        ubx.resource(Watchlist, "ci-runner", WatchlistConfig(
            display_name="example-display-name",
            instance="example",
            location="example",
            description="Managed by ubx.",
            multiplying_factor=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_watchlist, with display name
    example-display-name, instance example, location example,
    description Managed by ubx., multiplying factor 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the watchlist. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Required. Display name of the watchlist. Note that it must be at least one character and less than 63 characters (https://google.aip.dev/148). Required.
</ResponseField>

<ResponseField name="entity_population_mechanism" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="manual" type="list(object)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  The unique identifier for the Chronicle instance, which is the same as the customer ID. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location of the resource. This is the geographical region where the Chronicle instance resides, such as "us" or "europe-west2". Required.
</ResponseField>

<ResponseField name="multiplying_factor" type="number">
  Optional. Weight applied to the risk score for entities in this watchlist. The default is 1.0 if it is not specified. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="watchlist_id" type="string">
  Optional. The ID to use for the watchlist, which will become the final component of the watchlist's resource name. This value should be 4-63 characters, and valid characters are /a-z-/. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="watchlist_user_preferences" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="pinned" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Output only. Time the watchlist was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="entity_count" type="list(object)">
  Count of different types of entities in the watchlist. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="asset" type="number">
    </ResponseField>
    <ResponseField name="user" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="entity_population_mechanism" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="manual" type="list(object)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Resource name of the watchlist. This unique identifier is generated using values provided for the URL parameters. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;/watchlists/&#123;watchlist&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time the watchlist was last updated. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="watchlist_id" type="string">
  Optional. The ID to use for the watchlist, which will become the final component of the watchlist's resource name. This value should be 4-63 characters, and valid characters are /a-z-/. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="watchlist_user_preferences" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="pinned" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>
