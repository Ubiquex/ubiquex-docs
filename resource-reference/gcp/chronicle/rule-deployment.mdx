---
title: "google_chronicle_rule_deployment"
description: "Real, generated bindings for `google_chronicle_rule_deployment`."
---

`google_chronicle_rule_deployment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chronicle "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/chronicle"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own rule deployment"})

    		ubx.Resource(chronicle.RuleDeployment, "ci-runner", chronicle.RuleDeploymentConfig{
    			Instance:     "example",
    			Location:     "example",
    			Rule:         "example",
    			Alerting:     true,
    			Archived:     true,
    			Enabled:      true,
    			RunFrequency: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuleDeployment } from "jsr:@ubx/sdk-google/google/chronicle/rule_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own rule deployment" });

      resource(RuleDeployment, "ci-runner", {
        instance: "example",
        location: "example",
        rule: "example",
        alerting: true,
        archived: true,
        enabled: true,
        runFrequency: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import RuleDeployment, RuleDeploymentConfig

    def describe():
        ubx.intent("payments own rule deployment")

        ubx.resource(RuleDeployment, "ci-runner", RuleDeploymentConfig(
            instance="example",
            location="example",
            rule="example",
            alerting=True,
            archived=True,
            enabled=True,
            run_frequency="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_rule_deployment, with instance example,
    location example, rule example, alerting true, archived true,
    enabled true, run frequency example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="alerting" type="bool">
  Optional.
</ResponseField>

<ResponseField name="archived" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="rule" type="string" required>
  Required.
</ResponseField>

<ResponseField name="run_frequency" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="archive_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="consumer_rules" type="list(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="execution_state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_alert_status_change_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="producer_rules" type="list(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
