---
title: "google_chronicle_findings_refinement_deployment"
description: "Real, generated bindings for `google_chronicle_findings_refinement_deployment`."
---

`google_chronicle_findings_refinement_deployment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chronicle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/chronicle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own findings refinement deployment"})

    		ubx.Resource(chronicle.FindingsRefinementDeployment, "ci-runner", chronicle.FindingsRefinementDeploymentConfig{
    			FindingsRefinement: "example",
    			Instance:           "example",
    			Location:           "example",
    			Archived:           true,
    			Enabled:            true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FindingsRefinementDeployment } from "jsr:@ubx/sdk-google/google/chronicle/findings_refinement_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own findings refinement deployment" });

      resource(FindingsRefinementDeployment, "ci-runner", {
        findingsRefinement: "example",
        instance: "example",
        location: "example",
        archived: true,
        enabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import FindingsRefinementDeployment, FindingsRefinementDeploymentConfig

    def describe():
        ubx.intent("payments own findings refinement deployment")

        ubx.resource(FindingsRefinementDeployment, "ci-runner", FindingsRefinementDeploymentConfig(
            findings_refinement="example",
            instance="example",
            location="example",
            archived=True,
            enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_findings_refinement_deployment, with
    findings refinement example, instance example, location example,
    archived true, enabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="archived" type="bool">
  The archive state of the findings refinement deployment. Cannot be set to true unless enabled is set to false. If currently set to true, enabled cannot be updated to true. Optional.
</ResponseField>

<ResponseField name="detection_exclusion_application" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="curated_rule_sets" type="list(string)">
    </ResponseField>
    <ResponseField name="curated_rules" type="list(string)">
    </ResponseField>
    <ResponseField name="deleted_curated_rule_sets" type="list(string)">
    </ResponseField>
    <ResponseField name="rules" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="enabled" type="bool">
  Whether the findings refinement is currently deployed continuously against incoming findings. Optional.
</ResponseField>

<ResponseField name="findings_refinement" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="detection_exclusion_application" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="curated_rule_sets" type="list(string)">
    </ResponseField>
    <ResponseField name="curated_rules" type="list(string)">
    </ResponseField>
    <ResponseField name="deleted_curated_rule_sets" type="list(string)">
    </ResponseField>
    <ResponseField name="rules" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the findings refinement deployment. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;/findingsRefinements/&#123;findings_refinement&#125;/deployment Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The timestamp when the findings refinement deployment was last updated. Computed by GCP after `ship`.
</ResponseField>
