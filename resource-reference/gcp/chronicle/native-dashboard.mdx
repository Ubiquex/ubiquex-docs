---
title: "google_chronicle_native_dashboard"
description: "Real, generated bindings for `google_chronicle_native_dashboard`."
---

`google_chronicle_native_dashboard` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chronicle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/chronicle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own native dashboard"})

    		ubx.Resource(chronicle.NativeDashboard, "ci-runner", chronicle.NativeDashboardConfig{
    			DisplayName: "example-display-name",
    			Instance:    "example",
    			Location:    "example",
    			Access:      "example",
    			Description: "Managed by ubx.",
    			IsPinned:    true,
    			Type:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NativeDashboard } from "jsr:@ubx/sdk-google/google/chronicle/native_dashboard";

    export default stack("payments", () => {
      intent({ summary: "payments own native dashboard" });

      resource(NativeDashboard, "ci-runner", {
        displayName: "example-display-name",
        instance: "example",
        location: "example",
        access: "example",
        description: "Managed by ubx.",
        isPinned: true,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import NativeDashboard, NativeDashboardConfig

    def describe():
        ubx.intent("payments own native dashboard")

        ubx.resource(NativeDashboard, "ci-runner", NativeDashboardConfig(
            display_name="example-display-name",
            instance="example",
            location="example",
            access="example",
            description="Managed by ubx.",
            is_pinned=True,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_native_dashboard, with display name
    example-display-name, instance example, location example, access
    example, description Managed by ubx., is pinned true, type
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access" type="string">
  The access level of the dashboard. Possible values: DASHBOARD_PRIVATE DASHBOARD_PUBLIC Optional.
</ResponseField>

<ResponseField name="charts" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="chart_layout" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="span_x" type="number" required>
        </ResponseField>
        <ResponseField name="span_y" type="number" required>
        </ResponseField>
        <ResponseField name="start_x" type="number">
        </ResponseField>
        <ResponseField name="start_y" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dashboard_chart" type="string">
    </ResponseField>
    <ResponseField name="filters_ids" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the dashboard. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The display name/title of the dashboard visible to users. Required.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="chart_ids" type="list(string)">
    </ResponseField>
    <ResponseField name="data_source" type="string">
    </ResponseField>
    <ResponseField name="display_name" type="string">
    </ResponseField>
    <ResponseField name="field_path" type="string">
    </ResponseField>
    <ResponseField name="filter_operator_and_field_values" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="field_values" type="list(string)">
        </ResponseField>
        <ResponseField name="filter_operator" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
    </ResponseField>
    <ResponseField name="is_mandatory" type="bool">
    </ResponseField>
    <ResponseField name="is_standard_time_range_filter" type="bool">
    </ResponseField>
    <ResponseField name="is_standard_time_range_filter_enabled" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  The ID of the Chronicle instance. Required.
</ResponseField>

<ResponseField name="is_pinned" type="bool">
  Whether the dashboard is pinned by the user. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location of the Chronicle instance. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of dashboard. Possible values: CURATED, PRIVATE, PUBLIC, CUSTOM, MARKETPLACE Optional.
</ResponseField>

## Output properties

<ResponseField name="charts" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="chart_layout" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="span_x" type="number" required>
        </ResponseField>
        <ResponseField name="span_y" type="number" required>
        </ResponseField>
        <ResponseField name="start_x" type="number">
        </ResponseField>
        <ResponseField name="start_y" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dashboard_chart" type="string">
    </ResponseField>
    <ResponseField name="filters_ids" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  The creation time of the dashboard. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_user_id" type="string">
  The ID of the user who created the dashboard. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="dashboard_id" type="string">
  The unique ID of the Dashboard. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  Server-computed checksum for optimistic concurrency control, sent on update and delete requests. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="chart_ids" type="list(string)">
    </ResponseField>
    <ResponseField name="data_source" type="string">
    </ResponseField>
    <ResponseField name="display_name" type="string">
    </ResponseField>
    <ResponseField name="field_path" type="string">
    </ResponseField>
    <ResponseField name="filter_operator_and_field_values" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="field_values" type="list(string)">
        </ResponseField>
        <ResponseField name="filter_operator" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
    </ResponseField>
    <ResponseField name="is_mandatory" type="bool">
    </ResponseField>
    <ResponseField name="is_standard_time_range_filter" type="bool">
    </ResponseField>
    <ResponseField name="is_standard_time_range_filter_enabled" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fingerprint" type="string">
  The server-generated fingerprint of the dashboard definition. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_viewed_time" type="string">
  The time when this dashboard was last viewed. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The full resource name of the dashboard. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The time when the dashboard was last edited. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_user_id" type="string">
  The ID of the user who last edited the dashboard. Computed by GCP after `ship`.
</ResponseField>
