---
title: "google_chronicle_parser_extension"
description: "Real, generated bindings for `google_chronicle_parser_extension`."
---

`google_chronicle_parser_extension` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chronicle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/chronicle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own parser extension"})

    		ubx.Resource(chronicle.ParserExtension, "ci-runner", chronicle.ParserExtensionConfig{
    			Instance:          "example",
    			Location:          "example",
    			LogType:           "example",
    			CbnSnippet:        "example",
    			Log:               "example",
    			ValidationSkipped: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ParserExtension } from "jsr:@ubx/sdk-google/google/chronicle/parser_extension";

    export default stack("payments", () => {
      intent({ summary: "payments own parser extension" });

      resource(ParserExtension, "ci-runner", {
        instance: "example",
        location: "example",
        logType: "example",
        cbnSnippet: "example",
        log: "example",
        validationSkipped: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import ParserExtension, ParserExtensionConfig

    def describe():
        ubx.intent("payments own parser extension")

        ubx.resource(ParserExtension, "ci-runner", ParserExtensionConfig(
            instance="example",
            location="example",
            log_type="example",
            cbn_snippet="example",
            log="example",
            validation_skipped=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_parser_extension, with instance example,
    location example, log type example, cbn snippet example, log
    example, validation skipped true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cbn_snippet" type="string">
  Parser config could be a cbn snippet. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="dynamic_parsing" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="opted_fields" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="path" type="string">
        </ResponseField>
        <ResponseField name="sample_value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="field_extractors" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="append_repeated_fields" type="bool">
    </ResponseField>
    <ResponseField name="extractors" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="destination_path" type="string">
        </ResponseField>
        <ResponseField name="field_path" type="string">
        </ResponseField>
        <ResponseField name="precondition_op" type="string">
        </ResponseField>
        <ResponseField name="precondition_path" type="string">
        </ResponseField>
        <ResponseField name="precondition_value" type="string">
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="log_format" type="string">
    </ResponseField>
    <ResponseField name="preprocess_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="grok_regex" type="string">
        </ResponseField>
        <ResponseField name="target" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="transformed_cbn_snippet" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="log" type="string">
  Raw log used to assist the user in creation of augmentation. Optional.
</ResponseField>

<ResponseField name="log_type" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="validation_skipped" type="bool">
  Flag to bypass parser extension validation. If enabled, the parser extension won't be rejected during the validation phase and validation will be skipped. Optional.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time the parser extension was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="dynamic_parsing" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="opted_fields" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="path" type="string">
        </ResponseField>
        <ResponseField name="sample_value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="extension_validation_report" type="string">
  The latest extension validation report for this extension. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="field_extractors" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="append_repeated_fields" type="bool">
    </ResponseField>
    <ResponseField name="extractors" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="destination_path" type="string">
        </ResponseField>
        <ResponseField name="field_path" type="string">
        </ResponseField>
        <ResponseField name="precondition_op" type="string">
        </ResponseField>
        <ResponseField name="precondition_path" type="string">
        </ResponseField>
        <ResponseField name="precondition_value" type="string">
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="log_format" type="string">
    </ResponseField>
    <ResponseField name="preprocess_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="grok_regex" type="string">
        </ResponseField>
        <ResponseField name="target" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="transformed_cbn_snippet" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_live_time" type="string">
  The time the config was last serving live traffic. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Format: projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;/logTypes/&#123;logtype&#125;/parserExtensions/&#123;parserExtension&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="parserextension" type="string">
  Output only. The server-generated ID of the parser extension. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the parser extension Possible values: NEW VALIDATING LIVE REJECTED INTERNAL_ERROR VALIDATED ARCHIVED VALIDATION_SKIPPED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state_last_changed_time" type="string">
  The time the config state was last changed. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="validation_report" type="string">
  The validation report generated during extension validation. Computed by GCP after `ship`.
</ResponseField>
