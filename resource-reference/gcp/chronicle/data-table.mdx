---
title: "google_chronicle_data_table"
description: "Real, generated bindings for `google_chronicle_data_table`."
---

`google_chronicle_data_table` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chronicle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/chronicle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own data table"})

    		ubx.Resource(chronicle.DataTable, "ci-runner", chronicle.DataTableConfig{
    			DataTableId:   "example",
    			Description:   "Managed by ubx.",
    			Instance:      "example",
    			Location:      "example",
    			RowTimeToLive: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataTable } from "jsr:@ubx/sdk-google/google/chronicle/data_table";

    export default stack("payments", () => {
      intent({ summary: "payments own data table" });

      resource(DataTable, "ci-runner", {
        dataTableId: "example",
        description: "Managed by ubx.",
        instance: "example",
        location: "example",
        rowTimeToLive: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import DataTable, DataTableConfig

    def describe():
        ubx.intent("payments own data table")

        ubx.resource(DataTable, "ci-runner", DataTableConfig(
            data_table_id="example",
            description="Managed by ubx.",
            instance="example",
            location="example",
            row_time_to_live="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_data_table, with data table id example,
    description Managed by ubx., instance example, location example,
    row time to live example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="column_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="column_index" type="number" required>
    </ResponseField>
    <ResponseField name="column_type" type="string">
    </ResponseField>
    <ResponseField name="key_column" type="bool">
    </ResponseField>
    <ResponseField name="mapped_column_path" type="string">
    </ResponseField>
    <ResponseField name="original_column" type="string" required>
    </ResponseField>
    <ResponseField name="repeated_values" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_table_id" type="string" required>
  The ID to use for the data table. This is also the display name for the data table. It must satisfy the following requirements: - Starts with letter. - Contains only letters, numbers and underscore. - Must be unique and has length &lt; 256. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  This field uses a custom implementation please refer to documentation under /hashicorp/terraform-provider-google-beta/website/docs/r/chronicle_data_table.html.markdown for specifics Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string" required>
  A user-provided description of the data table. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="row_time_to_live" type="string">
  User-provided TTL of the data table. Optional.
</ResponseField>

<ResponseField name="scope_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="data_access_scopes" type="list(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="approximate_row_count" type="number">
  The count of rows in the data table. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="column_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="column_index" type="number" required>
    </ResponseField>
    <ResponseField name="column_type" type="string">
    </ResponseField>
    <ResponseField name="key_column" type="bool">
    </ResponseField>
    <ResponseField name="mapped_column_path" type="string">
    </ResponseField>
    <ResponseField name="original_column" type="string" required>
    </ResponseField>
    <ResponseField name="repeated_values" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Table create time Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="data_table_uuid" type="string">
  Data table unique id Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  This field uses a custom implementation please refer to documentation under /hashicorp/terraform-provider-google-beta/website/docs/r/chronicle_data_table.html.markdown for specifics Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string">
  The unique display name of the data table. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the data table Format: "&#123;project&#125;/locations/&#123;region&#125;/instances/&#123;instance&#125;/dataTables/&#123;data_table&#125;" Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="row_time_to_live_update_time" type="string">
  Last update time of the TTL of the data table. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="rule_associations_count" type="number">
  The count of rules using the data table. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="rules" type="list(string)">
  The resource names for the associated Rules that use this data table. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;/rules/&#123;rule&#125;. &#123;rule&#125; here refers to the rule id. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="scope_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="data_access_scopes" type="list(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_source" type="string">
  Possible values: USER RULE SEARCH Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Table update time Computed by GCP after `ship`.
</ResponseField>
