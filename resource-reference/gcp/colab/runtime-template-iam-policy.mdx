---
title: "google_colab_runtime_template_iam_policy"
description: "Real, generated bindings for `google_colab_runtime_template_iam_policy`."
---

`google_colab_runtime_template_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/colab"
    )

    cfg := colab.RuntimeTemplateIamPolicyConfig{
        PolicyData: "example",
        RuntimeTemplate: "example",
    }
    ubx.Resource(colab.RuntimeTemplateIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RuntimeTemplateIamPolicy } from
      "jsr:@ubx/sdk-google/google/colab/runtime_template_iam_policy";

    resource(RuntimeTemplateIamPolicy, "example", {
      policyData: "example",
      runtimeTemplate: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.colab import (
        RuntimeTemplateIamPolicy,
        RuntimeTemplateIamPolicyConfig,
    )

    cfg = RuntimeTemplateIamPolicyConfig(
        policy_data="example",
        runtime_template="example",
    )
    ubx.resource(RuntimeTemplateIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_colab_runtime_template_iam_policy with
policy_data, runtime_template.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="runtime_template" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
