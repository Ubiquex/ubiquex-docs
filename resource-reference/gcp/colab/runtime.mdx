---
title: "google_colab_runtime"
description: "Real, generated bindings for `google_colab_runtime`."
---

`google_colab_runtime` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	colab "github.com/ubiquex/ubx-sdk-google/sdk/go/google/colab"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own runtime"})

    		ubx.Resource(colab.Runtime, "ci-runner", colab.RuntimeConfig{
    			DisplayName:  "example-display-name",
    			Location:     "example",
    			RuntimeUser:  "example",
    			Name:         "ci-runner",
    			AutoUpgrade:  true,
    			Description:  "Managed by ubx.",
    			DesiredState: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Runtime } from "jsr:@ubx/sdk-google/google/colab/runtime";

    export default stack("payments", () => {
      intent({ summary: "payments own runtime" });

      resource(Runtime, "ci-runner", {
        displayName: "example-display-name",
        location: "example",
        runtimeUser: "example",
        name: "ci-runner",
        autoUpgrade: true,
        description: "Managed by ubx.",
        desiredState: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.colab import Runtime, RuntimeConfig

    def describe():
        ubx.intent("payments own runtime")

        ubx.resource(Runtime, "ci-runner", RuntimeConfig(
            display_name="example-display-name",
            location="example",
            runtime_user="example",
            name="ci-runner",
            auto_upgrade=True,
            description="Managed by ubx.",
            desired_state="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_colab_runtime called "ci-runner", with display name
    example-display-name, location example, runtime user example,
    auto upgrade true, description Managed by ubx., desired state
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_upgrade" type="bool">
  Triggers an upgrade anytime the runtime is started if it is upgradable. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the Runtime. Optional.
</ResponseField>

<ResponseField name="desired_state" type="string">
  Desired state of the Colab Runtime. Set this field to 'RUNNING' to start the runtime, and 'STOPPED' to stop it. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Required. The display name of the Runtime. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the resource: https://cloud.google.com/colab/docs/locations Required.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the Runtime Optional.
</ResponseField>

<ResponseField name="notebook_runtime_template_ref" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="notebook_runtime_template" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="runtime_user" type="string" required>
  The user email of the NotebookRuntime. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="expiration_time" type="string">
  Output only. Timestamp when this NotebookRuntime will be expired. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="is_upgradable" type="bool">
  Output only. Checks if the NotebookRuntime is upgradable. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="notebook_runtime_template_ref" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="notebook_runtime_template" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="notebook_runtime_type" type="string">
  Output only. The type of the notebook runtime. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the runtime. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
