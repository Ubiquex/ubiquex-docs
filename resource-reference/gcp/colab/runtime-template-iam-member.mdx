---
title: "google_colab_runtime_template_iam_member"
description: "Real, generated bindings for `google_colab_runtime_template_iam_member`."
---

`google_colab_runtime_template_iam_member` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	colab "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/colab"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own runtime template iam member"})

    		ubx.Resource(colab.RuntimeTemplateIamMember, "ci-runner", colab.RuntimeTemplateIamMemberConfig{
    			Member:          "example",
    			Role:            "example",
    			RuntimeTemplate: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuntimeTemplateIamMember } from "jsr:@ubx/sdk-google/google/colab/runtime_template_iam_member";

    export default stack("payments", () => {
      intent({ summary: "payments own runtime template iam member" });

      resource(RuntimeTemplateIamMember, "ci-runner", {
        member: "example",
        role: "example",
        runtimeTemplate: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.colab import RuntimeTemplateIamMember, RuntimeTemplateIamMemberConfig

    def describe():
        ubx.intent("payments own runtime template iam member")

        ubx.resource(RuntimeTemplateIamMember, "ci-runner", RuntimeTemplateIamMemberConfig(
            member="example",
            role="example",
            runtime_template="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_colab_runtime_template_iam_member, with member
    example, role example, runtime template example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="member" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="runtime_template" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
