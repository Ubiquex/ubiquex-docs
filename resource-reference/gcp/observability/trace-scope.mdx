---
title: "google_observability_trace_scope"
description: "`google_observability_trace_scope` defines a named set of project(s) whose own trace data can be jointly searched together in Cloud Trace..."
---

`google_observability_trace_scope` defines a named set of project(s) whose own trace data can be jointly searched together in Cloud Trace, for cross-project distributed tracing.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_observability --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_observability/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	observability "github.com/ubiquex/ubx-sdk-google/sdk/go/google_observability/observability"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own trace scope"})

    		ubx.Resource(observability.ObservabilityTraceScope, "example", observability.ObservabilityTraceScopeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_observability --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ObservabilityTraceScope } from "./local-sdk/google_observability/sdk/typescript/google_observability/observability/observability_trace_scope.ts";

    export default stack("example", () => {
      intent({ summary: "example own trace scope" });

      resource(ObservabilityTraceScope, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_observability --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_observability/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_observability.observability import ObservabilityTraceScope, ObservabilityTraceScopeConfig

    def describe():
        ubx.intent("example own trace scope")

        ubx.resource(ObservabilityTraceScope, "example", ObservabilityTraceScopeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_observability_trace_scope called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The creation timestamp of the trace scope. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Describes this trace scope. The maximum length of the description is 8000 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the trace scope. For example: projects/my-project/locations/global/traceScopes/my-trace-scope Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="resource_names" type="list(string)">
  Required. Names of the projects that are included in this trace scope. * `projects/[PROJECT_ID]` A trace scope can include a maximum of 20 projects. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The last update timestamp of the trace scope. Optional; if omitted, computed by Google Cloud.
</ResponseField>
