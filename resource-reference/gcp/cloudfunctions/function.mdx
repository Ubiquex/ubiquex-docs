---
title: "google_cloudfunctions_function"
description: "Real, generated bindings for `google_cloudfunctions_function`."
---

`google_cloudfunctions_function` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudfunctions "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloudfunctions"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own function"})

    		ubx.Resource(cloudfunctions.Function, "ci-runner", cloudfunctions.FunctionConfig{
    			Name:                      "ci-runner",
    			Runtime:                   "example",
    			AvailableMemoryMb:         1,
    			BuildEnvironmentVariables: map[string]string{"managed-by": "ubx"},
    			BuildWorkerPool:           "example",
    			Description:               "Managed by ubx.",
    			DockerRepository:          "example",
    			EntryPoint:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Function } from "jsr:@ubx/sdk-google/google/cloudfunctions/function";

    export default stack("payments", () => {
      intent({ summary: "payments own function" });

      resource(Function, "ci-runner", {
        name: "ci-runner",
        runtime: "example",
        availableMemoryMb: 1,
        buildEnvironmentVariables: { "managed-by": "ubx" },
        buildWorkerPool: "example",
        description: "Managed by ubx.",
        dockerRepository: "example",
        entryPoint: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.cloudfunctions import Function, FunctionConfig

    def describe():
        ubx.intent("payments own function")

        ubx.resource(Function, "ci-runner", FunctionConfig(
            name="ci-runner",
            runtime="example",
            available_memory_mb=1,
            build_environment_variables={"managed-by": "ubx"},
            build_worker_pool="example",
            description="Managed by ubx.",
            docker_repository="example",
            entry_point="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudfunctions_function called "ci-runner", with
    runtime example, available memory mb 1, build environment
    variables a set value, build worker pool example, description
    Managed by ubx., docker repository example, entry point example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="automatic_update_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="available_memory_mb" type="number">
  Memory (in MB), available to the function. Default value is 256. Possible values include 128, 256, 512, 1024, etc. Optional.
</ResponseField>

<ResponseField name="build_environment_variables" type="map(string)">
  A set of key/value environment variable pairs available during build time. Optional.
</ResponseField>

<ResponseField name="build_service_account" type="string">
  The fully-qualified name of the service account to be used for the build step of deploying this function Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="build_worker_pool" type="string">
  Name of the Cloud Build Custom Worker Pool that should be used to build the function. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Description of the function. Optional.
</ResponseField>

<ResponseField name="docker_registry" type="string">
  Docker Registry to use for storing the function's Docker images. Allowed values are ARTIFACT_REGISTRY (default) and CONTAINER_REGISTRY. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="docker_repository" type="string">
  User managed repository created in Artifact Registry optionally with a customer managed encryption key. If specified, deployments will use Artifact Registry for storing images built with Cloud Build. Optional.
</ResponseField>

<ResponseField name="entry_point" type="string">
  Name of the function that will be executed when the Google Cloud Function is triggered. Optional.
</ResponseField>

<ResponseField name="environment_variables" type="map(string)">
  A set of key/value environment variable pairs to assign to the function. Optional.
</ResponseField>

<ResponseField name="event_trigger" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="event_type" type="string" required>
    </ResponseField>
    <ResponseField name="failure_policy" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="retry" type="bool" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="https_trigger_security_level" type="string">
  The security level for the function. Defaults to SECURE_OPTIONAL. Valid only if trigger_http is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="https_trigger_url" type="string">
  URL which triggers function execution. Returned only if trigger_http is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ingress_settings" type="string">
  String value that controls what traffic can reach the function. Allowed values are ALLOW_ALL and ALLOW_INTERNAL_ONLY. Changes to this field will recreate the cloud function. Optional.
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  Resource name of a KMS crypto key (managed by the user) used to encrypt/decrypt function resources. Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  A set of key/value label pairs to assign to the function. Label keys must follow the requirements at https://docs.cloud.google.com/resource-manager/docs/creating-managing-labels#requirements. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="max_instances" type="number">
  The limit on the maximum number of function instances that may coexist at a given time. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="min_instances" type="number">
  The limit on the minimum number of function instances that may coexist at a given time. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  A user-defined name of the function. Function names must be unique globally. Required.
</ResponseField>

<ResponseField name="on_deploy_update_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="runtime_version" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Project of the function. If it is not provided, the provider project is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Region of function. If it is not provided, the provider region is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="runtime" type="string" required>
  The runtime in which the function is going to run. Eg. "nodejs20", "python37", "go111". Required.
</ResponseField>

<ResponseField name="secret_environment_variables" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="key" type="string" required>
    </ResponseField>
    <ResponseField name="project_id" type="string">
    </ResponseField>
    <ResponseField name="secret" type="string" required>
    </ResponseField>
    <ResponseField name="version" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="secret_volumes" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="mount_path" type="string" required>
    </ResponseField>
    <ResponseField name="project_id" type="string">
    </ResponseField>
    <ResponseField name="secret" type="string" required>
    </ResponseField>
    <ResponseField name="versions" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="path" type="string" required>
        </ResponseField>
        <ResponseField name="version" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_account_email" type="string">
  If provided, the self-provided service account to run the function with. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_archive_bucket" type="string">
  The GCS bucket containing the zip archive which contains the function. Optional.
</ResponseField>

<ResponseField name="source_archive_object" type="string">
  The source archive object (file) in archive bucket. Optional.
</ResponseField>

<ResponseField name="source_repository" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="deployed_url" type="string">
    </ResponseField>
    <ResponseField name="url" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeout" type="number">
  Timeout (in seconds) for the function. Default value is 60 seconds. Cannot be more than 540 seconds. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="trigger_http" type="bool">
  Boolean variable. Any HTTP request (of a supported type) to the endpoint will trigger function execution. Supported HTTP request types are: POST, PUT, GET, DELETE, and OPTIONS. Endpoint is returned as https_trigger_url. Cannot be used with trigger_bucket and trigger_topic. Optional.
</ResponseField>

<ResponseField name="vpc_connector" type="string">
  The VPC Network Connector that this cloud function can connect to. It can be either the fully-qualified URI, or the short name of the network connector resource. The format of this field is projects/*/locations/*/connectors/*. Optional.
</ResponseField>

<ResponseField name="vpc_connector_egress_settings" type="string">
  The egress settings for the connector, controlling what traffic is diverted through it. Allowed values are ALL_TRAFFIC and PRIVATE_RANGES_ONLY. Defaults to PRIVATE_RANGES_ONLY. If unset, this field preserves the previously set value. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="automatic_update_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="build_service_account" type="string">
  The fully-qualified name of the service account to be used for the build step of deploying this function Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="docker_registry" type="string">
  Docker Registry to use for storing the function's Docker images. Allowed values are ARTIFACT_REGISTRY (default) and CONTAINER_REGISTRY. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="event_trigger" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="event_type" type="string" required>
    </ResponseField>
    <ResponseField name="failure_policy" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="retry" type="bool" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="https_trigger_security_level" type="string">
  The security level for the function. Defaults to SECURE_OPTIONAL. Valid only if trigger_http is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="https_trigger_url" type="string">
  URL which triggers function execution. Returned only if trigger_http is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="max_instances" type="number">
  The limit on the maximum number of function instances that may coexist at a given time. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="on_deploy_update_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="runtime_version" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Project of the function. If it is not provided, the provider project is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Region of function. If it is not provided, the provider region is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="secret_environment_variables" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="key" type="string" required>
    </ResponseField>
    <ResponseField name="project_id" type="string">
    </ResponseField>
    <ResponseField name="secret" type="string" required>
    </ResponseField>
    <ResponseField name="version" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="secret_volumes" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="mount_path" type="string" required>
    </ResponseField>
    <ResponseField name="project_id" type="string">
    </ResponseField>
    <ResponseField name="secret" type="string" required>
    </ResponseField>
    <ResponseField name="versions" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="path" type="string" required>
        </ResponseField>
        <ResponseField name="version" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_account_email" type="string">
  If provided, the self-provided service account to run the function with. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_repository" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="deployed_url" type="string">
    </ResponseField>
    <ResponseField name="url" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  Describes the current stage of a deployment. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version_id" type="string">
  The version identifier of the Cloud Function. Each deployment attempt results in a new version of a function being created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="vpc_connector_egress_settings" type="string">
  The egress settings for the connector, controlling what traffic is diverted through it. Allowed values are ALL_TRAFFIC and PRIVATE_RANGES_ONLY. Defaults to PRIVATE_RANGES_ONLY. If unset, this field preserves the previously set value. Optional; if omitted, computed by GCP.
</ResponseField>
