---
title: "google_cloudfunctions_function_iam_policy"
description: "Real, generated bindings for `google_cloudfunctions_function_iam_policy`."
---

`google_cloudfunctions_function_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfunctions "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/cloudfunctions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own function iam policy"})

    		ubx.Resource(cloudfunctions.FunctionIamPolicy, "ci-runner", cloudfunctions.FunctionIamPolicyConfig{
    			CloudFunction: "example",
    			PolicyData:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FunctionIamPolicy } from "jsr:@ubx/sdk-google/google/cloudfunctions/function_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own function iam policy" });

      resource(FunctionIamPolicy, "ci-runner", {
        cloudFunction: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.cloudfunctions import FunctionIamPolicy, FunctionIamPolicyConfig

    def describe():
        ubx.intent("payments own function iam policy")

        ubx.resource(FunctionIamPolicy, "ci-runner", FunctionIamPolicyConfig(
            cloud_function="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudfunctions_function_iam_policy, with cloud
    function example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_function" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
