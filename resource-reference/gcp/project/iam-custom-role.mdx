---
title: "google_project_iam_custom_role"
description: "Real, generated bindings for `google_project_iam_custom_role`."
---

`google_project_iam_custom_role` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/project"
    )

    cfg := project.IamCustomRoleConfig{
        Permissions: []string{"example"},
        RoleId: "example",
        Title: "example",
        Description: "example",
        Stage: "example",
    }
    ubx.Resource(project.IamCustomRole, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IamCustomRole } from
      "jsr:@ubx/sdk-google/google/project/iam_custom_role";

    resource(IamCustomRole, "example", {
      permissions: ["example"],
      roleId: "example",
      title: "example",
      description: "example",
      stage: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.project import IamCustomRole, IamCustomRoleConfig

    cfg = IamCustomRoleConfig(
        permissions=["example"],
        role_id="example",
        title="example",
        description="example",
        stage="example",
    )
    ubx.resource(IamCustomRole, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_project_iam_custom_role with permissions,
role_id, title, description, stage.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="permissions" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="stage" type="string">
  Optional.
</ResponseField>

<ResponseField name="title" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="deleted" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
