---
title: "google_project_usage_export_bucket"
description: "Real, generated bindings for `google_project_usage_export_bucket`."
---

`google_project_usage_export_bucket` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/project"
    )

    cfg := project.UsageExportBucketConfig{
        BucketName: "example-bucket-name",
        Prefix: "example",
    }
    ubx.Resource(project.UsageExportBucket, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { UsageExportBucket } from
      "jsr:@ubx/sdk-google/google/project/usage_export_bucket";

    resource(UsageExportBucket, "example", {
      bucketName: "example-bucket-name",
      prefix: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.project import (
        UsageExportBucket,
        UsageExportBucketConfig,
    )

    cfg = UsageExportBucketConfig(
        bucket_name="example-bucket-name",
        prefix="example",
    )
    ubx.resource(UsageExportBucket, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_project_usage_export_bucket with bucket_name,
prefix.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
