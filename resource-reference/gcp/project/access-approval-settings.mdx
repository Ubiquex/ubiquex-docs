---
title: "google_project_access_approval_settings"
description: "Real, generated bindings for `google_project_access_approval_settings`."
---

`google_project_access_approval_settings` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	project "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/project"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own access approval settings"})

    		ubx.Resource(project.AccessApprovalSettings, "ci-runner", project.AccessApprovalSettingsConfig{
    			ProjectId:        "example",
    			ActiveKeyVersion: "example",
    			Project:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccessApprovalSettings } from "jsr:@ubx/sdk-google/google/project/access_approval_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own access approval settings" });

      resource(AccessApprovalSettings, "ci-runner", {
        projectId: "example",
        activeKeyVersion: "example",
        project: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.project import AccessApprovalSettings, AccessApprovalSettingsConfig

    def describe():
        ubx.intent("payments own access approval settings")

        ubx.resource(AccessApprovalSettings, "ci-runner", AccessApprovalSettingsConfig(
            project_id="example",
            active_key_version="example",
            project="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_project_access_approval_settings, with project id
    example, active key version example, project example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="active_key_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enrolled_services" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cloud_product" type="string" required>
    </ResponseField>
    <ResponseField name="enrollment_level" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="notification_emails" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional.
</ResponseField>

<ResponseField name="project_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="ancestor_has_active_key_version" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enrolled_ancestor" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="enrolled_services" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cloud_product" type="string" required>
    </ResponseField>
    <ResponseField name="enrollment_level" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="invalid_key_version" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="notification_emails" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
