---
title: "google_project_iam_member_remove"
description: "Real, generated bindings for `google_project_iam_member_remove`."
---

`google_project_iam_member_remove` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/project"
    )

    cfg := project.IamMemberRemoveConfig{
        Member: "example",
        Project: "example",
        Role: "example",
    }
    ubx.Resource(project.IamMemberRemove, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IamMemberRemove } from
      "jsr:@ubx/sdk-google/google/project/iam_member_remove";

    resource(IamMemberRemove, "example", {
      member: "example",
      project: "example",
      role: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.project import (
        IamMemberRemove,
        IamMemberRemoveConfig,
    )

    cfg = IamMemberRemoveConfig(
        member="example",
        project="example",
        role="example",
    )
    ubx.resource(IamMemberRemove, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_project_iam_member_remove with member, project,
role.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="member" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string" required>
  Required.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
