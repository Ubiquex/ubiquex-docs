---
title: "google_project_iam_member_remove"
description: "Real, generated bindings for `google_project_iam_member_remove`."
---

`google_project_iam_member_remove` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	project "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/project"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own iam member remove"})

    		ubx.Resource(project.IamMemberRemove, "ci-runner", project.IamMemberRemoveConfig{
    			Member:  "example",
    			Project: "example",
    			Role:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IamMemberRemove } from "jsr:@ubx/sdk-google/google/project/iam_member_remove";

    export default stack("payments", () => {
      intent({ summary: "payments own iam member remove" });

      resource(IamMemberRemove, "ci-runner", {
        member: "example",
        project: "example",
        role: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.project import IamMemberRemove, IamMemberRemoveConfig

    def describe():
        ubx.intent("payments own iam member remove")

        ubx.resource(IamMemberRemove, "ci-runner", IamMemberRemoveConfig(
            member="example",
            project="example",
            role="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_project_iam_member_remove, with member example,
    project example, role example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="member" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string" required>
  Required.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
