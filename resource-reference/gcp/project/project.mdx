---
title: "google_project"
description: "Real, generated bindings for `google_project`."
---

`google_project` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	project "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/project"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own project"})

    		ubx.Resource(project.Project, "ci-runner", project.ProjectConfig{
    			Name:              "ci-runner",
    			ProjectId:         "example",
    			AutoCreateNetwork: true,
    			BillingAccount:    "example",
    			FolderId:          "example",
    			Labels:            map[string]string{"managed-by": "ubx"},
    			OrgId:             "example",
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Project } from "jsr:@ubx/sdk-google/google/project/project";

    export default stack("payments", () => {
      intent({ summary: "payments own project" });

      resource(Project, "ci-runner", {
        name: "ci-runner",
        projectId: "example",
        autoCreateNetwork: true,
        billingAccount: "example",
        folderId: "example",
        labels: { "managed-by": "ubx" },
        orgId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.project import Project, ProjectConfig

    def describe():
        ubx.intent("payments own project")

        ubx.resource(Project, "ci-runner", ProjectConfig(
            name="ci-runner",
            project_id="example",
            auto_create_network=True,
            billing_account="example",
            folder_id="example",
            labels={"managed-by": "ubx"},
            org_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_project called "ci-runner", with project id
    example, auto create network true, billing account example,
    folder id example, labels a set value, org id example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_create_network" type="bool">
  Optional.
</ResponseField>

<ResponseField name="billing_account" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="folder_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="org_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="project_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="number" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
