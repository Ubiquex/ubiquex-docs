---
title: "google_project_iam_audit_config"
description: "Real, generated bindings for `google_project_iam_audit_config`."
---

`google_project_iam_audit_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/project"
    )

    cfg := project.IamAuditConfigConfig{
        Project: "example",
        Service: "example",
    }
    ubx.Resource(project.IamAuditConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IamAuditConfig } from
      "jsr:@ubx/sdk-google/google/project/iam_audit_config";

    resource(IamAuditConfig, "example", {
      project: "example",
      service: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.project import (
        IamAuditConfig,
        IamAuditConfigConfig,
    )

    cfg = IamAuditConfigConfig(
        project="example",
        service="example",
    )
    ubx.resource(IamAuditConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_project_iam_audit_config with project, service.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="audit_log_config" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="exempted_members" type="set(string)">
    </ResponseField>
    <ResponseField name="log_type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="audit_log_config" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="exempted_members" type="set(string)">
    </ResponseField>
    <ResponseField name="log_type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
