---
title: "google_project_iam_audit_config"
description: "Real, generated bindings for `google_project_iam_audit_config`."
---

`google_project_iam_audit_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	project "github.com/ubiquex/ubx-sdk-google/sdk/go/google/project"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own iam audit config"})

    		ubx.Resource(project.IamAuditConfig, "ci-runner", project.IamAuditConfigConfig{
    			Project: "example",
    			Service: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IamAuditConfig } from "jsr:@ubx/sdk-google/google/project/iam_audit_config";

    export default stack("payments", () => {
      intent({ summary: "payments own iam audit config" });

      resource(IamAuditConfig, "ci-runner", {
        project: "example",
        service: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.project import IamAuditConfig, IamAuditConfigConfig

    def describe():
        ubx.intent("payments own iam audit config")

        ubx.resource(IamAuditConfig, "ci-runner", IamAuditConfigConfig(
            project="example",
            service="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_project_iam_audit_config, with project example,
    service example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="audit_log_config" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="exempted_members" type="set(string)">
    </ResponseField>
    <ResponseField name="log_type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="audit_log_config" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="exempted_members" type="set(string)">
    </ResponseField>
    <ResponseField name="log_type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
