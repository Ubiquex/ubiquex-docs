---
title: "google_project_iam_policy"
description: "Real, generated bindings for `google_project_iam_policy`."
---

`google_project_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/project"
    )

    cfg := project.IamPolicyConfig{
        PolicyData: "example",
        Project: "example",
    }
    ubx.Resource(project.IamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IamPolicy } from
      "jsr:@ubx/sdk-google/google/project/iam_policy";

    resource(IamPolicy, "example", {
      policyData: "example",
      project: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.project import IamPolicy, IamPolicyConfig

    cfg = IamPolicyConfig(
        policy_data="example",
        project="example",
    )
    ubx.resource(IamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_project_iam_policy with policy_data, project.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
