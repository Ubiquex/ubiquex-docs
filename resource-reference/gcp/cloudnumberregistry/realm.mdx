---
title: "google_cloudnumberregistry_realm"
description: "`google_cloudnumberregistry_realm` is a real organizational grouping within Cloud Number Registry used to segment IP address management data..."
---

`google_cloudnumberregistry_realm` is a real organizational grouping within Cloud Number Registry used to segment IP address management data, listed and managed via the `gcloud alpha number-registry realms` surface.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_cloudnumberregistry --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_cloudnumberregistry/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudnumberregistry "github.com/ubiquex/ubx-sdk-google/sdk/go/google_cloudnumberregistry/cloudnumberregistry"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own realm"})

    		ubx.Resource(cloudnumberregistry.CloudnumberregistryRealm, "example", cloudnumberregistry.CloudnumberregistryRealmConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_cloudnumberregistry --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CloudnumberregistryRealm } from "./local-sdk/google_cloudnumberregistry/sdk/typescript/google_cloudnumberregistry/cloudnumberregistry/cloudnumberregistry_realm.ts";

    export default stack("example", () => {
      intent({ summary: "example own realm" });

      resource(CloudnumberregistryRealm, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_cloudnumberregistry --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_cloudnumberregistry/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_cloudnumberregistry.cloudnumberregistry import CloudnumberregistryRealm, CloudnumberregistryRealmConfig

    def describe():
        ubx.intent("example own realm")

        ubx.resource(CloudnumberregistryRealm, "example", CloudnumberregistryRealmConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudnumberregistry_realm called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="aggregated_data" type="object">
  Aggregated data for the Realm. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="custom_ranges_count" type="number">
      Output only. Number of CustomRanges in the Realm. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="discovered_ranges_count" type="number">
      Output only. Number of DiscoveredRanges in the Realm. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time at which the Realm was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="discovery_metadata" type="object">
  Metadata about a discovered resource, tracking event times, state, and source information. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="create_time" type="string">
      Output only. The time when the resource was created. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="event_time" type="string">
      Output only. The time when the event happened. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="resource" type="string">
      Output only. The resource name of the discovered resource, should be API-agnostic. Example: "projects/&#123;project_number&#125;/networks/&#123;network_id&#125;". Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="resource_uri" type="string">
      Output only. The resource uri of the discovered resource. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="source_id" type="string">
      Output only. The canonical google.aip.dev/122 name of the source resource. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="source_sub_id" type="string">
      Output only. A single source resource can be the source of multiple CNR resources. This sub_id is used to distinguish between the different CNR resources derived from the same upstream resource. For example, a single subnetwork can be the source of multiple Ranges, one for each protocol. In this case, the sub_id could be "private-ipv4" or "private-ipv6". Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The state of the resource. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. The time when the resource was last modified. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ip_version" type="string">
  Optional. IP version of the Realm. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. User-defined labels. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="management_type" type="string">
  Optional. Management type of the Realm. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. Identifier. The resource name of the Realm. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="registry_book" type="string">
  Required. Name of the RegistryBook that claims the Realm. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="traffic_type" type="string">
  Required. Traffic type of the Realm. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which the Realm was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
