---
title: "google_active_directory_domain"
description: "Real, generated bindings for `google_active_directory_domain`."
---

`google_active_directory_domain` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/active"
    )

    cfg := active.DirectoryDomainConfig{
        DomainName: "example-domain-name",
        Locations: []string{"example"},
        ReservedIpRange: "example",
        Admin: "example",
        AuthorizedNetworks: []string{"example"},
    }
    ubx.Resource(active.DirectoryDomain, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DirectoryDomain } from
      "jsr:@ubx/sdk-google/google/active/directory_domain";

    resource(DirectoryDomain, "example", {
      domainName: "example-domain-name",
      locations: ["example"],
      reservedIpRange: "example",
      admin: "example",
      authorizedNetworks: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.active import (
        DirectoryDomain,
        DirectoryDomainConfig,
    )

    cfg = DirectoryDomainConfig(
        domain_name="example-domain-name",
        locations=["example"],
        reserved_ip_range="example",
        admin="example",
        authorized_networks=["example"],
    )
    ubx.resource(DirectoryDomain, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_active_directory_domain with domain_name,
locations, reserved_ip_range, admin, authorized_networks.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="admin" type="string">
  Optional.
</ResponseField>

<ResponseField name="authorized_networks" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_protection" type="bool">
  Optional.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="locations" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reserved_ip_range" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="fqdn" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
