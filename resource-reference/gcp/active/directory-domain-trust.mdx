---
title: "google_active_directory_domain_trust"
description: "Real, generated bindings for `google_active_directory_domain_trust`."
---

`google_active_directory_domain_trust` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	active "github.com/ubiquex/ubx-sdk-google/sdk/go/google/active"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own directory domain trust"})

    		ubx.Resource(active.DirectoryDomainTrust, "ci-runner", active.DirectoryDomainTrustConfig{
    			Domain:                  "example",
    			TargetDnsIpAddresses:    []string{"example"},
    			TargetDomainName:        "example-target-domain-name",
    			TrustDirection:          "example",
    			TrustHandshakeSecret:    "example",
    			TrustType:               "example",
    			SelectiveAuthentication: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DirectoryDomainTrust } from "jsr:@ubx/sdk-google/google/active/directory_domain_trust";

    export default stack("payments", () => {
      intent({ summary: "payments own directory domain trust" });

      resource(DirectoryDomainTrust, "ci-runner", {
        domain: "example",
        targetDnsIpAddresses: ["example"],
        targetDomainName: "example-target-domain-name",
        trustDirection: "example",
        trustHandshakeSecret: "example",
        trustType: "example",
        selectiveAuthentication: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.active import DirectoryDomainTrust, DirectoryDomainTrustConfig

    def describe():
        ubx.intent("payments own directory domain trust")

        ubx.resource(DirectoryDomainTrust, "ci-runner", DirectoryDomainTrustConfig(
            domain="example",
            target_dns_ip_addresses=["example"],
            target_domain_name="example-target-domain-name",
            trust_direction="example",
            trust_handshake_secret="example",
            trust_type="example",
            selective_authentication=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_active_directory_domain_trust, with domain example,
    target dns ip addresses a set value, target domain name
    example-target-domain-name, trust direction example, trust
    handshake secret example, trust type example, selective
    authentication true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="domain" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="selective_authentication" type="bool">
  Optional.
</ResponseField>

<ResponseField name="target_dns_ip_addresses" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="target_domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="trust_direction" type="string" required>
  Required.
</ResponseField>

<ResponseField name="trust_handshake_secret" type="string" required>
  Required.
</ResponseField>

<ResponseField name="trust_type" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
