---
title: "google_dataflow_job"
description: "Real, generated bindings for `google_dataflow_job`."
---

`google_dataflow_job` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataflow "github.com/ubiquex/ubx-sdk-google/sdk/go/google/dataflow"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own job"})

    		ubx.Resource(dataflow.Job, "ci-runner", dataflow.JobConfig{
    			Name:                  "ci-runner",
    			TempGcsLocation:       "example",
    			TemplateGcsPath:       "example",
    			EnableStreamingEngine: true,
    			IpConfiguration:       "example",
    			KmsKeyName:            "example-kms-key-name",
    			Labels:                map[string]string{"managed-by": "ubx"},
    			MachineType:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Job } from "jsr:@ubx/sdk-google/google/dataflow/job";

    export default stack("payments", () => {
      intent({ summary: "payments own job" });

      resource(Job, "ci-runner", {
        name: "ci-runner",
        tempGcsLocation: "example",
        templateGcsPath: "example",
        enableStreamingEngine: true,
        ipConfiguration: "example",
        kmsKeyName: "example-kms-key-name",
        labels: { "managed-by": "ubx" },
        machineType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dataflow import Job, JobConfig

    def describe():
        ubx.intent("payments own job")

        ubx.resource(Job, "ci-runner", JobConfig(
            name="ci-runner",
            temp_gcs_location="example",
            template_gcs_path="example",
            enable_streaming_engine=True,
            ip_configuration="example",
            kms_key_name="example-kms-key-name",
            labels={"managed-by": "ubx"},
            machine_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataflow_job called "ci-runner", with temp gcs
    location example, template gcs path example, enable streaming
    engine true, ip configuration example, kms key name
    example-kms-key-name, labels a set value, machine type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="additional_experiments" type="set(string)">
  List of experiments that should be used by the job. An example value is ["enable_stackdriver_agent_metrics"]. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_streaming_engine" type="bool">
  Indicates if the job should use the streaming engine feature. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_configuration" type="string">
  The configuration for VM IPs. Options are "WORKER_IP_PUBLIC" or "WORKER_IP_PRIVATE". Optional.
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  The name for the Cloud KMS key for the job. Key format is: projects/PROJECT_ID/locations/LOCATION/keyRings/KEY_RING/cryptoKeys/KEY Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  User labels to be specified for the job. Keys and values should follow the restrictions specified in the labeling restrictions page. NOTE: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="machine_type" type="string">
  The machine type to use for the job. Optional.
</ResponseField>

<ResponseField name="max_workers" type="number">
  The number of workers permitted to work on the job. More workers may improve processing speed at additional cost. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  A unique name for the resource, required by Dataflow. Required.
</ResponseField>

<ResponseField name="network" type="string">
  The network to which VMs will be assigned. If it is not provided, "default" will be used. Optional.
</ResponseField>

<ResponseField name="on_delete" type="string">
  One of "drain" or "cancel". Specifies behavior of deletion during terraform destroy. Optional.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Key/Value pairs to be passed to the Dataflow job (as used in the template). Optional.
</ResponseField>

<ResponseField name="project" type="string">
  The project in which the resource belongs. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The region in which the created job should run. Optional.
</ResponseField>

<ResponseField name="service_account_email" type="string">
  The Service Account email used to create the job. Optional.
</ResponseField>

<ResponseField name="skip_wait_on_job_termination" type="bool">
  If true, treat DRAINING and CANCELLING as terminal job states and do not wait for further changes before removing from terraform state and moving on. WARNING: this will lead to job name conflicts if you do not ensure that the job names are different, e.g. by embedding a release ID or by using a random_id. Optional.
</ResponseField>

<ResponseField name="subnetwork" type="string">
  The subnetwork to which VMs will be assigned. Should be of the form "regions/REGION/subnetworks/SUBNETWORK". Optional.
</ResponseField>

<ResponseField name="temp_gcs_location" type="string" required>
  A writeable location on Google Cloud Storage for the Dataflow job to dump its temporary data. Required.
</ResponseField>

<ResponseField name="template_gcs_path" type="string" required>
  The Google Cloud Storage path to the Dataflow job template. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transform_name_mapping" type="map(string)">
  Only applicable when updating a pipeline. Map of transform name prefixes of the job to be replaced with the corresponding name prefixes of the new job. Optional.
</ResponseField>

<ResponseField name="zone" type="string">
  The zone in which the created job should run. If it is not provided, the provider zone is used. Optional.
</ResponseField>

## Output properties

<ResponseField name="additional_experiments" type="set(string)">
  List of experiments that should be used by the job. An example value is ["enable_stackdriver_agent_metrics"]. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="job_id" type="string">
  The unique ID of this job. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  The project in which the resource belongs. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the resource, selected from the JobState enum. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of this job, selected from the JobType enum. Computed by GCP after `ship`.
</ResponseField>
