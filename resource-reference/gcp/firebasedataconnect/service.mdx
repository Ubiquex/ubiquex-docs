---
title: "google_firebasedataconnect_service"
description: "`google_firebasedataconnect_service` is the top-level Firebase Data Connect resource that a schema and its own connector(s) are deployed under."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebasedataconnect "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firebasedataconnect"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service"})

    		ubx.Resource(firebasedataconnect.Service, "example", firebasedataconnect.ServiceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Service } from "@ubx/sdk-google/google/firebasedataconnect/service";

    export default stack("example", () => {
      intent({ summary: "example own service" });

      resource(Service, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firebasedataconnect import Service, ServiceConfig

    def describe():
        ubx.intent("example own service")

        ubx.resource(Service, "example", ServiceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebasedataconnect_service called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Optional. Stores small amounts of arbitrary data. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="connectors" type="list(object)">
  Output only. The list of connectors in this service. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
      Optional. Arbitrary key/value metadata attached to this resource for client tooling to read; not used by Google Cloud itself for filtering or lookup. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="client_cache" type="object">
      Caching configuration applied on the client side. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="entity_id_included" type="bool">
          Whether the entity's own identifier is included in this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="strict_validation_enabled" type="bool">
          Whether strict schema validation is enforced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. The timestamp when this resource was created. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="display_name" type="string">
      A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="etag" type="string">
      A checksum computed by the server from the resource's own current content, used for optimistic concurrency control -- an update/delete that includes a stale `etag` is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Optional. User-provided key/value labels on this resource, usable for organizing and filtering resources in Cloud Billing and the console. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="reconciling" type="bool">
      Output only. `true` while the backend is still working to bring this resource's actual state in line with its most recent requested change. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source" type="object">
      The GraphQL source document defining this schema or connector. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="files" type="list(object)">
          The file(s) making up this schema or connector. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="content" type="string">
              The literal content of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path" type="string">
              A file or resource path. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="uid" type="string">
      Output only. A system-assigned, globally unique identifier for this resource, in UUID4 format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. The timestamp when this resource was most recently updated. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. [Output only] Create time stamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Mutable human-readable name. 63 character limit. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. [AIP-154](https://google.aip.dev/154) Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels as key value pairs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The relative resource name of the Firebase SQL Connect service, in the format: ``` projects/&#123;project&#125;/locations/&#123;location&#125;/services/&#123;service&#125; ``` Note that the service ID is specific to Firebase SQL Connect and does not correspond to any of the instance IDs of the underlying data source connections. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. A field that if true, indicates that the system is working update the service. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="schemas" type="list(object)">
  Output only. The list of schemas in this service. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
      Optional. Arbitrary key/value metadata attached to this resource for client tooling to read; not used by Google Cloud itself for filtering or lookup. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. The timestamp when this resource was created. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="datasources" type="list(object)">
      The data source(s) this service connects to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="http_graphql" type="object">
          Configuration for exposing this connector's own operations over HTTP GraphQL. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="timeout" type="string">
              How long this operation is allowed to run before timing out. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="uri" type="string">
              A resource URI. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="postgresql" type="object">
          Configuration specific to a PostgreSQL data source. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cloud_sql" type="object">
              Configuration for connecting to a Cloud SQL database. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="edition" type="string">
                  The Cloud SQL edition this instance runs, e.g. Enterprise or Enterprise Plus. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="instance" type="string">
                  A reference to the underlying database instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="database" type="string">
              A reference to the database this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ephemeral" type="bool">
              Whether this resource is temporary and not intended to persist. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="schema" type="string">
              The GraphQL schema defining this service's own data model. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="schema_migration" type="string">
              How schema change(s) are applied to the underlying database. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="schema_validation" type="string">
              How strictly the schema is validated against the underlying database before deployment. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="unlinked" type="bool">
              Whether this schema is not yet linked to a data source. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="display_name" type="string">
      A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="etag" type="string">
      A checksum computed by the server from the resource's own current content, used for optimistic concurrency control -- an update/delete that includes a stale `etag` is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Optional. User-provided key/value labels on this resource, usable for organizing and filtering resources in Cloud Billing and the console. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="reconciling" type="bool">
      Output only. `true` while the backend is still working to bring this resource's actual state in line with its most recent requested change. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source" type="object">
      The GraphQL source document defining this schema or connector. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="files" type="list(object)">
          The file(s) making up this schema or connector. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="content" type="string">
              The literal content of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path" type="string">
              A file or resource path. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="uid" type="string">
      Output only. A system-assigned, globally unique identifier for this resource, in UUID4 format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. The timestamp when this resource was most recently updated. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source" type="object">
  Used to represent a set of source files. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="files" type="list(object)">
      Required. The files that comprise the source set. Optional.
      <Expandable title="properties">
        <ResponseField name="content" type="string">
          The literal content of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="path" type="string">
          A file or resource path. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System-assigned, unique identifier. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. [Output only] Update time stamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>
