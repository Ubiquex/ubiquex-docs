---
title: "google_firebasedataconnect_connector"
description: "`google_firebasedataconnect_connector` defines a set of GraphQL queries and mutations exposed to client apps through Firebase Data Connect..."
---

`google_firebasedataconnect_connector` defines a set of GraphQL queries and mutations exposed to client apps through Firebase Data Connect, generated into a typed client SDK.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_firebasedataconnect --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_firebasedataconnect/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebasedataconnect "github.com/ubiquex/ubx-sdk-google/sdk/go/google_firebasedataconnect/firebasedataconnect"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connector"})

    		ubx.Resource(firebasedataconnect.FirebasedataconnectConnector, "ci-runner", firebasedataconnect.FirebasedataconnectConnectorConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_firebasedataconnect --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirebasedataconnectConnector } from "./local-sdk/google_firebasedataconnect/sdk/typescript/google_firebasedataconnect/firebasedataconnect/firebasedataconnect_connector.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own connector" });

      resource(FirebasedataconnectConnector, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_firebasedataconnect --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_firebasedataconnect/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_firebasedataconnect.firebasedataconnect import FirebasedataconnectConnector, FirebasedataconnectConnectorConfig

    def describe():
        ubx.intent("payments own connector")

        ubx.resource(FirebasedataconnectConnector, "ci-runner", FirebasedataconnectConnectorConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebasedataconnect_connector called "ci-runner",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Optional. Stores small amounts of arbitrary data. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="client_cache" type="object">
  Client caching settings of a connector. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="entity_id_included" type="bool">
      Optional. A field that, if true, means that responses served by this connector will include entityIds in GraphQL response extensions. This helps the client SDK cache responses in an improved way, known as "normalized caching", if caching is enabled on the client. Each entityId is a stable key based on primary key values. Therefore, this field should only be set to true if the primary keys of accessed tables do not contain sensitive information. Optional.
    </ResponseField>
    <ResponseField name="strict_validation_enabled" type="bool">
      Optional. A field that, if true, enables stricter validation on the connector source code to make sure the operation response shapes are suitable for client-side caching. This can include additional errors and warnings. For example, using the same alias for different fields is disallowed, as it may cause conflicts or confusion with normalized caching. (This field is off by default for compatibility, but enabling it is highly recommended to catch common caching pitfalls.) Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. [Output only] Create time stamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Mutable human-readable name. 63 character limit. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. [AIP-154](https://google.aip.dev/154) Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels as key value pairs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The relative resource name of the connector, in the format: ``` projects/&#123;project&#125;/locations/&#123;location&#125;/services/&#123;service&#125;/connectors/&#123;connector&#125; ``` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. A field that if true, indicates that the system is working to compile and deploy the connector. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="source" type="object">
  Used to represent a set of source files. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="files" type="list(object)">
      Required. The files that comprise the source set. Optional.
      <Expandable title="properties">
        <ResponseField name="content" type="string">
          The literal content of this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="path" type="string">
          A file or resource path. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System-assigned, unique identifier. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. [Output only] Update time stamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>
