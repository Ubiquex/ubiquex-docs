---
title: "google_pubsublite_reservation"
description: "`google_pubsublite_reservation` purchases a fixed pool of Pub/Sub Lite throughput capacity that topic(s) draw from, for predictable..."
---

`google_pubsublite_reservation` purchases a fixed pool of Pub/Sub Lite throughput capacity that topic(s) draw from, for predictable, capacity-based pricing.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	pubsublite "github.com/ubiquex/ubx-sdk-google/sdk/go/google/pubsublite"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own reservation"})

    		ubx.Resource(pubsublite.Reservation, "example", pubsublite.ReservationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Reservation } from "@ubx/sdk-google/google/pubsublite/reservation";

    export default stack("example", () => {
      intent({ summary: "example own reservation" });

      resource(Reservation, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.pubsublite import Reservation, ReservationConfig

    def describe():
        ubx.intent("example own reservation")

        ubx.resource(Reservation, "example", ReservationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_pubsublite_reservation called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string">
  The name of the reservation. Structured like: projects/&#123;project_number&#125;/locations/&#123;location&#125;/reservations/&#123;reservation_id&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="throughput_capacity" type="string">
  The reserved throughput capacity. Every unit of throughput capacity is equivalent to 1 MiB/s of published messages or 2 MiB/s of subscribed messages. Any topics which are declared as using capacity from a Reservation will consume resources from this reservation instead of being charged individually. Optional; if omitted, computed by Google Cloud.
</ResponseField>
