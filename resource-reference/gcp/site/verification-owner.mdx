---
title: "google_site_verification_owner"
description: "Real, generated bindings for `google_site_verification_owner`."
---

`google_site_verification_owner` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/site"
    )

    cfg := site.VerificationOwnerConfig{
        Email: "example",
        WebResourceId: "example",
    }
    ubx.Resource(site.VerificationOwner, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VerificationOwner } from
      "jsr:@ubx/sdk-google/google/site/verification_owner";

    resource(VerificationOwner, "example", {
      email: "example",
      webResourceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.site import (
        VerificationOwner,
        VerificationOwnerConfig,
    )

    cfg = VerificationOwnerConfig(
        email="example",
        web_resource_id="example",
    )
    ubx.resource(VerificationOwner, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_site_verification_owner with email,
web_resource_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="email" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="web_resource_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
