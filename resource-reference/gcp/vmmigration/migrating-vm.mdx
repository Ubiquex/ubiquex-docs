---
title: "google_vmmigration_migrating_vm"
description: "`google_vmmigration_migrating_vm` is the real, central VM Migration resource representing one source VM's entire migration lifecycle end to end..."
---

`google_vmmigration_migrating_vm` is the real, central VM Migration resource representing one source VM's entire migration lifecycle end to end, from initial replication through `google_vmmigration_clone_job` validation to final `google_vmmigration_cutover_job`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_vmmigration --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_vmmigration/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmmigration "github.com/ubiquex/ubx-sdk-google/sdk/go/google_vmmigration/vmmigration"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own migrating vm"})

    		ubx.Resource(vmmigration.VmmigrationMigratingVm, "example", vmmigration.VmmigrationMigratingVmConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_vmmigration --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmmigrationMigratingVm } from "./local-sdk/google_vmmigration/sdk/typescript/google_vmmigration/vmmigration/vmmigration_migrating_vm.ts";

    export default stack("example", () => {
      intent({ summary: "example own migrating vm" });

      resource(VmmigrationMigratingVm, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_vmmigration --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_vmmigration/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_vmmigration.vmmigration import VmmigrationMigratingVm, VmmigrationMigratingVmConfig

    def describe():
        ubx.intent("example own migrating vm")

        ubx.resource(VmmigrationMigratingVm, "example", VmmigrationMigratingVmConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vmmigration_migrating_vm called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="aws_source_vm_details" type="object">
  Represent the source AWS VM details. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="architecture" type="string">
      Output only. The VM architecture. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="committed_storage_bytes" type="string">
      Output only. The total size of the disks being migrated in bytes. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="disks" type="list(object)">
      Output only. The disks attached to the source VM. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="disk_number" type="number">
          The real, source-side index identifying which of the source VM's disks this migration status entry describes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="size_gb" type="string">
          The real, provisioned size, in GiB, of a migrated disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="volume_id" type="string">
          The real, source-side volume identifier of one disk on an AWS-sourced migrating VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="firmware" type="string">
      Output only. The firmware type of the source VM. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="vm_capabilities_info" type="object">
      Migrating VM source information about the VM capabilities needed for some Compute Engine features. Optional.
      <Expandable title="properties">
        <ResponseField name="last_os_capabilities_update_time" type="string">
          Output only. The last time OS capabilities list was updated. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="os_capabilities" type="list(string)">
          Output only. Unordered list. List of certain VM OS capabilities needed for some Compute Engine features. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="azure_source_vm_details" type="object">
  Represent the source Azure VM details. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="architecture" type="string">
      Output only. The VM architecture. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="committed_storage_bytes" type="string">
      Output only. The total size of the disks being migrated in bytes. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="disks" type="list(object)">
      Output only. The disks attached to the source VM. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="disk_id" type="string">
          The real, source-side disk identifier of one disk on an Azure-sourced migrating VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_number" type="number">
          The real, source-side index identifying which of the source VM's disks this migration status entry describes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="size_gb" type="string">
          The real, provisioned size, in GiB, of a migrated disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="firmware" type="string">
      Output only. The firmware type of the source VM. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="vm_capabilities_info" type="object">
      Migrating VM source information about the VM capabilities needed for some Compute Engine features. Optional.
      <Expandable title="properties">
        <ResponseField name="last_os_capabilities_update_time" type="string">
          Output only. The last time OS capabilities list was updated. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="os_capabilities" type="list(string)">
          Output only. Unordered list. List of certain VM OS capabilities needed for some Compute Engine features. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="compute_engine_disks_target_defaults" type="object">
  ComputeEngineDisksTargetDefaults is a collection of details for creating Persistent Disks in a target Compute Engine project. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="disks" type="list(object)">
      The details of each Persistent Disk to create. Optional.
      <Expandable title="properties">
        <ResponseField name="additional_labels" type="map(string)">
          Real, extra Compute Engine labels to apply to the migrated VM or disk beyond whatever VM Migration applies automatically. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_name" type="string">
          The real, target-side name to assign to a migrated disk in Google Cloud. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_type" type="string">
          The real Compute Engine disk type (e.g. `pd-ssd`, `pd-standard`) to use for a migrated disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="encryption" type="object">
          Real encryption configuration (e.g. the `kms_key` to use) applied to a migrated disk or VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key" type="string">
              The real Cloud KMS key used to encrypt this migrated resource's data at rest. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_disk_number" type="number">
          The real, source-side index identifying which of the source VM's own disks a target disk or job status corresponds to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vm_attachment_details" type="object">
          Real, target-side details describing how a migrated disk attaches to its VM instance (device name, boot disk status). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="device_name" type="string">
              The real device name a migrated disk is attached to the target VM instance under. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="disks_target_defaults" type="any">
      Details for a disk only migration. Optional.
    </ResponseField>
    <ResponseField name="target_project" type="string">
      The full path of the resource of type TargetProject which represents the Compute Engine project in which to create the Persistent Disks. Optional.
    </ResponseField>
    <ResponseField name="vm_target_defaults" type="object">
      Details for creation of a VM that migrated data disks will be attached to. Optional.
      <Expandable title="properties">
        <ResponseField name="additional_licenses" type="list(string)">
          Optional. Additional licenses to assign to the VM. Optional.
        </ResponseField>
        <ResponseField name="boot_disk_defaults" type="object">
          BootDiskDefaults hold information about the boot disk of a VM. Optional.
          <Expandable title="properties">
            <ResponseField name="device_name" type="string">
              Optional. Specifies a unique device name of your choice that is reflected into the /dev/disk/by-id/google-* tree of a Linux operating system running within the instance. If not specified, the server chooses a default device name to apply to this disk, in the form persistent-disk-x, where x is a number assigned by Google Compute Engine. This field is only applicable for persistent disks. Optional.
            </ResponseField>
            <ResponseField name="disk_name" type="string">
              Optional. The name of the disk. Optional.
            </ResponseField>
            <ResponseField name="disk_type" type="string">
              Optional. The type of disk provisioning to use for the VM. Optional.
            </ResponseField>
            <ResponseField name="encryption" type="object">
              Encryption message describes the details of the applied encryption. Optional.
              <Expandable title="properties">
                <ResponseField name="kms_key" type="string">
                  Required. The name of the encryption key that is stored in Google Cloud KMS. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="image" type="object">
              Contains details about the image source used to create the disk. Optional.
              <Expandable title="properties">
                <ResponseField name="source_image" type="string">
                  Required. The Image resource used when creating the disk. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="compute_scheduling" type="object">
          Scheduling information for VM on maintenance/restart behaviour and node allocation in sole tenant nodes. Options for instance behavior when the host machine undergoes maintenance that may temporarily impact instance performance. Optional.
          <Expandable title="properties">
            <ResponseField name="min_node_cpus" type="number">
              The minimum number of virtual CPUs this instance will consume when running on a sole-tenant node. Ignored if no node_affinites are configured. Optional.
            </ResponseField>
            <ResponseField name="node_affinities" type="list(object)">
              A set of node affinity and anti-affinity configurations for sole tenant nodes. Optional.
              <Expandable title="properties">
                <ResponseField name="key" type="string">
                  The real Compute Engine node affinity label key this scheduling rule matches against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  The real comparison or matching rule an `adaptation_modifiers` entry's own `modifier` is applied under. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="values" type="list(string)">
                  The real, allowed label value(s) this node affinity rule matches against its own `key`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="on_host_maintenance" type="string">
              How the instance should behave when the host machine undergoes maintenance that may temporarily impact instance performance. Optional.
            </ResponseField>
            <ResponseField name="restart_type" type="string">
              Whether the Instance should be automatically restarted whenever it is terminated by Compute Engine (not terminated by user). This configuration is identical to `automaticRestart` field in Compute Engine create instance under scheduling. It was changed to an enum (instead of a boolean) to match the default value in Compute Engine which is automatic restart. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enable_integrity_monitoring" type="bool">
          Optional. Defines whether the instance has integrity monitoring enabled. Optional.
        </ResponseField>
        <ResponseField name="enable_vtpm" type="bool">
          Optional. Defines whether the instance has vTPM enabled. Optional.
        </ResponseField>
        <ResponseField name="encryption" type="object">
          Encryption message describes the details of the applied encryption. Optional.
          <Expandable title="properties">
            <ResponseField name="kms_key" type="string">
              Required. The name of the encryption key that is stored in Google Cloud KMS. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="hostname" type="string">
          Optional. The hostname to assign to the VM. Optional.
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. A map of labels to associate with the VM. Optional.
        </ResponseField>
        <ResponseField name="machine_type" type="string">
          Required. The machine type to create the VM with. Optional.
        </ResponseField>
        <ResponseField name="machine_type_series" type="string">
          Optional. The machine type series to create the VM with. For presentation only. Optional.
        </ResponseField>
        <ResponseField name="metadata" type="map(string)">
          Optional. The metadata key/value pairs to assign to the VM. Optional.
        </ResponseField>
        <ResponseField name="network_interfaces" type="list(object)">
          Optional. NICs to attach to the VM. Optional.
          <Expandable title="properties">
            <ResponseField name="external_ip" type="string">
              The real external (public) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="internal_ip" type="string">
              The real internal (private) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network" type="string">
              The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The real Google Cloud network service tier (`PREMIUM` or `STANDARD`) assigned to a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork" type="string">
              The VPC subnetwork this resource is attached to, in the form `projects/&#123;project&#125;/regions/&#123;region&#125;/subnetworks/&#123;subnetwork&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_tags" type="list(string)">
          Optional. A list of network tags to associate with the VM. Optional.
        </ResponseField>
        <ResponseField name="secure_boot" type="bool">
          Optional. Defines whether the instance has Secure Boot enabled. This can be set to true only if the VM boot option is EFI. Optional.
        </ResponseField>
        <ResponseField name="service_account" type="string">
          Optional. The service account to associate the VM with. Optional.
        </ResponseField>
        <ResponseField name="vm_name" type="string">
          Required. The name of the VM to create. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="zone" type="string">
      The zone in which to create the Persistent Disks. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="compute_engine_target_defaults" type="object">
  ComputeEngineTargetDefaults is a collection of details for creating a VM in a target Compute Engine project. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="adaptation_modifiers" type="list(object)">
      Optional. AdaptationModifiers are the set of modifiers used during OS adaptation. Optional.
      <Expandable title="properties">
        <ResponseField name="modifier" type="string">
          A real, single OS-adaptation adjustment (paired with an `operator`) applied during migration for a specific detected guest-OS condition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The real value of this key/value adaptation modifier or node affinity entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="additional_licenses" type="list(string)">
      Additional licenses to assign to the VM. Optional.
    </ResponseField>
    <ResponseField name="applied_license" type="object">
      AppliedLicense holds the license data returned by adaptation module report. Optional.
      <Expandable title="properties">
        <ResponseField name="os_license" type="string">
          The OS license returned from the adaptation module's report. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          The license type that was used in OS adaptation. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="boot_conversion" type="string">
      Optional. By default the virtual machine will keep its existing boot option. Setting this property will trigger an internal process which will convert the virtual machine from using the existing boot option to another. Optional.
    </ResponseField>
    <ResponseField name="boot_option" type="string">
      Output only. The VM Boot Option, as set in the source VM. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="compute_scheduling" type="object">
      Scheduling information for VM on maintenance/restart behaviour and node allocation in sole tenant nodes. Options for instance behavior when the host machine undergoes maintenance that may temporarily impact instance performance. Optional.
      <Expandable title="properties">
        <ResponseField name="min_node_cpus" type="number">
          The minimum number of virtual CPUs this instance will consume when running on a sole-tenant node. Ignored if no node_affinites are configured. Optional.
        </ResponseField>
        <ResponseField name="node_affinities" type="list(object)">
          A set of node affinity and anti-affinity configurations for sole tenant nodes. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The real Compute Engine node affinity label key this scheduling rule matches against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              The real comparison or matching rule an `adaptation_modifiers` entry's own `modifier` is applied under. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              The real, allowed label value(s) this node affinity rule matches against its own `key`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="on_host_maintenance" type="string">
          How the instance should behave when the host machine undergoes maintenance that may temporarily impact instance performance. Optional.
        </ResponseField>
        <ResponseField name="restart_type" type="string">
          Whether the Instance should be automatically restarted whenever it is terminated by Compute Engine (not terminated by user). This configuration is identical to `automaticRestart` field in Compute Engine create instance under scheduling. It was changed to an enum (instead of a boolean) to match the default value in Compute Engine which is automatic restart. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="disk_replica_zones" type="list(string)">
      Optional. Additional replica zones of the target regional disks. If this list is not empty a regional disk will be created. The first supported zone would be the one stated in the zone field. The rest are taken from this list. Please refer to the [regional disk creation API](https://cloud.google.com/compute/docs/regions-zones/global-regional-zonal-resources) for further details about regional vs zonal disks. If not specified, a zonal disk will be created in the same zone the VM is created. Optional.
    </ResponseField>
    <ResponseField name="disk_type" type="string">
      The disk type to use in the VM. Optional.
    </ResponseField>
    <ResponseField name="disks" type="list(object)">
      Optional. The details of each disk to create. Optional.
      <Expandable title="properties">
        <ResponseField name="additional_labels" type="map(string)">
          Real, extra Compute Engine labels to apply to the migrated VM or disk beyond whatever VM Migration applies automatically. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_name" type="string">
          The real, target-side name to assign to a migrated disk in Google Cloud. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_type" type="string">
          The real Compute Engine disk type (e.g. `pd-ssd`, `pd-standard`) to use for a migrated disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="encryption" type="object">
          Real encryption configuration (e.g. the `kms_key` to use) applied to a migrated disk or VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key" type="string">
              The real Cloud KMS key used to encrypt this migrated resource's data at rest. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_disk_number" type="number">
          The real, source-side index identifying which of the source VM's own disks a target disk or job status corresponds to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vm_attachment_details" type="object">
          Real, target-side details describing how a migrated disk attaches to its VM instance (device name, boot disk status). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="device_name" type="string">
              The real device name a migrated disk is attached to the target VM instance under. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enable_integrity_monitoring" type="bool">
      Optional. Defines whether the instance has integrity monitoring enabled. This can be set to true only if the VM boot option is EFI, and vTPM is enabled. Optional.
    </ResponseField>
    <ResponseField name="enable_vtpm" type="bool">
      Optional. Defines whether the instance has vTPM enabled. This can be set to true only if the VM boot option is EFI. Optional.
    </ResponseField>
    <ResponseField name="encryption" type="object">
      Encryption message describes the details of the applied encryption. Optional.
      <Expandable title="properties">
        <ResponseField name="kms_key" type="string">
          Required. The name of the encryption key that is stored in Google Cloud KMS. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hostname" type="string">
      The hostname to assign to the VM. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      A map of labels to associate with the VM. Optional.
    </ResponseField>
    <ResponseField name="license_type" type="string">
      The license type to use in OS adaptation. Optional.
    </ResponseField>
    <ResponseField name="machine_type" type="string">
      The machine type to create the VM with. Optional.
    </ResponseField>
    <ResponseField name="machine_type_series" type="string">
      The machine type series to create the VM with. Optional.
    </ResponseField>
    <ResponseField name="metadata" type="map(string)">
      The metadata key/value pairs to assign to the VM. Optional.
    </ResponseField>
    <ResponseField name="network_interfaces" type="list(object)">
      List of NICs connected to this VM. Optional.
      <Expandable title="properties">
        <ResponseField name="external_ip" type="string">
          The real external (public) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="internal_ip" type="string">
          The real internal (private) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network" type="string">
          The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_tier" type="string">
          The real Google Cloud network service tier (`PREMIUM` or `STANDARD`) assigned to a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnetwork" type="string">
          The VPC subnetwork this resource is attached to, in the form `projects/&#123;project&#125;/regions/&#123;region&#125;/subnetworks/&#123;subnetwork&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_tags" type="list(string)">
      A list of network tags to associate with the VM. Optional.
    </ResponseField>
    <ResponseField name="secure_boot" type="bool">
      Defines whether the instance has Secure Boot enabled. This can be set to true only if the VM boot option is EFI. Optional.
    </ResponseField>
    <ResponseField name="service_account" type="string">
      Optional. The service account to associate the VM with. Optional.
    </ResponseField>
    <ResponseField name="storage_pool" type="string">
      Optional. If specified this will be the storage pool in which the disk is created. This is the full path of the storage pool resource, for example: "projects/my-project/zones/us-central1-a/storagePools/my-storage-pool". The storage pool must be in the same project and zone as the target disks. The storage pool's type must match the disk type. Optional.
    </ResponseField>
    <ResponseField name="target_project" type="string">
      The full path of the resource of type TargetProject which represents the Compute Engine project in which to create this VM. Optional.
    </ResponseField>
    <ResponseField name="vm_name" type="string">
      The name of the VM to create. Optional.
    </ResponseField>
    <ResponseField name="zone" type="string">
      The zone in which to create the VM. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time the migrating VM was created (this refers to this resource and not to the time it was installed in the source). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="current_sync_info" type="object">
  ReplicationCycle contains information about the current replication cycle status. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cycle_number" type="number">
      The cycle's ordinal number. Optional.
    </ResponseField>
    <ResponseField name="end_time" type="string">
      The time the replication cycle has ended. Optional.
    </ResponseField>
    <ResponseField name="error" type="object">
      The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). Optional.
      <Expandable title="properties">
        <ResponseField name="code" type="number">
          The status code, which should be an enum value of google.rpc.Code. Optional.
        </ResponseField>
        <ResponseField name="details" type="list(map(any))">
          A list of messages that carry the error details. There is a common set of message types for APIs to use. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The identifier of the ReplicationCycle. Optional.
    </ResponseField>
    <ResponseField name="progress_percent" type="number">
      The current progress in percentage of this cycle. Was replaced by 'steps' field, which breaks down the cycle progression more accurately. Optional.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      The time the replication cycle has started. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      State of the ReplicationCycle. Optional.
    </ResponseField>
    <ResponseField name="steps" type="list(object)">
      The cycle's steps list representing its progress. Optional.
      <Expandable title="properties">
        <ResponseField name="end_time" type="string">
          The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="initializing_replication" type="any">
          Real, detailed progress for VM Migration's initializing-replication step, the phase before the first full data sync begins. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="post_processing" type="any">
          Real, detailed progress for VM Migration's post-processing step, the cleanup and finalization work after replication data has landed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="replicating" type="object">
          Real, detailed progress for VM Migration's own active data-replication step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="last_thirty_minutes_average_bytes_per_second" type="string">
              The real, measured average replication throughput, in bytes per second, over the last 30 minutes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="last_two_minutes_average_bytes_per_second" type="string">
              The real, measured average replication throughput, in bytes per second, over the last 2 minutes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replicated_bytes" type="string">
              The real number of bytes already replicated to Google Cloud so far in the current replication cycle. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="total_bytes" type="string">
              The real total number of bytes that need to be replicated to Google Cloud for the current replication cycle to complete. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="start_time" type="string">
          The real timestamp when this migration step or cycle started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="total_pause_duration" type="string">
      The accumulated duration the replication cycle was paused. Optional.
    </ResponseField>
    <ResponseField name="warnings" type="list(object)">
      Output only. Warnings that occurred during the cycle. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="action_item" type="object">
          Real, recommended remediation steps for a migration warning or error, telling the operator what to do about it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="locale" type="string">
              The real, detected or configured locale (language and region) of the source VM's own guest operating system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="code" type="string">
          The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="help_links" type="list(object)">
          Real documentation links attached to a migration warning or error, pointing to more detail on the issue and how to resolve it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="url" type="string">
              A real URL pointing to more information about this migration warning or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="warning_message" type="object">
          The real, human-readable text of one migration warning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="locale" type="string">
              The real, detected or configured locale (language and region) of the source VM's own guest operating system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="warning_time" type="string">
          The real timestamp when this migration warning was raised. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cutover_forecast" type="object">
  CutoverForecast holds information about future CutoverJobs of a MigratingVm. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="estimated_cutover_job_duration" type="string">
      Output only. Estimation of the CutoverJob duration. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  The description attached to the migrating VM by the user. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name attached to the MigratingVm by the user. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="error" type="object">
  The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
      The status code, which should be an enum value of google.rpc.Code. Optional.
    </ResponseField>
    <ResponseField name="details" type="list(map(any))">
      A list of messages that carry the error details. There is a common set of message types for APIs to use. Optional.
    </ResponseField>
    <ResponseField name="message" type="string">
      A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="expiration" type="object">
  Expiration holds information about the expiration of a MigratingVm. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="expire_time" type="string">
      Output only. Timestamp of when this resource is considered expired. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="extendable" type="bool">
      Output only. Describes whether the expiration can be extended. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="extension_count" type="number">
      Output only. The number of times expiration was extended. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="group" type="string">
  Output only. The group this migrating vm is included in, if any. The group is represented by the full path of the appropriate Group resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  The labels of the migrating VM. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="last_replication_cycle" type="object">
  ReplicationCycle contains information about the current replication cycle status. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cycle_number" type="number">
      The cycle's ordinal number. Optional.
    </ResponseField>
    <ResponseField name="end_time" type="string">
      The time the replication cycle has ended. Optional.
    </ResponseField>
    <ResponseField name="error" type="object">
      The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). Optional.
      <Expandable title="properties">
        <ResponseField name="code" type="number">
          The status code, which should be an enum value of google.rpc.Code. Optional.
        </ResponseField>
        <ResponseField name="details" type="list(map(any))">
          A list of messages that carry the error details. There is a common set of message types for APIs to use. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The identifier of the ReplicationCycle. Optional.
    </ResponseField>
    <ResponseField name="progress_percent" type="number">
      The current progress in percentage of this cycle. Was replaced by 'steps' field, which breaks down the cycle progression more accurately. Optional.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      The time the replication cycle has started. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      State of the ReplicationCycle. Optional.
    </ResponseField>
    <ResponseField name="steps" type="list(object)">
      The cycle's steps list representing its progress. Optional.
      <Expandable title="properties">
        <ResponseField name="end_time" type="string">
          The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="initializing_replication" type="any">
          Real, detailed progress for VM Migration's initializing-replication step, the phase before the first full data sync begins. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="post_processing" type="any">
          Real, detailed progress for VM Migration's post-processing step, the cleanup and finalization work after replication data has landed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="replicating" type="object">
          Real, detailed progress for VM Migration's own active data-replication step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="last_thirty_minutes_average_bytes_per_second" type="string">
              The real, measured average replication throughput, in bytes per second, over the last 30 minutes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="last_two_minutes_average_bytes_per_second" type="string">
              The real, measured average replication throughput, in bytes per second, over the last 2 minutes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replicated_bytes" type="string">
              The real number of bytes already replicated to Google Cloud so far in the current replication cycle. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="total_bytes" type="string">
              The real total number of bytes that need to be replicated to Google Cloud for the current replication cycle to complete. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="start_time" type="string">
          The real timestamp when this migration step or cycle started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="total_pause_duration" type="string">
      The accumulated duration the replication cycle was paused. Optional.
    </ResponseField>
    <ResponseField name="warnings" type="list(object)">
      Output only. Warnings that occurred during the cycle. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="action_item" type="object">
          Real, recommended remediation steps for a migration warning or error, telling the operator what to do about it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="locale" type="string">
              The real, detected or configured locale (language and region) of the source VM's own guest operating system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="code" type="string">
          The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="help_links" type="list(object)">
          Real documentation links attached to a migration warning or error, pointing to more detail on the issue and how to resolve it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="url" type="string">
              A real URL pointing to more information about this migration warning or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="warning_message" type="object">
          The real, human-readable text of one migration warning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="locale" type="string">
              The real, detected or configured locale (language and region) of the source VM's own guest operating system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="warning_time" type="string">
          The real timestamp when this migration warning was raised. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="last_sync" type="object">
  ReplicationSync contain information about the last replica sync to the cloud. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="last_sync_time" type="string">
      The most updated snapshot created time in the source that finished replication. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The identifier of the MigratingVm. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="policy" type="object">
  A policy for scheduling replications. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="idle_duration" type="string">
      The idle duration between replication stages. Optional.
    </ResponseField>
    <ResponseField name="skip_os_adaptation" type="bool">
      A flag to indicate whether to skip OS adaptation during the replication sync. OS adaptation is a process where the VM's operating system undergoes changes and adaptations to fully function on Compute Engine. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="recent_clone_jobs" type="list(object)">
  Output only. The recent clone jobs performed on the migrating VM. This field holds the vm's last completed clone job and the vm's running clone job, if one exists. Note: To have this field populated you need to explicitly request it via the "view" parameter of the Get/List request. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="compute_engine_disks_target_details" type="object">
      Real, target-side Compute Engine disk configuration (type, size, name) for a migrated disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="disks" type="list(object)">
          The real, target-side disk configuration for a migrated VM's own disks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="disk_uri" type="string">
              The real Compute Engine resource URI of one disk involved in this migration job. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_disk_number" type="number">
              The real, source-side index identifying which of the source VM's own disks a target disk or job status corresponds to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disks_target_details" type="any">
          Real, target-side migration configuration for the source VM's own individual disks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vm_target_details" type="object">
          Real, target-side configuration for the migrated VM instance itself, distinct from its individual disks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="vm_uri" type="string">
              The real Compute Engine resource URI of the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="compute_engine_target_details" type="object">
      Real, target-side Compute Engine configuration (machine type, zone, network, disks) the source VM migrates into. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="adaptation_modifiers" type="list(object)">
          Real, fine-grained adjustments applied during OS adaptation for specific guest OS quirks encountered on the source VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="modifier" type="string">
              A real, single OS-adaptation adjustment (paired with an `operator`) applied during migration for a specific detected guest-OS condition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The real value of this key/value adaptation modifier or node affinity entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="additional_licenses" type="list(string)">
          Extra real Google Cloud licenses to attach to the migrated VM, beyond whatever license VM Migration infers automatically from the source OS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="applied_license" type="object">
          The real license type VM Migration actually applied to the migrated VM, after resolving any requested `license_type`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="os_license" type="string">
              The real, detected operating system license of the source VM, used to determine appropriate licensing for the migrated VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="boot_conversion" type="string">
          Real settings and progress for converting the source VM's own boot mode (e.g. BIOS to UEFI) during migration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="boot_option" type="string">
          Which real boot mode (BIOS or EFI) the migrated VM instance is configured to use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="compute_scheduling" type="object">
          Real Compute Engine scheduling configuration (host maintenance behavior, restart policy, node affinity) for the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="min_node_cpus" type="number">
              The real minimum number of virtual CPUs the migrated VM instance requires from its sole-tenant node, when node-affinity scheduling is used. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_affinities" type="list(object)">
              Real sole-tenant node affinity rules controlling which Compute Engine nodes the migrated VM instance can be scheduled onto. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="key" type="string">
                  The real Compute Engine node affinity label key this scheduling rule matches against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  The real comparison or matching rule an `adaptation_modifiers` entry's own `modifier` is applied under. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="values" type="list(string)">
                  The real, allowed label value(s) this node affinity rule matches against its own `key`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="on_host_maintenance" type="string">
              The real Compute Engine host-maintenance behavior (`MIGRATE` or `TERMINATE`) for the migrated VM instance during a host maintenance event. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="restart_type" type="string">
              The real Compute Engine automatic-restart policy for the migrated VM instance after a crash or maintenance event. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disk_replica_zones" type="list(string)">
          The real, additional Compute Engine zones a migrated disk is replicated to, for a regional persistent disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_type" type="string">
          The real Compute Engine disk type (e.g. `pd-ssd`, `pd-standard`) to use for a migrated disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enable_integrity_monitoring" type="bool">
          Whether Shielded VM integrity monitoring is real, enabled on the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enable_vtpm" type="bool">
          Whether a Shielded VM virtual Trusted Platform Module is real, enabled on the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="encryption" type="object">
          Real encryption configuration (e.g. the `kms_key` to use) applied to a migrated disk or VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key" type="string">
              The real Cloud KMS key used to encrypt this migrated resource's data at rest. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="hostname" type="string">
          The real hostname to assign to the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. User-provided key/value labels on this resource, usable for organizing and filtering resources in Cloud Billing and the console. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="license_type" type="string">
          Which real licensing model applies to the migrated VM: the source's own existing license (BYOL) or a new Google Cloud-provided one (PAYG). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="machine_type" type="string">
          The real target Compute Engine machine type (e.g. `e2-standard-4`) to use for the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="machine_type_series" type="string">
          The real target Compute Engine machine type series (e.g. `e2`, `n2`) to select a machine type from, when an exact `machine_type` isn't pinned. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="metadata" type="map(string)">
          Real Compute Engine instance metadata key/value pairs to apply to the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_interfaces" type="list(object)">
          The real, target-side network interface configuration for the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ip" type="string">
              The real external (public) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="internal_ip" type="string">
              The real internal (private) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network" type="string">
              The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The real Google Cloud network service tier (`PREMIUM` or `STANDARD`) assigned to a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork" type="string">
              The VPC subnetwork this resource is attached to, in the form `projects/&#123;project&#125;/regions/&#123;region&#125;/subnetworks/&#123;subnetwork&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_tags" type="list(string)">
          Real Compute Engine network tags to apply to the migrated VM instance, used to match firewall rules and routes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="project" type="string">
          The real Google Cloud project ID this migration resource applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="secure_boot" type="bool">
          Whether Shielded VM Secure Boot is real, enabled on the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_account" type="string">
          The service account this resource acts as (or is managed by) when calling other Google Cloud APIs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="storage_pool" type="string">
          A reference to the real Compute Engine storage pool a migrated disk should be provisioned from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vm_name" type="string">
          The real, target-side name to assign to the migrated VM instance in Google Cloud. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="zone" type="string">
          The real Compute Engine zone the migrated VM instance or disk is created in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. The timestamp when this resource was created. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="end_time" type="string">
      The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="error" type="object">
      The real error, if any, that caused this migration step or job to fail. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="code" type="number">
          The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="details" type="list(map(any))">
          Real, structured detail data attached to a migration error, giving more specific context than its own `message` alone. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The real, target-side name given to this migration step, sync, or final-sync entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state" type="string">
      The real, current state of this migration job, step, or resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state_time" type="string">
      The real timestamp when this resource most recently entered its current `state`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="steps" type="list(object)">
      The real, ordered sequence of steps (such as `initializing_replication`, `replicating`, `post_processing`) this migration cycle progresses through. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="adapting_os" type="any">
          Real, detailed progress for VM Migration's own guest OS adaptation step, adjusting drivers and boot configuration for Google Cloud. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="end_time" type="string">
          The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instantiating_migrated_vm" type="any">
          Real, detailed progress for VM Migration's own step creating the actual Compute Engine VM instance from the migrated disks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="preparing_vm_disks" type="any">
          Real, detailed progress for VM Migration's own step preparing the migrated VM's disks in Google Cloud ahead of instantiation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_time" type="string">
          The real timestamp when this migration step or cycle started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="recent_cutover_jobs" type="list(object)">
  Output only. The recent cutover jobs performed on the migrating VM. This field holds the vm's last completed cutover job and the vm's running cutover job, if one exists. Note: To have this field populated you need to explicitly request it via the "view" parameter of the Get/List request. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="compute_engine_disks_target_details" type="object">
      Real, target-side Compute Engine disk configuration (type, size, name) for a migrated disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="disks" type="list(object)">
          The real, target-side disk configuration for a migrated VM's own disks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="disk_uri" type="string">
              The real Compute Engine resource URI of one disk involved in this migration job. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_disk_number" type="number">
              The real, source-side index identifying which of the source VM's own disks a target disk or job status corresponds to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disks_target_details" type="any">
          Real, target-side migration configuration for the source VM's own individual disks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vm_target_details" type="object">
          Real, target-side configuration for the migrated VM instance itself, distinct from its individual disks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="vm_uri" type="string">
              The real Compute Engine resource URI of the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="compute_engine_target_details" type="object">
      Real, target-side Compute Engine configuration (machine type, zone, network, disks) the source VM migrates into. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="adaptation_modifiers" type="list(object)">
          Real, fine-grained adjustments applied during OS adaptation for specific guest OS quirks encountered on the source VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="modifier" type="string">
              A real, single OS-adaptation adjustment (paired with an `operator`) applied during migration for a specific detected guest-OS condition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The real value of this key/value adaptation modifier or node affinity entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="additional_licenses" type="list(string)">
          Extra real Google Cloud licenses to attach to the migrated VM, beyond whatever license VM Migration infers automatically from the source OS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="applied_license" type="object">
          The real license type VM Migration actually applied to the migrated VM, after resolving any requested `license_type`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="os_license" type="string">
              The real, detected operating system license of the source VM, used to determine appropriate licensing for the migrated VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="boot_conversion" type="string">
          Real settings and progress for converting the source VM's own boot mode (e.g. BIOS to UEFI) during migration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="boot_option" type="string">
          Which real boot mode (BIOS or EFI) the migrated VM instance is configured to use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="compute_scheduling" type="object">
          Real Compute Engine scheduling configuration (host maintenance behavior, restart policy, node affinity) for the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="min_node_cpus" type="number">
              The real minimum number of virtual CPUs the migrated VM instance requires from its sole-tenant node, when node-affinity scheduling is used. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_affinities" type="list(object)">
              Real sole-tenant node affinity rules controlling which Compute Engine nodes the migrated VM instance can be scheduled onto. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="key" type="string">
                  The real Compute Engine node affinity label key this scheduling rule matches against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  The real comparison or matching rule an `adaptation_modifiers` entry's own `modifier` is applied under. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="values" type="list(string)">
                  The real, allowed label value(s) this node affinity rule matches against its own `key`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="on_host_maintenance" type="string">
              The real Compute Engine host-maintenance behavior (`MIGRATE` or `TERMINATE`) for the migrated VM instance during a host maintenance event. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="restart_type" type="string">
              The real Compute Engine automatic-restart policy for the migrated VM instance after a crash or maintenance event. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disk_replica_zones" type="list(string)">
          The real, additional Compute Engine zones a migrated disk is replicated to, for a regional persistent disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_type" type="string">
          The real Compute Engine disk type (e.g. `pd-ssd`, `pd-standard`) to use for a migrated disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enable_integrity_monitoring" type="bool">
          Whether Shielded VM integrity monitoring is real, enabled on the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enable_vtpm" type="bool">
          Whether a Shielded VM virtual Trusted Platform Module is real, enabled on the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="encryption" type="object">
          Real encryption configuration (e.g. the `kms_key` to use) applied to a migrated disk or VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key" type="string">
              The real Cloud KMS key used to encrypt this migrated resource's data at rest. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="hostname" type="string">
          The real hostname to assign to the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. User-provided key/value labels on this resource, usable for organizing and filtering resources in Cloud Billing and the console. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="license_type" type="string">
          Which real licensing model applies to the migrated VM: the source's own existing license (BYOL) or a new Google Cloud-provided one (PAYG). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="machine_type" type="string">
          The real target Compute Engine machine type (e.g. `e2-standard-4`) to use for the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="machine_type_series" type="string">
          The real target Compute Engine machine type series (e.g. `e2`, `n2`) to select a machine type from, when an exact `machine_type` isn't pinned. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="metadata" type="map(string)">
          Real Compute Engine instance metadata key/value pairs to apply to the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_interfaces" type="list(object)">
          The real, target-side network interface configuration for the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ip" type="string">
              The real external (public) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="internal_ip" type="string">
              The real internal (private) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network" type="string">
              The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The real Google Cloud network service tier (`PREMIUM` or `STANDARD`) assigned to a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork" type="string">
              The VPC subnetwork this resource is attached to, in the form `projects/&#123;project&#125;/regions/&#123;region&#125;/subnetworks/&#123;subnetwork&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_tags" type="list(string)">
          Real Compute Engine network tags to apply to the migrated VM instance, used to match firewall rules and routes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="project" type="string">
          The real Google Cloud project ID this migration resource applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="secure_boot" type="bool">
          Whether Shielded VM Secure Boot is real, enabled on the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_account" type="string">
          The service account this resource acts as (or is managed by) when calling other Google Cloud APIs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="storage_pool" type="string">
          A reference to the real Compute Engine storage pool a migrated disk should be provisioned from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vm_name" type="string">
          The real, target-side name to assign to the migrated VM instance in Google Cloud. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="zone" type="string">
          The real Compute Engine zone the migrated VM instance or disk is created in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. The timestamp when this resource was created. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="end_time" type="string">
      The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="error" type="object">
      The real error, if any, that caused this migration step or job to fail. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="code" type="number">
          The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="details" type="list(map(any))">
          Real, structured detail data attached to a migration error, giving more specific context than its own `message` alone. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The real, target-side name given to this migration step, sync, or final-sync entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="progress_percent" type="number">
      Output only. The real, measured completion percentage (0-100) of the current migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state" type="string">
      The real, current state of this migration job, step, or resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state_message" type="string">
      Real, additional detail explaining this migrating VM's own current cutover job state. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state_time" type="string">
      The real timestamp when this resource most recently entered its current `state`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="steps" type="list(object)">
      The real, ordered sequence of steps (such as `initializing_replication`, `replicating`, `post_processing`) this migration cycle progresses through. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="end_time" type="string">
          The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="final_sync" type="object">
          Real, detailed progress for VM Migration's own final-sync step, the last data sync performed immediately before cutover. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cycle_number" type="number">
              The real, sequential number of the current replication cycle for this migrating VM, incrementing each time a new sync cycle starts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="end_time" type="string">
              The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="error" type="object">
              The real error, if any, that caused this migration step or job to fail. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="number">
                  The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="details" type="list(map(any))">
                  Real, structured detail data attached to a migration error, giving more specific context than its own `message` alone. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              The real, target-side name given to this migration step, sync, or final-sync entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="progress_percent" type="number">
              Output only. The real, measured completion percentage (0-100) of the current migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="start_time" type="string">
              The real timestamp when this migration step or cycle started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              The real, current state of this migration job, step, or resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="steps" type="list(object)">
              The real, ordered sequence of steps (such as `initializing_replication`, `replicating`, `post_processing`) this migration cycle progresses through. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <Note>`steps` recurs here (its own type contains itself); see the `steps` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
            <ResponseField name="total_pause_duration" type="string">
              The real total time this migration job has spent paused, across every pause since it started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="warnings" type="list(object)">
              Real, non-fatal warnings raised during this migration job, each with its own `warning_message`, severity, and `action_item`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="action_item" type="object">
                  Real, recommended remediation steps for a migration warning or error, telling the operator what to do about it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="locale" type="string">
                      The real, detected or configured locale (language and region) of the source VM's own guest operating system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="code" type="string">
                  The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="help_links" type="list(object)">
                  Real documentation links attached to a migration warning or error, pointing to more detail on the issue and how to resolve it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="description" type="string">
                      A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="url" type="string">
                      A real URL pointing to more information about this migration warning or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="warning_message" type="object">
                  The real, human-readable text of one migration warning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="locale" type="string">
                      The real, detected or configured locale (language and region) of the source VM's own guest operating system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="warning_time" type="string">
                  The real timestamp when this migration warning was raised. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="instantiating_migrated_vm" type="any">
          Real, detailed progress for VM Migration's own step creating the actual Compute Engine VM instance from the migrated disks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="preparing_vm_disks" type="any">
          Real, detailed progress for VM Migration's own step preparing the migrated VM's disks in Google Cloud ahead of instantiation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="previous_replication_cycle" type="object">
          The real, previous replication cycle for this migrating VM, kept for reference alongside the current one. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cycle_number" type="number">
              The real, sequential number of the current replication cycle for this migrating VM, incrementing each time a new sync cycle starts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="end_time" type="string">
              The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="error" type="object">
              The real error, if any, that caused this migration step or job to fail. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="number">
                  The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="details" type="list(map(any))">
                  Real, structured detail data attached to a migration error, giving more specific context than its own `message` alone. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              The real, target-side name given to this migration step, sync, or final-sync entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="progress_percent" type="number">
              Output only. The real, measured completion percentage (0-100) of the current migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="start_time" type="string">
              The real timestamp when this migration step or cycle started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              The real, current state of this migration job, step, or resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="steps" type="list(object)">
              The real, ordered sequence of steps (such as `initializing_replication`, `replicating`, `post_processing`) this migration cycle progresses through. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <Note>`steps` recurs here (its own type contains itself); see the `steps` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
            <ResponseField name="total_pause_duration" type="string">
              The real total time this migration job has spent paused, across every pause since it started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="warnings" type="list(object)">
              Real, non-fatal warnings raised during this migration job, each with its own `warning_message`, severity, and `action_item`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="action_item" type="object">
                  Real, recommended remediation steps for a migration warning or error, telling the operator what to do about it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="locale" type="string">
                      The real, detected or configured locale (language and region) of the source VM's own guest operating system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="code" type="string">
                  The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="help_links" type="list(object)">
                  Real documentation links attached to a migration warning or error, pointing to more detail on the issue and how to resolve it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="description" type="string">
                      A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="url" type="string">
                      A real URL pointing to more information about this migration warning or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="warning_message" type="object">
                  The real, human-readable text of one migration warning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="locale" type="string">
                      The real, detected or configured locale (language and region) of the source VM's own guest operating system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="warning_time" type="string">
                  The real timestamp when this migration warning was raised. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="shutting_down_source_vm" type="any">
          Real, detailed progress for VM Migration's own step shutting down the source VM as part of a cutover. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_time" type="string">
          The real timestamp when this migration step or cycle started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_vm_id" type="string">
  The unique ID of the VM in the source. The VM's name in vSphere can be changed, so this is not the VM's name but rather its moRef id. This id is of the form vm-. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the MigratingVm. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state_time" type="string">
  Output only. The last time the migrating VM state was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The last time the migrating VM resource was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="vmware_source_vm_details" type="object">
  Represent the source Vmware VM details. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="architecture" type="string">
      Output only. The VM architecture. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="committed_storage_bytes" type="string">
      Output only. The total size of the disks being migrated in bytes. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="disks" type="list(object)">
      Output only. The disks attached to the source VM. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="disk_number" type="number">
          The real, source-side index identifying which of the source VM's disks this migration status entry describes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="label" type="string">
          The real, source-side disk label of one disk on a VMware-sourced migrating VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="size_gb" type="string">
          The real, provisioned size, in GiB, of a migrated disk. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="firmware" type="string">
      Output only. The firmware type of the source VM. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="vm_capabilities_info" type="object">
      Migrating VM source information about the VM capabilities needed for some Compute Engine features. Optional.
      <Expandable title="properties">
        <ResponseField name="last_os_capabilities_update_time" type="string">
          Output only. The last time OS capabilities list was updated. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="os_capabilities" type="list(string)">
          Output only. Unordered list. List of certain VM OS capabilities needed for some Compute Engine features. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
