---
title: "google_vmmigration_disk_migration_job"
description: "`google_vmmigration_disk_migration_job` migrates a real, standalone disk (not an entire running VM) to Google Cloud Persistent Disk..."
---

`google_vmmigration_disk_migration_job` migrates a real, standalone disk (not an entire running VM) to Google Cloud Persistent Disk, for cases where only the data, not the source machine's live state, needs to move.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_vmmigration --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_vmmigration/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmmigration "github.com/ubiquex/ubx-sdk-google/sdk/go/google_vmmigration/vmmigration"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own disk migration job"})

    		ubx.Resource(vmmigration.VmmigrationDiskMigrationJob, "ci-runner", vmmigration.VmmigrationDiskMigrationJobConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_vmmigration --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmmigrationDiskMigrationJob } from "./local-sdk/google_vmmigration/sdk/typescript/google_vmmigration/vmmigration/vmmigration_disk_migration_job.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own disk migration job" });

      resource(VmmigrationDiskMigrationJob, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_vmmigration --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_vmmigration/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_vmmigration.vmmigration import VmmigrationDiskMigrationJob, VmmigrationDiskMigrationJobConfig

    def describe():
        ubx.intent("payments own disk migration job")

        ubx.resource(VmmigrationDiskMigrationJob, "ci-runner", VmmigrationDiskMigrationJobConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vmmigration_disk_migration_job called "ci-runner",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="aws_source_disk_details" type="object">
  Represents the source AWS Disk details. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="disk_type" type="string">
      Optional. Output only. Disk type. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="size_gib" type="string">
      Output only. Size in GiB. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="tags" type="map(string)">
      Optional. Output only. A map of AWS volume tags. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="volume_id" type="string">
      Required. AWS volume ID. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time the DiskMigrationJob resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="errors" type="list(object)">
  Output only. Provides details on the errors that led to the disk migration job's state in case of an error. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
      The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="details" type="list(map(any))">
      Real, structured detail data attached to a migration error, giving more specific context than its own `message` alone. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="message" type="string">
      A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. The identifier of the DiskMigrationJob. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the DiskMigrationJob. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="steps" type="list(object)">
  Output only. The disk migration steps list representing its progress. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="copying_source_disk_snapshot" type="any">
      Real, detailed progress for VM Migration's own step copying a snapshot of the source disk to Google Cloud, for a standalone disk migration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="creating_source_disk_snapshot" type="any">
      Real, detailed progress for VM Migration's own step creating a snapshot of the source disk, for a standalone disk migration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="end_time" type="string">
      The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="provisioning_target_disk" type="any">
      Real, detailed progress for VM Migration's own step provisioning the target Persistent Disk in Google Cloud, for a standalone disk migration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="start_time" type="string">
      The real timestamp when this migration step or cycle started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_details" type="object">
  Details of the target disk in Compute Engine. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="encryption" type="object">
      Encryption message describes the details of the applied encryption. Optional.
      <Expandable title="properties">
        <ResponseField name="kms_key" type="string">
          Required. The name of the encryption key that is stored in Google Cloud KMS. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Optional. A map of labels to associate with the disk. Optional.
    </ResponseField>
    <ResponseField name="target_disk" type="object">
      Compute Engine disk target details. Optional.
      <Expandable title="properties">
        <ResponseField name="disk_id" type="string">
          Optional. Target Compute Engine Disk ID. This is the resource ID segment of the Compute Engine Disk to create. In the resource name compute/v1/projects/&#123;project&#125;/zones/&#123;zone&#125;/disks/disk1 "disk1" is the resource ID for the disk. Optional.
        </ResponseField>
        <ResponseField name="disk_type" type="string">
          Required. The disk type to use. Optional.
        </ResponseField>
        <ResponseField name="replica_zones" type="list(string)">
          Optional. Replication zones of the regional disk. Should be of the form: projects/&#123;target-project&#125;/locations/&#123;replica-zone&#125; Currently only one replica zone is supported. Optional.
        </ResponseField>
        <ResponseField name="zone" type="string">
          Required. The Compute Engine zone in which to create the disk. Should be of the form: projects/&#123;target-project&#125;/locations/&#123;zone&#125; Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target_project" type="string">
      Required. The name of the resource of type TargetProject which represents the Compute Engine project in which to create the disk. Should be of the form: projects/&#123;project&#125;/locations/global/targetProjects/&#123;target-project&#125; Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The last time the DiskMigrationJob resource was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
