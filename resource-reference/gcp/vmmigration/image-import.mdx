---
title: "google_vmmigration_image_import"
description: "`google_vmmigration_image_import` imports a real, existing disk image into VM Migration's own managed image format..."
---

`google_vmmigration_image_import` imports a real, existing disk image into VM Migration's own managed image format, a source for creating migrated VMs from an image rather than from a live, running source VM.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmmigration "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vmmigration"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own image import"})

    		ubx.Resource(vmmigration.ImageImport, "example", vmmigration.ImageImportConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ImageImport } from "@ubx/sdk-google/google/vmmigration/image_import";

    export default stack("example", () => {
      intent({ summary: "example own image import" });

      resource(ImageImport, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vmmigration import ImageImport, ImageImportConfig

    def describe():
        ubx.intent("example own image import")

        ubx.resource(ImageImport, "example", ImageImportConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vmmigration_image_import called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="cloud_storage_uri" type="string">
  Immutable. The path to the Cloud Storage file from which the image should be imported. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time the image import was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="disk_image_target_defaults" type="object">
  The target details of the image resource that will be created by the import job. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="additional_licenses" type="list(string)">
      Optional. Additional licenses to assign to the image. Format: https://www.googleapis.com/compute/v1/projects/PROJECT_ID/global/licenses/LICENSE_NAME Or https://www.googleapis.com/compute/beta/projects/PROJECT_ID/global/licenses/LICENSE_NAME Optional.
    </ResponseField>
    <ResponseField name="data_disk_image_import" type="object">
      Used when the image import is not using OS adaptation process. Optional.
      <Expandable title="properties">
        <ResponseField name="guest_os_features" type="list(string)">
          Optional. A list of guest OS features to apply to the imported image. These features are flags that are used by Compute Engine to enable certain capabilities for virtual machine instances that are created from the image. This field does not change the OS of the image; it only marks the image with the specified features. The user must ensure that the OS is compatible with the features. For a list of available features, see https://cloud.google.com/compute/docs/images/create-custom#guest-os-features. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional. An optional description of the image. Optional.
    </ResponseField>
    <ResponseField name="encryption" type="object">
      Encryption message describes the details of the applied encryption. Optional.
      <Expandable title="properties">
        <ResponseField name="kms_key" type="string">
          Required. The name of the encryption key that is stored in Google Cloud KMS. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="family_name" type="string">
      Optional. The name of the image family to which the new image belongs. Optional.
    </ResponseField>
    <ResponseField name="image_name" type="string">
      Required. The name of the image to be created. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Optional. A map of labels to associate with the image. Optional.
    </ResponseField>
    <ResponseField name="os_adaptation_parameters" type="object">
      Parameters affecting the OS adaptation process. Optional.
      <Expandable title="properties">
        <ResponseField name="adaptation_modifiers" type="list(object)">
          Optional. Modifiers to be used as configuration of the OS adaptation process. Optional.
          <Expandable title="properties">
            <ResponseField name="modifier" type="string">
              A real, single OS-adaptation adjustment (paired with an `operator`) applied during migration for a specific detected guest-OS condition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The real value of this key/value adaptation modifier or node affinity entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="boot_conversion" type="string">
          Optional. By default the image will keep its existing boot option. Setting this property will trigger an internal process which will convert the image from using the existing boot option to another. The size of the boot disk might be increased to allow the conversion Optional.
        </ResponseField>
        <ResponseField name="generalize" type="bool">
          Optional. Set to true in order to generalize the imported image. The generalization process enables co-existence of multiple VMs created from the same image. For Windows, generalizing the image removes computer-specific information such as installed drivers and the computer security identifier (SID). Optional.
        </ResponseField>
        <ResponseField name="license_type" type="string">
          Optional. Choose which type of license to apply to the imported image. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="single_region_storage" type="bool">
      Optional. Set to true to set the image storageLocations to the single region of the import job. When false, the closest multi-region is selected. Optional.
    </ResponseField>
    <ResponseField name="target_project" type="string">
      Required. Reference to the TargetProject resource that represents the target project in which the imported image will be created. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="encryption" type="object">
  Encryption message describes the details of the applied encryption. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="kms_key" type="string">
      Required. The name of the encryption key that is stored in Google Cloud KMS. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="machine_image_target_defaults" type="object">
  The target details of the machine image resource that will be created by the image import job. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="additional_licenses" type="list(string)">
      Optional. Additional licenses to assign to the instance created by the machine image. Format: https://www.googleapis.com/compute/v1/projects/PROJECT_ID/global/licenses/LICENSE_NAME Or https://www.googleapis.com/compute/beta/projects/PROJECT_ID/global/licenses/LICENSE_NAME Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional. An optional description of the machine image. Optional.
    </ResponseField>
    <ResponseField name="encryption" type="object">
      Encryption message describes the details of the applied encryption. Optional.
      <Expandable title="properties">
        <ResponseField name="kms_key" type="string">
          Required. The name of the encryption key that is stored in Google Cloud KMS. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Optional. The labels to apply to the instance created by the machine image. Optional.
    </ResponseField>
    <ResponseField name="machine_image_name" type="string">
      Required. The name of the machine image to be created. Optional.
    </ResponseField>
    <ResponseField name="machine_image_parameters_overrides" type="object">
      Parameters overriding decisions based on the source machine image configurations. Optional.
      <Expandable title="properties">
        <ResponseField name="machine_type" type="string">
          Optional. The machine type to create the MachineImage with. If empty, the service will choose a relevant machine type based on the information from the source image. For more information about machine types, please refer to https://cloud.google.com/compute/docs/machine-resource. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_interfaces" type="list(object)">
      Optional. The network interfaces to create with the instance created by the machine image. Internal and external IP addresses, and network tiers are ignored for machine image import. Optional.
      <Expandable title="properties">
        <ResponseField name="external_ip" type="string">
          The real external (public) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="internal_ip" type="string">
          The real internal (private) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network" type="string">
          The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_tier" type="string">
          The real Google Cloud network service tier (`PREMIUM` or `STANDARD`) assigned to a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnetwork" type="string">
          The VPC subnetwork this resource is attached to, in the form `projects/&#123;project&#125;/regions/&#123;region&#125;/subnetworks/&#123;subnetwork&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="os_adaptation_parameters" type="object">
      Parameters affecting the OS adaptation process. Optional.
      <Expandable title="properties">
        <ResponseField name="adaptation_modifiers" type="list(object)">
          Optional. Modifiers to be used as configuration of the OS adaptation process. Optional.
          <Expandable title="properties">
            <ResponseField name="modifier" type="string">
              A real, single OS-adaptation adjustment (paired with an `operator`) applied during migration for a specific detected guest-OS condition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The real value of this key/value adaptation modifier or node affinity entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="boot_conversion" type="string">
          Optional. By default the image will keep its existing boot option. Setting this property will trigger an internal process which will convert the image from using the existing boot option to another. The size of the boot disk might be increased to allow the conversion Optional.
        </ResponseField>
        <ResponseField name="generalize" type="bool">
          Optional. Set to true in order to generalize the imported image. The generalization process enables co-existence of multiple VMs created from the same image. For Windows, generalizing the image removes computer-specific information such as installed drivers and the computer security identifier (SID). Optional.
        </ResponseField>
        <ResponseField name="license_type" type="string">
          Optional. Choose which type of license to apply to the imported image. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_account" type="object">
      Service account to assign to the instance created by the machine image. Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          Required. The email address of the service account. Optional.
        </ResponseField>
        <ResponseField name="scopes" type="list(string)">
          Optional. The list of scopes to be made available for this service account. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="shielded_instance_config" type="object">
      Shielded instance configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="enable_integrity_monitoring" type="bool">
          Optional. Defines whether the instance created by the machine image has integrity monitoring enabled. This can be set to true only if the image boot option is EFI, and vTPM is enabled. Optional.
        </ResponseField>
        <ResponseField name="enable_vtpm" type="bool">
          Optional. Defines whether the instance created by the machine image has vTPM enabled. This can be set to true only if the image boot option is EFI. Optional.
        </ResponseField>
        <ResponseField name="secure_boot" type="string">
          Optional. Defines whether the instance created by the machine image has Secure Boot enabled. This can be set to true only if the image boot option is EFI. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="single_region_storage" type="bool">
      Optional. Set to true to set the machine image storageLocations to the single region of the import job. When false, the closest multi-region is selected. Optional.
    </ResponseField>
    <ResponseField name="skip_os_adaptation" type="any">
      Mentions that the machine image import is not using OS adaptation process. Optional.
    </ResponseField>
    <ResponseField name="tags" type="list(string)">
      Optional. The tags to apply to the instance created by the machine image. Optional.
    </ResponseField>
    <ResponseField name="target_project" type="string">
      Required. Reference to the TargetProject resource that represents the target project in which the imported machine image will be created. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource path of the ImageImport. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="recent_image_import_jobs" type="list(object)">
  Output only. The result of the most recent runs for this ImageImport. All jobs for this ImageImport can be listed via ListImageImportJobs. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cloud_storage_uri" type="string">
      The real Cloud Storage URI of the image file this VM Migration image import job is importing from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. The timestamp when this resource was created. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="created_resources" type="list(string)">
      The real Google Cloud resource(s) (such as a Compute Engine image) this image import job created. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disk_image_target_details" type="object">
      Real, target-side configuration for importing the source image as a standalone Compute Engine disk image. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="additional_licenses" type="list(string)">
          Extra real Google Cloud licenses to attach to the migrated VM, beyond whatever license VM Migration infers automatically from the source OS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="data_disk_image_import" type="object">
          Real configuration for importing a data disk image (as opposed to a bootable image) via VM Migration's own image import. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="guest_os_features" type="list(string)">
              Real, Compute Engine guest OS feature flags (e.g. `UEFI_COMPATIBLE`) to set on the imported image. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="encryption" type="object">
          Real encryption configuration (e.g. the `kms_key` to use) applied to a migrated disk or VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key" type="string">
              The real Cloud KMS key used to encrypt this migrated resource's data at rest. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="family_name" type="string">
          A person's family (last) name. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="image_name" type="string">
          The real, target-side name to assign to the imported Compute Engine image. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. User-provided key/value labels on this resource, usable for organizing and filtering resources in Cloud Billing and the console. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="os_adaptation_parameters" type="object">
          Real, additional parameters supplied to guide VM Migration's own guest OS adaptation step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="adaptation_modifiers" type="list(object)">
              Real, fine-grained adjustments applied during OS adaptation for specific guest OS quirks encountered on the source VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="modifier" type="string">
                  A real, single OS-adaptation adjustment (paired with an `operator`) applied during migration for a specific detected guest-OS condition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The real value of this key/value adaptation modifier or node affinity entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="boot_conversion" type="string">
              Real settings and progress for converting the source VM's own boot mode (e.g. BIOS to UEFI) during migration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="generalize" type="bool">
              Whether VM Migration should generalize the source VM's own OS image (removing machine-specific identity) before migrating it, similar to running sysprep. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="license_type" type="string">
              Which real licensing model applies to the migrated VM: the source's own existing license (BYOL) or a new Google Cloud-provided one (PAYG). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="single_region_storage" type="bool">
          Whether the migrated VM's own boot disk should use single-region (rather than multi-region) Cloud Storage during the migration process. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_project" type="string">
          A reference to the real `google_vmmigration_target_project` this migrated VM or disk should land in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="end_time" type="string">
      The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="errors" type="list(object)">
      Real errors, if any, that occurred while running this image import job. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="code" type="number">
          The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="details" type="list(map(any))">
          Real, structured detail data attached to a migration error, giving more specific context than its own `message` alone. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="machine_image_target_details" type="object">
      Real, target-side configuration for importing the source image as a Compute Engine machine image (a full VM template) rather than a plain disk image. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="additional_licenses" type="list(string)">
          Extra real Google Cloud licenses to attach to the migrated VM, beyond whatever license VM Migration infers automatically from the source OS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="encryption" type="object">
          Real encryption configuration (e.g. the `kms_key` to use) applied to a migrated disk or VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key" type="string">
              The real Cloud KMS key used to encrypt this migrated resource's data at rest. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. User-provided key/value labels on this resource, usable for organizing and filtering resources in Cloud Billing and the console. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="machine_image_name" type="string">
          The real, target-side name to assign to the imported Compute Engine machine image. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="machine_image_parameters_overrides" type="object">
          Real, target-side property overrides (like `tags` or `shielded_instance_config`) applied to the imported machine image, beyond what's inferred from the source. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="machine_type" type="string">
              The real target Compute Engine machine type (e.g. `e2-standard-4`) to use for the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_interfaces" type="list(object)">
          The real, target-side network interface configuration for the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ip" type="string">
              The real external (public) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="internal_ip" type="string">
              The real internal (private) IP address configuration for a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network" type="string">
              The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The real Google Cloud network service tier (`PREMIUM` or `STANDARD`) assigned to a migrated VM's network interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork" type="string">
              The VPC subnetwork this resource is attached to, in the form `projects/&#123;project&#125;/regions/&#123;region&#125;/subnetworks/&#123;subnetwork&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="os_adaptation_parameters" type="object">
          Real, additional parameters supplied to guide VM Migration's own guest OS adaptation step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="adaptation_modifiers" type="list(object)">
              Real, fine-grained adjustments applied during OS adaptation for specific guest OS quirks encountered on the source VM. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="modifier" type="string">
                  A real, single OS-adaptation adjustment (paired with an `operator`) applied during migration for a specific detected guest-OS condition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The real value of this key/value adaptation modifier or node affinity entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="boot_conversion" type="string">
              Real settings and progress for converting the source VM's own boot mode (e.g. BIOS to UEFI) during migration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="generalize" type="bool">
              Whether VM Migration should generalize the source VM's own OS image (removing machine-specific identity) before migrating it, similar to running sysprep. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="license_type" type="string">
              Which real licensing model applies to the migrated VM: the source's own existing license (BYOL) or a new Google Cloud-provided one (PAYG). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_account" type="object">
          The real Google Cloud service account the imported machine image's target Compute Engine instance runs as. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="email" type="string">
              The real service account email the imported machine image's own VM instances run as. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="scopes" type="list(string)">
              The real OAuth scopes granted to the imported machine image's own configured service account. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="shielded_instance_config" type="object">
          Real Shielded VM configuration applied to the imported machine image. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enable_integrity_monitoring" type="bool">
              Whether Shielded VM integrity monitoring is real, enabled on the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enable_vtpm" type="bool">
              Whether a Shielded VM virtual Trusted Platform Module is real, enabled on the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="secure_boot" type="string">
              Whether Shielded VM Secure Boot is real, enabled on the migrated VM instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="single_region_storage" type="bool">
          Whether the migrated VM's own boot disk should use single-region (rather than multi-region) Cloud Storage during the migration process. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="skip_os_adaptation" type="any">
          Whether VM Migration should real, skip its own guest OS adaptation step when importing this image. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="tags" type="list(string)">
          Real Compute Engine network tags applied to VM instances created from the imported machine image. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_project" type="string">
          A reference to the real `google_vmmigration_target_project` this migrated VM or disk should land in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The real, target-side name given to this migration step, sync, or final-sync entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state" type="string">
      The real, current state of this migration job, step, or resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="steps" type="list(object)">
      The real, ordered sequence of steps (such as `initializing_replication`, `replicating`, `post_processing`) this migration cycle progresses through. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="adapting_os" type="any">
          Real, detailed progress for VM Migration's own guest OS adaptation step, adjusting drivers and boot configuration for Google Cloud. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="creating_image" type="any">
          Real, detailed progress for VM Migration's own step creating the final Compute Engine image or machine image, for an image import job. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="end_time" type="string">
          The real timestamp when this migration step or cycle finished, successfully or not. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="initializing" type="any">
          Real, detailed progress for VM Migration's own initial setup step, before real data transfer begins. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="loading_source_files" type="any">
          Real, detailed progress for VM Migration's own step loading the source image's files ahead of import. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_time" type="string">
          The real timestamp when this migration step or cycle started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="warnings" type="list(object)">
      Real, non-fatal warnings raised during this migration job, each with its own `warning_message`, severity, and `action_item`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="action_item" type="object">
          Real, recommended remediation steps for a migration warning or error, telling the operator what to do about it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="locale" type="string">
              The real, detected or configured locale (language and region) of the source VM's own guest operating system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="code" type="string">
          The `google.rpc.Code` enum value identifying this error's real category, following the same status-code semantics gRPC and most Google APIs share. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="help_links" type="list(object)">
          Real documentation links attached to a migration warning or error, pointing to more detail on the issue and how to resolve it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="url" type="string">
              A real URL pointing to more information about this migration warning or error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="warning_message" type="object">
          The real, human-readable text of one migration warning. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="locale" type="string">
              The real, detected or configured locale (language and region) of the source VM's own guest operating system. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A real, human-readable status or error message for this migration step. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="warning_time" type="string">
          The real timestamp when this migration warning was raised. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
