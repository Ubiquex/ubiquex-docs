---
title: "google_vmmigration_v1alpha1_disk_migration_job"
description: "`google_vmmigration_v1alpha1_disk_migration_job` migrates a real, standalone disk (not an entire running VM) to Google Cloud Persistent Disk..."
---

<Warning>
  This is Google Cloud's own **v1alpha1** release channel (alpha). Google may change or remove alpha/preview endpoints at any time, often with no deprecation notice -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_vmmigration_disk_migration_job`.
</Warning>

`google_vmmigration_v1alpha1_disk_migration_job` migrates a real, standalone disk (not an entire running VM) to Google Cloud Persistent Disk, for cases where only the data, not the source machine's live state, needs to move.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmmigration "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vmmigration"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1alpha1 disk migration job"})

    		ubx.Resource(vmmigration.V1alpha1DiskMigrationJob, "example", vmmigration.V1alpha1DiskMigrationJobConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1alpha1DiskMigrationJob } from "@ubx/sdk-google/google/vmmigration/v1alpha1_disk_migration_job";

    export default stack("example", () => {
      intent({ summary: "example own v1alpha1 disk migration job" });

      resource(V1alpha1DiskMigrationJob, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vmmigration.v1alpha1_disk_migration_job import V1alpha1DiskMigrationJob, V1alpha1DiskMigrationJobConfig

    def describe():
        ubx.intent("example own v1alpha1 disk migration job")

        ubx.resource(V1alpha1DiskMigrationJob, "example", V1alpha1DiskMigrationJobConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vmmigration_v1alpha1_disk_migration_job called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="aws_source_disk_details" type="object">
  Represents the source AWS Disk details. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="disk_type" type="string">
      Optional. Output only. Disk type. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="size_gib" type="string">
      Output only. Size in GiB. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="tags" type="map(string)">
      Optional. Output only. A map of AWS volume tags. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="volume_id" type="string">
      Required. AWS volume ID. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time the DiskMigrationJob resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="errors" type="list(object)">
  Output only. Provides details on the errors that led to the disk migration job's state in case of an error. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="details" type="list(map(any))">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="message" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. The identifier of the DiskMigrationJob. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the DiskMigrationJob. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="steps" type="list(object)">
  Output only. The disk migration steps list representing its progress. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="copying_source_disk_snapshot" type="any">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="creating_source_disk_snapshot" type="any">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="end_time" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="provisioning_target_disk" type="any">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_details" type="object">
  Details of the target disk in Compute Engine. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="encryption" type="object">
      Encryption message describes the details of the applied encryption. Optional.
      <Expandable title="properties">
        <ResponseField name="kms_key" type="string">
          Required. The name of the encryption key that is stored in Google Cloud KMS. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Optional. A map of labels to associate with the disk. Optional.
    </ResponseField>
    <ResponseField name="target_disk" type="object">
      Compute Engine disk target details. Optional.
      <Expandable title="properties">
        <ResponseField name="disk_id" type="string">
          Optional. Target Compute Engine Disk ID. This is the resource ID segment of the Compute Engine Disk to create. In the resource name compute/v1/projects/&#123;project&#125;/zones/&#123;zone&#125;/disks/disk1 "disk1" is the resource ID for the disk. Optional.
        </ResponseField>
        <ResponseField name="disk_type" type="string">
          Required. The disk type to use. Optional.
        </ResponseField>
        <ResponseField name="replica_zones" type="list(string)">
          Optional. Replication zones of the regional disk. Should be of the form: projects/&#123;target-project&#125;/locations/&#123;replica-zone&#125; Currently only one replica zone is supported. Optional.
        </ResponseField>
        <ResponseField name="zone" type="string">
          Required. The Compute Engine zone in which to create the disk. Should be of the form: projects/&#123;target-project&#125;/locations/&#123;zone&#125; Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target_project" type="string">
      Required. The name of the resource of type TargetProject which represents the Compute Engine project in which to create the disk. Should be of the form: projects/&#123;project&#125;/locations/global/targetProjects/&#123;target-project&#125; Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The last time the DiskMigrationJob resource was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
