---
title: "google_vmmigration_target_project"
description: "`google_vmmigration_target_project` is the real Google Cloud project VM Migration lands a migrated VM's Persistent Disk volumes and Compute Engine..."
---

`google_vmmigration_target_project` is the real Google Cloud project VM Migration lands a migrated VM's Persistent Disk volumes and Compute Engine instance into, configurable up until the actual clone or cutover is executed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmmigration "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vmmigration"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own target project"})

    		ubx.Resource(vmmigration.TargetProject, "example", vmmigration.TargetProjectConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TargetProject } from "@ubx/sdk-google/google/vmmigration/target_project";

    export default stack("example", () => {
      intent({ summary: "example own target project" });

      resource(TargetProject, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vmmigration import TargetProject, TargetProjectConfig

    def describe():
        ubx.intent("example own target project")

        ubx.resource(TargetProject, "example", TargetProjectConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vmmigration_target_project called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The time this target project resource was created (not related to when the Compute Engine project it points to was created). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  The target project's description. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The name of the target project. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="project" type="string">
  Required. The target project ID (number) or project name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The last time the target project resource was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
