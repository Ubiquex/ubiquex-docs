---
title: "google_contact_center_insights_assessment_rule"
description: "Real, generated bindings for `google_contact_center_insights_assessment_rule`."
---

`google_contact_center_insights_assessment_rule` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	contact "github.com/ubiquex/ubx-sdk-google/sdk/go/google/contact"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own center insights assessment rule"})

    		ubx.Resource(contact.CenterInsightsAssessmentRule, "ci-runner", contact.CenterInsightsAssessmentRuleConfig{
    			Location:         "example",
    			Active:           true,
    			AssessmentRuleId: "example",
    			DisplayName:      "example-display-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CenterInsightsAssessmentRule } from "jsr:@ubx/sdk-google/google/contact/center_insights_assessment_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own center insights assessment rule" });

      resource(CenterInsightsAssessmentRule, "ci-runner", {
        location: "example",
        active: true,
        assessmentRuleId: "example",
        displayName: "example-display-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.contact import CenterInsightsAssessmentRule, CenterInsightsAssessmentRuleConfig

    def describe():
        ubx.intent("payments own center insights assessment rule")

        ubx.resource(CenterInsightsAssessmentRule, "ci-runner", CenterInsightsAssessmentRuleConfig(
            location="example",
            active=True,
            assessment_rule_id="example",
            display_name="example-display-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_contact_center_insights_assessment_rule, with
    location example, active true, assessment rule id example,
    display name example-display-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="active" type="bool">
  If true, apply this rule to conversations. Otherwise, this rule is inactive. Optional.
</ResponseField>

<ResponseField name="assessment_rule_id" type="string">
  A unique ID for the new AssessmentRule. This ID will become the final component of the AssessmentRule's resource name. If no ID is specified, a server-generated ID will be used. This value should be 4-64 characters and must match the regular expression '^[A-Za-z0-9]&#123;4,64&#125;$'. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string">
  Display Name of the assessment rule. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Location of the resource. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="sample_rule" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="conversation_filter" type="string">
    </ResponseField>
    <ResponseField name="dimension" type="string">
    </ResponseField>
    <ResponseField name="sample_percentage" type="number">
    </ResponseField>
    <ResponseField name="sample_row" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="schedule_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
    </ResponseField>
    <ResponseField name="schedule" type="string">
    </ResponseField>
    <ResponseField name="start_time" type="string">
    </ResponseField>
    <ResponseField name="time_zone" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time at which this assessment rule was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the assessment rule. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/assessmentRules/&#123;assessment_rule&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="sample_rule" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="conversation_filter" type="string">
    </ResponseField>
    <ResponseField name="dimension" type="string">
    </ResponseField>
    <ResponseField name="sample_percentage" type="number">
    </ResponseField>
    <ResponseField name="sample_row" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="schedule_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
    </ResponseField>
    <ResponseField name="schedule" type="string">
    </ResponseField>
    <ResponseField name="start_time" type="string">
    </ResponseField>
    <ResponseField name="time_zone" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The most recent time at which this assessment rule was updated. Computed by GCP after `ship`.
</ResponseField>
