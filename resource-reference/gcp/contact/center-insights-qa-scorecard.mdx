---
title: "google_contact_center_insights_qa_scorecard"
description: "Real, generated bindings for `google_contact_center_insights_qa_scorecard`."
---

`google_contact_center_insights_qa_scorecard` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	contact "github.com/ubiquex/ubx-sdk-google/sdk/go/google/contact"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own center insights qa scorecard"})

    		ubx.Resource(contact.CenterInsightsQaScorecard, "ci-runner", contact.CenterInsightsQaScorecardConfig{
    			Location:      "example",
    			QaScorecardId: "example",
    			Description:   "Managed by ubx.",
    			DisplayName:   "example-display-name",
    			IsDefault:     true,
    			Source:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CenterInsightsQaScorecard } from "jsr:@ubx/sdk-google/google/contact/center_insights_qa_scorecard";

    export default stack("payments", () => {
      intent({ summary: "payments own center insights qa scorecard" });

      resource(CenterInsightsQaScorecard, "ci-runner", {
        location: "example",
        qaScorecardId: "example",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        isDefault: true,
        source: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.contact import CenterInsightsQaScorecard, CenterInsightsQaScorecardConfig

    def describe():
        ubx.intent("payments own center insights qa scorecard")

        ubx.resource(CenterInsightsQaScorecard, "ci-runner", CenterInsightsQaScorecardConfig(
            location="example",
            qa_scorecard_id="example",
            description="Managed by ubx.",
            display_name="example-display-name",
            is_default=True,
            source="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_contact_center_insights_qa_scorecard, with location
    example, qa scorecard id example, description Managed by ubx.,
    display name example-display-name, is default true, source
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A text description explaining the intent of the scorecard. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  The user-specified display name of the scorecard. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Whether the scorecard is the default one for the project. A default scorecard cannot be deleted and will always appear first in scorecard selector. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="qa_scorecard_id" type="string" required>
  A unique ID for the new QaScorecard. This ID will become the final component of the QaScorecard's resource name. If no ID is specified, a server-generated ID will be used. This value should be 4-64 characters and must match the regular expression '^[a-z0-9-]&#123;4,64&#125;$'. Valid characters are 'a-z-'. Required.
</ResponseField>

<ResponseField name="source" type="string">
  Possible values: QA_SCORECARD_SOURCE_CUSTOMER_DEFINED QA_SCORECARD_SOURCE_DISCOVERY_ENGINE Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time at which this scorecard was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The scorecard name. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/qaScorecards/&#123;qa_scorecard&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The most recent time at which the scorecard was updated. Computed by GCP after `ship`.
</ResponseField>
