---
title: "google_cloudresourcemanager_v1beta1_project"
description: "`google_cloudresourcemanager_v1beta1_project` is a Google Cloud project, the base-level container that billing, IAM policy..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_cloudresourcemanager_project`.
</Warning>

`google_cloudresourcemanager_v1beta1_project` is a Google Cloud project, the base-level container that billing, IAM policy, and every other resource in this system are ultimately scoped under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudresourcemanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloudresourcemanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 project"})

    		ubx.Resource(cloudresourcemanager.V1beta1Project, "example", cloudresourcemanager.V1beta1ProjectConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1Project } from "@ubx/sdk-google/google/cloudresourcemanager/v1beta1_project";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 project" });

      resource(V1beta1Project, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.cloudresourcemanager.v1beta1_project import V1beta1Project, V1beta1ProjectConfig

    def describe():
        ubx.intent("example own v1beta1 project")

        ubx.resource(V1beta1Project, "example", V1beta1ProjectConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudresourcemanager_v1beta1_project called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="configured_capabilities" type="list(string)">
  Output only. If this project is a Management Project, list of capabilities configured on the parent folder. Note, presence of any capability implies that this is a Management Project. Example: `folders/123/capabilities/app-management`. OUTPUT ONLY. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Creation time. Read-only. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  The labels associated with this Project. Label keys must be between 1 and 63 characters long and must conform to the following regular expression: a-z&#123;0,62&#125;. Label values must be between 0 and 63 characters long and must conform to the regular expression [a-z0-9_-]&#123;0,63&#125;. A label value can be empty. No more than 256 labels can be associated with a given resource. Clients should store labels in a representation such as JSON that does not depend on specific characters being disallowed. Example: `"environment" : "dev"` Read-write. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="lifecycle_state" type="string">
  The Project lifecycle state. Read-only. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  The optional user-assigned display name of the Project. When present it must be between 4 to 30 characters. Allowed characters are: lowercase and uppercase letters, numbers, hyphen, single-quote, double-quote, space, and exclamation point. Example: `My Project` Read-write. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parent" type="object">
  A container to reference an id for any resource type. A `resource` in Google Cloud Platform is a generic term for something you (a developer) may want to interact with through one of our API's. Some examples are an App Engine app, a Compute Engine instance, a Cloud SQL database, and so on. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Required field for the type-specific id. This should correspond to the id used in the type-specific API's. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      Required field representing the resource type this id is for. At present, the valid types are "project", "folder", and "organization". Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project_id" type="string">
  The unique, user-assigned ID of the Project. It must be 6 to 30 lowercase letters, digits, or hyphens. It must start with a letter. Trailing hyphens are prohibited. Example: `tokyo-rain-123` Read-only after creation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="project_number" type="string">
  The number uniquely identifying the project. Example: `415104041262` Read-only. Optional; if omitted, computed by Google Cloud.
</ResponseField>
