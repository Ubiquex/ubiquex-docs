---
title: "google_oslogin_v1beta_ssh_public_key"
description: "`google_oslogin_v1beta_ssh_public_key` registers an SSH public key against a user's own OS Login profile..."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_oslogin_ssh_public_key`.
</Warning>

`google_oslogin_v1beta_ssh_public_key` registers an SSH public key against a user's own OS Login profile, authorizing that key for SSH access across every VM instance with OS Login enabled the user has IAM access to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_oslogin_v1beta --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_oslogin_v1beta/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	oslogin "github.com/ubiquex/ubx-sdk-google/sdk/go/google_oslogin_v1beta/oslogin"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta ssh public key"})

    		ubx.Resource(oslogin.V1betaOsloginV1betaSshPublicKey, "example", oslogin.V1betaOsloginV1betaSshPublicKeyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_oslogin_v1beta --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaOsloginV1betaSshPublicKey } from "./local-sdk/google_oslogin_v1beta/sdk/typescript/google_oslogin_v1beta/oslogin/v1beta_oslogin_v1beta_ssh_public_key.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta ssh public key" });

      resource(V1betaOsloginV1betaSshPublicKey, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_oslogin_v1beta --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_oslogin_v1beta/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_oslogin_v1beta.oslogin import V1betaOsloginV1betaSshPublicKey, V1betaOsloginV1betaSshPublicKeyConfig

    def describe():
        ubx.intent("example own v1beta ssh public key")

        ubx.resource(V1betaOsloginV1betaSshPublicKey, "example", V1betaOsloginV1betaSshPublicKeyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_oslogin_v1beta_ssh_public_key called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="expiration_time_usec" type="string">
  An expiration time in microseconds since epoch. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Output only. The SHA-256 fingerprint of the SSH public key. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="key" type="string">
  Required. Public key text in SSH format, defined by [RFC4253](https://www.ietf.org/rfc/rfc4253.txt) section 6.6. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The canonical resource name. Optional; if omitted, computed by Google Cloud.
</ResponseField>
