---
title: "google_oracledatabase_odb_subnet"
description: "`google_oracledatabase_odb_subnet` defines a subnet within an Oracle Database@Google Cloud ODB network..."
---

`google_oracledatabase_odb_subnet` defines a subnet within an Oracle Database@Google Cloud ODB network, the IP address range Exadata or Exascale VM cluster resources are allocated from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	oracledatabase "github.com/ubiquex/ubx-sdk-google/sdk/go/google/oracledatabase"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own odb subnet"})

    		ubx.Resource(oracledatabase.OdbSubnet, "example", oracledatabase.OdbSubnetConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OdbSubnet } from "@ubx/sdk-google/google/oracledatabase/odb_subnet";

    export default stack("example", () => {
      intent({ summary: "example own odb subnet" });

      resource(OdbSubnet, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.oracledatabase import OdbSubnet, OdbSubnetConfig

    def describe():
        ubx.intent("example own odb subnet")

        ubx.resource(OdbSubnet, "example", OdbSubnetConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_oracledatabase_odb_subnet called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="cidr_range" type="string">
  Required. The CIDR range of the subnet. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The date and time that the OdbNetwork was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels or tags associated with the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The name of the OdbSubnet resource in the following format: projects/&#123;project&#125;/locations/&#123;location&#125;/odbNetworks/&#123;odb_network&#125;/odbSubnets/&#123;odb_subnet&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="purpose" type="string">
  Required. Purpose of the subnet. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the ODB Subnet. Optional; if omitted, computed by Google Cloud.
</ResponseField>
