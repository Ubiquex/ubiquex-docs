---
title: "google_oracledatabase_goldengate_deployment"
description: "`google_oracledatabase_goldengate_deployment` provisions a managed Oracle GoldenGate instance through Oracle Database@Google Cloud..."
---

`google_oracledatabase_goldengate_deployment` provisions a managed Oracle GoldenGate instance through Oracle Database@Google Cloud, used for real-time data replication and integration between databases.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	oracledatabase "github.com/ubiquex/ubx-sdk-google/sdk/go/google/oracledatabase"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own goldengate deployment"})

    		ubx.Resource(oracledatabase.GoldengateDeployment, "example", oracledatabase.GoldengateDeploymentConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GoldengateDeployment } from "@ubx/sdk-google/google/oracledatabase/goldengate_deployment";

    export default stack("example", () => {
      intent({ summary: "example own goldengate deployment" });

      resource(GoldengateDeployment, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.oracledatabase import GoldengateDeployment, GoldengateDeploymentConfig

    def describe():
        ubx.intent("example own goldengate deployment")

        ubx.resource(GoldengateDeployment, "example", GoldengateDeploymentConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_oracledatabase_goldengate_deployment called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The date and time that the GoldengateDeployment was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The display name for the GoldengateDeployment. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="entitlement_id" type="string">
  Output only. The ID of the subscription entitlement associated with the GoldengateDeployment Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="gcp_oracle_zone" type="string">
  Optional. The GCP Oracle zone where Oracle GoldengateDeployment is hosted. Example: us-east4-b-r2. If not specified, the system will pick a zone based on availability. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. The labels or tags associated with the GoldengateDeployment. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The name of the GoldengateDeployment resource in the following format: projects/&#123;project&#125;/locations/&#123;region&#125;/goldengateDeployments/&#123;goldengate_deployment&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="oci_url" type="string">
  Output only. HTTPS link to OCI resources exposed to Customer via UI Interface. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="odb_network" type="string">
  Optional. The name of the OdbNetwork associated with the GoldengateDeployment. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="odb_subnet" type="string">
  Required. The name of the OdbSubnet associated with the GoldengateDeployment for IP allocation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of GoldengateDeployment. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="backup_schedule" type="object">
      The backup schedule of the GoldengateDeployment. Optional.
      <Expandable title="properties">
        <ResponseField name="backup_scheduled_time" type="string">
          Output only. The timestamp of when the backup was scheduled. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="bucket" type="string">
          Output only. The bucket name. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="compartment_id" type="string">
          Output only. The compartment id. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="frequency_backup_scheduled" type="string">
          Output only. The frequency backup scheduled. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="metadata_only" type="bool">
          Output only. If metadata only. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="namespace" type="string">
          Output only. The namespace name. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="category" type="string">
      Output only. The category of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="cpu_core_count" type="number">
      Optional. The Minimum number of OCPUs to be made available for this Deployment. Optional.
    </ResponseField>
    <ResponseField name="deployment_backup_id" type="string">
      Output only. The deployment backup id of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="deployment_diagnostic_data" type="object">
      The deployment diagnostic data. Optional.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string">
          Output only. The bucket name. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="diagnostic_end_time" type="string">
          Output only. The time diagnostic end. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="diagnostic_start_time" type="string">
          Output only. The time diagnostic start. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="diagnostic_state" type="string">
          Output only. The diagnostic state. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="namespace" type="string">
          Output only. The namespace name. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="object" type="string">
          Output only. The object name. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deployment_role" type="string">
      Output only. The deployment role of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="deployment_type" type="string">
      Required. A valid Goldengate Deployment type. For a list of supported types, use the `ListGoldengateDeploymentTypes` operation. Optional.
    </ResponseField>
    <ResponseField name="deployment_url" type="string">
      Output only. The deployment url of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional. The description of the GoldengateDeployment. Optional.
    </ResponseField>
    <ResponseField name="environment_type" type="string">
      Optional. The environment type of the GoldengateDeployment. Optional.
    </ResponseField>
    <ResponseField name="fqdn" type="string">
      Output only. The Fully Qualified Domain Name of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="healthy" type="bool">
      Output only. Whether the GoldengateDeployment is healthy. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="ingress_ips" type="list(object)">
      Output only. The ingress ips of the GoldengateDeployment. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="ingress_ip_address" type="string">
          The IP address this resource accepts inbound connections on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_auto_scaling_enabled" type="bool">
      Optional. Indicates if auto scaling is enabled for the Deployment's CPU core count. Optional.
    </ResponseField>
    <ResponseField name="is_latest_version" type="bool">
      Output only. Whether the GoldengateDeployment is of the latest version. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="is_public" type="bool">
      Output only. Whether the GoldengateDeployment is public. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="is_storage_utilization_limit_exceeded" type="bool">
      Output only. Whether storage utilization limit is exceeded of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="last_backup_schedule_time" type="string">
      Output only. The time last backup scheduled of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="license_model" type="string">
      Optional. The Oracle license model that applies to a Deployment. Optional.
    </ResponseField>
    <ResponseField name="lifecycle_details" type="string">
      Output only. The lifecycle details of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="lifecycle_state" type="string">
      Output only. State of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="lifecycle_sub_state" type="string">
      Output only. The lifecycle sub-state of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="load_balancer_id" type="string">
      Output only. The load balancer id of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="load_balancer_subnet_id" type="string">
      Output only. The load balancer subnet id of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="locks" type="list(object)">
      Output only. The locks of the GoldengateDeployment. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="compartment_id" type="string">
          A reference to the Oracle Cloud Infrastructure compartment this resource belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="create_time" type="string">
          Output only. The timestamp when this resource was created. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="related_resource_id" type="string">
          A reference to a related resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="maintenance_config" type="object">
      The maintenance configuration of the GoldengateDeployment. Optional.
      <Expandable title="properties">
        <ResponseField name="bundle_release_upgrade_period_days" type="number">
          Optional. Defines auto upgrade period for bundle releases. Manually configured period cannot be longer than service defined period for bundle releases. This period must be shorter or equal to major release upgrade period. Not passing this field during create will equate to using the service default. Optional.
        </ResponseField>
        <ResponseField name="interim_release_upgrade_period_days" type="number">
          Optional. Defines auto upgrade period for interim releases. This period must be shorter or equal to bundle release upgrade period. Optional.
        </ResponseField>
        <ResponseField name="is_interim_release_auto_upgrade_enabled" type="bool">
          Optional. By default auto upgrade for interim releases are not enabled. If auto-upgrade is enabled for interim release, you have to specify interim_release_upgrade_period_days too. Optional.
        </ResponseField>
        <ResponseField name="major_release_upgrade_period_days" type="number">
          Optional. Defines auto upgrade period for major releases. Manually configured period cannot be longer than service defined period for major releases. Not passing this field during create will equate to using the service default. Optional.
        </ResponseField>
        <ResponseField name="security_patch_upgrade_period_days" type="number">
          Optional. Defines auto upgrade period for releases with security fix. Manually configured period cannot be longer than service defined period for security releases. Not passing this field during create will equate to using the service default. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="maintenance_window" type="object">
      The maintenance window of the GoldengateDeployment. Optional.
      <Expandable title="properties">
        <ResponseField name="day" type="string">
          Required. Days of the week. Optional.
        </ResponseField>
        <ResponseField name="start_hour" type="number">
          Required. Start hour for maintenance period. Hour is in UTC. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="next_backup_schedule_time" type="string">
      Output only. The time next backup scheduled of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="next_maintenance_action_type" type="string">
      Output only. The next maintenance action type of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="next_maintenance_description" type="string">
      Output only. The next maintenance description of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="next_maintenance_time" type="string">
      Output only. The time of next maintenance of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="nsg_ids" type="list(string)">
      Output only. The nsg ids of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="ocid" type="string">
      Output only. OCID of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="ogg_data" type="object">
      The Ogg data of the GoldengateDeployment. Optional.
      <Expandable title="properties">
        <ResponseField name="admin_password" type="string">
          Optional. The Goldengate deployment console password in plain text. Optional.
        </ResponseField>
        <ResponseField name="admin_password_secret_version" type="string">
          Optional. Input only. The Goldengate deployment console password secret version. Optional.
        </ResponseField>
        <ResponseField name="admin_username" type="string">
          Required. The Goldengate deployment console username. Optional.
        </ResponseField>
        <ResponseField name="certificate" type="string">
          Output only. The certificate of the GoldengateDeployment. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="credential_store" type="string">
          Output only. The credential store of the GoldengateDeployment. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="deployment" type="string">
          Required. The name given to the Goldengate service deployment. The name must be 1 to 32 characters long, must contain only alphanumeric characters and must start with a letter. Optional.
        </ResponseField>
        <ResponseField name="group_roles_mapping" type="object">
          The group to roles mapping of the GoldengateDeployment. Optional.
          <Expandable title="properties">
            <ResponseField name="administrator_group_id" type="string">
              Output only. The administrator group id. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="operator_group_id" type="string">
              Output only. The operator group id. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="security_group_id" type="string">
              Output only. The security group id. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="user_group_id" type="string">
              Output only. The user group id. Computed by Google Cloud after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="identity_domain_id" type="string">
          Output only. The identity domain id of the GoldengateDeployment. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="ogg_version" type="string">
          Optional. Version of OGG Optional.
        </ResponseField>
        <ResponseField name="password_secret_id" type="string">
          Output only. The password secret id of the GoldengateDeployment. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ogg_version_support_end_time" type="string">
      Output only. The time ogg version supported until of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="placements" type="list(object)">
      Output only. The placements of the GoldengateDeployment. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="availability_domain" type="string">
          The Oracle Cloud Infrastructure availability domain this resource runs in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fault_domain" type="string">
          The Oracle Cloud Infrastructure fault domain this resource runs in. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_ip_address" type="string">
      Output only. The private ip address of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="public_ip_address" type="string">
      Output only. The public ip address of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="role_change_time" type="string">
      Output only. The time when the role of the GoldengateDeployment was changed. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="storage_utilization_bytes" type="string">
      Output only. The storage utilization in bytes of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. The time the GoldengateDeployment was updated. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="upgrade_required_time" type="string">
      Output only. The time upgrade required of the GoldengateDeployment. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
