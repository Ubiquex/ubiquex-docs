---
title: "google_oracledatabase_exadb_vm_cluster"
description: "`google_oracledatabase_exadb_vm_cluster` provisions an Exadata VM cluster on Exascale Infrastructure through Oracle Database@Google Cloud..."
---

`google_oracledatabase_exadb_vm_cluster` provisions an Exadata VM cluster on Exascale Infrastructure through Oracle Database@Google Cloud, backed by an Exascale storage vault rather than fixed, pre-allocated Exadata storage.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_oracledatabase --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_oracledatabase/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	oracledatabase "github.com/ubiquex/ubx-sdk-google/sdk/go/google_oracledatabase/oracledatabase"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own exadb vm cluster"})

    		ubx.Resource(oracledatabase.OracledatabaseExadbVmCluster, "example", oracledatabase.OracledatabaseExadbVmClusterConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_oracledatabase --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OracledatabaseExadbVmCluster } from "./local-sdk/google_oracledatabase/sdk/typescript/google_oracledatabase/oracledatabase/oracledatabase_exadb_vm_cluster.ts";

    export default stack("example", () => {
      intent({ summary: "example own exadb vm cluster" });

      resource(OracledatabaseExadbVmCluster, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_oracledatabase --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_oracledatabase/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_oracledatabase.oracledatabase import OracledatabaseExadbVmCluster, OracledatabaseExadbVmClusterConfig

    def describe():
        ubx.intent("example own exadb vm cluster")

        ubx.resource(OracledatabaseExadbVmCluster, "example", OracledatabaseExadbVmClusterConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_oracledatabase_exadb_vm_cluster called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="backup_odb_subnet" type="string">
  Required. Immutable. The name of the backup OdbSubnet associated with the ExadbVmCluster. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/odbNetworks/&#123;odb_network&#125;/odbSubnets/&#123;odb_subnet&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The date and time that the ExadbVmCluster was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. Immutable. The display name for the ExadbVmCluster. The name does not have to be unique within your project. The name must be 1-255 characters long and can only contain alphanumeric characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="entitlement_id" type="string">
  Output only. The ID of the subscription entitlement associated with the ExadbVmCluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="gcp_oracle_zone" type="string">
  Output only. Immutable. The GCP Oracle zone where Oracle ExadbVmCluster is hosted. Example: us-east4-b-r2. During creation, the system will pick the zone assigned to the ExascaleDbStorageVault. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="identity_connector" type="object">
  The identity connector details which will allow OCI to securely access the resources in the customer project. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="connection_state" type="string">
      Output only. The connection state of the identity connector. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="service_agent_email" type="string">
      Output only. A google managed service account on which customers can grant roles to access resources in the customer project. Example: `p176944527254-55-75119d87fd8f@gcp-sa-oci.iam.gserviceaccount.com` Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. The labels or tags associated with the ExadbVmCluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The name of the ExadbVmCluster resource in the following format: projects/&#123;project&#125;/locations/&#123;region&#125;/exadbVmClusters/&#123;exadb_vm_cluster&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="odb_network" type="string">
  Optional. Immutable. The name of the OdbNetwork associated with the ExadbVmCluster. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/odbNetworks/&#123;odb_network&#125; It is optional but if specified, this should match the parent ODBNetwork of the OdbSubnet. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="odb_subnet" type="string">
  Required. Immutable. The name of the OdbSubnet associated with the ExadbVmCluster for IP allocation. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/odbNetworks/&#123;odb_network&#125;/odbSubnets/&#123;odb_subnet&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of an ExadbVmCluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="additional_ecpu_count_per_node" type="number">
      Optional. Immutable. The number of additional ECPUs per node for an Exadata VM cluster on exascale infrastructure. Optional.
    </ResponseField>
    <ResponseField name="cluster_name" type="string">
      Optional. Immutable. The cluster name for Exascale vm cluster. The cluster name must begin with an alphabetic character and may contain hyphens(-) but can not contain underscores(_). It should be not more than 11 characters and is not case sensitive. OCI Cluster name. Optional.
    </ResponseField>
    <ResponseField name="data_collection_options" type="object">
      Data collection options for diagnostics. https://docs.oracle.com/en-us/iaas/api/#/en/database/20160918/datatypes/DataCollectionOptions Optional.
      <Expandable title="properties">
        <ResponseField name="is_diagnostics_events_enabled" type="bool">
          Optional. Indicates whether to enable data collection for diagnostics. Optional.
        </ResponseField>
        <ResponseField name="is_health_monitoring_enabled" type="bool">
          Optional. Indicates whether to enable health monitoring. Optional.
        </ResponseField>
        <ResponseField name="is_incident_logs_enabled" type="bool">
          Optional. Indicates whether to enable incident logs and trace collection. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enabled_ecpu_count_per_node" type="number">
      Required. Immutable. The number of ECPUs enabled per node for an exadata vm cluster on exascale infrastructure. Optional.
    </ResponseField>
    <ResponseField name="exascale_db_storage_vault" type="string">
      Required. Immutable. The name of ExascaleDbStorageVault associated with the ExadbVmCluster. It can refer to an existing ExascaleDbStorageVault. Or a new one can be created during the ExadbVmCluster creation (requires storage_vault_properties to be set). Format: projects/&#123;project&#125;/locations/&#123;location&#125;/exascaleDbStorageVaults/&#123;exascale_db_storage_vault&#125; Optional.
    </ResponseField>
    <ResponseField name="gi_version" type="string">
      Output only. The Oracle Grid Infrastructure (GI) software version. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="grid_image_id" type="string">
      Required. Immutable. Grid Infrastructure Version. Optional.
    </ResponseField>
    <ResponseField name="hostname" type="string">
      Output only. The hostname of the ExadbVmCluster. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="hostname_prefix" type="string">
      Required. Immutable. Prefix for VM cluster host names. Optional.
    </ResponseField>
    <ResponseField name="license_model" type="string">
      Optional. Immutable. The license type of the ExadbVmCluster. Optional.
    </ResponseField>
    <ResponseField name="lifecycle_state" type="string">
      Output only. State of the cluster. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="memory_size_gb" type="number">
      Output only. Memory per VM (GB) (Read-only): Shows the amount of memory allocated to each VM. Memory is calculated based on 2.75 GB per Total ECPUs. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="node_count" type="number">
      Required. The number of nodes/VMs in the ExadbVmCluster. Optional.
    </ResponseField>
    <ResponseField name="oci_uri" type="string">
      Output only. Deep link to the OCI console to view this resource. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="scan_listener_port_tcp" type="number">
      Optional. Immutable. SCAN listener port - TCP Optional.
    </ResponseField>
    <ResponseField name="shape_attribute" type="string">
      Required. Immutable. The shape attribute of the VM cluster. The type of Exascale storage used for Exadata VM cluster. The default is SMART_STORAGE which supports Oracle Database 23ai and later Optional.
    </ResponseField>
    <ResponseField name="ssh_public_keys" type="list(string)">
      Required. Immutable. The SSH public keys for the ExadbVmCluster. Optional.
    </ResponseField>
    <ResponseField name="time_zone" type="object">
      Represents a time zone from the [IANA Time Zone Database](https://www.iana.org/time-zones). Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          IANA Time Zone Database time zone. For example "America/New_York". Optional.
        </ResponseField>
        <ResponseField name="version" type="string">
          Optional. IANA Time Zone Database version number. For example "2019a". Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vm_file_system_storage" type="object">
      The storage allocation for the exadbvmcluster, in gigabytes (GB). Optional.
      <Expandable title="properties">
        <ResponseField name="size_in_gbs_per_node" type="number">
          Required. The storage allocation for the exadbvmcluster per node, in gigabytes (GB). This field is used to calculate the total storage allocation for the exadbvmcluster. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
