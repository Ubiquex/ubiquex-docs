---
title: "google_securesourcemanager_issue"
description: "`google_securesourcemanager_issue` is a tracked issue within a Secure Source Manager repository..."
---

`google_securesourcemanager_issue` is a tracked issue within a Secure Source Manager repository, used for bug and task tracking alongside the source code itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_securesourcemanager --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_securesourcemanager/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	securesourcemanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google_securesourcemanager/securesourcemanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own issue"})

    		ubx.Resource(securesourcemanager.SecuresourcemanagerIssue, "ci-runner", securesourcemanager.SecuresourcemanagerIssueConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_securesourcemanager --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecuresourcemanagerIssue } from "./local-sdk/google_securesourcemanager/sdk/typescript/google_securesourcemanager/securesourcemanager/securesourcemanager_issue.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own issue" });

      resource(SecuresourcemanagerIssue, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_securesourcemanager --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_securesourcemanager/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_securesourcemanager.securesourcemanager import SecuresourcemanagerIssue, SecuresourcemanagerIssueConfig

    def describe():
        ubx.intent("payments own issue")

        ubx.resource(SecuresourcemanagerIssue, "ci-runner", SecuresourcemanagerIssueConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_securesourcemanager_issue called "ci-runner", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="body" type="string">
  Optional. Issue body. Provides a detailed description of the issue. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="close_time" type="string">
  Output only. Close timestamp (if closed). Cleared when is re-opened. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Creation timestamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Unique identifier for an issue. The issue id is generated by the server. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/repositories/&#123;repository&#125;/issues/&#123;issue_id&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the issue. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="title" type="string">
  Required. Issue title. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Last updated timestamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>
