---
title: "google_securesourcemanager_instance"
description: "`google_securesourcemanager_instance` is the top-level..."
---

`google_securesourcemanager_instance` is the top-level, privately hosted Secure Source Manager deployment that repository/repositories are created within, isolated to a single Google Cloud project and VPC.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	securesourcemanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google/securesourcemanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own instance"})

    		ubx.Resource(securesourcemanager.Instance, "example", securesourcemanager.InstanceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Instance } from "@ubx/sdk-google/google/securesourcemanager/instance";

    export default stack("example", () => {
      intent({ summary: "example own instance" });

      resource(Instance, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.securesourcemanager.instance import Instance, InstanceConfig

    def describe():
        ubx.intent("example own instance")

        ubx.resource(Instance, "example", InstanceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_securesourcemanager_instance called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Create timestamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="host_config" type="object">
  HostConfig has different instance endpoints. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="api" type="string">
      Output only. API hostname. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="git_http" type="string">
      Output only. Git HTTP hostname. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="git_ssh" type="string">
      Output only. Git SSH hostname. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="html" type="string">
      Output only. HTML hostname. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kms_key" type="string">
  Optional. Immutable. Customer-managed encryption key name, in the format projects/*/locations/*/keyRings/*/cryptoKeys/*. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels as key value pairs. Keys and values can contain only lowercase letters, numeric characters, underscores, and dashes. For more information, see [Requirements for labels](https://cloud.google.com/resource-manager/docs/best-practices-labels#label_encoding). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. A unique identifier for an instance. The name should be of the format: `projects/&#123;project_number&#125;/locations/&#123;location_id&#125;/instances/&#123;instance_id&#125;` `project_number`: Maps to a unique int64 id assigned to each project. `location_id`: Refers to the region where the instance will be deployed. Since Secure Source Manager is a regional service, it must be one of the valid GCP regions. `instance_id`: User provided name for the instance, must be unique for a project_number and location_id combination. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="private_config" type="object">
  PrivateConfig includes settings for private instance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="ca_pool" type="string">
      Optional. Immutable. CA pool resource, resource must in the format of `projects/&#123;project&#125;/locations/&#123;location&#125;/caPools/&#123;ca_pool&#125;`. Optional.
    </ResponseField>
    <ResponseField name="custom_host_config" type="object">
      Custom host config for the instance. Optional.
      <Expandable title="properties">
        <ResponseField name="api" type="string">
          Required. The custom API hostname for the instance, e.g., "api.source.internal.mycompany.com" Optional.
        </ResponseField>
        <ResponseField name="git_http" type="string">
          Required. The custom git http hostname for the instance, e.g., "git.source.internal.mycompany.com" Optional.
        </ResponseField>
        <ResponseField name="git_ssh" type="string">
          Required. The custom git ssh hostname for the instance, e.g., "ssh.source.internal.mycompany.com" Optional.
        </ResponseField>
        <ResponseField name="html" type="string">
          Required. The custom UI hostname for the instance, e.g., "git.source.internal.mycompany.com" Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="http_service_attachment" type="string">
      Output only. Service Attachment for HTTP, resource is in the format of `projects/&#123;project&#125;/regions/&#123;region&#125;/serviceAttachments/&#123;service_attachment&#125;`. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="is_private" type="bool">
      Required. Immutable. Indicate if it's private instance. Optional.
    </ResponseField>
    <ResponseField name="psc_allowed_projects" type="list(string)">
      Optional. Additional allowed projects for setting up PSC connections. Instance host project is automatically allowed and does not need to be included in this list. Optional.
    </ResponseField>
    <ResponseField name="ssh_service_attachment" type="string">
      Output only. Service Attachment for SSH, resource is in the format of `projects/&#123;project&#125;/regions/&#123;region&#125;/serviceAttachments/&#123;service_attachment&#125;`. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. Current state of the instance. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state_note" type="string">
  Output only. An optional field providing information about the current instance state. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Update timestamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="workforce_identity_federation_config" type="object">
  WorkforceIdentityFederationConfig allows this instance to support users from external identity providers. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Optional. Immutable. Whether Workforce Identity Federation is enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
