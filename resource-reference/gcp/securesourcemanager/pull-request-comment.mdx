---
title: "google_securesourcemanager_pull_request_comment"
description: "`google_securesourcemanager_pull_request_comment` is a single review comment posted on a Secure Source Manager pull request..."
---

`google_securesourcemanager_pull_request_comment` is a single review comment posted on a Secure Source Manager pull request, either general or attached to a specific line of the diff.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	securesourcemanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google/securesourcemanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own pull request comment"})

    		ubx.Resource(securesourcemanager.PullRequestComment, "example", securesourcemanager.PullRequestCommentConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PullRequestComment } from "@ubx/sdk-google/google/securesourcemanager/pull_request_comment";

    export default stack("example", () => {
      intent({ summary: "example own pull request comment" });

      resource(PullRequestComment, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.securesourcemanager.pull_request_comment import PullRequestComment, PullRequestCommentConfig

    def describe():
        ubx.intent("example own pull request comment")

        ubx.resource(PullRequestComment, "example", PullRequestCommentConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_securesourcemanager_pull_request_comment called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="code" type="object">
  The comment on a code line. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="body" type="string">
      Required. The comment body. Optional.
    </ResponseField>
    <ResponseField name="effective_commit_sha" type="string">
      Output only. The effective commit sha this code comment is pointing to. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="effective_root_comment" type="string">
      Output only. The root comment of the conversation, derived from the reply field. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="position" type="object">
      The position of the code comment. Optional.
      <Expandable title="properties">
        <ResponseField name="line" type="string">
          Required. The line number of the comment. Positive value means it's on the new side of the diff, negative value means it's on the old side. Optional.
        </ResponseField>
        <ResponseField name="path" type="string">
          Required. The path of the file. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reply" type="string">
      Optional. Input only. The PullRequestComment resource name that this comment is replying to. Optional.
    </ResponseField>
    <ResponseField name="resolved" type="bool">
      Output only. Boolean indicator if the comment is resolved. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="comment" type="object">
  The general pull request comment. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="body" type="string">
      Required. The comment body. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Creation timestamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Unique identifier for the pull request comment. The comment id is generated by the server. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/repositories/&#123;repository&#125;/pullRequests/&#123;pull_request&#125;/pullRequestComments/&#123;comment_id&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="review" type="object">
  The review summary comment. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="action_type" type="string">
      Required. The review action type. Optional.
    </ResponseField>
    <ResponseField name="body" type="string">
      Optional. The comment body. Optional.
    </ResponseField>
    <ResponseField name="effective_commit_sha" type="string">
      Output only. The effective commit sha this review is pointing to. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Last updated timestamp. Optional; if omitted, computed by Google Cloud.
</ResponseField>
