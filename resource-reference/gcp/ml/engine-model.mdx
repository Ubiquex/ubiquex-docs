---
title: "google_ml_engine_model"
description: "Real, generated bindings for `google_ml_engine_model`."
---

`google_ml_engine_model` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	ml "github.com/ubiquex/ubx-sdk-google/sdk/go/google/ml"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine model"})

    		ubx.Resource(ml.EngineModel, "ci-runner", ml.EngineModelConfig{
    			Name:                           "ci-runner",
    			Description:                    "Managed by ubx.",
    			Labels:                         map[string]string{"managed-by": "ubx"},
    			OnlinePredictionConsoleLogging: true,
    			OnlinePredictionLogging:        true,
    			Regions:                        []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineModel } from "jsr:@ubx/sdk-google/google/ml/engine_model";

    export default stack("payments", () => {
      intent({ summary: "payments own engine model" });

      resource(EngineModel, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
        onlinePredictionConsoleLogging: true,
        onlinePredictionLogging: true,
        regions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.ml import EngineModel, EngineModelConfig

    def describe():
        ubx.intent("payments own engine model")

        ubx.resource(EngineModel, "ci-runner", EngineModelConfig(
            name="ci-runner",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
            online_prediction_console_logging=True,
            online_prediction_logging=True,
            regions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_ml_engine_model called "ci-runner", with
    description Managed by ubx., labels a set value, online
    prediction console logging true, online prediction logging true,
    regions a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="default_version" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  The description specified for the model when it was created. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  One or more labels that you can add, to organize your models. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name specified for the model. Required.
</ResponseField>

<ResponseField name="online_prediction_console_logging" type="bool">
  If true, online prediction nodes send stderr and stdout streams to Stackdriver Logging Optional.
</ResponseField>

<ResponseField name="online_prediction_logging" type="bool">
  If true, online prediction access logs are sent to StackDriver Logging. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="regions" type="list(string)">
  The list of regions where the model is going to be deployed. Currently only one region per model is supported Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="default_version" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
