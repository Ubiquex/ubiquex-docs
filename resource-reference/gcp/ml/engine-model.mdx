---
title: "google_ml_engine_model"
description: "Real, generated bindings for `google_ml_engine_model`."
---

`google_ml_engine_model` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ml "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/ml"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine model"})

    		ubx.Resource(ml.EngineModel, "ci-runner", ml.EngineModelConfig{
    			Name:                           "ci-runner",
    			Description:                    "Managed by ubx.",
    			Labels:                         map[string]string{"managed-by": "ubx"},
    			OnlinePredictionConsoleLogging: true,
    			OnlinePredictionLogging:        true,
    			Regions:                        []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineModel } from "jsr:@ubx/sdk-google/google/ml/engine_model";

    export default stack("payments", () => {
      intent({ summary: "payments own engine model" });

      resource(EngineModel, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
        onlinePredictionConsoleLogging: true,
        onlinePredictionLogging: true,
        regions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.ml import EngineModel, EngineModelConfig

    def describe():
        ubx.intent("payments own engine model")

        ubx.resource(EngineModel, "ci-runner", EngineModelConfig(
            name="ci-runner",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
            online_prediction_console_logging=True,
            online_prediction_logging=True,
            regions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_ml_engine_model called "ci-runner", with
    description Managed by ubx., labels a set value, online
    prediction console logging true, online prediction logging true,
    regions a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="default_version" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="online_prediction_console_logging" type="bool">
  Optional.
</ResponseField>

<ResponseField name="online_prediction_logging" type="bool">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="regions" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="default_version" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
