---
title: "google_artifactregistry_v1beta2_tag"
description: "`google_artifactregistry_v1beta2_tag` is a human-readable alias (e.g. `latest`) pointing at a specific version of an artifact stored in an Artifact..."
---

`google_artifactregistry_v1beta2_tag` is a human-readable alias (e.g. `latest`) pointing at a specific version of an artifact stored in an Artifact Registry repository.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	artifactregistry "github.com/ubiquex/ubx-sdk-google/sdk/go/google/artifactregistry"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta2 tag"})

    		ubx.Resource(artifactregistry.V1beta2Tag, "example", artifactregistry.V1beta2TagConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta2Tag } from "@ubx/sdk-google/google/artifactregistry/v1beta2_tag";

    export default stack("example", () => {
      intent({ summary: "example own v1beta2 tag" });

      resource(V1beta2Tag, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.artifactregistry import V1beta2Tag, V1beta2TagConfig

    def describe():
        ubx.intent("example own v1beta2 tag")

        ubx.resource(V1beta2Tag, "example", V1beta2TagConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_artifactregistry_v1beta2_tag called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string">
  The name of the tag, for example: "projects/p1/locations/us-central1/repositories/repo1/packages/pkg1/tags/tag1". If the package part contains slashes, the slashes are escaped. The tag part can only have characters in [a-zA-Z0-9\-._~:@], anything else must be URL encoded. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="version" type="string">
  The name of the version the tag refers to, for example: `projects/p1/locations/us-central1/repositories/repo1/packages/pkg1/versions/sha256:5243811` If the package or version ID parts contain slashes, the slashes are escaped. Optional; if omitted, computed by Google Cloud.
</ResponseField>
