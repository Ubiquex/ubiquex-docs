---
title: "google_service_account_iam_policy"
description: "Real, generated bindings for `google_service_account_iam_policy`."
---

`google_service_account_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	service "github.com/ubiquex/ubx-sdk-google/sdk/go/google/service"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account iam policy"})

    		ubx.Resource(service.AccountIamPolicy, "ci-runner", service.AccountIamPolicyConfig{
    			PolicyData:       "example",
    			ServiceAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccountIamPolicy } from "jsr:@ubx/sdk-google/google/service/account_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own account iam policy" });

      resource(AccountIamPolicy, "ci-runner", {
        policyData: "example",
        serviceAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.service import AccountIamPolicy, AccountIamPolicyConfig

    def describe():
        ubx.intent("payments own account iam policy")

        ubx.resource(AccountIamPolicy, "ci-runner", AccountIamPolicyConfig(
            policy_data="example",
            service_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_service_account_iam_policy, with policy data
    example, service account id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_account_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
