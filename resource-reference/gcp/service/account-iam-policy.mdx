---
title: "google_service_account_iam_policy"
description: "Real, generated bindings for `google_service_account_iam_policy`."
---

`google_service_account_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/service"
    )

    cfg := service.AccountIamPolicyConfig{
        PolicyData: "example",
        ServiceAccountId: "example",
    }
    ubx.Resource(service.AccountIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccountIamPolicy } from
      "jsr:@ubx/sdk-google/google/service/account_iam_policy";

    resource(AccountIamPolicy, "example", {
      policyData: "example",
      serviceAccountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.service import (
        AccountIamPolicy,
        AccountIamPolicyConfig,
    )

    cfg = AccountIamPolicyConfig(
        policy_data="example",
        service_account_id="example",
    )
    ubx.resource(AccountIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_service_account_iam_policy with policy_data,
service_account_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_account_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
