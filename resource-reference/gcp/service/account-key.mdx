---
title: "google_service_account_key"
description: "Real, generated bindings for `google_service_account_key`."
---

`google_service_account_key` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/service"
    )

    cfg := service.AccountKeyConfig{
        ServiceAccountId: "example",
        Keepers: map[string]string{"key": "value"},
        KeyAlgorithm: "example",
    }
    ubx.Resource(service.AccountKey, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccountKey } from
      "jsr:@ubx/sdk-google/google/service/account_key";

    resource(AccountKey, "example", {
      serviceAccountId: "example",
      keepers: { key: "value" },
      keyAlgorithm: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.service import AccountKey, AccountKeyConfig

    cfg = AccountKeyConfig(
        service_account_id="example",
        keepers={"key": "value"},
        key_algorithm="example",
    )
    ubx.resource(AccountKey, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_service_account_key with service_account_id,
keepers, key_algorithm.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="keepers" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="key_algorithm" type="string">
  Optional.
</ResponseField>

<ResponseField name="private_key_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="public_key_data" type="string">
  Optional.
</ResponseField>

<ResponseField name="public_key_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="service_account_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="private_key" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="public_key" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="valid_after" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="valid_before" type="string">
  Computed by GCP after `ship`.
</ResponseField>
