---
title: "google_service_directory_endpoint"
description: "Real, generated bindings for `google_service_directory_endpoint`."
---

`google_service_directory_endpoint` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	service "github.com/ubiquex/ubx-sdk-google/sdk/go/google/service"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own directory endpoint"})

    		ubx.Resource(service.DirectoryEndpoint, "ci-runner", service.DirectoryEndpointConfig{
    			EndpointId: "example",
    			Service:    "example",
    			Address:    "example",
    			Metadata:   map[string]string{"managed-by": "ubx"},
    			Network:    "example",
    			Port:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DirectoryEndpoint } from "jsr:@ubx/sdk-google/google/service/directory_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own directory endpoint" });

      resource(DirectoryEndpoint, "ci-runner", {
        endpointId: "example",
        service: "example",
        address: "example",
        metadata: { "managed-by": "ubx" },
        network: "example",
        port: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.service import DirectoryEndpoint, DirectoryEndpointConfig

    def describe():
        ubx.intent("payments own directory endpoint")

        ubx.resource(DirectoryEndpoint, "ci-runner", DirectoryEndpointConfig(
            endpoint_id="example",
            service="example",
            address="example",
            metadata={"managed-by": "ubx"},
            network="example",
            port=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_service_directory_endpoint, with endpoint id
    example, service example, address example, metadata a set value,
    network example, port 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="address" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="endpoint_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="metadata" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="network" type="string">
  Optional.
</ResponseField>

<ResponseField name="port" type="number">
  Optional.
</ResponseField>

<ResponseField name="service" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
