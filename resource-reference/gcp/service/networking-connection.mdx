---
title: "google_service_networking_connection"
description: "Real, generated bindings for `google_service_networking_connection`."
---

`google_service_networking_connection` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/service"
    )

    cfg := service.NetworkingConnectionConfig{
        Network: "example",
        ReservedPeeringRanges: []string{"example"},
        Service: "example",
        UpdateOnCreationFail: true,
    }
    ubx.Resource(service.NetworkingConnection, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { NetworkingConnection } from
      "jsr:@ubx/sdk-google/google/service/networking_connection";

    resource(NetworkingConnection, "example", {
      network: "example",
      reservedPeeringRanges: ["example"],
      service: "example",
      updateOnCreationFail: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.service import (
        NetworkingConnection,
        NetworkingConnectionConfig,
    )

    cfg = NetworkingConnectionConfig(
        network="example",
        reserved_peering_ranges=["example"],
        service="example",
        update_on_creation_fail=True,
    )
    ubx.resource(NetworkingConnection, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_service_networking_connection with network,
reserved_peering_ranges, service, update_on_creation_fail.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network" type="string" required>
  Required.
</ResponseField>

<ResponseField name="reserved_peering_ranges" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="service" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_on_creation_fail" type="bool">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="peering" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
