---
title: "google_service_networking_connection"
description: "Real, generated bindings for `google_service_networking_connection`."
---

`google_service_networking_connection` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own networking connection"})

    		ubx.Resource(service.NetworkingConnection, "ci-runner", service.NetworkingConnectionConfig{
    			Network:               "example",
    			ReservedPeeringRanges: []string{"example"},
    			Service:               "example",
    			UpdateOnCreationFail:  true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkingConnection } from "jsr:@ubx/sdk-google/google/service/networking_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own networking connection" });

      resource(NetworkingConnection, "ci-runner", {
        network: "example",
        reservedPeeringRanges: ["example"],
        service: "example",
        updateOnCreationFail: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.service import NetworkingConnection, NetworkingConnectionConfig

    def describe():
        ubx.intent("payments own networking connection")

        ubx.resource(NetworkingConnection, "ci-runner", NetworkingConnectionConfig(
            network="example",
            reserved_peering_ranges=["example"],
            service="example",
            update_on_creation_fail=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_service_networking_connection, with network
    example, reserved peering ranges a set value, service example,
    update on creation fail true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network" type="string" required>
  Required.
</ResponseField>

<ResponseField name="reserved_peering_ranges" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="service" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_on_creation_fail" type="bool">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="peering" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
