---
title: "google_service_directory_service_iam_policy"
description: "Real, generated bindings for `google_service_directory_service_iam_policy`."
---

`google_service_directory_service_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/service"
    )

    cfg := service.DirectoryServiceIamPolicyConfig{
        Name: "example-name",
        PolicyData: "example",
    }
    ubx.Resource(service.DirectoryServiceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DirectoryServiceIamPolicy } from
      "jsr:@ubx/sdk-google/google/service/directory_service_iam_policy";

    resource(DirectoryServiceIamPolicy, "example", {
      name: "example-name",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.service import (
        DirectoryServiceIamPolicy,
        DirectoryServiceIamPolicyConfig,
    )

    cfg = DirectoryServiceIamPolicyConfig(
        name="example-name",
        policy_data="example",
    )
    ubx.resource(DirectoryServiceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_service_directory_service_iam_policy with name,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
