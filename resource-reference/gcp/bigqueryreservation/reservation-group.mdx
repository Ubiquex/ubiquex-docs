---
title: "google_bigqueryreservation_reservation_group"
description: "`google_bigqueryreservation_reservation_group` groups related BigQuery reservations together for unified capacity management and reporting."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigqueryreservation "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigqueryreservation"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own reservation group"})

    		ubx.Resource(bigqueryreservation.ReservationGroup, "example", bigqueryreservation.ReservationGroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReservationGroup } from "@ubx/sdk-google/google/bigqueryreservation/reservation_group";

    export default stack("example", () => {
      intent({ summary: "example own reservation group" });

      resource(ReservationGroup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigqueryreservation import ReservationGroup, ReservationGroupConfig

    def describe():
        ubx.intent("example own reservation group")

        ubx.resource(ReservationGroup, "example", ReservationGroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigqueryreservation_reservation_group called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="creation_time" type="string">
  Output only. Creation time of the reservation group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the reservation group, e.g., `projects/*/locations/*/reservationGroups/team1-prod`. The reservation_group_id must only contain lower case alphanumeric characters or dashes. It must start with a letter and must not end with a dash. Its maximum length is 64 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parent_group" type="string">
  Optional. The parent reservation group of the reservation group. Format: `projects/*/locations/*/reservationGroups/team1-prod` for non-root reservation groups, or `projects/*/locations/*` for root reservation groups. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Last update time of the reservation group via a user operation. This timestamp is updated only when an update operation explicitly targets this reservation group directly. It is not updated when parent or child groups are created, updated, or deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>
