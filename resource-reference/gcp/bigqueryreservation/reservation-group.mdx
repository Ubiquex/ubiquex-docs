---
title: "google_bigqueryreservation_reservation_group"
description: "`google_bigqueryreservation_reservation_group` groups related BigQuery reservations together for unified capacity management and reporting."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_bigqueryreservation --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_bigqueryreservation/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigqueryreservation "github.com/ubiquex/ubx-sdk-google/sdk/go/google_bigqueryreservation/bigqueryreservation"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own reservation group"})

    		ubx.Resource(bigqueryreservation.BigqueryreservationReservationGroup, "ci-runner", bigqueryreservation.BigqueryreservationReservationGroupConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_bigqueryreservation --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BigqueryreservationReservationGroup } from "./local-sdk/google_bigqueryreservation/sdk/typescript/google_bigqueryreservation/bigqueryreservation/bigqueryreservation_reservation_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own reservation group" });

      resource(BigqueryreservationReservationGroup, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_bigqueryreservation --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_bigqueryreservation/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_bigqueryreservation.bigqueryreservation import BigqueryreservationReservationGroup, BigqueryreservationReservationGroupConfig

    def describe():
        ubx.intent("payments own reservation group")

        ubx.resource(BigqueryreservationReservationGroup, "ci-runner", BigqueryreservationReservationGroupConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigqueryreservation_reservation_group called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string">
  Identifier. The resource name of the reservation group, e.g., `projects/*/locations/*/reservationGroups/team1-prod`. The reservation_group_id must only contain lower case alphanumeric characters or dashes. It must start with a letter and must not end with a dash. Its maximum length is 64 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parent_group" type="string">
  Optional. The parent reservation group of the reservation group. Format: `projects/*/locations/*/reservationGroups/team1-prod` for non-root reservation groups, or `projects/*/locations/*` for root reservation groups. Optional; if omitted, computed by Google Cloud.
</ResponseField>
