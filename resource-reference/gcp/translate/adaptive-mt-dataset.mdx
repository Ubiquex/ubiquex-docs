---
title: "google_translate_adaptive_mt_dataset"
description: "`google_translate_adaptive_mt_dataset` is a Cloud Translation Adaptive Translation dataset..."
---

`google_translate_adaptive_mt_dataset` is a Cloud Translation Adaptive Translation dataset, a real, small set of example translations combined with a large language model at request time to produce nuanced, style-consistent translations without training or maintaining a full custom model.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_translate --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_translate/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	translate "github.com/ubiquex/ubx-sdk-google/sdk/go/google_translate/translate"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own adaptive mt dataset"})

    		ubx.Resource(translate.TranslateAdaptiveMtDataset, "ci-runner", translate.TranslateAdaptiveMtDatasetConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_translate --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TranslateAdaptiveMtDataset } from "./local-sdk/google_translate/sdk/typescript/google_translate/translate/translate_adaptive_mt_dataset.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own adaptive mt dataset" });

      resource(TranslateAdaptiveMtDataset, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_translate --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_translate/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_translate.translate import TranslateAdaptiveMtDataset, TranslateAdaptiveMtDatasetConfig

    def describe():
        ubx.intent("payments own adaptive mt dataset")

        ubx.resource(TranslateAdaptiveMtDataset, "ci-runner", TranslateAdaptiveMtDatasetConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_translate_adaptive_mt_dataset called "ci-runner",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp when this dataset was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  The name of the dataset to show in the interface. The name can be up to 32 characters long and can consist only of ASCII Latin letters A-Z and a-z, underscores (_), and ASCII digits 0-9. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="example_count" type="number">
  The number of examples in the dataset. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the dataset, in form of `projects/&#123;project-number-or-id&#125;/locations/&#123;location_id&#125;/adaptiveMtDatasets/&#123;dataset_id&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="source_language_code" type="string">
  The BCP-47 language code of the source language. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="target_language_code" type="string">
  The BCP-47 language code of the target language. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when this dataset was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
