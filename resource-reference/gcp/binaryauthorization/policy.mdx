---
title: "google_binaryauthorization_policy"
description: "`google_binaryauthorization_policy` defines the deployment rule(s) (which attestor(s) must have signed off) Binary Authorization enforces across..."
---

`google_binaryauthorization_policy` defines the deployment rule(s) (which attestor(s) must have signed off) Binary Authorization enforces across a project's own GKE and Cloud Run deployments.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_binaryauthorization --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_binaryauthorization/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	binaryauthorization "github.com/ubiquex/ubx-sdk-google/sdk/go/google_binaryauthorization/binaryauthorization"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own policy"})

    		ubx.Resource(binaryauthorization.BinaryauthorizationPolicy, "example", binaryauthorization.BinaryauthorizationPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_binaryauthorization --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BinaryauthorizationPolicy } from "./local-sdk/google_binaryauthorization/sdk/typescript/google_binaryauthorization/binaryauthorization/binaryauthorization_policy.ts";

    export default stack("example", () => {
      intent({ summary: "example own policy" });

      resource(BinaryauthorizationPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_binaryauthorization --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_binaryauthorization/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_binaryauthorization.binaryauthorization import BinaryauthorizationPolicy, BinaryauthorizationPolicyConfig

    def describe():
        ubx.intent("example own policy")

        ubx.resource(BinaryauthorizationPolicy, "example", BinaryauthorizationPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_binaryauthorization_policy called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string">
  Optional. A description comment about the policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. Used to prevent updating the policy when another request has updated it since it was retrieved. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="gke_policy" type="object">
  A Binary Authorization policy for a GKE cluster. This is one type of policy that can occur as a `PlatformPolicy`. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="check_sets" type="list(object)">
      Optional. The `CheckSet` objects to apply, scoped by namespace or namespace and service account. Exactly one `CheckSet` will be evaluated for a given Pod (unless the list is empty, in which case the behavior is "always allow"). If multiple `CheckSet` objects have scopes that match the namespace and service account of the Pod being evaluated, only the `CheckSet` with the MOST SPECIFIC scope will match. `CheckSet` objects must be listed in order of decreasing specificity, i.e. if a scope matches a given service account (which must include the namespace), it must come before a `CheckSet` with a scope matching just that namespace. This property is enforced by server-side validation. The purpose of this restriction is to ensure that if more than one `CheckSet` matches a given Pod, the `CheckSet` that will be evaluated will always be the first in the list to match (because if any other matches, it must be less specific). If `check_sets` is empty, the default behavior is to allow all images. If `check_sets` is non-empty, the last `check_sets` entry must always be a `CheckSet` with no scope set, i.e. a catchall to handle any situation not caught by the preceding `CheckSet` objects. Optional.
      <Expandable title="properties">
        <ResponseField name="checks" type="list(object)">
          The check(s) an image must satisfy for this policy to allow it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="always_deny" type="bool">
              Whether every image is denied, with no exceptions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_name" type="string">
              A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="image_allowlist" type="object">
              The container image URL pattern(s) exempt from this policy's own checks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allow_pattern" type="list(string)">
                  A glob pattern of container image URL(s) exempt from attestation requirements. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="image_freshness_check" type="object">
              Verifies the image was built recently enough, within a configured age limit. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max_upload_age_days" type="number">
                  The maximum age, in days, an image may have been uploaded and still pass this check. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="sigstore_signature_check" type="object">
              Verifies the image was signed by a trusted Sigstore identity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="sigstore_authorities" type="list(object)">
                  The Sigstore authority/authorities trusted to verify this check's own signatures. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="display_name" type="string">
                      A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="public_key_set" type="object">
                      A set of public key(s), any one of which may satisfy this check. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="public_keys" type="list(object)">
                          The public key(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="public_key_pem" type="string">
                              A public key, PEM-encoded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="simple_signing_attestation_check" type="object">
              Verifies the image carries a valid Simple Signing format attestation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="attestation_authenticators" type="list(object)">
                  The authenticator(s) permitted to sign attestation(s) satisfying this check. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="display_name" type="string">
                      A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="pkix_public_key_set" type="object">
                      A set of PKIX public key(s), any one of which may satisfy this check. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="pkix_public_keys" type="list(object)">
                          The PKIX public key(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key_id" type="string">
                              An identifier for this key. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="public_key_pem" type="string">
                              A public key, PEM-encoded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="signature_algorithm" type="string">
                              The algorithm used to sign this key or attestation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="container_analysis_attestation_projects" type="list(string)">
                  The project(s) whose own Container Analysis occurrences are trusted as attestation sources. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="slsa_check" type="object">
              Verifies the image's own SLSA provenance meets a required build level. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="rules" type="list(object)">
                  The rule(s) making up this policy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="attestation_source" type="object">
                      Where the attestation(s) evaluated by this check are sourced from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="container_analysis_attestation_projects" type="list(string)">
                          The project(s) whose own Container Analysis occurrences are trusted as attestation sources. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="config_based_build_required" type="bool">
                      Whether the image must have been built from a specific, allow-listed build configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="custom_constraints" type="string">
                      Caller-defined constraint(s) this policy enforces. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="trusted_builder" type="string">
                      The build system trusted to produce this image's own provenance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="trusted_source_repo_patterns" type="list(string)">
                      The source repository URL pattern(s) trusted to have produced this image. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="trusted_directory_check" type="object">
              Verifies the image was built from a trusted source directory. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="trusted_dir_patterns" type="list(string)">
                  The source directory pattern(s) trusted to have produced this image. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="vulnerability_check" type="object">
              Verifies the image's own known vulnerabilities stay within configured severity limits. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allowed_cves" type="list(string)">
                  The known CVE identifier(s) explicitly permitted, even though they would otherwise fail this check. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="blocked_cves" type="list(string)">
                  The known CVE identifier(s) that cause this check to fail if present. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="container_analysis_vulnerability_projects" type="list(string)">
                  The project(s) whose own Container Analysis occurrences are trusted as vulnerability data sources. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="maximum_fixable_severity" type="string">
                  The highest severity of fixable vulnerability an image is permitted to contain. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="maximum_unfixable_severity" type="string">
                  The highest severity of unfixable vulnerability an image is permitted to contain. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="display_name" type="string">
          A mutable, user-settable, human-readable name for this resource, shown in the console UI. Not required to be unique. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="image_allowlist" type="object">
          The container image URL pattern(s) exempt from this policy's own checks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="allow_pattern" type="list(string)">
              A glob pattern of container image URL(s) exempt from attestation requirements. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="scope" type="object">
          What this configuration's own scope covers. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kubernetes_namespace" type="string">
              The Kubernetes namespace this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kubernetes_service_account" type="string">
              The Kubernetes service account this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="image_allowlist" type="object">
      Images that are exempted from normal checks based on name pattern only. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_pattern" type="list(string)">
          Required. A disjunction of image patterns to allow. If any of these patterns match, then the image is considered exempted by this allowlist. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The relative resource name of the Binary Authorization platform policy, in the form of `projects/*/platforms/*/policies/*`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time when the policy was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
