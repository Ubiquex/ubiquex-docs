---
title: "google_binaryauthorization_v1beta1_attestor"
description: "`google_binaryauthorization_v1beta1_attestor` defines a trusted signer whose own attestation(s) Binary Authorization checks for before allowing..."
---

`google_binaryauthorization_v1beta1_attestor` defines a trusted signer whose own attestation(s) Binary Authorization checks for before allowing a container image to deploy.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	binaryauthorization "github.com/ubiquex/ubx-sdk-google/sdk/go/google/binaryauthorization"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 attestor"})

    		ubx.Resource(binaryauthorization.V1beta1Attestor, "example", binaryauthorization.V1beta1AttestorConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1Attestor } from "@ubx/sdk-google/google/binaryauthorization/v1beta1_attestor";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 attestor" });

      resource(V1beta1Attestor, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.binaryauthorization import V1beta1Attestor, V1beta1AttestorConfig

    def describe():
        ubx.intent("example own v1beta1 attestor")

        ubx.resource(V1beta1Attestor, "example", V1beta1AttestorConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_binaryauthorization_v1beta1_attestor called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string">
  Optional. A descriptive comment. This field may be updated. The field may be displayed in chooser dialogs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. A checksum, returned by the server, that can be sent on update requests to ensure the attestor has an up-to-date value before attempting to update it. See https://google.aip.dev/154. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. The resource name, in the format: `projects/*/attestors/*`. This field may not be updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time when the attestor was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="user_owned_drydock_note" type="object">
  An user owned drydock note references a Drydock ATTESTATION_AUTHORITY Note created by the user. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="delegation_service_account_email" type="string">
      Output only. This field will contain the service account email address that this Attestor will use as the principal when querying Container Analysis. Attestor administrators must grant this service account the IAM role needed to read attestations from the note_reference in Container Analysis (`containeranalysis.notes.occurrences.viewer`). This email address is fixed for the lifetime of the Attestor, but callers should not make any other assumptions about the service account email; future versions may use an email based on a different naming pattern. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="note_reference" type="string">
      Required. The Drydock resource name of a ATTESTATION_AUTHORITY Note, created by the user, in the format: `projects/*/notes/*` (or the legacy `providers/*/notes/*`). This field may not be updated. An attestation by this attestor is stored as a Drydock ATTESTATION_AUTHORITY Occurrence that names a container image and that links to this Note. Drydock is an external dependency. Optional.
    </ResponseField>
    <ResponseField name="public_keys" type="list(object)">
      Optional. Public keys that verify attestations signed by this attestor. This field may be updated. If this field is non-empty, one of the specified public keys must verify that an attestation was signed by this attestor for the image specified in the admission request. If this field is empty, this attestor always returns that no valid attestations exist. Optional.
      <Expandable title="properties">
        <ResponseField name="ascii_armored_pgp_public_key" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="comment" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="id" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="pkix_public_key" type="object">
          Optional; if omitted, computed by Google Cloud.
          <Expandable title="properties">
            <ResponseField name="public_key_pem" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="signature_algorithm" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
