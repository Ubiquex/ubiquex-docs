---
title: "google_monitoring_monitored_project"
description: "Real, generated bindings for `google_monitoring_monitored_project`."
---

`google_monitoring_monitored_project` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/monitoring"
    )

    cfg := monitoring.MonitoredProjectConfig{
        MetricsScope: "example",
        Name: "example-name",
    }
    ubx.Resource(monitoring.MonitoredProject, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { MonitoredProject } from
      "jsr:@ubx/sdk-google/google/monitoring/monitored_project";

    resource(MonitoredProject, "example", {
      metricsScope: "example",
      name: "example-name",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.monitoring import (
        MonitoredProject,
        MonitoredProjectConfig,
    )

    cfg = MonitoredProjectConfig(
        metrics_scope="example",
        name="example-name",
    )
    ubx.resource(MonitoredProject, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_monitoring_monitored_project with metrics_scope,
name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="metrics_scope" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
