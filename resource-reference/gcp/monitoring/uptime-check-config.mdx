---
title: "google_monitoring_uptime_check_config"
description: "Real, generated bindings for `google_monitoring_uptime_check_config`."
---

`google_monitoring_uptime_check_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	monitoring "github.com/ubiquex/ubx-sdk-google/sdk/go/google/monitoring"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own uptime check config"})

    		ubx.Resource(monitoring.UptimeCheckConfig, "ci-runner", monitoring.UptimeCheckConfigConfig{
    			DisplayName:      "example-display-name",
    			Timeout:          "example",
    			LogCheckFailures: true,
    			Period:           "example",
    			SelectedRegions:  []string{"example"},
    			UserLabels:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UptimeCheckConfig } from "jsr:@ubx/sdk-google/google/monitoring/uptime_check_config";

    export default stack("payments", () => {
      intent({ summary: "payments own uptime check config" });

      resource(UptimeCheckConfig, "ci-runner", {
        displayName: "example-display-name",
        timeout: "example",
        logCheckFailures: true,
        period: "example",
        selectedRegions: ["example"],
        userLabels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.monitoring import UptimeCheckConfig, UptimeCheckConfigConfig

    def describe():
        ubx.intent("payments own uptime check config")

        ubx.resource(UptimeCheckConfig, "ci-runner", UptimeCheckConfigConfig(
            display_name="example-display-name",
            timeout="example",
            log_check_failures=True,
            period="example",
            selected_regions=["example"],
            user_labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_monitoring_uptime_check_config, with display name
    example-display-name, timeout example, log check failures true,
    period example, selected regions a set value, user labels a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="checker_type" type="string">
  The checker type to use for the check. If the monitored resource type is 'servicedirectory_service', 'checker_type' must be set to 'VPC_CHECKERS'. Possible values: ["STATIC_IP_CHECKERS", "VPC_CHECKERS"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="content_matchers" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="content" type="string" required>
    </ResponseField>
    <ResponseField name="json_path_matcher" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="json_matcher" type="string">
        </ResponseField>
        <ResponseField name="json_path" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="matcher" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  A human-friendly name for the uptime check configuration. The display name should be unique within a Stackdriver Workspace in order to make it easier to identify; however, uniqueness is not enforced. Required.
</ResponseField>

<ResponseField name="http_check" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="accepted_response_status_codes" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="status_class" type="string">
        </ResponseField>
        <ResponseField name="status_value" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="auth_info" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="password" type="string">
        </ResponseField>
        <ResponseField name="password_wo" type="string">
        </ResponseField>
        <ResponseField name="password_wo_version" type="string">
        </ResponseField>
        <ResponseField name="username" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="body" type="string">
    </ResponseField>
    <ResponseField name="content_type" type="string">
    </ResponseField>
    <ResponseField name="custom_content_type" type="string">
    </ResponseField>
    <ResponseField name="headers" type="map(string)">
    </ResponseField>
    <ResponseField name="mask_headers" type="bool">
    </ResponseField>
    <ResponseField name="path" type="string">
    </ResponseField>
    <ResponseField name="ping_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="pings_count" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="port" type="number">
    </ResponseField>
    <ResponseField name="request_method" type="string">
    </ResponseField>
    <ResponseField name="service_agent_authentication" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="type" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="use_ssl" type="bool">
    </ResponseField>
    <ResponseField name="validate_ssl" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="log_check_failures" type="bool">
  Specifies whether to log the results of failed probes to Cloud Logging. Optional.
</ResponseField>

<ResponseField name="monitored_resource" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="labels" type="map(string)" required>
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="period" type="string">
  How often, in seconds, the uptime check is performed. Currently, the only supported values are 60s (1 minute), 300s (5 minutes), 600s (10 minutes), and 900s (15 minutes). Optional, defaults to 300s. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_group" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="group_id" type="string">
    </ResponseField>
    <ResponseField name="resource_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="selected_regions" type="list(string)">
  The list of regions from which the check will be run. Some regions contain one location, and others contain more than one. If this field is specified, enough regions to include a minimum of 3 locations must be provided, or an error message is returned. Not specifying this field will result in uptime checks running from all regions. Optional.
</ResponseField>

<ResponseField name="synthetic_monitor" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cloud_function_v2" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tcp_check" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ping_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="pings_count" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="port" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeout" type="string" required>
  The maximum amount of time to wait for the request to complete (must be between 1 and 60 seconds). [See the accepted formats]( https://developers.google.com/protocol-buffers/docs/reference/google.protobuf#google.protobuf.Duration) Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_labels" type="map(string)">
  User-supplied key/value data to be used for organizing and identifying the 'UptimeCheckConfig' objects. The field can contain up to 64 entries. Each key and value is limited to 63 Unicode characters or 128 bytes, whichever is smaller. Labels and values can contain only lowercase letters, numerals, underscores, and dashes. Keys must begin with a letter. Optional.
</ResponseField>

## Output properties

<ResponseField name="checker_type" type="string">
  The checker type to use for the check. If the monitored resource type is 'servicedirectory_service', 'checker_type' must be set to 'VPC_CHECKERS'. Possible values: ["STATIC_IP_CHECKERS", "VPC_CHECKERS"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="content_matchers" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="content" type="string" required>
    </ResponseField>
    <ResponseField name="json_path_matcher" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="json_matcher" type="string">
        </ResponseField>
        <ResponseField name="json_path" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="matcher" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="http_check" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="accepted_response_status_codes" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="status_class" type="string">
        </ResponseField>
        <ResponseField name="status_value" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="auth_info" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="password" type="string">
        </ResponseField>
        <ResponseField name="password_wo" type="string">
        </ResponseField>
        <ResponseField name="password_wo_version" type="string">
        </ResponseField>
        <ResponseField name="username" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="body" type="string">
    </ResponseField>
    <ResponseField name="content_type" type="string">
    </ResponseField>
    <ResponseField name="custom_content_type" type="string">
    </ResponseField>
    <ResponseField name="headers" type="map(string)">
    </ResponseField>
    <ResponseField name="mask_headers" type="bool">
    </ResponseField>
    <ResponseField name="path" type="string">
    </ResponseField>
    <ResponseField name="ping_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="pings_count" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="port" type="number">
    </ResponseField>
    <ResponseField name="request_method" type="string">
    </ResponseField>
    <ResponseField name="service_agent_authentication" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="type" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="use_ssl" type="bool">
    </ResponseField>
    <ResponseField name="validate_ssl" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="monitored_resource" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="labels" type="map(string)" required>
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  A unique resource name for this UptimeCheckConfig. The format is 'projects/[PROJECT_ID]/uptimeCheckConfigs/[UPTIME_CHECK_ID]'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_group" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="group_id" type="string">
    </ResponseField>
    <ResponseField name="resource_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="synthetic_monitor" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cloud_function_v2" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tcp_check" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ping_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="pings_count" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="port" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uptime_check_id" type="string">
  The id of the uptime check Computed by GCP after `ship`.
</ResponseField>
