---
title: "google_composer_user_workloads_config_map"
description: "`google_composer_user_workloads_config_map` is a Kubernetes ConfigMap deployed into a Cloud Composer environment's own underlying GKE cluster..."
---

`google_composer_user_workloads_config_map` is a Kubernetes ConfigMap deployed into a Cloud Composer environment's own underlying GKE cluster, made available to user Airflow task workloads.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_composer --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_composer/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	composer "github.com/ubiquex/ubx-sdk-google/sdk/go/google_composer/composer"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own user workloads config map"})

    		ubx.Resource(composer.ComposerUserWorkloadsConfigMap, "example", composer.ComposerUserWorkloadsConfigMapConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_composer --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ComposerUserWorkloadsConfigMap } from "./local-sdk/google_composer/sdk/typescript/google_composer/composer/composer_user_workloads_config_map.ts";

    export default stack("example", () => {
      intent({ summary: "example own user workloads config map" });

      resource(ComposerUserWorkloadsConfigMap, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_composer --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_composer/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_composer.composer import ComposerUserWorkloadsConfigMap, ComposerUserWorkloadsConfigMapConfig

    def describe():
        ubx.intent("example own user workloads config map")

        ubx.resource(ComposerUserWorkloadsConfigMap, "example", ComposerUserWorkloadsConfigMapConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_composer_user_workloads_config_map called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="map(string)">
  Optional. The "data" field of Kubernetes ConfigMap, organized in key-value pairs. For details see: https://kubernetes.io/docs/concepts/configuration/configmap/ Example: &#123; "example_key": "example_value", "another_key": "another_value" &#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the ConfigMap, in the form: "projects/&#123;projectId&#125;/locations/&#123;locationId&#125;/environments/&#123;environmentId&#125;/userWorkloadsConfigMaps/&#123;userWorkloadsConfigMapId&#125;" Optional; if omitted, computed by Google Cloud.
</ResponseField>
