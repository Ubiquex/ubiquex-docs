---
title: "google_composer_v1beta1_user_workloads_secret"
description: "`google_composer_v1beta1_user_workloads_secret` is a Kubernetes Secret deployed into a Cloud Composer environment's own underlying GKE cluster..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_composer_user_workloads_secret`.
</Warning>

`google_composer_v1beta1_user_workloads_secret` is a Kubernetes Secret deployed into a Cloud Composer environment's own underlying GKE cluster, made available to user Airflow task workloads.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_composer_v1beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_composer_v1beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	composer "github.com/ubiquex/ubx-sdk-google/sdk/go/google_composer_v1beta1/composer"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 user workloads secret"})

    		ubx.Resource(composer.V1beta1ComposerV1beta1UserWorkloadsSecret, "example", composer.V1beta1ComposerV1beta1UserWorkloadsSecretConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_composer_v1beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1ComposerV1beta1UserWorkloadsSecret } from "./local-sdk/google_composer_v1beta1/sdk/typescript/google_composer_v1beta1/composer/v1beta1_composer_v1beta1_user_workloads_secret.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 user workloads secret" });

      resource(V1beta1ComposerV1beta1UserWorkloadsSecret, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_composer_v1beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_composer_v1beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_composer_v1beta1.composer import V1beta1ComposerV1beta1UserWorkloadsSecret, V1beta1ComposerV1beta1UserWorkloadsSecretConfig

    def describe():
        ubx.intent("example own v1beta1 user workloads secret")

        ubx.resource(V1beta1ComposerV1beta1UserWorkloadsSecret, "example", V1beta1ComposerV1beta1UserWorkloadsSecretConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_composer_v1beta1_user_workloads_secret called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="map(string)">
  Optional. The "data" field of Kubernetes Secret, organized in key-value pairs, which can contain sensitive values such as a password, a token, or a key. The values for all keys have to be base64-encoded strings. For details see: https://kubernetes.io/docs/concepts/configuration/secret/ Example: &#123; "example": "ZXhhbXBsZV92YWx1ZQ==", "another-example": "YW5vdGhlcl9leGFtcGxlX3ZhbHVl" &#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the Secret, in the form: "projects/&#123;projectId&#125;/locations/&#123;locationId&#125;/environments/&#123;environmentId&#125;/userWorkloadsSecrets/&#123;userWorkloadsSecretId&#125;" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  Optional. The "type" field of Kubernetes Secret. For details see: https://kubernetes.io/docs/concepts/configuration/secret/#secret-types Optional; if omitted, computed by Google Cloud.
</ResponseField>
