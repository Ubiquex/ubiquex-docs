---
title: "google_composer_user_workloads_secret"
description: "Real, generated bindings for `google_composer_user_workloads_secret`."
---

`google_composer_user_workloads_secret` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/composer"
    )

    cfg := composer.UserWorkloadsSecretConfig{
        Environment: "example",
        Name: "example-name",
        Data: map[string]string{"key": "value"},
    }
    ubx.Resource(composer.UserWorkloadsSecret, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { UserWorkloadsSecret } from
      "jsr:@ubx/sdk-google/google/composer/user_workloads_secret";

    resource(UserWorkloadsSecret, "example", {
      environment: "example",
      name: "example-name",
      data: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.composer import (
        UserWorkloadsSecret,
        UserWorkloadsSecretConfig,
    )

    cfg = UserWorkloadsSecretConfig(
        environment="example",
        name="example-name",
        data={"key": "value"},
    )
    ubx.resource(UserWorkloadsSecret, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_composer_user_workloads_secret with environment,
name, data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="environment" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
