---
title: "google_securityposture_posture_deployment"
description: "Real, generated bindings for `google_securityposture_posture_deployment`."
---

`google_securityposture_posture_deployment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	securityposture "github.com/ubiquex/ubx-sdk-google/sdk/go/google/securityposture"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own posture deployment"})

    		ubx.Resource(securityposture.PostureDeployment, "ci-runner", securityposture.PostureDeploymentConfig{
    			Location:            "example",
    			Parent:              "example",
    			PostureDeploymentId: "example",
    			PostureId:           "example",
    			PostureRevisionId:   "example",
    			TargetResource:      "example",
    			Description:         "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PostureDeployment } from "jsr:@ubx/sdk-google/google/securityposture/posture_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own posture deployment" });

      resource(PostureDeployment, "ci-runner", {
        location: "example",
        parent: "example",
        postureDeploymentId: "example",
        postureId: "example",
        postureRevisionId: "example",
        targetResource: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.securityposture import PostureDeployment, PostureDeploymentConfig

    def describe():
        ubx.intent("payments own posture deployment")

        ubx.resource(PostureDeployment, "ci-runner", PostureDeploymentConfig(
            location="example",
            parent="example",
            posture_deployment_id="example",
            posture_id="example",
            posture_revision_id="example",
            target_resource="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_securityposture_posture_deployment, with location
    example, parent example, posture deployment id example, posture
    id example, posture revision id example, target resource example,
    description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  Required.
</ResponseField>

<ResponseField name="posture_deployment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="posture_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="posture_revision_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="target_resource" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="desired_posture_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="desired_posture_revision_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="failure_message" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
