---
title: "google_securityposture_posture_deployment"
description: "Real, generated bindings for `google_securityposture_posture_deployment`."
---

`google_securityposture_posture_deployment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	securityposture "github.com/ubiquex/ubx-sdk-google/sdk/go/google/securityposture"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own posture deployment"})

    		ubx.Resource(securityposture.PostureDeployment, "ci-runner", securityposture.PostureDeploymentConfig{
    			Location:            "example",
    			Parent:              "example",
    			PostureDeploymentId: "example",
    			PostureId:           "example",
    			PostureRevisionId:   "example",
    			TargetResource:      "example",
    			Description:         "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PostureDeployment } from "jsr:@ubx/sdk-google/google/securityposture/posture_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own posture deployment" });

      resource(PostureDeployment, "ci-runner", {
        location: "example",
        parent: "example",
        postureDeploymentId: "example",
        postureId: "example",
        postureRevisionId: "example",
        targetResource: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.securityposture import PostureDeployment, PostureDeploymentConfig

    def describe():
        ubx.intent("payments own posture deployment")

        ubx.resource(PostureDeployment, "ci-runner", PostureDeploymentConfig(
            location="example",
            parent="example",
            posture_deployment_id="example",
            posture_id="example",
            posture_revision_id="example",
            target_resource="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_securityposture_posture_deployment, with location
    example, parent example, posture deployment id example, posture
    id example, posture revision id example, target resource example,
    description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Description of the posture deployment. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location of the resource, eg. global'. Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  The parent of the resource, an organization. Format should be 'organizations/&#123;organization_id&#125;'. Required.
</ResponseField>

<ResponseField name="posture_deployment_id" type="string" required>
  ID of the posture deployment. Required.
</ResponseField>

<ResponseField name="posture_id" type="string" required>
  Relative name of the posture which needs to be deployed. It should be in the format: organizations/&#123;organization_id&#125;/locations/&#123;location&#125;/postures/&#123;posture_id&#125; Required.
</ResponseField>

<ResponseField name="posture_revision_id" type="string" required>
  Revision_id the posture which needs to be deployed. Required.
</ResponseField>

<ResponseField name="target_resource" type="string" required>
  The resource on which the posture should be deployed. This can be in one of the following formats: projects/&#123;project_number&#125;, folders/&#123;folder_number&#125;, organizations/&#123;organization_id&#125; Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Time the posture deployment was created in UTC. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="desired_posture_id" type="string">
  This is an output only optional field which will be filled in case when PostureDeployment state is UPDATE_FAILED or CREATE_FAILED or DELETE_FAILED. It denotes the desired posture to be deployed. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="desired_posture_revision_id" type="string">
  This is an output only optional field which will be filled in case when PostureDeployment state is UPDATE_FAILED or CREATE_FAILED or DELETE_FAILED. It denotes the desired posture revision_id to be deployed. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  For Resource freshness validation (https://google.aip.dev/154) Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="failure_message" type="string">
  This is a output only optional field which will be filled in case where PostureDeployment enters a failure state like UPDATE_FAILED or CREATE_FAILED or DELETE_FAILED. It will have the failure message for posture deployment's CREATE/UPDATE/DELETE methods. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the posture deployment instance. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  If set, there are currently changes in flight to the posture deployment. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  State of the posture deployment. A posture deployment can be in the following terminal states: ACTIVE, CREATE_FAILED, UPDATE_FAILED, DELETE_FAILED. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Time the posture deployment was updated in UTC. Computed by GCP after `ship`.
</ResponseField>
