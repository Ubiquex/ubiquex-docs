---
title: "google_gkeonprem_bare_metal_admin_cluster"
description: "`google_gkeonprem_bare_metal_admin_cluster` is the administrative GKE on Bare Metal cluster that manages the lifecycle of other bare metal user..."
---

`google_gkeonprem_bare_metal_admin_cluster` is the administrative GKE on Bare Metal cluster that manages the lifecycle of other bare metal user cluster(s) running on the same on-premises hardware.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_gkeonprem --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_gkeonprem/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gkeonprem "github.com/ubiquex/ubx-sdk-google/sdk/go/google_gkeonprem/gkeonprem"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bare metal admin cluster"})

    		ubx.Resource(gkeonprem.GkeonpremBareMetalAdminCluster, "ci-runner", gkeonprem.GkeonpremBareMetalAdminClusterConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_gkeonprem --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GkeonpremBareMetalAdminCluster } from "./local-sdk/google_gkeonprem/sdk/typescript/google_gkeonprem/gkeonprem/gkeonprem_bare_metal_admin_cluster.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own bare metal admin cluster" });

      resource(GkeonpremBareMetalAdminCluster, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_gkeonprem --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_gkeonprem/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_gkeonprem.gkeonprem import GkeonpremBareMetalAdminCluster, GkeonpremBareMetalAdminClusterConfig

    def describe():
        ubx.intent("payments own bare metal admin cluster")

        ubx.resource(GkeonpremBareMetalAdminCluster, "ci-runner", GkeonpremBareMetalAdminClusterConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gkeonprem_bare_metal_admin_cluster called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Annotations on the bare metal admin cluster. This field has the same restrictions as Kubernetes annotations. The total size of all keys and values combined is limited to 256k. Key can have 2 segments: prefix (optional) and name (required), separated by a slash (/). Prefix must be a DNS subdomain. Name must be 63 characters or less, begin and end with alphanumerics, with dashes (-), underscores (_), dots (.), and alphanumerics between. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="bare_metal_version" type="string">
  The Anthos clusters on bare metal version for the bare metal admin cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="binary_authorization" type="object">
  Configuration for Binary Authorization. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="evaluation_mode" type="string">
      Mode of operation for binauthz policy evaluation. If unspecified, defaults to DISABLED. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cluster_operations" type="object">
  BareMetalAdminClusterOperationsConfig specifies the admin cluster's observability infrastructure. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="enable_application_logs" type="bool">
      Whether collection of application logs/metrics should be enabled (in addition to system logs/metrics). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="control_plane" type="object">
  BareMetalAdminControlPlaneConfig specifies the control plane configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="api_server_args" type="list(object)">
      Customizes the default API server args. Only a subset of customized flags are supported. Please refer to the API server documentation below to know the exact format: https://kubernetes.io/docs/reference/command-line-tools-reference/kube-apiserver/ Optional.
      <Expandable title="properties">
        <ResponseField name="argument" type="string">
          A command-line argument. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of this taint or label. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="control_plane_node_pool_config" type="object">
      BareMetalAdminControlPlaneNodePoolConfig specifies the control plane node pool configuration. We have a control plane specific node pool config so that we can flexible about supporting control plane specific fields in the future. Optional.
      <Expandable title="properties">
        <ResponseField name="node_pool_config" type="object">
          BareMetalNodePoolConfig describes the configuration of all nodes within a given bare metal node pool. Optional.
          <Expandable title="properties">
            <ResponseField name="kubelet_config" type="object">
              KubeletConfig defines the modifiable kubelet configurations for bare metal machines. Note: this list includes fields supported in GKE (see https://cloud.google.com/kubernetes-engine/docs/how-to/node-system-config#kubelet-options). Optional.
              <Expandable title="properties">
                <ResponseField name="registry_burst" type="number">
                  The maximum size of bursty pulls, temporarily allows pulls to burst to this number, while still not exceeding registry_pull_qps. The value must not be a negative number. Updating this field may impact scalability by changing the amount of traffic produced by image pulls. Defaults to 10. Optional.
                </ResponseField>
                <ResponseField name="registry_pull_qps" type="number">
                  The limit of registry pulls per second. Setting this value to 0 means no limit. Updating this field may impact scalability by changing the amount of traffic produced by image pulls. Defaults to 5. Optional.
                </ResponseField>
                <ResponseField name="serialize_image_pulls_disabled" type="bool">
                  Prevents the Kubelet from pulling multiple images at a time. We recommend *not* changing the default value on nodes that run docker daemon with version &lt; 1.9 or an Another Union File System (Aufs) storage backend. Issue https://github.com/kubernetes/kubernetes/issues/10959 has more details. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="labels" type="map(string)">
              The labels assigned to nodes of this node pool. An object containing a list of key/value pairs. Example: &#123; "name": "wrench", "mass": "1.3kg", "count": "3" &#125;. Optional.
            </ResponseField>
            <ResponseField name="node_configs" type="list(object)">
              Required. The list of machine addresses in the bare metal node pool. Optional.
              <Expandable title="properties">
                <ResponseField name="labels" type="map(string)">
                  Optional. User-provided key/value labels on this resource, usable for organizing and filtering resources in Cloud Billing and the console. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="node_ip" type="string">
                  The IP address of this node. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="operating_system" type="string">
              Specifies the nodes operating system (default: LINUX). Optional.
            </ResponseField>
            <ResponseField name="taints" type="list(object)">
              The initial taints assigned to nodes of this node pool. Optional.
              <Expandable title="properties">
                <ResponseField name="effect" type="string">
                  What happens to a pod that doesn't tolerate this taint. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key" type="string">
                  The key of this taint or label. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The value of this taint or label. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time at which this bare metal admin cluster was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. The time at which this bare metal admin cluster was deleted. If the resource is not deleted, this must be empty Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  A human readable description of this bare metal admin cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="endpoint" type="string">
  Output only. The IP address name of bare metal admin cluster's API server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Allows clients to perform consistent read-modify-writes through optimistic concurrency control. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fleet" type="object">
  Fleet related configuration. Fleets are a Google Cloud concept for logically organizing clusters, letting you use and manage multi-cluster capabilities and apply consistent policies across your systems. See [Anthos Fleets](`https://cloud.google.com/anthos/multicluster-management/fleets`) for more details on Anthos multi-cluster capabilities using Fleets. ## Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="membership" type="string">
      Output only. The name of the managed fleet Membership resource associated to this cluster. Membership names are formatted as `projects//locations//memberships/`. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="load_balancer" type="object">
  BareMetalAdminLoadBalancerConfig specifies the load balancer configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="bgp_lb_config" type="object">
      BareMetalAdminBgpLbConfig represents configuration parameters for a Border Gateway Protocol (BGP) load balancer. Optional.
      <Expandable title="properties">
        <ResponseField name="address_pools" type="list(object)">
          Required. AddressPools is a list of non-overlapping IP pools used by load balancer typed services. All addresses must be routable to load balancer nodes. IngressVIP must be included in the pools. Optional.
          <Expandable title="properties">
            <ResponseField name="addresses" type="list(string)">
              The IP address(es) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="avoid_buggy_ips" type="bool">
              Whether known problematic IP addresses are avoided when allocating this pool. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="manual_assign" type="bool">
              Whether IP addresses are manually assigned, rather than allocated from a pool. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pool" type="string">
              A reference to the node pool this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="asn" type="string">
          Required. BGP autonomous system number (ASN) of the cluster. This field can be updated after cluster creation. Optional.
        </ResponseField>
        <ResponseField name="bgp_peer_configs" type="list(object)">
          Required. The list of BGP peers that the cluster will connect to. At least one peer must be configured for each control plane node. Control plane nodes will connect to these peers to advertise the control plane VIP. The Services load balancer also uses these peers by default. This field can be updated after cluster creation. Optional.
          <Expandable title="properties">
            <ResponseField name="asn" type="string">
              The BGP Autonomous System Number this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="control_plane_nodes" type="list(string)">
              The node(s) making up this cluster's own control plane. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ip_address" type="string">
              An IP address, in IPv4 or IPv6 format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="load_balancer_node_pool_config" type="object">
          Specifies the load balancer's node pool configuration. Optional.
          <Expandable title="properties">
            <ResponseField name="node_pool_config" type="object">
              BareMetalNodePoolConfig describes the configuration of all nodes within a given bare metal node pool. Optional.
              <Expandable title="properties">
                <ResponseField name="kubelet_config" type="object">
                  KubeletConfig defines the modifiable kubelet configurations for bare metal machines. Note: this list includes fields supported in GKE (see https://cloud.google.com/kubernetes-engine/docs/how-to/node-system-config#kubelet-options). Optional.
                  <Expandable title="properties">
                    <ResponseField name="registry_burst" type="number">
                      The maximum size of bursty pulls, temporarily allows pulls to burst to this number, while still not exceeding registry_pull_qps. The value must not be a negative number. Updating this field may impact scalability by changing the amount of traffic produced by image pulls. Defaults to 10. Optional.
                    </ResponseField>
                    <ResponseField name="registry_pull_qps" type="number">
                      The limit of registry pulls per second. Setting this value to 0 means no limit. Updating this field may impact scalability by changing the amount of traffic produced by image pulls. Defaults to 5. Optional.
                    </ResponseField>
                    <ResponseField name="serialize_image_pulls_disabled" type="bool">
                      Prevents the Kubelet from pulling multiple images at a time. We recommend *not* changing the default value on nodes that run docker daemon with version &lt; 1.9 or an Another Union File System (Aufs) storage backend. Issue https://github.com/kubernetes/kubernetes/issues/10959 has more details. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="labels" type="map(string)">
                  The labels assigned to nodes of this node pool. An object containing a list of key/value pairs. Example: &#123; "name": "wrench", "mass": "1.3kg", "count": "3" &#125;. Optional.
                </ResponseField>
                <ResponseField name="node_configs" type="list(object)">
                  Required. The list of machine addresses in the bare metal node pool. Optional.
                  <Expandable title="properties">
                    <ResponseField name="labels" type="map(string)">
                      Optional. User-provided key/value labels on this resource, usable for organizing and filtering resources in Cloud Billing and the console. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="node_ip" type="string">
                      The IP address of this node. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="operating_system" type="string">
                  Specifies the nodes operating system (default: LINUX). Optional.
                </ResponseField>
                <ResponseField name="taints" type="list(object)">
                  The initial taints assigned to nodes of this node pool. Optional.
                  <Expandable title="properties">
                    <ResponseField name="effect" type="string">
                      What happens to a pod that doesn't tolerate this taint. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key" type="string">
                      The key of this taint or label. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of this taint or label. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="manual_lb_config" type="object">
      BareMetalAdminManualLbConfig represents configuration parameters for a manual load balancer. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether manual load balancing is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="port_config" type="object">
      BareMetalAdminPortConfig is the specification of load balancer ports. Optional.
      <Expandable title="properties">
        <ResponseField name="control_plane_load_balancer_port" type="number">
          The port that control plane hosted load balancers will listen on. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vip_config" type="object">
      BareMetalAdminVipConfig for bare metal load balancer configurations. Optional.
      <Expandable title="properties">
        <ResponseField name="control_plane_vip" type="string">
          The VIP which you previously set aside for the Kubernetes API of this bare metal admin cluster. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="local_name" type="string">
  Output only. The object name of the bare metal cluster custom resource. This field is used to support conflicting names when enrolling existing clusters to the API. When used as a part of cluster enrollment, this field will differ from the ID in the resource name. For new clusters, this field will match the user provided cluster name and be visible in the last component of the resource name. It is not modifiable. All users should use this name to access their cluster using gkectl or kubectl and should expect to see the local name when viewing admin cluster controller logs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="maintenance_config" type="object">
  BareMetalAdminMaintenanceConfig specifies configurations to put bare metal Admin cluster CRs nodes in and out of maintenance. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="maintenance_address_cidr_blocks" type="list(string)">
      Required. All IPv4 address from these ranges will be placed into maintenance mode. Nodes in maintenance mode will be cordoned and drained. When both of these are true, the "baremetal.cluster.gke.io/maintenance" annotation will be set on the node resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="maintenance_status" type="object">
  BareMetalAdminMaintenanceStatus represents the maintenance status for bare metal Admin cluster CR's nodes. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="machine_drain_status" type="object">
      BareMetalAdminMachineDrainStatus represents the status of bare metal node machines that are undergoing drain operations. Optional.
      <Expandable title="properties">
        <ResponseField name="drained_machines" type="list(object)">
          The list of drained machines. Optional.
          <Expandable title="properties">
            <ResponseField name="node_ip" type="string">
              The IP address of this node. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="draining_machines" type="list(object)">
          The list of draning machines. Optional.
          <Expandable title="properties">
            <ResponseField name="node_ip" type="string">
              The IP address of this node. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pod_count" type="number">
              How many pods are scheduled on this node. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. The bare metal admin cluster resource name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="network_config" type="object">
  BareMetalAdminNetworkConfig specifies the cluster network configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="advanced_networking" type="bool">
      Enables the use of advanced Anthos networking features, such as Bundled Load Balancing with BGP or the egress NAT gateway. Setting configuration for advanced networking features will automatically set this flag. Optional.
    </ResponseField>
    <ResponseField name="island_mode_cidr" type="object">
      BareMetalAdminIslandModeCidrConfig specifies the cluster CIDR configuration while running in island mode. Optional.
      <Expandable title="properties">
        <ResponseField name="pod_address_cidr_blocks" type="list(string)">
          Required. All pods in the cluster are assigned an RFC1918 IPv4 address from these ranges. This field cannot be changed after creation. Optional.
        </ResponseField>
        <ResponseField name="service_address_cidr_blocks" type="list(string)">
          Required. All services in the cluster are assigned an RFC1918 IPv4 address from these ranges. This field cannot be changed after creation. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="multiple_network_interfaces_config" type="object">
      Specifies the multiple networking interfaces cluster configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether to enable multiple network interfaces for your pods. When set network_config.advanced_networking is automatically set to true. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="node_access_config" type="object">
  Specifies the node access related settings for the bare metal admin cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="login_user" type="string">
      Required. LoginUser is the user name used to access node machines. It defaults to "root" if not set. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="node_config" type="object">
  BareMetalAdminWorkloadNodeConfig specifies the workload node configurations. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="max_pods_per_node" type="string">
      The maximum number of pods a node can run. The size of the CIDR range assigned to the node will be derived from this parameter. By default 110 Pods are created per Node. Upper bound is 250 for both HA and non-HA admin cluster. Lower bound is 64 for non-HA admin cluster and 32 for HA admin cluster. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="os_environment_config" type="object">
  Specifies operating system operation settings for cluster provisioning. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="package_repo_excluded" type="bool">
      Whether the package repo should be added when initializing bare metal machines. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="proxy" type="object">
  BareMetalAdminProxyConfig specifies the cluster proxy configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="no_proxy" type="list(string)">
      A list of IPs, hostnames, and domains that should skip the proxy. Examples: ["127.0.0.1", "example.com", ".corp", "localhost"]. Optional.
    </ResponseField>
    <ResponseField name="uri" type="string">
      Required. Specifies the address of your proxy server. Examples: `http://domain` WARNING: Do not provide credentials in the format `http://(username:password@)domain` these will be rejected by the server. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. If set, there are currently changes in flight to the bare metal Admin Cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="security_config" type="object">
  Specifies the security related settings for the bare metal admin cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="authorization" type="object">
      Authorization defines the On-Prem cluster authorization configuration to bootstrap onto the admin cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="admin_users" type="list(object)">
          For VMware and bare metal user clusters, users will be granted the cluster-admin role on the cluster, which provides full administrative access to the cluster. For bare metal admin clusters, users will be granted the cluster-view role, which limits users to read-only access. Optional.
          <Expandable title="properties">
            <ResponseField name="username" type="string">
              The username used to authenticate. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the bare metal admin cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="status" type="object">
  ResourceStatus describes why a cluster or node pool has a certain status. (e.g., ERROR or DEGRADED). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="conditions" type="list(object)">
      ResourceCondition provide a standard mechanism for higher-level status reporting from controller. Optional.
      <Expandable title="properties">
        <ResponseField name="last_transition_time" type="string">
          When this condition last changed from one status to another. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reason" type="string">
          A machine-readable reason code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_message" type="string">
      Human-friendly representation of the error message from controller. The error message can be temporary as the controller controller creates a cluster or node pool. If the error message persists for a longer period of time, it can be used to surface error message to indicate real problems requiring user intervention. Optional.
    </ResponseField>
    <ResponseField name="version" type="string">
      Reflect current version of the resource. Optional.
    </ResponseField>
    <ResponseField name="versions" type="object">
      Versions describes the mapping of a given version to the number of machines under this version. Optional.
      <Expandable title="properties">
        <ResponseField name="versions" type="list(object)">
          Shows the mapping of a given version to the number of machines under this version. Optional.
          <Expandable title="properties">
            <Note>`versions` recurs here (its own type contains itself); see the `versions` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="storage" type="object">
  BareMetalAdminStorageConfig specifies the cluster storage configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="lvp_node_mounts_config" type="object">
      Specifies the configs for local persistent volumes (PVs). Optional.
      <Expandable title="properties">
        <ResponseField name="path" type="string">
          Required. The host machine path. Optional.
        </ResponseField>
        <ResponseField name="storage_class" type="string">
          Required. The StorageClass name that PVs will be created with. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lvp_share_config" type="object">
      Specifies the configs for local persistent volumes under a shared file system. Optional.
      <Expandable title="properties">
        <ResponseField name="lvp_config" type="object">
          Specifies the configs for local persistent volumes (PVs). Optional.
          <Expandable title="properties">
            <ResponseField name="path" type="string">
              Required. The host machine path. Optional.
            </ResponseField>
            <ResponseField name="storage_class" type="string">
              Required. The StorageClass name that PVs will be created with. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="shared_path_pv_count" type="number">
          The number of subdirectories to create under path. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The unique identifier of the bare metal admin cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which this bare metal admin cluster was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="validation_check" type="object">
  ValidationCheck represents the result of preflight check. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="option" type="string">
      Options used for the validation check Optional.
    </ResponseField>
    <ResponseField name="scenario" type="string">
      Output only. The scenario when the preflight checks were run. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="status" type="object">
      ValidationCheckStatus defines the detailed validation check status. Optional.
      <Expandable title="properties">
        <ResponseField name="result" type="list(object)">
          Individual checks which failed as part of the Preflight check execution. Optional.
          <Expandable title="properties">
            <ResponseField name="category" type="string">
              The category this belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="details" type="string">
              Additional detail about this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="reason" type="string">
              A machine-readable reason code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
