---
title: "google_gkeonprem_vmware_admin_cluster"
description: "`google_gkeonprem_vmware_admin_cluster` is the administrative GKE on VMware cluster that manages the lifecycle of other VMware user cluster(s) running..."
---

`google_gkeonprem_vmware_admin_cluster` is the administrative GKE on VMware cluster that manages the lifecycle of other VMware user cluster(s) running on the same vSphere environment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_gkeonprem --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_gkeonprem/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gkeonprem "github.com/ubiquex/ubx-sdk-google/sdk/go/google_gkeonprem/gkeonprem"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware admin cluster"})

    		ubx.Resource(gkeonprem.GkeonpremVmwareAdminCluster, "example", gkeonprem.GkeonpremVmwareAdminClusterConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_gkeonprem --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GkeonpremVmwareAdminCluster } from "./local-sdk/google_gkeonprem/sdk/typescript/google_gkeonprem/gkeonprem/gkeonprem_vmware_admin_cluster.ts";

    export default stack("example", () => {
      intent({ summary: "example own vmware admin cluster" });

      resource(GkeonpremVmwareAdminCluster, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_gkeonprem --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_gkeonprem/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_gkeonprem.gkeonprem import GkeonpremVmwareAdminCluster, GkeonpremVmwareAdminClusterConfig

    def describe():
        ubx.intent("example own vmware admin cluster")

        ubx.resource(GkeonpremVmwareAdminCluster, "example", GkeonpremVmwareAdminClusterConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gkeonprem_vmware_admin_cluster called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="addon_node" type="object">
  VmwareAdminAddonNodeConfig contains add-on node configurations for VMware admin cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="auto_resize_config" type="object">
      Represents auto resizing configurations for the VMware user cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether to enable controle plane node auto resizing. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="annotations" type="map(string)">
  Annotations on the VMware admin cluster. This field has the same restrictions as Kubernetes annotations. The total size of all keys and values combined is limited to 256k. Key can have 2 segments: prefix (optional) and name (required), separated by a slash (/). Prefix must be a DNS subdomain. Name must be 63 characters or less, begin and end with alphanumerics, with dashes (-), underscores (_), dots (.), and alphanumerics between. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="anti_affinity_groups" type="object">
  Specifies anti affinity group config for the VMware user cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="aag_config_disabled" type="bool">
      Spread nodes across at least three physical hosts (requires at least three hosts). Enabled by default. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="authorization" type="object">
  VmwareAdminAuthorizationConfig represents configuration for admin cluster authorization. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="viewer_users" type="list(object)">
      For VMware admin clusters, users will be granted the cluster-viewer role on the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="username" type="string">
          The username used to authenticate. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="auto_repair_config" type="object">
  Specifies config to enable/disable auto repair. The cluster-health-controller is deployed only if Enabled is true. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Whether auto repair is enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bootstrap_cluster_membership" type="string">
  The bootstrap cluster this VMware admin cluster belongs to. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="control_plane_node" type="object">
  VmwareAdminControlPlaneNodeConfig contains control plane node configuration for VMware admin cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cpus" type="string">
      The number of vCPUs for the control-plane node of the admin cluster. Optional.
    </ResponseField>
    <ResponseField name="memory" type="string">
      The number of mebibytes of memory for the control-plane node of the admin cluster. Optional.
    </ResponseField>
    <ResponseField name="replicas" type="string">
      The number of control plane nodes for this VMware admin cluster. (default: 1 replica). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time at which VMware admin cluster was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  A human readable description of this VMware admin cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="enable_advanced_cluster" type="bool">
  Enable advanced cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="endpoint" type="string">
  Output only. The DNS name of VMware admin cluster's API server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Allows clients to perform consistent read-modify-writes through optimistic concurrency control. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fleet" type="object">
  Fleet related configuration. Fleets are a Google Cloud concept for logically organizing clusters, letting you use and manage multi-cluster capabilities and apply consistent policies across your systems. See [Anthos Fleets](`https://cloud.google.com/anthos/multicluster-management/fleets`) for more details on Anthos multi-cluster capabilities using Fleets. ## Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="membership" type="string">
      Output only. The name of the managed fleet Membership resource associated to this cluster. Membership names are formatted as `projects//locations//memberships/`. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="image_type" type="string">
  The OS image type for the VMware admin cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="load_balancer" type="object">
  VmwareAdminLoadBalancerConfig contains load balancer configuration for VMware admin cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="f5_config" type="object">
      VmwareAdminF5BigIpConfig represents configuration parameters for an F5 BIG-IP load balancer. Optional.
      <Expandable title="properties">
        <ResponseField name="address" type="string">
          The load balancer's IP address. Optional.
        </ResponseField>
        <ResponseField name="partition" type="string">
          The preexisting partition to be used by the load balancer. This partition is usually created for the admin cluster for example: 'my-f5-admin-partition'. Optional.
        </ResponseField>
        <ResponseField name="snat_pool" type="string">
          The pool name. Only necessary, if using SNAT. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="manual_lb_config" type="object">
      Configuration for a manually (rather than automatically) provisioned load balancer. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="addons_node_port" type="number">
          NodePort for add-ons server in the admin cluster. Optional.
        </ResponseField>
        <ResponseField name="control_plane_node_port" type="number">
          NodePort for control plane service. The Kubernetes API server in the admin cluster is implemented as a Service of type NodePort (ex. 30968). Optional.
        </ResponseField>
        <ResponseField name="ingress_http_node_port" type="number">
          NodePort for ingress service's http. The ingress service in the admin cluster is implemented as a Service of type NodePort (ex. 32527). Optional.
        </ResponseField>
        <ResponseField name="ingress_https_node_port" type="number">
          NodePort for ingress service's https. The ingress service in the admin cluster is implemented as a Service of type NodePort (ex. 30139). Optional.
        </ResponseField>
        <ResponseField name="konnectivity_server_node_port" type="number">
          NodePort for konnectivity server service running as a sidecar in each kube-apiserver pod (ex. 30564). Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="metal_lb_config" type="object">
      VmwareAdminMetalLbConfig represents configuration parameters for a MetalLB load balancer. For admin clusters, currently no configurations is needed. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether MetalLB is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="seesaw_config" type="object">
      VmwareSeesawConfig represents configuration parameters for an already existing Seesaw load balancer. IMPORTANT: Please note that the Anthos On-Prem API will not generate or update Seesaw configurations it can only bind a pre-existing configuration to a new user cluster. IMPORTANT: When attempting to create a user cluster with a pre-existing Seesaw load balancer you will need to follow some preparation steps before calling the 'CreateVmwareCluster' API method. First you will need to create the user cluster's namespace via kubectl. The namespace will need to use the following naming convention : -gke-onprem-mgmt or -gke-onprem-mgmt depending on whether you used the 'VmwareCluster.local_name' to disambiguate collisions; for more context see the documentation of 'VmwareCluster.local_name'. Once the namespace is created you will need to create a secret resource via kubectl. This secret will contain copies of your Seesaw credentials. The Secret must be called 'user-cluster-creds' and contain Seesaw's SSH and Cert credentials. The credentials must be keyed with the following names: 'seesaw-ssh-private-key', 'seesaw-ssh-public-key', 'seesaw-ssh-ca-key', 'seesaw-ssh-ca-cert'. Optional.
      <Expandable title="properties">
        <ResponseField name="enable_ha" type="bool">
          Enable two load balancer VMs to achieve a highly-available Seesaw load balancer. Optional.
        </ResponseField>
        <ResponseField name="group" type="string">
          In general the following format should be used for the Seesaw group name: seesaw-for-[cluster_name]. Optional.
        </ResponseField>
        <ResponseField name="ip_blocks" type="list(object)">
          The IP Blocks to be used by the Seesaw load balancer Optional.
          <Expandable title="properties">
            <ResponseField name="gateway" type="string">
              The default gateway IP address for this network. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ips" type="list(object)">
              The IP address(es) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="hostname" type="string">
                  The hostname of this node. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ip" type="string">
                  The IP address of the gkeonprem vmware admin cluster's ips. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="netmask" type="string">
              The subnet mask for this network. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="master_ip" type="string">
          MasterIP is the IP announced by the master of Seesaw group. Optional.
        </ResponseField>
        <ResponseField name="stackdriver_name" type="string">
          Name to be used by Stackdriver. Optional.
        </ResponseField>
        <ResponseField name="vms" type="list(string)">
          Names of the VMs created for this Seesaw group. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vip_config" type="object">
      VmwareAdminVipConfig for VMware load balancer configurations. Optional.
      <Expandable title="properties">
        <ResponseField name="addons_vip" type="string">
          The VIP to configure the load balancer for add-ons. Optional.
        </ResponseField>
        <ResponseField name="control_plane_vip" type="string">
          The VIP which you previously set aside for the Kubernetes API of the admin cluster. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="local_name" type="string">
  Output only. The object name of the VMware OnPremAdminCluster custom resource. This field is used to support conflicting names when enrolling existing clusters to the API. When used as a part of cluster enrollment, this field will differ from the ID in the resource name. For new clusters, this field will match the user provided cluster name and be visible in the last component of the resource name. It is not modifiable. All users should use this name to access their cluster using gkectl or kubectl and should expect to see the local name when viewing admin cluster controller logs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. The VMware admin cluster resource name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="network_config" type="object">
  VmwareAdminNetworkConfig contains network configuration for VMware admin cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="dhcp_ip_config" type="object">
      Represents the network configuration required for the VMware user clusters with DHCP IP configurations. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          enabled is a flag to mark if DHCP IP allocation is used for VMware user clusters. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ha_control_plane_config" type="object">
      Specifies HA admin control plane config. Optional.
      <Expandable title="properties">
        <ResponseField name="control_plane_ip_block" type="object">
          Represents a collection of IP addresses to assign to nodes. Optional.
          <Expandable title="properties">
            <ResponseField name="gateway" type="string">
              The network gateway used by the VMware user cluster. Optional.
            </ResponseField>
            <ResponseField name="ips" type="list(object)">
              The node's network configurations used by the VMware user cluster. Optional.
              <Expandable title="properties">
                <ResponseField name="hostname" type="string">
                  The hostname of this node. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ip" type="string">
                  The IP address of the gkeonprem vmware admin cluster's ips. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="netmask" type="string">
              The netmask used by the VMware user cluster. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="host_config" type="object">
      Represents the common parameters for all the hosts irrespective of their IP address. Optional.
      <Expandable title="properties">
        <ResponseField name="dns_search_domains" type="list(string)">
          DNS search domains. Optional.
        </ResponseField>
        <ResponseField name="dns_servers" type="list(string)">
          The DNS servers for the gkeonprem vmware admin cluster's host config. Optional.
        </ResponseField>
        <ResponseField name="ntp_servers" type="list(string)">
          The ntp servers for the gkeonprem vmware admin cluster's host config. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pod_address_cidr_blocks" type="list(string)">
      Required. All pods in the cluster are assigned an RFC1918 IPv4 address from these ranges. Only a single range is supported. This field cannot be changed after creation. Optional.
    </ResponseField>
    <ResponseField name="service_address_cidr_blocks" type="list(string)">
      Required. All services in the cluster are assigned an RFC1918 IPv4 address from these ranges. Only a single range is supported. This field cannot be changed after creation. Optional.
    </ResponseField>
    <ResponseField name="static_ip_config" type="object">
      Represents the network configuration required for the VMware user clusters with Static IP configurations. Optional.
      <Expandable title="properties">
        <ResponseField name="ip_blocks" type="list(object)">
          Represents the configuration values for static IP allocation to nodes. Optional.
          <Expandable title="properties">
            <ResponseField name="gateway" type="string">
              The default gateway IP address for this network. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ips" type="list(object)">
              The IP address(es) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="hostname" type="string">
                  The hostname of this node. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ip" type="string">
                  The IP address of the gkeonprem vmware admin cluster's ips. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="netmask" type="string">
              The subnet mask for this network. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vcenter_network" type="string">
      vcenter_network specifies vCenter network name. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="on_prem_version" type="string">
  The Anthos clusters on the VMware version for the admin cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="platform_config" type="object">
  VmwarePlatformConfig represents configuration for the VMware platform. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="bundles" type="list(object)">
      Output only. The list of bundles installed in the admin cluster. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="status" type="object">
          The current status of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="conditions" type="list(object)">
              The current condition(s) affecting this resource's own status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="last_transition_time" type="string">
                  When this condition last changed from one status to another. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable description of this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="reason" type="string">
                  A machine-readable reason code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="state" type="string">
                  The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="error_message" type="string">
              A human-readable description of the error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="version" type="string">
              The version identifier this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="versions" type="object">
              The version(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="versions" type="list(object)">
                  The version(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <Note>`versions` recurs here (its own type contains itself); see the `versions` field's own properties above for the repeating pattern.</Note>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="version" type="string">
          The version identifier this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="platform_version" type="string">
      Output only. The platform version e.g. 1.13.2. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="required_platform_version" type="string">
      Input only. The required platform version e.g. 1.13.1. If the current platform version is lower than the target version, the platform version will be updated to the target version. If the target version is not installed in the platform (bundle versions), download the target version bundle. Optional.
    </ResponseField>
    <ResponseField name="status" type="object">
      ResourceStatus describes why a cluster or node pool has a certain status. (e.g., ERROR or DEGRADED). Optional.
      <Expandable title="properties">
        <ResponseField name="conditions" type="list(object)">
          ResourceCondition provide a standard mechanism for higher-level status reporting from controller. Optional.
          <Expandable title="properties">
            <ResponseField name="last_transition_time" type="string">
              When this condition last changed from one status to another. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="reason" type="string">
              A machine-readable reason code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="error_message" type="string">
          Human-friendly representation of the error message from controller. The error message can be temporary as the controller controller creates a cluster or node pool. If the error message persists for a longer period of time, it can be used to surface error message to indicate real problems requiring user intervention. Optional.
        </ResponseField>
        <ResponseField name="version" type="string">
          Reflect current version of the resource. Optional.
        </ResponseField>
        <ResponseField name="versions" type="object">
          Versions describes the mapping of a given version to the number of machines under this version. Optional.
          <Expandable title="properties">
            <ResponseField name="versions" type="list(object)">
              Shows the mapping of a given version to the number of machines under this version. Optional.
              <Expandable title="properties">
                <Note>`versions` recurs here (its own type contains itself); see the `versions` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="prepared_secrets" type="object">
  VmwareAdminPreparedSecretsConfig represents configuration for admin cluster prepared secrets. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Whether prepared secrets is enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="private_registry_config" type="object">
  VmwareAdminPrivateRegistryConfig represents configuration for admin cluster registry. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="address" type="string">
      The registry address. Optional.
    </ResponseField>
    <ResponseField name="ca_cert" type="string">
      When the container runtime pulls an image from private registry, the registry must prove its identity by presenting a certificate. The registry's certificate is signed by a certificate authority (CA). The container runtime uses the CA's certificate to validate the registry's certificate. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="proxy" type="object">
  VmwareAdminProxy represents configuration for admin cluster proxy. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="no_proxy" type="string">
      A comma-separated list of IP addresses, IP address ranges, host names, and domain names that should not go through the proxy server. When Google Distributed Cloud sends a request to one of these addresses, hosts, or domains, the request is sent directly. Optional.
    </ResponseField>
    <ResponseField name="url" type="string">
      The HTTP address of proxy server. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. If set, there are currently changes in flight to the VMware admin cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of VMware admin cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="status" type="object">
  ResourceStatus describes why a cluster or node pool has a certain status. (e.g., ERROR or DEGRADED). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="conditions" type="list(object)">
      ResourceCondition provide a standard mechanism for higher-level status reporting from controller. Optional.
      <Expandable title="properties">
        <ResponseField name="last_transition_time" type="string">
          When this condition last changed from one status to another. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reason" type="string">
          A machine-readable reason code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_message" type="string">
      Human-friendly representation of the error message from controller. The error message can be temporary as the controller controller creates a cluster or node pool. If the error message persists for a longer period of time, it can be used to surface error message to indicate real problems requiring user intervention. Optional.
    </ResponseField>
    <ResponseField name="version" type="string">
      Reflect current version of the resource. Optional.
    </ResponseField>
    <ResponseField name="versions" type="object">
      Versions describes the mapping of a given version to the number of machines under this version. Optional.
      <Expandable title="properties">
        <ResponseField name="versions" type="list(object)">
          Shows the mapping of a given version to the number of machines under this version. Optional.
          <Expandable title="properties">
            <Note>`versions` recurs here (its own type contains itself); see the `versions` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The unique identifier of the VMware admin cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which VMware admin cluster was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="validation_check" type="object">
  ValidationCheck represents the result of preflight check. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="option" type="string">
      Options used for the validation check Optional.
    </ResponseField>
    <ResponseField name="scenario" type="string">
      Output only. The scenario when the preflight checks were run. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="status" type="object">
      ValidationCheckStatus defines the detailed validation check status. Optional.
      <Expandable title="properties">
        <ResponseField name="result" type="list(object)">
          Individual checks which failed as part of the Preflight check execution. Optional.
          <Expandable title="properties">
            <ResponseField name="category" type="string">
              The category this belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="details" type="string">
              Additional detail about this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="reason" type="string">
              A machine-readable reason code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vcenter" type="object">
  VmwareAdminVCenterConfig contains VCenter configuration for VMware admin cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="address" type="string">
      The vCenter IP address. Optional.
    </ResponseField>
    <ResponseField name="ca_cert_data" type="string">
      Contains the vCenter CA certificate public key for SSL verification. Optional.
    </ResponseField>
    <ResponseField name="cluster" type="string">
      The name of the vCenter cluster for the admin cluster. Optional.
    </ResponseField>
    <ResponseField name="data_disk" type="string">
      The name of the virtual machine disk (VMDK) for the admin cluster. Optional.
    </ResponseField>
    <ResponseField name="datacenter" type="string">
      The name of the vCenter datacenter for the admin cluster. Optional.
    </ResponseField>
    <ResponseField name="datastore" type="string">
      The name of the vCenter datastore for the admin cluster. Optional.
    </ResponseField>
    <ResponseField name="folder" type="string">
      The name of the vCenter folder for the admin cluster. Optional.
    </ResponseField>
    <ResponseField name="resource_pool" type="string">
      The name of the vCenter resource pool for the admin cluster. Optional.
    </ResponseField>
    <ResponseField name="storage_policy_name" type="string">
      The name of the vCenter storage policy for the user cluster. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
