---
title: "google_gkeonprem_vmware_node_pool"
description: "`google_gkeonprem_vmware_node_pool` is a group of worker node(s) with shared configuration within a GKE on VMware cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gkeonprem "github.com/ubiquex/ubx-sdk-google/sdk/go/google/gkeonprem"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware node pool"})

    		ubx.Resource(gkeonprem.VmwareNodePool, "example", gkeonprem.VmwareNodePoolConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwareNodePool } from "@ubx/sdk-google/google/gkeonprem/vmware_node_pool";

    export default stack("example", () => {
      intent({ summary: "example own vmware node pool" });

      resource(VmwareNodePool, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.gkeonprem.vmware_node_pool import VmwareNodePool, VmwareNodePoolConfig

    def describe():
        ubx.intent("example own vmware node pool")

        ubx.resource(VmwareNodePool, "example", VmwareNodePoolConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gkeonprem_vmware_node_pool called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="annotations" type="map(string)">
  Annotations on the node pool. This field has the same restrictions as Kubernetes annotations. The total size of all keys and values combined is limited to 256k. Key can have 2 segments: prefix (optional) and name (required), separated by a slash (/). Prefix must be a DNS subdomain. Name must be 63 characters or less, begin and end with alphanumerics, with dashes (-), underscores (_), dots (.), and alphanumerics between. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="config" type="object">
  Parameters that describe the configuration of all nodes within a given node pool. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="boot_disk_size_gb" type="string">
      VMware disk size to be used during creation. Optional.
    </ResponseField>
    <ResponseField name="cpus" type="string">
      The number of CPUs for each node in the node pool. Optional.
    </ResponseField>
    <ResponseField name="enable_load_balancer" type="bool">
      Allow node pool traffic to be load balanced. Only works for clusters with MetalLB load balancers. Optional.
    </ResponseField>
    <ResponseField name="image" type="string">
      The OS image name in vCenter, only valid when using Windows. Optional.
    </ResponseField>
    <ResponseField name="image_type" type="string">
      Required. The OS image to be used for each node in a node pool. Currently `cos`, `cos_cgv2`, `ubuntu`, `ubuntu_cgv2`, `ubuntu_containerd` and `windows` are supported. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      The map of Kubernetes labels (key/value pairs) to be applied to each node. These will added in addition to any default label(s) that Kubernetes may apply to the node. In case of conflict in label keys, the applied set may differ depending on the Kubernetes version -- it's best to assume the behavior is undefined and conflicts should be avoided. For more information, including usage and the valid values, see: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/ Optional.
    </ResponseField>
    <ResponseField name="memory_mb" type="string">
      The megabytes of memory for each node in the node pool. Optional.
    </ResponseField>
    <ResponseField name="replicas" type="string">
      The number of nodes in the node pool. Optional.
    </ResponseField>
    <ResponseField name="taints" type="list(object)">
      The initial taints assigned to nodes of this node pool. Optional.
      <Expandable title="properties">
        <ResponseField name="effect" type="string">
          What happens to a pod that doesn't tolerate this taint. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key" type="string">
          The key of this taint or label. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of this taint or label. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vsphere_config" type="object">
      VmwareVsphereConfig represents configuration for the VMware VCenter for node pool. Optional.
      <Expandable title="properties">
        <ResponseField name="datastore" type="string">
          The name of the vCenter datastore. Inherited from the user cluster. Optional.
        </ResponseField>
        <ResponseField name="host_groups" type="list(string)">
          Vsphere host groups to apply to all VMs in the node pool Optional.
        </ResponseField>
        <ResponseField name="tags" type="list(object)">
          Tags to apply to VMs. Optional.
          <Expandable title="properties">
            <ResponseField name="category" type="string">
              The category this belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tag" type="string">
              A label attached to this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time at which this node pool was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. The time at which this node pool was deleted. If the resource is not deleted, this must be empty Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name for the node pool. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Allows clients to perform consistent read-modify-writes through optimistic concurrency control. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. The resource name of this node pool. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="node_pool_autoscaling" type="object">
  NodePoolAutoscaling config for the NodePool to allow for the kubernetes to scale NodePool. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="max_replicas" type="number">
      Maximum number of replicas in the NodePool. Optional.
    </ResponseField>
    <ResponseField name="min_replicas" type="number">
      Minimum number of replicas in the NodePool. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="on_prem_version" type="string">
  Anthos version for the node pool. Defaults to the user cluster version. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. If set, there are currently changes in flight to the node pool. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the node pool. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="status" type="object">
  ResourceStatus describes why a cluster or node pool has a certain status. (e.g., ERROR or DEGRADED). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="conditions" type="list(object)">
      ResourceCondition provide a standard mechanism for higher-level status reporting from controller. Optional.
      <Expandable title="properties">
        <ResponseField name="last_transition_time" type="string">
          When this condition last changed from one status to another. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reason" type="string">
          A machine-readable reason code. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_message" type="string">
      Human-friendly representation of the error message from controller. The error message can be temporary as the controller controller creates a cluster or node pool. If the error message persists for a longer period of time, it can be used to surface error message to indicate real problems requiring user intervention. Optional.
    </ResponseField>
    <ResponseField name="version" type="string">
      Reflect current version of the resource. Optional.
    </ResponseField>
    <ResponseField name="versions" type="object">
      Versions describes the mapping of a given version to the number of machines under this version. Optional.
      <Expandable title="properties">
        <ResponseField name="versions" type="list(object)">
          Shows the mapping of a given version to the number of machines under this version. Optional.
          <Expandable title="properties">
            <Note>`versions` recurs here (its own type contains itself); see the `versions` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. The unique identifier of the node pool. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which this node pool was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
