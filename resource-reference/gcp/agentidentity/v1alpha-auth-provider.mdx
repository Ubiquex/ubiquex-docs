---
title: "google_agentidentity_v1alpha_auth_provider"
description: "`google_agentidentity_v1alpha_auth_provider` registers an external OAuth or API-key credential source with Agent Identity's own auth manager..."
---

<Warning>
  This is Google Cloud's own **v1alpha** release channel (alpha). Google may change or remove alpha/preview endpoints at any time, often with no deprecation notice -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_agentidentity_auth_provider`.
</Warning>

`google_agentidentity_v1alpha_auth_provider` registers an external OAuth or API-key credential source with Agent Identity's own auth manager, letting agents authenticate to outbound tool(s) via that provider's own credential flow.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	agentidentity "github.com/ubiquex/ubx-sdk-google/sdk/go/google/agentidentity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1alpha auth provider"})

    		ubx.Resource(agentidentity.V1alphaAuthProvider, "example", agentidentity.V1alphaAuthProviderConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1alphaAuthProvider } from "@ubx/sdk-google/google/agentidentity/v1alpha_auth_provider";

    export default stack("example", () => {
      intent({ summary: "example own v1alpha auth provider" });

      resource(V1alphaAuthProvider, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.agentidentity.v1alpha_auth_provider import V1alphaAuthProvider, V1alphaAuthProviderConfig

    def describe():
        ubx.intent("example own v1alpha auth provider")

        ubx.resource(V1alphaAuthProvider, "example", V1alphaAuthProviderConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_agentidentity_v1alpha_auth_provider called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="allowed_scopes" type="list(string)">
  Optional. List of scopes that are allowed to be requested for this auth_provider. If this list is non-empty, only scopes within this list may be requested. If this list is empty, all scopes may be requested. Scopes appearing in `blocked_scopes` are disallowed even if they appear in `allowed_scopes`. The number of allowed scopes is limited to 200. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="auth_provider_type_params" type="object">
  AuthProvider type specific parameters. Required when creating an auth_provider. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="api_key" type="object">
      Message describing ApiKeyParams object. Optional.
      <Expandable title="properties">
        <ResponseField name="api_key" type="string">
          Optional. Input only. The API key for this auth_provider. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ge_auth_provider" type="any">
      Message describing GeminiEnterpriseAuthProviderParams object. Optional.
    </ResponseField>
    <ResponseField name="three_legged_oauth" type="object">
      Message describing ThreeLeggedOAuth object. Optional.
      <Expandable title="properties">
        <ResponseField name="authorization_url" type="string">
          Optional. The authorization endpoint to send users to for consenting to delegate to the agent. eg. "https://auth.atlassian.com/authorize" Optional.
        </ResponseField>
        <ResponseField name="client_id" type="string">
          Optional. The client ID of the OAuth client. Optional.
        </ResponseField>
        <ResponseField name="client_secret" type="string">
          Optional. Input only. The client secret of the OAuth client. Optional.
        </ResponseField>
        <ResponseField name="default_continue_uri" type="string">
          Optional. The default continue URI for 3LO flow and it will be used when no continue URI is provided in the RetrieveCredentials request. Optional.
        </ResponseField>
        <ResponseField name="enable_pkce" type="bool">
          Optional. Enables Proof Key for Code Exchange (PKCE) for the OAuth flow to prevent authorization code interception attacks. Optional.
        </ResponseField>
        <ResponseField name="redirect_url" type="string">
          Output only. The redirect URL this auth_provider uses for the OAuth exchange. This is deterministic based on the name of the auth_provider. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="token_url" type="string">
          Optional. The token endpoint for requesting tokens on behalf of an end user. eg. "https://auth.atlassian.com/oauth/token" Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="two_legged_oauth" type="object">
      Message describing TwoLeggedOAuth object. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Optional. The client ID of the OAuth client. Optional.
        </ResponseField>
        <ResponseField name="client_secret" type="string">
          Optional. Input only. The client secret of the OAuth client. Optional.
        </ResponseField>
        <ResponseField name="token_url" type="string">
          Optional. The token endpoint of the OAuth client. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="blocked_scopes" type="list(string)">
  Optional. List of scopes that are blocked from being requested for this auth_provider. If a scope appears in this list, it will not be requested, even if it also appears in `allowed_scopes`. `blocked_scopes` takes precedence over `allowed_scopes`. The number of blocked scopes is limited to 200. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. [Output only] Create time stamp Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="deleted" type="bool">
  Output only. This is set to true if the auth_provider is deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the resource. Must be less than 256 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Output only. The time when the auth_provider will expire. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels as key value pairs Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The full resource name of the auth_provider. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/authProviders/&#123;auth_provider&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the auth_provider. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. [Output only] Update time stamp Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="workload_ids" type="list(string)">
  Optional. Input only. Represents the workload identity in IAM `principal://` format of the agent(s) that will use this AuthProvider. Example: `principal://agents.global.org-$&#123;ORG_ID&#125;.system.id.goog/resources/aiplatform/projects/&#123;PROJECT_ID&#125;/locations/&#123;LOCATIONS&#125;/reasoningEngines/&#123;ID&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>
