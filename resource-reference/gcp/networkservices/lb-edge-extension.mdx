---
title: "google_networkservices_lb_edge_extension"
description: "`google_networkservices_lb_edge_extension` runs a Service Extension at a load balancer's own edge..."
---

`google_networkservices_lb_edge_extension` runs a Service Extension at a load balancer's own edge, letting it influence backend selection and Cloud CDN cache key computation by modifying request headers before caching/routing decisions are made.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_networkservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_networkservices/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networkservices "github.com/ubiquex/ubx-sdk-google/sdk/go/google_networkservices/networkservices"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lb edge extension"})

    		ubx.Resource(networkservices.NetworkservicesLbEdgeExtension, "ci-runner", networkservices.NetworkservicesLbEdgeExtensionConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_networkservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkservicesLbEdgeExtension } from "./local-sdk/google_networkservices/sdk/typescript/google_networkservices/networkservices/networkservices_lb_edge_extension.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own lb edge extension" });

      resource(NetworkservicesLbEdgeExtension, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_networkservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_networkservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_networkservices.networkservices import NetworkservicesLbEdgeExtension, NetworkservicesLbEdgeExtensionConfig

    def describe():
        ubx.intent("payments own lb edge extension")

        ubx.resource(NetworkservicesLbEdgeExtension, "ci-runner", NetworkservicesLbEdgeExtensionConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_networkservices_lb_edge_extension called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the resource was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. A human-readable description of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="extension_chains" type="list(object)">
  Required. A set of ordered extension chains that contain the match conditions and extensions to execute. Match conditions for each extension chain are evaluated in sequence for a given request. The first extension chain that has a condition that matches the request is executed. Any subsequent extension chains do not execute. Limited to 5 extension chains per resource. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="extensions" type="list(object)">
      Additional plugin(s) or extension(s) applied to this traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="authority" type="string">
          The `:authority` (Host) value this rule matches or sets. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fail_open" type="bool">
          Whether traffic is allowed through when this check itself fails to run, rather than being blocked. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="forward_attributes" type="list(string)">
          Additional caller-defined attribute(s) forwarded alongside this request. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="forward_headers" type="list(string)">
          The header(s) forwarded from the original request. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="metadata" type="map(any)">
          Free-form key/value metadata attached to this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="observability_mode" type="bool">
          Whether this extension only observes traffic, without modifying it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="request_body_send_mode" type="string">
          Whether the request body is streamed or buffered when sent to this extension. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="response_body_send_mode" type="string">
          Whether the response body is streamed or buffered when sent to this extension. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service" type="string">
          A reference to the backend service this routes traffic to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="supported_events" type="list(string)">
          The event type(s) this plugin can respond to, e.g. request headers or response body. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="timeout" type="string">
          How long to wait before this operation is considered to have timed out. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="match_condition" type="object">
      The condition that must hold for this rule to apply. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="cel_expression" type="string">
          A Common Expression Language expression evaluated to determine whether this rule applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="forwarding_rules" type="list(string)">
  Required. A list of references to the forwarding rules to which this service extension is attached. At least one forwarding rule is required. Only one `LbEdgeExtension` resource can be associated with a forwarding rule. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Set of labels associated with the `LbEdgeExtension` resource. The format must comply with [the requirements for labels](https://cloud.google.com/compute/docs/labeling-resources#requirements) for Google Cloud resources. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="load_balancing_scheme" type="string">
  Required. All forwarding rules referenced by this extension must share the same load balancing scheme. Supported values: `EXTERNAL_MANAGED`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. Identifier. Name of the `LbEdgeExtension` resource in the following format: `projects/&#123;project&#125;/locations/&#123;location&#125;/lbEdgeExtensions/&#123;lb_edge_extension&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when the resource was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
