---
title: "google_discovery_engine_schema"
description: "Real, generated bindings for `google_discovery_engine_schema`."
---

`google_discovery_engine_schema` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discovery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/discovery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine schema"})

    		ubx.Resource(discovery.EngineSchema, "ci-runner", discovery.EngineSchemaConfig{
    			DataStoreId: "example",
    			Location:    "example",
    			SchemaId:    "example",
    			JsonSchema:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineSchema } from "jsr:@ubx/sdk-google/google/discovery/engine_schema";

    export default stack("payments", () => {
      intent({ summary: "payments own engine schema" });

      resource(EngineSchema, "ci-runner", {
        dataStoreId: "example",
        location: "example",
        schemaId: "example",
        jsonSchema: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.discovery import EngineSchema, EngineSchemaConfig

    def describe():
        ubx.intent("payments own engine schema")

        ubx.resource(EngineSchema, "ci-runner", EngineSchemaConfig(
            data_store_id="example",
            location="example",
            schema_id="example",
            json_schema="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discovery_engine_schema, with data store id
    example, location example, schema id example, json schema
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="json_schema" type="string">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="schema_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
