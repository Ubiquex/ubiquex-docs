---
title: "google_discovery_engine_target_site"
description: "Real, generated bindings for `google_discovery_engine_target_site`."
---

`google_discovery_engine_target_site` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discovery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/discovery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine target site"})

    		ubx.Resource(discovery.EngineTargetSite, "ci-runner", discovery.EngineTargetSiteConfig{
    			DataStoreId:        "example",
    			Location:           "example",
    			ProvidedUriPattern: "example",
    			ExactMatch:         true,
    			Type:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineTargetSite } from "jsr:@ubx/sdk-google/google/discovery/engine_target_site";

    export default stack("payments", () => {
      intent({ summary: "payments own engine target site" });

      resource(EngineTargetSite, "ci-runner", {
        dataStoreId: "example",
        location: "example",
        providedUriPattern: "example",
        exactMatch: true,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.discovery import EngineTargetSite, EngineTargetSiteConfig

    def describe():
        ubx.intent("payments own engine target site")

        ubx.resource(EngineTargetSite, "ci-runner", EngineTargetSiteConfig(
            data_store_id="example",
            location="example",
            provided_uri_pattern="example",
            exact_match=True,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discovery_engine_target_site, with data store id
    example, location example, provided uri pattern example, exact
    match true, type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_store_id" type="string" required>
  The unique id of the data store. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="exact_match" type="bool">
  If set to false, a uri_pattern is generated to include all pages whose address contains the provided_uri_pattern. If set to true, an uri_pattern is generated to try to be an exact match of the provided_uri_pattern or just the specific page if the provided_uri_pattern is a specific one. provided_uri_pattern is always normalized to generate the URI pattern to be used by the search engine. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The geographic location where the data store should reside. The value can only be one of "global", "us" and "eu". Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="provided_uri_pattern" type="string" required>
  The user provided URI pattern from which the 'generated_uri_pattern' is generated. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The possible target site types. Possible values: ["INCLUDE", "EXCLUDE"] Optional.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="failure_reason" type="list(object)">
  Site search indexing failure reasons. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="quota_failure" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="total_required_quota" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="generated_uri_pattern" type="string">
  This is system-generated based on the 'provided_uri_pattern'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="indexing_status" type="string">
  The indexing status. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The unique full resource name of the target site. Values are of the format 'projects/&#123;project&#125;/locations/&#123;location&#125;/collections/&#123;collection_id&#125;/dataStores/&#123;data_store_id&#125;/siteSearchEngine/targetSites/&#123;target_site_id&#125;'. This field must be a UTF-8 encoded string with a length limit of 1024 characters. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="root_domain_uri" type="string">
  Root domain of the 'provided_uri_pattern'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="site_verification_info" type="list(object)">
  Site ownership and validity verification status. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="site_verification_state" type="string">
    </ResponseField>
    <ResponseField name="verify_time" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_site_id" type="string">
  The unique id of the target site. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The target site's last updated time. Computed by GCP after `ship`.
</ResponseField>
