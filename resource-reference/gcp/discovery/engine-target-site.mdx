---
title: "google_discovery_engine_target_site"
description: "Real, generated bindings for `google_discovery_engine_target_site`."
---

`google_discovery_engine_target_site` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	discovery "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/discovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine target site"})

    		ubx.Resource(discovery.EngineTargetSite, "ci-runner", discovery.EngineTargetSiteConfig{
    			DataStoreId:        "example",
    			Location:           "example",
    			ProvidedUriPattern: "example",
    			ExactMatch:         true,
    			Type:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineTargetSite } from "jsr:@ubx/sdk-google/google/discovery/engine_target_site";

    export default stack("payments", () => {
      intent({ summary: "payments own engine target site" });

      resource(EngineTargetSite, "ci-runner", {
        dataStoreId: "example",
        location: "example",
        providedUriPattern: "example",
        exactMatch: true,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.discovery import EngineTargetSite, EngineTargetSiteConfig

    def describe():
        ubx.intent("payments own engine target site")

        ubx.resource(EngineTargetSite, "ci-runner", EngineTargetSiteConfig(
            data_store_id="example",
            location="example",
            provided_uri_pattern="example",
            exact_match=True,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discovery_engine_target_site, with data store id
    example, location example, provided uri pattern example, exact
    match true, type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="exact_match" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="provided_uri_pattern" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="failure_reason" type="list(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="quota_failure" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="total_required_quota" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="generated_uri_pattern" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="indexing_status" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="root_domain_uri" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="site_verification_info" type="list(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="site_verification_state" type="string">
    </ResponseField>
    <ResponseField name="verify_time" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_site_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
