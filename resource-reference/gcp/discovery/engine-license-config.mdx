---
title: "google_discovery_engine_license_config"
description: "Real, generated bindings for `google_discovery_engine_license_config`."
---

`google_discovery_engine_license_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discovery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/discovery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine license config"})

    		ubx.Resource(discovery.EngineLicenseConfig, "ci-runner", discovery.EngineLicenseConfigConfig{
    			LicenseConfigId:  "example",
    			LicenseCount:     1,
    			Location:         "example",
    			SubscriptionTerm: "example",
    			SubscriptionTier: "example",
    			AutoRenew:        true,
    			FreeTrial:        true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineLicenseConfig } from "jsr:@ubx/sdk-google/google/discovery/engine_license_config";

    export default stack("payments", () => {
      intent({ summary: "payments own engine license config" });

      resource(EngineLicenseConfig, "ci-runner", {
        licenseConfigId: "example",
        licenseCount: 1,
        location: "example",
        subscriptionTerm: "example",
        subscriptionTier: "example",
        autoRenew: true,
        freeTrial: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.discovery import EngineLicenseConfig, EngineLicenseConfigConfig

    def describe():
        ubx.intent("payments own engine license config")

        ubx.resource(EngineLicenseConfig, "ci-runner", EngineLicenseConfigConfig(
            license_config_id="example",
            license_count=1,
            location="example",
            subscription_term="example",
            subscription_tier="example",
            auto_renew=True,
            free_trial=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discovery_engine_license_config, with license
    config id example, license count 1, location example,
    subscription term example, subscription tier example, auto renew
    true, free trial true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_renew" type="bool">
  Whether the license config should be auto renewed when it reaches the end date. Optional.
</ResponseField>

<ResponseField name="end_date" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="day" type="number">
    </ResponseField>
    <ResponseField name="month" type="number">
    </ResponseField>
    <ResponseField name="year" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="free_trial" type="bool">
  Whether the license config is for free trial. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="license_config_id" type="string" required>
  The unique id of the license config. Required.
</ResponseField>

<ResponseField name="license_count" type="number" required>
  Number of licenses purchased. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  The geographic location where the data store should reside. The value can only be one of "global", "us" and "eu". Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="start_date" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="day" type="number">
    </ResponseField>
    <ResponseField name="month" type="number">
    </ResponseField>
    <ResponseField name="year" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="subscription_term" type="string" required>
  Subscription term. Possible values: ["SUBSCRIPTION_TERM_UNSPECIFIED", "SUBSCRIPTION_TERM_ONE_MONTH", "SUBSCRIPTION_TERM_ONE_YEAR", "SUBSCRIPTION_TERM_THREE_YEARS", "SUBSCRIPTION_TERM_THREE_MONTHS", "SUBSCRIPTION_TERM_FOURTEEN_DAYS", "SUBSCRIPTION_TERM_CUSTOM"] Required.
</ResponseField>

<ResponseField name="subscription_tier" type="string" required>
  Subscription tier information for the license config. Possible values: ["SUBSCRIPTION_TIER_UNSPECIFIED", "SUBSCRIPTION_TIER_SEARCH", "SUBSCRIPTION_TIER_SEARCH_AND_ASSISTANT", "SUBSCRIPTION_TIER_NOTEBOOK_LM", "SUBSCRIPTION_TIER_FRONTLINE_WORKER", "SUBSCRIPTION_TIER_AGENTSPACE_STARTER", "SUBSCRIPTION_TIER_AGENTSPACE_BUSINESS", "SUBSCRIPTION_TIER_ENTERPRISE", "SUBSCRIPTION_TIER_EDU", "SUBSCRIPTION_TIER_EDU_PRO"] Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="end_date" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="day" type="number">
    </ResponseField>
    <ResponseField name="month" type="number">
    </ResponseField>
    <ResponseField name="year" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The unique full resource name of the license config. Values are of the format 'projects/&#123;project&#125;/locations/&#123;location&#125;/licenseConfigs/&#123;license_config&#125;'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="start_date" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="day" type="number">
    </ResponseField>
    <ResponseField name="month" type="number">
    </ResponseField>
    <ResponseField name="year" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
