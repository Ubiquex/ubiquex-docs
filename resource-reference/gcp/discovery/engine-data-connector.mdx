---
title: "google_discovery_engine_data_connector"
description: "Real, generated bindings for `google_discovery_engine_data_connector`."
---

`google_discovery_engine_data_connector` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discovery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/discovery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine data connector"})

    		ubx.Resource(discovery.EngineDataConnector, "ci-runner", discovery.EngineDataConnectorConfig{
    			CollectionDisplayName:      "example-collection-display-name",
    			CollectionId:               "example",
    			DataSource:                 "example",
    			Location:                   "example",
    			RefreshInterval:            "example",
    			AutoRunDisabled:            true,
    			ConnectorModes:             []string{"example"},
    			IncrementalRefreshInterval: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineDataConnector } from "jsr:@ubx/sdk-google/google/discovery/engine_data_connector";

    export default stack("payments", () => {
      intent({ summary: "payments own engine data connector" });

      resource(EngineDataConnector, "ci-runner", {
        collectionDisplayName: "example-collection-display-name",
        collectionId: "example",
        dataSource: "example",
        location: "example",
        refreshInterval: "example",
        autoRunDisabled: true,
        connectorModes: ["example"],
        incrementalRefreshInterval: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.discovery import EngineDataConnector, EngineDataConnectorConfig

    def describe():
        ubx.intent("payments own engine data connector")

        ubx.resource(EngineDataConnector, "ci-runner", EngineDataConnectorConfig(
            collection_display_name="example-collection-display-name",
            collection_id="example",
            data_source="example",
            location="example",
            refresh_interval="example",
            auto_run_disabled=True,
            connector_modes=["example"],
            incremental_refresh_interval="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discovery_engine_data_connector, with collection
    display name example-collection-display-name, collection id
    example, data source example, location example, refresh interval
    example, auto run disabled true, connector modes a set value,
    incremental refresh interval example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="action_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="action_params" type="map(string)">
    </ResponseField>
    <ResponseField name="create_bap_connection" type="bool">
    </ResponseField>
    <ResponseField name="is_action_configured" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="auto_run_disabled" type="bool">
  Indicates whether full syncs are paused for this connector Optional.
</ResponseField>

<ResponseField name="bap_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enabled_actions" type="list(string)">
    </ResponseField>
    <ResponseField name="supported_connector_modes" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="collection_display_name" type="string" required>
  The display name of the Collection. Should be human readable, used to display collections in the Console Dashboard. UTF-8 encoded string with limit of 1024 characters. Required.
</ResponseField>

<ResponseField name="collection_id" type="string" required>
  The ID to use for the Collection, which will become the final component of the Collection's resource name. A new Collection is created as part of the DataConnector setup. DataConnector is a singleton resource under Collection, managing all DataStores of the Collection. This field must conform to [RFC-1034](https://tools.ietf.org/html/rfc1034) standard with a length limit of 63 characters. Otherwise, an INVALID_ARGUMENT error is returned. Required.
</ResponseField>

<ResponseField name="connector_modes" type="list(string)">
  The modes enabled for this connector. The possible value can be: 'DATA_INGESTION', 'ACTIONS', 'FEDERATED' 'EUA', 'FEDERATED_AND_EUA'. Optional.
</ResponseField>

<ResponseField name="data_source" type="string" required>
  The identifier for the data source. This is a partial list of supported connectors. Please refer to the [documentation](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/introduction-to-connectors-and-data-stores) for the full list of connectors. Supported first-party connectors include: * 'bigquery' * 'gcp_fhir' * 'google_mail' * 'google_drive' * 'google_calendar' * 'google_chat' Supported third-party connectors include: Generally available (GA) connectors: * 'onedrive' * 'outlook' * 'confluence' * 'jira' * 'servicenow' * 'sharepoint' Preview connectors: * 'asana' * 'azure_active_directory' * 'box' * 'canva' * 'confluence_server' * 'custom_connector' * 'docusign' * 'dropbox' * 'dynamics365' * 'github' * 'gitlab' * 'hubspot' * 'jira_server' * 'linear' * 'native_cloud_identity' * 'notion' * 'okta' * 'pagerduty' * 'peoplesoft' * 'salesforce' * 'shopify' * 'slack' * 'snowflake' * 'teams' * 'trello' * 'workday' * 'zendesk' Required.
</ResponseField>

<ResponseField name="data_source_version" type="number">
  The version of the data source. For example, '3' for Jira v3. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="destination_configs" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="destinations" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="host" type="string">
        </ResponseField>
        <ResponseField name="port" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="key" type="string">
    </ResponseField>
    <ResponseField name="params" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="entities" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="data_store" type="string">
    </ResponseField>
    <ResponseField name="entity_name" type="string">
    </ResponseField>
    <ResponseField name="key_property_mappings" type="map(string)">
    </ResponseField>
    <ResponseField name="params" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="incremental_refresh_interval" type="string">
  The refresh interval specifically for incremental data syncs. If unset, incremental syncs will use the default from env, set to 3hrs. The minimum is 30 minutes and maximum is 7 days. Applicable to only 3P connectors. When the refresh interval is set to the same value as the incremental refresh interval, incremental sync will be disabled. Optional.
</ResponseField>

<ResponseField name="incremental_sync_disabled" type="bool">
  Indicates whether incremental syncs are paused for this connector. Optional.
</ResponseField>

<ResponseField name="json_params" type="string">
  Params needed to access the source in the format of json string. Optional.
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  The KMS key to be used to protect the DataStores managed by this connector. Must be set for requests that need to comply with CMEK Org Policy protections. If this field is set and processed successfully, the DataStores created by this connector will be protected by the KMS key. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The geographic location where the data store should reside. The value can only be one of "global", "us" and "eu". Required.
</ResponseField>

<ResponseField name="params" type="map(string)">
  Params needed to access the source in the format of String-to-String (Key, Value) pairs. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="refresh_interval" type="string" required>
  The refresh interval for data sync. If duration is set to 0, the data will be synced in real time. The streaming feature is not supported yet. The minimum is 30 minutes and maximum is 7 days. When the refresh interval is set to the same value as the incremental refresh interval, incremental sync will be disabled. Required.
</ResponseField>

<ResponseField name="static_ip_enabled" type="bool">
  Whether customer has enabled static IP addresses for this connector. Optional.
</ResponseField>

<ResponseField name="sync_mode" type="string">
  The data synchronization mode supported by the data connector. The possible value can be: 'PERIODIC', 'STREAMING'. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="action_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="action_params" type="map(string)">
    </ResponseField>
    <ResponseField name="create_bap_connection" type="bool">
    </ResponseField>
    <ResponseField name="is_action_configured" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="action_state" type="string">
  State of the action connector. This reflects whether the action connector is initializing, active or has encountered errors. The possible value can be: 'STATE_UNSPECIFIED', 'CREATING', 'ACTIVE', 'FAILED', 'RUNNING', 'WARNING', 'INITIALIZATION_FAILED', 'UPDATING'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="bap_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enabled_actions" type="list(string)">
    </ResponseField>
    <ResponseField name="supported_connector_modes" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="blocking_reasons" type="list(string)">
  User actions that must be completed before the connector can start syncing data. The possible values can be: 'ALLOWLIST_STATIC_IP', 'ALLOWLIST_IN_SERVICE_ATTACHMENT'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="connector_type" type="string">
  The type of connector. Each source can only map to one type. For example, salesforce, confluence and jira have THIRD_PARTY connector type. It is not mutable once set by system. The possible value can be: 'CONNECTOR_TYPE_UNSPECIFIED', 'THIRD_PARTY', 'GCP_FHIR', 'BIG_QUERY', 'GCS', 'GOOGLE_MAIL', 'GOOGLE_CALENDAR', 'GOOGLE_DRIVE', 'NATIVE_CLOUD_IDENTITY', 'THIRD_PARTY_FEDERATED', 'THIRD_PARTY_EUA', 'GCNV'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Timestamp when the DataConnector was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="data_source_version" type="number">
  The version of the data source. For example, '3' for Jira v3. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="destination_configs" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="destinations" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="host" type="string">
        </ResponseField>
        <ResponseField name="port" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="key" type="string">
    </ResponseField>
    <ResponseField name="params" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="entities" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="data_store" type="string">
    </ResponseField>
    <ResponseField name="entity_name" type="string">
    </ResponseField>
    <ResponseField name="key_property_mappings" type="map(string)">
    </ResponseField>
    <ResponseField name="params" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="errors" type="list(object)">
  The errors from initialization or from the latest connector run. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
    </ResponseField>
    <ResponseField name="message" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_sync_time" type="string">
  For periodic connectors only, the last time a data sync was completed. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="latest_pause_time" type="string">
  The most recent timestamp when this [DataConnector][] was paused, affecting all functionalities such as data synchronization. Pausing a connector has the following effects: - All functionalities, including data synchronization, are halted. - Any ongoing data synchronization job will be canceled. - No future data synchronization runs will be scheduled nor can be triggered. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The full resource name of the Data Connector. Format: 'projects/*/locations/*/collections/*/dataConnector'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="private_connectivity_project_id" type="string">
  The tenant project ID associated with private connectivity connectors. This project must be allowlisted by in order for the connector to function. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="realtime_state" type="string">
  The real-time sync state. The possible values can be: 'STATE_UNSPECIFIED', 'CREATING', 'ACTIVE', 'FAILED', 'RUNNING', 'WARNING', 'INITIALIZATION_FAILED', 'UPDATING'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of connector. The possible value can be: 'STATE_UNSPECIFIED', 'CREATING', 'ACTIVE', 'FAILED', 'RUNNING', 'WARNING', 'INITIALIZATION_FAILED', 'UPDATING'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="static_ip_addresses" type="list(string)">
  The static IP addresses used by this connector. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Timestamp when the DataConnector was updated. Computed by GCP after `ship`.
</ResponseField>
