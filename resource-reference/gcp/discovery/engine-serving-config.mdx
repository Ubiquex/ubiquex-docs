---
title: "google_discovery_engine_serving_config"
description: "Real, generated bindings for `google_discovery_engine_serving_config`."
---

`google_discovery_engine_serving_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discovery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/discovery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine serving config"})

    		ubx.Resource(discovery.EngineServingConfig, "ci-runner", discovery.EngineServingConfigConfig{
    			EngineId:           "example",
    			Location:           "example",
    			BoostControlIds:    []string{"example"},
    			CollectionId:       "example",
    			FilterControlIds:   []string{"example"},
    			PromoteControlIds:  []string{"example"},
    			RedirectControlIds: []string{"example"},
    			ServingConfigId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineServingConfig } from "jsr:@ubx/sdk-google/google/discovery/engine_serving_config";

    export default stack("payments", () => {
      intent({ summary: "payments own engine serving config" });

      resource(EngineServingConfig, "ci-runner", {
        engineId: "example",
        location: "example",
        boostControlIds: ["example"],
        collectionId: "example",
        filterControlIds: ["example"],
        promoteControlIds: ["example"],
        redirectControlIds: ["example"],
        servingConfigId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.discovery import EngineServingConfig, EngineServingConfigConfig

    def describe():
        ubx.intent("payments own engine serving config")

        ubx.resource(EngineServingConfig, "ci-runner", EngineServingConfigConfig(
            engine_id="example",
            location="example",
            boost_control_ids=["example"],
            collection_id="example",
            filter_control_ids=["example"],
            promote_control_ids=["example"],
            redirect_control_ids=["example"],
            serving_config_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discovery_engine_serving_config, with engine id
    example, location example, boost control ids a set value,
    collection id example, filter control ids a set value, promote
    control ids a set value, redirect control ids a set value,
    serving config id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="boost_control_ids" type="list(string)">
  The resource IDs of the boost controls to be applied. Optional.
</ResponseField>

<ResponseField name="collection_id" type="string">
  The collection ID. Currently only accepts "default_collection". Optional.
</ResponseField>

<ResponseField name="engine_id" type="string" required>
  The ID of the engine associated with the serving config. Required.
</ResponseField>

<ResponseField name="filter_control_ids" type="list(string)">
  The resource IDs of the filter controls to be applied. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The geographic location where the data store should reside. The value can only be one of "global", "us" and "eu". Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="promote_control_ids" type="list(string)">
  The resource IDs of the promote controls to be applied. Optional.
</ResponseField>

<ResponseField name="redirect_control_ids" type="list(string)">
  The resource IDs of the redirect controls to be applied. Optional.
</ResponseField>

<ResponseField name="serving_config_id" type="string">
  'The unique ID of the serving config. Currently only accepts "default_search".' Optional.
</ResponseField>

<ResponseField name="synonyms_control_ids" type="list(string)">
  The resource IDs of the synonyms controls to be applied. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The unique full resource name of the serving config. Values are of the format 'projects/&#123;project&#125;/locations/&#123;location&#125;/collections/&#123;collection_id&#125;/engines/&#123;engine_id&#125;/servingConfigs/&#123;serving_config_id&#125;'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
