---
title: "google_public_ca_external_account_key"
description: "Real, generated bindings for `google_public_ca_external_account_key`."
---

`google_public_ca_external_account_key` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	public "github.com/ubiquex/ubx-sdk-google/sdk/go/google/public"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ca external account key"})

    		ubx.Resource(public.CaExternalAccountKey, "ci-runner", public.CaExternalAccountKeyConfig{
    			Location: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CaExternalAccountKey } from "jsr:@ubx/sdk-google/google/public/ca_external_account_key";

    export default stack("payments", () => {
      intent({ summary: "payments own ca external account key" });

      resource(CaExternalAccountKey, "ci-runner", {
        location: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.public import CaExternalAccountKey, CaExternalAccountKeyConfig

    def describe():
        ubx.intent("payments own ca external account key")

        ubx.resource(CaExternalAccountKey, "ci-runner", CaExternalAccountKeyConfig(
            location="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_public_ca_external_account_key, with location
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="b64_mac_key" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="b64url_mac_key" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="mac_key" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
