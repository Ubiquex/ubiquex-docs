---
title: "google_backup_dr_service_config"
description: "Real, generated bindings for `google_backup_dr_service_config`."
---

`google_backup_dr_service_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/backup"
    )

    cfg := backup.DrServiceConfigConfig{
        Location: "example",
        ResourceType: "example",
    }
    ubx.Resource(backup.DrServiceConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DrServiceConfig } from
      "jsr:@ubx/sdk-google/google/backup/dr_service_config";

    resource(DrServiceConfig, "example", {
      location: "example",
      resourceType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.backup import (
        DrServiceConfig,
        DrServiceConfigConfig,
    )

    cfg = DrServiceConfigConfig(
        location="example",
        resource_type="example",
    )
    ubx.resource(DrServiceConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_backup_dr_service_config with location,
resource_type.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
