---
title: "google_cloudbuildv2_connection_iam_policy"
description: "Real, generated bindings for `google_cloudbuildv2_connection_iam_policy`."
---

`google_cloudbuildv2_connection_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloudbuildv2"
    )

    cfg := cloudbuildv2.ConnectionIamPolicyConfig{
        Name: "example-name",
        PolicyData: "example",
    }
    ubx.Resource(cloudbuildv2.ConnectionIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConnectionIamPolicy } from
      "jsr:@ubx/sdk-google/google/cloudbuildv2/connection_iam_policy";

    resource(ConnectionIamPolicy, "example", {
      name: "example-name",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.cloudbuildv2 import (
        ConnectionIamPolicy,
        ConnectionIamPolicyConfig,
    )

    cfg = ConnectionIamPolicyConfig(
        name="example-name",
        policy_data="example",
    )
    ubx.resource(ConnectionIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_cloudbuildv2_connection_iam_policy with name,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
