---
title: "google_compute_network_attachment"
description: "Real, generated bindings for `google_compute_network_attachment`."
---

`google_compute_network_attachment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network attachment"})

    		ubx.Resource(compute.NetworkAttachment, "ci-runner", compute.NetworkAttachmentConfig{
    			ConnectionPreference: "example",
    			Name:                 "ci-runner",
    			Subnetworks:          []string{"example"},
    			Description:          "Managed by ubx.",
    			ProducerAcceptLists:  []string{"example"},
    			ProducerRejectLists:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkAttachment } from "jsr:@ubx/sdk-google/google/compute/network_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own network attachment" });

      resource(NetworkAttachment, "ci-runner", {
        connectionPreference: "example",
        name: "ci-runner",
        subnetworks: ["example"],
        description: "Managed by ubx.",
        producerAcceptLists: ["example"],
        producerRejectLists: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import NetworkAttachment, NetworkAttachmentConfig

    def describe():
        ubx.intent("payments own network attachment")

        ubx.resource(NetworkAttachment, "ci-runner", NetworkAttachmentConfig(
            connection_preference="example",
            name="ci-runner",
            subnetworks=["example"],
            description="Managed by ubx.",
            producer_accept_lists=["example"],
            producer_reject_lists=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_network_attachment called "ci-runner", with
    connection preference example, subnetworks a set value,
    description Managed by ubx., producer accept lists a set value,
    producer reject lists a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="connection_preference" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="producer_accept_lists" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="producer_reject_lists" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="subnetworks" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="connection_endpoints" type="list(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="ip_address" type="string">
    </ResponseField>
    <ResponseField name="project_id_or_num" type="string">
    </ResponseField>
    <ResponseField name="secondary_ip_cidr_ranges" type="string">
    </ResponseField>
    <ResponseField name="status" type="string">
    </ResponseField>
    <ResponseField name="subnetwork" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="kind" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="network" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
