---
title: "google_compute_network_firewall_policy_iam_member"
description: "Real, generated bindings for `google_compute_network_firewall_policy_iam_member`."
---

`google_compute_network_firewall_policy_iam_member` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network firewall policy iam member"})

    		ubx.Resource(compute.NetworkFirewallPolicyIamMember, "ci-runner", compute.NetworkFirewallPolicyIamMemberConfig{
    			Member: "example",
    			Name:   "ci-runner",
    			Role:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkFirewallPolicyIamMember } from "jsr:@ubx/sdk-google/google/compute/network_firewall_policy_iam_member";

    export default stack("payments", () => {
      intent({ summary: "payments own network firewall policy iam member" });

      resource(NetworkFirewallPolicyIamMember, "ci-runner", {
        member: "example",
        name: "ci-runner",
        role: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import NetworkFirewallPolicyIamMember, NetworkFirewallPolicyIamMemberConfig

    def describe():
        ubx.intent("payments own network firewall policy iam member")

        ubx.resource(NetworkFirewallPolicyIamMember, "ci-runner", NetworkFirewallPolicyIamMemberConfig(
            member="example",
            name="ci-runner",
            role="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_network_firewall_policy_iam_member called
    "ci-runner", with member example, role example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="member" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
