---
title: "google_compute_router"
description: "`google_compute_router` runs real, dynamic BGP routing for a VPC network -- exchanging routes with an on-premises network over Cloud VPN or..."
---

`google_compute_router` runs real, dynamic BGP routing for a VPC network -- exchanging routes with an on-premises network over Cloud VPN or Interconnect rather than relying on static routes alone.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own router"})

    		ubx.Resource(compute.Router, "example", compute.RouterConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Router } from "@ubx/sdk-google/google/compute/router";

    export default stack("example", () => {
      intent({ summary: "example own router" });

      resource(Router, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import Router, RouterConfig

    def describe():
        ubx.intent("example own router")

        ubx.resource(Router, "example", RouterConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_router called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="bgp" type="object">
  BGP configuration for the Cloud Router, including the autonomous system number (ASN), advertised route settings, and other BGP parameters. This field is optional when creating a router and is computed by the provider when not explicitly set. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="advertise_mode" type="string">
      User-specified flag to indicate which mode to use for advertisement. The options are DEFAULT or CUSTOM. Optional.
    </ResponseField>
    <ResponseField name="advertised_groups" type="list(string)">
      User-specified list of prefix groups to advertise in custom mode. This field can only be populated if advertise_mode is CUSTOM and is advertised to all peers of the router. These groups will be advertised in addition to any specified prefixes. Leave this field blank to advertise no custom groups. Optional.
    </ResponseField>
    <ResponseField name="advertised_ip_ranges" type="list(object)">
      User-specified list of individual IP ranges to advertise in custom mode. This field can only be populated if advertise_mode is CUSTOM and is advertised to all peers of the router. These IP ranges will be advertised in addition to any specified groups. Leave this field blank to advertise no custom IP ranges. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="range" type="string">
          The IP range in CIDR notation (e.g., 192.168.0.0/16) that the router advertises to BGP peers. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="asn" type="number">
      Local BGP Autonomous System Number (ASN). Must be anRFC6996 private ASN, either 16-bit or 32-bit. The value will be fixed for this router resource. All VPN tunnels that link to this router will have the same local ASN. Optional.
    </ResponseField>
    <ResponseField name="identifier_range" type="string">
      Explicitly specifies a range of valid BGP Identifiers for this Router. It is provided as a link-local IPv4 range (from 169.254.0.0/16), of size at least /30, even if the BGP sessions are over IPv6. It must not overlap with any IPv4 BGP session ranges. Other vendors commonly call this "router ID". Optional.
    </ResponseField>
    <ResponseField name="keepalive_interval" type="number">
      The interval in seconds between BGP keepalive messages that are sent to the peer. Hold time is three times the interval at which keepalive messages are sent, and the hold time is the maximum number of seconds allowed to elapse between successive keepalive messages that BGP receives from a peer. BGP will use the smaller of either the local hold time value or the peer's hold time value as the hold time for the BGP connection between the two peers. If set, this value must be between 20 and 60. The default is 20. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bgp_peers" type="list(object)">
  BGP information that must be configured into the routing stack to establish BGP peering. This information must specify the peer ASN and either the interface name, IP address, or peer IP address. Please refer toRFC4273. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="advertise_mode" type="string">
      The route advertisement mode for the BGP peer. Use DEFAULT to advertise the provider's default route set, or CUSTOM to define specific routes via the advertised_ip_ranges field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="advertised_groups" type="list(string)">
      Specifies the list of prefix groups to advertise in custom mode. Valid values are 'ALL_SUBNETS', 'ALL_VPC_SUBNETS', and 'ALL_PEER_SUBNETS'. This field can only be populated when advertise_mode is 'CUSTOM'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="advertised_ip_ranges" type="list(object)">
      A list of custom IP ranges to advertise to the BGP peer. Each object contains a CIDR 'range' (required) and an optional 'description'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          An optional, user-defined description for the advertised IP range. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="range" type="string">
          The IP range to advertise, specified in CIDR notation (e.g., '192.168.0.0/16'). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="advertised_route_priority" type="number">
      The priority of routes advertised to this BGP peer. In the case of multiple BGP peers advertising the same route, the route with the lowest priority is preferred. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="bfd" type="object">
      Configures Bidirectional Forwarding Detection (BFD) settings for this BGP peer. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="min_receive_interval" type="number">
          The minimum interval, in milliseconds, between received BFD control packets for this BGP peer. This setting controls how quickly the router detects a peer failure. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="min_transmit_interval" type="number">
          The minimum interval (in milliseconds) between BFD transmissions for this BGP peer. The value must be between 100 and 30000 milliseconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="multiplier" type="number">
          The number of consecutive BFD control packets that must be missed before BFD declares the peer down. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="session_initialization_mode" type="string">
          The BFD session initialization mode. Valid values are ACTIVE, DISABLED, and PASSIVE. ACTIVE initiates the BFD session, PASSIVE waits for the peer to initiate, and DISABLED disables BFD. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_learned_ip_ranges" type="list(object)">
      A list of custom learned route IP address ranges for this BGP peer. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="range" type="string">
          The IP range in CIDR notation to use as a custom learned IP range for the BGP peer. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_learned_route_priority" type="number">
      User-specified priority for routes learned from this BGP peer. Must be between 0 and 65535 inclusive, defaults to 100. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable" type="string">
      Indicates whether the BGP peer is enabled. The allowed values are TRUE and FALSE. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_ipv4" type="bool">
      Enables IPv4 on the BGP peer. This flag is set to true by default. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_ipv6" type="bool">
      Enables IPv6 for the BGP peer. Set to true to allow the BGP peer to exchange IPv6 routes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="export_policies" type="list(string)">
      List of names of Cloud Router route policies to apply to outgoing routes advertised to this BGP peer. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="import_policies" type="list(string)">
      A list of Cloud Router route policy names that this BGP peer applies to incoming routes. These policies control which routes are accepted from the BGP peer. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="interface_name" type="string">
      The name of the router interface the BGP peer is associated with. This must match the name of an existing interface on the same Compute Router. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ip_address" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="ipv4_nexthop_address" type="string">
      The IPv4 address that this BGP peer uses as the next hop for advertised routes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ipv6_nexthop_address" type="string">
      The IPv6 address used as the next-hop for routes advertised to this BGP peer. If not specified, the default IPv6 next-hop (typically the interface's address) is used. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="management_type" type="string">
      The management type of the BGP peer, which indicates whether the peer is managed by the associated attachment or by the user. Valid values are 'MANAGED_BY_ATTACHMENT' and 'MANAGED_BY_USER'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="md5_authentication_key_name" type="string">
      The name of the MD5 authentication key to use for this BGP peer. The key must be defined in the router's BGP configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="peer_asn" type="number">
      The autonomous system number (ASN) of the BGP peer. Required when configuring a BGP peer on a Compute Router. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="peer_ip_address" type="string">
      The IP address of the BGP peer that this router will establish a BGP session with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="peer_ipv4_nexthop_address" type="string">
      The IPv4 address of the BGP peer, used as the next hop for routes learned from this peer. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="peer_ipv6_nexthop_address" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="router_appliance_instance" type="string">
      The URI of the VM instance that is the router appliance for this BGP peer. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="encrypted_interconnect_router" type="bool">
  Indicates if a router is dedicated for use with encrypted VLAN attachments (interconnectAttachments). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="interfaces" type="list(object)">
  Router interfaces. To create a BGP peer that uses a router interface, the interface must have one of the following fields specified: - linkedVpnTunnel - linkedInterconnectAttachment - subnetwork You can create a router interface without any of these fields specified. However, you cannot create a BGP peer that uses that interface. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="ip_range" type="string">
      The IP address and range for the router interface, used for BGP sessions. This can be a single IP address or a CIDR range in the form of 'IP/30'. If not specified, GCP will assign an IP address automatically. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ip_version" type="string">
      The IP version used by this interface. Valid values are IPV4 and IPV6. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="linked_interconnect_attachment" type="string">
      The URL of the interconnect attachment to link with this router interface. This is used when the interface is for a VLAN attachment on an Interconnect. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="linked_vpn_tunnel" type="string">
      The name of the VPN tunnel that this interface is linked to. Use this when the interface is of type 'tunnel'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="management_type" type="string">
      Indicates who manages this interface: MANAGED_BY_ATTACHMENT means the interface is managed by a VLAN attachment (Cloud Interconnect), while MANAGED_BY_USER means it is configured by the user. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="private_ip_address" type="string">
      The private IP address of the Compute Router interface. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="redundant_interface" type="string">
      The name of another interface in the same router that serves as the redundancy pair for this interface. When set, this interface is configured as part of a redundant BGP session. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnetwork" type="string">
      The name or self-link of the subnetwork to which this router interface is attached. This field is used when the interface is associated with a subnetwork, such as for router appliance interfaces. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of resource. Always compute#router for routers. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="md5_authentication_keys" type="list(object)">
  Keys used for MD5 authentication. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The shared secret key used for MD5 authentication in the BGP session. This value is provided to the peer to authenticate the router. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the MD5 authentication key. It uniquely identifies the key within the router's BGP configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="nats" type="list(object)">
  A list of NAT services created in this router. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="auto_network_tier" type="string">
      Specifies the network tier for the NAT. Allowed values are FIXED_STANDARD, PREMIUM, STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="drain_nat_ips" type="list(string)">
      A list of external IP addresses to be drained from the NAT. These IPs will no longer be used for new connections but will continue to serve existing connections until they are closed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="effective_tcp_time_wait_timeout_sec" type="number">
      The effective TCP TIME_WAIT timeout in seconds for Cloud NAT connections, reflecting the actual value in use (either the configured value or the default). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_dynamic_port_allocation" type="bool">
      If set to true, the NAT gateway dynamically allocates ports for each VM from a shared pool instead of using static port allocation. Defaults to false. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_endpoint_independent_mapping" type="bool">
      Whether to enable endpoint independent mapping for the NAT gateway. When enabled, the NAT gateway maps each internal IP and port pair to a unique external IP and port, independent of the destination. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="endpoint_types" type="list(string)">
      The list of endpoint types supported by this NAT configuration, controlling which types of network endpoints can use the NAT. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="icmp_idle_timeout_sec" type="number">
      The idle timeout for ICMP connections in seconds. This controls how long an ICMP session is kept alive in the NAT without traffic before being closed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="log_config" type="object">
      Configuration for NAT logging. This block allows enabling or disabling logging for the NAT gateway and specifying a filter to determine which connections are logged. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enable" type="bool">
          Whether to enable logging for the NAT gateway. When set to true, logs are generated according to the filter specified in the log_config block. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="filter" type="string">
          Determines which NAT logs are exported. Valid values: ALL (log all NAT traffic), ERRORS_ONLY (log only failed translations), TRANSLATIONS_ONLY (log successful translations only). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_ports_per_vm" type="number">
      Maximum number of ports per VM used by the NAT configuration, per subnet. Defaults to 2048, with an allowed range of 64 to 65536. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="min_ports_per_vm" type="number">
      The minimum number of ports allocated to each VM in the NAT configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the Cloud NAT. This name must be unique within the parent router. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="nat64_subnetworks" type="list(object)">
      A list of subnetworks (self-links) to be used for NAT64. NAT64 enables IPv6-only resources to communicate with IPv4-only services by mapping IPv6 addresses to IPv4 addresses. Each listed subnetwork must be configured with an IPv6 address range. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the subnetwork to use for NAT64. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="nat_ip_allocate_option" type="string">
      Specifies how NAT IP addresses are allocated. AUTO_ONLY automatically allocates NAT IPs; MANUAL_ONLY requires you to specify them in nat_ips. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="nat_ips" type="list(string)">
      List of self-links of external IP addresses to be used for NAT. Only valid when nat_ip_allocate_option is set to MANUAL_ONLY; if empty, an automatically allocated IP is used. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rules" type="list(object)">
      A list of custom NAT rules. Each rule specifies a rule number, a match expression, and an action (e.g., translation or no translation) to control how traffic is routed through the NAT. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="action" type="object">
          The action configuration for this NAT rule, defining how traffic matching the rule's match condition is NATed. It specifies the source NAT IPs (active and drain) to use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="source_nat_active_ips" type="list(string)">
              The list of IP addresses to use as source NAT IPs for active connections when this NAT rule is applied. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_nat_active_ranges" type="list(string)">
              The list of source NAT IP ranges (in CIDR format) to use as the active NAT addresses for packets matching this rule. Required for the action block of a NAT rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_nat_drain_ips" type="list(string)">
              List of source NAT IP addresses to be drained. These IPs will not be used for new connections, but existing connections will continue to use them until they are removed or connections terminate. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_nat_drain_ranges" type="list(string)">
              A list of source NAT IP ranges to be drained. These ranges are used to gracefully terminate existing connections during a migration or deactivation of NAT IPs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          A user-defined, human-readable description for the NAT rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="match" type="string">
          An expression in Common Expression Language (CEL) that defines which traffic this NAT rule applies to. It can match on packet attributes like source or destination IP, protocol, and ports, e.g., `inIpRange(destination.ip, '203.0.113.0/24')`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rule_number" type="number">
          The unique number assigned to this NAT rule. Rules are evaluated in ascending order, and this number must be unique within the NAT configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source_subnetwork_ip_ranges_to_nat" type="string">
      Specifies which IP ranges of the subnets are eligible for NAT. Valid values are: ALL_SUBNETWORKS_ALL_IP_RANGES, ALL_SUBNETWORKS_ALL_PRIMARY_IP_RANGES, and LIST_OF_SUBNETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_subnetwork_ip_ranges_to_nat64" type="string">
      Controls which IPv6 subnetwork IP ranges are eligible for NAT64. Set to ALL_IPV6_SUBNETWORKS to use all IPv6 subnets in the region, or LIST_OF_IPV6_SUBNETWORKS to use only the specific subnets listed in the associated subnetwork configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnetworks" type="list(object)">
      The list of subnetworks that this NAT configuration applies to. Each object specifies a subnetwork and its NAT settings. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the subnetwork to include in the NAT configuration for this router NAT. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="secondary_ip_range_names" type="list(string)">
          List of the secondary ranges of the subnetwork that are allowed to use NAT. This can be provided only when 'LIST_OF_SECONDARY_IP_RANGES' is used for source_ip_ranges_to_nat. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="source_ip_ranges_to_nat" type="list(string)">
          List of source IP ranges in the subnetwork that should be NATed. Valid values are 'ALL_IP_RANGES', 'PRIMARY_IP_RANGE', and 'LIST_OF_SECONDARY_IP_RANGES'. If 'LIST_OF_SECONDARY_IP_RANGES' is used, you must also specify `secondary_ip_range_names`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tcp_established_idle_timeout_sec" type="number">
      Timeout in seconds for established TCP connections before they are considered idle and closed. Defaults to 1200 seconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tcp_time_wait_timeout_sec" type="number">
      Timeout for TCP connections in the TIME_WAIT state, specified in seconds, within the router's NAT configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tcp_transitory_idle_timeout_sec" type="number">
      The timeout in seconds for TCP transitory idle connections. A transitory connection is one that is in the process of being established (e.g., during the TCP handshake); if no traffic is seen within this timeout, the connection is dropped. Defaults to 30 seconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of NAT configuration. Valid values are `PRIVATE` and `PUBLIC`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="udp_idle_timeout_sec" type="number">
      The timeout (in seconds) for UDP connections through the NAT. Defaults to 30 seconds if not specified. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ncc_gateway" type="string">
  URI of the ncc_gateway to which this router associated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="network" type="string">
  URI of the network to which this router belongs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="params" type="object">
  Additional router parameters. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
      Tag keys/values directly bound to this resource. The field is allowed for INSERT only. The keys/values to set on the resource should be specified in either ID &#123; : &#125; or Namespaced format &#123; : &#125;. For example the following are valid inputs: * &#123;"tagKeys/333" : "tagValues/444", "tagKeys/123" : "tagValues/456"&#125; * &#123;"123/environment" : "production", "345/abc" : "xyz"&#125; Note: * Invalid combinations of ID & namespaced format is not supported. For instance: &#123;"123/environment" : "tagValues/444"&#125; is invalid. * Inconsistent format is not supported. For instance: &#123;"tagKeys/333" : "tagValues/444", "123/env" : "prod"&#125; is invalid. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  [Output Only] URI of the region where the router resides. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>
