---
title: "google_compute_target_pool"
description: "Real, generated bindings for `google_compute_target_pool`."
---

`google_compute_target_pool` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own target pool"})

    		ubx.Resource(compute.TargetPool, "ci-runner", compute.TargetPoolConfig{
    			Name:            "ci-runner",
    			BackupPool:      "example",
    			Description:     "Managed by ubx.",
    			FailoverRatio:   1,
    			HealthChecks:    []string{"example"},
    			SessionAffinity: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TargetPool } from "jsr:@ubx/sdk-google/google/compute/target_pool";

    export default stack("payments", () => {
      intent({ summary: "payments own target pool" });

      resource(TargetPool, "ci-runner", {
        name: "ci-runner",
        backupPool: "example",
        description: "Managed by ubx.",
        failoverRatio: 1,
        healthChecks: ["example"],
        sessionAffinity: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import TargetPool, TargetPoolConfig

    def describe():
        ubx.intent("payments own target pool")

        ubx.resource(TargetPool, "ci-runner", TargetPoolConfig(
            name="ci-runner",
            backup_pool="example",
            description="Managed by ubx.",
            failover_ratio=1,
            health_checks=["example"],
            session_affinity="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_target_pool called "ci-runner", with backup
    pool example, description Managed by ubx., failover ratio 1,
    health checks a set value, session affinity example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backup_pool" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="failover_ratio" type="number">
  Optional.
</ResponseField>

<ResponseField name="health_checks" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instances" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="session_affinity" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instances" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
