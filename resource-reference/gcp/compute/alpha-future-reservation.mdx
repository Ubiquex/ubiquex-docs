---
title: "google_compute_alpha_future_reservation"
description: "`google_compute_alpha_future_reservation` requests real compute capacity -- a specific VM size, GPU..."
---

`google_compute_alpha_future_reservation` requests real compute capacity -- a specific VM size, GPU, or TPU -- to become available starting on a date up to a year out, so high-demand hardware is guaranteed to actually be there when a real, planned workload needs it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own alpha future reservation"})

    		ubx.Resource(compute.AlphaFutureReservation, "example", compute.AlphaFutureReservationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AlphaFutureReservation } from "@ubx/sdk-google/google/compute/alpha_future_reservation";

    export default stack("example", () => {
      intent({ summary: "example own alpha future reservation" });

      resource(AlphaFutureReservation, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import AlphaFutureReservation, AlphaFutureReservationConfig

    def describe():
        ubx.intent("example own alpha future reservation")

        ubx.resource(AlphaFutureReservation, "example", AlphaFutureReservationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_alpha_future_reservation called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="advanced_deployment_control" type="object">
  Advance control for cluster management, applicable only to DENSE deployment type reservations. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="reservation_operational_mode" type="string">
      Indicates chosen reservation operational mode for the reservation. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="aggregate_reservation" type="object">
  This reservation type is specified by total resource amounts (e.g. total count of CPUs) and can account for multiple instance SKUs. In other words, one can create instances of varying shapes against this reservation. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="host_count" type="number">
      Count of reserved hosts of specified VM family. The host has fixed number of accelerators based on the accelerator/vm-family selected. Optional.
    </ResponseField>
    <ResponseField name="in_use_host_count" type="number">
      Output only. Number of hosts currently in use. If there is one or more Instances running on the host, it is considered in use. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="in_use_instance_count" type="number">
      Output only. Number of instances currently in use in this reservation. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="in_use_resources" type="list(object)">
      Output only. [Output only] List of resources currently in use. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="accelerator" type="object">
          Optional; if omitted, computed by Google Cloud.
          <Expandable title="properties">
            <ResponseField name="accelerator_count" type="number">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="accelerator_type" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reserved_resources" type="list(object)">
      List of reserved resources (CPUs, memory, accelerators). Optional.
      <Expandable title="properties">
        <ResponseField name="accelerator" type="object">
          Optional; if omitted, computed by Google Cloud.
          <Expandable title="properties">
            <ResponseField name="accelerator_count" type="number">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="accelerator_type" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vm_family" type="string">
      The VM family that all instances scheduled against this reservation must belong to. Optional.
    </ResponseField>
    <ResponseField name="workload_type" type="string">
      The workload type of the instances that will target this reservation. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="auto_created_reservations_delete_time" type="string">
  Future timestamp when the FR auto-created reservations will be deleted by Compute Engine. Format of this field must be a valid href="https://www.ietf.org/rfc/rfc3339.txt"&gt;RFC3339 value. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="auto_created_reservations_duration" type="object">
  A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="nanos" type="number">
      Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
    </ResponseField>
    <ResponseField name="seconds" type="string">
      Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="auto_delete_auto_created_reservations" type="bool">
  Setting for enabling or disabling automatic deletion for auto-created reservation. If set to true, auto-created reservations will be deleted at Future Reservation's end time (default) or at user's defined timestamp if any of the [auto_created_reservations_delete_time, auto_created_reservations_duration] values is specified. For keeping auto-created reservation indefinitely, this value should be set to false. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="colocation_resource" type="string">
  Full or partial URL of an existing future reservation to indicate intent for reserving capacity in the same cluster as the colocation resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="commitment_info" type="object">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="commitment_name" type="string">
      name of the commitment where capacity is being delivered to. Optional.
    </ResponseField>
    <ResponseField name="commitment_plan" type="string">
      Indicates if a Commitment needs to be created as part of FR delivery. If this field is not present, then no commitment needs to be created. Optional.
    </ResponseField>
    <ResponseField name="previous_commitment_terms" type="string">
      Only applicable if FR is delivering to the same reservation. If set, all parent commitments will be extended to match the end date of the plan for this commitment. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="confidential_compute_type" type="string">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] The creation timestamp for this future reservation inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="deployment_type" type="string">
  Type of the deployment requested as part of future reservation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the future reservation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="enable_emergent_maintenance" type="bool">
  Indicates if this group of VMs have emergent maintenance enabled. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] A unique identifier for this future reservation. The server defines this identifier. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#futureReservation for future reservations. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the resource, provided by the client when initially creating the resource. The resource name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Name prefix for the reservations to be created at the time of delivery. The name prefix must comply with RFC1035. Maximum allowed length for name prefix is 20. Automatically created reservations name format will be -date-####. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="params" type="object">
  Additional future reservation params. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the future reservation. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="planning_status" type="string">
  Planning state before being submitted for evaluation Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="protection_tier" type="string">
  Protection tier for the workload. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="reservation_mode" type="string">
  The reservation mode which determines reservation-termination behavior and expected pricing. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="reservation_name" type="string">
  Name of reservations where the capacity is provisioned at the time of delivery of future reservations. If the reservation with the given name does not exist already, it is created automatically at the time of Approval with INACTIVE state till specified start-time. Either provide the reservation_name or a name_prefix. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="resource_name" type="string">
  Name of the resource intended to be delivered. Name should conform to RFC1035. This will be the name of storage pool or Exapool for persistent disk FRs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="scheduling_type" type="string">
  Maintenance information for this reservation Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined fully-qualified URL for this resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource with the resource id. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="share_settings" type="object">
  The share setting for reservations and sole tenancy node groups. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="folder_map" type="map(object)">
      A map of folder id and folder config to specify consumer projects for this shared-reservation. This is only valid when share_type's value is DIRECT_PROJECTS_UNDER_SPECIFIC_FOLDERS. Folder id should be a string of number, and without "folders/" prefix. Optional.
      <Expandable title="properties">
        <ResponseField name="folder_id" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="project_map" type="map(object)">
      A map of project id and project config. This is only valid when share_type's value is SPECIFIC_PROJECTS. Optional.
      <Expandable title="properties">
        <ResponseField name="project_id" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="projects" type="list(string)">
      A List of Project names to specify consumer projects for this shared-reservation. This is only valid when share_type's value is SPECIFIC_PROJECTS. Optional.
    </ResponseField>
    <ResponseField name="share_type" type="string">
      Type of sharing for this shared-reservation Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="specific_reservation_required" type="bool">
  Indicates whether the auto-created reservation can be consumed by VMs with affinity for "any" reservation. If the field is set, then only VMs that target the reservation by name can consume from the delivered reservation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="specific_sku_properties" type="object">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="instance_properties" type="object">
      Properties of the SKU instances being reserved. Next ID: 10 Optional.
      <Expandable title="properties">
        <ResponseField name="guest_accelerators" type="list(object)">
          Specifies accelerator type and count. Optional.
          <Expandable title="properties">
            <ResponseField name="accelerator_count" type="number">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="accelerator_type" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="local_ssds" type="list(object)">
          Specifies amount of local ssd to reserve with each instance. The type of disk is local-ssd. Optional.
          <Expandable title="properties">
            <ResponseField name="disk_size_gb" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="interface" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="location_hint" type="string">
          An opaque location hint used to place the allocation close to other resources. This field is for use by internal tools that use the public API. Optional.
        </ResponseField>
        <ResponseField name="machine_type" type="string">
          Specifies type of machine (name only) which has fixed number of vCPUs and fixed amount of memory. This also includes specifying custom machine type following custom-NUMBER_OF_CPUS-AMOUNT_OF_MEMORY pattern. Optional.
        </ResponseField>
        <ResponseField name="maintenance_freeze_duration_hours" type="number">
          Specifies the number of hours after reservation creation where instances using the reservation won't be scheduled for maintenance. Optional.
        </ResponseField>
        <ResponseField name="maintenance_interval" type="string">
          Specifies the frequency of planned maintenance events. The accepted values are: `PERIODIC`. Optional.
        </ResponseField>
        <ResponseField name="min_cpu_platform" type="string">
          Minimum cpu platform the reservation. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source_instance_template" type="string">
      The instance template that will be used to populate the ReservedInstanceProperties of the future reservation Optional.
    </ResponseField>
    <ResponseField name="total_count" type="string">
      Total number of instances for which capacity assurance is requested at a future time period. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="object">
  [Output only] Represents status related to the future reservation. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="amendment_status" type="string">
      Output only. [Output Only] The current status of the requested amendment. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="auto_created_reservations" type="list(string)">
      Output only. Fully qualified urls of the automatically created reservations at start_time. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="exapool_provisioned_capacity_gb" type="object">
      Exapool provisioned capacities for each SKU type Optional.
      <Expandable title="properties">
        <ResponseField name="capacity_optimized" type="string">
          Size, in GiB, of provisioned capacity-optimized capacity for this Exapool Optional.
        </ResponseField>
        <ResponseField name="read_optimized" type="string">
          Size, in GiB, of provisioned read-optimized capacity for this Exapool Optional.
        </ResponseField>
        <ResponseField name="write_optimized" type="string">
          Size, in GiB, of provisioned write-optimized capacity for this Exapool Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="existing_matching_usage_info" type="object">
      [Output Only] Represents the existing matching usage for the future reservation. Optional.
      <Expandable title="properties">
        <ResponseField name="count" type="string">
          Output only. Count to represent min(FR total_count, matching_reserved_capacity+matching_unreserved_instances) Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="timestamp" type="string">
          Output only. Timestamp when the matching usage was calculated Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fulfilled_count" type="string">
      Output only. This count indicates the fulfilled capacity so far. This is set during "PROVISIONING" state. This count also includes capacity delivered as part of existing matching reservations. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="last_known_good_state" type="object">
      The state that the future reservation will be reverted to should the amendment be declined. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Output only. [Output Only] The description of the FutureReservation before an amendment was requested. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="existing_matching_usage_info" type="object">
          [Output Only] Represents the existing matching usage for the future reservation. Optional.
          <Expandable title="properties">
            <ResponseField name="count" type="string">
              Output only. Count to represent min(FR total_count, matching_reserved_capacity+matching_unreserved_instances) Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="timestamp" type="string">
              Output only. Timestamp when the matching usage was calculated Computed by Google Cloud after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="future_reservation_specs" type="object">
          The properties of the last known good state for the Future Reservation. Optional.
          <Expandable title="properties">
            <ResponseField name="share_settings" type="object">
              The share setting for reservations and sole tenancy node groups. Optional.
              <Expandable title="properties">
                <ResponseField name="folder_map" type="map(object)">
                  A map of folder id and folder config to specify consumer projects for this shared-reservation. This is only valid when share_type's value is DIRECT_PROJECTS_UNDER_SPECIFIC_FOLDERS. Folder id should be a string of number, and without "folders/" prefix. Optional.
                  <Expandable title="properties">
                    <ResponseField name="folder_id" type="string">
                      Optional; if omitted, computed by Google Cloud.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="project_map" type="map(object)">
                  A map of project id and project config. This is only valid when share_type's value is SPECIFIC_PROJECTS. Optional.
                  <Expandable title="properties">
                    <ResponseField name="project_id" type="string">
                      Optional; if omitted, computed by Google Cloud.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="projects" type="list(string)">
                  A List of Project names to specify consumer projects for this shared-reservation. This is only valid when share_type's value is SPECIFIC_PROJECTS. Optional.
                </ResponseField>
                <ResponseField name="share_type" type="string">
                  Type of sharing for this shared-reservation Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="specific_sku_properties" type="object">
              Optional.
              <Expandable title="properties">
                <ResponseField name="instance_properties" type="object">
                  Properties of the SKU instances being reserved. Next ID: 10 Optional.
                  <Expandable title="properties">
                    <ResponseField name="guest_accelerators" type="list(object)">
                      Specifies accelerator type and count. Optional.
                      <Expandable title="properties">
                        <ResponseField name="accelerator_count" type="number">
                          Optional; if omitted, computed by Google Cloud.
                        </ResponseField>
                        <ResponseField name="accelerator_type" type="string">
                          Optional; if omitted, computed by Google Cloud.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="local_ssds" type="list(object)">
                      Specifies amount of local ssd to reserve with each instance. The type of disk is local-ssd. Optional.
                      <Expandable title="properties">
                        <ResponseField name="disk_size_gb" type="string">
                          Optional; if omitted, computed by Google Cloud.
                        </ResponseField>
                        <ResponseField name="interface" type="string">
                          Optional; if omitted, computed by Google Cloud.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="location_hint" type="string">
                      An opaque location hint used to place the allocation close to other resources. This field is for use by internal tools that use the public API. Optional.
                    </ResponseField>
                    <ResponseField name="machine_type" type="string">
                      Specifies type of machine (name only) which has fixed number of vCPUs and fixed amount of memory. This also includes specifying custom machine type following custom-NUMBER_OF_CPUS-AMOUNT_OF_MEMORY pattern. Optional.
                    </ResponseField>
                    <ResponseField name="maintenance_freeze_duration_hours" type="number">
                      Specifies the number of hours after reservation creation where instances using the reservation won't be scheduled for maintenance. Optional.
                    </ResponseField>
                    <ResponseField name="maintenance_interval" type="string">
                      Specifies the frequency of planned maintenance events. The accepted values are: `PERIODIC`. Optional.
                    </ResponseField>
                    <ResponseField name="min_cpu_platform" type="string">
                      Minimum cpu platform the reservation. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="source_instance_template" type="string">
                  The instance template that will be used to populate the ReservedInstanceProperties of the future reservation Optional.
                </ResponseField>
                <ResponseField name="total_count" type="string">
                  Total number of instances for which capacity assurance is requested at a future time period. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="time_window" type="object">
              Optional.
              <Expandable title="properties">
                <ResponseField name="duration" type="object">
                  A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="end_time" type="string">
                  Optional.
                </ResponseField>
                <ResponseField name="start_time" type="string">
                  Start time of the Future Reservation. The start_time is an RFC3339 string. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="lock_time" type="string">
          Output only. [Output Only] The lock time of the FutureReservation before an amendment was requested. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="name_prefix" type="string">
          Output only. [Output Only] The name prefix of the Future Reservation before an amendment was requested. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="procurement_status" type="string">
          Output only. [Output Only] The status of the last known good state for the Future Reservation. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lock_time" type="string">
      Output only. Time when Future Reservation would become LOCKED, after which no modifications to Future Reservation will be allowed. Applicable only after the Future Reservation is in the APPROVED state. The lock_time is an RFC3339 string. The procurement_status will transition to PROCURING state at this time. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="procurement_status" type="string">
      Output only. Current state of this Future Reservation Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="specific_sku_properties" type="object">
      Properties to be set for the Future Reservation. Optional.
      <Expandable title="properties">
        <ResponseField name="source_instance_template_id" type="string">
          ID of the instance template used to populate the Future Reservation properties. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="storage_pool_provisioned_capacity" type="object">
      Storage pool provisioned capacities for each SKU type. Optional.
      <Expandable title="properties">
        <ResponseField name="pool_provisioned_capacity_gb" type="string">
          Size of the storage pool in GiB. Optional.
        </ResponseField>
        <ResponseField name="pool_provisioned_iops" type="string">
          Provisioned IOPS of the storage pool. Only relevant if the storage pool type is hyperdisk-balanced. Optional.
        </ResponseField>
        <ResponseField name="pool_provisioned_throughput" type="string">
          Provisioned throughput of the storage pool in MiB/s. Only relevant if the storage pool type is hyperdisk-balanced or hyperdisk-throughput. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="storage_pool_properties" type="object">
  Storage pool properties for the future reservation. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="requested_exapool_provisioned_capacity_gb" type="object">
      Exapool provisioned capacities for each SKU type Optional.
      <Expandable title="properties">
        <ResponseField name="capacity_optimized" type="string">
          Size, in GiB, of provisioned capacity-optimized capacity for this Exapool Optional.
        </ResponseField>
        <ResponseField name="read_optimized" type="string">
          Size, in GiB, of provisioned read-optimized capacity for this Exapool Optional.
        </ResponseField>
        <ResponseField name="write_optimized" type="string">
          Size, in GiB, of provisioned write-optimized capacity for this Exapool Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="requested_storage_pool_provisioned_capacity" type="object">
      Storage pool provisioned capacities for each SKU type. Optional.
      <Expandable title="properties">
        <ResponseField name="pool_provisioned_capacity_gb" type="string">
          Size of the storage pool in GiB. Optional.
        </ResponseField>
        <ResponseField name="pool_provisioned_iops" type="string">
          Provisioned IOPS of the storage pool. Only relevant if the storage pool type is hyperdisk-balanced. Optional.
        </ResponseField>
        <ResponseField name="pool_provisioned_throughput" type="string">
          Provisioned throughput of the storage pool in MiB/s. Only relevant if the storage pool type is hyperdisk-balanced or hyperdisk-throughput. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="storage_pool_type" type="string">
      Type of the storage pool. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="time_window" type="object">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="duration" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="end_time" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Start time of the Future Reservation. The start_time is an RFC3339 string. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  Output only. [Output Only] URL of the Zone where this future reservation resides. Optional; if omitted, computed by Google Cloud.
</ResponseField>
