---
title: "google_compute_organization_security_policy"
description: "`google_compute_organization_security_policy` is a real, hierarchical firewall policy enforceable across an entire organization or folder -- rules set..."
---

`google_compute_organization_security_policy` is a real, hierarchical firewall policy enforceable across an entire organization or folder -- rules set here can't be overridden by a lower-level policy, giving organization-wide admins one real place to manage critical, non-negotiable firewall rules.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own organization security policy"})

    		ubx.Resource(compute.OrganizationSecurityPolicy, "example", compute.OrganizationSecurityPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationSecurityPolicy } from "@ubx/sdk-google/google/compute/organization_security_policy";

    export default stack("example", () => {
      intent({ summary: "example own organization security policy" });

      resource(OrganizationSecurityPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import OrganizationSecurityPolicy, OrganizationSecurityPolicyConfig

    def describe():
        ubx.intent("example own organization security policy")

        ubx.resource(OrganizationSecurityPolicy, "example", OrganizationSecurityPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_organization_security_policy called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="adaptive_protection_config" type="object">
  Configuration options for Cloud Armor Adaptive Protection (CAAP). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="layer7_ddos_defense_config" type="object">
      Configuration options for L7 DDoS detection. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
      <Expandable title="properties">
        <ResponseField name="enable" type="bool">
          If set to true, enables CAAP for L7 DDoS detection. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
        </ResponseField>
        <ResponseField name="rule_visibility" type="string">
          Rule visibility can be one of the following: STANDARD - opaque rules. (default) PREMIUM - transparent rules. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
        </ResponseField>
        <ResponseField name="threshold_configs" type="list(object)">
          Configuration options for layer7 adaptive protection for various customizable thresholds. Optional.
          <Expandable title="properties">
            <ResponseField name="auto_deploy_confidence_threshold" type="number">
              Specifies the confidence threshold (between 0.0 and 1.0) used to decide when Adaptive Protection automatically deploys a mitigation action against a potential DDoS attack. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="auto_deploy_expiration_sec" type="number">
              The length of time in seconds that an automatically deployed adaptive protection mitigation stays active before expiring. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="auto_deploy_impacted_baseline_threshold" type="number">
              The threshold in requests per second that triggers auto-deploy when the baseline traffic is impacted. This is part of the Layer 7 DDoS defense adaptive protection configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="auto_deploy_load_threshold" type="number">
              The load threshold in requests per second that triggers automatic deployment of mitigation actions. If traffic load exceeds this value, the adaptive protection system will automatically deploy protections. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="detection_absolute_qps" type="number">
              Absolute requests-per-second (QPS) threshold that triggers detection of a DDoS attack in the adaptive protection configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="detection_load_threshold" type="number">
              The detection load threshold in requests per second. When the traffic load exceeds this value for the associated traffic type, adaptive protection starts detecting potential DDoS attacks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="detection_relative_to_baseline_qps" type="number">
              Detection threshold in queries per second (QPS), expressed as a multiple of the baseline QPS. For example, a value of 1.5 means the detection threshold is 1.5 times the baseline QPS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="traffic_granularity_configs" type="list(object)">
              A list of traffic granularity configurations for a threshold. Each configuration specifies a granularity type (e.g., SOURCE_IP or COOKIE) and the corresponding value. If not set, traffic granularity defaults to SOURCE_IP. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="enable_each_unique_value" type="bool">
                  Whether to evaluate traffic thresholds separately for each unique value of the configured traffic granularity type. When enabled, each unique value is tracked individually; when disabled, traffic is aggregated. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the granularity type for traffic aggregation in the Layer 7 DDoS defense threshold configuration. Allowed values are HTTP_HEADER_HOST (aggregate by host header), HTTP_PATH (aggregate by request path), and UNSPECIFIED_TYPE (unspecified). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The value for the traffic granularity configuration, such as a specific IP address or user identifier that the threshold applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="advanced_options_config" type="object">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="json_custom_config" type="object">
      Custom configuration for JSON parsing in the security policy, including the list of content types to be parsed. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="content_types" type="list(string)">
          A list of custom Content-Type header values to apply the JSON parsing. As per RFC 1341, a Content-Type header value has the following format: Content-Type := type "/" subtype *[";" parameter] When configuring a custom Content-Type header value, only the type/subtype needs to be specified, and the parameters should be excluded. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="json_parsing" type="string">
      Configures JSON payload parsing for the security policy. Allowed values: DISABLED (no JSON parsing), STANDARD (standard JSON parsing), and STANDARD_WITH_GRAPHQL (standard JSON parsing plus GraphQL support). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="log_level" type="string">
      The log level for the security policy, controlling the verbosity of logs. Allowed values are 'NORMAL' and 'VERBOSE'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="request_body_inspection_size" type="string">
      The maximum request size chosen by the customer with Waf enabled. Values supported are "8KB", "16KB, "32KB", "48KB" and "64KB". Values are case insensitive. Optional.
    </ResponseField>
    <ResponseField name="user_ip_request_headers" type="list(string)">
      An optional list of case-insensitive request header names to use for resolving the callers client IP address. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="associations" type="list(object)">
  A list of associations that belong to this policy. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="attachment_id" type="string">
      The ID of the folder or organization to which the security policy is attached. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The display name for the organization security policy association. This is a human-readable name used to identify the association. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="excluded_folders" type="list(string)">
      A list of folder IDs that are excluded from the association's scope. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="excluded_projects" type="list(string)">
      A list of project IDs that are excluded from this association. The organization security policy does not apply to the specified projects. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="security_policy_id" type="string">
      The ID of the security policy that this association belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="short_name" type="string">
      The short name of the project or folder that this organizational security policy is associated with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ddos_protection_config" type="object">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="ddos_adaptive_protection" type="string">
      Configures the adaptive protection mode for DDoS protection. Allowed values: DISABLED, ENABLED, PREVIEW, or UNSPECIFIED_ADAPTIVE_PROTECTION. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ddos_impacted_baseline_threshold" type="number">
      DDoS Protection for Network Load Balancers (and VMs with public IPs) builds DDoS mitigations that minimize collateral damage. It quantifies this as the fraction of a non-abuse baseline that's inadvertently blocked. Rules whose collateral damage exceeds ddosImpactedBaselineThreshold will not be deployed. Using a lower value will prioritize keeping collateral damage low, possibly at the cost of its effectiveness in rate limiting some or all of the attack. It should typically be unset, so Advanced DDoS (and Adaptive Protection) uses the best mitigation it can find. Setting the threshold is advised if there are logs for false positive detections with high collateral damage, and will cause Advanced DDoS to attempt to find a less aggressive rule that satisfies the constraint. If a suitable rule cannot be found, the system falls back to either no mitigation for smaller attacks or broader network throttles for larger ones. Optional.
    </ResponseField>
    <ResponseField name="ddos_protection" type="string">
      Enables or configures DDoS protection for the security policy. Allowed values are ADVANCED, ADVANCED_PREVIEW, and STANDARD. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Specifies a fingerprint for this resource, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make get() request to the security policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  A fingerprint for the labels being applied to this security policy, which is essentially a hash of the labels set used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update labels. You must always provide an up-to-date fingerprint hash in order to update or change labels. To see the latest fingerprint, make get() request to the security policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035. Label values may be empty. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parent" type="string">
  Output only. [Output Only] The parent of the security policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="recaptcha_options_config" type="object">
  Configuration for reCAPTCHA options in the security policy. It includes the enforcement mode and redirect site key used for reCAPTCHA verification. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="redirect_site_key" type="string">
      An optional field to supply a reCAPTCHA site key to be used for all the rules using the redirect action with the type of GOOGLE_RECAPTCHA under the security policy. The specified site key needs to be created from the reCAPTCHA API. The user is responsible for the validity of the specified site key. If not specified, a Google-managed site key is used. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the regional security policy resides. This field is not applicable to global security policies. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  A list of rules that belong to this policy. There must always be a default rule which is a rule with priority 2147483647 and match all condition (for the match condition this means match "*" for srcIpRanges and for the networkMatch condition every field must be either match "*" or not set). If no rules are provided when creating a security policy, a default rule with action "allow" will be added. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      The action to take when a request matches this rule. Valid values are 'allow', 'deny', and 'goto_next'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="header_action" type="object">
      The `header_action` block configures modifications to HTTP headers for requests that match the rule. It allows adding or removing request and response headers as defined in the policy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="request_headers_to_adds" type="list(object)">
          A list of request headers to add to the request when the rule matches. Each object contains 'header_name' and 'header_value' fields specifying the header to insert. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="header_name" type="string">
              The name of the request header that will be added to the request. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_value" type="string">
              The value to be set for the header added to requests that match the rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kind" type="string">
      The type of resource, always 'compute#securityPolicyRule'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="match" type="object">
      The match condition that defines which traffic this rule applies to, including source and destination IP ranges, protocols, and ports. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="config" type="object">
          The configuration block for the match. It specifies L4 network match criteria, such as source and destination IP ranges and layer 4 protocol/port settings. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="src_ip_ranges" type="list(string)">
              A list of source IP address ranges in CIDR notation that the rule matches on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="expr" type="object">
          The CEL (Common Expression Language) expression that defines the condition for when this rule matches traffic. This allows custom matching based on request attributes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="expression" type="string">
              The Common Expression Language (CEL) expression that defines which traffic matches this rule. It is evaluated against the request/response attributes to determine if the rule applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="location" type="string">
              The location of the expression, such as 'global' or a specific GCP region. Defaults to 'global' if not set. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="title" type="string">
              An optional title for the expression, providing a short summary of its purpose. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="expr_options" type="object">
          The `expr_options` block configures options for the expression used in the match condition. It can contain `recaptcha_options` to control how reCAPTCHA is handled for the rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="recaptcha_options" type="object">
              Optional; if omitted, computed by Google Cloud.
              <Expandable title="properties">
                <ResponseField name="action_token_site_keys" type="list(string)">
                  List of reCAPTCHA site keys used to verify action tokens for this security policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="session_token_site_keys" type="list(string)">
                  A list of site keys to be used during the validation of reCAPTCHA session tokens. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="versioned_expr" type="string">
          The version of the match expression syntax. The only supported value is SRC_IPS_V1, which indicates version 1 of the source IP expression format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_match" type="object">
      The network_match block specifies the network-level match criteria for the rule, including source and destination IP ranges or networks that determine when the rule is applied. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dest_ip_ranges" type="list(string)">
          A list of destination IPv4 or IPv6 CIDR ranges to which this rule applies. Traffic destined for an IP address within these ranges will be matched. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_ports" type="list(string)">
          A list of destination port numbers or port ranges (e.g., '80' or '8000-8080') that the rule matches for network traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ip_protocols" type="list(string)">
          A list of IP protocol types (e.g., 'tcp', 'udp', 'icmp') that this network match rule applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_asns" type="list(number)">
          A list of source autonomous system numbers (ASNs). The rule matches traffic that originates from any of the specified ASNs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_ip_ranges" type="list(string)">
          A list of source IP addresses in CIDR notation that the rule matches. If specified, the rule applies only to traffic originating from these IP ranges. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_ports" type="list(string)">
          The list of source ports to match in the network match condition. Each entry is a string that can be a single port (e.g., '80') or a port range (e.g., '443-8443'). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_region_codes" type="list(string)">
          Specifies the source region codes to match for traffic from specific geographic regions. The rule applies to traffic originating from the listed regions, using ISO 3166-1 alpha-2 codes (e.g., 'US', 'DE'). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_defined_fields" type="list(object)">
          A list of user-defined fields to match against in the network match criteria. Each object typically contains a 'name' and a list of 'values', allowing custom matching beyond standard IP/port fields. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the user-defined field, as defined in the organization security policy, used for matching network traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              The list of values to match against for the user-defined field. The rule applies when the field value is one of these strings. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preconfigured_waf_config" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="exclusions" type="list(object)">
          A list of exclusions that specify which request components (e.g., headers, cookies, query parameters) should be ignored when the preconfigured WAF rule is evaluated. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="request_cookies_to_exclude" type="list(object)">
              A list of request cookies that should not be checked by the preconfigured WAF rules. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator to apply for matching the cookie value. Valid values are: CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  The exact value of the cookie to match for exclusion. If specified, only cookies with this value and the corresponding key are excluded; if omitted, all cookies with the specified key are excluded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_headers_to_exclude" type="list(object)">
              A list of request headers to exclude from evaluation by the preconfigured WAF rule. Each object in the list specifies a header that should be ignored. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator used to match the request header value to be excluded. Accepted values are: CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, and STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  The value of the request header to exclude from WAF evaluation. Pairs with the header name in the corresponding key field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_query_params_to_exclude" type="list(object)">
              Specifies the request query parameters to exclude from the preconfigured WAF rule evaluation. Each item in the list identifies a query parameter to be excluded via a key and an operator. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator used to match the query parameter value for exclusion. Valid values are: CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  The value of the query parameter to match for exclusion. If omitted, all values for the specified key are excluded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_uris_to_exclude" type="list(object)">
              A list of objects, each defining a request URI to exclude from the WAF rule evaluation. The excluded URIs will not be subject to the preconfigured WAF rule set. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator used to match the request URI to exclude. Allowed values are: CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, and STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  The request URI to exclude from the preconfigured WAF configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="target_rule_ids" type="list(string)">
              A list of preconfigured WAF rule IDs that this exclusion targets. These rules will be exempt from the preconfigured WAF configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="target_rule_set" type="string">
              The name of the preconfigured WAF rule set to which this exclusion applies, such as 'xss-v33-stable' or 'sqli-v33-stable'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preview" type="bool">
      Whether the rule is in preview mode. When true, the rule is evaluated but not enforced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="priority" type="number">
      An integer value that determines the relative priority of this rule. Lower numbers indicate higher priority and are evaluated first. Must be unique within the security policy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rate_limit_options" type="object">
      A block for configuring rate limiting on the security policy rule. It defines actions and thresholds for traffic that exceeds the configured rate limit. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ban_duration_sec" type="number">
          Specifies the duration in seconds that a client is banned after exceeding the rate limit threshold. This can only be set when the rule action is 'rate_based_ban'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ban_threshold" type="object">
          Threshold at which a client is banned for exceeding the rate limit, as defined within the rate limit options. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              The number of requests that must occur within the specified interval to trigger a ban. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="interval_sec" type="number">
              The time interval, in seconds, over which the request count is evaluated to determine if the ban threshold is exceeded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="conform_action" type="string">
          The action to take when a request conforms to the rate limit. Valid values are 'allow' and 'deny'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enforce_on_key" type="string">
          The key type used for enforcing rate limits, determining how requests are grouped for rate limiting. Allowed values are: ALL, ASN, HTTP_COOKIE, HTTP_HEADER, HTTP_PATH, IP, REGION_CODE, SNI, TLS_JA3_FINGERPRINT, TLS_JA4_FINGERPRINT, USER_IP, XFF_IP. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enforce_on_key_configs" type="list(object)">
          A list of configurations for enforcing rate limits on specific keys. Each configuration specifies a key type (e.g., IP, HTTP_HEADER) and an optional name, enabling the rate limit to be applied based on different request attributes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enforce_on_key_name" type="string">
              The name of the key to enforce the rate limit on, such as a header name or cookie name, depending on the configured key type. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enforce_on_key_type" type="string">
              Specifies the type of key used to identify clients for rate limiting. For example, IP enforces on client IP addresses, HTTP_HEADER on a specific HTTP header, and ASN on autonomous system numbers. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enforce_on_key_name" type="string">
          Name of the key to enforce rate limiting on, used when enforce_on_key is set to a value such as HTTP_HEADER. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="exceed_action" type="string">
          The action to take when the rate limit is exceeded. Valid values are 'deny' and 'redirect'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="exceed_redirect_options" type="object">
          Defines the redirect behavior for requests that exceed the configured rate limit. This object specifies how to redirect traffic when the limit is surpassed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="target" type="string">
              The URL to redirect requests to when the rate limit is exceeded. Required if the redirect type is EXTERNAL_302. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of redirect to use when the rate limit is exceeded. Possible values are EXTERNAL_302 (redirect to an external URL via HTTP 302) and GOOGLE_RECAPTCHA (use Google reCAPTCHA to verify the user). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="rate_limit_threshold" type="object">
          An object that defines the threshold at which the rate limit rule is triggered, controlling when the rate limiting action is applied to traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              The number of requests allowed within the specified rate limit interval before the rate limit action is enforced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="interval_sec" type="number">
              The time interval, in seconds, for the rate limit threshold. This defines the window in which the 'count' of events is measured. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="redirect_options" type="object">
      Nested block that configures the redirect behavior for a security policy rule. It is used when the rule's action is set to redirect, specifying how requests should be redirected. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="target" type="string">
          The URL to redirect traffic to when the redirect type is EXTERNAL_302. This is required for EXTERNAL_302 type redirects. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of redirect. Valid values are EXTERNAL_302 (redirect to a custom external URL with HTTP 302) and GOOGLE_RECAPTCHA (redirect to Google reCAPTCHA). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="short_name" type="string">
  User-provided name of the organization security policy. The name should be unique in the organization in which the security policy is created. This should only be used when SecurityPolicyType is CLOUD_ARMOR. The name must be 1-63 characters long, and comply with https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  The type indicates the intended use of the security policy. - CLOUD_ARMOR: Cloud Armor backend security policies can be configured to filter incoming HTTP requests targeting backend services. They filter requests before they hit the origin servers. - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can be configured to filter incoming HTTP requests targeting backend services (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage). They filter requests before the request is served from Google's cache. - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor internal service policies can be configured to filter HTTP requests targeting services managed by Traffic Director in a service mesh. They filter requests before the request is served from the application. - CLOUD_ARMOR_NETWORK: Cloud Armor network policies can be configured to filter packets targeting network load balancing resources such as backend services, target pools, target instances, and instances with external IPs. They filter requests before the request is served from the application. This field can be set only at resource creation time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="user_defined_fields" type="list(object)">
  Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A user-defined field consists of up to 4 bytes extracted from a fixed offset in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an optional mask to select certain bits. Rules may then specify matching values for these fields. Example: userDefinedFields: - name: "ipv4_fragment_offset" base: IPV4 offset: 6 size: 2 mask: "0x1fff" Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="base" type="string">
      The base protocol or address family that the user-defined field applies to. Allowed values are IPV4, IPV6, TCP, and UDP. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mask" type="string">
      The mask specifies the exact field or pattern to extract from the selected base, such as a specific HTTP header name when base is 'request.headers' or a cookie name when base is 'request.cookies'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="offset" type="number">
      The byte offset from the base of the field. Specifies the start position of the user-defined field relative to the base (e.g., REQUEST). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="size" type="number">
      The size of the user-defined field in bytes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
