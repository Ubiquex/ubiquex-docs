---
title: "google_compute_organization_security_policy"
description: "Real, generated bindings for `google_compute_organization_security_policy`."
---

`google_compute_organization_security_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organization security policy"})

    		ubx.Resource(compute.OrganizationSecurityPolicy, "ci-runner", compute.OrganizationSecurityPolicyConfig{
    			Parent:      "example",
    			Description: "Managed by ubx.",
    			DisplayName: "example-display-name",
    			ShortName:   "example-short-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationSecurityPolicy } from "jsr:@ubx/sdk-google/google/compute/organization_security_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own organization security policy" });

      resource(OrganizationSecurityPolicy, "ci-runner", {
        parent: "example",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        shortName: "example-short-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import OrganizationSecurityPolicy, OrganizationSecurityPolicyConfig

    def describe():
        ubx.intent("payments own organization security policy")

        ubx.resource(OrganizationSecurityPolicy, "ci-runner", OrganizationSecurityPolicyConfig(
            parent="example",
            description="Managed by ubx.",
            display_name="example-display-name",
            short_name="example-short-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_organization_security_policy, with parent
    example, description Managed by ubx., display name
    example-display-name, short name example-short-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="advanced_options_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="json_custom_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="content_types" type="set(string)" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="json_parsing" type="string">
    </ResponseField>
    <ResponseField name="log_level" type="string">
    </ResponseField>
    <ResponseField name="request_body_inspection_size" type="string">
    </ResponseField>
    <ResponseField name="user_ip_request_headers" type="set(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A textual description for the organization security policy. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  User-provided name of the organization security policy. The name should be unique in the organization in which the security policy is created. This should only be used when SecurityPolicyType is FIREWALL. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent" type="string" required>
  The parent of this OrganizationSecurityPolicy in the Cloud Resource Hierarchy. Format: organizations/&#123;organization_id&#125; or folders/&#123;folder_id&#125; Required.
</ResponseField>

<ResponseField name="short_name" type="string">
  User-provided name of the organization security policy. The name should be unique in the organization in which the security policy is created. This should only be used when SecurityPolicyType is CLOUD_ARMOR. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type indicates the intended use of the security policy. This field can be set only at resource creation time. **NOTE** : 'FIREWALL' type is deprecated and will be removed in a future major release. Please use 'google_compute_firewall_policy' instead." Possible values: ["FIREWALL", "CLOUD_ARMOR", "CLOUD_ARMOR_EDGE", "CLOUD_ARMOR_INTERNAL_SERVICE", "CLOUD_ARMOR_NETWORK"] Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="advanced_options_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="json_custom_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="content_types" type="set(string)" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="json_parsing" type="string">
    </ResponseField>
    <ResponseField name="log_level" type="string">
    </ResponseField>
    <ResponseField name="request_body_inspection_size" type="string">
    </ResponseField>
    <ResponseField name="user_ip_request_headers" type="set(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. This field is used internally during updates of this resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_id" type="string">
  The unique identifier for the resource. This identifier is defined by the server. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type indicates the intended use of the security policy. This field can be set only at resource creation time. **NOTE** : 'FIREWALL' type is deprecated and will be removed in a future major release. Please use 'google_compute_firewall_policy' instead." Possible values: ["FIREWALL", "CLOUD_ARMOR", "CLOUD_ARMOR_EDGE", "CLOUD_ARMOR_INTERNAL_SERVICE", "CLOUD_ARMOR_NETWORK"] Optional; if omitted, computed by GCP.
</ResponseField>
