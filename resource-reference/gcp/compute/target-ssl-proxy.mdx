---
title: "google_compute_target_ssl_proxy"
description: "Real, generated bindings for `google_compute_target_ssl_proxy`."
---

`google_compute_target_ssl_proxy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own target ssl proxy"})

    		ubx.Resource(compute.TargetSslProxy, "ci-runner", compute.TargetSslProxyConfig{
    			BackendService:  "example",
    			Name:            "ci-runner",
    			CertificateMap:  "example",
    			Description:     "Managed by ubx.",
    			ProxyHeader:     "example",
    			SslCertificates: []string{"example"},
    			SslPolicy:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TargetSslProxy } from "jsr:@ubx/sdk-google/google/compute/target_ssl_proxy";

    export default stack("payments", () => {
      intent({ summary: "payments own target ssl proxy" });

      resource(TargetSslProxy, "ci-runner", {
        backendService: "example",
        name: "ci-runner",
        certificateMap: "example",
        description: "Managed by ubx.",
        proxyHeader: "example",
        sslCertificates: ["example"],
        sslPolicy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import TargetSslProxy, TargetSslProxyConfig

    def describe():
        ubx.intent("payments own target ssl proxy")

        ubx.resource(TargetSslProxy, "ci-runner", TargetSslProxyConfig(
            backend_service="example",
            name="ci-runner",
            certificate_map="example",
            description="Managed by ubx.",
            proxy_header="example",
            ssl_certificates=["example"],
            ssl_policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_target_ssl_proxy called "ci-runner", with
    backend service example, certificate map example, description
    Managed by ubx., proxy header example, ssl certificates a set
    value, ssl policy example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backend_service" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_map" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="proxy_header" type="string">
  Optional.
</ResponseField>

<ResponseField name="ssl_certificates" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="ssl_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_timestamp" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="proxy_id" type="number">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
