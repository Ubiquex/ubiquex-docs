---
title: "google_compute_packet_mirroring"
description: "`google_compute_packet_mirroring` clones real network traffic to and from a set of instances and sends the copy to a collector..."
---

`google_compute_packet_mirroring` clones real network traffic to and from a set of instances and sends the copy to a collector, without disrupting the original traffic at all -- used for real, out-of-band security monitoring and intrusion detection.

Real, typed bindings generated directly from real Google Discovery Documents, in every SDK language -- every tab below is a complete, runnable program, not a fragment, real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own packet mirroring"})

    		ubx.Resource(compute.PacketMirroring, "ci-runner", compute.PacketMirroringConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PacketMirroring } from "jsr:@ubx/sdk-google/google/compute/packet_mirroring";

    export default stack("payments", () => {
      intent({ summary: "payments own packet mirroring" });

      resource(PacketMirroring, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import PacketMirroring, PacketMirroringConfig

    def describe():
        ubx.intent("payments own packet mirroring")

        ubx.resource(PacketMirroring, "ci-runner", PacketMirroringConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_packet_mirroring called "ci-runner", with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="collector_ilb" type="object">
  The internal load balancer (ILB) used as the collector for mirrored traffic. This output-only field identifies the forwarding rule that receives mirrored packets. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="canonical_url" type="string">
      Output only. [Output Only] Unique identifier for the forwarding rule; defined by the server. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="url" type="string">
      Resource URL to the forwarding rule representing the ILB configured as destination of the mirrored traffic. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable" type="string">
  Indicates whether or not this packet mirroring takes effect. If set to FALSE, this packet mirroring policy will not be enforced on the network. The default is TRUE. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="filter" type="object">
  The filter configuration as returned by the API (output only). It defines the criteria that determine which packets are mirrored. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cidr_ranges" type="list(string)">
      One or more IPv4 or IPv6 CIDR ranges that apply as filters on the source (ingress) or destination (egress) IP in the IP header. If no ranges are specified, all IPv4 traffic that matches the specified IPProtocols is mirrored. If neither cidrRanges nor IPProtocols is specified, all IPv4 traffic is mirrored. To mirror all IPv4 and IPv6 traffic, use "0.0.0.0/0,::/0". Optional.
    </ResponseField>
    <ResponseField name="direction" type="string">
      Direction of traffic to mirror, either INGRESS, EGRESS, or BOTH. The default is BOTH. Optional.
    </ResponseField>
    <ResponseField name="ipprotocols" type="list(string)">
      Protocols that apply as filter on mirrored traffic. If no protocols are specified, all traffic that matches the specified CIDR ranges is mirrored. If neither cidrRanges nor IPProtocols is specified, all IPv4 traffic is mirrored. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#packetMirroring for packet mirrorings. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="mirrored_resources" type="object">
  An object defining the instances, subnetworks, or tags whose traffic is mirrored. This field is output-only and reflects the effective mirroring configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="instances" type="list(object)">
      A set of virtual machine instances that are being mirrored. They must live in zones contained in the same region as this packetMirroring. Note that this config will apply only to those network interfaces of the Instances that belong to the network specified in this packetMirroring. You may specify a maximum of 50 Instances. Optional.
      <Expandable title="properties">
        <ResponseField name="canonical_url" type="string">
          Canonical URL of the Compute Engine instance that will have its traffic mirrored. This is the self_link of the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string">
          The URL of the Compute Engine instance to mirror. This is typically the full self-link of the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="subnetworks" type="list(object)">
      A set of subnetworks for which traffic from/to all VM instances will be mirrored. They must live in the same region as this packetMirroring. You may specify a maximum of 5 subnetworks. Optional.
      <Expandable title="properties">
        <ResponseField name="canonical_url" type="string">
          The canonical URL of the subnetwork whose traffic is mirrored. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string">
          The URL of the subnetwork that this mirroring rule applies to. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="list(string)">
      A set of mirrored tags. Traffic from/to all VM instances that have one or more of these tags will be mirrored. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network" type="object">
  The network that the packet mirroring applies to. This field is output-only and contains the URL of the VPC network. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="canonical_url" type="string">
      Output only. [Output Only] Unique identifier for the network; defined by the server. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="url" type="string">
      URL of the network resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="priority" type="number">
  The priority of applying this configuration. Priority is used to break ties in cases where there is more than one matching rule. In the case of two rules that apply for a given Instance, the one with the lowest-numbered priority value wins. Default value is 1000. Valid range is 0 through 65535. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  [Output Only] URI of the region where the packetMirroring resides. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="collector_ilb" type="object">
  The internal load balancer (ILB) used as the collector for mirrored traffic. This output-only field identifies the forwarding rule that receives mirrored packets. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="canonical_url" type="string">
      Output only. [Output Only] Unique identifier for the forwarding rule; defined by the server. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="url" type="string">
      Resource URL to the forwarding rule representing the ILB configured as destination of the mirrored traffic. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable" type="string">
  Indicates whether or not this packet mirroring takes effect. If set to FALSE, this packet mirroring policy will not be enforced on the network. The default is TRUE. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="filter" type="object">
  The filter configuration as returned by the API (output only). It defines the criteria that determine which packets are mirrored. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cidr_ranges" type="list(string)">
      One or more IPv4 or IPv6 CIDR ranges that apply as filters on the source (ingress) or destination (egress) IP in the IP header. If no ranges are specified, all IPv4 traffic that matches the specified IPProtocols is mirrored. If neither cidrRanges nor IPProtocols is specified, all IPv4 traffic is mirrored. To mirror all IPv4 and IPv6 traffic, use "0.0.0.0/0,::/0". Optional.
    </ResponseField>
    <ResponseField name="direction" type="string">
      Direction of traffic to mirror, either INGRESS, EGRESS, or BOTH. The default is BOTH. Optional.
    </ResponseField>
    <ResponseField name="ipprotocols" type="list(string)">
      Protocols that apply as filter on mirrored traffic. If no protocols are specified, all traffic that matches the specified CIDR ranges is mirrored. If neither cidrRanges nor IPProtocols is specified, all IPv4 traffic is mirrored. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#packetMirroring for packet mirrorings. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="mirrored_resources" type="object">
  An object defining the instances, subnetworks, or tags whose traffic is mirrored. This field is output-only and reflects the effective mirroring configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="instances" type="list(object)">
      A set of virtual machine instances that are being mirrored. They must live in zones contained in the same region as this packetMirroring. Note that this config will apply only to those network interfaces of the Instances that belong to the network specified in this packetMirroring. You may specify a maximum of 50 Instances. Optional.
      <Expandable title="properties">
        <ResponseField name="canonical_url" type="string">
          Canonical URL of the Compute Engine instance that will have its traffic mirrored. This is the self_link of the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string">
          The URL of the Compute Engine instance to mirror. This is typically the full self-link of the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="subnetworks" type="list(object)">
      A set of subnetworks for which traffic from/to all VM instances will be mirrored. They must live in the same region as this packetMirroring. You may specify a maximum of 5 subnetworks. Optional.
      <Expandable title="properties">
        <ResponseField name="canonical_url" type="string">
          The canonical URL of the subnetwork whose traffic is mirrored. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string">
          The URL of the subnetwork that this mirroring rule applies to. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="list(string)">
      A set of mirrored tags. Traffic from/to all VM instances that have one or more of these tags will be mirrored. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network" type="object">
  The network that the packet mirroring applies to. This field is output-only and contains the URL of the VPC network. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="canonical_url" type="string">
      Output only. [Output Only] Unique identifier for the network; defined by the server. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="url" type="string">
      URL of the network resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="priority" type="number">
  The priority of applying this configuration. Priority is used to break ties in cases where there is more than one matching rule. In the case of two rules that apply for a given Instance, the one with the lowest-numbered priority value wins. Default value is 1000. Valid range is 0 through 65535. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  [Output Only] URI of the region where the packetMirroring resides. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>
