---
title: "google_compute_image_iam_policy"
description: "Real, generated bindings for `google_compute_image_iam_policy`."
---

`google_compute_image_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    cfg := compute.ImageIamPolicyConfig{
        Image: "example",
        PolicyData: "example",
    }
    ubx.Resource(compute.ImageIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ImageIamPolicy } from
      "jsr:@ubx/sdk-google/google/compute/image_iam_policy";

    resource(ImageIamPolicy, "example", {
      image: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        ImageIamPolicy,
        ImageIamPolicyConfig,
    )

    cfg = ImageIamPolicyConfig(
        image="example",
        policy_data="example",
    )
    ubx.resource(ImageIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_image_iam_policy with image,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="image" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
