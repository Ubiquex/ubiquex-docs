---
title: "google_compute_network_endpoint"
description: "Real, generated bindings for `google_compute_network_endpoint`."
---

`google_compute_network_endpoint` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/compute"
    )

    cfg := compute.NetworkEndpointConfig{
        IpAddress: "example",
        NetworkEndpointGroup: "example",
        Instance: "example",
        Port: 1,
    }
    ubx.Resource(compute.NetworkEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { NetworkEndpoint } from
      "jsr:@ubx/sdk-google/google/compute/network_endpoint";

    resource(NetworkEndpoint, "example", {
      ipAddress: "example",
      networkEndpointGroup: "example",
      instance: "example",
      port: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        NetworkEndpoint,
        NetworkEndpointConfig,
    )

    cfg = NetworkEndpointConfig(
        ip_address="example",
        network_endpoint_group="example",
        instance="example",
        port=1,
    )
    ubx.resource(NetworkEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_network_endpoint with ip_address,
network_endpoint_group, instance, port.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string">
  Optional.
</ResponseField>

<ResponseField name="ip_address" type="string" required>
  Required.
</ResponseField>

<ResponseField name="network_endpoint_group" type="string" required>
  Required.
</ResponseField>

<ResponseField name="port" type="number">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
