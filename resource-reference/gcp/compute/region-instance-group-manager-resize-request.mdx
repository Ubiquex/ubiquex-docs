---
title: "google_compute_region_instance_group_manager_resize_request"
description: "`google_compute_region_instance_group_manager_resize_request` is the regional counterpart to a zonal resize request -- requesting a real, specific..."
---

`google_compute_region_instance_group_manager_resize_request` is the regional counterpart to a zonal resize request -- requesting a real, specific number of instances be added at once to a regional managed instance group.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own region instance group manager resize request"})

    		ubx.Resource(compute.RegionInstanceGroupManagerResizeRequest, "example", compute.RegionInstanceGroupManagerResizeRequestConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionInstanceGroupManagerResizeRequest } from "@ubx/sdk-google/google/compute/region_instance_group_manager_resize_request";

    export default stack("example", () => {
      intent({
        summary: "example own region instance group manager resize request",
      });

      resource(RegionInstanceGroupManagerResizeRequest, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute.region_instance_group_manager_resize_request import RegionInstanceGroupManagerResizeRequest, RegionInstanceGroupManagerResizeRequestConfig

    def describe():
        ubx.intent("example own region instance group manager resize request")

        ubx.resource(RegionInstanceGroupManagerResizeRequest, "example", RegionInstanceGroupManagerResizeRequestConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    google_compute_region_instance_group_manager_resize_request
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="creation_timestamp" type="string">
  Output only. The creation timestamp for this resize request inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. A unique identifier for this resource type. The server generates this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instances" type="list(object)">
  The names of instances to be created by this resize request. The number of names specified determines the number of instances to create. The group's target size will be increased by this number. This field cannot be used together with 'resize_by'. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="fingerprint" type="string">
      The fingerprint of the instance used for optimistic concurrency control, ensuring the instance hasn't changed since it was last read. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="preserved_state" type="object">
      Configuration for preserving instance state during a resize operation, such as metadata and disk settings. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="disks" type="map(object)">
          Map of disk names to preserved disk state. Each key is the disk name, and the value is an object specifying the source of the disk and optionally the disk mode (READ_ONLY or READ_WRITE). Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="auto_delete" type="string">
              Indicates whether the disk is automatically deleted when the instance is deleted (true) or preserved (false). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="mode" type="string">
              The mode of a preserved disk, indicating whether the disk is attached in read-only or read-write mode. Acceptable values are 'READ_WRITE' and 'READ_ONLY'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source" type="string">
              The source disk or image to preserve for the instance's stateful disk, specified as a name, self-link, or partial URL. This disk or image is used to restore the instance's disk state during resize operations. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="external_ips" type="map(object)">
          A map of external IPs to preserve for the instance as part of its preserved state during a resize operation. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="auto_delete" type="string">
              Indicates whether the external IP is automatically deleted when the instance is deleted. Valid values are 'TRUE' and 'FALSE'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ip_address" type="object">
              The preserved external IP address configuration for a network interface. This object includes the IP address and its address type (such as INTERNAL or EXTERNAL). Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="address" type="string">
                  The external IP address to preserve and assign to the instance when the resize request is processed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="literal" type="string">
                  The literal IP address value to assign as the preserved external IP address for this instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="internal_ips" type="map(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="auto_delete" type="string">
              Whether to automatically delete the internal IP address when the instance is deleted. When true, the IP is deleted with the instance; when false, it is preserved. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ip_address" type="object">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="address" type="string">
                  The IP address value to preserve for the instance's internal IP configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="literal" type="string">
                  The literal IP address to preserve for the internal IP. This is used when you want to specify the IP address directly as a string, rather than using a reference to another resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="metadata" type="map(string)">
          A map of metadata key-value pairs to preserve for the instance when the resize request is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="status" type="string">
      The status of an instance in the resize request. Possible values are: APPLYING, DELETING, EFFECTIVE, NONE, UNAPPLIED, and UNAPPLIED_DELETION. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for resize requests. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of this resize request. The name must be 1-63 characters long, and comply withRFC1035. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Output only. The URL of a region where the resize request is located. Populated only for regional resize requests. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="requested_run_duration" type="object">
  A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="nanos" type="number">
      Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
    </ResponseField>
    <ResponseField name="seconds" type="string">
      Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resize_by" type="number">
  The number of instances to be created by this resize request. The group's target size will be increased by this number. This field cannot be used together with 'instances'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. The URL for this resize request. The server defines this URL. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. Server-defined URL for this resource with the resource id. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. Current state of the request. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="error" type="object">
      Output only. Fatal errors encountered during the queueing or provisioning phases of the ResizeRequest that caused the transition to the FAILED state. Contrary to the last_attempt errors, this field is final and errors are never removed from here, as the ResizeRequest is not going to retry. Computed by GCP after `ship`.
      <Expandable title="properties">
        <ResponseField name="errors" type="list(object)">
          [Output Only] The array of errors encountered while processing this operation. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The machine-readable error code for the resize request failure, as returned by the Compute Engine API. This string typically identifies the specific error condition. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="error_details" type="list(object)">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="error_info" type="object">
                  Optional; if omitted, computed by GCP.
                  <Expandable title="properties">
                    <ResponseField name="domain" type="string">
                      The logical grouping to which the error belongs, often a service domain such as 'googleapis.com' or 'gke.io'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="metadatas" type="map(string)">
                      Additional structured details about this error, provided as key-value pairs. Keys must match the regex `[a-z][a-zA-Z0-9]*` or `_[a-z][a-zA-Z0-9]*`. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="reason" type="string">
                      The reason that identifies the specific cause of the error. This value is unique within the error's domain and typically follows the pattern `[A-Z0-9_]+` (e.g., 'SERVICE_DISABLED'). Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="help" type="object">
                  An object containing links to documentation that help resolve the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="links" type="list(object)">
                      A list of links to helpful resources related to the error, where each link object typically contains a description and a URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="description" type="string">
                          A human-readable description of the help link, providing context or guidance related to the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="url" type="string">
                          URL to a help resource related to the error detail. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="localized_message" type="object">
                  A localized error message object, typically containing a 'locale' (e.g., 'en-US') and a 'message' string with the error text in that locale. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="locale" type="string">
                      The locale (language tag, e.g., 'en-US') of the localized error message. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      The localized human-readable error message text, provided in the language specified by the corresponding 'locale' field within the localized_message error detail. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="quota_info" type="object">
                  Information about the quota limit that was exceeded, including the metric name, the quota limit, and the current usage. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dimensions" type="map(string)">
                      A map of quota dimension keys to values, such as region, zone, or resource type, that identifies the specific quota limit that was exceeded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="future_limit" type="number">
                      The quota limit that will apply if the resize request is accepted, used in quota error details. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="limit" type="number">
                      The maximum allowed value for the quota metric, as specified by the quota limit. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="limit_name" type="string">
                      The name of the specific quota limit that was exceeded, as reported in the quota_info error details. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="metric_name" type="string">
                      The name of the quota metric that was exceeded, such as 'compute.googleapis.com/cpus'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="rollout_status" type="string">
                      The rollout status of the quota, indicating whether the rollout is in progress. Allowed values: IN_PROGRESS and ROLLOUT_STATUS_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="location" type="string">
              The path or field in the request that the error pertains to, typically a JSON Pointer or field name. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable error message that explains the failure. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_attempt" type="object">
      Represents the most recent attempt to resize the instance group, including error details if the attempt failed. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="error" type="object">
          Output only. Errors that prevented the ResizeRequest to be fulfilled. Computed by GCP after `ship`.
          <Expandable title="properties">
            <ResponseField name="errors" type="list(object)">
              [Output Only] The array of errors encountered while processing this operation. Optional.
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  The error code associated with this error entry, as returned by the Compute Engine API. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="error_details" type="list(object)">
                  A list of structured error details that provide additional information about the failure, such as specific reasons or troubleshooting hints, complementing the error code and message. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="error_info" type="object">
                      Optional; if omitted, computed by GCP.
                      <Expandable title="properties">
                        <ResponseField name="domain" type="string">
                          The logical grouping to which the error reason belongs. Often a domain name like 'googleapis.com' or a custom service domain. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="metadatas" type="map(string)">
                          A map of metadata associated with the error, providing additional context as key-value pairs. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="reason" type="string">
                          The machine-readable reason for the error, as defined by the service in the ErrorInfo structure. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="help" type="object">
                      The help object provides links to documentation or guidance for resolving the error. It is part of the structured error details returned when a resize attempt fails. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="links" type="list(object)">
                          A list of links to documentation or resources that can help resolve the error. Each link typically contains a description and a URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="description" type="string">
                              A human-readable description of the help link. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="url" type="string">
                              The URL of a help link providing additional documentation about the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="localized_message" type="object">
                      A localized error message object that contains the language locale and the localized message text, used to display error details in the user's preferred language. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="locale" type="string">
                          The locale (language tag) of the localized error message, in BCP-47 format. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="message" type="string">
                          The localized error message text, providing the error details in the language specified by the associated locale. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="quota_info" type="object">
                      Details about the quota limit that was exceeded or otherwise triggered the error, such as the metric name, limit, and usage values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="dimensions" type="map(string)">
                          A map of quota dimensions (e.g., region, zone) that identify the specific quota that was exceeded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="future_limit" type="number">
                          Optional; if omitted, computed by GCP.
                        </ResponseField>
                        <ResponseField name="limit" type="number">
                          The quota limit for the metric, representing the maximum allowed value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="limit_name" type="string">
                          The name of the specific quota limit that was exceeded, as defined in the project's quota configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="metric_name" type="string">
                          The name of the quota metric that was exceeded in the resize request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="rollout_status" type="string">
                          Indicates the current rollout status of the quota operation. Possible values are IN_PROGRESS (the rollout is underway) or ROLLOUT_STATUS_UNSPECIFIED (the status has not been specified). Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="location" type="string">
                  The location of the error, typically a field path or resource name indicating where the error occurred in the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable description of the error that occurred. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  Output only. The URL of a zone where the resize request is located. Populated only for zonal resize requests. Optional; if omitted, computed by GCP.
</ResponseField>
