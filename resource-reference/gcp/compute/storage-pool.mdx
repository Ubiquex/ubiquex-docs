---
title: "google_compute_storage_pool"
description: "Real, generated bindings for `google_compute_storage_pool`."
---

`google_compute_storage_pool` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own storage pool"})

    		ubx.Resource(compute.StoragePool, "ci-runner", compute.StoragePoolConfig{
    			Name:                      "ci-runner",
    			PoolProvisionedCapacityGb: "example",
    			PoolProvisionedThroughput: "example",
    			StoragePoolType:           "example",
    			DeletionProtection:        true,
    			Description:               "Managed by ubx.",
    			Labels:                    map[string]string{"managed-by": "ubx"},
    			PoolProvisionedIops:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StoragePool } from "jsr:@ubx/sdk-google/google/compute/storage_pool";

    export default stack("payments", () => {
      intent({ summary: "payments own storage pool" });

      resource(StoragePool, "ci-runner", {
        name: "ci-runner",
        poolProvisionedCapacityGb: "example",
        poolProvisionedThroughput: "example",
        storagePoolType: "example",
        deletionProtection: true,
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
        poolProvisionedIops: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import StoragePool, StoragePoolConfig

    def describe():
        ubx.intent("payments own storage pool")

        ubx.resource(StoragePool, "ci-runner", StoragePoolConfig(
            name="ci-runner",
            pool_provisioned_capacity_gb="example",
            pool_provisioned_throughput="example",
            storage_pool_type="example",
            deletion_protection=True,
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
            pool_provisioned_iops="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_storage_pool called "ci-runner", with pool
    provisioned capacity gb example, pool provisioned throughput
    example, storage pool type example, deletion protection true,
    description Managed by ubx., labels a set value, pool provisioned
    iops example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="capacity_provisioning_type" type="string">
  Provisioning type of the byte capacity of the pool. Possible values: ["STANDARD", "ADVANCED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_protection" type="bool">
  Whether Terraform will be prevented from destroying the StoragePool. When the field is set to true or unset in Terraform state, a 'terraform apply' or 'terraform destroy' that would delete the StoragePool will fail. When the field is set to false, deleting the StoragePool is allowed. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  A description of this resource. Provide this property when you create the resource. Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels to apply to this storage pool. These can be later modified by the setLabels method. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Required.
</ResponseField>

<ResponseField name="params" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="performance_provisioning_type" type="string">
  Provisioning type of the performance-related parameters of the pool, such as throughput and IOPS. Possible values: ["STANDARD", "ADVANCED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="pool_provisioned_capacity_gb" type="string" required>
  Size, in GiB, of the storage pool. For more information about the size limits, see https://cloud.google.com/compute/docs/disks/storage-pools. Required.
</ResponseField>

<ResponseField name="pool_provisioned_iops" type="string">
  Provisioned IOPS of the storage pool. Only relevant if the storage pool type is 'hyperdisk-balanced'. Optional.
</ResponseField>

<ResponseField name="pool_provisioned_throughput" type="string" required>
  Provisioned throughput, in MB/s, of the storage pool. Only relevant if the storage pool type is 'hyperdisk-balanced' or 'hyperdisk-throughput'. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="storage_pool_type" type="string" required>
  Type of the storage pool. For example, the following are valid values: * 'https://www.googleapis.com/compute/v1/projects/&#123;project_id&#125;/zones/&#123;zone&#125;/storagePoolTypes/hyperdisk-balanced' * 'hyperdisk-throughput' Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  A reference to the zone where the storage pool resides. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="capacity_provisioning_type" type="string">
  Provisioning type of the byte capacity of the pool. Possible values: ["STANDARD", "ADVANCED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Creation timestamp in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier for the resource. This identifier is defined by the server. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="kind" type="string">
  Type of the resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  The fingerprint used for optimistic locking of this resource. Used internally during updates. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="params" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="performance_provisioning_type" type="string">
  Provisioning type of the performance-related parameters of the pool, such as throughput and IOPS. Possible values: ["STANDARD", "ADVANCED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_status" type="list(object)">
  Status information for the storage pool resource. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="disk_count" type="string">
    </ResponseField>
    <ResponseField name="last_resize_timestamp" type="string">
    </ResponseField>
    <ResponseField name="max_total_provisioned_disk_capacity_gb" type="string">
    </ResponseField>
    <ResponseField name="pool_used_capacity_bytes" type="string">
    </ResponseField>
    <ResponseField name="pool_used_iops" type="string">
    </ResponseField>
    <ResponseField name="pool_used_throughput" type="string">
    </ResponseField>
    <ResponseField name="pool_user_written_bytes" type="string">
    </ResponseField>
    <ResponseField name="total_provisioned_disk_capacity_gb" type="string">
    </ResponseField>
    <ResponseField name="total_provisioned_disk_iops" type="string">
    </ResponseField>
    <ResponseField name="total_provisioned_disk_throughput" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="list(object)">
  Status information for the storage pool resource. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="disk_count" type="string">
    </ResponseField>
    <ResponseField name="last_resize_timestamp" type="string">
    </ResponseField>
    <ResponseField name="max_total_provisioned_disk_capacity_gb" type="string">
    </ResponseField>
    <ResponseField name="pool_used_capacity_bytes" type="string">
    </ResponseField>
    <ResponseField name="pool_used_iops" type="string">
    </ResponseField>
    <ResponseField name="pool_used_throughput" type="string">
    </ResponseField>
    <ResponseField name="pool_user_written_bytes" type="string">
    </ResponseField>
    <ResponseField name="total_provisioned_disk_capacity_gb" type="string">
    </ResponseField>
    <ResponseField name="total_provisioned_disk_iops" type="string">
    </ResponseField>
    <ResponseField name="total_provisioned_disk_throughput" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  A reference to the zone where the storage pool resides. Optional; if omitted, computed by GCP.
</ResponseField>
