---
title: "google_compute_network_endpoint_group"
description: "Real, generated bindings for `google_compute_network_endpoint_group`."
---

`google_compute_network_endpoint_group` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network endpoint group"})

    		ubx.Resource(compute.NetworkEndpointGroup, "ci-runner", compute.NetworkEndpointGroupConfig{
    			Name:                "ci-runner",
    			Network:             "example",
    			DefaultPort:         1,
    			Description:         "Managed by ubx.",
    			NetworkEndpointType: "example",
    			Subnetwork:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkEndpointGroup } from "jsr:@ubx/sdk-google/google/compute/network_endpoint_group";

    export default stack("payments", () => {
      intent({ summary: "payments own network endpoint group" });

      resource(NetworkEndpointGroup, "ci-runner", {
        name: "ci-runner",
        network: "example",
        defaultPort: 1,
        description: "Managed by ubx.",
        networkEndpointType: "example",
        subnetwork: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import NetworkEndpointGroup, NetworkEndpointGroupConfig

    def describe():
        ubx.intent("payments own network endpoint group")

        ubx.resource(NetworkEndpointGroup, "ci-runner", NetworkEndpointGroupConfig(
            name="ci-runner",
            network="example",
            default_port=1,
            description="Managed by ubx.",
            network_endpoint_type="example",
            subnetwork="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_network_endpoint_group called "ci-runner",
    with network example, default port 1, description Managed by
    ubx., network endpoint type example, subnetwork example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="default_port" type="number">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="network" type="string" required>
  Required.
</ResponseField>

<ResponseField name="network_endpoint_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="subnetwork" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="generated_id" type="number">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="size" type="number">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
