---
title: "google_compute_region_snapshot"
description: "A complete, runnable google_compute_region_snapshot program, in every SDK language."
---

`google_compute_region_snapshot` -- real, typed bindings generated directly from
real Google Discovery Documents, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region snapshot"})

    		ubx.Resource(compute.RegionSnapshot, "ci-runner", compute.RegionSnapshotConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionSnapshot } from "./local-sdk/google/sdk/typescript/google/compute/region_snapshot.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own region snapshot" });

      resource(RegionSnapshot, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.compute import RegionSnapshot, RegionSnapshotConfig

    def describe():
        ubx.intent("payments own region snapshot")

        ubx.resource(RegionSnapshot, "ci-runner", RegionSnapshotConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_snapshot called "ci-runner", with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="architecture" type="string">
  Output only. [Output Only] The architecture of the snapshot. Valid values are ARM64 or X86_64. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="auto_created" type="bool">
  Output only. [Output Only] Set to true if snapshots are automatically created by applying resource policy on the target disk. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="chain_name" type="string">
  Creates the new snapshot in the snapshot chain labeled with the specified name. The chain name must be 1-63 characters long and comply with RFC1035. This is an uncommon option only for advanced service owners who needs to create separate snapshot chains, for example, for chargeback tracking. When you describe your snapshot resource, this field is visible only if it has a non-empty value. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_size_bytes" type="string">
  Output only. [Output Only] Size in bytes of the snapshot at creation time. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disk_size_gb" type="string">
  Output only. [Output Only] Size of the source disk, specified in GB. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="download_bytes" type="string">
  Output only. [Output Only] Number of bytes downloaded to restore a snapshot to a disk. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_confidential_compute" type="bool">
  Output only. Whether this snapshot is created from a confidential compute mode disk. [Output Only]: This field is not set by user, but from source disk. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="guest_flush" type="bool">
  [Input Only] Whether to attempt an application consistent snapshot by informing the OS to prepare for the snapshot process. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="guest_os_features" type="list(object)">
  Output only. [Output Only] A list of features to enable on the guest operating system. Applicable only for bootable images. Read Enabling guest operating system features to see a list of available options. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="type" type="string">
      The guest OS feature type to enable for the snapshot. Possible values include: BARE_METAL_LINUX_COMPATIBLE, CCA_CAPABLE, FEATURE_TYPE_UNSPECIFIED, GVNIC, IDPF, MULTI_IP_SUBNET, SECURE_BOOT, SEV_CAPABLE, SEV_LIVE_MIGRATABLE, SEV_LIVE_MIGRATABLE_V2, SEV_SNP_CAPABLE, SNP_SVSM_CAPABLE, TDX_CAPABLE, UEFI_COMPATIBLE, VIRTIO_SCSI_MULTIQUEUE, WINDOWS. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#snapshot for Snapshot resources. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  A fingerprint for the labels being applied to this snapshot, which is essentially a hash of the labels set used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update labels. You must always provide an up-to-date fingerprint hash in order to update or change labels, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels to apply to this snapshot. These can be later modified by the setLabels method. Label values may be empty. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="license_codes" type="list(string)">
  Output only. [Output Only] Integer license codes indicating which licenses are attached to this snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="licenses" type="list(string)">
  Output only. [Output Only] A list of public visible licenses that apply to this snapshot. This can be because the original image had licenses attached (such as a Windows image). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location_hint" type="string">
  An opaque location hint used to place the snapshot close to other resources. This field is for use by internal tools that use the public API. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="params" type="object">
  Additional snapshot params. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the snapshot. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the snapshot resides. Only applicable for regional snapshots. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. [Output Only] Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="snapshot_encryption_key" type="object">
  The customer-managed encryption key used to encrypt this region snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional.
    </ResponseField>
    <ResponseField name="kms_key_service_account" type="string">
      The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional.
    </ResponseField>
    <ResponseField name="raw_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional.
    </ResponseField>
    <ResponseField name="rsa_encrypted_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional.
    </ResponseField>
    <ResponseField name="sha256" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="snapshot_group_id" type="string">
  Output only. [Output Only] The unique ID of the snapshot group that this snapshot belongs to. The usage of snapshot group feature is restricted. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="snapshot_group_name" type="string">
  Output only. [Output only] The snapshot group that this snapshot belongs to. The usage of snapshot group feature is restricted. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="snapshot_type" type="string">
  Indicates the type of the snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_disk" type="string">
  The source disk used to create this snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_disk_encryption_key" type="object">
  The customer-supplied encryption key details for the source disk. This field is output-only and reflects the encryption key used by the source disk when it is protected by a customer-supplied encryption key. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional.
    </ResponseField>
    <ResponseField name="kms_key_service_account" type="string">
      The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional.
    </ResponseField>
    <ResponseField name="raw_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional.
    </ResponseField>
    <ResponseField name="rsa_encrypted_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional.
    </ResponseField>
    <ResponseField name="sha256" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_disk_for_recovery_checkpoint" type="string">
  The source disk whose recovery checkpoint will be used to create this snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_disk_id" type="string">
  Output only. [Output Only] The ID value of the disk used to create this snapshot. This value may be used to determine whether the snapshot was taken from the current or a previous instance of a given disk name. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instant_snapshot" type="string">
  The source instant snapshot used to create this snapshot. You can provide this as a partial or full URL to the resource. For example, the following are valid values: - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot - projects/project/zones/zone/instantSnapshots/instantSnapshot - zones/zone/instantSnapshots/instantSnapshot Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instant_snapshot_encryption_key" type="object">
  The customer-supplied encryption key of the source instant snapshot. This key is required if the source instant snapshot is encrypted with a customer-managed key, and it is also exposed as an output when populated by the API. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional.
    </ResponseField>
    <ResponseField name="kms_key_service_account" type="string">
      The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional.
    </ResponseField>
    <ResponseField name="raw_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional.
    </ResponseField>
    <ResponseField name="rsa_encrypted_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional.
    </ResponseField>
    <ResponseField name="sha256" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_instant_snapshot_id" type="string">
  Output only. [Output Only] The unique ID of the instant snapshot used to create this snapshot. This value identifies the exact instant snapshot that was used to create this snapshot. For example, if you created the snapshot from an instant snapshot that was later deleted and recreated under the same name, the source instant snapshot ID would identify the exact instant snapshot that was used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_snapshot_schedule_policy" type="string">
  Output only. [Output Only] URL of the resource policy which created this scheduled snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_snapshot_schedule_policy_id" type="string">
  Output only. [Output Only] ID of the resource policy which created this scheduled snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="storage_bytes" type="string">
  Output only. [Output Only] A size of the storage used by the snapshot. As snapshots share storage, this number is expected to change with snapshot creation/deletion. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="storage_bytes_status" type="string">
  Output only. [Deprecated] Instead, check the storageBytes field. After snapshot creation, the storageBytesStatus field is alwaysUP_TO_DATE. [Output Only] An indicator whether storageBytes is in a stable state or it is being adjusted as a result of shared storage reallocation. This status can either be unset, meaning the snapshot is being created, or UP_TO_DATE, meaning the size of the snapshot is up-to-date. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="storage_locations" type="list(string)">
  Cloud Storage bucket storage location of the snapshot (regional or multi-regional). Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="architecture" type="string">
  Output only. [Output Only] The architecture of the snapshot. Valid values are ARM64 or X86_64. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="auto_created" type="bool">
  Output only. [Output Only] Set to true if snapshots are automatically created by applying resource policy on the target disk. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="chain_name" type="string">
  Creates the new snapshot in the snapshot chain labeled with the specified name. The chain name must be 1-63 characters long and comply with RFC1035. This is an uncommon option only for advanced service owners who needs to create separate snapshot chains, for example, for chargeback tracking. When you describe your snapshot resource, this field is visible only if it has a non-empty value. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_size_bytes" type="string">
  Output only. [Output Only] Size in bytes of the snapshot at creation time. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disk_size_gb" type="string">
  Output only. [Output Only] Size of the source disk, specified in GB. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="download_bytes" type="string">
  Output only. [Output Only] Number of bytes downloaded to restore a snapshot to a disk. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_confidential_compute" type="bool">
  Output only. Whether this snapshot is created from a confidential compute mode disk. [Output Only]: This field is not set by user, but from source disk. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="guest_flush" type="bool">
  [Input Only] Whether to attempt an application consistent snapshot by informing the OS to prepare for the snapshot process. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="guest_os_features" type="list(object)">
  Output only. [Output Only] A list of features to enable on the guest operating system. Applicable only for bootable images. Read Enabling guest operating system features to see a list of available options. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="type" type="string">
      The guest OS feature type to enable for the snapshot. Possible values include: BARE_METAL_LINUX_COMPATIBLE, CCA_CAPABLE, FEATURE_TYPE_UNSPECIFIED, GVNIC, IDPF, MULTI_IP_SUBNET, SECURE_BOOT, SEV_CAPABLE, SEV_LIVE_MIGRATABLE, SEV_LIVE_MIGRATABLE_V2, SEV_SNP_CAPABLE, SNP_SVSM_CAPABLE, TDX_CAPABLE, UEFI_COMPATIBLE, VIRTIO_SCSI_MULTIQUEUE, WINDOWS. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#snapshot for Snapshot resources. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  A fingerprint for the labels being applied to this snapshot, which is essentially a hash of the labels set used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update labels. You must always provide an up-to-date fingerprint hash in order to update or change labels, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels to apply to this snapshot. These can be later modified by the setLabels method. Label values may be empty. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="license_codes" type="list(string)">
  Output only. [Output Only] Integer license codes indicating which licenses are attached to this snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="licenses" type="list(string)">
  Output only. [Output Only] A list of public visible licenses that apply to this snapshot. This can be because the original image had licenses attached (such as a Windows image). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location_hint" type="string">
  An opaque location hint used to place the snapshot close to other resources. This field is for use by internal tools that use the public API. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="params" type="object">
  Additional snapshot params. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the snapshot. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the snapshot resides. Only applicable for regional snapshots. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. [Output Only] Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="snapshot_encryption_key" type="object">
  The customer-managed encryption key used to encrypt this region snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional.
    </ResponseField>
    <ResponseField name="kms_key_service_account" type="string">
      The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional.
    </ResponseField>
    <ResponseField name="raw_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional.
    </ResponseField>
    <ResponseField name="rsa_encrypted_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional.
    </ResponseField>
    <ResponseField name="sha256" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="snapshot_group_id" type="string">
  Output only. [Output Only] The unique ID of the snapshot group that this snapshot belongs to. The usage of snapshot group feature is restricted. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="snapshot_group_name" type="string">
  Output only. [Output only] The snapshot group that this snapshot belongs to. The usage of snapshot group feature is restricted. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="snapshot_type" type="string">
  Indicates the type of the snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_disk" type="string">
  The source disk used to create this snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_disk_encryption_key" type="object">
  The customer-supplied encryption key details for the source disk. This field is output-only and reflects the encryption key used by the source disk when it is protected by a customer-supplied encryption key. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional.
    </ResponseField>
    <ResponseField name="kms_key_service_account" type="string">
      The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional.
    </ResponseField>
    <ResponseField name="raw_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional.
    </ResponseField>
    <ResponseField name="rsa_encrypted_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional.
    </ResponseField>
    <ResponseField name="sha256" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_disk_for_recovery_checkpoint" type="string">
  The source disk whose recovery checkpoint will be used to create this snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_disk_id" type="string">
  Output only. [Output Only] The ID value of the disk used to create this snapshot. This value may be used to determine whether the snapshot was taken from the current or a previous instance of a given disk name. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instant_snapshot" type="string">
  The source instant snapshot used to create this snapshot. You can provide this as a partial or full URL to the resource. For example, the following are valid values: - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot - projects/project/zones/zone/instantSnapshots/instantSnapshot - zones/zone/instantSnapshots/instantSnapshot Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instant_snapshot_encryption_key" type="object">
  The customer-supplied encryption key of the source instant snapshot. This key is required if the source instant snapshot is encrypted with a customer-managed key, and it is also exposed as an output when populated by the API. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional.
    </ResponseField>
    <ResponseField name="kms_key_service_account" type="string">
      The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional.
    </ResponseField>
    <ResponseField name="raw_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional.
    </ResponseField>
    <ResponseField name="rsa_encrypted_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional.
    </ResponseField>
    <ResponseField name="sha256" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_instant_snapshot_id" type="string">
  Output only. [Output Only] The unique ID of the instant snapshot used to create this snapshot. This value identifies the exact instant snapshot that was used to create this snapshot. For example, if you created the snapshot from an instant snapshot that was later deleted and recreated under the same name, the source instant snapshot ID would identify the exact instant snapshot that was used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_snapshot_schedule_policy" type="string">
  Output only. [Output Only] URL of the resource policy which created this scheduled snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_snapshot_schedule_policy_id" type="string">
  Output only. [Output Only] ID of the resource policy which created this scheduled snapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="storage_bytes" type="string">
  Output only. [Output Only] A size of the storage used by the snapshot. As snapshots share storage, this number is expected to change with snapshot creation/deletion. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="storage_bytes_status" type="string">
  Output only. [Deprecated] Instead, check the storageBytes field. After snapshot creation, the storageBytesStatus field is alwaysUP_TO_DATE. [Output Only] An indicator whether storageBytes is in a stable state or it is being adjusted as a result of shared storage reallocation. This status can either be unset, meaning the snapshot is being created, or UP_TO_DATE, meaning the size of the snapshot is up-to-date. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="storage_locations" type="list(string)">
  Cloud Storage bucket storage location of the snapshot (regional or multi-regional). Optional; if omitted, computed by GCP.
</ResponseField>
