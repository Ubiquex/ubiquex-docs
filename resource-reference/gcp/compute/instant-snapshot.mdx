---
title: "google_compute_instant_snapshot"
description: "`google_compute_instant_snapshot` is a real, near-instantaneous..."
---

`google_compute_instant_snapshot` is a real, near-instantaneous, zonal point-in-time copy of a persistent disk -- faster to create and restore from than a standard snapshot, at the cost of needing to stay in the same zone as the disk it came from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own instant snapshot"})

    		ubx.Resource(compute.InstantSnapshot, "example", compute.InstantSnapshotConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstantSnapshot } from "jsr:@ubx/sdk-google/google/compute/instant_snapshot";

    export default stack("example", () => {
      intent({ summary: "example own instant snapshot" });

      resource(InstantSnapshot, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import InstantSnapshot, InstantSnapshotConfig

    def describe():
        ubx.intent("example own instant snapshot")

        ubx.resource(InstantSnapshot, "example", InstantSnapshotConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_instant_snapshot called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="architecture" type="string">
  Output only. [Output Only] The architecture of the instant snapshot. Valid values are ARM64 or X86_64. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disk_size_gb" type="string">
  Output only. [Output Only] Size of the source disk, specified in GB. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  A fingerprint for the labels being applied to this InstantSnapshot, which is essentially a hash of the labels set used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update labels. You must always provide an up-to-date fingerprint hash in order to update or change labels, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a InstantSnapshot. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels to apply to this InstantSnapshot. These can be later modified by the setLabels method. Label values may be empty. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="params" type="object">
  Additional instant snapshot params. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the instant snapshot. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the instant snapshot resides. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_status" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="storage_size_bytes" type="string">
      [Output Only] The storage size of this instant snapshot. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. [Output Only] Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource's resource id. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_disk" type="string">
  URL of the source disk used to create this instant snapshot. Note that the source disk must be in the same zone/region as the instant snapshot to be created. This can be a full or valid partial URL. For example, the following are valid values: - https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk - https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk - projects/project/zones/zone/disks/disk - projects/project/regions/region/disks/disk - zones/zone/disks/disk - regions/region/disks/disk Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_disk_id" type="string">
  Output only. [Output Only] The ID value of the disk used to create this InstantSnapshot. This value may be used to determine whether the InstantSnapshot was taken from the current or a previous instance of a given disk name. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instant_snapshot_group" type="string">
  Output only. [Output Only] URL of the source instant snapshot this instant snapshot is part of. Note that the source instant snapshot group must be in the same zone/region as the instant snapshot to be created. This can be a full or valid partial URL. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instant_snapshot_group_id" type="string">
  Output only. [Output Only] The ID value of the source instant snapshot group this InstantSnapshot is part of. This value may be used to determine whether the InstantSnapshot was created as part of an InstantSnapshotGroup creation. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="zone" type="string">
  Output only. [Output Only] URL of the zone where the instant snapshot resides. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>
