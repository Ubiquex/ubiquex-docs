---
title: "google_compute_security_policy"
description: "`google_compute_security_policy` is a Cloud Armor policy -- real WAF rules, rate limiting..."
---

`google_compute_security_policy` is a Cloud Armor policy -- real WAF rules, rate limiting, and DDoS protection -- attached to a backend service to filter traffic before it ever reaches a real backend.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own security policy"})

    		ubx.Resource(compute.SecurityPolicy, "example", compute.SecurityPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityPolicy } from "@ubx/sdk-google/google/compute/security_policy";

    export default stack("example", () => {
      intent({ summary: "example own security policy" });

      resource(SecurityPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import SecurityPolicy, SecurityPolicyConfig

    def describe():
        ubx.intent("example own security policy")

        ubx.resource(SecurityPolicy, "example", SecurityPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_security_policy called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="adaptive_protection_config" type="object">
  Configuration options for Cloud Armor Adaptive Protection (CAAP). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="layer7_ddos_defense_config" type="object">
      Configuration options for L7 DDoS detection. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
      <Expandable title="properties">
        <ResponseField name="enable" type="bool">
          If set to true, enables CAAP for L7 DDoS detection. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
        </ResponseField>
        <ResponseField name="rule_visibility" type="string">
          Rule visibility can be one of the following: STANDARD - opaque rules. (default) PREMIUM - transparent rules. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
        </ResponseField>
        <ResponseField name="threshold_configs" type="list(object)">
          Configuration options for layer7 adaptive protection for various customizable thresholds. Optional.
          <Expandable title="properties">
            <ResponseField name="auto_deploy_confidence_threshold" type="number">
              The minimum confidence score (between 0 and 1) that adaptive protection requires before automatically deploying mitigation rules for layer 7 DDoS defense. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="auto_deploy_expiration_sec" type="number">
              The number of seconds that the auto-deploy system waits before automatically deploying the mitigation rules. If not set, the default value is 3600 seconds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="auto_deploy_impacted_baseline_threshold" type="number">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="auto_deploy_load_threshold" type="number">
              Specifies the traffic load threshold in requests per second that triggers automatic deployment of Layer 7 DDoS adaptive protection. When traffic exceeds this threshold and other conditions are met, Google Cloud Armor may automatically enable mitigation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="detection_absolute_qps" type="number">
              The absolute queries per second (QPS) threshold that triggers detection of a layer 7 DDoS attack. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="detection_load_threshold" type="number">
              The load threshold in requests per second that triggers detection of a potential DDoS attack by the layer 7 adaptive protection. Exceeding this threshold causes the system to start monitoring traffic for attack indicators. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="detection_relative_to_baseline_qps" type="number">
              Detection threshold in queries per second (QPS) relative to the baseline traffic level for triggering adaptive protection. A value greater than 1 indicates a multiple of the baseline QPS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="traffic_granularity_configs" type="list(object)">
              A list of traffic granularity configurations for the threshold. Each configuration defines a traffic type (such as SOURCE_IP or USER_AGENT) and a value to specify the granularity of traffic to which the threshold applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="enable_each_unique_value" type="bool">
                  If true, the threshold is applied to each unique value (e.g., each source IP) independently, instead of aggregating all traffic together. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The type of traffic granularity. Valid values are `HTTP_HEADER_HOST` (group traffic by the HTTP Host header), `HTTP_PATH` (group traffic by the HTTP request path), and `UNSPECIFIED_TYPE` (default, unspecified). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  Optional; if omitted, computed by Google Cloud.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="advanced_options_config" type="object">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="json_custom_config" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="content_types" type="list(string)">
          A list of custom Content-Type header values to apply the JSON parsing. As per RFC 1341, a Content-Type header value has the following format: Content-Type := type "/" subtype *[";" parameter] When configuring a custom Content-Type header value, only the type/subtype needs to be specified, and the parameters should be excluded. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="json_parsing" type="string">
      Controls the JSON payload parsing mode for the security policy. Possible values: DISABLED (no JSON parsing), STANDARD (standard JSON parsing), or STANDARD_WITH_GRAPHQL (standard JSON plus GraphQL parsing). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="log_level" type="string">
      Specifies the log level for the security policy, controlling how much detail is logged. Valid values are NORMAL and VERBOSE. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="request_body_inspection_size" type="string">
      The maximum request size chosen by the customer with Waf enabled. Values supported are "8KB", "16KB, "32KB", "48KB" and "64KB". Values are case insensitive. Optional.
    </ResponseField>
    <ResponseField name="user_ip_request_headers" type="list(string)">
      An optional list of case-insensitive request header names to use for resolving the callers client IP address. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="associations" type="list(object)">
  A list of associations that belong to this policy. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="attachment_id" type="string">
      The ID of the resource (e.g., backend service or target instance) to which the security policy is attached. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The display name of the security policy association. This is a user-defined name used to identify the association. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="excluded_folders" type="list(string)">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="excluded_projects" type="list(string)">
      A list of project IDs or project numbers to exclude from this security policy association. This is used when the security policy is associated with a parent resource such as an organization or folder, to prevent the policy from applying to the specified projects. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="security_policy_id" type="string">
      The ID of the security policy that is being associated. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="short_name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ddos_protection_config" type="object">
  Configuration for DDoS protection on the security policy, containing the protection class. This field is computed and output-only, so it cannot be set in configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="ddos_adaptive_protection" type="string">
      Configures the adaptive protection mode for DDoS attacks. Allowed values are DISABLED, ENABLED, PREVIEW, and UNSPECIFIED_ADAPTIVE_PROTECTION (the default, which uses the provider's unspecified behavior). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ddos_impacted_baseline_threshold" type="number">
      DDoS Protection for Network Load Balancers (and VMs with public IPs) builds DDoS mitigations that minimize collateral damage. It quantifies this as the fraction of a non-abuse baseline that's inadvertently blocked. Rules whose collateral damage exceeds ddosImpactedBaselineThreshold will not be deployed. Using a lower value will prioritize keeping collateral damage low, possibly at the cost of its effectiveness in rate limiting some or all of the attack. It should typically be unset, so Advanced DDoS (and Adaptive Protection) uses the best mitigation it can find. Setting the threshold is advised if there are logs for false positive detections with high collateral damage, and will cause Advanced DDoS to attempt to find a less aggressive rule that satisfies the constraint. If a suitable rule cannot be found, the system falls back to either no mitigation for smaller attacks or broader network throttles for larger ones. Optional.
    </ResponseField>
    <ResponseField name="ddos_protection" type="string">
      Specifies the Google Cloud Armor DDoS protection tier for this security policy. Valid values are ADVANCED, ADVANCED_PREVIEW, and STANDARD. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Specifies a fingerprint for this resource, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make get() request to the security policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  A fingerprint for the labels being applied to this security policy, which is essentially a hash of the labels set used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update labels. You must always provide an up-to-date fingerprint hash in order to update or change labels. To see the latest fingerprint, make get() request to the security policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035. Label values may be empty. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parent" type="string">
  Output only. [Output Only] The parent of the security policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="recaptcha_options_config" type="object">
  Configuration for reCAPTCHA options in the security policy. This field is computed, meaning it is populated by the API and cannot be set manually. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="redirect_site_key" type="string">
      An optional field to supply a reCAPTCHA site key to be used for all the rules using the redirect action with the type of GOOGLE_RECAPTCHA under the security policy. The specified site key needs to be created from the reCAPTCHA API. The user is responsible for the validity of the specified site key. If not specified, a Google-managed site key is used. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the regional security policy resides. This field is not applicable to global security policies. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  A list of rules that belong to this policy. There must always be a default rule which is a rule with priority 2147483647 and match all condition (for the match condition this means match "*" for srcIpRanges and for the networkMatch condition every field must be either match "*" or not set). If no rules are provided when creating a security policy, a default rule with action "allow" will be added. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      The action to take when a request matches the rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="header_action" type="object">
      The header_action block configures the request headers to add or remove for requests that match this security policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="request_headers_to_adds" type="list(object)">
          A list of request headers to be added to matching requests. Each object specifies a header name and its value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="header_name" type="string">
              The name of the header to add to the request. For example, 'X-Frame-Options'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_value" type="string">
              The value of the request header to add. This is the literal string value that will be sent with the header. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kind" type="string">
      The type of the resource. Always 'compute#securityPolicyRule'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="match" type="object">
      The match block configures the request criteria that trigger the rule. It can contain either a config block for source IP ranges or an expr block for a CEL expression, depending on the policy version. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="config" type="object">
          The match configuration for the rule, which defines the traffic to match using parameters such as source IP ranges, versioned expression, and layer 4 configs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="src_ip_ranges" type="list(string)">
              List of source IP address ranges (in CIDR notation) that the rule uses to match traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="expr" type="object">
          The expr block defines a match expression in Common Expression Language (CEL) that is evaluated against incoming requests. If the expression evaluates to true, the rule is applied. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              A human-readable description of the expression, used to document the purpose or intent of the match condition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="expression" type="string">
              The CEL expression that defines the match condition for the security policy rule. This expression is evaluated against incoming requests to determine if the rule applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="location" type="string">
              Optional string that identifies the source location (e.g., file and line) of the CEL expression, used for error reporting and debugging. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="title" type="string">
              The title for the match expression, providing a human-readable name to identify the expression in the security policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="expr_options" type="object">
          Optional; if omitted, computed by Google Cloud.
          <Expandable title="properties">
            <ResponseField name="recaptcha_options" type="object">
              Optional; if omitted, computed by Google Cloud.
              <Expandable title="properties">
                <ResponseField name="action_token_site_keys" type="list(string)">
                  A list of site keys to be used for validating reCAPTCHA action tokens. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="session_token_site_keys" type="list(string)">
                  Specifies a list of reCAPTCHA site keys used to verify session tokens for this rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="versioned_expr" type="string">
          The version of the expression to use for the match rule. Currently, only 'SRC_IPS_V1' is supported, which matches on source IP addresses. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_match" type="object">
      Defines the network match criteria for a security policy rule, specifying conditions such as source and destination IP ranges, IP protocols, and ports that determine when the rule applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dest_ip_ranges" type="list(string)">
          List of destination IP ranges (in CIDR format) that this network match rule applies to. Each entry can be an IPv4 or IPv6 CIDR block, e.g., '192.0.2.0/24' or '2001:db8::/32'. Traffic whose destination IP falls within any of these ranges will match the rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_ports" type="list(string)">
          An optional list of destination ports to match. Each element may be a single port number (e.g., '80') or a range of ports (e.g., '80-90'). If not specified, all destination ports are matched. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ip_protocols" type="list(string)">
          A list of IP protocols that the rule matches. Traffic must use one of the specified protocols to match the rule; if omitted, the rule matches all IP protocols. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_asns" type="list(number)">
          List of source Autonomous System Numbers (ASNs) that the source IP address of the request must belong to for the rule to match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_ip_ranges" type="list(string)">
          A list of source IP address ranges in CIDR notation that incoming traffic must match for the rule to apply. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_ports" type="list(string)">
          A list of source ports or port ranges (e.g., '80' or '80-90') to match in the network rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_region_codes" type="list(string)">
          List of source region codes (two-letter ISO 3166 alpha-2 country codes) used to match requests based on the geographic location of the source IP address. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_defined_fields" type="list(object)">
          A list of user-defined fields for matching network traffic. Each object contains a field name and optional values to match against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Identifies the user-defined field to match in network traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              A list of string values to match against the referenced user-defined field. The rule matches when the field value is equal to one of these values. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preconfigured_waf_config" type="object">
      Configuration block for preconfigured WAF (Web Application Firewall) rules. This block contains exclusions that allow selectively disabling specific preconfigured WAF rules or parts of them for this security policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="exclusions" type="list(object)">
          A list of exclusions for the preconfigured WAF configuration. Each exclusion defines rules or rule sets that are excluded from the managed preconfigured WAF rules. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="request_cookies_to_exclude" type="list(object)">
              A list of request cookies to exclude from the preconfigured WAF rule evaluation. Each object defines a cookie to be ignored when the rule inspects incoming requests. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The match operator to use for the cookie, such as CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, or STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  The value of the request cookie to exclude. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_headers_to_exclude" type="list(object)">
              Specifies a list of request headers to exclude from the preconfigured WAF rule evaluation. When configured, these headers are not inspected by the WAF rules, helping to avoid false positives. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator used to match the request header value for exclusion from the preconfigured WAF rules. Valid values are CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, and STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  The value of the request header to exclude from the preconfigured WAF rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_query_params_to_exclude" type="list(object)">
              A list of request query parameters to exclude from evaluation by the preconfigured WAF rule. Each object defines a query parameter that should be ignored when the rule is applied. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator used to match the request query parameter name to exclude. Allowed values are: CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  The value of the query parameter to be excluded. This is used together with the key to specify a particular query parameter and value that should be excluded from evaluation by the preconfigured WAF rules. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_uris_to_exclude" type="list(object)">
              A list of request URIs to exclude from matching the preconfigured WAF rule. Each item defines a specific URI that should be excluded from evaluation, so requests to these URIs will not be blocked or flagged by this rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator used to match the request URI to exclude. Valid values are CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, and STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  A request URI to exclude from the preconfigured WAF rule's evaluation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="target_rule_ids" type="list(string)">
              The list of specific rule IDs to exclude from the preconfigured WAF configuration. These are the target rules that the exclusion applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="target_rule_set" type="string">
              The name of the predefined WAF rule set that this exclusion applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preview" type="bool">
      If true, the rule is in preview mode, meaning the action is not enforced but traffic is logged for testing. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="priority" type="number">
      The priority of this rule. Rules are evaluated in order from lowest to highest value (i.e., lower numbers have higher precedence). The priority must be an integer between 0 and 2147483647. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rate_limit_options" type="object">
      This block configures the rate limiting behavior for the rule, including parameters like the threshold and enforcement key. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ban_duration_sec" type="number">
          The duration in seconds that a client is banned when the rule action is 'rate_based_ban'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ban_threshold" type="object">
          The threshold that triggers a temporary ban for an IP address that has exceeded the rate limit. It includes a count of requests and an interval in seconds over which the count is measured. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              The number of requests that must be exceeded to trigger the ban threshold. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="interval_sec" type="number">
              The time interval in seconds during which the number of requests (count) is evaluated to determine if the ban threshold is exceeded. For example, an interval_sec of 60 means the count must be exceeded within a 60-second rolling window. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="conform_action" type="string">
          Action to take for requests that are within the configured rate limit (i.e., do not exceed the threshold). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enforce_on_key" type="string">
          The key type on which the rate limit is enforced. Valid values are ALL, ASN, HTTP_COOKIE, HTTP_HEADER, HTTP_PATH, IP, REGION_CODE, SNI, TLS_JA3_FINGERPRINT, TLS_JA4_FINGERPRINT, USER_IP, and XFF_IP. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enforce_on_key_configs" type="list(object)">
          A list of configurations that specify the keys on which to enforce the rate limit. Each configuration includes a key type (e.g., IP, HTTP_HEADER, HTTP_COOKIE) and can optionally define exclusions for the rate limit. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enforce_on_key_name" type="string">
              The name of the key to enforce rate limiting on, such as the header name or cookie name, as specified by the corresponding enforce_on_key_type field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enforce_on_key_type" type="string">
              The type of request key to enforce rate limiting on. Valid values include: ALL, ASN, HTTP_COOKIE, HTTP_HEADER, HTTP_PATH, IP, REGION_CODE, SNI, TLS_JA3_FINGERPRINT, TLS_JA4_FINGERPRINT, USER_IP, XFF_IP. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enforce_on_key_name" type="string">
          The name of the key to enforce the rate limit on, used when the enforceOnKey field requires a key name (e.g., the HTTP header name when enforceOnKey is HTTP_HEADER). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="exceed_action" type="string">
          Action to take when the rate limit threshold is exceeded. Valid values are 'deny(403)' and 'rateBasedBan'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="exceed_redirect_options" type="object">
          Options for the redirect action taken when requests exceed the rate limit threshold. Used when the exceed action is set to redirect, specifying the redirect type and target URL. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="target" type="string">
              The target URL to redirect to when the rate limit is exceeded. Required when the redirect type is EXTERNAL_302. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of redirect to use when the rate limit is exceeded. Possible values are EXTERNAL_302, which redirects to an external URL, and GOOGLE_RECAPTCHA, which presents a Google reCAPTCHA challenge. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="rate_limit_threshold" type="object">
          An object defining the request threshold that triggers rate limiting. It specifies the number of requests (count) allowed within a given time interval (interval_sec). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              The number of requests allowed within the configured interval before the rate limit action is applied. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="interval_sec" type="number">
              The time interval in seconds over which the rate limit count is measured. The rate limit is triggered when the number of requests within this interval reaches the specified count. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="redirect_options" type="object">
      Configuration for the redirect action to take when this rule matches. Contains settings such as redirect type and target. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="target" type="string">
          The URL to redirect the request to when the redirect type is set to EXTERNAL_302. It specifies the destination for the redirect action. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of redirect action. Valid values are EXTERNAL_302, which redirects to an external URL with HTTP 302, and GOOGLE_RECAPTCHA, which redirects to Google reCAPTCHA. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="short_name" type="string">
  User-provided name of the organization security policy. The name should be unique in the organization in which the security policy is created. This should only be used when SecurityPolicyType is CLOUD_ARMOR. The name must be 1-63 characters long, and comply with https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  The type indicates the intended use of the security policy. - CLOUD_ARMOR: Cloud Armor backend security policies can be configured to filter incoming HTTP requests targeting backend services. They filter requests before they hit the origin servers. - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can be configured to filter incoming HTTP requests targeting backend services (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage). They filter requests before the request is served from Google's cache. - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor internal service policies can be configured to filter HTTP requests targeting services managed by Traffic Director in a service mesh. They filter requests before the request is served from the application. - CLOUD_ARMOR_NETWORK: Cloud Armor network policies can be configured to filter packets targeting network load balancing resources such as backend services, target pools, target instances, and instances with external IPs. They filter requests before the request is served from the application. This field can be set only at resource creation time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="user_defined_fields" type="list(object)">
  Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A user-defined field consists of up to 4 bytes extracted from a fixed offset in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an optional mask to select certain bits. Rules may then specify matching values for these fields. Example: userDefinedFields: - name: "ipv4_fragment_offset" base: IPV4 offset: 6 size: 2 mask: "0x1fff" Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="base" type="string">
      The base protocol or address family on which this user-defined field operates. Valid values are IPV4, IPV6, TCP, and UDP. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mask" type="string">
      The mask that identifies the field to extract from the request or response, such as an HTTP header name when base is 'request'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="offset" type="number">
      The byte offset from the start of the selected base (e.g., TCP header) where the user-defined field begins. Offsets are zero-based; an offset of 0 refers to the first byte. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="size" type="number">
      Specifies the fixed length in bytes to extract from the base (e.g., TCP or UDP header) when defining a user-defined field by size. This is mutually exclusive with 'match' and is used to capture a fixed-size portion of the packet. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
