---
title: "google_compute_subnetwork_iam_policy"
description: "Real, generated bindings for `google_compute_subnetwork_iam_policy`."
---

`google_compute_subnetwork_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    cfg := compute.SubnetworkIamPolicyConfig{
        PolicyData: "example",
        Subnetwork: "example",
    }
    ubx.Resource(compute.SubnetworkIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SubnetworkIamPolicy } from
      "jsr:@ubx/sdk-google/google/compute/subnetwork_iam_policy";

    resource(SubnetworkIamPolicy, "example", {
      policyData: "example",
      subnetwork: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        SubnetworkIamPolicy,
        SubnetworkIamPolicyConfig,
    )

    cfg = SubnetworkIamPolicyConfig(
        policy_data="example",
        subnetwork="example",
    )
    ubx.resource(SubnetworkIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_subnetwork_iam_policy with policy_data,
subnetwork.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="subnetwork" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
