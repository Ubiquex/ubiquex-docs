---
title: "google_compute_route"
description: "Real, generated bindings for `google_compute_route`."
---

`google_compute_route` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own route"})

    		ubx.Resource(compute.Route, "ci-runner", compute.RouteConfig{
    			DestRange:        "example",
    			Name:             "ci-runner",
    			Network:          "example",
    			Description:      "Managed by ubx.",
    			NextHopGateway:   "example",
    			NextHopIlb:       "example",
    			NextHopInstance:  "example",
    			NextHopVpnTunnel: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Route } from "jsr:@ubx/sdk-google/google/compute/route";

    export default stack("payments", () => {
      intent({ summary: "payments own route" });

      resource(Route, "ci-runner", {
        destRange: "example",
        name: "ci-runner",
        network: "example",
        description: "Managed by ubx.",
        nextHopGateway: "example",
        nextHopIlb: "example",
        nextHopInstance: "example",
        nextHopVpnTunnel: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import Route, RouteConfig

    def describe():
        ubx.intent("payments own route")

        ubx.resource(Route, "ci-runner", RouteConfig(
            dest_range="example",
            name="ci-runner",
            network="example",
            description="Managed by ubx.",
            next_hop_gateway="example",
            next_hop_ilb="example",
            next_hop_instance="example",
            next_hop_vpn_tunnel="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_route called "ci-runner", with dest range
    example, network example, description Managed by ubx., next hop
    gateway example, next hop ilb example, next hop instance example,
    next hop vpn tunnel example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional.
</ResponseField>

<ResponseField name="dest_range" type="string" required>
  The destination range of outgoing packets that this route applies to. Only IPv4 is supported. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Required.
</ResponseField>

<ResponseField name="network" type="string" required>
  The network that this route applies to. Required.
</ResponseField>

<ResponseField name="next_hop_gateway" type="string">
  URL to a gateway that should handle matching packets. Currently, you can only specify the internet gateway, using a full or partial valid URL: * 'https://www.googleapis.com/compute/v1/projects/project/global/gateways/default-internet-gateway' * 'projects/project/global/gateways/default-internet-gateway' * 'global/gateways/default-internet-gateway' * The string 'default-internet-gateway'. Optional.
</ResponseField>

<ResponseField name="next_hop_ilb" type="string">
  The IP address or URL to a forwarding rule of type loadBalancingScheme=INTERNAL that should handle matching packets. With the GA provider you can only specify the forwarding rule as a partial or full URL. For example, the following are all valid values: * 10.128.0.56 * https://www.googleapis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule * regions/region/forwardingRules/forwardingRule When the beta provider, you can also specify the IP address of a forwarding rule from the same VPC or any peered VPC. Note that this can only be used when the destinationRange is a public (non-RFC 1918) IP CIDR range. Optional.
</ResponseField>

<ResponseField name="next_hop_instance" type="string">
  URL to an instance that should handle matching packets. You can specify this as a full or partial URL. For example: * 'https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance' * 'projects/project/zones/zone/instances/instance' * 'zones/zone/instances/instance' * Just the instance name, with the zone in 'next_hop_instance_zone'. Optional.
</ResponseField>

<ResponseField name="next_hop_instance_zone" type="string">
  The zone of the instance specified in next_hop_instance. Omit if next_hop_instance is specified as a URL. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="next_hop_ip" type="string">
  Network IP address of an instance that should handle matching packets. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="next_hop_vpn_tunnel" type="string">
  URL to a VpnTunnel that should handle matching packets. Optional.
</ResponseField>

<ResponseField name="params" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="priority" type="number">
  The priority of this route. Priority is used to break ties in cases where there is more than one matching route of equal prefix length. In the case of two routes with equal prefix length, the one with the lowest-numbered priority value wins. Default value is 1000. Valid range is 0 through 65535. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tags" type="set(string)">
  A list of instance tags to which this route applies. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="as_paths" type="list(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="as_lists" type="list(number)">
    </ResponseField>
    <ResponseField name="path_segment_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Creation timestamp in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="next_hop_hub" type="string">
  The hub network that should handle matching packets, which should conform to RFC1035. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="next_hop_instance_zone" type="string">
  The zone of the instance specified in next_hop_instance. Omit if next_hop_instance is specified as a URL. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="next_hop_inter_region_cost" type="string">
  Internal fixed region-to-region cost that Google Cloud calculates based on factors such as network performance, distance, and available bandwidth between regions. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="next_hop_ip" type="string">
  Network IP address of an instance that should handle matching packets. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="next_hop_med" type="string">
  Multi-Exit Discriminator, a BGP route metric that indicates the desirability of a particular route in a network. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="next_hop_network" type="string">
  URL to a Network that should handle matching packets. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="next_hop_origin" type="string">
  Indicates the origin of the route. Can be IGP (Interior Gateway Protocol), EGP (Exterior Gateway Protocol), or INCOMPLETE. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="next_hop_peering" type="string">
  The network peering name that should handle matching packets, which should conform to RFC1035. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="params" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="route_status" type="string">
  The status of the route, which can be one of the following values: - 'ACTIVE' for an active route - 'INACTIVE' for an inactive route Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="route_type" type="string">
  The type of this route, which can be one of the following values: - 'TRANSIT' for a transit route that this router learned from another Cloud Router and will readvertise to one of its BGP peers - 'SUBNET' for a route from a subnet of the VPC - 'BGP' for a route learned from a BGP peer of this router - 'STATIC' for a static route Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="warnings" type="list(object)">
  If potential misconfigurations are detected for this route, this field will be populated with warning messages. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
    </ResponseField>
    <ResponseField name="data" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="key" type="string">
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="message" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
