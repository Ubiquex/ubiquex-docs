---
title: "google_compute_alpha_network_edge_security_service"
description: "`google_compute_alpha_network_edge_security_service` enables advanced network DDoS protection for every applicable regional resource -- network load..."
---

`google_compute_alpha_network_edge_security_service` enables advanced network DDoS protection for every applicable regional resource -- network load balancers, protocol forwarding, VMs with public IPs -- in one region at once, by attaching a Cloud Armor network security policy to it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own alpha network edge security service"})

    		ubx.Resource(compute.AlphaNetworkEdgeSecurityService, "example", compute.AlphaNetworkEdgeSecurityServiceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AlphaNetworkEdgeSecurityService } from "@ubx/sdk-google/google/compute/alpha_network_edge_security_service";

    export default stack("example", () => {
      intent({ summary: "example own alpha network edge security service" });

      resource(AlphaNetworkEdgeSecurityService, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import AlphaNetworkEdgeSecurityService, AlphaNetworkEdgeSecurityServiceConfig

    def describe():
        ubx.intent("example own alpha network edge security service")

        ubx.resource(AlphaNetworkEdgeSecurityService, "example", AlphaNetworkEdgeSecurityServiceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_alpha_network_edge_security_service called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. A hash of the contents stored in this object. This field is used in optimistic locking. This field will be ignored when inserting a NetworkEdgeSecurityService. An up-to-date fingerprint must be provided in order to update the NetworkEdgeSecurityService, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a NetworkEdgeSecurityService. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output only] Type of the resource. Alwayscompute#networkEdgeSecurityService for NetworkEdgeSecurityServices Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the resource resides. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="security_policy" type="string">
  The resource URL for the network edge security service associated with this network edge security service. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource with the resource id. Optional; if omitted, computed by Google Cloud.
</ResponseField>
