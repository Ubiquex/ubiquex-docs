---
title: "google_compute_snapshot_iam_policy"
description: "Real, generated bindings for `google_compute_snapshot_iam_policy`."
---

`google_compute_snapshot_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/compute"
    )

    cfg := compute.SnapshotIamPolicyConfig{
        Name: "example-name",
        PolicyData: "example",
    }
    ubx.Resource(compute.SnapshotIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SnapshotIamPolicy } from
      "jsr:@ubx/sdk-google/google/compute/snapshot_iam_policy";

    resource(SnapshotIamPolicy, "example", {
      name: "example-name",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        SnapshotIamPolicy,
        SnapshotIamPolicyConfig,
    )

    cfg = SnapshotIamPolicyConfig(
        name="example-name",
        policy_data="example",
    )
    ubx.resource(SnapshotIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_snapshot_iam_policy with name,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
