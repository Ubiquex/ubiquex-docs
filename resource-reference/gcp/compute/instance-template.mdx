---
title: "google_compute_instance_template"
description: "`google_compute_instance_template` is a real, reusable specification for creating instances -- machine type, boot disk..."
---

`google_compute_instance_template` is a real, reusable specification for creating instances -- machine type, boot disk, network settings -- that a managed instance group uses to stamp out every instance in the group identically.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own instance template"})

    		ubx.Resource(compute.InstanceTemplate, "example", compute.InstanceTemplateConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceTemplate } from "@ubx/sdk-google/google/compute/instance_template";

    export default stack("example", () => {
      intent({ summary: "example own instance template" });

      resource(InstanceTemplate, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import InstanceTemplate, InstanceTemplateConfig

    def describe():
        ubx.intent("example own instance template")

        ubx.resource(InstanceTemplate, "example", InstanceTemplateConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_instance_template called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] The creation timestamp for this instance template inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] A unique identifier for this instance template. The server defines this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="properties" type="object">
  The instance properties defined by the template, containing configuration such as machine type, disks, network interfaces, and metadata. This field is computed and output-only. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="advanced_machine_features" type="object">
      Specifies options for controlling advanced machine features. Options that would traditionally be configured in a BIOS belong here. Features that require operating system support may have corresponding entries in the GuestOsFeatures of anImage (e.g., whether or not the OS in theImage supports nested virtualization being enabled or disabled). Optional.
      <Expandable title="properties">
        <ResponseField name="enable_nested_virtualization" type="bool">
          Whether to enable nested virtualization or not (default is false). Optional.
        </ResponseField>
        <ResponseField name="enable_uefi_networking" type="bool">
          Whether to enable UEFI networking for instance creation. Optional.
        </ResponseField>
        <ResponseField name="performance_monitoring_unit" type="string">
          Type of Performance Monitoring Unit requested on instance. Optional.
        </ResponseField>
        <ResponseField name="threads_per_core" type="number">
          The number of threads per physical core. To disable simultaneous multithreading (SMT) set this to 1. If unset, the maximum number of threads supported per core by the underlying processor is assumed. Optional.
        </ResponseField>
        <ResponseField name="turbo_mode" type="string">
          Turbo frequency mode to use for the instance. Supported modes include: * ALL_CORE_MAX Using empty string or not setting this field will use the platform-specific default turbo mode. Optional.
        </ResponseField>
        <ResponseField name="visible_core_count" type="number">
          The number of physical cores to expose to an instance. Multiply by the number of threads per core to compute the total number of virtual CPUs to expose to the instance. If unset, the number of cores is inferred from the instance's nominal CPU count and the underlying platform's SMT width. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="can_ip_forward" type="bool">
      Enables instances created based on these properties to send packets with source IP addresses other than their own and receive packets with destination IP addresses other than their own. If these instances will be used as an IP gateway or it will be set as the next-hop in a Route resource, specify true. If unsure, leave this set tofalse. See theEnable IP forwarding documentation for more information. Optional.
    </ResponseField>
    <ResponseField name="confidential_instance_config" type="object">
      A set of Confidential Instance options. Optional.
      <Expandable title="properties">
        <ResponseField name="confidential_instance_type" type="string">
          Defines the type of technology used by the confidential instance. Optional.
        </ResponseField>
        <ResponseField name="enable_confidential_compute" type="bool">
          Defines whether the instance should have confidential compute enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      An optional text description for the instances that are created from these properties. Optional.
    </ResponseField>
    <ResponseField name="disks" type="list(object)">
      An array of disks that are associated with the instances that are created from these properties. Optional.
      <Expandable title="properties">
        <ResponseField name="architecture" type="string">
          Specifies the CPU architecture of the disk, used for the boot image. Allowed values are ARCHITECTURE_UNSPECIFIED (default), ARM64, and X86_64. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="auto_delete" type="bool">
          Indicates whether the disk should be automatically deleted when the instance is deleted. Defaults to false if not specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="boot" type="bool">
          Whether this disk is the boot disk for the instance. Only one disk can be marked as boot. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="device_name" type="string">
          The name of the disk device as exposed to the guest operating system. This can be used to reference the disk inside the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_encryption_key" type="object">
          The customer-supplied encryption key for the disk. This object configures how the disk is encrypted, typically using a raw key or a Cloud KMS key. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key_name" type="string">
              The fully qualified Cloud KMS key name used to encrypt the disk, such as projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/keyRings/&#123;&#123;key_ring&#125;&#125;/cryptoKeys/&#123;&#123;key&#125;&#125;. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kms_key_service_account" type="string">
              The service account used to access the Cloud KMS key for disk encryption. If not specified, the Compute Engine Service Agent is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="raw_key" type="string">
              The raw customer-supplied encryption key, a base64-encoded 256-bit key, used to encrypt the disk. This key must be kept secret and is not retrievable after creation. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rsa_encrypted_key" type="string">
              The RSA-encrypted customer-supplied encryption key, base64-encoded. This key is used to encrypt the disk and must be a 256-bit key encrypted with Google's public RSA key. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sha256" type="string">
              The SHA256 hash of the raw disk encryption key. This is used to verify the key integrity and is often computed by Google Cloud when a raw_key is provided, but it can also be supplied to match a specific key. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disk_size_gb" type="string">
          The size of the disk in gigabytes, specified as a string (e.g., "10"). If omitted, the size is taken from the source image or snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="force_attach" type="bool">
          If set to true, the disk will be attached even if it is currently in use by another instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="guest_os_features" type="list(object)">
          List of features to enable for the guest operating system on this disk. Each object in the list specifies a feature via its `type` field, such as `UEFI_COMPATIBLE` or `VIRTIO_SCSI_MULTIQUEUE`, which allows the disk to support advanced guest OS capabilities. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              The type of guest OS feature to enable. Allowed values: BARE_METAL_LINUX_COMPATIBLE, CCA_CAPABLE, FEATURE_TYPE_UNSPECIFIED, GVNIC, IDPF, MULTI_IP_SUBNET, SECURE_BOOT, SEV_CAPABLE, SEV_LIVE_MIGRATABLE, SEV_LIVE_MIGRATABLE_V2, SEV_SNP_CAPABLE, SNP_SVSM_CAPABLE, TDX_CAPABLE, UEFI_COMPATIBLE, VIRTIO_SCSI_MULTIQUEUE, WINDOWS. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="index" type="number">
          The zero-based index of this disk. The boot disk must have index 0. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="initialize_params" type="object">
          Specifies the parameters for initializing the disk, such as the source image, disk size, and disk type. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="architecture" type="string">
              The architecture of the disk image to use for the boot volume. Valid values are ARM64 and X86_64; ARCHITECTURE_UNSPECIFIED indicates that the architecture is not set. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              The description of the disk that will be created from these initialization parameters. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_name" type="string">
              Specifies the name of the disk to create. If not provided, a name is automatically generated based on the instance name. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_size_gb" type="string">
              The size of the disk in gigabytes. If not specified, the default size is the size of the source image or snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_type" type="string">
              The type of disk to provision, such as 'pd-standard' or 'pd-ssd'. Can be specified as a full or partial URL to a disk type resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enable_confidential_compute" type="bool">
              Enable Confidential Compute mode for this disk. When enabled, the disk uses a hardware-based secure environment and supports confidential VM features. Defaults to false. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="labels" type="map(string)">
              A set of key-value pairs to label the disk created from this initialization configuration. Labels can be used for organizing and identifying the disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="licenses" type="list(string)">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="on_update_action" type="string">
              Specifies the update action to take when the disk configuration changes. Allowed values are 'RECREATE_DISK', 'RECREATE_DISK_IF_SOURCE_CHANGED', and 'USE_EXISTING_DISK', controlling whether to recreate the disk, recreate only if the source changed, or keep the existing disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="provisioned_iops" type="string">
              Indicates how many IOPS to provision for the disk. This sets the number of I/O operations per second that the disk can handle, relevant for performance optimization of persistent disks. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="provisioned_throughput" type="string">
              The provisioned throughput for the disk, in MB/s, used to configure performance for supported disk types (e.g., Hyperdisk Throughput). Value is provided as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replica_zones" type="list(string)">
              Specifies the zones for a regional persistent disk. The disk is replicated to these zones. Only applicable when creating a regional persistent disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_manager_tags" type="map(string)">
              A map of resource manager tags to apply to the disk. Keys are resource manager tag key IDs (format: tagKeys/1234) and values are resource manager tag value IDs (format: tagValues/5678). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_policies" type="list(string)">
              List of resource policies (e.g., snapshot schedule policies) to attach to the disk. These policies define automated operations such as scheduled snapshots. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_image" type="string">
              The image to use for this disk, specified as a fully-qualified URL, self-link, or name. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_image_encryption_key" type="object">
              The customer-supplied encryption key used to decrypt the source image when creating the disk. This is required if the source image is encrypted with a customer-supplied encryption key. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kms_key_name" type="string">
                  The fully qualified Cloud KMS key name (e.g., projects/PROJECT_ID/locations/LOCATION/keyRings/RING_NAME/cryptoKeys/KEY_NAME) used to encrypt the source image. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kms_key_service_account" type="string">
                  The email address of the service account used to authenticate to the KMS key for encrypting the source image. If omitted, the Compute Engine default service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="raw_key" type="string">
                  The RFC 4648 base64-encoded 256-bit raw key used to decrypt the source image. Required if the source image is protected by a customer-supplied encryption key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rsa_encrypted_key" type="string">
                  The RSA-encrypted 256-bit AES key, base64 encoded, used to decrypt the source image. Either this or `raw_key` must be specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sha256" type="string">
                  The SHA256 hash of the customer-supplied encryption key. This is used to verify the integrity of the key when decrypting the source image. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="source_snapshot" type="string">
              Specifies the source snapshot to use to create the disk. If provided, the disk is initialized from the snapshot. The snapshot can be specified as a full URL, partial URL, or name. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_snapshot_encryption_key" type="object">
              The customer-supplied encryption key of the source snapshot. Required if the source snapshot is protected by a customer-managed encryption key, used to decrypt the snapshot when initializing the disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kms_key_name" type="string">
                  The name of the encryption key that is stored in Google Cloud KMS, used to encrypt the source snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kms_key_service_account" type="string">
                  The service account used to access the Cloud KMS key that encrypts the source snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="raw_key" type="string">
                  The base64-encoded 256-bit customer-supplied encryption key used to encrypt the source snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rsa_encrypted_key" type="string">
                  The base64-encoded RSA-encrypted copy of the customer-supplied encryption key used to decrypt the source snapshot. Either this field or raw_key must be set. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sha256" type="string">
                  The SHA-256 hash of the encryption key used to decrypt the source snapshot. This is used to verify the integrity of the key when using a raw or RSA-encrypted key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="storage_pool" type="string">
              The URL of the storage pool in which the disk is created. When set, the disk is provisioned from the named storage pool, which must exist in the same project and zone as the instance template. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="interface" type="string">
          Specifies the disk interface type. Allowed values are 'SCSI' and 'NVME'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The type of resource. For an attached disk in an instance template, this is always 'compute#attachedDisk'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="licenses" type="list(string)">
          List of license URIs that apply to the disk, typically derived from the source image or snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mode" type="string">
          Specifies the access mode of the disk. Valid values are READ_ONLY (attaches the disk as read-only) and READ_WRITE (attaches the disk with read-write access). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="saved_state" type="string">
          The saved state of the disk when the instance is stopped. Allowed values are DISK_SAVED_STATE_UNSPECIFIED (default) and PRESERVED. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="shielded_instance_initial_state" type="object">
          Sets the initial state for shielded VM features on the boot disk, such as secure boot, vTPM, and integrity monitoring. This object configures which Shielded VM settings are enabled when the disk is used to boot a shielded instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dbs" type="list(object)">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded content of the DB file (e.g., a certificate or key) used in the shielded instance initial state for this disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The file type of the database entry in the shielded instance initial state. Allowed values are BIN, X509, and UNDEFINED. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="dbxs" type="list(object)">
              The list of DBX (database exchange) entries for the shielded instance's UEFI secure boot. These entries define the forbidden database used to revoke untrusted certificates or manifests during integrity verification. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded content of the dbxs file for the shielded instance initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The file type of the DBXS database entry. Valid values are BIN, X509, and UNDEFINED, indicating the encoding format of the file content. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="keks" type="list(object)">
              The Key Exchange Key (KEK) for the shielded instance's initial state. This is a list of objects, each specifying a KEK file's content and file type. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded content of the Key Exchange Key (KEK) to set for the shielded instance's initial state. This is the actual key data. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The file type of the key content. Valid values are BIN (binary format), X509 (X.509 certificate format), and UNDEFINED. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="pk" type="object">
              The Platform Key (PK) of the shielded VM's initial state, which serves as the root of trust for the UEFI secure boot process. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The content of the platform key (PK) certificate for the shielded instance initial state, typically base64-encoded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The format of the public key file. Allowed values are `BIN` (binary DER), `X509` (PEM encoded X.509 certificate), and `UNDEFINED` (unspecified). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source" type="string">
          The name or fully qualified URL of the source disk, image, or snapshot used to create the disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of disk to attach. Valid values are PERSISTENT for a standard persistent disk and SCRATCH for a local SSD. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="guest_accelerators" type="list(object)">
      A list of guest accelerator cards' type and count to use for instances created from these properties. Optional.
      <Expandable title="properties">
        <ResponseField name="accelerator_count" type="number">
          The number of accelerators of the specified type to attach to the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="accelerator_type" type="string">
          The name of the accelerator type to attach, such as 'nvidia-tesla-k80' or 'nvidia-tesla-p100'. Example: 'nvidia-tesla-k80'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="key_revocation_action_type" type="string">
      KeyRevocationActionType of the instance. Supported options are "STOP" and "NONE". The default value is "NONE" if it is not specified. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Labels to apply to instances that are created from these properties. Optional.
    </ResponseField>
    <ResponseField name="local_ssd_encryption_mode" type="string">
      Specifies which method should be used for encrypting the Local SSDs attached to the VM. Optional.
    </ResponseField>
    <ResponseField name="machine_type" type="string">
      The machine type to use for instances that are created from these properties. This field only accepts a machine type name, for example `n2-standard-4`. If you use the machine type full or partial URL, for example `projects/my-l7ilb-project/zones/us-central1-a/machineTypes/n2-standard-4`, the request will result in an `INTERNAL_ERROR`. Optional.
    </ResponseField>
    <ResponseField name="metadata" type="object">
      A metadata key/value entry. Optional.
      <Expandable title="properties">
        <ResponseField name="fingerprint" type="string">
          Specifies a fingerprint for this request, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve the resource. Optional.
        </ResponseField>
        <ResponseField name="items" type="list(object)">
          Array of key/value pairs. The total size of all keys and values must be less than 512 KB. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of the metadata item, used to set custom metadata on the instance template. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The value of the metadata item, corresponding to the metadata key in the instance template's metadata. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kind" type="string">
          Output only. [Output Only] Type of the resource. Always compute#metadata for metadata. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="min_cpu_platform" type="string">
      Minimum cpu/platform to be used by instances. The instance may be scheduled on the specified or newer cpu/platform. Applicable values are the friendly names of CPU platforms, such asminCpuPlatform: "Intel Haswell" orminCpuPlatform: "Intel Sandy Bridge". For more information, read Specifying a Minimum CPU Platform. Optional.
    </ResponseField>
    <ResponseField name="network_interfaces" type="list(object)">
      An array of network access configurations for this interface. Optional.
      <Expandable title="properties">
        <ResponseField name="access_configs" type="list(object)">
          A list of access configurations for the network interface. Each access config assigns a public IP address to the instance, enabling external connectivity, and can include settings such as the NAT IP and network tier. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ipv6" type="string">
              The external IPv6 address assigned to the instance's network interface access config. If omitted, an ephemeral IPv6 address is automatically assigned. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_ipv6_prefix_length" type="number">
              The prefix length of the external IPv6 address assigned to the instance. In Google Cloud, the only supported value is 96. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The type of resource. For an access config, this is always 'compute#accessConfig'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the access configuration. Defaults to 'external-nat' if not specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nat_ip" type="string">
              The external IP address to assign to the instance. If not specified, an ephemeral IP address is automatically assigned. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The network service tier for the access config. Accepted values are FIXED_STANDARD, PREMIUM, STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="public_ptr_domain_name" type="string">
              The public DNS domain name to be used for the reverse DNS (PTR) record of the external IP address associated with this access config. Must be a valid DNS name. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="security_policy" type="string">
              The URL of a Cloud Armor security policy to apply to this access config. This policy filters traffic to the external IP address of the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="set_public_ptr" type="bool">
              If true, a public DNS PTR record is set for the external IP address of this access config, enabling reverse DNS lookup. Defaults to false. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of access configuration. Valid values are DIRECT_IPV6 for direct IPv6 access and ONE_TO_ONE_NAT for static external IP address (NAT). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alias_ip_ranges" type="list(object)">
          Alias IP ranges for this network interface. Each object in the list specifies a CIDR range (ip_cidr_range) and optionally a subnetwork range name (subnetwork_range_name) to assign as an alias IP range. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="ip_cidr_range" type="string">
              The IP CIDR range for this alias IP range, in CIDR notation (e.g., '10.0.0.0/24'). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork_range_name" type="string">
              The name of a secondary IP range in the subnetwork to allocate IP addresses from for this alias IP range. If not specified, the primary IP range of the subnetwork is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alias_ipv6_ranges" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="ip_cidr_range" type="string">
              The IPv6 CIDR range for the alias IP range on the network interface, specified in CIDR notation. This range must lie within the subnetwork's IPv6 address space and should not overlap with other alias ranges. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork_range_name" type="string">
              The name of the subnetwork's secondary IPv6 range to which the alias IPv6 IP address belongs. This must match an existing IPv6 range defined in the subnetwork's secondary IP ranges. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enable_vpc_scoped_dns" type="bool">
          Enables VPC-scoped DNS for this network interface, allowing DNS queries to use the VPC's DNS configuration and policies. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fingerprint" type="string">
          The fingerprint of the network interface configuration, generated by the API and used for optimistic locking. It changes whenever the interface settings are modified. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="igmp_query" type="string">
          Whether IGMP query is enabled on this network interface. Valid values are `IGMP_QUERY_DISABLED` (no IGMP queries) and `IGMP_QUERY_V2` (enable IGMP v2 queries). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="internal_ipv6_prefix_length" type="number">
          The prefix length of the internal IPv6 address assigned to the network interface, such as 64 for a /64 subnetwork. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv6_access_configs" type="list(object)">
          The IPv6 access configurations for this network interface. Each configuration assigns an external IPv6 address to the interface, enabling IPv6 internet access, with only one access config supported per interface. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ipv6" type="string">
              The external IPv6 address for this access configuration. If not specified, an automatically allocated address is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_ipv6_prefix_length" type="number">
              The prefix length of the external IPv6 address assigned to the instance, expressed as an integer (e.g., 96). It defines the IPv6 network prefix and is used together with the external IPv6 address to specify the IPv6 configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The kind of the IPv6 access config. This field is always set to 'compute#accessConfig'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of this IPv6 access configuration. If omitted, the default value 'external-ipv6' is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nat_ip" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The network tier for this IPv6 access config. Valid values are FIXED_STANDARD, PREMIUM, STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="public_ptr_domain_name" type="string">
              The public PTR domain name for the IPv6 address, used for reverse DNS lookup. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="security_policy" type="string">
              The security policy to associate with this IPv6 access configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="set_public_ptr" type="bool">
              Whether to set a public DNS PTR record for the IPv6 address. Set to true to enable. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of IPv6 access configuration. Possible values are DIRECT_IPV6 (direct external IPv6 connectivity) and ONE_TO_ONE_NAT (IPv6 connectivity through one-to-one NAT). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ipv6_access_type" type="string">
          Specifies the access type for IPv6 addresses on this network interface. Set to EXTERNAL for an external IPv6 address, or INTERNAL for an internal IPv6 address. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv6_address" type="string">
          The IPv6 address to assign to the network interface. This is used to specify a static IPv6 address for the interface. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The type of the resource. Always 'compute#networkInterface'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="network" type="string">
          The name or self_link of the VPC network to attach this interface to. Either `network` or `subnetwork` is required; if you specify `subnetwork`, `network` is inferred. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_attachment" type="string">
          URL of the network attachment resource to which this network interface attaches. If specified, it overrides the network and subnetwork fields, and is used for the network attachment type of network interface. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_ip" type="string">
          The private IPv4 address to assign to this network interface. If not set, an ephemeral address is automatically assigned. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="nic_type" type="string">
          Specifies the type of network interface card (NIC) to use for the interface. Allowed values are GVNIC, IDPF, IRDMA, MRDMA, UNSPECIFIED_NIC_TYPE, and VIRTIO_NET. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="parent_nic_name" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="queue_count" type="number">
          The number of queues to use for this network interface. A value of 0 indicates the default number of queues (one per vCPU). The value must be a power of 2 and cannot exceed the instance's vCPU count. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_class_id" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="stack_type" type="string">
          The IP stack type for this network interface. Valid values are IPV4_IPV6 (dual-stack), IPV4_ONLY, and IPV6_ONLY. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnetwork" type="string">
          The name or self-link of the subnetwork to which the network interface is attached. If not specified, the default subnetwork of the specified network is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vlan" type="number">
          Optional; if omitted, computed by GCP.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_performance_config" type="object">
      Configuration block for network performance settings, including the total egress bandwidth tier which can be set to 'DEFAULT' or 'TIER_1'. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="total_egress_bandwidth_tier" type="string">
          The egress bandwidth tier to enable for the instance's network performance configuration. Allowed values are DEFAULT and TIER_1. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_ipv6_google_access" type="string">
      The private IPv6 google access type for VMs. If not specified, use INHERIT_FROM_SUBNETWORK as default. Note that for MachineImage, this is not supported yet. Optional.
    </ResponseField>
    <ResponseField name="reservation_affinity" type="object">
      Specifies the reservations that this instance can consume from. Optional.
      <Expandable title="properties">
        <ResponseField name="consume_reservation_type" type="string">
          Specifies the type of reservation from which this instance can consume resources: ANY_RESERVATION (default),SPECIFIC_RESERVATION, or NO_RESERVATION. See Consuming reserved instances for examples. Optional.
        </ResponseField>
        <ResponseField name="key" type="string">
          Corresponds to the label key of a reservation resource. To target aSPECIFIC_RESERVATION by name, specifygoogleapis.com/reservation-name as the key and specify the name of your reservation as its value. Optional.
        </ResponseField>
        <ResponseField name="values" type="list(string)">
          Corresponds to the label values of a reservation resource. This can be either a name to a reservation in the same project or "projects/different-project/reservations/some-reservation-name" to target a shared reservation in the same zone but in a different project. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the instance. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
    <ResponseField name="resource_policies" type="list(string)">
      Resource policies (names, not URLs) applied to instances created from these properties. Note that for MachineImage, this is not supported yet. Optional.
    </ResponseField>
    <ResponseField name="scheduling" type="object">
      Sets the scheduling options for an Instance. Optional.
      <Expandable title="properties">
        <ResponseField name="automatic_restart" type="bool">
          Specifies whether the instance should be automatically restarted if it is terminated by Compute Engine (not terminated by a user). You can only set the automatic restart option for standard instances.Preemptible instances cannot be automatically restarted. By default, this is set to true so an instance is automatically restarted if it is terminated by Compute Engine. Optional.
        </ResponseField>
        <ResponseField name="availability_domain" type="number">
          Specifies the availability domain to place the instance in. The value must be a number between 1 and the number of availability domains specified in the spread placement policy attached to the instance. Optional.
        </ResponseField>
        <ResponseField name="graceful_shutdown" type="object">
          The configuration for gracefully shutting down the instance. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Opts-in for graceful shutdown. Optional.
            </ResponseField>
            <ResponseField name="max_duration" type="object">
              A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="host_error_timeout_seconds" type="number">
          Specify the time in seconds for host error detection, the value must be within the range of [90, 330] with the increment of 30, if unset, the default behavior of host error recovery will be used. Optional.
        </ResponseField>
        <ResponseField name="instance_termination_action" type="string">
          Specifies the termination action for the instance. Optional.
        </ResponseField>
        <ResponseField name="local_ssd_recovery_timeout" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="location_hint" type="string">
          An opaque location hint used to place the instance close to other resources. This field is for use by internal tools that use the public API. Optional.
        </ResponseField>
        <ResponseField name="max_run_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="min_node_cpus" type="number">
          The minimum number of virtual CPUs this instance will consume when running on a sole-tenant node. Optional.
        </ResponseField>
        <ResponseField name="node_affinities" type="list(object)">
          A set of node affinity and anti-affinity configurations. Refer toConfiguring node affinity for more information. Overrides reservationAffinity. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The label key for the node affinity requirement. This is the key of a node label used to match nodes. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              Specifies the operator for the node affinity expression. Valid values are IN, NOT_IN, and OPERATOR_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              The list of node affinity values. These values are matched against the key and operator to determine the scheduling constraint, e.g., the node group names when key is 'node-group-name'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="on_host_maintenance" type="string">
          Defines the maintenance behavior for this instance. For standard instances, the default behavior is MIGRATE. Forpreemptible instances, the default and only possible behavior is TERMINATE. For more information, see Set VM host maintenance policy. Optional.
        </ResponseField>
        <ResponseField name="on_instance_stop_action" type="object">
          Defines the behaviour for instances with the instance_termination_actionSTOP. Optional.
          <Expandable title="properties">
            <ResponseField name="discard_local_ssd" type="bool">
              If true, the contents of any attached Local SSD disks will be discarded else, the Local SSD data will be preserved when the instance is stopped at the end of the run duration/termination time. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="preemptible" type="bool">
          Defines whether the instance is preemptible. This can only be set during instance creation or while the instance isstopped and therefore, in a `TERMINATED` state. SeeInstance Life Cycle for more information on the possible instance states. Optional.
        </ResponseField>
        <ResponseField name="preemption_notice_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="provisioning_model" type="string">
          Specifies the provisioning model of the instance. Optional.
        </ResponseField>
        <ResponseField name="skip_guest_os_shutdown" type="bool">
          Default is false and there will be 120 seconds between GCE ACPI G2 Soft Off and ACPI G3 Mechanical Off for Standard VMs and 30 seconds for Spot VMs. Optional.
        </ResponseField>
        <ResponseField name="termination_time" type="string">
          Specifies the timestamp, when the instance will be terminated, inRFC3339 text format. If specified, the instance termination action will be performed at the termination time. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_accounts" type="list(object)">
      A list of service accounts with specified scopes. Access tokens for these service accounts are available to the instances that are created from these properties. Use metadata queries to obtain the access tokens for these instances. Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          The email address of the service account to associate with the instance. If not provided, the default Compute Engine service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scopes" type="list(string)">
          A list of OAuth scopes to associate with the service account for the instance. For example, 'cloud-platform' gives full access, while 'storage-ro' grants read-only access to Cloud Storage. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="shielded_instance_config" type="object">
      A set of Shielded Instance options. Optional.
      <Expandable title="properties">
        <ResponseField name="enable_integrity_monitoring" type="bool">
          Defines whether the instance has integrity monitoring enabled.Enabled by default. Optional.
        </ResponseField>
        <ResponseField name="enable_secure_boot" type="bool">
          Defines whether the instance has Secure Boot enabled.Disabled by default. Optional.
        </ResponseField>
        <ResponseField name="enable_vtpm" type="bool">
          Defines whether the instance has the vTPM enabled.Enabled by default. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="object">
      A set of instance tags. Optional.
      <Expandable title="properties">
        <ResponseField name="fingerprint" type="string">
          Specifies a fingerprint for this request, which is essentially a hash of the tags' contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update tags. You must always provide an up-to-date fingerprint hash in order to update or change tags. To see the latest fingerprint, make get() request to the instance. Optional.
        </ResponseField>
        <ResponseField name="items" type="list(string)">
          An array of tags. Each tag must be 1-63 characters long, and comply with RFC1035. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="workload_identity_config" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="identity" type="string">
          The service account email to use as the workload identity for the instance. If omitted, the default Compute Engine service account is used. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="identity_certificate_enabled" type="bool">
          Whether the identity certificate is enabled for this workload identity configuration. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the instance template resides. Only applicable for regional resources. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] The URL for this instance template. The server defines this URL. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instance" type="string">
  The source instance used to create the template. You can provide this as a partial or full URL to the resource. For example, the following are valid values: - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance - projects/project/zones/zone/instances/instance Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instance_params" type="object">
  A specification of the parameters to use when creating the instance template from a source instance. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="disk_configs" type="list(object)">
      Attached disks configuration. If not provided, defaults are applied: For boot disk and any other R/W disks, the source images for each disk will be used. For read-only disks, they will be attached in read-only mode. Local SSD disks will be created as blank volumes. Optional.
      <Expandable title="properties">
        <ResponseField name="auto_delete" type="bool">
          Whether the disk will be auto-deleted when the instance is deleted. When set in source_instance_params.disk_configs, this overrides the corresponding property from the source instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="custom_image" type="string">
          The custom image to use for this disk when creating the instance template from a source instance. If specified, this overrides the image of the corresponding source disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="device_name" type="string">
          The device name of the disk in the source instance to which this disk configuration applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instantiate_from" type="string">
          Specifies how the disk is created from the source instance's disk. Valid values are ATTACH_READ_ONLY (attach read-only), BLANK (create a blank disk), CUSTOM_IMAGE (create from a custom image), DEFAULT (use default behavior), DO_NOT_INCLUDE (exclude the disk), SOURCE_IMAGE (create from the source image), and SOURCE_IMAGE_FAMILY (create from the source image family). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
