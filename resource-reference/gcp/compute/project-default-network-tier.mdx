---
title: "google_compute_project_default_network_tier"
description: "Real, generated bindings for `google_compute_project_default_network_tier`."
---

`google_compute_project_default_network_tier` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    cfg := compute.ProjectDefaultNetworkTierConfig{
        NetworkTier: "example",
    }
    ubx.Resource(compute.ProjectDefaultNetworkTier, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProjectDefaultNetworkTier } from
      "jsr:@ubx/sdk-google/google/compute/project_default_network_tier";

    resource(ProjectDefaultNetworkTier, "example", {
      networkTier: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        ProjectDefaultNetworkTier,
        ProjectDefaultNetworkTierConfig,
    )

    cfg = ProjectDefaultNetworkTierConfig(
        network_tier="example",
    )
    ubx.resource(ProjectDefaultNetworkTier, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_project_default_network_tier with
network_tier.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network_tier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
