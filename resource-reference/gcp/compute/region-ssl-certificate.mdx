---
title: "google_compute_region_ssl_certificate"
description: "Real, generated bindings for `google_compute_region_ssl_certificate`."
---

`google_compute_region_ssl_certificate` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region ssl certificate"})

    		ubx.Resource(compute.RegionSslCertificate, "ci-runner", compute.RegionSslCertificateConfig{
    			Certificate:         "example",
    			Name:                "ci-runner",
    			Description:         "Managed by ubx.",
    			PrivateKey:          "example",
    			PrivateKeyWo:        "example",
    			PrivateKeyWoVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionSslCertificate } from "jsr:@ubx/sdk-google/google/compute/region_ssl_certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own region ssl certificate" });

      resource(RegionSslCertificate, "ci-runner", {
        certificate: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        privateKey: "example",
        privateKeyWo: "example",
        privateKeyWoVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RegionSslCertificate, RegionSslCertificateConfig

    def describe():
        ubx.intent("payments own region ssl certificate")

        ubx.resource(RegionSslCertificate, "ci-runner", RegionSslCertificateConfig(
            certificate="example",
            name="ci-runner",
            description="Managed by ubx.",
            private_key="example",
            private_key_wo="example",
            private_key_wo_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_ssl_certificate called "ci-runner",
    with certificate example, description Managed by ubx., private
    key example, private key wo example, private key wo version
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="private_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="private_key_wo" type="string">
  Optional.
</ResponseField>

<ResponseField name="private_key_wo_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="certificate_id" type="number">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
