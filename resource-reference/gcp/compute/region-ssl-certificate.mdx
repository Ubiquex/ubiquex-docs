---
title: "google_compute_region_ssl_certificate"
description: "Real, generated bindings for `google_compute_region_ssl_certificate`."
---

`google_compute_region_ssl_certificate` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region ssl certificate"})

    		ubx.Resource(compute.RegionSslCertificate, "ci-runner", compute.RegionSslCertificateConfig{
    			Certificate:         "example",
    			Name:                "ci-runner",
    			Description:         "Managed by ubx.",
    			PrivateKey:          "example",
    			PrivateKeyWo:        "example",
    			PrivateKeyWoVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionSslCertificate } from "jsr:@ubx/sdk-google/google/compute/region_ssl_certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own region ssl certificate" });

      resource(RegionSslCertificate, "ci-runner", {
        certificate: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        privateKey: "example",
        privateKeyWo: "example",
        privateKeyWoVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RegionSslCertificate, RegionSslCertificateConfig

    def describe():
        ubx.intent("payments own region ssl certificate")

        ubx.resource(RegionSslCertificate, "ci-runner", RegionSslCertificateConfig(
            certificate="example",
            name="ci-runner",
            description="Managed by ubx.",
            private_key="example",
            private_key_wo="example",
            private_key_wo_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_ssl_certificate called "ci-runner",
    with certificate example, description Managed by ubx., private
    key example, private key wo example, private key wo version
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate" type="string" required>
  The certificate in PEM format. The certificate chain must be no greater than 5 certs long. The chain must include at least one intermediate cert. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. These are in the same namespace as the managed SSL certificates. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Creates a unique name beginning with the specified prefix. Conflicts with name. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="private_key" type="string">
  The write-only private key in PEM format. Optional.
</ResponseField>

<ResponseField name="private_key_wo" type="string">
  The write-only private key in PEM format. Optional.
</ResponseField>

<ResponseField name="private_key_wo_version" type="string">
  Triggers update of 'private_key_wo' write-only. Increment this value when an update to 'private_key_wo' is needed. For more info see [updating write-only arguments](/docs/providers/google/guides/using_write_only_arguments.html#updating-write-only-arguments) Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The Region in which the created regional ssl certificate should reside. If it is not provided, the provider region is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="certificate_id" type="number">
  The unique identifier for the resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Creation timestamp in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Expire time of the certificate in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. These are in the same namespace as the managed SSL certificates. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Creates a unique name beginning with the specified prefix. Conflicts with name. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The Region in which the created regional ssl certificate should reside. If it is not provided, the provider region is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
