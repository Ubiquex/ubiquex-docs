---
title: "google_compute_router_nat"
description: "Real, generated bindings for `google_compute_router_nat`."
---

`google_compute_router_nat` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own router nat"})

    		ubx.Resource(compute.RouterNat, "ci-runner", compute.RouterNatConfig{
    			Name:                            "ci-runner",
    			Router:                          "example",
    			SourceSubnetworkIpRangesToNat:   "example",
    			IcmpIdleTimeoutSec:              1,
    			InitialNatIps:                   []string{"example"},
    			MaxPortsPerVm:                   1,
    			NatIpAllocateOption:             "example",
    			SourceSubnetworkIpRangesToNat64: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RouterNat } from "jsr:@ubx/sdk-google/google/compute/router_nat";

    export default stack("payments", () => {
      intent({ summary: "payments own router nat" });

      resource(RouterNat, "ci-runner", {
        name: "ci-runner",
        router: "example",
        sourceSubnetworkIpRangesToNat: "example",
        icmpIdleTimeoutSec: 1,
        initialNatIps: ["example"],
        maxPortsPerVm: 1,
        natIpAllocateOption: "example",
        sourceSubnetworkIpRangesToNat64: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RouterNat, RouterNatConfig

    def describe():
        ubx.intent("payments own router nat")

        ubx.resource(RouterNat, "ci-runner", RouterNatConfig(
            name="ci-runner",
            router="example",
            source_subnetwork_ip_ranges_to_nat="example",
            icmp_idle_timeout_sec=1,
            initial_nat_ips=["example"],
            max_ports_per_vm=1,
            nat_ip_allocate_option="example",
            source_subnetwork_ip_ranges_to_nat64="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_router_nat called "ci-runner", with router
    example, source subnetwork ip ranges to nat example, icmp idle
    timeout sec 1, initial nat ips a set value, max ports per vm 1,
    nat ip allocate option example, source subnetwork ip ranges to
    nat64 example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_network_tier" type="string">
  The network tier to use when automatically reserving NAT IP addresses. Must be one of: PREMIUM, STANDARD. If not specified, then the current project-level default tier is used. Possible values: ["PREMIUM", "STANDARD"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="drain_nat_ips" type="set(string)">
  A list of URLs of the IP resources to be drained. These IPs must be valid static external IPs that have been assigned to the NAT. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_dynamic_port_allocation" type="bool">
  Enable Dynamic Port Allocation. If minPortsPerVm is set, minPortsPerVm must be set to a power of two greater than or equal to 32. If minPortsPerVm is not set, a minimum of 32 ports will be allocated to a VM from this NAT config. If maxPortsPerVm is set, maxPortsPerVm must be set to a power of two greater than minPortsPerVm. If maxPortsPerVm is not set, a maximum of 65536 ports will be allocated to a VM from this NAT config. Mutually exclusive with enableEndpointIndependentMapping. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_endpoint_independent_mapping" type="bool">
  Enable endpoint independent mapping. For more information see the [official documentation](https://docs.cloud.google.com/nat/docs/public-nat#specs-rfcs). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="endpoint_types" type="list(string)">
  Specifies the endpoint Types supported by the NAT Gateway. Supported values include: 'ENDPOINT_TYPE_VM', 'ENDPOINT_TYPE_SWG', 'ENDPOINT_TYPE_MANAGED_PROXY_LB'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="icmp_idle_timeout_sec" type="number">
  Timeout (in seconds) for ICMP connections. Defaults to 30s if not set. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="initial_nat_ips" type="set(string)">
  Self-links of NAT IPs to be used as initial value for creation alongside a RouterNatAddress resource. Conflicts with natIps and drainNatIps. Only valid if natIpAllocateOption is set to MANUAL_ONLY. Optional.
</ResponseField>

<ResponseField name="log_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable" type="bool" required>
    </ResponseField>
    <ResponseField name="filter" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_ports_per_vm" type="number">
  Maximum number of ports allocated to a VM from this NAT. This field can only be set when enableDynamicPortAllocation is enabled. Optional.
</ResponseField>

<ResponseField name="min_ports_per_vm" type="number">
  Minimum number of ports allocated to a VM from this NAT. Defaults to 64 for static port allocation and 32 dynamic port allocation if not set. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the NAT service. The name must be 1-63 characters long and comply with RFC1035. Required.
</ResponseField>

<ResponseField name="nat64_subnetwork" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="nat_ip_allocate_option" type="string">
  How external IPs should be allocated for this NAT. Valid values are 'AUTO_ONLY' for only allowing NAT IPs allocated by Google Cloud Platform, or 'MANUAL_ONLY' for only user-allocated NAT IP addresses. Possible values: ["MANUAL_ONLY", "AUTO_ONLY"] Optional.
</ResponseField>

<ResponseField name="nat_ips" type="set(string)">
  Self-links of NAT IPs. Only valid if natIpAllocateOption is set to MANUAL_ONLY. If this field is used alongside with a count created list of address resources 'google_compute_address.foobar.*.self_link', the access level resource for the address resource must have a 'lifecycle' block with 'create_before_destroy = true' so the number of resources can be increased/decreased without triggering the 'resourceInUseByAnotherResource' error. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Region where the router and NAT reside. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="router" type="string" required>
  The name of the Cloud Router in which this NAT will be configured. Required.
</ResponseField>

<ResponseField name="rules" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="action" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="source_nat_active_ips" type="set(string)">
        </ResponseField>
        <ResponseField name="source_nat_active_ranges" type="set(string)">
        </ResponseField>
        <ResponseField name="source_nat_drain_ips" type="set(string)">
        </ResponseField>
        <ResponseField name="source_nat_drain_ranges" type="set(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="match" type="string" required>
    </ResponseField>
    <ResponseField name="rule_number" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_subnetwork_ip_ranges_to_nat" type="string" required>
  How NAT should be configured per Subnetwork. If 'ALL_SUBNETWORKS_ALL_IP_RANGES', all of the IP ranges in every Subnetwork are allowed to Nat. If 'ALL_SUBNETWORKS_ALL_PRIMARY_IP_RANGES', all of the primary IP ranges in every Subnetwork are allowed to Nat. 'LIST_OF_SUBNETWORKS': A list of Subnetworks are allowed to Nat (specified in the field subnetwork below). Note that if this field contains ALL_SUBNETWORKS_ALL_IP_RANGES or ALL_SUBNETWORKS_ALL_PRIMARY_IP_RANGES, then there should not be any other RouterNat section in any Router for this network in this region. Possible values: ["ALL_SUBNETWORKS_ALL_IP_RANGES", "ALL_SUBNETWORKS_ALL_PRIMARY_IP_RANGES", "LIST_OF_SUBNETWORKS"] Required.
</ResponseField>

<ResponseField name="source_subnetwork_ip_ranges_to_nat64" type="string">
  Specify the Nat option for NAT64, which can take one of the following values: ALL_IPV6_SUBNETWORKS: All of the IP ranges in every Subnetwork are allowed to Nat. LIST_OF_IPV6_SUBNETWORKS: A list of Subnetworks are allowed to Nat (specified in the field nat64Subnetwork below). Note that if this field contains NAT64_ALL_V6_SUBNETWORKS no other Router.Nat section in this region can also enable NAT64 for any Subnetworks in this network. Other Router.Nat sections can still be present to enable NAT44 only. Possible values: ["ALL_IPV6_SUBNETWORKS", "LIST_OF_IPV6_SUBNETWORKS"] Optional.
</ResponseField>

<ResponseField name="subnetwork" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="secondary_ip_range_names" type="set(string)">
    </ResponseField>
    <ResponseField name="source_ip_ranges_to_nat" type="set(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tcp_established_idle_timeout_sec" type="number">
  Timeout (in seconds) for TCP established connections. Defaults to 1200s if not set. Optional.
</ResponseField>

<ResponseField name="tcp_time_wait_timeout_sec" type="number">
  Timeout (in seconds) for TCP connections that are in TIME_WAIT state. Defaults to 120s if not set. Optional.
</ResponseField>

<ResponseField name="tcp_transitory_idle_timeout_sec" type="number">
  Timeout (in seconds) for TCP transitory connections. Defaults to 30s if not set. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Indicates whether this NAT is used for public or private IP translation. If unspecified, it defaults to PUBLIC. If 'PUBLIC' NAT used for public IP translation. If 'PRIVATE' NAT used for private IP translation. Default value: "PUBLIC" Possible values: ["PUBLIC", "PRIVATE"] Optional.
</ResponseField>

<ResponseField name="udp_idle_timeout_sec" type="number">
  Timeout (in seconds) for UDP connections. Defaults to 30s if not set. Optional.
</ResponseField>

## Output properties

<ResponseField name="auto_network_tier" type="string">
  The network tier to use when automatically reserving NAT IP addresses. Must be one of: PREMIUM, STANDARD. If not specified, then the current project-level default tier is used. Possible values: ["PREMIUM", "STANDARD"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="drain_nat_ips" type="set(string)">
  A list of URLs of the IP resources to be drained. These IPs must be valid static external IPs that have been assigned to the NAT. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_dynamic_port_allocation" type="bool">
  Enable Dynamic Port Allocation. If minPortsPerVm is set, minPortsPerVm must be set to a power of two greater than or equal to 32. If minPortsPerVm is not set, a minimum of 32 ports will be allocated to a VM from this NAT config. If maxPortsPerVm is set, maxPortsPerVm must be set to a power of two greater than minPortsPerVm. If maxPortsPerVm is not set, a maximum of 65536 ports will be allocated to a VM from this NAT config. Mutually exclusive with enableEndpointIndependentMapping. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_endpoint_independent_mapping" type="bool">
  Enable endpoint independent mapping. For more information see the [official documentation](https://docs.cloud.google.com/nat/docs/public-nat#specs-rfcs). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="endpoint_types" type="list(string)">
  Specifies the endpoint Types supported by the NAT Gateway. Supported values include: 'ENDPOINT_TYPE_VM', 'ENDPOINT_TYPE_SWG', 'ENDPOINT_TYPE_MANAGED_PROXY_LB'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="log_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable" type="bool" required>
    </ResponseField>
    <ResponseField name="filter" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="min_ports_per_vm" type="number">
  Minimum number of ports allocated to a VM from this NAT. Defaults to 64 for static port allocation and 32 dynamic port allocation if not set. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="nat64_subnetwork" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="nat_ips" type="set(string)">
  Self-links of NAT IPs. Only valid if natIpAllocateOption is set to MANUAL_ONLY. If this field is used alongside with a count created list of address resources 'google_compute_address.foobar.*.self_link', the access level resource for the address resource must have a 'lifecycle' block with 'create_before_destroy = true' so the number of resources can be increased/decreased without triggering the 'resourceInUseByAnotherResource' error. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Region where the router and NAT reside. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="rules" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="action" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="source_nat_active_ips" type="set(string)">
        </ResponseField>
        <ResponseField name="source_nat_active_ranges" type="set(string)">
        </ResponseField>
        <ResponseField name="source_nat_drain_ips" type="set(string)">
        </ResponseField>
        <ResponseField name="source_nat_drain_ranges" type="set(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="match" type="string" required>
    </ResponseField>
    <ResponseField name="rule_number" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="subnetwork" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="secondary_ip_range_names" type="set(string)">
    </ResponseField>
    <ResponseField name="source_ip_ranges_to_nat" type="set(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
