---
title: "google_compute_organization_security_policy_association"
description: "Real, generated bindings for `google_compute_organization_security_policy_association`."
---

`google_compute_organization_security_policy_association` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organization security policy association"})

    		ubx.Resource(compute.OrganizationSecurityPolicyAssociation, "ci-runner", compute.OrganizationSecurityPolicyAssociationConfig{
    			AttachmentId:     "example",
    			Name:             "ci-runner",
    			PolicyId:         "example",
    			ExcludedFolders:  []string{"example"},
    			ExcludedProjects: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationSecurityPolicyAssociation } from "jsr:@ubx/sdk-google/google/compute/organization_security_policy_association";

    export default stack("payments", () => {
      intent({ summary: "payments own organization security policy association" });

      resource(OrganizationSecurityPolicyAssociation, "ci-runner", {
        attachmentId: "example",
        name: "ci-runner",
        policyId: "example",
        excludedFolders: ["example"],
        excludedProjects: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import OrganizationSecurityPolicyAssociation, OrganizationSecurityPolicyAssociationConfig

    def describe():
        ubx.intent("payments own organization security policy association")

        ubx.resource(OrganizationSecurityPolicyAssociation, "ci-runner", OrganizationSecurityPolicyAssociationConfig(
            attachment_id="example",
            name="ci-runner",
            policy_id="example",
            excluded_folders=["example"],
            excluded_projects=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_organization_security_policy_association
    called "ci-runner", with attachment id example, policy id
    example, excluded folders a set value, excluded projects a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attachment_id" type="string" required>
  The resource that the security policy is attached to. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="excluded_folders" type="list(string)">
  A list of folders to exclude from the security policy. Optional.
</ResponseField>

<ResponseField name="excluded_projects" type="list(string)">
  A list of projects to exclude from the security policy. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name for an association. Required.
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  The security policy ID of the association. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name of the security policy of the association. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
