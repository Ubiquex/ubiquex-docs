---
title: "google_compute_http_health_check"
description: "Real, generated bindings for `google_compute_http_health_check`."
---

`google_compute_http_health_check` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own http health check"})

    		ubx.Resource(compute.HttpHealthCheck, "ci-runner", compute.HttpHealthCheckConfig{
    			Name:             "ci-runner",
    			CheckIntervalSec: 1,
    			Description:      "Managed by ubx.",
    			HealthyThreshold: 1,
    			Host:             "example",
    			Port:             1,
    			RequestPath:      "example",
    			TimeoutSec:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HttpHealthCheck } from "jsr:@ubx/sdk-google/google/compute/http_health_check";

    export default stack("payments", () => {
      intent({ summary: "payments own http health check" });

      resource(HttpHealthCheck, "ci-runner", {
        name: "ci-runner",
        checkIntervalSec: 1,
        description: "Managed by ubx.",
        healthyThreshold: 1,
        host: "example",
        port: 1,
        requestPath: "example",
        timeoutSec: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import HttpHealthCheck, HttpHealthCheckConfig

    def describe():
        ubx.intent("payments own http health check")

        ubx.resource(HttpHealthCheck, "ci-runner", HttpHealthCheckConfig(
            name="ci-runner",
            check_interval_sec=1,
            description="Managed by ubx.",
            healthy_threshold=1,
            host="example",
            port=1,
            request_path="example",
            timeout_sec=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_http_health_check called "ci-runner", with
    check interval sec 1, description Managed by ubx., healthy
    threshold 1, host example, port 1, request path example, timeout
    sec 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="check_interval_sec" type="number">
  How often (in seconds) to send a health check. The default value is 5 seconds. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional.
</ResponseField>

<ResponseField name="healthy_threshold" type="number">
  A so-far unhealthy instance will be marked healthy after this many consecutive successes. The default value is 2. Optional.
</ResponseField>

<ResponseField name="host" type="string">
  The value of the host header in the HTTP health check request. If left empty (default value), the public IP on behalf of which this health check is performed will be used. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Required.
</ResponseField>

<ResponseField name="port" type="number">
  The TCP port number for the HTTP health check request. The default value is 80. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="request_path" type="string">
  The request path of the HTTP health check request. The default value is /. Optional.
</ResponseField>

<ResponseField name="timeout_sec" type="number">
  How long (in seconds) to wait before claiming failure. The default value is 5 seconds. It is invalid for timeoutSec to have greater value than checkIntervalSec. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="unhealthy_threshold" type="number">
  A so-far healthy instance will be marked unhealthy after this many consecutive failures. The default value is 2. Optional.
</ResponseField>

## Output properties

<ResponseField name="creation_timestamp" type="string">
  Creation timestamp in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
