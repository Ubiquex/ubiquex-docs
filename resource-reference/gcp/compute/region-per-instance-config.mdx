---
title: "google_compute_region_per_instance_config"
description: "Real, generated bindings for `google_compute_region_per_instance_config`."
---

`google_compute_region_per_instance_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region per instance config"})

    		ubx.Resource(compute.RegionPerInstanceConfig, "ci-runner", compute.RegionPerInstanceConfigConfig{
    			Name:                         "ci-runner",
    			RegionInstanceGroupManager:   "example",
    			MinimalAction:                "example",
    			MostDisruptiveAllowedAction:  "example",
    			RemoveInstanceOnDestroy:      true,
    			RemoveInstanceStateOnDestroy: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionPerInstanceConfig } from "jsr:@ubx/sdk-google/google/compute/region_per_instance_config";

    export default stack("payments", () => {
      intent({ summary: "payments own region per instance config" });

      resource(RegionPerInstanceConfig, "ci-runner", {
        name: "ci-runner",
        regionInstanceGroupManager: "example",
        minimalAction: "example",
        mostDisruptiveAllowedAction: "example",
        removeInstanceOnDestroy: true,
        removeInstanceStateOnDestroy: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RegionPerInstanceConfig, RegionPerInstanceConfigConfig

    def describe():
        ubx.intent("payments own region per instance config")

        ubx.resource(RegionPerInstanceConfig, "ci-runner", RegionPerInstanceConfigConfig(
            name="ci-runner",
            region_instance_group_manager="example",
            minimal_action="example",
            most_disruptive_allowed_action="example",
            remove_instance_on_destroy=True,
            remove_instance_state_on_destroy=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_per_instance_config called
    "ci-runner", with region instance group manager example, minimal
    action example, most disruptive allowed action example, remove
    instance on destroy true, remove instance state on destroy true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="minimal_action" type="string">
  The minimal action to perform on the instance during an update. Default is 'NONE'. Possible values are: * REPLACE * RESTART * REFRESH * NONE Optional.
</ResponseField>

<ResponseField name="most_disruptive_allowed_action" type="string">
  The most disruptive action to perform on the instance during an update. Default is 'REPLACE'. Possible values are: * REPLACE * RESTART * REFRESH * NONE Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name for this per-instance config and its corresponding instance. Required.
</ResponseField>

<ResponseField name="preserved_state" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="disk" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="delete_rule" type="string">
        </ResponseField>
        <ResponseField name="device_name" type="string" required>
        </ResponseField>
        <ResponseField name="mode" type="string">
        </ResponseField>
        <ResponseField name="source" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="external_ip" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="auto_delete" type="string">
        </ResponseField>
        <ResponseField name="interface_name" type="string" required>
        </ResponseField>
        <ResponseField name="ip_address" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="address" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="internal_ip" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="auto_delete" type="string">
        </ResponseField>
        <ResponseField name="interface_name" type="string" required>
        </ResponseField>
        <ResponseField name="ip_address" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="address" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="metadata" type="map(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Region where the containing instance group manager is located Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region_instance_group_manager" type="string" required>
  The region instance group manager this instance config is part of. Required.
</ResponseField>

<ResponseField name="remove_instance_on_destroy" type="bool">
  When true, deleting this config will immediately remove the underlying instance. When false, deleting this config will use the behavior as determined by remove_instance_on_destroy. Optional.
</ResponseField>

<ResponseField name="remove_instance_state_on_destroy" type="bool">
  When true, deleting this config will immediately remove any specified state from the underlying instance. When false, deleting this config will *not* immediately remove any state from the underlying instance. State will be removed on the next instance recreation or update. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="preserved_state" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="disk" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="delete_rule" type="string">
        </ResponseField>
        <ResponseField name="device_name" type="string" required>
        </ResponseField>
        <ResponseField name="mode" type="string">
        </ResponseField>
        <ResponseField name="source" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="external_ip" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="auto_delete" type="string">
        </ResponseField>
        <ResponseField name="interface_name" type="string" required>
        </ResponseField>
        <ResponseField name="ip_address" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="address" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="internal_ip" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="auto_delete" type="string">
        </ResponseField>
        <ResponseField name="interface_name" type="string" required>
        </ResponseField>
        <ResponseField name="ip_address" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="address" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="metadata" type="map(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Region where the containing instance group manager is located Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
