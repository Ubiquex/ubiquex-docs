---
title: "google_compute_public_delegated_prefix"
description: "Real, generated bindings for `google_compute_public_delegated_prefix`."
---

`google_compute_public_delegated_prefix` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own public delegated prefix"})

    		ubx.Resource(compute.PublicDelegatedPrefix, "ci-runner", compute.PublicDelegatedPrefixConfig{
    			IpCidrRange:     "example",
    			Name:            "ci-runner",
    			ParentPrefix:    "example",
    			Region:          "example",
    			Description:     "Managed by ubx.",
    			IsLiveMigration: true,
    			Mode:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PublicDelegatedPrefix } from "jsr:@ubx/sdk-google/google/compute/public_delegated_prefix";

    export default stack("payments", () => {
      intent({ summary: "payments own public delegated prefix" });

      resource(PublicDelegatedPrefix, "ci-runner", {
        ipCidrRange: "example",
        name: "ci-runner",
        parentPrefix: "example",
        region: "example",
        description: "Managed by ubx.",
        isLiveMigration: true,
        mode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import PublicDelegatedPrefix, PublicDelegatedPrefixConfig

    def describe():
        ubx.intent("payments own public delegated prefix")

        ubx.resource(PublicDelegatedPrefix, "ci-runner", PublicDelegatedPrefixConfig(
            ip_cidr_range="example",
            name="ci-runner",
            parent_prefix="example",
            region="example",
            description="Managed by ubx.",
            is_live_migration=True,
            mode="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_public_delegated_prefix called "ci-runner",
    with ip cidr range example, parent prefix example, region
    example, description Managed by ubx., is live migration true,
    mode example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allocatable_prefix_length" type="number">
  The allocatable prefix length supported by this public delegated prefix. This field is optional and cannot be set for prefixes in DELEGATION mode. It cannot be set for IPv4 prefixes either, and it always defaults to 32. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_cidr_range" type="string" required>
  The IP address range, in CIDR format, represented by this public delegated prefix. Required.
</ResponseField>

<ResponseField name="is_live_migration" type="bool">
  If true, the prefix will be live migrated. Optional.
</ResponseField>

<ResponseField name="mode" type="string">
  Specifies the mode of this IPv6 PDP. MODE must be one of: * DELEGATION * EXTERNAL_IPV6_FORWARDING_RULE_CREATION * EXTERNAL_IPV6_SUBNETWORK_CREATION * INTERNAL_IPV6_SUBNETWORK_CREATION Possible values: ["DELEGATION", "EXTERNAL_IPV6_FORWARDING_RULE_CREATION", "EXTERNAL_IPV6_SUBNETWORK_CREATION", "INTERNAL_IPV6_SUBNETWORK_CREATION"] Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Required.
</ResponseField>

<ResponseField name="parent_prefix" type="string" required>
  The URL of parent prefix. Either PublicAdvertisedPrefix or PublicDelegatedPrefix. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string" required>
  A region where the prefix will reside. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="allocatable_prefix_length" type="number">
  The allocatable prefix length supported by this public delegated prefix. This field is optional and cannot be set for prefixes in DELEGATION mode. It cannot be set for IPv4 prefixes either, and it always defaults to 32. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_enhanced_ipv4_allocation" type="bool">
  Whether this PublicDelegatedPrefix supports enhanced IPv4 allocations. Applicable for IPv4 PDPs only. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ipv6_access_type" type="string">
  The internet access type for IPv6 Public Delegated Prefixes. Inherited from parent prefix and can be one of following: * EXTERNAL: The prefix will be announced to the internet. All children PDPs will have access type as EXTERNAL. * INTERNAL: The prefix won’t be announced to the internet. Prefix will be used privately within Google Cloud. All children PDPs will have access type as INTERNAL. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="public_delegated_sub_prefixs" type="list(object)">
  List of sub public delegated fixes for BYO IP functionality. Each item in this array represents a sub prefix that can be used to create addresses or further allocations. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="allocatable_prefix_length" type="number">
    </ResponseField>
    <ResponseField name="delegatee_project" type="string">
    </ResponseField>
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="enable_enhanced_ipv4_allocation" type="bool">
    </ResponseField>
    <ResponseField name="ip_cidr_range" type="string">
    </ResponseField>
    <ResponseField name="ipv6_access_type" type="string">
    </ResponseField>
    <ResponseField name="is_address" type="bool">
    </ResponseField>
    <ResponseField name="mode" type="string">
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="region" type="string">
    </ResponseField>
    <ResponseField name="status" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
