---
title: "google_compute_instance_template_iam_binding"
description: "Real, generated bindings for `google_compute_instance_template_iam_binding`."
---

`google_compute_instance_template_iam_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/compute"
    )

    cfg := compute.InstanceTemplateIamBindingConfig{
        Members: []string{"example"},
        Name: "example-name",
        Role: "example",
    }
    ubx.Resource(compute.InstanceTemplateIamBinding, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InstanceTemplateIamBinding } from
      "jsr:@ubx/sdk-google/google/compute/instance_template_iam_binding";

    resource(InstanceTemplateIamBinding, "example", {
      members: ["example"],
      name: "example-name",
      role: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        InstanceTemplateIamBinding,
        InstanceTemplateIamBindingConfig,
    )

    cfg = InstanceTemplateIamBindingConfig(
        members=["example"],
        name="example-name",
        role="example",
    )
    ubx.resource(InstanceTemplateIamBinding, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_instance_template_iam_binding with
members, name, role.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="members" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
