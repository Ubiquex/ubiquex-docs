---
title: "google_compute_backend_bucket_signed_url_key"
description: "Real, generated bindings for `google_compute_backend_bucket_signed_url_key`."
---

`google_compute_backend_bucket_signed_url_key` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own backend bucket signed url key"})

    		ubx.Resource(compute.BackendBucketSignedUrlKey, "ci-runner", compute.BackendBucketSignedUrlKeyConfig{
    			BackendBucket: "example",
    			KeyValue:      "example",
    			Name:          "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BackendBucketSignedUrlKey } from "jsr:@ubx/sdk-google/google/compute/backend_bucket_signed_url_key";

    export default stack("payments", () => {
      intent({ summary: "payments own backend bucket signed url key" });

      resource(BackendBucketSignedUrlKey, "ci-runner", {
        backendBucket: "example",
        keyValue: "example",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import BackendBucketSignedUrlKey, BackendBucketSignedUrlKeyConfig

    def describe():
        ubx.intent("payments own backend bucket signed url key")

        ubx.resource(BackendBucketSignedUrlKey, "ci-runner", BackendBucketSignedUrlKeyConfig(
            backend_bucket="example",
            key_value="example",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_backend_bucket_signed_url_key called
    "ci-runner", with backend bucket example, key value example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backend_bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_value" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
