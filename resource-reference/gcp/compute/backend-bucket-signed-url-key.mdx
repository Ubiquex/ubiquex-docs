---
title: "google_compute_backend_bucket_signed_url_key"
description: "Real, generated bindings for `google_compute_backend_bucket_signed_url_key`."
---

`google_compute_backend_bucket_signed_url_key` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/compute"
    )

    cfg := compute.BackendBucketSignedUrlKeyConfig{
        BackendBucket: "example",
        KeyValue: "example",
        Name: "example-name",
    }
    ubx.Resource(compute.BackendBucketSignedUrlKey, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BackendBucketSignedUrlKey } from
      "jsr:@ubx/sdk-google/google/compute/backend_bucket_signed_url_key";

    resource(BackendBucketSignedUrlKey, "example", {
      backendBucket: "example",
      keyValue: "example",
      name: "example-name",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        BackendBucketSignedUrlKey,
        BackendBucketSignedUrlKeyConfig,
    )

    cfg = BackendBucketSignedUrlKeyConfig(
        backend_bucket="example",
        key_value="example",
        name="example-name",
    )
    ubx.resource(BackendBucketSignedUrlKey, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_backend_bucket_signed_url_key with
backend_bucket, key_value, name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backend_bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_value" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
