---
title: "google_compute_region_security_policy"
description: "`google_compute_region_security_policy` is the regional counterpart to a global Cloud Armor security policy, applied to backends behind a regional..."
---

`google_compute_region_security_policy` is the regional counterpart to a global Cloud Armor security policy, applied to backends behind a regional, rather than global, load balancer.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own region security policy"})

    		ubx.Resource(compute.RegionSecurityPolicy, "example", compute.RegionSecurityPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionSecurityPolicy } from "@ubx/sdk-google/google/compute/region_security_policy";

    export default stack("example", () => {
      intent({ summary: "example own region security policy" });

      resource(RegionSecurityPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RegionSecurityPolicy, RegionSecurityPolicyConfig

    def describe():
        ubx.intent("example own region security policy")

        ubx.resource(RegionSecurityPolicy, "example", RegionSecurityPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_security_policy called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="adaptive_protection_config" type="object">
  Configuration options for Cloud Armor Adaptive Protection (CAAP). Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="layer7_ddos_defense_config" type="object">
      Configuration options for L7 DDoS detection. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
      <Expandable title="properties">
        <ResponseField name="enable" type="bool">
          If set to true, enables CAAP for L7 DDoS detection. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
        </ResponseField>
        <ResponseField name="rule_visibility" type="string">
          Rule visibility can be one of the following: STANDARD - opaque rules. (default) PREMIUM - transparent rules. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
        </ResponseField>
        <ResponseField name="threshold_configs" type="list(object)">
          Configuration options for layer7 adaptive protection for various customizable thresholds. Optional.
          <Expandable title="properties">
            <ResponseField name="auto_deploy_confidence_threshold" type="number">
              The confidence threshold percentage that triggers auto-deployment of adaptive protection. Must be a number between 0 and 100. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="auto_deploy_expiration_sec" type="number">
              Duration in seconds after which the auto-deployed adaptive protection mitigation expires. This sets how long the mitigation remains active before being automatically withdrawn. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="auto_deploy_impacted_baseline_threshold" type="number">
              Threshold for the ratio of impacted baseline traffic that triggers automatic deployment of the DDoS defense. When the measured impacted baseline exceeds this value, the adaptive protection automatically deploys the defense. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="auto_deploy_load_threshold" type="number">
              The load threshold in requests per second per backend instance that triggers automatic deployment of Layer 7 adaptive protection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="detection_absolute_qps" type="number">
              The absolute QPS threshold (in requests per second) that triggers adaptive protection detection. When traffic to the protected resource exceeds this rate, the system may consider it anomalous and apply DDoS defense actions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="detection_load_threshold" type="number">
              The minimum number of requests per second that triggers adaptive protection detection for this threshold configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="detection_relative_to_baseline_qps" type="number">
              Detection threshold relative to the baseline queries per second (QPS). A value of 1.0 means the threshold equals the baseline QPS; values greater than 1.0 require proportionally more traffic to trigger detection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              A unique name for this threshold configuration, used to identify it within the layer 7 DDoS defense settings of the adaptive protection config. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="traffic_granularity_configs" type="list(object)">
              A list of traffic granularity configurations for a layer 7 DDoS defense threshold. These configs define how traffic is aggregated or segmented (e.g., by source IP or range) when determining whether a threshold is exceeded, enabling granular control over adaptive protection rules. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="enable_each_unique_value" type="bool">
                  When set to true, traffic is tracked separately for each unique value of the configured traffic granularity type (e.g., each unique HTTP header host). When false, traffic is aggregated according to the granularity type. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The type of traffic granularity to use for the layer 7 DDoS defense threshold. This can be HTTP_HEADER_HOST to group traffic by host header, HTTP_PATH to group by request path, or UNSPECIFIED_TYPE if not set. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The IP address or CIDR range for this traffic granularity configuration. The interpretation depends on the associated type field (IP or CIDR). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="advanced_options_config" type="object">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="json_custom_config" type="object">
      Custom configuration for JSON parsing, used in the advanced options of a security policy. It typically specifies content types that should be parsed as JSON. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="content_types" type="list(string)">
          A list of custom Content-Type header values to apply the JSON parsing. As per RFC 1341, a Content-Type header value has the following format: Content-Type := type "/" subtype *[";" parameter] When configuring a custom Content-Type header value, only the type/subtype needs to be specified, and the parameters should be excluded. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="json_parsing" type="string">
      Configures JSON parsing mode for the security policy: DISABLED (no JSON parsing), STANDARD (standard JSON parsing), or STANDARD_WITH_GRAPHQL (standard JSON parsing with GraphQL support). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="log_level" type="string">
      The logging level for the security policy. Valid values are NORMAL and VERBOSE, where VERBOSE enables more detailed logging. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="request_body_inspection_size" type="string">
      The maximum request size chosen by the customer with Waf enabled. Values supported are "8KB", "16KB, "32KB", "48KB" and "64KB". Values are case insensitive. Optional.
    </ResponseField>
    <ResponseField name="user_ip_request_headers" type="list(string)">
      An optional list of case-insensitive request header names to use for resolving the callers client IP address. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="associations" type="list(object)">
  A list of associations that belong to this policy. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="attachment_id" type="string">
      The ID of the resource to which the regional security policy is associated, typically the self_link of a backend service. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The display_name is an optional user-defined name for the association, used for display purposes. It does not affect the association's identity, which is determined by the name field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="excluded_folders" type="list(string)">
      List of folder IDs to exclude from the association when the target is an organization or a folder. Resources in these excluded folders will not have the security policy applied. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="excluded_projects" type="list(string)">
      An optional list of project IDs that are excluded from this security policy association. When the association targets a project or organization, these projects will not have the policy applied. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="security_policy_id" type="string">
      The ID of the security policy to associate with the target in this association block. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="short_name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ddos_protection_config" type="object">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="ddos_adaptive_protection" type="string">
      The mode of DDoS adaptive protection for this region security policy. Set to DISABLED to turn it off, ENABLED to turn it on, or PREVIEW to test without enforcing. The UNSPECIFIED values are placeholders for internal use. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ddos_impacted_baseline_threshold" type="number">
      DDoS Protection for Network Load Balancers (and VMs with public IPs) builds DDoS mitigations that minimize collateral damage. It quantifies this as the fraction of a non-abuse baseline that's inadvertently blocked. Rules whose collateral damage exceeds ddosImpactedBaselineThreshold will not be deployed. Using a lower value will prioritize keeping collateral damage low, possibly at the cost of its effectiveness in rate limiting some or all of the attack. It should typically be unset, so Advanced DDoS (and Adaptive Protection) uses the best mitigation it can find. Setting the threshold is advised if there are logs for false positive detections with high collateral damage, and will cause Advanced DDoS to attempt to find a less aggressive rule that satisfies the constraint. If a suitable rule cannot be found, the system falls back to either no mitigation for smaller attacks or broader network throttles for larger ones. Optional.
    </ResponseField>
    <ResponseField name="ddos_protection" type="string">
      The DDoS protection tier to use for the regional security policy. Valid values are STANDARD, ADVANCED, and ADVANCED_PREVIEW. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Specifies a fingerprint for this resource, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make get() request to the security policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  A fingerprint for the labels being applied to this security policy, which is essentially a hash of the labels set used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update labels. You must always provide an up-to-date fingerprint hash in order to update or change labels. To see the latest fingerprint, make get() request to the security policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035. Label values may be empty. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parent" type="string">
  Output only. [Output Only] The parent of the security policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="recaptcha_options_config" type="object">
  The reCAPTCHA options configuration for the region security policy. This output-only object is populated by the API and includes settings such as whether to fail open when reCAPTCHA is unavailable. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="redirect_site_key" type="string">
      An optional field to supply a reCAPTCHA site key to be used for all the rules using the redirect action with the type of GOOGLE_RECAPTCHA under the security policy. The specified site key needs to be created from the reCAPTCHA API. The user is responsible for the validity of the specified site key. If not specified, a Google-managed site key is used. This field is only supported in Global Security Policies of type CLOUD_ARMOR. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the regional security policy resides. This field is not applicable to global security policies. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  A list of rules that belong to this policy. There must always be a default rule which is a rule with priority 2147483647 and match all condition (for the match condition this means match "*" for srcIpRanges and for the networkMatch condition every field must be either match "*" or not set). If no rules are provided when creating a security policy, a default rule with action "allow" will be added. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      The action to perform when a request matches the rule. Allowed values are 'allow' or 'deny', with an optional custom HTTP status code for deny (e.g., 'deny(403)'). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      An optional description of the security policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="header_action" type="object">
      Specifies the header action to apply to requests that match this rule. This block allows you to add or remove HTTP headers using request_headers_to_adds and request_headers_to_remove. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="request_headers_to_adds" type="list(object)">
          A list of request headers to add to the request. Each element in the list contains a header_name and header_value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="header_name" type="string">
              The name of the HTTP request header to add to the request. For example, 'X-Forwarded-For'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_value" type="string">
              The value of the custom header to be added to requests. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kind" type="string">
      The kind of the resource, set to 'compute#securityPolicyRule' for a region security policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="match" type="object">
      The match criteria for this security policy rule. It defines the conditions under which the rule is applied, such as source IP ranges, ports, or request expressions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="config" type="object">
          The configuration for the match condition, specifying the source and destination IP ranges to match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="src_ip_ranges" type="list(string)">
              A list of source IP address ranges in CIDR format. The rule matches requests whose source IP address falls within any of these ranges. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="expr" type="object">
          The expr block provides a Common Expression Language (CEL) expression that defines the matching condition for the security policy rule. The rule is applied when the expression evaluates to true. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="expression" type="string">
              The Common Expression Language (CEL) expression that defines the criteria for matching this security policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="location" type="string">
              The source location of the expression, used for debugging. This may specify a file name, line number, or other identifier of where the expression was defined. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="title" type="string">
              A short, human-readable title for the expression. This is optional and helps identify the purpose or meaning of the match expression. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="expr_options" type="object">
          Options for the expression in the match rule, such as reCAPTCHA configuration. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="recaptcha_options" type="object">
              Optional; if omitted, computed by Google Cloud.
              <Expandable title="properties">
                <ResponseField name="action_token_site_keys" type="list(string)">
                  A list of reCAPTCHA Enterprise site keys used for action tokens. These keys are used to validate the action token in the request. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="session_token_site_keys" type="list(string)">
                  A list of site keys used to verify reCAPTCHA session tokens. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="versioned_expr" type="string">
          The versioned expression for the match rule. Must be set to SRC_IPS_V1 when using source IP-based matching; this is the only supported value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_match" type="object">
      The network match configuration for the rule, specifying network conditions like source IP ranges, source region codes, and user-defined fields that the request must satisfy for the rule to apply. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dest_ip_ranges" type="list(string)">
          Destination IP address ranges in CIDR format for matching network traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_ports" type="list(string)">
          A list of destination ports to match, where each entry is a single port (e.g., '80') or a range (e.g., '80-90'). Used in L4 network match conditions for security policy rules. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ip_protocols" type="list(string)">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="src_asns" type="list(number)">
          A list of source Autonomous System Numbers (ASNs) to match. The rule applies to requests originating from networks belonging to any of the specified ASNs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_ip_ranges" type="list(string)">
          A list of source IP address ranges (CIDR format) that the rule matches for incoming traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_ports" type="list(string)">
          A list of source ports to match for this rule. Each entry can be a single port number or a port range (e.g., '80' or '80-90'). If unspecified, all source ports are matched. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_region_codes" type="list(string)">
          A list of source region codes to match against the source region of the traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_defined_fields" type="list(object)">
          A list of user-defined field configurations used to match network traffic within a security policy rule. Each configuration specifies a field name and a list of values to match against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the user-defined field to match in the request. This corresponds to the field name as defined in the security policy's user-defined fields. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              A list of values to match the user-defined field against. The incoming packet matches if the field value equals any of the specified values. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preconfigured_waf_config" type="object">
      The preconfigured_waf_config block configures settings for the preconfigured WAF rules applied to this security policy rule. It allows you to customize the inspection behavior, such as defining exclusions to skip certain parts of requests from WAF evaluation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="exclusions" type="list(object)">
          A list of exclusion rules for this preconfigured WAF rule, specifying request attributes (e.g., headers, cookies, query parameters) that should be ignored during WAF evaluation to avoid false positives or allow specific traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="request_cookies_to_exclude" type="list(object)">
              A list of request cookies to exclude from WAF evaluation. Each exclusion specifies an operator (EQUALS or STARTS_WITH) and a value representing the cookie name to match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator to apply when matching the request cookie value. Allowed values are CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  The value of the request cookie to exclude from WAF evaluation. When specified, only cookies whose value exactly matches this field are excluded; when omitted, the exclusion applies to any cookie with the matching key. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_headers_to_exclude" type="list(object)">
              A list of request headers to exclude from the preconfigured WAF rule evaluation. Each object specifies a header key, a match operator, and a value; the operator can be one of EQUALS_TO, EQUALS_ANY, STARTS_WITH, ENDS_WITH, or CONTAINS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator to apply when matching the request header. Valid values are: CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  Optional; if omitted, computed by Google Cloud.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_query_params_to_exclude" type="list(object)">
              A list of query parameters to exclude from WAF evaluation, allowing specific query parameters to be ignored by preconfigured WAF rules. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator used to match the query parameter in the exclusion rule. Valid values are `CONTAINS`, `ENDS_WITH`, `EQUALS`, `EQUALS_ANY`, and `STARTS_WITH`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  The value of the query parameter to exclude from WAF evaluation. If a request query parameter matches this value, the parameter (or key-value pair) is excluded from the preconfigured WAF rule evaluation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_uris_to_exclude" type="list(object)">
              A list of request URIs to exclude from the preconfigured WAF rule evaluation. Each object specifies a URI matching condition, typically including a value and an operator. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="op" type="string">
                  The operator used to match the request URI. Allowed values are: CONTAINS, ENDS_WITH, EQUALS, EQUALS_ANY, STARTS_WITH. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="val" type="string">
                  The URI value to be excluded from the preconfigured WAF rule evaluation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="target_rule_ids" type="list(string)">
              List of target rule IDs for the exclusion. When empty, all preconfigured WAF rules are excluded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="target_rule_set" type="string">
              The target WAF rule set to which the exclusion applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preview" type="bool">
      Whether the rule is in preview mode. Preview rules are not enforced, but their actions are still logged and can be used to test rule behavior before enabling enforcement. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="priority" type="number">
      An integer that specifies the evaluation priority of the rule. Lower values are evaluated first. Must be unique within the security policy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rate_limit_options" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="ban_duration_sec" type="number">
          The duration in seconds that a client is banned after exceeding the ban threshold. Only applicable when exceed_action is set to 'ban'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ban_threshold" type="object">
          Threshold that, when exceeded, triggers banning of clients. This nested block is part of rate limit options; when a client surpasses the specified count within the interval, the client is banned. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              The number of requests that must be exceeded within the configured interval to trigger a ban. Used in the ban_threshold of rate limiting for a security policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="interval_sec" type="number">
              The time interval, in seconds, over which the ban threshold count is measured before a ban is enforced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="conform_action" type="string">
          The action to take when a request is within the allowed rate limit. For rate limiting, this must be set to 'allow'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enforce_on_key" type="string">
          Specifies the key type used to identify requests for rate limiting enforcement. Accepted values are: ALL, ASN, HTTP_COOKIE, HTTP_HEADER, HTTP_PATH, IP, REGION_CODE, SNI, TLS_JA3_FINGERPRINT, TLS_JA4_FINGERPRINT, USER_IP, and XFF_IP. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enforce_on_key_configs" type="list(object)">
          Specifies a list of key configurations used to enforce the rate limit. Each configuration contains the key type (e.g., IP, HTTP_HEADER) and an optional key name. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enforce_on_key_name" type="string">
              The name of the key (e.g., HTTP header or cookie name) to enforce rate limiting on. Required when enforce_on_key_type is HTTP_HEADER or COOKIE. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enforce_on_key_type" type="string">
              The type of key on which to enforce rate limiting. This specifies which request attribute (e.g., source IP, HTTP header, path) is used to identify and group clients for rate limiting. Valid values are: ALL, ASN, HTTP_COOKIE, HTTP_HEADER, HTTP_PATH, IP, REGION_CODE, SNI, TLS_JA3_FINGERPRINT, TLS_JA4_FINGERPRINT, USER_IP, XFF_IP. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enforce_on_key_name" type="string">
          The name of the header or cookie to enforce the rate limit on. This field is required when enforce_on_key is set to HTTP_HEADER or HTTP_COOKIE. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="exceed_action" type="string">
          The action to take when a request exceeds the rate limit defined in these rate limit options. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="exceed_redirect_options" type="object">
          Specifies the redirect action for requests that exceed the configured rate limit. This object defines the redirect type (e.g., EXTERNAL_302) and the target URL to redirect excess traffic to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="target" type="string">
              The URL to which requests that exceed the rate limit are redirected. This is used when the redirect type is set to EXTERNAL_302. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of redirect action to take when the rate limit is exceeded. Allowed values are EXTERNAL_302 and GOOGLE_RECAPTCHA. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="rate_limit_threshold" type="object">
          Configuration for the rate limit threshold, specifying the maximum number of requests allowed within a given time interval before triggering the configured action. Typically includes fields like count and interval. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              The number of requests allowed within the specified time interval before the rate limit action is enforced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="interval_sec" type="number">
              The interval in seconds over which the rate limit count is enforced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="redirect_options" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="target" type="string">
          The target URL to redirect to when the redirect type is EXTERNAL_302. Only set this when the redirect type is EXTERNAL_302. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of redirect to apply. If set to EXTERNAL_302, the request is redirected to the specified external target with a 302 status. If set to GOOGLE_RECAPTCHA, the request is redirected to a Google reCAPTCHA page. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="short_name" type="string">
  User-provided name of the organization security policy. The name should be unique in the organization in which the security policy is created. This should only be used when SecurityPolicyType is CLOUD_ARMOR. The name must be 1-63 characters long, and comply with https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  The type indicates the intended use of the security policy. - CLOUD_ARMOR: Cloud Armor backend security policies can be configured to filter incoming HTTP requests targeting backend services. They filter requests before they hit the origin servers. - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can be configured to filter incoming HTTP requests targeting backend services (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage). They filter requests before the request is served from Google's cache. - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor internal service policies can be configured to filter HTTP requests targeting services managed by Traffic Director in a service mesh. They filter requests before the request is served from the application. - CLOUD_ARMOR_NETWORK: Cloud Armor network policies can be configured to filter packets targeting network load balancing resources such as backend services, target pools, target instances, and instances with external IPs. They filter requests before the request is served from the application. This field can be set only at resource creation time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="user_defined_fields" type="list(object)">
  Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A user-defined field consists of up to 4 bytes extracted from a fixed offset in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an optional mask to select certain bits. Rules may then specify matching values for these fields. Example: userDefinedFields: - name: "ipv4_fragment_offset" base: IPV4 offset: 6 size: 2 mask: "0x1fff" Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="base" type="string">
      The base protocol or address family that the user-defined field applies to. Valid values are IPV4, IPV6, TCP, UDP. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mask" type="string">
      A string representation of the bitmask used to extract the user-defined field from the network packet. The mask is applied to the field specified by the base, offset, and size to select the relevant bits for matching. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="offset" type="number">
      The byte offset from the specified base (e.g., IP header) where the user-defined field begins. Used together with `base` and `size` to define a custom packet field for matching. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="size" type="number">
      The size of the user-defined field in bytes, which specifies how many bytes of the named field to inspect. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
