---
title: "google_compute_interconnect_attachment_group"
description: "Real, generated bindings for `google_compute_interconnect_attachment_group`."
---

`google_compute_interconnect_attachment_group` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own interconnect attachment group"})

    		ubx.Resource(compute.InterconnectAttachmentGroup, "ci-runner", compute.InterconnectAttachmentGroupConfig{
    			Name:              "ci-runner",
    			Description:       "Managed by ubx.",
    			InterconnectGroup: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InterconnectAttachmentGroup } from "jsr:@ubx/sdk-google/google/compute/interconnect_attachment_group";

    export default stack("payments", () => {
      intent({ summary: "payments own interconnect attachment group" });

      resource(InterconnectAttachmentGroup, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        interconnectGroup: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import InterconnectAttachmentGroup, InterconnectAttachmentGroupConfig

    def describe():
        ubx.intent("payments own interconnect attachment group")

        ubx.resource(InterconnectAttachmentGroup, "ci-runner", InterconnectAttachmentGroupConfig(
            name="ci-runner",
            description="Managed by ubx.",
            interconnect_group="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_interconnect_attachment_group called
    "ci-runner", with description Managed by ubx., interconnect group
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attachments" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="attachment" type="string">
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="intent" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="availability_sla" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="interconnect_group" type="string">
  The URL of an InterconnectGroup that groups these Attachments' Interconnects. Customers do not need to set this unless directed by Google Support. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="attachments" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="attachment" type="string">
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="configured" type="list(object)">
  The redundancy this group is configured to support. The way a user queries what SLA their Attachment gets is by looking at this field of the Attachment's AttachmentGroup. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="availability_sla" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="effective_sla" type="string">
        </ResponseField>
        <ResponseField name="intended_sla_blockers" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="attachments" type="list(string)">
            </ResponseField>
            <ResponseField name="blocker_type" type="string">
            </ResponseField>
            <ResponseField name="documentation_link" type="string">
            </ResponseField>
            <ResponseField name="explanation" type="string">
            </ResponseField>
            <ResponseField name="metros" type="list(string)">
            </ResponseField>
            <ResponseField name="regions" type="list(string)">
            </ResponseField>
            <ResponseField name="zones" type="list(string)">
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Creation timestamp in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="intent" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="availability_sla" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="logical_structure" type="list(object)">
  An analysis of the logical layout of Attachments in this group. Every Attachment in the group is shown once in this structure. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="regions" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="metros" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="facilities" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="facility" type="string">
                </ResponseField>
                <ResponseField name="zones" type="list(object)">
                  <Expandable title="properties">
                    <ResponseField name="attachment" type="list(string)">
                    </ResponseField>
                    <ResponseField name="attachments" type="list(string)">
                    </ResponseField>
                    <ResponseField name="zone" type="string">
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="metro" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="region" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
