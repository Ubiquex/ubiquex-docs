---
title: "google_compute_interconnect_attachment_group"
description: "A complete, runnable google_compute_interconnect_attachment_group program, in every SDK language."
---

`google_compute_interconnect_attachment_group` -- real, typed bindings generated directly from
real Google Discovery Documents, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own interconnect attachment group"})

    		ubx.Resource(compute.InterconnectAttachmentGroup, "ci-runner", compute.InterconnectAttachmentGroupConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { InterconnectAttachmentGroup } from "./local-sdk/google/sdk/typescript/google/compute/interconnect_attachment_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own interconnect attachment group" });

      resource(InterconnectAttachmentGroup, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.compute import InterconnectAttachmentGroup, InterconnectAttachmentGroupConfig

    def describe():
        ubx.intent("payments own interconnect attachment group")

        ubx.resource(InterconnectAttachmentGroup, "ci-runner", InterconnectAttachmentGroupConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_interconnect_attachment_group called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="attachments" type="map(object)">
  Attachments in the AttachmentGroup. Keys are arbitrary user-specified strings. Users are encouraged, but not required, to use their preferred format for resource links as keys. Note that there are add-members and remove-members methods in gcloud. The size of this map is limited by an "Attachments per group" quota. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="attachment" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="configured" type="object">
  [Output Only] The redundancy this group is configured to support. The way a user queries what SLA their Attachment gets is by looking at this field of the Attachment's AttachmentGroup. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="availability_sla" type="object">
      [Output Only] Which SLA this group is configured to support, and why this group does or does not meet that SLA's requirements. Optional.
      <Expandable title="properties">
        <ResponseField name="effective_sla" type="string">
          The effective Service Level Agreement (SLA) for the interconnect attachment group, computed by the provider. Possible values: EFFECTIVE_SLA_UNSPECIFIED, NO_SLA, PRODUCTION_CRITICAL, PRODUCTION_NON_CRITICAL. Computed by GCP after `ship`. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="intended_sla_blockers" type="list(object)">
          Computed list of blockers that would prevent the configured availability SLA from being met. Computed by GCP after `ship`. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="attachments" type="list(string)">
              List of interconnect attachments that are configured as intended blockers for the availability SLA. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="blocker_type" type="string">
              The type of blocker preventing the intended SLA from being met. Possible values include: BLOCKER_TYPE_UNSPECIFIED, INCOMPATIBLE_METROS, INCOMPATIBLE_REGIONS, MISSING_GLOBAL_ROUTING, NO_ATTACHMENTS, NO_ATTACHMENTS_IN_METRO_AND_ZONE, and OTHER. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="documentation_link" type="string">
              A URL to documentation describing this intended SLA blocker. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="explanation" type="string">
              A human-readable explanation of the intended SLA blocker, describing why it might prevent meeting the availability SLA. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metros" type="list(string)">
              List of metro locations that are excluded from the availability SLA, as they are considered blockers. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="regions" type="list(string)">
              The list of regions designated as intended SLA blockers. These regions are excluded from the availability SLA calculation, so the SLA guarantee does not apply to attachments located in them. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="zones" type="list(string)">
              A list of zone names that are intended to be treated as SLA blockers in the configured availability SLA. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  Opaque system-generated token that uniquely identifies the configuration. If provided when patching a configuration in update mode, the provided token must match the current token or the update is rejected. This provides a reliable means of doing read-modify-write (optimistic locking) as described byAIP 154. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource type. The server generates this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="intent" type="object">
  The user's intent for this AttachmentGroup. This is the only required field besides the name that must be specified on group creation. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="availability_sla" type="string">
      The availability SLA (service level agreement) for this interconnect attachment. Valid values are: AVAILABILITY_SLA_UNSPECIFIED, NO_SLA, PRODUCTION_CRITICAL, and PRODUCTION_NON_CRITICAL. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="interconnect_group" type="string">
  The URL of an InterconnectGroup that groups these Attachments' Interconnects. Customers do not need to set this unless directed by Google Support. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#interconnectAttachmentGroup. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="logical_structure" type="object">
  [Output Only] An analysis of the logical layout of Attachments in this group. Every Attachment in the group is shown once in this structure. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="regions" type="list(object)">
      A list of regions where the interconnect attachment group is available, along with the status of the group in each region. Computed by GCP after `ship`. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="metros" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="facilities" type="list(object)">
              A list of physical data center facilities available in the metro for this interconnect attachment group. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="facility" type="string">
                  The name of the physical facility (data center) within the metro where the interconnect attachment is located. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="zones" type="list(object)">
                  A list of zones within the facility. Each zone represents an availability zone in the facility where the interconnect attachment group operates. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="attachments" type="list(string)">
                      Optional; if omitted, computed by GCP.
                    </ResponseField>
                    <ResponseField name="zone" type="string">
                      The availability zone within the facility, typically in the form `&lt;region&gt;-&lt;zone&gt;` (e.g., us-central1-a). Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="metro" type="string">
              The name of a metropolitan area (metro) that is associated with a specific region in the logical structure of an Interconnect attachment group. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="region" type="string">
          The Google Cloud region for this logical structure entry. Must be a valid region name, e.g., 'us-central1'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="attachments" type="map(object)">
  Attachments in the AttachmentGroup. Keys are arbitrary user-specified strings. Users are encouraged, but not required, to use their preferred format for resource links as keys. Note that there are add-members and remove-members methods in gcloud. The size of this map is limited by an "Attachments per group" quota. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="attachment" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="configured" type="object">
  [Output Only] The redundancy this group is configured to support. The way a user queries what SLA their Attachment gets is by looking at this field of the Attachment's AttachmentGroup. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="availability_sla" type="object">
      [Output Only] Which SLA this group is configured to support, and why this group does or does not meet that SLA's requirements. Optional.
      <Expandable title="properties">
        <ResponseField name="effective_sla" type="string">
          The effective Service Level Agreement (SLA) for the interconnect attachment group, computed by the provider. Possible values: EFFECTIVE_SLA_UNSPECIFIED, NO_SLA, PRODUCTION_CRITICAL, PRODUCTION_NON_CRITICAL. Computed by GCP after `ship`. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="intended_sla_blockers" type="list(object)">
          Computed list of blockers that would prevent the configured availability SLA from being met. Computed by GCP after `ship`. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="attachments" type="list(string)">
              List of interconnect attachments that are configured as intended blockers for the availability SLA. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="blocker_type" type="string">
              The type of blocker preventing the intended SLA from being met. Possible values include: BLOCKER_TYPE_UNSPECIFIED, INCOMPATIBLE_METROS, INCOMPATIBLE_REGIONS, MISSING_GLOBAL_ROUTING, NO_ATTACHMENTS, NO_ATTACHMENTS_IN_METRO_AND_ZONE, and OTHER. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="documentation_link" type="string">
              A URL to documentation describing this intended SLA blocker. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="explanation" type="string">
              A human-readable explanation of the intended SLA blocker, describing why it might prevent meeting the availability SLA. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metros" type="list(string)">
              List of metro locations that are excluded from the availability SLA, as they are considered blockers. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="regions" type="list(string)">
              The list of regions designated as intended SLA blockers. These regions are excluded from the availability SLA calculation, so the SLA guarantee does not apply to attachments located in them. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="zones" type="list(string)">
              A list of zone names that are intended to be treated as SLA blockers in the configured availability SLA. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  Opaque system-generated token that uniquely identifies the configuration. If provided when patching a configuration in update mode, the provided token must match the current token or the update is rejected. This provides a reliable means of doing read-modify-write (optimistic locking) as described byAIP 154. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource type. The server generates this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="intent" type="object">
  The user's intent for this AttachmentGroup. This is the only required field besides the name that must be specified on group creation. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="availability_sla" type="string">
      The availability SLA (service level agreement) for this interconnect attachment. Valid values are: AVAILABILITY_SLA_UNSPECIFIED, NO_SLA, PRODUCTION_CRITICAL, and PRODUCTION_NON_CRITICAL. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="interconnect_group" type="string">
  The URL of an InterconnectGroup that groups these Attachments' Interconnects. Customers do not need to set this unless directed by Google Support. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#interconnectAttachmentGroup. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="logical_structure" type="object">
  [Output Only] An analysis of the logical layout of Attachments in this group. Every Attachment in the group is shown once in this structure. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="regions" type="list(object)">
      A list of regions where the interconnect attachment group is available, along with the status of the group in each region. Computed by GCP after `ship`. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="metros" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="facilities" type="list(object)">
              A list of physical data center facilities available in the metro for this interconnect attachment group. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="facility" type="string">
                  The name of the physical facility (data center) within the metro where the interconnect attachment is located. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="zones" type="list(object)">
                  A list of zones within the facility. Each zone represents an availability zone in the facility where the interconnect attachment group operates. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="attachments" type="list(string)">
                      Optional; if omitted, computed by GCP.
                    </ResponseField>
                    <ResponseField name="zone" type="string">
                      The availability zone within the facility, typically in the form `&lt;region&gt;-&lt;zone&gt;` (e.g., us-central1-a). Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="metro" type="string">
              The name of a metropolitan area (metro) that is associated with a specific region in the logical structure of an Interconnect attachment group. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="region" type="string">
          The Google Cloud region for this logical structure entry. Must be a valid region name, e.g., 'us-central1'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>
