---
title: "google_compute_interconnect_group"
description: "`google_compute_interconnect_group` groups multiple real Interconnect connections together -- typically for redundancy across different physical..."
---

`google_compute_interconnect_group` groups multiple real Interconnect connections together -- typically for redundancy across different physical facilities -- so their combined resilience can be validated and monitored as a real, single unit.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own interconnect group"})

    		ubx.Resource(compute.InterconnectGroup, "example", compute.InterconnectGroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InterconnectGroup } from "jsr:@ubx/sdk-google/google/compute/interconnect_group";

    export default stack("example", () => {
      intent({ summary: "example own interconnect group" });

      resource(InterconnectGroup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import InterconnectGroup, InterconnectGroupConfig

    def describe():
        ubx.intent("example own interconnect group")

        ubx.resource(InterconnectGroup, "example", InterconnectGroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_interconnect_group called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="configured" type="object">
  [Output Only] The status of the group as configured. This has the same structure as the operational field reported by the OperationalStatus method, but does not take into account the operational status of each resource. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="topology_capability" type="object">
      [Output Only] How reliable this topology is configured to be, and why this group does or does not meet the requirements for the intended capability. Optional.
      <Expandable title="properties">
        <ResponseField name="intended_capability_blockers" type="list(object)">
          A list of reasons that prevent the interconnect group's intended topology capability from being realized. This computed field is empty when the intended capability is fully supported, and provides details on any issues otherwise. Computed by GCP after `ship`. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="blocker_type" type="string">
              The type of blocker preventing the intended capability. Possible values: INCOMPATIBLE_METROS, NOT_AVAILABLE, NO_INTERCONNECTS, NO_INTERCONNECTS_IN_METRO_AND_ZONE, OTHER, UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="documentation_link" type="string">
              A URL to documentation explaining why the intended capability is blocked. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="explanation" type="string">
              A human-readable explanation of why the intended topology capability is blocked. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="facilities" type="list(string)">
              List of facility names that are blocking the intended topology capability for this interconnect group. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="interconnects" type="list(string)">
              The list of interconnect names that are blocking the intended topology capability for this interconnect group. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metros" type="list(string)">
              List of metro names (geographic locations) that block the intended topology capability from being achieved. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="zones" type="list(string)">
              A list of zones in which the intended topology capability is blocked or unavailable, as part of a capability blocker. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="supported_sla" type="string">
          The supported service level agreement (SLA) for this topology capability. Possible values are: NO_SLA, PRODUCTION_CRITICAL, PRODUCTION_NON_CRITICAL, UNSPECIFIED. Computed by GCP after `ship`. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  Opaque system-generated token that uniquely identifies the configuration. If provided when patching a configuration in update mode, the provided token must match the current token or the update is rejected. This provides a reliable means of doing read-modify-write (optimistic locking) as described by AIP 154. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource type. The server generates this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="intent" type="object">
  The user's intent for this group. This is the only required field besides the name that must be specified on group creation. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="topology_capability" type="string">
      Specifies the topology capability level for the interconnect group, indicating the required service level. Valid values are: NO_SLA (no service level agreement), PRODUCTION_CRITICAL (production workloads requiring high reliability), PRODUCTION_NON_CRITICAL (production workloads without critical reliability requirements), and UNSPECIFIED (value not set). Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="interconnects" type="map(object)">
  Interconnects in the InterconnectGroup. Keys are arbitrary user-specified strings. Users are encouraged, but not required, to use their preferred format for resource links as keys. Note that there are add-members and remove-members methods in gcloud. The size of this map is limited by an "Interconnects per group" quota. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="interconnect" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#InterconnectGroup Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="physical_structure" type="object">
  [Output Only] An analysis of the physical layout of Interconnects in this group. Every Interconnect in the group is shown once in this structure. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="metros" type="list(object)">
      The metropolitan areas where the physical structure of the interconnect group is located. This computed field provides the list of metros supported by the physical infrastructure. Computed by GCP after `ship`. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="facilities" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="facility" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="zones" type="list(object)">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="interconnects" type="list(string)">
                  Optional; if omitted, computed by GCP.
                </ResponseField>
                <ResponseField name="zone" type="string">
                  Optional; if omitted, computed by GCP.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="metro" type="string">
          The metropolitan area where the interconnect group's physical structure is located, such as 'london' or 'sydney'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>
