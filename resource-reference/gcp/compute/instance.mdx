---
title: "google_compute_instance"
description: "`google_compute_instance` is a real, running virtual machine in Compute Engine -- its machine type, boot disk, network interfaces..."
---

`google_compute_instance` is a real, running virtual machine in Compute Engine -- its machine type, boot disk, network interfaces, and metadata all defined here, the core compute resource everything else in this reference ultimately supports.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own instance"})

    		ubx.Resource(compute.Instance, "example", compute.InstanceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Instance } from "@ubx/sdk-google/google/compute/instance";

    export default stack("example", () => {
      intent({ summary: "example own instance" });

      resource(Instance, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import Instance, InstanceConfig

    def describe():
        ubx.intent("example own instance")

        ubx.resource(Instance, "example", InstanceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_instance called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="advanced_machine_features" type="object">
  Specifies options for controlling advanced machine features. Options that would traditionally be configured in a BIOS belong here. Features that require operating system support may have corresponding entries in the GuestOsFeatures of anImage (e.g., whether or not the OS in theImage supports nested virtualization being enabled or disabled). Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable_nested_virtualization" type="bool">
      Whether to enable nested virtualization or not (default is false). Optional.
    </ResponseField>
    <ResponseField name="enable_uefi_networking" type="bool">
      Whether to enable UEFI networking for instance creation. Optional.
    </ResponseField>
    <ResponseField name="performance_monitoring_unit" type="string">
      Type of Performance Monitoring Unit requested on instance. Optional.
    </ResponseField>
    <ResponseField name="threads_per_core" type="number">
      The number of threads per physical core. To disable simultaneous multithreading (SMT) set this to 1. If unset, the maximum number of threads supported per core by the underlying processor is assumed. Optional.
    </ResponseField>
    <ResponseField name="turbo_mode" type="string">
      Turbo frequency mode to use for the instance. Supported modes include: * ALL_CORE_MAX Using empty string or not setting this field will use the platform-specific default turbo mode. Optional.
    </ResponseField>
    <ResponseField name="visible_core_count" type="number">
      The number of physical cores to expose to an instance. Multiply by the number of threads per core to compute the total number of virtual CPUs to expose to the instance. If unset, the number of cores is inferred from the instance's nominal CPU count and the underlying platform's SMT width. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="can_ip_forward" type="bool">
  Allows this instance to send and receive packets with non-matching destination or source IPs. This is required if you plan to use this instance to forward routes. For more information, seeEnabling IP Forwarding. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="confidential_instance_config" type="object">
  A set of Confidential Instance options. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="confidential_instance_type" type="string">
      Defines the type of technology used by the confidential instance. Optional.
    </ResponseField>
    <ResponseField name="enable_confidential_compute" type="bool">
      Defines whether the instance should have confidential compute enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cpu_platform" type="string">
  Output only. [Output Only] The CPU platform used by this instance. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_protection" type="bool">
  Whether the resource should be protected against deletion. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disks" type="list(object)">
  Array of disks associated with this instance. Persistent disks must be created before you can assign them. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="architecture" type="string">
      The CPU architecture of the disk. Must be one of: ARCHITECTURE_UNSPECIFIED, ARM64, or X86_64. This specifies whether the disk is compatible with ARM64 or x86_64 instances. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="auto_delete" type="bool">
      If true, the disk will be automatically deleted when the instance is deleted. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="boot" type="bool">
      A boolean flag indicating whether this disk is the boot disk for the instance. Only one disk can be the boot disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="device_name" type="string">
      The device name exposed to the instance for this disk. If not specified, Google Cloud assigns a default device name. This name is used to reference the disk inside the guest OS (e.g., as /dev/sdb). Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disk_encryption_key" type="object">
      Configuration for a customer-supplied encryption key (CSEK) to encrypt this disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="kms_key_name" type="string">
          The full resource name of the Cloud KMS key used to encrypt the disk, in the format projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;keyRing&#125;/cryptoKeys/&#123;key&#125;. This key is a customer-managed encryption key (CMEK) used for disk encryption. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kms_key_service_account" type="string">
          The service account being used for the encryption request for the given KMS key. If not specified, the instance's default service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="raw_key" type="string">
          The customer-supplied encryption key for the disk, provided as a base64-encoded 256-bit key. This is used to encrypt the disk when using customer-supplied encryption keys. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rsa_encrypted_key" type="string">
          The base64-encoded RSA-encrypted version of the customer-supplied encryption key, encrypted using Google's public RSA key. This is provided as an alternative to the unencrypted raw_key for protecting the key during transmission. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sha256" type="string">
          The SHA256 hash of the customer-supplied encryption key (raw_key). This field is computed by Google and used to verify the key. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="disk_size_gb" type="string">
      The size of the persistent disk in gigabytes (GB). If not set, the disk size is determined by the source image or the disk type's default. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="force_attach" type="bool">
      If true, the disk is attached to this instance even if it is already attached to another instance. Defaults to false. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="guest_os_features" type="list(object)">
      A list of guest OS features to enable for the disk. Each feature is an object with a `type` field, such as `UEFI_COMPATIBLE`, `SECURE_BOOT`, or `VIRTIO_SCSI_MULTIQUEUE`. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="type" type="string">
          Specifies the type of guest OS feature to enable. Valid values include BARE_METAL_LINUX_COMPATIBLE, CCA_CAPABLE, FEATURE_TYPE_UNSPECIFIED, GVNIC, IDPF, MULTI_IP_SUBNET, SECURE_BOOT, SEV_CAPABLE, SEV_LIVE_MIGRATABLE, SEV_LIVE_MIGRATABLE_V2, SEV_SNP_CAPABLE, SNP_SVSM_CAPABLE, TDX_CAPABLE, UEFI_COMPATIBLE, VIRTIO_SCSI_MULTIQUEUE, and WINDOWS. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="index" type="number">
      The zero-based index of the disk in the instance, where index 0 corresponds to the boot disk. This uniquely identifies the disk among all disks attached to the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="initialize_params" type="object">
      This block is used to initialize a new disk from a source image or snapshot, specifying parameters such as the image, disk size, and disk type. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="architecture" type="string">
          The CPU architecture for the disk image. Valid values are ARCHITECTURE_UNSPECIFIED, ARM64, and X86_64. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          An optional description for the disk being initialized. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_name" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="disk_size_gb" type="string">
          Specifies the size of the disk in gigabytes. If not set, the default size of the underlying image is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_type" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="enable_confidential_compute" type="bool">
          Enables Confidential Compute for this disk, which encrypts data in use and allows the disk to be used with confidential VM instances. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Labels to apply to the boot disk created from this initialize_params block. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="licenses" type="list(string)">
          A list of license URIs to apply to this disk. This may be used to apply licenses that are not already present on the image. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="on_update_action" type="string">
          The update action to perform when the disk configuration changes. Supported values: RECREATE_DISK (recreate the disk on any update), RECREATE_DISK_IF_SOURCE_CHANGED (recreate only if the source image/snapshot changes), USE_EXISTING_DISK (keep the existing disk). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="provisioned_iops" type="string">
          The provisioned IOPS for the disk. This is applicable to disk types that support configurable IOPS, such as Hyperdisk or Extreme persistent disks. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="provisioned_throughput" type="string">
          The provisioned throughput, in MB/s, for the disk. Only applicable to disk types that support provisioned throughput, such as Hyperdisk. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="replica_zones" type="list(string)">
          The zones for a regional persistent disk. Provide exactly two zones to create a regional disk; if omitted, a zonal disk is created. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_manager_tags" type="map(string)">
          A map of resource manager tags to apply to the disk. Keys must be in the format tagKeys/&#123;tag_key_id&#125; and values in the format tagValues/&#123;tag_value_id&#125;. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_policies" type="list(string)">
          A list of self-links to resource policies (such as snapshot schedules) to apply to the disk when it is created. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="source_image" type="string">
          The source image to use for creating this disk. Can be a fully-qualified image URL, a project/image reference, or an image family name. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="source_image_encryption_key" type="object">
          Provides the customer-supplied encryption key used to decrypt the source image when creating a disk from an encrypted image. This block is only used if the source image is encrypted with a customer-supplied key. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key_name" type="string">
              The customer-managed encryption key (CMEK) name used to encrypt the source image. Required if the source image is encrypted with a CMEK. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kms_key_service_account" type="string">
              The service account being used for the encryption request for the source image. If not specified, the default Compute Engine service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="raw_key" type="string">
              The raw key material, base64-encoded, used to decrypt the source image. Required if the source image is encrypted with a customer-supplied encryption key. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rsa_encrypted_key" type="string">
              The base64-encoded RSA-encrypted key used to decrypt the source image. This key must be encrypted with the public key from the image's KMS key or the service account specified in the same encryption key block. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sha256" type="string">
              The SHA256 hash of the customer-supplied encryption key used to encrypt the source image. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_snapshot" type="string">
          The source snapshot used to create this disk. The disk will be initialized from the specified snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="source_snapshot_encryption_key" type="object">
          The customer-supplied encryption key used to decrypt the source snapshot, if it was created with a customer-managed encryption key. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key_name" type="string">
              The name of the customer-managed encryption key (CMEK) used to encrypt the source snapshot. Must be a full resource name in the format projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;keyRing&#125;/cryptoKeys/&#123;key&#125;. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kms_key_service_account" type="string">
              The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="raw_key" type="string">
              The raw 256-bit customer-supplied encryption key, base64-encoded, used to decrypt the source snapshot. This key must match the encryption key used to encrypt the snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rsa_encrypted_key" type="string">
              The RSA-encrypted (wrapped) customer-supplied encryption key used to decrypt the source snapshot. Provide either `raw_key` or `rsa_encrypted_key`; when using `rsa_encrypted_key`, also supply `kms_key_self_link`. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sha256" type="string">
              The SHA256 hash of the customer-supplied encryption key used to decrypt the source snapshot. This is required when the source snapshot is encrypted with a customer-supplied key. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="storage_pool" type="string">
          The URL of the storage pool in which to create the disk. If not specified, the default storage pool is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="interface" type="string">
      The disk interface type to use for attaching this disk. Must be one of 'SCSI' or 'NVME'. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kind" type="string">
      The kind of the disk resource, which is always 'compute#attachedDisk' for disks attached to an instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="licenses" type="list(string)">
      List of license self-links (URLs) to apply to the disk. This is typically used when creating a boot disk from a custom image to specify the operating system licenses that apply, such as 'https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-10-buster'. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mode" type="string">
      The mode in which the disk is attached to the instance, either READ_ONLY or READ_WRITE. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="saved_state" type="string">
      Specifies whether the disk's data is preserved when the instance is stopped. Use PRESERVED to keep the data, or DISK_SAVED_STATE_UNSPECIFIED for the default behavior. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="shielded_instance_initial_state" type="object">
      The shielded instance initial state for the disk, specifying the platform key (pk), key exchange key (kek), and database (db) that define the integrity policy for a Shielded VM instance booted from this disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dbs" type="list(object)">
          The list of database certificates for the shielded instance initial state. Each object in the list represents a certificate with fields for content and file type. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="content" type="string">
              The base64-encoded contents of the UEFI db (database) file for the shielded instance's initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="file_type" type="string">
              The file type of the database file. Valid values are BIN, UNDEFINED, and X509. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dbxs" type="list(object)">
          A list of DBX (forbidden signature database) entries for the shielded VM initial state. These entries specify the forbidden signatures that Secure Boot will deny during boot. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="content" type="string">
              The base64-encoded content of the DBX file (a forbidden signature database) used as part of the shielded VM initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="file_type" type="string">
              The file type of the DBXS, indicating whether the content is binary (BIN), an X.509 certificate (X509), or undefined (UNDEFINED). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="keks" type="list(object)">
          List of key exchange key (KEK) certificates used by the shielded instance's initial state. Each entry includes the certificate content and file type. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="content" type="string">
              The base64-encoded content of the key exchange key (KEK). This is used to provide the raw key data for shielded instance initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="file_type" type="string">
              The format of the key file. Allowed values are BIN, X509, and UNDEFINED. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="pk" type="object">
          The platform key (PK) entry, containing the base64-encoded content and file type of the platform key certificate used for verified boot. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="content" type="string">
              Base64-encoded content of the platform key (PK) certificate. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="file_type" type="string">
              The file type of the public key (pk) used in the shielded instance initial state. Valid values are BIN (binary), X509 (PEM certificate), or UNDEFINED. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source" type="string">
      A reference (URL or name) to the source disk, image, or snapshot used to create or attach this disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of the disk, either PERSISTENT (persistent disk) or SCRATCH (local SSD). Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="display_device" type="object">
  A set of Display Device options Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable_display" type="bool">
      Defines whether the instance has Display enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Specifies a fingerprint for this resource, which is essentially a hash of the instance's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update the instance. You must always provide an up-to-date fingerprint hash in order to update the instance. To see the latest fingerprint, make get() request to the instance. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="guest_accelerators" type="list(object)">
  A list of the type and count of accelerator cards attached to the instance. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="accelerator_count" type="number">
      The number of the guest accelerator cards of the specified type to attach to the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="accelerator_type" type="string">
      The type of accelerator (GPU) to attach to the instance, specified by its name or a full/partial URL to the accelerator type resource. For example, 'nvidia-tesla-k80' or 'zones/us-central1-a/acceleratorTypes/nvidia-tesla-p100'. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="hostname" type="string">
  Specifies the hostname of the instance. The specified hostname must be RFC1035 compliant. If hostname is not specified, the default hostname is [INSTANCE_NAME].c.[PROJECT_ID].internal when using the global DNS, and [INSTANCE_NAME].[ZONE].c.[PROJECT_ID].internal when using zonal DNS. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_encryption_key" type="object">
  The customer-supplied encryption key used to encrypt the instance's boot disk. This field is computed and output-only. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional.
    </ResponseField>
    <ResponseField name="kms_key_service_account" type="string">
      The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional.
    </ResponseField>
    <ResponseField name="raw_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional.
    </ResponseField>
    <ResponseField name="rsa_encrypted_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional.
    </ResponseField>
    <ResponseField name="sha256" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="key_revocation_action_type" type="string">
  KeyRevocationActionType of the instance. Supported options are "STOP" and "NONE". The default value is "NONE" if it is not specified. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#instance for instances. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  A fingerprint for this request, which is essentially a hash of the label's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update labels. You must always provide an up-to-date fingerprint hash in order to update or change labels. To see the latest fingerprint, make get() request to the instance. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels to apply to this instance. These can be later modified by the setLabels method. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_start_timestamp" type="string">
  Output only. [Output Only] Last start timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_stop_timestamp" type="string">
  Output only. [Output Only] Last stop timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_suspended_timestamp" type="string">
  Output only. [Output Only] Last suspended timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="local_ssd_encryption_mode" type="string">
  Specifies which method should be used for encrypting the Local SSDs attached to the VM. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="machine_type" type="string">
  Full or partial URL of the machine type resource to use for this instance, in the format:zones/zone/machineTypes/machine-type. This is provided by the client when the instance is created. For example, the following is a valid partial url to a predefined machine type: zones/us-central1-f/machineTypes/n1-standard-1 To create acustom machine type, provide a URL to a machine type in the following format, where CPUS is 1 or an even number up to 32 (2, 4, 6, ... 24, etc), and MEMORY is the total memory for this instance. Memory must be a multiple of 256 MB and must be supplied in MB (e.g. 5 GB of memory is 5120 MB): zones/zone/machineTypes/custom-CPUS-MEMORY For example: zones/us-central1-f/machineTypes/custom-4-5120 For a full list of restrictions, read theSpecifications for custom machine types. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="metadata" type="object">
  A metadata key/value entry. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="fingerprint" type="string">
      Specifies a fingerprint for this request, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve the resource. Optional.
    </ResponseField>
    <ResponseField name="items" type="list(object)">
      Array of key/value pairs. The total size of all keys and values must be less than 512 KB. Optional.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The metadata key for the instance metadata entry. This key, combined with the value, defines a key-value pair for custom instance metadata. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value for this metadata entry. This is the data associated with the metadata key on the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kind" type="string">
      Output only. [Output Only] Type of the resource. Always compute#metadata for metadata. Computed by GCP after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="min_cpu_platform" type="string">
  Specifies aminimum CPU platform for the VM instance. Applicable values are the friendly names of CPU platforms, such as minCpuPlatform: "Intel Haswell" or minCpuPlatform: "Intel Sandy Bridge". Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the resource, provided by the client when initially creating the resource. The resource name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network_interfaces" type="list(object)">
  An array of network configurations for this instance. These specify how interfaces are configured to interact with other network services, such as connecting to the internet. Multiple interfaces are supported per instance. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="access_configs" type="list(object)">
      Configures external IP addresses for this network interface. You can specify a static IP with `nat_ip`, or leave it unset to receive an ephemeral IP. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="external_ipv6" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="external_ipv6_prefix_length" type="number">
          The prefix length, in bits, of the external IPv6 address range assigned to this access configuration. It defines the network portion of the external IPv6 address. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of the access config, always set to 'compute#accessConfig'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of this access configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="nat_ip" type="string">
          The external IPv4 address assigned to the instance. Specify a static external IP (reserved) to assign it, or leave unset to have an ephemeral IP automatically assigned. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_tier" type="string">
          The network tier assigned to the access config's external IP. Allowed values are FIXED_STANDARD, PREMIUM, STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD, which control the pricing and performance tier for the external IP. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="public_ptr_domain_name" type="string">
          Specifies a custom DNS PTR record for the public IP address of this access config. If not set, the default PTR record for the external IP is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="security_policy" type="string">
          The name of the Cloud Armor security policy to apply to this access config. This field is only applicable if the access_config type is EXTERNAL_MANAGED. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="set_public_ptr" type="bool">
          Indicates whether a public DNS PTR record should be set for the external IP address associated with this access config. Set to true to enable public PTR record creation. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of access configuration. Valid values are DIRECT_IPV6 for direct IPv6 access and ONE_TO_ONE_NAT for standard one-to-one NAT (external IP). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="alias_ip_ranges" type="list(object)">
      A list of alias IP ranges to assign to this network interface. Each alias IP range allows the instance to use additional internal IP addresses. Typically includes an IP CIDR range and optionally a subnetwork range name. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ip_cidr_range" type="string">
          The IP CIDR range for this alias IP, specified in CIDR notation (e.g., '10.0.0.0/24'). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnetwork_range_name" type="string">
          The name of the subnetwork secondary range to use for this alias IP range. This range must have an internal IP address type. If not specified, the primary range of the subnetwork is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="alias_ipv6_ranges" type="list(object)">
      A list of IPv6 alias IP ranges to assign to this network interface. Each object defines an IPv6 CIDR range that the interface can use as an additional address. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ip_cidr_range" type="string">
          The IPv6 CIDR range for this alias IP range, specified in CIDR notation (e.g., /64). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnetwork_range_name" type="string">
          The name of the subnetwork range from which the IPv6 alias range is allocated. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enable_vpc_scoped_dns" type="bool">
      Enable VPC-scoped DNS for the network interface. When true, DNS queries from the instance are resolved using the VPC network's DNS configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="fingerprint" type="string">
      The fingerprint of the network interface, used for optimistic locking. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="igmp_query" type="string">
      Configures the IGMP query mode for this network interface. Possible values are IGMP_QUERY_DISABLED and IGMP_QUERY_V2. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="internal_ipv6_prefix_length" type="number">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="ipv6_access_configs" type="list(object)">
      List of IPv6 access configurations for the network interface. Each configuration sets the external IPv6 address options, such as the network tier (e.g., PREMIUM or STANDARD) and optional public DNS domain name. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="external_ipv6" type="string">
          The external IPv6 address assigned to the instance's network interface in this IPv6 access configuration. This is a public IPv6 address used for internet-facing communication. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="external_ipv6_prefix_length" type="number">
          The prefix length of the external IPv6 address assigned to the instance's access config. Typically set to 32 for a /32 IPv6 address. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="nat_ip" type="string">
          The external IPv6 address assigned to the instance. If not specified, an ephemeral IPv6 address is automatically assigned. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_tier" type="string">
          The network tier for this IPv6 access config. Valid values are FIXED_STANDARD, PREMIUM, STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="public_ptr_domain_name" type="string">
          The domain name for the reverse DNS (PTR) record of the instance's public IPv6 address. When set, this custom domain is used for PTR lookups; otherwise, a Google Cloud default PTR record is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="security_policy" type="string">
          The name of the security policy (Cloud Armor policy) to apply to this IPv6 access config. This policy can be used to filter and protect traffic to the instance's public IPv6 address. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="set_public_ptr" type="bool">
          Whether to set the public PTR record for the IPv6 address. If set to true, a PTR record is created for the external IPv6 address to enable reverse DNS lookup. Defaults to false. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of IPv6 access configuration. Allowed values are DIRECT_IPV6 and ONE_TO_ONE_NAT. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ipv6_access_type" type="string">
      The type of IPv6 access. Valid values are EXTERNAL and INTERNAL. EXTERNAL means the IPv6 address is publicly accessible, while INTERNAL means it is private to the VPC network. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ipv6_address" type="string">
      The external IPv6 address assigned to this network interface. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kind" type="string">
      The kind of the network interface resource. It is always set to 'compute#networkInterface'. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name assigned to this network interface, such as 'nic0'. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="network" type="string">
      The name or self_link of the VPC network to attach this interface to. If subnetwork is not set, this field is required; otherwise, the network is inferred from the subnetwork. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="network_attachment" type="string">
      The URL of the network attachment that this network interface should use. This can be specified instead of network or subnetwork when the interface is attached to a Network Attachment. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="network_ip" type="string">
      The internal IPv4 address to assign to this network interface. If left unspecified, Compute Engine automatically assigns an ephemeral internal IP address. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="nic_type" type="string">
      The type of network interface controller (NIC) to use for this network interface. Allowed values: GVNIC, IDPF, IRDMA, MRDMA, UNSPECIFIED_NIC_TYPE, VIRTIO_NET. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="parent_nic_name" type="string">
      The name of the parent network interface that this interface is attached to or derived from, used in virtual NIC configurations (e.g., gVNIC). Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="queue_count" type="number">
      The number of queues to create for this network interface. This field is supported only for gVNIC NICs and is used to configure the number of queue pairs for the interface. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="service_class_id" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="stack_type" type="string">
      The IP stack type for the network interface. Valid values are IPV4_IPV6 (dual-stack), IPV4_ONLY, or IPV6_ONLY. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnetwork" type="string">
      The name or self_link of the subnetwork to attach to this network interface. If not specified, the default subnetwork for the network is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="vlan" type="number">
      The VLAN ID used for this network interface, applicable when the interface is attached to a VLAN attachment. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network_performance_config" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="total_egress_bandwidth_tier" type="string">
      The tier of total egress bandwidth for the instance. Valid values are DEFAULT (standard) and TIER_1 (higher bandwidth). Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="params" type="object">
  Additional instance params. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="request_valid_for_duration" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the instance. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="private_ipv6_google_access" type="string">
  The private IPv6 google access type for the VM. If not specified, use INHERIT_FROM_SUBNETWORK as default. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reservation_affinity" type="object">
  Specifies the reservations that this instance can consume from. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="consume_reservation_type" type="string">
      Specifies the type of reservation from which this instance can consume resources: ANY_RESERVATION (default),SPECIFIC_RESERVATION, or NO_RESERVATION. See Consuming reserved instances for examples. Optional.
    </ResponseField>
    <ResponseField name="key" type="string">
      Corresponds to the label key of a reservation resource. To target aSPECIFIC_RESERVATION by name, specifygoogleapis.com/reservation-name as the key and specify the name of your reservation as its value. Optional.
    </ResponseField>
    <ResponseField name="values" type="list(string)">
      Corresponds to the label values of a reservation resource. This can be either a name to a reservation in the same project or "projects/different-project/reservations/some-reservation-name" to target a shared reservation in the same zone but in a different project. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_policies" type="list(string)">
  Resource policies applied to this instance. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_status" type="object">
  Contains output only fields. Use this sub-message for actual values set on Instance attributes as compared to the value requested by the user (intent) in their instance CRUD calls. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="effective_instance_metadata" type="object">
      Effective values of predefined metadata keys for an instance. Optional.
      <Expandable title="properties">
        <ResponseField name="block_project_ssh_keys_metadata_value" type="bool">
          Effective block-project-ssh-keys value at Instance level. Optional.
        </ResponseField>
        <ResponseField name="enable_guest_attributes_metadata_value" type="bool">
          Effective enable-guest-attributes value at Instance level. Optional.
        </ResponseField>
        <ResponseField name="enable_os_inventory_metadata_value" type="bool">
          Effective enable-os-inventory value at Instance level. Optional.
        </ResponseField>
        <ResponseField name="enable_osconfig_metadata_value" type="bool">
          Effective enable-osconfig value at Instance level. Optional.
        </ResponseField>
        <ResponseField name="enable_oslogin_metadata_value" type="bool">
          Effective enable-oslogin value at Instance level. Optional.
        </ResponseField>
        <ResponseField name="gce_container_declaration_metadata_value" type="bool">
          Effective gce-container-declaration value at Instance level. Optional.
        </ResponseField>
        <ResponseField name="serial_port_enable_metadata_value" type="bool">
          Effective serial-port-enable value at Instance level. Optional.
        </ResponseField>
        <ResponseField name="serial_port_logging_enable_metadata_value" type="bool">
          Effective serial-port-logging-enable value at Instance level. Optional.
        </ResponseField>
        <ResponseField name="vm_dns_setting_metadata_value" type="string">
          Effective VM DNS setting at Instance level. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="physical_host" type="string">
      Output only. [Output Only] The precise location of your instance within the zone's data center, including the block, sub-block, and host. The field is formatted as follows: blockId/subBlockId/hostId. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="physical_host_topology" type="object">
      Represents the physical host topology of the host on which the VM is running. Optional.
      <Expandable title="properties">
        <ResponseField name="additional_attributes" type="object">
          Additional location information of the running instance. Optional.
          <Expandable title="properties">
            <ResponseField name="accelerator_topology_ids" type="map(string)">
              Output only. The IDs of the accelerator topologies the instance belongs to. For example The key will be topologies like "4x4", "2x2x2" and the value will be the location ID of the topologies. Computed by GCP after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="block" type="string">
          [Output Only] The ID of the block in which the running instance is located. Instances within the same block experience low network latency. Optional.
        </ResponseField>
        <ResponseField name="cluster" type="string">
          [Output Only] The global name of the Compute Engine cluster where the running instance is located. Optional.
        </ResponseField>
        <ResponseField name="host" type="string">
          [Output Only] The ID of the host on which the running instance is located. Instances on the same host experience the lowest possible network latency. Optional.
        </ResponseField>
        <ResponseField name="subblock" type="string">
          [Output Only] The ID of the sub-block in which the running instance is located. Instances in the same sub-block experience lower network latency than instances in the same block. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reservation_consumption_info" type="object">
      Reservation consumption information that the instance is consuming from. Optional.
      <Expandable title="properties">
        <ResponseField name="consumed_reservation" type="string">
          Output only. [Output Only] The full resource name of the reservation that this instance is consuming from. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="consumed_reservation_block" type="string">
          Output only. [Output Only] The full resource name of the reservation block that this instance is consuming from. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="consumed_reservation_sub_block" type="string">
          Output only. [Output Only] The full resource name of the reservation sub-block that this instance is consuming from. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scheduling" type="object">
      The scheduling configuration of the instance as reported by the API in the resource_status block. This includes computed values such as host error timeout seconds and node affinities. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="availability_domain" type="number">
          Specifies the availability domain to place the instance in. The value must be a number between 1 and the number of availability domains specified in the spread placement policy attached to the instance. Optional.
        </ResponseField>
        <ResponseField name="graceful_shutdown_timestamp" type="string">
          Output only. Specifies the timestamp, when the instance will start graceful shutdown process, in RFC3339 text format. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="termination_timestamp" type="string">
          Time in future when the instance will be terminated inRFC3339 text format. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="shutdown_details" type="object">
      Specifies if the instance is in `PENDING_STOP` state or there is a programmed stop scheduled. Optional.
      <Expandable title="properties">
        <ResponseField name="max_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_timestamp" type="string">
          Past timestamp indicating the beginning of current `stopState` in RFC3339 text format. Optional.
        </ResponseField>
        <ResponseField name="stop_state" type="string">
          Current stopping state of the instance. Optional.
        </ResponseField>
        <ResponseField name="target_state" type="string">
          Target instance state. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="upcoming_maintenance" type="object">
      Upcoming Maintenance notification information. Optional.
      <Expandable title="properties">
        <ResponseField name="can_reschedule" type="bool">
          Indicates if the maintenance can be customer triggered. Optional.
        </ResponseField>
        <ResponseField name="latest_window_start_time" type="string">
          The latest time for the planned maintenance window to start. This timestamp value is in RFC3339 text format. Optional.
        </ResponseField>
        <ResponseField name="maintenance_on_shutdown" type="bool">
          Indicates whether the UpcomingMaintenance will be triggered on VM shutdown. Optional.
        </ResponseField>
        <ResponseField name="maintenance_reasons" type="list(string)">
          The reasons for the maintenance. Only valid for vms. Optional.
        </ResponseField>
        <ResponseField name="maintenance_status" type="string">
          The status of upcoming maintenance for the instance, indicating whether maintenance is ongoing, pending, or unknown. Possible values are ONGOING, PENDING, and UNKNOWN. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Defines the type of maintenance. Optional.
        </ResponseField>
        <ResponseField name="window_end_time" type="string">
          The time by which the maintenance disruption will be completed. This timestamp value is in RFC3339 text format. Optional.
        </ResponseField>
        <ResponseField name="window_start_time" type="string">
          The current start time of the maintenance window. This timestamp value is in RFC3339 text format. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. [Output Only] Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. [Output Only] Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="scheduling" type="object">
  Sets the scheduling options for an Instance. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="automatic_restart" type="bool">
      Specifies whether the instance should be automatically restarted if it is terminated by Compute Engine (not terminated by a user). You can only set the automatic restart option for standard instances.Preemptible instances cannot be automatically restarted. By default, this is set to true so an instance is automatically restarted if it is terminated by Compute Engine. Optional.
    </ResponseField>
    <ResponseField name="availability_domain" type="number">
      Specifies the availability domain to place the instance in. The value must be a number between 1 and the number of availability domains specified in the spread placement policy attached to the instance. Optional.
    </ResponseField>
    <ResponseField name="graceful_shutdown" type="object">
      The configuration for gracefully shutting down the instance. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Opts-in for graceful shutdown. Optional.
        </ResponseField>
        <ResponseField name="max_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="host_error_timeout_seconds" type="number">
      Specify the time in seconds for host error detection, the value must be within the range of [90, 330] with the increment of 30, if unset, the default behavior of host error recovery will be used. Optional.
    </ResponseField>
    <ResponseField name="instance_termination_action" type="string">
      Specifies the termination action for the instance. Optional.
    </ResponseField>
    <ResponseField name="local_ssd_recovery_timeout" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="location_hint" type="string">
      An opaque location hint used to place the instance close to other resources. This field is for use by internal tools that use the public API. Optional.
    </ResponseField>
    <ResponseField name="max_run_duration" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="min_node_cpus" type="number">
      The minimum number of virtual CPUs this instance will consume when running on a sole-tenant node. Optional.
    </ResponseField>
    <ResponseField name="node_affinities" type="list(object)">
      A set of node affinity and anti-affinity configurations. Refer toConfiguring node affinity for more information. Overrides reservationAffinity. Optional.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The key for the node affinity, which corresponds to a node label key in the Compute Engine environment. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operator" type="string">
          The operator for the node affinity condition. Valid values are IN (matches when the node is in the specified values), NOT_IN (matches when the node is not in the specified values), and OPERATOR_UNSPECIFIED (unspecified). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="values" type="list(string)">
          The list of values for the node affinity key. This field is required when the operator is IN or NOT_IN. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="on_host_maintenance" type="string">
      Defines the maintenance behavior for this instance. For standard instances, the default behavior is MIGRATE. Forpreemptible instances, the default and only possible behavior is TERMINATE. For more information, see Set VM host maintenance policy. Optional.
    </ResponseField>
    <ResponseField name="on_instance_stop_action" type="object">
      Defines the behaviour for instances with the instance_termination_actionSTOP. Optional.
      <Expandable title="properties">
        <ResponseField name="discard_local_ssd" type="bool">
          If true, the contents of any attached Local SSD disks will be discarded else, the Local SSD data will be preserved when the instance is stopped at the end of the run duration/termination time. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preemptible" type="bool">
      Defines whether the instance is preemptible. This can only be set during instance creation or while the instance isstopped and therefore, in a `TERMINATED` state. SeeInstance Life Cycle for more information on the possible instance states. Optional.
    </ResponseField>
    <ResponseField name="preemption_notice_duration" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_model" type="string">
      Specifies the provisioning model of the instance. Optional.
    </ResponseField>
    <ResponseField name="skip_guest_os_shutdown" type="bool">
      Default is false and there will be 120 seconds between GCE ACPI G2 Soft Off and ACPI G3 Mechanical Off for Standard VMs and 30 seconds for Spot VMs. Optional.
    </ResponseField>
    <ResponseField name="termination_time" type="string">
      Specifies the timestamp, when the instance will be terminated, inRFC3339 text format. If specified, the instance termination action will be performed at the termination time. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for this resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_accounts" type="list(object)">
  A list of service accounts, with their specified scopes, authorized for this instance. Only one service account per VM instance is supported. Service accounts generate access tokens that can be accessed through the metadata server and used to authenticate applications on the instance. SeeService Accounts for more information. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="email" type="string">
      The email address of the service account to associate with the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="scopes" type="list(string)">
      List of OAuth 2.0 scopes that should be granted to the service account for accessing Google Cloud APIs. For example, 'https://www.googleapis.com/auth/compute.readonly' or 'https://www.googleapis.com/auth/cloud-platform'. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="shielded_instance_config" type="object">
  A set of Shielded Instance options. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable_integrity_monitoring" type="bool">
      Defines whether the instance has integrity monitoring enabled.Enabled by default. Optional.
    </ResponseField>
    <ResponseField name="enable_secure_boot" type="bool">
      Defines whether the instance has Secure Boot enabled.Disabled by default. Optional.
    </ResponseField>
    <ResponseField name="enable_vtpm" type="bool">
      Defines whether the instance has the vTPM enabled.Enabled by default. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="shielded_instance_integrity_policy" type="object">
  The policy describes the baseline against which Instance boot integrity is measured. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="update_auto_learn_policy" type="bool">
      Updates the integrity policy baseline using the measurements from the VM instance's most recent boot. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_machine_image" type="string">
  Source machine image Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_machine_image_encryption_key" type="object">
  The customer-supplied encryption key for the source machine image used to create the instance. This field is output-only and reflects the encryption key details of the source image. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional.
    </ResponseField>
    <ResponseField name="kms_key_service_account" type="string">
      The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional.
    </ResponseField>
    <ResponseField name="raw_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional.
    </ResponseField>
    <ResponseField name="rsa_encrypted_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional.
    </ResponseField>
    <ResponseField name="sha256" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_restricted" type="bool">
  Output only. [Output Only] Whether a VM has been restricted for start because Compute Engine has detected suspicious activity. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] The status of the instance. One of the following values: PROVISIONING, STAGING,RUNNING, STOPPING, SUSPENDING,SUSPENDED, REPAIRING, andTERMINATED. For more information about the status of the instance, see Instance life cycle. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status_message" type="string">
  Output only. [Output Only] An optional, human-readable explanation of the status. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tags" type="object">
  A set of instance tags. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="fingerprint" type="string">
      Specifies a fingerprint for this request, which is essentially a hash of the tags' contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update tags. You must always provide an up-to-date fingerprint hash in order to update or change tags. To see the latest fingerprint, make get() request to the instance. Optional.
    </ResponseField>
    <ResponseField name="items" type="list(string)">
      An array of tags. Each tag must be 1-63 characters long, and comply with RFC1035. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workload_identity_config" type="object">
  An output-only object containing the workload identity configuration for the instance, automatically computed and not user-settable. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="identity" type="string">
      The email address of the service account to attach to the instance for workload identity. This enables the instance to use workload identity federation. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="identity_certificate_enabled" type="bool">
      Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  Output only. [Output Only] URL of the zone where the instance resides. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>
