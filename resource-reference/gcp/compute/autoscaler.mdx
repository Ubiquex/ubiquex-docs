---
title: "google_compute_autoscaler"
description: "Real, generated bindings for `google_compute_autoscaler`."
---

`google_compute_autoscaler` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own autoscaler"})

    		ubx.Resource(compute.Autoscaler, "ci-runner", compute.AutoscalerConfig{
    			Name:        "ci-runner",
    			Target:      "example",
    			Description: "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Autoscaler } from "jsr:@ubx/sdk-google/google/compute/autoscaler";

    export default stack("payments", () => {
      intent({ summary: "payments own autoscaler" });

      resource(Autoscaler, "ci-runner", {
        name: "ci-runner",
        target: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import Autoscaler, AutoscalerConfig

    def describe():
        ubx.intent("payments own autoscaler")

        ubx.resource(Autoscaler, "ci-runner", AutoscalerConfig(
            name="ci-runner",
            target="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_autoscaler called "ci-runner", with target
    example, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="autoscaling_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cooldown_period" type="number">
    </ResponseField>
    <ResponseField name="cpu_utilization" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="predictive_method" type="string">
        </ResponseField>
        <ResponseField name="target" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="load_balancing_utilization" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="target" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_replicas" type="number" required>
    </ResponseField>
    <ResponseField name="metric" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="filter" type="string">
        </ResponseField>
        <ResponseField name="name" type="string" required>
        </ResponseField>
        <ResponseField name="single_instance_assignment" type="number">
        </ResponseField>
        <ResponseField name="target" type="number">
        </ResponseField>
        <ResponseField name="type" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="min_replicas" type="number" required>
    </ResponseField>
    <ResponseField name="mode" type="string">
    </ResponseField>
    <ResponseField name="scale_in_control" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="max_scaled_in_replicas" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="fixed" type="number">
            </ResponseField>
            <ResponseField name="percent" type="number">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="time_window_sec" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scaling_schedules" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="description" type="string">
        </ResponseField>
        <ResponseField name="disabled" type="bool">
        </ResponseField>
        <ResponseField name="duration_sec" type="number" required>
        </ResponseField>
        <ResponseField name="min_required_replicas" type="number" required>
        </ResponseField>
        <ResponseField name="name" type="string" required>
        </ResponseField>
        <ResponseField name="schedule" type="string" required>
        </ResponseField>
        <ResponseField name="time_zone" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stabilization_period" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource. The name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target" type="string" required>
  URL of the managed instance group that this autoscaler will scale. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  URL of the zone where the instance group resides. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="autoscaling_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cooldown_period" type="number">
    </ResponseField>
    <ResponseField name="cpu_utilization" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="predictive_method" type="string">
        </ResponseField>
        <ResponseField name="target" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="load_balancing_utilization" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="target" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_replicas" type="number" required>
    </ResponseField>
    <ResponseField name="metric" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="filter" type="string">
        </ResponseField>
        <ResponseField name="name" type="string" required>
        </ResponseField>
        <ResponseField name="single_instance_assignment" type="number">
        </ResponseField>
        <ResponseField name="target" type="number">
        </ResponseField>
        <ResponseField name="type" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="min_replicas" type="number" required>
    </ResponseField>
    <ResponseField name="mode" type="string">
    </ResponseField>
    <ResponseField name="scale_in_control" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="max_scaled_in_replicas" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="fixed" type="number">
            </ResponseField>
            <ResponseField name="percent" type="number">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="time_window_sec" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scaling_schedules" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="description" type="string">
        </ResponseField>
        <ResponseField name="disabled" type="bool">
        </ResponseField>
        <ResponseField name="duration_sec" type="number" required>
        </ResponseField>
        <ResponseField name="min_required_replicas" type="number" required>
        </ResponseField>
        <ResponseField name="name" type="string" required>
        </ResponseField>
        <ResponseField name="schedule" type="string" required>
        </ResponseField>
        <ResponseField name="time_zone" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stabilization_period" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Creation timestamp in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  URL of the zone where the instance group resides. Optional; if omitted, computed by GCP.
</ResponseField>
