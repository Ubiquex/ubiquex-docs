---
title: "google_compute_ssl_policy"
description: "Real, generated bindings for `google_compute_ssl_policy`."
---

`google_compute_ssl_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ssl policy"})

    		ubx.Resource(compute.SslPolicy, "ci-runner", compute.SslPolicyConfig{
    			Name:                   "ci-runner",
    			CustomFeatures:         []string{"example"},
    			Description:            "Managed by ubx.",
    			MinTlsVersion:          "example",
    			PostQuantumKeyExchange: "example",
    			Profile:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SslPolicy } from "jsr:@ubx/sdk-google/google/compute/ssl_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own ssl policy" });

      resource(SslPolicy, "ci-runner", {
        name: "ci-runner",
        customFeatures: ["example"],
        description: "Managed by ubx.",
        minTlsVersion: "example",
        postQuantumKeyExchange: "example",
        profile: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import SslPolicy, SslPolicyConfig

    def describe():
        ubx.intent("payments own ssl policy")

        ubx.resource(SslPolicy, "ci-runner", SslPolicyConfig(
            name="ci-runner",
            custom_features=["example"],
            description="Managed by ubx.",
            min_tls_version="example",
            post_quantum_key_exchange="example",
            profile="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_ssl_policy called "ci-runner", with custom
    features a set value, description Managed by ubx., min tls
    version example, post quantum key exchange example, profile
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="custom_features" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="min_tls_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="post_quantum_key_exchange" type="string">
  Optional.
</ResponseField>

<ResponseField name="profile" type="string">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_timestamp" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enabled_features" type="set(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
