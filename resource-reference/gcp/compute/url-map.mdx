---
title: "google_compute_url_map"
description: "`google_compute_url_map` defines the real, host- and path-based routing rules a global HTTP(S) load balancer uses to decide which backend service..."
---

`google_compute_url_map` defines the real, host- and path-based routing rules a global HTTP(S) load balancer uses to decide which backend service actually handles a given incoming request.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own url map"})

    		ubx.Resource(compute.UrlMap, "example", compute.UrlMapConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UrlMap } from "@ubx/sdk-google/google/compute/url_map";

    export default stack("example", () => {
      intent({ summary: "example own url map" });

      resource(UrlMap, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import UrlMap, UrlMapConfig

    def describe():
        ubx.intent("example own url map")

        ubx.resource(UrlMap, "example", UrlMapConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_url_map called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="default_custom_error_response_policy" type="object">
  Specifies the custom error response policy that must be applied when the backend service or backend bucket responds with an error. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="error_response_rules" type="list(object)">
      Specifies rules for returning error responses. In a given policy, if you specify rules for both a range of error codes as well as rules for specific error codes then rules with specific error codes have a higher priority. For example, assume that you configure a rule for 401 (Un-authorized) code, and another for all 4 series error codes (4XX). If the backend service returns a401, then the rule for 401 will be applied. However if the backend service returns a 403, the rule for4xx takes effect. Optional.
      <Expandable title="properties">
        <ResponseField name="match_response_codes" type="list(string)">
          A list of HTTP response codes (as strings) that this error response rule applies to. When a backend returns one of these codes, the corresponding custom error response is served. For example, '404' or '503'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="override_response_code" type="number">
          The HTTP status code that will be returned to the client instead of the original error code when this error response rule is matched. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="path" type="string">
          The full path to the file to use as the custom error page, for example /errors/my-error.html. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_service" type="string">
      The full or partial URL to the BackendBucket resource that contains the custom error content. Examples are: - https://www.googleapis.com/compute/v1/projects/project/global/backendBuckets/myBackendBucket - compute/v1/projects/project/global/backendBuckets/myBackendBucket - global/backendBuckets/myBackendBucket If errorService is not specified at lower levels likepathMatcher, pathRule and routeRule, an errorService specified at a higher level in theUrlMap will be used. IfUrlMap.defaultCustomErrorResponsePolicy contains one or moreerrorResponseRules[], it must specifyerrorService. If load balancer cannot reach the backendBucket, a simple Not Found Error will be returned, with the original response code (oroverrideResponseCode if configured). errorService is not supported for internal or regionalHTTP/HTTPS load balancers. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_route_action" type="object">
  The default route action for the URL map, applied to traffic that does not match any host or path rules. This computed field is populated by the API with the actual default action configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cache_policy" type="object">
      Message containing CachePolicy configuration for URL Map's Route Action. Optional.
      <Expandable title="properties">
        <ResponseField name="cache_bypass_request_header_names" type="list(string)">
          Bypass the cache when the specified request headers are matched by name, e.g. Pragma or Authorization headers. Values are case-insensitive. Up to 5 header names can be specified. The cache is bypassed for all `cacheMode` values. Optional.
        </ResponseField>
        <ResponseField name="cache_key_policy" type="object">
          Message containing what to include in the cache key for a request for Cache Policy defined on Route Action. Optional.
          <Expandable title="properties">
            <ResponseField name="excluded_query_parameters" type="list(string)">
              Names of query string parameters to exclude in cache keys. All other parameters will be included. Either specify `excludedQueryParameters` or `includedQueryParameters`, not both. '&' and '=' will be percent encoded and not treated as delimiters. Note: This field applies to routes that use backend services. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. For routes that point to a Backend Bucket, use `includedQueryParameters` to define which parameters should be part of the cache key. Optional.
            </ResponseField>
            <ResponseField name="include_host" type="bool">
              If true, requests to different hosts will be cached separately. Note: This setting is only applicable to routes that use a Backend Service. It does not affect requests served by a Backend Bucket, as the host is never included in a Backend Bucket's cache key. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. Optional.
            </ResponseField>
            <ResponseField name="include_protocol" type="bool">
              If true, http and https requests will be cached separately. Note: This setting is only applicable to routes that use a Backend Service. It does not affect requests served by a Backend Bucket, as the protocol is never included in a Backend Bucket's cache key. Attempting to set on a route that points exclusively to Backend Buckets will result in a configuration error. Optional.
            </ResponseField>
            <ResponseField name="include_query_string" type="bool">
              If true, include query string parameters in the cache key according to `includedQueryParameters` and `excludedQueryParameters`. If neither is set, the entire query string will be included. If false, the query string will be excluded from the cache key entirely. Note: This field applies to routes that use backend services. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. For routes that point to a Backend Bucket, use `includedQueryParameters` to define which parameters should be part of the cache key. Optional.
            </ResponseField>
            <ResponseField name="included_cookie_names" type="list(string)">
              Allows HTTP cookies (by name) to be used in the cache key. The name=value pair will be used in the cache key Cloud CDN generates. Note: This setting is only applicable to routes that use a Backend Service. It does not affect requests served by a Backend Bucket. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. Up to 5 cookie names can be specified. Optional.
            </ResponseField>
            <ResponseField name="included_header_names" type="list(string)">
              Allows HTTP request headers (by name) to be used in the cache key. Optional.
            </ResponseField>
            <ResponseField name="included_query_parameters" type="list(string)">
              Names of query string parameters to include in cache keys. All other parameters will be excluded. Either specify `includedQueryParameters` or `excludedQueryParameters`, not both. '&' and '=' will be percent encoded and not treated as delimiters. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="cache_mode" type="string">
          Specifies the cache setting for all responses from this route. If not specified, Cloud CDN uses `CACHE_ALL_STATIC` mode. Optional.
        </ResponseField>
        <ResponseField name="client_ttl" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="default_ttl" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="max_ttl" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="negative_caching" type="bool">
          Negative caching allows per-status code TTLs to be set, in order to apply fine-grained caching for common errors or redirects. This can reduce the load on your origin and improve end-user experience by reducing response latency. When the `cacheMode` is set to `CACHE_ALL_STATIC` or `USE_ORIGIN_HEADERS`, negative caching applies to responses with the specified response code that lack any Cache-Control, Expires, or Pragma: no-cache directives. When the `cacheMode` is set to `FORCE_CACHE_ALL`, negative caching applies to all responses with the specified response code, and overrides any caching headers. By default, Cloud CDN applies the following TTLs to these HTTP status codes: * 300 (Multiple Choice), 301, 308 (Permanent Redirects): 10m * 404 (Not Found), 410 (Gone), 451 (Unavailable For Legal Reasons): 120s * 405 (Method Not Found), 501 (Not Implemented): 60s These defaults can be overridden in `negativeCachingPolicy`. If not specified, Cloud CDN applies negative caching by default. Optional.
        </ResponseField>
        <ResponseField name="negative_caching_policy" type="list(object)">
          Sets a cache TTL for the specified HTTP status code. `negativeCaching` must be enabled to configure `negativeCachingPolicy`. Omitting the policy and leaving `negativeCaching` enabled will use Cloud CDN's default cache TTLs. Note that when specifying an explicit `negativeCachingPolicy`, you should take care to specify a cache TTL for all response codes that you wish to cache. Cloud CDN will not apply any default negative caching when a policy exists. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="number">
              The HTTP status code to cache as a negative response. For example, 404 or 500. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ttl" type="object">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional seconds component of the TTL duration, in nanoseconds. Must be between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of seconds in the TTL duration for the negative caching policy, represented as a string (e.g., "300"). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_coalescing" type="bool">
          If true then Cloud CDN will combine multiple concurrent cache fill requests into a small number of requests to the origin. If not specified, Cloud CDN applies request coalescing by default. Optional.
        </ResponseField>
        <ResponseField name="serve_while_stale" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cors_policy" type="object">
      The specification for allowing client-side cross-origin requests. For more information about the W3C recommendation for cross-origin resource sharing (CORS), see Fetch API Living Standard. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_credentials" type="bool">
          In response to a preflight request, setting this to true indicates that the actual request can include user credentials. This field translates to the Access-Control-Allow-Credentials header. Default is false. Optional.
        </ResponseField>
        <ResponseField name="allow_headers" type="list(string)">
          Specifies the content for the Access-Control-Allow-Headers header. Optional.
        </ResponseField>
        <ResponseField name="allow_methods" type="list(string)">
          Specifies the content for the Access-Control-Allow-Methods header. Optional.
        </ResponseField>
        <ResponseField name="allow_origin_regexes" type="list(string)">
          Specifies a regular expression that matches allowed origins. For more information, see regular expression syntax. An origin is allowed if it matches either an item inallowOrigins or an item inallowOriginRegexes. Regular expressions can only be used when the loadBalancingScheme is set to INTERNAL_SELF_MANAGED. Optional.
        </ResponseField>
        <ResponseField name="allow_origins" type="list(string)">
          Specifies the list of origins that is allowed to do CORS requests. An origin is allowed if it matches either an item inallowOrigins or an item inallowOriginRegexes. Optional.
        </ResponseField>
        <ResponseField name="disabled" type="bool">
          If true, disables the CORS policy. The default value is false, which indicates that the CORS policy is in effect. Optional.
        </ResponseField>
        <ResponseField name="expose_headers" type="list(string)">
          Specifies the content for the Access-Control-Expose-Headers header. Optional.
        </ResponseField>
        <ResponseField name="max_age" type="number">
          Specifies how long results of a preflight request can be cached in seconds. This field translates to the Access-Control-Max-Age header. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fault_injection_policy" type="object">
      The specification for fault injection introduced into traffic to test the resiliency of clients to backend service failure. As part of fault injection, when clients send requests to a backend service, delays can be introduced by the load balancer on a percentage of requests before sending those request to the backend service. Similarly requests from clients can be aborted by the load balancer for a percentage of requests. Optional.
      <Expandable title="properties">
        <ResponseField name="abort" type="object">
          Specification for how requests are aborted as part of fault injection. Optional.
          <Expandable title="properties">
            <ResponseField name="http_status" type="number">
              The HTTP status code used to abort the request. The value must be from 200 to 599 inclusive. For gRPC protocol, the gRPC status code is mapped to HTTP status code according to this mapping table. HTTP status 200 is mapped to gRPC status UNKNOWN. Injecting an OK status is currently not supported by Traffic Director. Optional.
            </ResponseField>
            <ResponseField name="percentage" type="number">
              The percentage of traffic for connections, operations, or requests that is aborted as part of fault injection. The value must be from 0.0 to 100.0 inclusive. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="delay" type="object">
          Specifies the delay introduced by the load balancer before forwarding the request to the backend service as part of fault injection. Optional.
          <Expandable title="properties">
            <ResponseField name="fixed_delay" type="object">
              A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="percentage" type="number">
              The percentage of traffic for connections, operations, or requests for which a delay is introduced as part of fault injection. The value must be from 0.0 to 100.0 inclusive. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_stream_duration" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="request_mirror_policy" type="object">
      A policy that specifies how requests intended for the route's backends are shadowed to a separate mirrored backend service. The load balancer doesn't wait for responses from the shadow service. Before sending traffic to the shadow service, the host or authority header is suffixed with-shadow. Optional.
      <Expandable title="properties">
        <ResponseField name="backend_service" type="string">
          The full or partial URL to the BackendService resource being mirrored to. The backend service configured for a mirroring policy must reference backends that are of the same type as the original backend service matched in the URL map. Serverless NEG backends are not currently supported as a mirrored backend service. Optional.
        </ResponseField>
        <ResponseField name="mirror_percent" type="number">
          The percentage of requests to be mirrored to `backend_service`. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="retry_policy" type="object">
      The retry policy associates with HttpRouteRule Optional.
      <Expandable title="properties">
        <ResponseField name="num_retries" type="number">
          Specifies the allowed number retries. This number must be &gt; 0. If not specified, defaults to 1. Optional.
        </ResponseField>
        <ResponseField name="per_try_timeout" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="retry_conditions" type="list(string)">
          Specifies one or more conditions when this retry policy applies. Valid values are: - 5xx: retry is attempted if the instance or endpoint responds with any 5xx response code, or if the instance or endpoint does not respond at all. For example, disconnects, reset, read timeout, connection failure, and refused streams. - gateway-error: Similar to 5xx, but only applies to response codes 502, 503 or504. - connect-failure: a retry is attempted on failures connecting to the instance or endpoint. For example, connection timeouts. - retriable-4xx: a retry is attempted if the instance or endpoint responds with a 4xx response code. The only error that you can retry is error code 409. - refused-stream: a retry is attempted if the instance or endpoint resets the stream with a REFUSED_STREAM error code. This reset type indicates that it is safe to retry. - cancelled: a retry is attempted if the gRPC status code in the response header is set to cancelled. - deadline-exceeded: a retry is attempted if the gRPC status code in the response header is set todeadline-exceeded. - internal: a retry is attempted if the gRPC status code in the response header is set tointernal. - resource-exhausted: a retry is attempted if the gRPC status code in the response header is set toresource-exhausted. - unavailable: a retry is attempted if the gRPC status code in the response header is set tounavailable. Only the following codes are supported when the URL map is bound to target gRPC proxy that has validateForProxyless field set to true. - cancelled - deadline-exceeded - internal - resource-exhausted - unavailable Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="timeout" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="url_rewrite" type="object">
      The spec for modifying the path before sending the request to the matched backend service. Optional.
      <Expandable title="properties">
        <ResponseField name="host_rewrite" type="string">
          Before forwarding the request to the selected service, the request's host header is replaced with contents of hostRewrite. The value must be from 1 to 255 characters. Optional.
        </ResponseField>
        <ResponseField name="path_prefix_rewrite" type="string">
          Before forwarding the request to the selected backend service, the matching portion of the request's path is replaced bypathPrefixRewrite. The value must be from 1 to 1024 characters. Optional.
        </ResponseField>
        <ResponseField name="path_template_rewrite" type="string">
          If specified, the pattern rewrites the URL path (based on the :path header) using the HTTP template syntax. A corresponding path_template_match must be specified. Any template variables must exist in the path_template_match field. - -At least one variable must be specified in the path_template_match field - You can omit variables from the rewritten URL - The * and ** operators cannot be matched unless they have a corresponding variable name - e.g. &#123;format=*&#125; or &#123;var=**&#125;. For example, a path_template_match of /static/&#123;format=**&#125; could be rewritten as /static/content/&#123;format&#125; to prefix/content to the URL. Variables can also be re-ordered in a rewrite, so that /&#123;country&#125;/&#123;format&#125;/&#123;suffix=**&#125; can be rewritten as /content/&#123;format&#125;/&#123;country&#125;/&#123;suffix&#125;. At least one non-empty routeRules[].matchRules[].path_template_match is required. Only one of path_prefix_rewrite orpath_template_rewrite may be specified. Optional.
        </ResponseField>
        <ResponseField name="regex_rewrite" type="object">
          The spec for modifying the path using a regular expression. Optional.
          <Expandable title="properties">
            <ResponseField name="path_pattern" type="string">
              Required. The regular expression used to match against the URL path. It uses RE2 syntax with the following constraints: - Any single character operators - Groups are allowed to have only submatch operator inside - Groups are allowed only without any char repetition, e.g. .* - Any char repetition, e.g. .*, is only allowed to be used in a single regex together with: - Empty string operators - Other repetitions - Ranges - Repetitions of ranges - Ranges are only allowed to have: - Character range - Digits range - Symbols listed in characters allowed for ranges Optional.
            </ResponseField>
            <ResponseField name="path_substitution" type="string">
              Required. Required when path pattern is specified. Used to rewrite matching parts of the path. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="weighted_backend_services" type="list(object)">
      A list of weighted backend services to send traffic to when a route match occurs. The weights determine the fraction of traffic that flows to their corresponding backend service. If all traffic needs to go to a single backend service, there must be oneweightedBackendService with weight set to a non-zero number. After a backend service is identified and before forwarding the request to the backend service, advanced routing actions such as URL rewrites and header transformations are applied depending on additional settings specified in this HttpRouteAction. Optional.
      <Expandable title="properties">
        <ResponseField name="backend_service" type="string">
          The name or full URL of the backend service to which traffic is routed for this weighted entry. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_action" type="object">
          The header_action block configures request and response header modifications for this weighted backend service. It allows adding or removing custom headers on requests to and responses from the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="request_headers_to_add" type="list(object)">
              List of request headers to add or override before the request is forwarded to the backend service. Each object in the list specifies a header name and a header value. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the request header to add to the request before forwarding it to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value to assign to the request header specified by header_name. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  If set to true, the header value replaces any existing value for the header; if false, the value is appended to existing values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_headers_to_remove" type="list(string)">
              Specifies a list of HTTP header names that will be removed from the request before it is forwarded to the backend service. These headers are stripped from the incoming request headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="response_headers_to_add" type="list(object)">
              Configures the response headers to add. Each object defines a header name and value to be appended to the response before it is sent to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the response header to add. This is the header key (e.g., 'X-Custom-Header') that will be appended to the response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value of the response header to be added. This is used with header_name in the response_headers_to_add block. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  Indicates whether to replace the existing response header value (true) or append the new value to it (false). If false, multiple values are added for the same header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="response_headers_to_remove" type="list(string)">
              List of response header names to remove. These headers are stripped from the response before it is returned to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="weight" type="number">
          The weight of this backend service, used to determine the proportion of traffic that it receives relative to other weighted backend services. Must be an integer between 0 and 1000. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_service" type="string">
  The full or partial URL of the defaultService resource to which traffic is directed if none of the hostRules match. If defaultRouteAction is also specified, advanced routing actions, such as URL rewrites, take effect before sending the request to the backend. Only one of defaultUrlRedirect, defaultService or defaultRouteAction.weightedBackendService can be set. defaultService has no effect when the URL map is bound to a target gRPC proxy that has the validateForProxyless field set to true. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="default_url_redirect" type="object">
  Specifies settings for an HTTP redirect. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="host_redirect" type="string">
      The host that is used in the redirect response instead of the one that was supplied in the request. The value must be from 1 to 255 characters. Optional.
    </ResponseField>
    <ResponseField name="https_redirect" type="bool">
      If set to true, the URL scheme in the redirected request is set to HTTPS. If set to false, the URL scheme of the redirected request remains the same as that of the request. This must only be set for URL maps used inTargetHttpProxys. Setting this true forTargetHttpsProxy is not permitted. The default is set to false. Optional.
    </ResponseField>
    <ResponseField name="path_redirect" type="string">
      The path that is used in the redirect response instead of the one that was supplied in the request. pathRedirect cannot be supplied together withprefixRedirect. Supply one alone or neither. If neither is supplied, the path of the original request is used for the redirect. The value must be from 1 to 1024 characters. Optional.
    </ResponseField>
    <ResponseField name="prefix_redirect" type="string">
      The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch, retaining the remaining portion of the URL before redirecting the request. prefixRedirect cannot be supplied together withpathRedirect. Supply one alone or neither. If neither is supplied, the path of the original request is used for the redirect. The value must be from 1 to 1024 characters. Optional.
    </ResponseField>
    <ResponseField name="redirect_response_code" type="string">
      The HTTP Status code to use for this RedirectAction. Supported values are: - MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301. - FOUND, which corresponds to 302. - SEE_OTHER which corresponds to 303. - TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method is retained. - PERMANENT_REDIRECT, which corresponds to 308. In this case, the request method is retained. Optional.
    </ResponseField>
    <ResponseField name="strip_query" type="bool">
      If set to true, any accompanying query portion of the original URL is removed before redirecting the request. If set to false, the query portion of the original URL is retained. The default is set to false. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. A hash of the contents stored in this object. This field is used in optimistic locking. This field is ignored when inserting a UrlMap. An up-to-date fingerprint must be provided in order to update the UrlMap, otherwise the request will fail with error 412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a UrlMap. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="header_action" type="object">
  The request and response header transformations that take effect before the request is passed along to the selected backendService. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="request_headers_to_add" type="list(object)">
      Headers to add to a matching request before forwarding the request to thebackendService. Optional.
      <Expandable title="properties">
        <ResponseField name="header_name" type="string">
          The name of the HTTP header to add to the request. For example, 'X-Custom-Header'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_value" type="string">
          The value of the header to add to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="replace" type="bool">
          Indicates whether to replace an existing request header with the same name. If set to true, existing headers with the same name are removed before the new header is added; if false, the new header is added without removing existing ones. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="request_headers_to_remove" type="list(string)">
      A list of header names for headers that need to be removed from the request before forwarding the request to the backendService. Optional.
    </ResponseField>
    <ResponseField name="response_headers_to_add" type="list(object)">
      Headers to add the response before sending the response back to the client. Optional.
      <Expandable title="properties">
        <ResponseField name="header_name" type="string">
          The name of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_value" type="string">
          The value of the header to add to the response. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="replace" type="bool">
          If true, replaces an existing response header with the same name. If false, adds the header without replacing. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="response_headers_to_remove" type="list(string)">
      A list of header names for headers that need to be removed from the response before sending the response back to the client. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="host_rules" type="list(object)">
  The list of host rules to use against the URL. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      An optional description for this host rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="hosts" type="list(string)">
      The list of host patterns to match against the Host header of incoming requests. Each host must be a fully qualified domain name (FQDN), optionally starting with a wildcard `*.` to match subdomains. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path_matcher" type="string">
      The name of the PathMatcher to use for this host rule. This must reference a path matcher defined in the url_map's path_matcher block. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#urlMaps for url maps. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="path_matchers" type="list(object)">
  The list of named PathMatchers to use against the URL. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="default_custom_error_response_policy" type="object">
      The default custom error response policy for the path matcher's default service. This policy defines custom error response rules that override the default HTTP error responses for requests routed to the default service. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="error_response_rules" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="match_response_codes" type="list(string)">
              A list of HTTP response codes to match for this error response rule. When the origin returns a code in this list, the custom error response is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="override_response_code" type="number">
              Specifies the HTTP status code to return when this error response rule is matched, overriding the original error code. Must be a valid HTTP status code (e.g., 404, 500). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path" type="string">
              The request path that this error response rule applies to. If left empty, the rule applies to all paths. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="error_service" type="string">
          The full or partial URL to the BackendBucket that serves custom error responses for this path matcher's default policy. For example, a full URL like 'https://www.googleapis.com/compute/v1/projects/project/global/backendBuckets/backendBucket' or a partial URL such as 'backendBucket'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_route_action" type="object">
      The default route action to apply when no path rule matches in this path matcher. This object can define actions such as URL rewrites, header modifications, and redirects. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="cache_policy" type="object">
          Defines the cache policy for the default route action, controlling how responses from backend services are cached. It includes options for cache mode and time-to-live (TTL) settings. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cache_bypass_request_header_names" type="list(string)">
              Specifies a list of HTTP request header names. If any of these headers is present in a request, the request bypasses the CDN cache and is forwarded to the origin server. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="cache_key_policy" type="object">
              The cache key policy for the cache policy, specifying which parts of the request (such as host, protocol, query string, headers, and cookies) are used to construct the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="excluded_query_parameters" type="list(string)">
                  The list of query parameters to exclude from the cache key. Any query parameters in this list will not be included when constructing the cache key, allowing different requests with different values for these parameters to share a cached response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="include_host" type="bool">
                  If true, includes the Host header in the cache key, meaning requests with different Host values are cached separately. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="include_protocol" type="bool">
                  Indicates whether the request protocol (e.g., HTTP or HTTPS) should be included in the cache key. When set to true, the protocol is part of the cache key, causing separate cache entries for different protocols. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="include_query_string" type="bool">
                  Specifies whether query strings are included in the cache key. If false, requests with different query strings will share the same cached response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="included_cookie_names" type="list(string)">
                  Specifies the list of cookie names that should be included in the cache key. If this list is empty, no cookies are included. Any cookies not listed are not part of the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="included_header_names" type="list(string)">
                  A list of HTTP header names to include in the cache key. These headers are used to vary the cache key by their values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="included_query_parameters" type="list(string)">
                  A list of query parameters to include in the cache key. When set, only these parameters and their values are used to form the cache key; all other query parameters are ignored. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cache_mode" type="string">
              Specifies the cache mode for the backend. Allowed values are CACHE_ALL_STATIC (automatically caches static content), FORCE_CACHE_ALL (caches all content regardless of headers), and USE_ORIGIN_HEADERS (caches based on origin response headers). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="client_ttl" type="object">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanoseconds component of the TTL duration for cached responses, representing the fractional second part of the time-to-live. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds component of the client TTL duration, representing the whole-second value of the time-to-live in the cache policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="default_ttl" type="object">
              The default time-to-live (TTL) for cached responses, expressed as a duration object (e.g., seconds and nanoseconds). It is applied when no other TTL is specified by the client or origin. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanosecond component of the TTL duration, representing the fractional seconds (0-999999999). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds component of the default TTL duration. Must be a string representation of an integer between 0 and 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_ttl" type="object">
              The maximum amount of time for which a cached response will be served, expressed as a duration (an object with seconds and nanos). This overrides any default TTL in the cache policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanosecond component of the max_ttl duration, representing the fractional seconds. Valid range is 0 to 999,999,999. Together with seconds, it defines the maximum time-to-live for cached content. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of whole seconds in the maximum time-to-live duration for the cache policy. This is the seconds component of the duration, which together with the nanos component determines how long a cache entry remains valid. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="negative_caching" type="bool">
              Indicates whether the cache policy uses negative caching, which caches responses with error status codes (e.g., 404, 502). When enabled, such responses are cached according to the negative_ttl field. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="negative_caching_policy" type="list(object)">
              A list of objects that configure how long negative responses (such as HTTP error status codes) are cached, allowing different TTLs per status code. Applies to the cache policy of the URL map path matcher's default route action. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="number">
                  The HTTP status code (between 300 and 599) for which the negative caching policy applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ttl" type="object">
                  The time-to-live for cached negative responses. This is a Duration object specifying how long to cache an error response before checking the backend again. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional seconds component of the TTL duration. Valid values are 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the negative caching TTL, representing the duration in seconds for which the negative cache entry is valid. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_coalescing" type="bool">
              If true, multiple concurrent requests for the same resource are coalesced into a single request to the origin, reducing load on the origin server when there is a cache miss. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="serve_while_stale" type="object">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional seconds component of the serve-while-stale duration, in nanoseconds. Valid values are between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The duration in seconds for which the cache will continue to serve stale content after the original TTL has expired. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="cors_policy" type="object">
          The specification for allowing client side cross-origin requests. This policy defines CORS settings for the route action, including allowed origins, methods, and headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="allow_credentials" type="bool">
              Specifies whether the client is allowed to send credentials (such as cookies or HTTP authentication) with cross-origin requests. When set to true, the Access-Control-Allow-Credentials header is included in responses. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_headers" type="list(string)">
              The list of HTTP headers that are allowed in cross-origin requests. This sets the Access-Control-Allow-Headers header in the CORS policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_methods" type="list(string)">
              Specifies the list of HTTP methods allowed for CORS requests. For example, GET, PUT, POST, DELETE, HEAD, OPTIONS, and PATCH. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_origin_regexes" type="list(string)">
              A list of origin regular expressions that are allowed to make CORS requests. An origin matches if it matches any of the listed regular expressions. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_origins" type="list(string)">
              Specifies a list of origins that are permitted to make cross-origin requests. Each item is a string representing an origin, such as a domain name. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disabled" type="bool">
              If true, the CORS policy is disabled. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="expose_headers" type="list(string)">
              Specifies the headers that the browser is allowed to access in the response, as part of the CORS policy. This corresponds to the Access-Control-Expose-Headers header. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="max_age" type="number">
              Specifies how long (in seconds) the results of a preflight request can be cached by the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="fault_injection_policy" type="object">
          The fault injection policy for the default route action. Can be used to delay or abort requests that match this route, helping to test resiliency. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="abort" type="object">
              Configuration for aborting requests as part of fault injection, used to simulate service failures by returning a specific HTTP status code. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="http_status" type="number">
                  The HTTP status code used to abort the request. The value must be between 100 and 599. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="percentage" type="number">
                  The percentage of inbound requests to which the abort fault is applied, as a number between 0.0 and 100.0 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="delay" type="object">
              The delay block configures a fixed delay before forwarding requests. It includes settings for the delay amount and the percentage of requests to which the delay applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="fixed_delay" type="object">
                  Specifies a fixed delay duration for requests when fault injection is enabled, expressed as an object containing seconds and nanoseconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional seconds component of the fixed delay, in nanoseconds. Must be a non-negative integer between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the fixed delay duration before forwarding the request. Combined with the nanos field, it specifies the total delay. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="percentage" type="number">
                  The percentage of requests that will be delayed by the fault injection policy. Must be a number between 0.0 and 100.0. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="max_stream_duration" type="object">
          The maximum duration for a stream (request/response) before the connection is closed. This is a Duration object containing 'seconds' and 'nanos' fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              The fractional seconds component of the maximum stream duration, expressed in nanoseconds. Valid values are from 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_mirror_policy" type="object">
          The request mirroring policy for this route action, which sends a copy of incoming requests to a separate backend service for monitoring or analysis without affecting the response to the original request. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="backend_service" type="string">
              The backend service that receives mirrored traffic. This can be a full or partial URL to a backend service resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="mirror_percent" type="number">
              The percentage of requests to mirror to the backend service specified in the request mirror policy. Must be between 0 and 100, where 0 mirrors no requests and 100 mirrors all requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="retry_policy" type="object">
          Specifies the retry policy for this route action, including retry conditions (e.g., connect-failure, refused-stream, unavailable) and the maximum number of retries. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="num_retries" type="number">
              The number of retries to attempt before the request is considered failed. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="per_try_timeout" type="object">
              The timeout for each retry attempt, specified as a duration string (e.g., '30s'). If unset, the default per-try timeout for the route is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional part of the per-try timeout duration, specified in nanoseconds. Valid values range from 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Optional; if omitted, computed by GCP.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="retry_conditions" type="list(string)">
              The retry conditions that define which errors or status codes should cause a retry. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="timeout" type="object">
          Specifies the timeout for the selected route, as a duration. This sets the maximum time for the backend to respond before the request is considered timed out. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              The fractional part of the duration in nanoseconds, allowing sub-second precision for the timeout value. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="seconds" type="string">
              The seconds portion of the timeout duration. The total timeout is the sum of this value and the nanos field (if specified). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="url_rewrite" type="object">
          Defines the URL rewrite rules to apply to requests matched by this default route action, allowing modification of the host and path before forwarding the request to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="host_rewrite" type="string">
              Specifies the value to replace the Host header in the request with for URL rewriting. This is used in the default route action of a path matcher to rewrite the host portion of the request URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_prefix_rewrite" type="string">
              Specifies the path prefix that replaces the matched path prefix before the request is forwarded to the backend service. This allows rewriting the URL path for the backend. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_template_rewrite" type="string">
              Specifies the template that the request path is rewritten to as part of the URL rewrite action. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="regex_rewrite" type="object">
              Defines a regex-based URL path rewrite. The regex field specifies the pattern to match, and the substitution field provides the replacement string. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="path_pattern" type="string">
                  The regular expression pattern to match against the URL path for the regex rewrite operation. Only paths that match this pattern will be rewritten according to the rewrite rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_substitution" type="string">
                  The substitution string used to replace the part of the request path matched by the specified regex in this regex rewrite. It defines the rewritten path segment for the URL rewrite. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="weighted_backend_services" type="list(object)">
          A list of weighted backend services that handle traffic when a path matcher's default route action is used. Each entry specifies a backend service and a weight to control proportional traffic distribution. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="backend_service" type="string">
              The name or URL of the backend service to which traffic is routed. This must reference an existing backend service in the same project; the load balancer applies any header transformations defined on that service before forwarding requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_action" type="object">
              Defines the header action for this weighted backend service, specifying request and response headers to add or remove. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="request_headers_to_add" type="list(object)">
                  Optional; if omitted, computed by GCP.
                  <Expandable title="properties">
                    <ResponseField name="header_name" type="string">
                      The name of the HTTP request header to add to the request before it is sent to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="header_value" type="string">
                      The value of the request header to be added to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="replace" type="bool">
                      If true, replaces an existing request header with the same name; if false, appends the header value to any existing values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="request_headers_to_remove" type="list(string)">
                  A list of request header names to remove before forwarding the request to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="response_headers_to_add" type="list(object)">
                  A list of headers to add to the response before it is sent to the client. Each object in the list contains a header name and value, and the headers are applied to responses from the associated weighted backend services. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="header_name" type="string">
                      The name of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="header_value" type="string">
                      The value to assign to the response header specified by the corresponding header_name in the response_headers_to_add list. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="replace" type="bool">
                      If true, the header value replaces the existing header value with the same name. If false, the value is appended to the existing header value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="response_headers_to_remove" type="list(string)">
                  List of header names to remove from the response before it is sent to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weight" type="number">
              The weight of this backend service, used for weighted traffic distribution. Must be an integer between 0 and 1000, with a default of 100. Traffic is routed proportionally based on the weights of all backend services in the weighted_backend_services list. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_service" type="string">
      The default backend service or backend bucket to use if no path rules match. This must be a full or partial URL to a backend service or backend bucket. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="default_url_redirect" type="object">
      Defines a default URL redirect for the path matcher. When a request does not match any route rule, this redirect action is applied, allowing configuration of host, path, prefix, HTTPS redirect, response code, and query string handling. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="host_redirect" type="string">
          The host that will be used in the redirect response instead of the one that was supplied in the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="https_redirect" type="bool">
          If set to true, the URL scheme in the redirected request is set to HTTPS. If false or unset, the scheme remains the same as the original request (typically HTTP). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="path_redirect" type="string">
          The path that the request will be redirected to. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="prefix_redirect" type="string">
          The prefix to substitute for the matched path prefix in the redirect URL. If set, only one of prefix_redirect or path_redirect can be specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="redirect_response_code" type="string">
          The HTTP redirect response code used when the defaultUrlRedirect is applied. Valid values are: FOUND, MOVED_PERMANENTLY_DEFAULT, PERMANENT_REDIRECT, SEE_OTHER, and TEMPORARY_REDIRECT. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="strip_query" type="bool">
          If set to true, the query string from the original request is removed before constructing the redirect URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="header_action" type="object">
      The header_action block configures header transformations (both request and response) for this path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="request_headers_to_add" type="list(object)">
          A list of request headers to add to the request before forwarding it to the backend service. Each object defines a header name and value. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="header_name" type="string">
              The name of the HTTP request header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_value" type="string">
              The value of the HTTP request header to add. This is used in conjunction with header_name to set a custom header on requests routed by this path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replace" type="bool">
              A boolean that determines whether the header value should replace any existing header values with the same name. If false, the value is appended to existing values; if true, the value replaces them. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_headers_to_remove" type="list(string)">
          A list of header names to remove from the request before it is forwarded to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="response_headers_to_add" type="list(object)">
          List of response headers to add or overwrite. Each object includes headerName, headerValue, and optionally replace, specifying whether to overwrite an existing header. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="header_name" type="string">
              The name of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_value" type="string">
              The value of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replace" type="bool">
              If true, replaces an existing response header with the same name using the specified value. If false, appends the value to the existing header, which may result in multiple header values. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="response_headers_to_remove" type="list(string)">
          A list of response header names to remove from the response before sending it back to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the path matcher. This must be a unique identifier within the URL map and is referenced by host rules. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path_rules" type="list(object)">
      The list of path rules that map URL paths to backend services or backend buckets. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="custom_error_response_policy" type="object">
          Defines a custom error response policy for this path rule. It includes error response rules that map specific HTTP status codes to custom error pages, and optionally specifies an error service to serve those responses. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_response_rules" type="list(object)">
              A list of rules that define custom error responses for the path rule. Each rule specifies which HTTP response codes to match (e.g., 404, 500) and the path to the custom error page to serve for those codes. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="match_response_codes" type="list(string)">
                  The HTTP response codes that this error response rule applies to. When a backend returns one of these codes, the custom error response is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="override_response_code" type="number">
                  The HTTP status code to override the original error response with. When specified, this code replaces the original error response code for the matched error condition. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path" type="string">
                  Optional; if omitted, computed by GCP.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="error_service" type="string">
              The backend service that serves the custom error responses for this path rule. It must be a reference to an existing backend service resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="paths" type="list(string)">
          A list of URL path patterns (e.g., '/home', '/static/*') that this path rule matches. Requests whose paths match any pattern in this list are handled according to this rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="route_action" type="object">
          The route action for this path rule, specifying how matching requests are handled, including backend service, timeout, retry policy, request mirroring, CORS, and other settings. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cache_policy" type="object">
              Defines the Cloud CDN cache policy for this route action, determining how responses are cached for matching path rules. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="cache_bypass_request_header_names" type="list(string)">
                  List of request header names that, when present in a request, cause the request to bypass the cache. Requests containing any of these headers will not use cached content. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="cache_key_policy" type="object">
                  The cache key policy, which controls how Cloud CDN constructs cache keys by specifying included components like host, protocol, query string, headers, and cookies. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="excluded_query_parameters" type="list(string)">
                      A list of query parameter names to exclude from the cache key. When set, requests that differ only by these parameters are served from the same cached response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_host" type="bool">
                      Specifies whether the origin host should be included in the cache key. When set to true, requests with different Host headers are cached separately. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_protocol" type="bool">
                      If true, the request protocol (HTTP or HTTPS) is included in the cache key, so requests with different protocols are cached separately. If false, the protocol is not included, and HTTP/HTTPS requests for the same resource share a cache entry. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_query_string" type="bool">
                      If true, requests with different query string values are cached separately. If false, all requests with the same path are cached together regardless of query string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_cookie_names" type="list(string)">
                      The names of cookies to include in the cache key. Only cookies with these exact names are included; other cookies are ignored. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_header_names" type="list(string)">
                      The list of HTTP header names to include in the cache key. Specifies which request headers are used to form the cache key, so that requests with different values for these headers are cached separately. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_query_parameters" type="list(string)">
                      A list of query parameter names to include in the cache key. Requests with these query parameters will have them considered part of the cache key determination. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cache_mode" type="string">
                  The cache mode for the route action. Valid values are CACHE_ALL_STATIC (cache static content), FORCE_CACHE_ALL (cache all content regardless of headers), and USE_ORIGIN_HEADERS (respect origin cache headers). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="client_ttl" type="object">
                  Specifies the maximum amount of time (TTL) that cached responses are served to clients. The object uses the duration format with `seconds` and `nanos` fields representing the time period. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond component of the client TTL duration, providing sub-second precision. Valid range is 0 to 999,999,999 nanoseconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the client TTL duration, represented as a string to accommodate large values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="default_ttl" type="object">
                  The default amount of time that a cached response is stored by the cache, specified as a Duration object with a 'seconds' field (and optionally 'nanos'). This TTL applies when the response has no explicit Cache-Control headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional second component of the default cache TTL, expressed in nanoseconds. Valid values range from 0 to 999,999,999 nanoseconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the default TTL (time-to-live) duration for cached responses in the path rule's cache policy. Despite being typed as a string, it represents an integer number of seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="max_ttl" type="object">
                  Optional; if omitted, computed by GCP.
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond component of the maximum time-to-live (TTL) duration. Specifies the fractional second part of the cache TTL, used together with the seconds field. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the max_ttl duration, specified as a 64-bit integer encoded as a string. This determines the maximum time (in seconds) that a cached response is considered fresh. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="negative_caching" type="bool">
                  When true, enables negative caching for the path rule, which caches HTTP error responses (such as 404 or 502) for the specified path. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="negative_caching_policy" type="list(object)">
                  Specifies the negative caching settings for HTTP error responses. Each entry defines a response code and its time-to-live (TTL) for caching error responses. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="number">
                      The HTTP status code for which negative caching is enabled. For example, 404 or 500. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="ttl" type="object">
                      Optional; if omitted, computed by GCP.
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The non-negative fractional seconds in nanoseconds for the negative caching TTL. Valid values are from 0 to 999,999,999 nanoseconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          Specifies the time-to-live (TTL) for the negative caching policy in seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="request_coalescing" type="bool">
                  If set to true, enables request coalescing, which combines multiple requests for the same cacheable resource into a single request sent to the backend, reducing load. If false, disables this behavior. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="serve_while_stale" type="object">
                  Optional; if omitted, computed by GCP.
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanoseconds component of the serve-while-stale duration. Valid values range from 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The duration, in seconds, for which stale cached content is served while the origin is unavailable. This value is specified as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cors_policy" type="object">
              The Cross-Origin Resource Sharing (CORS) policy for this route action, defining allowed origins, methods, headers, and other CORS settings. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allow_credentials" type="bool">
                  Whether to allow credentials (cookies, authorization headers, etc.) to be sent with cross-origin requests. If true, the Access-Control-Allow-Credentials response header is set to 'true'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_headers" type="list(string)">
                  Specifies the list of headers that the origin may include in CORS requests. This corresponds to the Access-Control-Allow-Headers header in the response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_methods" type="list(string)">
                  Specifies the list of HTTP methods allowed for CORS requests, such as GET, POST, PUT, DELETE, or '*' to allow all methods. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origin_regexes" type="list(string)">
                  Specifies a list of origin regexes that are allowed to make CORS requests. A request's Origin header must match one of these regexes for the CORS policy to permit the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origins" type="list(string)">
                  Specifies the list of origins allowed to make cross-origin requests. Each origin is a string, such as 'https://example.com'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="disabled" type="bool">
                  If true, the CORS policy is disabled for this route and no CORS headers will be applied to responses. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="expose_headers" type="list(string)">
                  Specifies the response headers that are exposed to the browser and can be accessed by client-side scripts, as part of the CORS policy for this path rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_age" type="number">
                  The maximum age, in seconds, that the browser should cache preflight request results, as defined by the Access-Control-Max-Age header. A value of -1 disables caching. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="fault_injection_policy" type="object">
              Configuration for injecting faults into requests matched by this route action. This block allows specifying delay and abort behaviors to simulate failures, useful for testing service resilience. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="abort" type="object">
                  Abort fault injection configuration. When specified, the policy aborts requests with a configured HTTP status code to simulate service failures. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="http_status" type="number">
                      The HTTP status code that will be returned to the client when the fault injection abort is triggered. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      The percentage of requests that will be aborted. Must be a number between 0.0 and 100.0. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="delay" type="object">
                  Configuration for injecting delay faults into requests. Specifies the fixed delay amount and the percentage of requests to which the delay is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fixed_delay" type="object">
                      Specifies a fixed delay duration (in seconds and nanos) that is applied to a percentage of requests for fault injection. This object defines the amount of delay to inject before forwarding the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The nanosecond portion of the fixed delay duration. Valid values range from 0 to 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          The seconds component of the fixed delay. Accepts a string representation of an integer from 0 to 315576000000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      The percentage of requests to which the delay is applied in the fault injection policy. A value of 10 means 10% of requests are delayed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_stream_duration" type="object">
              Specifies the maximum duration for a stream to be active. The object contains 'seconds' and 'nanos' fields that together define the duration (for example, 30 seconds). Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional second component of the duration, measured in nanoseconds. Valid values range from 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds component of the maximum stream duration. This is the whole-number part of the duration in seconds, specified as a decimal string (e.g., '10' for 10 seconds). Fractional seconds are handled by the associated 'nanos' field. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_mirror_policy" type="object">
              The request_mirror_policy block specifies a backend service to which requests are mirrored. This allows traffic shadowing, where a copy of the matched requests is sent to the specified backend service in addition to the original destination. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The backend service to which to mirror traffic. This can be specified as a name or a fully-qualified URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="mirror_percent" type="number">
                  The percentage of requests (0-100) to mirror to the mirrored backend. For example, set to 50 to mirror half of the requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="retry_policy" type="object">
              The retry policy for this route action, defining the conditions that trigger a retry and the maximum number of retry attempts. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="num_retries" type="number">
                  The number of retries before the request is considered failed. This value must be a non-negative integer. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="per_try_timeout" type="object">
                  Specifies the timeout for each retry attempt. This is a Duration object with integer fields 'seconds' and 'nanos' that together determine the maximum time a single retry can take. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanoseconds component of the per-try timeout duration. Must be between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The whole seconds component of the per-try timeout duration, used to specify the timeout length. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="retry_conditions" type="list(string)">
                  Specifies one or more conditions when this retry policy applies. Valid values are: '5xx', 'gateway-error', 'connect-failure', 'refused-stream', 'reset', 'http-error', and 'internal'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="timeout" type="object">
              Configures the timeout for the route action. This is a Duration object that specifies the maximum time allowed for the request to complete, typically containing 'seconds' and 'nanos' fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional seconds component of the timeout duration, expressed in nanoseconds. This field, along with `seconds`, defines the total timeout period, with valid values ranging from 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds component of the timeout duration. This is a string representation of an integer number of seconds, and must be non-negative. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="url_rewrite" type="object">
              Specifies a URL rewrite action for requests matched by this path rule. Within it, you can set hostRewrite and pathPrefixRewrite to modify the host and path prefix before forwarding to the backend. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="host_rewrite" type="string">
                  Specifies the new host header value to rewrite the incoming request's host header to before forwarding the request to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_prefix_rewrite" type="string">
                  The value to replace the path prefix of the request with before forwarding to the backend service. Must start with '/'. If empty, the original path is preserved. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_template_rewrite" type="string">
                  The rewrite path template, which must have the same format as the path template in the path matcher's `path_template_match`. For example, with `path_template_match = "/v1/*/products/*"`, setting `path_template_rewrite = "/v2/*/products/*"` rewrites `/v1/books/products/123` to `/v2/books/products/123`. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="regex_rewrite" type="object">
                  The regex_rewrite block defines a regular expression pattern to match the request path and a substitution string to replace it, enabling regex-based URL path rewrites. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path_pattern" type="string">
                      The regex pattern (RE2 syntax) that matches the path component of the request URL. The rewrite applies when the path matches this pattern. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path_substitution" type="string">
                      The substitution string to replace the portion of the URL path matched by the regex. Backreferences like `\1` can be used to reference captured groups from the regex. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weighted_backend_services" type="list(object)">
              A list of weighted backend services to which traffic will be distributed. Each element specifies a backend service and a weight (0-1000) controlling the proportion of traffic routed to it. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The name or self_link of the backend service that this weighted backend service routes traffic to. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_action" type="object">
                  Configures the header action for this weighted backend service, specifying request and response headers to add or remove. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="request_headers_to_add" type="list(object)">
                      A list of headers to add to the request before it is forwarded to the backend service. Each header is defined by a `header_name` and `header_value`. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the request header to add to requests sent to the weighted backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the header to be added to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          Whether to replace an existing header value with the specified value. If true, the header is replaced; if false, the header is added. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="request_headers_to_remove" type="list(string)">
                      A list of HTTP request header names to remove from the request before it is forwarded to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="response_headers_to_add" type="list(object)">
                      A list of response headers to add to the response before it is sent back to the client. Each object in the list defines a header name and value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the response header to add to the outbound response for the matched request. For example, 'X-Custom-Header'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          Specifies the value of the response header to add to the response for the associated weighted backend service. This value is used as the header value when the header is added. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          If set to true, replaces the existing header with the same name; if false, appends this header value to any existing values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="response_headers_to_remove" type="list(string)">
                      List of header names to remove from the response before it is sent back to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="weight" type="number">
                  The weight of the backend service, determining the fraction of traffic it receives relative to other weighted backend services. Must be an integer between 0 and 1000. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service" type="string">
          A reference (URL) to the backend service that will receive requests matching this path rule. If routeAction is configured, advanced routing actions are applied before forwarding the request to this service. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url_redirect" type="object">
          Specifies a URL redirect for requests matching this path rule. When set, requests are redirected to the configured target instead of being sent to a backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="host_redirect" type="string">
              The host that will be used in the redirect response instead of the one that was supplied in the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="https_redirect" type="bool">
              If true, the redirect uses HTTPS protocol; otherwise, it uses HTTP protocol. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_redirect" type="string">
              The path to redirect requests to. When set, the request path is replaced with this value in the redirect response. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="prefix_redirect" type="string">
              The prefix that replaces the matched prefix in the original request path for this redirect. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="redirect_response_code" type="string">
              Specifies the HTTP status code to use for the redirect. Allowed values are FOUND (302), MOVED_PERMANENTLY_DEFAULT (301), PERMANENT_REDIRECT (308), SEE_OTHER (303), and TEMPORARY_REDIRECT (307). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="strip_query" type="bool">
              Indicates whether the query string of the original URL is removed before redirecting. Set to true to strip the query, or false to preserve it. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="route_rules" type="list(object)">
      A list of route rules that define how requests are matched and routed for this path matcher. Each rule includes match conditions and the corresponding action. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="custom_error_response_policy" type="object">
          Specifies the custom error response policy for the route rule. When configured, this policy defines custom error responses that replace the default error responses for the rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_response_rules" type="list(object)">
              A list of rules that define custom error responses for specific HTTP error codes. Each rule includes a set of response codes to match and a path to the custom error page. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="match_response_codes" type="list(string)">
                  A list of HTTP response codes that trigger this custom error response rule. May include specific codes (e.g., '404') or wildcard ranges (e.g., '4xx', '5xx'). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="override_response_code" type="number">
                  The HTTP status code to use as the override for the original error response status code when this error response rule matches. This allows returning a custom status code for the error response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path" type="string">
                  The path to the custom error page on the backend service or bucket that handles the error response, used when a response code in matchResponseCodes is returned. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="error_service" type="string">
              The full or partial URL of the backend service that will serve custom error responses for this route rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          An optional, human-readable description of this route rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_action" type="object">
          Configures the request and response headers to add or remove for the matched route. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="request_headers_to_add" type="list(object)">
              A list of header name/value pairs to add to the request before it is forwarded to the backend. Each object contains `header_name` and `header_value` fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the request header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value of the request header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  Indicates whether to replace the header if it already exists in the request. If true, the header value is replaced; if false, the header is added without overwriting existing values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_headers_to_remove" type="list(string)">
              List of request header names to remove from the request before forwarding to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="response_headers_to_add" type="list(object)">
              A list of response headers to add for this route rule. Each object specifies a header name and value, which are added to the response to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the header to add to the response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value to assign to the response header being added. This, together with the corresponding header name, defines a header that will be added to the response for matching requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  If true, replaces the value of the existing response header with the specified header value. If false, the header value is appended to the existing header value, potentially resulting in multiple headers with the same name. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="response_headers_to_remove" type="list(string)">
              A list of response header names to remove from the response before it is returned to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_rules" type="list(object)">
          Specifies a list of match rules that determine whether a request applies to this route rule. Each match rule can define path-based matching (e.g., prefix, exact, regex) and additional header or query parameter conditions. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="full_path_match" type="string">
              Specifies a full path that must exactly match the request path for the match rule to apply. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_matches" type="list(object)">
              A list of header match criteria. Each element specifies a header name and a value (exact match or regular expression) to match against the request headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="exact_match" type="string">
                  The exact value that the request header must match for this header match rule to apply. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header to match. For example, to match the `User-Agent` header, set this field to `User-Agent`. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="invert_match" type="bool">
                  When set to true, the header match condition is inverted. A request matches this rule if the header does NOT satisfy the specified header match criteria. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="prefix_match" type="string">
                  The prefix of the header value to match. Only one of exact_match, prefix_match, suffix_match, regex_match, or present_match can be set. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="present_match" type="bool">
                  If set to true, the match is satisfied when the request header is present, regardless of its value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_match" type="object">
                  Defines a range of integer values that a numeric header value must fall within for the header match to apply. The object contains rangeStart and rangeEnd fields specifying the inclusive lower and upper bounds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="range_end" type="string">
                      The end of the range for the header value to match. Used together with range_start. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="range_start" type="string">
                      The start of the range (inclusive) to match against the header value. The header value must be a number within the specified range. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="regex_match" type="string">
                  The regular expression (RE2 syntax) to match against the header value. Only one of exactMatch, regexMatch, prefixMatch, suffixMatch, or rangeMatch can be set. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="suffix_match" type="string">
                  Specifies a suffix match for the value of a request header. The header value must end with the given string for the match to succeed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="ignore_case" type="bool">
              Whether to ignore case when matching the URL path. When set to true, case differences are ignored; when false, matching is case-sensitive. Defaults to false. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metadata_filters" type="list(object)">
              A list of metadata filters that the request metadata (headers) must satisfy for the route rule match to apply. Each filter defines a matching criterion and a set of label name/value pairs to match against. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="filter_labels" type="list(object)">
                  A list of label filters used to match request metadata. Each label filter defines a key-value pair that must match the request's metadata for the metadata_filters condition to be true. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name (key) of the metadata filter label, used as the key in a key-value pair for request filtering. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value part of a metadata filter label key-value pair. This is used to specify the value of the label that metadata must match for the filter to apply. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="filter_match_criteria" type="string">
                  Specifies the match criteria for applying the metadata filters. MATCH_ALL requires all filter criteria to match, MATCH_ANY requires at least one to match, and NOT_SET indicates no criteria is set. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="path_template_match" type="string">
              The path template to match against the request path, using a template syntax with variables (e.g., '/foo/&#123;bar&#125;') for dynamic segment matching. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="prefix_match" type="string">
              Matches requests whose URL path begins with this string. For example, a prefix_match of '/foo' matches '/foo' and '/foo/bar'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="query_parameter_matches" type="list(object)">
              Defines the query parameters that must be present and match in the request URL for the match rule to apply. Each object in the list specifies a query parameter name and a value or regular expression to match against. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="exact_match" type="string">
                  Optional; if omitted, computed by GCP.
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the query parameter to match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="present_match" type="bool">
                  Indicates whether the query parameter must be present for the match to succeed. Set to true to match when the parameter is present, and false to match when it is absent. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="regex_match" type="string">
                  The regular expression to match the query parameter value against. If set, the query parameter value must satisfy this regex for the match rule to apply. Cannot be set together with value_match; exactly one of the two must be specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="regex_match" type="string">
              A regular expression (RE2) to match against the request path. When set, this match rule applies if the path matches the regex. It is mutually exclusive with full_path_match and prefix_match. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="priority" type="number">
          The priority of this route rule, used to determine the order in which rules are evaluated. Lower values have higher precedence; rules are matched in increasing priority order. This value must be unique within the path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="route_action" type="object">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="cache_policy" type="object">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="cache_bypass_request_header_names" type="list(string)">
                  The list of request header names to consider for cache bypass. When a request contains any of these headers, the cache is bypassed and the request is sent to the origin server. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="cache_key_policy" type="object">
                  Configuration for the cache key used by the cache policy. It defines which request attributes (such as host, protocol, query string, headers, and cookies) are included in the cache key for this route rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="excluded_query_parameters" type="list(string)">
                      A list of query parameters to exclude from the cache key. Requests that differ only in these parameters will share the same cached response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_host" type="bool">
                      If true, the HTTP host (host header) is included in the cache key. If false, the host is excluded. This affects cache key differentiation based on the requested host. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_protocol" type="bool">
                      If true, HTTP and HTTPS requests are treated as distinct cache keys, meaning the protocol is included in the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_query_string" type="bool">
                      If true, the query string is included in the cache key. If false, the query string is excluded, meaning all requests with different query strings share the same cache entry. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_cookie_names" type="list(string)">
                      Specifies a list of cookie names to include in the cache key. If empty, no cookies are included in the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_header_names" type="list(string)">
                      A list of HTTP header names to include in the cache key. If specified, each listed header is added as a component of the cache key; if empty, no custom headers are included. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_query_parameters" type="list(string)">
                      The list of query parameters to include in the cache key. If specified, only these query parameters are considered when generating the cache key; other query parameters are ignored. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cache_mode" type="string">
                  The cache mode for the cache policy. Valid values are: CACHE_ALL_STATIC (cache static content automatically), FORCE_CACHE_ALL (cache all content, ignoring origin cache headers), and USE_ORIGIN_HEADERS (respect the origin's cache-control headers). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="client_ttl" type="object">
                  Specifies the time-to-live (TTL) for the client-side cache. This is a Duration object with 'seconds' and 'nanos' fields that limits how long the client can cache a response before revalidating with the origin. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond portion of the duration. For a client_ttl, this specifies the fractional seconds component, and must be an integer between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The number of seconds for the client TTL, represented as a string. This value specifies how long to keep requests cached. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="default_ttl" type="object">
                  Specifies the default time-to-live (TTL) for cached responses, used when the origin server does not provide an explicit TTL. Represented as a duration object. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond component of the default TTL duration. Valid values are 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      Optional; if omitted, computed by GCP.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="max_ttl" type="object">
                  The maximum time-to-live for cached responses, specified as a duration object. This sets an upper bound on how long a response can be stored in the cache. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanoseconds component of the maximum TTL duration. Must be an integer from 0 to 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the maximum time-to-live (TTL) duration. This is a string representation of an integer number of seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="negative_caching" type="bool">
                  Enables or disables negative caching, which allows the CDN to cache responses for error status codes (such as 404) according to the negative caching policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="negative_caching_policy" type="list(object)">
                  A list of negative caching policies for HTTP error status codes. Each entry specifies an HTTP status code (e.g., 404) and the TTL (in seconds) for caching that error response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="number">
                      The HTTP status code (e.g., 404, 500) that triggers negative caching for responses with that status. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="ttl" type="object">
                      The time-to-live (TTL) for this negative caching entry, defining how long the cache should retain the negative response for the associated status code. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The nanoseconds component of the TTL duration. Must be a non-negative integer between 0 and 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          The number of seconds in the TTL duration for the negative caching policy, specified as a string (e.g., '60' for 60 seconds). Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="request_coalescing" type="bool">
                  Enables request coalescing, which combines concurrent requests for the same cached object into a single origin request. This reduces origin load when multiple clients request the same content simultaneously. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="serve_while_stale" type="object">
                  Optional; if omitted, computed by GCP.
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond component of the duration, ranging from 0 to 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The duration, in seconds, for which stale cached content may be served while the cache attempts to revalidate the origin resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cors_policy" type="object">
              The Cross-Origin Resource Sharing (CORS) policy for this route. This specifies how the backend responds to cross-origin requests, including allowed origins, methods, headers, and other CORS settings. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allow_credentials" type="bool">
                  Specifies whether the response to the request can be exposed when the credentials flag is true. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_headers" type="list(string)">
                  The set of HTTP request headers that are allowed in a CORS request. This corresponds to the Access-Control-Allow-Headers header in a CORS response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_methods" type="list(string)">
                  Specifies the list of HTTP methods that are allowed for cross-origin requests in the CORS policy. For example, GET, POST, PUT, DELETE, etc. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origin_regexes" type="list(string)">
                  Specifies a list of regular expressions that match request origins allowed to make cross-origin requests. If an origin matches one of these expressions, it is included in the Access-Control-Allow-Origin response header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origins" type="list(string)">
                  List of origins that are allowed to make cross-origin requests. Each origin is a string such as `http://example.com`. This is part of the CORS policy for route actions in a URL map path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="disabled" type="bool">
                  When true, this CORS policy is disabled for the route, and other CORS settings are ignored. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="expose_headers" type="list(string)">
                  Specifies the response headers that are exposed to the client as part of the CORS policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_age" type="number">
                  The maximum time in seconds that the client can cache the CORS preflight response. This value is sent in the Access-Control-Max-Age header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="fault_injection_policy" type="object">
              The fault injection policy for this route. It allows configuring how requests are aborted or delayed to test the behavior of the backend. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="abort" type="object">
                  The abort fault injection action, which aborts a percentage of requests routed to the backend service by returning a configured HTTP status code. It contains the HTTP status code and the percentage of requests to abort. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="http_status" type="number">
                      The HTTP status code used to abort the request. Must be between 200 and 599 (inclusive). Defaults to 500. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      The percentage of requests that will be aborted, between 0.0 and 100.0. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="delay" type="object">
                  The delay fault injection configuration for the route action. It sets a fixed delay and percentage of requests to which the delay is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fixed_delay" type="object">
                      A fixed delay duration to inject before forwarding the request. This object specifies the exact delay period, used to simulate latency in fault injection testing. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The fractional seconds component of the fixed delay duration, in nanoseconds. A value from 0 to 999,999,999 inclusive, used together with the seconds field to specify the total delay. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          Specifies the number of seconds for the fixed delay applied in the fault injection policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      The percentage of requests that will be subjected to the delay specified in the fixed_delay field. Must be a number between 0.0 and 100.0. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_stream_duration" type="object">
              Specifies the maximum duration for a stream. When the stream duration exceeds this limit, the connection is terminated. The duration is represented as a Duration object with `seconds` and `nanos` fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional seconds component of the maximum stream duration, expressed in nanoseconds. Valid values are from 0 to 999,999,999, and are used in conjunction with the `seconds` field to define the total duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Span of time at a resolution of seconds. Must be from 0 to 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_mirror_policy" type="object">
              Defines the request mirroring policy for the route action. It configures a backend service to receive a copy of the requests for traffic analysis. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The backend service resource that will receive mirrored traffic. This is the target to which a copy of the original request is sent. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="mirror_percent" type="number">
                  The percentage of requests that will be mirrored to the backend configured in the request mirroring policy. Must be a number between 0 and 100. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="retry_policy" type="object">
              The retry policy for requests routed by this route rule. It specifies the conditions under which a request should be retried, the maximum number of retry attempts, and the timeout for each attempt. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="num_retries" type="number">
                  The number of retry attempts to perform for a request before returning an error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="per_try_timeout" type="object">
                  Specifies a timeout for each retry attempt. The value is a duration string, such as '30s'. If not set, the default per-try timeout is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional part of the per-try timeout duration in nanoseconds. Valid values are from 0 to 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      Optional; if omitted, computed by GCP.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="retry_conditions" type="list(string)">
                  A list of conditions that trigger a retry attempt. Each string represents a failure type (such as a connection error or a server error) that causes the request to be retried. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="timeout" type="object">
              Specifies the timeout for this route rule, expressed as a duration string ending in 's' (e.g., '30s'). If not set, the largest timeout among the associated backend services is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanosecond component of the timeout duration. Must be a non-negative integer less than 1,000,000,000. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of seconds in the timeout duration, as a string. For example, '30' represents a 30-second timeout. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="url_rewrite" type="object">
              Configures the URL rewriting for this route rule. This block allows setting a new host header and/or a new path prefix before the request is forwarded. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="host_rewrite" type="string">
                  Specifies the new host name to use in the request. If set, the Host header is rewritten to this value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_prefix_rewrite" type="string">
                  Before forwarding the request to the selected service, the matched portion of the request's path is replaced by this value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_template_rewrite" type="string">
                  Specifies a path template to which the URL path will be rewritten, allowing the use of variables extracted from the matching path template. For example, a match template like '/users/&#123;id&#125;' can be rewritten to '/profiles/&#123;id&#125;'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="regex_rewrite" type="object">
                  The regex_rewrite block configures a regular expression pattern and a replacement string to rewrite the request path within the URL rewrite action. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path_pattern" type="string">
                      The regular expression pattern that matches a portion of the URL path. This pattern is used together with the replacement string to perform the URL rewrite. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path_substitution" type="string">
                      The string to substitute for the matched path pattern. It can include capture group references such as \1, \2, etc., referencing groups from the regex pattern in the parent regex_rewrite. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weighted_backend_services" type="list(object)">
              This block defines a list of backend services with assigned weights to control the proportion of traffic each receives, enabling traffic splitting in the route action. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The name of the backend service that the traffic is routed to for the given weight. This should reference a backend service resource, either by name or URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_action" type="object">
                  Specifies modifications to request and response headers for this weighted backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="request_headers_to_add" type="list(object)">
                      A list of request headers to be added to the request before forwarding to the backend service. Each object in the list contains a 'header_name' and a 'header_value'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the HTTP header to add to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the request header to add. This is the actual string that will be sent as the header value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          Determines whether to replace the value of the specified header (true) or append the value to an existing header (false). Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="request_headers_to_remove" type="list(string)">
                      Specifies a list of header names to remove from the request before forwarding to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="response_headers_to_add" type="list(object)">
                      Optional; if omitted, computed by GCP.
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the HTTP response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          Indicates whether to replace an existing response header with the same name. If true, the header value is replaced; if false or unset, the header is added as an additional header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="response_headers_to_remove" type="list(string)">
                      The names of response headers to remove from the outgoing response before sending it to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="weight" type="number">
                  The weight of this backend service. Must be between 0 and 1000. Higher values receive more traffic. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service" type="string">
          The name or URL of a BackendService or BackendBucket that defines where traffic matching this route rule should be sent. In a URL map, this service is used as the destination for requests that match the rule's match conditions. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url_redirect" type="object">
          The url_redirect block configures the redirect behavior for this route rule. It defines where and how requests are redirected, including settings for host, path, prefix, response code, and query string handling. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="host_redirect" type="string">
              The host that will be used in the redirect response instead of the one that was supplied in the request. If empty, the host from the request is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="https_redirect" type="bool">
              If set to true, the request is redirected to the HTTPS version of the same URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_redirect" type="string">
              The path portion of the redirect URL. If empty, the path of the incoming request is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="prefix_redirect" type="string">
              The prefix that replaces the matched prefix in the request path when a redirect is triggered. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="redirect_response_code" type="string">
              Specifies the HTTP status code to return for the redirect. Allowed values are FOUND (302), MOVED_PERMANENTLY_DEFAULT (301), PERMANENT_REDIRECT (308), SEE_OTHER (303), and TEMPORARY_REDIRECT (307). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="strip_query" type="bool">
              If set to true, any query portion of the original URL is removed before the redirect is performed. Defaults to false. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the regional URL map resides. This field is not applicable to global URL maps. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tests" type="list(object)">
  The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a maximum of 100 tests per UrlMap. Not supported when the URL map is bound to a target gRPC proxy that has validateForProxyless field set to true. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      An optional description of this test. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="expected_output_url" type="string">
      The expected URL that the host and path specified in the test case should resolve to, used to verify URL map routing behavior. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="expected_redirect_response_code" type="number">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="headers" type="list(object)">
      A list of headers to include in the test request, where each header is an object with 'name' and 'value' fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of the HTTP header to include in the test request. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="host" type="string">
      The hostname used in the test request to match against the URL map's host rules. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path" type="string">
      The request path to test against the URL map's routing rules. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="service" type="string">
      The backend service that the test expects the request to be routed to, validating the URL map's routing configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
