---
title: "google_compute_node_group"
description: "A complete, runnable google_compute_node_group program, in every SDK language."
---

`google_compute_node_group` -- real, typed bindings generated directly from
real Google Discovery Documents, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own node group"})

    		ubx.Resource(compute.NodeGroup, "ci-runner", compute.NodeGroupConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NodeGroup } from "./local-sdk/google/sdk/typescript/google/compute/node_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own node group" });

      resource(NodeGroup, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.compute import NodeGroup, NodeGroupConfig

    def describe():
        ubx.intent("payments own node group")

        ubx.resource(NodeGroup, "ci-runner", NodeGroupConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_node_group called "ci-runner", with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="autoscaling_policy" type="object">
  The autoscaling policy of the node group. This output-only field contains the configuration that determines how the node group automatically scales the number of nodes. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="max_nodes" type="number">
      The maximum number of nodes that the group should have. Must be set if autoscaling is enabled. Maximum value allowed is 100. Optional.
    </ResponseField>
    <ResponseField name="min_nodes" type="number">
      The minimum number of nodes that the group should have. Optional.
    </ResponseField>
    <ResponseField name="mode" type="string">
      The autoscaling mode. Set to one of: ON, OFF, or ONLY_SCALE_OUT. For more information, see Autoscaler modes. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  The fingerprint of the node group, used for optimistic concurrency control. This is an output-only value generated by the server that changes whenever the node group is updated. Optional; if omitted, computed by GCP. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] The type of the resource. Alwayscompute#nodeGroup for node group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location_hint" type="string">
  An opaque location hint used to place the Node close to other resources. This field is for use by internal tools that use the public API. The location hint here on the NodeGroup overrides any location_hint present in the NodeTemplate. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="maintenance_interval" type="string">
  Specifies the frequency of planned maintenance events. The accepted values are: `AS_NEEDED` and `RECURRENT`. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="maintenance_policy" type="string">
  Specifies how to handle instances when a node in the group undergoes maintenance. Set to one of: DEFAULT,RESTART_IN_PLACE, or MIGRATE_WITHIN_NODE_GROUP. The default value is DEFAULT. For more information, see Maintenance policies. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="maintenance_window" type="object">
  Time window specified for daily maintenance operations. GCE's internal maintenance will be performed within this window. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="maintenance_duration" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Start time of the window. This must be in UTC format that resolves to one of 00:00, 04:00, 08:00,12:00, 16:00, or 20:00. For example, both 13:00-5 and 08:00 are valid. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The name of the resource, provided by the client when initially creating the resource. The resource name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="node_template" type="string">
  URL of the node template to create the node group from. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="share_settings" type="object">
  The share setting for reservations and sole tenancy node groups. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="project_map" type="map(object)">
      A map of project id and project config. This is only valid when share_type's value is SPECIFIC_PROJECTS. Optional.
      <Expandable title="properties">
        <ResponseField name="project_id" type="string">
          The Google Cloud project ID for which the share settings are configured in this project map entry. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="share_type" type="string">
      Type of sharing for this shared-reservation Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="size" type="number">
  Output only. [Output Only] The total number of nodes in the node group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status" type="string">
  The current lifecycle state of the node group, computed by the provider. Possible values are CREATING, DELETING, INVALID, and READY. Optional; if omitted, computed by GCP. **(AI-inferred)**
</ResponseField>

<ResponseField name="zone" type="string">
  Output only. [Output Only] The name of the zone where the node group resides, such as us-central1-a. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="autoscaling_policy" type="object">
  The autoscaling policy of the node group. This output-only field contains the configuration that determines how the node group automatically scales the number of nodes. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="max_nodes" type="number">
      The maximum number of nodes that the group should have. Must be set if autoscaling is enabled. Maximum value allowed is 100. Optional.
    </ResponseField>
    <ResponseField name="min_nodes" type="number">
      The minimum number of nodes that the group should have. Optional.
    </ResponseField>
    <ResponseField name="mode" type="string">
      The autoscaling mode. Set to one of: ON, OFF, or ONLY_SCALE_OUT. For more information, see Autoscaler modes. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  The fingerprint of the node group, used for optimistic concurrency control. This is an output-only value generated by the server that changes whenever the node group is updated. Optional; if omitted, computed by GCP. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] The type of the resource. Alwayscompute#nodeGroup for node group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location_hint" type="string">
  An opaque location hint used to place the Node close to other resources. This field is for use by internal tools that use the public API. The location hint here on the NodeGroup overrides any location_hint present in the NodeTemplate. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="maintenance_interval" type="string">
  Specifies the frequency of planned maintenance events. The accepted values are: `AS_NEEDED` and `RECURRENT`. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="maintenance_policy" type="string">
  Specifies how to handle instances when a node in the group undergoes maintenance. Set to one of: DEFAULT,RESTART_IN_PLACE, or MIGRATE_WITHIN_NODE_GROUP. The default value is DEFAULT. For more information, see Maintenance policies. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="maintenance_window" type="object">
  Time window specified for daily maintenance operations. GCE's internal maintenance will be performed within this window. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="maintenance_duration" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Start time of the window. This must be in UTC format that resolves to one of 00:00, 04:00, 08:00,12:00, 16:00, or 20:00. For example, both 13:00-5 and 08:00 are valid. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The name of the resource, provided by the client when initially creating the resource. The resource name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="node_template" type="string">
  URL of the node template to create the node group from. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="share_settings" type="object">
  The share setting for reservations and sole tenancy node groups. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="project_map" type="map(object)">
      A map of project id and project config. This is only valid when share_type's value is SPECIFIC_PROJECTS. Optional.
      <Expandable title="properties">
        <ResponseField name="project_id" type="string">
          The Google Cloud project ID for which the share settings are configured in this project map entry. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="share_type" type="string">
      Type of sharing for this shared-reservation Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="size" type="number">
  Output only. [Output Only] The total number of nodes in the node group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status" type="string">
  The current lifecycle state of the node group, computed by the provider. Possible values are CREATING, DELETING, INVALID, and READY. Optional; if omitted, computed by GCP. **(AI-inferred)**
</ResponseField>

<ResponseField name="zone" type="string">
  Output only. [Output Only] The name of the zone where the node group resides, such as us-central1-a. Optional; if omitted, computed by GCP.
</ResponseField>
