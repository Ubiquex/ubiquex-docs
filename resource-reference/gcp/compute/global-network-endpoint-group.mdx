---
title: "google_compute_global_network_endpoint_group"
description: "Real, generated bindings for `google_compute_global_network_endpoint_group`."
---

`google_compute_global_network_endpoint_group` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own global network endpoint group"})

    		ubx.Resource(compute.GlobalNetworkEndpointGroup, "ci-runner", compute.GlobalNetworkEndpointGroupConfig{
    			Name:                "ci-runner",
    			NetworkEndpointType: "example",
    			DefaultPort:         1,
    			Description:         "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GlobalNetworkEndpointGroup } from "jsr:@ubx/sdk-google/google/compute/global_network_endpoint_group";

    export default stack("payments", () => {
      intent({ summary: "payments own global network endpoint group" });

      resource(GlobalNetworkEndpointGroup, "ci-runner", {
        name: "ci-runner",
        networkEndpointType: "example",
        defaultPort: 1,
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import GlobalNetworkEndpointGroup, GlobalNetworkEndpointGroupConfig

    def describe():
        ubx.intent("payments own global network endpoint group")

        ubx.resource(GlobalNetworkEndpointGroup, "ci-runner", GlobalNetworkEndpointGroupConfig(
            name="ci-runner",
            network_endpoint_type="example",
            default_port=1,
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_global_network_endpoint_group called
    "ci-runner", with network endpoint type example, default port 1,
    description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="default_port" type="number">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="network_endpoint_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
