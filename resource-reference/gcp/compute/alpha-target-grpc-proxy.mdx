---
title: "google_compute_alpha_target_grpc_proxy"
description: "`google_compute_alpha_target_grpc_proxy` routes real gRPC traffic to a backend service via a URL map, the gRPC-specific counterpart to a target HTTP proxy."
---

<Warning>
  This is Google Cloud Compute's own **alpha** release channel -- earlier and less stable than beta. Google may change or remove alpha endpoints at any time, with no deprecation notice -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_compute_target_grpc_proxy`.
</Warning>

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_compute_alpha --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_compute_alpha/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google_compute_alpha/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own alpha target grpc proxy"})

    		ubx.Resource(compute.AlphaComputeAlphaTargetGrpcProxy, "example", compute.AlphaComputeAlphaTargetGrpcProxyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_compute_alpha --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AlphaComputeAlphaTargetGrpcProxy } from "./local-sdk/google_compute_alpha/sdk/typescript/google_compute_alpha/compute/alpha_compute_alpha_target_grpc_proxy.ts";

    export default stack("example", () => {
      intent({ summary: "example own alpha target grpc proxy" });

      resource(AlphaComputeAlphaTargetGrpcProxy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_compute_alpha --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_compute_alpha/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_compute_alpha.compute import AlphaComputeAlphaTargetGrpcProxy, AlphaComputeAlphaTargetGrpcProxyConfig

    def describe():
        ubx.intent("example own alpha target grpc proxy")

        ubx.resource(AlphaComputeAlphaTargetGrpcProxy, "example", AlphaComputeAlphaTargetGrpcProxyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_alpha_target_grpc_proxy called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. A hash of the contents stored in this object. This field is used in optimistic locking. This field will be ignored when inserting a TargetGrpcProxy. An up-to-date fingerprint must be provided in order to patch/update the TargetGrpcProxy; otherwise, the request will fail with error 412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve the TargetGrpcProxy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource type. The server generates this identifier. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#targetGrpcProxy for target grpc proxies. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL with id for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="url_map" type="string">
  URL to the UrlMap resource that defines the mapping from URL to the BackendService. The protocol field in the BackendService must be set to GRPC. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="validate_for_proxyless" type="bool">
  If true, indicates that the BackendServices referenced by the urlMap may be accessed by gRPC applications without using a sidecar proxy. This will enable configuration checks on urlMap and its referenced BackendServices to not allow unsupported features. A gRPC application must use "xds:///" scheme in the target URI of the service it is connecting to. If false, indicates that the BackendServices referenced by the urlMap will be accessed by gRPC applications via a sidecar proxy. In this case, a gRPC application must not use "xds:///" scheme in the target URI of the service it is connecting to Optional; if omitted, computed by Google Cloud.
</ResponseField>
