---
title: "google_compute_region_instance_template"
description: "A complete, runnable google_compute_region_instance_template program, in every SDK language."
---

`google_compute_region_instance_template` -- real, typed bindings generated directly from
real Google Discovery Documents, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region instance template"})

    		ubx.Resource(compute.RegionInstanceTemplate, "ci-runner", compute.RegionInstanceTemplateConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionInstanceTemplate } from "./local-sdk/google/sdk/typescript/google/compute/region_instance_template.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own region instance template" });

      resource(RegionInstanceTemplate, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.compute import RegionInstanceTemplate, RegionInstanceTemplateConfig

    def describe():
        ubx.intent("payments own region instance template")

        ubx.resource(RegionInstanceTemplate, "ci-runner", RegionInstanceTemplateConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_instance_template called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] The creation timestamp for this instance template inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] A unique identifier for this instance template. The server defines this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="properties" type="object">
  The computed properties of the region instance template, containing the effective instance configuration such as machine type, disks, network interfaces, metadata, and scheduling options. This field is output-only and reflects the actual properties used by the template. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="advanced_machine_features" type="object">
      Specifies options for controlling advanced machine features. Options that would traditionally be configured in a BIOS belong here. Features that require operating system support may have corresponding entries in the GuestOsFeatures of anImage (e.g., whether or not the OS in theImage supports nested virtualization being enabled or disabled). Optional.
      <Expandable title="properties">
        <ResponseField name="enable_nested_virtualization" type="bool">
          Whether to enable nested virtualization or not (default is false). Optional.
        </ResponseField>
        <ResponseField name="enable_uefi_networking" type="bool">
          Whether to enable UEFI networking for instance creation. Optional.
        </ResponseField>
        <ResponseField name="performance_monitoring_unit" type="string">
          Type of Performance Monitoring Unit requested on instance. Optional.
        </ResponseField>
        <ResponseField name="threads_per_core" type="number">
          The number of threads per physical core. To disable simultaneous multithreading (SMT) set this to 1. If unset, the maximum number of threads supported per core by the underlying processor is assumed. Optional.
        </ResponseField>
        <ResponseField name="turbo_mode" type="string">
          Turbo frequency mode to use for the instance. Supported modes include: * ALL_CORE_MAX Using empty string or not setting this field will use the platform-specific default turbo mode. Optional.
        </ResponseField>
        <ResponseField name="visible_core_count" type="number">
          The number of physical cores to expose to an instance. Multiply by the number of threads per core to compute the total number of virtual CPUs to expose to the instance. If unset, the number of cores is inferred from the instance's nominal CPU count and the underlying platform's SMT width. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="can_ip_forward" type="bool">
      Enables instances created based on these properties to send packets with source IP addresses other than their own and receive packets with destination IP addresses other than their own. If these instances will be used as an IP gateway or it will be set as the next-hop in a Route resource, specify true. If unsure, leave this set tofalse. See theEnable IP forwarding documentation for more information. Optional.
    </ResponseField>
    <ResponseField name="confidential_instance_config" type="object">
      A set of Confidential Instance options. Optional.
      <Expandable title="properties">
        <ResponseField name="confidential_instance_type" type="string">
          Defines the type of technology used by the confidential instance. Optional.
        </ResponseField>
        <ResponseField name="enable_confidential_compute" type="bool">
          Defines whether the instance should have confidential compute enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      An optional text description for the instances that are created from these properties. Optional.
    </ResponseField>
    <ResponseField name="disks" type="list(object)">
      An array of disks that are associated with the instances that are created from these properties. Optional.
      <Expandable title="properties">
        <ResponseField name="architecture" type="string">
          The CPU architecture for the disk. Allowed values are ARM64 and X86_64. ARCHITECTURE_UNSPECIFIED means the architecture is not explicitly set. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="auto_delete" type="bool">
          Whether the disk should be automatically deleted when the instance to which it is attached is deleted. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="boot" type="bool">
          Indicates whether this disk is used as a boot disk. Only one disk in a template can be marked as boot. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="device_name" type="string">
          The device name of the disk as seen by the guest OS. This name can be used to reference the disk from within the instance, for example when mounting. If not specified, a default device name is assigned by Google Compute Engine. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_encryption_key" type="object">
          The customer-supplied encryption key used to encrypt the disk. This can include a raw base64-encoded key, an RSA-encrypted key, or a reference to a Cloud KMS key. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key_name" type="string">
              The resource name of the Cloud KMS key used to encrypt the disk. This is a customer-managed encryption key (CMEK). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kms_key_service_account" type="string">
              The service account used for the KMS encryption operation on the disk. If not specified, the Compute Engine default service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="raw_key" type="string">
              The raw 256-bit disk encryption key, provided in base64 format, used to encrypt the disk attached to the instance template. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rsa_encrypted_key" type="string">
              The RSA-encrypted customer-supplied encryption key, used as an alternative to raw_key to protect the key in transit. The key is a 256-bit key encoded in RFC 4648 base64, wrapped with the RSA public key provided by Google. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sha256" type="string">
              The SHA256 hash of the customer-supplied encryption key used to encrypt the disk. This is computed automatically from the raw_key and is used for key verification. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disk_size_gb" type="string">
          The size of the disk in gigabytes. If not specified, the disk size is taken from the source image. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="force_attach" type="bool">
          If true, allows the disk to be attached even if it is already attached to another instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="guest_os_features" type="list(object)">
          Specifies a list of features that must be enabled on the guest operating system for this disk. This is commonly used to enable UEFI boot or multi-queue SCSI for bootable images. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              The type of guest OS feature. Possible values include: BARE_METAL_LINUX_COMPATIBLE, CCA_CAPABLE, FEATURE_TYPE_UNSPECIFIED, GVNIC, IDPF, MULTI_IP_SUBNET, SECURE_BOOT, SEV_CAPABLE, SEV_LIVE_MIGRATABLE, SEV_LIVE_MIGRATABLE_V2, SEV_SNP_CAPABLE, SNP_SVSM_CAPABLE, TDX_CAPABLE, UEFI_COMPATIBLE, VIRTIO_SCSI_MULTIQUEUE, WINDOWS. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="index" type="number">
          The zero-based index of the disk, where index 0 is reserved for the boot disk and subsequent disks are numbered sequentially in the instance's disk list. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="initialize_params" type="object">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="architecture" type="string">
              The architecture of the disk, indicating the CPU type. Allowed values are ARCHITECTURE_UNSPECIFIED (default), ARM64, or X86_64. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="disk_name" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="disk_size_gb" type="string">
              The size of the boot disk, in gigabytes (GB), for the disk initialization parameters. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_type" type="string">
              The type of disk to use. Can be specified as a full or partial URL to a disk type resource, such as 'pd-ssd' or 'zones/us-east1-a/diskTypes/pd-standard'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enable_confidential_compute" type="bool">
              Enables Confidential Compute for the disk created by this initialization parameter. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="labels" type="map(string)">
              A map of key/value pairs to label the disk. Labels can be used to organize and identify the disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="licenses" type="list(string)">
              A list of license URLs to apply to the disk image. Overrides licenses inherited from the source image. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="on_update_action" type="string">
              Specifies the action to take on this disk when the instance template is updated. Valid values are RECREATE_DISK (always recreate the disk), RECREATE_DISK_IF_SOURCE_CHANGED (recreate only if the source image or snapshot changes), and USE_EXISTING_DISK (preserve the existing disk). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="provisioned_iops" type="string">
              The number of provisioned IOPS (input/output operations per second) for the disk, specified as a string. This is used for disk types that support performance provisioning. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="provisioned_throughput" type="string">
              The provisioned throughput of the disk in MB/s, used to configure performance for supported disk types. Only applicable to hyperdisk-balanced and hyperdisk-throughput disks. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replica_zones" type="list(string)">
              The zones that the regional persistent disk should be replicated across. This is required for regional persistent disks (when the disk type is regional). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_manager_tags" type="map(string)">
              Resource manager tags to be applied to the disk. These are key-value pairs used for organizing and controlling access to resources. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_policies" type="list(string)">
              List of resource policy self-links (e.g., snapshot schedule policies) to be attached to the disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_image" type="string">
              The source image to use for this disk. Can be a full image URL, self link, or image name in the same project or a shared image family. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_image_encryption_key" type="object">
              The customer-supplied encryption key used to decrypt the source image when creating the disk. This is required if the source image is encrypted with a customer-managed key. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kms_key_name" type="string">
                  The name of the Cloud KMS customer-managed encryption key (CMEK) used to encrypt the source image. Must be specified as a resource name in the format 'projects/PROJECT_ID/locations/LOCATION/keyRings/KEY_RING/cryptoKeys/KEY'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kms_key_service_account" type="string">
                  The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="raw_key" type="string">
                  The base64-encoded 256-bit AES key used to decrypt the source image. Must be kept secret. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rsa_encrypted_key" type="string">
                  The RSA-wrapped customer-supplied encryption key, base64-encoded, used to decrypt the source image when creating a disk from an encrypted image. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sha256" type="string">
                  SHA-256 hash of the customer-supplied encryption key used to encrypt the source image. This value is used to verify the key identity without exposing the key itself. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="source_snapshot" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="source_snapshot_encryption_key" type="object">
              The customer-supplied encryption key used to decrypt the source snapshot when creating this disk. This is required if the source snapshot is encrypted with a customer-supplied key. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kms_key_name" type="string">
                  The name of the encryption key that is stored in Google Cloud KMS. This key is used to encrypt the source snapshot. For example: 'projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;keyRing&#125;/cryptoKeys/&#123;key&#125;'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kms_key_service_account" type="string">
                  The service account being used for the KMS key to decrypt the source snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="raw_key" type="string">
                  The raw 256-bit key (base64 encoded) used to encrypt or decrypt the source snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rsa_encrypted_key" type="string">
                  The RSA-encrypted customer-supplied encryption key used to decrypt the source snapshot. Provide the key base64-encoded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sha256" type="string">
                  The SHA256 hash of the customer-supplied encryption key that was used to encrypt the source snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="storage_pool" type="string">
              The URL of the storage pool in which the disk is created. If not specified, the default storage pool is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="interface" type="string">
          The disk interface type. Valid values are 'NVME' and 'SCSI', which determine the protocol used to communicate with the attached disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The type of the resource. For attached disks in instance templates, this is always 'compute#attachedDisk'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="licenses" type="list(string)">
          A list of license URIs associated with the disk, typically inherited from the source image and used to identify applicable software licenses. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mode" type="string">
          The disk mode, either READ_ONLY or READ_WRITE. Specifies whether the disk is attached as read-only or read-write. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="saved_state" type="string">
          Indicates the saved state of the disk. Valid values are `PRESERVED` to preserve the disk state and `DISK_SAVED_STATE_UNSPECIFIED` for the default unspecified state. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="shielded_instance_initial_state" type="object">
          This block configures the initial trusted state of the shielded instance for this disk, including integrity measurements used to verify the boot chain. It is only supported for the boot disk of a shielded VM instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dbs" type="list(object)">
              The list of database certificates (DBs) for the shielded instance initial state. Each object in the list specifies a base64-encoded certificate content and its file type. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded content of the database (db) file for the Shielded VM initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The file type of the database file, indicating how the content is encoded. Allowed values are BIN (binary), X509 (certificate), and UNDEFINED. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="dbxs" type="list(object)">
              A list of DBX (denied boot) binaries to include in the shielded instance initial state. Each object contains the file content and type (e.g., BINARY or X509). Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded content of the database exchange (dbx) file for the shielded instance initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The type of the file, indicating whether it is a binary file (BIN) or an X509 certificate (X509). The value UNDEFINED indicates that the type is not specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="keks" type="list(object)">
              A list of key exchange keys (KEKs) for the shielded VM initial state. Each item is an object representing a key entry within the shielded instance initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The content of the Key Encryption Key (KEK) used in the shielded instance initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The format of the key or certificate file. Valid values are BIN (raw binary), X509 (PEM-encoded certificate), or UNDEFINED (default, if not set). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="pk" type="object">
              The Platform Key (PK) certificate used by Secure Boot to verify the boot loader and OS kernel. This object contains the certificate content and its file type. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The raw content of the platform key (PK) certificate in the secure keys file format, used for the shielded VM initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The format of the certificate file. Valid values are BIN (binary), X509 (PEM encoded), and UNDEFINED. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source" type="string">
          The URL or name of the source disk to use for this disk. This is used when creating a disk from an existing persistent disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of disk. Valid values are PERSISTENT and SCRATCH. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="guest_accelerators" type="list(object)">
      A list of guest accelerator cards' type and count to use for instances created from these properties. Optional.
      <Expandable title="properties">
        <ResponseField name="accelerator_count" type="number">
          Specifies the number of accelerator cards of the specified type to attach to the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="accelerator_type" type="string">
          The type of accelerator (GPU) to attach to the instance. Specify the accelerator type as a full or partial URL, for example 'nvidia-tesla-t4' or 'projects/project/zones/zone/acceleratorTypes/nvidia-tesla-t4'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="key_revocation_action_type" type="string">
      KeyRevocationActionType of the instance. Supported options are "STOP" and "NONE". The default value is "NONE" if it is not specified. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Labels to apply to instances that are created from these properties. Optional.
    </ResponseField>
    <ResponseField name="local_ssd_encryption_mode" type="string">
      Specifies which method should be used for encrypting the Local SSDs attached to the VM. Optional.
    </ResponseField>
    <ResponseField name="machine_type" type="string">
      The machine type to use for instances that are created from these properties. This field only accepts a machine type name, for example `n2-standard-4`. If you use the machine type full or partial URL, for example `projects/my-l7ilb-project/zones/us-central1-a/machineTypes/n2-standard-4`, the request will result in an `INTERNAL_ERROR`. Optional.
    </ResponseField>
    <ResponseField name="metadata" type="object">
      A metadata key/value entry. Optional.
      <Expandable title="properties">
        <ResponseField name="fingerprint" type="string">
          Specifies a fingerprint for this request, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve the resource. Optional.
        </ResponseField>
        <ResponseField name="items" type="list(object)">
          Array of key/value pairs. The total size of all keys and values must be less than 512 KB. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of this metadata entry. Each item in the metadata is a key-value pair. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The value of the metadata item, paired with the associated key in the instance template's metadata. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kind" type="string">
          Output only. [Output Only] Type of the resource. Always compute#metadata for metadata. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="min_cpu_platform" type="string">
      Minimum cpu/platform to be used by instances. The instance may be scheduled on the specified or newer cpu/platform. Applicable values are the friendly names of CPU platforms, such asminCpuPlatform: "Intel Haswell" orminCpuPlatform: "Intel Sandy Bridge". For more information, read Specifying a Minimum CPU Platform. Optional.
    </ResponseField>
    <ResponseField name="network_interfaces" type="list(object)">
      An array of network access configurations for this interface. Optional.
      <Expandable title="properties">
        <ResponseField name="access_configs" type="list(object)">
          List of access configurations for this network interface, used to assign external IP addresses. Each access config can specify a NAT IP and network tier. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ipv6" type="string">
              The external IPv6 address to assign to the instance's network interface access config. If not specified, an IPv6 address is automatically allocated when IPv6 is enabled on the network. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_ipv6_prefix_length" type="number">
              The length, in bits, of the external IPv6 prefix associated with this access configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The kind of the access config, which is always 'compute#accessConfig'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="nat_ip" type="string">
              The external IP address to assign to the instance's network interface. If unset, an ephemeral external IP is assigned automatically. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The network tier to assign to this access config. Valid values are PREMIUM, STANDARD, FIXED_STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD. If unspecified, defaults to PREMIUM. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="public_ptr_domain_name" type="string">
              Sets a custom PTR (reverse DNS) record for the external IP address of this access config. When specified, the instance's public IP will resolve to this domain name in reverse DNS lookups. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="security_policy" type="string">
              The Google Cloud Armor security policy to apply to this access config. Specify the security policy name or URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="set_public_ptr" type="bool">
              Specifies whether a public DNS PTR record should be created for the external IP address assigned to this access config. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of access configuration. Valid values are ONE_TO_ONE_NAT for external NAT (IPv4) and DIRECT_IPV6 for direct IPv6 access. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alias_ip_ranges" type="list(object)">
          A list of alias IP ranges for this network interface. Each alias IP range specifies an IP range (in CIDR format) to assign to this interface and optionally a subnetwork secondary range name. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="ip_cidr_range" type="string">
              The IP CIDR range for the alias IP. This range must be within the primary or secondary IP ranges of the subnetwork. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork_range_name" type="string">
              The name of a subnetwork secondary IP range from which to allocate an IP address range. If not specified, the primary IP range of the subnetwork is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alias_ipv6_ranges" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="ip_cidr_range" type="string">
              The IPv6 CIDR range (in CIDR notation, e.g., /64) for the alias IPv6 range associated with the network interface. The range must be within the IPv6 address range of the subnet. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork_range_name" type="string">
              The name of the subnetwork's secondary IP range to use for the alias IPv6 range. This must correspond to a secondary range of the subnetwork that the network interface is attached to. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enable_vpc_scoped_dns" type="bool">
          Enables VPC-scoped DNS on this network interface. When set to true, the instance uses the VPC's DNS settings to resolve internal hostnames. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fingerprint" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="igmp_query" type="string">
          Controls whether IGMP query is enabled on this network interface and which version is used. Valid values are IGMP_QUERY_DISABLED (no IGMP query) and IGMP_QUERY_V2 (enable IGMPv2 query). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="internal_ipv6_prefix_length" type="number">
          The prefix length in bits for the internal IPv6 range assigned to the network interface. When set, this determines the size of the IPv6 subnet; the only supported value is /96. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv6_access_configs" type="list(object)">
          A list of IPv6 access configurations for the network interface. Each configuration specifies the external IPv6 address and network tier. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ipv6" type="string">
              The external IPv6 address assigned to the instance via this IPv6 access configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_ipv6_prefix_length" type="number">
              The prefix length of the external IPv6 address. Valid values are 32, 64, 96, and 128. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The type of the resource. For IPv6 access configurations, this is always 'compute#ipv6AccessConfig'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the IPv6 access configuration. It is used to identify the access config within the network interface. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nat_ip" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The network tier for this IPv6 access config. Valid values are FIXED_STANDARD, PREMIUM, STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="public_ptr_domain_name" type="string">
              The domain name to be used for reverse DNS lookup (PTR record) for the IPv6 address. If not specified, no reverse DNS record is created. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="security_policy" type="string">
              The URL or reference of a Compute Engine security policy to apply to this IPv6 access configuration. It allows you to attach a security policy to the external IPv6 address. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="set_public_ptr" type="bool">
              When true, sets a public DNS PTR record for the external IPv6 address associated with this access config. When false (default), no public PTR record is created. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of IPv6 access configuration. Valid values are `DIRECT_IPV6` for direct IPv6 access and `ONE_TO_ONE_NAT` for one-to-one NAT. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ipv6_access_type" type="string">
          The access type for IPv6 addresses on this network interface. Valid values are EXTERNAL (default) and INTERNAL. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv6_address" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="kind" type="string">
          The resource type, always compute#networkInterface for network interface resources. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="network" type="string">
          The name or self_link of the network to attach this interface to. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_attachment" type="string">
          The URL of the network attachment resource that this network interface should be attached to. When set, the interface connects to a VPC via the specified network attachment. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_ip" type="string">
          The private IPv4 address to assign to the interface. If not specified, an ephemeral address is automatically allocated from the subnet. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="nic_type" type="string">
          The type of network interface controller (NIC) to attach. Allowed values: GVNIC, IDPF, IRDMA, MRDMA, UNSPECIFIED_NIC_TYPE, VIRTIO_NET. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="parent_nic_name" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="queue_count" type="number">
          The number of queues for this network interface. If not specified, the number of queues is automatically selected based on the machine type. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_class_id" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="stack_type" type="string">
          The stack type for the network interface. Valid values are IPV4_IPV6 (dual-stack), IPV4_ONLY, and IPV6_ONLY. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnetwork" type="string">
          The name or self-link of the subnetwork to attach this network interface to. Must be in the same region as the instance template. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vlan" type="number">
          The VLAN ID to associate with this network interface when using a network attachment. Required if network_attachment is specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_performance_config" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="total_egress_bandwidth_tier" type="string">
          Specifies the tier for total egress bandwidth for the instance. Allowed values are DEFAULT and TIER_1. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_ipv6_google_access" type="string">
      The private IPv6 google access type for VMs. If not specified, use INHERIT_FROM_SUBNETWORK as default. Note that for MachineImage, this is not supported yet. Optional.
    </ResponseField>
    <ResponseField name="reservation_affinity" type="object">
      Specifies the reservations that this instance can consume from. Optional.
      <Expandable title="properties">
        <ResponseField name="consume_reservation_type" type="string">
          Specifies the type of reservation from which this instance can consume resources: ANY_RESERVATION (default),SPECIFIC_RESERVATION, or NO_RESERVATION. See Consuming reserved instances for examples. Optional.
        </ResponseField>
        <ResponseField name="key" type="string">
          Corresponds to the label key of a reservation resource. To target aSPECIFIC_RESERVATION by name, specifygoogleapis.com/reservation-name as the key and specify the name of your reservation as its value. Optional.
        </ResponseField>
        <ResponseField name="values" type="list(string)">
          Corresponds to the label values of a reservation resource. This can be either a name to a reservation in the same project or "projects/different-project/reservations/some-reservation-name" to target a shared reservation in the same zone but in a different project. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the instance. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
    <ResponseField name="resource_policies" type="list(string)">
      Resource policies (names, not URLs) applied to instances created from these properties. Note that for MachineImage, this is not supported yet. Optional.
    </ResponseField>
    <ResponseField name="scheduling" type="object">
      Sets the scheduling options for an Instance. Optional.
      <Expandable title="properties">
        <ResponseField name="automatic_restart" type="bool">
          Specifies whether the instance should be automatically restarted if it is terminated by Compute Engine (not terminated by a user). You can only set the automatic restart option for standard instances.Preemptible instances cannot be automatically restarted. By default, this is set to true so an instance is automatically restarted if it is terminated by Compute Engine. Optional.
        </ResponseField>
        <ResponseField name="availability_domain" type="number">
          Specifies the availability domain to place the instance in. The value must be a number between 1 and the number of availability domains specified in the spread placement policy attached to the instance. Optional.
        </ResponseField>
        <ResponseField name="graceful_shutdown" type="object">
          The configuration for gracefully shutting down the instance. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Opts-in for graceful shutdown. Optional.
            </ResponseField>
            <ResponseField name="max_duration" type="object">
              A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="host_error_timeout_seconds" type="number">
          Specify the time in seconds for host error detection, the value must be within the range of [90, 330] with the increment of 30, if unset, the default behavior of host error recovery will be used. Optional.
        </ResponseField>
        <ResponseField name="instance_termination_action" type="string">
          Specifies the termination action for the instance. Optional.
        </ResponseField>
        <ResponseField name="local_ssd_recovery_timeout" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="location_hint" type="string">
          An opaque location hint used to place the instance close to other resources. This field is for use by internal tools that use the public API. Optional.
        </ResponseField>
        <ResponseField name="max_run_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="min_node_cpus" type="number">
          The minimum number of virtual CPUs this instance will consume when running on a sole-tenant node. Optional.
        </ResponseField>
        <ResponseField name="node_affinities" type="list(object)">
          A set of node affinity and anti-affinity configurations. Refer toConfiguring node affinity for more information. Overrides reservationAffinity. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key for the node affinity label, used to match instances to node groups. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              Specifies the operator for the node affinity. Possible values are IN (match the given values), NOT_IN (do not match the given values), and OPERATOR_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              Specifies the values of the node affinity key. These are the actual label values used to match nodes. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="on_host_maintenance" type="string">
          Defines the maintenance behavior for this instance. For standard instances, the default behavior is MIGRATE. Forpreemptible instances, the default and only possible behavior is TERMINATE. For more information, see Set VM host maintenance policy. Optional.
        </ResponseField>
        <ResponseField name="on_instance_stop_action" type="object">
          Defines the behaviour for instances with the instance_termination_actionSTOP. Optional.
          <Expandable title="properties">
            <ResponseField name="discard_local_ssd" type="bool">
              If true, the contents of any attached Local SSD disks will be discarded else, the Local SSD data will be preserved when the instance is stopped at the end of the run duration/termination time. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="preemptible" type="bool">
          Defines whether the instance is preemptible. This can only be set during instance creation or while the instance isstopped and therefore, in a `TERMINATED` state. SeeInstance Life Cycle for more information on the possible instance states. Optional.
        </ResponseField>
        <ResponseField name="preemption_notice_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="provisioning_model" type="string">
          Specifies the provisioning model of the instance. Optional.
        </ResponseField>
        <ResponseField name="skip_guest_os_shutdown" type="bool">
          Default is false and there will be 120 seconds between GCE ACPI G2 Soft Off and ACPI G3 Mechanical Off for Standard VMs and 30 seconds for Spot VMs. Optional.
        </ResponseField>
        <ResponseField name="termination_time" type="string">
          Specifies the timestamp, when the instance will be terminated, inRFC3339 text format. If specified, the instance termination action will be performed at the termination time. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_accounts" type="list(object)">
      A list of service accounts with specified scopes. Access tokens for these service accounts are available to the instances that are created from these properties. Use metadata queries to obtain the access tokens for these instances. Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          The email address of the service account to assign to the instance. If not specified, the default Compute Engine service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scopes" type="list(string)">
          The list of OAuth scopes to be granted to the service account. These scopes determine the API access permissions for the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="shielded_instance_config" type="object">
      A set of Shielded Instance options. Optional.
      <Expandable title="properties">
        <ResponseField name="enable_integrity_monitoring" type="bool">
          Defines whether the instance has integrity monitoring enabled.Enabled by default. Optional.
        </ResponseField>
        <ResponseField name="enable_secure_boot" type="bool">
          Defines whether the instance has Secure Boot enabled.Disabled by default. Optional.
        </ResponseField>
        <ResponseField name="enable_vtpm" type="bool">
          Defines whether the instance has the vTPM enabled.Enabled by default. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="object">
      A set of instance tags. Optional.
      <Expandable title="properties">
        <ResponseField name="fingerprint" type="string">
          Specifies a fingerprint for this request, which is essentially a hash of the tags' contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update tags. You must always provide an up-to-date fingerprint hash in order to update or change tags. To see the latest fingerprint, make get() request to the instance. Optional.
        </ResponseField>
        <ResponseField name="items" type="list(string)">
          An array of tags. Each tag must be 1-63 characters long, and comply with RFC1035. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="workload_identity_config" type="object">
      Configures the workload identity settings for the instance template, enabling workload identity on instances created from it. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="identity" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="identity_certificate_enabled" type="bool">
          Enables the identity certificate for the workload identity configuration. When set to true, the instance template will use an identity certificate, which is typically used with Google Kubernetes Engine to provide a stable identity for workloads. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the instance template resides. Only applicable for regional resources. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] The URL for this instance template. The server defines this URL. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instance" type="string">
  The source instance used to create the template. You can provide this as a partial or full URL to the resource. For example, the following are valid values: - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance - projects/project/zones/zone/instances/instance Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instance_params" type="object">
  A specification of the parameters to use when creating the instance template from a source instance. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="disk_configs" type="list(object)">
      Attached disks configuration. If not provided, defaults are applied: For boot disk and any other R/W disks, the source images for each disk will be used. For read-only disks, they will be attached in read-only mode. Local SSD disks will be created as blank volumes. Optional.
      <Expandable title="properties">
        <ResponseField name="auto_delete" type="bool">
          Whether the disk should be automatically deleted when the instance is deleted. Set to true to delete the disk along with the instance, or false to retain it. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="custom_image" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="device_name" type="string">
          The device name to assign to the disk, used to identify the disk inside the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instantiate_from" type="string">
          Specifies how the disk is instantiated from the source instance in a region instance template. Valid values are: ATTACH_READ_ONLY (attach the disk as read-only), BLANK (create an empty disk), CUSTOM_IMAGE (use a custom image), DEFAULT (use the default behavior), DO_NOT_INCLUDE (omit the disk from the template), SOURCE_IMAGE (use the source instance's boot image), and SOURCE_IMAGE_FAMILY (use the source image family). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] The creation timestamp for this instance template inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] A unique identifier for this instance template. The server defines this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="properties" type="object">
  The computed properties of the region instance template, containing the effective instance configuration such as machine type, disks, network interfaces, metadata, and scheduling options. This field is output-only and reflects the actual properties used by the template. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="advanced_machine_features" type="object">
      Specifies options for controlling advanced machine features. Options that would traditionally be configured in a BIOS belong here. Features that require operating system support may have corresponding entries in the GuestOsFeatures of anImage (e.g., whether or not the OS in theImage supports nested virtualization being enabled or disabled). Optional.
      <Expandable title="properties">
        <ResponseField name="enable_nested_virtualization" type="bool">
          Whether to enable nested virtualization or not (default is false). Optional.
        </ResponseField>
        <ResponseField name="enable_uefi_networking" type="bool">
          Whether to enable UEFI networking for instance creation. Optional.
        </ResponseField>
        <ResponseField name="performance_monitoring_unit" type="string">
          Type of Performance Monitoring Unit requested on instance. Optional.
        </ResponseField>
        <ResponseField name="threads_per_core" type="number">
          The number of threads per physical core. To disable simultaneous multithreading (SMT) set this to 1. If unset, the maximum number of threads supported per core by the underlying processor is assumed. Optional.
        </ResponseField>
        <ResponseField name="turbo_mode" type="string">
          Turbo frequency mode to use for the instance. Supported modes include: * ALL_CORE_MAX Using empty string or not setting this field will use the platform-specific default turbo mode. Optional.
        </ResponseField>
        <ResponseField name="visible_core_count" type="number">
          The number of physical cores to expose to an instance. Multiply by the number of threads per core to compute the total number of virtual CPUs to expose to the instance. If unset, the number of cores is inferred from the instance's nominal CPU count and the underlying platform's SMT width. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="can_ip_forward" type="bool">
      Enables instances created based on these properties to send packets with source IP addresses other than their own and receive packets with destination IP addresses other than their own. If these instances will be used as an IP gateway or it will be set as the next-hop in a Route resource, specify true. If unsure, leave this set tofalse. See theEnable IP forwarding documentation for more information. Optional.
    </ResponseField>
    <ResponseField name="confidential_instance_config" type="object">
      A set of Confidential Instance options. Optional.
      <Expandable title="properties">
        <ResponseField name="confidential_instance_type" type="string">
          Defines the type of technology used by the confidential instance. Optional.
        </ResponseField>
        <ResponseField name="enable_confidential_compute" type="bool">
          Defines whether the instance should have confidential compute enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      An optional text description for the instances that are created from these properties. Optional.
    </ResponseField>
    <ResponseField name="disks" type="list(object)">
      An array of disks that are associated with the instances that are created from these properties. Optional.
      <Expandable title="properties">
        <ResponseField name="architecture" type="string">
          The CPU architecture for the disk. Allowed values are ARM64 and X86_64. ARCHITECTURE_UNSPECIFIED means the architecture is not explicitly set. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="auto_delete" type="bool">
          Whether the disk should be automatically deleted when the instance to which it is attached is deleted. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="boot" type="bool">
          Indicates whether this disk is used as a boot disk. Only one disk in a template can be marked as boot. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="device_name" type="string">
          The device name of the disk as seen by the guest OS. This name can be used to reference the disk from within the instance, for example when mounting. If not specified, a default device name is assigned by Google Compute Engine. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_encryption_key" type="object">
          The customer-supplied encryption key used to encrypt the disk. This can include a raw base64-encoded key, an RSA-encrypted key, or a reference to a Cloud KMS key. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key_name" type="string">
              The resource name of the Cloud KMS key used to encrypt the disk. This is a customer-managed encryption key (CMEK). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kms_key_service_account" type="string">
              The service account used for the KMS encryption operation on the disk. If not specified, the Compute Engine default service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="raw_key" type="string">
              The raw 256-bit disk encryption key, provided in base64 format, used to encrypt the disk attached to the instance template. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rsa_encrypted_key" type="string">
              The RSA-encrypted customer-supplied encryption key, used as an alternative to raw_key to protect the key in transit. The key is a 256-bit key encoded in RFC 4648 base64, wrapped with the RSA public key provided by Google. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sha256" type="string">
              The SHA256 hash of the customer-supplied encryption key used to encrypt the disk. This is computed automatically from the raw_key and is used for key verification. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disk_size_gb" type="string">
          The size of the disk in gigabytes. If not specified, the disk size is taken from the source image. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="force_attach" type="bool">
          If true, allows the disk to be attached even if it is already attached to another instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="guest_os_features" type="list(object)">
          Specifies a list of features that must be enabled on the guest operating system for this disk. This is commonly used to enable UEFI boot or multi-queue SCSI for bootable images. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              The type of guest OS feature. Possible values include: BARE_METAL_LINUX_COMPATIBLE, CCA_CAPABLE, FEATURE_TYPE_UNSPECIFIED, GVNIC, IDPF, MULTI_IP_SUBNET, SECURE_BOOT, SEV_CAPABLE, SEV_LIVE_MIGRATABLE, SEV_LIVE_MIGRATABLE_V2, SEV_SNP_CAPABLE, SNP_SVSM_CAPABLE, TDX_CAPABLE, UEFI_COMPATIBLE, VIRTIO_SCSI_MULTIQUEUE, WINDOWS. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="index" type="number">
          The zero-based index of the disk, where index 0 is reserved for the boot disk and subsequent disks are numbered sequentially in the instance's disk list. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="initialize_params" type="object">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="architecture" type="string">
              The architecture of the disk, indicating the CPU type. Allowed values are ARCHITECTURE_UNSPECIFIED (default), ARM64, or X86_64. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="disk_name" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="disk_size_gb" type="string">
              The size of the boot disk, in gigabytes (GB), for the disk initialization parameters. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_type" type="string">
              The type of disk to use. Can be specified as a full or partial URL to a disk type resource, such as 'pd-ssd' or 'zones/us-east1-a/diskTypes/pd-standard'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enable_confidential_compute" type="bool">
              Enables Confidential Compute for the disk created by this initialization parameter. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="labels" type="map(string)">
              A map of key/value pairs to label the disk. Labels can be used to organize and identify the disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="licenses" type="list(string)">
              A list of license URLs to apply to the disk image. Overrides licenses inherited from the source image. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="on_update_action" type="string">
              Specifies the action to take on this disk when the instance template is updated. Valid values are RECREATE_DISK (always recreate the disk), RECREATE_DISK_IF_SOURCE_CHANGED (recreate only if the source image or snapshot changes), and USE_EXISTING_DISK (preserve the existing disk). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="provisioned_iops" type="string">
              The number of provisioned IOPS (input/output operations per second) for the disk, specified as a string. This is used for disk types that support performance provisioning. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="provisioned_throughput" type="string">
              The provisioned throughput of the disk in MB/s, used to configure performance for supported disk types. Only applicable to hyperdisk-balanced and hyperdisk-throughput disks. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replica_zones" type="list(string)">
              The zones that the regional persistent disk should be replicated across. This is required for regional persistent disks (when the disk type is regional). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_manager_tags" type="map(string)">
              Resource manager tags to be applied to the disk. These are key-value pairs used for organizing and controlling access to resources. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_policies" type="list(string)">
              List of resource policy self-links (e.g., snapshot schedule policies) to be attached to the disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_image" type="string">
              The source image to use for this disk. Can be a full image URL, self link, or image name in the same project or a shared image family. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_image_encryption_key" type="object">
              The customer-supplied encryption key used to decrypt the source image when creating the disk. This is required if the source image is encrypted with a customer-managed key. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kms_key_name" type="string">
                  The name of the Cloud KMS customer-managed encryption key (CMEK) used to encrypt the source image. Must be specified as a resource name in the format 'projects/PROJECT_ID/locations/LOCATION/keyRings/KEY_RING/cryptoKeys/KEY'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kms_key_service_account" type="string">
                  The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="raw_key" type="string">
                  The base64-encoded 256-bit AES key used to decrypt the source image. Must be kept secret. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rsa_encrypted_key" type="string">
                  The RSA-wrapped customer-supplied encryption key, base64-encoded, used to decrypt the source image when creating a disk from an encrypted image. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sha256" type="string">
                  SHA-256 hash of the customer-supplied encryption key used to encrypt the source image. This value is used to verify the key identity without exposing the key itself. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="source_snapshot" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="source_snapshot_encryption_key" type="object">
              The customer-supplied encryption key used to decrypt the source snapshot when creating this disk. This is required if the source snapshot is encrypted with a customer-supplied key. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kms_key_name" type="string">
                  The name of the encryption key that is stored in Google Cloud KMS. This key is used to encrypt the source snapshot. For example: 'projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;keyRing&#125;/cryptoKeys/&#123;key&#125;'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kms_key_service_account" type="string">
                  The service account being used for the KMS key to decrypt the source snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="raw_key" type="string">
                  The raw 256-bit key (base64 encoded) used to encrypt or decrypt the source snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rsa_encrypted_key" type="string">
                  The RSA-encrypted customer-supplied encryption key used to decrypt the source snapshot. Provide the key base64-encoded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sha256" type="string">
                  The SHA256 hash of the customer-supplied encryption key that was used to encrypt the source snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="storage_pool" type="string">
              The URL of the storage pool in which the disk is created. If not specified, the default storage pool is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="interface" type="string">
          The disk interface type. Valid values are 'NVME' and 'SCSI', which determine the protocol used to communicate with the attached disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The type of the resource. For attached disks in instance templates, this is always 'compute#attachedDisk'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="licenses" type="list(string)">
          A list of license URIs associated with the disk, typically inherited from the source image and used to identify applicable software licenses. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mode" type="string">
          The disk mode, either READ_ONLY or READ_WRITE. Specifies whether the disk is attached as read-only or read-write. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="saved_state" type="string">
          Indicates the saved state of the disk. Valid values are `PRESERVED` to preserve the disk state and `DISK_SAVED_STATE_UNSPECIFIED` for the default unspecified state. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="shielded_instance_initial_state" type="object">
          This block configures the initial trusted state of the shielded instance for this disk, including integrity measurements used to verify the boot chain. It is only supported for the boot disk of a shielded VM instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dbs" type="list(object)">
              The list of database certificates (DBs) for the shielded instance initial state. Each object in the list specifies a base64-encoded certificate content and its file type. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded content of the database (db) file for the Shielded VM initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The file type of the database file, indicating how the content is encoded. Allowed values are BIN (binary), X509 (certificate), and UNDEFINED. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="dbxs" type="list(object)">
              A list of DBX (denied boot) binaries to include in the shielded instance initial state. Each object contains the file content and type (e.g., BINARY or X509). Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded content of the database exchange (dbx) file for the shielded instance initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The type of the file, indicating whether it is a binary file (BIN) or an X509 certificate (X509). The value UNDEFINED indicates that the type is not specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="keks" type="list(object)">
              A list of key exchange keys (KEKs) for the shielded VM initial state. Each item is an object representing a key entry within the shielded instance initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The content of the Key Encryption Key (KEK) used in the shielded instance initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The format of the key or certificate file. Valid values are BIN (raw binary), X509 (PEM-encoded certificate), or UNDEFINED (default, if not set). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="pk" type="object">
              The Platform Key (PK) certificate used by Secure Boot to verify the boot loader and OS kernel. This object contains the certificate content and its file type. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The raw content of the platform key (PK) certificate in the secure keys file format, used for the shielded VM initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The format of the certificate file. Valid values are BIN (binary), X509 (PEM encoded), and UNDEFINED. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source" type="string">
          The URL or name of the source disk to use for this disk. This is used when creating a disk from an existing persistent disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of disk. Valid values are PERSISTENT and SCRATCH. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="guest_accelerators" type="list(object)">
      A list of guest accelerator cards' type and count to use for instances created from these properties. Optional.
      <Expandable title="properties">
        <ResponseField name="accelerator_count" type="number">
          Specifies the number of accelerator cards of the specified type to attach to the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="accelerator_type" type="string">
          The type of accelerator (GPU) to attach to the instance. Specify the accelerator type as a full or partial URL, for example 'nvidia-tesla-t4' or 'projects/project/zones/zone/acceleratorTypes/nvidia-tesla-t4'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="key_revocation_action_type" type="string">
      KeyRevocationActionType of the instance. Supported options are "STOP" and "NONE". The default value is "NONE" if it is not specified. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Labels to apply to instances that are created from these properties. Optional.
    </ResponseField>
    <ResponseField name="local_ssd_encryption_mode" type="string">
      Specifies which method should be used for encrypting the Local SSDs attached to the VM. Optional.
    </ResponseField>
    <ResponseField name="machine_type" type="string">
      The machine type to use for instances that are created from these properties. This field only accepts a machine type name, for example `n2-standard-4`. If you use the machine type full or partial URL, for example `projects/my-l7ilb-project/zones/us-central1-a/machineTypes/n2-standard-4`, the request will result in an `INTERNAL_ERROR`. Optional.
    </ResponseField>
    <ResponseField name="metadata" type="object">
      A metadata key/value entry. Optional.
      <Expandable title="properties">
        <ResponseField name="fingerprint" type="string">
          Specifies a fingerprint for this request, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve the resource. Optional.
        </ResponseField>
        <ResponseField name="items" type="list(object)">
          Array of key/value pairs. The total size of all keys and values must be less than 512 KB. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of this metadata entry. Each item in the metadata is a key-value pair. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The value of the metadata item, paired with the associated key in the instance template's metadata. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kind" type="string">
          Output only. [Output Only] Type of the resource. Always compute#metadata for metadata. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="min_cpu_platform" type="string">
      Minimum cpu/platform to be used by instances. The instance may be scheduled on the specified or newer cpu/platform. Applicable values are the friendly names of CPU platforms, such asminCpuPlatform: "Intel Haswell" orminCpuPlatform: "Intel Sandy Bridge". For more information, read Specifying a Minimum CPU Platform. Optional.
    </ResponseField>
    <ResponseField name="network_interfaces" type="list(object)">
      An array of network access configurations for this interface. Optional.
      <Expandable title="properties">
        <ResponseField name="access_configs" type="list(object)">
          List of access configurations for this network interface, used to assign external IP addresses. Each access config can specify a NAT IP and network tier. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ipv6" type="string">
              The external IPv6 address to assign to the instance's network interface access config. If not specified, an IPv6 address is automatically allocated when IPv6 is enabled on the network. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_ipv6_prefix_length" type="number">
              The length, in bits, of the external IPv6 prefix associated with this access configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The kind of the access config, which is always 'compute#accessConfig'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="nat_ip" type="string">
              The external IP address to assign to the instance's network interface. If unset, an ephemeral external IP is assigned automatically. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The network tier to assign to this access config. Valid values are PREMIUM, STANDARD, FIXED_STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD. If unspecified, defaults to PREMIUM. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="public_ptr_domain_name" type="string">
              Sets a custom PTR (reverse DNS) record for the external IP address of this access config. When specified, the instance's public IP will resolve to this domain name in reverse DNS lookups. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="security_policy" type="string">
              The Google Cloud Armor security policy to apply to this access config. Specify the security policy name or URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="set_public_ptr" type="bool">
              Specifies whether a public DNS PTR record should be created for the external IP address assigned to this access config. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of access configuration. Valid values are ONE_TO_ONE_NAT for external NAT (IPv4) and DIRECT_IPV6 for direct IPv6 access. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alias_ip_ranges" type="list(object)">
          A list of alias IP ranges for this network interface. Each alias IP range specifies an IP range (in CIDR format) to assign to this interface and optionally a subnetwork secondary range name. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="ip_cidr_range" type="string">
              The IP CIDR range for the alias IP. This range must be within the primary or secondary IP ranges of the subnetwork. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork_range_name" type="string">
              The name of a subnetwork secondary IP range from which to allocate an IP address range. If not specified, the primary IP range of the subnetwork is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alias_ipv6_ranges" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="ip_cidr_range" type="string">
              The IPv6 CIDR range (in CIDR notation, e.g., /64) for the alias IPv6 range associated with the network interface. The range must be within the IPv6 address range of the subnet. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork_range_name" type="string">
              The name of the subnetwork's secondary IP range to use for the alias IPv6 range. This must correspond to a secondary range of the subnetwork that the network interface is attached to. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enable_vpc_scoped_dns" type="bool">
          Enables VPC-scoped DNS on this network interface. When set to true, the instance uses the VPC's DNS settings to resolve internal hostnames. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fingerprint" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="igmp_query" type="string">
          Controls whether IGMP query is enabled on this network interface and which version is used. Valid values are IGMP_QUERY_DISABLED (no IGMP query) and IGMP_QUERY_V2 (enable IGMPv2 query). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="internal_ipv6_prefix_length" type="number">
          The prefix length in bits for the internal IPv6 range assigned to the network interface. When set, this determines the size of the IPv6 subnet; the only supported value is /96. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv6_access_configs" type="list(object)">
          A list of IPv6 access configurations for the network interface. Each configuration specifies the external IPv6 address and network tier. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ipv6" type="string">
              The external IPv6 address assigned to the instance via this IPv6 access configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_ipv6_prefix_length" type="number">
              The prefix length of the external IPv6 address. Valid values are 32, 64, 96, and 128. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The type of the resource. For IPv6 access configurations, this is always 'compute#ipv6AccessConfig'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the IPv6 access configuration. It is used to identify the access config within the network interface. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nat_ip" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The network tier for this IPv6 access config. Valid values are FIXED_STANDARD, PREMIUM, STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="public_ptr_domain_name" type="string">
              The domain name to be used for reverse DNS lookup (PTR record) for the IPv6 address. If not specified, no reverse DNS record is created. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="security_policy" type="string">
              The URL or reference of a Compute Engine security policy to apply to this IPv6 access configuration. It allows you to attach a security policy to the external IPv6 address. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="set_public_ptr" type="bool">
              When true, sets a public DNS PTR record for the external IPv6 address associated with this access config. When false (default), no public PTR record is created. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of IPv6 access configuration. Valid values are `DIRECT_IPV6` for direct IPv6 access and `ONE_TO_ONE_NAT` for one-to-one NAT. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ipv6_access_type" type="string">
          The access type for IPv6 addresses on this network interface. Valid values are EXTERNAL (default) and INTERNAL. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv6_address" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="kind" type="string">
          The resource type, always compute#networkInterface for network interface resources. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="network" type="string">
          The name or self_link of the network to attach this interface to. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_attachment" type="string">
          The URL of the network attachment resource that this network interface should be attached to. When set, the interface connects to a VPC via the specified network attachment. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_ip" type="string">
          The private IPv4 address to assign to the interface. If not specified, an ephemeral address is automatically allocated from the subnet. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="nic_type" type="string">
          The type of network interface controller (NIC) to attach. Allowed values: GVNIC, IDPF, IRDMA, MRDMA, UNSPECIFIED_NIC_TYPE, VIRTIO_NET. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="parent_nic_name" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="queue_count" type="number">
          The number of queues for this network interface. If not specified, the number of queues is automatically selected based on the machine type. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_class_id" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="stack_type" type="string">
          The stack type for the network interface. Valid values are IPV4_IPV6 (dual-stack), IPV4_ONLY, and IPV6_ONLY. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnetwork" type="string">
          The name or self-link of the subnetwork to attach this network interface to. Must be in the same region as the instance template. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vlan" type="number">
          The VLAN ID to associate with this network interface when using a network attachment. Required if network_attachment is specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_performance_config" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="total_egress_bandwidth_tier" type="string">
          Specifies the tier for total egress bandwidth for the instance. Allowed values are DEFAULT and TIER_1. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_ipv6_google_access" type="string">
      The private IPv6 google access type for VMs. If not specified, use INHERIT_FROM_SUBNETWORK as default. Note that for MachineImage, this is not supported yet. Optional.
    </ResponseField>
    <ResponseField name="reservation_affinity" type="object">
      Specifies the reservations that this instance can consume from. Optional.
      <Expandable title="properties">
        <ResponseField name="consume_reservation_type" type="string">
          Specifies the type of reservation from which this instance can consume resources: ANY_RESERVATION (default),SPECIFIC_RESERVATION, or NO_RESERVATION. See Consuming reserved instances for examples. Optional.
        </ResponseField>
        <ResponseField name="key" type="string">
          Corresponds to the label key of a reservation resource. To target aSPECIFIC_RESERVATION by name, specifygoogleapis.com/reservation-name as the key and specify the name of your reservation as its value. Optional.
        </ResponseField>
        <ResponseField name="values" type="list(string)">
          Corresponds to the label values of a reservation resource. This can be either a name to a reservation in the same project or "projects/different-project/reservations/some-reservation-name" to target a shared reservation in the same zone but in a different project. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the instance. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
    <ResponseField name="resource_policies" type="list(string)">
      Resource policies (names, not URLs) applied to instances created from these properties. Note that for MachineImage, this is not supported yet. Optional.
    </ResponseField>
    <ResponseField name="scheduling" type="object">
      Sets the scheduling options for an Instance. Optional.
      <Expandable title="properties">
        <ResponseField name="automatic_restart" type="bool">
          Specifies whether the instance should be automatically restarted if it is terminated by Compute Engine (not terminated by a user). You can only set the automatic restart option for standard instances.Preemptible instances cannot be automatically restarted. By default, this is set to true so an instance is automatically restarted if it is terminated by Compute Engine. Optional.
        </ResponseField>
        <ResponseField name="availability_domain" type="number">
          Specifies the availability domain to place the instance in. The value must be a number between 1 and the number of availability domains specified in the spread placement policy attached to the instance. Optional.
        </ResponseField>
        <ResponseField name="graceful_shutdown" type="object">
          The configuration for gracefully shutting down the instance. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Opts-in for graceful shutdown. Optional.
            </ResponseField>
            <ResponseField name="max_duration" type="object">
              A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="host_error_timeout_seconds" type="number">
          Specify the time in seconds for host error detection, the value must be within the range of [90, 330] with the increment of 30, if unset, the default behavior of host error recovery will be used. Optional.
        </ResponseField>
        <ResponseField name="instance_termination_action" type="string">
          Specifies the termination action for the instance. Optional.
        </ResponseField>
        <ResponseField name="local_ssd_recovery_timeout" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="location_hint" type="string">
          An opaque location hint used to place the instance close to other resources. This field is for use by internal tools that use the public API. Optional.
        </ResponseField>
        <ResponseField name="max_run_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="min_node_cpus" type="number">
          The minimum number of virtual CPUs this instance will consume when running on a sole-tenant node. Optional.
        </ResponseField>
        <ResponseField name="node_affinities" type="list(object)">
          A set of node affinity and anti-affinity configurations. Refer toConfiguring node affinity for more information. Overrides reservationAffinity. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key for the node affinity label, used to match instances to node groups. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              Specifies the operator for the node affinity. Possible values are IN (match the given values), NOT_IN (do not match the given values), and OPERATOR_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              Specifies the values of the node affinity key. These are the actual label values used to match nodes. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="on_host_maintenance" type="string">
          Defines the maintenance behavior for this instance. For standard instances, the default behavior is MIGRATE. Forpreemptible instances, the default and only possible behavior is TERMINATE. For more information, see Set VM host maintenance policy. Optional.
        </ResponseField>
        <ResponseField name="on_instance_stop_action" type="object">
          Defines the behaviour for instances with the instance_termination_actionSTOP. Optional.
          <Expandable title="properties">
            <ResponseField name="discard_local_ssd" type="bool">
              If true, the contents of any attached Local SSD disks will be discarded else, the Local SSD data will be preserved when the instance is stopped at the end of the run duration/termination time. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="preemptible" type="bool">
          Defines whether the instance is preemptible. This can only be set during instance creation or while the instance isstopped and therefore, in a `TERMINATED` state. SeeInstance Life Cycle for more information on the possible instance states. Optional.
        </ResponseField>
        <ResponseField name="preemption_notice_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="provisioning_model" type="string">
          Specifies the provisioning model of the instance. Optional.
        </ResponseField>
        <ResponseField name="skip_guest_os_shutdown" type="bool">
          Default is false and there will be 120 seconds between GCE ACPI G2 Soft Off and ACPI G3 Mechanical Off for Standard VMs and 30 seconds for Spot VMs. Optional.
        </ResponseField>
        <ResponseField name="termination_time" type="string">
          Specifies the timestamp, when the instance will be terminated, inRFC3339 text format. If specified, the instance termination action will be performed at the termination time. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_accounts" type="list(object)">
      A list of service accounts with specified scopes. Access tokens for these service accounts are available to the instances that are created from these properties. Use metadata queries to obtain the access tokens for these instances. Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          The email address of the service account to assign to the instance. If not specified, the default Compute Engine service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scopes" type="list(string)">
          The list of OAuth scopes to be granted to the service account. These scopes determine the API access permissions for the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="shielded_instance_config" type="object">
      A set of Shielded Instance options. Optional.
      <Expandable title="properties">
        <ResponseField name="enable_integrity_monitoring" type="bool">
          Defines whether the instance has integrity monitoring enabled.Enabled by default. Optional.
        </ResponseField>
        <ResponseField name="enable_secure_boot" type="bool">
          Defines whether the instance has Secure Boot enabled.Disabled by default. Optional.
        </ResponseField>
        <ResponseField name="enable_vtpm" type="bool">
          Defines whether the instance has the vTPM enabled.Enabled by default. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="object">
      A set of instance tags. Optional.
      <Expandable title="properties">
        <ResponseField name="fingerprint" type="string">
          Specifies a fingerprint for this request, which is essentially a hash of the tags' contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update tags. You must always provide an up-to-date fingerprint hash in order to update or change tags. To see the latest fingerprint, make get() request to the instance. Optional.
        </ResponseField>
        <ResponseField name="items" type="list(string)">
          An array of tags. Each tag must be 1-63 characters long, and comply with RFC1035. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="workload_identity_config" type="object">
      Configures the workload identity settings for the instance template, enabling workload identity on instances created from it. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="identity" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="identity_certificate_enabled" type="bool">
          Enables the identity certificate for the workload identity configuration. When set to true, the instance template will use an identity certificate, which is typically used with Google Kubernetes Engine to provide a stable identity for workloads. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the instance template resides. Only applicable for regional resources. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] The URL for this instance template. The server defines this URL. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instance" type="string">
  The source instance used to create the template. You can provide this as a partial or full URL to the resource. For example, the following are valid values: - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance - projects/project/zones/zone/instances/instance Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instance_params" type="object">
  A specification of the parameters to use when creating the instance template from a source instance. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="disk_configs" type="list(object)">
      Attached disks configuration. If not provided, defaults are applied: For boot disk and any other R/W disks, the source images for each disk will be used. For read-only disks, they will be attached in read-only mode. Local SSD disks will be created as blank volumes. Optional.
      <Expandable title="properties">
        <ResponseField name="auto_delete" type="bool">
          Whether the disk should be automatically deleted when the instance is deleted. Set to true to delete the disk along with the instance, or false to retain it. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="custom_image" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="device_name" type="string">
          The device name to assign to the disk, used to identify the disk inside the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instantiate_from" type="string">
          Specifies how the disk is instantiated from the source instance in a region instance template. Valid values are: ATTACH_READ_ONLY (attach the disk as read-only), BLANK (create an empty disk), CUSTOM_IMAGE (use a custom image), DEFAULT (use the default behavior), DO_NOT_INCLUDE (omit the disk from the template), SOURCE_IMAGE (use the source instance's boot image), and SOURCE_IMAGE_FAMILY (use the source image family). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
