---
title: "google_compute_public_advertised_prefix"
description: "Real, generated bindings for `google_compute_public_advertised_prefix`."
---

`google_compute_public_advertised_prefix` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    cfg := compute.PublicAdvertisedPrefixConfig{
        IpCidrRange: "example",
        Name: "example-name",
        Description: "example",
        DnsVerificationIp: "example",
    }
    ubx.Resource(compute.PublicAdvertisedPrefix, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PublicAdvertisedPrefix } from
      "jsr:@ubx/sdk-google/google/compute/public_advertised_prefix";

    resource(PublicAdvertisedPrefix, "example", {
      ipCidrRange: "example",
      name: "example-name",
      description: "example",
      dnsVerificationIp: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        PublicAdvertisedPrefix,
        PublicAdvertisedPrefixConfig,
    )

    cfg = PublicAdvertisedPrefixConfig(
        ip_cidr_range="example",
        name="example-name",
        description="example",
        dns_verification_ip="example",
    )
    ubx.resource(PublicAdvertisedPrefix, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_public_advertised_prefix with
ip_cidr_range, name, description, dns_verification_ip.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="dns_verification_ip" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_cidr_range" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ipv6_access_type" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="pdp_scope" type="string">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ipv6_access_type" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="shared_secret" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
