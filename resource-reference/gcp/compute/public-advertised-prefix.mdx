---
title: "google_compute_public_advertised_prefix"
description: "Real, generated bindings for `google_compute_public_advertised_prefix`."
---

`google_compute_public_advertised_prefix` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own public advertised prefix"})

    		ubx.Resource(compute.PublicAdvertisedPrefix, "ci-runner", compute.PublicAdvertisedPrefixConfig{
    			IpCidrRange:       "example",
    			Name:              "ci-runner",
    			Description:       "Managed by ubx.",
    			DnsVerificationIp: "example",
    			PdpScope:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PublicAdvertisedPrefix } from "jsr:@ubx/sdk-google/google/compute/public_advertised_prefix";

    export default stack("payments", () => {
      intent({ summary: "payments own public advertised prefix" });

      resource(PublicAdvertisedPrefix, "ci-runner", {
        ipCidrRange: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        dnsVerificationIp: "example",
        pdpScope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import PublicAdvertisedPrefix, PublicAdvertisedPrefixConfig

    def describe():
        ubx.intent("payments own public advertised prefix")

        ubx.resource(PublicAdvertisedPrefix, "ci-runner", PublicAdvertisedPrefixConfig(
            ip_cidr_range="example",
            name="ci-runner",
            description="Managed by ubx.",
            dns_verification_ip="example",
            pdp_scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_public_advertised_prefix called
    "ci-runner", with ip cidr range example, description Managed by
    ubx., dns verification ip example, pdp scope example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional.
</ResponseField>

<ResponseField name="dns_verification_ip" type="string">
  The IPv4 address to be used for reverse DNS verification. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_cidr_range" type="string" required>
  The address range, in CIDR format, represented by this public advertised prefix. Required.
</ResponseField>

<ResponseField name="ipv6_access_type" type="string">
  The internet access type for IPv6 Public Advertised Prefixes. It can be set to one of following: * EXTERNAL: Default access type. The prefix will be announced to the internet. All children PDPs will have access type as EXTERNAL. * INTERNAL: The prefix won’t be announced to the internet. Prefix will be used privately within Google Cloud. All children PDPs will have access type as INTERNAL. Possible values: ["EXTERNAL", "INTERNAL"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Required.
</ResponseField>

<ResponseField name="pdp_scope" type="string">
  Specifies how child public delegated prefix will be scoped. pdpScope must be one of: GLOBAL, REGIONAL * REGIONAL: The public delegated prefix is regional only. The provisioning will take a few minutes. * GLOBAL: The public delegated prefix is global only. The provisioning will take ~4 weeks. Possible values: ["GLOBAL", "REGIONAL"] Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ipv6_access_type" type="string">
  The internet access type for IPv6 Public Advertised Prefixes. It can be set to one of following: * EXTERNAL: Default access type. The prefix will be announced to the internet. All children PDPs will have access type as EXTERNAL. * INTERNAL: The prefix won’t be announced to the internet. Prefix will be used privately within Google Cloud. All children PDPs will have access type as INTERNAL. Possible values: ["EXTERNAL", "INTERNAL"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="shared_secret" type="string">
  Output Only. The shared secret to be used for reverse DNS verification. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
