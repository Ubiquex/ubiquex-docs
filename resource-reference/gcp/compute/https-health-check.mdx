---
title: "google_compute_https_health_check"
description: "Real, generated bindings for `google_compute_https_health_check`."
---

`google_compute_https_health_check` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own https health check"})

    		ubx.Resource(compute.HttpsHealthCheck, "ci-runner", compute.HttpsHealthCheckConfig{
    			Name:             "ci-runner",
    			CheckIntervalSec: 1,
    			Description:      "Managed by ubx.",
    			HealthyThreshold: 1,
    			Host:             "example",
    			Port:             1,
    			RequestPath:      "example",
    			TimeoutSec:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HttpsHealthCheck } from "jsr:@ubx/sdk-google/google/compute/https_health_check";

    export default stack("payments", () => {
      intent({ summary: "payments own https health check" });

      resource(HttpsHealthCheck, "ci-runner", {
        name: "ci-runner",
        checkIntervalSec: 1,
        description: "Managed by ubx.",
        healthyThreshold: 1,
        host: "example",
        port: 1,
        requestPath: "example",
        timeoutSec: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import HttpsHealthCheck, HttpsHealthCheckConfig

    def describe():
        ubx.intent("payments own https health check")

        ubx.resource(HttpsHealthCheck, "ci-runner", HttpsHealthCheckConfig(
            name="ci-runner",
            check_interval_sec=1,
            description="Managed by ubx.",
            healthy_threshold=1,
            host="example",
            port=1,
            request_path="example",
            timeout_sec=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_https_health_check called "ci-runner", with
    check interval sec 1, description Managed by ubx., healthy
    threshold 1, host example, port 1, request path example, timeout
    sec 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="check_interval_sec" type="number">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="healthy_threshold" type="number">
  Optional.
</ResponseField>

<ResponseField name="host" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="port" type="number">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="request_path" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeout_sec" type="number">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="unhealthy_threshold" type="number">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="creation_timestamp" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
