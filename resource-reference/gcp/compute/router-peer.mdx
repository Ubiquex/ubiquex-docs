---
title: "google_compute_router_peer"
description: "Real, generated bindings for `google_compute_router_peer`."
---

`google_compute_router_peer` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own router peer"})

    		ubx.Resource(compute.RouterPeer, "ci-runner", compute.RouterPeerConfig{
    			Interface:                  "example",
    			Name:                       "ci-runner",
    			PeerAsn:                    1,
    			Router:                     "example",
    			AdvertiseMode:              "example",
    			AdvertisedGroups:           []string{"example"},
    			AdvertisedRoutePriority:    1,
    			CustomLearnedRoutePriority: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RouterPeer } from "jsr:@ubx/sdk-google/google/compute/router_peer";

    export default stack("payments", () => {
      intent({ summary: "payments own router peer" });

      resource(RouterPeer, "ci-runner", {
        interface: "example",
        name: "ci-runner",
        peerAsn: 1,
        router: "example",
        advertiseMode: "example",
        advertisedGroups: ["example"],
        advertisedRoutePriority: 1,
        customLearnedRoutePriority: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RouterPeer, RouterPeerConfig

    def describe():
        ubx.intent("payments own router peer")

        ubx.resource(RouterPeer, "ci-runner", RouterPeerConfig(
            interface="example",
            name="ci-runner",
            peer_asn=1,
            router="example",
            advertise_mode="example",
            advertised_groups=["example"],
            advertised_route_priority=1,
            custom_learned_route_priority=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_router_peer called "ci-runner", with
    interface example, peer asn 1, router example, advertise mode
    example, advertised groups a set value, advertised route priority
    1, custom learned route priority 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="advertise_mode" type="string">
  User-specified flag to indicate which mode to use for advertisement. Valid values of this enum field are: 'DEFAULT', 'CUSTOM' Default value: "DEFAULT" Possible values: ["DEFAULT", "CUSTOM"] Optional.
</ResponseField>

<ResponseField name="advertised_groups" type="list(string)">
  User-specified list of prefix groups to advertise in custom mode, which currently supports the following option: * 'ALL_SUBNETS': Advertises all of the router's own VPC subnets. This excludes any routes learned for subnets that use VPC Network Peering. Note that this field can only be populated if advertiseMode is 'CUSTOM' and overrides the list defined for the router (in the "bgp" message). These groups are advertised in addition to any specified prefixes. Leave this field blank to advertise no custom groups. Optional.
</ResponseField>

<ResponseField name="advertised_ip_ranges" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="range" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="advertised_route_priority" type="number">
  The priority of routes advertised to this BGP peer. Where there is more than one matching route of maximum length, the routes with the lowest priority value win. Optional.
</ResponseField>

<ResponseField name="bfd" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="min_receive_interval" type="number">
    </ResponseField>
    <ResponseField name="min_transmit_interval" type="number">
    </ResponseField>
    <ResponseField name="multiplier" type="number">
    </ResponseField>
    <ResponseField name="session_initialization_mode" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="custom_learned_ip_ranges" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="range" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="custom_learned_route_priority" type="number">
  The user-defined custom learned route priority for a BGP session. This value is applied to all custom learned route ranges for the session. You can choose a value from 0 to 65335. If you don't provide a value, Google Cloud assigns a priority of 100 to the ranges. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable" type="bool">
  The status of the BGP peer connection. If set to false, any active session with the peer is terminated and all associated routing information is removed. If set to true, the peer connection can be established with routing information. The default is true. Optional.
</ResponseField>

<ResponseField name="enable_ipv4" type="bool">
  Enable IPv4 traffic over BGP Peer. It is enabled by default if the peerIpAddress is version 4. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_ipv6" type="bool">
  Enable IPv6 traffic over BGP Peer. If not specified, it is disabled by default. Optional.
</ResponseField>

<ResponseField name="export_policies" type="list(string)">
  routers.list of export policies applied to this peer, in the order they must be evaluated. The name must correspond to an existing policy that has ROUTE_POLICY_TYPE_EXPORT type. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="import_policies" type="list(string)">
  routers.list of import policies applied to this peer, in the order they must be evaluated. The name must correspond to an existing policy that has ROUTE_POLICY_TYPE_IMPORT type. Optional.
</ResponseField>

<ResponseField name="interface" type="string" required>
  Name of the interface the BGP peer is associated with. Required.
</ResponseField>

<ResponseField name="ip_address" type="string">
  IP address of the interface inside Google Cloud Platform. Only IPv4 is supported. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ipv4_nexthop_address" type="string">
  IPv4 address of the interface inside Google Cloud Platform. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ipv6_nexthop_address" type="string">
  IPv6 address of the interface inside Google Cloud Platform. The address must be in the range 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64. If you do not specify the next hop addresses, Google Cloud automatically assigns unused addresses from the 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64 range for you. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="md5_authentication_key" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="key" type="string" required>
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of this BGP peer. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Required.
</ResponseField>

<ResponseField name="peer_asn" type="number" required>
  Peer BGP Autonomous System Number (ASN). Each BGP interface may use a different value. Required.
</ResponseField>

<ResponseField name="peer_ip_address" type="string">
  IP address of the BGP interface outside Google Cloud Platform. Only IPv4 is supported. Required if 'ip_address' is set. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="peer_ipv4_nexthop_address" type="string">
  IPv4 address of the BGP interface outside Google Cloud Platform. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="peer_ipv6_nexthop_address" type="string">
  IPv6 address of the BGP interface outside Google Cloud Platform. The address must be in the range 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64. If you do not specify the next hop addresses, Google Cloud automatically assigns unused addresses from the 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64 range for you. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Region where the router and BgpPeer reside. If it is not provided, the provider region is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="router" type="string" required>
  The name of the Cloud Router in which this BgpPeer will be configured. Required.
</ResponseField>

<ResponseField name="router_appliance_instance" type="string">
  The URI of the VM instance that is used as third-party router appliances such as Next Gen Firewalls, Virtual Routers, or Router Appliances. The VM instance must be located in zones contained in the same region as this Cloud Router. The VM instance is the peer side of the BGP session. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zero_advertised_route_priority" type="bool">
  Force the advertised_route_priority to be 0. Optional.
</ResponseField>

<ResponseField name="zero_custom_learned_route_priority" type="bool">
  Force the custom_learned_route_priority to be 0. Optional.
</ResponseField>

## Output properties

<ResponseField name="advertised_ip_ranges" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="range" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bfd" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="min_receive_interval" type="number">
    </ResponseField>
    <ResponseField name="min_transmit_interval" type="number">
    </ResponseField>
    <ResponseField name="multiplier" type="number">
    </ResponseField>
    <ResponseField name="session_initialization_mode" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="custom_learned_ip_ranges" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="range" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_ipv4" type="bool">
  Enable IPv4 traffic over BGP Peer. It is enabled by default if the peerIpAddress is version 4. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_address" type="string">
  IP address of the interface inside Google Cloud Platform. Only IPv4 is supported. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ipv4_nexthop_address" type="string">
  IPv4 address of the interface inside Google Cloud Platform. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ipv6_nexthop_address" type="string">
  IPv6 address of the interface inside Google Cloud Platform. The address must be in the range 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64. If you do not specify the next hop addresses, Google Cloud automatically assigns unused addresses from the 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64 range for you. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="is_advertised_route_priority_set" type="bool">
  An internal boolean field for provider use for zero_advertised_route_priority. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="is_custom_learned_priority_set" type="bool">
  An internal boolean field for provider use. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="management_type" type="string">
  The resource that configures and manages this BGP peer. * 'MANAGED_BY_USER' is the default value and can be managed by you or other users * 'MANAGED_BY_ATTACHMENT' is a BGP peer that is configured and managed by Cloud Interconnect, specifically by an InterconnectAttachment of type PARTNER. Google automatically creates, updates, and deletes this type of BGP peer when the PARTNER InterconnectAttachment is created, updated, or deleted. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="md5_authentication_key" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="key" type="string" required>
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="peer_ip_address" type="string">
  IP address of the BGP interface outside Google Cloud Platform. Only IPv4 is supported. Required if 'ip_address' is set. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="peer_ipv4_nexthop_address" type="string">
  IPv4 address of the BGP interface outside Google Cloud Platform. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="peer_ipv6_nexthop_address" type="string">
  IPv6 address of the BGP interface outside Google Cloud Platform. The address must be in the range 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64. If you do not specify the next hop addresses, Google Cloud automatically assigns unused addresses from the 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64 range for you. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Region where the router and BgpPeer reside. If it is not provided, the provider region is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
