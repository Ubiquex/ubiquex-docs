---
title: "google_compute_vpn_tunnel"
description: "`google_compute_vpn_tunnel` is one real, encrypted IPsec tunnel between a Google VPN gateway and an external peer -- the actual..."
---

`google_compute_vpn_tunnel` is one real, encrypted IPsec tunnel between a Google VPN gateway and an external peer -- the actual, active connection real traffic flows through once a VPN gateway is set up.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vpn tunnel"})

    		ubx.Resource(compute.VpnTunnel, "example", compute.VpnTunnelConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpnTunnel } from "jsr:@ubx/sdk-google/google/compute/vpn_tunnel";

    export default stack("example", () => {
      intent({ summary: "example own vpn tunnel" });

      resource(VpnTunnel, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import VpnTunnel, VpnTunnelConfig

    def describe():
        ubx.intent("example own vpn tunnel")

        ubx.resource(VpnTunnel, "example", VpnTunnelConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_vpn_tunnel called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="cipher_suite" type="object">
  The cryptographic suite (e.g., encryption and authentication algorithms) used for the VPN tunnel. This is a computed read-only field provided by the system. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="phase1" type="object">
      The phase1 object defines the IKE Phase 1 (ISAKMP SA) settings for the VPN tunnel's cipher suite, including the encryption, integrity, and DH group algorithms used during the initial key exchange. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dh" type="list(string)">
          List of Diffie-Hellman group identifiers used during IKE phase 1 negotiation for the VPN tunnel cipher suite. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="encryption" type="list(string)">
          List of encryption algorithms used for IKE phase 1 negotiation. These algorithms define the encryption methods permitted during the initial IPsec/IKE key exchange. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="integrity" type="list(string)">
          The list of integrity algorithms permitted for IKE phase 1 negotiation. Supported values include SHA1, SHA256, SHA384, and SHA512; if left empty, the default set of algorithms is used. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="prf" type="list(string)">
          The list of allowed Pseudo-Random Function (PRF) algorithms for the IKE phase 1 proposal, used for key derivation during the IKE handshake. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="phase2" type="object">
      Settings for IPsec Phase 2 (IKE Quick Mode), including encryption and integrity algorithms, within the cipher suite for the VPN tunnel. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="encryption" type="list(string)">
          A list of acceptable encryption algorithms for Phase 2 of the IKE negotiation. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="integrity" type="list(string)">
          A list of integrity algorithms to be used for the IPsec phase 2 proposal in the VPN tunnel. The tunnel negotiates using one of the listed algorithms to ensure data integrity. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="pfs" type="list(string)">
          Specifies the Diffie-Hellman groups allowed for Perfect Forward Secrecy (PFS) during Phase 2 of the IKE negotiation. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="detailed_status" type="string">
  [Output Only] Detailed status message for the VPN tunnel. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ike_version" type="number">
  IKE protocol version to use when establishing the VPN tunnel with the peer VPN gateway. Acceptable IKE versions are 1 or 2. The default version is 2. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of resource. Always compute#vpnTunnel for VPN tunnels. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  A fingerprint for the labels being applied to this VpnTunnel, which is essentially a hash of the labels set used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update labels. You must always provide an up-to-date fingerprint hash in order to update or change labels, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a VpnTunnel. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035. Label values may be empty. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="local_traffic_selector" type="list(string)">
  Local traffic selector to use when establishing the VPN tunnel with the peer VPN gateway. The value should be a CIDR formatted string, for example: 192.168.0.0/16. The ranges must be disjoint. Only IPv4 is supported for Classic VPN tunnels. This field is output only for HA VPN tunnels. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="params" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
      Tag keys/values directly bound to this resource. Tag keys and values have the same definition as resource manager tags. The field is allowed for INSERT only. The keys/values to set on the resource should be specified in either ID &#123; : &#125; or Namespaced format &#123; : &#125;. For example the following are valid inputs: * &#123;"tagKeys/333" : "tagValues/444", "tagKeys/123" : "tagValues/456"&#125; * &#123;"123/environment" : "production", "345/abc" : "xyz"&#125; Note: * Invalid combinations of ID & namespaced format is not supported. For instance: &#123;"123/environment" : "tagValues/444"&#125; is invalid. * Inconsistent format is not supported. For instance: &#123;"tagKeys/333" : "tagValues/444", "123/env" : "prod"&#125; is invalid. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="peer_external_gateway" type="string">
  URL of the peer side external VPN gateway to which this VPN tunnel is connected. Provided by the client when the VPN tunnel is created. This field is exclusive with the field peerGcpGateway. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="peer_external_gateway_interface" type="number">
  The interface ID of the external VPN gateway to which this VPN tunnel is connected. Provided by the client when the VPN tunnel is created. Possible values are: `0`, `1`, `2`, `3`. The number of IDs in use depends on the external VPN gateway redundancy type. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="peer_gcp_gateway" type="string">
  URL of the peer side HA VPN gateway to which this VPN tunnel is connected. Provided by the client when the VPN tunnel is created. This field can be used when creating highly available VPN from VPC network to VPC network, the field is exclusive with the field peerExternalGateway. If provided, the VPN tunnel will automatically use the same vpnGatewayInterface ID in the peer Google Cloud VPN gateway. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="peer_ip" type="string">
  IP address of the peer VPN gateway. Only IPv4 is supported. This field can be set only for Classic VPN tunnels. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  [Output Only] URL of the region where the VPN tunnel resides. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="remote_traffic_selector" type="list(string)">
  Remote traffic selectors to use when establishing the VPN tunnel with the peer VPN gateway. The value should be a CIDR formatted string, for example: 192.168.0.0/16. The ranges should be disjoint. Only IPv4 is supported for Classic VPN tunnels. This field is output only for HA VPN tunnels. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="router" type="string">
  URL of the router resource to be used for dynamic routing. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="shared_secret" type="string">
  Shared secret used to set the secure session between the Cloud VPN gateway and the peer VPN gateway. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="shared_secret_hash" type="string">
  Hash of the shared secret. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status" type="string">
  [Output Only] The status of the VPN tunnel, which can be one of the following: - PROVISIONING: Resource is being allocated for the VPN tunnel. - WAITING_FOR_FULL_CONFIG: Waiting to receive all VPN-related configs from the user. Network, TargetVpnGateway, VpnTunnel, ForwardingRule, and Route resources are needed to setup the VPN tunnel. - FIRST_HANDSHAKE: Successful first handshake with the peer VPN. - ESTABLISHED: Secure session is successfully established with the peer VPN. - NETWORK_ERROR: Deprecated, replaced by NO_INCOMING_PACKETS - AUTHORIZATION_ERROR: Auth error (for example, bad shared secret). - NEGOTIATION_FAILURE: Handshake failed. - DEPROVISIONING: Resources are being deallocated for the VPN tunnel. - FAILED: Tunnel creation has failed and the tunnel is not ready to be used. - NO_INCOMING_PACKETS: No incoming packets from peer. - REJECTED: Tunnel configuration was rejected, can be result of being denied access. - ALLOCATING_RESOURCES: Cloud VPN is in the process of allocating all required resources. - STOPPED: Tunnel is stopped due to its Forwarding Rules being deleted for Classic VPN tunnels or the project is in frozen state. - PEER_IDENTITY_MISMATCH: Peer identity does not match peer IP, probably behind NAT. - TS_NARROWING_NOT_ALLOWED: Traffic selector narrowing not allowed for an HA-VPN tunnel. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_vpn_gateway" type="string">
  URL of the Target VPN gateway with which this VPN tunnel is associated. Provided by the client when the VPN tunnel is created. This field can be set only for Classic VPN tunnels. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="vpn_gateway" type="string">
  URL of the VPN gateway with which this VPN tunnel is associated. Provided by the client when the VPN tunnel is created. This must be used (instead of target_vpn_gateway) if a High Availability VPN gateway resource is created. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="vpn_gateway_interface" type="number">
  The interface ID of the VPN gateway with which this VPN tunnel is associated. Possible values are: `0`, `1`. Optional; if omitted, computed by GCP.
</ResponseField>
