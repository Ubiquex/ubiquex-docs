---
title: "google_compute_vpn_tunnel"
description: "Real, generated bindings for `google_compute_vpn_tunnel`."
---

`google_compute_vpn_tunnel` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpn tunnel"})

    		ubx.Resource(compute.VpnTunnel, "ci-runner", compute.VpnTunnelConfig{
    			Name:                         "ci-runner",
    			Description:                  "Managed by ubx.",
    			IkeVersion:                   1,
    			Labels:                       map[string]string{"managed-by": "ubx"},
    			PeerExternalGateway:          "example",
    			PeerExternalGatewayInterface: 1,
    			PeerGcpGateway:               "example",
    			Router:                       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpnTunnel } from "jsr:@ubx/sdk-google/google/compute/vpn_tunnel";

    export default stack("payments", () => {
      intent({ summary: "payments own vpn tunnel" });

      resource(VpnTunnel, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        ikeVersion: 1,
        labels: { "managed-by": "ubx" },
        peerExternalGateway: "example",
        peerExternalGatewayInterface: 1,
        peerGcpGateway: "example",
        router: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import VpnTunnel, VpnTunnelConfig

    def describe():
        ubx.intent("payments own vpn tunnel")

        ubx.resource(VpnTunnel, "ci-runner", VpnTunnelConfig(
            name="ci-runner",
            description="Managed by ubx.",
            ike_version=1,
            labels={"managed-by": "ubx"},
            peer_external_gateway="example",
            peer_external_gateway_interface=1,
            peer_gcp_gateway="example",
            router="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_vpn_tunnel called "ci-runner", with
    description Managed by ubx., ike version 1, labels a set value,
    peer external gateway example, peer external gateway interface 1,
    peer gcp gateway example, router example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cipher_suite" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="phase1" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="dh" type="list(string)">
        </ResponseField>
        <ResponseField name="encryption" type="list(string)">
        </ResponseField>
        <ResponseField name="integrity" type="list(string)">
        </ResponseField>
        <ResponseField name="prf" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="phase2" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="encryption" type="list(string)">
        </ResponseField>
        <ResponseField name="integrity" type="list(string)">
        </ResponseField>
        <ResponseField name="pfs" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ike_version" type="number">
  IKE protocol version to use when establishing the VPN tunnel with peer VPN gateway. Acceptable IKE versions are 1 or 2. Default version is 2. Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels to apply to this VpnTunnel. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="local_traffic_selector" type="set(string)">
  Local traffic selector to use when establishing the VPN tunnel with peer VPN gateway. The value should be a CIDR formatted string, for example '192.168.0.0/16'. The ranges should be disjoint. Only IPv4 is supported. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Required.
</ResponseField>

<ResponseField name="params" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="peer_external_gateway" type="string">
  URL of the peer side external VPN gateway to which this VPN tunnel is connected. Optional.
</ResponseField>

<ResponseField name="peer_external_gateway_interface" type="number">
  The interface ID of the external VPN gateway to which this VPN tunnel is connected. Optional.
</ResponseField>

<ResponseField name="peer_gcp_gateway" type="string">
  URL of the peer side HA GCP VPN gateway to which this VPN tunnel is connected. If provided, the VPN tunnel will automatically use the same vpn_gateway_interface ID in the peer GCP VPN gateway. This field must reference a 'google_compute_ha_vpn_gateway' resource. Optional.
</ResponseField>

<ResponseField name="peer_ip" type="string">
  IP address of the peer VPN gateway. Only IPv4 is supported. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The region where the tunnel is located. If unset, is set to the region of 'target_vpn_gateway'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="remote_traffic_selector" type="set(string)">
  Remote traffic selector to use when establishing the VPN tunnel with peer VPN gateway. The value should be a CIDR formatted string, for example '192.168.0.0/16'. The ranges should be disjoint. Only IPv4 is supported. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="router" type="string">
  URL of router resource to be used for dynamic routing. Optional.
</ResponseField>

<ResponseField name="shared_secret" type="string">
  Shared secret used to set the secure session between the Cloud VPN gateway and the peer VPN gateway. Optional.
</ResponseField>

<ResponseField name="shared_secret_wo" type="string">
  Shared secret used to set the secure session between the Cloud VPN gateway and the peer VPN gateway. Optional.
</ResponseField>

<ResponseField name="shared_secret_wo_version" type="string">
  Triggers update of 'shared_secret_wo' write-only. Increment this value when an update to 'shared_secret_wo' is needed. For more info see [updating write-only arguments](/docs/providers/google/guides/using_write_only_arguments.html#updating-write-only-arguments) Optional.
</ResponseField>

<ResponseField name="target_vpn_gateway" type="string">
  URL of the Target VPN gateway with which this VPN tunnel is associated. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpn_gateway" type="string">
  URL of the VPN gateway with which this VPN tunnel is associated. This must be used if a High Availability VPN gateway resource is created. This field must reference a 'google_compute_ha_vpn_gateway' resource. Optional.
</ResponseField>

<ResponseField name="vpn_gateway_interface" type="number">
  The interface ID of the VPN gateway with which this VPN tunnel is associated. Optional.
</ResponseField>

## Output properties

<ResponseField name="cipher_suite" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="phase1" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="dh" type="list(string)">
        </ResponseField>
        <ResponseField name="encryption" type="list(string)">
        </ResponseField>
        <ResponseField name="integrity" type="list(string)">
        </ResponseField>
        <ResponseField name="prf" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="phase2" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="encryption" type="list(string)">
        </ResponseField>
        <ResponseField name="integrity" type="list(string)">
        </ResponseField>
        <ResponseField name="pfs" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Creation timestamp in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="detailed_status" type="string">
  Detailed status message for the VPN tunnel. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  The fingerprint used for optimistic locking of this resource. Used internally during updates. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="local_traffic_selector" type="set(string)">
  Local traffic selector to use when establishing the VPN tunnel with peer VPN gateway. The value should be a CIDR formatted string, for example '192.168.0.0/16'. The ranges should be disjoint. Only IPv4 is supported. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="params" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="peer_ip" type="string">
  IP address of the peer VPN gateway. Only IPv4 is supported. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The region where the tunnel is located. If unset, is set to the region of 'target_vpn_gateway'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="remote_traffic_selector" type="set(string)">
  Remote traffic selector to use when establishing the VPN tunnel with peer VPN gateway. The value should be a CIDR formatted string, for example '192.168.0.0/16'. The ranges should be disjoint. Only IPv4 is supported. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="shared_secret_hash" type="string">
  Hash of the shared secret. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tunnel_id" type="string">
  The unique identifier for the resource. This identifier is defined by the server. Computed by GCP after `ship`.
</ResponseField>
