---
title: "google_compute_global_public_delegated_prefix"
description: "`google_compute_global_public_delegated_prefix` delegates a real, global block of your own public IP addresses to Google Cloud to advertise and manage..."
---

`google_compute_global_public_delegated_prefix` delegates a real, global block of your own public IP addresses to Google Cloud to advertise and manage on your behalf, rather than using addresses Google itself owns.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own global public delegated prefix"})

    		ubx.Resource(compute.GlobalPublicDelegatedPrefix, "ci-runner", compute.GlobalPublicDelegatedPrefixConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GlobalPublicDelegatedPrefix } from "./local-sdk/google/sdk/typescript/google/compute/global_public_delegated_prefix.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own global public delegated prefix" });

      resource(GlobalPublicDelegatedPrefix, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.compute import GlobalPublicDelegatedPrefix, GlobalPublicDelegatedPrefixConfig

    def describe():
        ubx.intent("payments own global public delegated prefix")

        ubx.resource(GlobalPublicDelegatedPrefix, "ci-runner", GlobalPublicDelegatedPrefixConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_global_public_delegated_prefix called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="allocatable_prefix_length" type="number">
  The allocatable prefix length supported by this public delegated prefix. This field is optional and cannot be set for prefixes in DELEGATION mode. It cannot be set for IPv4 prefixes either, and it always defaults to 32. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="byoip_api_version" type="string">
  Output only. [Output Only] The version of BYOIP API. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_enhanced_ipv4_allocation" type="bool">
  Output only. [Output Only] Whether this PDP supports enhanced IPv4 allocations. Applicable for IPv4 PDPs only. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. A hash of the contents stored in this object. This field is used in optimistic locking. This field will be ignored when inserting a new PublicDelegatedPrefix. An up-to-date fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with error 412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a PublicDelegatedPrefix. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource type. The server generates this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_cidr_range" type="string">
  The IP address range, in CIDR format, represented by this public delegated prefix. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ipv6_access_type" type="string">
  Output only. [Output Only] The internet access type for IPv6 Public Delegated Prefixes. Inherited from parent prefix. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="is_live_migration" type="bool">
  If true, the prefix will be live migrated. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="mode" type="string">
  The public delegated prefix mode for IPv6 only. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent_prefix" type="string">
  The URL of parent prefix. Either PublicAdvertisedPrefix or PublicDelegatedPrefix. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="public_delegated_sub_prefixs" type="list(object)">
  The list of sub public delegated prefixes that exist for this public delegated prefix. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allocatable_prefix_length" type="number">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="delegatee_project" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="enable_enhanced_ipv4_allocation" type="bool">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="ip_cidr_range" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="ipv6_access_type" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="is_address" type="bool">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="mode" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="region" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the public delegated prefix resides. This field applies only to the region resource. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status" type="string">
  [Output Only] The status of the public delegated prefix, which can be one of following values: - `INITIALIZING` The public delegated prefix is being initialized and addresses cannot be created yet. - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration prefix and is active. - `ANNOUNCED` The public delegated prefix is announced and ready to use. - `DELETING` The public delegated prefix is being deprovsioned. - `ACTIVE` The public delegated prefix is ready to use. Optional; if omitted, computed by GCP.
</ResponseField>
