---
title: "google_compute_node_template"
description: "`google_compute_node_template` defines the real hardware specification -- node type..."
---

`google_compute_node_template` defines the real hardware specification -- node type, CPU overcommit settings -- that a sole-tenant node group uses to actually provision the physical nodes inside it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own node template"})

    		ubx.Resource(compute.NodeTemplate, "example", compute.NodeTemplateConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NodeTemplate } from "@ubx/sdk-google/google/compute/node_template";

    export default stack("example", () => {
      intent({ summary: "example own node template" });

      resource(NodeTemplate, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import NodeTemplate, NodeTemplateConfig

    def describe():
        ubx.intent("example own node template")

        ubx.resource(NodeTemplate, "example", NodeTemplateConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_node_template called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="accelerators" type="list(object)">
  A list of accelerator configurations (e.g., GPUs) for the node template. Each object specifies the accelerator type and count. This field is output-only, so it is populated by the API and reflects the actual accelerators associated with the node template. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="accelerator_count" type="number">
      The number of accelerators of the specified type to attach to nodes created from this node template. This field is part of the accelerators block, which also specifies the accelerator type. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="accelerator_type" type="string">
      The type of accelerator (GPU) to attach to the node, such as 'nvidia-tesla-a100'. This should be a valid accelerator type identifier supported by Compute Engine. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cpu_overcommit_type" type="string">
  CPU overcommit. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disks" type="list(object)">
  The list of local disk configurations associated with the node template. This is an output-only field computed by the API, containing the disk type and size for each local disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="disk_count" type="number">
      The number of disks of the specified type to attach to the node template. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disk_size_gb" type="number">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="disk_type" type="string">
      The type of disk to attach to the node, such as 'local-ssd'. Defaults to 'local-ssd'. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] The type of the resource. Alwayscompute#nodeTemplate for node templates. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the resource, provided by the client when initially creating the resource. The resource name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="node_affinity_labels" type="map(string)">
  Labels to use for node affinity, which will be used in instance scheduling. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="node_type" type="string">
  The node type to use for nodes group that are created from this template. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="node_type_flexibility" type="object">
  Node type flexibility settings for the template, allowing the template to be used with a range of node types by specifying a CPU and memory range instead of a fixed node type. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cpus" type="string">
      The number of virtual CPUs to allocate for the node template. Provide a specific integer value as a string (e.g., "8") or use "any" to allow flexible selection based on available resources. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="local_ssd" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="memory" type="string">
      Specifies the amount of memory (in MB) that the flexible node type must have. This is used to define a custom node type for the node template. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] The name of the region where the node template resides, such as us-central1. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="server_binding" type="object">
  The server binding configuration of the node template, which controls how nodes are restarted during maintenance events. This field is output-only and reflects the binding type used by the template. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="type" type="string">
      Specifies the server binding type for the node template, controlling whether the node can be restarted on any server or only on a minimal set of servers. Valid values are RESTART_NODE_ON_ANY_SERVER, RESTART_NODE_ON_MINIMAL_SERVERS, or SERVER_BINDING_TYPE_UNSPECIFIED. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] The status of the node template. One of the following values:CREATING, READY, and DELETING. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status_message" type="string">
  Output only. [Output Only] An optional, human-readable explanation of the status. Optional; if omitted, computed by GCP.
</ResponseField>
