---
title: "google_compute_global_network_endpoint"
description: "Real, generated bindings for `google_compute_global_network_endpoint`."
---

`google_compute_global_network_endpoint` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own global network endpoint"})

    		ubx.Resource(compute.GlobalNetworkEndpoint, "ci-runner", compute.GlobalNetworkEndpointConfig{
    			GlobalNetworkEndpointGroup: "example",
    			Port:                       1,
    			Fqdn:                       "example",
    			IpAddress:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GlobalNetworkEndpoint } from "jsr:@ubx/sdk-google/google/compute/global_network_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own global network endpoint" });

      resource(GlobalNetworkEndpoint, "ci-runner", {
        globalNetworkEndpointGroup: "example",
        port: 1,
        fqdn: "example",
        ipAddress: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import GlobalNetworkEndpoint, GlobalNetworkEndpointConfig

    def describe():
        ubx.intent("payments own global network endpoint")

        ubx.resource(GlobalNetworkEndpoint, "ci-runner", GlobalNetworkEndpointConfig(
            global_network_endpoint_group="example",
            port=1,
            fqdn="example",
            ip_address="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_global_network_endpoint, with global
    network endpoint group example, port 1, fqdn example, ip address
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fqdn" type="string">
  Optional.
</ResponseField>

<ResponseField name="global_network_endpoint_group" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_address" type="string">
  Optional.
</ResponseField>

<ResponseField name="port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
