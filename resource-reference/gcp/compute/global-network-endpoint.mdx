---
title: "google_compute_global_network_endpoint"
description: "Real, generated bindings for `google_compute_global_network_endpoint`."
---

`google_compute_global_network_endpoint` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/compute"
    )

    cfg := compute.GlobalNetworkEndpointConfig{
        GlobalNetworkEndpointGroup: "example",
        Port: 1,
        Fqdn: "example",
        IpAddress: "example",
    }
    ubx.Resource(compute.GlobalNetworkEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GlobalNetworkEndpoint } from
      "jsr:@ubx/sdk-google/google/compute/global_network_endpoint";

    resource(GlobalNetworkEndpoint, "example", {
      globalNetworkEndpointGroup: "example",
      port: 1,
      fqdn: "example",
      ipAddress: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        GlobalNetworkEndpoint,
        GlobalNetworkEndpointConfig,
    )

    cfg = GlobalNetworkEndpointConfig(
        global_network_endpoint_group="example",
        port=1,
        fqdn="example",
        ip_address="example",
    )
    ubx.resource(GlobalNetworkEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_global_network_endpoint with
global_network_endpoint_group, port, fqdn, ip_address.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fqdn" type="string">
  Optional.
</ResponseField>

<ResponseField name="global_network_endpoint_group" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_address" type="string">
  Optional.
</ResponseField>

<ResponseField name="port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
