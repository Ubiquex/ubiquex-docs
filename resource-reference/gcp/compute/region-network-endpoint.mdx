---
title: "google_compute_region_network_endpoint"
description: "Real, generated bindings for `google_compute_region_network_endpoint`."
---

`google_compute_region_network_endpoint` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region network endpoint"})

    		ubx.Resource(compute.RegionNetworkEndpoint, "ci-runner", compute.RegionNetworkEndpointConfig{
    			Port:                       1,
    			RegionNetworkEndpointGroup: "example",
    			ClientDestinationPort:      1,
    			Fqdn:                       "example",
    			Instance:                   "example",
    			IpAddress:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionNetworkEndpoint } from "jsr:@ubx/sdk-google/google/compute/region_network_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own region network endpoint" });

      resource(RegionNetworkEndpoint, "ci-runner", {
        port: 1,
        regionNetworkEndpointGroup: "example",
        clientDestinationPort: 1,
        fqdn: "example",
        instance: "example",
        ipAddress: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RegionNetworkEndpoint, RegionNetworkEndpointConfig

    def describe():
        ubx.intent("payments own region network endpoint")

        ubx.resource(RegionNetworkEndpoint, "ci-runner", RegionNetworkEndpointConfig(
            port=1,
            region_network_endpoint_group="example",
            client_destination_port=1,
            fqdn="example",
            instance="example",
            ip_address="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_network_endpoint, with port 1,
    region network endpoint group example, client destination port 1,
    fqdn example, instance example, ip address example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="client_destination_port" type="number">
  Client destination port for the 'GCE_VM_IP_PORTMAP' NEG. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fqdn" type="string">
  Fully qualified domain name of network endpoint. This can only be specified when network_endpoint_type of the NEG is INTERNET_FQDN_PORT. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string">
  The name for a specific VM instance that the IP address belongs to. This is required for network endpoints of type GCE_VM_IP_PORTMAP. Optional.
</ResponseField>

<ResponseField name="ip_address" type="string">
  IPv4 address external endpoint. This can only be specified when network_endpoint_type of the NEG is INTERNET_IP_PORT. Optional.
</ResponseField>

<ResponseField name="port" type="number" required>
  Port number of network endpoint. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Region where the containing network endpoint group is located. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region_network_endpoint_group" type="string" required>
  The network endpoint group this endpoint is part of. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network_endpoint_id" type="number">
  The unique identifier number for the resource. This identifier is defined by the server. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Region where the containing network endpoint group is located. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
