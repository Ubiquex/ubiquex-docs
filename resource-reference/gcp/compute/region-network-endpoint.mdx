---
title: "google_compute_region_network_endpoint"
description: "Real, generated bindings for `google_compute_region_network_endpoint`."
---

`google_compute_region_network_endpoint` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/compute"
    )

    cfg := compute.RegionNetworkEndpointConfig{
        Port: 1,
        RegionNetworkEndpointGroup: "example",
        ClientDestinationPort: 1,
        Fqdn: "example",
    }
    ubx.Resource(compute.RegionNetworkEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RegionNetworkEndpoint } from
      "jsr:@ubx/sdk-google/google/compute/region_network_endpoint";

    resource(RegionNetworkEndpoint, "example", {
      port: 1,
      regionNetworkEndpointGroup: "example",
      clientDestinationPort: 1,
      fqdn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        RegionNetworkEndpoint,
        RegionNetworkEndpointConfig,
    )

    cfg = RegionNetworkEndpointConfig(
        port=1,
        region_network_endpoint_group="example",
        client_destination_port=1,
        fqdn="example",
    )
    ubx.resource(RegionNetworkEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_region_network_endpoint with port,
region_network_endpoint_group, client_destination_port, fqdn.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="client_destination_port" type="number">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fqdn" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string">
  Optional.
</ResponseField>

<ResponseField name="ip_address" type="string">
  Optional.
</ResponseField>

<ResponseField name="port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region_network_endpoint_group" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network_endpoint_id" type="number">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
