---
title: "google_compute_alpha_snapshot_group"
description: "`google_compute_alpha_snapshot_group` is a set of disk snapshots captured together as one consistent point-in-time group..."
---

<Warning>
  This is Google Cloud Compute's own **alpha** release channel -- earlier and less stable than beta. Google may change or remove alpha endpoints at any time, with no deprecation notice -- confirm this endpoint is still live before depending on it in anything long-lived. No stable, generally-available equivalent is published yet.
</Warning>

`google_compute_alpha_snapshot_group` is a set of disk snapshots captured together as one consistent point-in-time group, typically sourced from an instant snapshot group, so a multi-disk workload can be restored from a single, mutually-consistent moment rather than disks that were each snapshotted independently. No stable, generally-available equivalent is published yet -- this is preview-only right now.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own alpha snapshot group"})

    		ubx.Resource(compute.AlphaSnapshotGroup, "example", compute.AlphaSnapshotGroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AlphaSnapshotGroup } from "@ubx/sdk-google/google/compute/alpha_snapshot_group";

    export default stack("example", () => {
      intent({ summary: "example own alpha snapshot group" });

      resource(AlphaSnapshotGroup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute.alpha_snapshot_group import AlphaSnapshotGroup, AlphaSnapshotGroupConfig

    def describe():
        ubx.intent("example own alpha snapshot group")

        ubx.resource(AlphaSnapshotGroup, "example", AlphaSnapshotGroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_alpha_snapshot_group called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#snapshotGroup for SnapshotGroup resources. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource's resource id. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="source_info" type="object">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="consistency_group" type="string">
      Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="consistency_group_id" type="string">
      Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_instant_snapshot_group" type="string">
  Input field for the source instant snapshot group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="source_instant_snapshot_group_info" type="object">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="instant_snapshot_group" type="string">
      Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="instant_snapshot_group_id" type="string">
      Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] Optional; if omitted, computed by Google Cloud.
</ResponseField>
