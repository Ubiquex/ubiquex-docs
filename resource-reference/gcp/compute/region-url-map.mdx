---
title: "google_compute_region_url_map"
description: "A complete, runnable google_compute_region_url_map program, in every SDK language."
---

`google_compute_region_url_map` -- real, typed bindings generated directly from
real Google Discovery Documents, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region url map"})

    		ubx.Resource(compute.RegionUrlMap, "ci-runner", compute.RegionUrlMapConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionUrlMap } from "./local-sdk/google/sdk/typescript/google/compute/region_url_map.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own region url map" });

      resource(RegionUrlMap, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.compute import RegionUrlMap, RegionUrlMapConfig

    def describe():
        ubx.intent("payments own region url map")

        ubx.resource(RegionUrlMap, "ci-runner", RegionUrlMapConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_url_map called "ci-runner", with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="default_custom_error_response_policy" type="object">
  Specifies the custom error response policy that must be applied when the backend service or backend bucket responds with an error. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="error_response_rules" type="list(object)">
      Specifies rules for returning error responses. In a given policy, if you specify rules for both a range of error codes as well as rules for specific error codes then rules with specific error codes have a higher priority. For example, assume that you configure a rule for 401 (Un-authorized) code, and another for all 4 series error codes (4XX). If the backend service returns a401, then the rule for 401 will be applied. However if the backend service returns a 403, the rule for4xx takes effect. Optional.
      <Expandable title="properties">
        <ResponseField name="match_response_codes" type="list(string)">
          A list of HTTP status codes that this error response rule matches. When a backend returns one of these response codes, the custom error response is used. For example, "404" or "500". Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="override_response_code" type="number">
          The HTTP status code that the URL Map should return to the client when the error occurs. The value must be between 100 and 599. If not specified, the original error code is returned. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="path" type="string">
          The path to the custom error page for this error response rule. This is typically a relative URL path (e.g., '/custom-error.html') that serves the error response from the backend or a static content bucket. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_service" type="string">
      The full or partial URL to the BackendBucket resource that contains the custom error content. Examples are: - https://www.googleapis.com/compute/v1/projects/project/global/backendBuckets/myBackendBucket - compute/v1/projects/project/global/backendBuckets/myBackendBucket - global/backendBuckets/myBackendBucket If errorService is not specified at lower levels likepathMatcher, pathRule and routeRule, an errorService specified at a higher level in theUrlMap will be used. IfUrlMap.defaultCustomErrorResponsePolicy contains one or moreerrorResponseRules[], it must specifyerrorService. If load balancer cannot reach the backendBucket, a simple Not Found Error will be returned, with the original response code (oroverrideResponseCode if configured). errorService is not supported for internal or regionalHTTP/HTTPS load balancers. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_route_action" type="object">
  Specifies the default route action for the region URL map, applied to requests that do not match any other route rule. This is an output-only field, populated by the API with the computed default action configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cache_policy" type="object">
      Message containing CachePolicy configuration for URL Map's Route Action. Optional.
      <Expandable title="properties">
        <ResponseField name="cache_bypass_request_header_names" type="list(string)">
          Bypass the cache when the specified request headers are matched by name, e.g. Pragma or Authorization headers. Values are case-insensitive. Up to 5 header names can be specified. The cache is bypassed for all `cacheMode` values. Optional.
        </ResponseField>
        <ResponseField name="cache_key_policy" type="object">
          Message containing what to include in the cache key for a request for Cache Policy defined on Route Action. Optional.
          <Expandable title="properties">
            <ResponseField name="excluded_query_parameters" type="list(string)">
              Names of query string parameters to exclude in cache keys. All other parameters will be included. Either specify `excludedQueryParameters` or `includedQueryParameters`, not both. '&' and '=' will be percent encoded and not treated as delimiters. Note: This field applies to routes that use backend services. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. For routes that point to a Backend Bucket, use `includedQueryParameters` to define which parameters should be part of the cache key. Optional.
            </ResponseField>
            <ResponseField name="include_host" type="bool">
              If true, requests to different hosts will be cached separately. Note: This setting is only applicable to routes that use a Backend Service. It does not affect requests served by a Backend Bucket, as the host is never included in a Backend Bucket's cache key. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. Optional.
            </ResponseField>
            <ResponseField name="include_protocol" type="bool">
              If true, http and https requests will be cached separately. Note: This setting is only applicable to routes that use a Backend Service. It does not affect requests served by a Backend Bucket, as the protocol is never included in a Backend Bucket's cache key. Attempting to set on a route that points exclusively to Backend Buckets will result in a configuration error. Optional.
            </ResponseField>
            <ResponseField name="include_query_string" type="bool">
              If true, include query string parameters in the cache key according to `includedQueryParameters` and `excludedQueryParameters`. If neither is set, the entire query string will be included. If false, the query string will be excluded from the cache key entirely. Note: This field applies to routes that use backend services. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. For routes that point to a Backend Bucket, use `includedQueryParameters` to define which parameters should be part of the cache key. Optional.
            </ResponseField>
            <ResponseField name="included_cookie_names" type="list(string)">
              Allows HTTP cookies (by name) to be used in the cache key. The name=value pair will be used in the cache key Cloud CDN generates. Note: This setting is only applicable to routes that use a Backend Service. It does not affect requests served by a Backend Bucket. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. Up to 5 cookie names can be specified. Optional.
            </ResponseField>
            <ResponseField name="included_header_names" type="list(string)">
              Allows HTTP request headers (by name) to be used in the cache key. Optional.
            </ResponseField>
            <ResponseField name="included_query_parameters" type="list(string)">
              Names of query string parameters to include in cache keys. All other parameters will be excluded. Either specify `includedQueryParameters` or `excludedQueryParameters`, not both. '&' and '=' will be percent encoded and not treated as delimiters. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="cache_mode" type="string">
          Specifies the cache setting for all responses from this route. If not specified, Cloud CDN uses `CACHE_ALL_STATIC` mode. Optional.
        </ResponseField>
        <ResponseField name="client_ttl" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="default_ttl" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="max_ttl" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="negative_caching" type="bool">
          Negative caching allows per-status code TTLs to be set, in order to apply fine-grained caching for common errors or redirects. This can reduce the load on your origin and improve end-user experience by reducing response latency. When the `cacheMode` is set to `CACHE_ALL_STATIC` or `USE_ORIGIN_HEADERS`, negative caching applies to responses with the specified response code that lack any Cache-Control, Expires, or Pragma: no-cache directives. When the `cacheMode` is set to `FORCE_CACHE_ALL`, negative caching applies to all responses with the specified response code, and overrides any caching headers. By default, Cloud CDN applies the following TTLs to these HTTP status codes: * 300 (Multiple Choice), 301, 308 (Permanent Redirects): 10m * 404 (Not Found), 410 (Gone), 451 (Unavailable For Legal Reasons): 120s * 405 (Method Not Found), 501 (Not Implemented): 60s These defaults can be overridden in `negativeCachingPolicy`. If not specified, Cloud CDN applies negative caching by default. Optional.
        </ResponseField>
        <ResponseField name="negative_caching_policy" type="list(object)">
          Sets a cache TTL for the specified HTTP status code. `negativeCaching` must be enabled to configure `negativeCachingPolicy`. Omitting the policy and leaving `negativeCaching` enabled will use Cloud CDN's default cache TTLs. Note that when specifying an explicit `negativeCachingPolicy`, you should take care to specify a cache TTL for all response codes that you wish to cache. Cloud CDN will not apply any default negative caching when a policy exists. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="number">
              HTTP status code for which the negative caching policy applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ttl" type="object">
              The duration for which a negative cache entry is retained. This is a Duration object containing 'seconds' and 'nanos' fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanosecond portion of the TTL duration. Must be an integer between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of seconds for the negative caching policy's TTL (time-to-live) duration, expressed as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_coalescing" type="bool">
          If true then Cloud CDN will combine multiple concurrent cache fill requests into a small number of requests to the origin. If not specified, Cloud CDN applies request coalescing by default. Optional.
        </ResponseField>
        <ResponseField name="serve_while_stale" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cors_policy" type="object">
      The specification for allowing client-side cross-origin requests. For more information about the W3C recommendation for cross-origin resource sharing (CORS), see Fetch API Living Standard. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_credentials" type="bool">
          In response to a preflight request, setting this to true indicates that the actual request can include user credentials. This field translates to the Access-Control-Allow-Credentials header. Default is false. Optional.
        </ResponseField>
        <ResponseField name="allow_headers" type="list(string)">
          Specifies the content for the Access-Control-Allow-Headers header. Optional.
        </ResponseField>
        <ResponseField name="allow_methods" type="list(string)">
          Specifies the content for the Access-Control-Allow-Methods header. Optional.
        </ResponseField>
        <ResponseField name="allow_origin_regexes" type="list(string)">
          Specifies a regular expression that matches allowed origins. For more information, see regular expression syntax. An origin is allowed if it matches either an item inallowOrigins or an item inallowOriginRegexes. Regular expressions can only be used when the loadBalancingScheme is set to INTERNAL_SELF_MANAGED. Optional.
        </ResponseField>
        <ResponseField name="allow_origins" type="list(string)">
          Specifies the list of origins that is allowed to do CORS requests. An origin is allowed if it matches either an item inallowOrigins or an item inallowOriginRegexes. Optional.
        </ResponseField>
        <ResponseField name="disabled" type="bool">
          If true, disables the CORS policy. The default value is false, which indicates that the CORS policy is in effect. Optional.
        </ResponseField>
        <ResponseField name="expose_headers" type="list(string)">
          Specifies the content for the Access-Control-Expose-Headers header. Optional.
        </ResponseField>
        <ResponseField name="max_age" type="number">
          Specifies how long results of a preflight request can be cached in seconds. This field translates to the Access-Control-Max-Age header. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fault_injection_policy" type="object">
      The specification for fault injection introduced into traffic to test the resiliency of clients to backend service failure. As part of fault injection, when clients send requests to a backend service, delays can be introduced by the load balancer on a percentage of requests before sending those request to the backend service. Similarly requests from clients can be aborted by the load balancer for a percentage of requests. Optional.
      <Expandable title="properties">
        <ResponseField name="abort" type="object">
          Specification for how requests are aborted as part of fault injection. Optional.
          <Expandable title="properties">
            <ResponseField name="http_status" type="number">
              The HTTP status code used to abort the request. The value must be from 200 to 599 inclusive. For gRPC protocol, the gRPC status code is mapped to HTTP status code according to this mapping table. HTTP status 200 is mapped to gRPC status UNKNOWN. Injecting an OK status is currently not supported by Traffic Director. Optional.
            </ResponseField>
            <ResponseField name="percentage" type="number">
              The percentage of traffic for connections, operations, or requests that is aborted as part of fault injection. The value must be from 0.0 to 100.0 inclusive. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="delay" type="object">
          Specifies the delay introduced by the load balancer before forwarding the request to the backend service as part of fault injection. Optional.
          <Expandable title="properties">
            <ResponseField name="fixed_delay" type="object">
              A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="percentage" type="number">
              The percentage of traffic for connections, operations, or requests for which a delay is introduced as part of fault injection. The value must be from 0.0 to 100.0 inclusive. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_stream_duration" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="request_mirror_policy" type="object">
      A policy that specifies how requests intended for the route's backends are shadowed to a separate mirrored backend service. The load balancer doesn't wait for responses from the shadow service. Before sending traffic to the shadow service, the host or authority header is suffixed with-shadow. Optional.
      <Expandable title="properties">
        <ResponseField name="backend_service" type="string">
          The full or partial URL to the BackendService resource being mirrored to. The backend service configured for a mirroring policy must reference backends that are of the same type as the original backend service matched in the URL map. Serverless NEG backends are not currently supported as a mirrored backend service. Optional.
        </ResponseField>
        <ResponseField name="mirror_percent" type="number">
          The percentage of requests to be mirrored to `backend_service`. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="retry_policy" type="object">
      The retry policy associates with HttpRouteRule Optional.
      <Expandable title="properties">
        <ResponseField name="num_retries" type="number">
          Specifies the allowed number retries. This number must be &gt; 0. If not specified, defaults to 1. Optional.
        </ResponseField>
        <ResponseField name="per_try_timeout" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="retry_conditions" type="list(string)">
          Specifies one or more conditions when this retry policy applies. Valid values are: - 5xx: retry is attempted if the instance or endpoint responds with any 5xx response code, or if the instance or endpoint does not respond at all. For example, disconnects, reset, read timeout, connection failure, and refused streams. - gateway-error: Similar to 5xx, but only applies to response codes 502, 503 or504. - connect-failure: a retry is attempted on failures connecting to the instance or endpoint. For example, connection timeouts. - retriable-4xx: a retry is attempted if the instance or endpoint responds with a 4xx response code. The only error that you can retry is error code 409. - refused-stream: a retry is attempted if the instance or endpoint resets the stream with a REFUSED_STREAM error code. This reset type indicates that it is safe to retry. - cancelled: a retry is attempted if the gRPC status code in the response header is set to cancelled. - deadline-exceeded: a retry is attempted if the gRPC status code in the response header is set todeadline-exceeded. - internal: a retry is attempted if the gRPC status code in the response header is set tointernal. - resource-exhausted: a retry is attempted if the gRPC status code in the response header is set toresource-exhausted. - unavailable: a retry is attempted if the gRPC status code in the response header is set tounavailable. Only the following codes are supported when the URL map is bound to target gRPC proxy that has validateForProxyless field set to true. - cancelled - deadline-exceeded - internal - resource-exhausted - unavailable Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="timeout" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="url_rewrite" type="object">
      The spec for modifying the path before sending the request to the matched backend service. Optional.
      <Expandable title="properties">
        <ResponseField name="host_rewrite" type="string">
          Before forwarding the request to the selected service, the request's host header is replaced with contents of hostRewrite. The value must be from 1 to 255 characters. Optional.
        </ResponseField>
        <ResponseField name="path_prefix_rewrite" type="string">
          Before forwarding the request to the selected backend service, the matching portion of the request's path is replaced bypathPrefixRewrite. The value must be from 1 to 1024 characters. Optional.
        </ResponseField>
        <ResponseField name="path_template_rewrite" type="string">
          If specified, the pattern rewrites the URL path (based on the :path header) using the HTTP template syntax. A corresponding path_template_match must be specified. Any template variables must exist in the path_template_match field. - -At least one variable must be specified in the path_template_match field - You can omit variables from the rewritten URL - The * and ** operators cannot be matched unless they have a corresponding variable name - e.g. &#123;format=*&#125; or &#123;var=**&#125;. For example, a path_template_match of /static/&#123;format=**&#125; could be rewritten as /static/content/&#123;format&#125; to prefix/content to the URL. Variables can also be re-ordered in a rewrite, so that /&#123;country&#125;/&#123;format&#125;/&#123;suffix=**&#125; can be rewritten as /content/&#123;format&#125;/&#123;country&#125;/&#123;suffix&#125;. At least one non-empty routeRules[].matchRules[].path_template_match is required. Only one of path_prefix_rewrite orpath_template_rewrite may be specified. Optional.
        </ResponseField>
        <ResponseField name="regex_rewrite" type="object">
          The spec for modifying the path using a regular expression. Optional.
          <Expandable title="properties">
            <ResponseField name="path_pattern" type="string">
              Required. The regular expression used to match against the URL path. It uses RE2 syntax with the following constraints: - Any single character operators - Groups are allowed to have only submatch operator inside - Groups are allowed only without any char repetition, e.g. .* - Any char repetition, e.g. .*, is only allowed to be used in a single regex together with: - Empty string operators - Other repetitions - Ranges - Repetitions of ranges - Ranges are only allowed to have: - Character range - Digits range - Symbols listed in characters allowed for ranges Optional.
            </ResponseField>
            <ResponseField name="path_substitution" type="string">
              Required. Required when path pattern is specified. Used to rewrite matching parts of the path. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="weighted_backend_services" type="list(object)">
      A list of weighted backend services to send traffic to when a route match occurs. The weights determine the fraction of traffic that flows to their corresponding backend service. If all traffic needs to go to a single backend service, there must be oneweightedBackendService with weight set to a non-zero number. After a backend service is identified and before forwarding the request to the backend service, advanced routing actions such as URL rewrites and header transformations are applied depending on additional settings specified in this HttpRouteAction. Optional.
      <Expandable title="properties">
        <ResponseField name="backend_service" type="string">
          The name of the backend service that receives traffic for this weighted backend service entry. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_action" type="object">
          Defines the header modifications to apply to requests and responses for the corresponding weighted backend service in the region URL map. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="request_headers_to_add" type="list(object)">
              The list of headers to be added to the request before it is forwarded to the backend service for this weighted backend service configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header to add to the request. Must be a valid header name, typically containing only letters, digits, and hyphens (e.g., 'X-Forwarded-For'). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value of the request header to add. This value is sent to the backend service along with the header name. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  If true, the header value replaces an existing header with the same name. If false, the header is added as an additional header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_headers_to_remove" type="list(string)">
              A list of HTTP request header names to remove from the request before forwarding to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="response_headers_to_add" type="list(object)">
              A list of response headers to add, with each item specifying a header name and value. These headers are added to HTTP responses sent to the client when the request is served by this weighted backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the HTTP response header to add to responses from the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value of the response header to add, paired with the specified header_name. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  If true, replaces an existing response header with the same name; if false, appends the new header value to any existing values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="response_headers_to_remove" type="list(string)">
              Specifies a list of response header names to remove before sending the response back to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="weight" type="number">
          Specifies the relative weight for this backend service. Traffic is distributed proportionally to the weights of all backend services in the weighted_backend_services list. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_service" type="string">
  The full or partial URL of the defaultService resource to which traffic is directed if none of the hostRules match. If defaultRouteAction is also specified, advanced routing actions, such as URL rewrites, take effect before sending the request to the backend. Only one of defaultUrlRedirect, defaultService or defaultRouteAction.weightedBackendService can be set. defaultService has no effect when the URL map is bound to a target gRPC proxy that has the validateForProxyless field set to true. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="default_url_redirect" type="object">
  Specifies settings for an HTTP redirect. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="host_redirect" type="string">
      The host that is used in the redirect response instead of the one that was supplied in the request. The value must be from 1 to 255 characters. Optional.
    </ResponseField>
    <ResponseField name="https_redirect" type="bool">
      If set to true, the URL scheme in the redirected request is set to HTTPS. If set to false, the URL scheme of the redirected request remains the same as that of the request. This must only be set for URL maps used inTargetHttpProxys. Setting this true forTargetHttpsProxy is not permitted. The default is set to false. Optional.
    </ResponseField>
    <ResponseField name="path_redirect" type="string">
      The path that is used in the redirect response instead of the one that was supplied in the request. pathRedirect cannot be supplied together withprefixRedirect. Supply one alone or neither. If neither is supplied, the path of the original request is used for the redirect. The value must be from 1 to 1024 characters. Optional.
    </ResponseField>
    <ResponseField name="prefix_redirect" type="string">
      The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch, retaining the remaining portion of the URL before redirecting the request. prefixRedirect cannot be supplied together withpathRedirect. Supply one alone or neither. If neither is supplied, the path of the original request is used for the redirect. The value must be from 1 to 1024 characters. Optional.
    </ResponseField>
    <ResponseField name="redirect_response_code" type="string">
      The HTTP Status code to use for this RedirectAction. Supported values are: - MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301. - FOUND, which corresponds to 302. - SEE_OTHER which corresponds to 303. - TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method is retained. - PERMANENT_REDIRECT, which corresponds to 308. In this case, the request method is retained. Optional.
    </ResponseField>
    <ResponseField name="strip_query" type="bool">
      If set to true, any accompanying query portion of the original URL is removed before redirecting the request. If set to false, the query portion of the original URL is retained. The default is set to false. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. A hash of the contents stored in this object. This field is used in optimistic locking. This field is ignored when inserting a UrlMap. An up-to-date fingerprint must be provided in order to update the UrlMap, otherwise the request will fail with error 412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a UrlMap. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="header_action" type="object">
  The request and response header transformations that take effect before the request is passed along to the selected backendService. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="request_headers_to_add" type="list(object)">
      Headers to add to a matching request before forwarding the request to thebackendService. Optional.
      <Expandable title="properties">
        <ResponseField name="header_name" type="string">
          The name of the request header to add to the outgoing request. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_value" type="string">
          The value of the header to be added to requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="replace" type="bool">
          If false, the header value is appended to any existing values for the same header. If true, the header value replaces any existing values for that header. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="request_headers_to_remove" type="list(string)">
      A list of header names for headers that need to be removed from the request before forwarding the request to the backendService. Optional.
    </ResponseField>
    <ResponseField name="response_headers_to_add" type="list(object)">
      Headers to add the response before sending the response back to the client. Optional.
      <Expandable title="properties">
        <ResponseField name="header_name" type="string">
          The name of the response header to add, as specified in the response_headers_to_add header action of the URL map. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_value" type="string">
          The value of the response header to add. This value is paired with the header_name in the same response_headers_to_add block to define a custom header for responses. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="replace" type="bool">
          Indicates whether to replace the existing response header with the provided value. If set to false, the header is added only if it is not already present. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="response_headers_to_remove" type="list(string)">
      A list of header names for headers that need to be removed from the response before sending the response back to the client. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="host_rules" type="list(object)">
  The list of host rules to use against the URL. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="hosts" type="list(string)">
      The list of host patterns to match. The request's Host header must match one of these patterns (e.g., 'example.com' or '*.example.com') for this host rule to apply. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path_matcher" type="string">
      The name of the path matcher to use for this host rule. This must match the name of a path matcher defined in the region URL map's path_matchers list. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#urlMaps for url maps. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="path_matchers" type="list(object)">
  The list of named PathMatchers to use against the URL. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="default_custom_error_response_policy" type="object">
      Defines the default custom error response policy for this path matcher, controlling how errors from backend services are customized, such as serving a custom error page or redirecting. If this policy is not set, the URL map's default custom error response policy is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="error_response_rules" type="list(object)">
          A list of error response rules that define custom error handling for specific HTTP error codes. Each rule specifies which response codes to match and the path to the custom error page. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="match_response_codes" type="list(string)">
              A list of HTTP response codes that this error response rule should match. When a backend returns any of these codes, the specified custom error response is served. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="override_response_code" type="number">
              The HTTP status code to return to the client in place of the original error code. If not specified, the original error code is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path" type="string">
              The request path to match for this error response rule. Must start with a forward slash (/). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="error_service" type="string">
          A reference to a backend service or Cloud Storage bucket that serves the custom error response for this path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_route_action" type="object">
      The default route action for the path matcher, applied when no path rule matches the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="cache_policy" type="object">
          The cache policy for the route action, used to configure Cloud CDN cache key generation for requests matched by this path matcher. It defines which parts of the request (host, protocol, query string, headers) are included in the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cache_bypass_request_header_names" type="list(string)">
              List of HTTP request header names that, when present in a request, cause the cache to be bypassed for that request. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="cache_key_policy" type="object">
              Configures the cache key for this route's cache policy, controlling which request attributes are used to form the cache key and thus determine cache hits. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="excluded_query_parameters" type="list(string)">
                  A list of query parameters to exclude from the cache key. Requests that differ only in these parameters will share the same cached response, improving the cache hit ratio. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="include_host" type="bool">
                  Specifies whether to include the Host header in the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="include_protocol" type="bool">
                  Specifies whether to include the protocol (HTTP or HTTPS) in the cache key. If set to true, requests with different protocols are cached separately; if false, they share the same cache entry. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="include_query_string" type="bool">
                  Whether to include the query string in the cache key. If set to false, the query string is excluded, which can improve cache hit rates for URLs that differ only by query parameters. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="included_cookie_names" type="list(string)">
                  A list of cookie names to include in the cache key. Cookies not listed here are excluded from the cache key, allowing you to vary cached responses based on specific cookie values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="included_header_names" type="list(string)">
                  A list of request header names to include in the cache key. Requests with different values for these headers will be cached as separate entries. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="included_query_parameters" type="list(string)">
                  Specifies the list of query parameter names to include in the cache key. If empty, all query parameters are included. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cache_mode" type="string">
              Specifies how responses are cached. Possible values are `CACHE_ALL_STATIC` (caches static content), `FORCE_CACHE_ALL` (caches all responses regardless of headers), and `USE_ORIGIN_HEADERS` (caches according to the origin's Cache-Control headers). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="client_ttl" type="object">
              The time-to-live (TTL) for cached responses served to clients. This specifies how long a cached response remains valid for client requests before it must be revalidated. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional second component of the client TTL, specified in nanoseconds. Used together with the 'seconds' field to define the cache duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds component of the client TTL duration, specifying how long a response is considered fresh in the cache. This is used together with the nanos field to define the full duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="default_ttl" type="object">
              The default time-to-live (TTL) for cached responses in this cache policy, used when no per-key or per-route TTL is configured. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanoseconds component of the TTL duration. Must be between 0 and 999,999,999, representing the sub-second part of the duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of seconds for the default time-to-live (TTL) of the cache policy. This value is provided as a string and determines how long cached content is served before revalidation. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_ttl" type="object">
              The maximum time-to-live (TTL) for cached responses, expressed as a Duration object with `seconds` and `nanos` fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional seconds component of the maximum TTL, specified in nanoseconds. This value supplements the 'seconds' field to form the full duration, and must be between 0 and 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of seconds for the maximum TTL duration in the cache policy. The value is provided as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="negative_caching" type="bool">
              When true, enables negative caching, causing responses with status codes 404, 405, 410, and 421 to be cached (unless overridden by Cache-Control headers from the origin). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="negative_caching_policy" type="list(object)">
              A list of negative caching policies for the cache policy. Each object in the list defines an HTTP status code and the corresponding time-to-live (TTL) for caching responses with that status code. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="number">
                  The HTTP status code to which this negative caching policy applies. Typically used for status codes like 404 and 500. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ttl" type="object">
                  The time-to-live (TTL) for the negative cache entry, specified as a duration object with 'seconds' and 'nanos' fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional seconds component of the TTL duration, expressed in nanoseconds. Valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the TTL (as a string) for the negative caching policy, representing the duration in whole seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_coalescing" type="bool">
              If true, requests to the backend will be coalesced together when the cache is enabled, reducing the number of requests sent to the backend. Defaults to true. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="serve_while_stale" type="object">
              This block configures serving stale content while revalidating the cache with the origin. It can specify how long stale responses are served and how often to revalidate. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  Specifies the fractional seconds, in nanoseconds, for the serve-while-stale duration. Used together with the seconds field to define the total duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds component of the serve-while-stale duration, encoded as a string (e.g., '3600' for one hour). This is part of a protobuf Duration, where the total duration is the sum of this seconds value and any nanos component. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="cors_policy" type="object">
          The Cross-Origin Resource Sharing (CORS) policy for this route action. It specifies allowed origins, methods, headers, and credentials for cross-origin requests, or disables CORS if not configured. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="allow_credentials" type="bool">
              Specifies whether the browser is allowed to send credentials (such as cookies and HTTP authentication) with cross-origin requests. When set to true, the Access-Control-Allow-Credentials response header is set. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_headers" type="list(string)">
              Specifies the list of HTTP headers that the origin is allowed to include in CORS requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_methods" type="list(string)">
              The list of HTTP methods that are allowed for cross-origin requests. This field configures which methods (e.g., GET, POST, PUT, DELETE) are permitted by the CORS policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_origin_regexes" type="list(string)">
              A list of regular expressions that match origins allowed to make CORS requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_origins" type="list(string)">
              Specifies the list of origins that are allowed to make cross-origin requests. An origin is a combination of scheme, host, and port. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disabled" type="bool">
              Whether the CORS policy is disabled. When true, the CORS policy is not applied to requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="expose_headers" type="list(string)">
              Specifies the response headers that are exposed to the client in a CORS response. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="max_age" type="number">
              Specifies how long (in seconds) the results of a preflight request can be cached in the CORS policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="fault_injection_policy" type="object">
          The fault injection policy for this route action, used to test service resilience by introducing delays or aborting a subset of requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="abort" type="object">
              The specification for how client requests are aborted as part of fault injection. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="http_status" type="number">
                  The HTTP status code used to abort the request. Must be between 200 and 599 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="percentage" type="number">
                  The percentage of requests to abort, expressed as a number from 0 to 100. For example, a value of 10 aborts 10% of requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="delay" type="object">
              Defines the delay fault injection policy. Can set a fixed delay duration and the percentage of traffic to which the delay is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="fixed_delay" type="object">
                  Specifies a fixed delay for fault injection. This object typically contains the delay duration and the percentage of requests to which the delay applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional seconds component of the delay, in nanoseconds. Must be between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="percentage" type="number">
                  The percentage of traffic (between 0.0 and 1.0) to which the delay fault is applied. For example, 0.5 delays 50% of requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="max_stream_duration" type="object">
          The maximum duration a stream (connection) may remain open before being terminated, specified as an object with 'seconds' and 'nanos'. This is part of the route action for a path matcher in a regional URL map; if unset, the stream duration is unlimited. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              The number of nanoseconds (fractional seconds) in the max stream duration. Valid values are from 0 to 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of seconds. Must be from 0 to 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_mirror_policy" type="object">
          The request mirroring policy for the default route action. Specifies a backend service to which mirrored requests are sent. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="backend_service" type="string">
              The backend service resource to which mirrored traffic is sent. This can be a full or partial URL to a BackendService resource, e.g., 'projects/&#123;&#123;project&#125;&#125;/global/backendServices/&#123;&#123;name&#125;&#125;'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="mirror_percent" type="number">
              The percentage of requests to be mirrored to the mirror backend service. Valid values are from 0 to 100, where 0 means no requests are mirrored and 100 means all requests are mirrored. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="retry_policy" type="object">
          Specifies the retry policy for the default route action, including the number of retries, per-try timeout, and retry conditions. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="num_retries" type="number">
              Specifies the number of retries to attempt. Must be greater than 0; if not specified, defaults to 1. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="per_try_timeout" type="object">
              Specifies the timeout for each retry attempt. This is a Duration object that includes 'seconds' and 'nanos' fields to define the length of time. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanosecond component of the per-try timeout duration, used together with the seconds field. Must be between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds portion of the per-try timeout duration for the retry policy. The value is specified as a string, e.g., '30' for 30 seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="retry_conditions" type="list(string)">
              Optional; if omitted, computed by GCP.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="timeout" type="object">
          The timeout for this route action, specified as a Duration object with 'seconds' and 'nanos' fields, defining how long the backend service should wait before considering the request timed out. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              The fractional seconds component of the timeout duration, in nanoseconds. Must be between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="seconds" type="string">
              The number of seconds for the timeout duration, specified as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="url_rewrite" type="object">
          Configures the URL rewrite settings for the default route action of the path matcher, allowing modifications to the host header and path prefix before the request is forwarded. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="host_rewrite" type="string">
              The value to rewrite the Host header to. If specified, the host header is rewritten to this value before the request is forwarded to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_prefix_rewrite" type="string">
              Specifies the value to substitute for the matched path prefix in the request URL. The original matched path prefix is replaced with this string. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_template_rewrite" type="string">
              Specifies a template for rewriting the URL path. The template can include variables in curly braces that capture parts of the original request path, for example /products/&#123;product&#125;. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="regex_rewrite" type="object">
              A regex rewrite rule that rewrites the URL path based on a regular expression match. Typically contains a `regex` pattern and a `substitution` string to replace the matched portion of the path. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="path_pattern" type="string">
                  Optional; if omitted, computed by GCP.
                </ResponseField>
                <ResponseField name="path_substitution" type="string">
                  The substitution text to replace the matched path pattern. Capture groups from the path_pattern can be referenced with backslash-number syntax (e.g., \1, \2). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="weighted_backend_services" type="list(object)">
          Specifies a list of weighted backend services that determine how traffic is distributed among them. Each item includes a backend service and a weight, where the weight controls the proportion of traffic sent to that service. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="backend_service" type="string">
              The name or self_link of the backend service that this weighted backend service entry routes traffic to. This is the target service for the weighted distribution. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_action" type="object">
              Configures the header modifications for this weighted backend service. This block allows adding, overwriting, or removing request and response headers for traffic routed to the backend. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="request_headers_to_add" type="list(object)">
                  Specifies a list of request headers to add to the request before forwarding to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="header_name" type="string">
                      The name of the HTTP request header to add to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="header_value" type="string">
                      The value of the header to add to requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="replace" type="bool">
                      If true, the header will replace an existing header with the same name. If false, the value is appended to the existing header value, if the header already exists. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="request_headers_to_remove" type="list(string)">
                  A list of HTTP header names to remove from the request before it is forwarded to the backend. Removing headers prevents them from reaching the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="response_headers_to_add" type="list(object)">
                  A list of response headers to add to the response. Each header is defined by a name and value that will be appended to the backend's response before it is sent to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="header_name" type="string">
                      The name of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="header_value" type="string">
                      The value of the response header to add. This header is added to the response when the corresponding weighted backend service is selected. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="replace" type="bool">
                      If true, the header value replaces any existing header values with the same name; if false, it appends to existing values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="response_headers_to_remove" type="list(string)">
                  Specifies a list of response header names to remove from the response before it is returned to the client. This is part of the headerAction configuration for the weighted backend services. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weight" type="number">
              Specifies the fraction of traffic sent to this backend service, computed as weight / (sum of all weights in the weighted backend service group). Must be between 0 and 1000. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_service" type="string">
      The default BackendService that receives traffic for this path matcher when the request path does not match any pathRule. Specify the full or partial URL to a BackendService resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="default_url_redirect" type="object">
      Default redirect configuration for requests that don't match any path rules in this path matcher. When set, unmatched requests are redirected according to this object instead of being forwarded to a default service. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="host_redirect" type="string">
          The host that will be used in the redirect response instead of the one that was supplied in the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="https_redirect" type="bool">
          If set to true, the URL redirect uses the HTTPS protocol, redirecting clients to the HTTPS version of the requested URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="path_redirect" type="string">
          The path to which the request will be redirected. This value replaces the original request path in the redirect response. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="prefix_redirect" type="string">
          The prefix used to replace the matched path prefix in the redirect target URL. When set, the redirected URL's path starts with this prefix instead of the original matched prefix. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="redirect_response_code" type="string">
          Specifies the HTTP redirect response code to use for the default URL redirect. Valid values are FOUND (302), MOVED_PERMANENTLY_DEFAULT (301), PERMANENT_REDIRECT (308), SEE_OTHER (303), and TEMPORARY_REDIRECT (307). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="strip_query" type="bool">
          Whether to strip the query string from the original URL before performing the redirect. If set to true, the query portion is removed; if set to false or omitted, the query portion is retained. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      An optional description of the path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="header_action" type="object">
      Configures the request header modifications (additions or removals) applied to requests that match this path matcher, before they are forwarded to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="request_headers_to_add" type="list(object)">
          A list of request headers to add to requests that match this path matcher. Each object in the list specifies a header name and value to be added. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="header_name" type="string">
              The name of the request header to add to the request. For example, X-Custom-Header. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_value" type="string">
              The value of the request header to add to the matched request. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replace" type="bool">
              Whether to replace or append the header value. If true, the header value is replaced; if false, the value is appended. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_headers_to_remove" type="list(string)">
          A list of HTTP header names to remove from the request before it is forwarded to the backend service. The header action for a path matcher allows removing specific request headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="response_headers_to_add" type="list(object)">
          A list of response header name/value pairs to add to the response. Each object contains `header_name` and `header_value` fields, specifying the header to add and its value. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="header_name" type="string">
              The name of the HTTP response header to add. Must be a valid header name, as specified in RFC 7230. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_value" type="string">
              The value of the response header to be added. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replace" type="bool">
              Whether to replace the value of an existing response header with the same name. If set to true, the new value overwrites the existing header; otherwise, the header is appended as an additional header. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="response_headers_to_remove" type="list(string)">
          Specifies the list of response header names to remove. These headers are stripped from the HTTP response before it is returned to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path_rules" type="list(object)">
      Path rules within a path matcher. Each rule contains a list of paths and the corresponding service (backend) to which traffic matching those paths should be routed. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="custom_error_response_policy" type="object">
          The custom error response policy for this path rule. It defines how to customize responses for specific HTTP error codes, such as the error page content and response code to serve. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_response_rules" type="list(object)">
              List of rules that define custom error responses. Each rule specifies which response codes (or ranges) trigger a custom error page, and the path to that page. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="match_response_codes" type="list(string)">
                  Specifies a list of HTTP status codes (e.g., '404', '500') that should trigger this error response rule. When the backend returns any of these response codes, the rule's custom error response is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="override_response_code" type="number">
                  The HTTP status code to return to the client when this error response rule is triggered. Must be an integer between 100 and 599 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path" type="string">
                  The full path (starting with '/') to the custom error page, relative to the backend service or backend bucket serving the original request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="error_service" type="string">
              The full or partial URL of a backend bucket that serves custom error responses. If not specified, the default error behavior applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="paths" type="list(string)">
          List of path patterns to match against the incoming request path. Each path can contain a single trailing * wildcard. If the request path matches any of these patterns, the specified route action is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="route_action" type="object">
          Specifies the route action to take for requests that match this path rule, such as forwarding to a backend service, configuring weighted backend services, URL rewrite, or setting timeouts. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cache_policy" type="object">
              Configures the cache key policy for this route. It determines which parts of a request (such as query parameters, headers, and cookies) are used in the cache key for CDN caching. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="cache_bypass_request_header_names" type="list(string)">
                  The list of request header names that will cause the request to bypass the cache if present in the request. When a request contains any of these headers, the cache is bypassed regardless of other cache policies. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="cache_key_policy" type="object">
                  Specifies the cache key parameters for the cache policy, including which parts of the request (host, protocol, query string) to include in the cache key, and how to handle query string parameters. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="excluded_query_parameters" type="list(string)">
                      Specifies a list of query parameters to exclude from the cache key. When include_query_string is true, these parameters are omitted when generating the cache key; ignored if include_query_string is false. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_host" type="bool">
                      Specifies whether to include the HTTP Host header in the cache key. When true, requests with different Host headers are cached separately. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_protocol" type="bool">
                      If true, HTTP and HTTPS requests will be served with the same response, meaning the protocol is not included in the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_query_string" type="bool">
                      Specifies whether the query string should be included in the cache key. If false, the query string is excluded from the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_cookie_names" type="list(string)">
                      The list of cookie names to include in the cache key. If empty, no cookies are included in the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_header_names" type="list(string)">
                      A list of HTTP header names to include in the cache key. The values of these headers will be incorporated into the cache key, affecting cache hits. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_query_parameters" type="list(string)">
                      Specifies a list of query string parameters to include in the cache key. When non-empty, only these parameters are used to construct the cache key; all other query parameters are ignored. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cache_mode" type="string">
                  The cache mode determines what type of responses are cached. CACHE_ALL_STATIC automatically caches static content, FORCE_CACHE_ALL caches all content regardless of origin headers, and USE_ORIGIN_HEADERS relies on the origin's Cache-Control headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="client_ttl" type="object">
                  Specifies the time-to-live (TTL) for cached responses in the client's cache, determining how long a client can serve a response before revalidating with the origin. This is configured within the cache policy of a URL map route action. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional part of the duration in nanoseconds, in the range 0 to 999,999,999. This represents the sub-second component of the client TTL cache policy duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      Optional; if omitted, computed by GCP.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="default_ttl" type="object">
                  The default time-to-live (TTL) for cached responses, represented as a duration object. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional second component of the default TTL, in nanoseconds. This is combined with the seconds field to specify the time-to-live for the cache policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The whole seconds portion of the default TTL duration for the cache policy. Provided as a string representation of an integer number of seconds (e.g., '30'). Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="max_ttl" type="object">
                  Optional; if omitted, computed by GCP.
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond component of the maximum TTL duration for cached responses. Valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the maximum TTL duration for cached responses. Must be a non-negative integer represented as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="negative_caching" type="bool">
                  Enables negative caching, which caches HTTP error responses (e.g., 404) for a short duration. Defaults to false when not specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="negative_caching_policy" type="list(object)">
                  Defines caching policies for error responses (negative caching). Each entry specifies an HTTP status code and a TTL (in seconds) for which responses with that code are cached. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="number">
                      The HTTP status code (e.g., 404, 301) for which negative caching is enabled. This code determines which responses are cached according to the negative caching policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="ttl" type="object">
                      The time-to-live (TTL) for caching a negative response for the specified status code. As a Duration object, it specifies how long the negative cache entry remains valid, with a valid range of 0 to 3600 seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The fractional seconds component of the TTL, in nanoseconds. The valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          The seconds component of the TTL duration for negative caching. The negative cache entry expires after this many seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="request_coalescing" type="bool">
                  If set to true (default), enables request coalescing for the cache policy. When a cached object goes stale and multiple requests for it arrive, Cloud CDN coalesces them into a single request to the origin, reducing backend load. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="serve_while_stale" type="object">
                  Specifies how long the CDN can serve a stale cached response while attempting to revalidate the content with the origin server. Used in the cache_policy of path rules for a regional URL map. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond component of the serve_while_stale duration. Together with the seconds field, it specifies the length of time to serve stale content. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The duration in seconds for serving stale content when the origin is unavailable, expressed as a string. This is part of the CDN cache policy's serve_while_stale setting. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cors_policy" type="object">
              The CORS (Cross-Origin Resource Sharing) policy for this route action, specifying how the backend responds to cross-origin requests. This object defines allowed origins, methods, headers, and whether credentials are allowed. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allow_credentials" type="bool">
                  When set to true, the CORS policy allows credentials (e.g., cookies, authorization headers) in cross-origin requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_headers" type="list(string)">
                  The list of HTTP headers that are allowed in cross-origin requests. This corresponds to the Access-Control-Allow-Headers header in the CORS response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_methods" type="list(string)">
                  Specifies the list of HTTP methods that are allowed for cross-origin requests in the CORS policy. For example, GET, POST, PUT, DELETE, etc. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origin_regexes" type="list(string)">
                  A list of origin regexes that are allowed to make CORS requests. Cross-origin requests from origins matching any of these regular expressions are permitted. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origins" type="list(string)">
                  Specifies the list of origins allowed to make cross-origin requests. Origins are specified in the format scheme://host:port. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="disabled" type="bool">
                  If set to true, the CORS policy is disabled and CORS responses are not applied. The default is false, meaning CORS is enabled. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="expose_headers" type="list(string)">
                  Specifies the response headers that the browser can access. These headers are exposed via the Access-Control-Expose-Headers header in CORS responses. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_age" type="number">
                  Specifies the maximum age in seconds that the results of a preflight request can be cached by the client. This is used in the Access-Control-Max-Age header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="fault_injection_policy" type="object">
              Configures the fault injection policy for this route action, allowing the introduction of delays or aborts to simulate failures for testing resilience. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="abort" type="object">
                  The specification for how client requests are aborted as part of fault injection. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="http_status" type="number">
                      The HTTP status code to use when aborting the request. Must be an integer between 100 and 599. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      The percentage of requests to abort, as a number between 0 and 100. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="delay" type="object">
                  Configuration for delaying requests as part of fault injection. This block defines a fixed delay duration and the percentage of requests that will experience the delay. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fixed_delay" type="object">
                      Specifies the fixed delay duration to be injected before forwarding the request. The delay is defined as a Duration object with seconds and nanos fields, allowing precise timing. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The fractional part of the fixed delay duration in nanoseconds, used together with the 'seconds' field to specify the total delay amount. Valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          The number of seconds for the fixed delay. Together with the nanos field, this defines the total delay duration for fault injection. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      The percentage of requests for which the delay is injected. Must be a value between 0 and 100. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_stream_duration" type="object">
              Settings for the maximum duration of a stream for this route action. This is a duration object, typically containing seconds and nanos fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional part of the maximum stream duration, specified in nanoseconds (0-999,999,999). Combined with the seconds field to express the total duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Specifies the seconds portion of the max stream duration. This is a string field, representing an integer number of seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_mirror_policy" type="object">
              This block configures request mirroring for the route action, allowing incoming traffic to be copied to a separate backend service for observation or analysis. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The backend service (selfLink or URL) that requests are mirrored to. This is a reference to a compute backend service resource in the same region as the URL map. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="mirror_percent" type="number">
                  The percentage of requests that will be mirrored to the mirror backend. Must be a value between 0 and 100. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="retry_policy" type="object">
              The retry policy configuration for this route action, specifying how requests are retried, including conditions and try timeouts. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="num_retries" type="number">
                  The number of retries to attempt before giving up. Must be an integer greater than 0. If not specified, the default is 1. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="per_try_timeout" type="object">
                  The timeout for each retry attempt, specified as a Duration object. This defines the maximum time to wait for an individual attempt to complete before it is considered failed and the next retry is triggered. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional seconds component of the timeout duration, in nanoseconds. Valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the per-try timeout duration. Specifies the whole-second portion of the timeout, typically combined with a nanos field for sub-second precision. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="retry_conditions" type="list(string)">
                  Specifies the retry conditions under which the request is retried. Empty list means no retries. Allowed values include '5xx', 'gateway-error', 'connect-failure', 'refused-stream', 'canceled', 'deadline-exceeded', 'internal', 'reset', 'resource-exhausted', 'unavailable'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="timeout" type="object">
              The timeout for requests routed by this path rule. This object represents a Duration, containing 'seconds' and 'nanos' fields, and specifies how long to wait for a backend response before considering the request timed out. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanosecond component of the timeout duration, representing the fractional seconds part of the span. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Specifies the duration of the timeout in seconds. The value must be a string representation of a non-negative integer from 0 to 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="url_rewrite" type="object">
              The url_rewrite block configures rewriting of the request URL for matched requests. It can set a new host header and path prefix that will be used when forwarding the request to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="host_rewrite" type="string">
                  Specifies the value of the Host header in the rewritten URL. This can be used to rewrite the hostname of the request before forwarding to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_prefix_rewrite" type="string">
                  Specifies the string that replaces the matched URL path prefix in the request before it is forwarded to the backend service. For instance, if the path rule matches a prefix like /old, setting this to /new rewrites the URL path from /old/... to /new/... while preserving the remainder of the path. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_template_rewrite" type="string">
                  The path template rewrite to apply to the request path. This can use variables like &#123;variable&#125; to reference captured path segments. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="regex_rewrite" type="object">
                  Defines a regular expression-based URL path rewrite. This object includes a pattern to match against the path and a substitution string to replace it. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path_pattern" type="string">
                      The regular expression (RE2) pattern to match against the URL path. This pattern defines the portions of the path that can be captured and referenced in the rewrite template. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path_substitution" type="string">
                      The substitution string that replaces the portion of the URL path matched by the `regex` pattern in this regex rewrite rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weighted_backend_services" type="list(object)">
              A list of weighted backend services that will be used to distribute traffic according to assigned weights. Each entry specifies a backend service and a weight; traffic is routed proportionally to the weights across the listed services. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The backend service to which traffic is routed for this weighted backend service entry. This can be specified as the full URL, partial URL, or name of the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_action" type="object">
                  Defines header actions to apply to requests and responses for this weighted backend service, including adding or removing headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="request_headers_to_add" type="list(object)">
                      A list of request headers to be added to the request before forwarding it to the chosen backend service. Each object in the list specifies a header name and a header value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the request header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the request header to add. This value replaces or appends to the existing header value for the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          Determines whether the existing header value is replaced (true) or the new value is appended (false) when adding a request header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="request_headers_to_remove" type="list(string)">
                      A list of request header names to remove before the request is forwarded to the backend service for this weighted backend service. Each item is a string header name. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="response_headers_to_add" type="list(object)">
                      A list of headers to add to the response before it is sent back to the client. Each object in the list specifies a header name and value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          Indicates whether to replace an existing response header with the specified value. If true, the header is replaced; if false, the value is added, potentially resulting in duplicate headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="response_headers_to_remove" type="list(string)">
                      List of response header names to remove from the response sent to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="weight" type="number">
                  The weight of this backend service. The weight determines the fraction of traffic that goes to this backend service, computed as weight divided by the sum of all weights in the weighted backend services list. Must be an integer between 0 and 1000. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service" type="string">
          The backend service or backend bucket to use when the path matches this rule. Specify the self_link or name of a regional backend service or backend bucket. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url_redirect" type="object">
          Specifies a redirect for requests that match this path rule. When set, matched requests are redirected according to the configured redirect behavior instead of being forwarded to a backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="host_redirect" type="string">
              The host that will be used in the redirect response instead of the one that was supplied in the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="https_redirect" type="bool">
              If set to true, the URL scheme in the redirected request is set to https. If set to false, the scheme remains unchanged. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_redirect" type="string">
              The path portion of the URL to redirect to. If set, the original request path is replaced with this value when the redirect is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="prefix_redirect" type="string">
              The prefix to use in the redirect URL, replacing the matched prefix of the original request path. For example, if the path rule matches the prefix '/old' and prefix_redirect is set to '/new', a request to '/old/foo' redirects to '/new/foo'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="redirect_response_code" type="string">
              The HTTP redirect status code. Valid values are FOUND (302), MOVED_PERMANENTLY_DEFAULT (301), PERMANENT_REDIRECT (308), SEE_OTHER (303), and TEMPORARY_REDIRECT (307). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="strip_query" type="bool">
              Indicates whether the query string of the original request is preserved or removed during the redirect. When set to true, the query string is stripped; when false (default), the query string is kept. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="route_rules" type="list(object)">
      List of route rules that define how requests are matched and routed to backend services within the path matcher. Each route rule specifies match criteria and a corresponding service. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="custom_error_response_policy" type="object">
          The custom_error_response_policy block configures custom error responses for this route rule, allowing you to define custom error pages or redirects for specific HTTP error codes returned by the load balancer. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_response_rules" type="list(object)">
              A list of error response rules. Each rule specifies an error code (e.g., 404, 500) and the path to a custom error page that should be served when that error occurs. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="match_response_codes" type="list(string)">
                  A list of HTTP status codes (e.g., '404', '500') that this custom error response rule matches. When the backend returns one of these codes, the associated custom error page is served. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="override_response_code" type="number">
                  The HTTP status code that the original error response is overridden with. This is the code returned to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path" type="string">
                  The full path to the error response content, such as a Cloud Storage object path (e.g., gs://bucket/errors/404.html) that should be served when this error response rule matches. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="error_service" type="string">
              The full or partial URL of a BackendService or BackendBucket that will serve the custom error responses for this route rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          An optional, human-readable description of this route rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_action" type="object">
          Defines the header actions for this route rule, including adding and removing request and response headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="request_headers_to_add" type="list(object)">
              A list of objects representing additional HTTP headers to be added to the request when the route rule matches. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header to add to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value of the header to be added to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  Indicates whether to replace or append the header value. When true, the new value replaces any existing header with the same name; when false, the header is added as an additional header, preserving any existing ones. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_headers_to_remove" type="list(string)">
              Specifies a list of HTTP header names to remove from the request before forwarding. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="response_headers_to_add" type="list(object)">
              Specifies a list of response headers to add or override in the response sent to the client. Each object in the list defines a header name and header value. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the response header to add to the response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  Indicates whether to replace an existing response header with the same name (true) or append the header value (false). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="response_headers_to_remove" type="list(string)">
              A list of response header names to remove. These headers are removed from the response before it is returned to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_rules" type="list(object)">
          List of match rules that define the conditions under which incoming requests are matched to this route rule. Each match rule can specify path matching criteria (such as full path, prefix, or regex), header and query parameter matches, and case sensitivity. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="full_path_match" type="string">
              Specifies the full request path that must exactly match for this match rule to apply. This is an exact match on the entire path, as opposed to prefix or regex matching. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_matches" type="list(object)">
              Defines a list of HTTP header match conditions. The request must satisfy all specified header conditions for the match rule to be considered a match. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="exact_match" type="string">
                  Specifies the exact header value that must match for the request to satisfy the match condition. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header to match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="invert_match" type="bool">
                  If set to true, the match condition is inverted, meaning the request matches when the header value does NOT equal the specified value. For example, with invert_match true and a specified value of 'foo', the match succeeds only if the header value is not 'foo'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="prefix_match" type="string">
                  Specifies a prefix match for the header value. The header value must start with this prefix. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="present_match" type="bool">
                  When set to true, the match is successful only if the header is present. This is an alternative to using exact_match, prefix_match, suffix_match, and regex_match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_match" type="object">
                  Defines an inclusive numeric range that the header value must fall within to match. The object contains integer fields rangeStart and rangeEnd. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="range_end" type="string">
                      The inclusive end of the integer range used to match a header value. The header value must be less than or equal to this number to match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="range_start" type="string">
                      The start of the range (inclusive) to match for the header value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="regex_match" type="string">
                  The regular expression pattern that the value of the header must match for the match rule to apply. It is used to match the header value against a regular expression, as an alternative to exact matching. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="suffix_match" type="string">
                  The suffix that the header value must end with for the match to be considered successful. The match is case-sensitive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="ignore_case" type="bool">
              Indicates whether to ignore case when matching the request path. When set to true, the match is case-insensitive. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metadata_filters" type="list(object)">
              A list of metadata filters that requests must match. Each filter specifies a list of labels and a match criterion (MATCH_ALL or MATCH_ANY) to determine whether all or any labels must match. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="filter_labels" type="list(object)">
                  Specifies a list of metadata label filters, each containing a key (name) and value (value), used to match incoming request metadata for this routing rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The key of the metadata filter label. This specifies the label name to match against when filtering requests. For example, in a filter label 'env=prod', the name is 'env'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of the metadata filter label. This value is matched against the corresponding metadata header value (e.g., x-goog-meta- prefixed header) for the request, and the request is considered matched when the header value equals this value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="filter_match_criteria" type="string">
                  Specifies how the filter labels are combined: MATCH_ALL requires all specified labels to match, MATCH_ANY requires at least one to match, and NOT_SET indicates the field is not configured. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="path_template_match" type="string">
              Specifies a path template to match against the request path. The template can include variables in curly braces (e.g., /users/&#123;id&#125;) that match any value in that segment, enabling flexible path-based routing. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="prefix_match" type="string">
              Specifies the prefix of the request path to match. The path must begin with this prefix, and the match is case-sensitive. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="query_parameter_matches" type="list(object)">
              A list of query parameter match criteria for this match rule. Each entry specifies a query parameter name and value to match, with an optional exactMatch flag to control whether the match is exact or uses regular expressions. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="exact_match" type="string">
                  The exact value to match against the query parameter's value. The match rule is satisfied only when the query parameter value equals this string exactly. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the query parameter to match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="present_match" type="bool">
                  When set to true, the match requires the query parameter to be present in the request, regardless of its value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="regex_match" type="string">
                  The regular expression (RE2 syntax) that the query parameter value must match. This is mutually exclusive with exact_match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="regex_match" type="string">
              A regular expression that the entire request path must match. This is used to determine if the route rule applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="priority" type="number">
          The priority for this route rule, as an integer from 0 to 2147483647. Lower values are evaluated first, and priorities must be unique within the path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="route_action" type="object">
          The action to be performed when a request matches this route rule, which may include URL rewriting, routing to weighted backend services, or other routing policies. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cache_policy" type="object">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="cache_bypass_request_header_names" type="list(string)">
                  Specifies the list of request header names that cause the cache to be bypassed for requests containing them. Such requests are forwarded to the origin rather than served from cache. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="cache_key_policy" type="object">
                  Defines the cache key policy for the cache policy, controlling how cache keys are generated (e.g., which query parameters, HTTP headers, or cookies are included). Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="excluded_query_parameters" type="list(string)">
                      A list of query parameter names to exclude from the cache key. Requests that differ only in these parameters will share the same cached response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_host" type="bool">
                      Whether to include the hostname in the cache key. If true, requests to different hosts are cached separately. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_protocol" type="bool">
                      If true, the request protocol (http or https) is included in the cache key, so cache entries are separated by protocol. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_query_string" type="bool">
                      Controls whether the query string from the request URI is included in the cache key. When set to false, the cache key ignores query string parameters. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_cookie_names" type="list(string)">
                      Specifies the names of cookies to include in the cache key. When set, only these cookies are used as part of the cache key; any other cookies are ignored. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_header_names" type="list(string)">
                      A list of HTTP header names to include in the cache key. Headers are case-insensitive and are used to construct the cache key for the cached response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_query_parameters" type="list(string)">
                      Optional; if omitted, computed by GCP.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cache_mode" type="string">
                  Specifies the cache mode for the CDN. CACHE_ALL_STATIC caches static content automatically while respecting origin headers for dynamic content; FORCE_CACHE_ALL caches all content regardless of origin headers; USE_ORIGIN_HEADERS relies on the origin's Cache-Control headers for both static and dynamic content. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="client_ttl" type="object">
                  Defines the client-side TTL for cached responses, specified as a Duration object with seconds and nanos fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond component of the client TTL (time-to-live) duration, representing fractional seconds. Valid range is 0 to 999999999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The number of seconds in the TTL duration for the client cache. Must be a non-negative integer expressed as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="default_ttl" type="object">
                  The default time-to-live for cached responses in the CDN cache, applied when the response does not specify explicit cache directives. The value is a duration object. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional seconds component of the TTL duration, in nanoseconds. Must be between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The number of seconds in the default TTL duration, as a string. For example, '60' represents 60 seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="max_ttl" type="object">
                  The maximum time for which the content can be cached. This is a Duration object with 'seconds' and 'nanos' fields. It must be greater than the default TTL if a default is set. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond part of the maximum TTL duration, in the range 0-999,999,999. Used together with the seconds value to specify the total max_ttl duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the maximum TTL duration. This corresponds to the `seconds` field of a Google Cloud Duration object, representing signed seconds of the span of time. Must be from 0 to 315,576,000,000 inclusive, specified as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="negative_caching" type="bool">
                  Whether to cache responses that are considered errors (e.g., HTTP 404, 502). Setting this to true enables negative caching. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="negative_caching_policy" type="list(object)">
                  A list of negative caching policies that configure a cache TTL for specific HTTP status codes when negative caching is enabled. Each policy sets a TTL for a given HTTP status code. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="number">
                      The HTTP status code to define a negative caching TTL for. Valid values are 300, 301, 308, 404, 405, 410, 421, 451, and 501. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="ttl" type="object">
                      Optional; if omitted, computed by GCP.
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The nanosecond component of the TTL duration, ranging from 0 to 999999999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          The number of seconds in the TTL duration for the negative caching policy. Valid values range from 0 to 315576000000 (about 10 years), represented as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="request_coalescing" type="bool">
                  If true, requests for the same resource from different hosts are coalesced into a single cache entry, reducing the number of cache lookups. The default is false. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="serve_while_stale" type="object">
                  Defines the stale-while-revalidate behavior for the CDN cache policy, allowing stale content to be served while the cache is refreshed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanoseconds component of the serve-while-stale duration. Must be a non-negative integer between 0 and 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The number of seconds for which the CDN will continue to serve stale content while attempting to fetch a fresh response from the origin. Provided as a decimal string, e.g., "300". Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cors_policy" type="object">
              The CORS (Cross-Origin Resource Sharing) policy for this route action. Configures how requests from different origins are handled, including allowed origins, methods, headers, and credentials. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allow_credentials" type="bool">
                  Specifies whether the response to the request can be exposed when the credentials flag is true. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_headers" type="list(string)">
                  The list of HTTP headers that the CORS policy allows. This corresponds to the Access-Control-Allow-Headers header in CORS responses, indicating which headers are permitted in the actual request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_methods" type="list(string)">
                  Specifies the list of HTTP methods that are allowed for CORS requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origin_regexes" type="list(string)">
                  Specifies a list of regular expressions that matchallowed origins for CORS requests. A request's Origin header is allowed if it matches any of the provided regexes. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origins" type="list(string)">
                  Specifies the list of origins that are allowed to make cross-origin requests as part of the CORS policy for this route rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="disabled" type="bool">
                  Indicates whether the CORS policy is disabled. When set to true, the CORS policy is not applied to the route. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="expose_headers" type="list(string)">
                  A list of response headers that browsers are allowed to expose to the client. These headers appear in the Access-Control-Expose-Headers response header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_age" type="number">
                  The maximum time (in seconds) that the client can cache the response to a preflight request. Must be a non-negative integer. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="fault_injection_policy" type="object">
              The fault injection policy for this route action. It can be used to simulate failures by delaying or aborting requests, helping test the resilience of your backend services. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="abort" type="object">
                  Configures abort fault injection, returning a specified HTTP status code to a percentage of requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="http_status" type="number">
                      The HTTP status code to return when aborting the request, typically a 4xx or 5xx code to simulate an upstream error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      The percentage of traffic on which the abort fault is injected. Must be a number between 0.00 and 100.00 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="delay" type="object">
                  Configuration for delay fault injection, specifying the fixed delay amount (in Duration format) and the percentage of requests to which the delay is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fixed_delay" type="object">
                      The fixed_delay block specifies the fixed amount of delay to inject into requests, expressed as a duration with 'seconds' and 'nanoseconds' fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The fractional seconds component of the delay duration, in nanoseconds. Valid values are 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          The whole seconds component of the fixed delay duration. For example, a 1.5-second delay uses seconds: "1" and nanos: "500000000". Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      Percentage of requests that will get the delay fault injection. Must be between 0 and 100. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_stream_duration" type="object">
              A duration object specifying the maximum time a stream can remain open before being terminated. Contains 'seconds' and 'nanoseconds' fields. This value is ignored for non-streaming requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional seconds component of the max stream duration, expressed in nanoseconds. This value is combined with the seconds component to specify the total duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of seconds in the max stream duration. Must be from 0 to 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_mirror_policy" type="object">
              Request mirroring policy for the route action. When set, incoming requests are mirrored to the backend service specified in the policy's `backend_service` field, while the original request proceeds normally, allowing traffic inspection without altering the response. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The regional backend service that will receive mirrored traffic. This field is required and must reference an existing backend service resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="mirror_percent" type="number">
                  The percentage of requests to mirror to the backend service, as a number from 0.0 to 100.0 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="retry_policy" type="object">
              The retry policy for the route rule's route action. It determines the number of retries, the timeout per retry attempt, and the conditions (such as HTTP status codes) under which a request is retried. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="num_retries" type="number">
                  Number of retry attempts to perform before the request is considered failed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="per_try_timeout" type="object">
                  The timeout for each retry attempt in the retry policy, specified as a Duration object (for example, seconds). This applies per individual attempt, separate from the overall request timeout. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The number of nanoseconds in the `per_try_timeout` duration. Must be an integer between 0 and 999,999,999, representing the fractional seconds part of the timeout. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The number of seconds (as a string) for the per-try timeout duration. This is part of the retry policy and sets the maximum time a single retry attempt is allowed to take before it is considered failed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="retry_conditions" type="list(string)">
                  A list of conditions that trigger a retry for the associated route, such as specific HTTP status codes (e.g., '5xx') or connection errors (e.g., 'connect-failure', 'reset'). These conditions are evaluated in the retry policy of the route action. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="timeout" type="object">
              Specifies the timeout for the request to the backend service. It is a duration defined in seconds and nanoseconds. If not set, the backend service's default timeout is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional seconds component of the timeout duration, in nanoseconds. Valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds component of the timeout duration for this route action. For example, '30' means 30 seconds. This is part of a duration that may also include a nanos component. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="url_rewrite" type="object">
              The URL rewrite configuration for this route action. It defines how to rewrite the request's host header and path prefix before forwarding to the target service. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="host_rewrite" type="string">
                  Specifies the value of the Host header that will be set in the request. If not specified, the original Host header is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_prefix_rewrite" type="string">
                  Specifies the path prefix to use when rewriting the URL before forwarding the request to the backend. If not set, the original path prefix is preserved. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_template_rewrite" type="string">
                  Defines a template for rewriting the request URL path. Variables from the matched path template (using pathTemplateMatch) can be referenced in the rewrite template. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="regex_rewrite" type="object">
                  Specifies a regex rewrite rule for the URL rewrite action. This rule defines a regular expression pattern and a substitution string used to rewrite the URL path. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path_pattern" type="string">
                      The regular expression pattern to match against the request path. It must be a valid RE2 expression, between 1 and 1024 characters long, and is used to determine which portions of the URL path to rewrite. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path_substitution" type="string">
                      Substitution string for the path regex rewrite. Can reference capture groups from the match regex using backslash-number syntax (e.g., \1). Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weighted_backend_services" type="list(object)">
              A list of weighted backend services used to distribute traffic proportionally across multiple backend services. Each entry specifies a backend service and a weight; traffic is routed based on the relative weights. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The name or self_link of the regional backend service that this weighted entry routes traffic to. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_action" type="object">
                  The header action for this weighted backend service. This specifies custom headers to add, set, or remove on requests forwarded to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="request_headers_to_add" type="list(object)">
                      This block configures a list of request headers to add to the request before it is sent to the backend service. Each object contains 'header_name' and 'header_value' fields, specifying the header to add and its value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the HTTP header to add to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the request header to add. This pairs with the header name (set in a sibling field) to define a header that will be appended to requests routed to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          Optional; if omitted, computed by GCP.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="request_headers_to_remove" type="list(string)">
                      A list of header names to be removed from the request before forwarding to the backend service. These headers are stripped from the request when routed to the associated weighted backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="response_headers_to_add" type="list(object)">
                      A list of response headers to add. Each entry specifies a header name, header value, and a boolean indicating whether to replace existing headers with the same name. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the HTTP response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          If set to true, the added response header will replace an existing header with the same name; otherwise, the value is appended to the existing header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="response_headers_to_remove" type="list(string)">
                      A list of response header names to remove. These headers are stripped from the response before it is sent to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="weight" type="number">
                  The weight of this backend service in the weighted group, determining the proportion of traffic routed to it relative to the total weight of all services in the group. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service" type="string">
          The full or partial URL (or name) of the backend service to which traffic matching this rule is routed. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url_redirect" type="object">
          A block that defines a URL redirect action for this route rule. When specified, requests matching this rule are redirected to the URL specified by the redirect parameters. If url_redirect is set, neither the service nor the urlRewrite action may be set for the same rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="host_redirect" type="string">
              Specifies the host to use in the redirect response. If not set, the request's original host is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="https_redirect" type="bool">
              If set to true, the redirect uses the HTTPS protocol; otherwise it uses the same protocol as the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_redirect" type="string">
              The path to which the request should be redirected. If specified, the redirect URL uses this exact path; cannot be used together with prefix_redirect. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="prefix_redirect" type="string">
              Specifies the prefix used for the redirect URL. This value replaces the matched prefix in the original request path. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="redirect_response_code" type="string">
              The HTTP redirect response code to use for the redirect. Allowed values: FOUND (302), MOVED_PERMANENTLY_DEFAULT (301), PERMANENT_REDIRECT (308), SEE_OTHER (303), TEMPORARY_REDIRECT (307). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="strip_query" type="bool">
              If set to true, any accompanying query portion of the original URL is removed before redirecting. If set to false, the query string is preserved. Defaults to false. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the regional URL map resides. This field is not applicable to global URL maps. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tests" type="list(object)">
  The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a maximum of 100 tests per UrlMap. Not supported when the URL map is bound to a target gRPC proxy that has validateForProxyless field set to true. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      An optional description for a test case in the URL map, used to provide additional context or document the test's purpose. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="expected_output_url" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="expected_redirect_response_code" type="number">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="headers" type="list(object)">
      A list of HTTP request headers that must be present for the URL map test to match. Each item in the list is an object with a header name and value. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the request header to match against in the test. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="host" type="string">
      The hostname or host pattern to match in the test request for validating URL map routing rules. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path" type="string">
      The URL path to be used in the test request. For example, '/path'. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="service" type="string">
      A reference to the backend service that the test expects to handle requests for the specified host and path. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="default_custom_error_response_policy" type="object">
  Specifies the custom error response policy that must be applied when the backend service or backend bucket responds with an error. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="error_response_rules" type="list(object)">
      Specifies rules for returning error responses. In a given policy, if you specify rules for both a range of error codes as well as rules for specific error codes then rules with specific error codes have a higher priority. For example, assume that you configure a rule for 401 (Un-authorized) code, and another for all 4 series error codes (4XX). If the backend service returns a401, then the rule for 401 will be applied. However if the backend service returns a 403, the rule for4xx takes effect. Optional.
      <Expandable title="properties">
        <ResponseField name="match_response_codes" type="list(string)">
          A list of HTTP status codes that this error response rule matches. When a backend returns one of these response codes, the custom error response is used. For example, "404" or "500". Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="override_response_code" type="number">
          The HTTP status code that the URL Map should return to the client when the error occurs. The value must be between 100 and 599. If not specified, the original error code is returned. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="path" type="string">
          The path to the custom error page for this error response rule. This is typically a relative URL path (e.g., '/custom-error.html') that serves the error response from the backend or a static content bucket. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_service" type="string">
      The full or partial URL to the BackendBucket resource that contains the custom error content. Examples are: - https://www.googleapis.com/compute/v1/projects/project/global/backendBuckets/myBackendBucket - compute/v1/projects/project/global/backendBuckets/myBackendBucket - global/backendBuckets/myBackendBucket If errorService is not specified at lower levels likepathMatcher, pathRule and routeRule, an errorService specified at a higher level in theUrlMap will be used. IfUrlMap.defaultCustomErrorResponsePolicy contains one or moreerrorResponseRules[], it must specifyerrorService. If load balancer cannot reach the backendBucket, a simple Not Found Error will be returned, with the original response code (oroverrideResponseCode if configured). errorService is not supported for internal or regionalHTTP/HTTPS load balancers. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_route_action" type="object">
  Specifies the default route action for the region URL map, applied to requests that do not match any other route rule. This is an output-only field, populated by the API with the computed default action configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cache_policy" type="object">
      Message containing CachePolicy configuration for URL Map's Route Action. Optional.
      <Expandable title="properties">
        <ResponseField name="cache_bypass_request_header_names" type="list(string)">
          Bypass the cache when the specified request headers are matched by name, e.g. Pragma or Authorization headers. Values are case-insensitive. Up to 5 header names can be specified. The cache is bypassed for all `cacheMode` values. Optional.
        </ResponseField>
        <ResponseField name="cache_key_policy" type="object">
          Message containing what to include in the cache key for a request for Cache Policy defined on Route Action. Optional.
          <Expandable title="properties">
            <ResponseField name="excluded_query_parameters" type="list(string)">
              Names of query string parameters to exclude in cache keys. All other parameters will be included. Either specify `excludedQueryParameters` or `includedQueryParameters`, not both. '&' and '=' will be percent encoded and not treated as delimiters. Note: This field applies to routes that use backend services. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. For routes that point to a Backend Bucket, use `includedQueryParameters` to define which parameters should be part of the cache key. Optional.
            </ResponseField>
            <ResponseField name="include_host" type="bool">
              If true, requests to different hosts will be cached separately. Note: This setting is only applicable to routes that use a Backend Service. It does not affect requests served by a Backend Bucket, as the host is never included in a Backend Bucket's cache key. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. Optional.
            </ResponseField>
            <ResponseField name="include_protocol" type="bool">
              If true, http and https requests will be cached separately. Note: This setting is only applicable to routes that use a Backend Service. It does not affect requests served by a Backend Bucket, as the protocol is never included in a Backend Bucket's cache key. Attempting to set on a route that points exclusively to Backend Buckets will result in a configuration error. Optional.
            </ResponseField>
            <ResponseField name="include_query_string" type="bool">
              If true, include query string parameters in the cache key according to `includedQueryParameters` and `excludedQueryParameters`. If neither is set, the entire query string will be included. If false, the query string will be excluded from the cache key entirely. Note: This field applies to routes that use backend services. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. For routes that point to a Backend Bucket, use `includedQueryParameters` to define which parameters should be part of the cache key. Optional.
            </ResponseField>
            <ResponseField name="included_cookie_names" type="list(string)">
              Allows HTTP cookies (by name) to be used in the cache key. The name=value pair will be used in the cache key Cloud CDN generates. Note: This setting is only applicable to routes that use a Backend Service. It does not affect requests served by a Backend Bucket. Attempting to set it on a route that points exclusively to Backend Buckets will result in a configuration error. Up to 5 cookie names can be specified. Optional.
            </ResponseField>
            <ResponseField name="included_header_names" type="list(string)">
              Allows HTTP request headers (by name) to be used in the cache key. Optional.
            </ResponseField>
            <ResponseField name="included_query_parameters" type="list(string)">
              Names of query string parameters to include in cache keys. All other parameters will be excluded. Either specify `includedQueryParameters` or `excludedQueryParameters`, not both. '&' and '=' will be percent encoded and not treated as delimiters. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="cache_mode" type="string">
          Specifies the cache setting for all responses from this route. If not specified, Cloud CDN uses `CACHE_ALL_STATIC` mode. Optional.
        </ResponseField>
        <ResponseField name="client_ttl" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="default_ttl" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="max_ttl" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="negative_caching" type="bool">
          Negative caching allows per-status code TTLs to be set, in order to apply fine-grained caching for common errors or redirects. This can reduce the load on your origin and improve end-user experience by reducing response latency. When the `cacheMode` is set to `CACHE_ALL_STATIC` or `USE_ORIGIN_HEADERS`, negative caching applies to responses with the specified response code that lack any Cache-Control, Expires, or Pragma: no-cache directives. When the `cacheMode` is set to `FORCE_CACHE_ALL`, negative caching applies to all responses with the specified response code, and overrides any caching headers. By default, Cloud CDN applies the following TTLs to these HTTP status codes: * 300 (Multiple Choice), 301, 308 (Permanent Redirects): 10m * 404 (Not Found), 410 (Gone), 451 (Unavailable For Legal Reasons): 120s * 405 (Method Not Found), 501 (Not Implemented): 60s These defaults can be overridden in `negativeCachingPolicy`. If not specified, Cloud CDN applies negative caching by default. Optional.
        </ResponseField>
        <ResponseField name="negative_caching_policy" type="list(object)">
          Sets a cache TTL for the specified HTTP status code. `negativeCaching` must be enabled to configure `negativeCachingPolicy`. Omitting the policy and leaving `negativeCaching` enabled will use Cloud CDN's default cache TTLs. Note that when specifying an explicit `negativeCachingPolicy`, you should take care to specify a cache TTL for all response codes that you wish to cache. Cloud CDN will not apply any default negative caching when a policy exists. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="number">
              HTTP status code for which the negative caching policy applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ttl" type="object">
              The duration for which a negative cache entry is retained. This is a Duration object containing 'seconds' and 'nanos' fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanosecond portion of the TTL duration. Must be an integer between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of seconds for the negative caching policy's TTL (time-to-live) duration, expressed as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_coalescing" type="bool">
          If true then Cloud CDN will combine multiple concurrent cache fill requests into a small number of requests to the origin. If not specified, Cloud CDN applies request coalescing by default. Optional.
        </ResponseField>
        <ResponseField name="serve_while_stale" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cors_policy" type="object">
      The specification for allowing client-side cross-origin requests. For more information about the W3C recommendation for cross-origin resource sharing (CORS), see Fetch API Living Standard. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_credentials" type="bool">
          In response to a preflight request, setting this to true indicates that the actual request can include user credentials. This field translates to the Access-Control-Allow-Credentials header. Default is false. Optional.
        </ResponseField>
        <ResponseField name="allow_headers" type="list(string)">
          Specifies the content for the Access-Control-Allow-Headers header. Optional.
        </ResponseField>
        <ResponseField name="allow_methods" type="list(string)">
          Specifies the content for the Access-Control-Allow-Methods header. Optional.
        </ResponseField>
        <ResponseField name="allow_origin_regexes" type="list(string)">
          Specifies a regular expression that matches allowed origins. For more information, see regular expression syntax. An origin is allowed if it matches either an item inallowOrigins or an item inallowOriginRegexes. Regular expressions can only be used when the loadBalancingScheme is set to INTERNAL_SELF_MANAGED. Optional.
        </ResponseField>
        <ResponseField name="allow_origins" type="list(string)">
          Specifies the list of origins that is allowed to do CORS requests. An origin is allowed if it matches either an item inallowOrigins or an item inallowOriginRegexes. Optional.
        </ResponseField>
        <ResponseField name="disabled" type="bool">
          If true, disables the CORS policy. The default value is false, which indicates that the CORS policy is in effect. Optional.
        </ResponseField>
        <ResponseField name="expose_headers" type="list(string)">
          Specifies the content for the Access-Control-Expose-Headers header. Optional.
        </ResponseField>
        <ResponseField name="max_age" type="number">
          Specifies how long results of a preflight request can be cached in seconds. This field translates to the Access-Control-Max-Age header. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fault_injection_policy" type="object">
      The specification for fault injection introduced into traffic to test the resiliency of clients to backend service failure. As part of fault injection, when clients send requests to a backend service, delays can be introduced by the load balancer on a percentage of requests before sending those request to the backend service. Similarly requests from clients can be aborted by the load balancer for a percentage of requests. Optional.
      <Expandable title="properties">
        <ResponseField name="abort" type="object">
          Specification for how requests are aborted as part of fault injection. Optional.
          <Expandable title="properties">
            <ResponseField name="http_status" type="number">
              The HTTP status code used to abort the request. The value must be from 200 to 599 inclusive. For gRPC protocol, the gRPC status code is mapped to HTTP status code according to this mapping table. HTTP status 200 is mapped to gRPC status UNKNOWN. Injecting an OK status is currently not supported by Traffic Director. Optional.
            </ResponseField>
            <ResponseField name="percentage" type="number">
              The percentage of traffic for connections, operations, or requests that is aborted as part of fault injection. The value must be from 0.0 to 100.0 inclusive. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="delay" type="object">
          Specifies the delay introduced by the load balancer before forwarding the request to the backend service as part of fault injection. Optional.
          <Expandable title="properties">
            <ResponseField name="fixed_delay" type="object">
              A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="percentage" type="number">
              The percentage of traffic for connections, operations, or requests for which a delay is introduced as part of fault injection. The value must be from 0.0 to 100.0 inclusive. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_stream_duration" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="request_mirror_policy" type="object">
      A policy that specifies how requests intended for the route's backends are shadowed to a separate mirrored backend service. The load balancer doesn't wait for responses from the shadow service. Before sending traffic to the shadow service, the host or authority header is suffixed with-shadow. Optional.
      <Expandable title="properties">
        <ResponseField name="backend_service" type="string">
          The full or partial URL to the BackendService resource being mirrored to. The backend service configured for a mirroring policy must reference backends that are of the same type as the original backend service matched in the URL map. Serverless NEG backends are not currently supported as a mirrored backend service. Optional.
        </ResponseField>
        <ResponseField name="mirror_percent" type="number">
          The percentage of requests to be mirrored to `backend_service`. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="retry_policy" type="object">
      The retry policy associates with HttpRouteRule Optional.
      <Expandable title="properties">
        <ResponseField name="num_retries" type="number">
          Specifies the allowed number retries. This number must be &gt; 0. If not specified, defaults to 1. Optional.
        </ResponseField>
        <ResponseField name="per_try_timeout" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="retry_conditions" type="list(string)">
          Specifies one or more conditions when this retry policy applies. Valid values are: - 5xx: retry is attempted if the instance or endpoint responds with any 5xx response code, or if the instance or endpoint does not respond at all. For example, disconnects, reset, read timeout, connection failure, and refused streams. - gateway-error: Similar to 5xx, but only applies to response codes 502, 503 or504. - connect-failure: a retry is attempted on failures connecting to the instance or endpoint. For example, connection timeouts. - retriable-4xx: a retry is attempted if the instance or endpoint responds with a 4xx response code. The only error that you can retry is error code 409. - refused-stream: a retry is attempted if the instance or endpoint resets the stream with a REFUSED_STREAM error code. This reset type indicates that it is safe to retry. - cancelled: a retry is attempted if the gRPC status code in the response header is set to cancelled. - deadline-exceeded: a retry is attempted if the gRPC status code in the response header is set todeadline-exceeded. - internal: a retry is attempted if the gRPC status code in the response header is set tointernal. - resource-exhausted: a retry is attempted if the gRPC status code in the response header is set toresource-exhausted. - unavailable: a retry is attempted if the gRPC status code in the response header is set tounavailable. Only the following codes are supported when the URL map is bound to target gRPC proxy that has validateForProxyless field set to true. - cancelled - deadline-exceeded - internal - resource-exhausted - unavailable Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="timeout" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="url_rewrite" type="object">
      The spec for modifying the path before sending the request to the matched backend service. Optional.
      <Expandable title="properties">
        <ResponseField name="host_rewrite" type="string">
          Before forwarding the request to the selected service, the request's host header is replaced with contents of hostRewrite. The value must be from 1 to 255 characters. Optional.
        </ResponseField>
        <ResponseField name="path_prefix_rewrite" type="string">
          Before forwarding the request to the selected backend service, the matching portion of the request's path is replaced bypathPrefixRewrite. The value must be from 1 to 1024 characters. Optional.
        </ResponseField>
        <ResponseField name="path_template_rewrite" type="string">
          If specified, the pattern rewrites the URL path (based on the :path header) using the HTTP template syntax. A corresponding path_template_match must be specified. Any template variables must exist in the path_template_match field. - -At least one variable must be specified in the path_template_match field - You can omit variables from the rewritten URL - The * and ** operators cannot be matched unless they have a corresponding variable name - e.g. &#123;format=*&#125; or &#123;var=**&#125;. For example, a path_template_match of /static/&#123;format=**&#125; could be rewritten as /static/content/&#123;format&#125; to prefix/content to the URL. Variables can also be re-ordered in a rewrite, so that /&#123;country&#125;/&#123;format&#125;/&#123;suffix=**&#125; can be rewritten as /content/&#123;format&#125;/&#123;country&#125;/&#123;suffix&#125;. At least one non-empty routeRules[].matchRules[].path_template_match is required. Only one of path_prefix_rewrite orpath_template_rewrite may be specified. Optional.
        </ResponseField>
        <ResponseField name="regex_rewrite" type="object">
          The spec for modifying the path using a regular expression. Optional.
          <Expandable title="properties">
            <ResponseField name="path_pattern" type="string">
              Required. The regular expression used to match against the URL path. It uses RE2 syntax with the following constraints: - Any single character operators - Groups are allowed to have only submatch operator inside - Groups are allowed only without any char repetition, e.g. .* - Any char repetition, e.g. .*, is only allowed to be used in a single regex together with: - Empty string operators - Other repetitions - Ranges - Repetitions of ranges - Ranges are only allowed to have: - Character range - Digits range - Symbols listed in characters allowed for ranges Optional.
            </ResponseField>
            <ResponseField name="path_substitution" type="string">
              Required. Required when path pattern is specified. Used to rewrite matching parts of the path. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="weighted_backend_services" type="list(object)">
      A list of weighted backend services to send traffic to when a route match occurs. The weights determine the fraction of traffic that flows to their corresponding backend service. If all traffic needs to go to a single backend service, there must be oneweightedBackendService with weight set to a non-zero number. After a backend service is identified and before forwarding the request to the backend service, advanced routing actions such as URL rewrites and header transformations are applied depending on additional settings specified in this HttpRouteAction. Optional.
      <Expandable title="properties">
        <ResponseField name="backend_service" type="string">
          The name of the backend service that receives traffic for this weighted backend service entry. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_action" type="object">
          Defines the header modifications to apply to requests and responses for the corresponding weighted backend service in the region URL map. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="request_headers_to_add" type="list(object)">
              The list of headers to be added to the request before it is forwarded to the backend service for this weighted backend service configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header to add to the request. Must be a valid header name, typically containing only letters, digits, and hyphens (e.g., 'X-Forwarded-For'). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value of the request header to add. This value is sent to the backend service along with the header name. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  If true, the header value replaces an existing header with the same name. If false, the header is added as an additional header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_headers_to_remove" type="list(string)">
              A list of HTTP request header names to remove from the request before forwarding to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="response_headers_to_add" type="list(object)">
              A list of response headers to add, with each item specifying a header name and value. These headers are added to HTTP responses sent to the client when the request is served by this weighted backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the HTTP response header to add to responses from the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value of the response header to add, paired with the specified header_name. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  If true, replaces an existing response header with the same name; if false, appends the new header value to any existing values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="response_headers_to_remove" type="list(string)">
              Specifies a list of response header names to remove before sending the response back to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="weight" type="number">
          Specifies the relative weight for this backend service. Traffic is distributed proportionally to the weights of all backend services in the weighted_backend_services list. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_service" type="string">
  The full or partial URL of the defaultService resource to which traffic is directed if none of the hostRules match. If defaultRouteAction is also specified, advanced routing actions, such as URL rewrites, take effect before sending the request to the backend. Only one of defaultUrlRedirect, defaultService or defaultRouteAction.weightedBackendService can be set. defaultService has no effect when the URL map is bound to a target gRPC proxy that has the validateForProxyless field set to true. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="default_url_redirect" type="object">
  Specifies settings for an HTTP redirect. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="host_redirect" type="string">
      The host that is used in the redirect response instead of the one that was supplied in the request. The value must be from 1 to 255 characters. Optional.
    </ResponseField>
    <ResponseField name="https_redirect" type="bool">
      If set to true, the URL scheme in the redirected request is set to HTTPS. If set to false, the URL scheme of the redirected request remains the same as that of the request. This must only be set for URL maps used inTargetHttpProxys. Setting this true forTargetHttpsProxy is not permitted. The default is set to false. Optional.
    </ResponseField>
    <ResponseField name="path_redirect" type="string">
      The path that is used in the redirect response instead of the one that was supplied in the request. pathRedirect cannot be supplied together withprefixRedirect. Supply one alone or neither. If neither is supplied, the path of the original request is used for the redirect. The value must be from 1 to 1024 characters. Optional.
    </ResponseField>
    <ResponseField name="prefix_redirect" type="string">
      The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch, retaining the remaining portion of the URL before redirecting the request. prefixRedirect cannot be supplied together withpathRedirect. Supply one alone or neither. If neither is supplied, the path of the original request is used for the redirect. The value must be from 1 to 1024 characters. Optional.
    </ResponseField>
    <ResponseField name="redirect_response_code" type="string">
      The HTTP Status code to use for this RedirectAction. Supported values are: - MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301. - FOUND, which corresponds to 302. - SEE_OTHER which corresponds to 303. - TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method is retained. - PERMANENT_REDIRECT, which corresponds to 308. In this case, the request method is retained. Optional.
    </ResponseField>
    <ResponseField name="strip_query" type="bool">
      If set to true, any accompanying query portion of the original URL is removed before redirecting the request. If set to false, the query portion of the original URL is retained. The default is set to false. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. A hash of the contents stored in this object. This field is used in optimistic locking. This field is ignored when inserting a UrlMap. An up-to-date fingerprint must be provided in order to update the UrlMap, otherwise the request will fail with error 412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a UrlMap. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="header_action" type="object">
  The request and response header transformations that take effect before the request is passed along to the selected backendService. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="request_headers_to_add" type="list(object)">
      Headers to add to a matching request before forwarding the request to thebackendService. Optional.
      <Expandable title="properties">
        <ResponseField name="header_name" type="string">
          The name of the request header to add to the outgoing request. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_value" type="string">
          The value of the header to be added to requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="replace" type="bool">
          If false, the header value is appended to any existing values for the same header. If true, the header value replaces any existing values for that header. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="request_headers_to_remove" type="list(string)">
      A list of header names for headers that need to be removed from the request before forwarding the request to the backendService. Optional.
    </ResponseField>
    <ResponseField name="response_headers_to_add" type="list(object)">
      Headers to add the response before sending the response back to the client. Optional.
      <Expandable title="properties">
        <ResponseField name="header_name" type="string">
          The name of the response header to add, as specified in the response_headers_to_add header action of the URL map. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_value" type="string">
          The value of the response header to add. This value is paired with the header_name in the same response_headers_to_add block to define a custom header for responses. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="replace" type="bool">
          Indicates whether to replace the existing response header with the provided value. If set to false, the header is added only if it is not already present. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="response_headers_to_remove" type="list(string)">
      A list of header names for headers that need to be removed from the response before sending the response back to the client. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="host_rules" type="list(object)">
  The list of host rules to use against the URL. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="hosts" type="list(string)">
      The list of host patterns to match. The request's Host header must match one of these patterns (e.g., 'example.com' or '*.example.com') for this host rule to apply. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path_matcher" type="string">
      The name of the path matcher to use for this host rule. This must match the name of a path matcher defined in the region URL map's path_matchers list. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#urlMaps for url maps. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="path_matchers" type="list(object)">
  The list of named PathMatchers to use against the URL. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="default_custom_error_response_policy" type="object">
      Defines the default custom error response policy for this path matcher, controlling how errors from backend services are customized, such as serving a custom error page or redirecting. If this policy is not set, the URL map's default custom error response policy is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="error_response_rules" type="list(object)">
          A list of error response rules that define custom error handling for specific HTTP error codes. Each rule specifies which response codes to match and the path to the custom error page. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="match_response_codes" type="list(string)">
              A list of HTTP response codes that this error response rule should match. When a backend returns any of these codes, the specified custom error response is served. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="override_response_code" type="number">
              The HTTP status code to return to the client in place of the original error code. If not specified, the original error code is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path" type="string">
              The request path to match for this error response rule. Must start with a forward slash (/). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="error_service" type="string">
          A reference to a backend service or Cloud Storage bucket that serves the custom error response for this path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_route_action" type="object">
      The default route action for the path matcher, applied when no path rule matches the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="cache_policy" type="object">
          The cache policy for the route action, used to configure Cloud CDN cache key generation for requests matched by this path matcher. It defines which parts of the request (host, protocol, query string, headers) are included in the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cache_bypass_request_header_names" type="list(string)">
              List of HTTP request header names that, when present in a request, cause the cache to be bypassed for that request. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="cache_key_policy" type="object">
              Configures the cache key for this route's cache policy, controlling which request attributes are used to form the cache key and thus determine cache hits. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="excluded_query_parameters" type="list(string)">
                  A list of query parameters to exclude from the cache key. Requests that differ only in these parameters will share the same cached response, improving the cache hit ratio. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="include_host" type="bool">
                  Specifies whether to include the Host header in the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="include_protocol" type="bool">
                  Specifies whether to include the protocol (HTTP or HTTPS) in the cache key. If set to true, requests with different protocols are cached separately; if false, they share the same cache entry. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="include_query_string" type="bool">
                  Whether to include the query string in the cache key. If set to false, the query string is excluded, which can improve cache hit rates for URLs that differ only by query parameters. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="included_cookie_names" type="list(string)">
                  A list of cookie names to include in the cache key. Cookies not listed here are excluded from the cache key, allowing you to vary cached responses based on specific cookie values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="included_header_names" type="list(string)">
                  A list of request header names to include in the cache key. Requests with different values for these headers will be cached as separate entries. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="included_query_parameters" type="list(string)">
                  Specifies the list of query parameter names to include in the cache key. If empty, all query parameters are included. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cache_mode" type="string">
              Specifies how responses are cached. Possible values are `CACHE_ALL_STATIC` (caches static content), `FORCE_CACHE_ALL` (caches all responses regardless of headers), and `USE_ORIGIN_HEADERS` (caches according to the origin's Cache-Control headers). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="client_ttl" type="object">
              The time-to-live (TTL) for cached responses served to clients. This specifies how long a cached response remains valid for client requests before it must be revalidated. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional second component of the client TTL, specified in nanoseconds. Used together with the 'seconds' field to define the cache duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds component of the client TTL duration, specifying how long a response is considered fresh in the cache. This is used together with the nanos field to define the full duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="default_ttl" type="object">
              The default time-to-live (TTL) for cached responses in this cache policy, used when no per-key or per-route TTL is configured. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanoseconds component of the TTL duration. Must be between 0 and 999,999,999, representing the sub-second part of the duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of seconds for the default time-to-live (TTL) of the cache policy. This value is provided as a string and determines how long cached content is served before revalidation. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_ttl" type="object">
              The maximum time-to-live (TTL) for cached responses, expressed as a Duration object with `seconds` and `nanos` fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional seconds component of the maximum TTL, specified in nanoseconds. This value supplements the 'seconds' field to form the full duration, and must be between 0 and 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of seconds for the maximum TTL duration in the cache policy. The value is provided as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="negative_caching" type="bool">
              When true, enables negative caching, causing responses with status codes 404, 405, 410, and 421 to be cached (unless overridden by Cache-Control headers from the origin). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="negative_caching_policy" type="list(object)">
              A list of negative caching policies for the cache policy. Each object in the list defines an HTTP status code and the corresponding time-to-live (TTL) for caching responses with that status code. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="number">
                  The HTTP status code to which this negative caching policy applies. Typically used for status codes like 404 and 500. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ttl" type="object">
                  The time-to-live (TTL) for the negative cache entry, specified as a duration object with 'seconds' and 'nanos' fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional seconds component of the TTL duration, expressed in nanoseconds. Valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the TTL (as a string) for the negative caching policy, representing the duration in whole seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_coalescing" type="bool">
              If true, requests to the backend will be coalesced together when the cache is enabled, reducing the number of requests sent to the backend. Defaults to true. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="serve_while_stale" type="object">
              This block configures serving stale content while revalidating the cache with the origin. It can specify how long stale responses are served and how often to revalidate. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  Specifies the fractional seconds, in nanoseconds, for the serve-while-stale duration. Used together with the seconds field to define the total duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds component of the serve-while-stale duration, encoded as a string (e.g., '3600' for one hour). This is part of a protobuf Duration, where the total duration is the sum of this seconds value and any nanos component. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="cors_policy" type="object">
          The Cross-Origin Resource Sharing (CORS) policy for this route action. It specifies allowed origins, methods, headers, and credentials for cross-origin requests, or disables CORS if not configured. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="allow_credentials" type="bool">
              Specifies whether the browser is allowed to send credentials (such as cookies and HTTP authentication) with cross-origin requests. When set to true, the Access-Control-Allow-Credentials response header is set. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_headers" type="list(string)">
              Specifies the list of HTTP headers that the origin is allowed to include in CORS requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_methods" type="list(string)">
              The list of HTTP methods that are allowed for cross-origin requests. This field configures which methods (e.g., GET, POST, PUT, DELETE) are permitted by the CORS policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_origin_regexes" type="list(string)">
              A list of regular expressions that match origins allowed to make CORS requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_origins" type="list(string)">
              Specifies the list of origins that are allowed to make cross-origin requests. An origin is a combination of scheme, host, and port. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disabled" type="bool">
              Whether the CORS policy is disabled. When true, the CORS policy is not applied to requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="expose_headers" type="list(string)">
              Specifies the response headers that are exposed to the client in a CORS response. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="max_age" type="number">
              Specifies how long (in seconds) the results of a preflight request can be cached in the CORS policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="fault_injection_policy" type="object">
          The fault injection policy for this route action, used to test service resilience by introducing delays or aborting a subset of requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="abort" type="object">
              The specification for how client requests are aborted as part of fault injection. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="http_status" type="number">
                  The HTTP status code used to abort the request. Must be between 200 and 599 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="percentage" type="number">
                  The percentage of requests to abort, expressed as a number from 0 to 100. For example, a value of 10 aborts 10% of requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="delay" type="object">
              Defines the delay fault injection policy. Can set a fixed delay duration and the percentage of traffic to which the delay is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="fixed_delay" type="object">
                  Specifies a fixed delay for fault injection. This object typically contains the delay duration and the percentage of requests to which the delay applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional seconds component of the delay, in nanoseconds. Must be between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="percentage" type="number">
                  The percentage of traffic (between 0.0 and 1.0) to which the delay fault is applied. For example, 0.5 delays 50% of requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="max_stream_duration" type="object">
          The maximum duration a stream (connection) may remain open before being terminated, specified as an object with 'seconds' and 'nanos'. This is part of the route action for a path matcher in a regional URL map; if unset, the stream duration is unlimited. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              The number of nanoseconds (fractional seconds) in the max stream duration. Valid values are from 0 to 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of seconds. Must be from 0 to 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_mirror_policy" type="object">
          The request mirroring policy for the default route action. Specifies a backend service to which mirrored requests are sent. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="backend_service" type="string">
              The backend service resource to which mirrored traffic is sent. This can be a full or partial URL to a BackendService resource, e.g., 'projects/&#123;&#123;project&#125;&#125;/global/backendServices/&#123;&#123;name&#125;&#125;'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="mirror_percent" type="number">
              The percentage of requests to be mirrored to the mirror backend service. Valid values are from 0 to 100, where 0 means no requests are mirrored and 100 means all requests are mirrored. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="retry_policy" type="object">
          Specifies the retry policy for the default route action, including the number of retries, per-try timeout, and retry conditions. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="num_retries" type="number">
              Specifies the number of retries to attempt. Must be greater than 0; if not specified, defaults to 1. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="per_try_timeout" type="object">
              Specifies the timeout for each retry attempt. This is a Duration object that includes 'seconds' and 'nanos' fields to define the length of time. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanosecond component of the per-try timeout duration, used together with the seconds field. Must be between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds portion of the per-try timeout duration for the retry policy. The value is specified as a string, e.g., '30' for 30 seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="retry_conditions" type="list(string)">
              Optional; if omitted, computed by GCP.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="timeout" type="object">
          The timeout for this route action, specified as a Duration object with 'seconds' and 'nanos' fields, defining how long the backend service should wait before considering the request timed out. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              The fractional seconds component of the timeout duration, in nanoseconds. Must be between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="seconds" type="string">
              The number of seconds for the timeout duration, specified as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="url_rewrite" type="object">
          Configures the URL rewrite settings for the default route action of the path matcher, allowing modifications to the host header and path prefix before the request is forwarded. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="host_rewrite" type="string">
              The value to rewrite the Host header to. If specified, the host header is rewritten to this value before the request is forwarded to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_prefix_rewrite" type="string">
              Specifies the value to substitute for the matched path prefix in the request URL. The original matched path prefix is replaced with this string. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_template_rewrite" type="string">
              Specifies a template for rewriting the URL path. The template can include variables in curly braces that capture parts of the original request path, for example /products/&#123;product&#125;. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="regex_rewrite" type="object">
              A regex rewrite rule that rewrites the URL path based on a regular expression match. Typically contains a `regex` pattern and a `substitution` string to replace the matched portion of the path. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="path_pattern" type="string">
                  Optional; if omitted, computed by GCP.
                </ResponseField>
                <ResponseField name="path_substitution" type="string">
                  The substitution text to replace the matched path pattern. Capture groups from the path_pattern can be referenced with backslash-number syntax (e.g., \1, \2). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="weighted_backend_services" type="list(object)">
          Specifies a list of weighted backend services that determine how traffic is distributed among them. Each item includes a backend service and a weight, where the weight controls the proportion of traffic sent to that service. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="backend_service" type="string">
              The name or self_link of the backend service that this weighted backend service entry routes traffic to. This is the target service for the weighted distribution. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_action" type="object">
              Configures the header modifications for this weighted backend service. This block allows adding, overwriting, or removing request and response headers for traffic routed to the backend. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="request_headers_to_add" type="list(object)">
                  Specifies a list of request headers to add to the request before forwarding to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="header_name" type="string">
                      The name of the HTTP request header to add to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="header_value" type="string">
                      The value of the header to add to requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="replace" type="bool">
                      If true, the header will replace an existing header with the same name. If false, the value is appended to the existing header value, if the header already exists. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="request_headers_to_remove" type="list(string)">
                  A list of HTTP header names to remove from the request before it is forwarded to the backend. Removing headers prevents them from reaching the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="response_headers_to_add" type="list(object)">
                  A list of response headers to add to the response. Each header is defined by a name and value that will be appended to the backend's response before it is sent to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="header_name" type="string">
                      The name of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="header_value" type="string">
                      The value of the response header to add. This header is added to the response when the corresponding weighted backend service is selected. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="replace" type="bool">
                      If true, the header value replaces any existing header values with the same name; if false, it appends to existing values. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="response_headers_to_remove" type="list(string)">
                  Specifies a list of response header names to remove from the response before it is returned to the client. This is part of the headerAction configuration for the weighted backend services. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weight" type="number">
              Specifies the fraction of traffic sent to this backend service, computed as weight / (sum of all weights in the weighted backend service group). Must be between 0 and 1000. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_service" type="string">
      The default BackendService that receives traffic for this path matcher when the request path does not match any pathRule. Specify the full or partial URL to a BackendService resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="default_url_redirect" type="object">
      Default redirect configuration for requests that don't match any path rules in this path matcher. When set, unmatched requests are redirected according to this object instead of being forwarded to a default service. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="host_redirect" type="string">
          The host that will be used in the redirect response instead of the one that was supplied in the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="https_redirect" type="bool">
          If set to true, the URL redirect uses the HTTPS protocol, redirecting clients to the HTTPS version of the requested URL. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="path_redirect" type="string">
          The path to which the request will be redirected. This value replaces the original request path in the redirect response. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="prefix_redirect" type="string">
          The prefix used to replace the matched path prefix in the redirect target URL. When set, the redirected URL's path starts with this prefix instead of the original matched prefix. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="redirect_response_code" type="string">
          Specifies the HTTP redirect response code to use for the default URL redirect. Valid values are FOUND (302), MOVED_PERMANENTLY_DEFAULT (301), PERMANENT_REDIRECT (308), SEE_OTHER (303), and TEMPORARY_REDIRECT (307). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="strip_query" type="bool">
          Whether to strip the query string from the original URL before performing the redirect. If set to true, the query portion is removed; if set to false or omitted, the query portion is retained. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      An optional description of the path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="header_action" type="object">
      Configures the request header modifications (additions or removals) applied to requests that match this path matcher, before they are forwarded to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="request_headers_to_add" type="list(object)">
          A list of request headers to add to requests that match this path matcher. Each object in the list specifies a header name and value to be added. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="header_name" type="string">
              The name of the request header to add to the request. For example, X-Custom-Header. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_value" type="string">
              The value of the request header to add to the matched request. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replace" type="bool">
              Whether to replace or append the header value. If true, the header value is replaced; if false, the value is appended. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_headers_to_remove" type="list(string)">
          A list of HTTP header names to remove from the request before it is forwarded to the backend service. The header action for a path matcher allows removing specific request headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="response_headers_to_add" type="list(object)">
          A list of response header name/value pairs to add to the response. Each object contains `header_name` and `header_value` fields, specifying the header to add and its value. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="header_name" type="string">
              The name of the HTTP response header to add. Must be a valid header name, as specified in RFC 7230. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_value" type="string">
              The value of the response header to be added. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replace" type="bool">
              Whether to replace the value of an existing response header with the same name. If set to true, the new value overwrites the existing header; otherwise, the header is appended as an additional header. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="response_headers_to_remove" type="list(string)">
          Specifies the list of response header names to remove. These headers are stripped from the HTTP response before it is returned to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path_rules" type="list(object)">
      Path rules within a path matcher. Each rule contains a list of paths and the corresponding service (backend) to which traffic matching those paths should be routed. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="custom_error_response_policy" type="object">
          The custom error response policy for this path rule. It defines how to customize responses for specific HTTP error codes, such as the error page content and response code to serve. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_response_rules" type="list(object)">
              List of rules that define custom error responses. Each rule specifies which response codes (or ranges) trigger a custom error page, and the path to that page. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="match_response_codes" type="list(string)">
                  Specifies a list of HTTP status codes (e.g., '404', '500') that should trigger this error response rule. When the backend returns any of these response codes, the rule's custom error response is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="override_response_code" type="number">
                  The HTTP status code to return to the client when this error response rule is triggered. Must be an integer between 100 and 599 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path" type="string">
                  The full path (starting with '/') to the custom error page, relative to the backend service or backend bucket serving the original request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="error_service" type="string">
              The full or partial URL of a backend bucket that serves custom error responses. If not specified, the default error behavior applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="paths" type="list(string)">
          List of path patterns to match against the incoming request path. Each path can contain a single trailing * wildcard. If the request path matches any of these patterns, the specified route action is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="route_action" type="object">
          Specifies the route action to take for requests that match this path rule, such as forwarding to a backend service, configuring weighted backend services, URL rewrite, or setting timeouts. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cache_policy" type="object">
              Configures the cache key policy for this route. It determines which parts of a request (such as query parameters, headers, and cookies) are used in the cache key for CDN caching. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="cache_bypass_request_header_names" type="list(string)">
                  The list of request header names that will cause the request to bypass the cache if present in the request. When a request contains any of these headers, the cache is bypassed regardless of other cache policies. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="cache_key_policy" type="object">
                  Specifies the cache key parameters for the cache policy, including which parts of the request (host, protocol, query string) to include in the cache key, and how to handle query string parameters. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="excluded_query_parameters" type="list(string)">
                      Specifies a list of query parameters to exclude from the cache key. When include_query_string is true, these parameters are omitted when generating the cache key; ignored if include_query_string is false. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_host" type="bool">
                      Specifies whether to include the HTTP Host header in the cache key. When true, requests with different Host headers are cached separately. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_protocol" type="bool">
                      If true, HTTP and HTTPS requests will be served with the same response, meaning the protocol is not included in the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_query_string" type="bool">
                      Specifies whether the query string should be included in the cache key. If false, the query string is excluded from the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_cookie_names" type="list(string)">
                      The list of cookie names to include in the cache key. If empty, no cookies are included in the cache key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_header_names" type="list(string)">
                      A list of HTTP header names to include in the cache key. The values of these headers will be incorporated into the cache key, affecting cache hits. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_query_parameters" type="list(string)">
                      Specifies a list of query string parameters to include in the cache key. When non-empty, only these parameters are used to construct the cache key; all other query parameters are ignored. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cache_mode" type="string">
                  The cache mode determines what type of responses are cached. CACHE_ALL_STATIC automatically caches static content, FORCE_CACHE_ALL caches all content regardless of origin headers, and USE_ORIGIN_HEADERS relies on the origin's Cache-Control headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="client_ttl" type="object">
                  Specifies the time-to-live (TTL) for cached responses in the client's cache, determining how long a client can serve a response before revalidating with the origin. This is configured within the cache policy of a URL map route action. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional part of the duration in nanoseconds, in the range 0 to 999,999,999. This represents the sub-second component of the client TTL cache policy duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      Optional; if omitted, computed by GCP.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="default_ttl" type="object">
                  The default time-to-live (TTL) for cached responses, represented as a duration object. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional second component of the default TTL, in nanoseconds. This is combined with the seconds field to specify the time-to-live for the cache policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The whole seconds portion of the default TTL duration for the cache policy. Provided as a string representation of an integer number of seconds (e.g., '30'). Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="max_ttl" type="object">
                  Optional; if omitted, computed by GCP.
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond component of the maximum TTL duration for cached responses. Valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the maximum TTL duration for cached responses. Must be a non-negative integer represented as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="negative_caching" type="bool">
                  Enables negative caching, which caches HTTP error responses (e.g., 404) for a short duration. Defaults to false when not specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="negative_caching_policy" type="list(object)">
                  Defines caching policies for error responses (negative caching). Each entry specifies an HTTP status code and a TTL (in seconds) for which responses with that code are cached. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="number">
                      The HTTP status code (e.g., 404, 301) for which negative caching is enabled. This code determines which responses are cached according to the negative caching policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="ttl" type="object">
                      The time-to-live (TTL) for caching a negative response for the specified status code. As a Duration object, it specifies how long the negative cache entry remains valid, with a valid range of 0 to 3600 seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The fractional seconds component of the TTL, in nanoseconds. The valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          The seconds component of the TTL duration for negative caching. The negative cache entry expires after this many seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="request_coalescing" type="bool">
                  If set to true (default), enables request coalescing for the cache policy. When a cached object goes stale and multiple requests for it arrive, Cloud CDN coalesces them into a single request to the origin, reducing backend load. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="serve_while_stale" type="object">
                  Specifies how long the CDN can serve a stale cached response while attempting to revalidate the content with the origin server. Used in the cache_policy of path rules for a regional URL map. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond component of the serve_while_stale duration. Together with the seconds field, it specifies the length of time to serve stale content. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The duration in seconds for serving stale content when the origin is unavailable, expressed as a string. This is part of the CDN cache policy's serve_while_stale setting. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cors_policy" type="object">
              The CORS (Cross-Origin Resource Sharing) policy for this route action, specifying how the backend responds to cross-origin requests. This object defines allowed origins, methods, headers, and whether credentials are allowed. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allow_credentials" type="bool">
                  When set to true, the CORS policy allows credentials (e.g., cookies, authorization headers) in cross-origin requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_headers" type="list(string)">
                  The list of HTTP headers that are allowed in cross-origin requests. This corresponds to the Access-Control-Allow-Headers header in the CORS response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_methods" type="list(string)">
                  Specifies the list of HTTP methods that are allowed for cross-origin requests in the CORS policy. For example, GET, POST, PUT, DELETE, etc. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origin_regexes" type="list(string)">
                  A list of origin regexes that are allowed to make CORS requests. Cross-origin requests from origins matching any of these regular expressions are permitted. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origins" type="list(string)">
                  Specifies the list of origins allowed to make cross-origin requests. Origins are specified in the format scheme://host:port. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="disabled" type="bool">
                  If set to true, the CORS policy is disabled and CORS responses are not applied. The default is false, meaning CORS is enabled. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="expose_headers" type="list(string)">
                  Specifies the response headers that the browser can access. These headers are exposed via the Access-Control-Expose-Headers header in CORS responses. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_age" type="number">
                  Specifies the maximum age in seconds that the results of a preflight request can be cached by the client. This is used in the Access-Control-Max-Age header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="fault_injection_policy" type="object">
              Configures the fault injection policy for this route action, allowing the introduction of delays or aborts to simulate failures for testing resilience. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="abort" type="object">
                  The specification for how client requests are aborted as part of fault injection. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="http_status" type="number">
                      The HTTP status code to use when aborting the request. Must be an integer between 100 and 599. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      The percentage of requests to abort, as a number between 0 and 100. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="delay" type="object">
                  Configuration for delaying requests as part of fault injection. This block defines a fixed delay duration and the percentage of requests that will experience the delay. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fixed_delay" type="object">
                      Specifies the fixed delay duration to be injected before forwarding the request. The delay is defined as a Duration object with seconds and nanos fields, allowing precise timing. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The fractional part of the fixed delay duration in nanoseconds, used together with the 'seconds' field to specify the total delay amount. Valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          The number of seconds for the fixed delay. Together with the nanos field, this defines the total delay duration for fault injection. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      The percentage of requests for which the delay is injected. Must be a value between 0 and 100. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_stream_duration" type="object">
              Settings for the maximum duration of a stream for this route action. This is a duration object, typically containing seconds and nanos fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional part of the maximum stream duration, specified in nanoseconds (0-999,999,999). Combined with the seconds field to express the total duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Specifies the seconds portion of the max stream duration. This is a string field, representing an integer number of seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_mirror_policy" type="object">
              This block configures request mirroring for the route action, allowing incoming traffic to be copied to a separate backend service for observation or analysis. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The backend service (selfLink or URL) that requests are mirrored to. This is a reference to a compute backend service resource in the same region as the URL map. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="mirror_percent" type="number">
                  The percentage of requests that will be mirrored to the mirror backend. Must be a value between 0 and 100. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="retry_policy" type="object">
              The retry policy configuration for this route action, specifying how requests are retried, including conditions and try timeouts. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="num_retries" type="number">
                  The number of retries to attempt before giving up. Must be an integer greater than 0. If not specified, the default is 1. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="per_try_timeout" type="object">
                  The timeout for each retry attempt, specified as a Duration object. This defines the maximum time to wait for an individual attempt to complete before it is considered failed and the next retry is triggered. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional seconds component of the timeout duration, in nanoseconds. Valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the per-try timeout duration. Specifies the whole-second portion of the timeout, typically combined with a nanos field for sub-second precision. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="retry_conditions" type="list(string)">
                  Specifies the retry conditions under which the request is retried. Empty list means no retries. Allowed values include '5xx', 'gateway-error', 'connect-failure', 'refused-stream', 'canceled', 'deadline-exceeded', 'internal', 'reset', 'resource-exhausted', 'unavailable'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="timeout" type="object">
              The timeout for requests routed by this path rule. This object represents a Duration, containing 'seconds' and 'nanos' fields, and specifies how long to wait for a backend response before considering the request timed out. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The nanosecond component of the timeout duration, representing the fractional seconds part of the span. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Specifies the duration of the timeout in seconds. The value must be a string representation of a non-negative integer from 0 to 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="url_rewrite" type="object">
              The url_rewrite block configures rewriting of the request URL for matched requests. It can set a new host header and path prefix that will be used when forwarding the request to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="host_rewrite" type="string">
                  Specifies the value of the Host header in the rewritten URL. This can be used to rewrite the hostname of the request before forwarding to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_prefix_rewrite" type="string">
                  Specifies the string that replaces the matched URL path prefix in the request before it is forwarded to the backend service. For instance, if the path rule matches a prefix like /old, setting this to /new rewrites the URL path from /old/... to /new/... while preserving the remainder of the path. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_template_rewrite" type="string">
                  The path template rewrite to apply to the request path. This can use variables like &#123;variable&#125; to reference captured path segments. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="regex_rewrite" type="object">
                  Defines a regular expression-based URL path rewrite. This object includes a pattern to match against the path and a substitution string to replace it. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path_pattern" type="string">
                      The regular expression (RE2) pattern to match against the URL path. This pattern defines the portions of the path that can be captured and referenced in the rewrite template. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path_substitution" type="string">
                      The substitution string that replaces the portion of the URL path matched by the `regex` pattern in this regex rewrite rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weighted_backend_services" type="list(object)">
              A list of weighted backend services that will be used to distribute traffic according to assigned weights. Each entry specifies a backend service and a weight; traffic is routed proportionally to the weights across the listed services. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The backend service to which traffic is routed for this weighted backend service entry. This can be specified as the full URL, partial URL, or name of the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_action" type="object">
                  Defines header actions to apply to requests and responses for this weighted backend service, including adding or removing headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="request_headers_to_add" type="list(object)">
                      A list of request headers to be added to the request before forwarding it to the chosen backend service. Each object in the list specifies a header name and a header value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the request header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the request header to add. This value replaces or appends to the existing header value for the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          Determines whether the existing header value is replaced (true) or the new value is appended (false) when adding a request header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="request_headers_to_remove" type="list(string)">
                      A list of request header names to remove before the request is forwarded to the backend service for this weighted backend service. Each item is a string header name. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="response_headers_to_add" type="list(object)">
                      A list of headers to add to the response before it is sent back to the client. Each object in the list specifies a header name and value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          Indicates whether to replace an existing response header with the specified value. If true, the header is replaced; if false, the value is added, potentially resulting in duplicate headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="response_headers_to_remove" type="list(string)">
                      List of response header names to remove from the response sent to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="weight" type="number">
                  The weight of this backend service. The weight determines the fraction of traffic that goes to this backend service, computed as weight divided by the sum of all weights in the weighted backend services list. Must be an integer between 0 and 1000. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service" type="string">
          The backend service or backend bucket to use when the path matches this rule. Specify the self_link or name of a regional backend service or backend bucket. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url_redirect" type="object">
          Specifies a redirect for requests that match this path rule. When set, matched requests are redirected according to the configured redirect behavior instead of being forwarded to a backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="host_redirect" type="string">
              The host that will be used in the redirect response instead of the one that was supplied in the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="https_redirect" type="bool">
              If set to true, the URL scheme in the redirected request is set to https. If set to false, the scheme remains unchanged. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_redirect" type="string">
              The path portion of the URL to redirect to. If set, the original request path is replaced with this value when the redirect is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="prefix_redirect" type="string">
              The prefix to use in the redirect URL, replacing the matched prefix of the original request path. For example, if the path rule matches the prefix '/old' and prefix_redirect is set to '/new', a request to '/old/foo' redirects to '/new/foo'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="redirect_response_code" type="string">
              The HTTP redirect status code. Valid values are FOUND (302), MOVED_PERMANENTLY_DEFAULT (301), PERMANENT_REDIRECT (308), SEE_OTHER (303), and TEMPORARY_REDIRECT (307). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="strip_query" type="bool">
              Indicates whether the query string of the original request is preserved or removed during the redirect. When set to true, the query string is stripped; when false (default), the query string is kept. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="route_rules" type="list(object)">
      List of route rules that define how requests are matched and routed to backend services within the path matcher. Each route rule specifies match criteria and a corresponding service. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="custom_error_response_policy" type="object">
          The custom_error_response_policy block configures custom error responses for this route rule, allowing you to define custom error pages or redirects for specific HTTP error codes returned by the load balancer. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_response_rules" type="list(object)">
              A list of error response rules. Each rule specifies an error code (e.g., 404, 500) and the path to a custom error page that should be served when that error occurs. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="match_response_codes" type="list(string)">
                  A list of HTTP status codes (e.g., '404', '500') that this custom error response rule matches. When the backend returns one of these codes, the associated custom error page is served. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="override_response_code" type="number">
                  The HTTP status code that the original error response is overridden with. This is the code returned to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path" type="string">
                  The full path to the error response content, such as a Cloud Storage object path (e.g., gs://bucket/errors/404.html) that should be served when this error response rule matches. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="error_service" type="string">
              The full or partial URL of a BackendService or BackendBucket that will serve the custom error responses for this route rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          An optional, human-readable description of this route rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="header_action" type="object">
          Defines the header actions for this route rule, including adding and removing request and response headers. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="request_headers_to_add" type="list(object)">
              A list of objects representing additional HTTP headers to be added to the request when the route rule matches. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header to add to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value of the header to be added to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  Indicates whether to replace or append the header value. When true, the new value replaces any existing header with the same name; when false, the header is added as an additional header, preserving any existing ones. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_headers_to_remove" type="list(string)">
              Specifies a list of HTTP header names to remove from the request before forwarding. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="response_headers_to_add" type="list(object)">
              Specifies a list of response headers to add or override in the response sent to the client. Each object in the list defines a header name and header value. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="header_name" type="string">
                  The name of the response header to add to the response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_value" type="string">
                  The value of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replace" type="bool">
                  Indicates whether to replace an existing response header with the same name (true) or append the header value (false). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="response_headers_to_remove" type="list(string)">
              A list of response header names to remove. These headers are removed from the response before it is returned to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_rules" type="list(object)">
          List of match rules that define the conditions under which incoming requests are matched to this route rule. Each match rule can specify path matching criteria (such as full path, prefix, or regex), header and query parameter matches, and case sensitivity. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="full_path_match" type="string">
              Specifies the full request path that must exactly match for this match rule to apply. This is an exact match on the entire path, as opposed to prefix or regex matching. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header_matches" type="list(object)">
              Defines a list of HTTP header match conditions. The request must satisfy all specified header conditions for the match rule to be considered a match. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="exact_match" type="string">
                  Specifies the exact header value that must match for the request to satisfy the match condition. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header to match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="invert_match" type="bool">
                  If set to true, the match condition is inverted, meaning the request matches when the header value does NOT equal the specified value. For example, with invert_match true and a specified value of 'foo', the match succeeds only if the header value is not 'foo'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="prefix_match" type="string">
                  Specifies a prefix match for the header value. The header value must start with this prefix. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="present_match" type="bool">
                  When set to true, the match is successful only if the header is present. This is an alternative to using exact_match, prefix_match, suffix_match, and regex_match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_match" type="object">
                  Defines an inclusive numeric range that the header value must fall within to match. The object contains integer fields rangeStart and rangeEnd. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="range_end" type="string">
                      The inclusive end of the integer range used to match a header value. The header value must be less than or equal to this number to match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="range_start" type="string">
                      The start of the range (inclusive) to match for the header value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="regex_match" type="string">
                  The regular expression pattern that the value of the header must match for the match rule to apply. It is used to match the header value against a regular expression, as an alternative to exact matching. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="suffix_match" type="string">
                  The suffix that the header value must end with for the match to be considered successful. The match is case-sensitive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="ignore_case" type="bool">
              Indicates whether to ignore case when matching the request path. When set to true, the match is case-insensitive. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metadata_filters" type="list(object)">
              A list of metadata filters that requests must match. Each filter specifies a list of labels and a match criterion (MATCH_ALL or MATCH_ANY) to determine whether all or any labels must match. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="filter_labels" type="list(object)">
                  Specifies a list of metadata label filters, each containing a key (name) and value (value), used to match incoming request metadata for this routing rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The key of the metadata filter label. This specifies the label name to match against when filtering requests. For example, in a filter label 'env=prod', the name is 'env'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of the metadata filter label. This value is matched against the corresponding metadata header value (e.g., x-goog-meta- prefixed header) for the request, and the request is considered matched when the header value equals this value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="filter_match_criteria" type="string">
                  Specifies how the filter labels are combined: MATCH_ALL requires all specified labels to match, MATCH_ANY requires at least one to match, and NOT_SET indicates the field is not configured. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="path_template_match" type="string">
              Specifies a path template to match against the request path. The template can include variables in curly braces (e.g., /users/&#123;id&#125;) that match any value in that segment, enabling flexible path-based routing. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="prefix_match" type="string">
              Specifies the prefix of the request path to match. The path must begin with this prefix, and the match is case-sensitive. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="query_parameter_matches" type="list(object)">
              A list of query parameter match criteria for this match rule. Each entry specifies a query parameter name and value to match, with an optional exactMatch flag to control whether the match is exact or uses regular expressions. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="exact_match" type="string">
                  The exact value to match against the query parameter's value. The match rule is satisfied only when the query parameter value equals this string exactly. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the query parameter to match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="present_match" type="bool">
                  When set to true, the match requires the query parameter to be present in the request, regardless of its value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="regex_match" type="string">
                  The regular expression (RE2 syntax) that the query parameter value must match. This is mutually exclusive with exact_match. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="regex_match" type="string">
              A regular expression that the entire request path must match. This is used to determine if the route rule applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="priority" type="number">
          The priority for this route rule, as an integer from 0 to 2147483647. Lower values are evaluated first, and priorities must be unique within the path matcher. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="route_action" type="object">
          The action to be performed when a request matches this route rule, which may include URL rewriting, routing to weighted backend services, or other routing policies. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cache_policy" type="object">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="cache_bypass_request_header_names" type="list(string)">
                  Specifies the list of request header names that cause the cache to be bypassed for requests containing them. Such requests are forwarded to the origin rather than served from cache. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="cache_key_policy" type="object">
                  Defines the cache key policy for the cache policy, controlling how cache keys are generated (e.g., which query parameters, HTTP headers, or cookies are included). Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="excluded_query_parameters" type="list(string)">
                      A list of query parameter names to exclude from the cache key. Requests that differ only in these parameters will share the same cached response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_host" type="bool">
                      Whether to include the hostname in the cache key. If true, requests to different hosts are cached separately. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_protocol" type="bool">
                      If true, the request protocol (http or https) is included in the cache key, so cache entries are separated by protocol. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="include_query_string" type="bool">
                      Controls whether the query string from the request URI is included in the cache key. When set to false, the cache key ignores query string parameters. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_cookie_names" type="list(string)">
                      Specifies the names of cookies to include in the cache key. When set, only these cookies are used as part of the cache key; any other cookies are ignored. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_header_names" type="list(string)">
                      A list of HTTP header names to include in the cache key. Headers are case-insensitive and are used to construct the cache key for the cached response. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="included_query_parameters" type="list(string)">
                      Optional; if omitted, computed by GCP.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cache_mode" type="string">
                  Specifies the cache mode for the CDN. CACHE_ALL_STATIC caches static content automatically while respecting origin headers for dynamic content; FORCE_CACHE_ALL caches all content regardless of origin headers; USE_ORIGIN_HEADERS relies on the origin's Cache-Control headers for both static and dynamic content. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="client_ttl" type="object">
                  Defines the client-side TTL for cached responses, specified as a Duration object with seconds and nanos fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond component of the client TTL (time-to-live) duration, representing fractional seconds. Valid range is 0 to 999999999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The number of seconds in the TTL duration for the client cache. Must be a non-negative integer expressed as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="default_ttl" type="object">
                  The default time-to-live for cached responses in the CDN cache, applied when the response does not specify explicit cache directives. The value is a duration object. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The fractional seconds component of the TTL duration, in nanoseconds. Must be between 0 and 999,999,999 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The number of seconds in the default TTL duration, as a string. For example, '60' represents 60 seconds. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="max_ttl" type="object">
                  The maximum time for which the content can be cached. This is a Duration object with 'seconds' and 'nanos' fields. It must be greater than the default TTL if a default is set. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanosecond part of the maximum TTL duration, in the range 0-999,999,999. Used together with the seconds value to specify the total max_ttl duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The seconds component of the maximum TTL duration. This corresponds to the `seconds` field of a Google Cloud Duration object, representing signed seconds of the span of time. Must be from 0 to 315,576,000,000 inclusive, specified as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="negative_caching" type="bool">
                  Whether to cache responses that are considered errors (e.g., HTTP 404, 502). Setting this to true enables negative caching. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="negative_caching_policy" type="list(object)">
                  A list of negative caching policies that configure a cache TTL for specific HTTP status codes when negative caching is enabled. Each policy sets a TTL for a given HTTP status code. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="number">
                      The HTTP status code to define a negative caching TTL for. Valid values are 300, 301, 308, 404, 405, 410, 421, 451, and 501. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="ttl" type="object">
                      Optional; if omitted, computed by GCP.
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The nanosecond component of the TTL duration, ranging from 0 to 999999999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          The number of seconds in the TTL duration for the negative caching policy. Valid values range from 0 to 315576000000 (about 10 years), represented as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="request_coalescing" type="bool">
                  If true, requests for the same resource from different hosts are coalesced into a single cache entry, reducing the number of cache lookups. The default is false. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="serve_while_stale" type="object">
                  Defines the stale-while-revalidate behavior for the CDN cache policy, allowing stale content to be served while the cache is refreshed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The nanoseconds component of the serve-while-stale duration. Must be a non-negative integer between 0 and 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The number of seconds for which the CDN will continue to serve stale content while attempting to fetch a fresh response from the origin. Provided as a decimal string, e.g., "300". Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="cors_policy" type="object">
              The CORS (Cross-Origin Resource Sharing) policy for this route action. Configures how requests from different origins are handled, including allowed origins, methods, headers, and credentials. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allow_credentials" type="bool">
                  Specifies whether the response to the request can be exposed when the credentials flag is true. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_headers" type="list(string)">
                  The list of HTTP headers that the CORS policy allows. This corresponds to the Access-Control-Allow-Headers header in CORS responses, indicating which headers are permitted in the actual request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_methods" type="list(string)">
                  Specifies the list of HTTP methods that are allowed for CORS requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origin_regexes" type="list(string)">
                  Specifies a list of regular expressions that matchallowed origins for CORS requests. A request's Origin header is allowed if it matches any of the provided regexes. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allow_origins" type="list(string)">
                  Specifies the list of origins that are allowed to make cross-origin requests as part of the CORS policy for this route rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="disabled" type="bool">
                  Indicates whether the CORS policy is disabled. When set to true, the CORS policy is not applied to the route. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="expose_headers" type="list(string)">
                  A list of response headers that browsers are allowed to expose to the client. These headers appear in the Access-Control-Expose-Headers response header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_age" type="number">
                  The maximum time (in seconds) that the client can cache the response to a preflight request. Must be a non-negative integer. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="fault_injection_policy" type="object">
              The fault injection policy for this route action. It can be used to simulate failures by delaying or aborting requests, helping test the resilience of your backend services. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="abort" type="object">
                  Configures abort fault injection, returning a specified HTTP status code to a percentage of requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="http_status" type="number">
                      The HTTP status code to return when aborting the request, typically a 4xx or 5xx code to simulate an upstream error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      The percentage of traffic on which the abort fault is injected. Must be a number between 0.00 and 100.00 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="delay" type="object">
                  Configuration for delay fault injection, specifying the fixed delay amount (in Duration format) and the percentage of requests to which the delay is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fixed_delay" type="object">
                      The fixed_delay block specifies the fixed amount of delay to inject into requests, expressed as a duration with 'seconds' and 'nanoseconds' fields. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="nanos" type="number">
                          The fractional seconds component of the delay duration, in nanoseconds. Valid values are 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="seconds" type="string">
                          The whole seconds component of the fixed delay duration. For example, a 1.5-second delay uses seconds: "1" and nanos: "500000000". Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="percentage" type="number">
                      Percentage of requests that will get the delay fault injection. Must be between 0 and 100. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_stream_duration" type="object">
              A duration object specifying the maximum time a stream can remain open before being terminated. Contains 'seconds' and 'nanoseconds' fields. This value is ignored for non-streaming requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional seconds component of the max stream duration, expressed in nanoseconds. This value is combined with the seconds component to specify the total duration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The number of seconds in the max stream duration. Must be from 0 to 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="request_mirror_policy" type="object">
              Request mirroring policy for the route action. When set, incoming requests are mirrored to the backend service specified in the policy's `backend_service` field, while the original request proceeds normally, allowing traffic inspection without altering the response. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The regional backend service that will receive mirrored traffic. This field is required and must reference an existing backend service resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="mirror_percent" type="number">
                  The percentage of requests to mirror to the backend service, as a number from 0.0 to 100.0 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="retry_policy" type="object">
              The retry policy for the route rule's route action. It determines the number of retries, the timeout per retry attempt, and the conditions (such as HTTP status codes) under which a request is retried. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="num_retries" type="number">
                  Number of retry attempts to perform before the request is considered failed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="per_try_timeout" type="object">
                  The timeout for each retry attempt in the retry policy, specified as a Duration object (for example, seconds). This applies per individual attempt, separate from the overall request timeout. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="nanos" type="number">
                      The number of nanoseconds in the `per_try_timeout` duration. Must be an integer between 0 and 999,999,999, representing the fractional seconds part of the timeout. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="seconds" type="string">
                      The number of seconds (as a string) for the per-try timeout duration. This is part of the retry policy and sets the maximum time a single retry attempt is allowed to take before it is considered failed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="retry_conditions" type="list(string)">
                  A list of conditions that trigger a retry for the associated route, such as specific HTTP status codes (e.g., '5xx') or connection errors (e.g., 'connect-failure', 'reset'). These conditions are evaluated in the retry policy of the route action. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="timeout" type="object">
              Specifies the timeout for the request to the backend service. It is a duration defined in seconds and nanoseconds. If not set, the backend service's default timeout is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The fractional seconds component of the timeout duration, in nanoseconds. Valid range is 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The seconds component of the timeout duration for this route action. For example, '30' means 30 seconds. This is part of a duration that may also include a nanos component. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="url_rewrite" type="object">
              The URL rewrite configuration for this route action. It defines how to rewrite the request's host header and path prefix before forwarding to the target service. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="host_rewrite" type="string">
                  Specifies the value of the Host header that will be set in the request. If not specified, the original Host header is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_prefix_rewrite" type="string">
                  Specifies the path prefix to use when rewriting the URL before forwarding the request to the backend. If not set, the original path prefix is preserved. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path_template_rewrite" type="string">
                  Defines a template for rewriting the request URL path. Variables from the matched path template (using pathTemplateMatch) can be referenced in the rewrite template. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="regex_rewrite" type="object">
                  Specifies a regex rewrite rule for the URL rewrite action. This rule defines a regular expression pattern and a substitution string used to rewrite the URL path. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path_pattern" type="string">
                      The regular expression pattern to match against the request path. It must be a valid RE2 expression, between 1 and 1024 characters long, and is used to determine which portions of the URL path to rewrite. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path_substitution" type="string">
                      Substitution string for the path regex rewrite. Can reference capture groups from the match regex using backslash-number syntax (e.g., \1). Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weighted_backend_services" type="list(object)">
              A list of weighted backend services used to distribute traffic proportionally across multiple backend services. Each entry specifies a backend service and a weight; traffic is routed based on the relative weights. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_service" type="string">
                  The name or self_link of the regional backend service that this weighted entry routes traffic to. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_action" type="object">
                  The header action for this weighted backend service. This specifies custom headers to add, set, or remove on requests forwarded to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="request_headers_to_add" type="list(object)">
                      This block configures a list of request headers to add to the request before it is sent to the backend service. Each object contains 'header_name' and 'header_value' fields, specifying the header to add and its value. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the HTTP header to add to the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the request header to add. This pairs with the header name (set in a sibling field) to define a header that will be appended to requests routed to the backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          Optional; if omitted, computed by GCP.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="request_headers_to_remove" type="list(string)">
                      A list of header names to be removed from the request before forwarding to the backend service. These headers are stripped from the request when routed to the associated weighted backend service. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="response_headers_to_add" type="list(object)">
                      A list of response headers to add. Each entry specifies a header name, header value, and a boolean indicating whether to replace existing headers with the same name. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header_name" type="string">
                          The name of the HTTP response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="header_value" type="string">
                          The value of the response header to add. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="replace" type="bool">
                          If set to true, the added response header will replace an existing header with the same name; otherwise, the value is appended to the existing header. Optional; if omitted, computed by GCP. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="response_headers_to_remove" type="list(string)">
                      A list of response header names to remove. These headers are stripped from the response before it is sent to the client. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="weight" type="number">
                  The weight of this backend service in the weighted group, determining the proportion of traffic routed to it relative to the total weight of all services in the group. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service" type="string">
          The full or partial URL (or name) of the backend service to which traffic matching this rule is routed. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url_redirect" type="object">
          A block that defines a URL redirect action for this route rule. When specified, requests matching this rule are redirected to the URL specified by the redirect parameters. If url_redirect is set, neither the service nor the urlRewrite action may be set for the same rule. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="host_redirect" type="string">
              Specifies the host to use in the redirect response. If not set, the request's original host is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="https_redirect" type="bool">
              If set to true, the redirect uses the HTTPS protocol; otherwise it uses the same protocol as the request. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path_redirect" type="string">
              The path to which the request should be redirected. If specified, the redirect URL uses this exact path; cannot be used together with prefix_redirect. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="prefix_redirect" type="string">
              Specifies the prefix used for the redirect URL. This value replaces the matched prefix in the original request path. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="redirect_response_code" type="string">
              The HTTP redirect response code to use for the redirect. Allowed values: FOUND (302), MOVED_PERMANENTLY_DEFAULT (301), PERMANENT_REDIRECT (308), SEE_OTHER (303), TEMPORARY_REDIRECT (307). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="strip_query" type="bool">
              If set to true, any accompanying query portion of the original URL is removed before redirecting. If set to false, the query string is preserved. Defaults to false. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the regional URL map resides. This field is not applicable to global URL maps. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tests" type="list(object)">
  The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a maximum of 100 tests per UrlMap. Not supported when the URL map is bound to a target gRPC proxy that has validateForProxyless field set to true. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      An optional description for a test case in the URL map, used to provide additional context or document the test's purpose. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="expected_output_url" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="expected_redirect_response_code" type="number">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="headers" type="list(object)">
      A list of HTTP request headers that must be present for the URL map test to match. Each item in the list is an object with a header name and value. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the request header to match against in the test. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="host" type="string">
      The hostname or host pattern to match in the test request for validating URL map routing rules. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path" type="string">
      The URL path to be used in the test request. For example, '/path'. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="service" type="string">
      A reference to the backend service that the test expects to handle requests for the specified host and path. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
