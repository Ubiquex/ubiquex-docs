---
title: "google_compute_region_commitment"
description: "`google_compute_region_commitment` is a regional committed-use discount -- a real, one- or three-year commitment to a specific amount of vCPU, memory..."
---

`google_compute_region_commitment` is a regional committed-use discount -- a real, one- or three-year commitment to a specific amount of vCPU, memory, or GPU usage in one region, in exchange for a lower real hourly rate than on-demand pricing.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own region commitment"})

    		ubx.Resource(compute.RegionCommitment, "example", compute.RegionCommitmentConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionCommitment } from "jsr:@ubx/sdk-google/google/compute/region_commitment";

    export default stack("example", () => {
      intent({ summary: "example own region commitment" });

      resource(RegionCommitment, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RegionCommitment, RegionCommitmentConfig

    def describe():
        ubx.intent("example own region commitment")

        ubx.resource(RegionCommitment, "example", RegionCommitmentConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_commitment called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="auto_renew" type="bool">
  Specifies whether to automatically renew the commitment at the end of its current term. The default value is false. If you set the field to true, each time your commitment reaches the end of its term, Compute Engine automatically renews it for another term. You can update this field anytime before the commitment expires. For example, if the commitment is set to expire at 12 AM UTC-8 on January 3, 2027, you can update this field until 11:59 PM UTC-8 on January 2, 2027. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="category" type="string">
  The category of the commitment; specifies whether the commitment is for hardware or software resources. Category MACHINE specifies that you are committing to hardware machine resources such asVCPU or MEMORY, listed in resources. Category LICENSE specifies that you are committing to software licenses, listed in licenseResources. Note that if you specify MACHINE commitments, then you must also specify a type to indicate the machine series of the hardware resource that you are committing to. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="custom_end_timestamp" type="string">
  [Input Only] Optional, specifies the requested commitment end time inRFC3339 text format. Use this option when the desired commitment's end date is later than the start date + term duration. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of the commitment. You can provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="end_timestamp" type="string">
  Output only. [Output Only] Commitment end time inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="existing_reservations" type="list(string)">
  The list of existing reservations that are automatically included in this region commitment. This field is output-only and is determined by Google Cloud based on the commitment's resources. Optional; if omitted, computed by GCP. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#commitment for commitments. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="license_resource" type="object">
  Commitment for a particular license resource. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="amount" type="string">
      The number of licenses you plan to purchase. Optional.
    </ResponseField>
    <ResponseField name="cores_per_license" type="string">
      The number of cores per license. Optional.
    </ResponseField>
    <ResponseField name="license" type="string">
      The applicable license URI. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="merge_source_commitments" type="list(string)">
  The list of source commitments that you are merging to create the new merged commitment. For more information, see Merging commitments. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the commitment. You must specify a name when you purchase the commitment. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="params" type="object">
  Additional commitment params. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the commitment. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="plan" type="string">
  The minimum time duration that you commit to purchasing resources. The plan that you choose determines the preset term length of the commitment (which is 1 year or 3 years) and affects the discount rate that you receive for your resources. Committing to a longer time duration typically gives you a higher discount rate. The supported values for this field are TWELVE_MONTH (1 year), andTHIRTY_SIX_MONTH (3 years). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the commitment and committed resources are located. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reservations" type="list(object)">
  The list of new reservations that you want to create and attach to this commitment. You must attach reservations to your commitment if your commitment specifies any GPUs or Local SSD disks. For more information, see Attach reservations to resource-based commitments. Specify this property only if you want to create new reservations to attach. To attach existing reservations, specify theexistingReservations property instead. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="advanced_deployment_control" type="object">
      Optional; if omitted, computed by GCP.
      <Expandable title="properties">
        <ResponseField name="reservation_operational_mode" type="string">
          The operational mode for the reservation. Valid values are ALL_CAPACITY (all capacity is provisioned immediately), HIGHLY_AVAILABLE_CAPACITY (capacity is distributed to support high availability), and RESERVATION_OPERATIONAL_MODE_UNSPECIFIED (mode not set, defaults to ALL_CAPACITY). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="aggregate_reservation" type="object">
      The aggregate reservation configuration, used to reserve capacity for a pool of resources (such as CPU and memory) without specifying exact instance shapes. It includes a VM family and a list of reserved resource types with counts, offering flexible capacity reservation for a group of instances. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="in_use_resources" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="accelerator" type="object">
              Optional; if omitted, computed by GCP.
              <Expandable title="properties">
                <ResponseField name="accelerator_count" type="number">
                  Optional; if omitted, computed by GCP.
                </ResponseField>
                <ResponseField name="accelerator_type" type="string">
                  Specifies the type of accelerator (GPU) used by the resources currently in use within the aggregate reservation. This value corresponds to the acceleratorType in the machine type, such as 'nvidia-tesla-t4'. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="reserved_resources" type="list(object)">
          Specifies the resources included in this aggregate reservation. Each entry defines a resource type and the amount to reserve. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="accelerator" type="object">
              The accelerator block specifies the type and count of accelerators to reserve in the aggregate reservation. It includes fields for the accelerator type and the number of accelerators. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="accelerator_count" type="number">
                  The number of accelerators of the specified type to reserve in this resource reservation. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="accelerator_type" type="string">
                  The type of accelerator (e.g., GPU) to reserve in the aggregate reservation. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="vm_family" type="string">
          The family of TPU VMs to reserve capacity for. Valid values include VM_FAMILY_CLOUD_TPU_DEVICE_CT3, VM_FAMILY_CLOUD_TPU_LITE_DEVICE_CT5L, VM_FAMILY_CLOUD_TPU_LITE_POD_SLICE_CT5LP, VM_FAMILY_CLOUD_TPU_LITE_POD_SLICE_CT6E, VM_FAMILY_CLOUD_TPU_POD_SLICE_CT3P, VM_FAMILY_CLOUD_TPU_POD_SLICE_CT4P, VM_FAMILY_CLOUD_TPU_POD_SLICE_CT5P, and VM_FAMILY_CLOUD_TPU_POD_SLICE_TPU7X. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="workload_type" type="string">
          The workload type of the aggregate reservation. Valid values are BATCH, SERVING, or UNSPECIFIED. This determines how the reservation is used for batch or serving workloads. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="commitment" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="confidential_compute_type" type="string">
      Specifies the confidential computing type for the reservation. Valid values are CONFIDENTIAL_COMPUTE_TYPE_TDX (for Intel TDX) and CONFIDENTIAL_COMPUTE_TYPE_UNSPECIFIED (default, unspecified). Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="creation_timestamp" type="string">
      Creation timestamp in RFC3339 text format. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="delete_after_duration" type="object">
      A duration (seconds and optional nanos) after which the reservation is automatically deleted. If unset, the reservation persists until the commitment ends. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          The duration's sub-second component in nanoseconds, ranging from 0 to 999,999,999. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="seconds" type="string">
          The number of seconds in the duration after which the reservation is automatically deleted. Must be a string representation of an integer from 0 to 315,576,000,000 inclusive. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="delete_at_time" type="string">
      The time at which the reservation will be automatically deleted, in RFC3339 format. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="deployment_type" type="string">
      The deployment type for this reservation. Allowed values are DENSE and DEPLOYMENT_TYPE_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="early_access_maintenance" type="string">
      Specifies the early access mode for maintenance events on this reservation. Valid values are NO_EARLY_ACCESS, WAVE1, and WAVE2, indicating no early access, first wave, or second wave, respectively. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_emergent_maintenance" type="bool">
      Whether the reservation can be consumed by emergent maintenance requests. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="kind" type="string">
      The type of the resource. Always 'compute#reservation' for this resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="linked_commitments" type="list(string)">
      List of self_links of commitments linked to this reservation. This is used to associate the reservation with one or more commitments so that the commitment's discounts apply to the reserved capacity. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="params" type="object">
      Optional; if omitted, computed by GCP.
      <Expandable title="properties">
        <ResponseField name="resource_manager_tags" type="map(string)">
          Resource manager tags to be bound to the instances created from this reservation. A map of tag key-value pairs, where the value may be an empty string for a tag with only a key. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protection_tier" type="string">
      The protection tier for the reservation, which determines the level of capacity protection. Valid values are `STANDARD`, `CAPACITY_OPTIMIZED`, and `PROTECTION_TIER_UNSPECIFIED` (the latter is the default when unspecified). Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="reservation_sharing_policy" type="object">
      Optional; if omitted, computed by GCP.
      <Expandable title="properties">
        <ResponseField name="service_share_type" type="string">
          Specifies the sharing type for this reservation, controlling which services can use it. ALLOW_ALL permits all services, DISALLOW_ALL blocks all services, and SERVICE_SHARE_TYPE_UNSPECIFIED is the default unspecified value. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_metadata" type="object">
      Optional; if omitted, computed by GCP.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="resource_type" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_policies" type="map(string)">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="resource_status" type="object">
      Optional; if omitted, computed by GCP.
      <Expandable title="properties">
        <ResponseField name="health_info" type="object">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="degraded_block_count" type="number">
              The number of blocks that are degraded in the resource, as reported in its health information. This value indicates the extent of storage degradation for the resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="health_status" type="string">
              The health status of the resource. Possible values: DEGRADED, HEALTHY, HEALTH_STATUS_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="healthy_block_count" type="number">
              The number of healthy blocks in the reservation's resource health status. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="reservation_block_count" type="number">
          The number of capacity blocks that this reservation uses. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reservation_maintenance" type="object">
          An object that provides the maintenance status details for the reservation, as part of the reservation's resource status. This is a computed field reflecting the current maintenance state of the reservation. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="instance_maintenance_ongoing_count" type="number">
              The number of instances in the reservation that are currently undergoing maintenance, as reported in the reservation maintenance status. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="instance_maintenance_pending_count" type="number">
              The number of instances in this reservation that have maintenance pending. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="maintenance_ongoing_count" type="number">
              The number of resources within this reservation that are currently undergoing maintenance. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="maintenance_pending_count" type="number">
              The number of instances in this reservation that have a pending maintenance event. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="scheduling_type" type="string">
              The maintenance scheduling type for the reservation. Possible values are: 'GROUPED' (maintenance is grouped for the reservation), 'INDEPENDENT' (maintenance is handled independently), and 'GROUP_MAINTENANCE_TYPE_UNSPECIFIED' (the type is not specified). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subblock_infra_maintenance_ongoing_count" type="number">
              The number of ongoing maintenance operations for the subblock infrastructure within the reservation. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subblock_infra_maintenance_pending_count" type="number">
              The number of pending maintenance items for the subblock infrastructure, as reported in the reservation maintenance status. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="upcoming_group_maintenance" type="object">
              Upcoming maintenance information for the entire reservation group, shown in the resource status. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="can_reschedule" type="bool">
                  Indicates whether the upcoming group maintenance event can be rescheduled. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="latest_window_start_time" type="string">
                  The latest time at which the upcoming group maintenance window can start, as reported for this reservation's resource status. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="maintenance_on_shutdown" type="bool">
                  Optional; if omitted, computed by GCP.
                </ResponseField>
                <ResponseField name="maintenance_reasons" type="list(string)">
                  Optional; if omitted, computed by GCP.
                </ResponseField>
                <ResponseField name="maintenance_status" type="string">
                  The maintenance status of the upcoming group maintenance, indicating whether it is ONGOING, PENDING, or UNKNOWN. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The type of upcoming maintenance for the reservation group. Possible values are: MULTIPLE, SCHEDULED, UNKNOWN_TYPE, UNSCHEDULED. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="window_end_time" type="string">
                  The end time of the maintenance window for the upcoming group maintenance event, expressed as a timestamp string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="window_start_time" type="string">
                  The start time of the maintenance window for an upcoming maintenance event on the reservation, as a timestamp. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="specific_sku_allocation" type="object">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="source_instance_template_id" type="string">
              The ID of the instance template used to specify the machine type and other properties for this specific SKU allocation in the reservation. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="utilizations" type="map(string)">
              The utilization of the specific SKU reservation, keyed by resource type (e.g., memory). Each value is a fraction of the total capacity of that resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="satisfies_pzs" type="bool">
      Whether this reservation satisfies the requirements for physical zone separation (PZS) for sole-tenant node groups. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="scheduling_type" type="string">
      The scheduling type of the reservation. Possible values: INDEPENDENT (reserves capacity for standalone instances), GROUPED (reserves capacity for a group of instances, typically used with managed instance groups), and GROUP_MAINTENANCE_TYPE_UNSPECIFIED (the scheduling type is not specified). Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="self_link" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="share_settings" type="object">
      Configuration for sharing a reservation with other projects. It specifies the share type (LOCAL or SPECIFIC_PROJECTS) and, for SPECIFIC_PROJECTS, the set of projects that can use the reservation. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="project_map" type="map(object)">
          A map of project IDs to project-specific configuration, used to specify which projects can share this commitment's reservations as part of the sharing settings. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="project_id" type="string">
              The ID of the Google Cloud project that has shared access to the commitment reservation. This is the key for the project_map entry. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="share_type" type="string">
          Defines the sharing type for this reservation. Allowed values are: LOCAL (only within the owning project), SPECIFIC_PROJECTS (shared with a list of specific projects), ORGANIZATION (shared with all projects in the organization), and SHARE_TYPE_UNSPECIFIED (default, indicating the field is not set). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="specific_reservation" type="object">
      Configuration for a specific reservation within the commitment. This block specifies the instance count and instance properties (such as machine type and GPUs) for reserved VMs, which are dedicated to a single project. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="assured_count" type="string">
          The number of virtual machine instances that are guaranteed to be available in this specific reservation. This value must be less than or equal to the total 'count' of the reservation. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="count" type="string">
          The number of virtual machine instances to reserve in the specific reservation. The value must be a positive integer. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="in_use_count" type="string">
          Number of reserved instances from this specific reservation that are currently in use. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instance_properties" type="object">
          The properties of the virtual machines to be reserved in this specific reservation, including machine type, minimum CPU platform, accelerator configuration, and local SSD count. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="guest_accelerators" type="list(object)">
              A list of guest accelerator (GPU) configurations to attach to instances created from this reservation. Each object defines an accelerator type and the number of accelerators. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="accelerator_count" type="number">
                  The number of guest accelerators of the type specified by `accelerator_type` to attach to each instance in the reservation. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="accelerator_type" type="string">
                  The type of GPU accelerator to attach, such as 'nvidia-tesla-t4' or 'nvidia-tesla-p100'. This is specified in the guest accelerator configuration for the instance properties. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="local_ssds" type="list(object)">
              A list of local SSD configurations to attach to instances in this specific reservation. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="disk_size_gb" type="string">
                  The size of the local SSD disk in gigabytes (GB), expressed as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="interface" type="string">
                  The interface type for the local SSD. Supported values are `NVME` and `SCSI`. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="location_hint" type="string">
              Optional location hint for the reserved instances, used by Compute Engine to optimize physical placement. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="machine_type" type="string">
              Specifies the machine type for the VM instances in this specific reservation. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="min_cpu_platform" type="string">
              Specifies the minimum CPU platform allowed for VM instances in the specific reservation. The value must be a valid CPU platform name (e.g., 'Intel Skylake' or 'AMD Milan') and instances will be guaranteed to use a CPU platform at least as recent as the specified one. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_instance_template" type="string">
          The self-link of an instance template that defines the properties of the instances to be reserved. When specified, the specific reservation is created based on the template's configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="specific_reservation_required" type="bool">
      Indicates whether the reservation is a specific reservation (for a specific instance type) or a non-specific reservation (for an instance family). When true, the reservation is specific; when false, it is non-specific. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      The current status of the reservation, indicating its lifecycle state. Possible values are CREATING, DELETING, INVALID, READY, and UPDATING. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="zone" type="string">
      The zone in which the reservation is created, within the commitment's region. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_status" type="object">
  [Output Only] Contains output only fields. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="custom_term_eligibility_end_timestamp" type="string">
      Output only. [Output Only] Indicates the end time of customer's eligibility to send custom term requests in RFC3339 text format. Term extension requests that (not the end time in the request) after this time will be rejected. Computed by GCP after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resources" type="list(object)">
  The list of all the hardware resources, with their types and amounts, that you want to commit to. Specify as a separate entry in the list for each individual resource type. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="accelerator_type" type="string">
      The type of accelerator (GPU) to include in the commitment, e.g., 'nvidia-tesla-v100'. Must be a valid accelerator type available in the region. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="amount" type="string">
      The quantity of the resource type (e.g., VCPU, MEMORY) to reserve in the commitment. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of resource to commit to. Allowed values are `VCPU`, `MEMORY`, `LOCAL_SSD`, `ACCELERATOR`, and `UNSPECIFIED`. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="split_source_commitment" type="string">
  The source commitment from which you are transferring resources to create the new split commitment. For more information, see Split commitments. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="start_timestamp" type="string">
  Output only. [Output Only] Commitment start time inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] Status of the commitment with regards to eventual expiration (each commitment has an end date defined). Status can be one of the following values: NOT_YET_ACTIVE, ACTIVE, orEXPIRED. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="status_message" type="string">
  Output only. [Output Only] An optional, human-readable explanation of the status. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="type" type="string">
  The type of commitment; specifies the machine series for which you want to commit to purchasing resources. The choice of machine series affects the discount rate and the eligible resource types. The type must be one of the following:ACCELERATOR_OPTIMIZED, ACCELERATOR_OPTIMIZED_A3,ACCELERATOR_OPTIMIZED_A3_MEGA,COMPUTE_OPTIMIZED, COMPUTE_OPTIMIZED_C2D, COMPUTE_OPTIMIZED_C3, COMPUTE_OPTIMIZED_C3D,COMPUTE_OPTIMIZED_H3, GENERAL_PURPOSE,GENERAL_PURPOSE_C4, GENERAL_PURPOSE_E2,GENERAL_PURPOSE_N2, GENERAL_PURPOSE_N2D,GENERAL_PURPOSE_N4, GENERAL_PURPOSE_T2D,GRAPHICS_OPTIMIZED, GRAPHICS_OPTIMIZED_G4,GRAPHICS_OPTIMIZED_G4_VGPU,MEMORY_OPTIMIZED, MEMORY_OPTIMIZED_M3,MEMORY_OPTIMIZED_X4, STORAGE_OPTIMIZED_Z3. For example, type MEMORY_OPTIMIZED specifies a commitment that applies only to eligible resources of memory optimized M1 and M2 machine series. Type GENERAL_PURPOSE specifies a commitment that applies only to eligible resources of general purpose N1 machine series. Optional; if omitted, computed by GCP.
</ResponseField>
