---
title: "google_compute_region_network_firewall_policy"
description: "`google_compute_region_network_firewall_policy` is the regional counterpart to a global network firewall policy -- a real, reusable rule set attachable..."
---

`google_compute_region_network_firewall_policy` is the regional counterpart to a global network firewall policy -- a real, reusable rule set attachable to networks, scoped to a single region.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own region network firewall policy"})

    		ubx.Resource(compute.RegionNetworkFirewallPolicy, "example", compute.RegionNetworkFirewallPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionNetworkFirewallPolicy } from "@ubx/sdk-google/google/compute/region_network_firewall_policy";

    export default stack("example", () => {
      intent({ summary: "example own region network firewall policy" });

      resource(RegionNetworkFirewallPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RegionNetworkFirewallPolicy, RegionNetworkFirewallPolicyConfig

    def describe():
        ubx.intent("example own region network firewall policy")

        ubx.resource(RegionNetworkFirewallPolicy, "example", RegionNetworkFirewallPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_network_firewall_policy called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="associations" type="list(object)">
  A list of associations that belong to this firewall policy. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="attachment_target" type="string">
      The VPC network to which the firewall policy is attached. This can be a network name or a self-link. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="display_name" type="string">
      A human-readable name for the firewall policy association, used as a display label to identify the association in user-facing contexts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="firewall_policy_id" type="string">
      The ID of the firewall policy to which the network is being associated. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the firewall policy association, which uniquely identifies it within the regional network firewall policy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="short_name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Deprecated, please use short name instead. User-provided name of the Organization firewall policy. The name should be unique in the organization in which the firewall policy is created. This field is not applicable to network firewall policies. This name must be set on creation and cannot be changed. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Specifies a fingerprint for this resource, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make get() request to the firewall policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. For Organization Firewall Policies it's a [Output Only] numeric ID allocated by Google Cloud which uniquely identifies the Organization Firewall Policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="packet_mirroring_rules" type="list(object)">
  A list of packet mirroring rules that belong to this policy. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      The action to apply to packets that match this packet mirroring rule. Valid values are 'mirror' to mirror the traffic and 'do_not_mirror' to exclude the traffic from mirroring. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="direction" type="string">
      Direction of traffic to which this packet mirroring rule applies. Valid values are INGRESS (incoming traffic) and EGRESS (outgoing traffic). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disabled" type="bool">
      If set to true, the rule is disabled and will not be considered for packet mirroring. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_logging" type="bool">
      Whether to enable logging for the packet mirroring rule. When set to true, logs are generated for traffic that matches this rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kind" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="match" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="dest_address_groups" type="list(string)">
          List of destination address group URLs that the packet mirroring rule matches. Traffic destined to these address groups is selected for mirroring. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_fqdns" type="list(string)">
          A list of destination fully qualified domain names (FQDNs) to match against in the packet mirroring rule. Traffic destined to any of these FQDNs matches the rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_ip_ranges" type="list(string)">
          A list of destination IP address ranges in CIDR notation that this packet mirroring rule matches. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_network_context" type="string">
          Specifies the network context of the destination traffic to match for packet mirroring. Allowed values are INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_network_type" type="string">
          The destination network type to which the packet mirroring rule applies. Valid values are: INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_region_codes" type="list(string)">
          Lists destination region codes that the packet's destination IP must be within for the mirroring rule to match. For example, 'us-central1'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_threat_intelligences" type="list(string)">
          A list of threat intelligence categories used to match the destination IP address of a packet. When specified, only packets whose destination IP is associated with one of these categories are affected (e.g., mirrored). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="layer4_configs" type="list(object)">
          Layer 4 configurations to match, where each configuration specifies an IP protocol (such as tcp, udp, or icmp) and optionally a list of ports to match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="ip_protocol" type="string">
              The IP protocol to which this layer4 config applies. Can be one of the well-known protocol strings (e.g., "tcp", "udp", "icmp", "esp", "ah", "ipip", "sctp") or "all" to match all IP protocols. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ports" type="list(string)">
              List of destination port numbers or ranges (e.g., '80', '1000-2000') for the layer4 config. Required when the protocol is TCP, UDP, or SCTP; must be empty for other protocols. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="src_address_groups" type="list(string)">
          List of source address groups that this packet mirroring rule uses for matching. When specified, only traffic originating from addresses in these groups will be affected by the rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_fqdns" type="list(string)">
          List of fully qualified domain names (FQDNs) used as a source match condition. When specified, only traffic originating from these FQDNs will be mirrored. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_ip_ranges" type="list(string)">
          List of source IP address ranges (in CIDR format) that this packet mirroring rule matches. Packets originating from these source IPs will match the rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_network_context" type="string">
          The source network context to match against for this packet mirroring rule. Allowed values are INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. This restricts the rule to traffic originating from the specified network context. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_network_type" type="string">
          The type of source network from which the traffic originates. Valid values are: INTERNET (traffic from the internet), INTRA_VPC (traffic within the same VPC network), NON_INTERNET (traffic from non-internet sources such as on-premises or other networks), VPC_NETWORKS (traffic from other VPC networks), and UNSPECIFIED (no source network type specified). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_networks" type="list(string)">
          The list of source network resource URLs that packets must come from for this packet mirroring rule to match. If this field is empty, the rule matches from all source networks. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_region_codes" type="list(string)">
          A list of source region codes (e.g., 'US', 'EU') that the traffic's source region must match for this rule to apply. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_secure_tags" type="list(object)">
          A list of secure tags that the source instance must have for the packet to match this rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the source secure tag, in the format `tagKeys/&#123;resource_id&#125;` or `tagValues/&#123;resource_id&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              The current state of the secure tag, either EFFECTIVE or INEFFECTIVE, indicating whether the tag is currently in effect. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="src_threat_intelligences" type="list(string)">
          A list of source threat intelligence lists used to match the source IP address of the packet. The source IP must be present in one of the specified threat intelligence lists. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="priority" type="number">
      The priority of the packet mirroring rule, which determines the order in which rules are evaluated. Lower numbers are evaluated first and take precedence. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="rule_tuple_count" type="number">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="security_profile_group" type="string">
      The security profile group to apply to this packet mirroring rule. This should be a self link (URL) to a SecurityProfileGroup resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_forwarding_rules" type="list(string)">
      A list of forwarding rule URLs that this packet mirroring rule applies to. The rule will mirror traffic for the specified forwarding rules. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_resources" type="list(string)">
      Specifies the list of resources (such as Compute Engine instances) whose traffic will be mirrored by this packet mirroring rule. Each resource should be provided as a fully-qualified URL. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_secure_tags" type="list(object)">
      A list of secure tags that identify the target instances to which this packet mirroring rule applies. Traffic matching the rule and destined for instances with these tags is mirrored. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the secure tag to match for this packet mirroring rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The current state of the target secure tag, indicating whether it is active (EFFECTIVE) or inactive (INEFFECTIVE) for this rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target_service_accounts" type="list(string)">
      A list of service account emails that the packet mirroring rule applies to. The rule matches traffic from instances running as one of these service accounts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_type" type="string">
      The target type of a packet mirroring rule, indicating which type of resource the rule applies to. Allowed values are `INSTANCES` (for VM instances) and `INTERNAL_MANAGED_LB` (for internal managed load balancers). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tls_inspect" type="bool">
      If set to true, enables TLS inspection for traffic matching this packet mirroring rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="parent" type="string">
  Output only. [Output Only] The parent of the firewall policy. This field is not applicable to network firewall policies. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="policy_type" type="string">
  The type of the firewall policy. This field can be one of VPC_POLICY, RDMA_ROCE_POLICY or ULL_POLICY. Note: if not specified then VPC_POLICY will be used. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the regional firewall policy resides. This field is not applicable to global firewall policies. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rule_tuple_count" type="number">
  Output only. [Output Only] Total count of all firewall policy rule tuples. A firewall policy can not exceed a set number of tuples. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  A list of rules that belong to this policy. There must always be a default rule (rule with priority 2147483647 and match "*"). If no rules are provided when creating a firewall policy, a default rule with action "allow" will be added. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      The action to apply when a packet matches this rule, such as 'allow' or 'deny'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      A human-readable explanation of the firewall policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="direction" type="string">
      Specifies the direction of traffic to which this rule applies: INGRESS for inbound or EGRESS for outbound. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disabled" type="bool">
      Denotes whether the firewall rule is disabled. When set to true, the firewall rule is not enforced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_logging" type="bool">
      Whether to enable logging for this firewall policy rule. When set to true, firewall logs are generated for traffic that matches this rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kind" type="string">
      The kind of the rule, which is always 'compute#firewallPolicyRule'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="match" type="object">
      The match block defines the traffic criteria (such as source and destination IP ranges and protocol/port) that determine whether this firewall policy rule applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dest_address_groups" type="list(string)">
          List of destination address groups that the rule matches. Address groups should be referenced by their full or partial URLs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_fqdns" type="list(string)">
          List of destination fully qualified domain names (FQDNs) to match for egress traffic in this firewall policy rule. When specified, the rule applies only to traffic destined for these domains. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_ip_ranges" type="list(string)">
          A list of destination IP address ranges (in CIDR notation) to which this firewall policy rule applies. Traffic is matched based on the destination IP address being within one of these ranges. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_network_context" type="string">
          Specifies the destination network context for the firewall rule. Valid values are INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_network_type" type="string">
          The type of destination network that the firewall rule matches. Allowed values are: INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_region_codes" type="list(string)">
          A list of destination region codes. When specified, the rule matches only traffic destined for resources in those regions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_threat_intelligences" type="list(string)">
          A list of threat intelligence feed names (e.g., 'iplist-known-malicious-ips') that the destination IP address must match for the rule to apply. If omitted, no threat intelligence matching is performed on the destination address. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="layer4_configs" type="list(object)">
          A list of layer 4 configurations for the match condition. Each configuration specifies an IP protocol and optional destination port ranges to match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="ip_protocol" type="string">
              The IP protocol to match. Can be a well-known protocol name such as tcp, udp, icmp, esp, ah, ipip, sctp, or a custom IP protocol number. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ports" type="list(string)">
              An optional list of destination ports to which this layer4 config applies. This field is only valid for TCP or UDP protocols. Each entry can be an integer (e.g., '80') or a range (e.g., '1024-2048'). If omitted, the rule applies to all ports. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="src_address_groups" type="list(string)">
          List of source address groups (IP addresses or CIDR blocks) used to match the source of traffic for this firewall rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_fqdns" type="list(string)">
          A list of fully qualified domain names (FQDN) to match as source in the firewall policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_ip_ranges" type="list(string)">
          A list of source IP address ranges in CIDR notation that the firewall rule matches. Traffic from any of these source IPs will be evaluated against the rule's action. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_network_context" type="string">
          Specifies the network context that the source of the traffic must match. Valid values are INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_network_type" type="string">
          The source network type to match against. Valid values are INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_networks" type="list(string)">
          A list of source network resource URLs to match against. Each network is specified in the format projects/&#123;project&#125;/global/networks/&#123;network&#125;. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_region_codes" type="list(string)">
          Specifies the source region codes that traffic must originate from for the rule to match. The list can contain GCP region codes such as 'us-central1' or 'europe-west1'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_secure_tags" type="list(object)">
          List of secure tag objects that the source of the traffic must match for the firewall policy rule to apply. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The full resource name of the secure tag value, for example `tagValues/123456`. This tag value is used to match traffic originating from resources that have this tag applied. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              The state of the secure tag, indicating whether it is currently effective (EFFECTIVE) or ineffective (INEFFECTIVE) for the firewall policy rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="src_threat_intelligences" type="list(string)">
          A list of threat intelligence list names used to match the source IP of traffic. The rule applies when the source IP appears in any of the specified threat intelligence lists. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="priority" type="number">
      Integer priority for this firewall rule. Lower values are evaluated first and indicate higher priority. Must be an integer between 0 and 2147483647. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="rule_tuple_count" type="number">
      The number of tuples that the firewall policy rule applies to. This is a computed (read-only) field; a value of 1 indicates the rule applies to a single tuple. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="security_profile_group" type="string">
      This field specifies the security profile group to apply to the firewall rule. It must be set when the rule action is `apply_security_profile_group`, and the value should be a full URL of the form `projects/&#123;project_id&#125;/locations/&#123;location&#125;/securityProfileGroups/&#123;security_profile_group_name&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_forwarding_rules" type="list(string)">
      A list of forwarding rule URLs that this regional network firewall policy rule applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_resources" type="list(string)">
      A list of URLs to target resources (such as Compute Engine instances) that this firewall policy rule applies to. The rule is evaluated for traffic to these resources. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_secure_tags" type="list(object)">
      A list of secure tags that control which instances this firewall rule applies to, by matching resources that have at least one of the specified secure tags. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the secure tag that this firewall policy rule targets. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The state of the secure tag, indicating whether it is currently effective or ineffective. Allowed values are: EFFECTIVE and INEFFECTIVE. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target_service_accounts" type="list(string)">
      A list of service accounts. When specified, the rule applies only to traffic from instances whose service account is in this list. If empty, the rule applies to all instances. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_type" type="string">
      Specifies the target type of the rule. Valid values are INSTANCES and INTERNAL_MANAGED_LB. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tls_inspect" type="bool">
      If true, the rule applies only to traffic that has been TLS-inspected. If false, the rule applies only to traffic that has not been TLS-inspected. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource with the resource id. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="short_name" type="string">
  User-provided name of the Organization firewall policy. The name should be unique in the organization in which the firewall policy is created. This field is not applicable to network firewall policies. This name must be set on creation and cannot be changed. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>
