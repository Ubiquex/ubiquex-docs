---
title: "google_compute_network_peering"
description: "Real, generated bindings for `google_compute_network_peering`."
---

`google_compute_network_peering` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network peering"})

    		ubx.Resource(compute.NetworkPeering, "ci-runner", compute.NetworkPeeringConfig{
    			Name:                           "ci-runner",
    			Network:                        "example",
    			PeerNetwork:                    "example",
    			ExportCustomRoutes:             true,
    			ExportSubnetRoutesWithPublicIp: true,
    			ImportCustomRoutes:             true,
    			ImportSubnetRoutesWithPublicIp: true,
    			StackType:                      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkPeering } from "jsr:@ubx/sdk-google/google/compute/network_peering";

    export default stack("payments", () => {
      intent({ summary: "payments own network peering" });

      resource(NetworkPeering, "ci-runner", {
        name: "ci-runner",
        network: "example",
        peerNetwork: "example",
        exportCustomRoutes: true,
        exportSubnetRoutesWithPublicIp: true,
        importCustomRoutes: true,
        importSubnetRoutesWithPublicIp: true,
        stackType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import NetworkPeering, NetworkPeeringConfig

    def describe():
        ubx.intent("payments own network peering")

        ubx.resource(NetworkPeering, "ci-runner", NetworkPeeringConfig(
            name="ci-runner",
            network="example",
            peer_network="example",
            export_custom_routes=True,
            export_subnet_routes_with_public_ip=True,
            import_custom_routes=True,
            import_subnet_routes_with_public_ip=True,
            stack_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_network_peering called "ci-runner", with
    network example, peer network example, export custom routes true,
    export subnet routes with public ip true, import custom routes
    true, import subnet routes with public ip true, stack type
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="export_custom_routes" type="bool">
  Whether to export the custom routes to the peer network. Defaults to false. Optional.
</ResponseField>

<ResponseField name="export_subnet_routes_with_public_ip" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="import_custom_routes" type="bool">
  Whether to export the custom routes from the peer network. Defaults to false. Optional.
</ResponseField>

<ResponseField name="import_subnet_routes_with_public_ip" type="bool">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the peering. Required.
</ResponseField>

<ResponseField name="network" type="string" required>
  The primary network of the peering. Required.
</ResponseField>

<ResponseField name="peer_network" type="string" required>
  The peer network in the peering. The peer network may belong to a different project. Required.
</ResponseField>

<ResponseField name="stack_type" type="string">
  Which IP version(s) of traffic and routes are allowed to be imported or exported between peer networks. The default value is IPV4_ONLY. Possible values: ["IPV4_ONLY", "IPV4_IPV6"] Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_strategy" type="string">
  The update strategy determines the semantics for updates and deletes to the peering connection configuration. The default value is INDEPENDENT. Possible values: ["INDEPENDENT", "CONSENSUS"] Optional.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  State for the peering, either ACTIVE or INACTIVE. The peering is ACTIVE when there's a matching configuration in the peer network. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state_details" type="string">
  Details about the current state of the peering. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
