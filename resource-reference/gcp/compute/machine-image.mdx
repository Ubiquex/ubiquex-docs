---
title: "google_compute_machine_image"
description: "`google_compute_machine_image` captures a real, complete instance -- boot disk, any additional disks..."
---

`google_compute_machine_image` captures a real, complete instance -- boot disk, any additional disks, and its own instance configuration -- as one reusable artifact, unlike a plain image which only captures the boot disk.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own machine image"})

    		ubx.Resource(compute.MachineImage, "ci-runner", compute.MachineImageConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MachineImage } from "./local-sdk/google/sdk/typescript/google/compute/machine_image.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own machine image" });

      resource(MachineImage, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.compute import MachineImage, MachineImageConfig

    def describe():
        ubx.intent("payments own machine image")

        ubx.resource(MachineImage, "ci-runner", MachineImageConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_machine_image called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] The creation timestamp for this machine image inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="guest_flush" type="bool">
  [Input Only] Whether to attempt an application consistent machine image by informing the OS to prepare for the snapshot process. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] A unique identifier for this machine image. The server defines this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_properties" type="object">
  The properties of the source instance used to create the machine image. This is an output-only field that captures the instance configuration for reference. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="advanced_machine_features" type="object">
      Specifies options for controlling advanced machine features. Options that would traditionally be configured in a BIOS belong here. Features that require operating system support may have corresponding entries in the GuestOsFeatures of anImage (e.g., whether or not the OS in theImage supports nested virtualization being enabled or disabled). Optional.
      <Expandable title="properties">
        <ResponseField name="enable_nested_virtualization" type="bool">
          Whether to enable nested virtualization or not (default is false). Optional.
        </ResponseField>
        <ResponseField name="enable_uefi_networking" type="bool">
          Whether to enable UEFI networking for instance creation. Optional.
        </ResponseField>
        <ResponseField name="performance_monitoring_unit" type="string">
          Type of Performance Monitoring Unit requested on instance. Optional.
        </ResponseField>
        <ResponseField name="threads_per_core" type="number">
          The number of threads per physical core. To disable simultaneous multithreading (SMT) set this to 1. If unset, the maximum number of threads supported per core by the underlying processor is assumed. Optional.
        </ResponseField>
        <ResponseField name="turbo_mode" type="string">
          Turbo frequency mode to use for the instance. Supported modes include: * ALL_CORE_MAX Using empty string or not setting this field will use the platform-specific default turbo mode. Optional.
        </ResponseField>
        <ResponseField name="visible_core_count" type="number">
          The number of physical cores to expose to an instance. Multiply by the number of threads per core to compute the total number of virtual CPUs to expose to the instance. If unset, the number of cores is inferred from the instance's nominal CPU count and the underlying platform's SMT width. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="can_ip_forward" type="bool">
      Enables instances created based on these properties to send packets with source IP addresses other than their own and receive packets with destination IP addresses other than their own. If these instances will be used as an IP gateway or it will be set as the next-hop in a Route resource, specify true. If unsure, leave this set tofalse. See theEnable IP forwarding documentation for more information. Optional.
    </ResponseField>
    <ResponseField name="confidential_instance_config" type="object">
      A set of Confidential Instance options. Optional.
      <Expandable title="properties">
        <ResponseField name="confidential_instance_type" type="string">
          Defines the type of technology used by the confidential instance. Optional.
        </ResponseField>
        <ResponseField name="enable_confidential_compute" type="bool">
          Defines whether the instance should have confidential compute enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      An optional text description for the instances that are created from these properties. Optional.
    </ResponseField>
    <ResponseField name="disks" type="list(object)">
      An array of disks that are associated with the instances that are created from these properties. Optional.
      <Expandable title="properties">
        <ResponseField name="architecture" type="string">
          The architecture of the disk, either ARM64 or X86_64, or ARCHITECTURE_UNSPECIFIED if not set. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="auto_delete" type="bool">
          Specifies whether this disk will be automatically deleted when the instance is deleted. This controls the disk's lifecycle relative to the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="boot" type="bool">
          Indicates whether this disk is a boot disk. A boot disk is the primary disk from which the instance boots. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="device_name" type="string">
          Specifies a unique device name of your choice that is reflected into the /dev/disk/by-id/google-* tree of a Linux operating system running within the instance. This name can be used to reference the device for mounting, resizing, and so on, from within the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_encryption_key" type="object">
          The customer-supplied encryption key (CSEK) or Cloud KMS key used to encrypt the disk. This block can contain a raw key, an RSA-encrypted key, or a KMS key self-link. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key_name" type="string">
              The full resource name of the Cloud KMS CryptoKey used to encrypt the disk. For example: projects/PROJECT_ID/locations/LOCATION/keyRings/KEY_RING/cryptoKeys/KEY_NAME. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kms_key_service_account" type="string">
              The service account used to encrypt or decrypt the disk with the KMS key. This account must have the required Cloud KMS IAM permissions on the key. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="raw_key" type="string">
              The raw customer-supplied encryption key for the disk, base64-encoded. This is used when providing your own key instead of a KMS key. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rsa_encrypted_key" type="string">
              The customer-supplied encryption key, encrypted using an RSA public key and encoded in base64, used to protect the disk and its contents. This allows the key to be provided securely without exposing the raw key in plaintext. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sha256" type="string">
              The SHA-256 hash of the customer-supplied encryption key. This value is used to verify the key's integrity and to identify the key when the disk is attached to a VM. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disk_size_gb" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="force_attach" type="bool">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="guest_os_features" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              The type of guest OS feature to enable on the disk. Must be one of the predefined feature types: BARE_METAL_LINUX_COMPATIBLE, CCA_CAPABLE, FEATURE_TYPE_UNSPECIFIED, GVNIC, IDPF, MULTI_IP_SUBNET, SECURE_BOOT, SEV_CAPABLE, SEV_LIVE_MIGRATABLE, SEV_LIVE_MIGRATABLE_V2, SEV_SNP_CAPABLE, SNP_SVSM_CAPABLE, TDX_CAPABLE, UEFI_COMPATIBLE, VIRTIO_SCSI_MULTIQUEUE, WINDOWS. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="index" type="number">
          The zero-based index of this disk in the attached disks list, which determines the device name (e.g., /dev/sda) and boot order. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="initialize_params" type="object">
          Initialize parameters for the disk, including source image, disk size, and disk type, used when creating an instance from this machine image. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="architecture" type="string">
              The CPU architecture of the disk, indicating compatibility with ARM64 or X86_64. Valid values are ARCHITECTURE_UNSPECIFIED, ARM64, and X86_64. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              An optional description for the disk. This is a user-defined field that can be used to provide additional context about the disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_name" type="string">
              Specifies the name of the disk. If not set, the default is to use the name of the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_size_gb" type="string">
              The size of the disk in gigabytes (GB). This value is provided as a string, but should represent a numeric size (e.g., '10'). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_type" type="string">
              The type of disk to use, such as 'pd-standard' or 'pd-ssd'. This can be a full or partial URL to a disk type resource. If not specified, the default disk type is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enable_confidential_compute" type="bool">
              Whether this disk adds confidential computing capability. Note: This field is only applicable to boot disks of VM instances that use Confidential VMs. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="labels" type="map(string)">
              Labels to apply to this disk. A map of key/value pairs that can be used to organize and identify the disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="licenses" type="list(string)">
              A list of URLs referencing license resources that the disk will be subject to when initialized. When specified, these licenses override any licenses inherited from the source image or snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="on_update_action" type="string">
              Specifies the action to take on the disk when its configuration is updated. Possible values: `RECREATE_DISK` (always recreate), `RECREATE_DISK_IF_SOURCE_CHANGED` (recreate only if the source changes), `USE_EXISTING_DISK` (keep the existing disk). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="provisioned_iops" type="string">
              The provisioned I/O operations per second (IOPS) for the persistent disk. This value is represented as a string and determines the disk's performance level when initialized. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="provisioned_throughput" type="string">
              The provisioned throughput of the disk in MB/s, used to configure performance for supported disk types such as SSD persistent disks. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replica_zones" type="list(string)">
              Specifies the zones for a regional persistent disk. Provide exactly two zones to create a regional disk; leave unset for a zonal disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_manager_tags" type="map(string)">
              A map of resource manager tags (key-value pairs) to apply to the disk when it is created. These tags can be used for access control, cost tracking, or resource organization. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_policies" type="list(string)">
              List of resource policy self-links to apply to the disk. Resource policies control schedules for snapshots or other disk management operations. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_image" type="string">
              The source image to use for the disk. This can be a full URL, a partial URL, or an image name. The disk is initialized from this image. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_image_encryption_key" type="object">
              Encryption key used to encrypt the source image. If the specified source image is encrypted, this key is required to access it when creating the disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kms_key_name" type="string">
                  The customer-managed encryption key name used to encrypt the source image. Provide the full resource name in the format projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;keyRing&#125;/cryptoKeys/&#123;key&#125;. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kms_key_service_account" type="string">
                  The service account used to perform cryptographic operations with the KMS key specified in this encryption key block. This is required if the key's permissions are scoped to a specific service account. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="raw_key" type="string">
                  The base64-encoded customer-supplied encryption key (256-bit) used to encrypt the source image. This is required when the source image is encrypted with a customer-supplied key rather than a Google-managed key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rsa_encrypted_key" type="string">
                  The RSA-encrypted (base64-encoded) key used to encrypt the source image. This is an alternative to using `raw_key`. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sha256" type="string">
                  The SHA-256 hash of the customer-supplied encryption key used to protect the source image. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="source_snapshot" type="string">
              The source snapshot used to initialize this disk. Provide the full or partial URL of the snapshot resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_snapshot_encryption_key" type="object">
              Specifies the customer-supplied encryption key used to decrypt the source snapshot. Required if the source snapshot is protected by a customer-managed encryption key. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kms_key_name" type="string">
                  The fully-qualified Cloud KMS key name used to decrypt the source snapshot referenced by this initialize parameter. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kms_key_service_account" type="string">
                  The service account used to access the Cloud KMS key that encrypts the source snapshot. This is required when the snapshot uses a Customer-Managed Encryption Key (CMEK) and the service account must have permissions to use the KMS key. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="raw_key" type="string">
                  The 256-bit customer-supplied encryption key, encoded in RFC 4648 base64, used to decrypt the source snapshot. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rsa_encrypted_key" type="string">
                  Specifies an encryption key wrapped by an RSA public key, as an alternative to supplying the raw key. The RSA public key must be created from a Google Cloud key with algorithm RSA_2048 and padding PKCS1, and the RSA key must be 2048 bits. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sha256" type="string">
                  The SHA256 hash of the customer-supplied encryption key for the source snapshot. This is used to verify the key and is typically computed when a raw key or RSA-encrypted key is provided. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="storage_pool" type="string">
              The URL or name of a storage pool to use for creating this disk. If specified, the disk is provisioned from the given storage pool (e.g., a Hyperdisk storage pool) rather than standard storage. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="interface" type="string">
          The disk interface used for attaching this disk. Valid values are 'NVME' or 'SCSI'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The type of the resource. For a disk in instance properties, this is always 'compute#disk'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="licenses" type="list(string)">
          List of license URIs to apply to this disk, such as operating system licenses. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mode" type="string">
          The access mode of the disk. Valid values are READ_ONLY and READ_WRITE. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="saved_state" type="string">
          The saved state of the disk when creating a machine image. Set to PRESERVED to preserve the disk's state, or DISK_SAVED_STATE_UNSPECIFIED for the default behavior. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="shielded_instance_initial_state" type="object">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="dbs" type="list(object)">
              The list of UEFI secure boot database (db) entries for this shielded instance initial state. Each entry contains the base64-encoded content and file type (e.g., X509, BIN) for a secure boot database. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded certificate content for the UEFI secure boot database (dbs). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The format of the file. Allowed values are: BIN (binary content), X509 (X.509 certificate), or UNDEFINED (unspecified). Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="dbxs" type="list(object)">
              List of X.509 certificates that are revoked for UEFI secure boot (dbx). Each entry includes the certificate content and file type. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded content of the DBX (database exchange) file for the shielded instance initial state, used in the `dbxs` list of a disk's shielded instance configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The file type of the database exchange (dbx) file. Possible values are: BIN, UNDEFINED, X509. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="keks" type="list(object)">
              A list of key exchange key (KEK) certificates used for verifying firmware and kernel signatures during the shielded VM boot process. Each object in the list specifies a certificate's content and file type. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded content of the key exchange key (KEK) for the shielded instance initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The format of the key file. Valid values are BIN (binary), X509 (X.509 certificate), or UNDEFINED if not specified. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="pk" type="object">
              The Platform Key (PK) certificate for the shielded instance's initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  The base64-encoded content of the platform key (pk) certificate for the Shielded VM initial state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  The format of the platform key file. Valid values are BIN, UNDEFINED, and X509. Optional; if omitted, computed by GCP. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source" type="string">
          The full or partial URL of the persistent disk to attach. This is used to reference an existing disk resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of disk. Valid values are PERSISTENT and SCRATCH. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="guest_accelerators" type="list(object)">
      A list of guest accelerator cards' type and count to use for instances created from these properties. Optional.
      <Expandable title="properties">
        <ResponseField name="accelerator_count" type="number">
          The number of accelerator cards of the specified type to attach to the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="accelerator_type" type="string">
          Specifies the accelerator type (GPU) to attach to the instance, in the form of a name or a fully-qualified URL to an accelerator type resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="key_revocation_action_type" type="string">
      KeyRevocationActionType of the instance. Supported options are "STOP" and "NONE". The default value is "NONE" if it is not specified. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Labels to apply to instances that are created from these properties. Optional.
    </ResponseField>
    <ResponseField name="local_ssd_encryption_mode" type="string">
      Specifies which method should be used for encrypting the Local SSDs attached to the VM. Optional.
    </ResponseField>
    <ResponseField name="machine_type" type="string">
      The machine type to use for instances that are created from these properties. This field only accepts a machine type name, for example `n2-standard-4`. If you use the machine type full or partial URL, for example `projects/my-l7ilb-project/zones/us-central1-a/machineTypes/n2-standard-4`, the request will result in an `INTERNAL_ERROR`. Optional.
    </ResponseField>
    <ResponseField name="metadata" type="object">
      A metadata key/value entry. Optional.
      <Expandable title="properties">
        <ResponseField name="fingerprint" type="string">
          Specifies a fingerprint for this request, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve the resource. Optional.
        </ResponseField>
        <ResponseField name="items" type="list(object)">
          Array of key/value pairs. The total size of all keys and values must be less than 512 KB. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of the metadata entry. In Compute Engine metadata, keys are unique and can be used to retrieve the corresponding value. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The value of the metadata item. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kind" type="string">
          Output only. [Output Only] Type of the resource. Always compute#metadata for metadata. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="min_cpu_platform" type="string">
      Minimum cpu/platform to be used by instances. The instance may be scheduled on the specified or newer cpu/platform. Applicable values are the friendly names of CPU platforms, such asminCpuPlatform: "Intel Haswell" orminCpuPlatform: "Intel Sandy Bridge". For more information, read Specifying a Minimum CPU Platform. Optional.
    </ResponseField>
    <ResponseField name="network_interfaces" type="list(object)">
      An array of network access configurations for this interface. Optional.
      <Expandable title="properties">
        <ResponseField name="access_configs" type="list(object)">
          List of access configurations for the network interface. Each configuration defines how the instance is connected to the internet, typically assigning an external IP address. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ipv6" type="string">
              The external IPv6 address assigned to this access configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_ipv6_prefix_length" type="number">
              Specifies the prefix length for the external IPv6 address in the access configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the access configuration. The default and recommended name is 'External NAT'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nat_ip" type="string">
              The external IPv4 address to assign to the instance for this access config. If not specified, an ephemeral address is automatically allocated. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The network tier for this access config. Determines the service tier (Premium or Standard) used for the external IP address. Possible values: FIXED_STANDARD, PREMIUM, STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="public_ptr_domain_name" type="string">
              The public PTR domain name to be set for the external IP address. This field is used when `set_public_ptr` is enabled. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="security_policy" type="string">
              Specifies the URL of a security policy (e.g., Cloud Armor policy) to apply to this access config for the external IP address. This field is only applicable when the access config type is EXTERNAL_NAT. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="set_public_ptr" type="bool">
              Specifies whether a public DNS PTR record should be created for the external IP address associated with this access config. When set to true, a reverse DNS record is automatically provisioned for the public IP. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              Specifies the type of access configuration. Use ONE_TO_ONE_NAT for NAT-based external IPv4 access and DIRECT_IPV6 for direct external IPv6 access. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alias_ip_ranges" type="list(object)">
          A list of alias IP ranges allocated to this network interface. Each alias IP range specifies an IP CIDR range and optionally the subnetwork range name it belongs to. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="ip_cidr_range" type="string">
              The IP CIDR range for this alias IP range, specified in CIDR notation (e.g., 10.0.0.0/24). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork_range_name" type="string">
              The name of the subnetwork secondary range to which the alias IP range belongs. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alias_ipv6_ranges" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="ip_cidr_range" type="string">
              The IPv6 CIDR range for this alias IP range, in CIDR notation (e.g., '2001:db8::/32'). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork_range_name" type="string">
              The name of a subnetwork's secondary IP range to use for this alias IPv6 range. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enable_vpc_scoped_dns" type="bool">
          Enables VPC-scoped DNS for the network interface, allowing DNS queries to be resolved using the VPC's private DNS zones. When set to true, the instance uses the DNS configuration scoped to the VPC network. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fingerprint" type="string">
          The fingerprint of the network interface, a hash of its contents used for optimistic locking and concurrency control. This value is computed automatically by the API. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="igmp_query" type="string">
          The IGMP query mode for the network interface. Set to 'IGMP_QUERY_DISABLED' to disable IGMP queries, or 'IGMP_QUERY_V2' to use IGMPv2. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="internal_ipv6_prefix_length" type="number">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="ipv6_access_configs" type="list(object)">
          A list of access configurations for IPv6 addresses on the network interface. Each configuration specifies how the instance is accessed via IPv6, including the external IPv6 address, network tier, and access config type. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ipv6" type="string">
              The external IPv6 address assigned to the instance's network interface, used for outbound internet connectivity. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_ipv6_prefix_length" type="number">
              The length of the IPv6 prefix assigned to the external IPv6 address. For external IPv6 addresses on Compute Engine, this value is always 96. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the IPv6 access configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nat_ip" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              Network tier for the IPv6 access configuration. Allowed values: FIXED_STANDARD, PREMIUM, STANDARD, STANDARD_OVERRIDES_FIXED_STANDARD. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="public_ptr_domain_name" type="string">
              The DNS domain name for the public PTR record associated with the IPv6 address. This enables reverse DNS lookup for the IPv6 address. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="security_policy" type="string">
              Specifies the security policy to apply to this IPv6 access config. If not set, no security policy is applied. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="set_public_ptr" type="bool">
              Whether to set a public PTR (reverse DNS) record for the IPv6 address of this access config. If true, a PTR record is created; if false, no PTR record is created. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of IPv6 access configuration. Valid values are DIRECT_IPV6 and ONE_TO_ONE_NAT. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ipv6_access_type" type="string">
          The IPv6 access type for the network interface. Allowed values are EXTERNAL (for public IPv6 addresses) and INTERNAL (for private IPv6 addresses). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv6_address" type="string">
          The IPv6 address to assign to the network interface. This is an optional field that specifies a static IPv6 address for the interface when an instance is created from the machine image. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The type of resource. For a network interface, this is always 'compute#networkInterface'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the network interface, such as 'nic0'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network" type="string">
          The name or self-link of the VPC network to attach this network interface to. If not specified, the default network is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_attachment" type="string">
          The URL of the network attachment resource that this network interface is associated with. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_ip" type="string">
          Specifies the primary internal IPv4 address of the network interface. If not set, an ephemeral internal IP is automatically assigned. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="nic_type" type="string">
          The type of network interface card (NIC) used for the instance. Valid values include GVNIC, IDPF, IRDMA, MRDMA, UNSPECIFIED_NIC_TYPE, and VIRTIO_NET. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="parent_nic_name" type="string">
          The name of the parent network interface that this interface is attached to, used when configuring a child or secondary network interface. This corresponds to the `parentNicName` field in the Compute Engine API. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="queue_count" type="number">
          The number of queues for this network interface. This is applicable to only certain instance types and allows configuring multi-queue networking. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_class_id" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="stack_type" type="string">
          The stack type for the network interface. Specifies whether the interface uses IPv4 only, IPv6 only, or both IPv4 and IPv6. Allowed values are IPV4_IPV6, IPV4_ONLY, and IPV6_ONLY. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnetwork" type="string">
          The name or self_link of the subnetwork to attach this network interface to. If not specified, the network's default subnetwork is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vlan" type="number">
          Optional; if omitted, computed by GCP.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_performance_config" type="object">
      An object to configure network performance settings for the instance, such as the total egress bandwidth tier. When omitted, default network performance settings apply. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="total_egress_bandwidth_tier" type="string">
          Specifies the tier to use for total egress bandwidth. Valid values are DEFAULT (default tier) and TIER_1 (higher bandwidth tier). Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_ipv6_google_access" type="string">
      The private IPv6 google access type for VMs. If not specified, use INHERIT_FROM_SUBNETWORK as default. Note that for MachineImage, this is not supported yet. Optional.
    </ResponseField>
    <ResponseField name="reservation_affinity" type="object">
      Specifies the reservations that this instance can consume from. Optional.
      <Expandable title="properties">
        <ResponseField name="consume_reservation_type" type="string">
          Specifies the type of reservation from which this instance can consume resources: ANY_RESERVATION (default),SPECIFIC_RESERVATION, or NO_RESERVATION. See Consuming reserved instances for examples. Optional.
        </ResponseField>
        <ResponseField name="key" type="string">
          Corresponds to the label key of a reservation resource. To target aSPECIFIC_RESERVATION by name, specifygoogleapis.com/reservation-name as the key and specify the name of your reservation as its value. Optional.
        </ResponseField>
        <ResponseField name="values" type="list(string)">
          Corresponds to the label values of a reservation resource. This can be either a name to a reservation in the same project or "projects/different-project/reservations/some-reservation-name" to target a shared reservation in the same zone but in a different project. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the instance. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
    <ResponseField name="resource_policies" type="list(string)">
      Resource policies (names, not URLs) applied to instances created from these properties. Note that for MachineImage, this is not supported yet. Optional.
    </ResponseField>
    <ResponseField name="scheduling" type="object">
      Sets the scheduling options for an Instance. Optional.
      <Expandable title="properties">
        <ResponseField name="automatic_restart" type="bool">
          Specifies whether the instance should be automatically restarted if it is terminated by Compute Engine (not terminated by a user). You can only set the automatic restart option for standard instances.Preemptible instances cannot be automatically restarted. By default, this is set to true so an instance is automatically restarted if it is terminated by Compute Engine. Optional.
        </ResponseField>
        <ResponseField name="availability_domain" type="number">
          Specifies the availability domain to place the instance in. The value must be a number between 1 and the number of availability domains specified in the spread placement policy attached to the instance. Optional.
        </ResponseField>
        <ResponseField name="graceful_shutdown" type="object">
          The configuration for gracefully shutting down the instance. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Opts-in for graceful shutdown. Optional.
            </ResponseField>
            <ResponseField name="max_duration" type="object">
              A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="host_error_timeout_seconds" type="number">
          Specify the time in seconds for host error detection, the value must be within the range of [90, 330] with the increment of 30, if unset, the default behavior of host error recovery will be used. Optional.
        </ResponseField>
        <ResponseField name="instance_termination_action" type="string">
          Specifies the termination action for the instance. Optional.
        </ResponseField>
        <ResponseField name="local_ssd_recovery_timeout" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="location_hint" type="string">
          An opaque location hint used to place the instance close to other resources. This field is for use by internal tools that use the public API. Optional.
        </ResponseField>
        <ResponseField name="max_run_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="min_node_cpus" type="number">
          The minimum number of virtual CPUs this instance will consume when running on a sole-tenant node. Optional.
        </ResponseField>
        <ResponseField name="node_affinities" type="list(object)">
          A set of node affinity and anti-affinity configurations. Refer toConfiguring node affinity for more information. Overrides reservationAffinity. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of the node affinity label. It specifies the node label to match against when scheduling the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              The operator specifies how the node affinity is evaluated against the label values. Valid values are IN, NOT_IN, and OPERATOR_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              Optional; if omitted, computed by GCP.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="on_host_maintenance" type="string">
          Defines the maintenance behavior for this instance. For standard instances, the default behavior is MIGRATE. Forpreemptible instances, the default and only possible behavior is TERMINATE. For more information, see Set VM host maintenance policy. Optional.
        </ResponseField>
        <ResponseField name="on_instance_stop_action" type="object">
          Defines the behaviour for instances with the instance_termination_actionSTOP. Optional.
          <Expandable title="properties">
            <ResponseField name="discard_local_ssd" type="bool">
              If true, the contents of any attached Local SSD disks will be discarded else, the Local SSD data will be preserved when the instance is stopped at the end of the run duration/termination time. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="preemptible" type="bool">
          Defines whether the instance is preemptible. This can only be set during instance creation or while the instance isstopped and therefore, in a `TERMINATED` state. SeeInstance Life Cycle for more information on the possible instance states. Optional.
        </ResponseField>
        <ResponseField name="preemption_notice_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="provisioning_model" type="string">
          Specifies the provisioning model of the instance. Optional.
        </ResponseField>
        <ResponseField name="skip_guest_os_shutdown" type="bool">
          Default is false and there will be 120 seconds between GCE ACPI G2 Soft Off and ACPI G3 Mechanical Off for Standard VMs and 30 seconds for Spot VMs. Optional.
        </ResponseField>
        <ResponseField name="termination_time" type="string">
          Specifies the timestamp, when the instance will be terminated, inRFC3339 text format. If specified, the instance termination action will be performed at the termination time. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_accounts" type="list(object)">
      A list of service accounts with specified scopes. Access tokens for these service accounts are available to the instances that are created from these properties. Use metadata queries to obtain the access tokens for these instances. Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          Email address of the service account to associate with the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scopes" type="list(string)">
          The list of OAuth2 scopes to be granted to the service account for the machine image instance. These scopes control the API access permissions available to the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="shielded_instance_config" type="object">
      A set of Shielded Instance options. Optional.
      <Expandable title="properties">
        <ResponseField name="enable_integrity_monitoring" type="bool">
          Defines whether the instance has integrity monitoring enabled.Enabled by default. Optional.
        </ResponseField>
        <ResponseField name="enable_secure_boot" type="bool">
          Defines whether the instance has Secure Boot enabled.Disabled by default. Optional.
        </ResponseField>
        <ResponseField name="enable_vtpm" type="bool">
          Defines whether the instance has the vTPM enabled.Enabled by default. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="object">
      A set of instance tags. Optional.
      <Expandable title="properties">
        <ResponseField name="fingerprint" type="string">
          Specifies a fingerprint for this request, which is essentially a hash of the tags' contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update tags. You must always provide an up-to-date fingerprint hash in order to update or change tags. To see the latest fingerprint, make get() request to the instance. Optional.
        </ResponseField>
        <ResponseField name="items" type="list(string)">
          An array of tags. Each tag must be 1-63 characters long, and comply with RFC1035. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="workload_identity_config" type="object">
      The workload identity configuration for the instance from which the machine image was created. It defines the workload identity settings applied to the instance. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="identity" type="string">
          The email address of the service account used for the workload identity configuration. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="identity_certificate_enabled" type="bool">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] The resource type, which is alwayscompute#machineImage for machine image. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="label_fingerprint" type="string">
  A fingerprint for the labels being applied to this machine image, which is essentially a hash of the labels set used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update labels. You must always provide an up-to-date fingerprint hash in order to update or change labels. To see the latest fingerprint, make get() request to the machine image. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels to apply to this machine image. These can be later modified by the setLabels method. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="machine_image_encryption_key" type="object">
  The customer-managed encryption key used to encrypt the machine image. This field is output-only, so it reflects the actual key configuration applied to the resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional.
    </ResponseField>
    <ResponseField name="kms_key_service_account" type="string">
      The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional.
    </ResponseField>
    <ResponseField name="raw_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional.
    </ResponseField>
    <ResponseField name="rsa_encrypted_key" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional.
    </ResponseField>
    <ResponseField name="sha256" type="string">
      [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="params" type="object">
  Machine Image parameters Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
      Input only. Resource manager tags to be bound to the machine image. Tag keys and values have the same definition as resource manager tags. Keys and values can be either in numeric format, such as `tagKeys/&#123;tag_key_id&#125;` and `tagValues/&#123;tag_value_id&#125;` or in namespaced format such as `&#123;org_id|project_id&#125;/&#123;tag_key_short_name&#125;` and `&#123;tag_value_short_name&#125;`. The field is ignored (both PUT & PATCH) when empty. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. [Output Only] Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="saved_disks" type="list(object)">
  Output only. An array of Machine Image specific properties for disks attached to the source instance Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="architecture" type="string">
      The CPU architecture of the disk. Allowed values are ARCHITECTURE_UNSPECIFIED, ARM64, and X86_64. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kind" type="string">
      The kind of resource this saved disk represents. For a saved disk in a machine image, this value is always 'compute#savedDisk'. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_disk" type="string">
      The URL of the source disk to be stored in the machine image. This can be a full or partial URL of a persistent disk in the same project or a cross-project disk reference. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="storage_bytes" type="string">
      The storage size of the saved disk in bytes, represented as a string. This is a computed value reflecting the actual storage size of the disk image. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="storage_bytes_status" type="string">
      The status of the storage_bytes field, indicating whether it is currently being updated (UPDATING) or is current and stable (UP_TO_DATE). Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] The URL for this machine image. The server defines this URL. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_disk_encryption_keys" type="list(object)">
  [Input Only] Thecustomer-supplied encryption key of the disks attached to the source instance. Required if the source disk is protected by a customer-supplied encryption key. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="disk_encryption_key" type="object">
      The encryption key details for a source disk, used to decrypt the disk when creating the machine image. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="kms_key_name" type="string">
          The name of the Customer-Managed Encryption Key (CMEK) used to encrypt the source disk. If provided, this key is used to decrypt the disk during image creation. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kms_key_service_account" type="string">
          The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="raw_key" type="string">
          The raw customer-supplied encryption key (CSEK) for the source disk, base64-encoded. For example, a 256-bit key. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rsa_encrypted_key" type="string">
          The RSA-encrypted 256-bit symmetric key used to decrypt the source disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sha256" type="string">
          The SHA-256 hash of the customer-supplied encryption key used to encrypt the source disk. This value is used to verify the key. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source_disk" type="string">
      The name or URL of the source disk to which this encryption key applies. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_instance" type="string">
  The source instance used to create the machine image. You can provide this as a partial or full URL to the resource. For example, the following are valid values: - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance - projects/project/zones/zone/instances/instance Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_instance_properties" type="object">
  DEPRECATED: Please use compute#instanceProperties instead. New properties will not be added to this field. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="can_ip_forward" type="bool">
      Enables instances created based on this machine image to send packets with source IP addresses other than their own and receive packets with destination IP addresses other than their own. If these instances will be used as an IP gateway or it will be set as the next-hop in a Route resource, specify true. If unsure, leave this set tofalse. See theEnable IP forwarding documentation for more information. Optional.
    </ResponseField>
    <ResponseField name="deletion_protection" type="bool">
      Whether the instance created from this machine image should be protected against deletion. Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      An optional text description for the instances that are created from this machine image. Optional.
    </ResponseField>
    <ResponseField name="disks" type="list(object)">
      An array of disks that are associated with the instances that are created from this machine image. Optional.
      <Expandable title="properties">
        <ResponseField name="auto_delete" type="bool">
          Specifies whether the disk is automatically deleted when the instance is deleted. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="boot" type="bool">
          Indicates whether this disk is the boot disk. Only one disk per instance can be a boot disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="device_name" type="string">
          The device name with which the disk is exposed to the instance, as specified in the source instance's disk configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_encryption_key" type="object">
          The customer-supplied encryption key for the disk. This key is used to encrypt or decrypt the disk when creating an instance from the machine image. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kms_key_name" type="string">
              The fully qualified Cloud KMS key name (e.g., projects/PROJECT/locations/REGION/keyRings/RING/cryptoKeys/KEY) used to encrypt the disk. This is referenced when the disk uses a customer-managed encryption key. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kms_key_service_account" type="string">
              The email address of the service account that has permissions to use the Cloud KMS key. This service account is used to encrypt or decrypt the disk when using customer-managed encryption keys. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="raw_key" type="string">
              The raw 256-bit encryption key, base64-encoded, used to encrypt or decrypt the source disk. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rsa_encrypted_key" type="string">
              The customer-supplied encryption key, encrypted using RSA, used to encrypt the disk. This is part of the disk encryption key configuration for the machine image's source instance properties. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sha256" type="string">
              The SHA-256 hash of the customer-supplied encryption key. Used to verify the key's integrity without exposing the key itself. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disk_size_gb" type="string">
          The size of the disk in gigabytes, as configured on the source instance. This value is read from the source instance's disk properties. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disk_type" type="string">
          The type of disk, such as pd-standard, pd-ssd, or pd-balanced. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="guest_os_features" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              The type of guest OS feature for the disk. Valid values include BARE_METAL_LINUX_COMPATIBLE, CCA_CAPABLE, FEATURE_TYPE_UNSPECIFIED, GVNIC, IDPF, MULTI_IP_SUBNET, SECURE_BOOT, SEV_CAPABLE, SEV_LIVE_MIGRATABLE, SEV_LIVE_MIGRATABLE_V2, SEV_SNP_CAPABLE, SNP_SVSM_CAPABLE, TDX_CAPABLE, UEFI_COMPATIBLE, VIRTIO_SCSI_MULTIQUEUE, and WINDOWS. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="index" type="number">
          The zero-based index of this disk within the source instance's disk list. For example, the boot disk typically has index 0. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="interface" type="string">
          The disk interface type. Valid values are `NVME` and `SCSI`. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The resource type identifier. For disk resources, this value is typically 'compute#disk'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="licenses" type="list(string)">
          A list of license URIs associated with the disk, typically representing the operating system license (e.g., Debian, Ubuntu, Windows Server). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mode" type="string">
          The access mode of the disk: READ_ONLY or READ_WRITE. In READ_ONLY mode, the disk can be attached to multiple instances simultaneously, but writes are not allowed; in READ_WRITE mode, the disk can be attached to only one instance and supports read and write operations. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="source" type="string">
          The URL of the source disk or image from which this disk was created in the source instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="storage_bytes" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="storage_bytes_status" type="string">
          The status of the disk's storage byte count, indicating whether the value is being updated (UPDATING) or is current (UP_TO_DATE). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The disk type, which determines the storage medium and behavior. Valid values are PERSISTENT (standard persistent disk) and SCRATCH (local SSD). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="guest_accelerators" type="list(object)">
      A list of guest accelerator cards' type and count to use for instances created from this machine image. Optional.
      <Expandable title="properties">
        <ResponseField name="accelerator_count" type="number">
          The number of accelerator cards of the type specified in `accelerator_type` to attach to the instance. This field is used in the `guest_accelerators` list to specify the quantity of each GPU type. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="accelerator_type" type="string">
          Specifies the accelerator type resource for the guest accelerator. This can be a full or partial URL to an accelerator type, such as 'nvidia-tesla-t4' or 'projects/&#123;project&#125;/zones/&#123;zone&#125;/acceleratorTypes/&#123;accelerator_type&#125;'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="key_revocation_action_type" type="string">
      KeyRevocationActionType of the instance. Supported options are "STOP" and "NONE". The default value is "NONE" if it is not specified. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Labels to apply to instances that are created from this machine image. Optional.
    </ResponseField>
    <ResponseField name="machine_type" type="string">
      The machine type to use for instances that are created from this machine image. Optional.
    </ResponseField>
    <ResponseField name="metadata" type="object">
      A metadata key/value entry. Optional.
      <Expandable title="properties">
        <ResponseField name="fingerprint" type="string">
          Specifies a fingerprint for this request, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve the resource. Optional.
        </ResponseField>
        <ResponseField name="items" type="list(object)">
          Array of key/value pairs. The total size of all keys and values must be less than 512 KB. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="value" type="string">
              The value of this metadata entry, as a string. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kind" type="string">
          Output only. [Output Only] Type of the resource. Always compute#metadata for metadata. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="min_cpu_platform" type="string">
      Minimum cpu/platform to be used by instances created from this machine image. The instance may be scheduled on the specified or newer cpu/platform. Applicable values are the friendly names of CPU platforms, such as minCpuPlatform: "Intel Haswell" orminCpuPlatform: "Intel Sandy Bridge". For more information, read Specifying a Minimum CPU Platform. Optional.
    </ResponseField>
    <ResponseField name="network_interfaces" type="list(object)">
      An array of network access configurations for this interface. Optional.
      <Expandable title="properties">
        <ResponseField name="access_configs" type="list(object)">
          List of access configurations for this network interface, each defining the external IP address settings (e.g., NAT IP, network tier) for the interface. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ipv6" type="string">
              The external IPv6 address for this access configuration. It can be a static IPv6 address or left empty for an ephemeral address automatically assigned by Google Cloud. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_ipv6_prefix_length" type="number">
              The prefix length of the external IPv6 address configured in this access config. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The type of resource. For an access config, this is always 'compute#accessConfig'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the access configuration. If not specified, the default name is 'external-nat'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nat_ip" type="string">
              The external IP address (NAT IP) assigned to the access configuration for the instance when the machine image was created. This is the public IPv4 address used for communication from the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The network tier of the access config, which determines the performance and pricing level for the external IP. Valid values are 'FIXED_STANDARD', 'PREMIUM', 'STANDARD', and 'STANDARD_OVERRIDES_FIXED_STANDARD'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="public_ptr_domain_name" type="string">
              The DNS domain name for the public PTR record associated with the external IP address. If not set, no PTR record is created. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="security_policy" type="string">
              The URL of a Cloud Armor security policy associated with this access config, used to control traffic for the external IP address. This field is only set for external access configs. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="set_public_ptr" type="bool">
              Whether to set the public DNS PTR record for the public IP address of the access config. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of access configuration. Allowed values are ONE_TO_ONE_NAT (default) for NAT, and DIRECT_IPV6 for direct IPv6. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alias_ip_ranges" type="list(object)">
          A list of alias IP ranges configured on the network interface of the source instance. Each alias IP range includes an IP CIDR range and optionally a subnetwork range name. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="ip_cidr_range" type="string">
              The IP CIDR range for the alias IP range, specified in CIDR notation (e.g., '10.0.0.0/24'). This defines the alias IP address range assigned to the network interface. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork_range_name" type="string">
              The name of the subnetwork secondary range to assign an alias IP address from. This field is used when you want to use a secondary IP range of the subnetwork rather than the primary range. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="alias_ipv6_ranges" type="list(object)">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="ip_cidr_range" type="string">
              The IPv6 CIDR range for this alias IPv6 range. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnetwork_range_name" type="string">
              The name of the subnetwork secondary range from which the IPv6 alias IP range is allocated. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enable_vpc_scoped_dns" type="bool">
          Enables VPC-scoped DNS for this network interface. When true, DNS queries use the VPC-scoped DNS domain; when false, the global DNS domain is used. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fingerprint" type="string">
          The fingerprint of the network interface configuration, used for optimistic locking to prevent simultaneous updates. This field is output-only. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="igmp_query" type="string">
          The IGMP query mode for the network interface. Allowed values are IGMP_QUERY_DISABLED (disable IGMP queries) and IGMP_QUERY_V2 (use IGMP version 2). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="internal_ipv6_prefix_length" type="number">
          The prefix length of the internal IPv6 range assigned to the network interface. Typically 96 for internal IPv6 addresses in Google Cloud. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv6_access_configs" type="list(object)">
          A list of IPv6 access configurations for the network interface. Each configuration specifies settings such as the external IPv6 address and network tier. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="external_ipv6" type="string">
              The external IPv6 address for the IPv6 access configuration. This can be assigned automatically or set to a specific address. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="external_ipv6_prefix_length" type="number">
              The prefix length of the external IPv6 address assigned to this interface. Specifies the subnet prefix length, typically 96. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              The API resource type identifier. For IPv6 access configurations, this field is always set to the constant string 'compute#accessConfig'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="nat_ip" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
            <ResponseField name="network_tier" type="string">
              The network tier assigned to the IPv6 address. Valid values are FIXED_STANDARD, PREMIUM, STANDARD, and STANDARD_OVERRIDES_FIXED_STANDARD. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="public_ptr_domain_name" type="string">
              The domain name to use for the PTR (reverse DNS) record of the IPv6 address associated with this access config. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="security_policy" type="string">
              The Cloud Armor security policy associated with this IPv6 access configuration. This policy filters traffic to the external IPv6 address. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="set_public_ptr" type="bool">
              Whether this IPv6 access config will set a public PTR (pointer) record for the IPv6 address. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of the IPv6 access configuration. The possible values are DIRECT_IPV6 for direct IPv6 assignment and ONE_TO_ONE_NAT for NAT-based IPv6 access. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ipv6_access_type" type="string">
          Specifies the IPv6 access type for the network interface. Possible values are EXTERNAL (IPv6 address is publicly accessible) and INTERNAL (IPv6 address is accessible only within the VPC network). Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv6_address" type="string">
          The IPv6 address assigned to this network interface of the source instance, if one was configured. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The type of the resource. For network interfaces in a machine image, this is always 'compute#networkInterface'. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the network interface, such as 'nic0' or 'nic1'. This identifies the interface within the instance's network configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network" type="string">
          The URL of the VPC network resource to which this network interface is attached, typically in the format projects/&#123;project&#125;/global/networks/&#123;network&#125;. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_attachment" type="string">
          The URL of the network attachment to which this network interface is connected, if any. Network attachments enable instances to connect to a Network Connectivity Center service attachment. If set, the value must be a full or partial URL of a network attachment resource. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_ip" type="string">
          The primary internal IPv4 address assigned to this network interface. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="nic_type" type="string">
          The type of network interface controller (NIC) to be used for this network interface. Possible values include GVNIC, VIRTIO_NET, IDPF, IRDMA, MRDMA, and UNSPECIFIED_NIC_TYPE. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="parent_nic_name" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="queue_count" type="number">
          The number of queues for this network interface. This specifies how many queues the virtual network device supports, which can improve network performance. The maximum number of queues depends on the machine type. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_class_id" type="string">
          Optional; if omitted, computed by GCP.
        </ResponseField>
        <ResponseField name="stack_type" type="string">
          Specifies the stack type for the network interface. Valid values are IPV4_IPV6, IPV4_ONLY, and IPV6_ONLY. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnetwork" type="string">
          The name or URL of the subnetwork to which the network interface belongs. This specifies the subnetwork used for the interface within the instance's VPC network. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vlan" type="number">
          The VLAN tag (802.1Q) to use for this network interface, applicable when the interface is attached to a VLAN-based network. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="post_key_revocation_action_type" type="string">
      PostKeyRevocationActionType of the instance. Optional.
    </ResponseField>
    <ResponseField name="scheduling" type="object">
      Sets the scheduling options for an Instance. Optional.
      <Expandable title="properties">
        <ResponseField name="automatic_restart" type="bool">
          Specifies whether the instance should be automatically restarted if it is terminated by Compute Engine (not terminated by a user). You can only set the automatic restart option for standard instances.Preemptible instances cannot be automatically restarted. By default, this is set to true so an instance is automatically restarted if it is terminated by Compute Engine. Optional.
        </ResponseField>
        <ResponseField name="availability_domain" type="number">
          Specifies the availability domain to place the instance in. The value must be a number between 1 and the number of availability domains specified in the spread placement policy attached to the instance. Optional.
        </ResponseField>
        <ResponseField name="graceful_shutdown" type="object">
          The configuration for gracefully shutting down the instance. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Opts-in for graceful shutdown. Optional.
            </ResponseField>
            <ResponseField name="max_duration" type="object">
              A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="host_error_timeout_seconds" type="number">
          Specify the time in seconds for host error detection, the value must be within the range of [90, 330] with the increment of 30, if unset, the default behavior of host error recovery will be used. Optional.
        </ResponseField>
        <ResponseField name="instance_termination_action" type="string">
          Specifies the termination action for the instance. Optional.
        </ResponseField>
        <ResponseField name="local_ssd_recovery_timeout" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="location_hint" type="string">
          An opaque location hint used to place the instance close to other resources. This field is for use by internal tools that use the public API. Optional.
        </ResponseField>
        <ResponseField name="max_run_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="min_node_cpus" type="number">
          The minimum number of virtual CPUs this instance will consume when running on a sole-tenant node. Optional.
        </ResponseField>
        <ResponseField name="node_affinities" type="list(object)">
          A set of node affinity and anti-affinity configurations. Refer toConfiguring node affinity for more information. Overrides reservationAffinity. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The label key used for node affinity, representing a node label key in the node affinity constraint. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              The operator used to evaluate the node affinity. Valid values are IN (match nodes in the list), NOT_IN (match nodes not in the list), and OPERATOR_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              The values used to match against the node affinity key. This list contains the specific label values that the scheduler considers when placing the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="on_host_maintenance" type="string">
          Defines the maintenance behavior for this instance. For standard instances, the default behavior is MIGRATE. Forpreemptible instances, the default and only possible behavior is TERMINATE. For more information, see Set VM host maintenance policy. Optional.
        </ResponseField>
        <ResponseField name="on_instance_stop_action" type="object">
          Defines the behaviour for instances with the instance_termination_actionSTOP. Optional.
          <Expandable title="properties">
            <ResponseField name="discard_local_ssd" type="bool">
              If true, the contents of any attached Local SSD disks will be discarded else, the Local SSD data will be preserved when the instance is stopped at the end of the run duration/termination time. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="preemptible" type="bool">
          Defines whether the instance is preemptible. This can only be set during instance creation or while the instance isstopped and therefore, in a `TERMINATED` state. SeeInstance Life Cycle for more information on the possible instance states. Optional.
        </ResponseField>
        <ResponseField name="preemption_notice_duration" type="object">
          A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="provisioning_model" type="string">
          Specifies the provisioning model of the instance. Optional.
        </ResponseField>
        <ResponseField name="skip_guest_os_shutdown" type="bool">
          Default is false and there will be 120 seconds between GCE ACPI G2 Soft Off and ACPI G3 Mechanical Off for Standard VMs and 30 seconds for Spot VMs. Optional.
        </ResponseField>
        <ResponseField name="termination_time" type="string">
          Specifies the timestamp, when the instance will be terminated, inRFC3339 text format. If specified, the instance termination action will be performed at the termination time. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_accounts" type="list(object)">
      A list of service accounts with specified scopes. Access tokens for these service accounts are available to the instances that are created from this machine image. Use metadata queries to obtain the access tokens for these instances. Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          Email address of the service account. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scopes" type="list(string)">
          List of OAuth 2.0 scopes to be applied to the service account for the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="object">
      A set of instance tags. Optional.
      <Expandable title="properties">
        <ResponseField name="fingerprint" type="string">
          Specifies a fingerprint for this request, which is essentially a hash of the tags' contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update tags. You must always provide an up-to-date fingerprint hash in order to update or change tags. To see the latest fingerprint, make get() request to the instance. Optional.
        </ResponseField>
        <ResponseField name="items" type="list(string)">
          An array of tags. Each tag must be 1-63 characters long, and comply with RFC1035. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] The status of the machine image. One of the following values:INVALID, CREATING, READY,DELETING, and UPLOADING. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="storage_locations" type="list(string)">
  The regional or multi-regional Cloud Storage bucket location where themachine image is stored. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="total_storage_bytes" type="string">
  Output only. [Output Only] Total size of the storage used by the machine image. Optional; if omitted, computed by GCP.
</ResponseField>
