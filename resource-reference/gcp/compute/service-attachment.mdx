---
title: "google_compute_service_attachment"
description: "Real, generated bindings for `google_compute_service_attachment`."
---

`google_compute_service_attachment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service attachment"})

    		ubx.Resource(compute.ServiceAttachment, "ci-runner", compute.ServiceAttachmentConfig{
    			ConnectionPreference: "example",
    			EnableProxyProtocol:  true,
    			Name:                 "ci-runner",
    			NatSubnets:           []string{"example"},
    			TargetService:        "example",
    			ConsumerRejectLists:  []string{"example"},
    			Description:          "Managed by ubx.",
    			DomainNames:          []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceAttachment } from "jsr:@ubx/sdk-google/google/compute/service_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own service attachment" });

      resource(ServiceAttachment, "ci-runner", {
        connectionPreference: "example",
        enableProxyProtocol: true,
        name: "ci-runner",
        natSubnets: ["example"],
        targetService: "example",
        consumerRejectLists: ["example"],
        description: "Managed by ubx.",
        domainNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import ServiceAttachment, ServiceAttachmentConfig

    def describe():
        ubx.intent("payments own service attachment")

        ubx.resource(ServiceAttachment, "ci-runner", ServiceAttachmentConfig(
            connection_preference="example",
            enable_proxy_protocol=True,
            name="ci-runner",
            nat_subnets=["example"],
            target_service="example",
            consumer_reject_lists=["example"],
            description="Managed by ubx.",
            domain_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_service_attachment called "ci-runner", with
    connection preference example, enable proxy protocol true, nat
    subnets a set value, target service example, consumer reject
    lists a set value, description Managed by ubx., domain names a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="connection_preference" type="string" required>
  The connection preference to use for this service attachment. Valid values include "ACCEPT_AUTOMATIC", "ACCEPT_MANUAL". Required.
</ResponseField>

<ResponseField name="consumer_accept_lists" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="connection_limit" type="number" required>
    </ResponseField>
    <ResponseField name="endpoint_url" type="string">
    </ResponseField>
    <ResponseField name="network_url" type="string">
    </ResponseField>
    <ResponseField name="project_id_or_num" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="consumer_reject_lists" type="list(string)">
  An array of projects that are not allowed to connect to this service attachment. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional.
</ResponseField>

<ResponseField name="domain_names" type="list(string)">
  If specified, the domain name will be used during the integration between the PSC connected endpoints and the Cloud DNS. For example, this is a valid domain name: "p.mycompany.com.". Current max number of domain names supported is 1. Optional.
</ResponseField>

<ResponseField name="enable_proxy_protocol" type="bool" required>
  If true, enable the proxy protocol which is for supplying client TCP/IP address data in TCP connections that traverse proxies on their way to destination servers. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Required.
</ResponseField>

<ResponseField name="nat_subnets" type="list(string)" required>
  An array of subnets that is provided for NAT in this service attachment. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="propagated_connection_limit" type="number">
  The number of consumer spokes that connected Private Service Connect endpoints can be propagated to through Network Connectivity Center. This limit lets the service producer limit how many propagated Private Service Connect connections can be established to this service attachment from a single consumer. If the connection preference of the service attachment is ACCEPT_MANUAL, the limit applies to each project or network that is listed in the consumer accept list. If the connection preference of the service attachment is ACCEPT_AUTOMATIC, the limit applies to each project that contains a connected endpoint. If unspecified, the default propagated connection limit is 250. To explicitly send a zero value, set 'send_propagated_connection_limit_if_zero = true'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reconcile_connections" type="bool">
  This flag determines whether a consumer accept/reject list change can reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints. If false, connection policy update will only affect existing PENDING PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched regardless how the connection policy is modified . If true, update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project is added to the reject list. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  URL of the region where the resource resides. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="send_propagated_connection_limit_if_zero" type="bool">
  Controls the behavior of propagated_connection_limit. When false, setting propagated_connection_limit to zero causes the provider to use to the API's default value. When true, the provider will set propagated_connection_limit to zero. Defaults to false. Optional.
</ResponseField>

<ResponseField name="show_nat_ips" type="bool">
  NOTE: This field is temporarily non-functional due to an underlying API issue. Any value provided here will be ignored until the API issue is resolved, expected around 2026-03. [If true, show NAT IPs of all connected endpoints.] Optional.
</ResponseField>

<ResponseField name="target_service" type="string" required>
  The URL of a service serving the endpoint identified by this service attachment. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="connected_endpoints" type="list(object)">
  An array of the consumer forwarding rules connected to this service attachment. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="consumer_network" type="string">
    </ResponseField>
    <ResponseField name="endpoint" type="string">
    </ResponseField>
    <ResponseField name="nat_ips" type="list(string)">
    </ResponseField>
    <ResponseField name="propagated_connection_count" type="number">
    </ResponseField>
    <ResponseField name="psc_connection_id" type="string">
    </ResponseField>
    <ResponseField name="status" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="consumer_accept_lists" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="connection_limit" type="number" required>
    </ResponseField>
    <ResponseField name="endpoint_url" type="string">
    </ResponseField>
    <ResponseField name="network_url" type="string">
    </ResponseField>
    <ResponseField name="project_id_or_num" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. This field is used internally during updates of this resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="propagated_connection_limit" type="number">
  The number of consumer spokes that connected Private Service Connect endpoints can be propagated to through Network Connectivity Center. This limit lets the service producer limit how many propagated Private Service Connect connections can be established to this service attachment from a single consumer. If the connection preference of the service attachment is ACCEPT_MANUAL, the limit applies to each project or network that is listed in the consumer accept list. If the connection preference of the service attachment is ACCEPT_AUTOMATIC, the limit applies to each project that contains a connected endpoint. If unspecified, the default propagated connection limit is 250. To explicitly send a zero value, set 'send_propagated_connection_limit_if_zero = true'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="psc_service_attachment_id" type="list(object)">
  An 128-bit global unique ID of the PSC service attachment. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="high" type="string">
    </ResponseField>
    <ResponseField name="low" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reconcile_connections" type="bool">
  This flag determines whether a consumer accept/reject list change can reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints. If false, connection policy update will only affect existing PENDING PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched regardless how the connection policy is modified . If true, update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project is added to the reject list. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  URL of the region where the resource resides. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
