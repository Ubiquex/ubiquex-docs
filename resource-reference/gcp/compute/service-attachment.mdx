---
title: "google_compute_service_attachment"
description: "A complete, runnable google_compute_service_attachment program, in every SDK language."
---

`google_compute_service_attachment` -- real, typed bindings generated directly from
real Google Discovery Documents, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service attachment"})

    		ubx.Resource(compute.ServiceAttachment, "ci-runner", compute.ServiceAttachmentConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceAttachment } from "./local-sdk/google/sdk/typescript/google/compute/service_attachment.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own service attachment" });

      resource(ServiceAttachment, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.compute import ServiceAttachment, ServiceAttachmentConfig

    def describe():
        ubx.intent("payments own service attachment")

        ubx.resource(ServiceAttachment, "ci-runner", ServiceAttachmentConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_service_attachment called "ci-runner", with
    .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="connected_endpoints" type="list(object)">
  Output only. [Output Only] An array of connections for all the consumers connected to this service attachment. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="consumer_network" type="string">
      The URL of the consumer network that is connected to this service attachment, e.g. projects/&#123;project&#125;/global/networks/&#123;network&#125;. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="endpoint" type="string">
      The URL of the consumer endpoint (for example, a forwarding rule) that is connected to the service attachment. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="endpoint_with_id" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="nat_ips" type="list(string)">
      The list of NAT IP addresses allocated for the consumer endpoint. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="propagated_connection_count" type="number">
      The number of connections that have been propagated to the consumer network from the producer network for this connected endpoint. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="psc_connection_id" type="string">
      The unique identifier for the Private Service Connect connection associated with this connected endpoint. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      The current status of the connected endpoint. Possible values are: ACCEPTED, CLOSED, NEEDS_ATTENTION, PENDING, REJECTED, and STATUS_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="connection_preference" type="string">
  The connection preference of service attachment. The value can be set to ACCEPT_AUTOMATIC. An ACCEPT_AUTOMATIC service attachment is one that always accepts the connection from consumer forwarding rules. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="consumer_accept_lists" type="list(object)">
  Specifies which consumer projects or networks are allowed to connect to the service attachment. Each project or network has a connection limit. A given service attachment can manage connections at either the project or network level. Therefore, both the accept and reject lists for a given service attachment must contain either only projects or only networks or only endpoints. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="connection_limit" type="number">
      The maximum number of connections allowed for the consumer in the accept list. A value of 0 means no limit. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="endpoint_url" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="network_url" type="string">
      The URL of the network that is allowed to connect to this service attachment. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="project_id_or_num" type="string">
      The project ID or project number of a consumer project that is allowed to connect to the service attachment. Used in the consumer accept list to identify the allowed project. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="consumer_reject_lists" type="list(string)">
  Specifies a list of projects or networks that are not allowed to connect to this service attachment. The project can be specified using its project ID or project number and the network can be specified using its URL. A given service attachment can manage connections at either the project or network level. Therefore, both the reject and accept lists for a given service attachment must contain either only projects or only networks. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="domain_names" type="list(string)">
  If specified, the domain name will be used during the integration between the PSC connected endpoints and the Cloud DNS. For example, this is a valid domain name: "p.mycompany.com.". Current max number of domain names supported is 1. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_proxy_protocol" type="bool">
  If true, enable the proxy protocol which is for supplying client TCP/IP address data in TCP connections that traverse proxies on their way to destination servers. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. A hash of the contents stored in this object. This field is used in optimistic locking. This field will be ignored when inserting a ServiceAttachment. An up-to-date fingerprint must be provided in order to patch/update the ServiceAttachment; otherwise, the request will fail with error 412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve the ServiceAttachment. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource type. The server generates this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#serviceAttachment for service attachments. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="metadata" type="map(string)">
  Metadata of the service attachment. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="nat_ips_per_endpoint" type="number">
  The number of NAT IP addresses to be allocated per connected endpoint. If not specified, the default value is 1. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="nat_subnets" type="list(string)">
  An array of URLs where each entry is the URL of a subnet provided by the service producer to use for NAT in this service attachment. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="producer_forwarding_rule" type="string">
  The URL of a forwarding rule with loadBalancingScheme INTERNAL* that is serving the endpoint identified by this service attachment. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="propagated_connection_limit" type="number">
  The number of consumer spokes that connected Private Service Connect endpoints can be propagated to through Network Connectivity Center. This limit lets the service producer limit how many propagated Private Service Connect connections can be established to this service attachment from a single consumer. If the connection preference of the service attachment is ACCEPT_MANUAL, the limit applies to each project or network that is listed in the consumer accept list. If the connection preference of the service attachment is ACCEPT_AUTOMATIC, the limit applies to each project that contains a connected endpoint. If unspecified, the default propagated connection limit is 250. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="psc_service_attachment_id" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="high" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="low" type="string">
      The lower 32 bits of the 64-bit PSC service attachment ID, represented as a decimal string. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reconcile_connections" type="bool">
  This flag determines whether a consumer accept/reject list change can reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints. - If false, connection policy update will only affect existing PENDING PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched regardless how the connection policy is modified . - If true, update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project is added to the reject list. For newly created service attachment, this boolean defaults to false. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the service attachment resides. This field applies only to the region resource. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_service" type="string">
  The URL of a service serving the endpoint identified by this service attachment. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="connected_endpoints" type="list(object)">
  Output only. [Output Only] An array of connections for all the consumers connected to this service attachment. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="consumer_network" type="string">
      The URL of the consumer network that is connected to this service attachment, e.g. projects/&#123;project&#125;/global/networks/&#123;network&#125;. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="endpoint" type="string">
      The URL of the consumer endpoint (for example, a forwarding rule) that is connected to the service attachment. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="endpoint_with_id" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="nat_ips" type="list(string)">
      The list of NAT IP addresses allocated for the consumer endpoint. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="propagated_connection_count" type="number">
      The number of connections that have been propagated to the consumer network from the producer network for this connected endpoint. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="psc_connection_id" type="string">
      The unique identifier for the Private Service Connect connection associated with this connected endpoint. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      The current status of the connected endpoint. Possible values are: ACCEPTED, CLOSED, NEEDS_ATTENTION, PENDING, REJECTED, and STATUS_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="connection_preference" type="string">
  The connection preference of service attachment. The value can be set to ACCEPT_AUTOMATIC. An ACCEPT_AUTOMATIC service attachment is one that always accepts the connection from consumer forwarding rules. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="consumer_accept_lists" type="list(object)">
  Specifies which consumer projects or networks are allowed to connect to the service attachment. Each project or network has a connection limit. A given service attachment can manage connections at either the project or network level. Therefore, both the accept and reject lists for a given service attachment must contain either only projects or only networks or only endpoints. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="connection_limit" type="number">
      The maximum number of connections allowed for the consumer in the accept list. A value of 0 means no limit. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="endpoint_url" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="network_url" type="string">
      The URL of the network that is allowed to connect to this service attachment. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="project_id_or_num" type="string">
      The project ID or project number of a consumer project that is allowed to connect to the service attachment. Used in the consumer accept list to identify the allowed project. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="consumer_reject_lists" type="list(string)">
  Specifies a list of projects or networks that are not allowed to connect to this service attachment. The project can be specified using its project ID or project number and the network can be specified using its URL. A given service attachment can manage connections at either the project or network level. Therefore, both the reject and accept lists for a given service attachment must contain either only projects or only networks. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="domain_names" type="list(string)">
  If specified, the domain name will be used during the integration between the PSC connected endpoints and the Cloud DNS. For example, this is a valid domain name: "p.mycompany.com.". Current max number of domain names supported is 1. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_proxy_protocol" type="bool">
  If true, enable the proxy protocol which is for supplying client TCP/IP address data in TCP connections that traverse proxies on their way to destination servers. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. A hash of the contents stored in this object. This field is used in optimistic locking. This field will be ignored when inserting a ServiceAttachment. An up-to-date fingerprint must be provided in order to patch/update the ServiceAttachment; otherwise, the request will fail with error 412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve the ServiceAttachment. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource type. The server generates this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#serviceAttachment for service attachments. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="metadata" type="map(string)">
  Metadata of the service attachment. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="nat_ips_per_endpoint" type="number">
  The number of NAT IP addresses to be allocated per connected endpoint. If not specified, the default value is 1. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="nat_subnets" type="list(string)">
  An array of URLs where each entry is the URL of a subnet provided by the service producer to use for NAT in this service attachment. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="producer_forwarding_rule" type="string">
  The URL of a forwarding rule with loadBalancingScheme INTERNAL* that is serving the endpoint identified by this service attachment. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="propagated_connection_limit" type="number">
  The number of consumer spokes that connected Private Service Connect endpoints can be propagated to through Network Connectivity Center. This limit lets the service producer limit how many propagated Private Service Connect connections can be established to this service attachment from a single consumer. If the connection preference of the service attachment is ACCEPT_MANUAL, the limit applies to each project or network that is listed in the consumer accept list. If the connection preference of the service attachment is ACCEPT_AUTOMATIC, the limit applies to each project that contains a connected endpoint. If unspecified, the default propagated connection limit is 250. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="psc_service_attachment_id" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="high" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="low" type="string">
      The lower 32 bits of the 64-bit PSC service attachment ID, represented as a decimal string. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reconcile_connections" type="bool">
  This flag determines whether a consumer accept/reject list change can reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints. - If false, connection policy update will only affect existing PENDING PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched regardless how the connection policy is modified . - If true, update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project is added to the reject list. For newly created service attachment, this boolean defaults to false. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the service attachment resides. This field applies only to the region resource. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_service" type="string">
  The URL of a service serving the endpoint identified by this service attachment. Optional; if omitted, computed by GCP.
</ResponseField>
