---
title: "google_compute_network_firewall_policy_iam_policy"
description: "Real, generated bindings for `google_compute_network_firewall_policy_iam_policy`."
---

`google_compute_network_firewall_policy_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    cfg := compute.NetworkFirewallPolicyIamPolicyConfig{
        Name: "example-name",
        PolicyData: "example",
    }
    ubx.Resource(compute.NetworkFirewallPolicyIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { NetworkFirewallPolicyIamPolicy } from
      "jsr:@ubx/sdk-google/google/compute/network_firewall_policy_iam_policy";

    resource(NetworkFirewallPolicyIamPolicy, "example", {
      name: "example-name",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        NetworkFirewallPolicyIamPolicy,
        NetworkFirewallPolicyIamPolicyConfig,
    )

    cfg = NetworkFirewallPolicyIamPolicyConfig(
        name="example-name",
        policy_data="example",
    )
    ubx.resource(NetworkFirewallPolicyIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_network_firewall_policy_iam_policy with
name, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
