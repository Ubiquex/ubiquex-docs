---
title: "google_compute_shared_vpc_service_project"
description: "Real, generated bindings for `google_compute_shared_vpc_service_project`."
---

`google_compute_shared_vpc_service_project` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own shared vpc service project"})

    		ubx.Resource(compute.SharedVpcServiceProject, "ci-runner", compute.SharedVpcServiceProjectConfig{
    			HostProject:    "example",
    			ServiceProject: "example",
    			DeletionPolicy: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SharedVpcServiceProject } from "jsr:@ubx/sdk-google/google/compute/shared_vpc_service_project";

    export default stack("payments", () => {
      intent({ summary: "payments own shared vpc service project" });

      resource(SharedVpcServiceProject, "ci-runner", {
        hostProject: "example",
        serviceProject: "example",
        deletionPolicy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import SharedVpcServiceProject, SharedVpcServiceProjectConfig

    def describe():
        ubx.intent("payments own shared vpc service project")

        ubx.resource(SharedVpcServiceProject, "ci-runner", SharedVpcServiceProjectConfig(
            host_project="example",
            service_project="example",
            deletion_policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_shared_vpc_service_project, with host
    project example, service project example, deletion policy
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="host_project" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_project" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
