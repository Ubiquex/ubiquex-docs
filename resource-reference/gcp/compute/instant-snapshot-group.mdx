---
title: "google_compute_instant_snapshot_group"
description: "A complete, runnable google_compute_instant_snapshot_group program, in every SDK language."
---

`google_compute_instant_snapshot_group` -- real, typed bindings generated directly from
real Google Discovery Documents, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instant snapshot group"})

    		ubx.Resource(compute.InstantSnapshotGroup, "ci-runner", compute.InstantSnapshotGroupConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstantSnapshotGroup } from "./local-sdk/google/sdk/typescript/google/compute/instant_snapshot_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own instant snapshot group" });

      resource(InstantSnapshotGroup, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.compute import InstantSnapshotGroup, InstantSnapshotGroupConfig

    def describe():
        ubx.intent("payments own instant snapshot group")

        ubx.resource(InstantSnapshotGroup, "ci-runner", InstantSnapshotGroupConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_instant_snapshot_group called "ci-runner",
    with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#instantSnapshotGroup for InstantSnapshotGroup resources. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the instant snapshot group resides. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_status" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="consistency_membership_resolution_time" type="string">
      Output only. [Output Only] Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="source_info" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="consistency_group" type="string">
          The consistency group that the source of this instant snapshot group belongs to, if any. This is a computed output field. Computed by GCP after `ship`. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="consistency_group_id" type="string">
          Output only. The identifier of the consistency group associated with this instant snapshot group, used to coordinate group-consistent operations. Computed by GCP after `ship`. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource's resource id. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_consistency_group" type="string">
  The self-link of the consistency group (resource policy) used as the source for the instant snapshots in this group. This field is computed and output only, populated when the group is associated with a consistency group. Optional; if omitted, computed by GCP. **(AI-inferred)**
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="zone" type="string">
  Output only. [Output Only] URL of the zone where the instant snapshot group resides. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#instantSnapshotGroup for InstantSnapshotGroup resources. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the instant snapshot group resides. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_status" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="consistency_membership_resolution_time" type="string">
      Output only. [Output Only] Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="source_info" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="consistency_group" type="string">
          The consistency group that the source of this instant snapshot group belongs to, if any. This is a computed output field. Computed by GCP after `ship`. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="consistency_group_id" type="string">
          Output only. The identifier of the consistency group associated with this instant snapshot group, used to coordinate group-consistent operations. Computed by GCP after `ship`. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource's resource id. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_consistency_group" type="string">
  The self-link of the consistency group (resource policy) used as the source for the instant snapshots in this group. This field is computed and output only, populated when the group is associated with a consistency group. Optional; if omitted, computed by GCP. **(AI-inferred)**
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="zone" type="string">
  Output only. [Output Only] URL of the zone where the instant snapshot group resides. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by GCP.
</ResponseField>
