---
title: "google_compute_beta_region_notification_endpoint"
description: "`google_compute_beta_region_notification_endpoint` is the regional counterpart to a global notification endpoint -- receiving real status-change..."
---

<Warning>
  This is Google Cloud Compute's own **beta** release channel. Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_compute_region_notification_endpoint`.
</Warning>

`google_compute_beta_region_notification_endpoint` is the regional counterpart to a global notification endpoint -- receiving real status-change notifications from a regional health check service the moment a monitored resource's own health changes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own beta region notification endpoint"})

    		ubx.Resource(compute.BetaRegionNotificationEndpoint, "example", compute.BetaRegionNotificationEndpointConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BetaRegionNotificationEndpoint } from "@ubx/sdk-google/google/compute/beta_region_notification_endpoint";

    export default stack("example", () => {
      intent({ summary: "example own beta region notification endpoint" });

      resource(BetaRegionNotificationEndpoint, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute.beta_region_notification_endpoint import BetaRegionNotificationEndpoint, BetaRegionNotificationEndpointConfig

    def describe():
        ubx.intent("example own beta region notification endpoint")

        ubx.resource(BetaRegionNotificationEndpoint, "example", BetaRegionNotificationEndpointConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_beta_region_notification_endpoint called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="grpc_settings" type="object">
  Represents a gRPC setting that describes one gRPC notification endpoint and the retry duration attempting to send notification to this endpoint. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="authority" type="string">
      Optional. If specified, this field is used to set the authority header by the sender of notifications. See https://tools.ietf.org/html/rfc7540#section-8.1.2.3 Optional.
    </ResponseField>
    <ResponseField name="endpoint" type="string">
      Endpoint to which gRPC notifications are sent. This must be a valid gRPCLB DNS name. Optional.
    </ResponseField>
    <ResponseField name="payload_name" type="string">
      Optional. If specified, this field is used to populate the "name" field in gRPC requests. Optional.
    </ResponseField>
    <ResponseField name="resend_interval" type="object">
      A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Optional.
      <Expandable title="properties">
        <ResponseField name="nanos" type="number">
          Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional.
        </ResponseField>
        <ResponseField name="seconds" type="string">
          Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="retry_duration_sec" type="number">
      How much time (in seconds) is spent attempting notification retries until a successful response is received. Default is 30s. Limit is 20m (1200s). Must be a positive number. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] A unique identifier for this resource type. The server generates this identifier. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#notificationEndpoint for notification endpoints. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the notification endpoint resides. This field applies only to the regional resource. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>
