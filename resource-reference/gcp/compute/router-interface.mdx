---
title: "google_compute_router_interface"
description: "Real, generated bindings for `google_compute_router_interface`."
---

`google_compute_router_interface` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own router interface"})

    		ubx.Resource(compute.RouterInterface, "ci-runner", compute.RouterInterfaceConfig{
    			Name:                   "ci-runner",
    			Router:                 "example",
    			InterconnectAttachment: "example",
    			PrivateIpAddress:       "example",
    			Subnetwork:             "example",
    			VpnTunnel:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RouterInterface } from "jsr:@ubx/sdk-google/google/compute/router_interface";

    export default stack("payments", () => {
      intent({ summary: "payments own router interface" });

      resource(RouterInterface, "ci-runner", {
        name: "ci-runner",
        router: "example",
        interconnectAttachment: "example",
        privateIpAddress: "example",
        subnetwork: "example",
        vpnTunnel: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RouterInterface, RouterInterfaceConfig

    def describe():
        ubx.intent("payments own router interface")

        ubx.resource(RouterInterface, "ci-runner", RouterInterfaceConfig(
            name="ci-runner",
            router="example",
            interconnect_attachment="example",
            private_ip_address="example",
            subnetwork="example",
            vpn_tunnel="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_router_interface called "ci-runner", with
    router example, interconnect attachment example, private ip
    address example, subnetwork example, vpn tunnel example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="interconnect_attachment" type="string">
  Optional.
</ResponseField>

<ResponseField name="ip_range" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_version" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="private_ip_address" type="string">
  Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="redundant_interface" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="router" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnetwork" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpn_tunnel" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_range" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_version" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="redundant_interface" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
