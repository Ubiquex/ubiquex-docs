---
title: "google_compute_network"
description: "`google_compute_network` is a real Virtual Private Cloud network -- a global, software-defined network that a project's own subnets, firewall rules..."
---

`google_compute_network` is a real Virtual Private Cloud network -- a global, software-defined network that a project's own subnets, firewall rules, and routes all live inside, the top-level container every other real networking resource here is scoped underneath.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network"})

    		ubx.Resource(compute.Network, "ci-runner", compute.NetworkConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Network } from "jsr:@ubx/sdk-google/google/compute/network";

    export default stack("payments", () => {
      intent({ summary: "payments own network" });

      resource(Network, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import Network, NetworkConfig

    def describe():
        ubx.intent("payments own network")

        ubx.resource(Network, "ci-runner", NetworkConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_network called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="auto_create_subnetworks" type="bool">
  Must be set to create a VPC network. If not set, a legacy network is created. When set to true, the VPC network is created in auto mode. When set to false, the VPC network is created in custom mode. An auto mode VPC network starts with one subnet per region. Each subnet has a predetermined range as described inAuto mode VPC network IP ranges. For custom mode VPC networks, you can add subnets using the subnetworksinsert method. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this field when you create the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enable_ula_internal_ipv6" type="bool">
  Enable ULA internal ipv6 on this network. Enabling this feature will assign a /48 from google defined ULA prefix fd20::/20. . Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="firewall_policy" type="string">
  Output only. [Output Only] URL of the firewall policy the network is associated with. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="gateway_ipv4" type="string">
  [Output Only] The gateway address for default routing out of the network, selected by Google Cloud. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="internal_ipv6_range" type="string">
  When enabling ula internal ipv6, caller optionally can specify the /48 range they want from the google defined ULA prefix fd20::/20. The input must be a valid /48 ULA IPv6 address and must be within the fd20::/20. Operation will fail if the speficied /48 is already in used by another resource. If the field is not speficied, then a /48 range will be randomly allocated from fd20::/20 and returned via this field. . Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ipv4_range" type="string">
  Deprecated in favor of subnet mode networks. The range of internal addresses that are legal on this network. This range is aCIDR specification, for example:192.168.0.0/16. Provided by the client when the network is created. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#network for networks. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="mtu" type="number">
  Maximum Transmission Unit in bytes. The minimum value for this field is 1300 and the maximum value is 8896. The suggested value is 1500, which is the default MTU used on the Internet, or 8896 if you want to use Jumbo frames. If unspecified, the value defaults to 1460. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a lowercase letter, and all following characters (except for the last character) must be a dash, lowercase letter, or digit. The last character must be a lowercase letter or digit. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network_firewall_policy_enforcement_order" type="string">
  The network firewall policy enforcement order. Can be either AFTER_CLASSIC_FIREWALL or BEFORE_CLASSIC_FIREWALL. Defaults to AFTER_CLASSIC_FIREWALL if the field is not specified. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network_profile" type="string">
  A full or partial URL of the network profile to apply to this network. This field can be set only at resource creation time. For example, the following are valid URLs: - https://www.googleapis.com/compute/&#123;api_version&#125;/projects/&#123;project_id&#125;/global/networkProfiles/&#123;network_profile_name&#125; - projects/&#123;project_id&#125;/global/networkProfiles/&#123;network_profile_name&#125; Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="params" type="object">
  Additional network parameters. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="resource_manager_tags" type="map(string)">
      Tag keys/values directly bound to this resource. Tag keys and values have the same definition as resource manager tags. The field is allowed for INSERT only. The keys/values to set on the resource should be specified in either ID &#123; : &#125; or Namespaced format &#123; : &#125;. For example the following are valid inputs: * &#123;"tagKeys/333" : "tagValues/444", "tagKeys/123" : "tagValues/456"&#125; * &#123;"123/environment" : "production", "345/abc" : "xyz"&#125; Note: * Invalid combinations of ID & namespaced format is not supported. For instance: &#123;"123/environment" : "tagValues/444"&#125; is invalid. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="peerings" type="list(object)">
  Output only. [Output Only] A list of network peerings for the resource. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="auto_create_routes" type="bool">
      Whether to automatically create routes for the peer network in this VPC peering connection. When true, routes are automatically generated for destinations in the peered network; when false, no automatic route creation occurs. This field is deprecated in favor of export_custom_routes and import_custom_routes. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="connection_status" type="object">
      Optional; if omitted, computed by GCP.
      <Expandable title="properties">
        <ResponseField name="consensus_state" type="object">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="delete_status" type="string">
              The deletion status of this VPC network peering connection as part of the consensus state between local and peer networks. Possible values: DELETE_ACKNOWLEDGED, DELETE_STATUS_UNSPECIFIED, LOCAL_CANCEL_REQUESTED, LOCAL_DELETE_REQUESTED, PEER_CANCEL_REQUESTED, PEER_DELETE_REQUESTED. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="update_status" type="string">
              Represents the update status of the peering consensus state. Possible values: IN_SYNC (no pending updates), PENDING_LOCAL_ACKNOWLEDMENT (waiting for local acknowledgment), PENDING_PEER_ACKNOWLEDGEMENT (waiting for peer acknowledgment), UPDATE_STATUS_UNSPECIFIED (unset). Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="traffic_configuration" type="object">
          The traffic configuration of the peering connection, providing the current traffic state and any associated details. Optional; if omitted, computed by GCP. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="export_custom_routes_to_peer" type="bool">
              Indicates whether the VPC network exports its custom routes (routes not automatically generated from subnets) to the peered network. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="export_subnet_routes_with_public_ip_to_peer" type="bool">
              This boolean flag controls whether subnet routes with public IP addresses are exported to the peer network. When true, the VPC network exports routes to its peer for subnets that have public IPs, allowing the peer to reach these subnets via the peering connection. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="import_custom_routes_from_peer" type="bool">
              When true, this peering connection imports custom routes (non-subnet routes) from the peer network. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="import_subnet_routes_with_public_ip_from_peer" type="bool">
              A boolean flag indicating whether the peering connection imports subnet routes with public IP addresses from the peer network. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="stack_type" type="string">
              The IP stack type for the traffic over the peering connection, either IPV4_ONLY or IPV4_IPV6. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="update_strategy" type="string">
          Determines how updates to the peering are applied. CONSENSUS requires both networks to agree on updates, INDEPENDENT allows either side to update without the other's approval, and UNSPECIFIED indicates no strategy has been set. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="exchange_subnet_routes" type="bool">
      Controls whether subnet routes are exchanged with the peered network. When true (default), subnet routes are automatically advertised to the peer network; setting to false disables the exchange. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="export_custom_routes" type="bool">
      Whether this network exports custom routes (such as dynamic routes and routes learned from other networks) to the peered network. When true, these routes are advertised to the peer. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="export_subnet_routes_with_public_ip" type="bool">
      Indicates whether subnet routes with public IP addresses are exported to the peer network. When set to true, the peer network can learn these routes; when false, they are not exported. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="import_custom_routes" type="bool">
      Indicates whether custom routes from the peered network are imported into this network's route table. Set to true to enable importing custom routes. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="import_subnet_routes_with_public_ip" type="bool">
      Whether to import subnet routes with public IP addresses from the peer network. When false, only private subnet routes are imported. Defaults to false. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="network" type="string">
      The name or self_link of the peer network to create the peering with. This can be a network in the same project or in a different project, and must be specified as a URL if cross-project. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="peer_mtu" type="number">
      The Maximum Transmission Unit (MTU) for the VPC network peering connection, specified in bytes. This is the MTU of the peer network and determines the maximum size of packets that can be sent over the peering link without fragmentation. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="stack_type" type="string">
      The stack type for the peering, either IPV4_ONLY or IPV4_IPV6, controlling whether IPv6 traffic is supported over the peering. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state" type="string">
      Current state of the VPC network peering connection, either ACTIVE or INACTIVE. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state_details" type="string">
      The output-only details of the VPC network peering state, providing additional context about the current peering status, such as the reason for the state. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="update_strategy" type="string">
      Specifies the update strategy for the VPC peering. Possible values are 'CONSENSUS' (updates require agreement from both peering sides), 'INDEPENDENT' (updates can be made independently by either side), and 'UNSPECIFIED' (default, behavior is determined by the API). Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="routing_config" type="object">
  A routing configuration attached to a network resource. The message includes the list of routers associated with the network, and a flag indicating the type of routing behavior to enforce network-wide. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="bgp_always_compare_med" type="bool">
      Enable comparison of Multi-Exit Discriminators (MED) across routes with different neighbor ASNs when using the STANDARD BGP best path selection algorithm. Optional.
    </ResponseField>
    <ResponseField name="bgp_best_path_selection_mode" type="string">
      The BGP best path selection algorithm to be employed within this network for dynamic routes learned by Cloud Routers. Can be LEGACY (default) or STANDARD. Optional.
    </ResponseField>
    <ResponseField name="bgp_inter_region_cost" type="string">
      Allows to define a preferred approach for handling inter-region cost in the selection process when using the STANDARD BGP best path selection algorithm. Can be DEFAULT orADD_COST_TO_MED. Optional.
    </ResponseField>
    <ResponseField name="effective_bgp_always_compare_med" type="bool">
      Output only. [Output Only] Effective value of the bgp_always_compare_med field. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="effective_bgp_inter_region_cost" type="string">
      Output only. [Output Only] Effective value of the bgp_inter_region_cost field. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="routing_mode" type="string">
      The network-wide routing mode to use. If set to REGIONAL, this network's Cloud Routers will only advertise routes with subnets of this network in the same region as the router. If set toGLOBAL, this network's Cloud Routers will advertise routes with all subnets of this network, across regions. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource with the resource id. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="subnetworks" type="list(string)">
  [Output Only] Server-defined fully-qualified URLs for all subnetworks in this VPC network. Optional; if omitted, computed by GCP.
</ResponseField>
