---
title: "google_compute_firewall_policy"
description: "`google_compute_firewall_policy` is a real, reusable set of firewall rules that can be attached to a VPC network -- unlike a plain firewall rule..."
---

`google_compute_firewall_policy` is a real, reusable set of firewall rules that can be attached to a VPC network -- unlike a plain firewall rule, a policy's own rules can be shared across multiple networks and evaluated together with a defined priority order.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own firewall policy"})

    		ubx.Resource(compute.FirewallPolicy, "example", compute.FirewallPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirewallPolicy } from "@ubx/sdk-google/google/compute/firewall_policy";

    export default stack("example", () => {
      intent({ summary: "example own firewall policy" });

      resource(FirewallPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import FirewallPolicy, FirewallPolicyConfig

    def describe():
        ubx.intent("example own firewall policy")

        ubx.resource(FirewallPolicy, "example", FirewallPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_firewall_policy called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="associations" type="list(object)">
  A list of associations that belong to this firewall policy. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="attachment_target" type="string">
      The full resource name of the folder or organization that this firewall policy is associated with. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="display_name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="firewall_policy_id" type="string">
      The unique ID of the firewall policy to associate with the target. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="short_name" type="string">
      A short, unique identifier for the association within the firewall policy, used for display and management purposes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Deprecated, please use short name instead. User-provided name of the Organization firewall policy. The name should be unique in the organization in which the firewall policy is created. This field is not applicable to network firewall policies. This name must be set on creation and cannot be changed. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Specifies a fingerprint for this resource, which is essentially a hash of the metadata's contents and used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update metadata. You must always provide an up-to-date fingerprint hash in order to update or change metadata, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make get() request to the firewall policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. For Organization Firewall Policies it's a [Output Only] numeric ID allocated by Google Cloud which uniquely identifies the Organization Firewall Policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="packet_mirroring_rules" type="list(object)">
  A list of packet mirroring rules that belong to this policy. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="description" type="string">
      An optional user-defined description for this packet mirroring rule within the firewall policy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="direction" type="string">
      Specifies the direction of traffic to which this packet mirroring rule applies. Valid values are 'INGRESS' and 'EGRESS'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disabled" type="bool">
      Whether this packet mirroring rule is disabled. When set to true, the rule is not enforced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_logging" type="bool">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="kind" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="match" type="object">
      The match criteria that specify which network traffic is subject to this packet mirroring rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dest_address_groups" type="list(string)">
          List of destination address groups that define the destination IP addresses to match in the packet mirroring rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_fqdns" type="list(string)">
          A list of destination fully qualified domain names (FQDNs) used by the match criteria for packet mirroring rules. Traffic mirrored by the rule must be destined to one of the specified FQDNs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_ip_ranges" type="list(string)">
          A list of destination IP address ranges in CIDR format. Traffic whose destination IP falls within any of these ranges matches this packet mirroring rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_network_context" type="string">
          Specifies the network context of the destination traffic that this rule matches. Valid values are INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_network_type" type="string">
          The destination network type to match for packet mirroring. The allowed values are: INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_region_codes" type="list(string)">
          A list of destination region codes (for example, 'us-central1') that the packet mirroring rule matches on. The rule applies only to packets with a destination in one of the specified regions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_threat_intelligences" type="list(string)">
          A list of threat intelligence feeds to match against the destination IP address of the traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="layer4_configs" type="list(object)">
          A list of layer 4 configurations used to match packets based on protocol and port. Each configuration specifies the IP protocol (such as TCP, UDP, ICMP) and an optional list of destination ports, allowing the firewall policy rule to apply to specific traffic types. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="ip_protocol" type="string">
              The IP protocol to match. Accepts protocol names such as tcp, udp, icmp, esp, ah, ipip, sctp, a numeric IP protocol number, or 'all' to match any protocol. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ports" type="list(string)">
              Specifies the list of destination port numbers or ranges to match for this layer4 configuration. Each entry is a string, e.g., '80' or '8000-9000'. If omitted, the rule matches all ports. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="src_address_groups" type="list(string)">
          A list of full resource URLs to network address groups used as the source address match. Packets from sources that belong to these groups are mirrored. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_fqdns" type="list(string)">
          The list of fully qualified domain names (FQDNs) that the rule uses as the source match condition. Traffic originating from these FQDNs matches the rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_ip_ranges" type="list(string)">
          A list of source IP address ranges in CIDR notation that the packet mirroring rule matches on. If specified, the rule only applies to traffic originating from these ranges. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_network_context" type="string">
          Specifies the source network context for matching traffic. Allowed values are: INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_network_type" type="string">
          The type of source network that the rule matches. Valid values are INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_networks" type="list(string)">
          List of source CIDR networks to match. Traffic from any of these source IP ranges will match the rule and be mirrored. Each entry must be a valid CIDR block, for example: '10.0.0.0/8'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_region_codes" type="list(string)">
          Specifies the list of source region codes (e.g., 'us-central1', 'europe-west1') from which traffic must originate for the rule to apply. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_secure_tags" type="list(object)">
          List of secure tags used to match source resources for this packet mirroring rule. Traffic originating from any resource carrying one of these tags will be mirrored. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the Secure Tag value to match in the source of the packet. This is the full resource name, such as `tagValues/123456789`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              The state of the secure tag, indicating whether it is currently effective or ineffective. Allowed values are 'EFFECTIVE' and 'INEFFECTIVE'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="src_threat_intelligences" type="list(string)">
          A list of threat intelligence sources used to match the source IP address of traffic. If specified, the rule applies only to traffic whose source IP is identified by one of the listed threat intelligence feeds. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="priority" type="number">
      The priority of this packet mirroring rule. Lower values are evaluated first. Must be an integer between 0 and 65535. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_name" type="string">
      The name of the packet mirroring rule. This is a user-provided name used to identify the rule in the firewall policy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_tuple_count" type="number">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="security_profile_group" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="target_forwarding_rules" type="list(string)">
      List of forwarding rule URLs that are the target of the packet mirroring rule. Traffic sent to these forwarding rules is mirrored to the collector. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_resources" type="list(string)">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="target_secure_tags" type="list(object)">
      A list of secure tags that identify the target instances whose traffic will be mirrored by this packet mirroring rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the secure tag. Secure tags are used to identify which instances or resources this packet mirroring rule applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The state of the secure tag, indicating whether the tag is currently effective or ineffective. Valid values are EFFECTIVE and INEFFECTIVE. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target_service_accounts" type="list(string)">
      A list of service accounts that the rule targets. Instances with any of these service accounts will have their traffic affected by this packet mirroring rule. If not specified, the rule applies to all instances. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_type" type="string">
      The type of target to which the packet mirroring rule applies. Valid values are INSTANCES and INTERNAL_MANAGED_LB. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tls_inspect" type="bool">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="parent" type="string">
  Output only. [Output Only] The parent of the firewall policy. This field is not applicable to network firewall policies. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="policy_type" type="string">
  The type of the firewall policy. This field can be one of VPC_POLICY, RDMA_ROCE_POLICY or ULL_POLICY. Note: if not specified then VPC_POLICY will be used. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] URL of the region where the regional firewall policy resides. This field is not applicable to global firewall policies. You must specify this field as part of the HTTP request URL. It is not settable as a field in the request body. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rule_tuple_count" type="number">
  Output only. [Output Only] Total count of all firewall policy rule tuples. A firewall policy can not exceed a set number of tuples. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  A list of rules that belong to this policy. There must always be a default rule (rule with priority 2147483647 and match "*"). If no rules are provided when creating a firewall policy, a default rule with action "allow" will be added. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="direction" type="string">
      The direction in which the rule applies. Possible values are EGRESS for outbound traffic and INGRESS for inbound traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disabled" type="bool">
      Whether this rule is disabled. When set to true, the rule is not enforced. Defaults to false. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_logging" type="bool">
      Whether to enable logging for this firewall policy rule. When set to true, traffic matching the rule is logged to Cloud Logging. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kind" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="match" type="object">
      The match block specifies the criteria that determine which traffic the firewall policy rule applies to, including source and destination IP ranges, protocols, and ports. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dest_address_groups" type="list(string)">
          The list of destination address groups that the rule matches. Address groups are named collections of IP addresses, and this field specifies which groups the rule’s destination addresses must belong to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_fqdns" type="list(string)">
          A list of destination fully qualified domain names (FQDNs) to match for this firewall policy rule. Traffic destined to any of these FQDNs is evaluated against the rule's action. This field is available only for hierarchical firewall policy rules (e.g., organization or folder firewall policies), not for standard VPC firewall rules. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_ip_ranges" type="list(string)">
          A list of destination IP ranges (in CIDR format) to which this firewall policy rule applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_network_context" type="string">
          Specifies the destination network context for the firewall rule match. Valid values are INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_network_type" type="string">
          The type of destination network for the firewall policy rule. Allowed values are: INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. This field is used in the match condition to specify the network type of the destination. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_region_codes" type="list(string)">
          The list of destination region codes (e.g., 'us-central1') that the rule matches. If set, the rule applies only to traffic destined for the specified GCP regions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dest_threat_intelligences" type="list(string)">
          A list of threat intelligence types used to match traffic based on the destination IP's known threat category. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="layer4_configs" type="list(object)">
          List of layer 4 configurations used to match traffic by IP protocol and port(s). Each object specifies an IP protocol and an optional list of ports. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="ip_protocol" type="string">
              The IP protocol to match for this layer-4 config. Supported values include tcp, udp, icmp, esp, ah, ipip, or a custom protocol number. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ports" type="list(string)">
              An optional list of destination ports or port ranges (e.g., '80' or '8000-8080') to which this layer4 config applies. This field is only applicable when the protocol is TCP or UDP; if not specified, the rule applies to all ports. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="src_address_groups" type="list(string)">
          A list of source address groups (as URLs or names) that the source IP of the packet must belong to for the rule to match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_fqdns" type="list(string)">
          A list of source fully qualified domain names (FQDNs) that this firewall policy rule matches. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_ip_ranges" type="list(string)">
          A list of source IP address ranges in CIDR notation. The firewall rule matches traffic originating from any of the specified IP ranges. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_network_context" type="string">
          Specifies the network context of the source of traffic to match. Allowed values are INTERNET (traffic from the internet), INTRA_VPC (traffic within the same VPC), NON_INTERNET (traffic not from the internet), UNSPECIFIED (no context specified), and VPC_NETWORKS (traffic from other VPC networks). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_network_type" type="string">
          The source network type used for the firewall policy rule's match condition. Valid values are: INTERNET, INTRA_VPC, NON_INTERNET, UNSPECIFIED, and VPC_NETWORKS. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_networks" type="list(string)">
          A list of source networks (by name or URL) that the rule matches. Traffic originating from any of these networks is considered a match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_region_codes" type="list(string)">
          A list of source region codes (e.g., 'us-central1') for the firewall policy rule to match. Traffic originating from any of the specified source regions will match the rule; if left empty, the rule matches all source regions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="src_secure_tags" type="list(object)">
          List of secure tags that the source of the traffic must match for the rule to apply. Each object requires a `name` (the secure tag name) and optionally a `scope` (the scope of the tag, e.g., 'global' or a folder ID). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the secure tag to match for the source of the traffic. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              State of the secure tag, indicating whether it is effective (EFFECTIVE) or ineffective (INEFFECTIVE) for the firewall rule's source tag matching. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="src_threat_intelligences" type="list(string)">
          Threat intelligence sources to match against the source IP address. This can include lists such as known malicious IPs, public cloud ranges, scanners, or proxies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="priority" type="number">
      Priority for this firewall policy rule, determining evaluation order; lower numbers have higher precedence and are evaluated first. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_name" type="string">
      An optional name for the rule. This field is not a unique identifier. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_tuple_count" type="number">
      The number of tuples in the firewall policy rule, representing the combinations of source ranges, destination ranges, and port ranges. This is a computed value returned by the API. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="security_profile_group" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="target_forwarding_rules" type="list(string)">
      A list of target forwarding rule URLs that this firewall policy rule applies to. If unspecified, the rule applies to all forwarding rules in the policy's network. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_resources" type="list(string)">
      A list of network resource URLs to which this firewall policy rule applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_secure_tags" type="list(object)">
      A list of secure tags that determine which resources the firewall policy rule applies to. The rule targets resources that have any of the specified secure tags. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
        <ResponseField name="state" type="string">
          The state of the secure tag, indicating whether it is currently in effect (EFFECTIVE) or not (INEFFECTIVE) for the firewall policy rule's target. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target_service_accounts" type="list(string)">
      A list of service account email addresses that the firewall rule applies to. If specified, the rule applies only to instances whose service account matches one of these accounts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_type" type="string">
      Specifies the type of target that the firewall policy rule applies to. Allowed values are `INSTANCES` and `INTERNAL_MANAGED_LB`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tls_inspect" type="bool">
      If true, this rule is used for TLS inspection. When enabled, the rule's action must be either 'apply' or 'deny'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  [Output Only] Server-defined URL for the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource with the resource id. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="short_name" type="string">
  User-provided name of the Organization firewall policy. The name should be unique in the organization in which the firewall policy is created. This field is not applicable to network firewall policies. This name must be set on creation and cannot be changed. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>
