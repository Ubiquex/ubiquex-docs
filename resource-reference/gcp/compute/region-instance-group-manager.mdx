---
title: "google_compute_region_instance_group_manager"
description: "Real, generated bindings for `google_compute_region_instance_group_manager`."
---

`google_compute_region_instance_group_manager` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region instance group manager"})

    		ubx.Resource(compute.RegionInstanceGroupManager, "ci-runner", compute.RegionInstanceGroupManagerConfig{
    			BaseInstanceName:            "example-base-instance-name",
    			Name:                        "ci-runner",
    			Description:                 "Managed by ubx.",
    			ListManagedInstancesResults: "example",
    			TargetPools:                 []string{"example"},
    			WaitForInstances:            true,
    			WaitForInstancesStatus:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionInstanceGroupManager } from "jsr:@ubx/sdk-google/google/compute/region_instance_group_manager";

    export default stack("payments", () => {
      intent({ summary: "payments own region instance group manager" });

      resource(RegionInstanceGroupManager, "ci-runner", {
        baseInstanceName: "example-base-instance-name",
        name: "ci-runner",
        description: "Managed by ubx.",
        listManagedInstancesResults: "example",
        targetPools: ["example"],
        waitForInstances: true,
        waitForInstancesStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RegionInstanceGroupManager, RegionInstanceGroupManagerConfig

    def describe():
        ubx.intent("payments own region instance group manager")

        ubx.resource(RegionInstanceGroupManager, "ci-runner", RegionInstanceGroupManagerConfig(
            base_instance_name="example-base-instance-name",
            name="ci-runner",
            description="Managed by ubx.",
            list_managed_instances_results="example",
            target_pools=["example"],
            wait_for_instances=True,
            wait_for_instances_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_instance_group_manager called
    "ci-runner", with base instance name example-base-instance-name,
    description Managed by ubx., list managed instances results
    example, target pools a set value, wait for instances true, wait
    for instances status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="all_instances_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="labels" type="map(string)">
    </ResponseField>
    <ResponseField name="metadata" type="map(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="auto_healing_policies" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="health_check" type="string" required>
    </ResponseField>
    <ResponseField name="initial_delay_sec" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="base_instance_name" type="string" required>
  The base instance name to use for instances in this group. The value must be a valid RFC1035 name. Supported characters are lowercase letters, numbers, and hyphens (-). Instances are named by appending a hyphen and a random four-character string to the base instance name. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional textual description of the instance group manager. Optional.
</ResponseField>

<ResponseField name="distribution_policy_target_shape" type="string">
  The shape to which the group converges either proactively or on resize events (depending on the value set in updatePolicy.instanceRedistributionType). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="distribution_policy_zones" type="set(string)">
  The distribution policy for this managed instance group. You can specify one or more values. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_flexibility_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="instance_selections" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="machine_types" type="set(string)" required>
        </ResponseField>
        <ResponseField name="name" type="string" required>
        </ResponseField>
        <ResponseField name="rank" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_lifecycle_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="default_action_on_failure" type="string">
    </ResponseField>
    <ResponseField name="force_update_on_repair" type="string">
    </ResponseField>
    <ResponseField name="on_failed_health_check" type="string">
    </ResponseField>
    <ResponseField name="on_repair" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="allow_changing_zone" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="list_managed_instances_results" type="string">
  Pagination behavior of the listManagedInstances API method for this managed instance group. Valid values are: "PAGELESS", "PAGINATED". If PAGELESS (default), Pagination is disabled for the group's listManagedInstances API method. maxResults and pageToken query parameters are ignored and all instances are returned in a single response. If PAGINATED, pagination is enabled, maxResults and pageToken query parameters are respected. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the instance group manager. Must be 1-63 characters long and comply with RFC1035. Supported characters include lowercase letters, numbers, and hyphens. Required.
</ResponseField>

<ResponseField name="named_port" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="port" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project in which the resource belongs. If it is not provided, the provider project is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The region where the managed instance group resides. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_policies" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="workload_policy" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="standby_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="initial_delay_sec" type="number">
    </ResponseField>
    <ResponseField name="mode" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stateful_disk" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="delete_rule" type="string">
    </ResponseField>
    <ResponseField name="device_name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stateful_external_ip" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="delete_rule" type="string">
    </ResponseField>
    <ResponseField name="interface_name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stateful_internal_ip" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="delete_rule" type="string">
    </ResponseField>
    <ResponseField name="interface_name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_pools" type="set(string)">
  The full URL of all target pools to which new instances in the group are added. Updating the target pools attribute does not affect existing instances. Optional.
</ResponseField>

<ResponseField name="target_size" type="number">
  The target number of running instances for this managed instance group. This value should always be explicitly set unless this resource is attached to an autoscaler, in which case it should never be set. Defaults to 0. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_size_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="mode" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_stopped_size" type="number">
  The target number of stopped instances for this managed instance group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_suspended_size" type="number">
  The target number of suspended instances for this managed instance group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="instance_redistribution_type" type="string">
    </ResponseField>
    <ResponseField name="max_surge_fixed" type="number">
    </ResponseField>
    <ResponseField name="max_surge_percent" type="number">
    </ResponseField>
    <ResponseField name="max_unavailable_fixed" type="number">
    </ResponseField>
    <ResponseField name="max_unavailable_percent" type="number">
    </ResponseField>
    <ResponseField name="minimal_action" type="string" required>
    </ResponseField>
    <ResponseField name="most_disruptive_allowed_action" type="string">
    </ResponseField>
    <ResponseField name="replacement_method" type="string">
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="instance_template" type="string" required>
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="target_size" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="fixed" type="number">
        </ResponseField>
        <ResponseField name="percent" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="wait_for_instances" type="bool">
  Whether to wait for all instances to be created/updated before returning. Note that if this is set to true and the operation does not succeed, Terraform will continue trying until it times out. Optional.
</ResponseField>

<ResponseField name="wait_for_instances_status" type="string">
  When used with wait_for_instances specifies the status to wait for. When STABLE is specified this resource will wait until the instances are stable before returning. When UPDATED is set, it will wait for the version target to be reached and any per instance configs to be effective and all instances configs to be effective as well as all instances to be stable before returning. Optional.
</ResponseField>

## Output properties

<ResponseField name="all_instances_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="labels" type="map(string)">
    </ResponseField>
    <ResponseField name="metadata" type="map(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="auto_healing_policies" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="health_check" type="string" required>
    </ResponseField>
    <ResponseField name="initial_delay_sec" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Creation timestamp in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="distribution_policy_target_shape" type="string">
  The shape to which the group converges either proactively or on resize events (depending on the value set in updatePolicy.instanceRedistributionType). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="distribution_policy_zones" type="set(string)">
  The distribution policy for this managed instance group. You can specify one or more values. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  The fingerprint of the instance group manager. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_flexibility_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="instance_selections" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="machine_types" type="set(string)" required>
        </ResponseField>
        <ResponseField name="name" type="string" required>
        </ResponseField>
        <ResponseField name="rank" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_group" type="string">
  The full URL of the instance group created by the manager. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="instance_group_manager_id" type="number">
  The unique identifier number for the resource. This identifier is defined by the server. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="instance_lifecycle_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="default_action_on_failure" type="string">
    </ResponseField>
    <ResponseField name="force_update_on_repair" type="string">
    </ResponseField>
    <ResponseField name="on_failed_health_check" type="string">
    </ResponseField>
    <ResponseField name="on_repair" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="allow_changing_zone" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="named_port" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="port" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project in which the resource belongs. If it is not provided, the provider project is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The region where the managed instance group resides. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_policies" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="workload_policy" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  The URL of the created resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="standby_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="initial_delay_sec" type="number">
    </ResponseField>
    <ResponseField name="mode" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stateful_disk" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="delete_rule" type="string">
    </ResponseField>
    <ResponseField name="device_name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stateful_external_ip" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="delete_rule" type="string">
    </ResponseField>
    <ResponseField name="interface_name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stateful_internal_ip" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="delete_rule" type="string">
    </ResponseField>
    <ResponseField name="interface_name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="list(object)">
  The status of this managed instance group. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="all_instances_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="current_revision" type="string">
        </ResponseField>
        <ResponseField name="effective" type="bool">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_stable" type="bool">
    </ResponseField>
    <ResponseField name="stateful" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="has_stateful_config" type="bool">
        </ResponseField>
        <ResponseField name="per_instance_configs" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="all_effective" type="bool">
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="version_target" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="is_reached" type="bool">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_size" type="number">
  The target number of running instances for this managed instance group. This value should always be explicitly set unless this resource is attached to an autoscaler, in which case it should never be set. Defaults to 0. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_size_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="mode" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_stopped_size" type="number">
  The target number of stopped instances for this managed instance group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_suspended_size" type="number">
  The target number of suspended instances for this managed instance group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="instance_redistribution_type" type="string">
    </ResponseField>
    <ResponseField name="max_surge_fixed" type="number">
    </ResponseField>
    <ResponseField name="max_surge_percent" type="number">
    </ResponseField>
    <ResponseField name="max_unavailable_fixed" type="number">
    </ResponseField>
    <ResponseField name="max_unavailable_percent" type="number">
    </ResponseField>
    <ResponseField name="minimal_action" type="string" required>
    </ResponseField>
    <ResponseField name="most_disruptive_allowed_action" type="string">
    </ResponseField>
    <ResponseField name="replacement_method" type="string">
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="instance_template" type="string" required>
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="target_size" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="fixed" type="number">
        </ResponseField>
        <ResponseField name="percent" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
