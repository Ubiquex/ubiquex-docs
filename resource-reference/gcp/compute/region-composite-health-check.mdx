---
title: "google_compute_region_composite_health_check"
description: "Real, generated bindings for `google_compute_region_composite_health_check`."
---

`google_compute_region_composite_health_check` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/compute"
    )

    cfg := compute.RegionCompositeHealthCheckConfig{
        HealthDestination: "example",
        Name: "example-name",
        Region: "example",
        Description: "example",
        HealthSources: []string{"example"},
    }
    ubx.Resource(compute.RegionCompositeHealthCheck, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RegionCompositeHealthCheck } from
      "jsr:@ubx/sdk-google/google/compute/region_composite_health_check";

    resource(RegionCompositeHealthCheck, "example", {
      healthDestination: "example",
      name: "example-name",
      region: "example",
      description: "example",
      healthSources: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.compute import (
        RegionCompositeHealthCheck,
        RegionCompositeHealthCheckConfig,
    )

    cfg = RegionCompositeHealthCheckConfig(
        health_destination="example",
        name="example-name",
        region="example",
        description="example",
        health_sources=["example"],
    )
    ubx.resource(RegionCompositeHealthCheck, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_compute_region_composite_health_check with
health_destination, name, region, description, health_sources.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="health_destination" type="string" required>
  Required.
</ResponseField>

<ResponseField name="health_sources" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_timestamp" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
