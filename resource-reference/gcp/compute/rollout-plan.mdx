---
title: "google_compute_rollout_plan"
description: "`google_compute_rollout_plan` defines the real waves a change should roll out in -- grouped by zone or region..."
---

`google_compute_rollout_plan` defines the real waves a change should roll out in -- grouped by zone or region, with a configurable delay between each wave -- used by global VM extension policies to apply a change gradually across a fleet instead of everywhere at once.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own rollout plan"})

    		ubx.Resource(compute.RolloutPlan, "example", compute.RolloutPlanConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RolloutPlan } from "@ubx/sdk-google/google/compute/rollout_plan";

    export default stack("example", () => {
      intent({ summary: "example own rollout plan" });

      resource(RolloutPlan, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RolloutPlan, RolloutPlanConfig

    def describe():
        ubx.intent("example own rollout plan")

        ubx.resource(RolloutPlan, "example", RolloutPlanConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_rollout_plan called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Provide this property when you create the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Always compute#rolloutPlan for rolloutPlans. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="location_scope" type="string">
  The location scope of the rollout plan. If not specified, the location scope is considered as ZONAL. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Provided by the client when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined fully-qualified URL for this resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource's resource id. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="waves" type="list(object)">
  Required. The waves included in this rollout plan. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="number" type="string">
      The wave number, represented as a string, indicating the sequential order of the wave within the rollout plan. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="orchestration_options" type="object">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="delays" type="list(object)">
          Optional; if omitted, computed by Google Cloud.
          <Expandable title="properties">
            <ResponseField name="delimiter" type="string">
              Specifies the grouping delimiter for delay execution. Valid values: DELIMITER_BATCH (delay applies per batch), DELIMITER_LOCATION (delay applies per location), DELIMITER_UNSPECIFIED (no specific grouping). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="duration" type="string">
              The duration of the delay, specified as a string with a time unit (e.g., '30s' or '5m'). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of delay for this orchestration step. Allowed values are TYPE_MINIMUM (minimum delay), TYPE_OFFSET (offset delay), and TYPE_UNSPECIFIED (not specified). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="max_concurrent_locations" type="string">
          The maximum number of locations (zones) that can be updated concurrently during this wave of the rollout. The value can be a fixed number or a percentage of the total locations, e.g., '5' or '25%'. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="max_concurrent_resources_per_location" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="selectors" type="list(object)">
      Optional; if omitted, computed by Google Cloud.
      <Expandable title="properties">
        <ResponseField name="location_selector" type="object">
          Optional; if omitted, computed by Google Cloud.
          <Expandable title="properties">
            <ResponseField name="included_locations" type="list(string)">
              A list of Google Cloud locations (regions or zones) to include in this location selector. Resources in these locations will be selected for the rollout wave. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource_hierarchy_selector" type="object">
          Optional; if omitted, computed by Google Cloud.
          <Expandable title="properties">
            <ResponseField name="included_folders" type="list(string)">
              A list of folder resource names (or IDs) to include in the resource hierarchy selector. Resources contained within these folders are selected for the rollout wave. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="included_organizations" type="list(string)">
              The list of organization IDs to include in the rollout wave's resource hierarchy selector. Only the resources within these organizations are targeted by the wave. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="included_projects" type="list(string)">
              A list of GCP project identifiers to include in the resource hierarchy selector. The rollout plan will apply to resources in these projects. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="validation" type="object">
      Configures the validation procedure for this wave, specifying how the rollout verifies the health and readiness of the instances in the wave before advancing to the next wave. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="time_based_validation_metadata" type="object">
          Optional; if omitted, computed by Google Cloud.
          <Expandable title="properties">
            <ResponseField name="wait_duration" type="string">
              The duration to wait as part of time-based validation, represented as a string. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          Optional; if omitted, computed by Google Cloud.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
