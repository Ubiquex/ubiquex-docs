---
title: "google_compute_network_peering_routes_config"
description: "Real, generated bindings for `google_compute_network_peering_routes_config`."
---

`google_compute_network_peering_routes_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network peering routes config"})

    		ubx.Resource(compute.NetworkPeeringRoutesConfig, "ci-runner", compute.NetworkPeeringRoutesConfigConfig{
    			ExportCustomRoutes: true,
    			ImportCustomRoutes: true,
    			Network:            "example",
    			Peering:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkPeeringRoutesConfig } from "jsr:@ubx/sdk-google/google/compute/network_peering_routes_config";

    export default stack("payments", () => {
      intent({ summary: "payments own network peering routes config" });

      resource(NetworkPeeringRoutesConfig, "ci-runner", {
        exportCustomRoutes: true,
        importCustomRoutes: true,
        network: "example",
        peering: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import NetworkPeeringRoutesConfig, NetworkPeeringRoutesConfigConfig

    def describe():
        ubx.intent("payments own network peering routes config")

        ubx.resource(NetworkPeeringRoutesConfig, "ci-runner", NetworkPeeringRoutesConfigConfig(
            export_custom_routes=True,
            import_custom_routes=True,
            network="example",
            peering="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_network_peering_routes_config, with export
    custom routes true, import custom routes true, network example,
    peering example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="export_custom_routes" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="export_subnet_routes_with_public_ip" type="bool">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="import_custom_routes" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="import_subnet_routes_with_public_ip" type="bool">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network" type="string" required>
  Required.
</ResponseField>

<ResponseField name="peering" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="export_subnet_routes_with_public_ip" type="bool">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="import_subnet_routes_with_public_ip" type="bool">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
