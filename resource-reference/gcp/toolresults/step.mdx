---
title: "google_toolresults_step"
description: "`google_toolresults_step` is a single real operation performed as part of a `google_toolresults_execution`..."
---

`google_toolresults_step` is a single real operation performed as part of a `google_toolresults_execution`, such as one test runner run or one compilation step, the unit performance sample series and results actually attach to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_toolresults --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_toolresults/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	toolresults "github.com/ubiquex/ubx-sdk-google/sdk/go/google_toolresults/toolresults"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own step"})

    		ubx.Resource(toolresults.ToolresultsStep, "example", toolresults.ToolresultsStepConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_toolresults --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ToolresultsStep } from "./local-sdk/google_toolresults/sdk/typescript/google_toolresults/toolresults/toolresults_step.ts";

    export default stack("example", () => {
      intent({ summary: "example own step" });

      resource(ToolresultsStep, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_toolresults --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_toolresults/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_toolresults.toolresults import ToolresultsStep, ToolresultsStepConfig

    def describe():
        ubx.intent("example own step")

        ubx.resource(ToolresultsStep, "example", ToolresultsStepConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_toolresults_step called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="completion_time" type="object">
  A Timestamp represents a point in time independent of any time zone or local calendar, encoded as a count of seconds and fractions of seconds at nanosecond resolution. The count is relative to an epoch at UTC midnight on January 1, 1970, in the proleptic Gregorian calendar which extends the Gregorian calendar backwards to year one. All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap second table is needed for interpretation, using a [24-hour linear smear](https://developers.google.com/time/smear). The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By restricting to that range, we ensure that we can convert to and from [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) date strings. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="nanos" type="number">
      Non-negative fractions of a second at nanosecond resolution. Negative second values with fractions must still have non-negative nanos values that count forward in time. Must be from 0 to 999,999,999 inclusive. Optional.
    </ResponseField>
    <ResponseField name="seconds" type="string">
      Represents seconds of UTC time since Unix epoch 1970-01-01T00:00:00Z. Must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_time" type="object">
  A Timestamp represents a point in time independent of any time zone or local calendar, encoded as a count of seconds and fractions of seconds at nanosecond resolution. The count is relative to an epoch at UTC midnight on January 1, 1970, in the proleptic Gregorian calendar which extends the Gregorian calendar backwards to year one. All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap second table is needed for interpretation, using a [24-hour linear smear](https://developers.google.com/time/smear). The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By restricting to that range, we ensure that we can convert to and from [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) date strings. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="nanos" type="number">
      Non-negative fractions of a second at nanosecond resolution. Negative second values with fractions must still have non-negative nanos values that count forward in time. Must be from 0 to 999,999,999 inclusive. Optional.
    </ResponseField>
    <ResponseField name="seconds" type="string">
      Represents seconds of UTC time since Unix epoch 1970-01-01T00:00:00Z. Must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  A description of this tool For example: mvn clean package -D skipTests=true - In response: present if set by create/update request - In create/update request: optional Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="device_usage_duration" type="object">
  A Duration represents a signed, fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". It is related to Timestamp in that the difference between two Timestamp values is a Duration and it can be added or subtracted from a Timestamp. Range is approximately +-10,000 years. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="nanos" type="number">
      Signed fractions of a second at nanosecond resolution of the span of time. Durations less than one second are represented with a 0 `seconds` field and a positive or negative `nanos` field. For durations of one second or more, a non-zero value for the `nanos` field must be of the same sign as the `seconds` field. Must be from -999,999,999 to +999,999,999 inclusive. Optional.
    </ResponseField>
    <ResponseField name="seconds" type="string">
      Signed seconds of the span of time. Must be from -315,576,000,000 to +315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dimension_value" type="list(object)">
  If the execution containing this step has any dimension_definition set, then this field allows the child to specify the values of the dimensions. The keys must exactly match the dimension_definition of the execution. For example, if the execution has `dimension_definition = ['attempt', 'device']` then a step must define values for those dimensions, eg. `dimension_value = ['attempt': '1', 'device': 'Nexus 6']` If a step does not participate in one dimension of the matrix, the value for that dimension should be empty string. For example, if one of the tests is executed by a runner which does not support retries, the step could have `dimension_value = ['attempt': '', 'device': 'Nexus 6']` If the step does not participate in any dimensions of the matrix, it may leave dimension_value unset. A PRECONDITION_FAILED will be returned if any of the keys do not exist in the dimension_definition of the execution. A PRECONDITION_FAILED will be returned if another step in this execution already has the same name and dimension_value, but differs on other data fields, for example, step field is different. A PRECONDITION_FAILED will be returned if dimension_value is set, and there is a dimension_definition in the execution which is not specified as one of the keys. - In response: present if set by create - In create request: optional - In update request: never set Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The real key of this dimension value, identifying what real axis (e.g. device model, API level) this step's own results are broken out by. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The real value of this dimension, matching its own `key`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="has_images" type="bool">
  Whether any of the outputs of this step are images whose thumbnails can be fetched with ListThumbnails. - In response: always set - In create/update request: never set Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="list(object)">
  Arbitrary user-supplied key/value pairs that are associated with the step. Users are responsible for managing the key namespace such that keys don't accidentally collide. An INVALID_ARGUMENT will be returned if the number of labels exceeds 100 or if the length of any of the keys or values exceeds 100 characters. - In response: always set - In create request: optional - In update request: optional; any new key/value pair will be added to the map, and any new value for an existing key will update that key's value Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The real key of this dimension value, identifying what real axis (e.g. device model, API level) this step's own results are broken out by. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The real value of this dimension, matching its own `key`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="multi_step" type="object">
  Details when multiple steps are run with the same configuration as a group. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="multistep_number" type="number">
      Unique int given to each step. Ranges from 0(inclusive) to total number of steps(exclusive). The primary step is 0. Optional.
    </ResponseField>
    <ResponseField name="primary_step" type="object">
      Stores rollup test status of multiple steps that were run as a group and outcome of each individual step. Optional.
      <Expandable title="properties">
        <ResponseField name="individual_outcome" type="list(object)">
          Step Id and outcome of each individual step. Optional.
          <Expandable title="properties">
            <ResponseField name="multistep_number" type="number">
              The real, sequential number of this individual step's own outcome within a multi-step (sharded) test run. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="outcome_summary" type="string">
              The real, overall outcome (`success`, `failure`, `inconclusive`, `flaky`, `skipped`) of this individual step or multi-step run. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="run_duration" type="object">
              The real, measured wall-clock duration this individual step's own run took. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The sub-second component, in nanoseconds (0 to 999,999,999), of a `google.protobuf.Timestamp`/`Duration` or a `google.type.TimeOfDay`, paired with that same message's own whole-unit `seconds`/`hours`/etc. field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The second-of-minute component (0-60, allowing a leap second) of a `google.type.TimeOfDay`/`DateTime`, or the whole-unit component of a `google.protobuf.Duration`/`Timestamp` paired with that same message's own `nanos`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="step_id" type="string">
              The real Tool Results step ID this individual multi-step outcome corresponds to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="roll_up" type="string">
          Rollup test status of multiple steps that were run with the same configuration as a group. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="primary_step_id" type="string">
      Step Id of the primary (original) step, which might be this step. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  A short human-readable name to display in the UI. Maximum of 100 characters. For example: Clean build A PRECONDITION_FAILED will be returned upon creating a new step if it shares its name and dimension_value with an existing step. If two steps represent a similar action, but have different dimension values, they should share the same name. For instance, if the same set of tests is run on two different platforms, the two steps should have the same name. - In response: always set - In create request: always set - In update request: never set Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="outcome" type="object">
  Interprets a result so that humans and machines can act on it. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="failure_detail" type="object">
      Details for an outcome with a FAILURE outcome summary. Optional.
      <Expandable title="properties">
        <ResponseField name="crashed" type="bool">
          If the failure was severe because the system (app) under test crashed. Optional.
        </ResponseField>
        <ResponseField name="device_out_of_memory" type="bool">
          If the device ran out of memory during a test, causing the test to crash. Optional.
        </ResponseField>
        <ResponseField name="failed_roboscript" type="bool">
          If the Roboscript failed to complete successfully, e.g., because a Roboscript action or assertion failed or a Roboscript action could not be matched during the entire crawl. Optional.
        </ResponseField>
        <ResponseField name="not_installed" type="bool">
          If an app is not installed and thus no test can be run with the app. This might be caused by trying to run a test on an unsupported platform. Optional.
        </ResponseField>
        <ResponseField name="other_native_crash" type="bool">
          If a native process (including any other than the app) crashed. Optional.
        </ResponseField>
        <ResponseField name="timed_out" type="bool">
          If the test overran some time limit, and that is why it failed. Optional.
        </ResponseField>
        <ResponseField name="unable_to_crawl" type="bool">
          If the robo was unable to crawl the app; perhaps because the app did not start. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="inconclusive_detail" type="object">
      Details for an outcome with an INCONCLUSIVE outcome summary. Optional.
      <Expandable title="properties">
        <ResponseField name="aborted_by_user" type="bool">
          If the end user aborted the test execution before a pass or fail could be determined. For example, the user pressed ctrl-c which sent a kill signal to the test runner while the test was running. Optional.
        </ResponseField>
        <ResponseField name="has_error_logs" type="bool">
          If results are being provided to the user in certain cases of infrastructure failures Optional.
        </ResponseField>
        <ResponseField name="infrastructure_failure" type="bool">
          If the test runner could not determine success or failure because the test depends on a component other than the system under test which failed. For example, a mobile test requires provisioning a device where the test executes, and that provisioning can fail. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="skipped_detail" type="object">
      Details for an outcome with a SKIPPED outcome summary. Optional.
      <Expandable title="properties">
        <ResponseField name="incompatible_app_version" type="bool">
          If the App doesn't support the specific API level. Optional.
        </ResponseField>
        <ResponseField name="incompatible_architecture" type="bool">
          If the App doesn't run on the specific architecture, for example, x86. Optional.
        </ResponseField>
        <ResponseField name="incompatible_device" type="bool">
          If the requested OS version doesn't run on the specific device model. Optional.
        </ResponseField>
        <ResponseField name="pending_timeout" type="bool">
          Indicates that the test could not be scheduled in the requested time because no suitable device was available. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="success_detail" type="object">
      Details for an outcome with a SUCCESS outcome summary. LINT.IfChange Optional.
      <Expandable title="properties">
        <ResponseField name="other_native_crash" type="bool">
          If a native process other than the app crashed. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="summary" type="string">
      The simplest way to interpret a result. Required Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="run_duration" type="object">
  A Duration represents a signed, fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". It is related to Timestamp in that the difference between two Timestamp values is a Duration and it can be added or subtracted from a Timestamp. Range is approximately +-10,000 years. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="nanos" type="number">
      Signed fractions of a second at nanosecond resolution of the span of time. Durations less than one second are represented with a 0 `seconds` field and a positive or negative `nanos` field. For durations of one second or more, a non-zero value for the `nanos` field must be of the same sign as the `seconds` field. Must be from -999,999,999 to +999,999,999 inclusive. Optional.
    </ResponseField>
    <ResponseField name="seconds" type="string">
      Signed seconds of the span of time. Must be from -315,576,000,000 to +315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  The initial state is IN_PROGRESS. The only legal state transitions are * IN_PROGRESS -&gt; COMPLETE A PRECONDITION_FAILED will be returned if an invalid transition is requested. It is valid to create Step with a state set to COMPLETE. The state can only be set to COMPLETE once. A PRECONDITION_FAILED will be returned if the state is set to COMPLETE multiple times. - In response: always set - In create/update request: optional Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="step_id" type="string">
  A unique identifier within a Execution for this Step. Returns INVALID_ARGUMENT if this field is set or overwritten by the caller. - In response: always set - In create/update request: never set Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="test_execution_step" type="object">
  A step that represents running tests. It accepts ant-junit xml files which will be parsed into structured test results by the service. Xml file paths are updated in order to append more files, however they can't be deleted. Users can also add test results manually by using the test_result field. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="test_issues" type="list(object)">
      Issues observed during the test execution. For example, if the mobile app under test crashed during the test, the error message and the stack trace content can be recorded here to assist debugging. - In response: present if set by create or update - In create/update request: optional Optional.
      <Expandable title="properties">
        <ResponseField name="category" type="string">
          The real category (`common` or `robo`) this detected test issue falls under. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="error_message" type="string">
          A real, human-readable error message explaining this detected test issue. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="severity" type="string">
          The real severity level (`info`, `suggestion`, `warning`, `severe`) Firebase Test Lab assigned this detected test issue. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stack_trace" type="object">
          The real, captured stack trace for this detected test issue, when the issue is an exception or crash. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="exception" type="string">
              The real, raw exception text captured in this test issue's own `stack_trace`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="warning_migration" type="object">
          Real, structured detail for a test issue that's been migrated to a newer warning representation. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="type_url" type="string">
              The real, fully-qualified type name of the structured warning payload in this `warning_migration` entry. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The real value of this dimension, matching its own `key`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="test_suite_overviews" type="list(object)">
      List of test suite overview contents. This could be parsed from xUnit XML log by server, or uploaded directly by user. This references should only be called when test suites are fully parsed or uploaded. The maximum allowed number of test suite overviews per step is 1000. - In response: always set - In create request: optional - In update request: never (use publishXunitXmlFiles custom method instead) Optional.
      <Expandable title="properties">
        <ResponseField name="elapsed_time" type="object">
          The real, total wall-clock duration this test suite took to run. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              The sub-second component, in nanoseconds (0 to 999,999,999), of a `google.protobuf.Timestamp`/`Duration` or a `google.type.TimeOfDay`, paired with that same message's own whole-unit `seconds`/`hours`/etc. field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="seconds" type="string">
              The second-of-minute component (0-60, allowing a leap second) of a `google.type.TimeOfDay`/`DateTime`, or the whole-unit component of a `google.protobuf.Duration`/`Timestamp` paired with that same message's own `nanos`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="error_count" type="number">
          The real number of tests in this test suite that ended in an error (as opposed to a normal failure). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="failure_count" type="number">
          The real number of tests in this test suite that failed. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="flaky_count" type="number">
          The real number of tests in this test suite detected as flaky (inconsistent across reruns). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The real name of this test suite. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="skipped_count" type="number">
          The real number of tests in this test suite that were skipped. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="total_count" type="number">
          The real total number of tests in this test suite. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="xml_source" type="object">
          A real, uploaded XML test report (e.g. JUnit-format) this test suite overview was parsed from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="file_uri" type="string">
              The real Cloud Storage URI of this test suite's own uploaded `xml_source` file. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="test_timing" type="object">
      Testing timing break down to know phases. Optional.
      <Expandable title="properties">
        <ResponseField name="test_process_duration" type="object">
          A Duration represents a signed, fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". It is related to Timestamp in that the difference between two Timestamp values is a Duration and it can be added or subtracted from a Timestamp. Range is approximately +-10,000 years. Optional.
          <Expandable title="properties">
            <ResponseField name="nanos" type="number">
              Signed fractions of a second at nanosecond resolution of the span of time. Durations less than one second are represented with a 0 `seconds` field and a positive or negative `nanos` field. For durations of one second or more, a non-zero value for the `nanos` field must be of the same sign as the `seconds` field. Must be from -999,999,999 to +999,999,999 inclusive. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="string">
              Signed seconds of the span of time. Must be from -315,576,000,000 to +315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tool_execution" type="object">
      An execution of an arbitrary tool. It could be a test runner or a tool copying artifacts or deploying code. Optional.
      <Expandable title="properties">
        <ResponseField name="command_line_arguments" type="list(string)">
          The full tokenized command line including the program name (equivalent to argv in a C program). - In response: present if set by create request - In create request: optional - In update request: never set Optional.
        </ResponseField>
        <ResponseField name="exit_code" type="object">
          Exit code from a tool execution. Optional.
          <Expandable title="properties">
            <ResponseField name="number" type="number">
              Tool execution exit code. A value of 0 means that the execution was successful. - In response: always set - In create/update request: always set Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tool_logs" type="list(object)">
          References to any plain text logs output the tool execution. This field can be set before the tool has exited in order to be able to have access to a live view of the logs while the tool is running. The maximum allowed number of tool logs per step is 1000. - In response: present if set by create/update request - In create request: optional - In update request: optional, any value provided will be appended to the existing list Optional.
          <Expandable title="properties">
            <ResponseField name="file_uri" type="string">
              The real Cloud Storage URI of this test suite's own uploaded `xml_source` file. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tool_outputs" type="list(object)">
          References to opaque files of any format output by the tool execution. The maximum allowed number of tool outputs per step is 1000. - In response: present if set by create/update request - In create request: optional - In update request: optional, any value provided will be appended to the existing list Optional.
          <Expandable title="properties">
            <ResponseField name="creation_time" type="object">
              The real timestamp when this tool execution's own output was recorded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The sub-second component, in nanoseconds (0 to 999,999,999), of a `google.protobuf.Timestamp`/`Duration` or a `google.type.TimeOfDay`, paired with that same message's own whole-unit `seconds`/`hours`/etc. field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The second-of-minute component (0-60, allowing a leap second) of a `google.type.TimeOfDay`/`DateTime`, or the whole-unit component of a `google.protobuf.Duration`/`Timestamp` paired with that same message's own `nanos`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="output" type="object">
              The real, captured output (such as a log or report file) produced by running this tool. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="file_uri" type="string">
                  The real Cloud Storage URI of this test suite's own uploaded `xml_source` file. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="test_case" type="object">
              Real, structured detail about one individual test case this tool execution's own output describes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="class_name" type="string">
                  The real, fully-qualified class name of the test case this tool output describes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The real name of this test suite. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="test_suite_name" type="string">
                  The real name of the test suite this test case belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tool_execution_step" type="object">
  Generic tool step to be used for binaries we do not explicitly support. For example: running cp to copy artifacts from one location to another. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="tool_execution" type="object">
      An execution of an arbitrary tool. It could be a test runner or a tool copying artifacts or deploying code. Optional.
      <Expandable title="properties">
        <ResponseField name="command_line_arguments" type="list(string)">
          The full tokenized command line including the program name (equivalent to argv in a C program). - In response: present if set by create request - In create request: optional - In update request: never set Optional.
        </ResponseField>
        <ResponseField name="exit_code" type="object">
          Exit code from a tool execution. Optional.
          <Expandable title="properties">
            <ResponseField name="number" type="number">
              Tool execution exit code. A value of 0 means that the execution was successful. - In response: always set - In create/update request: always set Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tool_logs" type="list(object)">
          References to any plain text logs output the tool execution. This field can be set before the tool has exited in order to be able to have access to a live view of the logs while the tool is running. The maximum allowed number of tool logs per step is 1000. - In response: present if set by create/update request - In create request: optional - In update request: optional, any value provided will be appended to the existing list Optional.
          <Expandable title="properties">
            <ResponseField name="file_uri" type="string">
              The real Cloud Storage URI of this test suite's own uploaded `xml_source` file. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tool_outputs" type="list(object)">
          References to opaque files of any format output by the tool execution. The maximum allowed number of tool outputs per step is 1000. - In response: present if set by create/update request - In create request: optional - In update request: optional, any value provided will be appended to the existing list Optional.
          <Expandable title="properties">
            <ResponseField name="creation_time" type="object">
              The real timestamp when this tool execution's own output was recorded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="nanos" type="number">
                  The sub-second component, in nanoseconds (0 to 999,999,999), of a `google.protobuf.Timestamp`/`Duration` or a `google.type.TimeOfDay`, paired with that same message's own whole-unit `seconds`/`hours`/etc. field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="seconds" type="string">
                  The second-of-minute component (0-60, allowing a leap second) of a `google.type.TimeOfDay`/`DateTime`, or the whole-unit component of a `google.protobuf.Duration`/`Timestamp` paired with that same message's own `nanos`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="output" type="object">
              The real, captured output (such as a log or report file) produced by running this tool. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="file_uri" type="string">
                  The real Cloud Storage URI of this test suite's own uploaded `xml_source` file. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="test_case" type="object">
              Real, structured detail about one individual test case this tool execution's own output describes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="class_name" type="string">
                  The real, fully-qualified class name of the test case this tool output describes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The real name of this test suite. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="test_suite_name" type="string">
                  The real name of the test suite this test case belongs to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
