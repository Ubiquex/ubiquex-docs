---
title: "google_cloudidentity_allowlisted_domain"
description: "`google_cloudidentity_allowlisted_domain` permits a specific external Cloud Identity domain to have its own users provisioned or referenced (e.g. as..."
---

`google_cloudidentity_allowlisted_domain` permits a specific external Cloud Identity domain to have its own users provisioned or referenced (e.g. as group members) from within the calling organization.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_cloudidentity --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_cloudidentity/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudidentity "github.com/ubiquex/ubx-sdk-google/sdk/go/google_cloudidentity/cloudidentity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own allowlisted domain"})

    		ubx.Resource(cloudidentity.CloudidentityAllowlistedDomain, "example", cloudidentity.CloudidentityAllowlistedDomainConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_cloudidentity --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CloudidentityAllowlistedDomain } from "./local-sdk/google_cloudidentity/sdk/typescript/google_cloudidentity/cloudidentity/cloudidentity_allowlisted_domain.ts";

    export default stack("example", () => {
      intent({ summary: "example own allowlisted domain" });

      resource(CloudidentityAllowlistedDomain, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_cloudidentity --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_cloudidentity/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_cloudidentity.cloudidentity import CloudidentityAllowlistedDomain, CloudidentityAllowlistedDomainConfig

    def describe():
        ubx.intent("example own allowlisted domain")

        ubx.resource(CloudidentityAllowlistedDomain, "example", CloudidentityAllowlistedDomainConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudidentity_allowlisted_domain called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="domain" type="string">
  Required. Immutable. Name of the domain that is in the allowlist. e.g. "google.com" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. Resource name of the domain in the allowlist e.g. "allowlistedDomains/0184mhaj1smlusv" Optional; if omitted, computed by Google Cloud.
</ResponseField>
