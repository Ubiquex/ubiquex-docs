---
title: "google_cloudidentity_v1beta1_allowlisted_domain"
description: "`google_cloudidentity_v1beta1_allowlisted_domain` permits a specific external Cloud Identity domain to have its own users provisioned or referenced..."
---

<Warning>
  This is Google Cloud's own **v1beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_cloudidentity_allowlisted_domain`.
</Warning>

`google_cloudidentity_v1beta1_allowlisted_domain` permits a specific external Cloud Identity domain to have its own users provisioned or referenced (e.g. as group members) from within the calling organization.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_cloudidentity_v1beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_cloudidentity_v1beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudidentity "github.com/ubiquex/ubx-sdk-google/sdk/go/google_cloudidentity_v1beta1/cloudidentity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 allowlisted domain"})

    		ubx.Resource(cloudidentity.V1beta1CloudidentityV1beta1AllowlistedDomain, "example", cloudidentity.V1beta1CloudidentityV1beta1AllowlistedDomainConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_cloudidentity_v1beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1CloudidentityV1beta1AllowlistedDomain } from "./local-sdk/google_cloudidentity_v1beta1/sdk/typescript/google_cloudidentity_v1beta1/cloudidentity/v1beta1_cloudidentity_v1beta1_allowlisted_domain.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 allowlisted domain" });

      resource(V1beta1CloudidentityV1beta1AllowlistedDomain, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_cloudidentity_v1beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_cloudidentity_v1beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_cloudidentity_v1beta1.cloudidentity import V1beta1CloudidentityV1beta1AllowlistedDomain, V1beta1CloudidentityV1beta1AllowlistedDomainConfig

    def describe():
        ubx.intent("example own v1beta1 allowlisted domain")

        ubx.resource(V1beta1CloudidentityV1beta1AllowlistedDomain, "example", V1beta1CloudidentityV1beta1AllowlistedDomainConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudidentity_v1beta1_allowlisted_domain called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="domain" type="string">
  Required. Immutable. Name of the domain that is in the allowlist. e.g. "google.com" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. Resource name of the domain in the allowlist e.g. "allowlistedDomains/0184mhaj1smlusv" Optional; if omitted, computed by Google Cloud.
</ResponseField>
