---
title: "google_cloudidentity_group"
description: "`google_cloudidentity_group` is a Cloud Identity group -- a named collection of member(s), used for email distribution, access control..."
---

`google_cloudidentity_group` is a Cloud Identity group -- a named collection of member(s), used for email distribution, access control, or organizational structure across Google Workspace and Google Cloud.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_cloudidentity --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_cloudidentity/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudidentity "github.com/ubiquex/ubx-sdk-google/sdk/go/google_cloudidentity/cloudidentity"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group"})

    		ubx.Resource(cloudidentity.CloudidentityGroup, "ci-runner", cloudidentity.CloudidentityGroupConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_cloudidentity --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CloudidentityGroup } from "./local-sdk/google_cloudidentity/sdk/typescript/google_cloudidentity/cloudidentity/cloudidentity_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own group" });

      resource(CloudidentityGroup, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_cloudidentity --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_cloudidentity/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_cloudidentity.cloudidentity import CloudidentityGroup, CloudidentityGroupConfig

    def describe():
        ubx.intent("payments own group")

        ubx.resource(CloudidentityGroup, "ci-runner", CloudidentityGroupConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudidentity_group called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="additional_group_keys" type="list(object)">
  Output only. Additional group keys associated with the Group. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      A unique identifier for this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="namespace" type="string">
      A container grouping related external identifier(s) together. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the `Group` was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  An extended description to help users determine the purpose of a `Group`. Must not be longer than 4,096 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name of the `Group`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="dynamic_group_metadata" type="object">
  Dynamic group metadata like queries and status. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="queries" type="list(object)">
      Memberships will be the union of all queries. Only one entry with USER resource is currently supported. Customers can create up to 500 dynamic groups. Optional.
      <Expandable title="properties">
        <ResponseField name="query" type="string">
          The query evaluated to produce this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_type" type="string">
          The category of resource this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="status" type="object">
      The current status of a dynamic group along with timestamp. Optional.
      <Expandable title="properties">
        <ResponseField name="status" type="string">
          Status of the dynamic group. Optional.
        </ResponseField>
        <ResponseField name="status_time" type="string">
          The latest time at which the dynamic group is guaranteed to be in the given status. If status is `UP_TO_DATE`, the latest time at which the dynamic group was confirmed to be up-to-date. If status is `UPDATING_MEMBERSHIPS`, the time at which dynamic group was created. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="group_key" type="object">
  A unique identifier for an entity in the Cloud Identity Groups API. An entity can represent either a group with an optional `namespace` or a user without a `namespace`. The combination of `id` and `namespace` must be unique; however, the same `id` can be used with different `namespace`s. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      The ID of the entity. For Google-managed entities, the `id` should be the email address of an existing group or user. Email addresses need to adhere to [name guidelines for users and groups](https://support.google.com/a/answer/9193374). For external-identity-mapped entities, the `id` must be a string conforming to the Identity Source's requirements. Must be unique within a `namespace`. Optional.
    </ResponseField>
    <ResponseField name="namespace" type="string">
      The namespace in which the entity exists. If not specified, the `EntityKey` represents a Google-managed entity such as a Google user or a Google Group. If specified, the `EntityKey` represents an external-identity-mapped group. The namespace must correspond to an identity source created in Admin Console and must be in the form of `identitysources/&#123;identity_source&#125;`. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Required. One or more label entries that apply to the Group. Labels contain a key with an empty value. Google Groups are the default type of group and have a label with a key of `cloudidentity.googleapis.com/groups.discussion_forum` and an empty value. Existing Google Groups can have an additional label with a key of `cloudidentity.googleapis.com/groups.security` and an empty value added to them. **This is an immutable change and the security label cannot be removed once added.** Dynamic groups have a label with a key of `cloudidentity.googleapis.com/groups.dynamic`. Identity-mapped groups for Cloud Search have a label with a key of `system/groups/external` and an empty value. Google Groups can be [locked](https://support.google.com/a?p=locked-groups). To lock a group, add a label with a key of `cloudidentity.googleapis.com/groups.locked` and an empty value. Doing so locks the group. To unlock the group, remove this label. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The [resource name](https://cloud.google.com/apis/design/resource_names) of the `Group`. Shall be of the form `groups/&#123;group&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parent" type="string">
  Required. Immutable. The resource name of the entity under which this `Group` resides in the Cloud Identity resource hierarchy. Must be of the form `identitysources/&#123;identity_source&#125;` for external [identity-mapped groups](https://support.google.com/a/answer/9039510) or `customers/&#123;customer_id&#125;` for Google Groups. The `customer_id` must begin with "C" (for example, 'C046psxkn'). [Find your customer ID.] (https://support.google.com/cloudidentity/answer/10070793) Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the `Group` was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
