---
title: "Google Cloud"
description: "Google Cloud resource types, grouped by real service."
---

Google Cloud covers 3 resource types (as of the
version this reference was generated from), sourced directly from
real Google Discovery Documents via ubx-provider-dynamic, grouped here
the same way the real generated bindings are, mechanically by
`Google Cloud` service boundary (see each generated repo's own
`ServiceAndLocalName`). Every page's field list is parsed directly from
the real provider schema, not hand-authored.

<CardGroup cols={2}>
  <Card title="Workstations" icon="cube" href="/resource-reference/gcp/workstations/workstation">
    3 resource types documented
  </Card>
</CardGroup>
