---
title: "google_certificatemanager_trust_config"
description: "`google_certificatemanager_trust_config` defines a set of trusted root and intermediate CA certificates Certificate Manager uses to validate client..."
---

`google_certificatemanager_trust_config` defines a set of trusted root and intermediate CA certificates Certificate Manager uses to validate client certificates during mutual TLS.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_certificatemanager --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_certificatemanager/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	certificatemanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google_certificatemanager/certificatemanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own trust config"})

    		ubx.Resource(certificatemanager.CertificatemanagerTrustConfig, "ci-runner", certificatemanager.CertificatemanagerTrustConfigConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_certificatemanager --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CertificatemanagerTrustConfig } from "./local-sdk/google_certificatemanager/sdk/typescript/google_certificatemanager/certificatemanager/certificatemanager_trust_config.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own trust config" });

      resource(CertificatemanagerTrustConfig, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_certificatemanager --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_certificatemanager/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_certificatemanager.certificatemanager import CertificatemanagerTrustConfig, CertificatemanagerTrustConfigConfig

    def describe():
        ubx.intent("payments own trust config")

        ubx.resource(CertificatemanagerTrustConfig, "ci-runner", CertificatemanagerTrustConfigConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_certificatemanager_trust_config called "ci-runner",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="allowlisted_certificates" type="list(object)">
  Optional. A certificate matching an allowlisted certificate is always considered valid as long as the certificate is parseable, proof of private key possession is established, and constraints on the certificate's SAN field are met. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="pem_certificate" type="string">
      The certificate, PEM-encoded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The creation timestamp of a TrustConfig. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. One or more paragraphs of text description of a TrustConfig. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Set of labels associated with a TrustConfig. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. A user-defined name of the trust config. TrustConfig names must be unique globally and match pattern `projects/*/locations/*/trustConfigs/*`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="spiffe_trust_stores" type="map(object)">
  Optional. Defines a mapping from a trust domain to a TrustStore. This is used for SPIFFE certificate validation. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="intermediate_cas" type="list(object)">
      The intermediate CA certificate(s) chained below the root. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="pem_certificate" type="string">
          The certificate, PEM-encoded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="trust_anchors" type="list(object)">
      The root CA certificate(s) this trust configuration trusts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="pem_certificate" type="string">
          The certificate, PEM-encoded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="trust_stores" type="list(object)">
  Optional. Set of trust stores to perform validation against. This field is supported when TrustConfig is configured with Load Balancers, currently not supported for SPIFFE certificate validation. Only one TrustStore specified is currently allowed. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="intermediate_cas" type="list(object)">
      The intermediate CA certificate(s) chained below the root. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="pem_certificate" type="string">
          The certificate, PEM-encoded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="trust_anchors" type="list(object)">
      The root CA certificate(s) this trust configuration trusts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="pem_certificate" type="string">
          The certificate, PEM-encoded. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The last update timestamp of a TrustConfig. Optional; if omitted, computed by Google Cloud.
</ResponseField>
