---
title: "google_organization_iam_custom_role"
description: "Real, generated bindings for `google_organization_iam_custom_role`."
---

`google_organization_iam_custom_role` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organization "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/organization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own iam custom role"})

    		ubx.Resource(organization.IamCustomRole, "ci-runner", organization.IamCustomRoleConfig{
    			OrgId:       "example",
    			Permissions: []string{"example"},
    			RoleId:      "example",
    			Title:       "example",
    			Description: "Managed by ubx.",
    			Stage:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IamCustomRole } from "jsr:@ubx/sdk-google/google/organization/iam_custom_role";

    export default stack("payments", () => {
      intent({ summary: "payments own iam custom role" });

      resource(IamCustomRole, "ci-runner", {
        orgId: "example",
        permissions: ["example"],
        roleId: "example",
        title: "example",
        description: "Managed by ubx.",
        stage: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.organization import IamCustomRole, IamCustomRoleConfig

    def describe():
        ubx.intent("payments own iam custom role")

        ubx.resource(IamCustomRole, "ci-runner", IamCustomRoleConfig(
            org_id="example",
            permissions=["example"],
            role_id="example",
            title="example",
            description="Managed by ubx.",
            stage="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_organization_iam_custom_role, with org id example,
    permissions a set value, role id example, title example,
    description Managed by ubx., stage example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="permissions" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="role_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="stage" type="string">
  Optional.
</ResponseField>

<ResponseField name="title" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="deleted" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>
