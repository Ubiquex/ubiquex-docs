---
title: "google_organization_access_approval_settings"
description: "Real, generated bindings for `google_organization_access_approval_settings`."
---

`google_organization_access_approval_settings` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	organization "github.com/ubiquex/ubx-sdk-google/sdk/go/google/organization"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own access approval settings"})

    		ubx.Resource(organization.AccessApprovalSettings, "ci-runner", organization.AccessApprovalSettingsConfig{
    			OrganizationId:   "example",
    			ActiveKeyVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccessApprovalSettings } from "jsr:@ubx/sdk-google/google/organization/access_approval_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own access approval settings" });

      resource(AccessApprovalSettings, "ci-runner", {
        organizationId: "example",
        activeKeyVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.organization import AccessApprovalSettings, AccessApprovalSettingsConfig

    def describe():
        ubx.intent("payments own access approval settings")

        ubx.resource(AccessApprovalSettings, "ci-runner", AccessApprovalSettingsConfig(
            organization_id="example",
            active_key_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_organization_access_approval_settings, with
    organization id example, active key version example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="active_key_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enrolled_services" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cloud_product" type="string" required>
    </ResponseField>
    <ResponseField name="enrollment_level" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="notification_emails" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="ancestor_has_active_key_version" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enrolled_ancestor" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="enrolled_services" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cloud_product" type="string" required>
    </ResponseField>
    <ResponseField name="enrollment_level" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="invalid_key_version" type="bool">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="notification_emails" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
