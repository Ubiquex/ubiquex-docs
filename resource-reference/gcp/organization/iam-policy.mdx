---
title: "google_organization_iam_policy"
description: "Real, generated bindings for `google_organization_iam_policy`."
---

`google_organization_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/organization"
    )

    cfg := organization.IamPolicyConfig{
        OrgId: "example",
        PolicyData: "example",
    }
    ubx.Resource(organization.IamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IamPolicy } from
      "jsr:@ubx/sdk-google/google/organization/iam_policy";

    resource(IamPolicy, "example", {
      orgId: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.organization import IamPolicy, IamPolicyConfig

    cfg = IamPolicyConfig(
        org_id="example",
        policy_data="example",
    )
    ubx.resource(IamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_organization_iam_policy with org_id,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
