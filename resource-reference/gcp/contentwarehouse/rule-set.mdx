---
title: "google_contentwarehouse_rule_set"
description: "`google_contentwarehouse_rule_set` is a set of real, event-triggered rules in Document AI Warehouse..."
---

`google_contentwarehouse_rule_set` is a set of real, event-triggered rules in Document AI Warehouse, for example an action that deletes a document automatically when it matches a specific document type and state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_contentwarehouse --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_contentwarehouse/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	contentwarehouse "github.com/ubiquex/ubx-sdk-google/sdk/go/google_contentwarehouse/contentwarehouse"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own rule set"})

    		ubx.Resource(contentwarehouse.ContentwarehouseRuleSet, "example", contentwarehouse.ContentwarehouseRuleSetConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_contentwarehouse --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContentwarehouseRuleSet } from "./local-sdk/google_contentwarehouse/sdk/typescript/google_contentwarehouse/contentwarehouse/contentwarehouse_rule_set.ts";

    export default stack("example", () => {
      intent({ summary: "example own rule set" });

      resource(ContentwarehouseRuleSet, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_contentwarehouse --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_contentwarehouse/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_contentwarehouse.contentwarehouse import ContentwarehouseRuleSet, ContentwarehouseRuleSetConfig

    def describe():
        ubx.intent("example own rule set")

        ubx.resource(ContentwarehouseRuleSet, "example", ContentwarehouseRuleSetConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_contentwarehouse_rule_set called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string">
  Short description of the rule-set. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the rule set. Managed internally. Format: projects/&#123;project_number&#125;/locations/&#123;location&#125;/ruleSet/&#123;rule_set_id&#125;. The name is ignored when creating a rule set. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  List of rules given by the customer. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="actions" type="list(object)">
      The real, ordered action(s) a matching `google_contentwarehouse_rule_set` rule runs when its own `condition` and `trigger_type` match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="access_control" type="object">
          A real rule action that adds, removes, or replaces an IAM policy binding on the matching document. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="operation_type" type="string">
              The real operation (`ADD_POLICY_BINDING`, `REMOVE_POLICY_BINDING`, `REPLACE_POLICY_BINDING`) this access-control action performs. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="policy" type="object">
              The real IAM policy (or policy fragment) this access-control action applies to the matching document. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="audit_configs" type="list(object)">
                  Real, proposed IAM audit logging configuration this access-control action applies alongside its own policy bindings. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="audit_log_configs" type="list(object)">
                      Per-`service` Cloud Audit Logs configuration on a `google.iam.v1.Policy`, controlling which `log_type`s are enabled and which `exempted_members` are excluded from logging. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exempted_members" type="list(string)">
                          The identities exempted from Cloud Audit Logs for the `log_type` this `audit_log_configs` entry configures. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="log_type" type="string">
                          Which Cloud Audit Log type (`ADMIN_READ`, `DATA_READ`, or `DATA_WRITE`) an `audit_log_configs` entry configures. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="service" type="string">
                      The real Google Cloud service this document's own embedded IAM audit log configuration applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="bindings" type="list(object)">
                  The list of `google.iam.v1.Policy` bindings, each granting a `role` to a set of `members`, optionally scoped by a `condition`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="condition" type="object">
                      The real trigger condition (e.g. a document type and state match) a rule in a `google_contentwarehouse_rule_set` evaluates before running its own actions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="description" type="string">
                          A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="expression" type="string">
                          The real Common Expression Language (CEL) expression text of this document IAM policy binding's own condition. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="location" type="string">
                          The real, human-readable source location of this document IAM policy binding's own condition expression, used for error reporting. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="title" type="string">
                          The resource's own formal name -- a more official variant of `display_name`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="members" type="list(string)">
                      The identities (users, service accounts, groups, or domains) a `google.iam.v1.Policy` binding's own `role` is granted to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="role" type="string">
                      The real IAM role this document-level policy binding grants. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="etag" type="string">
                  A checksum computed by the server from the resource's own current content, used for optimistic concurrency control -- an update/delete that includes a stale `etag` is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="version" type="number">
                  A real identifier for the specific version of this document schema, processor, or content that produced the element. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="action_id" type="string">
          A real, stable identifier for this specific action within its own parent rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="add_to_folder" type="object">
          A real rule action that adds the matching document to one or more `folders`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="folders" type="list(string)">
              The real, target folder ID(s) an `add_to_folder`/`remove_from_folder_action` rule action operates on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="data_update" type="object">
          A real rule action that writes new property `entries` onto the matching document. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="entries" type="map(string)">
              The real, named property values a `data_update` rule action writes onto the matching document. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="data_validation" type="object">
          A real rule action that checks the matching document's own properties against a set of `conditions`, failing the rule if they don't hold. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="conditions" type="map(string)">
              The real, named property conditions a `data_validation` rule action checks the matching document against. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="delete_document_action" type="object">
          A real rule action that deletes the matching document, optionally with `enable_hard_delete` for permanent deletion. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enable_hard_delete" type="bool">
              Whether a `delete_document_action` real, permanently deletes the matching document rather than moving it to a recoverable trash state. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="publish_to_pub_sub" type="object">
          A real rule action that publishes one or more `messages` to a Pub/Sub `topic_id` when the rule matches. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="messages" type="list(string)">
              The real message payload(s) a `publish_to_pub_sub` rule action publishes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="topic_id" type="string">
              The real Pub/Sub topic a `publish_to_pub_sub` rule action publishes its own messages to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="remove_from_folder_action" type="object">
          A real rule action that removes the matching document from a specific `folder`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="condition" type="string">
              The real trigger condition (e.g. a document type and state match) a rule in a `google_contentwarehouse_rule_set` evaluates before running its own actions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="folder" type="string">
              The real, target folder ID a `remove_from_folder_action` removes the matching document from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="condition" type="string">
      The real trigger condition (e.g. a document type and state match) a rule in a `google_contentwarehouse_rule_set` evaluates before running its own actions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_id" type="string">
      A real, stable identifier for this rule within its own parent rule set. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="trigger_type" type="string">
      The real document lifecycle event (`ON_CREATE`, `ON_UPDATE`, `ON_CREATE_LINK`, `ON_DELETE_LINK`) that evaluates this rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source" type="string">
  Source of the rules i.e., customer name. Optional; if omitted, computed by Google Cloud.
</ResponseField>
