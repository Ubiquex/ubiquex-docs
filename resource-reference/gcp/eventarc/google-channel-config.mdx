---
title: "google_eventarc_google_channel_config"
description: "Real, generated bindings for `google_eventarc_google_channel_config`."
---

`google_eventarc_google_channel_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	eventarc "github.com/ubiquex/ubx-sdk-google/sdk/go/google/eventarc"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own google channel config"})

    		ubx.Resource(eventarc.GoogleChannelConfig, "ci-runner", eventarc.GoogleChannelConfigConfig{
    			Location:      "example",
    			Name:          "ci-runner",
    			CryptoKeyName: "example-crypto-key-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GoogleChannelConfig } from "jsr:@ubx/sdk-google/google/eventarc/google_channel_config";

    export default stack("payments", () => {
      intent({ summary: "payments own google channel config" });

      resource(GoogleChannelConfig, "ci-runner", {
        location: "example",
        name: "ci-runner",
        cryptoKeyName: "example-crypto-key-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.eventarc import GoogleChannelConfig, GoogleChannelConfigConfig

    def describe():
        ubx.intent("payments own google channel config")

        ubx.resource(GoogleChannelConfig, "ci-runner", GoogleChannelConfigConfig(
            location="example",
            name="ci-runner",
            crypto_key_name="example-crypto-key-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_eventarc_google_channel_config called "ci-runner",
    with location example, crypto key name example-crypto-key-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="crypto_key_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
