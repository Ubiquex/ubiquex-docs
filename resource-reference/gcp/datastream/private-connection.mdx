---
title: "google_datastream_private_connection"
description: "Real, generated bindings for `google_datastream_private_connection`."
---

`google_datastream_private_connection` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	datastream "github.com/ubiquex/ubx-sdk-google/sdk/go/google/datastream"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own private connection"})

    		ubx.Resource(datastream.PrivateConnection, "ci-runner", datastream.PrivateConnectionConfig{
    			DisplayName:             "example-display-name",
    			Location:                "example",
    			PrivateConnectionId:     "example",
    			CreateWithoutValidation: true,
    			Labels:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivateConnection } from "jsr:@ubx/sdk-google/google/datastream/private_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own private connection" });

      resource(PrivateConnection, "ci-runner", {
        displayName: "example-display-name",
        location: "example",
        privateConnectionId: "example",
        createWithoutValidation: true,
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.datastream import PrivateConnection, PrivateConnectionConfig

    def describe():
        ubx.intent("payments own private connection")

        ubx.resource(PrivateConnection, "ci-runner", PrivateConnectionConfig(
            display_name="example-display-name",
            location="example",
            private_connection_id="example",
            create_without_validation=True,
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_datastream_private_connection, with display name
    example-display-name, location example, private connection id
    example, create without validation true, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="create_without_validation" type="bool">
  If set to true, will skip validations. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  This field uses a custom implementation please refer to documentation under /hashicorp/terraform-provider-google-beta/website/docs/r/datastream_private_connection.html.markdown for specifics Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Display name. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The name of the location this private connection is located in. Required.
</ResponseField>

<ResponseField name="private_connection_id" type="string" required>
  The private connectivity identifier. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="psc_interface_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="network_attachment" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_peering_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="subnet" type="string" required>
    </ResponseField>
    <ResponseField name="vpc" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  This field uses a custom implementation please refer to documentation under /hashicorp/terraform-provider-google-beta/website/docs/r/datastream_private_connection.html.markdown for specifics Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="error" type="list(object)">
  The PrivateConnection error in case of failure. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="details" type="map(string)">
    </ResponseField>
    <ResponseField name="message" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource's name. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="psc_interface_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="network_attachment" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  State of the PrivateConnection. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_peering_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="subnet" type="string" required>
    </ResponseField>
    <ResponseField name="vpc" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>
