---
title: "google_datastream_connection_profile"
description: "`google_datastream_connection_profile` defines the connectivity and credentials Datastream uses to reach a specific source or destination database or..."
---

`google_datastream_connection_profile` defines the connectivity and credentials Datastream uses to reach a specific source or destination database or storage system.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_datastream --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_datastream/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	datastream "github.com/ubiquex/ubx-sdk-google/sdk/go/google_datastream/datastream"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own connection profile"})

    		ubx.Resource(datastream.DatastreamConnectionProfile, "example", datastream.DatastreamConnectionProfileConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_datastream --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatastreamConnectionProfile } from "./local-sdk/google_datastream/sdk/typescript/google_datastream/datastream/datastream_connection_profile.ts";

    export default stack("example", () => {
      intent({ summary: "example own connection profile" });

      resource(DatastreamConnectionProfile, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_datastream --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_datastream/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_datastream.datastream import DatastreamConnectionProfile, DatastreamConnectionProfileConfig

    def describe():
        ubx.intent("example own connection profile")

        ubx.resource(DatastreamConnectionProfile, "example", DatastreamConnectionProfileConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_datastream_connection_profile called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="bigquery_profile" type="any">
  Profile for connecting to a BigQuery destination. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The create time of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="dataverse_profile" type="object">
  Profile for connecting to a Dataverse source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="environment_url" type="string">
      Required. Environment URL of the Microsoft Dataverse instance. Example: `.crm.dynamics.com` Optional.
    </ResponseField>
    <ResponseField name="oauth_client_credentials" type="object">
      OAuth Client Credentials. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Required. Client ID for OAuth Client Credentials. Optional.
        </ResponseField>
        <ResponseField name="client_secret" type="object">
          A confidential piece of information where the actual value is either directly specified in the message as a raw string or stored in GCP secret manager. Optional.
          <Expandable title="properties">
            <ResponseField name="raw_value" type="string">
              Optional. Input only. The actual raw value of the secret as plain text. Optional.
            </ResponseField>
            <ResponseField name="secret_version" type="string">
              Optional. A Secret Manager resource name storing the actual value of the secret. Supported formats: * projects/&#123;project&#125;/locations/&#123;location&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; * projects/&#123;project&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Required. Tenant id of the Microsoft Dataverse instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. Display name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="forward_ssh_connectivity" type="object">
  Forward SSH Tunnel connectivity. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="hostname" type="string">
      Required. Hostname for the SSH tunnel. Optional.
    </ResponseField>
    <ResponseField name="password" type="string">
      Input only. SSH password. Optional.
    </ResponseField>
    <ResponseField name="port" type="number">
      Port for the SSH tunnel, default value is 22. Optional.
    </ResponseField>
    <ResponseField name="private_key" type="string">
      Input only. SSH private key. Optional.
    </ResponseField>
    <ResponseField name="username" type="string">
      Required. Username for the SSH tunnel. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="gcs_profile" type="object">
  Profile for connecting to a Cloud Storage destination. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string">
      Required. The Cloud Storage bucket name. Optional.
    </ResponseField>
    <ResponseField name="root_path" type="string">
      Optional. The root path inside the Cloud Storage bucket. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels to apply to this datastream connection profile. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="mongodb_profile" type="object">
  Profile for connecting to a MongoDB source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="additional_options" type="map(string)">
      Optional. Specifies additional options for the MongoDB connection. The options should be sent as key-value pairs, for example: `additional_options = &#123;"serverSelectionTimeoutMS": "10000", "directConnection": "true"&#125;`. Keys are case-sensitive and should match the official MongoDB connection string options: https://www.mongodb.com/docs/manual/reference/connection-string-options/ The server will not modify the values provided by the user. Optional.
    </ResponseField>
    <ResponseField name="host_addresses" type="list(object)">
      Required. List of host addresses for a MongoDB cluster. For SRV connection format, this list must contain exactly one DNS host without a port. For Standard connection format, this list must contain all the required hosts in the cluster with their respective ports. Optional.
      <Expandable title="properties">
        <ResponseField name="hostname" type="string">
          The hostname of the source database server. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="port" type="number">
          The port the source database server listens on. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="password" type="string">
      Optional. Password for the MongoDB connection. Mutually exclusive with the `secret_manager_stored_password` field. Optional.
    </ResponseField>
    <ResponseField name="replica_set" type="string">
      Optional. Name of the replica set. Only needed for self hosted replica set type MongoDB cluster. For SRV connection format, this field must be empty. For Standard connection format, this field must be specified. Optional.
    </ResponseField>
    <ResponseField name="secret_manager_stored_password" type="string">
      Optional. A reference to a Secret Manager resource name storing the SQLServer connection password. Mutually exclusive with the `password` field. Optional.
    </ResponseField>
    <ResponseField name="srv_connection_format" type="any">
      Srv connection format. Optional.
    </ResponseField>
    <ResponseField name="ssl_config" type="object">
      MongoDB SSL configuration information. Optional.
      <Expandable title="properties">
        <ResponseField name="ca_certificate" type="string">
          Optional. Input only. PEM-encoded certificate of the CA that signed the source database server's certificate. Optional.
        </ResponseField>
        <ResponseField name="ca_certificate_set" type="bool">
          Output only. Indicates whether the ca_certificate field is set. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="client_certificate" type="string">
          Optional. Input only. PEM-encoded certificate that will be used by the replica to authenticate against the source database server. If this field is used then the 'client_key' and the 'ca_certificate' fields are mandatory. Optional.
        </ResponseField>
        <ResponseField name="client_certificate_set" type="bool">
          Output only. Indicates whether the client_certificate field is set. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="client_key" type="string">
          Optional. Input only. PEM-encoded private key associated with the Client Certificate. If this field is used then the 'client_certificate' and the 'ca_certificate' fields are mandatory. Optional.
        </ResponseField>
        <ResponseField name="client_key_set" type="bool">
          Output only. Indicates whether the client_key field is set. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="secret_manager_stored_client_key" type="string">
          Optional. Input only. A reference to a Secret Manager resource name storing the PEM-encoded private key associated with the Client Certificate. If this field is used then the 'client_certificate' and the 'ca_certificate' fields are mandatory. Mutually exclusive with the `client_key` field. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="standard_connection_format" type="object">
      Standard connection format. Optional.
      <Expandable title="properties">
        <ResponseField name="direct_connection" type="bool">
          Optional. Deprecated: Use the `additional_options` map to specify the `directConnection` parameter instead. For example: `additional_options = &#123;"directConnection": "true"&#125;`. Specifies whether the client connects directly to the host[:port] in the connection URI. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="username" type="string">
      Required. Username for the MongoDB connection. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="mysql_profile" type="object">
  Profile for connecting to a MySQL source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="hostname" type="string">
      Required. Hostname for the MySQL connection. Optional.
    </ResponseField>
    <ResponseField name="password" type="string">
      Optional. Input only. Password for the MySQL connection. Mutually exclusive with the `secret_manager_stored_password` field. Optional.
    </ResponseField>
    <ResponseField name="port" type="number">
      Port for the MySQL connection, default value is 3306. Optional.
    </ResponseField>
    <ResponseField name="secret_manager_stored_password" type="string">
      Optional. A reference to a Secret Manager resource name storing the MySQL connection password. Mutually exclusive with the `password` field. Optional.
    </ResponseField>
    <ResponseField name="ssl_config" type="object">
      MySQL SSL configuration information. Optional.
      <Expandable title="properties">
        <ResponseField name="ca_certificate" type="string">
          Input only. PEM-encoded certificate of the CA that signed the source database server's certificate. Optional.
        </ResponseField>
        <ResponseField name="ca_certificate_set" type="bool">
          Output only. Indicates whether the ca_certificate field is set. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="client_certificate" type="string">
          Optional. Input only. PEM-encoded certificate that will be used by the replica to authenticate against the source database server. If this field is used then the 'client_key' and the 'ca_certificate' fields are mandatory. Optional.
        </ResponseField>
        <ResponseField name="client_certificate_set" type="bool">
          Output only. Indicates whether the client_certificate field is set. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="client_key" type="string">
          Optional. Input only. PEM-encoded private key associated with the Client Certificate. If this field is used then the 'client_certificate' and the 'ca_certificate' fields are mandatory. Optional.
        </ResponseField>
        <ResponseField name="client_key_set" type="bool">
          Output only. Indicates whether the client_key field is set. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="username" type="string">
      Required. Username for the MySQL connection. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. The resource's name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="oracle_profile" type="object">
  Profile for connecting to an Oracle source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="connection_attributes" type="map(string)">
      Connection string attributes Optional.
    </ResponseField>
    <ResponseField name="database_service" type="string">
      Required. Database for the Oracle connection. Optional.
    </ResponseField>
    <ResponseField name="hostname" type="string">
      Required. Hostname for the Oracle connection. Optional.
    </ResponseField>
    <ResponseField name="oracle_asm_config" type="object">
      Configuration for Oracle Automatic Storage Management (ASM) connection. Optional.
      <Expandable title="properties">
        <ResponseField name="asm_service" type="string">
          Required. ASM service name for the Oracle ASM connection. Optional.
        </ResponseField>
        <ResponseField name="connection_attributes" type="map(string)">
          Optional. Connection string attributes Optional.
        </ResponseField>
        <ResponseField name="hostname" type="string">
          Required. Hostname for the Oracle ASM connection. Optional.
        </ResponseField>
        <ResponseField name="oracle_ssl_config" type="object">
          Oracle SSL configuration information. Optional.
          <Expandable title="properties">
            <ResponseField name="ca_certificate" type="string">
              Input only. PEM-encoded certificate of the CA that signed the source database server's certificate. Optional.
            </ResponseField>
            <ResponseField name="ca_certificate_set" type="bool">
              Output only. Indicates whether the ca_certificate field has been set for this Connection-Profile. Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="server_certificate_distinguished_name" type="string">
              Optional. The distinguished name (DN) mentioned in the server certificate. This corresponds to SSL_SERVER_CERT_DN sqlnet parameter. Refer https://docs.oracle.com/en/database/oracle/oracle-database/19/netrf/local-naming-parameters-in-tns-ora-file.html#GUID-70AB0695-A9AA-4A94-B141-4C605236EEB7 If this field is not provided, the DN matching is not enforced. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="password" type="string">
          Optional. Password for the Oracle ASM connection. Mutually exclusive with the `secret_manager_stored_password` field. Optional.
        </ResponseField>
        <ResponseField name="port" type="number">
          Required. Port for the Oracle ASM connection. Optional.
        </ResponseField>
        <ResponseField name="secret_manager_stored_password" type="string">
          Optional. A reference to a Secret Manager resource name storing the Oracle ASM connection password. Mutually exclusive with the `password` field. Optional.
        </ResponseField>
        <ResponseField name="username" type="string">
          Required. Username for the Oracle ASM connection. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="oracle_ssl_config" type="object">
      Oracle SSL configuration information. Optional.
      <Expandable title="properties">
        <ResponseField name="ca_certificate" type="string">
          Input only. PEM-encoded certificate of the CA that signed the source database server's certificate. Optional.
        </ResponseField>
        <ResponseField name="ca_certificate_set" type="bool">
          Output only. Indicates whether the ca_certificate field has been set for this Connection-Profile. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="server_certificate_distinguished_name" type="string">
          Optional. The distinguished name (DN) mentioned in the server certificate. This corresponds to SSL_SERVER_CERT_DN sqlnet parameter. Refer https://docs.oracle.com/en/database/oracle/oracle-database/19/netrf/local-naming-parameters-in-tns-ora-file.html#GUID-70AB0695-A9AA-4A94-B141-4C605236EEB7 If this field is not provided, the DN matching is not enforced. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="password" type="string">
      Optional. Password for the Oracle connection. Mutually exclusive with the `secret_manager_stored_password` field. Optional.
    </ResponseField>
    <ResponseField name="port" type="number">
      Port for the Oracle connection, default value is 1521. Optional.
    </ResponseField>
    <ResponseField name="secret_manager_stored_password" type="string">
      Optional. A reference to a Secret Manager resource name storing the Oracle connection password. Mutually exclusive with the `password` field. Optional.
    </ResponseField>
    <ResponseField name="username" type="string">
      Required. Username for the Oracle connection. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="postgresql_profile" type="object">
  Profile for connecting to a PostgreSQL source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="database" type="string">
      Required. Database for the PostgreSQL connection. Optional.
    </ResponseField>
    <ResponseField name="hostname" type="string">
      Required. Hostname for the PostgreSQL connection. Optional.
    </ResponseField>
    <ResponseField name="password" type="string">
      Optional. Password for the PostgreSQL connection. Mutually exclusive with the `secret_manager_stored_password` field. Optional.
    </ResponseField>
    <ResponseField name="port" type="number">
      Port for the PostgreSQL connection, default value is 5432. Optional.
    </ResponseField>
    <ResponseField name="secret_manager_stored_password" type="string">
      Optional. A reference to a Secret Manager resource name storing the PostgreSQL connection password. Mutually exclusive with the `password` field. Optional.
    </ResponseField>
    <ResponseField name="ssl_config" type="object">
      PostgreSQL SSL configuration information. Optional.
      <Expandable title="properties">
        <ResponseField name="server_and_client_verification" type="object">
          Message represents the option where Datastream will enforce the encryption and authenticate the server identity as well as the client identity. ca_certificate, client_certificate and client_key must be set if user selects this option. Optional.
          <Expandable title="properties">
            <ResponseField name="ca_certificate" type="string">
              Required. Input only. PEM-encoded server root CA certificate. Optional.
            </ResponseField>
            <ResponseField name="client_certificate" type="string">
              Required. Input only. PEM-encoded certificate used by the source database to authenticate the client identity (i.e., the Datastream's identity). This certificate is signed by either a root certificate trusted by the server or one or more intermediate certificates (which is stored with the leaf certificate) to link the this certificate to the trusted root certificate. Optional.
            </ResponseField>
            <ResponseField name="client_key" type="string">
              Optional. Input only. PEM-encoded private key associated with the client certificate. This value will be used during the SSL/TLS handshake, allowing the PostgreSQL server to authenticate the client's identity, i.e. identity of the Datastream. Optional.
            </ResponseField>
            <ResponseField name="server_certificate_hostname" type="string">
              Optional. The hostname mentioned in the Subject or SAN extension of the server certificate. If this field is not provided, the hostname in the server certificate is not validated. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="server_verification" type="object">
          Message represents the option where Datastream will enforce the encryption and authenticate the server identity. ca_certificate must be set if user selects this option. Optional.
          <Expandable title="properties">
            <ResponseField name="ca_certificate" type="string">
              Required. Input only. PEM-encoded server root CA certificate. Optional.
            </ResponseField>
            <ResponseField name="server_certificate_hostname" type="string">
              Optional. The hostname mentioned in the Subject or SAN extension of the server certificate. If this field is not provided, the hostname in the server certificate is not validated. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="username" type="string">
      Required. Username for the PostgreSQL connection. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="private_connectivity" type="object">
  Private Connectivity Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="private_connection" type="string">
      Required. A reference to a private connection resource. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/privateConnections/&#123;name&#125;` Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="salesforce_marketing_cloud_profile" type="object">
  Profile for connecting to a Salesforce Marketing Cloud source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="oauth_client_credentials" type="object">
      OAuth Client Credentials. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Required. Client ID for OAuth Client Credentials. Optional.
        </ResponseField>
        <ResponseField name="client_secret" type="object">
          A confidential piece of information where the actual value is either directly specified in the message as a raw string or stored in GCP secret manager. Optional.
          <Expandable title="properties">
            <ResponseField name="raw_value" type="string">
              Optional. Input only. The actual raw value of the secret as plain text. Optional.
            </ResponseField>
            <ResponseField name="secret_version" type="string">
              Optional. A Secret Manager resource name storing the actual value of the secret. Supported formats: * projects/&#123;project&#125;/locations/&#123;location&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; * projects/&#123;project&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="subdomain" type="string">
      Required. Subdomain for the Salesforce Marketing Cloud connection. Example: if your specific endpoint is `https://&#123;your-specific-subdomain&#125;.rest.marketingcloudapis.com/`, the subdomain is `&#123;your-specific-subdomain&#125;`. Must be 1-63 characters, start and end with an alphanumeric character, and contain only lowercase letters, numbers, and hyphens (-). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="salesforce_profile" type="object">
  Profile for connecting to a Salesforce source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="domain" type="string">
      Required. Domain endpoint for the Salesforce connection. Optional.
    </ResponseField>
    <ResponseField name="oauth2_client_credentials" type="object">
      OAuth2 Client Credentials. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Required. Client ID for Salesforce OAuth2 Client Credentials. Optional.
        </ResponseField>
        <ResponseField name="client_secret" type="string">
          Optional. Client secret for Salesforce OAuth2 Client Credentials. Mutually exclusive with the `secret_manager_stored_client_secret` field. Optional.
        </ResponseField>
        <ResponseField name="secret_manager_stored_client_secret" type="string">
          Optional. A reference to a Secret Manager resource name storing the Salesforce OAuth2 client_secret. Mutually exclusive with the `client_secret` field. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="user_credentials" type="object">
      Deprecated: Salesforce is retiring Username-Password authentication. Use `Oauth2ClientCredentials` instead. Optional.
      <Expandable title="properties">
        <ResponseField name="password" type="string">
          Optional. Password for the Salesforce connection. Mutually exclusive with the `secret_manager_stored_password` field. Optional.
        </ResponseField>
        <ResponseField name="secret_manager_stored_password" type="string">
          Optional. A reference to a Secret Manager resource name storing the Salesforce connection's password. Mutually exclusive with the `password` field. Optional.
        </ResponseField>
        <ResponseField name="secret_manager_stored_security_token" type="string">
          Optional. A reference to a Secret Manager resource name storing the Salesforce connection's security token. Mutually exclusive with the `security_token` field. Optional.
        </ResponseField>
        <ResponseField name="security_token" type="string">
          Optional. Security token for the Salesforce connection. Mutually exclusive with the `secret_manager_stored_security_token` field. Optional.
        </ResponseField>
        <ResponseField name="username" type="string">
          Required. Username for the Salesforce connection. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="service_now_profile" type="object">
  Profile for connecting to a ServiceNow source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="instance" type="string">
      Required. The instance of the ServiceNow account. This is the `` part of the URL `https://.service-now.com`. Optional.
    </ResponseField>
    <ResponseField name="oauth_client_credentials" type="object">
      OAuth Client Credentials. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Required. Client ID for OAuth Client Credentials. Optional.
        </ResponseField>
        <ResponseField name="client_secret" type="object">
          A confidential piece of information where the actual value is either directly specified in the message as a raw string or stored in GCP secret manager. Optional.
          <Expandable title="properties">
            <ResponseField name="raw_value" type="string">
              Optional. Input only. The actual raw value of the secret as plain text. Optional.
            </ResponseField>
            <ResponseField name="secret_version" type="string">
              Optional. A Secret Manager resource name storing the actual value of the secret. Supported formats: * projects/&#123;project&#125;/locations/&#123;location&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; * projects/&#123;project&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="user_password_credentials" type="object">
      User-password credentials. Optional.
      <Expandable title="properties">
        <ResponseField name="password" type="object">
          A confidential piece of information where the actual value is either directly specified in the message as a raw string or stored in GCP secret manager. Optional.
          <Expandable title="properties">
            <ResponseField name="raw_value" type="string">
              Optional. Input only. The actual raw value of the secret as plain text. Optional.
            </ResponseField>
            <ResponseField name="secret_version" type="string">
              Optional. A Secret Manager resource name storing the actual value of the secret. Supported formats: * projects/&#123;project&#125;/locations/&#123;location&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; * projects/&#123;project&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="username" type="string">
          Required. Username for the connection. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="spanner_profile" type="object">
  Profile for connecting to a Spanner source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="database" type="string">
      Required. Immutable. Cloud Spanner database resource. This field is immutable. Must be in the format: projects/&#123;project&#125;/instances/&#123;instance&#125;/databases/&#123;database_id&#125;. Optional.
    </ResponseField>
    <ResponseField name="host" type="string">
      Optional. The Spanner endpoint to connect to. Defaults to the global endpoint (https://spanner.googleapis.com). Must be in the format: https://spanner.&#123;region&#125;.rep.googleapis.com. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sql_server_profile" type="object">
  Profile for connecting to a SQLServer source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="database" type="string">
      Required. Database for the SQLServer connection. Optional.
    </ResponseField>
    <ResponseField name="hostname" type="string">
      Required. Hostname for the SQLServer connection. Optional.
    </ResponseField>
    <ResponseField name="password" type="string">
      Optional. Password for the SQLServer connection. Mutually exclusive with the `secret_manager_stored_password` field. Optional.
    </ResponseField>
    <ResponseField name="port" type="number">
      Port for the SQLServer connection, default value is 1433. Optional.
    </ResponseField>
    <ResponseField name="secret_manager_stored_password" type="string">
      Optional. A reference to a Secret Manager resource name storing the SQLServer connection password. Mutually exclusive with the `password` field. Optional.
    </ResponseField>
    <ResponseField name="ssl_config" type="object">
      SQL Server SSL configuration information. Optional.
      <Expandable title="properties">
        <ResponseField name="basic_encryption" type="any">
          Message to represent the option where Datastream will enforce encryption without authenticating server identity. Server certificates will be trusted by default. Optional.
        </ResponseField>
        <ResponseField name="encryption_and_server_validation" type="object">
          Message to represent the option where Datastream will enforce encryption and authenticate server identity. ca_certificate must be set if user selects this option. Optional.
          <Expandable title="properties">
            <ResponseField name="ca_certificate" type="string">
              Optional. Input only. PEM-encoded certificate of the CA that signed the source database server's certificate. Optional.
            </ResponseField>
            <ResponseField name="server_certificate_hostname" type="string">
              Optional. The hostname mentioned in the Subject or SAN extension of the server certificate. This field is used for bypassing the hostname validation while verifying server certificate. This is required for scenarios where the host name that datastream connects to is different from the certificate's subject. This specifically happens for private connectivity. It could also happen when the customer provides a public IP in connection profile but the same is not present in the server certificate. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="encryption_not_enforced" type="any">
          Message to represent the option where encryption is not enforced. An empty message right now to allow future extensibility. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="username" type="string">
      Required. Username for the SQLServer connection. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="static_service_ip_connectivity" type="any">
  Static IP address connectivity. Used when the source database is configured to allow incoming connections from the Datastream public IP addresses for the region specified in the connection profile. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The update time of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="workday_profile" type="object">
  Profile for connecting to a Workday source. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="host" type="string">
      Required. Host for the Workday connection. Must be a valid hostname (e.g., `wd3-impl-services1.workday.com`). Optional.
    </ResponseField>
    <ResponseField name="oauth_refresh_token_credentials" type="object">
      OAuth Refresh Token Credentials. Optional.
      <Expandable title="properties">
        <ResponseField name="oauth_client_credentials" type="object">
          OAuth Client Credentials. Optional.
          <Expandable title="properties">
            <ResponseField name="client_id" type="string">
              Required. Client ID for OAuth Client Credentials. Optional.
            </ResponseField>
            <ResponseField name="client_secret" type="object">
              A confidential piece of information where the actual value is either directly specified in the message as a raw string or stored in GCP secret manager. Optional.
              <Expandable title="properties">
                <ResponseField name="raw_value" type="string">
                  Optional. Input only. The actual raw value of the secret as plain text. Optional.
                </ResponseField>
                <ResponseField name="secret_version" type="string">
                  Optional. A Secret Manager resource name storing the actual value of the secret. Supported formats: * projects/&#123;project&#125;/locations/&#123;location&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; * projects/&#123;project&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="refresh_token" type="object">
          A confidential piece of information where the actual value is either directly specified in the message as a raw string or stored in GCP secret manager. Optional.
          <Expandable title="properties">
            <ResponseField name="raw_value" type="string">
              Optional. Input only. The actual raw value of the secret as plain text. Optional.
            </ResponseField>
            <ResponseField name="secret_version" type="string">
              Optional. A Secret Manager resource name storing the actual value of the secret. Supported formats: * projects/&#123;project&#125;/locations/&#123;location&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; * projects/&#123;project&#125;/secrets/&#123;secret&#125;/versions/&#123;version&#125; Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tenant" type="string">
      Required. Tenant for the Workday connection (e.g., `google12`). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
