---
title: "google_datastream_route"
description: "`google_datastream_route` defines a network route within a Datastream private connection..."
---

`google_datastream_route` defines a network route within a Datastream private connection, letting traffic reach source database(s) on an overlapping or non-default IP range.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_datastream --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_datastream/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	datastream "github.com/ubiquex/ubx-sdk-google/sdk/go/google_datastream/datastream"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own route"})

    		ubx.Resource(datastream.DatastreamRoute, "example", datastream.DatastreamRouteConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_datastream --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatastreamRoute } from "./local-sdk/google_datastream/sdk/typescript/google_datastream/datastream/datastream_route.ts";

    export default stack("example", () => {
      intent({ summary: "example own route" });

      resource(DatastreamRoute, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_datastream --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_datastream/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_datastream.datastream import DatastreamRoute, DatastreamRouteConfig

    def describe():
        ubx.intent("example own route")

        ubx.resource(DatastreamRoute, "example", DatastreamRouteConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_datastream_route called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The create time of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="destination_address" type="string">
  Required. Destination address for connection Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="destination_port" type="number">
  Destination port for connection Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. Display name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. The resource's name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The update time of the resource. Optional; if omitted, computed by Google Cloud.
</ResponseField>
