---
title: "google_managedidentities_backup"
description: "`google_managedidentities_backup` is a point-in-time backup of a Managed Service for Microsoft Active Directory domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_managedidentities --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_managedidentities/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	managedidentities "github.com/ubiquex/ubx-sdk-google/sdk/go/google_managedidentities/managedidentities"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own backup"})

    		ubx.Resource(managedidentities.ManagedidentitiesBackup, "ci-runner", managedidentities.ManagedidentitiesBackupConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_managedidentities --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedidentitiesBackup } from "./local-sdk/google_managedidentities/sdk/typescript/google_managedidentities/managedidentities/managedidentities_backup.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own backup" });

      resource(ManagedidentitiesBackup, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_managedidentities --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_managedidentities/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_managedidentities.managedidentities import ManagedidentitiesBackup, ManagedidentitiesBackupConfig

    def describe():
        ubx.intent("payments own backup")

        ubx.resource(ManagedidentitiesBackup, "ci-runner", ManagedidentitiesBackupConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_managedidentities_backup called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The time the backups was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Resource labels to represent user provided metadata. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The unique name of the Backup in the form of `projects/&#123;project_id&#125;/locations/global/domains/&#123;domain_name&#125;/backups/&#123;name&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the backup. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="status_message" type="string">
  Output only. Additional information about the current status of this backup, if available. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  Output only. Indicates whether it’s an on-demand backup or scheduled. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Last update time. Optional; if omitted, computed by Google Cloud.
</ResponseField>
