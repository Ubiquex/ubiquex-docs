---
title: "google_bigtableadmin_schema_bundle"
description: "`google_bigtableadmin_schema_bundle` attaches Protocol Buffer schema definitions to a Bigtable table..."
---

`google_bigtableadmin_schema_bundle` attaches Protocol Buffer schema definitions to a Bigtable table, letting stored `PROTO`-typed column values be interpreted with real field names and types instead of raw bytes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigtableadmin "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigtableadmin"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own schema bundle"})

    		ubx.Resource(bigtableadmin.SchemaBundle, "example", bigtableadmin.SchemaBundleConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SchemaBundle } from "@ubx/sdk-google/google/bigtableadmin/schema_bundle";

    export default stack("example", () => {
      intent({ summary: "example own schema bundle" });

      resource(SchemaBundle, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigtableadmin.schema_bundle import SchemaBundle, SchemaBundleConfig

    def describe():
        ubx.intent("example own schema bundle")

        ubx.resource(SchemaBundle, "example", SchemaBundleConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigtableadmin_schema_bundle called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Optional. The etag for this schema bundle. This may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. The server returns an ABORTED error on a mismatched etag. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The unique name identifying this schema bundle. Values are of the form `projects/&#123;project&#125;/instances/&#123;instance&#125;/tables/&#123;table&#125;/schemaBundles/&#123;schema_bundle&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="proto_schema" type="object">
  Represents a collection of protobuf schemas. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="proto_descriptors" type="string">
      Required. Contains a protobuf-serialized [google.protobuf.FileDescriptorSet](https://github.com/protocolbuffers/protobuf/blob/main/src/google/protobuf/descriptor.proto), which could include multiple proto files. To generate it, [install](https://grpc.io/docs/protoc-installation/) and run `protoc` with `--include_imports` and `--descriptor_set_out`. For example, to generate for moon/shot/app.proto, run ``` $protoc --proto_path=/app_path --proto_path=/lib_path \ --include_imports \ --descriptor_set_out=descriptors.pb \ moon/shot/app.proto ``` For more details, see protobuffer [self description](https://developers.google.com/protocol-buffers/docs/techniques#self-description). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
