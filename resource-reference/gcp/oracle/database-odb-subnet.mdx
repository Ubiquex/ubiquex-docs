---
title: "google_oracle_database_odb_subnet"
description: "Real, generated bindings for `google_oracle_database_odb_subnet`."
---

`google_oracle_database_odb_subnet` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	oracle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/oracle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own database odb subnet"})

    		ubx.Resource(oracle.DatabaseOdbSubnet, "ci-runner", oracle.DatabaseOdbSubnetConfig{
    			CidrRange:          "example",
    			Location:           "example",
    			OdbSubnetId:        "example",
    			Odbnetwork:         "example",
    			Purpose:            "example",
    			DeletionProtection: true,
    			Labels:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatabaseOdbSubnet } from "jsr:@ubx/sdk-google/google/oracle/database_odb_subnet";

    export default stack("payments", () => {
      intent({ summary: "payments own database odb subnet" });

      resource(DatabaseOdbSubnet, "ci-runner", {
        cidrRange: "example",
        location: "example",
        odbSubnetId: "example",
        odbnetwork: "example",
        purpose: "example",
        deletionProtection: true,
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.oracle import DatabaseOdbSubnet, DatabaseOdbSubnetConfig

    def describe():
        ubx.intent("payments own database odb subnet")

        ubx.resource(DatabaseOdbSubnet, "ci-runner", DatabaseOdbSubnetConfig(
            cidr_range="example",
            location="example",
            odb_subnet_id="example",
            odbnetwork="example",
            purpose="example",
            deletion_protection=True,
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_oracle_database_odb_subnet, with cidr range
    example, location example, odb subnet id example, odbnetwork
    example, purpose example, deletion protection true, labels a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cidr_range" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_protection" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="odb_subnet_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="odbnetwork" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="purpose" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
