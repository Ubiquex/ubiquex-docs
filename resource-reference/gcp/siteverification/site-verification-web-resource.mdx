---
title: "google_siteverification_site_verification_web_resource"
description: "`google_siteverification_site_verification_web_resource` records a verified website or domain..."
---

`google_siteverification_site_verification_web_resource` records a verified website or domain, confirming that the owner controls it via one of Google Site Verification real methods (a hosted file, an HTML meta tag, Google Analytics, Google Tag Manager, a DNS TXT record, or a DNS CNAME record). Verification unlocks access to other Google services (e.g. Search Console) that require confirmed site or domain ownership, and each resource can list additional owner accounts beyond the one that performed the original verification.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_siteverification --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_siteverification/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	siteverification "github.com/ubiquex/ubx-sdk-google/sdk/go/google_siteverification/siteverification"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own site verification web resource"})

    		ubx.Resource(siteverification.SiteVerificationWebResource, "ci-runner", siteverification.SiteVerificationWebResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_siteverification --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SiteVerificationWebResource } from "./local-sdk/google_siteverification/sdk/typescript/google_siteverification/siteverification/site_verification_web_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own site verification web resource" });

      resource(SiteVerificationWebResource, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_siteverification --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_siteverification/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_siteverification.siteverification import SiteVerificationWebResource, SiteVerificationWebResourceConfig

    def describe():
        ubx.intent("payments own site verification web resource")

        ubx.resource(SiteVerificationWebResource, "ci-runner", SiteVerificationWebResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_siteverification_site_verification_web_resource,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="id" type="string">
  The string used to identify this site. This value should be used in the "id" portion of the REST URL for the Get, Update, and Delete operations. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="owners" type="list(string)">
  The email addresses of all verified owners. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="site" type="object">
  The address and type of a site that is verified or will be verified. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="identifier" type="string">
      The site identifier. If the type is set to SITE, the identifier is a URL. If the type is set to INET_DOMAIN, the site identifier is a domain name. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The site type. Can be SITE or INET_DOMAIN (domain name). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
