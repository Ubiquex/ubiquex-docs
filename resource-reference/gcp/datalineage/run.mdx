---
title: "google_datalineage_run"
description: "`google_datalineage_run` is a single execution of a lineage process, capturing which data asset(s) it read from and wrote to for that specific run."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_datalineage --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_datalineage/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	datalineage "github.com/ubiquex/ubx-sdk-google/sdk/go/google_datalineage/datalineage"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own run"})

    		ubx.Resource(datalineage.DatalineageRun, "example", datalineage.DatalineageRunConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_datalineage --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatalineageRun } from "./local-sdk/google_datalineage/sdk/typescript/google_datalineage/datalineage/datalineage_run.ts";

    export default stack("example", () => {
      intent({ summary: "example own run" });

      resource(DatalineageRun, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_datalineage --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_datalineage/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_datalineage.datalineage import DatalineageRun, DatalineageRunConfig

    def describe():
        ubx.intent("example own run")

        ubx.resource(DatalineageRun, "example", DatalineageRunConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_datalineage_run called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="attributes" type="map(any)">
  Optional. The attributes of the run. Should only be used for the purpose of non-semantic management (classifying, describing or labeling the run). Up to 100 attributes are allowed. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. A human-readable name you can set to display in a user interface. Must be not longer than 200 characters and only contain UTF-8 letters or numbers, spaces or characters like `_-:&.` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional. The timestamp of the end of the run. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. The resource name of the run. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/processes/&#123;process&#125;/runs/&#123;run&#125;`. Can be specified or auto-assigned. &#123;run&#125; must be not longer than 200 characters and only contain characters in a set: `a-zA-Z0-9_-:.` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="start_time" type="string">
  Required. The timestamp of the start of the run. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Required. The state of the run. Optional; if omitted, computed by Google Cloud.
</ResponseField>
