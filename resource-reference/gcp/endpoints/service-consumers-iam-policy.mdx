---
title: "google_endpoints_service_consumers_iam_policy"
description: "Real, generated bindings for `google_endpoints_service_consumers_iam_policy`."
---

`google_endpoints_service_consumers_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/endpoints"
    )

    cfg := endpoints.ServiceConsumersIamPolicyConfig{
        ConsumerProject: "example",
        PolicyData: "example",
        ServiceName: "example-service-name",
    }
    ubx.Resource(endpoints.ServiceConsumersIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceConsumersIamPolicy } from
      "jsr:@ubx/sdk-google/google/endpoints/service_consumers_iam_policy";

    resource(ServiceConsumersIamPolicy, "example", {
      consumerProject: "example",
      policyData: "example",
      serviceName: "example-service-name",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.endpoints import (
        ServiceConsumersIamPolicy,
        ServiceConsumersIamPolicyConfig,
    )

    cfg = ServiceConsumersIamPolicyConfig(
        consumer_project="example",
        policy_data="example",
        service_name="example-service-name",
    )
    ubx.resource(ServiceConsumersIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_endpoints_service_consumers_iam_policy with
consumer_project, policy_data, service_name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="consumer_project" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
