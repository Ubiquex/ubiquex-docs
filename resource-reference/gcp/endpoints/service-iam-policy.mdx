---
title: "google_endpoints_service_iam_policy"
description: "Real, generated bindings for `google_endpoints_service_iam_policy`."
---

`google_endpoints_service_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	endpoints "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/endpoints"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service iam policy"})

    		ubx.Resource(endpoints.ServiceIamPolicy, "ci-runner", endpoints.ServiceIamPolicyConfig{
    			PolicyData:  "example",
    			ServiceName: "example-service-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceIamPolicy } from "jsr:@ubx/sdk-google/google/endpoints/service_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own service iam policy" });

      resource(ServiceIamPolicy, "ci-runner", {
        policyData: "example",
        serviceName: "example-service-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.endpoints import ServiceIamPolicy, ServiceIamPolicyConfig

    def describe():
        ubx.intent("payments own service iam policy")

        ubx.resource(ServiceIamPolicy, "ci-runner", ServiceIamPolicyConfig(
            policy_data="example",
            service_name="example-service-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_endpoints_service_iam_policy, with policy data
    example, service name example-service-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
