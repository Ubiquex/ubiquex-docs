---
title: "google_endpoints_service_iam_policy"
description: "Real, generated bindings for `google_endpoints_service_iam_policy`."
---

`google_endpoints_service_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/endpoints"
    )

    cfg := endpoints.ServiceIamPolicyConfig{
        PolicyData: "example",
        ServiceName: "example-service-name",
    }
    ubx.Resource(endpoints.ServiceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceIamPolicy } from
      "jsr:@ubx/sdk-google/google/endpoints/service_iam_policy";

    resource(ServiceIamPolicy, "example", {
      policyData: "example",
      serviceName: "example-service-name",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.endpoints import (
        ServiceIamPolicy,
        ServiceIamPolicyConfig,
    )

    cfg = ServiceIamPolicyConfig(
        policy_data="example",
        service_name="example-service-name",
    )
    ubx.resource(ServiceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_endpoints_service_iam_policy with policy_data,
service_name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
