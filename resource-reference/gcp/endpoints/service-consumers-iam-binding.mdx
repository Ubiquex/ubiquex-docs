---
title: "google_endpoints_service_consumers_iam_binding"
description: "Real, generated bindings for `google_endpoints_service_consumers_iam_binding`."
---

`google_endpoints_service_consumers_iam_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	endpoints "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/endpoints"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service consumers iam binding"})

    		ubx.Resource(endpoints.ServiceConsumersIamBinding, "ci-runner", endpoints.ServiceConsumersIamBindingConfig{
    			ConsumerProject: "example",
    			Members:         []string{"example"},
    			Role:            "example",
    			ServiceName:     "example-service-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceConsumersIamBinding } from "jsr:@ubx/sdk-google/google/endpoints/service_consumers_iam_binding";

    export default stack("payments", () => {
      intent({ summary: "payments own service consumers iam binding" });

      resource(ServiceConsumersIamBinding, "ci-runner", {
        consumerProject: "example",
        members: ["example"],
        role: "example",
        serviceName: "example-service-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.endpoints import ServiceConsumersIamBinding, ServiceConsumersIamBindingConfig

    def describe():
        ubx.intent("payments own service consumers iam binding")

        ubx.resource(ServiceConsumersIamBinding, "ci-runner", ServiceConsumersIamBindingConfig(
            consumer_project="example",
            members=["example"],
            role="example",
            service_name="example-service-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_endpoints_service_consumers_iam_binding, with
    consumer project example, members a set value, role example,
    service name example-service-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="consumer_project" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="members" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
