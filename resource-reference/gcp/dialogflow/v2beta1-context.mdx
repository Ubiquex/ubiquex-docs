---
title: "google_dialogflow_v2beta1_context"
description: "`google_dialogflow_v2beta1_context` holds real, structured parameters (its own parameters map) that persist across a Dialogflow conversation for..."
---

<Warning>
  This is Google Cloud's own **v2beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. No stable, generally-available equivalent is published yet.
</Warning>

`google_dialogflow_v2beta1_context` holds real, structured parameters (its own parameters map) that persist across a Dialogflow conversation for a limited window (lifespan_count), letting later turns in the same conversation reference values collected earlier without the caller re-sending them on every request.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_dialogflow_v2beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_dialogflow_v2beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dialogflow "github.com/ubiquex/ubx-sdk-google/sdk/go/google_dialogflow_v2beta1/dialogflow"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v2beta1 context"})

    		ubx.Resource(dialogflow.V2beta1DialogflowV2beta1Context, "example", dialogflow.V2beta1DialogflowV2beta1ContextConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_dialogflow_v2beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2beta1DialogflowV2beta1Context } from "./local-sdk/google_dialogflow_v2beta1/sdk/typescript/google_dialogflow_v2beta1/dialogflow/v2beta1_dialogflow_v2beta1_context.ts";

    export default stack("example", () => {
      intent({ summary: "example own v2beta1 context" });

      resource(V2beta1DialogflowV2beta1Context, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_dialogflow_v2beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_dialogflow_v2beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_dialogflow_v2beta1.dialogflow import V2beta1DialogflowV2beta1Context, V2beta1DialogflowV2beta1ContextConfig

    def describe():
        ubx.intent("example own v2beta1 context")

        ubx.resource(V2beta1DialogflowV2beta1Context, "example", V2beta1DialogflowV2beta1ContextConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dialogflow_v2beta1_context called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="lifespan_count" type="number">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parameters" type="map(any)">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>
