---
title: "google_dialogflow_v2beta1_participant"
description: "`google_dialogflow_v2beta1_participant` is a real participant in a Dialogflow conversation -- human agent, end user..."
---

<Warning>
  This is Google Cloud's own **v2beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. No stable, generally-available equivalent is published yet.
</Warning>

`google_dialogflow_v2beta1_participant` is a real participant in a Dialogflow conversation -- human agent, end user, or automated agent (its own role) -- with an obfuscated_external_user_id and documents_metadata_filters scoping what knowledge that participant's own responses may draw from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_dialogflow_v2beta1 --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_dialogflow_v2beta1/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dialogflow "github.com/ubiquex/ubx-sdk-google/sdk/go/google_dialogflow_v2beta1/dialogflow"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v2beta1 participant"})

    		ubx.Resource(dialogflow.V2beta1DialogflowV2beta1Participant, "example", dialogflow.V2beta1DialogflowV2beta1ParticipantConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_dialogflow_v2beta1 --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2beta1DialogflowV2beta1Participant } from "./local-sdk/google_dialogflow_v2beta1/sdk/typescript/google_dialogflow_v2beta1/dialogflow/v2beta1_dialogflow_v2beta1_participant.ts";

    export default stack("example", () => {
      intent({ summary: "example own v2beta1 participant" });

      resource(V2beta1DialogflowV2beta1Participant, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_dialogflow_v2beta1 --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_dialogflow_v2beta1/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_dialogflow_v2beta1.dialogflow import V2beta1DialogflowV2beta1Participant, V2beta1DialogflowV2beta1ParticipantConfig

    def describe():
        ubx.intent("example own v2beta1 participant")

        ubx.resource(V2beta1DialogflowV2beta1Participant, "example", V2beta1DialogflowV2beta1ParticipantConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dialogflow_v2beta1_participant called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="agent_desktop_source" type="string">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="documents_metadata_filters" type="map(string)">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="obfuscated_external_user_id" type="string">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="role" type="string">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>
