---
title: "google_dialogflow_v2beta1_entity_type"
description: "`google_dialogflow_v2beta1_entity_type` defines a category of value (e.g. a city name or product SKU) a Dialogflow agent can extract from user input..."
---

<Warning>
  This is Google Cloud's own **v2beta1** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_dialogflow_entity_type`.
</Warning>

`google_dialogflow_v2beta1_entity_type` defines a category of value (e.g. a city name or product SKU) a Dialogflow agent can extract from user input, along with its own synonyms and matching behavior.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dialogflow "github.com/ubiquex/ubx-sdk-google/sdk/go/google/dialogflow"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v2beta1 entity type"})

    		ubx.Resource(dialogflow.V2beta1EntityType, "example", dialogflow.V2beta1EntityTypeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2beta1EntityType } from "@ubx/sdk-google/google/dialogflow/v2beta1_entity_type";

    export default stack("example", () => {
      intent({ summary: "example own v2beta1 entity type" });

      resource(V2beta1EntityType, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dialogflow.v2beta1_entity_type import V2beta1EntityType, V2beta1EntityTypeConfig

    def describe():
        ubx.intent("example own v2beta1 entity type")

        ubx.resource(V2beta1EntityType, "example", V2beta1EntityTypeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dialogflow_v2beta1_entity_type called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="auto_expansion_mode" type="string">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="enable_fuzzy_extraction" type="bool">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="entities" type="list(object)">
  Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="synonyms" type="list(string)">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by Google Cloud.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by Google Cloud.
</ResponseField>
