---
title: "google_dialogflow_cx_security_settings"
description: "Real, generated bindings for `google_dialogflow_cx_security_settings`."
---

`google_dialogflow_cx_security_settings` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dialogflow "github.com/ubiquex/ubx-sdk-google/sdk/go/google/dialogflow"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cx security settings"})

    		ubx.Resource(dialogflow.CxSecuritySettings, "ci-runner", dialogflow.CxSecuritySettingsConfig{
    			DisplayName:        "example-display-name",
    			Location:           "example",
    			DeidentifyTemplate: "example",
    			InspectTemplate:    "example",
    			PurgeDataTypes:     []string{"example"},
    			RedactionScope:     "example",
    			RedactionStrategy:  "example",
    			RetentionStrategy:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CxSecuritySettings } from "jsr:@ubx/sdk-google/google/dialogflow/cx_security_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own cx security settings" });

      resource(CxSecuritySettings, "ci-runner", {
        displayName: "example-display-name",
        location: "example",
        deidentifyTemplate: "example",
        inspectTemplate: "example",
        purgeDataTypes: ["example"],
        redactionScope: "example",
        redactionStrategy: "example",
        retentionStrategy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dialogflow import CxSecuritySettings, CxSecuritySettingsConfig

    def describe():
        ubx.intent("payments own cx security settings")

        ubx.resource(CxSecuritySettings, "ci-runner", CxSecuritySettingsConfig(
            display_name="example-display-name",
            location="example",
            deidentify_template="example",
            inspect_template="example",
            purge_data_types=["example"],
            redaction_scope="example",
            redaction_strategy="example",
            retention_strategy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dialogflow_cx_security_settings, with display name
    example-display-name, location example, deidentify template
    example, inspect template example, purge data types a set value,
    redaction scope example, redaction strategy example, retention
    strategy example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="audio_export_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="audio_export_pattern" type="string">
    </ResponseField>
    <ResponseField name="audio_format" type="string">
    </ResponseField>
    <ResponseField name="enable_audio_redaction" type="bool">
    </ResponseField>
    <ResponseField name="gcs_bucket" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deidentify_template" type="string">
  [DLP](https://cloud.google.com/dlp/docs) deidentify template name. Use this template to define de-identification configuration for the content. If empty, Dialogflow replaces sensitive info with [redacted] text. Note: deidentifyTemplate must be located in the same region as the SecuritySettings. Format: projects/&lt;Project ID&gt;/locations/&lt;Location ID&gt;/deidentifyTemplates/&lt;Template ID&gt; OR organizations/&lt;Organization ID&gt;/locations/&lt;Location ID&gt;/deidentifyTemplates/&lt;Template ID&gt; Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The human-readable name of the security settings, unique within the location. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="insights_export_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable_insights_export" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="inspect_template" type="string">
  [DLP](https://cloud.google.com/dlp/docs) inspect template name. Use this template to define inspect base settings. If empty, we use the default DLP inspect config. Note: inspectTemplate must be located in the same region as the SecuritySettings. Format: projects/&lt;Project ID&gt;/locations/&lt;Location ID&gt;/inspectTemplates/&lt;Template ID&gt; OR organizations/&lt;Organization ID&gt;/locations/&lt;Location ID&gt;/inspectTemplates/&lt;Template ID&gt; Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location these settings are located in. Settings can only be applied to an agent in the same location. See [Available Regions](https://cloud.google.com/dialogflow/cx/docs/concept/region#avail) for a list of supported locations. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="purge_data_types" type="list(string)">
  List of types of data to remove when retention settings triggers purge. Possible values: ["DIALOGFLOW_HISTORY"] Optional.
</ResponseField>

<ResponseField name="redaction_scope" type="string">
  Defines what types of data to redact. If not set, defaults to not redacting any kind of data. * REDACT_DISK_STORAGE: On data to be written to disk or similar devices that are capable of holding data even if power is disconnected. This includes data that are temporarily saved on disk. Possible values: ["REDACT_DISK_STORAGE"] Optional.
</ResponseField>

<ResponseField name="redaction_strategy" type="string">
  Defines how we redact data. If not set, defaults to not redacting. * REDACT_WITH_SERVICE: Call redaction service to clean up the data to be persisted. Possible values: ["REDACT_WITH_SERVICE"] Optional.
</ResponseField>

<ResponseField name="retention_strategy" type="string">
  Defines how long we retain persisted data that contains sensitive info. Only one of 'retention_window_days' and 'retention_strategy' may be set. * REMOVE_AFTER_CONVERSATION: Removes data when the conversation ends. If there is no conversation explicitly established, a default conversation ends when the corresponding Dialogflow session ends. Possible values: ["REMOVE_AFTER_CONVERSATION"] Optional.
</ResponseField>

<ResponseField name="retention_window_days" type="number">
  Retains the data for the specified number of days. User must set a value lower than Dialogflow's default 365d TTL (30 days for Agent Assist traffic), higher value will be ignored and use default. Setting a value higher than that has no effect. A missing value or setting to 0 also means we use default TTL. Only one of 'retention_window_days' and 'retention_strategy' may be set. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="audio_export_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="audio_export_pattern" type="string">
    </ResponseField>
    <ResponseField name="audio_format" type="string">
    </ResponseField>
    <ResponseField name="enable_audio_redaction" type="bool">
    </ResponseField>
    <ResponseField name="gcs_bucket" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="insights_export_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable_insights_export" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The unique identifier of the settings. Format: projects/&lt;Project ID&gt;/locations/&lt;Location ID&gt;/securitySettings/&lt;Security Settings ID&gt;. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
