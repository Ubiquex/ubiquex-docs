---
title: "google_gkehub_v1beta_fleet"
description: "`google_gkehub_v1beta_fleet` is the top-level GKE Fleet resource -- the real, named grouping every membership (registered cluster), namespace..."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. No stable, generally-available equivalent is published yet.
</Warning>

`google_gkehub_v1beta_fleet` is the top-level GKE Fleet resource -- the real, named grouping every membership (registered cluster), namespace, and scope in a project belongs to, with its own default_cluster_config setting defaults new member clusters inherit.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gkehub "github.com/ubiquex/ubx-sdk-google/sdk/go/google/gkehub"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta fleet"})

    		ubx.Resource(gkehub.V1betaFleet, "example", gkehub.V1betaFleetConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaFleet } from "@ubx/sdk-google/google/gkehub/v1beta_fleet";

    export default stack("example", () => {
      intent({ summary: "example own v1beta fleet" });

      resource(V1betaFleet, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.gkehub.v1beta_fleet import V1betaFleet, V1betaFleetConfig

    def describe():
        ubx.intent("example own v1beta fleet")

        ubx.resource(V1betaFleet, "example", V1betaFleetConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gkehub_v1beta_fleet called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. When the Fleet was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_cluster_config" type="object">
  DefaultClusterConfig describes the default cluster configurations to be applied to all clusters born-in-fleet. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="binary_authorization_config" type="object">
      BinaryAuthorizationConfig defines the fleet level configuration of binary authorization feature. Optional.
      <Expandable title="properties">
        <ResponseField name="evaluation_mode" type="string">
          Optional. Mode of operation for binauthz policy evaluation. Optional.
        </ResponseField>
        <ResponseField name="policy_bindings" type="list(object)">
          Optional. Binauthz policies that apply to this cluster. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="compliance_posture_config" type="object">
      Deprecated: Compliance Posture is no longer supported. For more details, see https://cloud.google.com/kubernetes-engine/docs/deprecations/posture-management-deprecation. CompliancePostureConfig defines the settings needed to enable/disable features for the Compliance Posture. Optional.
      <Expandable title="properties">
        <ResponseField name="compliance_standards" type="list(object)">
          List of enabled compliance standards. Optional.
          <Expandable title="properties">
            <ResponseField name="standard" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="mode" type="string">
          Defines the enablement mode for Compliance Posture. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="security_posture_config" type="object">
      SecurityPostureConfig defines the flags needed to enable/disable features for the Security Posture API. Optional.
      <Expandable title="properties">
        <ResponseField name="mode" type="string">
          Sets which mode to use for Security Posture features. Optional.
        </ResponseField>
        <ResponseField name="vulnerability_mode" type="string">
          Sets which mode to use for vulnerability scanning. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. When the Fleet was deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. A user-assigned display name of the Fleet. When present, it must be between 4 to 30 characters. Allowed characters are: lowercase and uppercase letters, numbers, hyphen, single-quote, double-quote, space, and exclamation point. Example: `Production Fleet` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels for this Fleet. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The full, unique resource name of this fleet in the format of `projects/&#123;project&#125;/locations/&#123;location&#125;/fleets/&#123;fleet&#125;`. Each Google Cloud project can have at most one fleet resource, named "default". Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="object">
  FleetLifecycleState describes the state of a Fleet resource. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
      Output only. The current state of the Fleet resource. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. Google-generated UUID for this resource. This is unique across all Fleet resources. If a Fleet resource is deleted and another resource with the same name is created, it gets a different uid. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. When the Fleet was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
