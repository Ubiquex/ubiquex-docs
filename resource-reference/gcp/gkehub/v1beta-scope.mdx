---
title: "google_gkehub_v1beta_scope"
description: "`google_gkehub_v1beta_scope` is a real Fleet Scope -- a named grouping of namespaces and memberships within a fleet..."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. No stable, generally-available equivalent is published yet.
</Warning>

`google_gkehub_v1beta_scope` is a real Fleet Scope -- a named grouping of namespaces and memberships within a fleet, tracked by its own state and uid -- used to apply configuration (bindings, RBAC, policy) to a real subset of a fleet's clusters rather than the whole fleet at once.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_gkehub_v1beta --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_gkehub_v1beta/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gkehub "github.com/ubiquex/ubx-sdk-google/sdk/go/google_gkehub_v1beta/gkehub"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta scope"})

    		ubx.Resource(gkehub.V1betaGkehubV1betaScope, "example", gkehub.V1betaGkehubV1betaScopeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_gkehub_v1beta --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaGkehubV1betaScope } from "./local-sdk/google_gkehub_v1beta/sdk/typescript/google_gkehub_v1beta/gkehub/v1beta_gkehub_v1beta_scope.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta scope" });

      resource(V1betaGkehubV1betaScope, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_gkehub_v1beta --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_gkehub_v1beta/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_gkehub_v1beta.gkehub import V1betaGkehubV1betaScope, V1betaGkehubV1betaScopeConfig

    def describe():
        ubx.intent("example own v1beta scope")

        ubx.resource(V1betaGkehubV1betaScope, "example", V1betaGkehubV1betaScopeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gkehub_v1beta_scope called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. When the scope was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. When the scope was deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels for this Scope. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name for the scope `projects/&#123;project&#125;/locations/&#123;location&#125;/scopes/&#123;scope&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="namespace_labels" type="map(string)">
  Optional. Scope-level cluster namespace labels. For the member clusters bound to the Scope, these labels are applied to each namespace under the Scope. Scope-level labels take precedence over Namespace-level labels (`namespace_labels` in the Fleet Namespace resource) if they share a key. Keys and values must be Kubernetes-conformant. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="object">
  ScopeLifecycleState describes the state of a Scope resource. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
      Output only. The current state of the scope resource. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. Google-generated UUID for this resource. This is unique across all scope resources. If a scope resource is deleted and another resource with the same name is created, it gets a different uid. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. When the scope was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
