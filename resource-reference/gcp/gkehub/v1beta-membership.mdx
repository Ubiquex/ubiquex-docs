---
title: "google_gkehub_v1beta_membership"
description: "`google_gkehub_v1beta_membership` represents one real Kubernetes cluster registered into a GKE Fleet -- its own authority, endpoint, external_id..."
---

`google_gkehub_v1beta_membership` represents one real Kubernetes cluster registered into a GKE Fleet -- its own authority, endpoint, external_id, cluster_tier, and monitoring_config -- so a cluster (GKE or attached from elsewhere) can be managed as a fleet member rather than only through its own, separate cluster API.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gkehub "github.com/ubiquex/ubx-sdk-google/sdk/go/google/gkehub"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta membership"})

    		ubx.Resource(gkehub.V1betaMembership, "example", gkehub.V1betaMembershipConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaMembership } from "@ubx/sdk-google/google/gkehub/v1beta_membership";

    export default stack("example", () => {
      intent({ summary: "example own v1beta membership" });

      resource(V1betaMembership, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.gkehub import V1betaMembership, V1betaMembershipConfig

    def describe():
        ubx.intent("example own v1beta membership")

        ubx.resource(V1betaMembership, "example", V1betaMembershipConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gkehub_v1beta_membership called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="authority" type="object">
  Authority encodes how Google will recognize identities from this Membership. See the workload identity documentation for more details: https://cloud.google.com/kubernetes-engine/docs/how-to/workload-identity Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="identity_provider" type="string">
      Output only. An identity provider that reflects the `issuer` in the workload identity pool. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="issuer" type="string">
      Optional. A JSON Web Token (JWT) issuer URI. `issuer` must start with `https://` and be a valid URL with length &lt;2000 characters, it must use `location` rather than `zone` for GKE clusters. If set, then Google will allow valid OIDC tokens from this issuer to authenticate within the workload_identity_pool. OIDC discovery will be performed on this URI to validate tokens from the issuer. Clearing `issuer` disables Workload Identity. `issuer` cannot be directly modified; it must be cleared (and Workload Identity disabled) before using a new issuer (and re-enabling Workload Identity). Optional.
    </ResponseField>
    <ResponseField name="oidc_jwks" type="string">
      Optional. OIDC verification keys for this Membership in JWKS format (RFC 7517). When this field is set, OIDC discovery will NOT be performed on `issuer`, and instead OIDC tokens will be validated using this field. Optional.
    </ResponseField>
    <ResponseField name="scope_tenancy_identity_provider" type="string">
      Optional. Output only. The identity provider for the scope-tenancy workload identity pool. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="scope_tenancy_workload_identity_pool" type="string">
      Optional. Output only. The name of the scope-tenancy workload identity pool. This pool is set in the fleet-level feature. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="workload_identity_pool" type="string">
      Output only. The name of the workload identity pool in which `issuer` will be recognized. There is a single Workload Identity Pool per Hub that is shared between all Memberships that belong to that Hub. For a Hub hosted in &#123;PROJECT_ID&#125;, the workload pool format is `&#123;PROJECT_ID&#125;.hub.id.goog`, although this is subject to change in newer versions of this API. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cluster_tier" type="string">
  Output only. The tier of the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. When the Membership was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. When the Membership was deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Output only. Description of this membership, limited to 63 characters. Must match the regex: `a-zA-Z0-9*` This field is present for legacy purposes. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="endpoint" type="object">
  MembershipEndpoint contains information needed to contact a Kubernetes API, endpoint and any additional Kubernetes metadata. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="appliance_cluster" type="object">
      ApplianceCluster contains information specific to GDC Edge Appliance Clusters. Optional.
      <Expandable title="properties">
        <ResponseField name="resource_link" type="string">
          Immutable. Self-link of the Google Cloud resource for the Appliance Cluster. For example: //transferappliance.googleapis.com/projects/my-project/locations/us-west1-a/appliances/my-appliance Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="edge_cluster" type="object">
      EdgeCluster contains information specific to Google Edge Clusters. Optional.
      <Expandable title="properties">
        <ResponseField name="resource_link" type="string">
          Immutable. Self-link of the Google Cloud resource for the Edge Cluster. For example: //edgecontainer.googleapis.com/projects/my-project/locations/us-west1-a/clusters/my-cluster Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="gke_cluster" type="object">
      GkeCluster contains information specific to GKE clusters. Optional.
      <Expandable title="properties">
        <ResponseField name="cluster_missing" type="bool">
          Output only. If cluster_missing is set then it denotes that the GKE cluster no longer exists in the GKE Control Plane. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="resource_link" type="string">
          Immutable. Self-link of the Google Cloud resource for the GKE cluster. For example: //container.googleapis.com/projects/my-project/locations/us-west1-a/clusters/my-cluster Zonal clusters are also supported. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="google_managed" type="bool">
      Output only. Whether the lifecycle of this membership is managed by a google cluster platform service. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="kubernetes_metadata" type="object">
      KubernetesMetadata provides informational metadata for Memberships representing Kubernetes clusters. Optional.
      <Expandable title="properties">
        <ResponseField name="kubernetes_api_server_version" type="string">
          Output only. Kubernetes API server version string as reported by `/version`. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="memory_mb" type="number">
          Output only. The total memory capacity as reported by the sum of all Kubernetes nodes resources, defined in MB. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="node_count" type="number">
          Output only. Node count as reported by Kubernetes nodes resources. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="node_provider_id" type="string">
          Output only. Node providerID as reported by the first node in the list of nodes on the Kubernetes endpoint. On Kubernetes platforms that support zero-node clusters (like GKE on Google Cloud), the node_count will be zero and the node_provider_id will be empty. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="update_time" type="string">
          Output only. The time at which these details were last updated. This update_time is different from the Membership-level update_time since EndpointDetails are updated internally for API consumers. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="vcpu_count" type="number">
          Output only. vCPU count as reported by Kubernetes nodes resources. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kubernetes_resource" type="object">
      KubernetesResource contains the YAML manifests and configuration for Membership Kubernetes resources in the cluster. After CreateMembership or UpdateMembership, these resources should be re-applied in the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="connect_resources" type="list(object)">
          Output only. The Kubernetes resources for installing the GKE Connect agent This field is only populated in the Membership returned from a successful long-running operation from CreateMembership or UpdateMembership. It is not populated during normal GetMembership or ListMemberships requests. To get the resource manifest after the initial registration, the caller should make a UpdateMembership call with an empty field mask. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="cluster_scoped" type="bool">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="manifest" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="membership_cr_manifest" type="string">
          Input only. The YAML representation of the Membership CR. This field is ignored for GKE clusters where Hub can read the CR directly. Callers should provide the CR that is currently present in the cluster during CreateMembership or UpdateMembership, or leave this field empty if none exists. The CR manifest is used to validate the cluster has not been registered with another Membership. Optional.
        </ResponseField>
        <ResponseField name="membership_resources" type="list(object)">
          Output only. Additional Kubernetes resources that need to be applied to the cluster after Membership creation, and after every update. This field is only populated in the Membership returned from a successful long-running operation from CreateMembership or UpdateMembership. It is not populated during normal GetMembership or ListMemberships requests. To get the resource manifest after the initial registration, the caller should make a UpdateMembership call with an empty field mask. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="cluster_scoped" type="bool">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
            <ResponseField name="manifest" type="string">
              Optional; if omitted, computed by Google Cloud.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource_options" type="object">
          ResourceOptions represent options for Kubernetes resource generation. Optional.
          <Expandable title="properties">
            <ResponseField name="connect_version" type="string">
              Optional. The Connect agent version to use for connect_resources. Defaults to the latest GKE Connect version. The version must be a currently supported version, obsolete versions will be rejected. Optional.
            </ResponseField>
            <ResponseField name="k8s_git_version" type="string">
              Optional. Git version of the Kubernetes cluster. This is only used to gate the Connect Agent migration to svc.id.goog on GDC-SO 1.33.100 patch and above. Optional.
            </ResponseField>
            <ResponseField name="k8s_version" type="string">
              Optional. Major and minor version of the Kubernetes cluster. This is only used to determine which version to use for the CustomResourceDefinition resources, `apiextensions/v1beta1` or`apiextensions/v1`. Optional.
            </ResponseField>
            <ResponseField name="v1beta1_crd" type="bool">
              Optional. Use `apiextensions/v1beta1` instead of `apiextensions/v1` for CustomResourceDefinition resources. This option should be set for clusters with Kubernetes apiserver versions &lt;1.16. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="multi_cloud_cluster" type="object">
      MultiCloudCluster contains information specific to GKE Multi-Cloud clusters. Optional.
      <Expandable title="properties">
        <ResponseField name="cluster_missing" type="bool">
          Output only. If cluster_missing is set then it denotes that API(gkemulticloud.googleapis.com) resource for this GKE Multi-Cloud cluster no longer exists. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="resource_link" type="string">
          Immutable. Self-link of the Google Cloud resource for the GKE Multi-Cloud cluster. For example: //gkemulticloud.googleapis.com/projects/my-project/locations/us-west1-a/awsClusters/my-cluster //gkemulticloud.googleapis.com/projects/my-project/locations/us-west1-a/azureClusters/my-cluster //gkemulticloud.googleapis.com/projects/my-project/locations/us-west1-a/attachedClusters/my-cluster Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="on_prem_cluster" type="object">
      OnPremCluster contains information specific to GKE On-Prem clusters. Optional.
      <Expandable title="properties">
        <ResponseField name="admin_cluster" type="bool">
          Immutable. Whether the cluster is an admin cluster. Optional.
        </ResponseField>
        <ResponseField name="cluster_missing" type="bool">
          Output only. If cluster_missing is set then it denotes that API(gkeonprem.googleapis.com) resource for this GKE On-Prem cluster no longer exists. Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="cluster_type" type="string">
          Immutable. The on prem cluster's type. Optional.
        </ResponseField>
        <ResponseField name="resource_link" type="string">
          Immutable. Self-link of the Google Cloud resource for the GKE On-Prem cluster. For example: //gkeonprem.googleapis.com/projects/my-project/locations/us-west1-a/vmwareClusters/my-cluster //gkeonprem.googleapis.com/projects/my-project/locations/us-west1-a/bareMetalClusters/my-cluster Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="external_id" type="string">
  Optional. An externally-generated and managed ID for this Membership. This ID may be modified after creation, but this is not recommended. The ID must match the regex: `a-zA-Z0-9*` If this Membership represents a Kubernetes cluster, this value should be set to the UID of the `kube-system` namespace object. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels for this membership. These labels are not leveraged by multi-cluster features, instead, we prefer cluster labels, which can be set on GKE cluster or other cluster types. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="last_connection_time" type="string">
  Output only. For clusters using Connect, the timestamp of the most recent connection established with Google Cloud. This time is updated every several minutes, not continuously. For clusters that do not use GKE Connect, or that have never connected successfully, this field will be unset. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="membership_type" type="string">
  Output only. The type of the membership. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="monitoring_config" type="object">
  MonitoringConfig informs Fleet-based applications/services/UIs how the metrics for the underlying cluster is reported to cloud monitoring services. It can be set from empty to non-empty, but can't be mutated directly to prevent accidentally breaking the constinousty of metrics. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cluster" type="string">
      Optional. Cluster name used to report metrics. For Anthos on VMWare/Baremetal/MultiCloud clusters, it would be in format &#123;cluster_type&#125;/&#123;cluster_name&#125;, e.g., "awsClusters/cluster_1". Optional.
    </ResponseField>
    <ResponseField name="cluster_hash" type="string">
      Optional. For GKE and Multicloud clusters, this is the UUID of the cluster resource. For VMWare and Baremetal clusters, this is the kube-system UID. Optional.
    </ResponseField>
    <ResponseField name="kubernetes_metrics_prefix" type="string">
      Optional. Kubernetes system metrics, if available, are written to this prefix. This defaults to kubernetes.io for GKE, and kubernetes.io/anthos for Anthos eventually. Noted: Anthos MultiCloud will have kubernetes.io prefix today but will migration to be under kubernetes.io/anthos. Optional.
    </ResponseField>
    <ResponseField name="location" type="string">
      Optional. Location used to report Metrics Optional.
    </ResponseField>
    <ResponseField name="project_id" type="string">
      Optional. Project used to report Metrics Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The full, unique name of this Membership resource in the format `projects/*/locations/*/memberships/&#123;membership_id&#125;`, set during creation. `membership_id` must be a valid RFC 1123 compliant DNS label: 1. At most 63 characters in length 2. It must consist of lower case alphanumeric characters or `-` 3. It must start and end with an alphanumeric character Which can be expressed as the regex: `[a-z0-9]([-a-z0-9]*[a-z0-9])?`, with a maximum length of 63 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="object">
  MembershipState describes the state of a Membership resource. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
      Output only. The current state of the Membership resource. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="unique_id" type="string">
  Output only. Google-generated UUID for this resource. This is unique across all Membership resources. If a Membership resource is deleted and another resource with the same name is created, it gets a different unique_id. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. When the Membership was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
