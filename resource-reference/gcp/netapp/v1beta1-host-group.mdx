---
title: "google_netapp_v1beta1_host_group"
description: "`google_netapp_v1beta1_host_group` defines a named group of client host IP address(es) Google Cloud NetApp Volumes export policy rules can reference..."
---

`google_netapp_v1beta1_host_group` defines a named group of client host IP address(es) Google Cloud NetApp Volumes export policy rules can reference together.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	netapp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/netapp"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta1 host group"})

    		ubx.Resource(netapp.V1beta1HostGroup, "example", netapp.V1beta1HostGroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta1HostGroup } from "@ubx/sdk-google/google/netapp/v1beta1_host_group";

    export default stack("example", () => {
      intent({ summary: "example own v1beta1 host group" });

      resource(V1beta1HostGroup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.netapp import V1beta1HostGroup, V1beta1HostGroupConfig

    def describe():
        ubx.intent("example own v1beta1 host group")

        ubx.resource(V1beta1HostGroup, "example", V1beta1HostGroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_netapp_v1beta1_host_group called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Create time of the host group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the host group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="hosts" type="list(string)">
  Required. The list of hosts associated with the host group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels of the host group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the host group. Format: `projects/&#123;project_number&#125;/locations/&#123;location_id&#125;/hostGroups/&#123;host_group_id&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="os_type" type="string">
  Required. The OS type of the host group. It indicates the type of operating system used by all of the hosts in the HostGroup. All hosts in a HostGroup must be of the same OS type. This can be set only when creating a HostGroup. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the host group. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  Required. Type of the host group. Optional; if omitted, computed by Google Cloud.
</ResponseField>
