---
title: "google_netapp_backup"
description: "Real, generated bindings for `google_netapp_backup`."
---

`google_netapp_backup` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	netapp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/netapp"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own backup"})

    		ubx.Resource(netapp.Backup, "ci-runner", netapp.BackupConfig{
    			Location:       "example",
    			Name:           "ci-runner",
    			VaultName:      "example-vault-name",
    			Description:    "Managed by ubx.",
    			Labels:         map[string]string{"managed-by": "ubx"},
    			SourceSnapshot: "example",
    			SourceVolume:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Backup } from "jsr:@ubx/sdk-google/google/netapp/backup";

    export default stack("payments", () => {
      intent({ summary: "payments own backup" });

      resource(Backup, "ci-runner", {
        location: "example",
        name: "ci-runner",
        vaultName: "example-vault-name",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
        sourceSnapshot: "example",
        sourceVolume: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.netapp import Backup, BackupConfig

    def describe():
        ubx.intent("payments own backup")

        ubx.resource(Backup, "ci-runner", BackupConfig(
            location="example",
            name="ci-runner",
            vault_name="example-vault-name",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
            source_snapshot="example",
            source_volume="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_netapp_backup called "ci-runner", with location
    example, vault name example-vault-name, description Managed by
    ubx., labels a set value, source snapshot example, source volume
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the backup with 2048 characters or less. Requests with longer descriptions will be rejected. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs. Example: '&#123; "owner": "Bob", "department": "finance", "purpose": "testing" &#125;'. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Location of the backup. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The resource name of the backup. Needs to be unique per location. Required.
</ResponseField>

<ResponseField name="ontap_source" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="snapshot_uuid" type="string">
    </ResponseField>
    <ResponseField name="storage_pool" type="string" required>
    </ResponseField>
    <ResponseField name="volume_uuid" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_snapshot" type="string">
  If specified, backup will be created from the given snapshot. If not specified, there will be a new snapshot taken to initiate the backup creation. Format: 'projects/&#123;&#123;projectId&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/volumes/&#123;&#123;volumename&#125;&#125;/snapshots/&#123;&#123;snapshotname&#125;&#125;'' Optional.
</ResponseField>

<ResponseField name="source_volume" type="string">
  ID of volumes this backup belongs to. Format: 'projects/&#123;&#123;projects_id&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/volumes/&#123;&#123;name&#125;&#125;'' Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  Name of the backup vault to store the backup in. Required.
</ResponseField>

## Output properties

<ResponseField name="backup_region" type="string">
  Region in which backup is stored. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="backup_type" type="string">
  Type of backup, manually created or created by a backup policy. Possible Values : [TYPE_UNSPECIFIED, MANUAL, SCHEDULED] Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="chain_storage_bytes" type="string">
  Backups of a volume build incrementally on top of each other. They form a "backup chain". Total size of all backups in a chain in bytes = baseline backup size + sum(incremental backup size) Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Create time of the backup. A timestamp in RFC3339 UTC "Zulu" format. Examples: "2023-06-22T09:13:01.617Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ontap_source" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="snapshot_uuid" type="string">
    </ResponseField>
    <ResponseField name="storage_pool" type="string" required>
    </ResponseField>
    <ResponseField name="volume_uuid" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the Backup Vault. Possible Values : [STATE_UNSPECIFIED, CREATING, UPLOADING, READY, DELETING, ERROR, UPDATING] Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="volume_region" type="string">
  Region of the volume from which the backup was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="volume_usage_bytes" type="string">
  Size of the file system when the backup was created. When creating a new volume from the backup, the volume capacity will have to be at least as big. Computed by GCP after `ship`.
</ResponseField>
