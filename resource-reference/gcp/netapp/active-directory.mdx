---
title: "google_netapp_active_directory"
description: "Real, generated bindings for `google_netapp_active_directory`."
---

`google_netapp_active_directory` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	netapp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/netapp"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own active directory"})

    		ubx.Resource(netapp.ActiveDirectory, "ci-runner", netapp.ActiveDirectoryConfig{
    			Dns:            "example",
    			Domain:         "example",
    			Location:       "example",
    			Name:           "ci-runner",
    			NetBiosPrefix:  "example",
    			Password:       "example",
    			Username:       "example",
    			Administrators: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ActiveDirectory } from "jsr:@ubx/sdk-google/google/netapp/active_directory";

    export default stack("payments", () => {
      intent({ summary: "payments own active directory" });

      resource(ActiveDirectory, "ci-runner", {
        dns: "example",
        domain: "example",
        location: "example",
        name: "ci-runner",
        netBiosPrefix: "example",
        password: "example",
        username: "example",
        administrators: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.netapp import ActiveDirectory, ActiveDirectoryConfig

    def describe():
        ubx.intent("payments own active directory")

        ubx.resource(ActiveDirectory, "ci-runner", ActiveDirectoryConfig(
            dns="example",
            domain="example",
            location="example",
            name="ci-runner",
            net_bios_prefix="example",
            password="example",
            username="example",
            administrators=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_netapp_active_directory called "ci-runner", with
    dns example, domain example, location example, net bios prefix
    example, password example, username example, administrators a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="administrators" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="aes_encryption" type="bool">
  Optional.
</ResponseField>

<ResponseField name="backup_operators" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="dns" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain" type="string" required>
  Required.
</ResponseField>

<ResponseField name="encrypt_dc_connections" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kdc_hostname" type="string">
  Optional.
</ResponseField>

<ResponseField name="kdc_ip" type="string">
  Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="ldap_signing" type="bool">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="net_bios_prefix" type="string" required>
  Required.
</ResponseField>

<ResponseField name="nfs_users_with_ldap" type="bool">
  Optional.
</ResponseField>

<ResponseField name="organizational_unit" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="password" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="security_operators" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="site" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="username" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="organizational_unit" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state_details" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
