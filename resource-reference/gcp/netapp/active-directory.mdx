---
title: "google_netapp_active_directory"
description: "Real, generated bindings for `google_netapp_active_directory`."
---

`google_netapp_active_directory` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	netapp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/netapp"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own active directory"})

    		ubx.Resource(netapp.ActiveDirectory, "ci-runner", netapp.ActiveDirectoryConfig{
    			Dns:            "example",
    			Domain:         "example",
    			Location:       "example",
    			Name:           "ci-runner",
    			NetBiosPrefix:  "example",
    			Password:       "example",
    			Username:       "example",
    			Administrators: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ActiveDirectory } from "jsr:@ubx/sdk-google/google/netapp/active_directory";

    export default stack("payments", () => {
      intent({ summary: "payments own active directory" });

      resource(ActiveDirectory, "ci-runner", {
        dns: "example",
        domain: "example",
        location: "example",
        name: "ci-runner",
        netBiosPrefix: "example",
        password: "example",
        username: "example",
        administrators: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.netapp import ActiveDirectory, ActiveDirectoryConfig

    def describe():
        ubx.intent("payments own active directory")

        ubx.resource(ActiveDirectory, "ci-runner", ActiveDirectoryConfig(
            dns="example",
            domain="example",
            location="example",
            name="ci-runner",
            net_bios_prefix="example",
            password="example",
            username="example",
            administrators=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_netapp_active_directory called "ci-runner", with
    dns example, domain example, location example, net bios prefix
    example, password example, username example, administrators a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="administrators" type="list(string)">
  Domain user accounts to be added to the local Administrators group of the SMB service. Comma-separated list of domain users or groups. The Domain Admin group is automatically added when the service joins your domain as a hidden group. Optional.
</ResponseField>

<ResponseField name="aes_encryption" type="bool">
  Enables AES-128 and AES-256 encryption for Kerberos-based communication with Active Directory. Optional.
</ResponseField>

<ResponseField name="backup_operators" type="list(string)">
  Domain user/group accounts to be added to the Backup Operators group of the SMB service. The Backup Operators group allows members to backup and restore files regardless of whether they have read or write access to the files. Comma-separated list. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional.
</ResponseField>

<ResponseField name="dns" type="string" required>
  Comma separated list of DNS server IP addresses for the Active Directory domain. Required.
</ResponseField>

<ResponseField name="domain" type="string" required>
  Fully qualified domain name for the Active Directory domain. Required.
</ResponseField>

<ResponseField name="encrypt_dc_connections" type="bool">
  If enabled, traffic between the SMB server to Domain Controller (DC) will be encrypted. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kdc_hostname" type="string">
  Hostname of the Active Directory server used as Kerberos Key Distribution Center. Only required for volumes using kerberized NFSv4.1 Optional.
</ResponseField>

<ResponseField name="kdc_ip" type="string">
  IP address of the Active Directory server used as Kerberos Key Distribution Center. Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs. Example: '&#123; "owner": "Bob", "department": "finance", "purpose": "testing" &#125;'. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="ldap_signing" type="bool">
  Specifies whether or not the LDAP traffic needs to be signed. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Name of the region for the policy to apply to. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The resource name of the Active Directory pool. Needs to be unique per location. Required.
</ResponseField>

<ResponseField name="net_bios_prefix" type="string" required>
  NetBIOS name prefix of the server to be created. A five-character random ID is generated automatically, for example, -6f9a, and appended to the prefix. The full UNC share path will have the following format: '\\NetBIOS_PREFIX-ABCD.DOMAIN_NAME\SHARE_NAME' Required.
</ResponseField>

<ResponseField name="nfs_users_with_ldap" type="bool">
  Local UNIX users on clients without valid user information in Active Directory are blocked from access to LDAP enabled volumes. This option can be used to temporarily switch such volumes to AUTH_SYS authentication (user ID + 1-16 groups). Optional.
</ResponseField>

<ResponseField name="organizational_unit" type="string">
  Name of the Organizational Unit where you intend to create the computer account for NetApp Volumes. Defaults to 'CN=Computers' if left empty. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="password" type="string" required>
  Password for specified username. Note - Manual changes done to the password will not be detected. Terraform will not re-apply the password, unless you use a new password in Terraform. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="security_operators" type="list(string)">
  Domain accounts that require elevated privileges such as 'SeSecurityPrivilege' to manage security logs. Comma-separated list. Optional.
</ResponseField>

<ResponseField name="site" type="string">
  Specifies an Active Directory site to manage domain controller selection. Use when Active Directory domain controllers in multiple regions are configured. Defaults to 'Default-First-Site-Name' if left empty. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="username" type="string" required>
  Username for the Active Directory account with permissions to create the compute account within the specified organizational unit. Required.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Create time of the active directory. A timestamp in RFC3339 UTC "Zulu" format. Examples: "2023-06-22T09:13:01.617Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="organizational_unit" type="string">
  Name of the Organizational Unit where you intend to create the computer account for NetApp Volumes. Defaults to 'CN=Computers' if left empty. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the Active Directory policy (not the Active Directory itself). Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state_details" type="string">
  The state details of the Active Directory. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
