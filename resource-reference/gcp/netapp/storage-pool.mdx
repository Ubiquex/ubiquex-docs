---
title: "google_netapp_storage_pool"
description: "Real, generated bindings for `google_netapp_storage_pool`."
---

`google_netapp_storage_pool` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	netapp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/netapp"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own storage pool"})

    		ubx.Resource(netapp.StoragePool, "ci-runner", netapp.StoragePoolConfig{
    			CapacityGib:      "example",
    			Location:         "example",
    			Name:             "ci-runner",
    			Network:          "example",
    			ServiceLevel:     "example",
    			ActiveDirectory:  "example",
    			AllowAutoTiering: true,
    			Description:      "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StoragePool } from "jsr:@ubx/sdk-google/google/netapp/storage_pool";

    export default stack("payments", () => {
      intent({ summary: "payments own storage pool" });

      resource(StoragePool, "ci-runner", {
        capacityGib: "example",
        location: "example",
        name: "ci-runner",
        network: "example",
        serviceLevel: "example",
        activeDirectory: "example",
        allowAutoTiering: true,
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.netapp import StoragePool, StoragePoolConfig

    def describe():
        ubx.intent("payments own storage pool")

        ubx.resource(StoragePool, "ci-runner", StoragePoolConfig(
            capacity_gib="example",
            location="example",
            name="ci-runner",
            network="example",
            service_level="example",
            active_directory="example",
            allow_auto_tiering=True,
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_netapp_storage_pool called "ci-runner", with
    capacity gib example, location example, network example, service
    level example, active directory example, allow auto tiering true,
    description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="active_directory" type="string">
  Specifies the Active Directory policy to be used. Format: 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/activeDirectories/&#123;&#123;name&#125;&#125;'. The policy needs to be in the same location as the storage pool. Optional.
</ResponseField>

<ResponseField name="allow_auto_tiering" type="bool">
  Optional. True if the storage pool supports Auto Tiering enabled volumes. Default is false. Auto-tiering can be enabled after storage pool creation but it can't be disabled once enabled. Optional.
</ResponseField>

<ResponseField name="capacity_gib" type="string" required>
  Capacity of the storage pool (in GiB). Required.
</ResponseField>

<ResponseField name="custom_performance_enabled" type="bool">
  Optional. True if using Independent Scaling of capacity and performance (Hyperdisk). Default is false. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional.
</ResponseField>

<ResponseField name="enable_hot_tier_auto_resize" type="bool">
  Flag indicating that the hot-tier threshold will be auto-increased by 10% of the hot-tier when it hits 100%. Default is true. The increment will kick in only if the new size after increment is still less than or equal to storage pool size. Optional.
</ResponseField>

<ResponseField name="hot_tier_size_gib" type="string">
  Total hot tier capacity for the Storage Pool. It is applicable only to Flex service level. It should be less than the minimum storage pool size and cannot be more than the current storage pool size. It cannot be decreased once set. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_config" type="string">
  Specifies the CMEK policy to be used for volume encryption. Format: 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/kmsConfigs/&#123;&#123;name&#125;&#125;'. The policy needs to be in the same location as the storage pool. Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs. Example: '&#123; "owner": "Bob", "department": "finance", "purpose": "testing" &#125;'. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="ldap_enabled" type="bool">
  When enabled, the volumes uses Active Directory as LDAP name service for UID/GID lookups. Required to enable extended group support for NFSv3, using security identifiers for NFSv4.1 or principal names for kerberized NFSv4.1. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Name of the location. For zonal Flex pools specify a zone name, in all other cases a region name. Required.
</ResponseField>

<ResponseField name="mode" type="string">
  Mode of the storage pool. The operational mode of the storage pool. ONTAP mode enables operations via ONTAP Mode APIs, while DEFAULT mode enables operations via NetApp Volumes APIs. If not specified during creation, the mode defaults to DEFAULT. Possible values: ["MODE_UNSPECIFIED", "DEFAULT", "ONTAP"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  The resource name of the storage pool. Needs to be unique per location/region. Required.
</ResponseField>

<ResponseField name="network" type="string" required>
  VPC network name with format: 'projects/&#123;&#123;project&#125;&#125;/global/networks/&#123;&#123;network&#125;&#125;' Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="qos_type" type="string">
  QoS (Quality of Service) type of the storage pool. Possible values are: AUTO, MANUAL. Possible values: ["QOS_TYPE_UNSPECIFIED", "AUTO", "MANUAL"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="replica_zone" type="string">
  Specifies the replica zone for regional Flex pools. 'zone' and 'replica_zone' values can be swapped to initiate a [zone switch](https://cloud.google.com/netapp/volumes/docs/configure-and-use/storage-pools/edit-or-delete-storage-pool#switch_active_and_replica_zones). Optional.
</ResponseField>

<ResponseField name="scale_type" type="string">
  The scale type of the storage pool. Defaults to 'SCALE_TYPE_DEFAULT' if not specified. Possible values: ["SCALE_TYPE_UNSPECIFIED", "SCALE_TYPE_DEFAULT", "SCALE_TYPE_SCALEOUT"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_level" type="string" required>
  Service level of the storage pool. Possible values: ["PREMIUM", "EXTREME", "STANDARD", "FLEX"] Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="total_iops" type="string">
  Optional. Custom Performance Total IOPS of the pool If not provided, it will be calculated based on the totalThroughputMibps Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="total_throughput_mibps" type="string">
  Optional. Custom Performance Total Throughput of the pool (in MiB/s). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="type" type="string">
  Type of the storage pool. This field is used to control whether the pool supports FILE based volumes only or UNIFIED (both FILE and BLOCK) volumes. If not specified during creation, it defaults to FILE. Possible values: ["STORAGE_POOL_TYPE_UNSPECIFIED", "FILE", "UNIFIED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="zone" type="string">
  Specifies the active zone for regional Flex pools. 'zone' and 'replica_zone' values can be swapped to initiate a [zone switch](https://cloud.google.com/netapp/volumes/docs/configure-and-use/storage-pools/edit-or-delete-storage-pool#switch_active_and_replica_zones). If you want to create a zonal Flex pool, specify a zone name for 'location' and omit 'zone'. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="available_throughput_mibps" type="number">
  Available throughput of the storage pool (in MiB/s). Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="cold_tier_size_used_gib" type="string">
  Total cold tier data rounded down to the nearest GiB used by the storage pool. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="custom_performance_enabled" type="bool">
  Optional. True if using Independent Scaling of capacity and performance (Hyperdisk). Default is false. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="encryption_type" type="string">
  Reports if volumes in the pool are encrypted using a Google-managed encryption key or CMEK. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="hot_tier_size_used_gib" type="string">
  Total hot tier data rounded down to the nearest GiB used by the storage pool. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="mode" type="string">
  Mode of the storage pool. The operational mode of the storage pool. ONTAP mode enables operations via ONTAP Mode APIs, while DEFAULT mode enables operations via NetApp Volumes APIs. If not specified during creation, the mode defaults to DEFAULT. Possible values: ["MODE_UNSPECIFIED", "DEFAULT", "ONTAP"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="qos_type" type="string">
  QoS (Quality of Service) type of the storage pool. Possible values are: AUTO, MANUAL. Possible values: ["QOS_TYPE_UNSPECIFIED", "AUTO", "MANUAL"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="scale_type" type="string">
  The scale type of the storage pool. Defaults to 'SCALE_TYPE_DEFAULT' if not specified. Possible values: ["SCALE_TYPE_UNSPECIFIED", "SCALE_TYPE_DEFAULT", "SCALE_TYPE_SCALEOUT"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="total_iops" type="string">
  Optional. Custom Performance Total IOPS of the pool If not provided, it will be calculated based on the totalThroughputMibps Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="total_throughput_mibps" type="string">
  Optional. Custom Performance Total Throughput of the pool (in MiB/s). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="type" type="string">
  Type of the storage pool. This field is used to control whether the pool supports FILE based volumes only or UNIFIED (both FILE and BLOCK) volumes. If not specified during creation, it defaults to FILE. Possible values: ["STORAGE_POOL_TYPE_UNSPECIFIED", "FILE", "UNIFIED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="volume_capacity_gib" type="string">
  Size allocated to volumes in the storage pool (in GiB). Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="volume_count" type="number">
  Number of volume in the storage pool. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="zone" type="string">
  Specifies the active zone for regional Flex pools. 'zone' and 'replica_zone' values can be swapped to initiate a [zone switch](https://cloud.google.com/netapp/volumes/docs/configure-and-use/storage-pools/edit-or-delete-storage-pool#switch_active_and_replica_zones). If you want to create a zonal Flex pool, specify a zone name for 'location' and omit 'zone'. Optional; if omitted, computed by GCP.
</ResponseField>
