---
title: "google_netapp_volume_replication"
description: "Real, generated bindings for `google_netapp_volume_replication`."
---

`google_netapp_volume_replication` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	netapp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/netapp"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own volume replication"})

    		ubx.Resource(netapp.VolumeReplication, "ci-runner", netapp.VolumeReplicationConfig{
    			Location:                "example",
    			Name:                    "ci-runner",
    			ReplicationSchedule:     "example",
    			VolumeName:              "example-volume-name",
    			DeleteDestinationVolume: true,
    			Description:             "Managed by ubx.",
    			ForceStopping:           true,
    			Labels:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VolumeReplication } from "jsr:@ubx/sdk-google/google/netapp/volume_replication";

    export default stack("payments", () => {
      intent({ summary: "payments own volume replication" });

      resource(VolumeReplication, "ci-runner", {
        location: "example",
        name: "ci-runner",
        replicationSchedule: "example",
        volumeName: "example-volume-name",
        deleteDestinationVolume: true,
        description: "Managed by ubx.",
        forceStopping: true,
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.netapp import VolumeReplication, VolumeReplicationConfig

    def describe():
        ubx.intent("payments own volume replication")

        ubx.resource(VolumeReplication, "ci-runner", VolumeReplicationConfig(
            location="example",
            name="ci-runner",
            replication_schedule="example",
            volume_name="example-volume-name",
            delete_destination_volume=True,
            description="Managed by ubx.",
            force_stopping=True,
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_netapp_volume_replication called "ci-runner", with
    location example, replication schedule example, volume name
    example-volume-name, delete destination volume true, description
    Managed by ubx., force stopping true, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="delete_destination_volume" type="bool">
  A destination volume is created as part of replication creation. The destination volume will not became under Terraform management unless you import it manually. If you delete the replication, this volume will remain. Setting this parameter to true will delete the *current* destination volume when destroying the replication. If you reversed the replication direction, this will be your former source volume! For production use, it is recommended to keep this parameter false to avoid accidental volume deletion. Handle with care. Default is false. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An description of this resource. Optional.
</ResponseField>

<ResponseField name="destination_volume_parameters" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="share_name" type="string">
    </ResponseField>
    <ResponseField name="storage_pool" type="string" required>
    </ResponseField>
    <ResponseField name="tiering_policy" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="cooling_threshold_days" type="number">
        </ResponseField>
        <ResponseField name="tier_action" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="volume_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="force_stopping" type="bool">
  Only replications with mirror_state=MIRRORED can be stopped. A replication in mirror_state=TRANSFERRING currently receives an update and stopping the update might be undesirable. Set this parameter to true to stop anyway. All data transferred to the destination will be discarded and content of destination volume will remain at the state of the last successful update. Default is false. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs. Example: '&#123; "owner": "Bob", "department": "finance", "purpose": "testing" &#125;' **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Name of region for this resource. The resource needs to be created in the region of the destination volume. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the replication. Needs to be unique per location. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="replication_enabled" type="bool">
  Set to false to stop/break the mirror. Stopping the mirror makes the destination volume read-write and act independently from the source volume. Set to true to enable/resume the mirror. WARNING: Resuming a mirror overwrites any changes done to the destination volume with the content of the source volume. Optional.
</ResponseField>

<ResponseField name="replication_schedule" type="string" required>
  Specifies the replication interval. Possible values: ["EVERY_10_MINUTES", "HOURLY", "DAILY"] Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="volume_name" type="string" required>
  The name of the existing source volume. Required.
</ResponseField>

<ResponseField name="wait_for_mirror" type="bool">
  Replication resource state is independent of mirror_state. With enough data, it can take many hours for mirror_state to reach MIRRORED. If you want Terraform to wait for the mirror to finish on create/stop/resume operations, set this parameter to true. Default is false. Optional.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Create time of the active directory. A timestamp in RFC3339 UTC "Zulu" format. Examples: "2023-06-22T09:13:01.617Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="destination_volume" type="string">
  Full resource name of destination volume with format: 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/volumes/&#123;&#123;volumeId&#125;&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="destination_volume_parameters" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="share_name" type="string">
    </ResponseField>
    <ResponseField name="storage_pool" type="string" required>
    </ResponseField>
    <ResponseField name="tiering_policy" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="cooling_threshold_days" type="number">
        </ResponseField>
        <ResponseField name="tier_action" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="volume_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="healthy" type="bool">
  Condition of the relationship. Can be one of the following: - true: The replication relationship is healthy. It has not missed the most recent scheduled transfer. - false: The replication relationship is not healthy. It has missed the most recent scheduled transfer. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="hybrid_peering_details" type="list(object)">
  HybridPeeringDetails contains details about the hybrid peering. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="command" type="string">
    </ResponseField>
    <ResponseField name="command_expiry_time" type="string">
    </ResponseField>
    <ResponseField name="passphrase" type="string">
    </ResponseField>
    <ResponseField name="peer_cluster_name" type="string">
    </ResponseField>
    <ResponseField name="peer_svm_name" type="string">
    </ResponseField>
    <ResponseField name="peer_volume_name" type="string">
    </ResponseField>
    <ResponseField name="subnet_ip" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="hybrid_replication_type" type="string">
  Hybrid replication type. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="hybrid_replication_user_commands" type="list(object)">
  Copy pastable snapmirror commands to be executed on onprem cluster by the customer. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="commands" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="mirror_state" type="string">
  Indicates the state of the mirror between source and destination volumes. Depending on the amount of data in your source volume, PREPARING phase can take hours or days. mirrorState = MIRRORED indicates your baseline transfer ended and destination volume became accessible read-only. TRANSFERRING means a MIRRORED volume currently receives an update. Updated every 5 minutes. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role" type="string">
  Reverting a replication can swap source and destination volume roles. This field indicates if the 'location' hosts the source or destination volume. For resume and revert and resume operations it is critical to understand which volume is the source volume, since it will overwrite changes done to the destination volume. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="source_volume" type="string">
  Full resource name of source volume with format: 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/volumes/&#123;&#123;volumeId&#125;&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Indicates the state of replication resource. State of the mirror itself is indicated in mirrorState. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state_details" type="string">
  State details of the replication resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transfer_stats" type="list(object)">
  Replication transfer statistics. All statistics are updated every 5 minutes. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="lag_duration" type="string">
    </ResponseField>
    <ResponseField name="last_transfer_bytes" type="string">
    </ResponseField>
    <ResponseField name="last_transfer_duration" type="string">
    </ResponseField>
    <ResponseField name="last_transfer_end_time" type="string">
    </ResponseField>
    <ResponseField name="last_transfer_error" type="string">
    </ResponseField>
    <ResponseField name="total_transfer_duration" type="string">
    </ResponseField>
    <ResponseField name="transfer_bytes" type="string">
    </ResponseField>
    <ResponseField name="update_time" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
