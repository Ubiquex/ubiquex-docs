---
title: "google_netapp_backup_vault"
description: "Real, generated bindings for `google_netapp_backup_vault`."
---

`google_netapp_backup_vault` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	netapp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/netapp"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own backup vault"})

    		ubx.Resource(netapp.BackupVault, "ci-runner", netapp.BackupVaultConfig{
    			Location:     "example",
    			Name:         "ci-runner",
    			BackupRegion: "example",
    			Description:  "Managed by ubx.",
    			KmsConfig:    "example",
    			Labels:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BackupVault } from "jsr:@ubx/sdk-google/google/netapp/backup_vault";

    export default stack("payments", () => {
      intent({ summary: "payments own backup vault" });

      resource(BackupVault, "ci-runner", {
        location: "example",
        name: "ci-runner",
        backupRegion: "example",
        description: "Managed by ubx.",
        kmsConfig: "example",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.netapp import BackupVault, BackupVaultConfig

    def describe():
        ubx.intent("payments own backup vault")

        ubx.resource(BackupVault, "ci-runner", BackupVaultConfig(
            location="example",
            name="ci-runner",
            backup_region="example",
            description="Managed by ubx.",
            kms_config="example",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_netapp_backup_vault called "ci-runner", with
    location example, backup region example, description Managed by
    ubx., kms config example, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backup_region" type="string">
  Region in which backup is stored. Optional.
</ResponseField>

<ResponseField name="backup_retention_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="backup_minimum_enforced_retention_days" type="number" required>
    </ResponseField>
    <ResponseField name="daily_backup_immutable" type="bool">
    </ResponseField>
    <ResponseField name="manual_backup_immutable" type="bool">
    </ResponseField>
    <ResponseField name="monthly_backup_immutable" type="bool">
    </ResponseField>
    <ResponseField name="weekly_backup_immutable" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="backup_vault_type" type="string">
  Type of the backup vault to be created. Default is IN_REGION. Possible values: ["BACKUP_VAULT_TYPE_UNSPECIFIED", "IN_REGION", "CROSS_REGION"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_config" type="string">
  Specifies the Key Management System (KMS) configuration to be used for backup encryption. Format: 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/kmsConfigs/&#123;&#123;kms_config&#125;&#125;' Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs. Example: '&#123; "owner": "Bob", "department": "finance", "purpose": "testing" &#125;'. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Location (region) of the backup vault. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The resource name of the backup vault. Needs to be unique per location. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="backup_retention_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="backup_minimum_enforced_retention_days" type="number" required>
    </ResponseField>
    <ResponseField name="daily_backup_immutable" type="bool">
    </ResponseField>
    <ResponseField name="manual_backup_immutable" type="bool">
    </ResponseField>
    <ResponseField name="monthly_backup_immutable" type="bool">
    </ResponseField>
    <ResponseField name="weekly_backup_immutable" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="backup_vault_type" type="string">
  Type of the backup vault to be created. Default is IN_REGION. Possible values: ["BACKUP_VAULT_TYPE_UNSPECIFIED", "IN_REGION", "CROSS_REGION"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="backups_crypto_key_version" type="string">
  The crypto key version used to encrypt the backup vault. Format: 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/keyRings/&#123;&#123;key_ring&#125;&#125;/cryptoKeys/&#123;&#123;crypto_key&#125;&#125;/cryptoKeyVersions/&#123;&#123;crypto_key_version&#125;&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Create time of the backup vault. A timestamp in RFC3339 UTC "Zulu" format. Examples: "2023-06-22T09:13:01.617Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="destination_backup_vault" type="string">
  Name of the Backup vault created in backup region. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="encryption_state" type="string">
  Encryption state of customer-managed encryption keys (CMEK) backups. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="source_backup_vault" type="string">
  Name of the Backup vault created in source region. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="source_region" type="string">
  Region in which the backup vault is created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the Backup Vault. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
