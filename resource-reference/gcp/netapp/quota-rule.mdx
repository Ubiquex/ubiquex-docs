---
title: "google_netapp_quota_rule"
description: "`google_netapp_quota_rule` sets a per-user or per-group storage quota on a Google Cloud NetApp Volumes volume..."
---

`google_netapp_quota_rule` sets a per-user or per-group storage quota on a Google Cloud NetApp Volumes volume, limiting how much space or how many files that user or group may consume.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_netapp --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_netapp/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	netapp "github.com/ubiquex/ubx-sdk-google/sdk/go/google_netapp/netapp"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own quota rule"})

    		ubx.Resource(netapp.NetappQuotaRule, "ci-runner", netapp.NetappQuotaRuleConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_netapp --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetappQuotaRule } from "./local-sdk/google_netapp/sdk/typescript/google_netapp/netapp/netapp_quota_rule.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own quota rule" });

      resource(NetappQuotaRule, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_netapp --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_netapp/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_netapp.netapp import NetappQuotaRule, NetappQuotaRuleConfig

    def describe():
        ubx.intent("payments own quota rule")

        ubx.resource(NetappQuotaRule, "ci-runner", NetappQuotaRuleConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_netapp_quota_rule called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Create time of the quota rule Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the quota rule Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="disk_limit_mib" type="number">
  Required. The maximum allowed disk space in MiB. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels of the quota rule Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the quota rule. Format: `projects/&#123;project_number&#125;/locations/&#123;location_id&#125;/volumes/volumes/&#123;volume_id&#125;/quotaRules/&#123;quota_rule_id&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the quota rule Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state_details" type="string">
  Output only. State details of the quota rule Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="target" type="string">
  Optional. The quota rule applies to the specified user or group, identified by a Unix UID/GID, Windows SID, or null for default. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  Required. The type of quota rule. Optional; if omitted, computed by Google Cloud.
</ResponseField>
