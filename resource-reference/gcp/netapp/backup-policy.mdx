---
title: "google_netapp_backup_policy"
description: "Real, generated bindings for `google_netapp_backup_policy`."
---

`google_netapp_backup_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/netapp"
    )

    cfg := netapp.BackupPolicyConfig{
        DailyBackupLimit: 1,
        Location: "example",
        MonthlyBackupLimit: 1,
        Name: "example-name",
        WeeklyBackupLimit: 1,
        Description: "example",
        Enabled: true,
    }
    ubx.Resource(netapp.BackupPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BackupPolicy } from
      "jsr:@ubx/sdk-google/google/netapp/backup_policy";

    resource(BackupPolicy, "example", {
      dailyBackupLimit: 1,
      location: "example",
      monthlyBackupLimit: 1,
      name: "example-name",
      weeklyBackupLimit: 1,
      description: "example",
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.netapp import BackupPolicy, BackupPolicyConfig

    cfg = BackupPolicyConfig(
        daily_backup_limit=1,
        location="example",
        monthly_backup_limit=1,
        name="example-name",
        weekly_backup_limit=1,
        description="example",
        enabled=True,
    )
    ubx.resource(BackupPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_netapp_backup_policy with daily_backup_limit,
location, monthly_backup_limit, name, weekly_backup_limit,
description, enabled.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="daily_backup_limit" type="number" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="monthly_backup_limit" type="number" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="weekly_backup_limit" type="number" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="assigned_volume_count" type="number">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
