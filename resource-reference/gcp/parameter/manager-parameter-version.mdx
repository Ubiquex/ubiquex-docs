---
title: "google_parameter_manager_parameter_version"
description: "Real, generated bindings for `google_parameter_manager_parameter_version`."
---

`google_parameter_manager_parameter_version` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	parameter "github.com/ubiquex/ubx-sdk-google/sdk/go/google/parameter"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own manager parameter version"})

    		ubx.Resource(parameter.ManagerParameterVersion, "ci-runner", parameter.ManagerParameterVersionConfig{
    			Parameter:          "example",
    			ParameterData:      "example",
    			ParameterVersionId: "example",
    			Disabled:           true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerParameterVersion } from "jsr:@ubx/sdk-google/google/parameter/manager_parameter_version";

    export default stack("payments", () => {
      intent({ summary: "payments own manager parameter version" });

      resource(ManagerParameterVersion, "ci-runner", {
        parameter: "example",
        parameterData: "example",
        parameterVersionId: "example",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.parameter import ManagerParameterVersion, ManagerParameterVersionConfig

    def describe():
        ubx.intent("payments own manager parameter version")

        ubx.resource(ManagerParameterVersion, "ci-runner", ManagerParameterVersionConfig(
            parameter="example",
            parameter_data="example",
            parameter_version_id="example",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_parameter_manager_parameter_version, with parameter
    example, parameter data example, parameter version id example,
    disabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parameter" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parameter_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parameter_version_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_key_version" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by GCP after `ship`.
</ResponseField>
