---
title: "google_documentai_schema"
description: "`google_documentai_schema` defines the entity type(s) and field(s) a custom Document AI processor is trained to extract from documents."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	documentai "github.com/ubiquex/ubx-sdk-google/sdk/go/google/documentai"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own schema"})

    		ubx.Resource(documentai.Schema, "example", documentai.SchemaConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Schema } from "@ubx/sdk-google/google/documentai/schema";

    export default stack("example", () => {
      intent({ summary: "example own schema" });

      resource(Schema, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.documentai import Schema, SchemaConfig

    def describe():
        ubx.intent("example own schema")

        ubx.resource(Schema, "example", SchemaConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_documentai_schema called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The time when the Schema was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The user-defined name of the Schema. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. The &#123;&#123;gcp_name_short&#125;&#125; labels for the Schema. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the Schema. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/schemas/&#123;schema&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the Schema was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
