---
title: "google_documentai_processor"
description: "`google_documentai_processor` is a Document AI processor instance -- a specific document-parsing model (e.g. invoice or form parser)..."
---

`google_documentai_processor` is a Document AI processor instance -- a specific document-parsing model (e.g. invoice or form parser) -- that document(s) are sent to for extraction.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_documentai --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_documentai/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	documentai "github.com/ubiquex/ubx-sdk-google/sdk/go/google_documentai/documentai"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own processor"})

    		ubx.Resource(documentai.DocumentaiProcessor, "ci-runner", documentai.DocumentaiProcessorConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_documentai --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DocumentaiProcessor } from "./local-sdk/google_documentai/sdk/typescript/google_documentai/documentai/documentai_processor.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own processor" });

      resource(DocumentaiProcessor, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_documentai --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_documentai/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_documentai.documentai import DocumentaiProcessor, DocumentaiProcessorConfig

    def describe():
        ubx.intent("payments own processor")

        ubx.resource(DocumentaiProcessor, "ci-runner", DocumentaiProcessorConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_documentai_processor called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="active_schema_version" type="string">
  Optional. SchemaVersion used by the Processor. It is the same as Processor's DatasetSchema.schema_version Format is `projects/&#123;project&#125;/locations/&#123;location&#125;/schemas/&#123;schema&#125;/schemaVersions/&#123;schema_version&#125;. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time the processor was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_processor_version" type="string">
  The default processor version. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name of the processor. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  The [KMS key](https://cloud.google.com/security-key-management) used for encryption and decryption in CMEK scenarios. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Immutable. The resource name of the processor. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/processors/&#123;processor&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="process_endpoint" type="string">
  Output only. Immutable. The http endpoint that can be called to invoke processing. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="processor_version_aliases" type="list(object)">
  Output only. The processor version aliases. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="alias" type="string">
      An alternate name this schema entity is also known by. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="processor_version" type="string">
      A reference to the specific processor version this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the processor. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="type" type="string">
  The processor type, such as: `OCR_PROCESSOR`, `INVOICE_PROCESSOR`. To get a list of processor types, see FetchProcessorTypes. Optional; if omitted, computed by Google Cloud.
</ResponseField>
