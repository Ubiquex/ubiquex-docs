---
title: "google_documentai_v1beta3_schema"
description: "`google_documentai_v1beta3_schema` defines the entity type(s) and field(s) a custom Document AI processor is trained to extract from documents."
---

<Warning>
  This is Google Cloud's own **v1beta3** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_documentai_schema`.
</Warning>

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	documentai "github.com/ubiquex/ubx-sdk-google/sdk/go/google/documentai"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta3 schema"})

    		ubx.Resource(documentai.V1beta3Schema, "example", documentai.V1beta3SchemaConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1beta3Schema } from "@ubx/sdk-google/google/documentai/v1beta3_schema";

    export default stack("example", () => {
      intent({ summary: "example own v1beta3 schema" });

      resource(V1beta3Schema, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.documentai.v1beta3_schema import V1beta3Schema, V1beta3SchemaConfig

    def describe():
        ubx.intent("example own v1beta3 schema")

        ubx.resource(V1beta3Schema, "example", V1beta3SchemaConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_documentai_v1beta3_schema called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The time when the Schema was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The user-defined name of the Schema. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. The &#123;&#123;gcp_name_short&#125;&#125; labels for the Schema. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the Schema. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/schemas/&#123;schema&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the Schema was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
