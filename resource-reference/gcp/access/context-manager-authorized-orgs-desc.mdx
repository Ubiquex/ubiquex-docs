---
title: "google_access_context_manager_authorized_orgs_desc"
description: "Real, generated bindings for `google_access_context_manager_authorized_orgs_desc`."
---

`google_access_context_manager_authorized_orgs_desc` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	access "github.com/ubiquex/ubx-sdk-google/sdk/go/google/access"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own context manager authorized orgs desc"})

    		ubx.Resource(access.ContextManagerAuthorizedOrgsDesc, "ci-runner", access.ContextManagerAuthorizedOrgsDescConfig{
    			Name:                   "ci-runner",
    			Parent:                 "example",
    			AssetType:              "example",
    			AuthorizationDirection: "example",
    			AuthorizationType:      "example",
    			Orgs:                   []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContextManagerAuthorizedOrgsDesc } from "jsr:@ubx/sdk-google/google/access/context_manager_authorized_orgs_desc";

    export default stack("payments", () => {
      intent({ summary: "payments own context manager authorized orgs desc" });

      resource(ContextManagerAuthorizedOrgsDesc, "ci-runner", {
        name: "ci-runner",
        parent: "example",
        assetType: "example",
        authorizationDirection: "example",
        authorizationType: "example",
        orgs: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.access import ContextManagerAuthorizedOrgsDesc, ContextManagerAuthorizedOrgsDescConfig

    def describe():
        ubx.intent("payments own context manager authorized orgs desc")

        ubx.resource(ContextManagerAuthorizedOrgsDesc, "ci-runner", ContextManagerAuthorizedOrgsDescConfig(
            name="ci-runner",
            parent="example",
            asset_type="example",
            authorization_direction="example",
            authorization_type="example",
            orgs=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_access_context_manager_authorized_orgs_desc called
    "ci-runner", with parent example, asset type example,
    authorization direction example, authorization type example, orgs
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="asset_type" type="string">
  The type of entities that need to use the authorization relationship during evaluation, such as a device. Valid values are "ASSET_TYPE_DEVICE" and "ASSET_TYPE_CREDENTIAL_STRENGTH". Possible values: ["ASSET_TYPE_DEVICE", "ASSET_TYPE_CREDENTIAL_STRENGTH"] Optional.
</ResponseField>

<ResponseField name="authorization_direction" type="string">
  The direction of the authorization relationship between this organization and the organizations listed in the "orgs" field. The valid values for this field include the following: AUTHORIZATION_DIRECTION_FROM: Allows this organization to evaluate traffic in the organizations listed in the 'orgs' field. AUTHORIZATION_DIRECTION_TO: Allows the organizations listed in the 'orgs' field to evaluate the traffic in this organization. For the authorization relationship to take effect, all of the organizations must authorize and specify the appropriate relationship direction. For example, if organization A authorized organization B and C to evaluate its traffic, by specifying "AUTHORIZATION_DIRECTION_TO" as the authorization direction, organizations B and C must specify "AUTHORIZATION_DIRECTION_FROM" as the authorization direction in their "AuthorizedOrgsDesc" resource. Possible values: ["AUTHORIZATION_DIRECTION_TO", "AUTHORIZATION_DIRECTION_FROM"] Optional.
</ResponseField>

<ResponseField name="authorization_type" type="string">
  A granular control type for authorization levels. Valid value is "AUTHORIZATION_TYPE_TRUST". Possible values: ["AUTHORIZATION_TYPE_TRUST"] Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Resource name for the 'AuthorizedOrgsDesc'. Format: 'accessPolicies/&#123;access_policy&#125;/authorizedOrgsDescs/&#123;authorized_orgs_desc&#125;'. The 'authorized_orgs_desc' component must begin with a letter, followed by alphanumeric characters or '_'. After you create an 'AuthorizedOrgsDesc', you cannot change its 'name'. Required.
</ResponseField>

<ResponseField name="orgs" type="list(string)">
  The list of organization ids in this AuthorizedOrgsDesc. Format: 'organizations/&lt;org_number&gt;' Example: 'organizations/123456' Optional.
</ResponseField>

<ResponseField name="parent" type="string" required>
  Required. Resource name for the access policy which owns this 'AuthorizedOrgsDesc'. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Time the AuthorizedOrgsDesc was created in UTC. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Time the AuthorizedOrgsDesc was updated in UTC. Computed by GCP after `ship`.
</ResponseField>
