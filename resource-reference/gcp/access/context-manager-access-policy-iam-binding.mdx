---
title: "google_access_context_manager_access_policy_iam_binding"
description: "Real, generated bindings for `google_access_context_manager_access_policy_iam_binding`."
---

`google_access_context_manager_access_policy_iam_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	access "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/access"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own context manager access policy iam binding"})

    		ubx.Resource(access.ContextManagerAccessPolicyIamBinding, "ci-runner", access.ContextManagerAccessPolicyIamBindingConfig{
    			Members: []string{"example"},
    			Name:    "ci-runner",
    			Role:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContextManagerAccessPolicyIamBinding } from "jsr:@ubx/sdk-google/google/access/context_manager_access_policy_iam_binding";

    export default stack("payments", () => {
      intent({ summary: "payments own context manager access policy iam binding" });

      resource(ContextManagerAccessPolicyIamBinding, "ci-runner", {
        members: ["example"],
        name: "ci-runner",
        role: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.access import ContextManagerAccessPolicyIamBinding, ContextManagerAccessPolicyIamBindingConfig

    def describe():
        ubx.intent("payments own context manager access policy iam binding")

        ubx.resource(ContextManagerAccessPolicyIamBinding, "ci-runner", ContextManagerAccessPolicyIamBindingConfig(
            members=["example"],
            name="ci-runner",
            role="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_access_context_manager_access_policy_iam_binding
    called "ci-runner", with members a set value, role example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="members" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
