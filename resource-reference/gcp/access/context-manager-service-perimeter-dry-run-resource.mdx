---
title: "google_access_context_manager_service_perimeter_dry_run_resource"
description: "Real, generated bindings for `google_access_context_manager_service_perimeter_dry_run_resource`."
---

`google_access_context_manager_service_perimeter_dry_run_resource` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/access"
    )

    cfg := access.ContextManagerServicePerimeterDryRunResourceConfig{
        PerimeterName: "example-perimeter-name",
        Resource: "example",
    }
    ubx.Resource(access.ContextManagerServicePerimeterDryRunResource, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ContextManagerServicePerimeterDryRunResource } from
      "jsr:@ubx/sdk-google/google/access/context_manager_service_perimeter_dry_run_resource";

    resource(ContextManagerServicePerimeterDryRunResource, "example", {
      perimeterName: "example-perimeter-name",
      resource: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.access import (
        ContextManagerServicePerimeterDryRunResource,
        ContextManagerServicePerimeterDryRunResourceConfig,
    )

    cfg = ContextManagerServicePerimeterDryRunResourceConfig(
        perimeter_name="example-perimeter-name",
        resource="example",
    )
    ubx.resource(ContextManagerServicePerimeterDryRunResource, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision
google_access_context_manager_service_perimeter_dry_run_resource
with perimeter_name, resource.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="perimeter_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="access_policy_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
