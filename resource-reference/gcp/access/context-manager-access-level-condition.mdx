---
title: "google_access_context_manager_access_level_condition"
description: "Real, generated bindings for `google_access_context_manager_access_level_condition`."
---

`google_access_context_manager_access_level_condition` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	access "github.com/ubiquex/ubx-sdk-google/sdk/go/google/access"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own context manager access level condition"})

    		ubx.Resource(access.ContextManagerAccessLevelCondition, "ci-runner", access.ContextManagerAccessLevelConditionConfig{
    			AccessLevel:          "example",
    			IpSubnetworks:        []string{"example"},
    			Members:              []string{"example"},
    			Negate:               true,
    			Regions:              []string{"example"},
    			RequiredAccessLevels: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContextManagerAccessLevelCondition } from "jsr:@ubx/sdk-google/google/access/context_manager_access_level_condition";

    export default stack("payments", () => {
      intent({ summary: "payments own context manager access level condition" });

      resource(ContextManagerAccessLevelCondition, "ci-runner", {
        accessLevel: "example",
        ipSubnetworks: ["example"],
        members: ["example"],
        negate: true,
        regions: ["example"],
        requiredAccessLevels: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.access import ContextManagerAccessLevelCondition, ContextManagerAccessLevelConditionConfig

    def describe():
        ubx.intent("payments own context manager access level condition")

        ubx.resource(ContextManagerAccessLevelCondition, "ci-runner", ContextManagerAccessLevelConditionConfig(
            access_level="example",
            ip_subnetworks=["example"],
            members=["example"],
            negate=True,
            regions=["example"],
            required_access_levels=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_access_context_manager_access_level_condition, with
    access level example, ip subnetworks a set value, members a set
    value, negate true, regions a set value, required access levels a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access_level" type="string" required>
  The name of the Access Level to add this condition to. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="device_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allowed_device_management_levels" type="list(string)">
    </ResponseField>
    <ResponseField name="allowed_encryption_statuses" type="list(string)">
    </ResponseField>
    <ResponseField name="os_constraints" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="minimum_version" type="string">
        </ResponseField>
        <ResponseField name="os_type" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="require_admin_approval" type="bool">
    </ResponseField>
    <ResponseField name="require_corp_owned" type="bool">
    </ResponseField>
    <ResponseField name="require_screen_lock" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_subnetworks" type="list(string)">
  A list of CIDR block IP subnetwork specification. May be IPv4 or IPv6. Note that for a CIDR IP address block, the specified IP address portion must be properly truncated (i.e. all the host bits must be zero) or the input is considered malformed. For example, "192.0.2.0/24" is accepted but "192.0.2.1/24" is not. Similarly, for IPv6, "2001:db8::/32" is accepted whereas "2001:db8::1/32" is not. The originating IP of a request must be in one of the listed subnets in order for this Condition to be true. If empty, all IP addresses are allowed. Optional.
</ResponseField>

<ResponseField name="members" type="list(string)">
  An allowed list of members (users, service accounts). Using groups is not supported yet. The signed-in user originating the request must be a part of one of the provided members. If not specified, a request may come from any user (logged in/not logged in, not present in any groups, etc.). Formats: 'user:&#123;emailid&#125;', 'serviceAccount:&#123;emailid&#125;' Optional.
</ResponseField>

<ResponseField name="negate" type="bool">
  Whether to negate the Condition. If true, the Condition becomes a NAND over its non-empty fields, each field must be false for the Condition overall to be satisfied. Defaults to false. Optional.
</ResponseField>

<ResponseField name="regions" type="list(string)">
  The request must originate from one of the provided countries/regions. Format: A valid ISO 3166-1 alpha-2 code. Optional.
</ResponseField>

<ResponseField name="required_access_levels" type="list(string)">
  A list of other access levels defined in the same Policy, referenced by resource name. Referencing an AccessLevel which does not exist is an error. All access levels listed must be granted for the Condition to be true. Format: accessPolicies/&#123;policy_id&#125;/accessLevels/&#123;short_name&#125; Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_network_sources" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="vpc_subnetwork" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="network" type="string" required>
        </ResponseField>
        <ResponseField name="vpc_ip_subnetworks" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="access_policy_id" type="string">
  The name of the Access Policy this resource belongs to. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="device_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allowed_device_management_levels" type="list(string)">
    </ResponseField>
    <ResponseField name="allowed_encryption_statuses" type="list(string)">
    </ResponseField>
    <ResponseField name="os_constraints" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="minimum_version" type="string">
        </ResponseField>
        <ResponseField name="os_type" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="require_admin_approval" type="bool">
    </ResponseField>
    <ResponseField name="require_corp_owned" type="bool">
    </ResponseField>
    <ResponseField name="require_screen_lock" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_network_sources" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="vpc_subnetwork" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="network" type="string" required>
        </ResponseField>
        <ResponseField name="vpc_ip_subnetworks" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
