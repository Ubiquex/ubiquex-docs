---
title: "google_access_context_manager_access_policy"
description: "Real, generated bindings for `google_access_context_manager_access_policy`."
---

`google_access_context_manager_access_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	access "github.com/ubiquex/ubx-sdk-google/sdk/go/google/access"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own context manager access policy"})

    		ubx.Resource(access.ContextManagerAccessPolicy, "ci-runner", access.ContextManagerAccessPolicyConfig{
    			Parent: "example",
    			Title:  "example",
    			Scopes: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContextManagerAccessPolicy } from "jsr:@ubx/sdk-google/google/access/context_manager_access_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own context manager access policy" });

      resource(ContextManagerAccessPolicy, "ci-runner", {
        parent: "example",
        title: "example",
        scopes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.access import ContextManagerAccessPolicy, ContextManagerAccessPolicyConfig

    def describe():
        ubx.intent("payments own context manager access policy")

        ubx.resource(ContextManagerAccessPolicy, "ci-runner", ContextManagerAccessPolicyConfig(
            parent="example",
            title="example",
            scopes=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_access_context_manager_access_policy, with parent
    example, title example, scopes a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent" type="string" required>
  The parent of this AccessPolicy in the Cloud Resource Hierarchy. Format: 'organizations/&#123;&#123;organization_id&#125;&#125;' Required.
</ResponseField>

<ResponseField name="scopes" type="list(string)">
  Folder or project on which this policy is applicable. Format: 'folders/&#123;&#123;folder_id&#125;&#125;' or 'projects/&#123;&#123;project_number&#125;&#125;' Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="title" type="string" required>
  Human readable title. Does not affect behavior. Required.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Time the AccessPolicy was created in UTC. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Resource name of the AccessPolicy. Format: '&#123;&#123;policy_id&#125;&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Time the AccessPolicy was updated in UTC. Computed by GCP after `ship`.
</ResponseField>
