---
title: "google_healthcare_dataset_iam_policy"
description: "Real, generated bindings for `google_healthcare_dataset_iam_policy`."
---

`google_healthcare_dataset_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/healthcare"
    )

    cfg := healthcare.DatasetIamPolicyConfig{
        DatasetId: "example",
        PolicyData: "example",
    }
    ubx.Resource(healthcare.DatasetIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DatasetIamPolicy } from
      "jsr:@ubx/sdk-google/google/healthcare/dataset_iam_policy";

    resource(DatasetIamPolicy, "example", {
      datasetId: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.healthcare import (
        DatasetIamPolicy,
        DatasetIamPolicyConfig,
    )

    cfg = DatasetIamPolicyConfig(
        dataset_id="example",
        policy_data="example",
    )
    ubx.resource(DatasetIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_healthcare_dataset_iam_policy with dataset_id,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dataset_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
