---
title: "google_healthcare_dataset_iam_policy"
description: "Real, generated bindings for `google_healthcare_dataset_iam_policy`."
---

`google_healthcare_dataset_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	healthcare "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/healthcare"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dataset iam policy"})

    		ubx.Resource(healthcare.DatasetIamPolicy, "ci-runner", healthcare.DatasetIamPolicyConfig{
    			DatasetId:  "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatasetIamPolicy } from "jsr:@ubx/sdk-google/google/healthcare/dataset_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own dataset iam policy" });

      resource(DatasetIamPolicy, "ci-runner", {
        datasetId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.healthcare import DatasetIamPolicy, DatasetIamPolicyConfig

    def describe():
        ubx.intent("payments own dataset iam policy")

        ubx.resource(DatasetIamPolicy, "ci-runner", DatasetIamPolicyConfig(
            dataset_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_healthcare_dataset_iam_policy, with dataset id
    example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dataset_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
