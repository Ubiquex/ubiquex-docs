---
title: "google_healthcare_consent_store_iam_policy"
description: "Real, generated bindings for `google_healthcare_consent_store_iam_policy`."
---

`google_healthcare_consent_store_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	healthcare "github.com/ubiquex/ubx-sdk-google/sdk/go/google/healthcare"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own consent store iam policy"})

    		ubx.Resource(healthcare.ConsentStoreIamPolicy, "ci-runner", healthcare.ConsentStoreIamPolicyConfig{
    			ConsentStoreId: "example",
    			Dataset:        "example",
    			PolicyData:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConsentStoreIamPolicy } from "jsr:@ubx/sdk-google/google/healthcare/consent_store_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own consent store iam policy" });

      resource(ConsentStoreIamPolicy, "ci-runner", {
        consentStoreId: "example",
        dataset: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.healthcare import ConsentStoreIamPolicy, ConsentStoreIamPolicyConfig

    def describe():
        ubx.intent("payments own consent store iam policy")

        ubx.resource(ConsentStoreIamPolicy, "ci-runner", ConsentStoreIamPolicyConfig(
            consent_store_id="example",
            dataset="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_healthcare_consent_store_iam_policy, with consent
    store id example, dataset example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="consent_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="dataset" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
