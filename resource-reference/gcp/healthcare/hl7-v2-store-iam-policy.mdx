---
title: "google_healthcare_hl7_v2_store_iam_policy"
description: "Real, generated bindings for `google_healthcare_hl7_v2_store_iam_policy`."
---

`google_healthcare_hl7_v2_store_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	healthcare "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/healthcare"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hl7 v2 store iam policy"})

    		ubx.Resource(healthcare.Hl7V2StoreIamPolicy, "ci-runner", healthcare.Hl7V2StoreIamPolicyConfig{
    			Hl7V2StoreId: "example",
    			PolicyData:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Hl7V2StoreIamPolicy } from "jsr:@ubx/sdk-google/google/healthcare/hl7_v2_store_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own hl7 v2 store iam policy" });

      resource(Hl7V2StoreIamPolicy, "ci-runner", {
        hl7V2StoreId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.healthcare import Hl7V2StoreIamPolicy, Hl7V2StoreIamPolicyConfig

    def describe():
        ubx.intent("payments own hl7 v2 store iam policy")

        ubx.resource(Hl7V2StoreIamPolicy, "ci-runner", Hl7V2StoreIamPolicyConfig(
            hl7_v2_store_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_healthcare_hl7_v2_store_iam_policy, with hl7 v2
    store id example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="hl7_v2_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
