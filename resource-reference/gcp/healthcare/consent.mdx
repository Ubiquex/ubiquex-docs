---
title: "google_healthcare_consent"
description: "`google_healthcare_consent` records a single patient's own consent decision (granted or revoked) for a defined use of their health data..."
---

`google_healthcare_consent` records a single patient's own consent decision (granted or revoked) for a defined use of their health data, managed within a Cloud Healthcare API consent store.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_healthcare --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_healthcare/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	healthcare "github.com/ubiquex/ubx-sdk-google/sdk/go/google_healthcare/healthcare"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own consent"})

    		ubx.Resource(healthcare.HealthcareConsent, "ci-runner", healthcare.HealthcareConsentConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_healthcare --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HealthcareConsent } from "./local-sdk/google_healthcare/sdk/typescript/google_healthcare/healthcare/healthcare_consent.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own consent" });

      resource(HealthcareConsent, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_healthcare --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_healthcare/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_healthcare.healthcare import HealthcareConsent, HealthcareConsentConfig

    def describe():
        ubx.intent("payments own consent")

        ubx.resource(HealthcareConsent, "ci-runner", HealthcareConsentConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_healthcare_consent called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="consent_artifact" type="string">
  Required. The resource name of the Consent artifact that contains proof of the end user's consent, of the form `projects/&#123;project_id&#125;/locations/&#123;location_id&#125;/datasets/&#123;dataset_id&#125;/consentStores/&#123;consent_store_id&#125;/consentArtifacts/&#123;consent_artifact_id&#125;`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Timestamp in UTC of when this Consent is considered expired. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="metadata" type="map(string)">
  Optional. User-supplied key-value pairs used to organize Consent resources. Metadata keys must: - be between 1 and 63 characters long - have a UTF-8 encoding of maximum 128 bytes - begin with a letter - consist of up to 63 characters including lowercase letters, numeric characters, underscores, and dashes Metadata values must be: - be between 1 and 63 characters long - have a UTF-8 encoding of maximum 128 bytes - consist of up to 63 characters including lowercase letters, numeric characters, underscores, and dashes No more than 64 metadata entries can be associated with a given consent. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Resource name of the Consent, of the form `projects/&#123;project_id&#125;/locations/&#123;location_id&#125;/datasets/&#123;dataset_id&#125;/consentStores/&#123;consent_store_id&#125;/consents/&#123;consent_id&#125;`. Cannot be changed after creation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="policies" type="list(object)">
  Optional. Represents a user's consent in terms of the resources that can be accessed and under what conditions. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="authorization_rule" type="object">
      The condition determining whether a specific consent authorization applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          A human-readable explanation of this schema's own meaning and intended use. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="expression" type="string">
          A boolean expression evaluated to determine whether this rule applies. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="location" type="string">
          The Google Cloud region this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="title" type="string">
          The resource's own formal name -- a more official variant of `display_name`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_attributes" type="list(object)">
      Custom attribute(s) attached to this consent resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="attribute_definition_id" type="string">
          A reference to the attribute definition this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="values" type="list(string)">
          The value(s) making up this field. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="revision_create_time" type="string">
  Output only. The timestamp that the revision was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="revision_id" type="string">
  Output only. The revision ID of the Consent. The format is an 8-character hexadecimal string. Refer to a specific revision of a Consent by appending `@&#123;revision_id&#125;` to the Consent's resource name. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Required. Indicates the current state of this Consent. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ttl" type="string">
  Input only. The time to live for this Consent from when it is created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="user_id" type="string">
  Required. User's UUID provided by the client. Optional; if omitted, computed by Google Cloud.
</ResponseField>
