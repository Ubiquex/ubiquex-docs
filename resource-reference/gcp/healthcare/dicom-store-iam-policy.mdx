---
title: "google_healthcare_dicom_store_iam_policy"
description: "Real, generated bindings for `google_healthcare_dicom_store_iam_policy`."
---

`google_healthcare_dicom_store_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/healthcare"
    )

    cfg := healthcare.DicomStoreIamPolicyConfig{
        DicomStoreId: "example",
        PolicyData: "example",
    }
    ubx.Resource(healthcare.DicomStoreIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DicomStoreIamPolicy } from
      "jsr:@ubx/sdk-google/google/healthcare/dicom_store_iam_policy";

    resource(DicomStoreIamPolicy, "example", {
      dicomStoreId: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.healthcare import (
        DicomStoreIamPolicy,
        DicomStoreIamPolicyConfig,
    )

    cfg = DicomStoreIamPolicyConfig(
        dicom_store_id="example",
        policy_data="example",
    )
    ubx.resource(DicomStoreIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_healthcare_dicom_store_iam_policy with
dicom_store_id, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dicom_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
