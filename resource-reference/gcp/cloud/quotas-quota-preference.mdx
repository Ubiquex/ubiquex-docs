---
title: "google_cloud_quotas_quota_preference"
description: "Real, generated bindings for `google_cloud_quotas_quota_preference`."
---

`google_cloud_quotas_quota_preference` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloud "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloud"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own quotas quota preference"})

    		ubx.Resource(cloud.QuotasQuotaPreference, "ci-runner", cloud.QuotasQuotaPreferenceConfig{
    			Name:               "ci-runner",
    			ContactEmail:       "example",
    			IgnoreSafetyChecks: "example",
    			Justification:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { QuotasQuotaPreference } from "jsr:@ubx/sdk-google/google/cloud/quotas_quota_preference";

    export default stack("payments", () => {
      intent({ summary: "payments own quotas quota preference" });

      resource(QuotasQuotaPreference, "ci-runner", {
        name: "ci-runner",
        contactEmail: "example",
        ignoreSafetyChecks: "example",
        justification: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.cloud import QuotasQuotaPreference, QuotasQuotaPreferenceConfig

    def describe():
        ubx.intent("payments own quotas quota preference")

        ubx.resource(QuotasQuotaPreference, "ci-runner", QuotasQuotaPreferenceConfig(
            name="ci-runner",
            contact_email="example",
            ignore_safety_checks="example",
            justification="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloud_quotas_quota_preference called "ci-runner",
    with contact email example, ignore safety checks example,
    justification example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="contact_email" type="string">
  An email address that can be used for quota related communication between the Google Cloud and the user in case the Google Cloud needs further information to make a decision on whether the user preferred quota can be granted. The Google account for the email address must have quota update permission for the project, folder or organization this quota preference is for. Optional.
</ResponseField>

<ResponseField name="dimensions" type="map(string)">
  The dimensions that this quota preference applies to. The key of the map entry is the name of a dimension, such as "region", "zone", "network_id", and the value of the map entry is the dimension value. If a dimension is missing from the map of dimensions, the quota preference applies to all the dimension values except for those that have other quota preferences configured for the specific value. NOTE: QuotaPreferences can only be applied across all values of "user" and "resource" dimension. Do not set values for "user" or "resource" in the dimension map. Example: '&#123;"provider": "Foo Inc"&#125;' where "provider" is a service specific dimension. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ignore_safety_checks" type="string">
  The list of quota safety checks to be ignored. Default value: "QUOTA_SAFETY_CHECK_UNSPECIFIED" Possible values: ["QUOTA_SAFETY_CHECK_UNSPECIFIED", "QUOTA_DECREASE_BELOW_USAGE", "QUOTA_DECREASE_PERCENTAGE_TOO_HIGH"] Optional.
</ResponseField>

<ResponseField name="justification" type="string">
  The reason / justification for this quota preference. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the quota preference. Required except in the CREATE requests. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent" type="string">
  The parent of the quota preference. Allowed parents are "projects/[project-id / number]" or "folders/[folder-id / number]" or "organizations/[org-id / number]". Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="quota_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
    </ResponseField>
    <ResponseField name="granted_value" type="string">
    </ResponseField>
    <ResponseField name="preferred_value" type="string" required>
    </ResponseField>
    <ResponseField name="request_origin" type="string">
    </ResponseField>
    <ResponseField name="state_detail" type="string">
    </ResponseField>
    <ResponseField name="trace_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="quota_id" type="string">
  The id of the quota to which the quota preference is applied. A quota id is unique in the service. Example: 'CPUS-per-project-region'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service" type="string">
  The name of the service to which the quota preference is applied. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Create time stamp. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: '2014-10-02T15:01:23Z' and '2014-10-02T15:01:23.045123456Z'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="dimensions" type="map(string)">
  The dimensions that this quota preference applies to. The key of the map entry is the name of a dimension, such as "region", "zone", "network_id", and the value of the map entry is the dimension value. If a dimension is missing from the map of dimensions, the quota preference applies to all the dimension values except for those that have other quota preferences configured for the specific value. NOTE: QuotaPreferences can only be applied across all values of "user" and "resource" dimension. Do not set values for "user" or "resource" in the dimension map. Example: '&#123;"provider": "Foo Inc"&#125;' where "provider" is a service specific dimension. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  The current etag of the quota preference. If an etag is provided on update and does not match the current server's etag of the quota preference, the request will be blocked and an ABORTED error will be returned. See https://google.aip.dev/134#etags for more details on etags. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the quota preference. Required except in the CREATE requests. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent" type="string">
  The parent of the quota preference. Allowed parents are "projects/[project-id / number]" or "folders/[folder-id / number]" or "organizations/[org-id / number]". Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="quota_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
    </ResponseField>
    <ResponseField name="granted_value" type="string">
    </ResponseField>
    <ResponseField name="preferred_value" type="string" required>
    </ResponseField>
    <ResponseField name="request_origin" type="string">
    </ResponseField>
    <ResponseField name="state_detail" type="string">
    </ResponseField>
    <ResponseField name="trace_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="quota_id" type="string">
  The id of the quota to which the quota preference is applied. A quota id is unique in the service. Example: 'CPUS-per-project-region'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Is the quota preference pending Google Cloud approval and fulfillment. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="service" type="string">
  The name of the service to which the quota preference is applied. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Update time stamp. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: '2014-10-02T15:01:23Z' and '2014-10-02T15:01:23.045123456Z'. Computed by GCP after `ship`.
</ResponseField>
