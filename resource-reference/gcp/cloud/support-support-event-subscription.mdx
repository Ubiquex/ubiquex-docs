---
title: "google_cloud_support_support_event_subscription"
description: "Real, generated bindings for `google_cloud_support_support_event_subscription`."
---

`google_cloud_support_support_event_subscription` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloud "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloud"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own support support event subscription"})

    		ubx.Resource(cloud.SupportSupportEventSubscription, "ci-runner", cloud.SupportSupportEventSubscriptionConfig{
    			Organization: "example",
    			PubSubTopic:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SupportSupportEventSubscription } from "jsr:@ubx/sdk-google/google/cloud/support_support_event_subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own support support event subscription" });

      resource(SupportSupportEventSubscription, "ci-runner", {
        organization: "example",
        pubSubTopic: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.cloud import SupportSupportEventSubscription, SupportSupportEventSubscriptionConfig

    def describe():
        ubx.intent("payments own support support event subscription")

        ubx.resource(SupportSupportEventSubscription, "ci-runner", SupportSupportEventSubscriptionConfig(
            organization="example",
            pub_sub_topic="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloud_support_support_event_subscription, with
    organization example, pub sub topic example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="organization" type="string" required>
  The organization ID for the support event subscription. Required.
</ResponseField>

<ResponseField name="pub_sub_topic" type="string" required>
  The name of the Pub/Sub topic to publish notifications to. Format: projects/&#123;project&#125;/topics/&#123;topic&#125; Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time at which the subscription was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="delete_time" type="string">
  The time at which the subscription was deleted. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  Reason why subscription is failing. State of subscription must be FAILING in order for this to have a value. Possible values: PERMISSION_DENIED TOPIC_NOT_FOUND OTHER Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the support event subscription. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="purge_time" type="string">
  The time at which the subscription will be purged. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the subscription. Possible values: WORKING FAILING DELETED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The time at which the subscription was last updated. Computed by GCP after `ship`.
</ResponseField>
