---
title: "google_cloud_quotas_quota_adjuster_settings"
description: "Real, generated bindings for `google_cloud_quotas_quota_adjuster_settings`."
---

`google_cloud_quotas_quota_adjuster_settings` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloud "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloud"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own quotas quota adjuster settings"})

    		ubx.Resource(cloud.QuotasQuotaAdjusterSettings, "ci-runner", cloud.QuotasQuotaAdjusterSettingsConfig{
    			Enablement: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { QuotasQuotaAdjusterSettings } from "jsr:@ubx/sdk-google/google/cloud/quotas_quota_adjuster_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own quotas quota adjuster settings" });

      resource(QuotasQuotaAdjusterSettings, "ci-runner", {
        enablement: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.cloud import QuotasQuotaAdjusterSettings, QuotasQuotaAdjusterSettingsConfig

    def describe():
        ubx.intent("payments own quotas quota adjuster settings")

        ubx.resource(QuotasQuotaAdjusterSettings, "ci-runner", QuotasQuotaAdjusterSettingsConfig(
            enablement="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloud_quotas_quota_adjuster_settings, with
    enablement example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="enablement" type="string" required>
  Required. The configured value of the enablement at the given resource. Possible values: ["ENABLED", "DISABLED"] Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent" type="string">
  The parent of the quota preference. Allowed parent format is "projects/[project-id / number]". Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="effective_container" type="string">
  The resource container that determines if the quota adjuster is set for this project. Expect this field to be empty currently. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="effective_enablement" type="string">
  Based on the effective container's setting above, determines Whether this resource container has the quota adjuster enabled. Expect this field to be empty currently. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="inherited" type="bool">
  Indicates whether the setting is inherited or explicitly specified. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="inherited_from" type="string">
  The resource container from which the setting is inherited. This refers to the nearest ancestor with enablement set (either ENABLED or DISABLED). The value can be 'organizations/&#123;organization_id&#125;', 'folders/&#123;folder_id&#125;', or can be 'default' if no ancestor exists with enablement set. The value will be empty when 'enablement' is specified on this resource container. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="parent" type="string">
  The parent of the quota preference. Allowed parent format is "projects/[project-id / number]". Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
