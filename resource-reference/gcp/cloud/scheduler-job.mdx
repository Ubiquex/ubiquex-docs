---
title: "google_cloud_scheduler_job"
description: "Real, generated bindings for `google_cloud_scheduler_job`."
---

`google_cloud_scheduler_job` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloud "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloud"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own scheduler job"})

    		ubx.Resource(cloud.SchedulerJob, "ci-runner", cloud.SchedulerJobConfig{
    			Name:            "ci-runner",
    			AttemptDeadline: "example",
    			Description:     "Managed by ubx.",
    			Schedule:        "example",
    			TimeZone:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SchedulerJob } from "jsr:@ubx/sdk-google/google/cloud/scheduler_job";

    export default stack("payments", () => {
      intent({ summary: "payments own scheduler job" });

      resource(SchedulerJob, "ci-runner", {
        name: "ci-runner",
        attemptDeadline: "example",
        description: "Managed by ubx.",
        schedule: "example",
        timeZone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.cloud import SchedulerJob, SchedulerJobConfig

    def describe():
        ubx.intent("payments own scheduler job")

        ubx.resource(SchedulerJob, "ci-runner", SchedulerJobConfig(
            name="ci-runner",
            attempt_deadline="example",
            description="Managed by ubx.",
            schedule="example",
            time_zone="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloud_scheduler_job called "ci-runner", with
    attempt deadline example, description Managed by ubx., schedule
    example, time zone example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_engine_http_target" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="app_engine_routing" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="instance" type="string">
        </ResponseField>
        <ResponseField name="service" type="string">
        </ResponseField>
        <ResponseField name="version" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="body" type="string">
    </ResponseField>
    <ResponseField name="headers" type="map(string)">
    </ResponseField>
    <ResponseField name="http_method" type="string">
    </ResponseField>
    <ResponseField name="relative_uri" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="attempt_deadline" type="string">
  The deadline for job attempts. If the request handler does not respond by this deadline then the request is cancelled and the attempt is marked as a DEADLINE_EXCEEDED failure. The failed attempt can be viewed in execution logs. Cloud Scheduler will retry the job according to the RetryConfig. The allowed duration for this deadline is: * For HTTP targets, between 15 seconds and 30 minutes. * For App Engine HTTP targets, between 15 seconds and 24 hours. * **Note**: For PubSub targets, this field is ignored - setting it will introduce an unresolvable diff. A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s" Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A human-readable description for the job. This string must not contain more than 500 characters. Optional.
</ResponseField>

<ResponseField name="http_target" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="body" type="string">
    </ResponseField>
    <ResponseField name="headers" type="map(string)">
    </ResponseField>
    <ResponseField name="http_method" type="string">
    </ResponseField>
    <ResponseField name="oauth_token" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="scope" type="string">
        </ResponseField>
        <ResponseField name="service_account_email" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="oidc_token" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="audience" type="string">
        </ResponseField>
        <ResponseField name="service_account_email" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="uri" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the job. Required.
</ResponseField>

<ResponseField name="paused" type="bool">
  Sets the job to a paused state. Jobs default to being enabled when this property is not set. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="pubsub_target" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="attributes" type="map(string)">
    </ResponseField>
    <ResponseField name="data" type="string">
    </ResponseField>
    <ResponseField name="topic_name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where the scheduler job resides. If it is not provided, Terraform will use the provider default. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="retry_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="max_backoff_duration" type="string">
    </ResponseField>
    <ResponseField name="max_doublings" type="number">
    </ResponseField>
    <ResponseField name="max_retry_duration" type="string">
    </ResponseField>
    <ResponseField name="min_backoff_duration" type="string">
    </ResponseField>
    <ResponseField name="retry_count" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="schedule" type="string">
  Describes the schedule on which the job will be executed. Optional.
</ResponseField>

<ResponseField name="time_zone" type="string">
  Specifies the time zone to be used in interpreting schedule. The value of this field must be a time zone name from the tz database. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="app_engine_http_target" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="app_engine_routing" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="instance" type="string">
        </ResponseField>
        <ResponseField name="service" type="string">
        </ResponseField>
        <ResponseField name="version" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="body" type="string">
    </ResponseField>
    <ResponseField name="headers" type="map(string)">
    </ResponseField>
    <ResponseField name="http_method" type="string">
    </ResponseField>
    <ResponseField name="relative_uri" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="http_target" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="body" type="string">
    </ResponseField>
    <ResponseField name="headers" type="map(string)">
    </ResponseField>
    <ResponseField name="http_method" type="string">
    </ResponseField>
    <ResponseField name="oauth_token" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="scope" type="string">
        </ResponseField>
        <ResponseField name="service_account_email" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="oidc_token" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="audience" type="string">
        </ResponseField>
        <ResponseField name="service_account_email" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="uri" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="paused" type="bool">
  Sets the job to a paused state. Jobs default to being enabled when this property is not set. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="pubsub_target" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="attributes" type="map(string)">
    </ResponseField>
    <ResponseField name="data" type="string">
    </ResponseField>
    <ResponseField name="topic_name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where the scheduler job resides. If it is not provided, Terraform will use the provider default. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="retry_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="max_backoff_duration" type="string">
    </ResponseField>
    <ResponseField name="max_doublings" type="number">
    </ResponseField>
    <ResponseField name="max_retry_duration" type="string">
    </ResponseField>
    <ResponseField name="min_backoff_duration" type="string">
    </ResponseField>
    <ResponseField name="retry_count" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  State of the job. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
