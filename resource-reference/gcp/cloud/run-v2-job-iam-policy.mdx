---
title: "google_cloud_run_v2_job_iam_policy"
description: "Real, generated bindings for `google_cloud_run_v2_job_iam_policy`."
---

`google_cloud_run_v2_job_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloud "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloud"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own run v2 job iam policy"})

    		ubx.Resource(cloud.RunV2JobIamPolicy, "ci-runner", cloud.RunV2JobIamPolicyConfig{
    			Name:       "ci-runner",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RunV2JobIamPolicy } from "jsr:@ubx/sdk-google/google/cloud/run_v2_job_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own run v2 job iam policy" });

      resource(RunV2JobIamPolicy, "ci-runner", {
        name: "ci-runner",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.cloud import RunV2JobIamPolicy, RunV2JobIamPolicyConfig

    def describe():
        ubx.intent("payments own run v2 job iam policy")

        ubx.resource(RunV2JobIamPolicy, "ci-runner", RunV2JobIamPolicyConfig(
            name="ci-runner",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloud_run_v2_job_iam_policy called "ci-runner",
    with policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
