---
title: "google_cloud_tasks_queue_iam_policy"
description: "Real, generated bindings for `google_cloud_tasks_queue_iam_policy`."
---

`google_cloud_tasks_queue_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloud"
    )

    cfg := cloud.TasksQueueIamPolicyConfig{
        Name: "example-name",
        PolicyData: "example",
    }
    ubx.Resource(cloud.TasksQueueIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TasksQueueIamPolicy } from
      "jsr:@ubx/sdk-google/google/cloud/tasks_queue_iam_policy";

    resource(TasksQueueIamPolicy, "example", {
      name: "example-name",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.cloud import (
        TasksQueueIamPolicy,
        TasksQueueIamPolicyConfig,
    )

    cfg = TasksQueueIamPolicyConfig(
        name="example-name",
        policy_data="example",
    )
    ubx.resource(TasksQueueIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_cloud_tasks_queue_iam_policy with name,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
