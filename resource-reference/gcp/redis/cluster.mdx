---
title: "google_redis_cluster"
description: "`google_redis_cluster` provisions a Memorystore for Redis Cluster instance, a horizontally scalable, clustered deployment of managed Redis."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	redis "github.com/ubiquex/ubx-sdk-google/sdk/go/google/redis"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cluster"})

    		ubx.Resource(redis.Cluster, "example", redis.ClusterConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Cluster } from "@ubx/sdk-google/google/redis/cluster";

    export default stack("example", () => {
      intent({ summary: "example own cluster" });

      resource(Cluster, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.redis import Cluster, ClusterConfig

    def describe():
        ubx.intent("example own cluster")

        ubx.resource(Cluster, "example", ClusterConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_redis_cluster called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="acl_policy" type="string">
  Optional. The ACL policy to be applied to the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="acl_policy_in_sync" type="bool">
  Optional. Output only. Deprecated: Indicates whether the ACL rules applied to the cluster are in sync. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="acl_policy_info" type="object">
  Details of the applied ACL policy. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="acl_policy_revision_statuses" type="list(object)">
      Output only. A list of status for various revisions of this ACL policy on the cluster. Computed by Google Cloud after `ship`.
      <Expandable title="properties">
        <ResponseField name="acl_policy_revision" type="string">
          The current revision of this cluster's own access control list policy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="acl_policy_revision_number" type="string">
          The numeric revision identifier of this ACL policy. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="error_message" type="string">
          A human-readable description of the error. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The current lifecycle state of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="applied_acl_policy" type="string">
      Output only. The resource name of the applied ACL policy. Format: "projects/&#123;project&#125;/locations/&#123;location&#125;/aclPolicies/&#123;acl_policy&#125;" Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="applied_acl_policy_revision" type="string">
      Output only. The resource name of the applied ACL policy revision. Format: "projects/&#123;project&#125;/locations/&#123;location&#125;/aclPolicies/&#123;acl_policy&#125;/revisions/&#123;revision&#125;" Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="applied_acl_policy_revision_number" type="string">
      Output only. The revision number of the applied ACL policy revision. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="allow_fewer_zones_deployment" type="bool">
  Optional. Immutable. Deprecated, do not use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="async_cluster_endpoints_deletion_enabled" type="bool">
  Optional. If true, cluster endpoints that are created and registered by customers can be deleted asynchronously. That is, such a cluster endpoint can be de-registered before the forwarding rules in the cluster endpoint are deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="authorization_mode" type="string">
  Optional. The authorization mode of the Redis cluster. If not provided, auth feature is disabled for the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="automated_backup_config" type="object">
  The automated backup config for a cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="automated_backup_mode" type="string">
      Optional. The automated backup mode. If the mode is disabled, the other fields will be ignored. Optional.
    </ResponseField>
    <ResponseField name="fixed_frequency_schedule" type="object">
      This schedule allows the backup to be triggered at a fixed frequency (currently only daily is supported). Optional.
      <Expandable title="properties">
        <ResponseField name="start_time" type="object">
          Represents a time of day. The date and time zone are either not significant or are specified elsewhere. An API may choose to allow leap seconds. Related types are google.type.Date and `google.protobuf.Timestamp`. Optional.
          <Expandable title="properties">
            <ResponseField name="hours" type="number">
              Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value "24:00:00" for scenarios like business closing time. Optional.
            </ResponseField>
            <ResponseField name="minutes" type="number">
              Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59. Optional.
            </ResponseField>
            <ResponseField name="nanos" type="number">
              Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999. Optional.
            </ResponseField>
            <ResponseField name="seconds" type="number">
              Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="retention" type="string">
      Optional. How long to keep automated backups before the backups are deleted. The value should be between 1 day and 365 days. If not specified, the default value is 35 days. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="available_maintenance_versions" type="list(string)">
  Output only. This field is used to determine the available maintenance versions for the self service update. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="backup_collection" type="string">
  Optional. Output only. The backup collection full resource name. Example: projects/&#123;project&#125;/locations/&#123;location&#125;/backupCollections/&#123;collection&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="cluster_endpoints" type="list(object)">
  Optional. A list of cluster endpoints. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="connections" type="list(object)">
      The active connection(s) to this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="psc_auto_connection" type="object">
          A Private Service Connect connection automatically established for this cluster. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="address" type="string">
              The IP address this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="connection_type" type="string">
              Whether this connection uses Private Service Connect or a direct peering connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="forwarding_rule" type="string">
              A reference to the forwarding rule this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network" type="string">
              The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="project_id" type="string">
              The identifier of the associated Google Cloud project. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="psc_connection_id" type="string">
              The unique identifier of this Private Service Connect connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="psc_connection_status" type="string">
              The current status of this Private Service Connect connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="service_attachment" type="string">
              A reference to the Private Service Connect service attachment this connects to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="psc_connection" type="object">
          A Private Service Connect connection associated with this instance. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="address" type="string">
              The IP address this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="connection_type" type="string">
              Whether this connection uses Private Service Connect or a direct peering connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="forwarding_rule" type="string">
              A reference to the forwarding rule this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network" type="string">
              The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="port" type="number">
              The port number this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="project_id" type="string">
              The identifier of the associated Google Cloud project. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="psc_connection_id" type="string">
              The unique identifier of this Private Service Connect connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="psc_connection_status" type="string">
              The current status of this Private Service Connect connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="service_attachment" type="string">
              A reference to the Private Service Connect service attachment this connects to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp associated with the cluster creation request. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="cross_cluster_replication_config" type="object">
  Cross cluster replication config. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="cluster_role" type="string">
      Output only. The role of the cluster in cross cluster replication. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="membership" type="object">
      An output only view of all the member clusters participating in the cross cluster replication. Optional.
      <Expandable title="properties">
        <ResponseField name="primary_cluster" type="object">
          Details of the remote cluster associated with this cluster in a cross cluster replication setup. Optional.
          <Expandable title="properties">
            <ResponseField name="cluster" type="string">
              Output only. The full resource path of the remote cluster in the format: projects//locations//clusters/ Computed by Google Cloud after `ship`.
            </ResponseField>
            <ResponseField name="uid" type="string">
              Output only. The unique identifier of the remote cluster. Computed by Google Cloud after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="secondary_clusters" type="list(object)">
          Output only. The list of secondary clusters replicating from the primary cluster. Computed by Google Cloud after `ship`.
          <Expandable title="properties">
            <ResponseField name="cluster" type="string">
              A reference to the Redis cluster this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="uid" type="string">
              Output only. A system-assigned, globally unique identifier for this resource, in UUID4 format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="primary_cluster" type="object">
      Details of the remote cluster associated with this cluster in a cross cluster replication setup. Optional.
      <Expandable title="properties">
        <ResponseField name="cluster" type="string">
          Output only. The full resource path of the remote cluster in the format: projects//locations//clusters/ Computed by Google Cloud after `ship`.
        </ResponseField>
        <ResponseField name="uid" type="string">
          Output only. The unique identifier of the remote cluster. Computed by Google Cloud after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="secondary_clusters" type="list(object)">
      List of secondary clusters that are replicating from this primary cluster. This field is only set for a primary cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="cluster" type="string">
          A reference to the Redis cluster this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="uid" type="string">
          Output only. A system-assigned, globally unique identifier for this resource, in UUID4 format. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. The last time cross cluster replication config was updated. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_protection_enabled" type="bool">
  Optional. The delete operation will fail when the value is set to true. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="discovery_endpoints" type="list(object)">
  Output only. Endpoints created on each given network, for Redis clients to connect to the cluster. Currently only one discovery endpoint is supported. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="address" type="string">
      The IP address this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="port" type="number">
      The port number this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="psc_config" type="object">
      Configuration for connecting to this instance via Private Service Connect. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="network" type="string">
          The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="effective_maintenance_version" type="string">
  Output only. This field represents the actual maintenance version of the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="encryption_info" type="object">
  EncryptionInfo describes the encryption information of a cluster or a backup. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="encryption_type" type="string">
      Output only. Type of encryption. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="kms_key_primary_state" type="string">
      Output only. The state of the primary version of the KMS key perceived by the system. This field is not populated in backups. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="kms_key_versions" type="list(string)">
      Output only. KMS key versions that are being used to protect the data at-rest. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="last_update_time" type="string">
      Output only. The most recent time when the encryption info was updated. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="gcs_source" type="object">
  Backups stored in Cloud Storage buckets. The Cloud Storage buckets need to be the same region as the clusters. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="uris" type="list(string)">
      Optional. URIs of the Cloud Storage objects to import. Example: gs://bucket1/object1, gs://bucket2/folder2/object2 Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kms_key" type="string">
  Optional. The KMS key used to encrypt the at-rest data of the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels to represent user-provided metadata. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="maintenance_policy" type="object">
  Maintenance policy per cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="create_time" type="string">
      Output only. The time when the policy was created i.e. Maintenance Window or Deny Period was assigned. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. The time when the policy was updated i.e. Maintenance Window or Deny Period was updated. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="weekly_maintenance_window" type="list(object)">
      Optional. Maintenance window that is applied to resources covered by this policy. Minimum 1. For the current version, the maximum number of weekly_maintenance_window is expected to be one. Optional.
      <Expandable title="properties">
        <ResponseField name="day" type="string">
          The day-of-month component of a date. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_time" type="object">
          When this event or window started. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="hours" type="number">
              The hour component of a time value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="minutes" type="number">
              The minute component of a time value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nanos" type="number">
              The sub-second, nanosecond component of a time value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="seconds" type="number">
              The second component of a time value. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="maintenance_schedule" type="object">
  Upcoming maintenance schedule. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
      Output only. The end time of any upcoming scheduled maintenance for this instance. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Output only. The start time of any upcoming scheduled maintenance for this instance. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="maintenance_version" type="string">
  Optional. This field can be used to trigger self service update to indicate the desired maintenance version. The input to this field can be determined by the available_maintenance_versions field. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="managed_backup_source" type="object">
  Backups that generated and managed by memorystore. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="backup" type="string">
      Optional. Example: //redis.googleapis.com/projects/&#123;project&#125;/locations/&#123;location&#125;/backupCollections/&#123;collection&#125;/backups/&#123;backup&#125; A shorter version (without the prefix) of the backup name is also supported, like projects/&#123;project&#125;/locations/&#123;location&#125;/backupCollections/&#123;collection&#125;/backups/&#123;backup_id&#125; In this case, it assumes the backup is under redis.googleapis.com. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Required. Identifier. Unique name of the resource in this scope including project and location using the form: `projects/&#123;project_id&#125;/locations/&#123;location_id&#125;/clusters/&#123;cluster_id&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="node_type" type="string">
  Optional. The type of a redis node in the cluster. NodeType determines the underlying machine-type of a redis node. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ondemand_maintenance" type="bool">
  Optional. Input only. Ondemand maintenance for the cluster. This field can be used to trigger ondemand critical update on the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="persistence_config" type="object">
  Configuration of the persistence functionality. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="aof_config" type="object">
      Configuration of the AOF based persistence. Optional.
      <Expandable title="properties">
        <ResponseField name="append_fsync" type="string">
          Optional. fsync configuration. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mode" type="string">
      Optional. The mode of persistence. Optional.
    </ResponseField>
    <ResponseField name="rdb_config" type="object">
      Configuration of the RDB based persistence. Optional.
      <Expandable title="properties">
        <ResponseField name="rdb_snapshot_period" type="string">
          Optional. Period between RDB snapshots. Optional.
        </ResponseField>
        <ResponseField name="rdb_snapshot_start_time" type="string">
          Optional. The time that the first snapshot was/will be attempted, and to which future snapshots will be aligned. If not provided, the current time will be used. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="precise_size_gb" type="number">
  Output only. Precise value of redis memory size in GB for the entire cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="psc_configs" type="list(object)">
  Optional. Each PscConfig configures the consumer network where IPs will be designated to the cluster for client access through Private Service Connect Automation. Currently, only one PscConfig is supported. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="network" type="string">
      The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="psc_connections" type="list(object)">
  Output only. The list of PSC connections that are auto-created through service connectivity automation. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="address" type="string">
      The IP address this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="connection_type" type="string">
      Whether this connection uses Private Service Connect or a direct peering connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="forwarding_rule" type="string">
      A reference to the forwarding rule this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="network" type="string">
      The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="port" type="number">
      The port number this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="project_id" type="string">
      The identifier of the associated Google Cloud project. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="psc_connection_id" type="string">
      The unique identifier of this Private Service Connect connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="psc_connection_status" type="string">
      The current status of this Private Service Connect connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="service_attachment" type="string">
      A reference to the Private Service Connect service attachment this connects to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="psc_service_attachments" type="list(object)">
  Output only. Service attachment details to configure Psc connections Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="connection_type" type="string">
      Whether this connection uses Private Service Connect or a direct peering connection. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="service_attachment" type="string">
      A reference to the Private Service Connect service attachment this connects to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="redis_configs" type="map(string)">
  Optional. Key/Value pairs of customer overrides for mutable Redis Configs Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="replica_count" type="number">
  Optional. The number of replica nodes per shard. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="rotate_server_certificate" type="bool">
  Optional. Input only. Rotate the server certificates. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Optional. Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Optional. Output only. Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="server_ca_mode" type="string">
  Optional. Server CA mode for the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="server_ca_pool" type="string">
  Optional. Customer-managed CA pool for the cluster. Only applicable for BYOCA i.e. if server_ca_mode is SERVER_CA_MODE_CUSTOMER_MANAGED_CAS_CA. Format: "projects/&#123;project&#125;/locations/&#123;region&#125;/caPools/&#123;ca_pool&#125;". Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="shard_count" type="number">
  Optional. Number of shards for the Redis cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="simulate_maintenance_event" type="bool">
  Optional. Input only. Simulate a maintenance event. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="size_gb" type="number">
  Output only. Redis memory size in GB for the entire cluster rounded up to the next integer. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of this cluster. Can be CREATING, READY, UPDATING, DELETING and SUSPENDED Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state_info" type="object">
  Represents additional information about the state of the cluster. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="update_info" type="object">
      Represents information about an updating cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="target_node_type" type="string">
          Target node type for redis cluster. Optional.
        </ResponseField>
        <ResponseField name="target_replica_count" type="number">
          Target number of replica nodes per shard. Optional.
        </ResponseField>
        <ResponseField name="target_shard_count" type="number">
          Target number of shards for redis cluster Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transit_encryption_mode" type="string">
  Optional. The in-transit encryption for the Redis cluster. If not provided, encryption is disabled for the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System assigned, unique identifier for the cluster. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="zone_distribution_config" type="object">
  Zone distribution config for allocation of cluster resources. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="mode" type="string">
      Optional. The mode of zone distribution. Defaults to MULTI_ZONE, when not specified. Optional.
    </ResponseField>
    <ResponseField name="zone" type="string">
      Optional. When SINGLE ZONE distribution is selected, zone field would be used to allocate all resources in that zone. This is not applicable to MULTI_ZONE, and would be ignored for MULTI_ZONE clusters. Optional.
    </ResponseField>
    <ResponseField name="zones" type="list(string)">
      Optional. Specify the zones of a multi-zone cluster where Redis Cluster allocates resources. This flag isn't applicable for single-zone clusters. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
