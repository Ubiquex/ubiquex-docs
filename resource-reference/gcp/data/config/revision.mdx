---
title: "google_config_revision"
description: "`data_config_revision` looks up a real revision of an Infrastructure Manager deployment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	config "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/config"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_config_revision"})

    		ubx.Data(config.Revision, "example", config.RevisionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { Revision } from "./local-sdk/google/sdk/typescript/google/data/config/revision.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_config_revision" });

      data(Revision, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.config import Revision, RevisionConfig

    def describe():
        ubx.intent("look up google_config_revision")

        ubx.data(Revision, "example", RevisionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="alternative_ids" type="list(string)">
  Output only. The alternative IDs of the deployment group revision. Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Time when the deployment group revision was created. Returned by the lookup.
</ResponseField>

<ResponseField name="snapshot" type="object">
  A DeploymentGroup is a collection of DeploymentUnits that in a DAG-like structure. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
      Optional. Arbitrary key-value metadata storage e.g. to help client tools identify deployment group during automation. See https://google.aip.dev/148#annotations for details on format and size limitations. Optional lookup argument.
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. Time when the deployment group was created. Returned by the lookup.
    </ResponseField>
    <ResponseField name="deployment_units" type="list(object)">
      The deployment units of the deployment group in a DAG like structure. When a deployment group is being provisioned, the deployment units are deployed in a DAG order. The provided units must be in a DAG order, otherwise an error will be returned. Optional lookup argument.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Optional. User-defined metadata for the deployment group. Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Identifier. The name of the deployment group. Format: 'projects/&#123;project_id&#125;/locations/&#123;location&#125;/deploymentGroups/&#123;deployment_group&#125;'. Optional lookup argument.
    </ResponseField>
    <ResponseField name="provisioning_error" type="object">
      The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="code" type="number">
          The status code, which should be an enum value of google.rpc.Code. Optional lookup argument.
        </ResponseField>
        <ResponseField name="details" type="list(map(any))">
          A list of messages that carry the error details. There is a common set of message types for APIs to use. Optional lookup argument.
        </ResponseField>
        <ResponseField name="message" type="string">
          A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Output only. The provisioning state of the deployment group. Returned by the lookup.
    </ResponseField>
    <ResponseField name="provisioning_state_description" type="string">
      Output only. Additional information regarding the current provisioning state. Returned by the lookup.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. Current state of the deployment group. Returned by the lookup.
    </ResponseField>
    <ResponseField name="state_description" type="string">
      Output only. Additional information regarding the current state. Returned by the lookup.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. Time when the deployment group was last updated. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
