---
title: "google_config_terraform_version"
description: "`data_config_terraform_version` looks up a real, supported Terraform version usable with Infrastructure Manager deployments."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	config "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/config"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_config_terraform_version"})

    		ubx.Data(config.TerraformVersion, "example", config.TerraformVersionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { TerraformVersion } from "./local-sdk/google/sdk/typescript/google/data/config/terraform_version.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_config_terraform_version" });

      data(TerraformVersion, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.config import TerraformVersion, TerraformVersionConfig

    def describe():
        ubx.intent("look up google_config_terraform_version")

        ubx.data(TerraformVersion, "example", TerraformVersionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="deprecate_time" type="string">
  Output only. When the version is deprecated. Returned by the lookup.
</ResponseField>

<ResponseField name="obsolete_time" type="string">
  Output only. When the version is obsolete. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the version, ACTIVE, DEPRECATED or OBSOLETE. Returned by the lookup.
</ResponseField>

<ResponseField name="support_time" type="string">
  Output only. When the version is supported. Returned by the lookup.
</ResponseField>
