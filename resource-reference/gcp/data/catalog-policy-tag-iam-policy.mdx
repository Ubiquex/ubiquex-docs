---
title: "google_data_catalog_policy_tag_iam_policy"
description: "Real, generated bindings for `google_data_catalog_policy_tag_iam_policy`."
---

`google_data_catalog_policy_tag_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own catalog policy tag iam policy"})

    		ubx.Resource(data.CatalogPolicyTagIamPolicy, "ci-runner", data.CatalogPolicyTagIamPolicyConfig{
    			PolicyData: "example",
    			PolicyTag:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CatalogPolicyTagIamPolicy } from "jsr:@ubx/sdk-google/google/data/catalog_policy_tag_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own catalog policy tag iam policy" });

      resource(CatalogPolicyTagIamPolicy, "ci-runner", {
        policyData: "example",
        policyTag: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data import CatalogPolicyTagIamPolicy, CatalogPolicyTagIamPolicyConfig

    def describe():
        ubx.intent("payments own catalog policy tag iam policy")

        ubx.resource(CatalogPolicyTagIamPolicy, "ci-runner", CatalogPolicyTagIamPolicyConfig(
            policy_data="example",
            policy_tag="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_data_catalog_policy_tag_iam_policy, with policy
    data example, policy tag example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_tag" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
