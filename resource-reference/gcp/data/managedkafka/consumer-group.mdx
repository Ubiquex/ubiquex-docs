---
title: "google_managedkafka_consumer_group"
description: "`data_managedkafka_consumer_group` looks up a real Kafka consumer group tracked by a Managed Service for Apache Kafka cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	managedkafka "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/managedkafka"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_managedkafka_consumer_group"})

    		ubx.Data(managedkafka.ConsumerGroup, "example", managedkafka.ConsumerGroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConsumerGroup } from "@ubx/sdk-google/google/data/managedkafka/consumer_group";

    export default stack("example", () => {
      intent({ summary: "look up google_managedkafka_consumer_group" });

      data(ConsumerGroup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.managedkafka import ConsumerGroup, ConsumerGroupConfig

    def describe():
        ubx.intent("look up google_managedkafka_consumer_group")

        ubx.data(ConsumerGroup, "example", ConsumerGroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="topics" type="map(object)">
  Optional. Metadata for this consumer group for all topics it has metadata for. The key of the map is a topic name, structured like: projects/&#123;project&#125;/locations/&#123;location&#125;/clusters/&#123;cluster&#125;/topics/&#123;topic&#125; Returned by the lookup.
</ResponseField>
