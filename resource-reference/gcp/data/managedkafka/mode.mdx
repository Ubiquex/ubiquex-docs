---
title: "google_managedkafka_mode"
description: "`data_managedkafka_mode` looks up the real, current compatibility mode setting for a Managed Service for Apache Kafka Schema Registry."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	managedkafka "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/managedkafka"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_managedkafka_mode"})

    		ubx.Data(managedkafka.Mode, "example", managedkafka.ModeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Mode } from "@ubx/sdk-google/google/data/managedkafka/mode";

    export default stack("example", () => {
      intent({ summary: "look up google_managedkafka_mode" });

      data(Mode, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.managedkafka import Mode, ModeConfig

    def describe():
        ubx.intent("look up google_managedkafka_mode")

        ubx.data(Mode, "example", ModeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mode" type="string">
  Required. The mode type of a schema registry (READWRITE by default) or of a subject (unset by default, which means use the global schema registry setting). Returned by the lookup.
</ResponseField>
