---
title: "google_toolresults_cluster"
description: "`data_toolresults_cluster` looks up a real cluster of similar test results grouped by Firebase Test Lab's own Tool Results service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	toolresults "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/toolresults"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_toolresults_cluster"})

    		ubx.Data(toolresults.Cluster, "example", toolresults.ClusterConfig{
    			ClusterId:   "example",
    			ExecutionId: "example",
    			HistoryId:   "example",
    			ProjectId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Cluster } from "@ubx/sdk-google/google/data/toolresults/cluster";

    export default stack("example", () => {
      intent({ summary: "look up google_toolresults_cluster" });

      data(Cluster, "example", {
        clusterId: "example",
        executionId: "example",
        historyId: "example",
        projectId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.toolresults import Cluster, ClusterConfig

    def describe():
        ubx.intent("look up google_toolresults_cluster")

        ubx.data(Cluster, "example", ClusterConfig(
            cluster_id="example",
            execution_id="example",
            history_id="example",
            project_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="execution_id" type="string" required>
  The real, unique identifier of the execution this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="history_id" type="string" required>
  The real, unique identifier of the test history this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="project_id" type="string" required>
  The real Google Cloud project ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="activity" type="string">
  A string that describes the activity of every screen in the cluster. Returned by the lookup.
</ResponseField>

<ResponseField name="key_screen" type="object">
  The real, associated key screen (test result screenshot) for this object. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="file_reference" type="string">
      File reference of the png file. Required. Optional lookup argument.
    </ResponseField>
    <ResponseField name="locale" type="string">
      Locale of the device that the screenshot was taken on. Required. Optional lookup argument.
    </ResponseField>
    <ResponseField name="model" type="string">
      Model of the device that the screenshot was taken on. Required. Optional lookup argument.
    </ResponseField>
    <ResponseField name="version" type="string">
      OS version of the device that the screenshot was taken on. Required. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="screens" type="list(object)">
  Full list of screens. Returned by the lookup.
</ResponseField>
