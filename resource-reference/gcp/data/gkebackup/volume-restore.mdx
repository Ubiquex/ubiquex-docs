---
title: "google_gkebackup_volume_restore"
description: "`data_gkebackup_volume_restore` looks up a real, already-existing `google_gkebackup_restore`."
---

It restores a GKE backup's own workload configuration and data into a target cluster, executed according to a restore plan.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gkebackup "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/gkebackup"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_gkebackup_volume_restore"})

    		ubx.Data(gkebackup.VolumeRestore, "example", gkebackup.VolumeRestoreConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VolumeRestore } from "@ubx/sdk-google/google/data/gkebackup/volume_restore";

    export default stack("example", () => {
      intent({ summary: "look up google_gkebackup_volume_restore" });

      data(VolumeRestore, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.gkebackup import VolumeRestore, VolumeRestoreConfig

    def describe():
        ubx.intent("look up google_gkebackup_volume_restore")

        ubx.data(VolumeRestore, "example", VolumeRestoreConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="complete_time" type="string">
  Output only. The timestamp when the associated underlying volume restoration completed. Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp when this VolumeRestore resource was created. Returned by the lookup.
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. `etag` is used for optimistic concurrency control as a way to help prevent simultaneous updates of a volume restore from overwriting each other. It is strongly suggested that systems make use of the `etag` in the read-modify-write cycle to perform volume restore updates in order to avoid race conditions. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of this VolumeRestore. Returned by the lookup.
</ResponseField>

<ResponseField name="state_message" type="string">
  Output only. A human readable message explaining why the VolumeRestore is in its current state. Returned by the lookup.
</ResponseField>

<ResponseField name="target_pvc" type="object">
  A reference to a namespaced resource in Kubernetes. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Optional. The name of the Kubernetes resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="namespace" type="string">
      Optional. The Namespace of the Kubernetes resource. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. Server generated global unique identifier of [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when this VolumeRestore resource was last updated. Returned by the lookup.
</ResponseField>

<ResponseField name="volume_backup" type="string">
  Output only. The full name of the VolumeBackup from which the volume will be restored. Format: `projects/*/locations/*/backupPlans/*/backups/*/volumeBackups/*`. Returned by the lookup.
</ResponseField>

<ResponseField name="volume_handle" type="string">
  Output only. A storage system-specific opaque handler to the underlying volume created for the target PVC from the volume backup. Returned by the lookup.
</ResponseField>

<ResponseField name="volume_type" type="string">
  Output only. The type of volume provisioned Returned by the lookup.
</ResponseField>
