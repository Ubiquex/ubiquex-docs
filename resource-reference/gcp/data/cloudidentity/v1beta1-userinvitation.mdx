---
title: "google_cloudidentity_v1beta1_userinvitation"
description: "`data_cloudidentity_v1beta1_userinvitation` looks up a real invitation sent to a Cloud Identity Free user to become fully managed, via the v1beta1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudidentity "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/cloudidentity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_cloudidentity_v1beta1_userinvitation"})

    		ubx.Data(cloudidentity.V1beta1Userinvitation, "example", cloudidentity.V1beta1UserinvitationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1beta1Userinvitation } from "./local-sdk/google/sdk/typescript/google/data/cloudidentity/v1beta1_userinvitation.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_cloudidentity_v1beta1_userinvitation" });

      data(V1beta1Userinvitation, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.cloudidentity import V1beta1Userinvitation, V1beta1UserinvitationConfig

    def describe():
        ubx.intent("look up google_cloudidentity_v1beta1_userinvitation")

        ubx.data(V1beta1Userinvitation, "example", V1beta1UserinvitationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mails_sent_count" type="string">
  Number of invitation emails sent to the user. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  State of the `UserInvitation`. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Time when the `UserInvitation` was last updated. Returned by the lookup.
</ResponseField>
