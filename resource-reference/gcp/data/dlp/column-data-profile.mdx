---
title: "google_dlp_column_data_profile"
description: "`google_dlp_column_data_profile` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_dlp_column_data_profile` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dlp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/dlp"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_dlp_column_data_profile"})

    		ubx.Data(dlp.ColumnDataProfile, "example", dlp.ColumnDataProfileConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ColumnDataProfile } from "@ubx/sdk-google/google/data/dlp/column_data_profile";

    export default stack("example", () => {
      intent({ summary: "look up google_dlp_column_data_profile" });

      data(ColumnDataProfile, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.dlp import ColumnDataProfile, ColumnDataProfileConfig

    def describe():
        ubx.intent("look up google_dlp_column_data_profile")

        ubx.data(ColumnDataProfile, "example", ColumnDataProfileConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="column" type="string">
  The name of the column. Returned by the lookup.
</ResponseField>

<ResponseField name="column_info_type" type="object">
  The infoType details for this column. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="estimated_prevalence" type="number">
      Not populated for predicted infotypes. Optional lookup argument.
    </ResponseField>
    <ResponseField name="info_type" type="object">
      Type of information detected by the API. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Name of the information type. Either a name of your choosing when creating a CustomInfoType, or one of the names listed at https://docs.cloud.google.com/sensitive-data-protection/docs/infotypes-reference when specifying a built-in type. When sending Cloud DLP results to Data Catalog, infoType names should conform to the pattern `[A-Za-z0-9$_-]&#123;1,64&#125;`. Optional lookup argument.
        </ResponseField>
        <ResponseField name="sensitivity_score" type="object">
          Score is calculated from of all elements in the data profile. A higher level means the data is more sensitive. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="score" type="string">
              The sensitivity score applied to the resource. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="version" type="string">
          Optional version name for this InfoType. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="column_type" type="string">
  The data type of a given column. Returned by the lookup.
</ResponseField>

<ResponseField name="data_risk_level" type="object">
  Score is a summary of all elements in the data profile. A higher number means more risk. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="score" type="string">
      The score applied to the resource. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dataset_id" type="string">
  The BigQuery dataset ID, if the resource profiled is a BigQuery table. Returned by the lookup.
</ResponseField>

<ResponseField name="dataset_location" type="string">
  If supported, the location where the dataset's data is stored. See https://docs.cloud.google.com/bigquery/docs/locations for supported BigQuery locations. Returned by the lookup.
</ResponseField>

<ResponseField name="dataset_project_id" type="string">
  The Google Cloud project ID that owns the profiled resource. Returned by the lookup.
</ResponseField>

<ResponseField name="estimated_null_percentage" type="string">
  Approximate percentage of entries being null in the column. Returned by the lookup.
</ResponseField>

<ResponseField name="estimated_uniqueness_score" type="string">
  Approximate uniqueness of the column. Returned by the lookup.
</ResponseField>

<ResponseField name="free_text_score" type="number">
  The likelihood that this column contains free-form text. A value close to 1 may indicate the column is likely to contain free-form or natural language text. Range in 0-1. Returned by the lookup.
</ResponseField>

<ResponseField name="other_matches" type="list(object)">
  Other types found within this column. List will be unordered. Returned by the lookup.
</ResponseField>

<ResponseField name="policy_state" type="string">
  Indicates if a policy tag has been applied to the column. Returned by the lookup.
</ResponseField>

<ResponseField name="profile_last_generated" type="string">
  The last time the profile was generated. Returned by the lookup.
</ResponseField>

<ResponseField name="profile_status" type="object">
  Success or errors for the profile generation. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="status" type="object">
      The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="code" type="number">
          The status code, which should be an enum value of google.rpc.Code. Optional lookup argument.
        </ResponseField>
        <ResponseField name="details" type="list(map(any))">
          A list of messages that carry the error details. There is a common set of message types for APIs to use. Optional lookup argument.
        </ResponseField>
        <ResponseField name="message" type="string">
          A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      Time when the profile generation status was updated Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sensitivity_score" type="object">
  Score is calculated from of all elements in the data profile. A higher level means the data is more sensitive. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="score" type="string">
      The sensitivity score applied to the resource. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  State of a profile. Returned by the lookup.
</ResponseField>

<ResponseField name="table_data_profile" type="string">
  The resource name of the table data profile. Returned by the lookup.
</ResponseField>

<ResponseField name="table_full_resource" type="string">
  The resource name of the resource this column is within. Returned by the lookup.
</ResponseField>

<ResponseField name="table_id" type="string">
  The table ID. Returned by the lookup.
</ResponseField>
