---
title: "google_vmwareengine_node"
description: "`data_vmwareengine_node` looks up a real physical ESXi host node backing a Google Cloud VMware Engine cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmwareengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/vmwareengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_vmwareengine_node"})

    		ubx.Data(vmwareengine.Node, "example", vmwareengine.NodeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { Node } from "./local-sdk/google/sdk/typescript/google/data/vmwareengine/node.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_vmwareengine_node" });

      data(Node, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.vmwareengine import Node, NodeConfig

    def describe():
        ubx.intent("look up google_vmwareengine_node")

        ubx.data(Node, "example", NodeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="custom_core_count" type="string">
  Output only. Customized number of cores Returned by the lookup.
</ResponseField>

<ResponseField name="fqdn" type="string">
  Output only. Fully qualified domain name of the node. Returned by the lookup.
</ResponseField>

<ResponseField name="internal_ip" type="string">
  Output only. Internal IP address of the node. Returned by the lookup.
</ResponseField>

<ResponseField name="node_type_id" type="string">
  Output only. The canonical identifier of the node type (corresponds to the `NodeType`). For example: standard-72. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the appliance. Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  Output only. The version number of the VMware ESXi management component in this cluster. Returned by the lookup.
</ResponseField>
