---
title: "google_observability_dataset"
description: "`google_observability_dataset` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_observability_dataset` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	observability "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/observability"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_observability_dataset"})

    		ubx.Data(observability.Dataset, "example", observability.DatasetConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Dataset } from "@ubx/sdk-google/google/data/observability/dataset";

    export default stack("example", () => {
      intent({ summary: "look up google_observability_dataset" });

      data(Dataset, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.observability import Dataset, DatasetConfig

    def describe():
        ubx.intent("look up google_observability_dataset")

        ubx.data(Dataset, "example", DatasetConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. Create timestamp. Returned by the lookup.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. Delete timestamp. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the dataset. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. User friendly display name. Returned by the lookup.
</ResponseField>

<ResponseField name="purge_time" type="string">
  Output only. Timestamp when the dataset in soft-deleted state is purged. Returned by the lookup.
</ResponseField>
