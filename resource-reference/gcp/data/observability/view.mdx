---
title: "google_observability_view"
description: "`data_observability_view` looks up a real, saved view configuration within Cloud Observability."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	observability "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/observability"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_observability_view"})

    		ubx.Data(observability.View, "example", observability.ViewConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { View } from "./local-sdk/google/sdk/typescript/google/data/observability/view.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_observability_view" });

      data(View, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.observability import View, ViewConfig

    def describe():
        ubx.intent("look up google_observability_view")

        ubx.data(View, "example", ViewConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. Create timestamp. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the view. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. User friendly display name. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Update timestamp. Returned by the lookup.
</ResponseField>
