---
title: "google_redis_backup_collection"
description: "`google_redis_backup_collection` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_redis_backup_collection` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	redis "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/redis"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_redis_backup_collection"})

    		ubx.Data(redis.BackupCollection, "example", redis.BackupCollectionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupCollection } from "@ubx/sdk-google/google/data/redis/backup_collection";

    export default stack("example", () => {
      intent({ summary: "look up google_redis_backup_collection" });

      data(BackupCollection, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.redis import BackupCollection, BackupCollectionConfig

    def describe():
        ubx.intent("look up google_redis_backup_collection")

        ubx.data(BackupCollection, "example", BackupCollectionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cluster" type="string">
  Output only. The full resource path of the cluster the backup collection belongs to. Example: projects/&#123;project&#125;/locations/&#123;location&#125;/clusters/&#123;cluster&#125; Returned by the lookup.
</ResponseField>

<ResponseField name="cluster_uid" type="string">
  Output only. The cluster uid of the backup collection. Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the backup collection was created. Returned by the lookup.
</ResponseField>

<ResponseField name="kms_key" type="string">
  Output only. The KMS key used to encrypt the backups under this backup collection. Returned by the lookup.
</ResponseField>

<ResponseField name="last_backup_time" type="string">
  Output only. The last time a backup was created in the backup collection. Returned by the lookup.
</ResponseField>

<ResponseField name="total_backup_count" type="string">
  Output only. Total number of backups in the backup collection. Returned by the lookup.
</ResponseField>

<ResponseField name="total_backup_size_bytes" type="string">
  Output only. Total size of all backups in the backup collection. Returned by the lookup.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System assigned unique identifier of the backup collection. Returned by the lookup.
</ResponseField>
