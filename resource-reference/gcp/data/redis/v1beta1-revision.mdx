---
title: "google_redis_v1beta1_revision"
description: "`data_redis_v1beta1_revision` looks up a real revision of a Memorystore for Redis cluster's own configuration, via the v1beta1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	redis "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/redis"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_redis_v1beta1_revision"})

    		ubx.Data(redis.V1beta1Revision, "example", redis.V1beta1RevisionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1beta1Revision } from "@ubx/sdk-google/google/data/redis/v1beta1_revision";

    export default stack("example", () => {
      intent({ summary: "look up google_redis_v1beta1_revision" });

      data(V1beta1Revision, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.redis import V1beta1Revision, V1beta1RevisionConfig

    def describe():
        ubx.intent("look up google_redis_v1beta1_revision")

        ubx.data(V1beta1Revision, "example", V1beta1RevisionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attached_clusters" type="list(string)">
  Output only. A list of clusters that are attached to this ACL policy revision. Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp that the revision was created. Returned by the lookup.
</ResponseField>

<ResponseField name="revision_number" type="string">
  Output only. The revision number of the ACL policy revision. Returned by the lookup.
</ResponseField>

<ResponseField name="snapshot" type="object">
  The ACL policy resource. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cluster_acl_policy_attachments" type="list(object)">
      Output only. The ACL policy attachment status for each attached cluster. Returned by the lookup.
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. The timestamp that the ACL policy was created. Returned by the lookup.
    </ResponseField>
    <ResponseField name="etag" type="string">
      Output only. Etag for the ACL policy. Returned by the lookup.
    </ResponseField>
    <ResponseField name="name" type="string">
      Identifier. Full resource path of the ACL policy. Optional lookup argument.
    </ResponseField>
    <ResponseField name="rules" type="list(object)">
      Required. The ACL rules within the ACL policy. Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The state of the ACL policy. Returned by the lookup.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. The timestamp that the ACL policy was last updated. Returned by the lookup.
    </ResponseField>
    <ResponseField name="version" type="string">
      Output only. Deprecated: Used in drift resolution. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
