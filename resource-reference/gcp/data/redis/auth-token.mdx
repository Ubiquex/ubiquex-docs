---
title: "google_redis_auth_token"
description: "`data_redis_auth_token` looks up a real, generated authentication token for a Memorystore for Redis instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	redis "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/redis"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_redis_auth_token"})

    		ubx.Data(redis.AuthToken, "example", redis.AuthTokenConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AuthToken } from "@ubx/sdk-google/google/data/redis/auth_token";

    export default stack("example", () => {
      intent({ summary: "look up google_redis_auth_token" });

      data(AuthToken, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.redis import AuthToken, AuthTokenConfig

    def describe():
        ubx.intent("look up google_redis_auth_token")

        ubx.data(AuthToken, "example", AuthTokenConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. Create time of the auth token. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the auth token. Returned by the lookup.
</ResponseField>

<ResponseField name="token" type="string">
  Output only. The service generated authentication token used to connect to the Redis cluster. Returned by the lookup.
</ResponseField>
