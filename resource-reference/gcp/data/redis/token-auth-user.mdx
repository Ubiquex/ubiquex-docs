---
title: "google_redis_token_auth_user"
description: "`data_redis_token_auth_user` looks up a real, token-authenticated user configured on a Memorystore for Redis instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	redis "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/redis"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_redis_token_auth_user"})

    		ubx.Data(redis.TokenAuthUser, "example", redis.TokenAuthUserConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TokenAuthUser } from "@ubx/sdk-google/google/data/redis/token_auth_user";

    export default stack("example", () => {
      intent({ summary: "look up google_redis_token_auth_user" });

      data(TokenAuthUser, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.redis import TokenAuthUser, TokenAuthUserConfig

    def describe():
        ubx.intent("look up google_redis_token_auth_user")

        ubx.data(TokenAuthUser, "example", TokenAuthUserConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="state" type="string">
  Output only. The state of the token based auth user. Returned by the lookup.
</ResponseField>
