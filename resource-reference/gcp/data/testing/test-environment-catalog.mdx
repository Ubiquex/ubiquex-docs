---
title: "google_testing_test_environment_catalog"
description: "`google_testing_test_environment_catalog` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_testing_test_environment_catalog` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	testing "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/testing"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_testing_test_environment_catalog"})

    		ubx.Data(testing.TestEnvironmentCatalog, "example", testing.TestEnvironmentCatalogConfig{
    			EnvironmentType:       "example",
    			IncludeViewableModels: true,
    			ProjectId:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestEnvironmentCatalog } from "@ubx/sdk-google/google/data/testing/test_environment_catalog";

    export default stack("example", () => {
      intent({ summary: "look up google_testing_test_environment_catalog" });

      data(TestEnvironmentCatalog, "example", {
        environmentType: "example",
        includeViewableModels: true,
        projectId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.testing import TestEnvironmentCatalog, TestEnvironmentCatalogConfig

    def describe():
        ubx.intent("look up google_testing_test_environment_catalog")

        ubx.data(TestEnvironmentCatalog, "example", TestEnvironmentCatalogConfig(
            environment_type="example",
            include_viewable_models=True,
            project_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="include_viewable_models" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="project_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="android_device_catalog" type="object">
  The currently supported Android devices. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="models" type="list(object)">
      The set of supported Android device models. Optional lookup argument.
    </ResponseField>
    <ResponseField name="runtime_configuration" type="object">
      Android configuration that can be selected at the time a test is run. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="locales" type="list(object)">
          The set of available locales. Optional lookup argument.
        </ResponseField>
        <ResponseField name="orientations" type="list(object)">
          The set of available orientations. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="versions" type="list(object)">
      The set of supported Android OS versions. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="device_ip_block_catalog" type="object">
  List of IP blocks used by the Firebase Test Lab Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="ip_blocks" type="list(object)">
      The device IP blocks used by Firebase Test Lab Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ios_device_catalog" type="object">
  The currently supported iOS devices. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="models" type="list(object)">
      The set of supported iOS device models. Optional lookup argument.
    </ResponseField>
    <ResponseField name="runtime_configuration" type="object">
      iOS configuration that can be selected at the time a test is run. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="locales" type="list(object)">
          The set of available locales. Optional lookup argument.
        </ResponseField>
        <ResponseField name="orientations" type="list(object)">
          The set of available orientations. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="versions" type="list(object)">
      The set of supported iOS software versions. Optional lookup argument.
    </ResponseField>
    <ResponseField name="xcode_versions" type="list(object)">
      The set of supported Xcode versions. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network_configuration_catalog" type="object">
  Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="configurations" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="software_catalog" type="object">
  The currently provided software environment on the devices under test. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="androidx_orchestrator_version" type="string">
      A string representing the current version of AndroidX Test Orchestrator that is used in the environment. The package is available at https://maven.google.com/web/index.html#androidx.test:orchestrator. Optional lookup argument.
    </ResponseField>
    <ResponseField name="orchestrator_version" type="string">
      Deprecated: Use AndroidX Test Orchestrator going forward. A string representing the current version of Android Test Orchestrator that is used in the environment. The package is available at https://maven.google.com/web/index.html#com.android.support.test:orchestrator. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
