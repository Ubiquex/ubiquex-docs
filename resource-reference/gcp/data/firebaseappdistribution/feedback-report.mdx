---
title: "google_firebaseappdistribution_feedback_report"
description: "`data_firebaseappdistribution_feedback_report` looks up a real tester feedback report submitted through Firebase App Distribution."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseappdistribution "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/firebaseappdistribution"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_firebaseappdistribution_feedback_report"})

    		ubx.Data(firebaseappdistribution.FeedbackReport, "example", firebaseappdistribution.FeedbackReportConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { FeedbackReport } from "./local-sdk/google/sdk/typescript/google/data/firebaseappdistribution/feedback_report.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_firebaseappdistribution_feedback_report" });

      data(FeedbackReport, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.firebaseappdistribution import FeedbackReport, FeedbackReportConfig

    def describe():
        ubx.intent("look up google_firebaseappdistribution_feedback_report")

        ubx.data(FeedbackReport, "example", FeedbackReportConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. The time when the feedback report was created. Returned by the lookup.
</ResponseField>

<ResponseField name="firebase_console_uri" type="string">
  Output only. A link to the Firebase console displaying the feedback report. Returned by the lookup.
</ResponseField>

<ResponseField name="screenshot_uri" type="string">
  Output only. A signed link (which expires in one hour) that lets you directly download the screenshot. Returned by the lookup.
</ResponseField>

<ResponseField name="tester" type="string">
  Output only. The resource name of the tester who submitted the feedback report. Returned by the lookup.
</ResponseField>

<ResponseField name="text" type="string">
  Output only. The text of the feedback report. Returned by the lookup.
</ResponseField>
