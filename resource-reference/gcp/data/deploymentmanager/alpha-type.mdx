---
title: "google_deploymentmanager_alpha_type"
description: "`google_deploymentmanager_alpha_type` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_deploymentmanager_alpha_type` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	deploymentmanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/deploymentmanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_deploymentmanager_alpha_type"})

    		ubx.Data(deploymentmanager.AlphaType, "example", deploymentmanager.AlphaTypeConfig{
    			Project:                   "example",
    			Type:                      "example",
    			HeaderBypassBillingFilter: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlphaType } from "@ubx/sdk-google/google/data/deploymentmanager/alpha_type";

    export default stack("example", () => {
      intent({ summary: "look up google_deploymentmanager_alpha_type" });

      data(AlphaType, "example", {
        project: "example",
        type: "example",
        headerBypassBillingFilter: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.deploymentmanager import AlphaType, AlphaTypeConfig

    def describe():
        ubx.intent("look up google_deploymentmanager_alpha_type")

        ubx.data(AlphaType, "example", AlphaTypeConfig(
            project="example",
            type="example",
            header_bypass_billing_filter=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="header_bypass_billing_filter" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="project" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="base" type="object">
  BaseType that describes a service-backed Type. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="collection_overrides" type="list(object)">
      Allows resource handling overrides for specific collections Optional lookup argument.
    </ResponseField>
    <ResponseField name="credential" type="object">
      The credential used by Deployment Manager and TypeProvider. Only one of the options is permitted. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="basic_auth" type="object">
          Basic Auth used as a credential. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="password" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="user" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_account" type="object">
          Service Account used as a credential. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="email" type="string">
              The IAM service account email address like test@myproject.iam.gserviceaccount.com Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="use_project_default" type="bool">
          Specify to use the project default credential, only supported by Deployment. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="descriptor_url" type="string">
      Descriptor Url for the this type. Optional lookup argument.
    </ResponseField>
    <ResponseField name="options" type="object">
      Options allows customized resource handling by Deployment Manager. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="async_options" type="list(object)">
          Options regarding how to thread async requests. Optional lookup argument.
        </ResponseField>
        <ResponseField name="input_mappings" type="list(object)">
          The mappings that apply for requests. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name_property" type="string">
          The json path to the field in the resource JSON body into which the resource name should be mapped. Leaving this empty indicates that there should be no mapping performed. Optional lookup argument.
        </ResponseField>
        <ResponseField name="validation_options" type="object">
          Options for how to validate and process properties on a resource. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="schema_validation" type="string">
              Customize how deployment manager will validate the resource against schema errors. Optional lookup argument.
            </ResponseField>
            <ResponseField name="undeclared_properties" type="string">
              Specify what to do with extra properties when executing a request. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional textual description of the resource; provided by the client when the resource is created. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="insert_time" type="string">
  Output only. Creation timestamp in RFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="labels" type="list(object)">
  Map of One Platform labels; provided by the client when the resource is created or updated. Specifically: Label keys must be between 1 and 63 characters long and must conform to the following regular expression: `[a-z]([-a-z0-9]*[a-z0-9])?` Label values must be between 0 and 63 characters long and must conform to the regular expression `([a-z]([-a-z0-9]*[a-z0-9])?)?`. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the type. Returned by the lookup.
</ResponseField>

<ResponseField name="operation" type="object">
  Represents an Operation resource. Google Compute Engine has three Operation resources: * [Global](/compute/docs/reference/rest/&#123;$api_version&#125;/globalOperations) * [Regional](/compute/docs/reference/rest/&#123;$api_version&#125;/regionOperations) * [Zonal](/compute/docs/reference/rest/&#123;$api_version&#125;/zoneOperations) You can use an operation resource to manage asynchronous API requests. For more information, read Handling API responses. Operations can be global, regional or zonal. - For global operations, use the `globalOperations` resource. - For regional operations, use the `regionOperations` resource. - For zonal operations, use the `zoneOperations` resource. For more information, read Global, Regional, and Zonal Resources. Note that completed Operation resources have a limited retention period. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="client_operation_id" type="string">
      [Output Only] The value of `requestId` if you provided it in the request. Not present otherwise. Optional lookup argument.
    </ResponseField>
    <ResponseField name="creation_timestamp" type="string">
      [Deprecated] This field is deprecated. Optional lookup argument.
    </ResponseField>
    <ResponseField name="description" type="string">
      [Output Only] A textual description of the operation, which is set when the operation is created. Optional lookup argument.
    </ResponseField>
    <ResponseField name="end_time" type="string">
      [Output Only] The time that this operation was completed. This value is in RFC3339 text format. Optional lookup argument.
    </ResponseField>
    <ResponseField name="error" type="object">
      [Output Only] If errors are generated during processing of the operation, this field will be populated. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="errors" type="list(object)">
          [Output Only] The array of errors encountered while processing this operation. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="firewall_policy_rule_operation_metadata" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="allocated_priority" type="number">
          The priority allocated for the firewall policy rule if query parameters specified minPriority/maxPriority. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="get_version_operation_metadata" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="inline_sbom_info" type="object">
          Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="current_component_versions" type="map(string)">
              SBOM versions currently applied to the resource. The key is the component name and the value is the version. Optional lookup argument.
            </ResponseField>
            <ResponseField name="target_component_versions" type="map(string)">
              SBOM versions scheduled for the next maintenance. The key is the component name and the value is the version. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="http_error_message" type="string">
      [Output Only] If the operation fails, this field contains the HTTP error message that was returned, such as `NOT FOUND`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="http_error_status_code" type="number">
      [Output Only] If the operation fails, this field contains the HTTP error status code that was returned. For example, a `404` means the resource was not found. Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      [Output Only] The unique identifier for the operation. This identifier is defined by the server. Optional lookup argument.
    </ResponseField>
    <ResponseField name="insert_time" type="string">
      [Output Only] The time that this operation was requested. This value is in RFC3339 text format. Optional lookup argument.
    </ResponseField>
    <ResponseField name="instances_bulk_insert_operation_metadata" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="machine_type" type="string">
          [Output Only] The machine type of the VMs that were created used internally only by KCP flex bulk insert. Optional lookup argument.
        </ResponseField>
        <ResponseField name="per_location_status" type="map(object)">
          Status information per location (location name is key). Example key: zones/us-central1-a Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kind" type="string">
      Output only. [Output Only] Type of the resource. Always `compute#operation` for Operation resources. Returned by the lookup.
    </ResponseField>
    <ResponseField name="name" type="string">
      [Output Only] Name of the operation. Optional lookup argument.
    </ResponseField>
    <ResponseField name="operation_group_id" type="string">
      Output only. [Output Only] An ID that represents a group of operations, such as when a group of operations results from a `bulkInsert` API request. Returned by the lookup.
    </ResponseField>
    <ResponseField name="operation_type" type="string">
      [Output Only] The type of operation, such as `insert`, `update`, or `delete`, and so on. Optional lookup argument.
    </ResponseField>
    <ResponseField name="progress" type="number">
      [Output Only] An optional progress indicator that ranges from 0 to 100. There is no requirement that this be linear or support any granularity of operations. This should not be used to guess when the operation will be complete. This number should monotonically increase as the operation progresses. Optional lookup argument.
    </ResponseField>
    <ResponseField name="region" type="string">
      [Output Only] The URL of the region where the operation resides. Only applicable when performing regional operations. Optional lookup argument.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      [Output Only] Server-defined URL for the resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="self_link_with_id" type="string">
      Output only. [Output Only] Server-defined URL for this resource with the resource id. Returned by the lookup.
    </ResponseField>
    <ResponseField name="set_autoscaler_link_operation_metadata" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="zonal_igm_ids" type="list(string)">
          List of zonal IGM IDs part of the RMIG. Optional lookup argument.
        </ResponseField>
        <ResponseField name="zone_to_igm_ids" type="map(string)">
          Map of zone to an ID of the zonal IGM belonging to the RMIG. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="set_common_instance_metadata_operation_metadata" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="client_operation_id" type="string">
          [Output Only] The client operation id. Optional lookup argument.
        </ResponseField>
        <ResponseField name="per_location_operations" type="map(object)">
          [Output Only] Status information per location (location name is key). Example key: zones/us-central1-a Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="start_time" type="string">
      [Output Only] The time that this operation was started by the server. This value is in RFC3339 text format. Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      [Output Only] The status of the operation, which can be one of the following: `PENDING`, `RUNNING`, or `DONE`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="status_message" type="string">
      [Output Only] An optional textual description of the current status of the operation. Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_id" type="string">
      [Output Only] The unique target ID, which identifies a specific incarnation of the target resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_link" type="string">
      [Output Only] The URL of the resource that the operation modifies. For operations related to creating a snapshot, this points to the disk that the snapshot was created from. Optional lookup argument.
    </ResponseField>
    <ResponseField name="user" type="string">
      [Output Only] User who requested the operation, for example: `user@example.com` or `alice_smith_identifier (global/workforcePools/example-com-us-employees)`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="warnings" type="list(object)">
      [Output Only] If warning messages are generated during processing of the operation, this field will be populated. Optional lookup argument.
    </ResponseField>
    <ResponseField name="zone" type="string">
      [Output Only] The URL of the zone where the operation resides. Only applicable when performing per-zone operations. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. Server defined URL for the resource. Returned by the lookup.
</ResponseField>
