---
title: "google_deploymentmanager_alpha_resource"
description: "`google_deploymentmanager_alpha_resource` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_deploymentmanager_alpha_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	deploymentmanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/deploymentmanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_deploymentmanager_alpha_resource"})

    		ubx.Data(deploymentmanager.AlphaResource, "example", deploymentmanager.AlphaResourceConfig{
    			Deployment:                "example",
    			Project:                   "example",
    			Resource:                  "example",
    			HeaderBypassBillingFilter: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlphaResource } from "@ubx/sdk-google/google/data/deploymentmanager/alpha_resource";

    export default stack("example", () => {
      intent({ summary: "look up google_deploymentmanager_alpha_resource" });

      data(AlphaResource, "example", {
        deployment: "example",
        project: "example",
        resource: "example",
        headerBypassBillingFilter: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.deploymentmanager import AlphaResource, AlphaResourceConfig

    def describe():
        ubx.intent("look up google_deploymentmanager_alpha_resource")

        ubx.data(AlphaResource, "example", AlphaResourceConfig(
            deployment="example",
            project="example",
            resource="example",
            header_bypass_billing_filter=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="deployment" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="header_bypass_billing_filter" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="project" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_control" type="object">
  The access controls set on the resource. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="gcp_iam_policy" type="string">
      The GCP IAM Policy to set on the resource. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="final_properties" type="string">
  Output only. The evaluated properties of the resource with references expanded. Returned as serialized YAML. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="insert_time" type="string">
  Output only. Creation timestamp in RFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="last_used_credential" type="object">
  The credential used by Deployment Manager and TypeProvider. Only one of the options is permitted. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="basic_auth" type="object">
      Basic Auth used as a credential. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="password" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="user" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_account" type="object">
      Service Account used as a credential. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          The IAM service account email address like test@myproject.iam.gserviceaccount.com Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="use_project_default" type="bool">
      Specify to use the project default credential, only supported by Deployment. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="manifest" type="string">
  Output only. URL of the manifest representing the current configuration of this resource. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The name of the resource as it appears in the YAML config. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="string">
  Output only. The current properties of the resource before any references have been filled in. Returned as serialized YAML. Returned by the lookup.
</ResponseField>

<ResponseField name="runtime_policies" type="list(string)">
  Output only. In case this is an action, it will show the runtimePolicies on which this action will run in the deployment Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  Output only. The type of the resource, for example `compute.v1.instance`, or `cloudfunctions.v1beta1.function`. Returned by the lookup.
</ResponseField>

<ResponseField name="update" type="object">
  Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="access_control" type="object">
      The access controls set on the resource. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="gcp_iam_policy" type="string">
          The GCP IAM Policy to set on the resource. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="credential" type="object">
      The credential used by Deployment Manager and TypeProvider. Only one of the options is permitted. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="basic_auth" type="object">
          Basic Auth used as a credential. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="password" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="user" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_account" type="object">
          Service Account used as a credential. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="email" type="string">
              The IAM service account email address like test@myproject.iam.gserviceaccount.com Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="use_project_default" type="bool">
          Specify to use the project default credential, only supported by Deployment. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error" type="object">
      Output only. If errors are generated during update of the resource, this field will be populated. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="errors" type="list(object)">
          [Output Only] The array of errors encountered while processing this operation. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="final_properties" type="string">
      Output only. The expanded properties of the resource with reference values expanded. Returned as serialized YAML. Optional lookup argument.
    </ResponseField>
    <ResponseField name="intent" type="string">
      Output only. The intent of the resource: `PREVIEW`, `UPDATE`, or `CANCEL`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="manifest" type="string">
      Output only. URL of the manifest representing the update configuration of this resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="properties" type="string">
      Output only. The set of updated properties for this resource, before references are expanded. Returned as serialized YAML. Optional lookup argument.
    </ResponseField>
    <ResponseField name="runtime_policies" type="list(string)">
      Output only. In case this is an action, it will show the runtimePolicies that this action will have after updating the deployment. Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The state of the resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="warnings" type="list(object)">
      Output only. If warning messages are generated during processing of this resource, this field will be populated. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Update timestamp in RFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  Output only. The URL of the actual resource. Returned by the lookup.
</ResponseField>

<ResponseField name="warnings" type="list(object)">
  Output only. If warning messages are generated during processing of this resource, this field will be populated. Returned by the lookup.
</ResponseField>
