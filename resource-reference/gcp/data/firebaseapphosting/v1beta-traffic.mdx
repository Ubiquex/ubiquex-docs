---
title: "google_firebaseapphosting_v1beta_traffic"
description: "`data_firebaseapphosting_v1beta_traffic` looks up the real, current traffic split for a Firebase App Hosting backend, via the v1beta API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseapphosting "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/firebaseapphosting"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_firebaseapphosting_v1beta_traffic"})

    		ubx.Data(firebaseapphosting.V1betaTraffic, "example", firebaseapphosting.V1betaTrafficConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1betaTraffic } from "@ubx/sdk-google/google/data/firebaseapphosting/v1beta_traffic";

    export default stack("example", () => {
      intent({ summary: "look up google_firebaseapphosting_v1beta_traffic" });

      data(V1betaTraffic, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.firebaseapphosting import V1betaTraffic, V1betaTrafficConfig

    def describe():
        ubx.intent("look up google_firebaseapphosting_v1beta_traffic")

        ubx.data(V1betaTraffic, "example", V1betaTrafficConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="annotations" type="map(string)">
  Optional. Unstructured key value map that may be set by external tools to store and arbitrary metadata. They are not queryable and should be preserved when modifying objects. Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Time at which the backend was created. Returned by the lookup.
</ResponseField>

<ResponseField name="current" type="object">
  A list of traffic splits that together represent where traffic is being routed. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="splits" type="list(object)">
      Required. The list of traffic splits. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. Server-computed checksum based on other values; may be sent on update or delete to ensure operation is done on expected resource. Returned by the lookup.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Unstructured key value map that can be used to organize and categorize objects. Returned by the lookup.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. A field that, if true, indicates that the system is working to make the backend's `current` match the requested `target` list. Returned by the lookup.
</ResponseField>

<ResponseField name="rollout_policy" type="object">
  The policy for how automatic builds and rollouts are triggered and rolled out. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="codebase_branch" type="string">
      If set, specifies a branch that triggers a new build to be started with this policy. Otherwise, no automatic rollouts will happen. Optional lookup argument.
    </ResponseField>
    <ResponseField name="disabled" type="bool">
      Optional. A flag that, if true, prevents automatic rollouts from being created via this RolloutPolicy. Optional lookup argument.
    </ResponseField>
    <ResponseField name="disabled_time" type="string">
      Output only. If `disabled` is set, the time at which the automatic rollouts were disabled. Returned by the lookup.
    </ResponseField>
    <ResponseField name="ignored_paths" type="list(object)">
      Optional. A list of file paths patterns to exclude from triggering a rollout. Patterns in this list take precedence over required_paths. **Note**: All paths must be in the ignored_paths in order for the rollout to be skipped. Limited to 100 paths. Example: ``` ignored_paths: &#123; pattern: "foo/bar/excluded/*", type: "GLOB" &#125; ``` Optional lookup argument.
    </ResponseField>
    <ResponseField name="required_paths" type="list(object)">
      Optional. A list of file paths patterns that trigger a build and rollout if at least one of the changed files in the commit are present in this list. This field is optional; the rollout policy will default to triggering on all paths if both ignored_paths and required_paths are not populated. Limited to 100 paths. Example: ``` required_paths: &#123; pattern: "foo/bar/*", type: "GLOB" &#125; ``` Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target" type="object">
  A list of traffic splits that together represent where traffic is being routed. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="splits" type="list(object)">
      Required. The list of traffic splits. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System-assigned, unique identifier. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time at which the backend was last updated. Returned by the lookup.
</ResponseField>
