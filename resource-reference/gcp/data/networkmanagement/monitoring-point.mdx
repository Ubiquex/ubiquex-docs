---
title: "google_networkmanagement_monitoring_point"
description: "`google_networkmanagement_monitoring_point` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_networkmanagement_monitoring_point` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networkmanagement "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/networkmanagement"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_networkmanagement_monitoring_point"})

    		ubx.Data(networkmanagement.MonitoringPoint, "example", networkmanagement.MonitoringPointConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MonitoringPoint } from "@ubx/sdk-google/google/data/networkmanagement/monitoring_point";

    export default stack("example", () => {
      intent({ summary: "look up google_networkmanagement_monitoring_point" });

      data(MonitoringPoint, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.networkmanagement import MonitoringPoint, MonitoringPointConfig

    def describe():
        ubx.intent("look up google_networkmanagement_monitoring_point")

        ubx.data(MonitoringPoint, "example", MonitoringPointConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auto_geo_location_enabled" type="bool">
  Output only. Indicates if automaitic geographic location is enabled for the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="connection_status" type="string">
  Output only. Connection status of the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time the MonitoringPoint was created. Returned by the lookup.
</ResponseField>

<ResponseField name="deployment_type" type="string">
  Output only. The deployment type of the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  Output only. Display name of the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="errors" type="list(string)">
  Output only. The codes of errors detected in the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="geo_location" type="object">
  The geographical location of the MonitoringPoint. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="formatted_address" type="string">
      Formatted address. Optional lookup argument.
    </ResponseField>
    <ResponseField name="region_code" type="string">
      Unicode CLDR region code. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="guid" type="string">
  Output only. The GUID of the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="host" type="object">
  Message describing information about the host. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cloud_instance_id" type="string">
      Output only. The cloud instance id of the host. Returned by the lookup.
    </ResponseField>
    <ResponseField name="cloud_project_id" type="string">
      Output only. The cloud project id of the host. Returned by the lookup.
    </ResponseField>
    <ResponseField name="cloud_provider" type="string">
      Output only. The cloud provider of the host. Returned by the lookup.
    </ResponseField>
    <ResponseField name="cloud_region" type="string">
      Output only. The cloud region of the host. Returned by the lookup.
    </ResponseField>
    <ResponseField name="cloud_virtual_network_ids" type="list(string)">
      Output only. The ids of cloud virtual networks of the host. Returned by the lookup.
    </ResponseField>
    <ResponseField name="cloud_zone" type="string">
      Output only. The cloud zone of the host. Returned by the lookup.
    </ResponseField>
    <ResponseField name="os" type="string">
      Output only. The operating system of the host. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="hostname" type="string">
  Output only. The hostname of the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="network_interfaces" type="list(object)">
  Output only. The network interfaces of the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="originating_ip" type="string">
  Output only. IP address visible when MonitoringPoint connects to the provider. Returned by the lookup.
</ResponseField>

<ResponseField name="provider_tags" type="list(object)">
  Output only. The provider tags of the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  Output only. Deployment type of the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time the MonitoringPoint was updated. Returned by the lookup.
</ResponseField>

<ResponseField name="upgrade_available" type="bool">
  Output only. Indicates if an upgrade is available for the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="upgrade_type" type="string">
  Output only. The type of upgrade available for the MonitoringPoint. Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  Output only. Version of the software running on the MonitoringPoint. Returned by the lookup.
</ResponseField>
