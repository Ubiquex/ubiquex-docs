---
title: "google_compute_beta_machine_type"
description: "`data_compute_beta_machine_type` looks up a real, available Compute Engine machine type, via the Beta API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_beta_machine_type"})

    		ubx.Data(compute.BetaMachineType, "example", compute.BetaMachineTypeConfig{
    			MachineType: "example",
    			Project:     "example",
    			Zone:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { BetaMachineType } from "./local-sdk/google/sdk/typescript/google/data/compute/beta_machine_type.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_beta_machine_type" });

      data(BetaMachineType, "example", {
        machineType: "example",
        project: "example",
        zone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.compute import BetaMachineType, BetaMachineTypeConfig

    def describe():
        ubx.intent("look up google_compute_beta_machine_type")

        ubx.data(BetaMachineType, "example", BetaMachineTypeConfig(
            machine_type="example",
            project="example",
            zone="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="machine_type" type="string" required>
  The real Compute Engine machine type this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="project" type="string" required>
  The real Google Cloud project ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="zone" type="string" required>
  The real Google Cloud zone this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accelerators" type="list(object)">
  [Output Only] A list of accelerator configurations assigned to this machine type. Returned by the lookup.
</ResponseField>

<ResponseField name="architecture" type="string">
  [Output Only] The architecture of the machine type. Returned by the lookup.
</ResponseField>

<ResponseField name="bundled_local_ssds" type="object">
  The real, bundled local SSD configuration for this machine type. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="default_interface" type="string">
      The default disk interface if the interface is not specified. Optional lookup argument.
    </ResponseField>
    <ResponseField name="partition_count" type="number">
      The number of partitions. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  [Output Only] Creation timestamp inRFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="deprecated" type="object">
  Deprecation status for a public resource. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="deleted" type="string">
      An optional RFC3339 timestamp on or after which the state of this resource is intended to change to DELETED. This is only informational and the status will not change unless the client explicitly changes it. Optional lookup argument.
    </ResponseField>
    <ResponseField name="deprecated" type="string">
      An optional RFC3339 timestamp on or after which the state of this resource is intended to change to DEPRECATED. This is only informational and the status will not change unless the client explicitly changes it. Optional lookup argument.
    </ResponseField>
    <ResponseField name="obsolete" type="string">
      An optional RFC3339 timestamp on or after which the state of this resource is intended to change to OBSOLETE. This is only informational and the status will not change unless the client explicitly changes it. Optional lookup argument.
    </ResponseField>
    <ResponseField name="replacement" type="string">
      The URL of the suggested replacement for a deprecated resource. The suggested replacement resource must be the same kind of resource as the deprecated resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      The deprecation state of this resource. This can be ACTIVE,DEPRECATED, OBSOLETE, or DELETED. Operations which communicate the end of life date for an image, can useACTIVE. Operations which create a new resource using aDEPRECATED resource will return successfully, but with a warning indicating the deprecated resource and recommending its replacement. Operations which use OBSOLETE orDELETED resources will be rejected and result in an error. Optional lookup argument.
    </ResponseField>
    <ResponseField name="state_override" type="object">
      A rollout policy configuration. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="default_rollout_time" type="string">
          An optional RFC3339 timestamp on or after which the update is considered rolled out to any zone that is not explicitly stated. Optional lookup argument.
        </ResponseField>
        <ResponseField name="location_rollout_policies" type="map(string)">
          Location based rollout policies to apply to the resource. Currently only zone names are supported and must be represented as valid URLs, like: zones/us-central1-a. The value expects an RFC3339 timestamp on or after which the update is considered rolled out to the specified location. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  [Output Only] An optional textual description of the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="guest_cpus" type="number">
  [Output Only] The number of virtual CPUs that are available to the instance. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  [Output Only] The unique identifier for the resource. This identifier is defined by the server. Returned by the lookup.
</ResponseField>

<ResponseField name="is_shared_cpu" type="bool">
  [Output Only] Whether this machine type has a shared CPU. SeeShared-core machine types for more information. Returned by the lookup.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] The type of the resource. Alwayscompute#machineType for machine types. Returned by the lookup.
</ResponseField>

<ResponseField name="maximum_persistent_disks" type="number">
  [Output Only] Maximum persistent disks allowed. Returned by the lookup.
</ResponseField>

<ResponseField name="maximum_persistent_disks_size_gb" type="string">
  [Output Only] Maximum total persistent disks size (GB) allowed. Returned by the lookup.
</ResponseField>

<ResponseField name="memory_mb" type="number">
  [Output Only] The amount of physical memory available to the instance, defined in MB. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  [Output Only] Name of the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="self_link" type="string">
  [Output Only] Server-defined URL for the resource. Returned by the lookup.
</ResponseField>
