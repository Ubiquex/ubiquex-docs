---
title: "google_compute_reliability_risk"
description: "`google_compute_reliability_risk` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_compute_reliability_risk` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_reliability_risk"})

    		ubx.Data(compute.ReliabilityRisk, "example", compute.ReliabilityRiskConfig{
    			Project:         "example",
    			ReliabilityRisk: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReliabilityRisk } from "@ubx/sdk-google/google/data/compute/reliability_risk";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_reliability_risk" });

      data(ReliabilityRisk, "example", {
        project: "example",
        reliabilityRisk: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.compute import ReliabilityRisk, ReliabilityRiskConfig

    def describe():
        ubx.intent("look up google_compute_reliability_risk")

        ubx.data(ReliabilityRisk, "example", ReliabilityRiskConfig(
            project="example",
            reliability_risk="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="project" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="reliability_risk" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp in RFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  An optional textual description of the resource; provided when the resource is created. Returned by the lookup.
</ResponseField>

<ResponseField name="details" type="object">
  Detailed insights and metrics about a detected reliability risk. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="duration" type="string">
      The duration of the risk since it was detected. Optional lookup argument.
    </ResponseField>
    <ResponseField name="global_dns_insight" type="object">
      Detailed insights for a global DNS reliability risk. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="project_default_is_global_dns" type="bool">
          Indicates whether the project's default DNS setting is global DNS. Optional lookup argument.
        </ResponseField>
        <ResponseField name="query_observation_window" type="string">
          The observation window for the query counts. Optional lookup argument.
        </ResponseField>
        <ResponseField name="risky_query_count" type="string">
          The number of queries that are risky. This is always less than or equal to total_query_count. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_query_count" type="string">
          The total number of queries in the observation window. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_update_timestamp" type="string">
      The last time the risk was updated. Optional lookup argument.
    </ResponseField>
    <ResponseField name="severity" type="string">
      The severity of the risk. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of risk. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  [Output Only] The unique identifier for the resource. This identifier is defined by the server. Returned by the lookup.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of resource. Always compute#reliabilityRisk for reliability risks. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. The name must be 1-63 characters long and comply with RFC1035. Returned by the lookup.
</ResponseField>

<ResponseField name="recommendation" type="object">
  Recommendation for mitigating a reliability risk, including a reference URL. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="content" type="string">
      Mitigation guide for the risk. Optional lookup argument.
    </ResponseField>
    <ResponseField name="reference_url" type="string">
      URL referencing a more detailed mitigation guide. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource with the resource id. Returned by the lookup.
</ResponseField>
