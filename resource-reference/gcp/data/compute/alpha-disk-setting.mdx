---
title: "google_compute_alpha_disk_setting"
description: "`google_compute_alpha_disk_setting` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_compute_alpha_disk_setting` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_alpha_disk_setting"})

    		ubx.Data(compute.AlphaDiskSetting, "example", compute.AlphaDiskSettingConfig{
    			Project: "example",
    			Zone:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlphaDiskSetting } from "@ubx/sdk-google/google/data/compute/alpha_disk_setting";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_alpha_disk_setting" });

      data(AlphaDiskSetting, "example", {
        project: "example",
        zone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.compute import AlphaDiskSetting, AlphaDiskSettingConfig

    def describe():
        ubx.intent("look up google_compute_alpha_disk_setting")

        ubx.data(AlphaDiskSetting, "example", AlphaDiskSettingConfig(
            project="example",
            zone="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="project" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="zone" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_location" type="object">
  AccessLocation is only used for regional snapshot. It contains which regions are allowed to create a regional snapshot from disks located in the given region/zone. It includes key-value pairs designed to store the following structure. The keys should match their corresponding values, which must be provided: access_location: &#123; locations &#123; us-central1 &#123; region: "us-central1" &#125;, asia-west2 &#123; region: "asia-west2" &#125; &#125; &#125; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="locations" type="map(object)">
      List of regions that can create a regional snapshot from the current region Optional lookup argument.
    </ResponseField>
    <ResponseField name="policy" type="string">
      Policy of which location is allowed to access snapshot. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_resource_policies" type="map(object)">
  An optional parameter for storing the default resource policies that will be used for the Disks created in the given scope. The Key is a string type, provided by customers to uniquely identify the default Resource Policy entry. The Value is a Default ResourcePolicyDetails Object used to represent the detailed information of the Resource Policy entry. Returned by the lookup.
</ResponseField>
