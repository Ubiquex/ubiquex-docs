---
title: "google_compute_alpha_reservation_block"
description: "`google_compute_alpha_reservation_block` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_compute_alpha_reservation_block` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_alpha_reservation_block"})

    		ubx.Data(compute.AlphaReservationBlock, "example", compute.AlphaReservationBlockConfig{
    			Project:          "example",
    			Reservation:      "example",
    			ReservationBlock: "example",
    			Zone:             "example",
    			View:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlphaReservationBlock } from "@ubx/sdk-google/google/data/compute/alpha_reservation_block";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_alpha_reservation_block" });

      data(AlphaReservationBlock, "example", {
        project: "example",
        reservation: "example",
        reservationBlock: "example",
        zone: "example",
        view: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.compute import AlphaReservationBlock, AlphaReservationBlockConfig

    def describe():
        ubx.intent("look up google_compute_alpha_reservation_block")

        ubx.data(AlphaReservationBlock, "example", AlphaReservationBlockConfig(
            project="example",
            reservation="example",
            reservation_block="example",
            zone="example",
            view="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="project" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="reservation" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="reservation_block" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="view" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="zone" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="resource" type="object">
  Represents a reservation block resource. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="count" type="number">
      Output only. [Output Only] The number of resources that are allocated in this reservation block. Returned by the lookup.
    </ResponseField>
    <ResponseField name="creation_timestamp" type="string">
      Output only. [Output Only] Creation timestamp inRFC3339 text format. Returned by the lookup.
    </ResponseField>
    <ResponseField name="health_info" type="object">
      Health information for the reservation block. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="degraded_sub_block_count" type="number">
          The number of subBlocks that are degraded. Optional lookup argument.
        </ResponseField>
        <ResponseField name="health_status" type="string">
          The health status of the reservation block. Optional lookup argument.
        </ResponseField>
        <ResponseField name="healthy_sub_block_count" type="number">
          The number of subBlocks that are healthy. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Returned by the lookup.
    </ResponseField>
    <ResponseField name="in_use_count" type="number">
      Output only. [Output Only] The number of instances that are currently in use on this reservation block. Returned by the lookup.
    </ResponseField>
    <ResponseField name="in_use_host_count" type="number">
      Output only. Number of hosts currently in use. If there is one or more Instances running on the host, it is considered in use. Returned by the lookup.
    </ResponseField>
    <ResponseField name="kind" type="string">
      Output only. [Output Only] Type of the resource. Alwayscompute#reservationBlock for reservation blocks. Returned by the lookup.
    </ResponseField>
    <ResponseField name="location_prefix" type="string">
      Output only. [Output Only] Obfuscated location where this reservation block physically resides in format /CCCC/GGGG. This matches the tripod specified in the VM topology information. Example: /USEAB1/0101/ is the location prefix in /USEAB1/0101/031/003 Returned by the lookup.
    </ResponseField>
    <ResponseField name="name" type="string">
      Output only. [Output Only] The name of this reservation block generated by Google Compute Engine. The name must be 1-63 characters long, and comply with RFC1035 @pattern [a-z](?:[-a-z0-9]&#123;0,61&#125;[a-z0-9])? Returned by the lookup.
    </ResponseField>
    <ResponseField name="physical_topology" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="block" type="string">
          The hash of the capacity block within the cluster. Optional lookup argument.
        </ResponseField>
        <ResponseField name="cluster" type="string">
          The cluster name of the reservation block. Optional lookup argument.
        </ResponseField>
        <ResponseField name="instances" type="list(object)">
          Output only. The detailed instances information for a given Block Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reservation_maintenance" type="object">
      Maintenance Info for ReservationBlocks. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="instance_maintenance_ongoing_count" type="number">
          Describes number of instances that have ongoing maintenance. Optional lookup argument.
        </ResponseField>
        <ResponseField name="instance_maintenance_pending_count" type="number">
          Describes number of instances that have pending maintenance. Optional lookup argument.
        </ResponseField>
        <ResponseField name="maintenance_ongoing_count" type="number">
          Progress for ongoing maintenance for this group of VMs/hosts. Describes number of hosts in the block that have ongoing maintenance. Optional lookup argument.
        </ResponseField>
        <ResponseField name="maintenance_pending_count" type="number">
          Progress for ongoing maintenance for this group of VMs/hosts. Describes number of hosts in the block that have pending maintenance. Optional lookup argument.
        </ResponseField>
        <ResponseField name="scheduling_type" type="string">
          The type of maintenance for the reservation. Optional lookup argument.
        </ResponseField>
        <ResponseField name="subblock_infra_maintenance_ongoing_count" type="number">
          Describes number of subblock Infrastructure that has ongoing maintenance. Here, Subblock Infrastructure Maintenance pertains to upstream hardware contained in the Subblock that is necessary for a VM Family(e.g. NVLink Domains). Not all VM Families will support this field. Optional lookup argument.
        </ResponseField>
        <ResponseField name="subblock_infra_maintenance_pending_count" type="number">
          Describes number of subblock Infrastructure that has pending maintenance. Here, Subblock Infrastructure Maintenance pertains to upstream hardware contained in the Subblock that is necessary for a VM Family (e.g. NVLink Domains). Not all VM Families will support this field. Optional lookup argument.
        </ResponseField>
        <ResponseField name="upcoming_group_maintenance" type="object">
          Upcoming Maintenance notification information. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="can_reschedule" type="bool">
              Indicates if the maintenance can be customer triggered. Optional lookup argument.
            </ResponseField>
            <ResponseField name="date" type="string">
              [Output Only] The date when the maintenance will take place. This value is inRFC3339 text format. DEPRECATED: Use window_start_time instead. Optional lookup argument.
            </ResponseField>
            <ResponseField name="latest_window_start_time" type="string">
              The latest time for the planned maintenance window to start. This timestamp value is in RFC3339 text format. Optional lookup argument.
            </ResponseField>
            <ResponseField name="maintenance_method" type="string">
              Output only. Maintenance method for the upcoming maintenance. Returned by the lookup.
            </ResponseField>
            <ResponseField name="maintenance_on_shutdown" type="bool">
              Indicates whether the UpcomingMaintenance will be triggered on VM shutdown. Optional lookup argument.
            </ResponseField>
            <ResponseField name="maintenance_reasons" type="list(string)">
              The reasons for the maintenance. Only valid for vms. Optional lookup argument.
            </ResponseField>
            <ResponseField name="maintenance_status" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="start_time_window" type="object">
              Represents a window of time using two timestamps: `earliest` and `latest`. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="earliest" type="string">
                  Optional lookup argument.
                </ResponseField>
                <ResponseField name="latest" type="string">
                  Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="time" type="string">
              [Output Only] The time when the maintenance will take place. This value is inRFC3339 text format. DEPRECATED: Use window_start_time instead. Optional lookup argument.
            </ResponseField>
            <ResponseField name="type" type="string">
              Defines the type of maintenance. Optional lookup argument.
            </ResponseField>
            <ResponseField name="window_end_time" type="string">
              The time by which the maintenance disruption will be completed. This timestamp value is in RFC3339 text format. Optional lookup argument.
            </ResponseField>
            <ResponseField name="window_start_time" type="string">
              The current start time of the maintenance window. This timestamp value is in RFC3339 text format. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reservation_sub_block_count" type="number">
      Output only. [Output Only] The number of reservation subBlocks associated with this reservation block. Returned by the lookup.
    </ResponseField>
    <ResponseField name="reservation_sub_block_in_use_count" type="number">
      Output only. [Output Only] The number of in-use reservation subBlocks associated with this reservation block. If at least one VM is running on a subBlock, it is considered in-use. Returned by the lookup.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      Output only. [Output Only] Server-defined fully-qualified URL for this resource. Returned by the lookup.
    </ResponseField>
    <ResponseField name="self_link_with_id" type="string">
      Output only. [Output Only] Server-defined URL for this resource with the resource id. Returned by the lookup.
    </ResponseField>
    <ResponseField name="status" type="string">
      Output only. [Output Only] Status of the reservation block. Returned by the lookup.
    </ResponseField>
    <ResponseField name="zone" type="string">
      Output only. [Output Only] Zone in which the reservation block resides. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
