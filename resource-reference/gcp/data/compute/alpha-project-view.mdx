---
title: "google_compute_alpha_project_view"
description: "`data_compute_alpha_project_view` looks up a real, read-only summary view of a project's own Compute Engine resources, via the Alpha API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_alpha_project_view"})

    		ubx.Data(compute.AlphaProjectView, "example", compute.AlphaProjectViewConfig{
    			Project: "example",
    			Region:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { AlphaProjectView } from "./local-sdk/google/sdk/typescript/google/data/compute/alpha_project_view.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_alpha_project_view" });

      data(AlphaProjectView, "example", {
        project: "example",
        region: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.compute import AlphaProjectView, AlphaProjectViewConfig

    def describe():
        ubx.intent("look up google_compute_alpha_project_view")

        ubx.data(AlphaProjectView, "example", AlphaProjectViewConfig(
            project="example",
            region="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="project" type="string" required>
  The real Google Cloud project ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="region" type="string" required>
  The real Google Cloud region this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
