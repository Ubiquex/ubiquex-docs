---
title: "google_compute_license_code"
description: "`data_compute_license_code` looks up a real, already-existing `google_compute_alpha_license`."
---

It tracks the real licensing terms and restrictions attached to an image or disk -- the mechanism behind bring-your-own-license and marketplace image billing, not something a user typically creates directly.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_license_code"})

    		ubx.Data(compute.LicenseCode, "example", compute.LicenseCodeConfig{
    			LicenseCode: "example",
    			Project:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { LicenseCode } from "./local-sdk/google/sdk/typescript/google/data/compute/license_code.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_license_code" });

      data(LicenseCode, "example", {
        licenseCode: "example",
        project: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.compute import LicenseCode, LicenseCodeConfig

    def describe():
        ubx.intent("look up google_compute_license_code")

        ubx.data(LicenseCode, "example", LicenseCodeConfig(
            license_code="example",
            project="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="license_code" type="string" required>
  The real, globally-unique license code this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="project" type="string" required>
  The real Google Cloud project ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="allowed_replacement_licenses" type="list(string)">
  Specifies licenseCodes of licenses that can replace this license. Note: such replacements are allowed even if removable_from_disk is false. Returned by the lookup.
</ResponseField>

<ResponseField name="appendable_to_disk" type="bool">
  If true, this license can be appended to an existing disk's set of licenses. Returned by the lookup.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Output only. [Output Only] Description of this License Code. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Returned by the lookup.
</ResponseField>

<ResponseField name="incompatible_licenses" type="list(string)">
  Specifies licenseCodes of licenses that are incompatible with this license. If a license is incompatible with this license, it cannot be attached to the same disk or image. Returned by the lookup.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of resource. Always compute#licenseCode for licenses. Returned by the lookup.
</ResponseField>

<ResponseField name="license_alias" type="list(object)">
  [Output Only] URL and description aliases of Licenses with the same License Code. Returned by the lookup.
</ResponseField>

<ResponseField name="minimum_retention" type="object">
  A Duration represents a fixed-length span of time represented as a count of seconds and fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts like "day" or "month". Range is approximately 10,000 years. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="nanos" type="number">
      Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 `seconds` field and a positive `nanos` field. Must be from 0 to 999,999,999 inclusive. Optional lookup argument.
    </ResponseField>
    <ResponseField name="seconds" type="string">
      Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="multi_tenant_only" type="bool">
  If true, this license can only be used on VMs on multi tenant nodes. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. [Output Only] Name of the resource. The name is 1-20 characters long and must be a valid 64 bit integer. Returned by the lookup.
</ResponseField>

<ResponseField name="os_license" type="bool">
  If true, indicates this is an OS license. Only one OS license can be attached to a disk or image at a time. Returned by the lookup.
</ResponseField>

<ResponseField name="removable_from_disk" type="bool">
  If true, this license can be removed from a disk's set of licenses, with no replacement license needed. Returned by the lookup.
</ResponseField>

<ResponseField name="required_coattached_licenses" type="list(string)">
  Specifies the set of permissible coattached licenseCodes of licenses that satisfy the coattachment requirement of this license. At least one license from the set must be attached to the same disk or image as this license. Returned by the lookup.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="sole_tenant_only" type="bool">
  If true, this license can only be used on VMs on sole tenant nodes. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. [Output Only] Current state of this License Code. Returned by the lookup.
</ResponseField>

<ResponseField name="transferable" type="bool">
  Output only. [Output Only] If true, the license will remain attached when creating images or snapshots from disks. Otherwise, the license is not transferred. Returned by the lookup.
</ResponseField>

<ResponseField name="update_timestamp" type="string">
  Output only. [Output Only] Last update timestamp inRFC3339 text format. Returned by the lookup.
</ResponseField>
