---
title: "google_compute_alpha_managed_ruleset"
description: "`data_compute_alpha_managed_ruleset` looks up a real, Google-managed Cloud Armor WAF rule set, via the Alpha API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_alpha_managed_ruleset"})

    		ubx.Data(compute.AlphaManagedRuleset, "example", compute.AlphaManagedRulesetConfig{
    			ManagedRuleset: "example",
    			Project:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { AlphaManagedRuleset } from "./local-sdk/google/sdk/typescript/google/data/compute/alpha_managed_ruleset.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_alpha_managed_ruleset" });

      data(AlphaManagedRuleset, "example", {
        managedRuleset: "example",
        project: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.compute import AlphaManagedRuleset, AlphaManagedRulesetConfig

    def describe():
        ubx.intent("look up google_compute_alpha_managed_ruleset")

        ubx.data(AlphaManagedRuleset, "example", AlphaManagedRulesetConfig(
            managed_ruleset="example",
            project="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="managed_ruleset" type="string" required>
  The real, Google-managed Cloud Armor WAF rule set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="project" type="string" required>
  The real Google Cloud project ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="change_log" type="string">
  Output only. [Output Only] The change log for this managed ruleset. Returned by the lookup.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp in RFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  [Output Only] An optional description of this resource. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource. Generated internally when the resource is created. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Returned by the lookup.
</ResponseField>

<ResponseField name="rule_ids" type="list(string)">
  Output only. [Output Only] The list of managed rule IDs that are included in this managed ruleset. Returned by the lookup.
</ResponseField>

<ResponseField name="ruleset_id" type="string">
  Output only. [Output Only] The managed ruleset identifier that can be configured in Security Policy rules. Returned by the lookup.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Returned by the lookup.
</ResponseField>
