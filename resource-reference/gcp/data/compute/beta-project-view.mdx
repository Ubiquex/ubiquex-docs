---
title: "google_compute_beta_project_view"
description: "`google_compute_beta_project_view` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_compute_beta_project_view` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_beta_project_view"})

    		ubx.Data(compute.BetaProjectView, "example", compute.BetaProjectViewConfig{
    			Project: "example",
    			Region:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BetaProjectView } from "@ubx/sdk-google/google/data/compute/beta_project_view";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_beta_project_view" });

      data(BetaProjectView, "example", {
        project: "example",
        region: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.compute import BetaProjectView, BetaProjectViewConfig

    def describe():
        ubx.intent("look up google_compute_beta_project_view")

        ubx.data(BetaProjectView, "example", BetaProjectViewConfig(
            project="example",
            region="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="project" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="region" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
