---
title: "google_compute_alpha_storage_pool_type"
description: "`data_compute_alpha_storage_pool_type` looks up a real, available Compute Engine Hyperdisk storage pool type, via the Alpha API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_alpha_storage_pool_type"})

    		ubx.Data(compute.AlphaStoragePoolType, "example", compute.AlphaStoragePoolTypeConfig{
    			Project:         "example",
    			StoragePoolType: "example",
    			Zone:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlphaStoragePoolType } from "@ubx/sdk-google/google/data/compute/alpha_storage_pool_type";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_alpha_storage_pool_type" });

      data(AlphaStoragePoolType, "example", {
        project: "example",
        storagePoolType: "example",
        zone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.compute import AlphaStoragePoolType, AlphaStoragePoolTypeConfig

    def describe():
        ubx.intent("look up google_compute_alpha_storage_pool_type")

        ubx.data(AlphaStoragePoolType, "example", AlphaStoragePoolTypeConfig(
            project="example",
            storage_pool_type="example",
            zone="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="project" type="string" required>
  The real Google Cloud project ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="storage_pool_type" type="string" required>
  The real Hyperdisk storage pool type this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="zone" type="string" required>
  The real Google Cloud zone this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_timestamp" type="string">
  [Output Only] Creation timestamp inRFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="deprecated" type="object">
  Deprecation status for a public resource. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="deleted" type="string">
      An optional RFC3339 timestamp on or after which the state of this resource is intended to change to DELETED. This is only informational and the status will not change unless the client explicitly changes it. Optional lookup argument.
    </ResponseField>
    <ResponseField name="deprecated" type="string">
      An optional RFC3339 timestamp on or after which the state of this resource is intended to change to DEPRECATED. This is only informational and the status will not change unless the client explicitly changes it. Optional lookup argument.
    </ResponseField>
    <ResponseField name="obsolete" type="string">
      An optional RFC3339 timestamp on or after which the state of this resource is intended to change to OBSOLETE. This is only informational and the status will not change unless the client explicitly changes it. Optional lookup argument.
    </ResponseField>
    <ResponseField name="replacement" type="string">
      The URL of the suggested replacement for a deprecated resource. The suggested replacement resource must be the same kind of resource as the deprecated resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      The deprecation state of this resource. This can be ACTIVE,DEPRECATED, OBSOLETE, or DELETED. Operations which communicate the end of life date for an image, can useACTIVE. Operations which create a new resource using aDEPRECATED resource will return successfully, but with a warning indicating the deprecated resource and recommending its replacement. Operations which use OBSOLETE orDELETED resources will be rejected and result in an error. Optional lookup argument.
    </ResponseField>
    <ResponseField name="state_override" type="object">
      A rollout policy configuration. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="default_rollout_time" type="string">
          An optional RFC3339 timestamp on or after which the update is considered rolled out to any zone that is not explicitly stated. Optional lookup argument.
        </ResponseField>
        <ResponseField name="location_rollout_policies" type="map(string)">
          Location based rollout policies to apply to the resource. Currently only zone names are supported and must be represented as valid URLs, like: zones/us-central1-a. The value expects an RFC3339 timestamp on or after which the update is considered rolled out to the specified location. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  [Output Only] An optional description of this resource. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  [Output Only] The unique identifier for the resource. This identifier is defined by the server. Returned by the lookup.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#storagePoolType for storage pool types. Returned by the lookup.
</ResponseField>

<ResponseField name="max_pool_provisioned_capacity_gb" type="string">
  [Output Only] Maximum storage pool size in GB. Returned by the lookup.
</ResponseField>

<ResponseField name="max_pool_provisioned_iops" type="string">
  [Output Only] Maximum provisioned IOPS. Returned by the lookup.
</ResponseField>

<ResponseField name="max_pool_provisioned_throughput" type="string">
  [Output Only] Maximum provisioned throughput. Returned by the lookup.
</ResponseField>

<ResponseField name="min_pool_provisioned_capacity_gb" type="string">
  [Output Only] Minimum storage pool size in GB. Returned by the lookup.
</ResponseField>

<ResponseField name="min_pool_provisioned_iops" type="string">
  [Output Only] Minimum provisioned IOPS. Returned by the lookup.
</ResponseField>

<ResponseField name="min_pool_provisioned_throughput" type="string">
  [Output Only] Minimum provisioned throughput. Returned by the lookup.
</ResponseField>

<ResponseField name="min_size_gb" type="string">
  [Deprecated] This field is deprecated. Use minPoolProvisionedCapacityGb instead. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  [Output Only] Name of the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="self_link" type="string">
  [Output Only] Server-defined URL for the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource with the resource id. Returned by the lookup.
</ResponseField>

<ResponseField name="supported_disk_types" type="list(string)">
  [Output Only] The list of disk types supported in this storage pool type. Returned by the lookup.
</ResponseField>
