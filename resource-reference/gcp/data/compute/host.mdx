---
title: "google_compute_host"
description: "`data_compute_host` looks up a real Compute Engine Sole-Tenant Node, a dedicated physical server reserved for a single customer's own VMs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_host"})

    		ubx.Data(compute.Host, "example", compute.HostConfig{
    			Association: "example",
    			Host:        "example",
    			Project:     "example",
    			Zone:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Host } from "@ubx/sdk-google/google/data/compute/host";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_host" });

      data(Host, "example", {
        association: "example",
        host: "example",
        project: "example",
        zone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.compute import Host, HostConfig

    def describe():
        ubx.intent("look up google_compute_host")

        ubx.data(Host, "example", HostConfig(
            association="example",
            host="example",
            project="example",
            zone="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="association" type="string" required>
  The real association this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="host" type="string" required>
  The real Sole-Tenant Node (host) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="project" type="string" required>
  The real Google Cloud project ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="zone" type="string" required>
  The real Google Cloud zone this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="alias_links" type="list(string)">
  Output only. All aliases for this resource. e.g. projects/123/zones/us-centra1-a/reservation/r1/reservationBlock/b1/hosts/h1 Returned by the lookup.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. The creation timestamp, formatted asRFC3339 text. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. The unique identifier for this resource. This identifier is defined by the server. Returned by the lookup.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. The type of resource. Alwayscompute#host for hosts. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The name of the host. Returned by the lookup.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. The self link of the host. Returned by the lookup.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. The self link with id of the host. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the host. Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="object">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="physical_topology" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="block" type="string">
          The unique identifier of the capacity block within the cluster. Optional lookup argument.
        </ResponseField>
        <ResponseField name="cluster" type="string">
          The cluster name of the reservation sub-block. Optional lookup argument.
        </ResponseField>
        <ResponseField name="host" type="string">
          The unique identifier of the capacity host within the capacity sub-block. Optional lookup argument.
        </ResponseField>
        <ResponseField name="sub_block" type="string">
          The unique identifier of the capacity sub-block within the capacity block. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="running_instances" type="list(string)">
      Output only. The URIs of the instances currently running on this host. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
