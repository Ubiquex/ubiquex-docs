---
title: "google_compute_alpha_organization_snapshot_recycle_bin_policy"
description: "`google_compute_alpha_organization_snapshot_recycle_bin_policy` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_compute_alpha_organization_snapshot_recycle_bin_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_alpha_organization_snapshot_recycle_bin_policy"})

    		ubx.Data(compute.AlphaOrganizationSnapshotRecycleBinPolicy, "example", compute.AlphaOrganizationSnapshotRecycleBinPolicyConfig{
    			Organization: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlphaOrganizationSnapshotRecycleBinPolicy } from "@ubx/sdk-google/google/data/compute/alpha_organization_snapshot_recycle_bin_policy";

    export default stack("example", () => {
      intent({
        summary:
          "look up google_compute_alpha_organization_snapshot_recycle_bin_policy",
      });

      data(AlphaOrganizationSnapshotRecycleBinPolicy, "example", {
        organization: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.compute import AlphaOrganizationSnapshotRecycleBinPolicy, AlphaOrganizationSnapshotRecycleBinPolicyConfig

    def describe():
        ubx.intent("look up google_compute_alpha_organization_snapshot_recycle_bin_policy")

        ubx.data(AlphaOrganizationSnapshotRecycleBinPolicy, "example", AlphaOrganizationSnapshotRecycleBinPolicyConfig(
            organization="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="organization" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="rules" type="map(object)">
  The rules for the snapshot recycle bin policy. The key is either 'default' or namespacedName of the TagValue which can be in the format: `&#123;organization_id&#125;/&#123;tag_key_short_name&#125;/&#123;tag_value_short_name&#125;` or `&#123;project_id&#125;/&#123;tag_key_short_name&#125;/&#123;tag_value_short_name&#125;` or `&#123;project_number&#125;/&#123;tag_key_short_name&#125;/&#123;tag_value_short_name&#125;`. The default rule is applied if snapshots do not have any of these tags. The value is the rule for the key. Returned by the lookup.
</ResponseField>

<ResponseField name="system_rules" type="map(object)">
  Output only. The system rules for snapshot recycle bin policy. Defines the default rule that applies if no customer-defined rule matches. Returned by the lookup.
</ResponseField>
