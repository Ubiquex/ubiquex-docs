---
title: "google_compute_preview_feature"
description: "`data_compute_preview_feature` looks up a real Compute Engine preview feature and its own current rollout state for a project."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_preview_feature"})

    		ubx.Data(compute.PreviewFeature, "example", compute.PreviewFeatureConfig{
    			PreviewFeature: "example",
    			Project:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { PreviewFeature } from "./local-sdk/google/sdk/typescript/google/data/compute/preview_feature.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_preview_feature" });

      data(PreviewFeature, "example", {
        previewFeature: "example",
        project: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.compute import PreviewFeature, PreviewFeatureConfig

    def describe():
        ubx.intent("look up google_compute_preview_feature")

        ubx.data(PreviewFeature, "example", PreviewFeatureConfig(
            preview_feature="example",
            project="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="preview_feature" type="string" required>
  The real Compute Engine preview feature this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="project" type="string" required>
  The real Google Cloud project ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="activation_status" type="string">
  Specifies whether the feature is enabled or disabled. Returned by the lookup.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Output only. [Output Only] Description of the feature. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Returned by the lookup.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output only] The type of the feature. Always "compute#previewFeature" for preview features. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the feature. Returned by the lookup.
</ResponseField>

<ResponseField name="rollout_operation" type="object">
  Represents the rollout operation Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="rollout_input" type="object">
      Represents the input for the rollout operation. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the rollout plan Ex. organizations//locations/global/rolloutPlans/ Ex. folders//locations/global/rolloutPlans/ Ex. projects//locations/global/rolloutPlans/. Optional lookup argument.
        </ResponseField>
        <ResponseField name="predefined_rollout_plan" type="string">
          Predefined rollout plan. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="object">
  [Output Only] The status of the feature. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Output only. [Output Only] The description of the feature. Returned by the lookup.
    </ResponseField>
    <ResponseField name="help_link" type="string">
      Output only. [Output Only] Link to the public documentation for the feature. Returned by the lookup.
    </ResponseField>
    <ResponseField name="release_status" type="object">
      [Output Only] The release status of the feature. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="stage" type="string">
          Output only. [Output Only] The stage of the feature. Returned by the lookup.
        </ResponseField>
        <ResponseField name="update_date" type="object">
          Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="day" type="number">
              Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant. Optional lookup argument.
            </ResponseField>
            <ResponseField name="month" type="number">
              Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day. Optional lookup argument.
            </ResponseField>
            <ResponseField name="year" type="number">
              Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
