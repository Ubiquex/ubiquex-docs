---
title: "google_compute_alpha_recoverable_snapshot"
description: "`google_compute_alpha_recoverable_snapshot` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_compute_alpha_recoverable_snapshot` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_compute_alpha_recoverable_snapshot"})

    		ubx.Data(compute.AlphaRecoverableSnapshot, "example", compute.AlphaRecoverableSnapshotConfig{
    			Project:             "example",
    			RecoverableSnapshot: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlphaRecoverableSnapshot } from "@ubx/sdk-google/google/data/compute/alpha_recoverable_snapshot";

    export default stack("example", () => {
      intent({ summary: "look up google_compute_alpha_recoverable_snapshot" });

      data(AlphaRecoverableSnapshot, "example", {
        project: "example",
        recoverableSnapshot: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.compute import AlphaRecoverableSnapshot, AlphaRecoverableSnapshotConfig

    def describe():
        ubx.intent("look up google_compute_alpha_recoverable_snapshot")

        ubx.data(AlphaRecoverableSnapshot, "example", AlphaRecoverableSnapshotConfig(
            project="example",
            recoverable_snapshot="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="project" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="recoverable_snapshot" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_timestamp" type="string">
  Output only. [Output Only] Creation timestamp inRFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. An optional description of this resource. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. Returned by the lookup.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. [Output Only] Type of the resource. Alwayscompute#recoverableSnapshot for RecoverableSnapshot resources. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. Name of the recoverable snapshot generated on the deletion of the snapshot. The name will be 1-63 characters long, and comply withRFC1035. Specifically, the name will be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character will be a lowercase letter, and all following characters can be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Returned by the lookup.
</ResponseField>

<ResponseField name="original_resource" type="object">
  Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="architecture" type="string">
      Output only. [Output Only] The architecture of the snapshot. Valid values are ARM64 or X86_64. Returned by the lookup.
    </ResponseField>
    <ResponseField name="auto_created" type="bool">
      Output only. [Output Only] Set to true if snapshots are automatically created by applying resource policy on the target disk. Returned by the lookup.
    </ResponseField>
    <ResponseField name="chain_name" type="string">
      Creates the new snapshot in the snapshot chain labeled with the specified name. The chain name must be 1-63 characters long and comply with RFC1035. This is an uncommon option only for advanced service owners who needs to create separate snapshot chains, for example, for chargeback tracking. When you describe your snapshot resource, this field is visible only if it has a non-empty value. Optional lookup argument.
    </ResponseField>
    <ResponseField name="creation_size_bytes" type="string">
      Output only. [Output Only] Size in bytes of the snapshot at creation time. Returned by the lookup.
    </ResponseField>
    <ResponseField name="creation_timestamp" type="string">
      Output only. [Output Only] Creation timestamp inRFC3339 text format. Returned by the lookup.
    </ResponseField>
    <ResponseField name="deletion_timestamp" type="string">
      Output only. [Output Only] Purge timestamp of recoverable snapshot inRFC3339 text format. Returned by the lookup.
    </ResponseField>
    <ResponseField name="description" type="string">
      An optional description of this resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="disk_size_gb" type="string">
      Output only. [Output Only] Size of the source disk, specified in GB. Returned by the lookup.
    </ResponseField>
    <ResponseField name="download_bytes" type="string">
      Output only. [Output Only] Number of bytes downloaded to restore a snapshot to a disk. Returned by the lookup.
    </ResponseField>
    <ResponseField name="enable_confidential_compute" type="bool">
      Output only. Whether this snapshot is created from a confidential compute mode disk. [Output Only]: This field is not set by user, but from source disk. Returned by the lookup.
    </ResponseField>
    <ResponseField name="guest_os_features" type="list(object)">
      Output only. [Output Only] A list of features to enable on the guest operating system. Applicable only for bootable images. Read Enabling guest operating system features to see a list of available options. Returned by the lookup.
    </ResponseField>
    <ResponseField name="id" type="string">
      Output only. [Output Only] The unique identifier for the original snapshot. This identifier is defined by the server. Returned by the lookup.
    </ResponseField>
    <ResponseField name="label_fingerprint" type="string">
      A fingerprint for the labels being applied to this snapshot, which is essentially a hash of the labels set used for optimistic locking. The fingerprint is initially generated by Compute Engine and changes after every request to modify or update labels. You must always provide an up-to-date fingerprint hash in order to update or change labels, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a snapshot. Optional lookup argument.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Labels to apply to this snapshot. These can be later modified by the setLabels method. Label values may be empty. Optional lookup argument.
    </ResponseField>
    <ResponseField name="license_codes" type="list(string)">
      Output only. [Output Only] Integer license codes indicating which licenses are attached to this snapshot. Returned by the lookup.
    </ResponseField>
    <ResponseField name="licenses" type="list(string)">
      Output only. [Output Only] A list of public visible licenses that apply to this snapshot. Returned by the lookup.
    </ResponseField>
    <ResponseField name="max_retention_days" type="number">
      Number of days the snapshot should be retained before being deleted automatically. Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of the original snapshot provided by the client. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. Optional lookup argument.
    </ResponseField>
    <ResponseField name="region" type="string">
      Output only. [Output Only] URL of the region where the snapshot resides. Only applicable for regional snapshots. Returned by the lookup.
    </ResponseField>
    <ResponseField name="satisfies_pzi" type="bool">
      Output only. Reserved for future use. Returned by the lookup.
    </ResponseField>
    <ResponseField name="satisfies_pzs" type="bool">
      Output only. [Output Only] Reserved for future use. Returned by the lookup.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      Output only. [Output Only] Server-defined URL for the resource. Returned by the lookup.
    </ResponseField>
    <ResponseField name="self_link_with_id" type="string">
      Output only. [Output Only] Server-defined URL for this resource's resource id. Returned by the lookup.
    </ResponseField>
    <ResponseField name="snapshot_encryption_key" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="kms_key_name" type="string">
          The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional lookup argument.
        </ResponseField>
        <ResponseField name="kms_key_service_account" type="string">
          The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional lookup argument.
        </ResponseField>
        <ResponseField name="raw_key" type="string">
          [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional lookup argument.
        </ResponseField>
        <ResponseField name="rsa_encrypted_key" type="string">
          [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional lookup argument.
        </ResponseField>
        <ResponseField name="sha256" type="string">
          [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="snapshot_group_id" type="string">
      Output only. [Output Only] The unique ID of the snapshot group that this snapshot belongs to. The usage of snapshot group feature is restricted. Returned by the lookup.
    </ResponseField>
    <ResponseField name="snapshot_group_name" type="string">
      Output only. [Output only] The snapshot group that this snapshot belongs to. The usage of snapshot group feature is restricted. Returned by the lookup.
    </ResponseField>
    <ResponseField name="snapshot_type" type="string">
      Indicates the type of the snapshot. Optional lookup argument.
    </ResponseField>
    <ResponseField name="source_disk" type="string">
      The source disk used to create this snapshot. Optional lookup argument.
    </ResponseField>
    <ResponseField name="source_disk_encryption_key" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="kms_key_name" type="string">
          The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional lookup argument.
        </ResponseField>
        <ResponseField name="kms_key_service_account" type="string">
          The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional lookup argument.
        </ResponseField>
        <ResponseField name="raw_key" type="string">
          [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional lookup argument.
        </ResponseField>
        <ResponseField name="rsa_encrypted_key" type="string">
          [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional lookup argument.
        </ResponseField>
        <ResponseField name="sha256" type="string">
          [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source_disk_for_recovery_checkpoint" type="string">
      The source disk whose recovery checkpoint will be used to create this snapshot. Optional lookup argument.
    </ResponseField>
    <ResponseField name="source_disk_id" type="string">
      Output only. [Output Only] The ID value of the disk used to create this snapshot Returned by the lookup.
    </ResponseField>
    <ResponseField name="source_instant_snapshot" type="string">
      The source instant snapshot used to create this snapshot. Optional lookup argument.
    </ResponseField>
    <ResponseField name="source_instant_snapshot_encryption_key" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="kms_key_name" type="string">
          The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/kms_project_id/locations/region/keyRings/ key_region/cryptoKeys/key /cryptoKeyVersions/1 Optional lookup argument.
        </ResponseField>
        <ResponseField name="kms_key_service_account" type="string">
          The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used. For example: "kmsKeyServiceAccount": "name@project_id.iam.gserviceaccount.com/ Optional lookup argument.
        </ResponseField>
        <ResponseField name="raw_key" type="string">
          [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rawKey": "SGVsbG8gZnJvbSBHb29nbGUgQ2xvdWQgUGxhdGZvcm0=" Optional lookup argument.
        </ResponseField>
        <ResponseField name="rsa_encrypted_key" type="string">
          [DEPRECATED] CSEK is no longer supported. Use CMEK instead. Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource. You can provide either the rawKey or thersaEncryptedKey. For example: "rsaEncryptedKey": "ieCx/NcW06PcT7Ep1X6LUTc/hLvUDYyzSZPPVCVPTVEohpeHASqC8uw5TzyO9U+Fka9JFH z0mBibXUInrC/jEk014kCK/NPjYgEMOyssZ4ZINPKxlUh2zn1bV+MCaTICrdmuSBTWlUUiFoD D6PYznLwh8ZNdaheCeZ8ewEXgFQ8V+sDroLaN3Xs3MDTXQEMMoNUXMCZEIpg9Vtp9x2oe==" The key must meet the following requirements before you can provide it to Compute Engine: 1. The key is wrapped using a RSA public key certificate provided by Google. 2. After being wrapped, the key must be encoded in RFC 4648 base64 encoding. Gets the RSA public key certificate provided by Google at: https://cloud-certs.storage.googleapis.com/google-cloud-csek-ingress.pem Optional lookup argument.
        </ResponseField>
        <ResponseField name="sha256" type="string">
          [DEPRECATED] CSEK is no longer supported. Use CMEK instead. [Output only] TheRFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source_instant_snapshot_id" type="string">
      Output only. [Output Only] The unique ID of the instant snapshot used to create this snapshot. This value identifies the exact instant snapshot that was used to create this persistent disk. For example, if you created the persistent disk from an instant snapshot that was later deleted and recreated under the same name, the source instant snapshot ID would identify the exact instant snapshot that was used. Returned by the lookup.
    </ResponseField>
    <ResponseField name="source_snapshot_schedule_policy" type="string">
      Output only. [Output Only] URL of the resource policy which created this scheduled snapshot. Returned by the lookup.
    </ResponseField>
    <ResponseField name="source_snapshot_schedule_policy_id" type="string">
      Output only. [Output Only] ID of the resource policy which created this scheduled snapshot. Returned by the lookup.
    </ResponseField>
    <ResponseField name="storage_bytes" type="string">
      Output only. [Output Only] A size of the storage used by the snapshot. Returned by the lookup.
    </ResponseField>
    <ResponseField name="storage_bytes_status" type="string">
      Output only. [Deprecated] Instead, check the storageBytes field. After snapshot creation, the storageBytesStatus field is alwaysUP_TO_DATE. [Output Only] An indicator whether storageBytes is in a stable state or it is being adjusted as a result of shared storage reallocation. This status can either be unset, meaning the snapshot is being created, or UP_TO_DATE, meaning the size of the snapshot is up-to-date. Returned by the lookup.
    </ResponseField>
    <ResponseField name="storage_locations" type="list(string)">
      Cloud Storage bucket storage location of the snapshot (regional or multi-regional). Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="purge_timestamp" type="string">
  Output only. [Output Only] Purge timestamp of recoverable snapshot inRFC3339 text format. Returned by the lookup.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. [Output Only] Reserved for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. [Output Only] Reserved for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. [Output Only] Server-defined URL for the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="self_link_with_id" type="string">
  Output only. [Output Only] Server-defined URL for this resource's resource id. Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Output only. [Output Only] Status of the recoverable snapshot. Returned by the lookup.
</ResponseField>
