---
title: "google_secretmanager_v1beta1_version"
description: "`data_secretmanager_v1beta1_version` looks up a real, specific version of a Secret Manager secret, via the v1beta1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	secretmanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/secretmanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_secretmanager_v1beta1_version"})

    		ubx.Data(secretmanager.V1beta1Version, "example", secretmanager.V1beta1VersionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1beta1Version } from "@ubx/sdk-google/google/data/secretmanager/v1beta1_version";

    export default stack("example", () => {
      intent({ summary: "look up google_secretmanager_v1beta1_version" });

      data(V1beta1Version, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.secretmanager import V1beta1Version, V1beta1VersionConfig

    def describe():
        ubx.intent("look up google_secretmanager_v1beta1_version")

        ubx.data(V1beta1Version, "example", V1beta1VersionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. The time at which the SecretVersion was created. Returned by the lookup.
</ResponseField>

<ResponseField name="destroy_time" type="string">
  Output only. The time this SecretVersion was destroyed. Only present if state is DESTROYED. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the SecretVersion. Returned by the lookup.
</ResponseField>
