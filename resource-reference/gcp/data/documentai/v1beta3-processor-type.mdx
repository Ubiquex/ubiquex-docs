---
title: "google_documentai_v1beta3_processor_type"
description: "`data_documentai_v1beta3_processor_type` looks up a real, available Document AI processor type, via the v1beta3 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	documentai "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/documentai"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_documentai_v1beta3_processor_type"})

    		ubx.Data(documentai.V1beta3ProcessorType, "example", documentai.V1beta3ProcessorTypeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1beta3ProcessorType } from "./local-sdk/google/sdk/typescript/google/data/documentai/v1beta3_processor_type.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_documentai_v1beta3_processor_type" });

      data(V1beta3ProcessorType, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.documentai import V1beta3ProcessorType, V1beta3ProcessorTypeConfig

    def describe():
        ubx.intent("look up google_documentai_v1beta3_processor_type")

        ubx.data(V1beta3ProcessorType, "example", V1beta3ProcessorTypeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="allow_creation" type="bool">
  Whether the processor type allows creation. If true, users can create a processor of this processor type. Otherwise, users need to request access. Returned by the lookup.
</ResponseField>

<ResponseField name="available_locations" type="list(object)">
  The locations in which this processor is available. Returned by the lookup.
</ResponseField>

<ResponseField name="category" type="string">
  The processor category, used by UI to group processor types. Returned by the lookup.
</ResponseField>

<ResponseField name="launch_stage" type="string">
  Launch stage of the processor type Returned by the lookup.
</ResponseField>

<ResponseField name="sample_document_uris" type="list(string)">
  A set of Cloud Storage URIs of sample documents for this processor. Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  The processor type, such as: `OCR_PROCESSOR`, `INVOICE_PROCESSOR`. Returned by the lookup.
</ResponseField>
