---
title: "google_documentai_processor_type"
description: "`data_documentai_processor_type` looks up a real, available Document AI processor type and its own supported capabilities."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	documentai "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/documentai"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_documentai_processor_type"})

    		ubx.Data(documentai.ProcessorType, "example", documentai.ProcessorTypeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProcessorType } from "@ubx/sdk-google/google/data/documentai/processor_type";

    export default stack("example", () => {
      intent({ summary: "look up google_documentai_processor_type" });

      data(ProcessorType, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.documentai import ProcessorType, ProcessorTypeConfig

    def describe():
        ubx.intent("look up google_documentai_processor_type")

        ubx.data(ProcessorType, "example", ProcessorTypeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="allow_creation" type="bool">
  Whether the processor type allows creation. If true, users can create a processor of this processor type. Otherwise, users need to request access. Returned by the lookup.
</ResponseField>

<ResponseField name="available_locations" type="list(object)">
  The locations in which this processor is available. Returned by the lookup.
</ResponseField>

<ResponseField name="category" type="string">
  The processor category, used by UI to group processor types. Returned by the lookup.
</ResponseField>

<ResponseField name="launch_stage" type="string">
  Launch stage of the processor type Returned by the lookup.
</ResponseField>

<ResponseField name="sample_document_uris" type="list(string)">
  A set of Cloud Storage URIs of sample documents for this processor. Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  The processor type, such as: `OCR_PROCESSOR`, `INVOICE_PROCESSOR`. Returned by the lookup.
</ResponseField>
