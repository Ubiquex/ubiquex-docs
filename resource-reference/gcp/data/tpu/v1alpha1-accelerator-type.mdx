---
title: "google_tpu_v1alpha1_accelerator_type"
description: "`data_tpu_v1alpha1_accelerator_type` looks up a real, available Cloud TPU accelerator type, via the v1alpha1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	tpu "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/tpu"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_tpu_v1alpha1_accelerator_type"})

    		ubx.Data(tpu.V1alpha1AcceleratorType, "example", tpu.V1alpha1AcceleratorTypeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1alpha1AcceleratorType } from "@ubx/sdk-google/google/data/tpu/v1alpha1_accelerator_type";

    export default stack("example", () => {
      intent({ summary: "look up google_tpu_v1alpha1_accelerator_type" });

      data(V1alpha1AcceleratorType, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.tpu import V1alpha1AcceleratorType, V1alpha1AcceleratorTypeConfig

    def describe():
        ubx.intent("look up google_tpu_v1alpha1_accelerator_type")

        ubx.data(V1alpha1AcceleratorType, "example", V1alpha1AcceleratorTypeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="type" type="string">
  the accelerator type. Returned by the lookup.
</ResponseField>
