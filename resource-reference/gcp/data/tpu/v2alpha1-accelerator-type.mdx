---
title: "google_tpu_v2alpha1_accelerator_type"
description: "`data_tpu_v2alpha1_accelerator_type` looks up a real, available Cloud TPU accelerator type, via the v2alpha1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	tpu "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/tpu"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_tpu_v2alpha1_accelerator_type"})

    		ubx.Data(tpu.V2alpha1AcceleratorType, "example", tpu.V2alpha1AcceleratorTypeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V2alpha1AcceleratorType } from "./local-sdk/google/sdk/typescript/google/data/tpu/v2alpha1_accelerator_type.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_tpu_v2alpha1_accelerator_type" });

      data(V2alpha1AcceleratorType, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.tpu import V2alpha1AcceleratorType, V2alpha1AcceleratorTypeConfig

    def describe():
        ubx.intent("look up google_tpu_v2alpha1_accelerator_type")

        ubx.data(V2alpha1AcceleratorType, "example", V2alpha1AcceleratorTypeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accelerator_configs" type="list(object)">
  The accelerator config. Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  The accelerator type. Returned by the lookup.
</ResponseField>
