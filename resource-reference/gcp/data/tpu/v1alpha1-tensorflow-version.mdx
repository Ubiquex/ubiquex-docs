---
title: "google_tpu_v1alpha1_tensorflow_version"
description: "`data_tpu_v1alpha1_tensorflow_version` looks up a real, available Cloud TPU TensorFlow runtime version, via the v1alpha1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	tpu "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/tpu"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_tpu_v1alpha1_tensorflow_version"})

    		ubx.Data(tpu.V1alpha1TensorflowVersion, "example", tpu.V1alpha1TensorflowVersionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1alpha1TensorflowVersion } from "./local-sdk/google/sdk/typescript/google/data/tpu/v1alpha1_tensorflow_version.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_tpu_v1alpha1_tensorflow_version" });

      data(V1alpha1TensorflowVersion, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.tpu import V1alpha1TensorflowVersion, V1alpha1TensorflowVersionConfig

    def describe():
        ubx.intent("look up google_tpu_v1alpha1_tensorflow_version")

        ubx.data(V1alpha1TensorflowVersion, "example", V1alpha1TensorflowVersionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="version" type="string">
  the tensorflow version. Returned by the lookup.
</ResponseField>
