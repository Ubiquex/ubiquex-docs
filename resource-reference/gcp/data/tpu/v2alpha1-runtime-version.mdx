---
title: "google_tpu_v2alpha1_runtime_version"
description: "`data_tpu_v2alpha1_runtime_version` looks up a real, available Cloud TPU runtime software version, via the v2alpha1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	tpu "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/tpu"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_tpu_v2alpha1_runtime_version"})

    		ubx.Data(tpu.V2alpha1RuntimeVersion, "example", tpu.V2alpha1RuntimeVersionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V2alpha1RuntimeVersion } from "@ubx/sdk-google/google/data/tpu/v2alpha1_runtime_version";

    export default stack("example", () => {
      intent({ summary: "look up google_tpu_v2alpha1_runtime_version" });

      data(V2alpha1RuntimeVersion, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.tpu import V2alpha1RuntimeVersion, V2alpha1RuntimeVersionConfig

    def describe():
        ubx.intent("look up google_tpu_v2alpha1_runtime_version")

        ubx.data(V2alpha1RuntimeVersion, "example", V2alpha1RuntimeVersionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="version" type="string">
  The runtime version. Returned by the lookup.
</ResponseField>
