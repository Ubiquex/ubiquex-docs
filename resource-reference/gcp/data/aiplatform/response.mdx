---
title: "google_aiplatform_response"
description: "`data_aiplatform_response` looks up a real, generated Vertex AI Gen AI model response, kept for logging and evaluation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_aiplatform_response"})

    		ubx.Data(aiplatform.Response, "example", aiplatform.ResponseConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { Response } from "./local-sdk/google/sdk/typescript/google/data/aiplatform/response.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_aiplatform_response" });

      data(Response, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.aiplatform import Response, ResponseConfig

    def describe():
        ubx.intent("look up google_aiplatform_response")

        ubx.data(Response, "example", ResponseConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="content_type" type="string">
  The HTTP Content-Type header value specifying the content type of the body. Returned by the lookup.
</ResponseField>

<ResponseField name="data" type="string">
  The HTTP request/response body as raw binary. Returned by the lookup.
</ResponseField>

<ResponseField name="extensions" type="list(map(any))">
  Application specific response metadata. Must be set in the first response for streaming APIs. Returned by the lookup.
</ResponseField>
