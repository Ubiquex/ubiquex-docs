---
title: "google_aiplatform_v1beta1_runtime_revision"
description: "`data_aiplatform_v1beta1_runtime_revision` looks up a real revision of a Vertex AI Agent Engine runtime configuration, via the v1beta1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_aiplatform_v1beta1_runtime_revision"})

    		ubx.Data(aiplatform.V1beta1RuntimeRevision, "example", aiplatform.V1beta1RuntimeRevisionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1beta1RuntimeRevision } from "./local-sdk/google/sdk/typescript/google/data/aiplatform/v1beta1_runtime_revision.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_aiplatform_v1beta1_runtime_revision" });

      data(V1beta1RuntimeRevision, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.aiplatform import V1beta1RuntimeRevision, V1beta1RuntimeRevisionConfig

    def describe():
        ubx.intent("look up google_aiplatform_v1beta1_runtime_revision")

        ubx.data(V1beta1RuntimeRevision, "example", V1beta1RuntimeRevisionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp when this ReasoningEngineRuntimeRevision was created. Returned by the lookup.
</ResponseField>

<ResponseField name="spec" type="object">
  ReasoningEngine configurations Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="agent_card" type="map(any)">
      Optional. The A2A Agent Card for the agent (if available). It follows the specification at https://a2a-protocol.org/latest/specification/#5-agent-discovery-the-agent-card. Optional lookup argument.
    </ResponseField>
    <ResponseField name="agent_framework" type="string">
      Optional. The OSS agent framework used to develop the agent. Currently supported values: "google-adk", "langchain", "langgraph", "ag2", "llama-index", "custom". Optional lookup argument.
    </ResponseField>
    <ResponseField name="build_spec" type="object">
      Specification for building container image. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="service_account" type="string">
          Optional. The service account that Cloud Build uses to run the build. This field is only applicable when `worker_pool` is specified (i.e., for custom worker pools). If `worker_pool` is not specified, this field is ignored and the build runs using the Google-managed service agent. Format: `projects/&#123;project&#125;/serviceAccounts/&#123;service_account&#125;` or `&#123;service_account&#125;@&#123;project&#125;.iam.gserviceaccount.com` Optional lookup argument.
        </ResponseField>
        <ResponseField name="worker_pool" type="string">
          Optional. Identifier. The resource name of the Cloud Build WorkerPool to use for the build. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/workerPools/&#123;worker_pool&#125;` Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="class_methods" type="list(map(any))">
      Optional. Declarations for object class methods in OpenAPI specification format. Optional lookup argument.
    </ResponseField>
    <ResponseField name="container_spec" type="object">
      Specification for deploying from a container image. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="image_uri" type="string">
          Required. The Artifact Registry Docker image URI (e.g., us-central1-docker.pkg.dev/my-project/my-repo/my-image:tag) of the container image that is to be run on each worker replica. Optional lookup argument.
        </ResponseField>
        <ResponseField name="port" type="number">
          Optional. The port the container listens on. Defaults to 8080 if unset. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deployment_spec" type="object">
      The specification of a Reasoning Engine deployment. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="agent_gateway_config" type="object">
          Agent Gateway configuration for a Reasoning Engine deployment. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="agent_to_anywhere_config" type="object">
              Configuration for traffic originating from a Reasoning Engine. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="agent_gateway" type="string">
                  Required. The resource name of the Agent Gateway for outbound traffic. It must be set to a Google-managed gateway whose `governed_access_path` is `AGENT_TO_ANYWHERE`. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/agentGateways/&#123;agent_gateway&#125;` Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="client_to_agent_config" type="object">
              Configuration for traffic targeting a Reasoning Engine. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="agent_gateway" type="string">
                  Required. The resource name of the Agent Gateway to use for inbound traffic. It must be set to a Google-managed gateway whose `governed_access_path` is `CLIENT_TO_AGENT`. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/agentGateways/&#123;agent_gateway&#125;` Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="agent_server_mode" type="string">
          The agent server mode. Optional lookup argument.
        </ResponseField>
        <ResponseField name="container_concurrency" type="number">
          Optional. Concurrency for each container and agent server. Recommended value: 2 * cpu + 1. Defaults to 9. Optional lookup argument.
        </ResponseField>
        <ResponseField name="env" type="list(object)">
          Optional. Environment variables to be set with the Reasoning Engine deployment. The environment variables can be updated through the UpdateReasoningEngine API. Optional lookup argument.
        </ResponseField>
        <ResponseField name="keep_alive_probe" type="object">
          Represents the configuration for keep-alive probe. Contains configuration on a specified endpoint that a deployment host should use to keep the container alive based on the probe settings. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="http_get" type="object">
              Specifies the HTTP GET configuration for the probe. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="path" type="string">
                  Required. Specifies the path of the HTTP GET request (e.g., `"/is_busy"`). Optional lookup argument.
                </ResponseField>
                <ResponseField name="port" type="number">
                  Optional. Specifies the port number on the container to which the request is sent. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_seconds" type="number">
              Optional. Specifies the maximum duration (in seconds) to keep the instance alive via this probe. Can be a maximum of 3600 seconds (1 hour). Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="max_instances" type="number">
          Optional. The maximum number of application instances that can be launched to handle increased traffic. Defaults to 100. Range: [1, 1000]. If VPC-SC or PSC-I is enabled, the acceptable range is [1, 100]. Optional lookup argument.
        </ResponseField>
        <ResponseField name="min_instances" type="number">
          Optional. The minimum number of application instances that will be kept running at all times. Defaults to 1. Range: [0, 75]. Optional lookup argument.
        </ResponseField>
        <ResponseField name="psc_interface_config" type="object">
          Configuration for PSC-I. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="dns_peering_configs" type="list(object)">
              Optional. DNS peering configurations. When specified, Vertex AI will attempt to configure DNS peering zones in the tenant project VPC to resolve the specified domains using the target network's Cloud DNS. The user must grant the dns.peer role to the Vertex AI Service Agent on the target project. Optional lookup argument.
            </ResponseField>
            <ResponseField name="network_attachment" type="string">
              Optional. The name of the Compute Engine [network attachment](https://cloud.google.com/vpc/docs/about-network-attachments) to attach to the resource within the region and user project. To specify this field, you must have already [created a network attachment] (https://cloud.google.com/vpc/docs/create-manage-network-attachments#create-network-attachments). This field is only used for resources using PSC-I. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource_limits" type="map(string)">
          Optional. Resource limits for each container. Only 'cpu' and 'memory' keys are supported. Defaults to &#123;"cpu": "4", "memory": "4Gi"&#125;. * The only supported values for CPU are '1', '2', '4', '6' and '8'. For more information, go to https://cloud.google.com/run/docs/configuring/cpu. * The only supported values for memory are '1Gi', '2Gi', ... '32 Gi'. * For required cpu on different memory values, go to https://cloud.google.com/run/docs/configuring/memory-limits Optional lookup argument.
        </ResponseField>
        <ResponseField name="secret_env" type="list(object)">
          Optional. Environment variables where the value is a secret in Cloud Secret Manager. To use this feature, add 'Secret Manager Secret Accessor' role (roles/secretmanager.secretAccessor) to AI Platform Reasoning Engine Service Agent. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="effective_identity" type="string">
      Output only. The identity to use for the Reasoning Engine. It can contain one of the following values: * service-&#123;project&#125;@gcp-sa-aiplatform-re.googleapis.com (for SERVICE_AGENT identity type) * &#123;name&#125;@&#123;project&#125;.gserviceaccount.com (for SERVICE_ACCOUNT identity type) * agents.global.&#123;org&#125;.system.id.goog/resources/aiplatform/projects/&#123;project&#125;/locations/&#123;location&#125;/reasoningEngines/&#123;reasoning_engine&#125; (for AGENT_IDENTITY identity type) Returned by the lookup.
    </ResponseField>
    <ResponseField name="identity_type" type="string">
      Optional. The identity type to use for the Reasoning Engine. If not specified, the `service_account` field will be used if set, otherwise the default Vertex AI Reasoning Engine Service Agent in the project will be used. Optional lookup argument.
    </ResponseField>
    <ResponseField name="package_spec" type="object">
      User-provided package specification, containing pickled object and package requirements. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="dependency_files_gcs_uri" type="string">
          Optional. The Cloud Storage URI of the dependency files in tar.gz format. Optional lookup argument.
        </ResponseField>
        <ResponseField name="pickle_object_gcs_uri" type="string">
          Optional. The Cloud Storage URI of the pickled python object. Optional lookup argument.
        </ResponseField>
        <ResponseField name="python_version" type="string">
          Optional. The Python version. Supported values are 3.10, 3.11, 3.12, 3.13, 3.14. If not specified, the default value is 3.10. Optional lookup argument.
        </ResponseField>
        <ResponseField name="requirements_gcs_uri" type="string">
          Optional. The Cloud Storage URI of the `requirements.txt` file Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_account" type="string">
      Optional. The service account that the Reasoning Engine artifact runs as. It should have "roles/storage.objectViewer" for reading the user project's Cloud Storage and "roles/aiplatform.user" for using Vertex extensions. If not specified, the Vertex AI Reasoning Engine Service Agent in the project will be used. Optional lookup argument.
    </ResponseField>
    <ResponseField name="source_code_spec" type="object">
      Specification for deploying from source code. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="agent_config_source" type="object">
          Specification for the deploying from agent config. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="adk_config" type="object">
              Configuration for the Agent Development Kit (ADK). Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="json_config" type="map(any)">
                  Required. The value of the ADK config in JSON format. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="inline_source" type="object">
              Specifies source code provided as a byte stream. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="source_archive" type="string">
                  Required. Input only. The application source code archive. It must be a compressed tarball (.tar.gz) file. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="developer_connect_source" type="object">
          Specifies source code to be fetched from a Git repository managed through the Developer Connect service. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="config" type="object">
              Specifies the configuration for fetching source code from a Git repository that is managed by Developer Connect. This includes the repository, revision, and directory to use. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="dir" type="string">
                  Required. Directory, relative to the source root, in which to run the build. Optional lookup argument.
                </ResponseField>
                <ResponseField name="git_repository_link" type="string">
                  Required. The Developer Connect Git repository link, formatted as `projects/*/locations/*/connections/*/gitRepositoryLink/*`. Optional lookup argument.
                </ResponseField>
                <ResponseField name="revision" type="string">
                  Required. The revision to fetch from the Git repository such as a branch, a tag, a commit SHA, or any Git ref. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="image_spec" type="object">
          The image spec for building an image (within a single build step), based on the config file (i.e. Dockerfile) in the source directory. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="build_args" type="map(string)">
              Optional. Build arguments to be used. They will be passed through --build-arg flags. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="inline_source" type="object">
          Specifies source code provided as a byte stream. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="source_archive" type="string">
              Required. Input only. The application source code archive. It must be a compressed tarball (.tar.gz) file. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="python_spec" type="object">
          Specification for running a Python application from source. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="entrypoint_module" type="string">
              Optional. The Python module to load as the entrypoint, specified as a fully qualified module name. For example: path.to.agent. If not specified, defaults to "agent". The project root will be added to Python sys.path, allowing imports to be specified relative to the root. This field should not be set if the source is `agent_config_source`. Optional lookup argument.
            </ResponseField>
            <ResponseField name="entrypoint_object" type="string">
              Optional. The name of the callable object within the `entrypoint_module` to use as the application If not specified, defaults to "root_agent". This field should not be set if the source is `agent_config_source`. Optional lookup argument.
            </ResponseField>
            <ResponseField name="requirements_file" type="string">
              Optional. The path to the requirements file, relative to the source root. If not specified, defaults to "requirements.txt". Optional lookup argument.
            </ResponseField>
            <ResponseField name="version" type="string">
              Optional. The version of Python to use. Supported versions include 3.10, 3.11, 3.12, 3.13, 3.14. If not specified, default value is 3.10. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the revision. Returned by the lookup.
</ResponseField>
