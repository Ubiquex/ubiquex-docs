---
title: "google_aiplatform_v1beta1_analyzed_invocation"
description: "`data_aiplatform_v1beta1_analyzed_invocation` looks up a real, analyzed record of a single Vertex AI Agent Engine agent invocation, via the v1beta1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_aiplatform_v1beta1_analyzed_invocation"})

    		ubx.Data(aiplatform.V1beta1AnalyzedInvocation, "example", aiplatform.V1beta1AnalyzedInvocationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1beta1AnalyzedInvocation } from "./local-sdk/google/sdk/typescript/google/data/aiplatform/v1beta1_analyzed_invocation.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_aiplatform_v1beta1_analyzed_invocation" });

      data(V1beta1AnalyzedInvocation, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.aiplatform import V1beta1AnalyzedInvocation, V1beta1AnalyzedInvocationConfig

    def describe():
        ubx.intent("look up google_aiplatform_v1beta1_analyzed_invocation")

        ubx.data(V1beta1AnalyzedInvocation, "example", V1beta1AnalyzedInvocationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment" type="object">
  Contains the detailed assessment for an analyzed session. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="detector_findings" type="list(object)">
      The list of detector findings. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="invocation_id" type="string">
  Output only. The invocation ID. Returned by the lookup.
</ResponseField>

<ResponseField name="invocation_state" type="string">
  Output only. The state of the invocation. Returned by the lookup.
</ResponseField>
