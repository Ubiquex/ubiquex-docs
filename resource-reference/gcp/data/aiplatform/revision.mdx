---
title: "google_aiplatform_revision"
description: "`google_aiplatform_revision` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_aiplatform_revision` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_aiplatform_revision"})

    		ubx.Data(aiplatform.Revision, "example", aiplatform.RevisionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Revision } from "@ubx/sdk-google/google/data/aiplatform/revision";

    export default stack("example", () => {
      intent({ summary: "look up google_aiplatform_revision" });

      data(Revision, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.aiplatform import Revision, RevisionConfig

    def describe():
        ubx.intent("look up google_aiplatform_revision")

        ubx.data(Revision, "example", RevisionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. Represents the timestamp when this Memory Revision was created. Returned by the lookup.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Output only. Represents the timestamp of when this resource is considered expired. Returned by the lookup.
</ResponseField>

<ResponseField name="extracted_memories" type="list(object)">
  Output only. Represents the extracted memories from the source content before consolidation when the memory was updated via GenerateMemories. This information was used to modify an existing Memory via Consolidation. Returned by the lookup.
</ResponseField>

<ResponseField name="fact" type="string">
  Output only. Represents the fact of the Memory Revision. This corresponds to the `fact` field of the parent Memory at the time of revision creation. Returned by the lookup.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Output only. Represents the labels of the Memory Revision. These labels are applied to the MemoryRevision when it is created based on `GenerateMemoriesRequest.revision_labels`. Returned by the lookup.
</ResponseField>

<ResponseField name="structured_data" type="map(any)">
  Output only. Represents the structured value of the memory at the time of revision creation. Returned by the lookup.
</ResponseField>
