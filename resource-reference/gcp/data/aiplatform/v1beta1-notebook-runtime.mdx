---
title: "google_aiplatform_v1beta1_notebook_runtime"
description: "`google_aiplatform_v1beta1_notebook_runtime` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_aiplatform_v1beta1_notebook_runtime` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_aiplatform_v1beta1_notebook_runtime"})

    		ubx.Data(aiplatform.V1beta1NotebookRuntime, "example", aiplatform.V1beta1NotebookRuntimeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1beta1NotebookRuntime } from "@ubx/sdk-google/google/data/aiplatform/v1beta1_notebook_runtime";

    export default stack("example", () => {
      intent({ summary: "look up google_aiplatform_v1beta1_notebook_runtime" });

      data(V1beta1NotebookRuntime, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.aiplatform import V1beta1NotebookRuntime, V1beta1NotebookRuntimeConfig

    def describe():
        ubx.intent("look up google_aiplatform_v1beta1_notebook_runtime")

        ubx.data(V1beta1NotebookRuntime, "example", V1beta1NotebookRuntimeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp when this NotebookRuntime was created. Returned by the lookup.
</ResponseField>

<ResponseField name="data_persistent_disk_spec" type="object">
  Represents the spec of persistent disk and hyperdisk options. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="disk_size_gb" type="string">
      Size in GB of the disk (default is 100GB). Optional lookup argument.
    </ResponseField>
    <ResponseField name="disk_type" type="string">
      Type of the disk (default is "pd-standard"). Valid values: "pd-ssd" (Persistent Disk Solid State Drive) "pd-standard" (Persistent Disk Hard Disk Drive) "pd-balanced" (Balanced Persistent Disk) "pd-extreme" (Extreme Persistent Disk) "hyperdisk-balanced" (Hyperdisk Balanced) "hyperdisk-extreme" (Hyperdisk Extreme) "hyperdisk-balanced-high-availability" (Hyperdisk Balanced High Availability) "hyperdisk-ml" (Hyperdisk ML) "hyperdisk-throughput" (Hyperdisk Throughput) Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  The description of the NotebookRuntime. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The display name of the NotebookRuntime. The name can be up to 128 characters long and can consist of any UTF-8 characters. Returned by the lookup.
</ResponseField>

<ResponseField name="encryption_spec" type="object">
  Represents a customer-managed encryption key specification that can be applied to a Vertex AI resource. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string">
      Required. Resource name of the Cloud KMS key used to protect the resource. The Cloud KMS key must be in the same region as the resource. It must have the format `projects/&#123;project&#125;/locations/&#123;location&#125;/keyRings/&#123;key_ring&#125;/cryptoKeys/&#123;crypto_key&#125;`. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="euc_config" type="object">
  The euc configuration of NotebookRuntimeTemplate. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="bypass_actas_check" type="bool">
      Output only. Whether ActAs check is bypassed for service account attached to the VM. If false, we need ActAs check for the default Compute Engine Service account. When a Runtime is created, a VM is allocated using Default Compute Engine Service Account. Any user requesting to use this Runtime requires Service Account User (ActAs) permission over this SA. If true, Runtime owner is using EUC and does not require the above permission as VM no longer use default Compute Engine SA, but a P4SA. Returned by the lookup.
    </ResponseField>
    <ResponseField name="euc_disabled" type="bool">
      Input only. Whether EUC is disabled in this NotebookRuntimeTemplate. In proto3, the default value of a boolean is false. In this way, by default EUC will be enabled for NotebookRuntimeTemplate. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="expiration_time" type="string">
  Output only. Timestamp when this NotebookRuntime will be expired: 1. System Predefined NotebookRuntime: 24 hours after creation. After expiration, system predifined runtime will be deleted. 2. User created NotebookRuntime: 6 months after last upgrade. After expiration, user created runtime will be stopped and allowed for upgrade. Returned by the lookup.
</ResponseField>

<ResponseField name="health_state" type="string">
  Output only. The health state of the NotebookRuntime. Returned by the lookup.
</ResponseField>

<ResponseField name="idle_shutdown_config" type="object">
  The idle shutdown configuration of NotebookRuntimeTemplate, which contains the idle_timeout as required field. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="idle_shutdown_disabled" type="bool">
      Whether Idle Shutdown is disabled in this NotebookRuntimeTemplate. Optional lookup argument.
    </ResponseField>
    <ResponseField name="idle_timeout" type="string">
      Required. Duration is accurate to the second. In Notebook, Idle Timeout is accurate to minute so the range of idle_timeout (second) is: 10 * 60 ~ 1440 * 60. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="is_upgradable" type="bool">
  Output only. Whether NotebookRuntime is upgradable. Returned by the lookup.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  The labels with user-defined metadata to organize your NotebookRuntime. Label keys and values can be no longer than 64 characters (Unicode codepoints), can only contain lowercase letters, numeric characters, underscores and dashes. International characters are allowed. No more than 64 user labels can be associated with one NotebookRuntime (System labels are excluded). See https://goo.gl/xmQnxf for more information and examples of labels. System reserved label keys are prefixed with "aiplatform.googleapis.com/" and are immutable. Following system labels exist for NotebookRuntime: * "aiplatform.googleapis.com/notebook_runtime_gce_instance_id": output only, its value is the Compute Engine instance id. * "aiplatform.googleapis.com/colab_enterprise_entry_service": its value is either "bigquery" or "vertex"; if absent, it should be "vertex". This is to describe the entry service, either BigQuery or Vertex. Returned by the lookup.
</ResponseField>

<ResponseField name="machine_spec" type="object">
  Specification of a single machine. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="accelerator_count" type="number">
      The number of accelerators to attach to the machine. For [accelerator optimized machine types](https://cloud.google.com/compute/docs/accelerator-optimized-machines), One may set the accelerator_count from 1 to N for machine with N GPUs. If accelerator_count is less than or equal to N / 2, Agent Platform co-schedules the replicas of the model into the same VM to save cost. For example, if the machine type is a3-highgpu-8g, which has 8 H100 GPUs, one can set accelerator_count to 1 to 8. If accelerator_count is 1, 2, 3, or 4, Agent Platform co-schedules 8, 4, 2, or 2 replicas of the model into the same VM to save cost. When co-scheduling, CPU, memory and storage on the VM will be distributed to replicas on the VM. For example, one can expect a co-scheduled replica requesting 2 GPUs out of a 8-GPU VM will receive 25% of the CPU, memory and storage of the VM. Note that the feature is not compatible with multihost_gpu_node_count. When multihost_gpu_node_count is set, the co-scheduling will not be enabled. Optional lookup argument.
    </ResponseField>
    <ResponseField name="accelerator_type" type="string">
      Immutable. The type of accelerator(s) that may be attached to the machine as per accelerator_count. Optional lookup argument.
    </ResponseField>
    <ResponseField name="gpu_partition_size" type="string">
      Optional. Immutable. The Nvidia GPU partition size. When specified, the requested accelerators will be partitioned into smaller GPU partitions. For example, if the request is for 8 units of NVIDIA A100 GPUs, and gpu_partition_size="1g.10gb", the service will create 8 * 7 = 56 partitioned MIG instances. The partition size must be a value supported by the requested accelerator. Refer to [Nvidia GPU Partitioning](https://cloud.google.com/kubernetes-engine/docs/how-to/gpus-multi#multi-instance_gpu_partitions) for the available partition sizes. If set, the accelerator_count should be set to 1. Optional lookup argument.
    </ResponseField>
    <ResponseField name="machine_type" type="string">
      Immutable. The type of the machine. See the [list of machine types supported for prediction](https://cloud.google.com/gemini-enterprise-agent-platform/machine-learning/predictions/configure-compute#machine-types) See the [list of machine types supported for custom training](https://cloud.google.com/gemini-enterprise-agent-platform/machine-learning/training/configure-compute#machine-types). For DeployedModel this field is optional, and the default value is `n1-standard-2`. For BatchPredictionJob or as part of WorkerPoolSpec this field is required. Optional lookup argument.
    </ResponseField>
    <ResponseField name="min_gpu_driver_version" type="string">
      Optional. Immutable. The minimum GPU driver version that this machine requires. For example, "535.104.06". If not specified, the default GPU driver version will be used by the underlying infrastructure. Optional lookup argument.
    </ResponseField>
    <ResponseField name="multihost_gpu_node_count" type="number">
      Optional. Immutable. The number of nodes per replica for multihost GPU deployments. Optional lookup argument.
    </ResponseField>
    <ResponseField name="reservation_affinity" type="object">
      A ReservationAffinity can be used to configure a Vertex AI resource (e.g., a DeployedModel) to draw its Compute Engine resources from a Shared Reservation, or exclusively from on-demand capacity. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          Optional. Corresponds to the label key of a reservation resource. To target a SPECIFIC_RESERVATION by name, use `compute.googleapis.com/reservation-name` as the key and specify the name of your reservation as its value. Optional lookup argument.
        </ResponseField>
        <ResponseField name="reservation_affinity_type" type="string">
          Required. Specifies the reservation affinity type. Optional lookup argument.
        </ResponseField>
        <ResponseField name="values" type="list(string)">
          Optional. Corresponds to the label values of a reservation resource. This must be the resource name of the reservation, reservation block, or reservation sub- block. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tpu_topology" type="string">
      Immutable. The topology of the TPUs. Corresponds to the TPU topologies available from GKE. (Example: tpu_topology: "2x2x1"). Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network_spec" type="object">
  Network spec. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="enable_internet_access" type="bool">
      Whether to enable public internet access. Default false. Optional lookup argument.
    </ResponseField>
    <ResponseField name="network" type="string">
      The full name of the Google Compute Engine [network](https://cloud.google.com//compute/docs/networks-and-firewalls#networks) Optional lookup argument.
    </ResponseField>
    <ResponseField name="subnetwork" type="string">
      The name of the subnet that this instance is in. Format: `projects/&#123;project_id_or_number&#125;/regions/&#123;region&#125;/subnetworks/&#123;subnetwork_id&#125;` Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network_tags" type="list(string)">
  Optional. The Compute Engine tags to add to runtime (see [Tagging instances](https://cloud.google.com/vpc/docs/add-remove-network-tags)). Returned by the lookup.
</ResponseField>

<ResponseField name="notebook_runtime_template_ref" type="object">
  Points to a NotebookRuntimeTemplateRef. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="notebook_runtime_template" type="string">
      Immutable. A resource name of the NotebookRuntimeTemplate. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="notebook_runtime_type" type="string">
  Output only. The type of the notebook runtime. Returned by the lookup.
</ResponseField>

<ResponseField name="proxy_uri" type="string">
  Output only. The proxy endpoint used to access the NotebookRuntime. Returned by the lookup.
</ResponseField>

<ResponseField name="reservation_affinity" type="object">
  Notebook Reservation Affinity for consuming Zonal reservation. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="consume_reservation_type" type="string">
      Required. Specifies the type of reservation from which this instance can consume resources: RESERVATION_ANY (default), RESERVATION_SPECIFIC, or RESERVATION_NONE. See Consuming reserved instances for examples. Optional lookup argument.
    </ResponseField>
    <ResponseField name="key" type="string">
      Optional. Corresponds to the label key of a reservation resource. To target a RESERVATION_SPECIFIC by name, use compute.googleapis.com/reservation-name as the key and specify the name of your reservation as its value. Optional lookup argument.
    </ResponseField>
    <ResponseField name="values" type="list(string)">
      Optional. Corresponds to the label values of a reservation resource. This must be the full path name of Reservation. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="runtime_state" type="string">
  Output only. The runtime (instance) state of the NotebookRuntime. Returned by the lookup.
</ResponseField>

<ResponseField name="runtime_user" type="string">
  Required. The user email of the NotebookRuntime. Returned by the lookup.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="service_account" type="string">
  Output only. Deprecated: This field is no longer used and the "Vertex AI Notebook Service Account" (service-PROJECT_NUMBER@gcp-sa-aiplatform-vm.iam.gserviceaccount.com) is used for the runtime workload identity. See https://cloud.google.com/iam/docs/service-agents#vertex-ai-notebook-service-account for more details. The service account that the NotebookRuntime workload runs as. Returned by the lookup.
</ResponseField>

<ResponseField name="shielded_vm_config" type="object">
  A set of Shielded Instance options. See [Images using supported Shielded VM features](https://cloud.google.com/compute/docs/instances/modifying-shielded-vm). Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="enable_secure_boot" type="bool">
      Defines whether the instance has [Secure Boot](https://cloud.google.com/compute/shielded-vm/docs/shielded-vm#secure-boot) enabled. Secure Boot helps ensure that the system only runs authentic software by verifying the digital signature of all boot components, and halting the boot process if signature verification fails. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="software_config" type="object">
  Notebook Software Config. This is passed to the backend when user makes software configurations in UI. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="colab_image" type="object">
      Colab image of the runtime. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Output only. A human-readable description of the specified colab image release, populated by the system. Example: "Python 3.10", "Latest - current Python 3.11" Returned by the lookup.
        </ResponseField>
        <ResponseField name="release_name" type="string">
          Optional. The release name of the NotebookRuntime Colab image, e.g. "py310". If not specified, detault to the latest release. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="env" type="list(object)">
      Optional. Environment variables to be passed to the container. Maximum limit is 100. Optional lookup argument.
    </ResponseField>
    <ResponseField name="post_startup_script_config" type="object">
      Post startup script config. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="post_startup_script" type="string">
          Optional. Post startup script to run after runtime is started. Optional lookup argument.
        </ResponseField>
        <ResponseField name="post_startup_script_behavior" type="string">
          Optional. Post startup script behavior that defines download and execution behavior. Optional lookup argument.
        </ResponseField>
        <ResponseField name="post_startup_script_url" type="string">
          Optional. Post startup script url to download. Example: `gs://bucket/script.sh` Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when this NotebookRuntime was most recently updated. Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  Output only. The VM os image version of NotebookRuntime. Returned by the lookup.
</ResponseField>
