---
title: "google_aiplatform_v1beta1_nas_trial_detail"
description: "`data_aiplatform_v1beta1_nas_trial_detail` looks up a real, already-existing `google_aiplatform_trial`."
---

It is a single parameter combination evaluated as part of a Vertex AI Vizier study or hyperparameter tuning job, recording the parameter values tried and the resulting objective metric value.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	aiplatform "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/aiplatform"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_aiplatform_v1beta1_nas_trial_detail"})

    		ubx.Data(aiplatform.V1beta1NasTrialDetail, "example", aiplatform.V1beta1NasTrialDetailConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1beta1NasTrialDetail } from "./local-sdk/google/sdk/typescript/google/data/aiplatform/v1beta1_nas_trial_detail.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_aiplatform_v1beta1_nas_trial_detail" });

      data(V1beta1NasTrialDetail, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.aiplatform import V1beta1NasTrialDetail, V1beta1NasTrialDetailConfig

    def describe():
        ubx.intent("look up google_aiplatform_v1beta1_nas_trial_detail")

        ubx.data(V1beta1NasTrialDetail, "example", V1beta1NasTrialDetailConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="parameters" type="string">
  The parameters for the NasJob NasTrial. Returned by the lookup.
</ResponseField>

<ResponseField name="search_trial" type="object">
  Represents a uCAIP NasJob trial. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
      Output only. Time when the NasTrial's status changed to `SUCCEEDED` or `INFEASIBLE`. Returned by the lookup.
    </ResponseField>
    <ResponseField name="final_measurement" type="object">
      A message representing a Measurement of a Trial. A Measurement contains the Metrics got by executing a Trial using suggested hyperparameter values. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="elapsed_duration" type="string">
          Output only. Time that the Trial has been running at the point of this Measurement. Returned by the lookup.
        </ResponseField>
        <ResponseField name="metrics" type="list(object)">
          Output only. A list of metrics got by evaluating the objective functions using suggested Parameter values. Returned by the lookup.
        </ResponseField>
        <ResponseField name="step_count" type="string">
          Output only. The number of steps the machine learning model has been trained for. Must be non-negative. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      Output only. The identifier of the NasTrial assigned by the service. Returned by the lookup.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Output only. Time when the NasTrial was started. Returned by the lookup.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The detailed state of the NasTrial. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="train_trial" type="object">
  Represents a uCAIP NasJob trial. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
      Output only. Time when the NasTrial's status changed to `SUCCEEDED` or `INFEASIBLE`. Returned by the lookup.
    </ResponseField>
    <ResponseField name="final_measurement" type="object">
      A message representing a Measurement of a Trial. A Measurement contains the Metrics got by executing a Trial using suggested hyperparameter values. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="elapsed_duration" type="string">
          Output only. Time that the Trial has been running at the point of this Measurement. Returned by the lookup.
        </ResponseField>
        <ResponseField name="metrics" type="list(object)">
          Output only. A list of metrics got by evaluating the objective functions using suggested Parameter values. Returned by the lookup.
        </ResponseField>
        <ResponseField name="step_count" type="string">
          Output only. The number of steps the machine learning model has been trained for. Must be non-negative. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      Output only. The identifier of the NasTrial assigned by the service. Returned by the lookup.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Output only. Time when the NasTrial was started. Returned by the lookup.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The detailed state of the NasTrial. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
