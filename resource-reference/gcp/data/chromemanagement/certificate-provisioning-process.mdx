---
title: "google_chromemanagement_certificate_provisioning_process"
description: "`google_chromemanagement_certificate_provisioning_process` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_chromemanagement_certificate_provisioning_process` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chromemanagement "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/chromemanagement"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_chromemanagement_certificate_provisioning_process"})

    		ubx.Data(chromemanagement.CertificateProvisioningProcess, "example", chromemanagement.CertificateProvisioningProcessConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CertificateProvisioningProcess } from "@ubx/sdk-google/google/data/chromemanagement/certificate_provisioning_process";

    export default stack("example", () => {
      intent({
        summary: "look up google_chromemanagement_certificate_provisioning_process",
      });

      data(CertificateProvisioningProcess, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.chromemanagement import CertificateProvisioningProcess, CertificateProvisioningProcessConfig

    def describe():
        ubx.intent("look up google_chromemanagement_certificate_provisioning_process")

        ubx.data(CertificateProvisioningProcess, "example", CertificateProvisioningProcessConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="chrome_os_device" type="object">
  Describes the ChromeOS device that a `CertificateProvisioningProcess` belongs to. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="device_directory_api_id" type="string">
      Output only. The unique Directory API ID of the device. This value is the same as the Admin Console's Directory API ID in the ChromeOS Devices tab. Returned by the lookup.
    </ResponseField>
    <ResponseField name="serial_number" type="string">
      Output only. Device serial number. This value is the same as the Admin Console's Serial Number in the ChromeOS Devices tab. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="chrome_os_user_session" type="object">
  Describes the ChromeOS user session that a `CertificateProvisioningProcess` belongs to. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="chrome_os_device" type="object">
      Describes the ChromeOS device that a `CertificateProvisioningProcess` belongs to. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="device_directory_api_id" type="string">
          Output only. The unique Directory API ID of the device. This value is the same as the Admin Console's Directory API ID in the ChromeOS Devices tab. Returned by the lookup.
        </ResponseField>
        <ResponseField name="serial_number" type="string">
          Output only. Device serial number. This value is the same as the Admin Console's Serial Number in the ChromeOS Devices tab. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="user_directory_api_id" type="string">
      Output only. The unique Directory API ID of the user. Returned by the lookup.
    </ResponseField>
    <ResponseField name="user_primary_email" type="string">
      Output only. The primary e-mail address of the user. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="failure_message" type="string">
  Output only. A message describing why this `CertificateProvisioningProcess` has failed. Presence of this field indicates that the `CertificateProvisioningProcess` has failed. Returned by the lookup.
</ResponseField>

<ResponseField name="generic_ca_connection" type="object">
  Describes a generic Certificate Authority Connection. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="ca_connection_adapter_config_reference" type="string">
      Output only. A string that references the administrator-provided configuration for the certification authority service. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="generic_profile" type="object">
  Describes a generic certificate provisioning profile. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="profile_adapter_config_reference" type="string">
      Output only. A string that references the administrator-provided configuration for the certificate provisioning profile. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="issued_certificate" type="string">
  Output only. The issued certificate for this `CertificateProvisioningProcess` in PEM format. Returned by the lookup.
</ResponseField>

<ResponseField name="provisioning_profile_id" type="string">
  Output only. The ID of the certificate provisioning profile. Returned by the lookup.
</ResponseField>

<ResponseField name="scep_ca_connection" type="object">
  Describes a SCEP Certificate Authority Connection. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="ca_connection_adapter_config_reference" type="string">
      Output only. A string that references the administrator-provided configuration for the certification authority service. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scep_profile" type="object">
  Describes a SCEP certificate provisioning profile. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="certificate_template_name" type="string">
      Output only. The certificate template name as defined by the admin on their on-prem infrastructure. The Certificate Authority uses this name to identify the certificate template. Returned by the lookup.
    </ResponseField>
    <ResponseField name="country" type="string">
      Output only. The country of the subject. Returned by the lookup.
    </ResponseField>
    <ResponseField name="key_usages" type="list(string)">
      Output only. The allowed key usages for certificate's key. Returned by the lookup.
    </ResponseField>
    <ResponseField name="locality" type="string">
      Output only. The locality of the subject. Returned by the lookup.
    </ResponseField>
    <ResponseField name="organization" type="string">
      Output only. The name of the organization the subject belongs to. Returned by the lookup.
    </ResponseField>
    <ResponseField name="organizational_units" type="list(string)">
      Output only. The organizational units of the subject. Returned by the lookup.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The state of the subject. Returned by the lookup.
    </ResponseField>
    <ResponseField name="subject_alt_names" type="list(object)">
      Output only. The subject alternative names. Returned by the lookup.
    </ResponseField>
    <ResponseField name="subject_common_name" type="string">
      Output only. The common name of the subject. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sign_data" type="string">
  Output only. The data that the client was asked to sign. This field is only present after the `SignData` operation has been initiated. Returned by the lookup.
</ResponseField>

<ResponseField name="signature" type="string">
  Output only. The signature of `signature_algorithm`, generated using the client's private key using `signature_algorithm`. This field is only present after the `SignData` operation has finished. Returned by the lookup.
</ResponseField>

<ResponseField name="signature_algorithm" type="string">
  Output only. The signature algorithm that the client and backend components use when processing `sign_data`. If the `profile_type` is a `GenericProfile`, this field will only be present after the `SignData` operation was initiated. If the `profile_type` is a `ScepProfile`, the field will always be present. Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  Output only. Server-generated timestamp of when the certificate provisioning process has been created. Returned by the lookup.
</ResponseField>

<ResponseField name="subject_public_key_info" type="string">
  Output only. The public key for which a certificate should be provisioned. Represented as a DER-encoded X.509 SubjectPublicKeyInfo. Returned by the lookup.
</ResponseField>
