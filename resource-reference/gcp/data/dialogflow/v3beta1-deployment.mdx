---
title: "google_dialogflow_v3beta1_deployment"
description: "`data_dialogflow_v3beta1_deployment` looks up a real deployment of a Dialogflow CX flow version, via the v3beta1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dialogflow "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/dialogflow"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_dialogflow_v3beta1_deployment"})

    		ubx.Data(dialogflow.V3beta1Deployment, "example", dialogflow.V3beta1DeploymentConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V3beta1Deployment } from "@ubx/sdk-google/google/data/dialogflow/v3beta1_deployment";

    export default stack("example", () => {
      intent({ summary: "look up google_dialogflow_v3beta1_deployment" });

      data(V3beta1Deployment, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.dialogflow import V3beta1Deployment, V3beta1DeploymentConfig

    def describe():
        ubx.intent("look up google_dialogflow_v3beta1_deployment")

        ubx.data(V3beta1Deployment, "example", V3beta1DeploymentConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="flow_version" type="string">
  The real version of the Dialogflow CX flow this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="result" type="object">
  The real result of this operation. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="deployment_test_results" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="experiment" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
