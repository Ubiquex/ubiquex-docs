---
title: "google_dataflow_snapshot"
description: "`data_dataflow_snapshot` looks up a real, saved snapshot of a running Dataflow pipeline's own state."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataflow "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/dataflow"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_dataflow_snapshot"})

    		ubx.Data(dataflow.Snapshot, "example", dataflow.SnapshotConfig{
    			Location:   "example",
    			ProjectId:  "example",
    			SnapshotId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { Snapshot } from "./local-sdk/google/sdk/typescript/google/data/dataflow/snapshot.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_dataflow_snapshot" });

      data(Snapshot, "example", {
        location: "example",
        projectId: "example",
        snapshotId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.dataflow import Snapshot, SnapshotConfig

    def describe():
        ubx.intent("look up google_dataflow_snapshot")

        ubx.data(Snapshot, "example", SnapshotConfig(
            location="example",
            project_id="example",
            snapshot_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="location" type="string" required>
  The real Google Cloud location (region or zone) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="project_id" type="string" required>
  The real Google Cloud project ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="snapshot_id" type="string" required>
  The real, unique identifier of the snapshot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_time" type="string">
  The time this snapshot was created. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  User specified description of the snapshot. Maybe empty. Returned by the lookup.
</ResponseField>

<ResponseField name="disk_size_bytes" type="string">
  The disk byte size of the snapshot. Only available for snapshots in READY state. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  The unique ID of this snapshot. Returned by the lookup.
</ResponseField>

<ResponseField name="pubsub_metadata" type="list(object)">
  Pub/Sub snapshot metadata. Returned by the lookup.
</ResponseField>

<ResponseField name="region" type="string">
  Cloud region where this snapshot lives in, e.g., "us-central1". Returned by the lookup.
</ResponseField>

<ResponseField name="source_job_id" type="string">
  The job this snapshot was created from. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  State of the snapshot. Returned by the lookup.
</ResponseField>

<ResponseField name="ttl" type="string">
  The time after which this snapshot will be automatically deleted. Returned by the lookup.
</ResponseField>
