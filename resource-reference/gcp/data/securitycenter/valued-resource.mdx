---
title: "google_securitycenter_valued_resource"
description: "`data_securitycenter_valued_resource` looks up a real resource Security Command Center has scored for its own business value and exposure."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	securitycenter "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/securitycenter"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_securitycenter_valued_resource"})

    		ubx.Data(securitycenter.ValuedResource, "example", securitycenter.ValuedResourceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ValuedResource } from "@ubx/sdk-google/google/data/securitycenter/valued_resource";

    export default stack("example", () => {
      intent({ summary: "look up google_securitycenter_valued_resource" });

      data(ValuedResource, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.securitycenter import ValuedResource, ValuedResourceConfig

    def describe():
        ubx.intent("look up google_securitycenter_valued_resource")

        ubx.data(ValuedResource, "example", ValuedResourceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="display_name" type="string">
  The real, human-readable display name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="exposed_score" type="number">
  The real, computed exposure score for this resource. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource" type="string">
  The real resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_value" type="string">
  The real, assigned business-value rating of this resource. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_value_configs_used" type="list(object)">
  The real resource-value configurations used to compute this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
