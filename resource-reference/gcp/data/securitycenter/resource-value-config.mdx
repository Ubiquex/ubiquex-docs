---
title: "google_securitycenter_resource_value_config"
description: "`google_securitycenter_resource_value_config` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_securitycenter_resource_value_config` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	securitycenter "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/securitycenter"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_securitycenter_resource_value_config"})

    		ubx.Data(securitycenter.ResourceValueConfig, "example", securitycenter.ResourceValueConfigConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceValueConfig } from "@ubx/sdk-google/google/data/securitycenter/resource_value_config";

    export default stack("example", () => {
      intent({ summary: "look up google_securitycenter_resource_value_config" });

      data(ResourceValueConfig, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.securitycenter import ResourceValueConfig, ResourceValueConfigConfig

    def describe():
        ubx.intent("look up google_securitycenter_resource_value_config")

        ubx.data(ResourceValueConfig, "example", ResourceValueConfigConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cloud_provider" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_labels_selector" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_value" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scope" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sensitive_data_protection_mapping" type="object">
  Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="high_sensitivity_mapping" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="medium_sensitivity_mapping" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tag_values" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Returned by the lookup.
</ResponseField>
