---
title: "google_securitycenter_effective_custom_module"
description: "`data_securitycenter_effective_custom_module` looks up a real, already-existing `google_securitycenter_custom_module`."
---

It defines a caller-written detection rule Security Command Center's Security Health Analytics evaluates alongside its own built-in detectors.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	securitycenter "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/securitycenter"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_securitycenter_effective_custom_module"})

    		ubx.Data(securitycenter.EffectiveCustomModule, "example", securitycenter.EffectiveCustomModuleConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { EffectiveCustomModule } from "./local-sdk/google/sdk/typescript/google/data/securitycenter/effective_custom_module.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_securitycenter_effective_custom_module" });

      data(EffectiveCustomModule, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.securitycenter import EffectiveCustomModule, EffectiveCustomModuleConfig

    def describe():
        ubx.intent("look up google_securitycenter_effective_custom_module")

        ubx.data(EffectiveCustomModule, "example", EffectiveCustomModuleConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cloud_provider" type="string">
  The real cloud provider this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="config" type="map(any)">
  The real, structured configuration of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  The real, human-readable display name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="enablement_state" type="string">
  The real, current enablement state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
