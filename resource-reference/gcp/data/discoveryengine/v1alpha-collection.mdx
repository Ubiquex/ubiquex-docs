---
title: "google_discoveryengine_v1alpha_collection"
description: "`data_discoveryengine_v1alpha_collection` looks up a real collection, a top-level grouping of Discovery Engine data stores and apps, via the v1alpha API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discoveryengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/discoveryengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_discoveryengine_v1alpha_collection"})

    		ubx.Data(discoveryengine.V1alphaCollection, "example", discoveryengine.V1alphaCollectionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1alphaCollection } from "./local-sdk/google/sdk/typescript/google/data/discoveryengine/v1alpha_collection.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_discoveryengine_v1alpha_collection" });

      data(V1alphaCollection, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.discoveryengine import V1alphaCollection, V1alphaCollectionConfig

    def describe():
        ubx.intent("look up google_discoveryengine_v1alpha_collection")

        ubx.data(V1alphaCollection, "example", V1alphaCollectionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. Timestamp the Collection was created at. Returned by the lookup.
</ResponseField>

<ResponseField name="data_connector" type="object">
  Manages the connection to external data sources for all data stores grouped under a Collection. It's a singleton resource of Collection. The initialization is only supported through DataConnectorService.SetUpDataConnector method, which will create a new Collection and initialize its DataConnector. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="acl_enabled" type="bool">
      Optional. Whether the connector will be created with an ACL config. Currently this field only affects Cloud Storage and BigQuery connectors. Optional lookup argument.
    </ResponseField>
    <ResponseField name="action_config" type="object">
      Informations to support actions on the connector. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="action_params" type="map(any)">
          Optional. Action parameters in structured json format. Optional lookup argument.
        </ResponseField>
        <ResponseField name="create_bap_connection" type="bool">
          Optional. Whether to create a BAP connection for the connector. Optional lookup argument.
        </ResponseField>
        <ResponseField name="is_action_configured" type="bool">
          Output only. The connector contains the necessary parameters and is configured to support actions. Returned by the lookup.
        </ResponseField>
        <ResponseField name="json_action_params" type="string">
          Optional. Action parameters in json string format. Optional lookup argument.
        </ResponseField>
        <ResponseField name="service_name" type="string">
          Optional. The Service Directory resource name (projects/*/locations/*/namespaces/*/services/*) representing a VPC network endpoint used to connect to the data source's `instance_uri`, defined in DataConnector.params. Required when VPC Service Controls are enabled. Optional lookup argument.
        </ResponseField>
        <ResponseField name="use_static_secrets" type="bool">
          Optional. Whether to use static secrets for the connector. If true, the secrets provided in the action_params will be ignored. Optional lookup argument.
        </ResponseField>
        <ResponseField name="user_defined_scopes_mapping" type="map(object)">
          Optional. Mapping from operation name to the list of scopes. Only be populated if there are user specified scopes. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="action_state" type="string">
      Output only. State of the action connector. This reflects whether the action connector is initializing, active or has encountered errors. Returned by the lookup.
    </ResponseField>
    <ResponseField name="alert_policy_configs" type="list(object)">
      Optional. The connector level alert config. Optional lookup argument.
    </ResponseField>
    <ResponseField name="auto_run_disabled" type="bool">
      Optional. Indicates whether the connector is disabled for auto run. It can be used to pause periodical and real time sync. Update: with the introduction of incremental_sync_disabled, auto_run_disabled is used to pause/disable only full syncs Optional lookup argument.
    </ResponseField>
    <ResponseField name="bap_config" type="object">
      The configuration for the BAP connector. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="enabled_actions" type="list(string)">
          Optional. The actions enabled on the associated BAP connection. Optional lookup argument.
        </ResponseField>
        <ResponseField name="supported_connector_modes" type="list(string)">
          Required. The supported connector modes for the associated BAP connection. Optional lookup argument.
        </ResponseField>
        <ResponseField name="toolspec_override" type="object">
          Customer-facing view of the admin-curated toolspec for a BAP connection. Holds the (simplified) per-tool definitions surfaced to and editable by the admin in the Discovery Engine UI. Mirrors the shape of the backend `google.cloud.connectorexecution.v1.ToolspecOverride` message, but is intentionally kept as a separate public type so the Discovery Engine API surface can evolve independently of the fed-API surface (AIP-215). Handlers convert between the two via helpers in //cloud/ml/discoveryengine/external_service/v1main/data_connector_service/lib:bap_custom_tool_util. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="base_version" type="string">
              Required. Base toolspec version against which `tools` were authored. On Update, MUST match the server's current stable toolspec version for the connection; mismatch is rejected with a user-facing error directing the admin to re-download the latest tools first. Optional lookup argument.
            </ResponseField>
            <ResponseField name="tools" type="list(map(any))">
              Required. Tool definitions (one Struct per tool) that the admin has customised on top of the base toolspec returned by the fed API. REQUIRED because it is the only user-editable field in the modify API; the request must carry at least one tool. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="blocking_reasons" type="list(string)">
      Output only. User actions that must be completed before the connector can start syncing data. Returned by the lookup.
    </ResponseField>
    <ResponseField name="cli_config" type="object">
      The configuration for the CLI execution based connectors. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="enabled_actions" type="list(string)">
          Optional. The actions enabled on the associated CLI connection. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="connector_modes" type="list(string)">
      Optional. The modes enabled for this connector. Default state is CONNECTOR_MODE_UNSPECIFIED. Optional lookup argument.
    </ResponseField>
    <ResponseField name="connector_source_id" type="string">
      Optional. If set, this value instead of `data_source` is used to fetch the corresponding connector source. Optional lookup argument.
    </ResponseField>
    <ResponseField name="connector_type" type="string">
      Output only. The type of connector. Each source can only map to one type. For example, salesforce, confluence and jira have THIRD_PARTY connector type. It is not mutable once set by system. Returned by the lookup.
    </ResponseField>
    <ResponseField name="create_eua_saas" type="bool">
      Optional. Whether the END USER AUTHENTICATION connector is created in SaaS. Optional lookup argument.
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. Timestamp the DataConnector was created at. Returned by the lookup.
    </ResponseField>
    <ResponseField name="data_protection_policy" type="object">
      Contains the data protection policy config for a DataStore or a connector. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="sensitive_data_protection_policy" type="object">
          Specifies a Sensitive Data Protection (https://cloud.google.com/sensitive-data-protection/docs/sensitive-data-protection-overview) policy. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="policy" type="string">
              Optional. Specifies the resource name of the Sensitive Data Protection content policy. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="data_source" type="string">
      Required. The identifier for the data source. For the full, up-to-date list of supported connectors and their values, see [Connect a third-party data source](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/connect-third-party-data-source#sources-by-launch-stage). Optional lookup argument.
    </ResponseField>
    <ResponseField name="destination_configs" type="list(object)">
      Optional. Any target destinations used to connect to third-party services. Optional lookup argument.
    </ResponseField>
    <ResponseField name="dynamic_tools" type="list(object)">
      Output only. The dynamic tools fetched for this connector. Returned by the lookup.
    </ResponseField>
    <ResponseField name="egress_fqdns" type="list(string)">
      Output only. The list of FQDNs of the data connector can egress to. This includes both FQDN derived from the customer provided instance URL and default per connector type FQDNs. Returned by the lookup.
    </ResponseField>
    <ResponseField name="end_user_config" type="object">
      Any params and credentials used specifically for EUA connectors. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="additional_params" type="map(any)">
          Optional. Any additional parameters needed for EUA. Optional lookup argument.
        </ResponseField>
        <ResponseField name="auth_params" type="map(any)">
          Optional. Any authentication parameters specific to EUA connectors. Optional lookup argument.
        </ResponseField>
        <ResponseField name="json_auth_params" type="string">
          Optional. Any authentication parameters specific to EUA connectors in json string format. Optional lookup argument.
        </ResponseField>
        <ResponseField name="tenant" type="object">
          Tenant information for a connector source. This includes some of the same information stored in the Credential message, but is limited to only what is needed to provide a list of accessible tenants to the user. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="display_name" type="string">
              Optional display name for the tenant, e.g. "My Slack Team". Optional lookup argument.
            </ResponseField>
            <ResponseField name="id" type="string">
              The tenant's instance ID. Examples: Jira ("8594f221-9797-5f78-1fa4-485e198d7cd0"), Slack ("T123456"). Optional lookup argument.
            </ResponseField>
            <ResponseField name="uri" type="string">
              The URI of the tenant, if applicable. For example, the URI of a Jira instance is https://my-jira-instance.atlassian.net, and a Slack tenant does not have a URI. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="entities" type="list(object)">
      List of entities from the connected data source to ingest. Optional lookup argument.
    </ResponseField>
    <ResponseField name="errors" type="list(object)">
      Output only. The errors from initialization or from the latest connector run. Returned by the lookup.
    </ResponseField>
    <ResponseField name="federated_config" type="object">
      Any params and credentials used specifically for hybrid connectors supporting FEDERATED mode. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="additional_params" type="map(any)">
          Optional. Any additional parameters needed for FEDERATED. Optional lookup argument.
        </ResponseField>
        <ResponseField name="auth_params" type="map(any)">
          Optional. Any authentication parameters specific to FEDERATED connectors. Optional lookup argument.
        </ResponseField>
        <ResponseField name="json_auth_params" type="string">
          Optional. Any authentication parameters specific to FEDERATED connectors in json string format. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hybrid_ingestion_disabled" type="bool">
      Optional. If the connector is a hybrid connector, determines whether ingestion is enabled and appropriate resources are provisioned during connector creation. If the connector is not a hybrid connector, this field is ignored. Optional lookup argument.
    </ResponseField>
    <ResponseField name="identity_refresh_interval" type="string">
      The refresh interval to sync the Access Control List information for the documents ingested by this connector. If not set, the access control list will be refreshed at the default interval of 30 minutes. The identity refresh interval can be at least 30 minutes and at most 7 days. Optional lookup argument.
    </ResponseField>
    <ResponseField name="identity_schedule_config" type="object">
      The configuration for the identity data synchronization runs. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="next_sync_time" type="object">
          Represents civil time (or occasionally physical time). This type can represent a civil time in one of a few possible ways: * When utc_offset is set and time_zone is unset: a civil time on a calendar day with a particular offset from UTC. * When time_zone is set and utc_offset is unset: a civil time on a calendar day in a particular time zone. * When neither time_zone nor utc_offset is set: a civil time on a calendar day in local time. The date is relative to the Proleptic Gregorian Calendar. If year, month, or day are 0, the DateTime is considered not to have a specific year, month, or day respectively. This type may also be used to represent a physical time if all the date and time fields are set and either case of the `time_offset` oneof is set. Consider using `Timestamp` message for physical time instead. If your use case also would like to store the user's timezone, that can be done in another field. This type is more flexible than some applications may want. Make sure to document and validate your application's limitations. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="day" type="number">
              Optional. Day of month. Must be from 1 to 31 and valid for the year and month, or 0 if specifying a datetime without a day. Optional lookup argument.
            </ResponseField>
            <ResponseField name="hours" type="number">
              Optional. Hours of day in 24 hour format. Should be from 0 to 23, defaults to 0 (midnight). An API may choose to allow the value "24:00:00" for scenarios like business closing time. Optional lookup argument.
            </ResponseField>
            <ResponseField name="minutes" type="number">
              Optional. Minutes of hour of day. Must be from 0 to 59, defaults to 0. Optional lookup argument.
            </ResponseField>
            <ResponseField name="month" type="number">
              Optional. Month of year. Must be from 1 to 12, or 0 if specifying a datetime without a month. Optional lookup argument.
            </ResponseField>
            <ResponseField name="nanos" type="number">
              Optional. Fractions of seconds in nanoseconds. Must be from 0 to 999,999,999, defaults to 0. Optional lookup argument.
            </ResponseField>
            <ResponseField name="seconds" type="number">
              Optional. Seconds of minutes of the time. Must normally be from 0 to 59, defaults to 0. An API may allow the value 60 if it allows leap-seconds. Optional lookup argument.
            </ResponseField>
            <ResponseField name="time_zone" type="object">
              Represents a time zone from the [IANA Time Zone Database](https://www.iana.org/time-zones). Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  IANA Time Zone Database time zone. For example "America/New_York". Optional lookup argument.
                </ResponseField>
                <ResponseField name="version" type="string">
                  Optional. IANA Time Zone Database version number. For example "2019a". Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="utc_offset" type="string">
              UTC offset. Must be whole seconds, between -18 hours and +18 hours. For example, a UTC offset of -4:00 would be represented as &#123; seconds: -14400 &#125;. Optional lookup argument.
            </ResponseField>
            <ResponseField name="year" type="number">
              Optional. Year of date. Must be from 1 to 9999, or 0 if specifying a datetime without a year. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="refresh_interval" type="string">
          Optional. The refresh interval to sync the Access Control List information for the documents ingested by this connector. If not set, the access control list will be refreshed at the default interval of 30 minutes. The identity refresh interval can be at least 30 minutes and at most 7 days. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="incremental_refresh_interval" type="string">
      Optional. The refresh interval specifically for incremental data syncs. If unset, incremental syncs will use the default from env, set to 3hrs. The minimum is 30 minutes and maximum is 7 days. Applicable to only 3P connectors. When the refresh interval is set to the same value as the incremental refresh interval, incremental sync will be disabled. Optional lookup argument.
    </ResponseField>
    <ResponseField name="incremental_sync_disabled" type="bool">
      Optional. Indicates whether incremental syncs are paused for this connector. This is independent of auto_run_disabled. Applicable to only 3P connectors. When the refresh interval is set to the same value as the incremental refresh interval, incremental sync will be disabled, i.e. set to true. Optional lookup argument.
    </ResponseField>
    <ResponseField name="json_params" type="string">
      Required data connector parameters in json string format. Optional lookup argument.
    </ResponseField>
    <ResponseField name="kms_key_name" type="string">
      Input only. The KMS key to be used to protect the DataStores managed by this connector. Must be set for requests that need to comply with CMEK Org Policy protections. If this field is set and processed successfully, the DataStores created by this connector will be protected by the KMS key. Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_sync_time" type="string">
      Output only. For periodic connectors only, the last time a data sync was completed. Returned by the lookup.
    </ResponseField>
    <ResponseField name="latest_pause_time" type="string">
      Output only. The most recent timestamp when this DataConnector was paused, affecting all functionalities such as data synchronization. Pausing a connector has the following effects: - All functionalities, including data synchronization, are halted. - Any ongoing data synchronization job will be canceled. - No future data synchronization runs will be scheduled nor can be triggered. Returned by the lookup.
    </ResponseField>
    <ResponseField name="metadata" type="object">
      User-facing metadata for the connector, shown on the connector detail page (title, description, short_description, author, note). Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="author" type="string">
          Optional. The party that authored the connector, e.g. "Google" or a third-party provider name. Lets end users see who authored a connector (future: third-party-authored connectors). Optional lookup argument.
        </ResponseField>
        <ResponseField name="description" type="string">
          Optional. Human-readable description of the connector, shown on the connector detail page. One connector has a single description. Optional lookup argument.
        </ResponseField>
        <ResponseField name="note" type="string">
          Optional. Free-form, multi-line note about the connector's capabilities or a custom note that can be set for the connector. Optional lookup argument.
        </ResponseField>
        <ResponseField name="short_description" type="string">
          Optional. Short, subtitle-length description of the connector (e.g. shown beneath the connector name in list and detail views). Optional lookup argument.
        </ResponseField>
        <ResponseField name="title" type="string">
          Optional. Display title of the connector. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      Identifier. The full resource name of the Data Connector. Format: `projects/*/locations/*/collections/*/dataConnector`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="next_sync_time" type="object">
      Represents civil time (or occasionally physical time). This type can represent a civil time in one of a few possible ways: * When utc_offset is set and time_zone is unset: a civil time on a calendar day with a particular offset from UTC. * When time_zone is set and utc_offset is unset: a civil time on a calendar day in a particular time zone. * When neither time_zone nor utc_offset is set: a civil time on a calendar day in local time. The date is relative to the Proleptic Gregorian Calendar. If year, month, or day are 0, the DateTime is considered not to have a specific year, month, or day respectively. This type may also be used to represent a physical time if all the date and time fields are set and either case of the `time_offset` oneof is set. Consider using `Timestamp` message for physical time instead. If your use case also would like to store the user's timezone, that can be done in another field. This type is more flexible than some applications may want. Make sure to document and validate your application's limitations. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="day" type="number">
          Optional. Day of month. Must be from 1 to 31 and valid for the year and month, or 0 if specifying a datetime without a day. Optional lookup argument.
        </ResponseField>
        <ResponseField name="hours" type="number">
          Optional. Hours of day in 24 hour format. Should be from 0 to 23, defaults to 0 (midnight). An API may choose to allow the value "24:00:00" for scenarios like business closing time. Optional lookup argument.
        </ResponseField>
        <ResponseField name="minutes" type="number">
          Optional. Minutes of hour of day. Must be from 0 to 59, defaults to 0. Optional lookup argument.
        </ResponseField>
        <ResponseField name="month" type="number">
          Optional. Month of year. Must be from 1 to 12, or 0 if specifying a datetime without a month. Optional lookup argument.
        </ResponseField>
        <ResponseField name="nanos" type="number">
          Optional. Fractions of seconds in nanoseconds. Must be from 0 to 999,999,999, defaults to 0. Optional lookup argument.
        </ResponseField>
        <ResponseField name="seconds" type="number">
          Optional. Seconds of minutes of the time. Must normally be from 0 to 59, defaults to 0. An API may allow the value 60 if it allows leap-seconds. Optional lookup argument.
        </ResponseField>
        <ResponseField name="time_zone" type="object">
          Represents a time zone from the [IANA Time Zone Database](https://www.iana.org/time-zones). Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              IANA Time Zone Database time zone. For example "America/New_York". Optional lookup argument.
            </ResponseField>
            <ResponseField name="version" type="string">
              Optional. IANA Time Zone Database version number. For example "2019a". Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="utc_offset" type="string">
          UTC offset. Must be whole seconds, between -18 hours and +18 hours. For example, a UTC offset of -4:00 would be represented as &#123; seconds: -14400 &#125;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="year" type="number">
          Optional. Year of date. Must be from 1 to 9999, or 0 if specifying a datetime without a year. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="oauth_static_ip_addresses" type="list(string)">
      Output only. The static IP addresses used by this connector for OAuth APIs (e.g. end user authentication). These are surfaced separately from `static_ip_addresses` so that customers can apply granular firewall settings for OAuth endpoints. Only populated for connectors that have static IP enabled and are used for actions and/or federated search. Returned by the lookup.
    </ResponseField>
    <ResponseField name="params" type="map(any)">
      Required data connector parameters in structured json format. Optional lookup argument.
    </ResponseField>
    <ResponseField name="private_connectivity_project_id" type="string">
      Output only. The tenant project ID associated with private connectivity connectors. This project must be allowlisted by in order for the connector to function. Returned by the lookup.
    </ResponseField>
    <ResponseField name="realtime_state" type="string">
      Output only. real-time sync state Returned by the lookup.
    </ResponseField>
    <ResponseField name="realtime_sync_config" type="object">
      The configuration for realtime sync to store additional params for realtime sync. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="realtime_sync_secret" type="string">
          Optional. The ID of the Secret Manager secret used for webhook secret. Optional lookup argument.
        </ResponseField>
        <ResponseField name="streaming_error" type="object">
          Streaming error details. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="error" type="object">
              The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="code" type="number">
                  The status code, which should be an enum value of google.rpc.Code. Optional lookup argument.
                </ResponseField>
                <ResponseField name="details" type="list(map(any))">
                  A list of messages that carry the error details. There is a common set of message types for APIs to use. Optional lookup argument.
                </ResponseField>
                <ResponseField name="message" type="string">
                  A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="streaming_error_reason" type="string">
              Optional. Streaming error. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="webhook_uri" type="string">
          Optional. Webhook url for the connector to specify additional params for realtime sync. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="refresh_interval" type="string">
      Required. The refresh interval for data sync. If duration is set to 0, the data will be synced in real time. The streaming feature is not supported yet. The minimum is 30 minutes and maximum is 7 days. When the refresh interval is set to the same value as the incremental refresh interval, incremental sync will be disabled. Optional lookup argument.
    </ResponseField>
    <ResponseField name="remove_param_keys" type="list(string)">
      Optional. Specifies keys to be removed from the 'params' field. This is only active when 'params' is included in the 'update_mask' in an UpdateDataConnectorRequest. Deletion takes precedence if a key is both in 'remove_param_keys' and present in the 'params' field of the request. Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. State of the connector. Returned by the lookup.
    </ResponseField>
    <ResponseField name="static_ip_addresses" type="list(string)">
      Output only. The static IP addresses used by this connector. Returned by the lookup.
    </ResponseField>
    <ResponseField name="static_ip_enabled" type="bool">
      Optional. Whether customer has enabled static IP addresses for this connector. Optional lookup argument.
    </ResponseField>
    <ResponseField name="sync_mode" type="string">
      The data synchronization mode supported by the data connector. Optional lookup argument.
    </ResponseField>
    <ResponseField name="tag" type="string">
      Optional. Immutable. User-facing, version-independent label for this connector. May be shared by multiple connectors under the same (project, location, collection, data_source); tag-based lookup returns the one with the greatest create_time. Optional at Create time. Agent Designer resolves connectors via (data_source, tag) when set, falling back to the legacy resource-name lookup when unset, so connectors created before the tag-write launch continue to work without a backfill. Optional lookup argument.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. Timestamp the DataConnector was last updated. Returned by the lookup.
    </ResponseField>
    <ResponseField name="vpcsc_enabled" type="bool">
      Output only. Whether the connector is created with VPC-SC enabled. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="display_name" type="string">
  Required. The Collection display name. This field must be a UTF-8 encoded string with a length limit of 128 characters. Otherwise, an INVALID_ARGUMENT error is returned. Returned by the lookup.
</ResponseField>
