---
title: "google_discoveryengine_v1alpha_billing_account_license_config"
description: "`google_discoveryengine_v1alpha_billing_account_license_config` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_discoveryengine_v1alpha_billing_account_license_config` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discoveryengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/discoveryengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_discoveryengine_v1alpha_billing_account_license_config"})

    		ubx.Data(discoveryengine.V1alphaBillingAccountLicenseConfig, "example", discoveryengine.V1alphaBillingAccountLicenseConfigConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1alphaBillingAccountLicenseConfig } from "@ubx/sdk-google/google/data/discoveryengine/v1alpha_billing_account_license_config";

    export default stack("example", () => {
      intent({
        summary:
          "look up google_discoveryengine_v1alpha_billing_account_license_config",
      });

      data(V1alphaBillingAccountLicenseConfig, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.discoveryengine import V1alphaBillingAccountLicenseConfig, V1alphaBillingAccountLicenseConfigConfig

    def describe():
        ubx.intent("look up google_discoveryengine_v1alpha_billing_account_license_config")

        ubx.data(V1alphaBillingAccountLicenseConfig, "example", V1alphaBillingAccountLicenseConfigConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auto_renew" type="bool">
  Whether the BillingAccountLicenseConfig is auto renewed when it reaches the end date. Returned by the lookup.
</ResponseField>

<ResponseField name="early_terminated" type="bool">
  Indication of whether the subscription is terminated earlier than the expiration date. This is usually terminated by pipeline once the subscription gets terminated from subsv3. Returned by the lookup.
</ResponseField>

<ResponseField name="early_termination_date" type="object">
  Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="day" type="number">
      Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant. Optional lookup argument.
    </ResponseField>
    <ResponseField name="month" type="number">
      Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day. Optional lookup argument.
    </ResponseField>
    <ResponseField name="year" type="number">
      Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="end_date" type="object">
  Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="day" type="number">
      Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant. Optional lookup argument.
    </ResponseField>
    <ResponseField name="month" type="number">
      Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day. Optional lookup argument.
    </ResponseField>
    <ResponseField name="year" type="number">
      Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="gemini_bundle" type="bool">
  Whether the license config is for Gemini bundle. Returned by the lookup.
</ResponseField>

<ResponseField name="license_config_distributions" type="map(string)">
  A map of LicenseConfig names to the number of licenses distributed to each. The key is the full resource name of the LicenseConfig, such as `projects/&#123;project&#125;/locations/&#123;location&#125;/licenseConfigs/&#123;license_config&#125;`. The value is the count of licenses allocated to it. Returned by the lookup.
</ResponseField>

<ResponseField name="license_count" type="string">
  Required. The number of licenses purchased under this billing account license config. Returned by the lookup.
</ResponseField>

<ResponseField name="procurement_entitlement_id" type="string">
  The procurement entitlement id of the subscription. Returned by the lookup.
</ResponseField>

<ResponseField name="start_date" type="object">
  Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="day" type="number">
      Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant. Optional lookup argument.
    </ResponseField>
    <ResponseField name="month" type="number">
      Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day. Optional lookup argument.
    </ResponseField>
    <ResponseField name="year" type="number">
      Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The state of the BillingAccountLicenseConfig. Returned by the lookup.
</ResponseField>

<ResponseField name="subscription_display_name" type="string">
  The subscription display name. Returned by the lookup.
</ResponseField>

<ResponseField name="subscription_name" type="string">
  Output only. The corresponding SubV3 subscription name. Returned by the lookup.
</ResponseField>

<ResponseField name="subscription_term" type="string">
  Required. The subscription term. Returned by the lookup.
</ResponseField>

<ResponseField name="subscription_tier" type="string">
  Required. The subscription tier. Returned by the lookup.
</ResponseField>
