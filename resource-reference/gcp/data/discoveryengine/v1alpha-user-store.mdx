---
title: "google_discoveryengine_v1alpha_user_store"
description: "`google_discoveryengine_v1alpha_user_store` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_discoveryengine_v1alpha_user_store` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discoveryengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/discoveryengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_discoveryengine_v1alpha_user_store"})

    		ubx.Data(discoveryengine.V1alphaUserStore, "example", discoveryengine.V1alphaUserStoreConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1alphaUserStore } from "@ubx/sdk-google/google/data/discoveryengine/v1alpha_user_store";

    export default stack("example", () => {
      intent({ summary: "look up google_discoveryengine_v1alpha_user_store" });

      data(V1alphaUserStore, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.discoveryengine import V1alphaUserStore, V1alphaUserStoreConfig

    def describe():
        ubx.intent("look up google_discoveryengine_v1alpha_user_store")

        ubx.data(V1alphaUserStore, "example", V1alphaUserStoreConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="default_license_config" type="string">
  Optional. The default subscription LicenseConfig for the UserStore, if UserStore.enable_license_auto_register is true, new users will automatically register under the default subscription. If default LicenseConfig doesn't have remaining license seats left, new users will not be assigned with license and will be blocked for Vertex AI Search features. This is used if `license_assignment_tier_rules` is not configured. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name of the User Store. Returned by the lookup.
</ResponseField>

<ResponseField name="enable_expired_license_auto_update" type="bool">
  Optional. Whether to enable license auto update for users in this User Store. If true, users with expired licenses will automatically be updated to use the default license config as long as the default license config has seats left. Returned by the lookup.
</ResponseField>

<ResponseField name="enable_license_auto_register" type="bool">
  Optional. Whether to enable license auto register for users in this User Store. If true, new users will automatically register under the default license config as long as the default license config has seats left. Returned by the lookup.
</ResponseField>
