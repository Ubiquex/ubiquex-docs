---
title: "google_discoveryengine_v1beta_user_store"
description: "`data_discoveryengine_v1beta_user_store` looks up a real user-event store used by Discovery Engine, via the v1beta API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discoveryengine "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/discoveryengine"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_discoveryengine_v1beta_user_store"})

    		ubx.Data(discoveryengine.V1betaUserStore, "example", discoveryengine.V1betaUserStoreConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1betaUserStore } from "./local-sdk/google/sdk/typescript/google/data/discoveryengine/v1beta_user_store.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_discoveryengine_v1beta_user_store" });

      data(V1betaUserStore, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.discoveryengine import V1betaUserStore, V1betaUserStoreConfig

    def describe():
        ubx.intent("look up google_discoveryengine_v1beta_user_store")

        ubx.data(V1betaUserStore, "example", V1betaUserStoreConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="default_license_config" type="string">
  Optional. The default subscription LicenseConfig for the UserStore, if UserStore.enable_license_auto_register is true, new users will automatically register under the default subscription. If default LicenseConfig doesn't have remaining license seats left, new users will not be assigned with license and will be blocked for Vertex AI Search features. This is used if `license_assignment_tier_rules` is not configured. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name of the User Store. Returned by the lookup.
</ResponseField>

<ResponseField name="enable_expired_license_auto_update" type="bool">
  Optional. Whether to enable license auto update for users in this User Store. If true, users with expired licenses will automatically be updated to use the default license config as long as the default license config has seats left. Returned by the lookup.
</ResponseField>

<ResponseField name="enable_license_auto_register" type="bool">
  Optional. Whether to enable license auto register for users in this User Store. If true, new users will automatically register under the default license config as long as the default license config has seats left. Returned by the lookup.
</ResponseField>
