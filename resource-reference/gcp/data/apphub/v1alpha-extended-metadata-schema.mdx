---
title: "google_apphub_v1alpha_extended_metadata_schema"
description: "`data_apphub_v1alpha_extended_metadata_schema` looks up a real, defined schema for custom extended metadata in App Hub, via the v1alpha API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apphub "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/apphub"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_apphub_v1alpha_extended_metadata_schema"})

    		ubx.Data(apphub.V1alphaExtendedMetadataSchema, "example", apphub.V1alphaExtendedMetadataSchemaConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1alphaExtendedMetadataSchema } from "@ubx/sdk-google/google/data/apphub/v1alpha_extended_metadata_schema";

    export default stack("example", () => {
      intent({ summary: "look up google_apphub_v1alpha_extended_metadata_schema" });

      data(V1alphaExtendedMetadataSchema, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.apphub import V1alphaExtendedMetadataSchema, V1alphaExtendedMetadataSchemaConfig

    def describe():
        ubx.intent("look up google_apphub_v1alpha_extended_metadata_schema")

        ubx.data(V1alphaExtendedMetadataSchema, "example", V1alphaExtendedMetadataSchemaConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="json_schema" type="string">
  Output only. The JSON schema as a string. Returned by the lookup.
</ResponseField>

<ResponseField name="schema_version" type="string">
  Output only. The version of the schema. New versions are required to be backwards compatible. Returned by the lookup.
</ResponseField>
