---
title: "google_vmmigration_v1alpha1_replication_cycle"
description: "`google_vmmigration_v1alpha1_replication_cycle` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_vmmigration_v1alpha1_replication_cycle` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vmmigration "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/vmmigration"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_vmmigration_v1alpha1_replication_cycle"})

    		ubx.Data(vmmigration.V1alpha1ReplicationCycle, "example", vmmigration.V1alpha1ReplicationCycleConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1alpha1ReplicationCycle } from "@ubx/sdk-google/google/data/vmmigration/v1alpha1_replication_cycle";

    export default stack("example", () => {
      intent({ summary: "look up google_vmmigration_v1alpha1_replication_cycle" });

      data(V1alpha1ReplicationCycle, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.vmmigration import V1alpha1ReplicationCycle, V1alpha1ReplicationCycleConfig

    def describe():
        ubx.intent("look up google_vmmigration_v1alpha1_replication_cycle")

        ubx.data(V1alpha1ReplicationCycle, "example", V1alpha1ReplicationCycleConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cycle_number" type="number">
  The cycle's ordinal number. Returned by the lookup.
</ResponseField>

<ResponseField name="end_time" type="string">
  The time the replication cycle has ended. Returned by the lookup.
</ResponseField>

<ResponseField name="error" type="object">
  The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
      The status code, which should be an enum value of google.rpc.Code. Optional lookup argument.
    </ResponseField>
    <ResponseField name="details" type="list(map(any))">
      A list of messages that carry the error details. There is a common set of message types for APIs to use. Optional lookup argument.
    </ResponseField>
    <ResponseField name="message" type="string">
      A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="progress" type="number">
  The current progress in percentage of this cycle. Returned by the lookup.
</ResponseField>

<ResponseField name="progress_percent" type="number">
  The current progress in percentage of this cycle. Was replaced by 'steps' field, which breaks down the cycle progression more accurately. Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  The time the replication cycle has started. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  State of the ReplicationCycle. Returned by the lookup.
</ResponseField>

<ResponseField name="steps" type="list(object)">
  The cycle's steps list representing its progress. Returned by the lookup.
</ResponseField>

<ResponseField name="total_pause_duration" type="string">
  The accumulated duration the replication cycle was paused. Returned by the lookup.
</ResponseField>

<ResponseField name="warnings" type="list(object)">
  Output only. Warnings that occurred during the cycle. Returned by the lookup.
</ResponseField>
