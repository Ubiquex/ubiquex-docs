---
title: "google_artifactregistry_v1beta1_version"
description: "`google_artifactregistry_v1beta1_version` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_artifactregistry_v1beta1_version` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	artifactregistry "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/artifactregistry"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_artifactregistry_v1beta1_version"})

    		ubx.Data(artifactregistry.V1beta1Version, "example", artifactregistry.V1beta1VersionConfig{
    			Name: "example",
    			View: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1beta1Version } from "@ubx/sdk-google/google/data/artifactregistry/v1beta1_version";

    export default stack("example", () => {
      intent({ summary: "look up google_artifactregistry_v1beta1_version" });

      data(V1beta1Version, "example", {
        name: "example",
        view: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.artifactregistry import V1beta1Version, V1beta1VersionConfig

    def describe():
        ubx.intent("look up google_artifactregistry_v1beta1_version")

        ubx.data(V1beta1Version, "example", V1beta1VersionConfig(
            name="example",
            view="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="view" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  The time when the version was created. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the version, as specified in its metadata. Returned by the lookup.
</ResponseField>

<ResponseField name="related_tags" type="list(object)">
  Output only. A list of related tags. Will contain up to 100 tags that reference this version. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  The time when the version was last updated. Returned by the lookup.
</ResponseField>
