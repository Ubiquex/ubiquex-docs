---
title: "google_artifactregistry_file"
description: "`google_artifactregistry_file` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_artifactregistry_file` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	artifactregistry "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/artifactregistry"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_artifactregistry_file"})

    		ubx.Data(artifactregistry.File, "example", artifactregistry.FileConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { File } from "@ubx/sdk-google/google/data/artifactregistry/file";

    export default stack("example", () => {
      intent({ summary: "look up google_artifactregistry_file" });

      data(File, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.artifactregistry import File, FileConfig

    def describe():
        ubx.intent("look up google_artifactregistry_file")

        ubx.data(File, "example", FileConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="annotations" type="map(string)">
  Optional. Client specified annotations. Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the File was created. Returned by the lookup.
</ResponseField>

<ResponseField name="fetch_time" type="string">
  Output only. The time when the last attempt to refresh the file's data was made. Only set when the repository is remote. Returned by the lookup.
</ResponseField>

<ResponseField name="hashes" type="list(object)">
  The hashes of the file content. Returned by the lookup.
</ResponseField>

<ResponseField name="owner" type="string">
  The name of the Package or Version that owns this file, if any. Returned by the lookup.
</ResponseField>

<ResponseField name="size_bytes" type="string">
  The size of the File in bytes. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the File was last updated. Returned by the lookup.
</ResponseField>
