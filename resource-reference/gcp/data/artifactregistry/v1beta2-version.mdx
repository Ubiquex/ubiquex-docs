---
title: "google_artifactregistry_v1beta2_version"
description: "`data_artifactregistry_v1beta2_version` looks up a real, specific version of an Artifact Registry package, via the v1beta2 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	artifactregistry "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/artifactregistry"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_artifactregistry_v1beta2_version"})

    		ubx.Data(artifactregistry.V1beta2Version, "example", artifactregistry.V1beta2VersionConfig{
    			Name: "example",
    			View: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1beta2Version } from "./local-sdk/google/sdk/typescript/google/data/artifactregistry/v1beta2_version.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_artifactregistry_v1beta2_version" });

      data(V1beta2Version, "example", {
        name: "example",
        view: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.artifactregistry import V1beta2Version, V1beta2VersionConfig

    def describe():
        ubx.intent("look up google_artifactregistry_v1beta2_version")

        ubx.data(V1beta2Version, "example", V1beta2VersionConfig(
            name="example",
            view="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="view" type="string">
  The real, requested view (which fields to include) of this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  The time when the version was created. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the version, as specified in its metadata. Returned by the lookup.
</ResponseField>

<ResponseField name="metadata" type="map(any)">
  Output only. Repository-specific Metadata stored against this version. The fields returned are defined by the underlying repository-specific resource. Currently, the resources could be: DockerImage MavenArtifact Returned by the lookup.
</ResponseField>

<ResponseField name="related_tags" type="list(object)">
  Output only. A list of related tags. Will contain up to 100 tags that reference this version. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  The time when the version was last updated. Returned by the lookup.
</ResponseField>
