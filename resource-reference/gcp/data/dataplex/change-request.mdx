---
title: "google_dataplex_change_request"
description: "`data_dataplex_change_request` looks up a real, pending change request submitted against a Dataplex entry or schema."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataplex "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/dataplex"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_dataplex_change_request"})

    		ubx.Data(dataplex.ChangeRequest, "example", dataplex.ChangeRequestConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { ChangeRequest } from "./local-sdk/google/sdk/typescript/google/data/dataplex/change_request.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_dataplex_change_request" });

      data(ChangeRequest, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.dataplex import ChangeRequest, ChangeRequestConfig

    def describe():
        ubx.intent("look up google_dataplex_change_request")

        ubx.data(ChangeRequest, "example", ChangeRequestConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="approver" type="string">
  Output only. The email address of the user who approved/rejected the ChangeRequest. Returned by the lookup.
</ResponseField>

<ResponseField name="author" type="string">
  Output only. The email address of the user who created the ChangeRequest. Returned by the lookup.
</ResponseField>

<ResponseField name="change_type" type="string">
  Output only. The type of change represented by the change_payload. This field is derived from the populated field in the change_payload oneof. Returned by the lookup.
</ResponseField>

<ResponseField name="create_entry" type="object">
  Create Entry request. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="entry" type="object">
      An entry is a representation of a data resource that can be described by various metadata. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="aspects" type="map(object)">
          Optional. The aspects that are attached to the entry. Depending on how the aspect is attached to the entry, the format of the aspect key can be one of the following: If the aspect is attached directly to the entry: &#123;project_id_or_number&#125;.&#123;location_id&#125;.&#123;aspect_type_id&#125; If the aspect is attached to an entry's path: &#123;project_id_or_number&#125;.&#123;location_id&#125;.&#123;aspect_type_id&#125;@&#123;path&#125; Optional lookup argument.
        </ResponseField>
        <ResponseField name="create_time" type="string">
          Output only. The time when the entry was created in Dataplex Universal Catalog. Returned by the lookup.
        </ResponseField>
        <ResponseField name="entry_source" type="object">
          Information related to the source system of the data resource that is represented by the entry. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="ancestors" type="list(object)">
              Immutable. The entries representing the ancestors of the data resource in the source system. Optional lookup argument.
            </ResponseField>
            <ResponseField name="create_time" type="string">
              The time when the resource was created in the source system. Optional lookup argument.
            </ResponseField>
            <ResponseField name="description" type="string">
              A description of the data resource. Maximum length is 2,000 characters. Optional lookup argument.
            </ResponseField>
            <ResponseField name="display_name" type="string">
              A user-friendly display name. Maximum length is 500 characters. Optional lookup argument.
            </ResponseField>
            <ResponseField name="labels" type="map(string)">
              User-defined labels. The maximum size of keys and values is 128 characters each. Optional lookup argument.
            </ResponseField>
            <ResponseField name="location" type="string">
              Output only. Location of the resource in the source system. You can search the entry by this location. By default, this should match the location of the entry group containing this entry. A different value allows capturing the source location for data external to Google Cloud. Returned by the lookup.
            </ResponseField>
            <ResponseField name="platform" type="string">
              The platform containing the source system. Maximum length is 64 characters. Optional lookup argument.
            </ResponseField>
            <ResponseField name="resource" type="string">
              The name of the resource in the source system. Maximum length is 4,000 characters. Optional lookup argument.
            </ResponseField>
            <ResponseField name="system" type="string">
              The name of the source system. Maximum length is 64 characters. Optional lookup argument.
            </ResponseField>
            <ResponseField name="update_time" type="string">
              The time when the resource was last updated in the source system. If the entry exists in the system and its EntrySource has update_time populated, further updates to the EntrySource of the entry must provide incremental updates to its update_time. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="entry_type" type="string">
          Required. Immutable. The relative resource name of the entry type that was used to create this entry, in the format projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/entryTypes/&#123;entry_type_id&#125;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="fully_qualified_name" type="string">
          Optional. A name for the entry that can be referenced by an external system. For more information, see Fully qualified names (https://cloud.google.com/data-catalog/docs/fully-qualified-names). The maximum size of the field is 4000 characters. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Identifier. The relative resource name of the entry, in the format projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/entryGroups/&#123;entry_group_id&#125;/entries/&#123;entry_id&#125;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="parent_entry" type="string">
          Optional. Immutable. The resource name of the parent entry, in the format projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/entryGroups/&#123;entry_group_id&#125;/entries/&#123;entry_id&#125;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="update_time" type="string">
          Output only. The time when the entry was last updated in Dataplex Universal Catalog. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="entry_id" type="string">
      Required. Entry identifier. It has to be unique within an Entry Group.Entries corresponding to Google Cloud resources use an Entry ID format based on full resource names (https://cloud.google.com/apis/design/resource_names#full_resource_name). The format is a full resource name of the resource without the prefix double slashes in the API service name part of the full resource name. This allows retrieval of entries using their associated resource name.For example, if the full resource name of a resource is //library.googleapis.com/shelves/shelf1/books/book2, then the suggested entry_id is library.googleapis.com/shelves/shelf1/books/book2.It is also suggested to follow the same convention for entries corresponding to resources from providers or systems other than Google Cloud.The maximum size of the field is 4000 characters. Optional lookup argument.
    </ResponseField>
    <ResponseField name="parent" type="string">
      Required. The resource name of the parent Entry Group: projects/&#123;project&#125;/locations/&#123;location&#125;/entryGroups/&#123;entry_group&#125;. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_entry_link" type="object">
  Request message for CreateEntryLink. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="entry_link" type="object">
      EntryLink represents a link between two Entries. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="aspects" type="map(object)">
          Optional. The aspects that are attached to the entry link. The format of the aspect key has to be the following: &#123;project_id_or_number&#125;.&#123;location_id&#125;.&#123;aspect_type_id&#125; Currently, only a single aspect of a Dataplex-owned Aspect Type is allowed. Optional lookup argument.
        </ResponseField>
        <ResponseField name="create_time" type="string">
          Output only. The time when the Entry Link was created. Returned by the lookup.
        </ResponseField>
        <ResponseField name="entry_link_type" type="string">
          Required. Immutable. Relative resource name of the Entry Link Type used to create this Entry Link. For example: Entry link between synonym terms in a glossary: projects/dataplex-types/locations/global/entryLinkTypes/synonym Entry link between related terms in a glossary: projects/dataplex-types/locations/global/entryLinkTypes/related Entry link between glossary terms and data assets: projects/dataplex-types/locations/global/entryLinkTypes/definition Optional lookup argument.
        </ResponseField>
        <ResponseField name="entry_references" type="list(object)">
          Required. Immutable. Specifies the Entries referenced in the Entry Link. There should be exactly two entry references. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Output only. Immutable. Identifier. The relative resource name of the Entry Link, of the form: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/entryGroups/&#123;entry_group_id&#125;/entryLinks/&#123;entry_link_id&#125; Returned by the lookup.
        </ResponseField>
        <ResponseField name="update_time" type="string">
          Output only. The time when the Entry Link was last updated. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="entry_link_id" type="string">
      Required. Entry Link identifier * Must contain only lowercase letters, numbers and hyphens. * Must start with a letter. * Must be between 1-63 characters. * Must end with a number or a letter. * Must be unique within the EntryGroup. Optional lookup argument.
    </ResponseField>
    <ResponseField name="parent" type="string">
      Required. The resource name of the parent Entry Group: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/entryGroups/&#123;entry_group_id&#125;. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_glossary" type="object">
  Create Glossary Request Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="glossary" type="object">
      A Glossary represents a collection of GlossaryCategories and GlossaryTerms defined by the user. Glossary is a top level resource and is the Google Cloud parent resource of all the GlossaryCategories and GlossaryTerms within it. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="category_count" type="number">
          Output only. The number of GlossaryCategories in the Glossary. Returned by the lookup.
        </ResponseField>
        <ResponseField name="create_time" type="string">
          Output only. The time at which the Glossary was created. Returned by the lookup.
        </ResponseField>
        <ResponseField name="description" type="string">
          Optional. The user-mutable description of the Glossary. Optional lookup argument.
        </ResponseField>
        <ResponseField name="display_name" type="string">
          Optional. User friendly display name of the Glossary. This is user-mutable. This will be same as the GlossaryId, if not specified. Optional lookup argument.
        </ResponseField>
        <ResponseField name="etag" type="string">
          Optional. Needed for resource freshness validation. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Optional lookup argument.
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. User-defined labels for the Glossary. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Output only. Identifier. The resource name of the Glossary. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125; Returned by the lookup.
        </ResponseField>
        <ResponseField name="term_count" type="number">
          Output only. The number of GlossaryTerms in the Glossary. Returned by the lookup.
        </ResponseField>
        <ResponseField name="uid" type="string">
          Output only. System generated unique id for the Glossary. This ID will be different if the Glossary is deleted and re-created with the same name. Returned by the lookup.
        </ResponseField>
        <ResponseField name="update_time" type="string">
          Output only. The time at which the Glossary was last updated. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="glossary_id" type="string">
      Required. Glossary ID: Glossary identifier. Optional lookup argument.
    </ResponseField>
    <ResponseField name="parent" type="string">
      Required. The parent resource where this Glossary will be created. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125; where location_id refers to a Google Cloud region. Optional lookup argument.
    </ResponseField>
    <ResponseField name="validate_only" type="bool">
      Optional. Validates the request without actually creating the Glossary. Default: false. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_glossary_category" type="object">
  Creates a new GlossaryCategory under the specified Glossary. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="category" type="object">
      A GlossaryCategory represents a collection of GlossaryCategories and GlossaryTerms within a Glossary that are related to each other. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="create_time" type="string">
          Output only. The time at which the GlossaryCategory was created. Returned by the lookup.
        </ResponseField>
        <ResponseField name="description" type="string">
          Optional. The user-mutable description of the GlossaryCategory. Optional lookup argument.
        </ResponseField>
        <ResponseField name="display_name" type="string">
          Optional. User friendly display name of the GlossaryCategory. This is user-mutable. This will be same as the GlossaryCategoryId, if not specified. Optional lookup argument.
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. User-defined labels for the GlossaryCategory. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Output only. Identifier. The resource name of the GlossaryCategory. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125;/categories/&#123;category_id&#125; Returned by the lookup.
        </ResponseField>
        <ResponseField name="parent" type="string">
          Required. The immediate parent of the GlossaryCategory in the resource-hierarchy. It can either be a Glossary or a GlossaryCategory. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125; OR projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125;/categories/&#123;category_id&#125; Optional lookup argument.
        </ResponseField>
        <ResponseField name="uid" type="string">
          Output only. System generated unique id for the GlossaryCategory. This ID will be different if the GlossaryCategory is deleted and re-created with the same name. Returned by the lookup.
        </ResponseField>
        <ResponseField name="update_time" type="string">
          Output only. The time at which the GlossaryCategory was last updated. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="category_id" type="string">
      Required. GlossaryCategory identifier. Optional lookup argument.
    </ResponseField>
    <ResponseField name="parent" type="string">
      Required. The parent resource where this GlossaryCategory will be created. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125; where locationId refers to a Google Cloud region. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_glossary_term" type="object">
  Creates a new GlossaryTerm under the specified Glossary. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="parent" type="string">
      Required. The parent resource where the GlossaryTerm will be created. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125; where location_id refers to a Google Cloud region. Optional lookup argument.
    </ResponseField>
    <ResponseField name="term" type="object">
      GlossaryTerms are the core of Glossary. A GlossaryTerm holds a rich text description that can be attached to Entries or specific columns to enrich them. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="create_time" type="string">
          Output only. The time at which the GlossaryTerm was created. Returned by the lookup.
        </ResponseField>
        <ResponseField name="description" type="string">
          Optional. The user-mutable description of the GlossaryTerm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="display_name" type="string">
          Optional. User friendly display name of the GlossaryTerm. This is user-mutable. This will be same as the GlossaryTermId, if not specified. Optional lookup argument.
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. User-defined labels for the GlossaryTerm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Output only. Identifier. The resource name of the GlossaryTerm. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125;/terms/&#123;term_id&#125; Returned by the lookup.
        </ResponseField>
        <ResponseField name="parent" type="string">
          Required. The immediate parent of the GlossaryTerm in the resource-hierarchy. It can either be a Glossary or a GlossaryCategory. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125; OR projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125;/categories/&#123;category_id&#125; Optional lookup argument.
        </ResponseField>
        <ResponseField name="uid" type="string">
          Output only. System generated unique id for the GlossaryTerm. This ID will be different if the GlossaryTerm is deleted and re-created with the same name. Returned by the lookup.
        </ResponseField>
        <ResponseField name="update_time" type="string">
          Output only. The time at which the GlossaryTerm was last updated. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="term_id" type="string">
      Required. GlossaryTerm identifier. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time when the ChangeRequest was created. Returned by the lookup.
</ResponseField>

<ResponseField name="data_product_access_request" type="object">
  Message for requesting access to a Data Product. This will be used to create a ChangeRequest of type REQUEST_DATA_PRODUCT_ACCESS. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="access_group_display_name" type="string">
      Output only. The display name of the access group defined in the Data Product for which access is being requested. Returned by the lookup.
    </ResponseField>
    <ResponseField name="access_group_id" type="string">
      Required. The ID of the access group for which access is being requested. This corresponds to the unique identifier of the AccessGroup defined in the Data Product. Optional lookup argument.
    </ResponseField>
    <ResponseField name="parent" type="string">
      Required. The resource name of the data product. Format: projects/&#123;project_number&#125;/locations/&#123;location_id&#125;/dataProducts/&#123;data_product_id&#125; Optional lookup argument.
    </ResponseField>
    <ResponseField name="requested_principal" type="string">
      Optional. The principal for which access is being requested in IAM format. If not specified, the requestor's principal will be used. Example: serviceAccount:my-sa@my-project.iam.gserviceaccount.com. Only service account principals are currently supported. https://cloud.google.com/iam/docs/principal-identifiers Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="delete_entry" type="object">
  Delete Entry request. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Required. The resource name of the Entry: projects/&#123;project&#125;/locations/&#123;location&#125;/entryGroups/&#123;entry_group&#125;/entries/&#123;entry&#125;. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="delete_entry_link" type="object">
  Request message for DeleteEntryLink. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Required. The resource name of the Entry Link: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/entryGroups/&#123;entry_group_id&#125;/entryLinks/&#123;entry_link_id&#125;. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="delete_glossary" type="object">
  Delete Glossary Request Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="etag" type="string">
      Optional. The etag of the Glossary. If this is provided, it must match the server's etag. If the etag is provided and does not match the server-computed etag, the request must fail with a ABORTED error code. Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Required. The name of the Glossary to delete. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="delete_glossary_category" type="object">
  Delete GlossaryCategory Request Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Required. The name of the GlossaryCategory to delete. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125;/categories/&#123;category_id&#125; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="delete_glossary_term" type="object">
  Delete GlossaryTerm Request Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Required. The name of the GlossaryTerm to delete. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125;/terms/&#123;term_id&#125; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. This checksum is computed by the service. It can be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Returned by the lookup.
</ResponseField>

<ResponseField name="justification" type="string">
  Optional. Justification of the ChangeRequest. This should explain why the change is needed or why it should be approved. Returned by the lookup.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. User-defined labels for the ChangeRequest. Returned by the lookup.
</ResponseField>

<ResponseField name="rejection_comment" type="string">
  Output only. The reason provided for rejecting the ChangeRequest. Returned by the lookup.
</ResponseField>

<ResponseField name="resource" type="string">
  Output only. The full resource name of the target resource to be modified. Example: //dataplex.googleapis.com/projects/my-project/locations/us-central1/entryGroups/my-group/entries/my-entry Returned by the lookup.
</ResponseField>

<ResponseField name="reviewer_comment" type="string">
  Output only. The comment provided by the reviewer when approving or rejecting the ChangeRequest. Maximum length is 1024 characters. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the ChangeRequest. Returned by the lookup.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. System generated globally unique ID for the ChangeRequest. Returned by the lookup.
</ResponseField>

<ResponseField name="update_entry" type="object">
  Update Entry request. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="allow_missing" type="bool">
      Optional. If set to true and the entry doesn't exist, the service will create it. Optional lookup argument.
    </ResponseField>
    <ResponseField name="aspect_keys" type="list(string)">
      Optional. The map keys of the Aspects which the service should modify. It supports the following syntaxes: - matches an aspect of the given type and empty path. @path - matches an aspect of the given type and specified path. For example, to attach an aspect to a field that is specified by the schema aspect, the path should have the format Schema.. @* - matches aspects of the given type for all paths. *@path - matches aspects of all types on the given path.The service will not remove existing aspects matching the syntax unless delete_missing_aspects is set to true.If this field is left empty, the service treats it as specifying exactly those Aspects present in the request. Optional lookup argument.
    </ResponseField>
    <ResponseField name="delete_missing_aspects" type="bool">
      Optional. If set to true and the aspect_keys specify aspect ranges, the service deletes any existing aspects from that range that weren't provided in the request. Optional lookup argument.
    </ResponseField>
    <ResponseField name="entry" type="object">
      An entry is a representation of a data resource that can be described by various metadata. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="aspects" type="map(object)">
          Optional. The aspects that are attached to the entry. Depending on how the aspect is attached to the entry, the format of the aspect key can be one of the following: If the aspect is attached directly to the entry: &#123;project_id_or_number&#125;.&#123;location_id&#125;.&#123;aspect_type_id&#125; If the aspect is attached to an entry's path: &#123;project_id_or_number&#125;.&#123;location_id&#125;.&#123;aspect_type_id&#125;@&#123;path&#125; Optional lookup argument.
        </ResponseField>
        <ResponseField name="create_time" type="string">
          Output only. The time when the entry was created in Dataplex Universal Catalog. Returned by the lookup.
        </ResponseField>
        <ResponseField name="entry_source" type="object">
          Information related to the source system of the data resource that is represented by the entry. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="ancestors" type="list(object)">
              Immutable. The entries representing the ancestors of the data resource in the source system. Optional lookup argument.
            </ResponseField>
            <ResponseField name="create_time" type="string">
              The time when the resource was created in the source system. Optional lookup argument.
            </ResponseField>
            <ResponseField name="description" type="string">
              A description of the data resource. Maximum length is 2,000 characters. Optional lookup argument.
            </ResponseField>
            <ResponseField name="display_name" type="string">
              A user-friendly display name. Maximum length is 500 characters. Optional lookup argument.
            </ResponseField>
            <ResponseField name="labels" type="map(string)">
              User-defined labels. The maximum size of keys and values is 128 characters each. Optional lookup argument.
            </ResponseField>
            <ResponseField name="location" type="string">
              Output only. Location of the resource in the source system. You can search the entry by this location. By default, this should match the location of the entry group containing this entry. A different value allows capturing the source location for data external to Google Cloud. Returned by the lookup.
            </ResponseField>
            <ResponseField name="platform" type="string">
              The platform containing the source system. Maximum length is 64 characters. Optional lookup argument.
            </ResponseField>
            <ResponseField name="resource" type="string">
              The name of the resource in the source system. Maximum length is 4,000 characters. Optional lookup argument.
            </ResponseField>
            <ResponseField name="system" type="string">
              The name of the source system. Maximum length is 64 characters. Optional lookup argument.
            </ResponseField>
            <ResponseField name="update_time" type="string">
              The time when the resource was last updated in the source system. If the entry exists in the system and its EntrySource has update_time populated, further updates to the EntrySource of the entry must provide incremental updates to its update_time. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="entry_type" type="string">
          Required. Immutable. The relative resource name of the entry type that was used to create this entry, in the format projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/entryTypes/&#123;entry_type_id&#125;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="fully_qualified_name" type="string">
          Optional. A name for the entry that can be referenced by an external system. For more information, see Fully qualified names (https://cloud.google.com/data-catalog/docs/fully-qualified-names). The maximum size of the field is 4000 characters. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Identifier. The relative resource name of the entry, in the format projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/entryGroups/&#123;entry_group_id&#125;/entries/&#123;entry_id&#125;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="parent_entry" type="string">
          Optional. Immutable. The resource name of the parent entry, in the format projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/entryGroups/&#123;entry_group_id&#125;/entries/&#123;entry_id&#125;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="update_time" type="string">
          Output only. The time when the entry was last updated in Dataplex Universal Catalog. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="update_mask" type="string">
      Optional. Mask of fields to update. To update Aspects, the update_mask must contain the value "aspects".If the update_mask is empty, the service will update all modifiable fields present in the request. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_glossary" type="object">
  Update Glossary Request Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="glossary" type="object">
      A Glossary represents a collection of GlossaryCategories and GlossaryTerms defined by the user. Glossary is a top level resource and is the Google Cloud parent resource of all the GlossaryCategories and GlossaryTerms within it. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="category_count" type="number">
          Output only. The number of GlossaryCategories in the Glossary. Returned by the lookup.
        </ResponseField>
        <ResponseField name="create_time" type="string">
          Output only. The time at which the Glossary was created. Returned by the lookup.
        </ResponseField>
        <ResponseField name="description" type="string">
          Optional. The user-mutable description of the Glossary. Optional lookup argument.
        </ResponseField>
        <ResponseField name="display_name" type="string">
          Optional. User friendly display name of the Glossary. This is user-mutable. This will be same as the GlossaryId, if not specified. Optional lookup argument.
        </ResponseField>
        <ResponseField name="etag" type="string">
          Optional. Needed for resource freshness validation. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Optional lookup argument.
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. User-defined labels for the Glossary. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Output only. Identifier. The resource name of the Glossary. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125; Returned by the lookup.
        </ResponseField>
        <ResponseField name="term_count" type="number">
          Output only. The number of GlossaryTerms in the Glossary. Returned by the lookup.
        </ResponseField>
        <ResponseField name="uid" type="string">
          Output only. System generated unique id for the Glossary. This ID will be different if the Glossary is deleted and re-created with the same name. Returned by the lookup.
        </ResponseField>
        <ResponseField name="update_time" type="string">
          Output only. The time at which the Glossary was last updated. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="update_mask" type="string">
      Required. The list of fields to update. Optional lookup argument.
    </ResponseField>
    <ResponseField name="validate_only" type="bool">
      Optional. Validates the request without actually updating the Glossary. Default: false. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_glossary_category" type="object">
  Update GlossaryCategory Request Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="category" type="object">
      A GlossaryCategory represents a collection of GlossaryCategories and GlossaryTerms within a Glossary that are related to each other. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="create_time" type="string">
          Output only. The time at which the GlossaryCategory was created. Returned by the lookup.
        </ResponseField>
        <ResponseField name="description" type="string">
          Optional. The user-mutable description of the GlossaryCategory. Optional lookup argument.
        </ResponseField>
        <ResponseField name="display_name" type="string">
          Optional. User friendly display name of the GlossaryCategory. This is user-mutable. This will be same as the GlossaryCategoryId, if not specified. Optional lookup argument.
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. User-defined labels for the GlossaryCategory. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Output only. Identifier. The resource name of the GlossaryCategory. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125;/categories/&#123;category_id&#125; Returned by the lookup.
        </ResponseField>
        <ResponseField name="parent" type="string">
          Required. The immediate parent of the GlossaryCategory in the resource-hierarchy. It can either be a Glossary or a GlossaryCategory. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125; OR projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125;/categories/&#123;category_id&#125; Optional lookup argument.
        </ResponseField>
        <ResponseField name="uid" type="string">
          Output only. System generated unique id for the GlossaryCategory. This ID will be different if the GlossaryCategory is deleted and re-created with the same name. Returned by the lookup.
        </ResponseField>
        <ResponseField name="update_time" type="string">
          Output only. The time at which the GlossaryCategory was last updated. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="update_mask" type="string">
      Required. The list of fields to update. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_glossary_term" type="object">
  Update GlossaryTerm Request Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="term" type="object">
      GlossaryTerms are the core of Glossary. A GlossaryTerm holds a rich text description that can be attached to Entries or specific columns to enrich them. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="create_time" type="string">
          Output only. The time at which the GlossaryTerm was created. Returned by the lookup.
        </ResponseField>
        <ResponseField name="description" type="string">
          Optional. The user-mutable description of the GlossaryTerm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="display_name" type="string">
          Optional. User friendly display name of the GlossaryTerm. This is user-mutable. This will be same as the GlossaryTermId, if not specified. Optional lookup argument.
        </ResponseField>
        <ResponseField name="labels" type="map(string)">
          Optional. User-defined labels for the GlossaryTerm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Output only. Identifier. The resource name of the GlossaryTerm. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125;/terms/&#123;term_id&#125; Returned by the lookup.
        </ResponseField>
        <ResponseField name="parent" type="string">
          Required. The immediate parent of the GlossaryTerm in the resource-hierarchy. It can either be a Glossary or a GlossaryCategory. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125; OR projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/glossaries/&#123;glossary_id&#125;/categories/&#123;category_id&#125; Optional lookup argument.
        </ResponseField>
        <ResponseField name="uid" type="string">
          Output only. System generated unique id for the GlossaryTerm. This ID will be different if the GlossaryTerm is deleted and re-created with the same name. Returned by the lookup.
        </ResponseField>
        <ResponseField name="update_time" type="string">
          Output only. The time at which the GlossaryTerm was last updated. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="update_mask" type="string">
      Required. The list of fields to update. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time when the ChangeRequest was last updated. Returned by the lookup.
</ResponseField>
