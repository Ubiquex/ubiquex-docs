---
title: "google_data_fusion_instance_iam_policy"
description: "Real, generated bindings for `google_data_fusion_instance_iam_policy`."
---

`google_data_fusion_instance_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/data"
    )

    cfg := data.FusionInstanceIamPolicyConfig{
        Name: "example-name",
        PolicyData: "example",
    }
    ubx.Resource(data.FusionInstanceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FusionInstanceIamPolicy } from
      "jsr:@ubx/sdk-google/google/data/fusion_instance_iam_policy";

    resource(FusionInstanceIamPolicy, "example", {
      name: "example-name",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.data import (
        FusionInstanceIamPolicy,
        FusionInstanceIamPolicyConfig,
    )

    cfg = FusionInstanceIamPolicyConfig(
        name="example-name",
        policy_data="example",
    )
    ubx.resource(FusionInstanceIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_data_fusion_instance_iam_policy with name,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
