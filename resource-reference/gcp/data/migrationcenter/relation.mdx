---
title: "google_migrationcenter_relation"
description: "`data_migrationcenter_relation` looks up a real, discovered relationship between two assets tracked by Migration Center."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	migrationcenter "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/migrationcenter"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_migrationcenter_relation"})

    		ubx.Data(migrationcenter.Relation, "example", migrationcenter.RelationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Relation } from "@ubx/sdk-google/google/data/migrationcenter/relation";

    export default stack("example", () => {
      intent({ summary: "look up google_migrationcenter_relation" });

      data(Relation, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.migrationcenter import Relation, RelationConfig

    def describe():
        ubx.intent("look up google_migrationcenter_relation")

        ubx.data(Relation, "example", RelationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the relation was created. Returned by the lookup.
</ResponseField>

<ResponseField name="dst_asset" type="string">
  Output only. The destination asset name in the relation. Returned by the lookup.
</ResponseField>

<ResponseField name="src_asset" type="string">
  Output only. The source asset name in the relation. Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  Optional. The type of the relation. Returned by the lookup.
</ResponseField>
