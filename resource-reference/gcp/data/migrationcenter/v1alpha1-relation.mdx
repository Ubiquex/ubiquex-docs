---
title: "google_migrationcenter_v1alpha1_relation"
description: "`data_migrationcenter_v1alpha1_relation` looks up a real relationship between two assets tracked by Migration Center, via the v1alpha1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	migrationcenter "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/migrationcenter"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_migrationcenter_v1alpha1_relation"})

    		ubx.Data(migrationcenter.V1alpha1Relation, "example", migrationcenter.V1alpha1RelationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1alpha1Relation } from "./local-sdk/google/sdk/typescript/google/data/migrationcenter/v1alpha1_relation.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_migrationcenter_v1alpha1_relation" });

      data(V1alpha1Relation, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.migrationcenter import V1alpha1Relation, V1alpha1RelationConfig

    def describe():
        ubx.intent("look up google_migrationcenter_v1alpha1_relation")

        ubx.data(V1alpha1Relation, "example", V1alpha1RelationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the relation was created. Returned by the lookup.
</ResponseField>

<ResponseField name="dst_asset" type="string">
  Output only. The destination asset name in the relation. Returned by the lookup.
</ResponseField>

<ResponseField name="src_asset" type="string">
  Output only. The source asset name in the relation. Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  Optional. The type of the relation. Returned by the lookup.
</ResponseField>
