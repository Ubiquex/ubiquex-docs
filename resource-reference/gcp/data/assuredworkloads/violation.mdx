---
title: "google_assuredworkloads_violation"
description: "`google_assuredworkloads_violation` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_assuredworkloads_violation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	assuredworkloads "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/assuredworkloads"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_assuredworkloads_violation"})

    		ubx.Data(assuredworkloads.Violation, "example", assuredworkloads.ViolationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Violation } from "@ubx/sdk-google/google/data/assuredworkloads/violation";

    export default stack("example", () => {
      intent({ summary: "look up google_assuredworkloads_violation" });

      data(Violation, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.assuredworkloads import Violation, ViolationConfig

    def describe():
        ubx.intent("look up google_assuredworkloads_violation")

        ubx.data(Violation, "example", ViolationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="acknowledged" type="bool">
  A boolean that indicates if the violation is acknowledged Returned by the lookup.
</ResponseField>

<ResponseField name="acknowledgement_time" type="string">
  Optional. Timestamp when this violation was acknowledged first. Check exception_contexts to find the last time the violation was acknowledged when there are more than one violations. This field will be absent when acknowledged field is marked as false. Returned by the lookup.
</ResponseField>

<ResponseField name="associated_org_policy_violation_id" type="string">
  Optional. Output only. Violation Id of the org-policy violation due to which the resource violation is caused. Empty for org-policy violations. Returned by the lookup.
</ResponseField>

<ResponseField name="audit_log_link" type="string">
  Output only. Immutable. Audit Log Link for violated resource Format: https://console.cloud.google.com/logs/query;query=&#123;logName&#125;&#123;protoPayload.resourceName&#125;&#123;timeRange&#125;&#123;folder&#125; Returned by the lookup.
</ResponseField>

<ResponseField name="begin_time" type="string">
  Output only. Time of the event which triggered the Violation. Returned by the lookup.
</ResponseField>

<ResponseField name="category" type="string">
  Output only. Category under which this violation is mapped. e.g. Location, Service Usage, Access, Encryption, etc. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Output only. Description for the Violation. e.g. OrgPolicy gcp.resourceLocations has non compliant value. Returned by the lookup.
</ResponseField>

<ResponseField name="exception_audit_log_link" type="string">
  Output only. Immutable. Audit Log link to find business justification provided for violation exception. Format: https://console.cloud.google.com/logs/query;query=&#123;logName&#125;&#123;protoPayload.resourceName&#125;&#123;protoPayload.methodName&#125;&#123;timeRange&#125;&#123;organization&#125; Returned by the lookup.
</ResponseField>

<ResponseField name="exception_contexts" type="list(object)">
  Output only. List of all the exception detail added for the violation. Returned by the lookup.
</ResponseField>

<ResponseField name="non_compliant_org_policy" type="string">
  Output only. Immutable. Name of the OrgPolicy which was modified with non-compliant change and resulted this violation. Format: projects/&#123;project_number&#125;/policies/&#123;constraint_name&#125; folders/&#123;folder_id&#125;/policies/&#123;constraint_name&#125; organizations/&#123;organization_id&#125;/policies/&#123;constraint_name&#125; Returned by the lookup.
</ResponseField>

<ResponseField name="org_policy_constraint" type="string">
  Output only. Immutable. The org-policy-constraint that was incorrectly changed, which resulted in this violation. Returned by the lookup.
</ResponseField>

<ResponseField name="parent_project_number" type="string">
  Optional. Output only. Parent project number where resource is present. Empty for org-policy violations. Returned by the lookup.
</ResponseField>

<ResponseField name="remediation" type="object">
  Represents remediation guidance to resolve compliance violation for AssuredWorkload Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="compliant_values" type="list(string)">
      Values that can resolve the violation For example: for list org policy violations, this will either be the list of allowed or denied values Optional lookup argument.
    </ResponseField>
    <ResponseField name="instructions" type="object">
      Instructions to remediate violation Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="console_instructions" type="object">
          Remediation instructions to resolve violation via cloud console Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="additional_links" type="list(string)">
              Additional urls for more information about steps Optional lookup argument.
            </ResponseField>
            <ResponseField name="console_uris" type="list(string)">
              Link to console page where violations can be resolved Optional lookup argument.
            </ResponseField>
            <ResponseField name="steps" type="list(string)">
              Steps to resolve violation via cloud console Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="gcloud_instructions" type="object">
          Remediation instructions to resolve violation via gcloud cli Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="additional_links" type="list(string)">
              Additional urls for more information about steps Optional lookup argument.
            </ResponseField>
            <ResponseField name="gcloud_commands" type="list(string)">
              Gcloud command to resolve violation Optional lookup argument.
            </ResponseField>
            <ResponseField name="steps" type="list(string)">
              Steps to resolve violation via gcloud cli Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="remediation_type" type="string">
      Output only. Reemediation type based on the type of org policy values violated Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resolve_time" type="string">
  Output only. Time of the event which fixed the Violation. If the violation is ACTIVE this will be empty. Returned by the lookup.
</ResponseField>

<ResponseField name="resource_name" type="string">
  Optional. Output only. Name of the resource like //storage.googleapis.com/myprojectxyz-testbucket. Empty for org-policy violations. Returned by the lookup.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Optional. Output only. Type of the resource like compute.googleapis.com/Disk, etc. Empty for org-policy violations. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. State of the violation Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The last time when the Violation record was updated. Returned by the lookup.
</ResponseField>

<ResponseField name="violation_type" type="string">
  Output only. Type of the violation Returned by the lookup.
</ResponseField>
