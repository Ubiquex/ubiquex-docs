---
title: "google_backupdr_revision"
description: "`google_backupdr_revision` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_backupdr_revision` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	backupdr "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/backupdr"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_backupdr_revision"})

    		ubx.Data(backupdr.Revision, "example", backupdr.RevisionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Revision } from "@ubx/sdk-google/google/data/backupdr/revision";

    export default stack("example", () => {
      intent({ summary: "look up google_backupdr_revision" });

      data(Revision, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.backupdr import Revision, RevisionConfig

    def describe():
        ubx.intent("look up google_backupdr_revision")

        ubx.data(Revision, "example", RevisionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backup_plan_snapshot" type="object">
  A `BackupPlan` specifies some common fields, such as `description` as well as one or more `BackupRule` messages. Each `BackupRule` has a retention policy and defines a schedule by which the system is to perform backup workloads. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="backup_rules" type="list(object)">
      Optional. The backup rules for this `BackupPlan`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_vault" type="string">
      Required. Resource name of backup vault which will be used as storage location for backups. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/backupVaults/&#123;backupvault&#125; Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_vault_service_account" type="string">
      Output only. The Google Cloud service account to be used by the BackupVault for taking backups. Specify the email address of the Backup Vault Service Account. Returned by the lookup.
    </ResponseField>
    <ResponseField name="compute_instance_backup_plan_properties" type="object">
      Properties for a compute instance backup plan. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="boot_disk_only" type="bool">
          Optional. If true, only the boot disk will be backed up. Optional lookup argument.
        </ResponseField>
        <ResponseField name="disk_exclusion_labels" type="object">
          Message for selective disk backup exclusion labels. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="labels" type="list(object)">
              Optional. Labels used to identify disks for exclusion from the backup. If a disk carries any of these labels, it will be excluded (OR logic). Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="guest_flush" type="bool">
          Optional. Indicates whether to perform a guest flush operation before taking a compute backup. When set to false, the system will create crash-consistent backups. Default value is false. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Output only. When the `BackupPlan` was created. Returned by the lookup.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional. The description of the `BackupPlan` resource. The description allows for additional details about `BackupPlan` and its use cases to be provided. An example description is the following: "This is a backup plan that performs a daily backup at 6pm and retains data for 3 months". The description must be at most 2048 characters. Optional lookup argument.
    </ResponseField>
    <ResponseField name="disk_backup_plan_properties" type="object">
      Properties for a disk backup plan. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="guest_flush" type="bool">
          Optional. Indicates whether to perform a guest flush operation before taking a disk backup. When set to false, the system will create crash-consistent backups. Default value is false. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="etag" type="string">
      Optional. `etag` is returned from the service in the response. As a user of the service, you may provide an etag value in this field to prevent stale resources. Optional lookup argument.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Optional. This collection of key/value pairs allows for custom labels to be supplied by the user. Example, &#123;"tag": "Weekly"&#125;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="log_retention_days" type="string">
      Optional. Applicable only for Cloud SQL resource_type. Configures how long logs will be stored. It is defined in “days”. This value should be greater than or equal to minimum enforced log retention duration of the backup vault. Optional lookup argument.
    </ResponseField>
    <ResponseField name="max_custom_on_demand_retention_days" type="number">
      Optional. Optional field to configure the maximum number of days for which a backup can be retained. This field is only applicable for on-demand backups taken with custom retention value. Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Output only. Identifier. The resource name of the `BackupPlan`. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/backupPlans/&#123;backup_plan&#125;` Returned by the lookup.
    </ResponseField>
    <ResponseField name="resource_type" type="string">
      Required. The resource type to which the `BackupPlan` will be applied. Examples include, "compute.googleapis.com/Instance", "sqladmin.googleapis.com/Instance", "alloydb.googleapis.com/Cluster", "compute.googleapis.com/Disk". Optional lookup argument.
    </ResponseField>
    <ResponseField name="revision_id" type="string">
      Output only. The user friendly revision ID of the `BackupPlanRevision`. Example: v0, v1, v2, etc. Returned by the lookup.
    </ResponseField>
    <ResponseField name="revision_name" type="string">
      Output only. The resource id of the `BackupPlanRevision`. Format: `projects/&#123;project&#125;/locations/&#123;location&#125;/backupPlans/&#123;backup_plan&#125;/revisions/&#123;revision_id&#125;` Returned by the lookup.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The `State` for the `BackupPlan`. Returned by the lookup.
    </ResponseField>
    <ResponseField name="supported_resource_types" type="list(string)">
      Output only. All resource types to which backupPlan can be applied. Returned by the lookup.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. When the `BackupPlan` was last updated. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The timestamp that the revision was created. Returned by the lookup.
</ResponseField>

<ResponseField name="revision_id" type="string">
  Output only. The user friendly revision ID of the `BackupPlanRevision`. Example: v0, v1, v2, etc. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. Resource State Returned by the lookup.
</ResponseField>
