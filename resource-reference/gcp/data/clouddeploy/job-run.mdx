---
title: "google_clouddeploy_job_run"
description: "`google_clouddeploy_job_run` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_clouddeploy_job_run` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	clouddeploy "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/clouddeploy"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_clouddeploy_job_run"})

    		ubx.Data(clouddeploy.JobRun, "example", clouddeploy.JobRunConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobRun } from "@ubx/sdk-google/google/data/clouddeploy/job_run";

    export default stack("example", () => {
      intent({ summary: "look up google_clouddeploy_job_run" });

      data(JobRun, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.clouddeploy import JobRun, JobRunConfig

    def describe():
        ubx.intent("look up google_clouddeploy_job_run")

        ubx.data(JobRun, "example", JobRunConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="advance_child_rollout_job_run" type="object">
  AdvanceChildRolloutJobRun contains information specific to a advanceChildRollout `JobRun`. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="rollout" type="string">
      Output only. Name of the `ChildRollout`. Format is `projects/&#123;project&#125;/locations/&#123;location&#125;/deliveryPipelines/&#123;deliveryPipeline&#125;/releases/&#123;release&#125;/rollouts/&#123;rollout&#125;`. Returned by the lookup.
    </ResponseField>
    <ResponseField name="rollout_phase_id" type="string">
      Output only. the ID of the ChildRollout's Phase. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="analysis_job_run" type="object">
  AnalysisJobRun contains information specific to an analysis `JobRun`. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="alert_policy_analyses" type="list(object)">
      Output only. The status of the running alert policy checks configured for this analysis. Returned by the lookup.
    </ResponseField>
    <ResponseField name="custom_check_analyses" type="list(object)">
      Output only. The status of the running custom checks configured for this analysis. Returned by the lookup.
    </ResponseField>
    <ResponseField name="failed_check_id" type="string">
      Output only. The ID of the configured check that failed. This will always be blank while the analysis is in progress or if it succeeded. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_child_rollout_job_run" type="object">
  CreateChildRolloutJobRun contains information specific to a createChildRollout `JobRun`. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="rollout" type="string">
      Output only. Name of the `ChildRollout`. Format is `projects/&#123;project&#125;/locations/&#123;location&#125;/deliveryPipelines/&#123;deliveryPipeline&#125;/releases/&#123;release&#125;/rollouts/&#123;rollout&#125;`. Returned by the lookup.
    </ResponseField>
    <ResponseField name="rollout_phase_id" type="string">
      Output only. The ID of the childRollout Phase initiated by this JobRun. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Time at which the `JobRun` was created. Returned by the lookup.
</ResponseField>

<ResponseField name="deploy_job_run" type="object">
  DeployJobRun contains information specific to a deploy `JobRun`. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="artifact" type="object">
      The artifacts produced by a deploy operation. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="artifact_uri" type="string">
          Output only. URI of a directory containing the artifacts. All paths are relative to this location. Returned by the lookup.
        </ResponseField>
        <ResponseField name="manifest_paths" type="list(string)">
          Output only. File paths of the manifests applied during the deploy operation relative to the URI. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="build" type="string">
      Output only. The resource name of the Cloud Build `Build` object that is used to deploy. Format is `projects/&#123;project&#125;/locations/&#123;location&#125;/builds/&#123;build&#125;`. Returned by the lookup.
    </ResponseField>
    <ResponseField name="failure_cause" type="string">
      Output only. The reason the deploy failed. This will always be unspecified while the deploy is in progress or if it succeeded. Returned by the lookup.
    </ResponseField>
    <ResponseField name="failure_message" type="string">
      Output only. Additional information about the deploy failure, if available. Returned by the lookup.
    </ResponseField>
    <ResponseField name="metadata" type="object">
      DeployJobRunMetadata surfaces information associated with a `DeployJobRun` to the user. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="cloud_run" type="object">
          CloudRunMetadata contains information from a Cloud Run deployment. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="job" type="string">
              Output only. The name of the Cloud Run job that is associated with a `Rollout`. Format is `projects/&#123;project&#125;/locations/&#123;location&#125;/jobs/&#123;job_name&#125;`. Returned by the lookup.
            </ResponseField>
            <ResponseField name="previous_revision" type="string">
              Output only. The previous Cloud Run Revision name associated with a `Rollout`. Only set when a canary deployment strategy is configured. Format for service is projects/&#123;project&#125;/locations/&#123;location&#125;/services/&#123;service&#125;/revisions/&#123;revision&#125;. Format for worker pool is projects/&#123;project&#125;/locations/&#123;location&#125;/workerPools/&#123;workerpool&#125;/revisions/&#123;revision&#125;. Returned by the lookup.
            </ResponseField>
            <ResponseField name="revision" type="string">
              Output only. The Cloud Run Revision id associated with a `Rollout`. Returned by the lookup.
            </ResponseField>
            <ResponseField name="service" type="string">
              Output only. The name of the Cloud Run Service that is associated with a `Rollout`. Format is `projects/&#123;project&#125;/locations/&#123;location&#125;/services/&#123;service&#125;`. Returned by the lookup.
            </ResponseField>
            <ResponseField name="service_urls" type="list(string)">
              Output only. The Cloud Run Service urls that are associated with a `Rollout`. Returned by the lookup.
            </ResponseField>
            <ResponseField name="worker_pool" type="string">
              Output only. The Cloud Run worker pool associated with a `Rollout`. Format is `projects/&#123;project&#125;/locations/&#123;location&#125;/workerPools/&#123;worker_pool&#125;`. Returned by the lookup.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="custom" type="object">
          CustomMetadata contains information from a user-defined operation. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="values" type="map(string)">
              Output only. Key-value pairs provided by the user-defined operation. Returned by the lookup.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="custom_target" type="object">
          CustomTargetDeployMetadata contains information from a Custom Target deploy operation. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="skip_message" type="string">
              Output only. Skip message provided in the results of a custom deploy operation. Returned by the lookup.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="end_time" type="string">
  Output only. Time at which the `JobRun` ended. Returned by the lookup.
</ResponseField>

<ResponseField name="etag" type="string">
  Output only. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Returned by the lookup.
</ResponseField>

<ResponseField name="job_id" type="string">
  Output only. ID of the `Rollout` job this `JobRun` corresponds to. Returned by the lookup.
</ResponseField>

<ResponseField name="phase_id" type="string">
  Output only. ID of the `Rollout` phase this `JobRun` belongs in. Returned by the lookup.
</ResponseField>

<ResponseField name="postdeploy_job_run" type="object">
  PostdeployJobRun contains information specific to a postdeploy `JobRun`. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="build" type="string">
      Output only. The resource name of the Cloud Build `Build` object that is used to execute the custom actions associated with the postdeploy Job. Format is `projects/&#123;project&#125;/locations/&#123;location&#125;/builds/&#123;build&#125;`. Returned by the lookup.
    </ResponseField>
    <ResponseField name="failure_cause" type="string">
      Output only. The reason the postdeploy failed. This will always be unspecified while the postdeploy is in progress or if it succeeded. Returned by the lookup.
    </ResponseField>
    <ResponseField name="failure_message" type="string">
      Output only. Additional information about the postdeploy failure, if available. Returned by the lookup.
    </ResponseField>
    <ResponseField name="metadata" type="object">
      PostdeployJobRunMetadata contains metadata about the postdeploy `JobRun`. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="custom" type="object">
          CustomMetadata contains information from a user-defined operation. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="values" type="map(string)">
              Output only. Key-value pairs provided by the user-defined operation. Returned by the lookup.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="predeploy_job_run" type="object">
  PredeployJobRun contains information specific to a predeploy `JobRun`. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="build" type="string">
      Output only. The resource name of the Cloud Build `Build` object that is used to execute the custom actions associated with the predeploy Job. Format is `projects/&#123;project&#125;/locations/&#123;location&#125;/builds/&#123;build&#125;`. Returned by the lookup.
    </ResponseField>
    <ResponseField name="failure_cause" type="string">
      Output only. The reason the predeploy failed. This will always be unspecified while the predeploy is in progress or if it succeeded. Returned by the lookup.
    </ResponseField>
    <ResponseField name="failure_message" type="string">
      Output only. Additional information about the predeploy failure, if available. Returned by the lookup.
    </ResponseField>
    <ResponseField name="metadata" type="object">
      PredeployJobRunMetadata contains metadata about the predeploy `JobRun`. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="custom" type="object">
          CustomMetadata contains information from a user-defined operation. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="values" type="map(string)">
              Output only. Key-value pairs provided by the user-defined operation. Returned by the lookup.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_time" type="string">
  Output only. Time at which the `JobRun` was started. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the `JobRun`. Returned by the lookup.
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. Unique identifier of the `JobRun`. Returned by the lookup.
</ResponseField>

<ResponseField name="verify_job_run" type="object">
  VerifyJobRun contains information specific to a verify `JobRun`. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="artifact_uri" type="string">
      Output only. URI of a directory containing the verify artifacts. This contains the Skaffold event log. Returned by the lookup.
    </ResponseField>
    <ResponseField name="build" type="string">
      Output only. The resource name of the Cloud Build `Build` object that is used to verify. Format is `projects/&#123;project&#125;/locations/&#123;location&#125;/builds/&#123;build&#125;`. Returned by the lookup.
    </ResponseField>
    <ResponseField name="event_log_path" type="string">
      Output only. File path of the Skaffold event log relative to the artifact URI. Returned by the lookup.
    </ResponseField>
    <ResponseField name="failure_cause" type="string">
      Output only. The reason the verify failed. This will always be unspecified while the verify is in progress or if it succeeded. Returned by the lookup.
    </ResponseField>
    <ResponseField name="failure_message" type="string">
      Output only. Additional information about the verify failure, if available. Returned by the lookup.
    </ResponseField>
    <ResponseField name="metadata" type="object">
      VerifyJobRunMetadata contains metadata about the verify `JobRun`. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="custom" type="object">
          CustomMetadata contains information from a user-defined operation. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="values" type="map(string)">
              Output only. Key-value pairs provided by the user-defined operation. Returned by the lookup.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
