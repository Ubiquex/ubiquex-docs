---
title: "google_osconfig_v1beta_patch_job"
description: "`data_osconfig_v1beta_patch_job` looks up a real OS patch job executed across a fleet of instances by OS Config, via the v1beta API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	osconfig "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/osconfig"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_osconfig_v1beta_patch_job"})

    		ubx.Data(osconfig.V1betaPatchJob, "example", osconfig.V1betaPatchJobConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1betaPatchJob } from "./local-sdk/google/sdk/typescript/google/data/osconfig/v1beta_patch_job.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_osconfig_v1beta_patch_job" });

      data(V1betaPatchJob, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.osconfig import V1betaPatchJob, V1betaPatchJobConfig

    def describe():
        ubx.intent("look up google_osconfig_v1beta_patch_job")

        ubx.data(V1betaPatchJob, "example", V1betaPatchJobConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Time this patch job was created. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Description of the patch job. Length of the description is limited to 1024 characters. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  Display name for this patch job. This is not a unique identifier. Returned by the lookup.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  If this patch job is a dry run, the agent reports that it has finished without running any updates on the VM instance. Returned by the lookup.
</ResponseField>

<ResponseField name="duration" type="string">
  Duration of the patch job. After the duration ends, the patch job times out. Returned by the lookup.
</ResponseField>

<ResponseField name="error_message" type="string">
  If this patch job failed, this message provides information about the failure. Returned by the lookup.
</ResponseField>

<ResponseField name="instance_details_summary" type="object">
  A summary of the current patch state across all instances that this patch job affects. Contains counts of instances in different states. These states map to `InstancePatchState`. List patch job instance details to see the specific states of each instance. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="acked_instance_count" type="string">
      Number of instances that have acked and will start shortly. Optional lookup argument.
    </ResponseField>
    <ResponseField name="applying_patches_instance_count" type="string">
      Number of instances that are applying patches. Optional lookup argument.
    </ResponseField>
    <ResponseField name="downloading_patches_instance_count" type="string">
      Number of instances that are downloading patches. Optional lookup argument.
    </ResponseField>
    <ResponseField name="failed_instance_count" type="string">
      Number of instances that failed. Optional lookup argument.
    </ResponseField>
    <ResponseField name="inactive_instance_count" type="string">
      Number of instances that are inactive. Optional lookup argument.
    </ResponseField>
    <ResponseField name="no_agent_detected_instance_count" type="string">
      Number of instances that do not appear to be running the agent. Check to ensure that the agent is installed, running, and able to communicate with the service. Optional lookup argument.
    </ResponseField>
    <ResponseField name="notified_instance_count" type="string">
      Number of instances notified about patch job. Optional lookup argument.
    </ResponseField>
    <ResponseField name="pending_instance_count" type="string">
      Number of instances pending patch job. Optional lookup argument.
    </ResponseField>
    <ResponseField name="post_patch_step_instance_count" type="string">
      Number of instances that are running the post-patch step. Optional lookup argument.
    </ResponseField>
    <ResponseField name="pre_patch_step_instance_count" type="string">
      Number of instances that are running the pre-patch step. Optional lookup argument.
    </ResponseField>
    <ResponseField name="rebooting_instance_count" type="string">
      Number of instances rebooting. Optional lookup argument.
    </ResponseField>
    <ResponseField name="skipped_instance_count" type="string">
      Number of instances that were skipped during patching. Optional lookup argument.
    </ResponseField>
    <ResponseField name="started_instance_count" type="string">
      Number of instances that have started. Optional lookup argument.
    </ResponseField>
    <ResponseField name="succeeded_instance_count" type="string">
      Number of instances that have completed successfully. Optional lookup argument.
    </ResponseField>
    <ResponseField name="succeeded_reboot_required_instance_count" type="string">
      Number of instances that require reboot. Optional lookup argument.
    </ResponseField>
    <ResponseField name="timed_out_instance_count" type="string">
      Number of instances that exceeded the time out while applying the patch. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_filter" type="object">
  A filter to target VM instances for patching. The targeted VMs must meet all criteria specified. So if both labels and zones are specified, the patch job targets only VMs with those labels and in those zones. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="all" type="bool">
      Target all VM instances in the project. If true, no other criteria is permitted. Optional lookup argument.
    </ResponseField>
    <ResponseField name="group_labels" type="list(object)">
      Targets VM instances matching at least one of these label sets. This allows targeting of disparate groups, for example "env=prod or env=staging". Optional lookup argument.
    </ResponseField>
    <ResponseField name="instance_name_prefixes" type="list(string)">
      Targets VMs whose name starts with one of these prefixes. Similar to labels, this is another way to group VMs when targeting configs, for example prefix="prod-". Optional lookup argument.
    </ResponseField>
    <ResponseField name="instances" type="list(string)">
      Targets any of the VM instances specified. Instances are specified by their URI in the form `zones/[ZONE]/instances/[INSTANCE_NAME]`, `projects/[PROJECT_ID]/zones/[ZONE]/instances/[INSTANCE_NAME]`, or `https://www.googleapis.com/compute/v1/projects/[PROJECT_ID]/zones/[ZONE]/instances/[INSTANCE_NAME]` Optional lookup argument.
    </ResponseField>
    <ResponseField name="zones" type="list(string)">
      Targets VM instances in ANY of these zones. Leave empty to target VM instances in any zone. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="patch_config" type="object">
  Patch configuration specifications. Contains details on how to apply the patch(es) to a VM instance. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="apt" type="object">
      Apt patching is completed by executing `apt-get update && apt-get upgrade`. Additional options can be set to control how this is executed. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="excludes" type="list(string)">
          List of packages to exclude from update. These packages will be excluded Optional lookup argument.
        </ResponseField>
        <ResponseField name="exclusive_packages" type="list(string)">
          An exclusive list of packages to be updated. These are the only packages that will be updated. If these packages are not installed, they will be ignored. This field cannot be specified with any other patch configuration fields. Optional lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string">
          By changing the type to DIST, the patching is performed using `apt-get dist-upgrade` instead. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="goo" type="any">
      Googet patching is performed by running `googet update`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="mig_instances_allowed" type="bool">
      Allows the patch job to run on Managed instance groups (MIGs). Optional lookup argument.
    </ResponseField>
    <ResponseField name="post_step" type="object">
      A step that runs an executable for a PatchJob. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="linux_exec_step_config" type="object">
          Common configurations for an ExecStep. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="allowed_success_codes" type="list(number)">
              Defaults to [0]. A list of possible return values that the execution can return to indicate a success. Optional lookup argument.
            </ResponseField>
            <ResponseField name="gcs_object" type="object">
              Google Cloud Storage object representation. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="bucket" type="string">
                  Required. Bucket of the Google Cloud Storage object. Optional lookup argument.
                </ResponseField>
                <ResponseField name="generation_number" type="string">
                  Required. Generation number of the Google Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change. Optional lookup argument.
                </ResponseField>
                <ResponseField name="object" type="string">
                  Required. Name of the Google Cloud Storage object. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="interpreter" type="string">
              The script interpreter to use to run the script. If no interpreter is specified the script will be executed directly, which will likely only succeed for scripts with [shebang lines] (https://en.wikipedia.org/wiki/Shebang_\(Unix\)). Optional lookup argument.
            </ResponseField>
            <ResponseField name="local_path" type="string">
              An absolute path to the executable on the VM. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="windows_exec_step_config" type="object">
          Common configurations for an ExecStep. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="allowed_success_codes" type="list(number)">
              Defaults to [0]. A list of possible return values that the execution can return to indicate a success. Optional lookup argument.
            </ResponseField>
            <ResponseField name="gcs_object" type="object">
              Google Cloud Storage object representation. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="bucket" type="string">
                  Required. Bucket of the Google Cloud Storage object. Optional lookup argument.
                </ResponseField>
                <ResponseField name="generation_number" type="string">
                  Required. Generation number of the Google Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change. Optional lookup argument.
                </ResponseField>
                <ResponseField name="object" type="string">
                  Required. Name of the Google Cloud Storage object. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="interpreter" type="string">
              The script interpreter to use to run the script. If no interpreter is specified the script will be executed directly, which will likely only succeed for scripts with [shebang lines] (https://en.wikipedia.org/wiki/Shebang_\(Unix\)). Optional lookup argument.
            </ResponseField>
            <ResponseField name="local_path" type="string">
              An absolute path to the executable on the VM. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pre_step" type="object">
      A step that runs an executable for a PatchJob. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="linux_exec_step_config" type="object">
          Common configurations for an ExecStep. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="allowed_success_codes" type="list(number)">
              Defaults to [0]. A list of possible return values that the execution can return to indicate a success. Optional lookup argument.
            </ResponseField>
            <ResponseField name="gcs_object" type="object">
              Google Cloud Storage object representation. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="bucket" type="string">
                  Required. Bucket of the Google Cloud Storage object. Optional lookup argument.
                </ResponseField>
                <ResponseField name="generation_number" type="string">
                  Required. Generation number of the Google Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change. Optional lookup argument.
                </ResponseField>
                <ResponseField name="object" type="string">
                  Required. Name of the Google Cloud Storage object. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="interpreter" type="string">
              The script interpreter to use to run the script. If no interpreter is specified the script will be executed directly, which will likely only succeed for scripts with [shebang lines] (https://en.wikipedia.org/wiki/Shebang_\(Unix\)). Optional lookup argument.
            </ResponseField>
            <ResponseField name="local_path" type="string">
              An absolute path to the executable on the VM. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="windows_exec_step_config" type="object">
          Common configurations for an ExecStep. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="allowed_success_codes" type="list(number)">
              Defaults to [0]. A list of possible return values that the execution can return to indicate a success. Optional lookup argument.
            </ResponseField>
            <ResponseField name="gcs_object" type="object">
              Google Cloud Storage object representation. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="bucket" type="string">
                  Required. Bucket of the Google Cloud Storage object. Optional lookup argument.
                </ResponseField>
                <ResponseField name="generation_number" type="string">
                  Required. Generation number of the Google Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change. Optional lookup argument.
                </ResponseField>
                <ResponseField name="object" type="string">
                  Required. Name of the Google Cloud Storage object. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="interpreter" type="string">
              The script interpreter to use to run the script. If no interpreter is specified the script will be executed directly, which will likely only succeed for scripts with [shebang lines] (https://en.wikipedia.org/wiki/Shebang_\(Unix\)). Optional lookup argument.
            </ResponseField>
            <ResponseField name="local_path" type="string">
              An absolute path to the executable on the VM. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reboot_config" type="string">
      Post-patch reboot settings. Optional lookup argument.
    </ResponseField>
    <ResponseField name="skip_unpatchable_vms" type="bool">
      Optional. Enables enhanced reporting for the patch job: 1. The patch job skips instances that cannot be patched and reports them as `SKIPPED`. An instance cannot be patched for two reasons: 1. The instance runs Container-Optimized OS (COS), which cannot be patched. 2. The instance is part of a managed instance group (MIG), and patching MIG instances is disabled in the patch job's configuration (PatchConfig.migInstancesAllowed is `false`). 2. The patch job is reported as `SUCCEEDED` if it completes without errors, even if some instances are `SKIPPED`. 3. The patch job is reported as `COMPLETED_WITH_INACTIVE_VMS` if it completes without errors, but does not patch instances that are `INACTIVE`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="windows_update" type="object">
      Windows patching is performed using the Windows Update Agent. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="classifications" type="list(string)">
          Only apply updates of these windows update classifications. If empty, all updates are applied. Optional lookup argument.
        </ResponseField>
        <ResponseField name="excludes" type="list(string)">
          List of KBs to exclude from update. Optional lookup argument.
        </ResponseField>
        <ResponseField name="exclusive_patches" type="list(string)">
          An exclusive list of kbs to be updated. These are the only patches that will be updated. This field must not be used with other patch configurations. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="yum" type="object">
      Yum patching is performed by executing `yum update`. Additional options can be set to control how this is executed. Note that not all settings are supported on all platforms. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="excludes" type="list(string)">
          List of packages to exclude from update. These packages are excluded by using the yum `--exclude` flag. Optional lookup argument.
        </ResponseField>
        <ResponseField name="exclusive_packages" type="list(string)">
          An exclusive list of packages to be updated. These are the only packages that will be updated. If these packages are not installed, they will be ignored. This field must not be specified with any other patch configuration fields. Optional lookup argument.
        </ResponseField>
        <ResponseField name="minimal" type="bool">
          Will cause patch to run `yum update-minimal` instead. Optional lookup argument.
        </ResponseField>
        <ResponseField name="security" type="bool">
          Adds the `--security` flag to `yum update`. Not supported on all platforms. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="zypper" type="object">
      Zypper patching is performed by running `zypper patch`. See also https://en.opensuse.org/SDB:Zypper_manual. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="categories" type="list(string)">
          Install only patches with these categories. Common categories include security, recommended, and feature. Optional lookup argument.
        </ResponseField>
        <ResponseField name="excludes" type="list(string)">
          List of patches to exclude from update. Optional lookup argument.
        </ResponseField>
        <ResponseField name="exclusive_patches" type="list(string)">
          An exclusive list of patches to be updated. These are the only patches that will be installed using 'zypper patch patch:' command. This field must not be used with any other patch configuration fields. Optional lookup argument.
        </ResponseField>
        <ResponseField name="severities" type="list(string)">
          Install only patches with these severities. Common severities include critical, important, moderate, and low. Optional lookup argument.
        </ResponseField>
        <ResponseField name="with_optional" type="bool">
          Adds the `--with-optional` flag to `zypper patch`. Optional lookup argument.
        </ResponseField>
        <ResponseField name="with_update" type="bool">
          Adds the `--with-update` flag, to `zypper patch`. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="patch_deployment" type="string">
  Output only. Name of the patch deployment that created this patch job. Returned by the lookup.
</ResponseField>

<ResponseField name="percent_complete" type="number">
  Reflects the overall progress of the patch job in the range of 0.0 being no progress to 100.0 being complete. Returned by the lookup.
</ResponseField>

<ResponseField name="rollout" type="object">
  Patch rollout configuration specifications. Contains details on the concurrency control when applying patch(es) to all targeted VMs. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="disruption_budget" type="object">
      Message encapsulating a value that can be either absolute ("fixed") or relative ("percent") to a value. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="fixed" type="number">
          Specifies a fixed value. Optional lookup argument.
        </ResponseField>
        <ResponseField name="percent" type="number">
          Specifies the relative value defined as a percentage, which will be multiplied by a reference value. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mode" type="string">
      Mode of the patch rollout. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the PatchJob. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Last time this patch job was updated. Returned by the lookup.
</ResponseField>
