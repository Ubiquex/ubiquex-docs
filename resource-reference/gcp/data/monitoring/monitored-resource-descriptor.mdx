---
title: "google_monitoring_monitored_resource_descriptor"
description: "`google_monitoring_monitored_resource_descriptor` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_monitoring_monitored_resource_descriptor` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	monitoring "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/monitoring"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_monitoring_monitored_resource_descriptor"})

    		ubx.Data(monitoring.MonitoredResourceDescriptor, "example", monitoring.MonitoredResourceDescriptorConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MonitoredResourceDescriptor } from "@ubx/sdk-google/google/data/monitoring/monitored_resource_descriptor";

    export default stack("example", () => {
      intent({
        summary: "look up google_monitoring_monitored_resource_descriptor",
      });

      data(MonitoredResourceDescriptor, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.monitoring import MonitoredResourceDescriptor, MonitoredResourceDescriptorConfig

    def describe():
        ubx.intent("look up google_monitoring_monitored_resource_descriptor")

        ubx.data(MonitoredResourceDescriptor, "example", MonitoredResourceDescriptorConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="description" type="string">
  Optional. A detailed description of the monitored resource type that might be used in documentation. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. A concise name for the monitored resource type that might be displayed in user interfaces. It should be a Title Cased Noun Phrase, without any article or other determiners. For example, "Google Cloud SQL Database". Returned by the lookup.
</ResponseField>

<ResponseField name="labels" type="list(object)">
  Required. A set of labels used to describe instances of this monitored resource type. For example, an individual Google Cloud SQL database is identified by values for the labels "database_id" and "zone". Returned by the lookup.
</ResponseField>

<ResponseField name="launch_stage" type="string">
  Optional. The launch stage of the monitored resource definition. Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  Required. The monitored resource type. For example, the type "cloudsql_database" represents databases in Google Cloud SQL. For a list of types, see Monitored resource types (https://cloud.google.com/monitoring/api/resources) and Logging resource types (https://cloud.google.com/logging/docs/api/v2/resource-list). Returned by the lookup.
</ResponseField>
