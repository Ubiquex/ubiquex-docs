---
title: "google_agentregistry_v1alpha_endpoint"
description: "`google_agentregistry_v1alpha_endpoint` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_agentregistry_v1alpha_endpoint` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	agentregistry "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/agentregistry"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_agentregistry_v1alpha_endpoint"})

    		ubx.Data(agentregistry.V1alphaEndpoint, "example", agentregistry.V1alphaEndpointConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1alphaEndpoint } from "@ubx/sdk-google/google/data/agentregistry/v1alpha_endpoint";

    export default stack("example", () => {
      intent({ summary: "look up google_agentregistry_v1alpha_endpoint" });

      data(V1alphaEndpoint, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.agentregistry import V1alphaEndpoint, V1alphaEndpointConfig

    def describe():
        ubx.intent("look up google_agentregistry_v1alpha_endpoint")

        ubx.data(V1alphaEndpoint, "example", V1alphaEndpointConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attributes" type="map(map(any))">
  Output only. Attributes of the Endpoint. Valid values: * `agentregistry.googleapis.com/system/RuntimeReference`: &#123;"uri": "//..."&#125; - the URI of the underlying resource hosting the Endpoint, for example, the GKE Deployment. Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Create time. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Output only. Description of an Endpoint. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  Output only. Display name for the Endpoint. Returned by the lookup.
</ResponseField>

<ResponseField name="endpoint_id" type="string">
  Output only. A stable, globally unique identifier for Endpoint. Returned by the lookup.
</ResponseField>

<ResponseField name="interfaces" type="list(object)">
  Required. The connection details for the Endpoint. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Update time. Returned by the lookup.
</ResponseField>
