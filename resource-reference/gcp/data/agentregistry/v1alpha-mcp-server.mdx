---
title: "google_agentregistry_v1alpha_mcp_server"
description: "`google_agentregistry_v1alpha_mcp_server` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_agentregistry_v1alpha_mcp_server` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	agentregistry "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/agentregistry"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_agentregistry_v1alpha_mcp_server"})

    		ubx.Data(agentregistry.V1alphaMcpServer, "example", agentregistry.V1alphaMcpServerConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1alphaMcpServer } from "@ubx/sdk-google/google/data/agentregistry/v1alpha_mcp_server";

    export default stack("example", () => {
      intent({ summary: "look up google_agentregistry_v1alpha_mcp_server" });

      data(V1alphaMcpServer, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.agentregistry import V1alphaMcpServer, V1alphaMcpServerConfig

    def describe():
        ubx.intent("look up google_agentregistry_v1alpha_mcp_server")

        ubx.data(V1alphaMcpServer, "example", V1alphaMcpServerConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attributes" type="map(map(any))">
  Output only. Attributes of the MCP Server. Valid values: * `agentregistry.googleapis.com/system/RuntimeIdentity`: &#123;"principal": "principal://..."&#125; - the runtime identity associated with the MCP Server. * `agentregistry.googleapis.com/system/RuntimeReference`: &#123;"uri": "//..."&#125; - the URI of the underlying resource hosting the MCP Server, for example, the GKE Deployment. Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Create time. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Output only. The description of the MCP Server. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  Output only. The display name of the MCP Server. Returned by the lookup.
</ResponseField>

<ResponseField name="interfaces" type="list(object)">
  Output only. The connection details for the MCP Server. Returned by the lookup.
</ResponseField>

<ResponseField name="mcp_server_id" type="string">
  Output only. A stable, globally unique identifier for MCP Servers. Returned by the lookup.
</ResponseField>

<ResponseField name="tools" type="list(object)">
  Output only. Tools provided by the MCP Server. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Update time. Returned by the lookup.
</ResponseField>
