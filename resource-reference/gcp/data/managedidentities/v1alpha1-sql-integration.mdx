---
title: "google_managedidentities_v1alpha1_sql_integration"
description: "`data_managedidentities_v1alpha1_sql_integration` looks up a real Managed Microsoft AD-to-Cloud SQL integration, via the v1alpha1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	managedidentities "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/managedidentities"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_managedidentities_v1alpha1_sql_integration"})

    		ubx.Data(managedidentities.V1alpha1SqlIntegration, "example", managedidentities.V1alpha1SqlIntegrationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1alpha1SqlIntegration } from "@ubx/sdk-google/google/data/managedidentities/v1alpha1_sql_integration";

    export default stack("example", () => {
      intent({
        summary: "look up google_managedidentities_v1alpha1_sql_integration",
      });

      data(V1alpha1SqlIntegration, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.managedidentities import V1alpha1SqlIntegration, V1alpha1SqlIntegrationConfig

    def describe():
        ubx.intent("look up google_managedidentities_v1alpha1_sql_integration")

        ubx.data(V1alpha1SqlIntegration, "example", V1alpha1SqlIntegrationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. The time the instance was created. Returned by the lookup.
</ResponseField>

<ResponseField name="sql_instance" type="string">
  The full resource name of an integrated sql instance Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the managed OU. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Last update time for this SQL instance. Returned by the lookup.
</ResponseField>
