---
title: "google_managedidentities_v1beta1_sql_integration"
description: "`google_managedidentities_v1beta1_sql_integration` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_managedidentities_v1beta1_sql_integration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	managedidentities "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/managedidentities"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_managedidentities_v1beta1_sql_integration"})

    		ubx.Data(managedidentities.V1beta1SqlIntegration, "example", managedidentities.V1beta1SqlIntegrationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1beta1SqlIntegration } from "@ubx/sdk-google/google/data/managedidentities/v1beta1_sql_integration";

    export default stack("example", () => {
      intent({
        summary: "look up google_managedidentities_v1beta1_sql_integration",
      });

      data(V1beta1SqlIntegration, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.managedidentities import V1beta1SqlIntegration, V1beta1SqlIntegrationConfig

    def describe():
        ubx.intent("look up google_managedidentities_v1beta1_sql_integration")

        ubx.data(V1beta1SqlIntegration, "example", V1beta1SqlIntegrationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. The time sql integration was created. Returned by the lookup.
</ResponseField>

<ResponseField name="sql_instance" type="string">
  The full resource name of an integrated sql instance Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the sql integration. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time sql integration was updated. Returned by the lookup.
</ResponseField>
