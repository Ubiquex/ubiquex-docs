---
title: "google_networkconnectivity_service_class"
description: "`data_networkconnectivity_service_class` looks up a real service class used to steer traffic through Network Connectivity Center."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	networkconnectivity "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/networkconnectivity"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_networkconnectivity_service_class"})

    		ubx.Data(networkconnectivity.ServiceClass, "example", networkconnectivity.ServiceClassConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceClass } from "@ubx/sdk-google/google/data/networkconnectivity/service_class";

    export default stack("example", () => {
      intent({ summary: "look up google_networkconnectivity_service_class" });

      data(ServiceClass, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.networkconnectivity import ServiceClass, ServiceClassConfig

    def describe():
        ubx.intent("look up google_networkconnectivity_service_class")

        ubx.data(ServiceClass, "example", ServiceClassConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. Time when the ServiceClass was created. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  A description of this resource. Returned by the lookup.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. The etag is computed by the server, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Returned by the lookup.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  User-defined labels. Returned by the lookup.
</ResponseField>

<ResponseField name="service_class" type="string">
  Output only. The generated service class name. Use this name to refer to the Service class in Service Connection Maps and Service Connection Policies. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time when the ServiceClass was updated. Returned by the lookup.
</ResponseField>
