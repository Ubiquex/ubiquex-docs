---
title: "google_cloudcontrolspartner_v1beta_workload"
description: "`google_cloudcontrolspartner_v1beta_workload` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_cloudcontrolspartner_v1beta_workload` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudcontrolspartner "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/cloudcontrolspartner"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_cloudcontrolspartner_v1beta_workload"})

    		ubx.Data(cloudcontrolspartner.V1betaWorkload, "example", cloudcontrolspartner.V1betaWorkloadConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1betaWorkload } from "@ubx/sdk-google/google/data/cloudcontrolspartner/v1beta_workload";

    export default stack("example", () => {
      intent({ summary: "look up google_cloudcontrolspartner_v1beta_workload" });

      data(V1betaWorkload, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.cloudcontrolspartner import V1betaWorkload, V1betaWorkloadConfig

    def describe():
        ubx.intent("look up google_cloudcontrolspartner_v1beta_workload")

        ubx.data(V1betaWorkload, "example", V1betaWorkloadConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. Time the resource was created. Returned by the lookup.
</ResponseField>

<ResponseField name="folder" type="string">
  Output only. The name of container folder of the assured workload Returned by the lookup.
</ResponseField>

<ResponseField name="folder_id" type="string">
  Output only. Folder id this workload is associated with Returned by the lookup.
</ResponseField>

<ResponseField name="is_onboarded" type="bool">
  Indicates whether a workload is fully onboarded. Returned by the lookup.
</ResponseField>

<ResponseField name="key_management_project_id" type="string">
  The project id of the key management project for the workload Returned by the lookup.
</ResponseField>

<ResponseField name="location" type="string">
  The Google Cloud location of the workload Returned by the lookup.
</ResponseField>

<ResponseField name="partner" type="string">
  Partner associated with this workload. Returned by the lookup.
</ResponseField>

<ResponseField name="workload_onboarding_state" type="object">
  Container for workload onboarding steps. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="onboarding_steps" type="list(object)">
      List of workload onboarding steps. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
