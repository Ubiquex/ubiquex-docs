---
title: "google_firebaseappcheck_v1beta_service"
description: "`data_firebaseappcheck_v1beta_service` looks up a real backend service's own Firebase App Check enforcement configuration, via the v1beta API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseappcheck "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/firebaseappcheck"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_firebaseappcheck_v1beta_service"})

    		ubx.Data(firebaseappcheck.V1betaService, "example", firebaseappcheck.V1betaServiceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1betaService } from "@ubx/sdk-google/google/data/firebaseappcheck/v1beta_service";

    export default stack("example", () => {
      intent({ summary: "look up google_firebaseappcheck_v1beta_service" });

      data(V1betaService, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.firebaseappcheck import V1betaService, V1betaServiceConfig

    def describe():
        ubx.intent("look up google_firebaseappcheck_v1beta_service")

        ubx.data(V1betaService, "example", V1betaServiceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="enforcement_mode" type="string">
  Required. The baseline protection EnforcementMode for this service. Returned by the lookup.
</ResponseField>

<ResponseField name="etag" type="string">
  This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. This etag is strongly validated as defined by RFC 7232. Returned by the lookup.
</ResponseField>

<ResponseField name="replay_protection" type="string">
  Optional. The replay protection EnforcementMode for this service. Note that this field cannot be set to a level higher than that of baseline protection. For example, if the enforcement mode for baseline protection is set to `UNENFORCED`, this field cannot be set to `ENFORCED`. In order to enforce replay protection, you must first enforce App Check's baseline protection. An HTTP 400 error will be returned in this case. By default, this field is set to `OFF`. Setting this field to `UNENFORCED` or `ENFORCED` is considered opting into replay protection. Opting in can impact your requests by adding some latency and sometimes cost (depending on your attestation provider). To opt out of replay protection after opting in, set this field to `OFF`. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when this service configuration object was most recently updated. Returned by the lookup.
</ResponseField>
