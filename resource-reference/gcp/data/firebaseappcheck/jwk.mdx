---
title: "google_firebaseappcheck_jwk"
description: "`data_firebaseappcheck_jwk` looks up a real, public JSON Web Key used to verify Firebase App Check tokens."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseappcheck "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/firebaseappcheck"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_firebaseappcheck_jwk"})

    		ubx.Data(firebaseappcheck.Jwk, "example", firebaseappcheck.JwkConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Jwk } from "@ubx/sdk-google/google/data/firebaseappcheck/jwk";

    export default stack("example", () => {
      intent({ summary: "look up google_firebaseappcheck_jwk" });

      data(Jwk, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.firebaseappcheck import Jwk, JwkConfig

    def describe():
        ubx.intent("look up google_firebaseappcheck_jwk")

        ubx.data(Jwk, "example", JwkConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="keys" type="list(object)">
  The set of public keys. See [section 5.1 of RFC 7517](https://tools.ietf.org/html/rfc7517#section-5). Returned by the lookup.
</ResponseField>
