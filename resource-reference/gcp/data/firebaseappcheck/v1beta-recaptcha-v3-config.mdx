---
title: "google_firebaseappcheck_v1beta_recaptcha_v3_config"
description: "`data_firebaseappcheck_v1beta_recaptcha_v3_config` looks up a real Firebase App Check reCAPTCHA v3 configuration, via the v1beta API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseappcheck "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/firebaseappcheck"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_firebaseappcheck_v1beta_recaptcha_v3_config"})

    		ubx.Data(firebaseappcheck.V1betaRecaptchaV3Config, "example", firebaseappcheck.V1betaRecaptchaV3ConfigConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { V1betaRecaptchaV3Config } from "./local-sdk/google/sdk/typescript/google/data/firebaseappcheck/v1beta_recaptcha_v3_config.ts";

    export default stack("example", () => {
      intent({
        summary: "look up google_firebaseappcheck_v1beta_recaptcha_v3_config",
      });

      data(V1betaRecaptchaV3Config, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.firebaseappcheck import V1betaRecaptchaV3Config, V1betaRecaptchaV3ConfigConfig

    def describe():
        ubx.intent("look up google_firebaseappcheck_v1beta_recaptcha_v3_config")

        ubx.data(V1betaRecaptchaV3Config, "example", V1betaRecaptchaV3ConfigConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="min_valid_score" type="number">
  Specifies a minimum score required for a reCAPTCHA token to be considered valid. If its score is greater than or equal to this value, it will be accepted; otherwise, it will be rejected. The value must be between 0.0 and 1.0. The default value is 0.5. Returned by the lookup.
</ResponseField>

<ResponseField name="site_secret" type="string">
  Required. Input only. The site secret used to identify your service for reCAPTCHA v3 verification. For security reasons, this field will never be populated in any response. Returned by the lookup.
</ResponseField>

<ResponseField name="site_secret_set" type="bool">
  Output only. Whether the `site_secret` field was previously set. Since we will never return the `site_secret` field, this field is the only way to find out whether it was previously set. Returned by the lookup.
</ResponseField>

<ResponseField name="token_ttl" type="string">
  Specifies the duration for which App Check tokens exchanged from reCAPTCHA tokens will be valid. If unset, a default value of 1 day is assumed. Must be between 30 minutes and 7 days, inclusive. Returned by the lookup.
</ResponseField>
