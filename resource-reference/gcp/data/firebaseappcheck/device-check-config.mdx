---
title: "google_firebaseappcheck_device_check_config"
description: "`data_firebaseappcheck_device_check_config` looks up a real Firebase App Check configuration using Apple's DeviceCheck provider for an iOS app."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseappcheck "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/firebaseappcheck"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_firebaseappcheck_device_check_config"})

    		ubx.Data(firebaseappcheck.DeviceCheckConfig, "example", firebaseappcheck.DeviceCheckConfigConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { DeviceCheckConfig } from "./local-sdk/google/sdk/typescript/google/data/firebaseappcheck/device_check_config.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_firebaseappcheck_device_check_config" });

      data(DeviceCheckConfig, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.firebaseappcheck import DeviceCheckConfig, DeviceCheckConfigConfig

    def describe():
        ubx.intent("look up google_firebaseappcheck_device_check_config")

        ubx.data(DeviceCheckConfig, "example", DeviceCheckConfigConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="key_id" type="string">
  Required. The key identifier of a private key enabled with DeviceCheck, created in your Apple Developer account. Returned by the lookup.
</ResponseField>

<ResponseField name="private_key" type="string">
  Required. Input only. The contents of the private key (`.p8`) file associated with the key specified by `key_id`. For security reasons, this field will never be populated in any response. Returned by the lookup.
</ResponseField>

<ResponseField name="private_key_set" type="bool">
  Output only. Whether the `private_key` field was previously set. Since we will never return the `private_key` field, this field is the only way to find out whether it was previously set. Returned by the lookup.
</ResponseField>

<ResponseField name="token_ttl" type="string">
  Specifies the duration for which App Check tokens exchanged from DeviceCheck tokens will be valid. If unset, a default value of 1 hour is assumed. Must be between 30 minutes and 7 days, inclusive. Returned by the lookup.
</ResponseField>
