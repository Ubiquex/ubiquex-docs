---
title: "google_firebaseappcheck_v1beta_jwk"
description: "`google_firebaseappcheck_v1beta_jwk` is a real, live lookup against Google Cloud, generated from google's own real schema -- read-only, never created or destroyed."
---

`google_firebaseappcheck_v1beta_jwk` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Google Cloud's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseappcheck "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/firebaseappcheck"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_firebaseappcheck_v1beta_jwk"})

    		ubx.Data(firebaseappcheck.V1betaJwk, "example", firebaseappcheck.V1betaJwkConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1betaJwk } from "@ubx/sdk-google/google/data/firebaseappcheck/v1beta_jwk";

    export default stack("example", () => {
      intent({ summary: "look up google_firebaseappcheck_v1beta_jwk" });

      data(V1betaJwk, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.firebaseappcheck import V1betaJwk, V1betaJwkConfig

    def describe():
        ubx.intent("look up google_firebaseappcheck_v1beta_jwk")

        ubx.data(V1betaJwk, "example", V1betaJwkConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="keys" type="list(object)">
  The set of public keys. See [section 5.1 of RFC 7517](https://tools.ietf.org/html/rfc7517#section-5). Returned by the lookup.
</ResponseField>
