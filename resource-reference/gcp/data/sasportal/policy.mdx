---
title: "google_sasportal_policy"
description: "`data_sasportal_policy` looks up a real IAM policy attached to a SAS Portal node or deployment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	sasportal "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/sasportal"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_sasportal_policy"})

    		ubx.Data(sasportal.Policy, "example", sasportal.PolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Policy } from "@ubx/sdk-google/google/data/sasportal/policy";

    export default stack("example", () => {
      intent({ summary: "look up google_sasportal_policy" });

      data(Policy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.sasportal import Policy, PolicyConfig

    def describe():
        ubx.intent("look up google_sasportal_policy")

        ubx.data(Policy, "example", PolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="assignments" type="list(object)">
  List of assignments Returned by the lookup.
</ResponseField>

<ResponseField name="etag" type="string">
  The etag is used for optimistic concurrency control as a way to help prevent simultaneous updates of a policy from overwriting each other. It is strongly suggested that systems make use of the etag in the read-modify-write cycle to perform policy updates in order to avoid race conditions: An etag is returned in the response to GetPolicy, and systems are expected to put that etag in the request to SetPolicy to ensure that their change will be applied to the same version of the policy. If no etag is provided in the call to GetPolicy, then the existing policy is overwritten blindly. Returned by the lookup.
</ResponseField>
