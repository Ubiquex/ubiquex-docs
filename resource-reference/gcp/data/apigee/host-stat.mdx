---
title: "google_apigee_host_stat"
description: "`data_apigee_host_stat` looks up real, host-level Apigee analytics statistics for an environment group's own hostname."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_apigee_host_stat"})

    		ubx.Data(apigee.HostStat, "example", apigee.HostStatConfig{
    			Name:             "example",
    			Accuracy:         "example",
    			EnvgroupHostname: "example",
    			Filter:           "example",
    			Limit:            "example",
    			Offset:           "example",
    			Realtime:         true,
    			Select:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { HostStat } from "./local-sdk/google/sdk/typescript/google/data/apigee/host_stat.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_apigee_host_stat" });

      data(HostStat, "example", {
        name: "example",
        accuracy: "example",
        envgroupHostname: "example",
        filter: "example",
        limit: "example",
        offset: "example",
        realtime: true,
        select: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.apigee import HostStat, HostStatConfig

    def describe():
        ubx.intent("look up google_apigee_host_stat")

        ubx.data(HostStat, "example", HostStatConfig(
            name="example",
            accuracy="example",
            envgroup_hostname="example",
            filter="example",
            limit="example",
            offset="example",
            realtime=True,
            select="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accuracy" type="string">
  The real, computed accuracy score for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="envgroup_hostname" type="string">
  The real hostname bound to an Apigee environment group. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="string">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="offset" type="string">
  The number of matching results to skip before returning the rest, for pagination. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="realtime" type="bool">
  Whether the response reflects real-time, rather than cached, data. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="select" type="string">
  The real fields selected for this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sortby" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="time_range" type="string">
  The real time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="time_unit" type="string">
  The real unit of time (such as day or hour) results are aggregated at. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="topk" type="string">
  The real, maximum number of top-ranked results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ts_ascending" type="bool">
  Whether results are ordered by timestamp in ascending order. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tzo" type="string">
  The real timezone offset applied to this request's own timestamps. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="environments" type="list(object)">
  List of query results on the environment level. Returned by the lookup.
</ResponseField>

<ResponseField name="hosts" type="list(object)">
  List of query results grouped by host. Returned by the lookup.
</ResponseField>

<ResponseField name="meta_data" type="object">
  Encapsulates additional information about query execution. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="errors" type="list(string)">
      List of error messages as strings. Optional lookup argument.
    </ResponseField>
    <ResponseField name="notices" type="list(string)">
      List of additional information such as data source, if result was truncated. For example: ``` "notices": [ "Source:Postgres", "PG Host:uappg0rw.e2e.apigeeks.net", "query served by:4b64601e-40de-4eb1-bfb9-eeee7ac929ed", "Table used: edge.api.uapgroup2.agg_api" ]``` Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
