---
title: "google_cloudnumberregistry_discovered_range"
description: "`data_cloudnumberregistry_discovered_range` looks up a real, automatically discovered phone number range surfaced by Cloud Number Registry."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudnumberregistry "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/cloudnumberregistry"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_cloudnumberregistry_discovered_range"})

    		ubx.Data(cloudnumberregistry.DiscoveredRange, "example", cloudnumberregistry.DiscoveredRangeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { DiscoveredRange } from "./local-sdk/google/sdk/typescript/google/data/cloudnumberregistry/discovered_range.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_cloudnumberregistry_discovered_range" });

      data(DiscoveredRange, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.cloudnumberregistry import DiscoveredRange, DiscoveredRangeConfig

    def describe():
        ubx.intent("look up google_cloudnumberregistry_discovered_range")

        ubx.data(DiscoveredRange, "example", DiscoveredRangeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attributes" type="list(object)">
  Optional. The attributes of the DiscoveredRange. Returned by the lookup.
</ResponseField>

<ResponseField name="child_cidr_overlap_allowed" type="bool">
  Output only. If true, allows child DiscoveredRanges of this DiscoveredRange to overlap with each other. Returned by the lookup.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time at which the DiscoveredRange was created. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Description of the DiscoveredRange. Returned by the lookup.
</ResponseField>

<ResponseField name="discovery_metadata" type="object">
  Metadata about a discovered resource, tracking event times, state, and source information. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="create_time" type="string">
      Output only. The time when the resource was created. Returned by the lookup.
    </ResponseField>
    <ResponseField name="event_time" type="string">
      Output only. The time when the event happened. Returned by the lookup.
    </ResponseField>
    <ResponseField name="resource" type="string">
      Output only. The resource name of the discovered resource, should be API-agnostic. Example: "projects/&#123;project_number&#125;/networks/&#123;network_id&#125;". Returned by the lookup.
    </ResponseField>
    <ResponseField name="resource_uri" type="string">
      Output only. The resource uri of the discovered resource. Returned by the lookup.
    </ResponseField>
    <ResponseField name="source_id" type="string">
      Output only. The canonical google.aip.dev/122 name of the source resource. Returned by the lookup.
    </ResponseField>
    <ResponseField name="source_sub_id" type="string">
      Output only. A single source resource can be the source of multiple CNR resources. This sub_id is used to distinguish between the different CNR resources derived from the same upstream resource. For example, a single subnetwork can be the source of multiple Ranges, one for each protocol. In this case, the sub_id could be "private-ipv4" or "private-ipv6". Returned by the lookup.
    </ResponseField>
    <ResponseField name="state" type="string">
      Output only. The state of the resource. Returned by the lookup.
    </ResponseField>
    <ResponseField name="update_time" type="string">
      Output only. The time when the resource was last modified. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ipv4_cidr_range" type="string">
  Optional. The IPv4 CIDR range of the DiscoveredRange. Returned by the lookup.
</ResponseField>

<ResponseField name="ipv6_cidr_range" type="string">
  Optional. The IPv6 CIDR range of the DiscoveredRange. Returned by the lookup.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. User-defined labels. Returned by the lookup.
</ResponseField>

<ResponseField name="parent_range" type="string">
  Optional. The resource name of the parent DiscoveredRange, in the format `projects/&#123;project&#125;/locations/&#123;location&#125;/discoveredRanges/&#123;discovered_range&#125;`. Returned by the lookup.
</ResponseField>

<ResponseField name="realm" type="string">
  Optional. The Realm of the DiscoveredRange. Returned by the lookup.
</ResponseField>

<ResponseField name="registry_book" type="string">
  Output only. The RegistryBook of the DiscoveredRange. Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which the DiscoveredRange was last updated. Returned by the lookup.
</ResponseField>
