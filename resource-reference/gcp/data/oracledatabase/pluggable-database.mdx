---
title: "google_oracledatabase_pluggable_database"
description: "`data_oracledatabase_pluggable_database` looks up a real pluggable database (PDB) running inside an Oracle Database@Google Cloud Autonomous Database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	oracledatabase "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/oracledatabase"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_oracledatabase_pluggable_database"})

    		ubx.Data(oracledatabase.PluggableDatabase, "example", oracledatabase.PluggableDatabaseConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only google --lang ts --out ./local-sdk
    import { PluggableDatabase } from "./local-sdk/google/sdk/typescript/google/data/oracledatabase/pluggable_database.ts";

    export default stack("example", () => {
      intent({ summary: "look up google_oracledatabase_pluggable_database" });

      data(PluggableDatabase, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google.data.oracledatabase import PluggableDatabase, PluggableDatabaseConfig

    def describe():
        ubx.intent("look up google_oracledatabase_pluggable_database")

        ubx.data(PluggableDatabase, "example", PluggableDatabaseConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  Output only. The date and time that the PluggableDatabase was created. Returned by the lookup.
</ResponseField>

<ResponseField name="oci_url" type="string">
  Output only. HTTPS link to OCI resources exposed to Customer via UI Interface. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of a PluggableDatabase. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="compartment_id" type="string">
      Required. The OCID of the compartment. Optional lookup argument.
    </ResponseField>
    <ResponseField name="connection_strings" type="object">
      The connection strings used to connect to the Oracle Database. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="all_connection_strings" type="map(string)">
          Optional. All connection strings to use to connect to the pluggable database. Optional lookup argument.
        </ResponseField>
        <ResponseField name="pdb_default" type="string">
          Optional. The default connection string to use to connect to the pluggable database. Optional lookup argument.
        </ResponseField>
        <ResponseField name="pdb_ip_default" type="string">
          Optional. The default connection string to use to connect to the pluggable database using IP. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="container_database_ocid" type="string">
      Required. The OCID of the CDB. Optional lookup argument.
    </ResponseField>
    <ResponseField name="database_management_config" type="object">
      The configuration of the Database Management service. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="management_state" type="string">
          Output only. The status of the Database Management service. Returned by the lookup.
        </ResponseField>
        <ResponseField name="management_type" type="string">
          Output only. The Database Management type. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="defined_tags" type="map(object)">
      Optional. Defined tags for this resource. Each key is predefined and scoped to a namespace. Optional lookup argument.
    </ResponseField>
    <ResponseField name="freeform_tags" type="map(string)">
      Optional. Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace. Optional lookup argument.
    </ResponseField>
    <ResponseField name="is_restricted" type="bool">
      Optional. The restricted mode of the pluggable database. If a pluggable database is opened in restricted mode, the user needs both create a session and have restricted session privileges to connect to it. Optional lookup argument.
    </ResponseField>
    <ResponseField name="lifecycle_details" type="string">
      Output only. Additional information about the current lifecycle state. Returned by the lookup.
    </ResponseField>
    <ResponseField name="lifecycle_state" type="string">
      Output only. The current state of the pluggable database. Returned by the lookup.
    </ResponseField>
    <ResponseField name="ocid" type="string">
      Output only. The OCID of the pluggable database. Returned by the lookup.
    </ResponseField>
    <ResponseField name="operations_insights_state" type="string">
      Output only. The status of Operations Insights for this Database. Returned by the lookup.
    </ResponseField>
    <ResponseField name="pdb_name" type="string">
      Required. The database name. Optional lookup argument.
    </ResponseField>
    <ResponseField name="pdb_node_level_details" type="list(object)">
      Optional. Pluggable Database Node Level Details Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
