---
title: "google_cloudresourcemanager_v1beta1_organization"
description: "`data_cloudresourcemanager_v1beta1_organization` looks up a real Google Cloud organization, via the v1beta1 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudresourcemanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data/cloudresourcemanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up google_cloudresourcemanager_v1beta1_organization"})

    		ubx.Data(cloudresourcemanager.V1beta1Organization, "example", cloudresourcemanager.V1beta1OrganizationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V1beta1Organization } from "@ubx/sdk-google/google/data/cloudresourcemanager/v1beta1_organization";

    export default stack("example", () => {
      intent({
        summary: "look up google_cloudresourcemanager_v1beta1_organization",
      });

      data(V1beta1Organization, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data.cloudresourcemanager import V1beta1Organization, V1beta1OrganizationConfig

    def describe():
        ubx.intent("look up google_cloudresourcemanager_v1beta1_organization")

        ubx.data(V1beta1Organization, "example", V1beta1OrganizationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, fully-qualified resource name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_time" type="string">
  Timestamp when the Organization was created. Assigned by the server. Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  A human-readable string that refers to the Organization in the Google Cloud console. This string is set by the server and cannot be changed. The string will be set to the primary domain (for example, "google.com") of the G Suite customer that owns the organization. Returned by the lookup.
</ResponseField>

<ResponseField name="lifecycle_state" type="string">
  The organization's current lifecycle state. Assigned by the server. Returned by the lookup.
</ResponseField>

<ResponseField name="organization_id" type="string">
  The real, unique identifier of the organization this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="owner" type="object">
  The entity that owns an Organization. The lifetime of the Organization and all of its descendants are bound to the `OrganizationOwner`. If the `OrganizationOwner` is deleted, the Organization and all its descendants will be deleted. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="directory_customer_id" type="string">
      The G Suite customer id used in the Directory API. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
