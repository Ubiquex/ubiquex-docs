---
title: "google_cloudchannel_channel_partner_link"
description: "`google_cloudchannel_channel_partner_link` represents a real link in the Cloud Channel API between a distributor and one of its indirect resellers..."
---

`google_cloudchannel_channel_partner_link` represents a real link in the Cloud Channel API between a distributor and one of its indirect resellers, the relationship a distributor uses to manage its reseller channel programmatically.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_cloudchannel --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_cloudchannel/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloudchannel "github.com/ubiquex/ubx-sdk-google/sdk/go/google_cloudchannel/cloudchannel"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own channel partner link"})

    		ubx.Resource(cloudchannel.CloudchannelChannelPartnerLink, "example", cloudchannel.CloudchannelChannelPartnerLinkConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_cloudchannel --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CloudchannelChannelPartnerLink } from "./local-sdk/google_cloudchannel/sdk/typescript/google_cloudchannel/cloudchannel/cloudchannel_channel_partner_link.ts";

    export default stack("example", () => {
      intent({ summary: "example own channel partner link" });

      resource(CloudchannelChannelPartnerLink, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_cloudchannel --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_cloudchannel/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_cloudchannel.cloudchannel import CloudchannelChannelPartnerLink, CloudchannelChannelPartnerLinkConfig

    def describe():
        ubx.intent("example own channel partner link")

        ubx.resource(CloudchannelChannelPartnerLink, "example", CloudchannelChannelPartnerLinkConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloudchannel_channel_partner_link called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="channel_partner_cloud_identity_info" type="object">
  Cloud Identity information for the Cloud Channel Customer. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="admin_console_uri" type="string">
      Output only. URI of Customer's Admin console dashboard. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="alternate_email" type="string">
      The alternate email. Optional.
    </ResponseField>
    <ResponseField name="customer_type" type="string">
      CustomerType indicates verification type needed for using services. Optional.
    </ResponseField>
    <ResponseField name="edu_data" type="object">
      Required Edu Attributes Optional.
      <Expandable title="properties">
        <ResponseField name="institute_size" type="string">
          Size of the institute. Optional.
        </ResponseField>
        <ResponseField name="institute_type" type="string">
          Designated institute type of customer. Optional.
        </ResponseField>
        <ResponseField name="website" type="string">
          Web address for the edu customer's institution. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_domain_verified" type="bool">
      Output only. Whether the domain is verified. This field is not returned for a Customer's cloud_identity_info resource. Partners can use the domains.get() method of the Workspace SDK's Directory API, or listen to the PRIMARY_DOMAIN_VERIFIED Pub/Sub event in to track domain verification of their resolve Workspace customers. Computed by Google Cloud after `ship`.
    </ResponseField>
    <ResponseField name="language_code" type="string">
      The language code for the cloudchannel channel partner link's channel partner cloud identity info. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="phone_number" type="string">
      Phone number associated with the Cloud Identity. Optional.
    </ResponseField>
    <ResponseField name="primary_domain" type="string">
      Output only. The primary domain name. Computed by Google Cloud after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. Timestamp of when the channel partner link is created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="invite_link_uri" type="string">
  Output only. URI of the web page where partner accepts the link invitation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="link_state" type="string">
  Required. State of the channel partner link. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Resource name for the channel partner link, in the format accounts/&#123;account_id&#125;/channelPartnerLinks/&#123;id&#125;. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="public_id" type="string">
  Output only. Public identifier that a customer must use to generate a transfer token to move to this distributor-reseller combination. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="reseller_cloud_identity_id" type="string">
  Required. Cloud Identity ID of the linked reseller. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp of when the channel partner link is updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
