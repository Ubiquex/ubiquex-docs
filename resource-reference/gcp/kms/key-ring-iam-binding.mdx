---
title: "google_kms_key_ring_iam_binding"
description: "Real, generated bindings for `google_kms_key_ring_iam_binding`."
---

`google_kms_key_ring_iam_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own key ring iam binding"})

    		ubx.Resource(kms.KeyRingIamBinding, "ci-runner", kms.KeyRingIamBindingConfig{
    			KeyRingId: "example",
    			Members:   []string{"example"},
    			Role:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeyRingIamBinding } from "jsr:@ubx/sdk-google/google/kms/key_ring_iam_binding";

    export default stack("payments", () => {
      intent({ summary: "payments own key ring iam binding" });

      resource(KeyRingIamBinding, "ci-runner", {
        keyRingId: "example",
        members: ["example"],
        role: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.kms import KeyRingIamBinding, KeyRingIamBindingConfig

    def describe():
        ubx.intent("payments own key ring iam binding")

        ubx.resource(KeyRingIamBinding, "ci-runner", KeyRingIamBindingConfig(
            key_ring_id="example",
            members=["example"],
            role="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_kms_key_ring_iam_binding, with key ring id example,
    members a set value, role example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_ring_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="members" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
