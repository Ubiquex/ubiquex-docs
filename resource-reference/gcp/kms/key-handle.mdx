---
title: "google_kms_key_handle"
description: "Real, generated bindings for `google_kms_key_handle`."
---

`google_kms_key_handle` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	kms "github.com/ubiquex/ubx-sdk-google/sdk/go/google/kms"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own key handle"})

    		ubx.Resource(kms.KeyHandle, "ci-runner", kms.KeyHandleConfig{
    			Location:             "example",
    			Name:                 "ci-runner",
    			ResourceTypeSelector: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeyHandle } from "jsr:@ubx/sdk-google/google/kms/key_handle";

    export default stack("payments", () => {
      intent({ summary: "payments own key handle" });

      resource(KeyHandle, "ci-runner", {
        location: "example",
        name: "ci-runner",
        resourceTypeSelector: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.kms import KeyHandle, KeyHandleConfig

    def describe():
        ubx.intent("payments own key handle")

        ubx.resource(KeyHandle, "ci-runner", KeyHandleConfig(
            location="example",
            name="ci-runner",
            resource_type_selector="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_kms_key_handle called "ci-runner", with location
    example, resource type selector example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the KeyHandle. A full list of valid locations can be found by running 'gcloud kms locations list'. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The resource name for the KeyHandle. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_type_selector" type="string" required>
  Selector of the resource type where we want to protect resources. For example, 'storage.googleapis.com/Bucket'. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_key" type="string">
  A reference to a Cloud KMS CryptoKey that can be used for CMEK in the requested product/project/location, for example 'projects/1/locations/us-east1/keyRings/foo/cryptoKeys/bar-ffffff' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
