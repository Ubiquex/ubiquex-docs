---
title: "google_kms_ekm_connection"
description: "Real, generated bindings for `google_kms_ekm_connection`."
---

`google_kms_ekm_connection` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	kms "github.com/ubiquex/ubx-sdk-google/sdk/go/google/kms"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ekm connection"})

    		ubx.Resource(kms.EkmConnection, "ci-runner", kms.EkmConnectionConfig{
    			Location:          "example",
    			Name:              "ci-runner",
    			KeyManagementMode: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EkmConnection } from "jsr:@ubx/sdk-google/google/kms/ekm_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own ekm connection" });

      resource(EkmConnection, "ci-runner", {
        location: "example",
        name: "ci-runner",
        keyManagementMode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.kms import EkmConnection, EkmConnectionConfig

    def describe():
        ubx.intent("payments own ekm connection")

        ubx.resource(EkmConnection, "ci-runner", EkmConnectionConfig(
            location="example",
            name="ci-runner",
            key_management_mode="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_kms_ekm_connection called "ci-runner", with
    location example, key management mode example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="crypto_space_path" type="string">
  Optional. Identifies the EKM Crypto Space that this EkmConnection maps to. Note: This field is required if KeyManagementMode is CLOUD_KMS. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. Etag of the currently stored EkmConnection. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_management_mode" type="string">
  Optional. Describes who can perform control plane operations on the EKM. If unset, this defaults to MANUAL Default value: "MANUAL" Possible values: ["MANUAL", "CLOUD_KMS"] Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the EkmConnection. A full list of valid locations can be found by running 'gcloud kms locations list'. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The resource name for the EkmConnection. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_resolvers" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="endpoint_filter" type="string">
    </ResponseField>
    <ResponseField name="hostname" type="string" required>
    </ResponseField>
    <ResponseField name="server_certificates" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="issuer" type="string">
        </ResponseField>
        <ResponseField name="not_after_time" type="string">
        </ResponseField>
        <ResponseField name="not_before_time" type="string">
        </ResponseField>
        <ResponseField name="parsed" type="bool">
        </ResponseField>
        <ResponseField name="raw_der" type="string" required>
        </ResponseField>
        <ResponseField name="serial_number" type="string">
        </ResponseField>
        <ResponseField name="sha256_fingerprint" type="string">
        </ResponseField>
        <ResponseField name="subject" type="string">
        </ResponseField>
        <ResponseField name="subject_alternative_dns_names" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_directory_service" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Output only. The time at which the EkmConnection was created. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="crypto_space_path" type="string">
  Optional. Identifies the EKM Crypto Space that this EkmConnection maps to. Note: This field is required if KeyManagementMode is CLOUD_KMS. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. Etag of the currently stored EkmConnection. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_resolvers" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="endpoint_filter" type="string">
    </ResponseField>
    <ResponseField name="hostname" type="string" required>
    </ResponseField>
    <ResponseField name="server_certificates" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="issuer" type="string">
        </ResponseField>
        <ResponseField name="not_after_time" type="string">
        </ResponseField>
        <ResponseField name="not_before_time" type="string">
        </ResponseField>
        <ResponseField name="parsed" type="bool">
        </ResponseField>
        <ResponseField name="raw_der" type="string" required>
        </ResponseField>
        <ResponseField name="serial_number" type="string">
        </ResponseField>
        <ResponseField name="sha256_fingerprint" type="string">
        </ResponseField>
        <ResponseField name="subject" type="string">
        </ResponseField>
        <ResponseField name="subject_alternative_dns_names" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_directory_service" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
