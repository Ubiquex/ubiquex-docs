---
title: "google_kms_key_ring_iam_policy"
description: "Real, generated bindings for `google_kms_key_ring_iam_policy`."
---

`google_kms_key_ring_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own key ring iam policy"})

    		ubx.Resource(kms.KeyRingIamPolicy, "ci-runner", kms.KeyRingIamPolicyConfig{
    			KeyRingId:  "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeyRingIamPolicy } from "jsr:@ubx/sdk-google/google/kms/key_ring_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own key ring iam policy" });

      resource(KeyRingIamPolicy, "ci-runner", {
        keyRingId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.kms import KeyRingIamPolicy, KeyRingIamPolicyConfig

    def describe():
        ubx.intent("payments own key ring iam policy")

        ubx.resource(KeyRingIamPolicy, "ci-runner", KeyRingIamPolicyConfig(
            key_ring_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_kms_key_ring_iam_policy, with key ring id example,
    policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_ring_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
