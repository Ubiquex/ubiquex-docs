---
title: "google_kms_key_ring_iam_policy"
description: "Real, generated bindings for `google_kms_key_ring_iam_policy`."
---

`google_kms_key_ring_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/kms"
    )

    cfg := kms.KeyRingIamPolicyConfig{
        KeyRingId: "example",
        PolicyData: "example",
    }
    ubx.Resource(kms.KeyRingIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { KeyRingIamPolicy } from
      "jsr:@ubx/sdk-google/google/kms/key_ring_iam_policy";

    resource(KeyRingIamPolicy, "example", {
      keyRingId: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.kms import (
        KeyRingIamPolicy,
        KeyRingIamPolicyConfig,
    )

    cfg = KeyRingIamPolicyConfig(
        key_ring_id="example",
        policy_data="example",
    )
    ubx.resource(KeyRingIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_kms_key_ring_iam_policy with key_ring_id,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_ring_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
