---
title: "google_kms_ekm_connection_iam_policy"
description: "Real, generated bindings for `google_kms_ekm_connection_iam_policy`."
---

`google_kms_ekm_connection_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ekm connection iam policy"})

    		ubx.Resource(kms.EkmConnectionIamPolicy, "ci-runner", kms.EkmConnectionIamPolicyConfig{
    			Name:       "ci-runner",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EkmConnectionIamPolicy } from "jsr:@ubx/sdk-google/google/kms/ekm_connection_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own ekm connection iam policy" });

      resource(EkmConnectionIamPolicy, "ci-runner", {
        name: "ci-runner",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.kms import EkmConnectionIamPolicy, EkmConnectionIamPolicyConfig

    def describe():
        ubx.intent("payments own ekm connection iam policy")

        ubx.resource(EkmConnectionIamPolicy, "ci-runner", EkmConnectionIamPolicyConfig(
            name="ci-runner",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_kms_ekm_connection_iam_policy called "ci-runner",
    with policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
