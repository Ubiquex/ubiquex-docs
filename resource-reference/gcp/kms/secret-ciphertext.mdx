---
title: "google_kms_secret_ciphertext"
description: "Real, generated bindings for `google_kms_secret_ciphertext`."
---

`google_kms_secret_ciphertext` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own secret ciphertext"})

    		ubx.Resource(kms.SecretCiphertext, "ci-runner", kms.SecretCiphertextConfig{
    			CryptoKey:                   "example",
    			Plaintext:                   "example",
    			AdditionalAuthenticatedData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecretCiphertext } from "jsr:@ubx/sdk-google/google/kms/secret_ciphertext";

    export default stack("payments", () => {
      intent({ summary: "payments own secret ciphertext" });

      resource(SecretCiphertext, "ci-runner", {
        cryptoKey: "example",
        plaintext: "example",
        additionalAuthenticatedData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.kms import SecretCiphertext, SecretCiphertextConfig

    def describe():
        ubx.intent("payments own secret ciphertext")

        ubx.resource(SecretCiphertext, "ci-runner", SecretCiphertextConfig(
            crypto_key="example",
            plaintext="example",
            additional_authenticated_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_kms_secret_ciphertext, with crypto key example,
    plaintext example, additional authenticated data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="additional_authenticated_data" type="string">
  Optional.
</ResponseField>

<ResponseField name="crypto_key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="plaintext" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="ciphertext" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
