---
title: "google_kms_crypto_key_iam_policy"
description: "Real, generated bindings for `google_kms_crypto_key_iam_policy`."
---

`google_kms_crypto_key_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/kms"
    )

    cfg := kms.CryptoKeyIamPolicyConfig{
        CryptoKeyId: "example",
        PolicyData: "example",
    }
    ubx.Resource(kms.CryptoKeyIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CryptoKeyIamPolicy } from
      "jsr:@ubx/sdk-google/google/kms/crypto_key_iam_policy";

    resource(CryptoKeyIamPolicy, "example", {
      cryptoKeyId: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.kms import (
        CryptoKeyIamPolicy,
        CryptoKeyIamPolicyConfig,
    )

    cfg = CryptoKeyIamPolicyConfig(
        crypto_key_id="example",
        policy_data="example",
    )
    ubx.resource(CryptoKeyIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_kms_crypto_key_iam_policy with crypto_key_id,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="crypto_key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
