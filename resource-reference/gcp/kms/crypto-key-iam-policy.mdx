---
title: "google_kms_crypto_key_iam_policy"
description: "Real, generated bindings for `google_kms_crypto_key_iam_policy`."
---

`google_kms_crypto_key_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own crypto key iam policy"})

    		ubx.Resource(kms.CryptoKeyIamPolicy, "ci-runner", kms.CryptoKeyIamPolicyConfig{
    			CryptoKeyId: "example",
    			PolicyData:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CryptoKeyIamPolicy } from "jsr:@ubx/sdk-google/google/kms/crypto_key_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own crypto key iam policy" });

      resource(CryptoKeyIamPolicy, "ci-runner", {
        cryptoKeyId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.kms import CryptoKeyIamPolicy, CryptoKeyIamPolicyConfig

    def describe():
        ubx.intent("payments own crypto key iam policy")

        ubx.resource(CryptoKeyIamPolicy, "ci-runner", CryptoKeyIamPolicyConfig(
            crypto_key_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_kms_crypto_key_iam_policy, with crypto key id
    example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="crypto_key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
