---
title: "google_contactcenterinsights_authorized_view"
description: "`google_contactcenterinsights_authorized_view` restricts a principal's own access within Contact Center AI Insights to a defined subset of conversation..."
---

`google_contactcenterinsights_authorized_view` restricts a principal's own access within Contact Center AI Insights to a defined subset of conversation data, for role-based data isolation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_contactcenterinsights --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_contactcenterinsights/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	contactcenterinsights "github.com/ubiquex/ubx-sdk-google/sdk/go/google_contactcenterinsights/contactcenterinsights"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own authorized view"})

    		ubx.Resource(contactcenterinsights.ContactcenterinsightsAuthorizedView, "ci-runner", contactcenterinsights.ContactcenterinsightsAuthorizedViewConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_contactcenterinsights --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContactcenterinsightsAuthorizedView } from "./local-sdk/google_contactcenterinsights/sdk/typescript/google_contactcenterinsights/contactcenterinsights/contactcenterinsights_authorized_view.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own authorized view" });

      resource(ContactcenterinsightsAuthorizedView, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_contactcenterinsights --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_contactcenterinsights/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_contactcenterinsights.contactcenterinsights import ContactcenterinsightsAuthorizedView, ContactcenterinsightsAuthorizedViewConfig

    def describe():
        ubx.intent("payments own authorized view")

        ubx.resource(ContactcenterinsightsAuthorizedView, "ci-runner", ContactcenterinsightsAuthorizedViewConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_contactcenterinsights_authorized_view called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="conversation_filter" type="string">
  A filter to reduce conversation results to a specific subset. The AuthorizedView's assigned permission (read/write) could be applied to the subset of conversations. If conversation_filter is empty, there is no restriction on the conversations that the AuthorizedView can access. Having *authorizedViews.get* access to the AuthorizedView means having the same read/write access to the Conversations (as well as metadata/annotations linked to the conversation) that this AuthorizedView has. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time at which the authorized view was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Display Name. Limit 64 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the AuthorizedView. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/authorizedViewSets/&#123;authorized_view_set&#125;/authorizedViews/&#123;authorized_view&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The most recent time at which the authorized view was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
