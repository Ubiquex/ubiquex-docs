---
title: "google_contactcenterinsights_authorized_view_set"
description: "`google_contactcenterinsights_authorized_view_set` groups related authorized views together for easier bulk assignment of the same data-access scope to..."
---

`google_contactcenterinsights_authorized_view_set` groups related authorized views together for easier bulk assignment of the same data-access scope to multiple principals.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	contactcenterinsights "github.com/ubiquex/ubx-sdk-google/sdk/go/google/contactcenterinsights"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own authorized view set"})

    		ubx.Resource(contactcenterinsights.AuthorizedViewSet, "example", contactcenterinsights.AuthorizedViewSetConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AuthorizedViewSet } from "@ubx/sdk-google/google/contactcenterinsights/authorized_view_set";

    export default stack("example", () => {
      intent({ summary: "example own authorized view set" });

      resource(AuthorizedViewSet, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.contactcenterinsights.authorized_view_set import AuthorizedViewSet, AuthorizedViewSetConfig

    def describe():
        ubx.intent("example own authorized view set")

        ubx.resource(AuthorizedViewSet, "example", AuthorizedViewSetConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_contactcenterinsights_authorized_view_set called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Create time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Display Name. Limit 64 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the AuthorizedViewSet. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/authorizedViewSets/&#123;authorized_view_set&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Update time. Optional; if omitted, computed by Google Cloud.
</ResponseField>
