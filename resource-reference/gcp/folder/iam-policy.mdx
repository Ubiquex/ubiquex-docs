---
title: "google_folder_iam_policy"
description: "Real, generated bindings for `google_folder_iam_policy`."
---

`google_folder_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/folder"
    )

    cfg := folder.IamPolicyConfig{
        Folder: "example",
        PolicyData: "example",
    }
    ubx.Resource(folder.IamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IamPolicy } from
      "jsr:@ubx/sdk-google/google/folder/iam_policy";

    resource(IamPolicy, "example", {
      folder: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.folder import IamPolicy, IamPolicyConfig

    cfg = IamPolicyConfig(
        folder="example",
        policy_data="example",
    )
    ubx.resource(IamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_folder_iam_policy with folder, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="folder" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
