---
title: "google_accesscontextmanager_access_policy"
description: "`google_accesscontextmanager_access_policy` is the top-level container for Access Context Manager..."
---

`google_accesscontextmanager_access_policy` is the top-level container for Access Context Manager, holding every access level and service perimeter defined for an organization.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_accesscontextmanager --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_accesscontextmanager/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	accesscontextmanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google_accesscontextmanager/accesscontextmanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own access policy"})

    		ubx.Resource(accesscontextmanager.AccesscontextmanagerAccessPolicy, "ci-runner", accesscontextmanager.AccesscontextmanagerAccessPolicyConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_accesscontextmanager --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccesscontextmanagerAccessPolicy } from "./local-sdk/google_accesscontextmanager/sdk/typescript/google_accesscontextmanager/accesscontextmanager/accesscontextmanager_access_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own access policy" });

      resource(AccesscontextmanagerAccessPolicy, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_accesscontextmanager --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_accesscontextmanager/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_accesscontextmanager.accesscontextmanager import AccesscontextmanagerAccessPolicy, AccesscontextmanagerAccessPolicyConfig

    def describe():
        ubx.intent("payments own access policy")

        ubx.resource(AccesscontextmanagerAccessPolicy, "ci-runner", AccesscontextmanagerAccessPolicyConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_accesscontextmanager_access_policy called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Output only. An opaque identifier for the current version of the `AccessPolicy`. This will always be a strongly validated etag, meaning that two Access Policies will be identical if and only if their etags are identical. Clients should not expect this to be in any specific format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. Resource name of the `AccessPolicy`. Format: `accessPolicies/&#123;access_policy&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="parent" type="string">
  Required. The parent of this `AccessPolicy` in the Cloud Resource Hierarchy. Currently immutable once created. Format: `organizations/&#123;organization_id&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="scopes" type="list(string)">
  The scopes of the AccessPolicy. Scopes define which resources a policy can restrict and where its resources can be referenced. For example, policy A with `scopes=["folders/123"]` has the following behavior: - ServicePerimeter can only restrict projects within `folders/123`. - ServicePerimeter within policy A can only reference access levels defined within policy A. - Only one policy can include a given scope; thus, attempting to create a second policy which includes `folders/123` will result in an error. If no scopes are provided, then any resource within the organization can be restricted. Scopes cannot be modified after a policy is created. Policies can only have a single scope. Format: list of `folders/&#123;folder_number&#125;` or `projects/&#123;project_number&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="title" type="string">
  Required. Human readable title. Does not affect behavior. Optional; if omitted, computed by Google Cloud.
</ResponseField>
