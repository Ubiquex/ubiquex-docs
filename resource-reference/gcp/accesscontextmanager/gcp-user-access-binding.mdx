---
title: "google_accesscontextmanager_gcp_user_access_binding"
description: "`google_accesscontextmanager_gcp_user_access_binding` restricts which Access Context Manager access level(s) a specific group of Google Workspace users..."
---

`google_accesscontextmanager_gcp_user_access_binding` restricts which Access Context Manager access level(s) a specific group of Google Workspace users must satisfy to reach Google Cloud resources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	accesscontextmanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google/accesscontextmanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own gcp user access binding"})

    		ubx.Resource(accesscontextmanager.GcpUserAccessBinding, "example", accesscontextmanager.GcpUserAccessBindingConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GcpUserAccessBinding } from "@ubx/sdk-google/google/accesscontextmanager/gcp_user_access_binding";

    export default stack("example", () => {
      intent({ summary: "example own gcp user access binding" });

      resource(GcpUserAccessBinding, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.accesscontextmanager.gcp_user_access_binding import GcpUserAccessBinding, GcpUserAccessBindingConfig

    def describe():
        ubx.intent("example own gcp user access binding")

        ubx.resource(GcpUserAccessBinding, "example", GcpUserAccessBindingConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_accesscontextmanager_gcp_user_access_binding called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="access_levels" type="list(string)">
  Optional. Access level that a user must have to be granted access. Only one access level is supported, not multiple. This repeated field must have exactly one element. Example: "accessPolicies/9522/accessLevels/device_trusted" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="dry_run_access_levels" type="list(string)">
  Optional. Dry run access level that will be evaluated but will not be enforced. The access denial based on dry run policy will be logged. Only one access level is supported, not multiple. This list must have exactly one element. Example: "accessPolicies/9522/accessLevels/device_trusted" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="group_key" type="string">
  Optional. Immutable. Google Group id whose users are subject to this binding's restrictions. See "id" in the [Google Workspace Directory API's Group Resource] (https://developers.google.com/admin-sdk/directory/v1/reference/groups#resource). If a group's email address/alias is changed, this resource will continue to point at the changed group. This field does not accept group email addresses or aliases. Example: "01d520gv4vjcrht" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Immutable. Assigned by the server during creation. The last segment has an arbitrary length and has only URI unreserved characters (as defined by [RFC 3986 Section 2.3](https://tools.ietf.org/html/rfc3986#section-2.3)). Should not be specified by the client during creation. Example: "organizations/256/gcpUserAccessBindings/b3-BhcX_Ud5N" Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="principal" type="object">
  The comprehensive identity container supporting identities including groups, service accounts, and federated identities. Only one of them can be set to create an access binding. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="federated_principal" type="string">
      Immutable. IAM federated principal name to assign policies to workforce/workload federated identities. Can be principal set or single principal, here are some examples: Single principal: principal://iam.googleapis.com/projects/&#123;project_number&#125;/locations/global/workloadIdentityPools/&#123;pool_id&#125;/subject/&#123;subject_attribute_value&#125; PrincipalSet: principalSet://iam.googleapis.com/projects/&#123;project_number&#125;/locations/global/workloadIdentityPools/&#123;pool_id&#125;/* Optional.
    </ResponseField>
    <ResponseField name="service_account" type="string">
      Immutable. Service account email used to assign policies to a specific service account. If a service account is subject to multiple policies (e.g., if there is a policy for all service accounts in a project and a policy for the service account), the closest (i.e. the most specific) dry-run policy will be used for the dry-run functionality and the closest enforcement policy will be used for the enforcement. Optional.
    </ResponseField>
    <ResponseField name="service_account_project_number" type="string">
      Immutable. Cloud project number used to assign policies to all service accounts owned by the project. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="restricted_client_applications" type="list(object)">
  Optional. Deprecated: Use `scoped_access_settings` instead. A list of applications that are subject to this binding's restrictions. If the list is empty, the binding restrictions will universally apply to all applications. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="client_id" type="string">
      The OAuth client ID this restriction applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scoped_access_settings" type="list(object)">
  Optional. A list of scoped access settings that set this binding's restrictions on a subset of applications. This field cannot be set if restricted_client_applications is set. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="active_settings" type="object">
      The settings currently in effect for this session. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="access_levels" type="list(string)">
          The access level(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="session_settings" type="object">
          Configuration for session length and re-authentication requirements. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="max_inactivity" type="string">
              How long a session may remain inactive before requiring re-authentication. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="session_length" type="string">
              How long a session remains valid before requiring re-authentication. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="session_length_enabled" type="bool">
              Whether session length limits are enforced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="session_reauth_method" type="string">
              How the user re-authenticates when their own session expires. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="use_oidc_max_age" type="bool">
              Whether the OIDC token's own `max_age` claim is honored when evaluating session freshness. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dry_run_settings" type="object">
      Settings evaluated but not yet enforced, letting the effect of a policy change be previewed first. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="access_levels" type="list(string)">
          The access level(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="session_settings" type="object">
          Configuration for session length and re-authentication requirements. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="max_inactivity" type="string">
              How long a session may remain inactive before requiring re-authentication. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="session_length" type="string">
              How long a session remains valid before requiring re-authentication. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="session_length_enabled" type="bool">
              Whether session length limits are enforced. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="session_reauth_method" type="string">
              How the user re-authenticates when their own session expires. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="use_oidc_max_age" type="bool">
              Whether the OIDC token's own `max_age` claim is honored when evaluating session freshness. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scope" type="object">
      What this configuration's own scope covers. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="client_scope" type="object">
          Restricts this rule to a specific client scope. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="restricted_client_application" type="object">
              Restricts this rule to a specific client application. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="client_id" type="string">
                  The OAuth client ID this restriction applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="restricted_project" type="object">
              A reference to the project this restriction applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource name or identifier of this object. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="session_settings" type="object">
  Stores settings related to Google Cloud Session Length including session duration, the type of challenge (i.e. method) they should face when their session expires, and other related settings. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="max_inactivity" type="string">
      Optional. How long a user is allowed to take between actions before a new access token must be issued. Only set for Google Cloud apps. Optional.
    </ResponseField>
    <ResponseField name="session_length" type="string">
      Optional. The session length. Setting this field to zero allows for sessions that are active indefinitely. Also, setting `session_length_enabled` to false disregards session limits, which means that sessions never expire. If use_oidc_max_age is true, for OIDC apps, the session length will be the minimum of this field and the OIDC max_age param. If this field is set to zero, `session_length_enabled` must be set to false or left unset. Optional.
    </ResponseField>
    <ResponseField name="session_length_enabled" type="bool">
      Optional. This field enables or disables Google Cloud session length. When false, all fields set above will be disregarded and the session length is basically infinite. If `session_length` is set to zero, this field must be set to false. Optional.
    </ResponseField>
    <ResponseField name="session_reauth_method" type="string">
      Optional. Session method when user's Google Cloud session is up. Optional.
    </ResponseField>
    <ResponseField name="use_oidc_max_age" type="bool">
      Optional. Only useful for OIDC apps. When false, the OIDC max_age param, if passed in the authentication request will be ignored. When true, the re-auth period will be the minimum of the session_length field and the max_age OIDC param. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
