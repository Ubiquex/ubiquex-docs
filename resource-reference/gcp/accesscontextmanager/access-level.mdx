---
title: "google_accesscontextmanager_access_level"
description: "`google_accesscontextmanager_access_level` defines a condition (device state, IP range..."
---

`google_accesscontextmanager_access_level` defines a condition (device state, IP range, geography) that must hold for a request to be granted access under VPC Service Controls' own Access Context Manager.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_accesscontextmanager --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_accesscontextmanager/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	accesscontextmanager "github.com/ubiquex/ubx-sdk-google/sdk/go/google_accesscontextmanager/accesscontextmanager"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own access level"})

    		ubx.Resource(accesscontextmanager.AccesscontextmanagerAccessLevel, "ci-runner", accesscontextmanager.AccesscontextmanagerAccessLevelConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_accesscontextmanager --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccesscontextmanagerAccessLevel } from "./local-sdk/google_accesscontextmanager/sdk/typescript/google_accesscontextmanager/accesscontextmanager/accesscontextmanager_access_level.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own access level" });

      resource(AccesscontextmanagerAccessLevel, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_accesscontextmanager --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_accesscontextmanager/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_accesscontextmanager.accesscontextmanager import AccesscontextmanagerAccessLevel, AccesscontextmanagerAccessLevelConfig

    def describe():
        ubx.intent("payments own access level")

        ubx.resource(AccesscontextmanagerAccessLevel, "ci-runner", AccesscontextmanagerAccessLevelConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_accesscontextmanager_access_level called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="basic" type="object">
  `BasicLevel` is an `AccessLevel` using a set of recommended features. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="combining_function" type="string">
      How the `conditions` list should be combined to determine if a request is granted this `AccessLevel`. If AND is used, each `Condition` in `conditions` must be satisfied for the `AccessLevel` to be applied. If OR is used, at least one `Condition` in `conditions` must be satisfied for the `AccessLevel` to be applied. Default behavior is AND. Optional.
    </ResponseField>
    <ResponseField name="conditions" type="list(object)">
      Required. A list of requirements for the `AccessLevel` to be granted. Optional.
      <Expandable title="properties">
        <ResponseField name="device_policy" type="object">
          Restricts access based on the requesting device's own posture, e.g. encryption and management status. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="allowed_device_management_levels" type="list(string)">
              The device management level(s) (e.g. managed, unmanaged) this access level accepts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allowed_encryption_statuses" type="list(string)">
              The device disk encryption status/statuses this access level accepts. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="os_constraints" type="list(object)">
              Device operating system requirement(s) this access level enforces. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="minimum_version" type="string">
                  The minimum required OS version. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="os_type" type="string">
                  The operating system type this constraint applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="require_verified_chrome_os" type="bool">
                  Whether the device must be a verified, unmodified Chrome OS device. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="require_admin_approval" type="bool">
              Whether admin approval is required for the device to be considered compliant. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_corp_owned" type="bool">
              Whether the device must be corporately owned. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_screenlock" type="bool">
              Whether the device must have a screen lock configured. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ip_subnetworks" type="list(string)">
          The IP address range(s), in CIDR notation, this access level accepts requests from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="members" type="list(string)">
          The principal(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="negate" type="bool">
          Whether this condition's own result is inverted. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="regions" type="list(string)">
          The geographic region(s) this access level accepts requests from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="required_access_levels" type="list(string)">
          The access level(s) a request must satisfy for this rule to permit it. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vpc_network_sources" type="list(object)">
          The VPC network(s) this rule matches traffic from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="vpc_subnetwork" type="object">
              A reference to the VPC subnetwork this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="network" type="string">
                  The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="vpc_ip_subnetworks" type="list(string)">
                  The VPC network and IP address range(s) this rule matches traffic from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="custom" type="object">
  `CustomLevel` is an `AccessLevel` using the Cloud Common Expression Language to represent the necessary conditions for the level to apply to a request. See CEL spec at: https://github.com/google/cel-spec Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="expr" type="object">
      Represents a textual expression in the Common Expression Language (CEL) syntax. CEL is a C-like expression language. The syntax and semantics of CEL are documented at https://github.com/google/cel-spec. Example (Comparison): title: "Summary size limit" description: "Determines if a summary is less than 100 chars" expression: "document.summary.size() &lt; 100" Example (Equality): title: "Requestor is owner" description: "Determines if requestor is the document owner" expression: "document.owner == request.auth.claims.email" Example (Logic): title: "Public documents" description: "Determine whether the document should be publicly visible" expression: "document.type != 'private' && document.type != 'internal'" Example (Data Manipulation): title: "Notification string" description: "Create a notification string with a timestamp." expression: "'New message received at ' + string(document.create_time)" The exact variables and functions that may be referenced within an expression are determined by the service that evaluates it. See the service documentation for additional information. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Optional. Description of the expression. This is a longer text which describes the expression, e.g. when hovered over it in a UI. Optional.
        </ResponseField>
        <ResponseField name="expression" type="string">
          Textual representation of an expression in Common Expression Language syntax. Optional.
        </ResponseField>
        <ResponseField name="location" type="string">
          Optional. String indicating the location of the expression for error reporting, e.g. a file name and a position in the file. Optional.
        </ResponseField>
        <ResponseField name="title" type="string">
          Optional. Title for the expression, i.e. a short string describing its purpose. This can be used e.g. in UIs which allow to enter the expression. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  Description of the `AccessLevel` and its use. Does not affect behavior. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. Resource name for the `AccessLevel`. Format: `accessPolicies/&#123;access_policy&#125;/accessLevels/&#123;access_level&#125;`. The `access_level` component must begin with a letter, followed by alphanumeric characters or `_`. Its maximum length is 50 characters. After you create an `AccessLevel`, you cannot change its `name`. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="title" type="string">
  Human readable title. Must be unique within the Policy. Optional; if omitted, computed by Google Cloud.
</ResponseField>
