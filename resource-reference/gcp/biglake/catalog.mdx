---
title: "google_biglake_catalog"
description: "`google_biglake_catalog` is the top-level BigLake metastore container that database(s) and table(s) are organized within, for open-format (Iceberg..."
---

`google_biglake_catalog` is the top-level BigLake metastore container that database(s) and table(s) are organized within, for open-format (Iceberg, Parquet) data managed under BigLake.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	biglake "github.com/ubiquex/ubx-sdk-google/sdk/go/google/biglake"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own catalog"})

    		ubx.Resource(biglake.Catalog, "example", biglake.CatalogConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Catalog } from "@ubx/sdk-google/google/biglake/catalog";

    export default stack("example", () => {
      intent({ summary: "example own catalog" });

      resource(Catalog, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.biglake import Catalog, CatalogConfig

    def describe():
        ubx.intent("example own catalog")

        ubx.resource(Catalog, "example", CatalogConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_biglake_catalog called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The creation time of the catalog. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="delete_time" type="string">
  Output only. The deletion time of the catalog. Only set after the catalog is deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="expire_time" type="string">
  Output only. The time when this catalog is considered expired. Only set after the catalog is deleted. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource name. Format: projects/&#123;project_id_or_number&#125;/locations/&#123;location_id&#125;/catalogs/&#123;catalog_id&#125; Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The last modification time of the catalog. Optional; if omitted, computed by Google Cloud.
</ResponseField>
