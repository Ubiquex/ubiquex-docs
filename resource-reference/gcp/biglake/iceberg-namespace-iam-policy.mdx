---
title: "google_biglake_iceberg_namespace_iam_policy"
description: "Real, generated bindings for `google_biglake_iceberg_namespace_iam_policy`."
---

`google_biglake_iceberg_namespace_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	biglake "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/biglake"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own iceberg namespace iam policy"})

    		ubx.Resource(biglake.IcebergNamespaceIamPolicy, "ci-runner", biglake.IcebergNamespaceIamPolicyConfig{
    			Catalog:     "example",
    			NamespaceId: "example",
    			PolicyData:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IcebergNamespaceIamPolicy } from "jsr:@ubx/sdk-google/google/biglake/iceberg_namespace_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own iceberg namespace iam policy" });

      resource(IcebergNamespaceIamPolicy, "ci-runner", {
        catalog: "example",
        namespaceId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.biglake import IcebergNamespaceIamPolicy, IcebergNamespaceIamPolicyConfig

    def describe():
        ubx.intent("payments own iceberg namespace iam policy")

        ubx.resource(IcebergNamespaceIamPolicy, "ci-runner", IcebergNamespaceIamPolicyConfig(
            catalog="example",
            namespace_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_biglake_iceberg_namespace_iam_policy, with catalog
    example, namespace id example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="catalog" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="namespace_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
