---
title: "google_storage_notification"
description: "Real, generated bindings for `google_storage_notification`."
---

`google_storage_notification` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/storage"
    )

    cfg := storage.NotificationConfig{
        Bucket: "example",
        PayloadFormat: "example",
        Topic: "example",
        CustomAttributes: map[string]string{"key": "value"},
        EventTypes: []string{"example"},
    }
    ubx.Resource(storage.Notification, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Notification } from
      "jsr:@ubx/sdk-google/google/storage/notification";

    resource(Notification, "example", {
      bucket: "example",
      payloadFormat: "example",
      topic: "example",
      customAttributes: { key: "value" },
      eventTypes: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.storage import Notification, NotificationConfig

    cfg = NotificationConfig(
        bucket="example",
        payload_format="example",
        topic="example",
        custom_attributes={"key": "value"},
        event_types=["example"],
    )
    ubx.resource(Notification, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_storage_notification with bucket,
payload_format, topic, custom_attributes, event_types.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="custom_attributes" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="event_types" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="object_name_prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="payload_format" type="string" required>
  Required.
</ResponseField>

<ResponseField name="topic" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="notification_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="self_link" type="string">
  Computed by GCP after `ship`.
</ResponseField>
