---
title: "google_storage_default_object_access_control"
description: "Real, generated bindings for `google_storage_default_object_access_control`."
---

`google_storage_default_object_access_control` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	storage "github.com/ubiquex/ubx-sdk-google/sdk/go/google/storage"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own default object access control"})

    		ubx.Resource(storage.DefaultObjectAccessControl, "ci-runner", storage.DefaultObjectAccessControlConfig{
    			Bucket: "example",
    			Entity: "example",
    			Role:   "example",
    			Object: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DefaultObjectAccessControl } from "jsr:@ubx/sdk-google/google/storage/default_object_access_control";

    export default stack("payments", () => {
      intent({ summary: "payments own default object access control" });

      resource(DefaultObjectAccessControl, "ci-runner", {
        bucket: "example",
        entity: "example",
        role: "example",
        object: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.storage import DefaultObjectAccessControl, DefaultObjectAccessControlConfig

    def describe():
        ubx.intent("payments own default object access control")

        ubx.resource(DefaultObjectAccessControl, "ci-runner", DefaultObjectAccessControlConfig(
            bucket="example",
            entity="example",
            role="example",
            object="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_storage_default_object_access_control, with bucket
    example, entity example, role example, object example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  The name of the bucket. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="entity" type="string" required>
  The entity holding the permission, in one of the following forms: * user-&#123;&#123;userId&#125;&#125; * user-&#123;&#123;email&#125;&#125; (such as "user-liz@example.com") * group-&#123;&#123;groupId&#125;&#125; * group-&#123;&#123;email&#125;&#125; (such as "group-example@googlegroups.com") * domain-&#123;&#123;domain&#125;&#125; (such as "domain-example.com") * project-team-&#123;&#123;projectId&#125;&#125; * allUsers * allAuthenticatedUsers Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="object" type="string">
  The name of the object, if applied to an object. Optional.
</ResponseField>

<ResponseField name="role" type="string" required>
  The access permission for the entity. Possible values: ["OWNER", "READER"] Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="domain" type="string">
  The domain associated with the entity. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="email" type="string">
  The email address associated with the entity. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="entity_id" type="string">
  The ID for the entity Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="generation" type="number">
  The content generation of the object, if applied to an object. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project_team" type="list(object)">
  The project team associated with the entity Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="project_number" type="string">
    </ResponseField>
    <ResponseField name="team" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
