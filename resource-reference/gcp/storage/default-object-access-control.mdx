---
title: "google_storage_default_object_access_control"
description: "`google_storage_default_object_access_control` defines the ACL entry newly uploaded objects in a Cloud Storage bucket inherit by default..."
---

`google_storage_default_object_access_control` defines the ACL entry newly uploaded objects in a Cloud Storage bucket inherit by default, when the bucket uses fine-grained (rather than uniform) access control.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_storage --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_storage/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	storage "github.com/ubiquex/ubx-sdk-google/sdk/go/google_storage/storage"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own default object access control"})

    		ubx.Resource(storage.StorageDefaultObjectAccessControl, "example", storage.StorageDefaultObjectAccessControlConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_storage --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { StorageDefaultObjectAccessControl } from "./local-sdk/google_storage/sdk/typescript/google_storage/storage/storage_default_object_access_control.ts";

    export default stack("example", () => {
      intent({ summary: "example own default object access control" });

      resource(StorageDefaultObjectAccessControl, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_storage --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_storage/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_storage.storage import StorageDefaultObjectAccessControl, StorageDefaultObjectAccessControlConfig

    def describe():
        ubx.intent("example own default object access control")

        ubx.resource(StorageDefaultObjectAccessControl, "example", StorageDefaultObjectAccessControlConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_storage_default_object_access_control, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="bucket" type="string">
  The name of the bucket. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="domain" type="string">
  The domain associated with the entity, if any. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="email" type="string">
  The email address associated with the entity, if any. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="entity" type="string">
  The entity holding the permission, in one of the following forms: - user-userId - user-email - group-groupId - group-email - domain-domain - project-team-projectId - allUsers - allAuthenticatedUsers Examples: - The user liz@example.com would be user-liz@example.com. - The group example@googlegroups.com would be group-example@googlegroups.com. - To refer to all members of the Google Apps for Business domain example.com, the entity would be domain-example.com. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="entity_id" type="string">
  The ID for the entity, if any. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  HTTP 1.1 Entity tag for the access-control entry. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="generation" type="string">
  The content generation of the object, if applied to an object. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="id" type="string">
  The ID of the access-control entry. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  The kind of item this is. For object access control entries, this is always storage#objectAccessControl. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="object" type="string">
  The name of the object, if applied to an object. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="project_team" type="object">
  The project team associated with the entity, if any. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="project_number" type="string">
      The project number. Optional.
    </ResponseField>
    <ResponseField name="team" type="string">
      The team for the storage default object access control's project team. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role" type="string">
  The access permission for the entity. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  The link to this access-control entry. Optional; if omitted, computed by Google Cloud.
</ResponseField>
