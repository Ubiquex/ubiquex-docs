---
title: "google_storage_object_access_control"
description: "Real, generated bindings for `google_storage_object_access_control`."
---

`google_storage_object_access_control` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own object access control"})

    		ubx.Resource(storage.ObjectAccessControl, "ci-runner", storage.ObjectAccessControlConfig{
    			Bucket: "example",
    			Entity: "example",
    			Object: "example",
    			Role:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ObjectAccessControl } from "jsr:@ubx/sdk-google/google/storage/object_access_control";

    export default stack("payments", () => {
      intent({ summary: "payments own object access control" });

      resource(ObjectAccessControl, "ci-runner", {
        bucket: "example",
        entity: "example",
        object: "example",
        role: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.storage import ObjectAccessControl, ObjectAccessControlConfig

    def describe():
        ubx.intent("payments own object access control")

        ubx.resource(ObjectAccessControl, "ci-runner", ObjectAccessControlConfig(
            bucket="example",
            entity="example",
            object="example",
            role="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_storage_object_access_control, with bucket example,
    entity example, object example, role example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="entity" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="object" type="string" required>
  Required.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="domain" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="email" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="entity_id" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="generation" type="number">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project_team" type="list(object)">
  Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="project_number" type="string">
    </ResponseField>
    <ResponseField name="team" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
