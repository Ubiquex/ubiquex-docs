---
title: "google_storage_managed_folder_iam_member"
description: "Real, generated bindings for `google_storage_managed_folder_iam_member`."
---

`google_storage_managed_folder_iam_member` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/storage"
    )

    cfg := storage.ManagedFolderIamMemberConfig{
        Bucket: "example",
        ManagedFolder: "example",
        Member: "example",
        Role: "example",
    }
    ubx.Resource(storage.ManagedFolderIamMember, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ManagedFolderIamMember } from
      "jsr:@ubx/sdk-google/google/storage/managed_folder_iam_member";

    resource(ManagedFolderIamMember, "example", {
      bucket: "example",
      managedFolder: "example",
      member: "example",
      role: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.storage import (
        ManagedFolderIamMember,
        ManagedFolderIamMemberConfig,
    )

    cfg = ManagedFolderIamMemberConfig(
        bucket="example",
        managed_folder="example",
        member="example",
        role="example",
    )
    ubx.resource(ManagedFolderIamMember, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_storage_managed_folder_iam_member with bucket,
managed_folder, member, role.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="managed_folder" type="string" required>
  Required.
</ResponseField>

<ResponseField name="member" type="string" required>
  Required.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="condition" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="expression" type="string" required>
    </ResponseField>
    <ResponseField name="title" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
