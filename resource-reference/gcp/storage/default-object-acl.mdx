---
title: "google_storage_default_object_acl"
description: "Real, generated bindings for `google_storage_default_object_acl`."
---

`google_storage_default_object_acl` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/storage"
    )

    cfg := storage.DefaultObjectAclConfig{
        Bucket: "example",
    }
    ubx.Resource(storage.DefaultObjectAcl, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DefaultObjectAcl } from
      "jsr:@ubx/sdk-google/google/storage/default_object_acl";

    resource(DefaultObjectAcl, "example", {
      bucket: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.storage import (
        DefaultObjectAcl,
        DefaultObjectAclConfig,
    )

    cfg = DefaultObjectAclConfig(
        bucket="example",
    )
    ubx.resource(DefaultObjectAcl, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_storage_default_object_acl with bucket.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role_entity" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role_entity" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>
