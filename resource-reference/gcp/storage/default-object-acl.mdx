---
title: "google_storage_default_object_acl"
description: "Real, generated bindings for `google_storage_default_object_acl`."
---

`google_storage_default_object_acl` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own default object acl"})

    		ubx.Resource(storage.DefaultObjectAcl, "ci-runner", storage.DefaultObjectAclConfig{
    			Bucket: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DefaultObjectAcl } from "jsr:@ubx/sdk-google/google/storage/default_object_acl";

    export default stack("payments", () => {
      intent({ summary: "payments own default object acl" });

      resource(DefaultObjectAcl, "ci-runner", {
        bucket: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.storage import DefaultObjectAcl, DefaultObjectAclConfig

    def describe():
        ubx.intent("payments own default object acl")

        ubx.resource(DefaultObjectAcl, "ci-runner", DefaultObjectAclConfig(
            bucket="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_storage_default_object_acl, with bucket example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role_entity" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role_entity" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>
