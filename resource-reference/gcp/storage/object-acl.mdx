---
title: "google_storage_object_acl"
description: "Real, generated bindings for `google_storage_object_acl`."
---

`google_storage_object_acl` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/storage"
    )

    cfg := storage.ObjectAclConfig{
        Bucket: "example",
        Object: "example",
        PredefinedAcl: "example",
    }
    ubx.Resource(storage.ObjectAcl, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ObjectAcl } from
      "jsr:@ubx/sdk-google/google/storage/object_acl";

    resource(ObjectAcl, "example", {
      bucket: "example",
      object: "example",
      predefinedAcl: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.storage import ObjectAcl, ObjectAclConfig

    cfg = ObjectAclConfig(
        bucket="example",
        object="example",
        predefined_acl="example",
    )
    ubx.resource(ObjectAcl, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_storage_object_acl with bucket, object,
predefined_acl.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="object" type="string" required>
  Required.
</ResponseField>

<ResponseField name="predefined_acl" type="string">
  Optional.
</ResponseField>

<ResponseField name="role_entity" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role_entity" type="set(string)">
  Optional; if omitted, computed by GCP.
</ResponseField>
