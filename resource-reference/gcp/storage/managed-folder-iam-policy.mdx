---
title: "google_storage_managed_folder_iam_policy"
description: "Real, generated bindings for `google_storage_managed_folder_iam_policy`."
---

`google_storage_managed_folder_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	storage "github.com/ubiquex/ubx-sdk-google/sdk/go/google/storage"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own managed folder iam policy"})

    		ubx.Resource(storage.ManagedFolderIamPolicy, "ci-runner", storage.ManagedFolderIamPolicyConfig{
    			Bucket:        "example",
    			ManagedFolder: "example",
    			PolicyData:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedFolderIamPolicy } from "jsr:@ubx/sdk-google/google/storage/managed_folder_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own managed folder iam policy" });

      resource(ManagedFolderIamPolicy, "ci-runner", {
        bucket: "example",
        managedFolder: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.storage import ManagedFolderIamPolicy, ManagedFolderIamPolicyConfig

    def describe():
        ubx.intent("payments own managed folder iam policy")

        ubx.resource(ManagedFolderIamPolicy, "ci-runner", ManagedFolderIamPolicyConfig(
            bucket="example",
            managed_folder="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_storage_managed_folder_iam_policy, with bucket
    example, managed folder example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="managed_folder" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
