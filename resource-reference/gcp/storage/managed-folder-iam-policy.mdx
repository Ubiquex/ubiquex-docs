---
title: "google_storage_managed_folder_iam_policy"
description: "Real, generated bindings for `google_storage_managed_folder_iam_policy`."
---

`google_storage_managed_folder_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/storage"
    )

    cfg := storage.ManagedFolderIamPolicyConfig{
        Bucket: "example",
        ManagedFolder: "example",
        PolicyData: "example",
    }
    ubx.Resource(storage.ManagedFolderIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ManagedFolderIamPolicy } from
      "jsr:@ubx/sdk-google/google/storage/managed_folder_iam_policy";

    resource(ManagedFolderIamPolicy, "example", {
      bucket: "example",
      managedFolder: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.storage import (
        ManagedFolderIamPolicy,
        ManagedFolderIamPolicyConfig,
    )

    cfg = ManagedFolderIamPolicyConfig(
        bucket="example",
        managed_folder="example",
        policy_data="example",
    )
    ubx.resource(ManagedFolderIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_storage_managed_folder_iam_policy with bucket,
managed_folder, policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="managed_folder" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
