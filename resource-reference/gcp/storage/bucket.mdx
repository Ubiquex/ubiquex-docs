---
title: "google_storage_bucket"
description: "`google_storage_bucket` is a Cloud Storage bucket, the top-level container that object(s) are uploaded into, with its own region, storage class..."
---

`google_storage_bucket` is a Cloud Storage bucket, the top-level container that object(s) are uploaded into, with its own region, storage class, and access-control configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	storage "github.com/ubiquex/ubx-sdk-google/sdk/go/google/storage"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own bucket"})

    		ubx.Resource(storage.Bucket, "example", storage.BucketConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Bucket } from "@ubx/sdk-google/google/storage/bucket";

    export default stack("example", () => {
      intent({ summary: "example own bucket" });

      resource(Bucket, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.storage import Bucket, BucketConfig

    def describe():
        ubx.intent("example own bucket")

        ubx.resource(Bucket, "example", BucketConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_storage_bucket called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="acl" type="list(object)">
  Access controls on the bucket. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string">
      A reference to the Cloud Storage bucket this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="domain" type="string">
      The domain this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      An email address associated with this entity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="entity" type="string">
      The scope this access grant applies to, e.g. a specific user, group, or `allUsers`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="entity_id" type="string">
      The unique identifier of this entity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="etag" type="string">
      A checksum computed by the server from the resource's own current content, used for optimistic concurrency control -- an update/delete that includes a stale `etag` is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="string">
      A unique identifier for this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kind" type="string">
      The category of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="project_team" type="object">
      The project team role associated with this entity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="project_number" type="string">
          The numeric identifier of the associated Google Cloud project. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="team" type="string">
          The team associated with this entity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="role" type="string">
      The access role granted, e.g. `READER` or `OWNER`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="self_link" type="string">
      The API URL identifying this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="autoclass" type="object">
  The bucket's Autoclass configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Whether or not Autoclass is enabled on this bucket Optional.
    </ResponseField>
    <ResponseField name="terminal_storage_class" type="string">
      The storage class that objects in the bucket eventually transition to if they are not read for a certain length of time. Valid values are NEARLINE and ARCHIVE. Optional.
    </ResponseField>
    <ResponseField name="terminal_storage_class_update_time" type="string">
      A date and time in RFC 3339 format representing the time of the most recent update to "terminalStorageClass". Optional.
    </ResponseField>
    <ResponseField name="toggle_time" type="string">
      A date and time in RFC 3339 format representing the instant at which "enabled" was last toggled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="billing" type="object">
  The bucket's billing configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="requester_pays" type="bool">
      When set to true, Requester Pays is enabled for this bucket. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cors" type="list(object)">
  The bucket's Cross-Origin Resource Sharing (CORS) configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="max_age_seconds" type="number">
      The maximum time, in seconds, a cached response may be reused. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="method" type="list(string)">
      The HTTP method(s) this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="origin" type="list(string)">
      The origin(s) this CORS rule permits requests from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="response_header" type="list(string)">
      The HTTP response header(s) exposed by this CORS rule. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="custom_placement_config" type="object">
  The bucket's custom placement configuration for Custom Dual Regions. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="data_locations" type="list(string)">
      The list of regional locations in which data is placed. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_event_based_hold" type="bool">
  The default value for event-based hold on newly created objects in this bucket. Event-based hold is a way to retain objects indefinitely until an event occurs, signified by the hold's release. After being released, such objects will be subject to bucket-level retention (if any). One sample use case of this flag is for banks to hold loan documents for at least 3 years after loan is paid in full. Here, bucket-level retention is 3 years and the event is loan being paid in full. In this example, these objects will be held intact for any number of years until the event has occurred (event-based hold on the object is released) and then 3 more years after that. That means retention duration of the objects begins from the moment event-based hold transitioned from true to false. Objects under event-based hold cannot be deleted, overwritten or archived until the hold is removed. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="default_object_acl" type="list(object)">
  Default access controls to apply to new objects when no ACL is provided. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string">
      A reference to the Cloud Storage bucket this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="domain" type="string">
      The domain this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      An email address associated with this entity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="entity" type="string">
      The scope this access grant applies to, e.g. a specific user, group, or `allUsers`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="entity_id" type="string">
      The unique identifier of this entity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="etag" type="string">
      A checksum computed by the server from the resource's own current content, used for optimistic concurrency control -- an update/delete that includes a stale `etag` is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="generation" type="string">
      The specific object generation this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="string">
      A unique identifier for this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kind" type="string">
      The category of this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="object" type="string">
      A reference to the Cloud Storage object this applies to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="project_team" type="object">
      The project team role associated with this entity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="project_number" type="string">
          The numeric identifier of the associated Google Cloud project. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="team" type="string">
          The team associated with this entity. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="role" type="string">
      The access role granted, e.g. `READER` or `OWNER`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="self_link" type="string">
      The API URL identifying this resource. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="encryption" type="object">
  Encryption configuration for a bucket. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="customer_managed_encryption_enforcement_config" type="object">
      If set, the new objects created in this bucket must comply with this enforcement config. Changing this has no effect on existing objects; it applies to new objects only. If omitted, the new objects are allowed to be encrypted with Customer Managed Encryption type by default. Optional.
      <Expandable title="properties">
        <ResponseField name="effective_time" type="string">
          Server-determined value that indicates the time from which configuration was enforced and effective. This value is in RFC 3339 format. Optional.
        </ResponseField>
        <ResponseField name="restriction_mode" type="string">
          Restriction mode for Customer-Managed Encryption Keys. Defaults to NotRestricted. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="customer_supplied_encryption_enforcement_config" type="object">
      If set, the new objects created in this bucket must comply with this enforcement config. Changing this has no effect on existing objects; it applies to new objects only. If omitted, the new objects are allowed to be encrypted with Customer Supplied Encryption type by default. Optional.
      <Expandable title="properties">
        <ResponseField name="effective_time" type="string">
          Server-determined value that indicates the time from which configuration was enforced and effective. This value is in RFC 3339 format. Optional.
        </ResponseField>
        <ResponseField name="restriction_mode" type="string">
          Restriction mode for Customer-Supplied Encryption Keys. Defaults to NotRestricted. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_kms_key_name" type="string">
      A Cloud KMS key that will be used to encrypt objects inserted into this bucket, if no encryption method is specified. Optional.
    </ResponseField>
    <ResponseField name="google_managed_encryption_enforcement_config" type="object">
      If set, the new objects created in this bucket must comply with this enforcement config. Changing this has no effect on existing objects; it applies to new objects only. If omitted, the new objects are allowed to be encrypted with Google Managed Encryption type by default. Optional.
      <Expandable title="properties">
        <ResponseField name="effective_time" type="string">
          Server-determined value that indicates the time from which configuration was enforced and effective. This value is in RFC 3339 format. Optional.
        </ResponseField>
        <ResponseField name="restriction_mode" type="string">
          Restriction mode for Google-Managed Encryption Keys. Defaults to NotRestricted. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  HTTP 1.1 Entity tag for the bucket. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="generation" type="string">
  The generation of this bucket. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="hard_delete_time" type="string">
  The hard delete time of the bucket in RFC 3339 format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="hierarchical_namespace" type="object">
  The bucket's hierarchical namespace configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      When set to true, hierarchical namespace is enabled for this bucket. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="iam_configuration" type="object">
  The bucket's IAM configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="bucket_policy_only" type="object">
      The bucket's uniform bucket-level access configuration. The feature was formerly known as Bucket Policy Only. For backward compatibility, this field will be populated with identical information as the uniformBucketLevelAccess field. We recommend using the uniformBucketLevelAccess field to enable and disable the feature. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          If set, access is controlled only by bucket-level or above IAM policies. Optional.
        </ResponseField>
        <ResponseField name="locked_time" type="string">
          The deadline for changing iamConfiguration.bucketPolicyOnly.enabled from true to false in RFC 3339 format. iamConfiguration.bucketPolicyOnly.enabled may be changed from true to false until the locked time, after which the field is immutable. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="public_access_prevention" type="string">
      The bucket's Public Access Prevention configuration. Currently, 'inherited' and 'enforced' are supported. Optional.
    </ResponseField>
    <ResponseField name="uniform_bucket_level_access" type="object">
      The bucket's uniform bucket-level access configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          If set, access is controlled only by bucket-level or above IAM policies. Optional.
        </ResponseField>
        <ResponseField name="locked_time" type="string">
          The deadline for changing iamConfiguration.uniformBucketLevelAccess.enabled from true to false in RFC 3339 format. iamConfiguration.uniformBucketLevelAccess.enabled may be changed from true to false until the locked time, after which the field is immutable. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  The ID of the bucket. For buckets, the id and name properties are the same. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ip_filter" type="object">
  The bucket's IP filter configuration. Specifies the network sources that are allowed to access the operations on the bucket, as well as its underlying objects. Only enforced when the mode is set to 'Enabled'. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="allow_all_service_agent_access" type="bool">
      Whether to allow all service agents to access the bucket regardless of the IP filter configuration. Optional.
    </ResponseField>
    <ResponseField name="allow_cross_org_vpcs" type="bool">
      Whether to allow cross-org VPCs in the bucket's IP filter configuration. Optional.
    </ResponseField>
    <ResponseField name="mode" type="string">
      The mode of the IP filter. Valid values are 'Enabled' and 'Disabled'. Optional.
    </ResponseField>
    <ResponseField name="public_network_source" type="object">
      The public network source of the bucket's IP filter. Optional.
      <Expandable title="properties">
        <ResponseField name="allowed_ip_cidr_ranges" type="list(string)">
          The list of public IPv4, IPv6 cidr ranges that are allowed to access the bucket. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vpc_network_sources" type="list(object)">
      The list of [VPC network](https://cloud.google.com/vpc/docs/vpc) sources of the bucket's IP filter. Optional.
      <Expandable title="properties">
        <ResponseField name="allowed_ip_cidr_ranges" type="list(string)">
          The IP address range(s), in CIDR notation, permitted to access this bucket. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network" type="string">
          The VPC network this resource is attached to, in the form `projects/&#123;project&#125;/global/networks/&#123;network&#125;`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  The kind of item this is. For buckets, this is always storage#bucket. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  User-provided labels, in key/value pairs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="lifecycle" type="object">
  The bucket's lifecycle configuration. See [Lifecycle Management](https://cloud.google.com/storage/docs/lifecycle) for more information. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="rule" type="list(object)">
      A lifecycle management rule, which is made of an action to take and the condition(s) under which the action will be taken. Optional.
      <Expandable title="properties">
        <ResponseField name="action" type="object">
          The action to take when this rule's own condition is satisfied. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="storage_class" type="string">
              The Cloud Storage class this object or bucket uses. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The JSON Schema data type this (sub-)schema constrains values to: `string`, `number`, `integer`, `boolean`, `object`, `array`, or `null`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="condition" type="object">
          The condition that must be satisfied for this rule to apply. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="age" type="number">
              The age, in days, an object must reach for this condition to match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="created_before" type="string">
              Matches object(s) created before this date. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="custom_time_before" type="string">
              Matches object(s) whose own custom time is before this date. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="days_since_custom_time" type="number">
              The number of days since an object's own custom time for this condition to match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="days_since_noncurrent_time" type="number">
              The number of days since an object became noncurrent for this condition to match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="is_live" type="bool">
              Whether this condition matches only live (non-archived) object versions. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="matches_pattern" type="string">
              A glob pattern object names must match for this rule to apply. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="matches_prefix" type="list(string)">
              The object name prefix(es) this rule matches. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="matches_storage_class" type="list(string)">
              The storage class(es) this rule matches. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="matches_suffix" type="list(string)">
              The object name suffix(es) this rule matches. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="noncurrent_time_before" type="string">
              Matches object(s) that became noncurrent before this date. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="num_newer_versions" type="number">
              The number of newer version(s) an object must have for this condition to match. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="size_above_bytes" type="string">
              Matches object(s) larger than this size, in bytes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="size_below_bytes" type="string">
              Matches object(s) smaller than this size, in bytes. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location" type="string">
  The location of the bucket. Object data for objects in the bucket resides in physical storage within this region. Defaults to US. See the [Developer's Guide](https://cloud.google.com/storage/docs/locations) for the authoritative list. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="location_type" type="string">
  The type of the bucket location. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="logging" type="object">
  The bucket's logging configuration, which defines the destination bucket and optional name prefix for the current bucket's logs. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="log_bucket" type="string">
      The destination bucket where the current bucket's logs should be placed. Optional.
    </ResponseField>
    <ResponseField name="log_object_prefix" type="string">
      A prefix for log object names. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metageneration" type="string">
  The metadata generation of this bucket. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the bucket. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="object_retention" type="object">
  The bucket's object retention config. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="mode" type="string">
      The bucket's object retention mode. Can be Enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="owner" type="object">
  The owner of the bucket. This is always the project team's owner group. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="entity" type="string">
      The entity, in the form project-owner-projectId. Optional.
    </ResponseField>
    <ResponseField name="entity_id" type="string">
      The ID for the entity. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project_number" type="string">
  The project number of the project the bucket belongs to. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="retention_policy" type="object">
  The bucket's retention policy. The retention policy enforces a minimum retention time for all objects contained in the bucket, based on their creation time. Any attempt to overwrite or delete objects younger than the retention period will result in a PERMISSION_DENIED error. An unlocked retention policy can be modified or removed from the bucket via a storage.buckets.update operation. A locked retention policy cannot be removed or shortened in duration for the lifetime of the bucket. Attempting to remove or decrease period of a locked retention policy will result in a PERMISSION_DENIED error. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="effective_time" type="string">
      Server-determined value that indicates the time from which policy was enforced and effective. This value is in RFC 3339 format. Optional.
    </ResponseField>
    <ResponseField name="is_locked" type="bool">
      Once locked, an object retention policy cannot be modified. Optional.
    </ResponseField>
    <ResponseField name="retention_period" type="string">
      The duration in seconds that objects need to be retained. Retention duration must be greater than zero and less than 100 years. Note that enforcement of retention periods less than a day is not guaranteed. Such periods should only be used for testing purposes. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rpo" type="string">
  The Recovery Point Objective (RPO) of this bucket. Set to ASYNC_TURBO to turn on Turbo Replication on a bucket. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Reserved for future use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="self_link" type="string">
  The URI of this bucket. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="soft_delete_policy" type="object">
  The bucket's soft delete policy, which defines the period of time that soft-deleted objects will be retained, and cannot be permanently deleted. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="effective_time" type="string">
      Server-determined value that indicates the time from which the policy, or one with a greater retention, was effective. This value is in RFC 3339 format. Optional.
    </ResponseField>
    <ResponseField name="retention_duration_seconds" type="string">
      The duration in seconds that soft-deleted objects in the bucket will be retained and cannot be permanently deleted. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="soft_delete_time" type="string">
  The soft delete time of the bucket in RFC 3339 format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="storage_class" type="string">
  The bucket's default storage class, used whenever no storageClass is specified for a newly-created object. This defines how objects in the bucket are stored and determines the SLA and the cost of storage. Values include MULTI_REGIONAL, REGIONAL, STANDARD, NEARLINE, COLDLINE, ARCHIVE, and DURABLE_REDUCED_AVAILABILITY. If this value is not specified when the bucket is created, it will default to STANDARD. For more information, see [Storage Classes](https://cloud.google.com/storage/docs/storage-classes). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="time_created" type="string">
  The creation time of the bucket in RFC 3339 format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="updated" type="string">
  The modification time of the bucket in RFC 3339 format. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="versioning" type="object">
  The bucket's versioning configuration. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      While set to true, versioning is fully enabled for this bucket. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="website" type="object">
  The bucket's website configuration, controlling how the service behaves when accessing bucket contents as a web site. See the [Static Website Examples](https://cloud.google.com/storage/docs/static-website) for more information. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="main_page_suffix" type="string">
      If the requested object path is missing, the service will ensure the path has a trailing '/', append this suffix, and attempt to retrieve the resulting object. This allows the creation of index.html objects to represent directory pages. Optional.
    </ResponseField>
    <ResponseField name="not_found_page" type="string">
      If the requested object path is missing, and any mainPageSuffix object is missing, if applicable, the service will return the named object from this bucket as the content for a 404 Not Found result. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
