---
title: "google_firebaserules_release"
description: "`google_firebaserules_release` publishes a specific Firebase Security Rules ruleset live for a project's own Firestore, Realtime Database..."
---

`google_firebaserules_release` publishes a specific Firebase Security Rules ruleset live for a project's own Firestore, Realtime Database, or Cloud Storage resource.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaserules "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firebaserules"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own release"})

    		ubx.Resource(firebaserules.Release, "example", firebaserules.ReleaseConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Release } from "@ubx/sdk-google/google/firebaserules/release";

    export default stack("example", () => {
      intent({ summary: "example own release" });

      resource(Release, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firebaserules import Release, ReleaseConfig

    def describe():
        ubx.intent("example own release")

        ubx.resource(Release, "example", ReleaseConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebaserules_release called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Time the release was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. Format: `projects/&#123;project_id&#125;/releases/&#123;release_id&#125;` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ruleset_name" type="string">
  Required. Name of the `Ruleset` referred to by this `Release`. The `Ruleset` must exist for the `Release` to be created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time the release was updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
