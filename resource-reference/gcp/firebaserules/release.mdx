---
title: "google_firebaserules_release"
description: "Real, generated bindings for `google_firebaserules_release`."
---

`google_firebaserules_release` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaserules "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firebaserules"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own release"})

    		ubx.Resource(firebaserules.Release, "ci-runner", firebaserules.ReleaseConfig{
    			Name:        "ci-runner",
    			RulesetName: "example-ruleset-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Release } from "jsr:@ubx/sdk-google/google/firebaserules/release";

    export default stack("payments", () => {
      intent({ summary: "payments own release" });

      resource(Release, "ci-runner", {
        name: "ci-runner",
        rulesetName: "example-ruleset-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firebaserules import Release, ReleaseConfig

    def describe():
        ubx.intent("payments own release")

        ubx.resource(Release, "ci-runner", ReleaseConfig(
            name="ci-runner",
            ruleset_name="example-ruleset-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebaserules_release called "ci-runner", with
    ruleset name example-ruleset-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Format: `projects/&#123;project_id&#125;/releases/&#123;release_id&#125;`\Firestore Rules Releases will **always** have the name 'cloud.firestore' Required.
</ResponseField>

<ResponseField name="project" type="string">
  The project for the resource Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ruleset_name" type="string" required>
  Name of the `Ruleset` referred to by this `Release`. The `Ruleset` must exist for the `Release` to be created. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Output only. Time the release was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Disable the release to keep it from being served. The response code of NOT_FOUND will be given for executables generated from this Release. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  The project for the resource Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time the release was updated. Computed by GCP after `ship`.
</ResponseField>
