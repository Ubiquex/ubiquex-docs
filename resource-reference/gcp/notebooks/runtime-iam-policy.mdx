---
title: "google_notebooks_runtime_iam_policy"
description: "Real, generated bindings for `google_notebooks_runtime_iam_policy`."
---

`google_notebooks_runtime_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/notebooks"
    )

    cfg := notebooks.RuntimeIamPolicyConfig{
        PolicyData: "example",
        RuntimeName: "example-runtime-name",
    }
    ubx.Resource(notebooks.RuntimeIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RuntimeIamPolicy } from
      "jsr:@ubx/sdk-google/google/notebooks/runtime_iam_policy";

    resource(RuntimeIamPolicy, "example", {
      policyData: "example",
      runtimeName: "example-runtime-name",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.notebooks import (
        RuntimeIamPolicy,
        RuntimeIamPolicyConfig,
    )

    cfg = RuntimeIamPolicyConfig(
        policy_data="example",
        runtime_name="example-runtime-name",
    )
    ubx.resource(RuntimeIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_notebooks_runtime_iam_policy with policy_data,
runtime_name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="runtime_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
