---
title: "google_notebooks_instance"
description: "Real, generated bindings for `google_notebooks_instance`."
---

`google_notebooks_instance` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	notebooks "github.com/ubiquex/ubx-sdk-google/sdk/go/google/notebooks"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance"})

    		ubx.Resource(notebooks.Instance, "ci-runner", notebooks.InstanceConfig{
    			Location:            "example",
    			MachineType:         "example",
    			Name:                "ci-runner",
    			BootDiskSizeGb:      1,
    			BootDiskType:        "example",
    			CustomGpuDriverPath: "example",
    			DataDiskSizeGb:      1,
    			DataDiskType:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Instance } from "jsr:@ubx/sdk-google/google/notebooks/instance";

    export default stack("payments", () => {
      intent({ summary: "payments own instance" });

      resource(Instance, "ci-runner", {
        location: "example",
        machineType: "example",
        name: "ci-runner",
        bootDiskSizeGb: 1,
        bootDiskType: "example",
        customGpuDriverPath: "example",
        dataDiskSizeGb: 1,
        dataDiskType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.notebooks import Instance, InstanceConfig

    def describe():
        ubx.intent("payments own instance")

        ubx.resource(Instance, "ci-runner", InstanceConfig(
            location="example",
            machine_type="example",
            name="ci-runner",
            boot_disk_size_gb=1,
            boot_disk_type="example",
            custom_gpu_driver_path="example",
            data_disk_size_gb=1,
            data_disk_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_notebooks_instance called "ci-runner", with
    location example, machine type example, boot disk size gb 1, boot
    disk type example, custom gpu driver path example, data disk size
    gb 1, data disk type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="accelerator_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="core_count" type="number" required>
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="boot_disk_size_gb" type="number">
  The size of the boot disk in GB attached to this instance, up to a maximum of 64000 GB (64 TB). The minimum recommended value is 100 GB. If not specified, this defaults to 100. Optional.
</ResponseField>

<ResponseField name="boot_disk_type" type="string">
  Possible disk types for notebook instances. Possible values: ["DISK_TYPE_UNSPECIFIED", "PD_STANDARD", "PD_SSD", "PD_BALANCED", "PD_EXTREME"] Optional.
</ResponseField>

<ResponseField name="container_image" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="repository" type="string" required>
    </ResponseField>
    <ResponseField name="tag" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Instance creation time Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="custom_gpu_driver_path" type="string">
  Specify a custom Cloud Storage path where the GPU driver is stored. If not specified, we'll automatically choose from official GPU drivers. Optional.
</ResponseField>

<ResponseField name="data_disk_size_gb" type="number">
  The size of the data disk in GB attached to this instance, up to a maximum of 64000 GB (64 TB). You can choose the size of the data disk based on how big your notebooks and data are. If not specified, this defaults to 100. Optional.
</ResponseField>

<ResponseField name="data_disk_type" type="string">
  Possible disk types for notebook instances. Possible values: ["DISK_TYPE_UNSPECIFIED", "PD_STANDARD", "PD_SSD", "PD_BALANCED", "PD_EXTREME"] Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="desired_state" type="string">
  Desired state of the Notebook Instance. Set this field to 'ACTIVE' to start the Instance, and 'STOPPED' to stop the Instance. Optional.
</ResponseField>

<ResponseField name="disk_encryption" type="string">
  Disk encryption method used on the boot and data disks, defaults to GMEK. Possible values: ["DISK_ENCRYPTION_UNSPECIFIED", "GMEK", "CMEK"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="install_gpu_driver" type="bool">
  Whether the end user authorizes Google Cloud to install GPU driver on this instance. If this field is empty or set to false, the GPU driver won't be installed. Only applicable to instances with GPUs. Optional.
</ResponseField>

<ResponseField name="instance_owners" type="list(string)">
  The list of owners of this instance after creation. Format: alias@example.com. Currently supports one owner only. If not specified, all of the service account users of your VM instance's service account can use the instance. Optional.
</ResponseField>

<ResponseField name="kms_key" type="string">
  The KMS key used to encrypt the disks, only applicable if diskEncryption is CMEK. Format: projects/&#123;project_id&#125;/locations/&#123;location&#125;/keyRings/&#123;key_ring_id&#125;/cryptoKeys/&#123;key_id&#125; Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels to apply to this instance. These can be later modified by the setLabels method. An object containing a list of "key": value pairs. Example: &#123; "name": "wrench", "mass": "1.3kg", "count": "3" &#125;. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  A reference to the zone where the machine resides. Required.
</ResponseField>

<ResponseField name="machine_type" type="string" required>
  A reference to a machine type which defines VM kind. Required.
</ResponseField>

<ResponseField name="metadata" type="map(string)">
  Custom metadata to apply to this instance. An object containing a list of "key": value pairs. Example: &#123; "name": "wrench", "mass": "1.3kg", "count": "3" &#125;. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name specified for the Notebook instance. Required.
</ResponseField>

<ResponseField name="network" type="string">
  The name of the VPC that this instance is in. Format: projects/&#123;project_id&#125;/global/networks/&#123;network_id&#125; Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="nic_type" type="string">
  The type of vNIC driver. Possible values: ["UNSPECIFIED_NIC_TYPE", "VIRTIO_NET", "GVNIC"] Optional.
</ResponseField>

<ResponseField name="no_proxy_access" type="bool">
  The notebook instance will not register with the proxy.. Optional.
</ResponseField>

<ResponseField name="no_public_ip" type="bool">
  No public IP will be assigned to this instance. Optional.
</ResponseField>

<ResponseField name="no_remove_data_disk" type="bool">
  If true, the data disk will not be auto deleted when deleting the instance. Optional.
</ResponseField>

<ResponseField name="post_startup_script" type="string">
  Path to a Bash script that automatically runs after a notebook instance fully boots up. The path must be a URL or Cloud Storage path (gs://path-to-file/file-name). Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reservation_affinity" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="consume_reservation_type" type="string" required>
    </ResponseField>
    <ResponseField name="key" type="string">
    </ResponseField>
    <ResponseField name="values" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_account" type="string">
  The service account on this instance, giving access to other Google Cloud services. You can use any service account within the same project, but you must have the service account user permission to use the instance. If not specified, the Compute Engine default service account is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_account_scopes" type="list(string)">
  Optional. The URIs of service account scopes to be included in Compute Engine instances. If not specified, the following scopes are defined: - https://www.googleapis.com/auth/cloud-platform - https://www.googleapis.com/auth/userinfo.email Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="shielded_instance_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable_integrity_monitoring" type="bool">
    </ResponseField>
    <ResponseField name="enable_secure_boot" type="bool">
    </ResponseField>
    <ResponseField name="enable_vtpm" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="subnet" type="string">
  The name of the subnet that this instance is in. Format: projects/&#123;project_id&#125;/regions/&#123;region&#125;/subnetworks/&#123;subnetwork_id&#125; Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tags" type="list(string)">
  The Compute Engine tags to add to instance. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Instance update time. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="vm_image" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="image_family" type="string">
    </ResponseField>
    <ResponseField name="image_name" type="string">
    </ResponseField>
    <ResponseField name="project" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="accelerator_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="core_count" type="number" required>
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="container_image" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="repository" type="string" required>
    </ResponseField>
    <ResponseField name="tag" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Instance creation time Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disk_encryption" type="string">
  Disk encryption method used on the boot and data disks, defaults to GMEK. Possible values: ["DISK_ENCRYPTION_UNSPECIFIED", "GMEK", "CMEK"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network" type="string">
  The name of the VPC that this instance is in. Format: projects/&#123;project_id&#125;/global/networks/&#123;network_id&#125; Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="proxy_uri" type="string">
  The proxy endpoint that is used to access the Jupyter notebook. Only returned when the resource is in a 'PROVISIONED' state. If needed you can utilize 'terraform apply -refresh-only' to await the population of this value. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="reservation_affinity" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="consume_reservation_type" type="string" required>
    </ResponseField>
    <ResponseField name="key" type="string">
    </ResponseField>
    <ResponseField name="values" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_account" type="string">
  The service account on this instance, giving access to other Google Cloud services. You can use any service account within the same project, but you must have the service account user permission to use the instance. If not specified, the Compute Engine default service account is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_account_scopes" type="list(string)">
  Optional. The URIs of service account scopes to be included in Compute Engine instances. If not specified, the following scopes are defined: - https://www.googleapis.com/auth/cloud-platform - https://www.googleapis.com/auth/userinfo.email Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="shielded_instance_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable_integrity_monitoring" type="bool">
    </ResponseField>
    <ResponseField name="enable_secure_boot" type="bool">
    </ResponseField>
    <ResponseField name="enable_vtpm" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  The state of this instance. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="subnet" type="string">
  The name of the subnet that this instance is in. Format: projects/&#123;project_id&#125;/regions/&#123;region&#125;/subnetworks/&#123;subnetwork_id&#125; Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tags" type="list(string)">
  The Compute Engine tags to add to instance. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Instance update time. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="vm_image" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="image_family" type="string">
    </ResponseField>
    <ResponseField name="image_name" type="string">
    </ResponseField>
    <ResponseField name="project" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>
