---
title: "google_notebooks_environment"
description: "Real, generated bindings for `google_notebooks_environment`."
---

`google_notebooks_environment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	notebooks "github.com/ubiquex/ubx-sdk-google/sdk/go/google/notebooks"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own environment"})

    		ubx.Resource(notebooks.Environment, "ci-runner", notebooks.EnvironmentConfig{
    			Location:          "example",
    			Name:              "ci-runner",
    			Description:       "Managed by ubx.",
    			DisplayName:       "example-display-name",
    			PostStartupScript: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Environment } from "jsr:@ubx/sdk-google/google/notebooks/environment";

    export default stack("payments", () => {
      intent({ summary: "payments own environment" });

      resource(Environment, "ci-runner", {
        location: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        postStartupScript: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.notebooks import Environment, EnvironmentConfig

    def describe():
        ubx.intent("payments own environment")

        ubx.resource(Environment, "ci-runner", EnvironmentConfig(
            location="example",
            name="ci-runner",
            description="Managed by ubx.",
            display_name="example-display-name",
            post_startup_script="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_notebooks_environment called "ci-runner", with
    location example, description Managed by ubx., display name
    example-display-name, post startup script example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="container_image" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="repository" type="string" required>
    </ResponseField>
    <ResponseField name="tag" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A brief description of this environment. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  Display name of this environment for the UI. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  A reference to the zone where the machine resides. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name specified for the Environment instance. Format: projects/&#123;project_id&#125;/locations/&#123;location&#125;/environments/&#123;environmentId&#125; Required.
</ResponseField>

<ResponseField name="post_startup_script" type="string">
  Path to a Bash script that automatically runs after a notebook instance fully boots up. The path must be a URL or Cloud Storage path. Example: "gs://path-to-file/file-name" Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vm_image" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="image_family" type="string">
    </ResponseField>
    <ResponseField name="image_name" type="string">
    </ResponseField>
    <ResponseField name="project" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="container_image" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="repository" type="string" required>
    </ResponseField>
    <ResponseField name="tag" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Instance creation time Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vm_image" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="image_family" type="string">
    </ResponseField>
    <ResponseField name="image_name" type="string">
    </ResponseField>
    <ResponseField name="project" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>
