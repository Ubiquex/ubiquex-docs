---
title: "google_dns_managed_zone"
description: "Real, generated bindings for `google_dns_managed_zone`."
---

`google_dns_managed_zone` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dns "github.com/ubiquex/ubx-sdk-google/sdk/go/google/dns"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own managed zone"})

    		ubx.Resource(dns.ManagedZone, "ci-runner", dns.ManagedZoneConfig{
    			DnsName:      "example-dns-name",
    			Name:         "ci-runner",
    			Description:  "Managed by ubx.",
    			ForceDestroy: true,
    			Labels:       map[string]string{"managed-by": "ubx"},
    			Visibility:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedZone } from "jsr:@ubx/sdk-google/google/dns/managed_zone";

    export default stack("payments", () => {
      intent({ summary: "payments own managed zone" });

      resource(ManagedZone, "ci-runner", {
        dnsName: "example-dns-name",
        name: "ci-runner",
        description: "Managed by ubx.",
        forceDestroy: true,
        labels: { "managed-by": "ubx" },
        visibility: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dns import ManagedZone, ManagedZoneConfig

    def describe():
        ubx.intent("payments own managed zone")

        ubx.resource(ManagedZone, "ci-runner", ManagedZoneConfig(
            dns_name="example-dns-name",
            name="ci-runner",
            description="Managed by ubx.",
            force_destroy=True,
            labels={"managed-by": "ubx"},
            visibility="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dns_managed_zone called "ci-runner", with dns name
    example-dns-name, description Managed by ubx., force destroy
    true, labels a set value, visibility example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_logging_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable_logging" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A textual description field. Defaults to 'Managed by Terraform'. Optional.
</ResponseField>

<ResponseField name="dns_name" type="string" required>
  The DNS name of this managed zone, for instance "example.com.". Required.
</ResponseField>

<ResponseField name="dnssec_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="default_key_specs" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="algorithm" type="string">
        </ResponseField>
        <ResponseField name="key_length" type="number">
        </ResponseField>
        <ResponseField name="key_type" type="string">
        </ResponseField>
        <ResponseField name="kind" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kind" type="string">
    </ResponseField>
    <ResponseField name="non_existence" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  Set this true to delete all records in the zone. Optional.
</ResponseField>

<ResponseField name="forwarding_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="target_name_servers" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="domain_name" type="string">
        </ResponseField>
        <ResponseField name="forwarding_path" type="string">
        </ResponseField>
        <ResponseField name="ipv4_address" type="string">
        </ResponseField>
        <ResponseField name="ipv6_address" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  A set of key/value label pairs to assign to this ManagedZone. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  User assigned name for this resource. Must be unique within the project. Required.
</ResponseField>

<ResponseField name="peering_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="target_network" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="network_url" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="private_visibility_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="gke_clusters" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="gke_cluster_name" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="networks" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="network_url" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="visibility" type="string">
  The zone's visibility: public zones are exposed to the Internet, while private zones are visible only to Virtual Private Cloud resources. Default value: "public" Possible values: ["private", "public"] Optional.
</ResponseField>

## Output properties

<ResponseField name="cloud_logging_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enable_logging" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_time" type="string">
  The time that this resource was created on the server. This is in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="dnssec_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="default_key_specs" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="algorithm" type="string">
        </ResponseField>
        <ResponseField name="key_length" type="number">
        </ResponseField>
        <ResponseField name="key_type" type="string">
        </ResponseField>
        <ResponseField name="kind" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kind" type="string">
    </ResponseField>
    <ResponseField name="non_existence" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="forwarding_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="target_name_servers" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="domain_name" type="string">
        </ResponseField>
        <ResponseField name="forwarding_path" type="string">
        </ResponseField>
        <ResponseField name="ipv4_address" type="string">
        </ResponseField>
        <ResponseField name="ipv6_address" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="managed_zone_id" type="number">
  Unique identifier for the resource; defined by the server. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name_servers" type="list(string)">
  Delegate your managed_zone to these virtual name servers; defined by the server Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="peering_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="target_network" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="network_url" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="private_visibility_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="gke_clusters" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="gke_cluster_name" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="networks" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="network_url" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
