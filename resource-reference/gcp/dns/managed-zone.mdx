---
title: "google_dns_managed_zone"
description: "`google_dns_managed_zone` is a Cloud DNS zone -- a container of DNS record(s) for a specific domain..."
---

`google_dns_managed_zone` is a Cloud DNS zone -- a container of DNS record(s) for a specific domain, either publicly resolvable or private to a set of VPC networks.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_dns --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_dns/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dns "github.com/ubiquex/ubx-sdk-google/sdk/go/google_dns/dns"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managed zone"})

    		ubx.Resource(dns.DnsManagedZone, "example", dns.DnsManagedZoneConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_dns --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DnsManagedZone } from "./local-sdk/google_dns/sdk/typescript/google_dns/dns/dns_managed_zone.ts";

    export default stack("example", () => {
      intent({ summary: "example own managed zone" });

      resource(DnsManagedZone, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_dns --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_dns/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_dns.dns import DnsManagedZone, DnsManagedZoneConfig

    def describe():
        ubx.intent("example own managed zone")

        ubx.resource(DnsManagedZone, "example", DnsManagedZoneConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dns_managed_zone called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="cloud_logging_config" type="object">
  Cloud Logging configurations for publicly visible zones. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="enable_logging" type="bool">
      If set, enable query logging for this ManagedZone. False by default, making logging opt-in. Optional.
    </ResponseField>
    <ResponseField name="kind" type="string">
      The kind of resource or value this is. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_time" type="string">
  The time that this resource was created on the server. This is in RFC3339 text format. Output only. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  A mutable string of at most 1024 characters associated with this resource for the user's convenience. Has no effect on the managed zone's function. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="dns_name" type="string">
  The DNS name of this managed zone, for instance "example.com.". Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="dnssec_config" type="object">
  Configuration for signing this zone's own records with DNSSEC. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="default_key_specs" type="list(object)">
      Specifies parameters for generating initial DnsKeys for this ManagedZone. Can only be changed while the state is OFF. Optional.
      <Expandable title="properties">
        <ResponseField name="algorithm" type="string">
          The cryptographic algorithm used for this DNSSEC key. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key_length" type="number">
          The length, in bits, of this DNSSEC key. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key_type" type="string">
          Whether this is a DNSSEC key-signing key or zone-signing key. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of resource or value this is. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kind" type="string">
      The kind of resource or value this is. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="non_existence" type="string">
      Specifies the mechanism for authenticated denial-of-existence responses. Can only be changed while the state is OFF. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      Specifies whether DNSSEC is enabled, and what mode it is in. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="forwarding_config" type="object">
  Configures this zone to forward queries to external name servers, rather than answering from its own records. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kind" type="string">
      The kind of resource or value this is. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_name_servers" type="list(object)">
      List of target name servers to forward to. Cloud DNS selects the best available name server if more than one target is given. Optional.
      <Expandable title="properties">
        <ResponseField name="domain_name" type="string">
          The DNS domain name this zone serves, e.g. `example.com.`. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="forwarding_path" type="string">
          Whether forwarded queries are sent over the public internet or a private network path. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv4_address" type="string">
          An IPv4 address. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipv6_address" type="string">
          An IPv6 address. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of resource or value this is. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Unique identifier for the resource; defined by the server (output only) Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="kind" type="string">
  The kind of resource or value this is. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels to apply to this DNS managed zone. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  User assigned name for this resource. Must be unique within the project. The name must be 1-63 characters long, must begin with a letter, end with a letter or digit, and only contain lowercase letters, digits or dashes. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name_server_set" type="string">
  Optionally specifies the NameServerSet for this ManagedZone. A NameServerSet is a set of DNS name servers that all host the same ManagedZones. Most users leave this field unset. If you need to use this field, contact your account team. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name_servers" type="list(string)">
  Delegate your managed_zone to these virtual name servers; defined by the server (output only) Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="peering_config" type="object">
  Configures this zone to forward queries to records in a peered VPC network's own private zones. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kind" type="string">
      The kind of resource or value this is. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_network" type="object">
      A reference to the target VPC network. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="deactivate_time" type="string">
          The time at which the zone was deactivated, in RFC 3339 date-time format. An empty string indicates that the peering connection is active. The producer network can deactivate a zone. The zone is automatically deactivated if the producer network that the zone targeted is deleted. Output only. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of resource or value this is. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_url" type="string">
          The fully qualified URL of the VPC network to forward queries to. This should be formatted like `https://www.googleapis.com/compute/v1/projects/&#123;project&#125;/global/networks/&#123;network&#125;` Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="private_visibility_config" type="object">
  Restricts this private zone's own visibility to specific VPC network(s). Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="gke_clusters" type="list(object)">
      The list of Google Kubernetes Engine clusters that can see this zone. Optional.
      <Expandable title="properties">
        <ResponseField name="gke_cluster_name" type="string">
          The GKE cluster this private zone's own visibility is scoped to. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of resource or value this is. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kind" type="string">
      The kind of resource or value this is. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="networks" type="list(object)">
      The list of VPC networks that can see this zone. Optional.
      <Expandable title="properties">
        <ResponseField name="kind" type="string">
          The kind of resource or value this is. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_url" type="string">
          The VPC network this private zone or policy is visible from. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reverse_lookup_config" type="object">
  Enables automatic PTR (reverse DNS) record generation for this private zone. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kind" type="string">
      The kind of resource or value this is. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_directory_config" type="object">
  Contains information about Service Directory-backed zones. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="kind" type="string">
      The kind of resource or value this is. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="namespace" type="object">
      The DNS namespace this applies to. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="deletion_time" type="string">
          The time that the namespace backing this zone was deleted; an empty string if it still exists. This is in RFC3339 text format. Output only. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of resource or value this is. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="namespace_url" type="string">
          The fully qualified URL of the namespace associated with the zone. Format must be `https://servicedirectory.googleapis.com/v1/projects/&#123;project&#125;/locations/&#123;location&#125;/namespaces/&#123;namespace&#125;` Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="visibility" type="string">
  The zone's visibility: public zones are exposed to the Internet, while private zones are visible only to Virtual Private Cloud resources. Optional; if omitted, computed by Google Cloud.
</ResponseField>
