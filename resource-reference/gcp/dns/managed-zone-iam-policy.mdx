---
title: "google_dns_managed_zone_iam_policy"
description: "Real, generated bindings for `google_dns_managed_zone_iam_policy`."
---

`google_dns_managed_zone_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dns "github.com/ubiquex/ubx-sdk-google/sdk/go/google/dns"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own managed zone iam policy"})

    		ubx.Resource(dns.ManagedZoneIamPolicy, "ci-runner", dns.ManagedZoneIamPolicyConfig{
    			ManagedZone: "example",
    			PolicyData:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedZoneIamPolicy } from "jsr:@ubx/sdk-google/google/dns/managed_zone_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own managed zone iam policy" });

      resource(ManagedZoneIamPolicy, "ci-runner", {
        managedZone: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dns import ManagedZoneIamPolicy, ManagedZoneIamPolicyConfig

    def describe():
        ubx.intent("payments own managed zone iam policy")

        ubx.resource(ManagedZoneIamPolicy, "ci-runner", ManagedZoneIamPolicyConfig(
            managed_zone="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dns_managed_zone_iam_policy, with managed zone
    example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="managed_zone" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
