---
title: "google_dns_managed_zone_iam_policy"
description: "Real, generated bindings for `google_dns_managed_zone_iam_policy`."
---

`google_dns_managed_zone_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google-go/google/dns"
    )

    cfg := dns.ManagedZoneIamPolicyConfig{
        ManagedZone: "example",
        PolicyData: "example",
    }
    ubx.Resource(dns.ManagedZoneIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ManagedZoneIamPolicy } from
      "jsr:@ubx/sdk-google/google/dns/managed_zone_iam_policy";

    resource(ManagedZoneIamPolicy, "example", {
      managedZone: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.dns import (
        ManagedZoneIamPolicy,
        ManagedZoneIamPolicyConfig,
    )

    cfg = ManagedZoneIamPolicyConfig(
        managed_zone="example",
        policy_data="example",
    )
    ubx.resource(ManagedZoneIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_dns_managed_zone_iam_policy with managed_zone,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="managed_zone" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
