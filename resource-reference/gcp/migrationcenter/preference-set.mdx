---
title: "google_migrationcenter_preference_set"
description: "`google_migrationcenter_preference_set` defines the sizing preferences (target region, machine series..."
---

`google_migrationcenter_preference_set` defines the sizing preferences (target region, machine series, commitment terms) Migration Center applies when generating a migration recommendation and cost estimate for a set of assets.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_migrationcenter --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_migrationcenter/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	migrationcenter "github.com/ubiquex/ubx-sdk-google/sdk/go/google_migrationcenter/migrationcenter"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own preference set"})

    		ubx.Resource(migrationcenter.MigrationcenterPreferenceSet, "example", migrationcenter.MigrationcenterPreferenceSetConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_migrationcenter --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MigrationcenterPreferenceSet } from "./local-sdk/google_migrationcenter/sdk/typescript/google_migrationcenter/migrationcenter/migrationcenter_preference_set.ts";

    export default stack("example", () => {
      intent({ summary: "example own preference set" });

      resource(MigrationcenterPreferenceSet, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_migrationcenter --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_migrationcenter/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_migrationcenter.migrationcenter import MigrationcenterPreferenceSet, MigrationcenterPreferenceSetConfig

    def describe():
        ubx.intent("example own preference set")

        ubx.resource(MigrationcenterPreferenceSet, "example", MigrationcenterPreferenceSetConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_migrationcenter_preference_set called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the preference set was created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the preference set. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  User-friendly display name. Maximum length is 63 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Name of the preference set. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The timestamp when the preference set was last updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="virtual_machine_preferences" type="object">
  VirtualMachinePreferences enables you to create sets of assumptions, for example, a geographical location and pricing track, for your migrated virtual machines. The set of preferences influence recommendations for migrating virtual machine assets. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="commitment_plan" type="string">
      Commitment plan to consider when calculating costs for virtual machine insights and recommendations. If you are unsure which value to set, a 3 year commitment plan is often a good value to start with. Optional.
    </ResponseField>
    <ResponseField name="compute_engine_preferences" type="object">
      The user preferences relating to Compute Engine target platform. Optional.
      <Expandable title="properties">
        <ResponseField name="license_type" type="string">
          License type to consider when calculating costs for virtual machine insights and recommendations. If unspecified, costs are calculated based on the default licensing plan. Optional.
        </ResponseField>
        <ResponseField name="machine_preferences" type="object">
          The type of machines to consider when calculating virtual machine migration insights and recommendations. Not all machine types are available in all zones and regions. Optional.
          <Expandable title="properties">
            <ResponseField name="allowed_machine_series" type="list(object)">
              Compute Engine machine series to consider for insights and recommendations. If empty, no restriction is applied on the machine series. Optional.
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  A machine-readable code identifying this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="persistent_disk_type" type="string">
          Persistent disk type to use. If unspecified (default), all types are considered, based on available usage data. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="region_preferences" type="object">
      The user preferences relating to target regions. Optional.
      <Expandable title="properties">
        <ResponseField name="preferred_regions" type="list(string)">
          A list of preferred regions, ordered by the most preferred region first. Set only valid Google Cloud region names. See https://cloud.google.com/compute/docs/regions-zones for available regions. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sizing_optimization_strategy" type="string">
      Sizing optimization strategy specifies the preferred strategy used when extrapolating usage data to calculate insights and recommendations for a virtual machine. If you are unsure which value to set, a moderate sizing optimization strategy is often a good value to start with. Optional.
    </ResponseField>
    <ResponseField name="sole_tenancy_preferences" type="object">
      Preferences concerning Sole Tenancy nodes and VMs. Optional.
      <Expandable title="properties">
        <ResponseField name="commitment_plan" type="string">
          Commitment plan to consider when calculating costs for virtual machine insights and recommendations. If you are unsure which value to set, a 3 year commitment plan is often a good value to start with. Optional.
        </ResponseField>
        <ResponseField name="cpu_overcommit_ratio" type="number">
          CPU overcommit ratio. Acceptable values are between 1.0 and 2.0 inclusive. Optional.
        </ResponseField>
        <ResponseField name="host_maintenance_policy" type="string">
          Sole Tenancy nodes maintenance policy. Optional.
        </ResponseField>
        <ResponseField name="node_types" type="list(object)">
          A list of sole tenant node types. An empty list means that all possible node types will be considered. Optional.
          <Expandable title="properties">
            <ResponseField name="node_name" type="string">
              The name of this node. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target_product" type="string">
      Target product for assets using this preference set. Specify either target product or business goal, but not both. Optional.
    </ResponseField>
    <ResponseField name="vmware_engine_preferences" type="object">
      The user preferences relating to Google Cloud VMware Engine target platform. Optional.
      <Expandable title="properties">
        <ResponseField name="commitment_plan" type="string">
          Commitment plan to consider when calculating costs for virtual machine insights and recommendations. If you are unsure which value to set, a 3 year commitment plan is often a good value to start with. Optional.
        </ResponseField>
        <ResponseField name="cpu_overcommit_ratio" type="number">
          CPU overcommit ratio. Acceptable values are between 1.0 and 8.0, with 0.1 increment. Optional.
        </ResponseField>
        <ResponseField name="memory_overcommit_ratio" type="number">
          Memory overcommit ratio. Acceptable values are 1.0, 1.25, 1.5, 1.75 and 2.0. Optional.
        </ResponseField>
        <ResponseField name="storage_deduplication_compression_ratio" type="number">
          The Deduplication and Compression ratio is based on the logical (Used Before) space required to store data before applying deduplication and compression, in relation to the physical (Used After) space required after applying deduplication and compression. Specifically, the ratio is the Used Before space divided by the Used After space. For example, if the Used Before space is 3 GB, but the physical Used After space is 1 GB, the deduplication and compression ratio is 3x. Acceptable values are between 1.0 and 4.0. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
