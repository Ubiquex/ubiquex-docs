---
title: "google_migrationcenter_discovery_client"
description: "`google_migrationcenter_discovery_client` registers a Migration Center discovery agent instance deployed in a source environment..."
---

`google_migrationcenter_discovery_client` registers a Migration Center discovery agent instance deployed in a source environment, that scans and reports on-premises or other-cloud asset(s) back to Migration Center.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_migrationcenter --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_migrationcenter/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	migrationcenter "github.com/ubiquex/ubx-sdk-google/sdk/go/google_migrationcenter/migrationcenter"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own discovery client"})

    		ubx.Resource(migrationcenter.MigrationcenterDiscoveryClient, "ci-runner", migrationcenter.MigrationcenterDiscoveryClientConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_migrationcenter --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MigrationcenterDiscoveryClient } from "./local-sdk/google_migrationcenter/sdk/typescript/google_migrationcenter/migrationcenter/migrationcenter_discovery_client.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own discovery client" });

      resource(MigrationcenterDiscoveryClient, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_migrationcenter --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_migrationcenter/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_migrationcenter.migrationcenter import MigrationcenterDiscoveryClient, MigrationcenterDiscoveryClientConfig

    def describe():
        ubx.intent("payments own discovery client")

        ubx.resource(MigrationcenterDiscoveryClient, "ci-runner", MigrationcenterDiscoveryClientConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_migrationcenter_discovery_client called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="create_time" type="string">
  Output only. Time when the discovery client was first created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. Free text description. Maximum length is 1000 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional. Free text display name. Maximum length is 63 characters. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="errors" type="list(object)">
  Output only. Errors affecting client functionality. Optional; if omitted, computed by Google Cloud.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
      A machine-readable code identifying this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="details" type="list(map(any))">
      Additional detail about this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="message" type="string">
      A human-readable description of this result. Optional; if omitted, computed by Google Cloud. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="expire_time" type="string">
  Optional. Client expiration time in UTC. If specified, the backend will not accept new frames after this time. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="heartbeat_time" type="string">
  Output only. Last heartbeat time. Healthy clients are expected to send heartbeats regularly (normally every few minutes). Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional. Labels as key value pairs. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. Identifier. Full name of this discovery client. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="service_account" type="string">
  Required. Service account used by the discovery client for various operation. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="signals_endpoint" type="string">
  Output only. This field is intended for internal use. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="source" type="string">
  Required. Immutable. Full name of the source object associated with this discovery client. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. Current state of the discovery client. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="ttl" type="string">
  Optional. Input only. Client time-to-live. If specified, the backend will not accept new frames after this time. This field is input only. The derived expiration time is provided as output through the `expire_time` field. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Time when the discovery client was last updated. This value is not updated by heartbeats, to view the last heartbeat time please refer to the `heartbeat_time` field. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="version" type="string">
  Output only. Client version, as reported in recent heartbeat. Optional; if omitted, computed by Google Cloud.
</ResponseField>
