---
title: "google_secure_source_manager_repository_iam_policy"
description: "Real, generated bindings for `google_secure_source_manager_repository_iam_policy`."
---

`google_secure_source_manager_repository_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/secure"
    )

    cfg := secure.SourceManagerRepositoryIamPolicyConfig{
        PolicyData: "example",
        RepositoryId: "example",
    }
    ubx.Resource(secure.SourceManagerRepositoryIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SourceManagerRepositoryIamPolicy } from
      "jsr:@ubx/sdk-google/google/secure/source_manager_repository_iam_policy";

    resource(SourceManagerRepositoryIamPolicy, "example", {
      policyData: "example",
      repositoryId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.secure import (
        SourceManagerRepositoryIamPolicy,
        SourceManagerRepositoryIamPolicyConfig,
    )

    cfg = SourceManagerRepositoryIamPolicyConfig(
        policy_data="example",
        repository_id="example",
    )
    ubx.resource(SourceManagerRepositoryIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_secure_source_manager_repository_iam_policy with
policy_data, repository_id.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="repository_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
