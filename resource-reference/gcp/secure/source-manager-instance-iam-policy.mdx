---
title: "google_secure_source_manager_instance_iam_policy"
description: "Real, generated bindings for `google_secure_source_manager_instance_iam_policy`."
---

`google_secure_source_manager_instance_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	secure "github.com/ubiquex/ubx-sdk-aws/sdk/go/google/secure"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own source manager instance iam policy"})

    		ubx.Resource(secure.SourceManagerInstanceIamPolicy, "ci-runner", secure.SourceManagerInstanceIamPolicyConfig{
    			InstanceId: "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SourceManagerInstanceIamPolicy } from "jsr:@ubx/sdk-google/google/secure/source_manager_instance_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own source manager instance iam policy" });

      resource(SourceManagerInstanceIamPolicy, "ci-runner", {
        instanceId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.secure import SourceManagerInstanceIamPolicy, SourceManagerInstanceIamPolicyConfig

    def describe():
        ubx.intent("payments own source manager instance iam policy")

        ubx.resource(SourceManagerInstanceIamPolicy, "ci-runner", SourceManagerInstanceIamPolicyConfig(
            instance_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_secure_source_manager_instance_iam_policy, with
    instance id example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
