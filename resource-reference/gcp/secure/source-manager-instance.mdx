---
title: "google_secure_source_manager_instance"
description: "Real, generated bindings for `google_secure_source_manager_instance`."
---

`google_secure_source_manager_instance` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	secure "github.com/ubiquex/ubx-sdk-google/sdk/go/google/secure"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own source manager instance"})

    		ubx.Resource(secure.SourceManagerInstance, "ci-runner", secure.SourceManagerInstanceConfig{
    			InstanceId: "example",
    			Location:   "example",
    			KmsKey:     "example",
    			Labels:     map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SourceManagerInstance } from "jsr:@ubx/sdk-google/google/secure/source_manager_instance";

    export default stack("payments", () => {
      intent({ summary: "payments own source manager instance" });

      resource(SourceManagerInstance, "ci-runner", {
        instanceId: "example",
        location: "example",
        kmsKey: "example",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.secure import SourceManagerInstance, SourceManagerInstanceConfig

    def describe():
        ubx.intent("payments own source manager instance")

        ubx.resource(SourceManagerInstance, "ci-runner", SourceManagerInstanceConfig(
            instance_id="example",
            location="example",
            kms_key="example",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_secure_source_manager_instance, with instance id
    example, location example, kms key example, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "PREVENT". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The name for the Instance. Required.
</ResponseField>

<ResponseField name="kms_key" type="string">
  Customer-managed encryption key name, in the format projects/*/locations/*/keyRings/*/cryptoKeys/*. Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the Instance. Required.
</ResponseField>

<ResponseField name="private_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ca_pool" type="string">
    </ResponseField>
    <ResponseField name="custom_host_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="api" type="string" required>
        </ResponseField>
        <ResponseField name="git_http" type="string" required>
        </ResponseField>
        <ResponseField name="git_ssh" type="string" required>
        </ResponseField>
        <ResponseField name="html" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="http_service_attachment" type="string">
    </ResponseField>
    <ResponseField name="is_private" type="bool" required>
    </ResponseField>
    <ResponseField name="psc_allowed_projects" type="list(string)">
    </ResponseField>
    <ResponseField name="ssh_service_attachment" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workforce_identity_federation_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Time the Instance was created in UTC. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "PREVENT". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="host_config" type="list(object)">
  A list of hostnames for this instance. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="api" type="string">
    </ResponseField>
    <ResponseField name="git_http" type="string">
    </ResponseField>
    <ResponseField name="git_ssh" type="string">
    </ResponseField>
    <ResponseField name="html" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name for the Instance. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="private_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ca_pool" type="string">
    </ResponseField>
    <ResponseField name="custom_host_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="api" type="string" required>
        </ResponseField>
        <ResponseField name="git_http" type="string" required>
        </ResponseField>
        <ResponseField name="git_ssh" type="string" required>
        </ResponseField>
        <ResponseField name="html" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="http_service_attachment" type="string">
    </ResponseField>
    <ResponseField name="is_private" type="bool" required>
    </ResponseField>
    <ResponseField name="psc_allowed_projects" type="list(string)">
    </ResponseField>
    <ResponseField name="ssh_service_attachment" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the Instance. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state_note" type="string">
  Provides information about the current instance state. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Time the Instance was updated in UTC. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="workforce_identity_federation_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>
