---
title: "google_secure_source_manager_hook"
description: "Real, generated bindings for `google_secure_source_manager_hook`."
---

`google_secure_source_manager_hook` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	secure "github.com/ubiquex/ubx-sdk-google/sdk/go/google/secure"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own source manager hook"})

    		ubx.Resource(secure.SourceManagerHook, "ci-runner", secure.SourceManagerHookConfig{
    			HookId:               "example",
    			Location:             "example",
    			RepositoryId:         "example",
    			TargetUri:            "example",
    			Disabled:             true,
    			SensitiveQueryString: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SourceManagerHook } from "jsr:@ubx/sdk-google/google/secure/source_manager_hook";

    export default stack("payments", () => {
      intent({ summary: "payments own source manager hook" });

      resource(SourceManagerHook, "ci-runner", {
        hookId: "example",
        location: "example",
        repositoryId: "example",
        targetUri: "example",
        disabled: true,
        sensitiveQueryString: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.secure import SourceManagerHook, SourceManagerHookConfig

    def describe():
        ubx.intent("payments own source manager hook")

        ubx.resource(SourceManagerHook, "ci-runner", SourceManagerHookConfig(
            hook_id="example",
            location="example",
            repository_id="example",
            target_uri="example",
            disabled=True,
            sensitive_query_string="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_secure_source_manager_hook, with hook id example,
    location example, repository id example, target uri example,
    disabled true, sensitive query string example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Determines if the hook disabled or not. Set to true to stop sending traffic. Optional.
</ResponseField>

<ResponseField name="events" type="list(string)">
  The events that trigger hook on. Possible values: ["PUSH", "PULL_REQUEST"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="hook_id" type="string" required>
  The ID for the Hook. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the Repository. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="push_option" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="branch_filter" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="repository_id" type="string" required>
  The ID for the Repository. Required.
</ResponseField>

<ResponseField name="sensitive_query_string" type="string">
  The sensitive query string to be appended to the target URI. Optional.
</ResponseField>

<ResponseField name="target_uri" type="string" required>
  The target URI to which the payloads will be delivered. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Create timestamp. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="events" type="list(string)">
  The events that trigger hook on. Possible values: ["PUSH", "PULL_REQUEST"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  A unique identifier for a Hook. The name should be of the format: 'projects/&#123;project&#125;/locations/&#123;location_id&#125;/repositories/&#123;repository_id&#125;/hooks/&#123;hook_id&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="push_option" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="branch_filter" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Unique identifier of the hook. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Update timestamp. Computed by GCP after `ship`.
</ResponseField>
