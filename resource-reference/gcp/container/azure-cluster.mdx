---
title: "google_container_azure_cluster"
description: "Real, generated bindings for `google_container_azure_cluster`."
---

`google_container_azure_cluster` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	container "github.com/ubiquex/ubx-sdk-google/sdk/go/google/container"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own azure cluster"})

    		ubx.Resource(container.AzureCluster, "ci-runner", container.AzureClusterConfig{
    			AzureRegion:     "example",
    			Location:        "example",
    			Name:            "ci-runner",
    			ResourceGroupId: "example",
    			Annotations:     map[string]string{"managed-by": "ubx"},
    			Client:          "example",
    			Description:     "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AzureCluster } from "jsr:@ubx/sdk-google/google/container/azure_cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own azure cluster" });

      resource(AzureCluster, "ci-runner", {
        azureRegion: "example",
        location: "example",
        name: "ci-runner",
        resourceGroupId: "example",
        annotations: { "managed-by": "ubx" },
        client: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.container import AzureCluster, AzureClusterConfig

    def describe():
        ubx.intent("payments own azure cluster")

        ubx.resource(AzureCluster, "ci-runner", AzureClusterConfig(
            azure_region="example",
            location="example",
            name="ci-runner",
            resource_group_id="example",
            annotations={"managed-by": "ubx"},
            client="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_container_azure_cluster called "ci-runner", with
    azure region example, location example, resource group id
    example, annotations a set value, client example, description
    Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="annotations" type="map(string)">
  Optional. Annotations on the cluster. This field has the same restrictions as Kubernetes annotations. The total size of all keys and values combined is limited to 256k. Keys can have 2 segments: prefix (optional) and name (required), separated by a slash (/). Prefix must be a DNS subdomain. Name must be 63 characters or less, begin and end with alphanumerics, with dashes (-), underscores (_), dots (.), and alphanumerics between. **Note**: This field is non-authoritative, and will only manage the annotations present in your configuration. Please refer to the field `effective_annotations` for all of the annotations present on the resource. Optional.
</ResponseField>

<ResponseField name="authorization" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="admin_groups" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="group" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="admin_users" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="username" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="azure_region" type="string" required>
  The Azure region where the cluster runs. Each Google Cloud region supports a subset of nearby Azure regions. You can call to list all supported Azure regions within a given Google Cloud region. Required.
</ResponseField>

<ResponseField name="azure_services_authentication" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="application_id" type="string" required>
    </ResponseField>
    <ResponseField name="tenant_id" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="client" type="string">
  Name of the AzureClient. The `AzureClient` resource must reside on the same GCP project and region as the `AzureCluster`. `AzureClient` names are formatted as `projects/&lt;project-number&gt;/locations/&lt;region&gt;/azureClients/&lt;client-id&gt;`. See Resource Names (https:cloud.google.com/apis/design/resource_names) for more details on Google Cloud resource names. Optional.
</ResponseField>

<ResponseField name="control_plane" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="database_encryption" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="key_id" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="main_volume" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="size_gib" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="proxy_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="resource_group_id" type="string" required>
        </ResponseField>
        <ResponseField name="secret_id" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="replica_placements" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="azure_availability_zone" type="string" required>
        </ResponseField>
        <ResponseField name="subnet_id" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="root_volume" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="size_gib" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ssh_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="authorized_key" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="subnet_id" type="string" required>
    </ResponseField>
    <ResponseField name="tags" type="map(string)">
    </ResponseField>
    <ResponseField name="version" type="string" required>
    </ResponseField>
    <ResponseField name="vm_size" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Optional. A human readable description of this cluster. Cannot be longer than 255 UTF-8 encoded bytes. Optional.
</ResponseField>

<ResponseField name="fleet" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="membership" type="string">
    </ResponseField>
    <ResponseField name="project" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the resource Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of this resource. Required.
</ResponseField>

<ResponseField name="networking" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="pod_address_cidr_blocks" type="list(string)" required>
    </ResponseField>
    <ResponseField name="service_address_cidr_blocks" type="list(string)" required>
    </ResponseField>
    <ResponseField name="virtual_network_id" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  The project for the resource Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_group_id" type="string" required>
  The ARM ID of the resource group where the cluster resources are deployed. For example: `/subscriptions/*/resourceGroups/*` Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="authorization" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="admin_groups" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="group" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="admin_users" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="username" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="azure_services_authentication" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="application_id" type="string" required>
    </ResponseField>
    <ResponseField name="tenant_id" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="control_plane" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="database_encryption" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="key_id" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="main_volume" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="size_gib" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="proxy_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="resource_group_id" type="string" required>
        </ResponseField>
        <ResponseField name="secret_id" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="replica_placements" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="azure_availability_zone" type="string" required>
        </ResponseField>
        <ResponseField name="subnet_id" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="root_volume" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="size_gib" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ssh_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="authorized_key" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="subnet_id" type="string" required>
    </ResponseField>
    <ResponseField name="tags" type="map(string)">
    </ResponseField>
    <ResponseField name="version" type="string" required>
    </ResponseField>
    <ResponseField name="vm_size" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Output only. The time at which this cluster was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_annotations" type="map(string)">
  All of annotations (key/value pairs) present on the resource in GCP, including the annotations configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="endpoint" type="string">
  Output only. The endpoint of the cluster's API server. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Allows clients to perform consistent read-modify-writes through optimistic concurrency control. May be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="fleet" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="membership" type="string">
    </ResponseField>
    <ResponseField name="project" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="networking" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="pod_address_cidr_blocks" type="list(string)" required>
    </ResponseField>
    <ResponseField name="service_address_cidr_blocks" type="list(string)" required>
    </ResponseField>
    <ResponseField name="virtual_network_id" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  The project for the resource Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  Output only. If set, there are currently changes in flight to the cluster. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Output only. The current state of the cluster. Possible values: STATE_UNSPECIFIED, PROVISIONING, RUNNING, RECONCILING, STOPPING, ERROR, DEGRADED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Output only. A globally unique identifier for the cluster. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. The time at which this cluster was last updated. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="workload_identity_config" type="list(object)">
  Output only. Workload Identity settings. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="identity_provider" type="string">
    </ResponseField>
    <ResponseField name="issuer_uri" type="string">
    </ResponseField>
    <ResponseField name="workload_pool" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
