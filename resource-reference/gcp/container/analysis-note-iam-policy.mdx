---
title: "google_container_analysis_note_iam_policy"
description: "Real, generated bindings for `google_container_analysis_note_iam_policy`."
---

`google_container_analysis_note_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-google/sdk/go/google/container"
    )

    cfg := container.AnalysisNoteIamPolicyConfig{
        Note: "example",
        PolicyData: "example",
    }
    ubx.Resource(container.AnalysisNoteIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AnalysisNoteIamPolicy } from
      "jsr:@ubx/sdk-google/google/container/analysis_note_iam_policy";

    resource(AnalysisNoteIamPolicy, "example", {
      note: "example",
      policyData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.google.container import (
        AnalysisNoteIamPolicy,
        AnalysisNoteIamPolicyConfig,
    )

    cfg = AnalysisNoteIamPolicyConfig(
        note="example",
        policy_data="example",
    )
    ubx.resource(AnalysisNoteIamPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision google_container_analysis_note_iam_policy with note,
policy_data.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="note" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
