---
title: "google_firebaseappcheck_v1beta_debug_token"
description: "`google_firebaseappcheck_v1beta_debug_token` is a special credential letting a development or test build bypass Firebase App Check's own attestation..."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_firebaseappcheck_debug_token`.
</Warning>

`google_firebaseappcheck_v1beta_debug_token` is a special credential letting a development or test build bypass Firebase App Check's own attestation requirement, for local testing.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_firebaseappcheck_v1beta --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_firebaseappcheck_v1beta/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseappcheck "github.com/ubiquex/ubx-sdk-google/sdk/go/google_firebaseappcheck_v1beta/firebaseappcheck"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta debug token"})

    		ubx.Resource(firebaseappcheck.V1betaFirebaseappcheckV1betaDebugToken, "example", firebaseappcheck.V1betaFirebaseappcheckV1betaDebugTokenConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_firebaseappcheck_v1beta --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaFirebaseappcheckV1betaDebugToken } from "./local-sdk/google_firebaseappcheck_v1beta/sdk/typescript/google_firebaseappcheck_v1beta/firebaseappcheck/v1beta_firebaseappcheck_v1beta_debug_token.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta debug token" });

      resource(V1betaFirebaseappcheckV1betaDebugToken, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_firebaseappcheck_v1beta --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_firebaseappcheck_v1beta/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_firebaseappcheck_v1beta.firebaseappcheck import V1betaFirebaseappcheckV1betaDebugToken, V1betaFirebaseappcheckV1betaDebugTokenConfig

    def describe():
        ubx.intent("example own v1beta debug token")

        ubx.resource(V1betaFirebaseappcheckV1betaDebugToken, "example", V1betaFirebaseappcheckV1betaDebugTokenConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebaseappcheck_v1beta_debug_token called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="display_name" type="string">
  Required. A human readable display name used to identify this debug token. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  Optional. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. This etag is strongly validated as defined by RFC 7232. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. The relative resource name of the debug token, in the format: ``` projects/&#123;project_number&#125;/apps/&#123;app_id&#125;/debugTokens/&#123;debug_token_id&#125; ``` Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="token" type="string">
  Required. Input only. Immutable. The secret token itself. Must be provided during creation, and must be a UUID4, case insensitive. This field is immutable once set, and cannot be provided during a UpdateDebugToken request. You can, however, delete this debug token using DeleteDebugToken to revoke it. For security reasons, this field will never be populated in any response. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when this debug token was most recently updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
