---
title: "google_firebaseappcheck_v1beta_resource_policy"
description: "`google_firebaseappcheck_v1beta_resource_policy` configures Firebase App Check enforcement (required or monitoring-only) for a specific backend..."
---

<Warning>
  This is Google Cloud's own **v1beta** release channel (beta). Google may change or remove beta endpoints as a feature moves toward general availability -- confirm this endpoint is still live before depending on it in anything long-lived. The stable, generally-available equivalent is `google_firebaseappcheck_resource_policy`.
</Warning>

`google_firebaseappcheck_v1beta_resource_policy` configures Firebase App Check enforcement (required or monitoring-only) for a specific backend resource, e.g. a Cloud Functions function or API.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only google_firebaseappcheck_v1beta --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-google/sdk/go => ./local-sdk/google_firebaseappcheck_v1beta/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebaseappcheck "github.com/ubiquex/ubx-sdk-google/sdk/go/google_firebaseappcheck_v1beta/firebaseappcheck"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1beta resource policy"})

    		ubx.Resource(firebaseappcheck.V1betaFirebaseappcheckV1betaResourcePolicy, "example", firebaseappcheck.V1betaFirebaseappcheckV1betaResourcePolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only google_firebaseappcheck_v1beta --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1betaFirebaseappcheckV1betaResourcePolicy } from "./local-sdk/google_firebaseappcheck_v1beta/sdk/typescript/google_firebaseappcheck_v1beta/firebaseappcheck/v1beta_firebaseappcheck_v1beta_resource_policy.ts";

    export default stack("example", () => {
      intent({ summary: "example own v1beta resource policy" });

      resource(V1betaFirebaseappcheckV1betaResourcePolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only google_firebaseappcheck_v1beta --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/google_firebaseappcheck_v1beta/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.google_firebaseappcheck_v1beta.firebaseappcheck import V1betaFirebaseappcheckV1betaResourcePolicy, V1betaFirebaseappcheckV1betaResourcePolicyConfig

    def describe():
        ubx.intent("example own v1beta resource policy")

        ubx.resource(V1betaFirebaseappcheckV1betaResourcePolicy, "example", V1betaFirebaseappcheckV1betaResourcePolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebaseappcheck_v1beta_resource_policy called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="enforcement_mode" type="string">
  Required. The baseline protection EnforcementMode for this resource. This will override the service-level baseline protection EnforcementMode. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="etag" type="string">
  This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. This etag is strongly validated as defined by RFC 7232. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="name" type="string">
  Required. Identifier. The relative name of the resource policy object, in the format: ``` projects/&#123;project_number&#125;/services/&#123;service_id&#125;/resourcePolicies/&#123;resource_policy_id&#125; ``` Note that the `service_id` element must be a supported service ID. Currently, the following service IDs are supported: * `oauth2.googleapis.com` (Google Identity for iOS) `resource_policy_id` is a system-generated UID. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="target_resource" type="string">
  Required. Service specific name of the resource object to which this policy applies, in the format: * **iOS OAuth clients** (Google Identity for iOS): `//oauth2.googleapis.com/projects/&#123;project_number&#125;/oauthClients/&#123;oauth_client_id&#125;` Note that the resource must belong to the service specified in the `name` and be from the same project as this policy, but the resource is allowed to be missing at the time of creation of this policy; in that case, we make a best-effort attempt at respecting this policy, but it may not have any effect until the resource is fully created. Optional; if omitted, computed by Google Cloud.
</ResponseField>

<ResponseField name="update_time" type="string">
  Output only. Timestamp when this resource policy configuration object was most recently updated. Optional; if omitted, computed by Google Cloud.
</ResponseField>
