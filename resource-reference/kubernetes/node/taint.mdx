---
title: "kubernetes_node_taint"
description: "Real, generated bindings for `kubernetes_node_taint`."
---

`kubernetes_node_taint` -- real, typed bindings generated directly from the
`hashicorp/kubernetes` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	node "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/node"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own taint"})

    		ubx.Resource(node.Taint, "ci-runner", node.TaintConfig{
    			FieldManager: "example",
    			Force:        true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Taint } from "jsr:@ubx/sdk-kubernetes/kubernetes/node/taint";

    export default stack("payments", () => {
      intent({ summary: "payments own taint" });

      resource(Taint, "ci-runner", {
        fieldManager: "example",
        force: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.node import Taint, TaintConfig

    def describe():
        ubx.intent("payments own taint")

        ubx.resource(Taint, "ci-runner", TaintConfig(
            field_manager="example",
            force=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create kubernetes_node_taint, with field manager example, force
    true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="field_manager" type="string">
  Optional.
</ResponseField>

<ResponseField name="force" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="taint" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="effect" type="string" required>
    </ResponseField>
    <ResponseField name="key" type="string" required>
    </ResponseField>
    <ResponseField name="value" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="taint" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="effect" type="string" required>
    </ResponseField>
    <ResponseField name="key" type="string" required>
    </ResponseField>
    <ResponseField name="value" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>
