---
title: "kubernetes_secret_v1_data"
description: "Real, generated bindings for `kubernetes_secret_v1_data`."
---

`kubernetes_secret_v1_data` -- real, typed bindings generated directly from the
`hashicorp/kubernetes` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	secret "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/secret"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own v1 data"})

    		ubx.Resource(secret.V1Data, "ci-runner", secret.V1DataConfig{
    			Data:         map[string]string{"managed-by": "ubx"},
    			FieldManager: "example",
    			Force:        true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1Data } from "jsr:@ubx/sdk-kubernetes/kubernetes/secret/v1_data";

    export default stack("payments", () => {
      intent({ summary: "payments own v1 data" });

      resource(V1Data, "ci-runner", {
        data: { "managed-by": "ubx" },
        fieldManager: "example",
        force: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.secret import V1Data, V1DataConfig

    def describe():
        ubx.intent("payments own v1 data")

        ubx.resource(V1Data, "ci-runner", V1DataConfig(
            data={"managed-by": "ubx"},
            field_manager="example",
            force=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create kubernetes_secret_v1_data, with data a set value, field
    manager example, force true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="field_manager" type="string">
  Optional.
</ResponseField>

<ResponseField name="force" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="namespace" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="namespace" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
