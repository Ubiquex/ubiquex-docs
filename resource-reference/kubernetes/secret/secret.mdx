---
title: "kubernetes_secret"
description: "Real, generated bindings for `kubernetes_secret`."
---

`kubernetes_secret` -- real, typed bindings generated directly from the
`hashicorp/kubernetes` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	secret "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/secret"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own secret"})

    		ubx.Resource(secret.Secret, "ci-runner", secret.SecretConfig{
    			BinaryData:                 map[string]string{"managed-by": "ubx"},
    			BinaryDataWo:               map[string]string{"managed-by": "ubx"},
    			BinaryDataWoRevision:       1,
    			DataWo:                     map[string]string{"managed-by": "ubx"},
    			DataWoRevision:             1,
    			Immutable:                  true,
    			Type:                       "example",
    			WaitForServiceAccountToken: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Secret } from "jsr:@ubx/sdk-kubernetes/kubernetes/secret/secret";

    export default stack("payments", () => {
      intent({ summary: "payments own secret" });

      resource(Secret, "ci-runner", {
        binaryData: { "managed-by": "ubx" },
        binaryDataWo: { "managed-by": "ubx" },
        binaryDataWoRevision: 1,
        dataWo: { "managed-by": "ubx" },
        dataWoRevision: 1,
        immutable: true,
        type: "example",
        waitForServiceAccountToken: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.secret import Secret, SecretConfig

    def describe():
        ubx.intent("payments own secret")

        ubx.resource(Secret, "ci-runner", SecretConfig(
            binary_data={"managed-by": "ubx"},
            binary_data_wo={"managed-by": "ubx"},
            binary_data_wo_revision=1,
            data_wo={"managed-by": "ubx"},
            data_wo_revision=1,
            immutable=True,
            type="example",
            wait_for_service_account_token=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create kubernetes_secret, with binary data a set value, binary
    data wo a set value, binary data wo revision 1, data wo a set
    value, data wo revision 1, immutable true, type example, wait for
    service account token true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="binary_data" type="map(string)">
  A map of the secret data in base64 encoding. Use this for binary data. Optional.
</ResponseField>

<ResponseField name="binary_data_wo" type="map(string)">
  A write-only map of the secret data in base64 encoding. Use this for binary data. Optional.
</ResponseField>

<ResponseField name="binary_data_wo_revision" type="number">
  The current revision of the write-only "binary_data_wo" attribute. Incrementing this integer value will cause Terraform to update the write-only value. Optional.
</ResponseField>

<ResponseField name="data" type="map(string)">
  A map of the secret data. Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="data_wo" type="map(string)">
  A map write-only of the secret data. Optional.
</ResponseField>

<ResponseField name="data_wo_revision" type="number">
  The current revision of the write-only "data_wo" attribute. Incrementing this integer value will cause Terraform to update the write-only value. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="immutable" type="bool">
  Ensures that data stored in the Secret cannot be updated (only object metadata can be modified). Optional.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
    </ResponseField>
    <ResponseField name="generate_name" type="string">
    </ResponseField>
    <ResponseField name="generation" type="number">
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="namespace" type="string">
    </ResponseField>
    <ResponseField name="resource_version" type="string">
    </ResponseField>
    <ResponseField name="uid" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Type of secret Optional.
</ResponseField>

<ResponseField name="wait_for_service_account_token" type="bool">
  Terraform will wait for the service account token to be created. Optional.
</ResponseField>

## Output properties

<ResponseField name="data" type="map(string)">
  A map of the secret data. Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
    </ResponseField>
    <ResponseField name="generate_name" type="string">
    </ResponseField>
    <ResponseField name="generation" type="number">
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="namespace" type="string">
    </ResponseField>
    <ResponseField name="resource_version" type="string">
    </ResponseField>
    <ResponseField name="uid" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
