---
title: "kubernetes_storage_class_v1"
description: "Real, generated bindings for `kubernetes_storage_class_v1`."
---

`kubernetes_storage_class_v1` -- real, typed bindings generated directly from the
`hashicorp/kubernetes` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	storage "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/storage"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own class v1"})

    		ubx.Resource(storage.ClassV1, "ci-runner", storage.ClassV1Config{
    			StorageProvisioner:   "example",
    			AllowVolumeExpansion: true,
    			MountOptions:         []string{"example"},
    			Parameters:           map[string]string{"managed-by": "ubx"},
    			ReclaimPolicy:        "example",
    			VolumeBindingMode:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClassV1 } from "jsr:@ubx/sdk-kubernetes/kubernetes/storage/class_v1";

    export default stack("payments", () => {
      intent({ summary: "payments own class v1" });

      resource(ClassV1, "ci-runner", {
        storageProvisioner: "example",
        allowVolumeExpansion: true,
        mountOptions: ["example"],
        parameters: { "managed-by": "ubx" },
        reclaimPolicy: "example",
        volumeBindingMode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.storage import ClassV1, ClassV1Config

    def describe():
        ubx.intent("payments own class v1")

        ubx.resource(ClassV1, "ci-runner", ClassV1Config(
            storage_provisioner="example",
            allow_volume_expansion=True,
            mount_options=["example"],
            parameters={"managed-by": "ubx"},
            reclaim_policy="example",
            volume_binding_mode="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create kubernetes_storage_class_v1, with storage provisioner
    example, allow volume expansion true, mount options a set value,
    parameters a set value, reclaim policy example, volume binding
    mode example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allow_volume_expansion" type="bool">
  Indicates whether the storage class allow volume expand Optional.
</ResponseField>

<ResponseField name="allowed_topologies" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="match_label_expressions" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="key" type="string">
        </ResponseField>
        <ResponseField name="values" type="set(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
    </ResponseField>
    <ResponseField name="generate_name" type="string">
    </ResponseField>
    <ResponseField name="generation" type="number">
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="resource_version" type="string">
    </ResponseField>
    <ResponseField name="uid" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="mount_options" type="set(string)">
  Persistent Volumes that are dynamically created by a storage class will have the mount options specified Optional.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  The parameters for the provisioner that should create volumes of this storage class Optional.
</ResponseField>

<ResponseField name="reclaim_policy" type="string">
  Indicates the type of the reclaim policy Optional.
</ResponseField>

<ResponseField name="storage_provisioner" type="string" required>
  Indicates the type of the provisioner Required.
</ResponseField>

<ResponseField name="volume_binding_mode" type="string">
  Indicates when volume binding and dynamic provisioning should occur Optional.
</ResponseField>

## Output properties

<ResponseField name="allowed_topologies" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="match_label_expressions" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="key" type="string">
        </ResponseField>
        <ResponseField name="values" type="set(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
    </ResponseField>
    <ResponseField name="generate_name" type="string">
    </ResponseField>
    <ResponseField name="generation" type="number">
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="resource_version" type="string">
    </ResponseField>
    <ResponseField name="uid" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
