---
title: "kubernetes_admissionregistration_validating_admission_policy"
description: "`kubernetes_admissionregistration_validating_admission_policy` defines a reusable validation rule in CEL..."
---

`kubernetes_admissionregistration_validating_admission_policy` defines a reusable validation rule in CEL, evaluated in-process by the API server rather than by a real, separately-deployed webhook -- the same real job a validating webhook does, without the extra network hop or the availability risk of running one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	admissionregistration "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/admissionregistration"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own validating admission policy"})

    		ubx.Resource(admissionregistration.ValidatingAdmissionPolicy, "example", admissionregistration.ValidatingAdmissionPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ValidatingAdmissionPolicy } from "@ubx/sdk-kubernetes/kubernetes/admissionregistration/validating_admission_policy";

    export default stack("example", () => {
      intent({ summary: "example own validating admission policy" });

      resource(ValidatingAdmissionPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.admissionregistration import ValidatingAdmissionPolicy, ValidatingAdmissionPolicyConfig

    def describe():
        ubx.intent("example own validating admission policy")

        ubx.resource(ValidatingAdmissionPolicy, "example", ValidatingAdmissionPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    kubernetes_admissionregistration_validating_admission_policy
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="api_version" type="string">
  APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="object">
  ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
      Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations Optional.
    </ResponseField>
    <ResponseField name="creation_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="deletion_grace_period_seconds" type="number">
      Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only. Optional.
    </ResponseField>
    <ResponseField name="deletion_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="finalizers" type="list(string)">
      Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. Optional.
    </ResponseField>
    <ResponseField name="generate_name" type="string">
      GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server. If this field is specified and the generated name exists, the server will return a 409. Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency Optional.
    </ResponseField>
    <ResponseField name="generation" type="number">
      A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels Optional.
    </ResponseField>
    <ResponseField name="managed_fields" type="list(object)">
      ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          The API version of the resource that the field manager used when managing these fields, such as 'admissionregistration.k8s.io/v1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_type" type="string">
          The type of the managed fields, which indicates the format of the fieldsV1 object. In Kubernetes, this is always 'FieldsV1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_v1" type="any">
          fields_v1 is a dynamic object that stores the fields owned by the manager, using the Kubernetes FieldsV1 format (a JSON representation with keys like 'f:&lt;fieldName&gt;' that describes the managed fields). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="manager" type="string">
          The name of the manager (e.g., a controller or client) that owns or last modified the tracked fields in this managedFields entry. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operation" type="string">
          The operation that last modified the managed field, such as Update, Apply, or Delete. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subresource" type="string">
          The subresource to which these managed fields apply. An empty string indicates the main resource, while non-empty values such as 'status' or 'scale' refer to the respective subresource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time" type="string">
          The timestamp of when the managed field entry was last updated, in RFC3339 format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names Optional.
    </ResponseField>
    <ResponseField name="namespace" type="string">
      Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces Optional.
    </ResponseField>
    <ResponseField name="owner_references" type="list(object)">
      List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          The API version of the referent object as defined in the OwnerReference. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="block_owner_deletion" type="bool">
          If true, and if the owner has the 'foregroundDeletion' finalizer, the owner cannot be deleted from the key-value store until this dependency is deleted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="controller" type="bool">
          If true, this reference points to the managing controller of the object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of the owner object in Kubernetes, such as 'Deployment' or 'ConfigMap'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the referenced Kubernetes resource (e.g., the object's name within its namespace). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="uid" type="string">
          UID of the object referenced by this owner reference. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_version" type="string">
      An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency Optional.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. Optional.
    </ResponseField>
    <ResponseField name="uid" type="string">
      UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="spec" type="object">
  ValidatingAdmissionPolicySpec is the specification of the desired behavior of the AdmissionPolicy. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="audit_annotations" type="list(object)">
      auditAnnotations contains CEL expressions which are used to produce audit annotations for the audit event of the API request. validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is required. Optional.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The key for the audit annotation, which will appear in the audit event for the validating admission policy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value_expression" type="string">
          value_expression is a CEL expression that computes the value for the audit annotation. The expression must return a string or a value convertible to a string, and it can access variables from the policy's context. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="failure_policy" type="string">
      failurePolicy defines how to handle failures for the admission policy. Failures can occur from CEL expression parse errors, type check errors, runtime errors and invalid or mis-configured policy definitions or bindings. A policy is invalid if spec.paramKind refers to a non-existent Kind. A binding is invalid if spec.paramRef.name refers to a non-existent resource. failurePolicy does not define how validations that evaluate to false are handled. When failurePolicy is set to Fail, ValidatingAdmissionPolicyBinding validationActions define how failures are enforced. Allowed values are Ignore or Fail. Defaults to Fail. Optional.
    </ResponseField>
    <ResponseField name="match_conditions" type="list(object)">
      matchConditions is a list of conditions that must be met for a request to be validated. Match conditions filter requests that have already been matched by the rules, namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests. There are a maximum of 64 match conditions allowed. If a parameter object is provided, it can be accessed via the `params` handle in the same manner as validation expressions. The exact matching logic is (in order): 1. If ANY matchCondition evaluates to FALSE, the policy is skipped. 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated. 3. If any matchCondition evaluates to an error (but none are FALSE): - If failurePolicy=Fail, reject the request - If failurePolicy=Ignore, the policy is skipped Optional.
      <Expandable title="properties">
        <ResponseField name="expression" type="string">
          The CEL (Common Expression Language) expression that is evaluated against the request. If the expression evaluates to true, the policy applies to the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the match condition, used to uniquely identify it within the policy. Must be a valid DNS subdomain name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="match_constraints" type="object">
      MatchResources decides whether to run the admission control policy on an object based on whether it meets the match criteria. The exclude rules take precedence over include rules (if a resource matches both, it is excluded) Optional.
      <Expandable title="properties">
        <ResponseField name="exclude_resource_rules" type="list(object)">
          excludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about. The exclude rules take precedence over include rules (if a resource matches both, it is excluded) Optional.
          <Expandable title="properties">
            <ResponseField name="api_groups" type="list(string)">
              A list of API group names to which this exclude rule applies. An empty list is treated as all API groups; '*' also matches all groups. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="api_versions" type="list(string)">
              The API versions (e.g., 'v1', 'apps/v1') of resources that match this exclusion rule. The rule excludes resources with these API versions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operations" type="list(string)">
              The list of operations (e.g., CREATE, UPDATE, DELETE, CONNECT) that this exclude rule applies to. To match all operations, use '*'. These operations are excluded from validation for the specified resources. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_names" type="list(string)">
              An optional list of specific resource names (e.g., pod or service names) to which this exclude rule applies. If empty or omitted, the rule applies to all resources matching the specified api groups, versions, and kinds. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resources" type="list(string)">
              The Kubernetes resource types (e.g., 'pods', 'services') to which this exclusion applies. Requests for these resources are not subject to the policy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="scope" type="string">
              Scope specifies the scope of the resource rule. Allowed values are '*', 'Cluster', or 'Namespaced'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_policy" type="string">
          matchPolicy defines how the "MatchResources" list is used to match incoming requests. Allowed values are "Exact" or "Equivalent". - Exact: match a request only if it exactly matches a specified rule. For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1, but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`, a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy. - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version. For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1, and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`, a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy. Defaults to "Equivalent" Optional.
        </ResponseField>
        <ResponseField name="namespace_selector" type="object">
          A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects. Optional.
          <Expandable title="properties">
            <ResponseField name="match_expressions" type="list(object)">
              matchExpressions is a list of label selector requirements. The requirements are ANDed. Optional.
              <Expandable title="properties">
                <ResponseField name="key" type="string">
                  The label key that this match expression applies to. Must be a valid Kubernetes label key. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  Specifies the relationship between the label key and value(s) in a match expression. Must be one of: In, NotIn, Exists, DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="values" type="list(string)">
                  The list of values for the label selector expression. Must be provided for operators In and NotIn, and must be empty for Exists and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="match_labels" type="map(string)">
              matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="object_selector" type="object">
          A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects. Optional.
          <Expandable title="properties">
            <ResponseField name="match_expressions" type="list(object)">
              matchExpressions is a list of label selector requirements. The requirements are ANDed. Optional.
              <Expandable title="properties">
                <ResponseField name="key" type="string">
                  The label key that this match expression applies to. It identifies the label on the object being selected. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  The operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists, and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="values" type="list(string)">
                  values is a list of string values used when the operator is In or NotIn to specify the set of values that must (or must not) match. For Exists or DoesNotExist operators, values must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="match_labels" type="map(string)">
              matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource_rules" type="list(object)">
          resourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches. The policy cares about an operation if it matches _any_ Rule. Optional.
          <Expandable title="properties">
            <ResponseField name="api_groups" type="list(string)">
              api_groups is a list of API groups that the resource belongs to. Use '*' to match all API groups (must be the only entry when present). Common groups include '' for core, 'apps', 'batch', etc. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="api_versions" type="list(string)">
              A list of API versions that the resource rule applies to, such as 'v1', 'apps/v1'. Empty means all versions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operations" type="list(string)">
              Operations the rule applies to. Allowed values are CREATE, UPDATE, DELETE, CONNECT, or * for all operations. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_names" type="list(string)">
              The resource names to which the rule applies. If empty, the rule applies to all resource names in the specified resource groups and resources. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resources" type="list(string)">
              A list of Kubernetes resource types (e.g., 'pods', 'services') or '*' matching all resources. Determines which resource kinds the admission policy applies to. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="scope" type="string">
              The scope of the resources to which the rule applies. Valid values are "Cluster", "Namespaced", or "*" (all scopes). If not specified, the default is "*". Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="param_kind" type="object">
      ParamKind is a tuple of Group Kind and Version. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          apiVersion is the API group version the resources belong to. In format of "group/version". Required. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          kind is the API kind the resources belong to. Required. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="validations" type="list(object)">
      validations contain CEL expressions which is used to apply the validation. Validations and AuditAnnotations may not both be empty; a minimum of one Validations or AuditAnnotations is required. Optional.
      <Expandable title="properties">
        <ResponseField name="expression" type="string">
          Expression is the CEL expression that is evaluated for each admission request. It must return a boolean value, and may reference variables defined in the policy's spec.variables. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          An optional human-readable message that is displayed when the validation expression evaluates to false. This message is returned to the client as part of the admission denial response. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message_expression" type="string">
          A CEL expression that produces a custom validation failure message. The expression must evaluate to a string; if it produces an error, the validation fails with that error message. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reason" type="string">
          The machine-readable reason returned to the request when the validation rule evaluates to false. Must be a valid StatusReason; defaults to 'Invalid'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="variables" type="list(object)">
      variables contain definitions of variables that can be used in composition of other expressions. Each variable is defined as a named CEL expression. The variables defined here will be available under `variables` in other expressions of the policy except MatchConditions because MatchConditions are evaluated before the rest of the policy. The expression of a variable can refer to other variables defined earlier in the list but not those after. Thus, Variables must be sorted by the order of first appearance and acyclic. Optional.
      <Expandable title="properties">
        <ResponseField name="expression" type="string">
          The CEL expression that is evaluated to compute this variable's value. It is evaluated using the same context as the admission policy's match conditions, including the admission request and any previously defined variables. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the variable. This must be a valid CEL identifier and is used to refer to the variable in expressions within the policy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="object">
  ValidatingAdmissionPolicyStatus represents the status of an admission validation policy. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="conditions" type="list(object)">
      conditions represent the latest available observations of a policy's current state. Optional.
      <Expandable title="properties">
        <ResponseField name="last_transition_time" type="string">
          The last time the condition transitioned from one status to another. This should reflect when the underlying condition changed, typically represented as a timestamp string. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable message that provides additional details about the current condition, including information about the last transition. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="observed_generation" type="number">
          The generation of the ValidatingAdmissionPolicy that was last processed by the controller, used to determine whether the status reflects the latest spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reason" type="string">
          A machine-readable, CamelCase reason for the condition's last transition. This provides a stable identifier for the condition state, complementing the human-readable message. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          The status of the condition, indicating whether the condition is True, False, or Unknown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Type is a string that identifies the condition and distinguishes it from other conditions in the status.conditions list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="observed_generation" type="number">
      observedGeneration is the generation observed by the controller. Optional.
    </ResponseField>
    <ResponseField name="type_checking" type="object">
      TypeChecking contains results of type checking the expressions in the ValidatingAdmissionPolicy Optional.
      <Expandable title="properties">
        <ResponseField name="expression_warnings" type="list(object)">
          expressionWarnings contains the type checking warnings for each expression. Optional.
          <Expandable title="properties">
            <ResponseField name="field_ref" type="string">
              The field path (as a JSON pointer) within the resource that the warning message refers to, identifying which expression or sub-field caused the type-checking warning. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="warning" type="string">
              The warning message attached to the expression, describing the type-checking issue identified in the validating admission policy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
