---
title: "kubernetes_admissionregistration_validating_webhook_configuration"
description: "`kubernetes_admissionregistration_validating_webhook_configuration` registers a real, external webhook the API server calls to accept or reject..."
---

`kubernetes_admissionregistration_validating_webhook_configuration` registers a real, external webhook the API server calls to accept or reject a matching object before it's persisted -- a real, custom admission check beyond what the API server's own built-in validation covers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only kubernetes --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-kubernetes/sdk/go => ./local-sdk/kubernetes/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	admissionregistration "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/admissionregistration"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own validating webhook configuration"})

    		ubx.Resource(admissionregistration.ValidatingWebhookConfiguration, "example", admissionregistration.ValidatingWebhookConfigurationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only kubernetes --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ValidatingWebhookConfiguration } from "./local-sdk/kubernetes/sdk/typescript/kubernetes/admissionregistration/validating_webhook_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "example own validating webhook configuration" });

      resource(ValidatingWebhookConfiguration, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only kubernetes --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/kubernetes/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.kubernetes.admissionregistration import ValidatingWebhookConfiguration, ValidatingWebhookConfigurationConfig

    def describe():
        ubx.intent("example own validating webhook configuration")

        ubx.resource(ValidatingWebhookConfiguration, "example", ValidatingWebhookConfigurationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    kubernetes_admissionregistration_validating_webhook_configuration
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="api_version" type="string">
  APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="object">
  ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
      Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations Optional.
    </ResponseField>
    <ResponseField name="creation_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="deletion_grace_period_seconds" type="number">
      Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only. Optional.
    </ResponseField>
    <ResponseField name="deletion_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="finalizers" type="list(string)">
      Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. Optional.
    </ResponseField>
    <ResponseField name="generate_name" type="string">
      GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server. If this field is specified and the generated name exists, the server will return a 409. Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency Optional.
    </ResponseField>
    <ResponseField name="generation" type="number">
      A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels Optional.
    </ResponseField>
    <ResponseField name="managed_fields" type="list(object)">
      ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          The API version of the resource that the manager applied, such as 'admissionregistration.k8s.io/v1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_type" type="string">
          The type of the managed fields, typically 'FieldsV1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_v1" type="any">
          The managed fields for this entry, represented as a FieldsV1 object -- a JSON structure (trie-like) encoding the set of fields owned by the manager. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="manager" type="string">
          The name of the entity (such as a controller, operator, or user) that manages the fields listed in this managedFields entry. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operation" type="string">
          Operation is the type of operation applied to the managed field set. Valid values are 'Update' and 'Apply'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subresource" type="string">
          The subresource to which this managed field entry applies, such as an empty string for the main resource or "status" for the status subresource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time" type="string">
          Time is the timestamp of when the managed fields entry was last updated, represented as a string in RFC3339 format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names Optional.
    </ResponseField>
    <ResponseField name="namespace" type="string">
      Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces Optional.
    </ResponseField>
    <ResponseField name="owner_references" type="list(object)">
      List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          The API version of the owner resource referenced in this owner reference, such as 'v1' for core resources or 'apps/v1' for Deployments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="block_owner_deletion" type="bool">
          If true, and the owner has the foregroundDeletion finalizer, the owner cannot be deleted until this reference is removed. Defaults to false; setting it requires delete permission on the owner. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="controller" type="bool">
          Boolean flag indicating whether the referenced owner is the managing controller for this resource. Only one controller can be set for a resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind (type) of the object that owns this resource, as referenced in an ownerReference. For example, 'Deployment' or 'StatefulSet'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the referenced owner object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="uid" type="string">
          UID of the referent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_version" type="string">
      An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency Optional.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. Optional.
    </ResponseField>
    <ResponseField name="uid" type="string">
      UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="webhooks" type="list(object)">
  webhooks is a list of webhooks and the affected resources and operations. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="admission_review_versions" type="list(string)">
      The ordered list of preferred AdmissionReview versions (e.g., v1, v1beta1) that the webhook supports. Kubernetes will pick the first version in the list that is also supported by the API server. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="client_config" type="object">
      client_config defines how to communicate with the webhook. It must specify either a service reference (namespace, name, path, port) or a URL, and optionally a caBundle to verify the webhook's server certificate. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ca_bundle" type="string">
          The PEM-encoded CA bundle used to verify the webhook server's TLS certificate. If empty, the system root CA is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service" type="object">
          A reference to the Kubernetes Service that will receive the webhook requests. This object specifies the namespace, name, and optionally the path and port of the service. The service and url fields are mutually exclusive within a webhook clientConfig; one must be set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the Kubernetes service that serves as the webhook backend. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="namespace" type="string">
              The namespace of the service that provides the webhook endpoint. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path" type="string">
              Path is an optional URL path which will be sent in any request to this service. If not specified, the root path '/' is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="port" type="number">
              The port of the service to connect to. Defaults to 443 if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="url" type="string">
          The URL of the webhook endpoint, in standard URL form (scheme://host:port/path). Must use HTTPS unless the host is localhost, and must be provided if no service reference is configured. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="failure_policy" type="string">
      Specifies how failures from the webhook are handled. Allowed values are 'Ignore' (default) and 'Fail'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="match_conditions" type="list(object)">
      match_conditions is a list of CEL expressions that control when the webhook is invoked. Each entry contains a name and an expression; the webhook is called only if all expressions evaluate to true. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="expression" type="string">
          A CEL (Common Expression Language) expression that must evaluate to true for this match condition to match. The webhook is invoked only if all match conditions evaluate to true. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the match condition, used to identify it within the list of match conditions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="match_policy" type="string">
      Determines how the webhook matches requests when multiple rules are present. Valid values are 'Exact' and 'Equivalent'. 'Exact' requires the request to match a rule exactly, while 'Equivalent' also matches requests for equivalent API versions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of the admission webhook. Must be unique across all webhooks in the configuration. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="namespace_selector" type="object">
      A label selector to filter the namespaces where the webhook applies. If omitted, the webhook matches all namespaces. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="match_expressions" type="list(object)">
          A list of label selector requirements. Each requirement defines a key, an operator (In, NotIn, Exists, DoesNotExist), and optionally values. The namespace selector only matches namespaces whose labels satisfy all listed requirements. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key is the label key that the selector requirement applies to, matching labels on the object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              Operator represents a key's relationship to a set of values in a label selector requirement. Valid values are In, NotIn, Exists, and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              A list of values that the label key must match against. This is required and used when the operator is `In` or `NotIn`; must be empty for `Exists` and `DoesNotExist` operators. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_labels" type="map(string)">
          A map of key-value pairs that must match the labels on a namespace for the webhook to be invoked. The namespace must have all specified labels with the exact values for the selector to match. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="object_selector" type="object">
      The object_selector field is a label selector that restricts the admission webhook to only match objects with the specified labels. When set, the webhook will only be invoked for requests for resources that match the selector. If omitted, the webhook will be invoked for all matching requests based on the configured rules. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="match_expressions" type="list(object)">
          match_expressions is a list of label selector requirements. Each requirement consists of a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and optionally a list of values. All requirements are ANDed together; an object must satisfy every requirement to match the selector. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The label key that the selector requirement applies to. The object's labels must contain this key for the selector to match. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              The operator represents the relationship between the label key and value(s) in the match expression. Valid values are: In, NotIn, Exists, DoesNotExist, Gt, and Lt. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              values is a list of string values that the label selector matches against. For operators In and NotIn, this list must be non-empty; for Exists and DoesNotExist, it must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_labels" type="map(string)">
          A map of label keys to values that an object must have in order for the webhook to be invoked. All specified label pairs must match the object's labels (AND semantics). This is part of the objectSelector used to scope the webhook to a subset of resources. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="rules" type="list(object)">
      A list of rules that specify the operations and resources for which the webhook is invoked. Each rule includes operations (e.g., CREATE, UPDATE) and resource selectors such as apiGroups, apiVersions, and resources. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="api_groups" type="list(string)">
          List of API groups that the rule matches. An empty list matches all API groups. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="api_versions" type="list(string)">
          Specifies the API versions of the resources that this rule applies to, such as 'v1' or 'apps/v1'. Must be a non-empty list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operations" type="list(string)">
          Operations is a list of operations that trigger the webhook. Allowed values are CREATE, UPDATE, DELETE, CONNECT, and * (matches all operations). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resources" type="list(string)">
          Resources is a list of Kubernetes resource types (e.g., 'pods', 'deployments') that this rule applies to. Use '*' to match all resources within the specified apiGroups and apiVersions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scope" type="string">
          The scope field specifies whether the rule matches resources of a particular scope. Allowed values are 'Cluster', 'Namespaced', and 'All'. When omitted, the rule applies to all scopes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="side_effects" type="string">
      States whether this webhook has side effects. Acceptable values are: None, NoneOnDryRun, Some, SomeOnDryRun. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="timeout_seconds" type="number">
      Timeout in seconds for the webhook call. Must be an integer between 1 and 30. Defaults to 10 seconds if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
