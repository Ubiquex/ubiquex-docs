---
title: "kubernetes_apps_deployment"
description: "`kubernetes_apps_deployment` manages a real, replicated set of identical pods and their own rolling update, declaratively -- change the pod template..."
---

`kubernetes_apps_deployment` manages a real, replicated set of identical pods and their own rolling update, declaratively -- change the pod template, and the Deployment controller works out how to get from the old real state to the new one without downtime.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apps "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/apps"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own deployment"})

    		ubx.Resource(apps.Deployment, "example", apps.DeploymentConfig{
    			Name:      "example",
    			Namespace: "example",
    			Spec:      map[string]any{"selector": map[string]any{"match_expressions": []map[string]any{{"key": "example"}}}, "template": map[string]any{"metadata": map[string]any{"name": "example-name"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Deployment } from "@ubx/sdk-kubernetes/kubernetes/apps/deployment";

    export default stack("example", () => {
      intent({ summary: "example own deployment" });

      resource(Deployment, "example", {
        name: "example",
        namespace: "example",
        spec: {
          selector: { matchExpressions: [{ key: "example" }] },
          template: { metadata: { name: "example-name" } },
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.apps import Deployment, DeploymentConfig
    from ubx.kubernetes.apps.deployment import Deployment_Spec, Deployment_Spec_Selector, Deployment_Spec_Selector_MatchExpressions, Deployment_Spec_Template, Deployment_Metadata

    def describe():
        ubx.intent("example own deployment")

        ubx.resource(Deployment, "example", DeploymentConfig(
            name="example",
            namespace="example",
            spec=Deployment_Spec(selector=Deployment_Spec_Selector(match_expressions=[Deployment_Spec_Selector_MatchExpressions(key="example")]), template=Deployment_Spec_Template(metadata=Deployment_Metadata(name="example-name"))),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create kubernetes_apps_deployment called "example", with
    namespace example, spec a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="api_version" type="string">
  APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="object">
  ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
      Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations Optional.
    </ResponseField>
    <ResponseField name="creation_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="deletion_grace_period_seconds" type="number">
      Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only. Optional.
    </ResponseField>
    <ResponseField name="deletion_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="finalizers" type="list(string)">
      Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. Optional.
    </ResponseField>
    <ResponseField name="generate_name" type="string">
      GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server. If this field is specified and the generated name exists, the server will return a 409. Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency Optional.
    </ResponseField>
    <ResponseField name="generation" type="number">
      A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels Optional.
    </ResponseField>
    <ResponseField name="managed_fields" type="list(object)">
      ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          The version of the Kubernetes API that the managing controller uses for this managed field entry, typically in the format "apps/v1". Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_type" type="string">
          The type of the fields stored in the managed fields entry. In Kubernetes, this is typically "FieldsV1", indicating the structure and version of the fields map. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_v1" type="any">
          fields_v1 is a JSON representation (FieldsV1 format) of the set of fields that the manager owns or is responsible for, as recorded in a managedFields entry. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="manager" type="string">
          The name of the manager (e.g., a controller, agent, or tool) that owns or last modified this managed field entry in the object's metadata. It identifies which component manages these fields per Kubernetes managedFields semantics. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operation" type="string">
          The type of operation that set the field, e.g., Apply or Update. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subresource" type="string">
          The subresource associated with this managed field entry, such as 'status' or 'scale'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time" type="string">
          The timestamp of when the managed field operation occurred, represented as a string in RFC3339 format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names Optional.
    </ResponseField>
    <ResponseField name="namespace" type="string">
      Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces Optional.
    </ResponseField>
    <ResponseField name="owner_references" type="list(object)">
      List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          The API version (group/version) of the owner resource, e.g. apps/v1 for a Deployment or v1 for a ConfigMap. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="block_owner_deletion" type="bool">
          If true, and if the owner has the foregroundDeletion finalizer, the owner cannot be deleted from the key-value store until this reference is removed. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="controller" type="bool">
          Indicates whether the owner reference is the controlling owner. If true, this owner is the controller of the dependent object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of the owner object, such as 'Deployment' or 'ReplicaSet'. This is the Kubernetes API kind of the referenced resource and is required in an owner reference. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the owner object that this reference points to. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="uid" type="string">
          The unique identifier (UID) of the owner object referenced by this owner reference. Used to unambiguously identify the owner resource in the cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_version" type="string">
      An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency Optional.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. Optional.
    </ResponseField>
    <ResponseField name="uid" type="string">
      UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="spec" type="object" required>
  DeploymentSpec is the specification of the desired behavior of the Deployment. Required.
  <Expandable title="properties">
    <ResponseField name="min_ready_seconds" type="number">
      Minimum number of seconds for which a newly created pod should be ready without any of its container crashing, for it to be considered available. Defaults to 0 (pod will be considered available as soon as it is ready) Optional.
    </ResponseField>
    <ResponseField name="paused" type="bool">
      Indicates that the deployment is paused. Optional.
    </ResponseField>
    <ResponseField name="progress_deadline_seconds" type="number">
      The maximum time in seconds for a deployment to make progress before it is considered to be failed. The deployment controller will continue to process failed deployments and a condition with a ProgressDeadlineExceeded reason will be surfaced in the deployment status. Note that progress will not be estimated during the time a deployment is paused. Defaults to 600s. Optional.
    </ResponseField>
    <ResponseField name="replicas" type="number">
      Number of desired pods. This is a pointer to distinguish between explicit zero and not specified. Defaults to 1. Optional.
    </ResponseField>
    <ResponseField name="revision_history_limit" type="number">
      The number of old ReplicaSets to retain to allow rollback. This is a pointer to distinguish between explicit zero and not specified. Defaults to 10. Optional.
    </ResponseField>
    <ResponseField name="selector" type="object" required>
      A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects. Required.
      <Expandable title="properties">
        <ResponseField name="match_expressions" type="list(object)">
          matchExpressions is a list of label selector requirements. The requirements are ANDed. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of the label to which the selector applies. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              The operator represents a key's relationship to a set of values in a label selector requirement. Valid operators are In, NotIn, Exists, and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              A list of string values. For the In and NotIn operators, this specifies the set of values that the label key must match. For Exists and DoesNotExist, this field must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_labels" type="map(string)">
          matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="strategy" type="object">
      DeploymentStrategy describes how to replace existing pods with new ones. Optional.
      <Expandable title="properties">
        <ResponseField name="rolling_update" type="object">
          Spec to control the desired behavior of rolling update. Optional.
          <Expandable title="properties">
            <ResponseField name="max_surge" type="string">
              IntOrString is a type that can hold an int32 or a string. When used in JSON or YAML marshalling and unmarshalling, it produces or consumes the inner type. This allows you to have, for example, a JSON field that can accept a name or number. Optional.
            </ResponseField>
            <ResponseField name="max_unavailable" type="string">
              IntOrString is a type that can hold an int32 or a string. When used in JSON or YAML marshalling and unmarshalling, it produces or consumes the inner type. This allows you to have, for example, a JSON field that can accept a name or number. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          Type of deployment. Can be "Recreate" or "RollingUpdate". Default is RollingUpdate. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="template" type="object" required>
      PodTemplateSpec describes the data a pod should have when created from a template Required.
      <Expandable title="properties">
        <ResponseField name="metadata" type="object">
          ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. Optional.
          <Expandable title="properties">
            <ResponseField name="annotations" type="map(string)">
              Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations Optional.
            </ResponseField>
            <ResponseField name="creation_timestamp" type="string">
              Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
            </ResponseField>
            <ResponseField name="deletion_grace_period_seconds" type="number">
              Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only. Optional.
            </ResponseField>
            <ResponseField name="deletion_timestamp" type="string">
              Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
            </ResponseField>
            <ResponseField name="finalizers" type="list(string)">
              Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. Optional.
            </ResponseField>
            <ResponseField name="generate_name" type="string">
              GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server. If this field is specified and the generated name exists, the server will return a 409. Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency Optional.
            </ResponseField>
            <ResponseField name="generation" type="number">
              A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. Optional.
            </ResponseField>
            <ResponseField name="labels" type="map(string)">
              Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels Optional.
            </ResponseField>
            <ResponseField name="managed_fields" type="list(object)">
              ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. Optional.
              <Expandable title="properties">
                <ResponseField name="api_version" type="string">
                  The API version of the resource to which the managed fields apply, e.g., 'apps/v1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fields_type" type="string">
                  The type of the fields in a managed fields entry. In Kubernetes, this is typically 'FieldsV1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fields_v1" type="any">
                  The set of fields managed by the controller for this object, represented as a JSON structure in the FieldsV1 format. It maps field paths (using keys like 'f:') to their values, indicating exactly which fields are governed by the manager. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="manager" type="string">
                  Manager is the name of the manager that last applied this field. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operation" type="string">
                  The operation that produced this managed field entry. Valid values are 'Apply' and 'Update'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="subresource" type="string">
                  The subresource that the manager is operating on, such as 'status' or 'scale'. For example, in a Deployment, it could be 'status' for status updates. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="time" type="string">
                  The time at which the managed fields entry was last updated, expressed as a string in RFC3339 format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names Optional.
            </ResponseField>
            <ResponseField name="namespace" type="string">
              Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces Optional.
            </ResponseField>
            <ResponseField name="owner_references" type="list(object)">
              List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. Optional.
              <Expandable title="properties">
                <ResponseField name="api_version" type="string">
                  API version of the referent owner resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="block_owner_deletion" type="bool">
                  Indicates whether the dependent object should block the owner's deletion. If true, the owner is prevented from being deleted until this owner reference is removed (requires the owner to have a foregroundDeletion finalizer). Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="controller" type="bool">
                  Indicates whether this owner reference is the managing controller for the resource. When true, the referenced owner is the controller that manages the resource's lifecycle. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kind" type="string">
                  The kind of the owner resource (e.g., 'Deployment', 'ReplicaSet'). This must match the kind of the object that owns this resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the object that owns this resource, as specified in the owner reference. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="uid" type="string">
                  The UID of the referenced owner object. This is a unique identifier assigned by the Kubernetes API server to uniquely identify the object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="resource_version" type="string">
              An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency Optional.
            </ResponseField>
            <ResponseField name="self_link" type="string">
              Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. Optional.
            </ResponseField>
            <ResponseField name="uid" type="string">
              UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="spec" type="object">
          PodSpec is a description of a pod. Optional.
          <Expandable title="properties">
            <ResponseField name="active_deadline_seconds" type="number">
              Optional duration in seconds the pod may be active on the node relative to StartTime before the system will actively try to mark it failed and kill associated containers. Value must be a positive integer. Optional.
            </ResponseField>
            <ResponseField name="affinity" type="object">
              Affinity is a group of affinity scheduling rules. Optional.
              <Expandable title="properties">
                <ResponseField name="node_affinity" type="object">
                  Node affinity is a group of node affinity scheduling rules. Optional.
                  <Expandable title="properties">
                    <ResponseField name="preferred_during_scheduling_ignored_during_execution" type="list(object)">
                      The scheduler will prefer to schedule pods to nodes that satisfy the affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node matches the corresponding matchExpressions; the node(s) with the highest sum are the most preferred. Optional.
                      <Expandable title="properties">
                        <ResponseField name="preference" type="object">
                          The node selector term that defines the preference rule. It contains matchExpressions and matchFields constraints used to select nodes that satisfy the preferred scheduling requirement. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              A list of node selector requirements. Each requirement contains a key, an operator, and a list of values. All requirements in the list must match the node's labels for the preference to apply. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key that the node must have for the affinity rule to match. It is a required field in each match expression. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator used to compare the label key to the listed values. Allowed values: In, NotIn, Exists, DoesNotExist, Gt, Lt. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  The list of values for the node selector requirement. When the operator is In or NotIn, this list must contain at least one value; for Exists or DoesNotExist, it must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_fields" type="list(object)">
                              A list of node selector requirements that specify field-based constraints for the node, such as metadata.name, to match for this preferred scheduling term. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The key of the node label that the match field expression applies to. This key must exist on the node for the affinity rule to match. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator represents the relationship between the node label/field key and the value(s) to match. Valid operators are In, NotIn, Exists, DoesNotExist, Gt, and Lt. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  A list of string values for the field selector. This is required when the operator is In, NotIn, Gt, or Lt, and must be empty for Exists or DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="weight" type="number">
                          The weight associated with this node affinity preference. Must be an integer between 1 and 100. During scheduling, each node that satisfies the preference adds this weight to its overall score; nodes with higher total weights are preferred. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="required_during_scheduling_ignored_during_execution" type="object">
                      A node selector represents the union of the results of one or more label queries over a set of nodes; that is, it represents the OR of the selectors represented by the node selector terms. Optional.
                      <Expandable title="properties">
                        <ResponseField name="node_selector_terms" type="list(object)" required>
                          Required. A list of node selector terms. The terms are ORed. Required.
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              A list of node selector requirements. Each requirement defines a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and optionally a list of values. All requirements in the list must be satisfied for the node to match the selector term. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The key of the node label to match against. This key is used with the specified operator to evaluate node affinity. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator used to match node labels in a node affinity match expression. Valid values are In, NotIn, Exists, DoesNotExist, Gt, and Lt. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  The list of values for the match expression. For operators In, NotIn, Gt, and Lt, this list contains the values to match against the node label. For Exists and DoesNotExist, this list must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_fields" type="list(object)">
                              A list of node selector requirements that specify node fields (e.g., metadata.name) to match. Each item includes a key, operator (e.g., In, NotIn, Exists, DoesNotExist), and optional values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The key of the node field that this requirement applies to. For match_fields, common keys include 'metadata.name'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator represents the relationship between the key and value(s). Valid values are In, NotIn, Exists, DoesNotExist, Gt, and Lt. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  List of values for the match field constraint. Required when the operator is In or NotIn; must be empty for Exists or DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="pod_affinity" type="object">
                  Pod affinity is a group of inter pod affinity scheduling rules. Optional.
                  <Expandable title="properties">
                    <ResponseField name="preferred_during_scheduling_ignored_during_execution" type="list(object)">
                      The scheduler will prefer to schedule pods to nodes that satisfy the affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the node(s) with the highest sum are the most preferred. Optional.
                      <Expandable title="properties">
                        <ResponseField name="pod_affinity_term" type="object">
                          Defines a set of pods (via label selectors) and a topology domain (via topologyKey) used to express pod affinity or anti-affinity rules. In this context, it supplies the pod matching criteria for a preferred scheduling term. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="label_selector" type="object">
                              A standard Kubernetes label selector (with matchLabels and matchExpressions) that selects the set of pods to which this pod affinity term applies. It determines which pods are considered as affinity targets. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements. Each requirement specifies a key, an operator (In, NotIn, Exists, or DoesNotExist), and optionally a list of values. Multiple requirements are logically ANDed together with matchLabels. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  A map of keys and values that the target pods must have. All labels specified here must be present on the pod for the affinity term to match. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_label_keys" type="list(string)">
                              A list of pod label keys used to select which pods should be considered for the affinity term. The values of these keys from the pod being scheduled are combined with the labelSelector to filter candidate pods; all specified keys are ANDed together. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="mismatch_label_keys" type="list(string)">
                              A list of pod label keys. If any of these keys are present on the candidate pod with a value equal to the corresponding label value on the pod being scheduled, the affinity term is considered not matching. This is used to exclude pods from the affinity evaluation in addition to the labelSelector, typically for scenarios like excluding pods from the same release or rollout. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="namespace_selector" type="object">
                              A label selector for namespaces, used to scope the pod affinity term to pods in matching namespaces. If omitted, only the pod's own namespace is considered. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements for namespace selection. Each requirement specifies a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and optionally values, used to match Kubernetes namespaces. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  A map of key-value pairs that must be present in a namespace's labels for the namespace to be selected. This is used within the namespace selector of a pod affinity term to narrow down the set of namespaces whose pods are considered for affinity/anti-affinity. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="namespaces" type="list(string)">
                              Specifies the namespaces that this pod affinity/anti-affinity term applies to. If omitted, it defaults to the namespace of the pod where the affinity is defined. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="topology_key" type="string">
                              The key of node labels that defines the topology domain for this pod affinity term. For example, 'kubernetes.io/hostname' groups nodes by host, or a custom label can define a different failure domain. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="weight" type="number">
                          The weight associated with this preferred pod affinity term. Must be an integer between 1 and 100. Higher weights give the corresponding affinity term more influence when summing preferred scheduling preferences. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="required_during_scheduling_ignored_during_execution" type="list(object)">
                      If the affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to a pod label update), the system may or may not try to eventually evict the pod from its node. When there are multiple elements, the lists of nodes corresponding to each podAffinityTerm are intersected, i.e. all terms must be satisfied. Optional.
                      <Expandable title="properties">
                        <ResponseField name="label_selector" type="object">
                          A label selector used to identify the set of pods that the affinity rule applies to. It matches pods based on their labels using matchLabels and matchExpressions (if defined). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              A list of label selector requirements. Each requirement specifies a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and optionally a list of values. Multiple requirements are logically ANDed together to select matching pods. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key that the selector matches against. The pod must have a label with this key and a value satisfying the operator for the affinity rule to apply. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator represents the relationship between the label key and the set of values. Valid values are In, NotIn, Exists, and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  A list of string values that the key must match when the operator is In or NotIn. For Exists and DoesNotExist operators, this list must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_labels" type="map(string)">
                              matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="match_label_keys" type="list(string)">
                          A list of pod label keys to filter which pods are considered for the affinity match. The pod affinity term applies to pods that match the label selector and have the same values for these specified label keys, helping to maintain consistency when pod labels change. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mismatch_label_keys" type="list(string)">
                          A list of label keys. Pods with label values that equal the incoming pod's label values for any of these keys are excluded from the affinity match. This allows expressing 'not equal' constraints on specific labels. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="namespace_selector" type="object">
                          Label selector to select the namespaces whose pods are considered for the pod affinity/anti-affinity rule. When omitted, only the pod's own namespace is evaluated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              A list of label selector requirements. The requirements are ANDed. Each requirement specifies a key, an operator (In, NotIn, Exists, DoesNotExist), and optionally a list of values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key that the selector applies to. In a namespace selector match expression, this is the key of the label that must exist (or match according to the operator) on a namespace for the pod to be scheduled in that namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator represents the relationship between the label key and the set of values. Valid values are In, NotIn, Exists, and DoesNotExist. For In/NotIn, the values field must be provided; for Exists/DoesNotExist, values must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  The list of values that correspond to the key in the match expression. Required when the operator is In or NotIn; must be empty for Exists or DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_labels" type="map(string)">
                              A map of key-value pairs that must match labels on a namespace. A namespace must have all specified labels to satisfy this selector, and multiple requirements are ANDed together. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="namespaces" type="list(string)">
                          Specifies the namespaces to which the pod affinity/anti-affinity rule applies. If empty, the rule applies only to the namespace of the current pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="topology_key" type="string">
                          The key to the node labels that define the topology domain, such as 'kubernetes.io/hostname' for node-level topology or 'failure-domain.beta.kubernetes.io/zone' for zone-level topology. This field is required for pod affinity/anti-affinity terms. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="pod_anti_affinity" type="object">
                  Pod anti affinity is a group of inter pod anti affinity scheduling rules. Optional.
                  <Expandable title="properties">
                    <ResponseField name="preferred_during_scheduling_ignored_during_execution" type="list(object)">
                      The scheduler will prefer to schedule pods to nodes that satisfy the anti-affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling anti-affinity expressions, etc.), compute a sum by iterating through the elements of this field and subtracting "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the node(s) with the highest sum are the most preferred. Optional.
                      <Expandable title="properties">
                        <ResponseField name="pod_affinity_term" type="object">
                          The pod affinity term defines the label selector and topology key used to identify a set of pods for anti-affinity. It specifies which pods to avoid scheduling together in the same topology domain. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="label_selector" type="object">
                              A label selector that identifies the set of pods for which the affinity or anti-affinity term applies. It uses matchLabels and matchExpressions constraints to define the selection. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements. The requirements are ANDed together; each requirement specifies a key, an operator (such as In, NotIn, Exists, DoesNotExist), and optionally a list of values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  matchLabels is a map of &#123;key,value&#125; pairs that the pod's labels must match for the affinity term to apply. It selects pods that have all specified label keys and values, equivalent to a set of matchExpressions with operator In and a single value each, ANDed together. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_label_keys" type="list(string)">
                              A list of label keys used to select the pods for this affinity term. The values for these keys from the new pod are merged with the label selector to determine the existing pods to which the rule applies. Keys not present in the new pod's labels are ignored. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="mismatch_label_keys" type="list(string)">
                              MismatchLabelKeys is a list of label keys that must not match between the incoming pod and existing pods for the affinity term to apply. If any key in this list exists on both the incoming pod and an existing pod with the same value, that existing pod is excluded from consideration. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="namespace_selector" type="object">
                              A label selector to filter namespaces where the target pods are located. If empty, only the pod's own namespace is considered. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements. Each requirement specifies a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and optionally a list of values. The namespace selector matches namespaces that satisfy all listed requirements. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="namespaces" type="list(string)">
                              A list of namespaces that the pod affinity term applies to. If empty or null, the term applies to the namespace of the pod where the affinity is defined. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="topology_key" type="string">
                              The key of node labels that defines the topology domain used to match pods. For example, 'kubernetes.io/hostname' to spread pods across nodes, or a zone label for availability zones. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="weight" type="number">
                          Weight associated with the corresponding pod anti-affinity term, ranging from 1 to 100, used to calculate the preferred scheduling score. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="required_during_scheduling_ignored_during_execution" type="list(object)">
                      If the anti-affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the anti-affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to a pod label update), the system may or may not try to eventually evict the pod from its node. When there are multiple elements, the lists of nodes corresponding to each podAffinityTerm are intersected, i.e. all terms must be satisfied. Optional.
                      <Expandable title="properties">
                        <ResponseField name="label_selector" type="object">
                          A label selector is a core Kubernetes query over labels on objects. In pod anti-affinity, this selector identifies the group of pods to which the anti-affinity rule applies, using matchLabels and matchExpressions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              List of label selector requirements. Each requirement is an object with key, operator, and values fields, used to match pod labels for pod anti-affinity. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key that the selector applies to. For example, if the expression is 'app in (frontend, backend)', the key is 'app'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  The list of label values that the match expression selects on. For the In and NotIn operators, at least one value is required; for Exists and DoesNotExist, this list must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_labels" type="map(string)">
                              matchLabels is a map of key-value pairs that must match the labels of the pods selected for this anti-affinity rule. Each key-value pair is a requirement: the pod must have that label key set to that value. Multiple requirements are ANDed together. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="match_label_keys" type="list(string)">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="mismatch_label_keys" type="list(string)">
                          A list of label keys that should not match between this pod and candidate pods for this anti-affinity rule. If a candidate pod has a label with the same key and value as this pod for any of these keys, that candidate pod is excluded from consideration. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="namespace_selector" type="object">
                          A label selector to identify the namespaces to which this pod anti-affinity rule applies. The rule will only consider pods in namespaces matching this selector. If not set, the rule applies to all namespaces (or the pod's own namespace when no namespaces are specified). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              A list of label selector requirements for selecting namespaces. Each requirement specifies a key, operator, and optional values, and all requirements are combined with a logical AND. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key that the namespace selector matches against. In this pod anti-affinity rule, the match expressions apply to labels on namespaces, and this key is the label key whose value is compared using the operator and values specified in the same expression. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  In a namespace selector match expression, the operator defines how the label key is matched against the values. Allowed values are 'In', 'NotIn', 'Exists', and 'DoesNotExist'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  The list of values that the label key must match when the operator is 'In' or 'NotIn'. Must be empty for 'Exists' and 'DoesNotExist' operators. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_labels" type="map(string)">
                              match_labels is a map of label keys and values that must be matched by the namespaces considered for the pod anti-affinity rule. Only namespaces with all specified labels are selected. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="namespaces" type="list(string)">
                          A list of namespaces to which this pod affinity/anti-affinity term applies. If empty, it defaults to the namespace of the pod where the rule is defined. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="topology_key" type="string">
                          The key of node labels that defines the topology domain. For required anti-affinity, this field is required and must not be empty; pods will be scheduled to avoid multiple pods in the same topology domain as defined by this key. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="automount_service_account_token" type="bool">
              AutomountServiceAccountToken indicates whether a service account token should be automatically mounted. Optional.
            </ResponseField>
            <ResponseField name="containers" type="list(object)" required>
              List of containers belonging to the pod. Containers cannot currently be added or removed. There must be at least one container in a Pod. Cannot be updated. Required.
              <Expandable title="properties">
                <ResponseField name="args" type="list(string)">
                  Arguments to the entrypoint. The container image's CMD is used if this is not provided. Overrides the CMD directive in the image when both the command and args are specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="command" type="list(string)">
                  The command to run in the container, specified as an array of strings. It overrides the image's default entrypoint. Equivalent to the Docker ENTRYPOINT directive. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="env" type="list(object)">
                  List of environment variables to set in the container. Each entry defines a variable name and either a direct value or a source for the value (e.g., config map, secret, or field reference). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the environment variable, as referenced in the container's environment configuration. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of the environment variable as a string. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value_from" type="object">
                      valueFrom is an optional source for the environment variable's value. It can reference a ConfigMap key, Secret key, or a field of the pod (downward API). Only one source may be specified, and it cannot be used when value is also set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="config_map_key_ref" type="object">
                          Selects a key from a ConfigMap to populate the value of the environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key to select from the referenced ConfigMap. This is the name of the entry in the ConfigMap whose value will be used as the value for the environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the ConfigMap to select from. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Indicates whether the ConfigMap or its key must be defined. If true, the container starts even if the ConfigMap or key is missing. If false or unset, the container fails if the referenced ConfigMap or key does not exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="field_ref" type="object">
                          Selects a field of the pod as the value for the environment variable. The supported fields include metadata.name, metadata.namespace, metadata.labels['&lt;KEY&gt;'], metadata.annotations['&lt;KEY&gt;'], spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, and status.podIPs. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="api_version" type="string">
                              The API version of the schema that the fieldPath refers to. Typically 'v1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="field_path" type="string">
                              The path to the desired field of the Pod, relative to the Pod object, from which to select the value. Examples include 'status.podIP', 'metadata.name', and 'metadata.namespace'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="file_key_ref" type="object">
                          Optional; if omitted, computed by Kubernetes.
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the key referenced by the file key ref is required to exist. If true, the container is created even when the key is absent; if false (default), the container fails to start when the key is missing. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="volume_name" type="string">
                              The name of the volume that contains the file referenced by the file key. This volume must be defined in the same pod spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="resource_field_ref" type="object">
                          Selects a container resource field (for example, limits.cpu or requests.memory) to expose as an environment variable value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="container_name" type="string">
                              The name of the container to select the resource field from. If not specified, the pod's default container is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="divisor" type="string">
                              The divisor to divide the resource value by, specified as a Kubernetes quantity. For example, use '1' for CPU cores or '1m' for millicores. If unset, defaults to '1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="resource" type="string">
                              The resource name to select, such as 'limits.cpu' or 'requests.memory'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="secret_key_ref" type="object">
                          Selects a key of a Secret in the pod's namespace. The value of the specified key is used as the environment variable's value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key name within the referenced Secret to select the value from. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the Secret that contains the key. The Secret must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specify whether the referenced Secret key must be defined. If true, the container starts even if the key is missing; if false (default), the container fails to start when the key does not exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="env_from" type="list(object)">
                  A list of sources to populate environment variables in the container. Each entry can reference a ConfigMap or Secret, optionally with a prefix to prepend to variable names. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="config_map_ref" type="object">
                      Refers to a Kubernetes ConfigMap. When specified, all key-value pairs from the referenced ConfigMap are imported as environment variables into the container. The object's 'name' field specifies the ConfigMap name, and 'optional' indicates whether the ConfigMap must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the ConfigMap to reference for environment variables. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Indicates whether the ConfigMap must be defined. If true, the ConfigMap may be absent, and the environment variables from it are ignored. Defaults to false, meaning the ConfigMap must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="prefix" type="string">
                      Optional prefix that is prepended to each key in the referenced ConfigMap or Secret to form the environment variable name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      secret_ref is a reference to a Kubernetes Secret from which all key-value pairs are exported as environment variables. It references the Secret by name in the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Secret to read environment variables from. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Optional specifies whether the Secret must be defined. If true, the Secret may not exist and the container will still start; if false, the container will not start if the Secret is absent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="image" type="string">
                  The container image to run, specified as a name with optional tag or digest (e.g., 'nginx:latest' or 'myregistry.example.com/foo@sha256:...'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="image_pull_policy" type="string">
                  Defines when the kubelet should pull the container image. Allowed values: Always, IfNotPresent, Never. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="lifecycle" type="object">
                  lifecycle defines lifecycle hooks for the container, such as postStart and preStop handlers, which allow executing commands at specific points in the container's lifecycle (e.g., right after container start or before container termination). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="post_start" type="object">
                      Specifies a lifecycle hook that runs after a container is successfully created and started. It can define an exec command or HTTP request to execute at that point. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          The exec action executes a command inside the container during the postStart lifecycle hook. It contains a command field, which is an array of strings specifying the command and its arguments to run. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              The command and arguments to execute when the postStart lifecycle hook is triggered. This is a list of strings where the first element is the executable and subsequent elements are arguments. The command is executed directly inside the container without a shell, so shell features like pipes or variables require invoking a shell explicitly. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          Specifies an HTTP GET request to perform after the container starts. This is part of the postStart lifecycle hook in Kubernetes, and the object defines the request details such as host, port, and path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the HTTP GET request. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              A list of HTTP headers to include in the HTTP GET request sent by the postStart lifecycle hook. Each item defines a header name and value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The name of the HTTP header to include in the HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header to include in the GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              The HTTP path to request on the server. Defaults to '/' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The port number on the container to access for the HTTP GET request. Can be a numeric port (1-65535) or a named port defined in the container's ports. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              The protocol to use for the HTTP request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The scheme to use for the HTTP GET request. Valid values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          Specifies a sleep action for the postStart lifecycle hook. The sleep action object contains a 'seconds' field that defines the duration of the sleep in seconds, delaying the completion of the postStart hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              The duration in seconds that the container should sleep before continuing. This is used in the sleep action of a Kubernetes lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          tcp_socket specifies a TCP socket action to be performed during the container's postStart lifecycle hook. It defines the host and port to connect to. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The hostname or IP address to connect to for the TCP socket check. If not specified, defaults to the pod IP address. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The port number (as a string) to which the TCP socket connection is made during the postStart lifecycle hook. Must be a valid port number (1-65535). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="pre_stop" type="object">
                      pre_stop defines a hook that is executed immediately before a container is terminated. It can specify an exec command or an HTTP GET request to perform a task such as graceful shutdown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          Exec specifies the command to execute as a pre-stop lifecycle hook. It runs inside the container; the command is provided as a list of strings where the first element is the executable and the rest are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              The command to run inside the container during the pre-stop lifecycle hook. This is given as an array of strings, where the first element is the command and subsequent elements are arguments. If omitted, the container's default command is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          Configures an HTTP GET request that Kubernetes sends to the container's IP address when the container is being terminated as part of a pre-stop hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the HTTP GET request. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              A list of HTTP headers to send with the GET request. Each item contains a name and value for the header. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The name of the HTTP header to set in the HTTP request, e.g., 'X-Custom-Header'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header field that will be sent in the HTTP GET request during the pre-stop lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Path to access on the HTTP server, for example /healthz. If empty, defaults to /. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The port number or named port on the container to which the HTTP GET request is sent. Must be a valid port number (1-65535) or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              Protocol to use for the HTTP GET request, either HTTP or HTTPS. Defaults to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The URI scheme for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          The `sleep` block configures a Sleep action for the pre-stop lifecycle hook. It contains a single argument, `seconds`, which specifies the number of seconds to delay the container shutdown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              Specifies the sleep duration in seconds before the container is terminated, used in the preStop lifecycle hook to allow graceful shutdown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          Defines a TCP socket handler for the pre-stop lifecycle hook. The kubelet attempts to open a connection to the container's IP address on the specified port to trigger the hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The hostname or IP address to connect to for the TCP socket check. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port to connect to for the preStop hook. Can be a numeric port or a named port defined in the container's ports. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="stop_signal" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="liveness_probe" type="object">
                  Defines a liveness probe for the container. If the probe fails, the kubelet kills the container and restarts it according to the pod's restart policy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the action to take in the liveness probe. It defines a command to execute inside the container to check its health. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to run inside the container for the exec liveness probe, specified as a list of strings (command and arguments). The container is considered healthy if the command exits with status code 0; otherwise, the probe fails. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures required for the liveness probe to be considered failed after having succeeded. Defaults to 3. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      The gRPC action for the liveness probe. It allows the kubelet to check the health of a container using a gRPC service. The action specifies the port to connect to (required) and an optional service name; if omitted, the container's name is used as the default service. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number of the gRPC service to query for the liveness probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the service to place in the gRPC HealthCheckRequest. If not specified, the default is empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Configures the liveness probe to issue an HTTP GET request to the container. The container is considered healthy if the response status code is between 200 and 399. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP probe. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          Custom headers to include in the HTTP GET request for the liveness probe. Each object in the list has 'name' and 'value' fields, specified as strings, which are sent as HTTP headers to the server. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to include in the liveness probe HTTP request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header to send with the probe request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The URL path to access on the HTTP server. Defaults to '/' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to send the HTTP request to. Specify an integer port number or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol (scheme) to use for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS', with a default of 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          Scheme to use for connecting to the host. Valid values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before liveness probes are initiated. Defaults to 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      How often (in seconds) to perform the liveness probe. Defaults to 10 seconds, minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      Minimum consecutive successes required for the liveness probe to be considered successful after a failure. For liveness probes, this value must be 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Defines a TCP socket probe for the liveness check, specifying the port to test for an open TCP connection. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name or IP address to connect to. If not specified, the pod IP is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to probe for liveness. This is the port on the container that the kubelet will attempt to connect to during the probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The duration in seconds Kubernetes waits for a pod to terminate gracefully before forcefully terminating it. This field is part of the pod specification, not the liveness probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      The number of seconds after which the liveness probe times out and is considered failed. This timeout is part of the probe configuration for the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  Name of the container, specified as a DNS_LABEL. Each container in a pod must have a unique name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ports" type="list(object)">
                  List of ports to expose from the container. Each port definition specifies the container port number and protocol, and may include additional attributes such as name or host port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="container_port" type="number">
                      The port number that the container exposes. Must be an integer between 1 and 65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_ip" type="string">
                      The host IP address to bind the port to. If not set, the port binds to all host interfaces. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_port" type="number">
                      The host port number to map to the container port. If set, the container is accessible from outside the cluster via the host's IP address at this port. Must be a valid TCP port number (0-65535). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the container port. Must be an IANA_SVC_NAME (unique identifier, up to 15 characters) and unique among ports in the same pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="protocol" type="string">
                      The protocol used for the container port. Allowed values are TCP, UDP, or SCTP. Defaults to TCP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="readiness_probe" type="object">
                  Defines a probe that determines when the container is ready to accept traffic. If the probe fails, the pod is not added to service endpoints until it succeeds again. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the action to take in the readiness probe by running a command inside the container. The command to execute is defined in the 'command' field of this object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to run inside the container for the exec probe. It is specified as a list of strings; the first element is the command and the rest are arguments. A zero exit code indicates success, any non-zero exit code indicates failure. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures of the readiness probe required before the container is marked not ready. Kubernetes will stop retrying after this many failures; default is 3 and minimum is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      grpc defines the gRPC health check parameters for this readiness probe. It contains the target port (required) and an optional service name to use in the gRPC request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number to connect to for the gRPC health check. Must be a valid TCP port (1-65535). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the service to place in the gRPC HealthCheckRequest. If not specified, the default is an empty string, which indicates the overall server health rather than a specific service. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies the parameters for an HTTP GET request that the readiness probe performs to check container health. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP GET request; defaults to the pod IP address. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          A list of custom HTTP headers to include in the readiness probe's HTTP GET request. Each item specifies a header with 'name' and 'value' fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to set in the request (e.g., X-Custom-Header). Must be a valid HTTP header field name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header included in the readiness probe's HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The HTTP request path to access on the HTTP server, such as "/healthz". Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port to access on the container for the HTTP readiness probe. Can be a numeric port or the name of a container port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol used for the HTTP GET request, such as HTTP or HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for connecting to the host, either 'HTTP' or 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container starts before the readiness probe is initiated. Must be a non-negative integer. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      How often (in seconds) to perform the probe. Defaults to 10 seconds; minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      The minimum consecutive successes required for the probe to be considered successful after having failed. For readiness probes, this defaults to 1 and can be set higher. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      An optional TCP socket probe action. When set, the kubelet attempts to open a TCP connection to the container on the specified port to determine readiness. The port field is required; host is optional. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The hostname or IP address to connect to for the TCP socket check. If not specified, the pod's IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port number or named port to connect to during the readiness check. Provide a numeric port (e.g., 8080) or the name of a port defined in the container's ports list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      The number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resize_policy" type="list(object)">
                  A list of resource resize policies for the container. Each item specifies a resource (cpu or memory) and the restart policy (NotRequired or RestartContainer) that determines whether the container must be restarted when its resource limits are changed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="resource_name" type="string">
                      The name of the resource (e.g., 'cpu' or 'memory') that this container resize policy applies to. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="restart_policy" type="string">
                      Specifies the restart behavior for a container when its resource limits are adjusted in-place. Valid values are 'NotRequired' and 'RestartContainer'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resources" type="object">
                  Specifies the compute resource requirements for the container, such as CPU and memory, including limits and requests. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="claims" type="list(object)">
                      The list of resource claims that this container requires. Each claim is an object containing a name that references a matching ResourceClaim in the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name must match the name of the ResourceClaim that this claim refers to. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="request" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="limits" type="map(string)">
                      The maximum amount of compute resources (e.g., CPU, memory) that the container can use. The key is the resource name (e.g., 'cpu', 'memory') and the value is a quantity string such as '500m' or '1Gi'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="requests" type="map(string)">
                      Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if Limits are explicitly specified, otherwise to an implementation-defined value. The map keys are resource names (e.g., cpu, memory, ephemeral-storage) and values are quantities as strings (e.g., '100m', '1Gi'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="restart_policy" type="string">
                  Optional; if omitted, computed by Kubernetes.
                </ResponseField>
                <ResponseField name="restart_policy_rules" type="list(object)">
                  Optional; if omitted, computed by Kubernetes.
                  <Expandable title="properties">
                    <ResponseField name="action" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="exit_codes" type="object">
                      Defines the container exit codes that trigger the restart behavior as per the restart policy rules. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="operator" type="string">
                          The comparison operator used to evaluate container exit codes against configured values in the restart policy rule. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="values" type="list(number)">
                          List of container exit codes that this restart policy rule applies to. The rule triggers when the container exits with any of these codes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="security_context" type="object">
                  Security settings for the container, including user and group IDs, Linux capabilities, SELinux/AppArmor options, and read-only root filesystem configuration. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="allow_privilege_escalation" type="bool">
                      AllowPrivilegeEscalation controls whether a process can gain more privileges than its parent process. Defaults to true. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="app_armor_profile" type="object">
                      AppArmor profile configuration for the container. The type field can be 'Localhost' (referencing a local profile via localhostProfile), 'RuntimeDefault', or 'Unconfined'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          The name of an AppArmor profile that is pre-loaded on the node and applied to the container. This is used only when the AppArmor profile type is set to 'Localhost'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The type of AppArmor profile to apply. Accepts 'Localhost', 'RuntimeDefault', or 'Unconfined'. When 'Localhost' is selected, the 'localhostProfile' field must also be set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="capabilities" type="object">
                      Capabilities configures Linux capabilities for the container. It allows adding or dropping capabilities via the 'add' and 'drop' lists. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="add" type="list(string)">
                          Specifies a list of Linux capabilities to add to the container. For example, 'NET_ADMIN' or 'SYS_TIME'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="drop" type="list(string)">
                          Specifies capabilities to drop from the container. Each entry is a Linux capability name (e.g., 'ALL', 'NET_ADMIN', 'SYS_TIME'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="privileged" type="bool">
                      Indicates whether the container runs in privileged mode, granting it all capabilities and access to host devices as if it were root on the host. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="proc_mount" type="string">
                      Specifies the proc mount type for the container. Allowed values are 'Default' (kernel-defined default) and 'Unmasked' (unmasks all /proc entries). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only_root_filesystem" type="bool">
                      If true, mounts the container's root filesystem as read-only, preventing any writes to the root filesystem and enhancing security. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_group" type="number">
                      Specifies the group ID (GID) that the container's main process will run as. When not specified, the default GID from the container image is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_non_root" type="bool">
                      Indicates that the container must run as a non-root user. If set to true, the kubelet will validate the image at runtime to ensure that it does not run as UID 0, in which case the container will fail to start. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_user" type="number">
                      The user ID (UID) that the container's main process will run as. If unspecified, the default is the user defined in the container image. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="se_linux_options" type="object">
                      The SELinux options for the container's security context, defining the SELinux label (user, role, type, level) applied to the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="level" type="string">
                          The SELinux level, representing the sensitivity or MLS/MCS category, applied to the container. For example, 's0:c123,c456'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="role" type="string">
                          The SELinux role label applied to the container's security context. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The SELinux type label to apply to the container, such as container_t. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="user" type="string">
                          The SELinux user label that applies to the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="seccomp_profile" type="object">
                      SeccompProfile defines the seccomp profile to apply to the container. The type field must be one of Unconfined, RuntimeDefault, or Localhost; if type is Localhost, localhost_profile specifies the path to the profile on the node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          The path to the seccomp profile on the node, used when the seccomp type is 'Localhost'. The profile must be pre-configured on the node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The type of seccomp profile to apply to the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="windows_options" type="object">
                      Windows-specific security context options, such as GMSA credential spec, runAsUserName, and hostProcess settings for containers running on Windows. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="gmsa_credential_spec" type="string">
                          The GMSA credential spec as a string, containing the JSON-formatted Group Managed Service Account specification for Windows containers. This is used to configure the container to run with a specific GMSA identity. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="gmsa_credential_spec_name" type="string">
                          The name of a GMSA credential spec to use for the Windows container's security context. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="host_process" type="bool">
                          When true, runs the container as a Windows HostProcess container, which grants it access to the host's process and network namespaces. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_user_name" type="string">
                          The username to run the container's main process as, used for Windows containers. This is part of the Windows security context options. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="startup_probe" type="object">
                  startup_probe defines a probe that indicates whether the application within the container is started. It supports exec, HTTP GET, and TCP socket checks, and if the probe fails, the container is restarted according to its restart policy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the action to take, which is to execute a command inside the container. The command's exit code determines the probe result (0 is success). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to run in the container for the startup probe's exec action. It is a list of strings, where the first element is the command and subsequent elements are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      Specifies the number of consecutive failures allowed before the startup probe is considered failed. The default is 3; the minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      grpc specifies a gRPC-based health check for the startup probe. It configures a gRPC connection to the container, typically requiring a port and optionally a service name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number of the gRPC service to check. Must be an integer between 1 and 65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the service to place in the gRPC HealthCheckRequest. If not specified, the default behavior is defined by the gRPC health checking protocol. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies an HTTP GET request to perform for the startup probe. The request targets the container's IP address and includes configurable host, port, path, and HTTP headers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP GET request. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          List of custom HTTP headers to attach to the probe request. Each item defines a header name and value to send with the GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to include in the probe request, e.g., X-Custom-Header. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header. This is the value associated with the header name in the startup probe's HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The HTTP path to request on the server. Defaults to '/'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or name of the port to access on the container during the HTTP GET probe. Must be an integer between 1 and 65535, or the name of a container port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol to use for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'; defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The URI scheme to use for the HTTP GET request. Allowed values are HTTP and HTTPS; defaults to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before the startup probe is initiated. Defaults to 0 if unspecified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      How often (in seconds) to perform the startup probe. Defaults to 10, and the minimum allowed value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      The number of consecutive successful probe checks required for the startup probe to be considered successful. For startup probes, Kubernetes requires this value to be exactly 1, as a startup probe must succeed on the first check after failure. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      The TCP socket check for this startup probe. If the specified port is accepting connections, the probe is considered successful. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          Optional host name or IP address to connect to for the TCP socket health check. If not set, defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to connect to for the startup probe. Can be a numeric port (specified as a string) or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The number of seconds the pod is allowed to terminate gracefully before being forcefully killed. Must be a non-negative integer; defaults to 30 seconds. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the startup probe times out. If the probe does not finish within this period, it is considered failed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="stdin" type="bool">
                  Indicates whether the container runtime should allocate a stdin stream for the container. If set to true, the container can read from standard input. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stdin_once" type="bool">
                  If true, the container's standard input is opened on container start, remains empty until the first client attaches, then stays open until the client disconnects, at which point it is closed and remains closed until the container restarts. This field only has effect when stdin is also true. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_path" type="string">
                  The path where the container's termination message is written. Defaults to /dev/termination-log. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_policy" type="string">
                  termination_message_policy sets how the container's termination message is read by kubelet. Valid values are 'File' (default), which reads the message from the file specified by terminationMessagePath, and 'FallbackToLogsOnError', which falls back to the container logs if the message file is empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="tty" type="bool">
                  Indicates whether the container should allocate a pseudo-TTY. Set to true to enable interactive terminal access to the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volume_devices" type="list(object)">
                  List of volume devices to expose to the container, allowing raw block device access. Each entry specifies a device path and the name of a volume with the 'block' volume mode. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="device_path" type="string">
                      Path within the container where the volume device is mounted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the volume that this block device mounts, corresponding to a volume defined in the pod's volumes list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="volume_mounts" type="list(object)">
                  List of volumes to mount into the container's filesystem. Each item specifies a volume name, a mount path within the container, and optionally read-only access and sub-path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="bind_mount_options" type="list(string)">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="mount_path" type="string">
                      The path inside the container at which the volume will be mounted. This is where the volume's data appears in the container's filesystem. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mount_propagation" type="string">
                      Controls whether and how mounts are propagated between the host and the container. Allowed values: None, HostToContainer, Bidirectional. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      Name of the volume to mount. Must correspond to the name of a volume defined in the pod spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      If true, the volume mount is read-only. If false (default), the volume is mounted read-write. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="recursive_read_only" type="string">
                      Sets the recursive read-only mode for the volume mount. Allowed values are 'Enabled' (mount is read-only recursively), 'Disabled' (mount is not read-only recursively), and 'IfInherited' (inherits the recursive read-only setting from the parent mount). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path" type="string">
                      Path within the volume from which the container's volume mount is mounted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path_expr" type="string">
                      subPathExpr is similar to subPath, but supports environment variable references using $(VAR_NAME) syntax. It allows dynamically selecting a path within the volume based on container environment variables. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="working_dir" type="string">
                  The container's working directory. If not specified, the container runtime default is used. Must be an absolute path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="dns_config" type="object">
              PodDNSConfig defines the DNS parameters of a pod in addition to those generated from DNSPolicy. Optional.
              <Expandable title="properties">
                <ResponseField name="nameservers" type="list(string)">
                  A list of DNS name server IP addresses. This will be appended to the base nameservers generated from DNSPolicy. Duplicated nameservers will be removed. Optional.
                </ResponseField>
                <ResponseField name="options" type="list(object)">
                  A list of DNS resolver options. This will be merged with the base options generated from DNSPolicy. Duplicated entries will be removed. Resolution options given in Options will override those that appear in the base DNSPolicy. Optional.
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the DNS resolver option, such as 'ndots', 'timeout', 'attempts', or other valid resolv.conf options. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value for the DNS resolver option specified in the name field. This is optional; some options may not require a value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="searches" type="list(string)">
                  A list of DNS search domains for host-name lookup. This will be appended to the base search paths generated from DNSPolicy. Duplicated search paths will be removed. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="dns_policy" type="string">
              Set DNS policy for the pod. Defaults to "ClusterFirst". Valid values are 'ClusterFirstWithHostNet', 'ClusterFirst', 'Default' or 'None'. DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy. To have DNS options set along with hostNetwork, you have to specify DNS policy explicitly to 'ClusterFirstWithHostNet'. Optional.
            </ResponseField>
            <ResponseField name="enable_service_links" type="bool">
              EnableServiceLinks indicates whether information about services should be injected into pod's environment variables, matching the syntax of Docker links. Optional: Defaults to true. Optional.
            </ResponseField>
            <ResponseField name="ephemeral_containers" type="list(object)">
              List of ephemeral containers run in this pod. Ephemeral containers may be run in an existing pod to perform user-initiated actions such as debugging. This list cannot be specified when creating a pod, and it cannot be modified by updating the pod spec. In order to add an ephemeral container to an existing pod, use the pod's ephemeralcontainers subresource. Optional.
              <Expandable title="properties">
                <ResponseField name="args" type="list(string)">
                  List of arguments to pass to the container's entrypoint. These override the default arguments provided in the container image. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="command" type="list(string)">
                  The command to run in this ephemeral container, overriding the container image's entrypoint. If not provided, the image's default entrypoint is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="env" type="list(object)">
                  List of environment variables to set in the ephemeral container. Each item defines a variable name and optionally its value or value source (e.g., secret or config map reference). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the environment variable. Must be a valid C identifier (e.g., starts with a letter or underscore, followed by alphanumeric characters or underscores). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      Value of the environment variable. This field is optional if 'valueFrom' is specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value_from" type="object">
                      Specifies an optional source for the environment variable's value, such as a ConfigMap, Secret, field reference, or resource field reference. Only one of value or valueFrom may be specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="config_map_key_ref" type="object">
                          Selects a key of a ConfigMap in the pod's namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key of the ConfigMap entry to select. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              Name of the ConfigMap being referenced. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the ConfigMap or its key must be defined. If set to true, a missing ConfigMap or key does not prevent container startup; the value is treated as unset. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="field_ref" type="object">
                          Selects a field of the pod (e.g., metadata.name, metadata.namespace, spec.nodeName, status.podIP) and injects its value into the environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="api_version" type="string">
                              The API version of the schema that fieldPath is written in terms of. Defaults to 'v1' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="field_path" type="string">
                              The path of the field to select. For example, metadata.name or metadata.namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="file_key_ref" type="object">
                          Optional; if omitted, computed by Kubernetes.
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Optional specifies whether the referenced key must exist. If true, a missing key is allowed and the environment variable is omitted; if false, the key must exist or the pod will not start. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="volume_name" type="string">
                              The name of the volume containing the file from which the environment variable value is read. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="resource_field_ref" type="object">
                          Selects a resource field (e.g., limits.cpu, requests.memory) from the container's resource specification to expose as an environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="container_name" type="string">
                              The name of the container to select the resource field from. If omitted, the pod's default container is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="divisor" type="string">
                              Divisor for the resource field value. The resource value is divided by this quantity, which must be a valid Kubernetes quantity (e.g., '1', '100m'); defaults to 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="resource" type="string">
                              The name of the resource to select, such as 'limits.cpu' or 'requests.memory'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="secret_key_ref" type="object">
                          Selects a key from a Kubernetes Secret to populate the environment variable's value. The Secret must exist in the same namespace unless the optional flag is set to true. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key of the secret to select from. Must be a valid key in the referenced Secret. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the Secret from which the key is selected. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the Secret or its key must be defined. When set to true, the reference is optional and the container can start even if the Secret or key is missing; the environment variable is omitted. When false (default), the referenced Secret must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="env_from" type="list(object)">
                  env_from specifies a list of sources (ConfigMap or Secret) whose key-value pairs are injected as environment variables into the ephemeral container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="config_map_ref" type="object">
                      Reference to a ConfigMap that populates environment variables in the container. The referenced ConfigMap must exist in the same namespace as the Pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the ConfigMap to reference. This ConfigMap's data is used to populate environment variables for the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Indicates whether the referenced ConfigMap must be defined. If true, a missing ConfigMap is ignored; if false, the container fails to start when the ConfigMap is absent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="prefix" type="string">
                      An optional prefix that is prepended to each key in the referenced ConfigMap or Secret when converting it to environment variable names. Keys are uppercased and invalid characters are replaced with underscores before the prefix is applied. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      secretRef is a reference to a Secret in the same namespace as the Pod. The data of the referenced Secret is exposed as environment variables via envFrom. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Secret to use. The Secret must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Specifies whether the referenced Secret must be defined. If set to true, the Secret is optional and its absence will not prevent the container from starting. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="image" type="string">
                  The container image to use for the ephemeral container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="image_pull_policy" type="string">
                  The image pull policy for the ephemeral container. Valid values are Always, IfNotPresent, and Never. Defaults to Always if the image tag is 'latest', otherwise IfNotPresent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="lifecycle" type="object">
                  Lifecycle defines lifecycle hooks for the ephemeral container, including postStart and preStop handlers that execute at container startup and shutdown, respectively. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="post_start" type="object">
                      post_start is a lifecycle hook that runs immediately after the ephemeral container is started. It defines a handler (exec, httpGet, or tcpSocket) to perform actions at container start. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          The exec-based lifecycle handler, which defines a command to run inside the ephemeral container after it starts, as part of the post-start hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              The command to run inside the container for the post-start lifecycle exec hook, specified as a list of strings where the first element is the command and the remaining elements are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          Specifies an HTTP GET request to perform on the container's lifecycle post-start hook. The hook is considered successful if the request returns an HTTP status code between 200 and 399. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the HTTP GET request. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              Specifies custom HTTP headers to send with the GET request. Each header object contains 'name' and 'value' fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The HTTP header field name, e.g., 'Authorization' or 'Content-Type'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header for the postStart lifecycle hook's HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              The path to access on the HTTP server for the HTTP GET request in the post-start lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The port number or name of the port to access on the container for the HTTP GET request. It can be a numeric port (1-65535) or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              Specifies the protocol (HTTP or HTTPS) to use for the HTTP GET request. Defaults to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The scheme to use for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          Specifies a sleep action to be executed as part of the postStart lifecycle hook. The sleep action pauses execution for a set number of seconds, allowing the container to delay subsequent hook completion. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              The number of seconds to sleep as part of the postStart lifecycle hook's sleep action. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          Specifies a TCP socket action to be performed as part of the post-start lifecycle hook. This includes checking that a TCP socket is listening on the given port (with optional host). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The hostname or IP address to connect to for the TCP socket action. If not specified, the pod IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              Specifies the port number to connect to on the container for the TCP socket. Can be a numeric port (1-65535) or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="pre_stop" type="object">
                      The pre_stop block configures the preStop lifecycle hook, which is invoked immediately before a container is terminated, allowing for graceful shutdown or cleanup operations. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          Exec specifies the command to be run inside the container as the preStop lifecycle hook handler. It represents the ExecAction, which executes a command to perform cleanup or graceful shutdown before the container is terminated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              Command is the command line to execute inside the container, specified as an array of strings. The first element is the command, and remaining elements are arguments. If omitted, the container's default command is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          Specifies an HTTP GET request to perform as the pre-stop lifecycle action. The action succeeds if the request returns a 2xx status code. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the HTTP GET request. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              A list of custom HTTP headers to include in the GET request. Each header should specify a header name and value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  Specifies the name of an HTTP header to include in the preStop HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header to include in the HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Path to access on the HTTP server. Defaults to '/'. For example, '/healthz'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              Port number or name to access for the HTTP GET request. Can be a numeric port or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              The protocol (scheme) used for the HTTP GET request, such as HTTP or HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The scheme to use for the HTTP request, either HTTP or HTTPS. Defaults to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          Specifies a sleep action for the pre-stop hook, which delays the container's termination by a specified number of seconds to allow graceful shutdown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              The number of seconds the container sleeps before termination, used by the sleep action in the preStop lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          Represents a TCP socket action, defined by a port and optional host, that Kubernetes performs when the pre-stop lifecycle hook is triggered. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the TCP socket action. If not specified, defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port to connect to. Accepts a port number (1-65535) or a named port (IANA_SVC_NAME). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="stop_signal" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="liveness_probe" type="object">
                  Defines the liveness probe for the ephemeral container. Note: Kubernetes ignores liveness probes for ephemeral containers, as they are not restarted by the kubelet. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies a command to run inside the container for the liveness probe. The probe succeeds if the command exits with status 0, and fails on any non-zero exit code. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          Command to execute inside the container for the liveness probe. The first element is the command, and any remaining elements are arguments passed to it. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      Minimum consecutive failures for the liveness probe to be considered failed after having succeeded. Defaults to 3; minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      Specifies the gRPC probe configuration for the liveness probe. This probe performs a gRPC health check on the container, requiring a port and optionally a service name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number to send the gRPC health check request to. Must be a valid TCP port (1-65535). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the gRPC service to check. If not specified, the default is an empty string, which means the gRPC server's default health check is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      The HTTP GET action to perform for the liveness probe. It configures a request to a specific port and path to determine container liveness. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP GET request. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          Custom headers to set in the HTTP GET request for the liveness probe. Each header object contains a name and a value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The header field name for the HTTP header sent in the liveness probe's HTTP GET request. For example, 'Accept' or 'Authorization'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header to be sent in the liveness probe request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The HTTP path to request on the server for the liveness probe. Defaults to '/'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port to access on the container. This is a string that can be an integer port number or the name of a port defined in the container's ports. Valid port numbers range from 1 to 65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          Protocol for the HTTP request. Must be either 'HTTP' or 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The URI scheme to use for the HTTP request. Valid values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before liveness probes are initiated. Must be a non-negative integer (default 0). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      The frequency in seconds at which the liveness probe is performed. A higher value means less frequent checks. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      Minimum consecutive successes for the probe to be considered successful after having failed. For liveness probes, this value must be 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Specifies a TCP socket probe action, which checks the container's health by attempting to open a TCP connection to the port defined in the `port` field of this object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the TCP socket liveness probe. If not specified, it defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port number to connect to when performing the liveness probe. This can be a numeric port (e.g., '8080') or a named port defined in the container's ports, and must be in the range 1-65535 if numeric. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The duration in seconds the container is allowed to terminate gracefully after a probe failure before being forcefully stopped. If set, this value overrides the pod-level termination grace period. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the ephemeral container. Must be unique within the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ports" type="list(object)">
                  List of ports to expose from the container. For ephemeral containers, this field is not allowed and specifying it will be rejected by the API server. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="container_port" type="number">
                      The port number that the container listens on, used to expose the container's service. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_ip" type="string">
                      The host IP address to bind the port to. Defaults to 0.0.0.0 if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_port" type="number">
                      The port number on the host node that maps to the container port for this ephemeral container. If specified, the container's port is exposed directly on the host's network at this port. Valid range is 1-65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The optional name of the container port. Must be a valid IANA_SVC_NAME (up to 15 characters, lowercase letters, digits, and hyphens, not starting or ending with a hyphen). This name can be used to reference the port in other Kubernetes resources, such as services. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="protocol" type="string">
                      The network protocol for this port. Supported values are TCP, UDP, and SCTP; defaults to TCP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="readiness_probe" type="object">
                  Defines a readiness probe for the ephemeral container. This probe determines when the container is ready to accept traffic and includes settings such as initial delay, period, timeout, and failure thresholds. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the command to run inside the container for the readiness probe. A successful probe (exit code 0) indicates the container is ready. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to run inside the container for the exec readiness probe. It is a list of strings representing the command and its arguments, executed directly without a shell unless the first element is '/bin/sh' or similar. The working directory is root ('/') in the container's filesystem, and the command runs as the container's default user (subject to securityContext overrides). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures of the readiness probe before the container is considered not ready. Once the probe fails this many times, the container is removed from service endpoints. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      Specifies the gRPC action for the probe, configuring a gRPC health check against the container. It includes parameters such as the port to query (required) and an optional service name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number of the gRPC server to connect to. Required when using a gRPC probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          Specifies the name of the gRPC service to query for health status. If empty, the health check applies to the entire gRPC server. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies an HTTP GET request to check container readiness. Includes settings such as the path, port, HTTP headers, and scheme. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP GET request. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          Optional list of HTTP headers to send with the request. Each header is an object with 'name' and 'value' fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              Name of the HTTP header to include in the probe request, e.g., 'Authorization'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header to include in the readiness probe request. This is paired with the corresponding header name in the httpHeaders list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The HTTP path to request when performing the readiness probe. For example, '/healthz'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or name of the port on the container to connect to for the HTTP GET request. This accepts a numeric value as a string or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol (scheme) used for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for connecting to the host. Defaults to HTTP. Allowed values are HTTP and HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before the readiness probe is initiated. Defaults to 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      How often (in seconds) to perform the readiness probe. Defaults to 10 seconds; the minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      Minimum consecutive successes required for the probe to be considered successful after having failed. Defaults to 1. For readiness probes, this value must be 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Specifies a TCP socket check for the readiness probe. The kubelet attempts to open a TCP connection to the container on the specified port to determine if the container is ready. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          Optional host name or IP address to connect to for the TCP socket probe. If not specified, the pod IP is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP socket port to probe on the container. It can be a numeric port or a named port defined in the container's ports. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      Optional number of seconds the pod will wait after receiving a termination signal before it is forcefully terminated. Defaults to 30 seconds if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resize_policy" type="list(object)">
                  List of resource resize policies for the ephemeral container, defining which resources (CPU, memory) can be resized and the restart behavior if a resize fails. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="resource_name" type="string">
                      The name of the resource (e.g., cpu or memory) to which this resize policy applies, as part of the Kubernetes ephemeral container resize policy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="restart_policy" type="string">
                      Specifies the restart policy when a container's resource is resized. Allowed values are 'NotRequired' and 'RestartContainer'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resources" type="object">
                  Resource requirements for the ephemeral container, defining requests and limits for compute resources such as CPU and memory. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="claims" type="list(object)">
                      Claims lists the names of resources, defined in spec.resourceClaims, that are used by this ephemeral container. This is an alpha field and requires enabling the DynamicResourceAllocation feature gate. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the ResourceClaim this container references. The ResourceClaim must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="request" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="limits" type="map(string)">
                      Limits specifies the maximum amount of compute resources allowed for the ephemeral container. Each key is a resource name (e.g., cpu, memory) and the corresponding value is a quantity string (e.g., '100m', '1Gi'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="requests" type="map(string)">
                      Requests is a map of resource names to quantities, specifying the minimum amount of each resource the ephemeral container needs. For example, 'cpu: 100m' and 'memory: 128Mi'. The keys are standard Kubernetes resource names (cpu, memory, hugepages, etc.) and the values are strings in Kubernetes quantity format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="restart_policy" type="string">
                  Specifies the restart policy for the ephemeral container. Allowed values are Always, OnFailure, and Never. Defaults to Always if not set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="restart_policy_rules" type="list(object)">
                  Optional; if omitted, computed by Kubernetes.
                  <Expandable title="properties">
                    <ResponseField name="action" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="exit_codes" type="object">
                      Specifies the container exit code(s) that this restart policy rule matches. When the ephemeral container exits with one of these codes, the restart action defined in the rule is triggered. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="operator" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="values" type="list(number)">
                          A list of integer exit codes that define which container terminations this restart policy rule applies to. If a container exits with one of these codes, the restart policy rule is followed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="security_context" type="object">
                  SecurityContext holds security configuration for the ephemeral container, including parameters like runAsUser, runAsGroup, capabilities, SELinux options, and whether the root filesystem is read-only. It controls the container's access to the host and privileges at runtime. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="allow_privilege_escalation" type="bool">
                      AllowPrivilegeEscalation controls whether a process can gain more privileges than its parent process. When set to false, the container's processes are prevented from gaining additional privileges (e.g., via setuid or setgid binaries). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="app_armor_profile" type="object">
                      Configures AppArmor for the container. Defines the profile type (RuntimeDefault, Localhost, Unconfined) and optionally references a profile on the node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          The name of the AppArmor profile to be loaded from the node's localhost, used only when the profile type is 'Localhost'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          Type of AppArmor profile to use. Allowed values are 'Localhost' (use the profile specified in localhostProfile), 'RuntimeDefault' (use the container runtime's default AppArmor profile), and 'Unconfined' (disable AppArmor enforcement). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="capabilities" type="object">
                      Capabilities to add or drop for the ephemeral container's process. The object contains 'add' and 'drop' lists of Linux capabilities. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="add" type="list(string)">
                          The list of Linux capabilities to add to the container's security context. Valid entries are Linux capability names like CAP_NET_ADMIN or CAP_SYS_TIME. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="drop" type="list(string)">
                          The list of capabilities to drop from the container's permitted capabilities. For example, can drop 'NET_ADMIN' or 'SYS_TIME'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="privileged" type="bool">
                      Specifies whether the container runs in privileged mode. When true, the container has access to all devices on the host and essentially runs as root on the host. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="proc_mount" type="string">
                      Sets the proc mount type for the ephemeral container. Allowed values are 'Default' and 'Unmasked'; 'Unmasked' removes the masking of /proc to expose all host proc information, while 'Default' applies the standard restrictions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only_root_filesystem" type="bool">
                      Mounts the container's root filesystem as read-only. When set to true, the root filesystem cannot be modified, enhancing security by preventing unauthorized changes to container filesystem content. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_group" type="number">
                      The GID (group ID) to run the container's entrypoint process with. If omitted, defaults to the container image's group ID. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_non_root" type="bool">
                      Indicates that the container must run as a non-root user. If true, the kubelet will validate the image at runtime to ensure that it does not run as UID 0 (root) and will fail to start the container if it does. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_user" type="number">
                      The UID (user ID) that the container's entrypoint process will run as. Defaults to the user specified in the container image if not set. Must be a non-negative integer. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="se_linux_options" type="object">
                      SELinux options for the ephemeral container, specifying the level, role, type, and user as part of its security context. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="level" type="string">
                          Level is the SELinux level label that applies to the container. For example, s0:c123,c456. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="role" type="string">
                          The SELinux role label that applies to the container. It defines the role portion of the SELinux context, such as 'system_r'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The SELinux type label that applies to the container. This specifies the SELinux type, such as 'container_t'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="user" type="string">
                          The SELinux user label for the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="seccomp_profile" type="object">
                      SeccompProfile configures the seccomp profile for this ephemeral container. It can specify a runtime default, unconfined, or a localhost profile via its type and localhostProfile fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          localhostProfile indicates a profile defined in a file on the node should be used. The profile must be preconfigured on the node to work. Must be a descending path, relative to the kubelet's configured seccomp profile location. Must only be set if type is 'Localhost'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The type of seccomp profile to use. Allowed values are RuntimeDefault, Unconfined, and Localhost. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="windows_options" type="object">
                      Windows-specific security options for the container. Configures settings such as GMSA credentials, host process, and the Windows user name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="gmsa_credential_spec" type="string">
                          The name of the GMSA credential spec to use for the container, enabling Windows GMSA authentication. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="gmsa_credential_spec_name" type="string">
                          The name of the GMSA credential spec to use for the Windows container, as defined in the Kubernetes Windows security context options. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="host_process" type="bool">
                          HostProcess determines if a container should be run as a HostProcess container. All containers in the pod must have the same value, and the default is false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_user_name" type="string">
                          The username in Windows to run the entrypoint of the container process. Defaults to the user specified in image metadata if unspecified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="startup_probe" type="object">
                  Configures a startup probe for the ephemeral container. This probe determines when the container has started successfully; until it succeeds, liveness and readiness probes are not activated, and if it fails, the container is restarted according to its restart policy. Useful for slow-starting containers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies a command to run inside the container for the startup probe. The probe is considered successful if the command exits with status 0; any other exit code indicates a failure. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          Command line to execute inside the container for the startup probe. The working directory is '/' in the container's filesystem. The command is executed directly, not via a shell, so shell constructs like pipes are not supported. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      Minimum consecutive failures for the startup probe to be considered failed after having succeeded. Defaults to 3. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      grpc specifies a gRPC health check to perform against the container. It is an object containing the port to check and, optionally, the service name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number on which the gRPC server is listening. Must be a valid TCP port (1-65535). This field is required for a gRPC probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          Specifies the name of the service to place in the gRPC HealthCheckRequest. If not specified, the default behavior is defined by gRPC. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Configures an HTTP GET request that the startup probe performs against the container. This is an object containing the host, port, path, and optional HTTP headers for the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name or IP address to connect to for the HTTP GET request. If not specified, defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          List of custom HTTP headers to send with the probe request. Each item specifies a header name and value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to send in the HTTP request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header. This string is sent as the header's value when the probe performs the HTTP request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The URL path to access on the HTTP server when performing a GET request. Defaults to '/' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port to send the HTTP request to. Accepts either a numeric port (1-65535) or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          Protocol for the HTTP GET request. Must be HTTP or HTTPS. Defaults to HTTP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for connecting to the host. Defaults to HTTP, and valid values are HTTP and HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      The number of seconds after the container has started before the startup probe is initiated. Defaults to 0; minimum value is 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      How often, in seconds, to perform the startup probe. Minimum value is 1; default is 10. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      The minimum consecutive successes required for the startup probe to be considered successful after having failed. Defaults to 1 and must be 1 for startup probes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Configures a TCP socket checker for the startup probe. It defines the port (and optionally host) to check for TCP connectivity, indicating the container has started. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          Host name to connect to for the TCP socket probe. If not specified, the pod IP is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          Number or name of the port to access on the container. The number must be in the range 1 to 65535, or a name must be a valid IANA_SVC_NAME. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The grace period in seconds that Kubernetes allows a pod to run after receiving a termination signal before forcefully killing it. Defaults to 30 seconds if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      The number of seconds after which the startup probe times out. Defaults to 1 second. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="stdin" type="bool">
                  Whether this ephemeral container should have a stdin stream allocated for it. When true, the container runtime allocates a stdin channel, enabling interactive access. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stdin_once" type="bool">
                  If true, the container runtime closes the stdin channel after the first attach session disconnects, and stdin remains closed until the container is restarted. When false, stdin remains open across multiple attach sessions if stdin is enabled. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="target_container_name" type="string">
                  The name of the target container within the pod that this ephemeral container shares its process namespace with. If not specified, the last started container is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_path" type="string">
                  Path at which the container's termination message is written, used to record the reason for container termination. Defaults to /dev/termination-log. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_policy" type="string">
                  Provides the policy for how the termination message of the ephemeral container is captured -- either from the termination message file or through container logs under error conditions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="tty" type="bool">
                  Indicates whether a TTY (pseudo-TTY) should be allocated for the ephemeral container. When true, the container will have a terminal attached, enabling interactive sessions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volume_devices" type="list(object)">
                  List of block devices to be used by the ephemeral container. Each item defines the name of a volume and the device path inside the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="device_path" type="string">
                      The path inside the container where the volume device is mounted. This is required when specifying a volume device for an ephemeral container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the volume to use. Must match the name of a volume defined in the pod spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="volume_mounts" type="list(object)">
                  List of volume mounts for an ephemeral container. Each item specifies a volume to mount into the container's filesystem, including mount path and read-only options. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="bind_mount_options" type="list(string)">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="mount_path" type="string">
                      Path within the ephemeral container at which the volume should be mounted. Must be an absolute path, e.g., /data. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mount_propagation" type="string">
                      mount_propagation controls how volume mounts are propagated from the host to the container and between containers. Valid values are None (default), HostToContainer, and Bidirectional. It affects the visibility of mounts in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the volume to mount. This must match the name of a volume defined in the pod spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the volume is mounted as read-only. When true, the container can only read from the volume; default is false (read-write). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="recursive_read_only" type="string">
                      Configures recursive read-only mounts for this volume mount. Allowed values: Enabled, IfPossible, Disabled. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path" type="string">
                      Path within the volume from which the container's volume is mounted. Defaults to the volume's root if empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path_expr" type="string">
                      Specifies the path within the volume to mount, with support for environment variable expansion using $(VAR_NAME) syntax, similar to subPath but allowing dynamic paths. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="working_dir" type="string">
                  The working directory for the ephemeral container. If not specified, the container runtime uses the default working directory defined in the container image. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="eviction_responders" type="list(object)">
              evictionResponders reference responders that react to Evictions based on EvictionRequests. Responders should observe and communicate through the Eviction Resource API to help with the graceful termination of a pod. The responders are selected sequentially, according to their specified priority. Responders should periodically report on an eviction progress by updating the .status.responders[].heartbeatTime field of the Eviction object. If this field is not updated within the heartbeat deadline defined by the Eviction API (currently 20 minutes), the eviction is passed over to the next responder with a lower priority. If there is no other responder, the last default imperative-eviction.k8s.io/evictor responder with a priority of 100 will evict the pod using the imperative Eviction API (pods/&lt;name&gt;/eviction subresource). The maximum length of the responders list is 10. Responders are not supported when the pod is part of a PodGroup (.spec.schedulingGroup is set). This field can only be set on creation and is immutable afterwards. Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  Optional; if omitted, computed by Kubernetes.
                </ResponseField>
                <ResponseField name="priority" type="number">
                  Optional; if omitted, computed by Kubernetes.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="host_aliases" type="list(object)">
              HostAliases is an optional list of hosts and IPs that will be injected into the pod's hosts file if specified. Optional.
              <Expandable title="properties">
                <ResponseField name="hostnames" type="list(string)">
                  The list of hostnames that resolve to the IP address specified in this hostAliases entry. Kubernetes adds these hostnames to the /etc/hosts file of the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ip" type="string">
                  The IP address to which the hostnames in this hostAliases entry should resolve. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="host_ipc" type="bool">
              Use the host's ipc namespace. Optional: Default to false. Optional.
            </ResponseField>
            <ResponseField name="host_network" type="bool">
              Host networking requested for this pod. Use the host's network namespace. When using HostNetwork you should specify ports so the scheduler is aware. When `hostNetwork` is true, specified `hostPort` fields in port definitions must match `containerPort`, and unspecified `hostPort` fields in port definitions are defaulted to match `containerPort`. Default to false. Optional.
            </ResponseField>
            <ResponseField name="host_pid" type="bool">
              Use the host's pid namespace. Optional: Default to false. Optional.
            </ResponseField>
            <ResponseField name="host_users" type="bool">
              Use the host's user namespace. Optional: Default to true. If set to true or not present, the pod will be run in the host user namespace, useful for when the pod needs a feature only available to the host user namespace, such as loading a kernel module with CAP_SYS_MODULE. When set to false, a new userns is created for the pod. Setting false is useful for mitigating container breakout vulnerabilities even allowing users to run their containers as root without actually having root privileges on the host. Optional.
            </ResponseField>
            <ResponseField name="hostname" type="string">
              Specifies the hostname of the Pod If not specified, the pod's hostname will be set to a system-defined value. Optional.
            </ResponseField>
            <ResponseField name="hostname_override" type="string">
              HostnameOverride specifies an explicit override for the pod's hostname as perceived by the pod. This field only specifies the pod's hostname and does not affect its DNS records. When this field is set to a non-empty string: - It takes precedence over the values set in `hostname` and `subdomain`. - The Pod's hostname will be set to this value. - `setHostnameAsFQDN` must be nil or set to false. - `hostNetwork` must be set to false. This field must be a valid DNS subdomain as defined in RFC 1123 and contain at most 64 characters. Optional.
            </ResponseField>
            <ResponseField name="image_pull_secrets" type="list(object)">
              ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec. If specified, these secrets will be passed to individual puller implementations for them to use. More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name of the image pull secret. Must correspond to a Secret in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="init_containers" type="list(object)">
              List of initialization containers belonging to the pod. Init containers are executed in order prior to containers being started. If any init container fails, the pod is considered to have failed and is handled according to its restartPolicy. The name for an init container or normal container must be unique among all containers. Init containers may not have Lifecycle actions, Readiness probes, Liveness probes, or Startup probes. The resourceRequirements of an init container are taken into account during scheduling by finding the highest request/limit for each resource type, and then using the max of that value or the sum of the normal containers. Limits are applied to init containers in a similar fashion. Init containers cannot currently be added or removed. Cannot be updated. More info: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/ Optional.
              <Expandable title="properties">
                <ResponseField name="args" type="list(string)">
                  Arguments to the command that this init container runs. These are passed to the container's entrypoint process. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="command" type="list(string)">
                  The entrypoint array for the init container, overriding the image's ENTRYPOINT. This is a list of strings, e.g., ['/bin/sh', '-c']. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="env" type="list(object)">
                  List of environment variables to set in the container. Each item defines a variable name and optionally a value or a source for the value (e.g., config map, secret, or field reference). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the environment variable. Must be a valid C identifier (e.g., MY_VAR). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value to set for the environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value_from" type="object">
                      Specifies a source for the environment variable's value. It allows referencing a ConfigMap key, Secret key, a field of the pod (e.g., metadata.name), or a resource (e.g., limits.cpu). Only one of configMapKeyRef, secretKeyRef, fieldRef, or resourceFieldRef can be set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="config_map_key_ref" type="object">
                          Selects a key from a ConfigMap to populate the value of the environment variable. The object contains the ConfigMap name and the key to retrieve. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key to select from the referenced ConfigMap. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the ConfigMap being referenced. Must match a ConfigMap name in the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Optional specifies whether the ConfigMap or its key must be defined. Defaults to false, meaning the key must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="field_ref" type="object">
                          Selects a field of the pod (e.g., metadata.name, status.podIP, spec.nodeName) to use as the value of the environment variable. The specific field path is provided in the nested fieldPath property. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="api_version" type="string">
                              Version of the schema the FieldPath is written in terms of, defaults to 'v1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="field_path" type="string">
                              Path of the field to select from the pod spec, such as 'metadata.name' or 'spec.nodeName'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="file_key_ref" type="object">
                          Optional; if omitted, computed by Kubernetes.
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Indicates whether the referenced file key is optional. If true, the container can start even when the key does not exist; if false, the key must be present. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              The path to the file that contains the key to be used as the value for the environment variable. This is part of a file-based key reference, where the file is read to extract the value at the specified key. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="volume_name" type="string">
                              The name of the volume that contains the file referenced by the file key reference. This volume must be defined in the same pod's volume list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="resource_field_ref" type="object">
                          ResourceFieldRef selects a resource field of the container, such as limits.cpu or requests.memory, for use as the value of the environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="container_name" type="string">
                              The name of the container for which to select a resource. If omitted, defaults to the container where this environment variable is defined. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="divisor" type="string">
                              Divisor is the divisor for the resource value in a resource field reference. It specifies the scaling factor applied to the selected resource quantity (e.g., '1' yields the raw value, '1k' divides by 1000, '1Mi' divides by 1,048,576). Defaults to '1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="resource" type="string">
                              The resource name to select from the pod's resource specification, such as 'requests.cpu' or 'limits.memory'. This is used in a resource field ref to populate the environment variable's value from a container resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="secret_key_ref" type="object">
                          Selects a key of a Secret in the pod's namespace. This is a SecretKeySelector object that specifies the Secret name and key to use, and may include an 'optional' flag to control behavior when the Secret or key is missing. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key name within the referenced Secret to retrieve the value from. Must be a valid secret key. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the Secret to select from. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the Secret or its key must be defined. If true, and the secret or key is missing, the environment variable is omitted and the container can start; if false (default), a missing secret or key causes an error. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="env_from" type="list(object)">
                  envFrom is a list of sources to populate environment variables in the container. Each entry can reference a ConfigMap or Secret (or all keys), or use a prefix to prepend to variable names. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="config_map_ref" type="object">
                      Reference to a ConfigMap from which to populate environment variables for this container. The ConfigMap is referenced by its name within the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the ConfigMap to reference. This ConfigMap must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Specifies whether the referenced ConfigMap must exist. If true, a missing ConfigMap is allowed; if false (default), the ConfigMap must exist or the pod will fail to start. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="prefix" type="string">
                      An optional identifier to prepend to each key of the referenced ConfigMap or Secret. Must be a valid C identifier. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      Reference to a Kubernetes Secret in the same namespace, used to populate environment variables for the init container. The Secret's data keys become environment variable names and their values become the variable values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the Secret to populate environment variables from. The Secret must exist in the same namespace as the Pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Indicates whether the referenced Secret must be defined. When set to true, the Secret may be absent and the container will still start; when false or unset, the Secret must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="image" type="string">
                  The container image to run for the init container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="image_pull_policy" type="string">
                  Specifies when the kubelet should pull the container image for this init container. Valid values are Always, IfNotPresent, and Never. Defaults to Always if the image tag is :latest, otherwise IfNotPresent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="lifecycle" type="object">
                  Lifecycle configuration for the init container, specifying postStart and preStop hooks to execute at container start and shutdown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="post_start" type="object">
                      post_start defines the postStart hook for the container lifecycle. This hook is executed immediately after the container is started and can be used to run initialization commands or checks. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          Exec runs a command inside the container when the postStart lifecycle hook is triggered. The command is specified as a list of strings in the 'command' field. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              The command to execute in the container for the postStart lifecycle hook. It is executed directly, not through a shell; the working directory is '/' in the container's filesystem. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          Configures an HTTP GET request to be sent to the container as part of the postStart lifecycle hook. The request is sent to a specific endpoint on the container to trigger a post-start action. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              Host name to connect to for the HTTP GET request; defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              Custom HTTP headers to include in the HTTP GET request to the postStart handler. Each header object requires a name and value string. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The name of the HTTP header to include in the HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header field. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Path to access on the HTTP server. Defaults to '/'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port number or named port on the container to which the HTTP GET request is sent. Specify the port number as a string, or use a named container port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              The protocol (HTTP or HTTPS) to use for the HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The scheme to use for the connection, either HTTP or HTTPS. Defaults to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          Configures a sleep action for the postStart lifecycle hook, pausing container execution for a specified number of seconds before continuing. The nested 'seconds' field defines the duration of the sleep. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              The number of seconds to sleep for the sleep lifecycle hook action. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          Specifies a TCP socket to connect to as part of the postStart lifecycle hook. The handler attempts to open a TCP connection to the specified host and port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name or IP address to connect to for the TCP socket check. If not specified, the pod IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port to connect to on the container. Accepts a numeric port or a named port defined in the container's ports. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="pre_stop" type="object">
                      preStop is a lifecycle hook that is called immediately before a container is terminated. It can be used to gracefully stop the container or perform cleanup actions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          Exec specifies the command to run in the container as part of the preStop lifecycle hook. It is an ExecAction object containing the command to execute. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              The command to run in the container during the preStop lifecycle hook. It is an array of strings; the first element is the command and subsequent elements are arguments. The command is executed directly, not through a shell, and the working directory is root ('/') in the container's filesystem. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          Represents an HTTP GET request to perform as the container's preStop lifecycle hook. This object defines the endpoint and headers for the request sent to the container before termination. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the HTTP GET request. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              List of HTTP headers to include in the HTTP GET request. Each header object contains `name` and `value` fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The name of the HTTP header to include in the HTTP GET request for this preStop lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header to include in the HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Path to access on the HTTP server. Defaults to `/`. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port to send the HTTP request to. This can be a numeric port number or a named port defined in the container's port list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              The protocol (scheme) to use for the HTTP GET request. Allowed values are HTTP and HTTPS; defaults to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The scheme to use for the HTTP GET request. Allowed values are HTTP and HTTPS, with HTTP as the default. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          Represents a sleep action in the pre-stop lifecycle hook. It specifies a duration in seconds to wait before the container is terminated, allowing for graceful shutdown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              Specifies the number of seconds the container should sleep before it is terminated as part of the preStop hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          Specifies a TCP socket action for the pre-stop lifecycle hook. This action attempts to open a TCP connection to a specified host and port, and the hook is considered successful when the connection is established. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The hostname or IP address to connect to for the TCP socket check. If not specified, the pod's IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port number to connect to for the preStop lifecycle hook. Can be specified as a numeric port (e.g., "8080") or a named port defined in the container's ports. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="stop_signal" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="liveness_probe" type="object">
                  Liveness probe configuration for the init container. In Kubernetes, liveness probes are not supported for init containers and this field is ignored. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec is the exec-based action for the liveness probe, specifying a command to run inside the container to determine its health status. The command is defined as a list of arguments, which are executed directly in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to run inside the container for the exec probe. It is a list of strings, where the first element is the command path and the rest are arguments. The kubelet executes this command periodically to determine whether the container is healthy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures of the liveness probe before the container is restarted. The minimum value is 1; if not specified, the default is 3. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      Specifies a gRPC health check action for the liveness probe. It contains a required port number and an optional service name to check, as defined by Kubernetes' GRPCAction. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number of the gRPC service to check. This should be the port on which the gRPC server is listening for health checks. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the gRPC service to check in the health check request. If omitted, the probe checks the overall health of the server. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Defines an HTTP GET request used by the kubelet to perform a liveness check on the container. It specifies the path, port, and optional HTTP headers for the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP GET probe. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          Specifies custom HTTP headers to include in the request sent by the liveness probe. Each header is an object with `name` and `value` fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to include in the liveness probe's HTTP GET request. For example, `X-Custom-Header`. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header. This is the actual string sent as the header's value; for example, for a header 'Authorization: Bearer token', the value is 'Bearer token'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          Path to access on the HTTP server for the probe request. Defaults to '/' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          Specifies the port to use for the HTTP GET request. Accepts a numeric port (1-65535) or the name of a container port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          Specifies the protocol used for the HTTP GET liveness probe, typically 'HTTP' or 'HTTPS'. Defaults to 'HTTP' if not set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for the connection, either 'HTTP' or 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before liveness probes are initiated. Defaults to 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      How often (in seconds) to perform the liveness probe on the init container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      Minimum consecutive successes for the liveness probe to be considered successful after a failure. For liveness probes, this value must be 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Specifies the TCP socket action for the liveness probe. The probe will attempt to open a TCP connection to the container on the specified port to determine if it is healthy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the TCP socket probe. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or named port on the container to probe for liveness. In a tcpSocket probe, it specifies the TCP port to connect to; can be a numeric port or a port name defined in the container's ports. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the liveness probe times out. Defaults to 1 second; minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the init container. Must be a valid DNS label and unique within the pod. Required. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ports" type="list(object)">
                  List of ports exposed by the init container. Each port object specifies a name, container port number, protocol, and optionally host IP and host port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="container_port" type="number">
                      The port number on which the container listens. In Kubernetes, the containerPort field specifies the network port that the container expects to receive traffic on, with a valid range of 1-65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_ip" type="string">
                      The host IP address to bind the port to. Defaults to 0.0.0.0 (all interfaces). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_port" type="number">
                      The port on the host node to which the container port is mapped. If specified, this makes the container accessible from outside the node via the node's IP address. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      Name of the port, used for referencing the port in the pod spec. Must be an IANA_SVC_NAME (up to 15 characters, lowercase alphanumeric, hyphens, and dots). Must be unique among all ports in the same container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="protocol" type="string">
                      The network protocol used by the port. Allowed values are TCP, UDP, and SCTP. Defaults to TCP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="readiness_probe" type="object">
                  Readiness probe configuration for an init container. It defines a health check that determines when the init container is considered ready, though init containers typically run to completion and this probe is rarely used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the action to take, which is to run a command inside the container. The exit status of the command is used to determine whether the probe succeeded (exit code 0 indicates success). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to execute inside the container for this exec readiness probe. It is a list of strings, where the first element is the executable and subsequent elements are arguments. The probe succeeds if the command exits with status code 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      Number of consecutive failures of the readiness probe required before the container is considered not ready. Defaults to 3; minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      Specifies a gRPC probe action for this readiness check. It defines the port and optional service name used to perform a gRPC health check against the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          Specifies the port number to use for the gRPC health check in the container's readiness probe. Must be a valid TCP port number (1-65535). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the service to place in the gRPC HealthCheckRequest. If not specified, defaults to an empty string. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Performs an HTTP GET request against the container to check its readiness. A response with a status code in the 2xx or 3xx range indicates success. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The hostname to connect to for the HTTP GET request. Defaults to the pod IP. You can set this to override the Host header or connect to a specific host. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          Optional list of custom HTTP headers to include in the GET request. Each object specifies a header name and value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to include in the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header to include in the readiness probe's HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The URL path (e.g., /healthz) that the HTTP probe requests from the container. An empty path results in a request to the server root. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or name to use for the HTTP GET request. Must be a valid port number (1-65535) or an IANA service name; defaults to the container's first allocated port if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The HTTP protocol (scheme) used for the request, such as 'HTTP' or 'HTTPS'. Defaults to 'HTTP' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The URL scheme to use for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before the readiness probe is initiated. This is the initial delay, in seconds, before the first probe runs. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      The interval in seconds between each readiness probe execution. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      Minimum consecutive successes for the readiness probe to be considered successful after having failed. Defaults to 1. For readiness probes, can be set higher to require multiple successful checks, but must be 1 for liveness and startup probes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Configures a TCP socket check for the readiness probe. The probe attempts to open a TCP connection to the container on the specified port, optionally using a specified host. The container is considered ready if the connection succeeds. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          Host name to connect to for the TCP socket probe. If not specified, the pod IP is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to check on the container. Can be a port number (1-65535) or a named port defined in the container spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the readiness probe times out. If the probe does not complete within this duration, it is considered failed. Minimum value is 1; defaults to 1 second if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resize_policy" type="list(object)">
                  List of resource resize policies for the init container. Each policy specifies a resource name (e.g., cpu, memory) and a restart policy (NotRequired or RestartContainer) that determines whether the container must be restarted when the resource is resized. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="resource_name" type="string">
                      The name of the resource (e.g., cpu or memory) to which this resize policy applies. Valid values are "cpu" and "memory". Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="restart_policy" type="string">
                      The restart policy to apply when a container's resource resize is requested. Valid values are 'NotRequired' (no restart needed) and 'RestartContainer' (restart the container to apply the new resource requirements). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resources" type="object">
                  Resource requirements for the init container, including requests and limits for compute resources such as CPU and memory. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="claims" type="list(object)">
                      List of resource claims that can be referenced by the container. Each item contains a 'name' field that refers to a ResourceClaim object in the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the ResourceClaim resource that this container requests. The referenced ResourceClaim must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="request" type="string">
                          The name of the specific request in the ResourceClaim. If not set, the default request is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="limits" type="map(string)">
                      Limits describes the maximum amount of compute resources allowed for the container. Each key is a resource name such as 'cpu' or 'memory', and the value is a quantity string (e.g., '500m', '1Gi'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="requests" type="map(string)">
                      Map of resource names to quantities, specifying the amount of compute resources requested for the init container (e.g., cpu, memory). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="restart_policy" type="string">
                  Specifies the restart policy for the init container. In Kubernetes, this field is only supported for sidecar init containers, and must be set to 'Always' if specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="restart_policy_rules" type="list(object)">
                  Optional; if omitted, computed by Kubernetes.
                  <Expandable title="properties">
                    <ResponseField name="action" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="exit_codes" type="object">
                      Optional; if omitted, computed by Kubernetes.
                      <Expandable title="properties">
                        <ResponseField name="operator" type="string">
                          The comparison operator that determines how the specified exit codes are evaluated for the restart policy rule. Valid values include 'In' and 'NotIn'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="values" type="list(number)">
                          A list of numeric exit codes that this restart policy rule applies to. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="security_context" type="object">
                  SecurityContext defines the security configuration for the init container, including settings like runAsUser, capabilities, and SELinux options, controlling the access and privileges of the container process. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="allow_privilege_escalation" type="bool">
                      Controls whether the init container's processes can gain more privileges than their parent (e.g., via setuid/setgid binaries). When false, the container runs with no_new_privs, preventing privilege escalation. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="app_armor_profile" type="object">
                      The AppArmor profile to apply to the init container. The type must be 'Localhost', 'RuntimeDefault', or 'Unconfined'. If type is 'Localhost', the 'localhostProfile' field must be set to the name of the profile. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          Specifies the name of an AppArmor profile that is pre-loaded on the node. This field is used only when the type is 'Localhost'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          Specifies the type of AppArmor profile to apply to the container. Valid values are: Localhost, RuntimeDefault, Unconfined. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="capabilities" type="object">
                      Configures Linux capabilities for the container. Allows adding or dropping specific capabilities via the 'add' and 'drop' subfields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="add" type="list(string)">
                          Adds the specified Linux capabilities to the init container's capability set. Each entry is a capability name (e.g., 'CAP_NET_ADMIN') that will be granted in addition to the default capabilities. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="drop" type="list(string)">
                          List of Linux capabilities to drop from the container's default set. For example, 'ALL' or specific capability names like 'NET_RAW'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="privileged" type="bool">
                      Indicates whether the container runs in privileged mode. Processes in privileged containers are equivalent to root on the host. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="proc_mount" type="string">
                      Specifies the proc mount type for the container. Valid values are 'Default' (default behavior) and 'Unmasked' (allows viewing all processes). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only_root_filesystem" type="bool">
                      Indicates whether the container's root filesystem is mounted as read-only. When set to true, the container cannot write to its root filesystem, which helps prevent modifying system files and improves security. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_group" type="number">
                      Specifies the group ID (GID) that the container's main process runs as. If omitted, the default group from the container image is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_non_root" type="bool">
                      Indicates whether the container must run as a non-root user. If true, the kubelet will reject the container if it is configured to run as UID 0 (root). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_user" type="number">
                      The numerical user ID (UID) that the init container's process will run as. If not specified, the default user in the container image is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="se_linux_options" type="object">
                      SELinux options for the container, which specify the SELinux labels (user, role, type, and level) applied to the container's processes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="level" type="string">
                          SELinux level label applied to the container, such as s0 or s0:c1,c2. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="role" type="string">
                          SELinux role label for the container. This is part of the SELinux options applied to the init container's security context, used to enforce mandatory access control policies. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The SELinux type label for the container, such as 'spc_t' or 'svirt_lxc_net_t'. This is part of the SELinux security context (user:role:type:level). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="user" type="string">
                          The SELinux user label applied to the container process. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="seccomp_profile" type="object">
                      Configures the seccomp profile for the container. The profile type can be RuntimeDefault, Localhost, or Unconfined; if Localhost, the localhostProfile field specifies the path to the profile on the node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          Specifies the path to a seccomp profile on the node. This field is used only when the seccomp profile type is 'Localhost'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The seccomp profile type. Valid values are RuntimeDefault, Unconfined, and Localhost. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="windows_options" type="object">
                      Windows-specific security options for the container. These options are only applicable to containers running on Windows. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="gmsa_credential_spec" type="string">
                          Specifies the Group Managed Service Account (GMSA) credential spec to use, provided as a JSON string. This allows the container to access resources with the specified GMSA identity. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="gmsa_credential_spec_name" type="string">
                          The name of the GMSA (Group Managed Service Account) credential spec to use for the Windows container. This is part of the Windows security context options for the init container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="host_process" type="bool">
                          Whether the container should run as a HostProcess container. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_user_name" type="string">
                          The username in Windows to run the container's entrypoint process. If unspecified, the container uses the default user. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="startup_probe" type="object">
                  The startup probe configuration for the init container. This object defines the probe that determines when the container has started successfully, using the same probe mechanisms (such as HTTP, TCP, or command execution) as other Kubernetes probes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the action to perform: running a command inside the container. The command's exit status is used to determine probe success. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          Command is the command line to execute inside the container. The exit status of the command is the probe result: 0 indicates success, non-zero indicates failure. The command is executed directly, not through a shell. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures of the startup probe that must occur before the container is considered failed and restarted. This value must be an integer. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      Specifies a gRPC health check action for this startup probe, allowing the kubelet to verify container health via gRPC according to the gRPC Health Checking Protocol. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number on which the gRPC server is listening. Must be an integer between 1 and 65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          Specifies the name of the service to place in the gRPC HealthCheckRequest. If not specified, the default is the container name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies an HTTP GET request performed against the container for the startup probe. The probe is considered successful if the HTTP response code is between 200 and 399. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP GET request. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          A list of custom HTTP headers to include in the startup probe's HTTP GET request. Each item defines a header with a `name` and a `value`. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to include in the startup probe's HTTP request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header that is sent in the startup probe's HTTP GET request. This pairs with the header name to form a key-value pair. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The URL path to access on the HTTP server. For example, '/healthz'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or named port on the container that the HTTP GET request will be sent to. Must be a valid integer port number or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol to use for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for connecting to the host. Defaults to HTTP. Allowed values are HTTP and HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before the startup probe is initiated. If not specified, defaults to 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      Number of seconds between startup probe attempts. Minimum value is 1; defaults to 10. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      Number of consecutive successes required for the startup probe to be considered successful after having failed. In Kubernetes, this value must be 1 for startup probes; it defaults to 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Specifies the TCP socket action for the startup probe. The probe attempts to open a TCP connection to the container's IP address on a specified port. If the connection succeeds, the probe is considered successful. Contains 'port' (required) and 'host' (optional) fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name or IP address to connect to for the TCP socket probe. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to probe, which can be a numeric port (e.g., 8080) or a named port defined in the container's port list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The duration in seconds the pod needs to terminate gracefully upon deletion. This value may be decreased in delete requests, and defaults to 30 seconds if not specified. It applies to all containers in the pod, including init containers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="stdin" type="bool">
                  Indicates whether the container runtime should keep the container's standard input (stdin) open. This allows a process to read from stdin, typically for interactive sessions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stdin_once" type="bool">
                  Whether the container runtime should close the stdin channel after it has been opened by a single attach. If true, stdin is opened on container start, remains open until the first client disconnects, and then stays closed until the container restarts. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_path" type="string">
                  Path to a file where the container's termination message is written. Defaults to /dev/termination-log. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_policy" type="string">
                  Specifies how the termination message is derived. Supported values: 'File' (default) and 'FallbackToLogsOnError'. When set to 'FallbackToLogsOnError', the termination message is read from the container's log if the message file is empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="tty" type="bool">
                  Indicates whether a TTY (teletypewriter) should be allocated to the container, enabling interactive terminal access. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volume_devices" type="list(object)">
                  The list of block device volume mounts for the init container. Each item maps a volume name to a device path at which the raw block device is exposed inside the container. Typically used when a volume's volumeMode is 'Block'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="device_path" type="string">
                      The path inside the container where the block device is mounted, such as /dev/xvda. This is the device path that the container will use to access the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the volume that this device refers to. Must match the name of a volume defined in the pod spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="volume_mounts" type="list(object)">
                  List of volumes to mount into the init container's filesystem. Each volume mount references a volume defined in the pod spec and specifies the mount path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="bind_mount_options" type="list(string)">
                      List of mount options for a bind mount, such as 'ro' (read-only), 'rw' (read-write), 'nosuid', 'nodev', etc. These options control the behavior of the mounted filesystem. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mount_path" type="string">
                      The path inside the container at which the volume should be mounted. This is a required field for each volume mount. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mount_propagation" type="string">
                      Defines how mount propagation is set for this volume mount. Allowed values are 'None', 'HostToContainer', and 'Bidirectional'. If not specified, 'None' is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the volume to mount into the init container. This must match the name of a volume defined in the pod's spec.volumes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the volume mount is read-only. When set to true, the container can only read from the mounted volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="recursive_read_only" type="string">
                      Specifies whether the volume mount is recursive read-only. When enabled, the mount and all of its subpaths are read-only. This corresponds to the Kubernetes `recursiveReadOnly` field. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path" type="string">
                      Specifies a sub-path inside the volume to mount instead of the volume's root. The sub-path must already exist before the container starts, and it is relative to the volume's root directory. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path_expr" type="string">
                      Specifies a sub-path inside the volume to mount, with support for environment variable references using $(VAR_NAME) syntax. This is similar to subPath but expands environment variables at runtime. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="working_dir" type="string">
                  Container's working directory. If not specified, the container runtime's default is used, which might be configured in the container image. Must be an absolute path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="node_name" type="string">
              NodeName indicates in which node this pod is scheduled. If empty, this pod is a candidate for scheduling by the scheduler defined in schedulerName. Once this field is set, the kubelet for this node becomes responsible for the lifecycle of this pod. This field should not be used to express a desire for the pod to be scheduled on a specific node. https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodename Optional.
            </ResponseField>
            <ResponseField name="node_selector" type="map(string)">
              NodeSelector is a selector which must be true for the pod to fit on a node. Selector which must match a node's labels for the pod to be scheduled on that node. More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/ Optional.
            </ResponseField>
            <ResponseField name="os" type="object">
              PodOS defines the OS parameters of a pod. Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string" required>
                  Name is the name of the operating system. The currently supported values are linux and windows. Additional value may be defined in future and can be one of: https://github.com/opencontainers/runtime-spec/blob/master/config.md#platform-specific-configuration Clients should expect to handle additional values and treat unrecognized values in this field as os: null Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="overhead" type="map(string)">
              Overhead represents the resource overhead associated with running a pod for a given RuntimeClass. This field will be autopopulated at admission time by the RuntimeClass admission controller. If the RuntimeClass admission controller is enabled, overhead must not be set in Pod create requests. The RuntimeClass admission controller will reject Pod create requests which have the overhead already set. If RuntimeClass is configured and selected in the PodSpec, Overhead will be set to the value defined in the corresponding RuntimeClass, otherwise it will remain unset and treated as zero. More info: https://git.k8s.io/enhancements/keps/sig-node/688-pod-overhead/README.md Optional.
            </ResponseField>
            <ResponseField name="preemption_policy" type="string">
              PreemptionPolicy is the Policy for preempting pods with lower priority. One of Never, PreemptLowerPriority. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. Defaults to PreemptLowerPriority if unset. Optional.
            </ResponseField>
            <ResponseField name="priority" type="number">
              The priority value. Various system components use this field to find the priority of the pod. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. The higher the value, the higher the priority. Optional.
            </ResponseField>
            <ResponseField name="priority_class_name" type="string">
              If specified, indicates the pod's priority. "system-node-critical" and "system-cluster-critical" are two special keywords which indicate the highest priorities with the former being the highest priority. Any other name must be defined by creating a PriorityClass object with that name. If not specified, the pod priority will be default or zero if there is no default. Optional.
            </ResponseField>
            <ResponseField name="readiness_gates" type="list(object)">
              If specified, all readiness gates will be evaluated for pod readiness. A pod is ready when all its containers are ready AND all conditions specified in the readiness gates have status equal to "True" More info: https://git.k8s.io/enhancements/keps/sig-network/580-pod-readiness-gates Optional.
              <Expandable title="properties">
                <ResponseField name="condition_type" type="string">
                  The condition type that must be satisfied for the pod to be considered ready. This corresponds to a condition in the pod's status. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="resource_claims" type="list(object)">
              ResourceClaims defines which ResourceClaims must be allocated and reserved before the Pod is allowed to start. The resources will be made available to those containers which consume them by name. This is a stable field but requires that the DynamicResourceAllocation feature gate is enabled. This field is immutable. Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name of this resource claim, used to reference the claim from container resource claims (e.g., in `resources.claims`). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="resource_claim_name" type="string">
                  The name of the ResourceClaim referenced in the pod. This name must be unique within the pod and is used by containers to bind to the claimed resource via their resource claim references. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="resource_claim_template_name" type="string">
                  The name of a ResourceClaimTemplate that provides the resource claim for this pod. The template is used to dynamically create a ResourceClaim. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="resources" type="object">
              ResourceRequirements describes the compute resource requirements. Optional.
              <Expandable title="properties">
                <ResponseField name="claims" type="list(object)">
                  Claims lists the names of resources, defined in spec.resourceClaims, that are used by this container. This field depends on the DynamicResourceAllocation feature gate. This field is immutable. It can only be set for containers. Optional.
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the ResourceClaim resource (in the same namespace) that this container requests. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="request" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="limits" type="map(string)">
                  Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/ Optional.
                </ResponseField>
                <ResponseField name="requests" type="map(string)">
                  Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/ Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="restart_policy" type="string">
              Restart policy for all containers within the pod. One of Always, OnFailure, Never. In some contexts, only a subset of those values may be permitted. Default to Always. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy Optional.
            </ResponseField>
            <ResponseField name="runtime_class_name" type="string">
              RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used to run this pod. If no RuntimeClass resource matches the named class, the pod will not be run. If unset or empty, the "legacy" RuntimeClass will be used, which is an implicit class with an empty definition that uses the default runtime handler. More info: https://git.k8s.io/enhancements/keps/sig-node/585-runtime-class Optional.
            </ResponseField>
            <ResponseField name="scheduler_name" type="string">
              If specified, the pod will be dispatched by specified scheduler. If not specified, the pod will be dispatched by default scheduler. Optional.
            </ResponseField>
            <ResponseField name="scheduling_gates" type="list(object)">
              SchedulingGates is an opaque list of values that if specified will block scheduling the pod. If schedulingGates is not empty, the pod will stay in the SchedulingGated state and the scheduler will not attempt to schedule the pod. SchedulingGates can only be set at pod creation time, and be removed only afterwards. Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name of the scheduling gate. The pod will not be scheduled until a matching condition with this name is removed from the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="scheduling_group" type="object">
              PodSchedulingGroup identifies the runtime scheduling group instance that a Pod belongs to. The scheduler uses this information to apply workload-aware scheduling semantics. Exactly one field must be specified. Optional.
              <Expandable title="properties">
                <ResponseField name="pod_group_name" type="string">
                  PodGroupName specifies the name of the standalone PodGroup object that represents the runtime instance of this group. Must be a DNS subdomain. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="security_context" type="object">
              PodSecurityContext holds pod-level security attributes and common container settings. Some fields are also present in container.securityContext. Field values of container.securityContext take precedence over field values of PodSecurityContext. Optional.
              <Expandable title="properties">
                <ResponseField name="app_armor_profile" type="object">
                  AppArmorProfile defines a pod or container's AppArmor settings. Optional.
                  <Expandable title="properties">
                    <ResponseField name="localhost_profile" type="string">
                      localhostProfile indicates a profile loaded on the node that should be used. The profile must be preconfigured on the node to work. Must match the loaded name of the profile. Must be set if and only if type is "Localhost". Optional.
                    </ResponseField>
                    <ResponseField name="type" type="string" required>
                      type indicates which kind of AppArmor profile will be applied. Valid options are: Localhost - a profile pre-loaded on the node. RuntimeDefault - the container runtime's default profile. Unconfined - no AppArmor enforcement. Required.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="fs_group" type="number">
                  A special supplemental group that applies to all containers in a pod. Some volume types allow the Kubelet to change the ownership of that volume to be owned by the pod: 1. The owning GID will be the FSGroup 2. The setgid bit is set (new files created in the volume will be owned by FSGroup) 3. The permission bits are OR'd with rw-rw---- If unset, the Kubelet will not modify the ownership and permissions of any volume. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="fs_group_change_policy" type="string">
                  fsGroupChangePolicy defines behavior of changing ownership and permission of the volume before being exposed inside Pod. This field will only apply to volume types which support fsGroup based ownership(and permissions). It will have no effect on ephemeral volume types such as: secret, configmaps and emptydir. Valid values are "OnRootMismatch" and "Always". If not specified, "Always" is used. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="run_as_group" type="number">
                  The GID to run the entrypoint of the container process. Uses runtime default if unset. May also be set in SecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence for that container. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="run_as_non_root" type="bool">
                  Indicates that the container must run as a non-root user. If true, the Kubelet will validate the image at runtime to ensure that it does not run as UID 0 (root) and fail to start the container if it does. If unset or false, no such validation will be performed. May also be set in SecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Optional.
                </ResponseField>
                <ResponseField name="run_as_user" type="number">
                  The UID to run the entrypoint of the container process. Defaults to user specified in image metadata if unspecified. May also be set in SecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence for that container. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="se_linux_change_policy" type="string">
                  seLinuxChangePolicy defines how the container's SELinux label is applied to all volumes used by the Pod. It has no effect on nodes that do not support SELinux or to volumes does not support SELinux. Valid values are "MountOption" and "Recursive". "Recursive" means relabeling of all files on all Pod volumes by the container runtime. This may be slow for large volumes, but allows mixing privileged and unprivileged Pods sharing the same volume on the same node. "MountOption" mounts all eligible Pod volumes with `-o context` mount option. This requires all Pods that share the same volume to use the same SELinux label. It is not possible to share the same volume among privileged and unprivileged Pods. Eligible volumes are in-tree FibreChannel and iSCSI volumes, and all CSI volumes whose CSI driver announces SELinux support by setting spec.seLinuxMount: true in their CSIDriver instance. Other volumes are always re-labelled recursively. If not specified, "MountOption" is used. This field affects only Pods that have SELinux label set, either in PodSecurityContext or in SecurityContext of all containers. All Pods that use the same volume should use the same seLinuxChangePolicy, otherwise some pods can get stuck in ContainerCreating state. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="se_linux_options" type="object">
                  SELinuxOptions are the labels to be applied to the container Optional.
                  <Expandable title="properties">
                    <ResponseField name="level" type="string">
                      Level is SELinux level label that applies to the container. Optional.
                    </ResponseField>
                    <ResponseField name="role" type="string">
                      Role is a SELinux role label that applies to the container. Optional.
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      Type is a SELinux type label that applies to the container. Optional.
                    </ResponseField>
                    <ResponseField name="user" type="string">
                      User is a SELinux user label that applies to the container. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="seccomp_profile" type="object">
                  SeccompProfile defines a pod/container's seccomp profile settings. Only one profile source may be set. Optional.
                  <Expandable title="properties">
                    <ResponseField name="localhost_profile" type="string">
                      localhostProfile indicates a profile defined in a file on the node should be used. The profile must be preconfigured on the node to work. Must be a descending path, relative to the kubelet's configured seccomp profile location. Must be set if type is "Localhost". Must NOT be set for any other type. Optional.
                    </ResponseField>
                    <ResponseField name="type" type="string" required>
                      type indicates which kind of seccomp profile will be applied. Valid options are: Localhost - a profile defined in a file on the node should be used. RuntimeDefault - the container runtime default profile should be used. Unconfined - no profile should be applied. Required.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="supplemental_groups" type="list(number)">
                  A list of groups applied to the first process run in each container, in addition to the container's primary GID and fsGroup (if specified). If the SupplementalGroupsPolicy feature is enabled, the supplementalGroupsPolicy field determines whether these are in addition to or instead of any group memberships defined in the container image. If unspecified, no additional groups are added, though group memberships defined in the container image may still be used, depending on the supplementalGroupsPolicy field. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="supplemental_groups_policy" type="string">
                  Defines how supplemental groups of the first container processes are calculated. Valid values are "Merge" and "Strict". If not specified, "Merge" is used. (Alpha) Using the field requires the SupplementalGroupsPolicy feature gate to be enabled and the container runtime must implement support for this feature. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="sysctls" type="list(object)">
                  Sysctls hold a list of namespaced sysctls used for the pod. Pods with unsupported sysctls (by the container runtime) might fail to launch. Note that this field cannot be set when spec.os.name is windows. Optional.
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The sysctl parameter name to set, e.g. kernel.shm_rmid_forced. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value to assign to the sysctl parameter. This is a string, but many sysctls expect numeric-like values (e.g., "0" or "1") depending on the specific parameter. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="windows_options" type="object">
                  WindowsSecurityContextOptions contain Windows-specific options and credentials. Optional.
                  <Expandable title="properties">
                    <ResponseField name="gmsa_credential_spec" type="string">
                      GMSACredentialSpec is where the GMSA admission webhook (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the GMSA credential spec named by the GMSACredentialSpecName field. Optional.
                    </ResponseField>
                    <ResponseField name="gmsa_credential_spec_name" type="string">
                      GMSACredentialSpecName is the name of the GMSA credential spec to use. Optional.
                    </ResponseField>
                    <ResponseField name="host_process" type="bool">
                      HostProcess determines if a container should be run as a 'Host Process' container. All of a Pod's containers must have the same effective HostProcess value (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers). In addition, if HostProcess is true then HostNetwork must also be set to true. Optional.
                    </ResponseField>
                    <ResponseField name="run_as_user_name" type="string">
                      The UserName in Windows to run the entrypoint of the container process. Defaults to the user specified in image metadata if unspecified. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="service_account" type="string">
              DeprecatedServiceAccount is a deprecated alias for ServiceAccountName. Deprecated: Use serviceAccountName instead. Optional.
            </ResponseField>
            <ResponseField name="service_account_name" type="string">
              ServiceAccountName is the name of the ServiceAccount to use to run this pod. More info: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/ Optional.
            </ResponseField>
            <ResponseField name="set_hostname_as_fqdn" type="bool">
              If true the pod's hostname will be configured as the pod's FQDN, rather than the leaf name (the default). In Linux containers, this means setting the FQDN in the hostname field of the kernel (the nodename field of struct utsname). In Windows containers, this means setting the registry value of hostname for the registry key HKEY_LOCAL_MACHINE\\SYSTEM\\CurrentControlSet\\Services\\Tcpip\\Parameters to FQDN. If a pod does not have FQDN, this has no effect. Default to false. Optional.
            </ResponseField>
            <ResponseField name="share_process_namespace" type="bool">
              Share a single process namespace between all of the containers in a pod. When this is set containers will be able to view and signal processes from other containers in the same pod, and the first process in each container will not be assigned PID 1. HostPID and ShareProcessNamespace cannot both be set. Optional: Default to false. Optional.
            </ResponseField>
            <ResponseField name="subdomain" type="string">
              If specified, the fully qualified Pod hostname will be "&lt;hostname&gt;.&lt;subdomain&gt;.&lt;pod namespace&gt;.svc.&lt;cluster domain&gt;". If not specified, the pod will not have a domainname at all. Optional.
            </ResponseField>
            <ResponseField name="termination_grace_period_seconds" type="number">
              Optional duration in seconds the pod needs to terminate gracefully. May be decreased in delete request. Value must be non-negative integer. The value zero indicates stop immediately via the kill signal (no opportunity to shut down). If this value is nil, the default grace period will be used instead. The grace period is the duration in seconds after the processes running in the pod are sent a termination signal and the time when the processes are forcibly halted with a kill signal. Set this value longer than the expected cleanup time for your process. Defaults to 30 seconds. Optional.
            </ResponseField>
            <ResponseField name="tolerations" type="list(object)">
              If specified, the pod's tolerations. Optional.
              <Expandable title="properties">
                <ResponseField name="effect" type="string">
                  The effect of the taint that this toleration matches. Allowed values are NoSchedule, PreferNoSchedule, and NoExecute. An empty string matches all effects. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key" type="string">
                  The key of the taint that this toleration applies to. If the operator is 'Exists', the key can be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  The operator to apply to the key and value. Valid values are 'Exists' and 'Equal'. If 'Exists', the toleration matches if the taint key matches; if 'Equal', the value must also match. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="toleration_seconds" type="number">
                  The number of seconds a pod tolerates a taint before it is evicted when the toleration effect is NoExecute. If unset, the toleration persists indefinitely. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The taint value that the toleration matches. This is required when the operator is 'Equal' and should be empty when the operator is 'Exists'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="topology_spread_constraints" type="list(object)">
              TopologySpreadConstraints describes how a group of pods ought to spread across topology domains. Scheduler will schedule pods in a way which abides by the constraints. All topologySpreadConstraints are ANDed. Optional.
              <Expandable title="properties">
                <ResponseField name="label_selector" type="object">
                  LabelSelector is used to find matching pods to which this topology spread constraint applies. If not set, the constraint applies to all pods in the namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_expressions" type="list(object)">
                      A list of label selector requirements. Each requirement specifies a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and a list of values, enabling complex label selection for the topology spread constraint's label selector. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="key" type="string">
                          The label key that the match expression applies to, used to select resources based on their labels. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="operator" type="string">
                          The operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="values" type="list(string)">
                          values is a list of string values. When the operator is 'In' or 'NotIn', this list specifies the values the label key must (or must not) match; for 'Exists' or 'DoesNotExist', it must be empty (and is typically omitted). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_labels" type="map(string)">
                      A map of key-value pairs that must match the pod's labels for the pod to be selected by this topology spread constraint. Equivalent to matchLabels in a Kubernetes LabelSelector. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="match_label_keys" type="list(string)">
                  A list of label keys that specifiy which pods to consider for spreading. Pods having any of these label keys (regardless of value) are included in the topology spread calculation. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_skew" type="number">
                  The maximum difference in the number of matching Pods between the topology domain with the most Pods and the domain with the fewest. Must be greater than zero. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min_domains" type="number">
                  The minimum number of topology domains that must exist for pod spreading to be applied. If specified, the cluster must have at least this many domains; otherwise, pods are not scheduled. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="node_affinity_policy" type="string">
                  Indicates how to treat node affinity when calculating pod topology spread skew. Allowed values are 'Honor' (default) and 'Ignore'. With 'Honor', only nodes matching the pod's node affinity are considered; with 'Ignore', node affinity is ignored. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="node_taints_policy" type="string">
                  Specifies how to treat nodes with taints when calculating pod topology spread skew. Allowed values are 'Honor' and 'Ignore' (default is 'Ignore'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="topology_key" type="string">
                  The key of node labels. Nodes that have a label with this key and identical values are considered to be in the same topology domain. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="when_unsatisfiable" type="string">
                  Indicates how to handle a pod when the topology spread constraint is not satisfied. Allowed values are 'DoNotSchedule' (default) and 'ScheduleAnyway'. 'DoNotSchedule' prevents the pod from being scheduled, while 'ScheduleAnyway' allows scheduling while still attempting to honor the constraint as much as possible. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="volumes" type="list(object)">
              List of volumes that can be mounted by containers belonging to the pod. More info: https://kubernetes.io/docs/concepts/storage/volumes Optional.
              <Expandable title="properties">
                <ResponseField name="aws_elastic_block_store" type="object">
                  Defines an AWS Elastic Block Store (EBS) volume as a source for a Kubernetes volume. This field provides the configuration to attach an EBS volume to the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount on the AWS EBS volume, e.g., 'ext4', 'xfs', or 'ntfs'. Must be supported by the host operating system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="partition" type="number">
                      The partition number of the AWS EBS volume to mount. If the volume is partitioned, specify the partition number (e.g., 1 for /dev/sdb1). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the AWS EBS volume is mounted as read-only. If true, the volume is mounted read-only; otherwise it can be read-write. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_id" type="string">
                      The unique identifier of the AWS EBS volume to be mounted, typically in the form 'vol-xxxxxxxx'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="azure_disk" type="object">
                  Represents an Azure Disk volume source, enabling a pod to mount a Microsoft Azure managed disk as a volume for persistent storage. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="caching_mode" type="string">
                      CachingMode specifies the caching policy for the Azure disk. Allowed values are None, ReadOnly, and ReadWrite; if not set, the default is None. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="disk_name" type="string">
                      The name of the Azure data disk to mount. This must be the name of a disk in the same resource group as the cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="disk_uri" type="string">
                      The URI of the virtual hard disk (VHD) for unmanaged disks, or the resource ID for managed disks, that the volume should mount. This identifies the Azure disk to attach. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to use when mounting the Azure Disk volume. Defaults to 'ext4' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="kind" type="string">
                      Kind of the Azure disk: Shared (multiple blob disks per storage account), Dedicated (single blob disk per storage account), or Managed (managed disk). Defaults to Shared. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Whether the Azure disk volume is mounted as read-only. If set to true, the volume is mounted with read-only access; otherwise, it is mounted with read-write access. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="azure_file" type="object">
                  azureFile represents an Azure File volume source that can be mounted as a volume in a pod, using an Azure Files share. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="read_only" type="bool">
                      If set to true, the Azure file share is mounted as read-only. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_name" type="string">
                      The name of the Kubernetes secret that stores the Azure Storage account name and key used to access the Azure file share. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="share_name" type="string">
                      The name of the Azure File share to mount. This must match an existing share in the Azure storage account. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cephfs" type="object">
                  Configures a Ceph Filesystem (CephFS) volume that mounts a Ceph share into the pod. This is one of the supported volume source types in the volumes list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="monitors" type="list(string)">
                      Ceph monitors are the addresses (hostnames or IPs) of the Ceph monitor nodes. This field specifies a list of endpoints used to connect to the Ceph cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path" type="string">
                      Path within the CephFS volume to mount. If not specified, defaults to the root directory. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      If true, the CephFS volume is mounted as read-only, preventing write access from the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_file" type="string">
                      Path to the Ceph keyring file used for authentication. Defaults to /etc/ceph/user.secret. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      Reference to a Secret that contains the CephFS credentials (e.g., keyring). The Secret must be in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Secret that holds the CephFS authentication credentials. The Secret must be in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="user" type="string">
                      The CephFS user to use for authentication. Defaults to 'admin' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cinder" type="object">
                  Cinder represents a Cinder volume (OpenStack block storage) that is mounted into the pod. It contains configuration for the Cinder volume source, such as the volume ID, filesystem type, and read-only settings. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount, such as 'ext4', 'xfs', etc. If not specified, the default filesystem type of the volume is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the Cinder volume is mounted as read-only. When set to true, the volume is read-only; otherwise it is read-write. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      SecretRef is a reference to a Kubernetes Secret that contains authentication credentials for the Cinder volume. This is optional and used to provide credentials when mounting the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Kubernetes secret containing the Cinder authentication credentials. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="volume_id" type="string">
                      The ID of the Cinder volume to attach and mount. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="config_map" type="object">
                  config_map is a volume source that configures a volume to use a Kubernetes ConfigMap as its content. The specified ConfigMap's data is exposed as files in the volume, enabling pods to consume configuration data. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="default_mode" type="number">
                      Sets the file permission mode (in octal, as a decimal integer) for all files within the ConfigMap volume. If unset, the default mode is 0644. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="default_user" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="items" type="list(object)">
                      A list of key-to-path mappings. If specified, only the listed keys from the ConfigMap are exposed as files at the specified paths. If omitted, all keys are exposed as files using their key names as paths. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="key" type="string">
                          The key to select from the ConfigMap data. This is the key within the ConfigMap's data map whose value will be written to the file path specified by the 'path' field. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mode" type="number">
                          Optional permission mode bits for the file (octal). Valid values are 0000 to 0777 (decimal 0 to 511). In JSON, the number must be expressed in decimal (e.g., 420 for 0644). If unset, the volume's defaultMode is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The relative path within the volume where the file from the corresponding configMap key will be mounted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="user" type="number">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the ConfigMap that provides the volume data. This must match the name of a ConfigMap in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="optional" type="bool">
                      Specifies whether the referenced ConfigMap and its keys must be defined. If set to true, a missing ConfigMap or missing keys are tolerated (resulting in an empty volume or ignored key); if false or omitted, the Pod fails when the ConfigMap or specified keys are absent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="csi" type="object">
                  Represents a volume that is provisioned by a CSI (Container Storage Interface) driver. This allows Kubernetes to consume storage from a driver that implements the CSI specification, enabling integration with external storage systems. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="driver" type="string">
                      Name of the CSI driver to use. This field is required and must match the driver name as registered on the kubelet. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount for the CSI volume, such as 'ext4' or 'xfs'. If not specified, it is typically inferred as 'ext4', though the CSI driver may define its own default. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="node_publish_secret_ref" type="object">
                      A local object reference to a Kubernetes secret in the same namespace that provides credentials for the CSI driver when publishing the volume on the node. This corresponds to the nodePublishSecretRef field of the CSI volume source. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the Secret that contains the credentials for publishing the volume to the node. This Secret must exist in the same namespace as the Pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the CSI volume is mounted as read-only. When set to true, the volume is exposed as read-only to the pod; defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_attributes" type="map(string)">
                      Map of string keys and values specifying volume attributes for the CSI driver. The meaning of each attribute is driver-specific. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="downward_api" type="object">
                  Represents a volume that exposes the downward API data, including pod metadata like labels, annotations, and resource limits, to the container as files. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="default_mode" type="number">
                      Optional mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Defaults to 0644. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="default_user" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="items" type="list(object)">
                      Optional list of files to create in the volume. Each item consists of a relative path and a fieldRef or resourceFieldRef that specifies which pod field or container resource value to expose at that path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="field_ref" type="object">
                          fieldRef is a reference to a field in the pod's metadata or spec to be exposed, defined by a fieldPath (e.g., metadata.name, metadata.labels['key']) and optionally an apiVersion for the field's schema. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="api_version" type="string">
                              The API version of the referent (e.g., 'v1') that the fieldPath is evaluated against. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="field_path" type="string">
                              The path to the field in the pod's object that will be exposed. For example, 'metadata.name' or 'spec.nodeName'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="mode" type="number">
                          Mode bits to use on this file, represented as an integer. If not set, the volume's default mode is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The relative path of the file within the volume where the downward API value will be written. The path must not start with '..' and can include directory components. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="resource_field_ref" type="object">
                          The resource field selector that selects a container resource field (e.g., limits.cpu or requests.memory) to expose via the downward API. It is represented by a ResourceFieldSelector object with fields for resource, containerName, and divisor. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="container_name" type="string">
                              The name of the container for which the resource value is referenced. If not specified, defaults to the first container in the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="divisor" type="string">
                              Divisor is the divisor for the resource quantity, specified as a Kubernetes quantity (e.g., '1m' for CPU, '1Gi' for memory). It defaults to '1' if not provided. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="resource" type="string">
                              The name of the container resource to select, such as 'limits.cpu' or 'requests.memory'. This field specifies which resource (request or limit) is exposed via the downward API volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="user" type="number">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="empty_dir" type="object">
                  Defines a temporary directory that is created empty when the pod starts and exists for the lifetime of the pod, allowing containers in the pod to share files. This volume is removed when the pod is deleted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="medium" type="string">
                      The storage medium for the emptyDir volume. Set to 'Memory' to use a RAM-backed tmpfs, or 'HugePages' to use hugepages. The default (empty string) uses the node's disk. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mode" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="size_limit" type="string">
                      The total amount of local storage required for this emptyDir volume, specified as a Kubernetes quantity (e.g., '128Mi'). If not set, the node's default limit applies. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ephemeral" type="object">
                  Configures an ephemeral volume for the pod. This volume type is created and removed with the pod, and can be backed by either a PersistentVolumeClaim template (generic ephemeral volume) or a CSI driver (CSI ephemeral volume). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="volume_claim_template" type="object">
                      A template for a PersistentVolumeClaim (PVC) that defines the volume's storage characteristics, such as access modes, resources, and storage class. When specified, Kubernetes creates a PVC from this template for the ephemeral volume's lifetime. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="metadata" type="object">
                          Metadata for the PersistentVolumeClaim generated from this volume claim template, such as labels and annotations. This is standard Kubernetes object metadata (ObjectMeta) applied to the PVC created by the ephemeral volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="annotations" type="map(string)">
                              Annotations is a map of string keys to string values that can be used to attach arbitrary non-identifying metadata to the volume claim template created for this ephemeral volume. This metadata is accessible to clients and tooling but does not affect the volume's behavior. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="creation_timestamp" type="string">
                              CreationTimestamp is a timestamp representing the server time when this object was created. It is represented in RFC3339 form (UTC) and is set by the system; clients cannot set it. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="deletion_grace_period_seconds" type="number">
                              The duration in seconds before the object is forcibly deleted, used during graceful deletion. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="deletion_timestamp" type="string">
                              The time at which the resource is marked for deletion, in RFC 3339 format. This is set by the server when a deletion is requested and is not directly settable by clients. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="finalizers" type="list(string)">
                              List of finalizers that must be removed before the object can be deleted. Each finalizer is a string identifier for a controller or owner that must clean up before deletion is allowed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="generate_name" type="string">
                              A prefix used by the Kubernetes API server to generate a unique name for this object if the name field is not provided. The generated name consists of the prefix followed by a random alphanumeric suffix. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="generation" type="number">
                              A sequence number representing a specific generation of the desired state for the persistent volume claim template. This field is populated by the Kubernetes system and is read-only. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="labels" type="map(string)">
                              Map of string keys and values that can be used to organize and categorize the PersistentVolumeClaim created from this template. These labels will be set on the generated PVC's metadata. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="managed_fields" type="list(object)">
                              A list of managed fields entries that map the workflow-id and version to the set of fields managed by that workflow. This is mostly for internal housekeeping and typically should not be set or modified by users. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="api_version" type="string">
                                  The API version (e.g., 'apps/v1') of the Kubernetes resource that this managed fields entry belongs to. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="fields_type" type="string">
                                  Specifies the type of the fields managed by this entry. In Kubernetes, the only supported value is 'FieldsV1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="fields_v1" type="any">
                                  fieldsV1 stores the fields owned by the manager in a structured format (like a Trie) represented as a JSON object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="manager" type="string">
                                  The name of the field manager that last applied changes to this managed field. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operation" type="string">
                                  The operation performed by the manager on this managed field, typically 'Apply' or 'Update'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="subresource" type="string">
                                  Describes which subresource of the object the managing controller acted on, such as the main resource (empty string), 'status', or 'scale'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="time" type="string">
                                  The timestamp at which the operation recorded in the managed field entry was performed. This is a string in RFC3339 format (e.g., 2023-01-01T00:00:00Z). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the volumeClaimTemplate. This name is used as a base for the PersistentVolumeClaim name generated for each pod that uses this ephemeral volume (the PVC name is typically &lt;pod-name&gt;-&lt;template-name&gt;). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="namespace" type="string">
                              The namespace for the volume claim template. In an ephemeral volume, the generated PersistentVolumeClaim is created in the same namespace as the pod, so this field must be empty or set to that namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="owner_references" type="list(object)">
                              List of references to the resources that own this object. Used for garbage collection and cascading deletion; each item contains apiVersion, kind, name, uid, and optionally controller and blockOwnerDeletion. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="api_version" type="string">
                                  The API version of the referenced owner resource, e.g., 'apps/v1'. This field is used in an OwnerReference to identify the version of the owning object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="block_owner_deletion" type="bool">
                                  If true, prevents the owner from being deleted in foreground deletion until this dependent object is removed. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="controller" type="bool">
                                  Indicates whether the referenced owner is the managing controller of the resource. If true, this owner manages the resource and prevents it from being garbage-collected until the owner is deleted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="kind" type="string">
                                  The kind of the owner object referenced, such as 'Deployment' or 'ReplicaSet'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="name" type="string">
                                  Name of the referenced owner object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="uid" type="string">
                                  The UID of the referenced owner object. This is a unique identifier assigned by Kubernetes to the object, used to unambiguously identify the owner in the cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="resource_version" type="string">
                              ResourceVersion is an opaque string that identifies the internal version of an object. It is used for optimistic concurrency control; clients should treat it as an opaque value and not interpret it. For a volume claim template, this field is part of the standard metadata schema but is typically not set in template definitions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="self_link" type="string">
                              self_link is a string that contains the URL of the volume claim template for this ephemeral volume within the Kubernetes API. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="uid" type="string">
                              UID is a system-generated unique identifier for the object. It is read-only and typically assigned by the server when the resource is created. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="spec" type="object">
                          spec is the specification of the persistent volume claim to be created for the ephemeral volume. It defines the desired characteristics of the volume, such as storage class, access modes, and resources. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="access_modes" type="list(string)">
                              Specifies the access modes for the ephemeral volume claim. Allowed values are ReadWriteOnce, ReadOnlyMany, ReadWriteMany, and ReadWriteOncePod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="data_source" type="object">
                              Specifies the data source for the volume claim, such as a VolumeSnapshot or PersistentVolumeClaim, used to pre-populate the ephemeral volume with data. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="api_group" type="string">
                                  apiGroup is the group for the resource being referenced. If apiGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, apiGroup is required. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="kind" type="string">
                                  Kind is the kind of the referenced data source (e.g., VolumeSnapshot or PersistentVolumeClaim). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="name" type="string">
                                  The name of the data source object referenced by this volume claim template's dataSource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="data_source_ref" type="object">
                              Specifies a reference to an object that should be used to populate the volume claim, such as a VolumeSnapshot, PersistentVolumeClaim, or any custom resource. Unlike dataSource, dataSourceRef can reference objects in other namespaces and is typically used with volume populator controllers. This field is part of the PVC spec generated for an ephemeral volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="api_group" type="string">
                                  APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="kind" type="string">
                                  The kind of the referenced data source object. Typical values are 'PersistentVolumeClaim' or 'VolumeSnapshot', matching the resource type of the data source. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="name" type="string">
                                  The name of the data source object (e.g., VolumeSnapshot, PersistentVolumeClaim) referenced by the dataSourceRef for the ephemeral volume claim. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="namespace" type="string">
                                  The namespace of the referenced data source object. When not set, the namespace of the containing PersistentVolumeClaim is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="resources" type="object">
                              Defines the storage resource requirements for the ephemeral volume, such as the requested capacity. Supports requests and limits for storage, e.g., requests: &#123; storage: 1Gi &#125;. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="limits" type="map(string)">
                                  limits specifies the maximum resources for this ephemeral volume claim, as a map of resource names (e.g., 'storage') to quantities (e.g., '1Gi'). In Kubernetes, this is used to cap the volume size or other resource usage for the claim. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="requests" type="map(string)">
                                  Requests describes the minimum resources required. For a volume claim, this typically specifies the desired storage size, e.g., &#123;'storage': '1Gi'&#125;. The keys are resource names like 'storage', and values are Kubernetes quantity strings. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="selector" type="object">
                              A label query over PersistentVolumes to consider for binding. Only volumes that match the selector are eligible for use by the ephemeral volume claim. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements. Each requirement specifies a key, an operator (such as In, NotIn, Exists, DoesNotExist), and an optional list of values. This enables complex label selection logic for the volume claim template. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  A map of string keys and values that defines a label selector. The selector matches resources whose labels contain all specified key-value pairs. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="storage_class_name" type="string">
                              The name of the StorageClass to use for this volume claim. If omitted, the default StorageClass is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="volume_attributes_class_name" type="string">
                              The name of a VolumeAttributesClass to apply to the volume claim. If specified, the class sets additional attributes on the provisioned volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="volume_mode" type="string">
                              The volume mode that the volume will be mounted in. Supported values are 'Filesystem' (default) and 'Block'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="volume_name" type="string">
                              The name of the PersistentVolume to bind to this claim. This binding reference directs the persistent volume claim to use a specific volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="fc" type="object">
                  Defines a Fibre Channel (FC) volume source. This allows mounting a Fibre Channel storage volume into the pod, typically by specifying target World Wide Names (WWNs), a logical unit number (LUN), and filesystem type. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to be mounted on the Fibre Channel volume (e.g., ext4, xfs). If not specified, Kubernetes attempts to detect the filesystem automatically. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="lun" type="number">
                      The LUN (Logical Unit Number) of the Fibre Channel target to mount. This is a required integer. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Indicates whether the Fibre Channel volume is mounted as read-only. If true, the volume is read-only; if false or omitted, it is read-write. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="target_wwns" type="list(string)">
                      List of Fibre Channel target World Wide Names (WWNs) that specify the devices to connect to. This field is required for FC volumes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="wwids" type="list(string)">
                      A list of Fibre Channel WWIDs (World Wide Identifiers) used to identify the target volume. If specified, this replaces the targetWWNs and lun fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="flex_volume" type="object">
                  FlexVolume represents a generic volume plugin that is mounted via an exec-based interface. It enables integration with third-party storage systems that implement the FlexVolume API, using driver-specific options. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="driver" type="string">
                      The name of the FlexVolume driver to use for mounting this volume. This driver must be installed on each node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to be mounted by the FlexVolume driver, such as ext4, xfs, or ntfs. If unspecified, the driver may use its own default. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="options" type="map(string)">
                      Extra command options to pass to the FlexVolume driver, as a map of string keys and values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      When set to true, the FlexVolume is mounted as read-only. Defaults to false (read-write). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      Optional reference to a Kubernetes Secret that contains sensitive information (e.g., credentials) needed by the FlexVolume driver. The secret must exist in the same namespace as the pod and is referenced by name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Secret resource that contains the secret data to pass to the FlexVolume driver. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="flocker" type="object">
                  Volume source that represents a Flocker dataset to be mounted into the pod. Configures the Flocker volume using dataset identifiers or name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dataset_name" type="string">
                      The name of the Flocker dataset to mount as a volume. Should be set if dataset_uuid is not set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="dataset_uuid" type="string">
                      The UUID of the Flocker dataset to mount. Either datasetName or datasetUUID must be specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="gce_persistent_disk" type="object">
                  Configuration for a Google Compute Engine Persistent Disk volume. This volume source mounts an existing GCE persistent disk into the pod, identified by its disk name and with optional filesystem type, partition, and read-only settings. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type for the GCE Persistent Disk volume, e.g., 'ext4' or 'xfs'. Defaults to 'ext4' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="partition" type="number">
                      The partition number of the GCE Persistent Disk to mount. If omitted, the entire disk is mounted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="pd_name" type="string">
                      pdName is the unique name of the GCE Persistent Disk resource. It identifies the disk to be mounted in the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Mounts the GCE Persistent Disk as read-only. When true, the volume is mounted read-only; when false (default), it is mounted read-write. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="git_repo" type="object">
                  gitRepo represents a volume populated from a git repository. This is a deprecated volume source; use an emptyDir with an init container instead. The object allows specifying the repository URL, revision, and target directory. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="directory" type="string">
                      The target directory name inside the volume where the git repository will be cloned. If omitted, the repository's name is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="repository" type="string">
                      The repository URL for the git repository to clone into the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="revision" type="string">
                      The commit hash to check out from the specified Git repository. If not provided, the repository's HEAD is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="glusterfs" type="object">
                  Glusterfs represents a Glusterfs volume that can be mounted by the pod. It specifies the Glusterfs endpoints and path, and optionally read-only. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="endpoints" type="string">
                      The name of the Endpoints object that defines the Glusterfs server addresses for this volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path" type="string">
                      Path to the Glusterfs volume (e.g., /vol1). This field is required when using a glusterfs volume source. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the GlusterFS volume is mounted as read-only. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="host_path" type="object">
                  Defines a hostPath volume that mounts a file or directory from the host node's filesystem into the pod. This allows the pod to access host paths. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path" type="string">
                      The path of the directory or file on the host node that will be mounted into the pod. This field is required when using a hostPath volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      The type of the host path. Supported values include '', Directory, DirectoryOrCreate, File, FileOrCreate, Socket, CharDevice, and BlockDevice. An empty string means no check is performed before mounting. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="image" type="object">
                  Image volume source configuration. Specifies an OCI image to be mounted as a volume, including the image reference and pull policy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="pull_policy" type="string">
                      The image pull policy for the image volume source. Allowed values are 'Always', 'IfNotPresent', and 'Never'. Defaults to 'IfNotPresent' unless the image reference uses the ':latest' tag, in which case it defaults to 'Always'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="reference" type="string">
                      The image reference (name and tag) of the container image to mount as a volume, for example 'nginx:latest'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="iscsi" type="object">
                  Represents an iSCSI volume source, which allows mounting an iSCSI target (LUN) as a volume in the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="chap_auth_discovery" type="bool">
                      Enables CHAP authentication for iSCSI discovery. When true, the initiator authenticates the target during the discovery phase. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="chap_auth_session" type="bool">
                      Indicates whether CHAP (Challenge Handshake Authentication Protocol) authentication is enabled for the iSCSI session. When true, the iSCSI session uses CHAP authentication with the secret specified in the secretRef field. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="fs_type" type="string">
                      The filesystem type to be mounted (e.g., ext4, xfs). If not specified, the filesystem type of the underlying iSCSI volume is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="initiator_name" type="string">
                      The custom iSCSI initiator name. If not set, the default initiator name (derived from the system) is used for iSCSI connections. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="iqn" type="string">
                      The iSCSI Qualified Name (IQN) of the iSCSI target. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="iscsi_interface" type="string">
                      Specifies the iSCSI interface name for the iSCSI transport. Defaults to 'default' if not set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="lun" type="number">
                      The LUN (Logical Unit Number) of the iSCSI target. This numeric identifier specifies the logical unit to mount from the target. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="portals" type="list(string)">
                      List of iSCSI target portals for the volume. Each portal is an IP address or IP:port pair when using non-default ports (default TCP ports are 8600 and 3260). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Indicates whether the iSCSI volume is mounted as read-only. Defaults to false (read-write). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      References a Kubernetes Secret that contains the CHAP authentication credentials for the iSCSI target. The Secret must exist in the same namespace as the Pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the Secret that contains CHAP authentication credentials for the iSCSI target. The Secret must be in the same namespace as the Pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="target_portal" type="string">
                      The iSCSI target portal. The portal is either an IP address or ip_addr:port if the port is other than the default (typically TCP ports 860 and 3260). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  Name of the volume. Must be a unique DNS_LABEL within the pod. This name is used to reference the volume in container volumeMounts. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="nfs" type="object">
                  Configures an NFS (Network File System) share as a volume. Includes settings for the NFS server, exported path, and read-only flag. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path" type="string">
                      Path of the NFS export on the NFS server. This is the directory on the NFS server that will be mounted into the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Forces the NFS export to be mounted with read-only permissions. Defaults to false (read-write). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="server" type="string">
                      The hostname or IP address of the NFS server that exports the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="persistent_volume_claim" type="object">
                  Configures a volume that is backed by a PersistentVolumeClaim (PVC). This allows a pod to use persistent storage by referencing a PVC in the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="claim_name" type="string">
                      The name of the PersistentVolumeClaim in the same namespace to reference as a volume source. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      If true, the persistent volume claim is mounted as read-only. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="photon_persistent_disk" type="object">
                  Defines a volume that uses a Photon Controller persistent disk, allowing the container to access a persistent disk managed by Photon Controller. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. 'ext4', 'xfs', 'ntfs'. Implicitly inferred to be 'ext4' if unspecified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="pd_id" type="string">
                      The unique identifier of the Photon persistent disk. This is the ID that identifies the disk in Photon Controller, and it is required to mount the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="portworx_volume" type="object">
                  Configures a Portworx volume as a pod volume. This block defines a volume backed by a Portworx storage volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      The filesystem type to mount on the Portworx volume, such as 'ext4' or 'xfs'. If not specified, defaults to 'ext4'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the Portworx volume is mounted as read-only. If set to true, the volume is mounted with read-only permissions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_id" type="string">
                      The unique identifier of the Portworx volume to be mounted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="projected" type="object">
                  Represents a projected volume source, which can mount multiple volume sources into the same directory. It supports sources such as secret, configMap, downwardAPI, and serviceAccountToken. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="default_mode" type="number">
                      Optional mode bits used to set permissions on files created in the projected volume. Must be an octal value between 0000 and 0777 or decimal between 0 and 511. Defaults to 0644. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="default_user" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="sources" type="list(object)">
                      sources is the list of VolumeProjection objects that are projected into the volume. Each projection can be a configMap, secret, downwardAPI, or serviceAccountToken. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="cluster_trust_bundle" type="object">
                          Configures a ClusterTrustBundle as a source for a projected volume, allowing the pod to access the trust bundle (such as a CA bundle) from a named ClusterTrustBundle object in the cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="label_selector" type="object">
                              labelSelector is a standard Kubernetes label selector that filters ClusterTrustBundle resources by labels. Only ClusterTrustBundles matching this selector are projected into the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements. Each requirement specifies a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and optionally a list of values. All requirements must be satisfied for the selector to match. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              Name of the referenced ClusterTrustBundle object to project into the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Indicates whether the ClusterTrustBundle can be omitted from the projected volume. If set to true, the projection is skipped when the bundle is absent; if false or unset, the bundle must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              The relative path of the file within the projected volume where the cluster trust bundle content is mounted. This path must be a relative path and cannot start with '..'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="signer_name" type="string">
                              The signer name of the cluster trust bundle to project. Selects the trust bundle(s) that match this signer name when multiple bundles exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="user" type="number">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="config_map" type="object">
                          ConfigMap projection to include in the projected volume. Used to project a ConfigMap's data (or specific items) into the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="items" type="list(object)">
                              Optional list of key mappings that specify which keys from the referenced ConfigMap to project into the volume, and the paths and file modes for each. If omitted, all key-value pairs are projected as files with key names; if specified, only listed keys are projected. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The key in the ConfigMap to select. This must be a valid key name within the referenced ConfigMap's data. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="mode" type="number">
                                  Optional file mode (permission bits) for the file. If not specified, the default is 0644. Must be a 32-bit integer representing an octal permission value, typically between 0 and 0777. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="path" type="string">
                                  Path specifies the relative file path (within the volume) where the value of the corresponding key is mounted. It must be a relative path, must not contain '..' or start with '/', and determines the filename as seen in the projected volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="user" type="number">
                                  Optional; if omitted, computed by Kubernetes.
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              Name of the ConfigMap to project into the volume. Must be a valid Kubernetes object name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the referenced ConfigMap is optional. When set to true, the volume will not fail if the ConfigMap does not exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="downward_api" type="object">
                          The `downward_api` source within a projected volume exposes Pod metadata such as labels and annotations as files to the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="items" type="list(object)">
                              The list of DownwardAPIVolumeFile items that specify the files to project via the downward API. Each item defines a relative file path and the pod field reference to write to that path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="field_ref" type="object">
                                  A reference to a pod field whose value is projected into the volume, such as metadata.name or spec.nodeName. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `field_ref` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="mode" type="number">
                                  Optional file permissions for the file, specified in octal (e.g., 0644). If unset, the default file mode is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="path" type="string">
                                  Path within the volume where the field value will be exposed as a file. This is the relative path of the file in the projected volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="resource_field_ref" type="object">
                                  Selects a container resource field (such as requests.cpu or limits.memory) to expose in the downward API volume. Only resource limits and requests are supported. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `resource_field_ref` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="user" type="number">
                                  Optional; if omitted, computed by Kubernetes.
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="pod_certificate" type="object">
                          Optional; if omitted, computed by Kubernetes.
                          <Expandable title="properties">
                            <ResponseField name="certificate_chain_path" type="string">
                              Path within the projected volume where the certificate chain is exposed. The file at this path contains the full certificate chain. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="credential_bundle_path" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="key_path" type="string">
                              Specifies the relative path within the projected volume where the certificate's private key file is placed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="key_type" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="max_expiration_seconds" type="number">
                              The maximum expiration time in seconds for the pod certificate injected into the projected volume. This bounds the lifetime of the certificate, ensuring it does not exceed the specified duration. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="signer_name" type="string">
                              The name of the certificate signer that should issue the pod certificate. This is used to select the appropriate signer when requesting a certificate for the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="user" type="number">
                              The numeric UID that will own the certificate file projected from this source. If unset, the file is owned by UID 0 (root). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="user_annotations" type="map(string)">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="secret" type="object">
                          SecretProjection adapts a secret into a projected volume. The secret's data keys are exposed as files in the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="items" type="list(object)">
                              A list of key-to-path mappings that specify which secret keys are projected into the volume and the file paths they are exposed at. If items is not set, all keys from the referenced secret are projected. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The key within the referenced Secret that corresponds to the data to project into the volume. This is the name of the entry in the Secret's data map. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="mode" type="number">
                                  The permissions for the file representing the secret key. Must be a numeric value between 0 and 511 (octal 0000-0777) representing Unix file mode bits. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="path" type="string">
                                  Relative path within the projected volume to which the secret key is mapped. Must be a relative path and must not contain '..' segments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="user" type="number">
                                  Optional; if omitted, computed by Kubernetes.
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the Secret to project into the volume. This must match the name of a Secret in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              If true, the referenced secret is not required to exist; if it is missing, the volume is created without files from that secret. Defaults to false, requiring the secret to be present. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="service_account_token" type="object">
                          Configures a projected volume source that injects the service account token into the pod. The token can be customized with fields such as audience, expirationSeconds, and path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="audience" type="string">
                              The audience (aud) claim for the service account token, specifying the intended recipient. The token is valid only for this audience, such as the Kubernetes API server or a specific service. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="expiration_seconds" type="number">
                              The expiration time of the projected service account token, in seconds. Must be between 600 and 86400 (10 minutes to 24 hours), and defaults to 3600 (1 hour). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              The relative path within the projected volume at which to mount the service account token. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="user" type="number">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="quobyte" type="object">
                  Configuration for a Quobyte volume source, which mounts a Quobyte distributed storage system into the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="group" type="string">
                      Group to map volume access to. Defaults to no group. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Whether to mount the volume as read-only. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="registry" type="string">
                      The Quobyte registry to connect to, as a host or host:port, or a comma-separated list of registries. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tenant" type="string">
                      The tenant name that owns the Quobyte volume in the backend; used with dynamically provisioned Quobyte volumes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="user" type="string">
                      User to map volume access to. Defaults to the volume name if not set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume" type="string">
                      The name of the Quobyte volume to mount. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="rbd" type="object">
                  rbd represents a Rados Block Device (RBD) volume source, allowing a pod to mount an RBD image into the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type of the RBD volume that Kubernetes will mount (e.g., ext4, xfs). If not set, the filesystem type is determined by the image's existing filesystem. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="image" type="string">
                      The rados image name. This is the name of the RBD image to mount as a volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="keyring" type="string">
                      The path to the keyring file for RBD user authentication. Defaults to /etc/ceph/keyring. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="monitors" type="list(string)">
                      Monitors is a list of Ceph monitor addresses (host:port) used to connect to the Ceph cluster for the RBD volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="pool" type="string">
                      The Ceph pool name where the RBD image resides. Defaults to 'rbd' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      If set to true, the RBD volume is mounted as read-only. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      Reference to a Kubernetes secret that provides the Ceph authentication key for the RBD volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Kubernetes Secret that holds the Ceph authentication credentials for the RBD volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="user" type="string">
                      Specifies the RADOS user name used for authentication with the Ceph cluster. Defaults to "admin". Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="scale_io" type="object">
                  ScaleIO represents a ScaleIO persistent volume source. This block configures the ScaleIO volume to be used as a volume in the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount. Must be a filesystem type supported by the host operating system, e.g., ext4, xfs. Defaults to ext4. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="gateway" type="string">
                      The host address of the ScaleIO API gateway. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="protection_domain" type="string">
                      The name of the ScaleIO Protection Domain for the configured storage. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Marks the ScaleIO volume as read-only. When set to true, the volume is mounted with read-only permissions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      secret_ref is a reference to a Kubernetes Secret that contains the ScaleIO admin password. It must be in the same namespace as the Pod, and is required for authentication with the ScaleIO system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Secret object that contains authentication credentials for ScaleIO volume access. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="ssl_enabled" type="bool">
                      Enables or disables SSL communication with the ScaleIO Gateway. When true, the volume plugin connects to the gateway over SSL. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="storage_mode" type="string">
                      Storage mode for the ScaleIO volume. Allowed values are 'ThinProvisioned' (default) and 'ThickProvisioned'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="storage_pool" type="string">
                      The name of the ScaleIO storage pool in which the volume is created or from which it is provisioned. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="system" type="string">
                      The name of the ScaleIO storage system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_name" type="string">
                      Name of the ScaleIO volume, as defined in the ScaleIO storage system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="secret" type="object">
                  secret is a volume source that references a Kubernetes Secret object, allowing the Secret's data to be mounted as files into the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="default_mode" type="number">
                      Specifies the default permission mode (in octal) applied to files within the secret volume. Defaults to 0644 if unspecified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="default_user" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="items" type="list(object)">
                      Optional list of key-to-path mappings for the secret volume. Each item specifies a key from the secret, the file path where that key should be mounted, and an optional permission mode. If omitted, all keys in the secret are mounted as files using their key names. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="key" type="string">
                          The key in the secret's data that should be exposed as a file. The file path is specified by the corresponding 'path' field in the same item. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mode" type="number">
                          The permission mode for the file, specified as a numeric value in octal (e.g., 420 for 0644). If omitted, the volume's defaultMode is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The relative path of the file to which the corresponding secret key is mapped within the volume's mount path. Must not contain '..' or start with '../'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="user" type="number">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="optional" type="bool">
                      Specifies whether the referenced Secret must be defined. When true, the volume can still be used even if the Secret does not exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_name" type="string">
                      The name of the Secret to mount as a volume. The Secret must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="storageos" type="object">
                  Defines a StorageOS volume source for the pod, providing configuration for mounting a StorageOS volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount. Must be a filesystem type supported by the host operating system. If not specified, the filesystem type is inferred from the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Indicates whether the StorageOS volume is mounted as read-only. When set to true, the volume cannot be written to; default is false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      Reference to a Kubernetes Secret that contains StorageOS API credentials. If not set, default credentials will be attempted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the Secret that contains the StorageOS API credentials. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="volume_name" type="string">
                      The name of the StorageOS volume to mount. This must match an existing volume in the StorageOS system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_namespace" type="string">
                      volumeNamespace specifies the namespace of the StorageOS volume. If not set, it defaults to the Kubernetes namespace of the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="vsphere_volume" type="object">
                  vsphereVolume represents a vSphere volume to be mounted into the container. It configures a vSphere datastore volume as a storage source for the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="storage_policy_id" type="string">
                      The storage policy ID based on which storage is provisioned for the vSphere volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="storage_policy_name" type="string">
                      The name of the vSphere storage policy to be applied to the volume. This policy determines storage characteristics such as performance and availability. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_path" type="string">
                      The datastore path that identifies the vSphere volume to mount, e.g., '[datastore1] vmdisk'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="object">
  DeploymentStatus is the most recently observed status of the Deployment. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="available_replicas" type="number">
      Total number of available non-terminating pods (ready for at least minReadySeconds) targeted by this deployment. Optional.
    </ResponseField>
    <ResponseField name="collision_count" type="number">
      Count of hash collisions for the Deployment. The Deployment controller uses this field as a collision avoidance mechanism when it needs to create the name for the newest ReplicaSet. Optional.
    </ResponseField>
    <ResponseField name="conditions" type="list(object)">
      Represents the latest available observations of a deployment's current state. Optional.
      <Expandable title="properties">
        <ResponseField name="last_transition_time" type="string">
          last_transition_time is the last time the condition transitioned from one status to another. It is a string representing the timestamp in RFC3339 format, as defined in Kubernetes deployment status conditions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="last_update_time" type="string">
          The last time the condition was updated, as a timestamp in RFC3339 format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable message providing details about the current condition, such as the reason for the deployment's state. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reason" type="string">
          A brief, machine-readable reason for the condition's last transition, typically in CamelCase. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          The status of the condition, one of True, False, or Unknown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Type of the condition, e.g., Available, Progressing, or ReplicaFailure, indicating the specific aspect of deployment status being reported. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="observed_generation" type="number">
      The generation observed by the deployment controller. Optional.
    </ResponseField>
    <ResponseField name="ready_replicas" type="number">
      Total number of non-terminating pods targeted by this Deployment with a Ready Condition. Optional.
    </ResponseField>
    <ResponseField name="replicas" type="number">
      Total number of non-terminating pods targeted by this deployment (their labels match the selector). Optional.
    </ResponseField>
    <ResponseField name="terminating_replicas" type="number">
      Total number of terminating pods targeted by this deployment. Terminating pods have a non-null .metadata.deletionTimestamp and have not yet reached the Failed or Succeeded .status.phase. This is a beta field and requires enabling DeploymentReplicaSetTerminatingReplicas feature (enabled by default). Optional.
    </ResponseField>
    <ResponseField name="unavailable_replicas" type="number">
      Total number of unavailable pods targeted by this deployment. This is the total number of pods that are still required for the deployment to have 100% available capacity. They may either be pods that are running but not yet available or pods that still have not been created. Optional.
    </ResponseField>
    <ResponseField name="updated_replicas" type="number">
      Total number of non-terminating pods targeted by this deployment that have the desired template spec. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
