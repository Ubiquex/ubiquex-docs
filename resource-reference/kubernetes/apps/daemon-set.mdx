---
title: "kubernetes_apps_daemon_set"
description: "`kubernetes_apps_daemon_set` runs exactly one copy of a pod on every real node in the cluster (or every node matching a selector) -- the mechanism..."
---

`kubernetes_apps_daemon_set` runs exactly one copy of a pod on every real node in the cluster (or every node matching a selector) -- the mechanism behind node-level agents like log collectors and monitoring daemons that need to be everywhere, not scaled to an arbitrary count.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apps "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/apps"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own daemon set"})

    		ubx.Resource(apps.DaemonSet, "example", apps.DaemonSetConfig{
    			Name:      "example",
    			Namespace: "example",
    			Spec:      map[string]any{"selector": map[string]any{"match_expressions": []map[string]any{{"key": "example"}}}, "template": map[string]any{"metadata": map[string]any{"name": "example-name"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DaemonSet } from "@ubx/sdk-kubernetes/kubernetes/apps/daemon_set";

    export default stack("example", () => {
      intent({ summary: "example own daemon set" });

      resource(DaemonSet, "example", {
        name: "example",
        namespace: "example",
        spec: {
          selector: { matchExpressions: [{ key: "example" }] },
          template: { metadata: { name: "example-name" } },
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.apps import DaemonSet, DaemonSetConfig
    from ubx.kubernetes.apps.daemon_set import DaemonSet_Spec, DaemonSet_Spec_Selector, DaemonSet_Spec_Selector_MatchExpressions, DaemonSet_Spec_Template, DaemonSet_Metadata

    def describe():
        ubx.intent("example own daemon set")

        ubx.resource(DaemonSet, "example", DaemonSetConfig(
            name="example",
            namespace="example",
            spec=DaemonSet_Spec(selector=DaemonSet_Spec_Selector(match_expressions=[DaemonSet_Spec_Selector_MatchExpressions(key="example")]), template=DaemonSet_Spec_Template(metadata=DaemonSet_Metadata(name="example-name"))),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create kubernetes_apps_daemon_set called "example", with
    namespace example, spec a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="api_version" type="string">
  APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="object">
  ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
      Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations Optional.
    </ResponseField>
    <ResponseField name="creation_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="deletion_grace_period_seconds" type="number">
      Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only. Optional.
    </ResponseField>
    <ResponseField name="deletion_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="finalizers" type="list(string)">
      Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. Optional.
    </ResponseField>
    <ResponseField name="generate_name" type="string">
      GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server. If this field is specified and the generated name exists, the server will return a 409. Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency Optional.
    </ResponseField>
    <ResponseField name="generation" type="number">
      A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels Optional.
    </ResponseField>
    <ResponseField name="managed_fields" type="list(object)">
      ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          The API version of the resource that this managed field set applies to, in the format 'group/version' (e.g., 'apps/v1'). It tracks the version of the field set because field sets cannot be automatically converted between versions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_type" type="string">
          The type of the fields in the managedFields entry, indicating the schema version used to encode the fields. Typically 'FieldsV1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_v1" type="any">
          fields_v1 contains the JSON-serialized fields that the manager manages, in the Kubernetes fieldsV1 format (a map of field paths to values). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="manager" type="string">
          The name of the manager (e.g., a controller, user, or client) that last applied the fields in this managedFields entry. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operation" type="string">
          The operation type that was used to manage the fields, with valid values being 'Apply' or 'Update'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subresource" type="string">
          The name of the subresource (for example, 'status' or 'scale') that the manager field set pertains to, if any. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time" type="string">
          The timestamp, in RFC3339 format, indicating when this managedFields entry was last updated by the manager. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names Optional.
    </ResponseField>
    <ResponseField name="namespace" type="string">
      Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces Optional.
    </ResponseField>
    <ResponseField name="owner_references" type="list(object)">
      List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          APIVersion of the referent object. This is the API version of the owner resource, such as 'apps/v1' or 'batch/v1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="block_owner_deletion" type="bool">
          If true, the owner reference blocks deletion of the object until the owner is deleted or the reference is removed. Requires delete permission on the owner. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="controller" type="bool">
          Indicates whether the referenced owner is the managing controller of the resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          Kind of the referenced owner resource (e.g., 'ReplicaSet', 'DaemonSet', 'StatefulSet'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the owner object referenced by this owner reference entry (matching the kind and apiVersion in the same owner reference). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="uid" type="string">
          The UID of the owner object. This field uniquely identifies the referenced object, as names can be reused over time. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_version" type="string">
      An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency Optional.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. Optional.
    </ResponseField>
    <ResponseField name="uid" type="string">
      UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="spec" type="object" required>
  DaemonSetSpec is the specification of a daemon set. Required.
  <Expandable title="properties">
    <ResponseField name="min_ready_seconds" type="number">
      The minimum number of seconds for which a newly created DaemonSet pod should be ready without any of its container crashing, for it to be considered available. Defaults to 0 (pod will be considered available as soon as it is ready). Optional.
    </ResponseField>
    <ResponseField name="revision_history_limit" type="number">
      The number of old history to retain to allow rollback. This is a pointer to distinguish between explicit zero and not specified. Defaults to 10. Optional.
    </ResponseField>
    <ResponseField name="selector" type="object" required>
      A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects. Required.
      <Expandable title="properties">
        <ResponseField name="match_expressions" type="list(object)">
          matchExpressions is a list of label selector requirements. The requirements are ANDed. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The label key that the selector requirement operates on. This is the key of a label that must match (or not match, depending on the operator) for a resource to be selected. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              The operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              values is the list of label values that the selector matches. For operators In and NotIn, values must be non-empty and each entry must be a string. For operators Exists and DoesNotExist, values must be empty (i.e., not set). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_labels" type="map(string)">
          matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="template" type="object" required>
      PodTemplateSpec describes the data a pod should have when created from a template Required.
      <Expandable title="properties">
        <ResponseField name="metadata" type="object">
          ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. Optional.
          <Expandable title="properties">
            <ResponseField name="annotations" type="map(string)">
              Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations Optional.
            </ResponseField>
            <ResponseField name="creation_timestamp" type="string">
              Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
            </ResponseField>
            <ResponseField name="deletion_grace_period_seconds" type="number">
              Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only. Optional.
            </ResponseField>
            <ResponseField name="deletion_timestamp" type="string">
              Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
            </ResponseField>
            <ResponseField name="finalizers" type="list(string)">
              Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. Optional.
            </ResponseField>
            <ResponseField name="generate_name" type="string">
              GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server. If this field is specified and the generated name exists, the server will return a 409. Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency Optional.
            </ResponseField>
            <ResponseField name="generation" type="number">
              A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. Optional.
            </ResponseField>
            <ResponseField name="labels" type="map(string)">
              Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels Optional.
            </ResponseField>
            <ResponseField name="managed_fields" type="list(object)">
              ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. Optional.
              <Expandable title="properties">
                <ResponseField name="api_version" type="string">
                  The API version of the object that this managed fields entry applies to, used to identify the schema version for the managed fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fields_type" type="string">
                  Indicates the type of field ownership information recorded in this managedFields entry. Kubernetes only uses the value 'FieldsV1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fields_v1" type="any">
                  fields_v1 contains the parsed fields managed by this field manager, serialized in a JSON structure that mirrors the object hierarchy. It indicates which fields are managed by this entry. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="manager" type="string">
                  The name of the manager that last applied this field, as recorded in the Kubernetes managedFields metadata. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operation" type="string">
                  The operation that was applied to manage this field. Valid values are 'Apply' and 'Update'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="subresource" type="string">
                  The subresource this managed fields entry applies to, such as "status" or an empty string for the main resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="time" type="string">
                  The timestamp, in RFC3339 format, indicating when the last operation was applied to this managedFields entry. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names Optional.
            </ResponseField>
            <ResponseField name="namespace" type="string">
              Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces Optional.
            </ResponseField>
            <ResponseField name="owner_references" type="list(object)">
              List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. Optional.
              <Expandable title="properties">
                <ResponseField name="api_version" type="string">
                  apiVersion is the API version of the referent. For example, 'v1' or 'apps/v1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="block_owner_deletion" type="bool">
                  If true, and the owner has the foregroundDeletion finalizer, the owner cannot be deleted until this dependent object is removed. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="controller" type="bool">
                  Indicates whether the owner reference points to the managing controller. When true, the referenced owner is the controller of the dependent object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kind" type="string">
                  Kind is the Kubernetes resource type of the owner object, such as 'ReplicaSet' or 'DaemonSet'. It is part of an owner reference that identifies the owning object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  Name of the referenced owner resource. It identifies the object along with apiVersion and kind. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="uid" type="string">
                  UID of the owner object referenced by this owner reference. It uniquely identifies the referred resource within the cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="resource_version" type="string">
              An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency Optional.
            </ResponseField>
            <ResponseField name="self_link" type="string">
              Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. Optional.
            </ResponseField>
            <ResponseField name="uid" type="string">
              UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="spec" type="object">
          PodSpec is a description of a pod. Optional.
          <Expandable title="properties">
            <ResponseField name="active_deadline_seconds" type="number">
              Optional duration in seconds the pod may be active on the node relative to StartTime before the system will actively try to mark it failed and kill associated containers. Value must be a positive integer. Optional.
            </ResponseField>
            <ResponseField name="affinity" type="object">
              Affinity is a group of affinity scheduling rules. Optional.
              <Expandable title="properties">
                <ResponseField name="node_affinity" type="object">
                  Node affinity is a group of node affinity scheduling rules. Optional.
                  <Expandable title="properties">
                    <ResponseField name="preferred_during_scheduling_ignored_during_execution" type="list(object)">
                      The scheduler will prefer to schedule pods to nodes that satisfy the affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node matches the corresponding matchExpressions; the node(s) with the highest sum are the most preferred. Optional.
                      <Expandable title="properties">
                        <ResponseField name="preference" type="object">
                          A node selector term that defines which nodes are preferred for scheduling. It consists of matchExpressions and matchFields fields to constrain the node selection. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              A list of label selector requirements that define which node labels must match for a node to be considered for this preferred scheduling preference. Each requirement includes a key, an operator (such as In, NotIn, Exists, DoesNotExist), and optional values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key that the selector applies to. The node must have this label key to satisfy the match expression. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist, Gt, and Lt. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  The list of values that the node label key must match for the affinity rule. Used only with operators that require values, such as In or NotIn. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_fields" type="list(object)">
                              A list of node selector requirements that match against node fields (e.g., metadata.name). Each requirement defines a key, operator, and values to constrain node selection based on node fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The key (field path) of the node attribute to match, such as 'metadata.name' or a label key in matchFields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator represents the relationship between the key and the set of values. Must be one of In, NotIn, Exists, DoesNotExist, Gt, Lt. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  A list of values that the node field (specified by the key) must match when the operator is 'In' or 'NotIn'. For 'Exists' or 'DoesNotExist', this list must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="weight" type="number">
                          Weight associated with the corresponding node preference term. The scheduler adds this weight to the sum of all preferences that match the node, and nodes with the highest total weight are preferred. Must be an integer in the range 1-100. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="required_during_scheduling_ignored_during_execution" type="object">
                      A node selector represents the union of the results of one or more label queries over a set of nodes; that is, it represents the OR of the selectors represented by the node selector terms. Optional.
                      <Expandable title="properties">
                        <ResponseField name="node_selector_terms" type="list(object)" required>
                          Required. A list of node selector terms. The terms are ORed. Required.
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              A list of label selector requirements for node affinity. Each requirement specifies a key, operator, and values. The node must satisfy all match expressions within at least one node selector term to be considered eligible for scheduling. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key on the node that this expression matches. The operator and values determine the condition applied to this key. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator represents the relationship between the key and the set of values in a node selector requirement. Allowed values are In, NotIn, Exists, DoesNotExist, Gt, and Lt. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  A list of values that the node's label must match for In/NotIn operators. Must be empty for Exists/DoesNotExist operators. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_fields" type="list(object)">
                              A list of node selector requirements that match on node fields (e.g., metadata.name) to satisfy this node selector term, using a key, operator, and optional values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The key of the node field to match, such as metadata.name, in this node selector term. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  Operator for a field-based node selector requirement. Valid values are In, NotIn, Exists, DoesNotExist, Gt, and Lt. The Gt and Lt operators require the value to be an integer. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  The list of values that the node's field value must match according to the requirement's operator. For In and NotIn operators, these are the allowed or disallowed values; for Exists and DoesNotExist, this list must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="pod_affinity" type="object">
                  Pod affinity is a group of inter pod affinity scheduling rules. Optional.
                  <Expandable title="properties">
                    <ResponseField name="preferred_during_scheduling_ignored_during_execution" type="list(object)">
                      The scheduler will prefer to schedule pods to nodes that satisfy the affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the node(s) with the highest sum are the most preferred. Optional.
                      <Expandable title="properties">
                        <ResponseField name="pod_affinity_term" type="object">
                          The pod affinity term for the preferred scheduling rule. It specifies a label selector and topology key to define a group of pods to co-locate with, and optionally restricts the search to specific namespaces. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="label_selector" type="object">
                              Labels selector used to identify the set of pods that the affinity term applies to. The affinity will match pods that have the labels specified in this selector. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements. Each requirement contains a key, an operator (such as In, NotIn, Exists, or DoesNotExist), and, for In and NotIn, a list of values; all requirements must be satisfied for a pod to match the label selector. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_label_keys" type="list(string)">
                              A list of label keys that must be present on the pods for the affinity term to be satisfied. Only the presence of these keys is considered, not their values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="mismatch_label_keys" type="list(string)">
                              A list of pod label keys. The affinity term applies only to pods that have a value for each key that differs from the value on the pod being scheduled; if the scheduling pod has no value for a key, that key does not select any pods. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="namespace_selector" type="object">
                              A label selector used to select the namespaces in which the target pods exist. If set, the affinity is scoped to pods running in namespaces matching this selector. If empty, it selects all namespaces. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements used to select namespaces. Each requirement defines a key, an operator (In, NotIn, Exists, DoesNotExist), and optionally a list of values. All expressions must be satisfied for a namespace to match. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  A map of key-value pairs that must be matched by the labels of a namespace for it to be selected by this namespace selector. All specified labels must be present and equal for a namespace to match. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="namespaces" type="list(string)">
                              List of namespaces to which this pod affinity term applies. If empty, the term applies to the pod's own namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="topology_key" type="string">
                              The node label key that defines the topology domain for the affinity term. For example, 'kubernetes.io/hostname' groups nodes by host name, and the affinity/anti-affinity rule is applied per node group. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="weight" type="number">
                          The weight associated with a preferred pod affinity term. Must be an integer between 1 and 100. Higher weights increase the preference for scheduling on nodes that match the affinity term. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="required_during_scheduling_ignored_during_execution" type="list(object)">
                      If the affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to a pod label update), the system may or may not try to eventually evict the pod from its node. When there are multiple elements, the lists of nodes corresponding to each podAffinityTerm are intersected, i.e. all terms must be satisfied. Optional.
                      <Expandable title="properties">
                        <ResponseField name="label_selector" type="object">
                          A label selector is a query over labels on pods to identify the set of pods that the pod affinity rule applies to. It can include matchLabels (exact key-value pairs) and matchExpressions (conditional expressions) to define the selection criteria. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              match_expressions is a list of label selector requirements. Each requirement specifies a key, an operator, and optionally a list of values. The requirements are ANDed together, and the resulting label selector determines which pods are considered for pod affinity during scheduling. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key that this match expression applies to. This is the key in the Kubernetes label selector, e.g., 'app' or 'tier'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator for the label selector requirement, indicating how the key is compared to the values. Allowed values are In, NotIn, Exists, and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  The list of values for the label selector match expression. These values are used with the operator (e.g., In or NotIn) to match against the labels of pods or nodes. For operators like Exists or DoesNotExist, this list must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_labels" type="map(string)">
                              A map of key-value pairs. The requirements are ANDed: only pods with all specified labels (matching each key-value pair) are selected. A single &#123;key, value&#125; in the matchLabels map is equivalent to an element of matchExpressions, with key 'key', operator 'In', and values ['value']. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="match_label_keys" type="list(string)">
                          A list of pod label keys used to select pods for this affinity term. Only pods that have the same label values for all specified keys as the incoming pod are considered, in addition to any label selector. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mismatch_label_keys" type="list(string)">
                          A list of label keys. The scheduler considers only existing pods that have different values for these keys than the pod being scheduled, which helps precisely control pod placement (e.g., anti-affinity among pods with the same replica label). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="namespace_selector" type="object">
                          NamespaceSelector is a label selector for namespaces. If specified, the pod affinity term applies only to pods in namespaces matching this selector; if omitted, it applies to the pod's own namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              A list of label selector requirements that constrain which namespaces match. Each requirement specifies a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and optionally a list of values. All requirements are ANDed together. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key that the selector applies to. In this context, it is the key for matching namespace labels in the pod affinity namespace selector. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator to apply to the key and value(s) in this namespace selector requirement. Allowed values are In, NotIn, Exists, and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  Values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_labels" type="map(string)">
                              A map of label keys and values. Namespaces that have all the specified label-value pairs are selected for the pod affinity/anti-affinity rule. This restricts the affinity rule to namespaces matching these labels. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="namespaces" type="list(string)">
                          Restricts the pod affinity term to a set of namespaces. If empty or omitted, the term applies to the namespace of the pod that the affinity is defined on. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="topology_key" type="string">
                          The topology key defines the node label that groups nodes into topology domains (e.g., kubernetes.io/hostname). It is required for requiredDuringSchedulingIgnoredDuringExecution affinity terms, as it determines which nodes are considered for pod placement relative to each other. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="pod_anti_affinity" type="object">
                  Pod anti affinity is a group of inter pod anti affinity scheduling rules. Optional.
                  <Expandable title="properties">
                    <ResponseField name="preferred_during_scheduling_ignored_during_execution" type="list(object)">
                      The scheduler will prefer to schedule pods to nodes that satisfy the anti-affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling anti-affinity expressions, etc.), compute a sum by iterating through the elements of this field and subtracting "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the node(s) with the highest sum are the most preferred. Optional.
                      <Expandable title="properties">
                        <ResponseField name="pod_affinity_term" type="object">
                          Represents a pod affinity term, specifying the label selector, namespaces, and topology key that determine the group of pods and the topology domain for the anti-affinity preference. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="label_selector" type="object">
                              A label query over a set of pods for the affinity term. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects, while a null label selector matches no objects. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements. Each requirement specifies a key, an operator (such as In, NotIn, Exists, DoesNotExist), and optionally a list of values. The requirements are ANDed, meaning all conditions must be satisfied for a match. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  A map of label keys and values that must all be present on a pod for the label selector to match. Used to select pods for this pod affinity or anti-affinity term. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_label_keys" type="list(string)">
                              List of pod label keys to select which pods should be considered for the pod affinity/anti-affinity term. The keys are matched against the labels of the pod being scheduled and ANDed with any label selector. If empty, all label keys are considered. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="mismatch_label_keys" type="list(string)">
                              A list of pod label keys that must have different values between the current pod and candidate pods for the affinity term to match. This excludes pods with the same label values, preventing self-selection in anti-affinity rules. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="namespace_selector" type="object">
                              A label selector to select namespaces for the pod affinity term. When specified, the affinity/anti-affinity rule applies to pods in the matching namespaces; if omitted, it applies to the pod's own namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements that are ANDed. Each requirement specifies a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and optionally a list of values. The namespace selector matches namespaces whose labels satisfy these expressions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  A map of label keys to values used to select namespaces. The pod affinity or anti-affinity rule applies only to pods running in namespaces that match all of the specified label key-value pairs. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="namespaces" type="list(string)">
                              A list of namespaces that the pod affinity/anti-affinity term applies to. If empty or omitted, it defaults to the namespace of the pod where the rule is defined. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="topology_key" type="string">
                              The key of node labels that defines the topology domain (e.g., kubernetes.io/hostname) used to group nodes for pod anti-affinity. Pods are scheduled to avoid being placed on the same topology domain as other pods that match the label selector. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="weight" type="number">
                          The weight associated with this preferred scheduling term. It must be an integer between 1 and 100, and higher weights increase the preference for this anti-affinity rule. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="required_during_scheduling_ignored_during_execution" type="list(object)">
                      If the anti-affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the anti-affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to a pod label update), the system may or may not try to eventually evict the pod from its node. When there are multiple elements, the lists of nodes corresponding to each podAffinityTerm are intersected, i.e. all terms must be satisfied. Optional.
                      <Expandable title="properties">
                        <ResponseField name="label_selector" type="object">
                          A label query over a set of pods. The pods that match this selector are considered as targets for the anti-affinity rule. It is a standard Kubernetes label selector, which can include matchLabels and matchExpressions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              match_expressions is a list of label selector requirements. Each requirement specifies a key, an operator (such as In, NotIn, Exists, DoesNotExist), and an optional list of values, used to select pods based on labels for pod anti-affinity rules. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key that this match expression applies to. The selector will match resources based on the value of this key on their labels. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator used to compare the label key with the values in a match expression. Valid values are In, NotIn, Exists, and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  values is a list of string values that the key must match according to the operator. For In and NotIn operators, values must be non-empty. For Exists and DoesNotExist, values must be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_labels" type="map(string)">
                              match_labels is a map of label keys and values used to select pods for the anti-affinity rule. Only pods that have all of the specified labels are considered when determining pod placement. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="match_label_keys" type="list(string)">
                          A list of pod label keys used to select the pods that the anti-affinity rule applies to. Only labels with these keys are considered when matching pods, restricting the scope of the affinity selector. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mismatch_label_keys" type="list(string)">
                          A list of label keys that, if present on the incoming pod, require existing pods in the selected group to have different values for those keys. This is used to exclude pods that share the same label values from the anti-affinity term. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="namespace_selector" type="object">
                          A label selector that determines which namespaces the pod anti-affinity rule applies to. Only pods running in namespaces matching this selector are considered when evaluating the anti-affinity constraint. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="match_expressions" type="list(object)">
                              A list of label selector requirements that the namespace labels must satisfy. Each requirement specifies a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and an optional list of values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The label key that the selector applies to. It must match a label on the namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operator" type="string">
                                  The operator represents the relationship between the key and a set of values. Valid operators are In, NotIn, Exists, and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="values" type="list(string)">
                                  A list of values that the namespace label key must match for the operator. Required for the In and NotIn operators; must be empty for Exists and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="match_labels" type="map(string)">
                              A map of key-value pairs that must match the labels of the namespaces to be selected. Only namespaces with all specified labels are considered for the anti-affinity rule. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="namespaces" type="list(string)">
                          A list of namespaces that the label selector for this pod anti-affinity term applies to. If unspecified, it defaults to the namespace of the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="topology_key" type="string">
                          Specifies the node label key that defines the topology domain for pod anti-affinity. The required anti-affinity rule is evaluated across all nodes sharing this label value, and the pod is scheduled only if the constraint is satisfied. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="automount_service_account_token" type="bool">
              AutomountServiceAccountToken indicates whether a service account token should be automatically mounted. Optional.
            </ResponseField>
            <ResponseField name="containers" type="list(object)" required>
              List of containers belonging to the pod. Containers cannot currently be added or removed. There must be at least one container in a Pod. Cannot be updated. Required.
              <Expandable title="properties">
                <ResponseField name="args" type="list(string)">
                  Arguments to the entrypoint. The container image's CMD is used if this is not provided. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="command" type="list(string)">
                  Entrypoint array for the container. This is the command that is run when the container starts, equivalent to Docker's ENTRYPOINT. It overrides the image's ENTRYPOINT. The command is not run inside a shell. If not provided, the image's default entrypoint is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="env" type="list(object)">
                  List of environment variables to set in the container. Each entry has a name and optionally a value or valueFrom (e.g., configMapKeyRef, secretKeyRef, fieldRef, resourceFieldRef). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the environment variable. Must be a C identifier (alphanumeric, '_', and '-', starting with an alphabetic character or '_'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of the environment variable, expressed as a string. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value_from" type="object">
                      value_from specifies the source for the environment variable's value, such as a reference to a ConfigMap, Secret, or a pod field. It is used when the value is not a literal string. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="config_map_key_ref" type="object">
                          Selects a key from a ConfigMap to populate the value of the environment variable. The ConfigMap must exist in the same namespace as the Pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key within the referenced ConfigMap whose value is used for this environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the ConfigMap to select a key from. This must match the name of an existing ConfigMap in the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the ConfigMap or its key must be defined. If true, and the key does not exist, the environment variable will be omitted. If false and the key does not exist, the container will fail to start. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="field_ref" type="object">
                          Selects a field of the pod. Supports metadata.name, metadata.namespace, metadata.labels['&lt;KEY&gt;'], metadata.annotations['&lt;KEY&gt;'], spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, and status.podIPs. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="api_version" type="string">
                              The API version of the referent field (e.g., 'v1') used to interpret the fieldPath in a field reference for environment variable values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="field_path" type="string">
                              Specifies the path to a field in the pod object (e.g., metadata.name, status.podIP) from which the value is selected. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="file_key_ref" type="object">
                          Optional; if omitted, computed by Kubernetes.
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key inside the referenced file whose value is used for the environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the referenced file key is optional. If set to true, the absence of the key is allowed; otherwise, the key is required. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="volume_name" type="string">
                              The name of the volume that contains the file referenced by the file key. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="resource_field_ref" type="object">
                          ResourceFieldSelector that selects a resource field (such as limits.cpu or requests.memory) from the container to populate the environment variable's value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="container_name" type="string">
                              Name of the container to select the resource value from. If not specified, defaults to the container where this environment variable is defined. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="divisor" type="string">
                              The divisor for the resource value. The environment variable value is the resource value divided by this divisor. If not specified, it defaults to 1. Must be a valid Kubernetes quantity (e.g., '1', '100m'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="resource" type="string">
                              The name of the container resource to select, such as 'limits.cpu' or 'requests.memory'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="secret_key_ref" type="object">
                          SecretKeyRef selects a key of a Secret in the pod's namespace. This is used to populate an environment variable from a Kubernetes secret. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key of the secret to select from. Specifies which entry in the Secret's data map to use. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the Secret whose key is referenced. This is the name of the secret in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Indicates whether the Secret key must be defined. If true, the system will not fail if the key is missing; if false (default), the key must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="env_from" type="list(object)">
                  List of sources to populate environment variables in the container. Each source can be a ConfigMap or a Secret, with optional prefix for variable names. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="config_map_ref" type="object">
                      Reference to a ConfigMap that supplies environment variables to the container, used as one source in the envFrom list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the ConfigMap to reference. This ConfigMap's data will be used to populate environment variables in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Indicates whether the referenced ConfigMap must be defined. When true, the ConfigMap may be absent without causing an error, and other envFrom sources are still used. When false (default), the ConfigMap must exist or the pod creation fails. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="prefix" type="string">
                      Optional prefix that is prepended to each environment variable name derived from the referenced ConfigMap or Secret, preventing name collisions when using multiple sources. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      The Secret to use as the source of environment variables. The Secret's data keys become environment variable names, and their values become the environment variable values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the Secret whose data will be exposed as environment variables in the container. It must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Specifies whether the referenced Secret must be defined. If true, and the Secret is not found, the environment variables from this source are skipped and the container can still start. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="image" type="string">
                  The container image to run, specified as an image reference like `repository:tag` or `registry/repository:tag`. If the tag is omitted, the `latest` tag is used by default. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="image_pull_policy" type="string">
                  Specifies when the kubelet should pull the container image. Allowed values: Always, IfNotPresent, Never. Default is IfNotPresent for images with a tag, Always for images with :latest, and Never for images with :digest. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="lifecycle" type="object">
                  Defines lifecycle hooks for the container, including postStart and preStop handlers that specify actions to run at container startup or before termination. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="post_start" type="object">
                      postStart is a lifecycle hook that runs immediately after a container is created. It can contain an exec command or an HTTP request to be executed, though the container's main process may start before the hook completes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          The exec action to run a command inside the container as the postStart lifecycle hook. This is an object that defines the command to execute. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              The command to run inside the container as the post-start lifecycle hook. It is specified as an array of strings, where the first element is the executable and subsequent elements are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          Specifies an HTTP GET request to perform as the post-start lifecycle hook. The request is sent to the container's IP address and port as specified in this action. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the HTTP GET request. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              Specifies custom HTTP headers to include in the GET request. Each item in the list contains a `name` and `value` string pair. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The name of the HTTP header to include in the HTTP request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header entry. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              The URL path for the HTTP GET request sent to the container's IP address during the postStart lifecycle hook. If not specified, defaults to '/'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The port number (1-65535) or IANA service name to access on the container for the HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              The protocol used for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              Specifies the scheme (HTTP or HTTPS) to use for the HTTP GET request. Allowed values are "HTTP" and "HTTPS"; defaults to "HTTP". Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          Defines a sleep action for the post-start lifecycle hook, pausing the container for a specified number of seconds (see the `seconds` field within this object). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              Seconds is the number of seconds to sleep during the postStart lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          TCPSocket specifies a TCP action for the postStart lifecycle hook. It attempts to open a TCP connection to the specified port on the given host; the hook succeeds if the connection is established. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the TCP socket check. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              Specifies the TCP port to connect to on the container. Accepts an integer port number (1-65535) or a named port defined in the container's ports. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="pre_stop" type="object">
                      pre_stop defines a lifecycle hook that is executed immediately before a container is terminated. It can specify an exec action or an HTTP GET request to trigger a custom action before the container is stopped. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          Exec specifies the action to run a command inside the container as the pre-stop lifecycle hook handler. This is mutually exclusive with the HTTP GET handler. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              The command to execute in the container during the pre-stop lifecycle hook. The first element is the command, and subsequent elements are arguments. It is exec'd directly, not run in a shell, so shell constructs are not expanded unless a shell is explicitly invoked. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          Specifies an HTTP GET request that the container runtime sends to the container as the pre-stop lifecycle hook. This is used to allow the container to gracefully shut down before it is terminated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the HTTP GET request. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              A list of custom HTTP headers to include in the HTTP GET request. Each header object specifies a name and value to be sent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The name of the HTTP header to include in the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header to send in the pre-stop lifecycle hook's HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Path to access on the HTTP server. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port number or named port on the container to which the preStop HTTP GET request is sent. Can be an integer value or a string specifying a named container port; this field is required when using an HTTP GET lifecycle handler. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              The protocol to use for the HTTP GET request. Allowed values are `HTTP` and `HTTPS`, with a default of `HTTP`. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The scheme to use for connecting to the host. Defaults to HTTP. Allowed values are HTTP and HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          Sleep specifies the SleepAction for the pre-stop lifecycle hook, which pauses the container for a defined period to allow graceful shutdown or finalization before termination. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              Specifies the duration of the sleep in seconds before the container is terminated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          TCPSocketAction specifies a TCP socket action. In a container lifecycle preStop hook, it connects to a specified port on the container, allowing the container to detect the connection before it is terminated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The hostname or IP address to connect to for the TCP socket. If not specified, it defaults to the pod IP address. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port to connect to on the container for the preStop lifecycle hook. This can be an integer port number or a named port that must be defined in the container's ports. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="stop_signal" type="string">
                      Specifies the signal to be sent to the container when it is being stopped. This is typically a signal name like SIGTERM or SIGKILL. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="liveness_probe" type="object">
                  Configures the liveness probe for the container. The kubelet periodically executes the probe; if it fails, the container is restarted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies a command to execute inside the container. The liveness probe succeeds if the command exits with status 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to run inside the container for the liveness probe. It is a list of strings where the first element is the command and the rest are arguments. The probe is considered successful if the command exits with a status code of 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures before the liveness probe is considered failed, causing the container to be restarted. The minimum value is 1; the default is 3. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      Configures a gRPC probe for the container. Specifies the port and optional service name to check for liveness. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number on which the gRPC server is listening for the liveness probe. This is a required field for gRPC probes and must be a valid TCP port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the service to place in the gRPC HealthCheckRequest. If not specified, the default is to check the overall server health (no service name). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies an HTTP GET request to perform for the liveness probe. This checks container health by making an HTTP GET request to the configured path and port on the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          Host name to connect to for the HTTP GET request. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          List of custom HTTP headers to include in the GET request sent to the container's liveness probe endpoint. Each header is an object containing a name and a value, allowing the probe to authenticate or pass metadata as required by the application. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The HTTP header name. For example, 'Accept' or 'Content-Type'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header to include in the request to the liveness probe endpoint. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The HTTP path to request when performing the liveness probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or name of the port to access on the container for the HTTP GET request. Can be an integer port number (1-65535) or an IANA_SVC_NAME (a named port defined in the container). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol (scheme) to use for the HTTP GET request, e.g., HTTP or HTTPS. Defaults to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for the HTTP GET request. Valid values are "HTTP" and "HTTPS". Defaults to "HTTP". Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before liveness probes are initiated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      How often (in seconds) to perform the liveness probe. Must be at least 1; defaults to 10 seconds. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      The minimum consecutive successes for the probe to be considered successful after having failed. For liveness probes, this value must be 1 (the default). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Defines a TCP socket probe for the liveness check. The kubelet attempts to open a TCP connection to the specified port on the container; if the connection succeeds, the probe is considered successful. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The hostname or IP address to connect to. If not specified, the pod's IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to check. This can be a numeric port number or a named port defined in the container's ports. The kubelet attempts to open a TCP connection to this port on the container's IP address. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The number of seconds to wait for the pod to terminate gracefully after receiving a termination signal; after this period, the pod is forcefully killed. This applies to the pod as a whole, not just the container's liveness probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the container within the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ports" type="list(object)">
                  List of ports to expose from the container. Each port entry typically includes a container port number and protocol, and may optionally specify a name and host port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="container_port" type="number">
                      The port number that the container exposes on the pod's IP address. Must be a valid port number (1-65535). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_ip" type="string">
                      The host IP address to bind the container port to. If not specified, the port is bound to all host interfaces (0.0.0.0). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_port" type="number">
                      The port number on the host node to which the container port is exposed. This enables external access to the container via the host's IP address. If omitted, the container port is not mapped to the host. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      Optional name for the container port. Must be a valid IANA_SVC_NAME (DNS label) and unique among all ports in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="protocol" type="string">
                      The protocol for the container port. One of TCP, UDP, or SCTP. Defaults to TCP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="readiness_probe" type="object">
                  Defines a probe that determines whether the container is ready to accept traffic. If the probe fails, the container is removed from service endpoints but is not restarted. Supports exec, HTTP GET, and TCP socket probe types along with timing options such as initialDelaySeconds, periodSeconds, and failureThreshold. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the action to take, which runs a command inside the container. It defines a command to execute to determine the health of the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          Command to run inside the container for the readiness probe. The first item is the command, and any remaining items are arguments passed to the command. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures required for the readiness probe to be considered failed, causing the container to be marked as not ready. Must be an integer greater than or equal to 1; defaults to 3. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      Configures the gRPC health check for the readiness probe. It specifies the port to check and an optional service name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number on the container where the gRPC server is listening for the readiness probe. Must be an integer in the range 1 to 65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the service to place in the gRPC HealthCheckRequest. If this field is not set, the default behavior follows the gRPC health checking protocol, typically using an empty service name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Performs an HTTP GET request against the container to determine its health within the readiness probe. Configures the endpoint, port, and optional HTTP headers for the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP request. Defaults to the pod IP. You generally should not set this; instead, set the 'Host' header via httpHeaders. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          A list of custom HTTP headers to include in the probe request. Each header object contains a name and a value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to be set in the probe request, such as 'X-Custom-Header'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The HTTP request path to access on the server. Defaults to '/' if not specified, for example '/healthz'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or name to access on the container for the HTTP GET request. Must be a valid port number (1-65535) or an IANA_SVC_NAME corresponding to a named port in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol to use for the HTTP GET request, either 'HTTP' or 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The URI scheme to use for the HTTP GET request. Allowed values are HTTP and HTTPS, defaults to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container starts before the readiness probe is initiated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      The frequency in seconds at which the readiness probe is performed. Must be a positive integer. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      Specifies the number of consecutive successful probe responses required for the container to be considered ready after a failed probe. The default is 1, and the value must be a positive integer (minimum 1). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Defines a TCP socket probe that checks readiness by attempting to open a TCP connection to a specific port (and optionally host) on the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name or IP address to connect to for the TCP socket check. If not specified, the pod's IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port number or named port to connect to for the readiness probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The number of seconds a pod is allowed to terminate gracefully before it is forcefully terminated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      The number of seconds after which the readiness probe times out. If the probe does not respond within this time, the container is considered not ready. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resize_policy" type="list(object)">
                  A list of container resize policies, allowing in-place updates of CPU and memory resources. Each policy specifies a resource (cpu or memory) and a restart policy (Always or OnFailure) that determines whether the container needs to restart to apply the resize. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="resource_name" type="string">
                      ResourceName is the name of the resource (e.g., 'cpu' or 'memory') to which this resize policy applies. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="restart_policy" type="string">
                      Specifies the policy for handling container resizes. Valid values are 'NotRequired' and 'RestartContainer', where 'RestartContainer' causes the kubelet to restart the container if the resize fails. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resources" type="object">
                  Compute resource requirements for the container, specifying requests (minimum guaranteed resources) and limits (maximum allowed resources) for CPU and memory. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="claims" type="list(object)">
                      Lists the names of resources, defined in the pod's spec.resourceClaims, that are used by this container. This field is alpha and may change in future Kubernetes versions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the ResourceClaim that this container requires. The claim must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="request" type="string">
                          The name of the ResourceClaim as defined in the ResourceClaimTemplate that this container requests. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="limits" type="map(string)">
                      limits specifies the maximum amount of compute resources allowed for the container. Each key is a resource name (e.g., cpu, memory) and the value is a Kubernetes quantity string (e.g., '500m', '2Gi'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="requests" type="map(string)">
                      A map of resource names to quantities (e.g., 'cpu', 'memory') specifying the minimum amount of compute resources required for the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="restart_policy" type="string">
                  Optional; if omitted, computed by Kubernetes.
                </ResponseField>
                <ResponseField name="restart_policy_rules" type="list(object)">
                  Optional; if omitted, computed by Kubernetes.
                  <Expandable title="properties">
                    <ResponseField name="action" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="exit_codes" type="object">
                      Optional; if omitted, computed by Kubernetes.
                      <Expandable title="properties">
                        <ResponseField name="operator" type="string">
                          The comparison operator used to evaluate the container exit code against the specified value in the restart policy rule. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="values" type="list(number)">
                          The list of numeric container exit codes to which this restart policy rule applies. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="security_context" type="object">
                  Security context that defines privilege and access control settings for the container, such as user/group ID, Linux capabilities, and SELinux options. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="allow_privilege_escalation" type="bool">
                      Controls whether a process in the container can gain more privileges than its parent process. When set to false, privilege escalation is disabled, which is a security hardening measure. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="app_armor_profile" type="object">
                      AppArmor profile to apply to the container. The profile type must be one of RuntimeDefault, Localhost, or Unconfined; if Localhost, the localhostProfile field names a pre-loaded profile on the node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          The name of the AppArmor profile to use when type is Localhost. This field is only used if the AppArmor profile type is set to Localhost. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          Defines the type of AppArmor profile to apply to the container. Valid values are 'RuntimeDefault', 'Localhost', and 'Unconfined'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="capabilities" type="object">
                      The capabilities field in a security context specifies Linux capabilities that can be added to or dropped from the container's default capability set. It contains an 'add' list for additional capabilities and a 'drop' list for capabilities to remove. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="add" type="list(string)">
                          Specifies the Linux capabilities to add to the container's capability set. For example, 'SYS_ADMIN' or 'NET_ADMIN'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="drop" type="list(string)">
                          List of capabilities to drop from the container's effective capabilities. Each entry is a capability name (e.g., 'ALL' or 'NET_RAW'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="privileged" type="bool">
                      Run container in privileged mode. Processes in privileged containers are essentially equivalent to root on the host. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="proc_mount" type="string">
                      The type of proc mount to use for the container. It defaults to 'Default', which uses the runtime default; set to 'Unmasked' to bypass the default masking of /proc. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only_root_filesystem" type="bool">
                      Mounts the container's root filesystem as read-only. When set to true, the root filesystem becomes read-only, preventing processes from writing to it. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_group" type="number">
                      The GID to run the entrypoint of the container process. If not specified, the container runs with the group ID specified in the image or the runtime default. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_non_root" type="bool">
                      If true, the container will run as a non-root user, preventing it from running with root privileges. This is a Kubernetes security context setting. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_user" type="number">
                      The numeric UID to run the container process as. Must be a non-negative integer. If unspecified, defaults to the user defined in the container image's metadata. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="se_linux_options" type="object">
                      SELinux options for the container, specifying the SELinux context (user, role, type, and level) applied to the container's processes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="level" type="string">
                          The SELinux level (MLS/MCS) for the container, such as 's0' or 's0:c0'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="role" type="string">
                          The SELinux role label for the container. This label is part of the SELinux context and is used to enforce role-based access control. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The SELinux type label, such as 'spc_t', applied to the container. This is part of the SELinux security context options. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="user" type="string">
                          The SELinux user label assigned to the container, such as 'system_u'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="seccomp_profile" type="object">
                      SeccompProfile holds the seccomp profile configuration for the container. The type field specifies the profile type: RuntimeDefault, Unconfined, or Localhost; if Localhost, localhostProfile must name a profile path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          The path to a seccomp profile defined on the node's filesystem. This profile must be preconfigured on the node to be used by the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The seccomp profile type. Allowed values are 'RuntimeDefault', 'Unconfined', and 'Localhost'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="windows_options" type="object">
                      Windows-specific security context options for the container. These settings are applied only when the container is running on a Windows node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="gmsa_credential_spec" type="string">
                          The GMSA credential spec (as a string) used for Windows container security context. This is the full credential spec content, not the name of a credential spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="gmsa_credential_spec_name" type="string">
                          The name of the GMSA credential spec to use for the container's Windows security context. This references a pre-configured Group Managed Service Account credential spec on the Windows node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="host_process" type="bool">
                          Determines if the container should run as a HostProcess container. When true, it grants the container access to the host's process namespace and privileges. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_user_name" type="string">
                          The UserName in Windows to run the container's entrypoint. If not specified, the container runs as the container's default user. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="startup_probe" type="object">
                  Defines a probe that checks whether the application within the container has started successfully. If the startup probe fails, the container is restarted, and the liveness probe is not started until the startup probe succeeds. Useful for containers with slow startup times. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the exec action for the startup probe. It defines a command to run inside the container to determine if the container is ready. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to run inside the container for the startup probe. It is executed directly without a shell; an exit status of 0 indicates success (healthy). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures required for the startup probe to be considered failed, after which the container is restarted. Must be a positive integer; the default is 3. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      grpc specifies the gRPC action for the startup probe. It contains the port to probe and optionally the service name to check, enabling gRPC-based health checks. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          Port number on the container that the gRPC server is listening on, used for the startup probe. Must be an integer between 1 and 65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the service to place in the gRPC health check request. If not set, the default is the pod name (lowercased). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies an HTTP GET request to perform on the container as part of the startup probe. The probe succeeds if the HTTP response status code is in the 200-399 range. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP GET request. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          Custom HTTP headers to send with the probe request. Each item defines a header name and value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The HTTP header field name to include in the probe request. For example, 'X-Custom-Header'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header to include in the startup probe's HTTP GET request. For example, if the header name is 'X-Custom-Header', this field holds the corresponding value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The URL path to access on the HTTP server. Defaults to '/' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or name of the port to access on the container. Accepts a numeric port (as a string) or a named port defined in the container's ports. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol (scheme) to use for the HTTP GET request. Valid values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for the HTTP request. Valid values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before the startup probe is initiated. Must be a non-negative integer. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      The interval, in seconds, between successive startup probe checks. If unspecified, Kubernetes defaults this to 10 seconds. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      Minimum consecutive successes for the probe to be considered successful after having failed. For startup probes, this value must be 1. Defaults to 1 if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Specifies a TCP socket probe handler that checks if a TCP connection can be established to the container on the given port. Used as an alternative to HTTP or command probes in the startup probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name or IP address to connect to for the TCP socket probe. If not set, defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to probe. Accepts a numeric port (e.g., '80') or a named port (e.g., 'http') defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The number of seconds to allow the pod to terminate gracefully after the startup probe fails. If set, this overrides the pod's default termination grace period. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the probe times out. Defaults to 1 second; must be at least 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="stdin" type="bool">
                  Whether this container should allocate a buffer for stdin in the container runtime. If not set, reads from stdin in the container will always result in EOF. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stdin_once" type="bool">
                  If true, the container's stdin is expected to be a TTY, and stdin will be redirected to the container only once (on container start). Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_path" type="string">
                  The path inside the container where the termination message is written. The kubelet reads this file to retrieve information about the container's termination reason. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_policy" type="string">
                  Specifies how the container's termination message is read. Valid values are 'File' (default) and 'FallbackToLogsOnError'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="tty" type="bool">
                  Indicates whether a TTY (teletypewriter) should be allocated for the container. When true, the container gets an interactive terminal. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volume_devices" type="list(object)">
                  volume_devices is a list of volumeDevice objects that define mappings of raw block devices within the container. Each entry specifies the volume name (name) and the device path inside the container (devicePath). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="device_path" type="string">
                      The path inside the container where the device will be mounted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the volume to which this device applies. Must match the name of a volume defined in the pod's volumes list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="volume_mounts" type="list(object)">
                  List of volume mounts to attach to the container. Each mount specifies the volume name, mount path, and options like read-only. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="bind_mount_options" type="list(string)">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="mount_path" type="string">
                      Path within the container at which the volume will be mounted. Must be an absolute path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mount_propagation" type="string">
                      Determines how mounts are propagated from the host to container and vice versa. Allowed values are None, HostToContainer, and Bidirectional. When not specified, None is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      Name of the volume to mount. Must match the name of a volume defined in the pod spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Mounts the volume as read-only when true. Defaults to false (read-write). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="recursive_read_only" type="string">
                      Controls whether a read-only mount is applied recursively to all volumes under the mount path. Valid values are Enabled, Disabled, and IfPossible. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path" type="string">
                      Mounts a sub-path of the referenced volume instead of its root, allowing you to mount a specific directory or file from a volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path_expr" type="string">
                      Specifies a sub-path of the volume to mount, with support for environment variable references using $(VAR_NAME) syntax. The expression is expanded using container environment variables before the volume is mounted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="working_dir" type="string">
                  The working directory inside the container where commands are executed. If not set, the default is the image's WORKDIR or the container's root directory. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="dns_config" type="object">
              PodDNSConfig defines the DNS parameters of a pod in addition to those generated from DNSPolicy. Optional.
              <Expandable title="properties">
                <ResponseField name="nameservers" type="list(string)">
                  A list of DNS name server IP addresses. This will be appended to the base nameservers generated from DNSPolicy. Duplicated nameservers will be removed. Optional.
                </ResponseField>
                <ResponseField name="options" type="list(object)">
                  A list of DNS resolver options. This will be merged with the base options generated from DNSPolicy. Duplicated entries will be removed. Resolution options given in Options will override those that appear in the base DNSPolicy. Optional.
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the DNS resolver option, as defined in resolv.conf (e.g., 'ndots', 'timeout'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of the DNS resolver option. This field is optional; if omitted, the option is treated as a flag or empty string. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="searches" type="list(string)">
                  A list of DNS search domains for host-name lookup. This will be appended to the base search paths generated from DNSPolicy. Duplicated search paths will be removed. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="dns_policy" type="string">
              Set DNS policy for the pod. Defaults to "ClusterFirst". Valid values are 'ClusterFirstWithHostNet', 'ClusterFirst', 'Default' or 'None'. DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy. To have DNS options set along with hostNetwork, you have to specify DNS policy explicitly to 'ClusterFirstWithHostNet'. Optional.
            </ResponseField>
            <ResponseField name="enable_service_links" type="bool">
              EnableServiceLinks indicates whether information about services should be injected into pod's environment variables, matching the syntax of Docker links. Optional: Defaults to true. Optional.
            </ResponseField>
            <ResponseField name="ephemeral_containers" type="list(object)">
              List of ephemeral containers run in this pod. Ephemeral containers may be run in an existing pod to perform user-initiated actions such as debugging. This list cannot be specified when creating a pod, and it cannot be modified by updating the pod spec. In order to add an ephemeral container to an existing pod, use the pod's ephemeralcontainers subresource. Optional.
              <Expandable title="properties">
                <ResponseField name="args" type="list(string)">
                  A list of arguments to pass to the command for the ephemeral container. If not specified, the image's default arguments are used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="command" type="list(string)">
                  The command to run in the ephemeral container, as a list of strings. This overrides the image's entrypoint; the first element is the executable and the rest are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="env" type="list(object)">
                  List of environment variables to set in the ephemeral container. Each entry specifies a variable name and either a direct value or a value source (e.g., configMapKeyRef, secretKeyRef, fieldRef). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Name of the environment variable. Must be a valid environment variable name consisting of alphanumeric characters, '_', or '.', and must not start with a digit. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The literal value of the environment variable. This field is mutually exclusive with `valueFrom`, which can be used to set the value from a source like a config map or secret. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value_from" type="object">
                      Specifies a source for the value of the environment variable, such as a reference to a ConfigMap key, Secret key, or other field/resource reference. Cannot be used if 'value' is set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="config_map_key_ref" type="object">
                          Selects a key from a ConfigMap to populate the environment variable's value. Contains the ConfigMap name, the key to reference, and an optional flag indicating whether the key must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The name of the key in the referenced ConfigMap to select the value from. This field is required. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the ConfigMap to select from. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the referenced ConfigMap key must exist. If set to true, the container can start even if the key is not present in the ConfigMap. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="field_ref" type="object">
                          Selects a field of the pod (or container) to use as the value of the environment variable. The fieldPath subfield specifies the field to select, such as metadata.name, status.podIP, or spec.nodeName. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="api_version" type="string">
                              The API version of the schema that fieldPath is written in terms of. Defaults to 'v1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="field_path" type="string">
                              The path to the field in the referenced object from which to select the value. For example, 'metadata.name' or 'status.podIP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="file_key_ref" type="object">
                          Optional; if omitted, computed by Kubernetes.
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key to select from the referenced file (e.g., ConfigMap or Secret) to obtain the value for the environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Boolean flag indicating whether the referenced key is optional. If true, a missing key does not prevent the container from starting; the environment variable is simply not set. If false, the key must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              The path to the file that contains the key referenced by the fileKeyRef. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="volume_name" type="string">
                              The name of the volume that contains the file referenced by this file key reference. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="resource_field_ref" type="object">
                          ResourceFieldRef selects a resource field (such as limits.cpu or requests.memory) from the container. The value of that field is used as the value of the environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="container_name" type="string">
                              Container name: required if the resource is a container resource, ignored if it is a pod resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="divisor" type="string">
                              Divisor is the value by which the resource value is divided. Defaults to 1 if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="resource" type="string">
                              The name of the resource to select from the container, such as 'limits.cpu' or 'requests.memory'. This is a standard Kubernetes resource quantity name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="secret_key_ref" type="object">
                          Selects a key of a Secret in the pod's namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key of the secret to select from. Must be a valid secret key. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the Secret from which to select the key. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the referenced secret key must exist. When set to true, the key is optional and its absence is tolerated; when false (default), the key must exist or an error is raised. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="env_from" type="list(object)">
                  List of sources to populate environment variables in the ephemeral container. Each entry can reference a ConfigMap or Secret to inject environment variables. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="config_map_ref" type="object">
                      configMapRef specifies the ConfigMap to use as a source for environment variables in the container. The referenced ConfigMap must exist in the same namespace, and its keys become environment variable names. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the ConfigMap to reference for environment variables. The ConfigMap must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Specifies whether the referenced ConfigMap must exist. If set to true, the ConfigMap is optional; if false (default), the ConfigMap must exist or the container will not start. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="prefix" type="string">
                      An optional identifier to prepend to each key in the referenced ConfigMap or Secret. Must be a valid C identifier. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      Selects a Kubernetes Secret whose keys and values are exposed as environment variables. The block contains 'name' (the Secret name) and 'optional' (whether the Secret must exist). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Secret to reference. The Secret must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Specify whether the referenced Secret must be defined. If set to true, the Pod will start even if the Secret is missing; if false (default), the Secret must exist before the Pod starts. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="image" type="string">
                  The container image to use for the ephemeral container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="image_pull_policy" type="string">
                  Determines when the kubelet pulls the container image for the ephemeral container. Valid values are Always, IfNotPresent, and Never; if not specified, the default is Always if the image tag is 'latest', otherwise IfNotPresent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="lifecycle" type="object">
                  Lifecycle describes actions that the container runtime should take in response to container lifecycle events (postStart and preStop). It defines handlers executed at container startup and before termination. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="post_start" type="object">
                      postStart represents the lifecycle hook that is executed immediately after a container is started. It can specify an exec command or a TCP/HTTP request to perform at container startup. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          Exec specifies the action to run a command inside the container as the post-start lifecycle hook. This object contains a 'command' field that is a list of strings to execute. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              The command line to execute inside the container. The working directory is '/' and it is not run inside a shell, so shell directives like pipes or redirection won't work unless you explicitly invoke a shell. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          HTTPGetAction describes an action based on an HTTP GET request. It configures the HTTP request sent to the container's endpoint to trigger the lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name or IP address to connect to for the HTTP GET request. If not set, defaults to the pod IP. You probably want to set the 'Host' header in httpHeaders instead. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              A list of custom HTTP headers to include in the GET request. Each item is an object containing a header name and value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The name of the HTTP header to send in the HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header to include in the HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Path to access on the HTTP server. Defaults to '/' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              Name or number of the port to access on the container. If a number, it must be in the range 1 to 65535. If a name, it must be an IANA_SVC_NAME. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              The protocol to use for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The URI scheme used for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS', with a default of 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          Configures a sleep action in the postStart lifecycle hook, pausing the container for a specified duration before it continues startup. This field corresponds to the Kubernetes SleepAction, available from v1.29. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              The number of seconds the container sleeps when this lifecycle hook is triggered. Must be a non-negative integer. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          Defines a TCP socket check to be performed during the postStart lifecycle hook, with fields for host and port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              Optional host name to connect to for the TCP socket. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port to connect to. Can be a numeric port or a string referencing a named port in the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="pre_stop" type="object">
                      Configuration for a lifecycle hook that runs before the container is terminated. This can specify an exec command or an HTTP request to perform actions such as graceful shutdown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          Exec specifies the action to take, which is to run a command inside the container as a pre-stop lifecycle hook. It corresponds to a Kubernetes ExecAction and contains the command to be executed before the container is terminated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              The command line to execute inside the ephemeral container as part of the pre-stop lifecycle hook. The command is run directly without a shell, so shell features like pipes or redirects are not interpreted; the first element is the executable and subsequent elements are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          Specifies an HTTP GET request to run as part of the pre-stop lifecycle hook. It configures the endpoint (host, path, port, scheme) and optional headers for the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the HTTP GET request. Defaults to the pod IP; can be overridden, e.g., to use a service name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              HTTP headers to send in the HTTP GET request. Each item is an object containing the header name and value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The name of the HTTP header, such as 'X-Custom-Header'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header. In a Kubernetes lifecycle hook's HTTP GET request, each header is a name-value pair, and this field provides the value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Path to access on the HTTP server. Defaults to '/'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The port number to access on the container for the HTTP GET request. Must be a valid port number (1-65535) or the name of an existing container port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              The protocol to use when making the HTTP GET request, such as HTTP or HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The URL scheme to use for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'; defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          Sleep action for the preStop lifecycle hook. It defines a duration in seconds (via the 'seconds' field) for which the container will sleep before being terminated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              Seconds specifies the duration, in seconds, to sleep before the container is terminated, as part of the preStop lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          Represents a TCP socket action as part of a pre-stop lifecycle hook. This action attempts to open a TCP connection to the specified port on the container's IP address to determine whether the hook succeeds. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name or IP address to connect to for the TCP socket check. If not specified, the pod IP is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port number or named port on the container to send the pre-stop hook request to. The value can be an integer port number or a string naming a port in the container's ports list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="stop_signal" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="liveness_probe" type="object">
                  Liveness probe configuration for the ephemeral container. Specifies how to check container health and whether to restart it if unhealthy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the exec action, which runs a command inside the container to check liveness. It is a list of command arguments where the first element is the command and subsequent elements are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          Command to run inside the container for the liveness probe's exec action. A list of strings where the first element is the executable and the rest are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      Minimum consecutive failures for the liveness probe to be considered failed after having succeeded. Defaults to 3. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      Configuration for a gRPC health check, used in place of an HTTP or TCP probe. Requires a port number, and may specify a service name (as defined by the gRPC Health Checking Protocol). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number of the gRPC service that the probe will connect to. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the service to place in the gRPC HealthCheckRequest. If not specified, the default behavior is defined by the gRPC health check protocol. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies an HTTP GET request to perform as the liveness probe. If the probe receives an HTTP response with a status code between 200 and 399, the container is considered healthy; otherwise, it is considered failed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP GET request. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          Custom HTTP headers to include in the HTTP GET request for the probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to include in the request, such as 'X-Custom-Header'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header field. This is combined with the header name to form a header sent in the HTTP GET request for the liveness probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          Path to access on the HTTP server for this HTTP GET request of the liveness probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or name of the port to access on the container. Must be a valid port number (1-65535) or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol (HTTP or HTTPS) to use for the HTTP GET request from the liveness probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for the HTTP GET request, either HTTP or HTTPS. Defaults to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before liveness probes are initiated. Defaults to 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      How often (in seconds) to perform the liveness probe. Defaults to 10 seconds; minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      The minimum consecutive successes required for the liveness probe to be considered successful after a failure. For liveness probes, this value must be set to 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Specifies a TCP socket probe for the liveness check. This object configures the host and port to attempt a TCP connection, and the probe is considered successful if the connection is established. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The hostname or IP address to connect to for the TCP socket liveness probe. If omitted, the pod IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          Number or name of the port to access on the container. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The number of seconds the pod is allowed to terminate gracefully after receiving a termination signal before it is forcefully killed. This is a pod-level setting applied to all containers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the liveness probe times out. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the ephemeral container in the pod spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ports" type="list(object)">
                  List of ports to expose from the ephemeral container. Each item typically includes containerPort, protocol, and optionally hostPort, hostIP, and name, following the Kubernetes container port specification. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="container_port" type="number">
                      Port number that the container listens on. Must be a valid port number between 1 and 65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_ip" type="string">
                      The host IP address to bind the container port to. When set, the port will be exposed only on that specific host IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_port" type="number">
                      The port on the host node that maps to the container port. This allows the container to be accessed externally via the node's IP and this port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      Optional name for the port. Must be a valid IANA_SVC_NAME and unique among container ports. Required when multiple ports are specified in the same container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="protocol" type="string">
                      The network protocol for the port, such as TCP, UDP, or SCTP. Defaults to TCP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="readiness_probe" type="object">
                  The readiness probe for the ephemeral container. Kubernetes does not support readiness probes on ephemeral containers, so this field is rejected by the API server if set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies a command to run inside the container. The probe succeeds if the command exits with status 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to run inside the container for the readiness probe. It is specified as an array of strings, where the first element is the command and subsequent elements are arguments. If omitted, the container's entrypoint is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      Specifies the number of consecutive failures required for the readiness probe to be considered failed. The default is 3. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      Specifies the gRPC health check configuration for the probe. It contains the port to connect to and an optional service name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number to connect to for the gRPC health check. Must be a valid TCP port (1-65535). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the service to place in the gRPC HealthCheckRequest. If not set, the default is empty, meaning no service name is sent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies the HTTP GET request to perform for the readiness probe. This block configures parameters such as the path, port, scheme, and headers used to check the health of the ephemeral container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name or IP address to connect to for the HTTP GET request. If not specified, the pod IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          List of custom HTTP headers to include in the probe request. Each header is a name-value pair, with 'name' and 'value' fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to include in the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header sent in the readiness probe's HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          Path to access on the HTTP server. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or name of the port to access on the container for the HTTP GET probe. Must be a valid integer port (1-65535) or an IANA service name (e.g., 'http'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The network protocol to use for the HTTP probe: either 'HTTP' or 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for connecting to the host. Defaults to HTTP. Allowed values are HTTP and HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before the readiness probe is initiated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      Specifies the frequency (in seconds) at which the readiness probe is performed. Defaults to 10 seconds. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      The minimum consecutive successes required for the probe to be considered successful after having failed. For readiness probes, the default is 1; set higher to require multiple consecutive successes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      The TCP socket action for the readiness probe. It checks whether a TCP connection can be established to the specified port on the container, typically defined with a 'port' field. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name or IP address to connect to for the TCP socket probe. If not specified, defaults to the pod IP address. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to connect to. Can be a numeric port or a named port on the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      Specifies the duration in seconds that a pod is allowed to terminate gracefully before being forcefully killed. If set to 0, the pod is deleted immediately; if unset, the Kubernetes default grace period (30 seconds) is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the readiness probe times out. If the probe does not respond within this time, it is considered failed. Minimum value is 1, and the default is 1 second. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resize_policy" type="list(object)">
                  Resize policy for the ephemeral container, specifying how each resource can be resized and the restart policy applied (e.g., NotRequired or RestartContainer). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="resource_name" type="string">
                      The name of the resource to which the resize policy applies. In Kubernetes, valid values are 'cpu' and 'memory'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="restart_policy" type="string">
                      Specifies the restart behavior for an ephemeral container after a resize operation. Valid values are 'NotRequired' and 'RestartContainer'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resources" type="object">
                  Specifies the compute resources required for the ephemeral container, including requests and limits for resources such as CPU and memory. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="claims" type="list(object)">
                      A list of resource claims that this ephemeral container uses. Each claim references a resource defined in the pod's spec.resourceClaims, enabling the container to request dynamic cluster resources. This field corresponds to the Kubernetes ResourceRequirements `claims` field, which is an alpha feature. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name must match the name of one entry in pod.spec.resourceClaims of the Pod where this field is used. It makes that resource available inside a container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="request" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="limits" type="map(string)">
                      Limits describes the maximum amount of compute resources allowed. The keys of the map are resource names (e.g., cpu, memory), and the values are quantities (e.g., "100m", "1Gi"). More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/ Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="requests" type="map(string)">
                      Requests describes the minimum amount of compute resources required for the ephemeral container. It is a map of resource names (e.g., cpu, memory) to quantities; if requests are omitted, they default to limits if limits are explicitly specified, otherwise to an implementation-defined value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="restart_policy" type="string">
                  Optional; if omitted, computed by Kubernetes.
                </ResponseField>
                <ResponseField name="restart_policy_rules" type="list(object)">
                  Optional; if omitted, computed by Kubernetes.
                  <Expandable title="properties">
                    <ResponseField name="action" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="exit_codes" type="object">
                      Optional; if omitted, computed by Kubernetes.
                      <Expandable title="properties">
                        <ResponseField name="operator" type="string">
                          The comparison operator used to evaluate container exit codes against specified rules, determining whether a restart policy applies. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="values" type="list(number)">
                          The list of container exit codes that trigger this restart policy rule. The rule applies when the container exits with one of these numeric codes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="security_context" type="object">
                  SecurityContext defines the security options the ephemeral container should be run with, including user ID, group ID, Linux capabilities, SELinux options, and whether the container is privileged. If set, these settings override the Pod-level security context for this container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="allow_privilege_escalation" type="bool">
                      Controls whether a process can gain more privileges than its parent process. When set to true, privilege escalation is allowed; when false, it is prevented. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="app_armor_profile" type="object">
                      Defines the AppArmor profile for the container, including the profile type (RuntimeDefault, Localhost, Unconfined) and an optional localhost profile path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          Specifies a profile loaded on the node that the pod is running on. This is required when the AppArmor profile type is set to Localhost. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          Type of AppArmor profile to use. Valid values are: 'RuntimeDefault' uses the container runtime's default profile, 'Localhost' uses a profile pre-loaded on the node, and 'Unconfined' disables AppArmor enforcement. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="capabilities" type="object">
                      The Linux capabilities to add to or drop from the container's process, specified as add and drop lists. Part of the security context for ephemeral containers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="add" type="list(string)">
                          Linux capabilities to add to the ephemeral container's capability set. Each entry is a capability name, such as 'CAP_SYS_ADMIN'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="drop" type="list(string)">
                          Drops the specified Linux capabilities from the container's default set. Entries are capability names like 'ALL' or 'CAP_NET_ADMIN'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="privileged" type="bool">
                      Indicates whether the container runs in privileged mode, granting it access to all host devices and capabilities. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="proc_mount" type="string">
                      The type of proc mount to use for the ephemeral container. Supported values are DefaultProcMount (default) and UnmaskedProcMount. This field is alpha and only honored when the kubelet enables the ProcMountType feature gate. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only_root_filesystem" type="bool">
                      If true, the container's root filesystem is mounted as read-only. This prevents writes to the root filesystem; writes must go to a writable volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_group" type="number">
                      The GID (group ID) to run the container's entrypoint process as. If not specified, the group ID defaults to the image's primary group. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_non_root" type="bool">
                      Indicates whether the ephemeral container must run as a non-root user. If set to true, the container will be prevented from running as root and will fail if the image's user is root or unspecified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_user" type="number">
                      The user ID (UID) to run the ephemeral container process as. If unspecified, the pod-level or container-level security context applies. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="se_linux_options" type="object">
                      SELinux options to apply to the ephemeral container's security context. This object defines the SELinux label (user, role, type, and level) for the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="level" type="string">
                          The SELinux level (also known as sensitivity or MLS/MCS label) that applies to the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="role" type="string">
                          The SELinux role label for the container. In Kubernetes, seLinuxOptions define the SELinux context, and the role field sets the role portion (e.g., system_u). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The SELinux type label for the context, as defined in SELinuxOptions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="user" type="string">
                          The SELinux user label for the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="seccomp_profile" type="object">
                      SeccompProfile defines the seccomp profile for the container. It allows specifying the type of seccomp profile (e.g., RuntimeDefault, Unconfined, Localhost) and optionally a localhost profile path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          The path to a seccomp profile on the node, used when the seccomp_profile type is 'Localhost'. This path is typically relative to the kubelet's seccomp profile root directory (e.g., /var/lib/kubelet/seccomp). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The type of seccomp profile to use. Allowed values are 'RuntimeDefault', 'Localhost', and 'Unconfined'. When 'Localhost' is used, the 'localhostProfile' field must be specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="windows_options" type="object">
                      Specifies Windows-specific security options for the container, such as GMSA credentials and the username to run container processes as. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="gmsa_credential_spec" type="string">
                          GMSACredentialSpec is where the GMSA admission webhook (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the GMSA credential spec named by the GMSACredentialSpecName field. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="gmsa_credential_spec_name" type="string">
                          The name of the GMSA credential spec to use for the ephemeral container's Windows security context. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="host_process" type="bool">
                          Indicates whether the Windows container should be run as a HostProcess container, granting it access to the host's process and network namespaces. This is only applicable to Windows containers running with containerd. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_user_name" type="string">
                          The Windows username to run the container's processes as. This is part of the Windows security context options for ephemeral containers in a DaemonSet pod spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="startup_probe" type="object">
                  Specifies the startup probe for the container. The startup probe indicates whether the application inside the container has started successfully. If it fails, the kubelet kills the container following the container's restart policy. Supported probe types include HTTP, TCP, and exec, and the probe can be configured with fields such as initialDelaySeconds, periodSeconds, timeoutSeconds, and successThreshold. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the action for the startup probe by running a command inside the container. The probe is considered successful if the command exits with status 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to execute for the startup probe's exec action. It is a list of strings: the first element is the executable and subsequent elements are arguments. The command runs directly inside the container without a shell. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures of the startup probe that must occur before the container is considered unhealthy and is restarted. The value must be a positive integer. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      GRPC specifies an action involving a gRPC port. This can be used to perform a gRPC health check on the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          Port number for the gRPC health check. Must be between 1 and 65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          Service is the name of the service to place in the gRPC HealthCheckRequest. If not specified, the default behavior is defined by the gRPC health checking protocol. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies the HTTP GET request used by the startup probe to check container health. Includes configuration like target host, port, HTTP path, and headers for the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          Host name to connect to for the HTTP request. Defaults to the pod IP. You probably want to set 'Host' in httpHeaders instead. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          Custom headers to set in the HTTP request. Each header is an object with `name` and `value` fields. Allows specifying multiple headers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to include in the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header to send in the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          Path to access on the HTTP server for the probe. Defaults to '/'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port to access on the container. This can be a numeric port number (1-65535) or a named port as defined in the container's ports specification. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The network protocol to use for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          Scheme to use for connecting to the host. Defaults to HTTP. Allowed values: HTTP, HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before the startup probe is initiated. This delays the start of the probe to allow the container to initialize. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      The number of seconds between each probe execution. The minimum value is 1 second. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      The minimum number of consecutive successes required for the startup probe to be considered successful after a failure. For startup probes, this value must be 1 and it defaults to 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      TCP socket probe action for the startup probe. Specifies the host and port to check, performing a TCP connection attempt to determine if the container has started. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The hostname or IP address to connect to for the TCP socket check. If not specified, the pod's IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to connect to. Can be a numeric port (e.g., 8080) or a named port defined in the pod's containers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      Optional duration in seconds that the pod needs to terminate gracefully when a startup probe fails. Overrides the pod-level `termination_grace_period_seconds` for the corresponding container; must be a non-negative integer, where zero means immediate termination. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the startup probe times out. If the probe does not finish within this time, it is considered failed. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="stdin" type="bool">
                  Specifies whether the standard input stream for the ephemeral container is enabled. When true, the container can read from standard input. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stdin_once" type="bool">
                  Whether stdin is closed after a single attach. If true, stdin is opened on container start, remains open until the first client attaches, then closes when that client disconnects and stays closed until the container restarts. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="target_container_name" type="string">
                  The name of the container that this ephemeral container attaches to. If unset, it defaults to the first container in the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_path" type="string">
                  The path inside the container where a termination message is written. When the container exits, Kubernetes reads this file to include in the pod's status. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_policy" type="string">
                  Defines how the termination message of the ephemeral container is read. Valid values are 'File' (default, reads the file specified by terminationMessagePath) and 'FallbackToLogsOnError' (uses the last chunk of container logs if the termination message file is empty). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="tty" type="bool">
                  Indicates whether a TTY should be allocated for the ephemeral container. If true, also requires 'stdin' to be true. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volume_devices" type="list(object)">
                  List of block devices to be used by the ephemeral container. Each item specifies a device path and the name of a volume to mount as a block device. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="device_path" type="string">
                      The path inside the container where the raw block device is mounted. This is used when a volume device is specified for a raw block volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      Name of the volume device. Must match the name of a volume in the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="volume_mounts" type="list(object)">
                  A list of volume mounts to expose to the ephemeral container, specifying the volume name, mount path, and mount options (e.g., read-only, subPath). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="bind_mount_options" type="list(string)">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="mount_path" type="string">
                      Path within the container at which the volume should be mounted. Must not contain ':'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mount_propagation" type="string">
                      Controls how volume mounts propagate from the host to the container and vice versa. Valid values are 'None', 'HostToContainer', and 'Bidirectional'. Default is 'None'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the volume to mount. Must match the name of a volume defined in the pod spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      If true, the volume is mounted as read-only; the container cannot write to it. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="recursive_read_only" type="string">
                      Controls whether the volume mount is recursive read-only. Allowed values are 'Enabled', 'Disabled', and 'IfPossible', with 'Disabled' being the default when unset. 'Enabled' requires the volume type to support recursive read-only; 'IfPossible' enables it only if the volume type supports it. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path" type="string">
                      The path within the volume from which to mount, rather than the volume's root. This allows mounting a subdirectory of a volume. Must be a relative path and cannot contain '..' to prevent escaping the volume root. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path_expr" type="string">
                      subPathExpr is an expression to compute the subPath directory to mount, using environment variable references (e.g., $(VAR)) to construct the path. It cannot be used together with subPath. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="working_dir" type="string">
                  The working directory for the ephemeral container. If not specified, the container runtime default is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="eviction_responders" type="list(object)">
              evictionResponders reference responders that react to Evictions based on EvictionRequests. Responders should observe and communicate through the Eviction Resource API to help with the graceful termination of a pod. The responders are selected sequentially, according to their specified priority. Responders should periodically report on an eviction progress by updating the .status.responders[].heartbeatTime field of the Eviction object. If this field is not updated within the heartbeat deadline defined by the Eviction API (currently 20 minutes), the eviction is passed over to the next responder with a lower priority. If there is no other responder, the last default imperative-eviction.k8s.io/evictor responder with a priority of 100 will evict the pod using the imperative Eviction API (pods/&lt;name&gt;/eviction subresource). The maximum length of the responders list is 10. Responders are not supported when the pod is part of a PodGroup (.spec.schedulingGroup is set). This field can only be set on creation and is immutable afterwards. Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  Optional; if omitted, computed by Kubernetes.
                </ResponseField>
                <ResponseField name="priority" type="number">
                  Optional; if omitted, computed by Kubernetes.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="host_aliases" type="list(object)">
              HostAliases is an optional list of hosts and IPs that will be injected into the pod's hosts file if specified. Optional.
              <Expandable title="properties">
                <ResponseField name="hostnames" type="list(string)">
                  The list of hostnames that should be resolved to the IP address specified in the same hostAlias entry. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ip" type="string">
                  The IP address to add as an alias for the specified hostnames in the pod's /etc/hosts file. Each entry in host_aliases maps an IP to a list of hostnames. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="host_ipc" type="bool">
              Use the host's ipc namespace. Optional: Default to false. Optional.
            </ResponseField>
            <ResponseField name="host_network" type="bool">
              Host networking requested for this pod. Use the host's network namespace. When using HostNetwork you should specify ports so the scheduler is aware. When `hostNetwork` is true, specified `hostPort` fields in port definitions must match `containerPort`, and unspecified `hostPort` fields in port definitions are defaulted to match `containerPort`. Default to false. Optional.
            </ResponseField>
            <ResponseField name="host_pid" type="bool">
              Use the host's pid namespace. Optional: Default to false. Optional.
            </ResponseField>
            <ResponseField name="host_users" type="bool">
              Use the host's user namespace. Optional: Default to true. If set to true or not present, the pod will be run in the host user namespace, useful for when the pod needs a feature only available to the host user namespace, such as loading a kernel module with CAP_SYS_MODULE. When set to false, a new userns is created for the pod. Setting false is useful for mitigating container breakout vulnerabilities even allowing users to run their containers as root without actually having root privileges on the host. Optional.
            </ResponseField>
            <ResponseField name="hostname" type="string">
              Specifies the hostname of the Pod If not specified, the pod's hostname will be set to a system-defined value. Optional.
            </ResponseField>
            <ResponseField name="hostname_override" type="string">
              HostnameOverride specifies an explicit override for the pod's hostname as perceived by the pod. This field only specifies the pod's hostname and does not affect its DNS records. When this field is set to a non-empty string: - It takes precedence over the values set in `hostname` and `subdomain`. - The Pod's hostname will be set to this value. - `setHostnameAsFQDN` must be nil or set to false. - `hostNetwork` must be set to false. This field must be a valid DNS subdomain as defined in RFC 1123 and contain at most 64 characters. Optional.
            </ResponseField>
            <ResponseField name="image_pull_secrets" type="list(object)">
              ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec. If specified, these secrets will be passed to individual puller implementations for them to use. More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  Name of the image pull secret, as defined in the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="init_containers" type="list(object)">
              List of initialization containers belonging to the pod. Init containers are executed in order prior to containers being started. If any init container fails, the pod is considered to have failed and is handled according to its restartPolicy. The name for an init container or normal container must be unique among all containers. Init containers may not have Lifecycle actions, Readiness probes, Liveness probes, or Startup probes. The resourceRequirements of an init container are taken into account during scheduling by finding the highest request/limit for each resource type, and then using the max of that value or the sum of the normal containers. Limits are applied to init containers in a similar fashion. Init containers cannot currently be added or removed. Cannot be updated. More info: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/ Optional.
              <Expandable title="properties">
                <ResponseField name="args" type="list(string)">
                  List of arguments to pass to the init container's entrypoint. If the command field is not set, the image's default entrypoint is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="command" type="list(string)">
                  Command to run in the init container. This overrides the ENTRYPOINT of the container image. Specified as a list of strings; the first element is the command, and the remaining elements are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="env" type="list(object)">
                  A list of environment variables for the init container. Each variable can be defined with a name and either a direct value or a source (such as a config map, secret, or field reference) from which the value is derived. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Name of the environment variable. Must be a valid C identifier, e.g., MY_VAR. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of the environment variable, provided as a string. This is the direct value that will be assigned to the environment variable in the container's init container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value_from" type="object">
                      value_from is an optional field that specifies a source for the environment variable's value. It can reference a ConfigMap, Secret, or a field in the pod spec (e.g., downward API). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="config_map_key_ref" type="object">
                          ConfigMapKeyRef selects a key from a ConfigMap to populate the environment variable's value. Specify the ConfigMap name and key; the optional field indicates whether the ConfigMap or its key must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key in the referenced ConfigMap to select the value from. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the ConfigMap from which to pull the value for this environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Indicates whether the ConfigMap key must exist. If set to true, the container starts even if the key or its ConfigMap is missing; if false (default), a missing key causes an error. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="field_ref" type="object">
                          Selects a field of the pod for the environment variable value. Common field paths include metadata.name, metadata.namespace, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, and label/annotation references via metadata.labels['KEY'] or metadata.annotations['KEY']. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="api_version" type="string">
                              The API version of the schema that the fieldPath refers to. Defaults to 'v1' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="field_path" type="string">
                              The path to the field to select, such as 'metadata.name' or 'spec.nodeName'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="file_key_ref" type="object">
                          Optional; if omitted, computed by Kubernetes.
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the referenced key is optional. If true, the container can start even if the key does not exist; if false or omitted, the key must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="volume_name" type="string">
                              The name of the volume to read the file key from. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="resource_field_ref" type="object">
                          Specifies a container resource field (e.g., limits.cpu or requests.memory) whose value is used as the environment variable's value. The selector includes the container name, resource, and optional divisor. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="container_name" type="string">
                              The name of the container to which the resource field refers. Defaults to the current container if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="divisor" type="string">
                              The value by which to divide the selected resource quantity. Defaults to 1 if unset. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="resource" type="string">
                              The name of the resource to select, such as 'limits.cpu' or 'requests.memory', from the container's resource field reference. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="secret_key_ref" type="object">
                          Selects a key of a Secret in the pod's namespace to use as the value for the environment variable. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="key" type="string">
                              The key within the Secret to fetch the value from. This must correspond to a valid key in the referenced Secret. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the Secret resource being referenced. Must be a valid Secret name in the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specify whether the Secret or its key must be defined. If set to true, and the secret or key is not found, the environment variable is not set; if false (default), the pod fails to start when the secret or key is missing. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="env_from" type="list(object)">
                  List of sources to populate environment variables in the init container. Each source can be a ConfigMap or Secret, or a reference to such resources, and variables are added to the container's environment. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="config_map_ref" type="object">
                      ConfigMapRef is a reference to a ConfigMap from which environment variables are populated. It contains the name of the ConfigMap and an optional flag to indicate if the ConfigMap must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the ConfigMap to reference. The ConfigMap must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Specifies whether the referenced ConfigMap is optional. If true, a missing ConfigMap is ignored and no environment variables are injected from it; if false or unset, the pod fails to start if the ConfigMap does not exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="prefix" type="string">
                      An optional prefix to apply to the keys of the referenced ConfigMap or Secret. When set, each environment variable name is prefixed with this value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      Reference to a Kubernetes Secret. The keys in the Secret's data become environment variables in the container. The Secret must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Secret to reference for populating environment variables. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optional" type="bool">
                          Specify whether the Secret must be defined. If set to true, the Secret is optional and its absence does not prevent pod startup; if false or omitted, the Secret must exist or the container fails to start. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="image" type="string">
                  The container image to run for this init container, typically specified as a repository, tag, or digest (e.g., busybox:1.36). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="image_pull_policy" type="string">
                  Specifies the image pull policy for the container. Valid values are Always, IfNotPresent, and Never. Defaults to Always if image tag is 'latest' or omitted, otherwise IfNotPresent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="lifecycle" type="object">
                  lifecycle is an object that defines lifecycle hooks for the container, such as postStart and preStop, which allow custom actions to be executed at container startup and before termination, respectively. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="post_start" type="object">
                      The post_start field defines the PostStart lifecycle hook, which is executed immediately after a container is started. It is an object that specifies the handler to run, such as an exec command, HTTP GET request, or TCP socket probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          Exec specifies the action to be taken as an exec-based lifecycle hook. It runs a command inside the container after it is started (PostStart). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              The command to run inside the container for this lifecycle hook. It is represented as a list of strings, where the first element is the command and subsequent elements are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          HTTPGet specifies the HTTP GET request to perform for the post-start lifecycle hook. It defines the endpoint to check and can include path, port, host, and headers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name to connect to for the HTTP GET request. Defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              A list of custom HTTP headers to include in the HTTP GET request sent by the postStart lifecycle hook. Each item defines a header name and value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The name of the HTTP header to set in the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header included in the HTTP GET request to the container's postStart lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              The HTTP path (URL path) to send the GET request to when the lifecycle hook is triggered. If not specified, the path defaults to '/'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The port number or name of the port on the container to send the HTTP GET request to. Must be an integer between 1 and 65535, or a named port defined for the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              Specifies the protocol (HTTP or HTTPS) to use for the HTTP GET request. Allowed values are HTTP and HTTPS; defaults to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The URL scheme to use for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          Sleep performs a sleep action, pausing the container for a specified number of seconds. It requires a `seconds` field indicating the duration of the pause. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              The number of seconds to sleep in the postStart lifecycle hook. This delays subsequent lifecycle actions by the specified duration. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          Configuration for a TCP socket probe executed as a post-start lifecycle hook for a container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The hostname or IP address to connect to. If not specified, defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port to perform the liveness check on. This can be a numeric port or a named port as defined in the container's ports list. The value must be a valid Kubernetes port number (1-65535) or an IANA_SVC_NAME. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="pre_stop" type="object">
                      PreStop is a lifecycle hook that is called immediately before a container is terminated. It allows the container to perform graceful shutdown tasks, such as draining connections or saving state, before the container is stopped. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="exec" type="object">
                          Exec specifies the action to execute a command inside the container when the preStop lifecycle hook is triggered. This allows you to run a command immediately before the container is terminated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="command" type="list(string)">
                              Command is the command line to execute inside the container for the pre-stop lifecycle hook. It is a list of strings; the first element is the command and subsequent elements are arguments. The command is executed directly without a shell, and the working directory is the container's root. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="http_get" type="object">
                          An HTTP GET request that kubelet sends to the container as part of a lifecycle hook, typically used in preStop to trigger a graceful shutdown or health check. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The hostname of the HTTP server to connect to for the pre-stop lifecycle hook's GET request. If not specified, the pod IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="http_headers" type="list(object)">
                              List of custom HTTP headers to include in the GET request. Each header is an object with `name` and `value` fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="name" type="string">
                                  The name of the HTTP header to set in the HTTP GET request for the pre-stop lifecycle hook of an init container in a DaemonSet. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="value" type="string">
                                  The value of the HTTP header field. This is paired with the header name to form a header entry in the HTTP GET request for the container's preStop lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              The URL path to access on the HTTP server for the pre-stop lifecycle hook's HTTP GET request. Defaults to '/' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The port number or name to send the HTTP GET request to. It can be an integer port number (1-65535) or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="protocol" type="string">
                              The protocol (HTTP or HTTPS) to use for the HTTP GET request, defaulting to HTTP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="scheme" type="string">
                              The scheme to use for the HTTP GET request. Defaults to HTTP. Valid values are HTTP and HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sleep" type="object">
                          The 'sleep' field specifies a SleepAction handler for the preStop lifecycle hook, which pauses the container for a specified duration in seconds before termination, allowing time for graceful shutdown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="seconds" type="number">
                              The number of seconds the container sleeps before it is terminated, used in the sleep action of the pre-stop lifecycle hook. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcp_socket" type="object">
                          tcp_socket is an object that specifies a TCP socket action to be performed during the pre-stop lifecycle hook. It accepts a required port and an optional host, defining the TCP endpoint to connect to before the container is terminated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="host" type="string">
                              The host name or IP address to connect to for the TCP socket action. If not specified, defaults to the pod IP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="port" type="string">
                              The TCP port number to connect to for the pre-stop lifecycle hook's TCP socket check. The value is a string containing a numeric port (e.g., "8080"). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="stop_signal" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="liveness_probe" type="object">
                  Defines a probe that checks whether the container is still alive. If the probe fails, the kubelet restarts the container. This configures the liveness probe for the init container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Specifies a command to run inside the container. The liveness probe is successful if the command exits with status code 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          Command is the command line to execute inside the container. The working directory for the command is root ('/') in the container's filesystem. The command is simply exec'd, not run inside a shell. Exit status of 0 indicates success; non-zero is unhealthy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures of the liveness probe before the container is restarted. The minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      grpc configures a gRPC-based liveness probe. It specifies the port to check and optionally the service name for the gRPC health check. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number to use for the gRPC health check. Must be an integer between 1 and 65535. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the gRPC service to query for the health check. If not specified, the default is an empty string, which typically refers to the server's default health check service. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies the HTTP GET request to perform as the liveness probe. It configures the HTTP request details such as path, port, and headers used to determine container health. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP GET request. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          List of custom HTTP headers to include in the probe request. Each header is defined by a name and value pair, allowing the probe to send authentication or other required headers to the target endpoint. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to send in the liveness probe request. This is the header field name (e.g., 'X-Custom-Header') as defined in the HTTPHeader schema. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header sent in the liveness probe request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          Path to access on the HTTP server for the liveness probe. Defaults to '/' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to send the HTTP GET request to. Accepts an integer port number or a named port (string). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol (scheme) used for the HTTP GET request, such as HTTP or HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          Scheme to use for connecting to the host. Defaults to HTTP. Allowed values are HTTP and HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before liveness probes are initiated. Defaults to 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      How often (in seconds) to perform the liveness probe. Defaults to 10 seconds; minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      The minimum number of consecutive successes required for the probe to be considered successful after having failed. For liveness probes, this must be 1. Minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      TCPSocket specifies the TCP socket probe action for the liveness check. It defines the port (and optionally host) that the kubelet will attempt to connect to in order to determine container health. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name or IP address to connect to for the TCP socket check. If not specified, the pod's IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port number to check, either as a numeric value or a name of a port defined in the container. For example, 8080 or 'http'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The number of seconds to wait for the container to terminate gracefully after sending the termination signal before forcefully killing it. Must be a non-negative integer; 0 means delete immediately. Defaults to 30 seconds if not set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the probe times out. Defaults to 1 second, and must be at least 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the init container. Must be a valid DNS_LABEL and unique within the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ports" type="list(object)">
                  List of ports exposed by the init container. Each port object can specify the container port number, protocol (TCP or UDP), optional name, and host port mapping. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="container_port" type="number">
                      The port number that the container exposes on its network interface. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_ip" type="string">
                      The host IP address to bind the container port to on the node. If specified, the container port is accessible only on this IP. Defaults to all interfaces (0.0.0.0) when not set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_port" type="number">
                      The port number on the host node to which the container port is bound. When set, the container can be accessed externally via the node's IP at this port; if unset, the container port is only reachable from within the cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the container port. Must be an IANA_SVC_NAME (up to 15 characters, alphanumeric and hyphens) and unique within the pod. This name can be referenced by services to target this port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="protocol" type="string">
                      The protocol for the port. Must be UDP, TCP, or SCTP. Defaults to TCP. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="readiness_probe" type="object">
                  Specifies how kubelet checks the container's readiness. For init containers, this probe is ignored and has no effect. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the action to take, which involves running a command inside the container. The command is an array of strings, with the first element being the command and the rest being arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          The command to run inside the container for the readiness check. The probe succeeds if the command exits with status 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      The number of consecutive failures allowed before the container is marked as unready. Defaults to 3; minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      gRPC specifies the parameters for a gRPC health check used by the readiness probe. It includes the port to check (required) and optionally the service name (defaults to the container's hostname). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number of the gRPC service. The probe connects to this port to perform the health check. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the gRPC service to place in the health check request. If empty, the default behavior defined by the gRPC health checking protocol is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      HTTPGet specifies the HTTP GET request to perform as part of the readiness probe. It defines the request path, port, and optional headers used to check container health. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          Host name or IP address to connect to for the HTTP GET probe. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          A list of custom HTTP headers to include in the GET request. Each header is an object with 'name' and 'value' fields. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header to set in the request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header to include in the readiness probe request, paired with the corresponding header name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The HTTP path to request, e.g., /healthz. Defaults to the root path "/" if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port number or named port on the container to which the HTTP GET request should be sent. It must be a valid port number (1-65535) or the name of a port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The protocol (HTTP or HTTPS) to use for the HTTP GET request in the readiness probe. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for connecting to the host. Defaults to HTTP. Allowed values are HTTP and HTTPS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container has started before the readiness probe is initiated. Minimum value is 0 (default). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      How often (in seconds) to perform the readiness probe. Minimum value is 1; defaults to 10 if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      The minimum number of consecutive successful probe results required for the probe to be considered successful after a failure. For readiness probes, this defaults to 1 and must be at least 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Configures a TCP socket probe for the readiness check, which verifies container health by attempting to open a TCP connection to a specified port (and optionally a host). The object defines the port and host for the TCP connection. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the TCP socket probe. Defaults to the pod IP if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to check, specified as a number or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The amount of time in seconds to wait for the container to terminate gracefully after receiving a termination signal, before forcing shutdown. If unset, Kubernetes uses a default of 30 seconds. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the probe times out. Defaults to 1 second. Must be at least 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resize_policy" type="list(object)">
                  A list of resource resize policies for the container. Each policy specifies a resource name (e.g., cpu, memory) and a restart policy (NotRequired or RestartContainer) that determines whether the container must be restarted when the resource size changes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="resource_name" type="string">
                      The name of the resource (e.g., 'cpu' or 'memory') to which this resize policy applies. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="restart_policy" type="string">
                      The restart policy for the container when its resources are resized. Valid values are 'NotRequired' (resize can occur without restarting the container) and 'RestartContainer' (the container must be restarted to apply the resize). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="resources" type="object">
                  Compute resources required by the init container, including requests and limits for CPU and memory. The requests field specifies the minimum resources required, and the limits field specifies the maximum resources allowed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="claims" type="list(object)">
                      Claims lists the names of resources, defined in spec.resourceClaims, that are used by this container. This is an alpha feature and may change in the future. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the ResourceClaim that this container requires. The claim must be created in the same namespace as the pod before it can be used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="request" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="limits" type="map(string)">
                      The maximum amount of compute resources (such as cpu and memory) that the init container can use. The map keys are resource names (e.g., 'cpu', 'memory') and the values are Kubernetes quantity strings (e.g., '100m', '1Gi'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="requests" type="map(string)">
                      A map of resource names (e.g., cpu, memory) to quantities specifying the minimum amount of compute resources required for the init container. If requests are omitted, they default to the container's limits (if set) or an implementation-defined value, and requests cannot exceed limits. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="restart_policy" type="string">
                  Optional; if omitted, computed by Kubernetes.
                </ResponseField>
                <ResponseField name="restart_policy_rules" type="list(object)">
                  Optional; if omitted, computed by Kubernetes.
                  <Expandable title="properties">
                    <ResponseField name="action" type="string">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="exit_codes" type="object">
                      Optional; if omitted, computed by Kubernetes.
                      <Expandable title="properties">
                        <ResponseField name="operator" type="string">
                          Specifies the operator used to match the container's exit code against the values in the exit_codes list for the restart policy rule. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="values" type="list(number)">
                          The list of container exit codes that this restart policy rule matches. Each value is a numeric exit code. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="security_context" type="object">
                  SecurityContext defines the security options the container should be run with, such as user IDs, SELinux options, capabilities, and read-only root filesystem. If set, the fields of securityContext override the equivalent fields of the PodSecurityContext for this init container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="allow_privilege_escalation" type="bool">
                      Controls whether a process can gain more privileges than its parent process. When set to false, the container process is run with the 'no_new_privs' flag, preventing privilege escalation such as setuid or file capabilities. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="app_armor_profile" type="object">
                      AppArmorProfile defines the AppArmor profile to be applied to the container. It includes a type (RuntimeDefault, Localhost, or Unconfined) and, when type is Localhost, a localhostProfile field specifying the profile name on the node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          Name of a profile on the node to use. Only relevant when the AppArmor profile type is 'Localhost'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The type of AppArmor profile to apply. Valid values are `RuntimeDefault`, `Localhost`, and `Unconfined`. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="capabilities" type="object">
                      Capabilities to add or drop for the container, given as an object with `add` and `drop` lists of Linux capabilities. This allows fine-grained privilege control for init containers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="add" type="list(string)">
                          Adds the specified Linux capabilities to the container's security context. Each entry is a capability name, e.g., 'NET_ADMIN' or 'SYS_TIME'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="drop" type="list(string)">
                          Specifies a list of capabilities to drop from the container's default capability set. Each entry is a Linux capability name (e.g., 'NET_ADMIN'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="privileged" type="bool">
                      Indicates whether the init container runs in privileged mode. When true, the container gains elevated permissions, including access to host devices and capabilities, similar to running as root on the host. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="proc_mount" type="string">
                      Specifies the proc mount type for the container, controlling how the /proc filesystem is exposed within the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only_root_filesystem" type="bool">
                      Indicates whether the container's root filesystem is mounted as read-only. When true, the container cannot write to its root filesystem, enhancing security for init containers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_group" type="number">
                      The GID (group ID) to run the entrypoint of the container process. If not set, the default is the group ID specified in the container image. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_non_root" type="bool">
                      Indicates whether the container must run as a non-root user. If true, the kubelet will validate the image at runtime to ensure that it does not run as UID 0 (root) and will fail to start the container if it does. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="run_as_user" type="number">
                      The UID to run the entrypoint of the init container process. Defaults to the user specified in the image metadata if not set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="se_linux_options" type="object">
                      SELinux options for the init container. This object specifies the SELinux label (user, role, type, level) that the container's processes will run with. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="level" type="string">
                          The SELinux level (MLS/MCS) to apply to the container, such as 's0' or 's0:c1,c2'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="role" type="string">
                          The SELinux role label that applies to the container. This corresponds to the `role` field in Kubernetes' SELinuxOptions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The SELinux type label applied to the container, e.g., svirt_apache_t. This field is part of the SELinux security context options for the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="user" type="string">
                          The SELinux user label assigned to the container, which is the user component of the SELinux context (e.g., 'system_u'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="seccomp_profile" type="object">
                      seccomp_profile defines the seccomp profile applied to the init container's security context, specifying how system calls are filtered using supported profile types (e.g., RuntimeDefault, Localhost). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="localhost_profile" type="string">
                          The path to the localhost seccomp profile. Used when the seccomp profile type is set to 'Localhost'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          The type of seccomp profile to use. Valid values are: RuntimeDefault, Localhost, and Unconfined. When set to Localhost, the localhostProfile field must also be specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="windows_options" type="object">
                      WindowsSecurityContextOptions contain Windows-specific options and credentials. This includes settings such as GMSA credentials and runAsUserName for Windows containers. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="gmsa_credential_spec" type="string">
                          The path to the GMSA credential spec file. This is used to configure Group Managed Service Accounts (GMSA) for Windows containers in the init container's security context. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="gmsa_credential_spec_name" type="string">
                          The name of the GMSA credential spec to use for the Windows container. This field is part of the Windows security context options for the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="host_process" type="bool">
                          Indicates whether the container runs as a HostProcess container. When true, the container gains access to the Windows host namespace and resources. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_user_name" type="string">
                          The Windows user name (in the form 'user' or 'domain\user') to run the container's entrypoint process as. If not specified, the default user from the container image metadata is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="startup_probe" type="object">
                  startup_probe is a Kubernetes probe that indicates whether the application in the container is started. If configured, it disables liveness and readiness checks until the startup probe succeeds, ensuring those probes do not interfere with application startup. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="exec" type="object">
                      Exec specifies the action to take, consisting of a command to be executed inside the container. The probe succeeds if the command exits with status code 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="command" type="list(string)">
                          Command is the command line to execute inside the container. It is specified as a list of strings; the first element is the command, and the remaining elements are arguments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="failure_threshold" type="number">
                      failureThreshold is the minimum consecutive failures of the startup probe before it is considered failed. The value must be an integer greater than or equal to 1; the default is 3. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="grpc" type="object">
                      Defines a gRPC action for the startup probe, specifying the port and optional service name to check container health via gRPC. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mode" type="string">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                        <ResponseField name="port" type="number">
                          The port number to connect to for the gRPC health check. Must be a valid TCP port (1-65535). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="service" type="string">
                          The name of the service to place in the gRPC HealthCheckRequest. If not specified, the default behavior is defined by the gRPC server. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="http_get" type="object">
                      Specifies an HTTP GET request to perform against the container to check its startup status. The probe succeeds if the response status code is between 200 and 399. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name to connect to for the HTTP GET request. Defaults to the pod IP if not set. Consider setting the Host header via httpHeaders instead for most use cases. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="http_headers" type="list(object)">
                          Specifies custom HTTP headers to include in the HTTP request sent by the startup probe. Each entry defines a header name and value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the HTTP header in a list of headers to send with the probe request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of the HTTP header used in a startup probe's HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The path to access on the HTTP server for the startup probe's HTTP GET request. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The port number or name of the port to access on the container for the HTTP GET request. Must be a valid integer port number or a named port (as defined in the container's ports). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="protocol" type="string">
                          The network protocol to use for the HTTP request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="scheme" type="string">
                          The scheme to use for the HTTP GET request. Allowed values are 'HTTP' and 'HTTPS'. Defaults to 'HTTP'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="initial_delay_seconds" type="number">
                      Number of seconds after the container starts before the startup probe is initiated. Minimum value is 0 and defaults to 0. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="period_seconds" type="number">
                      The interval in seconds between successive startup probe checks. Determines how often the kubelet performs the probe on the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="success_threshold" type="number">
                      The minimum consecutive successes required for the startup probe to be considered successful after having failed. Defaults to 1; increasing it requires multiple consecutive successes before the container is marked healthy. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tcp_socket" type="object">
                      Specifies the TCP socket probe action for the startup probe. The kubelet attempts to open a TCP connection to the specified port to determine container health. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="host" type="string">
                          The host name or IP address to connect to for the TCP probe. If omitted, the pod IP address is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="port" type="string">
                          The TCP port to connect to for the socket probe. Accepts a numeric port (1-65535) or a named port defined in the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="termination_grace_period_seconds" type="number">
                      The number of seconds to wait for the container to terminate gracefully after a startup probe failure, before it is forcibly killed. Overrides the pod-level terminationGracePeriodSeconds for this container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="timeout_seconds" type="number">
                      Number of seconds after which the probe times out. Defaults to 1 second; minimum value is 1. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="stdin" type="bool">
                  Indicates whether the container runtime should allocate a stdin stream for the container. When true, the container's standard input is kept open and available. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stdin_once" type="bool">
                  If true, the container's standard input is closed after the first client attaches, preventing subsequent attaches. This requires 'stdin' to be true. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_path" type="string">
                  Path where the container's termination message is written. When the container exits, Kubernetes reads this file to get the termination message. Defaults to /dev/termination-log. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="termination_message_policy" type="string">
                  Determines how the container's termination message is captured. Valid values are 'File' (default), which reads from the file specified by terminationMessagePath, and 'Log', which reads from the container's log output. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="tty" type="bool">
                  Whether this container should allocate a TTY for itself. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volume_devices" type="list(object)">
                  VolumeDevices is the list of block devices to be used by the container. It is used to expose a raw block device to the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="device_path" type="string">
                      Path inside the container where the block device will be mounted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      Name of the volume device, which must match the name of a volume defined in the pod's volumes list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="volume_mounts" type="list(object)">
                  A list of volumes to mount into the init container's filesystem. Each item specifies the volume name and the mount path inside the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="bind_mount_options" type="list(string)">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="mount_path" type="string">
                      Path within the container at which the volume should be mounted. Must be an absolute path and must not contain ':'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mount_propagation" type="string">
                      Determines how mount propagation works between the host and the container, and between containers sharing the same volume. Allowed values: None (no propagation), HostToContainer (mounts created on the host are visible in the container), and Bidirectional (mounts propagate both ways, requiring privileged containers). Defaults to None. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the volume to mount. Must match the name of a volume defined in the pod's spec.volumes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Whether the volume is mounted as read-only. If set to true, the container can only read from the volume, not write to it. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="recursive_read_only" type="string">
                      Indicates whether the read-only mount option is applied recursively to all nested mounts under this volume mount. Must be one of 'Enabled', 'Disabled', or 'IfPossible'. Only supported for volume types that enable recursive mounts, such as CSI volumes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path" type="string">
                      Specifies a sub-path inside the referenced volume to mount instead of the volume's root. This allows mounting only a subdirectory or file from the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sub_path_expr" type="string">
                      Specifies an alternative to subPath that supports environment variable expansion using $(VAR_NAME) syntax to define the path within the volume to mount. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="working_dir" type="string">
                  The working directory for the init container. If not specified, the container runtime default is used (usually the image's specified working directory). Overrides the image's working directory when set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="node_name" type="string">
              NodeName indicates in which node this pod is scheduled. If empty, this pod is a candidate for scheduling by the scheduler defined in schedulerName. Once this field is set, the kubelet for this node becomes responsible for the lifecycle of this pod. This field should not be used to express a desire for the pod to be scheduled on a specific node. https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodename Optional.
            </ResponseField>
            <ResponseField name="node_selector" type="map(string)">
              NodeSelector is a selector which must be true for the pod to fit on a node. Selector which must match a node's labels for the pod to be scheduled on that node. More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/ Optional.
            </ResponseField>
            <ResponseField name="os" type="object">
              PodOS defines the OS parameters of a pod. Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string" required>
                  Name is the name of the operating system. The currently supported values are linux and windows. Additional value may be defined in future and can be one of: https://github.com/opencontainers/runtime-spec/blob/master/config.md#platform-specific-configuration Clients should expect to handle additional values and treat unrecognized values in this field as os: null Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="overhead" type="map(string)">
              Overhead represents the resource overhead associated with running a pod for a given RuntimeClass. This field will be autopopulated at admission time by the RuntimeClass admission controller. If the RuntimeClass admission controller is enabled, overhead must not be set in Pod create requests. The RuntimeClass admission controller will reject Pod create requests which have the overhead already set. If RuntimeClass is configured and selected in the PodSpec, Overhead will be set to the value defined in the corresponding RuntimeClass, otherwise it will remain unset and treated as zero. More info: https://git.k8s.io/enhancements/keps/sig-node/688-pod-overhead/README.md Optional.
            </ResponseField>
            <ResponseField name="preemption_policy" type="string">
              PreemptionPolicy is the Policy for preempting pods with lower priority. One of Never, PreemptLowerPriority. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. Defaults to PreemptLowerPriority if unset. Optional.
            </ResponseField>
            <ResponseField name="priority" type="number">
              The priority value. Various system components use this field to find the priority of the pod. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. The higher the value, the higher the priority. Optional.
            </ResponseField>
            <ResponseField name="priority_class_name" type="string">
              If specified, indicates the pod's priority. "system-node-critical" and "system-cluster-critical" are two special keywords which indicate the highest priorities with the former being the highest priority. Any other name must be defined by creating a PriorityClass object with that name. If not specified, the pod priority will be default or zero if there is no default. Optional.
            </ResponseField>
            <ResponseField name="readiness_gates" type="list(object)">
              If specified, all readiness gates will be evaluated for pod readiness. A pod is ready when all its containers are ready AND all conditions specified in the readiness gates have status equal to "True" More info: https://git.k8s.io/enhancements/keps/sig-network/580-pod-readiness-gates Optional.
              <Expandable title="properties">
                <ResponseField name="condition_type" type="string">
                  Specifies the condition type that must be satisfied for the pod to be considered ready. This corresponds to a condition name in the pod's status.conditions, allowing custom readiness conditions beyond the default pod status. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="resource_claims" type="list(object)">
              ResourceClaims defines which ResourceClaims must be allocated and reserved before the Pod is allowed to start. The resources will be made available to those containers which consume them by name. This is a stable field but requires that the DynamicResourceAllocation feature gate is enabled. This field is immutable. Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  Name of the resource claim. Must be unique within the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="resource_claim_name" type="string">
                  The name of the ResourceClaim object in the same namespace that provides the resource. This references a specific ResourceClaim, and only one of resourceClaimName and resourceClaimTemplateName can be set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="resource_claim_template_name" type="string">
                  The name of a ResourceClaimTemplate object in the same namespace as the pod. The resource claim is generated from this template, defining the requested resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="resources" type="object">
              ResourceRequirements describes the compute resource requirements. Optional.
              <Expandable title="properties">
                <ResponseField name="claims" type="list(object)">
                  Claims lists the names of resources, defined in spec.resourceClaims, that are used by this container. This field depends on the DynamicResourceAllocation feature gate. This field is immutable. It can only be set for containers. Optional.
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Name of the ResourceClaim object that provides the resources. Must be in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="request" type="string">
                      The name of the ResourceClaim object that this claim refers to. The ResourceClaim must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="limits" type="map(string)">
                  Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/ Optional.
                </ResponseField>
                <ResponseField name="requests" type="map(string)">
                  Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/ Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="restart_policy" type="string">
              Restart policy for all containers within the pod. One of Always, OnFailure, Never. In some contexts, only a subset of those values may be permitted. Default to Always. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy Optional.
            </ResponseField>
            <ResponseField name="runtime_class_name" type="string">
              RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used to run this pod. If no RuntimeClass resource matches the named class, the pod will not be run. If unset or empty, the "legacy" RuntimeClass will be used, which is an implicit class with an empty definition that uses the default runtime handler. More info: https://git.k8s.io/enhancements/keps/sig-node/585-runtime-class Optional.
            </ResponseField>
            <ResponseField name="scheduler_name" type="string">
              If specified, the pod will be dispatched by specified scheduler. If not specified, the pod will be dispatched by default scheduler. Optional.
            </ResponseField>
            <ResponseField name="scheduling_gates" type="list(object)">
              SchedulingGates is an opaque list of values that if specified will block scheduling the pod. If schedulingGates is not empty, the pod will stay in the SchedulingGated state and the scheduler will not attempt to schedule the pod. SchedulingGates can only be set at pod creation time, and be removed only afterwards. Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name of the scheduling gate. Pods are not scheduled until all gates with these names are removed from the pod's spec. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="scheduling_group" type="object">
              PodSchedulingGroup identifies the runtime scheduling group instance that a Pod belongs to. The scheduler uses this information to apply workload-aware scheduling semantics. Exactly one field must be specified. Optional.
              <Expandable title="properties">
                <ResponseField name="pod_group_name" type="string">
                  PodGroupName specifies the name of the standalone PodGroup object that represents the runtime instance of this group. Must be a DNS subdomain. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="security_context" type="object">
              PodSecurityContext holds pod-level security attributes and common container settings. Some fields are also present in container.securityContext. Field values of container.securityContext take precedence over field values of PodSecurityContext. Optional.
              <Expandable title="properties">
                <ResponseField name="app_armor_profile" type="object">
                  AppArmorProfile defines a pod or container's AppArmor settings. Optional.
                  <Expandable title="properties">
                    <ResponseField name="localhost_profile" type="string">
                      localhostProfile indicates a profile loaded on the node that should be used. The profile must be preconfigured on the node to work. Must match the loaded name of the profile. Must be set if and only if type is "Localhost". Optional.
                    </ResponseField>
                    <ResponseField name="type" type="string" required>
                      type indicates which kind of AppArmor profile will be applied. Valid options are: Localhost - a profile pre-loaded on the node. RuntimeDefault - the container runtime's default profile. Unconfined - no AppArmor enforcement. Required.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="fs_group" type="number">
                  A special supplemental group that applies to all containers in a pod. Some volume types allow the Kubelet to change the ownership of that volume to be owned by the pod: 1. The owning GID will be the FSGroup 2. The setgid bit is set (new files created in the volume will be owned by FSGroup) 3. The permission bits are OR'd with rw-rw---- If unset, the Kubelet will not modify the ownership and permissions of any volume. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="fs_group_change_policy" type="string">
                  fsGroupChangePolicy defines behavior of changing ownership and permission of the volume before being exposed inside Pod. This field will only apply to volume types which support fsGroup based ownership(and permissions). It will have no effect on ephemeral volume types such as: secret, configmaps and emptydir. Valid values are "OnRootMismatch" and "Always". If not specified, "Always" is used. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="run_as_group" type="number">
                  The GID to run the entrypoint of the container process. Uses runtime default if unset. May also be set in SecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence for that container. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="run_as_non_root" type="bool">
                  Indicates that the container must run as a non-root user. If true, the Kubelet will validate the image at runtime to ensure that it does not run as UID 0 (root) and fail to start the container if it does. If unset or false, no such validation will be performed. May also be set in SecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Optional.
                </ResponseField>
                <ResponseField name="run_as_user" type="number">
                  The UID to run the entrypoint of the container process. Defaults to user specified in image metadata if unspecified. May also be set in SecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence for that container. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="se_linux_change_policy" type="string">
                  seLinuxChangePolicy defines how the container's SELinux label is applied to all volumes used by the Pod. It has no effect on nodes that do not support SELinux or to volumes does not support SELinux. Valid values are "MountOption" and "Recursive". "Recursive" means relabeling of all files on all Pod volumes by the container runtime. This may be slow for large volumes, but allows mixing privileged and unprivileged Pods sharing the same volume on the same node. "MountOption" mounts all eligible Pod volumes with `-o context` mount option. This requires all Pods that share the same volume to use the same SELinux label. It is not possible to share the same volume among privileged and unprivileged Pods. Eligible volumes are in-tree FibreChannel and iSCSI volumes, and all CSI volumes whose CSI driver announces SELinux support by setting spec.seLinuxMount: true in their CSIDriver instance. Other volumes are always re-labelled recursively. If not specified, "MountOption" is used. This field affects only Pods that have SELinux label set, either in PodSecurityContext or in SecurityContext of all containers. All Pods that use the same volume should use the same seLinuxChangePolicy, otherwise some pods can get stuck in ContainerCreating state. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="se_linux_options" type="object">
                  SELinuxOptions are the labels to be applied to the container Optional.
                  <Expandable title="properties">
                    <ResponseField name="level" type="string">
                      Level is SELinux level label that applies to the container. Optional.
                    </ResponseField>
                    <ResponseField name="role" type="string">
                      Role is a SELinux role label that applies to the container. Optional.
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      Type is a SELinux type label that applies to the container. Optional.
                    </ResponseField>
                    <ResponseField name="user" type="string">
                      User is a SELinux user label that applies to the container. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="seccomp_profile" type="object">
                  SeccompProfile defines a pod/container's seccomp profile settings. Only one profile source may be set. Optional.
                  <Expandable title="properties">
                    <ResponseField name="localhost_profile" type="string">
                      localhostProfile indicates a profile defined in a file on the node should be used. The profile must be preconfigured on the node to work. Must be a descending path, relative to the kubelet's configured seccomp profile location. Must be set if type is "Localhost". Must NOT be set for any other type. Optional.
                    </ResponseField>
                    <ResponseField name="type" type="string" required>
                      type indicates which kind of seccomp profile will be applied. Valid options are: Localhost - a profile defined in a file on the node should be used. RuntimeDefault - the container runtime default profile should be used. Unconfined - no profile should be applied. Required.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="supplemental_groups" type="list(number)">
                  A list of groups applied to the first process run in each container, in addition to the container's primary GID and fsGroup (if specified). If the SupplementalGroupsPolicy feature is enabled, the supplementalGroupsPolicy field determines whether these are in addition to or instead of any group memberships defined in the container image. If unspecified, no additional groups are added, though group memberships defined in the container image may still be used, depending on the supplementalGroupsPolicy field. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="supplemental_groups_policy" type="string">
                  Defines how supplemental groups of the first container processes are calculated. Valid values are "Merge" and "Strict". If not specified, "Merge" is used. (Alpha) Using the field requires the SupplementalGroupsPolicy feature gate to be enabled and the container runtime must implement support for this feature. Note that this field cannot be set when spec.os.name is windows. Optional.
                </ResponseField>
                <ResponseField name="sysctls" type="list(object)">
                  Sysctls hold a list of namespaced sysctls used for the pod. Pods with unsupported sysctls (by the container runtime) might fail to launch. Note that this field cannot be set when spec.os.name is windows. Optional.
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the sysctl parameter to set, for example `kernel.shm_rmid_forced`. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value to assign to the sysctl setting, expressed as a string. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="windows_options" type="object">
                  WindowsSecurityContextOptions contain Windows-specific options and credentials. Optional.
                  <Expandable title="properties">
                    <ResponseField name="gmsa_credential_spec" type="string">
                      GMSACredentialSpec is where the GMSA admission webhook (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the GMSA credential spec named by the GMSACredentialSpecName field. Optional.
                    </ResponseField>
                    <ResponseField name="gmsa_credential_spec_name" type="string">
                      GMSACredentialSpecName is the name of the GMSA credential spec to use. Optional.
                    </ResponseField>
                    <ResponseField name="host_process" type="bool">
                      HostProcess determines if a container should be run as a 'Host Process' container. All of a Pod's containers must have the same effective HostProcess value (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers). In addition, if HostProcess is true then HostNetwork must also be set to true. Optional.
                    </ResponseField>
                    <ResponseField name="run_as_user_name" type="string">
                      The UserName in Windows to run the entrypoint of the container process. Defaults to the user specified in image metadata if unspecified. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="service_account" type="string">
              DeprecatedServiceAccount is a deprecated alias for ServiceAccountName. Deprecated: Use serviceAccountName instead. Optional.
            </ResponseField>
            <ResponseField name="service_account_name" type="string">
              ServiceAccountName is the name of the ServiceAccount to use to run this pod. More info: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/ Optional.
            </ResponseField>
            <ResponseField name="set_hostname_as_fqdn" type="bool">
              If true the pod's hostname will be configured as the pod's FQDN, rather than the leaf name (the default). In Linux containers, this means setting the FQDN in the hostname field of the kernel (the nodename field of struct utsname). In Windows containers, this means setting the registry value of hostname for the registry key HKEY_LOCAL_MACHINE\\SYSTEM\\CurrentControlSet\\Services\\Tcpip\\Parameters to FQDN. If a pod does not have FQDN, this has no effect. Default to false. Optional.
            </ResponseField>
            <ResponseField name="share_process_namespace" type="bool">
              Share a single process namespace between all of the containers in a pod. When this is set containers will be able to view and signal processes from other containers in the same pod, and the first process in each container will not be assigned PID 1. HostPID and ShareProcessNamespace cannot both be set. Optional: Default to false. Optional.
            </ResponseField>
            <ResponseField name="subdomain" type="string">
              If specified, the fully qualified Pod hostname will be "&lt;hostname&gt;.&lt;subdomain&gt;.&lt;pod namespace&gt;.svc.&lt;cluster domain&gt;". If not specified, the pod will not have a domainname at all. Optional.
            </ResponseField>
            <ResponseField name="termination_grace_period_seconds" type="number">
              Optional duration in seconds the pod needs to terminate gracefully. May be decreased in delete request. Value must be non-negative integer. The value zero indicates stop immediately via the kill signal (no opportunity to shut down). If this value is nil, the default grace period will be used instead. The grace period is the duration in seconds after the processes running in the pod are sent a termination signal and the time when the processes are forcibly halted with a kill signal. Set this value longer than the expected cleanup time for your process. Defaults to 30 seconds. Optional.
            </ResponseField>
            <ResponseField name="tolerations" type="list(object)">
              If specified, the pod's tolerations. Optional.
              <Expandable title="properties">
                <ResponseField name="effect" type="string">
                  The taint effect to tolerate. Valid values are 'NoSchedule', 'PreferNoSchedule', and 'NoExecute'. If empty, matches all effects. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key" type="string">
                  The taint key that the toleration applies to. An empty key matches all taint keys when the operator is 'Exists'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  The operator represents the relationship between the key and value. Valid values are 'Exists' and 'Equal'. When set to 'Exists', the value should be empty. When set to 'Equal', the value must match the taint value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="toleration_seconds" type="number">
                  The number of seconds the pod will tolerate the taint before being evicted, typically used with the NoExecute effect. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The taint value the toleration matches. This is used only when the operator is "Equal"; for "Exists" the value must be empty. In the context of a DaemonSet pod spec, it defines which taint value this toleration can tolerate. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="topology_spread_constraints" type="list(object)">
              TopologySpreadConstraints describes how a group of pods ought to spread across topology domains. Scheduler will schedule pods in a way which abides by the constraints. All topologySpreadConstraints are ANDed. Optional.
              <Expandable title="properties">
                <ResponseField name="label_selector" type="object">
                  A label selector to identify the set of pods that this topology spread constraint applies to. If omitted, the constraint applies to all pods in the namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_expressions" type="list(object)">
                      A list of label selector requirements. Each requirement specifies a key, an operator (e.g., In, NotIn, Exists, DoesNotExist), and optionally a list of values. These expressions are combined with matchLabels to define the label selector for the topology spread constraint. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="key" type="string">
                          The label key that the match expression applies to. The expression matches pods with this label key based on the operator and values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="operator" type="string">
                          The operator specifies the relationship between the label key and the set of values. Valid values are In, NotIn, Exists, and DoesNotExist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="values" type="list(string)">
                          List of string values that the label key must match when the operator is In or NotIn. Must be empty for Exists and DoesNotExist operators. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_labels" type="map(string)">
                      A map of label keys to values that the topology spread constraint applies to. Only pods with all specified labels are considered when spreading across the topology domain. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="match_label_keys" type="list(string)">
                  MatchLabelKeys is a set of pod label keys that identifies the pods over which spreading is calculated. If non-empty, the values of these labels from the pod owning this constraint are used to filter the pods matched by the selector; each key must be unique and exist in the pod's labels. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_skew" type="number">
                  The maximum difference in the number of matching pods between any two topology domains for the workload. Must be greater than zero. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min_domains" type="number">
                  Specifies the minimum number of eligible topology domains that must be used for pod spreading. If the number of eligible domains is less than this value, the global minimum skew is treated as zero when calculating spread. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="node_affinity_policy" type="string">
                  Specifies how to treat node affinity when calculating pod topology spread skew. Allowed values are "Honor" and "Ignore". When "Honor", nodes with matching node affinity are included; when "Ignore", node affinity is not considered. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="node_taints_policy" type="string">
                  Indicates how to handle nodes with taints when calculating pod spread. Accepts 'Honor' to exclude tainted nodes that do not tolerate the pod's tolerations, or 'Ignore' to include all nodes regardless of taints. Defaults to 'Honor'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="topology_key" type="string">
                  The key of node labels. Nodes that have a label with this key and identical values are considered to be in the same topology domain. This key is required for each topology spread constraint and defines how pods are spread across different domains, such as zones or hosts. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="when_unsatisfiable" type="string">
                  Specifies how to handle pods when the topology spread constraint is not satisfied. Allowed values are 'DoNotSchedule' (default), which prevents scheduling, and 'ScheduleAnyway', which schedules the pod regardless. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="volumes" type="list(object)">
              List of volumes that can be mounted by containers belonging to the pod. More info: https://kubernetes.io/docs/concepts/storage/volumes Optional.
              <Expandable title="properties">
                <ResponseField name="aws_elastic_block_store" type="object">
                  Represents an AWS Elastic Block Store volume source that can be mounted into a container within a pod. This field configures the volume to use an AWS EBS volume, allowing persistent storage for the daemon set's pods. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to be used for the AWS EBS volume, such as ext4, xfs, or ntfs. Defaults to ext4 if unspecified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="partition" type="number">
                      The partition number of the AWS EBS volume to mount. If specified, only that partition is mounted; otherwise, the entire volume is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Indicates whether the AWS Elastic Block Store volume is mounted as read-only. Defaults to false; set to true to prevent writes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_id" type="string">
                      The unique ID of the AWS EBS volume to mount. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="azure_disk" type="object">
                  Configures an Azure Disk volume to be mounted into the pod. This is a volume source that references an Azure Data Disk and contains settings for the disk mount. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="caching_mode" type="string">
                      Specifies the caching mode for the Azure disk volume. Allowed values are None, ReadOnly, and ReadWrite; defaults to None if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="disk_name" type="string">
                      The name of the Azure data disk to attach to the pod. Must be the name of an existing Azure disk in the same location as the cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="disk_uri" type="string">
                      The URI of the Azure blob storage disk to mount. Must be specified when using a blob-based disk, in the format https://&lt;account&gt;.blob.core.windows.net/&lt;container&gt;/&lt;vhd-name&gt;.vhd. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount (e.g., ext4, xfs). Defaults to ext4. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="kind" type="string">
                      The kind of Azure Data Disk. Allowed values are 'Shared', 'Dedicated', and 'Managed'. Defaults to 'Shared'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Mounts the Azure disk as read-only when set to true. Defaults to false (read/write). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="azure_file" type="object">
                  Represents an Azure File volume source, which mounts an Azure File share into the pod. This configuration typically requires a secret name with Azure storage credentials and a share name, and optionally supports read-only mounting. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="read_only" type="bool">
                      Indicates whether the Azure file share should be mounted read-only. If set to true, the volume is mounted read-only; otherwise it is mounted read-write. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_name" type="string">
                      The name of the secret that contains the Azure Storage account name and key, used to authenticate to the Azure file share. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="share_name" type="string">
                      The name of the Azure File share that should be mounted. This corresponds to the shareName field in the Kubernetes azureFile volume source. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cephfs" type="object">
                  Defines a CephFS volume source, used to mount a Ceph Filesystem into the pod. Contains configuration such as monitors, path, user, and secret references. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="monitors" type="list(string)">
                      List of Ceph monitor addresses (hostnames or IPs) for the CephFS volume. Required to connect to the Ceph cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path" type="string">
                      Path within the CephFS filesystem to mount into the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the CephFS volume is mounted as read-only. If true, the volume is read-only; if false (or omitted), it is read-write. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_file" type="string">
                      Path to the Ceph keyring file on the host, used for authentication with the Ceph cluster. Either this or secret_ref is required to provide credentials. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      secretRef is an optional reference to a Kubernetes Secret that contains the Ceph credentials (keyring) for accessing the CephFS filesystem. The secret must be in the same namespace as the pod; if not specified, Ceph uses the default keyring. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the Kubernetes secret that provides the CephFS credentials. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="user" type="string">
                      The CephFS user to use when mounting the volume. If unset, defaults to 'admin'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cinder" type="object">
                  Represents a Cinder volume in OpenStack. Used to attach a Cinder volume to the pod for persistent storage. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount for the Cinder volume. Must be a filesystem type supported by the host operating system, e.g., ext4, xfs, ntfs. If not specified, ext4 is typically inferred. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the Cinder volume is mounted as read-only. Defaults to false (read/write). If true, forces the read-only setting in volume mounts. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      secret_ref is a reference to a Kubernetes secret that contains the authentication credentials (username and password) for the Cinder volume. It is an object with a 'name' field specifying the name of the secret in the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the Kubernetes Secret object that contains authentication credentials for the Cinder volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="volume_id" type="string">
                      The unique ID of the Cinder volume to be mounted. This is a required field for Cinder volume sources. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="config_map" type="object">
                  config_map configures a volume sourced from a Kubernetes ConfigMap. It specifies the name of the ConfigMap to reference and optionally defines which items (keys) to project as files, including their paths and permissions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="default_mode" type="number">
                      Sets the default file permission mode (in octal) for all files in the ConfigMap volume. When unspecified, the default is 0644. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="default_user" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="items" type="list(object)">
                      Maps keys from the ConfigMap to specific file paths and optional mode. If unspecified, all keys are projected as files. If specified, only the listed keys are projected, with custom paths and permissions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="key" type="string">
                          The key to select from the ConfigMap's data. The value corresponding to this key will be exposed at the defined path within the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mode" type="number">
                          Mode bits used to set permissions on this file, expressed as an octal integer (e.g., 0644). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The relative path of the file within the volume where the value from the corresponding config map key is written. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="user" type="number">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      Name of the ConfigMap to reference for this volume. The volume will be populated with data from the specified ConfigMap. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="optional" type="bool">
                      Optional specifies whether the ConfigMap or its keys must be defined. If true, the volume will not fail if the ConfigMap is missing or if any of the specified keys are absent. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="csi" type="object">
                  Represents a volume that is provisioned and mounted using a Container Storage Interface (CSI) driver. This configuration includes the driver's name, the volume handle, filesystem type, read-only setting, and any driver-specific volume attributes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="driver" type="string">
                      The name of the CSI driver that manages the volume. This must match the name of a CSI driver installed on the cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount for the CSI volume (e.g., ext4, xfs). If omitted, the CSI driver's default filesystem type is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="node_publish_secret_ref" type="object">
                      A reference to a Secret containing credentials or configuration required by the CSI driver for the node publish (or unpublish) operation. It is optional and typically specified when the driver requires additional secrets at node stage/publish time. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the secret. This secret is referenced for the CSI node publish operation. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Indicates if the CSI volume is mounted as read-only. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_attributes" type="map(string)">
                      volumeAttributes are arbitrary attributes of the volume that are passed to the external CSI volume driver. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="downward_api" type="object">
                  Configures a Downward API volume that exposes pod metadata (such as labels, annotations, and other fields) to the container as files. This allows containers to read information about themselves without using the Kubernetes API. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="default_mode" type="number">
                      The default permissions mode (in octal) for files created by the downward API volume. Must be a value between 0 and 0777; defaults to 0644. This setting does not affect directories. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="default_user" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="items" type="list(object)">
                      items specifies the list of downward API volume files. Each item defines a file path and a field reference (fieldRef or resourceFieldRef) to expose. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="field_ref" type="object">
                          Selects a field of the pod to expose via the downward API volume. Supported fields include metadata.name, metadata.namespace, metadata.labels, metadata.annotations, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, and status.podIPs. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="api_version" type="string">
                              Version of the schema the field_path is written in terms of. Typically the Kubernetes API version, e.g., 'v1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="field_path" type="string">
                              The path to the pod field whose value is exposed to the container via the downward API volume, e.g., metadata.name or metadata.namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="mode" type="number">
                          The file permission mode, represented as an octal number (e.g., 0644). If not specified, the default mode is 0644. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          Path is the file path within the downward API volume where the field value will be written. It is relative to the volume's mount point and must not contain '..'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="resource_field_ref" type="object">
                          Selects a resource field of a container (e.g., limits.cpu or requests.memory) to expose to the downward API volume item. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="container_name" type="string">
                              The name of the container to which the resource field reference applies, used to expose that container's resource limits or requests via the downward API. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="divisor" type="string">
                              The divisor for the resource value, used to scale the quantity (e.g., CPU time). Must be a Quantity string (e.g., '1m', '1Ki'), defaults to '1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="resource" type="string">
                              The name of the container resource to expose, such as 'requests.cpu' or 'limits.memory'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="user" type="number">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="empty_dir" type="object">
                  Represents an emptyDir volume, a temporary directory created empty when the pod is assigned to a node and deleted when the pod is removed. It provides scratch space that does not persist across pod restarts and is shared by all containers in the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="medium" type="string">
                      Specifies the storage medium for the emptyDir volume. An empty string (default) uses the node's default storage (disk), 'Memory' uses a RAM-backed tmpfs filesystem, and 'HugePages' uses the Linux huge pages filesystem. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mode" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="size_limit" type="string">
                      The maximum size of the emptyDir volume, specified as a Kubernetes quantity (e.g., '1Gi'). If unset, the volume is limited by the node's storage. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ephemeral" type="object">
                  Represents an ephemeral volume that is created with the pod and deleted when the pod is removed. This field can be used to define a volume from a generic ephemeral volume or a CSI ephemeral volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="volume_claim_template" type="object">
                      Defines the PersistentVolumeClaim template that is used to create a PVC for this ephemeral volume. The template specifies the desired characteristics of the PVC, such as storage class, access modes, and resources. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="metadata" type="object">
                          Metadata for the PersistentVolumeClaim created by this ephemeral volume claim template, used to set labels, annotations, and optionally a name for the PVC. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="annotations" type="map(string)">
                              Annotations is a map of string keys and values that attaches arbitrary non-identifying metadata to the volume claim template. This is commonly used to store tooling information or other custom metadata without affecting selection. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="creation_timestamp" type="string">
                              The timestamp, in RFC3339 format, indicating when the persistent volume claim generated from this template was created. This field is set by the server and cannot be set by clients. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="deletion_grace_period_seconds" type="number">
                              In Kubernetes ObjectMeta, this field specifies the duration in seconds before the object should be deleted. It is set when a graceful deletion is requested and must be a non-negative integer. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="deletion_timestamp" type="string">
                              The deletionTimestamp is set when the object is marked for deletion, indicating the time at which it will be (or was) removed, in RFC3339 format. In this context, it applies to the PersistentVolumeClaims generated from the volume claim template. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="finalizers" type="list(string)">
                              A list of finalizers for this object. Finalizers are strings that must be removed before the object can be deleted, ensuring pre-delete operations complete. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="generate_name" type="string">
                              generateName is an optional prefix used by the API server to generate a unique name if the name field is not provided. The generated name will be the prefix plus a random suffix, and the server handles uniqueness conflicts automatically. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="generation" type="number">
                              A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="labels" type="map(string)">
                              Labels to assign to the volume claim template's metadata. These labels are applied to the persistent volume claim created from this template, allowing selection and identification of the claim. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="managed_fields" type="list(object)">
                              A list of ManagedFieldsEntry objects that track the field management operations affecting this metadata. Each entry records the manager, operation, API version, and managed fields (e.g., for server-side apply). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="api_version" type="string">
                                  The API version of the resource that the manager used to manage these fields. This is part of the managedFields metadata entry, indicating which API version the managing controller or agent operated with. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="fields_type" type="string">
                                  FieldsType is the type of the managed fields entry, indicating the serialization format of the FieldsV1 member. For the Kubernetes API, this is typically set to 'FieldsV1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="fields_v1" type="any">
                                  This field contains a JSON representation of the managed fields for this entry, encoding which fields are managed by the manager in the v1 format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="manager" type="string">
                                  The name of the manager (e.g., kubectl, a controller, or an operator) that last applied or updated this managed field. In Kubernetes, each managedFields entry tracks which component owns the fields it manages, and this string identifies that component. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="operation" type="string">
                                  The operation type of the managed field entry, indicating whether the field was applied or updated. Supported values are 'Apply' and 'Update'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="subresource" type="string">
                                  The name of the subresource used to update the object, or empty string if the update was through the main resource. Distinguishes managers even if they share the same name; for example, 'status' for status subresource updates. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="time" type="string">
                                  Timestamp of when the operation recorded in this managed fields entry was performed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The metadata name for the volume claim template. This name is not used for the resulting PersistentVolumeClaim; the system generates the PVC name from the pod name and volume name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="namespace" type="string">
                              The namespace for the volume claim template. For ephemeral volumes, this field must be empty because the resulting PersistentVolumeClaim is always created in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="owner_references" type="list(object)">
                              List of references to the owning objects. Each entry includes fields such as apiVersion, kind, name, uid, and optionally controller and blockOwnerDeletion to specify ownership relationships for garbage collection. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="api_version" type="string">
                                  The API version of the owner (referent) object, e.g. 'apps/v1'. This is used together with kind and name to identify the owning object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="block_owner_deletion" type="bool">
                                  Whether this owner reference blocks deletion of the dependent object. If true, the dependent (the volume claim template's metadata) cannot be deleted by the Kubernetes garbage collector until the owner is deleted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="controller" type="bool">
                                  Indicates whether the referenced owner is the managing controller of the object. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="kind" type="string">
                                  The Kubernetes resource kind of the owner object referenced by this owner reference, such as 'DaemonSet'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="name" type="string">
                                  The name of the owner object that this reference points to. This corresponds to the metadata.name of the referenced resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="uid" type="string">
                                  The UID of the owner object referenced by this OwnerReference. It uniquely identifies the referenced resource in the Kubernetes API. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="resource_version" type="string">
                              The resource version of the object, used for optimistic concurrency. This value is set by the server and should not be modified by clients. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="self_link" type="string">
                              The selfLink attribute contains a URL that identifies the resource. It is deprecated and may not be populated in newer Kubernetes versions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="uid" type="string">
                              UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="spec" type="object">
                          The specification of the persistent volume claim to be created for this ephemeral volume. It defines properties such as access modes, resource requests, storage class name, and selector for the underlying persistent volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="access_modes" type="list(string)">
                              Specifies the access modes for the persistent volume claim, determining how the volume can be mounted. Common values include ReadWriteOnce, ReadOnlyMany, and ReadWriteMany. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="data_source" type="object">
                              data_source is an object that specifies an existing data source (e.g., a VolumeSnapshot or another PersistentVolumeClaim) from which to provision the volume. It corresponds to the dataSource field in a Kubernetes PersistentVolumeClaimSpec and uses a TypedLocalObjectReference. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="api_group" type="string">
                                  The API group of the referenced data source. Use an empty string for the core API group, or a specific group such as 'snapshot.storage.k8s.io' for VolumeSnapshot. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="kind" type="string">
                                  Kind of the referenced object (e.g., PersistentVolumeClaim). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="name" type="string">
                                  The name of the data source object referenced by this volume claim template. This is used with the apiGroup and kind fields to identify a specific PersistentVolumeClaim, VolumeSnapshot, or other supported data source. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="data_source_ref" type="object">
                              Reference to a data source object, such as a VolumeSnapshot or another PersistentVolumeClaim, used to pre-populate the volume. The reference includes the API group, kind, and name of the data source. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="api_group" type="string">
                                  The API group of the referenced resource, such as 'snapshot.storage.k8s.io' for a VolumeSnapshot. For core resources, this is empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="kind" type="string">
                                  The kind (resource type) of the object referenced by this data source reference. For example, 'PersistentVolumeClaim' or 'VolumeSnapshot'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="name" type="string">
                                  The name of the referenced object in the same namespace, such as a PersistentVolumeClaim or VolumeSnapshot. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="namespace" type="string">
                                  Namespace of the referenced data source object. If omitted, it is assumed to be in the same namespace as the volume claim template. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="resources" type="object">
                              Minimum storage resources required by the volume claim, typically specified as a quantity in `requests.storage` (e.g., "5Gi"). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="limits" type="map(string)">
                                  Resource limits for the ephemeral volume claim, defined as a map of resource names (like 'storage') to quantity strings (e.g., '1Gi'). These limits cap the amount of storage that can be requested by the volume claim template. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="requests" type="map(string)">
                                  A map of resource names to Kubernetes quantity strings (e.g., 'cpu': '500m', 'memory': '1Gi') that specifies the minimum amount of resources required for the ephemeral volume claim. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="selector" type="object">
                              A label query over volumes to consider for binding. This selector is used to match existing PersistentVolumes to the claim. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements. The requirements are ANDed. Each requirement specifies a key, operator, and values. This selector is used to match the labels of the volume claim template. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="storage_class_name" type="string">
                              The name of the StorageClass to use for dynamically provisioned storage. If not specified, the cluster's default StorageClass is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="volume_attributes_class_name" type="string">
                              Name of the VolumeAttributesClass to use for this persistent volume claim. When specified, it applies a set of customizable attributes (such as IOPS or throughput) to the dynamically provisioned volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="volume_mode" type="string">
                              volumeMode specifies whether the volume is treated as a raw block device (Block) or as a filesystem (Filesystem). The default is Filesystem. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="volume_name" type="string">
                              The name of the PersistentVolume to bind to this volume claim. When specified, the claim is bound to that existing PersistentVolume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="fc" type="object">
                  Configures a Fibre Channel (FC) volume source, allowing the Pod to mount a Fibre Channel LUN. This is used to define an FC volume with parameters such as target WWNs and filesystem type. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount for the Fibre Channel volume, such as ext4 or xfs. If not specified, the filesystem type of the first partition is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="lun" type="number">
                      The logical unit number (LUN) of the Fibre Channel volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Indicates whether the fibre channel volume is mounted as read-only. When set to true, the volume is read-only; otherwise it is read-write. Defaults to false if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="target_wwns" type="list(string)">
                      List of Fibre Channel target World Wide Names (WWNs) for the FC volume. This specifies the addresses of the storage targets to connect to. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="wwids" type="list(string)">
                      Optional list of Fibre Channel World Wide Identifiers (WWIDs) that specify the target volume. This field can be used instead of targetWWNs and lun to identify the Fibre Channel volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="flex_volume" type="object">
                  FlexVolume represents a generic volume plugin that mounts a volume using a user-specified driver and optional options. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="driver" type="string">
                      Specifies the FlexVolume driver to use for mounting this volume. This is typically a registered volume plugin name. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount on the flex volume, e.g. 'ext4' or 'xfs'. Must be supported by the host operating system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="options" type="map(string)">
                      Optional: Extra command options to pass to the FlexVolume plugin, as a map of string keys and values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      If true, the FlexVolume is mounted as read-only. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      Optional reference to a Secret that provides sensitive information to the FlexVolume plugin. The Secret must be in the same namespace as the Pod; only the Secret name is specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the Secret in the pod's namespace that provides sensitive information to the FlexVolume plugin. The Secret must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="flocker" type="object">
                  Defines a Flocker volume to be mounted into the container. Flocker is a deprecated volume source in Kubernetes, used to mount volumes from a Flocker dataset. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dataset_name" type="string">
                      The name of the Flocker dataset to use for this volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="dataset_uuid" type="string">
                      UUID of the Flocker dataset. Either dataset_name or dataset_uuid is required, but not both. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="gce_persistent_disk" type="object">
                  gcePersistentDisk represents a Google Compute Engine Persistent Disk resource. This configures the disk name, filesystem type, partition, and read-only mode for mounting the disk into the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount, such as ext4 or xfs. Defaults to ext4 if unset. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="partition" type="number">
                      The partition number on the GCE persistent disk. If specified, only that partition is mounted; if omitted, the entire disk is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="pd_name" type="string">
                      The name of the Google Compute Engine persistent disk (PD) to mount as a volume in the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the GCE persistent disk is mounted as read-only. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="git_repo" type="object">
                  Represents a gitRepo volume, which clones a Git repository into a volume. Contains settings such as repository, revision, and directory. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="directory" type="string">
                      Target directory name for the git repo volume. Must not contain or start with '..'. If omitted, the volume root is the git repository root; if specified, it mounts the named subdirectory of the repository. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="repository" type="string">
                      Repository is the URL of the Git repository to clone into the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="revision" type="string">
                      The commit revision (hash, tag, or branch) to check out from the git repository. If omitted, the repository's HEAD revision is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="glusterfs" type="object">
                  GlusterFS volume source configuration. Defines how to mount a GlusterFS volume into the pod, including the GlusterFS cluster endpoint and path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="endpoints" type="string">
                      Name of the Endpoints object that defines the GlusterFS cluster topology. Required when using the GlusterFS volume type, and must reference a valid Endpoints resource in the same namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path" type="string">
                      The GlusterFS volume path (e.g., the exported volume name) that is mounted into the pod. This is the name of the GlusterFS volume as defined on the GlusterFS server, such as 'gv0'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      ReadOnly here will force the Glusterfs volume to be mounted with read-only permissions. Defaults to false (read/write). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="host_path" type="object">
                  hostPath represents a pre-existing file or directory on the host machine that is directly exposed to the container. This volume type mounts a file or directory from the host node's filesystem into the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path" type="string">
                      Path of the directory or file on the host node to mount into the pod. This is the absolute path on the node's filesystem. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      Specifies the type of hostPath volume. Allowed values are: DirectoryOrCreate, Directory, FileOrCreate, File, Socket, CharDevice, BlockDevice. If not set, the hostPath is used without any type check. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="image" type="object">
                  The image volume source that mounts an OCI image as a volume. This field represents an ImageVolumeSource object, allowing the pod to access the contents of the image without running it as a container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="pull_policy" type="string">
                      The Kubernetes image pull policy for an image volume source. Valid values are Always, IfNotPresent, and Never, controlling whether the image is pulled each time, only if absent locally, or never. Defaults to Always when not set. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="reference" type="string">
                      Reference is the fully qualified image name and tag to mount as the volume, e.g., 'example.com/foo/bar:1.0'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="iscsi" type="object">
                  Configures an ISCSI volume source, allowing a pod to mount an ISCSI target as a volume. This object defines connection and mounting parameters for the ISCSI disk. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="chap_auth_discovery" type="bool">
                      Enable CHAP authentication for iSCSI discovery. When set to true, the initiator performs CHAP authentication with the target during the discovery phase. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="chap_auth_session" type="bool">
                      Whether to enable CHAP authentication for the iSCSI session. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount, such as ext4, xfs, or ntfs. Must be a filesystem type supported by the host operating system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="initiator_name" type="string">
                      Specifies a custom iSCSI initiator name. If not set, the system uses the default initiator name configured on the kubelet. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="iqn" type="string">
                      The iSCSI Qualified Name (IQN) of the target iSCSI device, used to identify the specific iSCSI target for mounting. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="iscsi_interface" type="string">
                      The name of the iSCSI interface. If not specified, defaults to "default". Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="lun" type="number">
                      lun is the iSCSI Target Lun number, specifying the logical unit number of the target. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="portals" type="list(string)">
                      The list of iSCSI portals to connect to if the targetPortal is not the default. Each portal is typically an IP address or hostname along with a port. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the iSCSI volume is mounted as read-only. When true, the filesystem is read-only; otherwise it is read-write (default false). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      Reference to the secret that contains CHAP authentication credentials for the iSCSI volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Secret object that provides authentication credentials (e.g., CHAP) for the iSCSI volume. The Secret must exist in the same namespace as the Pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="target_portal" type="string">
                      The iSCSI target portal. The portal is either an IP or ip_addr:port if a different port is needed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the volume. This must be a DNS_LABEL and unique within the pod. Containers reference this name in their volumeMounts to mount the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="nfs" type="object">
                  NFS volume source configuration, allowing the pod to mount a Network File System share. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path" type="string">
                      Path that is exported by the NFS server. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Indicates whether the NFS volume is mounted as read-only. When set to true, the volume is read-only; otherwise, it is read-write. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="server" type="string">
                      Hostname or IP address of the NFS server. This field is required when configuring an NFS volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="persistent_volume_claim" type="object">
                  PersistentVolumeClaimVolumeSource. Specifies a PersistentVolumeClaim in the same namespace as the pod that should be mounted as a volume. The claim must exist and be bound before the pod can start. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="claim_name" type="string">
                      The name of the PersistentVolumeClaim (PVC) in the same namespace to use as the volume source for this volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      For a volume of type persistentVolumeClaim, if true, mounts the volume as read-only. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="photon_persistent_disk" type="object">
                  Specifies a volume that uses a Photon Controller persistent disk. This configuration includes the persistent disk's ID and the filesystem type to mount. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount for the Photon persistent disk volume. Must be a filesystem type supported by the host operating system (e.g., ext4, xfs). If not specified, the filesystem type is inferred from the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="pd_id" type="string">
                      The unique identifier of the Photon Controller persistent disk to mount as a volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="portworx_volume" type="object">
                  Configuration for a Portworx volume. When specified, this volume source mounts a Portworx volume into the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount for the Portworx volume, such as 'ext4', 'xfs', or 'ntfs'. If unspecified, it is inferred to be 'ext4'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      If true, the volume is mounted as read-only. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_id" type="string">
                      The unique identifier of the Portworx volume to mount. This field is required and must correspond to an existing Portworx volume in the cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="projected" type="object">
                  Projected volumes map several existing volume sources into the same directory. This allows a single volume to combine multiple sources such as secrets, configmaps, and service account tokens. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="default_mode" type="number">
                      The default permission mode bits for files in the projected volume, expressed as an octal value (e.g., 0o644). If omitted, the API server applies a default of 0644. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="default_user" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="sources" type="list(object)">
                      Specifies the list of volume sources to project into the same directory. Each entry can reference a Secret, ConfigMap, DownwardAPI, or ServiceAccountToken. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="cluster_trust_bundle" type="object">
                          Configures a ClusterTrustBundle as a projected volume source, allowing the bundle's certificates to be mounted into the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="label_selector" type="object">
                              label_selector is a Kubernetes label selector that filters the ClusterTrustBundles to be projected. When specified, only ClusterTrustBundles whose labels match this selector are included in the projected volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="match_expressions" type="list(object)">
                                  A list of label selector requirements. The requirements are ANDed. Each requirement specifies a key, an operator, and a list of values. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `match_expressions` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="match_labels" type="map(string)">
                                  match_labels is a map of key-value pairs that selects ClusterTrustBundle resources by their labels. Only ClusterTrustBundles whose labels match all specified key-value pairs are included in the projected volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              Select a single ClusterTrustBundle by name. If set, this overrides labelSelector to choose a specific cluster trust bundle; if omitted, bundles are selected via labelSelector or all bundles when no selector is specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the referenced ClusterTrustBundle is optional. When true, the pod starts even if the bundle is not found, and the projected volume contains no data. When false (default), the pod fails to start if the bundle is missing. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Path is the relative path of the file in the volume where the cluster trust bundle CA bundle will be written. The CA bundle contents are stored at this path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="signer_name" type="string">
                              The name of the certificate signer whose certificates are included in the trust bundle. If empty, the default signer (kubernetes.io/cluster-root) is used. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="user" type="number">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="config_map" type="object">
                          config_map configures a ConfigMap to be projected into the volume. It specifies the name of the ConfigMap, optional items (key-to-path mappings), and an optional default mode for the files. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="items" type="list(object)">
                              A list of key-to-path mappings for projecting selected keys from the referenced ConfigMap into the volume. If unspecified, all key-value pairs are projected as files named by key; if specified, only the listed keys are projected to the given paths, and unlisted keys are not present. Each item includes 'key' and 'path', and optionally 'mode'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The key in the ConfigMap to project into the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="mode" type="number">
                                  Optional mode bits to set permissions on the file. Must be a valid octal value between 0 and 0777 (or 0 and 511) or -1 to use the default mode. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="path" type="string">
                                  path is the relative path within the volume to place the file. It must not contain '..' or start with '/'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="user" type="number">
                                  Optional; if omitted, computed by Kubernetes.
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the ConfigMap to project into the volume. The ConfigMap must exist in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Indicates whether the referenced ConfigMap or its keys must be defined. When true, the volume can be mounted even if the ConfigMap does not exist; when false or unset, the ConfigMap must exist. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="downward_api" type="object">
                          The DownwardAPI projection source for a projected volume. This exposes pod metadata (such as labels, annotations, and resource limits) as files inside the container. Configure the 'items' field to define which fields and file paths to project. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="items" type="list(object)">
                              List of downward API volume file definitions. Each item specifies a path within the volume and a field selector (via fieldRef or resourceFieldRef) that exposes the corresponding field's value at that path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="field_ref" type="object">
                                  Specifies the Pod field to be projected via the downward API, using a FieldRef that identifies the field path (e.g., metadata.name or spec.nodeName). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `field_ref` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="mode" type="number">
                                  Optional file permission mode for the downward API file, expressed as an octal or decimal number (e.g., 0644 or 420). If omitted, defaults to 0644. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="path" type="string">
                                  Path is the relative path name of the file to be created in the projected volume. It must not be absolute or contain '..'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="resource_field_ref" type="object">
                                  Represents a reference to a container resource field (such as CPU or memory limits/requests) to be exposed via the downward API. The value is resolved from the pod's container resources and written to the projected volume at the specified path. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `resource_field_ref` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="user" type="number">
                                  Optional; if omitted, computed by Kubernetes.
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="pod_certificate" type="object">
                          Optional; if omitted, computed by Kubernetes.
                          <Expandable title="properties">
                            <ResponseField name="certificate_chain_path" type="string">
                              The path within the projected volume where the pod's certificate chain is mounted, as used in a pod_certificate projected volume source. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="credential_bundle_path" type="string">
                              The path within the projected volume where the credential bundle (the pod's certificate) is mounted. This is a relative path that determines the filename or directory where the certificate data appears to the container. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="key_path" type="string">
                              The path within the projected volume where the certificate private key is mounted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="key_type" type="string">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="max_expiration_seconds" type="number">
                              Maximum number of seconds for which the pod certificate remains valid. The certificate's lifetime will not exceed this value. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="signer_name" type="string">
                              The name of the certificate signer that issues the pod certificate projected into the volume. This must correspond to a signer name known to the cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="user" type="number">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                            <ResponseField name="user_annotations" type="map(string)">
                              A map of user-defined annotations to be added to the certificate. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="secret" type="object">
                          Specifies a Secret to project into the volume. This source makes the contents of a Kubernetes Secret available in the projected volume, with the ability to select specific items and mark the secret as optional. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="items" type="list(object)">
                              Optional list of key-to-path mappings for the secret data. Each item maps a specific key in the secret to a relative path within the projected volume. If omitted, all keys in the secret are projected automatically. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="key" type="string">
                                  The key within the referenced Secret's data to project into the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="mode" type="number">
                                  Mode is the file permission bits (in octal) for the secret file at the given path. If unspecified, the system defaults to 0644. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="path" type="string">
                                  The relative path of the file within the projected volume to which the secret item is mounted. Corresponds to the path in the secret item mapping. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="user" type="number">
                                  Optional; if omitted, computed by Kubernetes.
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="name" type="string">
                              The name of the Secret to be projected into the volume. This must match the name of a Secret in the pod's namespace. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="optional" type="bool">
                              Specifies whether the referenced Secret must exist. If true, the Secret is optional and its absence does not prevent the volume from being mounted or the pod from starting. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="service_account_token" type="object">
                          Defines a projected volume source that injects a Kubernetes service account token into the volume. Supports configuration for the token audience, expiration time, and path within the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="audience" type="string">
                              Audience is the intended audience of the returned token. The recipient of the token must identify itself with this audience. If not set, the token audience defaults to the API server's identifier. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="expiration_seconds" type="number">
                              The requested duration of validity for the service account token, in seconds. Defaults to 3600 (1 hour) and must be at least 600 (10 minutes). The kubelet will proactively rotate the token as it approaches expiration. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="path" type="string">
                              Specifies the relative path to the token file within the projected volume source. This path determines the file name under which the service account token will be mounted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="user" type="number">
                              Optional; if omitted, computed by Kubernetes.
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="quobyte" type="object">
                  Defines a Quobyte volume source, configuring how a Quobyte storage volume is mounted into the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="group" type="string">
                      Group to map the volume access to. Default is none. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Mounts the Quobyte volume as read-only when set to true. Defaults to false (read/write). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="registry" type="string">
                      The Quobyte registry service or list of registries (host:port) to connect to. Required field; multiple entries are comma-separated. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="tenant" type="string">
                      The Quobyte tenant name for the volume. This is used to identify the tenant when accessing the Quobyte storage system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="user" type="string">
                      User to map volume access to. If not provided, defaults to the service account user. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume" type="string">
                      The name of the Quobyte volume to mount. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="rbd" type="object">
                  Represents a Ceph RBD (Rados Block Device) volume source. This allows a pod to mount a Ceph block device as a volume, typically configured via monitors, pool, and image details. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount on the RBD volume, e.g., ext4, xfs, ntfs. If unspecified, the system infers ext4 as the default. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="image" type="string">
                      The name of the RBD (Ceph block device) image to mount. This field is required when using the RBD volume plugin. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="keyring" type="string">
                      Path to the keyring file used for Ceph authentication. If not specified, defaults to /etc/ceph/keyring. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="monitors" type="list(string)">
                      List of Ceph monitor addresses (e.g., '192.168.1.1:6789') used to connect to the Ceph cluster for this RBD volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="pool" type="string">
                      The Ceph RADOS pool where the RBD image resides. If omitted, Kubernetes defaults this to 'rbd'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the Ceph RBD volume is mounted read-only. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      secret_ref specifies a reference to a Kubernetes Secret that contains the Ceph authentication key for the RBD volume. The Secret must be of type 'kubernetes.io/rbd' and include the key in the 'key' data field. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the secret containing the Ceph RBD credentials. Must be in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="user" type="string">
                      The RADOS user name for authentication. Defaults to 'admin' if not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="scale_io" type="object">
                  Configuration for a ScaleIO volume source, enabling the pod to mount storage from a ScaleIO (Dell EMC) cluster. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Defaults to "ext4" if unspecified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="gateway" type="string">
                      Host name or IP address of the ScaleIO API Gateway used by the ScaleIO volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="protection_domain" type="string">
                      The name of the ScaleIO Protection Domain for the configured storage. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Specifies whether the ScaleIO volume is mounted as read-only. Defaults to false. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      A reference to a Kubernetes Secret that contains the ScaleIO credentials (access key and secret key). The Secret must exist in the same namespace as the pod. This field is required when using ScaleIO volumes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the Kubernetes Secret that contains the credentials needed to access the ScaleIO storage system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="ssl_enabled" type="bool">
                      Specifies whether SSL is used for communication with the ScaleIO Gateway. When true, the ScaleIO volume plugin enables SSL for API requests. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="storage_mode" type="string">
                      The storage mode for the ScaleIO volume. Valid values are 'ThinProvisioned' and 'ThickProvisioned', with 'ThinProvisioned' being the default if unspecified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="storage_pool" type="string">
                      Name of the StoragePool in the ScaleIO system where the volume is located. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="system" type="string">
                      The name of the ScaleIO system to use for this volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_name" type="string">
                      The name of the ScaleIO volume to mount from the storage system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="secret" type="object">
                  Secret represents a Secret volume source. It configures the volume to consume a Kubernetes Secret, allowing the secret data to be exposed as files in the container. The secret source can specify the secret name, optional key/path items, and default file permissions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="default_mode" type="number">
                      Specifies the permission mode for files within the secret volume, given as an octal integer (e.g., 0644). Defaults to 0644 when not specified. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="default_user" type="number">
                      Optional; if omitted, computed by Kubernetes.
                    </ResponseField>
                    <ResponseField name="items" type="list(object)">
                      A list of key-to-path mappings. If specified, only the listed keys from the referenced Secret are mounted as files, each at the specified path. If omitted, all keys in the Secret become files named after the keys. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="key" type="string">
                          The key in the Secret's data to project into the volume. This key must exist in the Secret's data map. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="mode" type="number">
                          Permissions mode for the file. Must be an octal value between 0000 and 0777 (or decimal between 0 and 511). YAML accepts both octal and decimal values; JSON requires decimal. Defaults to the volume's defaultMode. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          The relative path of the file within the secret volume where the corresponding secret key's value is mounted. The path must not be absolute, must not contain '..' elements, and must not start with '..'. When `items` is specified, this field is required for each entry. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="user" type="number">
                          Optional; if omitted, computed by Kubernetes.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="optional" type="bool">
                      Indicates whether the referenced Secret must exist. When set to true, the Secret may be missing, and the volume will be created without those files. When false or omitted, the Secret must be present. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_name" type="string">
                      The name of the Kubernetes Secret to mount as a volume. The Secret must exist in the same namespace as the Pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="storageos" type="object">
                  Represents a StorageOS volume source in a Kubernetes pod specification. This object defines the parameters for accessing a StorageOS volume, including the volume name, namespace, filesystem type, read-only setting, and optional secret for authentication. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      The filesystem type to mount, such as 'ext4' or 'xfs'. Must be a filesystem type supported by the host operating system. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="read_only" type="bool">
                      Mounts the StorageOS volume with read-only permissions. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="secret_ref" type="object">
                      A reference to a Secret that contains the API credentials required to access the StorageOS volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Name of the Kubernetes Secret that holds the StorageOS API credentials. The Secret must be in the same namespace as the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="volume_name" type="string">
                      The name of the StorageOS volume to mount. This is the human-readable name of the volume as defined in StorageOS. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_namespace" type="string">
                      The namespace of the StorageOS volume. Defaults to the namespace of the pod. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="vsphere_volume" type="object">
                  Defines a vSphere volume (VMDK) to be attached and mounted to the pod. This block specifies the configuration for the vSphere volume source. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fs_type" type="string">
                      Filesystem type to mount for the vSphere volume (e.g., ext4, xfs). If not specified, defaults to ext4. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="storage_policy_id" type="string">
                      The ID of the vSphere Storage Policy Based Management (SPBM) storage policy to apply to the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="storage_policy_name" type="string">
                      Name of the vSphere storage policy used to provision storage for the volume. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="volume_path" type="string">
                      Path that uniquely identifies the vSphere volume, usually a datastore path like '[datastore1] vm/vm.vmdk'. This field is required for vSphere volumes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="update_strategy" type="object">
      DaemonSetUpdateStrategy is a struct used to control the update strategy for a DaemonSet. Optional.
      <Expandable title="properties">
        <ResponseField name="rolling_update" type="object">
          Spec to control the desired behavior of daemon set rolling update. Optional.
          <Expandable title="properties">
            <ResponseField name="max_surge" type="string">
              IntOrString is a type that can hold an int32 or a string. When used in JSON or YAML marshalling and unmarshalling, it produces or consumes the inner type. This allows you to have, for example, a JSON field that can accept a name or number. Optional.
            </ResponseField>
            <ResponseField name="max_unavailable" type="string">
              IntOrString is a type that can hold an int32 or a string. When used in JSON or YAML marshalling and unmarshalling, it produces or consumes the inner type. This allows you to have, for example, a JSON field that can accept a name or number. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          Type of daemon set update. Can be "RollingUpdate" or "OnDelete". Default is RollingUpdate. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="object">
  DaemonSetStatus represents the current status of a daemon set. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="collision_count" type="number">
      Count of hash collisions for the DaemonSet. The DaemonSet controller uses this field as a collision avoidance mechanism when it needs to create the name for the newest ControllerRevision. Optional.
    </ResponseField>
    <ResponseField name="conditions" type="list(object)">
      Represents the latest available observations of a DaemonSet's current state. Optional.
      <Expandable title="properties">
        <ResponseField name="last_transition_time" type="string">
          The last time the condition transitioned from one status to another. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable message providing details about the condition. This field typically contains information about the reason and any relevant context for the condition's state. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reason" type="string">
          A machine-readable, short, CamelCase string explaining why the condition is in its current state, such as 'MinimumReplicasAvailable' or 'ReplicaFailure'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          The status of the condition, indicating whether the condition is True, False, or Unknown. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the DaemonSet condition, providing a machine-readable identifier such as 'Available' or 'Progressing'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="current_number_scheduled" type="number" required>
      The number of nodes that are running at least 1 daemon pod and are supposed to run the daemon pod. More info: https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/ Required.
    </ResponseField>
    <ResponseField name="desired_number_scheduled" type="number" required>
      The total number of nodes that should be running the daemon pod (including nodes correctly running the daemon pod). More info: https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/ Required.
    </ResponseField>
    <ResponseField name="number_available" type="number">
      The number of nodes that should be running the daemon pod and have one or more of the daemon pod running and available (ready for at least spec.minReadySeconds) Optional.
    </ResponseField>
    <ResponseField name="number_misscheduled" type="number" required>
      The number of nodes that are running the daemon pod, but are not supposed to run the daemon pod. More info: https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/ Required.
    </ResponseField>
    <ResponseField name="number_ready" type="number" required>
      numberReady is the number of nodes that should be running the daemon pod and have one or more of the daemon pod running with a Ready Condition. Required.
    </ResponseField>
    <ResponseField name="number_unavailable" type="number">
      The number of nodes that should be running the daemon pod and have none of the daemon pod running and available (ready for at least spec.minReadySeconds) Optional.
    </ResponseField>
    <ResponseField name="observed_generation" type="number">
      The most recent generation observed by the daemon set controller. Optional.
    </ResponseField>
    <ResponseField name="updated_number_scheduled" type="number">
      The total number of nodes that are running updated daemon pod Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
