---
title: "kubernetes_core_node"
description: "`kubernetes_core_node` represents one real worker machine in the cluster -- physical or virtual -- and its own real, current capacity, conditions..."
---

`kubernetes_core_node` represents one real worker machine in the cluster -- physical or virtual -- and its own real, current capacity, conditions, and the pods actually scheduled onto it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only kubernetes --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-kubernetes/sdk/go => ./local-sdk/kubernetes/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	core "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/core"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own node"})

    		ubx.Resource(core.Node, "example", core.NodeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only kubernetes --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Node } from "./local-sdk/kubernetes/sdk/typescript/kubernetes/core/node.ts";

    export default stack("example", () => {
      intent({ summary: "example own node" });

      resource(Node, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only kubernetes --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/kubernetes/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.kubernetes.core import Node, NodeConfig

    def describe():
        ubx.intent("example own node")

        ubx.resource(Node, "example", NodeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create kubernetes_core_node called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="api_version" type="string">
  APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="object">
  ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
      Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations Optional.
    </ResponseField>
    <ResponseField name="creation_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="deletion_grace_period_seconds" type="number">
      Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only. Optional.
    </ResponseField>
    <ResponseField name="deletion_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="finalizers" type="list(string)">
      Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. Optional.
    </ResponseField>
    <ResponseField name="generate_name" type="string">
      GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server. If this field is specified and the generated name exists, the server will return a 409. Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency Optional.
    </ResponseField>
    <ResponseField name="generation" type="number">
      A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels Optional.
    </ResponseField>
    <ResponseField name="managed_fields" type="list(object)">
      ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          The API version of the resource that this managed field set applies to, in 'group/version' format (e.g., 'v1' or 'apps/v1'). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_type" type="string">
          The type of fields in a Kubernetes managed fields entry, typically 'FieldsV1'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_v1" type="any">
          fieldsV1 is a JSON representation of the fields that are managed by this manager. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="manager" type="string">
          The name of the manager (e.g., a controller, user, or client) that last applied the fields in this managedFields entry. This identifies which workflow or component owns the field ownership information. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operation" type="string">
          The operation performed by the manager, either 'Apply' or 'Update'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subresource" type="string">
          The subresource (such as 'status') that the fields are associated with. If empty, the fields apply to the main resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time" type="string">
          Timestamp of when the managed field entry was last updated, in RFC3339 format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names Optional.
    </ResponseField>
    <ResponseField name="namespace" type="string">
      Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces Optional.
    </ResponseField>
    <ResponseField name="owner_references" type="list(object)">
      List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          The API version of the referenced owner object, such as 'v1' for core resources or 'apps/v1' for Deployments. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="block_owner_deletion" type="bool">
          Indicates whether the owner reference prevents deletion of the object until the owner is deleted. If true, the dependent object cannot be deleted when the owner is deleted, matching the Kubernetes 'blockOwnerDeletion' field in ownerReferences. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="controller" type="bool">
          If true, this owner reference points to the managing controller. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          Kind of the referenced owner object (e.g., Deployment, ReplicaSet). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the owner object referenced by this owner reference. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="uid" type="string">
          The UID of the referenced object, uniquely identifying the owner resource. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_version" type="string">
      An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency Optional.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. Optional.
    </ResponseField>
    <ResponseField name="uid" type="string">
      UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="spec" type="object">
  NodeSpec describes the attributes that a node is created with. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="config_source" type="object">
      NodeConfigSource specifies a source of node configuration. Exactly one subfield (excluding metadata) must be non-nil. This API is deprecated since 1.22 Optional.
      <Expandable title="properties">
        <ResponseField name="config_map" type="object">
          ConfigMapNodeConfigSource contains the information to reference a ConfigMap as a config source for the Node. This API is deprecated since 1.22: https://git.k8s.io/enhancements/keps/sig-node/281-dynamic-kubelet-configuration Optional.
          <Expandable title="properties">
            <ResponseField name="kubelet_config_key" type="string" required>
              KubeletConfigKey declares which key of the referenced ConfigMap corresponds to the KubeletConfiguration structure This field is required in all cases. Required.
            </ResponseField>
            <ResponseField name="name" type="string" required>
              Name is the metadata.name of the referenced ConfigMap. This field is required in all cases. Required.
            </ResponseField>
            <ResponseField name="namespace" type="string" required>
              Namespace is the metadata.namespace of the referenced ConfigMap. This field is required in all cases. Required.
            </ResponseField>
            <ResponseField name="resource_version" type="string">
              ResourceVersion is the metadata.ResourceVersion of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status. Optional.
            </ResponseField>
            <ResponseField name="uid" type="string">
              UID is the metadata.UID of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="external_id" type="string">
      Deprecated. Not all kubelets will set this field. Remove field after 1.13. see: https://issues.k8s.io/61966 Optional.
    </ResponseField>
    <ResponseField name="pod_cidr" type="string">
      PodCIDR represents the pod IP range assigned to the node. Optional.
    </ResponseField>
    <ResponseField name="pod_cidrs" type="list(string)">
      podCIDRs represents the IP ranges assigned to the node for usage by Pods on that node. If this field is specified, the 0th entry must match the podCIDR field. It may contain at most 1 value for each of IPv4 and IPv6. Optional.
    </ResponseField>
    <ResponseField name="pod_preemption_policy" type="object">
      NodePodPreemptionPolicy defines the node-level policies governing preemption for pods on this node. Optional.
      <Expandable title="properties">
        <ResponseField name="disable_resize_preemption" type="list(string)">
          DisableResizePreemption lists the owners (e.g., autoscalers, operators, administrators) that have requested to disable scheduler and Kubelet preemption for in-place pod resize on this node. If this list is non-empty, resize-induced preemption is disabled on this node. This is an alpha field and requires enabling the InPlacePodVerticalScalingSchedulerPreemption feature gate. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provider_id" type="string">
      ID of the node assigned by the cloud provider in the format: &lt;ProviderName&gt;://&lt;ProviderSpecificNodeID&gt; Optional.
    </ResponseField>
    <ResponseField name="taints" type="list(object)">
      If specified, the node's taints. Optional.
      <Expandable title="properties">
        <ResponseField name="effect" type="string">
          The effect of the taint on pods that do not tolerate it. Valid values are NoSchedule, PreferNoSchedule, and NoExecute. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key" type="string">
          The key of the taint, used to match against pod tolerations. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time_added" type="string">
          The timestamp indicating when the taint was added to the node. This is a string representation of the time, typically in RFC3339 format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value associated with the taint key. This is an optional string that can be used alongside the key to describe the taint; it may be empty. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="unschedulable" type="bool">
      Unschedulable controls node schedulability of new pods. By default, node is schedulable. More info: https://kubernetes.io/docs/concepts/nodes/node/#manual-node-administration Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="object">
  NodeStatus is information about the current status of a node. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="addresses" type="list(object)">
      List of addresses reachable to the node. Queried from cloud provider, if available. More info: https://kubernetes.io/docs/reference/node/node-status/#addresses Note: This field is declared as mergeable, but the merge key is not sufficiently unique, which can cause data corruption when it is merged. Callers should instead use a full-replacement patch. See https://pr.k8s.io/79391 for an example. Consumers should assume that addresses can change during the lifetime of a Node. However, there are some exceptions where this may not be possible, such as Pods that inherit a Node's address in its own status or consumers of the downward API (status.hostIP). Optional.
      <Expandable title="properties">
        <ResponseField name="address" type="string">
          The actual IP address or hostname for the node, corresponding to the address type (e.g., InternalIP, ExternalIP, Hostname). Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Optional; if omitted, computed by Kubernetes.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="allocatable" type="map(string)">
      Allocatable represents the resources of a node that are available for scheduling. Defaults to Capacity. Optional.
    </ResponseField>
    <ResponseField name="capacity" type="map(string)">
      Capacity represents the total resources of a node. More info: https://kubernetes.io/docs/reference/node/node-status/#capacity Optional.
    </ResponseField>
    <ResponseField name="conditions" type="list(object)">
      Conditions is an array of current observed node conditions. More info: https://kubernetes.io/docs/reference/node/node-status/#condition Optional.
      <Expandable title="properties">
        <ResponseField name="last_heartbeat_time" type="string">
          The last time the condition was probed by the kubelet, typically in RFC3339 format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="last_transition_time" type="string">
          last_transition_time is the last time the condition transitioned from one status to another. This is a string representing a timestamp in RFC3339 format. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable message providing details about the condition. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reason" type="string">
          Machine-readable CamelCase reason for the condition's last transition, as defined by the Kubernetes API. It provides a short, programmatic indication of why the condition changed. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          The status of the condition, typically one of 'True', 'False', or 'Unknown'. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of node condition, indicating the specific aspect of node health being reported. Common values include "Ready", "MemoryPressure", "DiskPressure", "PIDPressure", and "NetworkUnavailable". Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="config" type="object">
      NodeConfigStatus describes the status of the config assigned by Node.Spec.ConfigSource. Optional.
      <Expandable title="properties">
        <ResponseField name="active" type="object">
          NodeConfigSource specifies a source of node configuration. Exactly one subfield (excluding metadata) must be non-nil. This API is deprecated since 1.22 Optional.
          <Expandable title="properties">
            <ResponseField name="config_map" type="object">
              ConfigMapNodeConfigSource contains the information to reference a ConfigMap as a config source for the Node. This API is deprecated since 1.22: https://git.k8s.io/enhancements/keps/sig-node/281-dynamic-kubelet-configuration Optional.
              <Expandable title="properties">
                <ResponseField name="kubelet_config_key" type="string" required>
                  KubeletConfigKey declares which key of the referenced ConfigMap corresponds to the KubeletConfiguration structure This field is required in all cases. Required.
                </ResponseField>
                <ResponseField name="name" type="string" required>
                  Name is the metadata.name of the referenced ConfigMap. This field is required in all cases. Required.
                </ResponseField>
                <ResponseField name="namespace" type="string" required>
                  Namespace is the metadata.namespace of the referenced ConfigMap. This field is required in all cases. Required.
                </ResponseField>
                <ResponseField name="resource_version" type="string">
                  ResourceVersion is the metadata.ResourceVersion of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status. Optional.
                </ResponseField>
                <ResponseField name="uid" type="string">
                  UID is the metadata.UID of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="assigned" type="object">
          NodeConfigSource specifies a source of node configuration. Exactly one subfield (excluding metadata) must be non-nil. This API is deprecated since 1.22 Optional.
          <Expandable title="properties">
            <ResponseField name="config_map" type="object">
              ConfigMapNodeConfigSource contains the information to reference a ConfigMap as a config source for the Node. This API is deprecated since 1.22: https://git.k8s.io/enhancements/keps/sig-node/281-dynamic-kubelet-configuration Optional.
              <Expandable title="properties">
                <ResponseField name="kubelet_config_key" type="string" required>
                  KubeletConfigKey declares which key of the referenced ConfigMap corresponds to the KubeletConfiguration structure This field is required in all cases. Required.
                </ResponseField>
                <ResponseField name="name" type="string" required>
                  Name is the metadata.name of the referenced ConfigMap. This field is required in all cases. Required.
                </ResponseField>
                <ResponseField name="namespace" type="string" required>
                  Namespace is the metadata.namespace of the referenced ConfigMap. This field is required in all cases. Required.
                </ResponseField>
                <ResponseField name="resource_version" type="string">
                  ResourceVersion is the metadata.ResourceVersion of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status. Optional.
                </ResponseField>
                <ResponseField name="uid" type="string">
                  UID is the metadata.UID of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="error" type="string">
          Error describes any problems reconciling the Spec.ConfigSource to the Active config. Errors may occur, for example, attempting to checkpoint Spec.ConfigSource to the local Assigned record, attempting to checkpoint the payload associated with Spec.ConfigSource, attempting to load or validate the Assigned config, etc. Errors may occur at different points while syncing config. Earlier errors (e.g. download or checkpointing errors) will not result in a rollback to LastKnownGood, and may resolve across Kubelet retries. Later errors (e.g. loading or validating a checkpointed config) will result in a rollback to LastKnownGood. In the latter case, it is usually possible to resolve the error by fixing the config assigned in Spec.ConfigSource. You can find additional information for debugging by searching the error message in the Kubelet log. Error is a human-readable description of the error state; machines can check whether or not Error is empty, but should not rely on the stability of the Error text across Kubelet versions. Optional.
        </ResponseField>
        <ResponseField name="last_known_good" type="object">
          NodeConfigSource specifies a source of node configuration. Exactly one subfield (excluding metadata) must be non-nil. This API is deprecated since 1.22 Optional.
          <Expandable title="properties">
            <ResponseField name="config_map" type="object">
              ConfigMapNodeConfigSource contains the information to reference a ConfigMap as a config source for the Node. This API is deprecated since 1.22: https://git.k8s.io/enhancements/keps/sig-node/281-dynamic-kubelet-configuration Optional.
              <Expandable title="properties">
                <ResponseField name="kubelet_config_key" type="string" required>
                  KubeletConfigKey declares which key of the referenced ConfigMap corresponds to the KubeletConfiguration structure This field is required in all cases. Required.
                </ResponseField>
                <ResponseField name="name" type="string" required>
                  Name is the metadata.name of the referenced ConfigMap. This field is required in all cases. Required.
                </ResponseField>
                <ResponseField name="namespace" type="string" required>
                  Namespace is the metadata.namespace of the referenced ConfigMap. This field is required in all cases. Required.
                </ResponseField>
                <ResponseField name="resource_version" type="string">
                  ResourceVersion is the metadata.ResourceVersion of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status. Optional.
                </ResponseField>
                <ResponseField name="uid" type="string">
                  UID is the metadata.UID of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="daemon_endpoints" type="object">
      NodeDaemonEndpoints lists ports opened by daemons running on the Node. Optional.
      <Expandable title="properties">
        <ResponseField name="kubelet_endpoint" type="object">
          DaemonEndpoint contains information about a single Daemon endpoint. Optional.
          <Expandable title="properties">
            <ResponseField name="port" type="number" required>
              Port number of the given endpoint. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="declared_features" type="list(string)">
      DeclaredFeatures represents the features related to feature gates that are declared by the node. Optional.
    </ResponseField>
    <ResponseField name="features" type="object">
      NodeFeatures describes the set of features implemented by the CRI implementation. The features contained in the NodeFeatures should depend only on the cri implementation independent of runtime handlers. Optional.
      <Expandable title="properties">
        <ResponseField name="supplemental_groups_policy" type="bool">
          SupplementalGroupsPolicy is set to true if the runtime supports SupplementalGroupsPolicy and ContainerUser. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="images" type="list(object)">
      List of container images on this node Optional.
      <Expandable title="properties">
        <ResponseField name="names" type="list(string)">
          The names by which this container image is known, such as tags and digests. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="size_bytes" type="number">
          The size of the container image in bytes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="node_info" type="object">
      NodeSystemInfo is a set of ids/uuids to uniquely identify the node. Optional.
      <Expandable title="properties">
        <ResponseField name="architecture" type="string" required>
          The Architecture reported by the node Required.
        </ResponseField>
        <ResponseField name="boot_id" type="string" required>
          Boot ID reported by the node. Required.
        </ResponseField>
        <ResponseField name="container_runtime_version" type="string" required>
          ContainerRuntime Version reported by the node through runtime remote API (e.g. containerd://1.4.2). Required.
        </ResponseField>
        <ResponseField name="kernel_version" type="string" required>
          Kernel Version reported by the node from 'uname -r' (e.g. 3.16.0-0.bpo.4-amd64). Required.
        </ResponseField>
        <ResponseField name="kube_proxy_version" type="string" required>
          Deprecated: KubeProxy Version reported by the node. Required.
        </ResponseField>
        <ResponseField name="kubelet_version" type="string" required>
          Kubelet Version reported by the node. Required.
        </ResponseField>
        <ResponseField name="machine_id" type="string" required>
          MachineID reported by the node. For unique machine identification in the cluster this field is preferred. Learn more from man(5) machine-id: http://man7.org/linux/man-pages/man5/machine-id.5.html Required.
        </ResponseField>
        <ResponseField name="operating_system" type="string" required>
          The Operating System reported by the node Required.
        </ResponseField>
        <ResponseField name="os_image" type="string" required>
          OS Image reported by the node from /etc/os-release (e.g. Debian GNU/Linux 7 (wheezy)). Required.
        </ResponseField>
        <ResponseField name="running_in_user_namespace" type="bool">
          Whether the node is running in a user namespace. Optional.
        </ResponseField>
        <ResponseField name="swap" type="object">
          NodeSwapStatus represents swap memory information. Optional.
          <Expandable title="properties">
            <ResponseField name="capacity" type="number">
              Total amount of swap memory in bytes. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="system_uuid" type="string" required>
          SystemUUID reported by the node. For unique machine identification MachineID is preferred. This field is specific to Red Hat hosts https://access.redhat.com/documentation/en-us/red_hat_subscription_management/1/html/rhsm/uuid Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="phase" type="string">
      NodePhase is the recently observed lifecycle phase of the node. More info: https://kubernetes.io/docs/concepts/nodes/node/#phase The field is never populated, and now is deprecated. Optional.
    </ResponseField>
    <ResponseField name="runtime_handlers" type="list(object)">
      The available runtime handlers. Optional.
      <Expandable title="properties">
        <ResponseField name="features" type="object">
          Specifies the features supported by this runtime handler, such as whether recursive read-only mounts are enabled. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="recursive_read_only_mounts" type="bool">
              Indicates whether the runtime handler supports recursive read-only mounts for volumes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_namespaces" type="bool">
              Whether the runtime handler supports Linux user namespaces. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the runtime handler, as configured in the kubelet's runtime configuration. This identifies which runtime handler is in use on the node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="volumes_attached" type="list(object)">
      List of volumes that are attached to the node. Optional.
      <Expandable title="properties">
        <ResponseField name="device_path" type="string">
          The device path that the volume is attached to on the node. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The unique device name of the attached volume, as reported by the Kubernetes node status. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="volumes_in_use" type="list(string)">
      List of attachable volumes in use (mounted) by the node. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
