---
title: "kubernetes_annotations"
description: "Real, generated bindings for `kubernetes_annotations`."
---

`kubernetes_annotations` -- real, typed bindings generated directly from the
`hashicorp/kubernetes` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	annotations "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/annotations"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own annotations"})

    		ubx.Resource(annotations.Annotations, "ci-runner", annotations.AnnotationsConfig{
    			ApiVersion:          "example",
    			Kind:                "example",
    			Annotations:         map[string]string{"managed-by": "ubx"},
    			FieldManager:        "example",
    			Force:               true,
    			TemplateAnnotations: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Annotations } from "jsr:@ubx/sdk-kubernetes/kubernetes/annotations/annotations";

    export default stack("payments", () => {
      intent({ summary: "payments own annotations" });

      resource(Annotations, "ci-runner", {
        apiVersion: "example",
        kind: "example",
        annotations: { "managed-by": "ubx" },
        fieldManager: "example",
        force: true,
        templateAnnotations: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.annotations import Annotations, AnnotationsConfig

    def describe():
        ubx.intent("payments own annotations")

        ubx.resource(Annotations, "ci-runner", AnnotationsConfig(
            api_version="example",
            kind="example",
            annotations={"managed-by": "ubx"},
            field_manager="example",
            force=True,
            template_annotations={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create kubernetes_annotations, with api version example, kind
    example, annotations a set value, field manager example, force
    true, template annotations a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="annotations" type="map(string)">
  A map of annotations to apply to the resource. Optional.
</ResponseField>

<ResponseField name="api_version" type="string" required>
  The apiVersion of the resource to annotate. Required.
</ResponseField>

<ResponseField name="field_manager" type="string">
  Set the name of the field manager for the specified labels. Optional.
</ResponseField>

<ResponseField name="force" type="bool">
  Force overwriting annotations that were created or edited outside of Terraform. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="kind" type="string" required>
  The kind of the resource to annotate. Required.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="namespace" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="template_annotations" type="map(string)">
  A map of annotations to apply to the resource template. Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="namespace" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
