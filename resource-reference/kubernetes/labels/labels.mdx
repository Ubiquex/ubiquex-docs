---
title: "kubernetes_labels"
description: "Real, generated bindings for `kubernetes_labels`."
---

`kubernetes_labels` -- real, typed bindings generated directly from the
`hashicorp/kubernetes` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	labels "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/labels"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own labels"})

    		ubx.Resource(labels.Labels, "ci-runner", labels.LabelsConfig{
    			ApiVersion:   "example",
    			Kind:         "example",
    			Labels:       map[string]string{"managed-by": "ubx"},
    			FieldManager: "example",
    			Force:        true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Labels } from "jsr:@ubx/sdk-kubernetes/kubernetes/labels/labels";

    export default stack("payments", () => {
      intent({ summary: "payments own labels" });

      resource(Labels, "ci-runner", {
        apiVersion: "example",
        kind: "example",
        labels: { "managed-by": "ubx" },
        fieldManager: "example",
        force: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.labels import Labels, LabelsConfig

    def describe():
        ubx.intent("payments own labels")

        ubx.resource(Labels, "ci-runner", LabelsConfig(
            api_version="example",
            kind="example",
            labels={"managed-by": "ubx"},
            field_manager="example",
            force=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create kubernetes_labels, with api version example, kind example,
    labels a set value, field manager example, force true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="field_manager" type="string">
  Optional.
</ResponseField>

<ResponseField name="force" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="kind" type="string" required>
  Required.
</ResponseField>

<ResponseField name="labels" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="namespace" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="namespace" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
