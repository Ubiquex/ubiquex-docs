---
title: "kubernetes_scheduling_v1alpha3_workload"
description: "The real, older `scheduling.k8s.io/v1alpha3` shape of `kubernetes_scheduling_workload`..."
---

The real, older `scheduling.k8s.io/v1alpha3` shape of `kubernetes_scheduling_workload`, still served for compatibility but not the current one -- that's the plain, unversioned sibling (`v1beta1`), the generation new gang-scheduled workload templates should actually target.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	scheduling "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/scheduling"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own v1alpha3 workload"})

    		ubx.Resource(scheduling.V1alpha3Workload, "example", scheduling.V1alpha3WorkloadConfig{
    			Name:      "example",
    			Namespace: "example",
    			Spec:      map[string]any{"composite_pod_group_templates": []map[string]any{{"name": "example-name"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V1alpha3Workload } from "@ubx/sdk-kubernetes/kubernetes/scheduling/v1alpha3_workload";

    export default stack("example", () => {
      intent({ summary: "example own v1alpha3 workload" });

      resource(V1alpha3Workload, "example", {
        name: "example",
        namespace: "example",
        spec: { compositePodGroupTemplates: [{ name: "example-name" }] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.scheduling.v1alpha3_workload import V1alpha3Workload, V1alpha3WorkloadConfig

    def describe():
        ubx.intent("example own v1alpha3 workload")

        ubx.resource(V1alpha3Workload, "example", V1alpha3WorkloadConfig(
            name="example",
            namespace="example",
            spec={"composite_pod_group_templates": [{"name": "example-name"}]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create kubernetes_scheduling_v1alpha3_workload called
    "example", with namespace example, spec a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="api_version" type="string">
  APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds Optional; if omitted, computed by Kubernetes.
</ResponseField>

<ResponseField name="metadata" type="object">
  ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. Optional; if omitted, computed by Kubernetes.
  <Expandable title="properties">
    <ResponseField name="annotations" type="map(string)">
      Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations Optional.
    </ResponseField>
    <ResponseField name="creation_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="deletion_grace_period_seconds" type="number">
      Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only. Optional.
    </ResponseField>
    <ResponseField name="deletion_timestamp" type="string">
      Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON. Wrappers are provided for many of the factory methods that the time package offers. Optional.
    </ResponseField>
    <ResponseField name="finalizers" type="list(string)">
      Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. Optional.
    </ResponseField>
    <ResponseField name="generate_name" type="string">
      GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server. If this field is specified and the generated name exists, the server will return a 409. Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency Optional.
    </ResponseField>
    <ResponseField name="generation" type="number">
      A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels Optional.
    </ResponseField>
    <ResponseField name="managed_fields" type="list(object)">
      ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          The API version of the resource to which these managed fields apply. For example, 'v1' or 'batch/v1'. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_type" type="string">
          The type of fields managed by this entry, indicating the versioned format (e.g., FieldsV1). Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fields_v1" type="any">
          fieldsV1 stores the set of fields that a manager owns, represented as a JSON object where keys are field paths and values indicate ownership. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="manager" type="string">
          The name of the component (e.g., kubectl, a controller) that owns this managed field entry. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operation" type="string">
          Indicates the type of operation that led to this managed field entry, with allowed values 'Apply' and 'Update'. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subresource" type="string">
          The subresource, such as status, that this managed field entry applies to. An empty string indicates the main resource. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time" type="string">
          The timestamp of when this managed fields entry was added or updated, in RFC3339 format. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names Optional.
    </ResponseField>
    <ResponseField name="namespace" type="string">
      Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces Optional.
    </ResponseField>
    <ResponseField name="owner_references" type="list(object)">
      List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. Optional.
      <Expandable title="properties">
        <ResponseField name="api_version" type="string">
          apiVersion is the API version of the referent (the owner object being referenced). Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="block_owner_deletion" type="bool">
          If true, and if the owner has the 'foregroundDeletion' finalizer, the owner cannot be deleted from the key-value store until this dependent is deleted. This blocks the owner's deletion until the resource is removed, protecting against premature garbage collection. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="controller" type="bool">
          Indicates whether the referenced object is the managing controller of the workload. This helps Kubernetes determine which owner is responsible for managing and garbage-collecting the dependent resource. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind (resource type) of the owner object, such as 'Deployment' or 'ReplicaSet'. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the owner object that this reference points to. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="uid" type="string">
          The UID of the owner object referenced by this owner reference. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_version" type="string">
      An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency Optional.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. Optional.
    </ResponseField>
    <ResponseField name="uid" type="string">
      UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="spec" type="object" required>
  WorkloadSpec defines the desired state of a Workload. Required.
  <Expandable title="properties">
    <ResponseField name="composite_pod_group_templates" type="list(object)">
      compositePodGroupTemplates is the list of CompositePodGroup templates that make up the Workload. The maximum number of templates is 8. This field is immutable. Exactly one of CompositePodGroupTemplates and PodGroupTemplates must be set. This field is used only when the CompositePodGroup feature gate is enabled. Optional.
      <Expandable title="properties">
        <ResponseField name="composite_pod_group_templates" type="list(object)">
          Real, nested CompositePodGroupTemplates -- lets a multi-tier workload (e.g. a training job's own leader and worker roles) express its own real scheduling hierarchy as a tree, not just a flat list. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="disruption_mode" type="object">
          How the scheduler treats this composite group as a unit under preemption -- whether losing any member counts as disrupting the whole group, or only losing all of them does. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="all" type="any">
              Real settings for treating the WHOLE group as disrupted the moment any single member pod is evicted or preempted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="single" type="any">
              Real settings for treating only the specific evicted or preempted member as disrupted, not the group as a whole. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          The real name this pod-group template is identified by within its own parent Workload. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="pod_group_templates" type="list(object)">
          The list of pod group templates that define the individual pod groups contained in this composite pod group template. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="disruption_mode" type="object">
              How the scheduler treats this PodGroup as a unit under preemption -- whether losing any member counts as disrupting the whole group, or only losing all of them does. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="all" type="any">
                  Real settings for treating the WHOLE group as disrupted the moment any single member pod is evicted or preempted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="single" type="any">
                  Real settings for treating only the specific evicted or preempted member as disrupted, not the group as a whole. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              The real name this pod-group template is identified by within its own parent Workload. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="preemption_policy" type="string">
              Determines whether pods in this pod group can preempt lower-priority pods. Allowed values are Never and PreemptLowerPriority. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="priority" type="number">
              The real scheduling priority this pod group's own generated PodGroup is created with. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="priority_class_name" type="string">
              The name of the PriorityClass to apply to the pod. This sets the pod's priority level, influencing scheduling and preemption behavior. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_claims" type="list(object)">
              List of resource claims to associate with the pod group template. Each entry defines a named reference to a ResourceClaim or ResourceClaimTemplate, enabling dynamic resource allocation for containers in the pod (as used by Kubernetes PodSpec resourceClaims). Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  Name uniquely identifies this resource claim inside the pod. It must be used when generating the corresponding ResourceClaim object's name. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="resource_claim_name" type="string">
                  The name of the ResourceClaim that the pod references, used for Kubernetes dynamic resource allocation (DRA). Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="resource_claim_template_name" type="string">
                  The name of the ResourceClaimTemplate that defines the resource claim to be allocated for this pod. This references a Kubernetes ResourceClaimTemplate to provide the claim. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="scheduling_constraints" type="object">
              Real, placement-affecting constraints (like a required topology domain) the scheduler must honor when placing this pod group's own member pods. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="topology" type="list(object)">
                  The real node-label key naming the topology domain (e.g. a rack or a zone) this pod group's own members must be co-located within. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="key" type="string">
                      The topology key that defines the domain for this scheduling constraint, such as a well-known Kubernetes label like `kubernetes.io/hostname` or `topology.kubernetes.io/zone`. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="scheduling_policy" type="object">
              The real gang-scheduling policy (e.g. an all-or-nothing minimum count) this pod group's own generated PodGroup enforces. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="basic" type="any">
                  The real, basic gang-scheduling policy shape -- how many of this group's own pods must be schedulable together before any of them are bound. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="gang" type="object">
                  Specifies gang scheduling configuration for the pod group, ensuring that all member pods are scheduled together as a unit. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="min_count" type="number">
                      The minimum number of pods in the gang that must be successfully scheduled for the gang to be considered fulfilled. Gang scheduling will only proceed if at least this many pods can be scheduled together. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="preemption_policy" type="string">
          Real settings controlling whether and how this composite pod group's own members can preempt other, lower-priority workloads to get scheduled. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="priority" type="number">
          The real scheduling priority this composite pod group's own generated PodGroup is created with. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="priority_class_name" type="string">
          The name of the Kubernetes PriorityClass that determines the scheduling priority for pods created from this composite pod group template. Must match an existing PriorityClass in the cluster. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scheduling_constraints" type="object">
          Real, placement-affecting constraints (like a required topology domain) the scheduler must honor when placing this composite pod group's own member pods. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="topology" type="list(object)">
              The real node-label key naming the topology domain (e.g. a rack or a zone) this pod group's own members must be co-located within. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="key" type="string">
                  The topology key, typically a node label key such as topology.kubernetes.io/zone, that defines the domain for the scheduling constraint. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="scheduling_policy" type="object">
          The real gang-scheduling policy (e.g. an all-or-nothing minimum count) this composite pod group's own generated PodGroup enforces. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="basic" type="any">
              The real, basic gang-scheduling policy shape -- how many of this group's own pods must be schedulable together before any of them are bound. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="gang" type="object">
              Defines the gang scheduling policy for the composite pod group, ensuring that all pods in the group are scheduled together or not at all. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="min_group_count" type="number">
                  The minimum number of pod groups that must be co-scheduled for the gang scheduling policy to be satisfied. This value sets the required lower bound on the number of groups that must be available before the workload is admitted. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="controller_ref" type="object">
      TypedLocalObjectReference allows to reference typed object inside the same namespace. Optional.
      <Expandable title="properties">
        <ResponseField name="api_group" type="string">
          apiGroup is the group for the resource being referenced. If APIGroup is empty, the specified Kind must be in the core API group. For any other third-party types, setting APIGroup is required. It must be a DNS subdomain. Optional.
        </ResponseField>
        <ResponseField name="kind" type="string" required>
          kind is the type of resource being referenced. It must be a path segment name. Required.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          name is the name of resource being referenced. It must be a path segment name. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pod_group_templates" type="list(object)">
      podGroupTemplates is the list of templates that make up the Workload. The maximum number of templates is 8. Templates cannot be added or removed after the workload is created. Existing templates may still be updated where their individual fields allow it. Exactly one of CompositePodGroupTemplates and PodGroupTemplates must be set. Optional.
      <Expandable title="properties">
        <ResponseField name="disruption_mode" type="object">
          How the scheduler treats this PodGroup as a unit under preemption -- whether losing any member counts as disrupting the whole group, or only losing all of them does. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="all" type="any">
              Real settings for treating the WHOLE group as disrupted the moment any single member pod is evicted or preempted. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="single" type="any">
              Real settings for treating only the specific evicted or preempted member as disrupted, not the group as a whole. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          The real name this pod-group template is identified by within its own parent Workload. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="preemption_policy" type="string">
          Specifies the policy for preempting pods with lower priority when higher-priority pods need to be scheduled. Can be set to 'PreemptLowerPriority' (default) to allow preemption, or 'Never' to prevent it. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="priority" type="number">
          The real scheduling priority this pod group's own generated PodGroup is created with. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="priority_class_name" type="string">
          Name of the PriorityClass that defines the scheduling priority for pods in this pod group. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_claims" type="list(object)">
          A list of resource claims that reference ResourceClaim objects to be requested for pods in this group. Each entry specifies a name for the claim and either a reference to an existing ResourceClaim or a ResourceClaimTemplate to create one. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the ResourceClaim that the pod group template references. This must match the name of an existing ResourceClaim in the same namespace. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_claim_name" type="string">
              The name of the ResourceClaim that this pod group template references. The ResourceClaim must exist in the same namespace as the scheduling workload. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_claim_template_name" type="string">
              The name of the ResourceClaimTemplate that defines the resource claim to be created. This references an existing ResourceClaimTemplate resource in the same namespace. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="scheduling_constraints" type="object">
          Scheduling constraints for the pods in this pod group template, determining where and how they are scheduled onto cluster nodes. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="topology" type="list(object)">
              The real node-label key naming the topology domain (e.g. a rack or a zone) this pod group's own members must be co-located within. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="key" type="string">
                  The topology key that identifies the node topology domain for the scheduling constraint, typically a node label key. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="scheduling_policy" type="object">
          The real gang-scheduling policy (e.g. an all-or-nothing minimum count) this pod group's own generated PodGroup enforces. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="basic" type="any">
              The real, basic gang-scheduling policy shape -- how many of this group's own pods must be schedulable together before any of them are bound. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="gang" type="object">
              Defines the gang scheduling policy for the pod group, ensuring that all pods in the group are scheduled together as a unit (all-or-nothing). This is used to avoid resource deadlocks in multi-pod workloads. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="min_count" type="number">
                  The minimum number of pods that must be scheduled together for the gang to be scheduled. If this threshold is not met, the scheduling of the entire group is delayed. Optional; if omitted, computed by Kubernetes. Optional; if omitted, computed by Kubernetes. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
