---
title: "kubernetes_meta_apiresource"
description: "`kubernetes_meta_apiresource` reads one real resource kind the cluster's API server currently serves within a given API group and version -- whether..."
---

`kubernetes_meta_apiresource` reads one real resource kind the cluster's API server currently serves within a given API group and version -- whether it's namespaced, what verbs it supports -- part of the same discovery mechanism `kubectl api-resources` uses.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	meta "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/data/meta"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up kubernetes_meta_apiresource"})

    		ubx.Data(meta.Apiresource, "example", meta.ApiresourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Apiresource } from "@ubx/sdk-kubernetes/kubernetes/data/meta/apiresource";

    export default stack("example", () => {
      intent({ summary: "look up kubernetes_meta_apiresource" });

      data(Apiresource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.kubernetes.data.meta import Apiresource, ApiresourceConfig

    def describe():
        ubx.intent("look up kubernetes_meta_apiresource")

        ubx.data(Apiresource, "example", ApiresourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="api_version" type="string">
  APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources Returned by the lookup.
</ResponseField>

<ResponseField name="group_version" type="string">
  groupVersion is the group and version this APIResourceList is for. Returned by the lookup.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds Returned by the lookup.
</ResponseField>

<ResponseField name="resources" type="list(object)">
  resources contains the name of the resources and if they are namespaced. Returned by the lookup.
</ResponseField>
