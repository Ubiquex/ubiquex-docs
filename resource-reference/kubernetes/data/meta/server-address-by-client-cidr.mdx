---
title: "kubernetes_meta_server_address_by_client_cidr"
description: "`kubernetes_meta_server_address_by_client_cidr` reads which real API server address a client within a given CIDR range should actually use to reach..."
---

`kubernetes_meta_server_address_by_client_cidr` reads which real API server address a client within a given CIDR range should actually use to reach the cluster -- relevant for a cluster that advertises a different server address depending on where the client is connecting from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only kubernetes --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-kubernetes/sdk/go => ./local-sdk/kubernetes/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	meta "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/data/meta"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up kubernetes_meta_server_address_by_client_cidr"})

    		ubx.Data(meta.ServerAddressByClientCidr, "example", meta.ServerAddressByClientCidrConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only kubernetes --lang ts --out ./local-sdk
    import { ServerAddressByClientCidr } from "./local-sdk/kubernetes/sdk/typescript/kubernetes/data/meta/server_address_by_client_cidr.ts";

    export default stack("example", () => {
      intent({ summary: "look up kubernetes_meta_server_address_by_client_cidr" });

      data(ServerAddressByClientCidr, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only kubernetes --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/kubernetes/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.kubernetes.data.meta import ServerAddressByClientCidr, ServerAddressByClientCidrConfig

    def describe():
        ubx.intent("look up kubernetes_meta_server_address_by_client_cidr")

        ubx.data(ServerAddressByClientCidr, "example", ServerAddressByClientCidrConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="api_version" type="string">
  APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources Returned by the lookup.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds Returned by the lookup.
</ResponseField>

<ResponseField name="server_address_by_client_cidrs" type="list(object)">
  a map of client CIDR to server address that is serving this group. This is to help clients reach servers in the most network-efficient way possible. Clients can use the appropriate server address as per the CIDR that they match. In case of multiple matches, clients should use the longest matching CIDR. The server returns only those CIDRs that it thinks that the client can match. For example: the master will return an internal IP CIDR only, if the client reaches the server using an internal IP. Server looks at X-Forwarded-For header or X-Real-Ip header or request.RemoteAddr (in that order) to get the client IP. Returned by the lookup.
</ResponseField>

<ResponseField name="versions" type="list(string)">
  versions are the api versions that are available. Returned by the lookup.
</ResponseField>
