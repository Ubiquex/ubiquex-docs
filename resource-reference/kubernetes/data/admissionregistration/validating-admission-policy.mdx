---
title: "kubernetes_admissionregistration_validating_admission_policy"
description: "`kubernetes_admissionregistration_validating_admission_policy` looks up a real, already-defined reusable validation rule in CEL..."
---

`kubernetes_admissionregistration_validating_admission_policy` looks up a real, already-defined reusable validation rule in CEL, evaluated in-process by the API server rather than by a separately-deployed webhook.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only kubernetes --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-kubernetes/sdk/go => ./local-sdk/kubernetes/sdk/go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	admissionregistration "github.com/ubiquex/ubx-sdk-kubernetes/sdk/go/kubernetes/data/admissionregistration"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up kubernetes_admissionregistration_validating_admission_policy"})

    		ubx.Data(admissionregistration.ValidatingAdmissionPolicy, "example", admissionregistration.ValidatingAdmissionPolicyConfig{
    			AllowWatchBookmarks:  true,
    			Continue:             "example",
    			FieldSelector:        "example",
    			LabelSelector:        "example",
    			Limit:                1,
    			ResourceVersion:      "example",
    			ResourceVersionMatch: "example",
    			SendInitialEvents:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only kubernetes --lang ts --out ./local-sdk
    import { ValidatingAdmissionPolicy } from "./local-sdk/kubernetes/sdk/typescript/kubernetes/data/admissionregistration/validating_admission_policy.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up kubernetes_admissionregistration_validating_admission_policy",
      });

      data(ValidatingAdmissionPolicy, "example", {
        allowWatchBookmarks: true,
        continue: "example",
        fieldSelector: "example",
        labelSelector: "example",
        limit: 1,
        resourceVersion: "example",
        resourceVersionMatch: "example",
        sendInitialEvents: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only kubernetes --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/kubernetes/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.kubernetes.data.admissionregistration import ValidatingAdmissionPolicy, ValidatingAdmissionPolicyConfig

    def describe():
        ubx.intent("look up kubernetes_admissionregistration_validating_admission_policy")

        ubx.data(ValidatingAdmissionPolicy, "example", ValidatingAdmissionPolicyConfig(
            allow_watch_bookmarks=True,
            continue_="example",
            field_selector="example",
            label_selector="example",
            limit=1,
            resource_version="example",
            resource_version_match="example",
            send_initial_events=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="allow_watch_bookmarks" type="bool">
  Whether bookmark events should be sent, letting the client safely resume a broken watch without missing events, if the server supports it. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="continue" type="string">
  The continue token from a previous, size-limited list response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="field_selector" type="string">
  A selector restricting the list to objects whose fields match, for example metadata.name=foo. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="label_selector" type="string">
  A selector restricting the list to objects whose labels match. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return in one response; the server may return fewer, plus a continue token for the rest. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_version" type="string">
  Restricts the list to a specific resource version, applied according to resource_version_match, rather than the most recent. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_version_match" type="string">
  How resource_version is applied to the list call; the supported values are Exact and NotOlderThan. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="send_initial_events" type="bool">
  Whether to also send synthetic added events for every object currently matching the list before the normal watch stream begins. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="shard_selector" type="string">
  Restricts the list or watch to a specific shard, for a resource whose objects are distributed across multiple shards. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="timeout_seconds" type="number">
  The maximum duration a watch request stays open before the server closes it. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="watch" type="bool">
  Whether to watch for changes rather than return a single snapshot list. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="api_version" type="string">
  APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources Returned by the lookup.
</ResponseField>

<ResponseField name="items" type="list(object)">
  List of ValidatingAdmissionPolicy. Returned by the lookup.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds Returned by the lookup.
</ResponseField>

<ResponseField name="metadata" type="object">
  ListMeta describes metadata that synthetic resources must have, including lists and various status objects. A resource may have only one of &#123;ObjectMeta, ListMeta&#125;. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="continue" type="string">
      continue may be set if the user set a limit on the number of items returned, and indicates that the server has more data available. The value is opaque and may be used to issue another request to the endpoint that served this list to retrieve the next set of available objects. Continuing a consistent list may not be possible if the server configuration has changed or more than a few minutes have passed. The resourceVersion field returned when using this continue value will be identical to the value in the first response, unless you have received this token from an error message. Optional lookup argument.
    </ResponseField>
    <ResponseField name="remaining_item_count" type="number">
      remainingItemCount is the number of subsequent items in the list which are not included in this list response. If the list request contained label or field selectors, then the number of remaining items is unknown and the field will be left unset and omitted during serialization. If the list is complete (either because it is not chunking or because this is the last chunk), then there are no more remaining items and this field will be left unset and omitted during serialization. Servers older than v1.15 do not set this field. The intended use of the remainingItemCount is *estimating* the size of a collection. Clients should not rely on the remainingItemCount to be set or to be exact. Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_version" type="string">
      String that identifies the server's internal version of this object that can be used by clients to determine when objects have changed. Value must be treated as opaque by clients and passed unmodified back to the server. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency Optional lookup argument.
    </ResponseField>
    <ResponseField name="self_link" type="string">
      Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. Optional lookup argument.
    </ResponseField>
    <ResponseField name="shard_info" type="object">
      ShardInfo describes the shard selector that was applied to produce a list response. Its presence on a list response indicates the list is a filtered subset. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="selector" type="string" required>
          selector is the shard selector string from the request, echoed back so clients can verify which shard they received and merge responses from multiple shards. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
