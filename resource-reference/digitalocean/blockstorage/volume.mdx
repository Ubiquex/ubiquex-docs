---
title: "digitalocean_volume"
description: "`digitalocean_volume` looks up a real, already-created Block Storage volume -- an independently resizable SSD volume attachable to a Droplet..."
---

`digitalocean_volume` looks up a real, already-created Block Storage volume -- an independently resizable SSD volume attachable to a Droplet, outliving the Droplet's own lifecycle.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	blockstorage "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/blockstorage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own volume"})

    		ubx.Resource(blockstorage.Volume, "example", blockstorage.VolumeConfig{
    			VolumeId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Volume } from "@ubx/sdk-digitalocean/digitalocean/blockstorage/volume";

    export default stack("example", () => {
      intent({ summary: "example own volume" });

      resource(Volume, "example", {
        volumeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.blockstorage import Volume, VolumeConfig

    def describe():
        ubx.intent("example own volume")

        ubx.resource(Volume, "example", VolumeConfig(
            volume_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create digitalocean_volume, with volume id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="volume_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="volume" type="object">
  The Block Storage volume's own real, current representation. Computed by DigitalOcean after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      Optional; if omitted, computed by DigitalOcean.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by DigitalOcean.
    </ResponseField>
    <ResponseField name="droplet_ids" type="list(number)">
      Optional; if omitted, computed by DigitalOcean.
    </ResponseField>
    <ResponseField name="filesystem_label" type="string">
      Optional; if omitted, computed by DigitalOcean.
    </ResponseField>
    <ResponseField name="filesystem_type" type="string">
      Optional; if omitted, computed by DigitalOcean.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional; if omitted, computed by DigitalOcean.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by DigitalOcean.
    </ResponseField>
    <ResponseField name="region" type="object">
      Optional; if omitted, computed by DigitalOcean.
      <Expandable title="properties">
        <ResponseField name="available" type="bool">
          Optional; if omitted, computed by DigitalOcean.
        </ResponseField>
        <ResponseField name="features" type="list(string)">
          Optional; if omitted, computed by DigitalOcean.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by DigitalOcean.
        </ResponseField>
        <ResponseField name="sizes" type="list(string)">
          Optional; if omitted, computed by DigitalOcean.
        </ResponseField>
        <ResponseField name="slug" type="string">
          Optional; if omitted, computed by DigitalOcean.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="size_gigabytes" type="number">
      Optional; if omitted, computed by DigitalOcean.
    </ResponseField>
    <ResponseField name="tags" type="list(string)">
      Optional; if omitted, computed by DigitalOcean.
    </ResponseField>
  </Expandable>
</ResponseField>
