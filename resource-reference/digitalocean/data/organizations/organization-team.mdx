---
title: "digitalocean_organization_team"
description: "`data_digitalocean_organization_team` lists the real teams belonging to the account's own organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_organization_team"})

    		ubx.Data(organizations.OrganizationTeam, "example", organizations.OrganizationTeamConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrganizationTeam } from "@ubx/sdk-digitalocean/digitalocean/data/organizations/organization_team";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_organization_team" });

      data(OrganizationTeam, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.organizations import OrganizationTeam, OrganizationTeamConfig

    def describe():
        ubx.intent("look up digitalocean_organization_team")

        ubx.data(OrganizationTeam, "example", OrganizationTeamConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="teams" type="list(object)">
  The list of real teams in the account's own organization. **(AI-inferred)** Returned by the lookup.
</ResponseField>
