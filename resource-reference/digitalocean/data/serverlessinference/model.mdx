---
title: "digitalocean_model"
description: "`data_digitalocean_model` lists the real, available models exposed through the Gradient AI Platform's own OpenAI-compatible inference API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	serverlessinference "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/serverlessinference"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_model"})

    		ubx.Data(serverlessinference.Model, "example", serverlessinference.ModelConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Model } from "@ubx/sdk-digitalocean/digitalocean/data/serverlessinference/model";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_model" });

      data(Model, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.serverlessinference import Model, ModelConfig

    def describe():
        ubx.intent("look up digitalocean_model")

        ubx.data(Model, "example", ModelConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The list of available models. Returned by the lookup.
</ResponseField>

<ResponseField name="object" type="string">
  The object type, which is always "list". Returned by the lookup.
</ResponseField>
