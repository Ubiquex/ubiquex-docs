---
title: "digitalocean_cdn"
description: "`data_digitalocean_cdn` lists the real, already-created CDN endpoints on the account, paginated."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdnendpoints "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/cdnendpoints"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_cdn"})

    		ubx.Data(cdnendpoints.Cdn, "example", cdnendpoints.CdnConfig{
    			Page:    1,
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Cdn } from "@ubx/sdk-digitalocean/digitalocean/data/cdnendpoints/cdn";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_cdn" });

      data(Cdn, "example", {
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.cdnendpoints import Cdn, CdnConfig

    def describe():
        ubx.intent("look up digitalocean_cdn")

        ubx.data(Cdn, "example", CdnConfig(
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  The page number to return, for a paginated list of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of items to return per page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
