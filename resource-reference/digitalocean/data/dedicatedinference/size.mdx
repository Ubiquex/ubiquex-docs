---
title: "digitalocean_dedicated_inference_size"
description: "`data_digitalocean_dedicated_inference_size` lists the real, available dedicated inference sizes and the real regions each is enabled in."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dedicatedinference "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/dedicatedinference"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_dedicated_inference_size"})

    		ubx.Data(dedicatedinference.Size, "example", dedicatedinference.SizeConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Size } from "@ubx/sdk-digitalocean/digitalocean/data/dedicatedinference/size";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_dedicated_inference_size" });

      data(Size, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.dedicatedinference import Size, SizeConfig

    def describe():
        ubx.intent("look up digitalocean_dedicated_inference_size")

        ubx.data(Size, "example", SizeConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="enabled_regions" type="list(string)">
  Regions where Dedicated Inference is available. Returned by the lookup.
</ResponseField>

<ResponseField name="sizes" type="list(object)">
  The list of real, available dedicated inference sizes. **(AI-inferred)** Returned by the lookup.
</ResponseField>
