---
title: "digitalocean_dedicated_inference_gpu_model_config"
description: "`data_digitalocean_dedicated_inference_gpu_model_config` lists the real, available GPU model configurations a dedicated inference deployment can be..."
---

`data_digitalocean_dedicated_inference_gpu_model_config` lists the real, available GPU model configurations a dedicated inference deployment can be provisioned with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dedicatedinference "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/dedicatedinference"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_dedicated_inference_gpu_model_config"})

    		ubx.Data(dedicatedinference.GpuModelConfig, "example", dedicatedinference.GpuModelConfigConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GpuModelConfig } from "@ubx/sdk-digitalocean/digitalocean/data/dedicatedinference/gpu_model_config";

    export default stack("example", () => {
      intent({
        summary: "look up digitalocean_dedicated_inference_gpu_model_config",
      });

      data(GpuModelConfig, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.dedicatedinference import GpuModelConfig, GpuModelConfigConfig

    def describe():
        ubx.intent("look up digitalocean_dedicated_inference_gpu_model_config")

        ubx.data(GpuModelConfig, "example", GpuModelConfigConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="gpu_model_configs" type="list(object)">
  The list of real, available GPU model configurations. **(AI-inferred)** Returned by the lookup.
</ResponseField>
