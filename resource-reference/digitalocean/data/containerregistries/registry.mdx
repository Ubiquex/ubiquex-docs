---
title: "digitalocean_registry"
description: "`data_digitalocean_registry` lists the real, available container registry subscription tier options."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerregistries "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/containerregistries"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_registry"})

    		ubx.Data(containerregistries.Registry, "example", containerregistries.RegistryConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Registry } from "@ubx/sdk-digitalocean/digitalocean/data/containerregistries/registry";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_registry" });

      data(Registry, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.containerregistries import Registry, RegistryConfig

    def describe():
        ubx.intent("look up digitalocean_registry")

        ubx.data(Registry, "example", RegistryConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="options" type="object">
  The real, available container registry subscription tier options. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="available_regions" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="subscription_tiers" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
