---
title: "digitalocean_kubernete"
description: "`data_digitalocean_kubernete` lists the real, already-provisioned DigitalOcean Kubernetes clusters on the account, paginated."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kubernetes "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/kubernetes"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_kubernete"})

    		ubx.Data(kubernetes.Kubernete, "example", kubernetes.KuberneteConfig{
    			Page:    1,
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Kubernete } from "@ubx/sdk-digitalocean/digitalocean/data/kubernetes/kubernete";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_kubernete" });

      data(Kubernete, "example", {
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.kubernetes import Kubernete, KuberneteConfig

    def describe():
        ubx.intent("look up digitalocean_kubernete")

        ubx.data(Kubernete, "example", KuberneteConfig(
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  The page number to return, for a paginated list of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of items to return per page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
