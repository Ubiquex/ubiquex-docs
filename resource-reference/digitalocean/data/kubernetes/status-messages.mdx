---
title: "digitalocean_status_messages"
description: "`data_digitalocean_status_messages` lists the real, recent status messages for a Kubernetes cluster since a given point in time."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kubernetes "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/kubernetes"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_status_messages"})

    		ubx.Data(kubernetes.StatusMessages, "example", kubernetes.StatusMessagesConfig{
    			ClusterId: "example",
    			Since:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StatusMessages } from "@ubx/sdk-digitalocean/digitalocean/data/kubernetes/status_messages";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_status_messages" });

      data(StatusMessages, "example", {
        clusterId: "example",
        since: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.kubernetes import StatusMessages, StatusMessagesConfig

    def describe():
        ubx.intent("look up digitalocean_status_messages")

        ubx.data(StatusMessages, "example", StatusMessagesConfig(
            cluster_id="example",
            since="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  The ID of the Kubernetes cluster this belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="since" type="string">
  Returns only status messages recorded after this real point in time. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="messages" type="list(object)">
  The list of real, recent status messages. **(AI-inferred)** Returned by the lookup.
</ResponseField>
