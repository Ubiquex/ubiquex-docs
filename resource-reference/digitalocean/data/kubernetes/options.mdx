---
title: "digitalocean_kubernetes_options"
description: "`data_digitalocean_kubernetes_options` lists the real, available options (regions, versions..."
---

`data_digitalocean_kubernetes_options` lists the real, available options (regions, versions, machine sizes) for creating a new DigitalOcean Kubernetes cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kubernetes "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/kubernetes"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_kubernetes_options"})

    		ubx.Data(kubernetes.Options, "example", kubernetes.OptionsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Options } from "@ubx/sdk-digitalocean/digitalocean/data/kubernetes/options";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_kubernetes_options" });

      data(Options, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.kubernetes import Options, OptionsConfig

    def describe():
        ubx.intent("look up digitalocean_kubernetes_options")

        ubx.data(Options, "example", OptionsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="options" type="object">
  The real, available options (regions, versions, sizes) for creating a new cluster. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="regions" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sizes" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="versions" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
