---
title: "digitalocean_kubernetes_node_pool"
description: "`data_digitalocean_kubernetes_node_pool` lists the real, already-created node pools for a specific Kubernetes cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kubernetes "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/kubernetes"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_kubernetes_node_pool"})

    		ubx.Data(kubernetes.NodePool, "example", kubernetes.NodePoolConfig{
    			ClusterId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NodePool } from "@ubx/sdk-digitalocean/digitalocean/data/kubernetes/node_pool";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_kubernetes_node_pool" });

      data(NodePool, "example", {
        clusterId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.kubernetes import NodePool, NodePoolConfig

    def describe():
        ubx.intent("look up digitalocean_kubernetes_node_pool")

        ubx.data(NodePool, "example", NodePoolConfig(
            cluster_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  The ID of the Kubernetes cluster this belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="node_pools" type="list(object)">
  The list of real, already-created node pools. **(AI-inferred)** Returned by the lookup.
</ResponseField>
