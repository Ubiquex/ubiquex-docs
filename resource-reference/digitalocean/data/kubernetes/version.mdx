---
title: "digitalocean_kubernetes_version"
description: "`data_digitalocean_kubernetes_version` lists the real Kubernetes versions a specific cluster can be upgraded to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kubernetes "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/kubernetes"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_kubernetes_version"})

    		ubx.Data(kubernetes.Version, "example", kubernetes.VersionConfig{
    			ClusterId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Version } from "@ubx/sdk-digitalocean/digitalocean/data/kubernetes/version";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_kubernetes_version" });

      data(Version, "example", {
        clusterId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.kubernetes import Version, VersionConfig

    def describe():
        ubx.intent("look up digitalocean_kubernetes_version")

        ubx.data(Version, "example", VersionConfig(
            cluster_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  The ID of the Kubernetes cluster this belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="available_upgrade_versions" type="list(object)">
  The real Kubernetes versions this cluster can be upgraded to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
