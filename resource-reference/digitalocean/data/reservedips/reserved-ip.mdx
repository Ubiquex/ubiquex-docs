---
title: "digitalocean_reserved_ip"
description: "`data_digitalocean_reserved_ip` looks up a real, already-reserved IPv4 address."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	reservedips "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/reservedips"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_reserved_ip"})

    		ubx.Data(reservedips.ReservedIp, "example", reservedips.ReservedIpConfig{
    			ReservedIp: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedIp } from "@ubx/sdk-digitalocean/digitalocean/data/reservedips/reserved_ip";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_reserved_ip" });

      data(ReservedIp, "example", {
        reservedIp: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.reservedips import ReservedIp, ReservedIpConfig

    def describe():
        ubx.intent("look up digitalocean_reserved_ip")

        ubx.data(ReservedIp, "example", ReservedIpConfig(
            reserved_ip="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="reserved_ip" type="string" required>
  The reserved IP address to look up. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
