---
title: "digitalocean_partner_network_connect"
description: "`data_digitalocean_partner_network_connect` lists the real, already-created Partner Network Connect attachments on the account, paginated."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	partnernetworkconnect "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/partnernetworkconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_partner_network_connect"})

    		ubx.Data(partnernetworkconnect.Partnernetworkconnect, "example", partnernetworkconnect.PartnernetworkconnectConfig{
    			Page:    1,
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Partnernetworkconnect } from "@ubx/sdk-digitalocean/digitalocean/data/partnernetworkconnect/partnernetworkconnect";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_partner_network_connect" });

      data(Partnernetworkconnect, "example", {
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.partnernetworkconnect import Partnernetworkconnect, PartnernetworkconnectConfig

    def describe():
        ubx.intent("look up digitalocean_partner_network_connect")

        ubx.data(Partnernetworkconnect, "example", PartnernetworkconnectConfig(
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  The page number to return, for a paginated list of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of items to return per page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
