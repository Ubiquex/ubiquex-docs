---
title: "digitalocean_attachment"
description: "`data_digitalocean_attachment` looks up a real, already-created Partner Network Connect attachment by its own ID."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	partnernetworkconnect "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/partnernetworkconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_attachment"})

    		ubx.Data(partnernetworkconnect.Attachment, "example", partnernetworkconnect.AttachmentConfig{
    			PaId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Attachment } from "@ubx/sdk-digitalocean/digitalocean/data/partnernetworkconnect/attachment";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_attachment" });

      data(Attachment, "example", {
        paId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.partnernetworkconnect import Attachment, AttachmentConfig

    def describe():
        ubx.intent("look up digitalocean_attachment")

        ubx.data(Attachment, "example", AttachmentConfig(
            pa_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="pa_id" type="string" required>
  The ID of the Partner Network Connect attachment to look up. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
