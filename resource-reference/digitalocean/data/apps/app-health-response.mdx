---
title: "digitalocean_app_health_response"
description: "`data_digitalocean_app_health_response` looks up the real, current health status of a specific App Platform application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apps "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/apps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_app_health_response"})

    		ubx.Data(apps.AppHealthResponse, "example", apps.AppHealthResponseConfig{
    			AppId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppHealthResponse } from "@ubx/sdk-digitalocean/digitalocean/data/apps/app_health_response";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_app_health_response" });

      data(AppHealthResponse, "example", {
        appId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.apps import AppHealthResponse, AppHealthResponseConfig

    def describe():
        ubx.intent("look up digitalocean_app_health_response")

        ubx.data(AppHealthResponse, "example", AppHealthResponseConfig(
            app_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_id" type="string" required>
  The ID of the App Platform application to check health for. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_health" type="object">
  The application's own real, current health status. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="components" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="functions_components" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
