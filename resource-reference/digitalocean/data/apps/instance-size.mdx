---
title: "digitalocean_apps_instance_size"
description: "`data_digitalocean_apps_instance_size` lists the real, available App Platform instance sizes an application's own components can be deployed at."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apps "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/apps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_apps_instance_size"})

    		ubx.Data(apps.InstanceSize, "example", apps.InstanceSizeConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceSize } from "@ubx/sdk-digitalocean/digitalocean/data/apps/instance_size";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_apps_instance_size" });

      data(InstanceSize, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.apps import InstanceSize, InstanceSizeConfig

    def describe():
        ubx.intent("look up digitalocean_apps_instance_size")

        ubx.data(InstanceSize, "example", InstanceSizeConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="discount_percent" type="number">
  The real percentage discount applied to this instance size, if any. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instance_sizes" type="list(object)">
  The list of real, available App Platform instance sizes. **(AI-inferred)** Returned by the lookup.
</ResponseField>
