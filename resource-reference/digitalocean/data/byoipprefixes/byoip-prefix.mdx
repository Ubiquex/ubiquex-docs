---
title: "digitalocean_byoip_prefix"
description: "`data_digitalocean_byoip_prefix` lists the real, already-registered Bring Your Own IP prefixes on the account, paginated."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	byoipprefixes "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/byoipprefixes"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_byoip_prefix"})

    		ubx.Data(byoipprefixes.ByoipPrefix, "example", byoipprefixes.ByoipPrefixConfig{
    			ByoipPrefixUuid: "example",
    			Page:            1,
    			PerPage:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ByoipPrefix } from "@ubx/sdk-digitalocean/digitalocean/data/byoipprefixes/byoip_prefix";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_byoip_prefix" });

      data(ByoipPrefix, "example", {
        byoipPrefixUuid: "example",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.byoipprefixes import ByoipPrefix, ByoipPrefixConfig

    def describe():
        ubx.intent("look up digitalocean_byoip_prefix")

        ubx.data(ByoipPrefix, "example", ByoipPrefixConfig(
            byoip_prefix_uuid="example",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="byoip_prefix_uuid" type="string" required>
  The UUID of the BYOIP prefix to look up. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number to return, for a paginated list of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of items to return per page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
