---
title: "digitalocean_logsink_schema"
description: "`data_digitalocean_logsink_schema` lists the real, already-configured log sinks for a managed database cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	databases "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/databases"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_logsink_schema"})

    		ubx.Data(databases.LogsinkSchema, "example", databases.LogsinkSchemaConfig{
    			DatabaseClusterUuid: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LogsinkSchema } from "@ubx/sdk-digitalocean/digitalocean/data/databases/logsink_schema";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_logsink_schema" });

      data(LogsinkSchema, "example", {
        databaseClusterUuid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.databases import LogsinkSchema, LogsinkSchemaConfig

    def describe():
        ubx.intent("look up digitalocean_logsink_schema")

        ubx.data(LogsinkSchema, "example", LogsinkSchemaConfig(
            database_cluster_uuid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_cluster_uuid" type="string" required>
  The UUID of the managed database cluster this belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="sinks" type="list(object)">
  The list of real, already-configured log sinks. **(AI-inferred)** Returned by the lookup.
</ResponseField>
