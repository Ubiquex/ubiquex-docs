---
title: "digitalocean_firewall_rule"
description: "`data_digitalocean_firewall_rule` lists the real, already-configured firewall rules for a managed database cluster -- which sources are allowed to connect."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	databases "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/databases"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_firewall_rule"})

    		ubx.Data(databases.FirewallRule, "example", databases.FirewallRuleConfig{
    			DatabaseClusterUuid: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallRule } from "@ubx/sdk-digitalocean/digitalocean/data/databases/firewall_rule";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_firewall_rule" });

      data(FirewallRule, "example", {
        databaseClusterUuid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.databases import FirewallRule, FirewallRuleConfig

    def describe():
        ubx.intent("look up digitalocean_firewall_rule")

        ubx.data(FirewallRule, "example", FirewallRuleConfig(
            database_cluster_uuid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_cluster_uuid" type="string" required>
  The UUID of the managed database cluster this belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="rules" type="list(object)">
  The list of real, already-configured firewall rules. **(AI-inferred)** Returned by the lookup.
</ResponseField>
