---
title: "digitalocean_metric"
description: "`data_digitalocean_metric` retrieves the real credentials needed to query a resource's own monitoring metrics directly."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	databases "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/databases"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_metric"})

    		ubx.Data(databases.Metric, "example", databases.MetricConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Metric } from "@ubx/sdk-digitalocean/digitalocean/data/databases/metric";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_metric" });

      data(Metric, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.databases import Metric, MetricConfig

    def describe():
        ubx.intent("look up digitalocean_metric")

        ubx.data(Metric, "example", MetricConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="credentials" type="object">
  The real credentials needed to query this resource's own monitoring metrics. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="credentials" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="basic_auth_password" type="string">
          basic authentication password for metrics HTTP endpoint Optional lookup argument.
        </ResponseField>
        <ResponseField name="basic_auth_username" type="string">
          basic authentication username for metrics HTTP endpoint Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
