---
title: "digitalocean_opensearch_index"
description: "`data_digitalocean_opensearch_index` lists the real, already-created indexes for a managed OpenSearch cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	databases "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/databases"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_opensearch_index"})

    		ubx.Data(databases.OpensearchIndex, "example", databases.OpensearchIndexConfig{
    			DatabaseClusterUuid: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpensearchIndex } from "@ubx/sdk-digitalocean/digitalocean/data/databases/opensearch_index";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_opensearch_index" });

      data(OpensearchIndex, "example", {
        databaseClusterUuid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.databases import OpensearchIndex, OpensearchIndexConfig

    def describe():
        ubx.intent("look up digitalocean_opensearch_index")

        ubx.data(OpensearchIndex, "example", OpensearchIndexConfig(
            database_cluster_uuid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_cluster_uuid" type="string" required>
  The UUID of the managed OpenSearch cluster these indexes belong to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="indexes" type="list(object)">
  The list of real, already-created OpenSearch indexes. **(AI-inferred)** Returned by the lookup.
</ResponseField>
