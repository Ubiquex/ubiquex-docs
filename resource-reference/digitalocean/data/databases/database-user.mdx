---
title: "digitalocean_database_user"
description: "`data_digitalocean_database_user` lists the real, already-created database users for a managed database cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	databases "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/databases"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_database_user"})

    		ubx.Data(databases.DatabaseUser, "example", databases.DatabaseUserConfig{
    			DatabaseClusterUuid: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DatabaseUser } from "@ubx/sdk-digitalocean/digitalocean/data/databases/database_user";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_database_user" });

      data(DatabaseUser, "example", {
        databaseClusterUuid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.databases import DatabaseUser, DatabaseUserConfig

    def describe():
        ubx.intent("look up digitalocean_database_user")

        ubx.data(DatabaseUser, "example", DatabaseUserConfig(
            database_cluster_uuid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_cluster_uuid" type="string" required>
  The UUID of the managed database cluster this belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="users" type="list(object)">
  The list of real, already-created database users. **(AI-inferred)** Returned by the lookup.
</ResponseField>
