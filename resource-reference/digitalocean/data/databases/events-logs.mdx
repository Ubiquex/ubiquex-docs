---
title: "digitalocean_events_logs"
description: "`data_digitalocean_events_logs` lists the real, already-recorded operational events for a managed database cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	databases "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/databases"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_events_logs"})

    		ubx.Data(databases.EventsLogs, "example", databases.EventsLogsConfig{
    			DatabaseClusterUuid: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EventsLogs } from "@ubx/sdk-digitalocean/digitalocean/data/databases/events_logs";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_events_logs" });

      data(EventsLogs, "example", {
        databaseClusterUuid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.databases import EventsLogs, EventsLogsConfig

    def describe():
        ubx.intent("look up digitalocean_events_logs")

        ubx.data(EventsLogs, "example", EventsLogsConfig(
            database_cluster_uuid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_cluster_uuid" type="string" required>
  The UUID of the managed database cluster this belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="events" type="list(object)">
  The list of real, already-recorded operational events. **(AI-inferred)** Returned by the lookup.
</ResponseField>
