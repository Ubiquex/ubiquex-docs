---
title: "digitalocean_database_cluster_read"
description: "`data_digitalocean_database_cluster_read` lists the real, already-created logical databases inside a managed database cluster, optionally scoped by tag."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	databases "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/databases"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_database_cluster_read"})

    		ubx.Data(databases.DatabaseClusterRead, "example", databases.DatabaseClusterReadConfig{
    			TagName: "example-tag-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DatabaseClusterRead } from "@ubx/sdk-digitalocean/digitalocean/data/databases/database_cluster_read";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_database_cluster_read" });

      data(DatabaseClusterRead, "example", {
        tagName: "example-tag-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.databases import DatabaseClusterRead, DatabaseClusterReadConfig

    def describe():
        ubx.intent("look up digitalocean_database_cluster_read")

        ubx.data(DatabaseClusterRead, "example", DatabaseClusterReadConfig(
            tag_name="example-tag-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="tag_name" type="string">
  Restricts the listed databases to those tagged with this real tag name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="databases" type="list(object)">
  The list of real, already-created logical databases. **(AI-inferred)** Returned by the lookup.
</ResponseField>
