---
title: "digitalocean_floating_ip"
description: "`data_digitalocean_floating_ip` looks up a real, already-reserved floating IP."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	floatingips "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/floatingips"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_floating_ip"})

    		ubx.Data(floatingips.FloatingIp, "example", floatingips.FloatingIpConfig{
    			FloatingIp: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FloatingIp } from "@ubx/sdk-digitalocean/digitalocean/data/floatingips/floating_ip";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_floating_ip" });

      data(FloatingIp, "example", {
        floatingIp: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.floatingips import FloatingIp, FloatingIpConfig

    def describe():
        ubx.intent("look up digitalocean_floating_ip")

        ubx.data(FloatingIp, "example", FloatingIpConfig(
            floating_ip="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="floating_ip" type="string" required>
  The floating IP address to look up. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
