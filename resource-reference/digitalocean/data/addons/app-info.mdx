---
title: "digitalocean_addons_app_info"
description: "`data_digitalocean_addons_app_info` lists the real, current DigitalOcean App Platform applications an account has, for use when configuring an add-on..."
---

`data_digitalocean_addons_app_info` lists the real, current DigitalOcean App Platform applications an account has, for use when configuring an add-on that targets a specific app.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	addons "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/addons"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_addons_app_info"})

    		ubx.Data(addons.AppInfo, "example", addons.AppInfoConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppInfo } from "@ubx/sdk-digitalocean/digitalocean/data/addons/app_info";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_addons_app_info" });

      data(AppInfo, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.addons import AppInfo, AppInfoConfig

    def describe():
        ubx.intent("look up digitalocean_addons_app_info")

        ubx.data(AppInfo, "example", AppInfoConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="apps" type="list(object)">
  The list of real App Platform applications. **(AI-inferred)** Returned by the lookup.
</ResponseField>
