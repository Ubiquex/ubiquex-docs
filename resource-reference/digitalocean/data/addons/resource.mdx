---
title: "digitalocean_addons_resource"
description: "`data_digitalocean_addons_resource` lists the real, current resources an account's own enabled add-ons apply to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	addons "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/addons"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_addons_resource"})

    		ubx.Data(addons.Resource, "example", addons.ResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Resource } from "@ubx/sdk-digitalocean/digitalocean/data/addons/resource";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_addons_resource" });

      data(Resource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.addons import Resource, ResourceConfig

    def describe():
        ubx.intent("look up digitalocean_addons_resource")

        ubx.data(Resource, "example", ResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="resources" type="list(object)">
  The list of real resources the account's own add-ons apply to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
