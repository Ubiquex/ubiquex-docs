---
title: "digitalocean_autoscale"
description: "`data_digitalocean_autoscale` lists the real, already-created Droplet autoscale pools on the account, paginated."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dropletautoscalepools "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/dropletautoscalepools"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_autoscale"})

    		ubx.Data(dropletautoscalepools.Autoscale, "example", dropletautoscalepools.AutoscaleConfig{
    			AutoscalePoolId: "example",
    			Page:            1,
    			PerPage:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Autoscale } from "@ubx/sdk-digitalocean/digitalocean/data/dropletautoscalepools/autoscale";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_autoscale" });

      data(Autoscale, "example", {
        autoscalePoolId: "example",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.dropletautoscalepools import Autoscale, AutoscaleConfig

    def describe():
        ubx.intent("look up digitalocean_autoscale")

        ubx.data(Autoscale, "example", AutoscaleConfig(
            autoscale_pool_id="example",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="autoscale_pool_id" type="string" required>
  The ID of the autoscale pool to look up. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number to return, for a paginated list of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of items to return per page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
