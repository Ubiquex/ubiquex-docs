---
title: "digitalocean_image"
description: "`data_digitalocean_image` looks up a real, already-available image by its own ID."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	images "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/images"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_image"})

    		ubx.Data(images.Image, "example", images.ImageConfig{
    			ImageId: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Image } from "@ubx/sdk-digitalocean/digitalocean/data/images/image";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_image" });

      data(Image, "example", {
        imageId: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.images import Image, ImageConfig

    def describe():
        ubx.intent("look up digitalocean_image")

        ubx.data(Image, "example", ImageConfig(
            image_id=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_id" type="number" required>
  The ID of the image to look up. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
