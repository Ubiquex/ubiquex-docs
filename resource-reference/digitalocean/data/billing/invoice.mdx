---
title: "digitalocean_invoice"
description: "`data_digitalocean_invoice` lists the real line items of a specific past invoice, paginated."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_invoice"})

    		ubx.Data(billing.Invoice, "example", billing.InvoiceConfig{
    			InvoiceUuid: "example",
    			Page:        1,
    			PerPage:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Invoice } from "@ubx/sdk-digitalocean/digitalocean/data/billing/invoice";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_invoice" });

      data(Invoice, "example", {
        invoiceUuid: "example",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.billing import Invoice, InvoiceConfig

    def describe():
        ubx.intent("look up digitalocean_invoice")

        ubx.data(Invoice, "example", InvoiceConfig(
            invoice_uuid="example",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="invoice_uuid" type="string" required>
  The UUID of the invoice to retrieve line items for. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number to return, for a paginated list of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of items to return per page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
