---
title: "digitalocean_vpc"
description: "`data_digitalocean_vpc` lists the real, already-created VPCs on the account, paginated and filterable by the type of resource they're intended to hold."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpcs "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/vpcs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_vpc"})

    		ubx.Data(vpcs.Vpc, "example", vpcs.VpcConfig{
    			VpcId:        "example",
    			Page:         1,
    			PerPage:      1,
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Vpc } from "@ubx/sdk-digitalocean/digitalocean/data/vpcs/vpc";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_vpc" });

      data(Vpc, "example", {
        vpcId: "example",
        page: 1,
        perPage: 1,
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.vpcs import Vpc, VpcConfig

    def describe():
        ubx.intent("look up digitalocean_vpc")

        ubx.data(Vpc, "example", VpcConfig(
            vpc_id="example",
            page=1,
            per_page=1,
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  The page number to return, for a paginated list of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of items to return per page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Filters VPCs by the real type of resource they're intended to hold. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  The ID of the VPC to look up. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
