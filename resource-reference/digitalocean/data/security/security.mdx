---
title: "digitalocean_security"
description: "`data_digitalocean_security` lists the real, already-run security scans across the account, paginated."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_security"})

    		ubx.Data(security.Security, "example", security.SecurityConfig{
    			Page:    1,
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Security } from "@ubx/sdk-digitalocean/digitalocean/data/security/security";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_security" });

      data(Security, "example", {
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.security import Security, SecurityConfig

    def describe():
        ubx.intent("look up digitalocean_security")

        ubx.data(Security, "example", SecurityConfig(
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  The page number to return, for a paginated list of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of items to return per page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
