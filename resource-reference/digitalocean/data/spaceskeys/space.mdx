---
title: "digitalocean_space"
description: "`data_digitalocean_space` lists the real, already-created Spaces object storage buckets on the account, paginated and sortable."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	spaceskeys "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/spaceskeys"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_space"})

    		ubx.Data(spaceskeys.Space, "example", spaceskeys.SpaceConfig{
    			Name:          "example",
    			Bucket:        "example",
    			Page:          1,
    			PerPage:       1,
    			Permission:    "example",
    			Sort:          "example",
    			SortDirection: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Space } from "@ubx/sdk-digitalocean/digitalocean/data/spaceskeys/space";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_space" });

      data(Space, "example", {
        name: "example",
        bucket: "example",
        page: 1,
        perPage: 1,
        permission: "example",
        sort: "example",
        sortDirection: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.spaceskeys import Space, SpaceConfig

    def describe():
        ubx.intent("look up digitalocean_space")

        ubx.data(Space, "example", SpaceConfig(
            name="example",
            bucket="example",
            page=1,
            per_page=1,
            permission="example",
            sort="example",
            sort_direction="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string">
  The real, exact name of the Spaces bucket to look up. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  Filters Spaces buckets by their real, exact name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number to return, for a paginated list of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of items to return per page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="permission" type="string">
  Filters Spaces buckets by the real, current access permission. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The real field to sort Spaces buckets by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_direction" type="string">
  The direction to sort results in, ascending or descending. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
