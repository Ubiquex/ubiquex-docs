---
title: "digitalocean_nfs_snapshot_response"
description: "`data_digitalocean_nfs_snapshot_response` lists the real, already-created snapshots for a specific NFS share."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	nfs "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/nfs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_nfs_snapshot_response"})

    		ubx.Data(nfs.SnapshotResponse, "example", nfs.SnapshotResponseConfig{
    			Region:  "example",
    			ShareId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SnapshotResponse } from "@ubx/sdk-digitalocean/digitalocean/data/nfs/snapshot_response";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_nfs_snapshot_response" });

      data(SnapshotResponse, "example", {
        region: "example",
        shareId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.nfs import SnapshotResponse, SnapshotResponseConfig

    def describe():
        ubx.intent("look up digitalocean_nfs_snapshot_response")

        ubx.data(SnapshotResponse, "example", SnapshotResponseConfig(
            region="example",
            share_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="region" type="string">
  The real region the NFS share belongs to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="share_id" type="string">
  The ID of the NFS share these snapshots belong to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="snapshots" type="list(object)">
  The list of real, already-created snapshots for this share. **(AI-inferred)** Returned by the lookup.
</ResponseField>
