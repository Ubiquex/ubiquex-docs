---
title: "digitalocean_nfs_response"
description: "`data_digitalocean_nfs_response` lists the real, already-provisioned NFS shares in a given region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	nfs "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/nfs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_nfs_response"})

    		ubx.Data(nfs.Response, "example", nfs.ResponseConfig{
    			Region: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Response } from "@ubx/sdk-digitalocean/digitalocean/data/nfs/response";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_nfs_response" });

      data(Response, "example", {
        region: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.nfs import Response, ResponseConfig

    def describe():
        ubx.intent("look up digitalocean_nfs_response")

        ubx.data(Response, "example", ResponseConfig(
            region="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="region" type="string">
  Filters NFS shares by real, current region. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="shares" type="list(object)">
  The list of real, already-provisioned NFS shares. **(AI-inferred)** Returned by the lookup.
</ResponseField>
