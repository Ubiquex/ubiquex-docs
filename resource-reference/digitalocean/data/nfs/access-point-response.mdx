---
title: "digitalocean_access_point_response"
description: "`data_digitalocean_access_point_response` lists the real, already-created access points for a given NFS share, with their real, current status."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	nfs "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/nfs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_access_point_response"})

    		ubx.Data(nfs.AccessPointResponse, "example", nfs.AccessPointResponseConfig{
    			ShareId: "example",
    			Status:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessPointResponse } from "@ubx/sdk-digitalocean/digitalocean/data/nfs/access_point_response";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_access_point_response" });

      data(AccessPointResponse, "example", {
        shareId: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.nfs import AccessPointResponse, AccessPointResponseConfig

    def describe():
        ubx.intent("look up digitalocean_access_point_response")

        ubx.data(AccessPointResponse, "example", AccessPointResponseConfig(
            share_id="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="share_id" type="string" required>
  The ID of the NFS share these access points belong to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The current status of the access point listing. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_points" type="list(object)">
  Array of access point objects. Returned by the lookup.
</ResponseField>
