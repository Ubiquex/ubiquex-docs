---
title: "digitalocean_destination_omit_credentials"
description: "`data_digitalocean_destination_omit_credentials` lists the real, already-configured log/metrics sink destinations for a managed database cluster..."
---

`data_digitalocean_destination_omit_credentials` lists the real, already-configured log/metrics sink destinations for a managed database cluster, with credentials redacted.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_destination_omit_credentials"})

    		ubx.Data(monitoring.DestinationOmitCredentials, "example", monitoring.DestinationOmitCredentialsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DestinationOmitCredentials } from "@ubx/sdk-digitalocean/digitalocean/data/monitoring/destination_omit_credentials";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_destination_omit_credentials" });

      data(DestinationOmitCredentials, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.monitoring import DestinationOmitCredentials, DestinationOmitCredentialsConfig

    def describe():
        ubx.intent("look up digitalocean_destination_omit_credentials")

        ubx.data(DestinationOmitCredentials, "example", DestinationOmitCredentialsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="destinations" type="list(object)">
  The list of real, already-configured sink destinations. **(AI-inferred)** Returned by the lookup.
</ResponseField>
