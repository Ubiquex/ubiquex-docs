---
title: "digitalocean_namespace"
description: "`data_digitalocean_namespace` looks up a real, already-created Functions namespace by its own ID."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	functions "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/functions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_namespace"})

    		ubx.Data(functions.Namespace, "example", functions.NamespaceConfig{
    			NamespaceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Namespace } from "@ubx/sdk-digitalocean/digitalocean/data/functions/namespace";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_namespace" });

      data(Namespace, "example", {
        namespaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.functions import Namespace, NamespaceConfig

    def describe():
        ubx.intent("look up digitalocean_namespace")

        ubx.data(Namespace, "example", NamespaceConfig(
            namespace_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="namespace_id" type="string" required>
  The ID of the Functions namespace to look up. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
