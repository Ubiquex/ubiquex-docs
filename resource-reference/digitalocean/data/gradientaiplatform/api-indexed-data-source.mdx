---
title: "digitalocean_api_indexed_data_source"
description: "`data_digitalocean_api_indexed_data_source` lists the real data sources a specific knowledge base indexing job actually indexed."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gradientaiplatform "github.com/ubiquex/ubx-sdk-digitalocean/sdk/go/digitalocean/data/gradientaiplatform"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up digitalocean_api_indexed_data_source"})

    		ubx.Data(gradientaiplatform.ApiIndexedDataSource, "example", gradientaiplatform.ApiIndexedDataSourceConfig{
    			IndexingJobUuid: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIndexedDataSource } from "@ubx/sdk-digitalocean/digitalocean/data/gradientaiplatform/api_indexed_data_source";

    export default stack("example", () => {
      intent({ summary: "look up digitalocean_api_indexed_data_source" });

      data(ApiIndexedDataSource, "example", {
        indexingJobUuid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.digitalocean.data.gradientaiplatform import ApiIndexedDataSource, ApiIndexedDataSourceConfig

    def describe():
        ubx.intent("look up digitalocean_api_indexed_data_source")

        ubx.data(ApiIndexedDataSource, "example", ApiIndexedDataSourceConfig(
            indexing_job_uuid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="indexing_job_uuid" type="string" required>
  The UUID of the indexing job these data sources belong to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="indexed_data_sources" type="list(object)">
  The list of real data sources this indexing job actually indexed. **(AI-inferred)** Returned by the lookup.
</ResponseField>
