---
title: "azurerm_kubernetes_fleet_member"
description: "Real, generated bindings for `azurerm_kubernetes_fleet_member`."
---

`azurerm_kubernetes_fleet_member` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kubernetes "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/kubernetes"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own fleet member"})

    		ubx.Resource(kubernetes.FleetMember, "ci-runner", kubernetes.FleetMemberConfig{
    			KubernetesClusterId: "example",
    			KubernetesFleetId:   "example",
    			Name:                "ci-runner",
    			Group:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FleetMember } from "jsr:@ubx/sdk-azure/azurerm/kubernetes/fleet_member";

    export default stack("payments", () => {
      intent({ summary: "payments own fleet member" });

      resource(FleetMember, "ci-runner", {
        kubernetesClusterId: "example",
        kubernetesFleetId: "example",
        name: "ci-runner",
        group: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.kubernetes import FleetMember, FleetMemberConfig

    def describe():
        ubx.intent("payments own fleet member")

        ubx.resource(FleetMember, "ci-runner", FleetMemberConfig(
            kubernetes_cluster_id="example",
            kubernetes_fleet_id="example",
            name="ci-runner",
            group="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_kubernetes_fleet_member called "ci-runner", with
    kubernetes cluster id example, kubernetes fleet id example, group
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="group" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="kubernetes_cluster_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kubernetes_fleet_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
