---
title: "azure_mongocluster_firewall_rule"
description: "`azure_mongocluster_mongocluster_firewall_rule` allows a specific IP address range to connect to an Azure Cosmos DB for MongoDB (vCore) cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mongocluster "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/mongocluster"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own mongocluster firewall rule"})

    		ubx.Resource(mongocluster.FirewallRule, "example", mongocluster.FirewallRuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirewallRule } from "@ubx/sdk-azure/azure/mongocluster/firewall_rule";

    export default stack("example", () => {
      intent({ summary: "example own mongocluster firewall rule" });

      resource(FirewallRule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.mongocluster import FirewallRule, FirewallRuleConfig

    def describe():
        ubx.intent("example own mongocluster firewall rule")

        ubx.resource(FirewallRule, "example", FirewallRuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_mongocluster_firewall_rule, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of a mongo cluster firewall rule. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="end_ip_address" type="string" required>
      The end IP address of the mongo cluster firewall rule. Must be IPv4 format. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the last accepted operation. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="start_ip_address" type="string" required>
      The start IP address of the mongo cluster firewall rule. Must be IPv4 format. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
