---
title: "azure_mongocluster_mongocluster_user"
description: "`azure_mongocluster_mongocluster_user` is a database user on an Azure Cosmos DB for MongoDB (vCore) cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_mongocluster_mongocluster --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_mongocluster_mongocluster/sdk/go
    package main

    import (
    	mongocluster "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_mongocluster_mongocluster/mongocluster"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own mongocluster user"})

    		ubx.Resource(mongocluster.MongoclusterUser, "ci-runner", mongocluster.MongoclusterUserConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_mongocluster_mongocluster --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MongoclusterUser } from "./local-sdk/azure_mongocluster_mongocluster/sdk/typescript/azure_mongocluster_mongocluster/mongocluster/mongocluster_user.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own mongocluster user" });

      resource(MongoclusterUser, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_mongocluster_mongocluster --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_mongocluster_mongocluster/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_mongocluster_mongocluster.mongocluster import MongoclusterUser, MongoclusterUserConfig

    def describe():
        ubx.intent("payments own mongocluster user")

        ubx.resource(MongoclusterUser, "ci-runner", MongoclusterUserConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_mongocluster_mongocluster_user, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Definition of Mongo user resource on a cluster. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="identity_provider" type="object">
      Defines a user's identity provider definition. Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string" required>
          Identity provider types that a a user identity can belong to. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the last accepted operation. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="roles" type="list(object)">
      Database roles that are assigned to the user. Optional.
      <Expandable title="properties">
        <ResponseField name="db" type="string">
          A reference to the database this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="role" type="string">
          The role granted by this assignment. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
