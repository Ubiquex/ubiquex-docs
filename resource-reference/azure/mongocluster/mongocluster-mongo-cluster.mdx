---
title: "azure_mongocluster_mongocluster_mongo_cluster"
description: "`azure_mongocluster_mongocluster_mongo_cluster` is an Azure Cosmos DB for MongoDB (vCore) cluster..."
---

`azure_mongocluster_mongocluster_mongo_cluster` is an Azure Cosmos DB for MongoDB (vCore) cluster, a fully managed deployment offering native MongoDB wire-protocol compatibility.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_mongocluster_mongocluster --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_mongocluster_mongocluster/sdk/go
    package main

    import (
    	mongocluster "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_mongocluster_mongocluster/mongocluster"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own mongocluster mongo cluster"})

    		ubx.Resource(mongocluster.MongoclusterMongoCluster, "ci-runner", mongocluster.MongoclusterMongoClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_mongocluster_mongocluster --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MongoclusterMongoCluster } from "./local-sdk/azure_mongocluster_mongocluster/sdk/typescript/azure_mongocluster_mongocluster/mongocluster/mongocluster_mongo_cluster.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own mongocluster mongo cluster" });

      resource(MongoclusterMongoCluster, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_mongocluster_mongocluster --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_mongocluster_mongocluster/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_mongocluster_mongocluster.mongocluster import MongoclusterMongoCluster, MongoclusterMongoClusterConfig

    def describe():
        ubx.intent("payments own mongocluster mongo cluster")

        ubx.resource(MongoclusterMongoCluster, "ci-runner", MongoclusterMongoClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_mongocluster_mongocluster_mongo_cluster, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of a mongo cluster. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="administrator" type="object">
      The local administrator login properties. Optional.
      <Expandable title="properties">
        <ResponseField name="password" type="string">
          The administrator password. Optional.
        </ResponseField>
        <ResponseField name="user_name" type="string">
          The administrator user name. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="auth_config" type="object">
      The authentication configuration for the Mongo cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="allowed_modes" type="list(string)">
          Allowed authentication modes for data access on the cluster. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="backup" type="object">
      The backup properties of the cluster. This includes the earliest restore time and retention settings. Optional.
      <Expandable title="properties">
        <ResponseField name="earliest_restore_time" type="string">
          Earliest restore timestamp in UTC ISO8601 format. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cluster_status" type="string">
      The status of the Mongo cluster resource. Optional.
    </ResponseField>
    <ResponseField name="compute" type="object">
      The compute properties of the cluster. This includes the virtual-cores/memory and scaling options applied to servers in the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="tier" type="string">
          The compute tier to assign to the cluster, where each tier maps to a virtual-core and memory size. Example values: 'M30', 'M40'. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="connection_string" type="string">
      The default mongo connection string for the cluster. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="create_mode" type="string">
      The mode that the Mongo Cluster is created with. Optional.
    </ResponseField>
    <ResponseField name="data_api" type="object">
      Data API properties. Optional.
      <Expandable title="properties">
        <ResponseField name="mode" type="string">
          The mode to apply to the Mongo Data API. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="encryption" type="object">
      The encryption configuration for the mongo cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="customer_managed_key_encryption" type="object">
          Customer managed key encryption settings. Optional.
          <Expandable title="properties">
            <ResponseField name="key_encryption_key_identity" type="object">
              The identity used for key encryption key. Optional.
              <Expandable title="properties">
                <ResponseField name="identity_type" type="string">
                  The type of identity for key encryption key. Optional.
                </ResponseField>
                <ResponseField name="user_assigned_identity_resource_id" type="string">
                  The user assigned identity resource id. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="key_encryption_key_url" type="string">
              The URI of the key vault key used for encryption. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="high_availability" type="object">
      The high availability properties of the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="target_mode" type="string">
          The high availability modes for a cluster. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="infrastructure_version" type="string">
      The infrastructure version the cluster is provisioned on. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="network_bypass_mode" type="string">
      The network bypass mode for the Mongo cluster. Optional.
    </ResponseField>
    <ResponseField name="preview_features" type="list(string)">
      List of private endpoint connections. Optional.
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      List of private endpoint connections. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the last accepted operation. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Whether or not public endpoint access is allowed for this Mongo cluster. Value is optional and default value is 'Enabled' Optional.
    </ResponseField>
    <ResponseField name="replica" type="object">
      Replica properties of the mongo cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="replication_state" type="string">
          The state of the replication link between the replica and source cluster. Optional.
        </ResponseField>
        <ResponseField name="role" type="string">
          Replication role of the mongo cluster. Optional.
        </ResponseField>
        <ResponseField name="source_resource_id" type="string">
          Identifier for a mongo cluster resource. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="replica_parameters" type="object">
      Parameters used for replica operations. Optional.
      <Expandable title="properties">
        <ResponseField name="source_location" type="string" required>
          Represents an Azure geography region where supported resource providers live. Required.
        </ResponseField>
        <ResponseField name="source_resource_id" type="string" required>
          Identifier for a mongo cluster resource. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="restore_parameters" type="object">
      Parameters used for restore operations Optional.
      <Expandable title="properties">
        <ResponseField name="point_in_time_utc" type="string">
          UTC point in time to restore a mongo cluster Optional.
        </ResponseField>
        <ResponseField name="source_resource_id" type="string">
          Identifier for a mongo cluster resource. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="server_version" type="string">
      The Mongo DB server version. Defaults to the latest available version if not specified. Optional.
    </ResponseField>
    <ResponseField name="sharding" type="object">
      The sharding properties of the cluster. This includes the shard count and scaling options for the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="shard_count" type="number">
          Number of shards to provision on the cluster. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="storage" type="object">
      The storage properties of the cluster. This includes the data storage size and scaling applied to servers in the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="size_gb" type="number">
          The size of the data disk assigned to each server. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of storage that a mongo cluster can be provisioned with. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
