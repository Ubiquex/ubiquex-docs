---
title: "azure_privatedns_private_zone"
description: "`azure_privatedns_privatedns_private_zone` is an Azure Private DNS zone..."
---

`azure_privatedns_privatedns_private_zone` is an Azure Private DNS zone, resolvable only from virtual network(s) it's linked to rather than the public internet.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_privatedns_privatedns --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_privatedns_privatedns/sdk/go
    package main

    import (
    	privatedns "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_privatedns_privatedns/privatedns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own privatedns private zone"})

    		ubx.Resource(privatedns.PrivatednsPrivateZone, "example", privatedns.PrivatednsPrivateZoneConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_privatedns_privatedns --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivatednsPrivateZone } from "./local-sdk/azure_privatedns_privatedns/sdk/typescript/azure_privatedns_privatedns/privatedns/privatedns_private_zone.ts";

    export default stack("example", () => {
      intent({ summary: "example own privatedns private zone" });

      resource(PrivatednsPrivateZone, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_privatedns_privatedns --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_privatedns_privatedns/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_privatedns_privatedns.privatedns import PrivatednsPrivateZone, PrivatednsPrivateZoneConfig

    def describe():
        ubx.intent("example own privatedns private zone")

        ubx.resource(PrivatednsPrivateZone, "example", PrivatednsPrivateZoneConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_privatedns_private_zone, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  The ETag of the zone. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="location" type="string">
  The Azure Region where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Represents the properties of the Private DNS zone. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="internal_id" type="string">
      Private zone internal Id Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="max_number_of_record_sets" type="number">
      The maximum number of record sets that can be created in this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="max_number_of_virtual_network_links" type="number">
      The maximum number of virtual networks that can be linked to this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="max_number_of_virtual_network_links_with_registration" type="number">
      The maximum number of virtual networks that can be linked to this Private DNS zone with registration enabled. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="number_of_record_sets" type="number">
      The current number of record sets in this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="number_of_virtual_network_links" type="number">
      The current number of virtual networks that are linked to this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="number_of_virtual_network_links_with_registration" type="number">
      The current number of virtual networks that are linked to this Private DNS zone with registration enabled. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the resource. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
