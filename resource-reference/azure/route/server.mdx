---
title: "azurerm_route_server"
description: "Real, generated bindings for `azurerm_route_server`."
---

`azurerm_route_server` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/route"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own server"})

    		ubx.Resource(route.Server, "ci-runner", route.ServerConfig{
    			Location:                     "example",
    			Name:                         "ci-runner",
    			PublicIpAddressId:            "example",
    			ResourceGroupName:            "example-resource-group-name",
    			Sku:                          "example",
    			SubnetId:                     "example",
    			BranchToBranchTrafficEnabled: true,
    			HubRoutingPreference:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Server } from "jsr:@ubx/sdk-azure/azurerm/route/server";

    export default stack("payments", () => {
      intent({ summary: "payments own server" });

      resource(Server, "ci-runner", {
        location: "example",
        name: "ci-runner",
        publicIpAddressId: "example",
        resourceGroupName: "example-resource-group-name",
        sku: "example",
        subnetId: "example",
        branchToBranchTrafficEnabled: true,
        hubRoutingPreference: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.route import Server, ServerConfig

    def describe():
        ubx.intent("payments own server")

        ubx.resource(Server, "ci-runner", ServerConfig(
            location="example",
            name="ci-runner",
            public_ip_address_id="example",
            resource_group_name="example-resource-group-name",
            sku="example",
            subnet_id="example",
            branch_to_branch_traffic_enabled=True,
            hub_routing_preference="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_route_server called "ci-runner", with location
    example, public ip address id example, resource group name
    example-resource-group-name, sku example, subnet id example,
    branch to branch traffic enabled true, hub routing preference
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="branch_to_branch_traffic_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="hub_routing_preference" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="public_ip_address_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="sku" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="routing_state" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="virtual_router_asn" type="number">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="virtual_router_ips" type="set(string)">
  Computed by Azure after `ship`.
</ResponseField>
