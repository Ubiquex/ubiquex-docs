---
title: "azure_ssh_public_key_resource"
description: "A complete, runnable azure_ssh_public_key_resource program, in every SDK language."
---

`azure_ssh_public_key_resource` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	ssh "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/ssh"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own public key resource"})

    		ubx.Resource(ssh.PublicKeyResource, "ci-runner", ssh.PublicKeyResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PublicKeyResource } from "./local-sdk/azure/sdk/typescript/azure/ssh/public_key_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own public key resource" });

      resource(PublicKeyResource, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.ssh import PublicKeyResource, PublicKeyResourceConfig

    def describe():
        ubx.intent("payments own public key resource")

        ubx.resource(PublicKeyResource, "ci-runner", PublicKeyResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_ssh_public_key_resource, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object">
  Properties of the SSH public key. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="public_key" type="string">
      SSH public key used to authenticate to a virtual machine through ssh. If this property is not initially provided when the resource is created, the publicKey property will be populated when generateKeyPair is called. If the public key is provided upon resource creation, the provided public key needs to be at least 2048-bit and in ssh-rsa format. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="properties" type="object">
  Properties of the SSH public key. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="public_key" type="string">
      SSH public key used to authenticate to a virtual machine through ssh. If this property is not initially provided when the resource is created, the publicKey property will be populated when generateKeyPair is called. If the public key is provided upon resource creation, the provided public key needs to be at least 2048-bit and in ssh-rsa format. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
