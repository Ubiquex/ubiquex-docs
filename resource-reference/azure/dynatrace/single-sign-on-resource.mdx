---
title: "azure_dynatrace_single_sign_on_resource"
description: "A complete, runnable azure_dynatrace_single_sign_on_resource program, in every SDK language."
---

`azure_dynatrace_single_sign_on_resource` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_dynatrace --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_dynatrace/sdk/go
    package main

    import (
    	dynatrace "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_dynatrace/dynatrace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own single sign on resource"})

    		ubx.Resource(dynatrace.SingleSignOnResource, "ci-runner", dynatrace.SingleSignOnResourceConfig{
    			Properties: map[string]any{"aad_domains": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_dynatrace --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SingleSignOnResource } from "./local-sdk/azure_dynatrace/sdk/typescript/azure_dynatrace/dynatrace/single_sign_on_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own single sign on resource" });

      resource(SingleSignOnResource, "ci-runner", {
        properties: { aadDomains: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_dynatrace --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_dynatrace/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_dynatrace.dynatrace import SingleSignOnResource, SingleSignOnResourceConfig

    def describe():
        ubx.intent("payments own single sign on resource")

        ubx.resource(SingleSignOnResource, "ci-runner", SingleSignOnResourceConfig(
            properties={"aad_domains": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dynatrace_single_sign_on_resource, with properties a
    set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  The details of a Dynatrace single sign-on. Required.
  <Expandable title="properties">
    <ResponseField name="aad_domains" type="list(string)">
      array of Aad(azure active directory) domains Optional.
    </ResponseField>
    <ResponseField name="enterprise_app_id" type="string">
      Version of the Dynatrace agent installed on the VM. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the monitoring resource Optional.
    </ResponseField>
    <ResponseField name="single_sign_on_state" type="string">
      Various states of the SSO resource Optional.
    </ResponseField>
    <ResponseField name="single_sign_on_url" type="string">
      The login URL specific to this Dynatrace Environment Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
