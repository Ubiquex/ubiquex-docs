---
title: "azure_dynatrace_single_sign_on_resource"
description: "A complete, runnable azure_dynatrace_single_sign_on_resource program, in every SDK language."
---

`azure_dynatrace_single_sign_on_resource` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dynatrace "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/dynatrace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own single sign on resource"})

    		ubx.Resource(dynatrace.SingleSignOnResource, "example", dynatrace.SingleSignOnResourceConfig{
    			Properties: map[string]any{"aad_domains": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SingleSignOnResource } from "@ubx/sdk-azure/azure/dynatrace/single_sign_on_resource";

    export default stack("example", () => {
      intent({ summary: "example own single sign on resource" });

      resource(SingleSignOnResource, "example", {
        properties: { aadDomains: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.dynatrace import SingleSignOnResource, SingleSignOnResourceConfig

    def describe():
        ubx.intent("example own single sign on resource")

        ubx.resource(SingleSignOnResource, "example", SingleSignOnResourceConfig(
            properties={"aad_domains": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dynatrace_single_sign_on_resource, with properties a
    set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  The details of a Dynatrace single sign-on. Required.
  <Expandable title="properties">
    <ResponseField name="aad_domains" type="list(string)">
      array of Aad(azure active directory) domains Optional.
    </ResponseField>
    <ResponseField name="enterprise_app_id" type="string">
      Version of the Dynatrace agent installed on the VM. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the monitoring resource Optional.
    </ResponseField>
    <ResponseField name="single_sign_on_state" type="string">
      Various states of the SSO resource Optional.
    </ResponseField>
    <ResponseField name="single_sign_on_url" type="string">
      The login URL specific to this Dynatrace Environment Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
