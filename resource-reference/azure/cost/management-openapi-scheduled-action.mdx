---
title: "azure_cost_management_openapi_scheduled_action"
description: "`azure_cost_management_openapi_scheduled_action` schedules a Cost Management view or alert to be emailed to recipient(s) automatically on a recurring..."
---

`azure_cost_management_openapi_scheduled_action` schedules a Cost Management view or alert to be emailed to recipient(s) automatically on a recurring basis.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cost_management_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cost_management_openapi/sdk/go
    package main

    import (
    	cost "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cost_management_openapi/cost"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own management openapi scheduled action"})

    		ubx.Resource(cost.ManagementOpenapiScheduledAction, "example", cost.ManagementOpenapiScheduledActionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cost_management_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagementOpenapiScheduledAction } from "./local-sdk/azure_cost_management_openapi/sdk/typescript/azure_cost_management_openapi/cost/management_openapi_scheduled_action.ts";

    export default stack("example", () => {
      intent({ summary: "example own management openapi scheduled action" });

      resource(ManagementOpenapiScheduledAction, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cost_management_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cost_management_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cost_management_openapi.cost import ManagementOpenapiScheduledAction, ManagementOpenapiScheduledActionConfig

    def describe():
        ubx.intent("example own management openapi scheduled action")

        ubx.resource(ManagementOpenapiScheduledAction, "example", ManagementOpenapiScheduledActionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cost_management_openapi_scheduled_action called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  The ETag (or entity tag) HTTP response header is an identifier for a specific version of a resource. It lets caches be more efficient and save bandwidth, as a web server does not need to resend a full response if the content was not changed. It is a string of ASCII characters placed between double quotes, like "675af34563dc-tr34". Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind of the scheduled action. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the scheduled action. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string" required>
      Scheduled action name. Required.
    </ResponseField>
    <ResponseField name="file_destination" type="object">
      Destination of the view data. This is optional. Currently only CSV format is supported. Optional.
      <Expandable title="properties">
        <ResponseField name="file_formats" type="list(string)">
          Destination of the view data. Currently only CSV format is supported. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="notification" type="object" required>
      The properties of the scheduled action notification. Required.
      <Expandable title="properties">
        <ResponseField name="language" type="string">
          Locale of the email. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          Optional message to be added in the email. Length is limited to 250 characters. Optional.
        </ResponseField>
        <ResponseField name="regional_format" type="string">
          Regional format used for formatting date/time and currency values in the email. Optional.
        </ResponseField>
        <ResponseField name="subject" type="string" required>
          Subject of the email. Length is limited to 70 characters. Required.
        </ResponseField>
        <ResponseField name="to" type="list(string)" required>
          Array of email addresses. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="notification_email" type="string">
      Email address of the point of contact that should get the unsubscribe requests and notification emails. Optional.
    </ResponseField>
    <ResponseField name="schedule" type="object" required>
      The properties of the schedule. Required.
      <Expandable title="properties">
        <ResponseField name="day_of_month" type="number">
          UTC day on which cost analysis data will be emailed. Must be between 1 and 31. This property is applicable when frequency is Monthly and overrides weeksOfMonth or daysOfWeek. Optional.
        </ResponseField>
        <ResponseField name="days_of_week" type="list(string)">
          Day names in english on which cost analysis data will be emailed. This property is applicable when frequency is Weekly or Monthly. Optional.
        </ResponseField>
        <ResponseField name="end_date" type="string" required>
          The end date and time of the scheduled action (UTC). Required.
        </ResponseField>
        <ResponseField name="frequency" type="string" required>
          Frequency of the schedule. Required.
        </ResponseField>
        <ResponseField name="hour_of_day" type="number">
          UTC time at which cost analysis data will be emailed. Optional.
        </ResponseField>
        <ResponseField name="start_date" type="string" required>
          The start date and time of the scheduled action (UTC). Required.
        </ResponseField>
        <ResponseField name="weeks_of_month" type="list(string)">
          Weeks in which cost analysis data will be emailed. This property is applicable when frequency is Monthly and used in combination with daysOfWeek. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scope" type="string">
      For private scheduled action(Create or Update), scope will be empty.&lt;br /&gt; For shared scheduled action(Create or Update By Scope), Cost Management scope can be 'subscriptions/&#123;subscriptionId&#125;' for subscription scope, 'subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;' for resourceGroup scope, 'providers/Microsoft.Billing/billingAccounts/&#123;billingAccountId&#125;' for Billing Account scope, 'providers/Microsoft.Billing/billingAccounts/&#123;billingAccountId&#125;/departments/&#123;departmentId&#125;' for Department scope, 'providers/Microsoft.Billing/billingAccounts/&#123;billingAccountId&#125;/enrollmentAccounts/&#123;enrollmentAccountId&#125;' for EnrollmentAccount scope, 'providers/Microsoft.Billing/billingAccounts/&#123;billingAccountId&#125;/billingProfiles/&#123;billingProfileId&#125;' for BillingProfile scope, 'providers/Microsoft.Billing/billingAccounts/&#123;billingAccountId&#125;/invoiceSections/&#123;invoiceSectionId&#125;' for InvoiceSection scope, '/providers/Microsoft.CostManagement/externalBillingAccounts/&#123;externalBillingAccountName&#125;' for ExternalBillingAccount scope, and '/providers/Microsoft.CostManagement/externalSubscriptions/&#123;externalSubscriptionName&#125;' for ExternalSubscription scope. Optional.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Status of the scheduled action. Required.
    </ResponseField>
    <ResponseField name="view_id" type="string" required>
      Cost analysis viewId used for scheduled action. For example, '/providers/Microsoft.CostManagement/views/swaggerExample' Required.
    </ResponseField>
  </Expandable>
</ResponseField>
