---
title: "azure_cost_management_openapi_export"
description: "`azure_cost_management_openapi_export` schedules Cost Management to automatically write cost and usage data to a Storage account on a recurring basis."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cost"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own management openapi export"})

    		ubx.Resource(cost.ManagementOpenapiExport, "example", cost.ManagementOpenapiExportConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagementOpenapiExport } from "@ubx/sdk-azure/azure/cost/management_openapi_export";

    export default stack("example", () => {
      intent({ summary: "example own management openapi export" });

      resource(ManagementOpenapiExport, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cost import ManagementOpenapiExport, ManagementOpenapiExportConfig

    def describe():
        ubx.intent("example own management openapi export")

        ubx.resource(ManagementOpenapiExport, "example", ManagementOpenapiExportConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cost_management_openapi_export, with scope example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  The ETag (or entity tag) HTTP response header is an identifier for a specific version of a resource. It lets caches be more efficient and save bandwidth, as a web server does not need to resend a full response if the content was not changed. It is a string of ASCII characters placed between double quotes, like "675af34563dc-tr34". Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="identity" type="object">
  Managed service identity (either system assigned, or none) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (either system assigned, or none). Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location" type="string">
  The location of the Export's managed identity. Only required when utilizing managed identity. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the export. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="schedule" type="object">
      The schedule associated with the export. Optional.
      <Expandable title="properties">
        <ResponseField name="recurrence" type="string">
          The schedule recurrence. Optional.
        </ResponseField>
        <ResponseField name="recurrence_period" type="object">
          The start and end date for recurrence schedule. Optional.
          <Expandable title="properties">
            <ResponseField name="from" type="string" required>
              The start date of recurrence. Required.
            </ResponseField>
            <ResponseField name="to" type="string">
              The end date of recurrence. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="status" type="string">
          The status of the export's schedule. If 'Inactive', the export's schedule is paused. To enable export set the status to be Active and then make a PUT request. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scope" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
