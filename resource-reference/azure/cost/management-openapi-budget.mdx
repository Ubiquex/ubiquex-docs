---
title: "azure_cost_management_openapi_budget"
description: "`azure_cost_management_openapi_budget` sets a spending threshold on a scope (subscription, resource group..."
---

`azure_cost_management_openapi_budget` sets a spending threshold on a scope (subscription, resource group, or management group) and triggers notification(s) when actual or forecasted spend crosses it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cost"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own management openapi budget"})

    		ubx.Resource(cost.ManagementOpenapiBudget, "example", cost.ManagementOpenapiBudgetConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagementOpenapiBudget } from "@ubx/sdk-azure/azure/cost/management_openapi_budget";

    export default stack("example", () => {
      intent({ summary: "example own management openapi budget" });

      resource(ManagementOpenapiBudget, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cost import ManagementOpenapiBudget, ManagementOpenapiBudgetConfig

    def describe():
        ubx.intent("example own management openapi budget")

        ubx.resource(ManagementOpenapiBudget, "example", ManagementOpenapiBudgetConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cost_management_openapi_budget, with scope example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  The ETag (or entity tag) HTTP response header is an identifier for a specific version of a resource. It lets caches be more efficient and save bandwidth, as a web server does not need to resend a full response if the content was not changed. It is a string of ASCII characters placed between double quotes, like "675af34563dc-tr34". Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the budget. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="amount" type="number">
      The total amount of cost to track with the budget. Supported for CategoryType(s): Cost. Required for CategoryType(s): Cost. Optional.
    </ResponseField>
    <ResponseField name="category" type="string" required>
      The category of the budget. - 'Cost' defines a Budget. - 'ReservationUtilization' defines a Reservation Utilization Alert Rule. Required.
    </ResponseField>
    <ResponseField name="current_spend" type="object">
      The current amount of cost which is being tracked for a budget. Supported for CategoryType(s): Cost. Optional.
      <Expandable title="properties">
        <ResponseField name="amount" type="number">
          The total amount of cost which is being tracked by the budget. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="unit" type="string">
          The unit of measure for the budget amount. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="filter" type="object">
      May be used to filter budgets by user-specified dimensions and/or tags. Supported for CategoryType(s): Cost, ReservationUtilization. Optional.
      <Expandable title="properties">
        <ResponseField name="and" type="list(object)">
          The logical "AND" expression. Must have at least 2 items. Supported for CategoryType(s): Cost. Optional.
          <Expandable title="properties">
            <ResponseField name="dimensions" type="object">
              The dimension(s) this data is grouped or filtered by. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  How this condition's own operands are compared. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="values" type="list(string)">
                  The value(s) making up this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="tags" type="object">
              User-defined key/value tags on this resource, usable for organizing and filtering resources across the subscription. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  How this condition's own operands are compared. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="values" type="list(string)">
                  The value(s) making up this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dimensions" type="object">
          The comparison expression to be used in the budgets. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string" required>
              The name of the column to use in comparison. Required.
            </ResponseField>
            <ResponseField name="operator" type="string" required>
              The operator to use for comparison. Required.
            </ResponseField>
            <ResponseField name="values" type="list(string)" required>
              Array of values to use for comparison Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tags" type="object">
          The comparison expression to be used in the budgets. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string" required>
              The name of the column to use in comparison. Required.
            </ResponseField>
            <ResponseField name="operator" type="string" required>
              The operator to use for comparison. Required.
            </ResponseField>
            <ResponseField name="values" type="list(string)" required>
              Array of values to use for comparison Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="forecast_spend" type="object">
      The forecasted cost which is being tracked for a budget. Supported for CategoryType(s): Cost. Optional.
      <Expandable title="properties">
        <ResponseField name="amount" type="number">
          The forecasted cost for the total time period which is being tracked by the budget. This value is only provided if the budget contains a forecast alert type. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="unit" type="string">
          The unit of measure for the budget amount. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="notifications" type="map(object)">
      Dictionary of notifications associated with the budget. Supported for CategoryType(s): Cost, ReservationUtilization. - Constraints for **CategoryType: Cost** - Budget can have up to 5 notifications with thresholdType: Actual and 5 notifications with thresholdType: Forecasted. - Constraints for **CategoryType: ReservationUtilization** - Only one notification allowed. thresholdType is not applicable. Optional.
      <Expandable title="properties">
        <ResponseField name="contact_emails" type="list(string)">
          The email address(es) notified for this alert. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="contact_groups" type="list(string)">
          The Action Group(s) notified for this alert. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="contact_roles" type="list(string)">
          The Azure RBAC role(s) whose own members are notified for this alert. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Whether this feature is turned on. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="frequency" type="string">
          How often this schedule recurs. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="locale" type="string">
          The locale this content is expressed in. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operator" type="string">
          How this condition's own operands are compared. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="threshold" type="number">
          The value that must be crossed for this condition to trigger. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="threshold_type" type="string">
          The kind of threshold this represents, e.g. actual or forecasted cost. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="time_grain" type="string" required>
      The time covered by a budget. Tracking of the amount will be reset based on the time grain. Supported for CategoryType(s): Cost, ReservationUtilization. Supported timeGrainTypes for **CategoryType: Cost** - Monthly - Quarterly - Annually - BillingMonth* - BillingQuarter* - BillingAnnual* *only supported for Web Direct customers. Supported timeGrainTypes for **CategoryType: ReservationUtilization** - Last7Days - Last30Days Required for CategoryType(s): Cost, ReservationUtilization. Required.
    </ResponseField>
    <ResponseField name="time_period" type="object" required>
      The time period that defines the active period of the budget. The budget will evaluate data on or after the startDate and will expire on the endDate. Supported for CategoryType(s): Cost, ReservationUtilization. Required for CategoryType(s): Cost, ReservationUtilization. Required.
      <Expandable title="properties">
        <ResponseField name="end_date" type="string">
          The end date for the budget. - Constraints for **CategoryType: Cost** - No constraints. If not provided, we default this to 10 years from the start date. - Constraints for **CategoryType: ReservationUtilization** - End date cannot be more than 3 years after the start date. Optional.
        </ResponseField>
        <ResponseField name="start_date" type="string" required>
          The start date for the budget. - Constraints for **CategoryType: Cost** - Must be first of the month and should be less than the end date. Budget start date must be on or after June 1, 2017. Future start date should not be more than twelve months. Past start date should be selected within the timegrain period. - Constraints for **CategoryType: ReservationUtilization** - Must be on or after the current date and less than the end date. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scope" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
