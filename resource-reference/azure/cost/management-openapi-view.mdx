---
title: "azure_cost_management_openapi_view"
description: "`azure_cost_management_openapi_view` is a saved, reusable cost analysis view (chart type, grouping, filters) in Cost Management."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cost_management_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cost_management_openapi/sdk/go
    package main

    import (
    	cost "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cost_management_openapi/cost"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own management openapi view"})

    		ubx.Resource(cost.ManagementOpenapiView, "ci-runner", cost.ManagementOpenapiViewConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cost_management_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagementOpenapiView } from "./local-sdk/azure_cost_management_openapi/sdk/typescript/azure_cost_management_openapi/cost/management_openapi_view.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own management openapi view" });

      resource(ManagementOpenapiView, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cost_management_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cost_management_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cost_management_openapi.cost import ManagementOpenapiView, ManagementOpenapiViewConfig

    def describe():
        ubx.intent("payments own management openapi view")

        ubx.resource(ManagementOpenapiView, "ci-runner", ManagementOpenapiViewConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cost_management_openapi_view, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  The ETag (or entity tag) HTTP response header is an identifier for a specific version of a resource. It lets caches be more efficient and save bandwidth, as a web server does not need to resend a full response if the content was not changed. It is a string of ASCII characters placed between double quotes, like "675af34563dc-tr34". Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the view. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="accumulated" type="string">
      Show costs accumulated over time. Optional.
    </ResponseField>
    <ResponseField name="chart" type="string">
      Chart type of the main view in Cost Analysis. Required. Optional.
    </ResponseField>
    <ResponseField name="created_on" type="string">
      Date the user created this view. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="currency" type="string">
      Currency of the current view. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="date_range" type="string">
      Date range of the current view. Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      User input name of the view. Required. Optional.
    </ResponseField>
    <ResponseField name="kpis" type="list(object)">
      List of KPIs to show in Cost Analysis UI. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether this feature is turned on. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="metric" type="string">
      Metric to use when displaying costs. Optional.
    </ResponseField>
    <ResponseField name="modified_on" type="string">
      Date when the user last modified this view. Optional.
    </ResponseField>
    <ResponseField name="pivots" type="list(object)">
      Configuration of 3 sub-views in the Cost Analysis UI. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="query" type="object">
      The definition of a report config. Optional.
      <Expandable title="properties">
        <ResponseField name="data_set" type="object">
          The definition of data present in the report. Optional.
          <Expandable title="properties">
            <ResponseField name="aggregation" type="map(object)">
              Dictionary of aggregation expression to use in the report. The key of each item in the dictionary is the alias for the aggregated column. Report can have up to 2 aggregation clauses. Optional.
              <Expandable title="properties">
                <ResponseField name="function" type="string">
                  The aggregation function applied to this value. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="configuration" type="object">
              The configuration of dataset in the report. Optional.
              <Expandable title="properties">
                <ResponseField name="columns" type="list(string)">
                  Array of column names to be included in the report. Any valid report column name is allowed. If not provided, then report includes all columns. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="filter" type="object">
              The filter expression to be used in the report. Optional.
              <Expandable title="properties">
                <ResponseField name="and" type="list(object)">
                  The logical "AND" expression. Must have at least 2 items. Optional.
                </ResponseField>
                <ResponseField name="dimensions" type="object">
                  The comparison expression to be used in the report. Optional.
                  <Expandable title="properties">
                    <ResponseField name="name" type="string" required>
                      The name of the column to use in comparison. Required.
                    </ResponseField>
                    <ResponseField name="operator" type="string" required>
                      The operator to use for comparison. Required.
                    </ResponseField>
                    <ResponseField name="values" type="list(string)" required>
                      Array of values to use for comparison Required.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="or" type="list(object)">
                  The logical "OR" expression. Must have at least 2 items. Optional.
                </ResponseField>
                <ResponseField name="tags" type="object">
                  The comparison expression to be used in the report. Optional.
                  <Expandable title="properties">
                    <ResponseField name="name" type="string" required>
                      The name of the column to use in comparison. Required.
                    </ResponseField>
                    <ResponseField name="operator" type="string" required>
                      The operator to use for comparison. Required.
                    </ResponseField>
                    <ResponseField name="values" type="list(string)" required>
                      Array of values to use for comparison Required.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="granularity" type="string">
              The granularity of rows in the report. Optional.
            </ResponseField>
            <ResponseField name="grouping" type="list(object)">
              Array of group by expression to use in the report. Report can have up to 2 group by clauses. Optional.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="sorting" type="list(object)">
              Array of order by expression to use in the report. Optional.
              <Expandable title="properties">
                <ResponseField name="direction" type="string">
                  Whether this schedule is triggered by an increase or decrease. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="include_monetary_commitment" type="bool">
          If true, report includes monetary commitment. Optional.
        </ResponseField>
        <ResponseField name="time_period" type="object">
          The start and end date for pulling data for the report. Optional.
          <Expandable title="properties">
            <ResponseField name="from" type="string" required>
              The start date to pull data from. Required.
            </ResponseField>
            <ResponseField name="to" type="string" required>
              The end date to pull data to. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="timeframe" type="string" required>
          The time frame for pulling data for the report. If custom, then a specific time period must be provided. Required.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          The type of the report. Usage represents actual usage, forecast represents forecasted data and UsageAndForecast represents both usage and forecasted data. Actual usage and forecasted data can be differentiated based on dates. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scope" type="string">
      Cost Management scope to save the view on. This includes 'subscriptions/&#123;subscriptionId&#125;' for subscription scope, 'subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;' for resourceGroup scope, 'providers/Microsoft.Billing/billingAccounts/&#123;billingAccountId&#125;' for Billing Account scope, 'providers/Microsoft.Billing/billingAccounts/&#123;billingAccountId&#125;/departments/&#123;departmentId&#125;' for Department scope, 'providers/Microsoft.Billing/billingAccounts/&#123;billingAccountId&#125;/enrollmentAccounts/&#123;enrollmentAccountId&#125;' for EnrollmentAccount scope, 'providers/Microsoft.Billing/billingAccounts/&#123;billingAccountId&#125;/billingProfiles/&#123;billingProfileId&#125;' for BillingProfile scope, 'providers/Microsoft.Billing/billingAccounts/&#123;billingAccountId&#125;/invoiceSections/&#123;invoiceSectionId&#125;' for InvoiceSection scope, 'providers/Microsoft.Management/managementGroups/&#123;managementGroupId&#125;' for Management Group scope, '/providers/Microsoft.CostManagement/externalBillingAccounts/&#123;externalBillingAccountName&#125;' for ExternalBillingAccount scope, and '/providers/Microsoft.CostManagement/externalSubscriptions/&#123;externalSubscriptionName&#125;' for ExternalSubscription scope. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
