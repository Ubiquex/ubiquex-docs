---
title: "azure_attestation_private_endpoint_connection"
description: "`azure_attestation_private_endpoint_connection` represents the connection between a Private Endpoint and an Azure Attestation provider..."
---

`azure_attestation_private_endpoint_connection` represents the connection between a Private Endpoint and an Azure Attestation provider, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_attestation --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_attestation/sdk/go
    package main

    import (
    	attestation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_attestation/attestation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own attestation private endpoint connection"})

    		ubx.Resource(attestation.AttestationPrivateEndpointConnection, "example", attestation.AttestationPrivateEndpointConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_attestation --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AttestationPrivateEndpointConnection } from "./local-sdk/azure_attestation/sdk/typescript/azure_attestation/attestation/attestation_private_endpoint_connection.ts";

    export default stack("example", () => {
      intent({ summary: "example own attestation private endpoint connection" });

      resource(AttestationPrivateEndpointConnection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_attestation --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_attestation/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_attestation.attestation import AttestationPrivateEndpointConnection, AttestationPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("example own attestation private endpoint connection")

        ubx.resource(AttestationPrivateEndpointConnection, "example", AttestationPrivateEndpointConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_attestation_private_endpoint_connection, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of the PrivateEndpointConnectProperties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="private_endpoint" type="object">
      The Private Endpoint resource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The ARM identifier for Private Endpoint Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_link_service_connection_state" type="object" required>
      A collection of information about the state of the connection between service consumer and provider. Required.
      <Expandable title="properties">
        <ResponseField name="actions_required" type="string">
          A message indicating if changes on the service provider require any updates on the consumer. Optional.
        </ResponseField>
        <ResponseField name="description" type="string">
          The reason for approval/rejection of the connection. Optional.
        </ResponseField>
        <ResponseField name="status" type="string">
          The private endpoint connection status. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The current provisioning state. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
