---
title: "azure_attestation_provider"
description: "A complete, runnable azure_attestation_provider program, in every SDK language."
---

`azure_attestation_provider` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_attestation --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_attestation/sdk/go
    package main

    import (
    	attestation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_attestation/attestation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own provider"})

    		ubx.Resource(attestation.Provider, "ci-runner", attestation.ProviderConfig{
    			Location:   "example",
    			Properties: map[string]any{"policy_signing_certificates": map[string]any{"keys": []map[string]any{{"alg": "example"}}}},
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_attestation --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Provider } from "./local-sdk/azure_attestation/sdk/typescript/azure_attestation/attestation/provider.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own provider" });

      resource(Provider, "ci-runner", {
        location: "example",
        properties: { policySigningCertificates: { keys: [{ alg: "example" }] } },
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_attestation --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_attestation/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_attestation.attestation import Provider, ProviderConfig

    def describe():
        ubx.intent("payments own provider")

        ubx.resource(Provider, "ci-runner", ProviderConfig(
            location="example",
            properties={"policy_signing_certificates": {"keys": [{"alg": "example"}]}},
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_attestation_provider, with location example,
    properties a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string" required>
  The supported Azure location where the attestation provider should be created. Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Client supplied parameters used to create a new attestation provider. Required.
  <Expandable title="properties">
    <ResponseField name="policy_signing_certificates" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="keys" type="list(object)">
          The value of the "keys" parameter is an array of JWK values. By default, the order of the JWK values within the array does not imply an order of preference among them, although applications of JWK Sets can choose to assign a meaning to the order for their purposes, if desired. Optional.
          <Expandable title="properties">
            <ResponseField name="alg" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="crv" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="d" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="dp" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="dq" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="e" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="k" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="kid" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="kty" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="n" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="p" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="q" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="qi" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="use" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="x" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="x5c" type="list(string)">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="y" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Controls whether traffic from the public network is allowed to access the Attestation Provider APIs. Optional.
    </ResponseField>
    <ResponseField name="tpm_attestation_authentication" type="string">
      The setting that controls whether authentication is enabled or disabled for TPM Attestation REST APIs. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  The tags that will be assigned to the attestation provider. Optional.
</ResponseField>
