---
title: "azure_attestation_attestation_attestation_provider"
description: "`azure_attestation_attestation_attestation_provider` is an Azure Attestation provider..."
---

`azure_attestation_attestation_attestation_provider` is an Azure Attestation provider, verifying the trustworthiness of a platform (e.g. a confidential computing enclave or TPM-backed device) and issuing signed attestation token(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_attestation_attestation --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_attestation_attestation/sdk/go
    package main

    import (
    	attestation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_attestation_attestation/attestation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own attestation attestation provider"})

    		ubx.Resource(attestation.AttestationAttestationProvider, "ci-runner", attestation.AttestationAttestationProviderConfig{
    			Location:   "example",
    			Properties: map[string]any{"policy_signing_certificates": map[string]any{"keys": []map[string]any{{"alg": "example"}}}},
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_attestation_attestation --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AttestationAttestationProvider } from "./local-sdk/azure_attestation_attestation/sdk/typescript/azure_attestation_attestation/attestation/attestation_attestation_provider.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own attestation attestation provider" });

      resource(AttestationAttestationProvider, "ci-runner", {
        location: "example",
        properties: { policySigningCertificates: { keys: [{ alg: "example" }] } },
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_attestation_attestation --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_attestation_attestation/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_attestation_attestation.attestation import AttestationAttestationProvider, AttestationAttestationProviderConfig

    def describe():
        ubx.intent("payments own attestation attestation provider")

        ubx.resource(AttestationAttestationProvider, "ci-runner", AttestationAttestationProviderConfig(
            location="example",
            properties={"policy_signing_certificates": {"keys": [{"alg": "example"}]}},
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_attestation_attestation_attestation_provider, with
    location example, properties a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string" required>
  The supported Azure location where the attestation provider should be created. Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Client supplied parameters used to create a new attestation provider. Required.
  <Expandable title="properties">
    <ResponseField name="policy_signing_certificates" type="object">
      The JSON Web Key Set (JWKS) of certificates trusted to sign attestation policy updates for this provider -- only a policy signed by one of these keys is accepted. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="keys" type="list(object)">
          The value of the "keys" parameter is an array of JWK values. By default, the order of the JWK values within the array does not imply an order of preference among them, although applications of JWK Sets can choose to assign a meaning to the order for their purposes, if desired. Optional.
          <Expandable title="properties">
            <ResponseField name="alg" type="string">
              The cryptographic algorithm this key is used with, per the JWK spec (e.g. `RS256`). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="crv" type="string">
              The elliptic curve this key uses, per the JWK spec, when `kty` is `EC`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="d" type="string">
              The private exponent (or private key value) of this key, per the JWK spec -- only ever present on a private key. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="dp" type="string">
              The RSA CRT parameter `dP`, per the JWK spec -- present only on an RSA private key. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="dq" type="string">
              The RSA CRT parameter `dQ`, per the JWK spec -- present only on an RSA private key. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="e" type="string">
              The RSA public exponent, per the JWK spec. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="k" type="string">
              The symmetric key value, per the JWK spec, when `kty` is `oct`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kid" type="string">
              A key ID identifying this specific key within the key set, per the JWK spec. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kty" type="string">
              The key type, per the JWK spec, e.g. `RSA` or `EC`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="n" type="string">
              The RSA modulus, per the JWK spec. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="p" type="string">
              The RSA CRT parameter `p` (first prime factor), per the JWK spec -- present only on an RSA private key. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="q" type="string">
              The RSA CRT parameter `q` (second prime factor), per the JWK spec -- present only on an RSA private key. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="qi" type="string">
              The RSA CRT parameter `qInv`, per the JWK spec -- present only on an RSA private key. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="use" type="string">
              The intended use of this key, per the JWK spec, e.g. `sig` for signature verification. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="x" type="string">
              The elliptic curve X coordinate, per the JWK spec, when `kty` is `EC`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="x5c" type="list(string)">
              The X.509 certificate chain for this key, per the JWK spec, each entry base64-encoded DER. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="y" type="string">
              The elliptic curve Y coordinate, per the JWK spec, when `kty` is `EC`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Controls whether traffic from the public network is allowed to access the Attestation Provider APIs. Optional.
    </ResponseField>
    <ResponseField name="tpm_attestation_authentication" type="string">
      The setting that controls whether authentication is enabled or disabled for TPM Attestation REST APIs. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  The tags that will be assigned to the attestation provider. Optional.
</ResponseField>
