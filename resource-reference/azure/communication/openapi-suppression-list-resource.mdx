---
title: "azure_communication_openapi_suppression_list_resource"
description: "`azure_communication_openapi_suppression_list_resource` is a named list of email address(es) Azure Communication Services Email excludes from delivery..."
---

`azure_communication_openapi_suppression_list_resource` is a named list of email address(es) Azure Communication Services Email excludes from delivery, e.g. addresses that previously bounced or unsubscribed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	communication "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/communication"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi suppression list resource"})

    		ubx.Resource(communication.OpenapiSuppressionListResource, "example", communication.OpenapiSuppressionListResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiSuppressionListResource } from "@ubx/sdk-azure/azure/communication/openapi_suppression_list_resource";

    export default stack("example", () => {
      intent({ summary: "example own openapi suppression list resource" });

      resource(OpenapiSuppressionListResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.communication import OpenapiSuppressionListResource, OpenapiSuppressionListResourceConfig

    def describe():
        ubx.intent("example own openapi suppression list resource")

        ubx.resource(OpenapiSuppressionListResource, "example", OpenapiSuppressionListResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_communication_openapi_suppression_list_resource,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  A class that describes the properties of a SuppressionList resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_time_stamp" type="string">
      The date the resource was created. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="data_location" type="string">
      The location where the SuppressionListAddress data is stored at rest. This value is inherited from the parent Domains resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="last_updated_time_stamp" type="string">
      The date the resource was last updated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="list_name" type="string">
      The name of the suppression list. This value must match one of the valid sender usernames of the sending domain. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
