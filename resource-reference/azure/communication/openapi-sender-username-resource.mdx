---
title: "azure_communication_openapi_sender_username_resource"
description: "`azure_communication_openapi_sender_username_resource` defines a sender username (the part before `@`) permitted to send email from a verified domain..."
---

`azure_communication_openapi_sender_username_resource` defines a sender username (the part before `@`) permitted to send email from a verified domain in Azure Communication Services.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	communication "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/communication"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi sender username resource"})

    		ubx.Resource(communication.OpenapiSenderUsernameResource, "example", communication.OpenapiSenderUsernameResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiSenderUsernameResource } from "@ubx/sdk-azure/azure/communication/openapi_sender_username_resource";

    export default stack("example", () => {
      intent({ summary: "example own openapi sender username resource" });

      resource(OpenapiSenderUsernameResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.communication import OpenapiSenderUsernameResource, OpenapiSenderUsernameResourceConfig

    def describe():
        ubx.intent("example own openapi sender username resource")

        ubx.resource(OpenapiSenderUsernameResource, "example", OpenapiSenderUsernameResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_communication_openapi_sender_username_resource, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  A class that describes the properties of a SenderUsername resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="data_location" type="string">
      The location where the SenderUsername resource data is stored at rest. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The display name for the senderUsername. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Unknown is the default state for Communication Services. Optional.
    </ResponseField>
    <ResponseField name="username" type="string" required>
      A sender senderUsername to be used when sending emails. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
