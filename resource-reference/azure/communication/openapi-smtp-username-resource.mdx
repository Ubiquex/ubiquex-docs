---
title: "azure_communication_openapi_smtp_username_resource"
description: "`azure_communication_openapi_smtp_username_resource` is a SMTP-specific credential username for authenticating to Azure Communication Services Email..."
---

`azure_communication_openapi_smtp_username_resource` is a SMTP-specific credential username for authenticating to Azure Communication Services Email over SMTP.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	communication "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/communication"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi smtp username resource"})

    		ubx.Resource(communication.OpenapiSmtpUsernameResource, "example", communication.OpenapiSmtpUsernameResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiSmtpUsernameResource } from "@ubx/sdk-azure/azure/communication/openapi_smtp_username_resource";

    export default stack("example", () => {
      intent({ summary: "example own openapi smtp username resource" });

      resource(OpenapiSmtpUsernameResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.communication.openapi_smtp_username_resource import OpenapiSmtpUsernameResource, OpenapiSmtpUsernameResourceConfig

    def describe():
        ubx.intent("example own openapi smtp username resource")

        ubx.resource(OpenapiSmtpUsernameResource, "example", OpenapiSmtpUsernameResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_communication_openapi_smtp_username_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  A class that describes the properties of a SmtpUsername resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="entra_application_id" type="string" required>
      The application Id for the linked Entra Application. Required.
    </ResponseField>
    <ResponseField name="tenant_id" type="string" required>
      The tenant of the linked Entra Application. Required.
    </ResponseField>
    <ResponseField name="username" type="string" required>
      The SMTP username. Could be free form or in the email address format. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
