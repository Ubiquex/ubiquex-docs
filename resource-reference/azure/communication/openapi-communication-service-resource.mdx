---
title: "azure_communication_openapi_communication_service_resource"
description: "`azure_communication_openapi_communication_service_resource` is an Azure Communication Services resource, the top-level container that chat, voice..."
---

`azure_communication_openapi_communication_service_resource` is an Azure Communication Services resource, the top-level container that chat, voice, video, SMS, and email capability(ies) are provisioned under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	communication "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/communication"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi communication service resource"})

    		ubx.Resource(communication.OpenapiCommunicationServiceResource, "example", communication.OpenapiCommunicationServiceResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiCommunicationServiceResource } from "@ubx/sdk-azure/azure/communication/openapi_communication_service_resource";

    export default stack("example", () => {
      intent({ summary: "example own openapi communication service resource" });

      resource(OpenapiCommunicationServiceResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.communication.openapi_communication_service_resource import OpenapiCommunicationServiceResource, OpenapiCommunicationServiceResourceConfig

    def describe():
        ubx.intent("example own openapi communication service resource")

        ubx.resource(OpenapiCommunicationServiceResource, "example", OpenapiCommunicationServiceResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_communication_openapi_communication_service_resource, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  A class that describes the properties of the CommunicationService. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="data_location" type="string" required>
      The location where the communication service stores its data at rest. Required.
    </ResponseField>
    <ResponseField name="disable_local_auth" type="bool">
      Disable local authentication for the CommunicationService. Optional.
    </ResponseField>
    <ResponseField name="host_name" type="string">
      FQDN of the CommunicationService instance. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="immutable_resource_id" type="string">
      The immutable resource Id of the communication service. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="linked_domains" type="list(string)">
      List of email Domain resource Ids. Optional.
    </ResponseField>
    <ResponseField name="notification_hub_id" type="string">
      Resource ID of an Azure Notification Hub linked to this resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Optional.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Allow, disallow, or let network security perimeter configuration control public network access to the protected resource. Value is optional but if passed in, it must be 'Enabled', 'Disabled' or 'SecuredByPerimeter'. Optional.
    </ResponseField>
    <ResponseField name="version" type="string">
      Version of the CommunicationService resource. Probably you need the same or higher version of client SDKs. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
