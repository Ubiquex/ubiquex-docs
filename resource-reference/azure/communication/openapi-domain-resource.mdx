---
title: "azure_communication_openapi_domain_resource"
description: "`azure_communication_openapi_domain_resource` registers a sending domain (Azure-managed or a caller-verified custom domain) for Azure Communication..."
---

`azure_communication_openapi_domain_resource` registers a sending domain (Azure-managed or a caller-verified custom domain) for Azure Communication Services Email.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_communication_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_communication_openapi/sdk/go
    package main

    import (
    	communication "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_communication_openapi/communication"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi domain resource"})

    		ubx.Resource(communication.OpenapiDomainResource, "example", communication.OpenapiDomainResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_communication_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiDomainResource } from "./local-sdk/azure_communication_openapi/sdk/typescript/azure_communication_openapi/communication/openapi_domain_resource.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi domain resource" });

      resource(OpenapiDomainResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_communication_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_communication_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_communication_openapi.communication import OpenapiDomainResource, OpenapiDomainResourceConfig

    def describe():
        ubx.intent("example own openapi domain resource")

        ubx.resource(OpenapiDomainResource, "example", OpenapiDomainResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_communication_openapi_domain_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  A class that describes the properties of a Domains resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="data_location" type="string">
      The location where the Domains resource data is stored at rest. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="domain_management" type="string" required>
      Describes how a Domains resource is being managed. Required.
    </ResponseField>
    <ResponseField name="from_sender_domain" type="string">
      P2 sender domain that is displayed to the email recipients [RFC 5322]. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="mail_from_sender_domain" type="string">
      P1 sender domain that is present on the email envelope [RFC 5321]. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Optional.
    </ResponseField>
    <ResponseField name="user_engagement_tracking" type="string">
      Describes whether user engagement tracking is enabled or disabled. Optional.
    </ResponseField>
    <ResponseField name="verification_records" type="object">
      List of DnsRecord Optional.
      <Expandable title="properties">
        <ResponseField name="dkim" type="object">
          A class that represents a VerificationStatus record. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Name of the DNS record. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="ttl" type="number">
              Represents an expiry time in seconds to represent how long this entry can be cached by the resolver, default = 3600sec. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="type" type="string">
              Type of the DNS record. Example: TXT Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="value" type="string">
              Value of the DNS record. Computed by Microsoft Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dkim2" type="object">
          A class that represents a VerificationStatus record. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Name of the DNS record. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="ttl" type="number">
              Represents an expiry time in seconds to represent how long this entry can be cached by the resolver, default = 3600sec. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="type" type="string">
              Type of the DNS record. Example: TXT Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="value" type="string">
              Value of the DNS record. Computed by Microsoft Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dmarc" type="object">
          A class that represents a VerificationStatus record. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Name of the DNS record. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="ttl" type="number">
              Represents an expiry time in seconds to represent how long this entry can be cached by the resolver, default = 3600sec. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="type" type="string">
              Type of the DNS record. Example: TXT Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="value" type="string">
              Value of the DNS record. Computed by Microsoft Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="domain" type="object">
          A class that represents a VerificationStatus record. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Name of the DNS record. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="ttl" type="number">
              Represents an expiry time in seconds to represent how long this entry can be cached by the resolver, default = 3600sec. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="type" type="string">
              Type of the DNS record. Example: TXT Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="value" type="string">
              Value of the DNS record. Computed by Microsoft Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="spf" type="object">
          A class that represents a VerificationStatus record. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Name of the DNS record. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="ttl" type="number">
              Represents an expiry time in seconds to represent how long this entry can be cached by the resolver, default = 3600sec. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="type" type="string">
              Type of the DNS record. Example: TXT Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="value" type="string">
              Value of the DNS record. Computed by Microsoft Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="verification_states" type="object">
      List of VerificationStatusRecord Optional.
      <Expandable title="properties">
        <ResponseField name="dkim" type="object">
          A class that represents a VerificationStatus record. Optional.
          <Expandable title="properties">
            <ResponseField name="error_code" type="string">
              Error code. This property will only be present if the status is UnableToVerify. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="status" type="string">
              Status of the verification operation. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dkim2" type="object">
          A class that represents a VerificationStatus record. Optional.
          <Expandable title="properties">
            <ResponseField name="error_code" type="string">
              Error code. This property will only be present if the status is UnableToVerify. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="status" type="string">
              Status of the verification operation. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dmarc" type="object">
          A class that represents a VerificationStatus record. Optional.
          <Expandable title="properties">
            <ResponseField name="error_code" type="string">
              Error code. This property will only be present if the status is UnableToVerify. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="status" type="string">
              Status of the verification operation. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="domain" type="object">
          A class that represents a VerificationStatus record. Optional.
          <Expandable title="properties">
            <ResponseField name="error_code" type="string">
              Error code. This property will only be present if the status is UnableToVerify. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="status" type="string">
              Status of the verification operation. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="spf" type="object">
          A class that represents a VerificationStatus record. Optional.
          <Expandable title="properties">
            <ResponseField name="error_code" type="string">
              Error code. This property will only be present if the status is UnableToVerify. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="status" type="string">
              Status of the verification operation. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
