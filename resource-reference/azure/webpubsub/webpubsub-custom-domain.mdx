---
title: "azure_webpubsub_custom_domain"
description: "`azure_webpubsub_webpubsub_custom_domain` maps a custom domain to a Web PubSub resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	webpubsub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/webpubsub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own webpubsub custom domain"})

    		ubx.Resource(webpubsub.CustomDomain, "example", webpubsub.CustomDomainConfig{
    			Name:       "example",
    			Properties: map[string]any{"custom_certificate": map[string]any{"id": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CustomDomain } from "@ubx/sdk-azure/azure/webpubsub/custom_domain";

    export default stack("example", () => {
      intent({ summary: "example own webpubsub custom domain" });

      resource(CustomDomain, "example", {
        name: "example",
        properties: { customCertificate: { id: "example" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.webpubsub import CustomDomain, CustomDomainConfig

    def describe():
        ubx.intent("example own webpubsub custom domain")

        ubx.resource(CustomDomain, "example", CustomDomainConfig(
            name="example",
            properties={"custom_certificate": {"id": "example"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_webpubsub_custom_domain called "example", with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of a custom domain. Required.
  <Expandable title="properties">
    <ResponseField name="custom_certificate" type="object" required>
      Reference to a resource. Required.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="domain_name" type="string" required>
      The custom domain name. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
