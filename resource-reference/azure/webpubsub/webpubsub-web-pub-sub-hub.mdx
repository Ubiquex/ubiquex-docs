---
title: "azure_webpubsub_web_pub_sub_hub"
description: "`azure_webpubsub_web_pub_sub_hub` is a hub within a Web PubSub resource..."
---

`azure_webpubsub_web_pub_sub_hub` is a hub within a Web PubSub resource, defining the event handler(s) and behavior applied to WebSocket connections scoped to that hub.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_webpubsub_webpubsub --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_webpubsub_webpubsub/sdk/go
    package main

    import (
    	webpubsub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_webpubsub_webpubsub/webpubsub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own webpubsub web pub sub hub"})

    		ubx.Resource(webpubsub.WebpubsubWebPubSubHub, "example", webpubsub.WebpubsubWebPubSubHubConfig{
    			Properties: map[string]any{"anonymous_connect_policy": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_webpubsub_webpubsub --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { WebpubsubWebPubSubHub } from "./local-sdk/azure_webpubsub_webpubsub/sdk/typescript/azure_webpubsub_webpubsub/webpubsub/webpubsub_web_pub_sub_hub.ts";

    export default stack("example", () => {
      intent({ summary: "example own webpubsub web pub sub hub" });

      resource(WebpubsubWebPubSubHub, "example", {
        properties: { anonymousConnectPolicy: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_webpubsub_webpubsub --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_webpubsub_webpubsub/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_webpubsub_webpubsub.webpubsub import WebpubsubWebPubSubHub, WebpubsubWebPubSubHubConfig

    def describe():
        ubx.intent("example own webpubsub web pub sub hub")

        ubx.resource(WebpubsubWebPubSubHub, "example", WebpubsubWebPubSubHubConfig(
            properties={"anonymous_connect_policy": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_webpubsub_web_pub_sub_hub, with properties a set
    value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  Properties of a hub. Required.
  <Expandable title="properties">
    <ResponseField name="anonymous_connect_policy" type="string">
      The settings for configuring if anonymous connections are allowed for this hub: "allow" or "deny". Default to "deny". Optional.
    </ResponseField>
    <ResponseField name="event_handlers" type="list(object)">
      Event handler of a hub. Optional.
      <Expandable title="properties">
        <ResponseField name="auth" type="object">
          Authentication configuration for this upstream endpoint. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="managed_identity" type="object">
              The managed identity used to authenticate requests to this upstream endpoint. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="resource" type="string">
                  A reference to the resource this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="type" type="string">
              The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="system_events" type="list(string)">
          The system event type(s) this event handler subscribes to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url_template" type="string">
          The URL template used to construct the upstream endpoint address for a matching event. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_event_pattern" type="string">
          The user event name pattern this event handler matches. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="event_listeners" type="list(object)">
      Event listener settings for forwarding your client events to listeners. Event listener is transparent to Web PubSub clients, and it doesn't return any result to clients nor interrupt the lifetime of clients. One event can be sent to multiple listeners, as long as it matches the filters in those listeners. The order of the array elements doesn't matter. Maximum count of event listeners among all hubs is 10. Optional.
      <Expandable title="properties">
        <ResponseField name="endpoint" type="object">
          The endpoint this resource is reachable at. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="filter" type="object">
          Restricts this rule to matching event(s). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="web_socket_keep_alive_interval_in_seconds" type="number">
      The settings for configuring the WebSocket ping-pong interval in seconds for all clients in the hub. Valid range: 1 to 120. Default to 20 seconds. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
