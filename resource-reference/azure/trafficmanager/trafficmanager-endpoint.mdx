---
title: "azure_trafficmanager_trafficmanager_endpoint"
description: "`azure_trafficmanager_trafficmanager_endpoint` is a single destination (an Azure resource, external endpoint..."
---

`azure_trafficmanager_trafficmanager_endpoint` is a single destination (an Azure resource, external endpoint, or nested profile) that Traffic Manager routes DNS-level traffic to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_trafficmanager_trafficmanager --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_trafficmanager_trafficmanager/sdk/go
    package main

    import (
    	trafficmanager "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_trafficmanager_trafficmanager/trafficmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own trafficmanager endpoint"})

    		ubx.Resource(trafficmanager.TrafficmanagerEndpoint, "ci-runner", trafficmanager.TrafficmanagerEndpointConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_trafficmanager_trafficmanager --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrafficmanagerEndpoint } from "./local-sdk/azure_trafficmanager_trafficmanager/sdk/typescript/azure_trafficmanager_trafficmanager/trafficmanager/trafficmanager_endpoint.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own trafficmanager endpoint" });

      resource(TrafficmanagerEndpoint, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_trafficmanager_trafficmanager --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_trafficmanager_trafficmanager/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_trafficmanager_trafficmanager.trafficmanager import TrafficmanagerEndpoint, TrafficmanagerEndpointConfig

    def describe():
        ubx.intent("payments own trafficmanager endpoint")

        ubx.resource(TrafficmanagerEndpoint, "ci-runner", TrafficmanagerEndpointConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_trafficmanager_trafficmanager_endpoint, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Class representing a Traffic Manager endpoint properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="always_serve" type="string">
      If Always Serve is enabled, probing for endpoint health will be disabled and endpoints will be included in the traffic routing method. Optional.
    </ResponseField>
    <ResponseField name="custom_headers" type="list(object)">
      List of custom headers. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="endpoint_location" type="string">
      Specifies the location of the external or nested endpoints when using the 'Performance' traffic routing method. Optional.
    </ResponseField>
    <ResponseField name="endpoint_monitor_status" type="string">
      The monitoring status of the endpoint. Optional.
    </ResponseField>
    <ResponseField name="endpoint_status" type="string">
      The status of the endpoint. If the endpoint is Enabled, it is probed for endpoint health and is included in the traffic routing method. Optional.
    </ResponseField>
    <ResponseField name="geo_mapping" type="list(string)">
      The list of countries/regions mapped to this endpoint when using the 'Geographic' traffic routing method. Please consult Traffic Manager Geographic documentation for a full list of accepted values. Optional.
    </ResponseField>
    <ResponseField name="min_child_endpoints" type="number">
      The minimum number of endpoints that must be available in the child profile in order for the parent profile to be considered available. Only applicable to endpoint of type 'NestedEndpoints'. Optional.
    </ResponseField>
    <ResponseField name="min_child_endpoints_ipv4" type="number">
      The minimum number of IPv4 (DNS record type A) endpoints that must be available in the child profile in order for the parent profile to be considered available. Only applicable to endpoint of type 'NestedEndpoints'. Optional.
    </ResponseField>
    <ResponseField name="min_child_endpoints_ipv6" type="number">
      The minimum number of IPv6 (DNS record type AAAA) endpoints that must be available in the child profile in order for the parent profile to be considered available. Only applicable to endpoint of type 'NestedEndpoints'. Optional.
    </ResponseField>
    <ResponseField name="priority" type="number">
      The priority of this endpoint when using the 'Priority' traffic routing method. Possible values are from 1 to 1000, lower values represent higher priority. This is an optional parameter. If specified, it must be specified on all endpoints, and no two endpoints can share the same priority value. Optional.
    </ResponseField>
    <ResponseField name="subnets" type="list(object)">
      The list of subnets, IP addresses, and/or address ranges mapped to this endpoint when using the 'Subnet' traffic routing method. An empty list will match all ranges not covered by other endpoints. Optional.
      <Expandable title="properties">
        <ResponseField name="first" type="string">
          The first address in this range, inclusive. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="last" type="string">
          The last address in this range, inclusive. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scope" type="number">
          The subnet or IP range this endpoint applies to, for subnet-based routing. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target" type="string">
      The fully-qualified DNS name or IP address of the endpoint. Traffic Manager returns this value in DNS responses to direct traffic to this endpoint. Optional.
    </ResponseField>
    <ResponseField name="target_resource_id" type="string">
      The Azure Resource URI of the of the endpoint. Not applicable to endpoints of type 'ExternalEndpoints'. Optional.
    </ResponseField>
    <ResponseField name="weight" type="number">
      The weight of this endpoint when using the 'Weighted' traffic routing method. Possible values are from 1 to 1000. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
