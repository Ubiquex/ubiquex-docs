---
title: "azure_trafficmanager_profile"
description: "`azure_trafficmanager_trafficmanager_profile` is a Traffic Manager profile..."
---

`azure_trafficmanager_trafficmanager_profile` is a Traffic Manager profile, using DNS-based routing to direct client traffic across globally distributed endpoint(s) based on a chosen routing method (priority, weighted, performance, geographic).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_trafficmanager_trafficmanager --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_trafficmanager_trafficmanager/sdk/go
    package main

    import (
    	trafficmanager "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_trafficmanager_trafficmanager/trafficmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own trafficmanager profile"})

    		ubx.Resource(trafficmanager.TrafficmanagerProfile, "ci-runner", trafficmanager.TrafficmanagerProfileConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_trafficmanager_trafficmanager --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrafficmanagerProfile } from "./local-sdk/azure_trafficmanager_trafficmanager/sdk/typescript/azure_trafficmanager_trafficmanager/trafficmanager/trafficmanager_profile.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own trafficmanager profile" });

      resource(TrafficmanagerProfile, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_trafficmanager_trafficmanager --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_trafficmanager_trafficmanager/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_trafficmanager_trafficmanager.trafficmanager import TrafficmanagerProfile, TrafficmanagerProfileConfig

    def describe():
        ubx.intent("payments own trafficmanager profile")

        ubx.resource(TrafficmanagerProfile, "ci-runner", TrafficmanagerProfileConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_trafficmanager_profile, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Class representing the Traffic Manager profile properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="allowed_endpoint_record_types" type="list(string)">
      The list of allowed endpoint record types. Optional.
    </ResponseField>
    <ResponseField name="dns_config" type="object">
      Class containing DNS settings in a Traffic Manager profile. Optional.
      <Expandable title="properties">
        <ResponseField name="fqdn" type="string">
          The fully-qualified domain name (FQDN) of the Traffic Manager profile. This is formed from the concatenation of the RelativeName with the DNS domain used by Azure Traffic Manager. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="relative_name" type="string">
          The relative DNS name provided by this Traffic Manager profile. This value is combined with the DNS domain name used by Azure Traffic Manager to form the fully-qualified domain name (FQDN) of the profile. Optional.
        </ResponseField>
        <ResponseField name="ttl" type="number">
          The DNS Time-To-Live (TTL), in seconds. This informs the local DNS resolvers and DNS clients how long to cache DNS responses provided by this Traffic Manager profile. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="endpoints" type="list(object)">
      The list of endpoints in the Traffic Manager profile. Optional.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_return" type="number">
      Maximum number of endpoints to be returned for MultiValue routing type. Optional.
    </ResponseField>
    <ResponseField name="monitor_config" type="object">
      Class containing endpoint monitoring settings in a Traffic Manager profile. Optional.
      <Expandable title="properties">
        <ResponseField name="custom_headers" type="list(object)">
          List of custom headers. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="expected_status_code_ranges" type="list(object)">
          List of expected status code ranges. Optional.
          <Expandable title="properties">
            <ResponseField name="max" type="number">
              The maximum value allowed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="min" type="number">
              The minimum value allowed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="interval_in_seconds" type="number">
          The monitor interval for endpoints in this profile. This is the interval at which Traffic Manager will check the health of each endpoint in this profile. Optional.
        </ResponseField>
        <ResponseField name="path" type="string">
          The path relative to the endpoint domain name used to probe for endpoint health. Optional.
        </ResponseField>
        <ResponseField name="port" type="number">
          The TCP port used to probe for endpoint health. Optional.
        </ResponseField>
        <ResponseField name="profile_monitor_status" type="string">
          The profile-level monitoring status of the Traffic Manager profile. Optional.
        </ResponseField>
        <ResponseField name="protocol" type="string">
          The protocol (HTTP, HTTPS or TCP) used to probe for endpoint health. Optional.
        </ResponseField>
        <ResponseField name="timeout_in_seconds" type="number">
          The monitor timeout for endpoints in this profile. This is the time that Traffic Manager allows endpoints in this profile to response to the health check. Optional.
        </ResponseField>
        <ResponseField name="tolerated_number_of_failures" type="number">
          The number of consecutive failed health check that Traffic Manager tolerates before declaring an endpoint in this profile Degraded after the next failed health check. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="profile_status" type="string">
      The status of the Traffic Manager profile. Optional.
    </ResponseField>
    <ResponseField name="traffic_routing_method" type="string">
      The traffic routing method of the Traffic Manager profile. Optional.
    </ResponseField>
    <ResponseField name="traffic_view_enrollment_status" type="string">
      Indicates whether Traffic View is 'Enabled' or 'Disabled' for the Traffic Manager profile. Null, indicates 'Disabled'. Enabling this feature will increase the cost of the Traffic Manage profile. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
