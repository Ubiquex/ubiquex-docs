---
title: "azure_synapse_sqlpool_restore_point"
description: "`azure_synapse_sqlpool_restore_point` is a caller-created, named restore point for a Synapse dedicated SQL pool..."
---

`azure_synapse_sqlpool_restore_point` is a caller-created, named restore point for a Synapse dedicated SQL pool, letting it be restored to that exact moment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sqlpool restore point"})

    		ubx.Resource(synapse.SqlpoolRestorePoint, "example", synapse.SqlpoolRestorePointConfig{
    			RestorePointLabel: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SqlpoolRestorePoint } from "@ubx/sdk-azure/azure/synapse/sqlpool_restore_point";

    export default stack("example", () => {
      intent({ summary: "example own sqlpool restore point" });

      resource(SqlpoolRestorePoint, "example", {
        restorePointLabel: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.synapse.sqlpool_restore_point import SqlpoolRestorePoint, SqlpoolRestorePointConfig

    def describe():
        ubx.intent("example own sqlpool restore point")

        ubx.resource(SqlpoolRestorePoint, "example", SqlpoolRestorePointConfig(
            restore_point_label="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_synapse_sqlpool_restore_point, with restore point
    label example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="restore_point_label" type="string" required>
  The restore point label to apply Required.
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource location. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of a database restore point Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="earliest_restore_date" type="string">
      The earliest time to which this database can be restored Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="restore_point_creation_date" type="string">
      The time the backup was taken Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="restore_point_label" type="string">
      The label of restore point for backup request by user Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="restore_point_type" type="string">
      The type of restore point Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
