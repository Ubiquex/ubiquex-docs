---
title: "azure_synapse_sqlpool_sensitivity_label"
description: "`azure_synapse_sqlpool_sensitivity_label` classifies a specific column in a Synapse dedicated SQL pool with a data sensitivity label (e.g...."
---

`azure_synapse_sqlpool_sensitivity_label` classifies a specific column in a Synapse dedicated SQL pool with a data sensitivity label (e.g. `Confidential`), surfaced in vulnerability assessment and compliance reporting.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sqlpool sensitivity label"})

    		ubx.Resource(synapse.SqlpoolSensitivityLabel, "example", synapse.SqlpoolSensitivityLabelConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SqlpoolSensitivityLabel } from "@ubx/sdk-azure/azure/synapse/sqlpool_sensitivity_label";

    export default stack("example", () => {
      intent({ summary: "example own sqlpool sensitivity label" });

      resource(SqlpoolSensitivityLabel, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.synapse import SqlpoolSensitivityLabel, SqlpoolSensitivityLabelConfig

    def describe():
        ubx.intent("example own sqlpool sensitivity label")

        ubx.resource(SqlpoolSensitivityLabel, "example", SqlpoolSensitivityLabelConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_synapse_sqlpool_sensitivity_label, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="managed_by" type="string">
  managed by Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of a sensitivity label. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="column_name" type="string">
      The column name. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="information_type" type="string">
      The information type. Optional.
    </ResponseField>
    <ResponseField name="information_type_id" type="string">
      The information type ID. Optional.
    </ResponseField>
    <ResponseField name="is_disabled" type="bool">
      Is sensitivity recommendation disabled. Applicable for recommended sensitivity label only. Specifies whether the sensitivity recommendation on this column is disabled (dismissed) or not. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="label_id" type="string">
      The label ID. Optional.
    </ResponseField>
    <ResponseField name="label_name" type="string">
      The label name. Optional.
    </ResponseField>
    <ResponseField name="rank" type="string">
      The sensitivity level of this label, from `Low` to `Critical`. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="schema_name" type="string">
      The schema name. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="table_name" type="string">
      The table name. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
