---
title: "azure_synapse_sqlpool_sql_pool"
description: "`azure_synapse_sqlpool_sql_pool` is a Synapse dedicated SQL pool, a provisioned, MPP data warehouse compute and storage unit within a Synapse workspace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sqlpool sql pool"})

    		ubx.Resource(synapse.SqlpoolSqlPool, "example", synapse.SqlpoolSqlPoolConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SqlpoolSqlPool } from "@ubx/sdk-azure/azure/synapse/sqlpool_sql_pool";

    export default stack("example", () => {
      intent({ summary: "example own sqlpool sql pool" });

      resource(SqlpoolSqlPool, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.synapse import SqlpoolSqlPool, SqlpoolSqlPoolConfig

    def describe():
        ubx.intent("example own sqlpool sql pool")

        ubx.resource(SqlpoolSqlPool, "example", SqlpoolSqlPoolConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_synapse_sqlpool_sql_pool, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a SQL Analytics pool Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="collation" type="string">
      Collation mode Optional.
    </ResponseField>
    <ResponseField name="create_mode" type="string">
      Specifies the mode of sql pool creation. Default: regular sql pool creation. PointInTimeRestore: Creates a sql pool by restoring a point in time backup of an existing sql pool. sourceDatabaseId must be specified as the resource ID of the existing sql pool, and restorePointInTime must be specified. Recovery: Creates a sql pool by a geo-replicated backup. sourceDatabaseId must be specified as the recoverableDatabaseId to restore. Restore: Creates a sql pool by restoring a backup of a deleted sql pool. SourceDatabaseId should be the sql pool's original resource ID. SourceDatabaseId and sourceDatabaseDeletionDate must be specified. Optional.
    </ResponseField>
    <ResponseField name="creation_date" type="string">
      Date the SQL pool was created Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="max_size_bytes" type="number">
      Maximum size in bytes Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Resource state Optional.
    </ResponseField>
    <ResponseField name="recoverable_database_id" type="string">
      Backup database to restore from Optional.
    </ResponseField>
    <ResponseField name="restore_point_in_time" type="string">
      Snapshot time to restore Optional.
    </ResponseField>
    <ResponseField name="source_database_deletion_date" type="string">
      Specifies the time that the sql pool was deleted Optional.
    </ResponseField>
    <ResponseField name="source_database_id" type="string">
      Source database to create from Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      Resource status Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="storage_account_type" type="string">
      The storage account type used to store backups for this sql pool. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  SQL pool SKU Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      The SKU name Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The service tier Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
