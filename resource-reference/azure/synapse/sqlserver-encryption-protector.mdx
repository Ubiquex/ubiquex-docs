---
title: "azure_synapse_sqlserver_encryption_protector"
description: "`azure_synapse_sqlserver_encryption_protector` designates the customer-managed key used to protect transparent data encryption keys across a Synapse..."
---

`azure_synapse_sqlserver_encryption_protector` designates the customer-managed key used to protect transparent data encryption keys across a Synapse workspace's own SQL pools.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sqlserver encryption protector"})

    		ubx.Resource(synapse.SqlserverEncryptionProtector, "example", synapse.SqlserverEncryptionProtectorConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SqlserverEncryptionProtector } from "@ubx/sdk-azure/azure/synapse/sqlserver_encryption_protector";

    export default stack("example", () => {
      intent({ summary: "example own sqlserver encryption protector" });

      resource(SqlserverEncryptionProtector, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.synapse.sqlserver_encryption_protector import SqlserverEncryptionProtector, SqlserverEncryptionProtectorConfig

    def describe():
        ubx.intent("example own sqlserver encryption protector")

        ubx.resource(SqlserverEncryptionProtector, "example", SqlserverEncryptionProtectorConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_synapse_sqlserver_encryption_protector, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string">
  Kind of encryption protector. This is metadata used for the Azure portal experience. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="location" type="string">
  Resource location. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties for an encryption protector execution. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="server_key_name" type="string">
      The name of the server key. Optional.
    </ResponseField>
    <ResponseField name="server_key_type" type="string" required>
      The encryption protector type like 'ServiceManaged', 'AzureKeyVault'. Required.
    </ResponseField>
    <ResponseField name="subregion" type="string">
      Subregion of the encryption protector. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="thumbprint" type="string">
      Thumbprint of the server key. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="uri" type="string">
      The URI of the server key. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
