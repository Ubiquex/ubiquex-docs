---
title: "azure_synapse_keys_key"
description: "`azure_synapse_keys_key` registers a Key Vault key with a Synapse workspace for use in transparent data encryption."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own keys key"})

    		ubx.Resource(synapse.KeysKey, "example", synapse.KeysKeyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeysKey } from "@ubx/sdk-azure/azure/synapse/keys_key";

    export default stack("example", () => {
      intent({ summary: "example own keys key" });

      resource(KeysKey, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.synapse.keys_key import KeysKey, KeysKeyConfig

    def describe():
        ubx.intent("example own keys key")

        ubx.resource(KeysKey, "example", KeysKeyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_synapse_keys_key, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Key properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="is_active_cmk" type="bool">
      Used to activate the workspace after a customer managed key is provided. Optional.
    </ResponseField>
    <ResponseField name="key_vault_url" type="string">
      The Key Vault Url of the workspace key. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
