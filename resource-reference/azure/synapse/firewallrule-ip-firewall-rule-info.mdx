---
title: "azure_synapse_firewallrule_ip_firewall_rule_info"
description: "`azure_synapse_firewallrule_ip_firewall_rule_info` allows a specific IP address range to connect to a Synapse workspace that hasn't been placed inside..."
---

`azure_synapse_firewallrule_ip_firewall_rule_info` allows a specific IP address range to connect to a Synapse workspace that hasn't been placed inside a virtual network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own firewallrule ip firewall rule info"})

    		ubx.Resource(synapse.FirewallruleIpFirewallRuleInfo, "example", synapse.FirewallruleIpFirewallRuleInfoConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirewallruleIpFirewallRuleInfo } from "@ubx/sdk-azure/azure/synapse/firewallrule_ip_firewall_rule_info";

    export default stack("example", () => {
      intent({ summary: "example own firewallrule ip firewall rule info" });

      resource(FirewallruleIpFirewallRuleInfo, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.synapse import FirewallruleIpFirewallRuleInfo, FirewallruleIpFirewallRuleInfoConfig

    def describe():
        ubx.intent("example own firewallrule ip firewall rule info")

        ubx.resource(FirewallruleIpFirewallRuleInfo, "example", FirewallruleIpFirewallRuleInfoConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_synapse_firewallrule_ip_firewall_rule_info, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  IP firewall rule properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="end_ip_address" type="string">
      The end IP address of the firewall rule. Must be IPv4 format. Must be greater than or equal to startIpAddress Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Resource provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="start_ip_address" type="string">
      The start IP address of the firewall rule. Must be IPv4 format Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
