---
title: "azure_synapse_integrationruntime_integration_runtime_resource"
description: "`azure_synapse_integrationruntime_integration_runtime_resource` is the compute infrastructure (Azure-hosted or self-hosted) a Synapse Pipelines..."
---

`azure_synapse_integrationruntime_integration_runtime_resource` is the compute infrastructure (Azure-hosted or self-hosted) a Synapse Pipelines activity actually executes on.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own integrationruntime integration runtime resource"})

    		ubx.Resource(synapse.IntegrationruntimeIntegrationRuntimeResource, "example", synapse.IntegrationruntimeIntegrationRuntimeResourceConfig{
    			Properties: map[string]any{"type": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IntegrationruntimeIntegrationRuntimeResource } from "@ubx/sdk-azure/azure/synapse/integrationruntime_integration_runtime_resource";

    export default stack("example", () => {
      intent({
        summary: "example own integrationruntime integration runtime resource",
      });

      resource(IntegrationruntimeIntegrationRuntimeResource, "example", {
        properties: { type: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.synapse import IntegrationruntimeIntegrationRuntimeResource, IntegrationruntimeIntegrationRuntimeResourceConfig

    def describe():
        ubx.intent("example own integrationruntime integration runtime resource")

        ubx.resource(IntegrationruntimeIntegrationRuntimeResource, "example", IntegrationruntimeIntegrationRuntimeResourceConfig(
            properties={"type": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_synapse_integrationruntime_integration_runtime_resource,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  Azure Synapse nested object which serves as a compute resource for activities. Required.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Integration runtime description. Optional.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of integration runtime. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
