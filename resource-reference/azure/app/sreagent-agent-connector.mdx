---
title: "azure_app_sreagent_agent_connector"
description: "`azure_app_sreagent_agent_connector` connects an external tool or data source (e.g. a monitoring or ticketing system) to an Azure SRE Agent..."
---

`azure_app_sreagent_agent_connector` connects an external tool or data source (e.g. a monitoring or ticketing system) to an Azure SRE Agent, extending what it can observe or act on.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sreagent agent connector"})

    		ubx.Resource(app.SreagentAgentConnector, "example", app.SreagentAgentConnectorConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SreagentAgentConnector } from "@ubx/sdk-azure/azure/app/sreagent_agent_connector";

    export default stack("example", () => {
      intent({ summary: "example own sreagent agent connector" });

      resource(SreagentAgentConnector, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.app.sreagent_agent_connector import SreagentAgentConnector, SreagentAgentConnectorConfig

    def describe():
        ubx.intent("example own sreagent agent connector")

        ubx.resource(SreagentAgentConnector, "example", SreagentAgentConnectorConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_app_sreagent_agent_connector, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Agent Connector Properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="data_connector_type" type="string">
      The type of the data connector Optional.
    </ResponseField>
    <ResponseField name="data_source" type="string">
      Data source connection string or endpoint Optional.
    </ResponseField>
    <ResponseField name="deployment_error" type="string">
      Deployment error message if provisioning failed Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="endpoint" type="string">
      Endpoint of the connector Optional.
    </ResponseField>
    <ResponseField name="extended_properties" type="map(any)">
      Additional properties for the data connector which can be used to store custom key-value pairs Optional.
    </ResponseField>
    <ResponseField name="identity" type="string">
      Identity used to access the data source Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of a connector Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="source" type="string">
      Source of the data connector - "Agent" when directly stored in agent, "AgentSpace" when inherited Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
