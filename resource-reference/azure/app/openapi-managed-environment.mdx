---
title: "azure_app_openapi_managed_environment"
description: "`azure_app_openapi_managed_environment` is the Container Apps environment -- the secure..."
---

`azure_app_openapi_managed_environment` is the Container Apps environment -- the secure, isolated boundary sharing a virtual network and Log Analytics workspace -- that container app(s) and job(s) are deployed within.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_app_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_app_openapi/sdk/go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_app_openapi/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi managed environment"})

    		ubx.Resource(app.OpenapiManagedEnvironment, "ci-runner", app.OpenapiManagedEnvironmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_app_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiManagedEnvironment } from "./local-sdk/azure_app_openapi/sdk/typescript/azure_app_openapi/app/openapi_managed_environment.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi managed environment" });

      resource(OpenapiManagedEnvironment, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_app_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_app_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_app_openapi.app import OpenapiManagedEnvironment, OpenapiManagedEnvironmentConfig

    def describe():
        ubx.intent("payments own openapi managed environment")

        ubx.resource(OpenapiManagedEnvironment, "ci-runner", OpenapiManagedEnvironmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_app_openapi_managed_environment, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID used to authenticate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Kind of the Environment. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Managed environment resource specific properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="app_logs_configuration" type="object">
      Configuration of application logs Optional.
      <Expandable title="properties">
        <ResponseField name="destination" type="string">
          Logs destination, can be 'log-analytics', 'azure-monitor' or 'none' Optional.
        </ResponseField>
        <ResponseField name="log_analytics_configuration" type="object">
          Log Analytics configuration, must only be provided when destination is configured as 'log-analytics' Optional.
          <Expandable title="properties">
            <ResponseField name="customer_id" type="string">
              Log analytics customer id Optional.
            </ResponseField>
            <ResponseField name="shared_key" type="string">
              Log analytics customer key Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_domain_configuration" type="object">
      Configuration properties for apps environment custom domain Optional.
      <Expandable title="properties">
        <ResponseField name="certificate_key_vault_properties" type="object">
          Properties for a certificate stored in a Key Vault. Optional.
          <Expandable title="properties">
            <ResponseField name="identity" type="string">
              Resource ID of a managed identity to authenticate with Azure Key Vault, or System to use a system-assigned identity. Optional.
            </ResponseField>
            <ResponseField name="key_vault_url" type="string">
              URL pointing to the Azure Key Vault secret that holds the certificate. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="certificate_password" type="string">
          Certificate password Optional.
        </ResponseField>
        <ResponseField name="certificate_value" type="string">
          PFX or PEM blob Optional.
        </ResponseField>
        <ResponseField name="custom_domain_verification_id" type="string">
          Id used to verify domain name ownership Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="dns_suffix" type="string">
          Dns suffix for the environment domain Optional.
        </ResponseField>
        <ResponseField name="expiration_date" type="string">
          Certificate expiration date. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="subject_name" type="string">
          Subject name of the certificate. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="thumbprint" type="string">
          Certificate thumbprint. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dapr_aiconnection_string" type="string">
      Application Insights connection string used by Dapr to export Service to Service communication telemetry Optional.
    </ResponseField>
    <ResponseField name="dapr_aiinstrumentation_key" type="string">
      Azure Monitor instrumentation key used by Dapr to export Service to Service communication telemetry Optional.
    </ResponseField>
    <ResponseField name="dapr_configuration" type="object">
      Configuration properties Dapr component Optional.
      <Expandable title="properties">
        <ResponseField name="version" type="string">
          The version of Dapr Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_domain" type="string">
      Default Domain Name for the cluster Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="deployment_errors" type="string">
      Any errors that occurred during deployment or deployment validation Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="event_stream_endpoint" type="string">
      The endpoint of the eventstream of the Environment. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="infrastructure_resource_group" type="string">
      Name of the platform-managed resource group created for the Managed Environment to host infrastructure resources. If a subnet ID is provided, this resource group will be created in the same subscription as the subnet. Optional.
    </ResponseField>
    <ResponseField name="ingress_configuration" type="object">
      Settings for the ingress component, including workload profile, scaling, and connection handling. Optional.
      <Expandable title="properties">
        <ResponseField name="header_count_limit" type="number">
          Maximum number of headers per request allowed by the ingress. Must be at least 1. Defaults to 100. Optional.
        </ResponseField>
        <ResponseField name="request_idle_timeout" type="number">
          Duration (in minutes) before idle requests are timed out. Must be between 4 and 30 inclusive. Defaults to 4 minutes. Optional.
        </ResponseField>
        <ResponseField name="termination_grace_period_seconds" type="number">
          Time (in seconds) to allow active connections to complete on termination. Must be between 0 and 3600. Defaults to 480 seconds. Optional.
        </ResponseField>
        <ResponseField name="workload_profile_name" type="string">
          Name of the workload profile used by the ingress component. Required. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="keda_configuration" type="object">
      Configuration properties Keda component Optional.
      <Expandable title="properties">
        <ResponseField name="version" type="string">
          The version of Keda Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="peer_authentication" type="object">
      Peer authentication settings for the Managed Environment Optional.
      <Expandable title="properties">
        <ResponseField name="mtls" type="object">
          Configuration properties for mutual TLS authentication Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Boolean indicating whether the mutual TLS authentication is enabled Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="peer_traffic_configuration" type="object">
      Peer traffic settings for the Managed Environment Optional.
      <Expandable title="properties">
        <ResponseField name="encryption" type="object">
          Peer traffic encryption settings for the Managed Environment Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Boolean indicating whether the peer traffic encryption is enabled Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      Private endpoint connections to the resource. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the Environment. Optional.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Property to allow or block all public traffic. Allowed Values: 'Enabled', 'Disabled'. Optional.
    </ResponseField>
    <ResponseField name="static_ip" type="string">
      Static IP of the Environment Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="vnet_configuration" type="object">
      Configuration properties for apps environment to join a Virtual Network Optional.
      <Expandable title="properties">
        <ResponseField name="docker_bridge_cidr" type="string">
          CIDR notation IP range assigned to the Docker bridge, network. Must not overlap with any other provided IP ranges. Optional.
        </ResponseField>
        <ResponseField name="infrastructure_subnet_id" type="string">
          Resource ID of a subnet for infrastructure components. Must not overlap with any other provided IP ranges. Optional.
        </ResponseField>
        <ResponseField name="internal" type="bool">
          Boolean indicating the environment only has an internal load balancer. These environments do not have a public static IP resource. They must provide infrastructureSubnetId if enabling this property Optional.
        </ResponseField>
        <ResponseField name="platform_reserved_cidr" type="string">
          IP range in CIDR notation that can be reserved for environment infrastructure IP addresses. Must not overlap with any other provided IP ranges. Optional.
        </ResponseField>
        <ResponseField name="platform_reserved_dns_ip" type="string">
          An IP address from the IP range defined by platformReservedCidr that will be reserved for the internal DNS server. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="workload_profiles" type="list(object)">
      Workload profiles configured for the Managed Environment. Optional.
      <Expandable title="properties">
        <ResponseField name="maximum_count" type="number">
          The maximum number of replica(s) autoscaling is allowed to scale up to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="minimum_count" type="number">
          The minimum number of replica(s) autoscaling is allowed to scale down to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="workload_profile_type" type="string">
          The category of workload profile this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="zone_redundant" type="bool">
      Whether or not this Managed Environment is zone-redundant. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
