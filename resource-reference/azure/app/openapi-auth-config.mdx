---
title: "azure_app_openapi_auth_config"
description: "`azure_app_openapi_auth_config` configures authentication and authorization (identity provider(s), token validation) for a Container App."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi auth config"})

    		ubx.Resource(app.OpenapiAuthConfig, "example", app.OpenapiAuthConfigConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiAuthConfig } from "@ubx/sdk-azure/azure/app/openapi_auth_config";

    export default stack("example", () => {
      intent({ summary: "example own openapi auth config" });

      resource(OpenapiAuthConfig, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.app.openapi_auth_config import OpenapiAuthConfig, OpenapiAuthConfigConfig

    def describe():
        ubx.intent("example own openapi auth config")

        ubx.resource(OpenapiAuthConfig, "example", OpenapiAuthConfigConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_app_openapi_auth_config, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  AuthConfig resource specific properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="encryption_settings" type="object">
      The configuration settings of the secrets references of encryption key and signing key for ContainerApp Service Authentication/Authorization. Optional.
      <Expandable title="properties">
        <ResponseField name="container_app_auth_encryption_secret_name" type="string">
          The secret name which is referenced for EncryptionKey. Optional.
        </ResponseField>
        <ResponseField name="container_app_auth_signing_secret_name" type="string">
          The secret name which is referenced for SigningKey. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="global_validation" type="object">
      The configuration settings that determines the validation flow of users using ContainerApp Service Authentication/Authorization. Optional.
      <Expandable title="properties">
        <ResponseField name="excluded_paths" type="list(string)">
          The paths for which unauthenticated flow would not be redirected to the login page. Optional.
        </ResponseField>
        <ResponseField name="redirect_to_provider" type="string">
          The default authentication provider to use when multiple providers are configured. This setting is only needed if multiple providers are configured and the unauthenticated client action is set to "RedirectToLoginPage". Optional.
        </ResponseField>
        <ResponseField name="unauthenticated_client_action" type="string">
          The action to take when an unauthenticated client attempts to access the app. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="http_settings" type="object">
      The configuration settings of the HTTP requests for authentication and authorization requests made against ContainerApp Service Authentication/Authorization. Optional.
      <Expandable title="properties">
        <ResponseField name="forward_proxy" type="object">
          The configuration settings of a forward proxy used to make the requests. Optional.
          <Expandable title="properties">
            <ResponseField name="convention" type="string">
              The convention used to determine the url of the request made. Optional.
            </ResponseField>
            <ResponseField name="custom_host_header_name" type="string">
              The name of the header containing the host of the request. Optional.
            </ResponseField>
            <ResponseField name="custom_proto_header_name" type="string">
              The name of the header containing the scheme of the request. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="require_https" type="bool">
          &lt;code&gt;false&lt;/code&gt; if the authentication/authorization responses not having the HTTPS scheme are permissible; otherwise, &lt;code&gt;true&lt;/code&gt;. Optional.
        </ResponseField>
        <ResponseField name="routes" type="object">
          The configuration settings of the paths HTTP requests. Optional.
          <Expandable title="properties">
            <ResponseField name="api_prefix" type="string">
              The prefix that should precede all the authentication/authorization paths. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="identity_providers" type="object">
      The configuration settings of each of the identity providers used to configure ContainerApp Service Authentication/Authorization. Optional.
      <Expandable title="properties">
        <ResponseField name="apple" type="object">
          The configuration settings of the Apple provider. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              &lt;code&gt;false&lt;/code&gt; if the Apple provider should not be enabled despite the set registration; otherwise, &lt;code&gt;true&lt;/code&gt;. Optional.
            </ResponseField>
            <ResponseField name="login" type="object">
              The configuration settings of the login flow, including the scopes that should be requested. Optional.
              <Expandable title="properties">
                <ResponseField name="scopes" type="list(string)">
                  A list of the scopes that should be requested while authenticating. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="registration" type="object">
              The configuration settings of the registration for the Apple provider Optional.
              <Expandable title="properties">
                <ResponseField name="client_id" type="string">
                  The Client ID of the app used for login. Optional.
                </ResponseField>
                <ResponseField name="client_secret_setting_name" type="string">
                  The app setting name that contains the client secret. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="azure_active_directory" type="object">
          The configuration settings of the Azure Active directory provider. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              &lt;code&gt;false&lt;/code&gt; if the Azure Active Directory provider should not be enabled despite the set registration; otherwise, &lt;code&gt;true&lt;/code&gt;. Optional.
            </ResponseField>
            <ResponseField name="is_auto_provisioned" type="bool">
              Gets a value indicating whether the Azure AD configuration was auto-provisioned using 1st party tooling. This is an internal flag primarily intended to support the Azure Management Portal. Users should not read or write to this property. Optional.
            </ResponseField>
            <ResponseField name="login" type="object">
              The configuration settings of the Azure Active Directory login flow. Optional.
              <Expandable title="properties">
                <ResponseField name="disable_wwwauthenticate" type="bool">
                  &lt;code&gt;true&lt;/code&gt; if the www-authenticate provider should be omitted from the request; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional.
                </ResponseField>
                <ResponseField name="login_parameters" type="list(string)">
                  Login parameters to send to the OpenID Connect authorization endpoint when a user logs in. Each parameter must be in the form "key=value". Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="registration" type="object">
              The configuration settings of the Azure Active Directory app registration. Optional.
              <Expandable title="properties">
                <ResponseField name="client_id" type="string">
                  The Client ID of this relying party application, known as the client_id. This setting is required for enabling OpenID Connection authentication with Azure Active Directory or other 3rd party OpenID Connect providers. More information on OpenID Connect: http://openid.net/specs/openid-connect-core-1_0.html Optional.
                </ResponseField>
                <ResponseField name="client_secret_certificate_issuer" type="string">
                  An alternative to the client secret thumbprint, that is the issuer of a certificate used for signing purposes. This property acts as a replacement for the Client Secret Certificate Thumbprint. It is also optional. Optional.
                </ResponseField>
                <ResponseField name="client_secret_certificate_subject_alternative_name" type="string">
                  An alternative to the client secret thumbprint, that is the subject alternative name of a certificate used for signing purposes. This property acts as a replacement for the Client Secret Certificate Thumbprint. It is also optional. Optional.
                </ResponseField>
                <ResponseField name="client_secret_certificate_thumbprint" type="string">
                  An alternative to the client secret, that is the thumbprint of a certificate used for signing purposes. This property acts as a replacement for the Client Secret. It is also optional. Optional.
                </ResponseField>
                <ResponseField name="client_secret_setting_name" type="string">
                  The app setting name that contains the client secret of the relying party application. Optional.
                </ResponseField>
                <ResponseField name="open_id_issuer" type="string">
                  The OpenID Connect Issuer URI that represents the entity which issues access tokens for this application. When using Azure Active Directory, this value is the URI of the directory tenant, e.g. https://login.microsoftonline.com/v2.0/&#123;tenant-guid&#125;/. This URI is a case-sensitive identifier for the token issuer. More information on OpenID Connect Discovery: http://openid.net/specs/openid-connect-discovery-1_0.html Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="validation" type="object">
              The configuration settings of the Azure Active Directory token validation flow. Optional.
              <Expandable title="properties">
                <ResponseField name="allowed_audiences" type="list(string)">
                  The list of audiences that can make successful authentication/authorization requests. Optional.
                </ResponseField>
                <ResponseField name="default_authorization_policy" type="object">
                  The configuration settings of the Azure Active Directory default authorization policy. Optional.
                  <Expandable title="properties">
                    <ResponseField name="allowed_applications" type="list(string)">
                      The configuration settings of the Azure Active Directory allowed applications. Optional.
                    </ResponseField>
                    <ResponseField name="allowed_principals" type="object">
                      The configuration settings of the Azure Active Directory allowed principals. Optional.
                      <Expandable title="properties">
                        <ResponseField name="groups" type="list(string)">
                          The list of the allowed groups. Optional.
                        </ResponseField>
                        <ResponseField name="identities" type="list(string)">
                          The list of the allowed identities. Optional.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="jwt_claim_checks" type="object">
                  The configuration settings of the checks that should be made while validating the JWT Claims. Optional.
                  <Expandable title="properties">
                    <ResponseField name="allowed_client_applications" type="list(string)">
                      The list of the allowed client applications. Optional.
                    </ResponseField>
                    <ResponseField name="allowed_groups" type="list(string)">
                      The list of the allowed groups. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="azure_static_web_apps" type="object">
          The configuration settings of the Azure Static Web Apps provider. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              &lt;code&gt;false&lt;/code&gt; if the Azure Static Web Apps provider should not be enabled despite the set registration; otherwise, &lt;code&gt;true&lt;/code&gt;. Optional.
            </ResponseField>
            <ResponseField name="registration" type="object">
              The configuration settings of the registration for the Azure Static Web Apps provider Optional.
              <Expandable title="properties">
                <ResponseField name="client_id" type="string">
                  The Client ID of the app used for login. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="custom_open_id_connect_providers" type="map(object)">
          The map of the name of the alias of each custom Open ID Connect provider to the configuration settings of the custom Open ID Connect provider. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether this feature is turned on. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="login" type="object">
              Login configuration for this authentication provider. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name_claim_type" type="string">
                  Which JWT claim is used as the authenticated user's own display name. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="scopes" type="list(string)">
                  The OAuth scope(s) requested. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="registration" type="object">
              Registration configuration (client ID/secret, issuer) for this authentication provider. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="client_credential" type="object">
                  The credential used to authenticate this OAuth client. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="client_secret_setting_name" type="string">
                      The name of the app setting holding the OAuth client secret. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="method" type="string">
                      The HTTP method this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="client_id" type="string">
                  The client (application) ID used to authenticate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="open_id_connect_configuration" type="object">
                  OpenID Connect endpoint configuration for this authentication provider. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="authorization_endpoint" type="string">
                      The OAuth authorization endpoint URL. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="certification_uri" type="string">
                      The URI content is certified against. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="issuer" type="string">
                      The identity provider issuer URL trusted for this authentication. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="token_endpoint" type="string">
                      The OAuth token endpoint URL. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="well_known_open_id_configuration" type="string">
                      The URL of this identity provider's own OpenID Connect discovery document. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="facebook" type="object">
          The configuration settings of the Facebook provider. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              &lt;code&gt;false&lt;/code&gt; if the Facebook provider should not be enabled despite the set registration; otherwise, &lt;code&gt;true&lt;/code&gt;. Optional.
            </ResponseField>
            <ResponseField name="graph_api_version" type="string">
              The version of the Facebook api to be used while logging in. Optional.
            </ResponseField>
            <ResponseField name="login" type="object">
              The configuration settings of the login flow, including the scopes that should be requested. Optional.
              <Expandable title="properties">
                <ResponseField name="scopes" type="list(string)">
                  A list of the scopes that should be requested while authenticating. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="registration" type="object">
              The configuration settings of the app registration for providers that have app ids and app secrets Optional.
              <Expandable title="properties">
                <ResponseField name="app_id" type="string">
                  The App ID of the app used for login. Optional.
                </ResponseField>
                <ResponseField name="app_secret_setting_name" type="string">
                  The app setting name that contains the app secret. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="git_hub" type="object">
          The configuration settings of the GitHub provider. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              &lt;code&gt;false&lt;/code&gt; if the GitHub provider should not be enabled despite the set registration; otherwise, &lt;code&gt;true&lt;/code&gt;. Optional.
            </ResponseField>
            <ResponseField name="login" type="object">
              The configuration settings of the login flow, including the scopes that should be requested. Optional.
              <Expandable title="properties">
                <ResponseField name="scopes" type="list(string)">
                  A list of the scopes that should be requested while authenticating. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="registration" type="object">
              The configuration settings of the app registration for providers that have client ids and client secrets Optional.
              <Expandable title="properties">
                <ResponseField name="client_id" type="string">
                  The Client ID of the app used for login. Optional.
                </ResponseField>
                <ResponseField name="client_secret_setting_name" type="string">
                  The app setting name that contains the client secret. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="google" type="object">
          The configuration settings of the Google provider. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              &lt;code&gt;false&lt;/code&gt; if the Google provider should not be enabled despite the set registration; otherwise, &lt;code&gt;true&lt;/code&gt;. Optional.
            </ResponseField>
            <ResponseField name="login" type="object">
              The configuration settings of the login flow, including the scopes that should be requested. Optional.
              <Expandable title="properties">
                <ResponseField name="scopes" type="list(string)">
                  A list of the scopes that should be requested while authenticating. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="registration" type="object">
              The configuration settings of the app registration for providers that have client ids and client secrets Optional.
              <Expandable title="properties">
                <ResponseField name="client_id" type="string">
                  The Client ID of the app used for login. Optional.
                </ResponseField>
                <ResponseField name="client_secret_setting_name" type="string">
                  The app setting name that contains the client secret. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="validation" type="object">
              The configuration settings of the Allowed Audiences validation flow. Optional.
              <Expandable title="properties">
                <ResponseField name="allowed_audiences" type="list(string)">
                  The configuration settings of the allowed list of audiences from which to validate the JWT token. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="twitter" type="object">
          The configuration settings of the Twitter provider. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              &lt;code&gt;false&lt;/code&gt; if the Twitter provider should not be enabled despite the set registration; otherwise, &lt;code&gt;true&lt;/code&gt;. Optional.
            </ResponseField>
            <ResponseField name="registration" type="object">
              The configuration settings of the app registration for the Twitter provider. Optional.
              <Expandable title="properties">
                <ResponseField name="consumer_key" type="string">
                  The OAuth 1.0a consumer key of the Twitter application used for sign-in. This setting is required for enabling Twitter Sign-In. Twitter Sign-In documentation: https://dev.twitter.com/web/sign-in Optional.
                </ResponseField>
                <ResponseField name="consumer_secret_setting_name" type="string">
                  The app setting name that contains the OAuth 1.0a consumer secret of the Twitter application used for sign-in. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="login" type="object">
      The configuration settings of the login flow of users using ContainerApp Service Authentication/Authorization. Optional.
      <Expandable title="properties">
        <ResponseField name="allowed_external_redirect_urls" type="list(string)">
          External URLs that can be redirected to as part of logging in or logging out of the app. Note that the query string part of the URL is ignored. This is an advanced setting typically only needed by Windows Store application backends. Note that URLs within the current domain are always implicitly allowed. Optional.
        </ResponseField>
        <ResponseField name="cookie_expiration" type="object">
          The configuration settings of the session cookie's expiration. Optional.
          <Expandable title="properties">
            <ResponseField name="convention" type="string">
              The convention used when determining the session cookie's expiration. Optional.
            </ResponseField>
            <ResponseField name="time_to_expiration" type="string">
              The time after the request is made when the session cookie should expire. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="nonce" type="object">
          The configuration settings of the nonce used in the login flow. Optional.
          <Expandable title="properties">
            <ResponseField name="nonce_expiration_interval" type="string">
              The time after the request is made when the nonce should expire. Optional.
            </ResponseField>
            <ResponseField name="validate_nonce" type="bool">
              &lt;code&gt;false&lt;/code&gt; if the nonce should not be validated while completing the login flow; otherwise, &lt;code&gt;true&lt;/code&gt;. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="preserve_url_fragments_for_logins" type="bool">
          &lt;code&gt;true&lt;/code&gt; if the fragments from the request are preserved after the login request is made; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional.
        </ResponseField>
        <ResponseField name="routes" type="object">
          The routes that specify the endpoints used for login and logout requests. Optional.
          <Expandable title="properties">
            <ResponseField name="logout_endpoint" type="string">
              The endpoint at which a logout request should be made. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="token_store" type="object">
          The configuration settings of the token store. Optional.
          <Expandable title="properties">
            <ResponseField name="azure_blob_storage" type="object">
              The configuration settings of the storage of the tokens if blob storage is used. Optional.
              <Expandable title="properties">
                <ResponseField name="sas_url_setting_name" type="string" required>
                  The name of the app secrets containing the SAS URL of the blob storage containing the tokens. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="enabled" type="bool">
              &lt;code&gt;true&lt;/code&gt; to durably store platform-specific security tokens that are obtained during login flows; otherwise, &lt;code&gt;false&lt;/code&gt;. The default is &lt;code&gt;false&lt;/code&gt;. Optional.
            </ResponseField>
            <ResponseField name="token_refresh_extension_hours" type="number">
              The number of hours after session token expiration that a session token can be used to call the token refresh API. The default is 72 hours. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="platform" type="object">
      The configuration settings of the platform of ContainerApp Service Authentication/Authorization. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          &lt;code&gt;true&lt;/code&gt; if the Authentication / Authorization feature is enabled for the current app; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional.
        </ResponseField>
        <ResponseField name="runtime_version" type="string">
          The RuntimeVersion of the Authentication / Authorization feature in use for the current app. The setting in this value can control the behavior of certain features in the Authentication / Authorization module. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
