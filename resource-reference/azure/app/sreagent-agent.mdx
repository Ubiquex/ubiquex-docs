---
title: "azure_app_sreagent_agent"
description: "`azure_app_sreagent_agent` deploys Azure SRE Agent (preview), an AI agent that monitors application health, investigates incidents..."
---

`azure_app_sreagent_agent` deploys Azure SRE Agent (preview), an AI agent that monitors application health, investigates incidents, and proposes or takes remediation action within a defined agent space.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_app_sreagent --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_app_sreagent/sdk/go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_app_sreagent/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sreagent agent"})

    		ubx.Resource(app.SreagentAgent, "example", app.SreagentAgentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_app_sreagent --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SreagentAgent } from "./local-sdk/azure_app_sreagent/sdk/typescript/azure_app_sreagent/app/sreagent_agent.ts";

    export default stack("example", () => {
      intent({ summary: "example own sreagent agent" });

      resource(SreagentAgent, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_app_sreagent --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_app_sreagent/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_app_sreagent.app import SreagentAgent, SreagentAgentConfig

    def describe():
        ubx.intent("example own sreagent agent")

        ubx.resource(SreagentAgent, "example", SreagentAgentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_app_sreagent_agent, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the Agent Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="action_configuration" type="object">
      Configuration for action Optional.
      <Expandable title="properties">
        <ResponseField name="access_level" type="string">
          Agent access level Optional.
        </ResponseField>
        <ResponseField name="identity" type="string">
          The identity used by the action Optional.
        </ResponseField>
        <ResponseField name="mode" type="string">
          The mode for the app sreagent agent's action configuration. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="agent_endpoint" type="string">
      The endpoint of the Agent Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="agent_identity" type="object">
      Agent identity configuration Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Client ID (GUID) for the agent identity Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Indicates whether the agent identity is enabled Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="initial_sponsor_group_id" type="string" required>
          Initial sponsor group ID (required for agent identity) Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="agent_space_id" type="string">
      The agent space ID referenced by the agent Optional.
    </ResponseField>
    <ResponseField name="default_model" type="object">
      Default AI model configuration Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Model name (e.g., gpt-5, claude-opus-4-5, claude-sonnet-4-5) Optional.
        </ResponseField>
        <ResponseField name="provider" type="string">
          AI provider name (e.g., MicrosoftFoundry, Anthropic) Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="incident_management_configuration" type="object">
      Incident Management Configurations Optional.
      <Expandable title="properties">
        <ResponseField name="connection_key" type="string">
          The key for the connection Optional.
        </ResponseField>
        <ResponseField name="connection_name" type="string">
          The name of the connection Optional.
        </ResponseField>
        <ResponseField name="connection_url" type="string">
          The URL of the connection Optional.
        </ResponseField>
        <ResponseField name="obo_user" type="string">
          The user for the connection Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of incident management system Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="knowledge_graph_configuration" type="object">
      Knowledge graph configuration for agent Optional.
      <Expandable title="properties">
        <ResponseField name="identity" type="string">
          The identity used to access the knowledge graph Optional.
        </ResponseField>
        <ResponseField name="managed_resources" type="list(string)">
          The list of resources managed by agent Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="log_configuration" type="object">
      Log Configurations Optional.
      <Expandable title="properties">
        <ResponseField name="application_insights_configuration" type="object">
          Application Insights Configuration Optional.
          <Expandable title="properties">
            <ResponseField name="app_id" type="string">
              The Application ID for the Application Insights resource Optional.
            </ResponseField>
            <ResponseField name="connection_string" type="string">
              The connection string for the Application Insights resource Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="power_state" type="string">
      Power state of the Agent Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the Agent Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="running_state" type="string">
      The running state of the Agent Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="upgrade_channel" type="string">
      Upgrade channel for Agent Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
