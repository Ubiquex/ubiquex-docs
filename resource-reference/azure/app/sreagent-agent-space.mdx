---
title: "azure_app_sreagent_agent_space"
description: "`azure_app_sreagent_agent_space` is the top-level Azure SRE Agent resource, the scoped environment agent(s) and their own connector(s) are deployed within."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sreagent agent space"})

    		ubx.Resource(app.SreagentAgentSpace, "example", app.SreagentAgentSpaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SreagentAgentSpace } from "@ubx/sdk-azure/azure/app/sreagent_agent_space";

    export default stack("example", () => {
      intent({ summary: "example own sreagent agent space" });

      resource(SreagentAgentSpace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.app.sreagent_agent_space import SreagentAgentSpace, SreagentAgentSpaceConfig

    def describe():
        ubx.intent("example own sreagent agent space")

        ubx.resource(SreagentAgentSpace, "example", SreagentAgentSpaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_app_sreagent_agent_space, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Agent Space specific properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="compliance_status" type="object">
      Compliance status of the Agent Space Optional.
      <Expandable title="properties">
        <ResponseField name="compliance_issues" type="list(string)">
          List of compliance issues found in the Agent Space Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="is_compliant" type="bool">
          Indicates whether the Agent Space is compliant Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="last_compliance_check" type="string">
          Timestamp of the last compliance check Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="current_agent_count" type="number">
      Current number of agents in the Agent Space Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="description" type="string">
      Description of the Agent Space Optional.
    </ResponseField>
    <ResponseField name="last_policy_propagation" type="string">
      Timestamp of the last policy propagation to agents in this Agent Space Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="max_agent_count" type="number">
      Maximum number of agents allowed in the Agent Space Optional.
    </ResponseField>
    <ResponseField name="member_agents" type="list(string)">
      List of agents referencing the Agent Space Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="policies" type="object">
      Policy configurations for an Agent Space Optional.
      <Expandable title="properties">
        <ResponseField name="geneva_actions_configuration" type="object">
          Geneva Actions policy configuration for Agent Space Optional.
          <Expandable title="properties">
            <ResponseField name="acis_endpoint" type="string">
              ACIS (Azure Container Instance Service) endpoint URL Optional.
            </ResponseField>
            <ResponseField name="allowed_actions" type="list(object)">
              Collection of allowed Geneva actions Optional.
              <Expandable title="properties">
                <ResponseField name="action_name" type="string">
                  The name identifying the specific automated action this policy permits the agent to take. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="action_parameters" type="list(object)">
                  The parameter(s) configuring how this permitted action is executed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="approval_required" type="bool">
                  Whether a human must approve this action before the agent is allowed to run it. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="extension" type="string">
                  The extension or plugin this permitted action belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="authentication_mode" type="string">
              Geneva Action authentication mode Optional.
            </ResponseField>
            <ResponseField name="certificate_subject_alternative_name" type="string">
              Subject alternative name of the certificate used for authentication Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="certificate_subject_name" type="string">
              Subject name of the certificate used for authentication Optional.
            </ResponseField>
            <ResponseField name="client_id" type="string">
              Client ID for authentication Optional.
            </ResponseField>
            <ResponseField name="extension_name" type="string" required>
              Name of the Geneva extension Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the Agent Space Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="service_tree_id" type="string">
      Universal unique ID (UUID) of the Service Tree associated with this Agent Space Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
