---
title: "azure_app_openapi_session_pool"
description: "`azure_app_openapi_session_pool` provisions a pool of prewarmed, isolated Azure Container Apps dynamic sessions, used for secure..."
---

`azure_app_openapi_session_pool` provisions a pool of prewarmed, isolated Azure Container Apps dynamic sessions, used for secure, fast-starting per-session code execution (e.g. an AI agent's own code interpreter).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi session pool"})

    		ubx.Resource(app.OpenapiSessionPool, "example", app.OpenapiSessionPoolConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiSessionPool } from "@ubx/sdk-azure/azure/app/openapi_session_pool";

    export default stack("example", () => {
      intent({ summary: "example own openapi session pool" });

      resource(OpenapiSessionPool, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.app.openapi_session_pool import OpenapiSessionPool, OpenapiSessionPoolConfig

    def describe():
        ubx.intent("example own openapi session pool")

        ubx.resource(OpenapiSessionPool, "example", OpenapiSessionPoolConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_app_openapi_session_pool, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID used to authenticate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Container App session pool resource specific properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="container_type" type="string">
      The container type of the sessions. You can use your own container to build the session pool, or you can use a predefined container to run workload with specific language. Optional.
    </ResponseField>
    <ResponseField name="custom_container_template" type="object">
      Custom container configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="containers" type="list(object)">
          List of container definitions for the sessions of the session pool. Optional.
          <Expandable title="properties">
            <ResponseField name="args" type="list(string)">
              The argument(s) passed to the container's own command. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="command" type="list(string)">
              The container entrypoint command, overriding the image's own default. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="env" type="list(object)">
              Environment variable(s) set in the container. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="secret_ref" type="string">
                  A reference to the secret holding this value. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="image" type="string">
              The container image reference to run. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resources" type="object">
              The compute resource requirement(s) (CPU, memory) for this container. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="cpu" type="number">
                  The number of CPU core(s) allocated. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="memory" type="string">
                  The amount of memory allocated. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ingress" type="object">
          Session pool ingress configuration. Optional.
          <Expandable title="properties">
            <ResponseField name="target_port" type="number">
              Target port in containers for traffic from ingress Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="registry_credentials" type="object">
          Session pool private registry credentials. Optional.
          <Expandable title="properties">
            <ResponseField name="identity" type="string">
              A Managed Identity to use to authenticate with Azure Container Registry. For user-assigned identities, use the full user-assigned identity Resource ID. For system-assigned identities, use 'system' Optional.
            </ResponseField>
            <ResponseField name="password_secret_ref" type="string">
              The name of the secret that contains the registry login password Optional.
            </ResponseField>
            <ResponseField name="server" type="string">
              Container registry server. Optional.
            </ResponseField>
            <ResponseField name="username" type="string">
              Container registry username. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dynamic_pool_configuration" type="object">
      Dynamic pool configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="lifecycle_configuration" type="object">
          The lifecycle configuration properties of a session in the dynamic session pool Optional.
          <Expandable title="properties">
            <ResponseField name="cooldown_period_in_seconds" type="number">
              The cooldown period of a session in seconds when the lifecycle type is 'Timed'. Optional.
            </ResponseField>
            <ResponseField name="lifecycle_type" type="string">
              The lifecycle type of the session pool. Optional.
            </ResponseField>
            <ResponseField name="max_alive_period_in_seconds" type="number">
              The maximum alive period of a session in seconds when the lifecycle type is 'OnContainerExit'. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="environment_id" type="string">
      Resource ID of the session pool's environment. Optional.
    </ResponseField>
    <ResponseField name="managed_identity_settings" type="list(object)">
      Optional settings for a Managed Identity that is assigned to the Session pool. Optional.
      <Expandable title="properties">
        <ResponseField name="identity" type="string">
          The managed identity (or identities) this resource uses to authenticate to other Azure services. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lifecycle" type="string">
          Hook(s) run at specific points in this container's own lifecycle. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="node_count" type="number">
      The number of nodes the session pool is using. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="pool_management_endpoint" type="string">
      The endpoint to manage the pool. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="pool_management_type" type="string">
      The pool management type of the session pool. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the session pool. Optional.
    </ResponseField>
    <ResponseField name="scale_configuration" type="object">
      Scale configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="max_concurrent_sessions" type="number">
          The maximum count of sessions at the same time. Optional.
        </ResponseField>
        <ResponseField name="ready_session_instances" type="number">
          The minimum count of ready session instances. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="secrets" type="list(object)">
      The secrets of the session pool. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="session_network_configuration" type="object">
      Session network configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="status" type="string">
          Network status for the sessions. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
