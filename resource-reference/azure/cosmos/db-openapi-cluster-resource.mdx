---
title: "azure_cosmos_db_openapi_cluster_resource"
description: "`azure_cosmos_db_openapi_cluster_resource` provisions an Azure Managed Instance for Apache Cassandra cluster..."
---

`azure_cosmos_db_openapi_cluster_resource` provisions an Azure Managed Instance for Apache Cassandra cluster, a fully managed deployment of open-source Cassandra.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cosmos_db_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cosmos_db_openapi/sdk/go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cosmos_db_openapi/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own db openapi cluster resource"})

    		ubx.Resource(cosmos.DbOpenapiClusterResource, "ci-runner", cosmos.DbOpenapiClusterResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cosmos_db_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbOpenapiClusterResource } from "./local-sdk/azure_cosmos_db_openapi/sdk/typescript/azure_cosmos_db_openapi/cosmos/db_openapi_cluster_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own db openapi cluster resource" });

      resource(DbOpenapiClusterResource, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cosmos_db_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cosmos_db_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cosmos_db_openapi.cosmos import DbOpenapiClusterResource, DbOpenapiClusterResourceConfig

    def describe():
        ubx.intent("payments own db openapi cluster resource")

        ubx.resource(DbOpenapiClusterResource, "ci-runner", DbOpenapiClusterResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cosmos_db_openapi_cluster_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Identity for the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The object id of the identity resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location" type="string">
  The location of the resource group to which the resource belongs. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of a managed Cassandra cluster. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="authentication_method" type="string">
      Which authentication method Cassandra should use to authenticate clients. 'None' turns off authentication, so should not be used except in emergencies. 'Cassandra' is the default password based authentication. The default is 'Cassandra'. Optional.
    </ResponseField>
    <ResponseField name="auto_replicate" type="string">
      The form of AutoReplicate that is being used by this cluster. Optional.
    </ResponseField>
    <ResponseField name="azure_connection_method" type="string">
      How to connect to the azure services needed for running the cluster Optional.
    </ResponseField>
    <ResponseField name="backup_schedules" type="list(object)">
      List of backup schedules that define when you want to back up your data. Optional.
      <Expandable title="properties">
        <ResponseField name="cron_expression" type="string">
          The cron expression this scheduled operation runs on. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="retention_in_hours" type="number">
          How many hours of backup history are retained. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="schedule_name" type="string">
          The name of this schedule. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cassandra_audit_logging_enabled" type="bool">
      Whether Cassandra audit logging is enabled Optional.
    </ResponseField>
    <ResponseField name="cassandra_version" type="string">
      Which version of Cassandra should this cluster converge to running (e.g., 3.11). When updated, the cluster may take some time to migrate to the new version. Optional.
    </ResponseField>
    <ResponseField name="client_certificates" type="list(object)">
      List of TLS certificates used to authorize clients connecting to the cluster. All connections are TLS encrypted whether clientCertificates is set or not, but if clientCertificates is set, the managed Cassandra cluster will reject all connections not bearing a TLS client certificate that can be validated from one or more of the public certificates in this property. Optional.
      <Expandable title="properties">
        <ResponseField name="pem" type="string">
          A certificate, PEM-encoded. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cluster_name_override" type="string">
      If you need to set the clusterName property in cassandra.yaml to something besides the resource name of the cluster, set the value to use on this property. Optional.
    </ResponseField>
    <ResponseField name="deallocated" type="bool">
      Whether the cluster and associated data centers has been deallocated. Optional.
    </ResponseField>
    <ResponseField name="delegated_management_subnet_id" type="string">
      Resource id of a subnet that this cluster's management service should have its network interface attached to. The subnet must be routable to all subnets that will be delegated to data centers. The resource id must be of the form '/subscriptions/&lt;subscription id&gt;/resourceGroups/&lt;resource group&gt;/providers/Microsoft.Network/virtualNetworks/&lt;virtual network&gt;/subnets/&lt;subnet&gt;' Optional.
    </ResponseField>
    <ResponseField name="extensions" type="list(string)">
      Extensions to be added or updated on cluster. Optional.
    </ResponseField>
    <ResponseField name="external_data_centers" type="list(string)">
      List of the data center names for unmanaged data centers in this cluster to be included in auto-replication. Optional.
    </ResponseField>
    <ResponseField name="external_gossip_certificates" type="list(object)">
      List of TLS certificates used to authorize gossip from unmanaged data centers. The TLS certificates of all nodes in unmanaged data centers must be verifiable using one of the certificates provided in this property. Optional.
      <Expandable title="properties">
        <ResponseField name="pem" type="string">
          A certificate, PEM-encoded. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="external_seed_nodes" type="list(object)">
      List of IP addresses of seed nodes in unmanaged data centers. These will be added to the seed node lists of all managed nodes. Optional.
      <Expandable title="properties">
        <ResponseField name="ip_address" type="string">
          An IP address value. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="gossip_certificates" type="list(object)">
      List of TLS certificates that unmanaged nodes must trust for gossip with managed nodes. All managed nodes will present TLS client certificates that are verifiable using one of the certificates provided in this property. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="pem" type="string">
          A certificate, PEM-encoded. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hours_between_backups" type="number">
      (Deprecated) Number of hours to wait between taking a backup of the cluster. Optional.
    </ResponseField>
    <ResponseField name="initial_cassandra_admin_password" type="string">
      Initial password for clients connecting as admin to the cluster. Should be changed after cluster creation. Returns null on GET. This field only applies when the authenticationMethod field is 'Cassandra'. Optional.
    </ResponseField>
    <ResponseField name="private_link_resource_id" type="string">
      If the Connection Method is Vpn, this is the Id of the private link resource that the datacenters need to connect to. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="prometheus_endpoint" type="object">
      Output only. The URI metrics are exposed at in Prometheus format. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ip_address" type="string">
          IP address of this seed node. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provision_error" type="object">
      Detail about an error encountered while provisioning this resource. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="additional_error_info" type="string">
          Additional information about the error. Optional.
        </ResponseField>
        <ResponseField name="code" type="string">
          The code of error that occurred. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          The message of the error. Optional.
        </ResponseField>
        <ResponseField name="target" type="string">
          The target resource of the error. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The status of the resource at the time the operation was called. Optional.
    </ResponseField>
    <ResponseField name="repair_enabled" type="bool">
      Should automatic repairs run on this cluster? If omitted, this is true, and should stay true unless you are running a hybrid cluster where you are already doing your own repairs. Optional.
    </ResponseField>
    <ResponseField name="restore_from_backup_id" type="string">
      To create an empty cluster, omit this field or set it to null. To restore a backup into a new cluster, set this field to the resource id of the backup. Optional.
    </ResponseField>
    <ResponseField name="scheduled_event_strategy" type="string">
      How the nodes in the cluster react to scheduled events Optional.
    </ResponseField>
    <ResponseField name="seed_nodes" type="list(object)">
      List of IP addresses of seed nodes in the managed data centers. These should be added to the seed node lists of all unmanaged nodes. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="ip_address" type="string">
          An IP address value. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags are a list of key-value pairs that describe the resource. These tags can be used in viewing and grouping this resource (across resource groups). A maximum of 15 tags can be provided for a resource. Each tag must have a key no greater than 128 characters and value no greater than 256 characters. For example, the default experience for a template type is set with \"defaultExperience\": \"Cassandra\". Current \"defaultExperience\" values also include \"Table\", \"Graph\", \"DocumentDB\", and \"MongoDB\". Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
