---
title: "azure_cosmos_db_openapi_data_center_resource"
description: "`azure_cosmos_db_openapi_data_center_resource` is a single data center (a group of nodes in one region) within an Azure Managed Instance for Apache..."
---

`azure_cosmos_db_openapi_data_center_resource` is a single data center (a group of nodes in one region) within an Azure Managed Instance for Apache Cassandra cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cosmos_db_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cosmos_db_openapi/sdk/go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cosmos_db_openapi/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own db openapi data center resource"})

    		ubx.Resource(cosmos.DbOpenapiDataCenterResource, "ci-runner", cosmos.DbOpenapiDataCenterResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cosmos_db_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbOpenapiDataCenterResource } from "./local-sdk/azure_cosmos_db_openapi/sdk/typescript/azure_cosmos_db_openapi/cosmos/db_openapi_data_center_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own db openapi data center resource" });

      resource(DbOpenapiDataCenterResource, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cosmos_db_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cosmos_db_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cosmos_db_openapi.cosmos import DbOpenapiDataCenterResource, DbOpenapiDataCenterResourceConfig

    def describe():
        ubx.intent("payments own db openapi data center resource")

        ubx.resource(DbOpenapiDataCenterResource, "ci-runner", DbOpenapiDataCenterResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cosmos_db_openapi_data_center_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a managed Cassandra data center. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="authentication_method_ldap_properties" type="object">
      Ldap authentication method properties. This feature is in preview. Optional.
      <Expandable title="properties">
        <ResponseField name="connection_timeout_in_ms" type="number">
          Timeout for connecting to the LDAP server in miliseconds. The default is 5000 ms. Optional.
        </ResponseField>
        <ResponseField name="search_base_distinguished_name" type="string">
          Distinguished name of the object to start the recursive search of users from. Optional.
        </ResponseField>
        <ResponseField name="search_filter_template" type="string">
          Template to use for searching. Defaults to (cn=%s) where %s will be replaced by the username used to login. Optional.
        </ResponseField>
        <ResponseField name="server_certificates" type="list(object)">
          The trusted TLS server certificate(s) for connecting to this data source. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="pem" type="string">
              A certificate, PEM-encoded. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="server_hostname" type="string">
          Hostname of the LDAP server. Optional.
        </ResponseField>
        <ResponseField name="server_port" type="number">
          Port of the LDAP server. Optional.
        </ResponseField>
        <ResponseField name="service_user_distinguished_name" type="string">
          Distinguished name of the look up user account, who can look up user details on authentication. Optional.
        </ResponseField>
        <ResponseField name="service_user_password" type="string">
          Password of the look up user. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="availability_zone" type="bool">
      If the data center has Availability Zone support, apply it to the Virtual Machine ScaleSet that host the cassandra data center virtual machines. Optional.
    </ResponseField>
    <ResponseField name="backup_storage_customer_key_uri" type="string">
      Indicates the Key Uri of the customer key to use for encryption of the backup storage account. Optional.
    </ResponseField>
    <ResponseField name="base64_encoded_cassandra_yaml_fragment" type="string">
      A fragment of a cassandra.yaml configuration file to be included in the cassandra.yaml for all nodes in this data center. The fragment should be Base64 encoded, and only a subset of keys are allowed. Optional.
    </ResponseField>
    <ResponseField name="data_center_location" type="string">
      The region this data center should be created in. Optional.
    </ResponseField>
    <ResponseField name="deallocated" type="bool">
      Whether the data center has been deallocated. Optional.
    </ResponseField>
    <ResponseField name="delegated_subnet_id" type="string">
      Resource id of a subnet the nodes in this data center should have their network interfaces connected to. The subnet must be in the same region specified in 'dataCenterLocation' and must be able to route to the subnet specified in the cluster's 'delegatedManagementSubnetId' property. This resource id will be of the form '/subscriptions/&lt;subscription id&gt;/resourceGroups/&lt;resource group&gt;/providers/Microsoft.Network/virtualNetworks/&lt;virtual network&gt;/subnets/&lt;subnet&gt;'. Optional.
    </ResponseField>
    <ResponseField name="disk_capacity" type="number">
      Number of disks attached to each node. Default is 4. Optional.
    </ResponseField>
    <ResponseField name="disk_sku" type="string">
      Disk SKU used for data centers. Default value is P30. Optional.
    </ResponseField>
    <ResponseField name="managed_disk_customer_key_uri" type="string">
      Key uri to use for encryption of managed disks. Ensure the system assigned identity of the cluster has been assigned appropriate permissions(key get/wrap/unwrap permissions) on the key. Optional.
    </ResponseField>
    <ResponseField name="node_count" type="number">
      The number of nodes the data center should have. This is the desired number. After it is set, it may take some time for the data center to be scaled to match. To monitor the number of nodes and their status, use the fetchNodeStatus method on the cluster. Optional.
    </ResponseField>
    <ResponseField name="private_endpoint_ip_address" type="string">
      Ip of the VPN Endpoint for this data center. Optional.
    </ResponseField>
    <ResponseField name="provision_error" type="object">
      Detail about an error encountered while provisioning this resource. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="additional_error_info" type="string">
          Additional information about the error. Optional.
        </ResponseField>
        <ResponseField name="code" type="string">
          The code of error that occurred. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          The message of the error. Optional.
        </ResponseField>
        <ResponseField name="target" type="string">
          The target resource of the error. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The status of the resource at the time the operation was called. Optional.
    </ResponseField>
    <ResponseField name="seed_nodes" type="list(object)">
      IP addresses for seed nodes in this data center. This is for reference. Generally you will want to use the seedNodes property on the cluster, which aggregates the seed nodes from all data centers in the cluster. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="ip_address" type="string">
          An IP address value. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sku" type="string">
      Virtual Machine SKU used for data centers. Default value is Standard_DS14_v2 Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
