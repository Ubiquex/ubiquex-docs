---
title: "azure_cosmos_db_openapi_sql_stored_procedure_get_results"
description: "`azure_cosmos_db_openapi_sql_stored_procedure_get_results` is a JavaScript stored procedure registered on a Cosmos DB SQL API container..."
---

`azure_cosmos_db_openapi_sql_stored_procedure_get_results` is a JavaScript stored procedure registered on a Cosmos DB SQL API container, executed server-side against its own data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own db openapi sql stored procedure get results"})

    		ubx.Resource(cosmos.DbOpenapiSqlStoredProcedureGetResults, "example", cosmos.DbOpenapiSqlStoredProcedureGetResultsConfig{
    			Properties: map[string]any{"resource": map[string]any{"id": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbOpenapiSqlStoredProcedureGetResults } from "@ubx/sdk-azure/azure/cosmos/db_openapi_sql_stored_procedure_get_results";

    export default stack("example", () => {
      intent({
        summary: "example own db openapi sql stored procedure get results",
      });

      resource(DbOpenapiSqlStoredProcedureGetResults, "example", {
        properties: { resource: { id: "example" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cosmos import DbOpenapiSqlStoredProcedureGetResults, DbOpenapiSqlStoredProcedureGetResultsConfig

    def describe():
        ubx.intent("example own db openapi sql stored procedure get results")

        ubx.resource(DbOpenapiSqlStoredProcedureGetResults, "example", DbOpenapiSqlStoredProcedureGetResultsConfig(
            properties={"resource": {"id": "example"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cosmos_db_openapi_sql_stored_procedure_get_results,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object" required>
  Properties to create and update Azure Cosmos DB storedProcedure. Required.
  <Expandable title="properties">
    <ResponseField name="options" type="object">
      CreateUpdateOptions are a list of key-value pairs that describe the resource. Supported keys are "If-Match", "If-None-Match", "Session-Token" and "Throughput" Optional.
      <Expandable title="properties">
        <ResponseField name="autoscale_settings" type="object">
          Configuration for automatically scaling throughput within a defined maximum. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="max_throughput" type="number">
              Represents maximum throughput, the resource can scale up to. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="throughput" type="number">
          Request Units per second. For example, "throughput": 10000. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource" type="object" required>
      Cosmos DB SQL storedProcedure resource object Required.
      <Expandable title="properties">
        <ResponseField name="body" type="string">
          Body of the Stored Procedure Optional.
        </ResponseField>
        <ResponseField name="id" type="string" required>
          Name of the Cosmos DB SQL storedProcedure Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="identity" type="object">
  Identity for the resource. Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal id of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of identity used for the resource. The type 'SystemAssigned,UserAssigned' includes both an implicitly created identity and a set of user assigned identities. The type 'None' will remove any identities from the service. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user identities associated with resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location" type="string">
  The location of the resource group to which the resource belongs. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags are a list of key-value pairs that describe the resource. These tags can be used in viewing and grouping this resource (across resource groups). A maximum of 15 tags can be provided for a resource. Each tag must have a key no greater than 128 characters and value no greater than 256 characters. For example, the default experience for a template type is set with \"defaultExperience\": \"Cassandra\". Current \"defaultExperience\" values also include \"Table\", \"Graph\", \"DocumentDB\", and \"MongoDB\". Computed by Microsoft Azure after `ship`.
</ResponseField>
