---
title: "azurerm_redis_firewall_rule"
description: "Real, generated bindings for `azurerm_redis_firewall_rule`."
---

`azurerm_redis_firewall_rule` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redis "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/redis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own firewall rule"})

    		ubx.Resource(redis.FirewallRule, "ci-runner", redis.FirewallRuleConfig{
    			EndIp:             "example",
    			Name:              "ci-runner",
    			RedisCacheName:    "example-redis-cache-name",
    			ResourceGroupName: "example-resource-group-name",
    			StartIp:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirewallRule } from "jsr:@ubx/sdk-azure/azurerm/redis/firewall_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own firewall rule" });

      resource(FirewallRule, "ci-runner", {
        endIp: "example",
        name: "ci-runner",
        redisCacheName: "example-redis-cache-name",
        resourceGroupName: "example-resource-group-name",
        startIp: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.redis import FirewallRule, FirewallRuleConfig

    def describe():
        ubx.intent("payments own firewall rule")

        ubx.resource(FirewallRule, "ci-runner", FirewallRuleConfig(
            end_ip="example",
            name="ci-runner",
            redis_cache_name="example-redis-cache-name",
            resource_group_name="example-resource-group-name",
            start_ip="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_redis_firewall_rule called "ci-runner", with end
    ip example, redis cache name example-redis-cache-name, resource
    group name example-resource-group-name, start ip example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="end_ip" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="redis_cache_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="start_ip" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
