---
title: "azure_operationalinsights_openapi_linked_service"
description: "`azure_operationalinsights_openapi_linked_service` links a Log Analytics workspace to another Azure resource (e.g. an Automation account) for..."
---

`azure_operationalinsights_openapi_linked_service` links a Log Analytics workspace to another Azure resource (e.g. an Automation account) for integrated functionality.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_operationalinsights_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_operationalinsights_openapi/sdk/go
    package main

    import (
    	operationalinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_operationalinsights_openapi/operationalinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi linked service"})

    		ubx.Resource(operationalinsights.OpenapiLinkedService, "example", operationalinsights.OpenapiLinkedServiceConfig{
    			Properties: map[string]any{"provisioning_state": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_operationalinsights_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiLinkedService } from "./local-sdk/azure_operationalinsights_openapi/sdk/typescript/azure_operationalinsights_openapi/operationalinsights/openapi_linked_service.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi linked service" });

      resource(OpenapiLinkedService, "example", {
        properties: { provisioningState: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_operationalinsights_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_operationalinsights_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_operationalinsights_openapi.operationalinsights import OpenapiLinkedService, OpenapiLinkedServiceConfig

    def describe():
        ubx.intent("example own openapi linked service")

        ubx.resource(OpenapiLinkedService, "example", OpenapiLinkedServiceConfig(
            properties={"provisioning_state": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_operationalinsights_openapi_linked_service, with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  Linked service properties. Required.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the linked service. Optional.
    </ResponseField>
    <ResponseField name="resource_id" type="string">
      The resource id of the resource that will be linked to the workspace. This should be used for linking resources which require read access Optional.
    </ResponseField>
    <ResponseField name="write_access_resource_id" type="string">
      The resource id of the resource that will be linked to the workspace. This should be used for linking resources which require write access Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
