---
title: "azure_operationalinsights_openapi_cluster"
description: "`azure_operationalinsights_openapi_cluster` provisions a dedicated Log Analytics cluster..."
---

`azure_operationalinsights_openapi_cluster` provisions a dedicated Log Analytics cluster, providing customer-managed key encryption and reserved ingestion capacity pricing across attached workspace(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_operationalinsights_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_operationalinsights_openapi/sdk/go
    package main

    import (
    	operationalinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_operationalinsights_openapi/operationalinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi cluster"})

    		ubx.Resource(operationalinsights.OpenapiCluster, "ci-runner", operationalinsights.OpenapiClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_operationalinsights_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiCluster } from "./local-sdk/azure_operationalinsights_openapi/sdk/typescript/azure_operationalinsights_openapi/operationalinsights/openapi_cluster.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi cluster" });

      resource(OpenapiCluster, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_operationalinsights_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_operationalinsights_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_operationalinsights_openapi.operationalinsights import OpenapiCluster, OpenapiClusterConfig

    def describe():
        ubx.intent("payments own openapi cluster")

        ubx.resource(OpenapiCluster, "ci-runner", OpenapiClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_operationalinsights_openapi_cluster, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID used to authenticate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Cluster properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="associated_workspaces" type="list(object)">
      The list of Log Analytics workspaces associated with the cluster Optional.
      <Expandable title="properties">
        <ResponseField name="associate_date" type="string">
          When this resource was associated. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_id" type="string">
          A reference to the resource this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="workspace_id" type="string">
          A reference to the Log Analytics workspace this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="workspace_name" type="string">
          The name of the associated Log Analytics workspace. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="billing_type" type="string">
      Configures whether billing will be only on the cluster or each workspace will be billed by its proportional use. This does not change the overall billing, only how it will be distributed. Default value is 'Cluster' Optional.
    </ResponseField>
    <ResponseField name="capacity_reservation_properties" type="object">
      The Capacity Reservation properties. Optional.
      <Expandable title="properties">
        <ResponseField name="last_sku_update" type="string">
          The last time Sku was updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="min_capacity" type="number">
          Minimum CapacityReservation value in Gigabytes. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cluster_id" type="string">
      The ID associated with the cluster. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="created_date" type="string">
      The cluster creation time Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="is_availability_zones_enabled" type="bool">
      Sets whether the cluster will support availability zones. This can be set as true only in regions where Azure Data Explorer support Availability Zones. This Property can not be modified after cluster creation. Default value is 'true' if region supports Availability Zones. Optional.
    </ResponseField>
    <ResponseField name="is_double_encryption_enabled" type="bool">
      Configures whether cluster will use double encryption. This Property can not be modified after cluster creation. Default value is 'true' Optional.
    </ResponseField>
    <ResponseField name="key_vault_properties" type="object">
      The key vault properties. Optional.
      <Expandable title="properties">
        <ResponseField name="key_name" type="string">
          The name of the key associated with the Log Analytics cluster. Optional.
        </ResponseField>
        <ResponseField name="key_rsa_size" type="number">
          Selected key minimum required size. Optional.
        </ResponseField>
        <ResponseField name="key_vault_uri" type="string">
          The Key Vault uri which holds they key associated with the Log Analytics cluster. Optional.
        </ResponseField>
        <ResponseField name="key_version" type="string">
          The version of the key associated with the Log Analytics cluster. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_modified_date" type="string">
      The last time the cluster was updated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the cluster. Optional.
    </ResponseField>
    <ResponseField name="replication" type="object">
      Cluster replication properties. Optional.
      <Expandable title="properties">
        <ResponseField name="created_date" type="string">
          The cluster's replication creation time Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Specifies whether the replication is enabled or not. When true the cluster is replicate to the specified location. Optional.
        </ResponseField>
        <ResponseField name="is_availability_zones_enabled" type="bool">
          Should enable AvailabilityZones for the given replicated cluster Optional.
        </ResponseField>
        <ResponseField name="last_modified_date" type="string">
          The last time the cluster's replication was updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="location" type="string">
          The secondary location of the replication. If replication is being enabled, enabled must be provided. Optional.
        </ResponseField>
        <ResponseField name="provisioning_state" type="string">
          The provisioning state of the cluster replication. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The cluster sku definition. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      The capacity reservation level in Gigabytes for this cluster. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      The SKU (tier) of a cluster. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
