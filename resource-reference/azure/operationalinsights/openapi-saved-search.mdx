---
title: "azure_operationalinsights_openapi_saved_search"
description: "`azure_operationalinsights_openapi_saved_search` is a named, reusable Log Analytics query saved directly to a single workspace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_operationalinsights_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_operationalinsights_openapi/sdk/go
    package main

    import (
    	operationalinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_operationalinsights_openapi/operationalinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi saved search"})

    		ubx.Resource(operationalinsights.OpenapiSavedSearch, "ci-runner", operationalinsights.OpenapiSavedSearchConfig{
    			Properties: map[string]any{"category": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_operationalinsights_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiSavedSearch } from "./local-sdk/azure_operationalinsights_openapi/sdk/typescript/azure_operationalinsights_openapi/operationalinsights/openapi_saved_search.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi saved search" });

      resource(OpenapiSavedSearch, "ci-runner", {
        properties: { category: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_operationalinsights_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_operationalinsights_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_operationalinsights_openapi.operationalinsights import OpenapiSavedSearch, OpenapiSavedSearchConfig

    def describe():
        ubx.intent("payments own openapi saved search")

        ubx.resource(OpenapiSavedSearch, "ci-runner", OpenapiSavedSearchConfig(
            properties={"category": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_operationalinsights_openapi_saved_search, with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  The ETag of the saved search. To override an existing saved search, use "*" or specify the current Etag Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Value object for saved search results. Required.
  <Expandable title="properties">
    <ResponseField name="category" type="string" required>
      The category of the saved search. This helps the user to find a saved search faster. Required.
    </ResponseField>
    <ResponseField name="display_name" type="string" required>
      Saved search display name. Required.
    </ResponseField>
    <ResponseField name="function_alias" type="string">
      The function alias if query serves as a function. Optional.
    </ResponseField>
    <ResponseField name="function_parameters" type="string">
      The optional function parameters if query serves as a function. Value should be in the following format: 'param-name1:type1 = default_value1, param-name2:type2 = default_value2'. For more examples and proper syntax please refer to https://docs.microsoft.com/en-us/azure/kusto/query/functions/user-defined-functions. Optional.
    </ResponseField>
    <ResponseField name="query" type="string" required>
      The query expression for the saved search. Required.
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      The tags attached to the saved search. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="version" type="number">
      The version number of the query language. The current version is 2 and is the default. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
