---
title: "azure_operationalinsights_openapi_table"
description: "`azure_operationalinsights_openapi_table` is a table within a Log Analytics workspace..."
---

`azure_operationalinsights_openapi_table` is a table within a Log Analytics workspace, holding ingested log data of a specific schema and its own retention/plan configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_operationalinsights_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_operationalinsights_openapi/sdk/go
    package main

    import (
    	operationalinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_operationalinsights_openapi/operationalinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi table"})

    		ubx.Resource(operationalinsights.OpenapiTable, "example", operationalinsights.OpenapiTableConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_operationalinsights_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiTable } from "./local-sdk/azure_operationalinsights_openapi/sdk/typescript/azure_operationalinsights_openapi/operationalinsights/openapi_table.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi table" });

      resource(OpenapiTable, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_operationalinsights_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_operationalinsights_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_operationalinsights_openapi.operationalinsights import OpenapiTable, OpenapiTableConfig

    def describe():
        ubx.intent("example own openapi table")

        ubx.resource(OpenapiTable, "example", OpenapiTableConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_operationalinsights_openapi_table, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Table properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="archive_retention_in_days" type="number">
      The tables long-term retention in days. Calculated as (totalRetentionInDays-retentionInDays). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="last_plan_modified_date" type="string">
      The timestamp that table plan was last modified (UTC). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="plan" type="string">
      Instruct the system how to handle and charge the logs ingested to this table. Optional.
    </ResponseField>
    <ResponseField name="protection_level" type="string">
      The protection level of the table. Determines the default data access isolation behavior. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Table's current provisioning state. If set to 'updating', indicates a resource lock due to ongoing operation, forbidding any update to the table until the ongoing operation is concluded. Optional.
    </ResponseField>
    <ResponseField name="restored_logs" type="object">
      Restore parameters. Optional.
      <Expandable title="properties">
        <ResponseField name="azure_async_operation_id" type="string">
          Search results table async operation id. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="end_restore_time" type="string">
          The timestamp to end the restore by (UTC). Optional.
        </ResponseField>
        <ResponseField name="source_table" type="string">
          The table to restore data from. Optional.
        </ResponseField>
        <ResponseField name="start_restore_time" type="string">
          The timestamp to start the restore from (UTC). Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="result_statistics" type="object">
      Search job execution statistics. Optional.
      <Expandable title="properties">
        <ResponseField name="ingested_records" type="number">
          The number of rows that were returned by the search job. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="progress" type="number">
          Search job completion percentage. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="scanned_gb" type="number">
          Search job: Amount of scanned data. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="retention_in_days" type="number">
      In Analytics table: the tables analytics retention in days, between 4 and 730. Setting this property to -1 will default to the workspace retention. In Basic and Auxiliary table: read only property. Optional.
    </ResponseField>
    <ResponseField name="retention_in_days_as_default" type="bool">
      True - Value originates from workspace retention in days, False - Customer specific. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="schema" type="object">
      Table's schema. Optional.
      <Expandable title="properties">
        <ResponseField name="categories" type="list(string)">
          Table category. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="columns" type="list(object)">
          A list of table custom columns. Optional.
          <Expandable title="properties">
            <ResponseField name="data_type_hint" type="string">
              A hint indicating this custom field's own intended data type. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              A human-readable description of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_name" type="string">
              A human-readable name for this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="is_default_display" type="bool">
              Whether this column is shown by default in the query results view. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="is_hidden" type="bool">
              Whether this column is hidden from the query results view. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          Table description. Optional.
        </ResponseField>
        <ResponseField name="display_name" type="string">
          Table display name. Optional.
        </ResponseField>
        <ResponseField name="labels" type="list(string)">
          Labels to apply to the operationalinsights openapi table's schema. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the operationalinsights openapi table's schema. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="solutions" type="list(string)">
          List of solutions the table is affiliated with Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="source" type="string">
          Table's creator. Optional.
        </ResponseField>
        <ResponseField name="standard_columns" type="list(object)">
          A list of table standard columns. Computed by Microsoft Azure after `ship`.
          <Expandable title="properties">
            <ResponseField name="data_type_hint" type="string">
              A hint indicating this custom field's own intended data type. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              A human-readable description of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_name" type="string">
              A human-readable name for this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="is_default_display" type="bool">
              Whether this column is shown by default in the query results view. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="is_hidden" type="bool">
              Whether this column is hidden from the query results view. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="table_sub_type" type="string">
          The subtype describes what APIs can be used to interact with the table, and what features are available against it. Optional.
        </ResponseField>
        <ResponseField name="table_type" type="string">
          Table's creator. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="search_results" type="object">
      Parameters of the search job that initiated this table. Optional.
      <Expandable title="properties">
        <ResponseField name="azure_async_operation_id" type="string">
          Search results table async operation id. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="description" type="string">
          Search job Description. Optional.
        </ResponseField>
        <ResponseField name="end_search_time" type="string">
          The timestamp to end the search by (UTC) Optional.
        </ResponseField>
        <ResponseField name="limit" type="number">
          Limit the search job to return up to specified number of rows. Optional.
        </ResponseField>
        <ResponseField name="query" type="string">
          Search job query. Optional.
        </ResponseField>
        <ResponseField name="source_table" type="string">
          The table used in the search job. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="start_search_time" type="string">
          The timestamp to start the search from (UTC) Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="total_retention_in_days" type="number">
      The table total retention in days, between 4 and 4383. Setting this property to -1 will default to retentionInDays. Optional.
    </ResponseField>
    <ResponseField name="total_retention_in_days_as_default" type="bool">
      True - Value originates from retention in days, False - Customer specific. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
