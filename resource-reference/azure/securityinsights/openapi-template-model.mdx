---
title: "azure_securityinsights_openapi_template_model"
description: "`azure_securityinsights_openapi_template_model` is a reusable content template (e.g. for an analytics rule) published to the Microsoft Sentinel content..."
---

`azure_securityinsights_openapi_template_model` is a reusable content template (e.g. for an analytics rule) published to the Microsoft Sentinel content hub, that new instances can be created from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_securityinsights_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_securityinsights_openapi/sdk/go
    package main

    import (
    	securityinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_securityinsights_openapi/securityinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi template model"})

    		ubx.Resource(securityinsights.OpenapiTemplateModel, "ci-runner", securityinsights.OpenapiTemplateModelConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_securityinsights_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiTemplateModel } from "./local-sdk/azure_securityinsights_openapi/sdk/typescript/azure_securityinsights_openapi/securityinsights/openapi_template_model.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi template model" });

      resource(OpenapiTemplateModel, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_securityinsights_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_securityinsights_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_securityinsights_openapi.securityinsights import OpenapiTemplateModel, OpenapiTemplateModelConfig

    def describe():
        ubx.intent("payments own openapi template model")

        ubx.resource(OpenapiTemplateModel, "ci-runner", OpenapiTemplateModelConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_securityinsights_openapi_template_model, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Etag of the azure resource Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Template property bag. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="author" type="object">
      Publisher or creator of the content item. Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          Email of author contact Optional.
        </ResponseField>
        <ResponseField name="link" type="string">
          Link for author/vendor page Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the author. Company or person. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="categories" type="object">
      ies for the solution content item Optional.
      <Expandable title="properties">
        <ResponseField name="domains" type="list(string)">
          domain for the solution content item Optional.
        </ResponseField>
        <ResponseField name="verticals" type="list(string)">
          Industry verticals for the solution content item Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="content_id" type="string">
      Static ID for the content. Used to identify dependencies and content from solutions or community. Hard-coded/static for out of the box content and solutions. Dynamic for user-created. This is the resource name Optional.
    </ResponseField>
    <ResponseField name="content_kind" type="string">
      The kind of content the metadata is for. Optional.
    </ResponseField>
    <ResponseField name="content_product_id" type="string">
      Unique ID for the content. It should be generated based on the contentId of the package, contentId of the template, contentKind of the template and the contentVersion of the template Optional.
    </ResponseField>
    <ResponseField name="content_schema_version" type="string">
      Schema version of the content. Can be used to distinguish between different flow based on the schema version Optional.
    </ResponseField>
    <ResponseField name="custom_version" type="string">
      The custom version of the content. A optional free text Optional.
    </ResponseField>
    <ResponseField name="dependant_templates" type="list(object)">
      Dependant templates. Expandable. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="dependencies" type="object">
      Dependencies for the content item, what other content items it requires to work. Can describe more complex dependencies using a recursive/nested structure. For a single dependency an id/kind/version can be supplied or operator/criteria for complex dependencies. Optional.
      <Expandable title="properties">
        <ResponseField name="content_id" type="string">
          Id of the content item we depend on Optional.
        </ResponseField>
        <ResponseField name="criteria" type="list(object)">
          This is the list of dependencies we must fulfill, according to the AND/OR operator Optional.
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of content the metadata is for. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the content item Optional.
        </ResponseField>
        <ResponseField name="operator" type="string">
          Operator used for list of dependencies in criteria array. Optional.
        </ResponseField>
        <ResponseField name="version" type="string">
          Version of the the content item we depend on. Can be blank, * or missing to indicate any version fulfills the dependency. If version does not match our defined numeric format then an exact match is required. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The display name of the template Optional.
    </ResponseField>
    <ResponseField name="first_publish_date" type="string">
      first publish date content item Optional.
    </ResponseField>
    <ResponseField name="icon" type="string">
      the icon identifier. this id can later be fetched from the content metadata Optional.
    </ResponseField>
    <ResponseField name="is_deprecated" type="string">
      The boolean value the metadata is for. Optional.
    </ResponseField>
    <ResponseField name="last_publish_date" type="string">
      last publish date for the content item Optional.
    </ResponseField>
    <ResponseField name="main_template" type="any">
      The JSON of the ARM template to deploy active content. Expandable. Optional.
    </ResponseField>
    <ResponseField name="package_id" type="string">
      the package Id contains this template Optional.
    </ResponseField>
    <ResponseField name="package_kind" type="string">
      The package kind Optional.
    </ResponseField>
    <ResponseField name="package_name" type="string">
      the name of the package contains this template Optional.
    </ResponseField>
    <ResponseField name="package_version" type="string">
      Version of the package. Default and recommended format is numeric (e.g. 1, 1.0, 1.0.0, 1.0.0.0), following ARM metadata best practices. Can also be any string, but then we cannot guarantee any version checks Optional.
    </ResponseField>
    <ResponseField name="preview_images" type="list(string)">
      preview image file names. These will be taken from the solution artifacts Optional.
    </ResponseField>
    <ResponseField name="preview_images_dark" type="list(string)">
      preview image file names. These will be taken from the solution artifacts. used for dark theme support Optional.
    </ResponseField>
    <ResponseField name="providers" type="list(string)">
      Providers for the content item Optional.
    </ResponseField>
    <ResponseField name="source" type="object">
      The original source of the content item, where it comes from. Optional.
      <Expandable title="properties">
        <ResponseField name="kind" type="string" required>
          Source type of the content Required.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the content source. The repo name, solution name, LA workspace name etc. Optional.
        </ResponseField>
        <ResponseField name="source_id" type="string">
          ID of the content source. The solution ID, workspace ID, etc Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="support" type="object">
      Support information for the content item. Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          Email of support contact Optional.
        </ResponseField>
        <ResponseField name="link" type="string">
          Link for support help, like to support page to open a ticket etc. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the support contact. Company or person. Optional.
        </ResponseField>
        <ResponseField name="tier" type="string" required>
          Type of support for content item Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="threat_analysis_tactics" type="list(string)">
      the tactics the resource covers Optional.
    </ResponseField>
    <ResponseField name="threat_analysis_techniques" type="list(string)">
      the techniques the resource covers, these have to be aligned with the tactics being used Optional.
    </ResponseField>
    <ResponseField name="version" type="string">
      Version of the content. Default and recommended format is numeric (e.g. 1, 1.0, 1.0.0, 1.0.0.0), following ARM metadata best practices. Can also be any string, but then we cannot guarantee any version checks Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
