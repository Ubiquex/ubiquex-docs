---
title: "azure_securityinsights_openapi_watchlist"
description: "`azure_securityinsights_openapi_watchlist` is a named, uploaded reference dataset (e.g. a list of high-value assets) in Microsoft Sentinel..."
---

`azure_securityinsights_openapi_watchlist` is a named, uploaded reference dataset (e.g. a list of high-value assets) in Microsoft Sentinel, joinable against log query results for enrichment and filtering.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_securityinsights_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_securityinsights_openapi/sdk/go
    package main

    import (
    	securityinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_securityinsights_openapi/securityinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi watchlist"})

    		ubx.Resource(securityinsights.OpenapiWatchlist, "ci-runner", securityinsights.OpenapiWatchlistConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_securityinsights_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiWatchlist } from "./local-sdk/azure_securityinsights_openapi/sdk/typescript/azure_securityinsights_openapi/securityinsights/openapi_watchlist.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi watchlist" });

      resource(OpenapiWatchlist, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_securityinsights_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_securityinsights_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_securityinsights_openapi.securityinsights import OpenapiWatchlist, OpenapiWatchlistConfig

    def describe():
        ubx.intent("payments own openapi watchlist")

        ubx.resource(OpenapiWatchlist, "ci-runner", OpenapiWatchlistConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_securityinsights_openapi_watchlist, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Etag of the azure resource Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Describes watchlist properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="content_type" type="string">
      The content type of the raw content. Example : text/csv or text/tsv Optional.
    </ResponseField>
    <ResponseField name="created" type="string">
      The time the watchlist was created Optional.
    </ResponseField>
    <ResponseField name="created_by" type="object">
      User information that made some action Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          The email of the user. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the user. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="object_id" type="string">
          Universally Unique Identifier Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_duration" type="string">
      The default duration of a watchlist (in ISO 8601 duration format) Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      A description of the watchlist Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string" required>
      The display name of the watchlist Required.
    </ResponseField>
    <ResponseField name="is_deleted" type="bool">
      A flag that indicates if the watchlist is deleted or not Optional.
    </ResponseField>
    <ResponseField name="items_search_key" type="string" required>
      The search key is used to optimize query performance when using watchlists for joins with other data. For example, enable a column with IP addresses to be the designated SearchKey field, then use this field as the key field when joining to other event data by IP address. Required.
    </ResponseField>
    <ResponseField name="labels" type="list(string)">
      List of labels relevant to this watchlist Optional.
    </ResponseField>
    <ResponseField name="number_of_lines_to_skip" type="number">
      The number of lines in a csv/tsv content to skip before the header Optional.
    </ResponseField>
    <ResponseField name="provider" type="string" required>
      The provider of the watchlist Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the watchlist Optional.
    </ResponseField>
    <ResponseField name="raw_content" type="string">
      The raw content that represents to watchlist items to create. In case of csv/tsv content type, it's the content of the file that will parsed by the endpoint Optional.
    </ResponseField>
    <ResponseField name="source" type="string">
      The filename of the watchlist, called 'source' Optional.
    </ResponseField>
    <ResponseField name="source_type" type="string">
      The sourceType of the watchlist Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenantId where the watchlist belongs to Optional.
    </ResponseField>
    <ResponseField name="updated" type="string">
      The last time the watchlist was updated Optional.
    </ResponseField>
    <ResponseField name="updated_by" type="object">
      User information that made some action Optional.
      <Expandable title="properties">
        <ResponseField name="email" type="string">
          The email of the user. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the user. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="object_id" type="string">
          Universally Unique Identifier Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="upload_status" type="string">
      The status of the Watchlist upload : New, InProgress or Complete. **Note** : When a Watchlist upload status is InProgress, the Watchlist cannot be deleted Optional.
    </ResponseField>
    <ResponseField name="watchlist_alias" type="string">
      The alias of the watchlist Optional.
    </ResponseField>
    <ResponseField name="watchlist_id" type="string">
      The id (a Guid) of the watchlist Optional.
    </ResponseField>
    <ResponseField name="watchlist_type" type="string">
      The type of the watchlist Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
