---
title: "azure_servicefabric_application_service_resource"
description: "`azure_servicefabric_application_service_resource` is a single microservice within a Service Fabric application..."
---

`azure_servicefabric_application_service_resource` is a single microservice within a Service Fabric application, running as either a stateless or stateful service.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_servicefabric_application --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_servicefabric_application/sdk/go
    package main

    import (
    	servicefabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_servicefabric_application/servicefabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own application service resource"})

    		ubx.Resource(servicefabric.ApplicationServiceResource, "ci-runner", servicefabric.ApplicationServiceResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_servicefabric_application --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationServiceResource } from "./local-sdk/azure_servicefabric_application/sdk/typescript/azure_servicefabric_application/servicefabric/application_service_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own application service resource" });

      resource(ApplicationServiceResource, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_servicefabric_application --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_servicefabric_application/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_servicefabric_application.servicefabric import ApplicationServiceResource, ApplicationServiceResourceConfig

    def describe():
        ubx.intent("payments own application service resource")

        ubx.resource(ApplicationServiceResource, "ci-runner", ApplicationServiceResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicefabric_application_service_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The service resource properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="partition_description" type="object">
      Describes how the service is partitioned. Optional.
      <Expandable title="properties">
        <ResponseField name="partition_scheme" type="string" required>
          Enumerates the ways that a service can be partitioned. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The current deployment or provisioning state, which only appears in the response Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="service_dns_name" type="string">
      Dns name used for the service. If this is specified, then the service can be accessed via its DNS name instead of service name. Optional.
    </ResponseField>
    <ResponseField name="service_kind" type="string" required>
      The kind of service (Stateless or Stateful). Required.
    </ResponseField>
    <ResponseField name="service_package_activation_mode" type="string">
      The activation Mode of the service package Optional.
    </ResponseField>
    <ResponseField name="service_type_name" type="string">
      The name of the service type Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
