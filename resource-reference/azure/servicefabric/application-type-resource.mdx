---
title: "azure_servicefabric_application_type_resource"
description: "`azure_servicefabric_application_type_resource` registers an application type with a Service Fabric cluster..."
---

`azure_servicefabric_application_type_resource` registers an application type with a Service Fabric cluster, the versioned template application instance(s) are created from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_servicefabric_application --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_servicefabric_application/sdk/go
    package main

    import (
    	servicefabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_servicefabric_application/servicefabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own application type resource"})

    		ubx.Resource(servicefabric.ApplicationApplicationTypeResource, "example", servicefabric.ApplicationApplicationTypeResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_servicefabric_application --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationApplicationTypeResource } from "./local-sdk/azure_servicefabric_application/sdk/typescript/azure_servicefabric_application/servicefabric/application_application_type_resource.ts";

    export default stack("example", () => {
      intent({ summary: "example own application type resource" });

      resource(ApplicationApplicationTypeResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_servicefabric_application --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_servicefabric_application/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_servicefabric_application.servicefabric import ApplicationApplicationTypeResource, ApplicationApplicationTypeResourceConfig

    def describe():
        ubx.intent("example own application type resource")

        ubx.resource(ApplicationApplicationTypeResource, "example", ApplicationApplicationTypeResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicefabric_application_type_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The application type name properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      The current deployment or provisioning state, which only appears in the response. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
