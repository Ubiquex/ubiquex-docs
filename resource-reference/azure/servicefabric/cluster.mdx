---
title: "azure_servicefabric_cluster"
description: "`azure_servicefabric_cluster_cluster` is a Service Fabric cluster, a distributed systems platform for deploying and managing scalable..."
---

`azure_servicefabric_cluster_cluster` is a Service Fabric cluster, a distributed systems platform for deploying and managing scalable, reliable microservices and container-based applications.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicefabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/servicefabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cluster"})

    		ubx.Resource(servicefabric.Cluster, "example", servicefabric.ClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Cluster } from "@ubx/sdk-azure/azure/servicefabric/cluster";

    export default stack("example", () => {
      intent({ summary: "example own cluster" });

      resource(Cluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.servicefabric import Cluster, ClusterConfig

    def describe():
        ubx.intent("example own cluster")

        ubx.resource(Cluster, "example", ClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicefabric_cluster, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Describes the cluster resource properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="add_on_features" type="list(string)">
      The list of add-on features to enable in the cluster. Optional.
    </ResponseField>
    <ResponseField name="application_type_versions_cleanup_policy" type="object">
      Configuration for automatically removing unused application type versions. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="max_unused_versions_to_keep" type="number" required>
          Number of unused versions per application type to keep. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="available_cluster_versions" type="list(object)">
      The Service Fabric runtime versions available for this cluster. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="code_version" type="string">
          The Service Fabric runtime version this cluster runs. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="environment" type="string">
          The operating system environment this cluster runs on, e.g. `Windows` or `Linux`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="support_expiry_utc" type="string">
          When support for this cluster's own current code version ends, in UTC. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="azure_active_directory" type="object">
      The settings to enable AAD authentication on the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="client_application" type="string">
          Azure active directory client application id. Optional.
        </ResponseField>
        <ResponseField name="cluster_application" type="string">
          Azure active directory cluster application id. Optional.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          Azure active directory tenant id. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="certificate" type="object">
      Describes the certificate details. Optional.
      <Expandable title="properties">
        <ResponseField name="thumbprint" type="string" required>
          Thumbprint of the primary certificate. Required.
        </ResponseField>
        <ResponseField name="thumbprint_secondary" type="string">
          Thumbprint of the secondary certificate. Optional.
        </ResponseField>
        <ResponseField name="x509_store_name" type="string">
          The local certificate store location. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="certificate_common_names" type="object">
      Describes a list of server certificates referenced by common name that are used to secure the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="common_names" type="list(object)">
          The list of server certificates referenced by common name that are used to secure the cluster. Optional.
          <Expandable title="properties">
            <ResponseField name="certificate_common_name" type="string">
              The Common Name of the certificate trusted for cluster authentication. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="certificate_issuer_thumbprint" type="string">
              The thumbprint of the CA that issued the cluster certificate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="x509_store_name" type="string">
          The local certificate store location. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="client_certificate_common_names" type="list(object)">
      The list of client certificates referenced by common name that are allowed to manage the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="certificate_common_name" type="string">
          The Common Name of the certificate trusted for cluster authentication. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="certificate_issuer_thumbprint" type="string">
          The thumbprint of the CA that issued the cluster certificate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="is_admin" type="bool">
          Whether this client certificate is granted administrative access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="client_certificate_thumbprints" type="list(object)">
      The list of client certificates referenced by thumbprint that are allowed to manage the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="certificate_thumbprint" type="string">
          A cryptographic thumbprint of this certificate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="is_admin" type="bool">
          Whether this client certificate is granted administrative access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cluster_code_version" type="string">
      The Service Fabric runtime version of the cluster. This property can only by set the user when **upgradeMode** is set to 'Manual'. To get list of available Service Fabric versions for new clusters use [ClusterVersion API](https://learn.microsoft.com/rest/api/servicefabric/cluster-versions/list). To get the list of available version for existing clusters use **availableClusterVersions**. Optional.
    </ResponseField>
    <ResponseField name="cluster_endpoint" type="string">
      The Azure Resource Provider endpoint. A system service in the cluster connects to this endpoint. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="cluster_id" type="string">
      A service generated unique identifier for the cluster resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="cluster_state" type="string">
      The current state of the cluster. - WaitingForNodes - Indicates that the cluster resource is created and the resource provider is waiting for Service Fabric VM extension to boot up and report to it. - Deploying - Indicates that the Service Fabric runtime is being installed on the VMs. Cluster resource will be in this state until the cluster boots up and system services are up. - BaselineUpgrade - Indicates that the cluster is upgrading to establishes the cluster version. This upgrade is automatically initiated when the cluster boots up for the first time. - UpdatingUserConfiguration - Indicates that the cluster is being upgraded with the user provided configuration. - UpdatingUserCertificate - Indicates that the cluster is being upgraded with the user provided certificate. - UpdatingInfrastructure - Indicates that the cluster is being upgraded with the latest Service Fabric runtime version. This happens only when the **upgradeMode** is set to 'Automatic'. - EnforcingClusterVersion - Indicates that cluster is on a different version than expected and the cluster is being upgraded to the expected version. - UpgradeServiceUnreachable - Indicates that the system service in the cluster is no longer polling the Resource Provider. Clusters in this state cannot be managed by the Resource Provider. - AutoScale - Indicates that the ReliabilityLevel of the cluster is being adjusted. - Ready - Indicates that the cluster is in a stable state. Optional.
    </ResponseField>
    <ResponseField name="diagnostics_storage_account_config" type="object">
      The storage account information for storing Service Fabric diagnostic logs. Optional.
      <Expandable title="properties">
        <ResponseField name="blob_endpoint" type="string" required>
          The blob endpoint of the azure storage account. Required.
        </ResponseField>
        <ResponseField name="protected_account_key_name" type="string" required>
          The protected diagnostics storage key name. Required.
        </ResponseField>
        <ResponseField name="protected_account_key_name2" type="string">
          The secondary protected diagnostics storage key name. If one of the storage account keys is rotated the cluster will fallback to using the other. Optional.
        </ResponseField>
        <ResponseField name="queue_endpoint" type="string" required>
          The queue endpoint of the azure storage account. Required.
        </ResponseField>
        <ResponseField name="storage_account_name" type="string" required>
          The Azure storage account name. Required.
        </ResponseField>
        <ResponseField name="table_endpoint" type="string" required>
          The table endpoint of the azure storage account. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="event_store_service_enabled" type="bool">
      Indicates if the event store service is enabled. Optional.
    </ResponseField>
    <ResponseField name="fabric_settings" type="list(object)">
      The list of custom fabric settings to configure the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="parameters" type="list(object)">
          The parameter(s) making up this configuration. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="infrastructure_service_manager" type="bool">
      Indicates if infrastructure service manager is enabled. Optional.
    </ResponseField>
    <ResponseField name="management_endpoint" type="string" required>
      The http management endpoint of the cluster. Required.
    </ResponseField>
    <ResponseField name="node_types" type="list(object)" required>
      The list of node types in the cluster. Required.
      <Expandable title="properties">
        <ResponseField name="application_ports" type="object">
          The port range reserved for applications deployed to this cluster. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="end_port" type="number">
              The last port, inclusive, in this range. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="start_port" type="number">
              The first port, inclusive, in this range. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="capacities" type="map(string)">
          The named resource capacity/capacities (e.g. memory, disk) this node type provides. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="client_connection_endpoint_port" type="number">
          The port clients use to connect to this cluster. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="durability_level" type="string">
          The durability guarantee this node type provides, e.g. `Bronze`, `Silver`, or `Gold`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ephemeral_ports" type="object">
          The port range reserved for ephemeral (dynamic) connections on this cluster. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="end_port" type="number">
              The last port, inclusive, in this range. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="start_port" type="number">
              The first port, inclusive, in this range. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="http_gateway_endpoint_port" type="number">
          The port used for the cluster's own HTTP management endpoint. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="is_primary" type="bool">
          Whether this is the primary node type for the cluster. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="is_stateless" type="bool">
          Whether this service is stateless, rather than stateful. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="multiple_availability_zones" type="bool">
          Whether this node type is spread across multiple availability zones. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="placement_properties" type="map(string)">
          Custom key/value property(s) used to constrain where services are placed on this node type. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reverse_proxy_endpoint_port" type="number">
          The port used for the cluster's own reverse proxy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vm_instance_count" type="number">
          The number of virtual machine instance(s) in this node type. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="notifications" type="list(object)">
      Indicates a list of notification channels for cluster events. Optional.
      <Expandable title="properties">
        <ResponseField name="is_enabled" type="bool">
          Whether this feature is turned on. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="notification_category" type="string">
          The category of event this notification setting applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="notification_level" type="string">
          The minimum severity level that triggers this notification. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="notification_targets" type="list(object)">
          The recipient(s) this notification is delivered to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="notification_channel" type="string">
              The channel this notification is delivered through. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="receivers" type="list(string)">
              The recipient(s) this notification is delivered to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the cluster resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="reliability_level" type="string">
      The reliability level sets the replica set size of system services. Learn about [ReliabilityLevel](https://docs.microsoft.com/azure/service-fabric/service-fabric-cluster-capacity). - None - Run the System services with a target replica set count of 1. This should only be used for test clusters. - Bronze - Run the System services with a target replica set count of 3. This should only be used for test clusters. - Silver - Run the System services with a target replica set count of 5. - Gold - Run the System services with a target replica set count of 7. - Platinum - Run the System services with a target replica set count of 9. Optional.
    </ResponseField>
    <ResponseField name="reverse_proxy_certificate" type="object">
      Describes the certificate details. Optional.
      <Expandable title="properties">
        <ResponseField name="thumbprint" type="string" required>
          Thumbprint of the primary certificate. Required.
        </ResponseField>
        <ResponseField name="thumbprint_secondary" type="string">
          Thumbprint of the secondary certificate. Optional.
        </ResponseField>
        <ResponseField name="x509_store_name" type="string">
          The local certificate store location. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reverse_proxy_certificate_common_names" type="object">
      Describes a list of server certificates referenced by common name that are used to secure the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="common_names" type="list(object)">
          The list of server certificates referenced by common name that are used to secure the cluster. Optional.
          <Expandable title="properties">
            <ResponseField name="certificate_common_name" type="string">
              The Common Name of the certificate trusted for cluster authentication. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="certificate_issuer_thumbprint" type="string">
              The thumbprint of the CA that issued the cluster certificate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="x509_store_name" type="string">
          The local certificate store location. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sf_zonal_upgrade_mode" type="string">
      This property controls the logical grouping of VMs in upgrade domains (UDs). This property can't be modified if a node type with multiple Availability Zones is already present in the cluster. Optional.
    </ResponseField>
    <ResponseField name="upgrade_description" type="object">
      Describes the policy used when upgrading the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="delta_health_policy" type="object">
          Describes the delta health policies for the cluster upgrade. Optional.
          <Expandable title="properties">
            <ResponseField name="application_delta_health_policies" type="map(object)">
              Defines a map that contains specific application delta health policies for different applications. Each entry specifies as key the application name and as value an ApplicationDeltaHealthPolicy used to evaluate the application health when upgrading the cluster. The application name should include the 'fabric:' URI scheme. The map is empty by default. Optional.
              <Expandable title="properties">
                <ResponseField name="default_service_type_delta_health_policy" type="object">
                  The default health policy applied to a service type's own delta (upgrade-time) health evaluation. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="max_percent_delta_unhealthy_services" type="number">
                      The maximum percentage of services permitted to become newly unhealthy during an upgrade. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="service_type_delta_health_policies" type="map(object)">
                  Per-service-type health policy override(s) for delta (upgrade-time) health evaluation. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="max_percent_delta_unhealthy_services" type="number">
                      The maximum percentage of services permitted to become newly unhealthy during an upgrade. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_percent_delta_unhealthy_applications" type="number" required>
              The maximum allowed percentage of applications health degradation allowed during cluster upgrades. The delta is measured between the state of the applications at the beginning of upgrade and the state of the applications at the time of the health evaluation. The check is performed after every upgrade domain upgrade completion to make sure the global state of the cluster is within tolerated limits. System services are not included in this. Required.
            </ResponseField>
            <ResponseField name="max_percent_delta_unhealthy_nodes" type="number" required>
              The maximum allowed percentage of nodes health degradation allowed during cluster upgrades. The delta is measured between the state of the nodes at the beginning of upgrade and the state of the nodes at the time of the health evaluation. The check is performed after every upgrade domain upgrade completion to make sure the global state of the cluster is within tolerated limits. Required.
            </ResponseField>
            <ResponseField name="max_percent_upgrade_domain_delta_unhealthy_nodes" type="number" required>
              The maximum allowed percentage of upgrade domain nodes health degradation allowed during cluster upgrades. The delta is measured between the state of the upgrade domain nodes at the beginning of upgrade and the state of the upgrade domain nodes at the time of the health evaluation. The check is performed after every upgrade domain upgrade completion for all completed upgrade domains to make sure the state of the upgrade domains is within tolerated limits. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="force_restart" type="bool">
          If true, then processes are forcefully restarted during upgrade even when the code version has not changed (the upgrade only changes configuration or data). Optional.
        </ResponseField>
        <ResponseField name="health_check_retry_timeout" type="string" required>
          The amount of time to retry health evaluation when the application or cluster is unhealthy before the upgrade rolls back. The timeout can be in either hh:mm:ss or in d.hh:mm:ss.ms format. Required.
        </ResponseField>
        <ResponseField name="health_check_stable_duration" type="string" required>
          The amount of time that the application or cluster must remain healthy before the upgrade proceeds to the next upgrade domain. The duration can be in either hh:mm:ss or in d.hh:mm:ss.ms format. Required.
        </ResponseField>
        <ResponseField name="health_check_wait_duration" type="string" required>
          The length of time to wait after completing an upgrade domain before performing health checks. The duration can be in either hh:mm:ss or in d.hh:mm:ss.ms format. Required.
        </ResponseField>
        <ResponseField name="health_policy" type="object" required>
          Defines a health policy used to evaluate the health of the cluster or of a cluster node. Required.
          <Expandable title="properties">
            <ResponseField name="application_health_policies" type="map(object)">
              Defines a map that contains specific application health policies for different applications. Each entry specifies as key the application name and as value an ApplicationHealthPolicy used to evaluate the application health. The application name should include the 'fabric:' URI scheme. The map is empty by default. Optional.
              <Expandable title="properties">
                <ResponseField name="default_service_type_health_policy" type="object">
                  The default health policy applied to a service type's own steady-state health evaluation. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="max_percent_unhealthy_services" type="number">
                      The maximum percentage of services permitted to be unhealthy at steady state. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="service_type_health_policies" type="map(object)">
                  Per-service-type health policy override(s) for steady-state health evaluation. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="max_percent_unhealthy_services" type="number">
                      The maximum percentage of services permitted to be unhealthy at steady state. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_percent_unhealthy_applications" type="number">
              The maximum allowed percentage of unhealthy applications before reporting an error. For example, to allow 10% of applications to be unhealthy, this value would be 10. The percentage represents the maximum tolerated percentage of applications that can be unhealthy before the cluster is considered in error. If the percentage is respected but there is at least one unhealthy application, the health is evaluated as Warning. This is calculated by dividing the number of unhealthy applications over the total number of application instances in the cluster, excluding applications of application types that are included in the ApplicationTypeHealthPolicyMap. The computation rounds up to tolerate one failure on small numbers of applications. Default percentage is zero. Optional.
            </ResponseField>
            <ResponseField name="max_percent_unhealthy_nodes" type="number">
              The maximum allowed percentage of unhealthy nodes before reporting an error. For example, to allow 10% of nodes to be unhealthy, this value would be 10. The percentage represents the maximum tolerated percentage of nodes that can be unhealthy before the cluster is considered in error. If the percentage is respected but there is at least one unhealthy node, the health is evaluated as Warning. The percentage is calculated by dividing the number of unhealthy nodes over the total number of nodes in the cluster. The computation rounds up to tolerate one failure on small numbers of nodes. Default percentage is zero. In large clusters, some nodes will always be down or out for repairs, so this percentage should be configured to tolerate that. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="upgrade_domain_timeout" type="string" required>
          The amount of time each upgrade domain has to complete before the upgrade rolls back. The timeout can be in either hh:mm:ss or in d.hh:mm:ss.ms format. Required.
        </ResponseField>
        <ResponseField name="upgrade_replica_set_check_timeout" type="string" required>
          The maximum amount of time to block processing of an upgrade domain and prevent loss of availability when there are unexpected issues. When this timeout expires, processing of the upgrade domain will proceed regardless of availability loss issues. The timeout is reset at the start of each upgrade domain. The timeout can be in either hh:mm:ss or in d.hh:mm:ss.ms format. Required.
        </ResponseField>
        <ResponseField name="upgrade_timeout" type="string" required>
          The amount of time the overall upgrade has to complete before the upgrade rolls back. The timeout can be in either hh:mm:ss or in d.hh:mm:ss.ms format. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="upgrade_mode" type="string">
      The upgrade mode of the cluster when new Service Fabric runtime version is available. Optional.
    </ResponseField>
    <ResponseField name="upgrade_pause_end_timestamp_utc" type="string">
      Indicates the end date and time to pause automatic runtime version upgrades on the cluster for an specific period of time on the cluster (UTC). Optional.
    </ResponseField>
    <ResponseField name="upgrade_pause_start_timestamp_utc" type="string">
      Indicates the start date and time to pause automatic runtime version upgrades on the cluster for an specific period of time on the cluster (UTC). Optional.
    </ResponseField>
    <ResponseField name="upgrade_wave" type="string">
      Indicates when new cluster runtime version upgrades will be applied after they are released. By default is Wave0. Optional.
    </ResponseField>
    <ResponseField name="vm_image" type="string">
      The VM image VMSS has been configured with. Generic names such as Windows or Linux can be used. Optional.
    </ResponseField>
    <ResponseField name="vmss_zonal_upgrade_mode" type="string">
      This property defines the upgrade mode for the virtual machine scale set, it is mandatory if a node type with multiple Availability Zones is added. Optional.
    </ResponseField>
    <ResponseField name="wave_upgrade_paused" type="bool">
      Boolean to pause automatic runtime version upgrades to the cluster. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
