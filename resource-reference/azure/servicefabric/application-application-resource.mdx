---
title: "azure_servicefabric_application_application_resource"
description: "`azure_servicefabric_application_application_resource` is a running application instance deployed to a Service Fabric cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_servicefabric_application --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_servicefabric_application/sdk/go
    package main

    import (
    	servicefabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_servicefabric_application/servicefabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own application application resource"})

    		ubx.Resource(servicefabric.ApplicationApplicationResource, "ci-runner", servicefabric.ApplicationApplicationResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_servicefabric_application --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationApplicationResource } from "./local-sdk/azure_servicefabric_application/sdk/typescript/azure_servicefabric_application/servicefabric/application_application_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own application application resource" });

      resource(ApplicationApplicationResource, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_servicefabric_application --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_servicefabric_application/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_servicefabric_application.servicefabric import ApplicationApplicationResource, ApplicationApplicationResourceConfig

    def describe():
        ubx.intent("payments own application application resource")

        ubx.resource(ApplicationApplicationResource, "ci-runner", ApplicationApplicationResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicefabric_application_application_resource, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Describes the managed identities for an Azure resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal id of the managed identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id of the managed identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of managed identity for the resource. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  The application resource properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      The current deployment or provisioning state, which only appears in the response Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type_name" type="string">
      The application type name as defined in the application manifest. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
