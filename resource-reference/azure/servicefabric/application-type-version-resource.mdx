---
title: "azure_servicefabric_application_type_version_resource"
description: "`azure_servicefabric_application_type_version_resource` is a specific, provisioned version of an application type on a Service Fabric cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_servicefabric_application --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_servicefabric_application/sdk/go
    package main

    import (
    	servicefabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_servicefabric_application/servicefabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own application type version resource"})

    		ubx.Resource(servicefabric.ApplicationApplicationTypeVersionResource, "example", servicefabric.ApplicationApplicationTypeVersionResourceConfig{
    			Version: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_servicefabric_application --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationApplicationTypeVersionResource } from "./local-sdk/azure_servicefabric_application/sdk/typescript/azure_servicefabric_application/servicefabric/application_application_type_version_resource.ts";

    export default stack("example", () => {
      intent({ summary: "example own application type version resource" });

      resource(ApplicationApplicationTypeVersionResource, "example", {
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_servicefabric_application --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_servicefabric_application/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_servicefabric_application.servicefabric import ApplicationApplicationTypeVersionResource, ApplicationApplicationTypeVersionResourceConfig

    def describe():
        ubx.intent("example own application type version resource")

        ubx.resource(ApplicationApplicationTypeVersionResource, "example", ApplicationApplicationTypeVersionResourceConfig(
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicefabric_application_type_version_resource,
    with version example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of the application type version resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="app_package_url" type="string" required>
      The URL to the application package Required.
    </ResponseField>
    <ResponseField name="default_parameter_list" type="map(string)">
      List of application type parameters that can be overridden when creating or updating the application. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The current deployment or provisioning state, which only appears in the response Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
