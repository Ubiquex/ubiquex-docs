---
title: "azure_servicefabric_application_type_version_resource"
description: "`azure_servicefabric_application_type_version_resource` is a specific, provisioned version of an application type on a Service Fabric cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicefabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/servicefabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own application type version resource"})

    		ubx.Resource(servicefabric.ApplicationTypeVersionResource, "example", servicefabric.ApplicationTypeVersionResourceConfig{
    			Version: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationTypeVersionResource } from "@ubx/sdk-azure/azure/servicefabric/application_type_version_resource";

    export default stack("example", () => {
      intent({ summary: "example own application type version resource" });

      resource(ApplicationTypeVersionResource, "example", {
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.servicefabric.application_type_version_resource import ApplicationTypeVersionResource, ApplicationTypeVersionResourceConfig

    def describe():
        ubx.intent("example own application type version resource")

        ubx.resource(ApplicationTypeVersionResource, "example", ApplicationTypeVersionResourceConfig(
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicefabric_application_type_version_resource,
    with version example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of the application type version resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="app_package_url" type="string" required>
      The URL to the application package Required.
    </ResponseField>
    <ResponseField name="default_parameter_list" type="map(string)">
      List of application type parameters that can be overridden when creating or updating the application. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The current deployment or provisioning state, which only appears in the response Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
