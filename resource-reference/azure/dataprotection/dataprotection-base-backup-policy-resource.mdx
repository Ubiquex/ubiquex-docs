---
title: "azure_dataprotection_base_backup_policy_resource"
description: "`azure_dataprotection_base_backup_policy_resource` defines a backup policy in the Data Protection service -- how often backups run and how long each..."
---

`azure_dataprotection_base_backup_policy_resource` defines a backup policy in the Data Protection service -- how often backups run and how long each retention tier keeps them, for one or more datasource types.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_dataprotection_dataprotection --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_dataprotection_dataprotection/sdk/go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_dataprotection_dataprotection/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dataprotection base backup policy resource"})

    		ubx.Resource(dataprotection.DataprotectionBaseBackupPolicyResource, "ci-runner", dataprotection.DataprotectionBaseBackupPolicyResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_dataprotection_dataprotection --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataprotectionBaseBackupPolicyResource } from "./local-sdk/azure_dataprotection_dataprotection/sdk/typescript/azure_dataprotection_dataprotection/dataprotection/dataprotection_base_backup_policy_resource.ts";

    export default stack("payments", () => {
      intent({
        summary: "payments own dataprotection base backup policy resource",
      });

      resource(DataprotectionBaseBackupPolicyResource, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_dataprotection_dataprotection --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_dataprotection_dataprotection/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_dataprotection_dataprotection.dataprotection import DataprotectionBaseBackupPolicyResource, DataprotectionBaseBackupPolicyResourceConfig

    def describe():
        ubx.intent("payments own dataprotection base backup policy resource")

        ubx.resource(DataprotectionBaseBackupPolicyResource, "ci-runner", DataprotectionBaseBackupPolicyResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dataprotection_base_backup_policy_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  BackupPolicy base Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="datasource_types" type="list(string)" required>
      Type of datasource for the backup management Required.
    </ResponseField>
    <ResponseField name="object_type" type="string" required>
      A discriminator identifying this object's own concrete type, used when a field can hold more than one shape. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
