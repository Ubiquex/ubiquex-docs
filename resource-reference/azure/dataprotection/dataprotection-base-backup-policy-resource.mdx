---
title: "azure_dataprotection_base_backup_policy_resource"
description: "`azure_dataprotection_base_backup_policy_resource` defines a backup policy in the Data Protection service -- how often backups run and how long each..."
---

`azure_dataprotection_base_backup_policy_resource` defines a backup policy in the Data Protection service -- how often backups run and how long each retention tier keeps them, for one or more datasource types.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dataprotection base backup policy resource"})

    		ubx.Resource(dataprotection.BaseBackupPolicyResource, "example", dataprotection.BaseBackupPolicyResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BaseBackupPolicyResource } from "@ubx/sdk-azure/azure/dataprotection/base_backup_policy_resource";

    export default stack("example", () => {
      intent({
        summary: "example own dataprotection base backup policy resource",
      });

      resource(BaseBackupPolicyResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.dataprotection.base_backup_policy_resource import BaseBackupPolicyResource, BaseBackupPolicyResourceConfig

    def describe():
        ubx.intent("example own dataprotection base backup policy resource")

        ubx.resource(BaseBackupPolicyResource, "example", BaseBackupPolicyResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dataprotection_base_backup_policy_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  BackupPolicy base Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="datasource_types" type="list(string)" required>
      Type of datasource for the backup management Required.
    </ResponseField>
    <ResponseField name="object_type" type="string" required>
      A discriminator identifying this object's own concrete type, used when a field can hold more than one shape. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
