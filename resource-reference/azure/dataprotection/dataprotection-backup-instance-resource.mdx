---
title: "azure_dataprotection_backup_instance_resource"
description: "`azure_dataprotection_backup_instance_resource` represents one datasource (e.g. a disk, a Kubernetes cluster..."
---

`azure_dataprotection_backup_instance_resource` represents one datasource (e.g. a disk, a Kubernetes cluster, a database) actually configured for backup within a Backup vault, linking it to a backup policy.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_dataprotection_dataprotection --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_dataprotection_dataprotection/sdk/go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_dataprotection_dataprotection/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dataprotection backup instance resource"})

    		ubx.Resource(dataprotection.DataprotectionBackupInstanceResource, "example", dataprotection.DataprotectionBackupInstanceResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_dataprotection_dataprotection --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataprotectionBackupInstanceResource } from "./local-sdk/azure_dataprotection_dataprotection/sdk/typescript/azure_dataprotection_dataprotection/dataprotection/dataprotection_backup_instance_resource.ts";

    export default stack("example", () => {
      intent({ summary: "example own dataprotection backup instance resource" });

      resource(DataprotectionBackupInstanceResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_dataprotection_dataprotection --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_dataprotection_dataprotection/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_dataprotection_dataprotection.dataprotection import DataprotectionBackupInstanceResource, DataprotectionBackupInstanceResourceConfig

    def describe():
        ubx.intent("example own dataprotection backup instance resource")

        ubx.resource(DataprotectionBackupInstanceResource, "example", DataprotectionBackupInstanceResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dataprotection_backup_instance_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Backup Instance Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="current_protection_state" type="string">
      Specifies the current protection state of the resource Optional.
    </ResponseField>
    <ResponseField name="data_source_info" type="object" required>
      Datasource to be backed up Required.
      <Expandable title="properties">
        <ResponseField name="datasource_type" type="string">
          DatasourceType of the resource. Optional.
        </ResponseField>
        <ResponseField name="object_type" type="string">
          Type of Datasource object, used to initialize the right inherited type Optional.
        </ResponseField>
        <ResponseField name="resource_id" type="string" required>
          Full ARM ID of the resource. For azure resources, this is ARM ID. For non azure resources, this will be the ID created by backup service via Fabric/Vault. Required.
        </ResponseField>
        <ResponseField name="resource_location" type="string">
          Location of datasource. Optional.
        </ResponseField>
        <ResponseField name="resource_name" type="string">
          Unique identifier of the resource in the context of parent. Optional.
        </ResponseField>
        <ResponseField name="resource_properties" type="object">
          Properties which are specific to datasource/datasourceSets Optional.
          <Expandable title="properties">
            <ResponseField name="object_type" type="string" required>
              Type of the specific object - used for deserializing Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource_type" type="string">
          Resource Type of Datasource. Optional.
        </ResponseField>
        <ResponseField name="resource_uri" type="string">
          Uri of the resource. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="data_source_set_info" type="object">
      DatasourceSet details of datasource to be backed up Optional.
      <Expandable title="properties">
        <ResponseField name="datasource_type" type="string">
          DatasourceType of the resource. Optional.
        </ResponseField>
        <ResponseField name="object_type" type="string">
          Type of Datasource object, used to initialize the right inherited type Optional.
        </ResponseField>
        <ResponseField name="resource_id" type="string" required>
          Full ARM ID of the resource. For azure resources, this is ARM ID. For non azure resources, this will be the ID created by backup service via Fabric/Vault. Required.
        </ResponseField>
        <ResponseField name="resource_location" type="string">
          Location of datasource. Optional.
        </ResponseField>
        <ResponseField name="resource_name" type="string">
          Unique identifier of the resource in the context of parent. Optional.
        </ResponseField>
        <ResponseField name="resource_properties" type="object">
          Properties which are specific to datasource/datasourceSets Optional.
          <Expandable title="properties">
            <ResponseField name="object_type" type="string" required>
              Type of the specific object - used for deserializing Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource_type" type="string">
          Resource Type of Datasource. Optional.
        </ResponseField>
        <ResponseField name="resource_uri" type="string">
          Uri of the resource. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="datasource_auth_credentials" type="object">
      Base class for different types of authentication credentials. Optional.
      <Expandable title="properties">
        <ResponseField name="object_type" type="string" required>
          Type of the specific object - used for deserializing Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="friendly_name" type="string">
      Gets or sets the Backup Instance friendly name. Optional.
    </ResponseField>
    <ResponseField name="identity_details" type="object">
      Detail about the managed identity used to protect or access this resource. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="use_system_assigned_identity" type="bool">
          Specifies if the BI is protected by System Identity. Optional.
        </ResponseField>
        <ResponseField name="user_assigned_identity_arm_url" type="string">
          ARM URL for User Assigned Identity. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="object_type" type="string" required>
      A discriminator identifying this object's own concrete type, used when a field can hold more than one shape. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="policy_info" type="object" required>
      Policy Info in backupInstance Required.
      <Expandable title="properties">
        <ResponseField name="policy_id" type="string" required>
          A reference to the backup policy this applies to. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="policy_parameters" type="object">
          Parameters in Policy Optional.
          <Expandable title="properties">
            <ResponseField name="backup_datasource_parameters_list" type="list(object)">
              Gets or sets the Backup Data Source Parameters Optional.
              <Expandable title="properties">
                <ResponseField name="object_type" type="string">
                  A discriminator identifying this object's own concrete type, used when a field can hold more than one shape. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="data_store_parameters_list" type="list(object)">
              Gets or sets the DataStore Parameters Optional.
              <Expandable title="properties">
                <ResponseField name="data_store_type" type="string">
                  The type of datastore this backup data is retained in, e.g. `OperationalStore` or `VaultStore`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="object_type" type="string">
                  A discriminator identifying this object's own concrete type, used when a field can hold more than one shape. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="policy_version" type="string">
          The version of the backup policy in effect when this was created. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protection_error_details" type="object">
      Error object used by layers that have access to localized content, and propagate that to user Optional.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          Unique code for this error Optional.
        </ResponseField>
        <ResponseField name="details" type="list(object)">
          Additional related Errors Optional.
        </ResponseField>
        <ResponseField name="inner_error" type="object">
          Inner Error Optional.
          <Expandable title="properties">
            <ResponseField name="additional_info" type="map(string)">
              Any Key value pairs that can be provided to the client for additional verbose information. Optional.
            </ResponseField>
            <ResponseField name="code" type="string">
              Unique code for this error Optional.
            </ResponseField>
            <ResponseField name="embedded_inner_error" type="any">
              Inner Error Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="is_retryable" type="bool">
          Whether the operation will be retryable or not Optional.
        </ResponseField>
        <ResponseField name="is_user_error" type="bool">
          Whether the operation is due to a user error or service error Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of this result. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="map(string)">
          Any key value pairs that can be injected inside error object Optional.
        </ResponseField>
        <ResponseField name="recommended_action" type="list(string)">
          RecommendedAction � localized. Optional.
        </ResponseField>
        <ResponseField name="target" type="string">
          Target of the error. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protection_status" type="object">
      Protection status details Optional.
      <Expandable title="properties">
        <ResponseField name="error_details" type="object">
          Error object used by layers that have access to localized content, and propagate that to user Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              Unique code for this error Optional.
            </ResponseField>
            <ResponseField name="details" type="list(object)">
              Additional related Errors Optional.
            </ResponseField>
            <ResponseField name="inner_error" type="object">
              Inner Error Optional.
              <Expandable title="properties">
                <ResponseField name="additional_info" type="map(string)">
                  Any Key value pairs that can be provided to the client for additional verbose information. Optional.
                </ResponseField>
                <ResponseField name="code" type="string">
                  Unique code for this error Optional.
                </ResponseField>
                <ResponseField name="embedded_inner_error" type="any">
                  Inner Error Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="is_retryable" type="bool">
              Whether the operation will be retryable or not Optional.
            </ResponseField>
            <ResponseField name="is_user_error" type="bool">
              Whether the operation is due to a user error or service error Optional.
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this result. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="properties" type="map(string)">
              Any key value pairs that can be injected inside error object Optional.
            </ResponseField>
            <ResponseField name="recommended_action" type="list(string)">
              RecommendedAction � localized. Optional.
            </ResponseField>
            <ResponseField name="target" type="string">
              Target of the error. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="status" type="string">
          Specifies the protection status of the resource Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Specifies the provisioning state of the resource i.e. provisioning/updating/Succeeded/Failed Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="resource_guard_operation_requests" type="list(string)">
      ResourceGuardOperationRequests on which LAC check will be performed Optional.
    </ResponseField>
    <ResponseField name="validation_type" type="string">
      Specifies the type of validation. In case of DeepValidation, all validations from /validateForBackup API will run again. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Proxy Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
