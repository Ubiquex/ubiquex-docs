---
title: "azure_dataprotection_backup_vault_resource"
description: "`azure_dataprotection_backup_vault_resource` is a Backup vault..."
---

`azure_dataprotection_backup_vault_resource` is a Backup vault, the Azure Backup Data Protection service's own storage and management container for backup data and policies -- the modern, unified successor to the original Recovery Services vault model.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dataprotection backup vault resource"})

    		ubx.Resource(dataprotection.BackupVaultResource, "example", dataprotection.BackupVaultResourceConfig{
    			Properties: map[string]any{"bcdr_security_level": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BackupVaultResource } from "@ubx/sdk-azure/azure/dataprotection/backup_vault_resource";

    export default stack("example", () => {
      intent({ summary: "example own dataprotection backup vault resource" });

      resource(BackupVaultResource, "example", {
        properties: { bcdrSecurityLevel: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.dataprotection import BackupVaultResource, BackupVaultResourceConfig

    def describe():
        ubx.intent("example own dataprotection backup vault resource")

        ubx.resource(BackupVaultResource, "example", BackupVaultResourceConfig(
            properties={"bcdr_security_level": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dataprotection_backup_vault_resource, with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  Optional ETag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="identity" type="object">
  Identity details Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The object ID of the service principal object for the managed identity that is used to grant role-based access to an Azure resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      A Globally Unique Identifier (GUID) that represents the Azure AD tenant where the resource is now a member. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      The identityType which can be either SystemAssigned, UserAssigned, 'SystemAssigned,UserAssigned' or None Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      Gets or sets the user assigned identities. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object" required>
  Backup Vault Required.
  <Expandable title="properties">
    <ResponseField name="bcdr_security_level" type="string">
      Security Level of Backup Vault Optional.
    </ResponseField>
    <ResponseField name="cost_management_settings" type="object">
      Cost Management Settings of the vault Optional.
      <Expandable title="properties">
        <ResponseField name="granularity_level" type="string">
          The level of detail this applies at. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="feature_settings" type="object">
      Class containing feature settings of vault Optional.
      <Expandable title="properties">
        <ResponseField name="cross_region_restore_settings" type="object">
          Configuration for restoring backup data into a different Azure region than where it was backed up from. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              CrossRegionRestore state Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="cross_subscription_restore_settings" type="object">
          CrossSubscriptionRestore Settings Optional.
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              CrossSubscriptionRestore state Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_vault_protected_by_resource_guard" type="bool">
      Is vault protected by resource guard Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="monitoring_settings" type="object">
      Monitoring Settings Optional.
      <Expandable title="properties">
        <ResponseField name="azure_monitor_alert_settings" type="object">
          Settings for Azure Monitor based alerts Optional.
          <Expandable title="properties">
            <ResponseField name="alerts_for_all_job_failures" type="string">
              Whether alerts are raised for every failed backup job, not just critical ones. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the BackupVault resource Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="replicated_regions" type="list(string)">
      List of replicated regions for Backup Vault Optional.
    </ResponseField>
    <ResponseField name="resource_guard_operation_requests" type="list(string)">
      ResourceGuardOperationRequests on which LAC check will be performed Optional.
    </ResponseField>
    <ResponseField name="resource_move_details" type="object">
      ResourceMoveDetails will be returned in response to GetResource call from ARM Optional.
      <Expandable title="properties">
        <ResponseField name="completion_time_utc" type="string">
          Completion time in UTC of latest ResourceMove operation attempted. ISO 8601 format. Optional.
        </ResponseField>
        <ResponseField name="operation_id" type="string">
          CorrelationId of latest ResourceMove operation attempted Optional.
        </ResponseField>
        <ResponseField name="source_resource_path" type="string">
          ARM resource path of source resource Optional.
        </ResponseField>
        <ResponseField name="start_time_utc" type="string">
          Start time in UTC of latest ResourceMove operation attempted. ISO 8601 format. Optional.
        </ResponseField>
        <ResponseField name="target_resource_path" type="string">
          ARM resource path of target resource used in latest ResourceMove operation Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_move_state" type="string">
      Resource move state for backup vault Optional.
    </ResponseField>
    <ResponseField name="secure_score" type="string">
      Secure Score of Backup Vault Optional.
    </ResponseField>
    <ResponseField name="security_settings" type="object">
      Class containing security settings of vault Optional.
      <Expandable title="properties">
        <ResponseField name="encryption_settings" type="object">
          Customer Managed Key details of the resource. Optional.
          <Expandable title="properties">
            <ResponseField name="infrastructure_encryption" type="string">
              Enabling/Disabling the Double Encryption state Optional.
            </ResponseField>
            <ResponseField name="kek_identity" type="object">
              The details of the managed identity used for CMK Optional.
              <Expandable title="properties">
                <ResponseField name="identity_id" type="string">
                  The managed identity to be used which has access permissions to the Key Vault. Provide a value here in case identity types: 'UserAssigned' only. Optional.
                </ResponseField>
                <ResponseField name="identity_type" type="string">
                  The identity type. 'SystemAssigned' and 'UserAssigned' are mutually exclusive. 'SystemAssigned' will use implicitly created managed identity. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="key_vault_properties" type="object">
              The properties of the Key Vault which hosts CMK Optional.
              <Expandable title="properties">
                <ResponseField name="key_uri" type="string">
                  The key uri of the Customer Managed Key Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="state" type="string">
              Encryption state of the Backup Vault. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="immutability_settings" type="object">
          Immutability Settings at vault level Optional.
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              Immutability state Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="soft_delete_settings" type="object">
          Soft delete related settings Optional.
          <Expandable title="properties">
            <ResponseField name="retention_duration_in_days" type="number">
              Soft delete retention duration Optional.
            </ResponseField>
            <ResponseField name="state" type="string">
              State of soft delete Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="storage_settings" type="list(object)">
      Storage Settings Optional.
      <Expandable title="properties">
        <ResponseField name="datastore_type" type="string">
          The type of datastore this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
