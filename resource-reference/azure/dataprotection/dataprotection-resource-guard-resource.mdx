---
title: "azure_dataprotection_resource_guard_resource"
description: "`azure_dataprotection_resource_guard_resource` is a Resource Guard..."
---

`azure_dataprotection_resource_guard_resource` is a Resource Guard, the Azure resource multi-user authorization (MUA) uses to require an independent approver -- owned by a different user than the vault admin -- before a critical Backup vault operation can proceed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_dataprotection_dataprotection --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_dataprotection_dataprotection/sdk/go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_dataprotection_dataprotection/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dataprotection resource guard resource"})

    		ubx.Resource(dataprotection.DataprotectionResourceGuardResource, "ci-runner", dataprotection.DataprotectionResourceGuardResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_dataprotection_dataprotection --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataprotectionResourceGuardResource } from "./local-sdk/azure_dataprotection_dataprotection/sdk/typescript/azure_dataprotection_dataprotection/dataprotection/dataprotection_resource_guard_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own dataprotection resource guard resource" });

      resource(DataprotectionResourceGuardResource, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_dataprotection_dataprotection --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_dataprotection_dataprotection/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_dataprotection_dataprotection.dataprotection import DataprotectionResourceGuardResource, DataprotectionResourceGuardResourceConfig

    def describe():
        ubx.intent("payments own dataprotection resource guard resource")

        ubx.resource(DataprotectionResourceGuardResource, "ci-runner", DataprotectionResourceGuardResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dataprotection_resource_guard_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  Optional ETag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="allow_auto_approvals" type="bool">
      This flag indicates whether auto approval is allowed or not. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="description" type="string">
      Description about the pre-req steps to perform all the critical operations. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the BackupVault resource Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="resource_guard_operations" type="list(object)">
      &#123;readonly&#125; List of operation details those are protected by the ResourceGuard resource Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="request_resource_type" type="string">
          The resource type this request applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vault_critical_operation" type="string">
          The type of security-critical operation this applies to, e.g. disabling soft delete or changing immutability. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vault_critical_operation_exclusion_list" type="list(string)">
      List of critical operations which are not protected by this resourceGuard Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
