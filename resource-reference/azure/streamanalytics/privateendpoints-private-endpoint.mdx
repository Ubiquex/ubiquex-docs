---
title: "azure_streamanalytics_privateendpoints_private_endpoint"
description: "`azure_streamanalytics_privateendpoints_private_endpoint` creates a Private Endpoint from a Stream Analytics cluster to a private data source or..."
---

`azure_streamanalytics_privateendpoints_private_endpoint` creates a Private Endpoint from a Stream Analytics cluster to a private data source or destination.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_streamanalytics_privateendpoints --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_streamanalytics_privateendpoints/sdk/go
    package main

    import (
    	streamanalytics "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_streamanalytics_privateendpoints/streamanalytics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own privateendpoints private endpoint"})

    		ubx.Resource(streamanalytics.PrivateendpointsPrivateEndpoint, "example", streamanalytics.PrivateendpointsPrivateEndpointConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_streamanalytics_privateendpoints --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivateendpointsPrivateEndpoint } from "./local-sdk/azure_streamanalytics_privateendpoints/sdk/typescript/azure_streamanalytics_privateendpoints/streamanalytics/privateendpoints_private_endpoint.ts";

    export default stack("example", () => {
      intent({ summary: "example own privateendpoints private endpoint" });

      resource(PrivateendpointsPrivateEndpoint, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_streamanalytics_privateendpoints --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_streamanalytics_privateendpoints/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_streamanalytics_privateendpoints.streamanalytics import PrivateendpointsPrivateEndpoint, PrivateendpointsPrivateEndpointConfig

    def describe():
        ubx.intent("example own privateendpoints private endpoint")

        ubx.resource(PrivateendpointsPrivateEndpoint, "example", PrivateendpointsPrivateEndpointConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_streamanalytics_privateendpoints_private_endpoint,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Unique opaque string (generally a GUID) that represents the metadata state of the resource (private endpoint) and changes whenever the resource is updated. Required on PUT (CreateOrUpdate) requests. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties associated with a private endpoint. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_date" type="string">
      The date when this private endpoint was created. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="manual_private_link_service_connections" type="list(object)">
      A list of connections to the remote resource. Immutable after it is set. Optional.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
