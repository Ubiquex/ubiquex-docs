---
title: "azure_streamanalytics_inputs_input"
description: "`azure_streamanalytics_inputs_input` defines a source (Event Hub, IoT Hub, Blob Storage) a Stream Analytics job reads real-time data from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_streamanalytics_inputs --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_streamanalytics_inputs/sdk/go
    package main

    import (
    	streamanalytics "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_streamanalytics_inputs/streamanalytics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own inputs input"})

    		ubx.Resource(streamanalytics.InputsInput, "ci-runner", streamanalytics.InputsInputConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_streamanalytics_inputs --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { InputsInput } from "./local-sdk/azure_streamanalytics_inputs/sdk/typescript/azure_streamanalytics_inputs/streamanalytics/inputs_input.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own inputs input" });

      resource(InputsInput, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_streamanalytics_inputs --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_streamanalytics_inputs/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_streamanalytics_inputs.streamanalytics import InputsInput, InputsInputConfig

    def describe():
        ubx.intent("payments own inputs input")

        ubx.resource(InputsInput, "ci-runner", InputsInputConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_streamanalytics_inputs_input, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties that are associated with an input. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="compression" type="object">
      Describes how input data is compressed Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string" required>
          Indicates the type of compression that the input uses. Required on PUT (CreateOrReplace) requests. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="diagnostics" type="object">
      Describes conditions applicable to the Input, Output, or the job overall, that warrant customer attention. Optional.
      <Expandable title="properties">
        <ResponseField name="conditions" type="list(object)">
          A collection of zero or more conditions applicable to the resource, or to the job overall, that warrant customer attention. Computed by Microsoft Azure after `ship`.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              A short, machine-readable diagnostic code identifying this input condition, e.g. `INP-3`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this input diagnostic condition. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="since" type="string">
              When this diagnostic condition was first observed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="etag" type="string">
      The current entity tag for the input. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="partition_key" type="string">
      partitionKey Describes a key in the input data which is used for partitioning the input data Optional.
    </ResponseField>
    <ResponseField name="serialization" type="object">
      Describes how data from an input is serialized or how data is serialized when written to an output. Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string" required>
          Indicates the type of serialization that the input or output uses. Required on PUT (CreateOrReplace) requests. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Indicates whether the input is a source of reference data or stream data. Required on PUT (CreateOrReplace) requests. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
