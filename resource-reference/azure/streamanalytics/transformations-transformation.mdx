---
title: "azure_streamanalytics_transformations_transformation"
description: "`azure_streamanalytics_transformations_transformation` defines the SQL-like query a Stream Analytics job runs to transform its own input stream(s) into..."
---

`azure_streamanalytics_transformations_transformation` defines the SQL-like query a Stream Analytics job runs to transform its own input stream(s) into output.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_streamanalytics_transformations --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_streamanalytics_transformations/sdk/go
    package main

    import (
    	streamanalytics "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_streamanalytics_transformations/streamanalytics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transformations transformation"})

    		ubx.Resource(streamanalytics.TransformationsTransformation, "ci-runner", streamanalytics.TransformationsTransformationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_streamanalytics_transformations --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransformationsTransformation } from "./local-sdk/azure_streamanalytics_transformations/sdk/typescript/azure_streamanalytics_transformations/streamanalytics/transformations_transformation.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own transformations transformation" });

      resource(TransformationsTransformation, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_streamanalytics_transformations --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_streamanalytics_transformations/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_streamanalytics_transformations.streamanalytics import TransformationsTransformation, TransformationsTransformationConfig

    def describe():
        ubx.intent("payments own transformations transformation")

        ubx.resource(TransformationsTransformation, "ci-runner", TransformationsTransformationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_streamanalytics_transformations_transformation, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties that are associated with a transformation. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="etag" type="string">
      The current entity tag for the transformation. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="query" type="string">
      Specifies the query that will be run in the streaming job. You can learn more about the Stream Analytics Query Language (SAQL) here: https://msdn.microsoft.com/library/azure/dn834998 . Required on PUT (CreateOrReplace) requests. Optional.
    </ResponseField>
    <ResponseField name="streaming_units" type="number">
      Specifies the number of streaming units that the streaming job uses. Optional.
    </ResponseField>
    <ResponseField name="valid_streaming_units" type="list(number)">
      Specifies the valid streaming units a streaming job can scale to. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
