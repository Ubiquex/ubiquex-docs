---
title: "azure_applicationinsights_workbooks_workbook"
description: "`azure_applicationinsights_workbooks_workbook` is an Azure Monitor workbook, an interactive report combining text, metrics, logs..."
---

`azure_applicationinsights_workbooks_workbook` is an Azure Monitor workbook, an interactive report combining text, metrics, logs, and parameters into a single canvas for analysis and dashboards.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_applicationinsights_workbooks --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_applicationinsights_workbooks/sdk/go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_applicationinsights_workbooks/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own workbooks workbook"})

    		ubx.Resource(applicationinsights.WorkbooksWorkbook, "example", applicationinsights.WorkbooksWorkbookConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_applicationinsights_workbooks --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkbooksWorkbook } from "./local-sdk/azure_applicationinsights_workbooks/sdk/typescript/azure_applicationinsights_workbooks/applicationinsights/workbooks_workbook.ts";

    export default stack("example", () => {
      intent({ summary: "example own workbooks workbook" });

      resource(WorkbooksWorkbook, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_applicationinsights_workbooks --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_applicationinsights_workbooks/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_applicationinsights_workbooks.applicationinsights import WorkbooksWorkbook, WorkbooksWorkbookConfig

    def describe():
        ubx.intent("example own workbooks workbook")

        ubx.resource(WorkbooksWorkbook, "example", WorkbooksWorkbookConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_applicationinsights_workbooks_workbook, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A value used for optimistic concurrency control on this applicationinsights workbooks workbook, changed by the server on every update. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
</ResponseField>

<ResponseField name="identity" type="object">
  Identity used for BYOS Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Output only. The Microsoft Entra ID tenant this resource's own managed identity belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The user-assigned managed identity/identities associated with this workbook, keyed by resource ID. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  The kind of workbook. Only valid value is shared. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties that contain a workbook. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="category" type="string" required>
      Workbook category, as defined by the user at creation time. Required.
    </ResponseField>
    <ResponseField name="description" type="string">
      The description of the workbook. Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string" required>
      The user-defined name (display name) of the workbook. Required.
    </ResponseField>
    <ResponseField name="revision" type="string">
      The unique revision id for this workbook definition Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="serialized_data" type="string" required>
      Configuration of this particular workbook. Configuration data is a string containing valid JSON Required.
    </ResponseField>
    <ResponseField name="source_id" type="string">
      ResourceId for a source resource. Optional.
    </ResponseField>
    <ResponseField name="storage_uri" type="string">
      The resourceId to the storage account when bring your own storage is used Optional.
    </ResponseField>
    <ResponseField name="tags" type="list(string)">
      Being deprecated, please use the other tags field Optional.
    </ResponseField>
    <ResponseField name="time_modified" type="string">
      Date and time in UTC of the last modification that was made to this workbook definition. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="user_id" type="string">
      Unique user id of the specific user that owns this workbook. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="version" type="string">
      Workbook schema version format, like 'Notebook/1.0', which should match the workbook in serializedData Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
