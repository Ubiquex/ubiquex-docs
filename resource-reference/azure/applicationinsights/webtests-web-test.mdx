---
title: "azure_applicationinsights_webtests_web_test"
description: "`azure_applicationinsights_webtests_web_test` is an Application Insights web test..."
---

`azure_applicationinsights_webtests_web_test` is an Application Insights web test, a periodic synthetic HTTP request run from multiple global locations to monitor a URL's own availability and response time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own webtests web test"})

    		ubx.Resource(applicationinsights.WebtestsWebTest, "example", applicationinsights.WebtestsWebTestConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WebtestsWebTest } from "@ubx/sdk-azure/azure/applicationinsights/webtests_web_test";

    export default stack("example", () => {
      intent({ summary: "example own webtests web test" });

      resource(WebtestsWebTest, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.applicationinsights.webtests_web_test import WebtestsWebTest, WebtestsWebTestConfig

    def describe():
        ubx.intent("example own webtests web test")

        ubx.resource(WebtestsWebTest, "example", WebtestsWebTestConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_applicationinsights_webtests_web_test, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string">
  The kind of web test that this web test watches. Choices are ping and multistep. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Metadata describing a web test for an Azure resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="configuration" type="object">
      An XML configuration specification for a WebTest. Optional.
      <Expandable title="properties">
        <ResponseField name="web_test" type="string">
          The XML specification of a WebTest to run against an application. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      Purpose/user defined descriptive test for this WebTest. Optional.
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      Is the test actively being monitored. Optional.
    </ResponseField>
    <ResponseField name="frequency" type="number">
      Interval in seconds between test runs for this WebTest. Default value is 300. Optional.
    </ResponseField>
    <ResponseField name="kind" type="string" required>
      The kind of web test this is, valid choices are ping and multistep. Required.
    </ResponseField>
    <ResponseField name="locations" type="list(object)" required>
      A list of where to physically run the tests from to give global coverage for accessibility of your application. Required.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string" required>
      User defined name if this WebTest. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Current state of this component, whether or not is has been provisioned within the resource group it is defined. Users cannot change this value but are able to read from it. Values will include Succeeded, Deploying, Canceled, and Failed. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="retry_enabled" type="bool">
      Allow for retries should this WebTest fail. Optional.
    </ResponseField>
    <ResponseField name="synthetic_monitor_id" type="string" required>
      Unique ID of this WebTest. This is typically the same value as the Name field. Required.
    </ResponseField>
    <ResponseField name="timeout" type="number">
      Seconds until this WebTest will timeout and fail. Default value is 30. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
