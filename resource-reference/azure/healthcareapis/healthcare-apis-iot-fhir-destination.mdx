---
title: "azure_healthcareapis_healthcare_apis_iot_fhir_destination"
description: "`azure_healthcareapis_healthcare_apis_iot_fhir_destination` defines the mapping rules an IoT connector uses to transform incoming device data into FHIR..."
---

`azure_healthcareapis_healthcare_apis_iot_fhir_destination` defines the mapping rules an IoT connector uses to transform incoming device data into FHIR resource(s) written to a specific FHIR service.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_healthcareapis_healthcare_apis --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_healthcareapis_healthcare_apis/sdk/go
    package main

    import (
    	healthcareapis "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_healthcareapis_healthcare_apis/healthcareapis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own healthcare apis iot fhir destination"})

    		ubx.Resource(healthcareapis.HealthcareApisIotFhirDestination, "example", healthcareapis.HealthcareApisIotFhirDestinationConfig{
    			Properties: map[string]any{"fhir_mapping": map[string]any{"content": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_healthcareapis_healthcare_apis --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HealthcareApisIotFhirDestination } from "./local-sdk/azure_healthcareapis_healthcare_apis/sdk/typescript/azure_healthcareapis_healthcare_apis/healthcareapis/healthcare_apis_iot_fhir_destination.ts";

    export default stack("example", () => {
      intent({ summary: "example own healthcare apis iot fhir destination" });

      resource(HealthcareApisIotFhirDestination, "example", {
        properties: { fhirMapping: { content: "example" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_healthcareapis_healthcare_apis --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_healthcareapis_healthcare_apis/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_healthcareapis_healthcare_apis.healthcareapis import HealthcareApisIotFhirDestination, HealthcareApisIotFhirDestinationConfig

    def describe():
        ubx.intent("example own healthcare apis iot fhir destination")

        ubx.resource(HealthcareApisIotFhirDestination, "example", HealthcareApisIotFhirDestinationConfig(
            properties={"fhir_mapping": {"content": "example"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_healthcareapis_healthcare_apis_iot_fhir_destination,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  IoT Connector destination properties for an Azure FHIR service. Required.
  <Expandable title="properties">
    <ResponseField name="fhir_mapping" type="object" required>
      The mapping content. Required.
      <Expandable title="properties">
        <ResponseField name="content" type="any">
          The content for the healthcareapis healthcare apis iot fhir destination's fhir mapping. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fhir_service_resource_id" type="string" required>
      Fully qualified resource id of the FHIR service to connect to. Required.
    </ResponseField>
    <ResponseField name="resource_identity_resolution_type" type="string" required>
      The type of IoT identity resolution to use with the destination. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
