---
title: "azure_interconnect_block"
description: "A complete, runnable azure_interconnect_block program, in every SDK language."
---

`azure_interconnect_block` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	interconnect "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/interconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own block"})

    		ubx.Resource(interconnect.Block, "example", interconnect.BlockConfig{
    			Sku: map[string]any{"name": "example-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Block } from "./local-sdk/azure/sdk/typescript/azure/interconnect/block.ts";

    export default stack("example", () => {
      intent({ summary: "example own block" });

      resource(Block, "example", {
        sku: { name: "example-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.interconnect import Block, BlockConfig

    def describe():
        ubx.intent("example own block")

        ubx.resource(Block, "example", BlockConfig(
            sku={"name": "example-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_interconnect_block, with sku a set value.
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="placement" type="object">
  Describes the user-defined constraints for resource hardware placement. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="exclude_zones" type="list(string)">
      This property supplements the 'zonePlacementPolicy' property. If 'zonePlacementPolicy' is set to 'Any'/'Auto', availability zone selected by the system must not be present in the list of availability zones passed with 'excludeZones'. If 'excludeZones' is not provided, all availability zones in region will be considered for selection. Optional.
    </ResponseField>
    <ResponseField name="include_zones" type="list(string)">
      This property supplements the 'zonePlacementPolicy' property. If 'zonePlacementPolicy' is set to 'Any'/'Auto', availability zone selected by the system must be present in the list of availability zones passed with 'includeZones'. If 'includeZones' is not provided, all availability zones in region will be considered for selection. Optional.
    </ResponseField>
    <ResponseField name="zone_placement_policy" type="string">
      Specifies the policy for resource's placement in availability zone. Possible values are: **Any** (used for Virtual Machines), **Auto** (used for Virtual Machine Scale Sets) - An availability zone will be automatically picked by system as part of resource creation. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the Interconnect Block. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="instance_view" type="object">
      The instance view of an Interconnect Block. Optional.
      <Expandable title="properties">
        <ResponseField name="current_capacity" type="number">
          The current capacity allocated for this Interconnect Block. Computed by Azure after `ship`.
        </ResponseField>
        <ResponseField name="statuses" type="list(object)">
          The resource status information. Computed by Azure after `ship`.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The status code for the instance view of the Azure interconnect block. This is a machine-readable code that indicates the current status. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              A human-readable string that describes the current status of the Azure interconnect block. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the status. Possible values are Info, Warning, and Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing additional details about the corresponding status in the instance view. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The time at which the status was recorded, typically in a date-time format. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="interconnect_block_id" type="string">
      A unique id (GUID) generated and assigned to the Interconnect Block by the platform which does not change throughout the lifetime of the resource. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="interconnect_group" type="object" required>
      The API entity reference. Required.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The ARM resource id in the form of /subscriptions/&#123;SubscriptionId&#125;/resourceGroups/&#123;ResourceGroupName&#125;/... Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_time" type="string">
      The date time when the Interconnect Block was last updated. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="time_created" type="string">
      Specifies the time at which the Interconnect Block resource was created. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="virtual_machines_associated" type="list(object)">
      A list of all virtual machine resource ids that are associated with the Interconnect Block. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object" required>
  Describes a virtual machine scale set sku. NOTE: If the new VM SKU is not supported on the hardware the scale set is currently on, you need to deallocate the VMs in the scale set before you modify the SKU name. Required.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      Specifies the number of virtual machines in the scale set. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the interconnect block's SKU. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tier" type="string">
      Specifies the tier of virtual machines in a scale set.&lt;br /&gt;&lt;br /&gt; Possible Values:&lt;br /&gt;&lt;br /&gt; **Standard**&lt;br /&gt;&lt;br /&gt; **Basic** Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The availability zones. Optional; if omitted, computed by Azure.
</ResponseField>

## Output properties

<ResponseField name="placement" type="object">
  Describes the user-defined constraints for resource hardware placement. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="exclude_zones" type="list(string)">
      This property supplements the 'zonePlacementPolicy' property. If 'zonePlacementPolicy' is set to 'Any'/'Auto', availability zone selected by the system must not be present in the list of availability zones passed with 'excludeZones'. If 'excludeZones' is not provided, all availability zones in region will be considered for selection. Optional.
    </ResponseField>
    <ResponseField name="include_zones" type="list(string)">
      This property supplements the 'zonePlacementPolicy' property. If 'zonePlacementPolicy' is set to 'Any'/'Auto', availability zone selected by the system must be present in the list of availability zones passed with 'includeZones'. If 'includeZones' is not provided, all availability zones in region will be considered for selection. Optional.
    </ResponseField>
    <ResponseField name="zone_placement_policy" type="string">
      Specifies the policy for resource's placement in availability zone. Possible values are: **Any** (used for Virtual Machines), **Auto** (used for Virtual Machine Scale Sets) - An availability zone will be automatically picked by system as part of resource creation. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the Interconnect Block. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="instance_view" type="object">
      The instance view of an Interconnect Block. Optional.
      <Expandable title="properties">
        <ResponseField name="current_capacity" type="number">
          The current capacity allocated for this Interconnect Block. Computed by Azure after `ship`.
        </ResponseField>
        <ResponseField name="statuses" type="list(object)">
          The resource status information. Computed by Azure after `ship`.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The status code for the instance view of the Azure interconnect block. This is a machine-readable code that indicates the current status. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              A human-readable string that describes the current status of the Azure interconnect block. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the status. Possible values are Info, Warning, and Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing additional details about the corresponding status in the instance view. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The time at which the status was recorded, typically in a date-time format. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="interconnect_block_id" type="string">
      A unique id (GUID) generated and assigned to the Interconnect Block by the platform which does not change throughout the lifetime of the resource. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="interconnect_group" type="object" required>
      The API entity reference. Required.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The ARM resource id in the form of /subscriptions/&#123;SubscriptionId&#125;/resourceGroups/&#123;ResourceGroupName&#125;/... Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_time" type="string">
      The date time when the Interconnect Block was last updated. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="time_created" type="string">
      Specifies the time at which the Interconnect Block resource was created. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="virtual_machines_associated" type="list(object)">
      A list of all virtual machine resource ids that are associated with the Interconnect Block. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The availability zones. Optional; if omitted, computed by Azure.
</ResponseField>
