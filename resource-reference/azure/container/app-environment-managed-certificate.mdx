---
title: "azurerm_container_app_environment_managed_certificate"
description: "Real, generated bindings for `azurerm_container_app_environment_managed_certificate`."
---

`azurerm_container_app_environment_managed_certificate` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	container "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/container"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app environment managed certificate"})

    		ubx.Resource(container.AppEnvironmentManagedCertificate, "ci-runner", container.AppEnvironmentManagedCertificateConfig{
    			ContainerAppEnvironmentId: "example",
    			Name:                      "ci-runner",
    			SubjectName:               "example-subject-name",
    			DomainControlValidation:   "example",
    			Tags:                      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppEnvironmentManagedCertificate } from "jsr:@ubx/sdk-azure/azurerm/container/app_environment_managed_certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own app environment managed certificate" });

      resource(AppEnvironmentManagedCertificate, "ci-runner", {
        containerAppEnvironmentId: "example",
        name: "ci-runner",
        subjectName: "example-subject-name",
        domainControlValidation: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.container import AppEnvironmentManagedCertificate, AppEnvironmentManagedCertificateConfig

    def describe():
        ubx.intent("payments own app environment managed certificate")

        ubx.resource(AppEnvironmentManagedCertificate, "ci-runner", AppEnvironmentManagedCertificateConfig(
            container_app_environment_id="example",
            name="ci-runner",
            subject_name="example-subject-name",
            domain_control_validation="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_container_app_environment_managed_certificate
    called "ci-runner", with container app environment id example,
    subject name example-subject-name, domain control validation
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="container_app_environment_id" type="string" required>
  The Container App Managed Environment ID to configure this Managed Certificate on. Required.
</ResponseField>

<ResponseField name="domain_control_validation" type="string">
  The domain control validation type for the managed certificate. Possible values are `CNAME` and `HTTP`. Defaults to `HTTP`. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the Container Apps Managed Certificate. Required.
</ResponseField>

<ResponseField name="subject_name" type="string" required>
  The Subject Name of the Certificate. Must be a valid domain name. Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="validation_token" type="string">
  The validation token for the managed certificate. Computed by Azure after `ship`.
</ResponseField>
