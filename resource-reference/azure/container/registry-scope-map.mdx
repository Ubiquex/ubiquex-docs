---
title: "azurerm_container_registry_scope_map"
description: "Real, generated bindings for `azurerm_container_registry_scope_map`."
---

`azurerm_container_registry_scope_map` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	container "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/container"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own registry scope map"})

    		ubx.Resource(container.RegistryScopeMap, "ci-runner", container.RegistryScopeMapConfig{
    			Actions:               []string{"example"},
    			ContainerRegistryName: "example-container-registry-name",
    			Name:                  "ci-runner",
    			ResourceGroupName:     "example-resource-group-name",
    			Description:           "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegistryScopeMap } from "jsr:@ubx/sdk-azure/azurerm/container/registry_scope_map";

    export default stack("payments", () => {
      intent({ summary: "payments own registry scope map" });

      resource(RegistryScopeMap, "ci-runner", {
        actions: ["example"],
        containerRegistryName: "example-container-registry-name",
        name: "ci-runner",
        resourceGroupName: "example-resource-group-name",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.container import RegistryScopeMap, RegistryScopeMapConfig

    def describe():
        ubx.intent("payments own registry scope map")

        ubx.resource(RegistryScopeMap, "ci-runner", RegistryScopeMapConfig(
            actions=["example"],
            container_registry_name="example-container-registry-name",
            name="ci-runner",
            resource_group_name="example-resource-group-name",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_container_registry_scope_map called "ci-runner",
    with actions a set value, container registry name
    example-container-registry-name, resource group name
    example-resource-group-name, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="actions" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="container_registry_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
