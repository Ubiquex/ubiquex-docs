---
title: "azurerm_container_app_environment_certificate"
description: "Real, generated bindings for `azurerm_container_app_environment_certificate`."
---

`azurerm_container_app_environment_certificate` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	container "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/container"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app environment certificate"})

    		ubx.Resource(container.AppEnvironmentCertificate, "ci-runner", container.AppEnvironmentCertificateConfig{
    			ContainerAppEnvironmentId: "example",
    			Name:                      "ci-runner",
    			CertificateBlobBase64:     "example",
    			CertificatePassword:       "example",
    			Tags:                      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppEnvironmentCertificate } from "jsr:@ubx/sdk-azure/azurerm/container/app_environment_certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own app environment certificate" });

      resource(AppEnvironmentCertificate, "ci-runner", {
        containerAppEnvironmentId: "example",
        name: "ci-runner",
        certificateBlobBase64: "example",
        certificatePassword: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.container import AppEnvironmentCertificate, AppEnvironmentCertificateConfig

    def describe():
        ubx.intent("payments own app environment certificate")

        ubx.resource(AppEnvironmentCertificate, "ci-runner", AppEnvironmentCertificateConfig(
            container_app_environment_id="example",
            name="ci-runner",
            certificate_blob_base64="example",
            certificate_password="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_container_app_environment_certificate called
    "ci-runner", with container app environment id example,
    certificate blob base64 example, certificate password example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate_blob_base64" type="string">
  Optional.
</ResponseField>

<ResponseField name="certificate_key_vault" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="identity" type="string">
    </ResponseField>
    <ResponseField name="key_vault_secret_id" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="certificate_password" type="string">
  Optional.
</ResponseField>

<ResponseField name="container_app_environment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="certificate_key_vault" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="identity" type="string">
    </ResponseField>
    <ResponseField name="key_vault_secret_id" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="expiration_date" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="issue_date" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="issuer" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="subject_name" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="thumbprint" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
