---
title: "azurerm_container_registry_webhook"
description: "Real, generated bindings for `azurerm_container_registry_webhook`."
---

`azurerm_container_registry_webhook` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	container "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/container"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own registry webhook"})

    		ubx.Resource(container.RegistryWebhook, "ci-runner", container.RegistryWebhookConfig{
    			Actions:           []string{"example"},
    			Location:          "example",
    			Name:              "ci-runner",
    			RegistryName:      "example-registry-name",
    			ResourceGroupName: "example-resource-group-name",
    			ServiceUri:        "example",
    			CustomHeaders:     map[string]string{"managed-by": "ubx"},
    			Scope:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegistryWebhook } from "jsr:@ubx/sdk-azure/azurerm/container/registry_webhook";

    export default stack("payments", () => {
      intent({ summary: "payments own registry webhook" });

      resource(RegistryWebhook, "ci-runner", {
        actions: ["example"],
        location: "example",
        name: "ci-runner",
        registryName: "example-registry-name",
        resourceGroupName: "example-resource-group-name",
        serviceUri: "example",
        customHeaders: { "managed-by": "ubx" },
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.container import RegistryWebhook, RegistryWebhookConfig

    def describe():
        ubx.intent("payments own registry webhook")

        ubx.resource(RegistryWebhook, "ci-runner", RegistryWebhookConfig(
            actions=["example"],
            location="example",
            name="ci-runner",
            registry_name="example-registry-name",
            resource_group_name="example-resource-group-name",
            service_uri="example",
            custom_headers={"managed-by": "ubx"},
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_container_registry_webhook called "ci-runner",
    with actions a set value, location example, registry name
    example-registry-name, resource group name
    example-resource-group-name, service uri example, custom headers
    a set value, scope example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="actions" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="custom_headers" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="scope" type="string">
  Optional.
</ResponseField>

<ResponseField name="service_uri" type="string" required>
  Required.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
