---
title: "azure_dashboard_grafana_managed_grafana"
description: "`azure_dashboard_grafana_managed_grafana` is an Azure Managed Grafana instance..."
---

`azure_dashboard_grafana_managed_grafana` is an Azure Managed Grafana instance, a fully managed deployment of Grafana for visualizing metrics from Azure Monitor and other data sources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_dashboard_grafana --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_dashboard_grafana/sdk/go
    package main

    import (
    	dashboard "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_dashboard_grafana/dashboard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own grafana managed grafana"})

    		ubx.Resource(dashboard.GrafanaManagedGrafana, "example", dashboard.GrafanaManagedGrafanaConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_dashboard_grafana --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GrafanaManagedGrafana } from "./local-sdk/azure_dashboard_grafana/sdk/typescript/azure_dashboard_grafana/dashboard/grafana_managed_grafana.ts";

    export default stack("example", () => {
      intent({ summary: "example own grafana managed grafana" });

      resource(GrafanaManagedGrafana, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_dashboard_grafana --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_dashboard_grafana/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_dashboard_grafana.dashboard import GrafanaManagedGrafana, GrafanaManagedGrafanaConfig

    def describe():
        ubx.intent("example own grafana managed grafana")

        ubx.resource(GrafanaManagedGrafana, "example", GrafanaManagedGrafanaConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dashboard_grafana_managed_grafana, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties specific to the grafana resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="api_key" type="string">
      The api key setting of the Grafana instance. Optional.
    </ResponseField>
    <ResponseField name="auto_generated_domain_name_label_scope" type="string">
      Scope for dns deterministic name hash calculation Optional.
    </ResponseField>
    <ResponseField name="creator_can_admin" type="string">
      The creator will have admin access for the Grafana instance. Optional.
    </ResponseField>
    <ResponseField name="deterministic_outbound_ip" type="string">
      Whether a Grafana instance uses deterministic outbound IPs. Optional.
    </ResponseField>
    <ResponseField name="endpoint" type="string">
      The endpoint of the Grafana instance. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="enterprise_configurations" type="object">
      Enterprise settings of a Grafana instance Optional.
      <Expandable title="properties">
        <ResponseField name="marketplace_auto_renew" type="string">
          The AutoRenew setting of the Enterprise subscription Optional.
        </ResponseField>
        <ResponseField name="marketplace_plan_id" type="string">
          The Plan Id of the Azure Marketplace subscription for the Enterprise plugins Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="grafana_configurations" type="object">
      Server configurations of a Grafana instance Optional.
      <Expandable title="properties">
        <ResponseField name="security" type="object">
          Grafana security settings Optional.
          <Expandable title="properties">
            <ResponseField name="csrf_always_check" type="bool">
              Set to true to execute the CSRF check even if the login cookie is not in a request (default false). Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="smtp" type="object">
          Email server settings. https://grafana.com/docs/grafana/v9.0/setup-grafana/configure-grafana/#smtp Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Enable this to allow Grafana to send email. Default is false Optional.
            </ResponseField>
            <ResponseField name="from_address" type="string">
              Address used when sending out emails https://pkg.go.dev/net/mail#Address Optional.
            </ResponseField>
            <ResponseField name="from_name" type="string">
              Name to be used when sending out emails. Default is "Azure Managed Grafana Notification" https://pkg.go.dev/net/mail#Address Optional.
            </ResponseField>
            <ResponseField name="host" type="string">
              SMTP server hostname with port, e.g. test.email.net:587 Optional.
            </ResponseField>
            <ResponseField name="password" type="string">
              Password of SMTP auth. If the password contains # or ;, then you have to wrap it with triple quotes Optional.
            </ResponseField>
            <ResponseField name="skip_verify" type="bool">
              Verify SSL for SMTP server. Default is false https://pkg.go.dev/crypto/tls#Config Optional.
            </ResponseField>
            <ResponseField name="start_tlspolicy" type="string">
              The StartTLSPolicy setting of the SMTP configuration https://pkg.go.dev/github.com/go-mail/mail#StartTLSPolicy Optional.
            </ResponseField>
            <ResponseField name="user" type="string">
              User of SMTP auth Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="snapshots" type="object">
          Grafana Snapshots settings Optional.
          <Expandable title="properties">
            <ResponseField name="external_enabled" type="bool">
              Set to false to disable external snapshot publish endpoint Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="unified_alerting_screenshots" type="object">
          Grafana Unified Alerting Screenshots settings Optional.
          <Expandable title="properties">
            <ResponseField name="capture_enabled" type="bool">
              Set to false to disable capture screenshot in Unified Alert due to performance issue. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="users" type="object">
          Grafana users settings Optional.
          <Expandable title="properties">
            <ResponseField name="editors_can_admin" type="bool">
              Set to true so editors can administrate dashboards, folders and teams they create. Optional.
            </ResponseField>
            <ResponseField name="viewers_can_edit" type="bool">
              Set to true so viewers can access and use explore and perform temporary edits on panels in dashboards they have access to. They cannot save their changes. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="grafana_integrations" type="object">
      GrafanaIntegrations is a bundled observability experience (e.g. pre-configured data source, tailored Grafana dashboards, alerting defaults) for common monitoring scenarios. Optional.
      <Expandable title="properties">
        <ResponseField name="azure_monitor_workspace_integrations" type="list(object)">
          The Azure Monitor workspace(s) this Grafana instance is integrated with. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="azure_monitor_workspace_resource_id" type="string">
              A reference to the Azure Monitor workspace this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="grafana_major_version" type="string">
      The major Grafana software version to target. Optional.
    </ResponseField>
    <ResponseField name="grafana_plugins" type="map(object)">
      Installed plugin list of the Grafana instance. Key is plugin id, value is plugin definition. Optional.
      <Expandable title="properties">
        <ResponseField name="plugin_id" type="string">
          A reference to the Grafana plugin this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="grafana_version" type="string">
      The Grafana software version. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="outbound_ips" type="list(string)">
      List of outbound IPs if deterministicOutboundIP is enabled. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      The private endpoint connections of the Grafana instance. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The last, current provisioning status ARM reported for this resource, e.g. `Succeeded`, `Failed`, or `Updating`. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Indicate the state for enable or disable traffic over the public interface. Optional.
    </ResponseField>
    <ResponseField name="zone_redundancy" type="string">
      The zone redundancy setting of the Grafana instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  Represents the SKU of a resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      The name of the SKU. Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      Specifies the capacity tier of the Grafana instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
