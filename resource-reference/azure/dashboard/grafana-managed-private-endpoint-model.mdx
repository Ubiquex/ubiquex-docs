---
title: "azure_dashboard_grafana_managed_private_endpoint_model"
description: "`azure_dashboard_grafana_managed_private_endpoint_model` creates a Private Endpoint from an Azure Managed Grafana instance to a private data source..."
---

`azure_dashboard_grafana_managed_private_endpoint_model` creates a Private Endpoint from an Azure Managed Grafana instance to a private data source, so it can query it without traversing the public internet.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_dashboard_grafana --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_dashboard_grafana/sdk/go
    package main

    import (
    	dashboard "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_dashboard_grafana/dashboard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own grafana managed private endpoint model"})

    		ubx.Resource(dashboard.GrafanaManagedPrivateEndpointModel, "example", dashboard.GrafanaManagedPrivateEndpointModelConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_dashboard_grafana --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GrafanaManagedPrivateEndpointModel } from "./local-sdk/azure_dashboard_grafana/sdk/typescript/azure_dashboard_grafana/dashboard/grafana_managed_private_endpoint_model.ts";

    export default stack("example", () => {
      intent({ summary: "example own grafana managed private endpoint model" });

      resource(GrafanaManagedPrivateEndpointModel, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_dashboard_grafana --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_dashboard_grafana/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_dashboard_grafana.dashboard import GrafanaManagedPrivateEndpointModel, GrafanaManagedPrivateEndpointModelConfig

    def describe():
        ubx.intent("example own grafana managed private endpoint model")

        ubx.resource(GrafanaManagedPrivateEndpointModel, "example", GrafanaManagedPrivateEndpointModelConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dashboard_grafana_managed_private_endpoint_model,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties specific to the managed private endpoint. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="connection_state" type="object">
      The state of managed private endpoint connection. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Gets or sets the reason for approval/rejection of the connection. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="status" type="string">
          The approval/rejection status of managed private endpoint connection. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="group_ids" type="list(string)">
      The group Ids of the managed private endpoint. Optional.
    </ResponseField>
    <ResponseField name="private_link_resource_id" type="string">
      The ARM resource ID of the resource for which the managed private endpoint is pointing to. Optional.
    </ResponseField>
    <ResponseField name="private_link_resource_region" type="string">
      The region of the resource to which the managed private endpoint is pointing to. Optional.
    </ResponseField>
    <ResponseField name="private_link_service_private_ip" type="string">
      The private IP of private endpoint after approval. This property is empty before connection is approved. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="private_link_service_url" type="string">
      The URL of the data store behind the private link service. It would be the URL in the Grafana data source configuration page without the protocol and port. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The last, current provisioning status ARM reported for this resource, e.g. `Succeeded`, `Failed`, or `Updating`. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="request_message" type="string">
      User input request message of the managed private endpoint. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
