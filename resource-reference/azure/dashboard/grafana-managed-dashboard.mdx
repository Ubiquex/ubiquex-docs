---
title: "azure_dashboard_grafana_managed_dashboard"
description: "`azure_dashboard_grafana_managed_dashboard` is a Grafana dashboard resource managed and versioned as an Azure resource within an Azure Managed Grafana..."
---

`azure_dashboard_grafana_managed_dashboard` is a Grafana dashboard resource managed and versioned as an Azure resource within an Azure Managed Grafana instance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_dashboard_grafana --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_dashboard_grafana/sdk/go
    package main

    import (
    	dashboard "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_dashboard_grafana/dashboard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own grafana managed dashboard"})

    		ubx.Resource(dashboard.GrafanaManagedDashboard, "ci-runner", dashboard.GrafanaManagedDashboardConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_dashboard_grafana --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GrafanaManagedDashboard } from "./local-sdk/azure_dashboard_grafana/sdk/typescript/azure_dashboard_grafana/dashboard/grafana_managed_dashboard.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own grafana managed dashboard" });

      resource(GrafanaManagedDashboard, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_dashboard_grafana --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_dashboard_grafana/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_dashboard_grafana.dashboard import GrafanaManagedDashboard, GrafanaManagedDashboardConfig

    def describe():
        ubx.intent("payments own grafana managed dashboard")

        ubx.resource(GrafanaManagedDashboard, "ci-runner", GrafanaManagedDashboardConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dashboard_grafana_managed_dashboard, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties specific to the grafana resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      The last, current provisioning status ARM reported for this resource, e.g. `Succeeded`, `Failed`, or `Updating`. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
