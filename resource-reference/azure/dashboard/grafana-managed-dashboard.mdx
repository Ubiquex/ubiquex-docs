---
title: "azure_dashboard_grafana_managed_dashboard"
description: "`azure_dashboard_grafana_managed_dashboard` is a Grafana dashboard resource managed and versioned as an Azure resource within an Azure Managed Grafana..."
---

`azure_dashboard_grafana_managed_dashboard` is a Grafana dashboard resource managed and versioned as an Azure resource within an Azure Managed Grafana instance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dashboard "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/dashboard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own grafana managed dashboard"})

    		ubx.Resource(dashboard.GrafanaManagedDashboard, "example", dashboard.GrafanaManagedDashboardConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GrafanaManagedDashboard } from "@ubx/sdk-azure/azure/dashboard/grafana_managed_dashboard";

    export default stack("example", () => {
      intent({ summary: "example own grafana managed dashboard" });

      resource(GrafanaManagedDashboard, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.dashboard.grafana_managed_dashboard import GrafanaManagedDashboard, GrafanaManagedDashboardConfig

    def describe():
        ubx.intent("example own grafana managed dashboard")

        ubx.resource(GrafanaManagedDashboard, "example", GrafanaManagedDashboardConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dashboard_grafana_managed_dashboard, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties specific to the grafana resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      The last, current provisioning status ARM reported for this resource, e.g. `Succeeded`, `Failed`, or `Updating`. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
