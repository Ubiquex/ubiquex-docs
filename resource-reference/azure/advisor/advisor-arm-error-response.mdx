---
title: "azure_advisor_arm_error_response"
description: "`azure_advisor_advisor_arm_error_response` is Azure Advisor's own standard API error envelope..."
---

`azure_advisor_advisor_arm_error_response` is Azure Advisor's own standard API error envelope, returned when a request to the Advisor management API fails -- not a deployable resource in its own right.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_advisor_advisor --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_advisor_advisor/sdk/go
    package main

    import (
    	advisor "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_advisor_advisor/advisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own advisor arm error response"})

    		ubx.Resource(advisor.AdvisorArmErrorResponse, "ci-runner", advisor.AdvisorArmErrorResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_advisor_advisor --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AdvisorArmErrorResponse } from "./local-sdk/azure_advisor_advisor/sdk/typescript/azure_advisor_advisor/advisor/advisor_arm_error_response.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own advisor arm error response" });

      resource(AdvisorArmErrorResponse, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_advisor_advisor --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_advisor_advisor/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_advisor_advisor.advisor import AdvisorArmErrorResponse, AdvisorArmErrorResponseConfig

    def describe():
        ubx.intent("payments own advisor arm error response")

        ubx.resource(AdvisorArmErrorResponse, "ci-runner", AdvisorArmErrorResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_advisor_arm_error_response, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="error" type="object">
  ARM error response body. Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
      Gets or sets the string that can be used to programmatically identify the error. Optional.
    </ResponseField>
    <ResponseField name="message" type="string">
      Gets or sets the string that describes the error in detail and provides debugging information. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
