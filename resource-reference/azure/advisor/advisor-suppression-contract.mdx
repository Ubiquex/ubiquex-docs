---
title: "azure_advisor_suppression_contract"
description: "`azure_advisor_advisor_suppression_contract` suppresses a specific Azure Advisor recommendation for a resource..."
---

`azure_advisor_advisor_suppression_contract` suppresses a specific Azure Advisor recommendation for a resource, either permanently or for a defined time window.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_advisor_advisor --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_advisor_advisor/sdk/go
    package main

    import (
    	advisor "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_advisor_advisor/advisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own advisor suppression contract"})

    		ubx.Resource(advisor.AdvisorSuppressionContract, "example", advisor.AdvisorSuppressionContractConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_advisor_advisor --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AdvisorSuppressionContract } from "./local-sdk/azure_advisor_advisor/sdk/typescript/azure_advisor_advisor/advisor/advisor_suppression_contract.ts";

    export default stack("example", () => {
      intent({ summary: "example own advisor suppression contract" });

      resource(AdvisorSuppressionContract, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_advisor_advisor --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_advisor_advisor/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_advisor_advisor.advisor import AdvisorSuppressionContract, AdvisorSuppressionContractConfig

    def describe():
        ubx.intent("example own advisor suppression contract")

        ubx.resource(AdvisorSuppressionContract, "example", AdvisorSuppressionContractConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_advisor_suppression_contract called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the suppression. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="expiration_time_stamp" type="string">
      Gets or sets the expiration time stamp. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="suppression_id" type="string">
      The GUID of the suppression. Optional.
    </ResponseField>
    <ResponseField name="ttl" type="string">
      The duration for which the suppression is valid. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
