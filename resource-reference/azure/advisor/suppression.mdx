---
title: "azurerm_advisor_suppression"
description: "Real, generated bindings for `azurerm_advisor_suppression`."
---

`azurerm_advisor_suppression` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import (
        "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/advisor"
    )

    cfg := advisor.SuppressionConfig{
        Name: "example-name",
        RecommendationId: "example",
        ResourceId: "example",
        Ttl: "example",
    }
    ubx.Resource(advisor.Suppression, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Suppression } from
      "jsr:@ubx/sdk-azure/azurerm/advisor/suppression";

    resource(Suppression, "example", {
      name: "example-name",
      recommendationId: "example",
      resourceId: "example",
      ttl: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.azurerm.advisor import Suppression, SuppressionConfig

    cfg = SuppressionConfig(
        name="example-name",
        recommendation_id="example",
        resource_id="example",
        ttl="example",
    )
    ubx.resource(Suppression, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision azurerm_advisor_suppression with name,
recommendation_id, resource_id, ttl.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="recommendation_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ttl" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="suppression_id" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
