---
title: "azurerm_advisor_suppression"
description: "Real, generated bindings for `azurerm_advisor_suppression`."
---

`azurerm_advisor_suppression` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	advisor "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/advisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own suppression"})

    		ubx.Resource(advisor.Suppression, "ci-runner", advisor.SuppressionConfig{
    			Name:             "ci-runner",
    			RecommendationId: "example",
    			ResourceId:       "example",
    			Ttl:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Suppression } from "jsr:@ubx/sdk-azure/azurerm/advisor/suppression";

    export default stack("payments", () => {
      intent({ summary: "payments own suppression" });

      resource(Suppression, "ci-runner", {
        name: "ci-runner",
        recommendationId: "example",
        resourceId: "example",
        ttl: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.advisor import Suppression, SuppressionConfig

    def describe():
        ubx.intent("payments own suppression")

        ubx.resource(Suppression, "ci-runner", SuppressionConfig(
            name="ci-runner",
            recommendation_id="example",
            resource_id="example",
            ttl="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_advisor_suppression called "ci-runner", with
    recommendation id example, resource id example, ttl example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="recommendation_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ttl" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="suppression_id" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
