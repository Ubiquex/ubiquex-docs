---
title: "azure_msi_managedidentity_identity"
description: "`azure_msi_managedidentity_identity` is a user-assigned managed identity..."
---

`azure_msi_managedidentity_identity` is a user-assigned managed identity, an Azure AD identity that can be assigned to one or more resource(s) for passwordless authentication to other Azure services.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_msi_managedidentity --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_msi_managedidentity/sdk/go
    package main

    import (
    	msi "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_msi_managedidentity/msi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own managedidentity identity"})

    		ubx.Resource(msi.ManagedidentityIdentity, "ci-runner", msi.ManagedidentityIdentityConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_msi_managedidentity --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedidentityIdentity } from "./local-sdk/azure_msi_managedidentity/sdk/typescript/azure_msi_managedidentity/msi/managedidentity_identity.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own managedidentity identity" });

      resource(ManagedidentityIdentity, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_msi_managedidentity --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_msi_managedidentity/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_msi_managedidentity.msi import ManagedidentityIdentity, ManagedidentityIdentityConfig

    def describe():
        ubx.intent("payments own managedidentity identity")

        ubx.resource(ManagedidentityIdentity, "ci-runner", ManagedidentityIdentityConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_msi_managedidentity_identity, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties associated with the user assigned identity. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="client_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="isolation_scope" type="string">
      Enum to configure regional restrictions on identity assignment, as necessary. Optional.
    </ResponseField>
    <ResponseField name="principal_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
