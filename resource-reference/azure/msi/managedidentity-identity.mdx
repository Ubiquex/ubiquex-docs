---
title: "azure_msi_managedidentity_identity"
description: "`azure_msi_managedidentity_identity` is a user-assigned managed identity..."
---

`azure_msi_managedidentity_identity` is a user-assigned managed identity, an Azure AD identity that can be assigned to one or more resource(s) for passwordless authentication to other Azure services.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	msi "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/msi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managedidentity identity"})

    		ubx.Resource(msi.ManagedidentityIdentity, "example", msi.ManagedidentityIdentityConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedidentityIdentity } from "@ubx/sdk-azure/azure/msi/managedidentity_identity";

    export default stack("example", () => {
      intent({ summary: "example own managedidentity identity" });

      resource(ManagedidentityIdentity, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.msi.managedidentity_identity import ManagedidentityIdentity, ManagedidentityIdentityConfig

    def describe():
        ubx.intent("example own managedidentity identity")

        ubx.resource(ManagedidentityIdentity, "example", ManagedidentityIdentityConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_msi_managedidentity_identity, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties associated with the user assigned identity. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="client_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="isolation_scope" type="string">
      Enum to configure regional restrictions on identity assignment, as necessary. Optional.
    </ResponseField>
    <ResponseField name="principal_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
