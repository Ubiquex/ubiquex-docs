---
title: "azure_msi_managedidentity_federated_identity_credential"
description: "`azure_msi_managedidentity_federated_identity_credential` establishes trust between a user-assigned managed identity and an external OIDC identity..."
---

`azure_msi_managedidentity_federated_identity_credential` establishes trust between a user-assigned managed identity and an external OIDC identity provider (e.g. a Kubernetes service account or GitHub Actions), enabling passwordless workload identity federation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	msi "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/msi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managedidentity federated identity credential"})

    		ubx.Resource(msi.ManagedidentityFederatedIdentityCredential, "example", msi.ManagedidentityFederatedIdentityCredentialConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedidentityFederatedIdentityCredential } from "@ubx/sdk-azure/azure/msi/managedidentity_federated_identity_credential";

    export default stack("example", () => {
      intent({
        summary: "example own managedidentity federated identity credential",
      });

      resource(ManagedidentityFederatedIdentityCredential, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.msi import ManagedidentityFederatedIdentityCredential, ManagedidentityFederatedIdentityCredentialConfig

    def describe():
        ubx.intent("example own managedidentity federated identity credential")

        ubx.resource(ManagedidentityFederatedIdentityCredential, "example", ManagedidentityFederatedIdentityCredentialConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_msi_managedidentity_federated_identity_credential,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties associated with a federated identity credential. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="audiences" type="list(string)" required>
      The list of audiences that can appear in the issued token. Required.
    </ResponseField>
    <ResponseField name="issuer" type="string" required>
      The URL of the issuer to be trusted. Required.
    </ResponseField>
    <ResponseField name="subject" type="string" required>
      The identifier of the external identity. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
