---
title: "azure_msi_managedidentity_federated_identity_credential"
description: "`azure_msi_managedidentity_federated_identity_credential` establishes trust between a user-assigned managed identity and an external OIDC identity..."
---

`azure_msi_managedidentity_federated_identity_credential` establishes trust between a user-assigned managed identity and an external OIDC identity provider (e.g. a Kubernetes service account or GitHub Actions), enabling passwordless workload identity federation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_msi_managedidentity --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_msi_managedidentity/sdk/go
    package main

    import (
    	msi "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_msi_managedidentity/msi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own managedidentity federated identity credential"})

    		ubx.Resource(msi.ManagedidentityFederatedIdentityCredential, "ci-runner", msi.ManagedidentityFederatedIdentityCredentialConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_msi_managedidentity --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedidentityFederatedIdentityCredential } from "./local-sdk/azure_msi_managedidentity/sdk/typescript/azure_msi_managedidentity/msi/managedidentity_federated_identity_credential.ts";

    export default stack("payments", () => {
      intent({
        summary: "payments own managedidentity federated identity credential",
      });

      resource(ManagedidentityFederatedIdentityCredential, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_msi_managedidentity --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_msi_managedidentity/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_msi_managedidentity.msi import ManagedidentityFederatedIdentityCredential, ManagedidentityFederatedIdentityCredentialConfig

    def describe():
        ubx.intent("payments own managedidentity federated identity credential")

        ubx.resource(ManagedidentityFederatedIdentityCredential, "ci-runner", ManagedidentityFederatedIdentityCredentialConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_msi_managedidentity_federated_identity_credential,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties associated with a federated identity credential. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="audiences" type="list(string)" required>
      The list of audiences that can appear in the issued token. Required.
    </ResponseField>
    <ResponseField name="issuer" type="string" required>
      The URL of the issuer to be trusted. Required.
    </ResponseField>
    <ResponseField name="subject" type="string" required>
      The identifier of the external identity. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
