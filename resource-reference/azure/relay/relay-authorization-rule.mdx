---
title: "azure_relay_authorization_rule"
description: "`azure_relay_authorization_rule` defines a shared access policy (with specific permissions) for connecting to an Azure Relay namespace..."
---

`azure_relay_authorization_rule` defines a shared access policy (with specific permissions) for connecting to an Azure Relay namespace, hybrid connection, or WCF relay.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	relay "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/relay"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own relay authorization rule"})

    		ubx.Resource(relay.AuthorizationRule, "example", relay.AuthorizationRuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AuthorizationRule } from "@ubx/sdk-azure/azure/relay/authorization_rule";

    export default stack("example", () => {
      intent({ summary: "example own relay authorization rule" });

      resource(AuthorizationRule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.relay.authorization_rule import AuthorizationRule, AuthorizationRuleConfig

    def describe():
        ubx.intent("example own relay authorization rule")

        ubx.resource(AuthorizationRule, "example", AuthorizationRuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_relay_authorization_rule, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties supplied to create or update AuthorizationRule Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="rights" type="list(string)" required>
      The rights associated with the rule. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
