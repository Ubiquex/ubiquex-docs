---
title: "azurerm_relay_hybrid_connection"
description: "Real, generated bindings for `azurerm_relay_hybrid_connection`."
---

`azurerm_relay_hybrid_connection` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	relay "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/relay"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hybrid connection"})

    		ubx.Resource(relay.HybridConnection, "ci-runner", relay.HybridConnectionConfig{
    			Name:                        "ci-runner",
    			RelayNamespaceName:          "example-relay-namespace-name",
    			ResourceGroupName:           "example-resource-group-name",
    			RequiresClientAuthorization: true,
    			UserMetadata:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HybridConnection } from "jsr:@ubx/sdk-azure/azurerm/relay/hybrid_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own hybrid connection" });

      resource(HybridConnection, "ci-runner", {
        name: "ci-runner",
        relayNamespaceName: "example-relay-namespace-name",
        resourceGroupName: "example-resource-group-name",
        requiresClientAuthorization: true,
        userMetadata: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.relay import HybridConnection, HybridConnectionConfig

    def describe():
        ubx.intent("payments own hybrid connection")

        ubx.resource(HybridConnection, "ci-runner", HybridConnectionConfig(
            name="ci-runner",
            relay_namespace_name="example-relay-namespace-name",
            resource_group_name="example-resource-group-name",
            requires_client_authorization=True,
            user_metadata="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_relay_hybrid_connection called "ci-runner", with
    relay namespace name example-relay-namespace-name, resource group
    name example-resource-group-name, requires client authorization
    true, user metadata example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="relay_namespace_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="requires_client_authorization" type="bool">
  Optional.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_metadata" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
