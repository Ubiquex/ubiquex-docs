---
title: "azure_relay_relay_relay_namespace"
description: "`azure_relay_relay_relay_namespace` is an Azure Relay namespace, the management container that hybrid connection(s) and WCF relay(s) are created within."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_relay_relay --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_relay_relay/sdk/go
    package main

    import (
    	relay "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_relay_relay/relay"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own relay relay namespace"})

    		ubx.Resource(relay.RelayRelayNamespace, "ci-runner", relay.RelayRelayNamespaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_relay_relay --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RelayRelayNamespace } from "./local-sdk/azure_relay_relay/sdk/typescript/azure_relay_relay/relay/relay_relay_namespace.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own relay relay namespace" });

      resource(RelayRelayNamespace, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_relay_relay --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_relay_relay/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_relay_relay.relay import RelayRelayNamespace, RelayRelayNamespaceConfig

    def describe():
        ubx.intent("payments own relay relay namespace")

        ubx.resource(RelayRelayNamespace, "ci-runner", RelayRelayNamespaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_relay_relay_relay_namespace, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of the namespace. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The time the namespace was created. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="metric_id" type="string">
      Identifier for Azure Insights metrics. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="minimum_tls_version" type="string">
      TLS versions supported by Relay namespaces. Optional.
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      List of private endpoint connections. Optional.
      <Expandable title="properties">
        <ResponseField name="location" type="string">
          The Azure region this resource is deployed to, e.g. `eastus`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the Namespace. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      This determines if traffic is allowed over public network. By default it is enabled. Optional.
    </ResponseField>
    <ResponseField name="service_bus_endpoint" type="string">
      Endpoint you can use to perform Service Bus operations. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="status" type="string">
      Status of the Namespace. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      The time the namespace was updated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  SKU of the namespace. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      Name of this SKU. Required.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The tier of this SKU. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
