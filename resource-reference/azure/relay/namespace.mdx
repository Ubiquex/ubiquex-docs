---
title: "azure_relay_namespace"
description: "A complete, runnable azure_relay_namespace program, in every SDK language."
---

`azure_relay_namespace` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	relay "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/relay"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own namespace"})

    		ubx.Resource(relay.Namespace, "example", relay.NamespaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Namespace } from "@ubx/sdk-azure/azure/relay/namespace";

    export default stack("example", () => {
      intent({ summary: "example own namespace" });

      resource(Namespace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.relay.namespace import Namespace, NamespaceConfig

    def describe():
        ubx.intent("example own namespace")

        ubx.resource(Namespace, "example", NamespaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_relay_namespace, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of the namespace. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The time the namespace was created. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="metric_id" type="string">
      Identifier for Azure Insights metrics. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="minimum_tls_version" type="string">
      TLS versions supported by Relay namespaces. Optional.
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      List of private endpoint connections. Optional.
      <Expandable title="properties">
        <ResponseField name="location" type="string">
          Optional; if omitted, computed by Microsoft Azure.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Optional; if omitted, computed by Microsoft Azure.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the Namespace. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      This determines if traffic is allowed over public network. By default it is enabled. Optional.
    </ResponseField>
    <ResponseField name="service_bus_endpoint" type="string">
      Endpoint you can use to perform Service Bus operations. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="status" type="string">
      Status of the Namespace. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      The time the namespace was updated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  SKU of the namespace. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      Name of this SKU. Required.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The tier of this SKU. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
