---
title: "azure_relay_private_endpoint_connection"
description: "`azure_relay_private_endpoint_connection` represents the connection between a Private Endpoint and an Azure Relay namespace..."
---

`azure_relay_private_endpoint_connection` represents the connection between a Private Endpoint and an Azure Relay namespace, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_relay --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_relay/sdk/go
    package main

    import (
    	relay "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_relay/relay"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own relay private endpoint connection"})

    		ubx.Resource(relay.RelayPrivateEndpointConnection, "example", relay.RelayPrivateEndpointConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_relay --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RelayPrivateEndpointConnection } from "./local-sdk/azure_relay/sdk/typescript/azure_relay/relay/relay_private_endpoint_connection.ts";

    export default stack("example", () => {
      intent({ summary: "example own relay private endpoint connection" });

      resource(RelayPrivateEndpointConnection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_relay --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_relay/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_relay.relay import RelayPrivateEndpointConnection, RelayPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("example own relay private endpoint connection")

        ubx.resource(RelayPrivateEndpointConnection, "example", RelayPrivateEndpointConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_relay_private_endpoint_connection, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the private endpoint connection resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="private_endpoint" type="object">
      PrivateEndpoint information. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The ARM identifier for Private Endpoint. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_link_service_connection_state" type="object">
      ConnectionState information. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Description of the connection state. Optional.
        </ResponseField>
        <ResponseField name="status" type="string">
          Status of the connection. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the Private Endpoint Connection. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
