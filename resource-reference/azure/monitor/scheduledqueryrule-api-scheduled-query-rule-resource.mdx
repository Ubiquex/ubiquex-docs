---
title: "azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource"
description: "`azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource` defines an Azure Monitor alert rule that periodically runs a log query and fires..."
---

`azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource` defines an Azure Monitor alert rule that periodically runs a log query and fires when its own condition is met.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitor "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/monitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own scheduledqueryrule api scheduled query rule resource"})

    		ubx.Resource(monitor.ScheduledqueryruleApiScheduledQueryRuleResource, "example", monitor.ScheduledqueryruleApiScheduledQueryRuleResourceConfig{
    			Location:   "example",
    			Properties: map[string]any{"actions": map[string]any{"action_groups": []string{"example"}}},
    			Name:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ScheduledqueryruleApiScheduledQueryRuleResource } from "@ubx/sdk-azure/azure/monitor/scheduledqueryrule_api_scheduled_query_rule_resource";

    export default stack("example", () => {
      intent({
        summary:
          "example own scheduledqueryrule api scheduled query rule resource",
      });

      resource(ScheduledqueryruleApiScheduledQueryRuleResource, "example", {
        location: "example",
        properties: { actions: { actionGroups: ["example"] } },
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.monitor.scheduledqueryrule_api_scheduled_query_rule_resource import ScheduledqueryruleApiScheduledQueryRuleResource, ScheduledqueryruleApiScheduledQueryRuleResourceConfig

    def describe():
        ubx.intent("example own scheduledqueryrule api scheduled query rule resource")

        ubx.resource(ScheduledqueryruleApiScheduledQueryRuleResource, "example", ScheduledqueryruleApiScheduledQueryRuleResourceConfig(
            location="example",
            properties={"actions": {"action_groups": ["example"]}},
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource
    called "example", with location example, properties a set
    value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  The etag field is *not* required. If it is provided in the response body, it must also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="id" type="string">
  Fully qualified resource ID for the resource. Ex - /subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/&#123;resourceProviderNamespace&#125;/&#123;resourceType&#125;/&#123;resourceName&#125; Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="identity" type="object">
  Identity for the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal ID of resource identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity. Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Indicates the type of scheduled query rule. The default is LogAlert. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="location" type="string" required>
  The geo-location where the resource lives Required.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the resource Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  scheduled query rule Definition Required.
  <Expandable title="properties">
    <ResponseField name="actions" type="object">
      Actions to invoke when the alert fires. Optional.
      <Expandable title="properties">
        <ResponseField name="action_groups" type="list(string)">
          Action Group resource Ids to invoke when the alert fires. Optional.
        </ResponseField>
        <ResponseField name="action_properties" type="map(string)">
          The properties of an action properties. Optional.
        </ResponseField>
        <ResponseField name="custom_properties" type="map(string)">
          The properties of an alert payload. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="auto_mitigate" type="bool">
      The flag that indicates whether the alert should be automatically resolved or not. The default is true. Relevant only for rules of kinds LogAlert and SimpleLogAlert. Optional.
    </ResponseField>
    <ResponseField name="check_workspace_alerts_storage_configured" type="bool">
      The flag which indicates whether this scheduled query rule should be stored in the customer's storage. The default is false. Relevant only for rules of the kind LogAlert. Optional.
    </ResponseField>
    <ResponseField name="created_with_api_version" type="string">
      The api-version used when creating this alert rule Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="criteria" type="object">
      The rule criteria that defines the conditions of the scheduled query rule. Optional.
      <Expandable title="properties">
        <ResponseField name="all_of" type="list(object)">
          A list of conditions to evaluate against the specified scopes Optional.
          <Expandable title="properties">
            <ResponseField name="alert_sensitivity" type="string">
              How sensitive this dynamic threshold is to deviations in the metric. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="criterion_type" type="string">
              The category of alert criterion this represents, e.g. static or dynamic threshold. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="dimensions" type="list(object)">
              The dimension(s) this alert condition is evaluated across. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  How this condition's own operands are compared. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="values" type="list(string)">
                  The value(s) making up this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="failing_periods" type="object">
              The number of failing evaluation period(s), out of the last `number_of_evaluation_periods`, required to trigger this alert. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="min_failing_periods_to_alert" type="number">
                  The minimum number of failing evaluation period(s) required to trigger this alert. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="number_of_evaluation_periods" type="number">
                  How many recent evaluation period(s) this condition considers. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="ignore_data_before" type="string">
              Data before this date is excluded when learning a dynamic threshold's own baseline. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metric_measure_column" type="string">
              The result column holding the metric value this condition evaluates. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metric_name" type="string">
              The name of the metric this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="min_recurrence_count" type="number">
              The minimum number of times this pattern must recur to trigger the alert. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              How this condition's own operands are compared. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="query" type="string">
              The log query text this condition evaluates. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_id_column" type="string">
              The result column identifying which resource each row belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="threshold" type="number">
              The value that must be crossed for this condition to trigger. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time_aggregation" type="string">
              How values are aggregated over each evaluation period, e.g. average or count. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      The description of the scheduled query rule. Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The display name of the alert rule Optional.
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      The flag which indicates whether this scheduled query rule is enabled. Value should be true or false Optional.
    </ResponseField>
    <ResponseField name="evaluation_frequency" type="string">
      How often the scheduled query rule is evaluated represented in ISO 8601 duration format. Relevant and required only for rules of the kind LogAlert. Optional.
    </ResponseField>
    <ResponseField name="is_legacy_log_analytics_rule" type="bool">
      True if alert rule is legacy Log Analytic rule Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="is_workspace_alerts_storage_configured" type="bool">
      The flag which indicates whether this scheduled query rule has been configured to be stored in the customer's storage. The default is false. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="mute_actions_duration" type="string">
      Mute actions for the chosen period of time (in ISO 8601 duration format) after the alert is fired. Relevant only for rules of the kind LogAlert. Optional.
    </ResponseField>
    <ResponseField name="override_query_time_range" type="string">
      If specified then overrides the query time range (default is WindowSize*NumberOfEvaluationPeriods). Relevant only for rules of the kind LogAlert. Optional.
    </ResponseField>
    <ResponseField name="resolve_configuration" type="object">
      TBD. Relevant only for rules of the kind LogAlert. Optional.
      <Expandable title="properties">
        <ResponseField name="auto_resolved" type="bool">
          The flag that indicates whether or not to auto resolve a fired alert. Optional.
        </ResponseField>
        <ResponseField name="time_to_resolve" type="string">
          The duration a rule must evaluate as healthy before the fired alert is automatically resolved represented in ISO 8601 duration format. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scopes" type="list(string)">
      The list of resource id's that this scheduled query rule is scoped to. Optional.
    </ResponseField>
    <ResponseField name="severity" type="number">
      Severity of the alert. Should be an integer between [0-4]. Value of 0 is severest. Relevant and required only for rules of the kind LogAlert. Optional.
    </ResponseField>
    <ResponseField name="skip_query_validation" type="bool">
      The flag which indicates whether the provided query should be validated or not. The default is false. Relevant only for rules of the kind LogAlert. Optional.
    </ResponseField>
    <ResponseField name="target_resource_types" type="list(string)">
      List of resource type of the target resource(s) on which the alert is created/updated. For example if the scope is a resource group and targetResourceTypes is Microsoft.Compute/virtualMachines, then a different alert will be fired for each virtual machine in the resource group which meet the alert criteria. Relevant only for rules of the kind LogAlert Optional.
    </ResponseField>
    <ResponseField name="window_size" type="string">
      The period of time (in ISO 8601 duration format) on which the Alert query will be executed (bin size). Relevant and required only for rules of the kind LogAlert. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="type" type="string">
  The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts" Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
