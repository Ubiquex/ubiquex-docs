---
title: "azure_workloads_monitors_monitor"
description: "`azure_workloads_monitors_monitor` enables Azure Monitor for SAP solutions..."
---

`azure_workloads_monitors_monitor` enables Azure Monitor for SAP solutions, collecting telemetry from an SAP landscape's own infrastructure and database components.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workloads "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/workloads"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own monitors monitor"})

    		ubx.Resource(workloads.MonitorsMonitor, "example", workloads.MonitorsMonitorConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MonitorsMonitor } from "@ubx/sdk-azure/azure/workloads/monitors_monitor";

    export default stack("example", () => {
      intent({ summary: "example own monitors monitor" });

      resource(MonitorsMonitor, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.workloads.monitors_monitor import MonitorsMonitor, MonitorsMonitorConfig

    def describe():
        ubx.intent("example own monitors monitor")

        ubx.resource(MonitorsMonitor, "example", MonitorsMonitorConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_workloads_monitors_monitor, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  A pre-created user assigned identity with appropriate roles assigned. To learn more on identity and roles required, visit the ACSS how-to-guide. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="type" type="string" required>
      Type of managed service identity (only None, UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Describes the properties of a SAP monitor. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="app_location" type="string">
      The SAP monitor resources will be deployed in the SAP monitoring region. The subnet region should be same as the SAP monitoring region. Optional.
    </ResponseField>
    <ResponseField name="errors" type="object">
      Defines the SAP monitor errors. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          A machine-readable status or error code. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="details" type="list(object)">
          Additional detail about this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              A machine-readable status or error code. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="details" type="list(object)">
              Additional detail about this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="inner_error" type="object">
              The underlying error that caused this failure. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="inner_error" type="any">
                  The underlying error that caused this failure. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="target" type="string">
              A reference to the resource this operation targets. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="inner_error" type="object">
          The underlying error that caused this failure. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="inner_error" type="object">
              The underlying error that caused this failure. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <Note>`inner_error` recurs here (its own type contains itself); see the `inner_error` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target" type="string">
          A reference to the resource this operation targets. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="log_analytics_workspace_arm_id" type="string">
      The ARM ID of the Log Analytics Workspace that is used for SAP monitoring. Optional.
    </ResponseField>
    <ResponseField name="managed_resource_group_configuration" type="object">
      Managed resource group configuration Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Managed resource group name Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="monitor_subnet" type="string">
      The subnet which the SAP monitor will be deployed in Optional.
    </ResponseField>
    <ResponseField name="msi_arm_id" type="string">
      The ARM ID of the MSI used for SAP monitoring. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      State of provisioning of the SAP monitor. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="routing_preference" type="string">
      Sets the routing preference of the SAP monitor. By default only RFC1918 traffic is routed to the customer VNET. Optional.
    </ResponseField>
    <ResponseField name="storage_account_arm_id" type="string">
      The ARM ID of the Storage account used for SAP monitoring. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="zone_redundancy_preference" type="string">
      Sets the preference for zone redundancy on resources created for the SAP monitor. By default resources will be created which do not support zone redundancy. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
