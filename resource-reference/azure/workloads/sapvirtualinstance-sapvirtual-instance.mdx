---
title: "azure_workloads_sapvirtualinstance_sapvirtual_instance"
description: "`azure_workloads_sapvirtualinstance_sapvirtual_instance` is the top-level Azure Center for SAP solutions resource representing a complete SAP system..."
---

`azure_workloads_sapvirtualinstance_sapvirtual_instance` is the top-level Azure Center for SAP solutions resource representing a complete SAP system, grouping its own central services, application server, and database instance(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workloads "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/workloads"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sapvirtualinstance sapvirtual instance"})

    		ubx.Resource(workloads.SapvirtualinstanceSapvirtualInstance, "example", workloads.SapvirtualinstanceSapvirtualInstanceConfig{
    			Properties: map[string]any{"configuration": map[string]any{"configuration_type": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SapvirtualinstanceSapvirtualInstance } from "@ubx/sdk-azure/azure/workloads/sapvirtualinstance_sapvirtual_instance";

    export default stack("example", () => {
      intent({ summary: "example own sapvirtualinstance sapvirtual instance" });

      resource(SapvirtualinstanceSapvirtualInstance, "example", {
        properties: { configuration: { configurationType: "example" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.workloads import SapvirtualinstanceSapvirtualInstance, SapvirtualinstanceSapvirtualInstanceConfig

    def describe():
        ubx.intent("example own sapvirtualinstance sapvirtual instance")

        ubx.resource(SapvirtualinstanceSapvirtualInstance, "example", SapvirtualinstanceSapvirtualInstanceConfig(
            properties={"configuration": {"configuration_type": "example"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_workloads_sapvirtualinstance_sapvirtual_instance,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  A pre-created user assigned identity with appropriate roles assigned. To learn more on identity and roles required, visit the ACSS how-to-guide. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="type" type="string" required>
      Type of managed service identity (only None, UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object" required>
  Defines the Virtual Instance for SAP solutions resource properties. Required.
  <Expandable title="properties">
    <ResponseField name="configuration" type="object" required>
      The SAP Configuration. Required.
      <Expandable title="properties">
        <ResponseField name="configuration_type" type="string" required>
          The configuration Type. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="environment" type="string" required>
      Defines the environment type - Production/Non Production. Required.
    </ResponseField>
    <ResponseField name="errors" type="object">
      An error response from the Virtual Instance for SAP Workload service. Optional.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          Error definition. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="health" type="string">
      Defines the health of SAP Instances. Optional.
    </ResponseField>
    <ResponseField name="managed_resource_group_configuration" type="object">
      Managed resource group configuration Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Managed resource group name Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Defines the provisioning states. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="sap_product" type="string" required>
      Defines the SAP Product type. Required.
    </ResponseField>
    <ResponseField name="state" type="string">
      Defines the Virtual Instance for SAP state. Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      Defines the SAP Instance status. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
