---
title: "azure_workloads_monitors_provider_instance"
description: "`azure_workloads_monitors_provider_instance` registers a specific SAP system component (e.g. the database or a NetWeaver instance) as a data source for..."
---

`azure_workloads_monitors_provider_instance` registers a specific SAP system component (e.g. the database or a NetWeaver instance) as a data source for Azure Monitor for SAP solutions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workloads "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/workloads"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own monitors provider instance"})

    		ubx.Resource(workloads.MonitorsProviderInstance, "example", workloads.MonitorsProviderInstanceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MonitorsProviderInstance } from "@ubx/sdk-azure/azure/workloads/monitors_provider_instance";

    export default stack("example", () => {
      intent({ summary: "example own monitors provider instance" });

      resource(MonitorsProviderInstance, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.workloads.monitors_provider_instance import MonitorsProviderInstance, MonitorsProviderInstanceConfig

    def describe():
        ubx.intent("example own monitors provider instance")

        ubx.resource(MonitorsProviderInstance, "example", MonitorsProviderInstanceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_workloads_monitors_provider_instance, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  A pre-created user assigned identity with appropriate roles assigned. To learn more on identity and roles required, visit the ACSS how-to-guide. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="type" type="string" required>
      Type of managed service identity (only None, UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Describes the properties of a provider instance. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="errors" type="object">
      Defines the provider instance errors. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          A machine-readable status or error code. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="details" type="list(object)">
          Additional detail about this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              A machine-readable status or error code. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="details" type="list(object)">
              Additional detail about this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="inner_error" type="object">
              The underlying error that caused this failure. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="inner_error" type="any">
                  The underlying error that caused this failure. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="target" type="string">
              A reference to the resource this operation targets. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="inner_error" type="object">
          The underlying error that caused this failure. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="inner_error" type="object">
              The underlying error that caused this failure. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <Note>`inner_error` recurs here (its own type contains itself); see the `inner_error` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target" type="string">
          A reference to the resource this operation targets. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provider_settings" type="object">
      Gets or sets the provider specific properties. Optional.
      <Expandable title="properties">
        <ResponseField name="provider_type" type="string" required>
          The provider type. For example, the value can be SapHana. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      State of provisioning of the provider instance Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
