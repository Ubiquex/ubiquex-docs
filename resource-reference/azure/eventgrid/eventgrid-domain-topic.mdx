---
title: "azure_eventgrid_domain_topic"
description: "`azure_eventgrid_eventgrid_domain_topic` is a single logical topic within an Event Grid domain, that event subscription(s) are created against."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_eventgrid_eventgrid --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_eventgrid_eventgrid/sdk/go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_eventgrid_eventgrid/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own eventgrid domain topic"})

    		ubx.Resource(eventgrid.EventgridDomainTopic, "ci-runner", eventgrid.EventgridDomainTopicConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_eventgrid_eventgrid --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventgridDomainTopic } from "./local-sdk/azure_eventgrid_eventgrid/sdk/typescript/azure_eventgrid_eventgrid/eventgrid/eventgrid_domain_topic.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own eventgrid domain topic" });

      resource(EventgridDomainTopic, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_eventgrid_eventgrid --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_eventgrid_eventgrid/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_eventgrid_eventgrid.eventgrid import EventgridDomainTopic, EventgridDomainTopicConfig

    def describe():
        ubx.intent("payments own eventgrid domain topic")

        ubx.resource(EventgridDomainTopic, "ci-runner", EventgridDomainTopicConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_domain_topic, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="properties" type="object">
  Properties of the Domain Topic. Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the domain topic. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
