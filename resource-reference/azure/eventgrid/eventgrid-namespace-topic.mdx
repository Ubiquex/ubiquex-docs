---
title: "azure_eventgrid_namespace_topic"
description: "`azure_eventgrid_eventgrid_namespace_topic` is a topic within an Event Grid namespace..."
---

`azure_eventgrid_eventgrid_namespace_topic` is a topic within an Event Grid namespace, that publisher(s) send CloudEvents-formatted event(s) into and event subscription(s) pull or receive push delivery from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_eventgrid_eventgrid --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_eventgrid_eventgrid/sdk/go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_eventgrid_eventgrid/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own eventgrid namespace topic"})

    		ubx.Resource(eventgrid.EventgridNamespaceTopic, "ci-runner", eventgrid.EventgridNamespaceTopicConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_eventgrid_eventgrid --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventgridNamespaceTopic } from "./local-sdk/azure_eventgrid_eventgrid/sdk/typescript/azure_eventgrid_eventgrid/eventgrid/eventgrid_namespace_topic.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own eventgrid namespace topic" });

      resource(EventgridNamespaceTopic, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_eventgrid_eventgrid --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_eventgrid_eventgrid/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_eventgrid_eventgrid.eventgrid import EventgridNamespaceTopic, EventgridNamespaceTopicConfig

    def describe():
        ubx.intent("payments own eventgrid namespace topic")

        ubx.resource(EventgridNamespaceTopic, "ci-runner", EventgridNamespaceTopicConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_namespace_topic, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of the namespace topic. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="event_retention_in_days" type="number">
      Event retention for the namespace topic expressed in days. The property default value is 1 day. Min event retention duration value is 1 day and max event retention duration value is 1 day. Optional.
    </ResponseField>
    <ResponseField name="input_schema" type="string">
      This determines the format that is expected for incoming events published to the topic. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the namespace topic. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="publisher_type" type="string">
      Publisher type of the namespace topic. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
