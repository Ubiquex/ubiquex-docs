---
title: "azure_eventgrid_partner_topic"
description: "`azure_eventgrid_eventgrid_partner_topic` is the customer-side topic receiving event(s) published by a registered Event Grid partner's own SaaS platform."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own eventgrid partner topic"})

    		ubx.Resource(eventgrid.PartnerTopic, "example", eventgrid.PartnerTopicConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PartnerTopic } from "@ubx/sdk-azure/azure/eventgrid/partner_topic";

    export default stack("example", () => {
      intent({ summary: "example own eventgrid partner topic" });

      resource(PartnerTopic, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.eventgrid import PartnerTopic, PartnerTopicConfig

    def describe():
        ubx.intent("example own eventgrid partner topic")

        ubx.resource(PartnerTopic, "example", PartnerTopicConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_partner_topic, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  The identity information for the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal ID of resource identity. Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of resource. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of managed identity used. The type 'SystemAssigned, UserAssigned' includes both an implicitly created identity and a set of user-assigned identities. The type 'None' will remove any identity. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. This property is currently not used and reserved for future usage. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the Partner Topic. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="activation_state" type="string">
      Activation state of the partner topic. Optional.
    </ResponseField>
    <ResponseField name="event_type_info" type="object">
      The event type information for Channels. Optional.
      <Expandable title="properties">
        <ResponseField name="inline_event_types" type="map(object)">
          A collection of inline event types for the resource. The inline event type keys are of type string which represents the name of the event. An example of a valid inline event name is "Contoso.OrderCreated". The inline event type values are of type InlineEventProperties and will contain additional information for every inline event type. Optional.
          <Expandable title="properties">
            <ResponseField name="data_schema_url" type="string">
              A URL pointing to the schema describing this event's own data payload. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              A human-readable description of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_name" type="string">
              A human-readable name for this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="documentation_url" type="string">
              A URL pointing to documentation for this event type. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of event type used. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="expiration_time_if_not_activated_utc" type="string">
      Expiration time of the partner topic. If this timer expires while the partner topic is still never activated, the partner topic and corresponding event channel are deleted. Optional.
    </ResponseField>
    <ResponseField name="message_for_activation" type="string">
      Context or helpful message that can be used during the approval process by the subscriber. Optional.
    </ResponseField>
    <ResponseField name="partner_registration_immutable_id" type="string">
      The immutableId of the corresponding partner registration. Optional.
    </ResponseField>
    <ResponseField name="partner_topic_friendly_description" type="string">
      Friendly description about the topic. This can be set by the publisher/partner to show custom description for the customer partner topic. This will be helpful to remove any ambiguity of the origin of creation of the partner topic for the customer. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the partner topic. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="source" type="string">
      Source associated with this partner topic. This represents a unique partner resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
