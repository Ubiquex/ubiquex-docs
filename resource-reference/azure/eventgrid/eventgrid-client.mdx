---
title: "azure_eventgrid_client"
description: "`azure_eventgrid_client` registers an MQTT client identity permitted to connect to an Event Grid namespace's own MQTT broker."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own eventgrid client"})

    		ubx.Resource(eventgrid.Client, "example", eventgrid.ClientConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Client } from "@ubx/sdk-azure/azure/eventgrid/client";

    export default stack("example", () => {
      intent({ summary: "example own eventgrid client" });

      resource(Client, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.eventgrid.client import Client, ClientConfig

    def describe():
        ubx.intent("example own eventgrid client")

        ubx.resource(Client, "example", ClientConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_client, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of client. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="attributes" type="map(any)">
      Attributes for the client. Supported values are int, bool, string, string[]. Example: "attributes": &#123; "room": "345", "floor": 12, "deviceTypes": ["Fan", "Light"] &#125; Optional.
    </ResponseField>
    <ResponseField name="authentication_name" type="string">
      The name presented by the client for authentication. The default value is the name of the resource. Optional.
    </ResponseField>
    <ResponseField name="client_certificate_authentication" type="object">
      The certificate authentication properties for the client. Optional.
      <Expandable title="properties">
        <ResponseField name="allowed_thumbprints" type="list(string)">
          The list of thumbprints that are allowed during client authentication. This property is required only if the validationScheme is 'ThumbprintMatch'. Optional.
        </ResponseField>
        <ResponseField name="validation_scheme" type="string">
          The validation scheme used to authenticate the client. Default value is SubjectMatchesAuthenticationName. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      Description for the Client resource. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the Client resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="state" type="string">
      Indicates if the client is enabled or not. Default value is Enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
