---
title: "azure_eventgrid_partner_registration"
description: "`azure_eventgrid_partner_registration` registers a third-party SaaS provider as an Event Grid partner..."
---

`azure_eventgrid_partner_registration` registers a third-party SaaS provider as an Event Grid partner, authorizing it to publish events into customer Azure subscriptions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own eventgrid partner registration"})

    		ubx.Resource(eventgrid.PartnerRegistration, "example", eventgrid.PartnerRegistrationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PartnerRegistration } from "@ubx/sdk-azure/azure/eventgrid/partner_registration";

    export default stack("example", () => {
      intent({ summary: "example own eventgrid partner registration" });

      resource(PartnerRegistration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.eventgrid.partner_registration import PartnerRegistration, PartnerRegistrationConfig

    def describe():
        ubx.intent("example own eventgrid partner registration")

        ubx.resource(PartnerRegistration, "example", PartnerRegistrationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_partner_registration, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of the partner registration. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="partner_registration_immutable_id" type="string">
      The immutableId of the corresponding partner registration. Note: This property is marked for deprecation and is not supported in any future GA API version Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the partner registration. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
