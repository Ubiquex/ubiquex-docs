---
title: "azure_eventgrid_eventgrid_system_topic"
description: "`azure_eventgrid_eventgrid_system_topic` is the built-in Event Grid topic automatically available for a specific Azure resource (e.g. a Storage..."
---

`azure_eventgrid_eventgrid_system_topic` is the built-in Event Grid topic automatically available for a specific Azure resource (e.g. a Storage account) to publish its own lifecycle event(s) from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_eventgrid_eventgrid --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_eventgrid_eventgrid/sdk/go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_eventgrid_eventgrid/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own eventgrid system topic"})

    		ubx.Resource(eventgrid.EventgridSystemTopic, "ci-runner", eventgrid.EventgridSystemTopicConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_eventgrid_eventgrid --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventgridSystemTopic } from "./local-sdk/azure_eventgrid_eventgrid/sdk/typescript/azure_eventgrid_eventgrid/eventgrid/eventgrid_system_topic.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own eventgrid system topic" });

      resource(EventgridSystemTopic, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_eventgrid_eventgrid --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_eventgrid_eventgrid/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_eventgrid_eventgrid.eventgrid import EventgridSystemTopic, EventgridSystemTopicConfig

    def describe():
        ubx.intent("payments own eventgrid system topic")

        ubx.resource(EventgridSystemTopic, "ci-runner", EventgridSystemTopicConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_eventgrid_system_topic, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  The identity information for the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal ID of resource identity. Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of resource. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of managed identity used. The type 'SystemAssigned, UserAssigned' includes both an implicitly created identity and a set of user-assigned identities. The type 'None' will remove any identity. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. This property is currently not used and reserved for future usage. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the System Topic. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="metric_resource_id" type="string">
      Metric resource id for the system topic. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the system topic. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="source" type="string">
      Source for the system topic. Optional.
    </ResponseField>
    <ResponseField name="topic_type" type="string">
      TopicType for the system topic. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
