---
title: "azure_eventgrid_permission_binding"
description: "`azure_eventgrid_permission_binding` grants an Event Grid MQTT client or client group publish or subscribe permission on a specific topic space."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_eventgrid --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_eventgrid/sdk/go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_eventgrid/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own eventgrid permission binding"})

    		ubx.Resource(eventgrid.EventgridPermissionBinding, "example", eventgrid.EventgridPermissionBindingConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_eventgrid --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventgridPermissionBinding } from "./local-sdk/azure_eventgrid/sdk/typescript/azure_eventgrid/eventgrid/eventgrid_permission_binding.ts";

    export default stack("example", () => {
      intent({ summary: "example own eventgrid permission binding" });

      resource(EventgridPermissionBinding, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_eventgrid --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_eventgrid/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_eventgrid.eventgrid import EventgridPermissionBinding, EventgridPermissionBindingConfig

    def describe():
        ubx.intent("example own eventgrid permission binding")

        ubx.resource(EventgridPermissionBinding, "example", EventgridPermissionBindingConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_permission_binding, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of permission binding. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="client_group_name" type="string">
      The name of the client group resource that the permission is bound to. The client group needs to be a resource under the same namespace the permission binding is a part of. Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      Description for the Permission Binding resource. Optional.
    </ResponseField>
    <ResponseField name="permission" type="string">
      The allowed permission. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the PermissionBinding resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="topic_space_name" type="string">
      The name of the Topic Space resource that the permission is bound to. The Topic space needs to be a resource under the same namespace the permission binding is a part of. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
