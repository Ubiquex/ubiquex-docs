---
title: "azure_eventgrid_ca_certificate"
description: "`azure_eventgrid_ca_certificate` registers a CA certificate an Event Grid namespace trusts for validating client certificates during MQTT..."
---

`azure_eventgrid_ca_certificate` registers a CA certificate an Event Grid namespace trusts for validating client certificates during MQTT mutual TLS authentication.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own eventgrid ca certificate"})

    		ubx.Resource(eventgrid.CaCertificate, "example", eventgrid.CaCertificateConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CaCertificate } from "@ubx/sdk-azure/azure/eventgrid/ca_certificate";

    export default stack("example", () => {
      intent({ summary: "example own eventgrid ca certificate" });

      resource(CaCertificate, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.eventgrid.ca_certificate import CaCertificate, CaCertificateConfig

    def describe():
        ubx.intent("example own eventgrid ca certificate")

        ubx.resource(CaCertificate, "example", CaCertificateConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_ca_certificate, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of CA certificate. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Description for the CA Certificate resource. Optional.
    </ResponseField>
    <ResponseField name="encoded_certificate" type="string">
      Base64 encoded PEM (Privacy Enhanced Mail) format certificate data. Optional.
    </ResponseField>
    <ResponseField name="expiry_time_in_utc" type="string">
      Certificate expiry time in UTC. This is a read-only field. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="issue_time_in_utc" type="string">
      Certificate issue time in UTC. This is a read-only field. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the CA Certificate resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
