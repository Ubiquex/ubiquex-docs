---
title: "azure_eventgrid_eventgrid_ca_certificate"
description: "`azure_eventgrid_eventgrid_ca_certificate` registers a CA certificate an Event Grid namespace trusts for validating client certificates during MQTT..."
---

`azure_eventgrid_eventgrid_ca_certificate` registers a CA certificate an Event Grid namespace trusts for validating client certificates during MQTT mutual TLS authentication.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_eventgrid_eventgrid --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_eventgrid_eventgrid/sdk/go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_eventgrid_eventgrid/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own eventgrid ca certificate"})

    		ubx.Resource(eventgrid.EventgridCaCertificate, "ci-runner", eventgrid.EventgridCaCertificateConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_eventgrid_eventgrid --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventgridCaCertificate } from "./local-sdk/azure_eventgrid_eventgrid/sdk/typescript/azure_eventgrid_eventgrid/eventgrid/eventgrid_ca_certificate.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own eventgrid ca certificate" });

      resource(EventgridCaCertificate, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_eventgrid_eventgrid --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_eventgrid_eventgrid/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_eventgrid_eventgrid.eventgrid import EventgridCaCertificate, EventgridCaCertificateConfig

    def describe():
        ubx.intent("payments own eventgrid ca certificate")

        ubx.resource(EventgridCaCertificate, "ci-runner", EventgridCaCertificateConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_eventgrid_ca_certificate, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of CA certificate. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Description for the CA Certificate resource. Optional.
    </ResponseField>
    <ResponseField name="encoded_certificate" type="string">
      Base64 encoded PEM (Privacy Enhanced Mail) format certificate data. Optional.
    </ResponseField>
    <ResponseField name="expiry_time_in_utc" type="string">
      Certificate expiry time in UTC. This is a read-only field. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="issue_time_in_utc" type="string">
      Certificate issue time in UTC. This is a read-only field. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the CA Certificate resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
