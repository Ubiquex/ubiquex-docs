---
title: "azure_eventgrid_topic"
description: "`azure_eventgrid_eventgrid_topic` is a custom Event Grid topic, the endpoint a caller-defined application publishes its own event(s) to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_eventgrid_eventgrid --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_eventgrid_eventgrid/sdk/go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_eventgrid_eventgrid/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own eventgrid topic"})

    		ubx.Resource(eventgrid.EventgridTopic, "example", eventgrid.EventgridTopicConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_eventgrid_eventgrid --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventgridTopic } from "./local-sdk/azure_eventgrid_eventgrid/sdk/typescript/azure_eventgrid_eventgrid/eventgrid/eventgrid_topic.ts";

    export default stack("example", () => {
      intent({ summary: "example own eventgrid topic" });

      resource(EventgridTopic, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_eventgrid_eventgrid --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_eventgrid_eventgrid/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_eventgrid_eventgrid.eventgrid import EventgridTopic, EventgridTopicConfig

    def describe():
        ubx.intent("example own eventgrid topic")

        ubx.resource(EventgridTopic, "example", EventgridTopicConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_topic, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  The identity information for the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal ID of resource identity. Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of resource. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of managed identity used. The type 'SystemAssigned, UserAssigned' includes both an implicitly created identity and a set of user-assigned identities. The type 'None' will remove any identity. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. This property is currently not used and reserved for future usage. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the Topic. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="data_residency_boundary" type="string">
      Data Residency Boundary of the resource. Optional.
    </ResponseField>
    <ResponseField name="disable_local_auth" type="bool">
      This boolean is used to enable or disable local auth. Default value is false. When the property is set to true, only AAD token will be used to authenticate if user is allowed to publish to the topic. Optional.
    </ResponseField>
    <ResponseField name="endpoint" type="string">
      Endpoint for the topic. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="event_type_info" type="object">
      The event type information for Channels. Optional.
      <Expandable title="properties">
        <ResponseField name="inline_event_types" type="map(object)">
          A collection of inline event types for the resource. The inline event type keys are of type string which represents the name of the event. An example of a valid inline event name is "Contoso.OrderCreated". The inline event type values are of type InlineEventProperties and will contain additional information for every inline event type. Optional.
          <Expandable title="properties">
            <ResponseField name="data_schema_url" type="string">
              A URL pointing to the schema describing this event's own data payload. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              A human-readable description of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_name" type="string">
              A human-readable name for this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="documentation_url" type="string">
              A URL pointing to documentation for this event type. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kind" type="string">
          The kind of event type used. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="inbound_ip_rules" type="list(object)">
      This can be used to restrict traffic from specific IPs instead of all IPs. Note: These are considered only if PublicNetworkAccess is enabled. Optional.
      <Expandable title="properties">
        <ResponseField name="action" type="string">
          The action to perform. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ip_mask" type="string">
          The IP address range, in CIDR notation, this rule matches. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="input_schema" type="string">
      This determines the format that Event Grid should expect for incoming events published to the topic. Optional.
    </ResponseField>
    <ResponseField name="input_schema_mapping" type="object">
      By default, Event Grid expects events to be in the Event Grid event schema. Specifying an input schema mapping enables publishing to Event Grid using a custom input schema. Currently, the only supported type of InputSchemaMapping is 'JsonInputSchemaMapping'. Optional.
      <Expandable title="properties">
        <ResponseField name="input_schema_mapping_type" type="string" required>
          Type of the custom mapping Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="metric_resource_id" type="string">
      Metric resource id for the topic. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="minimum_tls_version_allowed" type="string">
      Minimum TLS version of the publisher allowed to publish to this topic Optional.
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      List of private endpoint connections. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the topic. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      This determines if traffic is allowed over public network. By default it is enabled. You can further restrict to specific IPs by configuring &lt;seealso cref="P:Microsoft.Azure.Events.ResourceProvider.Common.Contracts.TopicProperties.InboundIpRules" /&gt; Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
