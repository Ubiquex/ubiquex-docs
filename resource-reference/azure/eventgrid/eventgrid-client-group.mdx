---
title: "azure_eventgrid_client_group"
description: "`azure_eventgrid_eventgrid_client_group` groups related Event Grid MQTT client(s) together by a matching expression..."
---

`azure_eventgrid_eventgrid_client_group` groups related Event Grid MQTT client(s) together by a matching expression, so permission bindings and topic spaces can target the group instead of individual clients.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own eventgrid client group"})

    		ubx.Resource(eventgrid.ClientGroup, "example", eventgrid.ClientGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClientGroup } from "@ubx/sdk-azure/azure/eventgrid/client_group";

    export default stack("example", () => {
      intent({ summary: "example own eventgrid client group" });

      resource(ClientGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.eventgrid import ClientGroup, ClientGroupConfig

    def describe():
        ubx.intent("example own eventgrid client group")

        ubx.resource(ClientGroup, "example", ClientGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_client_group, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of client group. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Description for the Client Group resource. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the ClientGroup resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="query" type="string">
      The grouping query for the clients. Example : attributes.keyName IN ['a', 'b', 'c']. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
