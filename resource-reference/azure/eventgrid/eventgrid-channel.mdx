---
title: "azure_eventgrid_channel"
description: "`azure_eventgrid_eventgrid_channel` establishes the event delivery link between an Event Grid partner topic (or partner destination) and the consuming..."
---

`azure_eventgrid_eventgrid_channel` establishes the event delivery link between an Event Grid partner topic (or partner destination) and the consuming or publishing Azure subscription.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own eventgrid channel"})

    		ubx.Resource(eventgrid.Channel, "example", eventgrid.ChannelConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Channel } from "@ubx/sdk-azure/azure/eventgrid/channel";

    export default stack("example", () => {
      intent({ summary: "example own eventgrid channel" });

      resource(Channel, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.eventgrid import Channel, ChannelConfig

    def describe():
        ubx.intent("example own eventgrid channel")

        ubx.resource(Channel, "example", ChannelConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventgrid_channel, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of the Channel. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="channel_type" type="string">
      The type of the event channel which represents the direction flow of events. Optional.
    </ResponseField>
    <ResponseField name="expiration_time_if_not_activated_utc" type="string">
      Expiration time of the channel. If this timer expires while the corresponding partner topic is never activated, the channel and corresponding partner topic are deleted. Optional.
    </ResponseField>
    <ResponseField name="message_for_activation" type="string">
      Context or helpful message that can be used during the approval process by the subscriber. Optional.
    </ResponseField>
    <ResponseField name="partner_topic_info" type="object">
      Properties of the corresponding partner topic of a Channel. Optional.
      <Expandable title="properties">
        <ResponseField name="azure_subscription_id" type="string">
          Azure subscription ID of the subscriber. The partner topic associated with the channel will be created under this Azure subscription. Optional.
        </ResponseField>
        <ResponseField name="event_type_info" type="object">
          The event type information for Channels. Optional.
          <Expandable title="properties">
            <ResponseField name="inline_event_types" type="map(object)">
              A collection of inline event types for the resource. The inline event type keys are of type string which represents the name of the event. An example of a valid inline event name is "Contoso.OrderCreated". The inline event type values are of type InlineEventProperties and will contain additional information for every inline event type. Optional.
              <Expandable title="properties">
                <ResponseField name="data_schema_url" type="string">
                  A URL pointing to the schema describing this event's own data payload. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="description" type="string">
                  A human-readable description of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="display_name" type="string">
                  A human-readable name for this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="documentation_url" type="string">
                  A URL pointing to documentation for this event type. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="kind" type="string">
              The kind of event type used. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the partner topic associated with the channel. Optional.
        </ResponseField>
        <ResponseField name="resource_group_name" type="string">
          Azure Resource Group of the subscriber. The partner topic associated with the channel will be created under this resource group. Optional.
        </ResponseField>
        <ResponseField name="source" type="string">
          The source information is provided by the publisher to determine the scope or context from which the events are originating. This information can be used by the subscriber during the approval process of the created partner topic. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the channel. Optional.
    </ResponseField>
    <ResponseField name="readiness_state" type="string">
      The readiness state of the corresponding partner topic. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
