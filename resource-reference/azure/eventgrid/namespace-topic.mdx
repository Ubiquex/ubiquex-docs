---
title: "azurerm_eventgrid_namespace_topic"
description: "Real, generated bindings for `azurerm_eventgrid_namespace_topic`."
---

`azurerm_eventgrid_namespace_topic` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own namespace topic"})

    		ubx.Resource(eventgrid.NamespaceTopic, "ci-runner", eventgrid.NamespaceTopicConfig{
    			EventgridNamespaceId: "example",
    			Name:                 "ci-runner",
    			EventRetentionInDays: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NamespaceTopic } from "jsr:@ubx/sdk-azure/azurerm/eventgrid/namespace_topic";

    export default stack("payments", () => {
      intent({ summary: "payments own namespace topic" });

      resource(NamespaceTopic, "ci-runner", {
        eventgridNamespaceId: "example",
        name: "ci-runner",
        eventRetentionInDays: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.eventgrid import NamespaceTopic, NamespaceTopicConfig

    def describe():
        ubx.intent("payments own namespace topic")

        ubx.resource(NamespaceTopic, "ci-runner", NamespaceTopicConfig(
            eventgrid_namespace_id="example",
            name="ci-runner",
            event_retention_in_days=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_eventgrid_namespace_topic called "ci-runner", with
    eventgrid namespace id example, event retention in days 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="event_retention_in_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="eventgrid_namespace_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
