---
title: "azure_automanage_configuration_profile"
description: "`azure_automanage_configuration_profile` defines a reusable Azure Automanage best-practice configuration (backup, security..."
---

`azure_automanage_configuration_profile` defines a reusable Azure Automanage best-practice configuration (backup, security, monitoring settings) that VM(s) can be assigned.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automanage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/automanage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own automanage configuration profile"})

    		ubx.Resource(automanage.ConfigurationProfile, "example", automanage.ConfigurationProfileConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConfigurationProfile } from "@ubx/sdk-azure/azure/automanage/configuration_profile";

    export default stack("example", () => {
      intent({ summary: "example own automanage configuration profile" });

      resource(ConfigurationProfile, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.automanage.configuration_profile import ConfigurationProfile, ConfigurationProfileConfig

    def describe():
        ubx.intent("example own automanage configuration profile")

        ubx.resource(ConfigurationProfile, "example", ConfigurationProfileConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_automanage_configuration_profile, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Automanage configuration profile properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="configuration" type="any">
      The custom configuration for configuration profile. Name and value pairs that define the configuration details of the configuration profile. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
