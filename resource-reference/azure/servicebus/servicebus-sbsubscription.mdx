---
title: "azure_servicebus_sbsubscription"
description: "`azure_servicebus_servicebus_sbsubscription` is a named, independent view of a Service Bus topic's own published messages..."
---

`azure_servicebus_servicebus_sbsubscription` is a named, independent view of a Service Bus topic's own published messages, letting multiple subscription(s) each receive a copy of matching message(s) via the publish/subscribe pattern.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_servicebus_servicebus --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_servicebus_servicebus/sdk/go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_servicebus_servicebus/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own servicebus sbsubscription"})

    		ubx.Resource(servicebus.ServicebusSbsubscription, "example", servicebus.ServicebusSbsubscriptionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_servicebus_servicebus --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicebusSbsubscription } from "./local-sdk/azure_servicebus_servicebus/sdk/typescript/azure_servicebus_servicebus/servicebus/servicebus_sbsubscription.ts";

    export default stack("example", () => {
      intent({ summary: "example own servicebus sbsubscription" });

      resource(ServicebusSbsubscription, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_servicebus_servicebus --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_servicebus_servicebus/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_servicebus_servicebus.servicebus import ServicebusSbsubscription, ServicebusSbsubscriptionConfig

    def describe():
        ubx.intent("example own servicebus sbsubscription")

        ubx.resource(ServicebusSbsubscription, "example", ServicebusSbsubscriptionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicebus_sbsubscription, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Description of Subscription Resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="accessed_at" type="string">
      Last time there was a receive request to this subscription. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="auto_delete_on_idle" type="string">
      ISO 8061 timeSpan idle interval after which the topic is automatically deleted. The minimum duration is 5 minutes. Optional.
    </ResponseField>
    <ResponseField name="client_affine_properties" type="object">
      Properties specific to client affine subscriptions. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Indicates the Client ID of the application that created the client-affine subscription. Optional.
        </ResponseField>
        <ResponseField name="is_durable" type="bool">
          For client-affine subscriptions, this value indicates whether the subscription is durable or not. Optional.
        </ResponseField>
        <ResponseField name="is_shared" type="bool">
          For client-affine subscriptions, this value indicates whether the subscription is shared or not. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="count_details" type="object">
      Message Count Details. Optional.
      <Expandable title="properties">
        <ResponseField name="active_message_count" type="number">
          Number of active messages in the queue, topic, or subscription. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="dead_letter_message_count" type="number">
          Number of messages that are dead lettered. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="scheduled_message_count" type="number">
          Number of scheduled messages. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="transfer_dead_letter_message_count" type="number">
          Number of messages transferred into dead letters. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="transfer_message_count" type="number">
          Number of messages transferred to another queue, topic, or subscription. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_at" type="string">
      Exact time the message was created. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="dead_lettering_on_filter_evaluation_exceptions" type="bool">
      Value that indicates whether a subscription has dead letter support on filter evaluation exceptions. Optional.
    </ResponseField>
    <ResponseField name="dead_lettering_on_message_expiration" type="bool">
      Value that indicates whether a subscription has dead letter support when a message expires. Optional.
    </ResponseField>
    <ResponseField name="default_message_time_to_live" type="string">
      ISO 8061 Default message timespan to live value. This is the duration after which the message expires, starting from when the message is sent to Service Bus. This is the default value used when TimeToLive is not set on a message itself. Optional.
    </ResponseField>
    <ResponseField name="duplicate_detection_history_time_window" type="string">
      ISO 8601 timeSpan structure that defines the duration of the duplicate detection history. The default value is 10 minutes. Optional.
    </ResponseField>
    <ResponseField name="enable_batched_operations" type="bool">
      Value that indicates whether server-side batched operations are enabled. Optional.
    </ResponseField>
    <ResponseField name="forward_dead_lettered_messages_to" type="string">
      Queue/Topic name to forward the Dead Letter message Optional.
    </ResponseField>
    <ResponseField name="forward_to" type="string">
      Queue/Topic name to forward the messages Optional.
    </ResponseField>
    <ResponseField name="is_client_affine" type="bool">
      Value that indicates whether the subscription has an affinity to the client id. Optional.
    </ResponseField>
    <ResponseField name="lock_duration" type="string">
      ISO 8061 lock duration timespan for the subscription. The default value is 1 minute. Optional.
    </ResponseField>
    <ResponseField name="max_delivery_count" type="number">
      Number of maximum deliveries. Optional.
    </ResponseField>
    <ResponseField name="message_count" type="number">
      Number of messages. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="requires_session" type="bool">
      Value indicating if a subscription supports the concept of sessions. Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      Entity status. Optional.
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      The exact time the message was updated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="user_metadata" type="string">
      Gets and Sets Metadata of User. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
