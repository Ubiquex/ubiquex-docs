---
title: "azurerm_servicebus_subscription"
description: "Real, generated bindings for `azurerm_servicebus_subscription`."
---

`azurerm_servicebus_subscription` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own subscription"})

    		ubx.Resource(servicebus.Subscription, "ci-runner", servicebus.SubscriptionConfig{
    			MaxDeliveryCount:                     1,
    			Name:                                 "ci-runner",
    			TopicId:                              "example",
    			AutoDeleteOnIdle:                     "example",
    			BatchedOperationsEnabled:             true,
    			ClientScopedSubscriptionEnabled:      true,
    			DeadLetteringOnFilterEvaluationError: true,
    			DeadLetteringOnMessageExpiration:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Subscription } from "jsr:@ubx/sdk-azure/azurerm/servicebus/subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own subscription" });

      resource(Subscription, "ci-runner", {
        maxDeliveryCount: 1,
        name: "ci-runner",
        topicId: "example",
        autoDeleteOnIdle: "example",
        batchedOperationsEnabled: true,
        clientScopedSubscriptionEnabled: true,
        deadLetteringOnFilterEvaluationError: true,
        deadLetteringOnMessageExpiration: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.servicebus import Subscription, SubscriptionConfig

    def describe():
        ubx.intent("payments own subscription")

        ubx.resource(Subscription, "ci-runner", SubscriptionConfig(
            max_delivery_count=1,
            name="ci-runner",
            topic_id="example",
            auto_delete_on_idle="example",
            batched_operations_enabled=True,
            client_scoped_subscription_enabled=True,
            dead_lettering_on_filter_evaluation_error=True,
            dead_lettering_on_message_expiration=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_servicebus_subscription called "ci-runner", with
    max delivery count 1, topic id example, auto delete on idle
    example, batched operations enabled true, client scoped
    subscription enabled true, dead lettering on filter evaluation
    error true, dead lettering on message expiration true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_delete_on_idle" type="string">
  Optional.
</ResponseField>

<ResponseField name="batched_operations_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="client_scoped_subscription" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="client_id" type="string">
    </ResponseField>
    <ResponseField name="is_client_scoped_subscription_durable" type="bool">
    </ResponseField>
    <ResponseField name="is_client_scoped_subscription_shareable" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="client_scoped_subscription_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="dead_lettering_on_filter_evaluation_error" type="bool">
  Optional.
</ResponseField>

<ResponseField name="dead_lettering_on_message_expiration" type="bool">
  Optional.
</ResponseField>

<ResponseField name="default_message_ttl" type="string">
  Optional.
</ResponseField>

<ResponseField name="forward_dead_lettered_messages_to" type="string">
  Optional.
</ResponseField>

<ResponseField name="forward_to" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="lock_duration" type="string">
  Optional.
</ResponseField>

<ResponseField name="max_delivery_count" type="number" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="requires_session" type="bool">
  Optional.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="topic_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="client_scoped_subscription" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="client_id" type="string">
    </ResponseField>
    <ResponseField name="is_client_scoped_subscription_durable" type="bool">
    </ResponseField>
    <ResponseField name="is_client_scoped_subscription_shareable" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
