---
title: "azure_servicebus_migration_config_properties"
description: "`azure_servicebus_migration_config_properties` migrates a Service Bus Standard namespace to Premium tier in place..."
---

`azure_servicebus_migration_config_properties` migrates a Service Bus Standard namespace to Premium tier in place, preserving the namespace's own connection strings and endpoint.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_servicebus --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_servicebus/sdk/go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_servicebus/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own servicebus migration config properties"})

    		ubx.Resource(servicebus.ServicebusMigrationConfigProperties, "example", servicebus.ServicebusMigrationConfigPropertiesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_servicebus --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicebusMigrationConfigProperties } from "./local-sdk/azure_servicebus/sdk/typescript/azure_servicebus/servicebus/servicebus_migration_config_properties.ts";

    export default stack("example", () => {
      intent({ summary: "example own servicebus migration config properties" });

      resource(ServicebusMigrationConfigProperties, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_servicebus --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_servicebus/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_servicebus.servicebus import ServicebusMigrationConfigProperties, ServicebusMigrationConfigPropertiesConfig

    def describe():
        ubx.intent("example own servicebus migration config properties")

        ubx.resource(ServicebusMigrationConfigProperties, "example", ServicebusMigrationConfigPropertiesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicebus_migration_config_properties, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties required to the Create Migration Configuration Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="migration_state" type="string">
      State in which Standard to Premium Migration is, possible values : Unknown, Reverting, Completing, Initiating, Syncing, Active Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="pending_replication_operations_count" type="number">
      Number of entities pending to be replicated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="post_migration_name" type="string" required>
      Name to access Standard Namespace after migration Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of Migration ConfigurationProvisioning state of Migration Configuration Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="target_namespace" type="string" required>
      Existing premium Namespace ARM Id name which has no entities, will be used for migration Required.
    </ResponseField>
  </Expandable>
</ResponseField>
