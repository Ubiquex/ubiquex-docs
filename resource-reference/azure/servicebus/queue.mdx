---
title: "azurerm_servicebus_queue"
description: "Real, generated bindings for `azurerm_servicebus_queue`."
---

`azurerm_servicebus_queue` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own queue"})

    		ubx.Resource(servicebus.Queue, "ci-runner", servicebus.QueueConfig{
    			Name:                                "ci-runner",
    			NamespaceId:                         "example",
    			BatchedOperationsEnabled:            true,
    			DeadLetteringOnMessageExpiration:    true,
    			DuplicateDetectionHistoryTimeWindow: "example",
    			ExpressEnabled:                      true,
    			ForwardDeadLetteredMessagesTo:       "example",
    			ForwardTo:                           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Queue } from "jsr:@ubx/sdk-azure/azurerm/servicebus/queue";

    export default stack("payments", () => {
      intent({ summary: "payments own queue" });

      resource(Queue, "ci-runner", {
        name: "ci-runner",
        namespaceId: "example",
        batchedOperationsEnabled: true,
        deadLetteringOnMessageExpiration: true,
        duplicateDetectionHistoryTimeWindow: "example",
        expressEnabled: true,
        forwardDeadLetteredMessagesTo: "example",
        forwardTo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.servicebus import Queue, QueueConfig

    def describe():
        ubx.intent("payments own queue")

        ubx.resource(Queue, "ci-runner", QueueConfig(
            name="ci-runner",
            namespace_id="example",
            batched_operations_enabled=True,
            dead_lettering_on_message_expiration=True,
            duplicate_detection_history_time_window="example",
            express_enabled=True,
            forward_dead_lettered_messages_to="example",
            forward_to="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_servicebus_queue called "ci-runner", with
    namespace id example, batched operations enabled true, dead
    lettering on message expiration true, duplicate detection history
    time window example, express enabled true, forward dead lettered
    messages to example, forward to example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_delete_on_idle" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="batched_operations_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="dead_lettering_on_message_expiration" type="bool">
  Optional.
</ResponseField>

<ResponseField name="default_message_ttl" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="duplicate_detection_history_time_window" type="string">
  Optional.
</ResponseField>

<ResponseField name="express_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="forward_dead_lettered_messages_to" type="string">
  Optional.
</ResponseField>

<ResponseField name="forward_to" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="lock_duration" type="string">
  Optional.
</ResponseField>

<ResponseField name="max_delivery_count" type="number">
  Optional.
</ResponseField>

<ResponseField name="max_message_size_in_kilobytes" type="number">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="max_size_in_megabytes" type="number">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="namespace_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="partitioning_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="requires_duplicate_detection" type="bool">
  Optional.
</ResponseField>

<ResponseField name="requires_session" type="bool">
  Optional.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="auto_delete_on_idle" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="default_message_ttl" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="max_message_size_in_kilobytes" type="number">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="max_size_in_megabytes" type="number">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
