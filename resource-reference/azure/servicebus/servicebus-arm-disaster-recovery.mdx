---
title: "azure_servicebus_arm_disaster_recovery"
description: "`azure_servicebus_arm_disaster_recovery` pairs a Service Bus namespace with a secondary namespace in another region for geo-disaster..."
---

`azure_servicebus_arm_disaster_recovery` pairs a Service Bus namespace with a secondary namespace in another region for geo-disaster recovery, replicating metadata (not message data) between them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_servicebus_servicebus --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_servicebus_servicebus/sdk/go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_servicebus_servicebus/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own servicebus arm disaster recovery"})

    		ubx.Resource(servicebus.ServicebusArmDisasterRecovery, "example", servicebus.ServicebusArmDisasterRecoveryConfig{
    			Alias: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_servicebus_servicebus --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicebusArmDisasterRecovery } from "./local-sdk/azure_servicebus_servicebus/sdk/typescript/azure_servicebus_servicebus/servicebus/servicebus_arm_disaster_recovery.ts";

    export default stack("example", () => {
      intent({ summary: "example own servicebus arm disaster recovery" });

      resource(ServicebusArmDisasterRecovery, "example", {
        alias: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_servicebus_servicebus --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_servicebus_servicebus/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_servicebus_servicebus.servicebus import ServicebusArmDisasterRecovery, ServicebusArmDisasterRecoveryConfig

    def describe():
        ubx.intent("example own servicebus arm disaster recovery")

        ubx.resource(ServicebusArmDisasterRecovery, "example", ServicebusArmDisasterRecoveryConfig(
            alias="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicebus_arm_disaster_recovery, with alias
    example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="alias" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties required to the Create Or Update Alias(Disaster Recovery configurations) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="alternate_name" type="string">
      Primary/Secondary eventhub namespace name, which is part of GEO DR pairing Optional.
    </ResponseField>
    <ResponseField name="partner_namespace" type="string">
      ARM Id of the Primary/Secondary eventhub namespace name, which is part of GEO DR pairing Optional.
    </ResponseField>
    <ResponseField name="pending_replication_operations_count" type="number">
      Number of entities pending to be replicated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the Alias(Disaster Recovery configuration) - possible values 'Accepted' or 'Succeeded' or 'Failed' Optional.
    </ResponseField>
    <ResponseField name="role" type="string">
      role of namespace in GEO DR - possible values 'Primary' or 'PrimaryNotReplicating' or 'Secondary' Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
