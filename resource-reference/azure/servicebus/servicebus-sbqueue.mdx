---
title: "azure_servicebus_servicebus_sbqueue"
description: "`azure_servicebus_servicebus_sbqueue` is a Service Bus queue, providing reliable, ordered..."
---

`azure_servicebus_servicebus_sbqueue` is a Service Bus queue, providing reliable, ordered, first-in-first-out message delivery to a single consumer per message.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_servicebus_servicebus --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_servicebus_servicebus/sdk/go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_servicebus_servicebus/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own servicebus sbqueue"})

    		ubx.Resource(servicebus.ServicebusSbqueue, "ci-runner", servicebus.ServicebusSbqueueConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_servicebus_servicebus --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicebusSbqueue } from "./local-sdk/azure_servicebus_servicebus/sdk/typescript/azure_servicebus_servicebus/servicebus/servicebus_sbqueue.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own servicebus sbqueue" });

      resource(ServicebusSbqueue, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_servicebus_servicebus --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_servicebus_servicebus/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_servicebus_servicebus.servicebus import ServicebusSbqueue, ServicebusSbqueueConfig

    def describe():
        ubx.intent("payments own servicebus sbqueue")

        ubx.resource(ServicebusSbqueue, "ci-runner", ServicebusSbqueueConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicebus_servicebus_sbqueue, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The Queue Properties definition. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="accessed_at" type="string">
      Last time a message was sent, or the last time there was a receive request to this queue. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="auto_delete_on_idle" type="string">
      ISO 8061 timeSpan idle interval after which the queue is automatically deleted. The minimum duration is 5 minutes. Optional.
    </ResponseField>
    <ResponseField name="count_details" type="object">
      Message Count Details. Optional.
      <Expandable title="properties">
        <ResponseField name="active_message_count" type="number">
          Number of active messages in the queue, topic, or subscription. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="dead_letter_message_count" type="number">
          Number of messages that are dead lettered. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="scheduled_message_count" type="number">
          Number of scheduled messages. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="transfer_dead_letter_message_count" type="number">
          Number of messages transferred into dead letters. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="transfer_message_count" type="number">
          Number of messages transferred to another queue, topic, or subscription. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_at" type="string">
      The exact time the message was created. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="dead_lettering_on_message_expiration" type="bool">
      A value that indicates whether this queue has dead letter support when a message expires. Optional.
    </ResponseField>
    <ResponseField name="default_message_time_to_live" type="string">
      ISO 8601 default message timespan to live value. This is the duration after which the message expires, starting from when the message is sent to Service Bus. This is the default value used when TimeToLive is not set on a message itself. Optional.
    </ResponseField>
    <ResponseField name="duplicate_detection_history_time_window" type="string">
      ISO 8601 timeSpan structure that defines the duration of the duplicate detection history. The default value is 10 minutes. Optional.
    </ResponseField>
    <ResponseField name="enable_batched_operations" type="bool">
      Value that indicates whether server-side batched operations are enabled. Optional.
    </ResponseField>
    <ResponseField name="enable_express" type="bool">
      A value that indicates whether Express Entities are enabled. An express queue holds a message in memory temporarily before writing it to persistent storage. Optional.
    </ResponseField>
    <ResponseField name="enable_partitioning" type="bool">
      A value that indicates whether the queue is to be partitioned across multiple message brokers. Optional.
    </ResponseField>
    <ResponseField name="forward_dead_lettered_messages_to" type="string">
      Queue/Topic name to forward the Dead Letter message Optional.
    </ResponseField>
    <ResponseField name="forward_to" type="string">
      Queue/Topic name to forward the messages Optional.
    </ResponseField>
    <ResponseField name="lock_duration" type="string">
      ISO 8601 timespan duration of a peek-lock; that is, the amount of time that the message is locked for other receivers. The maximum value for LockDuration is 5 minutes; the default value is 1 minute. Optional.
    </ResponseField>
    <ResponseField name="max_delivery_count" type="number">
      The maximum delivery count. A message is automatically deadlettered after this number of deliveries. default value is 10. Optional.
    </ResponseField>
    <ResponseField name="max_message_size_in_kilobytes" type="number">
      Maximum size (in KB) of the message payload that can be accepted by the queue. This property is only used in Premium today and default is 1024. Optional.
    </ResponseField>
    <ResponseField name="max_size_in_megabytes" type="number">
      The maximum size of the queue in megabytes, which is the size of memory allocated for the queue. Default is 1024. Optional.
    </ResponseField>
    <ResponseField name="message_count" type="number">
      The number of messages in the queue. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="requires_duplicate_detection" type="bool">
      A value indicating if this queue requires duplicate detection. Optional.
    </ResponseField>
    <ResponseField name="requires_session" type="bool">
      A value that indicates whether the queue supports the concept of sessions. Optional.
    </ResponseField>
    <ResponseField name="size_in_bytes" type="number">
      The size of the queue, in bytes. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="status" type="string">
      Entity status. Optional.
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      The exact time the message was updated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="user_metadata" type="string">
      Gets and Sets Metadata of User. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
