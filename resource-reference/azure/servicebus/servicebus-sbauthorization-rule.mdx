---
title: "azure_servicebus_sbauthorization_rule"
description: "`azure_servicebus_servicebus_sbauthorization_rule` defines a shared access policy (with specific permissions) for connecting to a Service Bus..."
---

`azure_servicebus_servicebus_sbauthorization_rule` defines a shared access policy (with specific permissions) for connecting to a Service Bus namespace, queue, or topic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_servicebus_servicebus --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_servicebus_servicebus/sdk/go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_servicebus_servicebus/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own servicebus sbauthorization rule"})

    		ubx.Resource(servicebus.ServicebusSbauthorizationRule, "example", servicebus.ServicebusSbauthorizationRuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_servicebus_servicebus --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicebusSbauthorizationRule } from "./local-sdk/azure_servicebus_servicebus/sdk/typescript/azure_servicebus_servicebus/servicebus/servicebus_sbauthorization_rule.ts";

    export default stack("example", () => {
      intent({ summary: "example own servicebus sbauthorization rule" });

      resource(ServicebusSbauthorizationRule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_servicebus_servicebus --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_servicebus_servicebus/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_servicebus_servicebus.servicebus import ServicebusSbauthorizationRule, ServicebusSbauthorizationRuleConfig

    def describe():
        ubx.intent("example own servicebus sbauthorization rule")

        ubx.resource(ServicebusSbauthorizationRule, "example", ServicebusSbauthorizationRuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicebus_sbauthorization_rule, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  AuthorizationRule properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="rights" type="list(string)" required>
      The rights associated with the rule. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
