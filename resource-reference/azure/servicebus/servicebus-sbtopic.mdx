---
title: "azure_servicebus_sbtopic"
description: "`azure_servicebus_servicebus_sbtopic` is a Service Bus topic..."
---

`azure_servicebus_servicebus_sbtopic` is a Service Bus topic, publishing message(s) to every subscription attached to it, for one-to-many publish/subscribe messaging.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_servicebus_servicebus --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_servicebus_servicebus/sdk/go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_servicebus_servicebus/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own servicebus sbtopic"})

    		ubx.Resource(servicebus.ServicebusSbtopic, "example", servicebus.ServicebusSbtopicConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_servicebus_servicebus --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicebusSbtopic } from "./local-sdk/azure_servicebus_servicebus/sdk/typescript/azure_servicebus_servicebus/servicebus/servicebus_sbtopic.ts";

    export default stack("example", () => {
      intent({ summary: "example own servicebus sbtopic" });

      resource(ServicebusSbtopic, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_servicebus_servicebus --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_servicebus_servicebus/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_servicebus_servicebus.servicebus import ServicebusSbtopic, ServicebusSbtopicConfig

    def describe():
        ubx.intent("example own servicebus sbtopic")

        ubx.resource(ServicebusSbtopic, "example", ServicebusSbtopicConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicebus_sbtopic, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The Topic Properties definition. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="accessed_at" type="string">
      Last time the message was sent, or a request was received, for this topic. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="auto_delete_on_idle" type="string">
      ISO 8601 timespan idle interval after which the topic is automatically deleted. The minimum duration is 5 minutes. Optional.
    </ResponseField>
    <ResponseField name="count_details" type="object">
      Message Count Details. Optional.
      <Expandable title="properties">
        <ResponseField name="active_message_count" type="number">
          Number of active messages in the queue, topic, or subscription. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="dead_letter_message_count" type="number">
          Number of messages that are dead lettered. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="scheduled_message_count" type="number">
          Number of scheduled messages. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="transfer_dead_letter_message_count" type="number">
          Number of messages transferred into dead letters. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="transfer_message_count" type="number">
          Number of messages transferred to another queue, topic, or subscription. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_at" type="string">
      Exact time the message was created. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="default_message_time_to_live" type="string">
      ISO 8601 Default message timespan to live value. This is the duration after which the message expires, starting from when the message is sent to Service Bus. This is the default value used when TimeToLive is not set on a message itself. Optional.
    </ResponseField>
    <ResponseField name="duplicate_detection_history_time_window" type="string">
      ISO8601 timespan structure that defines the duration of the duplicate detection history. The default value is 10 minutes. Optional.
    </ResponseField>
    <ResponseField name="enable_batched_operations" type="bool">
      Value that indicates whether server-side batched operations are enabled. Optional.
    </ResponseField>
    <ResponseField name="enable_express" type="bool">
      Value that indicates whether Express Entities are enabled. An express topic holds a message in memory temporarily before writing it to persistent storage. Optional.
    </ResponseField>
    <ResponseField name="enable_partitioning" type="bool">
      Value that indicates whether the topic to be partitioned across multiple message brokers is enabled. Optional.
    </ResponseField>
    <ResponseField name="max_message_size_in_kilobytes" type="number">
      Maximum size (in KB) of the message payload that can be accepted by the topic. This property is only used in Premium today and default is 1024. Optional.
    </ResponseField>
    <ResponseField name="max_size_in_megabytes" type="number">
      Maximum size of the topic in megabytes, which is the size of the memory allocated for the topic. Default is 1024. Optional.
    </ResponseField>
    <ResponseField name="requires_duplicate_detection" type="bool">
      Value indicating if this topic requires duplicate detection. Optional.
    </ResponseField>
    <ResponseField name="size_in_bytes" type="number">
      Size of the topic, in bytes. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="status" type="string">
      Entity status. Optional.
    </ResponseField>
    <ResponseField name="subscription_count" type="number">
      Number of subscriptions. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="support_ordering" type="bool">
      Value that indicates whether the topic supports ordering. Optional.
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      The exact time the message was updated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="user_metadata" type="string">
      Gets and Sets Metadata of User. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
