---
title: "azure_batch_openapi_application_package"
description: "`azure_batch_openapi_application_package` is a specific uploaded version of an Azure Batch application..."
---

`azure_batch_openapi_application_package` is a specific uploaded version of an Azure Batch application, containing the actual executable and supporting file(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi application package"})

    		ubx.Resource(batch.OpenapiApplicationPackage, "example", batch.OpenapiApplicationPackageConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiApplicationPackage } from "@ubx/sdk-azure/azure/batch/openapi_application_package";

    export default stack("example", () => {
      intent({ summary: "example own openapi application package" });

      resource(OpenapiApplicationPackage, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.batch.openapi_application_package import OpenapiApplicationPackage, OpenapiApplicationPackageConfig

    def describe():
        ubx.intent("example own openapi application package")

        ubx.resource(OpenapiApplicationPackage, "example", OpenapiApplicationPackageConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_batch_openapi_application_package, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  The ETag of the resource, used for concurrency statements. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of an application package Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="format" type="string">
      The format of the application package, if the package is active. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="last_activation_time" type="string">
      The time at which the package was last activated, if the package is active. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="state" type="string">
      The current state of the application package. Optional.
    </ResponseField>
    <ResponseField name="storage_url" type="string">
      The URL for the application package in Azure Storage. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="storage_url_expiry" type="string">
      The UTC time at which the Azure Storage URL will expire. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  The tags of the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
