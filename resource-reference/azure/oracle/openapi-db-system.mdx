---
title: "azure_oracle_openapi_db_system"
description: "`azure_oracle_openapi_db_system` provisions an Oracle Base Database Service system through Oracle Database@Azure..."
---

`azure_oracle_openapi_db_system` provisions an Oracle Base Database Service system through Oracle Database@Azure, a lighter-weight (non-Exadata) managed Oracle database deployment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oracle "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/oracle"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi db system"})

    		ubx.Resource(oracle.OpenapiDbSystem, "example", oracle.OpenapiDbSystemConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiDbSystem } from "@ubx/sdk-azure/azure/oracle/openapi_db_system";

    export default stack("example", () => {
      intent({ summary: "example own openapi db system" });

      resource(OpenapiDbSystem, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.oracle import OpenapiDbSystem, OpenapiDbSystemConfig

    def describe():
        ubx.intent("example own openapi db system")

        ubx.resource(OpenapiDbSystem, "example", OpenapiDbSystemConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_oracle_openapi_db_system, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  DbSystem resource model. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="admin_password" type="string">
      Password string. Optional.
    </ResponseField>
    <ResponseField name="database_edition" type="string" required>
      Database edition type enum. Required.
    </ResponseField>
    <ResponseField name="db_version" type="string" required>
      A valid Oracle Database version. For a list of supported versions, use the ListDbVersions operation. Required.
    </ResponseField>
    <ResponseField name="pdb_name" type="string">
      The name of the pluggable database. The name must begin with an alphabetic character and can contain a maximum of thirty alphanumeric characters. Special characters are not permitted. Pluggable database should not be same as database name. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The availability zones. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
