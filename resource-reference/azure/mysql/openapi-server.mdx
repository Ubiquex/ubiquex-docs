---
title: "azure_mysql_openapi_server"
description: "`azure_mysql_openapi_server` is an Azure Database for MySQL flexible server, a fully managed MySQL database server."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_mysql_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_mysql_openapi/sdk/go
    package main

    import (
    	mysql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_mysql_openapi/mysql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi server"})

    		ubx.Resource(mysql.OpenapiServer, "ci-runner", mysql.OpenapiServerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_mysql_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiServer } from "./local-sdk/azure_mysql_openapi/sdk/typescript/azure_mysql_openapi/mysql/openapi_server.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi server" });

      resource(OpenapiServer, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_mysql_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_mysql_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_mysql_openapi.mysql import OpenapiServer, OpenapiServerConfig

    def describe():
        ubx.intent("payments own openapi server")

        ubx.resource(OpenapiServer, "ci-runner", OpenapiServerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_mysql_openapi_server, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Properties to configure Identity for Bring your Own Keys Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      ObjectId from the KeyVault Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      TenantId from the KeyVault Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      Type of managed service identity. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      Metadata of user assigned identity. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of a server. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="administrator_login" type="string">
      The administrator's login name of a server. Can only be specified when the server is being created (and is required for creation). Optional.
    </ResponseField>
    <ResponseField name="administrator_login_password" type="string">
      The password of the administrator login (required for server creation). Optional.
    </ResponseField>
    <ResponseField name="availability_zone" type="string">
      availability Zone information of the server. Optional.
    </ResponseField>
    <ResponseField name="backup" type="object">
      Storage Profile properties of a server Optional.
      <Expandable title="properties">
        <ResponseField name="backup_interval_hours" type="number">
          Backup interval hours for the server. Optional.
        </ResponseField>
        <ResponseField name="backup_retention_days" type="number">
          Backup retention days for the server. Optional.
        </ResponseField>
        <ResponseField name="earliest_restore_date" type="string">
          Earliest restore point creation time (ISO8601 format) Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="geo_redundant_backup" type="string">
          Whether or not geo redundant backup is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_mode" type="string">
      The mode to create a new MySQL server. Optional.
    </ResponseField>
    <ResponseField name="data_encryption" type="object">
      The date encryption for cmk. Optional.
      <Expandable title="properties">
        <ResponseField name="geo_backup_key_uri" type="string">
          Geo backup key uri as key vault can't cross region, need cmk in same region as geo backup Optional.
        </ResponseField>
        <ResponseField name="geo_backup_user_assigned_identity_id" type="string">
          Geo backup user identity resource id as identity can't cross region, need identity in same region as geo backup Optional.
        </ResponseField>
        <ResponseField name="primary_key_uri" type="string">
          Primary key uri Optional.
        </ResponseField>
        <ResponseField name="primary_user_assigned_identity_id" type="string">
          Primary user identity resource id Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          The key type, AzureKeyVault for enable cmk, SystemManaged for disable cmk. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="database_port" type="number">
      The server database port. Can only be specified when the server is being created. Optional.
    </ResponseField>
    <ResponseField name="full_version" type="string">
      Major version and actual engine version Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="fully_qualified_domain_name" type="string">
      The fully qualified domain name of a server. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="high_availability" type="object">
      High availability properties of a server Optional.
      <Expandable title="properties">
        <ResponseField name="mode" type="string">
          High availability mode for a server. Optional.
        </ResponseField>
        <ResponseField name="standby_availability_zone" type="string">
          Availability zone of the standby server. Optional.
        </ResponseField>
        <ResponseField name="state" type="string">
          The state of server high availability. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="import_source_properties" type="object">
      Import source related properties. Optional.
      <Expandable title="properties">
        <ResponseField name="data_dir_path" type="string">
          Relative path of data directory in storage. Optional.
        </ResponseField>
        <ResponseField name="sas_token" type="string">
          Sas token for accessing source storage. Read and list permissions are required for sas token. Optional.
        </ResponseField>
        <ResponseField name="storage_type" type="string">
          Storage type of import source. Optional.
        </ResponseField>
        <ResponseField name="storage_url" type="string">
          Uri of the import source storage. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="maintenance_policy" type="object">
      Maintenance policy of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="patch_strategy" type="string">
          Enum to indicate the patch strategy of a server Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="maintenance_window" type="object">
      Maintenance window of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="batch_of_maintenance" type="string">
          The batch of maintenance when enabled the custom managed maintenance window of a server. Optional.
        </ResponseField>
        <ResponseField name="custom_window" type="string">
          indicates whether custom window is enabled or disabled Optional.
        </ResponseField>
        <ResponseField name="day_of_week" type="number">
          day of week for maintenance window Optional.
        </ResponseField>
        <ResponseField name="start_hour" type="number">
          start hour for maintenance window Optional.
        </ResponseField>
        <ResponseField name="start_minute" type="number">
          start minute for maintenance window Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network" type="object">
      Network related properties of a server Optional.
      <Expandable title="properties">
        <ResponseField name="delegated_subnet_resource_id" type="string">
          Delegated subnet resource id used to setup vnet for a server. Optional.
        </ResponseField>
        <ResponseField name="private_dns_zone_resource_id" type="string">
          Private DNS zone resource id. Optional.
        </ResponseField>
        <ResponseField name="public_network_access" type="string">
          Enum to indicate whether value is 'Enabled' or 'Disabled' Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      PrivateEndpointConnections related properties of a server. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="replica_capacity" type="number">
      The maximum number of replicas that a primary server can have. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="replication_role" type="string">
      The replication role. Optional.
    </ResponseField>
    <ResponseField name="restore_point_in_time" type="string">
      Restore point creation time (ISO8601 format), specifying the time to restore from. Optional.
    </ResponseField>
    <ResponseField name="source_server_resource_id" type="string">
      The source MySQL server id. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      The state of a server. Optional.
    </ResponseField>
    <ResponseField name="storage" type="object">
      Storage Profile properties of a server Optional.
      <Expandable title="properties">
        <ResponseField name="auto_grow" type="string">
          Enable Storage Auto Grow or not. Optional.
        </ResponseField>
        <ResponseField name="auto_io_scaling" type="string">
          Enable IO Auto Scaling or not. Optional.
        </ResponseField>
        <ResponseField name="iops" type="number">
          Storage IOPS for a server. Optional.
        </ResponseField>
        <ResponseField name="log_on_disk" type="string">
          Enable Log On Disk or not. Optional.
        </ResponseField>
        <ResponseField name="storage_redundancy" type="string">
          The redundant type of the server storage. The parameter is used for server creation. Optional.
        </ResponseField>
        <ResponseField name="storage_size_gb" type="number">
          Max storage size allowed for a server. Optional.
        </ResponseField>
        <ResponseField name="storage_sku" type="string">
          The sku name of the server storage. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="version" type="string">
      The major version of a server. 8.0.21 stands for MySQL 8.0, 5.7.44 stands for MySQL 5.7 Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  Billing information related properties of a server. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      The name of the sku, e.g. Standard_D32s_v3. Required.
    </ResponseField>
    <ResponseField name="tier" type="string" required>
      The tier of the particular SKU, e.g. GeneralPurpose. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
