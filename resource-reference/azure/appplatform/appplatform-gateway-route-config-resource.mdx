---
title: "azure_appplatform_gateway_route_config_resource"
description: "`azure_appplatform_appplatform_gateway_route_config_resource` defines routing rule(s) directing matching Spring Cloud Gateway traffic to a specific..."
---

`azure_appplatform_appplatform_gateway_route_config_resource` defines routing rule(s) directing matching Spring Cloud Gateway traffic to a specific application within Azure Spring Apps.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appplatform "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/appplatform"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own appplatform gateway route config resource"})

    		ubx.Resource(appplatform.GatewayRouteConfigResource, "example", appplatform.GatewayRouteConfigResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayRouteConfigResource } from "@ubx/sdk-azure/azure/appplatform/gateway_route_config_resource";

    export default stack("example", () => {
      intent({ summary: "example own appplatform gateway route config resource" });

      resource(GatewayRouteConfigResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.appplatform import GatewayRouteConfigResource, GatewayRouteConfigResourceConfig

    def describe():
        ubx.intent("example own appplatform gateway route config resource")

        ubx.resource(GatewayRouteConfigResource, "example", GatewayRouteConfigResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_appplatform_gateway_route_config_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  API route config of the Spring Cloud Gateway Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="app_resource_id" type="string">
      The resource Id of the Azure Spring Apps app, required unless route defines `uri`. Optional.
    </ResponseField>
    <ResponseField name="filters" type="list(string)">
      To modify the request before sending it to the target endpoint, or the received response in app level. Optional.
    </ResponseField>
    <ResponseField name="open_api" type="object">
      OpenAPI properties of Spring Cloud Gateway route config. Optional.
      <Expandable title="properties">
        <ResponseField name="uri" type="string">
          The URI of OpenAPI specification. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="predicates" type="list(string)">
      A number of conditions to evaluate a route for each request in app level. Each predicate may be evaluated against request headers and parameter values. All of the predicates associated with a route must evaluate to true for the route to be matched to the request. Optional.
    </ResponseField>
    <ResponseField name="protocol" type="string">
      Protocol of routed Azure Spring Apps applications. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      State of the Spring Cloud Gateway route config. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="routes" type="list(object)">
      Array of API routes, each route contains properties such as `title`, `uri`, `ssoEnabled`, `predicates`, `filters`. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          A human-readable description of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="filters" type="list(string)">
          The condition(s) restricting which request(s) this rule applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="order" type="number">
          The relative evaluation order of this item. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="predicates" type="list(string)">
          The condition(s) a request must satisfy for this route to match. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sso_enabled" type="bool">
          Whether single sign-on is enabled for this application. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="tags" type="list(string)">
          User-defined key/value tags on this resource, usable for organizing and filtering resources across the subscription. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="title" type="string">
          A human-readable title for this item. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="token_relay" type="bool">
          Whether the caller's own OAuth access token is forwarded to backend services. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="uri" type="string">
          A resource URI. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sso_enabled" type="bool">
      Enable Single Sign-On in app level. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
