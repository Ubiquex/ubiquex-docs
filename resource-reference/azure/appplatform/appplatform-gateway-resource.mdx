---
title: "azure_appplatform_gateway_resource"
description: "`azure_appplatform_appplatform_gateway_resource` deploys Spring Cloud Gateway..."
---

`azure_appplatform_appplatform_gateway_resource` deploys Spring Cloud Gateway, the managed API gateway routing external traffic to application(s) within an Azure Spring Apps instance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appplatform "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/appplatform"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own appplatform gateway resource"})

    		ubx.Resource(appplatform.GatewayResource, "example", appplatform.GatewayResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayResource } from "@ubx/sdk-azure/azure/appplatform/gateway_resource";

    export default stack("example", () => {
      intent({ summary: "example own appplatform gateway resource" });

      resource(GatewayResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.appplatform import GatewayResource, GatewayResourceConfig

    def describe():
        ubx.intent("example own appplatform gateway resource")

        ubx.resource(GatewayResource, "example", GatewayResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_appplatform_gateway_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Spring Cloud Gateway properties payload Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="api_metadata_properties" type="object">
      API metadata property for Spring Cloud Gateway Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Detailed description of the APIs available on the Gateway instance (default: `Generated OpenAPI 3 document that describes the API routes configured.`) Optional.
        </ResponseField>
        <ResponseField name="documentation" type="string">
          Location of additional documentation for the APIs available on the Gateway instance Optional.
        </ResponseField>
        <ResponseField name="server_url" type="string">
          Base URL that API consumers will use to access APIs on the Gateway instance. Optional.
        </ResponseField>
        <ResponseField name="title" type="string">
          Title describing the context of the APIs available on the Gateway instance (default: `Spring Cloud Gateway for K8S`) Optional.
        </ResponseField>
        <ResponseField name="version" type="string">
          Version of APIs available on this Gateway instance (default: `unspecified`). Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="apms" type="list(object)">
      Collection of ApmReferences Optional.
      <Expandable title="properties">
        <ResponseField name="resource_id" type="string">
          A reference to the resource this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="client_auth" type="object">
      Client-Certification Authentication. Optional.
      <Expandable title="properties">
        <ResponseField name="certificate_verification" type="string">
          Whether to enable certificate verification or not Optional.
        </ResponseField>
        <ResponseField name="certificates" type="list(string)">
          Collection of certificate resource Ids in Azure Spring Apps. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cors_properties" type="object">
      Cross-Origin Resource Sharing property Optional.
      <Expandable title="properties">
        <ResponseField name="allow_credentials" type="bool">
          Whether user credentials are supported on cross-site requests. Valid values: `true`, `false`. Optional.
        </ResponseField>
        <ResponseField name="allowed_headers" type="list(string)">
          Allowed headers in cross-site requests. The special value `*` allows actual requests to send any header. Optional.
        </ResponseField>
        <ResponseField name="allowed_methods" type="list(string)">
          Allowed HTTP methods on cross-site requests. The special value `*` allows all methods. If not set, `GET` and `HEAD` are allowed by default. Optional.
        </ResponseField>
        <ResponseField name="allowed_origin_patterns" type="list(string)">
          Allowed origin patterns to make cross-site requests. Optional.
        </ResponseField>
        <ResponseField name="allowed_origins" type="list(string)">
          Allowed origins to make cross-site requests. The special value `*` allows all domains. Optional.
        </ResponseField>
        <ResponseField name="exposed_headers" type="list(string)">
          HTTP response headers to expose for cross-site requests. Optional.
        </ResponseField>
        <ResponseField name="max_age" type="number">
          How long, in seconds, the response from a pre-flight request can be cached by clients. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="environment_variables" type="object">
      Environment variables of Spring Cloud Gateway Optional.
      <Expandable title="properties">
        <ResponseField name="properties" type="map(string)">
          Non-sensitive properties Optional.
        </ResponseField>
        <ResponseField name="secrets" type="map(string)">
          Sensitive properties Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="https_only" type="bool">
      Indicate if only https is allowed. Optional.
    </ResponseField>
    <ResponseField name="instances" type="list(object)">
      Collection of instances belong to Spring Cloud Gateway. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          The current status of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="operator_properties" type="object">
      Properties of the Spring Cloud Gateway Operator. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="instances" type="list(object)">
          Collection of instances belong to Spring Cloud Gateway operator. Computed by Microsoft Azure after `ship`.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="status" type="string">
              The current status of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource_requests" type="object">
          Properties of the Spring Cloud Gateway Operator. Computed by Microsoft Azure after `ship`.
          <Expandable title="properties">
            <ResponseField name="cpu" type="string">
              Cpu allocated to each Spring Cloud Gateway Operator instance. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="instance_count" type="number">
              Instance count of the Spring Cloud Gateway Operator. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="memory" type="string">
              Memory allocated to each Spring Cloud Gateway Operator instance. Computed by Microsoft Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      State of the Spring Cloud Gateway. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="public" type="bool">
      Indicates whether the Spring Cloud Gateway exposes endpoint. Optional.
    </ResponseField>
    <ResponseField name="resource_requests" type="object">
      Resource request payload of Spring Cloud Gateway. Optional.
      <Expandable title="properties">
        <ResponseField name="cpu" type="string">
          Cpu allocated to each Spring Cloud Gateway instance. Optional.
        </ResponseField>
        <ResponseField name="memory" type="string">
          Memory allocated to each Spring Cloud Gateway instance. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sso_properties" type="object">
      Single sign-on related configuration Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The public identifier for the application Optional.
        </ResponseField>
        <ResponseField name="client_secret" type="string">
          The secret known only to the application and the authorization server Optional.
        </ResponseField>
        <ResponseField name="issuer_uri" type="string">
          The URI of Issuer Identifier Optional.
        </ResponseField>
        <ResponseField name="scope" type="list(string)">
          It defines the specific actions applications can be allowed to do on a user's behalf Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="url" type="string">
      URL of the Spring Cloud Gateway, exposed when 'public' is true. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  Sku of Azure Spring Apps Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      Current capacity of the target resource Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of the Sku Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      Tier of the Sku Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
