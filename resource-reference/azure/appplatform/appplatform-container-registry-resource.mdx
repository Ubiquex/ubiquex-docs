---
title: "azure_appplatform_container_registry_resource"
description: "`azure_appplatform_container_registry_resource` registers a container registry (in addition to the default one) that Azure Spring Apps can..."
---

`azure_appplatform_container_registry_resource` registers a container registry (in addition to the default one) that Azure Spring Apps can pull custom container image deployments from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appplatform "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/appplatform"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own appplatform container registry resource"})

    		ubx.Resource(appplatform.ContainerRegistryResource, "example", appplatform.ContainerRegistryResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContainerRegistryResource } from "@ubx/sdk-azure/azure/appplatform/container_registry_resource";

    export default stack("example", () => {
      intent({ summary: "example own appplatform container registry resource" });

      resource(ContainerRegistryResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.appplatform.container_registry_resource import ContainerRegistryResource, ContainerRegistryResourceConfig

    def describe():
        ubx.intent("example own appplatform container registry resource")

        ubx.resource(ContainerRegistryResource, "example", ContainerRegistryResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_appplatform_container_registry_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Container registry resource payload. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="credentials" type="object" required>
      The credential for the container registry resource. Required.
      <Expandable title="properties">
        <ResponseField name="type" type="string" required>
          The credential type of the container registry credentials. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      State of the Container Registry. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
