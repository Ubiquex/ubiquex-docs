---
title: "azure_appplatform_build_service"
description: "`azure_appplatform_build_service` is the Cloud Native Buildpacks-based build service within an Azure Spring Apps instance that compiles..."
---

`azure_appplatform_build_service` is the Cloud Native Buildpacks-based build service within an Azure Spring Apps instance that compiles source code into container images.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appplatform "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/appplatform"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own appplatform build service"})

    		ubx.Resource(appplatform.BuildService, "example", appplatform.BuildServiceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BuildService } from "@ubx/sdk-azure/azure/appplatform/build_service";

    export default stack("example", () => {
      intent({ summary: "example own appplatform build service" });

      resource(BuildService, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.appplatform.build_service import BuildService, BuildServiceConfig

    def describe():
        ubx.intent("example own appplatform build service")

        ubx.resource(BuildService, "example", BuildServiceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_appplatform_build_service, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Build service resource properties payload Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="container_registry" type="string">
      The resource id of the container registry used in this build service. Optional.
    </ResponseField>
    <ResponseField name="k_pack_version" type="string">
      The installed KPack version in this build service. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the KPack build service Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="resource_requests" type="object">
      The runtime resource configuration of this build service. Optional.
      <Expandable title="properties">
        <ResponseField name="cpu" type="string">
          vCPU allocated to the entire build service node pool. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="memory" type="string">
          Memory allocated to the entire build service node pool. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
