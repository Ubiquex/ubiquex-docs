---
title: "azure_appplatform_builder_resource"
description: "`azure_appplatform_builder_resource` defines the buildpack(s) and stack used by Azure Spring Apps' own build service to compile application..."
---

`azure_appplatform_builder_resource` defines the buildpack(s) and stack used by Azure Spring Apps' own build service to compile application source into a container image.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appplatform "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/appplatform"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own appplatform builder resource"})

    		ubx.Resource(appplatform.BuilderResource, "example", appplatform.BuilderResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BuilderResource } from "@ubx/sdk-azure/azure/appplatform/builder_resource";

    export default stack("example", () => {
      intent({ summary: "example own appplatform builder resource" });

      resource(BuilderResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.appplatform.builder_resource import BuilderResource, BuilderResourceConfig

    def describe():
        ubx.intent("example own appplatform builder resource")

        ubx.resource(BuilderResource, "example", BuilderResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_appplatform_builder_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  KPack Builder properties payload Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="buildpack_groups" type="list(object)">
      Builder buildpack groups. Optional.
      <Expandable title="properties">
        <ResponseField name="buildpacks" type="list(object)">
          The buildpack(s) applied when building this application. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Builder provision status. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="stack" type="object">
      KPack ClusterStack properties payload Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Id of the ClusterStack. Optional.
        </ResponseField>
        <ResponseField name="version" type="string">
          Version of the ClusterStack Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
