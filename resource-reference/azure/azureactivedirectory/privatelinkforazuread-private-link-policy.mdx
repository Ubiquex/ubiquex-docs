---
title: "azure_azureactivedirectory_privatelinkforazuread_private_link_policy"
description: "`azure_azureactivedirectory_privatelinkforazuread_private_link_policy` restricts Microsoft Entra ID sign-in for a tenant to only occur through..."
---

`azure_azureactivedirectory_privatelinkforazuread_private_link_policy` restricts Microsoft Entra ID sign-in for a tenant to only occur through an approved Private Link connection, blocking access from outside the configured network(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_azureactivedirectory_privatelinkforazuread --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_azureactivedirectory_privatelinkforazuread/sdk/go
    package main

    import (
    	azureactivedirectory "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_azureactivedirectory_privatelinkforazuread/azureactivedirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own privatelinkforazuread private link policy"})

    		ubx.Resource(azureactivedirectory.PrivatelinkforazureadPrivateLinkPolicy, "example", azureactivedirectory.PrivatelinkforazureadPrivateLinkPolicyConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_azureactivedirectory_privatelinkforazuread --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivatelinkforazureadPrivateLinkPolicy } from "./local-sdk/azure_azureactivedirectory_privatelinkforazuread/sdk/typescript/azure_azureactivedirectory_privatelinkforazuread/azureactivedirectory/privatelinkforazuread_private_link_policy.ts";

    export default stack("example", () => {
      intent({ summary: "example own privatelinkforazuread private link policy" });

      resource(PrivatelinkforazureadPrivateLinkPolicy, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_azureactivedirectory_privatelinkforazuread --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_azureactivedirectory_privatelinkforazuread/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_azureactivedirectory_privatelinkforazuread.azureactivedirectory import PrivatelinkforazureadPrivateLinkPolicy, PrivatelinkforazureadPrivateLinkPolicyConfig

    def describe():
        ubx.intent("example own privatelinkforazuread private link policy")

        ubx.resource(PrivatelinkforazureadPrivateLinkPolicy, "example", PrivatelinkforazureadPrivateLinkPolicyConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_azureactivedirectory_privatelinkforazuread_private_link_policy
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="all_tenants" type="bool">
  Flag indicating whether all tenants are allowed Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the Private Link Azure AD Policy. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="owner_tenant_id" type="string">
  Guid of the owner tenant Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="resource_group" type="string">
  Name of the resource group Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="resource_name" type="string">
  Name of the private link policy resource Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="subscription_id" type="string">
  Subscription Identifier Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags to apply to this azureactivedirectory privatelinkforazuread private link policy. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="tenants" type="list(string)">
  The list of tenantIds. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
