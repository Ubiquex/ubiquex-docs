---
title: "azure_cdn_openapi_afdorigin"
description: "`azure_cdn_openapi_afdorigin` defines a single backend server Azure Front Door retrieves content from, within an origin group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cdn_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cdn_openapi/sdk/go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cdn_openapi/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi afdorigin"})

    		ubx.Resource(cdn.OpenapiAfdorigin, "example", cdn.OpenapiAfdoriginConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cdn_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiAfdorigin } from "./local-sdk/azure_cdn_openapi/sdk/typescript/azure_cdn_openapi/cdn/openapi_afdorigin.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi afdorigin" });

      resource(OpenapiAfdorigin, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cdn_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cdn_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cdn_openapi.cdn import OpenapiAfdorigin, OpenapiAfdoriginConfig

    def describe():
        ubx.intent("example own openapi afdorigin")

        ubx.resource(OpenapiAfdorigin, "example", OpenapiAfdoriginConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cdn_openapi_afdorigin, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The JSON object that contains the properties of the origin. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="azure_origin" type="object">
      Reference to another resource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The unique identifier of the cdn openapi afdorigin's azure origin. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="certificate_name_check_validation_mode" type="string">
      The validation mode for certificate name check at origin level. Only applicable when enforceCertificateNameCheck is true. Optional.
    </ResponseField>
    <ResponseField name="custom_certificate_subjects" type="list(string)">
      The list of custom certificate subjects to validate against. Only applicable when certificateNameCheckValidationMode is 'CustomCertificateSubject'. Must contain 1 or 2 entries. Optional.
    </ResponseField>
    <ResponseField name="deployment_status" type="string">
      The current deployment status of this configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enabled_state" type="string">
      Whether to enable use of this rule. Permitted values are 'Enabled' or 'Disabled' Optional.
    </ResponseField>
    <ResponseField name="enforce_certificate_name_check" type="bool">
      Whether to enable certificate name check at origin level Optional.
    </ResponseField>
    <ResponseField name="host_name" type="string">
      The address of the origin. Domain names, IPv4 addresses, and IPv6 addresses are supported.This should be unique across all origins in an endpoint. Optional.
    </ResponseField>
    <ResponseField name="http_port" type="number">
      The value of the HTTP port. Must be between 1 and 65535. Optional.
    </ResponseField>
    <ResponseField name="https_port" type="number">
      The value of the HTTPS port. Must be between 1 and 65535. Optional.
    </ResponseField>
    <ResponseField name="origin_group_name" type="string">
      The name of the origin group which contains this origin. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="origin_host_header" type="string">
      The host header value sent to the origin with each request. If you leave this blank, the request hostname determines this value. Azure Front Door origins, such as Web Apps, Blob Storage, and Cloud Services require this host header value to match the origin hostname by default. This overrides the host header defined at Endpoint Optional.
    </ResponseField>
    <ResponseField name="priority" type="number">
      Priority of origin in given origin group for load balancing. Higher priorities will not be used for load balancing if any lower priority origin is healthy.Must be between 1 and 5 Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning status Optional.
    </ResponseField>
    <ResponseField name="shared_private_link_resource" type="object">
      Describes the properties of an existing Shared Private Link Resource to use when connecting to a private origin. Optional.
      <Expandable title="properties">
        <ResponseField name="group_id" type="string">
          The group id from the provider of resource the shared private link resource is for. Optional.
        </ResponseField>
        <ResponseField name="private_link" type="object">
          Reference to another resource. Optional.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              The unique identifier of the cdn openapi afdorigin's private link. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="private_link_location" type="string">
          The location of the shared private link resource Optional.
        </ResponseField>
        <ResponseField name="request_message" type="string">
          The request message for requesting approval of the shared private link resource. Optional.
        </ResponseField>
        <ResponseField name="status" type="string">
          Status of the shared private link resource. Can be Pending, Approved, Rejected, Disconnected, or Timeout. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="weight" type="number">
      Weight of the origin in given origin group for load balancing. Must be between 1 and 1000 Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
