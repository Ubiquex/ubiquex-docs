---
title: "azure_cdn_openapi_custom_domain"
description: "`azure_cdn_openapi_custom_domain` maps a custom domain to a classic Azure CDN endpoint."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi custom domain"})

    		ubx.Resource(cdn.OpenapiCustomDomain, "example", cdn.OpenapiCustomDomainConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiCustomDomain } from "@ubx/sdk-azure/azure/cdn/openapi_custom_domain";

    export default stack("example", () => {
      intent({ summary: "example own openapi custom domain" });

      resource(OpenapiCustomDomain, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cdn import OpenapiCustomDomain, OpenapiCustomDomainConfig

    def describe():
        ubx.intent("example own openapi custom domain")

        ubx.resource(OpenapiCustomDomain, "example", OpenapiCustomDomainConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cdn_openapi_custom_domain, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The JSON object that contains the properties of the custom domain to create. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="host_name" type="string" required>
      The host name of the custom domain. Must be a domain name. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
