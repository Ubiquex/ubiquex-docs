---
title: "azure_cdn_openapi_afddomain"
description: "`azure_cdn_openapi_afddomain` maps a custom domain to an Azure Front Door endpoint..."
---

`azure_cdn_openapi_afddomain` maps a custom domain to an Azure Front Door endpoint, the current-generation (AFD Standard/Premium) equivalent of a classic CDN custom domain.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cdn_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cdn_openapi/sdk/go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cdn_openapi/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi afddomain"})

    		ubx.Resource(cdn.OpenapiAfddomain, "ci-runner", cdn.OpenapiAfddomainConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cdn_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiAfddomain } from "./local-sdk/azure_cdn_openapi/sdk/typescript/azure_cdn_openapi/cdn/openapi_afddomain.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi afddomain" });

      resource(OpenapiAfddomain, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cdn_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cdn_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cdn_openapi.cdn import OpenapiAfddomain, OpenapiAfddomainConfig

    def describe():
        ubx.intent("payments own openapi afddomain")

        ubx.resource(OpenapiAfddomain, "ci-runner", OpenapiAfddomainConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cdn_openapi_afddomain, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The JSON object that contains the properties of the domain to create. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="azure_dns_zone" type="object">
      Reference to another resource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deployment_status" type="string">
      The current deployment status of this configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="domain_validation_state" type="string">
      Provisioning substate shows the progress of custom HTTPS enabling/disabling process step by step. DCV stands for DomainControlValidation. Optional.
    </ResponseField>
    <ResponseField name="extended_properties" type="map(string)">
      Key-Value pair representing migration properties for domains. Optional.
    </ResponseField>
    <ResponseField name="host_name" type="string" required>
      The host name of the domain. Must be a domain name. Required.
    </ResponseField>
    <ResponseField name="mtls_settings" type="object">
      Contains the properties to configure mutual TLS for a custom domain with FQDN. Mutual TLS cannot be configured for custom domains with wildcard host names. Optional.
      <Expandable title="properties">
        <ResponseField name="scenario" type="string" required>
          Supported scenarios for establishing mTLS connection. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pre_validated_custom_domain_resource_id" type="object">
      Reference to another resource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="profile_name" type="string">
      The name of the profile which holds the domain. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning status Optional.
    </ResponseField>
    <ResponseField name="tls_settings" type="object">
      The JSON object that contains the properties to secure a domain. Optional.
      <Expandable title="properties">
        <ResponseField name="certificate_type" type="string" required>
          Defines the source of the SSL certificate. Required.
        </ResponseField>
        <ResponseField name="cipher_suite_set_type" type="string">
          cipher suite set type that will be used for Https Optional.
        </ResponseField>
        <ResponseField name="customized_cipher_suite_set" type="object">
          Customized cipher suite set object that will be used for Https when cipherSuiteSetType is Customized. Optional.
          <Expandable title="properties">
            <ResponseField name="cipher_suite_set_for_tls12" type="list(string)">
              Cipher suites for TLS 1.2. Required at least one in minimumTlsVersion TLS 1.2. Optional.
            </ResponseField>
            <ResponseField name="cipher_suite_set_for_tls13" type="list(string)">
              Cipher suites for TLS 1.3. Required at least one in minimumTlsVersion TLS 1.2, TLS 1.3. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="minimum_tls_version" type="string">
          TLS protocol version that will be used for Https when cipherSuiteSetType is Customized. Optional.
        </ResponseField>
        <ResponseField name="secret" type="object">
          Reference to another resource. Optional.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              Resource ID. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="server_tls_group_policy" type="string">
          Server TLS group policy that will be used for Https. Standard and Enhanced are service-managed sets; Custom allows specifying serverTlsGroups explicitly. Optional.
        </ResponseField>
        <ResponseField name="server_tls_groups" type="list(string)">
          Server TLS groups that will be used for Https when serverTlsGroupPolicy is Custom. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="validation_properties" type="object">
      The JSON object that contains the properties to validate a domain. Optional.
      <Expandable title="properties">
        <ResponseField name="expiration_date" type="string">
          The date time that the token expires Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="validation_token" type="string">
          Challenge used for DNS TXT record or file based validation Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
