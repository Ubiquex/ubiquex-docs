---
title: "azure_cdn_openapi_afdorigin_group"
description: "`azure_cdn_openapi_afdorigin_group` groups related origin(s) together in Azure Front Door..."
---

`azure_cdn_openapi_afdorigin_group` groups related origin(s) together in Azure Front Door, providing health-probe-based load balancing and failover across them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cdn_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cdn_openapi/sdk/go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cdn_openapi/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi afdorigin group"})

    		ubx.Resource(cdn.OpenapiAfdoriginGroup, "example", cdn.OpenapiAfdoriginGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cdn_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiAfdoriginGroup } from "./local-sdk/azure_cdn_openapi/sdk/typescript/azure_cdn_openapi/cdn/openapi_afdorigin_group.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi afdorigin group" });

      resource(OpenapiAfdoriginGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cdn_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cdn_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cdn_openapi.cdn import OpenapiAfdoriginGroup, OpenapiAfdoriginGroupConfig

    def describe():
        ubx.intent("example own openapi afdorigin group")

        ubx.resource(OpenapiAfdoriginGroup, "example", OpenapiAfdoriginGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cdn_openapi_afdorigin_group, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The JSON object that contains the properties of the origin group. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="authentication" type="object">
      The JSON object that contains the properties of the origin authentication settings. Optional.
      <Expandable title="properties">
        <ResponseField name="scope" type="string">
          The scope used when requesting token from Microsoft Entra. For example, for Azure Blob Storage, scope could be "https://storage.azure.com/.default". Optional.
        </ResponseField>
        <ResponseField name="token_destination_header" type="string">
          The HTTP request header where the origin authentication token is placed when forwarding the request to the origin. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the authentication for the origin. Optional.
        </ResponseField>
        <ResponseField name="user_assigned_identity" type="object">
          Reference to another resource. Optional.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              The unique identifier of the cdn openapi afdorigin group's user assigned identity. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deployment_status" type="string">
      The current deployment status of this configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="health_probe_settings" type="object">
      The JSON object that contains the properties to send health probes to origin. Optional.
      <Expandable title="properties">
        <ResponseField name="probe_interval_in_seconds" type="number">
          The number of seconds between health probes.Default is 240sec. Optional.
        </ResponseField>
        <ResponseField name="probe_path" type="string">
          The path relative to the origin that is used to determine the health of the origin. Optional.
        </ResponseField>
        <ResponseField name="probe_protocol" type="string">
          Protocol to use for health probe. Optional.
        </ResponseField>
        <ResponseField name="probe_request_type" type="string">
          The type of health probe request that is made. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="load_balancing_settings" type="object">
      Round-Robin load balancing settings for a backend pool Optional.
      <Expandable title="properties">
        <ResponseField name="additional_latency_in_milliseconds" type="number">
          The additional latency in milliseconds for probes to fall into the lowest latency bucket Optional.
        </ResponseField>
        <ResponseField name="sample_size" type="number">
          The number of samples to consider for load balancing decisions Optional.
        </ResponseField>
        <ResponseField name="successful_samples_required" type="number">
          The number of samples within the sample period that must succeed Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="profile_name" type="string">
      The name of the profile which holds the origin group. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning status Optional.
    </ResponseField>
    <ResponseField name="session_affinity_state" type="string">
      Whether to enable use of this rule. Permitted values are 'Enabled' or 'Disabled' Optional.
    </ResponseField>
    <ResponseField name="traffic_restoration_time_to_healed_or_new_endpoints_in_minutes" type="number">
      Time in minutes to shift the traffic to the endpoint gradually when an unhealthy endpoint comes healthy or a new endpoint is added. Default is 10 mins. This property is currently not supported. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
