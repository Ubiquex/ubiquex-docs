---
title: "azure_cdn_openapi_route"
description: "`azure_cdn_openapi_route` maps an incoming Azure Front Door request pattern (domain and path) to a specific origin group..."
---

`azure_cdn_openapi_route` maps an incoming Azure Front Door request pattern (domain and path) to a specific origin group, with its own caching and protocol behavior.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cdn_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cdn_openapi/sdk/go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cdn_openapi/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi route"})

    		ubx.Resource(cdn.OpenapiRoute, "example", cdn.OpenapiRouteConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cdn_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiRoute } from "./local-sdk/azure_cdn_openapi/sdk/typescript/azure_cdn_openapi/cdn/openapi_route.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi route" });

      resource(OpenapiRoute, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cdn_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cdn_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cdn_openapi.cdn import OpenapiRoute, OpenapiRouteConfig

    def describe():
        ubx.intent("example own openapi route")

        ubx.resource(OpenapiRoute, "example", OpenapiRouteConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cdn_openapi_route, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The JSON object that contains the properties of the Routes to create. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="cache_configuration" type="object">
      Caching settings for a caching-type route. To disable caching, do not provide a cacheConfiguration object. Optional.
      <Expandable title="properties">
        <ResponseField name="compression_settings" type="object">
          settings for compression. Optional.
          <Expandable title="properties">
            <ResponseField name="content_types_to_compress" type="list(string)">
              List of content types on which compression applies. The value should be a valid MIME type. Optional.
            </ResponseField>
            <ResponseField name="is_compression_enabled" type="bool">
              Indicates whether content compression is enabled on AzureFrontDoor. Default value is false. If compression is enabled, content will be served as compressed if user requests for a compressed version. Content won't be compressed on AzureFrontDoor when requested content is smaller than 1 byte or larger than 1 MB. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="query_parameters" type="string">
          query parameters to include or exclude (comma separated). Optional.
        </ResponseField>
        <ResponseField name="query_string_caching_behavior" type="string">
          Defines how Frontdoor caches requests that include query strings. You can ignore any query strings when caching, ignore specific query strings, cache every request with a unique URL, or cache specific query strings. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_domains" type="list(object)">
      Domains referenced by this endpoint. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="is_active" type="bool">
          Whether this custom domain is currently active and serving traffic. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deployment_status" type="string">
      The current deployment status of this configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enabled_state" type="string">
      Whether to enable use of this rule. Permitted values are 'Enabled' or 'Disabled' Optional.
    </ResponseField>
    <ResponseField name="endpoint_name" type="string">
      The name of the endpoint which holds the route. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="forwarding_protocol" type="string">
      Protocol this rule will use when forwarding traffic to backends. Optional.
    </ResponseField>
    <ResponseField name="https_redirect" type="string">
      Whether to automatically redirect HTTP traffic to HTTPS traffic. Note that this is a easy way to set up this rule and it will be the first rule that gets executed. Optional.
    </ResponseField>
    <ResponseField name="link_to_default_domain" type="string">
      whether this route will be linked to the default endpoint domain. Optional.
    </ResponseField>
    <ResponseField name="origin_group" type="object">
      Reference to another resource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="origin_path" type="string">
      A directory path on the origin that AzureFrontDoor can use to retrieve content from, e.g. contoso.cloudapp.net/originpath. Optional.
    </ResponseField>
    <ResponseField name="patterns_to_match" type="list(string)">
      The route patterns of the rule. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning status Optional.
    </ResponseField>
    <ResponseField name="rule_sets" type="list(object)">
      rule sets referenced by this endpoint. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="supported_protocols" type="list(string)">
      List of supported protocols for this route. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
