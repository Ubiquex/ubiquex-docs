---
title: "azure_cdn_openapi_origin"
description: "`azure_cdn_openapi_origin` defines the backend server a classic Azure CDN endpoint retrieves and caches content from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cdn_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cdn_openapi/sdk/go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cdn_openapi/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi origin"})

    		ubx.Resource(cdn.OpenapiOrigin, "example", cdn.OpenapiOriginConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cdn_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiOrigin } from "./local-sdk/azure_cdn_openapi/sdk/typescript/azure_cdn_openapi/cdn/openapi_origin.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi origin" });

      resource(OpenapiOrigin, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cdn_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cdn_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cdn_openapi.cdn import OpenapiOrigin, OpenapiOriginConfig

    def describe():
        ubx.intent("example own openapi origin")

        ubx.resource(OpenapiOrigin, "example", OpenapiOriginConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cdn_openapi_origin, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The JSON object that contains the properties of the origin. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="host_name" type="string" required>
      The address of the origin. Domain names, IPv4 addresses, and IPv6 addresses are supported.This should be unique across all origins in an endpoint. Required.
    </ResponseField>
    <ResponseField name="private_endpoint_status" type="string">
      The approval status for the connection to the Private Link Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning status of the origin. Optional.
    </ResponseField>
    <ResponseField name="resource_state" type="string">
      Resource status of the origin. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
