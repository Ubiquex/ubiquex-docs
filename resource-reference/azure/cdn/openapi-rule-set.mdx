---
title: "azure_cdn_openapi_rule_set"
description: "`azure_cdn_openapi_rule_set` groups related rule(s) together, applied to route(s) referencing the rule set within Azure Front Door or CDN."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cdn_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cdn_openapi/sdk/go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cdn_openapi/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi rule set"})

    		ubx.Resource(cdn.OpenapiRuleSet, "example", cdn.OpenapiRuleSetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cdn_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiRuleSet } from "./local-sdk/azure_cdn_openapi/sdk/typescript/azure_cdn_openapi/cdn/openapi_rule_set.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi rule set" });

      resource(OpenapiRuleSet, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cdn_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cdn_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cdn_openapi.cdn import OpenapiRuleSet, OpenapiRuleSetConfig

    def describe():
        ubx.intent("example own openapi rule set")

        ubx.resource(OpenapiRuleSet, "example", OpenapiRuleSetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cdn_openapi_rule_set, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The JSON object that contains the properties of the Rule Set to create. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="batch_mode" type="bool">
      Indicates whether rule set is in batch mode. When batch mode is enabled, rules will be processed in a batch along with the rule set. When batch mode is disabled, rules would need to be processed independently. This property can only be set during rule set creation and cannot be updated later. For switching modes, a new rule set needs to be created with the desired mode and rules need to be migrated to the new rule set. Optional.
    </ResponseField>
    <ResponseField name="profile_name" type="string">
      The name of the profile which holds the rule set. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="rules" type="list(object)">
      A list of rules that are part of this rule set provided the rule set is in batch mode. This property will be ignored if the rule set is not in batch mode. Optional.
      <Expandable title="properties">
        <ResponseField name="actions" type="list(object)">
          The action(s) this rule applies. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="conditions" type="list(object)">
          The condition(s) that must all be satisfied for this rule to match. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_processing_behavior" type="string">
          Whether rule processing stops after this rule matches, or continues to subsequent rules. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="order" type="number">
          The relative evaluation order of this item. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rule_name" type="string">
          The name of this rule. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rule_set_name" type="string">
          The name of this managed rule set. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
