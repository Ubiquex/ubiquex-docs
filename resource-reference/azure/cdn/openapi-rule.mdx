---
title: "azure_cdn_openapi_rule"
description: "`azure_cdn_openapi_rule` defines a match condition and action(s) (e.g. a redirect or header modification) applied to matching requests within an Azure..."
---

`azure_cdn_openapi_rule` defines a match condition and action(s) (e.g. a redirect or header modification) applied to matching requests within an Azure Front Door or CDN rule set.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi rule"})

    		ubx.Resource(cdn.OpenapiRule, "example", cdn.OpenapiRuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiRule } from "@ubx/sdk-azure/azure/cdn/openapi_rule";

    export default stack("example", () => {
      intent({ summary: "example own openapi rule" });

      resource(OpenapiRule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cdn.openapi_rule import OpenapiRule, OpenapiRuleConfig

    def describe():
        ubx.intent("example own openapi rule")

        ubx.resource(OpenapiRule, "example", OpenapiRuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cdn_openapi_rule, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The JSON object that contains the properties of the Rules to create. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="actions" type="list(object)">
      A list of actions that are executed when all the conditions of a rule are satisfied. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="conditions" type="list(object)">
      A list of conditions that must be matched for the actions to be executed Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deployment_status" type="string">
      The current deployment status of this configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="match_processing_behavior" type="string">
      If this rule is a match should the rules engine continue running the remaining rules or stop. If not present, defaults to Continue. Optional.
    </ResponseField>
    <ResponseField name="order" type="number">
      The order in which the rules are applied for the endpoint. Possible values &#123;0,1,2,3,………&#125;. A rule with a lesser order will be applied before a rule with a greater order. Rule with order 0 is a special rule. It does not require any condition and actions listed in it will always be applied. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning status Optional.
    </ResponseField>
    <ResponseField name="rule_set_name" type="string">
      The name of the rule set containing the rule. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
