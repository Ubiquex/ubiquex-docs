---
title: "azure_cdn_openapi_origin_group"
description: "`azure_cdn_openapi_origin_group` groups related origin(s) together in a classic Azure CDN profile, providing failover across them."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi origin group"})

    		ubx.Resource(cdn.OpenapiOriginGroup, "example", cdn.OpenapiOriginGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiOriginGroup } from "@ubx/sdk-azure/azure/cdn/openapi_origin_group";

    export default stack("example", () => {
      intent({ summary: "example own openapi origin group" });

      resource(OpenapiOriginGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cdn.openapi_origin_group import OpenapiOriginGroup, OpenapiOriginGroupConfig

    def describe():
        ubx.intent("example own openapi origin group")

        ubx.resource(OpenapiOriginGroup, "example", OpenapiOriginGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cdn_openapi_origin_group, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The JSON object that contains the properties of the origin group. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning status of the origin group. Optional.
    </ResponseField>
    <ResponseField name="resource_state" type="string">
      Resource status of the origin group. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
