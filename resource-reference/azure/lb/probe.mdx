---
title: "azurerm_lb_probe"
description: "Real, generated bindings for `azurerm_lb_probe`."
---

`azurerm_lb_probe` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-azure-go/azurerm/lb"

    cfg := lb.ProbeConfig{
        LoadbalancerId: "example",
        Name: "example-name",
        Port: 1,
        IntervalInSeconds: 1,
        NumberOfProbes: 1,
    }
    ubx.Resource(lb.Probe, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Probe } from "jsr:@ubx/sdk-azure/azurerm/lb/probe";

    resource(Probe, "example", {
      loadbalancerId: "example",
      name: "example-name",
      port: 1,
      intervalInSeconds: 1,
      numberOfProbes: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.azurerm.lb import Probe, ProbeConfig

    cfg = ProbeConfig(
        loadbalancer_id="example",
        name="example-name",
        port=1,
        interval_in_seconds=1,
        number_of_probes=1,
    )
    ubx.resource(Probe, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision azurerm_lb_probe with loadbalancer_id, name, port,
interval_in_seconds, number_of_probes.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="interval_in_seconds" type="number">
  Optional.
</ResponseField>

<ResponseField name="loadbalancer_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="number_of_probes" type="number">
  Optional.
</ResponseField>

<ResponseField name="port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="probe_threshold" type="number">
  Optional.
</ResponseField>

<ResponseField name="protocol" type="string">
  Optional.
</ResponseField>

<ResponseField name="request_path" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="load_balancer_rules" type="set(string)">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
