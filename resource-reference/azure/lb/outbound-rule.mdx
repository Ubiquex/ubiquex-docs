---
title: "azurerm_lb_outbound_rule"
description: "Real, generated bindings for `azurerm_lb_outbound_rule`."
---

`azurerm_lb_outbound_rule` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lb "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/lb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own outbound rule"})

    		ubx.Resource(lb.OutboundRule, "ci-runner", lb.OutboundRuleConfig{
    			BackendAddressPoolId:   "example",
    			LoadbalancerId:         "example",
    			Name:                   "ci-runner",
    			Protocol:               "example",
    			AllocatedOutboundPorts: 1,
    			IdleTimeoutInMinutes:   1,
    			TcpResetEnabled:        true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OutboundRule } from "jsr:@ubx/sdk-azure/azurerm/lb/outbound_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own outbound rule" });

      resource(OutboundRule, "ci-runner", {
        backendAddressPoolId: "example",
        loadbalancerId: "example",
        name: "ci-runner",
        protocol: "example",
        allocatedOutboundPorts: 1,
        idleTimeoutInMinutes: 1,
        tcpResetEnabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.lb import OutboundRule, OutboundRuleConfig

    def describe():
        ubx.intent("payments own outbound rule")

        ubx.resource(OutboundRule, "ci-runner", OutboundRuleConfig(
            backend_address_pool_id="example",
            loadbalancer_id="example",
            name="ci-runner",
            protocol="example",
            allocated_outbound_ports=1,
            idle_timeout_in_minutes=1,
            tcp_reset_enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_lb_outbound_rule called "ci-runner", with backend
    address pool id example, loadbalancer id example, protocol
    example, allocated outbound ports 1, idle timeout in minutes 1,
    tcp reset enabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allocated_outbound_ports" type="number">
  Optional.
</ResponseField>

<ResponseField name="backend_address_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="frontend_ip_configuration" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="idle_timeout_in_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="loadbalancer_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tcp_reset_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="frontend_ip_configuration" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
