---
title: "azurerm_lb_rule"
description: "Real, generated bindings for `azurerm_lb_rule`."
---

`azurerm_lb_rule` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lb "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/lb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own rule"})

    		ubx.Resource(lb.Rule, "ci-runner", lb.RuleConfig{
    			BackendPort:                 1,
    			FrontendIpConfigurationName: "example-frontend-ip-configuration-name",
    			FrontendPort:                1,
    			LoadbalancerId:              "example",
    			Name:                        "ci-runner",
    			Protocol:                    "example",
    			BackendAddressPoolIds:       []string{"example"},
    			DisableOutboundSnat:         true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Rule } from "jsr:@ubx/sdk-azure/azurerm/lb/rule";

    export default stack("payments", () => {
      intent({ summary: "payments own rule" });

      resource(Rule, "ci-runner", {
        backendPort: 1,
        frontendIpConfigurationName: "example-frontend-ip-configuration-name",
        frontendPort: 1,
        loadbalancerId: "example",
        name: "ci-runner",
        protocol: "example",
        backendAddressPoolIds: ["example"],
        disableOutboundSnat: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.lb import Rule, RuleConfig

    def describe():
        ubx.intent("payments own rule")

        ubx.resource(Rule, "ci-runner", RuleConfig(
            backend_port=1,
            frontend_ip_configuration_name="example-frontend-ip-configuration-name",
            frontend_port=1,
            loadbalancer_id="example",
            name="ci-runner",
            protocol="example",
            backend_address_pool_ids=["example"],
            disable_outbound_snat=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_lb_rule called "ci-runner", with backend port 1,
    frontend ip configuration name
    example-frontend-ip-configuration-name, frontend port 1,
    loadbalancer id example, protocol example, backend address pool
    ids a set value, disable outbound snat true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backend_address_pool_ids" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="backend_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="disable_outbound_snat" type="bool">
  Optional.
</ResponseField>

<ResponseField name="floating_ip_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="frontend_ip_configuration_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="frontend_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="idle_timeout_in_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="load_distribution" type="string">
  Optional.
</ResponseField>

<ResponseField name="loadbalancer_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="probe_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tcp_reset_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="frontend_ip_configuration_id" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
