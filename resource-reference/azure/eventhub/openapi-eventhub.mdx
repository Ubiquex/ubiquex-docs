---
title: "azure_eventhub_openapi_eventhub"
description: "`azure_eventhub_openapi_eventhub` is an event hub, the partitioned append-only log that producer(s) publish event(s) to and consumer group(s) read from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventhub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/eventhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi eventhub"})

    		ubx.Resource(eventhub.OpenapiEventhub, "example", eventhub.OpenapiEventhubConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiEventhub } from "@ubx/sdk-azure/azure/eventhub/openapi_eventhub";

    export default stack("example", () => {
      intent({ summary: "example own openapi eventhub" });

      resource(OpenapiEventhub, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.eventhub.openapi_eventhub import OpenapiEventhub, OpenapiEventhubConfig

    def describe():
        ubx.intent("example own openapi eventhub")

        ubx.resource(OpenapiEventhub, "example", OpenapiEventhubConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventhub_openapi_eventhub, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties supplied to the Create Or Update Event Hub operation. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capture_description" type="object">
      Properties to configure capture description for eventhub Optional.
      <Expandable title="properties">
        <ResponseField name="destination" type="object">
          Capture storage details for capture description Optional.
          <Expandable title="properties">
            <ResponseField name="identity" type="object">
              A value that indicates whether capture description is enabled. Optional.
              <Expandable title="properties">
                <ResponseField name="type" type="string">
                  Type of Azure Active Directory Managed Identity. Optional.
                </ResponseField>
                <ResponseField name="user_assigned_identity" type="string">
                  ARM ID of Managed User Identity. This property is required is the type is UserAssignedIdentity. If type is SystemAssigned, then the System Assigned Identity Associated with the namespace will be used. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              Name for capture destination Optional.
            </ResponseField>
            <ResponseField name="properties" type="object">
              Properties describing the storage account, blob container and archive name format for capture destination Optional.
              <Expandable title="properties">
                <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          A value that indicates whether capture description is enabled. Optional.
        </ResponseField>
        <ResponseField name="encoding" type="string">
          Enumerates the possible values for the encoding format of capture description. Note: 'AvroDeflate' will be deprecated in New API Version Optional.
        </ResponseField>
        <ResponseField name="interval_in_seconds" type="number">
          The time window allows you to set the frequency with which the capture to Azure Blobs will happen, value should between 60 to 900 seconds Optional.
        </ResponseField>
        <ResponseField name="size_limit_in_bytes" type="number">
          The size window defines the amount of data built up in your Event Hub before an capture operation, value should be between 10485760 to 524288000 bytes Optional.
        </ResponseField>
        <ResponseField name="skip_empty_archives" type="bool">
          A value that indicates whether to Skip Empty Archives Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_at" type="string">
      Exact time the Event Hub was created. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="identifier" type="string">
      Denotes the unique identifier for event hub and is generated by service while creating topic. This identifier can be used in kafka runtime apis wherever a UUID is required e.g Fetch & Delete Topic. This identifier is not supported in AMQP runtime operations yet. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="message_retention_in_days" type="number">
      Number of days to retain the events for this Event Hub, value should be 1 to 7 days Optional.
    </ResponseField>
    <ResponseField name="message_timestamp_description" type="object">
      Properties of MessageTimestamp Description Optional.
      <Expandable title="properties">
        <ResponseField name="timestamp_type" type="string">
          Denotes the type of timestamp the message will hold.Two types of timestamp types - "AppendTime" and "CreateTime". AppendTime refers the time in which message got appended inside broker log. CreateTime refers to the time in which the message was generated on source side and producers can set this timestamp while sending the message. Default value is AppendTime. If you are using AMQP protocol, CreateTime equals AppendTime and its behavior remains the same. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="partition_count" type="number">
      Number of partitions created for the Event Hub, allowed values are from 1 to 32 partitions. Optional.
    </ResponseField>
    <ResponseField name="partition_ids" type="list(string)">
      Current number of shards on the Event Hub. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="retention_description" type="object">
      Properties to configure retention settings for the eventhub Optional.
      <Expandable title="properties">
        <ResponseField name="cleanup_policy" type="string">
          Enumerates the possible values for cleanup policy Optional.
        </ResponseField>
        <ResponseField name="min_compaction_lag_time_in_minutes" type="number">
          The minimum time a message will remain ineligible for compaction in the log. This value is used when cleanupPolicy is Compact or DeleteOrCompact. Optional.
        </ResponseField>
        <ResponseField name="retention_time_in_hours" type="number">
          Number of hours to retain the events for this Event Hub. This should be positive value upto namespace SKU max. -1 is a special case where retention time is infinite, but the size of an entity is restricted and its size depends on namespace SKU type. Optional.
        </ResponseField>
        <ResponseField name="tombstone_retention_time_in_hours" type="number">
          Number of hours to retain the tombstone markers of a compacted Event Hub. This value is used when cleanupPolicy is Compact or DeleteOrCompact. Consumer must complete reading the tombstone marker within this specified amount of time if consumer begins from starting offset to ensure they get a valid snapshot for the specific key described by the tombstone marker within the compacted Event Hub Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="status" type="string">
      Enumerates the possible values for the status of the Event Hub. Optional.
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      The exact time the message was updated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="user_metadata" type="string">
      Gets and Sets Metadata of User. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
