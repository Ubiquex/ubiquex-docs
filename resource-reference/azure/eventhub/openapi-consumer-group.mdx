---
title: "azure_eventhub_openapi_consumer_group"
description: "`azure_eventhub_openapi_consumer_group` is a named view of an event hub's own event stream..."
---

`azure_eventhub_openapi_consumer_group` is a named view of an event hub's own event stream, letting multiple independent consumer application(s) read the same stream at their own pace and offset.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventhub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/eventhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi consumer group"})

    		ubx.Resource(eventhub.OpenapiConsumerGroup, "example", eventhub.OpenapiConsumerGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiConsumerGroup } from "@ubx/sdk-azure/azure/eventhub/openapi_consumer_group";

    export default stack("example", () => {
      intent({ summary: "example own openapi consumer group" });

      resource(OpenapiConsumerGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.eventhub import OpenapiConsumerGroup, OpenapiConsumerGroupConfig

    def describe():
        ubx.intent("example own openapi consumer group")

        ubx.resource(OpenapiConsumerGroup, "example", OpenapiConsumerGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventhub_openapi_consumer_group, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Single item in List or Get Consumer group operation Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      Exact time the message was created. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      The exact time the message was updated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="user_metadata" type="string">
      User Metadata is a placeholder to store user-defined string data with maximum length 1024. e.g. it can be used to store descriptive data, such as list of teams and their contact information also user-defined configuration settings can be stored. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
