---
title: "azure_eventhub_openapi_schema_group"
description: "`azure_eventhub_openapi_schema_group` groups related message schema(s) together within an Event Hubs namespace's own integrated Schema Registry."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventhub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/eventhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi schema group"})

    		ubx.Resource(eventhub.OpenapiSchemaGroup, "example", eventhub.OpenapiSchemaGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiSchemaGroup } from "@ubx/sdk-azure/azure/eventhub/openapi_schema_group";

    export default stack("example", () => {
      intent({ summary: "example own openapi schema group" });

      resource(OpenapiSchemaGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.eventhub.openapi_schema_group import OpenapiSchemaGroup, OpenapiSchemaGroupConfig

    def describe():
        ubx.intent("example own openapi schema group")

        ubx.resource(OpenapiSchemaGroup, "example", OpenapiSchemaGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventhub_openapi_schema_group, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="created_at_utc" type="string">
      Exact time the Schema Group was created. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="e_tag" type="string">
      The ETag value. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="group_properties" type="map(string)">
      dictionary object for SchemaGroup group properties Optional.
    </ResponseField>
    <ResponseField name="schema_compatibility" type="string">
      How strictly a new schema version must be compatible with previous ones, e.g. `Forward` or `Backward`. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="schema_type" type="string">
      The serialization format this schema is expressed in, e.g. Avro. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="updated_at_utc" type="string">
      Exact time the Schema Group was updated Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
