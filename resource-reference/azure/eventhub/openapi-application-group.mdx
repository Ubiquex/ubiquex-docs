---
title: "azure_eventhub_openapi_application_group"
description: "`azure_eventhub_openapi_application_group` applies throttling and quota policy(ies) to a specific application (identified by its own client certificate..."
---

`azure_eventhub_openapi_application_group` applies throttling and quota policy(ies) to a specific application (identified by its own client certificate or SAS key) connecting to an Event Hubs namespace.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_eventhub_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_eventhub_openapi/sdk/go
    package main

    import (
    	eventhub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_eventhub_openapi/eventhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi application group"})

    		ubx.Resource(eventhub.OpenapiApplicationGroup, "ci-runner", eventhub.OpenapiApplicationGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_eventhub_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiApplicationGroup } from "./local-sdk/azure_eventhub_openapi/sdk/typescript/azure_eventhub_openapi/eventhub/openapi_application_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi application group" });

      resource(OpenapiApplicationGroup, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_eventhub_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_eventhub_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_eventhub_openapi.eventhub import OpenapiApplicationGroup, OpenapiApplicationGroupConfig

    def describe():
        ubx.intent("payments own openapi application group")

        ubx.resource(OpenapiApplicationGroup, "ci-runner", OpenapiApplicationGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_eventhub_openapi_application_group, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="client_app_group_identifier" type="string" required>
      The Unique identifier for application group.Supports SAS(SASKeyName=KeyName) or AAD(AADAppID=Guid) Required.
    </ResponseField>
    <ResponseField name="is_enabled" type="bool">
      Determines if Application Group is allowed to create connection with namespace or not. Once the isEnabled is set to false, all the existing connections of application group gets dropped and no new connections will be allowed Optional.
    </ResponseField>
    <ResponseField name="policies" type="list(object)">
      List of group policies that define the behavior of application group. The policies can support resource governance scenarios such as limiting ingress or egress traffic. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
