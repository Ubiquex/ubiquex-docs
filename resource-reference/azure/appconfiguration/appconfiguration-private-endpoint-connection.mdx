---
title: "azure_appconfiguration_private_endpoint_connection"
description: "`azure_appconfiguration_private_endpoint_connection` represents the connection between a Private Endpoint and an App Configuration..."
---

`azure_appconfiguration_private_endpoint_connection` represents the connection between a Private Endpoint and an App Configuration store, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appconfiguration "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/appconfiguration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own appconfiguration private endpoint connection"})

    		ubx.Resource(appconfiguration.PrivateEndpointConnection, "example", appconfiguration.PrivateEndpointConnectionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivateEndpointConnection } from "@ubx/sdk-azure/azure/appconfiguration/private_endpoint_connection";

    export default stack("example", () => {
      intent({
        summary: "example own appconfiguration private endpoint connection",
      });

      resource(PrivateEndpointConnection, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.appconfiguration.private_endpoint_connection import PrivateEndpointConnection, PrivateEndpointConnectionConfig

    def describe():
        ubx.intent("example own appconfiguration private endpoint connection")

        ubx.resource(PrivateEndpointConnection, "example", PrivateEndpointConnectionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_appconfiguration_private_endpoint_connection called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="id" type="string">
  The resource ID. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of a private endpoint connection. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="private_endpoint" type="object">
      Private endpoint which a connection belongs to. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The resource Id for private endpoint Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_link_service_connection_state" type="object" required>
      The state of a private link service connection. Required.
      <Expandable title="properties">
        <ResponseField name="actions_required" type="string">
          Any action that is required beyond basic workflow (approve/ reject/ disconnect) Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="description" type="string">
          The private link service connection description. Optional.
        </ResponseField>
        <ResponseField name="status" type="string">
          The private link service connection status. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning status of the private endpoint connection. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
