---
title: "azure_appconfiguration_key_value"
description: "`azure_appconfiguration_appconfiguration_key_value` is a single key/value setting (or feature flag) stored in an App Configuration store..."
---

`azure_appconfiguration_appconfiguration_key_value` is a single key/value setting (or feature flag) stored in an App Configuration store, optionally scoped by label.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_appconfiguration_appconfiguration --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_appconfiguration_appconfiguration/sdk/go
    package main

    import (
    	appconfiguration "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_appconfiguration_appconfiguration/appconfiguration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own appconfiguration key value"})

    		ubx.Resource(appconfiguration.AppconfigurationKeyValue, "example", appconfiguration.AppconfigurationKeyValueConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_appconfiguration_appconfiguration --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppconfigurationKeyValue } from "./local-sdk/azure_appconfiguration_appconfiguration/sdk/typescript/azure_appconfiguration_appconfiguration/appconfiguration/appconfiguration_key_value.ts";

    export default stack("example", () => {
      intent({ summary: "example own appconfiguration key value" });

      resource(AppconfigurationKeyValue, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_appconfiguration_appconfiguration --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_appconfiguration_appconfiguration/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_appconfiguration_appconfiguration.appconfiguration import AppconfigurationKeyValue, AppconfigurationKeyValueConfig

    def describe():
        ubx.intent("example own appconfiguration key value")

        ubx.resource(AppconfigurationKeyValue, "example", AppconfigurationKeyValueConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_appconfiguration_key_value called "example", with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="id" type="string">
  The resource ID. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  All key-value properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="content_type" type="string">
      The content type of the key-value's value. Providing a proper content-type can enable transformations of values when they are retrieved by applications. Optional.
    </ResponseField>
    <ResponseField name="e_tag" type="string">
      An ETag indicating the state of a key-value within a configuration store. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="key" type="string">
      The primary identifier of a key-value. The key is used in unison with the label to uniquely identify a key-value. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="label" type="string">
      A value used to group key-values. The label is used in unison with the key to uniquely identify a key-value. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="last_modified" type="string">
      The last time a modifying operation was performed on the given key-value. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="locked" type="bool">
      A value indicating whether the key-value is locked. A locked key-value may not be modified until it is unlocked. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tags" type="map(string)">
      A dictionary of tags that can help identify what a key-value may be applicable for. Optional.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of the key-value. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
