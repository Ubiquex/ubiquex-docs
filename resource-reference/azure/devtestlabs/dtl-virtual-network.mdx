---
title: "azure_devtestlabs_dtl_virtual_network"
description: "`azure_devtestlabs_dtl_virtual_network` is a virtual network made available to a lab's own virtual machines..."
---

`azure_devtestlabs_dtl_virtual_network` is a virtual network made available to a lab's own virtual machines, either created for the lab or a reference to an existing subscription virtual network shared into it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_devtestlabs_dtl --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_devtestlabs_dtl/sdk/go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_devtestlabs_dtl/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dtl virtual network"})

    		ubx.Resource(devtestlabs.DtlVirtualNetwork, "ci-runner", devtestlabs.DtlVirtualNetworkConfig{
    			Name:       "ci-runner",
    			Properties: map[string]any{"allowed_subnets": []map[string]any{{"allow_public_ip": "example"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_devtestlabs_dtl --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DtlVirtualNetwork } from "./local-sdk/azure_devtestlabs_dtl/sdk/typescript/azure_devtestlabs_dtl/devtestlabs/dtl_virtual_network.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own dtl virtual network" });

      resource(DtlVirtualNetwork, "ci-runner", {
        name: "ci-runner",
        properties: { allowedSubnets: [{ allowPublicIp: "example" }] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_devtestlabs_dtl --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_devtestlabs_dtl/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_devtestlabs_dtl.devtestlabs import DtlVirtualNetwork, DtlVirtualNetworkConfig

    def describe():
        ubx.intent("payments own dtl virtual network")

        ubx.resource(DtlVirtualNetwork, "ci-runner", DtlVirtualNetworkConfig(
            name="ci-runner",
            properties={"allowed_subnets": [{"allow_public_ip": "example"}]},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devtestlabs_dtl_virtual_network called "ci-runner",
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of a virtual network. Required.
  <Expandable title="properties">
    <ResponseField name="allowed_subnets" type="list(object)">
      The allowed subnets of the virtual network. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_public_ip" type="string">
          Whether a public IP address is allowed on virtual machines created from this virtual network. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lab_subnet_name" type="string">
          The name of the lab's own virtual network subnet this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_id" type="string">
          A reference to the underlying Azure resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_date" type="string">
      The creation date of the virtual network. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="description" type="string">
      The description of the virtual network. Optional.
    </ResponseField>
    <ResponseField name="external_provider_resource_id" type="string">
      The Microsoft.Network resource identifier of the virtual network. Optional.
    </ResponseField>
    <ResponseField name="external_subnets" type="list(object)">
      The external subnet properties. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning status of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="subnet_overrides" type="list(object)">
      The subnet overrides of the virtual network. Optional.
      <Expandable title="properties">
        <ResponseField name="lab_subnet_name" type="string">
          The name of the lab's own virtual network subnet this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_id" type="string">
          A reference to the underlying Azure resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="shared_public_ip_address_configuration" type="object">
          Configuration for a public IP address shared across multiple virtual machines on this subnet. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="allowed_ports" type="list(object)">
              The port(s) allowed through this shared public IP's own inbound NAT rule(s). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="backend_port" type="number">
                  The backend port traffic is forwarded to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="transport_protocol" type="string">
                  The transport protocol this rule applies to, e.g. `Tcp` or `Udp`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="use_in_vm_creation_permission" type="string">
          Whether lab users are allowed, denied, or given a default choice to use this subnet when creating a virtual machine. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="use_public_ip_address_permission" type="string">
          Whether lab users are allowed, denied, or given a default choice to assign a public IP address on this subnet. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="virtual_network_pool_name" type="string">
          The name of the shared IP pool this subnet's own virtual machines draw addresses from. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="unique_identifier" type="string">
      The unique immutable identifier of a resource (Guid). Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
