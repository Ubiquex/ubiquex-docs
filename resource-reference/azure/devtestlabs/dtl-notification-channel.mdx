---
title: "azure_devtestlabs_dtl_notification_channel"
description: "`azure_devtestlabs_dtl_notification_channel` configures where a lab sends event notifications (e.g. an autoshutdown warning)..."
---

`azure_devtestlabs_dtl_notification_channel` configures where a lab sends event notifications (e.g. an autoshutdown warning), such as a webhook URL or email address.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dtl notification channel"})

    		ubx.Resource(devtestlabs.DtlNotificationChannel, "example", devtestlabs.DtlNotificationChannelConfig{
    			Name:       "example",
    			Properties: map[string]any{"created_date": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DtlNotificationChannel } from "@ubx/sdk-azure/azure/devtestlabs/dtl_notification_channel";

    export default stack("example", () => {
      intent({ summary: "example own dtl notification channel" });

      resource(DtlNotificationChannel, "example", {
        name: "example",
        properties: { createdDate: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.devtestlabs import DtlNotificationChannel, DtlNotificationChannelConfig

    def describe():
        ubx.intent("example own dtl notification channel")

        ubx.resource(DtlNotificationChannel, "example", DtlNotificationChannelConfig(
            name="example",
            properties={"created_date": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devtestlabs_dtl_notification_channel called
    "example", with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of a schedule. Required.
  <Expandable title="properties">
    <ResponseField name="created_date" type="string">
      The creation date of the notification channel. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="description" type="string">
      Description of notification. Optional.
    </ResponseField>
    <ResponseField name="email_recipient" type="string">
      The email recipient to send notifications to (can be a list of semi-colon separated email addresses). Optional.
    </ResponseField>
    <ResponseField name="events" type="list(object)">
      The list of event for which this notification is enabled. Optional.
      <Expandable title="properties">
        <ResponseField name="event_name" type="string">
          The name of this event. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="notification_locale" type="string">
      The locale to use when sending a notification (fallback for unsupported languages is EN). Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning status of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="unique_identifier" type="string">
      The unique immutable identifier of a resource (Guid). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="web_hook_url" type="string">
      The webhook URL to send notifications to. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
