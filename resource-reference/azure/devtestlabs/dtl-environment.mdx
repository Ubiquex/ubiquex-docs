---
title: "azure_devtestlabs_dtl_environment"
description: "`azure_devtestlabs_dtl_environment` is a DevTest Labs environment..."
---

`azure_devtestlabs_dtl_environment` is a DevTest Labs environment, a set of resources deployed together from an Azure Resource Manager template (e.g. a multi-VM topology) and managed as a single unit within a lab.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_devtestlabs_dtl --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_devtestlabs_dtl/sdk/go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_devtestlabs_dtl/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dtl environment"})

    		ubx.Resource(devtestlabs.DtlDtlEnvironment, "example", devtestlabs.DtlDtlEnvironmentConfig{
    			Name:       "example",
    			Properties: map[string]any{"arm_template_display_name": "example-arm-template-display-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_devtestlabs_dtl --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DtlDtlEnvironment } from "./local-sdk/azure_devtestlabs_dtl/sdk/typescript/azure_devtestlabs_dtl/devtestlabs/dtl_dtl_environment.ts";

    export default stack("example", () => {
      intent({ summary: "example own dtl environment" });

      resource(DtlDtlEnvironment, "example", {
        name: "example",
        properties: { armTemplateDisplayName: "example-arm-template-display-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_devtestlabs_dtl --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_devtestlabs_dtl/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_devtestlabs_dtl.devtestlabs import DtlDtlEnvironment, DtlDtlEnvironmentConfig

    def describe():
        ubx.intent("example own dtl environment")

        ubx.resource(DtlDtlEnvironment, "example", DtlDtlEnvironmentConfig(
            name="example",
            properties={"arm_template_display_name": "example-arm-template-display-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devtestlabs_dtl_environment called "example", with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of an environment. Required.
  <Expandable title="properties">
    <ResponseField name="arm_template_display_name" type="string">
      The display name of the Azure Resource Manager template that produced the environment. Optional.
    </ResponseField>
    <ResponseField name="created_by_user" type="string">
      The creator of the environment. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="deployment_properties" type="object">
      Properties of an environment deployment. Optional.
      <Expandable title="properties">
        <ResponseField name="arm_template_id" type="string">
          The Azure Resource Manager template's identifier. Optional.
        </ResponseField>
        <ResponseField name="parameters" type="list(object)">
          The parameters of the Azure Resource Manager template. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning status of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="resource_group_id" type="string">
      The identifier of the resource group containing the environment's resources. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="unique_identifier" type="string">
      The unique immutable identifier of a resource (Guid). Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
