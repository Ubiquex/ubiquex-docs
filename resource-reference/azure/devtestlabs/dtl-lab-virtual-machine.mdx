---
title: "azure_devtestlabs_dtl_lab_virtual_machine"
description: "`azure_devtestlabs_dtl_lab_virtual_machine` is a virtual machine created within a DevTest Lab..."
---

`azure_devtestlabs_dtl_lab_virtual_machine` is a virtual machine created within a DevTest Lab, distinct from a plain Azure VM in that it's subject to the lab's own cost, schedule, and access policies.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_devtestlabs_dtl --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_devtestlabs_dtl/sdk/go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_devtestlabs_dtl/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dtl lab virtual machine"})

    		ubx.Resource(devtestlabs.DtlLabVirtualMachine, "ci-runner", devtestlabs.DtlLabVirtualMachineConfig{
    			Name:       "ci-runner",
    			Properties: map[string]any{"allow_claim": true},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_devtestlabs_dtl --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DtlLabVirtualMachine } from "./local-sdk/azure_devtestlabs_dtl/sdk/typescript/azure_devtestlabs_dtl/devtestlabs/dtl_lab_virtual_machine.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own dtl lab virtual machine" });

      resource(DtlLabVirtualMachine, "ci-runner", {
        name: "ci-runner",
        properties: { allowClaim: true },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_devtestlabs_dtl --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_devtestlabs_dtl/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_devtestlabs_dtl.devtestlabs import DtlLabVirtualMachine, DtlLabVirtualMachineConfig

    def describe():
        ubx.intent("payments own dtl lab virtual machine")

        ubx.resource(DtlLabVirtualMachine, "ci-runner", DtlLabVirtualMachineConfig(
            name="ci-runner",
            properties={"allow_claim": True},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devtestlabs_dtl_lab_virtual_machine called
    "ci-runner", with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of a virtual machine. Required.
  <Expandable title="properties">
    <ResponseField name="allow_claim" type="bool">
      Indicates whether another user can take ownership of the virtual machine Optional.
    </ResponseField>
    <ResponseField name="applicable_schedule" type="object">
      Schedules applicable to a virtual machine. The schedules may have been defined on a VM or on lab level. Optional.
      <Expandable title="properties">
        <ResponseField name="location" type="string">
          The location of the resource. Optional.
        </ResponseField>
        <ResponseField name="properties" type="object" required>
          Properties of a schedules applicable to a virtual machine. Required.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
        <ResponseField name="tags" type="map(string)">
          The tags of the resource. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="artifact_deployment_status" type="object">
      Properties of an artifact deployment. Optional.
      <Expandable title="properties">
        <ResponseField name="artifacts_applied" type="number">
          The total count of the artifacts that were successfully applied. Optional.
        </ResponseField>
        <ResponseField name="deployment_status" type="string">
          The deployment status of the artifact. Optional.
        </ResponseField>
        <ResponseField name="total_artifacts" type="number">
          The total count of the artifacts that were tentatively applied. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="artifacts" type="list(object)">
      The artifacts to be installed on the virtual machine. Optional.
      <Expandable title="properties">
        <ResponseField name="artifact_id" type="string">
          A reference to the artifact this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="artifact_title" type="string">
          A human-readable title for this artifact. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="deployment_status_message" type="string">
          A human-readable message describing this deployment's own current status. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="install_time" type="string">
          When this artifact was installed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="parameters" type="list(object)">
          The parameter value(s) supplied to this artifact or template. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="status" type="string">
          The current status of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vm_extension_status_message" type="string">
          A human-readable message describing this VM extension's own current status. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="compute_id" type="string">
      The resource identifier (Microsoft.Compute) of the virtual machine. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="compute_vm" type="object">
      Properties of a virtual machine returned by the Microsoft.Compute API. Optional.
      <Expandable title="properties">
        <ResponseField name="data_disk_ids" type="list(string)">
          Gets data disks blob uri for the virtual machine. Optional.
        </ResponseField>
        <ResponseField name="data_disks" type="list(object)">
          Gets all data disks attached to the virtual machine. Optional.
          <Expandable title="properties">
            <ResponseField name="disk_size_gi_b" type="number">
              This disk's own size, in gibibytes. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_uri" type="string">
              This disk's own storage URI. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="managed_disk_id" type="string">
              A reference to the underlying managed disk. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_interface_id" type="string">
          Gets the network interface ID of the virtual machine. Optional.
        </ResponseField>
        <ResponseField name="os_disk_id" type="string">
          Gets OS disk blob uri for the virtual machine. Optional.
        </ResponseField>
        <ResponseField name="os_type" type="string">
          Gets the OS type of the virtual machine. Optional.
        </ResponseField>
        <ResponseField name="statuses" type="list(object)">
          Gets the statuses of the virtual machine. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              A machine-readable status or error code. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              A human-readable summary of this status. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="vm_size" type="string">
          Gets the size of the virtual machine. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_by_user" type="string">
      The email address of creator of the virtual machine. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="created_by_user_id" type="string">
      The object identifier of the creator of the virtual machine. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="created_date" type="string">
      The creation date of the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="custom_image_id" type="string">
      The custom image identifier of the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="data_disk_parameters" type="list(object)">
      New or existing data disks to attach to the virtual machine after creation Optional.
      <Expandable title="properties">
        <ResponseField name="attach_new_data_disk_options" type="object">
          Configuration for a new data disk to attach when creating this environment. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="disk_name" type="string">
              The name of this disk. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_size_gi_b" type="number">
              This disk's own size, in gibibytes. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_type" type="string">
              The type of storage this disk uses, e.g. `Standard` or `Premium`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="existing_lab_disk_id" type="string">
          A reference to an existing lab disk to attach, rather than creating a new one. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="host_caching" type="string">
          The disk caching policy, e.g. `None`, `ReadOnly`, or `ReadWrite`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="disallow_public_ip_address" type="bool">
      Indicates whether the virtual machine is to be created without a public IP address. Optional.
    </ResponseField>
    <ResponseField name="environment_id" type="string">
      The resource ID of the environment that contains this virtual machine, if any. Optional.
    </ResponseField>
    <ResponseField name="expiration_date" type="string">
      The expiration date for VM. Optional.
    </ResponseField>
    <ResponseField name="fqdn" type="string">
      The fully-qualified domain name of the virtual machine. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="gallery_image_reference" type="object">
      The reference information for an Azure Marketplace image. Optional.
      <Expandable title="properties">
        <ResponseField name="offer" type="string">
          The offer of the gallery image. Optional.
        </ResponseField>
        <ResponseField name="os_type" type="string">
          The OS type of the gallery image. Optional.
        </ResponseField>
        <ResponseField name="publisher" type="string">
          The publisher of the gallery image. Optional.
        </ResponseField>
        <ResponseField name="sku" type="string">
          The SKU of the gallery image. Optional.
        </ResponseField>
        <ResponseField name="version" type="string">
          The version of the gallery image. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_authentication_with_ssh_key" type="bool">
      Indicates whether this virtual machine uses an SSH key for authentication. Optional.
    </ResponseField>
    <ResponseField name="lab_subnet_name" type="string">
      The lab subnet name of the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="lab_virtual_network_id" type="string">
      The lab virtual network identifier of the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="last_known_power_state" type="string">
      Last known compute power state captured in DTL Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="network_interface" type="object">
      Properties of a network interface. Optional.
      <Expandable title="properties">
        <ResponseField name="dns_name" type="string">
          The DNS name. Optional.
        </ResponseField>
        <ResponseField name="private_ip_address" type="string">
          The private IP address. Optional.
        </ResponseField>
        <ResponseField name="public_ip_address" type="string">
          The public IP address. Optional.
        </ResponseField>
        <ResponseField name="public_ip_address_id" type="string">
          The resource ID of the public IP address. Optional.
        </ResponseField>
        <ResponseField name="rdp_authority" type="string">
          The RdpAuthority property is a server DNS host name or IP address followed by the service port number for RDP (Remote Desktop Protocol). Optional.
        </ResponseField>
        <ResponseField name="shared_public_ip_address_configuration" type="object">
          Properties of a virtual machine that determine how it is connected to a load balancer. Optional.
          <Expandable title="properties">
            <ResponseField name="inbound_nat_rules" type="list(object)">
              The incoming NAT rules Optional.
              <Expandable title="properties">
                <ResponseField name="backend_port" type="number">
                  The backend port traffic is forwarded to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="frontend_port" type="number">
                  The public-facing port this rule applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="transport_protocol" type="string">
                  The transport protocol this rule applies to, e.g. `Tcp` or `Udp`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ssh_authority" type="string">
          The SshAuthority property is a server DNS host name or IP address followed by the service port number for SSH. Optional.
        </ResponseField>
        <ResponseField name="subnet_id" type="string">
          The resource ID of the sub net. Optional.
        </ResponseField>
        <ResponseField name="virtual_network_id" type="string">
          The resource ID of the virtual network. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="notes" type="string">
      The notes of the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="os_type" type="string">
      The OS type of the virtual machine. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="owner_object_id" type="string">
      The object identifier of the owner of the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="owner_user_principal_name" type="string">
      The user principal name of the virtual machine owner. Optional.
    </ResponseField>
    <ResponseField name="password" type="string">
      The password of the virtual machine administrator. Optional.
    </ResponseField>
    <ResponseField name="plan_id" type="string">
      The id of the plan associated with the virtual machine image Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning status of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="schedule_parameters" type="list(object)">
      Virtual Machine schedules to be created Optional.
      <Expandable title="properties">
        <ResponseField name="location" type="string">
          The Azure region this resource is deployed to, e.g. `eastus`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
        <ResponseField name="tags" type="map(string)">
          User-defined key/value tags on this resource, usable for organizing and filtering resources across the subscription. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="size" type="string">
      The size of the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="ssh_key" type="string">
      The SSH key of the virtual machine administrator. Optional.
    </ResponseField>
    <ResponseField name="storage_type" type="string">
      Storage type to use for virtual machine (i.e. Standard, Premium). Optional.
    </ResponseField>
    <ResponseField name="unique_identifier" type="string">
      The unique immutable identifier of a resource (Guid). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="user_name" type="string">
      The user name of the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="virtual_machine_creation_source" type="string">
      Tells source of creation of lab virtual machine. Output property only. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
