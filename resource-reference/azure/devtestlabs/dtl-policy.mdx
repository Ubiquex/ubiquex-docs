---
title: "azure_devtestlabs_dtl_policy"
description: "`azure_devtestlabs_dtl_policy` is a lab policy -- a constraint like a maximum VM count per user or an allowed VM size list -- enforced against every..."
---

`azure_devtestlabs_dtl_policy` is a lab policy -- a constraint like a maximum VM count per user or an allowed VM size list -- enforced against every virtual machine created in the lab.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dtl policy"})

    		ubx.Resource(devtestlabs.DtlPolicy, "example", devtestlabs.DtlPolicyConfig{
    			Name:       "example",
    			Properties: map[string]any{"created_date": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DtlPolicy } from "@ubx/sdk-azure/azure/devtestlabs/dtl_policy";

    export default stack("example", () => {
      intent({ summary: "example own dtl policy" });

      resource(DtlPolicy, "example", {
        name: "example",
        properties: { createdDate: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.devtestlabs.dtl_policy import DtlPolicy, DtlPolicyConfig

    def describe():
        ubx.intent("example own dtl policy")

        ubx.resource(DtlPolicy, "example", DtlPolicyConfig(
            name="example",
            properties={"created_date": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devtestlabs_dtl_policy called "example", with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The location of the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of a Policy. Required.
  <Expandable title="properties">
    <ResponseField name="created_date" type="string">
      The creation date of the policy. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="description" type="string">
      The description of the policy. Optional.
    </ResponseField>
    <ResponseField name="evaluator_type" type="string">
      The evaluator type of the policy (i.e. AllowedValuesPolicy, MaxValuePolicy). Optional.
    </ResponseField>
    <ResponseField name="fact_data" type="string">
      The fact data of the policy. Optional.
    </ResponseField>
    <ResponseField name="fact_name" type="string">
      The fact name of the policy (e.g. LabVmCount, LabVmSize, MaxVmsAllowedPerLab, etc. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning status of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="status" type="string">
      The status of the policy. Optional.
    </ResponseField>
    <ResponseField name="threshold" type="string">
      The threshold of the policy (i.e. a number for MaxValuePolicy, and a JSON array of values for AllowedValuesPolicy). Optional.
    </ResponseField>
    <ResponseField name="unique_identifier" type="string">
      The unique immutable identifier of a resource (Guid). Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  The tags of the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
