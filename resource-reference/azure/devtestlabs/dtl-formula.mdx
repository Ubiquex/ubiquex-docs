---
title: "azure_devtestlabs_dtl_formula"
description: "`azure_devtestlabs_dtl_formula` is a reusable VM creation template within a lab -- a saved set of VM size, image..."
---

`azure_devtestlabs_dtl_formula` is a reusable VM creation template within a lab -- a saved set of VM size, image, and artifact choices lab users can create new virtual machines from without re-specifying every setting.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_devtestlabs_dtl --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_devtestlabs_dtl/sdk/go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_devtestlabs_dtl/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dtl formula"})

    		ubx.Resource(devtestlabs.DtlFormula, "example", devtestlabs.DtlFormulaConfig{
    			Name:       "example",
    			Properties: map[string]any{"author": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_devtestlabs_dtl --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DtlFormula } from "./local-sdk/azure_devtestlabs_dtl/sdk/typescript/azure_devtestlabs_dtl/devtestlabs/dtl_formula.ts";

    export default stack("example", () => {
      intent({ summary: "example own dtl formula" });

      resource(DtlFormula, "example", {
        name: "example",
        properties: { author: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_devtestlabs_dtl --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_devtestlabs_dtl/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_devtestlabs_dtl.devtestlabs import DtlFormula, DtlFormulaConfig

    def describe():
        ubx.intent("example own dtl formula")

        ubx.resource(DtlFormula, "example", DtlFormulaConfig(
            name="example",
            properties={"author": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devtestlabs_dtl_formula called "example", with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of a formula. Required.
  <Expandable title="properties">
    <ResponseField name="author" type="string">
      The author of the formula. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="creation_date" type="string">
      The creation date of the formula. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="description" type="string">
      The description of the formula. Optional.
    </ResponseField>
    <ResponseField name="formula_content" type="object">
      Properties for creating a virtual machine. Optional.
      <Expandable title="properties">
        <ResponseField name="location" type="string">
          The location of the new virtual machine or environment Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the virtual machine or environment Optional.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Properties for virtual machine creation. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
        <ResponseField name="tags" type="map(string)">
          The tags of the resource. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="os_type" type="string">
      The OS type of the formula. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning status of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="unique_identifier" type="string">
      The unique immutable identifier of a resource (Guid). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="vm" type="object">
      Information about a VM from which a formula is to be created. Optional.
      <Expandable title="properties">
        <ResponseField name="lab_vm_id" type="string">
          The identifier of the VM from which a formula is to be created. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags to apply to this devtestlabs dtl formula. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
