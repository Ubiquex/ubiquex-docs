---
title: "azure_devtestlabs_dtl_service_fabric"
description: "`azure_devtestlabs_dtl_service_fabric` is a Service Fabric cluster deployed and managed within a DevTest Lab..."
---

`azure_devtestlabs_dtl_service_fabric` is a Service Fabric cluster deployed and managed within a DevTest Lab, for testing Service Fabric-hosted applications in an isolated lab environment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_devtestlabs_dtl --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_devtestlabs_dtl/sdk/go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_devtestlabs_dtl/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dtl service fabric"})

    		ubx.Resource(devtestlabs.DtlServiceFabric, "example", devtestlabs.DtlServiceFabricConfig{
    			Name:       "example",
    			Properties: map[string]any{"applicable_schedule": map[string]any{"properties": map[string]any{"lab_vms_shutdown": map[string]any{"properties": map[string]any{"created_date": "example"}}}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_devtestlabs_dtl --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DtlServiceFabric } from "./local-sdk/azure_devtestlabs_dtl/sdk/typescript/azure_devtestlabs_dtl/devtestlabs/dtl_service_fabric.ts";

    export default stack("example", () => {
      intent({ summary: "example own dtl service fabric" });

      resource(DtlServiceFabric, "example", {
        name: "example",
        properties: {
          applicableSchedule: {
            properties: {
              labVmsShutdown: { properties: { createdDate: "example" } },
            },
          },
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_devtestlabs_dtl --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_devtestlabs_dtl/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_devtestlabs_dtl.devtestlabs import DtlServiceFabric, DtlServiceFabricConfig

    def describe():
        ubx.intent("example own dtl service fabric")

        ubx.resource(DtlServiceFabric, "example", DtlServiceFabricConfig(
            name="example",
            properties={"applicable_schedule": {"properties": {"lab_vms_shutdown": {"properties": {"created_date": "example"}}}}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devtestlabs_dtl_service_fabric called "example",
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of a service fabric. Required.
  <Expandable title="properties">
    <ResponseField name="applicable_schedule" type="object">
      Schedules applicable to a virtual machine. The schedules may have been defined on a VM or on lab level. Optional.
      <Expandable title="properties">
        <ResponseField name="location" type="string">
          The location of the resource. Optional.
        </ResponseField>
        <ResponseField name="properties" type="object" required>
          Properties of a schedules applicable to a virtual machine. Required.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
        <ResponseField name="tags" type="map(string)">
          The tags of the resource. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="environment_id" type="string">
      The resource id of the environment under which the service fabric resource is present Optional.
    </ResponseField>
    <ResponseField name="external_service_fabric_id" type="string">
      The backing service fabric resource's id Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning status of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="unique_identifier" type="string">
      The unique immutable identifier of a resource (Guid). Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags to apply to this devtestlabs dtl service fabric. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
</ResponseField>
