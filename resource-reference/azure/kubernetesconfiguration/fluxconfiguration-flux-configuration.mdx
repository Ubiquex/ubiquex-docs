---
title: "azure_kubernetesconfiguration_fluxconfiguration_flux_configuration"
description: "`azure_kubernetesconfiguration_fluxconfiguration_flux_configuration` configures GitOps-based continuous deployment for a Kubernetes cluster using Flux..."
---

`azure_kubernetesconfiguration_fluxconfiguration_flux_configuration` configures GitOps-based continuous deployment for a Kubernetes cluster using Flux, reconciling cluster state against a Git repository.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_kubernetesconfiguration_fluxconfiguration --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_kubernetesconfiguration_fluxconfiguration/sdk/go
    package main

    import (
    	kubernetesconfiguration "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_kubernetesconfiguration_fluxconfiguration/kubernetesconfiguration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own fluxconfiguration flux configuration"})

    		ubx.Resource(kubernetesconfiguration.FluxconfigurationFluxConfiguration, "ci-runner", kubernetesconfiguration.FluxconfigurationFluxConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_kubernetesconfiguration_fluxconfiguration --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FluxconfigurationFluxConfiguration } from "./local-sdk/azure_kubernetesconfiguration_fluxconfiguration/sdk/typescript/azure_kubernetesconfiguration_fluxconfiguration/kubernetesconfiguration/fluxconfiguration_flux_configuration.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own fluxconfiguration flux configuration" });

      resource(FluxconfigurationFluxConfiguration, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_kubernetesconfiguration_fluxconfiguration --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_kubernetesconfiguration_fluxconfiguration/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_kubernetesconfiguration_fluxconfiguration.kubernetesconfiguration import FluxconfigurationFluxConfiguration, FluxconfigurationFluxConfigurationConfig

    def describe():
        ubx.intent("payments own fluxconfiguration flux configuration")

        ubx.resource(FluxconfigurationFluxConfiguration, "ci-runner", FluxconfigurationFluxConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_kubernetesconfiguration_fluxconfiguration_flux_configuration,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties to create a Flux Configuration resource Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="azure_blob" type="object">
      Parameters to reconcile to the AzureBlob source kind type. Optional.
      <Expandable title="properties">
        <ResponseField name="account_key" type="string">
          The account key (shared key) to access the storage account Optional.
        </ResponseField>
        <ResponseField name="container_name" type="string">
          The Azure Blob container name to sync from the url endpoint for the flux configuration. Optional.
        </ResponseField>
        <ResponseField name="local_auth_ref" type="string">
          Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the managed or user-provided configuration secrets. Optional.
        </ResponseField>
        <ResponseField name="managed_identity" type="object">
          Parameters to authenticate using a Managed Identity. Optional.
          <Expandable title="properties">
            <ResponseField name="client_id" type="string">
              The client Id for authenticating a Managed Identity. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sas_token" type="string">
          The Shared Access token to access the storage container Optional.
        </ResponseField>
        <ResponseField name="service_principal" type="object">
          Parameters to authenticate using Service Principal. Optional.
          <Expandable title="properties">
            <ResponseField name="client_certificate" type="string">
              Base64-encoded certificate used to authenticate a Service Principal Optional.
            </ResponseField>
            <ResponseField name="client_certificate_password" type="string">
              The password for the certificate used to authenticate a Service Principal Optional.
            </ResponseField>
            <ResponseField name="client_certificate_send_chain" type="bool">
              Specifies whether to include x5c header in client claims when acquiring a token to enable subject name / issuer based authentication for the Client Certificate Optional.
            </ResponseField>
            <ResponseField name="client_id" type="string">
              The client Id for authenticating a Service Principal. Optional.
            </ResponseField>
            <ResponseField name="client_secret" type="string">
              The client secret for authenticating a Service Principal Optional.
            </ResponseField>
            <ResponseField name="tenant_id" type="string">
              The tenant Id for authenticating a Service Principal Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sync_interval_in_seconds" type="number">
          The interval at which to re-reconcile the cluster Azure Blob source with the remote. Optional.
        </ResponseField>
        <ResponseField name="timeout_in_seconds" type="number">
          The maximum time to attempt to reconcile the cluster Azure Blob source with the remote. Optional.
        </ResponseField>
        <ResponseField name="url" type="string">
          The URL to sync for the flux configuration Azure Blob storage account. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="bucket" type="object">
      Parameters to reconcile to the Bucket source kind type. Optional.
      <Expandable title="properties">
        <ResponseField name="access_key" type="string">
          Plaintext access key used to securely access the S3 bucket Optional.
        </ResponseField>
        <ResponseField name="bucket_name" type="string">
          The bucket name to sync from the url endpoint for the flux configuration. Optional.
        </ResponseField>
        <ResponseField name="insecure" type="bool">
          Specify whether to use insecure communication when puling data from the S3 bucket. Optional.
        </ResponseField>
        <ResponseField name="local_auth_ref" type="string">
          Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the managed or user-provided configuration secrets. Optional.
        </ResponseField>
        <ResponseField name="sync_interval_in_seconds" type="number">
          The interval at which to re-reconcile the cluster bucket source with the remote. Optional.
        </ResponseField>
        <ResponseField name="timeout_in_seconds" type="number">
          The maximum time to attempt to reconcile the cluster bucket source with the remote. Optional.
        </ResponseField>
        <ResponseField name="url" type="string">
          The URL to sync for the flux configuration S3 bucket. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="compliance_state" type="string">
      Compliance state of the cluster object. Optional.
    </ResponseField>
    <ResponseField name="configuration_protected_settings" type="map(string)">
      Key-value pairs of protected configuration settings for the configuration Optional.
    </ResponseField>
    <ResponseField name="error_message" type="string">
      Error message returned to the user in the case of provisioning failure. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="git_repository" type="object">
      Parameters to reconcile to the GitRepository source kind type. Optional.
      <Expandable title="properties">
        <ResponseField name="https_cacert" type="string">
          Base64-encoded HTTPS certificate authority contents used to access git private git repositories over HTTPS Optional.
        </ResponseField>
        <ResponseField name="https_user" type="string">
          Plaintext HTTPS username used to access private git repositories over HTTPS Optional.
        </ResponseField>
        <ResponseField name="local_auth_ref" type="string">
          Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the managed or user-provided configuration secrets. Optional.
        </ResponseField>
        <ResponseField name="repository_ref" type="object">
          The source reference for the GitRepository object. Optional.
          <Expandable title="properties">
            <ResponseField name="branch" type="string">
              The git repository branch name to checkout. Optional.
            </ResponseField>
            <ResponseField name="commit" type="string">
              The commit SHA to checkout. This value must be combined with the branch name to be valid. This takes precedence over semver. Optional.
            </ResponseField>
            <ResponseField name="semver" type="string">
              The semver range used to match against git repository tags. This takes precedence over tag. Optional.
            </ResponseField>
            <ResponseField name="tag" type="string">
              The git repository tag name to checkout. This takes precedence over branch. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ssh_known_hosts" type="string">
          Base64-encoded known_hosts value containing public SSH keys required to access private git repositories over SSH Optional.
        </ResponseField>
        <ResponseField name="sync_interval_in_seconds" type="number">
          The interval at which to re-reconcile the cluster git repository source with the remote. Optional.
        </ResponseField>
        <ResponseField name="timeout_in_seconds" type="number">
          The maximum time to attempt to reconcile the cluster git repository source with the remote. Optional.
        </ResponseField>
        <ResponseField name="url" type="string">
          The URL to sync for the flux configuration git repository. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kustomizations" type="map(object)">
      Array of kustomizations used to reconcile the artifact pulled by the source type on the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="depends_on" type="list(string)">
          The Kustomization(s) that must be applied before this one runs. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="force" type="bool">
          Whether this Kustomization is applied with `--force`, recreating resources it can't otherwise patch. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="path" type="string">
          The path within the source repository this Kustomization applies. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="post_build" type="object">
          Substitution(s) applied to manifests after they're built, before being applied to the cluster. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="substitute" type="map(string)">
              Fixed key/value substitution(s) applied to manifests during post-build. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="substitute_from" type="list(object)">
              A reference to a ConfigMap or Secret whose own key/value pairs are substituted into manifests during post-build. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kind" type="string">
                  A finer-grained classification of this resource within its own resource type, when the type supports more than one variant. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="optional" type="bool">
                  Whether this dependency is optional, rather than required. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="prune" type="bool">
          Whether resources no longer present in the source are automatically removed from the cluster. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="retry_interval_in_seconds" type="number">
          How long, in seconds, to wait before retrying after a failed reconciliation. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sync_interval_in_seconds" type="number">
          How often, in seconds, this configuration is reconciled against its own source. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="timeout_in_seconds" type="number">
          How long, in seconds, this operation waits before timing out. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="wait" type="bool">
          Whether reconciliation waits for all applied resources to become ready before being marked complete. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="namespace" type="string">
      The namespace to which this configuration is installed to. Maximum of 253 lower case alphanumeric characters, hyphen and period only. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="reconciliation_wait_duration" type="string">
      Maximum duration to wait for flux configuration reconciliation. E.g PT1H, PT5M, P1D Optional.
    </ResponseField>
    <ResponseField name="repository_public_key" type="string">
      Public Key associated with this fluxConfiguration (either generated within the cluster or provided by the user). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="scope" type="string">
      Scope at which the configuration will be installed. Optional.
    </ResponseField>
    <ResponseField name="source_kind" type="string">
      Source Kind to pull the configuration data from. Optional.
    </ResponseField>
    <ResponseField name="source_synced_commit_id" type="string">
      Branch and/or SHA of the source commit synced with the cluster. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="source_updated_at" type="string">
      Datetime the fluxConfiguration synced its source on the cluster. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="status_updated_at" type="string">
      Datetime the fluxConfiguration synced its status on the cluster with Azure. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="statuses" type="list(object)">
      Statuses of the Flux Kubernetes resources created by the fluxConfiguration or created by the managed objects provisioned by the fluxConfiguration. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="applied_by" type="object">
          The component that applied this configuration. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="namespace" type="string">
              The Kubernetes namespace this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="compliance_state" type="string">
          Whether this configuration's own live state matches its declared, desired state. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="helm_release_properties" type="object">
          Configuration specific to this Helm release. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="failure_count" type="number">
              The number of times this reconciliation has failed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="helm_chart_ref" type="object">
              A reference to the Helm chart this release deploys. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="namespace" type="string">
                  The Kubernetes namespace this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="install_failure_count" type="number">
              The number of times the initial install of this Helm release has failed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="last_revision_applied" type="number">
              The most recent source revision successfully applied. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="upgrade_failure_count" type="number">
              The number of times an upgrade of this Helm release has failed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kind" type="string">
          A finer-grained classification of this resource within its own resource type, when the type supports more than one variant. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="namespace" type="string">
          The Kubernetes namespace this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status_conditions" type="list(object)">
          The current condition(s) affecting this resource's own status. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="last_transition_time" type="string">
              When this condition last changed from one status to another. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="reason" type="string">
              A machine-readable reason code. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="status" type="string">
              The current status of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="suspend" type="bool">
      Whether this configuration should suspend its reconciliation of its kustomizations and sources. Optional.
    </ResponseField>
    <ResponseField name="wait_for_reconciliation" type="bool">
      Whether flux configuration deployment should wait for cluster to reconcile the kustomizations. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
