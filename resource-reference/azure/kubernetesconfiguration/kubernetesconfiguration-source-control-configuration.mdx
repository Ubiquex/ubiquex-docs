---
title: "azure_kubernetesconfiguration_source_control_configuration"
description: "`azure_kubernetesconfiguration_kubernetesconfiguration_source_control_configuration` is the legacy GitOps configuration resource for Azure Arc-enabled..."
---

`azure_kubernetesconfiguration_kubernetesconfiguration_source_control_configuration` is the legacy GitOps configuration resource for Azure Arc-enabled and AKS clusters, superseded by Flux configurations.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kubernetesconfiguration "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/kubernetesconfiguration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own kubernetesconfiguration source control configuration"})

    		ubx.Resource(kubernetesconfiguration.SourceControlConfiguration, "example", kubernetesconfiguration.SourceControlConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SourceControlConfiguration } from "@ubx/sdk-azure/azure/kubernetesconfiguration/source_control_configuration";

    export default stack("example", () => {
      intent({
        summary: "example own kubernetesconfiguration source control configuration",
      });

      resource(SourceControlConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.kubernetesconfiguration import SourceControlConfiguration, SourceControlConfigurationConfig

    def describe():
        ubx.intent("example own kubernetesconfiguration source control configuration")

        ubx.resource(SourceControlConfiguration, "example", SourceControlConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_kubernetesconfiguration_source_control_configuration, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties to create a Source Control Configuration resource Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="compliance_status" type="object">
      Compliance Status details Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="compliance_state" type="string">
          The compliance state of the configuration. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="last_config_applied" type="string">
          Datetime the configuration was last applied. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          Message from when the configuration was applied. Optional.
        </ResponseField>
        <ResponseField name="message_level" type="string">
          Level of the message. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="configuration_protected_settings" type="map(string)">
      Name-value pairs of protected configuration settings for the configuration Optional.
    </ResponseField>
    <ResponseField name="enable_helm_operator" type="bool">
      Option to enable Helm Operator for this git configuration. Optional.
    </ResponseField>
    <ResponseField name="helm_operator_properties" type="object">
      Properties for Helm operator. Optional.
      <Expandable title="properties">
        <ResponseField name="chart_values" type="string">
          Values override for the operator Helm chart. Optional.
        </ResponseField>
        <ResponseField name="chart_version" type="string">
          Version of the operator Helm chart. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="operator_instance_name" type="string">
      Instance name of the operator - identifying the specific configuration. Optional.
    </ResponseField>
    <ResponseField name="operator_namespace" type="string">
      The namespace to which this operator is installed to. Maximum of 253 lower case alphanumeric characters, hyphen and period only. Optional.
    </ResponseField>
    <ResponseField name="operator_params" type="string">
      Any Parameters for the Operator instance in string format. Optional.
    </ResponseField>
    <ResponseField name="operator_scope" type="string">
      Scope at which the operator will be installed. Optional.
    </ResponseField>
    <ResponseField name="operator_type" type="string">
      Type of the operator Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the resource provider. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="repository_public_key" type="string">
      Public Key associated with this SourceControl configuration (either generated within the cluster or provided by the user). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="repository_url" type="string">
      Url of the SourceControl Repository. Optional.
    </ResponseField>
    <ResponseField name="ssh_known_hosts_contents" type="string">
      Base64-encoded known_hosts contents containing public SSH keys required to access private Git instances Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
