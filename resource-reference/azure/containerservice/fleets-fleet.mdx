---
title: "azure_containerservice_fleets_fleet"
description: "`azure_containerservice_fleets_fleet` is the top-level AKS Fleet Manager resource..."
---

`azure_containerservice_fleets_fleet` is the top-level AKS Fleet Manager resource, coordinating Kubernetes version upgrades and workload placement across multiple member AKS cluster(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own fleets fleet"})

    		ubx.Resource(containerservice.FleetsFleet, "example", containerservice.FleetsFleetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FleetsFleet } from "@ubx/sdk-azure/azure/containerservice/fleets_fleet";

    export default stack("example", () => {
      intent({ summary: "example own fleets fleet" });

      resource(FleetsFleet, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.containerservice.fleets_fleet import FleetsFleet, FleetsFleetConfig

    def describe():
        ubx.intent("example own fleets fleet")

        ubx.resource(FleetsFleet, "example", FleetsFleetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_containerservice_fleets_fleet, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  If eTag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Fleet properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="hub_profile" type="object">
      The FleetHubProfile configures the fleet hub. Optional.
      <Expandable title="properties">
        <ResponseField name="agent_profile" type="object">
          Agent profile for the Fleet hub. Optional.
          <Expandable title="properties">
            <ResponseField name="subnet_id" type="string">
              A type definition that refers the id to an Azure Resource Manager resource. Optional.
            </ResponseField>
            <ResponseField name="vm_size" type="string">
              The virtual machine size of the Fleet hub. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="api_server_access_profile" type="object">
          Access profile for the Fleet hub API server. Optional.
          <Expandable title="properties">
            <ResponseField name="enable_private_cluster" type="bool">
              Whether to create the Fleet hub as a private cluster or not. Optional.
            </ResponseField>
            <ResponseField name="enable_vnet_integration" type="bool">
              Whether to enable apiserver vnet integration for the Fleet hub or not. Optional.
            </ResponseField>
            <ResponseField name="subnet_id" type="string">
              A type definition that refers the id to an Azure Resource Manager resource. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dns_prefix" type="string">
          DNS prefix used to create the FQDN for the Fleet hub. Optional.
        </ResponseField>
        <ResponseField name="fqdn" type="string">
          The FQDN of the Fleet hub. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="kubernetes_version" type="string">
          The Kubernetes version of the Fleet hub. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="portal_fqdn" type="string">
          The Azure Portal FQDN of the Fleet hub. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the last accepted operation. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="status" type="object">
      Status information for the fleet. Optional.
      <Expandable title="properties">
        <ResponseField name="last_operation_error" type="object">
          The error detail. Optional.
          <Expandable title="properties">
            <ResponseField name="additional_info" type="list(object)">
              The error additional info. Computed by Microsoft Azure after `ship`.
              <Expandable title="properties">
                <ResponseField name="info" type="any">
                  Additional detail about this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="code" type="string">
              The error code. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="details" type="list(object)">
              The error details. Computed by Microsoft Azure after `ship`.
              <Expandable title="properties">
                <ResponseField name="additional_info" type="list(object)">
                  Additional detail about this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="info" type="any">
                      Additional detail about this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="code" type="string">
                  A machine-readable status or error code. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="details" type="list(object)">
                  Additional detail about this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="target" type="string">
                  A reference to the resource this operation targets. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="message" type="string">
              The error message. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="target" type="string">
              The error target. Computed by Microsoft Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="last_operation_id" type="string">
          The last operation ID for the fleet. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
