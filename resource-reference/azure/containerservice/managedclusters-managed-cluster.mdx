---
title: "azure_containerservice_managedclusters_managed_cluster"
description: "`azure_containerservice_managedclusters_managed_cluster` is an Azure Kubernetes Service (AKS) cluster..."
---

`azure_containerservice_managedclusters_managed_cluster` is an Azure Kubernetes Service (AKS) cluster, the managed Kubernetes control plane and node infrastructure that workload(s) are deployed to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managedclusters managed cluster"})

    		ubx.Resource(containerservice.ManagedclustersManagedCluster, "example", containerservice.ManagedclustersManagedClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedclustersManagedCluster } from "@ubx/sdk-azure/azure/containerservice/managedclusters_managed_cluster";

    export default stack("example", () => {
      intent({ summary: "example own managedclusters managed cluster" });

      resource(ManagedclustersManagedCluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.containerservice.managedclusters_managed_cluster import ManagedclustersManagedCluster, ManagedclustersManagedClusterConfig

    def describe():
        ubx.intent("example own managedclusters managed cluster")

        ubx.resource(ManagedclustersManagedCluster, "example", ManagedclustersManagedClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_containerservice_managedclusters_managed_cluster,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  If eTag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields. Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="extended_location" type="object">
  The complex type of the extended location. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of the extended location. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of extendedLocation. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="identity" type="object">
  Identity for the managed cluster. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="delegated_resources" type="map(object)">
      The delegated identity resources assigned to this managed cluster. This can only be set by another Azure Resource Provider, and managed cluster only accept one delegated identity resource. Internal use only. Optional.
      <Expandable title="properties">
        <ResponseField name="location" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="referral_resource" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="resource_id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="principal_id" type="string">
      The principal id of the system assigned identity which is used by master components. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id of the system assigned identity which is used by master components. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of identity used for the managed cluster. For more information see [use managed identities in AKS](https://docs.microsoft.com/azure/aks/use-managed-identity). Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The user identity associated with the managed cluster. This identity will be used in control plane. Only one user assigned identity is allowed. The keys must be ARM resource IDs in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  This is primarily used to expose different UI experiences in the portal for different kinds Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the managed cluster. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="aad_profile" type="object">
      AADProfile specifies attributes for Azure Active Directory integration. For more details see [managed AAD on AKS](https://docs.microsoft.com/azure/aks/managed-aad). Optional.
      <Expandable title="properties">
        <ResponseField name="admin_group_object_ids" type="list(string)">
          The list of AAD group object IDs that will have admin role of the cluster. Optional.
        </ResponseField>
        <ResponseField name="client_app_id" type="string">
          (DEPRECATED) The client AAD application ID. Learn more at https://aka.ms/aks/aad-legacy. Optional.
        </ResponseField>
        <ResponseField name="enable_azure_rbac" type="bool">
          Whether to enable Azure RBAC for Kubernetes authorization. Optional.
        </ResponseField>
        <ResponseField name="managed" type="bool">
          Whether to enable managed AAD. Optional.
        </ResponseField>
        <ResponseField name="server_app_id" type="string">
          (DEPRECATED) The server AAD application ID. Learn more at https://aka.ms/aks/aad-legacy. Optional.
        </ResponseField>
        <ResponseField name="server_app_secret" type="string">
          (DEPRECATED) The server AAD application secret. Learn more at https://aka.ms/aks/aad-legacy. Optional.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          The AAD tenant ID to use for authentication. If not specified, will use the tenant of the deployment subscription. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="addon_profiles" type="map(object)">
      The profile of managed cluster add-on. Optional.
      <Expandable title="properties">
        <ResponseField name="config" type="map(string)">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="identity" type="object">
          Optional; if omitted, computed by Azure.
          <Expandable title="properties">
            <ResponseField name="client_id" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="object_id" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="resource_id" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="agent_pool_profiles" type="list(object)">
      The agent pool properties. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ai_toolchain_operator_profile" type="object">
      When enabling the operator, a set of AKS managed CRDs and controllers will be installed in the cluster. The operator automates the deployment of OSS models for inference and/or training purposes. It provides a set of preset models and enables distributed inference against them. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether to enable AI toolchain operator to the cluster. Indicates if AI toolchain operator enabled or not. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="api_server_access_profile" type="object">
      Access profile for managed cluster API server. Optional.
      <Expandable title="properties">
        <ResponseField name="authorized_ipranges" type="list(string)">
          The IP ranges authorized to access the Kubernetes API server. IP ranges are specified in CIDR format, e.g. 137.117.106.88/29. This feature is not compatible with clusters that use Public IP Per Node, or clusters that are using a Basic Load Balancer. For more information see [API server authorized IP ranges](https://docs.microsoft.com/azure/aks/api-server-authorized-ip-ranges). Optional.
        </ResponseField>
        <ResponseField name="disable_run_command" type="bool">
          Whether to disable run command for the cluster or not. Optional.
        </ResponseField>
        <ResponseField name="enable_private_cluster" type="bool">
          Whether to create the cluster as a private cluster or not. For more details, see [Creating a private AKS cluster](https://docs.microsoft.com/azure/aks/private-clusters). Optional.
        </ResponseField>
        <ResponseField name="enable_private_cluster_public_fqdn" type="bool">
          Whether to create additional public FQDN for private cluster or not. Optional.
        </ResponseField>
        <ResponseField name="enable_vnet_integration" type="bool">
          Whether to enable apiserver vnet integration for the cluster or not. See aka.ms/AksVnetIntegration for more details. Optional.
        </ResponseField>
        <ResponseField name="private_dnszone" type="string">
          The private DNS zone mode for the cluster. The default is System. For more details see [configure private DNS zone](https://docs.microsoft.com/azure/aks/private-clusters#configure-private-dns-zone). Allowed values are 'system' and 'none'. Optional.
        </ResponseField>
        <ResponseField name="subnet_id" type="string">
          The subnet to be used when apiserver vnet integration is enabled. It is required when creating a new cluster with BYO Vnet, or when updating an existing cluster to enable apiserver vnet integration. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="auto_scaler_profile" type="object">
      Parameters to be applied to the cluster-autoscaler when enabled Optional.
      <Expandable title="properties">
        <ResponseField name="balance_similar_node_groups" type="string">
          Detects similar node pools and balances the number of nodes between them. Valid values are 'true' and 'false' Optional.
        </ResponseField>
        <ResponseField name="daemonset_eviction_for_empty_nodes" type="bool">
          DaemonSet pods will be gracefully terminated from empty nodes. If set to true, all daemonset pods on empty nodes will be evicted before deletion of the node. If the daemonset pod cannot be evicted another node will be chosen for scaling. If set to false, the node will be deleted without ensuring that daemonset pods are deleted or evicted. Optional.
        </ResponseField>
        <ResponseField name="daemonset_eviction_for_occupied_nodes" type="bool">
          DaemonSet pods will be gracefully terminated from non-empty nodes. If set to true, all daemonset pods on occupied nodes will be evicted before deletion of the node. If the daemonset pod cannot be evicted another node will be chosen for scaling. If set to false, the node will be deleted without ensuring that daemonset pods are deleted or evicted. Optional.
        </ResponseField>
        <ResponseField name="expander" type="string">
          The expander to use when scaling up. If not specified, the default is 'random'. See [expanders](https://github.com/kubernetes/autoscaler/blob/master/cluster-autoscaler/FAQ.md#what-are-expanders) for more information. Optional.
        </ResponseField>
        <ResponseField name="ignore_daemonsets_utilization" type="bool">
          Should CA ignore DaemonSet pods when calculating resource utilization for scaling down. If set to true, the resources used by daemonset will be taken into account when making scaling down decisions. Optional.
        </ResponseField>
        <ResponseField name="max_empty_bulk_delete" type="string">
          The maximum number of empty nodes that can be deleted at the same time. This must be a positive integer. The default is 10. Optional.
        </ResponseField>
        <ResponseField name="max_graceful_termination_sec" type="string">
          The maximum number of seconds the cluster autoscaler waits for pod termination when trying to scale down a node. The default is 600. Optional.
        </ResponseField>
        <ResponseField name="max_node_provision_time" type="string">
          The maximum time the autoscaler waits for a node to be provisioned. The default is '15m'. Values must be an integer followed by an 'm'. No unit of time other than minutes (m) is supported. Optional.
        </ResponseField>
        <ResponseField name="max_total_unready_percentage" type="string">
          The maximum percentage of unready nodes in the cluster. After this percentage is exceeded, cluster autoscaler halts operations. The default is 45. The maximum is 100 and the minimum is 0. Optional.
        </ResponseField>
        <ResponseField name="new_pod_scale_up_delay" type="string">
          Ignore unscheduled pods before they're a certain age. For scenarios like burst/batch scale where you don't want CA to act before the kubernetes scheduler could schedule all the pods, you can tell CA to ignore unscheduled pods before they're a certain age. The default is '0s'. Values must be an integer followed by a unit ('s' for seconds, 'm' for minutes, 'h' for hours, etc). Optional.
        </ResponseField>
        <ResponseField name="ok_total_unready_count" type="string">
          The number of allowed unready nodes, irrespective of max-total-unready-percentage. This must be an integer. The default is 3. Optional.
        </ResponseField>
        <ResponseField name="scale_down_delay_after_add" type="string">
          How long after scale up that scale down evaluation resumes. The default is '10m'. Values must be an integer followed by an 'm'. No unit of time other than minutes (m) is supported. Optional.
        </ResponseField>
        <ResponseField name="scale_down_delay_after_delete" type="string">
          How long after node deletion that scale down evaluation resumes. The default is the scan-interval. Values must be an integer followed by an 'm'. No unit of time other than minutes (m) is supported. Optional.
        </ResponseField>
        <ResponseField name="scale_down_delay_after_failure" type="string">
          How long after scale down failure that scale down evaluation resumes. The default is '3m'. Values must be an integer followed by an 'm'. No unit of time other than minutes (m) is supported. Optional.
        </ResponseField>
        <ResponseField name="scale_down_unneeded_time" type="string">
          How long a node should be unneeded before it is eligible for scale down. The default is '10m'. Values must be an integer followed by an 'm'. No unit of time other than minutes (m) is supported. Optional.
        </ResponseField>
        <ResponseField name="scale_down_unready_time" type="string">
          How long an unready node should be unneeded before it is eligible for scale down. The default is '20m'. Values must be an integer followed by an 'm'. No unit of time other than minutes (m) is supported. Optional.
        </ResponseField>
        <ResponseField name="scale_down_utilization_threshold" type="string">
          Node utilization level, defined as sum of requested resources divided by capacity, below which a node can be considered for scale down. The default is '0.5'. Optional.
        </ResponseField>
        <ResponseField name="scan_interval" type="string">
          How often cluster is reevaluated for scale up or down. The default is '10'. Values must be an integer number of seconds. Optional.
        </ResponseField>
        <ResponseField name="skip_nodes_with_local_storage" type="string">
          If cluster autoscaler will skip deleting nodes with pods with local storage, for example, EmptyDir or HostPath. The default is true. Optional.
        </ResponseField>
        <ResponseField name="skip_nodes_with_system_pods" type="string">
          If cluster autoscaler will skip deleting nodes with pods from kube-system (except for DaemonSet or mirror pods). The default is true. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="auto_upgrade_profile" type="object">
      Auto upgrade profile for a managed cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="node_osupgrade_channel" type="string">
          Node OS Upgrade Channel. Manner in which the OS on your nodes is updated. The default is NodeImage. Optional.
        </ResponseField>
        <ResponseField name="upgrade_channel" type="string">
          The upgrade channel for auto upgrade. The default is 'none'. For more information see [setting the AKS cluster auto-upgrade channel](https://docs.microsoft.com/azure/aks/upgrade-cluster#set-auto-upgrade-channel). Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="azure_monitor_profile" type="object">
      Azure Monitor addon profiles for monitoring the managed cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="app_monitoring" type="object">
          Application Monitoring profile for AKS. Optional.
          <Expandable title="properties">
            <ResponseField name="auto_instrumentation" type="object">
              Application Monitoring auto-instrumentation for AKS. Deploys a webhook that auto-instruments workloads with Microsoft OpenTelemetry Distros to collect OpenTelemetry metrics, logs, and traces. See https://aka.ms/AKSAppMonitoringDocs and https://aka.ms/AzureMonitorApplicationMonitoring for an overview. Optional.
              <Expandable title="properties">
                <ResponseField name="enabled" type="bool">
                  Indicates if Application Monitoring Auto-instrumentation is enabled or not. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="metrics" type="object">
          Metrics profile for the Azure Monitor managed service for Prometheus addon. Collect out-of-the-box Kubernetes infrastructure metrics to send to an Azure Monitor Workspace and configure additional scraping for custom targets. See aka.ms/AzureManagedPrometheus for an overview. Optional.
          <Expandable title="properties">
            <ResponseField name="control_plane" type="object">
              Control plane metrics collection profile for the Azure Managed Prometheus addon. Configures collection of operational runtime metrics from managed control plane components (kube-apiserver, etcd, etc). See aka.ms/aks/controlplane-metrics for an overview. Optional.
              <Expandable title="properties">
                <ResponseField name="enabled" type="bool">
                  Whether to enable or disable collection of control plane metrics by the Azure Managed Prometheus addon. Defaults to disabled. See aka.ms/aks/controlplane-metrics for details. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="enabled" type="bool" required>
              Whether to enable or disable the Azure Managed Prometheus addon for Prometheus monitoring. See aka.ms/AzureManagedPrometheus-aks-enable for details on enabling and disabling. Required.
            </ResponseField>
            <ResponseField name="kube_state_metrics" type="object">
              Kube State Metrics profile for the Azure Managed Prometheus addon. These optional settings are for the kube-state-metrics pod that is deployed with the addon. See aka.ms/AzureManagedPrometheus-optional-parameters for details. Optional.
              <Expandable title="properties">
                <ResponseField name="metric_annotations_allow_list" type="string">
                  Comma-separated list of Kubernetes annotation keys that will be used in the resource's labels metric (Example: 'namespaces=[kubernetes.io/team,...],pods=[kubernetes.io/team],...'). By default the metric contains only resource name and namespace labels. Optional.
                </ResponseField>
                <ResponseField name="metric_labels_allowlist" type="string">
                  Comma-separated list of additional Kubernetes label keys that will be used in the resource's labels metric (Example: 'namespaces=[k8s-label-1,k8s-label-n,...],pods=[app],...'). By default the metric contains only resource name and namespace labels. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="azure_portal_fqdn" type="string">
      The special FQDN used by the Azure Portal to access the Managed Cluster. This FQDN is for use only by the Azure Portal and should not be used by other clients. The Azure Portal requires certain Cross-Origin Resource Sharing (CORS) headers to be sent in some responses, which Kubernetes APIServer doesn't handle by default. This special FQDN supports CORS, allowing the Azure Portal to function properly. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="bootstrap_profile" type="object">
      The bootstrap profile. Optional.
      <Expandable title="properties">
        <ResponseField name="artifact_source" type="string">
          The artifact source. The source where the artifacts are downloaded from. Optional.
        </ResponseField>
        <ResponseField name="container_registry_id" type="string">
          The resource Id of Azure Container Registry. The registry must have private network access, premium SKU and zone redundancy. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="current_kubernetes_version" type="string">
      The version of Kubernetes the Managed Cluster is running. If kubernetesVersion was a fully specified version &lt;major.minor.patch&gt;, this field will be exactly equal to it. If kubernetesVersion was &lt;major.minor&gt;, this field will contain the full &lt;major.minor.patch&gt; version being used. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="disable_local_accounts" type="bool">
      If local accounts should be disabled on the Managed Cluster. If set to true, getting static credentials will be disabled for this cluster. This must only be used on Managed Clusters that are AAD enabled. For more details see [disable local accounts](https://docs.microsoft.com/azure/aks/managed-aad#disable-local-accounts-preview). Optional.
    </ResponseField>
    <ResponseField name="disk_encryption_set_id" type="string">
      The Resource ID of the disk encryption set to use for enabling encryption at rest. This is of the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.Compute/diskEncryptionSets/&#123;encryptionSetName&#125;' Optional.
    </ResponseField>
    <ResponseField name="dns_prefix" type="string">
      The DNS prefix of the Managed Cluster. This cannot be updated once the Managed Cluster has been created. Optional.
    </ResponseField>
    <ResponseField name="enable_rbac" type="bool">
      Whether to enable Kubernetes Role-Based Access Control. Optional.
    </ResponseField>
    <ResponseField name="fqdn" type="string">
      The FQDN of the master pool. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="fqdn_subdomain" type="string">
      The FQDN subdomain of the private cluster with custom private dns zone. This cannot be updated once the Managed Cluster has been created. Optional.
    </ResponseField>
    <ResponseField name="hosted_system_profile" type="object">
      Settings for hosted system addons. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether to enable hosted system addons for the cluster. Optional.
        </ResponseField>
        <ResponseField name="node_subnet_id" type="string">
          The ID of the subnet that will be joined by worker nodes managed by node auto provisioner for running workload pods in your tenant. This must be provided together with `systemNodeSubnetID` and `apiserverAccessProfile.subnetId`, and all three subnet IDs must be in the same VNet. If you don’t specify it, AKS will create a subnet in the managed resource group using a default /16 CIDR. Optional.
        </ResponseField>
        <ResponseField name="system_node_subnet_id" type="string">
          The ID of the subnet that will be joined by system nodes managed and hosted by AKS for running critical system addons. This ID must be provided together with `nodeSubnetID` and `apiserverAccessProfile.subnetId`, and all three subnet IDs must belong to the same VNet. If you don’t specify it, AKS will create a subnet in the managed resource group using a default /26 CIDR. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="http_proxy_config" type="object">
      Cluster HTTP proxy configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether to enable HTTP proxy. If disabled, the specified proxy configuration will be not be set on pods and nodes. If not specified, the default is true. Optional.
        </ResponseField>
        <ResponseField name="http_proxy" type="string">
          The HTTP proxy server endpoint to use. Optional.
        </ResponseField>
        <ResponseField name="https_proxy" type="string">
          The HTTPS proxy server endpoint to use. Optional.
        </ResponseField>
        <ResponseField name="no_proxy" type="list(string)">
          The endpoints that should not go through proxy. Optional.
        </ResponseField>
        <ResponseField name="trusted_ca" type="string">
          Alternative CA cert to use for connecting to proxy servers. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="identity_profile" type="map(object)">
      The user identity associated with the managed cluster. This identity will be used by the kubelet. Only one user assigned identity is allowed. The only accepted key is "kubeletidentity", with value of "resourceId": "/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;". Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="object_id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="resource_id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ingress_profile" type="object">
      Ingress profile for the container service cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="gateway_api" type="object">
          Configuration for managed Gateway API CRDs. See https://aka.ms/k8s-gateway-api for more details. Optional.
          <Expandable title="properties">
            <ResponseField name="installation" type="string">
              Configuration for the managed Gateway API installation. If not specified, the default is 'Disabled'. See https://aka.ms/k8s-gateway-api for more details. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="web_app_routing" type="object">
          Application Routing add-on settings for the ingress profile. Optional.
          <Expandable title="properties">
            <ResponseField name="dns_zone_resource_ids" type="list(string)">
              Resource IDs of the DNS zones to be associated with the Application Routing add-on. Used only when Application Routing add-on is enabled. Public and private DNS zones can be in different resource groups, but all public DNS zones must be in the same resource group and all private DNS zones must be in the same resource group. Optional.
            </ResponseField>
            <ResponseField name="enabled" type="bool">
              Whether to enable the Application Routing add-on. Optional.
            </ResponseField>
            <ResponseField name="gateway_apiimplementations" type="object">
              Configurations for Gateway API providers to be used for managed ingress with App Routing. Optional.
              <Expandable title="properties">
                <ResponseField name="app_routing_istio" type="object">
                  Configuration for using a sidecar-less Istio control plane for managed ingress via the Gateway API with App Routing. See https://aka.ms/gateway-on-istio for information on using Istio for ingress via the Gateway API. Optional.
                  <Expandable title="properties">
                    <ResponseField name="mode" type="string">
                      Whether to enable Istio as a Gateway API implementation for managed ingress with App Routing. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="identity" type="object">
              Details about a user assigned identity. Optional.
              <Expandable title="properties">
                <ResponseField name="client_id" type="string">
                  The client ID of the user assigned identity. Optional.
                </ResponseField>
                <ResponseField name="object_id" type="string">
                  The object ID of the user assigned identity. Optional.
                </ResponseField>
                <ResponseField name="resource_id" type="string">
                  The resource ID of the user assigned identity. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="nginx" type="object">
              Nginx ingress controller configuration for the managed cluster ingress profile. Optional.
              <Expandable title="properties">
                <ResponseField name="default_ingress_controller_type" type="string">
                  Ingress type for the default NginxIngressController custom resource Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kubernetes_version" type="string">
      The version of Kubernetes specified by the user. Both patch version &lt;major.minor.patch&gt; (e.g. 1.20.13) and &lt;major.minor&gt; (e.g. 1.20) are supported. When &lt;major.minor&gt; is specified, the latest supported GA patch version is chosen automatically. Updating the cluster with the same &lt;major.minor&gt; once it has been created (e.g. 1.14.x -&gt; 1.14) will not trigger an upgrade, even if a newer patch version is available. When you upgrade a supported AKS cluster, Kubernetes minor versions cannot be skipped. All upgrades must be performed sequentially by major version number. For example, upgrades between 1.14.x -&gt; 1.15.x or 1.15.x -&gt; 1.16.x are allowed, however 1.14.x -&gt; 1.16.x is not allowed. See [upgrading an AKS cluster](https://docs.microsoft.com/azure/aks/upgrade-cluster) for more details. Optional.
    </ResponseField>
    <ResponseField name="linux_profile" type="object">
      Profile for Linux VMs in the container service cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="admin_username" type="string" required>
          The administrator username to use for Linux VMs. Required.
        </ResponseField>
        <ResponseField name="ssh" type="object" required>
          SSH configuration for Linux-based VMs running on Azure. Required.
          <Expandable title="properties">
            <ResponseField name="public_keys" type="list(object)" required>
              The list of SSH public keys used to authenticate with Linux-based VMs. A maximum of 1 key may be specified. Required.
              <Expandable title="properties">
                <ResponseField name="key_data" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_agent_pools" type="number">
      The max number of agent pools for the managed cluster. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="metrics_profile" type="object">
      The metrics profile for the ManagedCluster. Optional.
      <Expandable title="properties">
        <ResponseField name="cost_analysis" type="object">
          The cost analysis configuration for the cluster Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether to enable cost analysis. The Managed Cluster sku.tier must be set to 'Standard' or 'Premium' to enable this feature. Enabling this will add Kubernetes Namespace and Deployment details to the Cost Analysis views in the Azure portal. If not specified, the default is false. For more information see aka.ms/aks/docs/cost-analysis. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_profile" type="object">
      Profile of network configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="advanced_networking" type="object">
          Advanced Networking profile for enabling observability and security feature suite on a cluster. For more information see aka.ms/aksadvancednetworking. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Indicates the enablement of Advanced Networking functionalities of observability and security on AKS clusters. When this is set to true, all observability and security features will be set to enabled unless explicitly disabled. If not specified, the default is false. Optional.
            </ResponseField>
            <ResponseField name="observability" type="object">
              Observability profile to enable advanced network metrics and flow logs with historical contexts. Optional.
              <Expandable title="properties">
                <ResponseField name="enabled" type="bool">
                  Indicates the enablement of Advanced Networking observability functionalities on clusters. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="performance" type="object">
              Profile to enable performance-enhancing features on clusters that use Azure CNI powered by Cilium. Optional.
              <Expandable title="properties">
                <ResponseField name="acceleration_mode" type="string">
                  Enable advanced network acceleration options. This allows users to configure acceleration using BPF host routing. This can be enabled only with Cilium dataplane. If not specified, the default value is None (no acceleration). The acceleration mode can be changed on a pre-existing cluster. See https://aka.ms/acnsperformance for a detailed explanation Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="security" type="object">
              Security profile to enable security features on cilium based cluster. Optional.
              <Expandable title="properties">
                <ResponseField name="advanced_network_policies" type="string">
                  Enable advanced network policies. This allows users to configure Layer 7 network policies (FQDN, HTTP, Kafka). Policies themselves must be configured via the Cilium Network Policy resources, see https://docs.cilium.io/en/latest/security/policy/index.html. This can be enabled only on cilium-based clusters. If not specified, the default value is FQDN if security.enabled is set to true. Optional.
                </ResponseField>
                <ResponseField name="enabled" type="bool">
                  This feature allows user to configure network policy based on DNS (FQDN) names. It can be enabled only on cilium based clusters. If not specified, the default is false. Optional.
                </ResponseField>
                <ResponseField name="transit_encryption" type="object">
                  Encryption configuration for Cilium-based clusters. Once enabled all traffic between Cilium managed pods will be encrypted when it leaves the node boundary. Optional.
                  <Expandable title="properties">
                    <ResponseField name="type" type="string">
                      Configures pod-to-pod encryption. This can be enabled only on Cilium-based clusters. If not specified, the default value is None. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dns_service_ip" type="string">
          An IP address assigned to the Kubernetes DNS service. It must be within the Kubernetes service address range specified in serviceCidr. Optional.
        </ResponseField>
        <ResponseField name="ip_families" type="list(string)">
          The IP families used to specify IP versions available to the cluster. IP families are used to determine single-stack or dual-stack clusters. For single-stack, the expected value is IPv4. For dual-stack, the expected values are IPv4 and IPv6. Optional.
        </ResponseField>
        <ResponseField name="load_balancer_profile" type="object">
          Profile of the managed cluster load balancer. Optional.
          <Expandable title="properties">
            <ResponseField name="allocated_outbound_ports" type="number">
              The desired number of allocated SNAT ports per VM. Allowed values are in the range of 0 to 64000 (inclusive). The default value is 0 which results in Azure dynamically allocating ports. Optional.
            </ResponseField>
            <ResponseField name="backend_pool_type" type="string">
              The type of the managed inbound Load Balancer BackendPool. Optional.
            </ResponseField>
            <ResponseField name="effective_outbound_ips" type="list(object)">
              The effective outbound IP resources of the cluster load balancer. Computed by Azure after `ship`.
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="enable_multiple_standard_load_balancers" type="bool">
              Enable multiple standard load balancers per AKS cluster or not. Optional.
            </ResponseField>
            <ResponseField name="idle_timeout_in_minutes" type="number">
              Desired outbound flow idle timeout in minutes. Allowed values are in the range of 4 to 120 (inclusive). The default value is 30 minutes. Optional.
            </ResponseField>
            <ResponseField name="managed_outbound_ips" type="object">
              Desired managed outbound IPs for the cluster load balancer. Optional.
              <Expandable title="properties">
                <ResponseField name="count" type="number">
                  The desired number of IPv4 outbound IPs created/managed by Azure for the cluster load balancer. Allowed values must be in the range of 1 to 100 (inclusive). The default value is 1. Optional.
                </ResponseField>
                <ResponseField name="count_ipv6" type="number">
                  The desired number of IPv6 outbound IPs created/managed by Azure for the cluster load balancer. Allowed values must be in the range of 1 to 100 (inclusive). The default value is 0 for single-stack and 1 for dual-stack. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="outbound_ipprefixes" type="object">
              Desired outbound IP Prefix resources for the cluster load balancer. Optional.
              <Expandable title="properties">
                <ResponseField name="public_ipprefixes" type="list(object)">
                  A list of public IP prefix resources. Optional.
                  <Expandable title="properties">
                    <ResponseField name="id" type="string">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="outbound_ips" type="object">
              Desired outbound IP resources for the cluster load balancer. Optional.
              <Expandable title="properties">
                <ResponseField name="public_ips" type="list(object)">
                  A list of public IP resources. Optional.
                  <Expandable title="properties">
                    <ResponseField name="id" type="string">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="load_balancer_sku" type="string">
          The load balancer sku for the managed cluster. The default is 'standard'. See [Azure Load Balancer SKUs](https://docs.microsoft.com/azure/load-balancer/skus) for more information about the differences between load balancer SKUs. Optional.
        </ResponseField>
        <ResponseField name="nat_gateway_profile" type="object">
          Profile of the managed cluster NAT gateway. Optional.
          <Expandable title="properties">
            <ResponseField name="effective_outbound_ips" type="list(object)">
              The effective outbound IP resources of the cluster NAT gateway. Computed by Azure after `ship`.
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="idle_timeout_in_minutes" type="number">
              Desired outbound flow idle timeout in minutes. Allowed values are in the range of 4 to 120 (inclusive). The default value is 4 minutes. Optional.
            </ResponseField>
            <ResponseField name="managed_outbound_ipprofile" type="object">
              Profile of the managed outbound IP resources of the managed cluster. Optional.
              <Expandable title="properties">
                <ResponseField name="count" type="number">
                  The desired number of outbound IPs created/managed by Azure. Allowed values must be in the range of 1 to 16 (inclusive). The default value is 1. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_dataplane" type="string">
          Network dataplane used in the Kubernetes cluster. Optional.
        </ResponseField>
        <ResponseField name="network_mode" type="string">
          The network mode Azure CNI is configured with. This cannot be specified if networkPlugin is anything other than 'azure'. Optional.
        </ResponseField>
        <ResponseField name="network_plugin" type="string">
          Network plugin used for building the Kubernetes network. Optional.
        </ResponseField>
        <ResponseField name="network_plugin_mode" type="string">
          The mode the network plugin should use. Optional.
        </ResponseField>
        <ResponseField name="network_policy" type="string">
          Network policy used for building the Kubernetes network. Optional.
        </ResponseField>
        <ResponseField name="outbound_type" type="string">
          The outbound (egress) routing method. This can only be set at cluster creation time and cannot be changed later. For more information see [egress outbound type](https://docs.microsoft.com/azure/aks/egress-outboundtype). Optional.
        </ResponseField>
        <ResponseField name="pod_cidr" type="string">
          A CIDR notation IP range from which to assign pod IPs when kubenet is used. Optional.
        </ResponseField>
        <ResponseField name="pod_cidrs" type="list(string)">
          The CIDR notation IP ranges from which to assign pod IPs. One IPv4 CIDR is expected for single-stack networking. Two CIDRs, one for each IP family (IPv4/IPv6), is expected for dual-stack networking. Optional.
        </ResponseField>
        <ResponseField name="service_cidr" type="string">
          A CIDR notation IP range from which to assign service cluster IPs. It must not overlap with any Subnet IP ranges. Optional.
        </ResponseField>
        <ResponseField name="service_cidrs" type="list(string)">
          The CIDR notation IP ranges from which to assign service cluster IPs. One IPv4 CIDR is expected for single-stack networking. Two CIDRs, one for each IP family (IPv4/IPv6), is expected for dual-stack networking. They must not overlap with any Subnet IP ranges. Optional.
        </ResponseField>
        <ResponseField name="static_egress_gateway_profile" type="object">
          The Static Egress Gateway addon configuration for the cluster. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Enable Static Egress Gateway addon. Indicates if Static Egress Gateway addon is enabled or not. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="node_provisioning_profile" type="object">
      Node provisioning profile for the managed cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="default_node_pools" type="string">
          The set of default Karpenter NodePools (CRDs) configured for node provisioning. This field has no effect unless mode is 'Auto'. Warning: Changing this from Auto to None on an existing cluster will cause the default Karpenter NodePools to be deleted, which will drain and delete the nodes associated with those pools. It is strongly recommended to not do this unless there are idle nodes ready to take the pods evicted by that action. If not specified, the default is Auto. For more information see aka.ms/aks/nap#node-pools. Optional.
        </ResponseField>
        <ResponseField name="mode" type="string">
          The node provisioning mode. If not specified, the default is Manual. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="node_resource_group" type="string">
      The name of the resource group containing agent pool nodes. Optional.
    </ResponseField>
    <ResponseField name="node_resource_group_profile" type="object">
      Node resource group lockdown profile for a managed cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="restriction_level" type="string">
          The restriction level applied to the cluster's node resource group. If not specified, the default is 'Unrestricted' Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="oidc_issuer_profile" type="object">
      The OIDC issuer profile of the Managed Cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether the OIDC issuer is enabled. Optional.
        </ResponseField>
        <ResponseField name="issuer_url" type="string">
          The OIDC issuer url of the Managed Cluster. Computed by Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pod_identity_profile" type="object">
      The pod identity profile of the Managed Cluster. See [use AAD pod identity](https://docs.microsoft.com/azure/aks/use-azure-ad-pod-identity) for more details on pod identity integration. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_network_plugin_kubenet" type="bool">
          Whether pod identity is allowed to run on clusters with Kubenet networking. Running in Kubenet is disabled by default due to the security related nature of AAD Pod Identity and the risks of IP spoofing. See [using Kubenet network plugin with AAD Pod Identity](https://docs.microsoft.com/azure/aks/use-azure-ad-pod-identity#using-kubenet-network-plugin-with-azure-active-directory-pod-managed-identities) for more information. Optional.
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Whether the pod identity addon is enabled. Optional.
        </ResponseField>
        <ResponseField name="user_assigned_identities" type="list(object)">
          The pod identities to use in the cluster. Optional.
          <Expandable title="properties">
            <ResponseField name="binding_selector" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="identity" type="object">
              Optional; if omitted, computed by Azure.
              <Expandable title="properties">
                <ResponseField name="client_id" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
                <ResponseField name="object_id" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
                <ResponseField name="resource_id" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="namespace" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="provisioning_info" type="object">
              Optional; if omitted, computed by Azure.
              <Expandable title="properties">
                <ResponseField name="error" type="object">
                  Optional; if omitted, computed by Azure.
                  <Expandable title="properties">
                    <ResponseField name="error" type="object">
                      Optional; if omitted, computed by Azure.
                      <Expandable title="properties">
                        <ResponseField name="code" type="string">
                          Optional; if omitted, computed by Azure.
                        </ResponseField>
                        <ResponseField name="details" type="list(object)">
                          Optional; if omitted, computed by Azure.
                        </ResponseField>
                        <ResponseField name="message" type="string">
                          Optional; if omitted, computed by Azure.
                        </ResponseField>
                        <ResponseField name="target" type="string">
                          Optional; if omitted, computed by Azure.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="provisioning_state" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="user_assigned_identity_exceptions" type="list(object)">
          The pod identity exceptions to allow. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="namespace" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="pod_labels" type="map(string)">
              Optional; if omitted, computed by Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="power_state" type="object">
      Describes the Power State of the cluster Optional.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          Tells whether the cluster is Running or Stopped Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_fqdn" type="string">
      The FQDN of private cluster. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="private_link_resources" type="list(object)">
      Private link resources associated with the cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="group_id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="private_link_service_id" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="required_members" type="list(string)">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="type" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The current provisioning state. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      PublicNetworkAccess of the managedCluster. Allow or deny public network access for AKS Optional.
    </ResponseField>
    <ResponseField name="resource_uid" type="string">
      The resourceUID uniquely identifies ManagedClusters that reuse ARM ResourceIds (i.e: create, delete, create sequence) Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="scheduler_profile" type="object">
      Profile with scheduler-related settings, like the configuration mode for each scheduler managed by AKS. See https://aka.ms/aks/scheduler-profile. Optional.
      <Expandable title="properties">
        <ResponseField name="upstream" type="object">
          Profile with settings related to a specific instance of an AKS-managed scheduler. Optional.
          <Expandable title="properties">
            <ResponseField name="scheduler_config_mode" type="string">
              The config customization mode for this scheduler instance. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="security_profile" type="object">
      Security profile for the container service cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="azure_key_vault_kms" type="object">
          Azure Key Vault key management service settings for the security profile. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether to enable Azure Key Vault key management service. The default is false. Optional.
            </ResponseField>
            <ResponseField name="key_id" type="string">
              Identifier of Azure Key Vault key. See [key identifier format](https://docs.microsoft.com/en-us/azure/key-vault/general/about-keys-secrets-certificates#vault-name-and-object-name) for more details. When Azure Key Vault key management service is enabled, this field is required and must be a valid key identifier. When Azure Key Vault key management service is disabled, leave the field empty. Optional.
            </ResponseField>
            <ResponseField name="key_vault_network_access" type="string">
              Network access of the key vault. Network access of key vault. The possible values are `Public` and `Private`. `Public` means the key vault allows public access from all networks. `Private` means the key vault disables public access and enables private link. The default value is `Public`. Optional.
            </ResponseField>
            <ResponseField name="key_vault_resource_id" type="string">
              Resource ID of key vault. When keyVaultNetworkAccess is `Private`, this field is required and must be a valid resource ID. When keyVaultNetworkAccess is `Public`, leave the field empty. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="custom_catrust_certificates" type="list(string)">
          A list of up to 10 base64 encoded CAs that will be added to the trust store on all nodes in the cluster. For more information see [Custom CA Trust Certificates](https://learn.microsoft.com/en-us/azure/aks/custom-certificate-authority). Optional.
        </ResponseField>
        <ResponseField name="defender" type="object">
          Microsoft Defender settings for the security profile. Optional.
          <Expandable title="properties">
            <ResponseField name="log_analytics_workspace_resource_id" type="string">
              Resource ID of the Log Analytics workspace to be associated with Microsoft Defender. When Microsoft Defender is enabled, this field is required and must be a valid workspace resource ID. When Microsoft Defender is disabled, leave the field empty. Optional.
            </ResponseField>
            <ResponseField name="security_gating" type="object">
              Microsoft Defender settings for security gating. This validates container image eligibility for deployment based on Defender for Containers security findings. Using Admission Controller, it either audits or prevents deployment of images that do not meet security standards. Optional.
              <Expandable title="properties">
                <ResponseField name="allow_secret_access" type="bool">
                  In use only while registry access is granted by secret rather than managed identity. Sets whether to grant the Defender gating agent access to cluster secrets for pulling images from registries. If secret access is denied and the registry requires pull secrets, the add-on will not perform image validation. Default value is false. Optional.
                </ResponseField>
                <ResponseField name="enabled" type="bool">
                  Whether to enable Defender security gating. When enabled, the gating feature scans container images and audits or blocks deployment of images that do not meet security standards according to configured security rules. For more information, see https://aka.ms/KubernetesDefenderAuditRule. Optional.
                </ResponseField>
                <ResponseField name="identities" type="list(object)">
                  List of identities that the admission controller uses to pull security artifacts from registries. These are the same identities used by the cluster to pull container images. For more information on configuring this identity, see https://learn.microsoft.com/en-us/azure/defender-for-cloud/gated-deployment-infrastructure-as-code. Optional.
                  <Expandable title="properties">
                    <ResponseField name="azure_container_registry" type="string">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                    <ResponseField name="identity" type="object">
                      Optional; if omitted, computed by Azure.
                      <Expandable title="properties">
                        <ResponseField name="client_id" type="string">
                          Optional; if omitted, computed by Azure.
                        </ResponseField>
                        <ResponseField name="object_id" type="string">
                          Optional; if omitted, computed by Azure.
                        </ResponseField>
                        <ResponseField name="resource_id" type="string">
                          Optional; if omitted, computed by Azure.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="security_monitoring" type="object">
              Microsoft Defender settings for the security profile threat detection. Optional.
              <Expandable title="properties">
                <ResponseField name="enabled" type="bool">
                  Whether to enable Defender threat detection Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="image_cleaner" type="object">
          Image Cleaner removes unused images from nodes, freeing up disk space and helping to reduce attack surface area. Here are settings for the security profile. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether to enable Image Cleaner on AKS cluster. Optional.
            </ResponseField>
            <ResponseField name="interval_hours" type="number">
              Image Cleaner scanning interval in hours. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="workload_identity" type="object">
          Workload identity settings for the security profile. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether to enable workload identity. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_mesh_profile" type="object">
      Service mesh profile for a managed cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="istio" type="object">
          Istio service mesh configuration. Optional.
          <Expandable title="properties">
            <ResponseField name="certificate_authority" type="object">
              Istio Service Mesh Certificate Authority (CA) configuration. For now, we only support plugin certificates as described here https://aka.ms/asm-plugin-ca Optional.
              <Expandable title="properties">
                <ResponseField name="plugin" type="object">
                  Plugin certificates information for Service Mesh. Optional.
                  <Expandable title="properties">
                    <ResponseField name="cert_chain_object_name" type="string">
                      Certificate chain object name in Azure Key Vault. Optional.
                    </ResponseField>
                    <ResponseField name="cert_object_name" type="string">
                      Intermediate certificate object name in Azure Key Vault. Optional.
                    </ResponseField>
                    <ResponseField name="key_object_name" type="string">
                      Intermediate certificate private key object name in Azure Key Vault. Optional.
                    </ResponseField>
                    <ResponseField name="key_vault_id" type="string">
                      The resource ID of the Key Vault. Optional.
                    </ResponseField>
                    <ResponseField name="root_cert_object_name" type="string">
                      Root certificate object name in Azure Key Vault. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="components" type="object">
              Istio components configuration. Optional.
              <Expandable title="properties">
                <ResponseField name="egress_gateways" type="list(object)">
                  Istio egress gateways. Optional.
                  <Expandable title="properties">
                    <ResponseField name="enabled" type="bool">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                    <ResponseField name="gateway_configuration_name" type="string">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                    <ResponseField name="namespace" type="string">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ingress_gateways" type="list(object)">
                  Istio ingress gateways. Optional.
                  <Expandable title="properties">
                    <ResponseField name="enabled" type="bool">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                    <ResponseField name="mode" type="string">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="proxy_redirection_mechanism" type="string">
                  Mode of traffic redirection. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="revisions" type="list(string)">
              The list of revisions of the Istio control plane. When an upgrade is not in progress, this holds one value. When canary upgrade is in progress, this can only hold two consecutive values. For more information, see: https://learn.microsoft.com/en-us/azure/aks/istio-upgrade Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="mode" type="string" required>
          Mode of the service mesh. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_principal_profile" type="object">
      Information about a service principal identity for the cluster to use for manipulating Azure APIs. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string" required>
          The ID for the service principal. Required.
        </ResponseField>
        <ResponseField name="secret" type="string">
          The secret password associated with the service principal in plain text. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="status" type="object">
      Contains read-only information about the Managed Cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="provisioning_error" type="object">
          The error detail. Optional.
          <Expandable title="properties">
            <ResponseField name="additional_info" type="list(object)">
              The error additional info. Computed by Azure after `ship`.
              <Expandable title="properties">
                <ResponseField name="info" type="any">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
                <ResponseField name="type" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="code" type="string">
              The error code. Computed by Azure after `ship`.
            </ResponseField>
            <ResponseField name="details" type="list(object)">
              The error details. Computed by Azure after `ship`.
              <Expandable title="properties">
                <ResponseField name="additional_info" type="list(object)">
                  Optional; if omitted, computed by Azure.
                  <Expandable title="properties">
                    <ResponseField name="info" type="any">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      Optional; if omitted, computed by Azure.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="code" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
                <ResponseField name="details" type="list(object)">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
                <ResponseField name="message" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
                <ResponseField name="target" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="message" type="string">
              The error message. Computed by Azure after `ship`.
            </ResponseField>
            <ResponseField name="target" type="string">
              The error target. Computed by Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="storage_profile" type="object">
      Storage profile for the container service cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="blob_csidriver" type="object">
          AzureBlob CSI Driver settings for the storage profile. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether to enable AzureBlob CSI Driver. The default value is false. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disk_csidriver" type="object">
          AzureDisk CSI Driver settings for the storage profile. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether to enable AzureDisk CSI Driver. The default value is true. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="file_csidriver" type="object">
          AzureFile CSI Driver settings for the storage profile. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether to enable AzureFile CSI Driver. The default value is true. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="snapshot_controller" type="object">
          Snapshot Controller settings for the storage profile. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether to enable Snapshot Controller. The default value is true. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="support_plan" type="string">
      Different support tiers for AKS managed clusters Optional.
    </ResponseField>
    <ResponseField name="upgrade_settings" type="object">
      Settings for upgrading a cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="override_settings" type="object">
          Settings for overrides when upgrading a cluster. Optional.
          <Expandable title="properties">
            <ResponseField name="force_upgrade" type="bool">
              Whether to force upgrade the cluster. Note that this option instructs upgrade operation to bypass upgrade protections such as checking for deprecated API usage. Enable this option only with caution. Optional.
            </ResponseField>
            <ResponseField name="until" type="string">
              Until when the overrides are effective. Note that this only matches the start time of an upgrade, and the effectiveness won't change once an upgrade starts even if the `until` expires as upgrade proceeds. This field is not set by default. It must be set for the overrides to take effect. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="windows_profile" type="object">
      Profile for Windows VMs in the managed cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="admin_password" type="string">
          Specifies the password of the administrator account. &lt;br&gt;&lt;br&gt; **Minimum-length:** 8 characters &lt;br&gt;&lt;br&gt; **Max-length:** 123 characters &lt;br&gt;&lt;br&gt; **Complexity requirements:** 3 out of 4 conditions below need to be fulfilled &lt;br&gt; Has lower characters &lt;br&gt;Has upper characters &lt;br&gt; Has a digit &lt;br&gt; Has a special character (Regex match [\W_]) &lt;br&gt;&lt;br&gt; **Disallowed values:** "abc@123", "P@$$w0rd", "P@ssw0rd", "P@ssword123", "Pa$$word", "pass@word1", "Password!", "Password1", "Password22", "iloveyou!" Optional.
        </ResponseField>
        <ResponseField name="admin_username" type="string" required>
          Specifies the name of the administrator account. &lt;br&gt;&lt;br&gt; **Restriction:** Cannot end in "." &lt;br&gt;&lt;br&gt; **Disallowed values:** "administrator", "admin", "user", "user1", "test", "user2", "test1", "user3", "admin1", "1", "123", "a", "actuser", "adm", "admin2", "aspnet", "backup", "console", "david", "guest", "john", "owner", "root", "server", "sql", "support", "support_388945a0", "sys", "test2", "test3", "user4", "user5". &lt;br&gt;&lt;br&gt; **Minimum-length:** 1 character &lt;br&gt;&lt;br&gt; **Max-length:** 20 characters Required.
        </ResponseField>
        <ResponseField name="enable_csiproxy" type="bool">
          Whether to enable CSI proxy. For more details on CSI proxy, see the [CSI proxy GitHub repo](https://github.com/kubernetes-csi/csi-proxy). Optional.
        </ResponseField>
        <ResponseField name="gmsa_profile" type="object">
          Windows gMSA Profile in the managed cluster. Optional.
          <Expandable title="properties">
            <ResponseField name="dns_server" type="string">
              Specifies the DNS server for Windows gMSA. &lt;br&gt;&lt;br&gt; Set it to empty if you have configured the DNS server in the vnet which is used to create the managed cluster. Optional.
            </ResponseField>
            <ResponseField name="enabled" type="bool">
              Whether to enable Windows gMSA. Specifies whether to enable Windows gMSA in the managed cluster. Optional.
            </ResponseField>
            <ResponseField name="root_domain_name" type="string">
              Specifies the root domain name for Windows gMSA. &lt;br&gt;&lt;br&gt; Set it to empty if you have configured the DNS server in the vnet which is used to create the managed cluster. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="license_type" type="string">
          The license type to use for Windows VMs. See [Azure Hybrid User Benefits](https://azure.microsoft.com/pricing/hybrid-benefit/faq/) for more details. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="workload_auto_scaler_profile" type="object">
      Workload Auto-scaler profile for the managed cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="keda" type="object">
          KEDA (Kubernetes Event-driven Autoscaling) settings for the workload auto-scaler profile. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool" required>
              Whether to enable KEDA. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="vertical_pod_autoscaler" type="object">
          VPA (Vertical Pod Autoscaler) settings for the workload auto-scaler profile. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool" required>
              Whether to enable VPA. Default value is false. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The SKU of a Managed Cluster. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of a managed cluster SKU. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The tier of a managed cluster SKU. If not specified, the default is 'Free'. See [AKS Pricing Tier](https://learn.microsoft.com/azure/aks/free-standard-pricing-tiers) for more details. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
