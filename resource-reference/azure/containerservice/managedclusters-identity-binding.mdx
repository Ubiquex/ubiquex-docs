---
title: "azure_containerservice_managedclusters_identity_binding"
description: "`azure_containerservice_managedclusters_identity_binding` (preview) lets multiple AKS clusters share a single user-assigned managed identity's..."
---

`azure_containerservice_managedclusters_identity_binding` (preview) lets multiple AKS clusters share a single user-assigned managed identity's own federated identity credential for workload identity, working around the per-identity FIC quota that limited large-scale multi-cluster deployments.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managedclusters identity binding"})

    		ubx.Resource(containerservice.ManagedclustersIdentityBinding, "example", containerservice.ManagedclustersIdentityBindingConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedclustersIdentityBinding } from "@ubx/sdk-azure/azure/containerservice/managedclusters_identity_binding";

    export default stack("example", () => {
      intent({ summary: "example own managedclusters identity binding" });

      resource(ManagedclustersIdentityBinding, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.containerservice.managedclusters_identity_binding import ManagedclustersIdentityBinding, ManagedclustersIdentityBindingConfig

    def describe():
        ubx.intent("example own managedclusters identity binding")

        ubx.resource(ManagedclustersIdentityBinding, "example", ManagedclustersIdentityBindingConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_containerservice_managedclusters_identity_binding,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  If eTag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  IdentityBinding properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="managed_identity" type="object" required>
      Managed identity profile for the identity binding. Required.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client ID of the managed identity. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="object_id" type="string">
          The object ID of the managed identity. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="resource_id" type="string" required>
          The resource ID of the managed identity. Required.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          The tenant ID of the managed identity. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="oidc_issuer" type="object">
      IdentityBinding OIDC issuer profile. Optional.
      <Expandable title="properties">
        <ResponseField name="oidc_issuer_url" type="string">
          The OIDC issuer URL of the IdentityBinding. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the last accepted operation. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
