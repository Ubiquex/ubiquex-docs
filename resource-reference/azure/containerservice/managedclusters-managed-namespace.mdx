---
title: "azure_containerservice_managedclusters_managed_namespace"
description: "`azure_containerservice_managedclusters_managed_namespace` provisions a Kubernetes namespace on an AKS cluster as its own manageable Azure resource..."
---

`azure_containerservice_managedclusters_managed_namespace` provisions a Kubernetes namespace on an AKS cluster as its own manageable Azure resource, with per-namespace resource quotas, network policy, and labels applied declaratively.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_containerservice_managedclusters --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_containerservice_managedclusters/sdk/go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_containerservice_managedclusters/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managedclusters managed namespace"})

    		ubx.Resource(containerservice.ManagedclustersManagedNamespace, "example", containerservice.ManagedclustersManagedNamespaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_containerservice_managedclusters --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedclustersManagedNamespace } from "./local-sdk/azure_containerservice_managedclusters/sdk/typescript/azure_containerservice_managedclusters/containerservice/managedclusters_managed_namespace.ts";

    export default stack("example", () => {
      intent({ summary: "example own managedclusters managed namespace" });

      resource(ManagedclustersManagedNamespace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_containerservice_managedclusters --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_containerservice_managedclusters/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_containerservice_managedclusters.containerservice import ManagedclustersManagedNamespace, ManagedclustersManagedNamespaceConfig

    def describe():
        ubx.intent("example own managedclusters managed namespace")

        ubx.resource(ManagedclustersManagedNamespace, "example", ManagedclustersManagedNamespaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_containerservice_managedclusters_managed_namespace,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  If eTag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of a namespace managed by ARM Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="adoption_policy" type="string">
      Action if Kubernetes namespace with same name already exists. Optional.
    </ResponseField>
    <ResponseField name="annotations" type="map(string)">
      The annotations of managed namespace. Optional.
    </ResponseField>
    <ResponseField name="default_network_policy" type="object">
      Default network policy of the namespace, specifying ingress and egress rules. Optional.
      <Expandable title="properties">
        <ResponseField name="egress" type="string">
          Enum representing different network policy rules. Optional.
        </ResponseField>
        <ResponseField name="ingress" type="string">
          Enum representing different network policy rules. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_resource_quota" type="object">
      Resource quota for the namespace. Optional.
      <Expandable title="properties">
        <ResponseField name="cpu_limit" type="string">
          CPU limit of the namespace in one-thousandth CPU form. See [CPU resource units](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-cpu) for more details. Optional.
        </ResponseField>
        <ResponseField name="cpu_request" type="string">
          CPU request of the namespace in one-thousandth CPU form. See [CPU resource units](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-cpu) for more details. Optional.
        </ResponseField>
        <ResponseField name="memory_limit" type="string">
          Memory limit of the namespace in the power-of-two equivalents form: Ei, Pi, Ti, Gi, Mi, Ki. See [Memory resource units](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-memory) for more details. Optional.
        </ResponseField>
        <ResponseField name="memory_request" type="string">
          Memory request of the namespace in the power-of-two equivalents form: Ei, Pi, Ti, Gi, Mi, Ki. See [Memory resource units](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-memory) for more details. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="delete_policy" type="string">
      Delete options of a namespace. Optional.
    </ResponseField>
    <ResponseField name="labels" type="map(string)">
      The labels of managed namespace. Optional.
    </ResponseField>
    <ResponseField name="portal_fqdn" type="string">
      The special FQDN used by the Azure Portal to access the Managed Cluster. This FQDN is for use only by the Azure Portal and should not be used by other clients. The Azure Portal requires certain Cross-Origin Resource Sharing (CORS) headers to be sent in some responses, which Kubernetes APIServer doesn't handle by default. This special FQDN supports CORS, allowing the Azure Portal to function properly. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The current provisioning state of the namespace. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
