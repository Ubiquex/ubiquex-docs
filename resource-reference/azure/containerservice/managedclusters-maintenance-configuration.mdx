---
title: "azure_containerservice_managedclusters_maintenance_configuration"
description: "`azure_containerservice_managedclusters_maintenance_configuration` defines a preferred maintenance window for control plane and node OS updates on..."
---

`azure_containerservice_managedclusters_maintenance_configuration` defines a preferred maintenance window for control plane and node OS updates on an AKS cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_containerservice_managedclusters --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_containerservice_managedclusters/sdk/go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_containerservice_managedclusters/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managedclusters maintenance configuration"})

    		ubx.Resource(containerservice.ManagedclustersMaintenanceConfiguration, "example", containerservice.ManagedclustersMaintenanceConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_containerservice_managedclusters --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedclustersMaintenanceConfiguration } from "./local-sdk/azure_containerservice_managedclusters/sdk/typescript/azure_containerservice_managedclusters/containerservice/managedclusters_maintenance_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "example own managedclusters maintenance configuration" });

      resource(ManagedclustersMaintenanceConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_containerservice_managedclusters --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_containerservice_managedclusters/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_containerservice_managedclusters.containerservice import ManagedclustersMaintenanceConfiguration, ManagedclustersMaintenanceConfigurationConfig

    def describe():
        ubx.intent("example own managedclusters maintenance configuration")

        ubx.resource(ManagedclustersMaintenanceConfiguration, "example", ManagedclustersMaintenanceConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_containerservice_managedclusters_maintenance_configuration,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties used to configure planned maintenance for a Managed Cluster. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="maintenance_window" type="object">
      Maintenance window used to configure scheduled auto-upgrade for a Managed Cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="duration_hours" type="number" required>
          Length of maintenance window range from 4 to 24 hours. Required.
        </ResponseField>
        <ResponseField name="not_allowed_dates" type="list(object)">
          Date ranges on which upgrade is not allowed. 'utcOffset' applies to this field. For example, with 'utcOffset: +02:00' and 'dateSpan' being '2022-12-23' to '2023-01-03', maintenance will be blocked from '2022-12-22 22:00' to '2023-01-03 22:00' in UTC time. Optional.
          <Expandable title="properties">
            <ResponseField name="end" type="string">
              The end of this range or window. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="start" type="string">
              The start of this range or window. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="schedule" type="object" required>
          One and only one of the schedule types should be specified. Choose either 'daily', 'weekly', 'absoluteMonthly' or 'relativeMonthly' for your maintenance schedule. Required.
          <Expandable title="properties">
            <ResponseField name="absolute_monthly" type="object">
              For schedules like: 'recur every month on the 15th' or 'recur every 3 months on the 20th'. Optional.
              <Expandable title="properties">
                <ResponseField name="day_of_month" type="number" required>
                  The date of the month. Required.
                </ResponseField>
                <ResponseField name="interval_months" type="number" required>
                  Specifies the number of months between each set of occurrences. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="daily" type="object">
              For schedules like: 'recur every day' or 'recur every 3 days'. Optional.
              <Expandable title="properties">
                <ResponseField name="interval_days" type="number" required>
                  Specifies the number of days between each set of occurrences. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="relative_monthly" type="object">
              For schedules like: 'recur every month on the first Monday' or 'recur every 3 months on last Friday'. Optional.
              <Expandable title="properties">
                <ResponseField name="day_of_week" type="string" required>
                  The weekday enum. Required.
                </ResponseField>
                <ResponseField name="interval_months" type="number" required>
                  Specifies the number of months between each set of occurrences. Required.
                </ResponseField>
                <ResponseField name="week_index" type="string" required>
                  The week index. Specifies on which week of the month the dayOfWeek applies. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weekly" type="object">
              For schedules like: 'recur every Monday' or 'recur every 3 weeks on Wednesday'. Optional.
              <Expandable title="properties">
                <ResponseField name="day_of_week" type="string" required>
                  The weekday enum. Required.
                </ResponseField>
                <ResponseField name="interval_weeks" type="number" required>
                  Specifies the number of weeks between each set of occurrences. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="start_date" type="string">
          The date the maintenance window activates. If the current date is before this date, the maintenance window is inactive and will not be used for upgrades. If not specified, the maintenance window will be active right away. Optional.
        </ResponseField>
        <ResponseField name="start_time" type="string" required>
          The start time of the maintenance window. Accepted values are from '00:00' to '23:59'. 'utcOffset' applies to this field. For example: '02:00' with 'utcOffset: +02:00' means UTC time '00:00'. Required.
        </ResponseField>
        <ResponseField name="utc_offset" type="string">
          The UTC offset in format +/-HH:mm. For example, '+05:30' for IST and '-07:00' for PST. If not specified, the default is '+00:00'. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="not_allowed_time" type="list(object)">
      Time slots on which upgrade is not allowed. Optional.
      <Expandable title="properties">
        <ResponseField name="end" type="string">
          The end of this range or window. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start" type="string">
          The start of this range or window. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="time_in_week" type="list(object)">
      Time slots during the week when planned maintenance is allowed to proceed. If two array entries specify the same day of the week, the applied configuration is the union of times in both entries. Optional.
      <Expandable title="properties">
        <ResponseField name="day" type="string">
          The day-of-week component of this schedule. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="hour_slots" type="list(number)">
          The hour(s) within this day this schedule is active. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
