---
title: "azure_vi_account"
description: "`azure_vi_account` is an Azure Video Indexer account, providing AI-powered extraction of insights (transcripts, faces, topics..."
---

`azure_vi_account` is an Azure Video Indexer account, providing AI-powered extraction of insights (transcripts, faces, topics, brands) from video and audio content.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vi "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/vi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vi account"})

    		ubx.Resource(vi.Account, "example", vi.AccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Account } from "@ubx/sdk-azure/azure/vi/account";

    export default stack("example", () => {
      intent({ summary: "example own vi account" });

      resource(Account, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.vi.account import Account, AccountConfig

    def describe():
        ubx.intent("example own vi account")

        ubx.resource(Account, "example", AccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vi_account, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Azure Video Indexer account properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      The account's data-plane ID. This can be set only when connecting an existing classic account Optional.
    </ResponseField>
    <ResponseField name="account_name" type="string">
      The account's name Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="open_ai_services" type="object">
      The openAi services details Optional.
      <Expandable title="properties">
        <ResponseField name="resource_id" type="string">
          The openAi services resource id Optional.
        </ResponseField>
        <ResponseField name="user_assigned_identity" type="string">
          The user assigned identity to be used to grant permissions Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      List of private endpoint connections associated with the account. Optional.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Gets the status of the account at the time the operation was called. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Whether or not public network access is allowed for the account. Optional.
    </ResponseField>
    <ResponseField name="storage_services" type="object">
      The storage services details Optional.
      <Expandable title="properties">
        <ResponseField name="resource_id" type="string">
          The storage services resource id Optional.
        </ResponseField>
        <ResponseField name="user_assigned_identity" type="string">
          The user assigned identity to be used to grant permissions Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The account's tenant id Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="total_minutes_indexed" type="number">
      An integer representing the total minutes that have been indexed on the account Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="total_seconds_indexed" type="number">
      An integer representing the total seconds that have been indexed on the account Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
