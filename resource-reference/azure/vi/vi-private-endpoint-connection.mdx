---
title: "azure_vi_private_endpoint_connection"
description: "`azure_vi_private_endpoint_connection` represents the connection between a Private Endpoint and an Azure Video Indexer account..."
---

`azure_vi_private_endpoint_connection` represents the connection between a Private Endpoint and an Azure Video Indexer account, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vi/sdk/go
    package main

    import (
    	vi "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vi/vi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vi private endpoint connection"})

    		ubx.Resource(vi.ViPrivateEndpointConnection, "example", vi.ViPrivateEndpointConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ViPrivateEndpointConnection } from "./local-sdk/azure_vi/sdk/typescript/azure_vi/vi/vi_private_endpoint_connection.ts";

    export default stack("example", () => {
      intent({ summary: "example own vi private endpoint connection" });

      resource(ViPrivateEndpointConnection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vi.vi import ViPrivateEndpointConnection, ViPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("example own vi private endpoint connection")

        ubx.resource(ViPrivateEndpointConnection, "example", ViPrivateEndpointConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vi_private_endpoint_connection, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of the private endpoint connection. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="group_ids" type="list(string)">
      The group ids for the private endpoint resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="private_endpoint" type="object">
      The private endpoint resource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The ARM identifier for private endpoint. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_link_service_connection_state" type="object" required>
      A collection of information about the state of the connection between service consumer and provider. Required.
      <Expandable title="properties">
        <ResponseField name="actions_required" type="string">
          A message indicating if changes on the service provider require any updates on the consumer. Optional.
        </ResponseField>
        <ResponseField name="description" type="string">
          The reason for approval/rejection of the connection. Optional.
        </ResponseField>
        <ResponseField name="status" type="string">
          The private endpoint connection status. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The current provisioning state. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
