---
title: "azurerm_kusto_database_principal_assignment"
description: "Real, generated bindings for `azurerm_kusto_database_principal_assignment`."
---

`azurerm_kusto_database_principal_assignment` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kusto "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/kusto"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own database principal assignment"})

    		ubx.Resource(kusto.DatabasePrincipalAssignment, "ci-runner", kusto.DatabasePrincipalAssignmentConfig{
    			ClusterName:       "example-cluster-name",
    			DatabaseName:      "example-database-name",
    			Name:              "ci-runner",
    			PrincipalId:       "example",
    			PrincipalType:     "example",
    			ResourceGroupName: "example-resource-group-name",
    			Role:              "example",
    			TenantId:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatabasePrincipalAssignment } from "jsr:@ubx/sdk-azure/azurerm/kusto/database_principal_assignment";

    export default stack("payments", () => {
      intent({ summary: "payments own database principal assignment" });

      resource(DatabasePrincipalAssignment, "ci-runner", {
        clusterName: "example-cluster-name",
        databaseName: "example-database-name",
        name: "ci-runner",
        principalId: "example",
        principalType: "example",
        resourceGroupName: "example-resource-group-name",
        role: "example",
        tenantId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.kusto import DatabasePrincipalAssignment, DatabasePrincipalAssignmentConfig

    def describe():
        ubx.intent("payments own database principal assignment")

        ubx.resource(DatabasePrincipalAssignment, "ci-runner", DatabasePrincipalAssignmentConfig(
            cluster_name="example-cluster-name",
            database_name="example-database-name",
            name="ci-runner",
            principal_id="example",
            principal_type="example",
            resource_group_name="example-resource-group-name",
            role="example",
            tenant_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_kusto_database_principal_assignment called
    "ci-runner", with cluster name example-cluster-name, database
    name example-database-name, principal id example, principal type
    example, resource group name example-resource-group-name, role
    example, tenant id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tenant_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="principal_name" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="tenant_name" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
