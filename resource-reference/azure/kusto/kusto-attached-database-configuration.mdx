---
title: "azure_kusto_attached_database_configuration"
description: "`azure_azure_kusto_kusto_attached_database_configuration` follows database(s) from a leader Azure Data Explorer cluster into a follower cluster in..."
---

`azure_azure_kusto_kusto_attached_database_configuration` follows database(s) from a leader Azure Data Explorer cluster into a follower cluster in read-only mode, without duplicating the underlying data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kusto "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/kusto"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own kusto attached database configuration"})

    		ubx.Resource(kusto.AttachedDatabaseConfiguration, "example", kusto.AttachedDatabaseConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AttachedDatabaseConfiguration } from "@ubx/sdk-azure/azure/kusto/attached_database_configuration";

    export default stack("example", () => {
      intent({ summary: "example own kusto attached database configuration" });

      resource(AttachedDatabaseConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.kusto import AttachedDatabaseConfiguration, AttachedDatabaseConfigurationConfig

    def describe():
        ubx.intent("example own kusto attached database configuration")

        ubx.resource(AttachedDatabaseConfiguration, "example", AttachedDatabaseConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_kusto_attached_database_configuration, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  Resource location. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Class representing the an attached database configuration properties of kind specific. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="attached_database_names" type="list(string)">
      The list of databases from the clusterResourceId which are currently attached to the cluster. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="cluster_resource_id" type="string" required>
      The resource id of the cluster where the databases you would like to attach reside. Required.
    </ResponseField>
    <ResponseField name="database_name" type="string" required>
      The name of the database which you would like to attach, use * if you want to follow all current and future databases. Required.
    </ResponseField>
    <ResponseField name="database_name_override" type="string">
      Overrides the original database name. Relevant only when attaching to a specific database. Optional.
    </ResponseField>
    <ResponseField name="database_name_prefix" type="string">
      Adds a prefix to the attached databases name. When following an entire cluster, that prefix would be added to all of the databases original names from leader cluster. Optional.
    </ResponseField>
    <ResponseField name="default_principals_modification_kind" type="string" required>
      The default principals modification kind Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioned state of the resource. Optional.
    </ResponseField>
    <ResponseField name="table_level_sharing_properties" type="object">
      Tables that will be included and excluded in the follower database Optional.
      <Expandable title="properties">
        <ResponseField name="external_tables_to_exclude" type="list(string)">
          List of external tables to exclude from the follower database Optional.
        </ResponseField>
        <ResponseField name="external_tables_to_include" type="list(string)">
          List of external tables to include in the follower database Optional.
        </ResponseField>
        <ResponseField name="functions_to_exclude" type="list(string)">
          List of functions to exclude from the follower database Optional.
        </ResponseField>
        <ResponseField name="functions_to_include" type="list(string)">
          List of functions to include in the follower database Optional.
        </ResponseField>
        <ResponseField name="materialized_views_to_exclude" type="list(string)">
          List of materialized views to exclude from the follower database Optional.
        </ResponseField>
        <ResponseField name="materialized_views_to_include" type="list(string)">
          List of materialized views to include in the follower database Optional.
        </ResponseField>
        <ResponseField name="tables_to_exclude" type="list(string)">
          List of tables to exclude from the follower database Optional.
        </ResponseField>
        <ResponseField name="tables_to_include" type="list(string)">
          List of tables to include in the follower database Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
