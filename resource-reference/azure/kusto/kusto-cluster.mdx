---
title: "azure_kusto_cluster"
description: "`azure_azure_kusto_kusto_cluster` is an Azure Data Explorer (Kusto) cluster..."
---

`azure_azure_kusto_kusto_cluster` is an Azure Data Explorer (Kusto) cluster, the provisioned compute that database(s) are created within for high-performance log and telemetry analytics.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_azure_kusto_kusto --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_azure_kusto_kusto/sdk/go
    package main

    import (
    	azure "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_azure_kusto_kusto/azure"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own kusto cluster"})

    		ubx.Resource(azure.KustoKustoCluster, "example", azure.KustoKustoClusterConfig{
    			Sku: map[string]any{"name": "example-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_azure_kusto_kusto --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { KustoKustoCluster } from "./local-sdk/azure_azure_kusto_kusto/sdk/typescript/azure_azure_kusto_kusto/azure/kusto_kusto_cluster.ts";

    export default stack("example", () => {
      intent({ summary: "example own kusto cluster" });

      resource(KustoKustoCluster, "example", {
        sku: { name: "example-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_azure_kusto_kusto --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_azure_kusto_kusto/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_azure_kusto_kusto.azure import KustoKustoCluster, KustoKustoClusterConfig

    def describe():
        ubx.intent("example own kusto cluster")

        ubx.resource(KustoKustoCluster, "example", KustoKustoClusterConfig(
            sku={"name": "example-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_kusto_cluster, with sku a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  "If etag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.") Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="identity" type="object">
  Identity for the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal ID of resource identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of managed identity used. The type 'SystemAssigned, UserAssigned' includes both an implicitly created identity and a set of user-assigned identities. The type 'None' will remove all identities. Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user identities associated with the Kusto cluster. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Class representing the Kusto cluster properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="accepted_audiences" type="list(object)">
      The cluster's accepted audiences. Optional.
      <Expandable title="properties">
        <ResponseField name="value" type="string">
          The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="allowed_fqdn_list" type="list(string)">
      List of allowed FQDNs(Fully Qualified Domain Name) for egress from Cluster. Optional.
    </ResponseField>
    <ResponseField name="allowed_ip_range_list" type="list(string)">
      The list of ips in the format of CIDR allowed to connect to the cluster. Optional.
    </ResponseField>
    <ResponseField name="callout_policies" type="list(object)">
      List of callout policies for egress from Cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="callout_id" type="string">
          A unique identifier for this outbound callout policy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="callout_type" type="string">
          The category of external resource this callout policy permits access to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="callout_uri_regex" type="string">
          A regular expression restricting which outbound URI(s) this callout policy permits. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="outbound_access" type="string">
          Whether outbound network access from this cluster is restricted. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="data_ingestion_uri" type="string">
      The cluster data ingestion URI. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="enable_auto_stop" type="bool">
      A boolean value that indicates if the cluster could be automatically stopped (due to lack of data or no activity for many days). Optional.
    </ResponseField>
    <ResponseField name="enable_disk_encryption" type="bool">
      A boolean value that indicates if the cluster's disks are encrypted. Optional.
    </ResponseField>
    <ResponseField name="enable_double_encryption" type="bool">
      A boolean value that indicates if double encryption is enabled. Optional.
    </ResponseField>
    <ResponseField name="enable_purge" type="bool">
      A boolean value that indicates if the purge operations are enabled. Optional.
    </ResponseField>
    <ResponseField name="enable_streaming_ingest" type="bool">
      A boolean value that indicates if the streaming ingest is enabled. Optional.
    </ResponseField>
    <ResponseField name="engine_type" type="string">
      The engine type Optional.
    </ResponseField>
    <ResponseField name="key_vault_properties" type="object">
      Properties of the key vault. Optional.
      <Expandable title="properties">
        <ResponseField name="federated_identity_client_id" type="string">
          The application (client) ID of the multi-tenant Microsoft Entra application. Used for cross-tenant customer-managed key scenarios where the encryption key is stored in a different tenant than the cluster. The application must be configured with the user-assigned managed identity as a federated identity credential. Optional.
        </ResponseField>
        <ResponseField name="key_name" type="string">
          The name of the key vault key. Optional.
        </ResponseField>
        <ResponseField name="key_vault_uri" type="string">
          The Uri of the key vault. Optional.
        </ResponseField>
        <ResponseField name="key_version" type="string">
          The version of the key vault key. Optional.
        </ResponseField>
        <ResponseField name="user_identity" type="string">
          The user assigned identity (ARM resource id) that has access to the key. The identity must have 'Get', 'Wrap Key', and 'Unwrap Key' permissions on the Key Vault key, or be assigned the 'Key Vault Crypto Service Encryption User' role. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="language_extensions" type="object">
      The list of language extension objects. Optional.
      <Expandable title="properties">
        <ResponseField name="next_link" type="string">
          The link to the next page of resources. Optional.
        </ResponseField>
        <ResponseField name="value" type="list(object)">
          The list of language extensions. Optional.
          <Expandable title="properties">
            <ResponseField name="language_extension_custom_image_name" type="string">
              The name of the custom container image this language extension runs. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="language_extension_image_name" type="string">
              The name of the built-in container image this language extension runs. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="language_extension_name" type="string">
              The name of this language extension, e.g. `PYTHON` or `R`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="migration_cluster" type="object">
      Represents a properties of a cluster that is part of a migration. Optional.
      <Expandable title="properties">
        <ResponseField name="data_ingestion_uri" type="string">
          The public data ingestion URL of the cluster. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="id" type="string">
          The resource ID of the cluster. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="role" type="string">
          The role of the cluster in the migration process. Optional.
        </ResponseField>
        <ResponseField name="uri" type="string">
          The public URL of the cluster. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="optimized_autoscale" type="object">
      A class that contains the optimized auto scale definition. Optional.
      <Expandable title="properties">
        <ResponseField name="is_enabled" type="bool" required>
          A boolean value that indicate if the optimized autoscale feature is enabled or not. Required.
        </ResponseField>
        <ResponseField name="maximum" type="number" required>
          Maximum allowed instances count. Required.
        </ResponseField>
        <ResponseField name="minimum" type="number" required>
          Minimum allowed instances count. Required.
        </ResponseField>
        <ResponseField name="version" type="number" required>
          The version of the template defined, for instance 1. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      A list of private endpoint connections. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioned state of the resource. Optional.
    </ResponseField>
    <ResponseField name="public_iptype" type="string">
      Indicates what public IP type to create - IPv4 (default), or DualStack (both IPv4 and IPv6) Optional.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Public network access to the cluster is enabled by default. When disabled, only private endpoint connection to the cluster is allowed Optional.
    </ResponseField>
    <ResponseField name="restrict_outbound_network_access" type="string">
      Whether or not to restrict outbound network access. Value is optional but if passed in, must be 'Enabled' or 'Disabled' Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      The state of the resource. Optional.
    </ResponseField>
    <ResponseField name="state_reason" type="string">
      The reason for the cluster's current state. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="trusted_external_tenants" type="list(object)">
      The cluster's external tenants. Optional.
      <Expandable title="properties">
        <ResponseField name="value" type="string">
          The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="uri" type="string">
      The cluster URI. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="virtual_cluster_graduation_properties" type="string">
      Virtual Cluster graduation properties Optional.
    </ResponseField>
    <ResponseField name="virtual_network_configuration" type="object">
      A class that contains virtual network definition. Optional.
      <Expandable title="properties">
        <ResponseField name="data_management_public_ip_id" type="string" required>
          Data management's service public IP address resource id. Required.
        </ResponseField>
        <ResponseField name="engine_public_ip_id" type="string" required>
          Engine service's public IP address resource id. Required.
        </ResponseField>
        <ResponseField name="state" type="string">
          When enabled, the cluster is deployed into the configured subnet, when disabled it will be removed from the subnet. Optional.
        </ResponseField>
        <ResponseField name="subnet_id" type="string" required>
          The subnet resource id. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="zone_status" type="string">
      Indicates whether the cluster is zonal or non-zonal. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object" required>
  Azure SKU definition. Required.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      The number of instances of the cluster. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the kusto cluster's SKU. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tier" type="string" required>
      The tier for the kusto cluster's SKU. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The availability zones. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
