---
title: "azure_kusto_managed_private_endpoint"
description: "`azure_azure_kusto_kusto_managed_private_endpoint` creates a Private Endpoint from an Azure Data Explorer cluster to an external resource..."
---

`azure_azure_kusto_kusto_managed_private_endpoint` creates a Private Endpoint from an Azure Data Explorer cluster to an external resource, so outbound data ingestion or external table access can stay private.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_azure_kusto_kusto --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_azure_kusto_kusto/sdk/go
    package main

    import (
    	azure "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_azure_kusto_kusto/azure"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own kusto managed private endpoint"})

    		ubx.Resource(azure.KustoKustoManagedPrivateEndpoint, "example", azure.KustoKustoManagedPrivateEndpointConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_azure_kusto_kusto --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { KustoKustoManagedPrivateEndpoint } from "./local-sdk/azure_azure_kusto_kusto/sdk/typescript/azure_azure_kusto_kusto/azure/kusto_kusto_managed_private_endpoint.ts";

    export default stack("example", () => {
      intent({ summary: "example own kusto managed private endpoint" });

      resource(KustoKustoManagedPrivateEndpoint, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_azure_kusto_kusto --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_azure_kusto_kusto/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_azure_kusto_kusto.azure import KustoKustoManagedPrivateEndpoint, KustoKustoManagedPrivateEndpointConfig

    def describe():
        ubx.intent("example own kusto managed private endpoint")

        ubx.resource(KustoKustoManagedPrivateEndpoint, "example", KustoKustoManagedPrivateEndpointConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_kusto_managed_private_endpoint, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  A class representing the properties of a managed private endpoint object. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="group_id" type="string" required>
      The groupId in which the managed private endpoint is created. Required.
    </ResponseField>
    <ResponseField name="private_link_resource_id" type="string" required>
      The ARM resource ID of the resource for which the managed private endpoint is created. Required.
    </ResponseField>
    <ResponseField name="private_link_resource_region" type="string">
      The region of the resource to which the managed private endpoint is created. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioned state of the resource. Optional.
    </ResponseField>
    <ResponseField name="request_message" type="string">
      The user request message. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
