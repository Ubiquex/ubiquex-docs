---
title: "azure_kusto_cluster_principal_assignment"
description: "`azure_kusto_cluster_principal_assignment` grants a principal a cluster-wide administrative role on an Azure Data Explorer cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kusto "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/kusto"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own kusto cluster principal assignment"})

    		ubx.Resource(kusto.ClusterPrincipalAssignment, "example", kusto.ClusterPrincipalAssignmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClusterPrincipalAssignment } from "@ubx/sdk-azure/azure/kusto/cluster_principal_assignment";

    export default stack("example", () => {
      intent({ summary: "example own kusto cluster principal assignment" });

      resource(ClusterPrincipalAssignment, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.kusto.cluster_principal_assignment import ClusterPrincipalAssignment, ClusterPrincipalAssignmentConfig

    def describe():
        ubx.intent("example own kusto cluster principal assignment")

        ubx.resource(ClusterPrincipalAssignment, "example", ClusterPrincipalAssignmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_kusto_cluster_principal_assignment, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  A class representing cluster principal property. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="aad_object_id" type="string">
      The service principal object id in AAD (Azure active directory) Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="principal_id" type="string" required>
      The principal ID assigned to the cluster principal. It can be a user email, application ID, or security group name. Required.
    </ResponseField>
    <ResponseField name="principal_name" type="string">
      The principal name Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="principal_type" type="string" required>
      Principal type. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioned state of the resource. Optional.
    </ResponseField>
    <ResponseField name="role" type="string" required>
      Cluster principal role. Required.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id of the principal Optional.
    </ResponseField>
    <ResponseField name="tenant_name" type="string">
      The tenant name of the principal Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
