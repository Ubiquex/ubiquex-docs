---
title: "azure_kusto_database_principal_assignment"
description: "`azure_azure_kusto_kusto_database_principal_assignment` grants a principal a role (admin, ingestor..."
---

`azure_azure_kusto_kusto_database_principal_assignment` grants a principal a role (admin, ingestor, viewer) scoped to a single Azure Data Explorer database.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_azure_kusto_kusto --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_azure_kusto_kusto/sdk/go
    package main

    import (
    	azure "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_azure_kusto_kusto/azure"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own kusto database principal assignment"})

    		ubx.Resource(azure.KustoKustoDatabasePrincipalAssignment, "ci-runner", azure.KustoKustoDatabasePrincipalAssignmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_azure_kusto_kusto --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { KustoKustoDatabasePrincipalAssignment } from "./local-sdk/azure_azure_kusto_kusto/sdk/typescript/azure_azure_kusto_kusto/azure/kusto_kusto_database_principal_assignment.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own kusto database principal assignment" });

      resource(KustoKustoDatabasePrincipalAssignment, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_azure_kusto_kusto --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_azure_kusto_kusto/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_azure_kusto_kusto.azure import KustoKustoDatabasePrincipalAssignment, KustoKustoDatabasePrincipalAssignmentConfig

    def describe():
        ubx.intent("payments own kusto database principal assignment")

        ubx.resource(KustoKustoDatabasePrincipalAssignment, "ci-runner", KustoKustoDatabasePrincipalAssignmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_kusto_database_principal_assignment, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  A class representing database principal property. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="aad_object_id" type="string">
      The service principal object id in AAD (Azure active directory) Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="principal_id" type="string" required>
      The principal ID assigned to the database principal. It can be a user email, application ID, or security group name. Required.
    </ResponseField>
    <ResponseField name="principal_name" type="string">
      The principal name Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="principal_type" type="string" required>
      Principal type. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioned state of the resource. Optional.
    </ResponseField>
    <ResponseField name="role" type="string" required>
      Database principal role. Required.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id of the principal Optional.
    </ResponseField>
    <ResponseField name="tenant_name" type="string">
      The tenant name of the principal Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
