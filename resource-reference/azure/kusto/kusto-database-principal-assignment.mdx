---
title: "azure_kusto_database_principal_assignment"
description: "`azure_kusto_database_principal_assignment` grants a principal a role (admin, ingestor..."
---

`azure_kusto_database_principal_assignment` grants a principal a role (admin, ingestor, viewer) scoped to a single Azure Data Explorer database.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kusto "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/kusto"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own kusto database principal assignment"})

    		ubx.Resource(kusto.DatabasePrincipalAssignment, "example", kusto.DatabasePrincipalAssignmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatabasePrincipalAssignment } from "@ubx/sdk-azure/azure/kusto/database_principal_assignment";

    export default stack("example", () => {
      intent({ summary: "example own kusto database principal assignment" });

      resource(DatabasePrincipalAssignment, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.kusto.database_principal_assignment import DatabasePrincipalAssignment, DatabasePrincipalAssignmentConfig

    def describe():
        ubx.intent("example own kusto database principal assignment")

        ubx.resource(DatabasePrincipalAssignment, "example", DatabasePrincipalAssignmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_kusto_database_principal_assignment, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  A class representing database principal property. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="aad_object_id" type="string">
      The service principal object id in AAD (Azure active directory) Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="principal_id" type="string" required>
      The principal ID assigned to the database principal. It can be a user email, application ID, or security group name. Required.
    </ResponseField>
    <ResponseField name="principal_name" type="string">
      The principal name Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="principal_type" type="string" required>
      Principal type. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioned state of the resource. Optional.
    </ResponseField>
    <ResponseField name="role" type="string" required>
      Database principal role. Required.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id of the principal Optional.
    </ResponseField>
    <ResponseField name="tenant_name" type="string">
      The tenant name of the principal Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
