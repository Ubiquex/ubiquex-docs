---
title: "azure_cognitiveservices_rai_blocklist"
description: "`azure_cognitiveservices_rai_blocklist` defines a custom list of blocked term(s) or phrase(s) enforced by a Responsible AI content..."
---

`azure_cognitiveservices_rai_blocklist` defines a custom list of blocked term(s) or phrase(s) enforced by a Responsible AI content filter policy on Azure AI services model deployment(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices rai blocklist"})

    		ubx.Resource(cognitiveservices.RaiBlocklist, "example", cognitiveservices.RaiBlocklistConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RaiBlocklist } from "@ubx/sdk-azure/azure/cognitiveservices/rai_blocklist";

    export default stack("example", () => {
      intent({ summary: "example own cognitiveservices rai blocklist" });

      resource(RaiBlocklist, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cognitiveservices.rai_blocklist import RaiBlocklist, RaiBlocklistConfig

    def describe():
        ubx.intent("example own cognitiveservices rai blocklist")

        ubx.resource(RaiBlocklist, "example", RaiBlocklistConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_rai_blocklist, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Resource Etag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  RAI Custom Blocklist properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Description of the block list. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
