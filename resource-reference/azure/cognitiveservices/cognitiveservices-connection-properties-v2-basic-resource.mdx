---
title: "azure_cognitiveservices_connection_properties_v2_basic_resource"
description: "`azure_cognitiveservices_connection_properties_v2_basic_resource` defines a connection from an Azure AI Foundry account to..."
---

`azure_cognitiveservices_connection_properties_v2_basic_resource` defines a connection from an Azure AI Foundry account to an external resource (a data source, an API key-authenticated service).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices connection properties v2 basic resource"})

    		ubx.Resource(cognitiveservices.ConnectionPropertiesV2BasicResource, "example", cognitiveservices.ConnectionPropertiesV2BasicResourceConfig{
    			Properties: map[string]any{"auth_type": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectionPropertiesV2BasicResource } from "@ubx/sdk-azure/azure/cognitiveservices/connection_properties_v2_basic_resource";

    export default stack("example", () => {
      intent({
        summary:
          "example own cognitiveservices connection properties v2 basic resource",
      });

      resource(ConnectionPropertiesV2BasicResource, "example", {
        properties: { authType: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cognitiveservices.connection_properties_v2_basic_resource import ConnectionPropertiesV2BasicResource, ConnectionPropertiesV2BasicResourceConfig

    def describe():
        ubx.intent("example own cognitiveservices connection properties v2 basic resource")

        ubx.resource(ConnectionPropertiesV2BasicResource, "example", ConnectionPropertiesV2BasicResourceConfig(
            properties={"auth_type": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_cognitiveservices_connection_properties_v2_basic_resource,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  Connection property base schema. Required.
  <Expandable title="properties">
    <ResponseField name="auth_type" type="string" required>
      Authentication type of the connection target Required.
    </ResponseField>
    <ResponseField name="category" type="string">
      Category of the connection Optional.
    </ResponseField>
    <ResponseField name="created_by_workspace_arm_id" type="string">
      A type definition that refers the id to an Azure Resource Manager resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="error" type="string">
      Provides the error message if the connection fails Optional.
    </ResponseField>
    <ResponseField name="expiry_time" type="string">
      When this resource or grant expires. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="group" type="string">
      Group based on connection category Optional.
    </ResponseField>
    <ResponseField name="is_shared_to_all" type="bool">
      Whether this resource is shared with every project, rather than a specific subset. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="metadata" type="map(string)">
      Store user metadata for this connection Optional.
    </ResponseField>
    <ResponseField name="pe_requirement" type="string">
      Whether a private endpoint connection is required, optional, or not applicable for this resource. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pe_status" type="string">
      The current status of this resource's own private endpoint connection(s). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="shared_user_list" type="list(string)">
      The user(s) this resource is shared with. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target" type="string">
      The connection URL to be used. Optional.
    </ResponseField>
    <ResponseField name="use_workspace_managed_identity" type="bool">
      Whether the workspace's own managed identity is used to authenticate, rather than a caller-supplied credential. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
