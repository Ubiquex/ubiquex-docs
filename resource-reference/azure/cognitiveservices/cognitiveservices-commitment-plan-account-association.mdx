---
title: "azure_cognitiveservices_commitment_plan_account_association"
description: "`azure_cognitiveservices_commitment_plan_account_association` attaches an Azure AI services account to a commitment plan..."
---

`azure_cognitiveservices_commitment_plan_account_association` attaches an Azure AI services account to a commitment plan, applying the plan's own discounted pricing to that account's usage.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices commitment plan account association"})

    		ubx.Resource(cognitiveservices.CommitmentPlanAccountAssociation, "example", cognitiveservices.CommitmentPlanAccountAssociationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CommitmentPlanAccountAssociation } from "@ubx/sdk-azure/azure/cognitiveservices/commitment_plan_account_association";

    export default stack("example", () => {
      intent({
        summary:
          "example own cognitiveservices commitment plan account association",
      });

      resource(CommitmentPlanAccountAssociation, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cognitiveservices.commitment_plan_account_association import CommitmentPlanAccountAssociation, CommitmentPlanAccountAssociationConfig

    def describe():
        ubx.intent("example own cognitiveservices commitment plan account association")

        ubx.resource(CommitmentPlanAccountAssociation, "example", CommitmentPlanAccountAssociationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_cognitiveservices_commitment_plan_account_association, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Resource Etag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The commitment plan account association properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      The Azure resource id of the account. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
