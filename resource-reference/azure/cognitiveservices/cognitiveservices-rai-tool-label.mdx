---
title: "azure_cognitiveservices_rai_tool_label"
description: "`azure_cognitiveservices_rai_tool_label` classifies a specific tool an AI agent can call under a Responsible AI risk category..."
---

`azure_cognitiveservices_rai_tool_label` classifies a specific tool an AI agent can call under a Responsible AI risk category, so Foundry Guardrails can apply appropriate policy when that tool is invoked.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices rai tool label"})

    		ubx.Resource(cognitiveservices.RaiToolLabel, "example", cognitiveservices.RaiToolLabelConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RaiToolLabel } from "@ubx/sdk-azure/azure/cognitiveservices/rai_tool_label";

    export default stack("example", () => {
      intent({ summary: "example own cognitiveservices rai tool label" });

      resource(RaiToolLabel, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cognitiveservices.rai_tool_label import RaiToolLabel, RaiToolLabelConfig

    def describe():
        ubx.intent("example own cognitiveservices rai tool label")

        ubx.resource(RaiToolLabel, "example", RaiToolLabelConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_rai_tool_label, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Resource Etag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  RAI Tool Label properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="account_scope" type="object">
      Account-level tool label definition. Optional.
      <Expandable title="properties">
        <ResponseField name="label_values" type="map(string)">
          Dictionary of label key-value pairs for the account scope. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="project_scopes" type="list(object)">
      List of project-level tool label definitions. Optional.
      <Expandable title="properties">
        <ResponseField name="label_values" type="map(string)">
          The label value(s) assigned during data collection. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="project" type="string">
          A reference to the AI Foundry project this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tool_connection_name" type="string" required>
      The unique tool connection name, e.g., 'Web_Search'. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
