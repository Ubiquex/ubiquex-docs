---
title: "azure_cognitiveservices_account"
description: "`azure_cognitiveservices_cognitiveservices_account` provisions an Azure AI services (Cognitive Services) account -- or an Azure AI Foundry (Microsoft..."
---

`azure_cognitiveservices_cognitiveservices_account` provisions an Azure AI services (Cognitive Services) account -- or an Azure AI Foundry (Microsoft Foundry) resource -- the top-level container that model deployment(s) and API access are billed and managed under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cognitiveservices_cognitiveservices/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices account"})

    		ubx.Resource(cognitiveservices.CognitiveservicesAccount, "example", cognitiveservices.CognitiveservicesAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CognitiveservicesAccount } from "./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/typescript/azure_cognitiveservices_cognitiveservices/cognitiveservices/cognitiveservices_account.ts";

    export default stack("example", () => {
      intent({ summary: "example own cognitiveservices account" });

      resource(CognitiveservicesAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cognitiveservices_cognitiveservices.cognitiveservices import CognitiveservicesAccount, CognitiveservicesAccountConfig

    def describe():
        ubx.intent("example own cognitiveservices account")

        ubx.resource(CognitiveservicesAccount, "example", CognitiveservicesAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_account, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A value used for optimistic concurrency control on this cognitiveservices account, changed by the server on every update. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="identity" type="object">
  Identity for the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal ID of resource identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      The identity type. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user assigned identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125; Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  The kind (type) of cognitive service account. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of Cognitive Services account. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="abuse_penalty" type="object">
      The abuse penalty. Optional.
      <Expandable title="properties">
        <ResponseField name="action" type="string">
          The action of AbusePenalty. Optional.
        </ResponseField>
        <ResponseField name="expiration" type="string">
          The datetime of expiration of the AbusePenalty. Optional.
        </ResponseField>
        <ResponseField name="rate_limit_percentage" type="number">
          The percentage of rate limit. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="allow_project_management" type="bool">
      Specifies whether this resource support project management as child resources, used as containers for access management, data isolation and cost in AI Foundry. Optional.
    </ResponseField>
    <ResponseField name="allowed_fqdn_list" type="list(string)">
      The fully qualified domain name(s) outbound traffic is permitted to reach. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="aml_workspace" type="object">
      The user owned AML account for Cognitive Services account. Optional.
      <Expandable title="properties">
        <ResponseField name="identity_client_id" type="string">
          Identity Client id of a AML account resource. Optional.
        </ResponseField>
        <ResponseField name="resource_id" type="string">
          Full resource id of a AML account resource. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="api_properties" type="object">
      The api properties for special APIs. Optional.
      <Expandable title="properties">
        <ResponseField name="aad_client_id" type="string">
          (Metrics Advisor Only) The Azure AD Client Id (Application Id). Optional.
        </ResponseField>
        <ResponseField name="aad_tenant_id" type="string">
          (Metrics Advisor Only) The Azure AD Tenant Id. Optional.
        </ResponseField>
        <ResponseField name="event_hub_connection_string" type="string">
          (Personalization Only) The flag to enable statistics of Bing Search. Optional.
        </ResponseField>
        <ResponseField name="qna_azure_search_endpoint_id" type="string">
          (QnAMaker Only) The Azure Search endpoint id of QnAMaker. Optional.
        </ResponseField>
        <ResponseField name="qna_azure_search_endpoint_key" type="string">
          (QnAMaker Only) The Azure Search endpoint key of QnAMaker. Optional.
        </ResponseField>
        <ResponseField name="qna_runtime_endpoint" type="string">
          (QnAMaker Only) The runtime endpoint of QnAMaker. Optional.
        </ResponseField>
        <ResponseField name="statistics_enabled" type="bool">
          (Bing Search Only) The flag to enable statistics of Bing Search. Optional.
        </ResponseField>
        <ResponseField name="storage_account_connection_string" type="string">
          (Personalization Only) The storage account connection string. Optional.
        </ResponseField>
        <ResponseField name="super_user" type="string">
          (Metrics Advisor Only) The super user of Metrics Advisor. Optional.
        </ResponseField>
        <ResponseField name="website_name" type="string">
          (Metrics Advisor Only) The website name of Metrics Advisor. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="associated_projects" type="list(string)">
      Specifies the projects, by project name, that are associated with this resource. Optional.
    </ResponseField>
    <ResponseField name="call_rate_limit" type="object">
      The call rate limit Cognitive Services account. Optional.
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          The count value of Call Rate Limit. Optional.
        </ResponseField>
        <ResponseField name="renewal_period" type="number">
          The renewal period in seconds of Call Rate Limit. Optional.
        </ResponseField>
        <ResponseField name="rules" type="list(object)">
          The rule(s) making up this policy. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              How many of this item there are. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="dynamic_throttling_enabled" type="bool">
              Whether request throttling is dynamically adjusted based on observed load. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key" type="string">
              The key of this attribute or label. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="match_patterns" type="list(object)">
              The pattern(s) a matching request must satisfy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="method" type="string">
                  The HTTP method this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path" type="string">
                  A file or resource path. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="min_count" type="number">
              The minimum count required for this condition to be satisfied. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="renewal_period" type="number">
              How often this quota or commitment period automatically renews. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="capabilities" type="list(object)">
      Gets the capabilities of the cognitive services account. Each item indicates the capability of a specific feature. The values are read-only and for reference only. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="commitment_plan_associations" type="list(object)">
      The commitment plan associations of Cognitive Services account. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="commitment_plan_id" type="string">
          A reference to the commitment plan this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="commitment_plan_location" type="string">
          The Azure region the associated commitment plan is deployed in. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_sub_domain_name" type="string">
      Optional subdomain name used for token-based authentication. Optional.
    </ResponseField>
    <ResponseField name="date_created" type="string">
      Gets the date of cognitive services account creation. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="default_project" type="string">
      Specifies the project, by project name, that is targeted when data plane endpoints are called without a project parameter. Optional.
    </ResponseField>
    <ResponseField name="deletion_date" type="string">
      The deletion date, only available for deleted account. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="disable_local_auth" type="bool">
      Whether authentication via API key is disabled, requiring Microsoft Entra ID authentication instead. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="dynamic_throttling_enabled" type="bool">
      The flag to enable dynamic throttling. Optional.
    </ResponseField>
    <ResponseField name="encryption" type="object">
      Properties to configure Encryption Optional.
      <Expandable title="properties">
        <ResponseField name="key_source" type="string">
          Enumerates the possible value of keySource for Encryption Optional.
        </ResponseField>
        <ResponseField name="key_vault_properties" type="object">
          Properties to configure keyVault Properties Optional.
          <Expandable title="properties">
            <ResponseField name="identity_client_id" type="string">
              The client ID of the managed identity used. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key_name" type="string">
              Name of the Key from KeyVault Optional.
            </ResponseField>
            <ResponseField name="key_vault_uri" type="string">
              Uri of KeyVault Optional.
            </ResponseField>
            <ResponseField name="key_version" type="string">
              Version of the Key from KeyVault Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="endpoint" type="string">
      Endpoint of the created account. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="endpoints" type="map(string)">
      Dictionary of &lt;string&gt; Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="internal_id" type="string">
      The internal identifier (deprecated, do not use this property). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="is_migrated" type="bool">
      If the resource is migrated from an existing key. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="locations" type="object">
      The multiregion settings Cognitive Services account. Optional.
      <Expandable title="properties">
        <ResponseField name="regions" type="list(object)">
          The Azure region(s) this applies to. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="customsubdomain" type="string">
              The custom subdomain name assigned to this resource, used for token-based authentication. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="number">
              The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="routing_method" type="string">
          Multiregion routing methods. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="migration_token" type="string">
      Resource migration token. Optional.
    </ResponseField>
    <ResponseField name="network_acls" type="object">
      A set of rules governing the network accessibility. Optional.
      <Expandable title="properties">
        <ResponseField name="bypass" type="string">
          Setting for trusted services. Optional.
        </ResponseField>
        <ResponseField name="default_action" type="string">
          The default action when no rule from ipRules and from virtualNetworkRules match. This is only used after the bypass property has been evaluated. Optional.
        </ResponseField>
        <ResponseField name="ip_rules" type="list(object)">
          The list of IP address rules. Optional.
          <Expandable title="properties">
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="virtual_network_rules" type="list(object)">
          The list of virtual network rules. Optional.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ignore_missing_vnet_service_endpoint" type="bool">
              Whether this rule is accepted even if the referenced subnet doesn't yet have the required service endpoint enabled. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              The current status of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_injections" type="list(object)">
      Configuration for injecting this resource's own compute into a customer virtual network. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="scenario" type="string">
          The usage scenario this configuration applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnet_arm_id" type="string">
          A reference to the subnet this resource is injected into. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="use_microsoft_managed_network" type="bool">
          Whether the Microsoft-managed network is used, rather than a customer-managed one. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      The private endpoint connection associated with the Cognitive Services account. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="location" type="string">
          The Azure region this resource is deployed to, e.g. `eastus`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Gets the status of the cognitive services account at the time the operation was called. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Whether or not public endpoint access is allowed for this account. Optional.
    </ResponseField>
    <ResponseField name="quota_limit" type="object">
      The maximum usage allowed within this quota's own period. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          How many of this item there are. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="renewal_period" type="number">
          How often this quota or commitment period automatically renews. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rules" type="list(object)">
          The rule(s) making up this policy. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              How many of this item there are. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="dynamic_throttling_enabled" type="bool">
              Whether request throttling is dynamically adjusted based on observed load. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key" type="string">
              The key of this attribute or label. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="match_patterns" type="list(object)">
              The pattern(s) a matching request must satisfy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="method" type="string">
                  The HTTP method this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path" type="string">
                  A file or resource path. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="min_count" type="number">
              The minimum count required for this condition to be satisfied. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="renewal_period" type="number">
              How often this quota or commitment period automatically renews. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="rai_monitor_config" type="object">
      Cognitive Services Rai Monitor Config. Optional.
      <Expandable title="properties">
        <ResponseField name="adx_storage_resource_id" type="string">
          The storage resource Id. Optional.
        </ResponseField>
        <ResponseField name="identity_client_id" type="string">
          The identity client Id to access the storage. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="restore" type="bool">
      Whether this soft-deleted resource is being restored, rather than newly created. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="restrict_outbound_network_access" type="bool">
      Whether outbound network access from this resource is restricted to explicitly allowed destinations. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="scheduled_purge_date" type="string">
      The scheduled purge date, only available for deleted account. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="sku_change_info" type="object">
      Sku change info of account. Optional.
      <Expandable title="properties">
        <ResponseField name="count_of_downgrades" type="number">
          Gets the count of downgrades. Optional.
        </ResponseField>
        <ResponseField name="count_of_upgrades_after_downgrades" type="number">
          Gets the count of upgrades after downgrades. Optional.
        </ResponseField>
        <ResponseField name="last_change_date" type="string">
          Gets the last change date. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stored_completions_disabled" type="bool">
      The flag to disable stored completions. Optional.
    </ResponseField>
    <ResponseField name="user_owned_storage" type="list(object)">
      The storage accounts for this resource. Optional.
      <Expandable title="properties">
        <ResponseField name="identity_client_id" type="string">
          The client ID of the managed identity used. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_id" type="string">
          A reference to the resource this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The resource model definition representing SKU Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU. Ex - P3. It is typically a letter+number code Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      This field is required to be implemented by the Resource Provider if the service has more than one tier, but is not required on a PUT. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags to apply to this cognitiveservices account. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
