---
title: "azure_cognitiveservices_deployment"
description: "`azure_cognitiveservices_deployment` is a specific model deployment (e.g. a GPT or embedding model) on an Azure AI services / Azure..."
---

`azure_cognitiveservices_deployment` is a specific model deployment (e.g. a GPT or embedding model) on an Azure AI services / Azure OpenAI account, servable for inference calls.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cognitiveservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cognitiveservices/sdk/go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cognitiveservices/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices deployment"})

    		ubx.Resource(cognitiveservices.CognitiveservicesDeployment, "example", cognitiveservices.CognitiveservicesDeploymentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cognitiveservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CognitiveservicesDeployment } from "./local-sdk/azure_cognitiveservices/sdk/typescript/azure_cognitiveservices/cognitiveservices/cognitiveservices_deployment.ts";

    export default stack("example", () => {
      intent({ summary: "example own cognitiveservices deployment" });

      resource(CognitiveservicesDeployment, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cognitiveservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cognitiveservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cognitiveservices.cognitiveservices import CognitiveservicesDeployment, CognitiveservicesDeploymentConfig

    def describe():
        ubx.intent("example own cognitiveservices deployment")

        ubx.resource(CognitiveservicesDeployment, "example", CognitiveservicesDeploymentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_deployment, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Resource Etag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of Cognitive Services account deployment. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="call_rate_limit" type="object">
      The call rate limit Cognitive Services account. Optional.
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          The count value of Call Rate Limit. Optional.
        </ResponseField>
        <ResponseField name="renewal_period" type="number">
          The renewal period in seconds of Call Rate Limit. Optional.
        </ResponseField>
        <ResponseField name="rules" type="list(object)">
          The rule(s) making up this policy. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              How many of this item there are. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="dynamic_throttling_enabled" type="bool">
              Whether request throttling is dynamically adjusted based on observed load. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key" type="string">
              The key of this attribute or label. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="match_patterns" type="list(object)">
              The pattern(s) a matching request must satisfy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="method" type="string">
                  The HTTP method this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="path" type="string">
                  A file or resource path. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="min_count" type="number">
              The minimum count required for this condition to be satisfied. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="renewal_period" type="number">
              How often this quota or commitment period automatically renews. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="capabilities" type="map(string)">
      The capabilities. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="capacity_settings" type="object">
      Internal use only. Optional.
      <Expandable title="properties">
        <ResponseField name="designated_capacity" type="number">
          The designated capacity. Optional.
        </ResponseField>
        <ResponseField name="priority" type="number">
          The priority of this capacity setting. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="current_capacity" type="number">
      The current capacity. Optional.
    </ResponseField>
    <ResponseField name="deployment_state" type="string">
      The state of the deployment. Controls whether the deployment is accepting inference requests. Use 'Running' for active deployments that process requests, or 'Paused' to temporarily stop inference while preserving the deployment configuration. Optional.
    </ResponseField>
    <ResponseField name="dynamic_throttling_enabled" type="bool">
      If the dynamic throttling is enabled. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="model" type="object">
      Properties of Cognitive Services account deployment model. Optional.
      <Expandable title="properties">
        <ResponseField name="call_rate_limit" type="object">
          The call rate limit Cognitive Services account. Optional.
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              The count value of Call Rate Limit. Optional.
            </ResponseField>
            <ResponseField name="renewal_period" type="number">
              The renewal period in seconds of Call Rate Limit. Optional.
            </ResponseField>
            <ResponseField name="rules" type="list(object)">
              The rule(s) making up this policy. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="count" type="number">
                  How many of this item there are. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="dynamic_throttling_enabled" type="bool">
                  Whether request throttling is dynamically adjusted based on observed load. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key" type="string">
                  The key of this attribute or label. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="match_patterns" type="list(object)">
                  The pattern(s) a matching request must satisfy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="method" type="string">
                      The HTTP method this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path" type="string">
                      A file or resource path. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="min_count" type="number">
                  The minimum count required for this condition to be satisfied. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="renewal_period" type="number">
                  How often this quota or commitment period automatically renews. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="format" type="string">
          Deployment model format. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Deployment model name. Optional.
        </ResponseField>
        <ResponseField name="publisher" type="string">
          Deployment model publisher. Optional.
        </ResponseField>
        <ResponseField name="source" type="string">
          Optional. Deployment model source ARM resource ID. Optional.
        </ResponseField>
        <ResponseField name="source_account" type="string">
          Optional. Source of the model, another Microsoft.CognitiveServices accounts ARM resource ID. Optional.
        </ResponseField>
        <ResponseField name="version" type="string">
          Optional. Deployment model version. If version is not specified, a default version will be assigned. The default version is different for different models and might change when there is new version available for a model. Default version for a model could be found from list models API. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="parent_deployment_name" type="string">
      The name of parent deployment. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Gets the status of the resource at the time the operation was called. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="rai_policy_name" type="string">
      The name of RAI policy. Optional.
    </ResponseField>
    <ResponseField name="rate_limits" type="list(object)">
      The rate limit(s) applied to this deployment or resource. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          How many of this item there are. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dynamic_throttling_enabled" type="bool">
          Whether request throttling is dynamically adjusted based on observed load. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key" type="string">
          The key of this attribute or label. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="match_patterns" type="list(object)">
          The pattern(s) a matching request must satisfy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="method" type="string">
              The HTTP method this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="path" type="string">
              A file or resource path. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="min_count" type="number">
          The minimum count required for this condition to be satisfied. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="renewal_period" type="number">
          How often this quota or commitment period automatically renews. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="routing" type="object">
      Routing configuration for the model-router deployment. Specifies how requests are routed across multiple models. Optional.
      <Expandable title="properties">
        <ResponseField name="mode" type="string">
          The model-router routing mode that determines how requests are distributed across models. Optional.
        </ResponseField>
        <ResponseField name="models" type="list(object)">
          Optional. The list of model-router supported models that the model router can use to route requests across. If not specified, the model router will route to all available models specified in the model-router version. Optional.
          <Expandable title="properties">
            <ResponseField name="call_rate_limit" type="object">
              The maximum call rate permitted for this deployment. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="count" type="number">
                  How many of this item there are. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="renewal_period" type="number">
                  How often this quota or commitment period automatically renews. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rules" type="list(object)">
                  The rule(s) making up this policy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="count" type="number">
                      How many of this item there are. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="dynamic_throttling_enabled" type="bool">
                      Whether request throttling is dynamically adjusted based on observed load. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key" type="string">
                      The key of this attribute or label. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="match_patterns" type="list(object)">
                      The pattern(s) a matching request must satisfy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="method" type="string">
                          The HTTP method this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="path" type="string">
                          A file or resource path. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="min_count" type="number">
                      The minimum count required for this condition to be satisfied. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="renewal_period" type="number">
                      How often this quota or commitment period automatically renews. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="format" type="string">
              The format this data is expressed in. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="publisher" type="string">
              The publisher of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source" type="string">
              Where this data or configuration is sourced from. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_account" type="string">
              A reference to the source account this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="version" type="string">
              The version identifier this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scale_settings" type="object">
      Properties of Cognitive Services account deployment model. (Deprecated, please use Deployment.sku instead.) Optional.
      <Expandable title="properties">
        <ResponseField name="active_capacity" type="number">
          Deployment active capacity. This value might be different from `capacity` if customer recently updated `capacity`. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="capacity" type="number">
          Deployment capacity. Optional.
        </ResponseField>
        <ResponseField name="scale_type" type="string">
          Deployment scale type. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_tier" type="string">
      The service tier for the deployment. Determines the pricing and performance level for request processing. Use 'Default' for standard pricing or 'Priority' for higher-priority processing with premium pricing. Note: Pause operations are only supported on Standard, DataZoneStandard, and GlobalStandard SKUs. Optional.
    </ResponseField>
    <ResponseField name="spillover_deployment_name" type="string">
      Specifies the deployment name that should serve requests when the request would have otherwise been throttled due to reaching current deployment throughput limit. Optional.
    </ResponseField>
    <ResponseField name="version_upgrade_option" type="string">
      Deployment model version upgrade option. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The resource model definition representing SKU Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU. Ex - P3. It is typically a letter+number code Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      This field is required to be implemented by the Resource Provider if the service has more than one tier, but is not required on a PUT. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
