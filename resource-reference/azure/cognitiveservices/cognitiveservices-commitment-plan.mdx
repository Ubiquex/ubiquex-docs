---
title: "azure_cognitiveservices_commitment_plan"
description: "`azure_cognitiveservices_cognitiveservices_commitment_plan` purchases a fixed monthly commitment tier for an Azure AI services feature (e.g. Translator..."
---

`azure_cognitiveservices_cognitiveservices_commitment_plan` purchases a fixed monthly commitment tier for an Azure AI services feature (e.g. Translator or Speech), at a discount versus pay-as-you-go pricing.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cognitiveservices_cognitiveservices/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cognitiveservices commitment plan"})

    		ubx.Resource(cognitiveservices.CognitiveservicesCommitmentPlan, "ci-runner", cognitiveservices.CognitiveservicesCommitmentPlanConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CognitiveservicesCommitmentPlan } from "./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/typescript/azure_cognitiveservices_cognitiveservices/cognitiveservices/cognitiveservices_commitment_plan.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own cognitiveservices commitment plan" });

      resource(CognitiveservicesCommitmentPlan, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cognitiveservices_cognitiveservices.cognitiveservices import CognitiveservicesCommitmentPlan, CognitiveservicesCommitmentPlanConfig

    def describe():
        ubx.intent("payments own cognitiveservices commitment plan")

        ubx.resource(CognitiveservicesCommitmentPlan, "ci-runner", CognitiveservicesCommitmentPlanConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_commitment_plan, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Resource Etag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="kind" type="string">
  The kind (type) of cognitive service account. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of Cognitive Services account commitment plan. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="auto_renew" type="bool">
      AutoRenew commitment plan. Optional.
    </ResponseField>
    <ResponseField name="commitment_plan_guid" type="string">
      Commitment plan guid. Optional.
    </ResponseField>
    <ResponseField name="current" type="object">
      Cognitive Services account commitment period. Optional.
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          Commitment period commitment count. Optional.
        </ResponseField>
        <ResponseField name="end_date" type="string">
          Commitment period end date. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="quota" type="object">
          Cognitive Services account commitment quota. Optional.
          <Expandable title="properties">
            <ResponseField name="quantity" type="number">
              Commitment quota quantity. Optional.
            </ResponseField>
            <ResponseField name="unit" type="string">
              Commitment quota unit. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="start_date" type="string">
          Commitment period start date. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="tier" type="string">
          Commitment period commitment tier. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hosting_model" type="string">
      Account hosting model. Optional.
    </ResponseField>
    <ResponseField name="last" type="object">
      Cognitive Services account commitment period. Optional.
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          Commitment period commitment count. Optional.
        </ResponseField>
        <ResponseField name="end_date" type="string">
          Commitment period end date. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="quota" type="object">
          Cognitive Services account commitment quota. Optional.
          <Expandable title="properties">
            <ResponseField name="quantity" type="number">
              Commitment quota quantity. Optional.
            </ResponseField>
            <ResponseField name="unit" type="string">
              Commitment quota unit. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="start_date" type="string">
          Commitment period start date. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="tier" type="string">
          Commitment period commitment tier. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="next" type="object">
      Cognitive Services account commitment period. Optional.
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          Commitment period commitment count. Optional.
        </ResponseField>
        <ResponseField name="end_date" type="string">
          Commitment period end date. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="quota" type="object">
          Cognitive Services account commitment quota. Optional.
          <Expandable title="properties">
            <ResponseField name="quantity" type="number">
              Commitment quota quantity. Optional.
            </ResponseField>
            <ResponseField name="unit" type="string">
              Commitment quota unit. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="start_date" type="string">
          Commitment period start date. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="tier" type="string">
          Commitment period commitment tier. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="plan_type" type="string">
      Commitment plan type. Optional.
    </ResponseField>
    <ResponseField name="provisioning_issues" type="list(string)">
      The list of ProvisioningIssue. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Gets the status of the resource at the time the operation was called. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The resource model definition representing SKU Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU. Ex - P3. It is typically a letter+number code Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      This field is required to be implemented by the Resource Provider if the service has more than one tier, but is not required on a PUT. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
