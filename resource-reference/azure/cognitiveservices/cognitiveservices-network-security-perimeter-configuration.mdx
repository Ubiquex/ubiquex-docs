---
title: "azure_cognitiveservices_network_security_perimeter_configuration"
description: "`azure_cognitiveservices_network_security_perimeter_configuration` associates an Azure AI services account with a network security..."
---

`azure_cognitiveservices_network_security_perimeter_configuration` associates an Azure AI services account with a network security perimeter, restricting its own network access to the perimeter's own boundary rules.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cognitiveservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cognitiveservices/sdk/go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cognitiveservices/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices network security perimeter configuration"})

    		ubx.Resource(cognitiveservices.CognitiveservicesNetworkSecurityPerimeterConfiguration, "example", cognitiveservices.CognitiveservicesNetworkSecurityPerimeterConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cognitiveservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CognitiveservicesNetworkSecurityPerimeterConfiguration } from "./local-sdk/azure_cognitiveservices/sdk/typescript/azure_cognitiveservices/cognitiveservices/cognitiveservices_network_security_perimeter_configuration.ts";

    export default stack("example", () => {
      intent({
        summary:
          "example own cognitiveservices network security perimeter configuration",
      });

      resource(
        CognitiveservicesNetworkSecurityPerimeterConfiguration,
        "example",
        {},
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cognitiveservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cognitiveservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cognitiveservices.cognitiveservices import CognitiveservicesNetworkSecurityPerimeterConfiguration, CognitiveservicesNetworkSecurityPerimeterConfigurationConfig

    def describe():
        ubx.intent("example own cognitiveservices network security perimeter configuration")

        ubx.resource(CognitiveservicesNetworkSecurityPerimeterConfiguration, "example", CognitiveservicesNetworkSecurityPerimeterConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_cognitiveservices_network_security_perimeter_configuration,
    with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="properties" type="object">
  The properties of an NSP Configuration. Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="network_security_perimeter" type="object">
      Information about a linked Network Security Perimeter Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Fully qualified identifier of the resource Optional.
        </ResponseField>
        <ResponseField name="location" type="string">
          Location of the resource Optional.
        </ResponseField>
        <ResponseField name="perimeter_guid" type="string">
          Guid of the resource Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="profile" type="object">
      Network Security Perimeter Profile Information Optional.
      <Expandable title="properties">
        <ResponseField name="access_rules" type="list(object)">
          The network access rule(s) governing this resource. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="properties" type="object">
              The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="access_rules_version" type="number">
          Access rules version of the resource profile Optional.
        </ResponseField>
        <ResponseField name="diagnostic_settings_version" type="number">
          Current diagnostic settings version Optional.
        </ResponseField>
        <ResponseField name="enabled_log_categories" type="list(string)">
          List of enabled log categories Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the resource profile Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_issues" type="list(object)">
      List of Provisioning Issues Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of NetworkSecurityPerimeter configuration Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="resource_association" type="object">
      Network Security Perimeter Configuration Association Information Optional.
      <Expandable title="properties">
        <ResponseField name="access_mode" type="string">
          Access Mode of the resource association Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the resource association Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
