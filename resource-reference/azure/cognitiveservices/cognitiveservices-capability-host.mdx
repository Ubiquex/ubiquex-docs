---
title: "azure_cognitiveservices_capability_host"
description: "`azure_cognitiveservices_capability_host` provisions the compute and storage backing a specific AI capability (e.g. agent execution)..."
---

`azure_cognitiveservices_capability_host` provisions the compute and storage backing a specific AI capability (e.g. agent execution) within an Azure AI Foundry account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cognitiveservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cognitiveservices/sdk/go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cognitiveservices/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices capability host"})

    		ubx.Resource(cognitiveservices.CognitiveservicesCapabilityHost, "example", cognitiveservices.CognitiveservicesCapabilityHostConfig{
    			Properties: map[string]any{"ai_services_connections": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cognitiveservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CognitiveservicesCapabilityHost } from "./local-sdk/azure_cognitiveservices/sdk/typescript/azure_cognitiveservices/cognitiveservices/cognitiveservices_capability_host.ts";

    export default stack("example", () => {
      intent({ summary: "example own cognitiveservices capability host" });

      resource(CognitiveservicesCapabilityHost, "example", {
        properties: { aiServicesConnections: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cognitiveservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cognitiveservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cognitiveservices.cognitiveservices import CognitiveservicesCapabilityHost, CognitiveservicesCapabilityHostConfig

    def describe():
        ubx.intent("example own cognitiveservices capability host")

        ubx.resource(CognitiveservicesCapabilityHost, "example", CognitiveservicesCapabilityHostConfig(
            properties={"ai_services_connections": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_capability_host, with properties a
    set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="ai_services_connections" type="list(string)">
      List of AI services connections. Optional.
    </ResponseField>
    <ResponseField name="capability_host_kind" type="string">
      The category of capability host this represents. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="customer_subnet" type="string">
      Customer subnet info to help set up this capability host. Optional.
    </ResponseField>
    <ResponseField name="enable_public_hosting_environment" type="bool">
      Whether public hosting environment is enabled for the capability host Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of capability host. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="storage_connections" type="list(string)">
      List of connection names from those available in the account or project to be used as a storage resource. Optional.
    </ResponseField>
    <ResponseField name="thread_storage_connections" type="list(string)">
      List of connection names from those available in the account or project to be used for Thread storage. Optional.
    </ResponseField>
    <ResponseField name="vector_store_connections" type="list(string)">
      List of connection names from those available in the account or project to be used for vector database (e.g. CosmosDB). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
