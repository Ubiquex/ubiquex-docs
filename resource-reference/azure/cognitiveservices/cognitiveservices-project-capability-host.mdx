---
title: "azure_cognitiveservices_project_capability_host"
description: "`azure_cognitiveservices_cognitiveservices_project_capability_host` provisions the compute and storage backing a specific AI capability (e.g. agent..."
---

`azure_cognitiveservices_cognitiveservices_project_capability_host` provisions the compute and storage backing a specific AI capability (e.g. agent execution) scoped to a single Azure AI Foundry project, rather than the whole account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices project capability host"})

    		ubx.Resource(cognitiveservices.ProjectCapabilityHost, "example", cognitiveservices.ProjectCapabilityHostConfig{
    			Properties: map[string]any{"ai_services_connections": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProjectCapabilityHost } from "@ubx/sdk-azure/azure/cognitiveservices/project_capability_host";

    export default stack("example", () => {
      intent({ summary: "example own cognitiveservices project capability host" });

      resource(ProjectCapabilityHost, "example", {
        properties: { aiServicesConnections: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cognitiveservices import ProjectCapabilityHost, ProjectCapabilityHostConfig

    def describe():
        ubx.intent("example own cognitiveservices project capability host")

        ubx.resource(ProjectCapabilityHost, "example", ProjectCapabilityHostConfig(
            properties={"ai_services_connections": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_project_capability_host, with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="ai_services_connections" type="list(string)">
      List of AI services connections. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of capability host. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="storage_connections" type="list(string)">
      List of connection names from those available in the account or project to be used as a storage resource. Optional.
    </ResponseField>
    <ResponseField name="thread_storage_connections" type="list(string)">
      List of connection names from those available in the account or project to be used for Thread storage. Optional.
    </ResponseField>
    <ResponseField name="vector_store_connections" type="list(string)">
      List of connection names from those available in the account or project to be used for vector database (e.g. CosmosDB). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
