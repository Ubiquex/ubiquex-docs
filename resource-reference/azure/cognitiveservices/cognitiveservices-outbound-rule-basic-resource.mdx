---
title: "azure_cognitiveservices_outbound_rule_basic_resource"
description: "`azure_cognitiveservices_outbound_rule_basic_resource` defines an allowed outbound network destination for an Azure AI Foundry..."
---

`azure_cognitiveservices_outbound_rule_basic_resource` defines an allowed outbound network destination for an Azure AI Foundry account's own managed virtual network, permitting egress to a specific service.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cognitiveservices_cognitiveservices/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices outbound rule basic resource"})

    		ubx.Resource(cognitiveservices.CognitiveservicesOutboundRuleBasicResource, "example", cognitiveservices.CognitiveservicesOutboundRuleBasicResourceConfig{
    			Properties: map[string]any{"type": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CognitiveservicesOutboundRuleBasicResource } from "./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/typescript/azure_cognitiveservices_cognitiveservices/cognitiveservices/cognitiveservices_outbound_rule_basic_resource.ts";

    export default stack("example", () => {
      intent({
        summary: "example own cognitiveservices outbound rule basic resource",
      });

      resource(CognitiveservicesOutboundRuleBasicResource, "example", {
        properties: { type: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cognitiveservices_cognitiveservices.cognitiveservices import CognitiveservicesOutboundRuleBasicResource, CognitiveservicesOutboundRuleBasicResourceConfig

    def describe():
        ubx.intent("example own cognitiveservices outbound rule basic resource")

        ubx.resource(CognitiveservicesOutboundRuleBasicResource, "example", CognitiveservicesOutboundRuleBasicResourceConfig(
            properties={"type": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_outbound_rule_basic_resource, with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  Outbound Rule for the managed network of a cognitive services account. Required.
  <Expandable title="properties">
    <ResponseField name="category" type="string">
      Category of a managed network Outbound Rule of a cognitive services account. Optional.
    </ResponseField>
    <ResponseField name="error_information" type="string">
      Error information about an outbound rule of a cognitive services account if RuleStatus is failed. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="parent_rule_names" type="list(string)">
      The name(s) of the parent rule(s) this suggestion or association derives from. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      Type of a managed network Outbound Rule of a cognitive services account. Optional.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of a managed network Outbound Rule of a cognitive services account. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
