---
title: "azure_cognitiveservices_rai_blocklist_item"
description: "`azure_cognitiveservices_cognitiveservices_rai_blocklist_item` is a single blocked term or phrase within a Responsible AI blocklist."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cognitiveservices_cognitiveservices/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices rai blocklist item"})

    		ubx.Resource(cognitiveservices.CognitiveservicesRaiBlocklistItem, "example", cognitiveservices.CognitiveservicesRaiBlocklistItemConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CognitiveservicesRaiBlocklistItem } from "./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/typescript/azure_cognitiveservices_cognitiveservices/cognitiveservices/cognitiveservices_rai_blocklist_item.ts";

    export default stack("example", () => {
      intent({ summary: "example own cognitiveservices rai blocklist item" });

      resource(CognitiveservicesRaiBlocklistItem, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cognitiveservices_cognitiveservices.cognitiveservices import CognitiveservicesRaiBlocklistItem, CognitiveservicesRaiBlocklistItemConfig

    def describe():
        ubx.intent("example own cognitiveservices rai blocklist item")

        ubx.resource(CognitiveservicesRaiBlocklistItem, "example", CognitiveservicesRaiBlocklistItemConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_rai_blocklist_item, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A value used for optimistic concurrency control on this cognitiveservices rai blocklist item, changed by the server on every update. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
</ResponseField>

<ResponseField name="properties" type="object">
  RAI Custom Blocklist Item properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="is_regex" type="bool">
      If the pattern is a regex pattern. Optional.
    </ResponseField>
    <ResponseField name="pattern" type="string">
      Pattern to match against. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags to apply to this cognitiveservices rai blocklist item. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
</ResponseField>
