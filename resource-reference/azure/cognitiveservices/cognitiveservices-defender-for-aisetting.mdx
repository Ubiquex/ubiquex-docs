---
title: "azure_cognitiveservices_defender_for_aisetting"
description: "`azure_cognitiveservices_defender_for_aisetting` enables Microsoft Defender for AI protection on an Azure AI services account..."
---

`azure_cognitiveservices_defender_for_aisetting` enables Microsoft Defender for AI protection on an Azure AI services account, surfacing threat alerts for anomalous or malicious AI workload activity in Microsoft Defender for Cloud.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices defender for aisetting"})

    		ubx.Resource(cognitiveservices.DefenderForAisetting, "example", cognitiveservices.DefenderForAisettingConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DefenderForAisetting } from "@ubx/sdk-azure/azure/cognitiveservices/defender_for_aisetting";

    export default stack("example", () => {
      intent({ summary: "example own cognitiveservices defender for aisetting" });

      resource(DefenderForAisetting, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cognitiveservices.defender_for_aisetting import DefenderForAisetting, DefenderForAisettingConfig

    def describe():
        ubx.intent("example own cognitiveservices defender for aisetting")

        ubx.resource(DefenderForAisetting, "example", DefenderForAisettingConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_defender_for_aisetting, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Resource Etag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The Defender for AI resource properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="state" type="string">
      Defender for AI state on the AI resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
