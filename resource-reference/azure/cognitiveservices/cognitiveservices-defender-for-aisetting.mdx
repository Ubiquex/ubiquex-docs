---
title: "azure_cognitiveservices_defender_for_aisetting"
description: "`azure_cognitiveservices_cognitiveservices_defender_for_aisetting` enables Microsoft Defender for AI protection on an Azure AI services account..."
---

`azure_cognitiveservices_cognitiveservices_defender_for_aisetting` enables Microsoft Defender for AI protection on an Azure AI services account, surfacing threat alerts for anomalous or malicious AI workload activity in Microsoft Defender for Cloud.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cognitiveservices_cognitiveservices/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices defender for aisetting"})

    		ubx.Resource(cognitiveservices.CognitiveservicesDefenderForAisetting, "example", cognitiveservices.CognitiveservicesDefenderForAisettingConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CognitiveservicesDefenderForAisetting } from "./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/typescript/azure_cognitiveservices_cognitiveservices/cognitiveservices/cognitiveservices_defender_for_aisetting.ts";

    export default stack("example", () => {
      intent({ summary: "example own cognitiveservices defender for aisetting" });

      resource(CognitiveservicesDefenderForAisetting, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cognitiveservices_cognitiveservices.cognitiveservices import CognitiveservicesDefenderForAisetting, CognitiveservicesDefenderForAisettingConfig

    def describe():
        ubx.intent("example own cognitiveservices defender for aisetting")

        ubx.resource(CognitiveservicesDefenderForAisetting, "example", CognitiveservicesDefenderForAisettingConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_defender_for_aisetting, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A value used for optimistic concurrency control on this cognitiveservices defender for aisetting, changed by the server on every update. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
</ResponseField>

<ResponseField name="properties" type="object">
  The Defender for AI resource properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="state" type="string">
      Defender for AI state on the AI resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags to apply to this cognitiveservices defender for aisetting. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
</ResponseField>
