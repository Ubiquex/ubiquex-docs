---
title: "azure_cognitiveservices_cognitiveservices_encryption_scope"
description: "`azure_cognitiveservices_cognitiveservices_encryption_scope` defines a customer-managed encryption key configuration scoped to a subset of data within..."
---

`azure_cognitiveservices_cognitiveservices_encryption_scope` defines a customer-managed encryption key configuration scoped to a subset of data within an Azure AI services account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_cognitiveservices_cognitiveservices/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cognitiveservices encryption scope"})

    		ubx.Resource(cognitiveservices.CognitiveservicesEncryptionScope, "ci-runner", cognitiveservices.CognitiveservicesEncryptionScopeConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CognitiveservicesEncryptionScope } from "./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/typescript/azure_cognitiveservices_cognitiveservices/cognitiveservices/cognitiveservices_encryption_scope.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own cognitiveservices encryption scope" });

      resource(CognitiveservicesEncryptionScope, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_cognitiveservices_cognitiveservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_cognitiveservices_cognitiveservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_cognitiveservices_cognitiveservices.cognitiveservices import CognitiveservicesEncryptionScope, CognitiveservicesEncryptionScopeConfig

    def describe():
        ubx.intent("payments own cognitiveservices encryption scope")

        ubx.resource(CognitiveservicesEncryptionScope, "ci-runner", CognitiveservicesEncryptionScopeConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_cognitiveservices_cognitiveservices_encryption_scope, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Resource Etag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties to EncryptionScope Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Gets the status of the resource at the time the operation was called. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="state" type="string">
      The encryptionScope state. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
