---
title: "azure_cognitiveservices_encryption_scope"
description: "`azure_cognitiveservices_cognitiveservices_encryption_scope` defines a customer-managed encryption key configuration scoped to a subset of data within..."
---

`azure_cognitiveservices_cognitiveservices_encryption_scope` defines a customer-managed encryption key configuration scoped to a subset of data within an Azure AI services account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cognitiveservices encryption scope"})

    		ubx.Resource(cognitiveservices.EncryptionScope, "example", cognitiveservices.EncryptionScopeConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EncryptionScope } from "@ubx/sdk-azure/azure/cognitiveservices/encryption_scope";

    export default stack("example", () => {
      intent({ summary: "example own cognitiveservices encryption scope" });

      resource(EncryptionScope, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.cognitiveservices import EncryptionScope, EncryptionScopeConfig

    def describe():
        ubx.intent("example own cognitiveservices encryption scope")

        ubx.resource(EncryptionScope, "example", EncryptionScopeConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_cognitiveservices_encryption_scope, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Resource Etag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties to EncryptionScope Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Gets the status of the resource at the time the operation was called. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="state" type="string">
      The encryptionScope state. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
