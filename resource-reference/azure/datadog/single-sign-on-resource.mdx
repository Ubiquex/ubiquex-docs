---
title: "azure_datadog_single_sign_on_resource"
description: "A complete, runnable azure_datadog_single_sign_on_resource program, in every SDK language."
---

`azure_datadog_single_sign_on_resource` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datadog "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/datadog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own single sign on resource"})

    		ubx.Resource(datadog.SingleSignOnResource, "example", datadog.SingleSignOnResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SingleSignOnResource } from "@ubx/sdk-azure/azure/datadog/single_sign_on_resource";

    export default stack("example", () => {
      intent({ summary: "example own single sign on resource" });

      resource(SingleSignOnResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.datadog.single_sign_on_resource import SingleSignOnResource, SingleSignOnResourceConfig

    def describe():
        ubx.intent("example own single sign on resource")

        ubx.resource(SingleSignOnResource, "example", SingleSignOnResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_datadog_single_sign_on_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="enterprise_app_id" type="string">
      The Id of the Enterprise App used for Single sign-on. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="single_sign_on_state" type="string">
      Various states of the SSO resource Optional.
    </ResponseField>
    <ResponseField name="single_sign_on_url" type="string">
      The login URL specific to this Datadog Organization. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
