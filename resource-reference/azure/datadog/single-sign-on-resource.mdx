---
title: "azure_datadog_single_sign_on_resource"
description: "A complete, runnable azure_datadog_single_sign_on_resource program, in every SDK language."
---

`azure_datadog_single_sign_on_resource` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_datadog/sdk/go
    package main

    import (
    	datadog "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_datadog/datadog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own single sign on resource"})

    		ubx.Resource(datadog.SingleSignOnResource, "ci-runner", datadog.SingleSignOnResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SingleSignOnResource } from "./local-sdk/azure_datadog/sdk/typescript/azure_datadog/datadog/single_sign_on_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own single sign on resource" });

      resource(SingleSignOnResource, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_datadog.datadog import SingleSignOnResource, SingleSignOnResourceConfig

    def describe():
        ubx.intent("payments own single sign on resource")

        ubx.resource(SingleSignOnResource, "ci-runner", SingleSignOnResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_datadog_single_sign_on_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="enterprise_app_id" type="string">
      The Id of the Enterprise App used for Single sign-on. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="single_sign_on_state" type="string">
      Various states of the SSO resource Optional.
    </ResponseField>
    <ResponseField name="single_sign_on_url" type="string">
      The login URL specific to this Datadog Organization. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
