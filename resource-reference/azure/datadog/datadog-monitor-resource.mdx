---
title: "azure_datadog_datadog_monitor_resource"
description: "`azure_datadog_datadog_datadog_monitor_resource` is a Datadog resource on Azure..."
---

`azure_datadog_datadog_datadog_monitor_resource` is a Datadog resource on Azure, the top-level integration that sends Azure telemetry to a linked Datadog organization.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_datadog_datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_datadog_datadog/sdk/go
    package main

    import (
    	datadog "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_datadog_datadog/datadog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own datadog monitor resource"})

    		ubx.Resource(datadog.DatadogDatadogMonitorResource, "ci-runner", datadog.DatadogDatadogMonitorResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_datadog_datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatadogDatadogMonitorResource } from "./local-sdk/azure_datadog_datadog/sdk/typescript/azure_datadog_datadog/datadog/datadog_datadog_monitor_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own datadog monitor resource" });

      resource(DatadogDatadogMonitorResource, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_datadog_datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_datadog_datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_datadog_datadog.datadog import DatadogDatadogMonitorResource, DatadogDatadogMonitorResourceConfig

    def describe():
        ubx.intent("payments own datadog monitor resource")

        ubx.resource(DatadogDatadogMonitorResource, "ci-runner", DatadogDatadogMonitorResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_datadog_datadog_monitor_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  The managed identity (or identities) this resource uses to authenticate to other Azure services. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The identity ID. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      Specifies the identity type of the Datadog Monitor. At this time the only allowed value is 'SystemAssigned'. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties specific to the monitor resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="datadog_organization_properties" type="object">
      Specify the Datadog organization name. In the case of linking to existing organizations, Id, ApiKey, and Applicationkey is required as well. Optional.
      <Expandable title="properties">
        <ResponseField name="api_key" type="string">
          Api key associated to the Datadog organization. Optional.
        </ResponseField>
        <ResponseField name="application_key" type="string">
          Application key associated to the Datadog organization. Optional.
        </ResponseField>
        <ResponseField name="cspm" type="bool">
          The configuration which describes the state of cloud security posture management. This collects configuration information for all resources in a subscription and track conformance to industry benchmarks. Optional.
        </ResponseField>
        <ResponseField name="enterprise_app_id" type="string">
          The Id of the Enterprise App used for Single sign on. Optional.
        </ResponseField>
        <ResponseField name="id" type="string">
          Id of the Datadog organization. Optional.
        </ResponseField>
        <ResponseField name="linking_auth_code" type="string">
          The auth code used to linking to an existing Datadog organization. Optional.
        </ResponseField>
        <ResponseField name="linking_client_id" type="string">
          The client_id from an existing in exchange for an auth token to link organization. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the Datadog organization. Optional.
        </ResponseField>
        <ResponseField name="redirect_uri" type="string">
          The redirect URI for linking. Optional.
        </ResponseField>
        <ResponseField name="resource_collection" type="bool">
          The configuration which describes the state of resource collection. This collects configuration information for all resources in a subscription. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="liftr_resource_category" type="string">
      The internal resource category this integration is classified under. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="liftr_resource_preference" type="number">
      The priority of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="marketplace_subscription_status" type="string">
      Flag specifying the Marketplace Subscription Status of the resource. If payment is not made in time, the resource will go in Suspended state. Optional.
    </ResponseField>
    <ResponseField name="monitoring_status" type="string">
      Flag specifying if the resource monitoring is enabled or disabled. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The last, current provisioning status ARM reported for this resource, e.g. `Succeeded`, `Failed`, or `Updating`. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_info" type="object">
      Includes name, email and optionally, phone number. User Information can't be null. Optional.
      <Expandable title="properties">
        <ResponseField name="email_address" type="string">
          Email of the user used by Datadog for contacting them if needed Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the user Optional.
        </ResponseField>
        <ResponseField name="phone_number" type="string">
          Phone number of the user used by Datadog for contacting them if needed Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The pricing tier and capacity this resource is provisioned at. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      Name of the SKU in &#123;PlanId&#125; format. For Terraform, the only allowed value is 'Linked'. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
