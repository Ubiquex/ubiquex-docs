---
title: "azure_managedservices_managedservices_registration_assignment"
description: "`azure_managedservices_managedservices_registration_assignment` delegates management of a specific resource group or subscription to a managing tenant..."
---

`azure_managedservices_managedservices_registration_assignment` delegates management of a specific resource group or subscription to a managing tenant, per an Azure Lighthouse registration definition.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_managedservices_managedservices --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_managedservices_managedservices/sdk/go
    package main

    import (
    	managedservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_managedservices_managedservices/managedservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own managedservices registration assignment"})

    		ubx.Resource(managedservices.ManagedservicesRegistrationAssignment, "ci-runner", managedservices.ManagedservicesRegistrationAssignmentConfig{
    			Scope: "example",
    			Name:  "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_managedservices_managedservices --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedservicesRegistrationAssignment } from "./local-sdk/azure_managedservices_managedservices/sdk/typescript/azure_managedservices_managedservices/managedservices/managedservices_registration_assignment.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own managedservices registration assignment" });

      resource(ManagedservicesRegistrationAssignment, "ci-runner", {
        scope: "example",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_managedservices_managedservices --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_managedservices_managedservices/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_managedservices_managedservices.managedservices import ManagedservicesRegistrationAssignment, ManagedservicesRegistrationAssignmentConfig

    def describe():
        ubx.intent("payments own managedservices registration assignment")

        ubx.resource(ManagedservicesRegistrationAssignment, "ci-runner", ManagedservicesRegistrationAssignmentConfig(
            scope="example",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_managedservices_managedservices_registration_assignment
    called "ci-runner", with scope example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="id" type="string">
  The fully qualified path of the registration assignment. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the registration assignment. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the registration assignment. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      The current provisioning state of the registration assignment. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="registration_definition" type="object">
      The registration definition associated with the registration assignment. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified path of the registration definition. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the registration definition. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="plan" type="object">
          The details for the Managed Services offer’s plan in Azure Marketplace. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string" required>
              Azure Marketplace plan name. Required.
            </ResponseField>
            <ResponseField name="product" type="string" required>
              Azure Marketplace product code. Required.
            </ResponseField>
            <ResponseField name="publisher" type="string" required>
              Azure Marketplace publisher ID. Required.
            </ResponseField>
            <ResponseField name="version" type="string" required>
              Azure Marketplace plan's version. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="properties" type="object">
          The properties of the registration definition associated with the registration assignment. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
        <ResponseField name="system_data" type="object">
          Metadata pertaining to creation and last modification of the resource. Computed by Microsoft Azure after `ship`.
          <Expandable title="properties">
            <ResponseField name="created_at" type="string">
              The timestamp of resource creation (UTC). Optional.
            </ResponseField>
            <ResponseField name="created_by" type="string">
              The identity that created the resource. Optional.
            </ResponseField>
            <ResponseField name="created_by_type" type="string">
              The type of identity that created the resource. Optional.
            </ResponseField>
            <ResponseField name="last_modified_at" type="string">
              The timestamp of resource last modification (UTC) Optional.
            </ResponseField>
            <ResponseField name="last_modified_by" type="string">
              The identity that last modified the resource. Optional.
            </ResponseField>
            <ResponseField name="last_modified_by_type" type="string">
              The type of identity that last modified the resource. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the Azure resource (Microsoft.ManagedServices/registrationDefinitions). Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="registration_definition_id" type="string" required>
      The fully qualified path of the registration definition. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scope" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of the Azure resource (Microsoft.ManagedServices/registrationAssignments). Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
