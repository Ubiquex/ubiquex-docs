---
title: "azure_managedservices_registration_definition"
description: "`azure_managedservices_managedservices_registration_definition` defines the role(s) and principal(s) an Azure Lighthouse delegation grants a managing..."
---

`azure_managedservices_managedservices_registration_definition` defines the role(s) and principal(s) an Azure Lighthouse delegation grants a managing tenant over a delegated customer resource.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	managedservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/managedservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managedservices registration definition"})

    		ubx.Resource(managedservices.RegistrationDefinition, "example", managedservices.RegistrationDefinitionConfig{
    			Scope: "example",
    			Name:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegistrationDefinition } from "@ubx/sdk-azure/azure/managedservices/registration_definition";

    export default stack("example", () => {
      intent({ summary: "example own managedservices registration definition" });

      resource(RegistrationDefinition, "example", {
        scope: "example",
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.managedservices import RegistrationDefinition, RegistrationDefinitionConfig

    def describe():
        ubx.intent("example own managedservices registration definition")

        ubx.resource(RegistrationDefinition, "example", RegistrationDefinitionConfig(
            scope="example",
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_managedservices_registration_definition called
    "example", with scope example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="id" type="string">
  The fully qualified path of the registration definition. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the registration definition. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="plan" type="object">
  The details for the Managed Services offer’s plan in Azure Marketplace. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      Azure Marketplace plan name. Required.
    </ResponseField>
    <ResponseField name="product" type="string" required>
      Azure Marketplace product code. Required.
    </ResponseField>
    <ResponseField name="publisher" type="string" required>
      Azure Marketplace publisher ID. Required.
    </ResponseField>
    <ResponseField name="version" type="string" required>
      Azure Marketplace plan's version. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of a registration definition. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="authorizations" type="list(object)" required>
      The collection of authorization objects describing the access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant. Required.
      <Expandable title="properties">
        <ResponseField name="delegated_role_definition_ids" type="list(string)">
          The role definition ID(s) delegated to the managing tenant's own principal(s). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id_display_name" type="string">
          A human-readable name for the delegated principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="role_definition_id" type="string">
          A reference to the role definition this delegation grants. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      The description of the registration definition. Optional.
    </ResponseField>
    <ResponseField name="eligible_authorizations" type="list(object)">
      The collection of eligible authorization objects describing the just-in-time access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant. Optional.
      <Expandable title="properties">
        <ResponseField name="just_in_time_access_policy" type="object">
          Policy configuration for temporary, approval-gated access to this delegation. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="managed_by_tenant_approvers" type="list(object)">
              The principal(s) in the managing tenant authorized to approve just-in-time access requests. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="principal_id" type="string">
                  Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="principal_id_display_name" type="string">
                  A human-readable name for the delegated principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="maximum_activation_duration" type="string">
              The maximum duration a just-in-time access grant may remain active for. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="multi_factor_auth_provider" type="string">
              The multi-factor authentication provider required to approve or request access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id_display_name" type="string">
          A human-readable name for the delegated principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="role_definition_id" type="string">
          A reference to the role definition this delegation grants. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="managed_by_tenant_id" type="string" required>
      The identifier of the managedBy tenant. Required.
    </ResponseField>
    <ResponseField name="managed_by_tenant_name" type="string">
      The name of the managedBy tenant. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="managee_tenant_id" type="string">
      The identifier of the managed tenant. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="managee_tenant_name" type="string">
      The name of the managed tenant. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The current provisioning state of the registration definition. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="registration_definition_name" type="string">
      The name of the registration definition. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scope" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of the Azure resource (Microsoft.ManagedServices/registrationDefinitions). Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
