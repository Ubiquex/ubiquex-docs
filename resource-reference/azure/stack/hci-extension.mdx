---
title: "azurerm_stack_hci_extension"
description: "Real, generated bindings for `azurerm_stack_hci_extension`."
---

`azurerm_stack_hci_extension` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	stack "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/stack"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hci extension"})

    		ubx.Resource(stack.HciExtension, "ci-runner", stack.HciExtensionConfig{
    			ArcSettingId:                   "example",
    			Name:                           "ci-runner",
    			Publisher:                      "example",
    			Type:                           "example",
    			AutoUpgradeMinorVersionEnabled: true,
    			AutomaticUpgradeEnabled:        true,
    			ProtectedSettings:              "example",
    			Settings:                       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HciExtension } from "jsr:@ubx/sdk-azure/azurerm/stack/hci_extension";

    export default stack("payments", () => {
      intent({ summary: "payments own hci extension" });

      resource(HciExtension, "ci-runner", {
        arcSettingId: "example",
        name: "ci-runner",
        publisher: "example",
        type: "example",
        autoUpgradeMinorVersionEnabled: true,
        automaticUpgradeEnabled: true,
        protectedSettings: "example",
        settings: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.stack import HciExtension, HciExtensionConfig

    def describe():
        ubx.intent("payments own hci extension")

        ubx.resource(HciExtension, "ci-runner", HciExtensionConfig(
            arc_setting_id="example",
            name="ci-runner",
            publisher="example",
            type="example",
            auto_upgrade_minor_version_enabled=True,
            automatic_upgrade_enabled=True,
            protected_settings="example",
            settings="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_stack_hci_extension called "ci-runner", with arc
    setting id example, publisher example, type example, auto upgrade
    minor version enabled true, automatic upgrade enabled true,
    protected settings example, settings example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="arc_setting_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="auto_upgrade_minor_version_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="automatic_upgrade_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="protected_settings" type="string">
  Optional.
</ResponseField>

<ResponseField name="publisher" type="string" required>
  Required.
</ResponseField>

<ResponseField name="settings" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="type_handler_version" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
