---
title: "azure_consumption_openapi_budget"
description: "`azure_consumption_openapi_budget` sets a spending threshold on a scope and triggers notification(s) when actual or forecasted spend crosses it, via..."
---

`azure_consumption_openapi_budget` sets a spending threshold on a scope and triggers notification(s) when actual or forecasted spend crosses it, via the Consumption API's own budget resource.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi budget"})

    		ubx.Resource(consumption.OpenapiBudget, "example", consumption.OpenapiBudgetConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiBudget } from "@ubx/sdk-azure/azure/consumption/openapi_budget";

    export default stack("example", () => {
      intent({ summary: "example own openapi budget" });

      resource(OpenapiBudget, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.consumption import OpenapiBudget, OpenapiBudgetConfig

    def describe():
        ubx.intent("example own openapi budget")

        ubx.resource(OpenapiBudget, "example", OpenapiBudgetConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_consumption_openapi_budget, with scope example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  eTag of the resource. To handle concurrent update scenario, this field will be used to determine whether the user is updating the latest version or not. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the budget. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="amount" type="number" required>
      The total amount of cost to track with the budget Required.
    </ResponseField>
    <ResponseField name="category" type="string" required>
      The category of the budget, whether the budget tracks cost or usage. Required.
    </ResponseField>
    <ResponseField name="current_spend" type="object">
      The current amount of cost which is being tracked for a budget. Optional.
      <Expandable title="properties">
        <ResponseField name="amount" type="number">
          The total amount of cost which is being tracked by the budget. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="unit" type="string">
          The unit of measure for the budget amount. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="filter" type="object">
      May be used to filter budgets by resource group, resource, or meter. Optional.
      <Expandable title="properties">
        <ResponseField name="and" type="list(object)">
          The logical "AND" expression. Must have at least 2 items. Optional.
          <Expandable title="properties">
            <ResponseField name="dimensions" type="object">
              The dimension(s) this budget is filtered by. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  How this condition's own operands are compared. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="values" type="list(string)">
                  The value(s) making up this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="tags" type="object">
              User-defined key/value tags on this resource, usable for organizing and filtering resources across the subscription. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operator" type="string">
                  How this condition's own operands are compared. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="values" type="list(string)">
                  The value(s) making up this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dimensions" type="object">
          The comparison expression to be used in the budgets. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string" required>
              The name of the column to use in comparison. Required.
            </ResponseField>
            <ResponseField name="operator" type="string" required>
              The operator to use for comparison. Required.
            </ResponseField>
            <ResponseField name="values" type="list(string)" required>
              Array of values to use for comparison Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tags" type="object">
          The comparison expression to be used in the budgets. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string" required>
              The name of the column to use in comparison. Required.
            </ResponseField>
            <ResponseField name="operator" type="string" required>
              The operator to use for comparison. Required.
            </ResponseField>
            <ResponseField name="values" type="list(string)" required>
              Array of values to use for comparison Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="forecast_spend" type="object">
      The forecasted cost which is being tracked for a budget. Optional.
      <Expandable title="properties">
        <ResponseField name="amount" type="number">
          The forecasted cost for the total time period which is being tracked by the budget. This value is only provided if the budget contains a forecast alert type. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="unit" type="string">
          The unit of measure for the budget amount. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="notifications" type="map(object)">
      Dictionary of notifications associated with the budget. Budget can have up to five notifications. Optional.
      <Expandable title="properties">
        <ResponseField name="contact_emails" type="list(string)">
          The email address(es) notified for this alert. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="contact_groups" type="list(string)">
          The Action Group(s) notified for this alert. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="contact_roles" type="list(string)">
          The Azure RBAC role(s) whose own members are notified for this alert. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Whether this feature is turned on. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="locale" type="string">
          The locale this content is expressed in. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operator" type="string">
          How this condition's own operands are compared. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="threshold" type="number">
          The value that must be crossed for this condition to trigger. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="threshold_type" type="string">
          The kind of threshold this represents, e.g. actual or forecasted cost. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="time_grain" type="string" required>
      The time covered by a budget. Tracking of the amount will be reset based on the time grain. BillingMonth, BillingQuarter, and BillingAnnual are only supported by WD customers Required.
    </ResponseField>
    <ResponseField name="time_period" type="object" required>
      The start and end date for a budget. Required.
      <Expandable title="properties">
        <ResponseField name="end_date" type="string">
          The end date for the budget. If not provided, we default this to 10 years from the start date. Optional.
        </ResponseField>
        <ResponseField name="start_date" type="string" required>
          The start date for the budget. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scope" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
