---
title: "azure_azure_kusto_kusto_sandbox_custom_image"
description: "`azure_azure_kusto_kusto_sandbox_custom_image` defines a custom Python runtime image (base image..."
---

`azure_azure_kusto_kusto_sandbox_custom_image` defines a custom Python runtime image (base image, package requirements) available to the `python()` plugin's own isolated sandbox execution in Azure Data Explorer queries.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_azure_kusto_kusto --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_azure_kusto_kusto/sdk/go
    package main

    import (
    	azure "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_azure_kusto_kusto/azure"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own kusto kusto sandbox custom image"})

    		ubx.Resource(azure.KustoKustoSandboxCustomImage, "ci-runner", azure.KustoKustoSandboxCustomImageConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_azure_kusto_kusto --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { KustoKustoSandboxCustomImage } from "./local-sdk/azure_azure_kusto_kusto/sdk/typescript/azure_azure_kusto_kusto/azure/kusto_kusto_sandbox_custom_image.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own kusto kusto sandbox custom image" });

      resource(KustoKustoSandboxCustomImage, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_azure_kusto_kusto --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_azure_kusto_kusto/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_azure_kusto_kusto.azure import KustoKustoSandboxCustomImage, KustoKustoSandboxCustomImageConfig

    def describe():
        ubx.intent("payments own kusto kusto sandbox custom image")

        ubx.resource(KustoKustoSandboxCustomImage, "ci-runner", KustoKustoSandboxCustomImageConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_azure_kusto_kusto_sandbox_custom_image, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  A class representing the properties of a sandbox custom image object. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="base_image_name" type="string">
      The base image name on which the custom image is built on top of. It can be one of the LanguageExtensionImageName (e.g.: 'Python3_10_8', 'Python3_10_8_DL') or the name of an existing custom image. Either this property or languageVersion should be specified. Optional.
    </ResponseField>
    <ResponseField name="language" type="string" required>
      The language name, for example Python. Required.
    </ResponseField>
    <ResponseField name="language_version" type="string">
      The version of the language. Either this property or baseImageName should be specified. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioned state of the resource. Optional.
    </ResponseField>
    <ResponseField name="requirements_file_content" type="string">
      The requirements file content. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
