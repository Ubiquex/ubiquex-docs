---
title: "azure_azure_kusto_kusto_database"
description: "`azure_azure_kusto_kusto_database` is a database within an Azure Data Explorer cluster, the container table(s) are created within and queried via KQL."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_azure_kusto_kusto --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_azure_kusto_kusto/sdk/go
    package main

    import (
    	azure "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_azure_kusto_kusto/azure"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own kusto kusto database"})

    		ubx.Resource(azure.KustoKustoDatabase, "ci-runner", azure.KustoKustoDatabaseConfig{
    			Kind: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_azure_kusto_kusto --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { KustoKustoDatabase } from "./local-sdk/azure_azure_kusto_kusto/sdk/typescript/azure_azure_kusto_kusto/azure/kusto_kusto_database.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own kusto kusto database" });

      resource(KustoKustoDatabase, "ci-runner", {
        kind: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_azure_kusto_kusto --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_azure_kusto_kusto/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_azure_kusto_kusto.azure import KustoKustoDatabase, KustoKustoDatabaseConfig

    def describe():
        ubx.intent("payments own kusto kusto database")

        ubx.resource(KustoKustoDatabase, "ci-runner", KustoKustoDatabaseConfig(
            kind="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_azure_kusto_kusto_database, with kind example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string" required>
  Kind of the database Required.
</ResponseField>

<ResponseField name="location" type="string">
  Resource location. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
