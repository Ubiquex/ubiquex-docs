---
title: "azure_nginx_swagger_nginx_deployment_waf_policy"
description: "`azure_nginx_swagger_nginx_deployment_waf_policy` configures NGINX App Protect Web Application Firewall protection for an NGINX for Azure deployment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	nginx "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/nginx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own swagger nginx deployment waf policy"})

    		ubx.Resource(nginx.SwaggerNginxDeploymentWafPolicy, "example", nginx.SwaggerNginxDeploymentWafPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SwaggerNginxDeploymentWafPolicy } from "@ubx/sdk-azure/azure/nginx/swagger_nginx_deployment_waf_policy";

    export default stack("example", () => {
      intent({ summary: "example own swagger nginx deployment waf policy" });

      resource(SwaggerNginxDeploymentWafPolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.nginx import SwaggerNginxDeploymentWafPolicy, SwaggerNginxDeploymentWafPolicyConfig

    def describe():
        ubx.intent("example own swagger nginx deployment waf policy")

        ubx.resource(SwaggerNginxDeploymentWafPolicy, "example", SwaggerNginxDeploymentWafPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_nginx_swagger_nginx_deployment_waf_policy, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Nginx Deployment Waf Policy Properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="applying_state" type="object">
      Nginx Deployment Waf Policy Applying Status Optional.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          Machine readable code indicating the applying status code of a WAF Policy. Optional.
        </ResponseField>
        <ResponseField name="display_status" type="string">
          A readable string of the current status, and sometimes have the reason for the current state. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="time" type="string">
          The date and time in UTC the current applying status was set. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="compiling_state" type="object">
      Nginx Deployment Waf Policy Compiling Status Optional.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          Machine readable code indicating the compilation status of a WAF Policy. Optional.
        </ResponseField>
        <ResponseField name="display_status" type="string">
          A readable string of the current status, and sometimes have the reason for the current state. If the CompilingStatus is Failed the Display Status will be The waf Policy failed to compile. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="time" type="string">
          The date and time the policy was compiled in UTC. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="content" type="string">
      The byte content of the Policy Optional.
    </ResponseField>
    <ResponseField name="filepath" type="string">
      The file path where the Policy is to be saved Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning State Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
