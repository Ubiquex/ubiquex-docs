---
title: "azure_nginx_swagger_nginx_certificate"
description: "`azure_nginx_swagger_nginx_certificate` uploads a TLS certificate (referenced from Key Vault) to an NGINX for Azure deployment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	nginx "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/nginx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own swagger nginx certificate"})

    		ubx.Resource(nginx.SwaggerNginxCertificate, "example", nginx.SwaggerNginxCertificateConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SwaggerNginxCertificate } from "@ubx/sdk-azure/azure/nginx/swagger_nginx_certificate";

    export default stack("example", () => {
      intent({ summary: "example own swagger nginx certificate" });

      resource(SwaggerNginxCertificate, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.nginx.swagger_nginx_certificate import SwaggerNginxCertificate, SwaggerNginxCertificateConfig

    def describe():
        ubx.intent("example own swagger nginx certificate")

        ubx.resource(SwaggerNginxCertificate, "example", SwaggerNginxCertificateConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_nginx_swagger_nginx_certificate, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The geo-location where the resource lives Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Nginx Certificate Properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="certificate_error" type="object">
      Nginx Certificate Error Response Body Optional.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          A machine-readable status or error code. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of this result. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="certificate_virtual_path" type="string">
      The path this certificate is mounted at inside the NGINX deployment. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="key_vault_secret_created" type="string">
      When the associated Key Vault secret was created. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="key_vault_secret_id" type="string">
      A reference to the Key Vault secret this value is sourced from. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="key_vault_secret_version" type="string">
      The specific version of the associated Key Vault secret. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="key_virtual_path" type="string">
      The path this key is mounted at inside the NGINX deployment. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning State Optional.
    </ResponseField>
    <ResponseField name="sha1_thumbprint" type="string">
      A SHA-1 thumbprint of this certificate. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
