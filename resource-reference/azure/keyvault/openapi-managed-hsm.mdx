---
title: "azure_keyvault_openapi_managed_hsm"
description: "`azure_keyvault_openapi_managed_hsm` provisions a Managed HSM, a dedicated, single-tenant, FIPS 140-2 Level 3-validated hardware security module pool..."
---

`azure_keyvault_openapi_managed_hsm` provisions a Managed HSM, a dedicated, single-tenant, FIPS 140-2 Level 3-validated hardware security module pool, for workloads requiring exclusive control over their own HSM.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_keyvault_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_keyvault_openapi/sdk/go
    package main

    import (
    	keyvault "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_keyvault_openapi/keyvault"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi managed hsm"})

    		ubx.Resource(keyvault.OpenapiManagedHsm, "example", keyvault.OpenapiManagedHsmConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_keyvault_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiManagedHsm } from "./local-sdk/azure_keyvault_openapi/sdk/typescript/azure_keyvault_openapi/keyvault/openapi_managed_hsm.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi managed hsm" });

      resource(OpenapiManagedHsm, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_keyvault_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_keyvault_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_keyvault_openapi.keyvault import OpenapiManagedHsm, OpenapiManagedHsmConfig

    def describe():
        ubx.intent("example own openapi managed hsm")

        ubx.resource(OpenapiManagedHsm, "example", OpenapiManagedHsmConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_keyvault_openapi_managed_hsm called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID used to authenticate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location" type="string">
  Represents an Azure geography region where supported resource providers live. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the managed HSM Pool Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="create_mode" type="string">
      The vault's create mode to indicate whether the vault need to be recovered or not. Optional.
    </ResponseField>
    <ResponseField name="enable_purge_protection" type="bool">
      Property specifying whether protection against purge is enabled for this managed HSM pool. Setting this property to true activates protection against purge for this managed HSM pool and its content - only the Managed HSM service may initiate a hard, irrecoverable deletion. Enabling this functionality is irreversible. Optional.
    </ResponseField>
    <ResponseField name="enable_soft_delete" type="bool">
      Property to specify whether the 'soft delete' functionality is enabled for this managed HSM pool. Soft delete is enabled by default for all managed HSMs and is immutable. Optional.
    </ResponseField>
    <ResponseField name="hsm_uri" type="string">
      The URI of the managed hsm pool for performing operations on keys. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="initial_admin_object_ids" type="list(string)">
      Array of initial administrators object ids for this managed hsm pool. Optional.
    </ResponseField>
    <ResponseField name="network_acls" type="object">
      A set of rules governing the network accessibility of a managed hsm pool. Optional.
      <Expandable title="properties">
        <ResponseField name="bypass" type="string">
          Tells what traffic can bypass network rules. This can be 'AzureServices' or 'None'. If not specified the default is 'AzureServices'. Optional.
        </ResponseField>
        <ResponseField name="default_action" type="string">
          The default action when no rule from ipRules and from virtualNetworkRules match. This is only used after the bypass property has been evaluated. Optional.
        </ResponseField>
        <ResponseField name="ip_rules" type="list(object)">
          The list of IP address rules. Optional.
          <Expandable title="properties">
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_tags" type="list(object)">
          The list of service tags. Optional.
          <Expandable title="properties">
            <ResponseField name="tag" type="string">
              A label attached to this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="virtual_network_rules" type="list(object)">
          The list of virtual network rules. Optional.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      List of private endpoint connections associated with the managed hsm pool. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Control permission to the managed HSM from public networks. Optional.
    </ResponseField>
    <ResponseField name="regions" type="list(object)">
      List of all regions associated with the managed hsm pool. Optional.
      <Expandable title="properties">
        <ResponseField name="is_primary" type="bool">
          Whether this contact is the primary contact for certificate notifications. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="provisioning_state" type="string">
          The last, current provisioning status ARM reported for this resource, e.g. `Succeeded`, `Failed`, or `Updating`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scheduled_purge_date" type="string">
      The scheduled purge date in UTC. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="security_domain_properties" type="object">
      The security domain properties of the managed hsm. Optional.
      <Expandable title="properties">
        <ResponseField name="activation_status" type="string">
          Activation Status Optional.
        </ResponseField>
        <ResponseField name="activation_status_message" type="string">
          Activation Status Message. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="soft_delete_retention_in_days" type="number">
      Soft deleted data retention days. When you delete an HSM or a key, it will remain recoverable for the configured retention period or for a default period of 90 days. It accepts values between 7 and 90. Optional.
    </ResponseField>
    <ResponseField name="status_message" type="string">
      Resource Status Message. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  SKU details Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="family" type="string" required>
      SKU Family of the managed HSM Pool Required.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      SKU of the managed HSM Pool Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
