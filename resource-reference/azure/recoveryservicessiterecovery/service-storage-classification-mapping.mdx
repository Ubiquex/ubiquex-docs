---
title: "azure_recoveryservicessiterecovery_service_storage_classification_mapping"
description: "`azure_recoveryservicessiterecovery_service_storage_classification_mapping` maps a source storage classification to a target one in Azure Site..."
---

`azure_recoveryservicessiterecovery_service_storage_classification_mapping` maps a source storage classification to a target one in Azure Site Recovery, so failed-over VM disks land on the correct target storage tier.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_recoveryservicessiterecovery_service/sdk/go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service storage classification mapping"})

    		ubx.Resource(recoveryservicessiterecovery.ServiceStorageClassificationMapping, "ci-runner", recoveryservicessiterecovery.ServiceStorageClassificationMappingConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceStorageClassificationMapping } from "./local-sdk/azure_recoveryservicessiterecovery_service/sdk/typescript/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery/service_storage_classification_mapping.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own service storage classification mapping" });

      resource(ServiceStorageClassificationMapping, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_recoveryservicessiterecovery_service/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_recoveryservicessiterecovery_service.recoveryservicessiterecovery import ServiceStorageClassificationMapping, ServiceStorageClassificationMappingConfig

    def describe():
        ubx.intent("payments own service storage classification mapping")

        ubx.resource(ServiceStorageClassificationMapping, "ci-runner", ServiceStorageClassificationMappingConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_recoveryservicessiterecovery_service_storage_classification_mapping,
    with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object">
  Storage mapping input properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="target_storage_classification_id" type="string">
      The ID of the storage object. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource Location Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  Storage mapping input properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="target_storage_classification_id" type="string">
      The ID of the storage object. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
