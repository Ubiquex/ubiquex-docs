---
title: "azure_recoveryservicessiterecovery_service_migration_item"
description: "`azure_recoveryservicessiterecovery_service_migration_item` tracks a single machine undergoing agentless migration (rather than ongoing disaster..."
---

`azure_recoveryservicessiterecovery_service_migration_item` tracks a single machine undergoing agentless migration (rather than ongoing disaster recovery replication) into Azure through Azure Site Recovery's own migration capability.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_recoveryservicessiterecovery_service/sdk/go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service migration item"})

    		ubx.Resource(recoveryservicessiterecovery.ServiceMigrationItem, "example", recoveryservicessiterecovery.ServiceMigrationItemConfig{
    			Properties: map[string]any{"policy_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceMigrationItem } from "./local-sdk/azure_recoveryservicessiterecovery_service/sdk/typescript/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery/service_migration_item.ts";

    export default stack("example", () => {
      intent({ summary: "example own service migration item" });

      resource(ServiceMigrationItem, "example", {
        properties: { policyId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_recoveryservicessiterecovery_service/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_recoveryservicessiterecovery_service.recoveryservicessiterecovery import ServiceMigrationItem, ServiceMigrationItemConfig

    def describe():
        ubx.intent("example own service migration item")

        ubx.resource(ServiceMigrationItem, "example", ServiceMigrationItemConfig(
            properties={"policy_id": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_recoveryservicessiterecovery_service_migration_item,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object" required>
  Enable migration input properties. Required.
  <Expandable title="properties">
    <ResponseField name="policy_id" type="string" required>
      The policy Id. Required.
    </ResponseField>
    <ResponseField name="provider_specific_details" type="object" required>
      Enable migration provider specific input. Required.
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string" required>
          The class type. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource Location Computed by Microsoft Azure after `ship`.
</ResponseField>
