---
title: "azure_recoveryservicessiterecovery_service_protection_container"
description: "`azure_recoveryservicessiterecovery_service_protection_container` is a logical grouping (e.g. a VMware datacenter or Hyper-V host) within a fabric..."
---

`azure_recoveryservicessiterecovery_service_protection_container` is a logical grouping (e.g. a VMware datacenter or Hyper-V host) within a fabric that replication protected item(s) belong to in Azure Site Recovery.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service protection container"})

    		ubx.Resource(recoveryservicessiterecovery.ServiceProtectionContainer, "example", recoveryservicessiterecovery.ServiceProtectionContainerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceProtectionContainer } from "@ubx/sdk-azure/azure/recoveryservicessiterecovery/service_protection_container";

    export default stack("example", () => {
      intent({ summary: "example own service protection container" });

      resource(ServiceProtectionContainer, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.recoveryservicessiterecovery.service_protection_container import ServiceProtectionContainer, ServiceProtectionContainerConfig

    def describe():
        ubx.intent("example own service protection container")

        ubx.resource(ServiceProtectionContainer, "example", ServiceProtectionContainerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_recoveryservicessiterecovery_service_protection_container,
    with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object">
  Create protection container input properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provider_specific_input" type="list(object)">
      Provider specific inputs for container creation. Optional.
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string">
          The specific subtype this resource is, distinguishing it within its own broader category. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource Location Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  Create protection container input properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provider_specific_input" type="list(object)">
      Provider specific inputs for container creation. Optional.
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string">
          The specific subtype this resource is, distinguishing it within its own broader category. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
