---
title: "azure_recoveryservicessiterecovery_service_protection_container"
description: "`azure_recoveryservicessiterecovery_service_protection_container` is a logical grouping (e.g. a VMware datacenter or Hyper-V host) within a fabric..."
---

`azure_recoveryservicessiterecovery_service_protection_container` is a logical grouping (e.g. a VMware datacenter or Hyper-V host) within a fabric that replication protected item(s) belong to in Azure Site Recovery.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_recoveryservicessiterecovery_service/sdk/go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service protection container"})

    		ubx.Resource(recoveryservicessiterecovery.ServiceProtectionContainer, "ci-runner", recoveryservicessiterecovery.ServiceProtectionContainerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceProtectionContainer } from "./local-sdk/azure_recoveryservicessiterecovery_service/sdk/typescript/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery/service_protection_container.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own service protection container" });

      resource(ServiceProtectionContainer, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_recoveryservicessiterecovery_service/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_recoveryservicessiterecovery_service.recoveryservicessiterecovery import ServiceProtectionContainer, ServiceProtectionContainerConfig

    def describe():
        ubx.intent("payments own service protection container")

        ubx.resource(ServiceProtectionContainer, "ci-runner", ServiceProtectionContainerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_recoveryservicessiterecovery_service_protection_container,
    with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object">
  Create protection container input properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provider_specific_input" type="list(object)">
      Provider specific inputs for container creation. Optional.
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string">
          The specific subtype this resource is, distinguishing it within its own broader category. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource Location Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  Create protection container input properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provider_specific_input" type="list(object)">
      Provider specific inputs for container creation. Optional.
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string">
          The specific subtype this resource is, distinguishing it within its own broader category. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
