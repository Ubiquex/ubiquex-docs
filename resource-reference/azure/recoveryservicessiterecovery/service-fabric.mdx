---
title: "azure_recoveryservicessiterecovery_service_fabric"
description: "`azure_recoveryservicessiterecovery_service_fabric` registers a source infrastructure (an on-premises VMM site, a VMware environment..."
---

`azure_recoveryservicessiterecovery_service_fabric` registers a source infrastructure (an on-premises VMM site, a VMware environment, or an Azure region) that Azure Site Recovery replicates workload(s) from or to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service fabric"})

    		ubx.Resource(recoveryservicessiterecovery.ServiceFabric, "example", recoveryservicessiterecovery.ServiceFabricConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceFabric } from "@ubx/sdk-azure/azure/recoveryservicessiterecovery/service_fabric";

    export default stack("example", () => {
      intent({ summary: "example own service fabric" });

      resource(ServiceFabric, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.recoveryservicessiterecovery import ServiceFabric, ServiceFabricConfig

    def describe():
        ubx.intent("example own service fabric")

        ubx.resource(ServiceFabric, "example", ServiceFabricConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_recoveryservicessiterecovery_service_fabric, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object">
  Properties of site details provided during the time of site creation. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="custom_details" type="object">
      Fabric provider specific settings. Optional.
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string" required>
          Gets the class type. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource Location Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of site details provided during the time of site creation. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="custom_details" type="object">
      Fabric provider specific settings. Optional.
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string" required>
          Gets the class type. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
