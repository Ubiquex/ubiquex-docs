---
title: "azure_recoveryservicessiterecovery_service_vcenter"
description: "`azure_recoveryservicessiterecovery_service_vcenter` registers a VMware vCenter Server with Azure Site Recovery..."
---

`azure_recoveryservicessiterecovery_service_vcenter` registers a VMware vCenter Server with Azure Site Recovery, letting it discover and replicate the VM(s) it manages.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_recoveryservicessiterecovery_service/sdk/go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service vcenter"})

    		ubx.Resource(recoveryservicessiterecovery.ServiceVcenter, "ci-runner", recoveryservicessiterecovery.ServiceVcenterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceVcenter } from "./local-sdk/azure_recoveryservicessiterecovery_service/sdk/typescript/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery/service_vcenter.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own service vcenter" });

      resource(ServiceVcenter, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_recoveryservicessiterecovery_service/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_recoveryservicessiterecovery_service.recoveryservicessiterecovery import ServiceVcenter, ServiceVcenterConfig

    def describe():
        ubx.intent("payments own service vcenter")

        ubx.resource(ServiceVcenter, "ci-runner", ServiceVcenterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_recoveryservicessiterecovery_service_vcenter, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object">
  The properties of an add vCenter request. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="friendly_name" type="string">
      The friendly name of the vCenter. Optional.
    </ResponseField>
    <ResponseField name="ip_address" type="string">
      The IP address of the vCenter to be discovered. Optional.
    </ResponseField>
    <ResponseField name="port" type="string">
      The port number for discovery. Optional.
    </ResponseField>
    <ResponseField name="process_server_id" type="string">
      The process server Id from where the discovery is orchestrated. Optional.
    </ResponseField>
    <ResponseField name="run_as_account_id" type="string">
      The account Id which has privileges to discover the vCenter. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource Location Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of an add vCenter request. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="friendly_name" type="string">
      The friendly name of the vCenter. Optional.
    </ResponseField>
    <ResponseField name="ip_address" type="string">
      The IP address of the vCenter to be discovered. Optional.
    </ResponseField>
    <ResponseField name="port" type="string">
      The port number for discovery. Optional.
    </ResponseField>
    <ResponseField name="process_server_id" type="string">
      The process server Id from where the discovery is orchestrated. Optional.
    </ResponseField>
    <ResponseField name="run_as_account_id" type="string">
      The account Id which has privileges to discover the vCenter. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
