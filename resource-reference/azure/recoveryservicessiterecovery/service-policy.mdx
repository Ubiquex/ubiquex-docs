---
title: "azure_recoveryservicessiterecovery_service_policy"
description: "`azure_recoveryservicessiterecovery_service_policy` defines the replication frequency, recovery point retention..."
---

`azure_recoveryservicessiterecovery_service_policy` defines the replication frequency, recovery point retention, and app-consistent snapshot interval Azure Site Recovery applies to protected item(s) using it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service policy"})

    		ubx.Resource(recoveryservicessiterecovery.ServicePolicy, "example", recoveryservicessiterecovery.ServicePolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicePolicy } from "@ubx/sdk-azure/azure/recoveryservicessiterecovery/service_policy";

    export default stack("example", () => {
      intent({ summary: "example own service policy" });

      resource(ServicePolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.recoveryservicessiterecovery.service_policy import ServicePolicy, ServicePolicyConfig

    def describe():
        ubx.intent("example own service policy")

        ubx.resource(ServicePolicy, "example", ServicePolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_recoveryservicessiterecovery_service_policy, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object">
  Policy creation properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provider_specific_input" type="object">
      Base class for provider specific input. Optional.
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string" required>
          The class type. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource Location Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  Policy creation properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provider_specific_input" type="object">
      Base class for provider specific input. Optional.
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string" required>
          The class type. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
