---
title: "azure_recoveryservicessiterecovery_service_alert"
description: "`azure_recoveryservicessiterecovery_service_alert` configures Azure Site Recovery to send an email notification when a defined replication health event..."
---

`azure_recoveryservicessiterecovery_service_alert` configures Azure Site Recovery to send an email notification when a defined replication health event occurs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service alert"})

    		ubx.Resource(recoveryservicessiterecovery.ServiceAlert, "example", recoveryservicessiterecovery.ServiceAlertConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceAlert } from "@ubx/sdk-azure/azure/recoveryservicessiterecovery/service_alert";

    export default stack("example", () => {
      intent({ summary: "example own service alert" });

      resource(ServiceAlert, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.recoveryservicessiterecovery import ServiceAlert, ServiceAlertConfig

    def describe():
        ubx.intent("example own service alert")

        ubx.resource(ServiceAlert, "example", ServiceAlertConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_recoveryservicessiterecovery_service_alert, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object">
  Properties of a configure alert request. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="custom_email_addresses" type="list(string)">
      The custom email address for sending emails. Optional.
    </ResponseField>
    <ResponseField name="locale" type="string">
      The locale for the email notification. Optional.
    </ResponseField>
    <ResponseField name="send_to_owners" type="string">
      A value indicating whether to send email to subscription administrator. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource Location Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of a configure alert request. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="custom_email_addresses" type="list(string)">
      The custom email address for sending emails. Optional.
    </ResponseField>
    <ResponseField name="locale" type="string">
      The locale for the email notification. Optional.
    </ResponseField>
    <ResponseField name="send_to_owners" type="string">
      A value indicating whether to send email to subscription administrator. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
