---
title: "azure_recoveryservicessiterecovery_service_alert"
description: "`azure_recoveryservicessiterecovery_service_alert` configures Azure Site Recovery to send an email notification when a defined replication health event..."
---

`azure_recoveryservicessiterecovery_service_alert` configures Azure Site Recovery to send an email notification when a defined replication health event occurs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_recoveryservicessiterecovery_service/sdk/go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service alert"})

    		ubx.Resource(recoveryservicessiterecovery.ServiceAlert, "ci-runner", recoveryservicessiterecovery.ServiceAlertConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceAlert } from "./local-sdk/azure_recoveryservicessiterecovery_service/sdk/typescript/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery/service_alert.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own service alert" });

      resource(ServiceAlert, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_recoveryservicessiterecovery_service/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_recoveryservicessiterecovery_service.recoveryservicessiterecovery import ServiceAlert, ServiceAlertConfig

    def describe():
        ubx.intent("payments own service alert")

        ubx.resource(ServiceAlert, "ci-runner", ServiceAlertConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_recoveryservicessiterecovery_service_alert, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object">
  Properties of a configure alert request. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="custom_email_addresses" type="list(string)">
      The custom email address for sending emails. Optional.
    </ResponseField>
    <ResponseField name="locale" type="string">
      The locale for the email notification. Optional.
    </ResponseField>
    <ResponseField name="send_to_owners" type="string">
      A value indicating whether to send email to subscription administrator. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource Location Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of a configure alert request. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="custom_email_addresses" type="list(string)">
      The custom email address for sending emails. Optional.
    </ResponseField>
    <ResponseField name="locale" type="string">
      The locale for the email notification. Optional.
    </ResponseField>
    <ResponseField name="send_to_owners" type="string">
      A value indicating whether to send email to subscription administrator. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
