---
title: "azure_recoveryservicessiterecovery_service_recovery_services_provider"
description: "`azure_recoveryservicessiterecovery_service_recovery_services_provider` registers the on-premises agent (e.g. the Azure Site Recovery Provider on a VMM..."
---

`azure_recoveryservicessiterecovery_service_recovery_services_provider` registers the on-premises agent (e.g. the Azure Site Recovery Provider on a VMM server) that communicates with a Recovery Services vault.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_recoveryservicessiterecovery_service/sdk/go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service recovery services provider"})

    		ubx.Resource(recoveryservicessiterecovery.ServiceRecoveryServicesProvider, "example", recoveryservicessiterecovery.ServiceRecoveryServicesProviderConfig{
    			Properties: map[string]any{"authentication_identity_input": map[string]any{"aad_authority": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceRecoveryServicesProvider } from "./local-sdk/azure_recoveryservicessiterecovery_service/sdk/typescript/azure_recoveryservicessiterecovery_service/recoveryservicessiterecovery/service_recovery_services_provider.ts";

    export default stack("example", () => {
      intent({ summary: "example own service recovery services provider" });

      resource(ServiceRecoveryServicesProvider, "example", {
        properties: { authenticationIdentityInput: { aadAuthority: "example" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_recoveryservicessiterecovery_service --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_recoveryservicessiterecovery_service/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_recoveryservicessiterecovery_service.recoveryservicessiterecovery import ServiceRecoveryServicesProvider, ServiceRecoveryServicesProviderConfig

    def describe():
        ubx.intent("example own service recovery services provider")

        ubx.resource(ServiceRecoveryServicesProvider, "example", ServiceRecoveryServicesProviderConfig(
            properties={"authentication_identity_input": {"aad_authority": "example"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_recoveryservicessiterecovery_service_recovery_services_provider,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object" required>
  The properties of an add provider request. Required.
  <Expandable title="properties">
    <ResponseField name="authentication_identity_input" type="object" required>
      Identity provider input. Required.
      <Expandable title="properties">
        <ResponseField name="aad_authority" type="string" required>
          The base authority for Azure Active Directory authentication. Required.
        </ResponseField>
        <ResponseField name="application_id" type="string" required>
          The application/client Id for the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
        <ResponseField name="audience" type="string" required>
          The intended Audience of the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
        <ResponseField name="object_id" type="string" required>
          The object Id of the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
        <ResponseField name="tenant_id" type="string" required>
          The tenant Id for the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="bios_id" type="string">
      The Bios Id of the machine. Optional.
    </ResponseField>
    <ResponseField name="data_plane_authentication_identity_input" type="object">
      Identity provider input. Optional.
      <Expandable title="properties">
        <ResponseField name="aad_authority" type="string" required>
          The base authority for Azure Active Directory authentication. Required.
        </ResponseField>
        <ResponseField name="application_id" type="string" required>
          The application/client Id for the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
        <ResponseField name="audience" type="string" required>
          The intended Audience of the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
        <ResponseField name="object_id" type="string" required>
          The object Id of the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
        <ResponseField name="tenant_id" type="string" required>
          The tenant Id for the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="machine_id" type="string">
      The Id of the machine where the provider is getting added. Optional.
    </ResponseField>
    <ResponseField name="machine_name" type="string" required>
      The name of the machine where the provider is getting added. Required.
    </ResponseField>
    <ResponseField name="resource_access_identity_input" type="object" required>
      Identity provider input. Required.
      <Expandable title="properties">
        <ResponseField name="aad_authority" type="string" required>
          The base authority for Azure Active Directory authentication. Required.
        </ResponseField>
        <ResponseField name="application_id" type="string" required>
          The application/client Id for the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
        <ResponseField name="audience" type="string" required>
          The intended Audience of the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
        <ResponseField name="object_id" type="string" required>
          The object Id of the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
        <ResponseField name="tenant_id" type="string" required>
          The tenant Id for the service principal with which the on-premise management/data plane components would communicate with our Azure services. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location" type="string">
  Resource Location Computed by Microsoft Azure after `ship`.
</ResponseField>
