---
title: "azurerm_resource_provider_feature_registration"
description: "Real, generated bindings for `azurerm_resource_provider_feature_registration`."
---

`azurerm_resource_provider_feature_registration` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/resource"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own provider feature registration"})

    		ubx.Resource(resource.ProviderFeatureRegistration, "ci-runner", resource.ProviderFeatureRegistrationConfig{
    			Name:         "ci-runner",
    			ProviderName: "example-provider-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProviderFeatureRegistration } from "jsr:@ubx/sdk-azure/azurerm/resource/provider_feature_registration";

    export default stack("payments", () => {
      intent({ summary: "payments own provider feature registration" });

      resource(ProviderFeatureRegistration, "ci-runner", {
        name: "ci-runner",
        providerName: "example-provider-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.resource import ProviderFeatureRegistration, ProviderFeatureRegistrationConfig

    def describe():
        ubx.intent("payments own provider feature registration")

        ubx.resource(ProviderFeatureRegistration, "ci-runner", ProviderFeatureRegistrationConfig(
            name="ci-runner",
            provider_name="example-provider-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_resource_provider_feature_registration called
    "ci-runner", with provider name example-provider-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
