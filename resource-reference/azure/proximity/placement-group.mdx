---
title: "azure_proximity_placement_group"
description: "`azure_proximity_placement_group` pulls compute resources physically together in the same datacenter, the opposite goal of an availability set or zone..."
---

`azure_proximity_placement_group` pulls compute resources physically together in the same datacenter, the opposite goal of an availability set or zone, which deliberately spread VMs apart for resilience. It exists for workloads where network latency between tiers matters more than fault isolation -- a database tier and an application tier on different hardware types can still be pinned into the same proximity group. The group is a colocation constraint, not a capacity guarantee: it's pinned to a datacenter by whichever resource uses it first, and stays pinned until every resource in the group is stopped or deleted.

Real, typed bindings generated directly from the real Azure Resource Manager API specification, in every SDK language -- every tab below is a complete, runnable program, not a fragment, real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	proximity "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/proximity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own placement group"})

    		ubx.Resource(proximity.PlacementGroup, "ci-runner", proximity.PlacementGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PlacementGroup } from "./local-sdk/azure/sdk/typescript/azure/proximity/placement_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own placement group" });

      resource(PlacementGroup, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.proximity import PlacementGroup, PlacementGroupConfig

    def describe():
        ubx.intent("payments own placement group")

        ubx.resource(PlacementGroup, "ci-runner", PlacementGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_proximity_placement_group, with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="properties" type="object">
  Describes the properties of a Proximity Placement Group. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="availability_sets" type="list(object)">
      A list of references to all availability sets in the proximity placement group. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="colocation_status" type="object">
          Optional; if omitted, computed by Azure.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The machine-readable status code for the colocation status of the availability set within the proximity placement group. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the colocation status, indicating whether the message is informational (Info), a warning (Warning), or an error (Error). Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing details about the colocation status of the availability set. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The time (in UTC) when the colocation status was last observed, typically in ISO 8601 format. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="colocation_status" type="object">
      Instance view status. Optional.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          The status code. Optional.
        </ResponseField>
        <ResponseField name="display_status" type="string">
          The short localizable label for the status. Optional.
        </ResponseField>
        <ResponseField name="level" type="string">
          The level code. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          The detailed status message, including for alerts and error messages. Optional.
        </ResponseField>
        <ResponseField name="time" type="string">
          The time of the status. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="intent" type="object">
      Specifies the user intent of the proximity placement group. Optional.
      <Expandable title="properties">
        <ResponseField name="vm_sizes" type="list(string)">
          Specifies possible sizes of virtual machines that can be created in the proximity placement group. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="proximity_placement_group_type" type="string">
      Specifies the type of the proximity placement group. Possible values are: **Standard** : Co-locate resources within an Azure region or Availability Zone. **Ultra** : For future use. Optional.
    </ResponseField>
    <ResponseField name="virtual_machine_scale_sets" type="list(object)">
      A list of references to all virtual machine scale sets in the proximity placement group. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="colocation_status" type="object">
          The colocation status of the virtual machine scale set within the proximity placement group, indicating whether the scale set's instances are co-located in the same data center. Optional; if omitted, computed by Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              A service-defined machine-readable code representing the colocation status of the virtual machine scale set in the proximity placement group. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              A human-readable status of the colocation status for the virtual machine scale set in this proximity placement group, such as whether it is colocated or not. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the colocation status. Possible values are Info, Warning, and Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing additional details about the colocation status of the virtual machine scale set. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The timestamp (in ISO 8601 format) indicating when the colocation status was last updated. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_machines" type="list(object)">
      A list of references to all virtual machines in the proximity placement group. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="colocation_status" type="object">
          Optional; if omitted, computed by Azure.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The status code for the colocation status of the virtual machine in the proximity placement group. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              A localized, human-readable description of the colocation status for the virtual machine. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the colocation status for the virtual machine in the proximity placement group. Possible values are 'Info', 'Warning', and 'Error'. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing additional details about the colocation status of the virtual machines in the proximity placement group. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The availability zones. Optional; if omitted, computed by Azure.
</ResponseField>

## Output properties

<ResponseField name="properties" type="object">
  Describes the properties of a Proximity Placement Group. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="availability_sets" type="list(object)">
      A list of references to all availability sets in the proximity placement group. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="colocation_status" type="object">
          Optional; if omitted, computed by Azure.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The machine-readable status code for the colocation status of the availability set within the proximity placement group. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the colocation status, indicating whether the message is informational (Info), a warning (Warning), or an error (Error). Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing details about the colocation status of the availability set. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The time (in UTC) when the colocation status was last observed, typically in ISO 8601 format. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="colocation_status" type="object">
      Instance view status. Optional.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          The status code. Optional.
        </ResponseField>
        <ResponseField name="display_status" type="string">
          The short localizable label for the status. Optional.
        </ResponseField>
        <ResponseField name="level" type="string">
          The level code. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          The detailed status message, including for alerts and error messages. Optional.
        </ResponseField>
        <ResponseField name="time" type="string">
          The time of the status. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="intent" type="object">
      Specifies the user intent of the proximity placement group. Optional.
      <Expandable title="properties">
        <ResponseField name="vm_sizes" type="list(string)">
          Specifies possible sizes of virtual machines that can be created in the proximity placement group. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="proximity_placement_group_type" type="string">
      Specifies the type of the proximity placement group. Possible values are: **Standard** : Co-locate resources within an Azure region or Availability Zone. **Ultra** : For future use. Optional.
    </ResponseField>
    <ResponseField name="virtual_machine_scale_sets" type="list(object)">
      A list of references to all virtual machine scale sets in the proximity placement group. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="colocation_status" type="object">
          The colocation status of the virtual machine scale set within the proximity placement group, indicating whether the scale set's instances are co-located in the same data center. Optional; if omitted, computed by Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              A service-defined machine-readable code representing the colocation status of the virtual machine scale set in the proximity placement group. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              A human-readable status of the colocation status for the virtual machine scale set in this proximity placement group, such as whether it is colocated or not. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the colocation status. Possible values are Info, Warning, and Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing additional details about the colocation status of the virtual machine scale set. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The timestamp (in ISO 8601 format) indicating when the colocation status was last updated. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_machines" type="list(object)">
      A list of references to all virtual machines in the proximity placement group. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="colocation_status" type="object">
          Optional; if omitted, computed by Azure.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The status code for the colocation status of the virtual machine in the proximity placement group. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              A localized, human-readable description of the colocation status for the virtual machine. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the colocation status for the virtual machine in the proximity placement group. Possible values are 'Info', 'Warning', and 'Error'. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing additional details about the colocation status of the virtual machines in the proximity placement group. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The availability zones. Optional; if omitted, computed by Azure.
</ResponseField>
