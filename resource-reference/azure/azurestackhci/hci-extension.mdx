---
title: "azure_azurestackhci_hci_extension"
description: "`azure_azurestackhci_hci_extension` installs an Azure Arc extension onto an Azure Stack HCI cluster's own node(s), e.g. for monitoring or Kubernetes."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hci extension"})

    		ubx.Resource(azurestackhci.HciExtension, "example", azurestackhci.HciExtensionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HciExtension } from "@ubx/sdk-azure/azure/azurestackhci/hci_extension";

    export default stack("example", () => {
      intent({ summary: "example own hci extension" });

      resource(HciExtension, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.azurestackhci.hci_extension import HciExtension, HciExtensionConfig

    def describe():
        ubx.intent("example own hci extension")

        ubx.resource(HciExtension, "example", HciExtensionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_azurestackhci_hci_extension, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Status of Arc Extension for a particular node in HCI Cluster. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="aggregate_state" type="string">
      Aggregate state of Arc Extensions across the nodes in this HCI cluster. This reflects the overall status of the extension deployment and operation across all nodes. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="extension_parameters" type="object">
      Describes the properties of a Machine Extension. This object mirrors the definition in HybridCompute. Optional.
      <Expandable title="properties">
        <ResponseField name="auto_upgrade_minor_version" type="bool">
          Indicates whether the extension should use a newer minor version if one is available at deployment time. Once deployed, however, the extension will not upgrade minor versions unless redeployed, even with this property set to true. Optional.
        </ResponseField>
        <ResponseField name="enable_automatic_upgrade" type="bool">
          Indicates whether the extension should be automatically upgraded by the platform if there is a newer version available. Optional.
        </ResponseField>
        <ResponseField name="force_update_tag" type="string">
          How the extension handler should be forced to update even if the extension configuration has not changed. Optional.
        </ResponseField>
        <ResponseField name="protected_settings" type="any">
          Protected settings (may contain secrets). Optional.
        </ResponseField>
        <ResponseField name="publisher" type="string">
          The name of the extension handler publisher. Optional.
        </ResponseField>
        <ResponseField name="settings" type="any">
          Json formatted public settings for the extension. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the type of the extension; an example is "CustomScriptExtension". Optional.
        </ResponseField>
        <ResponseField name="type_handler_version" type="string">
          Specifies the version of the script handler. Latest version would be used if not specified. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="managed_by" type="string">
      Indicates whether the extension is managed by the user or by Azure. Optional.
    </ResponseField>
    <ResponseField name="per_node_extension_details" type="list(object)">
      State of Arc Extension in each of the nodes. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="extension" type="string">
          A reference to the cluster extension this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instance_view" type="object">
          The current runtime status of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="status" type="object">
              The current status of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  A machine-readable status or error code. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="display_status" type="string">
                  A human-readable summary of this resource's own current status. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="level" type="string">
                  The severity level of this event. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="time" type="string">
                  The time this event occurred. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="type" type="string">
              The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type_handler_version" type="string">
              The version of the extension handler processing this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The current lifecycle state of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type_handler_version" type="string">
          The version of the extension handler processing this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the ArcSetting proxy resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
