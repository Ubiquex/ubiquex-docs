---
title: "azure_azurestackhci_hci_edge_device_job"
description: "`azure_azurestackhci_hci_edge_device_job` tracks an asynchronous operation (e.g. a firmware update) running against an Azure Stack HCI edge device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hci edge device job"})

    		ubx.Resource(azurestackhci.HciEdgeDeviceJob, "example", azurestackhci.HciEdgeDeviceJobConfig{
    			Kind: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HciEdgeDeviceJob } from "@ubx/sdk-azure/azure/azurestackhci/hci_edge_device_job";

    export default stack("example", () => {
      intent({ summary: "example own hci edge device job" });

      resource(HciEdgeDeviceJob, "example", {
        kind: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.azurestackhci.hci_edge_device_job import HciEdgeDeviceJob, HciEdgeDeviceJobConfig

    def describe():
        ubx.intent("example own hci edge device job")

        ubx.resource(HciEdgeDeviceJob, "example", HciEdgeDeviceJobConfig(
            kind="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_azurestackhci_hci_edge_device_job, with kind
    example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string" required>
  Edge device kind. Required.
</ResponseField>
