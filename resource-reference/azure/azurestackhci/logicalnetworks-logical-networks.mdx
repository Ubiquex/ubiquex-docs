---
title: "azure_azurestackhci_logicalnetworks_logical_networks"
description: "`azure_azurestackhci_logicalnetworks_logical_networks` defines a logical network on Azure Arc-enabled Azure Stack HCI..."
---

`azure_azurestackhci_logicalnetworks_logical_networks` defines a logical network on Azure Arc-enabled Azure Stack HCI, that VM(s) provisioned through Azure tooling are attached to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own logicalnetworks logical networks"})

    		ubx.Resource(azurestackhci.LogicalnetworksLogicalNetworks, "example", azurestackhci.LogicalnetworksLogicalNetworksConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogicalnetworksLogicalNetworks } from "@ubx/sdk-azure/azure/azurestackhci/logicalnetworks_logical_networks";

    export default stack("example", () => {
      intent({ summary: "example own logicalnetworks logical networks" });

      resource(LogicalnetworksLogicalNetworks, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.azurestackhci.logicalnetworks_logical_networks import LogicalnetworksLogicalNetworks, LogicalnetworksLogicalNetworksConfig

    def describe():
        ubx.intent("example own logicalnetworks logical networks")

        ubx.resource(LogicalnetworksLogicalNetworks, "example", LogicalnetworksLogicalNetworksConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_azurestackhci_logicalnetworks_logical_networks, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="extended_location" type="object">
  The complex type of the extended location. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of the extended location. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of extendedLocation. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties under the logical network resource Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="dhcp_options" type="object">
      DhcpOptions contains an array of DNS servers available to VMs deployed in the logical network. Standard DHCP option for a subnet overrides logical network DHCP options. Optional.
      <Expandable title="properties">
        <ResponseField name="dns_servers" type="list(string)">
          The list of DNS servers IP addresses. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the logical network. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="status" type="object">
      The observed state of logical networks Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="error_code" type="string">
          LogicalNetwork provisioning error code Optional.
        </ResponseField>
        <ResponseField name="error_message" type="string">
          Descriptive error message Optional.
        </ResponseField>
        <ResponseField name="provisioning_status" type="object">
          The current provisioning status of this resource. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="operation_id" type="string">
              The ID of the operation performed on the logical network Optional.
            </ResponseField>
            <ResponseField name="status" type="string">
              The status of the operation performed on the logical network [Succeeded, Failed, InProgress] Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="subnets" type="list(object)">
      Subnet - list of subnets under the logical network Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vm_switch_name" type="string">
      name of the network switch to be used for VMs Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
