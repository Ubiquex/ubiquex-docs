---
title: "azure_recoveryservicesbackup_bms_operation_status"
description: "`azure_recoveryservicesbackup_bms_operation_status` reports the outcome of a completed Backup Management Service operation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicesbackup "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/recoveryservicesbackup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own bms operation status"})

    		ubx.Resource(recoveryservicesbackup.BmsOperationStatus, "example", recoveryservicesbackup.BmsOperationStatusConfig{
    			SourceScanAction: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BmsOperationStatus } from "@ubx/sdk-azure/azure/recoveryservicesbackup/bms_operation_status";

    export default stack("example", () => {
      intent({ summary: "example own bms operation status" });

      resource(BmsOperationStatus, "example", {
        sourceScanAction: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.recoveryservicesbackup import BmsOperationStatus, BmsOperationStatusConfig

    def describe():
        ubx.intent("example own bms operation status")

        ubx.resource(BmsOperationStatus, "example", BmsOperationStatusConfig(
            source_scan_action="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_recoveryservicesbackup_bms_operation_status, with
    source scan action example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="source_scan_action" type="string">
  Source scan configuration status for enabling or disabling source scan Optional.
</ResponseField>

## Output properties

<ResponseField name="end_time" type="string">
  Operation end time. Format: ISO-8601. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="error" type="object">
  Error information associated with operation status call. Computed by Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
      Error code of the operation failure. Optional.
    </ResponseField>
    <ResponseField name="message" type="string">
      Error message displayed if the operation failure. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  ID of the operation. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the operation. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  Base class for additional information of operation status. Computed by Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="object_type" type="string" required>
      This property will be used as the discriminator for deciding the specific types in the polymorphic chain of types. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_time" type="string">
  Operation start time. Format: ISO-8601. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Operation status. Computed by Azure after `ship`.
</ResponseField>
