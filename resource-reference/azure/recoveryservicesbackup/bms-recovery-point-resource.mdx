---
title: "azure_recoveryservicesbackup_bms_recovery_point_resource"
description: "`azure_recoveryservicesbackup_bms_recovery_point_resource` is a single..."
---

`azure_recoveryservicesbackup_bms_recovery_point_resource` is a single, real recovery point (a restorable snapshot in time) created by an Azure Backup backup job for a protected item.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	recoveryservicesbackup "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/recoveryservicesbackup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own bms recovery point resource"})

    		ubx.Resource(recoveryservicesbackup.BmsRecoveryPointResource, "example", recoveryservicesbackup.BmsRecoveryPointResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BmsRecoveryPointResource } from "./local-sdk/azure/sdk/typescript/azure/recoveryservicesbackup/bms_recovery_point_resource.ts";

    export default stack("example", () => {
      intent({ summary: "example own bms recovery point resource" });

      resource(BmsRecoveryPointResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.recoveryservicesbackup import BmsRecoveryPointResource, BmsRecoveryPointResourceConfig

    def describe():
        ubx.intent("example own bms recovery point resource")

        ubx.resource(BmsRecoveryPointResource, "example", BmsRecoveryPointResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_recoveryservicesbackup_bms_recovery_point_resource,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="e_tag" type="string">
  Optional ETag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="location" type="string">
  Represents an Azure geography region where supported resource providers live. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Base class for restore request. Workload-specific restore requests are derived from this class. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="object_type" type="string" required>
      This property will be used as the discriminator for deciding the specific types in the polymorphic chain of types. Required.
    </ResponseField>
    <ResponseField name="resource_guard_operation_requests" type="list(string)">
      ResourceGuardOperationRequests on which LAC check will be performed Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
