---
title: "azure_netapp_backup_policy"
description: "`azure_netapp_netapp_backup_policy` defines the automated backup schedule (frequency, retention) applied to Azure NetApp Files volume(s) it's attached to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_netapp_netapp --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_netapp_netapp/sdk/go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_netapp_netapp/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own netapp backup policy"})

    		ubx.Resource(netapp.NetappBackupPolicy, "example", netapp.NetappBackupPolicyConfig{
    			Properties: map[string]any{"backup_policy_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_netapp_netapp --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetappBackupPolicy } from "./local-sdk/azure_netapp_netapp/sdk/typescript/azure_netapp_netapp/netapp/netapp_backup_policy.ts";

    export default stack("example", () => {
      intent({ summary: "example own netapp backup policy" });

      resource(NetappBackupPolicy, "example", {
        properties: { backupPolicyId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_netapp_netapp --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_netapp_netapp/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_netapp_netapp.netapp import NetappBackupPolicy, NetappBackupPolicyConfig

    def describe():
        ubx.intent("example own netapp backup policy")

        ubx.resource(NetappBackupPolicy, "example", NetappBackupPolicyConfig(
            properties={"backup_policy_id": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_backup_policy, with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  "If etag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.") Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Backup policy properties Required.
  <Expandable title="properties">
    <ResponseField name="backup_policy_id" type="string">
      Backup Policy GUID ID Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="daily_backups_to_keep" type="number">
      Daily backups count to keep Optional.
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      The property to decide policy is enabled or not Optional.
    </ResponseField>
    <ResponseField name="monthly_backups_to_keep" type="number">
      Monthly backups count to keep Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Azure lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="volume_backups" type="list(object)">
      A list of volumes assigned to this policy Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="backups_count" type="number">
          The number of backup(s) associated with this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="policy_enabled" type="bool">
          Whether this policy is currently active. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="volume_name" type="string">
          The name of this volume. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="volume_resource_id" type="string">
          A reference to the volume this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="volumes_assigned" type="number">
      Volumes using current backup policy Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="weekly_backups_to_keep" type="number">
      Weekly backups count to keep Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
