---
title: "azure_netapp_bucket"
description: "`azure_netapp_netapp_bucket` exposes a mapped directory within an Azure NetApp Files volume for S3-compatible object access via the object REST API..."
---

`azure_netapp_netapp_bucket` exposes a mapped directory within an Azure NetApp Files volume for S3-compatible object access via the object REST API, letting the same file data be reached over both NFS/SMB and object protocols.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own netapp bucket"})

    		ubx.Resource(netapp.Bucket, "example", netapp.BucketConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Bucket } from "@ubx/sdk-azure/azure/netapp/bucket";

    export default stack("example", () => {
      intent({ summary: "example own netapp bucket" });

      resource(Bucket, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.netapp import Bucket, BucketConfig

    def describe():
        ubx.intent("example own netapp bucket")

        ubx.resource(Bucket, "example", BucketConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_bucket, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Bucket resource properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="akv_details" type="object">
      Specifies the Azure Key Vault settings. These are used when a) retrieving the bucket server certificate, and b) storing the bucket credentials Notes: 1. If a bucket certificate was previously provided directly using the certificateObject property, it is possible to subsequently use the Azure Key Vault for certificate management by using these 'akvDetails' properties. However, once Azure Key Vault is configured, it is no longer possible to provide the certificate directly via the certificateObject property. 2. These properties are mutually exclusive with the server.certificateObject property. Optional.
      <Expandable title="properties">
        <ResponseField name="certificate_akv_details" type="object">
          Specifies the Azure Key Vault settings for retrieving the bucket server certificate. Optional.
          <Expandable title="properties">
            <ResponseField name="certificate_key_vault_uri" type="string">
              The base URI of the Azure Key Vault that is used when retrieving the bucket certificate. Optional.
            </ResponseField>
            <ResponseField name="certificate_name" type="string">
              The name of the bucket server certificate stored in the Azure Key Vault. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="credentials_akv_details" type="object">
          Specifies the Azure Key Vault settings for storing the bucket credentials. Optional.
          <Expandable title="properties">
            <ResponseField name="credentials_key_vault_uri" type="string">
              The base URI of the Azure Key Vault that is used when storing the bucket credentials. Optional.
            </ResponseField>
            <ResponseField name="secret_name" type="string">
              The name of the secret stored in Azure Key Vault. The associated key pair has the following structure: &#123; "access_key_id": "&lt;REDACTED&gt;", "secret_access_key": "&lt;REDACTED&gt;" &#125; Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="file_system_user" type="object">
      File System user having access to volume data. For Unix, this is the user's uid and gid. For Windows, this is the user's username. Note that the Unix and Windows user details are mutually exclusive, meaning one or other must be supplied, but not both. Optional.
      <Expandable title="properties">
        <ResponseField name="cifs_user" type="object">
          The effective CIFS username when accessing the volume data. Optional.
          <Expandable title="properties">
            <ResponseField name="username" type="string">
              The CIFS user's username Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="nfs_user" type="object">
          The effective NFS User ID and Group ID when accessing the volume data. Optional.
          <Expandable title="properties">
            <ResponseField name="group_id" type="number">
              The NFS user's GID Optional.
            </ResponseField>
            <ResponseField name="user_id" type="number">
              The NFS user's UID Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="path" type="string">
      The volume path mounted inside the bucket. The default is the root path '/' if no value is provided when the bucket is created. Optional.
    </ResponseField>
    <ResponseField name="permissions" type="string">
      Access permissions for the bucket. Either ReadOnly or ReadWrite. The default is ReadOnly if no value is provided during bucket creation. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Gets the status of the VolumeQuotaRule at the time the operation was called. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="server" type="object">
      Properties of the server managing the lifecycle of volume buckets Optional.
      <Expandable title="properties">
        <ResponseField name="certificate_common_name" type="string">
          Certificate Common Name taken from the certificate installed on the bucket server Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="certificate_expiry_date" type="string">
          The bucket server's certificate expiry date. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="certificate_object" type="string">
          The base64-encoded contents of a PEM file, which includes both the bucket server's certificate and private key. It is generated by the end user and allows the user to access volume data in a read-only manner. Note: This is only used when Azure Key Vault is not configured. This property is mutually exclusive with the Azure Key Vault 'akv' properties. Optional.
        </ResponseField>
        <ResponseField name="fqdn" type="string">
          The host part of the bucket URL, resolving to the bucket IP address and allowed by the server certificate. Optional.
        </ResponseField>
        <ResponseField name="ip_address" type="string">
          The bucket server's IPv4 address Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="on_certificate_conflict_action" type="string">
          This action is triggered when a certificate conflict occurs. A conflict arises if you try to create a new bucket while one or more already exist on the server, or if you update a bucket when multiple buckets are present. This happens because a single certificate is shared among all buckets on the same server. Note: This applies both to certificates provided directly via the certificateObject property and to those retrieved from Azure Key Vault. Details for the latter case are specified in the akvDetails.certificateAkvDetails section. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="status" type="string">
      The bucket credentials status. There states: "NoCredentialsSet": Access and Secret key pair have not been generated. "CredentialsExpired": Access and Secret key pair have expired. "Active": The certificate has been installed and credentials are unexpired. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
