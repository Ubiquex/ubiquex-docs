---
title: "azure_netapp_volume_quota_rule"
description: "`azure_netapp_volume_quota_rule` sets a per-user or per-group storage quota on an Azure NetApp Files volume."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own netapp volume quota rule"})

    		ubx.Resource(netapp.VolumeQuotaRule, "example", netapp.VolumeQuotaRuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VolumeQuotaRule } from "@ubx/sdk-azure/azure/netapp/volume_quota_rule";

    export default stack("example", () => {
      intent({ summary: "example own netapp volume quota rule" });

      resource(VolumeQuotaRule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.netapp.volume_quota_rule import VolumeQuotaRule, VolumeQuotaRuleConfig

    def describe():
        ubx.intent("example own netapp volume quota rule")

        ubx.resource(VolumeQuotaRule, "example", VolumeQuotaRuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_volume_quota_rule, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Volume Quota Rule properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Gets the status of the VolumeQuotaRule at the time the operation was called. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="quota_size_in_ki_bs" type="number">
      Size of quota Optional.
    </ResponseField>
    <ResponseField name="quota_target" type="string">
      UserID/GroupID/SID based on the quota target type. UserID and groupID can be found by running ‘id’ or ‘getent’ command for the user or group and SID can be found by running &lt;wmic useraccount where name='user-name' get sid&gt; Optional.
    </ResponseField>
    <ResponseField name="quota_type" type="string">
      Type of quota Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
