---
title: "azurerm_netapp_snapshot"
description: "Real, generated bindings for `azurerm_netapp_snapshot`."
---

`azurerm_netapp_snapshot` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own snapshot"})

    		ubx.Resource(netapp.Snapshot, "ci-runner", netapp.SnapshotConfig{
    			AccountName:       "example-account-name",
    			Location:          "example",
    			Name:              "ci-runner",
    			PoolName:          "example-pool-name",
    			ResourceGroupName: "example-resource-group-name",
    			VolumeName:        "example-volume-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Snapshot } from "jsr:@ubx/sdk-azure/azurerm/netapp/snapshot";

    export default stack("payments", () => {
      intent({ summary: "payments own snapshot" });

      resource(Snapshot, "ci-runner", {
        accountName: "example-account-name",
        location: "example",
        name: "ci-runner",
        poolName: "example-pool-name",
        resourceGroupName: "example-resource-group-name",
        volumeName: "example-volume-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.netapp import Snapshot, SnapshotConfig

    def describe():
        ubx.intent("payments own snapshot")

        ubx.resource(Snapshot, "ci-runner", SnapshotConfig(
            account_name="example-account-name",
            location="example",
            name="ci-runner",
            pool_name="example-pool-name",
            resource_group_name="example-resource-group-name",
            volume_name="example-volume-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_netapp_snapshot called "ci-runner", with account
    name example-account-name, location example, pool name
    example-pool-name, resource group name
    example-resource-group-name, volume name example-volume-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="pool_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="volume_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
