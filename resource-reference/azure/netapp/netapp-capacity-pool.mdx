---
title: "azure_netapp_capacity_pool"
description: "`azure_netapp_capacity_pool` is the underlying provisioned capacity and performance tier in Azure NetApp Files that individual volume(s)..."
---

`azure_netapp_capacity_pool` is the underlying provisioned capacity and performance tier in Azure NetApp Files that individual volume(s) are carved out of.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own capacity pool"})

    		ubx.Resource(netapp.CapacityPool, "example", netapp.CapacityPoolConfig{
    			Properties: map[string]any{"service_level": "example", "size": 1},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CapacityPool } from "@ubx/sdk-azure/azure/netapp/capacity_pool";

    export default stack("example", () => {
      intent({ summary: "example own capacity pool" });

      resource(CapacityPool, "example", {
        properties: { serviceLevel: "example", size: 1 },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.netapp import CapacityPool, CapacityPoolConfig
    from ubx.azure.netapp.capacity_pool import CapacityPool_Properties

    def describe():
        ubx.intent("example own capacity pool")

        ubx.resource(CapacityPool, "example", CapacityPoolConfig(
            properties=CapacityPool_Properties(service_level="example", size=1),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_capacity_pool, with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  "If etag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.") Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Pool properties Required.
  <Expandable title="properties">
    <ResponseField name="cool_access" type="bool">
      If enabled (true) the pool can contain cool Access enabled volumes. Optional.
    </ResponseField>
    <ResponseField name="custom_throughput_mibps" type="number">
      Maximum throughput in MiB/s that can be achieved by this pool and this will be accepted as input only for manual qosType pool with Flexible service level Optional.
    </ResponseField>
    <ResponseField name="encryption_type" type="string">
      Encryption type of the capacity pool, set encryption type for data at rest for this pool and all volumes in it. This value can only be set when creating new pool. Optional.
    </ResponseField>
    <ResponseField name="pool_id" type="string">
      UUID v4 used to identify the Pool Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Azure lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="qos_type" type="string">
      The qos type of the pool Optional.
    </ResponseField>
    <ResponseField name="service_level" type="string" required>
      The service level of the file system Required.
    </ResponseField>
    <ResponseField name="size" type="number" required>
      Provisioned size of the pool (in bytes). Allowed values are in 1TiB chunks (value must be multiple of 1099511627776). Required.
    </ResponseField>
    <ResponseField name="total_throughput_mibps" type="number">
      Total throughput of pool in MiB/s Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="utilized_throughput_mibps" type="number">
      Utilized throughput of pool in MiB/s Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
