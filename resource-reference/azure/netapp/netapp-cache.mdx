---
title: "azure_netapp_cache"
description: "`azure_netapp_cache` accelerates access to a remote data source by caching its own content on Azure NetApp Files storage close to compute."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_netapp_netapp --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_netapp_netapp/sdk/go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_netapp_netapp/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own netapp cache"})

    		ubx.Resource(netapp.NetappCache, "example", netapp.NetappCacheConfig{
    			Properties: map[string]any{"cache_subnet_resource_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_netapp_netapp --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetappCache } from "./local-sdk/azure_netapp_netapp/sdk/typescript/azure_netapp_netapp/netapp/netapp_cache.ts";

    export default stack("example", () => {
      intent({ summary: "example own netapp cache" });

      resource(NetappCache, "example", {
        properties: { cacheSubnetResourceId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_netapp_netapp --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_netapp_netapp/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_netapp_netapp.netapp import NetappCache, NetappCacheConfig

    def describe():
        ubx.intent("example own netapp cache")

        ubx.resource(NetappCache, "example", NetappCacheConfig(
            properties={"cache_subnet_resource_id": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_cache, with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  "If etag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.") Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Cache resource properties Required.
  <Expandable title="properties">
    <ResponseField name="actual_throughput_mibps" type="number">
      Actual throughput in MiB/s for auto qosType volumes calculated based on size and serviceLevel Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="cache_state" type="string">
      Azure NetApp Files Cache lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="cache_subnet_resource_id" type="string" required>
      The Azure Resource URI for a delegated cache subnet that will be used to allocate data IPs. Required.
    </ResponseField>
    <ResponseField name="cifs_change_notifications" type="string">
      Flag indicating whether a CIFS change notification is enabled for the cache. Optional.
    </ResponseField>
    <ResponseField name="encryption" type="string">
      Specifies if the cache is encryption or not. Optional.
    </ResponseField>
    <ResponseField name="encryption_key_source" type="string" required>
      Source of key used to encrypt data in volume. Applicable if NetApp account has encryption.keySource = 'Microsoft.KeyVault'. Possible values (case-insensitive) are: 'Microsoft.NetApp, Microsoft.KeyVault' Required.
    </ResponseField>
    <ResponseField name="export_policy" type="object">
      Set of export policy rules Optional.
      <Expandable title="properties">
        <ResponseField name="rules" type="list(object)">
          Export policy rule Optional.
          <Expandable title="properties">
            <ResponseField name="allowed_clients" type="string">
              The client IP address range(s), in CIDR notation, permitted to access this export. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="chown_mode" type="string">
              Who is permitted to change file ownership on this volume, e.g. only root or any user. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="cifs" type="bool">
              Whether the SMB/CIFS protocol is enabled for this volume. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="has_root_access" type="bool">
              Whether this client is granted root access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5_read_only" type="bool">
              Whether Kerberos v5 authenticated clients are granted read-only access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5_read_write" type="bool">
              Whether Kerberos v5 authenticated clients are granted read-write access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5i_read_only" type="bool">
              Whether Kerberos v5 with integrity checking authenticated clients are granted read-only access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5i_read_write" type="bool">
              Whether Kerberos v5 with integrity checking authenticated clients are granted read-write access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5p_read_only" type="bool">
              Whether Kerberos v5 with privacy protection authenticated clients are granted read-only access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5p_read_write" type="bool">
              Whether Kerberos v5 with privacy protection authenticated clients are granted read-write access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nfsv3" type="bool">
              Whether the NFSv3 protocol is enabled for this volume. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nfsv41" type="bool">
              Whether the NFSv4.1 protocol is enabled for this volume. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rule_index" type="number">
              This rule's own relative evaluation order. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="unix_read_only" type="bool">
              Whether UNIX clients are granted read-only access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="unix_read_write" type="bool">
              Whether UNIX clients are granted read-write access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="file_access_logs" type="string">
      Flag indicating whether file access logs are enabled for the Cache, based on active diagnostic settings present on the Cache. Optional.
    </ResponseField>
    <ResponseField name="file_path" type="string" required>
      The file path of the Cache. Required.
    </ResponseField>
    <ResponseField name="global_file_locking" type="string">
      Flag indicating whether the global file lock is enabled for the cache. Optional.
    </ResponseField>
    <ResponseField name="kerberos" type="string">
      Describe if a cache is Kerberos enabled. Optional.
    </ResponseField>
    <ResponseField name="key_vault_private_endpoint_resource_id" type="string">
      The resource ID of private endpoint for KeyVault. It must reside in the same VNET as the volume. Only applicable if encryptionKeySource = 'Microsoft.KeyVault'. Optional.
    </ResponseField>
    <ResponseField name="language" type="string">
      Language supported for volume. Optional.
    </ResponseField>
    <ResponseField name="ldap" type="string">
      Specifies whether LDAP is enabled or not. Optional.
    </ResponseField>
    <ResponseField name="ldap_server_type" type="string">
      The type of the LDAP server Optional.
    </ResponseField>
    <ResponseField name="maximum_number_of_files" type="number">
      Maximum number of files allowed. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="mount_targets" type="list(object)">
      List of mount targets that can be used to mount this cache Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="ip_address" type="string">
          An IP address value. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mount_target_id" type="string">
          A unique identifier for this volume's own mount target. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="smb_server_fqdn" type="string">
          The fully qualified domain name of the SMB server hosting this volume. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="origin_cluster_information" type="object" required>
      Stores the origin cluster information associated to a cache. Required.
      <Expandable title="properties">
        <ResponseField name="peer_addresses" type="list(string)" required>
          ONTAP Intercluster LIF IP addresses. One IP address per cluster node is required Required.
        </ResponseField>
        <ResponseField name="peer_cluster_name" type="string" required>
          ONTAP cluster name of external cluster hosting the origin volume. Must match the exact cluster name. Required.
        </ResponseField>
        <ResponseField name="peer_volume_name" type="string" required>
          External origin volume name associated to this cache Required.
        </ResponseField>
        <ResponseField name="peer_vserver_name" type="string" required>
          External Vserver (SVM) name name of the SVM hosting the origin volume Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="peering_subnet_resource_id" type="string" required>
      The Azure Resource URI for a delegated subnet that will be used for ANF Intercluster Interface IP addresses. Required.
    </ResponseField>
    <ResponseField name="protocol_types" type="list(string)">
      Set of supported protocol types, which include NFSv3, NFSv4 and SMB protocol Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Azure lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="size" type="number" required>
      Maximum storage quota allowed for a file system in bytes. Valid values are in the range 50GiB to 1PiB. Values expressed in bytes as multiples of 1GiB. Required.
    </ResponseField>
    <ResponseField name="smb_settings" type="object">
      SMB settings for the cache Optional.
      <Expandable title="properties">
        <ResponseField name="smb_access_based_enumeration" type="string">
          Enables access-based enumeration share property for SMB Shares. Only applicable for SMB/DualProtocol volume Optional.
        </ResponseField>
        <ResponseField name="smb_encryption" type="string">
          Enables encryption for in-flight smb3 data. Only applicable for SMB/DualProtocol cache Optional.
        </ResponseField>
        <ResponseField name="smb_non_browsable" type="string">
          Enables non-browsable property for SMB Shares. Only applicable for SMB/DualProtocol volume Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="throughput_mibps" type="number">
      Maximum throughput in MiB/s that can be achieved by this cache volume and this will be accepted as input only for manual qosType cache Optional.
    </ResponseField>
    <ResponseField name="write_back" type="string">
      Flag indicating whether writeback is enabled for the cache. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The availability zones. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
