---
title: "azure_netapp_net_app_account"
description: "`azure_netapp_net_app_account` is the top-level Azure NetApp Files resource that capacity pool(s) and their own volume(s) are organized within."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_netapp --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_netapp/sdk/go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_netapp/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own netapp net app account"})

    		ubx.Resource(netapp.NetappNetAppAccount, "example", netapp.NetappNetAppAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_netapp --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetappNetAppAccount } from "./local-sdk/azure_netapp/sdk/typescript/azure_netapp/netapp/netapp_net_app_account.ts";

    export default stack("example", () => {
      intent({ summary: "example own netapp net app account" });

      resource(NetappNetAppAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_netapp --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_netapp/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_netapp.netapp import NetappNetAppAccount, NetappNetAppAccountConfig

    def describe():
        ubx.intent("example own netapp net app account")

        ubx.resource(NetappNetAppAccount, "example", NetappNetAppAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_net_app_account, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  "If etag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.") Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID used to authenticate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  NetApp account properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="active_directories" type="list(object)">
      Active Directories Optional.
      <Expandable title="properties">
        <ResponseField name="active_directory_id" type="string">
          A reference to the Active Directory connection this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ad_name" type="string">
          The NetBIOS name of the Active Directory domain. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="administrators" type="list(string)">
          The account(s) granted administrative access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="aes_encryption" type="bool">
          Whether AES encryption is used for this Active Directory connection. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="allow_local_nfs_users_with_ldap" type="bool">
          Whether local NFS users are permitted to access this volume in addition to LDAP users. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="backup_operators" type="list(string)">
          The account(s) granted permission to manage backups. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dns" type="string">
          The DNS server IP address(es) used for this Active Directory connection. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="domain" type="string">
          The Active Directory domain this connection joins. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="encrypt_dcconnections" type="bool">
          Whether the connection to Domain Controllers is encrypted. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kdc_ip" type="string">
          The IP address of the Kerberos Key Distribution Center. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ldap_over_tls" type="bool">
          Whether LDAP traffic is encrypted using TLS. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ldap_search_scope" type="object">
          The base distinguished name(s) LDAP searches for users and groups are scoped to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="group_dn" type="string">
              The distinguished name of the LDAP group search base. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="group_membership_filter" type="string">
              The LDAP filter used to determine a user's own group memberships. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_dn" type="string">
              The distinguished name of the LDAP user search base. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ldap_signing" type="bool">
          Whether LDAP signing is required for this Active Directory connection. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="organizational_unit" type="string">
          The Active Directory Organizational Unit this connection's own computer objects are placed in. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="password" type="string">
          The password credential used to authenticate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="preferred_servers_for_ldap_client" type="string">
          The Domain Controller server(s) preferred for LDAP client connections. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="security_operators" type="list(string)">
          The account(s) granted permission to manage security settings. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="server_root_cacertificate" type="string">
          The root CA certificate used to validate connections to this LDAP server. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="site" type="string">
          The Active Directory site this connection is associated with. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="smb_server_name" type="string">
          The NetBIOS name of the SMB server hosting this volume. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          The current status of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status_details" type="string">
          Additional detail about this resource's own current status. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="username" type="string">
          The username used to authenticate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="disable_showmount" type="bool">
      Shows the status of disableShowmount for all volumes under the subscription, null equals false Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="encryption" type="object">
      Encryption settings Optional.
      <Expandable title="properties">
        <ResponseField name="identity" type="object">
          Identity used to authenticate with key vault. Optional.
          <Expandable title="properties">
            <ResponseField name="federated_client_id" type="string">
              ClientId of the multi-tenant Entra ID Application. Used to access cross-tenant keyvaults. Optional.
            </ResponseField>
            <ResponseField name="principal_id" type="string">
              The principal ID (object ID) of the identity used to authenticate with key vault. Read-only. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="user_assigned_identity" type="string">
              The ARM resource identifier of the user assigned identity used to authenticate with key vault. Applicable if identity.type has 'UserAssigned'. It should match key of identity.userAssignedIdentities. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="key_source" type="string">
          The encryption keySource (provider). Possible values (case-insensitive): Microsoft.NetApp, Microsoft.KeyVault Optional.
        </ResponseField>
        <ResponseField name="key_vault_properties" type="object">
          Properties of key vault. Optional.
          <Expandable title="properties">
            <ResponseField name="key_name" type="string" required>
              The name of KeyVault key. Required.
            </ResponseField>
            <ResponseField name="key_vault_id" type="string">
              UUID v4 used to identify the Azure Key Vault configuration Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="key_vault_resource_id" type="string">
              The resource ID of KeyVault. Optional.
            </ResponseField>
            <ResponseField name="key_vault_uri" type="string" required>
              The Uri of KeyVault. Required.
            </ResponseField>
            <ResponseField name="status" type="string">
              Status of the KeyVault connection. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="multi_ad_status" type="string">
      MultiAD Status for the account Optional.
    </ResponseField>
    <ResponseField name="nfs_v4_iddomain" type="string">
      The domain used to map NFSv4 user and group names to numeric IDs. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Azure lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
