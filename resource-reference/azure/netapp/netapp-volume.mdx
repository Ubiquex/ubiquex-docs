---
title: "azure_netapp_volume"
description: "`azure_netapp_netapp_volume` is a managed NFS or SMB file share provisioned from an Azure NetApp Files capacity pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own netapp volume"})

    		ubx.Resource(netapp.Volume, "example", netapp.VolumeConfig{
    			Properties: map[string]any{"creation_token": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Volume } from "@ubx/sdk-azure/azure/netapp/volume";

    export default stack("example", () => {
      intent({ summary: "example own netapp volume" });

      resource(Volume, "example", {
        properties: { creationToken: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.netapp import Volume, VolumeConfig

    def describe():
        ubx.intent("example own netapp volume")

        ubx.resource(Volume, "example", VolumeConfig(
            properties={"creation_token": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_volume, with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  "If etag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.") Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Volume properties Required.
  <Expandable title="properties">
    <ResponseField name="accept_grow_capacity_pool_for_short_term_clone_split" type="string">
      While auto splitting the short term clone volume, if the parent pool does not have enough space to accommodate the volume after split, it will be automatically resized, which will lead to increased billing. To accept capacity pool size auto grow and create a short term clone volume, set the property as accepted. Optional.
    </ResponseField>
    <ResponseField name="actual_throughput_mibps" type="number">
      Actual throughput in MiB/s for auto qosType volumes calculated based on size and serviceLevel Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="avs_data_store" type="string">
      Specifies whether the volume is enabled for Azure VMware Solution (AVS) datastore purpose Optional.
    </ResponseField>
    <ResponseField name="backup_id" type="string">
      Resource identifier used to identify the Backup. Optional.
    </ResponseField>
    <ResponseField name="baremetal_tenant_id" type="string">
      Unique Baremetal Tenant Identifier. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="breakthrough_mode" type="string">
      Specifies whether the volume operates in Breakthrough Mode. When set to 'Enabled', the volume runs on the resources configured for this mode, delivering improved performance and higher throughput. If set to 'Disabled' or omitted, the volume uses the basic configuration. This feature is available only in regions where it’s been configured and first-time users must finish onboarding prior to using Breakthrough Mode. Optional.
    </ResponseField>
    <ResponseField name="capacity_pool_resource_id" type="string">
      Pool Resource Id used in case of creating a volume through volume group Optional.
    </ResponseField>
    <ResponseField name="clone_progress" type="number">
      When a volume is being restored from another volume's snapshot, will show the percentage completion of this cloning process. When this value is empty/null there is no cloning process currently happening on this volume. This value will update every 5 minutes during cloning. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="cool_access" type="bool">
      Specifies whether Cool Access(tiering) is enabled for the volume. Optional.
    </ResponseField>
    <ResponseField name="cool_access_retrieval_policy" type="string">
      coolAccessRetrievalPolicy determines the data retrieval behavior from the cool tier to standard storage based on the read pattern for cool access enabled volumes. The possible values for this field are: Default - Data will be pulled from cool tier to standard storage on random reads. This policy is the default. OnRead - All client-driven data read is pulled from cool tier to standard storage on both sequential and random reads. Never - No client-driven data is pulled from cool tier to standard storage. Optional.
    </ResponseField>
    <ResponseField name="cool_access_tiering_policy" type="string">
      coolAccessTieringPolicy determines which cold data blocks are moved to cool tier. The possible values for this field are: Auto - Moves cold user data blocks in both the Snapshot copies and the active file system to the cool tier tier. This policy is the default. SnapshotOnly - Moves user data blocks of the Volume Snapshot copies that are not associated with the active file system to the cool tier. Optional.
    </ResponseField>
    <ResponseField name="coolness_period" type="number">
      Specifies the number of days after which data that is not accessed by clients will be tiered. Optional.
    </ResponseField>
    <ResponseField name="creation_token" type="string" required>
      A unique file path for the volume. Used when creating mount targets Required.
    </ResponseField>
    <ResponseField name="data_protection" type="object">
      DataProtection type volumes include an object containing details of the replication Optional.
      <Expandable title="properties">
        <ResponseField name="backup" type="object">
          Volume Backup Properties Optional.
          <Expandable title="properties">
            <ResponseField name="backup_policy_id" type="string">
              Backup Policy Resource ID Optional.
            </ResponseField>
            <ResponseField name="backup_vault_id" type="string">
              Backup Vault Resource ID Optional.
            </ResponseField>
            <ResponseField name="policy_enforced" type="bool">
              Policy Enforced Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ransomware_protection" type="object">
          Advanced Ransomware Protection reports (ARP) settings Optional.
          <Expandable title="properties">
            <ResponseField name="actual_ransomware_protection_state" type="string">
              The actual state of the Advanced Ransomware Protection feature Optional.
            </ResponseField>
            <ResponseField name="desired_ransomware_protection_state" type="string">
              The desired state of the Advanced Ransomware Protection feature Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="replication" type="object">
          Replication properties Optional.
          <Expandable title="properties">
            <ResponseField name="destination_replications" type="list(object)">
              A list of destination replications Computed by Microsoft Azure after `ship`.
              <Expandable title="properties">
                <ResponseField name="region" type="string">
                  The Azure region this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="replication_type" type="string">
                  The kind of replication relationship this represents, e.g. cross-region or cross-zone. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="resource_id" type="string">
                  A reference to the resource this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="zone" type="string">
                  The availability zone this resource is provisioned in. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="endpoint_type" type="string">
              Indicates whether the local volume is the source or destination for the Volume Replication Optional.
            </ResponseField>
            <ResponseField name="external_replication_setup_info" type="string">
              Contains human-readable instructions on what the next step is to finish the external replication setup. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="external_replication_setup_status" type="string">
              Property that only applies to external replications. Provides a machine-readable value for the status of the external replication setup. Optional.
            </ResponseField>
            <ResponseField name="mirror_state" type="string">
              The status of the replication Optional.
            </ResponseField>
            <ResponseField name="relationship_status" type="string">
              Status of the volume replication relationship Optional.
            </ResponseField>
            <ResponseField name="remote_path" type="object">
              The full path to a volume that is to be migrated into ANF. Required for Migration volumes Optional.
              <Expandable title="properties">
                <ResponseField name="external_host_name" type="string" required>
                  The Path to a ONTAP Host Required.
                </ResponseField>
                <ResponseField name="server_name" type="string" required>
                  The name of a server on the ONTAP Host Required.
                </ResponseField>
                <ResponseField name="volume_name" type="string" required>
                  The name of a volume on the server Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="remote_volume_region" type="string">
              The remote region for the other end of the Volume Replication. Optional.
            </ResponseField>
            <ResponseField name="remote_volume_resource_id" type="string">
              The resource ID of the remote volume. Required for cross region and cross zone replication Optional.
            </ResponseField>
            <ResponseField name="replication_id" type="string">
              Id Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="replication_schedule" type="string">
              Schedule Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="snapshot" type="object">
          Volume Snapshot Properties Optional.
          <Expandable title="properties">
            <ResponseField name="snapshot_policy_id" type="string">
              Snapshot Policy ResourceId Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="volume_relocation" type="object">
          Volume relocation properties Optional.
          <Expandable title="properties">
            <ResponseField name="ready_to_be_finalized" type="bool">
              Has relocation finished and is ready to be cleaned up Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="relocation_requested" type="bool">
              Has relocation been requested for this volume Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="data_store_resource_id" type="list(string)">
      Data store resource unique identifier Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="default_group_quota_in_ki_bs" type="number">
      Default group quota for volume in KiBs. If isDefaultQuotaEnabled is set, the minimum value of 4 KiBs applies. Optional.
    </ResponseField>
    <ResponseField name="default_user_quota_in_ki_bs" type="number">
      Default user quota for volume in KiBs. If isDefaultQuotaEnabled is set, the minimum value of 4 KiBs applies . Optional.
    </ResponseField>
    <ResponseField name="delete_base_snapshot" type="bool">
      If enabled (true) the snapshot the volume was created from will be automatically deleted after the volume create operation has finished. Defaults to false Optional.
    </ResponseField>
    <ResponseField name="effective_network_features" type="string">
      The effective value of the network features type available to the volume, or current effective state of update. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="enable_subvolumes" type="string">
      Flag indicating whether subvolume operations are enabled on the volume Deprecated. Subvolume operations and this flag will be removed in a future API version. Optional.
    </ResponseField>
    <ResponseField name="encrypted" type="bool">
      Specifies if the volume is encrypted or not. Only available on volumes created or updated after 2022-01-01. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="encryption_key_source" type="string">
      Source of key used to encrypt data in volume. Applicable if NetApp account has encryption.keySource = 'Microsoft.KeyVault'. Possible values (case-insensitive) are: 'Microsoft.NetApp, Microsoft.KeyVault' Optional.
    </ResponseField>
    <ResponseField name="export_policy" type="object">
      Set of export policy rules Optional.
      <Expandable title="properties">
        <ResponseField name="rules" type="list(object)">
          Export policy rule Optional.
          <Expandable title="properties">
            <ResponseField name="allowed_clients" type="string">
              The client IP address range(s), in CIDR notation, permitted to access this export. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="chown_mode" type="string">
              Who is permitted to change file ownership on this volume, e.g. only root or any user. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="cifs" type="bool">
              Whether the SMB/CIFS protocol is enabled for this volume. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="has_root_access" type="bool">
              Whether this client is granted root access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5_read_only" type="bool">
              Whether Kerberos v5 authenticated clients are granted read-only access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5_read_write" type="bool">
              Whether Kerberos v5 authenticated clients are granted read-write access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5i_read_only" type="bool">
              Whether Kerberos v5 with integrity checking authenticated clients are granted read-only access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5i_read_write" type="bool">
              Whether Kerberos v5 with integrity checking authenticated clients are granted read-write access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5p_read_only" type="bool">
              Whether Kerberos v5 with privacy protection authenticated clients are granted read-only access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kerberos5p_read_write" type="bool">
              Whether Kerberos v5 with privacy protection authenticated clients are granted read-write access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nfsv3" type="bool">
              Whether the NFSv3 protocol is enabled for this volume. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="nfsv41" type="bool">
              Whether the NFSv4.1 protocol is enabled for this volume. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rule_index" type="number">
              This rule's own relative evaluation order. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="unix_read_only" type="bool">
              Whether UNIX clients are granted read-only access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="unix_read_write" type="bool">
              Whether UNIX clients are granted read-write access. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="file_access_logs" type="string">
      Flag indicating whether file access logs are enabled for the volume, based on active diagnostic settings present on the volume. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="file_system_id" type="string">
      Unique FileSystem Identifier. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="inherited_size_in_bytes" type="number">
      Space shared by short term clone volume with parent volume in bytes. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="is_default_quota_enabled" type="bool">
      Specifies if default quota is enabled for the volume. Optional.
    </ResponseField>
    <ResponseField name="is_large_volume" type="bool">
      Specifies whether volume is a Large Volume or Regular Volume. Optional.
    </ResponseField>
    <ResponseField name="is_restoring" type="bool">
      Restoring Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="kerberos_enabled" type="bool">
      Describe if a volume is KerberosEnabled. To be use with swagger version 2020-05-01 or later Optional.
    </ResponseField>
    <ResponseField name="key_vault_private_endpoint_resource_id" type="string">
      The resource ID of private endpoint for KeyVault. It must reside in the same VNET as the volume. Only applicable if encryptionKeySource = 'Microsoft.KeyVault'. Optional.
    </ResponseField>
    <ResponseField name="ldap_enabled" type="bool">
      Specifies whether LDAP is enabled or not for a given NFS volume. Optional.
    </ResponseField>
    <ResponseField name="maximum_number_of_files" type="number">
      Maximum number of files allowed. Needs a service request in order to be changed. Only allowed to be changed if volume quota is more than 4TiB. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="mount_targets" type="list(object)">
      List of mount targets Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="file_system_id" type="string">
          A unique identifier for this volume's own file system. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ip_address" type="string">
          An IP address value. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mount_target_id" type="string">
          A unique identifier for this volume's own mount target. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="smb_server_fqdn" type="string">
          The fully qualified domain name of the SMB server hosting this volume. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_features" type="string">
      The original value of the network features type available to the volume at the time it was created. Optional.
    </ResponseField>
    <ResponseField name="network_sibling_set_id" type="string">
      Network Sibling Set ID for the the group of volumes sharing networking resources. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="originating_resource_id" type="string">
      Id of the snapshot or backup that the volume is restored from. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="placement_rules" type="list(object)">
      Application specific placement rules for the particular volume Optional.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The key of this metadata entry. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protocol_types" type="list(string)">
      Specify the protocol types for the volume. Supported values are NFSv3, NFSv4.1, and CIFS. For SMB volumes, specify CIFS. The value SMB isn't supported in the protocolTypes property. Default: NFSv3 Optional.
    </ResponseField>
    <ResponseField name="provisioned_availability_zone" type="string">
      The availability zone where the volume is provisioned. This refers to the logical availability zone where the volume resides. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Azure lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="proximity_placement_group" type="string">
      Proximity placement group associated with the volume Optional.
    </ResponseField>
    <ResponseField name="security_style" type="string">
      The security style of volume, default unix, defaults to ntfs for dual protocol or CIFS protocol Optional.
    </ResponseField>
    <ResponseField name="service_level" type="string">
      The service level of the file system Optional.
    </ResponseField>
    <ResponseField name="smb_access_based_enumeration" type="string">
      Enables access-based enumeration share property for SMB Shares. Only applicable for SMB/DualProtocol volume Optional.
    </ResponseField>
    <ResponseField name="smb_continuously_available" type="bool">
      Enables continuously available share property for smb volume. Only applicable for SMB volume Optional.
    </ResponseField>
    <ResponseField name="smb_encryption" type="bool">
      Enables encryption for in-flight smb3 data. Only applicable for SMB/DualProtocol volume. To be used with swagger version 2020-08-01 or later Optional.
    </ResponseField>
    <ResponseField name="smb_non_browsable" type="string">
      Enables non-browsable property for SMB Shares. Only applicable for SMB/DualProtocol volume Optional.
    </ResponseField>
    <ResponseField name="snapshot_directory_visible" type="bool">
      If enabled (true) the volume will contain a read-only snapshot directory which provides access to each of the volume's snapshots (defaults to true). Optional.
    </ResponseField>
    <ResponseField name="snapshot_id" type="string">
      Resource identifier used to identify the Snapshot. Optional.
    </ResponseField>
    <ResponseField name="storage_to_network_proximity" type="string">
      Provides storage to network proximity information for the volume. Optional.
    </ResponseField>
    <ResponseField name="subnet_id" type="string" required>
      The Azure Resource URI for a delegated subnet. Must have the delegation Microsoft.NetApp/volumes Required.
    </ResponseField>
    <ResponseField name="t2_network" type="string">
      T2 network information Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="throughput_mibps" type="number">
      Maximum throughput in MiB/s that can be achieved by this volume and this will be accepted as input only for manual qosType volume Optional.
    </ResponseField>
    <ResponseField name="unix_permissions" type="string">
      The UNIX-style file permission bits applied to this volume's own root. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="usage_threshold" type="number" required>
      Maximum storage quota allowed for a file system in bytes. This is a soft quota used for alerting only. For regular volumes, valid values are in the range 50GiB to 100TiB. For large volumes, valid values are in the range 100TiB to 500TiB, and on an exceptional basis, from to 2400GiB to 2400TiB. Values expressed in bytes as multiples of 1 GiB. Required.
    </ResponseField>
    <ResponseField name="volume_group_name" type="string">
      Volume Group Name Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="volume_spec_name" type="string">
      Volume spec name is the application specific designation or identifier for the particular volume in a volume group for e.g. data, log Optional.
    </ResponseField>
    <ResponseField name="volume_type" type="string">
      What type of volume is this. For destination volumes in Cross Region Replication, set type to DataProtection. For creating clone volume, set type to ShortTermClone Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The availability zones. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
