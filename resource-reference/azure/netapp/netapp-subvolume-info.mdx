---
title: "azure_netapp_subvolume_info"
description: "`azure_netapp_subvolume_info` describes a subvolume..."
---

`azure_netapp_subvolume_info` describes a subvolume, a directory-scoped sub-unit of an Azure NetApp Files volume managed and quota-restricted independently of the rest of the volume.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_netapp --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_netapp/sdk/go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_netapp/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own netapp subvolume info"})

    		ubx.Resource(netapp.NetappSubvolumeInfo, "example", netapp.NetappSubvolumeInfoConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_netapp --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetappSubvolumeInfo } from "./local-sdk/azure_netapp/sdk/typescript/azure_netapp/netapp/netapp_subvolume_info.ts";

    export default stack("example", () => {
      intent({ summary: "example own netapp subvolume info" });

      resource(NetappSubvolumeInfo, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_netapp --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_netapp/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_netapp.netapp import NetappSubvolumeInfo, NetappSubvolumeInfoConfig

    def describe():
        ubx.intent("example own netapp subvolume info")

        ubx.resource(NetappSubvolumeInfo, "example", NetappSubvolumeInfoConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_subvolume_info, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  This represents path associated with the subvolume Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="parent_path" type="string">
      parent path to the subvolume Optional.
    </ResponseField>
    <ResponseField name="path" type="string">
      Path to the subvolume Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Azure lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="size" type="number">
      Truncate subvolume to the provided size in bytes Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
