---
title: "azure_netapp_subvolume_info"
description: "`azure_netapp_netapp_subvolume_info` describes a subvolume..."
---

`azure_netapp_netapp_subvolume_info` describes a subvolume, a directory-scoped sub-unit of an Azure NetApp Files volume managed and quota-restricted independently of the rest of the volume.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own netapp subvolume info"})

    		ubx.Resource(netapp.SubvolumeInfo, "example", netapp.SubvolumeInfoConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SubvolumeInfo } from "@ubx/sdk-azure/azure/netapp/subvolume_info";

    export default stack("example", () => {
      intent({ summary: "example own netapp subvolume info" });

      resource(SubvolumeInfo, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.netapp import SubvolumeInfo, SubvolumeInfoConfig

    def describe():
        ubx.intent("example own netapp subvolume info")

        ubx.resource(SubvolumeInfo, "example", SubvolumeInfoConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_subvolume_info, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  This represents path associated with the subvolume Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="parent_path" type="string">
      parent path to the subvolume Optional.
    </ResponseField>
    <ResponseField name="path" type="string">
      Path to the subvolume Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Azure lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="size" type="number">
      Truncate subvolume to the provided size in bytes Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
