---
title: "azure_netapp_backup_vault"
description: "`azure_netapp_netapp_backup_vault` is the storage container Azure NetApp Files backups are written into and organized under."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_netapp_netapp --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_netapp_netapp/sdk/go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_netapp_netapp/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own netapp backup vault"})

    		ubx.Resource(netapp.NetappBackupVault, "ci-runner", netapp.NetappBackupVaultConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_netapp_netapp --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetappBackupVault } from "./local-sdk/azure_netapp_netapp/sdk/typescript/azure_netapp_netapp/netapp/netapp_backup_vault.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own netapp backup vault" });

      resource(NetappBackupVault, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_netapp_netapp --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_netapp_netapp/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_netapp_netapp.netapp import NetappBackupVault, NetappBackupVaultConfig

    def describe():
        ubx.intent("payments own netapp backup vault")

        ubx.resource(NetappBackupVault, "ci-runner", NetappBackupVaultConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_backup_vault, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Backup Vault properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Azure lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
