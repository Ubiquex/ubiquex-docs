---
title: "azure_netapp_backup_vault"
description: "`azure_netapp_backup_vault` is the storage container Azure NetApp Files backups are written into and organized under."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_netapp --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_netapp/sdk/go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_netapp/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own netapp backup vault"})

    		ubx.Resource(netapp.NetappBackupVault, "example", netapp.NetappBackupVaultConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_netapp --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetappBackupVault } from "./local-sdk/azure_netapp/sdk/typescript/azure_netapp/netapp/netapp_backup_vault.ts";

    export default stack("example", () => {
      intent({ summary: "example own netapp backup vault" });

      resource(NetappBackupVault, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_netapp --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_netapp/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_netapp.netapp import NetappBackupVault, NetappBackupVaultConfig

    def describe():
        ubx.intent("example own netapp backup vault")

        ubx.resource(NetappBackupVault, "example", NetappBackupVaultConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_backup_vault, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Backup Vault properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Azure lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
