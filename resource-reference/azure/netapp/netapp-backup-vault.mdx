---
title: "azure_netapp_backup_vault"
description: "`azure_netapp_backup_vault` is the storage container Azure NetApp Files backups are written into and organized under."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own netapp backup vault"})

    		ubx.Resource(netapp.BackupVault, "example", netapp.BackupVaultConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BackupVault } from "@ubx/sdk-azure/azure/netapp/backup_vault";

    export default stack("example", () => {
      intent({ summary: "example own netapp backup vault" });

      resource(BackupVault, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.netapp.backup_vault import BackupVault, BackupVaultConfig

    def describe():
        ubx.intent("example own netapp backup vault")

        ubx.resource(BackupVault, "example", BackupVaultConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_netapp_backup_vault, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Backup Vault properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Azure lifecycle management Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
