---
title: "azurerm_machine_learning_datastore_blobstorage"
description: "Real, generated bindings for `azurerm_machine_learning_datastore_blobstorage`."
---

`azurerm_machine_learning_datastore_blobstorage` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machine "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/machine"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own learning datastore blobstorage"})

    		ubx.Resource(machine.LearningDatastoreBlobstorage, "ci-runner", machine.LearningDatastoreBlobstorageConfig{
    			Name:                    "ci-runner",
    			StorageContainerId:      "example",
    			WorkspaceId:             "example",
    			AccountKey:              "example",
    			Description:             "Managed by ubx.",
    			IsDefault:               true,
    			ServiceDataAuthIdentity: "example",
    			SharedAccessSignature:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LearningDatastoreBlobstorage } from "jsr:@ubx/sdk-azure/azurerm/machine/learning_datastore_blobstorage";

    export default stack("payments", () => {
      intent({ summary: "payments own learning datastore blobstorage" });

      resource(LearningDatastoreBlobstorage, "ci-runner", {
        name: "ci-runner",
        storageContainerId: "example",
        workspaceId: "example",
        accountKey: "example",
        description: "Managed by ubx.",
        isDefault: true,
        serviceDataAuthIdentity: "example",
        sharedAccessSignature: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.machine import LearningDatastoreBlobstorage, LearningDatastoreBlobstorageConfig

    def describe():
        ubx.intent("payments own learning datastore blobstorage")

        ubx.resource(LearningDatastoreBlobstorage, "ci-runner", LearningDatastoreBlobstorageConfig(
            name="ci-runner",
            storage_container_id="example",
            workspace_id="example",
            account_key="example",
            description="Managed by ubx.",
            is_default=True,
            service_data_auth_identity="example",
            shared_access_signature="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_machine_learning_datastore_blobstorage called
    "ci-runner", with storage container id example, workspace id
    example, account key example, description Managed by ubx., is
    default true, service data auth identity example, shared access
    signature example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_data_auth_identity" type="string">
  Optional.
</ResponseField>

<ResponseField name="shared_access_signature" type="string">
  Optional.
</ResponseField>

<ResponseField name="storage_container_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
