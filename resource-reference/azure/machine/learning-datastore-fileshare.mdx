---
title: "azurerm_machine_learning_datastore_fileshare"
description: "Real, generated bindings for `azurerm_machine_learning_datastore_fileshare`."
---

`azurerm_machine_learning_datastore_fileshare` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machine "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/machine"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own learning datastore fileshare"})

    		ubx.Resource(machine.LearningDatastoreFileshare, "ci-runner", machine.LearningDatastoreFileshareConfig{
    			Name:                  "ci-runner",
    			StorageFileshareId:    "example",
    			WorkspaceId:           "example",
    			AccountKey:            "example",
    			Description:           "Managed by ubx.",
    			ServiceDataIdentity:   "example",
    			SharedAccessSignature: "example",
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LearningDatastoreFileshare } from "jsr:@ubx/sdk-azure/azurerm/machine/learning_datastore_fileshare";

    export default stack("payments", () => {
      intent({ summary: "payments own learning datastore fileshare" });

      resource(LearningDatastoreFileshare, "ci-runner", {
        name: "ci-runner",
        storageFileshareId: "example",
        workspaceId: "example",
        accountKey: "example",
        description: "Managed by ubx.",
        serviceDataIdentity: "example",
        sharedAccessSignature: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.machine import LearningDatastoreFileshare, LearningDatastoreFileshareConfig

    def describe():
        ubx.intent("payments own learning datastore fileshare")

        ubx.resource(LearningDatastoreFileshare, "ci-runner", LearningDatastoreFileshareConfig(
            name="ci-runner",
            storage_fileshare_id="example",
            workspace_id="example",
            account_key="example",
            description="Managed by ubx.",
            service_data_identity="example",
            shared_access_signature="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_machine_learning_datastore_fileshare called
    "ci-runner", with storage fileshare id example, workspace id
    example, account key example, description Managed by ubx.,
    service data identity example, shared access signature example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_data_identity" type="string">
  Optional.
</ResponseField>

<ResponseField name="shared_access_signature" type="string">
  Optional.
</ResponseField>

<ResponseField name="storage_fileshare_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
