---
title: "azure_maintenance_configuration_assignment"
description: "`azure_maintenance_maintenance_configuration_assignment` attaches an Azure Maintenance Configuration to a specific resource..."
---

`azure_maintenance_maintenance_configuration_assignment` attaches an Azure Maintenance Configuration to a specific resource, scoping when its own maintenance updates are applied.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	maintenance "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/maintenance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maintenance configuration assignment"})

    		ubx.Resource(maintenance.ConfigurationAssignment, "example", maintenance.ConfigurationAssignmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConfigurationAssignment } from "@ubx/sdk-azure/azure/maintenance/configuration_assignment";

    export default stack("example", () => {
      intent({ summary: "example own maintenance configuration assignment" });

      resource(ConfigurationAssignment, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.maintenance import ConfigurationAssignment, ConfigurationAssignmentConfig

    def describe():
        ubx.intent("example own maintenance configuration assignment")

        ubx.resource(ConfigurationAssignment, "example", ConfigurationAssignmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_maintenance_configuration_assignment, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  Location of the resource Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties for configuration assignment Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="filter" type="object">
      Azure query for the update configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="locations" type="list(string)">
          List of locations to scope the query to. Optional.
        </ResponseField>
        <ResponseField name="os_types" type="list(string)">
          List of allowed operating systems. Optional.
        </ResponseField>
        <ResponseField name="resource_groups" type="list(string)">
          List of allowed resource groups. Optional.
        </ResponseField>
        <ResponseField name="resource_types" type="list(string)">
          List of allowed resources. Optional.
        </ResponseField>
        <ResponseField name="tag_settings" type="object">
          Tag filter information for the VM. Optional.
          <Expandable title="properties">
            <ResponseField name="filter_operator" type="string">
              Filter VMs by Any or All specified tags. Optional.
            </ResponseField>
            <ResponseField name="tags" type="map(list(string))">
              Dictionary of tags with its list of values. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="maintenance_configuration_id" type="string">
      The maintenance configuration Id Optional.
    </ResponseField>
    <ResponseField name="resource_id" type="string">
      The unique resourceId Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
