---
title: "azure_maintenance_configuration"
description: "A complete, runnable azure_maintenance_configuration program, in every SDK language."
---

`azure_maintenance_configuration` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_maintenance --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_maintenance/sdk/go
    package main

    import (
    	maintenance "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_maintenance/maintenance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own configuration"})

    		ubx.Resource(maintenance.Configuration, "example", maintenance.ConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_maintenance --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Configuration } from "./local-sdk/azure_maintenance/sdk/typescript/azure_maintenance/maintenance/configuration.ts";

    export default stack("example", () => {
      intent({ summary: "example own configuration" });

      resource(Configuration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_maintenance --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_maintenance/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_maintenance.maintenance import Configuration, ConfigurationConfig

    def describe():
        ubx.intent("example own configuration")

        ubx.resource(Configuration, "example", ConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_maintenance_configuration, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  Gets or sets location of the resource Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties for maintenance configuration Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="extension_properties" type="map(string)">
      Gets or sets extensionProperties of the maintenanceConfiguration Optional.
    </ResponseField>
    <ResponseField name="install_patches" type="object">
      Input configuration for a patch run Optional.
      <Expandable title="properties">
        <ResponseField name="linux_parameters" type="object">
          Input properties for patching a Linux machine. Optional.
          <Expandable title="properties">
            <ResponseField name="classifications_to_include" type="list(string)">
              Classification category of patches to be patched. Allowed values are 'Critical', 'Security', and 'Other'. Optional.
            </ResponseField>
            <ResponseField name="package_name_masks_to_exclude" type="list(string)">
              Package names to be excluded for patching. Optional.
            </ResponseField>
            <ResponseField name="package_name_masks_to_include" type="list(string)">
              Package names to be included for patching. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="reboot_setting" type="string">
          Possible reboot preference as defined by the user based on which it would be decided to reboot the machine or not after the patch operation is completed. Optional.
        </ResponseField>
        <ResponseField name="windows_parameters" type="object">
          Input properties for patching a Windows machine. Optional.
          <Expandable title="properties">
            <ResponseField name="classifications_to_include" type="list(string)">
              Classification category of patches to be patched. Allowed values are 'Critical', 'Security', 'UpdateRollup', 'FeaturePack', 'ServicePack', 'Definition', 'Tools', and 'Updates'. Optional.
            </ResponseField>
            <ResponseField name="exclude_kbs_requiring_reboot" type="bool">
              Exclude patches which need reboot Optional.
            </ResponseField>
            <ResponseField name="kb_numbers_to_exclude" type="list(string)">
              Windows KBID to be excluded for patching. Optional.
            </ResponseField>
            <ResponseField name="kb_numbers_to_include" type="list(string)">
              Windows KBID to be included for patching. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="maintenance_scope" type="string">
      Gets or sets maintenanceScope of the configuration Optional.
    </ResponseField>
    <ResponseField name="maintenance_window" type="object">
      Definition of a MaintenanceWindow Optional.
      <Expandable title="properties">
        <ResponseField name="duration" type="string">
          Duration of the maintenance window in HH:mm format. If not provided, default value will be used based on maintenance scope provided. Example: 05:00. Optional.
        </ResponseField>
        <ResponseField name="expiration_date_time" type="string">
          Effective expiration date of the maintenance window in YYYY-MM-DD hh:mm format. The window will be created in the time zone provided and adjusted to daylight savings according to that time zone. Expiration date must be set to a future date. If not provided, it will be set to the maximum datetime 9999-12-31 23:59:59. Optional.
        </ResponseField>
        <ResponseField name="recur_every" type="string">
          Rate at which a Maintenance window is expected to recur. The rate can be expressed as daily, weekly, or monthly schedules. Daily schedule are formatted as recurEvery: [Frequency as integer]['Day(s)']. If no frequency is provided, the default frequency is 1. Daily schedule examples are recurEvery: Day, recurEvery: 3Days. Weekly schedule are formatted as recurEvery: [Frequency as integer]['Week(s)'] [Optional comma separated list of weekdays Monday-Sunday]. Weekly schedule examples are recurEvery: 3Weeks, recurEvery: Week Saturday,Sunday. Monthly schedules are formatted as [Frequency as integer]['Month(s)'] [Comma separated list of month days] or [Frequency as integer]['Month(s)'] [Week of Month (First, Second, Third, Fourth, Last)] [Weekday Monday-Sunday] [Optional Offset(No. of days)]. Offset value must be between -6 to 6 inclusive. Monthly schedule examples are recurEvery: Month, recurEvery: 2Months, recurEvery: Month day23,day24, recurEvery: Month Last Sunday, recurEvery: Month Fourth Monday, recurEvery: Month Last Sunday Offset-3, recurEvery: Month Third Sunday Offset6. Optional.
        </ResponseField>
        <ResponseField name="start_date_time" type="string">
          Effective start date of the maintenance window in YYYY-MM-DD hh:mm format. The start date can be set to either the current date or future date. The window will be created in the time zone provided and adjusted to daylight savings according to that time zone. Optional.
        </ResponseField>
        <ResponseField name="time_zone" type="string">
          Name of the timezone. List of timezones can be obtained by executing [System.TimeZoneInfo]::GetSystemTimeZones() in PowerShell. Example: Pacific Standard Time, UTC, W. Europe Standard Time, Korea Standard Time, Cen. Australia Standard Time. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="namespace" type="string">
      Gets or sets namespace of the resource Optional.
    </ResponseField>
    <ResponseField name="visibility" type="string">
      Gets or sets the visibility of the configuration. The default value is 'Custom' Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Gets or sets tags of the resource Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
