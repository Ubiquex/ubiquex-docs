---
title: "azure_maintenance_apply_update"
description: "`azure_maintenance_apply_update` triggers an immediate, on-demand application of pending maintenance update(s) to a resource..."
---

`azure_maintenance_apply_update` triggers an immediate, on-demand application of pending maintenance update(s) to a resource, outside its own scheduled maintenance window.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_maintenance_maintenance --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_maintenance_maintenance/sdk/go
    package main

    import (
    	maintenance "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_maintenance_maintenance/maintenance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maintenance apply update"})

    		ubx.Resource(maintenance.MaintenanceApplyUpdate, "example", maintenance.MaintenanceApplyUpdateConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_maintenance_maintenance --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MaintenanceApplyUpdate } from "./local-sdk/azure_maintenance_maintenance/sdk/typescript/azure_maintenance_maintenance/maintenance/maintenance_apply_update.ts";

    export default stack("example", () => {
      intent({ summary: "example own maintenance apply update" });

      resource(MaintenanceApplyUpdate, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_maintenance_maintenance --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_maintenance_maintenance/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_maintenance_maintenance.maintenance import MaintenanceApplyUpdate, MaintenanceApplyUpdateConfig

    def describe():
        ubx.intent("example own maintenance apply update")

        ubx.resource(MaintenanceApplyUpdate, "example", MaintenanceApplyUpdateConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_maintenance_apply_update, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="properties" type="object">
  Properties for apply update Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="last_update_time" type="string">
      Last Update time Optional.
    </ResponseField>
    <ResponseField name="resource_id" type="string">
      The resourceId Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      The status Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
