---
title: "azure_maintenance_apply_update"
description: "`azure_maintenance_maintenance_apply_update` triggers an immediate, on-demand application of pending maintenance update(s) to a resource..."
---

`azure_maintenance_maintenance_apply_update` triggers an immediate, on-demand application of pending maintenance update(s) to a resource, outside its own scheduled maintenance window.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	maintenance "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/maintenance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maintenance apply update"})

    		ubx.Resource(maintenance.ApplyUpdate, "example", maintenance.ApplyUpdateConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplyUpdate } from "@ubx/sdk-azure/azure/maintenance/apply_update";

    export default stack("example", () => {
      intent({ summary: "example own maintenance apply update" });

      resource(ApplyUpdate, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.maintenance import ApplyUpdate, ApplyUpdateConfig

    def describe():
        ubx.intent("example own maintenance apply update")

        ubx.resource(ApplyUpdate, "example", ApplyUpdateConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_maintenance_apply_update, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="properties" type="object">
  Properties for apply update Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="last_update_time" type="string">
      Last Update time Optional.
    </ResponseField>
    <ResponseField name="resource_id" type="string">
      The resourceId Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      The status Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
