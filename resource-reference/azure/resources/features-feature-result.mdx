---
title: "azure_resources_features_feature_result"
description: "`azure_resources_features_feature_result` reports whether a preview feature of a resource provider is registered for a subscription."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own features feature result"})

    		ubx.Resource(resources.FeaturesFeatureResult, "example", resources.FeaturesFeatureResultConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FeaturesFeatureResult } from "@ubx/sdk-azure/azure/resources/features_feature_result";

    export default stack("example", () => {
      intent({ summary: "example own features feature result" });

      resource(FeaturesFeatureResult, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.resources.features_feature_result import FeaturesFeatureResult, FeaturesFeatureResultConfig

    def describe():
        ubx.intent("example own features feature result")

        ubx.resource(FeaturesFeatureResult, "example", FeaturesFeatureResultConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_features_feature_result, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="id" type="string">
  The resource ID of the feature. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the feature. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  Information about feature. Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="state" type="string">
      The registration state of the feature for the subscription. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The resource type of the feature. Computed by Microsoft Azure after `ship`.
</ResponseField>
