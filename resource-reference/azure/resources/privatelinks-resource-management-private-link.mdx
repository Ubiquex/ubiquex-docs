---
title: "azure_resources_privatelinks_resource_management_private_link"
description: "`azure_resources_privatelinks_resource_management_private_link` creates a Private Endpoint for Azure Resource Manager itself..."
---

`azure_resources_privatelinks_resource_management_private_link` creates a Private Endpoint for Azure Resource Manager itself, letting management-plane API calls stay on a private network path.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_resources_privatelinks --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_resources_privatelinks/sdk/go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_resources_privatelinks/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own privatelinks resource management private link"})

    		ubx.Resource(resources.PrivatelinksResourceManagementPrivateLink, "ci-runner", resources.PrivatelinksResourceManagementPrivateLinkConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_resources_privatelinks --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivatelinksResourceManagementPrivateLink } from "./local-sdk/azure_resources_privatelinks/sdk/typescript/azure_resources_privatelinks/resources/privatelinks_resource_management_private_link.ts";

    export default stack("payments", () => {
      intent({
        summary: "payments own privatelinks resource management private link",
      });

      resource(PrivatelinksResourceManagementPrivateLink, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_resources_privatelinks --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_resources_privatelinks/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_resources_privatelinks.resources import PrivatelinksResourceManagementPrivateLink, PrivatelinksResourceManagementPrivateLinkConfig

    def describe():
        ubx.intent("payments own privatelinks resource management private link")

        ubx.resource(PrivatelinksResourceManagementPrivateLink, "ci-runner", PrivatelinksResourceManagementPrivateLinkConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_resources_privatelinks_resource_management_private_link,
    with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="location" type="string">
  the region to create private link association. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The rmplResourceID. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="location" type="string">
  the region to create private link association. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string">
  The rmpl Name. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Computed by Microsoft Azure after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="private_endpoint_connections" type="list(string)">
      The private endpoint connections. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The operation type. Computed by Microsoft Azure after `ship`.
</ResponseField>
