---
title: "azure_resources_privatelinks_resource_management_private_link"
description: "`azure_resources_privatelinks_resource_management_private_link` creates a Private Endpoint for Azure Resource Manager itself..."
---

`azure_resources_privatelinks_resource_management_private_link` creates a Private Endpoint for Azure Resource Manager itself, letting management-plane API calls stay on a private network path.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own privatelinks resource management private link"})

    		ubx.Resource(resources.PrivatelinksResourceManagementPrivateLink, "example", resources.PrivatelinksResourceManagementPrivateLinkConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivatelinksResourceManagementPrivateLink } from "@ubx/sdk-azure/azure/resources/privatelinks_resource_management_private_link";

    export default stack("example", () => {
      intent({
        summary: "example own privatelinks resource management private link",
      });

      resource(PrivatelinksResourceManagementPrivateLink, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.resources.privatelinks_resource_management_private_link import PrivatelinksResourceManagementPrivateLink, PrivatelinksResourceManagementPrivateLinkConfig

    def describe():
        ubx.intent("example own privatelinks resource management private link")

        ubx.resource(PrivatelinksResourceManagementPrivateLink, "example", PrivatelinksResourceManagementPrivateLinkConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_resources_privatelinks_resource_management_private_link,
    with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="location" type="string">
  the region to create private link association. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The rmplResourceID. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="location" type="string">
  the region to create private link association. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string">
  The rmpl Name. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Computed by Microsoft Azure after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="private_endpoint_connections" type="list(string)">
      The private endpoint connections. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The operation type. Computed by Microsoft Azure after `ship`.
</ResponseField>
