---
title: "azure_resources_locks_management_lock_object"
description: "`azure_resources_locks_management_lock_object` prevents a resource, resource group, or subscription from being accidentally deleted or modified..."
---

`azure_resources_locks_management_lock_object` prevents a resource, resource group, or subscription from being accidentally deleted or modified, requiring the lock to be explicitly removed first.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own locks management lock object"})

    		ubx.Resource(resources.LocksManagementLockObject, "example", resources.LocksManagementLockObjectConfig{
    			Properties: map[string]any{"level": "example"},
    			Name:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocksManagementLockObject } from "@ubx/sdk-azure/azure/resources/locks_management_lock_object";

    export default stack("example", () => {
      intent({ summary: "example own locks management lock object" });

      resource(LocksManagementLockObject, "example", {
        properties: { level: "example" },
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.resources.locks_management_lock_object import LocksManagementLockObject, LocksManagementLockObjectConfig

    def describe():
        ubx.intent("example own locks management lock object")

        ubx.resource(LocksManagementLockObject, "example", LocksManagementLockObjectConfig(
            properties={"level": "example"},
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_locks_management_lock_object called
    "example", with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="id" type="string">
  The resource ID of the lock. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the lock. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  The lock properties. Required.
  <Expandable title="properties">
    <ResponseField name="level" type="string" required>
      The level of the lock. Possible values are: NotSpecified, CanNotDelete, ReadOnly. CanNotDelete means authorized users are able to read and modify the resources, but not delete. ReadOnly means authorized users can only read from a resource, but they can't modify or delete it. Required.
    </ResponseField>
    <ResponseField name="notes" type="string">
      Notes about the lock. Maximum of 512 characters. Optional.
    </ResponseField>
    <ResponseField name="owners" type="list(object)">
      The owners of the lock. Optional.
      <Expandable title="properties">
        <ResponseField name="application_id" type="string">
          The application (service principal) ID of this lock's own owner. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The resource type of the lock - Microsoft.Authorization/locks. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
