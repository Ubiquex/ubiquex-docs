---
title: "azure_resources_templatespecs_template_spec"
description: "`azure_resources_templatespecs_template_spec` publishes a reusable ARM template as a versioned, shareable resource..."
---

`azure_resources_templatespecs_template_spec` publishes a reusable ARM template as a versioned, shareable resource, so teams can deploy from a governed, centrally managed template instead of copying template files around.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_resources_templatespecs --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_resources_templatespecs/sdk/go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_resources_templatespecs/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own templatespecs template spec"})

    		ubx.Resource(resources.TemplatespecsTemplateSpec, "example", resources.TemplatespecsTemplateSpecConfig{
    			Location: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_resources_templatespecs --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TemplatespecsTemplateSpec } from "./local-sdk/azure_resources_templatespecs/sdk/typescript/azure_resources_templatespecs/resources/templatespecs_template_spec.ts";

    export default stack("example", () => {
      intent({ summary: "example own templatespecs template spec" });

      resource(TemplatespecsTemplateSpec, "example", {
        location: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_resources_templatespecs --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_resources_templatespecs/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_resources_templatespecs.resources import TemplatespecsTemplateSpec, TemplatespecsTemplateSpecConfig

    def describe():
        ubx.intent("example own templatespecs template spec")

        ubx.resource(TemplatespecsTemplateSpec, "example", TemplatespecsTemplateSpecConfig(
            location="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_templatespecs_template_spec, with location
    example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string" required>
  The location of the Template Spec. It cannot be changed after Template Spec creation. It must be one of the supported Azure locations. Required.
</ResponseField>

<ResponseField name="properties" type="object">
  Template Spec properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Template Spec description. Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      Template Spec display name. Optional.
    </ResponseField>
    <ResponseField name="metadata" type="any">
      The Template Spec metadata. Metadata is an open-ended object and is typically a collection of key-value pairs. Optional.
    </ResponseField>
    <ResponseField name="versions" type="map(object)">
      High-level information about the versions within this Template Spec. The keys are the version names. Only populated if the $expand query parameter is set to 'versions'. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          A human-readable description of this template spec version. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time_created" type="string">
          Output only. When this template spec version was created. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time_modified" type="string">
          Output only. When this template spec version was last modified. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags to apply to this resources templatespecs template spec. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
</ResponseField>
