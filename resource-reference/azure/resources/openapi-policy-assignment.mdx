---
title: "azure_resources_openapi_policy_assignment"
description: "`azure_resources_openapi_policy_assignment` applies an Azure Policy definition or initiative at a specific scope (a management group, subscription..."
---

`azure_resources_openapi_policy_assignment` applies an Azure Policy definition or initiative at a specific scope (a management group, subscription, resource group, or resource), enforcing or auditing its own rule(s) there.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi policy assignment"})

    		ubx.Resource(resources.OpenapiPolicyAssignment, "example", resources.OpenapiPolicyAssignmentConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiPolicyAssignment } from "@ubx/sdk-azure/azure/resources/openapi_policy_assignment";

    export default stack("example", () => {
      intent({ summary: "example own openapi policy assignment" });

      resource(OpenapiPolicyAssignment, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.resources import OpenapiPolicyAssignment, OpenapiPolicyAssignmentConfig

    def describe():
        ubx.intent("example own openapi policy assignment")

        ubx.resource(OpenapiPolicyAssignment, "example", OpenapiPolicyAssignmentConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_openapi_policy_assignment, with scope
    example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Identity for the resource. Policy assignments support a maximum of one identity. That is either a system assigned identity or a single user assigned identity. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal ID of the resource identity. This property will only be provided for a system assigned identity Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the resource identity. This property will only be provided for a system assigned identity Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      The identity type. This is the only required field when adding a system or user assigned identity to a resource. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The user identity associated with the policy. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location" type="string">
  The location of the policy assignment. Only required when utilizing managed identity. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The policy assignment properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="assignment_type" type="string">
      The type of policy assignment. Possible values are NotSpecified, System, SystemHidden, and Custom. Immutable. Optional.
    </ResponseField>
    <ResponseField name="definition_version" type="string">
      The version of the policy definition to use. Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      This message will be part of response in case of policy violation. Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The display name of the policy assignment. Optional.
    </ResponseField>
    <ResponseField name="effective_definition_version" type="string">
      The effective version of the policy definition in use. This is only present if requested via the $expand query parameter. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="enforcement_mode" type="string">
      The policy assignment enforcement mode. Possible values are Default, DoNotEnforce, and Enroll Optional.
    </ResponseField>
    <ResponseField name="instance_id" type="string">
      The instance ID of the policy assignment. This ID only and always changes when the assignment is deleted and recreated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="latest_definition_version" type="string">
      The latest version of the policy definition available. This is only present if requested via the $expand query parameter. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="metadata" type="any">
      The policy assignment metadata. Metadata is an open ended object and is typically a collection of key value pairs. Optional.
    </ResponseField>
    <ResponseField name="non_compliance_messages" type="list(object)">
      The messages that describe why a resource is non-compliant with the policy. Optional.
      <Expandable title="properties">
        <ResponseField name="message" type="string">
          A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="policy_definition_reference_id" type="string">
          A reference to a specific policy definition within this initiative. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="not_scopes" type="list(string)">
      The policy's excluded scopes. Optional.
    </ResponseField>
    <ResponseField name="overrides" type="list(object)">
      The policy property value override. Optional.
      <Expandable title="properties">
        <ResponseField name="kind" type="string">
          A finer-grained classification of this resource within its own resource type, when the type supports more than one variant. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="selectors" type="list(object)">
          Restricts which policy definition version(s) within a range this reference resolves to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="in" type="list(string)">
              Matches when the field's own value is one of this set. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              A finer-grained classification of this resource within its own resource type, when the type supports more than one variant. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="not_in" type="list(string)">
              Matches when the field's own value is not one of this set. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="progress" type="number">
              How far this operation has progressed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="value" type="string">
          The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="parameters" type="map(object)">
      The parameter values for the assigned policy rule. The keys are the parameter names. Optional.
      <Expandable title="properties">
        <ResponseField name="value" type="any">
          The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="policy_definition_id" type="string">
      The ID of the policy definition or policy set definition being assigned. Optional.
    </ResponseField>
    <ResponseField name="resource_selectors" type="list(object)">
      The resource selector list to filter policies by resource properties. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="selectors" type="list(object)">
          Restricts which policy definition version(s) within a range this reference resolves to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="in" type="list(string)">
              Matches when the field's own value is one of this set. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kind" type="string">
              A finer-grained classification of this resource within its own resource type, when the type supports more than one variant. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="not_in" type="list(string)">
              Matches when the field's own value is not one of this set. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="progress" type="number">
              How far this operation has progressed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scope" type="string">
      The scope for the policy assignment. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="self_serve_exemption_settings" type="object">
      The self-serve exemption settings for a policy assignment. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Indicates whether self-serve exemption is enabled. Optional.
        </ResponseField>
        <ResponseField name="policy_definition_reference_ids" type="list(string)">
          The policy definition reference IDs for self-serve exemption. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scope" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
