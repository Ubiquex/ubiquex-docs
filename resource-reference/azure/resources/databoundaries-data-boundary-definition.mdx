---
title: "azure_resources_databoundaries_data_boundary_definition"
description: "`azure_resources_databoundaries_data_boundary_definition` configures Azure Resource Manager to store and process a tenant's own customer data within..."
---

`azure_resources_databoundaries_data_boundary_definition` configures Azure Resource Manager to store and process a tenant's own customer data within a defined geographic data boundary (currently the EU Data Boundary), for data residency compliance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_resources_databoundaries --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_resources_databoundaries/sdk/go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_resources_databoundaries/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own databoundaries data boundary definition"})

    		ubx.Resource(resources.DataboundariesDataBoundaryDefinition, "example", resources.DataboundariesDataBoundaryDefinitionConfig{
    			Default: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_resources_databoundaries --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataboundariesDataBoundaryDefinition } from "./local-sdk/azure_resources_databoundaries/sdk/typescript/azure_resources_databoundaries/resources/databoundaries_data_boundary_definition.ts";

    export default stack("example", () => {
      intent({ summary: "example own databoundaries data boundary definition" });

      resource(DataboundariesDataBoundaryDefinition, "example", {
        default: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_resources_databoundaries --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_resources_databoundaries/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_resources_databoundaries.resources import DataboundariesDataBoundaryDefinition, DataboundariesDataBoundaryDefinitionConfig

    def describe():
        ubx.intent("example own databoundaries data boundary definition")

        ubx.resource(DataboundariesDataBoundaryDefinition, "example", DataboundariesDataBoundaryDefinitionConfig(
            default="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_databoundaries_data_boundary_definition,
    with default example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="default" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object">
  Data boundary properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="data_boundary" type="string">
      The data boundary definition. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Denotes the state of provisioning. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
