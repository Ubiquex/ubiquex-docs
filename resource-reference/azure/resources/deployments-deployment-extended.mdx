---
title: "azure_resources_deployments_deployment_extended"
description: "`azure_resources_deployments_deployment_extended` is an ARM template or Bicep deployment..."
---

`azure_resources_deployments_deployment_extended` is an ARM template or Bicep deployment, the record of applying a declarative infrastructure definition to a resource group, subscription, or higher scope.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own deployments deployment extended"})

    		ubx.Resource(resources.DeploymentsDeploymentExtended, "example", resources.DeploymentsDeploymentExtendedConfig{
    			Location:   "example",
    			Properties: map[string]any{"mode": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DeploymentsDeploymentExtended } from "@ubx/sdk-azure/azure/resources/deployments_deployment_extended";

    export default stack("example", () => {
      intent({ summary: "example own deployments deployment extended" });

      resource(DeploymentsDeploymentExtended, "example", {
        location: "example",
        properties: { mode: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.resources import DeploymentsDeploymentExtended, DeploymentsDeploymentExtendedConfig

    def describe():
        ubx.intent("example own deployments deployment extended")

        ubx.resource(DeploymentsDeploymentExtended, "example", DeploymentsDeploymentExtendedConfig(
            location="example",
            properties={"mode": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_deployments_deployment_extended, with
    location example, properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string" required>
  The location to store the deployment data. Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Deployment properties. Required.
  <Expandable title="properties">
    <ResponseField name="debug_setting" type="object">
      The debug setting. Optional.
      <Expandable title="properties">
        <ResponseField name="detail_level" type="string">
          Specifies the type of information to log for debugging. The permitted values are none, requestContent, responseContent, or both requestContent and responseContent separated by a comma. The default is none. When setting this value, carefully consider the type of information you are passing in during deployment. By logging information about the request or response, you could potentially expose sensitive data that is retrieved through the deployment operations. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="expression_evaluation_options" type="object">
      Specifies whether template expressions are evaluated within the scope of the parent template or nested template. Optional.
      <Expandable title="properties">
        <ResponseField name="scope" type="string">
          The scope to be used for evaluation of parameters, variables and functions in a nested template. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="extension_configs" type="map(map(object))">
      The configurations to use for deployment extensions. The keys of this object are deployment extension aliases as defined in the deployment template. Optional.
    </ResponseField>
    <ResponseField name="external_input_definitions" type="map(object)">
      External input definitions, used by external tooling to define expected external input values. Optional.
      <Expandable title="properties">
        <ResponseField name="config" type="any">
          The configuration defining how this external input is sourced and validated. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kind" type="string">
          A finer-grained classification of this resource within its own resource type, when the type supports more than one variant. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="external_inputs" type="map(object)">
      External input values, used by external tooling for parameter evaluation. Optional.
      <Expandable title="properties">
        <ResponseField name="value" type="any">
          The resolved value supplied for this external input. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mode" type="string" required>
      The mode that is used to deploy resources. This value can be either Incremental or Complete. In Incremental mode, resources are deployed without deleting existing resources that are not included in the template. In Complete mode, resources are deployed and existing resources in the resource group that are not included in the template are deleted. Be careful when using Complete mode as you may unintentionally delete resources. Required.
    </ResponseField>
    <ResponseField name="on_error_deployment" type="object">
      Deployment on error behavior. Optional.
      <Expandable title="properties">
        <ResponseField name="deployment_name" type="string">
          The deployment to be used on error case. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          The deployment on error behavior type. Possible values are LastSuccessful and SpecificDeployment. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="parameters" type="map(object)">
      Name and value pairs that define the deployment parameters for the template. You use this element when you want to provide the parameter values directly in the request rather than link to an existing parameter file. Use either the parametersLink property or the parameters property, but not both. It can be a JObject or a well formed JSON string. Optional.
      <Expandable title="properties">
        <ResponseField name="expression" type="string">
          A template expression evaluated to produce this parameter's own value. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reference" type="object">
          A reference to a Key Vault secret this parameter's own value is sourced from. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="key_vault" type="object">
              A reference to the Key Vault this secret is sourced from. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="secret_name" type="string">
              The name of this secret. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="secret_version" type="string">
              The specific version of this secret. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="value" type="any">
          The literal value supplied for this parameter. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="parameters_link" type="object">
      Entity representing the reference to the deployment parameters. Optional.
      <Expandable title="properties">
        <ResponseField name="content_version" type="string">
          If included, must match the ContentVersion in the template. Optional.
        </ResponseField>
        <ResponseField name="uri" type="string" required>
          The URI of the parameters file. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="template" type="any">
      The template content. You use this element when you want to pass the template syntax directly in the request rather than link to an existing template. It can be a JObject or well-formed JSON string. Use either the templateLink property or the template property, but not both. Optional.
    </ResponseField>
    <ResponseField name="template_link" type="object">
      Entity representing the reference to the template. Optional.
      <Expandable title="properties">
        <ResponseField name="content_version" type="string">
          If included, must match the ContentVersion in the template. Optional.
        </ResponseField>
        <ResponseField name="id" type="string">
          The resource id of a Template Spec. Use either the id or uri property, but not both. Optional.
        </ResponseField>
        <ResponseField name="query_string" type="string">
          The query string (for example, a SAS token) to be used with the templateLink URI. Optional.
        </ResponseField>
        <ResponseField name="relative_path" type="string">
          The relativePath property can be used to deploy a linked template at a location relative to the parent. If the parent template was linked with a TemplateSpec, this will reference an artifact in the TemplateSpec. If the parent was linked with a URI, the child deployment will be a combination of the parent and relativePath URIs Optional.
        </ResponseField>
        <ResponseField name="uri" type="string">
          The URI of the template to deploy. Use either the uri or id property, but not both. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="validation_level" type="string">
      The level of validation performed on the deployment. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Deployment tags Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
