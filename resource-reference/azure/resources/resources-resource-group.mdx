---
title: "azure_resources_resource_group"
description: "`azure_resources_resources_resource_group` is an Azure resource group..."
---

`azure_resources_resources_resource_group` is an Azure resource group, the logical container that related resource(s) are deployed into and managed as a unit.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own resources resource group"})

    		ubx.Resource(resources.ResourceGroup, "example", resources.ResourceGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResourceGroup } from "@ubx/sdk-azure/azure/resources/resource_group";

    export default stack("example", () => {
      intent({ summary: "example own resources resource group" });

      resource(ResourceGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.resources import ResourceGroup, ResourceGroupConfig

    def describe():
        ubx.intent("example own resources resource group")

        ubx.resource(ResourceGroup, "example", ResourceGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_resource_group, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="managed_by" type="string">
  The ID of the resource that manages this resource group. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The resource group properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      The provisioning state. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
