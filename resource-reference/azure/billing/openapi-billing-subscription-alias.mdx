---
title: "azure_billing_openapi_billing_subscription_alias"
description: "`azure_billing_openapi_billing_subscription_alias` creates a new Azure subscription (or renames/moves an existing one between billing scopes) via..."
---

`azure_billing_openapi_billing_subscription_alias` creates a new Azure subscription (or renames/moves an existing one between billing scopes) via the billing subscription alias API.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_billing_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_billing_openapi/sdk/go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_billing_openapi/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi billing subscription alias"})

    		ubx.Resource(billing.OpenapiBillingSubscriptionAlias, "ci-runner", billing.OpenapiBillingSubscriptionAliasConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_billing_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiBillingSubscriptionAlias } from "./local-sdk/azure_billing_openapi/sdk/typescript/azure_billing_openapi/billing/openapi_billing_subscription_alias.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi billing subscription alias" });

      resource(OpenapiBillingSubscriptionAlias, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_billing_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_billing_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_billing_openapi.billing import OpenapiBillingSubscriptionAlias, OpenapiBillingSubscriptionAliasConfig

    def describe():
        ubx.intent("payments own openapi billing subscription alias")

        ubx.resource(OpenapiBillingSubscriptionAlias, "ci-runner", OpenapiBillingSubscriptionAliasConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_billing_openapi_billing_subscription_alias, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  A billing subscription alias. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="billing_subscription_id" type="string">
      The ID of the billing subscription with the subscription alias. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the resource during a long-running operation. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Dictionary of metadata associated with the resource. It may not be populated for all resource types. Maximum key/value length supported of 256 characters. Keys/value should not empty value nor null. Keys can not contain &lt; &gt; % & \ ? / Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
