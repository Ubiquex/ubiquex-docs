---
title: "azure_billing_openapi_invoice_section"
description: "`azure_billing_openapi_invoice_section` further subdivides a billing profile's own single invoice into separate section(s)..."
---

`azure_billing_openapi_invoice_section` further subdivides a billing profile's own single invoice into separate section(s), e.g. for cost tracking per team without needing separate invoices.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_billing_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_billing_openapi/sdk/go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_billing_openapi/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi invoice section"})

    		ubx.Resource(billing.OpenapiInvoiceSection, "example", billing.OpenapiInvoiceSectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_billing_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiInvoiceSection } from "./local-sdk/azure_billing_openapi/sdk/typescript/azure_billing_openapi/billing/openapi_invoice_section.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi invoice section" });

      resource(OpenapiInvoiceSection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_billing_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_billing_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_billing_openapi.billing import OpenapiInvoiceSection, OpenapiInvoiceSectionConfig

    def describe():
        ubx.intent("example own openapi invoice section")

        ubx.resource(OpenapiInvoiceSection, "example", OpenapiInvoiceSectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_billing_openapi_invoice_section, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  An invoice section. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      The name of the invoice section. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the resource during a long-running operation. Optional.
    </ResponseField>
    <ResponseField name="reason_code" type="string">
      Reason for the specified invoice section status. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      Identifies the status of an invoice section. Optional.
    </ResponseField>
    <ResponseField name="system_id" type="string">
      The system generated unique identifier for an invoice section. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tags" type="map(string)">
      Dictionary of metadata associated with the resource. Maximum key/value length supported of 256 characters. Keys/value should not empty value nor null. Keys can not contain &lt; &gt; % & \ ? / Optional.
    </ResponseField>
    <ResponseField name="target_cloud" type="string">
      Identifies the cloud environments that are associated with an invoice section. This is a system managed optional field and gets updated as the invoice section gets associated with accounts in various clouds. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Dictionary of metadata associated with the resource. It may not be populated for all resource types. Maximum key/value length supported of 256 characters. Keys/value should not empty value nor null. Keys can not contain &lt; &gt; % & \ ? / Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
