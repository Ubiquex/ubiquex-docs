---
title: "azure_billing_openapi_associated_tenant"
description: "`azure_billing_openapi_associated_tenant` links an additional Microsoft Entra ID tenant to a billing account..."
---

`azure_billing_openapi_associated_tenant` links an additional Microsoft Entra ID tenant to a billing account, letting resources in that tenant be billed against the account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi associated tenant"})

    		ubx.Resource(billing.OpenapiAssociatedTenant, "example", billing.OpenapiAssociatedTenantConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiAssociatedTenant } from "@ubx/sdk-azure/azure/billing/openapi_associated_tenant";

    export default stack("example", () => {
      intent({ summary: "example own openapi associated tenant" });

      resource(OpenapiAssociatedTenant, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.billing import OpenapiAssociatedTenant, OpenapiAssociatedTenantConfig

    def describe():
        ubx.intent("example own openapi associated tenant")

        ubx.resource(OpenapiAssociatedTenant, "example", OpenapiAssociatedTenantConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_billing_openapi_associated_tenant, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  An associated tenant. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="billing_management_state" type="string">
      The state determines whether users from the associated tenant can be assigned roles for commerce activities like viewing and downloading invoices, managing payments, and making purchases. Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The name of the associated tenant. Optional.
    </ResponseField>
    <ResponseField name="provisioning_billing_request_id" type="string">
      The unique identifier for the billing request that is created when enabling provisioning for an associated tenant. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_management_state" type="string">
      The state determines whether subscriptions and licenses can be provisioned in the associated tenant. It can be set to 'Pending' to initiate a billing request. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the resource during a long-running operation. Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The ID that uniquely identifies a tenant. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Dictionary of metadata associated with the resource. It may not be populated for all resource types. Maximum key/value length supported of 256 characters. Keys/value should not empty value nor null. Keys can not contain &lt; &gt; % & \ ? / Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
