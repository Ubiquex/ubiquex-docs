---
title: "azure_redisenterprise_cluster"
description: "`azure_redisenterprise_redisenterprise_cluster` is an Azure Managed Redis (Enterprise) cluster..."
---

`azure_redisenterprise_redisenterprise_cluster` is an Azure Managed Redis (Enterprise) cluster, a Redis Enterprise deployment offering active-active geo-replication and modules unavailable on standard Memorystore-style tiers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_redisenterprise_redisenterprise --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_redisenterprise_redisenterprise/sdk/go
    package main

    import (
    	redisenterprise "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_redisenterprise_redisenterprise/redisenterprise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own redisenterprise cluster"})

    		ubx.Resource(redisenterprise.RedisenterpriseCluster, "example", redisenterprise.RedisenterpriseClusterConfig{
    			Sku: map[string]any{"name": "example-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_redisenterprise_redisenterprise --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RedisenterpriseCluster } from "./local-sdk/azure_redisenterprise_redisenterprise/sdk/typescript/azure_redisenterprise_redisenterprise/redisenterprise/redisenterprise_cluster.ts";

    export default stack("example", () => {
      intent({ summary: "example own redisenterprise cluster" });

      resource(RedisenterpriseCluster, "example", {
        sku: { name: "example-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_redisenterprise_redisenterprise --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_redisenterprise_redisenterprise/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_redisenterprise_redisenterprise.redisenterprise import RedisenterpriseCluster, RedisenterpriseClusterConfig

    def describe():
        ubx.intent("example own redisenterprise cluster")

        ubx.resource(RedisenterpriseCluster, "example", RedisenterpriseClusterConfig(
            sku={"name": "example-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_redisenterprise_cluster, with sku a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Distinguishes the kind of cluster. Read-only. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of Redis Enterprise clusters for create operations Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="public_network_access" type="string" required>
      Whether or not public network traffic can access the Redis cluster. Only 'Enabled' or 'Disabled' can be set. null is returned only for clusters created using an old API version which do not have this property and cannot be set. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object" required>
  SKU parameters supplied to the create Redis Enterprise cluster operation. Required.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      This property is only used with Enterprise and EnterpriseFlash SKUs. Determines the size of the cluster. Valid values are (2, 4, 6, ...) for Enterprise SKUs and (3, 9, 15, ...) for EnterpriseFlash SKUs. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The level of Redis Enterprise cluster to deploy. Possible values: ('Balanced_B5', 'MemoryOptimized_M10', 'ComputeOptimized_X5', etc.). For more information on SKUs see the latest pricing documentation. Note that additional SKUs may become supported in the future. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The Availability Zones where this cluster will be deployed. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
