---
title: "azure_redisenterprise_access_policy_assignment"
description: "`azure_redisenterprise_access_policy_assignment` grants a Microsoft Entra ID identity data-plane access to an Azure Managed Redis..."
---

`azure_redisenterprise_access_policy_assignment` grants a Microsoft Entra ID identity data-plane access to an Azure Managed Redis (Enterprise) database via a named access policy.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_redisenterprise --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_redisenterprise/sdk/go
    package main

    import (
    	redisenterprise "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_redisenterprise/redisenterprise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own redisenterprise access policy assignment"})

    		ubx.Resource(redisenterprise.RedisenterpriseAccessPolicyAssignment, "example", redisenterprise.RedisenterpriseAccessPolicyAssignmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_redisenterprise --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RedisenterpriseAccessPolicyAssignment } from "./local-sdk/azure_redisenterprise/sdk/typescript/azure_redisenterprise/redisenterprise/redisenterprise_access_policy_assignment.ts";

    export default stack("example", () => {
      intent({ summary: "example own redisenterprise access policy assignment" });

      resource(RedisenterpriseAccessPolicyAssignment, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_redisenterprise --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_redisenterprise/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_redisenterprise.redisenterprise import RedisenterpriseAccessPolicyAssignment, RedisenterpriseAccessPolicyAssignmentConfig

    def describe():
        ubx.intent("example own redisenterprise access policy assignment")

        ubx.resource(RedisenterpriseAccessPolicyAssignment, "example", RedisenterpriseAccessPolicyAssignmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_redisenterprise_access_policy_assignment, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of Redis Enterprise database access policy assignment. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="access_policy_name" type="string" required>
      Name of access policy under specific access policy assignment. Only "default" policy is supported for now. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Current provisioning status Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="user" type="object" required>
      The user associated with the access policy. Required.
      <Expandable title="properties">
        <ResponseField name="object_id" type="string">
          The object ID of the user. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
