---
title: "azure_redisenterprise_database"
description: "`azure_redisenterprise_database` is a database within an Azure Managed Redis (Enterprise) cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_redisenterprise --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_redisenterprise/sdk/go
    package main

    import (
    	redisenterprise "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_redisenterprise/redisenterprise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own redisenterprise database"})

    		ubx.Resource(redisenterprise.RedisenterpriseDatabase, "example", redisenterprise.RedisenterpriseDatabaseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_redisenterprise --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RedisenterpriseDatabase } from "./local-sdk/azure_redisenterprise/sdk/typescript/azure_redisenterprise/redisenterprise/redisenterprise_database.ts";

    export default stack("example", () => {
      intent({ summary: "example own redisenterprise database" });

      resource(RedisenterpriseDatabase, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_redisenterprise --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_redisenterprise/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_redisenterprise.redisenterprise import RedisenterpriseDatabase, RedisenterpriseDatabaseConfig

    def describe():
        ubx.intent("example own redisenterprise database")

        ubx.resource(RedisenterpriseDatabase, "example", RedisenterpriseDatabaseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_redisenterprise_database, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties for creating Redis Enterprise databases Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="access_keys_authentication" type="string">
      This property can be Enabled/Disabled to allow or deny access with the current access keys. Can be updated even after database is created. Default is Disabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
