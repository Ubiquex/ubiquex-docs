---
title: "azurerm_managed_redis_geo_replication"
description: "Real, generated bindings for `azurerm_managed_redis_geo_replication`."
---

`azurerm_managed_redis_geo_replication` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	managed "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/managed"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own redis geo replication"})

    		ubx.Resource(managed.RedisGeoReplication, "ci-runner", managed.RedisGeoReplicationConfig{
    			LinkedManagedRedisIds: []string{"example"},
    			ManagedRedisId:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RedisGeoReplication } from "jsr:@ubx/sdk-azure/azurerm/managed/redis_geo_replication";

    export default stack("payments", () => {
      intent({ summary: "payments own redis geo replication" });

      resource(RedisGeoReplication, "ci-runner", {
        linkedManagedRedisIds: ["example"],
        managedRedisId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.managed import RedisGeoReplication, RedisGeoReplicationConfig

    def describe():
        ubx.intent("payments own redis geo replication")

        ubx.resource(RedisGeoReplication, "ci-runner", RedisGeoReplicationConfig(
            linked_managed_redis_ids=["example"],
            managed_redis_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_managed_redis_geo_replication, with linked managed
    redis ids a set value, managed redis id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="linked_managed_redis_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="managed_redis_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
