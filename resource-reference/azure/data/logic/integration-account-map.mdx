---
title: "azure_logic_integration_account_map"
description: "`data_azure_logic_integration_account_map` looks up a real, already-existing `azure_logic_integration_account_map`."
---

It is a reusable XSLT transformation map stored in a Logic Apps integration account, converting a message from one schema to another.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	logic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/logic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_logic_integration_account_map"})

    		ubx.Data(logic.IntegrationAccountMap, "example", logic.IntegrationAccountMapConfig{
    			ApiVersion:             "example",
    			IntegrationAccountName: "example-integration-account-name",
    			ResourceGroupName:      "example-resource-group-name",
    			SubscriptionId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { IntegrationAccountMap } from "./local-sdk/azure/sdk/typescript/azure/data/logic/integration_account_map.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_logic_integration_account_map" });

      data(IntegrationAccountMap, "example", {
        apiVersion: "example",
        integrationAccountName: "example-integration-account-name",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.logic import IntegrationAccountMap, IntegrationAccountMapConfig

    def describe():
        ubx.intent("look up azure_logic_integration_account_map")

        ubx.data(IntegrationAccountMap, "example", IntegrationAccountMapConfig(
            api_version="example",
            integration_account_name="example-integration-account-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="integration_account_name" type="string" required>
  The real name of the Logic Apps integration account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of integration account maps. Returned by the lookup.
</ResponseField>
