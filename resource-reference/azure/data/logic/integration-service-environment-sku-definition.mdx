---
title: "azure_logic_integration_service_environment_sku_definition"
description: "`azure_logic_integration_service_environment_sku_definition` reads a real, available pricing tier (SKU) for a Logic Apps Integration Service Environment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/logic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_logic_integration_service_environment_sku_definition"})

    		ubx.Data(logic.IntegrationServiceEnvironmentSkuDefinition, "example", logic.IntegrationServiceEnvironmentSkuDefinitionConfig{
    			ApiVersion:                        "example",
    			IntegrationServiceEnvironmentName: "example-integration-service-environment-name",
    			ResourceGroup:                     "example",
    			SubscriptionId:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IntegrationServiceEnvironmentSkuDefinition } from "@ubx/sdk-azure/azure/data/logic/integration_service_environment_sku_definition";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_logic_integration_service_environment_sku_definition",
      });

      data(IntegrationServiceEnvironmentSkuDefinition, "example", {
        apiVersion: "example",
        integrationServiceEnvironmentName:
          "example-integration-service-environment-name",
        resourceGroup: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.logic import IntegrationServiceEnvironmentSkuDefinition, IntegrationServiceEnvironmentSkuDefinitionConfig

    def describe():
        ubx.intent("look up azure_logic_integration_service_environment_sku_definition")

        ubx.data(IntegrationServiceEnvironmentSkuDefinition, "example", IntegrationServiceEnvironmentSkuDefinitionConfig(
            api_version="example",
            integration_service_environment_name="example-integration-service-environment-name",
            resource_group="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="integration_service_environment_name" type="string" required>
  The real name of the Logic Apps Integration Service Environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of integration service environment skus. Returned by the lookup.
</ResponseField>
