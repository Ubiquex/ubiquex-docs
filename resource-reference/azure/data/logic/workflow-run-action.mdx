---
title: "azure_logic_workflow_run_action"
description: "`azure_logic_workflow_run_action` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_logic_workflow_run_action` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/logic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_logic_workflow_run_action"})

    		ubx.Data(logic.WorkflowRunAction, "example", logic.WorkflowRunActionConfig{
    			ApiVersion:        "example",
    			ResourceGroupName: "example-resource-group-name",
    			RunName:           "example-run-name",
    			SubscriptionId:    "example",
    			WorkflowName:      "example-workflow-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkflowRunAction } from "@ubx/sdk-azure/azure/data/logic/workflow_run_action";

    export default stack("example", () => {
      intent({ summary: "look up azure_logic_workflow_run_action" });

      data(WorkflowRunAction, "example", {
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        runName: "example-run-name",
        subscriptionId: "example",
        workflowName: "example-workflow-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.logic import WorkflowRunAction, WorkflowRunActionConfig

    def describe():
        ubx.intent("look up azure_logic_workflow_run_action")

        ubx.data(WorkflowRunAction, "example", WorkflowRunActionConfig(
            api_version="example",
            resource_group_name="example-resource-group-name",
            run_name="example-run-name",
            subscription_id="example",
            workflow_name="example-workflow-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="run_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="workflow_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  A list of workflow run actions. Returned by the lookup.
</ResponseField>
