---
title: "azure_logic_integration_account_session"
description: "`data_azure_logic_integration_account_session` looks up a real, already-existing `azure_logic_integration_account_session`."
---

It tracks state (e.g. AS2 message IDs) used to detect duplicate B2B message delivery within a Logic Apps integration account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/logic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_logic_integration_account_session"})

    		ubx.Data(logic.IntegrationAccountSession, "example", logic.IntegrationAccountSessionConfig{
    			ApiVersion:             "example",
    			IntegrationAccountName: "example-integration-account-name",
    			ResourceGroupName:      "example-resource-group-name",
    			SubscriptionId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IntegrationAccountSession } from "@ubx/sdk-azure/azure/data/logic/integration_account_session";

    export default stack("example", () => {
      intent({ summary: "look up azure_logic_integration_account_session" });

      data(IntegrationAccountSession, "example", {
        apiVersion: "example",
        integrationAccountName: "example-integration-account-name",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.logic import IntegrationAccountSession, IntegrationAccountSessionConfig

    def describe():
        ubx.intent("look up azure_logic_integration_account_session")

        ubx.data(IntegrationAccountSession, "example", IntegrationAccountSessionConfig(
            api_version="example",
            integration_account_name="example-integration-account-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="integration_account_name" type="string" required>
  The real name of the Logic Apps integration account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of integration account sessions. Returned by the lookup.
</ResponseField>
