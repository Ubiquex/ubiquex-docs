---
title: "azure_logic_assembly_definition"
description: "`data_azure_logic_assembly_definition` looks up a real, already-existing `azure_logic_assembly_definition`."
---

It uploads a .NET assembly to a Logic Apps integration account, made available to custom code steps within its own workflow(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	logic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/logic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_logic_assembly_definition"})

    		ubx.Data(logic.AssemblyDefinition, "example", logic.AssemblyDefinitionConfig{
    			ApiVersion:             "example",
    			IntegrationAccountName: "example-integration-account-name",
    			ResourceGroupName:      "example-resource-group-name",
    			SubscriptionId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { AssemblyDefinition } from "./local-sdk/azure/sdk/typescript/azure/data/logic/assembly_definition.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_logic_assembly_definition" });

      data(AssemblyDefinition, "example", {
        apiVersion: "example",
        integrationAccountName: "example-integration-account-name",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.logic import AssemblyDefinition, AssemblyDefinitionConfig

    def describe():
        ubx.intent("look up azure_logic_assembly_definition")

        ubx.data(AssemblyDefinition, "example", AssemblyDefinitionConfig(
            api_version="example",
            integration_account_name="example-integration-account-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="integration_account_name" type="string" required>
  The real name of the Logic Apps integration account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
