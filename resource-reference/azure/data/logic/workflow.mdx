---
title: "azure_logic_workflow"
description: "`data_azure_logic_workflow` looks up a real, already-existing `azure_logic_workflow`."
---

It is a Logic App (Consumption) workflow, an ordered sequence of trigger(s) and action(s) orchestrating a business process or integration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/logic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_logic_workflow"})

    		ubx.Data(logic.Workflow, "example", logic.WorkflowConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Workflow } from "@ubx/sdk-azure/azure/data/logic/workflow";

    export default stack("example", () => {
      intent({ summary: "look up azure_logic_workflow" });

      data(Workflow, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.logic import Workflow, WorkflowConfig

    def describe():
        ubx.intent("look up azure_logic_workflow")

        ubx.data(Workflow, "example", WorkflowConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of workflows. Returned by the lookup.
</ResponseField>
