---
title: "azure_logic_workflow_run_action_repetition_definition"
description: "`azure_logic_workflow_run_action_repetition_definition` reads the real definition of one repetition (loop iteration) of an action within a Logic App..."
---

`azure_logic_workflow_run_action_repetition_definition` reads the real definition of one repetition (loop iteration) of an action within a Logic App workflow run.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	logic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/logic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_logic_workflow_run_action_repetition_definition"})

    		ubx.Data(logic.WorkflowRunActionRepetitionDefinition, "example", logic.WorkflowRunActionRepetitionDefinitionConfig{
    			ActionName:        "example-action-name",
    			ApiVersion:        "example",
    			ResourceGroupName: "example-resource-group-name",
    			RunName:           "example-run-name",
    			SubscriptionId:    "example",
    			WorkflowName:      "example-workflow-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { WorkflowRunActionRepetitionDefinition } from "./local-sdk/azure/sdk/typescript/azure/data/logic/workflow_run_action_repetition_definition.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_logic_workflow_run_action_repetition_definition",
      });

      data(WorkflowRunActionRepetitionDefinition, "example", {
        actionName: "example-action-name",
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        runName: "example-run-name",
        subscriptionId: "example",
        workflowName: "example-workflow-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.logic import WorkflowRunActionRepetitionDefinition, WorkflowRunActionRepetitionDefinitionConfig

    def describe():
        ubx.intent("look up azure_logic_workflow_run_action_repetition_definition")

        ubx.data(WorkflowRunActionRepetitionDefinition, "example", WorkflowRunActionRepetitionDefinitionConfig(
            action_name="example-action-name",
            api_version="example",
            resource_group_name="example-resource-group-name",
            run_name="example-run-name",
            subscription_id="example",
            workflow_name="example-workflow-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_name" type="string" required>
  The real name of the action this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="run_name" type="string" required>
  The real name of this run. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workflow_name" type="string" required>
  The real name of the workflow this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of recommendations. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
