---
title: "azure_logic_integration_account_certificate"
description: "`azure_logic_integration_account_certificate` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_logic_integration_account_certificate` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/logic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_logic_integration_account_certificate"})

    		ubx.Data(logic.IntegrationAccountCertificate, "example", logic.IntegrationAccountCertificateConfig{
    			ApiVersion:             "example",
    			IntegrationAccountName: "example-integration-account-name",
    			ResourceGroupName:      "example-resource-group-name",
    			SubscriptionId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IntegrationAccountCertificate } from "@ubx/sdk-azure/azure/data/logic/integration_account_certificate";

    export default stack("example", () => {
      intent({ summary: "look up azure_logic_integration_account_certificate" });

      data(IntegrationAccountCertificate, "example", {
        apiVersion: "example",
        integrationAccountName: "example-integration-account-name",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.logic import IntegrationAccountCertificate, IntegrationAccountCertificateConfig

    def describe():
        ubx.intent("look up azure_logic_integration_account_certificate")

        ubx.data(IntegrationAccountCertificate, "example", IntegrationAccountCertificateConfig(
            api_version="example",
            integration_account_name="example-integration-account-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="integration_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of integration account certificates. Returned by the lookup.
</ResponseField>
