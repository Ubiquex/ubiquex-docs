---
title: "azure_domainservices_oucontainer_ou_container"
description: "`data_azure_domainservices_oucontainer_ou_container` looks up a real, already-existing `azure_domainservices_oucontainer_ou_container`."
---

It is an Organizational Unit within a Microsoft Entra Domain Services managed domain, letting delegated administrators be granted control over just their own OU.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	domainservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/domainservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_domainservices_oucontainer_ou_container"})

    		ubx.Data(domainservices.OucontainerOuContainer, "example", domainservices.OucontainerOuContainerConfig{
    			ApiVersion:        "example",
    			DomainServiceName: "example-domain-service-name",
    			ResourceGroupName: "example-resource-group-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OucontainerOuContainer } from "./local-sdk/azure/sdk/typescript/azure/data/domainservices/oucontainer_ou_container.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_domainservices_oucontainer_ou_container" });

      data(OucontainerOuContainer, "example", {
        apiVersion: "example",
        domainServiceName: "example-domain-service-name",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.domainservices import OucontainerOuContainer, OucontainerOuContainerConfig

    def describe():
        ubx.intent("look up azure_domainservices_oucontainer_ou_container")

        ubx.data(OucontainerOuContainer, "example", OucontainerOuContainerConfig(
            api_version="example",
            domain_service_name="example-domain-service-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_service_name" type="string" required>
  The real name of the Microsoft Entra Domain Services instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The continuation token for the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of OuContainer. Returned by the lookup.
</ResponseField>
