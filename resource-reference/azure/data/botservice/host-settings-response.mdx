---
title: "azure_botservice_host_settings_response"
description: "`azure_botservice_host_settings_response` reads the real, tenant-wide hosting endpoint settings for Azure Bot Service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	botservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/botservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_botservice_host_settings_response"})

    		ubx.Data(botservice.HostSettingsResponse, "example", botservice.HostSettingsResponseConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { HostSettingsResponse } from "./local-sdk/azure/sdk/typescript/azure/data/botservice/host_settings_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_botservice_host_settings_response" });

      data(HostSettingsResponse, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.botservice import HostSettingsResponse, HostSettingsResponseConfig

    def describe():
        ubx.intent("look up azure_botservice_host_settings_response")

        ubx.data(HostSettingsResponse, "example", HostSettingsResponseConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bot_open_id_metadata" type="string">
  Same as toBotFromChannelOpenIdMetadataUrl, used by SDK &lt; v4.12 Returned by the lookup.
</ResponseField>

<ResponseField name="oauth_url" type="string">
  For in-conversation bot user authentication Returned by the lookup.
</ResponseField>

<ResponseField name="to_bot_from_channel_open_id_metadata_url" type="string">
  For verifying incoming tokens from the channels Returned by the lookup.
</ResponseField>

<ResponseField name="to_bot_from_channel_token_issuer" type="string">
  For verifying incoming tokens from the channels Returned by the lookup.
</ResponseField>

<ResponseField name="to_bot_from_emulator_open_id_metadata_url" type="string">
  For verifying incoming tokens from bot emulator Returned by the lookup.
</ResponseField>

<ResponseField name="to_channel_from_bot_login_url" type="string">
  For getting access token to channels from bot host Returned by the lookup.
</ResponseField>

<ResponseField name="to_channel_from_bot_oauth_scope" type="string">
  For getting access token to channels from bot host Returned by the lookup.
</ResponseField>

<ResponseField name="validate_authority" type="bool">
  Per cloud OAuth setting on whether authority is validated Returned by the lookup.
</ResponseField>
