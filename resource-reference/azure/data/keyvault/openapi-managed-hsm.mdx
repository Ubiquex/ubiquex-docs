---
title: "azure_keyvault_openapi_managed_hsm"
description: "`data_azure_keyvault_openapi_managed_hsm` looks up a real, already-existing `azure_keyvault_openapi_managed_hsm`."
---

It provisions a Managed HSM, a dedicated, single-tenant, FIPS 140-2 Level 3-validated hardware security module pool, for workloads requiring exclusive control over their own HSM.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	keyvault "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/keyvault"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_keyvault_openapi_managed_hsm"})

    		ubx.Data(keyvault.OpenapiManagedHsm, "example", keyvault.OpenapiManagedHsmConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiManagedHsm } from "@ubx/sdk-azure/azure/data/keyvault/openapi_managed_hsm";

    export default stack("example", () => {
      intent({ summary: "look up azure_keyvault_openapi_managed_hsm" });

      data(OpenapiManagedHsm, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.keyvault import OpenapiManagedHsm, OpenapiManagedHsmConfig

    def describe():
        ubx.intent("look up azure_keyvault_openapi_managed_hsm")

        ubx.data(OpenapiManagedHsm, "example", OpenapiManagedHsmConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ManagedHsm items on this page Returned by the lookup.
</ResponseField>
