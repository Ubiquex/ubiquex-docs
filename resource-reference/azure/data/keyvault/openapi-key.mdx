---
title: "azure_keyvault_openapi_key"
description: "`data_azure_keyvault_openapi_key` looks up a real, already-existing `azure_keyvault_openapi_key`."
---

It is a cryptographic key stored in a Key Vault, used for encrypt/decrypt, sign/verify, or wrap/unwrap operations.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	keyvault "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/keyvault"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi key"})

    		ubx.Resource(keyvault.OpenapiKey, "example", keyvault.OpenapiKeyConfig{
    			Properties: map[string]any{"attributes": map[string]any{"enabled": true}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiKey } from "./local-sdk/azure/sdk/typescript/azure/keyvault/openapi_key.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi key" });

      resource(OpenapiKey, "example", {
        properties: { attributes: { enabled: true } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.keyvault import OpenapiKey, OpenapiKeyConfig
    from ubx.azure.keyvault.openapi_key import OpenapiKey_Properties, OpenapiKey_Properties_Attributes

    def describe():
        ubx.intent("example own openapi key")

        ubx.resource(OpenapiKey, "example", OpenapiKeyConfig(
            properties=OpenapiKey_Properties(attributes=OpenapiKey_Properties_Attributes(enabled=True)),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_keyvault_openapi_key, with properties a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="vault_name" type="string" required>
  The real name of the vault this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Key items on this page Returned by the lookup.
</ResponseField>
