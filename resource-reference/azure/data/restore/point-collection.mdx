---
title: "azure_restore_point_collection"
description: "`azure_restore_point_collection` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_restore_point_collection` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	restore "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/restore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_restore_point_collection"})

    		ubx.Data(restore.PointCollection, "example", restore.PointCollectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PointCollection } from "@ubx/sdk-azure/azure/data/restore/point_collection";

    export default stack("example", () => {
      intent({ summary: "look up azure_restore_point_collection" });

      data(PointCollection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.restore import PointCollection, PointCollectionConfig

    def describe():
        ubx.intent("look up azure_restore_point_collection")

        ubx.data(PointCollection, "example", PointCollectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The uri to fetch the next page of RestorePointCollections. Call ListNext() with this to fetch the next page of RestorePointCollections. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Gets the list of restore point collections. Returned by the lookup.
</ResponseField>
