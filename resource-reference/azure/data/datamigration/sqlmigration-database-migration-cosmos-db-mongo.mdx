---
title: "azure_datamigration_sqlmigration_database_migration_cosmos_db_mongo"
description: "`azure_datamigration_sqlmigration_database_migration_cosmos_db_mongo` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_datamigration_sqlmigration_database_migration_cosmos_db_mongo` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datamigration "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/datamigration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_datamigration_sqlmigration_database_migration_cosmos_db_mongo"})

    		ubx.Data(datamigration.SqlmigrationDatabaseMigrationCosmosDbMongo, "example", datamigration.SqlmigrationDatabaseMigrationCosmosDbMongoConfig{
    			ApiVersion:         "example",
    			ResourceGroupName:  "example-resource-group-name",
    			SubscriptionId:     "example",
    			TargetResourceName: "example-target-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SqlmigrationDatabaseMigrationCosmosDbMongo } from "@ubx/sdk-azure/azure/data/datamigration/sqlmigration_database_migration_cosmos_db_mongo";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_datamigration_sqlmigration_database_migration_cosmos_db_mongo",
      });

      data(SqlmigrationDatabaseMigrationCosmosDbMongo, "example", {
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
        targetResourceName: "example-target-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.datamigration import SqlmigrationDatabaseMigrationCosmosDbMongo, SqlmigrationDatabaseMigrationCosmosDbMongoConfig

    def describe():
        ubx.intent("look up azure_datamigration_sqlmigration_database_migration_cosmos_db_mongo")

        ubx.data(SqlmigrationDatabaseMigrationCosmosDbMongo, "example", SqlmigrationDatabaseMigrationCosmosDbMongoConfig(
            api_version="example",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
            target_resource_name="example-target-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="target_resource_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Returned by the lookup.
</ResponseField>
