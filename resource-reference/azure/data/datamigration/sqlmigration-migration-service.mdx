---
title: "azure_datamigration_sqlmigration_migration_service"
description: "`data_azure_datamigration_sqlmigration_migration_service` looks up a real, already-existing `azure_datamigration_sqlmigration_migration_service`."
---

It is the top-level Azure Database Migration Service resource that migration project(s) and their own tracked database migration(s) run through.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datamigration "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/datamigration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_datamigration_sqlmigration_migration_service"})

    		ubx.Data(datamigration.SqlmigrationMigrationService, "example", datamigration.SqlmigrationMigrationServiceConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SqlmigrationMigrationService } from "@ubx/sdk-azure/azure/data/datamigration/sqlmigration_migration_service";

    export default stack("example", () => {
      intent({
        summary: "look up azure_datamigration_sqlmigration_migration_service",
      });

      data(SqlmigrationMigrationService, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.datamigration import SqlmigrationMigrationService, SqlmigrationMigrationServiceConfig

    def describe():
        ubx.intent("look up azure_datamigration_sqlmigration_migration_service")

        ubx.data(SqlmigrationMigrationService, "example", SqlmigrationMigrationServiceConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
