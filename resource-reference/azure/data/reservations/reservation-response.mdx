---
title: "azure_reservations_reservation_response"
description: "`azure_reservations_reservation_response` looks up a real Azure Reservation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	reservations "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/reservations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_reservations_reservation_response"})

    		ubx.Data(reservations.ReservationResponse, "example", reservations.ReservationResponseConfig{
    			ReservationOrderId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservationResponse } from "@ubx/sdk-azure/azure/data/reservations/reservation_response";

    export default stack("example", () => {
      intent({ summary: "look up azure_reservations_reservation_response" });

      data(ReservationResponse, "example", {
        reservationOrderId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.reservations import ReservationResponse, ReservationResponseConfig

    def describe():
        ubx.intent("look up azure_reservations_reservation_response")

        ubx.data(ReservationResponse, "example", ReservationResponseConfig(
            reservation_order_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="reservation_order_id" type="string" required>
  The real, unique identifier of the reservation order this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ReservationResponse items on this page Returned by the lookup.
</ResponseField>
