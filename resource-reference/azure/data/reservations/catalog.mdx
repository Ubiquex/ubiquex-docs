---
title: "azure_reservations_catalog"
description: "`azure_reservations_catalog` reads Microsoft's own real, published catalog of purchasable Azure Reservation SKUs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	reservations "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/reservations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_reservations_catalog"})

    		ubx.Data(reservations.Catalog, "example", reservations.CatalogConfig{
    			Location:             "example",
    			OfferId:              "example",
    			PlanId:               "example",
    			PublisherId:          "example",
    			ReservedResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Catalog } from "@ubx/sdk-azure/azure/data/reservations/catalog";

    export default stack("example", () => {
      intent({ summary: "look up azure_reservations_catalog" });

      data(Catalog, "example", {
        location: "example",
        offerId: "example",
        planId: "example",
        publisherId: "example",
        reservedResourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.reservations import Catalog, CatalogConfig

    def describe():
        ubx.intent("look up azure_reservations_catalog")

        ubx.data(Catalog, "example", CatalogConfig(
            location="example",
            offer_id="example",
            plan_id="example",
            publisher_id="example",
            reserved_resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="location" type="string">
  The real Azure region this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="offer_id" type="string">
  The real, unique identifier of the Marketplace offer this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="plan_id" type="string">
  The real, unique identifier of the plan this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="publisher_id" type="string">
  The real, unique identifier of the publisher this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_resource_type" type="string">
  The real Azure resource type this reservation applies to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="total_items" type="number">
  The total amount of catalog items. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Catalog items on this page Returned by the lookup.
</ResponseField>
