---
title: "azure_reservations_applied_reservations"
description: "`azure_reservations_applied_reservations` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_reservations_applied_reservations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	reservations "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/reservations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_reservations_applied_reservations"})

    		ubx.Data(reservations.AppliedReservations, "example", reservations.AppliedReservationsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppliedReservations } from "@ubx/sdk-azure/azure/data/reservations/applied_reservations";

    export default stack("example", () => {
      intent({ summary: "look up azure_reservations_applied_reservations" });

      data(AppliedReservations, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.reservations import AppliedReservations, AppliedReservationsConfig

    def describe():
        ubx.intent("look up azure_reservations_applied_reservations")

        ubx.data(AppliedReservations, "example", AppliedReservationsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="id" type="string">
  Identifier of the applied reservations Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Name of resource Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties for applied reservations returned Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="reservation_order_ids" type="object">
      Paginated list of applied reservations Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="next_link" type="string">
          Url to get the next page of reservations Optional lookup argument.
        </ResponseField>
        <ResponseField name="value" type="list(string)">
          Array of reservation resource ids Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Type of resource. "Microsoft.Capacity/AppliedReservations" Returned by the lookup.
</ResponseField>
