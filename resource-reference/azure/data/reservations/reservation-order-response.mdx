---
title: "azure_reservations_reservation_order_response"
description: "`data_azure_reservations_reservation_order_response` looks up a real, already-existing `azure_reservations_reservation_order_response`."
---

It is a purchased Azure Reservation (a committed-use discount for compute, database, or other resources), covering one or more individual reservation(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	reservations "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/reservations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_reservations_reservation_order_response"})

    		ubx.Data(reservations.ReservationOrderResponse, "example", reservations.ReservationOrderResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservationOrderResponse } from "@ubx/sdk-azure/azure/data/reservations/reservation_order_response";

    export default stack("example", () => {
      intent({ summary: "look up azure_reservations_reservation_order_response" });

      data(ReservationOrderResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.reservations import ReservationOrderResponse, ReservationOrderResponseConfig

    def describe():
        ubx.intent("look up azure_reservations_reservation_order_response")

        ubx.data(ReservationOrderResponse, "example", ReservationOrderResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ReservationOrderResponse items on this page Returned by the lookup.
</ResponseField>
