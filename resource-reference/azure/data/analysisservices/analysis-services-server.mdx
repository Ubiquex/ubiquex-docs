---
title: "azure_analysisservices_analysis_services_server"
description: "`azure_analysisservices_analysis_services_server` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_analysisservices_analysis_services_server` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	analysisservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/analysisservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_analysisservices_analysis_services_server"})

    		ubx.Data(analysisservices.AnalysisServicesServer, "example", analysisservices.AnalysisServicesServerConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AnalysisServicesServer } from "@ubx/sdk-azure/azure/data/analysisservices/analysis_services_server";

    export default stack("example", () => {
      intent({
        summary: "look up azure_analysisservices_analysis_services_server",
      });

      data(AnalysisServicesServer, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.analysisservices import AnalysisServicesServer, AnalysisServicesServerConfig

    def describe():
        ubx.intent("look up azure_analysisservices_analysis_services_server")

        ubx.data(AnalysisServicesServer, "example", AnalysisServicesServerConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  An array of Analysis Services resources. Returned by the lookup.
</ResponseField>
