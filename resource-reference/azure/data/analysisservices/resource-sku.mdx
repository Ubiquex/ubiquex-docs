---
title: "azure_analysisservices_resource_sku"
description: "`azure_analysisservices_resource_sku` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_analysisservices_resource_sku` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	analysisservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/analysisservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_analysisservices_resource_sku"})

    		ubx.Data(analysisservices.ResourceSku, "example", analysisservices.ResourceSkuConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceSku } from "@ubx/sdk-azure/azure/data/analysisservices/resource_sku";

    export default stack("example", () => {
      intent({ summary: "look up azure_analysisservices_resource_sku" });

      data(ResourceSku, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.analysisservices import ResourceSku, ResourceSkuConfig

    def describe():
        ubx.intent("look up azure_analysisservices_resource_sku")

        ubx.data(ResourceSku, "example", ResourceSkuConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The collection of available SKUs for new resources. Returned by the lookup.
</ResponseField>
