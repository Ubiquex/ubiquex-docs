---
title: "azure_analysisservices_sku_details_for_existing_resource"
description: "`azure_analysisservices_sku_details_for_existing_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_analysisservices_sku_details_for_existing_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	analysisservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/analysisservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_analysisservices_sku_details_for_existing_resource"})

    		ubx.Data(analysisservices.SkuDetailsForExistingResource, "example", analysisservices.SkuDetailsForExistingResourceConfig{
    			ApiVersion:        "example",
    			ResourceGroupName: "example-resource-group-name",
    			ServerName:        "example-server-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SkuDetailsForExistingResource } from "@ubx/sdk-azure/azure/data/analysisservices/sku_details_for_existing_resource";

    export default stack("example", () => {
      intent({
        summary: "look up azure_analysisservices_sku_details_for_existing_resource",
      });

      data(SkuDetailsForExistingResource, "example", {
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        serverName: "example-server-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.analysisservices import SkuDetailsForExistingResource, SkuDetailsForExistingResourceConfig

    def describe():
        ubx.intent("look up azure_analysisservices_sku_details_for_existing_resource")

        ubx.data(SkuDetailsForExistingResource, "example", SkuDetailsForExistingResourceConfig(
            api_version="example",
            resource_group_name="example-resource-group-name",
            server_name="example-server-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The collection of available SKUs for existing resources. Returned by the lookup.
</ResponseField>
