---
title: "azure_hdinsight_cluster"
description: "`data_azure_hdinsight_cluster` looks up a real, already-existing `azure_hdinsight_cluster`."
---

It is an HDInsight cluster, a managed deployment of open-source analytics frameworks (Hadoop, Spark, Kafka, HBase) on Azure.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	hdinsight "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/hdinsight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_hdinsight_cluster"})

    		ubx.Data(hdinsight.Cluster, "example", hdinsight.ClusterConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Cluster } from "@ubx/sdk-azure/azure/data/hdinsight/cluster";

    export default stack("example", () => {
      intent({ summary: "look up azure_hdinsight_cluster" });

      data(Cluster, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.hdinsight import Cluster, ClusterConfig

    def describe():
        ubx.intent("look up azure_hdinsight_cluster")

        ubx.data(Cluster, "example", ClusterConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of Clusters. Returned by the lookup.
</ResponseField>
