---
title: "azure_hdinsight_applications_azureasyncoperation"
description: "`azure_hdinsight_applications_azureasyncoperation` reads the real, current status of an asynchronous operation installing an application onto..."
---

`azure_hdinsight_applications_azureasyncoperation` reads the real, current status of an asynchronous operation installing an application onto an HDInsight cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	hdinsight "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/hdinsight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_hdinsight_applications_azureasyncoperation"})

    		ubx.Data(hdinsight.ApplicationsAzureasyncoperation, "example", hdinsight.ApplicationsAzureasyncoperationConfig{
    			ApiVersion:        "example",
    			ApplicationName:   "example-application-name",
    			ClusterName:       "example-cluster-name",
    			OperationId:       "example",
    			ResourceGroupName: "example-resource-group-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ApplicationsAzureasyncoperation } from "./local-sdk/azure/sdk/typescript/azure/data/hdinsight/applications_azureasyncoperation.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_hdinsight_applications_azureasyncoperation",
      });

      data(ApplicationsAzureasyncoperation, "example", {
        apiVersion: "example",
        applicationName: "example-application-name",
        clusterName: "example-cluster-name",
        operationId: "example",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.hdinsight import ApplicationsAzureasyncoperation, ApplicationsAzureasyncoperationConfig

    def describe():
        ubx.intent("look up azure_hdinsight_applications_azureasyncoperation")

        ubx.data(ApplicationsAzureasyncoperation, "example", ApplicationsAzureasyncoperationConfig(
            api_version="example",
            application_name="example-application-name",
            cluster_name="example-cluster-name",
            operation_id="example",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="application_name" type="string" required>
  The real name of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="operation_id" type="string" required>
  The real, unique identifier of this asynchronous operation. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error" type="object">
  The error message associated with the cluster creation. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
      The error code. Optional lookup argument.
    </ResponseField>
    <ResponseField name="message" type="string">
      The error message. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  The async operation state. Returned by the lookup.
</ResponseField>
