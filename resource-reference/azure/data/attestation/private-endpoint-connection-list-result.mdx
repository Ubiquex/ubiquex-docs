---
title: "azure_attestation_private_endpoint_connection_list_result"
description: "`azure_attestation_private_endpoint_connection_list_result` lists the real Private Endpoint connections attached to an Azure Attestation provider."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	attestation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/attestation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_attestation_private_endpoint_connection_list_result"})

    		ubx.Data(attestation.PrivateEndpointConnectionListResult, "example", attestation.PrivateEndpointConnectionListResultConfig{
    			ProviderName: "example-provider-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { PrivateEndpointConnectionListResult } from "./local-sdk/azure/sdk/typescript/azure/data/attestation/private_endpoint_connection_list_result.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_attestation_private_endpoint_connection_list_result",
      });

      data(PrivateEndpointConnectionListResult, "example", {
        providerName: "example-provider-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.attestation import PrivateEndpointConnectionListResult, PrivateEndpointConnectionListResultConfig

    def describe():
        ubx.intent("look up azure_attestation_private_endpoint_connection_list_result")

        ubx.data(PrivateEndpointConnectionListResult, "example", PrivateEndpointConnectionListResultConfig(
            provider_name="example-provider-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="provider_name" type="string" required>
  The real name of the resource provider this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrivateEndpointConnection items on this page Returned by the lookup.
</ResponseField>
