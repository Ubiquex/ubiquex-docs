---
title: "azure_attestation_private_link_resource_list_result"
description: "`azure_attestation_private_link_resource_list_result` lists the real Private Link resources exposed by an Azure Attestation provider."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	attestation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/attestation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_attestation_private_link_resource_list_result"})

    		ubx.Data(attestation.PrivateLinkResourceListResult, "example", attestation.PrivateLinkResourceListResultConfig{
    			ProviderName: "example-provider-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PrivateLinkResourceListResult } from "@ubx/sdk-azure/azure/data/attestation/private_link_resource_list_result";

    export default stack("example", () => {
      intent({
        summary: "look up azure_attestation_private_link_resource_list_result",
      });

      data(PrivateLinkResourceListResult, "example", {
        providerName: "example-provider-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.attestation import PrivateLinkResourceListResult, PrivateLinkResourceListResultConfig

    def describe():
        ubx.intent("look up azure_attestation_private_link_resource_list_result")

        ubx.data(PrivateLinkResourceListResult, "example", PrivateLinkResourceListResultConfig(
            provider_name="example-provider-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="provider_name" type="string" required>
  The real name of the resource provider this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrivateLinkResource items on this page Returned by the lookup.
</ResponseField>
