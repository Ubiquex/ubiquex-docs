---
title: "azure_attestation_provider"
description: "`data_azure_attestation_provider` looks up a real, already-existing `azure_attestation_provider`."
---

It -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	attestation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/attestation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own provider"})

    		ubx.Resource(attestation.Provider, "example", attestation.ProviderConfig{
    			Location:   "example",
    			Properties: map[string]any{"policy_signing_certificates": map[string]any{"keys": []map[string]any{{"alg": "example"}}}},
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Provider } from "@ubx/sdk-azure/azure/attestation/provider";

    export default stack("example", () => {
      intent({ summary: "example own provider" });

      resource(Provider, "example", {
        location: "example",
        properties: { policySigningCertificates: { keys: [{ alg: "example" }] } },
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.attestation import Provider, ProviderConfig
    from ubx.azure.attestation.provider import Provider_Properties, Provider_Properties_PolicySigningCertificates, Provider_Properties_PolicySigningCertificates_Keys

    def describe():
        ubx.intent("example own provider")

        ubx.resource(Provider, "example", ProviderConfig(
            location="example",
            properties=Provider_Properties(policy_signing_certificates=Provider_Properties_PolicySigningCertificates(keys=[Provider_Properties_PolicySigningCertificates_Keys(alg="example")])),
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_attestation_provider, with location example,
    properties a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="value" type="list(object)">
  Attestation Provider array. Returned by the lookup.
</ResponseField>
