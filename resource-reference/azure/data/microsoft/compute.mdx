---
title: "azure_microsoft_compute"
description: "`azure_microsoft_compute` looks up a real Microsoft.Compute resource provider registration on a subscription."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	microsoft "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/microsoft"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_microsoft_compute"})

    		ubx.Data(microsoft.Compute, "example", microsoft.ComputeConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { Compute } from "./local-sdk/azure/sdk/typescript/azure/data/microsoft/compute.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_microsoft_compute" });

      data(Compute, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.microsoft import Compute, ComputeConfig

    def describe():
        ubx.intent("look up azure_microsoft_compute")

        ubx.data(Compute, "example", ComputeConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
