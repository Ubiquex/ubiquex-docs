---
title: "azure_servicenetworking_trafficcontroller_security_policy"
description: "`data_azure_servicenetworking_trafficcontroller_security_policy` looks up a real, already-existing..."
---

`data_azure_servicenetworking_trafficcontroller_security_policy` looks up a real, already-existing `azure_servicenetworking_trafficcontroller_security_policy`. It attaches a Web Application Firewall policy to an Azure Application Load Balancer, applying WAF inspection to matching traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	servicenetworking "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/servicenetworking"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_servicenetworking_trafficcontroller_security_policy"})

    		ubx.Data(servicenetworking.TrafficcontrollerSecurityPolicy, "example", servicenetworking.TrafficcontrollerSecurityPolicyConfig{
    			TrafficControllerName: "example-traffic-controller-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { TrafficcontrollerSecurityPolicy } from "./local-sdk/azure/sdk/typescript/azure/data/servicenetworking/trafficcontroller_security_policy.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_servicenetworking_trafficcontroller_security_policy",
      });

      data(TrafficcontrollerSecurityPolicy, "example", {
        trafficControllerName: "example-traffic-controller-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.servicenetworking import TrafficcontrollerSecurityPolicy, TrafficcontrollerSecurityPolicyConfig

    def describe():
        ubx.intent("look up azure_servicenetworking_trafficcontroller_security_policy")

        ubx.data(TrafficcontrollerSecurityPolicy, "example", TrafficcontrollerSecurityPolicyConfig(
            traffic_controller_name="example-traffic-controller-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="traffic_controller_name" type="string" required>
  The real name of the Application Gateway for Containers Traffic Controller this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SecurityPolicy items on this page Returned by the lookup.
</ResponseField>
