---
title: "azure_redhatopenshift_open_shift_cluster"
description: "`data_azure_redhatopenshift_open_shift_cluster` looks up a real, already-existing `azure_redhatopenshift_open_shift_cluster`."
---

It provisions Azure Red Hat OpenShift (ARO), a jointly engineered and supported managed OpenShift Kubernetes distribution.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	redhatopenshift "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/redhatopenshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_redhatopenshift_open_shift_cluster"})

    		ubx.Data(redhatopenshift.OpenShiftCluster, "example", redhatopenshift.OpenShiftClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenShiftCluster } from "./local-sdk/azure/sdk/typescript/azure/data/redhatopenshift/open_shift_cluster.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_redhatopenshift_open_shift_cluster" });

      data(OpenShiftCluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.redhatopenshift import OpenShiftCluster, OpenShiftClusterConfig

    def describe():
        ubx.intent("look up azure_redhatopenshift_open_shift_cluster")

        ubx.data(OpenShiftCluster, "example", OpenShiftClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The OpenShiftCluster items on this page Returned by the lookup.
</ResponseField>
