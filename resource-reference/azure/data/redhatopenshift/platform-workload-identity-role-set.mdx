---
title: "azure_redhatopenshift_platform_workload_identity_role_set"
description: "`azure_redhatopenshift_platform_workload_identity_role_set` reads the real, required Azure roles for an Azure Red Hat OpenShift cluster using workload..."
---

`azure_redhatopenshift_platform_workload_identity_role_set` reads the real, required Azure roles for an Azure Red Hat OpenShift cluster using workload identity federation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redhatopenshift "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/redhatopenshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_redhatopenshift_platform_workload_identity_role_set"})

    		ubx.Data(redhatopenshift.PlatformWorkloadIdentityRoleSet, "example", redhatopenshift.PlatformWorkloadIdentityRoleSetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PlatformWorkloadIdentityRoleSet } from "@ubx/sdk-azure/azure/data/redhatopenshift/platform_workload_identity_role_set";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_redhatopenshift_platform_workload_identity_role_set",
      });

      data(PlatformWorkloadIdentityRoleSet, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.redhatopenshift import PlatformWorkloadIdentityRoleSet, PlatformWorkloadIdentityRoleSetConfig

    def describe():
        ubx.intent("look up azure_redhatopenshift_platform_workload_identity_role_set")

        ubx.data(PlatformWorkloadIdentityRoleSet, "example", PlatformWorkloadIdentityRoleSetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PlatformWorkloadIdentityRoleSet items on this page Returned by the lookup.
</ResponseField>
