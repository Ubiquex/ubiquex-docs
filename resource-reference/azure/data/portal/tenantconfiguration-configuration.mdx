---
title: "azure_portal_tenantconfiguration_configuration"
description: "`data_azure_portal_tenantconfiguration_configuration` looks up a real, already-existing `azure_portal_tenantconfiguration_configuration`."
---

It configures tenant-wide Azure portal settings, e.g. restricting users to only see resources within their own directory.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	portal "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/portal"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_portal_tenantconfiguration_configuration"})

    		ubx.Data(portal.TenantconfigurationConfiguration, "example", portal.TenantconfigurationConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TenantconfigurationConfiguration } from "@ubx/sdk-azure/azure/data/portal/tenantconfiguration_configuration";

    export default stack("example", () => {
      intent({ summary: "look up azure_portal_tenantconfiguration_configuration" });

      data(TenantconfigurationConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.portal import TenantconfigurationConfiguration, TenantconfigurationConfigurationConfig

    def describe():
        ubx.intent("look up azure_portal_tenantconfiguration_configuration")

        ubx.data(TenantconfigurationConfiguration, "example", TenantconfigurationConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Configuration items on this page Returned by the lookup.
</ResponseField>
