---
title: "azure_containerservice_fleets_fleet"
description: "`data_azure_containerservice_fleets_fleet` looks up a real, already-existing `azure_containerservice_fleets_fleet`."
---

It is the top-level AKS Fleet Manager resource, coordinating Kubernetes version upgrades and workload placement across multiple member AKS cluster(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_fleets_fleet"})

    		ubx.Data(containerservice.FleetsFleet, "example", containerservice.FleetsFleetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FleetsFleet } from "@ubx/sdk-azure/azure/data/containerservice/fleets_fleet";

    export default stack("example", () => {
      intent({ summary: "look up azure_containerservice_fleets_fleet" });

      data(FleetsFleet, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import FleetsFleet, FleetsFleetConfig

    def describe():
        ubx.intent("look up azure_containerservice_fleets_fleet")

        ubx.data(FleetsFleet, "example", FleetsFleetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Fleet items on this page Returned by the lookup.
</ResponseField>
