---
title: "azure_containerservice_fleets_gate"
description: "`azure_containerservice_fleets_gate` reads the real, current status of an approval gate blocking a Kubernetes Fleet update run."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_fleets_gate"})

    		ubx.Data(containerservice.FleetsGate, "example", containerservice.FleetsGateConfig{
    			FleetName: "example-fleet-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FleetsGate } from "@ubx/sdk-azure/azure/data/containerservice/fleets_gate";

    export default stack("example", () => {
      intent({ summary: "look up azure_containerservice_fleets_gate" });

      data(FleetsGate, "example", {
        fleetName: "example-fleet-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import FleetsGate, FleetsGateConfig

    def describe():
        ubx.intent("look up azure_containerservice_fleets_gate")

        ubx.data(FleetsGate, "example", FleetsGateConfig(
            fleet_name="example-fleet-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_name" type="string" required>
  The real name of the fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Gate items on this page Returned by the lookup.
</ResponseField>
