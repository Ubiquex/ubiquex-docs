---
title: "azure_containerservice_managedclusters_managed_cluster"
description: "`data_azure_containerservice_managedclusters_managed_cluster` looks up a real, already-existing `azure_containerservice_managedclusters_managed_cluster`."
---

It is an Azure Kubernetes Service (AKS) cluster, the managed Kubernetes control plane and node infrastructure that workload(s) are deployed to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_managed_cluster"})

    		ubx.Data(containerservice.ManagedclustersManagedCluster, "example", containerservice.ManagedclustersManagedClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedclustersManagedCluster } from "@ubx/sdk-azure/azure/data/containerservice/managedclusters_managed_cluster";

    export default stack("example", () => {
      intent({
        summary: "look up azure_containerservice_managedclusters_managed_cluster",
      });

      data(ManagedclustersManagedCluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersManagedCluster, ManagedclustersManagedClusterConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_managed_cluster")

        ubx.data(ManagedclustersManagedCluster, "example", ManagedclustersManagedClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ManagedCluster items on this page Returned by the lookup.
</ResponseField>
