---
title: "azure_containerservice_managedclusters_machine"
description: "`azure_containerservice_managedclusters_machine` looks up a real machine (the underlying VM) backing a node in an AKS cluster's own node pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_machine"})

    		ubx.Data(containerservice.ManagedclustersMachine, "example", containerservice.ManagedclustersMachineConfig{
    			AgentPoolName: "example-agent-pool-name",
    			ResourceName:  "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedclustersMachine } from "@ubx/sdk-azure/azure/data/containerservice/managedclusters_machine";

    export default stack("example", () => {
      intent({ summary: "look up azure_containerservice_managedclusters_machine" });

      data(ManagedclustersMachine, "example", {
        agentPoolName: "example-agent-pool-name",
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersMachine, ManagedclustersMachineConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_machine")

        ubx.data(ManagedclustersMachine, "example", ManagedclustersMachineConfig(
            agent_pool_name="example-agent-pool-name",
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_pool_name" type="string" required>
  The real name of the agent pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Machine items on this page Returned by the lookup.
</ResponseField>
