---
title: "azure_containerservice_managedclusters_kubernetes_version"
description: "`azure_containerservice_managedclusters_kubernetes_version` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_containerservice_managedclusters_kubernetes_version` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_kubernetes_version"})

    		ubx.Data(containerservice.ManagedclustersKubernetesVersion, "example", containerservice.ManagedclustersKubernetesVersionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedclustersKubernetesVersion } from "@ubx/sdk-azure/azure/data/containerservice/managedclusters_kubernetes_version";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_containerservice_managedclusters_kubernetes_version",
      });

      data(ManagedclustersKubernetesVersion, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersKubernetesVersion, ManagedclustersKubernetesVersionConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_kubernetes_version")

        ubx.data(ManagedclustersKubernetesVersion, "example", ManagedclustersKubernetesVersionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="values" type="list(object)">
  Array of AKS supported Kubernetes versions. Returned by the lookup.
</ResponseField>
