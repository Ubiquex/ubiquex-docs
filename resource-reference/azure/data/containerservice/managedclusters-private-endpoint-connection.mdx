---
title: "azure_containerservice_managedclusters_private_endpoint_connection"
description: "`data_azure_containerservice_managedclusters_private_endpoint_connection` looks up a real, already-existing..."
---

`data_azure_containerservice_managedclusters_private_endpoint_connection` looks up a real, already-existing `azure_containerservice_managedclusters_private_endpoint_connection`. It represents the connection between a Private Endpoint and an AKS cluster's own private API server, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_private_endpoint_connection"})

    		ubx.Data(containerservice.ManagedclustersPrivateEndpointConnection, "example", containerservice.ManagedclustersPrivateEndpointConnectionConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedclustersPrivateEndpointConnection } from "@ubx/sdk-azure/azure/data/containerservice/managedclusters_private_endpoint_connection";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_containerservice_managedclusters_private_endpoint_connection",
      });

      data(ManagedclustersPrivateEndpointConnection, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersPrivateEndpointConnection, ManagedclustersPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_private_endpoint_connection")

        ubx.data(ManagedclustersPrivateEndpointConnection, "example", ManagedclustersPrivateEndpointConnectionConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The collection value. Returned by the lookup.
</ResponseField>
