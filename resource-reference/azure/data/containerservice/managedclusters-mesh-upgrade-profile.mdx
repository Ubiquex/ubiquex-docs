---
title: "azure_containerservice_managedclusters_mesh_upgrade_profile"
description: "`azure_containerservice_managedclusters_mesh_upgrade_profile` reads the real, available upgrade path for an AKS cluster's own Istio service mesh add-on."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_mesh_upgrade_profile"})

    		ubx.Data(containerservice.ManagedclustersMeshUpgradeProfile, "example", containerservice.ManagedclustersMeshUpgradeProfileConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ManagedclustersMeshUpgradeProfile } from "./local-sdk/azure/sdk/typescript/azure/data/containerservice/managedclusters_mesh_upgrade_profile.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_containerservice_managedclusters_mesh_upgrade_profile",
      });

      data(ManagedclustersMeshUpgradeProfile, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersMeshUpgradeProfile, ManagedclustersMeshUpgradeProfileConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_mesh_upgrade_profile")

        ubx.data(ManagedclustersMeshUpgradeProfile, "example", ManagedclustersMeshUpgradeProfileConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The MeshUpgradeProfile items on this page Returned by the lookup.
</ResponseField>
