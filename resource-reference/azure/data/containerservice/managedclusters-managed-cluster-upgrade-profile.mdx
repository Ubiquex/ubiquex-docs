---
title: "azure_containerservice_managedclusters_managed_cluster_upgrade_profile"
description: "`azure_containerservice_managedclusters_managed_cluster_upgrade_profile` reads the real, available Kubernetes upgrade paths for an AKS cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_managed_cluster_upgrade_profile"})

    		ubx.Data(containerservice.ManagedclustersManagedClusterUpgradeProfile, "example", containerservice.ManagedclustersManagedClusterUpgradeProfileConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedclustersManagedClusterUpgradeProfile } from "@ubx/sdk-azure/azure/data/containerservice/managedclusters_managed_cluster_upgrade_profile";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_containerservice_managedclusters_managed_cluster_upgrade_profile",
      });

      data(ManagedclustersManagedClusterUpgradeProfile, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersManagedClusterUpgradeProfile, ManagedclustersManagedClusterUpgradeProfileConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_managed_cluster_upgrade_profile")

        ubx.data(ManagedclustersManagedClusterUpgradeProfile, "example", ManagedclustersManagedClusterUpgradeProfileConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  Control plane and agent pool upgrade profiles. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="agent_pool_profiles" type="list(object)" required>
      The list of available upgrade versions for agent pools. Required lookup argument.
    </ResponseField>
    <ResponseField name="control_plane_profile" type="object" required>
      The list of available upgrade versions. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="kubernetes_version" type="string" required>
          The Kubernetes version (major.minor.patch). Required lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          The Agent Pool name. Optional lookup argument.
        </ResponseField>
        <ResponseField name="os_type" type="string" required>
          The operating system type. The default is Linux. Required lookup argument.
        </ResponseField>
        <ResponseField name="upgrades" type="list(object)">
          List of orchestrator types and versions available for upgrade. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
