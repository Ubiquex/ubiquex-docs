---
title: "azure_containerservice_fleets_fleet_member"
description: "`data_azure_containerservice_fleets_fleet_member` looks up a real, already-existing `azure_containerservice_fleets_fleet_member`."
---

It joins an existing AKS cluster to a Fleet, making it eligible for Fleet-orchestrated update runs and multi-cluster workload placement.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_fleets_fleet_member"})

    		ubx.Data(containerservice.FleetsFleetMember, "example", containerservice.FleetsFleetMemberConfig{
    			FleetName: "example-fleet-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { FleetsFleetMember } from "./local-sdk/azure/sdk/typescript/azure/data/containerservice/fleets_fleet_member.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_containerservice_fleets_fleet_member" });

      data(FleetsFleetMember, "example", {
        fleetName: "example-fleet-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import FleetsFleetMember, FleetsFleetMemberConfig

    def describe():
        ubx.intent("look up azure_containerservice_fleets_fleet_member")

        ubx.data(FleetsFleetMember, "example", FleetsFleetMemberConfig(
            fleet_name="example-fleet-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_name" type="string" required>
  The real name of the fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The FleetMember items on this page Returned by the lookup.
</ResponseField>
