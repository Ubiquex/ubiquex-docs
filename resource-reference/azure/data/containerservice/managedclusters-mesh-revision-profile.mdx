---
title: "azure_containerservice_managedclusters_mesh_revision_profile"
description: "`azure_containerservice_managedclusters_mesh_revision_profile` reads a real, available Istio service mesh revision an AKS cluster can run."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_mesh_revision_profile"})

    		ubx.Data(containerservice.ManagedclustersMeshRevisionProfile, "example", containerservice.ManagedclustersMeshRevisionProfileConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ManagedclustersMeshRevisionProfile } from "./local-sdk/azure/sdk/typescript/azure/data/containerservice/managedclusters_mesh_revision_profile.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_containerservice_managedclusters_mesh_revision_profile",
      });

      data(ManagedclustersMeshRevisionProfile, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersMeshRevisionProfile, ManagedclustersMeshRevisionProfileConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_mesh_revision_profile")

        ubx.data(ManagedclustersMeshRevisionProfile, "example", ManagedclustersMeshRevisionProfileConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The MeshRevisionProfile items on this page Returned by the lookup.
</ResponseField>
