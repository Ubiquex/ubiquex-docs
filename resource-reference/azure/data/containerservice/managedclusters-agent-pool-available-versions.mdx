---
title: "azure_containerservice_managedclusters_agent_pool_available_versions"
description: "`azure_containerservice_managedclusters_agent_pool_available_versions` reads the real Kubernetes versions available for upgrading an AKS cluster's..."
---

`azure_containerservice_managedclusters_agent_pool_available_versions` reads the real Kubernetes versions available for upgrading an AKS cluster's own node pool.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_agent_pool_available_versions"})

    		ubx.Data(containerservice.ManagedclustersAgentPoolAvailableVersions, "example", containerservice.ManagedclustersAgentPoolAvailableVersionsConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ManagedclustersAgentPoolAvailableVersions } from "./local-sdk/azure/sdk/typescript/azure/data/containerservice/managedclusters_agent_pool_available_versions.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_containerservice_managedclusters_agent_pool_available_versions",
      });

      data(ManagedclustersAgentPoolAvailableVersions, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersAgentPoolAvailableVersions, ManagedclustersAgentPoolAvailableVersionsConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_agent_pool_available_versions")

        ubx.data(ManagedclustersAgentPoolAvailableVersions, "example", ManagedclustersAgentPoolAvailableVersionsConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="id" type="string">
  The ID of the agent pool version list. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the agent pool version list. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  The list of available agent pool versions. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="agent_pool_versions" type="list(object)">
      List of versions available for agent pool. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Type of the agent pool version list. Returned by the lookup.
</ResponseField>
