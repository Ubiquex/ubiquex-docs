---
title: "azure_containerservice_fleets_fleet_update_strategy"
description: "`data_azure_containerservice_fleets_fleet_update_strategy` looks up a real, already-existing `azure_containerservice_fleets_fleet_update_strategy`."
---

It defines a reusable, ordered stage sequence for rolling an update across an AKS Fleet's own member cluster(s), that update run(s) can reference.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_fleets_fleet_update_strategy"})

    		ubx.Data(containerservice.FleetsFleetUpdateStrategy, "example", containerservice.FleetsFleetUpdateStrategyConfig{
    			FleetName: "example-fleet-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FleetsFleetUpdateStrategy } from "@ubx/sdk-azure/azure/data/containerservice/fleets_fleet_update_strategy";

    export default stack("example", () => {
      intent({
        summary: "look up azure_containerservice_fleets_fleet_update_strategy",
      });

      data(FleetsFleetUpdateStrategy, "example", {
        fleetName: "example-fleet-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import FleetsFleetUpdateStrategy, FleetsFleetUpdateStrategyConfig

    def describe():
        ubx.intent("look up azure_containerservice_fleets_fleet_update_strategy")

        ubx.data(FleetsFleetUpdateStrategy, "example", FleetsFleetUpdateStrategyConfig(
            fleet_name="example-fleet-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_name" type="string" required>
  The real name of the fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The FleetUpdateStrategy items on this page Returned by the lookup.
</ResponseField>
