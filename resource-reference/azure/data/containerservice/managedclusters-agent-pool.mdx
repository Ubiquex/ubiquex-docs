---
title: "azure_containerservice_managedclusters_agent_pool"
description: "`data_azure_containerservice_managedclusters_agent_pool` looks up a real, already-existing `azure_containerservice_managedclusters_agent_pool`."
---

It is a group of node(s) with shared configuration (VM size, scaling, taints) within an AKS cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_agent_pool"})

    		ubx.Data(containerservice.ManagedclustersAgentPool, "example", containerservice.ManagedclustersAgentPoolConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedclustersAgentPool } from "@ubx/sdk-azure/azure/data/containerservice/managedclusters_agent_pool";

    export default stack("example", () => {
      intent({
        summary: "look up azure_containerservice_managedclusters_agent_pool",
      });

      data(ManagedclustersAgentPool, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersAgentPool, ManagedclustersAgentPoolConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_agent_pool")

        ubx.data(ManagedclustersAgentPool, "example", ManagedclustersAgentPoolConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AgentPool items on this page Returned by the lookup.
</ResponseField>
