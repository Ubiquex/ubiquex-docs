---
title: "azure_containerservice_managedclusters_maintenance_configuration"
description: "`data_azure_containerservice_managedclusters_maintenance_configuration` looks up a real, already-existing..."
---

`data_azure_containerservice_managedclusters_maintenance_configuration` looks up a real, already-existing `azure_containerservice_managedclusters_maintenance_configuration`. It defines a preferred maintenance window for control plane and node OS updates on an AKS cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_maintenance_configuration"})

    		ubx.Data(containerservice.ManagedclustersMaintenanceConfiguration, "example", containerservice.ManagedclustersMaintenanceConfigurationConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedclustersMaintenanceConfiguration } from "@ubx/sdk-azure/azure/data/containerservice/managedclusters_maintenance_configuration";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_containerservice_managedclusters_maintenance_configuration",
      });

      data(ManagedclustersMaintenanceConfiguration, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersMaintenanceConfiguration, ManagedclustersMaintenanceConfigurationConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_maintenance_configuration")

        ubx.data(ManagedclustersMaintenanceConfiguration, "example", ManagedclustersMaintenanceConfigurationConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The MaintenanceConfiguration items on this page Returned by the lookup.
</ResponseField>
