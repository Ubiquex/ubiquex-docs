---
title: "azure_containerservice_managedclusters_outbound_environment_endpoint"
description: "`azure_containerservice_managedclusters_outbound_environment_endpoint` reads a real, required outbound network endpoint for an AKS cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	containerservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/containerservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_containerservice_managedclusters_outbound_environment_endpoint"})

    		ubx.Data(containerservice.ManagedclustersOutboundEnvironmentEndpoint, "example", containerservice.ManagedclustersOutboundEnvironmentEndpointConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedclustersOutboundEnvironmentEndpoint } from "@ubx/sdk-azure/azure/data/containerservice/managedclusters_outbound_environment_endpoint";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_containerservice_managedclusters_outbound_environment_endpoint",
      });

      data(ManagedclustersOutboundEnvironmentEndpoint, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.containerservice import ManagedclustersOutboundEnvironmentEndpoint, ManagedclustersOutboundEnvironmentEndpointConfig

    def describe():
        ubx.intent("look up azure_containerservice_managedclusters_outbound_environment_endpoint")

        ubx.data(ManagedclustersOutboundEnvironmentEndpoint, "example", ManagedclustersOutboundEnvironmentEndpointConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The OutboundEnvironmentEndpoint items on this page Returned by the lookup.
</ResponseField>
