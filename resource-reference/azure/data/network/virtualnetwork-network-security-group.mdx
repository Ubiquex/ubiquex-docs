---
title: "azure_network_virtualnetwork_network_security_group"
description: "`azure_network_virtualnetwork_network_security_group` looks up a real Network Security Group -- a set of allow/deny rules filtering traffic to a subnet..."
---

`azure_network_virtualnetwork_network_security_group` looks up a real Network Security Group -- a set of allow/deny rules filtering traffic to a subnet or network interface.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_network_security_group"})

    		ubx.Data(network.VirtualnetworkNetworkSecurityGroup, "example", network.VirtualnetworkNetworkSecurityGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualnetworkNetworkSecurityGroup } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualnetwork_network_security_group.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_network_security_group",
      });

      data(VirtualnetworkNetworkSecurityGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkNetworkSecurityGroup, VirtualnetworkNetworkSecurityGroupConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_network_security_group")

        ubx.data(VirtualnetworkNetworkSecurityGroup, "example", VirtualnetworkNetworkSecurityGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NetworkSecurityGroup items on this page Returned by the lookup.
</ResponseField>
