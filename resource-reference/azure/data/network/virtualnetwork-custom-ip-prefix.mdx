---
title: "azure_network_virtualnetwork_custom_ip_prefix"
description: "`data_azure_network_virtualnetwork_custom_ip_prefix` looks up a real, already-existing `azure_network_virtualnetwork_custom_ip_prefix`."
---

It brings a caller-owned public IP address range into Azure for advertisement and use, via Custom IP Prefix (BYOIP).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_custom_ip_prefix"})

    		ubx.Data(network.VirtualnetworkCustomIpPrefix, "example", network.VirtualnetworkCustomIpPrefixConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualnetworkCustomIpPrefix } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualnetwork_custom_ip_prefix.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualnetwork_custom_ip_prefix" });

      data(VirtualnetworkCustomIpPrefix, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkCustomIpPrefix, VirtualnetworkCustomIpPrefixConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_custom_ip_prefix")

        ubx.data(VirtualnetworkCustomIpPrefix, "example", VirtualnetworkCustomIpPrefixConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The CustomIpPrefix items on this page Returned by the lookup.
</ResponseField>
