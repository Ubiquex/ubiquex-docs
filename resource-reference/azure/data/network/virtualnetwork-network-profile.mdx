---
title: "azure_network_virtualnetwork_network_profile"
description: "`azure_network_virtualnetwork_network_profile` looks up a real network profile -- the networking configuration template used by container groups (Azure..."
---

`azure_network_virtualnetwork_network_profile` looks up a real network profile -- the networking configuration template used by container groups (Azure Container Instances).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_network_profile"})

    		ubx.Data(network.VirtualnetworkNetworkProfile, "example", network.VirtualnetworkNetworkProfileConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualnetworkNetworkProfile } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualnetwork_network_profile.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualnetwork_network_profile" });

      data(VirtualnetworkNetworkProfile, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkNetworkProfile, VirtualnetworkNetworkProfileConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_network_profile")

        ubx.data(VirtualnetworkNetworkProfile, "example", VirtualnetworkNetworkProfileConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NetworkProfile items on this page Returned by the lookup.
</ResponseField>
