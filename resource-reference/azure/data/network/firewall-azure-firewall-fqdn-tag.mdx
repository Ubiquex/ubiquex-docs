---
title: "azure_network_firewall_azure_firewall_fqdn_tag"
description: "`azure_network_firewall_azure_firewall_fqdn_tag` reads a real, Microsoft-managed FQDN tag (such as WindowsUpdate) an Azure Firewall rule can allow..."
---

`azure_network_firewall_azure_firewall_fqdn_tag` reads a real, Microsoft-managed FQDN tag (such as WindowsUpdate) an Azure Firewall rule can allow traffic to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_firewall_azure_firewall_fqdn_tag"})

    		ubx.Data(network.FirewallAzureFirewallFqdnTag, "example", network.FirewallAzureFirewallFqdnTagConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { FirewallAzureFirewallFqdnTag } from "./local-sdk/azure/sdk/typescript/azure/data/network/firewall_azure_firewall_fqdn_tag.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_firewall_azure_firewall_fqdn_tag" });

      data(FirewallAzureFirewallFqdnTag, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import FirewallAzureFirewallFqdnTag, FirewallAzureFirewallFqdnTagConfig

    def describe():
        ubx.intent("look up azure_network_firewall_azure_firewall_fqdn_tag")

        ubx.data(FirewallAzureFirewallFqdnTag, "example", FirewallAzureFirewallFqdnTagConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AzureFirewallFqdnTag items on this page Returned by the lookup.
</ResponseField>
