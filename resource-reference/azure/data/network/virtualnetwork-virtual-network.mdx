---
title: "azure_network_virtualnetwork_virtual_network"
description: "`azure_network_virtualnetwork_virtual_network` looks up a real virtual network (VNet) -- the isolated network boundary Azure resources are deployed into."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_virtual_network"})

    		ubx.Data(network.VirtualnetworkVirtualNetwork, "example", network.VirtualnetworkVirtualNetworkConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkVirtualNetwork } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_virtual_network";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualnetwork_virtual_network" });

      data(VirtualnetworkVirtualNetwork, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkVirtualNetwork, VirtualnetworkVirtualNetworkConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_virtual_network")

        ubx.data(VirtualnetworkVirtualNetwork, "example", VirtualnetworkVirtualNetworkConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualNetwork items on this page Returned by the lookup.
</ResponseField>
