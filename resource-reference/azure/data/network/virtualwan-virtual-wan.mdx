---
title: "azure_network_virtualwan_virtual_wan"
description: "`data_azure_network_virtualwan_virtual_wan` looks up a real, already-existing `azure_network_virtualwan_virtual_wan`."
---

It is the top-level Virtual WAN resource that virtual hub(s) across regions are created within, providing a unified, Microsoft-managed global network backbone.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_virtual_wan"})

    		ubx.Data(network.VirtualwanVirtualWan, "example", network.VirtualwanVirtualWanConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualwanVirtualWan } from "@ubx/sdk-azure/azure/data/network/virtualwan_virtual_wan";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualwan_virtual_wan" });

      data(VirtualwanVirtualWan, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanVirtualWan, VirtualwanVirtualWanConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_virtual_wan")

        ubx.data(VirtualwanVirtualWan, "example", VirtualwanVirtualWanConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualWAN items on this page Returned by the lookup.
</ResponseField>
