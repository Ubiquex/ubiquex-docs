---
title: "azure_network_networkmanager_admin_rule_collection"
description: "`data_azure_network_networkmanager_admin_rule_collection` looks up a real, already-existing `azure_network_networkmanager_admin_rule_collection`."
---

It groups related security admin rule(s) together within an Azure Virtual Network Manager security admin configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networkmanager_admin_rule_collection"})

    		ubx.Data(network.NetworkmanagerAdminRuleCollection, "example", network.NetworkmanagerAdminRuleCollectionConfig{
    			ConfigurationName:  "example-configuration-name",
    			NetworkManagerName: "example-network-manager-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { NetworkmanagerAdminRuleCollection } from "./local-sdk/azure/sdk/typescript/azure/data/network/networkmanager_admin_rule_collection.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_networkmanager_admin_rule_collection",
      });

      data(NetworkmanagerAdminRuleCollection, "example", {
        configurationName: "example-configuration-name",
        networkManagerName: "example-network-manager-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworkmanagerAdminRuleCollection, NetworkmanagerAdminRuleCollectionConfig

    def describe():
        ubx.intent("look up azure_network_networkmanager_admin_rule_collection")

        ubx.data(NetworkmanagerAdminRuleCollection, "example", NetworkmanagerAdminRuleCollectionConfig(
            configuration_name="example-configuration-name",
            network_manager_name="example-network-manager-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_name" type="string" required>
  The real name of the configuration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="network_manager_name" type="string" required>
  The real name of the Azure Virtual Network Manager this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AdminRuleCollection items on this page Returned by the lookup.
</ResponseField>
