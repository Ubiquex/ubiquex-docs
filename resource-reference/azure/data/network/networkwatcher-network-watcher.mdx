---
title: "azure_network_networkwatcher_network_watcher"
description: "`data_azure_network_networkwatcher_network_watcher` looks up a real, already-existing `azure_network_networkwatcher_network_watcher`."
---

It is the regional Network Watcher resource that diagnostic and monitoring tool(s) (flow logs, connection monitors, packet capture) are created under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networkwatcher_network_watcher"})

    		ubx.Data(network.NetworkwatcherNetworkWatcher, "example", network.NetworkwatcherNetworkWatcherConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkwatcherNetworkWatcher } from "@ubx/sdk-azure/azure/data/network/networkwatcher_network_watcher";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_networkwatcher_network_watcher" });

      data(NetworkwatcherNetworkWatcher, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworkwatcherNetworkWatcher, NetworkwatcherNetworkWatcherConfig

    def describe():
        ubx.intent("look up azure_network_networkwatcher_network_watcher")

        ubx.data(NetworkwatcherNetworkWatcher, "example", NetworkwatcherNetworkWatcherConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of network watcher resources. Returned by the lookup.
</ResponseField>
