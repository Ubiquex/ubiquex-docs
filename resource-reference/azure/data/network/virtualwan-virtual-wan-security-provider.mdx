---
title: "azure_network_virtualwan_virtual_wan_security_provider"
description: "`azure_network_virtualwan_virtual_wan_security_provider` reads a real, available third-party security provider (NVA) that can be integrated into..."
---

`azure_network_virtualwan_virtual_wan_security_provider` reads a real, available third-party security provider (NVA) that can be integrated into a Virtual WAN.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_virtual_wan_security_provider"})

    		ubx.Data(network.VirtualwanVirtualWanSecurityProvider, "example", network.VirtualwanVirtualWanSecurityProviderConfig{
    			VirtualWanname: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualwanVirtualWanSecurityProvider } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualwan_virtual_wan_security_provider.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualwan_virtual_wan_security_provider",
      });

      data(VirtualwanVirtualWanSecurityProvider, "example", {
        virtualWanname: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanVirtualWanSecurityProvider, VirtualwanVirtualWanSecurityProviderConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_virtual_wan_security_provider")

        ubx.data(VirtualwanVirtualWanSecurityProvider, "example", VirtualwanVirtualWanSecurityProviderConfig(
            virtual_wanname="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="virtual_wanname" type="string" required>
  The real name of the Virtual WAN this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="supported_providers" type="list(object)">
  List of VirtualWAN security providers. Returned by the lookup.
</ResponseField>
