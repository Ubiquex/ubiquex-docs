---
title: "azure_network_applicationgateway_application_gateway_private_endpoint_connection"
description: "`data_azure_network_applicationgateway_application_gateway_private_endpoint_connection` looks up a real, already-existing..."
---

`data_azure_network_applicationgateway_application_gateway_private_endpoint_connection` looks up a real, already-existing `azure_network_applicationgateway_application_gateway_private_endpoint_connection`. It represents the connection between a Private Endpoint and an Application Gateway's own private frontend, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_applicationgateway_application_gateway_private_endpoint_connection"})

    		ubx.Data(network.ApplicationgatewayApplicationGatewayPrivateEndpointConnection, "example", network.ApplicationgatewayApplicationGatewayPrivateEndpointConnectionConfig{
    			ApplicationGatewayName: "example-application-gateway-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationgatewayApplicationGatewayPrivateEndpointConnection } from "@ubx/sdk-azure/azure/data/network/applicationgateway_application_gateway_private_endpoint_connection";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_network_applicationgateway_application_gateway_private_endpoint_connection",
      });

      data(
        ApplicationgatewayApplicationGatewayPrivateEndpointConnection,
        "example",
        {
          applicationGatewayName: "example-application-gateway-name",
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import ApplicationgatewayApplicationGatewayPrivateEndpointConnection, ApplicationgatewayApplicationGatewayPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("look up azure_network_applicationgateway_application_gateway_private_endpoint_connection")

        ubx.data(ApplicationgatewayApplicationGatewayPrivateEndpointConnection, "example", ApplicationgatewayApplicationGatewayPrivateEndpointConnectionConfig(
            application_gateway_name="example-application-gateway-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_gateway_name" type="string" required>
  The real name of the Application Gateway this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ApplicationGatewayPrivateEndpointConnection items on this page Returned by the lookup.
</ResponseField>
