---
title: "azure_network_virtualwan_vpn_site_link"
description: "`azure_network_virtualwan_vpn_site_link` looks up one real physical link (an ISP connection) at a Virtual WAN VPN site."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_vpn_site_link"})

    		ubx.Data(network.VirtualwanVpnSiteLink, "example", network.VirtualwanVpnSiteLinkConfig{
    			VpnSiteName: "example-vpn-site-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualwanVpnSiteLink } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualwan_vpn_site_link.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualwan_vpn_site_link" });

      data(VirtualwanVpnSiteLink, "example", {
        vpnSiteName: "example-vpn-site-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanVpnSiteLink, VirtualwanVpnSiteLinkConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_vpn_site_link")

        ubx.data(VirtualwanVpnSiteLink, "example", VirtualwanVpnSiteLinkConfig(
            vpn_site_name="example-vpn-site-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="vpn_site_name" type="string" required>
  The real name of the Virtual WAN VPN site this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VpnSiteLink items on this page Returned by the lookup.
</ResponseField>
