---
title: "azure_network_firewallpolicy_firewall_policy_draft"
description: "`azure_network_firewallpolicy_firewall_policy_draft` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_firewallpolicy_firewall_policy_draft` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_firewallpolicy_firewall_policy_draft"})

    		ubx.Data(network.FirewallpolicyFirewallPolicyDraft, "example", network.FirewallpolicyFirewallPolicyDraftConfig{
    			FirewallPolicyName: "example-firewall-policy-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallpolicyFirewallPolicyDraft } from "@ubx/sdk-azure/azure/data/network/firewallpolicy_firewall_policy_draft";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_firewallpolicy_firewall_policy_draft",
      });

      data(FirewallpolicyFirewallPolicyDraft, "example", {
        firewallPolicyName: "example-firewall-policy-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import FirewallpolicyFirewallPolicyDraft, FirewallpolicyFirewallPolicyDraftConfig

    def describe():
        ubx.intent("look up azure_network_firewallpolicy_firewall_policy_draft")

        ubx.data(FirewallpolicyFirewallPolicyDraft, "example", FirewallpolicyFirewallPolicyDraftConfig(
            firewall_policy_name="example-firewall-policy-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="firewall_policy_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="base_policy" type="object">
      Reference to another subresource. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dns_settings" type="object">
      DNS Proxy Settings in Firewall Policy. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="enable_proxy" type="bool">
          Enable DNS Proxy on Firewalls attached to the Firewall Policy. Optional lookup argument.
        </ResponseField>
        <ResponseField name="require_proxy_for_network_rules" type="bool">
          FQDNs in Network Rules are supported when set to true. Optional lookup argument.
        </ResponseField>
        <ResponseField name="servers" type="list(string)">
          List of Custom DNS Servers. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="explicit_proxy" type="object">
      Explicit Proxy Settings in Firewall Policy. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="enable_explicit_proxy" type="bool">
          When set to true, explicit proxy mode is enabled. Optional lookup argument.
        </ResponseField>
        <ResponseField name="enable_pac_file" type="bool">
          When set to true, pac file port and url needs to be provided. Optional lookup argument.
        </ResponseField>
        <ResponseField name="http_port" type="number">
          Port number for explicit proxy http protocol, cannot be greater than 64000. Optional lookup argument.
        </ResponseField>
        <ResponseField name="https_port" type="number">
          Port number for explicit proxy https protocol, cannot be greater than 64000. Optional lookup argument.
        </ResponseField>
        <ResponseField name="pac_file" type="string">
          SAS URL for PAC file. Optional lookup argument.
        </ResponseField>
        <ResponseField name="pac_file_port" type="number">
          Port number for firewall to serve PAC file. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="insights" type="object">
      Firewall Policy Insights. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="is_enabled" type="bool">
          A flag to indicate if the insights are enabled on the policy. Optional lookup argument.
        </ResponseField>
        <ResponseField name="log_analytics_resources" type="object">
          Log Analytics Resources for Firewall Policy Insights. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="default_workspace_id" type="object">
              Reference to another subresource. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  Resource ID. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="workspaces" type="list(object)">
              List of workspaces for Firewall Policy Insights. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="retention_days" type="number">
          Number of days the insights should be enabled on the policy. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="intrusion_detection" type="object">
      Configuration for intrusion detection mode and rules. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="configuration" type="object">
          The operation for configuring intrusion detection. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="bypass_traffic_settings" type="list(object)">
              List of rules for traffic to bypass. Optional lookup argument.
            </ResponseField>
            <ResponseField name="private_ranges" type="list(string)">
              IDPS Private IP address ranges are used to identify traffic direction (i.e. inbound, outbound, etc.). By default, only ranges defined by IANA RFC 1918 are considered private IP addresses. To modify default ranges, specify your Private IP address ranges with this property Optional lookup argument.
            </ResponseField>
            <ResponseField name="signature_overrides" type="list(object)">
              List of specific signatures states. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="mode" type="string">
          Possible state values. Optional lookup argument.
        </ResponseField>
        <ResponseField name="profile" type="string">
          Specifies the Intrusion Detection signature profile to apply. Values: - Off: IDPS profiles disabled; uses the same signature set that existed before profiles. - Emerging: Signatures of the newest, most recent threats. - Core: Complete, modern, standard set of signatures. - Extended: Core signatures plus older legacy signatures for maximum coverage. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="snat" type="object">
      The private IP addresses/IP ranges to which traffic will not be SNAT. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="auto_learn_private_ranges" type="string">
          The operation mode for automatically learning private ranges to not be SNAT Optional lookup argument.
        </ResponseField>
        <ResponseField name="private_ranges" type="list(string)">
          List of private IP addresses/IP address ranges to not be SNAT. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sql" type="object">
      SQL Settings in Firewall Policy. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="allow_sql_redirect" type="bool">
          A flag to indicate if SQL Redirect traffic filtering is enabled. Turning on the flag requires no rule using port 11000-11999. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="threat_intel_mode" type="string">
      The operation mode for Threat Intel. Optional lookup argument.
    </ResponseField>
    <ResponseField name="threat_intel_whitelist" type="object">
      ThreatIntel Whitelist for Firewall Policy. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="fqdns" type="list(string)">
          List of FQDNs for the ThreatIntel Whitelist. Optional lookup argument.
        </ResponseField>
        <ResponseField name="ip_addresses" type="list(string)">
          List of IP addresses for the ThreatIntel Whitelist. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
