---
title: "azure_network_virtualnetwork_route_table"
description: "`azure_network_virtualnetwork_route_table` looks up a real route table -- a set of custom routes controlling how traffic from a subnet is directed."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_route_table"})

    		ubx.Data(network.VirtualnetworkRouteTable, "example", network.VirtualnetworkRouteTableConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkRouteTable } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_route_table";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualnetwork_route_table" });

      data(VirtualnetworkRouteTable, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkRouteTable, VirtualnetworkRouteTableConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_route_table")

        ubx.data(VirtualnetworkRouteTable, "example", VirtualnetworkRouteTableConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The RouteTable items on this page Returned by the lookup.
</ResponseField>
