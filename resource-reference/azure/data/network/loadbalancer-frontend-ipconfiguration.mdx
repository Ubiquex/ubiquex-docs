---
title: "azure_network_loadbalancer_frontend_ipconfiguration"
description: "`azure_network_loadbalancer_frontend_ipconfiguration` reads a real, configured frontend IP address on a Load Balancer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_loadbalancer_frontend_ipconfiguration"})

    		ubx.Data(network.LoadbalancerFrontendIpconfiguration, "example", network.LoadbalancerFrontendIpconfigurationConfig{
    			LoadBalancerName: "example-load-balancer-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LoadbalancerFrontendIpconfiguration } from "@ubx/sdk-azure/azure/data/network/loadbalancer_frontend_ipconfiguration";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_loadbalancer_frontend_ipconfiguration",
      });

      data(LoadbalancerFrontendIpconfiguration, "example", {
        loadBalancerName: "example-load-balancer-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import LoadbalancerFrontendIpconfiguration, LoadbalancerFrontendIpconfigurationConfig

    def describe():
        ubx.intent("look up azure_network_loadbalancer_frontend_ipconfiguration")

        ubx.data(LoadbalancerFrontendIpconfiguration, "example", LoadbalancerFrontendIpconfigurationConfig(
            load_balancer_name="example-load-balancer-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="load_balancer_name" type="string" required>
  The real name of the Load Balancer this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The FrontendIPConfiguration items on this page Returned by the lookup.
</ResponseField>
