---
title: "azure_network_networkwatcher_flow_log"
description: "`azure_network_networkwatcher_flow_log` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_networkwatcher_flow_log` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networkwatcher_flow_log"})

    		ubx.Data(network.NetworkwatcherFlowLog, "example", network.NetworkwatcherFlowLogConfig{
    			NetworkWatcherName: "example-network-watcher-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkwatcherFlowLog } from "@ubx/sdk-azure/azure/data/network/networkwatcher_flow_log";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_networkwatcher_flow_log" });

      data(NetworkwatcherFlowLog, "example", {
        networkWatcherName: "example-network-watcher-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworkwatcherFlowLog, NetworkwatcherFlowLogConfig

    def describe():
        ubx.intent("look up azure_network_networkwatcher_flow_log")

        ubx.data(NetworkwatcherFlowLog, "example", NetworkwatcherFlowLogConfig(
            network_watcher_name="example-network-watcher-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_watcher_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The FlowLog items on this page Returned by the lookup.
</ResponseField>
