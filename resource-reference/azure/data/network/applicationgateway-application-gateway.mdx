---
title: "azure_network_applicationgateway_application_gateway"
description: "`data_azure_network_applicationgateway_application_gateway` looks up a real, already-existing `azure_network_applicationgateway_application_gateway`."
---

It is an Azure Application Gateway, a regional Layer 7 load balancer with integrated Web Application Firewall support.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_applicationgateway_application_gateway"})

    		ubx.Data(network.ApplicationgatewayApplicationGateway, "example", network.ApplicationgatewayApplicationGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationgatewayApplicationGateway } from "@ubx/sdk-azure/azure/data/network/applicationgateway_application_gateway";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_applicationgateway_application_gateway",
      });

      data(ApplicationgatewayApplicationGateway, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import ApplicationgatewayApplicationGateway, ApplicationgatewayApplicationGatewayConfig

    def describe():
        ubx.intent("look up azure_network_applicationgateway_application_gateway")

        ubx.data(ApplicationgatewayApplicationGateway, "example", ApplicationgatewayApplicationGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ApplicationGateway items on this page Returned by the lookup.
</ResponseField>
