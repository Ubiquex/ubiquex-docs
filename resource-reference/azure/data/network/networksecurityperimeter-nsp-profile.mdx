---
title: "azure_network_networksecurityperimeter_nsp_profile"
description: "`data_azure_network_networksecurityperimeter_nsp_profile` looks up a real, already-existing `azure_network_networksecurityperimeter_nsp_profile`."
---

It groups access rule(s) together within a Network Security Perimeter, that associated resource(s) inherit collectively.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networksecurityperimeter_nsp_profile"})

    		ubx.Data(network.NetworksecurityperimeterNspProfile, "example", network.NetworksecurityperimeterNspProfileConfig{
    			NetworkSecurityPerimeterName: "example-network-security-perimeter-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { NetworksecurityperimeterNspProfile } from "./local-sdk/azure/sdk/typescript/azure/data/network/networksecurityperimeter_nsp_profile.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_networksecurityperimeter_nsp_profile",
      });

      data(NetworksecurityperimeterNspProfile, "example", {
        networkSecurityPerimeterName: "example-network-security-perimeter-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworksecurityperimeterNspProfile, NetworksecurityperimeterNspProfileConfig

    def describe():
        ubx.intent("look up azure_network_networksecurityperimeter_nsp_profile")

        ubx.data(NetworksecurityperimeterNspProfile, "example", NetworksecurityperimeterNspProfileConfig(
            network_security_perimeter_name="example-network-security-perimeter-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_security_perimeter_name" type="string" required>
  The real name of the Network Security Perimeter this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NspProfile items on this page Returned by the lookup.
</ResponseField>
