---
title: "azure_network_loadbalancer_outbound_rule"
description: "`azure_network_loadbalancer_outbound_rule` looks up a real outbound SNAT rule configured on a Load Balancer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_loadbalancer_outbound_rule"})

    		ubx.Data(network.LoadbalancerOutboundRule, "example", network.LoadbalancerOutboundRuleConfig{
    			LoadBalancerName: "example-load-balancer-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { LoadbalancerOutboundRule } from "./local-sdk/azure/sdk/typescript/azure/data/network/loadbalancer_outbound_rule.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_loadbalancer_outbound_rule" });

      data(LoadbalancerOutboundRule, "example", {
        loadBalancerName: "example-load-balancer-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import LoadbalancerOutboundRule, LoadbalancerOutboundRuleConfig

    def describe():
        ubx.intent("look up azure_network_loadbalancer_outbound_rule")

        ubx.data(LoadbalancerOutboundRule, "example", LoadbalancerOutboundRuleConfig(
            load_balancer_name="example-load-balancer-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="load_balancer_name" type="string" required>
  The real name of the Load Balancer this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The OutboundRule items on this page Returned by the lookup.
</ResponseField>
