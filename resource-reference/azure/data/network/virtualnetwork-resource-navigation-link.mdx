---
title: "azure_network_virtualnetwork_resource_navigation_link"
description: "`azure_network_virtualnetwork_resource_navigation_link` reads a real link from a subnet to the delegated service resource that owns it."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_resource_navigation_link"})

    		ubx.Data(network.VirtualnetworkResourceNavigationLink, "example", network.VirtualnetworkResourceNavigationLinkConfig{
    			SubnetName:         "example-subnet-name",
    			VirtualNetworkName: "example-virtual-network-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkResourceNavigationLink } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_resource_navigation_link";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_resource_navigation_link",
      });

      data(VirtualnetworkResourceNavigationLink, "example", {
        subnetName: "example-subnet-name",
        virtualNetworkName: "example-virtual-network-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkResourceNavigationLink, VirtualnetworkResourceNavigationLinkConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_resource_navigation_link")

        ubx.data(VirtualnetworkResourceNavigationLink, "example", VirtualnetworkResourceNavigationLinkConfig(
            subnet_name="example-subnet-name",
            virtual_network_name="example-virtual-network-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="subnet_name" type="string" required>
  The real name of the subnet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="virtual_network_name" type="string" required>
  The real name of the virtual network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ResourceNavigationLink items on this page Returned by the lookup.
</ResponseField>
