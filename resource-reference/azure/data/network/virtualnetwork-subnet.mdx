---
title: "azure_network_virtualnetwork_subnet"
description: "`azure_network_virtualnetwork_subnet` looks up a real subnet within a virtual network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_subnet"})

    		ubx.Data(network.VirtualnetworkSubnet, "example", network.VirtualnetworkSubnetConfig{
    			VirtualNetworkName: "example-virtual-network-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkSubnet } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_subnet";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualnetwork_subnet" });

      data(VirtualnetworkSubnet, "example", {
        virtualNetworkName: "example-virtual-network-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkSubnet, VirtualnetworkSubnetConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_subnet")

        ubx.data(VirtualnetworkSubnet, "example", VirtualnetworkSubnetConfig(
            virtual_network_name="example-virtual-network-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="virtual_network_name" type="string" required>
  The real name of the virtual network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Subnet items on this page Returned by the lookup.
</ResponseField>
