---
title: "azure_network_applicationgateway_application_gateway_available_ssl_options"
description: "`azure_network_applicationgateway_application_gateway_available_ssl_options` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_applicationgateway_application_gateway_available_ssl_options` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_applicationgateway_application_gateway_available_ssl_options"})

    		ubx.Data(network.ApplicationgatewayApplicationGatewayAvailableSslOptions, "example", network.ApplicationgatewayApplicationGatewayAvailableSslOptionsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationgatewayApplicationGatewayAvailableSslOptions } from "@ubx/sdk-azure/azure/data/network/applicationgateway_application_gateway_available_ssl_options";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_network_applicationgateway_application_gateway_available_ssl_options",
      });

      data(ApplicationgatewayApplicationGatewayAvailableSslOptions, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import ApplicationgatewayApplicationGatewayAvailableSslOptions, ApplicationgatewayApplicationGatewayAvailableSslOptionsConfig

    def describe():
        ubx.intent("look up azure_network_applicationgateway_application_gateway_available_ssl_options")

        ubx.data(ApplicationgatewayApplicationGatewayAvailableSslOptions, "example", ApplicationgatewayApplicationGatewayAvailableSslOptionsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="properties" type="object">
  Properties of ApplicationGatewayAvailableSslOptions. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="available_cipher_suites" type="list(string)">
      List of available Ssl cipher suites. Optional lookup argument.
    </ResponseField>
    <ResponseField name="available_protocols" type="list(string)">
      List of available Ssl protocols. Optional lookup argument.
    </ResponseField>
    <ResponseField name="default_policy" type="string">
      Ssl predefined policy name enums. Optional lookup argument.
    </ResponseField>
    <ResponseField name="predefined_policies" type="list(object)">
      List of available Ssl predefined policy. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
