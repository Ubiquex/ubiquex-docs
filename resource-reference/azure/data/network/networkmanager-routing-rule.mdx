---
title: "azure_network_networkmanager_routing_rule"
description: "`data_azure_network_networkmanager_routing_rule` looks up a real, already-existing `azure_network_networkmanager_routing_rule`."
---

It is a single centrally managed route within a Virtual Network Manager routing configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networkmanager_routing_rule"})

    		ubx.Data(network.NetworkmanagerRoutingRule, "example", network.NetworkmanagerRoutingRuleConfig{
    			ConfigurationName:  "example-configuration-name",
    			NetworkManagerName: "example-network-manager-name",
    			RuleCollectionName: "example-rule-collection-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkmanagerRoutingRule } from "@ubx/sdk-azure/azure/data/network/networkmanager_routing_rule";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_networkmanager_routing_rule" });

      data(NetworkmanagerRoutingRule, "example", {
        configurationName: "example-configuration-name",
        networkManagerName: "example-network-manager-name",
        ruleCollectionName: "example-rule-collection-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworkmanagerRoutingRule, NetworkmanagerRoutingRuleConfig

    def describe():
        ubx.intent("look up azure_network_networkmanager_routing_rule")

        ubx.data(NetworkmanagerRoutingRule, "example", NetworkmanagerRoutingRuleConfig(
            configuration_name="example-configuration-name",
            network_manager_name="example-network-manager-name",
            rule_collection_name="example-rule-collection-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_name" type="string" required>
  The real name of the configuration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="network_manager_name" type="string" required>
  The real name of the Azure Virtual Network Manager this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="rule_collection_name" type="string" required>
  The real name of the rule collection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The RoutingRule items on this page Returned by the lookup.
</ResponseField>
