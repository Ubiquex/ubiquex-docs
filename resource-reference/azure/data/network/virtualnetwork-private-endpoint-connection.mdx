---
title: "azure_network_virtualnetwork_private_endpoint_connection"
description: "`azure_network_virtualnetwork_private_endpoint_connection` reads the real connection state between a Private Endpoint and the service it targets."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_private_endpoint_connection"})

    		ubx.Data(network.VirtualnetworkPrivateEndpointConnection, "example", network.VirtualnetworkPrivateEndpointConnectionConfig{
    			ServiceName: "example-service-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualnetworkPrivateEndpointConnection } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualnetwork_private_endpoint_connection.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_private_endpoint_connection",
      });

      data(VirtualnetworkPrivateEndpointConnection, "example", {
        serviceName: "example-service-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkPrivateEndpointConnection, VirtualnetworkPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_private_endpoint_connection")

        ubx.data(VirtualnetworkPrivateEndpointConnection, "example", VirtualnetworkPrivateEndpointConnectionConfig(
            service_name="example-service-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="service_name" type="string" required>
  The real name of the service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrivateEndpointConnection items on this page Returned by the lookup.
</ResponseField>
