---
title: "azure_network_expressroute_express_route_lag_member"
description: "`azure_network_expressroute_express_route_lag_member` reads a real member interface participating in an ExpressRoute Direct link aggregation group (LAG)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_expressroute_express_route_lag_member"})

    		ubx.Data(network.ExpressrouteExpressRouteLagMember, "example", network.ExpressrouteExpressRouteLagMemberConfig{
    			ExpressRouteLagName: "example-express-route-lag-name",
    			LinkName:            "example-link-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExpressrouteExpressRouteLagMember } from "@ubx/sdk-azure/azure/data/network/expressroute_express_route_lag_member";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_expressroute_express_route_lag_member",
      });

      data(ExpressrouteExpressRouteLagMember, "example", {
        expressRouteLagName: "example-express-route-lag-name",
        linkName: "example-link-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import ExpressrouteExpressRouteLagMember, ExpressrouteExpressRouteLagMemberConfig

    def describe():
        ubx.intent("look up azure_network_expressroute_express_route_lag_member")

        ubx.data(ExpressrouteExpressRouteLagMember, "example", ExpressrouteExpressRouteLagMemberConfig(
            express_route_lag_name="example-express-route-lag-name",
            link_name="example-link-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="express_route_lag_name" type="string" required>
  The real name of the ExpressRoute link aggregation group (LAG) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="link_name" type="string" required>
  The real name of the link this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ExpressRouteLagMember items on this page Returned by the lookup.
</ResponseField>
