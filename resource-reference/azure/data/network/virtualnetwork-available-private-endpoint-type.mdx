---
title: "azure_network_virtualnetwork_available_private_endpoint_type"
description: "`azure_network_virtualnetwork_available_private_endpoint_type` reads a real, available Private Endpoint type (target resource type) in a given region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_available_private_endpoint_type"})

    		ubx.Data(network.VirtualnetworkAvailablePrivateEndpointType, "example", network.VirtualnetworkAvailablePrivateEndpointTypeConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkAvailablePrivateEndpointType } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_available_private_endpoint_type";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_network_virtualnetwork_available_private_endpoint_type",
      });

      data(VirtualnetworkAvailablePrivateEndpointType, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkAvailablePrivateEndpointType, VirtualnetworkAvailablePrivateEndpointTypeConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_available_private_endpoint_type")

        ubx.data(VirtualnetworkAvailablePrivateEndpointType, "example", VirtualnetworkAvailablePrivateEndpointTypeConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AvailablePrivateEndpointType items on this page Returned by the lookup.
</ResponseField>
