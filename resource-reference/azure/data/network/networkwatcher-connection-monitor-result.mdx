---
title: "azure_network_networkwatcher_connection_monitor_result"
description: "`data_azure_network_networkwatcher_connection_monitor_result` looks up a real, already-existing `azure_network_networkwatcher_connection_monitor_result`."
---

It continuously monitors connectivity and latency between a source and destination over time, alerting on connectivity issues rather than checking just once.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networkwatcher_connection_monitor_result"})

    		ubx.Data(network.NetworkwatcherConnectionMonitorResult, "example", network.NetworkwatcherConnectionMonitorResultConfig{
    			NetworkWatcherName: "example-network-watcher-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkwatcherConnectionMonitorResult } from "@ubx/sdk-azure/azure/data/network/networkwatcher_connection_monitor_result";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_networkwatcher_connection_monitor_result",
      });

      data(NetworkwatcherConnectionMonitorResult, "example", {
        networkWatcherName: "example-network-watcher-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworkwatcherConnectionMonitorResult, NetworkwatcherConnectionMonitorResultConfig

    def describe():
        ubx.intent("look up azure_network_networkwatcher_connection_monitor_result")

        ubx.data(NetworkwatcherConnectionMonitorResult, "example", NetworkwatcherConnectionMonitorResultConfig(
            network_watcher_name="example-network-watcher-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_watcher_name" type="string" required>
  The real name of the Network Watcher this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Information about connection monitors. Returned by the lookup.
</ResponseField>
