---
title: "azure_network_networksecurityperimeter_nsp_association"
description: "`data_azure_network_networksecurityperimeter_nsp_association` looks up a real, already-existing `azure_network_networksecurityperimeter_nsp_association`."
---

It adds a specific PaaS resource to a Network Security Perimeter profile, bringing it under that perimeter's own access rules.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networksecurityperimeter_nsp_association"})

    		ubx.Data(network.NetworksecurityperimeterNspAssociation, "example", network.NetworksecurityperimeterNspAssociationConfig{
    			NetworkSecurityPerimeterName: "example-network-security-perimeter-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { NetworksecurityperimeterNspAssociation } from "./local-sdk/azure/sdk/typescript/azure/data/network/networksecurityperimeter_nsp_association.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_networksecurityperimeter_nsp_association",
      });

      data(NetworksecurityperimeterNspAssociation, "example", {
        networkSecurityPerimeterName: "example-network-security-perimeter-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworksecurityperimeterNspAssociation, NetworksecurityperimeterNspAssociationConfig

    def describe():
        ubx.intent("look up azure_network_networksecurityperimeter_nsp_association")

        ubx.data(NetworksecurityperimeterNspAssociation, "example", NetworksecurityperimeterNspAssociationConfig(
            network_security_perimeter_name="example-network-security-perimeter-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_security_perimeter_name" type="string" required>
  The real name of the Network Security Perimeter this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NspAssociation items on this page Returned by the lookup.
</ResponseField>
