---
title: "azure_network_virtualnetwork_verifier_workspace"
description: "`data_azure_network_virtualnetwork_verifier_workspace` looks up a real, already-existing `azure_network_virtualnetwork_verifier_workspace`."
---

It is the top-level Azure Virtual Network Manager resource that reachability analysis intent(s) and their own analysis run(s) are created under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_verifier_workspace"})

    		ubx.Data(network.VirtualnetworkVerifierWorkspace, "example", network.VirtualnetworkVerifierWorkspaceConfig{
    			NetworkManagerName: "example-network-manager-name",
    			Skip:               1,
    			SkipToken:          "example",
    			SortKey:            "example",
    			SortValue:          "example",
    			Top:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualnetworkVerifierWorkspace } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualnetwork_verifier_workspace.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_verifier_workspace",
      });

      data(VirtualnetworkVerifierWorkspace, "example", {
        networkManagerName: "example-network-manager-name",
        skip: 1,
        skipToken: "example",
        sortKey: "example",
        sortValue: "example",
        top: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkVerifierWorkspace, VirtualnetworkVerifierWorkspaceConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_verifier_workspace")

        ubx.data(VirtualnetworkVerifierWorkspace, "example", VirtualnetworkVerifierWorkspaceConfig(
            network_manager_name="example-network-manager-name",
            skip=1,
            skip_token="example",
            sort_key="example",
            sort_value="example",
            top=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_manager_name" type="string" required>
  The real name of the Azure Virtual Network Manager this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="skip" type="number">
  The number of matching results to skip before returning the rest, for pagination. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="skip_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_key" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_value" type="string">
  The real value results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="top" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VerifierWorkspace items on this page Returned by the lookup.
</ResponseField>
