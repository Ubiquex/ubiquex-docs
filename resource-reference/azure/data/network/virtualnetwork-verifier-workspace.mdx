---
title: "azure_network_virtualnetwork_verifier_workspace"
description: "`azure_network_virtualnetwork_verifier_workspace` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_virtualnetwork_verifier_workspace` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_verifier_workspace"})

    		ubx.Data(network.VirtualnetworkVerifierWorkspace, "example", network.VirtualnetworkVerifierWorkspaceConfig{
    			NetworkManagerName: "example-network-manager-name",
    			Skip:               1,
    			SkipToken:          "example",
    			SortKey:            "example",
    			SortValue:          "example",
    			Top:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkVerifierWorkspace } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_verifier_workspace";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_verifier_workspace",
      });

      data(VirtualnetworkVerifierWorkspace, "example", {
        networkManagerName: "example-network-manager-name",
        skip: 1,
        skipToken: "example",
        sortKey: "example",
        sortValue: "example",
        top: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkVerifierWorkspace, VirtualnetworkVerifierWorkspaceConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_verifier_workspace")

        ubx.data(VirtualnetworkVerifierWorkspace, "example", VirtualnetworkVerifierWorkspaceConfig(
            network_manager_name="example-network-manager-name",
            skip=1,
            skip_token="example",
            sort_key="example",
            sort_value="example",
            top=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_manager_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="skip" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="skip_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_key" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_value" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="top" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VerifierWorkspace items on this page Returned by the lookup.
</ResponseField>
