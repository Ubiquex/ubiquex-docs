---
title: "azure_network_virtualnetwork_ddos_custom_policy"
description: "`data_azure_network_virtualnetwork_ddos_custom_policy` looks up a real, already-existing `azure_network_virtualnetwork_ddos_custom_policy`."
---

It overrides Azure DDoS Protection's own default traffic thresholds for a specific public IP address, tuning detection sensitivity for that resource's own traffic pattern.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_ddos_custom_policy"})

    		ubx.Data(network.VirtualnetworkDdosCustomPolicy, "example", network.VirtualnetworkDdosCustomPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkDdosCustomPolicy } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_ddos_custom_policy";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_ddos_custom_policy",
      });

      data(VirtualnetworkDdosCustomPolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkDdosCustomPolicy, VirtualnetworkDdosCustomPolicyConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_ddos_custom_policy")

        ubx.data(VirtualnetworkDdosCustomPolicy, "example", VirtualnetworkDdosCustomPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DdosCustomPolicy items on this page Returned by the lookup.
</ResponseField>
