---
title: "azure_network_virtualnetwork_virtual_network_peering"
description: "`azure_network_virtualnetwork_virtual_network_peering` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_virtualnetwork_virtual_network_peering` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_virtual_network_peering"})

    		ubx.Data(network.VirtualnetworkVirtualNetworkPeering, "example", network.VirtualnetworkVirtualNetworkPeeringConfig{
    			VirtualNetworkName: "example-virtual-network-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkVirtualNetworkPeering } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_virtual_network_peering";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_virtual_network_peering",
      });

      data(VirtualnetworkVirtualNetworkPeering, "example", {
        virtualNetworkName: "example-virtual-network-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkVirtualNetworkPeering, VirtualnetworkVirtualNetworkPeeringConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_virtual_network_peering")

        ubx.data(VirtualnetworkVirtualNetworkPeering, "example", VirtualnetworkVirtualNetworkPeeringConfig(
            virtual_network_name="example-virtual-network-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="virtual_network_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualNetworkPeering items on this page Returned by the lookup.
</ResponseField>
