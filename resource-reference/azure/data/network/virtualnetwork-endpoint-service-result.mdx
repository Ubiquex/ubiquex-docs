---
title: "azure_network_virtualnetwork_endpoint_service_result"
description: "`azure_network_virtualnetwork_endpoint_service_result` reads a real, available service endpoint (such as Microsoft.Storage) that a subnet can be..."
---

`azure_network_virtualnetwork_endpoint_service_result` reads a real, available service endpoint (such as Microsoft.Storage) that a subnet can be configured with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_endpoint_service_result"})

    		ubx.Data(network.VirtualnetworkEndpointServiceResult, "example", network.VirtualnetworkEndpointServiceResultConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualnetworkEndpointServiceResult } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualnetwork_endpoint_service_result.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_endpoint_service_result",
      });

      data(VirtualnetworkEndpointServiceResult, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkEndpointServiceResult, VirtualnetworkEndpointServiceResultConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_endpoint_service_result")

        ubx.data(VirtualnetworkEndpointServiceResult, "example", VirtualnetworkEndpointServiceResultConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The EndpointServiceResult items on this page Returned by the lookup.
</ResponseField>
