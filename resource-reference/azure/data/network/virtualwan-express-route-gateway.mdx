---
title: "azure_network_virtualwan_express_route_gateway"
description: "`data_azure_network_virtualwan_express_route_gateway` looks up a real, already-existing `azure_network_virtualwan_express_route_gateway`."
---

It provisions the ExpressRoute gateway within a Virtual WAN hub, that ExpressRoute circuit connection(s) terminate on.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_express_route_gateway"})

    		ubx.Data(network.VirtualwanExpressRouteGateway, "example", network.VirtualwanExpressRouteGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualwanExpressRouteGateway } from "@ubx/sdk-azure/azure/data/network/virtualwan_express_route_gateway";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualwan_express_route_gateway" });

      data(VirtualwanExpressRouteGateway, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanExpressRouteGateway, VirtualwanExpressRouteGatewayConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_express_route_gateway")

        ubx.data(VirtualwanExpressRouteGateway, "example", VirtualwanExpressRouteGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  List of ExpressRoute gateways. Returned by the lookup.
</ResponseField>
