---
title: "azure_network_networkmanager_network_manager"
description: "`data_azure_network_networkmanager_network_manager` looks up a real, already-existing `azure_network_networkmanager_network_manager`."
---

It is the top-level Azure Virtual Network Manager resource, centrally managing connectivity and security configuration across virtual network(s) within its own scope.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networkmanager_network_manager"})

    		ubx.Data(network.NetworkmanagerNetworkManager, "example", network.NetworkmanagerNetworkManagerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkmanagerNetworkManager } from "@ubx/sdk-azure/azure/data/network/networkmanager_network_manager";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_networkmanager_network_manager" });

      data(NetworkmanagerNetworkManager, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworkmanagerNetworkManager, NetworkmanagerNetworkManagerConfig

    def describe():
        ubx.intent("look up azure_network_networkmanager_network_manager")

        ubx.data(NetworkmanagerNetworkManager, "example", NetworkmanagerNetworkManagerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NetworkManager items on this page Returned by the lookup.
</ResponseField>
