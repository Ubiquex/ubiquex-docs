---
title: "azure_network_expressroute_peer_express_route_circuit_connection"
description: "`azure_network_expressroute_peer_express_route_circuit_connection` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_expressroute_peer_express_route_circuit_connection` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_expressroute_peer_express_route_circuit_connection"})

    		ubx.Data(network.ExpressroutePeerExpressRouteCircuitConnection, "example", network.ExpressroutePeerExpressRouteCircuitConnectionConfig{
    			CircuitName: "example-circuit-name",
    			PeeringName: "example-peering-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExpressroutePeerExpressRouteCircuitConnection } from "@ubx/sdk-azure/azure/data/network/expressroute_peer_express_route_circuit_connection";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_network_expressroute_peer_express_route_circuit_connection",
      });

      data(ExpressroutePeerExpressRouteCircuitConnection, "example", {
        circuitName: "example-circuit-name",
        peeringName: "example-peering-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import ExpressroutePeerExpressRouteCircuitConnection, ExpressroutePeerExpressRouteCircuitConnectionConfig

    def describe():
        ubx.intent("look up azure_network_expressroute_peer_express_route_circuit_connection")

        ubx.data(ExpressroutePeerExpressRouteCircuitConnection, "example", ExpressroutePeerExpressRouteCircuitConnectionConfig(
            circuit_name="example-circuit-name",
            peering_name="example-peering-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="circuit_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="peering_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PeerExpressRouteCircuitConnection items on this page Returned by the lookup.
</ResponseField>
