---
title: "azure_network_servicegateway_service_gateway_service"
description: "`data_azure_network_servicegateway_service_gateway_service` is a real, live lookup against Microsoft Azure..."
---

`data_azure_network_servicegateway_service_gateway_service` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_servicegateway_service_gateway_service"})

    		ubx.Data(network.ServicegatewayServiceGatewayService, "example", network.ServicegatewayServiceGatewayServiceConfig{
    			ServiceGatewayName: "example-service-gateway-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServicegatewayServiceGatewayService } from "@ubx/sdk-azure/azure/data/network/servicegateway_service_gateway_service";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_servicegateway_service_gateway_service",
      });

      data(ServicegatewayServiceGatewayService, "example", {
        serviceGatewayName: "example-service-gateway-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import ServicegatewayServiceGatewayService, ServicegatewayServiceGatewayServiceConfig

    def describe():
        ubx.intent("look up azure_network_servicegateway_service_gateway_service")

        ubx.data(ServicegatewayServiceGatewayService, "example", ServicegatewayServiceGatewayServiceConfig(
            service_gateway_name="example-service-gateway-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="service_gateway_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ServiceGatewayService items on this page Returned by the lookup.
</ResponseField>
