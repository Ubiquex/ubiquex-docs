---
title: "azure_network_firewallpolicy_signatures_overrides"
description: "`azure_network_firewallpolicy_signatures_overrides` reads the real, manually overridden IDPS signature actions configured on an Azure Firewall Policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_firewallpolicy_signatures_overrides"})

    		ubx.Data(network.FirewallpolicySignaturesOverrides, "example", network.FirewallpolicySignaturesOverridesConfig{
    			FirewallPolicyName: "example-firewall-policy-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallpolicySignaturesOverrides } from "@ubx/sdk-azure/azure/data/network/firewallpolicy_signatures_overrides";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_firewallpolicy_signatures_overrides",
      });

      data(FirewallpolicySignaturesOverrides, "example", {
        firewallPolicyName: "example-firewall-policy-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import FirewallpolicySignaturesOverrides, FirewallpolicySignaturesOverridesConfig

    def describe():
        ubx.intent("look up azure_network_firewallpolicy_signatures_overrides")

        ubx.data(FirewallpolicySignaturesOverrides, "example", FirewallpolicySignaturesOverridesConfig(
            firewall_policy_name="example-firewall-policy-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="firewall_policy_name" type="string" required>
  The real name of the Azure Firewall Policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  Describes a list consisting exactly one item describing the policy's signature override status Returned by the lookup.
</ResponseField>
