---
title: "azure_network_virtualnetwork_network_virtual_appliance"
description: "`data_azure_network_virtualnetwork_network_virtual_appliance` looks up a real, already-existing `azure_network_virtualnetwork_network_virtual_appliance`."
---

It deploys a third-party network virtual appliance (firewall, router) from the Azure Marketplace into a virtual hub, for traffic inspection and routing.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_network_virtual_appliance"})

    		ubx.Data(network.VirtualnetworkNetworkVirtualAppliance, "example", network.VirtualnetworkNetworkVirtualApplianceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkNetworkVirtualAppliance } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_network_virtual_appliance";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_network_virtual_appliance",
      });

      data(VirtualnetworkNetworkVirtualAppliance, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkNetworkVirtualAppliance, VirtualnetworkNetworkVirtualApplianceConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_network_virtual_appliance")

        ubx.data(VirtualnetworkNetworkVirtualAppliance, "example", VirtualnetworkNetworkVirtualApplianceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NetworkVirtualAppliance items on this page Returned by the lookup.
</ResponseField>
