---
title: "azure_network_networkmanager_security_user_rule"
description: "`data_azure_network_networkmanager_security_user_rule` looks up a real, already-existing `azure_network_networkmanager_security_user_rule`."
---

It is a single centrally managed security rule within a Virtual Network Manager security user configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networkmanager_security_user_rule"})

    		ubx.Data(network.NetworkmanagerSecurityUserRule, "example", network.NetworkmanagerSecurityUserRuleConfig{
    			ConfigurationName:  "example-configuration-name",
    			NetworkManagerName: "example-network-manager-name",
    			RuleCollectionName: "example-rule-collection-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkmanagerSecurityUserRule } from "@ubx/sdk-azure/azure/data/network/networkmanager_security_user_rule";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_networkmanager_security_user_rule",
      });

      data(NetworkmanagerSecurityUserRule, "example", {
        configurationName: "example-configuration-name",
        networkManagerName: "example-network-manager-name",
        ruleCollectionName: "example-rule-collection-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworkmanagerSecurityUserRule, NetworkmanagerSecurityUserRuleConfig

    def describe():
        ubx.intent("look up azure_network_networkmanager_security_user_rule")

        ubx.data(NetworkmanagerSecurityUserRule, "example", NetworkmanagerSecurityUserRuleConfig(
            configuration_name="example-configuration-name",
            network_manager_name="example-network-manager-name",
            rule_collection_name="example-rule-collection-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_name" type="string" required>
  The real name of the configuration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="network_manager_name" type="string" required>
  The real name of the Azure Virtual Network Manager this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="rule_collection_name" type="string" required>
  The real name of the rule collection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SecurityUserRule items on this page Returned by the lookup.
</ResponseField>
