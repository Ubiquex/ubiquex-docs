---
title: "azure_network_expressroute_express_route_cross_connection_peering"
description: "`data_azure_network_expressroute_express_route_cross_connection_peering` looks up a real, already-existing..."
---

`data_azure_network_expressroute_express_route_cross_connection_peering` looks up a real, already-existing `azure_network_expressroute_express_route_cross_connection_peering`. It configures a peering on an ExpressRoute cross connection, the provider-side counterpart to a circuit peering.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_expressroute_express_route_cross_connection_peering"})

    		ubx.Data(network.ExpressrouteExpressRouteCrossConnectionPeering, "example", network.ExpressrouteExpressRouteCrossConnectionPeeringConfig{
    			CrossConnectionName: "example-cross-connection-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ExpressrouteExpressRouteCrossConnectionPeering } from "./local-sdk/azure/sdk/typescript/azure/data/network/expressroute_express_route_cross_connection_peering.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_network_expressroute_express_route_cross_connection_peering",
      });

      data(ExpressrouteExpressRouteCrossConnectionPeering, "example", {
        crossConnectionName: "example-cross-connection-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import ExpressrouteExpressRouteCrossConnectionPeering, ExpressrouteExpressRouteCrossConnectionPeeringConfig

    def describe():
        ubx.intent("look up azure_network_expressroute_express_route_cross_connection_peering")

        ubx.data(ExpressrouteExpressRouteCrossConnectionPeering, "example", ExpressrouteExpressRouteCrossConnectionPeeringConfig(
            cross_connection_name="example-cross-connection-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cross_connection_name" type="string" required>
  The real name of the ExpressRoute cross-connection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ExpressRouteCrossConnectionPeering items on this page Returned by the lookup.
</ResponseField>
