---
title: "azure_network_networkwatcher_packet_capture_result"
description: "`azure_network_networkwatcher_packet_capture_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_networkwatcher_packet_capture_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networkwatcher_packet_capture_result"})

    		ubx.Data(network.NetworkwatcherPacketCaptureResult, "example", network.NetworkwatcherPacketCaptureResultConfig{
    			NetworkWatcherName: "example-network-watcher-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkwatcherPacketCaptureResult } from "@ubx/sdk-azure/azure/data/network/networkwatcher_packet_capture_result";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_networkwatcher_packet_capture_result",
      });

      data(NetworkwatcherPacketCaptureResult, "example", {
        networkWatcherName: "example-network-watcher-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworkwatcherPacketCaptureResult, NetworkwatcherPacketCaptureResultConfig

    def describe():
        ubx.intent("look up azure_network_networkwatcher_packet_capture_result")

        ubx.data(NetworkwatcherPacketCaptureResult, "example", NetworkwatcherPacketCaptureResultConfig(
            network_watcher_name="example-network-watcher-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_watcher_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Information about packet capture sessions. Returned by the lookup.
</ResponseField>
