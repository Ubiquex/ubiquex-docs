---
title: "azure_network_networksecurityperimeter_nsp_access_rule"
description: "`azure_network_networksecurityperimeter_nsp_access_rule` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_networksecurityperimeter_nsp_access_rule` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networksecurityperimeter_nsp_access_rule"})

    		ubx.Data(network.NetworksecurityperimeterNspAccessRule, "example", network.NetworksecurityperimeterNspAccessRuleConfig{
    			NetworkSecurityPerimeterName: "example-network-security-perimeter-name",
    			ProfileName:                  "example-profile-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworksecurityperimeterNspAccessRule } from "@ubx/sdk-azure/azure/data/network/networksecurityperimeter_nsp_access_rule";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_networksecurityperimeter_nsp_access_rule",
      });

      data(NetworksecurityperimeterNspAccessRule, "example", {
        networkSecurityPerimeterName: "example-network-security-perimeter-name",
        profileName: "example-profile-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworksecurityperimeterNspAccessRule, NetworksecurityperimeterNspAccessRuleConfig

    def describe():
        ubx.intent("look up azure_network_networksecurityperimeter_nsp_access_rule")

        ubx.data(NetworksecurityperimeterNspAccessRule, "example", NetworksecurityperimeterNspAccessRuleConfig(
            network_security_perimeter_name="example-network-security-perimeter-name",
            profile_name="example-profile-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_security_perimeter_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NspAccessRule items on this page Returned by the lookup.
</ResponseField>
