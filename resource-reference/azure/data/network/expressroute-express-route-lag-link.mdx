---
title: "azure_network_expressroute_express_route_lag_link"
description: "`azure_network_expressroute_express_route_lag_link` looks up one real physical link within an ExpressRoute Direct link aggregation group (LAG)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_expressroute_express_route_lag_link"})

    		ubx.Data(network.ExpressrouteExpressRouteLagLink, "example", network.ExpressrouteExpressRouteLagLinkConfig{
    			ExpressRouteLagName: "example-express-route-lag-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExpressrouteExpressRouteLagLink } from "@ubx/sdk-azure/azure/data/network/expressroute_express_route_lag_link";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_expressroute_express_route_lag_link",
      });

      data(ExpressrouteExpressRouteLagLink, "example", {
        expressRouteLagName: "example-express-route-lag-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import ExpressrouteExpressRouteLagLink, ExpressrouteExpressRouteLagLinkConfig

    def describe():
        ubx.intent("look up azure_network_expressroute_express_route_lag_link")

        ubx.data(ExpressrouteExpressRouteLagLink, "example", ExpressrouteExpressRouteLagLinkConfig(
            express_route_lag_name="example-express-route-lag-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="express_route_lag_name" type="string" required>
  The real name of the ExpressRoute link aggregation group (LAG) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ExpressRouteLagLink items on this page Returned by the lookup.
</ResponseField>
