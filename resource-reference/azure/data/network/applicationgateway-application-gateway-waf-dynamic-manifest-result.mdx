---
title: "azure_network_applicationgateway_application_gateway_waf_dynamic_manifest_result"
description: "`azure_network_applicationgateway_application_gateway_waf_dynamic_manifest_result` reads the real, current manifest of WAF rule sets and rule groups..."
---

`azure_network_applicationgateway_application_gateway_waf_dynamic_manifest_result` reads the real, current manifest of WAF rule sets and rule groups available to an Application Gateway.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_applicationgateway_application_gateway_waf_dynamic_manifest_result"})

    		ubx.Data(network.ApplicationgatewayApplicationGatewayWafDynamicManifestResult, "example", network.ApplicationgatewayApplicationGatewayWafDynamicManifestResultConfig{
    			Location: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ApplicationgatewayApplicationGatewayWafDynamicManifestResult } from "./local-sdk/azure/sdk/typescript/azure/data/network/applicationgateway_application_gateway_waf_dynamic_manifest_result.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_network_applicationgateway_application_gateway_waf_dynamic_manifest_result",
      });

      data(
        ApplicationgatewayApplicationGatewayWafDynamicManifestResult,
        "example",
        {
          location: "example",
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import ApplicationgatewayApplicationGatewayWafDynamicManifestResult, ApplicationgatewayApplicationGatewayWafDynamicManifestResultConfig

    def describe():
        ubx.intent("look up azure_network_applicationgateway_application_gateway_waf_dynamic_manifest_result")

        ubx.data(ApplicationgatewayApplicationGatewayWafDynamicManifestResult, "example", ApplicationgatewayApplicationGatewayWafDynamicManifestResultConfig(
            location="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="location" type="string" required>
  The real Azure region this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ApplicationGatewayWafDynamicManifestResult items on this page Returned by the lookup.
</ResponseField>
