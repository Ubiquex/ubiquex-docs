---
title: "azure_network_firewall_azure_firewall"
description: "`data_azure_network_firewall_azure_firewall` looks up a real, already-existing `azure_network_firewall_azure_firewall`."
---

It is an Azure Firewall instance, a managed, cloud-native network firewall providing stateful traffic filtering, threat intelligence, and TLS inspection for a virtual network or hub.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_firewall_azure_firewall"})

    		ubx.Data(network.FirewallAzureFirewall, "example", network.FirewallAzureFirewallConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallAzureFirewall } from "@ubx/sdk-azure/azure/data/network/firewall_azure_firewall";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_firewall_azure_firewall" });

      data(FirewallAzureFirewall, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import FirewallAzureFirewall, FirewallAzureFirewallConfig

    def describe():
        ubx.intent("look up azure_network_firewall_azure_firewall")

        ubx.data(FirewallAzureFirewall, "example", FirewallAzureFirewallConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AzureFirewall items on this page Returned by the lookup.
</ResponseField>
