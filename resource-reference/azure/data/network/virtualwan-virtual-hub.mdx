---
title: "azure_network_virtualwan_virtual_hub"
description: "`data_azure_network_virtualwan_virtual_hub` looks up a real, already-existing `azure_network_virtualwan_virtual_hub`."
---

It is the regional routing hub within a Virtual WAN, that spoke virtual network(s), VPN/ExpressRoute gateway(s), and security appliance(s) connect to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_virtual_hub"})

    		ubx.Data(network.VirtualwanVirtualHub, "example", network.VirtualwanVirtualHubConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualwanVirtualHub } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualwan_virtual_hub.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualwan_virtual_hub" });

      data(VirtualwanVirtualHub, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanVirtualHub, VirtualwanVirtualHubConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_virtual_hub")

        ubx.data(VirtualwanVirtualHub, "example", VirtualwanVirtualHubConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualHub items on this page Returned by the lookup.
</ResponseField>
