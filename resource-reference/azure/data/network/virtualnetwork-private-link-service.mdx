---
title: "azure_network_virtualnetwork_private_link_service"
description: "`azure_network_virtualnetwork_private_link_service` looks up a real Private Link service -- a caller-published service that other virtual networks can..."
---

`azure_network_virtualnetwork_private_link_service` looks up a real Private Link service -- a caller-published service that other virtual networks can privately connect to via a Private Endpoint.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_private_link_service"})

    		ubx.Data(network.VirtualnetworkPrivateLinkService, "example", network.VirtualnetworkPrivateLinkServiceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkPrivateLinkService } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_private_link_service";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_private_link_service",
      });

      data(VirtualnetworkPrivateLinkService, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkPrivateLinkService, VirtualnetworkPrivateLinkServiceConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_private_link_service")

        ubx.data(VirtualnetworkPrivateLinkService, "example", VirtualnetworkPrivateLinkServiceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrivateLinkService items on this page Returned by the lookup.
</ResponseField>
