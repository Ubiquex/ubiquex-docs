---
title: "azure_network_virtualwan_express_route_connection"
description: "`data_azure_network_virtualwan_express_route_connection` looks up a real, already-existing `azure_network_virtualwan_express_route_connection`."
---

It connects an ExpressRoute circuit to a Virtual WAN hub's own ExpressRoute gateway.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_express_route_connection"})

    		ubx.Data(network.VirtualwanExpressRouteConnection, "example", network.VirtualwanExpressRouteConnectionConfig{
    			ExpressRouteGatewayName: "example-express-route-gateway-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualwanExpressRouteConnection } from "@ubx/sdk-azure/azure/data/network/virtualwan_express_route_connection";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualwan_express_route_connection",
      });

      data(VirtualwanExpressRouteConnection, "example", {
        expressRouteGatewayName: "example-express-route-gateway-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanExpressRouteConnection, VirtualwanExpressRouteConnectionConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_express_route_connection")

        ubx.data(VirtualwanExpressRouteConnection, "example", VirtualwanExpressRouteConnectionConfig(
            express_route_gateway_name="example-express-route-gateway-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="express_route_gateway_name" type="string" required>
  The real name of the ExpressRoute gateway this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The list of ExpressRoute connections. Returned by the lookup.
</ResponseField>
