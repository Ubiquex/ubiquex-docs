---
title: "azure_network_networkmanager_network_manager_connection"
description: "`data_azure_network_networkmanager_network_manager_connection` looks up a real, already-existing `azure_network_networkmanager_network_manager_connection`."
---

It delegates management of a subscription or management group's own virtual networks to a Virtual Network Manager instance in a different scope.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networkmanager_network_manager_connection"})

    		ubx.Data(network.NetworkmanagerNetworkManagerConnection, "example", network.NetworkmanagerNetworkManagerConnectionConfig{
    			ManagementGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkmanagerNetworkManagerConnection } from "@ubx/sdk-azure/azure/data/network/networkmanager_network_manager_connection";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_networkmanager_network_manager_connection",
      });

      data(NetworkmanagerNetworkManagerConnection, "example", {
        managementGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworkmanagerNetworkManagerConnection, NetworkmanagerNetworkManagerConnectionConfig

    def describe():
        ubx.intent("look up azure_network_networkmanager_network_manager_connection")

        ubx.data(NetworkmanagerNetworkManagerConnection, "example", NetworkmanagerNetworkManagerConnectionConfig(
            management_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="management_group_id" type="string" required>
  The real, unique identifier of the management group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NetworkManagerConnection items on this page Returned by the lookup.
</ResponseField>
