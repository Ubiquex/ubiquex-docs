---
title: "azure_network_networkgateway_local_network_gateway"
description: "`data_azure_network_networkgateway_local_network_gateway` looks up a real, already-existing `azure_network_networkgateway_local_network_gateway`."
---

It represents an on-premises VPN endpoint's own public IP address and address space, referenced by a site-to-site VPN connection.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_networkgateway_local_network_gateway"})

    		ubx.Data(network.NetworkgatewayLocalNetworkGateway, "example", network.NetworkgatewayLocalNetworkGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { NetworkgatewayLocalNetworkGateway } from "./local-sdk/azure/sdk/typescript/azure/data/network/networkgateway_local_network_gateway.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_networkgateway_local_network_gateway",
      });

      data(NetworkgatewayLocalNetworkGateway, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import NetworkgatewayLocalNetworkGateway, NetworkgatewayLocalNetworkGatewayConfig

    def describe():
        ubx.intent("look up azure_network_networkgateway_local_network_gateway")

        ubx.data(NetworkgatewayLocalNetworkGateway, "example", NetworkgatewayLocalNetworkGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The LocalNetworkGateway items on this page Returned by the lookup.
</ResponseField>
