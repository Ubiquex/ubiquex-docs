---
title: "azure_network_virtualnetwork_virtual_appliance_site"
description: "`data_azure_network_virtualnetwork_virtual_appliance_site` looks up a real, already-existing `azure_network_virtualnetwork_virtual_appliance_site`."
---

It defines a branch site whose own address space is advertised to and routed through a network virtual appliance deployed in a virtual hub.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_virtual_appliance_site"})

    		ubx.Data(network.VirtualnetworkVirtualApplianceSite, "example", network.VirtualnetworkVirtualApplianceSiteConfig{
    			NetworkVirtualApplianceName: "example-network-virtual-appliance-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkVirtualApplianceSite } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_virtual_appliance_site";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_virtual_appliance_site",
      });

      data(VirtualnetworkVirtualApplianceSite, "example", {
        networkVirtualApplianceName: "example-network-virtual-appliance-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkVirtualApplianceSite, VirtualnetworkVirtualApplianceSiteConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_virtual_appliance_site")

        ubx.data(VirtualnetworkVirtualApplianceSite, "example", VirtualnetworkVirtualApplianceSiteConfig(
            network_virtual_appliance_name="example-network-virtual-appliance-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_virtual_appliance_name" type="string" required>
  The real name of the network virtual appliance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualApplianceSite items on this page Returned by the lookup.
</ResponseField>
