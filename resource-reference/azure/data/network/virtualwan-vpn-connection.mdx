---
title: "azure_network_virtualwan_vpn_connection"
description: "`data_azure_network_virtualwan_vpn_connection` looks up a real, already-existing `azure_network_virtualwan_vpn_connection`."
---

It connects a remote VPN site to a Virtual WAN hub's own site-to-site VPN gateway.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_vpn_connection"})

    		ubx.Data(network.VirtualwanVpnConnection, "example", network.VirtualwanVpnConnectionConfig{
    			GatewayName: "example-gateway-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualwanVpnConnection } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualwan_vpn_connection.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualwan_vpn_connection" });

      data(VirtualwanVpnConnection, "example", {
        gatewayName: "example-gateway-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanVpnConnection, VirtualwanVpnConnectionConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_vpn_connection")

        ubx.data(VirtualwanVpnConnection, "example", VirtualwanVpnConnectionConfig(
            gateway_name="example-gateway-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gateway_name" type="string" required>
  The real name of the gateway this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VpnConnection items on this page Returned by the lookup.
</ResponseField>
