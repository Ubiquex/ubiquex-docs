---
title: "azure_network_virtualnetwork_swap_resource"
description: "`azure_network_virtualnetwork_swap_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_virtualnetwork_swap_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_swap_resource"})

    		ubx.Data(network.VirtualnetworkSwapResource, "example", network.VirtualnetworkSwapResourceConfig{
    			GroupName:    "example-group-name",
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkSwapResource } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_swap_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualnetwork_swap_resource" });

      data(VirtualnetworkSwapResource, "example", {
        groupName: "example-group-name",
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkSwapResource, VirtualnetworkSwapResourceConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_swap_resource")

        ubx.data(VirtualnetworkSwapResource, "example", VirtualnetworkSwapResourceConfig(
            group_name="example-group-name",
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  Returned by the lookup.
</ResponseField>
