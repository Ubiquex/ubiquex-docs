---
title: "azure_network_virtualwan_p2_svpn_gateway"
description: "`data_azure_network_virtualwan_p2_svpn_gateway` looks up a real, already-existing `azure_network_virtualwan_p2_svpn_gateway`."
---

It provisions the point-to-site VPN gateway within a Virtual WAN hub, that individual remote client(s) connect to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_p2_svpn_gateway"})

    		ubx.Data(network.VirtualwanP2SvpnGateway, "example", network.VirtualwanP2SvpnGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualwanP2SvpnGateway } from "@ubx/sdk-azure/azure/data/network/virtualwan_p2_svpn_gateway";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualwan_p2_svpn_gateway" });

      data(VirtualwanP2SvpnGateway, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanP2SvpnGateway, VirtualwanP2SvpnGatewayConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_p2_svpn_gateway")

        ubx.data(VirtualwanP2SvpnGateway, "example", VirtualwanP2SvpnGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The P2SVpnGateway items on this page Returned by the lookup.
</ResponseField>
