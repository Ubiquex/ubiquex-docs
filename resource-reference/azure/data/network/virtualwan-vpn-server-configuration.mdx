---
title: "azure_network_virtualwan_vpn_server_configuration"
description: "`data_azure_network_virtualwan_vpn_server_configuration` looks up a real, already-existing `azure_network_virtualwan_vpn_server_configuration`."
---

It defines the authentication method and protocol(s) a Virtual WAN point-to-site VPN gateway accepts client connections with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_vpn_server_configuration"})

    		ubx.Data(network.VirtualwanVpnServerConfiguration, "example", network.VirtualwanVpnServerConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualwanVpnServerConfiguration } from "@ubx/sdk-azure/azure/data/network/virtualwan_vpn_server_configuration";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualwan_vpn_server_configuration",
      });

      data(VirtualwanVpnServerConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanVpnServerConfiguration, VirtualwanVpnServerConfigurationConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_vpn_server_configuration")

        ubx.data(VirtualwanVpnServerConfiguration, "example", VirtualwanVpnServerConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VpnServerConfiguration items on this page Returned by the lookup.
</ResponseField>
