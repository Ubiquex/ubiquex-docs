---
title: "azure_network_servicegateway_service_gateway"
description: "`data_azure_network_servicegateway_service_gateway` looks up a real, already-existing `azure_network_servicegateway_service_gateway`."
---

It is a service gateway resource extending on-premises Active Directory-style connectivity or other managed gateway functionality into a virtual network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_servicegateway_service_gateway"})

    		ubx.Data(network.ServicegatewayServiceGateway, "example", network.ServicegatewayServiceGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServicegatewayServiceGateway } from "@ubx/sdk-azure/azure/data/network/servicegateway_service_gateway";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_servicegateway_service_gateway" });

      data(ServicegatewayServiceGateway, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import ServicegatewayServiceGateway, ServicegatewayServiceGatewayConfig

    def describe():
        ubx.intent("look up azure_network_servicegateway_service_gateway")

        ubx.data(ServicegatewayServiceGateway, "example", ServicegatewayServiceGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ServiceGateway items on this page Returned by the lookup.
</ResponseField>
