---
title: "azure_network_virtualnetwork_available_service_alias"
description: "`azure_network_virtualnetwork_available_service_alias` reads a real, available service alias usable in a subnet's own service endpoint policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_available_service_alias"})

    		ubx.Data(network.VirtualnetworkAvailableServiceAlias, "example", network.VirtualnetworkAvailableServiceAliasConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkAvailableServiceAlias } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_available_service_alias";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_available_service_alias",
      });

      data(VirtualnetworkAvailableServiceAlias, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkAvailableServiceAlias, VirtualnetworkAvailableServiceAliasConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_available_service_alias")

        ubx.data(VirtualnetworkAvailableServiceAlias, "example", VirtualnetworkAvailableServiceAliasConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AvailableServiceAlias items on this page Returned by the lookup.
</ResponseField>
