---
title: "azure_network_expressroute_express_route_service_provider"
description: "`azure_network_expressroute_express_route_service_provider` reads a real, available ExpressRoute connectivity provider and the regions it offers..."
---

`azure_network_expressroute_express_route_service_provider` reads a real, available ExpressRoute connectivity provider and the regions it offers peering in.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_expressroute_express_route_service_provider"})

    		ubx.Data(network.ExpressrouteExpressRouteServiceProvider, "example", network.ExpressrouteExpressRouteServiceProviderConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ExpressrouteExpressRouteServiceProvider } from "./local-sdk/azure/sdk/typescript/azure/data/network/expressroute_express_route_service_provider.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_network_expressroute_express_route_service_provider",
      });

      data(ExpressrouteExpressRouteServiceProvider, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import ExpressrouteExpressRouteServiceProvider, ExpressrouteExpressRouteServiceProviderConfig

    def describe():
        ubx.intent("look up azure_network_expressroute_express_route_service_provider")

        ubx.data(ExpressrouteExpressRouteServiceProvider, "example", ExpressrouteExpressRouteServiceProviderConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ExpressRouteServiceProvider items on this page Returned by the lookup.
</ResponseField>
