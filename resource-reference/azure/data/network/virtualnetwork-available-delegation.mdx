---
title: "azure_network_virtualnetwork_available_delegation"
description: "`azure_network_virtualnetwork_available_delegation` reads a real, available subnet delegation (a service that can take ownership of a subnet..."
---

`azure_network_virtualnetwork_available_delegation` reads a real, available subnet delegation (a service that can take ownership of a subnet, such as Azure Container Instances).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_available_delegation"})

    		ubx.Data(network.VirtualnetworkAvailableDelegation, "example", network.VirtualnetworkAvailableDelegationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkAvailableDelegation } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_available_delegation";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_available_delegation",
      });

      data(VirtualnetworkAvailableDelegation, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkAvailableDelegation, VirtualnetworkAvailableDelegationConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_available_delegation")

        ubx.data(VirtualnetworkAvailableDelegation, "example", VirtualnetworkAvailableDelegationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AvailableDelegation items on this page Returned by the lookup.
</ResponseField>
