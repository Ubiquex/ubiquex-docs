---
title: "azure_network_virtualwan_vpn_site_link_connection"
description: "`azure_network_virtualwan_vpn_site_link_connection` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_virtualwan_vpn_site_link_connection` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_vpn_site_link_connection"})

    		ubx.Data(network.VirtualwanVpnSiteLinkConnection, "example", network.VirtualwanVpnSiteLinkConnectionConfig{
    			ConnectionName: "example-connection-name",
    			GatewayName:    "example-gateway-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualwanVpnSiteLinkConnection } from "@ubx/sdk-azure/azure/data/network/virtualwan_vpn_site_link_connection";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualwan_vpn_site_link_connection",
      });

      data(VirtualwanVpnSiteLinkConnection, "example", {
        connectionName: "example-connection-name",
        gatewayName: "example-gateway-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanVpnSiteLinkConnection, VirtualwanVpnSiteLinkConnectionConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_vpn_site_link_connection")

        ubx.data(VirtualwanVpnSiteLinkConnection, "example", VirtualwanVpnSiteLinkConnectionConfig(
            connection_name="example-connection-name",
            gateway_name="example-gateway-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connection_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="gateway_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VpnSiteLinkConnection items on this page Returned by the lookup.
</ResponseField>
