---
title: "azure_network_virtualwan_hub_route_table"
description: "`data_azure_network_virtualwan_hub_route_table` looks up a real, already-existing `azure_network_virtualwan_hub_route_table`."
---

It is a route table within a Virtual WAN hub controlling which routes are propagated to and associated with specific connection(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_hub_route_table"})

    		ubx.Data(network.VirtualwanHubRouteTable, "example", network.VirtualwanHubRouteTableConfig{
    			VirtualHubName: "example-virtual-hub-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VirtualwanHubRouteTable } from "./local-sdk/azure/sdk/typescript/azure/data/network/virtualwan_hub_route_table.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualwan_hub_route_table" });

      data(VirtualwanHubRouteTable, "example", {
        virtualHubName: "example-virtual-hub-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanHubRouteTable, VirtualwanHubRouteTableConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_hub_route_table")

        ubx.data(VirtualwanHubRouteTable, "example", VirtualwanHubRouteTableConfig(
            virtual_hub_name="example-virtual-hub-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="virtual_hub_name" type="string" required>
  The real name of the Virtual WAN hub this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The HubRouteTable items on this page Returned by the lookup.
</ResponseField>
