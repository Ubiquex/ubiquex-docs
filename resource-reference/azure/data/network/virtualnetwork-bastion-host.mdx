---
title: "azure_network_virtualnetwork_bastion_host"
description: "`azure_network_virtualnetwork_bastion_host` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_virtualnetwork_bastion_host` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_bastion_host"})

    		ubx.Data(network.VirtualnetworkBastionHost, "example", network.VirtualnetworkBastionHostConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkBastionHost } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_bastion_host";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualnetwork_bastion_host" });

      data(VirtualnetworkBastionHost, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkBastionHost, VirtualnetworkBastionHostConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_bastion_host")

        ubx.data(VirtualnetworkBastionHost, "example", VirtualnetworkBastionHostConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The BastionHost items on this page Returned by the lookup.
</ResponseField>
