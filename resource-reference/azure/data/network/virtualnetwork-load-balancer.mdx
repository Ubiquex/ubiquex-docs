---
title: "azure_network_virtualnetwork_load_balancer"
description: "`data_azure_network_virtualnetwork_load_balancer` lists the real Load Balancers a network interface is a backend member of."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_load_balancer"})

    		ubx.Data(network.VirtualnetworkLoadBalancer, "example", network.VirtualnetworkLoadBalancerConfig{
    			NetworkInterfaceName: "example-network-interface-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkLoadBalancer } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_load_balancer";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualnetwork_load_balancer" });

      data(VirtualnetworkLoadBalancer, "example", {
        networkInterfaceName: "example-network-interface-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkLoadBalancer, VirtualnetworkLoadBalancerConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_load_balancer")

        ubx.data(VirtualnetworkLoadBalancer, "example", VirtualnetworkLoadBalancerConfig(
            network_interface_name="example-network-interface-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_interface_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The LoadBalancer items on this page Returned by the lookup.
</ResponseField>
