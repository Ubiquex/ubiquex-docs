---
title: "azure_network_expressroute_service_endpoint_policy"
description: "`data_azure_network_expressroute_service_endpoint_policy` looks up a real, already-existing `azure_network_expressroute_service_endpoint_policy`."
---

It restricts which specific Azure service resource(s) a subnet's own VNet service endpoint traffic can reach.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_expressroute_service_endpoint_policy"})

    		ubx.Data(network.ExpressrouteServiceEndpointPolicy, "example", network.ExpressrouteServiceEndpointPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExpressrouteServiceEndpointPolicy } from "@ubx/sdk-azure/azure/data/network/expressroute_service_endpoint_policy";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_expressroute_service_endpoint_policy",
      });

      data(ExpressrouteServiceEndpointPolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import ExpressrouteServiceEndpointPolicy, ExpressrouteServiceEndpointPolicyConfig

    def describe():
        ubx.intent("look up azure_network_expressroute_service_endpoint_policy")

        ubx.data(ExpressrouteServiceEndpointPolicy, "example", ExpressrouteServiceEndpointPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ServiceEndpointPolicy items on this page Returned by the lookup.
</ResponseField>
