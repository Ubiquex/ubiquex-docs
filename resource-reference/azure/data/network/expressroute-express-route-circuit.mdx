---
title: "azure_network_expressroute_express_route_circuit"
description: "`data_azure_network_expressroute_express_route_circuit` looks up a real, already-existing `azure_network_expressroute_express_route_circuit`."
---

It is the logical connection to a connectivity provider's own ExpressRoute service, that private and Microsoft peering(s) are established over.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_expressroute_express_route_circuit"})

    		ubx.Data(network.ExpressrouteExpressRouteCircuit, "example", network.ExpressrouteExpressRouteCircuitConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExpressrouteExpressRouteCircuit } from "@ubx/sdk-azure/azure/data/network/expressroute_express_route_circuit";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_expressroute_express_route_circuit",
      });

      data(ExpressrouteExpressRouteCircuit, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import ExpressrouteExpressRouteCircuit, ExpressrouteExpressRouteCircuitConfig

    def describe():
        ubx.intent("look up azure_network_expressroute_express_route_circuit")

        ubx.data(ExpressrouteExpressRouteCircuit, "example", ExpressrouteExpressRouteCircuitConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ExpressRouteCircuit items on this page Returned by the lookup.
</ResponseField>
