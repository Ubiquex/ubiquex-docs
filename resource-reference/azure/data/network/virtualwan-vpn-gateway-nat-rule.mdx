---
title: "azure_network_virtualwan_vpn_gateway_nat_rule"
description: "`data_azure_network_virtualwan_vpn_gateway_nat_rule` looks up a real, already-existing `azure_network_virtualwan_vpn_gateway_nat_rule`."
---

It applies NAT translation to traffic passing through a Virtual WAN VPN gateway, for connecting sites with overlapping address spaces.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_vpn_gateway_nat_rule"})

    		ubx.Data(network.VirtualwanVpnGatewayNatRule, "example", network.VirtualwanVpnGatewayNatRuleConfig{
    			GatewayName: "example-gateway-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualwanVpnGatewayNatRule } from "@ubx/sdk-azure/azure/data/network/virtualwan_vpn_gateway_nat_rule";

    export default stack("example", () => {
      intent({ summary: "look up azure_network_virtualwan_vpn_gateway_nat_rule" });

      data(VirtualwanVpnGatewayNatRule, "example", {
        gatewayName: "example-gateway-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanVpnGatewayNatRule, VirtualwanVpnGatewayNatRuleConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_vpn_gateway_nat_rule")

        ubx.data(VirtualwanVpnGatewayNatRule, "example", VirtualwanVpnGatewayNatRuleConfig(
            gateway_name="example-gateway-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gateway_name" type="string" required>
  The real name of the gateway this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VpnGatewayNatRule items on this page Returned by the lookup.
</ResponseField>
