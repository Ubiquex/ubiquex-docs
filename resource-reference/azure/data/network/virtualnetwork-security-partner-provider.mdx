---
title: "azure_network_virtualnetwork_security_partner_provider"
description: "`data_azure_network_virtualnetwork_security_partner_provider` looks up a real, already-existing `azure_network_virtualnetwork_security_partner_provider`."
---

It integrates a supported third-party security provider (e.g. Check Point, Zscaler) with an Azure Firewall-managed virtual hub for traffic inspection.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualnetwork_security_partner_provider"})

    		ubx.Data(network.VirtualnetworkSecurityPartnerProvider, "example", network.VirtualnetworkSecurityPartnerProviderConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkSecurityPartnerProvider } from "@ubx/sdk-azure/azure/data/network/virtualnetwork_security_partner_provider";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualnetwork_security_partner_provider",
      });

      data(VirtualnetworkSecurityPartnerProvider, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualnetworkSecurityPartnerProvider, VirtualnetworkSecurityPartnerProviderConfig

    def describe():
        ubx.intent("look up azure_network_virtualnetwork_security_partner_provider")

        ubx.data(VirtualnetworkSecurityPartnerProvider, "example", VirtualnetworkSecurityPartnerProviderConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SecurityPartnerProvider items on this page Returned by the lookup.
</ResponseField>
