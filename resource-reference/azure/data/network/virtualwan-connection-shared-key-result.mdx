---
title: "azure_network_virtualwan_connection_shared_key_result"
description: "`azure_network_virtualwan_connection_shared_key_result` reads the real, shared IPsec key of a Virtual WAN VPN site connection."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_virtualwan_connection_shared_key_result"})

    		ubx.Data(network.VirtualwanConnectionSharedKeyResult, "example", network.VirtualwanConnectionSharedKeyResultConfig{
    			ConnectionName:     "example-connection-name",
    			GatewayName:        "example-gateway-name",
    			LinkConnectionName: "example-link-connection-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualwanConnectionSharedKeyResult } from "@ubx/sdk-azure/azure/data/network/virtualwan_connection_shared_key_result";

    export default stack("example", () => {
      intent({
        summary: "look up azure_network_virtualwan_connection_shared_key_result",
      });

      data(VirtualwanConnectionSharedKeyResult, "example", {
        connectionName: "example-connection-name",
        gatewayName: "example-gateway-name",
        linkConnectionName: "example-link-connection-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import VirtualwanConnectionSharedKeyResult, VirtualwanConnectionSharedKeyResultConfig

    def describe():
        ubx.intent("look up azure_network_virtualwan_connection_shared_key_result")

        ubx.data(VirtualwanConnectionSharedKeyResult, "example", VirtualwanConnectionSharedKeyResultConfig(
            connection_name="example-connection-name",
            gateway_name="example-gateway-name",
            link_connection_name="example-link-connection-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connection_name" type="string" required>
  The real name of the connection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="gateway_name" type="string" required>
  The real name of the gateway this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="link_connection_name" type="string" required>
  The real name of the link connection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ConnectionSharedKeyResult items on this page Returned by the lookup.
</ResponseField>
