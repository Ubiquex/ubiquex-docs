---
title: "azure_network_firewallpolicy_firewall_policy_rule_collection_group_draft"
description: "`azure_network_firewallpolicy_firewall_policy_rule_collection_group_draft` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_network_firewallpolicy_firewall_policy_rule_collection_group_draft` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_network_firewallpolicy_firewall_policy_rule_collection_group_draft"})

    		ubx.Data(network.FirewallpolicyFirewallPolicyRuleCollectionGroupDraft, "example", network.FirewallpolicyFirewallPolicyRuleCollectionGroupDraftConfig{
    			FirewallPolicyName:      "example-firewall-policy-name",
    			RuleCollectionGroupName: "example-rule-collection-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallpolicyFirewallPolicyRuleCollectionGroupDraft } from "@ubx/sdk-azure/azure/data/network/firewallpolicy_firewall_policy_rule_collection_group_draft";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_network_firewallpolicy_firewall_policy_rule_collection_group_draft",
      });

      data(FirewallpolicyFirewallPolicyRuleCollectionGroupDraft, "example", {
        firewallPolicyName: "example-firewall-policy-name",
        ruleCollectionGroupName: "example-rule-collection-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.network import FirewallpolicyFirewallPolicyRuleCollectionGroupDraft, FirewallpolicyFirewallPolicyRuleCollectionGroupDraftConfig

    def describe():
        ubx.intent("look up azure_network_firewallpolicy_firewall_policy_rule_collection_group_draft")

        ubx.data(FirewallpolicyFirewallPolicyRuleCollectionGroupDraft, "example", FirewallpolicyFirewallPolicyRuleCollectionGroupDraftConfig(
            firewall_policy_name="example-firewall-policy-name",
            rule_collection_group_name="example-rule-collection-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="firewall_policy_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="rule_collection_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  Properties of the rule collection group draft. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="priority" type="number">
      Priority of the Firewall Policy Rule Collection Group resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="rule_collections" type="list(object)">
      Group of Firewall Policy rule collections. Optional lookup argument.
    </ResponseField>
    <ResponseField name="size" type="string">
      A read-only string that represents the size of the FirewallPolicyRuleCollectionGroupProperties in MB. (ex 1.2MB) Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
