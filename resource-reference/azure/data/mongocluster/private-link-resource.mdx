---
title: "azure_mongocluster_private_link_resource"
description: "`azure_mongocluster_private_link_resource` reads a real Private Link resource exposed by a MongoDB vCore cluster, that a Private Endpoint can connect to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mongocluster "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/mongocluster"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_mongocluster_private_link_resource"})

    		ubx.Data(mongocluster.PrivateLinkResource, "example", mongocluster.PrivateLinkResourceConfig{
    			MongoClusterName: "example-mongo-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PrivateLinkResource } from "@ubx/sdk-azure/azure/data/mongocluster/private_link_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_mongocluster_private_link_resource" });

      data(PrivateLinkResource, "example", {
        mongoClusterName: "example-mongo-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.mongocluster import PrivateLinkResource, PrivateLinkResourceConfig

    def describe():
        ubx.intent("look up azure_mongocluster_private_link_resource")

        ubx.data(PrivateLinkResource, "example", PrivateLinkResourceConfig(
            mongo_cluster_name="example-mongo-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="mongo_cluster_name" type="string" required>
  The real name of the MongoDB vCore cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrivateLinkResource items on this page Returned by the lookup.
</ResponseField>
