---
title: "azure_mongocluster_mongo_cluster"
description: "`data_azure_mongocluster_mongo_cluster` looks up a real, already-existing `azure_mongocluster_mongo_cluster`."
---

It is an Azure Cosmos DB for MongoDB (vCore) cluster, a fully managed deployment offering native MongoDB wire-protocol compatibility.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	mongocluster "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/mongocluster"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_mongocluster_mongo_cluster"})

    		ubx.Data(mongocluster.MongoCluster, "example", mongocluster.MongoClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MongoCluster } from "./local-sdk/azure/sdk/typescript/azure/data/mongocluster/mongo_cluster.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_mongocluster_mongo_cluster" });

      data(MongoCluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.mongocluster import MongoCluster, MongoClusterConfig

    def describe():
        ubx.intent("look up azure_mongocluster_mongo_cluster")

        ubx.data(MongoCluster, "example", MongoClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The MongoCluster items on this page Returned by the lookup.
</ResponseField>
