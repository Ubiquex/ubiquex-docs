---
title: "azure_virtual_machine_run_command"
description: "`data_azure_virtual_machine_run_command` looks up a real, already-existing `azure_virtual_machine_run_command`."
---

It runs a script inside a VM through the VM agent for general machine or application management -- fetching content, changing configuration, or one-off diagnostics. This is Azure's original, action-oriented Run Command shape: one script, one active execution, a 90-minute limit. Azure's newer Managed Run Command variant exists separately for recurring or multi-script scenarios this shape isn't built for.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_virtual_machine_run_command"})

    		ubx.Data(virtual.MachineRunCommand, "example", virtual.MachineRunCommandConfig{
    			InstanceId:     "example",
    			VmScaleSetName: "example-vm-scale-set-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MachineRunCommand } from "./local-sdk/azure/sdk/typescript/azure/data/virtual/machine_run_command.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_virtual_machine_run_command" });

      data(MachineRunCommand, "example", {
        instanceId: "example",
        vmScaleSetName: "example-vm-scale-set-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.virtual import MachineRunCommand, MachineRunCommandConfig

    def describe():
        ubx.intent("look up azure_virtual_machine_run_command")

        ubx.data(MachineRunCommand, "example", MachineRunCommandConfig(
            instance_id="example",
            vm_scale_set_name="example-vm-scale-set-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of this instance. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vm_scale_set_name" type="string" required>
  The real name of the virtual machine scale set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The uri to fetch the next page of run commands. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of run commands. Returned by the lookup.
</ResponseField>
