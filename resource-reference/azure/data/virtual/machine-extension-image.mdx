---
title: "azure_virtual_machine_extension_image"
description: "`azure_virtual_machine_extension_image` reads a real, available VM extension image (a specific publisher/type/version combination) that can be..."
---

`azure_virtual_machine_extension_image` reads a real, available VM extension image (a specific publisher/type/version combination) that can be installed onto a virtual machine.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_virtual_machine_extension_image"})

    		ubx.Data(virtual.MachineExtensionImage, "example", virtual.MachineExtensionImageConfig{
    			PublisherName: "example-publisher-name",
    			Type:          "example",
    			Version:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MachineExtensionImage } from "./local-sdk/azure/sdk/typescript/azure/data/virtual/machine_extension_image.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_virtual_machine_extension_image" });

      data(MachineExtensionImage, "example", {
        publisherName: "example-publisher-name",
        type: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.virtual import MachineExtensionImage, MachineExtensionImageConfig

    def describe():
        ubx.intent("look up azure_virtual_machine_extension_image")

        ubx.data(MachineExtensionImage, "example", MachineExtensionImageConfig(
            publisher_name="example-publisher-name",
            type="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="publisher_name" type="string" required>
  The real name of the publisher this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string" required>
  The real, discriminating type of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string" required>
  The real version identifier for this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  Describes the properties of a Virtual Machine Extension Image. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="compute_role" type="string" required>
      The type of role (IaaS or PaaS) this extension supports. Required lookup argument.
    </ResponseField>
    <ResponseField name="extension_feature_metadata" type="object">
      Additional metadata about extension features, including compliance and capability tags. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="extension_feature_tags" type="list(object)">
          List of additional metadata properties (e.g., compliance flags, supported features). Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="handler_schema" type="string" required>
      The schema defined by publisher, where extension consumers should provide settings in a matching schema. Required lookup argument.
    </ResponseField>
    <ResponseField name="operating_system" type="string" required>
      The operating system this extension supports. Required lookup argument.
    </ResponseField>
    <ResponseField name="release_category" type="string">
      Enumerates the type of change introduced in the extension version. Optional lookup argument.
    </ResponseField>
    <ResponseField name="release_notes" type="string">
      Summary of changes or updates in this extension version. Returned by the lookup.
    </ResponseField>
    <ResponseField name="run_profile" type="string">
      Specifies the execution profile for the extension. Optional lookup argument.
    </ResponseField>
    <ResponseField name="supports_multiple_extensions" type="bool">
      Whether the handler can support multiple extensions. Optional lookup argument.
    </ResponseField>
    <ResponseField name="urgency_level" type="string">
      Defines urgency levels for extension updates. Optional lookup argument.
    </ResponseField>
    <ResponseField name="vm_scale_set_enabled" type="bool">
      Whether the extension can be used on xRP VMScaleSets. By default existing extensions are usable on scalesets, but there might be cases where a publisher wants to explicitly indicate the extension is only enabled for CRP VMs but not VMSS. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
