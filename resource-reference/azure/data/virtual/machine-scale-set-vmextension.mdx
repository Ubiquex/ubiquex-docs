---
title: "azure_virtual_machine_scale_set_vmextension"
description: "`data_azure_virtual_machine_scale_set_vmextension` looks up a real, already-existing `azure_virtual_machine_scale_set_vmextension`."
---

It installs a VM extension on one specific instance within a scale set, rather than on every instance the way the scale-set-level extension resource does -- the per-instance counterpart for cases that don't need a uniform, set-wide rollout.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_virtual_machine_scale_set_vmextension"})

    		ubx.Data(virtual.MachineScaleSetVmextension, "example", virtual.MachineScaleSetVmextensionConfig{
    			InstanceId:     "example",
    			VmScaleSetName: "example-vm-scale-set-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MachineScaleSetVmextension } from "./local-sdk/azure/sdk/typescript/azure/data/virtual/machine_scale_set_vmextension.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_virtual_machine_scale_set_vmextension" });

      data(MachineScaleSetVmextension, "example", {
        instanceId: "example",
        vmScaleSetName: "example-vm-scale-set-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.virtual import MachineScaleSetVmextension, MachineScaleSetVmextensionConfig

    def describe():
        ubx.intent("look up azure_virtual_machine_scale_set_vmextension")

        ubx.data(MachineScaleSetVmextension, "example", MachineScaleSetVmextensionConfig(
            instance_id="example",
            vm_scale_set_name="example-vm-scale-set-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of this instance. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vm_scale_set_name" type="string" required>
  The real name of the virtual machine scale set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The list of VMSS VM extensions Returned by the lookup.
</ResponseField>
