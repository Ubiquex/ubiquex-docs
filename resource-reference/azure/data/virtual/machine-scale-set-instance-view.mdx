---
title: "azure_virtual_machine_scale_set_instance_view"
description: "`azure_virtual_machine_scale_set_instance_view` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_virtual_machine_scale_set_instance_view` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_virtual_machine_scale_set_instance_view"})

    		ubx.Data(virtual.MachineScaleSetInstanceView, "example", virtual.MachineScaleSetInstanceViewConfig{
    			VmScaleSetName: "example-vm-scale-set-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MachineScaleSetInstanceView } from "@ubx/sdk-azure/azure/data/virtual/machine_scale_set_instance_view";

    export default stack("example", () => {
      intent({ summary: "look up azure_virtual_machine_scale_set_instance_view" });

      data(MachineScaleSetInstanceView, "example", {
        vmScaleSetName: "example-vm-scale-set-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.virtual import MachineScaleSetInstanceView, MachineScaleSetInstanceViewConfig

    def describe():
        ubx.intent("look up azure_virtual_machine_scale_set_instance_view")

        ubx.data(MachineScaleSetInstanceView, "example", MachineScaleSetInstanceViewConfig(
            vm_scale_set_name="example-vm-scale-set-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="vm_scale_set_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="extensions" type="list(object)">
  The extensions information. Returned by the lookup.
</ResponseField>

<ResponseField name="orchestration_services" type="list(object)">
  The orchestration services information. Returned by the lookup.
</ResponseField>

<ResponseField name="statuses" type="list(object)">
  The resource status information. Returned by the lookup.
</ResponseField>

<ResponseField name="virtual_machine" type="object">
  Instance view statuses summary for virtual machines of a virtual machine scale set. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="statuses_summary" type="list(object)">
      The extensions information. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
