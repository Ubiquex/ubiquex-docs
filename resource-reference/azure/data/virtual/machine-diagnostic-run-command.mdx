---
title: "azure_virtual_machine_diagnostic_run_command"
description: "`data_azure_virtual_machine_diagnostic_run_command` looks up a real, already-existing `azure_virtual_machine_diagnostic_run_command`."
---

It runs a script against one specific VM instance inside a scale set through the VM agent, the same real Run Command mechanism Azure uses for general VM management -- diagnosing and remediating an instance's access or network problems without needing direct remote access to it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_virtual_machine_diagnostic_run_command"})

    		ubx.Data(virtual.MachineDiagnosticRunCommand, "example", virtual.MachineDiagnosticRunCommandConfig{
    			InstanceId:     "example",
    			VmScaleSetName: "example-vm-scale-set-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MachineDiagnosticRunCommand } from "@ubx/sdk-azure/azure/data/virtual/machine_diagnostic_run_command";

    export default stack("example", () => {
      intent({ summary: "look up azure_virtual_machine_diagnostic_run_command" });

      data(MachineDiagnosticRunCommand, "example", {
        instanceId: "example",
        vmScaleSetName: "example-vm-scale-set-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.virtual import MachineDiagnosticRunCommand, MachineDiagnosticRunCommandConfig

    def describe():
        ubx.intent("look up azure_virtual_machine_diagnostic_run_command")

        ubx.data(MachineDiagnosticRunCommand, "example", MachineDiagnosticRunCommandConfig(
            instance_id="example",
            vm_scale_set_name="example-vm-scale-set-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of this instance. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vm_scale_set_name" type="string" required>
  The real name of the virtual machine scale set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The uri to fetch the next page of diagnostic run commands. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of diagnostic run commands. Returned by the lookup.
</ResponseField>
