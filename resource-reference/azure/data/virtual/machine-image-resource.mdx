---
title: "azure_virtual_machine_image_resource"
description: "`azure_virtual_machine_image_resource` reads a real, available marketplace or custom VM image resource that a new virtual machine can be provisioned from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_virtual_machine_image_resource"})

    		ubx.Data(virtual.MachineImageResource, "example", virtual.MachineImageResourceConfig{
    			EdgeZone: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MachineImageResource } from "./local-sdk/azure/sdk/typescript/azure/data/virtual/machine_image_resource.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_virtual_machine_image_resource" });

      data(MachineImageResource, "example", {
        edgeZone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.virtual import MachineImageResource, MachineImageResourceConfig

    def describe():
        ubx.intent("look up azure_virtual_machine_image_resource")

        ubx.data(MachineImageResource, "example", MachineImageResourceConfig(
            edge_zone="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="edge_zone" type="string" required>
  The real Azure Edge Zone this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URI to fetch the next page of VMImages in EdgeZone. Call ListNext() with this URI to fetch the next page of VmImages. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of VMImages in EdgeZone Returned by the lookup.
</ResponseField>
