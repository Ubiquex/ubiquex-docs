---
title: "azure_scvmm_availability_set"
description: "`data_azure_scvmm_availability_set` looks up a real, already-existing `azure_scvmm_availability_set`."
---

It groups VM(s) together on an Azure Arc-enabled System Center Virtual Machine Manager private cloud so the underlying hypervisor keeps them on separate hosts for fault tolerance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	scvmm "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/scvmm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_scvmm_availability_set"})

    		ubx.Data(scvmm.AvailabilitySet, "example", scvmm.AvailabilitySetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AvailabilitySet } from "@ubx/sdk-azure/azure/data/scvmm/availability_set";

    export default stack("example", () => {
      intent({ summary: "look up azure_scvmm_availability_set" });

      data(AvailabilitySet, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.scvmm import AvailabilitySet, AvailabilitySetConfig

    def describe():
        ubx.intent("look up azure_scvmm_availability_set")

        ubx.data(AvailabilitySet, "example", AvailabilitySetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AvailabilitySet items on this page Returned by the lookup.
</ResponseField>
