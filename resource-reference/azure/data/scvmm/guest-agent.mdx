---
title: "azure_scvmm_guest_agent"
description: "`azure_scvmm_guest_agent` reads the real, current status of the Arc guest agent installed inside an SCVMM-managed virtual machine."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	scvmm "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/scvmm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_scvmm_guest_agent"})

    		ubx.Data(scvmm.GuestAgent, "example", scvmm.GuestAgentConfig{
    			ResourceUri: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GuestAgent } from "@ubx/sdk-azure/azure/data/scvmm/guest_agent";

    export default stack("example", () => {
      intent({ summary: "look up azure_scvmm_guest_agent" });

      data(GuestAgent, "example", {
        resourceUri: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.scvmm import GuestAgent, GuestAgentConfig

    def describe():
        ubx.intent("look up azure_scvmm_guest_agent")

        ubx.data(GuestAgent, "example", GuestAgentConfig(
            resource_uri="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_uri" type="string" required>
  The real, unique Azure resource ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The GuestAgent items on this page Returned by the lookup.
</ResponseField>
