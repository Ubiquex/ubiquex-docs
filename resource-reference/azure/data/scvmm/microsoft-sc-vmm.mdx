---
title: "azure_scvmm_microsoft_sc_vmm"
description: "`azure_scvmm_microsoft_sc_vmm` looks up a real, Arc-connected System Center Virtual Machine Manager (SCVMM) management server."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	scvmm "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/scvmm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_scvmm_microsoft_sc_vmm"})

    		ubx.Data(scvmm.MicrosoftScVmm, "example", scvmm.MicrosoftScVmmConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MicrosoftScVmm } from "./local-sdk/azure/sdk/typescript/azure/data/scvmm/microsoft_sc_vmm.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_scvmm_microsoft_sc_vmm" });

      data(MicrosoftScVmm, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.scvmm import MicrosoftScVmm, MicrosoftScVmmConfig

    def describe():
        ubx.intent("look up azure_scvmm_microsoft_sc_vmm")

        ubx.data(MicrosoftScVmm, "example", MicrosoftScVmmConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
