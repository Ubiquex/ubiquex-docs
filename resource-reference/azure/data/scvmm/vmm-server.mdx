---
title: "azure_scvmm_vmm_server"
description: "`data_azure_scvmm_vmm_server` looks up a real, Arc-connected System Center Virtual Machine Manager (SCVMM) management server."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	scvmm "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/scvmm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_scvmm_vmm_server"})

    		ubx.Data(scvmm.VmmServer, "example", scvmm.VmmServerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VmmServer } from "@ubx/sdk-azure/azure/data/scvmm/vmm_server";

    export default stack("example", () => {
      intent({ summary: "look up azure_scvmm_vmm_server" });

      data(VmmServer, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.scvmm import VmmServer, VmmServerConfig

    def describe():
        ubx.intent("look up azure_scvmm_vmm_server")

        ubx.data(VmmServer, "example", VmmServerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VmmServer items on this page Returned by the lookup.
</ResponseField>
