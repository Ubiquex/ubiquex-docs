---
title: "azure_msi_managedidentity_federated_identity_credential"
description: "`data_azure_msi_managedidentity_federated_identity_credential` looks up a real, already-existing `azure_msi_managedidentity_federated_identity_credential`."
---

It establishes trust between a user-assigned managed identity and an external OIDC identity provider (e.g. a Kubernetes service account or GitHub Actions), enabling passwordless workload identity federation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	msi "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/msi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_msi_managedidentity_federated_identity_credential"})

    		ubx.Data(msi.ManagedidentityFederatedIdentityCredential, "example", msi.ManagedidentityFederatedIdentityCredentialConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedidentityFederatedIdentityCredential } from "@ubx/sdk-azure/azure/data/msi/managedidentity_federated_identity_credential";

    export default stack("example", () => {
      intent({
        summary: "look up azure_msi_managedidentity_federated_identity_credential",
      });

      data(ManagedidentityFederatedIdentityCredential, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.msi import ManagedidentityFederatedIdentityCredential, ManagedidentityFederatedIdentityCredentialConfig

    def describe():
        ubx.intent("look up azure_msi_managedidentity_federated_identity_credential")

        ubx.data(ManagedidentityFederatedIdentityCredential, "example", ManagedidentityFederatedIdentityCredentialConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The FederatedIdentityCredential items on this page Returned by the lookup.
</ResponseField>
