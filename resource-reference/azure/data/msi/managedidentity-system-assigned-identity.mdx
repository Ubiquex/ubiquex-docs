---
title: "azure_msi_managedidentity_system_assigned_identity"
description: "`azure_msi_managedidentity_system_assigned_identity` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_msi_managedidentity_system_assigned_identity` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	msi "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/msi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_msi_managedidentity_system_assigned_identity"})

    		ubx.Data(msi.ManagedidentitySystemAssignedIdentity, "example", msi.ManagedidentitySystemAssignedIdentityConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedidentitySystemAssignedIdentity } from "@ubx/sdk-azure/azure/data/msi/managedidentity_system_assigned_identity";

    export default stack("example", () => {
      intent({
        summary: "look up azure_msi_managedidentity_system_assigned_identity",
      });

      data(ManagedidentitySystemAssignedIdentity, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.msi import ManagedidentitySystemAssignedIdentity, ManagedidentitySystemAssignedIdentityConfig

    def describe():
        ubx.intent("look up azure_msi_managedidentity_system_assigned_identity")

        ubx.data(ManagedidentitySystemAssignedIdentity, "example", ManagedidentitySystemAssignedIdentityConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="scope" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="location" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties associated with the system assigned identity. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="client_id" type="string">
      Universally Unique Identifier Optional lookup argument.
    </ResponseField>
    <ResponseField name="client_secret_url" type="string">
      The ManagedServiceIdentity DataPlane URL that can be queried to obtain the identity credentials. Returned by the lookup.
    </ResponseField>
    <ResponseField name="principal_id" type="string">
      Universally Unique Identifier Optional lookup argument.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Universally Unique Identifier Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Returned by the lookup.
</ResponseField>
