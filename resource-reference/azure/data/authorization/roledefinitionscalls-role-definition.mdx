---
title: "azure_authorization_roledefinitionscalls_role_definition"
description: "`azure_authorization_roledefinitionscalls_role_definition` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_authorization_roledefinitionscalls_role_definition` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	authorization "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/authorization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_authorization_roledefinitionscalls_role_definition"})

    		ubx.Data(authorization.RoledefinitionscallsRoleDefinition, "example", authorization.RoledefinitionscallsRoleDefinitionConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RoledefinitionscallsRoleDefinition } from "@ubx/sdk-azure/azure/data/authorization/roledefinitionscalls_role_definition";

    export default stack("example", () => {
      intent({
        summary: "look up azure_authorization_roledefinitionscalls_role_definition",
      });

      data(RoledefinitionscallsRoleDefinition, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.authorization import RoledefinitionscallsRoleDefinition, RoledefinitionscallsRoleDefinitionConfig

    def describe():
        ubx.intent("look up azure_authorization_roledefinitionscalls_role_definition")

        ubx.data(RoledefinitionscallsRoleDefinition, "example", RoledefinitionscallsRoleDefinitionConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="scope" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Role definition list. Returned by the lookup.
</ResponseField>
