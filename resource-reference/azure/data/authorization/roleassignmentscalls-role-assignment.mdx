---
title: "azure_authorization_roleassignmentscalls_role_assignment"
description: "`data_azure_authorization_roleassignmentscalls_role_assignment` looks up a real, already-existing..."
---

`data_azure_authorization_roleassignmentscalls_role_assignment` looks up a real, already-existing `azure_authorization_roleassignmentscalls_role_assignment`. It grants a principal a specific Azure RBAC role at a defined scope.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	authorization "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/authorization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own roleassignmentscalls role assignment"})

    		ubx.Resource(authorization.RoleassignmentscallsRoleAssignment, "example", authorization.RoleassignmentscallsRoleAssignmentConfig{
    			Properties: map[string]any{"principal_id": "example", "role_definition_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoleassignmentscallsRoleAssignment } from "./local-sdk/azure/sdk/typescript/azure/authorization/roleassignmentscalls_role_assignment.ts";

    export default stack("example", () => {
      intent({ summary: "example own roleassignmentscalls role assignment" });

      resource(RoleassignmentscallsRoleAssignment, "example", {
        properties: { principalId: "example", roleDefinitionId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.authorization import RoleassignmentscallsRoleAssignment, RoleassignmentscallsRoleAssignmentConfig
    from ubx.azure.authorization.roleassignmentscalls_role_assignment import RoleassignmentscallsRoleAssignment_Properties

    def describe():
        ubx.intent("example own roleassignmentscalls role assignment")

        ubx.resource(RoleassignmentscallsRoleAssignment, "example", RoleassignmentscallsRoleAssignmentConfig(
            properties=RoleassignmentscallsRoleAssignment_Properties(principal_id="example", role_definition_id="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_authorization_roleassignmentscalls_role_assignment,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="tenant_id" type="string">
  The real Microsoft Entra ID tenant this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The RoleAssignment items on this page Returned by the lookup.
</ResponseField>
