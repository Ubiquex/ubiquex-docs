---
title: "azure_authorization_roleassignmentscalls_role_assignment"
description: "`data_azure_authorization_roleassignmentscalls_role_assignment` looks up a real, already-existing..."
---

`data_azure_authorization_roleassignmentscalls_role_assignment` looks up a real, already-existing `azure_authorization_roleassignmentscalls_role_assignment`. It grants a principal a specific Azure RBAC role at a defined scope.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	authorization "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/authorization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_authorization_roleassignmentscalls_role_assignment"})

    		ubx.Data(authorization.RoleassignmentscallsRoleAssignment, "example", authorization.RoleassignmentscallsRoleAssignmentConfig{
    			TenantId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { RoleassignmentscallsRoleAssignment } from "./local-sdk/azure/sdk/typescript/azure/data/authorization/roleassignmentscalls_role_assignment.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_authorization_roleassignmentscalls_role_assignment",
      });

      data(RoleassignmentscallsRoleAssignment, "example", {
        tenantId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.authorization import RoleassignmentscallsRoleAssignment, RoleassignmentscallsRoleAssignmentConfig

    def describe():
        ubx.intent("look up azure_authorization_roleassignmentscalls_role_assignment")

        ubx.data(RoleassignmentscallsRoleAssignment, "example", RoleassignmentscallsRoleAssignmentConfig(
            tenant_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="tenant_id" type="string">
  The real Microsoft Entra ID tenant this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The RoleAssignment items on this page Returned by the lookup.
</ResponseField>
