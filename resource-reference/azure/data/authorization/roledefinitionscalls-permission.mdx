---
title: "azure_authorization_roledefinitionscalls_permission"
description: "`azure_authorization_roledefinitionscalls_permission` reads a real, individual permission (an action or data action) grantable through an Azure RBAC..."
---

`azure_authorization_roledefinitionscalls_permission` reads a real, individual permission (an action or data action) grantable through an Azure RBAC role definition.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	authorization "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/authorization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_authorization_roledefinitionscalls_permission"})

    		ubx.Data(authorization.RoledefinitionscallsPermission, "example", authorization.RoledefinitionscallsPermissionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { RoledefinitionscallsPermission } from "./local-sdk/azure/sdk/typescript/azure/data/authorization/roledefinitionscalls_permission.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_authorization_roledefinitionscalls_permission",
      });

      data(RoledefinitionscallsPermission, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.authorization import RoledefinitionscallsPermission, RoledefinitionscallsPermissionConfig

    def describe():
        ubx.intent("look up azure_authorization_roledefinitionscalls_permission")

        ubx.data(RoledefinitionscallsPermission, "example", RoledefinitionscallsPermissionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  An array of permissions. Returned by the lookup.
</ResponseField>
