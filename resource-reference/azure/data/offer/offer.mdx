---
title: "azure_offer"
description: "`azure_offer` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_offer` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	offer "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/offer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_offer"})

    		ubx.Data(offer.Offer, "example", offer.OfferConfig{
    			EdgeZone:      "example",
    			Offer:         "example",
    			PublisherName: "example-publisher-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Offer } from "@ubx/sdk-azure/azure/data/offer/offer";

    export default stack("example", () => {
      intent({ summary: "look up azure_offer" });

      data(Offer, "example", {
        edgeZone: "example",
        offer: "example",
        publisherName: "example-publisher-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.offer import Offer, OfferConfig

    def describe():
        ubx.intent("look up azure_offer")

        ubx.data(Offer, "example", OfferConfig(
            edge_zone="example",
            offer="example",
            publisher_name="example-publisher-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="edge_zone" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="offer" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="publisher_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
