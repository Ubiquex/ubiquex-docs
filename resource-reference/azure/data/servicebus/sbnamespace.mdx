---
title: "azure_servicebus_sbnamespace"
description: "`data_azure_servicebus_sbnamespace` looks up a real, already-existing `azure_servicebus_sbnamespace`."
---

It is a Service Bus namespace, the management container that queue(s) and topic(s) are created within.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_servicebus_sbnamespace"})

    		ubx.Data(servicebus.Sbnamespace, "example", servicebus.SbnamespaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Sbnamespace } from "@ubx/sdk-azure/azure/data/servicebus/sbnamespace";

    export default stack("example", () => {
      intent({ summary: "look up azure_servicebus_sbnamespace" });

      data(Sbnamespace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.servicebus import Sbnamespace, SbnamespaceConfig

    def describe():
        ubx.intent("look up azure_servicebus_sbnamespace")

        ubx.data(Sbnamespace, "example", SbnamespaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SBNamespace items on this page Returned by the lookup.
</ResponseField>
