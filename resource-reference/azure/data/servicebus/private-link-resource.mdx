---
title: "azure_servicebus_private_link_resource"
description: "`azure_servicebus_private_link_resource` reads a real Private Link resource exposed by a Service Bus namespace, that a Private Endpoint can connect to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_servicebus_private_link_resource"})

    		ubx.Data(servicebus.PrivateLinkResource, "example", servicebus.PrivateLinkResourceConfig{
    			NamespaceName: "example-namespace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PrivateLinkResource } from "@ubx/sdk-azure/azure/data/servicebus/private_link_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_servicebus_private_link_resource" });

      data(PrivateLinkResource, "example", {
        namespaceName: "example-namespace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.servicebus import PrivateLinkResource, PrivateLinkResourceConfig

    def describe():
        ubx.intent("look up azure_servicebus_private_link_resource")

        ubx.data(PrivateLinkResource, "example", PrivateLinkResourceConfig(
            namespace_name="example-namespace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="namespace_name" type="string" required>
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrivateLinkResource items on this page Returned by the lookup.
</ResponseField>
