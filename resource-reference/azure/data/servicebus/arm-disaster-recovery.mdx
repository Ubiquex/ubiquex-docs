---
title: "azure_servicebus_arm_disaster_recovery"
description: "`data_azure_servicebus_arm_disaster_recovery` looks up a real, already-existing `azure_servicebus_arm_disaster_recovery`."
---

It pairs a Service Bus namespace with a secondary namespace in another region for geo-disaster recovery, replicating metadata (not message data) between them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_servicebus_arm_disaster_recovery"})

    		ubx.Data(servicebus.ArmDisasterRecovery, "example", servicebus.ArmDisasterRecoveryConfig{
    			NamespaceName: "example-namespace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ArmDisasterRecovery } from "./local-sdk/azure/sdk/typescript/azure/data/servicebus/arm_disaster_recovery.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_servicebus_arm_disaster_recovery" });

      data(ArmDisasterRecovery, "example", {
        namespaceName: "example-namespace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.servicebus import ArmDisasterRecovery, ArmDisasterRecoveryConfig

    def describe():
        ubx.intent("look up azure_servicebus_arm_disaster_recovery")

        ubx.data(ArmDisasterRecovery, "example", ArmDisasterRecoveryConfig(
            namespace_name="example-namespace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="namespace_name" type="string" required>
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ArmDisasterRecovery items on this page Returned by the lookup.
</ResponseField>
