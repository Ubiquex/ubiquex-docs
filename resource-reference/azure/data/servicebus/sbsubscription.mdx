---
title: "azure_servicebus_sbsubscription"
description: "`data_azure_servicebus_sbsubscription` looks up a real, already-existing `azure_servicebus_sbsubscription`."
---

It is a named, independent view of a Service Bus topic's own published messages, letting multiple subscription(s) each receive a copy of matching message(s) via the publish/subscribe pattern.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_servicebus_sbsubscription"})

    		ubx.Data(servicebus.Sbsubscription, "example", servicebus.SbsubscriptionConfig{
    			NamespaceName: "example-namespace-name",
    			TopicName:     "example-topic-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { Sbsubscription } from "./local-sdk/azure/sdk/typescript/azure/data/servicebus/sbsubscription.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_servicebus_sbsubscription" });

      data(Sbsubscription, "example", {
        namespaceName: "example-namespace-name",
        topicName: "example-topic-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.servicebus import Sbsubscription, SbsubscriptionConfig

    def describe():
        ubx.intent("look up azure_servicebus_sbsubscription")

        ubx.data(Sbsubscription, "example", SbsubscriptionConfig(
            namespace_name="example-namespace-name",
            topic_name="example-topic-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="namespace_name" type="string" required>
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="topic_name" type="string" required>
  The real name of the Event Grid topic this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SBSubscription items on this page Returned by the lookup.
</ResponseField>
