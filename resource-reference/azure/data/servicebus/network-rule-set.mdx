---
title: "azure_servicebus_network_rule_set"
description: "`azure_servicebus_network_rule_set` reads the real network access rules (allowed IPs and virtual networks) configured on a Service Bus namespace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicebus "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/servicebus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_servicebus_network_rule_set"})

    		ubx.Data(servicebus.NetworkRuleSet, "example", servicebus.NetworkRuleSetConfig{
    			NamespaceName: "example-namespace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkRuleSet } from "@ubx/sdk-azure/azure/data/servicebus/network_rule_set";

    export default stack("example", () => {
      intent({ summary: "look up azure_servicebus_network_rule_set" });

      data(NetworkRuleSet, "example", {
        namespaceName: "example-namespace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.servicebus import NetworkRuleSet, NetworkRuleSetConfig

    def describe():
        ubx.intent("look up azure_servicebus_network_rule_set")

        ubx.data(NetworkRuleSet, "example", NetworkRuleSetConfig(
            namespace_name="example-namespace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="namespace_name" type="string" required>
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NetworkRuleSet items on this page Returned by the lookup.
</ResponseField>
