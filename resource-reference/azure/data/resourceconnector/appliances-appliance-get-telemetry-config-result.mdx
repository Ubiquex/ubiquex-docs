---
title: "azure_resourceconnector_appliances_appliance_get_telemetry_config_result"
description: "`azure_resourceconnector_appliances_appliance_get_telemetry_config_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_resourceconnector_appliances_appliance_get_telemetry_config_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resourceconnector "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resourceconnector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resourceconnector_appliances_appliance_get_telemetry_config_result"})

    		ubx.Data(resourceconnector.AppliancesApplianceGetTelemetryConfigResult, "example", resourceconnector.AppliancesApplianceGetTelemetryConfigResultConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppliancesApplianceGetTelemetryConfigResult } from "@ubx/sdk-azure/azure/data/resourceconnector/appliances_appliance_get_telemetry_config_result";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_resourceconnector_appliances_appliance_get_telemetry_config_result",
      });

      data(AppliancesApplianceGetTelemetryConfigResult, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.resourceconnector import AppliancesApplianceGetTelemetryConfigResult, AppliancesApplianceGetTelemetryConfigResultConfig

    def describe():
        ubx.intent("look up azure_resourceconnector_appliances_appliance_get_telemetry_config_result")

        ubx.data(AppliancesApplianceGetTelemetryConfigResult, "example", AppliancesApplianceGetTelemetryConfigResultConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="telemetry_instrumentation_key" type="string">
  Telemetry instrumentation key. Returned by the lookup.
</ResponseField>
