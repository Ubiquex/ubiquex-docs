---
title: "azure_resourceconnector_appliances_appliance"
description: "`azure_resourceconnector_appliances_appliance` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_resourceconnector_appliances_appliance` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resourceconnector "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resourceconnector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resourceconnector_appliances_appliance"})

    		ubx.Data(resourceconnector.AppliancesAppliance, "example", resourceconnector.AppliancesApplianceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppliancesAppliance } from "@ubx/sdk-azure/azure/data/resourceconnector/appliances_appliance";

    export default stack("example", () => {
      intent({ summary: "look up azure_resourceconnector_appliances_appliance" });

      data(AppliancesAppliance, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.resourceconnector import AppliancesAppliance, AppliancesApplianceConfig

    def describe():
        ubx.intent("look up azure_resourceconnector_appliances_appliance")

        ubx.data(AppliancesAppliance, "example", AppliancesApplianceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Appliance items on this page Returned by the lookup.
</ResponseField>
