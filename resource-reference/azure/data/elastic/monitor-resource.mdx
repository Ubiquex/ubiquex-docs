---
title: "azure_elastic_monitor_resource"
description: "`data_azure_elastic_monitor_resource` looks up a real, already-existing `azure_elastic_monitor_resource`."
---

It -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/elastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_elastic_monitor_resource"})

    		ubx.Data(elastic.MonitorResource, "example", elastic.MonitorResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MonitorResource } from "@ubx/sdk-azure/azure/data/elastic/monitor_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_elastic_monitor_resource" });

      data(MonitorResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.elastic import MonitorResource, MonitorResourceConfig

    def describe():
        ubx.intent("look up azure_elastic_monitor_resource")

        ubx.data(MonitorResource, "example", MonitorResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ElasticMonitorResource items on this page Returned by the lookup.
</ResponseField>
