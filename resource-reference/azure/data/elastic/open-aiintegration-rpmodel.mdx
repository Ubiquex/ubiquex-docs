---
title: "azure_elastic_open_aiintegration_rpmodel"
description: "`azure_elastic_open_aiintegration_rpmodel` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_elastic_open_aiintegration_rpmodel` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/elastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_elastic_open_aiintegration_rpmodel"})

    		ubx.Data(elastic.OpenAiintegrationRpmodel, "example", elastic.OpenAiintegrationRpmodelConfig{
    			MonitorName: "example-monitor-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenAiintegrationRpmodel } from "@ubx/sdk-azure/azure/data/elastic/open_aiintegration_rpmodel";

    export default stack("example", () => {
      intent({ summary: "look up azure_elastic_open_aiintegration_rpmodel" });

      data(OpenAiintegrationRpmodel, "example", {
        monitorName: "example-monitor-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.elastic import OpenAiintegrationRpmodel, OpenAiintegrationRpmodelConfig

    def describe():
        ubx.intent("look up azure_elastic_open_aiintegration_rpmodel")

        ubx.data(OpenAiintegrationRpmodel, "example", OpenAiintegrationRpmodelConfig(
            monitor_name="example-monitor-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="monitor_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The OpenAIIntegrationRPModel items on this page Returned by the lookup.
</ResponseField>
