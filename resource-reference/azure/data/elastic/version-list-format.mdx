---
title: "azure_elastic_version_list_format"
description: "`azure_elastic_version_list_format` reads the real, available Elastic Stack versions for an Elastic integration deployment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	elastic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/elastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_elastic_version_list_format"})

    		ubx.Data(elastic.VersionListFormat, "example", elastic.VersionListFormatConfig{
    			Region: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VersionListFormat } from "./local-sdk/azure/sdk/typescript/azure/data/elastic/version_list_format.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_elastic_version_list_format" });

      data(VersionListFormat, "example", {
        region: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.elastic import VersionListFormat, VersionListFormatConfig

    def describe():
        ubx.intent("look up azure_elastic_version_list_format")

        ubx.data(VersionListFormat, "example", VersionListFormatConfig(
            region="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="region" type="string" required>
  The real Azure region this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ElasticVersionListFormat items on this page Returned by the lookup.
</ResponseField>
