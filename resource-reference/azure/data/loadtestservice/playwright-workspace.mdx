---
title: "azure_loadtestservice_playwright_workspace"
description: "`data_azure_loadtestservice_playwright_workspace` looks up a real Azure Load Testing resource configured for Playwright workspace testing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	loadtestservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/loadtestservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_loadtestservice_playwright_workspace"})

    		ubx.Data(loadtestservice.PlaywrightWorkspace, "example", loadtestservice.PlaywrightWorkspaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PlaywrightWorkspace } from "@ubx/sdk-azure/azure/data/loadtestservice/playwright_workspace";

    export default stack("example", () => {
      intent({ summary: "look up azure_loadtestservice_playwright_workspace" });

      data(PlaywrightWorkspace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.loadtestservice import PlaywrightWorkspace, PlaywrightWorkspaceConfig

    def describe():
        ubx.intent("look up azure_loadtestservice_playwright_workspace")

        ubx.data(PlaywrightWorkspace, "example", PlaywrightWorkspaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PlaywrightWorkspace items on this page Returned by the lookup.
</ResponseField>
