---
title: "azure_loadtestservice_load_test_resource"
description: "`data_azure_loadtestservice_load_test_resource` looks up a real, already-existing `azure_loadtestservice_load_test_resource`."
---

It is an Azure Load Testing resource, a fully managed service for generating high-scale load against an application to test its own performance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	loadtestservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/loadtestservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_loadtestservice_load_test_resource"})

    		ubx.Data(loadtestservice.LoadTestResource, "example", loadtestservice.LoadTestResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LoadTestResource } from "@ubx/sdk-azure/azure/data/loadtestservice/load_test_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_loadtestservice_load_test_resource" });

      data(LoadTestResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.loadtestservice import LoadTestResource, LoadTestResourceConfig

    def describe():
        ubx.intent("look up azure_loadtestservice_load_test_resource")

        ubx.data(LoadTestResource, "example", LoadTestResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The LoadTestResource items on this page Returned by the lookup.
</ResponseField>
