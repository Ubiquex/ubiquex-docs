---
title: "azure_loadtestservice_outbound_environment_endpoint"
description: "`azure_loadtestservice_outbound_environment_endpoint` reads a real, required outbound network endpoint for Azure Load Testing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	loadtestservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/loadtestservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_loadtestservice_outbound_environment_endpoint"})

    		ubx.Data(loadtestservice.OutboundEnvironmentEndpoint, "example", loadtestservice.OutboundEnvironmentEndpointConfig{
    			LoadTestName: "example-load-test-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OutboundEnvironmentEndpoint } from "./local-sdk/azure/sdk/typescript/azure/data/loadtestservice/outbound_environment_endpoint.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_loadtestservice_outbound_environment_endpoint",
      });

      data(OutboundEnvironmentEndpoint, "example", {
        loadTestName: "example-load-test-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.loadtestservice import OutboundEnvironmentEndpoint, OutboundEnvironmentEndpointConfig

    def describe():
        ubx.intent("look up azure_loadtestservice_outbound_environment_endpoint")

        ubx.data(OutboundEnvironmentEndpoint, "example", OutboundEnvironmentEndpointConfig(
            load_test_name="example-load-test-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="load_test_name" type="string" required>
  The real name of the Azure Load Testing resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The OutboundEnvironmentEndpoint items on this page Returned by the lookup.
</ResponseField>
