---
title: "azure_netapp_ransomware_report"
description: "`azure_netapp_ransomware_report` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_netapp_ransomware_report` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_netapp_ransomware_report"})

    		ubx.Data(netapp.RansomwareReport, "example", netapp.RansomwareReportConfig{
    			AccountName: "example-account-name",
    			PoolName:    "example-pool-name",
    			VolumeName:  "example-volume-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RansomwareReport } from "@ubx/sdk-azure/azure/data/netapp/ransomware_report";

    export default stack("example", () => {
      intent({ summary: "look up azure_netapp_ransomware_report" });

      data(RansomwareReport, "example", {
        accountName: "example-account-name",
        poolName: "example-pool-name",
        volumeName: "example-volume-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.netapp import RansomwareReport, RansomwareReportConfig

    def describe():
        ubx.intent("look up azure_netapp_ransomware_report")

        ubx.data(RansomwareReport, "example", RansomwareReportConfig(
            account_name="example-account-name",
            pool_name="example-pool-name",
            volume_name="example-volume-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="pool_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="volume_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The RansomwareReport items on this page Returned by the lookup.
</ResponseField>
