---
title: "azure_netapp_restore_status"
description: "`azure_netapp_restore_status` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_netapp_restore_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_netapp_restore_status"})

    		ubx.Data(netapp.RestoreStatus, "example", netapp.RestoreStatusConfig{
    			AccountName: "example-account-name",
    			PoolName:    "example-pool-name",
    			VolumeName:  "example-volume-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RestoreStatus } from "@ubx/sdk-azure/azure/data/netapp/restore_status";

    export default stack("example", () => {
      intent({ summary: "look up azure_netapp_restore_status" });

      data(RestoreStatus, "example", {
        accountName: "example-account-name",
        poolName: "example-pool-name",
        volumeName: "example-volume-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.netapp import RestoreStatus, RestoreStatusConfig

    def describe():
        ubx.intent("look up azure_netapp_restore_status")

        ubx.data(RestoreStatus, "example", RestoreStatusConfig(
            account_name="example-account-name",
            pool_name="example-pool-name",
            volume_name="example-volume-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="pool_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="volume_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error_message" type="string">
  Displays error message if the restore is in an error state Returned by the lookup.
</ResponseField>

<ResponseField name="healthy" type="bool">
  Restore health status Returned by the lookup.
</ResponseField>

<ResponseField name="mirror_state" type="string">
  The status of the replication Returned by the lookup.
</ResponseField>

<ResponseField name="relationship_status" type="string">
  Status of the volume restore relationship Returned by the lookup.
</ResponseField>

<ResponseField name="total_transfer_bytes" type="number">
  Displays the total bytes transferred Returned by the lookup.
</ResponseField>

<ResponseField name="unhealthy_reason" type="string">
  Reason for the unhealthy restore relationship Returned by the lookup.
</ResponseField>
