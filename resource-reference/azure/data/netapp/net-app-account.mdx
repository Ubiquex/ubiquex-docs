---
title: "azure_netapp_net_app_account"
description: "`data_azure_netapp_net_app_account` looks up a real, already-existing `azure_netapp_net_app_account`."
---

It is the top-level Azure NetApp Files resource that capacity pool(s) and their own volume(s) are organized within.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_netapp_net_app_account"})

    		ubx.Data(netapp.NetAppAccount, "example", netapp.NetAppAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetAppAccount } from "@ubx/sdk-azure/azure/data/netapp/net_app_account";

    export default stack("example", () => {
      intent({ summary: "look up azure_netapp_net_app_account" });

      data(NetAppAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.netapp import NetAppAccount, NetAppAccountConfig

    def describe():
        ubx.intent("look up azure_netapp_net_app_account")

        ubx.data(NetAppAccount, "example", NetAppAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NetAppAccount items on this page Returned by the lookup.
</ResponseField>
