---
title: "azure_netapp_backup_status"
description: "`azure_netapp_backup_status` reads the real, current status of an Azure NetApp Files backup."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_netapp_backup_status"})

    		ubx.Data(netapp.BackupStatus, "example", netapp.BackupStatusConfig{
    			AccountName: "example-account-name",
    			PoolName:    "example-pool-name",
    			VolumeName:  "example-volume-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupStatus } from "@ubx/sdk-azure/azure/data/netapp/backup_status";

    export default stack("example", () => {
      intent({ summary: "look up azure_netapp_backup_status" });

      data(BackupStatus, "example", {
        accountName: "example-account-name",
        poolName: "example-pool-name",
        volumeName: "example-volume-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.netapp import BackupStatus, BackupStatusConfig

    def describe():
        ubx.intent("look up azure_netapp_backup_status")

        ubx.data(BackupStatus, "example", BackupStatusConfig(
            account_name="example-account-name",
            pool_name="example-pool-name",
            volume_name="example-volume-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="pool_name" type="string" required>
  The real name of the pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="volume_name" type="string" required>
  The real name of the Azure NetApp Files volume this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error_message" type="string">
  Displays error message if the backup is in an error state Returned by the lookup.
</ResponseField>

<ResponseField name="healthy" type="bool">
  Backup health status Returned by the lookup.
</ResponseField>

<ResponseField name="last_transfer_size" type="number">
  Displays the last transfer size Returned by the lookup.
</ResponseField>

<ResponseField name="last_transfer_type" type="string">
  Displays the last transfer type Returned by the lookup.
</ResponseField>

<ResponseField name="mirror_state" type="string">
  The status of the replication Returned by the lookup.
</ResponseField>

<ResponseField name="relationship_status" type="string">
  Status of the volume backup relationship Returned by the lookup.
</ResponseField>

<ResponseField name="total_transfer_bytes" type="number">
  Displays the total bytes transferred Returned by the lookup.
</ResponseField>

<ResponseField name="transfer_progress_bytes" type="number">
  Displays the total number of bytes transferred for the ongoing operation Returned by the lookup.
</ResponseField>

<ResponseField name="unhealthy_reason" type="string">
  Reason for the unhealthy backup relationship Returned by the lookup.
</ResponseField>
