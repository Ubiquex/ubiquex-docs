---
title: "azure_netapp_volume"
description: "`data_azure_netapp_volume` looks up a real, already-existing `azure_netapp_volume`."
---

It is a managed NFS or SMB file share provisioned from an Azure NetApp Files capacity pool.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	netapp "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/netapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_netapp_volume"})

    		ubx.Data(netapp.Volume, "example", netapp.VolumeConfig{
    			AccountName: "example-account-name",
    			PoolName:    "example-pool-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Volume } from "@ubx/sdk-azure/azure/data/netapp/volume";

    export default stack("example", () => {
      intent({ summary: "look up azure_netapp_volume" });

      data(Volume, "example", {
        accountName: "example-account-name",
        poolName: "example-pool-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.netapp import Volume, VolumeConfig

    def describe():
        ubx.intent("look up azure_netapp_volume")

        ubx.data(Volume, "example", VolumeConfig(
            account_name="example-account-name",
            pool_name="example-pool-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="pool_name" type="string" required>
  The real name of the pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Volume items on this page Returned by the lookup.
</ResponseField>
