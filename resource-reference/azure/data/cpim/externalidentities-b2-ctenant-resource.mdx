---
title: "azure_cpim_externalidentities_b2_ctenant_resource"
description: "`data_azure_cpim_externalidentities_b2_ctenant_resource` looks up a real, already-existing `azure_cpim_externalidentities_b2_ctenant_resource`."
---

It is an Azure AD B2C tenant, a separate Microsoft Entra directory dedicated to customer identity and access management for an organization's own consumer-facing applications. No longer available for new customers since May 2025; existing tenants continue to be supported, with Microsoft steering new deployments toward Microsoft Entra External ID instead.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cpim "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cpim"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cpim_externalidentities_b2_ctenant_resource"})

    		ubx.Data(cpim.ExternalidentitiesB2CtenantResource, "example", cpim.ExternalidentitiesB2CtenantResourceConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExternalidentitiesB2CtenantResource } from "@ubx/sdk-azure/azure/data/cpim/externalidentities_b2_ctenant_resource";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cpim_externalidentities_b2_ctenant_resource",
      });

      data(ExternalidentitiesB2CtenantResource, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cpim import ExternalidentitiesB2CtenantResource, ExternalidentitiesB2CtenantResourceConfig

    def describe():
        ubx.intent("look up azure_cpim_externalidentities_b2_ctenant_resource")

        ubx.data(ExternalidentitiesB2CtenantResource, "example", ExternalidentitiesB2CtenantResourceConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  List of Azure AD B2C tenant resources Returned by the lookup.
</ResponseField>
