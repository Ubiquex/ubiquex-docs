---
title: "azure_devtestlabs_dtl_service_fabric"
description: "`data_azure_devtestlabs_dtl_service_fabric` looks up a real, already-existing `azure_devtestlabs_dtl_service_fabric`."
---

It is a Service Fabric cluster deployed and managed within a DevTest Lab, for testing Service Fabric-hosted applications in an isolated lab environment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devtestlabs_dtl_service_fabric"})

    		ubx.Data(devtestlabs.DtlServiceFabric, "example", devtestlabs.DtlServiceFabricConfig{
    			LabName:  "example-lab-name",
    			UserName: "example-user-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DtlServiceFabric } from "@ubx/sdk-azure/azure/data/devtestlabs/dtl_service_fabric";

    export default stack("example", () => {
      intent({ summary: "look up azure_devtestlabs_dtl_service_fabric" });

      data(DtlServiceFabric, "example", {
        labName: "example-lab-name",
        userName: "example-user-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.devtestlabs import DtlServiceFabric, DtlServiceFabricConfig

    def describe():
        ubx.intent("look up azure_devtestlabs_dtl_service_fabric")

        ubx.data(DtlServiceFabric, "example", DtlServiceFabricConfig(
            lab_name="example-lab-name",
            user_name="example-user-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="lab_name" type="string" required>
  The real name of the DevTest Lab this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  The real username this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ServiceFabric items on this page Returned by the lookup.
</ResponseField>
