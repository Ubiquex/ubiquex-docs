---
title: "azure_devtestlabs_dtl_virtual_network"
description: "`data_azure_devtestlabs_dtl_virtual_network` looks up a real, already-existing `azure_devtestlabs_dtl_virtual_network`."
---

It is a virtual network made available to a lab's own virtual machines, either created for the lab or a reference to an existing subscription virtual network shared into it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devtestlabs_dtl_virtual_network"})

    		ubx.Data(devtestlabs.DtlVirtualNetwork, "example", devtestlabs.DtlVirtualNetworkConfig{
    			LabName: "example-lab-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { DtlVirtualNetwork } from "./local-sdk/azure/sdk/typescript/azure/data/devtestlabs/dtl_virtual_network.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_devtestlabs_dtl_virtual_network" });

      data(DtlVirtualNetwork, "example", {
        labName: "example-lab-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.devtestlabs import DtlVirtualNetwork, DtlVirtualNetworkConfig

    def describe():
        ubx.intent("look up azure_devtestlabs_dtl_virtual_network")

        ubx.data(DtlVirtualNetwork, "example", DtlVirtualNetworkConfig(
            lab_name="example-lab-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="lab_name" type="string" required>
  The real name of the DevTest Lab this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualNetwork items on this page Returned by the lookup.
</ResponseField>
