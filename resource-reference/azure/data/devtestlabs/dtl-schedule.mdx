---
title: "azure_devtestlabs_dtl_schedule"
description: "`data_azure_devtestlabs_dtl_schedule` looks up a real, already-existing `azure_devtestlabs_dtl_schedule`."
---

It is an automated schedule within a lab, most commonly an auto-shutdown or auto-start time applied to a lab virtual machine or the whole lab.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devtestlabs_dtl_schedule"})

    		ubx.Data(devtestlabs.DtlSchedule, "example", devtestlabs.DtlScheduleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DtlSchedule } from "@ubx/sdk-azure/azure/data/devtestlabs/dtl_schedule";

    export default stack("example", () => {
      intent({ summary: "look up azure_devtestlabs_dtl_schedule" });

      data(DtlSchedule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.devtestlabs import DtlSchedule, DtlScheduleConfig

    def describe():
        ubx.intent("look up azure_devtestlabs_dtl_schedule")

        ubx.data(DtlSchedule, "example", DtlScheduleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Schedule items on this page Returned by the lookup.
</ResponseField>
