---
title: "azure_devtestlabs_dtl_lab"
description: "`data_azure_devtestlabs_dtl_lab` looks up a real, already-existing `azure_devtestlabs_dtl_lab`."
---

It is a DevTest Lab, the top-level container for a team's own set of development/test virtual machines, environments, artifacts, and cost/policy controls.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	devtestlabs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devtestlabs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devtestlabs_dtl_lab"})

    		ubx.Data(devtestlabs.DtlLab, "example", devtestlabs.DtlLabConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { DtlLab } from "./local-sdk/azure/sdk/typescript/azure/data/devtestlabs/dtl_lab.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_devtestlabs_dtl_lab" });

      data(DtlLab, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.devtestlabs import DtlLab, DtlLabConfig

    def describe():
        ubx.intent("look up azure_devtestlabs_dtl_lab")

        ubx.data(DtlLab, "example", DtlLabConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Lab items on this page Returned by the lookup.
</ResponseField>
