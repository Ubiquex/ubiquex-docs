---
title: "azure_notificationhubs_notification_hub_resource"
description: "`data_azure_notificationhubs_notification_hub_resource` looks up a real, already-existing `azure_notificationhubs_notification_hub_resource`."
---

It is a Notification Hub, a scalable push notification engine sending messages to iOS, Android, Windows, and other platform devices.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	notificationhubs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/notificationhubs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_notificationhubs_notification_hub_resource"})

    		ubx.Data(notificationhubs.NotificationHubResource, "example", notificationhubs.NotificationHubResourceConfig{
    			NamespaceName: "example-namespace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NotificationHubResource } from "@ubx/sdk-azure/azure/data/notificationhubs/notification_hub_resource";

    export default stack("example", () => {
      intent({
        summary: "look up azure_notificationhubs_notification_hub_resource",
      });

      data(NotificationHubResource, "example", {
        namespaceName: "example-namespace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.notificationhubs import NotificationHubResource, NotificationHubResourceConfig

    def describe():
        ubx.intent("look up azure_notificationhubs_notification_hub_resource")

        ubx.data(NotificationHubResource, "example", NotificationHubResourceConfig(
            namespace_name="example-namespace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="namespace_name" type="string" required>
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Gets or sets link to the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Gets or sets result of the List AuthorizationRules operation. Returned by the lookup.
</ResponseField>
