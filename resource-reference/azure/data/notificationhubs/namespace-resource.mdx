---
title: "azure_notificationhubs_namespace_resource"
description: "`data_azure_notificationhubs_namespace_resource` looks up a real, already-existing `azure_notificationhubs_namespace_resource`."
---

It is a Notification Hubs namespace, the management container that notification hub(s) are created within.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	notificationhubs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/notificationhubs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_notificationhubs_namespace_resource"})

    		ubx.Data(notificationhubs.NamespaceResource, "example", notificationhubs.NamespaceResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NamespaceResource } from "@ubx/sdk-azure/azure/data/notificationhubs/namespace_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_notificationhubs_namespace_resource" });

      data(NamespaceResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.notificationhubs import NamespaceResource, NamespaceResourceConfig

    def describe():
        ubx.intent("look up azure_notificationhubs_namespace_resource")

        ubx.data(NamespaceResource, "example", NamespaceResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Gets or sets link to the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Gets or sets result of the List AuthorizationRules operation. Returned by the lookup.
</ResponseField>
