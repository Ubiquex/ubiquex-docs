---
title: "azure_notificationhubs_private_link_resource"
description: "`azure_notificationhubs_private_link_resource` reads a real Private Link resource exposed by a Notification Hubs namespace..."
---

`azure_notificationhubs_private_link_resource` reads a real Private Link resource exposed by a Notification Hubs namespace, that a Private Endpoint can connect to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	notificationhubs "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/notificationhubs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_notificationhubs_private_link_resource"})

    		ubx.Data(notificationhubs.PrivateLinkResource, "example", notificationhubs.PrivateLinkResourceConfig{
    			NamespaceName: "example-namespace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { PrivateLinkResource } from "./local-sdk/azure/sdk/typescript/azure/data/notificationhubs/private_link_resource.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_notificationhubs_private_link_resource" });

      data(PrivateLinkResource, "example", {
        namespaceName: "example-namespace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.notificationhubs import PrivateLinkResource, PrivateLinkResourceConfig

    def describe():
        ubx.intent("look up azure_notificationhubs_private_link_resource")

        ubx.data(PrivateLinkResource, "example", PrivateLinkResourceConfig(
            namespace_name="example-namespace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="namespace_name" type="string" required>
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Gets or sets link to the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Gets or sets result of the List AuthorizationRules operation. Returned by the lookup.
</ResponseField>
