---
title: "azure_cost_management_openapi_budget"
description: "`data_azure_cost_management_openapi_budget` looks up a real, already-existing `azure_cost_management_openapi_budget`."
---

It sets a spending threshold on a scope (subscription, resource group, or management group) and triggers notification(s) when actual or forecasted spend crosses it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cost"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cost_management_openapi_budget"})

    		ubx.Data(cost.ManagementOpenapiBudget, "example", cost.ManagementOpenapiBudgetConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagementOpenapiBudget } from "@ubx/sdk-azure/azure/data/cost/management_openapi_budget";

    export default stack("example", () => {
      intent({ summary: "look up azure_cost_management_openapi_budget" });

      data(ManagementOpenapiBudget, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cost import ManagementOpenapiBudget, ManagementOpenapiBudgetConfig

    def describe():
        ubx.intent("look up azure_cost_management_openapi_budget")

        ubx.data(ManagementOpenapiBudget, "example", ManagementOpenapiBudgetConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="scope" type="string" required>
  The real Azure Resource Manager scope (subscription, resource group, or resource) this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results.\r\nIt's null for now, added for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of budgets. Returned by the lookup.
</ResponseField>
