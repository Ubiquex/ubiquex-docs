---
title: "azure_cost_management_openapi_alert"
description: "`azure_cost_management_openapi_alert` looks up a real, triggered Cost Management alert (such as a budget threshold breach)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	cost "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cost"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cost_management_openapi_alert"})

    		ubx.Data(cost.ManagementOpenapiAlert, "example", cost.ManagementOpenapiAlertConfig{
    			ExternalCloudProviderId:   "example",
    			ExternalCloudProviderType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ManagementOpenapiAlert } from "./local-sdk/azure/sdk/typescript/azure/data/cost/management_openapi_alert.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_cost_management_openapi_alert" });

      data(ManagementOpenapiAlert, "example", {
        externalCloudProviderId: "example",
        externalCloudProviderType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.cost import ManagementOpenapiAlert, ManagementOpenapiAlertConfig

    def describe():
        ubx.intent("look up azure_cost_management_openapi_alert")

        ubx.data(ManagementOpenapiAlert, "example", ManagementOpenapiAlertConfig(
            external_cloud_provider_id="example",
            external_cloud_provider_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="external_cloud_provider_id" type="string" required>
  The real, unique identifier of the external cloud provider account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="external_cloud_provider_type" type="string" required>
  The real type of external cloud provider (such as AWS or GCP) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of alerts results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of alerts. Returned by the lookup.
</ResponseField>
