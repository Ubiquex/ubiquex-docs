---
title: "azure_cost_management_openapi_benefit_utilization_summary"
description: "`data_azure_cost_management_openapi_benefit_utilization_summary` is a real, live lookup against Microsoft Azure..."
---

`data_azure_cost_management_openapi_benefit_utilization_summary` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cost"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cost_management_openapi_benefit_utilization_summary"})

    		ubx.Data(cost.ManagementOpenapiBenefitUtilizationSummary, "example", cost.ManagementOpenapiBenefitUtilizationSummaryConfig{
    			BillingAccountId: "example",
    			BillingProfileId: "example",
    			Filter:           "example",
    			GrainParameter:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagementOpenapiBenefitUtilizationSummary } from "@ubx/sdk-azure/azure/data/cost/management_openapi_benefit_utilization_summary";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_cost_management_openapi_benefit_utilization_summary",
      });

      data(ManagementOpenapiBenefitUtilizationSummary, "example", {
        billingAccountId: "example",
        billingProfileId: "example",
        filter: "example",
        grainParameter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cost import ManagementOpenapiBenefitUtilizationSummary, ManagementOpenapiBenefitUtilizationSummaryConfig

    def describe():
        ubx.intent("look up azure_cost_management_openapi_benefit_utilization_summary")

        ubx.data(ManagementOpenapiBenefitUtilizationSummary, "example", ManagementOpenapiBenefitUtilizationSummaryConfig(
            billing_account_id="example",
            billing_profile_id="example",
            filter="example",
            grain_parameter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="billing_profile_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="grain_parameter" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (URL) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of benefit utilization summaries. Returned by the lookup.
</ResponseField>
