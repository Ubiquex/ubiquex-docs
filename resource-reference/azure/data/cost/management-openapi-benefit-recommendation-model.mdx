---
title: "azure_cost_management_openapi_benefit_recommendation_model"
description: "`azure_cost_management_openapi_benefit_recommendation_model` reads a real, system-generated recommendation for purchasing a Reservation or Savings Plan."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cost"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cost_management_openapi_benefit_recommendation_model"})

    		ubx.Data(cost.ManagementOpenapiBenefitRecommendationModel, "example", cost.ManagementOpenapiBenefitRecommendationModelConfig{
    			BillingScope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagementOpenapiBenefitRecommendationModel } from "@ubx/sdk-azure/azure/data/cost/management_openapi_benefit_recommendation_model";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_cost_management_openapi_benefit_recommendation_model",
      });

      data(ManagementOpenapiBenefitRecommendationModel, "example", {
        billingScope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cost import ManagementOpenapiBenefitRecommendationModel, ManagementOpenapiBenefitRecommendationModelConfig

    def describe():
        ubx.intent("look up azure_cost_management_openapi_benefit_recommendation_model")

        ubx.data(ManagementOpenapiBenefitRecommendationModel, "example", ManagementOpenapiBenefitRecommendationModelConfig(
            billing_scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_scope" type="string" required>
  The real billing scope (account, profile, or invoice section) this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of benefit recommendations. Returned by the lookup.
</ResponseField>
