---
title: "azure_cost_management_openapi_export_execution_list_result"
description: "`azure_cost_management_openapi_export_execution_list_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cost_management_openapi_export_execution_list_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cost"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cost_management_openapi_export_execution_list_result"})

    		ubx.Data(cost.ManagementOpenapiExportExecutionListResult, "example", cost.ManagementOpenapiExportExecutionListResultConfig{
    			ExportName: "example-export-name",
    			Scope:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagementOpenapiExportExecutionListResult } from "@ubx/sdk-azure/azure/data/cost/management_openapi_export_execution_list_result";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_cost_management_openapi_export_execution_list_result",
      });

      data(ManagementOpenapiExportExecutionListResult, "example", {
        exportName: "example-export-name",
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cost import ManagementOpenapiExportExecutionListResult, ManagementOpenapiExportExecutionListResultConfig

    def describe():
        ubx.intent("look up azure_cost_management_openapi_export_execution_list_result")

        ubx.data(ManagementOpenapiExportExecutionListResult, "example", ManagementOpenapiExportExecutionListResultConfig(
            export_name="example-export-name",
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="export_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  A list of export runs. Returned by the lookup.
</ResponseField>
