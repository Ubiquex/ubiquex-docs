---
title: "azure_cost_management_openapi_dimensions_list_result"
description: "`azure_cost_management_openapi_dimensions_list_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cost_management_openapi_dimensions_list_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cost"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cost_management_openapi_dimensions_list_result"})

    		ubx.Data(cost.ManagementOpenapiDimensionsListResult, "example", cost.ManagementOpenapiDimensionsListResultConfig{
    			ExternalCloudProviderId:   "example",
    			ExternalCloudProviderType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagementOpenapiDimensionsListResult } from "@ubx/sdk-azure/azure/data/cost/management_openapi_dimensions_list_result";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cost_management_openapi_dimensions_list_result",
      });

      data(ManagementOpenapiDimensionsListResult, "example", {
        externalCloudProviderId: "example",
        externalCloudProviderType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cost import ManagementOpenapiDimensionsListResult, ManagementOpenapiDimensionsListResultConfig

    def describe():
        ubx.intent("look up azure_cost_management_openapi_dimensions_list_result")

        ubx.data(ManagementOpenapiDimensionsListResult, "example", ManagementOpenapiDimensionsListResultConfig(
            external_cloud_provider_id="example",
            external_cloud_provider_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="external_cloud_provider_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="external_cloud_provider_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of dimensions. Returned by the lookup.
</ResponseField>
