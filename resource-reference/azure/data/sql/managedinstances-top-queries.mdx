---
title: "azure_sql_managedinstances_top_queries"
description: "`azure_sql_managedinstances_top_queries` reads the real, top resource-consuming queries on a SQL Managed Instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_managedinstances_top_queries"})

    		ubx.Data(sql.ManagedinstancesTopQueries, "example", sql.ManagedinstancesTopQueriesConfig{
    			ManagedInstanceName: "example-managed-instance-name",
    			AggregationFunction: "example",
    			Databases:           "example",
    			EndTime:             "example",
    			Interval:            "example",
    			NumberOfQueries:     1,
    			ObservationMetric:   "example",
    			StartTime:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ManagedinstancesTopQueries } from "./local-sdk/azure/sdk/typescript/azure/data/sql/managedinstances_top_queries.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_managedinstances_top_queries" });

      data(ManagedinstancesTopQueries, "example", {
        managedInstanceName: "example-managed-instance-name",
        aggregationFunction: "example",
        databases: "example",
        endTime: "example",
        interval: "example",
        numberOfQueries: 1,
        observationMetric: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.sql import ManagedinstancesTopQueries, ManagedinstancesTopQueriesConfig

    def describe():
        ubx.intent("look up azure_sql_managedinstances_top_queries")

        ubx.data(ManagedinstancesTopQueries, "example", ManagedinstancesTopQueriesConfig(
            managed_instance_name="example-managed-instance-name",
            aggregation_function="example",
            databases="example",
            end_time="example",
            interval="example",
            number_of_queries=1,
            observation_metric="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregation_function" type="string">
  The real aggregation function (such as sum or average) applied to the underlying data. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="databases" type="string">
  The real databases included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="interval" type="string">
  The real time interval results are aggregated over. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="managed_instance_name" type="string" required>
  The real name of the SQL Managed Instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="number_of_queries" type="number">
  The real number of top queries to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="observation_metric" type="string">
  The real metric this observation is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The TopQueries items on this page Returned by the lookup.
</ResponseField>
