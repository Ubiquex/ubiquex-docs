---
title: "azure_sql_virtualnetworkrules_virtual_network_rule"
description: "`data_azure_sql_virtualnetworkrules_virtual_network_rule` looks up a real, already-existing `azure_sql_virtualnetworkrules_virtual_network_rule`."
---

It allows a specific virtual network subnet (via VNet service endpoint) to connect to an Azure SQL logical server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_virtualnetworkrules_virtual_network_rule"})

    		ubx.Data(sql.VirtualnetworkrulesVirtualNetworkRule, "example", sql.VirtualnetworkrulesVirtualNetworkRuleConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualnetworkrulesVirtualNetworkRule } from "@ubx/sdk-azure/azure/data/sql/virtualnetworkrules_virtual_network_rule";

    export default stack("example", () => {
      intent({
        summary: "look up azure_sql_virtualnetworkrules_virtual_network_rule",
      });

      data(VirtualnetworkrulesVirtualNetworkRule, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import VirtualnetworkrulesVirtualNetworkRule, VirtualnetworkrulesVirtualNetworkRuleConfig

    def describe():
        ubx.intent("look up azure_sql_virtualnetworkrules_virtual_network_rule")

        ubx.data(VirtualnetworkrulesVirtualNetworkRule, "example", VirtualnetworkrulesVirtualNetworkRuleConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualNetworkRule items on this page Returned by the lookup.
</ResponseField>
