---
title: "azure_sql_managedinstanceencryptionprotectors_managed_instance_encryption_protector"
description: "`data_azure_sql_managedinstanceencryptionprotectors_managed_instance_encryption_protector` looks up a real, already-existing..."
---

`data_azure_sql_managedinstanceencryptionprotectors_managed_instance_encryption_protector` looks up a real, already-existing `azure_sql_managedinstanceencryptionprotectors_managed_instance_encryption_protector`. It designates the customer-managed key used to protect transparent data encryption keys on an Azure SQL Managed Instance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_managedinstanceencryptionprotectors_managed_instance_encryption_protector"})

    		ubx.Data(sql.ManagedinstanceencryptionprotectorsManagedInstanceEncryptionProtector, "example", sql.ManagedinstanceencryptionprotectorsManagedInstanceEncryptionProtectorConfig{
    			ManagedInstanceName: "example-managed-instance-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedinstanceencryptionprotectorsManagedInstanceEncryptionProtector } from "@ubx/sdk-azure/azure/data/sql/managedinstanceencryptionprotectors_managed_instance_encryption_protector";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_sql_managedinstanceencryptionprotectors_managed_instance_encryption_protector",
      });

      data(
        ManagedinstanceencryptionprotectorsManagedInstanceEncryptionProtector,
        "example",
        {
          managedInstanceName: "example-managed-instance-name",
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import ManagedinstanceencryptionprotectorsManagedInstanceEncryptionProtector, ManagedinstanceencryptionprotectorsManagedInstanceEncryptionProtectorConfig

    def describe():
        ubx.intent("look up azure_sql_managedinstanceencryptionprotectors_managed_instance_encryption_protector")

        ubx.data(ManagedinstanceencryptionprotectorsManagedInstanceEncryptionProtector, "example", ManagedinstanceencryptionprotectorsManagedInstanceEncryptionProtectorConfig(
            managed_instance_name="example-managed-instance-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="managed_instance_name" type="string" required>
  The real name of the SQL Managed Instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ManagedInstanceEncryptionProtector items on this page Returned by the lookup.
</ResponseField>
