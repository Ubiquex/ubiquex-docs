---
title: "azure_sql_syncagents_sync_agent_linked_database"
description: "`azure_sql_syncagents_sync_agent_linked_database` looks up a real database linked to a SQL Data Sync agent."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_syncagents_sync_agent_linked_database"})

    		ubx.Data(sql.SyncagentsSyncAgentLinkedDatabase, "example", sql.SyncagentsSyncAgentLinkedDatabaseConfig{
    			ServerName:    "example-server-name",
    			SyncAgentName: "example-sync-agent-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SyncagentsSyncAgentLinkedDatabase } from "@ubx/sdk-azure/azure/data/sql/syncagents_sync_agent_linked_database";

    export default stack("example", () => {
      intent({
        summary: "look up azure_sql_syncagents_sync_agent_linked_database",
      });

      data(SyncagentsSyncAgentLinkedDatabase, "example", {
        serverName: "example-server-name",
        syncAgentName: "example-sync-agent-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import SyncagentsSyncAgentLinkedDatabase, SyncagentsSyncAgentLinkedDatabaseConfig

    def describe():
        ubx.intent("look up azure_sql_syncagents_sync_agent_linked_database")

        ubx.data(SyncagentsSyncAgentLinkedDatabase, "example", SyncagentsSyncAgentLinkedDatabaseConfig(
            server_name="example-server-name",
            sync_agent_name="example-sync-agent-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sync_agent_name" type="string" required>
  The real name of the SQL Data Sync agent this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SyncAgentLinkedDatabase items on this page Returned by the lookup.
</ResponseField>
