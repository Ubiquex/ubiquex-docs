---
title: "azure_sql_ledgerdigestuploads_ledger_digest_uploads"
description: "`data_azure_sql_ledgerdigestuploads_ledger_digest_uploads` looks up a real, already-existing `azure_sql_ledgerdigestuploads_ledger_digest_uploads`."
---

It configures Azure SQL Database Ledger to periodically upload cryptographic digest(s) to an external store (Azure Storage or a blockchain), independently proving data hasn't been tampered with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_ledgerdigestuploads_ledger_digest_uploads"})

    		ubx.Data(sql.LedgerdigestuploadsLedgerDigestUploads, "example", sql.LedgerdigestuploadsLedgerDigestUploadsConfig{
    			DatabaseName: "example-database-name",
    			ServerName:   "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { LedgerdigestuploadsLedgerDigestUploads } from "./local-sdk/azure/sdk/typescript/azure/data/sql/ledgerdigestuploads_ledger_digest_uploads.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_sql_ledgerdigestuploads_ledger_digest_uploads",
      });

      data(LedgerdigestuploadsLedgerDigestUploads, "example", {
        databaseName: "example-database-name",
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.sql import LedgerdigestuploadsLedgerDigestUploads, LedgerdigestuploadsLedgerDigestUploadsConfig

    def describe():
        ubx.intent("look up azure_sql_ledgerdigestuploads_ledger_digest_uploads")

        ubx.data(LedgerdigestuploadsLedgerDigestUploads, "example", LedgerdigestuploadsLedgerDigestUploadsConfig(
            database_name="example-database-name",
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_name" type="string" required>
  The real name of the database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The LedgerDigestUploads items on this page Returned by the lookup.
</ResponseField>
