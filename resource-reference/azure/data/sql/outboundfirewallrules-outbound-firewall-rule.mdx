---
title: "azure_sql_outboundfirewallrules_outbound_firewall_rule"
description: "`data_azure_sql_outboundfirewallrules_outbound_firewall_rule` looks up a real, already-existing `azure_sql_outboundfirewallrules_outbound_firewall_rule`."
---

It allows an Azure SQL logical server to make outbound connections to a specific external FQDN, e.g. for cross-database queries via PolyBase or Elastic Query.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_outboundfirewallrules_outbound_firewall_rule"})

    		ubx.Data(sql.OutboundfirewallrulesOutboundFirewallRule, "example", sql.OutboundfirewallrulesOutboundFirewallRuleConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OutboundfirewallrulesOutboundFirewallRule } from "@ubx/sdk-azure/azure/data/sql/outboundfirewallrules_outbound_firewall_rule";

    export default stack("example", () => {
      intent({
        summary: "look up azure_sql_outboundfirewallrules_outbound_firewall_rule",
      });

      data(OutboundfirewallrulesOutboundFirewallRule, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import OutboundfirewallrulesOutboundFirewallRule, OutboundfirewallrulesOutboundFirewallRuleConfig

    def describe():
        ubx.intent("look up azure_sql_outboundfirewallrules_outbound_firewall_rule")

        ubx.data(OutboundfirewallrulesOutboundFirewallRule, "example", OutboundfirewallrulesOutboundFirewallRuleConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The OutboundFirewallRule items on this page Returned by the lookup.
</ResponseField>
