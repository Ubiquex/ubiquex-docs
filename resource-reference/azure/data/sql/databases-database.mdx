---
title: "azure_sql_databases_database"
description: "`data_azure_sql_databases_database` looks up a real, already-existing `azure_sql_databases_database`."
---

It is an Azure SQL Database, a fully managed, single or elastic-pool-hosted relational database.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_databases_database"})

    		ubx.Data(sql.DatabasesDatabase, "example", sql.DatabasesDatabaseConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DatabasesDatabase } from "@ubx/sdk-azure/azure/data/sql/databases_database";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_databases_database" });

      data(DatabasesDatabase, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import DatabasesDatabase, DatabasesDatabaseConfig

    def describe():
        ubx.intent("look up azure_sql_databases_database")

        ubx.data(DatabasesDatabase, "example", DatabasesDatabaseConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Database items on this page Returned by the lookup.
</ResponseField>
