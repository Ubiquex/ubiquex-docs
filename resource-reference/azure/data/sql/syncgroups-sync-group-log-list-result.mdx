---
title: "azure_sql_syncgroups_sync_group_log_list_result"
description: "`azure_sql_syncgroups_sync_group_log_list_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_sql_syncgroups_sync_group_log_list_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_syncgroups_sync_group_log_list_result"})

    		ubx.Data(sql.SyncgroupsSyncGroupLogListResult, "example", sql.SyncgroupsSyncGroupLogListResultConfig{
    			DatabaseName:      "example-database-name",
    			EndTime:           "example",
    			ServerName:        "example-server-name",
    			StartTime:         "example",
    			SyncGroupName:     "example-sync-group-name",
    			Type:              "example",
    			ContinuationToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SyncgroupsSyncGroupLogListResult } from "@ubx/sdk-azure/azure/data/sql/syncgroups_sync_group_log_list_result";

    export default stack("example", () => {
      intent({
        summary: "look up azure_sql_syncgroups_sync_group_log_list_result",
      });

      data(SyncgroupsSyncGroupLogListResult, "example", {
        databaseName: "example-database-name",
        endTime: "example",
        serverName: "example-server-name",
        startTime: "example",
        syncGroupName: "example-sync-group-name",
        type: "example",
        continuationToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import SyncgroupsSyncGroupLogListResult, SyncgroupsSyncGroupLogListResultConfig

    def describe():
        ubx.intent("look up azure_sql_syncgroups_sync_group_log_list_result")

        ubx.data(SyncgroupsSyncGroupLogListResult, "example", SyncgroupsSyncGroupLogListResultConfig(
            database_name="example-database-name",
            end_time="example",
            server_name="example-server-name",
            start_time="example",
            sync_group_name="example-sync-group-name",
            type="example",
            continuation_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="continuation_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sync_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SyncGroupLogProperties items on this page Returned by the lookup.
</ResponseField>
