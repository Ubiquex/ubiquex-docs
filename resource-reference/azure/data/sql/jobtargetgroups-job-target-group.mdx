---
title: "azure_sql_jobtargetgroups_job_target_group"
description: "`data_azure_sql_jobtargetgroups_job_target_group` looks up a real, already-existing `azure_sql_jobtargetgroups_job_target_group`."
---

It defines the database(s), elastic pool(s), or server(s) an Elastic Database Jobs job's own step(s) execute against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_jobtargetgroups_job_target_group"})

    		ubx.Data(sql.JobtargetgroupsJobTargetGroup, "example", sql.JobtargetgroupsJobTargetGroupConfig{
    			JobAgentName: "example-job-agent-name",
    			ServerName:   "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { JobtargetgroupsJobTargetGroup } from "./local-sdk/azure/sdk/typescript/azure/data/sql/jobtargetgroups_job_target_group.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_jobtargetgroups_job_target_group" });

      data(JobtargetgroupsJobTargetGroup, "example", {
        jobAgentName: "example-job-agent-name",
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.sql import JobtargetgroupsJobTargetGroup, JobtargetgroupsJobTargetGroupConfig

    def describe():
        ubx.intent("look up azure_sql_jobtargetgroups_job_target_group")

        ubx.data(JobtargetgroupsJobTargetGroup, "example", JobtargetgroupsJobTargetGroupConfig(
            job_agent_name="example-job-agent-name",
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_agent_name" type="string" required>
  The real name of the SQL Elastic Job agent this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The JobTargetGroup items on this page Returned by the lookup.
</ResponseField>
