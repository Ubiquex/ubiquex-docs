---
title: "azure_sql_serverdnsaliases_server_dns_alias"
description: "`data_azure_sql_serverdnsaliases_server_dns_alias` looks up a real, already-existing `azure_sql_serverdnsaliases_server_dns_alias`."
---

It creates an alternate, alias DNS name pointing to an Azure SQL logical server, usable to redirect connections during failover without changing client connection strings.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_serverdnsaliases_server_dns_alias"})

    		ubx.Data(sql.ServerdnsaliasesServerDnsAlias, "example", sql.ServerdnsaliasesServerDnsAliasConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ServerdnsaliasesServerDnsAlias } from "./local-sdk/azure/sdk/typescript/azure/data/sql/serverdnsaliases_server_dns_alias.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_serverdnsaliases_server_dns_alias" });

      data(ServerdnsaliasesServerDnsAlias, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.sql import ServerdnsaliasesServerDnsAlias, ServerdnsaliasesServerDnsAliasConfig

    def describe():
        ubx.intent("look up azure_sql_serverdnsaliases_server_dns_alias")

        ubx.data(ServerdnsaliasesServerDnsAlias, "example", ServerdnsaliasesServerDnsAliasConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ServerDnsAlias items on this page Returned by the lookup.
</ResponseField>
