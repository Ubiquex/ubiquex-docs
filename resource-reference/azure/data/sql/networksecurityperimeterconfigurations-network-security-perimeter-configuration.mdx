---
title: "azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration"
description: "`data_azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration` looks up a real, already-existing..."
---

`data_azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration` looks up a real, already-existing `azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration`. It associates an Azure SQL resource with a network security perimeter, restricting its own network access to the perimeter's own boundary rules.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration"})

    		ubx.Data(sql.NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfiguration, "example", sql.NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfigurationConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfiguration } from "@ubx/sdk-azure/azure/data/sql/networksecurityperimeterconfigurations_network_security_perimeter_configuration";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration",
      });

      data(
        NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfiguration,
        "example",
        {
          serverName: "example-server-name",
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfiguration, NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfigurationConfig

    def describe():
        ubx.intent("look up azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration")

        ubx.data(NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfiguration, "example", NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfigurationConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NetworkSecurityPerimeterConfiguration items on this page Returned by the lookup.
</ResponseField>
