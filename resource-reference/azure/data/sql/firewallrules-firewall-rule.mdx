---
title: "azure_sql_firewallrules_firewall_rule"
description: "`data_azure_sql_firewallrules_firewall_rule` looks up a real, already-existing `azure_sql_firewallrules_firewall_rule`."
---

It allows a specific IP address range to connect to an Azure SQL logical server that hasn't been placed inside a virtual network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_firewallrules_firewall_rule"})

    		ubx.Data(sql.FirewallrulesFirewallRule, "example", sql.FirewallrulesFirewallRuleConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { FirewallrulesFirewallRule } from "./local-sdk/azure/sdk/typescript/azure/data/sql/firewallrules_firewall_rule.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_firewallrules_firewall_rule" });

      data(FirewallrulesFirewallRule, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.sql import FirewallrulesFirewallRule, FirewallrulesFirewallRuleConfig

    def describe():
        ubx.intent("look up azure_sql_firewallrules_firewall_rule")

        ubx.data(FirewallrulesFirewallRule, "example", FirewallrulesFirewallRuleConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The FirewallRule items on this page Returned by the lookup.
</ResponseField>
