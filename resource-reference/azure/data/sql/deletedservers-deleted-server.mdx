---
title: "azure_sql_deletedservers_deleted_server"
description: "`data_azure_sql_deletedservers_deleted_server` looks up a real, already-existing `azure_sql_deletedservers_deleted_server`."
---

It reports a soft-deleted Azure SQL logical server, recoverable within its own retention window.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_deletedservers_deleted_server"})

    		ubx.Data(sql.DeletedserversDeletedServer, "example", sql.DeletedserversDeletedServerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeletedserversDeletedServer } from "@ubx/sdk-azure/azure/data/sql/deletedservers_deleted_server";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_deletedservers_deleted_server" });

      data(DeletedserversDeletedServer, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import DeletedserversDeletedServer, DeletedserversDeletedServerConfig

    def describe():
        ubx.intent("look up azure_sql_deletedservers_deleted_server")

        ubx.data(DeletedserversDeletedServer, "example", DeletedserversDeletedServerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DeletedServer items on this page Returned by the lookup.
</ResponseField>
