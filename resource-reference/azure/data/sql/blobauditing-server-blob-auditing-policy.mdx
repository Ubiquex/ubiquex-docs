---
title: "azure_sql_blobauditing_server_blob_auditing_policy"
description: "`data_azure_sql_blobauditing_server_blob_auditing_policy` looks up a real, already-existing `azure_sql_blobauditing_server_blob_auditing_policy`."
---

It configures audit log export (to a Storage account) for database events across every database on an Azure SQL logical server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_blobauditing_server_blob_auditing_policy"})

    		ubx.Data(sql.BlobauditingServerBlobAuditingPolicy, "example", sql.BlobauditingServerBlobAuditingPolicyConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BlobauditingServerBlobAuditingPolicy } from "@ubx/sdk-azure/azure/data/sql/blobauditing_server_blob_auditing_policy";

    export default stack("example", () => {
      intent({
        summary: "look up azure_sql_blobauditing_server_blob_auditing_policy",
      });

      data(BlobauditingServerBlobAuditingPolicy, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import BlobauditingServerBlobAuditingPolicy, BlobauditingServerBlobAuditingPolicyConfig

    def describe():
        ubx.intent("look up azure_sql_blobauditing_server_blob_auditing_policy")

        ubx.data(BlobauditingServerBlobAuditingPolicy, "example", BlobauditingServerBlobAuditingPolicyConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ServerBlobAuditingPolicy items on this page Returned by the lookup.
</ResponseField>
