---
title: "azure_sql_serverazureadadministrators_server_azure_adadministrator"
description: "`data_azure_sql_serverazureadadministrators_server_azure_adadministrator` looks up a real, already-existing..."
---

`data_azure_sql_serverazureadadministrators_server_azure_adadministrator` looks up a real, already-existing `azure_sql_serverazureadadministrators_server_azure_adadministrator`. It designates a Microsoft Entra ID principal as the administrator for an Azure SQL logical server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_serverazureadadministrators_server_azure_adadministrator"})

    		ubx.Data(sql.ServerazureadadministratorsServerAzureAdadministrator, "example", sql.ServerazureadadministratorsServerAzureAdadministratorConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServerazureadadministratorsServerAzureAdadministrator } from "@ubx/sdk-azure/azure/data/sql/serverazureadadministrators_server_azure_adadministrator";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_sql_serverazureadadministrators_server_azure_adadministrator",
      });

      data(ServerazureadadministratorsServerAzureAdadministrator, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import ServerazureadadministratorsServerAzureAdadministrator, ServerazureadadministratorsServerAzureAdadministratorConfig

    def describe():
        ubx.intent("look up azure_sql_serverazureadadministrators_server_azure_adadministrator")

        ubx.data(ServerazureadadministratorsServerAzureAdadministrator, "example", ServerazureadadministratorsServerAzureAdadministratorConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to retrieve next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of results. Returned by the lookup.
</ResponseField>
