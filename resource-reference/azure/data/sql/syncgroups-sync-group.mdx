---
title: "azure_sql_syncgroups_sync_group"
description: "`data_azure_sql_syncgroups_sync_group` looks up a real, already-existing `azure_sql_syncgroups_sync_group`."
---

It defines a bidirectional or one-way SQL Data Sync topology between an Azure SQL Database hub and its own member database(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_syncgroups_sync_group"})

    		ubx.Data(sql.SyncgroupsSyncGroup, "example", sql.SyncgroupsSyncGroupConfig{
    			DatabaseName: "example-database-name",
    			ServerName:   "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SyncgroupsSyncGroup } from "@ubx/sdk-azure/azure/data/sql/syncgroups_sync_group";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_syncgroups_sync_group" });

      data(SyncgroupsSyncGroup, "example", {
        databaseName: "example-database-name",
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import SyncgroupsSyncGroup, SyncgroupsSyncGroupConfig

    def describe():
        ubx.intent("look up azure_sql_syncgroups_sync_group")

        ubx.data(SyncgroupsSyncGroup, "example", SyncgroupsSyncGroupConfig(
            database_name="example-database-name",
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_name" type="string" required>
  The real name of the database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SyncGroup items on this page Returned by the lookup.
</ResponseField>
