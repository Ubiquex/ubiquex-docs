---
title: "azure_sql_syncmembers_sync_member"
description: "`azure_sql_syncmembers_sync_member` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_sql_syncmembers_sync_member` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_syncmembers_sync_member"})

    		ubx.Data(sql.SyncmembersSyncMember, "example", sql.SyncmembersSyncMemberConfig{
    			DatabaseName:  "example-database-name",
    			ServerName:    "example-server-name",
    			SyncGroupName: "example-sync-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SyncmembersSyncMember } from "@ubx/sdk-azure/azure/data/sql/syncmembers_sync_member";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_syncmembers_sync_member" });

      data(SyncmembersSyncMember, "example", {
        databaseName: "example-database-name",
        serverName: "example-server-name",
        syncGroupName: "example-sync-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import SyncmembersSyncMember, SyncmembersSyncMemberConfig

    def describe():
        ubx.intent("look up azure_sql_syncmembers_sync_member")

        ubx.data(SyncmembersSyncMember, "example", SyncmembersSyncMemberConfig(
            database_name="example-database-name",
            server_name="example-server-name",
            sync_group_name="example-sync-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sync_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SyncMember items on this page Returned by the lookup.
</ResponseField>
