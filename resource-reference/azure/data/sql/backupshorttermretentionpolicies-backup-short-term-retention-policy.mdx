---
title: "azure_sql_backupshorttermretentionpolicies_backup_short_term_retention_policy"
description: "`azure_sql_backupshorttermretentionpolicies_backup_short_term_retention_policy` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_sql_backupshorttermretentionpolicies_backup_short_term_retention_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_backupshorttermretentionpolicies_backup_short_term_retention_policy"})

    		ubx.Data(sql.BackupshorttermretentionpoliciesBackupShortTermRetentionPolicy, "example", sql.BackupshorttermretentionpoliciesBackupShortTermRetentionPolicyConfig{
    			DatabaseName: "example-database-name",
    			ServerName:   "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupshorttermretentionpoliciesBackupShortTermRetentionPolicy } from "@ubx/sdk-azure/azure/data/sql/backupshorttermretentionpolicies_backup_short_term_retention_policy";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_sql_backupshorttermretentionpolicies_backup_short_term_retention_policy",
      });

      data(
        BackupshorttermretentionpoliciesBackupShortTermRetentionPolicy,
        "example",
        {
          databaseName: "example-database-name",
          serverName: "example-server-name",
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import BackupshorttermretentionpoliciesBackupShortTermRetentionPolicy, BackupshorttermretentionpoliciesBackupShortTermRetentionPolicyConfig

    def describe():
        ubx.intent("look up azure_sql_backupshorttermretentionpolicies_backup_short_term_retention_policy")

        ubx.data(BackupshorttermretentionpoliciesBackupShortTermRetentionPolicy, "example", BackupshorttermretentionpoliciesBackupShortTermRetentionPolicyConfig(
            database_name="example-database-name",
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The BackupShortTermRetentionPolicy items on this page Returned by the lookup.
</ResponseField>
