---
title: "azure_sql_managedinstances_top_queries_list_result"
description: "`azure_sql_managedinstances_top_queries_list_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_sql_managedinstances_top_queries_list_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_managedinstances_top_queries_list_result"})

    		ubx.Data(sql.ManagedinstancesTopQueriesListResult, "example", sql.ManagedinstancesTopQueriesListResultConfig{
    			ManagedInstanceName: "example-managed-instance-name",
    			AggregationFunction: "example",
    			Databases:           "example",
    			EndTime:             "example",
    			Interval:            "example",
    			NumberOfQueries:     1,
    			ObservationMetric:   "example",
    			StartTime:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedinstancesTopQueriesListResult } from "@ubx/sdk-azure/azure/data/sql/managedinstances_top_queries_list_result";

    export default stack("example", () => {
      intent({
        summary: "look up azure_sql_managedinstances_top_queries_list_result",
      });

      data(ManagedinstancesTopQueriesListResult, "example", {
        managedInstanceName: "example-managed-instance-name",
        aggregationFunction: "example",
        databases: "example",
        endTime: "example",
        interval: "example",
        numberOfQueries: 1,
        observationMetric: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import ManagedinstancesTopQueriesListResult, ManagedinstancesTopQueriesListResultConfig

    def describe():
        ubx.intent("look up azure_sql_managedinstances_top_queries_list_result")

        ubx.data(ManagedinstancesTopQueriesListResult, "example", ManagedinstancesTopQueriesListResultConfig(
            managed_instance_name="example-managed-instance-name",
            aggregation_function="example",
            databases="example",
            end_time="example",
            interval="example",
            number_of_queries=1,
            observation_metric="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregation_function" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="databases" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="interval" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="managed_instance_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="number_of_queries" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="observation_metric" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The TopQueries items on this page Returned by the lookup.
</ResponseField>
