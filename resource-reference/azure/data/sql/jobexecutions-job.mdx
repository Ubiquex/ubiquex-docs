---
title: "azure_sql_jobexecutions_job"
description: "`azure_sql_jobexecutions_job` looks up one real, executed run of an Elastic Database Job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_jobexecutions_job"})

    		ubx.Data(sql.JobexecutionsJob, "example", sql.JobexecutionsJobConfig{
    			JobAgentName:  "example-job-agent-name",
    			JobName:       "example-job-name",
    			ServerName:    "example-server-name",
    			CreateTimeMax: "example",
    			CreateTimeMin: "example",
    			EndTimeMax:    "example",
    			EndTimeMin:    "example",
    			IsActive:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { JobexecutionsJob } from "./local-sdk/azure/sdk/typescript/azure/data/sql/jobexecutions_job.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_jobexecutions_job" });

      data(JobexecutionsJob, "example", {
        jobAgentName: "example-job-agent-name",
        jobName: "example-job-name",
        serverName: "example-server-name",
        createTimeMax: "example",
        createTimeMin: "example",
        endTimeMax: "example",
        endTimeMin: "example",
        isActive: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.sql import JobexecutionsJob, JobexecutionsJobConfig

    def describe():
        ubx.intent("look up azure_sql_jobexecutions_job")

        ubx.data(JobexecutionsJob, "example", JobexecutionsJobConfig(
            job_agent_name="example-job-agent-name",
            job_name="example-job-name",
            server_name="example-server-name",
            create_time_max="example",
            create_time_min="example",
            end_time_max="example",
            end_time_min="example",
            is_active=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="create_time_max" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="create_time_min" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time_max" type="string">
  The latest real end timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time_min" type="string">
  The earliest real end timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="is_active" type="bool">
  Whether this object is currently active. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="job_agent_name" type="string" required>
  The real name of the SQL Elastic Job agent this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="job_name" type="string" required>
  The real name of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The JobExecution items on this page Returned by the lookup.
</ResponseField>
