---
title: "azure_sql_jobexecutions_job"
description: "`azure_sql_jobexecutions_job` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_sql_jobexecutions_job` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_jobexecutions_job"})

    		ubx.Data(sql.JobexecutionsJob, "example", sql.JobexecutionsJobConfig{
    			JobAgentName:  "example-job-agent-name",
    			JobName:       "example-job-name",
    			ServerName:    "example-server-name",
    			CreateTimeMax: "example",
    			CreateTimeMin: "example",
    			EndTimeMax:    "example",
    			EndTimeMin:    "example",
    			IsActive:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobexecutionsJob } from "@ubx/sdk-azure/azure/data/sql/jobexecutions_job";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_jobexecutions_job" });

      data(JobexecutionsJob, "example", {
        jobAgentName: "example-job-agent-name",
        jobName: "example-job-name",
        serverName: "example-server-name",
        createTimeMax: "example",
        createTimeMin: "example",
        endTimeMax: "example",
        endTimeMin: "example",
        isActive: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import JobexecutionsJob, JobexecutionsJobConfig

    def describe():
        ubx.intent("look up azure_sql_jobexecutions_job")

        ubx.data(JobexecutionsJob, "example", JobexecutionsJobConfig(
            job_agent_name="example-job-agent-name",
            job_name="example-job-name",
            server_name="example-server-name",
            create_time_max="example",
            create_time_min="example",
            end_time_max="example",
            end_time_min="example",
            is_active=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="create_time_max" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="create_time_min" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time_max" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time_min" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="is_active" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="job_agent_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="job_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The JobExecution items on this page Returned by the lookup.
</ResponseField>
