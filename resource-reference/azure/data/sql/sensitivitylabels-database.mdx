---
title: "azure_sql_sensitivitylabels_database"
description: "`azure_sql_sensitivitylabels_database` reads a real sensitivity label (data classification tag) applied to a column in a SQL Database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_sensitivitylabels_database"})

    		ubx.Data(sql.SensitivitylabelsDatabase, "example", sql.SensitivitylabelsDatabaseConfig{
    			DatabaseName: "example-database-name",
    			ServerName:   "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { SensitivitylabelsDatabase } from "./local-sdk/azure/sdk/typescript/azure/data/sql/sensitivitylabels_database.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_sensitivitylabels_database" });

      data(SensitivitylabelsDatabase, "example", {
        databaseName: "example-database-name",
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.sql import SensitivitylabelsDatabase, SensitivitylabelsDatabaseConfig

    def describe():
        ubx.intent("look up azure_sql_sensitivitylabels_database")

        ubx.data(SensitivitylabelsDatabase, "example", SensitivitylabelsDatabaseConfig(
            database_name="example-database-name",
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_name" type="string" required>
  The real name of the database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SensitivityLabel items on this page Returned by the lookup.
</ResponseField>
