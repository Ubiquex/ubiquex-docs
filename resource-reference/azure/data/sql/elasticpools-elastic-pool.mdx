---
title: "azure_sql_elasticpools_elastic_pool"
description: "`data_azure_sql_elasticpools_elastic_pool` looks up a real, already-existing `azure_sql_elasticpools_elastic_pool`."
---

It is a shared pool of compute resources that multiple Azure SQL Databases draw from, letting variable-usage databases share capacity cost-effectively.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_elasticpools_elastic_pool"})

    		ubx.Data(sql.ElasticpoolsElasticPool, "example", sql.ElasticpoolsElasticPoolConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ElasticpoolsElasticPool } from "./local-sdk/azure/sdk/typescript/azure/data/sql/elasticpools_elastic_pool.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_elasticpools_elastic_pool" });

      data(ElasticpoolsElasticPool, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.sql import ElasticpoolsElasticPool, ElasticpoolsElasticPoolConfig

    def describe():
        ubx.intent("look up azure_sql_elasticpools_elastic_pool")

        ubx.data(ElasticpoolsElasticPool, "example", ElasticpoolsElasticPoolConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ElasticPool items on this page Returned by the lookup.
</ResponseField>
