---
title: "azure_sql_jobagents_job_agent"
description: "`azure_sql_jobagents_job_agent` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_sql_jobagents_job_agent` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_jobagents_job_agent"})

    		ubx.Data(sql.JobagentsJobAgent, "example", sql.JobagentsJobAgentConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobagentsJobAgent } from "@ubx/sdk-azure/azure/data/sql/jobagents_job_agent";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_jobagents_job_agent" });

      data(JobagentsJobAgent, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import JobagentsJobAgent, JobagentsJobAgentConfig

    def describe():
        ubx.intent("look up azure_sql_jobagents_job_agent")

        ubx.data(JobagentsJobAgent, "example", JobagentsJobAgentConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The JobAgent items on this page Returned by the lookup.
</ResponseField>
