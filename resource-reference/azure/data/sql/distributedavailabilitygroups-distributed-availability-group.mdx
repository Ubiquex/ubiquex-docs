---
title: "azure_sql_distributedavailabilitygroups_distributed_availability_group"
description: "`azure_sql_distributedavailabilitygroups_distributed_availability_group` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_sql_distributedavailabilitygroups_distributed_availability_group` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_distributedavailabilitygroups_distributed_availability_group"})

    		ubx.Data(sql.DistributedavailabilitygroupsDistributedAvailabilityGroup, "example", sql.DistributedavailabilitygroupsDistributedAvailabilityGroupConfig{
    			ManagedInstanceName: "example-managed-instance-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DistributedavailabilitygroupsDistributedAvailabilityGroup } from "@ubx/sdk-azure/azure/data/sql/distributedavailabilitygroups_distributed_availability_group";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_sql_distributedavailabilitygroups_distributed_availability_group",
      });

      data(DistributedavailabilitygroupsDistributedAvailabilityGroup, "example", {
        managedInstanceName: "example-managed-instance-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import DistributedavailabilitygroupsDistributedAvailabilityGroup, DistributedavailabilitygroupsDistributedAvailabilityGroupConfig

    def describe():
        ubx.intent("look up azure_sql_distributedavailabilitygroups_distributed_availability_group")

        ubx.data(DistributedavailabilitygroupsDistributedAvailabilityGroup, "example", DistributedavailabilitygroupsDistributedAvailabilityGroupConfig(
            managed_instance_name="example-managed-instance-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="managed_instance_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to retrieve next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of results. Returned by the lookup.
</ResponseField>
