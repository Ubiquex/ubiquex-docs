---
title: "azure_sql_virtualclusters_virtual_cluster"
description: "`data_azure_sql_virtualclusters_virtual_cluster` looks up a real, already-existing `azure_sql_virtualclusters_virtual_cluster`."
---

It reports the underlying shared infrastructure cluster grouping Azure SQL Managed Instance(s) deployed into the same subnet.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_virtualclusters_virtual_cluster"})

    		ubx.Data(sql.VirtualclustersVirtualCluster, "example", sql.VirtualclustersVirtualClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualclustersVirtualCluster } from "@ubx/sdk-azure/azure/data/sql/virtualclusters_virtual_cluster";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_virtualclusters_virtual_cluster" });

      data(VirtualclustersVirtualCluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import VirtualclustersVirtualCluster, VirtualclustersVirtualClusterConfig

    def describe():
        ubx.intent("look up azure_sql_virtualclusters_virtual_cluster")

        ubx.data(VirtualclustersVirtualCluster, "example", VirtualclustersVirtualClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualCluster items on this page Returned by the lookup.
</ResponseField>
