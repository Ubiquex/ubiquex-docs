---
title: "azure_sql_encryptionprotectors_encryption_protector"
description: "`data_azure_sql_encryptionprotectors_encryption_protector` looks up a real, already-existing `azure_sql_encryptionprotectors_encryption_protector`."
---

It designates the customer-managed key used to protect transparent data encryption keys on an Azure SQL logical server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_encryptionprotectors_encryption_protector"})

    		ubx.Data(sql.EncryptionprotectorsEncryptionProtector, "example", sql.EncryptionprotectorsEncryptionProtectorConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EncryptionprotectorsEncryptionProtector } from "@ubx/sdk-azure/azure/data/sql/encryptionprotectors_encryption_protector";

    export default stack("example", () => {
      intent({
        summary: "look up azure_sql_encryptionprotectors_encryption_protector",
      });

      data(EncryptionprotectorsEncryptionProtector, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import EncryptionprotectorsEncryptionProtector, EncryptionprotectorsEncryptionProtectorConfig

    def describe():
        ubx.intent("look up azure_sql_encryptionprotectors_encryption_protector")

        ubx.data(EncryptionprotectorsEncryptionProtector, "example", EncryptionprotectorsEncryptionProtectorConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The EncryptionProtector items on this page Returned by the lookup.
</ResponseField>
