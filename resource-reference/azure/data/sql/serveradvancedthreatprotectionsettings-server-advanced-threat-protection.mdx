---
title: "azure_sql_serveradvancedthreatprotectionsettings_server_advanced_threat_protection"
description: "`azure_sql_serveradvancedthreatprotectionsettings_server_advanced_threat_protection` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_sql_serveradvancedthreatprotectionsettings_server_advanced_threat_protection` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_serveradvancedthreatprotectionsettings_server_advanced_threat_protection"})

    		ubx.Data(sql.ServeradvancedthreatprotectionsettingsServerAdvancedThreatProtection, "example", sql.ServeradvancedthreatprotectionsettingsServerAdvancedThreatProtectionConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServeradvancedthreatprotectionsettingsServerAdvancedThreatProtection } from "@ubx/sdk-azure/azure/data/sql/serveradvancedthreatprotectionsettings_server_advanced_threat_protection";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_sql_serveradvancedthreatprotectionsettings_server_advanced_threat_protection",
      });

      data(
        ServeradvancedthreatprotectionsettingsServerAdvancedThreatProtection,
        "example",
        {
          serverName: "example-server-name",
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import ServeradvancedthreatprotectionsettingsServerAdvancedThreatProtection, ServeradvancedthreatprotectionsettingsServerAdvancedThreatProtectionConfig

    def describe():
        ubx.intent("look up azure_sql_serveradvancedthreatprotectionsettings_server_advanced_threat_protection")

        ubx.data(ServeradvancedthreatprotectionsettingsServerAdvancedThreatProtection, "example", ServeradvancedthreatprotectionsettingsServerAdvancedThreatProtectionConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to retrieve next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of results. Returned by the lookup.
</ResponseField>
