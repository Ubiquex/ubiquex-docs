---
title: "azure_sql_longtermretentionbackups_long_term_retention_backup"
description: "`azure_sql_longtermretentionbackups_long_term_retention_backup` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_sql_longtermretentionbackups_long_term_retention_backup` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_longtermretentionbackups_long_term_retention_backup"})

    		ubx.Data(sql.LongtermretentionbackupsLongTermRetentionBackup, "example", sql.LongtermretentionbackupsLongTermRetentionBackupConfig{
    			LocationName:          "example-location-name",
    			DatabaseState:         "example",
    			OnlyLatestPerDatabase: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LongtermretentionbackupsLongTermRetentionBackup } from "@ubx/sdk-azure/azure/data/sql/longtermretentionbackups_long_term_retention_backup";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_sql_longtermretentionbackups_long_term_retention_backup",
      });

      data(LongtermretentionbackupsLongTermRetentionBackup, "example", {
        locationName: "example-location-name",
        databaseState: "example",
        onlyLatestPerDatabase: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import LongtermretentionbackupsLongTermRetentionBackup, LongtermretentionbackupsLongTermRetentionBackupConfig

    def describe():
        ubx.intent("look up azure_sql_longtermretentionbackups_long_term_retention_backup")

        ubx.data(LongtermretentionbackupsLongTermRetentionBackup, "example", LongtermretentionbackupsLongTermRetentionBackupConfig(
            location_name="example-location-name",
            database_state="example",
            only_latest_per_database=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_state" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="location_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="only_latest_per_database" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to retrieve next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of results. Returned by the lookup.
</ResponseField>
