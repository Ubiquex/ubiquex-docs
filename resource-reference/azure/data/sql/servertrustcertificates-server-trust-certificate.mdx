---
title: "azure_sql_servertrustcertificates_server_trust_certificate"
description: "`data_azure_sql_servertrustcertificates_server_trust_certificate` looks up a real, already-existing..."
---

`data_azure_sql_servertrustcertificates_server_trust_certificate` looks up a real, already-existing `azure_sql_servertrustcertificates_server_trust_certificate`. It establishes trust for a distributed availability group linking an Azure SQL Managed Instance to an external SQL Server instance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_servertrustcertificates_server_trust_certificate"})

    		ubx.Data(sql.ServertrustcertificatesServerTrustCertificate, "example", sql.ServertrustcertificatesServerTrustCertificateConfig{
    			ManagedInstanceName: "example-managed-instance-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServertrustcertificatesServerTrustCertificate } from "@ubx/sdk-azure/azure/data/sql/servertrustcertificates_server_trust_certificate";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_sql_servertrustcertificates_server_trust_certificate",
      });

      data(ServertrustcertificatesServerTrustCertificate, "example", {
        managedInstanceName: "example-managed-instance-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import ServertrustcertificatesServerTrustCertificate, ServertrustcertificatesServerTrustCertificateConfig

    def describe():
        ubx.intent("look up azure_sql_servertrustcertificates_server_trust_certificate")

        ubx.data(ServertrustcertificatesServerTrustCertificate, "example", ServertrustcertificatesServerTrustCertificateConfig(
            managed_instance_name="example-managed-instance-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="managed_instance_name" type="string" required>
  The real name of the SQL Managed Instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to retrieve next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of results. Returned by the lookup.
</ResponseField>
