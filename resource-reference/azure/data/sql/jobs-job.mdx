---
title: "azure_sql_jobs_job"
description: "`data_azure_sql_jobs_job` looks up a real, already-existing `azure_sql_jobs_job`."
---

It is a scheduled or on-demand T-SQL script definition run by an Elastic Database Jobs agent against its own target group.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_jobs_job"})

    		ubx.Data(sql.JobsJob, "example", sql.JobsJobConfig{
    			JobAgentName: "example-job-agent-name",
    			ServerName:   "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobsJob } from "@ubx/sdk-azure/azure/data/sql/jobs_job";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_jobs_job" });

      data(JobsJob, "example", {
        jobAgentName: "example-job-agent-name",
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import JobsJob, JobsJobConfig

    def describe():
        ubx.intent("look up azure_sql_jobs_job")

        ubx.data(JobsJob, "example", JobsJobConfig(
            job_agent_name="example-job-agent-name",
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_agent_name" type="string" required>
  The real name of the SQL Elastic Job agent this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Job items on this page Returned by the lookup.
</ResponseField>
