---
title: "azure_sql_instancepools_instance_pool"
description: "`data_azure_sql_instancepools_instance_pool` looks up a real, already-existing `azure_sql_instancepools_instance_pool`."
---

It is a shared pool of compute resources that multiple Azure SQL Managed Instances draw from, for cost-efficient hosting of smaller instances.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_instancepools_instance_pool"})

    		ubx.Data(sql.InstancepoolsInstancePool, "example", sql.InstancepoolsInstancePoolConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstancepoolsInstancePool } from "@ubx/sdk-azure/azure/data/sql/instancepools_instance_pool";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_instancepools_instance_pool" });

      data(InstancepoolsInstancePool, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.sql import InstancepoolsInstancePool, InstancepoolsInstancePoolConfig

    def describe():
        ubx.intent("look up azure_sql_instancepools_instance_pool")

        ubx.data(InstancepoolsInstancePool, "example", InstancepoolsInstancePoolConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The InstancePool items on this page Returned by the lookup.
</ResponseField>
