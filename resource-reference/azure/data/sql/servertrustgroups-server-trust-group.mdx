---
title: "azure_sql_servertrustgroups_server_trust_group"
description: "`data_azure_sql_servertrustgroups_server_trust_group` looks up a real, already-existing `azure_sql_servertrustgroups_server_trust_group`."
---

It establishes mutual trust between Azure SQL Managed Instances (or Managed Instances and SQL Server) for cross-instance distributed transactions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_sql_servertrustgroups_server_trust_group"})

    		ubx.Data(sql.ServertrustgroupsServerTrustGroup, "example", sql.ServertrustgroupsServerTrustGroupConfig{
    			LocationName: "example-location-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ServertrustgroupsServerTrustGroup } from "./local-sdk/azure/sdk/typescript/azure/data/sql/servertrustgroups_server_trust_group.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_sql_servertrustgroups_server_trust_group" });

      data(ServertrustgroupsServerTrustGroup, "example", {
        locationName: "example-location-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.sql import ServertrustgroupsServerTrustGroup, ServertrustgroupsServerTrustGroupConfig

    def describe():
        ubx.intent("look up azure_sql_servertrustgroups_server_trust_group")

        ubx.data(ServertrustgroupsServerTrustGroup, "example", ServertrustgroupsServerTrustGroupConfig(
            location_name="example-location-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="location_name" type="string" required>
  The real Azure region this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ServerTrustGroup items on this page Returned by the lookup.
</ResponseField>
