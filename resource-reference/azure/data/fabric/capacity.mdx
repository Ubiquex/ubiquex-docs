---
title: "azure_fabric_capacity"
description: "`azure_fabric_capacity` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_fabric_capacity` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/fabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_fabric_capacity"})

    		ubx.Data(fabric.Capacity, "example", fabric.CapacityConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Capacity } from "@ubx/sdk-azure/azure/data/fabric/capacity";

    export default stack("example", () => {
      intent({ summary: "look up azure_fabric_capacity" });

      data(Capacity, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.fabric import Capacity, CapacityConfig

    def describe():
        ubx.intent("look up azure_fabric_capacity")

        ubx.data(Capacity, "example", CapacityConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The FabricCapacity items on this page Returned by the lookup.
</ResponseField>
