---
title: "azure_fabric_rp_sku_enumeration_for_existing_resource_result"
description: "`azure_fabric_rp_sku_enumeration_for_existing_resource_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_fabric_rp_sku_enumeration_for_existing_resource_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/fabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_fabric_rp_sku_enumeration_for_existing_resource_result"})

    		ubx.Data(fabric.RpSkuEnumerationForExistingResourceResult, "example", fabric.RpSkuEnumerationForExistingResourceResultConfig{
    			CapacityName: "example-capacity-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RpSkuEnumerationForExistingResourceResult } from "@ubx/sdk-azure/azure/data/fabric/rp_sku_enumeration_for_existing_resource_result";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_fabric_rp_sku_enumeration_for_existing_resource_result",
      });

      data(RpSkuEnumerationForExistingResourceResult, "example", {
        capacityName: "example-capacity-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.fabric import RpSkuEnumerationForExistingResourceResult, RpSkuEnumerationForExistingResourceResultConfig

    def describe():
        ubx.intent("look up azure_fabric_rp_sku_enumeration_for_existing_resource_result")

        ubx.data(RpSkuEnumerationForExistingResourceResult, "example", RpSkuEnumerationForExistingResourceResultConfig(
            capacity_name="example-capacity-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="capacity_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Url for the next page. Null if no more pages available Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SKU details Returned by the lookup.
</ResponseField>
