---
title: "azure_fabric_rp_sku_details_for_new_resource"
description: "`azure_fabric_rp_sku_details_for_new_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_fabric_rp_sku_details_for_new_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/fabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_fabric_rp_sku_details_for_new_resource"})

    		ubx.Data(fabric.RpSkuDetailsForNewResource, "example", fabric.RpSkuDetailsForNewResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RpSkuDetailsForNewResource } from "@ubx/sdk-azure/azure/data/fabric/rp_sku_details_for_new_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_fabric_rp_sku_details_for_new_resource" });

      data(RpSkuDetailsForNewResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.fabric import RpSkuDetailsForNewResource, RpSkuDetailsForNewResourceConfig

    def describe():
        ubx.intent("look up azure_fabric_rp_sku_details_for_new_resource")

        ubx.data(RpSkuDetailsForNewResource, "example", RpSkuDetailsForNewResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Url for the next page. Null if no more pages available Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The collection of available SKUs for new resources Returned by the lookup.
</ResponseField>
