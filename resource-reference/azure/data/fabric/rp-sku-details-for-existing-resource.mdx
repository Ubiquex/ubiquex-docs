---
title: "azure_fabric_rp_sku_details_for_existing_resource"
description: "`azure_fabric_rp_sku_details_for_existing_resource` reads a real, available pricing tier (SKU) an existing Microsoft Fabric capacity can be resized to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	fabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/fabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_fabric_rp_sku_details_for_existing_resource"})

    		ubx.Data(fabric.RpSkuDetailsForExistingResource, "example", fabric.RpSkuDetailsForExistingResourceConfig{
    			CapacityName: "example-capacity-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { RpSkuDetailsForExistingResource } from "./local-sdk/azure/sdk/typescript/azure/data/fabric/rp_sku_details_for_existing_resource.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_fabric_rp_sku_details_for_existing_resource",
      });

      data(RpSkuDetailsForExistingResource, "example", {
        capacityName: "example-capacity-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.fabric import RpSkuDetailsForExistingResource, RpSkuDetailsForExistingResourceConfig

    def describe():
        ubx.intent("look up azure_fabric_rp_sku_details_for_existing_resource")

        ubx.data(RpSkuDetailsForExistingResource, "example", RpSkuDetailsForExistingResourceConfig(
            capacity_name="example-capacity-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="capacity_name" type="string" required>
  The real name of the capacity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Url for the next page. Null if no more pages available Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SKU details Returned by the lookup.
</ResponseField>
