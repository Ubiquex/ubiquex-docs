---
title: "azure_webpubsub_web_pub_sub_resource"
description: "`data_azure_webpubsub_web_pub_sub_resource` looks up a real, already-existing `azure_webpubsub_web_pub_sub_resource`."
---

It is a Web PubSub resource, a fully managed service for building real-time WebSocket-based applications (chat, notifications, live updates).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	webpubsub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/webpubsub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_webpubsub_web_pub_sub_resource"})

    		ubx.Data(webpubsub.WebPubSubResource, "example", webpubsub.WebPubSubResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WebPubSubResource } from "@ubx/sdk-azure/azure/data/webpubsub/web_pub_sub_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_webpubsub_web_pub_sub_resource" });

      data(WebPubSubResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.webpubsub import WebPubSubResource, WebPubSubResourceConfig

    def describe():
        ubx.intent("look up azure_webpubsub_web_pub_sub_resource")

        ubx.data(WebPubSubResource, "example", WebPubSubResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URL the client should use to fetch the next page (per server side paging). It's null for now, added for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of the resources Returned by the lookup.
</ResponseField>
