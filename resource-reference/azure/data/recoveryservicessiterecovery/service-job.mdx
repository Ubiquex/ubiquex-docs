---
title: "azure_recoveryservicessiterecovery_service_job"
description: "`data_azure_recoveryservicessiterecovery_service_job` looks up a real, already-existing `azure_recoveryservicessiterecovery_service_job`."
---

It tracks a single asynchronous Azure Site Recovery operation, e.g. an initial replication, a test failover, or a full failover.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_recoveryservicessiterecovery_service_job"})

    		ubx.Data(recoveryservicessiterecovery.ServiceJob, "example", recoveryservicessiterecovery.ServiceJobConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceJob } from "@ubx/sdk-azure/azure/data/recoveryservicessiterecovery/service_job";

    export default stack("example", () => {
      intent({ summary: "look up azure_recoveryservicessiterecovery_service_job" });

      data(ServiceJob, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.recoveryservicessiterecovery import ServiceJob, ServiceJobConfig

    def describe():
        ubx.intent("look up azure_recoveryservicessiterecovery_service_job")

        ubx.data(ServiceJob, "example", ServiceJobConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Job items on this page Returned by the lookup.
</ResponseField>
