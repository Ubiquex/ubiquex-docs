---
title: "azure_recoveryservicessiterecovery_service_alert"
description: "`azure_recoveryservicessiterecovery_service_alert` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_recoveryservicessiterecovery_service_alert` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_recoveryservicessiterecovery_service_alert"})

    		ubx.Data(recoveryservicessiterecovery.ServiceAlert, "example", recoveryservicessiterecovery.ServiceAlertConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceAlert } from "@ubx/sdk-azure/azure/data/recoveryservicessiterecovery/service_alert";

    export default stack("example", () => {
      intent({
        summary: "look up azure_recoveryservicessiterecovery_service_alert",
      });

      data(ServiceAlert, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.recoveryservicessiterecovery import ServiceAlert, ServiceAlertConfig

    def describe():
        ubx.intent("look up azure_recoveryservicessiterecovery_service_alert")

        ubx.data(ServiceAlert, "example", ServiceAlertConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Alert items on this page Returned by the lookup.
</ResponseField>
