---
title: "azure_recoveryservicessiterecovery_service_logical_network"
description: "`azure_recoveryservicessiterecovery_service_logical_network` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_recoveryservicessiterecovery_service_logical_network` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_recoveryservicessiterecovery_service_logical_network"})

    		ubx.Data(recoveryservicessiterecovery.ServiceLogicalNetwork, "example", recoveryservicessiterecovery.ServiceLogicalNetworkConfig{
    			FabricName:   "example-fabric-name",
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceLogicalNetwork } from "@ubx/sdk-azure/azure/data/recoveryservicessiterecovery/service_logical_network";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_recoveryservicessiterecovery_service_logical_network",
      });

      data(ServiceLogicalNetwork, "example", {
        fabricName: "example-fabric-name",
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.recoveryservicessiterecovery import ServiceLogicalNetwork, ServiceLogicalNetworkConfig

    def describe():
        ubx.intent("look up azure_recoveryservicessiterecovery_service_logical_network")

        ubx.data(ServiceLogicalNetwork, "example", ServiceLogicalNetworkConfig(
            fabric_name="example-fabric-name",
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fabric_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The LogicalNetwork items on this page Returned by the lookup.
</ResponseField>
