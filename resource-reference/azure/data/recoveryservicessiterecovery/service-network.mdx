---
title: "azure_recoveryservicessiterecovery_service_network"
description: "`azure_recoveryservicessiterecovery_service_network` looks up a real network (on-premises or Azure-side) registered with a Recovery Services vault for..."
---

`azure_recoveryservicessiterecovery_service_network` looks up a real network (on-premises or Azure-side) registered with a Recovery Services vault for Site Recovery network mapping.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_recoveryservicessiterecovery_service_network"})

    		ubx.Data(recoveryservicessiterecovery.ServiceNetwork, "example", recoveryservicessiterecovery.ServiceNetworkConfig{
    			FabricName:   "example-fabric-name",
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ServiceNetwork } from "./local-sdk/azure/sdk/typescript/azure/data/recoveryservicessiterecovery/service_network.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_recoveryservicessiterecovery_service_network",
      });

      data(ServiceNetwork, "example", {
        fabricName: "example-fabric-name",
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.recoveryservicessiterecovery import ServiceNetwork, ServiceNetworkConfig

    def describe():
        ubx.intent("look up azure_recoveryservicessiterecovery_service_network")

        ubx.data(ServiceNetwork, "example", ServiceNetworkConfig(
            fabric_name="example-fabric-name",
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fabric_name" type="string" required>
  The real name of the Site Recovery fabric (a registered on-premises or Azure site) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Network items on this page Returned by the lookup.
</ResponseField>
