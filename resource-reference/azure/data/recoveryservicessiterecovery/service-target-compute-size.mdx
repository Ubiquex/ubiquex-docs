---
title: "azure_recoveryservicessiterecovery_service_target_compute_size"
description: "`data_azure_recoveryservicessiterecovery_service_target_compute_size` is a real, live lookup against Microsoft Azure..."
---

`data_azure_recoveryservicessiterecovery_service_target_compute_size` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_recoveryservicessiterecovery_service_target_compute_size"})

    		ubx.Data(recoveryservicessiterecovery.ServiceTargetComputeSize, "example", recoveryservicessiterecovery.ServiceTargetComputeSizeConfig{
    			FabricName:                  "example-fabric-name",
    			ProtectionContainerName:     "example-protection-container-name",
    			ReplicatedProtectedItemName: "example-replicated-protected-item-name",
    			ResourceName:                "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceTargetComputeSize } from "@ubx/sdk-azure/azure/data/recoveryservicessiterecovery/service_target_compute_size";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_recoveryservicessiterecovery_service_target_compute_size",
      });

      data(ServiceTargetComputeSize, "example", {
        fabricName: "example-fabric-name",
        protectionContainerName: "example-protection-container-name",
        replicatedProtectedItemName: "example-replicated-protected-item-name",
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.recoveryservicessiterecovery import ServiceTargetComputeSize, ServiceTargetComputeSizeConfig

    def describe():
        ubx.intent("look up azure_recoveryservicessiterecovery_service_target_compute_size")

        ubx.data(ServiceTargetComputeSize, "example", ServiceTargetComputeSizeConfig(
            fabric_name="example-fabric-name",
            protection_container_name="example-protection-container-name",
            replicated_protected_item_name="example-replicated-protected-item-name",
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fabric_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="protection_container_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="replicated_protected_item_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The TargetComputeSize items on this page Returned by the lookup.
</ResponseField>
