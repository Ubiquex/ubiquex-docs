---
title: "azure_recoveryservicessiterecovery_service_supported_operating_systems"
description: "`azure_recoveryservicessiterecovery_service_supported_operating_systems` reads the real, supported guest operating systems for Azure Site Recovery..."
---

`azure_recoveryservicessiterecovery_service_supported_operating_systems` reads the real, supported guest operating systems for Azure Site Recovery replication.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicessiterecovery "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/recoveryservicessiterecovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_recoveryservicessiterecovery_service_supported_operating_systems"})

    		ubx.Data(recoveryservicessiterecovery.ServiceSupportedOperatingSystems, "example", recoveryservicessiterecovery.ServiceSupportedOperatingSystemsConfig{
    			ResourceName: "example-resource-name",
    			InstanceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceSupportedOperatingSystems } from "@ubx/sdk-azure/azure/data/recoveryservicessiterecovery/service_supported_operating_systems";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_recoveryservicessiterecovery_service_supported_operating_systems",
      });

      data(ServiceSupportedOperatingSystems, "example", {
        resourceName: "example-resource-name",
        instanceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.recoveryservicessiterecovery import ServiceSupportedOperatingSystems, ServiceSupportedOperatingSystemsConfig

    def describe():
        ubx.intent("look up azure_recoveryservicessiterecovery_service_supported_operating_systems")

        ubx.data(ServiceSupportedOperatingSystems, "example", ServiceSupportedOperatingSystemsConfig(
            resource_name="example-resource-name",
            instance_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_type" type="string">
  The real type of this instance. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="location" type="string">
  Resource Location Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  Supported operating systems properties. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="supported_os_list" type="list(object)">
      The supported operating systems property list. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
