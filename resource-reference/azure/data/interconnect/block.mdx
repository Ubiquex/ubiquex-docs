---
title: "azure_interconnect_block"
description: "`data_azure_interconnect_block` looks up a real, already-existing `azure_interconnect_block`."
---

It -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	interconnect "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/interconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_interconnect_block"})

    		ubx.Data(interconnect.Block, "example", interconnect.BlockConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Block } from "@ubx/sdk-azure/azure/data/interconnect/block";

    export default stack("example", () => {
      intent({ summary: "look up azure_interconnect_block" });

      data(Block, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.interconnect import Block, BlockConfig

    def describe():
        ubx.intent("look up azure_interconnect_block")

        ubx.data(Block, "example", BlockConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URI to fetch the next page of Interconnect Blocks. Call ListNext() with this URI to fetch the next page of Interconnect Blocks. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of Interconnect Blocks. Returned by the lookup.
</ResponseField>
