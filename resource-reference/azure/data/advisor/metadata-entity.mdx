---
title: "azure_advisor_metadata_entity"
description: "`azure_advisor_metadata_entity` reads real, published metadata describing an Azure Advisor recommendation category."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	advisor "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/advisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_advisor_metadata_entity"})

    		ubx.Data(advisor.MetadataEntity, "example", advisor.MetadataEntityConfig{
    			ApiVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MetadataEntity } from "./local-sdk/azure/sdk/typescript/azure/data/advisor/metadata_entity.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_advisor_metadata_entity" });

      data(MetadataEntity, "example", {
        apiVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.advisor import MetadataEntity, MetadataEntityConfig

    def describe():
        ubx.intent("look up azure_advisor_metadata_entity")

        ubx.data(MetadataEntity, "example", MetadataEntityConfig(
            api_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of metadata. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of metadata entities. Returned by the lookup.
</ResponseField>
