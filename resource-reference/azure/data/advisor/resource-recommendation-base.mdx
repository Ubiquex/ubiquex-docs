---
title: "azure_advisor_resource_recommendation_base"
description: "`azure_advisor_resource_recommendation_base` looks up a real Azure Advisor recommendation for a resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	advisor "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/advisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_advisor_resource_recommendation_base"})

    		ubx.Data(advisor.ResourceRecommendationBase, "example", advisor.ResourceRecommendationBaseConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceRecommendationBase } from "@ubx/sdk-azure/azure/data/advisor/resource_recommendation_base";

    export default stack("example", () => {
      intent({ summary: "look up azure_advisor_resource_recommendation_base" });

      data(ResourceRecommendationBase, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.advisor import ResourceRecommendationBase, ResourceRecommendationBaseConfig

    def describe():
        ubx.intent("look up azure_advisor_resource_recommendation_base")

        ubx.data(ResourceRecommendationBase, "example", ResourceRecommendationBaseConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of recommendations. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of recommendations. Returned by the lookup.
</ResponseField>
