---
title: "azure_advisor_score_entity"
description: "`azure_advisor_score_entity` reads the real, current Azure Advisor score (a 0-100 rating across cost, security, reliability, and more) for a subscription."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	advisor "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/advisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_advisor_score_entity"})

    		ubx.Data(advisor.ScoreEntity, "example", advisor.ScoreEntityConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ScoreEntity } from "@ubx/sdk-azure/azure/data/advisor/score_entity";

    export default stack("example", () => {
      intent({ summary: "look up azure_advisor_score_entity" });

      data(ScoreEntity, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.advisor import ScoreEntity, ScoreEntityConfig

    def describe():
        ubx.intent("look up azure_advisor_score_entity")

        ubx.data(ScoreEntity, "example", ScoreEntityConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The list of operations. Returned by the lookup.
</ResponseField>
