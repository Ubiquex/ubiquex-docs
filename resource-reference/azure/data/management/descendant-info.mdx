---
title: "azure_management_descendant_info"
description: "`azure_management_descendant_info` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_management_descendant_info` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	management "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/management"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_management_descendant_info"})

    		ubx.Data(management.DescendantInfo, "example", management.DescendantInfoConfig{
    			GroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DescendantInfo } from "@ubx/sdk-azure/azure/data/management/descendant_info";

    export default stack("example", () => {
      intent({ summary: "look up azure_management_descendant_info" });

      data(DescendantInfo, "example", {
        groupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.management import DescendantInfo, DescendantInfoConfig

    def describe():
        ubx.intent("look up azure_management_descendant_info")

        ubx.data(DescendantInfo, "example", DescendantInfoConfig(
            group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DescendantInfo items on this page Returned by the lookup.
</ResponseField>
