---
title: "azure_management_hierarchy_settings"
description: "`azure_management_hierarchy_settings` reads the real, tenant-wide management group hierarchy settings, such as the default and root management groups."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	management "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/management"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_management_hierarchy_settings"})

    		ubx.Data(management.HierarchySettings, "example", management.HierarchySettingsConfig{
    			GroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { HierarchySettings } from "./local-sdk/azure/sdk/typescript/azure/data/management/hierarchy_settings.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_management_hierarchy_settings" });

      data(HierarchySettings, "example", {
        groupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.management import HierarchySettings, HierarchySettingsConfig

    def describe():
        ubx.intent("look up azure_management_hierarchy_settings")

        ubx.data(HierarchySettings, "example", HierarchySettingsConfig(
            group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_id" type="string" required>
  The real, unique identifier of this group. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  The generic properties of hierarchy settings. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="default_management_group" type="string">
      Settings that sets the default Management Group under which new subscriptions get added in this tenant. For example, /providers/Microsoft.Management/managementGroups/defaultGroup Optional lookup argument.
    </ResponseField>
    <ResponseField name="require_authorization_for_group_creation" type="bool">
      Indicates whether RBAC access is required upon group creation under the root Management Group. If set to true, user will require Microsoft.Management/managementGroups/write action on the root Management Group scope in order to create new Groups directly under the root. This will prevent new users from creating new Management Groups, unless they are given access. Optional lookup argument.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The AAD Tenant ID associated with the hierarchy settings. For example, 00000000-0000-0000-0000-000000000000 Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
