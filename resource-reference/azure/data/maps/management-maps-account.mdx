---
title: "azure_maps_management_maps_account"
description: "`data_azure_maps_management_maps_account` looks up a real, already-existing `azure_maps_management_maps_account`."
---

It is an Azure Maps account, providing geospatial API(s) for maps, routing, geocoding, and traffic data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	maps "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/maps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_maps_management_maps_account"})

    		ubx.Data(maps.ManagementMapsAccount, "example", maps.ManagementMapsAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagementMapsAccount } from "@ubx/sdk-azure/azure/data/maps/management_maps_account";

    export default stack("example", () => {
      intent({ summary: "look up azure_maps_management_maps_account" });

      data(ManagementMapsAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.maps import ManagementMapsAccount, ManagementMapsAccountConfig

    def describe():
        ubx.intent("look up azure_maps_management_maps_account")

        ubx.data(ManagementMapsAccount, "example", ManagementMapsAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL client should use to fetch the next page (per server side paging). It's null for now, added for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  a Maps Account. Returned by the lookup.
</ResponseField>
