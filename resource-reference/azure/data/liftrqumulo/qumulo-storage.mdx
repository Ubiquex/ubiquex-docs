---
title: "azure_liftrqumulo_qumulo_storage"
description: "`azure_liftrqumulo_qumulo_storage` looks up a real Qumulo Scalable File Service resource, a managed, high-performance file storage service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	liftrqumulo "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/liftrqumulo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_liftrqumulo_qumulo_storage"})

    		ubx.Data(liftrqumulo.QumuloStorage, "example", liftrqumulo.QumuloStorageConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QumuloStorage } from "@ubx/sdk-azure/azure/data/liftrqumulo/qumulo_storage";

    export default stack("example", () => {
      intent({ summary: "look up azure_liftrqumulo_qumulo_storage" });

      data(QumuloStorage, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.liftrqumulo import QumuloStorage, QumuloStorageConfig

    def describe():
        ubx.intent("look up azure_liftrqumulo_qumulo_storage")

        ubx.data(QumuloStorage, "example", QumuloStorageConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
