---
title: "azure_kusto_cluster"
description: "`data_azure_kusto_cluster` looks up a real, already-existing `azure_kusto_cluster`."
---

It is an Azure Data Explorer (Kusto) cluster, the provisioned compute that database(s) are created within for high-performance log and telemetry analytics.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kusto "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/kusto"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_kusto_cluster"})

    		ubx.Data(kusto.Cluster, "example", kusto.ClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Cluster } from "@ubx/sdk-azure/azure/data/kusto/cluster";

    export default stack("example", () => {
      intent({ summary: "look up azure_kusto_cluster" });

      data(Cluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.kusto import Cluster, ClusterConfig

    def describe():
        ubx.intent("look up azure_kusto_cluster")

        ubx.data(Cluster, "example", ClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Link to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of Kusto clusters. Returned by the lookup.
</ResponseField>
