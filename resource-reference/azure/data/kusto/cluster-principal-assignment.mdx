---
title: "azure_kusto_cluster_principal_assignment"
description: "`azure_kusto_cluster_principal_assignment` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_kusto_cluster_principal_assignment` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kusto "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/kusto"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_kusto_cluster_principal_assignment"})

    		ubx.Data(kusto.ClusterPrincipalAssignment, "example", kusto.ClusterPrincipalAssignmentConfig{
    			ClusterName: "example-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClusterPrincipalAssignment } from "@ubx/sdk-azure/azure/data/kusto/cluster_principal_assignment";

    export default stack("example", () => {
      intent({ summary: "look up azure_kusto_cluster_principal_assignment" });

      data(ClusterPrincipalAssignment, "example", {
        clusterName: "example-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.kusto import ClusterPrincipalAssignment, ClusterPrincipalAssignmentConfig

    def describe():
        ubx.intent("look up azure_kusto_cluster_principal_assignment")

        ubx.data(ClusterPrincipalAssignment, "example", ClusterPrincipalAssignmentConfig(
            cluster_name="example-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of Kusto cluster principal assignments. Returned by the lookup.
</ResponseField>
