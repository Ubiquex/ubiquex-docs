---
title: "azure_kusto_sku_description"
description: "`azure_kusto_sku_description` reads real, available pricing tier (SKU) descriptions for an Azure Data Explorer cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kusto "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/kusto"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_kusto_sku_description"})

    		ubx.Data(kusto.SkuDescription, "example", kusto.SkuDescriptionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SkuDescription } from "@ubx/sdk-azure/azure/data/kusto/sku_description";

    export default stack("example", () => {
      intent({ summary: "look up azure_kusto_sku_description" });

      data(SkuDescription, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.kusto import SkuDescription, SkuDescriptionConfig

    def describe():
        ubx.intent("look up azure_kusto_sku_description")

        ubx.data(SkuDescription, "example", SkuDescriptionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Link to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  SKU descriptions Returned by the lookup.
</ResponseField>
