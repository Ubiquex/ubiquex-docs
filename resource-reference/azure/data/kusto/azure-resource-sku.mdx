---
title: "azure_kusto_azure_resource_sku"
description: "`azure_kusto_azure_resource_sku` reads a real, available pricing tier (SKU) for an Azure Data Explorer cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	kusto "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/kusto"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_kusto_azure_resource_sku"})

    		ubx.Data(kusto.AzureResourceSku, "example", kusto.AzureResourceSkuConfig{
    			ClusterName: "example-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { AzureResourceSku } from "./local-sdk/azure/sdk/typescript/azure/data/kusto/azure_resource_sku.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_kusto_azure_resource_sku" });

      data(AzureResourceSku, "example", {
        clusterName: "example-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.kusto import AzureResourceSku, AzureResourceSkuConfig

    def describe():
        ubx.intent("look up azure_kusto_azure_resource_sku")

        ubx.data(AzureResourceSku, "example", AzureResourceSkuConfig(
            cluster_name="example-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The collection of available SKUs for an existing resource. Returned by the lookup.
</ResponseField>
