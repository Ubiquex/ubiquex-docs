---
title: "azure_kusto_private_link_resource"
description: "`azure_kusto_private_link_resource` reads a real Private Link resource exposed by an Azure Data Explorer cluster, that a Private Endpoint can connect to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kusto "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/kusto"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_kusto_private_link_resource"})

    		ubx.Data(kusto.PrivateLinkResource, "example", kusto.PrivateLinkResourceConfig{
    			ClusterName: "example-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PrivateLinkResource } from "@ubx/sdk-azure/azure/data/kusto/private_link_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_kusto_private_link_resource" });

      data(PrivateLinkResource, "example", {
        clusterName: "example-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.kusto import PrivateLinkResource, PrivateLinkResourceConfig

    def describe():
        ubx.intent("look up azure_kusto_private_link_resource")

        ubx.data(PrivateLinkResource, "example", PrivateLinkResourceConfig(
            cluster_name="example-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of private link resources Returned by the lookup.
</ResponseField>
