---
title: "azure_datafactory_openapi_private_endpoint_connection_resource"
description: "`azure_datafactory_openapi_private_endpoint_connection_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_datafactory_openapi_private_endpoint_connection_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datafactory "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/datafactory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_datafactory_openapi_private_endpoint_connection_resource"})

    		ubx.Data(datafactory.OpenapiPrivateEndpointConnectionResource, "example", datafactory.OpenapiPrivateEndpointConnectionResourceConfig{
    			FactoryName: "example-factory-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiPrivateEndpointConnectionResource } from "@ubx/sdk-azure/azure/data/datafactory/openapi_private_endpoint_connection_resource";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_datafactory_openapi_private_endpoint_connection_resource",
      });

      data(OpenapiPrivateEndpointConnectionResource, "example", {
        factoryName: "example-factory-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.datafactory import OpenapiPrivateEndpointConnectionResource, OpenapiPrivateEndpointConnectionResourceConfig

    def describe():
        ubx.intent("look up azure_datafactory_openapi_private_endpoint_connection_resource")

        ubx.data(OpenapiPrivateEndpointConnectionResource, "example", OpenapiPrivateEndpointConnectionResourceConfig(
            factory_name="example-factory-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="factory_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrivateEndpointConnectionResource items on this page Returned by the lookup.
</ResponseField>
