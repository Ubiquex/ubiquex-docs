---
title: "azure_datafactory_openapi_factory"
description: "`azure_datafactory_openapi_factory` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_datafactory_openapi_factory` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datafactory "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/datafactory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_datafactory_openapi_factory"})

    		ubx.Data(datafactory.OpenapiFactory, "example", datafactory.OpenapiFactoryConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiFactory } from "@ubx/sdk-azure/azure/data/datafactory/openapi_factory";

    export default stack("example", () => {
      intent({ summary: "look up azure_datafactory_openapi_factory" });

      data(OpenapiFactory, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.datafactory import OpenapiFactory, OpenapiFactoryConfig

    def describe():
        ubx.intent("look up azure_datafactory_openapi_factory")

        ubx.data(OpenapiFactory, "example", OpenapiFactoryConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Factory items on this page Returned by the lookup.
</ResponseField>
