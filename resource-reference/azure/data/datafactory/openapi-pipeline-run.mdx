---
title: "azure_datafactory_openapi_pipeline_run"
description: "`azure_datafactory_openapi_pipeline_run` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_datafactory_openapi_pipeline_run` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datafactory "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/datafactory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_datafactory_openapi_pipeline_run"})

    		ubx.Data(datafactory.OpenapiPipelineRun, "example", datafactory.OpenapiPipelineRunConfig{
    			FactoryName: "example-factory-name",
    			RunId:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiPipelineRun } from "@ubx/sdk-azure/azure/data/datafactory/openapi_pipeline_run";

    export default stack("example", () => {
      intent({ summary: "look up azure_datafactory_openapi_pipeline_run" });

      data(OpenapiPipelineRun, "example", {
        factoryName: "example-factory-name",
        runId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.datafactory import OpenapiPipelineRun, OpenapiPipelineRunConfig

    def describe():
        ubx.intent("look up azure_datafactory_openapi_pipeline_run")

        ubx.data(OpenapiPipelineRun, "example", OpenapiPipelineRunConfig(
            factory_name="example-factory-name",
            run_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="factory_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="run_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="duration_in_ms" type="number">
  The duration of a pipeline run. Returned by the lookup.
</ResponseField>

<ResponseField name="invoked_by" type="object">
  Provides entity name and id that started the pipeline run. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      The ID of the entity that started the run. Returned by the lookup.
    </ResponseField>
    <ResponseField name="invoked_by_type" type="string">
      The type of the entity that started the run. Returned by the lookup.
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of the entity that started the pipeline run. Returned by the lookup.
    </ResponseField>
    <ResponseField name="pipeline_name" type="string">
      The name of the pipeline that triggered the run, if any. Returned by the lookup.
    </ResponseField>
    <ResponseField name="pipeline_run_id" type="string">
      The run id of the pipeline that triggered the run, if any. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="is_latest" type="bool">
  Indicates if the recovered pipeline run is the latest in its group. Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated" type="string">
  The last updated timestamp for the pipeline run event in ISO8601 format. Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  The message from a pipeline run. Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  The full or partial list of parameter name, value pair used in the pipeline run. Returned by the lookup.
</ResponseField>

<ResponseField name="pipeline_name" type="string">
  The pipeline name. Returned by the lookup.
</ResponseField>

<ResponseField name="run_dimensions" type="map(string)">
  Run dimensions emitted by Pipeline run. Returned by the lookup.
</ResponseField>

<ResponseField name="run_end" type="string">
  The end time of a pipeline run in ISO8601 format. Returned by the lookup.
</ResponseField>

<ResponseField name="run_group_id" type="string">
  Identifier that correlates all the recovery runs of a pipeline run. Returned by the lookup.
</ResponseField>

<ResponseField name="run_start" type="string">
  The start time of a pipeline run in ISO8601 format. Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The status of a pipeline run. Possible values: Queued, InProgress, Succeeded, Failed, Canceling, Cancelled Returned by the lookup.
</ResponseField>
