---
title: "azure_image"
description: "`data_azure_image` looks up a real, already-existing `azure_image`."
---

It defines a reusable, versioned VM image inside an Azure Compute Gallery, built from an existing VM, a VHD, a snapshot, a managed image, or another image version. An image definition describes what the image is (Windows or Linux, generalized or specialized, memory requirements); an image version is the actual buildable artifact you point a new VM at, and you can keep several versions of the same definition side by side, replicated to whichever regions you choose.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	image "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/image"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_image"})

    		ubx.Data(image.Image, "example", image.ImageConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Image } from "@ubx/sdk-azure/azure/data/image/image";

    export default stack("example", () => {
      intent({ summary: "look up azure_image" });

      data(Image, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.image import Image, ImageConfig

    def describe():
        ubx.intent("look up azure_image")

        ubx.data(Image, "example", ImageConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The uri to fetch the next page of Images. Call ListNext() with this to fetch the next page of Images. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of Images Returned by the lookup.
</ResponseField>
