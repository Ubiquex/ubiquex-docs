---
title: "azure_dynatrace_observability"
description: "`azure_dynatrace_observability` looks up a real Dynatrace resource -- the top-level integration connecting Azure telemetry to Dynatrace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dynatrace "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dynatrace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dynatrace_observability"})

    		ubx.Data(dynatrace.Observability, "example", dynatrace.ObservabilityConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Observability } from "@ubx/sdk-azure/azure/data/dynatrace/observability";

    export default stack("example", () => {
      intent({ summary: "look up azure_dynatrace_observability" });

      data(Observability, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.dynatrace import Observability, ObservabilityConfig

    def describe():
        ubx.intent("look up azure_dynatrace_observability")

        ubx.data(Observability, "example", ObservabilityConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
