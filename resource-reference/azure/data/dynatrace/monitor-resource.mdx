---
title: "azure_dynatrace_monitor_resource"
description: "`data_azure_dynatrace_monitor_resource` looks up a real, already-existing `azure_dynatrace_monitor_resource`."
---

It is a Dynatrace resource on Azure, the top-level integration that sends Azure telemetry to a linked Dynatrace environment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	dynatrace "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dynatrace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dynatrace_monitor_resource"})

    		ubx.Data(dynatrace.MonitorResource, "example", dynatrace.MonitorResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MonitorResource } from "./local-sdk/azure/sdk/typescript/azure/data/dynatrace/monitor_resource.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_dynatrace_monitor_resource" });

      data(MonitorResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.dynatrace import MonitorResource, MonitorResourceConfig

    def describe():
        ubx.intent("look up azure_dynatrace_monitor_resource")

        ubx.data(MonitorResource, "example", MonitorResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The MonitorResource items on this page Returned by the lookup.
</ResponseField>
