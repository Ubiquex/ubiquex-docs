---
title: "azure_dynatrace_single_sign_on_resource"
description: "`data_azure_dynatrace_single_sign_on_resource` looks up a real, already-existing `azure_dynatrace_single_sign_on_resource`."
---

It -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dynatrace "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dynatrace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dynatrace_single_sign_on_resource"})

    		ubx.Data(dynatrace.SingleSignOnResource, "example", dynatrace.SingleSignOnResourceConfig{
    			MonitorName: "example-monitor-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SingleSignOnResource } from "@ubx/sdk-azure/azure/data/dynatrace/single_sign_on_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_dynatrace_single_sign_on_resource" });

      data(SingleSignOnResource, "example", {
        monitorName: "example-monitor-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.dynatrace import SingleSignOnResource, SingleSignOnResourceConfig

    def describe():
        ubx.intent("look up azure_dynatrace_single_sign_on_resource")

        ubx.data(SingleSignOnResource, "example", SingleSignOnResourceConfig(
            monitor_name="example-monitor-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="monitor_name" type="string" required>
  The real name of the monitor this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DynatraceSingleSignOnResource items on this page Returned by the lookup.
</ResponseField>
