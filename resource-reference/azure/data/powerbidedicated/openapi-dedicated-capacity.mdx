---
title: "azure_powerbidedicated_openapi_dedicated_capacity"
description: "`azure_powerbidedicated_openapi_dedicated_capacity` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_powerbidedicated_openapi_dedicated_capacity` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	powerbidedicated "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/powerbidedicated"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_powerbidedicated_openapi_dedicated_capacity"})

    		ubx.Data(powerbidedicated.OpenapiDedicatedCapacity, "example", powerbidedicated.OpenapiDedicatedCapacityConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiDedicatedCapacity } from "@ubx/sdk-azure/azure/data/powerbidedicated/openapi_dedicated_capacity";

    export default stack("example", () => {
      intent({
        summary: "look up azure_powerbidedicated_openapi_dedicated_capacity",
      });

      data(OpenapiDedicatedCapacity, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.powerbidedicated import OpenapiDedicatedCapacity, OpenapiDedicatedCapacityConfig

    def describe():
        ubx.intent("look up azure_powerbidedicated_openapi_dedicated_capacity")

        ubx.data(OpenapiDedicatedCapacity, "example", OpenapiDedicatedCapacityConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  An array of Dedicated capacities resources. Returned by the lookup.
</ResponseField>
