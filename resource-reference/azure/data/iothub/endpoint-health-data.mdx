---
title: "azure_iothub_endpoint_health_data"
description: "`azure_iothub_endpoint_health_data` reads the real, current health status of a custom routing endpoint configured on an IoT Hub."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	iothub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/iothub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_iothub_endpoint_health_data"})

    		ubx.Data(iothub.EndpointHealthData, "example", iothub.EndpointHealthDataConfig{
    			ApiVersion:        "example",
    			IotHubName:        "example-iot-hub-name",
    			ResourceGroupName: "example-resource-group-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { EndpointHealthData } from "./local-sdk/azure/sdk/typescript/azure/data/iothub/endpoint_health_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_iothub_endpoint_health_data" });

      data(EndpointHealthData, "example", {
        apiVersion: "example",
        iotHubName: "example-iot-hub-name",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.iothub import EndpointHealthData, EndpointHealthDataConfig

    def describe():
        ubx.intent("look up azure_iothub_endpoint_health_data")

        ubx.data(EndpointHealthData, "example", EndpointHealthDataConfig(
            api_version="example",
            iot_hub_name="example-iot-hub-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="iot_hub_name" type="string" required>
  The real name of the IoT Hub this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to more results Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  JSON-serialized array of Endpoint health data Returned by the lookup.
</ResponseField>
