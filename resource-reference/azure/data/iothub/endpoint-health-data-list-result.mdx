---
title: "azure_iothub_endpoint_health_data_list_result"
description: "`azure_iothub_endpoint_health_data_list_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_iothub_endpoint_health_data_list_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iothub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/iothub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_iothub_endpoint_health_data_list_result"})

    		ubx.Data(iothub.EndpointHealthDataListResult, "example", iothub.EndpointHealthDataListResultConfig{
    			ApiVersion:        "example",
    			IotHubName:        "example-iot-hub-name",
    			ResourceGroupName: "example-resource-group-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EndpointHealthDataListResult } from "@ubx/sdk-azure/azure/data/iothub/endpoint_health_data_list_result";

    export default stack("example", () => {
      intent({ summary: "look up azure_iothub_endpoint_health_data_list_result" });

      data(EndpointHealthDataListResult, "example", {
        apiVersion: "example",
        iotHubName: "example-iot-hub-name",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.iothub import EndpointHealthDataListResult, EndpointHealthDataListResultConfig

    def describe():
        ubx.intent("look up azure_iothub_endpoint_health_data_list_result")

        ubx.data(EndpointHealthDataListResult, "example", EndpointHealthDataListResultConfig(
            api_version="example",
            iot_hub_name="example-iot-hub-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="iot_hub_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to more results Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  JSON-serialized array of Endpoint health data Returned by the lookup.
</ResponseField>
