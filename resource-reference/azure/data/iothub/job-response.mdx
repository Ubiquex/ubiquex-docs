---
title: "azure_iothub_job_response"
description: "`data_azure_iothub_job_response` looks up a real, already-existing `azure_iothub_job_response`."
---

It reports the status of a bulk IoT Hub job (e.g. a scheduled device twin update or direct method call across many devices).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	iothub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/iothub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_iothub_job_response"})

    		ubx.Data(iothub.JobResponse, "example", iothub.JobResponseConfig{
    			ApiVersion:        "example",
    			ResourceGroupName: "example-resource-group-name",
    			ResourceName:      "example-resource-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { JobResponse } from "./local-sdk/azure/sdk/typescript/azure/data/iothub/job_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_iothub_job_response" });

      data(JobResponse, "example", {
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        resourceName: "example-resource-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.iothub import JobResponse, JobResponseConfig

    def describe():
        ubx.intent("look up azure_iothub_job_response")

        ubx.data(JobResponse, "example", JobResponseConfig(
            api_version="example",
            resource_group_name="example-resource-group-name",
            resource_name="example-resource-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The next link. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The array of JobResponse objects. Returned by the lookup.
</ResponseField>
