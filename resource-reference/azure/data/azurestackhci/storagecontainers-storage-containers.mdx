---
title: "azure_azurestackhci_storagecontainers_storage_containers"
description: "`data_azure_azurestackhci_storagecontainers_storage_containers` looks up a real, already-existing..."
---

`data_azure_azurestackhci_storagecontainers_storage_containers` looks up a real, already-existing `azure_azurestackhci_storagecontainers_storage_containers`. It defines a storage location on an Azure Stack HCI cluster that VM disk(s) provisioned through Azure tooling are placed in.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_azurestackhci_storagecontainers_storage_containers"})

    		ubx.Data(azurestackhci.StoragecontainersStorageContainers, "example", azurestackhci.StoragecontainersStorageContainersConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StoragecontainersStorageContainers } from "@ubx/sdk-azure/azure/data/azurestackhci/storagecontainers_storage_containers";

    export default stack("example", () => {
      intent({
        summary: "look up azure_azurestackhci_storagecontainers_storage_containers",
      });

      data(StoragecontainersStorageContainers, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.azurestackhci import StoragecontainersStorageContainers, StoragecontainersStorageContainersConfig

    def describe():
        ubx.intent("look up azure_azurestackhci_storagecontainers_storage_containers")

        ubx.data(StoragecontainersStorageContainers, "example", StoragecontainersStorageContainersConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
