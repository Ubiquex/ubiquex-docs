---
title: "azure_azurestackhci_galleryimages_gallery_images"
description: "`data_azure_azurestackhci_galleryimages_gallery_images` looks up a real, already-existing `azure_azurestackhci_galleryimages_gallery_images`."
---

It registers a VM gallery image (an OS image) available for provisioning new VM(s) on an Azure Stack HCI cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_azurestackhci_galleryimages_gallery_images"})

    		ubx.Data(azurestackhci.GalleryimagesGalleryImages, "example", azurestackhci.GalleryimagesGalleryImagesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { GalleryimagesGalleryImages } from "./local-sdk/azure/sdk/typescript/azure/data/azurestackhci/galleryimages_gallery_images.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_azurestackhci_galleryimages_gallery_images",
      });

      data(GalleryimagesGalleryImages, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.azurestackhci import GalleryimagesGalleryImages, GalleryimagesGalleryImagesConfig

    def describe():
        ubx.intent("look up azure_azurestackhci_galleryimages_gallery_images")

        ubx.data(GalleryimagesGalleryImages, "example", GalleryimagesGalleryImagesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Link to the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
