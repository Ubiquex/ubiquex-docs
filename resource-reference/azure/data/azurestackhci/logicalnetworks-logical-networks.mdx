---
title: "azure_azurestackhci_logicalnetworks_logical_networks"
description: "`data_azure_azurestackhci_logicalnetworks_logical_networks` looks up a real, already-existing `azure_azurestackhci_logicalnetworks_logical_networks`."
---

It defines a logical network on Azure Arc-enabled Azure Stack HCI, that VM(s) provisioned through Azure tooling are attached to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_azurestackhci_logicalnetworks_logical_networks"})

    		ubx.Data(azurestackhci.LogicalnetworksLogicalNetworks, "example", azurestackhci.LogicalnetworksLogicalNetworksConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { LogicalnetworksLogicalNetworks } from "./local-sdk/azure/sdk/typescript/azure/data/azurestackhci/logicalnetworks_logical_networks.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_azurestackhci_logicalnetworks_logical_networks",
      });

      data(LogicalnetworksLogicalNetworks, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.azurestackhci import LogicalnetworksLogicalNetworks, LogicalnetworksLogicalNetworksConfig

    def describe():
        ubx.intent("look up azure_azurestackhci_logicalnetworks_logical_networks")

        ubx.data(LogicalnetworksLogicalNetworks, "example", LogicalnetworksLogicalNetworksConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
