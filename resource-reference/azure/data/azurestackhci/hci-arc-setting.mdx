---
title: "azure_azurestackhci_hci_arc_setting"
description: "`data_azure_azurestackhci_hci_arc_setting` looks up a real, already-existing `azure_azurestackhci_hci_arc_setting`."
---

It configures Azure Arc integration for an Azure Stack HCI cluster, enabling Arc extensions and hybrid management once registered.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_azurestackhci_hci_arc_setting"})

    		ubx.Data(azurestackhci.HciArcSetting, "example", azurestackhci.HciArcSettingConfig{
    			ClusterName: "example-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HciArcSetting } from "@ubx/sdk-azure/azure/data/azurestackhci/hci_arc_setting";

    export default stack("example", () => {
      intent({ summary: "look up azure_azurestackhci_hci_arc_setting" });

      data(HciArcSetting, "example", {
        clusterName: "example-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.azurestackhci import HciArcSetting, HciArcSettingConfig

    def describe():
        ubx.intent("look up azure_azurestackhci_hci_arc_setting")

        ubx.data(HciArcSetting, "example", HciArcSettingConfig(
            cluster_name="example-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ArcSetting items on this page Returned by the lookup.
</ResponseField>
