---
title: "azure_azurestackhci_hci_cluster"
description: "`data_azure_azurestackhci_hci_cluster` looks up a real, already-existing `azure_azurestackhci_hci_cluster`."
---

It represents an Azure Stack HCI cluster's own Azure resource, the Arc-registered projection of the on-premises hyperconverged cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_azurestackhci_hci_cluster"})

    		ubx.Data(azurestackhci.HciCluster, "example", azurestackhci.HciClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { HciCluster } from "./local-sdk/azure/sdk/typescript/azure/data/azurestackhci/hci_cluster.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_azurestackhci_hci_cluster" });

      data(HciCluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.azurestackhci import HciCluster, HciClusterConfig

    def describe():
        ubx.intent("look up azure_azurestackhci_hci_cluster")

        ubx.data(HciCluster, "example", HciClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Cluster items on this page Returned by the lookup.
</ResponseField>
