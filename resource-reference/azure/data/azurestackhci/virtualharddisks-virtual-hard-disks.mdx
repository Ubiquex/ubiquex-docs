---
title: "azure_azurestackhci_virtualharddisks_virtual_hard_disks"
description: "`data_azure_azurestackhci_virtualharddisks_virtual_hard_disks` looks up a real, already-existing `azure_azurestackhci_virtualharddisks_virtual_hard_disks`."
---

It is a virtual hard disk attached to a VM running on Azure Arc-enabled Azure Stack HCI.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_azurestackhci_virtualharddisks_virtual_hard_disks"})

    		ubx.Data(azurestackhci.VirtualharddisksVirtualHardDisks, "example", azurestackhci.VirtualharddisksVirtualHardDisksConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualharddisksVirtualHardDisks } from "@ubx/sdk-azure/azure/data/azurestackhci/virtualharddisks_virtual_hard_disks";

    export default stack("example", () => {
      intent({
        summary: "look up azure_azurestackhci_virtualharddisks_virtual_hard_disks",
      });

      data(VirtualharddisksVirtualHardDisks, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.azurestackhci import VirtualharddisksVirtualHardDisks, VirtualharddisksVirtualHardDisksConfig

    def describe():
        ubx.intent("look up azure_azurestackhci_virtualharddisks_virtual_hard_disks")

        ubx.data(VirtualharddisksVirtualHardDisks, "example", VirtualharddisksVirtualHardDisksConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
