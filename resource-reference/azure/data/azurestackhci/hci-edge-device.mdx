---
title: "azure_azurestackhci_hci_edge_device"
description: "`azure_azurestackhci_hci_edge_device` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_azurestackhci_hci_edge_device` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_azurestackhci_hci_edge_device"})

    		ubx.Data(azurestackhci.HciEdgeDevice, "example", azurestackhci.HciEdgeDeviceConfig{
    			ResourceUri: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HciEdgeDevice } from "@ubx/sdk-azure/azure/data/azurestackhci/hci_edge_device";

    export default stack("example", () => {
      intent({ summary: "look up azure_azurestackhci_hci_edge_device" });

      data(HciEdgeDevice, "example", {
        resourceUri: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.azurestackhci import HciEdgeDevice, HciEdgeDeviceConfig

    def describe():
        ubx.intent("look up azure_azurestackhci_hci_edge_device")

        ubx.data(HciEdgeDevice, "example", HciEdgeDeviceConfig(
            resource_uri="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_uri" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The EdgeDevice items on this page Returned by the lookup.
</ResponseField>
