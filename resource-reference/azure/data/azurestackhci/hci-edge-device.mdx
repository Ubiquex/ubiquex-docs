---
title: "azure_azurestackhci_hci_edge_device"
description: "`data_azure_azurestackhci_hci_edge_device` looks up a real, already-existing `azure_azurestackhci_hci_edge_device`."
---

It represents a single physical node's own Azure resource within an Azure Stack HCI deployment, prior to or during cluster formation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	azurestackhci "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/azurestackhci"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_azurestackhci_hci_edge_device"})

    		ubx.Data(azurestackhci.HciEdgeDevice, "example", azurestackhci.HciEdgeDeviceConfig{
    			ResourceUri: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { HciEdgeDevice } from "./local-sdk/azure/sdk/typescript/azure/data/azurestackhci/hci_edge_device.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_azurestackhci_hci_edge_device" });

      data(HciEdgeDevice, "example", {
        resourceUri: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.azurestackhci import HciEdgeDevice, HciEdgeDeviceConfig

    def describe():
        ubx.intent("look up azure_azurestackhci_hci_edge_device")

        ubx.data(HciEdgeDevice, "example", HciEdgeDeviceConfig(
            resource_uri="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_uri" type="string" required>
  The real, unique Azure resource ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The EdgeDevice items on this page Returned by the lookup.
</ResponseField>
