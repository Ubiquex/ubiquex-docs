---
title: "azure_resources_locks_management_lock_object"
description: "`data_azure_resources_locks_management_lock_object` looks up a real, already-existing `azure_resources_locks_management_lock_object`."
---

It prevents a resource, resource group, or subscription from being accidentally deleted or modified, requiring the lock to be explicitly removed first.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own locks management lock object"})

    		ubx.Resource(resources.LocksManagementLockObject, "example", resources.LocksManagementLockObjectConfig{
    			Properties: map[string]any{"level": "example"},
    			Name:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocksManagementLockObject } from "./local-sdk/azure/sdk/typescript/azure/resources/locks_management_lock_object.ts";

    export default stack("example", () => {
      intent({ summary: "example own locks management lock object" });

      resource(LocksManagementLockObject, "example", {
        properties: { level: "example" },
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.resources import LocksManagementLockObject, LocksManagementLockObjectConfig
    from ubx.azure.resources.locks_management_lock_object import LocksManagementLockObject_Properties

    def describe():
        ubx.intent("example own locks management lock object")

        ubx.resource(LocksManagementLockObject, "example", LocksManagementLockObjectConfig(
            properties=LocksManagementLockObject_Properties(level="example"),
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_locks_management_lock_object called
    "example", with properties a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of locks. Returned by the lookup.
</ResponseField>
