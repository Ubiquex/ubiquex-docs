---
title: "azure_resources_subscriptionfeatureregistration_subscription_feature_registration"
description: "`azure_resources_subscriptionfeatureregistration_subscription_feature_registration` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_resources_subscriptionfeatureregistration_subscription_feature_registration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resources_subscriptionfeatureregistration_subscription_feature_registration"})

    		ubx.Data(resources.SubscriptionfeatureregistrationSubscriptionFeatureRegistration, "example", resources.SubscriptionfeatureregistrationSubscriptionFeatureRegistrationConfig{
    			ApiVersion:        "example",
    			ProviderNamespace: "example",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SubscriptionfeatureregistrationSubscriptionFeatureRegistration } from "@ubx/sdk-azure/azure/data/resources/subscriptionfeatureregistration_subscription_feature_registration";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_resources_subscriptionfeatureregistration_subscription_feature_registration",
      });

      data(
        SubscriptionfeatureregistrationSubscriptionFeatureRegistration,
        "example",
        {
          apiVersion: "example",
          providerNamespace: "example",
          subscriptionId: "example",
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.resources import SubscriptionfeatureregistrationSubscriptionFeatureRegistration, SubscriptionfeatureregistrationSubscriptionFeatureRegistrationConfig

    def describe():
        ubx.intent("look up azure_resources_subscriptionfeatureregistration_subscription_feature_registration")

        ubx.data(SubscriptionfeatureregistrationSubscriptionFeatureRegistration, "example", SubscriptionfeatureregistrationSubscriptionFeatureRegistrationConfig(
            api_version="example",
            provider_namespace="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="provider_namespace" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of subscription feature registrations list. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of subscription feature registrations. Returned by the lookup.
</ResponseField>
