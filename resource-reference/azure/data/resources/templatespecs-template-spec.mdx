---
title: "azure_resources_templatespecs_template_spec"
description: "`azure_resources_templatespecs_template_spec` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_resources_templatespecs_template_spec` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resources_templatespecs_template_spec"})

    		ubx.Data(resources.TemplatespecsTemplateSpec, "example", resources.TemplatespecsTemplateSpecConfig{
    			ApiVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TemplatespecsTemplateSpec } from "@ubx/sdk-azure/azure/data/resources/templatespecs_template_spec";

    export default stack("example", () => {
      intent({ summary: "look up azure_resources_templatespecs_template_spec" });

      data(TemplatespecsTemplateSpec, "example", {
        apiVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.resources import TemplatespecsTemplateSpec, TemplatespecsTemplateSpecConfig

    def describe():
        ubx.intent("look up azure_resources_templatespecs_template_spec")

        ubx.data(TemplatespecsTemplateSpec, "example", TemplatespecsTemplateSpecConfig(
            api_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  An array of Template Specs. Returned by the lookup.
</ResponseField>
