---
title: "azure_resources_managedapplications_application_definition"
description: "`data_azure_resources_managedapplications_application_definition` looks up a real, already-existing..."
---

`data_azure_resources_managedapplications_application_definition` looks up a real, already-existing `azure_resources_managedapplications_application_definition`. It publishes a reusable Managed Application template to the Azure Marketplace or a private catalog, that customer(s) can deploy instances from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resources_managedapplications_application_definition"})

    		ubx.Data(resources.ManagedapplicationsApplicationDefinition, "example", resources.ManagedapplicationsApplicationDefinitionConfig{
    			ApiVersion:        "example",
    			ResourceGroupName: "example-resource-group-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedapplicationsApplicationDefinition } from "@ubx/sdk-azure/azure/data/resources/managedapplications_application_definition";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_resources_managedapplications_application_definition",
      });

      data(ManagedapplicationsApplicationDefinition, "example", {
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.resources import ManagedapplicationsApplicationDefinition, ManagedapplicationsApplicationDefinitionConfig

    def describe():
        ubx.intent("look up azure_resources_managedapplications_application_definition")

        ubx.data(ManagedapplicationsApplicationDefinition, "example", ManagedapplicationsApplicationDefinitionConfig(
            api_version="example",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The array of managed application definitions. Returned by the lookup.
</ResponseField>
