---
title: "azure_resources_deploymentstacks_deployment_stacks_what_if_result"
description: "`data_azure_resources_deploymentstacks_deployment_stacks_what_if_result` looks up a real, already-existing..."
---

`data_azure_resources_deploymentstacks_deployment_stacks_what_if_result` looks up a real, already-existing `azure_resources_deploymentstacks_deployment_stacks_what_if_result`. It previews the change(s) a Deployment Stack update would make before it's actually applied.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resources_deploymentstacks_deployment_stacks_what_if_result"})

    		ubx.Data(resources.DeploymentstacksDeploymentStacksWhatIfResult, "example", resources.DeploymentstacksDeploymentStacksWhatIfResultConfig{
    			ManagementGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { DeploymentstacksDeploymentStacksWhatIfResult } from "./local-sdk/azure/sdk/typescript/azure/data/resources/deploymentstacks_deployment_stacks_what_if_result.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_resources_deploymentstacks_deployment_stacks_what_if_result",
      });

      data(DeploymentstacksDeploymentStacksWhatIfResult, "example", {
        managementGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.resources import DeploymentstacksDeploymentStacksWhatIfResult, DeploymentstacksDeploymentStacksWhatIfResultConfig

    def describe():
        ubx.intent("look up azure_resources_deploymentstacks_deployment_stacks_what_if_result")

        ubx.data(DeploymentstacksDeploymentStacksWhatIfResult, "example", DeploymentstacksDeploymentStacksWhatIfResultConfig(
            management_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="management_group_id" type="string" required>
  The real, unique identifier of the management group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DeploymentStacksWhatIfResult items on this page Returned by the lookup.
</ResponseField>
