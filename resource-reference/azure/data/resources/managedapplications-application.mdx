---
title: "azure_resources_managedapplications_application"
description: "`data_azure_resources_managedapplications_application` looks up a real, already-existing `azure_resources_managedapplications_application`."
---

It is a deployed instance of a Managed Application, whose own underlying resources are managed by the publisher rather than the consuming customer.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managedapplications application"})

    		ubx.Resource(resources.ManagedapplicationsApplication, "example", resources.ManagedapplicationsApplicationConfig{
    			Kind:       "example",
    			Properties: map[string]any{"application_definition_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedapplicationsApplication } from "./local-sdk/azure/sdk/typescript/azure/resources/managedapplications_application.ts";

    export default stack("example", () => {
      intent({ summary: "example own managedapplications application" });

      resource(ManagedapplicationsApplication, "example", {
        kind: "example",
        properties: { applicationDefinitionId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.resources import ManagedapplicationsApplication, ManagedapplicationsApplicationConfig
    from ubx.azure.resources.managedapplications_application import ManagedapplicationsApplication_Properties

    def describe():
        ubx.intent("example own managedapplications application")

        ubx.resource(ManagedapplicationsApplication, "example", ManagedapplicationsApplicationConfig(
            kind="example",
            properties=ManagedapplicationsApplication_Properties(application_definition_id="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_managedapplications_application, with kind
    example, properties a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The array of managed applications. Returned by the lookup.
</ResponseField>
