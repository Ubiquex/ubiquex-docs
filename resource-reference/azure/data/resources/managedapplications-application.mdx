---
title: "azure_resources_managedapplications_application"
description: "`data_azure_resources_managedapplications_application` looks up a real Azure Managed Application -- a solution deployed and managed on the customer's..."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resources_managedapplications_application"})

    		ubx.Data(resources.ManagedapplicationsApplication, "example", resources.ManagedapplicationsApplicationConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedapplicationsApplication } from "@ubx/sdk-azure/azure/data/resources/managedapplications_application";

    export default stack("example", () => {
      intent({
        summary: "look up azure_resources_managedapplications_application",
      });

      data(ManagedapplicationsApplication, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.resources import ManagedapplicationsApplication, ManagedapplicationsApplicationConfig

    def describe():
        ubx.intent("look up azure_resources_managedapplications_application")

        ubx.data(ManagedapplicationsApplication, "example", ManagedapplicationsApplicationConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The array of managed applications. Returned by the lookup.
</ResponseField>
