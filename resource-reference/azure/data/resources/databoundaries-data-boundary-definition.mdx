---
title: "azure_resources_databoundaries_data_boundary_definition"
description: "`data_azure_resources_databoundaries_data_boundary_definition` looks up a real, already-existing `azure_resources_databoundaries_data_boundary_definition`."
---

It configures Azure Resource Manager to store and process a tenant's own customer data within a defined geographic data boundary (currently the EU Data Boundary), for data residency compliance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resources_databoundaries_data_boundary_definition"})

    		ubx.Data(resources.DataboundariesDataBoundaryDefinition, "example", resources.DataboundariesDataBoundaryDefinitionConfig{
    			Default: "example",
    			Scope:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { DataboundariesDataBoundaryDefinition } from "./local-sdk/azure/sdk/typescript/azure/data/resources/databoundaries_data_boundary_definition.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_resources_databoundaries_data_boundary_definition",
      });

      data(DataboundariesDataBoundaryDefinition, "example", {
        default: "example",
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.resources import DataboundariesDataBoundaryDefinition, DataboundariesDataBoundaryDefinitionConfig

    def describe():
        ubx.intent("look up azure_resources_databoundaries_data_boundary_definition")

        ubx.data(DataboundariesDataBoundaryDefinition, "example", DataboundariesDataBoundaryDefinitionConfig(
            default="example",
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="default" type="string" required>
  The real, default value for this setting. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real Azure Resource Manager scope (subscription, resource group, or resource) this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  Data boundary properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="data_boundary" type="string">
      The data boundary definition. Optional lookup argument.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Denotes the state of provisioning. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
