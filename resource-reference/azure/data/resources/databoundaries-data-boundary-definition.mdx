---
title: "azure_resources_databoundaries_data_boundary_definition"
description: "`azure_resources_databoundaries_data_boundary_definition` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_resources_databoundaries_data_boundary_definition` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resources_databoundaries_data_boundary_definition"})

    		ubx.Data(resources.DataboundariesDataBoundaryDefinition, "example", resources.DataboundariesDataBoundaryDefinitionConfig{
    			Default: "example",
    			Scope:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataboundariesDataBoundaryDefinition } from "@ubx/sdk-azure/azure/data/resources/databoundaries_data_boundary_definition";

    export default stack("example", () => {
      intent({
        summary: "look up azure_resources_databoundaries_data_boundary_definition",
      });

      data(DataboundariesDataBoundaryDefinition, "example", {
        default: "example",
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.resources import DataboundariesDataBoundaryDefinition, DataboundariesDataBoundaryDefinitionConfig

    def describe():
        ubx.intent("look up azure_resources_databoundaries_data_boundary_definition")

        ubx.data(DataboundariesDataBoundaryDefinition, "example", DataboundariesDataBoundaryDefinitionConfig(
            default="example",
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="default" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  Data boundary properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="data_boundary" type="string">
      The data boundary definition. Optional lookup argument.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Denotes the state of provisioning. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
