---
title: "azure_resources_provider_resource_type"
description: "`azure_resources_provider_resource_type` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_resources_provider_resource_type` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resources_provider_resource_type"})

    		ubx.Data(resources.ProviderResourceType, "example", resources.ProviderResourceTypeConfig{
    			ResourceProviderNamespace: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProviderResourceType } from "@ubx/sdk-azure/azure/data/resources/provider_resource_type";

    export default stack("example", () => {
      intent({ summary: "look up azure_resources_provider_resource_type" });

      data(ProviderResourceType, "example", {
        resourceProviderNamespace: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.resources import ProviderResourceType, ProviderResourceTypeConfig

    def describe():
        ubx.intent("look up azure_resources_provider_resource_type")

        ubx.data(ProviderResourceType, "example", ProviderResourceTypeConfig(
            resource_provider_namespace="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_provider_namespace" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="id" type="string">
  The provider ID. Returned by the lookup.
</ResponseField>

<ResponseField name="namespace" type="string">
  The namespace of the resource provider. Returned by the lookup.
</ResponseField>

<ResponseField name="provider_authorization_consent_state" type="string">
  The provider authorization consent state. Returned by the lookup.
</ResponseField>

<ResponseField name="registration_policy" type="string">
  The registration policy of the resource provider. Returned by the lookup.
</ResponseField>

<ResponseField name="registration_state" type="string">
  The registration state of the resource provider. Returned by the lookup.
</ResponseField>

<ResponseField name="resource_types" type="list(object)">
  The collection of provider resource types. Returned by the lookup.
</ResponseField>
