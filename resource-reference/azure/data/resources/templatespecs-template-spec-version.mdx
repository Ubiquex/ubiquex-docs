---
title: "azure_resources_templatespecs_template_spec_version"
description: "`data_azure_resources_templatespecs_template_spec_version` looks up a real, already-existing `azure_resources_templatespecs_template_spec_version`."
---

It is a specific, immutable version of a Template Spec's own template content.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own templatespecs template spec version"})

    		ubx.Resource(resources.TemplatespecsTemplateSpecVersion, "example", resources.TemplatespecsTemplateSpecVersionConfig{
    			Location:   "example",
    			Properties: map[string]any{"description": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TemplatespecsTemplateSpecVersion } from "./local-sdk/azure/sdk/typescript/azure/resources/templatespecs_template_spec_version.ts";

    export default stack("example", () => {
      intent({ summary: "example own templatespecs template spec version" });

      resource(TemplatespecsTemplateSpecVersion, "example", {
        location: "example",
        properties: { description: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.resources import TemplatespecsTemplateSpecVersion, TemplatespecsTemplateSpecVersionConfig
    from ubx.azure.resources.templatespecs_template_spec_version import TemplatespecsTemplateSpecVersion_Properties

    def describe():
        ubx.intent("example own templatespecs template spec version")

        ubx.resource(TemplatespecsTemplateSpecVersion, "example", TemplatespecsTemplateSpecVersionConfig(
            location="example",
            properties=TemplatespecsTemplateSpecVersion_Properties(description="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_templatespecs_template_spec_version, with
    location example, properties a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="template_spec_name" type="string" required>
  The real name of the ARM template spec this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  An array of Template Spec versions. Returned by the lookup.
</ResponseField>
