---
title: "azure_resources_deployments_deployment_extended"
description: "`data_azure_resources_deployments_deployment_extended` looks up a real, already-existing `azure_resources_deployments_deployment_extended`."
---

It is an ARM template or Bicep deployment, the record of applying a declarative infrastructure definition to a resource group, subscription, or higher scope.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own deployments deployment extended"})

    		ubx.Resource(resources.DeploymentsDeploymentExtended, "example", resources.DeploymentsDeploymentExtendedConfig{
    			Location:   "example",
    			Properties: map[string]any{"mode": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DeploymentsDeploymentExtended } from "./local-sdk/azure/sdk/typescript/azure/resources/deployments_deployment_extended.ts";

    export default stack("example", () => {
      intent({ summary: "example own deployments deployment extended" });

      resource(DeploymentsDeploymentExtended, "example", {
        location: "example",
        properties: { mode: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.resources import DeploymentsDeploymentExtended, DeploymentsDeploymentExtendedConfig
    from ubx.azure.resources.deployments_deployment_extended import DeploymentsDeploymentExtended_Properties

    def describe():
        ubx.intent("example own deployments deployment extended")

        ubx.resource(DeploymentsDeploymentExtended, "example", DeploymentsDeploymentExtendedConfig(
            location="example",
            properties=DeploymentsDeploymentExtended_Properties(mode="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_resources_deployments_deployment_extended, with
    location example, properties a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_id" type="string" required>
  The real, unique identifier of this group. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DeploymentExtended items on this page Returned by the lookup.
</ResponseField>
