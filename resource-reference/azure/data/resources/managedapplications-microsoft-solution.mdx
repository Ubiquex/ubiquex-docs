---
title: "azure_resources_managedapplications_microsoft_solution"
description: "`azure_resources_managedapplications_microsoft_solution` looks up a real Azure Managed Application -- a solution deployed and managed on the customer's..."
---

`azure_resources_managedapplications_microsoft_solution` looks up a real Azure Managed Application -- a solution deployed and managed on the customer's behalf by a publisher.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	resources "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/resources"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_resources_managedapplications_microsoft_solution"})

    		ubx.Data(resources.ManagedapplicationsMicrosoftSolution, "example", resources.ManagedapplicationsMicrosoftSolutionConfig{
    			ApiVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ManagedapplicationsMicrosoftSolution } from "./local-sdk/azure/sdk/typescript/azure/data/resources/managedapplications_microsoft_solution.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_resources_managedapplications_microsoft_solution",
      });

      data(ManagedapplicationsMicrosoftSolution, "example", {
        apiVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.resources import ManagedapplicationsMicrosoftSolution, ManagedapplicationsMicrosoftSolutionConfig

    def describe():
        ubx.intent("look up azure_resources_managedapplications_microsoft_solution")

        ubx.data(ManagedapplicationsMicrosoftSolution, "example", ManagedapplicationsMicrosoftSolutionConfig(
            api_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
