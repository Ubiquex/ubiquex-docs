---
title: "azure_machinelearningservices_openapi_model_container"
description: "`data_azure_machinelearningservices_openapi_model_container` looks up a real, already-existing `azure_machinelearningservices_openapi_model_container`."
---

It is the named container in an Azure Machine Learning workspace that version(s) of a registered model are grouped under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_machinelearningservices_openapi_model_container"})

    		ubx.Data(machinelearningservices.OpenapiModelContainer, "example", machinelearningservices.OpenapiModelContainerConfig{
    			RegistryName: "example-registry-name",
    			ListViewType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiModelContainer } from "@ubx/sdk-azure/azure/data/machinelearningservices/openapi_model_container";

    export default stack("example", () => {
      intent({
        summary: "look up azure_machinelearningservices_openapi_model_container",
      });

      data(OpenapiModelContainer, "example", {
        registryName: "example-registry-name",
        listViewType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.machinelearningservices import OpenapiModelContainer, OpenapiModelContainerConfig

    def describe():
        ubx.intent("look up azure_machinelearningservices_openapi_model_container")

        ubx.data(OpenapiModelContainer, "example", OpenapiModelContainerConfig(
            registry_name="example-registry-name",
            list_view_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="list_view_type" type="string">
  Which real subset of results to include, such as only enabled entries or all of them. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ModelContainer items on this page Returned by the lookup.
</ResponseField>
