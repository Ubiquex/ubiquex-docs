---
title: "azure_machinelearningservices_openapi_model_container"
description: "`data_azure_machinelearningservices_openapi_model_container` looks up a real, already-existing `azure_machinelearningservices_openapi_model_container`."
---

It is the named container in an Azure Machine Learning workspace that version(s) of a registered model are grouped under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi model container"})

    		ubx.Resource(machinelearningservices.OpenapiModelContainer, "example", machinelearningservices.OpenapiModelContainerConfig{
    			Properties: map[string]any{"provisioning_state": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiModelContainer } from "./local-sdk/azure/sdk/typescript/azure/machinelearningservices/openapi_model_container.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi model container" });

      resource(OpenapiModelContainer, "example", {
        properties: { provisioningState: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices import OpenapiModelContainer, OpenapiModelContainerConfig
    from ubx.azure.machinelearningservices.openapi_model_container import OpenapiModelContainer_Properties

    def describe():
        ubx.intent("example own openapi model container")

        ubx.resource(OpenapiModelContainer, "example", OpenapiModelContainerConfig(
            properties=OpenapiModelContainer_Properties(provisioning_state="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_machinelearningservices_openapi_model_container,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="list_view_type" type="string">
  Which real subset of results to include, such as only enabled entries or all of them. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ModelContainer items on this page Returned by the lookup.
</ResponseField>
