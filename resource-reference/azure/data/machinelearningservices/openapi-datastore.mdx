---
title: "azure_machinelearningservices_openapi_datastore"
description: "`data_azure_machinelearningservices_openapi_datastore` looks up a real, already-existing `azure_machinelearningservices_openapi_datastore`."
---

It registers a connection to an external storage account (Blob, Data Lake, or similar) with an Azure Machine Learning workspace, that dataset(s) are sourced from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_machinelearningservices_openapi_datastore"})

    		ubx.Data(machinelearningservices.OpenapiDatastore, "example", machinelearningservices.OpenapiDatastoreConfig{
    			WorkspaceName: "example-workspace-name",
    			Count:         1,
    			IsDefault:     true,
    			Names:         []string{"example"},
    			OrderBy:       "example",
    			OrderByAsc:    true,
    			SearchText:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiDatastore } from "@ubx/sdk-azure/azure/data/machinelearningservices/openapi_datastore";

    export default stack("example", () => {
      intent({
        summary: "look up azure_machinelearningservices_openapi_datastore",
      });

      data(OpenapiDatastore, "example", {
        workspaceName: "example-workspace-name",
        count: 1,
        isDefault: true,
        names: ["example"],
        orderBy: "example",
        orderByAsc: true,
        searchText: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.machinelearningservices import OpenapiDatastore, OpenapiDatastoreConfig

    def describe():
        ubx.intent("look up azure_machinelearningservices_openapi_datastore")

        ubx.data(OpenapiDatastore, "example", OpenapiDatastoreConfig(
            workspace_name="example-workspace-name",
            count=1,
            is_default=True,
            names=["example"],
            order_by="example",
            order_by_asc=True,
            search_text="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="count" type="number">
  The total number of matching results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Whether this object is the default one for its own scope. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="names" type="list(string)">
  The real, given names of the objects this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  The real field results are ordered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order_by_asc" type="bool">
  Whether results are ordered ascending by the given field. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="search_text" type="string">
  The real search query string. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Datastore items on this page Returned by the lookup.
</ResponseField>
