---
title: "azure_machinelearningservices_openapi_sku_resource"
description: "`azure_machinelearningservices_openapi_sku_resource` reads a real, available pricing tier (SKU) for an Azure Machine Learning workspace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_machinelearningservices_openapi_sku_resource"})

    		ubx.Data(machinelearningservices.OpenapiSkuResource, "example", machinelearningservices.OpenapiSkuResourceConfig{
    			DeploymentName: "example-deployment-name",
    			EndpointName:   "example-endpoint-name",
    			WorkspaceName:  "example-workspace-name",
    			Count:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiSkuResource } from "./local-sdk/azure/sdk/typescript/azure/data/machinelearningservices/openapi_sku_resource.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_machinelearningservices_openapi_sku_resource",
      });

      data(OpenapiSkuResource, "example", {
        deploymentName: "example-deployment-name",
        endpointName: "example-endpoint-name",
        workspaceName: "example-workspace-name",
        count: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.machinelearningservices import OpenapiSkuResource, OpenapiSkuResourceConfig

    def describe():
        ubx.intent("look up azure_machinelearningservices_openapi_sku_resource")

        ubx.data(OpenapiSkuResource, "example", OpenapiSkuResourceConfig(
            deployment_name="example-deployment-name",
            endpoint_name="example-endpoint-name",
            workspace_name="example-workspace-name",
            count=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="count" type="number">
  The total number of matching results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="deployment_name" type="string" required>
  The real name of the deployment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="endpoint_name" type="string" required>
  The real name of the endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SkuResource items on this page Returned by the lookup.
</ResponseField>
