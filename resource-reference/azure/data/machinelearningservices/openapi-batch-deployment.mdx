---
title: "azure_machinelearningservices_openapi_batch_deployment"
description: "`data_azure_machinelearningservices_openapi_batch_deployment` looks up a real, already-existing `azure_machinelearningservices_openapi_batch_deployment`."
---

It is a specific model deployment behind an Azure Machine Learning batch endpoint, processing large offline datasets asynchronously rather than serving real-time requests.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_machinelearningservices_openapi_batch_deployment"})

    		ubx.Data(machinelearningservices.OpenapiBatchDeployment, "example", machinelearningservices.OpenapiBatchDeploymentConfig{
    			EndpointName:  "example-endpoint-name",
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiBatchDeployment } from "@ubx/sdk-azure/azure/data/machinelearningservices/openapi_batch_deployment";

    export default stack("example", () => {
      intent({
        summary: "look up azure_machinelearningservices_openapi_batch_deployment",
      });

      data(OpenapiBatchDeployment, "example", {
        endpointName: "example-endpoint-name",
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.machinelearningservices import OpenapiBatchDeployment, OpenapiBatchDeploymentConfig

    def describe():
        ubx.intent("look up azure_machinelearningservices_openapi_batch_deployment")

        ubx.data(OpenapiBatchDeployment, "example", OpenapiBatchDeploymentConfig(
            endpoint_name="example-endpoint-name",
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="endpoint_name" type="string" required>
  The real name of the endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The BatchDeployment items on this page Returned by the lookup.
</ResponseField>
