---
title: "azure_machinelearningservices_openapi_online_deployment"
description: "`data_azure_machinelearningservices_openapi_online_deployment` looks up a real, already-existing `azure_machinelearningservices_openapi_online_deployment`."
---

It is a specific model deployment behind an Azure Machine Learning online endpoint, serving real-time inference requests.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_machinelearningservices_openapi_online_deployment"})

    		ubx.Data(machinelearningservices.OpenapiOnlineDeployment, "example", machinelearningservices.OpenapiOnlineDeploymentConfig{
    			EndpointName:  "example-endpoint-name",
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiOnlineDeployment } from "./local-sdk/azure/sdk/typescript/azure/data/machinelearningservices/openapi_online_deployment.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_machinelearningservices_openapi_online_deployment",
      });

      data(OpenapiOnlineDeployment, "example", {
        endpointName: "example-endpoint-name",
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.machinelearningservices import OpenapiOnlineDeployment, OpenapiOnlineDeploymentConfig

    def describe():
        ubx.intent("look up azure_machinelearningservices_openapi_online_deployment")

        ubx.data(OpenapiOnlineDeployment, "example", OpenapiOnlineDeploymentConfig(
            endpoint_name="example-endpoint-name",
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="endpoint_name" type="string" required>
  The real name of the endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The OnlineDeployment items on this page Returned by the lookup.
</ResponseField>
