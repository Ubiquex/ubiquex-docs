---
title: "azure_machinelearningservices_openapi_environment_version"
description: "`data_azure_machinelearningservices_openapi_environment_version` looks up a real, already-existing..."
---

`data_azure_machinelearningservices_openapi_environment_version` looks up a real, already-existing `azure_machinelearningservices_openapi_environment_version`. It is a specific, immutable version of a training or inference software environment (a container image or Conda spec) registered in an Azure Machine Learning workspace.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_machinelearningservices_openapi_environment_version"})

    		ubx.Data(machinelearningservices.OpenapiEnvironmentVersion, "example", machinelearningservices.OpenapiEnvironmentVersionConfig{
    			EnvironmentName: "example-environment-name",
    			RegistryName:    "example-registry-name",
    			ListViewType:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiEnvironmentVersion } from "./local-sdk/azure/sdk/typescript/azure/data/machinelearningservices/openapi_environment_version.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_machinelearningservices_openapi_environment_version",
      });

      data(OpenapiEnvironmentVersion, "example", {
        environmentName: "example-environment-name",
        registryName: "example-registry-name",
        listViewType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.machinelearningservices import OpenapiEnvironmentVersion, OpenapiEnvironmentVersionConfig

    def describe():
        ubx.intent("look up azure_machinelearningservices_openapi_environment_version")

        ubx.data(OpenapiEnvironmentVersion, "example", OpenapiEnvironmentVersionConfig(
            environment_name="example-environment-name",
            registry_name="example-registry-name",
            list_view_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment_name" type="string" required>
  The real name of the environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="list_view_type" type="string">
  Which real subset of results to include, such as only enabled entries or all of them. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The EnvironmentVersion items on this page Returned by the lookup.
</ResponseField>
