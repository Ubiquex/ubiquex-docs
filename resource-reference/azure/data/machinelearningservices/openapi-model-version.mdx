---
title: "azure_machinelearningservices_openapi_model_version"
description: "`azure_machinelearningservices_openapi_model_version` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_machinelearningservices_openapi_model_version` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_machinelearningservices_openapi_model_version"})

    		ubx.Data(machinelearningservices.OpenapiModelVersion, "example", machinelearningservices.OpenapiModelVersionConfig{
    			ModelName:    "example-model-name",
    			RegistryName: "example-registry-name",
    			Description:  "Managed by ubx.",
    			ListViewType: "example",
    			Properties:   "example",
    			Tags:         "example",
    			Version:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiModelVersion } from "@ubx/sdk-azure/azure/data/machinelearningservices/openapi_model_version";

    export default stack("example", () => {
      intent({
        summary: "look up azure_machinelearningservices_openapi_model_version",
      });

      data(OpenapiModelVersion, "example", {
        modelName: "example-model-name",
        registryName: "example-registry-name",
        description: "Managed by ubx.",
        listViewType: "example",
        properties: "example",
        tags: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.machinelearningservices import OpenapiModelVersion, OpenapiModelVersionConfig

    def describe():
        ubx.intent("look up azure_machinelearningservices_openapi_model_version")

        ubx.data(OpenapiModelVersion, "example", OpenapiModelVersionConfig(
            model_name="example-model-name",
            registry_name="example-registry-name",
            description="Managed by ubx.",
            list_view_type="example",
            properties="example",
            tags="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="description" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="list_view_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="model_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="properties" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="tags" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="version" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ModelVersion items on this page Returned by the lookup.
</ResponseField>
