---
title: "azure_machinelearningservices_openapi_model_version"
description: "`data_azure_machinelearningservices_openapi_model_version` looks up a real, already-existing `azure_machinelearningservices_openapi_model_version`."
---

It is a specific, immutable version of a model registered in an Azure Machine Learning workspace, deployable to an online or batch endpoint.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_machinelearningservices_openapi_model_version"})

    		ubx.Data(machinelearningservices.OpenapiModelVersion, "example", machinelearningservices.OpenapiModelVersionConfig{
    			ModelName:    "example-model-name",
    			RegistryName: "example-registry-name",
    			Description:  "Managed by ubx.",
    			ListViewType: "example",
    			Properties:   "example",
    			Tags:         "example",
    			Version:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiModelVersion } from "@ubx/sdk-azure/azure/data/machinelearningservices/openapi_model_version";

    export default stack("example", () => {
      intent({
        summary: "look up azure_machinelearningservices_openapi_model_version",
      });

      data(OpenapiModelVersion, "example", {
        modelName: "example-model-name",
        registryName: "example-registry-name",
        description: "Managed by ubx.",
        listViewType: "example",
        properties: "example",
        tags: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.machinelearningservices import OpenapiModelVersion, OpenapiModelVersionConfig

    def describe():
        ubx.intent("look up azure_machinelearningservices_openapi_model_version")

        ubx.data(OpenapiModelVersion, "example", OpenapiModelVersionConfig(
            model_name="example-model-name",
            registry_name="example-registry-name",
            description="Managed by ubx.",
            list_view_type="example",
            properties="example",
            tags="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="list_view_type" type="string">
  Which real subset of results to include, such as only enabled entries or all of them. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="model_name" type="string" required>
  The real name of the model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="properties" type="string">
  The real, structured properties of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="tags" type="string">
  The real tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="version" type="string">
  The real version identifier for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ModelVersion items on this page Returned by the lookup.
</ResponseField>
