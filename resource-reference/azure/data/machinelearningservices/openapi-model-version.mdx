---
title: "azure_machinelearningservices_openapi_model_version"
description: "`data_azure_machinelearningservices_openapi_model_version` looks up a real, already-existing `azure_machinelearningservices_openapi_model_version`."
---

It is a specific, immutable version of a model registered in an Azure Machine Learning workspace, deployable to an online or batch endpoint.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi model version"})

    		ubx.Resource(machinelearningservices.OpenapiModelVersion, "example", machinelearningservices.OpenapiModelVersionConfig{
    			Properties: map[string]any{"datasets": []map[string]any{{"name": "example-name"}}},
    			Version:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiModelVersion } from "./local-sdk/azure/sdk/typescript/azure/machinelearningservices/openapi_model_version.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi model version" });

      resource(OpenapiModelVersion, "example", {
        properties: { datasets: [{ name: "example-name" }] },
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices import OpenapiModelVersion, OpenapiModelVersionConfig
    from ubx.azure.machinelearningservices.openapi_model_version import OpenapiModelVersion_Properties, OpenapiModelVersion_Properties_Datasets

    def describe():
        ubx.intent("example own openapi model version")

        ubx.resource(OpenapiModelVersion, "example", OpenapiModelVersionConfig(
            properties=OpenapiModelVersion_Properties(datasets=[OpenapiModelVersion_Properties_Datasets(name="example-name")]),
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_machinelearningservices_openapi_model_version, with
    properties a set value, version example.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="list_view_type" type="string">
  Which real subset of results to include, such as only enabled entries or all of them. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="model_name" type="string" required>
  The real name of the model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="properties" type="string">
  The real, structured properties of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="tags" type="string">
  The real tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="version" type="string">
  The real version identifier for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ModelVersion items on this page Returned by the lookup.
</ResponseField>
