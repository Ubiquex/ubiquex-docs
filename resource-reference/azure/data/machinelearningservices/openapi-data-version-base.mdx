---
title: "azure_machinelearningservices_openapi_data_version_base"
description: "`data_azure_machinelearningservices_openapi_data_version_base` looks up a real, already-existing `azure_machinelearningservices_openapi_data_version_base`."
---

It is a specific, immutable version of a dataset registered in an Azure Machine Learning workspace.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi data version base"})

    		ubx.Resource(machinelearningservices.OpenapiDataVersionBase, "example", machinelearningservices.OpenapiDataVersionBaseConfig{
    			Name:       "example",
    			Properties: map[string]any{"data_type": "example", "data_uri": "example"},
    			Version:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiDataVersionBase } from "./local-sdk/azure/sdk/typescript/azure/machinelearningservices/openapi_data_version_base.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi data version base" });

      resource(OpenapiDataVersionBase, "example", {
        name: "example",
        properties: { dataType: "example", dataUri: "example" },
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices import OpenapiDataVersionBase, OpenapiDataVersionBaseConfig
    from ubx.azure.machinelearningservices.openapi_data_version_base import OpenapiDataVersionBase_Properties

    def describe():
        ubx.intent("example own openapi data version base")

        ubx.resource(OpenapiDataVersionBase, "example", OpenapiDataVersionBaseConfig(
            name="example",
            properties=OpenapiDataVersionBase_Properties(data_type="example", data_uri="example"),
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_machinelearningservices_openapi_data_version_base
    called "example", with properties a set value, version example.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="list_view_type" type="string">
  Which real subset of results to include, such as only enabled entries or all of them. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DataVersionBase items on this page Returned by the lookup.
</ResponseField>
