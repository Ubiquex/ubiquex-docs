---
title: "azure_machinelearningservices_openapi_code_version"
description: "`data_azure_machinelearningservices_openapi_code_version` looks up a real, already-existing `azure_machinelearningservices_openapi_code_version`."
---

It is a specific, immutable version of source code registered in an Azure Machine Learning workspace, referenced by job(s) and component(s) that use it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi code version"})

    		ubx.Resource(machinelearningservices.OpenapiCodeVersion, "example", machinelearningservices.OpenapiCodeVersionConfig{
    			Properties: map[string]any{"code_uri": "example"},
    			Version:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiCodeVersion } from "./local-sdk/azure/sdk/typescript/azure/machinelearningservices/openapi_code_version.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi code version" });

      resource(OpenapiCodeVersion, "example", {
        properties: { codeUri: "example" },
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices import OpenapiCodeVersion, OpenapiCodeVersionConfig
    from ubx.azure.machinelearningservices.openapi_code_version import OpenapiCodeVersion_Properties

    def describe():
        ubx.intent("example own openapi code version")

        ubx.resource(OpenapiCodeVersion, "example", OpenapiCodeVersionConfig(
            properties=OpenapiCodeVersion_Properties(code_uri="example"),
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_machinelearningservices_openapi_code_version, with
    properties a set value, version example.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="code_name" type="string" required>
  The real code name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The CodeVersion items on this page Returned by the lookup.
</ResponseField>
