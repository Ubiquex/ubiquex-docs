---
title: "azure_machinelearningservices_openapi_online_endpoint"
description: "`azure_machinelearningservices_openapi_online_endpoint` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_machinelearningservices_openapi_online_endpoint` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_machinelearningservices_openapi_online_endpoint"})

    		ubx.Data(machinelearningservices.OpenapiOnlineEndpoint, "example", machinelearningservices.OpenapiOnlineEndpointConfig{
    			WorkspaceName: "example-workspace-name",
    			Name:          "example",
    			ComputeType:   "example",
    			Count:         1,
    			OrderBy:       "example",
    			Properties:    "example",
    			Tags:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiOnlineEndpoint } from "@ubx/sdk-azure/azure/data/machinelearningservices/openapi_online_endpoint";

    export default stack("example", () => {
      intent({
        summary: "look up azure_machinelearningservices_openapi_online_endpoint",
      });

      data(OpenapiOnlineEndpoint, "example", {
        workspaceName: "example-workspace-name",
        name: "example",
        computeType: "example",
        count: 1,
        orderBy: "example",
        properties: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.machinelearningservices import OpenapiOnlineEndpoint, OpenapiOnlineEndpointConfig

    def describe():
        ubx.intent("look up azure_machinelearningservices_openapi_online_endpoint")

        ubx.data(OpenapiOnlineEndpoint, "example", OpenapiOnlineEndpointConfig(
            workspace_name="example-workspace-name",
            name="example",
            compute_type="example",
            count=1,
            order_by="example",
            properties="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="compute_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="count" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="properties" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="tags" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The OnlineEndpoint items on this page Returned by the lookup.
</ResponseField>
