---
title: "azure_machinelearningservices_openapi_component_version"
description: "`data_azure_machinelearningservices_openapi_component_version` looks up a real, already-existing `azure_machinelearningservices_openapi_component_version`."
---

It is a specific, immutable version of a reusable Azure Machine Learning pipeline component.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_machinelearningservices_openapi_component_version"})

    		ubx.Data(machinelearningservices.OpenapiComponentVersion, "example", machinelearningservices.OpenapiComponentVersionConfig{
    			ComponentName: "example-component-name",
    			RegistryName:  "example-registry-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiComponentVersion } from "./local-sdk/azure/sdk/typescript/azure/data/machinelearningservices/openapi_component_version.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_machinelearningservices_openapi_component_version",
      });

      data(OpenapiComponentVersion, "example", {
        componentName: "example-component-name",
        registryName: "example-registry-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.machinelearningservices import OpenapiComponentVersion, OpenapiComponentVersionConfig

    def describe():
        ubx.intent("look up azure_machinelearningservices_openapi_component_version")

        ubx.data(OpenapiComponentVersion, "example", OpenapiComponentVersionConfig(
            component_name="example-component-name",
            registry_name="example-registry-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="component_name" type="string" required>
  The real name of the component this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ComponentVersion items on this page Returned by the lookup.
</ResponseField>
