---
title: "azure_machinelearningservices_openapi_component_version"
description: "`data_azure_machinelearningservices_openapi_component_version` looks up a real, already-existing `azure_machinelearningservices_openapi_component_version`."
---

It is a specific, immutable version of a reusable Azure Machine Learning pipeline component.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi component version"})

    		ubx.Resource(machinelearningservices.OpenapiComponentVersion, "example", machinelearningservices.OpenapiComponentVersionConfig{
    			Properties: map[string]any{"component_spec": "example"},
    			Version:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiComponentVersion } from "./local-sdk/azure/sdk/typescript/azure/machinelearningservices/openapi_component_version.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi component version" });

      resource(OpenapiComponentVersion, "example", {
        properties: { componentSpec: "example" },
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices import OpenapiComponentVersion, OpenapiComponentVersionConfig
    from ubx.azure.machinelearningservices.openapi_component_version import OpenapiComponentVersion_Properties

    def describe():
        ubx.intent("example own openapi component version")

        ubx.resource(OpenapiComponentVersion, "example", OpenapiComponentVersionConfig(
            properties=OpenapiComponentVersion_Properties(component_spec="example"),
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_machinelearningservices_openapi_component_version,
    with properties a set value, version example.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="component_name" type="string" required>
  The real name of the component this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ComponentVersion items on this page Returned by the lookup.
</ResponseField>
