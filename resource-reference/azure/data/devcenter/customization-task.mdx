---
title: "azure_devcenter_customization_task"
description: "`azure_devcenter_customization_task` looks up a real, available customization task usable in a Dev Box or Deployment Environment configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devcenter_customization_task"})

    		ubx.Data(devcenter.CustomizationTask, "example", devcenter.CustomizationTaskConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomizationTask } from "@ubx/sdk-azure/azure/data/devcenter/customization_task";

    export default stack("example", () => {
      intent({ summary: "look up azure_devcenter_customization_task" });

      data(CustomizationTask, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.devcenter import CustomizationTask, CustomizationTaskConfig

    def describe():
        ubx.intent("look up azure_devcenter_customization_task")

        ubx.data(CustomizationTask, "example", CustomizationTaskConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Current page of results. Returned by the lookup.
</ResponseField>
