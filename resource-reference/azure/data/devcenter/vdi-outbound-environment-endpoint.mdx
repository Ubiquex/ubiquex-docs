---
title: "azure_devcenter_vdi_outbound_environment_endpoint"
description: "`azure_devcenter_vdi_outbound_environment_endpoint` reads a real, required outbound network endpoint for Dev Box connectivity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devcenter_vdi_outbound_environment_endpoint"})

    		ubx.Data(devcenter.VdiOutboundEnvironmentEndpoint, "example", devcenter.VdiOutboundEnvironmentEndpointConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VdiOutboundEnvironmentEndpoint } from "./local-sdk/azure/sdk/typescript/azure/data/devcenter/vdi_outbound_environment_endpoint.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_devcenter_vdi_outbound_environment_endpoint",
      });

      data(VdiOutboundEnvironmentEndpoint, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.devcenter import VdiOutboundEnvironmentEndpoint, VdiOutboundEnvironmentEndpointConfig

    def describe():
        ubx.intent("look up azure_devcenter_vdi_outbound_environment_endpoint")

        ubx.data(VdiOutboundEnvironmentEndpoint, "example", VdiOutboundEnvironmentEndpointConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The continuation token. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The collection of outbound network dependency endpoints returned by the listing operation. Returned by the lookup.
</ResponseField>
