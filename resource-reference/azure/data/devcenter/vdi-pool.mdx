---
title: "azure_devcenter_vdi_pool"
description: "`data_azure_devcenter_vdi_pool` looks up a real, already-existing `azure_devcenter_vdi_pool`."
---

It is a Dev Box pool, combining a dev box definition and network connection to define what developers in a project can self-service provision.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devcenter_vdi_pool"})

    		ubx.Data(devcenter.VdiPool, "example", devcenter.VdiPoolConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VdiPool } from "./local-sdk/azure/sdk/typescript/azure/data/devcenter/vdi_pool.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_devcenter_vdi_pool" });

      data(VdiPool, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.devcenter import VdiPool, VdiPoolConfig

    def describe():
        ubx.intent("look up azure_devcenter_vdi_pool")

        ubx.data(VdiPool, "example", VdiPoolConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Current page of results. Returned by the lookup.
</ResponseField>
