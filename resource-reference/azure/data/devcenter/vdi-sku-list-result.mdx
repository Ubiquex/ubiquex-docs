---
title: "azure_devcenter_vdi_sku_list_result"
description: "`azure_devcenter_vdi_sku_list_result` lists the real, available compute SKUs a Dev Box can be provisioned with."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devcenter_vdi_sku_list_result"})

    		ubx.Data(devcenter.VdiSkuListResult, "example", devcenter.VdiSkuListResultConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VdiSkuListResult } from "@ubx/sdk-azure/azure/data/devcenter/vdi_sku_list_result";

    export default stack("example", () => {
      intent({ summary: "look up azure_devcenter_vdi_sku_list_result" });

      data(VdiSkuListResult, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.devcenter import VdiSkuListResult, VdiSkuListResultConfig

    def describe():
        ubx.intent("look up azure_devcenter_vdi_sku_list_result")

        ubx.data(VdiSkuListResult, "example", VdiSkuListResultConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Current page of results. Returned by the lookup.
</ResponseField>
