---
title: "azure_devcenter_project"
description: "`data_azure_devcenter_project` looks up a real, already-existing `azure_devcenter_project`."
---

It scopes Microsoft Dev Box and Azure Deployment Environments access to a specific team, granting them self-service provisioning of dev box(es) and environment(s) from dev center-provided resources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devcenter_project"})

    		ubx.Data(devcenter.Project, "example", devcenter.ProjectConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Project } from "@ubx/sdk-azure/azure/data/devcenter/project";

    export default stack("example", () => {
      intent({ summary: "look up azure_devcenter_project" });

      data(Project, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.devcenter import Project, ProjectConfig

    def describe():
        ubx.intent("look up azure_devcenter_project")

        ubx.data(Project, "example", ProjectConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Current page of results. Returned by the lookup.
</ResponseField>
