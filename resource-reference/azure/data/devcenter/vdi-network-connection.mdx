---
title: "azure_devcenter_vdi_network_connection"
description: "`data_azure_devcenter_vdi_network_connection` looks up a real, already-existing `azure_devcenter_vdi_network_connection`."
---

It defines the virtual network a Microsoft Dev Box's own dev box definition is domain-joined and deployed into.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devcenter_vdi_network_connection"})

    		ubx.Data(devcenter.VdiNetworkConnection, "example", devcenter.VdiNetworkConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { VdiNetworkConnection } from "./local-sdk/azure/sdk/typescript/azure/data/devcenter/vdi_network_connection.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_devcenter_vdi_network_connection" });

      data(VdiNetworkConnection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.devcenter import VdiNetworkConnection, VdiNetworkConnectionConfig

    def describe():
        ubx.intent("look up azure_devcenter_vdi_network_connection")

        ubx.data(VdiNetworkConnection, "example", VdiNetworkConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Current page of results. Returned by the lookup.
</ResponseField>
