---
title: "azure_devcenter_microsoft_dev_center"
description: "`azure_devcenter_microsoft_dev_center` looks up a real Dev Center -- the top-level resource organizing projects, images..."
---

`azure_devcenter_microsoft_dev_center` looks up a real Dev Center -- the top-level resource organizing projects, images, and Dev Box definitions for a team.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devcenter_microsoft_dev_center"})

    		ubx.Data(devcenter.MicrosoftDevCenter, "example", devcenter.MicrosoftDevCenterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MicrosoftDevCenter } from "@ubx/sdk-azure/azure/data/devcenter/microsoft_dev_center";

    export default stack("example", () => {
      intent({ summary: "look up azure_devcenter_microsoft_dev_center" });

      data(MicrosoftDevCenter, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.devcenter import MicrosoftDevCenter, MicrosoftDevCenterConfig

    def describe():
        ubx.intent("look up azure_devcenter_microsoft_dev_center")

        ubx.data(MicrosoftDevCenter, "example", MicrosoftDevCenterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
