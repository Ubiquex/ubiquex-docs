---
title: "azure_type"
description: "`azure_type` reads the real, discriminating type of an Azure resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	type_ "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/type_"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_type"})

    		ubx.Data(type_.Type, "example", type_.TypeConfig{
    			PublisherName: "example-publisher-name",
    			Type:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Type } from "@ubx/sdk-azure/azure/data/type/type";

    export default stack("example", () => {
      intent({ summary: "look up azure_type" });

      data(Type, "example", {
        publisherName: "example-publisher-name",
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.type import Type, TypeConfig

    def describe():
        ubx.intent("look up azure_type")

        ubx.data(Type, "example", TypeConfig(
            publisher_name="example-publisher-name",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="publisher_name" type="string" required>
  The real name of the publisher this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string" required>
  The real, discriminating type of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
