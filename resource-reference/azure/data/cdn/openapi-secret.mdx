---
title: "azure_cdn_openapi_secret"
description: "`data_azure_cdn_openapi_secret` looks up a real, already-existing `azure_cdn_openapi_secret`."
---

It stores a TLS certificate or managed identity credential used by Azure Front Door for custom domain HTTPS or origin authentication.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_secret"})

    		ubx.Data(cdn.OpenapiSecret, "example", cdn.OpenapiSecretConfig{
    			ProfileName: "example-profile-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiSecret } from "./local-sdk/azure/sdk/typescript/azure/data/cdn/openapi_secret.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_cdn_openapi_secret" });

      data(OpenapiSecret, "example", {
        profileName: "example-profile-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiSecret, OpenapiSecretConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_secret")

        ubx.data(OpenapiSecret, "example", OpenapiSecretConfig(
            profile_name="example-profile-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="profile_name" type="string" required>
  The real name of the profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Secret items on this page Returned by the lookup.
</ResponseField>
