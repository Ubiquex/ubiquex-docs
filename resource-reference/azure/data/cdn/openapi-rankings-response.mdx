---
title: "azure_cdn_openapi_rankings_response"
description: "`azure_cdn_openapi_rankings_response` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cdn_openapi_rankings_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_rankings_response"})

    		ubx.Data(cdn.OpenapiRankingsResponse, "example", cdn.OpenapiRankingsResponseConfig{
    			DateTimeBegin: "example",
    			DateTimeEnd:   "example",
    			MaxRanking:    1,
    			Metrics:       []string{"example"},
    			ProfileName:   "example-profile-name",
    			Rankings:      []string{"example"},
    			CustomDomains: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiRankingsResponse } from "@ubx/sdk-azure/azure/data/cdn/openapi_rankings_response";

    export default stack("example", () => {
      intent({ summary: "look up azure_cdn_openapi_rankings_response" });

      data(OpenapiRankingsResponse, "example", {
        dateTimeBegin: "example",
        dateTimeEnd: "example",
        maxRanking: 1,
        metrics: ["example"],
        profileName: "example-profile-name",
        rankings: ["example"],
        customDomains: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiRankingsResponse, OpenapiRankingsResponseConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_rankings_response")

        ubx.data(OpenapiRankingsResponse, "example", OpenapiRankingsResponseConfig(
            date_time_begin="example",
            date_time_end="example",
            max_ranking=1,
            metrics=["example"],
            profile_name="example-profile-name",
            rankings=["example"],
            custom_domains=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="custom_domains" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="date_time_begin" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="date_time_end" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_ranking" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="rankings" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tables" type="list(object)">
  Returned by the lookup.
</ResponseField>
