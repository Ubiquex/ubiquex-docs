---
title: "azure_cdn_openapi_waf_metrics_response_series_item"
description: "`azure_cdn_openapi_waf_metrics_response_series_item` reads one real, reported WAF metric data series for an Azure Front Door profile."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_waf_metrics_response_series_item"})

    		ubx.Data(cdn.OpenapiWafMetricsResponseSeriesItem, "example", cdn.OpenapiWafMetricsResponseSeriesItemConfig{
    			DateTimeBegin: "example",
    			DateTimeEnd:   "example",
    			Granularity:   "example",
    			Metrics:       []string{"example"},
    			ProfileName:   "example-profile-name",
    			Actions:       []string{"example"},
    			GroupBy:       []string{"example"},
    			RuleTypes:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiWafMetricsResponseSeriesItem } from "@ubx/sdk-azure/azure/data/cdn/openapi_waf_metrics_response_series_item";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cdn_openapi_waf_metrics_response_series_item",
      });

      data(OpenapiWafMetricsResponseSeriesItem, "example", {
        dateTimeBegin: "example",
        dateTimeEnd: "example",
        granularity: "example",
        metrics: ["example"],
        profileName: "example-profile-name",
        actions: ["example"],
        groupBy: ["example"],
        ruleTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiWafMetricsResponseSeriesItem, OpenapiWafMetricsResponseSeriesItemConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_waf_metrics_response_series_item")

        ubx.data(OpenapiWafMetricsResponseSeriesItem, "example", OpenapiWafMetricsResponseSeriesItemConfig(
            date_time_begin="example",
            date_time_end="example",
            granularity="example",
            metrics=["example"],
            profile_name="example-profile-name",
            actions=["example"],
            group_by=["example"],
            rule_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="actions" type="list(string)">
  The real actions included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="date_time_begin" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="date_time_end" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="granularity" type="string" required>
  The real time granularity results are aggregated at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="group_by" type="list(string)">
  The real field results are grouped by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(string)" required>
  The real metric names this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  The real name of the profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="rule_types" type="list(string)">
  The real types of rules included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="series" type="list(object)">
  The real, individual data series returned in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
