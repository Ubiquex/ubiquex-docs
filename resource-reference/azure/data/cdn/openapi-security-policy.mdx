---
title: "azure_cdn_openapi_security_policy"
description: "`azure_cdn_openapi_security_policy` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cdn_openapi_security_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_security_policy"})

    		ubx.Data(cdn.OpenapiSecurityPolicy, "example", cdn.OpenapiSecurityPolicyConfig{
    			ProfileName: "example-profile-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiSecurityPolicy } from "@ubx/sdk-azure/azure/data/cdn/openapi_security_policy";

    export default stack("example", () => {
      intent({ summary: "look up azure_cdn_openapi_security_policy" });

      data(OpenapiSecurityPolicy, "example", {
        profileName: "example-profile-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiSecurityPolicy, OpenapiSecurityPolicyConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_security_policy")

        ubx.data(OpenapiSecurityPolicy, "example", OpenapiSecurityPolicyConfig(
            profile_name="example-profile-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="profile_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SecurityPolicy items on this page Returned by the lookup.
</ResponseField>
