---
title: "azure_cdn_openapi_continents_response"
description: "`azure_cdn_openapi_continents_response` reads Azure CDN's own real, published geographic continent/country data used for traffic analytics."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_continents_response"})

    		ubx.Data(cdn.OpenapiContinentsResponse, "example", cdn.OpenapiContinentsResponseConfig{
    			ProfileName: "example-profile-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiContinentsResponse } from "@ubx/sdk-azure/azure/data/cdn/openapi_continents_response";

    export default stack("example", () => {
      intent({ summary: "look up azure_cdn_openapi_continents_response" });

      data(OpenapiContinentsResponse, "example", {
        profileName: "example-profile-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiContinentsResponse, OpenapiContinentsResponseConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_continents_response")

        ubx.data(OpenapiContinentsResponse, "example", OpenapiContinentsResponseConfig(
            profile_name="example-profile-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="profile_name" type="string" required>
  The real name of the profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="continents" type="list(object)">
  The real geographic continents included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="country_or_regions" type="list(object)">
  The real countries or regions to restrict this result to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
