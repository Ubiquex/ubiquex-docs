---
title: "azure_cdn_openapi_rankings_response_tables_item"
description: "`azure_cdn_openapi_rankings_response_tables_item` reads one real, ranked table of traffic analytics (such as top URLs) for an Azure CDN or Front Door..."
---

`azure_cdn_openapi_rankings_response_tables_item` reads one real, ranked table of traffic analytics (such as top URLs) for an Azure CDN or Front Door profile.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_rankings_response_tables_item"})

    		ubx.Data(cdn.OpenapiRankingsResponseTablesItem, "example", cdn.OpenapiRankingsResponseTablesItemConfig{
    			DateTimeBegin: "example",
    			DateTimeEnd:   "example",
    			MaxRanking:    1,
    			Metrics:       []string{"example"},
    			ProfileName:   "example-profile-name",
    			Rankings:      []string{"example"},
    			CustomDomains: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiRankingsResponseTablesItem } from "./local-sdk/azure/sdk/typescript/azure/data/cdn/openapi_rankings_response_tables_item.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cdn_openapi_rankings_response_tables_item",
      });

      data(OpenapiRankingsResponseTablesItem, "example", {
        dateTimeBegin: "example",
        dateTimeEnd: "example",
        maxRanking: 1,
        metrics: ["example"],
        profileName: "example-profile-name",
        rankings: ["example"],
        customDomains: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiRankingsResponseTablesItem, OpenapiRankingsResponseTablesItemConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_rankings_response_tables_item")

        ubx.data(OpenapiRankingsResponseTablesItem, "example", OpenapiRankingsResponseTablesItemConfig(
            date_time_begin="example",
            date_time_end="example",
            max_ranking=1,
            metrics=["example"],
            profile_name="example-profile-name",
            rankings=["example"],
            custom_domains=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="custom_domains" type="list(string)">
  The real custom domains bound to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="date_time_begin" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="date_time_end" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_ranking" type="number" required>
  The maximum real rank position to include in the results. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(string)" required>
  The real metric names this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  The real name of the profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="rankings" type="list(string)" required>
  The real, ranked entries in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tables" type="list(object)">
  The real tables included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
