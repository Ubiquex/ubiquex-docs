---
title: "azure_cdn_openapi_profile"
description: "`data_azure_cdn_openapi_profile` looks up a real, already-existing `azure_cdn_openapi_profile`."
---

It is the top-level Azure CDN or Front Door resource that endpoint(s), origin group(s), and their own configuration are created within.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_profile"})

    		ubx.Data(cdn.OpenapiProfile, "example", cdn.OpenapiProfileConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiProfile } from "@ubx/sdk-azure/azure/data/cdn/openapi_profile";

    export default stack("example", () => {
      intent({ summary: "look up azure_cdn_openapi_profile" });

      data(OpenapiProfile, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiProfile, OpenapiProfileConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_profile")

        ubx.data(OpenapiProfile, "example", OpenapiProfileConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Profile items on this page Returned by the lookup.
</ResponseField>
