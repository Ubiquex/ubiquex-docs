---
title: "azure_cdn_openapi_origin_group"
description: "`data_azure_cdn_openapi_origin_group` looks up a real, already-existing `azure_cdn_openapi_origin_group`."
---

It groups related origin(s) together in a classic Azure CDN profile, providing failover across them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_origin_group"})

    		ubx.Data(cdn.OpenapiOriginGroup, "example", cdn.OpenapiOriginGroupConfig{
    			EndpointName: "example-endpoint-name",
    			ProfileName:  "example-profile-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiOriginGroup } from "@ubx/sdk-azure/azure/data/cdn/openapi_origin_group";

    export default stack("example", () => {
      intent({ summary: "look up azure_cdn_openapi_origin_group" });

      data(OpenapiOriginGroup, "example", {
        endpointName: "example-endpoint-name",
        profileName: "example-profile-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiOriginGroup, OpenapiOriginGroupConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_origin_group")

        ubx.data(OpenapiOriginGroup, "example", OpenapiOriginGroupConfig(
            endpoint_name="example-endpoint-name",
            profile_name="example-profile-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="endpoint_name" type="string" required>
  The real name of the endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  The real name of the profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The OriginGroup items on this page Returned by the lookup.
</ResponseField>
