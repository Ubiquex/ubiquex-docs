---
title: "azure_cdn_openapi_resources_response"
description: "`azure_cdn_openapi_resources_response` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cdn_openapi_resources_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_resources_response"})

    		ubx.Data(cdn.OpenapiResourcesResponse, "example", cdn.OpenapiResourcesResponseConfig{
    			ProfileName: "example-profile-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiResourcesResponse } from "@ubx/sdk-azure/azure/data/cdn/openapi_resources_response";

    export default stack("example", () => {
      intent({ summary: "look up azure_cdn_openapi_resources_response" });

      data(OpenapiResourcesResponse, "example", {
        profileName: "example-profile-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiResourcesResponse, OpenapiResourcesResponseConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_resources_response")

        ubx.data(OpenapiResourcesResponse, "example", OpenapiResourcesResponseConfig(
            profile_name="example-profile-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="profile_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="custom_domains" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="endpoints" type="list(object)">
  Returned by the lookup.
</ResponseField>
