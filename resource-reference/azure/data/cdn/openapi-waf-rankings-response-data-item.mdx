---
title: "azure_cdn_openapi_waf_rankings_response_data_item"
description: "`azure_cdn_openapi_waf_rankings_response_data_item` reads one real, ranked WAF analytics entry (such as top blocked rules) for an Azure Front Door profile."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_waf_rankings_response_data_item"})

    		ubx.Data(cdn.OpenapiWafRankingsResponseDataItem, "example", cdn.OpenapiWafRankingsResponseDataItemConfig{
    			DateTimeBegin: "example",
    			DateTimeEnd:   "example",
    			MaxRanking:    1,
    			Metrics:       []string{"example"},
    			ProfileName:   "example-profile-name",
    			Rankings:      []string{"example"},
    			Actions:       []string{"example"},
    			RuleTypes:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiWafRankingsResponseDataItem } from "@ubx/sdk-azure/azure/data/cdn/openapi_waf_rankings_response_data_item";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cdn_openapi_waf_rankings_response_data_item",
      });

      data(OpenapiWafRankingsResponseDataItem, "example", {
        dateTimeBegin: "example",
        dateTimeEnd: "example",
        maxRanking: 1,
        metrics: ["example"],
        profileName: "example-profile-name",
        rankings: ["example"],
        actions: ["example"],
        ruleTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiWafRankingsResponseDataItem, OpenapiWafRankingsResponseDataItemConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_waf_rankings_response_data_item")

        ubx.data(OpenapiWafRankingsResponseDataItem, "example", OpenapiWafRankingsResponseDataItemConfig(
            date_time_begin="example",
            date_time_end="example",
            max_ranking=1,
            metrics=["example"],
            profile_name="example-profile-name",
            rankings=["example"],
            actions=["example"],
            rule_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="actions" type="list(string)">
  The real actions included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="date_time_begin" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="date_time_end" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_ranking" type="number" required>
  The maximum real rank position to include in the results. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(string)" required>
  The real metric names this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  The real name of the profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="rankings" type="list(string)" required>
  The real, ranked entries in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="rule_types" type="list(string)">
  The real types of rules included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="groups" type="list(string)">
  The real groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
