---
title: "azure_cdn_openapi_metrics_response_series_item"
description: "`azure_cdn_openapi_metrics_response_series_item` reads one real, reported metric data series for an Azure CDN or Front Door profile."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	cdn "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cdn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cdn_openapi_metrics_response_series_item"})

    		ubx.Data(cdn.OpenapiMetricsResponseSeriesItem, "example", cdn.OpenapiMetricsResponseSeriesItemConfig{
    			CustomDomains: []string{"example"},
    			DateTimeBegin: "example",
    			DateTimeEnd:   "example",
    			Granularity:   "example",
    			Metrics:       []string{"example"},
    			ProfileName:   "example-profile-name",
    			Protocols:     []string{"example"},
    			Continents:    []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiMetricsResponseSeriesItem } from "./local-sdk/azure/sdk/typescript/azure/data/cdn/openapi_metrics_response_series_item.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_cdn_openapi_metrics_response_series_item" });

      data(OpenapiMetricsResponseSeriesItem, "example", {
        customDomains: ["example"],
        dateTimeBegin: "example",
        dateTimeEnd: "example",
        granularity: "example",
        metrics: ["example"],
        profileName: "example-profile-name",
        protocols: ["example"],
        continents: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.cdn import OpenapiMetricsResponseSeriesItem, OpenapiMetricsResponseSeriesItemConfig

    def describe():
        ubx.intent("look up azure_cdn_openapi_metrics_response_series_item")

        ubx.data(OpenapiMetricsResponseSeriesItem, "example", OpenapiMetricsResponseSeriesItemConfig(
            custom_domains=["example"],
            date_time_begin="example",
            date_time_end="example",
            granularity="example",
            metrics=["example"],
            profile_name="example-profile-name",
            protocols=["example"],
            continents=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="continents" type="list(string)">
  The real geographic continents included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="country_or_regions" type="list(string)">
  The real countries or regions to restrict this result to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="custom_domains" type="list(string)" required>
  The real custom domains bound to this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="date_time_begin" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="date_time_end" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="granularity" type="string" required>
  The real time granularity results are aggregated at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="group_by" type="list(string)">
  The real field results are grouped by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(string)" required>
  The real metric names this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  The real name of the profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="protocols" type="list(string)" required>
  The real network protocols this object applies to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="series" type="list(object)">
  The real, individual data series returned in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
