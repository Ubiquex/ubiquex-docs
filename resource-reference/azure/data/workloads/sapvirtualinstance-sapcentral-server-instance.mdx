---
title: "azure_workloads_sapvirtualinstance_sapcentral_server_instance"
description: "`data_azure_workloads_sapvirtualinstance_sapcentral_server_instance` looks up a real, already-existing..."
---

`data_azure_workloads_sapvirtualinstance_sapcentral_server_instance` looks up a real, already-existing `azure_workloads_sapvirtualinstance_sapcentral_server_instance`. It represents an SAP central services instance (ASCS/SCS) managed by Azure Center for SAP solutions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	workloads "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/workloads"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_workloads_sapvirtualinstance_sapcentral_server_instance"})

    		ubx.Data(workloads.SapvirtualinstanceSapcentralServerInstance, "example", workloads.SapvirtualinstanceSapcentralServerInstanceConfig{
    			SapVirtualInstanceName: "example-sap-virtual-instance-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { SapvirtualinstanceSapcentralServerInstance } from "./local-sdk/azure/sdk/typescript/azure/data/workloads/sapvirtualinstance_sapcentral_server_instance.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_workloads_sapvirtualinstance_sapcentral_server_instance",
      });

      data(SapvirtualinstanceSapcentralServerInstance, "example", {
        sapVirtualInstanceName: "example-sap-virtual-instance-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.workloads import SapvirtualinstanceSapcentralServerInstance, SapvirtualinstanceSapcentralServerInstanceConfig

    def describe():
        ubx.intent("look up azure_workloads_sapvirtualinstance_sapcentral_server_instance")

        ubx.data(SapvirtualinstanceSapcentralServerInstance, "example", SapvirtualinstanceSapcentralServerInstanceConfig(
            sap_virtual_instance_name="example-sap-virtual-instance-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sap_virtual_instance_name" type="string" required>
  The real name of the SAP virtual instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Gets the value of next link. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Gets the list of SAP central services instance resources. Returned by the lookup.
</ResponseField>
