---
title: "azure_workloads_sapvirtualinstance_sapvirtual_instance"
description: "`data_azure_workloads_sapvirtualinstance_sapvirtual_instance` looks up a real, already-existing `azure_workloads_sapvirtualinstance_sapvirtual_instance`."
---

It is the top-level Azure Center for SAP solutions resource representing a complete SAP system, grouping its own central services, application server, and database instance(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workloads "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/workloads"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_workloads_sapvirtualinstance_sapvirtual_instance"})

    		ubx.Data(workloads.SapvirtualinstanceSapvirtualInstance, "example", workloads.SapvirtualinstanceSapvirtualInstanceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SapvirtualinstanceSapvirtualInstance } from "@ubx/sdk-azure/azure/data/workloads/sapvirtualinstance_sapvirtual_instance";

    export default stack("example", () => {
      intent({
        summary: "look up azure_workloads_sapvirtualinstance_sapvirtual_instance",
      });

      data(SapvirtualinstanceSapvirtualInstance, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.workloads import SapvirtualinstanceSapvirtualInstance, SapvirtualinstanceSapvirtualInstanceConfig

    def describe():
        ubx.intent("look up azure_workloads_sapvirtualinstance_sapvirtual_instance")

        ubx.data(SapvirtualinstanceSapvirtualInstance, "example", SapvirtualinstanceSapvirtualInstanceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Gets the value of next link. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Gets the list of Virtual Instances for SAP solutions resources. Returned by the lookup.
</ResponseField>
