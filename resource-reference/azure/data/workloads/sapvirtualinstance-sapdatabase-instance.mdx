---
title: "azure_workloads_sapvirtualinstance_sapdatabase_instance"
description: "`azure_workloads_sapvirtualinstance_sapdatabase_instance` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_workloads_sapvirtualinstance_sapdatabase_instance` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workloads "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/workloads"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_workloads_sapvirtualinstance_sapdatabase_instance"})

    		ubx.Data(workloads.SapvirtualinstanceSapdatabaseInstance, "example", workloads.SapvirtualinstanceSapdatabaseInstanceConfig{
    			SapVirtualInstanceName: "example-sap-virtual-instance-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SapvirtualinstanceSapdatabaseInstance } from "@ubx/sdk-azure/azure/data/workloads/sapvirtualinstance_sapdatabase_instance";

    export default stack("example", () => {
      intent({
        summary: "look up azure_workloads_sapvirtualinstance_sapdatabase_instance",
      });

      data(SapvirtualinstanceSapdatabaseInstance, "example", {
        sapVirtualInstanceName: "example-sap-virtual-instance-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.workloads import SapvirtualinstanceSapdatabaseInstance, SapvirtualinstanceSapdatabaseInstanceConfig

    def describe():
        ubx.intent("look up azure_workloads_sapvirtualinstance_sapdatabase_instance")

        ubx.data(SapvirtualinstanceSapdatabaseInstance, "example", SapvirtualinstanceSapdatabaseInstanceConfig(
            sap_virtual_instance_name="example-sap-virtual-instance-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sap_virtual_instance_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Gets the value of next link. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Gets the list of SAP Database instances. Returned by the lookup.
</ResponseField>
