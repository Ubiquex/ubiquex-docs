---
title: "azure_workloads_monitors_monitor"
description: "`data_azure_workloads_monitors_monitor` looks up a real, already-existing `azure_workloads_monitors_monitor`."
---

It enables Azure Monitor for SAP solutions, collecting telemetry from an SAP landscape's own infrastructure and database components.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	workloads "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/workloads"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_workloads_monitors_monitor"})

    		ubx.Data(workloads.MonitorsMonitor, "example", workloads.MonitorsMonitorConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MonitorsMonitor } from "./local-sdk/azure/sdk/typescript/azure/data/workloads/monitors_monitor.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_workloads_monitors_monitor" });

      data(MonitorsMonitor, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.workloads import MonitorsMonitor, MonitorsMonitorConfig

    def describe():
        ubx.intent("look up azure_workloads_monitors_monitor")

        ubx.data(MonitorsMonitor, "example", MonitorsMonitorConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of SAP monitors. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of SAP monitors. Returned by the lookup.
</ResponseField>
