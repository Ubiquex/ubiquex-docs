---
title: "azure_workloads_monitors_sap_landscape_monitor"
description: "`azure_workloads_monitors_sap_landscape_monitor` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_workloads_monitors_sap_landscape_monitor` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workloads "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/workloads"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_workloads_monitors_sap_landscape_monitor"})

    		ubx.Data(workloads.MonitorsSapLandscapeMonitor, "example", workloads.MonitorsSapLandscapeMonitorConfig{
    			MonitorName: "example-monitor-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MonitorsSapLandscapeMonitor } from "@ubx/sdk-azure/azure/data/workloads/monitors_sap_landscape_monitor";

    export default stack("example", () => {
      intent({ summary: "look up azure_workloads_monitors_sap_landscape_monitor" });

      data(MonitorsSapLandscapeMonitor, "example", {
        monitorName: "example-monitor-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.workloads import MonitorsSapLandscapeMonitor, MonitorsSapLandscapeMonitorConfig

    def describe():
        ubx.intent("look up azure_workloads_monitors_sap_landscape_monitor")

        ubx.data(MonitorsSapLandscapeMonitor, "example", MonitorsSapLandscapeMonitorConfig(
            monitor_name="example-monitor-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="monitor_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of SAP Landscape Monitor Dashboard. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of Sap Landscape Monitor configuration. Returned by the lookup.
</ResponseField>
