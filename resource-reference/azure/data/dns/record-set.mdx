---
title: "azure_dns_record_set"
description: "`data_azure_dns_record_set` looks up a real, already-existing `azure_dns_record_set`."
---

It is a single DNS record set within an Azure DNS public zone.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	dns "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dns_record_set"})

    		ubx.Data(dns.RecordSet, "example", dns.RecordSetConfig{
    			ApiVersion:        "example",
    			ResourceGroupName: "example-resource-group-name",
    			SubscriptionId:    "example",
    			ZoneName:          "example-zone-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { RecordSet } from "./local-sdk/azure/sdk/typescript/azure/data/dns/record_set.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_dns_record_set" });

      data(RecordSet, "example", {
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
        zoneName: "example-zone-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.dns import RecordSet, RecordSetConfig

    def describe():
        ubx.intent("look up azure_dns_record_set")

        ubx.data(RecordSet, "example", RecordSetConfig(
            api_version="example",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
            zone_name="example-zone-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="zone_name" type="string" required>
  The real name of the DNS zone this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The continuation token for the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Information about the record sets in the response. Returned by the lookup.
</ResponseField>
