---
title: "azure_billing_openapi_agreement"
description: "`azure_billing_openapi_agreement` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_billing_openapi_agreement` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_agreement"})

    		ubx.Data(billing.OpenapiAgreement, "example", billing.OpenapiAgreementConfig{
    			BillingAccountName: "example-billing-account-name",
    			Expand:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiAgreement } from "@ubx/sdk-azure/azure/data/billing/openapi_agreement";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_agreement" });

      data(OpenapiAgreement, "example", {
        billingAccountName: "example-billing-account-name",
        expand: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiAgreement, OpenapiAgreementConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_agreement")

        ubx.data(OpenapiAgreement, "example", OpenapiAgreementConfig(
            billing_account_name="example-billing-account-name",
            expand="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="expand" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Agreement items on this page Returned by the lookup.
</ResponseField>
