---
title: "azure_billing_openapi_billing_account"
description: "`azure_billing_openapi_billing_account` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_billing_openapi_billing_account` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_billing_account"})

    		ubx.Data(billing.OpenapiBillingAccount, "example", billing.OpenapiBillingAccountConfig{
    			Expand:                           "example",
    			Filter:                           "example",
    			IncludeAll:                       true,
    			IncludeAllWithoutBillingProfiles: true,
    			IncludeDeleted:                   true,
    			IncludePendingAgreement:          true,
    			IncludeResellee:                  true,
    			LegalOwnerOid:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiBillingAccount } from "@ubx/sdk-azure/azure/data/billing/openapi_billing_account";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_billing_account" });

      data(OpenapiBillingAccount, "example", {
        expand: "example",
        filter: "example",
        includeAll: true,
        includeAllWithoutBillingProfiles: true,
        includeDeleted: true,
        includePendingAgreement: true,
        includeResellee: true,
        legalOwnerOid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiBillingAccount, OpenapiBillingAccountConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_billing_account")

        ubx.data(OpenapiBillingAccount, "example", OpenapiBillingAccountConfig(
            expand="example",
            filter="example",
            include_all=True,
            include_all_without_billing_profiles=True,
            include_deleted=True,
            include_pending_agreement=True,
            include_resellee=True,
            legal_owner_oid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="expand" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_all" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_all_without_billing_profiles" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_deleted" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_pending_agreement" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_resellee" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="legal_owner_oid" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="legal_owner_tid" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="search" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="skip" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="top" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The BillingAccount items on this page Returned by the lookup.
</ResponseField>
