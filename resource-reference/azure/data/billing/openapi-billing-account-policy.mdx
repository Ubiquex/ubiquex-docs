---
title: "azure_billing_openapi_billing_account_policy"
description: "`azure_billing_openapi_billing_account_policy` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_billing_openapi_billing_account_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_billing_account_policy"})

    		ubx.Data(billing.OpenapiBillingAccountPolicy, "example", billing.OpenapiBillingAccountPolicyConfig{
    			BillingAccountName: "example-billing-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiBillingAccountPolicy } from "@ubx/sdk-azure/azure/data/billing/openapi_billing_account_policy";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_billing_account_policy" });

      data(OpenapiBillingAccountPolicy, "example", {
        billingAccountName: "example-billing-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiBillingAccountPolicy, OpenapiBillingAccountPolicyConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_billing_account_policy")

        ubx.data(OpenapiBillingAccountPolicy, "example", OpenapiBillingAccountPolicyConfig(
            billing_account_name="example-billing-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  A policy at billing account scope. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="enterprise_agreement_policies" type="object">
      The policies for Enterprise Agreement enrollments. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="account_owner_view_charges" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="authentication_type" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="department_admin_view_charges" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="marketplace_purchases" type="string">
      The policy that controls whether Azure marketplace purchases are allowed. Optional lookup argument.
    </ResponseField>
    <ResponseField name="policies" type="list(object)">
      List of all policies defined at the billing scope. Optional lookup argument.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the resource during a long-running operation. Optional lookup argument.
    </ResponseField>
    <ResponseField name="reservation_purchases" type="string">
      The policy that controls whether Azure reservation purchases are allowed. Optional lookup argument.
    </ResponseField>
    <ResponseField name="savings_plan_purchases" type="string">
      The policy that controls whether users with Azure savings plan purchase are allowed. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Dictionary of metadata associated with the resource. It may not be populated for all resource types. Maximum key/value length supported of 256 characters. Keys/value should not empty value nor null. Keys can not contain &lt; &gt; % & \ ? / Returned by the lookup.
</ResponseField>
