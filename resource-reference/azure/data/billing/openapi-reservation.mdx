---
title: "azure_billing_openapi_reservation"
description: "`azure_billing_openapi_reservation` looks up a real Azure Reservation (a prepaid capacity commitment) on a billing account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_reservation"})

    		ubx.Data(billing.OpenapiReservation, "example", billing.OpenapiReservationConfig{
    			BillingAccountName: "example-billing-account-name",
    			BillingProfileName: "example-billing-profile-name",
    			Filter:             "example",
    			OrderBy:            "example",
    			RefreshSummary:     "example",
    			SelectedState:      "example",
    			Skiptoken:          1,
    			Take:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiReservation } from "./local-sdk/azure/sdk/typescript/azure/data/billing/openapi_reservation.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_reservation" });

      data(OpenapiReservation, "example", {
        billingAccountName: "example-billing-account-name",
        billingProfileName: "example-billing-profile-name",
        filter: "example",
        orderBy: "example",
        refreshSummary: "example",
        selectedState: "example",
        skiptoken: 1,
        take: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiReservation, OpenapiReservationConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_reservation")

        ubx.data(OpenapiReservation, "example", OpenapiReservationConfig(
            billing_account_name="example-billing-account-name",
            billing_profile_name="example-billing-profile-name",
            filter="example",
            order_by="example",
            refresh_summary="example",
            selected_state="example",
            skiptoken=1,
            take=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="expand" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reservation_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="reservation_order_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="etag" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="location" type="string">
  The location of the reservation. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  The property of reservation object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="applied_scope_properties" type="object">
      Properties specific to applied scope type. Not required if not applicable. Required and need to provide tenantId and managementGroupId if AppliedScopeType is ManagementGroup Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="display_name" type="string">
          Display name Optional lookup argument.
        </ResponseField>
        <ResponseField name="management_group_id" type="string">
          Fully-qualified identifier of the management group where the benefit must be applied. Optional lookup argument.
        </ResponseField>
        <ResponseField name="resource_group_id" type="string">
          Fully-qualified identifier of the resource group. Optional lookup argument.
        </ResponseField>
        <ResponseField name="subscription_id" type="string">
          Fully-qualified identifier of the subscription. Optional lookup argument.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          Tenant ID where the reservation should apply benefit. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="applied_scope_type" type="string">
      The applied scope type of the reservation. Returned by the lookup.
    </ResponseField>
    <ResponseField name="applied_scopes" type="list(string)">
      The array of applied scopes of a reservation. Will be null if the reservation is in Shared scope Optional lookup argument.
    </ResponseField>
    <ResponseField name="archived" type="bool">
      Indicates if the reservation is archived Optional lookup argument.
    </ResponseField>
    <ResponseField name="benefit_start_time" type="string">
      This is the DateTime when the reservation benefit started. Optional lookup argument.
    </ResponseField>
    <ResponseField name="billing_plan" type="string">
      Represent the billing plans. Optional lookup argument.
    </ResponseField>
    <ResponseField name="billing_scope_id" type="string">
      Subscription that will be charged for purchasing reservation or savings plan Returned by the lookup.
    </ResponseField>
    <ResponseField name="capabilities" type="string">
      Capabilities of the reservation Optional lookup argument.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The display name of the reservation Returned by the lookup.
    </ResponseField>
    <ResponseField name="display_provisioning_state" type="string">
      The provisioning state of the reservation for display, e.g. Succeeded Returned by the lookup.
    </ResponseField>
    <ResponseField name="effective_date_time" type="string">
      The effective date time of the reservation Returned by the lookup.
    </ResponseField>
    <ResponseField name="expiry_date" type="string">
      The expiry date of the reservation Returned by the lookup.
    </ResponseField>
    <ResponseField name="expiry_date_time" type="string">
      This is the date-time when the reservation will expire. Optional lookup argument.
    </ResponseField>
    <ResponseField name="extended_status_info" type="object">
      Extended status information for the reservation. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="message" type="string">
          The message giving detailed information about the status code. Optional lookup argument.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Extended status definition properties Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="subscription_id" type="string">
              Subscription Id Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="status_code" type="string">
          The status of the reservation. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="instance_flexibility" type="string">
      Turning this on will apply the reservation discount to other VMs in the same VM size group. Only specify for VirtualMachines reserved resource type. Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_updated_date_time" type="string">
      DateTime of the last time the reservation was updated. Returned by the lookup.
    </ResponseField>
    <ResponseField name="merge_properties" type="object">
      Properties of reservation merge Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="merge_destination" type="string">
          Reservation resource id Created due to the merge. Format of the resource id is /providers/Microsoft.Capacity/reservationOrders/&#123;reservationOrderId&#125;/reservations/&#123;reservationId&#125; Optional lookup argument.
        </ResponseField>
        <ResponseField name="merge_sources" type="list(string)">
          Resource ids of the source reservation's merged to form this reservation. Format of the resource id is /providers/Microsoft.Capacity/reservationOrders/&#123;reservationOrderId&#125;/reservations/&#123;reservationId&#125; Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="product_code" type="string">
      Represents UPN Optional lookup argument.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the reservation, e.g. Succeeded Returned by the lookup.
    </ResponseField>
    <ResponseField name="provisioning_sub_state" type="string">
      The provisioning state of the reservation, e.g. Succeeded Returned by the lookup.
    </ResponseField>
    <ResponseField name="purchase_date" type="string">
      This is the date when the reservation was purchased. Optional lookup argument.
    </ResponseField>
    <ResponseField name="purchase_date_time" type="string">
      This is the date-time when the reservation was purchased. Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number">
      The number of the reservation. Returned by the lookup.
    </ResponseField>
    <ResponseField name="renew" type="bool">
      The renew state of the reservation Returned by the lookup.
    </ResponseField>
    <ResponseField name="renew_destination" type="string">
      Reservation Id of the reservation which is purchased because of renew. Format of the resource Id is /providers/Microsoft.Capacity/reservationOrders/&#123;reservationOrderId&#125;/reservations/&#123;reservationId&#125;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="renew_properties" type="object">
      The renew properties for a reservation. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="billing_currency_total" type="object">
          The price. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="amount" type="number">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="currency_code" type="string">
              The ISO 4217 3-letter currency code for the currency used by this purchase record. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="pricing_currency_total" type="object">
          The price. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="amount" type="number">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="currency_code" type="string">
              The ISO 4217 3-letter currency code for the currency used by this purchase record. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="purchase_properties" type="object">
          The request for reservation purchase Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="location" type="string">
              The Azure region where the reserved resource lives. Optional lookup argument.
            </ResponseField>
            <ResponseField name="properties" type="object">
              Properties of reservation purchase request Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="applied_scope_properties" type="object">
                  Properties specific to applied scope type. Not required if not applicable. Required and need to provide tenantId and managementGroupId if AppliedScopeType is ManagementGroup Optional lookup argument.
                  <Expandable title="properties">
                    <ResponseField name="display_name" type="string">
                      Display name Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="management_group_id" type="string">
                      Fully-qualified identifier of the management group where the benefit must be applied. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="resource_group_id" type="string">
                      Fully-qualified identifier of the resource group. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="subscription_id" type="string">
                      Fully-qualified identifier of the subscription. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="tenant_id" type="string">
                      Tenant ID where the reservation should apply benefit. Optional lookup argument.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="applied_scope_type" type="string">
                  Type of the Applied Scope. Optional lookup argument.
                </ResponseField>
                <ResponseField name="applied_scopes" type="list(string)">
                  List of the subscriptions that the benefit will be applied. Do not specify if AppliedScopeType is Shared. Optional lookup argument.
                </ResponseField>
                <ResponseField name="billing_plan" type="string">
                  Represent the billing plans. Optional lookup argument.
                </ResponseField>
                <ResponseField name="billing_scope_id" type="string">
                  Subscription that will be charged for purchasing reservation or savings plan Returned by the lookup.
                </ResponseField>
                <ResponseField name="display_name" type="string">
                  Friendly name of the reservation Optional lookup argument.
                </ResponseField>
                <ResponseField name="instance_flexibility" type="string">
                  Turning this on will apply the reservation discount to other VMs in the same VM size group. Only specify for VirtualMachines reserved resource type. Optional lookup argument.
                </ResponseField>
                <ResponseField name="quantity" type="number">
                  Quantity of the skus that are part of the reservation. Must be greater than zero. Optional lookup argument.
                </ResponseField>
                <ResponseField name="renew" type="bool">
                  Setting this to true will automatically purchase a new benefit on the expiration date time. Optional lookup argument.
                </ResponseField>
                <ResponseField name="reserved_resource_properties" type="object">
                  Properties specific to each reserved resource type. Not required if not applicable. Optional lookup argument.
                  <Expandable title="properties">
                    <ResponseField name="instance_flexibility" type="string">
                      Turning this on will apply the reservation discount to other VMs in the same VM size group. Only specify for VirtualMachines reserved resource type. Optional lookup argument.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="reserved_resource_type" type="string">
                  The reserved source type of the reservation, e.g. virtual machine. Returned by the lookup.
                </ResponseField>
                <ResponseField name="review_date_time" type="string">
                  This is the date-time when the Azure hybrid benefit needs to be reviewed. Optional lookup argument.
                </ResponseField>
                <ResponseField name="term" type="string">
                  The term of the reservation, e.g. P1Y Returned by the lookup.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="sku" type="object">
              The name of sku Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="renew_source" type="string">
      The renew source of the reservation Returned by the lookup.
    </ResponseField>
    <ResponseField name="reserved_resource_type" type="string">
      The reserved source type of the reservation, e.g. virtual machine. Returned by the lookup.
    </ResponseField>
    <ResponseField name="review_date_time" type="string">
      This is the date-time when the Azure Hybrid Benefit needs to be reviewed. Optional lookup argument.
    </ResponseField>
    <ResponseField name="sku_description" type="string">
      The sku description of the reservation Returned by the lookup.
    </ResponseField>
    <ResponseField name="split_properties" type="object">
      Properties of reservation split Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="split_destinations" type="list(string)">
          List of destination resource id that are created due to split. Format of the resource id is /providers/Microsoft.Capacity/reservationOrders/&#123;reservationOrderId&#125;/reservations/&#123;reservationId&#125; Optional lookup argument.
        </ResponseField>
        <ResponseField name="split_source" type="string">
          Resource id of the reservation from which this is split. Format of the resource id is /providers/Microsoft.Capacity/reservationOrders/&#123;reservationOrderId&#125;/reservations/&#123;reservationId&#125; Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="swap_properties" type="object">
      Properties of reservation swap Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="swap_destination" type="string">
          Reservation resource id that the original resource gets swapped to. Format of the resource id is /providers/microsoft.capacity/reservationOrders/&#123;reservationOrderId&#125;/reservations/&#123;reservationId&#125; Optional lookup argument.
        </ResponseField>
        <ResponseField name="swap_source" type="string">
          Resource id of the source reservation that gets swapped. Format of the resource id is /providers/microsoft.capacity/reservationOrders/&#123;reservationOrderId&#125;/reservations/&#123;reservationId&#125; Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="term" type="string">
      The term of the reservation, e.g. P1Y Returned by the lookup.
    </ResponseField>
    <ResponseField name="user_friendly_applied_scope_type" type="string">
      The applied scope type of the reservation for display, e.g. Shared Returned by the lookup.
    </ResponseField>
    <ResponseField name="user_friendly_renew_state" type="string">
      The renew state of the reservation for display, e.g. On Returned by the lookup.
    </ResponseField>
    <ResponseField name="utilization" type="object">
      Reservation utilization Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="aggregates" type="list(object)">
          The array of aggregates of a reservation's utilization Optional lookup argument.
        </ResponseField>
        <ResponseField name="trend" type="string">
          last 7 day utilization trend for a reservation Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The property of reservation sku object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of the reservation sku. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags for this reservation Returned by the lookup.
</ResponseField>
