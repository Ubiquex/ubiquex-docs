---
title: "azure_billing_openapi_customer"
description: "`azure_billing_openapi_customer` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_billing_openapi_customer` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_customer"})

    		ubx.Data(billing.OpenapiCustomer, "example", billing.OpenapiCustomerConfig{
    			BillingAccountName: "example-billing-account-name",
    			BillingProfileName: "example-billing-profile-name",
    			Count:              true,
    			Expand:             "example",
    			Filter:             "example",
    			OrderBy:            "example",
    			Search:             "example",
    			Skip:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiCustomer } from "@ubx/sdk-azure/azure/data/billing/openapi_customer";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_customer" });

      data(OpenapiCustomer, "example", {
        billingAccountName: "example-billing-account-name",
        billingProfileName: "example-billing-profile-name",
        count: true,
        expand: "example",
        filter: "example",
        orderBy: "example",
        search: "example",
        skip: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiCustomer, OpenapiCustomerConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_customer")

        ubx.data(OpenapiCustomer, "example", OpenapiCustomerConfig(
            billing_account_name="example-billing-account-name",
            billing_profile_name="example-billing-profile-name",
            count=True,
            expand="example",
            filter="example",
            order_by="example",
            search="example",
            skip=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="billing_profile_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="count" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="expand" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="search" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="skip" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="top" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Customer items on this page Returned by the lookup.
</ResponseField>
