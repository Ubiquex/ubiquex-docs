---
title: "azure_billing_openapi_savings_plan_model_list_result"
description: "`azure_billing_openapi_savings_plan_model_list_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_billing_openapi_savings_plan_model_list_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_savings_plan_model_list_result"})

    		ubx.Data(billing.OpenapiSavingsPlanModelListResult, "example", billing.OpenapiSavingsPlanModelListResultConfig{
    			BillingAccountName: "example-billing-account-name",
    			Filter:             "example",
    			OrderBy:            "example",
    			RefreshSummary:     "example",
    			SelectedState:      "example",
    			Skiptoken:          1,
    			Take:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiSavingsPlanModelListResult } from "@ubx/sdk-azure/azure/data/billing/openapi_savings_plan_model_list_result";

    export default stack("example", () => {
      intent({
        summary: "look up azure_billing_openapi_savings_plan_model_list_result",
      });

      data(OpenapiSavingsPlanModelListResult, "example", {
        billingAccountName: "example-billing-account-name",
        filter: "example",
        orderBy: "example",
        refreshSummary: "example",
        selectedState: "example",
        skiptoken: 1,
        take: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiSavingsPlanModelListResult, OpenapiSavingsPlanModelListResultConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_savings_plan_model_list_result")

        ubx.data(OpenapiSavingsPlanModelListResult, "example", OpenapiSavingsPlanModelListResultConfig(
            billing_account_name="example-billing-account-name",
            filter="example",
            order_by="example",
            refresh_summary="example",
            selected_state="example",
            skiptoken=1,
            take=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="refresh_summary" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="selected_state" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="skiptoken" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="take" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="summary" type="object">
  The roll up count summary of savings plans in each state Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cancelled_count" type="number">
      The number of savings plans in Cancelled state Returned by the lookup.
    </ResponseField>
    <ResponseField name="expired_count" type="number">
      The number of savings plans in Expired state Returned by the lookup.
    </ResponseField>
    <ResponseField name="expiring_count" type="number">
      The number of savings plans in Expiring state Returned by the lookup.
    </ResponseField>
    <ResponseField name="failed_count" type="number">
      The number of savings plans in Failed state Returned by the lookup.
    </ResponseField>
    <ResponseField name="no_benefit_count" type="number">
      The number of savings plans in No Benefit state Returned by the lookup.
    </ResponseField>
    <ResponseField name="pending_count" type="number">
      The number of savings plans in Pending state Returned by the lookup.
    </ResponseField>
    <ResponseField name="processing_count" type="number">
      The number of savings plans in Processing state Returned by the lookup.
    </ResponseField>
    <ResponseField name="succeeded_count" type="number">
      The number of savings plans in Succeeded state Returned by the lookup.
    </ResponseField>
    <ResponseField name="warning_count" type="number">
      The number of savings plans in Warning state Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
