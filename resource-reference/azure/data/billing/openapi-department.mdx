---
title: "azure_billing_openapi_department"
description: "`azure_billing_openapi_department` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_billing_openapi_department` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_department"})

    		ubx.Data(billing.OpenapiDepartment, "example", billing.OpenapiDepartmentConfig{
    			BillingAccountName: "example-billing-account-name",
    			Filter:             "example",
    			OrderBy:            "example",
    			Search:             "example",
    			Skip:               1,
    			Top:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiDepartment } from "@ubx/sdk-azure/azure/data/billing/openapi_department";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_department" });

      data(OpenapiDepartment, "example", {
        billingAccountName: "example-billing-account-name",
        filter: "example",
        orderBy: "example",
        search: "example",
        skip: 1,
        top: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiDepartment, OpenapiDepartmentConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_department")

        ubx.data(OpenapiDepartment, "example", OpenapiDepartmentConfig(
            billing_account_name="example-billing-account-name",
            filter="example",
            order_by="example",
            search="example",
            skip=1,
            top=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="search" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="skip" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="top" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Department items on this page Returned by the lookup.
</ResponseField>
