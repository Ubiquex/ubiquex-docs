---
title: "azure_billing_openapi_payment_method"
description: "`azure_billing_openapi_payment_method` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_billing_openapi_payment_method` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_payment_method"})

    		ubx.Data(billing.OpenapiPaymentMethod, "example", billing.OpenapiPaymentMethodConfig{
    			BillingAccountName: "example-billing-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiPaymentMethod } from "@ubx/sdk-azure/azure/data/billing/openapi_payment_method";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_payment_method" });

      data(OpenapiPaymentMethod, "example", {
        billingAccountName: "example-billing-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiPaymentMethod, OpenapiPaymentMethodConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_payment_method")

        ubx.data(OpenapiPaymentMethod, "example", OpenapiPaymentMethodConfig(
            billing_account_name="example-billing-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PaymentMethod items on this page Returned by the lookup.
</ResponseField>
