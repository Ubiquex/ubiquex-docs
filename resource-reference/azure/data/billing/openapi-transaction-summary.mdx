---
title: "azure_billing_openapi_transaction_summary"
description: "`azure_billing_openapi_transaction_summary` reads a real, aggregated summary of billing transactions on a billing account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_transaction_summary"})

    		ubx.Data(billing.OpenapiTransactionSummary, "example", billing.OpenapiTransactionSummaryConfig{
    			BillingAccountName: "example-billing-account-name",
    			InvoiceName:        "example-invoice-name",
    			Filter:             "example",
    			Search:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiTransactionSummary } from "@ubx/sdk-azure/azure/data/billing/openapi_transaction_summary";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_transaction_summary" });

      data(OpenapiTransactionSummary, "example", {
        billingAccountName: "example-billing-account-name",
        invoiceName: "example-invoice-name",
        filter: "example",
        search: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiTransactionSummary, OpenapiTransactionSummaryConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_transaction_summary")

        ubx.data(OpenapiTransactionSummary, "example", OpenapiTransactionSummaryConfig(
            billing_account_name="example-billing-account-name",
            invoice_name="example-invoice-name",
            filter="example",
            search="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  The real name of the billing account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  A real OData filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="invoice_name" type="string" required>
  The real name of the invoice this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="search" type="string">
  The real search query string. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="azure_credit_applied" type="number">
  The total amount of any Azure credits applied. Returned by the lookup.
</ResponseField>

<ResponseField name="billing_currency" type="string">
  The ISO 4217 code for the currency in which the transactions are billed. Returned by the lookup.
</ResponseField>

<ResponseField name="consumption_commitment_decremented" type="number">
  The total Microsoft Azure Consumption Commitment (MACC) decrement through the invoice. Returned by the lookup.
</ResponseField>

<ResponseField name="sub_total" type="number">
  The total pre-tax charged amount. Returned by the lookup.
</ResponseField>

<ResponseField name="tax" type="number">
  The total tax amount applied. Returned by the lookup.
</ResponseField>

<ResponseField name="total" type="number">
  The total charges. Returned by the lookup.
</ResponseField>
