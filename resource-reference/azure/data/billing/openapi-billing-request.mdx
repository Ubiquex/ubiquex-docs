---
title: "azure_billing_openapi_billing_request"
description: "`data_azure_billing_openapi_billing_request` looks up a real, already-existing `azure_billing_openapi_billing_request`."
---

It is a pending request to grant access to a billing account, profile, or invoice section, requiring approval before it takes effect.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_billing_request"})

    		ubx.Data(billing.OpenapiBillingRequest, "example", billing.OpenapiBillingRequestConfig{
    			BillingAccountName: "example-billing-account-name",
    			BillingProfileName: "example-billing-profile-name",
    			Count:              true,
    			Filter:             "example",
    			OrderBy:            "example",
    			Search:             "example",
    			Skip:               1,
    			Top:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiBillingRequest } from "@ubx/sdk-azure/azure/data/billing/openapi_billing_request";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_billing_request" });

      data(OpenapiBillingRequest, "example", {
        billingAccountName: "example-billing-account-name",
        billingProfileName: "example-billing-profile-name",
        count: true,
        filter: "example",
        orderBy: "example",
        search: "example",
        skip: 1,
        top: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiBillingRequest, OpenapiBillingRequestConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_billing_request")

        ubx.data(OpenapiBillingRequest, "example", OpenapiBillingRequestConfig(
            billing_account_name="example-billing-account-name",
            billing_profile_name="example-billing-profile-name",
            count=True,
            filter="example",
            order_by="example",
            search="example",
            skip=1,
            top=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  The real name of the billing account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="billing_profile_name" type="string" required>
  The real name of the billing profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="count" type="bool">
  The total number of matching results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  A real OData filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  The real field results are ordered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="search" type="string">
  The real search query string. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="skip" type="number">
  The number of matching results to skip before returning the rest, for pagination. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="top" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The BillingRequest items on this page Returned by the lookup.
</ResponseField>
