---
title: "azure_billing_openapi_savings_plan_order_model"
description: "`azure_billing_openapi_savings_plan_order_model` looks up a real savings plan order -- the purchase transaction grouping one or more Azure Savings Plans."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_savings_plan_order_model"})

    		ubx.Data(billing.OpenapiSavingsPlanOrderModel, "example", billing.OpenapiSavingsPlanOrderModelConfig{
    			BillingAccountName: "example-billing-account-name",
    			Filter:             "example",
    			OrderBy:            "example",
    			Skiptoken:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiSavingsPlanOrderModel } from "./local-sdk/azure/sdk/typescript/azure/data/billing/openapi_savings_plan_order_model.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_savings_plan_order_model" });

      data(OpenapiSavingsPlanOrderModel, "example", {
        billingAccountName: "example-billing-account-name",
        filter: "example",
        orderBy: "example",
        skiptoken: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiSavingsPlanOrderModel, OpenapiSavingsPlanOrderModelConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_savings_plan_order_model")

        ubx.data(OpenapiSavingsPlanOrderModel, "example", OpenapiSavingsPlanOrderModelConfig(
            billing_account_name="example-billing-account-name",
            filter="example",
            order_by="example",
            skiptoken=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  The real name of the billing account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  A real OData filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  The real field results are ordered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="skiptoken" type="number">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SavingsPlanOrderModel items on this page Returned by the lookup.
</ResponseField>
