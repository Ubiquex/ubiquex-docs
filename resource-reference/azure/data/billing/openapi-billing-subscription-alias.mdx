---
title: "azure_billing_openapi_billing_subscription_alias"
description: "`data_azure_billing_openapi_billing_subscription_alias` looks up a real, already-existing `azure_billing_openapi_billing_subscription_alias`."
---

It creates a new Azure subscription (or renames/moves an existing one between billing scopes) via the billing subscription alias API.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_billing_subscription_alias"})

    		ubx.Data(billing.OpenapiBillingSubscriptionAlias, "example", billing.OpenapiBillingSubscriptionAliasConfig{
    			BillingAccountName: "example-billing-account-name",
    			Count:              true,
    			Filter:             "example",
    			IncludeDeleted:     true,
    			OrderBy:            "example",
    			Search:             "example",
    			Skip:               1,
    			Top:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiBillingSubscriptionAlias } from "./local-sdk/azure/sdk/typescript/azure/data/billing/openapi_billing_subscription_alias.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_billing_openapi_billing_subscription_alias",
      });

      data(OpenapiBillingSubscriptionAlias, "example", {
        billingAccountName: "example-billing-account-name",
        count: true,
        filter: "example",
        includeDeleted: true,
        orderBy: "example",
        search: "example",
        skip: 1,
        top: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiBillingSubscriptionAlias, OpenapiBillingSubscriptionAliasConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_billing_subscription_alias")

        ubx.data(OpenapiBillingSubscriptionAlias, "example", OpenapiBillingSubscriptionAliasConfig(
            billing_account_name="example-billing-account-name",
            count=True,
            filter="example",
            include_deleted=True,
            order_by="example",
            search="example",
            skip=1,
            top=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  The real name of the billing account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="count" type="bool">
  The total number of matching results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  A real OData filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_deleted" type="bool">
  Whether deleted items are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  The real field results are ordered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="search" type="string">
  The real search query string. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="skip" type="number">
  The number of matching results to skip before returning the rest, for pagination. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="top" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The BillingSubscriptionAlias items on this page Returned by the lookup.
</ResponseField>
