---
title: "azure_billing_openapi_available_balance"
description: "`azure_billing_openapi_available_balance` reads the real, current available credit balance on a billing account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_available_balance"})

    		ubx.Data(billing.OpenapiAvailableBalance, "example", billing.OpenapiAvailableBalanceConfig{
    			BillingAccountName: "example-billing-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiAvailableBalance } from "@ubx/sdk-azure/azure/data/billing/openapi_available_balance";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_available_balance" });

      data(OpenapiAvailableBalance, "example", {
        billingAccountName: "example-billing-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiAvailableBalance, OpenapiAvailableBalanceConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_available_balance")

        ubx.data(OpenapiAvailableBalance, "example", OpenapiAvailableBalanceConfig(
            billing_account_name="example-billing-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  The real name of the billing account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  The Available Credit or Payment on Account Balance. The credit balance can be used to settle due or past due invoices. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="amount" type="object">
      Credit amount for immediate payment. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="currency" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="value" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="payments_on_account" type="list(object)">
      The list of payments on accounts. Returned by the lookup.
    </ResponseField>
    <ResponseField name="total_payments_on_account" type="object">
      Total amount of payments on accounts. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="currency" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="value" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Dictionary of metadata associated with the resource. It may not be populated for all resource types. Maximum key/value length supported of 256 characters. Keys/value should not empty value nor null. Keys can not contain &lt; &gt; % & \ ? / Returned by the lookup.
</ResponseField>
