---
title: "azure_billing_openapi_billing_profile_policy"
description: "`azure_billing_openapi_billing_profile_policy` reads the real policy settings governing a billing profile."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_billing_profile_policy"})

    		ubx.Data(billing.OpenapiBillingProfilePolicy, "example", billing.OpenapiBillingProfilePolicyConfig{
    			BillingAccountName: "example-billing-account-name",
    			BillingProfileName: "example-billing-profile-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiBillingProfilePolicy } from "@ubx/sdk-azure/azure/data/billing/openapi_billing_profile_policy";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_billing_profile_policy" });

      data(OpenapiBillingProfilePolicy, "example", {
        billingAccountName: "example-billing-account-name",
        billingProfileName: "example-billing-profile-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiBillingProfilePolicy, OpenapiBillingProfilePolicyConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_billing_profile_policy")

        ubx.data(OpenapiBillingProfilePolicy, "example", OpenapiBillingProfilePolicyConfig(
            billing_account_name="example-billing-account-name",
            billing_profile_name="example-billing-profile-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_name" type="string" required>
  The real name of the billing account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="billing_profile_name" type="string" required>
  The real name of the billing profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  A policy at billing profile scope. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="enterprise_agreement_policies" type="object">
      The policies for Enterprise Agreement enrollments. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="account_owner_view_charges" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="authentication_type" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="department_admin_view_charges" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="invoice_section_label_management" type="string">
      The policy that controls invoice section label management at invoice section scope. This is allowed by default. Optional lookup argument.
    </ResponseField>
    <ResponseField name="marketplace_purchases" type="string">
      The policy that controls whether Azure marketplace purchases are allowed. Optional lookup argument.
    </ResponseField>
    <ResponseField name="policies" type="list(object)">
      List of all policies defined at the billing scope. Optional lookup argument.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the resource during a long-running operation. Optional lookup argument.
    </ResponseField>
    <ResponseField name="reservation_purchases" type="string">
      The policy that controls whether Azure reservation purchases are allowed. Optional lookup argument.
    </ResponseField>
    <ResponseField name="savings_plan_purchases" type="string">
      The policy that controls whether users with Azure savings plan purchase are allowed. Optional lookup argument.
    </ResponseField>
    <ResponseField name="view_charges" type="string">
      The policy that controls whether the users in customer's organization can view charges at pay-as-you-go prices. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Dictionary of metadata associated with the resource. It may not be populated for all resource types. Maximum key/value length supported of 256 characters. Keys/value should not empty value nor null. Keys can not contain &lt; &gt; % & \ ? / Returned by the lookup.
</ResponseField>
