---
title: "azure_billing_openapi_invoice"
description: "`azure_billing_openapi_invoice` looks up a real, generated invoice on a billing account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_billing_openapi_invoice"})

    		ubx.Data(billing.OpenapiInvoice, "example", billing.OpenapiInvoiceConfig{
    			SubscriptionId:  "example",
    			Count:           true,
    			Filter:          "example",
    			OrderBy:         "example",
    			PeriodEndDate:   "example",
    			PeriodStartDate: "example",
    			Search:          "example",
    			Skip:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiInvoice } from "./local-sdk/azure/sdk/typescript/azure/data/billing/openapi_invoice.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_billing_openapi_invoice" });

      data(OpenapiInvoice, "example", {
        subscriptionId: "example",
        count: true,
        filter: "example",
        orderBy: "example",
        periodEndDate: "example",
        periodStartDate: "example",
        search: "example",
        skip: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.billing import OpenapiInvoice, OpenapiInvoiceConfig

    def describe():
        ubx.intent("look up azure_billing_openapi_invoice")

        ubx.data(OpenapiInvoice, "example", OpenapiInvoiceConfig(
            subscription_id="example",
            count=True,
            filter="example",
            order_by="example",
            period_end_date="example",
            period_start_date="example",
            search="example",
            skip=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="count" type="bool">
  The total number of matching results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  A real OData filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  The real field results are ordered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="period_end_date" type="string">
  The real, inclusive end of the billing period this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="period_start_date" type="string">
  The real, inclusive start of the billing period this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="search" type="string">
  The real search query string. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="skip" type="number">
  The number of matching results to skip before returning the rest, for pagination. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="top" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Invoice items on this page Returned by the lookup.
</ResponseField>
