---
title: "azure_eventgrid_topic_type_info"
description: "`azure_eventgrid_topic_type_info` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_eventgrid_topic_type_info` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventgrid "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/eventgrid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_eventgrid_topic_type_info"})

    		ubx.Data(eventgrid.TopicTypeInfo, "example", eventgrid.TopicTypeInfoConfig{
    			ApiVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TopicTypeInfo } from "@ubx/sdk-azure/azure/data/eventgrid/topic_type_info";

    export default stack("example", () => {
      intent({ summary: "look up azure_eventgrid_topic_type_info" });

      data(TopicTypeInfo, "example", {
        apiVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.eventgrid import TopicTypeInfo, TopicTypeInfoConfig

    def describe():
        ubx.intent("look up azure_eventgrid_topic_type_info")

        ubx.data(TopicTypeInfo, "example", TopicTypeInfoConfig(
            api_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  A collection of topic types Returned by the lookup.
</ResponseField>
