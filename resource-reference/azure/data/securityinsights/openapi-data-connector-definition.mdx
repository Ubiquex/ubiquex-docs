---
title: "azure_securityinsights_openapi_data_connector_definition"
description: "`azure_securityinsights_openapi_data_connector_definition` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_securityinsights_openapi_data_connector_definition` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/securityinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_securityinsights_openapi_data_connector_definition"})

    		ubx.Data(securityinsights.OpenapiDataConnectorDefinition, "example", securityinsights.OpenapiDataConnectorDefinitionConfig{
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiDataConnectorDefinition } from "@ubx/sdk-azure/azure/data/securityinsights/openapi_data_connector_definition";

    export default stack("example", () => {
      intent({
        summary: "look up azure_securityinsights_openapi_data_connector_definition",
      });

      data(OpenapiDataConnectorDefinition, "example", {
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.securityinsights import OpenapiDataConnectorDefinition, OpenapiDataConnectorDefinitionConfig

    def describe():
        ubx.intent("look up azure_securityinsights_openapi_data_connector_definition")

        ubx.data(OpenapiDataConnectorDefinition, "example", OpenapiDataConnectorDefinitionConfig(
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of data connector definitions. Returned by the lookup.
</ResponseField>
