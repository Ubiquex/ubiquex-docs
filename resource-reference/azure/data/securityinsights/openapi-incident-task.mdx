---
title: "azure_securityinsights_openapi_incident_task"
description: "`data_azure_securityinsights_openapi_incident_task` looks up a real, already-existing `azure_securityinsights_openapi_incident_task`."
---

It is a single checklist item tracked against a Microsoft Sentinel incident, part of a structured investigation or response playbook.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	securityinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/securityinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_securityinsights_openapi_incident_task"})

    		ubx.Data(securityinsights.OpenapiIncidentTask, "example", securityinsights.OpenapiIncidentTaskConfig{
    			IncidentId:    "example",
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiIncidentTask } from "./local-sdk/azure/sdk/typescript/azure/data/securityinsights/openapi_incident_task.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_securityinsights_openapi_incident_task" });

      data(OpenapiIncidentTask, "example", {
        incidentId: "example",
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.securityinsights import OpenapiIncidentTask, OpenapiIncidentTaskConfig

    def describe():
        ubx.intent("look up azure_securityinsights_openapi_incident_task")

        ubx.data(OpenapiIncidentTask, "example", OpenapiIncidentTaskConfig(
            incident_id="example",
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_id" type="string" required>
  The real, unique identifier of this incident. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of incident tasks. Returned by the lookup.
</ResponseField>
