---
title: "azure_securityinsights_openapi_threat_intelligence_information"
description: "`data_azure_securityinsights_openapi_threat_intelligence_information` looks up a real, already-existing..."
---

`data_azure_securityinsights_openapi_threat_intelligence_information` looks up a real, already-existing `azure_securityinsights_openapi_threat_intelligence_information`. It is a single threat intelligence indicator (an IP, domain, or hash) ingested into Microsoft Sentinel, used to enrich and match against incoming log data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/securityinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_securityinsights_openapi_threat_intelligence_information"})

    		ubx.Data(securityinsights.OpenapiThreatIntelligenceInformation, "example", securityinsights.OpenapiThreatIntelligenceInformationConfig{
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiThreatIntelligenceInformation } from "@ubx/sdk-azure/azure/data/securityinsights/openapi_threat_intelligence_information";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_securityinsights_openapi_threat_intelligence_information",
      });

      data(OpenapiThreatIntelligenceInformation, "example", {
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.securityinsights import OpenapiThreatIntelligenceInformation, OpenapiThreatIntelligenceInformationConfig

    def describe():
        ubx.intent("look up azure_securityinsights_openapi_threat_intelligence_information")

        ubx.data(OpenapiThreatIntelligenceInformation, "example", OpenapiThreatIntelligenceInformationConfig(
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ThreatIntelligenceInformation items on this page Returned by the lookup.
</ResponseField>
