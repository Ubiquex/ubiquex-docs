---
title: "azure_managedservices_registration_assignment"
description: "`data_azure_managedservices_registration_assignment` looks up a real, already-existing `azure_managedservices_registration_assignment`."
---

It delegates management of a specific resource group or subscription to a managing tenant, per an Azure Lighthouse registration definition.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	managedservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/managedservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_managedservices_registration_assignment"})

    		ubx.Data(managedservices.RegistrationAssignment, "example", managedservices.RegistrationAssignmentConfig{
    			ApiVersion: "example",
    			Scope:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RegistrationAssignment } from "@ubx/sdk-azure/azure/data/managedservices/registration_assignment";

    export default stack("example", () => {
      intent({ summary: "look up azure_managedservices_registration_assignment" });

      data(RegistrationAssignment, "example", {
        apiVersion: "example",
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.managedservices import RegistrationAssignment, RegistrationAssignmentConfig

    def describe():
        ubx.intent("look up azure_managedservices_registration_assignment")

        ubx.data(RegistrationAssignment, "example", RegistrationAssignmentConfig(
            api_version="example",
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real Azure Resource Manager scope (subscription, resource group, or resource) this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of registration assignments. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of registration assignments. Returned by the lookup.
</ResponseField>
