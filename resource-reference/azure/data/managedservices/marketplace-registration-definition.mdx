---
title: "azure_managedservices_marketplace_registration_definition"
description: "`azure_managedservices_marketplace_registration_definition` reads a real registration definition authorizing a Marketplace publisher to manage..."
---

`azure_managedservices_marketplace_registration_definition` reads a real registration definition authorizing a Marketplace publisher to manage resources on a customer's own subscription, via Azure Lighthouse.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	managedservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/managedservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_managedservices_marketplace_registration_definition"})

    		ubx.Data(managedservices.MarketplaceRegistrationDefinition, "example", managedservices.MarketplaceRegistrationDefinitionConfig{
    			ApiVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MarketplaceRegistrationDefinition } from "./local-sdk/azure/sdk/typescript/azure/data/managedservices/marketplace_registration_definition.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_managedservices_marketplace_registration_definition",
      });

      data(MarketplaceRegistrationDefinition, "example", {
        apiVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.managedservices import MarketplaceRegistrationDefinition, MarketplaceRegistrationDefinitionConfig

    def describe():
        ubx.intent("look up azure_managedservices_marketplace_registration_definition")

        ubx.data(MarketplaceRegistrationDefinition, "example", MarketplaceRegistrationDefinitionConfig(
            api_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of marketplace registration definitions. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of marketplace registration definitions. Returned by the lookup.
</ResponseField>
