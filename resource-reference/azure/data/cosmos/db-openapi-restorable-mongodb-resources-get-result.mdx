---
title: "azure_cosmos_db_openapi_restorable_mongodb_resources_get_result"
description: "`azure_cosmos_db_openapi_restorable_mongodb_resources_get_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cosmos_db_openapi_restorable_mongodb_resources_get_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_restorable_mongodb_resources_get_result"})

    		ubx.Data(cosmos.DbOpenapiRestorableMongodbResourcesGetResult, "example", cosmos.DbOpenapiRestorableMongodbResourcesGetResultConfig{
    			InstanceId:            "example",
    			Location:              "example",
    			RestoreLocation:       "example",
    			RestoreTimestampInUtc: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiRestorableMongodbResourcesGetResult } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_restorable_mongodb_resources_get_result";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_cosmos_db_openapi_restorable_mongodb_resources_get_result",
      });

      data(DbOpenapiRestorableMongodbResourcesGetResult, "example", {
        instanceId: "example",
        location: "example",
        restoreLocation: "example",
        restoreTimestampInUtc: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiRestorableMongodbResourcesGetResult, DbOpenapiRestorableMongodbResourcesGetResultConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_restorable_mongodb_resources_get_result")

        ubx.data(DbOpenapiRestorableMongodbResourcesGetResult, "example", DbOpenapiRestorableMongodbResourcesGetResultConfig(
            instance_id="example",
            location="example",
            restore_location="example",
            restore_timestamp_in_utc="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="restore_location" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="restore_timestamp_in_utc" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of restorable MongoDB resources, including the database and collection names. Returned by the lookup.
</ResponseField>
