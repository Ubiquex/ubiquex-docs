---
title: "azure_cosmos_db_openapi_cassandra_cluster_public_status"
description: "`azure_cosmos_db_openapi_cassandra_cluster_public_status` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cosmos_db_openapi_cassandra_cluster_public_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_cassandra_cluster_public_status"})

    		ubx.Data(cosmos.DbOpenapiCassandraClusterPublicStatus, "example", cosmos.DbOpenapiCassandraClusterPublicStatusConfig{
    			ClusterName: "example-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiCassandraClusterPublicStatus } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_cassandra_cluster_public_status";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cosmos_db_openapi_cassandra_cluster_public_status",
      });

      data(DbOpenapiCassandraClusterPublicStatus, "example", {
        clusterName: "example-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiCassandraClusterPublicStatus, DbOpenapiCassandraClusterPublicStatusConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_cassandra_cluster_public_status")

        ubx.data(DbOpenapiCassandraClusterPublicStatus, "example", DbOpenapiCassandraClusterPublicStatusConfig(
            cluster_name="example-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connection_errors" type="list(object)">
  List relevant information about any connection errors to the Datacenters. Returned by the lookup.
</ResponseField>

<ResponseField name="data_centers" type="list(object)">
  List of the status of each datacenter in this cluster. Returned by the lookup.
</ResponseField>

<ResponseField name="e_tag" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="errors" type="list(object)">
  List relevant information about any errors about cluster, data center and connection error. Returned by the lookup.
</ResponseField>

<ResponseField name="reaper_status" type="object">
  Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="healthy" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="repair_run_ids" type="map(string)">
      Dictionary of &lt;string&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="repair_schedules" type="map(string)">
      Dictionary of &lt;string&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
