---
title: "azure_cosmos_db_openapi_cassandra_table_get_results"
description: "`data_azure_cosmos_db_openapi_cassandra_table_get_results` looks up a real, already-existing `azure_cosmos_db_openapi_cassandra_table_get_results`."
---

It is a table within a Cosmos DB account's Cassandra API keyspace.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_cassandra_table_get_results"})

    		ubx.Data(cosmos.DbOpenapiCassandraTableGetResults, "example", cosmos.DbOpenapiCassandraTableGetResultsConfig{
    			AccountName:  "example-account-name",
    			KeyspaceName: "example-keyspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiCassandraTableGetResults } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_cassandra_table_get_results";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cosmos_db_openapi_cassandra_table_get_results",
      });

      data(DbOpenapiCassandraTableGetResults, "example", {
        accountName: "example-account-name",
        keyspaceName: "example-keyspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiCassandraTableGetResults, DbOpenapiCassandraTableGetResultsConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_cassandra_table_get_results")

        ubx.data(DbOpenapiCassandraTableGetResults, "example", DbOpenapiCassandraTableGetResultsConfig(
            account_name="example-account-name",
            keyspace_name="example-keyspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="keyspace_name" type="string" required>
  The real name of the Cassandra keyspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of Cassandra tables and their properties. Returned by the lookup.
</ResponseField>
