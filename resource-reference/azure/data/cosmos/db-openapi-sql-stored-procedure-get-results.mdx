---
title: "azure_cosmos_db_openapi_sql_stored_procedure_get_results"
description: "`data_azure_cosmos_db_openapi_sql_stored_procedure_get_results` looks up a real, already-existing..."
---

`data_azure_cosmos_db_openapi_sql_stored_procedure_get_results` looks up a real, already-existing `azure_cosmos_db_openapi_sql_stored_procedure_get_results`. It is a JavaScript stored procedure registered on a Cosmos DB SQL API container, executed server-side against its own data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_sql_stored_procedure_get_results"})

    		ubx.Data(cosmos.DbOpenapiSqlStoredProcedureGetResults, "example", cosmos.DbOpenapiSqlStoredProcedureGetResultsConfig{
    			AccountName:   "example-account-name",
    			ContainerName: "example-container-name",
    			DatabaseName:  "example-database-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiSqlStoredProcedureGetResults } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_sql_stored_procedure_get_results";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cosmos_db_openapi_sql_stored_procedure_get_results",
      });

      data(DbOpenapiSqlStoredProcedureGetResults, "example", {
        accountName: "example-account-name",
        containerName: "example-container-name",
        databaseName: "example-database-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiSqlStoredProcedureGetResults, DbOpenapiSqlStoredProcedureGetResultsConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_sql_stored_procedure_get_results")

        ubx.data(DbOpenapiSqlStoredProcedureGetResults, "example", DbOpenapiSqlStoredProcedureGetResultsConfig(
            account_name="example-account-name",
            container_name="example-container-name",
            database_name="example-database-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="container_name" type="string" required>
  The real name of the container this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  The real name of the database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of storedProcedures and their properties. Returned by the lookup.
</ResponseField>
