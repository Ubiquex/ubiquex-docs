---
title: "azure_cosmos_db_openapi_fleetspace_resource"
description: "`data_azure_cosmos_db_openapi_fleetspace_resource` looks up a real, already-existing `azure_cosmos_db_openapi_fleetspace_resource`."
---

It is a logical grouping of Cosmos DB account(s) within a fleet, optionally sharing a pooled throughput allocation across every account in the group.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_fleetspace_resource"})

    		ubx.Data(cosmos.DbOpenapiFleetspaceResource, "example", cosmos.DbOpenapiFleetspaceResourceConfig{
    			FleetName: "example-fleet-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { DbOpenapiFleetspaceResource } from "./local-sdk/azure/sdk/typescript/azure/data/cosmos/db_openapi_fleetspace_resource.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_cosmos_db_openapi_fleetspace_resource" });

      data(DbOpenapiFleetspaceResource, "example", {
        fleetName: "example-fleet-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiFleetspaceResource, DbOpenapiFleetspaceResourceConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_fleetspace_resource")

        ubx.data(DbOpenapiFleetspaceResource, "example", DbOpenapiFleetspaceResourceConfig(
            fleet_name="example-fleet-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_name" type="string" required>
  The real name of the fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The FleetspaceResource items on this page Returned by the lookup.
</ResponseField>
