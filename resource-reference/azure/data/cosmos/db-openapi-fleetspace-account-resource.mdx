---
title: "azure_cosmos_db_openapi_fleetspace_account_resource"
description: "`azure_cosmos_db_openapi_fleetspace_account_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cosmos_db_openapi_fleetspace_account_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_fleetspace_account_resource"})

    		ubx.Data(cosmos.DbOpenapiFleetspaceAccountResource, "example", cosmos.DbOpenapiFleetspaceAccountResourceConfig{
    			FleetName:      "example-fleet-name",
    			FleetspaceName: "example-fleetspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiFleetspaceAccountResource } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_fleetspace_account_resource";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cosmos_db_openapi_fleetspace_account_resource",
      });

      data(DbOpenapiFleetspaceAccountResource, "example", {
        fleetName: "example-fleet-name",
        fleetspaceName: "example-fleetspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiFleetspaceAccountResource, DbOpenapiFleetspaceAccountResourceConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_fleetspace_account_resource")

        ubx.data(DbOpenapiFleetspaceAccountResource, "example", DbOpenapiFleetspaceAccountResourceConfig(
            fleet_name="example-fleet-name",
            fleetspace_name="example-fleetspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="fleetspace_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The FleetspaceAccountResource items on this page Returned by the lookup.
</ResponseField>
