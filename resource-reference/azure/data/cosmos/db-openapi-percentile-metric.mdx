---
title: "azure_cosmos_db_openapi_percentile_metric"
description: "`azure_cosmos_db_openapi_percentile_metric` reads a real, reported latency percentile metric for a Cosmos DB account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_percentile_metric"})

    		ubx.Data(cosmos.DbOpenapiPercentileMetric, "example", cosmos.DbOpenapiPercentileMetricConfig{
    			AccountName: "example-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { DbOpenapiPercentileMetric } from "./local-sdk/azure/sdk/typescript/azure/data/cosmos/db_openapi_percentile_metric.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_cosmos_db_openapi_percentile_metric" });

      data(DbOpenapiPercentileMetric, "example", {
        accountName: "example-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiPercentileMetric, DbOpenapiPercentileMetricConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_percentile_metric")

        ubx.data(DbOpenapiPercentileMetric, "example", DbOpenapiPercentileMetricConfig(
            account_name="example-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of percentile metrics for the account. Returned by the lookup.
</ResponseField>
