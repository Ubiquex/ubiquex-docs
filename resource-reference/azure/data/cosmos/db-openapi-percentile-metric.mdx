---
title: "azure_cosmos_db_openapi_percentile_metric"
description: "`azure_cosmos_db_openapi_percentile_metric` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cosmos_db_openapi_percentile_metric` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_percentile_metric"})

    		ubx.Data(cosmos.DbOpenapiPercentileMetric, "example", cosmos.DbOpenapiPercentileMetricConfig{
    			AccountName: "example-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiPercentileMetric } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_percentile_metric";

    export default stack("example", () => {
      intent({ summary: "look up azure_cosmos_db_openapi_percentile_metric" });

      data(DbOpenapiPercentileMetric, "example", {
        accountName: "example-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiPercentileMetric, DbOpenapiPercentileMetricConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_percentile_metric")

        ubx.data(DbOpenapiPercentileMetric, "example", DbOpenapiPercentileMetricConfig(
            account_name="example-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of percentile metrics for the account. Returned by the lookup.
</ResponseField>
