---
title: "azure_cosmos_db_openapi_mongo_dbcollection_get_results"
description: "`azure_cosmos_db_openapi_mongo_dbcollection_get_results` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cosmos_db_openapi_mongo_dbcollection_get_results` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_mongo_dbcollection_get_results"})

    		ubx.Data(cosmos.DbOpenapiMongoDbcollectionGetResults, "example", cosmos.DbOpenapiMongoDbcollectionGetResultsConfig{
    			AccountName:  "example-account-name",
    			DatabaseName: "example-database-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiMongoDbcollectionGetResults } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_mongo_dbcollection_get_results";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cosmos_db_openapi_mongo_dbcollection_get_results",
      });

      data(DbOpenapiMongoDbcollectionGetResults, "example", {
        accountName: "example-account-name",
        databaseName: "example-database-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiMongoDbcollectionGetResults, DbOpenapiMongoDbcollectionGetResultsConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_mongo_dbcollection_get_results")

        ubx.data(DbOpenapiMongoDbcollectionGetResults, "example", DbOpenapiMongoDbcollectionGetResultsConfig(
            account_name="example-account-name",
            database_name="example-database-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of MongoDB collections and their properties. Returned by the lookup.
</ResponseField>
