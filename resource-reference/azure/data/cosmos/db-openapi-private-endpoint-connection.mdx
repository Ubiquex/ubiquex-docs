---
title: "azure_cosmos_db_openapi_private_endpoint_connection"
description: "`data_azure_cosmos_db_openapi_private_endpoint_connection` looks up a real, already-existing `azure_cosmos_db_openapi_private_endpoint_connection`."
---

It represents the connection between a Private Endpoint and a Cosmos DB account, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_private_endpoint_connection"})

    		ubx.Data(cosmos.DbOpenapiPrivateEndpointConnection, "example", cosmos.DbOpenapiPrivateEndpointConnectionConfig{
    			AccountName: "example-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { DbOpenapiPrivateEndpointConnection } from "./local-sdk/azure/sdk/typescript/azure/data/cosmos/db_openapi_private_endpoint_connection.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cosmos_db_openapi_private_endpoint_connection",
      });

      data(DbOpenapiPrivateEndpointConnection, "example", {
        accountName: "example-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiPrivateEndpointConnection, DbOpenapiPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_private_endpoint_connection")

        ubx.data(DbOpenapiPrivateEndpointConnection, "example", DbOpenapiPrivateEndpointConnectionConfig(
            account_name="example-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of private endpoint connections Returned by the lookup.
</ResponseField>
