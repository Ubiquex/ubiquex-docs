---
title: "azure_cosmos_db_openapi_metric_definition"
description: "`azure_cosmos_db_openapi_metric_definition` reads a real, available metric definition for a Cosmos DB account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_metric_definition"})

    		ubx.Data(cosmos.DbOpenapiMetricDefinition, "example", cosmos.DbOpenapiMetricDefinitionConfig{
    			AccountName:   "example-account-name",
    			CollectionRid: "example",
    			DatabaseRid:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiMetricDefinition } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_metric_definition";

    export default stack("example", () => {
      intent({ summary: "look up azure_cosmos_db_openapi_metric_definition" });

      data(DbOpenapiMetricDefinition, "example", {
        accountName: "example-account-name",
        collectionRid: "example",
        databaseRid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiMetricDefinition, DbOpenapiMetricDefinitionConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_metric_definition")

        ubx.data(DbOpenapiMetricDefinition, "example", DbOpenapiMetricDefinitionConfig(
            account_name="example-account-name",
            collection_rid="example",
            database_rid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="collection_rid" type="string" required>
  The real, internal resource identifier (RID) of this collection. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="database_rid" type="string" required>
  The real, internal resource identifier (RID) of this database. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of metric definitions for the account. Returned by the lookup.
</ResponseField>
