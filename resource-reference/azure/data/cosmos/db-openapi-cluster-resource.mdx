---
title: "azure_cosmos_db_openapi_cluster_resource"
description: "`data_azure_cosmos_db_openapi_cluster_resource` looks up a real, already-existing `azure_cosmos_db_openapi_cluster_resource`."
---

It provisions an Azure Managed Instance for Apache Cassandra cluster, a fully managed deployment of open-source Cassandra.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_cluster_resource"})

    		ubx.Data(cosmos.DbOpenapiClusterResource, "example", cosmos.DbOpenapiClusterResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiClusterResource } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_cluster_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_cosmos_db_openapi_cluster_resource" });

      data(DbOpenapiClusterResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiClusterResource, DbOpenapiClusterResourceConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_cluster_resource")

        ubx.data(DbOpenapiClusterResource, "example", DbOpenapiClusterResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Container for the array of clusters. Returned by the lookup.
</ResponseField>
