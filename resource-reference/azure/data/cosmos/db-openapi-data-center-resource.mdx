---
title: "azure_cosmos_db_openapi_data_center_resource"
description: "`data_azure_cosmos_db_openapi_data_center_resource` looks up a real, already-existing `azure_cosmos_db_openapi_data_center_resource`."
---

It is a single data center (a group of nodes in one region) within an Azure Managed Instance for Apache Cassandra cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_data_center_resource"})

    		ubx.Data(cosmos.DbOpenapiDataCenterResource, "example", cosmos.DbOpenapiDataCenterResourceConfig{
    			ClusterName: "example-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiDataCenterResource } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_data_center_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_cosmos_db_openapi_data_center_resource" });

      data(DbOpenapiDataCenterResource, "example", {
        clusterName: "example-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiDataCenterResource, DbOpenapiDataCenterResourceConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_data_center_resource")

        ubx.data(DbOpenapiDataCenterResource, "example", DbOpenapiDataCenterResourceConfig(
            cluster_name="example-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Container for array of data centers. Returned by the lookup.
</ResponseField>
