---
title: "azure_cosmos_db_openapi_gremlin_database_get_results"
description: "`data_azure_cosmos_db_openapi_gremlin_database_get_results` looks up a real, already-existing `azure_cosmos_db_openapi_gremlin_database_get_results`."
---

It is a database within a Cosmos DB account's Gremlin (graph) API surface, the container graph(s) are organized under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_gremlin_database_get_results"})

    		ubx.Data(cosmos.DbOpenapiGremlinDatabaseGetResults, "example", cosmos.DbOpenapiGremlinDatabaseGetResultsConfig{
    			AccountName: "example-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiGremlinDatabaseGetResults } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_gremlin_database_get_results";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cosmos_db_openapi_gremlin_database_get_results",
      });

      data(DbOpenapiGremlinDatabaseGetResults, "example", {
        accountName: "example-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiGremlinDatabaseGetResults, DbOpenapiGremlinDatabaseGetResultsConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_gremlin_database_get_results")

        ubx.data(DbOpenapiGremlinDatabaseGetResults, "example", DbOpenapiGremlinDatabaseGetResultsConfig(
            account_name="example-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of Gremlin databases and their properties. Returned by the lookup.
</ResponseField>
