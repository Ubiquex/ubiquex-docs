---
title: "azure_cosmos_db_openapi_partition_metric"
description: "`azure_cosmos_db_openapi_partition_metric` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cosmos_db_openapi_partition_metric` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cosmos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cosmos_db_openapi_partition_metric"})

    		ubx.Data(cosmos.DbOpenapiPartitionMetric, "example", cosmos.DbOpenapiPartitionMetricConfig{
    			AccountName:         "example-account-name",
    			CollectionRid:       "example",
    			DatabaseRid:         "example",
    			PartitionKeyRangeId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbOpenapiPartitionMetric } from "@ubx/sdk-azure/azure/data/cosmos/db_openapi_partition_metric";

    export default stack("example", () => {
      intent({ summary: "look up azure_cosmos_db_openapi_partition_metric" });

      data(DbOpenapiPartitionMetric, "example", {
        accountName: "example-account-name",
        collectionRid: "example",
        databaseRid: "example",
        partitionKeyRangeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cosmos import DbOpenapiPartitionMetric, DbOpenapiPartitionMetricConfig

    def describe():
        ubx.intent("look up azure_cosmos_db_openapi_partition_metric")

        ubx.data(DbOpenapiPartitionMetric, "example", DbOpenapiPartitionMetricConfig(
            account_name="example-account-name",
            collection_rid="example",
            database_rid="example",
            partition_key_range_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="collection_rid" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="database_rid" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="partition_key_range_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of partition-level metrics for the account. Returned by the lookup.
</ResponseField>
