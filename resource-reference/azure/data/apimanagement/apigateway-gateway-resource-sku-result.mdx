---
title: "azure_apimanagement_apigateway_gateway_resource_sku_result"
description: "`azure_apimanagement_apigateway_gateway_resource_sku_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_apimanagement_apigateway_gateway_resource_sku_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apigateway_gateway_resource_sku_result"})

    		ubx.Data(apimanagement.ApigatewayGatewayResourceSkuResult, "example", apimanagement.ApigatewayGatewayResourceSkuResultConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApigatewayGatewayResourceSkuResult } from "@ubx/sdk-azure/azure/data/apimanagement/apigateway_gateway_resource_sku_result";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_apimanagement_apigateway_gateway_resource_sku_result",
      });

      data(ApigatewayGatewayResourceSkuResult, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApigatewayGatewayResourceSkuResult, ApigatewayGatewayResourceSkuResultConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apigateway_gateway_resource_sku_result")

        ubx.data(ApigatewayGatewayResourceSkuResult, "example", ApigatewayGatewayResourceSkuResultConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The uri to fetch the next page of API Management gateway Skus. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of skus available for the gateway. Returned by the lookup.
</ResponseField>
