---
title: "azure_apimanagement_apimworkspaces_service"
description: "`azure_apimanagement_apimworkspaces_service` looks up a real workspace -- an isolated set of APIs and products with their own delegated ownership -- in..."
---

`azure_apimanagement_apimworkspaces_service` looks up a real workspace -- an isolated set of APIs and products with their own delegated ownership -- in API Management.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimworkspaces_service"})

    		ubx.Data(apimanagement.ApimworkspacesService, "example", apimanagement.ApimworkspacesServiceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApimworkspacesService } from "@ubx/sdk-azure/azure/data/apimanagement/apimworkspaces_service";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimworkspaces_service" });

      data(ApimworkspacesService, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimworkspacesService, ApimworkspacesServiceConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimworkspaces_service")

        ubx.data(ApimworkspacesService, "example", ApimworkspacesServiceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="count" type="number">
  Total record count number across all pages. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  Next page link if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Page values. Returned by the lookup.
</ResponseField>
