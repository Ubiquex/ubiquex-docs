---
title: "azure_apimanagement_apimapis_wiki"
description: "`azure_apimanagement_apimapis_wiki` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_apimanagement_apimapis_wiki` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimapis_wiki"})

    		ubx.Data(apimanagement.ApimapisWiki, "example", apimanagement.ApimapisWikiConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApimapisWiki } from "@ubx/sdk-azure/azure/data/apimanagement/apimapis_wiki";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimapis_wiki" });

      data(ApimapisWiki, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimapisWiki, ApimapisWikiConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimapis_wiki")

        ubx.data(ApimapisWiki, "example", ApimapisWikiConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="properties" type="object">
  Wiki contract details Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="documents" type="list(object)">
      Collection wiki documents included into this wiki. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
