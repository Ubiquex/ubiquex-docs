---
title: "azure_apimanagement_apimusers_user"
description: "`data_azure_apimanagement_apimusers_user` looks up a real, already-existing `azure_apimanagement_apimusers_user`."
---

It is a registered developer account in Azure API Management, that subscription(s) and their own API key(s) belong to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimusers_user"})

    		ubx.Data(apimanagement.ApimusersUser, "example", apimanagement.ApimusersUserConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ApimusersUser } from "./local-sdk/azure/sdk/typescript/azure/data/apimanagement/apimusers_user.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimusers_user" });

      data(ApimusersUser, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimusersUser, ApimusersUserConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimusers_user")

        ubx.data(ApimusersUser, "example", ApimusersUserConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="count" type="number">
  Total record count number across all pages. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  Next page link if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Page values. Returned by the lookup.
</ResponseField>
