---
title: "azure_apimanagement_apimworkspaces_product"
description: "`azure_apimanagement_apimworkspaces_product` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_apimanagement_apimworkspaces_product` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimworkspaces_product"})

    		ubx.Data(apimanagement.ApimworkspacesProduct, "example", apimanagement.ApimworkspacesProductConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApimworkspacesProduct } from "@ubx/sdk-azure/azure/data/apimanagement/apimworkspaces_product";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimworkspaces_product" });

      data(ApimworkspacesProduct, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimworkspacesProduct, ApimworkspacesProductConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimworkspaces_product")

        ubx.data(ApimworkspacesProduct, "example", ApimworkspacesProductConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="count" type="number">
  Total record count number across all pages. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  Next page link if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Page values. Returned by the lookup.
</ResponseField>
