---
title: "azure_apimanagement_apimusers_subscription_contract_2"
description: "`data_azure_apimanagement_apimusers_subscription_contract_2` grants a specific developer access to product(s) in Azure API Management..."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimusers_subscription_contract_2"})

    		ubx.Data(apimanagement.ApimusersSubscriptionContract2, "example", apimanagement.ApimusersSubscriptionContract2Config{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApimusersSubscriptionContract2 } from "@ubx/sdk-azure/azure/data/apimanagement/apimusers_subscription_contract_2";

    export default stack("example", () => {
      intent({
        summary: "look up azure_apimanagement_apimusers_subscription_contract_2",
      });

      data(ApimusersSubscriptionContract2, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimusersSubscriptionContract2, ApimusersSubscriptionContract2Config

    def describe():
        ubx.intent("look up azure_apimanagement_apimusers_subscription_contract_2")

        ubx.data(ApimusersSubscriptionContract2, "example", ApimusersSubscriptionContract2Config(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="properties" type="object">
  Subscription details. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="allow_tracing" type="bool">
      Determines whether tracing is enabled Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_date" type="string">
      Subscription creation date. The date conforms to the following format: `yyyy-MM-ddTHH:mm:ssZ` as specified by the ISO 8601 standard. Returned by the lookup.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The name of the subscription, or null if the subscription has no name. Optional lookup argument.
    </ResponseField>
    <ResponseField name="end_date" type="string">
      Date when subscription was cancelled or expired. The setting is for audit purposes only and the subscription is not automatically cancelled. The subscription lifecycle can be managed by using the `state` property. The date conforms to the following format: `yyyy-MM-ddTHH:mm:ssZ` as specified by the ISO 8601 standard. Optional lookup argument.
    </ResponseField>
    <ResponseField name="expiration_date" type="string">
      Subscription expiration date. The setting is for audit purposes only and the subscription is not automatically expired. The subscription lifecycle can be managed by using the `state` property. The date conforms to the following format: `yyyy-MM-ddTHH:mm:ssZ` as specified by the ISO 8601 standard. Optional lookup argument.
    </ResponseField>
    <ResponseField name="notification_date" type="string">
      Upcoming subscription expiration notification date. The date conforms to the following format: `yyyy-MM-ddTHH:mm:ssZ` as specified by the ISO 8601 standard. Optional lookup argument.
    </ResponseField>
    <ResponseField name="owner_id" type="string">
      The user resource identifier of the subscription owner. The value is a valid relative URL in the format of /users/&#123;userId&#125; where &#123;userId&#125; is a user identifier. Optional lookup argument.
    </ResponseField>
    <ResponseField name="primary_key" type="string">
      Subscription primary key. This property will not be filled on 'GET' operations! Use '/listSecrets' POST request to get the value. Optional lookup argument.
    </ResponseField>
    <ResponseField name="scope" type="string" required>
      Scope like /products/&#123;productId&#125; or /apis or /apis/&#123;apiId&#125;. Required lookup argument.
    </ResponseField>
    <ResponseField name="secondary_key" type="string">
      Subscription secondary key. This property will not be filled on 'GET' operations! Use '/listSecrets' POST request to get the value. Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_date" type="string">
      Subscription activation date. The setting is for audit purposes only and the subscription is not automatically activated. The subscription lifecycle can be managed by using the `state` property. The date conforms to the following format: `yyyy-MM-ddTHH:mm:ssZ` as specified by the ISO 8601 standard. Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string" required>
      Subscription state. Possible states are * active  --  the subscription is active, * suspended  --  the subscription is blocked, and the subscriber cannot call any APIs of the product, * submitted  --  the subscription request has been made by the developer, but has not yet been approved or rejected, * rejected  --  the subscription request has been denied by an administrator, * cancelled  --  the subscription has been cancelled by the developer or administrator, * expired  --  the subscription reached its expiration date and was deactivated. Required lookup argument.
    </ResponseField>
    <ResponseField name="state_comment" type="string">
      Optional subscription comment added by an administrator when the state is changed to the 'rejected'. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
