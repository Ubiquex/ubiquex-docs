---
title: "azure_apimanagement_apimapis_api"
description: "`data_azure_apimanagement_apimapis_api` looks up a real, already-existing `azure_apimanagement_apimapis_api`."
---

It is an API published in Azure API Management, the resource that operation(s), policy(ies), and product association(s) are organized under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimapis_api"})

    		ubx.Data(apimanagement.ApimapisApi, "example", apimanagement.ApimapisApiConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApimapisApi } from "@ubx/sdk-azure/azure/data/apimanagement/apimapis_api";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimapis_api" });

      data(ApimapisApi, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimapisApi, ApimapisApiConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimapis_api")

        ubx.data(ApimapisApi, "example", ApimapisApiConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="count" type="number">
  Total record count number across all pages. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  Next page link if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Page values. Returned by the lookup.
</ResponseField>
