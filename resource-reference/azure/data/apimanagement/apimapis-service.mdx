---
title: "azure_apimanagement_apimapis_service"
description: "`azure_apimanagement_apimapis_service` looks up a real API published in an Azure API Management instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimapis_service"})

    		ubx.Data(apimanagement.ApimapisService, "example", apimanagement.ApimapisServiceConfig{
    			ExpandApiVersionSet: true,
    			Tags:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApimapisService } from "@ubx/sdk-azure/azure/data/apimanagement/apimapis_service";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimapis_service" });

      data(ApimapisService, "example", {
        expandApiVersionSet: true,
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimapisService, ApimapisServiceConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimapis_service")

        ubx.data(ApimapisService, "example", ApimapisServiceConfig(
            expand_api_version_set=True,
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="expand_api_version_set" type="bool">
  Whether the related API version set is expanded and included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tags" type="string">
  The real tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="count" type="number">
  Total record count number across all pages. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  Next page link if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Page values. Returned by the lookup.
</ResponseField>
