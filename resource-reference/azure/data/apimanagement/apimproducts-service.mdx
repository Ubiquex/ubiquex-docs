---
title: "azure_apimanagement_apimproducts_service"
description: "`azure_apimanagement_apimproducts_service` looks up a real product -- a bundle of one or more APIs published together..."
---

`azure_apimanagement_apimproducts_service` looks up a real product -- a bundle of one or more APIs published together, with its own subscription requirement and quota -- in API Management.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimproducts_service"})

    		ubx.Data(apimanagement.ApimproductsService, "example", apimanagement.ApimproductsServiceConfig{
    			ExpandGroups: true,
    			Tags:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ApimproductsService } from "./local-sdk/azure/sdk/typescript/azure/data/apimanagement/apimproducts_service.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimproducts_service" });

      data(ApimproductsService, "example", {
        expandGroups: true,
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimproductsService, ApimproductsServiceConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimproducts_service")

        ubx.data(ApimproductsService, "example", ApimproductsServiceConfig(
            expand_groups=True,
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="expand_groups" type="bool">
  Whether related groups are expanded and included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tags" type="string">
  The real tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="count" type="number">
  Total record count number across all pages. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  Next page link if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Page values. Returned by the lookup.
</ResponseField>
