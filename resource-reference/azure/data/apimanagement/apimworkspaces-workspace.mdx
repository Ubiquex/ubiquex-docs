---
title: "azure_apimanagement_apimworkspaces_workspace"
description: "`data_azure_apimanagement_apimworkspaces_workspace` looks up a real, already-existing `azure_apimanagement_apimworkspaces_workspace`."
---

It is a self-contained administrative boundary within an API Management instance, letting a decentralized team own and manage its own API(s), product(s), and policy(ies) independently of the central platform team.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimworkspaces_workspace"})

    		ubx.Data(apimanagement.ApimworkspacesWorkspace, "example", apimanagement.ApimworkspacesWorkspaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApimworkspacesWorkspace } from "@ubx/sdk-azure/azure/data/apimanagement/apimworkspaces_workspace";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimworkspaces_workspace" });

      data(ApimworkspacesWorkspace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimworkspacesWorkspace, ApimworkspacesWorkspaceConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimworkspaces_workspace")

        ubx.data(ApimworkspacesWorkspace, "example", ApimworkspacesWorkspaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="count" type="number">
  Total record count number across all pages. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  Next page link if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Page values. Returned by the lookup.
</ResponseField>
