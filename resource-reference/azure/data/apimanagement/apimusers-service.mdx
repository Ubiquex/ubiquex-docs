---
title: "azure_apimanagement_apimusers_service"
description: "`azure_apimanagement_apimusers_service` looks up a real developer account registered in API Management."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimusers_service"})

    		ubx.Data(apimanagement.ApimusersService, "example", apimanagement.ApimusersServiceConfig{
    			ExpandGroups: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApimusersService } from "@ubx/sdk-azure/azure/data/apimanagement/apimusers_service";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimusers_service" });

      data(ApimusersService, "example", {
        expandGroups: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimusersService, ApimusersServiceConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimusers_service")

        ubx.data(ApimusersService, "example", ApimusersServiceConfig(
            expand_groups=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="expand_groups" type="bool">
  Whether related groups are expanded and included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="count" type="number">
  Total record count number across all pages. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  Next page link if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Page values. Returned by the lookup.
</ResponseField>
