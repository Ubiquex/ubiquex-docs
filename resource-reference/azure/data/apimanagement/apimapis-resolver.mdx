---
title: "azure_apimanagement_apimapis_resolver"
description: "`data_azure_apimanagement_apimapis_resolver` looks up a real, already-existing `azure_apimanagement_apimapis_resolver`."
---

It defines the backend data-fetching logic for a specific field on a GraphQL API managed through Azure API Management's own GraphQL passthrough or synthetic API support.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimapis_resolver"})

    		ubx.Data(apimanagement.ApimapisResolver, "example", apimanagement.ApimapisResolverConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApimapisResolver } from "@ubx/sdk-azure/azure/data/apimanagement/apimapis_resolver";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimapis_resolver" });

      data(ApimapisResolver, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimapisResolver, ApimapisResolverConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimapis_resolver")

        ubx.data(ApimapisResolver, "example", ApimapisResolverConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="count" type="number">
  Total record count number. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  Next page link if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Policy Contract value. Returned by the lookup.
</ResponseField>
