---
title: "azure_apimanagement_apimworkspaces_api"
description: "`data_azure_apimanagement_apimworkspaces_api` looks up a real, already-existing `azure_apimanagement_apimworkspaces_api`."
---

It is an API published within an Azure API Management workspace, scoped to and managed by that workspace's own decentralized team rather than the central platform team.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_apimanagement_apimworkspaces_api"})

    		ubx.Data(apimanagement.ApimworkspacesApi, "example", apimanagement.ApimworkspacesApiConfig{
    			Tags: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApimworkspacesApi } from "@ubx/sdk-azure/azure/data/apimanagement/apimworkspaces_api";

    export default stack("example", () => {
      intent({ summary: "look up azure_apimanagement_apimworkspaces_api" });

      data(ApimworkspacesApi, "example", {
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.apimanagement import ApimworkspacesApi, ApimworkspacesApiConfig

    def describe():
        ubx.intent("look up azure_apimanagement_apimworkspaces_api")

        ubx.data(ApimworkspacesApi, "example", ApimworkspacesApiConfig(
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="tags" type="string">
  The real tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="count" type="number">
  Total record count number across all pages. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  Next page link if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Page values. Returned by the lookup.
</ResponseField>
