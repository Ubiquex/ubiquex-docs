---
title: "azure_marketplace_request_approval_resource"
description: "`data_azure_marketplace_request_approval_resource` looks up a real, already-existing `azure_marketplace_request_approval_resource`."
---

It is a pending user request for access to a private, restricted Azure Marketplace offer.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	marketplace "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_marketplace_request_approval_resource"})

    		ubx.Data(marketplace.RequestApprovalResource, "example", marketplace.RequestApprovalResourceConfig{
    			PrivateStoreId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RequestApprovalResource } from "@ubx/sdk-azure/azure/data/marketplace/request_approval_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_marketplace_request_approval_resource" });

      data(RequestApprovalResource, "example", {
        privateStoreId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.marketplace import RequestApprovalResource, RequestApprovalResourceConfig

    def describe():
        ubx.intent("look up azure_marketplace_request_approval_resource")

        ubx.data(RequestApprovalResource, "example", RequestApprovalResourceConfig(
            private_store_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="private_store_id" type="string" required>
  The real, unique identifier of the Azure Marketplace private store this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of notifications list results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
