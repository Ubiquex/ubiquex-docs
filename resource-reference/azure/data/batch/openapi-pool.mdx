---
title: "azure_batch_openapi_pool"
description: "`azure_batch_openapi_pool` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_batch_openapi_pool` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_batch_openapi_pool"})

    		ubx.Data(batch.OpenapiPool, "example", batch.OpenapiPoolConfig{
    			AccountName: "example-account-name",
    			Maxresults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiPool } from "@ubx/sdk-azure/azure/data/batch/openapi_pool";

    export default stack("example", () => {
      intent({ summary: "look up azure_batch_openapi_pool" });

      data(OpenapiPool, "example", {
        accountName: "example-account-name",
        maxresults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.batch import OpenapiPool, OpenapiPoolConfig

    def describe():
        ubx.intent("look up azure_batch_openapi_pool")

        ubx.data(OpenapiPool, "example", OpenapiPoolConfig(
            account_name="example-account-name",
            maxresults=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="maxresults" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Pool items on this page Returned by the lookup.
</ResponseField>
