---
title: "azure_batch_openapi_application_package"
description: "`data_azure_batch_openapi_application_package` looks up a real, already-existing `azure_batch_openapi_application_package`."
---

It is a specific uploaded version of an Azure Batch application, containing the actual executable and supporting file(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_batch_openapi_application_package"})

    		ubx.Data(batch.OpenapiApplicationPackage, "example", batch.OpenapiApplicationPackageConfig{
    			AccountName:     "example-account-name",
    			ApplicationName: "example-application-name",
    			Maxresults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiApplicationPackage } from "./local-sdk/azure/sdk/typescript/azure/data/batch/openapi_application_package.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_batch_openapi_application_package" });

      data(OpenapiApplicationPackage, "example", {
        accountName: "example-account-name",
        applicationName: "example-application-name",
        maxresults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.batch import OpenapiApplicationPackage, OpenapiApplicationPackageConfig

    def describe():
        ubx.intent("look up azure_batch_openapi_application_package")

        ubx.data(OpenapiApplicationPackage, "example", OpenapiApplicationPackageConfig(
            account_name="example-account-name",
            application_name="example-application-name",
            maxresults=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="application_name" type="string" required>
  The real name of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="maxresults" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ApplicationPackage items on this page Returned by the lookup.
</ResponseField>
