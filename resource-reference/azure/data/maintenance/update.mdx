---
title: "azure_maintenance_update"
description: "`azure_maintenance_update` reads a real, applicable maintenance update for a resource enrolled in Azure Maintenance Configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	maintenance "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/maintenance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_maintenance_update"})

    		ubx.Data(maintenance.Update, "example", maintenance.UpdateConfig{
    			ApiVersion:         "example",
    			ProviderName:       "example-provider-name",
    			ResourceGroupName:  "example-resource-group-name",
    			ResourceName:       "example-resource-name",
    			ResourceParentName: "example-resource-parent-name",
    			ResourceParentType: "example",
    			ResourceType:       "example",
    			SubscriptionId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { Update } from "./local-sdk/azure/sdk/typescript/azure/data/maintenance/update.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_maintenance_update" });

      data(Update, "example", {
        apiVersion: "example",
        providerName: "example-provider-name",
        resourceGroupName: "example-resource-group-name",
        resourceName: "example-resource-name",
        resourceParentName: "example-resource-parent-name",
        resourceParentType: "example",
        resourceType: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.maintenance import Update, UpdateConfig

    def describe():
        ubx.intent("look up azure_maintenance_update")

        ubx.data(Update, "example", UpdateConfig(
            api_version="example",
            provider_name="example-provider-name",
            resource_group_name="example-resource-group-name",
            resource_name="example-resource-name",
            resource_parent_name="example-resource-parent-name",
            resource_parent_type="example",
            resource_type="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="provider_name" type="string" required>
  The real name of the resource provider this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_parent_name" type="string" required>
  The real name of the parent resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_parent_type" type="string" required>
  The real type of the parent resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real Azure resource type (such as `Microsoft.Compute/virtualMachines`) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The pending updates Returned by the lookup.
</ResponseField>
