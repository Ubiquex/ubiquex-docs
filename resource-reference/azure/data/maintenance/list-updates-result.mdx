---
title: "azure_maintenance_list_updates_result"
description: "`azure_maintenance_list_updates_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_maintenance_list_updates_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	maintenance "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/maintenance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_maintenance_list_updates_result"})

    		ubx.Data(maintenance.ListUpdatesResult, "example", maintenance.ListUpdatesResultConfig{
    			ApiVersion:         "example",
    			ProviderName:       "example-provider-name",
    			ResourceGroupName:  "example-resource-group-name",
    			ResourceName:       "example-resource-name",
    			ResourceParentName: "example-resource-parent-name",
    			ResourceParentType: "example",
    			ResourceType:       "example",
    			SubscriptionId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ListUpdatesResult } from "@ubx/sdk-azure/azure/data/maintenance/list_updates_result";

    export default stack("example", () => {
      intent({ summary: "look up azure_maintenance_list_updates_result" });

      data(ListUpdatesResult, "example", {
        apiVersion: "example",
        providerName: "example-provider-name",
        resourceGroupName: "example-resource-group-name",
        resourceName: "example-resource-name",
        resourceParentName: "example-resource-parent-name",
        resourceParentType: "example",
        resourceType: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.maintenance import ListUpdatesResult, ListUpdatesResultConfig

    def describe():
        ubx.intent("look up azure_maintenance_list_updates_result")

        ubx.data(ListUpdatesResult, "example", ListUpdatesResultConfig(
            api_version="example",
            provider_name="example-provider-name",
            resource_group_name="example-resource-group-name",
            resource_name="example-resource-name",
            resource_parent_name="example-resource-parent-name",
            resource_parent_type="example",
            resource_type="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="provider_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_parent_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_parent_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The pending updates Returned by the lookup.
</ResponseField>
