---
title: "azure_search_network_security_perimeter_configuration_list_result"
description: "`azure_search_network_security_perimeter_configuration_list_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_search_network_security_perimeter_configuration_list_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	search "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/search"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_search_network_security_perimeter_configuration_list_result"})

    		ubx.Data(search.NetworkSecurityPerimeterConfigurationListResult, "example", search.NetworkSecurityPerimeterConfigurationListResultConfig{
    			SearchServiceName: "example-search-service-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkSecurityPerimeterConfigurationListResult } from "@ubx/sdk-azure/azure/data/search/network_security_perimeter_configuration_list_result";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_search_network_security_perimeter_configuration_list_result",
      });

      data(NetworkSecurityPerimeterConfigurationListResult, "example", {
        searchServiceName: "example-search-service-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.search import NetworkSecurityPerimeterConfigurationListResult, NetworkSecurityPerimeterConfigurationListResultConfig

    def describe():
        ubx.intent("look up azure_search_network_security_perimeter_configuration_list_result")

        ubx.data(NetworkSecurityPerimeterConfigurationListResult, "example", NetworkSecurityPerimeterConfigurationListResultConfig(
            search_service_name="example-search-service-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="search_service_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NetworkSecurityPerimeterConfiguration items on this page Returned by the lookup.
</ResponseField>
