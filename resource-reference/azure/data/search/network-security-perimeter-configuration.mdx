---
title: "azure_search_network_security_perimeter_configuration"
description: "`azure_search_network_security_perimeter_configuration` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_search_network_security_perimeter_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	search "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/search"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_search_network_security_perimeter_configuration"})

    		ubx.Data(search.NetworkSecurityPerimeterConfiguration, "example", search.NetworkSecurityPerimeterConfigurationConfig{
    			NspConfigName:     "example-nsp-config-name",
    			SearchServiceName: "example-search-service-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkSecurityPerimeterConfiguration } from "@ubx/sdk-azure/azure/data/search/network_security_perimeter_configuration";

    export default stack("example", () => {
      intent({
        summary: "look up azure_search_network_security_perimeter_configuration",
      });

      data(NetworkSecurityPerimeterConfiguration, "example", {
        nspConfigName: "example-nsp-config-name",
        searchServiceName: "example-search-service-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.search import NetworkSecurityPerimeterConfiguration, NetworkSecurityPerimeterConfigurationConfig

    def describe():
        ubx.intent("look up azure_search_network_security_perimeter_configuration")

        ubx.data(NetworkSecurityPerimeterConfiguration, "example", NetworkSecurityPerimeterConfigurationConfig(
            nsp_config_name="example-nsp-config-name",
            search_service_name="example-search-service-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="nsp_config_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="search_service_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  Network security configuration properties. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="network_security_perimeter" type="object">
      Information about a network security perimeter (NSP) Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Fully qualified Azure resource ID of the NSP resource Optional lookup argument.
        </ResponseField>
        <ResponseField name="location" type="string">
          Location of the network security perimeter Optional lookup argument.
        </ResponseField>
        <ResponseField name="perimeter_guid" type="string">
          Universal unique ID (UUID) of the network security perimeter Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="profile" type="object">
      Network security perimeter configuration profile Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="access_rules" type="list(object)">
          List of Access Rules Optional lookup argument.
        </ResponseField>
        <ResponseField name="access_rules_version" type="number">
          Current access rules version Optional lookup argument.
        </ResponseField>
        <ResponseField name="diagnostic_settings_version" type="number">
          Current diagnostic settings version Optional lookup argument.
        </ResponseField>
        <ResponseField name="enabled_log_categories" type="list(string)">
          List of log categories that are enabled Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the profile Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_issues" type="list(object)">
      List of provisioning issues, if any Returned by the lookup.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of a network security perimeter configuration that is being created or updated. Returned by the lookup.
    </ResponseField>
    <ResponseField name="resource_association" type="object">
      Information about resource association Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="access_mode" type="string">
          Access mode of the resource association Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the resource association Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
