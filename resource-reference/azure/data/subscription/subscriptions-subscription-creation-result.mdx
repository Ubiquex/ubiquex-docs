---
title: "azure_subscription_subscriptions_subscription_creation_result"
description: "`azure_subscription_subscriptions_subscription_creation_result` reads the real, current status of creating a new Azure subscription."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	subscription "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/subscription"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_subscription_subscriptions_subscription_creation_result"})

    		ubx.Data(subscription.SubscriptionsSubscriptionCreationResult, "example", subscription.SubscriptionsSubscriptionCreationResultConfig{
    			ApiVersion:  "example",
    			OperationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { SubscriptionsSubscriptionCreationResult } from "./local-sdk/azure/sdk/typescript/azure/data/subscription/subscriptions_subscription_creation_result.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_subscription_subscriptions_subscription_creation_result",
      });

      data(SubscriptionsSubscriptionCreationResult, "example", {
        apiVersion: "example",
        operationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.subscription import SubscriptionsSubscriptionCreationResult, SubscriptionsSubscriptionCreationResultConfig

    def describe():
        ubx.intent("look up azure_subscription_subscriptions_subscription_creation_result")

        ubx.data(SubscriptionsSubscriptionCreationResult, "example", SubscriptionsSubscriptionCreationResultConfig(
            api_version="example",
            operation_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="operation_id" type="string" required>
  The real, unique identifier of this asynchronous operation. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="subscription_link" type="string">
  The link to the new subscription. Use this link to check the status of subscription creation operation. Returned by the lookup.
</ResponseField>
