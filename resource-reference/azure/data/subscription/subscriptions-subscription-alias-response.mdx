---
title: "azure_subscription_subscriptions_subscription_alias_response"
description: "`data_azure_subscription_subscriptions_subscription_alias_response` looks up a real, already-existing..."
---

`data_azure_subscription_subscriptions_subscription_alias_response` looks up a real, already-existing `azure_subscription_subscriptions_subscription_alias_response`. It creates a new Azure subscription (or renames/moves an existing one) via the subscription alias API.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	subscription "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/subscription"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_subscription_subscriptions_subscription_alias_response"})

    		ubx.Data(subscription.SubscriptionsSubscriptionAliasResponse, "example", subscription.SubscriptionsSubscriptionAliasResponseConfig{
    			ApiVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { SubscriptionsSubscriptionAliasResponse } from "./local-sdk/azure/sdk/typescript/azure/data/subscription/subscriptions_subscription_alias_response.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_subscription_subscriptions_subscription_alias_response",
      });

      data(SubscriptionsSubscriptionAliasResponse, "example", {
        apiVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.subscription import SubscriptionsSubscriptionAliasResponse, SubscriptionsSubscriptionAliasResponseConfig

    def describe():
        ubx.intent("look up azure_subscription_subscriptions_subscription_alias_response")

        ubx.data(SubscriptionsSubscriptionAliasResponse, "example", SubscriptionsSubscriptionAliasResponseConfig(
            api_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of alias. Returned by the lookup.
</ResponseField>
