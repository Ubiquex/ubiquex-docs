---
title: "azure_subscription_subscriptions_accept_ownership_status_response"
description: "`azure_subscription_subscriptions_accept_ownership_status_response` reads the real, current status of accepting ownership transfer of an Azure..."
---

`azure_subscription_subscriptions_accept_ownership_status_response` reads the real, current status of accepting ownership transfer of an Azure subscription.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	subscription "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/subscription"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_subscription_subscriptions_accept_ownership_status_response"})

    		ubx.Data(subscription.SubscriptionsAcceptOwnershipStatusResponse, "example", subscription.SubscriptionsAcceptOwnershipStatusResponseConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { SubscriptionsAcceptOwnershipStatusResponse } from "./local-sdk/azure/sdk/typescript/azure/data/subscription/subscriptions_accept_ownership_status_response.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_subscription_subscriptions_accept_ownership_status_response",
      });

      data(SubscriptionsAcceptOwnershipStatusResponse, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.subscription import SubscriptionsAcceptOwnershipStatusResponse, SubscriptionsAcceptOwnershipStatusResponseConfig

    def describe():
        ubx.intent("look up azure_subscription_subscriptions_accept_ownership_status_response")

        ubx.data(SubscriptionsAcceptOwnershipStatusResponse, "example", SubscriptionsAcceptOwnershipStatusResponseConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accept_ownership_state" type="string">
  The accept ownership state of the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="billing_owner" type="string">
  UPN of the billing owner Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name of the subscription. Returned by the lookup.
</ResponseField>

<ResponseField name="provisioning_state" type="string">
  The provisioning state of the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="subscription_tenant_id" type="string">
  Tenant Id of the subscription Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags for the subscription Returned by the lookup.
</ResponseField>
