---
title: "azure_chaos_openapi_target_type"
description: "`azure_chaos_openapi_target_type` reads the real, available fault-injection capabilities for a type of Chaos Studio target (e.g. Virtual Machine, AKS Pod)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	chaos "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/chaos"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_chaos_openapi_target_type"})

    		ubx.Data(chaos.OpenapiTargetType, "example", chaos.OpenapiTargetTypeConfig{
    			ContinuationToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiTargetType } from "./local-sdk/azure/sdk/typescript/azure/data/chaos/openapi_target_type.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_chaos_openapi_target_type" });

      data(OpenapiTargetType, "example", {
        continuationToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.chaos import OpenapiTargetType, OpenapiTargetTypeConfig

    def describe():
        ubx.intent("look up azure_chaos_openapi_target_type")

        ubx.data(OpenapiTargetType, "example", OpenapiTargetTypeConfig(
            continuation_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="continuation_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The TargetType items on this page Returned by the lookup.
</ResponseField>
