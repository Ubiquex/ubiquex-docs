---
title: "azure_vi_account"
description: "`data_azure_vi_account` looks up a real, already-existing `azure_vi_account`."
---

It is an Azure Video Indexer account, providing AI-powered extraction of insights (transcripts, faces, topics, brands) from video and audio content.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	vi "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/vi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_vi_account"})

    		ubx.Data(vi.Account, "example", vi.AccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { Account } from "./local-sdk/azure/sdk/typescript/azure/data/vi/account.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_vi_account" });

      data(Account, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.vi import Account, AccountConfig

    def describe():
        ubx.intent("look up azure_vi_account")

        ubx.data(Account, "example", AccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of accounts and their properties. Returned by the lookup.
</ResponseField>
