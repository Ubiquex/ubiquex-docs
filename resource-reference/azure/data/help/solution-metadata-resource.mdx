---
title: "azure_help_solution_metadata_resource"
description: "`azure_help_solution_metadata_resource` reads real, published metadata about a self-help troubleshooting solution offered by Azure."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	help "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/help"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_help_solution_metadata_resource"})

    		ubx.Data(help.SolutionMetadataResource, "example", help.SolutionMetadataResourceConfig{
    			ApiVersion: "example",
    			Scope:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SolutionMetadataResource } from "@ubx/sdk-azure/azure/data/help/solution_metadata_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_help_solution_metadata_resource" });

      data(SolutionMetadataResource, "example", {
        apiVersion: "example",
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.help import SolutionMetadataResource, SolutionMetadataResourceConfig

    def describe():
        ubx.intent("look up azure_help_solution_metadata_resource")

        ubx.data(SolutionMetadataResource, "example", SolutionMetadataResourceConfig(
            api_version="example",
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real Azure Resource Manager scope (subscription, resource group, or resource) this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of solution metadata. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of solution metadata. Returned by the lookup.
</ResponseField>
