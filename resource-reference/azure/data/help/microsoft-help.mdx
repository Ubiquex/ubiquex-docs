---
title: "azure_help_microsoft_help"
description: "`azure_help_microsoft_help` looks up a real Azure support diagnostic resource created under the Help + support blade."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	help "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/help"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_help_microsoft_help"})

    		ubx.Data(help.MicrosoftHelp, "example", help.MicrosoftHelpConfig{
    			ApiVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MicrosoftHelp } from "./local-sdk/azure/sdk/typescript/azure/data/help/microsoft_help.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_help_microsoft_help" });

      data(MicrosoftHelp, "example", {
        apiVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.help import MicrosoftHelp, MicrosoftHelpConfig

    def describe():
        ubx.intent("look up azure_help_microsoft_help")

        ubx.data(MicrosoftHelp, "example", MicrosoftHelpConfig(
            api_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
