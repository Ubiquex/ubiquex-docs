---
title: "azure_trafficmanager_heat_map_model"
description: "`azure_trafficmanager_heat_map_model` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_trafficmanager_heat_map_model` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	trafficmanager "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/trafficmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_trafficmanager_heat_map_model"})

    		ubx.Data(trafficmanager.HeatMapModel, "example", trafficmanager.HeatMapModelConfig{
    			HeatMapType: "example",
    			ProfileName: "example-profile-name",
    			BotRight:    []string{"example"},
    			TopLeft:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HeatMapModel } from "@ubx/sdk-azure/azure/data/trafficmanager/heat_map_model";

    export default stack("example", () => {
      intent({ summary: "look up azure_trafficmanager_heat_map_model" });

      data(HeatMapModel, "example", {
        heatMapType: "example",
        profileName: "example-profile-name",
        botRight: ["example"],
        topLeft: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.trafficmanager import HeatMapModel, HeatMapModelConfig

    def describe():
        ubx.intent("look up azure_trafficmanager_heat_map_model")

        ubx.data(HeatMapModel, "example", HeatMapModelConfig(
            heat_map_type="example",
            profile_name="example-profile-name",
            bot_right=["example"],
            top_left=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_right" type="list(number)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="heat_map_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="top_left" type="list(number)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  Class representing a Traffic Manager HeatMap properties. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
      The ending of the time window for this HeatMap, exclusive. Optional lookup argument.
    </ResponseField>
    <ResponseField name="endpoints" type="list(object)">
      The endpoints used in this HeatMap calculation. Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      The beginning of the time window for this HeatMap, inclusive. Optional lookup argument.
    </ResponseField>
    <ResponseField name="traffic_flows" type="list(object)">
      The traffic flows produced in this HeatMap calculation. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
