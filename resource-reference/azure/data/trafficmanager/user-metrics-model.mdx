---
title: "azure_trafficmanager_user_metrics_model"
description: "`azure_trafficmanager_user_metrics_model` reads the real, unique key used to enable Traffic Manager's own real user measurements."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	trafficmanager "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/trafficmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_trafficmanager_user_metrics_model"})

    		ubx.Data(trafficmanager.UserMetricsModel, "example", trafficmanager.UserMetricsModelConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UserMetricsModel } from "@ubx/sdk-azure/azure/data/trafficmanager/user_metrics_model";

    export default stack("example", () => {
      intent({ summary: "look up azure_trafficmanager_user_metrics_model" });

      data(UserMetricsModel, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.trafficmanager import UserMetricsModel, UserMetricsModelConfig

    def describe():
        ubx.intent("look up azure_trafficmanager_user_metrics_model")

        ubx.data(UserMetricsModel, "example", UserMetricsModelConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="properties" type="object">
  Class representing a Traffic Manager Real User Metrics key response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key returned by the User Metrics operation. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
