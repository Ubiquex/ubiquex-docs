---
title: "azure_trafficmanager_traffic_manager_geographic_hierarchy"
description: "`azure_trafficmanager_traffic_manager_geographic_hierarchy` reads Microsoft's own real, published geographic region hierarchy used by Traffic Manager's..."
---

`azure_trafficmanager_traffic_manager_geographic_hierarchy` reads Microsoft's own real, published geographic region hierarchy used by Traffic Manager's Geographic routing method.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	trafficmanager "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/trafficmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_trafficmanager_traffic_manager_geographic_hierarchy"})

    		ubx.Data(trafficmanager.TrafficManagerGeographicHierarchy, "example", trafficmanager.TrafficManagerGeographicHierarchyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TrafficManagerGeographicHierarchy } from "@ubx/sdk-azure/azure/data/trafficmanager/traffic_manager_geographic_hierarchy";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_trafficmanager_traffic_manager_geographic_hierarchy",
      });

      data(TrafficManagerGeographicHierarchy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.trafficmanager import TrafficManagerGeographicHierarchy, TrafficManagerGeographicHierarchyConfig

    def describe():
        ubx.intent("look up azure_trafficmanager_traffic_manager_geographic_hierarchy")

        ubx.data(TrafficManagerGeographicHierarchy, "example", TrafficManagerGeographicHierarchyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="properties" type="object">
  Class representing the properties of the Geographic hierarchy used with the Geographic traffic routing method. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="geographic_hierarchy" type="object">
      Class representing a region in the Geographic hierarchy used with the Geographic traffic routing method. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          The code of the region Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the region Optional lookup argument.
        </ResponseField>
        <ResponseField name="regions" type="list(object)">
          The list of Regions grouped under this Region in the Geographic Hierarchy. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
