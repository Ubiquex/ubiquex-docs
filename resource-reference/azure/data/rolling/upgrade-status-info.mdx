---
title: "azure_rolling_upgrade_status_info"
description: "`azure_rolling_upgrade_status_info` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_rolling_upgrade_status_info` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rolling "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/rolling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_rolling_upgrade_status_info"})

    		ubx.Data(rolling.UpgradeStatusInfo, "example", rolling.UpgradeStatusInfoConfig{
    			VmScaleSetName: "example-vm-scale-set-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UpgradeStatusInfo } from "@ubx/sdk-azure/azure/data/rolling/upgrade_status_info";

    export default stack("example", () => {
      intent({ summary: "look up azure_rolling_upgrade_status_info" });

      data(UpgradeStatusInfo, "example", {
        vmScaleSetName: "example-vm-scale-set-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.rolling import UpgradeStatusInfo, UpgradeStatusInfoConfig

    def describe():
        ubx.intent("look up azure_rolling_upgrade_status_info")

        ubx.data(UpgradeStatusInfo, "example", UpgradeStatusInfoConfig(
            vm_scale_set_name="example-vm-scale-set-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="vm_scale_set_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  The status of the latest virtual machine scale set rolling upgrade. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="error" type="object">
      Api error. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          The error code. Optional lookup argument.
        </ResponseField>
        <ResponseField name="details" type="list(object)">
          The Api error details Optional lookup argument.
        </ResponseField>
        <ResponseField name="innererror" type="object">
          Inner error details. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="errordetail" type="string">
              The internal error message or exception dump. Optional lookup argument.
            </ResponseField>
            <ResponseField name="exceptiontype" type="string">
              The exception type. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="message" type="string">
          The error message. Optional lookup argument.
        </ResponseField>
        <ResponseField name="target" type="string">
          The target of the particular error. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="policy" type="object">
      The configuration parameters used while performing a rolling upgrade. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="enable_cross_zone_upgrade" type="bool">
          Allow VMSS to ignore AZ boundaries when constructing upgrade batches. Take into consideration the Update Domain and maxBatchInstancePercent to determine the batch size. Optional lookup argument.
        </ResponseField>
        <ResponseField name="max_batch_instance_percent" type="number">
          The maximum percent of total virtual machine instances that will be upgraded simultaneously by the rolling upgrade in one batch. As this is a maximum, unhealthy instances in previous or future batches can cause the percentage of instances in a batch to decrease to ensure higher reliability. The default value for this parameter is 20%. Optional lookup argument.
        </ResponseField>
        <ResponseField name="max_surge" type="bool">
          Create new virtual machines to upgrade the scale set, rather than updating the existing virtual machines. Existing virtual machines will be deleted once the new virtual machines are created for each batch. Optional lookup argument.
        </ResponseField>
        <ResponseField name="max_unhealthy_instance_percent" type="number">
          The maximum percentage of the total virtual machine instances in the scale set that can be simultaneously unhealthy, either as a result of being upgraded, or by being found in an unhealthy state by the virtual machine health checks before the rolling upgrade aborts. This constraint will be checked prior to starting any batch. The default value for this parameter is 20%. Optional lookup argument.
        </ResponseField>
        <ResponseField name="max_unhealthy_upgraded_instance_percent" type="number">
          The maximum percentage of upgraded virtual machine instances that can be found to be in an unhealthy state. This check will happen after each batch is upgraded. If this percentage is ever exceeded, the rolling update aborts. The default value for this parameter is 20%. Optional lookup argument.
        </ResponseField>
        <ResponseField name="pause_time_between_batches" type="string">
          The wait time between completing the update for all virtual machines in one batch and starting the next batch. The time duration should be specified in ISO 8601 format. The default value is 0 seconds (PT0S). Optional lookup argument.
        </ResponseField>
        <ResponseField name="prioritize_unhealthy_instances" type="bool">
          Upgrade all unhealthy instances in a scale set before any healthy instances. Optional lookup argument.
        </ResponseField>
        <ResponseField name="rollback_failed_instances_on_policy_breach" type="bool">
          Rollback failed instances to previous model if the Rolling Upgrade policy is violated. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="progress" type="object">
      Information about the number of virtual machine instances in each upgrade state. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="failed_instance_count" type="number">
          The number of instances that have failed to be upgraded successfully. Returned by the lookup.
        </ResponseField>
        <ResponseField name="in_progress_instance_count" type="number">
          The number of instances that are currently being upgraded. Returned by the lookup.
        </ResponseField>
        <ResponseField name="pending_instance_count" type="number">
          The number of instances that have not yet begun to be upgraded. Returned by the lookup.
        </ResponseField>
        <ResponseField name="successful_instance_count" type="number">
          The number of instances that have been successfully upgraded. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="running_status" type="object">
      Information about the current running state of the overall upgrade. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          Code indicating the current status of the upgrade. Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_action" type="string">
          The last action performed on the rolling upgrade. Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_action_time" type="string">
          Last action time of the upgrade. Returned by the lookup.
        </ResponseField>
        <ResponseField name="start_time" type="string">
          Start time of the upgrade. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
