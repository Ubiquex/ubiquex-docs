---
title: "azure_nginx_swagger_nginx_deployment_api_key_response"
description: "`azure_nginx_swagger_nginx_deployment_api_key_response` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_nginx_swagger_nginx_deployment_api_key_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	nginx "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/nginx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_nginx_swagger_nginx_deployment_api_key_response"})

    		ubx.Data(nginx.SwaggerNginxDeploymentApiKeyResponse, "example", nginx.SwaggerNginxDeploymentApiKeyResponseConfig{
    			DeploymentName: "example-deployment-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SwaggerNginxDeploymentApiKeyResponse } from "@ubx/sdk-azure/azure/data/nginx/swagger_nginx_deployment_api_key_response";

    export default stack("example", () => {
      intent({
        summary: "look up azure_nginx_swagger_nginx_deployment_api_key_response",
      });

      data(SwaggerNginxDeploymentApiKeyResponse, "example", {
        deploymentName: "example-deployment-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.nginx import SwaggerNginxDeploymentApiKeyResponse, SwaggerNginxDeploymentApiKeyResponseConfig

    def describe():
        ubx.intent("look up azure_nginx_swagger_nginx_deployment_api_key_response")

        ubx.data(SwaggerNginxDeploymentApiKeyResponse, "example", SwaggerNginxDeploymentApiKeyResponseConfig(
            deployment_name="example-deployment-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="deployment_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NginxDeploymentApiKeyResponse items on this page Returned by the lookup.
</ResponseField>
