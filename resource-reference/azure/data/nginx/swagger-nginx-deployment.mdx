---
title: "azure_nginx_swagger_nginx_deployment"
description: "`data_azure_nginx_swagger_nginx_deployment` looks up a real, already-existing `azure_nginx_swagger_nginx_deployment`."
---

It is an NGINX for Azure (NGINXaaS) deployment, a fully managed NGINX Plus instance run and supported jointly by Microsoft and F5.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	nginx "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/nginx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_nginx_swagger_nginx_deployment"})

    		ubx.Data(nginx.SwaggerNginxDeployment, "example", nginx.SwaggerNginxDeploymentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SwaggerNginxDeployment } from "@ubx/sdk-azure/azure/data/nginx/swagger_nginx_deployment";

    export default stack("example", () => {
      intent({ summary: "look up azure_nginx_swagger_nginx_deployment" });

      data(SwaggerNginxDeployment, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.nginx import SwaggerNginxDeployment, SwaggerNginxDeploymentConfig

    def describe():
        ubx.intent("look up azure_nginx_swagger_nginx_deployment")

        ubx.data(SwaggerNginxDeployment, "example", SwaggerNginxDeploymentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NginxDeployment items on this page Returned by the lookup.
</ResponseField>
