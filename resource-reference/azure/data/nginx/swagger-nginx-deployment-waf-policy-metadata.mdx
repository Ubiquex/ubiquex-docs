---
title: "azure_nginx_swagger_nginx_deployment_waf_policy_metadata"
description: "`azure_nginx_swagger_nginx_deployment_waf_policy_metadata` reads real metadata (such as its own revision datestamp) about the WAF policy applied to..."
---

`azure_nginx_swagger_nginx_deployment_waf_policy_metadata` reads real metadata (such as its own revision datestamp) about the WAF policy applied to an NGINXaaS deployment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	nginx "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/nginx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_nginx_swagger_nginx_deployment_waf_policy_metadata"})

    		ubx.Data(nginx.SwaggerNginxDeploymentWafPolicyMetadata, "example", nginx.SwaggerNginxDeploymentWafPolicyMetadataConfig{
    			DeploymentName: "example-deployment-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { SwaggerNginxDeploymentWafPolicyMetadata } from "./local-sdk/azure/sdk/typescript/azure/data/nginx/swagger_nginx_deployment_waf_policy_metadata.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_nginx_swagger_nginx_deployment_waf_policy_metadata",
      });

      data(SwaggerNginxDeploymentWafPolicyMetadata, "example", {
        deploymentName: "example-deployment-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.nginx import SwaggerNginxDeploymentWafPolicyMetadata, SwaggerNginxDeploymentWafPolicyMetadataConfig

    def describe():
        ubx.intent("look up azure_nginx_swagger_nginx_deployment_waf_policy_metadata")

        ubx.data(SwaggerNginxDeploymentWafPolicyMetadata, "example", SwaggerNginxDeploymentWafPolicyMetadataConfig(
            deployment_name="example-deployment-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="deployment_name" type="string" required>
  The real name of the deployment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NginxDeploymentWafPolicyMetadata items on this page Returned by the lookup.
</ResponseField>
