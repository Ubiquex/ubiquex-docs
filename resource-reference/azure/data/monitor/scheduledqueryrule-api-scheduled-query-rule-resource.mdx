---
title: "azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource"
description: "`data_azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource` looks up a real, already-existing..."
---

`data_azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource` looks up a real, already-existing `azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource`. It defines an Azure Monitor alert rule that periodically runs a log query and fires when its own condition is met.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitor "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/monitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource"})

    		ubx.Data(monitor.ScheduledqueryruleApiScheduledQueryRuleResource, "example", monitor.ScheduledqueryruleApiScheduledQueryRuleResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ScheduledqueryruleApiScheduledQueryRuleResource } from "@ubx/sdk-azure/azure/data/monitor/scheduledqueryrule_api_scheduled_query_rule_resource";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource",
      });

      data(ScheduledqueryruleApiScheduledQueryRuleResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.monitor import ScheduledqueryruleApiScheduledQueryRuleResource, ScheduledqueryruleApiScheduledQueryRuleResourceConfig

    def describe():
        ubx.intent("look up azure_monitor_scheduledqueryrule_api_scheduled_query_rule_resource")

        ubx.data(ScheduledqueryruleApiScheduledQueryRuleResource, "example", ScheduledqueryruleApiScheduledQueryRuleResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Provides the link to retrieve the next set of elements. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The values for the scheduled query rule resources. Returned by the lookup.
</ResponseField>
