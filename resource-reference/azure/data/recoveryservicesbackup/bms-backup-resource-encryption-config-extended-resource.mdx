---
title: "azure_recoveryservicesbackup_bms_backup_resource_encryption_config_extended_resource"
description: "`azure_recoveryservicesbackup_bms_backup_resource_encryption_config_extended_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_recoveryservicesbackup_bms_backup_resource_encryption_config_extended_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicesbackup "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/recoveryservicesbackup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_recoveryservicesbackup_bms_backup_resource_encryption_config_extended_resource"})

    		ubx.Data(recoveryservicesbackup.BmsBackupResourceEncryptionConfigExtendedResource, "example", recoveryservicesbackup.BmsBackupResourceEncryptionConfigExtendedResourceConfig{
    			VaultName: "example-vault-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BmsBackupResourceEncryptionConfigExtendedResource } from "@ubx/sdk-azure/azure/data/recoveryservicesbackup/bms_backup_resource_encryption_config_extended_resource";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_recoveryservicesbackup_bms_backup_resource_encryption_config_extended_resource",
      });

      data(BmsBackupResourceEncryptionConfigExtendedResource, "example", {
        vaultName: "example-vault-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.recoveryservicesbackup import BmsBackupResourceEncryptionConfigExtendedResource, BmsBackupResourceEncryptionConfigExtendedResourceConfig

    def describe():
        ubx.intent("look up azure_recoveryservicesbackup_bms_backup_resource_encryption_config_extended_resource")

        ubx.data(BmsBackupResourceEncryptionConfigExtendedResource, "example", BmsBackupResourceEncryptionConfigExtendedResourceConfig(
            vault_name="example-vault-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="vault_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="e_tag" type="string">
  Optional ETag. Returned by the lookup.
</ResponseField>

<ResponseField name="location" type="string">
  Represents an Azure geography region where supported resource providers live. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="use_system_assigned_identity" type="bool">
      bool to indicate whether to use system Assigned Identity or not Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_assigned_identity" type="string">
      User Assigned Identity Id Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Returned by the lookup.
</ResponseField>
