---
title: "azure_recoveryservicesbackup_bms_backup_resource_config_resource"
description: "`azure_recoveryservicesbackup_bms_backup_resource_config_resource` reads the real backup storage configuration (redundancy type) of a Recovery Services..."
---

`azure_recoveryservicesbackup_bms_backup_resource_config_resource` reads the real backup storage configuration (redundancy type) of a Recovery Services vault.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicesbackup "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/recoveryservicesbackup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_recoveryservicesbackup_bms_backup_resource_config_resource"})

    		ubx.Data(recoveryservicesbackup.BmsBackupResourceConfigResource, "example", recoveryservicesbackup.BmsBackupResourceConfigResourceConfig{
    			VaultName: "example-vault-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BmsBackupResourceConfigResource } from "@ubx/sdk-azure/azure/data/recoveryservicesbackup/bms_backup_resource_config_resource";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_recoveryservicesbackup_bms_backup_resource_config_resource",
      });

      data(BmsBackupResourceConfigResource, "example", {
        vaultName: "example-vault-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.recoveryservicesbackup import BmsBackupResourceConfigResource, BmsBackupResourceConfigResourceConfig

    def describe():
        ubx.intent("look up azure_recoveryservicesbackup_bms_backup_resource_config_resource")

        ubx.data(BmsBackupResourceConfigResource, "example", BmsBackupResourceConfigResourceConfig(
            vault_name="example-vault-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="vault_name" type="string" required>
  The real name of the vault this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="e_tag" type="string">
  Optional ETag. Returned by the lookup.
</ResponseField>

<ResponseField name="location" type="string">
  Represents an Azure geography region where supported resource providers live. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  The resource storage details. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cross_region_restore_flag" type="bool">
      Opt in details of Cross Region Restore feature. Optional lookup argument.
    </ResponseField>
    <ResponseField name="dedup_state" type="string">
      Vault Dedup state Optional lookup argument.
    </ResponseField>
    <ResponseField name="storage_model_type" type="string">
      Storage type Optional lookup argument.
    </ResponseField>
    <ResponseField name="storage_type" type="string">
      Storage type Optional lookup argument.
    </ResponseField>
    <ResponseField name="storage_type_state" type="string">
      Locked or Unlocked. Once a machine is registered against a resource, the storageTypeState is always Locked. Optional lookup argument.
    </ResponseField>
    <ResponseField name="xcool_state" type="string">
      Vault x-cool state Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Returned by the lookup.
</ResponseField>
