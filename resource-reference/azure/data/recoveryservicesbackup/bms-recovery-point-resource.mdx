---
title: "azure_recoveryservicesbackup_bms_recovery_point_resource"
description: "`azure_recoveryservicesbackup_bms_recovery_point_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_recoveryservicesbackup_bms_recovery_point_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	recoveryservicesbackup "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/recoveryservicesbackup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_recoveryservicesbackup_bms_recovery_point_resource"})

    		ubx.Data(recoveryservicesbackup.BmsRecoveryPointResource, "example", recoveryservicesbackup.BmsRecoveryPointResourceConfig{
    			ContainerName:     "example-container-name",
    			FabricName:        "example-fabric-name",
    			ProtectedItemName: "example-protected-item-name",
    			VaultName:         "example-vault-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BmsRecoveryPointResource } from "@ubx/sdk-azure/azure/data/recoveryservicesbackup/bms_recovery_point_resource";

    export default stack("example", () => {
      intent({
        summary: "look up azure_recoveryservicesbackup_bms_recovery_point_resource",
      });

      data(BmsRecoveryPointResource, "example", {
        containerName: "example-container-name",
        fabricName: "example-fabric-name",
        protectedItemName: "example-protected-item-name",
        vaultName: "example-vault-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.recoveryservicesbackup import BmsRecoveryPointResource, BmsRecoveryPointResourceConfig

    def describe():
        ubx.intent("look up azure_recoveryservicesbackup_bms_recovery_point_resource")

        ubx.data(BmsRecoveryPointResource, "example", BmsRecoveryPointResourceConfig(
            container_name="example-container-name",
            fabric_name="example-fabric-name",
            protected_item_name="example-protected-item-name",
            vault_name="example-vault-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="container_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="fabric_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="protected_item_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  List of resources. Returned by the lookup.
</ResponseField>
