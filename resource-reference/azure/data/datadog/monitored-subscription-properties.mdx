---
title: "azure_datadog_monitored_subscription_properties"
description: "`azure_datadog_monitored_subscription_properties` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_datadog_monitored_subscription_properties` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datadog "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/datadog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_datadog_monitored_subscription_properties"})

    		ubx.Data(datadog.MonitoredSubscriptionProperties, "example", datadog.MonitoredSubscriptionPropertiesConfig{
    			MonitorName: "example-monitor-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MonitoredSubscriptionProperties } from "@ubx/sdk-azure/azure/data/datadog/monitored_subscription_properties";

    export default stack("example", () => {
      intent({
        summary: "look up azure_datadog_monitored_subscription_properties",
      });

      data(MonitoredSubscriptionProperties, "example", {
        monitorName: "example-monitor-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.datadog import MonitoredSubscriptionProperties, MonitoredSubscriptionPropertiesConfig

    def describe():
        ubx.intent("look up azure_datadog_monitored_subscription_properties")

        ubx.data(MonitoredSubscriptionProperties, "example", MonitoredSubscriptionPropertiesConfig(
            monitor_name="example-monitor-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="monitor_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The MonitoredSubscriptionProperties items on this page Returned by the lookup.
</ResponseField>
