---
title: "azure_synapse_sqlpool_sql_pool"
description: "`data_azure_synapse_sqlpool_sql_pool` looks up a real, already-existing `azure_synapse_sqlpool_sql_pool`."
---

It is a Synapse dedicated SQL pool, a provisioned, MPP data warehouse compute and storage unit within a Synapse workspace.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_synapse_sqlpool_sql_pool"})

    		ubx.Data(synapse.SqlpoolSqlPool, "example", synapse.SqlpoolSqlPoolConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { SqlpoolSqlPool } from "./local-sdk/azure/sdk/typescript/azure/data/synapse/sqlpool_sql_pool.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_synapse_sqlpool_sql_pool" });

      data(SqlpoolSqlPool, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.synapse import SqlpoolSqlPool, SqlpoolSqlPoolConfig

    def describe():
        ubx.intent("look up azure_synapse_sqlpool_sql_pool")

        ubx.data(SqlpoolSqlPool, "example", SqlpoolSqlPoolConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Link to the next page of results Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of SQL pools Returned by the lookup.
</ResponseField>
