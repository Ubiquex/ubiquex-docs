---
title: "azure_synapse_sqlpool_sql_pool_column"
description: "`azure_synapse_sqlpool_sql_pool_column` looks up a real column within a table in a Synapse SQL pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_synapse_sqlpool_sql_pool_column"})

    		ubx.Data(synapse.SqlpoolSqlPoolColumn, "example", synapse.SqlpoolSqlPoolColumnConfig{
    			SchemaName:  "example-schema-name",
    			SqlPoolName: "example-sql-pool-name",
    			TableName:   "example-table-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SqlpoolSqlPoolColumn } from "@ubx/sdk-azure/azure/data/synapse/sqlpool_sql_pool_column";

    export default stack("example", () => {
      intent({ summary: "look up azure_synapse_sqlpool_sql_pool_column" });

      data(SqlpoolSqlPoolColumn, "example", {
        schemaName: "example-schema-name",
        sqlPoolName: "example-sql-pool-name",
        tableName: "example-table-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.synapse import SqlpoolSqlPoolColumn, SqlpoolSqlPoolColumnConfig

    def describe():
        ubx.intent("look up azure_synapse_sqlpool_sql_pool_column")

        ubx.data(SqlpoolSqlPoolColumn, "example", SqlpoolSqlPoolColumnConfig(
            schema_name="example-schema-name",
            sql_pool_name="example-sql-pool-name",
            table_name="example-table-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="schema_name" type="string" required>
  The real name of the schema this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sql_pool_name" type="string" required>
  The real name of the Synapse SQL pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  The real name of the table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to retrieve next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of results. Returned by the lookup.
</ResponseField>
