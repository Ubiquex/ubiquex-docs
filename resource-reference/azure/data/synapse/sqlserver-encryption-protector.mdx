---
title: "azure_synapse_sqlserver_encryption_protector"
description: "`data_azure_synapse_sqlserver_encryption_protector` looks up a real, already-existing `azure_synapse_sqlserver_encryption_protector`."
---

It designates the customer-managed key used to protect transparent data encryption keys across a Synapse workspace's own SQL pools.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_synapse_sqlserver_encryption_protector"})

    		ubx.Data(synapse.SqlserverEncryptionProtector, "example", synapse.SqlserverEncryptionProtectorConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SqlserverEncryptionProtector } from "@ubx/sdk-azure/azure/data/synapse/sqlserver_encryption_protector";

    export default stack("example", () => {
      intent({ summary: "look up azure_synapse_sqlserver_encryption_protector" });

      data(SqlserverEncryptionProtector, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.synapse import SqlserverEncryptionProtector, SqlserverEncryptionProtectorConfig

    def describe():
        ubx.intent("look up azure_synapse_sqlserver_encryption_protector")

        ubx.data(SqlserverEncryptionProtector, "example", SqlserverEncryptionProtectorConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Link to retrieve next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of results. Returned by the lookup.
</ResponseField>
