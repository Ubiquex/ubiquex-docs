---
title: "azure_synapse_sqlpool_sensitivity_label"
description: "`data_azure_synapse_sqlpool_sensitivity_label` looks up a real, already-existing `azure_synapse_sqlpool_sensitivity_label`."
---

It classifies a specific column in a Synapse dedicated SQL pool with a data sensitivity label (e.g. `Confidential`), surfaced in vulnerability assessment and compliance reporting.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_synapse_sqlpool_sensitivity_label"})

    		ubx.Data(synapse.SqlpoolSensitivityLabel, "example", synapse.SqlpoolSensitivityLabelConfig{
    			SqlPoolName: "example-sql-pool-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { SqlpoolSensitivityLabel } from "./local-sdk/azure/sdk/typescript/azure/data/synapse/sqlpool_sensitivity_label.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_synapse_sqlpool_sensitivity_label" });

      data(SqlpoolSensitivityLabel, "example", {
        sqlPoolName: "example-sql-pool-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.synapse import SqlpoolSensitivityLabel, SqlpoolSensitivityLabelConfig

    def describe():
        ubx.intent("look up azure_synapse_sqlpool_sensitivity_label")

        ubx.data(SqlpoolSensitivityLabel, "example", SqlpoolSensitivityLabelConfig(
            sql_pool_name="example-sql-pool-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sql_pool_name" type="string" required>
  The real name of the Synapse SQL pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to retrieve next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of results. Returned by the lookup.
</ResponseField>
