---
title: "azure_synapse_integrationruntime_self_hosted_integration_runtime_node"
description: "`azure_synapse_integrationruntime_self_hosted_integration_runtime_node` looks up a real node within a self-hosted Synapse integration runtime."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_synapse_integrationruntime_self_hosted_integration_runtime_node"})

    		ubx.Data(synapse.IntegrationruntimeSelfHostedIntegrationRuntimeNode, "example", synapse.IntegrationruntimeSelfHostedIntegrationRuntimeNodeConfig{
    			IntegrationRuntimeName: "example-integration-runtime-name",
    			NodeName:               "example-node-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IntegrationruntimeSelfHostedIntegrationRuntimeNode } from "@ubx/sdk-azure/azure/data/synapse/integrationruntime_self_hosted_integration_runtime_node";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_synapse_integrationruntime_self_hosted_integration_runtime_node",
      });

      data(IntegrationruntimeSelfHostedIntegrationRuntimeNode, "example", {
        integrationRuntimeName: "example-integration-runtime-name",
        nodeName: "example-node-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.synapse import IntegrationruntimeSelfHostedIntegrationRuntimeNode, IntegrationruntimeSelfHostedIntegrationRuntimeNodeConfig

    def describe():
        ubx.intent("look up azure_synapse_integrationruntime_self_hosted_integration_runtime_node")

        ubx.data(IntegrationruntimeSelfHostedIntegrationRuntimeNode, "example", IntegrationruntimeSelfHostedIntegrationRuntimeNodeConfig(
            integration_runtime_name="example-integration-runtime-name",
            node_name="example-node-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="integration_runtime_name" type="string" required>
  The real name of the Data Factory or Synapse integration runtime this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="node_name" type="string" required>
  The real name of the node this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capabilities" type="map(string)">
  The integration runtime capabilities dictionary Returned by the lookup.
</ResponseField>

<ResponseField name="concurrent_jobs_limit" type="number">
  Maximum concurrent jobs on the integration runtime node. Returned by the lookup.
</ResponseField>

<ResponseField name="expiry_time" type="string">
  The time at which the integration runtime will expire in ISO8601 format. Returned by the lookup.
</ResponseField>

<ResponseField name="host_service_uri" type="string">
  URI for the host machine of the integration runtime. Returned by the lookup.
</ResponseField>

<ResponseField name="is_active_dispatcher" type="bool">
  Indicates whether this node is the active dispatcher for integration runtime requests. Returned by the lookup.
</ResponseField>

<ResponseField name="last_connect_time" type="string">
  The most recent time at which the integration runtime was connected in ISO8601 format. Returned by the lookup.
</ResponseField>

<ResponseField name="last_end_update_time" type="string">
  The last time for the integration runtime node update end. Returned by the lookup.
</ResponseField>

<ResponseField name="last_start_time" type="string">
  The time the node last started up. Returned by the lookup.
</ResponseField>

<ResponseField name="last_start_update_time" type="string">
  The last time for the integration runtime node update start. Returned by the lookup.
</ResponseField>

<ResponseField name="last_stop_time" type="string">
  The integration runtime node last stop time. Returned by the lookup.
</ResponseField>

<ResponseField name="last_update_result" type="string">
  The result of the last integration runtime node update. Returned by the lookup.
</ResponseField>

<ResponseField name="machine_name" type="string">
  Machine name of the integration runtime node. Returned by the lookup.
</ResponseField>

<ResponseField name="max_concurrent_jobs" type="number">
  The maximum concurrent jobs in this integration runtime. Returned by the lookup.
</ResponseField>

<ResponseField name="register_time" type="string">
  The time at which the integration runtime node was registered in ISO8601 format. Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Status of the integration runtime node. Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  Version of the integration runtime node. Returned by the lookup.
</ResponseField>

<ResponseField name="version_status" type="string">
  Status of the integration runtime node version. Returned by the lookup.
</ResponseField>
