---
title: "azure_synapse_workspace"
description: "`azure_synapse_workspace` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_synapse_workspace` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_synapse_workspace"})

    		ubx.Data(synapse.Workspace, "example", synapse.WorkspaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Workspace } from "@ubx/sdk-azure/azure/data/synapse/workspace";

    export default stack("example", () => {
      intent({ summary: "look up azure_synapse_workspace" });

      data(Workspace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.synapse import Workspace, WorkspaceConfig

    def describe():
        ubx.intent("look up azure_synapse_workspace")

        ubx.data(Workspace, "example", WorkspaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Link to the next page of results Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of workspaces Returned by the lookup.
</ResponseField>
