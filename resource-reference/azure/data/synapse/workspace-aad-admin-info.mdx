---
title: "azure_synapse_workspace_aad_admin_info"
description: "`azure_synapse_workspace_aad_admin_info` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_synapse_workspace_aad_admin_info` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_synapse_workspace_aad_admin_info"})

    		ubx.Data(synapse.WorkspaceAadAdminInfo, "example", synapse.WorkspaceAadAdminInfoConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspaceAadAdminInfo } from "@ubx/sdk-azure/azure/data/synapse/workspace_aad_admin_info";

    export default stack("example", () => {
      intent({ summary: "look up azure_synapse_workspace_aad_admin_info" });

      data(WorkspaceAadAdminInfo, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.synapse import WorkspaceAadAdminInfo, WorkspaceAadAdminInfoConfig

    def describe():
        ubx.intent("look up azure_synapse_workspace_aad_admin_info")

        ubx.data(WorkspaceAadAdminInfo, "example", WorkspaceAadAdminInfoConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="properties" type="object">
  Workspace active directory administrator properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="administrator_type" type="string">
      Workspace active directory administrator type Optional lookup argument.
    </ResponseField>
    <ResponseField name="login" type="string">
      Login of the workspace active directory administrator Optional lookup argument.
    </ResponseField>
    <ResponseField name="sid" type="string">
      Object ID of the workspace active directory administrator Optional lookup argument.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Tenant ID of the workspace active directory administrator Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
