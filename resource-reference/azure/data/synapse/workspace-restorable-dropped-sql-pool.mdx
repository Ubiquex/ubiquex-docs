---
title: "azure_synapse_workspace_restorable_dropped_sql_pool"
description: "`azure_synapse_workspace_restorable_dropped_sql_pool` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_synapse_workspace_restorable_dropped_sql_pool` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_synapse_workspace_restorable_dropped_sql_pool"})

    		ubx.Data(synapse.WorkspaceRestorableDroppedSqlPool, "example", synapse.WorkspaceRestorableDroppedSqlPoolConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspaceRestorableDroppedSqlPool } from "@ubx/sdk-azure/azure/data/synapse/workspace_restorable_dropped_sql_pool";

    export default stack("example", () => {
      intent({
        summary: "look up azure_synapse_workspace_restorable_dropped_sql_pool",
      });

      data(WorkspaceRestorableDroppedSqlPool, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.synapse import WorkspaceRestorableDroppedSqlPool, WorkspaceRestorableDroppedSqlPoolConfig

    def describe():
        ubx.intent("look up azure_synapse_workspace_restorable_dropped_sql_pool")

        ubx.data(WorkspaceRestorableDroppedSqlPool, "example", WorkspaceRestorableDroppedSqlPoolConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  A list of restorable dropped Sql pools Returned by the lookup.
</ResponseField>
