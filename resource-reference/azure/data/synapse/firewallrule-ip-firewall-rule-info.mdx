---
title: "azure_synapse_firewallrule_ip_firewall_rule_info"
description: "`data_azure_synapse_firewallrule_ip_firewall_rule_info` looks up a real, already-existing `azure_synapse_firewallrule_ip_firewall_rule_info`."
---

It allows a specific IP address range to connect to a Synapse workspace that hasn't been placed inside a virtual network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_synapse_firewallrule_ip_firewall_rule_info"})

    		ubx.Data(synapse.FirewallruleIpFirewallRuleInfo, "example", synapse.FirewallruleIpFirewallRuleInfoConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallruleIpFirewallRuleInfo } from "@ubx/sdk-azure/azure/data/synapse/firewallrule_ip_firewall_rule_info";

    export default stack("example", () => {
      intent({
        summary: "look up azure_synapse_firewallrule_ip_firewall_rule_info",
      });

      data(FirewallruleIpFirewallRuleInfo, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.synapse import FirewallruleIpFirewallRuleInfo, FirewallruleIpFirewallRuleInfoConfig

    def describe():
        ubx.intent("look up azure_synapse_firewallrule_ip_firewall_rule_info")

        ubx.data(FirewallruleIpFirewallRuleInfo, "example", FirewallruleIpFirewallRuleInfoConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Link to next page of results Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of IP firewall rules Returned by the lookup.
</ResponseField>
