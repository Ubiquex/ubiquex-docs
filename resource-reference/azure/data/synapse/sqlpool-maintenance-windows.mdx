---
title: "azure_synapse_sqlpool_maintenance_windows"
description: "`azure_synapse_sqlpool_maintenance_windows` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_synapse_sqlpool_maintenance_windows` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synapse "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/synapse"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_synapse_sqlpool_maintenance_windows"})

    		ubx.Data(synapse.SqlpoolMaintenanceWindows, "example", synapse.SqlpoolMaintenanceWindowsConfig{
    			MaintenanceWindowName: "example-maintenance-window-name",
    			SqlPoolName:           "example-sql-pool-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SqlpoolMaintenanceWindows } from "@ubx/sdk-azure/azure/data/synapse/sqlpool_maintenance_windows";

    export default stack("example", () => {
      intent({ summary: "look up azure_synapse_sqlpool_maintenance_windows" });

      data(SqlpoolMaintenanceWindows, "example", {
        maintenanceWindowName: "example-maintenance-window-name",
        sqlPoolName: "example-sql-pool-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.synapse import SqlpoolMaintenanceWindows, SqlpoolMaintenanceWindowsConfig

    def describe():
        ubx.intent("look up azure_synapse_sqlpool_maintenance_windows")

        ubx.data(SqlpoolMaintenanceWindows, "example", SqlpoolMaintenanceWindowsConfig(
            maintenance_window_name="example-maintenance-window-name",
            sql_pool_name="example-sql-pool-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="maintenance_window_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sql_pool_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  Maintenance windows resource properties. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="time_ranges" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
