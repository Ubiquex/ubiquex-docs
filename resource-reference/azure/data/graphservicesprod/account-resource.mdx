---
title: "azure_graphservicesprod_account_resource"
description: "`data_azure_graphservicesprod_account_resource` looks up a real, already-existing `azure_graphservicesprod_account_resource`."
---

It provisions a Microsoft Graph Data Connect account, enabling bulk export of Microsoft 365 data into Azure data stores for analytics.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	graphservicesprod "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/graphservicesprod"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_graphservicesprod_account_resource"})

    		ubx.Data(graphservicesprod.AccountResource, "example", graphservicesprod.AccountResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { AccountResource } from "./local-sdk/azure/sdk/typescript/azure/data/graphservicesprod/account_resource.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_graphservicesprod_account_resource" });

      data(AccountResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.graphservicesprod import AccountResource, AccountResourceConfig

    def describe():
        ubx.intent("look up azure_graphservicesprod_account_resource")

        ubx.data(AccountResource, "example", AccountResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of recommendations. Returned by the lookup.
</ResponseField>
