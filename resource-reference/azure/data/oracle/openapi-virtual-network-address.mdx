---
title: "azure_oracle_openapi_virtual_network_address"
description: "`azure_oracle_openapi_virtual_network_address` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_oracle_openapi_virtual_network_address` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oracle "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/oracle"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_oracle_openapi_virtual_network_address"})

    		ubx.Data(oracle.OpenapiVirtualNetworkAddress, "example", oracle.OpenapiVirtualNetworkAddressConfig{
    			Cloudvmclustername: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiVirtualNetworkAddress } from "@ubx/sdk-azure/azure/data/oracle/openapi_virtual_network_address";

    export default stack("example", () => {
      intent({ summary: "look up azure_oracle_openapi_virtual_network_address" });

      data(OpenapiVirtualNetworkAddress, "example", {
        cloudvmclustername: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.oracle import OpenapiVirtualNetworkAddress, OpenapiVirtualNetworkAddressConfig

    def describe():
        ubx.intent("look up azure_oracle_openapi_virtual_network_address")

        ubx.data(OpenapiVirtualNetworkAddress, "example", OpenapiVirtualNetworkAddressConfig(
            cloudvmclustername="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cloudvmclustername" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualNetworkAddress items on this page Returned by the lookup.
</ResponseField>
