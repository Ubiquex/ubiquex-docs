---
title: "azure_oracle_openapi_gi_version"
description: "`azure_oracle_openapi_gi_version` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_oracle_openapi_gi_version` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oracle "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/oracle"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_oracle_openapi_gi_version"})

    		ubx.Data(oracle.OpenapiGiVersion, "example", oracle.OpenapiGiVersionConfig{
    			Shape:          "example",
    			ShapeAttribute: "example",
    			Zone:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiGiVersion } from "@ubx/sdk-azure/azure/data/oracle/openapi_gi_version";

    export default stack("example", () => {
      intent({ summary: "look up azure_oracle_openapi_gi_version" });

      data(OpenapiGiVersion, "example", {
        shape: "example",
        shapeAttribute: "example",
        zone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.oracle import OpenapiGiVersion, OpenapiGiVersionConfig

    def describe():
        ubx.intent("look up azure_oracle_openapi_gi_version")

        ubx.data(OpenapiGiVersion, "example", OpenapiGiVersionConfig(
            shape="example",
            shape_attribute="example",
            zone="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="shape" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="shape_attribute" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="zone" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The GiVersion items on this page Returned by the lookup.
</ResponseField>
