---
title: "azure_oracle_openapi_gi_version"
description: "`azure_oracle_openapi_gi_version` reads a real, available Oracle Grid Infrastructure version."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	oracle "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/oracle"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_oracle_openapi_gi_version"})

    		ubx.Data(oracle.OpenapiGiVersion, "example", oracle.OpenapiGiVersionConfig{
    			Shape:          "example",
    			ShapeAttribute: "example",
    			Zone:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiGiVersion } from "./local-sdk/azure/sdk/typescript/azure/data/oracle/openapi_gi_version.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_oracle_openapi_gi_version" });

      data(OpenapiGiVersion, "example", {
        shape: "example",
        shapeAttribute: "example",
        zone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.oracle import OpenapiGiVersion, OpenapiGiVersionConfig

    def describe():
        ubx.intent("look up azure_oracle_openapi_gi_version")

        ubx.data(OpenapiGiVersion, "example", OpenapiGiVersionConfig(
            shape="example",
            shape_attribute="example",
            zone="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="shape" type="string">
  The real compute shape this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="shape_attribute" type="string">
  The real, available attribute for an Oracle compute shape. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="zone" type="string">
  The real availability zone this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The GiVersion items on this page Returned by the lookup.
</ResponseField>
