---
title: "azure_oracle_openapi_db_version"
description: "`azure_oracle_openapi_db_version` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_oracle_openapi_db_version` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oracle "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/oracle"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_oracle_openapi_db_version"})

    		ubx.Data(oracle.OpenapiDbVersion, "example", oracle.OpenapiDbVersionConfig{
    			DbSystemId:                       "example",
    			DbSystemShape:                    "example",
    			IsDatabaseSoftwareImageSupported: true,
    			IsUpgradeSupported:               true,
    			ShapeFamily:                      "example",
    			StorageManagement:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiDbVersion } from "@ubx/sdk-azure/azure/data/oracle/openapi_db_version";

    export default stack("example", () => {
      intent({ summary: "look up azure_oracle_openapi_db_version" });

      data(OpenapiDbVersion, "example", {
        dbSystemId: "example",
        dbSystemShape: "example",
        isDatabaseSoftwareImageSupported: true,
        isUpgradeSupported: true,
        shapeFamily: "example",
        storageManagement: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.oracle import OpenapiDbVersion, OpenapiDbVersionConfig

    def describe():
        ubx.intent("look up azure_oracle_openapi_db_version")

        ubx.data(OpenapiDbVersion, "example", OpenapiDbVersionConfig(
            db_system_id="example",
            db_system_shape="example",
            is_database_software_image_supported=True,
            is_upgrade_supported=True,
            shape_family="example",
            storage_management="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="db_system_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="db_system_shape" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="is_database_software_image_supported" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="is_upgrade_supported" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="shape_family" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="storage_management" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DbVersion items on this page Returned by the lookup.
</ResponseField>
