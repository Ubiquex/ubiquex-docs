---
title: "azure_oracle_openapi_gi_minor_version"
description: "`azure_oracle_openapi_gi_minor_version` reads a real, available Oracle Grid Infrastructure minor version."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oracle "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/oracle"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_oracle_openapi_gi_minor_version"})

    		ubx.Data(oracle.OpenapiGiMinorVersion, "example", oracle.OpenapiGiMinorVersionConfig{
    			Giversionname: "example",
    			ShapeFamily:   "example",
    			Zone:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiGiMinorVersion } from "@ubx/sdk-azure/azure/data/oracle/openapi_gi_minor_version";

    export default stack("example", () => {
      intent({ summary: "look up azure_oracle_openapi_gi_minor_version" });

      data(OpenapiGiMinorVersion, "example", {
        giversionname: "example",
        shapeFamily: "example",
        zone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.oracle import OpenapiGiMinorVersion, OpenapiGiMinorVersionConfig

    def describe():
        ubx.intent("look up azure_oracle_openapi_gi_minor_version")

        ubx.data(OpenapiGiMinorVersion, "example", OpenapiGiMinorVersionConfig(
            giversionname="example",
            shape_family="example",
            zone="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="giversionname" type="string" required>
  The real Oracle Grid Infrastructure version this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="shape_family" type="string">
  The real family of Oracle compute shapes this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="zone" type="string">
  The real availability zone this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The GiMinorVersion items on this page Returned by the lookup.
</ResponseField>
