---
title: "azure_oracle_openapi_db_system_shape"
description: "`azure_oracle_openapi_db_system_shape` reads a real, available compute shape for provisioning an Oracle Database@Azure DB System."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oracle "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/oracle"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_oracle_openapi_db_system_shape"})

    		ubx.Data(oracle.OpenapiDbSystemShape, "example", oracle.OpenapiDbSystemShapeConfig{
    			ShapeAttribute: "example",
    			Zone:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiDbSystemShape } from "@ubx/sdk-azure/azure/data/oracle/openapi_db_system_shape";

    export default stack("example", () => {
      intent({ summary: "look up azure_oracle_openapi_db_system_shape" });

      data(OpenapiDbSystemShape, "example", {
        shapeAttribute: "example",
        zone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.oracle import OpenapiDbSystemShape, OpenapiDbSystemShapeConfig

    def describe():
        ubx.intent("look up azure_oracle_openapi_db_system_shape")

        ubx.data(OpenapiDbSystemShape, "example", OpenapiDbSystemShapeConfig(
            shape_attribute="example",
            zone="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="shape_attribute" type="string">
  The real, available attribute for an Oracle compute shape. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="zone" type="string">
  The real availability zone this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DbSystemShape items on this page Returned by the lookup.
</ResponseField>
