---
title: "azure_automanage_configuration_profile_assignment"
description: "`data_azure_automanage_configuration_profile_assignment` looks up a real, already-existing `azure_automanage_configuration_profile_assignment`."
---

It applies an Azure Automanage configuration profile to a specific VM, keeping it continuously conformant to the profile's own best practices.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	automanage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automanage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automanage_configuration_profile_assignment"})

    		ubx.Data(automanage.ConfigurationProfileAssignment, "example", automanage.ConfigurationProfileAssignmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ConfigurationProfileAssignment } from "./local-sdk/azure/sdk/typescript/azure/data/automanage/configuration_profile_assignment.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_automanage_configuration_profile_assignment",
      });

      data(ConfigurationProfileAssignment, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.automanage import ConfigurationProfileAssignment, ConfigurationProfileAssignmentConfig

    def describe():
        ubx.intent("look up azure_automanage_configuration_profile_assignment")

        ubx.data(ConfigurationProfileAssignment, "example", ConfigurationProfileAssignmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  Result of the list configuration profile assignment operation. Returned by the lookup.
</ResponseField>
