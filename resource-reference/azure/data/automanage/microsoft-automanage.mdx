---
title: "azure_automanage_microsoft_automanage"
description: "`azure_automanage_microsoft_automanage` looks up a real Azure Automanage configuration profile assignment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automanage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automanage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automanage_microsoft_automanage"})

    		ubx.Data(automanage.MicrosoftAutomanage, "example", automanage.MicrosoftAutomanageConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MicrosoftAutomanage } from "@ubx/sdk-azure/azure/data/automanage/microsoft_automanage";

    export default stack("example", () => {
      intent({ summary: "look up azure_automanage_microsoft_automanage" });

      data(MicrosoftAutomanage, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.automanage import MicrosoftAutomanage, MicrosoftAutomanageConfig

    def describe():
        ubx.intent("look up azure_automanage_microsoft_automanage")

        ubx.data(MicrosoftAutomanage, "example", MicrosoftAutomanageConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
