---
title: "azure_automanage_service_principal"
description: "`azure_automanage_service_principal` reads the real service principal Azure Automanage uses to manage a subscription's own resources."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	automanage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automanage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automanage_service_principal"})

    		ubx.Data(automanage.ServicePrincipal, "example", automanage.ServicePrincipalConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ServicePrincipal } from "./local-sdk/azure/sdk/typescript/azure/data/automanage/service_principal.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_automanage_service_principal" });

      data(ServicePrincipal, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.automanage import ServicePrincipal, ServicePrincipalConfig

    def describe():
        ubx.intent("look up azure_automanage_service_principal")

        ubx.data(ServicePrincipal, "example", ServicePrincipalConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  The list of servicePrincipals. Returned by the lookup.
</ResponseField>
