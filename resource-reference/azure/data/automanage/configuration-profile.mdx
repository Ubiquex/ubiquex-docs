---
title: "azure_automanage_configuration_profile"
description: "`data_azure_automanage_configuration_profile` looks up a real, already-existing `azure_automanage_configuration_profile`."
---

It defines a reusable Azure Automanage best-practice configuration (backup, security, monitoring settings) that VM(s) can be assigned.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	automanage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automanage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automanage_configuration_profile"})

    		ubx.Data(automanage.ConfigurationProfile, "example", automanage.ConfigurationProfileConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ConfigurationProfile } from "./local-sdk/azure/sdk/typescript/azure/data/automanage/configuration_profile.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_automanage_configuration_profile" });

      data(ConfigurationProfile, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.automanage import ConfigurationProfile, ConfigurationProfileConfig

    def describe():
        ubx.intent("look up azure_automanage_configuration_profile")

        ubx.data(ConfigurationProfile, "example", ConfigurationProfileConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  Result of the list ConfigurationProfile operation. Returned by the lookup.
</ResponseField>
