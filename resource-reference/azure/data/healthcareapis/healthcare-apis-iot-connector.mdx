---
title: "azure_healthcareapis_healthcare_apis_iot_connector"
description: "`data_azure_healthcareapis_healthcare_apis_iot_connector` looks up a real, already-existing `azure_healthcareapis_healthcare_apis_iot_connector`."
---

It ingests device telemetry (e.g. from IoT Hub) into an Azure Health Data Services FHIR service, normalizing it into FHIR Observation resources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	healthcareapis "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/healthcareapis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_healthcareapis_healthcare_apis_iot_connector"})

    		ubx.Data(healthcareapis.HealthcareApisIotConnector, "example", healthcareapis.HealthcareApisIotConnectorConfig{
    			ResourceGroupName: "example-resource-group-name",
    			WorkspaceName:     "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { HealthcareApisIotConnector } from "./local-sdk/azure/sdk/typescript/azure/data/healthcareapis/healthcare_apis_iot_connector.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_healthcareapis_healthcare_apis_iot_connector",
      });

      data(HealthcareApisIotConnector, "example", {
        resourceGroupName: "example-resource-group-name",
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.healthcareapis import HealthcareApisIotConnector, HealthcareApisIotConnectorConfig

    def describe():
        ubx.intent("look up azure_healthcareapis_healthcare_apis_iot_connector")

        ubx.data(HealthcareApisIotConnector, "example", HealthcareApisIotConnectorConfig(
            resource_group_name="example-resource-group-name",
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of IoT Connectors. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of IoT Connectors. Returned by the lookup.
</ResponseField>
