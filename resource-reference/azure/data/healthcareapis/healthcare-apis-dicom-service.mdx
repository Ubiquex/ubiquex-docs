---
title: "azure_healthcareapis_healthcare_apis_dicom_service"
description: "`data_azure_healthcareapis_healthcare_apis_dicom_service` looks up a real, already-existing `azure_healthcareapis_healthcare_apis_dicom_service`."
---

It is a managed DICOM service within an Azure Health Data Services workspace, exposing a DICOMweb-compatible interface for medical imaging storage and retrieval.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	healthcareapis "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/healthcareapis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_healthcareapis_healthcare_apis_dicom_service"})

    		ubx.Data(healthcareapis.HealthcareApisDicomService, "example", healthcareapis.HealthcareApisDicomServiceConfig{
    			ResourceGroupName: "example-resource-group-name",
    			WorkspaceName:     "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { HealthcareApisDicomService } from "./local-sdk/azure/sdk/typescript/azure/data/healthcareapis/healthcare_apis_dicom_service.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_healthcareapis_healthcare_apis_dicom_service",
      });

      data(HealthcareApisDicomService, "example", {
        resourceGroupName: "example-resource-group-name",
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.healthcareapis import HealthcareApisDicomService, HealthcareApisDicomServiceConfig

    def describe():
        ubx.intent("look up azure_healthcareapis_healthcare_apis_dicom_service")

        ubx.data(HealthcareApisDicomService, "example", HealthcareApisDicomServiceConfig(
            resource_group_name="example-resource-group-name",
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of Dicom Services. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of Dicom Services. Returned by the lookup.
</ResponseField>
