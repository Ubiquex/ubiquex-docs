---
title: "azure_healthcareapis_healthcare_apis_iot_fhir_destination"
description: "`azure_healthcareapis_healthcare_apis_iot_fhir_destination` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_healthcareapis_healthcare_apis_iot_fhir_destination` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	healthcareapis "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/healthcareapis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_healthcareapis_healthcare_apis_iot_fhir_destination"})

    		ubx.Data(healthcareapis.HealthcareApisIotFhirDestination, "example", healthcareapis.HealthcareApisIotFhirDestinationConfig{
    			IotConnectorName:  "example-iot-connector-name",
    			ResourceGroupName: "example-resource-group-name",
    			WorkspaceName:     "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HealthcareApisIotFhirDestination } from "@ubx/sdk-azure/azure/data/healthcareapis/healthcare_apis_iot_fhir_destination";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_healthcareapis_healthcare_apis_iot_fhir_destination",
      });

      data(HealthcareApisIotFhirDestination, "example", {
        iotConnectorName: "example-iot-connector-name",
        resourceGroupName: "example-resource-group-name",
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.healthcareapis import HealthcareApisIotFhirDestination, HealthcareApisIotFhirDestinationConfig

    def describe():
        ubx.intent("look up azure_healthcareapis_healthcare_apis_iot_fhir_destination")

        ubx.data(HealthcareApisIotFhirDestination, "example", HealthcareApisIotFhirDestinationConfig(
            iot_connector_name="example-iot-connector-name",
            resource_group_name="example-resource-group-name",
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="iot_connector_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of IoT FHIR destinations. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of IoT Connector FHIR destinations. Returned by the lookup.
</ResponseField>
