---
title: "azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection"
description: "`data_azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection` looks up a real, already-existing..."
---

`data_azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection` looks up a real, already-existing `azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection`. It represents the connection between a Private Endpoint and Microsoft Entra ID's own Private Link resource, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	azureactivedirectory "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/azureactivedirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection"})

    		ubx.Data(azureactivedirectory.PrivateendpointconnectionsPrivateEndpointConnection, "example", azureactivedirectory.PrivateendpointconnectionsPrivateEndpointConnectionConfig{
    			ApiVersion:        "example",
    			PolicyName:        "example-policy-name",
    			ResourceGroupName: "example-resource-group-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { PrivateendpointconnectionsPrivateEndpointConnection } from "./local-sdk/azure/sdk/typescript/azure/data/azureactivedirectory/privateendpointconnections_private_endpoint_connection.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection",
      });

      data(PrivateendpointconnectionsPrivateEndpointConnection, "example", {
        apiVersion: "example",
        policyName: "example-policy-name",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.azureactivedirectory import PrivateendpointconnectionsPrivateEndpointConnection, PrivateendpointconnectionsPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("look up azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection")

        ubx.data(PrivateendpointconnectionsPrivateEndpointConnection, "example", PrivateendpointconnectionsPrivateEndpointConnectionConfig(
            api_version="example",
            policy_name="example-policy-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  The real name of the policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to next page of results Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of private link resources Returned by the lookup.
</ResponseField>
