---
title: "azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection_list_result"
description: "`azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection_list_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection_list_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	azureactivedirectory "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/azureactivedirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection_list_result"})

    		ubx.Data(azureactivedirectory.PrivateendpointconnectionsPrivateEndpointConnectionListResult, "example", azureactivedirectory.PrivateendpointconnectionsPrivateEndpointConnectionListResultConfig{
    			ApiVersion:        "example",
    			PolicyName:        "example-policy-name",
    			ResourceGroupName: "example-resource-group-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PrivateendpointconnectionsPrivateEndpointConnectionListResult } from "@ubx/sdk-azure/azure/data/azureactivedirectory/privateendpointconnections_private_endpoint_connection_list_result";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection_list_result",
      });

      data(
        PrivateendpointconnectionsPrivateEndpointConnectionListResult,
        "example",
        {
          apiVersion: "example",
          policyName: "example-policy-name",
          resourceGroupName: "example-resource-group-name",
          subscriptionId: "example",
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.azureactivedirectory import PrivateendpointconnectionsPrivateEndpointConnectionListResult, PrivateendpointconnectionsPrivateEndpointConnectionListResultConfig

    def describe():
        ubx.intent("look up azure_azureactivedirectory_privateendpointconnections_private_endpoint_connection_list_result")

        ubx.data(PrivateendpointconnectionsPrivateEndpointConnectionListResult, "example", PrivateendpointconnectionsPrivateEndpointConnectionListResultConfig(
            api_version="example",
            policy_name="example-policy-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to next page of results Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Array of private link resources Returned by the lookup.
</ResponseField>
