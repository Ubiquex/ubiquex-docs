---
title: "azure_cognitiveservices_rai_content_filter"
description: "`azure_cognitiveservices_rai_content_filter` looks up a real, custom Responsible AI content filter configured for a Cognitive Services account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cognitiveservices_rai_content_filter"})

    		ubx.Data(cognitiveservices.RaiContentFilter, "example", cognitiveservices.RaiContentFilterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RaiContentFilter } from "@ubx/sdk-azure/azure/data/cognitiveservices/rai_content_filter";

    export default stack("example", () => {
      intent({ summary: "look up azure_cognitiveservices_rai_content_filter" });

      data(RaiContentFilter, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cognitiveservices import RaiContentFilter, RaiContentFilterConfig

    def describe():
        ubx.intent("look up azure_cognitiveservices_rai_content_filter")

        ubx.data(RaiContentFilter, "example", RaiContentFilterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of Content Filters. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of RaiContentFilter. Returned by the lookup.
</ResponseField>
