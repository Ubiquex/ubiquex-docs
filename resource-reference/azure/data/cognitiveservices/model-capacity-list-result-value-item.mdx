---
title: "azure_cognitiveservices_model_capacity_list_result_value_item"
description: "`data_azure_cognitiveservices_model_capacity_list_result_value_item` is a real, live lookup against Microsoft Azure..."
---

`data_azure_cognitiveservices_model_capacity_list_result_value_item` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cognitiveservices_model_capacity_list_result_value_item"})

    		ubx.Data(cognitiveservices.ModelCapacityListResultValueItem, "example", cognitiveservices.ModelCapacityListResultValueItemConfig{
    			ModelFormat:  "example",
    			ModelName:    "example-model-name",
    			ModelVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelCapacityListResultValueItem } from "@ubx/sdk-azure/azure/data/cognitiveservices/model_capacity_list_result_value_item";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_cognitiveservices_model_capacity_list_result_value_item",
      });

      data(ModelCapacityListResultValueItem, "example", {
        modelFormat: "example",
        modelName: "example-model-name",
        modelVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cognitiveservices import ModelCapacityListResultValueItem, ModelCapacityListResultValueItemConfig

    def describe():
        ubx.intent("look up azure_cognitiveservices_model_capacity_list_result_value_item")

        ubx.data(ModelCapacityListResultValueItem, "example", ModelCapacityListResultValueItemConfig(
            model_format="example",
            model_name="example-model-name",
            model_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="model_format" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="model_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="model_version" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of ModelSkuCapacity. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Gets the list of Cognitive Services accounts ModelSkuCapacity. Returned by the lookup.
</ResponseField>
