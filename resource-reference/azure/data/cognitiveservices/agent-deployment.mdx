---
title: "azure_cognitiveservices_agent_deployment"
description: "`data_azure_cognitiveservices_agent_deployment` looks up a real, already-existing `azure_cognitiveservices_agent_deployment`."
---

It is a specific, running deployment of an Azure AI Foundry agent application, servable for real conversation traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cognitiveservices_agent_deployment"})

    		ubx.Data(cognitiveservices.AgentDeployment, "example", cognitiveservices.AgentDeploymentConfig{
    			AccountName: "example-account-name",
    			AppName:     "example-app-name",
    			ProjectName: "example-project-name",
    			Count:       1,
    			Names:       []string{"example"},
    			OrderBy:     "example",
    			OrderByAsc:  true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { AgentDeployment } from "./local-sdk/azure/sdk/typescript/azure/data/cognitiveservices/agent_deployment.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_cognitiveservices_agent_deployment" });

      data(AgentDeployment, "example", {
        accountName: "example-account-name",
        appName: "example-app-name",
        projectName: "example-project-name",
        count: 1,
        names: ["example"],
        orderBy: "example",
        orderByAsc: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.cognitiveservices import AgentDeployment, AgentDeploymentConfig

    def describe():
        ubx.intent("look up azure_cognitiveservices_agent_deployment")

        ubx.data(AgentDeployment, "example", AgentDeploymentConfig(
            account_name="example-account-name",
            app_name="example-app-name",
            project_name="example-project-name",
            count=1,
            names=["example"],
            order_by="example",
            order_by_asc=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="app_name" type="string" required>
  The real name of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="count" type="number">
  The total number of matching results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="names" type="list(string)">
  The real, given names of the objects this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  The real field results are ordered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order_by_asc" type="bool">
  Whether results are ordered ascending by the given field. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="project_name" type="string" required>
  The real name of the project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of Agent Deployment objects. If null, there are no additional pages. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  An array of objects of type Agent Deployment. Returned by the lookup.
</ResponseField>
