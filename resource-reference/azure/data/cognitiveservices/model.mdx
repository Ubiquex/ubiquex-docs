---
title: "azure_cognitiveservices_model"
description: "`azure_cognitiveservices_model` reads a real, available AI model that can be deployed on a Cognitive Services account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cognitiveservices_model"})

    		ubx.Data(cognitiveservices.Model, "example", cognitiveservices.ModelConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Model } from "@ubx/sdk-azure/azure/data/cognitiveservices/model";

    export default stack("example", () => {
      intent({ summary: "look up azure_cognitiveservices_model" });

      data(Model, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cognitiveservices import Model, ModelConfig

    def describe():
        ubx.intent("look up azure_cognitiveservices_model")

        ubx.data(Model, "example", ModelConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of Model. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Gets the list of Cognitive Services accounts Model and their properties. Returned by the lookup.
</ResponseField>
