---
title: "azure_cognitiveservices_rai_external_safety_provider_schema"
description: "`azure_cognitiveservices_rai_external_safety_provider_schema` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cognitiveservices_rai_external_safety_provider_schema` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cognitiveservices_rai_external_safety_provider_schema"})

    		ubx.Data(cognitiveservices.RaiExternalSafetyProviderSchema, "example", cognitiveservices.RaiExternalSafetyProviderSchemaConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RaiExternalSafetyProviderSchema } from "@ubx/sdk-azure/azure/data/cognitiveservices/rai_external_safety_provider_schema";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_cognitiveservices_rai_external_safety_provider_schema",
      });

      data(RaiExternalSafetyProviderSchema, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cognitiveservices import RaiExternalSafetyProviderSchema, RaiExternalSafetyProviderSchemaConfig

    def describe():
        ubx.intent("look up azure_cognitiveservices_rai_external_safety_provider_schema")

        ubx.data(RaiExternalSafetyProviderSchema, "example", RaiExternalSafetyProviderSchemaConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of Rai External Safety Provider. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of RaiExternalSafetyProvider. Returned by the lookup.
</ResponseField>
