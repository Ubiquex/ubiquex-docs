---
title: "azure_cognitiveservices_outbound_rule_basic_resource"
description: "`azure_cognitiveservices_outbound_rule_basic_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_cognitiveservices_outbound_rule_basic_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cognitiveservices_outbound_rule_basic_resource"})

    		ubx.Data(cognitiveservices.OutboundRuleBasicResource, "example", cognitiveservices.OutboundRuleBasicResourceConfig{
    			AccountName:        "example-account-name",
    			ManagedNetworkName: "example-managed-network-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OutboundRuleBasicResource } from "@ubx/sdk-azure/azure/data/cognitiveservices/outbound_rule_basic_resource";

    export default stack("example", () => {
      intent({
        summary: "look up azure_cognitiveservices_outbound_rule_basic_resource",
      });

      data(OutboundRuleBasicResource, "example", {
        accountName: "example-account-name",
        managedNetworkName: "example-managed-network-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cognitiveservices import OutboundRuleBasicResource, OutboundRuleBasicResourceConfig

    def describe():
        ubx.intent("look up azure_cognitiveservices_outbound_rule_basic_resource")

        ubx.data(OutboundRuleBasicResource, "example", OutboundRuleBasicResourceConfig(
            account_name="example-account-name",
            managed_network_name="example-managed-network-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="managed_network_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page constructed using the continuationToken. If null, there are no additional pages. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of cognitive services accounts. Since this list may be incomplete, the nextLink field should be used to request the next list of cognitive services accounts. Returned by the lookup.
</ResponseField>
