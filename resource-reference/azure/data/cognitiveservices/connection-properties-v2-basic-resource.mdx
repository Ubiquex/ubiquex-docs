---
title: "azure_cognitiveservices_connection_properties_v2_basic_resource"
description: "`data_azure_cognitiveservices_connection_properties_v2_basic_resource` looks up a real, already-existing..."
---

`data_azure_cognitiveservices_connection_properties_v2_basic_resource` looks up a real, already-existing `azure_cognitiveservices_connection_properties_v2_basic_resource`. It defines a connection from an Azure AI Foundry account to an external resource (a data source, an API key-authenticated service).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognitiveservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/cognitiveservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_cognitiveservices_connection_properties_v2_basic_resource"})

    		ubx.Data(cognitiveservices.ConnectionPropertiesV2BasicResource, "example", cognitiveservices.ConnectionPropertiesV2BasicResourceConfig{
    			AccountName: "example-account-name",
    			Category:    "example",
    			IncludeAll:  true,
    			Target:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectionPropertiesV2BasicResource } from "@ubx/sdk-azure/azure/data/cognitiveservices/connection_properties_v2_basic_resource";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_cognitiveservices_connection_properties_v2_basic_resource",
      });

      data(ConnectionPropertiesV2BasicResource, "example", {
        accountName: "example-account-name",
        category: "example",
        includeAll: true,
        target: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.cognitiveservices import ConnectionPropertiesV2BasicResource, ConnectionPropertiesV2BasicResourceConfig

    def describe():
        ubx.intent("look up azure_cognitiveservices_connection_properties_v2_basic_resource")

        ubx.data(ConnectionPropertiesV2BasicResource, "example", ConnectionPropertiesV2BasicResourceConfig(
            account_name="example-account-name",
            category="example",
            include_all=True,
            target="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="category" type="string">
  The real category this object belongs to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_all" type="bool">
  Whether every real matching item is included, rather than a filtered subset. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target" type="string">
  The real object this lookup targets. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
