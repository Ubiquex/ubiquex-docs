---
title: "azure_redisenterprise_cluster"
description: "`data_azure_redisenterprise_cluster` looks up a real Azure Cache for Redis Enterprise cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redisenterprise "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/redisenterprise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_redisenterprise_cluster"})

    		ubx.Data(redisenterprise.Cluster, "example", redisenterprise.ClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Cluster } from "@ubx/sdk-azure/azure/data/redisenterprise/cluster";

    export default stack("example", () => {
      intent({ summary: "look up azure_redisenterprise_cluster" });

      data(Cluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.redisenterprise import Cluster, ClusterConfig

    def describe():
        ubx.intent("look up azure_redisenterprise_cluster")

        ubx.data(Cluster, "example", ClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URI to fetch the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of clusters. Returned by the lookup.
</ResponseField>
