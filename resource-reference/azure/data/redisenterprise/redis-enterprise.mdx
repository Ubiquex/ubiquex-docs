---
title: "azure_redisenterprise_redis_enterprise"
description: "`azure_redisenterprise_redis_enterprise` looks up a real Azure Cache for Redis Enterprise cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	redisenterprise "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/redisenterprise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_redisenterprise_redis_enterprise"})

    		ubx.Data(redisenterprise.RedisEnterprise, "example", redisenterprise.RedisEnterpriseConfig{
    			ClusterName: "example-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { RedisEnterprise } from "./local-sdk/azure/sdk/typescript/azure/data/redisenterprise/redis_enterprise.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_redisenterprise_redis_enterprise" });

      data(RedisEnterprise, "example", {
        clusterName: "example-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.redisenterprise import RedisEnterprise, RedisEnterpriseConfig

    def describe():
        ubx.intent("look up azure_redisenterprise_redis_enterprise")

        ubx.data(RedisEnterprise, "example", RedisEnterpriseConfig(
            cluster_name="example-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  Array of private endpoint connections Returned by the lookup.
</ResponseField>
