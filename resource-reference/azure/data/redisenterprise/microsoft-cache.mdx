---
title: "azure_redisenterprise_microsoft_cache"
description: "`azure_redisenterprise_microsoft_cache` looks up a real Azure Cache for Redis Enterprise cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	redisenterprise "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/redisenterprise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_redisenterprise_microsoft_cache"})

    		ubx.Data(redisenterprise.MicrosoftCache, "example", redisenterprise.MicrosoftCacheConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { MicrosoftCache } from "./local-sdk/azure/sdk/typescript/azure/data/redisenterprise/microsoft_cache.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_redisenterprise_microsoft_cache" });

      data(MicrosoftCache, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.redisenterprise import MicrosoftCache, MicrosoftCacheConfig

    def describe():
        ubx.intent("look up azure_redisenterprise_microsoft_cache")

        ubx.data(MicrosoftCache, "example", MicrosoftCacheConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
