---
title: "azure_fluidrelay_fluid_relay_server"
description: "`azure_fluidrelay_fluid_relay_server` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_fluidrelay_fluid_relay_server` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fluidrelay "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/fluidrelay"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_fluidrelay_fluid_relay_server"})

    		ubx.Data(fluidrelay.FluidRelayServer, "example", fluidrelay.FluidRelayServerConfig{
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FluidRelayServer } from "@ubx/sdk-azure/azure/data/fluidrelay/fluid_relay_server";

    export default stack("example", () => {
      intent({ summary: "look up azure_fluidrelay_fluid_relay_server" });

      data(FluidRelayServer, "example", {
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.fluidrelay import FluidRelayServer, FluidRelayServerConfig

    def describe():
        ubx.intent("look up azure_fluidrelay_fluid_relay_server")

        ubx.data(FluidRelayServer, "example", FluidRelayServerConfig(
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  A link to the next page of results, if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  A sequence of FluidRelay servers. Returned by the lookup.
</ResponseField>
