---
title: "azure_proximity_placement_group"
description: "`data_azure_proximity_placement_group` looks up a real, already-existing `azure_proximity_placement_group`."
---

It pulls compute resources physically together in the same datacenter, the opposite goal of an availability set or zone, which deliberately spread VMs apart for resilience. It exists for workloads where network latency between tiers matters more than fault isolation -- a database tier and an application tier on different hardware types can still be pinned into the same proximity group. The group is a colocation constraint, not a capacity guarantee: it's pinned to a datacenter by whichever resource uses it first, and stays pinned until every resource in the group is stopped or deleted.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	proximity "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/proximity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_proximity_placement_group"})

    		ubx.Data(proximity.PlacementGroup, "example", proximity.PlacementGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { PlacementGroup } from "./local-sdk/azure/sdk/typescript/azure/data/proximity/placement_group.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_proximity_placement_group" });

      data(PlacementGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.proximity import PlacementGroup, PlacementGroupConfig

    def describe():
        ubx.intent("look up azure_proximity_placement_group")

        ubx.data(PlacementGroup, "example", PlacementGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URI to fetch the next page of proximity placement groups. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of proximity placement groups. Returned by the lookup.
</ResponseField>
