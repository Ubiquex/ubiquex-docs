---
title: "azure_newrelic_new_relic_observability"
description: "`azure_newrelic_new_relic_observability` looks up a real New Relic Observability resource -- the top-level integration connecting Azure telemetry to..."
---

`azure_newrelic_new_relic_observability` looks up a real New Relic Observability resource -- the top-level integration connecting Azure telemetry to New Relic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	newrelic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/newrelic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_newrelic_new_relic_observability"})

    		ubx.Data(newrelic.NewRelicObservability, "example", newrelic.NewRelicObservabilityConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NewRelicObservability } from "@ubx/sdk-azure/azure/data/newrelic/new_relic_observability";

    export default stack("example", () => {
      intent({ summary: "look up azure_newrelic_new_relic_observability" });

      data(NewRelicObservability, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.newrelic import NewRelicObservability, NewRelicObservabilityConfig

    def describe():
        ubx.intent("look up azure_newrelic_new_relic_observability")

        ubx.data(NewRelicObservability, "example", NewRelicObservabilityConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
