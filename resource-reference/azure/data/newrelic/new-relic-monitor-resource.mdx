---
title: "azure_newrelic_new_relic_monitor_resource"
description: "`data_azure_newrelic_new_relic_monitor_resource` looks up a real, already-existing `azure_newrelic_new_relic_monitor_resource`."
---

It is a New Relic resource on Azure, the top-level integration that sends Azure telemetry to a linked New Relic account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	newrelic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/newrelic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_newrelic_new_relic_monitor_resource"})

    		ubx.Data(newrelic.NewRelicMonitorResource, "example", newrelic.NewRelicMonitorResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NewRelicMonitorResource } from "@ubx/sdk-azure/azure/data/newrelic/new_relic_monitor_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_newrelic_new_relic_monitor_resource" });

      data(NewRelicMonitorResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.newrelic import NewRelicMonitorResource, NewRelicMonitorResourceConfig

    def describe():
        ubx.intent("look up azure_newrelic_new_relic_monitor_resource")

        ubx.data(NewRelicMonitorResource, "example", NewRelicMonitorResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The NewRelicMonitorResource items on this page Returned by the lookup.
</ResponseField>
