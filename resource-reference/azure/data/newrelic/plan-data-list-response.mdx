---
title: "azure_newrelic_plan_data_list_response"
description: "`azure_newrelic_plan_data_list_response` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_newrelic_plan_data_list_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	newrelic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/newrelic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_newrelic_plan_data_list_response"})

    		ubx.Data(newrelic.PlanDataListResponse, "example", newrelic.PlanDataListResponseConfig{
    			AccountId:      "example",
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PlanDataListResponse } from "@ubx/sdk-azure/azure/data/newrelic/plan_data_list_response";

    export default stack("example", () => {
      intent({ summary: "look up azure_newrelic_plan_data_list_response" });

      data(PlanDataListResponse, "example", {
        accountId: "example",
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.newrelic import PlanDataListResponse, PlanDataListResponseConfig

    def describe():
        ubx.intent("look up azure_newrelic_plan_data_list_response")

        ubx.data(PlanDataListResponse, "example", PlanDataListResponseConfig(
            account_id="example",
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PlanDataResource items on this page Returned by the lookup.
</ResponseField>
