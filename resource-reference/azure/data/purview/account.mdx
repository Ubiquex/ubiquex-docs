---
title: "azure_purview_account"
description: "`data_azure_purview_account` looks up a real, already-existing `azure_purview_account`."
---

It is a Microsoft Purview account, providing unified data governance, cataloging, and classification across an organization's own data estate.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	purview "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/purview"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_purview_account"})

    		ubx.Data(purview.Account, "example", purview.AccountConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { Account } from "./local-sdk/azure/sdk/typescript/azure/data/purview/account.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_purview_account" });

      data(Account, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.purview import Account, AccountConfig

    def describe():
        ubx.intent("look up azure_purview_account")

        ubx.data(Account, "example", AccountConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="count" type="number">
  Total item count. Returned by the lookup.
</ResponseField>

<ResponseField name="next_link" type="string">
  The Url of next result page. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Collection of items of type results. Returned by the lookup.
</ResponseField>
