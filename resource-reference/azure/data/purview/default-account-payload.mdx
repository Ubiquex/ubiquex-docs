---
title: "azure_purview_default_account_payload"
description: "`azure_purview_default_account_payload` reads which real Microsoft Purview account is set as the default for a scope, such as a subscription or tenant."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	purview "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/purview"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_purview_default_account_payload"})

    		ubx.Data(purview.DefaultAccountPayload, "example", purview.DefaultAccountPayloadConfig{
    			ApiVersion:    "example",
    			ScopeTenantId: "example",
    			ScopeType:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { DefaultAccountPayload } from "./local-sdk/azure/sdk/typescript/azure/data/purview/default_account_payload.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_purview_default_account_payload" });

      data(DefaultAccountPayload, "example", {
        apiVersion: "example",
        scopeTenantId: "example",
        scopeType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.purview import DefaultAccountPayload, DefaultAccountPayloadConfig

    def describe():
        ubx.intent("look up azure_purview_default_account_payload")

        ubx.data(DefaultAccountPayload, "example", DefaultAccountPayloadConfig(
            api_version="example",
            scope_tenant_id="example",
            scope_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scope_tenant_id" type="string" required>
  The real Microsoft Entra ID tenant that owns the scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scope_type" type="string" required>
  The real type of Azure Resource Manager scope (subscription, resource group, or resource) this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_name" type="string">
  The name of the account that is set as the default. Returned by the lookup.
</ResponseField>

<ResponseField name="resource_group_name" type="string">
  The resource group name of the account that is set as the default. Returned by the lookup.
</ResponseField>

<ResponseField name="scope" type="string">
  The real Azure Resource Manager scope (subscription, resource group, or resource) this lookup is applied at. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="subscription_id" type="string">
  The subscription ID of the account that is set as the default. Returned by the lookup.
</ResponseField>
