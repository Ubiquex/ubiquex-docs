---
title: "azure_paloaltonetworks_cloudngfw_prefix_list_resource"
description: "`data_azure_paloaltonetworks_cloudngfw_prefix_list_resource` looks up a real, already-existing `azure_paloaltonetworks_cloudngfw_prefix_list_resource`."
---

It is a reusable list of IP address prefix(es) referenced by rule(s) within a Cloud NGFW local rulestack.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	paloaltonetworks "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/paloaltonetworks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_paloaltonetworks_cloudngfw_prefix_list_resource"})

    		ubx.Data(paloaltonetworks.CloudngfwPrefixListResource, "example", paloaltonetworks.CloudngfwPrefixListResourceConfig{
    			LocalRulestackName: "example-local-rulestack-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CloudngfwPrefixListResource } from "@ubx/sdk-azure/azure/data/paloaltonetworks/cloudngfw_prefix_list_resource";

    export default stack("example", () => {
      intent({
        summary: "look up azure_paloaltonetworks_cloudngfw_prefix_list_resource",
      });

      data(CloudngfwPrefixListResource, "example", {
        localRulestackName: "example-local-rulestack-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.paloaltonetworks import CloudngfwPrefixListResource, CloudngfwPrefixListResourceConfig

    def describe():
        ubx.intent("look up azure_paloaltonetworks_cloudngfw_prefix_list_resource")

        ubx.data(CloudngfwPrefixListResource, "example", CloudngfwPrefixListResourceConfig(
            local_rulestack_name="example-local-rulestack-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="local_rulestack_name" type="string" required>
  The real name of the Palo Alto Networks Cloud NGFW local rulestack this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrefixListResource items on this page Returned by the lookup.
</ResponseField>
