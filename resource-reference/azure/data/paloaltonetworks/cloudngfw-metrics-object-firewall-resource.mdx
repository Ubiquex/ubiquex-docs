---
title: "azure_paloaltonetworks_cloudngfw_metrics_object_firewall_resource"
description: "`azure_paloaltonetworks_cloudngfw_metrics_object_firewall_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_paloaltonetworks_cloudngfw_metrics_object_firewall_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	paloaltonetworks "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/paloaltonetworks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_paloaltonetworks_cloudngfw_metrics_object_firewall_resource"})

    		ubx.Data(paloaltonetworks.CloudngfwMetricsObjectFirewallResource, "example", paloaltonetworks.CloudngfwMetricsObjectFirewallResourceConfig{
    			FirewallName: "example-firewall-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CloudngfwMetricsObjectFirewallResource } from "@ubx/sdk-azure/azure/data/paloaltonetworks/cloudngfw_metrics_object_firewall_resource";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_paloaltonetworks_cloudngfw_metrics_object_firewall_resource",
      });

      data(CloudngfwMetricsObjectFirewallResource, "example", {
        firewallName: "example-firewall-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.paloaltonetworks import CloudngfwMetricsObjectFirewallResource, CloudngfwMetricsObjectFirewallResourceConfig

    def describe():
        ubx.intent("look up azure_paloaltonetworks_cloudngfw_metrics_object_firewall_resource")

        ubx.data(CloudngfwMetricsObjectFirewallResource, "example", CloudngfwMetricsObjectFirewallResourceConfig(
            firewall_name="example-firewall-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="firewall_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The MetricsObjectFirewallResource items on this page Returned by the lookup.
</ResponseField>
