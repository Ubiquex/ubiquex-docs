---
title: "azure_compute_containerservice_container_service"
description: "`data_azure_compute_containerservice_container_service` looks up a real, already-existing `azure_compute_containerservice_container_service`."
---

It is the legacy Azure Container Service (ACS) resource, an early, now largely superseded orchestrator deployment predating Azure Kubernetes Service.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_compute_containerservice_container_service"})

    		ubx.Data(compute.ContainerserviceContainerService, "example", compute.ContainerserviceContainerServiceConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ContainerserviceContainerService } from "./local-sdk/azure/sdk/typescript/azure/data/compute/containerservice_container_service.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_compute_containerservice_container_service",
      });

      data(ContainerserviceContainerService, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.compute import ContainerserviceContainerService, ContainerserviceContainerServiceConfig

    def describe():
        ubx.intent("look up azure_compute_containerservice_container_service")

        ubx.data(ContainerserviceContainerService, "example", ContainerserviceContainerServiceConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of container service results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  the list of container services. Returned by the lookup.
</ResponseField>
