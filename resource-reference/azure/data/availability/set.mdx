---
title: "azure_availability_set"
description: "`azure_availability_set` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_availability_set` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	availability "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/availability"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_availability_set"})

    		ubx.Data(availability.Set, "example", availability.SetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Set } from "@ubx/sdk-azure/azure/data/availability/set";

    export default stack("example", () => {
      intent({ summary: "look up azure_availability_set" });

      data(Set, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.availability import Set, SetConfig

    def describe():
        ubx.intent("look up azure_availability_set")

        ubx.data(Set, "example", SetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URI to fetch the next page of AvailabilitySets. Call ListNext() with this URI to fetch the next page of AvailabilitySets. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of availability sets. Returned by the lookup.
</ResponseField>
