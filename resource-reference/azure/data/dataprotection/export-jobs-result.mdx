---
title: "azure_dataprotection_export_jobs_result"
description: "`azure_dataprotection_export_jobs_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_dataprotection_export_jobs_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dataprotection_export_jobs_result"})

    		ubx.Data(dataprotection.ExportJobsResult, "example", dataprotection.ExportJobsResultConfig{
    			OperationId: "example",
    			VaultName:   "example-vault-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExportJobsResult } from "@ubx/sdk-azure/azure/data/dataprotection/export_jobs_result";

    export default stack("example", () => {
      intent({ summary: "look up azure_dataprotection_export_jobs_result" });

      data(ExportJobsResult, "example", {
        operationId: "example",
        vaultName: "example-vault-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.dataprotection import ExportJobsResult, ExportJobsResultConfig

    def describe():
        ubx.intent("look up azure_dataprotection_export_jobs_result")

        ubx.data(ExportJobsResult, "example", ExportJobsResultConfig(
            operation_id="example",
            vault_name="example-vault-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="operation_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="blob_sas_key" type="string">
  SAS key to access the blob. Returned by the lookup.
</ResponseField>

<ResponseField name="blob_url" type="string">
  URL of the blob into which the serialized string of list of jobs is exported. Returned by the lookup.
</ResponseField>

<ResponseField name="excel_file_blob_sas_key" type="string">
  SAS key to access the ExcelFile blob. Returned by the lookup.
</ResponseField>

<ResponseField name="excel_file_blob_url" type="string">
  URL of the blob into which the ExcelFile is uploaded. Returned by the lookup.
</ResponseField>
