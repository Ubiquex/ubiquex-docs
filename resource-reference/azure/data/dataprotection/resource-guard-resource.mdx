---
title: "azure_dataprotection_resource_guard_resource"
description: "`data_azure_dataprotection_resource_guard_resource` looks up a real, already-existing `azure_dataprotection_resource_guard_resource`."
---

It is a Resource Guard, the Azure resource multi-user authorization (MUA) uses to require an independent approver -- owned by a different user than the vault admin -- before a critical Backup vault operation can proceed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dataprotection_resource_guard_resource"})

    		ubx.Data(dataprotection.ResourceGuardResource, "example", dataprotection.ResourceGuardResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceGuardResource } from "@ubx/sdk-azure/azure/data/dataprotection/resource_guard_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_dataprotection_resource_guard_resource" });

      data(ResourceGuardResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.dataprotection import ResourceGuardResource, ResourceGuardResourceConfig

    def describe():
        ubx.intent("look up azure_dataprotection_resource_guard_resource")

        ubx.data(ResourceGuardResource, "example", ResourceGuardResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  List of resources. Returned by the lookup.
</ResponseField>
