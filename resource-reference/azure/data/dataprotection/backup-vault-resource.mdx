---
title: "azure_dataprotection_backup_vault_resource"
description: "`data_azure_dataprotection_backup_vault_resource` looks up a real, already-existing `azure_dataprotection_backup_vault_resource`."
---

It is a Backup vault, the Azure Backup Data Protection service's own storage and management container for backup data and policies -- the modern, unified successor to the original Recovery Services vault model.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dataprotection_backup_vault_resource"})

    		ubx.Data(dataprotection.BackupVaultResource, "example", dataprotection.BackupVaultResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { BackupVaultResource } from "./local-sdk/azure/sdk/typescript/azure/data/dataprotection/backup_vault_resource.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_dataprotection_backup_vault_resource" });

      data(BackupVaultResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.dataprotection import BackupVaultResource, BackupVaultResourceConfig

    def describe():
        ubx.intent("look up azure_dataprotection_backup_vault_resource")

        ubx.data(BackupVaultResource, "example", BackupVaultResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  List of resources. Returned by the lookup.
</ResponseField>
