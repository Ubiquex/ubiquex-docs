---
title: "azure_dataprotection_backup_vault_resource"
description: "`data_azure_dataprotection_backup_vault_resource` looks up a real, already-existing `azure_dataprotection_backup_vault_resource`."
---

It is a Backup vault, the Azure Backup Data Protection service's own storage and management container for backup data and policies -- the modern, unified successor to the original Recovery Services vault model.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own backup vault resource"})

    		ubx.Resource(dataprotection.BackupVaultResource, "example", dataprotection.BackupVaultResourceConfig{
    			Properties: map[string]any{"bcdr_security_level": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BackupVaultResource } from "./local-sdk/azure/sdk/typescript/azure/dataprotection/backup_vault_resource.ts";

    export default stack("example", () => {
      intent({ summary: "example own backup vault resource" });

      resource(BackupVaultResource, "example", {
        properties: { bcdrSecurityLevel: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.dataprotection import BackupVaultResource, BackupVaultResourceConfig
    from ubx.azure.dataprotection.backup_vault_resource import BackupVaultResource_Properties

    def describe():
        ubx.intent("example own backup vault resource")

        ubx.resource(BackupVaultResource, "example", BackupVaultResourceConfig(
            properties=BackupVaultResource_Properties(bcdr_security_level="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dataprotection_backup_vault_resource, with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  List of resources. Returned by the lookup.
</ResponseField>
