---
title: "azure_dataprotection_dpp_base_resource"
description: "`azure_dataprotection_dpp_base_resource` looks up a real, backed-up resource tracked by a Backup vault, in the newer Data Protection backup stack."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dataprotection_dpp_base_resource"})

    		ubx.Data(dataprotection.DppBaseResource, "example", dataprotection.DppBaseResourceConfig{
    			ResourceGuardsName: "example-resource-guards-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { DppBaseResource } from "./local-sdk/azure/sdk/typescript/azure/data/dataprotection/dpp_base_resource.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_dataprotection_dpp_base_resource" });

      data(DppBaseResource, "example", {
        resourceGuardsName: "example-resource-guards-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.dataprotection import DppBaseResource, DppBaseResourceConfig

    def describe():
        ubx.intent("look up azure_dataprotection_dpp_base_resource")

        ubx.data(DppBaseResource, "example", DppBaseResourceConfig(
            resource_guards_name="example-resource-guards-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_guards_name" type="string" required>
  The real name of the Resource Guard this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DppBaseResource items on this page Returned by the lookup.
</ResponseField>
