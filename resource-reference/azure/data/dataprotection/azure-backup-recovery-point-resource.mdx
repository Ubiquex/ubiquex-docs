---
title: "azure_dataprotection_azure_backup_recovery_point_resource"
description: "`azure_dataprotection_azure_backup_recovery_point_resource` looks up one real, restorable recovery point created by a Backup vault..."
---

`azure_dataprotection_azure_backup_recovery_point_resource` looks up one real, restorable recovery point created by a Backup vault, in the newer Data Protection backup stack.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataprotection "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dataprotection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dataprotection_azure_backup_recovery_point_resource"})

    		ubx.Data(dataprotection.AzureBackupRecoveryPointResource, "example", dataprotection.AzureBackupRecoveryPointResourceConfig{
    			BackupInstanceName: "example-backup-instance-name",
    			VaultName:          "example-vault-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AzureBackupRecoveryPointResource } from "@ubx/sdk-azure/azure/data/dataprotection/azure_backup_recovery_point_resource";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_dataprotection_azure_backup_recovery_point_resource",
      });

      data(AzureBackupRecoveryPointResource, "example", {
        backupInstanceName: "example-backup-instance-name",
        vaultName: "example-vault-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.dataprotection import AzureBackupRecoveryPointResource, AzureBackupRecoveryPointResourceConfig

    def describe():
        ubx.intent("look up azure_dataprotection_azure_backup_recovery_point_resource")

        ubx.data(AzureBackupRecoveryPointResource, "example", AzureBackupRecoveryPointResourceConfig(
            backup_instance_name="example-backup-instance-name",
            vault_name="example-vault-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="backup_instance_name" type="string" required>
  The real name of the backup instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  The real name of the vault this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  List of resources. Returned by the lookup.
</ResponseField>
