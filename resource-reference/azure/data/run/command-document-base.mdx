---
title: "azure_run_command_document_base"
description: "`azure_run_command_document_base` looks up a real, available Run Command script document that can be executed against a virtual machine."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	run "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/run"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_run_command_document_base"})

    		ubx.Data(run.CommandDocumentBase, "example", run.CommandDocumentBaseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CommandDocumentBase } from "@ubx/sdk-azure/azure/data/run/command_document_base";

    export default stack("example", () => {
      intent({ summary: "look up azure_run_command_document_base" });

      data(CommandDocumentBase, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.run import CommandDocumentBase, CommandDocumentBaseConfig

    def describe():
        ubx.intent("look up azure_run_command_document_base")

        ubx.data(CommandDocumentBase, "example", CommandDocumentBaseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The uri to fetch the next page of run commands. Call ListNext() with this to fetch the next page of run commands. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of virtual machine run commands. Returned by the lookup.
</ResponseField>
