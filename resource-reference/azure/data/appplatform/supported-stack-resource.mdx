---
title: "azure_appplatform_supported_stack_resource"
description: "`azure_appplatform_supported_stack_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_appplatform_supported_stack_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appplatform "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/appplatform"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_appplatform_supported_stack_resource"})

    		ubx.Data(appplatform.SupportedStackResource, "example", appplatform.SupportedStackResourceConfig{
    			ApiVersion:        "example",
    			BuildServiceName:  "example-build-service-name",
    			ResourceGroupName: "example-resource-group-name",
    			ServiceName:       "example-service-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SupportedStackResource } from "@ubx/sdk-azure/azure/data/appplatform/supported_stack_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_appplatform_supported_stack_resource" });

      data(SupportedStackResource, "example", {
        apiVersion: "example",
        buildServiceName: "example-build-service-name",
        resourceGroupName: "example-resource-group-name",
        serviceName: "example-service-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.appplatform import SupportedStackResource, SupportedStackResourceConfig

    def describe():
        ubx.intent("look up azure_appplatform_supported_stack_resource")

        ubx.data(SupportedStackResource, "example", SupportedStackResourceConfig(
            api_version="example",
            build_service_name="example-build-service-name",
            resource_group_name="example-resource-group-name",
            service_name="example-service-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="build_service_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL client should use to fetch the next page (per server side paging). It's null for now, added for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Collection of supported stacks resources Returned by the lookup.
</ResponseField>
