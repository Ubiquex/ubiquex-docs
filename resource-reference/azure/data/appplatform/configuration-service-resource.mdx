---
title: "azure_appplatform_configuration_service_resource"
description: "`data_azure_appplatform_configuration_service_resource` looks up a real, already-existing `azure_appplatform_configuration_service_resource`."
---

It provisions Application Configuration Service, which centrally serves externalized configuration (from a linked Git repository) to applications running in Azure Spring Apps.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	appplatform "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/appplatform"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_appplatform_configuration_service_resource"})

    		ubx.Data(appplatform.ConfigurationServiceResource, "example", appplatform.ConfigurationServiceResourceConfig{
    			ApiVersion:        "example",
    			ResourceGroupName: "example-resource-group-name",
    			ServiceName:       "example-service-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ConfigurationServiceResource } from "./local-sdk/azure/sdk/typescript/azure/data/appplatform/configuration_service_resource.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_appplatform_configuration_service_resource",
      });

      data(ConfigurationServiceResource, "example", {
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        serviceName: "example-service-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.appplatform import ConfigurationServiceResource, ConfigurationServiceResourceConfig

    def describe():
        ubx.intent("look up azure_appplatform_configuration_service_resource")

        ubx.data(ConfigurationServiceResource, "example", ConfigurationServiceResourceConfig(
            api_version="example",
            resource_group_name="example-resource-group-name",
            service_name="example-service-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  The real name of the service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL client should use to fetch the next page (per server side paging). It's null for now, added for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Collection of configuration service resources Returned by the lookup.
</ResponseField>
