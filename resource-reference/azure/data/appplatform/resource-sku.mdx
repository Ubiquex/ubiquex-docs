---
title: "azure_appplatform_resource_sku"
description: "`azure_appplatform_resource_sku` reads a real, available pricing tier (SKU) for an Azure Spring Apps instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appplatform "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/appplatform"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_appplatform_resource_sku"})

    		ubx.Data(appplatform.ResourceSku, "example", appplatform.ResourceSkuConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceSku } from "@ubx/sdk-azure/azure/data/appplatform/resource_sku";

    export default stack("example", () => {
      intent({ summary: "look up azure_appplatform_resource_sku" });

      data(ResourceSku, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.appplatform import ResourceSku, ResourceSkuConfig

    def describe():
        ubx.intent("look up azure_appplatform_resource_sku")

        ubx.data(ResourceSku, "example", ResourceSkuConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL client should use to fetch the next page (per server side paging). It's null for now, added for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Collection of resource SKU Returned by the lookup.
</ResponseField>
