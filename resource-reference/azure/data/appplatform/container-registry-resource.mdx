---
title: "azure_appplatform_container_registry_resource"
description: "`data_azure_appplatform_container_registry_resource` looks up a real, already-existing `azure_appplatform_container_registry_resource`."
---

It registers a container registry (in addition to the default one) that Azure Spring Apps can pull custom container image deployments from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appplatform "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/appplatform"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_appplatform_container_registry_resource"})

    		ubx.Data(appplatform.ContainerRegistryResource, "example", appplatform.ContainerRegistryResourceConfig{
    			ApiVersion:        "example",
    			ResourceGroupName: "example-resource-group-name",
    			ServiceName:       "example-service-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContainerRegistryResource } from "@ubx/sdk-azure/azure/data/appplatform/container_registry_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_appplatform_container_registry_resource" });

      data(ContainerRegistryResource, "example", {
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        serviceName: "example-service-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.appplatform import ContainerRegistryResource, ContainerRegistryResourceConfig

    def describe():
        ubx.intent("look up azure_appplatform_container_registry_resource")

        ubx.data(ContainerRegistryResource, "example", ContainerRegistryResourceConfig(
            api_version="example",
            resource_group_name="example-resource-group-name",
            service_name="example-service-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  The real name of the service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to next page of storage list. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The container registry resources list. Returned by the lookup.
</ResponseField>
