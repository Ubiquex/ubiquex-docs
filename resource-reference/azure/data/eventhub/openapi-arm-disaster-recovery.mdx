---
title: "azure_eventhub_openapi_arm_disaster_recovery"
description: "`data_azure_eventhub_openapi_arm_disaster_recovery` looks up a real, already-existing `azure_eventhub_openapi_arm_disaster_recovery`."
---

It pairs an Event Hubs namespace with a secondary namespace in another region for geo-disaster recovery, replicating metadata (not event data) between them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventhub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/eventhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_eventhub_openapi_arm_disaster_recovery"})

    		ubx.Data(eventhub.OpenapiArmDisasterRecovery, "example", eventhub.OpenapiArmDisasterRecoveryConfig{
    			NamespaceName: "example-namespace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiArmDisasterRecovery } from "@ubx/sdk-azure/azure/data/eventhub/openapi_arm_disaster_recovery";

    export default stack("example", () => {
      intent({ summary: "look up azure_eventhub_openapi_arm_disaster_recovery" });

      data(OpenapiArmDisasterRecovery, "example", {
        namespaceName: "example-namespace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.eventhub import OpenapiArmDisasterRecovery, OpenapiArmDisasterRecoveryConfig

    def describe():
        ubx.intent("look up azure_eventhub_openapi_arm_disaster_recovery")

        ubx.data(OpenapiArmDisasterRecovery, "example", OpenapiArmDisasterRecoveryConfig(
            namespace_name="example-namespace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="namespace_name" type="string" required>
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ArmDisasterRecovery items on this page Returned by the lookup.
</ResponseField>
