---
title: "azure_eventhub_openapi_available_cluster"
description: "`azure_eventhub_openapi_available_cluster` reads whether a real, dedicated Event Hubs cluster is currently available for purchase in a region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventhub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/eventhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_eventhub_openapi_available_cluster"})

    		ubx.Data(eventhub.OpenapiAvailableCluster, "example", eventhub.OpenapiAvailableClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiAvailableCluster } from "@ubx/sdk-azure/azure/data/eventhub/openapi_available_cluster";

    export default stack("example", () => {
      intent({ summary: "look up azure_eventhub_openapi_available_cluster" });

      data(OpenapiAvailableCluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.eventhub import OpenapiAvailableCluster, OpenapiAvailableClusterConfig

    def describe():
        ubx.intent("look up azure_eventhub_openapi_available_cluster")

        ubx.data(OpenapiAvailableCluster, "example", OpenapiAvailableClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  The count of readily available and pre-provisioned Event Hubs Clusters per region. Returned by the lookup.
</ResponseField>
