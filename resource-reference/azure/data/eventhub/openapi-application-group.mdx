---
title: "azure_eventhub_openapi_application_group"
description: "`data_azure_eventhub_openapi_application_group` looks up a real, already-existing `azure_eventhub_openapi_application_group`."
---

It applies throttling and quota policy(ies) to a specific application (identified by its own client certificate or SAS key) connecting to an Event Hubs namespace.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	eventhub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/eventhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_eventhub_openapi_application_group"})

    		ubx.Data(eventhub.OpenapiApplicationGroup, "example", eventhub.OpenapiApplicationGroupConfig{
    			NamespaceName: "example-namespace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiApplicationGroup } from "./local-sdk/azure/sdk/typescript/azure/data/eventhub/openapi_application_group.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_eventhub_openapi_application_group" });

      data(OpenapiApplicationGroup, "example", {
        namespaceName: "example-namespace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.eventhub import OpenapiApplicationGroup, OpenapiApplicationGroupConfig

    def describe():
        ubx.intent("look up azure_eventhub_openapi_application_group")

        ubx.data(OpenapiApplicationGroup, "example", OpenapiApplicationGroupConfig(
            namespace_name="example-namespace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="namespace_name" type="string" required>
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ApplicationGroup items on this page Returned by the lookup.
</ResponseField>
