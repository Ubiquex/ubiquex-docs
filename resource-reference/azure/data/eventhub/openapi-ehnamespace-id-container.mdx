---
title: "azure_eventhub_openapi_ehnamespace_id_container"
description: "`azure_eventhub_openapi_ehnamespace_id_container` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_eventhub_openapi_ehnamespace_id_container` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventhub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/eventhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_eventhub_openapi_ehnamespace_id_container"})

    		ubx.Data(eventhub.OpenapiEhnamespaceIdContainer, "example", eventhub.OpenapiEhnamespaceIdContainerConfig{
    			ClusterName: "example-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiEhnamespaceIdContainer } from "@ubx/sdk-azure/azure/data/eventhub/openapi_ehnamespace_id_container";

    export default stack("example", () => {
      intent({
        summary: "look up azure_eventhub_openapi_ehnamespace_id_container",
      });

      data(OpenapiEhnamespaceIdContainer, "example", {
        clusterName: "example-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.eventhub import OpenapiEhnamespaceIdContainer, OpenapiEhnamespaceIdContainerConfig

    def describe():
        ubx.intent("look up azure_eventhub_openapi_ehnamespace_id_container")

        ubx.data(OpenapiEhnamespaceIdContainer, "example", OpenapiEhnamespaceIdContainerConfig(
            cluster_name="example-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  Result of the List Namespace IDs operation Returned by the lookup.
</ResponseField>
