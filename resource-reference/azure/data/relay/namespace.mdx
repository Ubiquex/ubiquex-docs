---
title: "azure_relay_namespace"
description: "`data_azure_relay_namespace` looks up a real, already-existing `azure_relay_namespace`."
---

It -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	relay "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/relay"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_relay_namespace"})

    		ubx.Data(relay.Namespace, "example", relay.NamespaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Namespace } from "@ubx/sdk-azure/azure/data/relay/namespace";

    export default stack("example", () => {
      intent({ summary: "look up azure_relay_namespace" });

      data(Namespace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.relay import Namespace, NamespaceConfig

    def describe():
        ubx.intent("look up azure_relay_namespace")

        ubx.data(Namespace, "example", NamespaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The RelayNamespace items on this page Returned by the lookup.
</ResponseField>
