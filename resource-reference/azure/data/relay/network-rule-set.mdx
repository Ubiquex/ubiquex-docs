---
title: "azure_relay_network_rule_set"
description: "`azure_relay_network_rule_set` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_relay_network_rule_set` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	relay "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/relay"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_relay_network_rule_set"})

    		ubx.Data(relay.NetworkRuleSet, "example", relay.NetworkRuleSetConfig{
    			NamespaceName: "example-namespace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkRuleSet } from "@ubx/sdk-azure/azure/data/relay/network_rule_set";

    export default stack("example", () => {
      intent({ summary: "look up azure_relay_network_rule_set" });

      data(NetworkRuleSet, "example", {
        namespaceName: "example-namespace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.relay import NetworkRuleSet, NetworkRuleSetConfig

    def describe():
        ubx.intent("look up azure_relay_network_rule_set")

        ubx.data(NetworkRuleSet, "example", NetworkRuleSetConfig(
            namespace_name="example-namespace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="namespace_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  NetworkRuleSet properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="default_action" type="string">
      Default Action for Network Rule Set Optional lookup argument.
    </ResponseField>
    <ResponseField name="ip_rules" type="list(object)">
      List of IpRules Optional lookup argument.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      This determines if traffic is allowed over public network. By default it is enabled. Optional lookup argument.
    </ResponseField>
    <ResponseField name="trusted_service_access_enabled" type="bool">
      Value that indicates whether Trusted Service Access is Enabled or not. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
