---
title: "azure_appconfiguration_configuration_store"
description: "`data_azure_appconfiguration_configuration_store` looks up a real, already-existing `azure_appconfiguration_configuration_store`."
---

It is an App Configuration store, the managed service for centrally storing application configuration and feature flag(s) separately from application code.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	appconfiguration "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/appconfiguration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_appconfiguration_configuration_store"})

    		ubx.Data(appconfiguration.ConfigurationStore, "example", appconfiguration.ConfigurationStoreConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ConfigurationStore } from "./local-sdk/azure/sdk/typescript/azure/data/appconfiguration/configuration_store.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_appconfiguration_configuration_store" });

      data(ConfigurationStore, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.appconfiguration import ConfigurationStore, ConfigurationStoreConfig

    def describe():
        ubx.intent("look up azure_appconfiguration_configuration_store")

        ubx.data(ConfigurationStore, "example", ConfigurationStoreConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URI that can be used to request the next set of paged results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The collection value. Returned by the lookup.
</ResponseField>
