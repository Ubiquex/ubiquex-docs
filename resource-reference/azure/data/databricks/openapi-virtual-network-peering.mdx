---
title: "azure_databricks_openapi_virtual_network_peering"
description: "`data_azure_databricks_openapi_virtual_network_peering` looks up a real, already-existing `azure_databricks_openapi_virtual_network_peering`."
---

It peers an Azure Databricks workspace's own managed virtual network with another VNet, extending connectivity between them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	databricks "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/databricks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_databricks_openapi_virtual_network_peering"})

    		ubx.Data(databricks.OpenapiVirtualNetworkPeering, "example", databricks.OpenapiVirtualNetworkPeeringConfig{
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiVirtualNetworkPeering } from "./local-sdk/azure/sdk/typescript/azure/data/databricks/openapi_virtual_network_peering.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_databricks_openapi_virtual_network_peering",
      });

      data(OpenapiVirtualNetworkPeering, "example", {
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.databricks import OpenapiVirtualNetworkPeering, OpenapiVirtualNetworkPeeringConfig

    def describe():
        ubx.intent("look up azure_databricks_openapi_virtual_network_peering")

        ubx.data(OpenapiVirtualNetworkPeering, "example", OpenapiVirtualNetworkPeeringConfig(
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualNetworkPeering items on this page Returned by the lookup.
</ResponseField>
