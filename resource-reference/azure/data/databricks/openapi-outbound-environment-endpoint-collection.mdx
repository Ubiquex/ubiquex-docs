---
title: "azure_databricks_openapi_outbound_environment_endpoint_collection"
description: "`azure_databricks_openapi_outbound_environment_endpoint_collection` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_databricks_openapi_outbound_environment_endpoint_collection` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	databricks "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/databricks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_databricks_openapi_outbound_environment_endpoint_collection"})

    		ubx.Data(databricks.OpenapiOutboundEnvironmentEndpointCollection, "example", databricks.OpenapiOutboundEnvironmentEndpointCollectionConfig{
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiOutboundEnvironmentEndpointCollection } from "@ubx/sdk-azure/azure/data/databricks/openapi_outbound_environment_endpoint_collection";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_databricks_openapi_outbound_environment_endpoint_collection",
      });

      data(OpenapiOutboundEnvironmentEndpointCollection, "example", {
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.databricks import OpenapiOutboundEnvironmentEndpointCollection, OpenapiOutboundEnvironmentEndpointCollectionConfig

    def describe():
        ubx.intent("look up azure_databricks_openapi_outbound_environment_endpoint_collection")

        ubx.data(OpenapiOutboundEnvironmentEndpointCollection, "example", OpenapiOutboundEnvironmentEndpointCollectionConfig(
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
