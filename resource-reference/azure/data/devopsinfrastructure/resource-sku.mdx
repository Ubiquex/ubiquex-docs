---
title: "azure_devopsinfrastructure_resource_sku"
description: "`azure_devopsinfrastructure_resource_sku` reads a real, available agent VM SKU for a Managed DevOps Pools resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	devopsinfrastructure "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/devopsinfrastructure"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_devopsinfrastructure_resource_sku"})

    		ubx.Data(devopsinfrastructure.ResourceSku, "example", devopsinfrastructure.ResourceSkuConfig{
    			LocationName: "example-location-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ResourceSku } from "./local-sdk/azure/sdk/typescript/azure/data/devopsinfrastructure/resource_sku.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_devopsinfrastructure_resource_sku" });

      data(ResourceSku, "example", {
        locationName: "example-location-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.devopsinfrastructure import ResourceSku, ResourceSkuConfig

    def describe():
        ubx.intent("look up azure_devopsinfrastructure_resource_sku")

        ubx.data(ResourceSku, "example", ResourceSkuConfig(
            location_name="example-location-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="location_name" type="string" required>
  The real Azure region this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ResourceSku items on this page Returned by the lookup.
</ResponseField>
