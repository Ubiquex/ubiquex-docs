---
title: "azure_codesigning_codesigningaccount_microsoft_code_signing"
description: "`azure_codesigning_codesigningaccount_microsoft_code_signing` looks up a real Trusted Signing account, Microsoft's managed code-signing service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codesigning "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/codesigning"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_codesigning_codesigningaccount_microsoft_code_signing"})

    		ubx.Data(codesigning.CodesigningaccountMicrosoftCodeSigning, "example", codesigning.CodesigningaccountMicrosoftCodeSigningConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodesigningaccountMicrosoftCodeSigning } from "@ubx/sdk-azure/azure/data/codesigning/codesigningaccount_microsoft_code_signing";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_codesigning_codesigningaccount_microsoft_code_signing",
      });

      data(CodesigningaccountMicrosoftCodeSigning, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.codesigning import CodesigningaccountMicrosoftCodeSigning, CodesigningaccountMicrosoftCodeSigningConfig

    def describe():
        ubx.intent("look up azure_codesigning_codesigningaccount_microsoft_code_signing")

        ubx.data(CodesigningaccountMicrosoftCodeSigning, "example", CodesigningaccountMicrosoftCodeSigningConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
