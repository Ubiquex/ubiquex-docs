---
title: "azure_storage_openapi_storage_account_migration"
description: "`azure_storage_openapi_storage_account_migration` reads the real, current status of a storage account's own migration between redundancy configurations."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_storage_openapi_storage_account_migration"})

    		ubx.Data(storage.OpenapiStorageAccountMigration, "example", storage.OpenapiStorageAccountMigrationConfig{
    			AccountName:   "example-account-name",
    			MigrationName: "example-migration-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiStorageAccountMigration } from "./local-sdk/azure/sdk/typescript/azure/data/storage/openapi_storage_account_migration.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_storage_openapi_storage_account_migration",
      });

      data(OpenapiStorageAccountMigration, "example", {
        accountName: "example-account-name",
        migrationName: "example-migration-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.storage import OpenapiStorageAccountMigration, OpenapiStorageAccountMigrationConfig

    def describe():
        ubx.intent("look up azure_storage_openapi_storage_account_migration")

        ubx.data(OpenapiStorageAccountMigration, "example", OpenapiStorageAccountMigrationConfig(
            account_name="example-account-name",
            migration_name="example-migration-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="migration_name" type="string" required>
  The real name of the migration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  The properties of a storage account's ongoing or enqueued migration. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="migration_failed_detailed_reason" type="string">
      Reason for migration failure Returned by the lookup.
    </ResponseField>
    <ResponseField name="migration_failed_reason" type="string">
      Error code for migration failure Returned by the lookup.
    </ResponseField>
    <ResponseField name="migration_status" type="string">
      Current status of migration Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_sku_name" type="string" required>
      The SKU name. Required for account creation; optional for update. Note that in older versions, SKU name was called accountType. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
