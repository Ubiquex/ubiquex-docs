---
title: "azure_storage_openapi_blob_inventory_policy"
description: "`data_azure_storage_openapi_blob_inventory_policy` looks up a real, already-existing `azure_storage_openapi_blob_inventory_policy`."
---

It schedules Azure Storage to generate a periodic inventory report listing every blob and its own metadata across a storage account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_storage_openapi_blob_inventory_policy"})

    		ubx.Data(storage.OpenapiBlobInventoryPolicy, "example", storage.OpenapiBlobInventoryPolicyConfig{
    			AccountName: "example-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiBlobInventoryPolicy } from "@ubx/sdk-azure/azure/data/storage/openapi_blob_inventory_policy";

    export default stack("example", () => {
      intent({ summary: "look up azure_storage_openapi_blob_inventory_policy" });

      data(OpenapiBlobInventoryPolicy, "example", {
        accountName: "example-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.storage import OpenapiBlobInventoryPolicy, OpenapiBlobInventoryPolicyConfig

    def describe():
        ubx.intent("look up azure_storage_openapi_blob_inventory_policy")

        ubx.data(OpenapiBlobInventoryPolicy, "example", OpenapiBlobInventoryPolicyConfig(
            account_name="example-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  An opaque URL from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of blob inventory policies. Returned by the lookup.
</ResponseField>
