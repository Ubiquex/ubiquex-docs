---
title: "azure_storage_openapi_sku_information"
description: "`azure_storage_openapi_sku_information` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_storage_openapi_sku_information` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_storage_openapi_sku_information"})

    		ubx.Data(storage.OpenapiSkuInformation, "example", storage.OpenapiSkuInformationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiSkuInformation } from "@ubx/sdk-azure/azure/data/storage/openapi_sku_information";

    export default stack("example", () => {
      intent({ summary: "look up azure_storage_openapi_sku_information" });

      data(OpenapiSkuInformation, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.storage import OpenapiSkuInformation, OpenapiSkuInformationConfig

    def describe():
        ubx.intent("look up azure_storage_openapi_sku_information")

        ubx.data(OpenapiSkuInformation, "example", OpenapiSkuInformationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Get the list result of storage SKUs and their properties. Returned by the lookup.
</ResponseField>
