---
title: "azure_storage_openapi_storage_task_report_instance"
description: "`azure_storage_openapi_storage_task_report_instance` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_storage_openapi_storage_task_report_instance` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_storage_openapi_storage_task_report_instance"})

    		ubx.Data(storage.OpenapiStorageTaskReportInstance, "example", storage.OpenapiStorageTaskReportInstanceConfig{
    			AccountName: "example-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiStorageTaskReportInstance } from "@ubx/sdk-azure/azure/data/storage/openapi_storage_task_report_instance";

    export default stack("example", () => {
      intent({
        summary: "look up azure_storage_openapi_storage_task_report_instance",
      });

      data(OpenapiStorageTaskReportInstance, "example", {
        accountName: "example-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.storage import OpenapiStorageTaskReportInstance, OpenapiStorageTaskReportInstanceConfig

    def describe():
        ubx.intent("look up azure_storage_openapi_storage_task_report_instance")

        ubx.data(OpenapiStorageTaskReportInstance, "example", OpenapiStorageTaskReportInstanceConfig(
            account_name="example-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The StorageTaskReportInstance items on this page Returned by the lookup.
</ResponseField>
