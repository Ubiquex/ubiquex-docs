---
title: "azure_storage_openapi_storage_task_report_instance"
description: "`azure_storage_openapi_storage_task_report_instance` reads the real result of one run of a Storage Task -- a scheduled..."
---

`azure_storage_openapi_storage_task_report_instance` reads the real result of one run of a Storage Task -- a scheduled, condition-based operation over blob data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_storage_openapi_storage_task_report_instance"})

    		ubx.Data(storage.OpenapiStorageTaskReportInstance, "example", storage.OpenapiStorageTaskReportInstanceConfig{
    			AccountName: "example-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiStorageTaskReportInstance } from "./local-sdk/azure/sdk/typescript/azure/data/storage/openapi_storage_task_report_instance.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_storage_openapi_storage_task_report_instance",
      });

      data(OpenapiStorageTaskReportInstance, "example", {
        accountName: "example-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.storage import OpenapiStorageTaskReportInstance, OpenapiStorageTaskReportInstanceConfig

    def describe():
        ubx.intent("look up azure_storage_openapi_storage_task_report_instance")

        ubx.data(OpenapiStorageTaskReportInstance, "example", OpenapiStorageTaskReportInstanceConfig(
            account_name="example-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_name" type="string" required>
  The real name of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The StorageTaskReportInstance items on this page Returned by the lookup.
</ResponseField>
