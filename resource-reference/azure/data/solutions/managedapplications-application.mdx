---
title: "azure_solutions_managedapplications_application"
description: "`azure_solutions_managedapplications_application` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_solutions_managedapplications_application` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	solutions "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/solutions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_solutions_managedapplications_application"})

    		ubx.Data(solutions.ManagedapplicationsApplication, "example", solutions.ManagedapplicationsApplicationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedapplicationsApplication } from "@ubx/sdk-azure/azure/data/solutions/managedapplications_application";

    export default stack("example", () => {
      intent({
        summary: "look up azure_solutions_managedapplications_application",
      });

      data(ManagedapplicationsApplication, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.solutions import ManagedapplicationsApplication, ManagedapplicationsApplicationConfig

    def describe():
        ubx.intent("look up azure_solutions_managedapplications_application")

        ubx.data(ManagedapplicationsApplication, "example", ManagedapplicationsApplicationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The array of managed applications. Returned by the lookup.
</ResponseField>
