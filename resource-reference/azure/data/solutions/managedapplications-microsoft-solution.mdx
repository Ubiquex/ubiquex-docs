---
title: "azure_solutions_managedapplications_microsoft_solution"
description: "`azure_solutions_managedapplications_microsoft_solution` looks up a real Azure Managed Application definition, published for others to deploy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	solutions "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/solutions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_solutions_managedapplications_microsoft_solution"})

    		ubx.Data(solutions.ManagedapplicationsMicrosoftSolution, "example", solutions.ManagedapplicationsMicrosoftSolutionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedapplicationsMicrosoftSolution } from "@ubx/sdk-azure/azure/data/solutions/managedapplications_microsoft_solution";

    export default stack("example", () => {
      intent({
        summary: "look up azure_solutions_managedapplications_microsoft_solution",
      });

      data(ManagedapplicationsMicrosoftSolution, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.solutions import ManagedapplicationsMicrosoftSolution, ManagedapplicationsMicrosoftSolutionConfig

    def describe():
        ubx.intent("look up azure_solutions_managedapplications_microsoft_solution")

        ubx.data(ManagedapplicationsMicrosoftSolution, "example", ManagedapplicationsMicrosoftSolutionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
