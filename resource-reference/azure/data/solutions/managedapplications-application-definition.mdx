---
title: "azure_solutions_managedapplications_application_definition"
description: "`data_azure_solutions_managedapplications_application_definition` looks up a real Azure Managed Application definition, published for others to deploy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	solutions "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/solutions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_solutions_managedapplications_application_definition"})

    		ubx.Data(solutions.ManagedapplicationsApplicationDefinition, "example", solutions.ManagedapplicationsApplicationDefinitionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedapplicationsApplicationDefinition } from "@ubx/sdk-azure/azure/data/solutions/managedapplications_application_definition";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_solutions_managedapplications_application_definition",
      });

      data(ManagedapplicationsApplicationDefinition, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.solutions import ManagedapplicationsApplicationDefinition, ManagedapplicationsApplicationDefinitionConfig

    def describe():
        ubx.intent("look up azure_solutions_managedapplications_application_definition")

        ubx.data(ManagedapplicationsApplicationDefinition, "example", ManagedapplicationsApplicationDefinitionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The array of managed application definitions. Returned by the lookup.
</ResponseField>
