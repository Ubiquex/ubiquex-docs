---
title: "azure_vmware_workload_network"
description: "`azure_vmware_workload_network` looks up a real NSX-T workload network within an Azure VMware Solution private cloud."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_vmware_workload_network"})

    		ubx.Data(vmware.WorkloadNetwork, "example", vmware.WorkloadNetworkConfig{
    			PrivateCloudName: "example-private-cloud-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkloadNetwork } from "@ubx/sdk-azure/azure/data/vmware/workload_network";

    export default stack("example", () => {
      intent({ summary: "look up azure_vmware_workload_network" });

      data(WorkloadNetwork, "example", {
        privateCloudName: "example-private-cloud-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.vmware import WorkloadNetwork, WorkloadNetworkConfig

    def describe():
        ubx.intent("look up azure_vmware_workload_network")

        ubx.data(WorkloadNetwork, "example", WorkloadNetworkConfig(
            private_cloud_name="example-private-cloud-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="private_cloud_name" type="string" required>
  The real name of the Azure VMware Solution private cloud this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The WorkloadNetwork items on this page Returned by the lookup.
</ResponseField>
