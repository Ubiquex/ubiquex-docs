---
title: "azure_vmware_maintenance"
description: "`data_azure_vmware_maintenance` looks up a real, already-existing `azure_vmware_maintenance`."
---

It reports scheduled maintenance event(s) affecting an Azure VMware Solution private cloud.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_vmware_maintenance"})

    		ubx.Data(vmware.Maintenance, "example", vmware.MaintenanceConfig{
    			PrivateCloudName: "example-private-cloud-name",
    			From:             "example",
    			StateName:        "example-state-name",
    			Status:           "example",
    			To:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { Maintenance } from "./local-sdk/azure/sdk/typescript/azure/data/vmware/maintenance.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_vmware_maintenance" });

      data(Maintenance, "example", {
        privateCloudName: "example-private-cloud-name",
        from: "example",
        stateName: "example-state-name",
        status: "example",
        to: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.vmware import Maintenance, MaintenanceConfig

    def describe():
        ubx.intent("look up azure_vmware_maintenance")

        ubx.data(Maintenance, "example", MaintenanceConfig(
            private_cloud_name="example-private-cloud-name",
            from_="example",
            state_name="example-state-name",
            status="example",
            to="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="from" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="private_cloud_name" type="string" required>
  The real name of the Azure VMware Solution private cloud this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="state_name" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="to" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Maintenance items on this page Returned by the lookup.
</ResponseField>
