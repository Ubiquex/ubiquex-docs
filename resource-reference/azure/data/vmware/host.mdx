---
title: "azure_vmware_host"
description: "`azure_vmware_host` looks up a real ESXi host within an Azure VMware Solution private cloud cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_vmware_host"})

    		ubx.Data(vmware.Host, "example", vmware.HostConfig{
    			ClusterName:      "example-cluster-name",
    			PrivateCloudName: "example-private-cloud-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Host } from "@ubx/sdk-azure/azure/data/vmware/host";

    export default stack("example", () => {
      intent({ summary: "look up azure_vmware_host" });

      data(Host, "example", {
        clusterName: "example-cluster-name",
        privateCloudName: "example-private-cloud-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.vmware import Host, HostConfig

    def describe():
        ubx.intent("look up azure_vmware_host")

        ubx.data(Host, "example", HostConfig(
            cluster_name="example-cluster-name",
            private_cloud_name="example-private-cloud-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="private_cloud_name" type="string" required>
  The real name of the Azure VMware Solution private cloud this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Host items on this page Returned by the lookup.
</ResponseField>
