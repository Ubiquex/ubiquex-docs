---
title: "azure_vmware_virtual_machine"
description: "`azure_vmware_virtual_machine` looks up a real virtual machine running inside an Azure VMware Solution private cloud's own vSphere environment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_vmware_virtual_machine"})

    		ubx.Data(vmware.VirtualMachine, "example", vmware.VirtualMachineConfig{
    			ClusterName:      "example-cluster-name",
    			PrivateCloudName: "example-private-cloud-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualMachine } from "@ubx/sdk-azure/azure/data/vmware/virtual_machine";

    export default stack("example", () => {
      intent({ summary: "look up azure_vmware_virtual_machine" });

      data(VirtualMachine, "example", {
        clusterName: "example-cluster-name",
        privateCloudName: "example-private-cloud-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.vmware import VirtualMachine, VirtualMachineConfig

    def describe():
        ubx.intent("look up azure_vmware_virtual_machine")

        ubx.data(VirtualMachine, "example", VirtualMachineConfig(
            cluster_name="example-cluster-name",
            private_cloud_name="example-private-cloud-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="private_cloud_name" type="string" required>
  The real name of the Azure VMware Solution private cloud this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The VirtualMachine items on this page Returned by the lookup.
</ResponseField>
