---
title: "azure_vmware_workload_network_port_mirroring"
description: "`azure_vmware_workload_network_port_mirroring` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_vmware_workload_network_port_mirroring` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_vmware_workload_network_port_mirroring"})

    		ubx.Data(vmware.WorkloadNetworkPortMirroring, "example", vmware.WorkloadNetworkPortMirroringConfig{
    			PrivateCloudName: "example-private-cloud-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkloadNetworkPortMirroring } from "@ubx/sdk-azure/azure/data/vmware/workload_network_port_mirroring";

    export default stack("example", () => {
      intent({ summary: "look up azure_vmware_workload_network_port_mirroring" });

      data(WorkloadNetworkPortMirroring, "example", {
        privateCloudName: "example-private-cloud-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.vmware import WorkloadNetworkPortMirroring, WorkloadNetworkPortMirroringConfig

    def describe():
        ubx.intent("look up azure_vmware_workload_network_port_mirroring")

        ubx.data(WorkloadNetworkPortMirroring, "example", WorkloadNetworkPortMirroringConfig(
            private_cloud_name="example-private-cloud-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="private_cloud_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The WorkloadNetworkPortMirroring items on this page Returned by the lookup.
</ResponseField>
