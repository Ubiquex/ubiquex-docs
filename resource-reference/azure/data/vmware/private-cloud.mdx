---
title: "azure_vmware_private_cloud"
description: "`data_azure_vmware_private_cloud` looks up a real, already-existing `azure_vmware_private_cloud`."
---

It is the top-level Azure VMware Solution resource -- a dedicated, VMware-based private cloud environment (vCenter, NSX-T, vSAN) running on Azure infrastructure.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_vmware_private_cloud"})

    		ubx.Data(vmware.PrivateCloud, "example", vmware.PrivateCloudConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { PrivateCloud } from "./local-sdk/azure/sdk/typescript/azure/data/vmware/private_cloud.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_vmware_private_cloud" });

      data(PrivateCloud, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.vmware import PrivateCloud, PrivateCloudConfig

    def describe():
        ubx.intent("look up azure_vmware_private_cloud")

        ubx.data(PrivateCloud, "example", PrivateCloudConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrivateCloud items on this page Returned by the lookup.
</ResponseField>
