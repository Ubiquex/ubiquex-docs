---
title: "azure_vmware_resource_sku"
description: "`azure_vmware_resource_sku` reads a real, available node SKU for provisioning an Azure VMware Solution private cloud cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_vmware_resource_sku"})

    		ubx.Data(vmware.ResourceSku, "example", vmware.ResourceSkuConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceSku } from "@ubx/sdk-azure/azure/data/vmware/resource_sku";

    export default stack("example", () => {
      intent({ summary: "look up azure_vmware_resource_sku" });

      data(ResourceSku, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.vmware import ResourceSku, ResourceSkuConfig

    def describe():
        ubx.intent("look up azure_vmware_resource_sku")

        ubx.data(ResourceSku, "example", ResourceSkuConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ResourceSku items on this page Returned by the lookup.
</ResponseField>
