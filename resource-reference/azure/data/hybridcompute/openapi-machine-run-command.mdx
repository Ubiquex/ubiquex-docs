---
title: "azure_hybridcompute_openapi_machine_run_command"
description: "`data_azure_hybridcompute_openapi_machine_run_command` looks up a real, already-existing `azure_hybridcompute_openapi_machine_run_command`."
---

It runs a one-time script on an Arc-enabled server via the Azure Arc connected machine agent, without needing direct network access or a pre-installed remote-execution tool.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	hybridcompute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/hybridcompute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_hybridcompute_openapi_machine_run_command"})

    		ubx.Data(hybridcompute.OpenapiMachineRunCommand, "example", hybridcompute.OpenapiMachineRunCommandConfig{
    			MachineName: "example-machine-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiMachineRunCommand } from "./local-sdk/azure/sdk/typescript/azure/data/hybridcompute/openapi_machine_run_command.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_hybridcompute_openapi_machine_run_command",
      });

      data(OpenapiMachineRunCommand, "example", {
        machineName: "example-machine-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.hybridcompute import OpenapiMachineRunCommand, OpenapiMachineRunCommandConfig

    def describe():
        ubx.intent("look up azure_hybridcompute_openapi_machine_run_command")

        ubx.data(OpenapiMachineRunCommand, "example", OpenapiMachineRunCommandConfig(
            machine_name="example-machine-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="machine_name" type="string" required>
  The real name of the machine this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The MachineRunCommand items on this page Returned by the lookup.
</ResponseField>
